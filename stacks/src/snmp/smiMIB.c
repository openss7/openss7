/*****************************************************************************

 @(#) $RCSfile: smiMIB.c,v $ $Name: OpenSS7-0_9_2 $($Revision: 0.9.2.1 $) $Date: 2009-01-04 13:16:00 $

 -----------------------------------------------------------------------------

 Copyright (c) 2008-2009  Monavacom Limited <http://www.monavacom.com/>
 Copyright (c) 2001-2008  OpenSS7 Corporation <http://www.openss7.com/>
 Copyright (c) 1997-2001  Brian F. G. Bidulock <bidulock@openss7.org>

 All Rights Reserved.

 This program is free software: you can redistribute it and/or modify it under
 the terms of the GNU Affero General Public License as published by the Free
 Software Foundation, version 3 of the license.

 This program is distributed in the hope that it will be useful, but WITHOUT
 ANY WARRANTY; without even the implied warranty of MERCHANTABILITY or FITNESS
 FOR A PARTICULAR PURPOSE.  See the GNU Affero General Public License for more
 details.

 You should have received a copy of the GNU Affero General Public License
 along with this program.  If not, see <http://www.gnu.org/licenses/>, or
 write to the Free Software Foundation, Inc., 675 Mass Ave, Cambridge, MA
 02139, USA.

 -----------------------------------------------------------------------------

 U.S. GOVERNMENT RESTRICTED RIGHTS.  If you are licensing this Software on
 behalf of the U.S. Government ("Government"), the following provisions apply
 to you.  If the Software is supplied by the Department of Defense ("DoD"), it
 is classified as "Commercial Computer Software" under paragraph 252.227-7014
 of the DoD Supplement to the Federal Acquisition Regulations ("DFARS") (or any
 successor regulations) and the Government is acquiring only the license rights
 granted herein (the license rights customarily provided to non-Government
 users).  If the Software is supplied to any unit or agency of the Government
 other than DoD, it is classified as "Restricted Computer Software" and the
 Government's rights in the Software are defined in paragraph 52.227-19 of the
 Federal Acquisition Regulations ("FAR") (or any successor regulations) or, in
 the cases of NASA, in paragraph 18.52.227-86 of the NASA Supplement to the FAR
 (or any successor regulations).

 -----------------------------------------------------------------------------

 Commercial licensing and support of this software is available from OpenSS7
 Corporation at a fee.  See http://www.openss7.com/

 -----------------------------------------------------------------------------

 Last Modified $Date: 2009-01-04 13:16:00 $ by $Author: brian $

 -----------------------------------------------------------------------------

 $Log: smiMIB.c,v $
 Revision 0.9.2.1  2009-01-04 13:16:00  brian
 - updated agents

 *****************************************************************************/

#ident "@(#) $RCSfile: smiMIB.c,v $ $Name: OpenSS7-0_9_2 $($Revision: 0.9.2.1 $) $Date: 2009-01-04 13:16:00 $"

static char const ident[] = "$RCSfile: smiMIB.c,v $ $Name: OpenSS7-0_9_2 $($Revision: 0.9.2.1 $) $Date: 2009-01-04 13:16:00 $";

/* This file was generated by mib2c and is intended for use as
   a mib module for the ucd-snmp snmpd agent. */
#include <ucd-snmp/ucd-snmp-config.h>
#include <ucd-snmp/ucd-snmp-includes.h>
#include <ucd-snmp/ucd-snmp-agent-includes.h>
#include <ucd-snmp/callback.h>
#include <ucd-snmp/snmp-tc.h>
#include <ucd-snmp/default_store.h>
#include <ucd-snmp/snmp_alarm.h>
/* The following header files are mangled in most recent net-snmp releases so
 * the versions from UCD-SNMP 4.2.5 are included here.  */
#if defined HAVE_LIBNETSNMP
#else				/* defined HAVE_LIBNETSNMP */
#endif				/* defined HAVE_LIBNETSNMP */
/* These are messed up on both. */
#include "ds_agent.h"
#ifdef HAVE_UCD_SNMP_UTIL_FUNCS_H
#include <ucd-snmp/util_funcs.h>
/* Many recent net-snmp UCD compatible headers do not declare header_generic. */
int header_generic(struct variable *, oid *, size_t *, int, size_t *, WriteMethod **);
#else				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#include "util_funcs.h"
#endif				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#ifdef HAVE_UCD_SNMP_HEADER_COMPLEX_H
#include <ucd-snmp/header_complex.h>
#else				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
#include "header_complex.h"
#endif				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
/* This one is the other way around: it is just fine for net-snmp, but
 * ucd-snmp does not provide the header file at all.  */
#ifdef HAVE_UCD_SNMP_MIB_MODULES_H
#include <ucd-snmp/mib_modules.h>
#else				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#ifdef HAVE_NET_SNMP_AGENT_MIB_MODULES_H
#include <net-snmp/agent/mib_modules.h>
#else				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#include "mib_modules.h"
#endif				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#endif				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#include <stdint.h>
#include <signal.h>
#include <sys/stat.h>		/* for struct stat, fstat() */
#include <sys/types.h>
#include <unistd.h>
#include <stdlib.h>
#include <pwd.h>		/* for getpwuid() getpwnam() */
#include <grp.h>		/* for getgrgid() getgrnam() */
#include <libgen.h>		/* for basename() */
#include <fcntl.h>		/* for O_CREAT */
#include <sys/sysctl.h>		/* for sysctl */
#include <errno.h>
#include <string.h>
#ifdef _GNU_SOURCE
#include <getopt.h>
#endif
#include "smiMIB.h"
extern const char sa_program[];

#define MY_FACILITY(__pri)	(LOG_DAEMON|(__pri))
#if !defined MODULE
extern int sa_dump;			/* default packet dump */
extern int sa_debug;			/* default no debug */
extern int sa_nomead;			/* default daemon mode */
extern int sa_output;			/* default normal output */
extern int sa_agentx;			/* default agentx mode */
extern int sa_alarms;			/* default application alarms */
extern int sa_fclose;			/* default close files between requests */
extern int sa_logaddr;			/* log addresses */
extern int sa_logfillog;		/* log to sa_logfile */
extern int sa_logstderr;		/* log to standard error */
extern int sa_logstdout;		/* log to standard output */
extern int sa_logsyslog;		/* log to system logs */
extern int sa_logcallog;		/* log to callback logs */
extern int sa_appendlog;		/* append to log file without truncating */
extern char sa_logfile[256];
extern char sa_pidfile[256];
extern char sa_sysctlf[256];

/* file stream for log file */
extern FILE *stdlog;

/* file descriptor for MIB use */
extern int sa_fd;

/* indication to reread MIB configuration */
extern int sa_changed;

/* indications that statistics, the mib or its tables need to be refreshed */
extern int sa_stats_refresh;
#endif				/* !defined MODULE */
/* request number for per-request actions */
extern int sa_request;
volatile int smiMIB_refresh = 1;
volatile int discriminatorTable_refresh = 1;
volatile int eventForwardingDiscriminatorTable_refresh = 1;
volatile int logTable_refresh = 1;
volatile int logRecordTable_refresh = 1;
volatile int eventLogRecordTable_refresh = 1;
volatile int alarmRecordTable_refresh = 1;
volatile int attributeValueChangeRecordTable_refresh = 1;
volatile int objectCreationRecordTable_refresh = 1;
volatile int objectDeletionRecordTable_refresh = 1;
volatile int relationshipChangeRecordTable_refresh = 1;
volatile int securityAlarmReportRecordTable_refresh = 1;
volatile int stateChangeRecordTable_refresh = 1;

/*
 * smiMIB_variables_oid: object identifier for smiMIB
 * This is the top level oid that we want to register under.  This is essentially a prefix, with the
 * suffix appearing in the variable below.
 */
oid smiMIB_variables_oid[] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2 };
oid discriminatorTable_variables_oid[] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 2, 1, 1, 1 };
oid eventForwardingDiscriminatorTable_variables_oid[] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 2, 2, 1, 1 };
oid logTable_variables_oid[] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 2, 3, 1, 1 };
oid logRecordTable_variables_oid[] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 2, 4, 1, 1 };

/*
 * Oids for use in notifications defined in this MIB.
 */
oid attributeValueChange_oid[11] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 0 };
oid communicationsAlarm_oid[11] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 0 };
oid environmentalAlarm_oid[11] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 0 };
oid equipmentAlarm_oid[11] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 0 };
oid integrityViolation_oid[11] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 0 };
oid objectCreation_oid[11] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 0 };
oid objectDeletion_oid[11] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 0 };
oid operationalViolation_oid[11] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 0 };
oid physicalViolation_oid[11] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 0 };
oid processingErrorAlarm_oid[11] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 0 };
oid qualityofServiceAlarm_oid[11] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 0 };
oid relationshipChange_oid[11] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 0 };
oid securityServiceOrMechanismViolation_oid[11] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 0 };
oid stateChange_oid[11] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 0 };
oid timeDomainViolation_oid[11] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 0 };
oid communicationsInformation_oid[11] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 0 };

/*
 * Oids accessible only for notify defined in this MIB.
 */
oid tmnObjectInstance_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 3 };
oid tmnNotificationIdentifier_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 3 };
oid tmnCorrelatedNotifications_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 3 };
oid tmnProbableCause_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 3 };
oid tmnSpecificProblems_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 3 };
oid tmnPerceivedSeverity_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 3 };
oid tmnBackedUpStatus_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 3 };
oid tmnBackUpObject_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 3 };
oid tmnTrendIndication_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 3 };
oid tmnThresholdInfo_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 3 };
oid tmnStateChangeDefinition_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 3 };
oid tmnMonitoredAttributes_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 3 };
oid tmnProposedRepairActions_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 3 };
oid tmnSourceIndicator_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 3 };
oid tmnAttributeIdentifierList_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 3 };
oid tmnAttributeValueChangeDefinition_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 3 };
oid tmnAdditionalText_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 3 };
oid tmnAdditionalInformation_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 3 };
oid tmnSecurityAlarmCause_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 3 };
oid tmnSecurityAlarmSeverity_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 3 };
oid tmnSecurityAlarmDetector_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 3 };
oid tmnServiceUser_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 3 };
oid tmnServiceProvider_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 3 };
oid tmnAttributeList_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 3 };
oid tmnRelationshipChangeDefinition_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 3 };
oid tmnInformationType_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 3 };
oid tmnInformationData_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 3 };

/*
 * Other oids defined in this MIB.
 */
oid attributeValueChangeType_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 0, 0 };
oid communicationsAlarmType_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 0, 0 };
oid environmentalAlarmType_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 0, 0 };
oid equipmentAlarmType_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 0, 0 };
oid integrityViolationType_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 0, 0 };
oid objectCreationType_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 0, 0 };
oid objectDeletionType_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 0, 0 };
oid operationalViolationType_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 0, 0 };
oid physicalViolationType_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 0, 0 };
oid processingErrorAlarmType_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 0, 0 };
oid qualityofServiceAlarmType_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 0, 0 };
oid relationshipChangeType_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 0, 0 };
oid securityServiceOrMechanismViolationType_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 0, 0 };
oid stateChangeType_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 0, 0 };
oid timeDomainViolationType_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 0, 0 };
oid communicationsInformationType_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 0, 0 };
oid adapterError_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 1 };
oid applicationSubsystemFailure_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 1 };
oid bandwidthReduced_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 1 };
oid callEstablishmentError_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 1 };
oid communicationsProtocolError_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 1 };
oid communicationsSubsystemFailure_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 1 };
oid configurationOrCustomizationError_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 1 };
oid congestion_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 1 };
oid corruptData_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 1 };
oid cpuCyclesLimitExceeded_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 1 };
oid dataSetOrModemError_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 1 };
oid degradedSignal_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 1 };
oid dTE_DCEInterfaceError_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 1 };
oid enclosureDoorOpen_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 1 };
oid equipmentMalfunction_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 1 };
oid excessiveVibration_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 1 };
oid fileError_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 1 };
oid fireDetected_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 1 };
oid floodDetected_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 1 };
oid framingError_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 1 };
oid heatingOrVentilationOrCoolingSystemProblem_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 1 };
oid humidityUnacceptable_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 1 };
oid inputOutputDeviceError_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 1 };
oid inputDeviceError_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 1 };
oid lANError_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 1 };
oid leakDetected_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 1 };
oid localNodeTransmissionError_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 1 };
oid lossOfFrame_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 1 };
oid lossOfSignal_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 1 };
oid materialSupplyExhausted_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 1 };
oid multiplexerProblem_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 1 };
oid outOfMemory_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 1 };
oid ouputDeviceError_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 1 };
oid performanceDegraded_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 1 };
oid powerProblem_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 1 };
oid pressureUnacceptable_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 1 };
oid processorProblem_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 1 };
oid pumpFailure_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 1 };
oid queueSizeExceeded_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 1 };
oid receiveFailure_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 1 };
oid receiverFailure_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 1 };
oid remoteNodeTransmissionError_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 1 };
oid resourceAtOrNearingCapacity_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 1 };
oid responseTimeExcessive_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 1 };
oid retransmissionRateExcessive_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 1 };
oid softwareError_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 1 };
oid softwareProgramAbnormallyTerminated_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 1 };
oid softwareProgramError_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 1 };
oid storageCapacityProblem_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 1 };
oid temperatureUnacceptable_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 1 };
oid thresholdCrossed_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 1 };
oid timingProblem_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 1 };
oid toxicLeakDetected_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 1 };
oid transmitFailure_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 1 };
oid transmitterFailure_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 1 };
oid underlyingResourceUnavailable_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 1 };
oid versionMismatch_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 1 };
oid authenticationFailure_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 2 };
oid breachOfConfidentiality_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 2 };
oid cableTamper_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 2 };
oid delayedInformation_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 2 };
oid denialOfService_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 2 };
oid duplicateInformation_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 2 };
oid informationMissing_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 2 };
oid informationModificationDetected_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 2 };
oid informationOutOfSequence_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 2 };
oid intrusionDetection_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 2 };
oid keyExpired_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 2 };
oid nonRepudiationFailure_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 2 };
oid outOfHoursActivity_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 2 };
oid outOfService_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 2 };
oid proceduralError_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 2 };
oid unauthorizedAccessAttempt_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 2 };
oid unexpectedInformation_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 2 };
oid unspecifiedReason_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 2 };
oid noActionRequired_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 3 };
oid repairActionRequired_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 3 };
oid aIS_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid callSetUpFailure_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid degradedSignalGnm_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid farEndReceiverFailure_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid framingErrorGnm_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid lossOfFrameGnm_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid lossOfPointer_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid lossOfSignalGnm_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid payloadTypeMismatch_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid transmissionError_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid remoteAlarmInterface_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid excessiveBER_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid pathTraceMismatch_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid unavailable_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid signalLabelMismatch_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid lossOfMultiFrame_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid backplaneFailure_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid dataSetProblem_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid equipmentIdentifierDuplication_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid externalIFDeviceProblem_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid lineCardProblem_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid multiplexerProblemGnm_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid nEIdentifierDuplication_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid powerProblemGnm_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid processorProblemGnm_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid protectionPathFailure_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid receiverFailureGnm_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid replaceableUnitMissing_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid replaceableUnitTypeMismatch_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid synchronizationSourceMismatch_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid terminalProblem_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid timingProblemGnm_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid transmitterFailureGnm_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid trunkCardProblem_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid replaceableUnitProblem_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid airCompressorFailure_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid airConditioningFailure_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid airDryerFailure_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid batteryDischarging_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid batteryFailure_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid commercialPowerFailure_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid coolingFanFailure_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid engineFailure_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid fireDetectorFailure_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid fuseFailure_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid generatorFailure_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid lowBatteryThreshold_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid pumpFailureGnm_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid rectifierFailure_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid rectifierHighVoltage_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid rectifierLowVoltage_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid ventilationsSystemFailure_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid enclosureDoorOpenGnm_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid explosiveGas_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid fire_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid flood_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid highHumidity_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid highTemperature_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid highWind_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid iceBuildUp_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid intrusionDetectionGnm_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid lowFuel_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid lowHumidity_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid lowCablePressure_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid lowTemperature_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid lowWater_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid smoke_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid toxicGas_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid storageCapacityProblemGnm_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid memoryMismatchGnm_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid corruptDataGnm_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid outOfCPUCycles_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid sfwrEnvironmentProblem_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
oid sfwrDownloadFailure_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 2, 1, 4, 4 };
static const oid zeroDotZero_oid[2] = { 0, 0 };
static const oid snmpTrapOID_oid[11] = { 1, 3, 6, 1, 6, 3, 1, 1, 4, 1, 0 };

/*
 * variable7 smiMIB_variables: tree for smiMIB
 * This variable defines function callbacks and type return information for the smiMIB mib section
 */
struct variable7 smiMIB_variables[] = {
	/* magic number, variable type, ro/rw, callback fn, L, oidsuffix */
#define   DISCRIMINATORCONSTRUCT  (4 % 256)
	{DISCRIMINATORCONSTRUCT, ASN_OCTET_STR, RWRITE, var_discriminatorTable, 6, {1, 2, 1, 1, 1, 2}},
#define   DISCRIMINATORADMINISTRATIVESTATE  (5 % 256)
	{DISCRIMINATORADMINISTRATIVESTATE, ASN_INTEGER, RWRITE, var_discriminatorTable, 6, {1, 2, 1, 1, 1, 3}},
#define   DISCRIMINATOROPERATIONALSTATE  (6 % 256)
	{DISCRIMINATOROPERATIONALSTATE, ASN_INTEGER, RONLY, var_discriminatorTable, 6, {1, 2, 1, 1, 1, 4}},
#define   DISCRIMINATORAVAILABILITYSTATUS  (7 % 256)
	{DISCRIMINATORAVAILABILITYSTATUS, ASN_BIT_STR, RONLY, var_discriminatorTable, 6, {1, 2, 1, 1, 1, 5}},
#define   DISCRIMINATORSTARTTIME  (8 % 256)
	{DISCRIMINATORSTARTTIME, ASN_TIMETICKS, RWRITE, var_discriminatorTable, 6, {1, 2, 1, 1, 1, 6}},
#define   DISCRIMINATORSTOPTIME  (9 % 256)
	{DISCRIMINATORSTOPTIME, ASN_TIMETICKS, RWRITE, var_discriminatorTable, 6, {1, 2, 1, 1, 1, 7}},
#define   DISCRIMINATORINTERVALSOFDAY  (10 % 256)
	{DISCRIMINATORINTERVALSOFDAY, ASN_OCTET_STR, RWRITE, var_discriminatorTable, 6, {1, 2, 1, 1, 1, 8}},
#define   DISCRIMINATORWEEKMASK  (11 % 256)
	{DISCRIMINATORWEEKMASK, ASN_OCTET_STR, RWRITE, var_discriminatorTable, 6, {1, 2, 1, 1, 1, 9}},
#define   DISCRIMINATORSCHEDULERNAME  (12 % 256)
	{DISCRIMINATORSCHEDULERNAME, ASN_OBJECT_ID, RWRITE, var_discriminatorTable, 6, {1, 2, 1, 1, 1, 10}},
#define   DISCRIMINATORENTRYSTATUS  (13 % 256)
	{DISCRIMINATORENTRYSTATUS, ASN_INTEGER, RWRITE, var_discriminatorTable, 6, {1, 2, 1, 1, 1, 11}},
#define   DESTINATION           (17 % 256)
	{DESTINATION, ASN_OCTET_STR, RWRITE, var_eventForwardingDiscriminatorTable, 6, {1, 2, 2, 1, 1, 1}},
#define   ACTIVEDESTINATION     (18 % 256)
	{ACTIVEDESTINATION, ASN_OCTET_STR, RWRITE, var_eventForwardingDiscriminatorTable, 6, {1, 2, 2, 1, 1, 2}},
#define   BACKUPDESTINATIONLIST  (19 % 256)
	{BACKUPDESTINATIONLIST, ASN_OCTET_STR, RWRITE, var_eventForwardingDiscriminatorTable, 6, {1, 2, 2, 1, 1, 3}},
#define   CONFIRMEDMODE         (20 % 256)
	{CONFIRMEDMODE, ASN_INTEGER, RWRITE, var_eventForwardingDiscriminatorTable, 6, {1, 2, 2, 1, 1, 4}},
#define   EVENTFORWARDINGDISCRIMINATORENTRYSTATUS  (21 % 256)
	{EVENTFORWARDINGDISCRIMINATORENTRYSTATUS, ASN_INTEGER, RWRITE, var_eventForwardingDiscriminatorTable, 6, {1, 2, 2, 1, 1, 5}},
#define   LOGDISCRIMINATORCONSTRUCT  (25 % 256)
	{LOGDISCRIMINATORCONSTRUCT, ASN_OCTET_STR, RWRITE, var_logTable, 6, {1, 2, 3, 1, 1, 2}},
#define   LOGADMINISTRATIVESTATE  (26 % 256)
	{LOGADMINISTRATIVESTATE, ASN_INTEGER, RWRITE, var_logTable, 6, {1, 2, 3, 1, 1, 3}},
#define   LOGOPERATIONALSTATE   (27 % 256)
	{LOGOPERATIONALSTATE, ASN_INTEGER, RONLY, var_logTable, 6, {1, 2, 3, 1, 1, 4}},
#define   LOGAVAILABILITYSTATUS  (28 % 256)
	{LOGAVAILABILITYSTATUS, ASN_BIT_STR, RWRITE, var_logTable, 6, {1, 2, 3, 1, 1, 5}},
#define   LOGFULLACTION         (29 % 256)
	{LOGFULLACTION, ASN_INTEGER, RWRITE, var_logTable, 6, {1, 2, 3, 1, 1, 6}},
#define   MAXLOGSIZE            (30 % 256)
	{MAXLOGSIZE, ASN_UNSIGNED, RWRITE, var_logTable, 6, {1, 2, 3, 1, 1, 7}},
#define   CURRENTLOGSIZE        (31 % 256)
	{CURRENTLOGSIZE, ASN_GAUGE, RONLY, var_logTable, 6, {1, 2, 3, 1, 1, 8}},
#define   NUMBEROFRECORDS       (32 % 256)
	{NUMBEROFRECORDS, ASN_GAUGE, RONLY, var_logTable, 6, {1, 2, 3, 1, 1, 9}},
#define   CAPACITYALARMTHRESHOLD  (33 % 256)
	{CAPACITYALARMTHRESHOLD, ASN_OCTET_STR, RWRITE, var_logTable, 6, {1, 2, 3, 1, 1, 10}},
#define   LOGSTARTTIME          (34 % 256)
	{LOGSTARTTIME, ASN_TIMETICKS, RWRITE, var_logTable, 6, {1, 2, 3, 1, 1, 11}},
#define   LOGSTOPTIME           (35 % 256)
	{LOGSTOPTIME, ASN_TIMETICKS, RWRITE, var_logTable, 6, {1, 2, 3, 1, 1, 12}},
#define   LOGINTERVALSOFDAY     (36 % 256)
	{LOGINTERVALSOFDAY, ASN_OCTET_STR, RWRITE, var_logTable, 6, {1, 2, 3, 1, 1, 13}},
#define   LOGWEEKMASK           (37 % 256)
	{LOGWEEKMASK, ASN_OCTET_STR, RWRITE, var_logTable, 6, {1, 2, 3, 1, 1, 14}},
#define   LOGSCHEDULERNAME      (38 % 256)
	{LOGSCHEDULERNAME, ASN_OBJECT_ID, RWRITE, var_logTable, 6, {1, 2, 3, 1, 1, 15}},
#define   LOGENTRYSTATUS        (39 % 256)
	{LOGENTRYSTATUS, ASN_INTEGER, RWRITE, var_logTable, 6, {1, 2, 3, 1, 1, 16}},
#define   LOGGINGTIME           (44 % 256)
	{LOGGINGTIME, ASN_TIMETICKS, RONLY, var_logRecordTable, 6, {1, 2, 4, 1, 1, 2}},
#define   LOGRECORDENTRYSTATUS  (45 % 256)
	{LOGRECORDENTRYSTATUS, ASN_INTEGER, RWRITE, var_logRecordTable, 6, {1, 2, 4, 1, 1, 3}},
#define   MANAGEDOBJECTCLASS    (50 % 256)
	{MANAGEDOBJECTCLASS, ASN_OBJECT_ID, RONLY, var_eventLogRecordTable, 6, {1, 2, 5, 1, 1, 1}},
#define   MANAGEDOBJECTINSTANCE  (51 % 256)
	{MANAGEDOBJECTINSTANCE, ASN_OBJECT_ID, RONLY, var_eventLogRecordTable, 6, {1, 2, 5, 1, 1, 2}},
#define   EVENTTYPE             (52 % 256)
	{EVENTTYPE, ASN_OBJECT_ID, RONLY, var_eventLogRecordTable, 6, {1, 2, 5, 1, 1, 3}},
#define   EVENTTIMES            (53 % 256)
	{EVENTTIMES, ASN_TIMETICKS, RONLY, var_eventLogRecordTable, 6, {1, 2, 5, 1, 1, 4}},
#define   EVENTNOTIFICATIONIDENTIFIER  (54 % 256)
	{EVENTNOTIFICATIONIDENTIFIER, ASN_INTEGER, RONLY, var_eventLogRecordTable, 6, {1, 2, 5, 1, 1, 5}},
#define   EVENTCORRELATEDNOTIFICATIONS  (55 % 256)
	{EVENTCORRELATEDNOTIFICATIONS, ASN_OCTET_STR, RONLY, var_eventLogRecordTable, 6, {1, 2, 5, 1, 1, 6}},
#define   EVENTADDITIONALTEXT   (56 % 256)
	{EVENTADDITIONALTEXT, ASN_OCTET_STR, RONLY, var_eventLogRecordTable, 6, {1, 2, 5, 1, 1, 7}},
#define   EVENTADDITIONALINFORMATION  (57 % 256)
	{EVENTADDITIONALINFORMATION, ASN_OCTET_STR, RONLY, var_eventLogRecordTable, 6, {1, 2, 5, 1, 1, 8}},
#define   ALARMRECORDPROBABLECAUSE  (62 % 256)
	{ALARMRECORDPROBABLECAUSE, ASN_OBJECT_ID, RONLY, var_alarmRecordTable, 6, {1, 2, 6, 1, 1, 1}},
#define   ALARMRECORDPERCEIVEDSEVERITY  (63 % 256)
	{ALARMRECORDPERCEIVEDSEVERITY, ASN_INTEGER, RONLY, var_alarmRecordTable, 6, {1, 2, 6, 1, 1, 2}},
#define   ALARMRECORDSPECIFICPROBLEMS  (64 % 256)
	{ALARMRECORDSPECIFICPROBLEMS, ASN_OBJECT_ID, RONLY, var_alarmRecordTable, 6, {1, 2, 6, 1, 1, 3}},
#define   ALARMRECORDBACKEDUPSTATUS  (65 % 256)
	{ALARMRECORDBACKEDUPSTATUS, ASN_INTEGER, RONLY, var_alarmRecordTable, 6, {1, 2, 6, 1, 1, 4}},
#define   ALARMRECORDBACKUPOBJECT  (66 % 256)
	{ALARMRECORDBACKUPOBJECT, ASN_OBJECT_ID, RONLY, var_alarmRecordTable, 6, {1, 2, 6, 1, 1, 5}},
#define   ALARMRECORDTRENDINDICATION  (67 % 256)
	{ALARMRECORDTRENDINDICATION, ASN_INTEGER, RONLY, var_alarmRecordTable, 6, {1, 2, 6, 1, 1, 6}},
#define   ALARMRECORDTHRESHOLDINFO  (68 % 256)
	{ALARMRECORDTHRESHOLDINFO, ASN_OCTET_STR, RONLY, var_alarmRecordTable, 6, {1, 2, 6, 1, 1, 7}},
#define   ALARMRECORDSTATECHANGEDEFINITION  (69 % 256)
	{ALARMRECORDSTATECHANGEDEFINITION, ASN_OCTET_STR, RONLY, var_alarmRecordTable, 6, {1, 2, 6, 1, 1, 8}},
#define   ALARMRECORDMONITOREDATTRIBUTES  (70 % 256)
	{ALARMRECORDMONITOREDATTRIBUTES, ASN_OCTET_STR, RONLY, var_alarmRecordTable, 6, {1, 2, 6, 1, 1, 9}},
#define   ALARMRECORDPROPOSEDREPAIRACTIONS  (71 % 256)
	{ALARMRECORDPROPOSEDREPAIRACTIONS, ASN_OBJECT_ID, RONLY, var_alarmRecordTable, 6, {1, 2, 6, 1, 1, 10}},
#define   ATTRIBUTEVALUECHANGERECORDDEFINITION  (76 % 256)
	{ATTRIBUTEVALUECHANGERECORDDEFINITION, ASN_OCTET_STR, RONLY, var_attributeValueChangeRecordTable, 6, {1, 2, 7, 1, 1, 1}},
#define   ATTRIBUTEVALUECHANGESOURCEINDICATOR  (77 % 256)
	{ATTRIBUTEVALUECHANGESOURCEINDICATOR, ASN_INTEGER, RONLY, var_attributeValueChangeRecordTable, 6, {1, 2, 7, 1, 1, 2}},
#define   ATTRIBUTEVALUECHANGEATTRIBUTEIDENTIFIERLIST  (78 % 256)
	{ATTRIBUTEVALUECHANGEATTRIBUTEIDENTIFIERLIST, ASN_OBJECT_ID, RONLY, var_attributeValueChangeRecordTable, 6, {1, 2, 7, 1, 1, 3}},
#define   OBJECTCREATIONSOURCEINDICATOR  (83 % 256)
	{OBJECTCREATIONSOURCEINDICATOR, ASN_INTEGER, RONLY, var_objectCreationRecordTable, 6, {1, 2, 8, 1, 1, 1}},
#define   OBJECTCREATIONATTRIBUTEIDENTIFIERLIST  (84 % 256)
	{OBJECTCREATIONATTRIBUTEIDENTIFIERLIST, ASN_OBJECT_ID, RONLY, var_objectCreationRecordTable, 6, {1, 2, 8, 1, 1, 2}},
#define   OBJECTDELETIONSOURCEINDICATOR  (89 % 256)
	{OBJECTDELETIONSOURCEINDICATOR, ASN_INTEGER, RONLY, var_objectDeletionRecordTable, 6, {1, 2, 9, 1, 1, 1}},
#define   OBJECTDELETIONATTRIBUTEIDENTIFIERLIST  (90 % 256)
	{OBJECTDELETIONATTRIBUTEIDENTIFIERLIST, ASN_OBJECT_ID, RONLY, var_objectDeletionRecordTable, 6, {1, 2, 9, 1, 1, 2}},
#define   RELATIONSHIPCHANGERECORDDEFINITION  (95 % 256)
	{RELATIONSHIPCHANGERECORDDEFINITION, ASN_OCTET_STR, RONLY, var_relationshipChangeRecordTable, 6, {1, 2, 10, 1, 1, 1}},
#define   RELATIONSHIPCHANGESOURCEINDICATOR  (96 % 256)
	{RELATIONSHIPCHANGESOURCEINDICATOR, ASN_INTEGER, RONLY, var_relationshipChangeRecordTable, 6, {1, 2, 10, 1, 1, 2}},
#define   RELATIONSHIPCHANGEATTRIBUTEIDENTIFIERLIST  (97 % 256)
	{RELATIONSHIPCHANGEATTRIBUTEIDENTIFIERLIST, ASN_OBJECT_ID, RONLY, var_relationshipChangeRecordTable, 6, {1, 2, 10, 1, 1, 3}},
#define   SECURITYALARMRECORDCAUSE  (102 % 256)
	{SECURITYALARMRECORDCAUSE, ASN_OBJECT_ID, RONLY, var_securityAlarmReportRecordTable, 6, {1, 2, 11, 1, 1, 1}},
#define   SECURITYALARMRECORDSEVERITY  (103 % 256)
	{SECURITYALARMRECORDSEVERITY, ASN_INTEGER, RONLY, var_securityAlarmReportRecordTable, 6, {1, 2, 11, 1, 1, 2}},
#define   SECURITYALARMRECORDDETECTOR  (104 % 256)
	{SECURITYALARMRECORDDETECTOR, ASN_OBJECT_ID, RONLY, var_securityAlarmReportRecordTable, 6, {1, 2, 11, 1, 1, 3}},
#define   SECURITYSERVICEUSER   (105 % 256)
	{SECURITYSERVICEUSER, ASN_OCTET_STR, RONLY, var_securityAlarmReportRecordTable, 6, {1, 2, 11, 1, 1, 4}},
#define   SECURITYSERVICEPROVIDER  (106 % 256)
	{SECURITYSERVICEPROVIDER, ASN_OCTET_STR, RONLY, var_securityAlarmReportRecordTable, 6, {1, 2, 11, 1, 1, 5}},
#define   STATECHANGERECORDDEFINITION  (111 % 256)
	{STATECHANGERECORDDEFINITION, ASN_OCTET_STR, RONLY, var_stateChangeRecordTable, 6, {1, 2, 12, 1, 1, 1}},
#define   STATECHANGESOURCEINDICATOR  (112 % 256)
	{STATECHANGESOURCEINDICATOR, ASN_INTEGER, RONLY, var_stateChangeRecordTable, 6, {1, 2, 12, 1, 1, 2}},
#define   STATECHANGEATTRIBUTEIDENTIFIERLIST  (113 % 256)
	{STATECHANGEATTRIBUTEIDENTIFIERLIST, ASN_OBJECT_ID, RONLY, var_stateChangeRecordTable, 6, {1, 2, 12, 1, 1, 3}},
};

/* (L = length of the oidsuffix) */
struct smiMIB_data *smiMIBStorage = NULL;

/* global storage of our data, saved in and configured by header_complex() */
struct header_complex_index *discriminatorTableStorage = NULL;
struct header_complex_index *eventForwardingDiscriminatorTableStorage = NULL;
struct header_complex_index *logTableStorage = NULL;
struct header_complex_index *logRecordTableStorage = NULL;
struct header_complex_index *eventLogRecordTableStorage = NULL;
struct header_complex_index *alarmRecordTableStorage = NULL;
struct header_complex_index *attributeValueChangeRecordTableStorage = NULL;
struct header_complex_index *objectCreationRecordTableStorage = NULL;
struct header_complex_index *objectDeletionRecordTableStorage = NULL;
struct header_complex_index *relationshipChangeRecordTableStorage = NULL;
struct header_complex_index *securityAlarmReportRecordTableStorage = NULL;
struct header_complex_index *stateChangeRecordTableStorage = NULL;

/*
 * init_smiMIB(): Initialization routine.
 * This is called when the agent starts up.  At a minimum, registration of your variables should
 * take place here.
 */
void
init_smiMIB(void)
{
	DEBUGMSGTL(("smiMIB", "initializing...  "));
	/* register ourselves with the agent to handle our mib tree */
	REGISTER_MIB("smiMIB", smiMIB_variables, variable7, smiMIB_variables_oid);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_SHUTDOWN, term_smiMIB, NULL);
	/* register our config handler(s) to deal with registrations */
	snmpd_register_config_handler("smiMIB", parse_smiMIB, NULL, "HELP STRING");
	snmpd_register_config_handler("discriminatorTable", parse_discriminatorTable, NULL, "HELP STRING");
	snmpd_register_config_handler("eventForwardingDiscriminatorTable", parse_eventForwardingDiscriminatorTable, NULL, "HELP STRING");
	snmpd_register_config_handler("logTable", parse_logTable, NULL, "HELP STRING");
	snmpd_register_config_handler("logRecordTable", parse_logRecordTable, NULL, "HELP STRING");
	snmpd_register_config_handler("eventLogRecordTable", parse_eventLogRecordTable, NULL, "HELP STRING");
	snmpd_register_config_handler("alarmRecordTable", parse_alarmRecordTable, NULL, "HELP STRING");
	snmpd_register_config_handler("attributeValueChangeRecordTable", parse_attributeValueChangeRecordTable, NULL, "HELP STRING");
	snmpd_register_config_handler("objectCreationRecordTable", parse_objectCreationRecordTable, NULL, "HELP STRING");
	snmpd_register_config_handler("objectDeletionRecordTable", parse_objectDeletionRecordTable, NULL, "HELP STRING");
	snmpd_register_config_handler("relationshipChangeRecordTable", parse_relationshipChangeRecordTable, NULL, "HELP STRING");
	snmpd_register_config_handler("securityAlarmReportRecordTable", parse_securityAlarmReportRecordTable, NULL, "HELP STRING");
	snmpd_register_config_handler("stateChangeRecordTable", parse_stateChangeRecordTable, NULL, "HELP STRING");

	/* we need to be called back later to store our data */
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_smiMIB, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_discriminatorTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_eventForwardingDiscriminatorTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_logTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_logRecordTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_eventLogRecordTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_alarmRecordTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_attributeValueChangeRecordTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_objectCreationRecordTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_objectDeletionRecordTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_relationshipChangeRecordTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_securityAlarmReportRecordTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_stateChangeRecordTable, NULL);

	/* place any other initialization junk you need here */
	DEBUGMSGTL(("smiMIB", "done.\n"));
}

/*
 * deinit_smiMIB(): Deinitialization routine.
 * This is called before the agent is unloaded.  At a minimum, deregistration of your variables
 * should take place here.
 */
void
deinit_smiMIB(void)
{
	DEBUGMSGTL(("smiMIB", "deinitializating...  "));
	unregister_mib(smiMIB_variables_oid, sizeof(smiMIB_variables_oid) / sizeof(oid));
	snmpd_unregister_config_handler("smiMIB");
	snmpd_unregister_config_handler("discriminatorTable");
	snmpd_unregister_config_handler("eventForwardingDiscriminatorTable");
	snmpd_unregister_config_handler("logTable");
	snmpd_unregister_config_handler("logRecordTable");
	snmpd_unregister_config_handler("eventLogRecordTable");
	snmpd_unregister_config_handler("alarmRecordTable");
	snmpd_unregister_config_handler("attributeValueChangeRecordTable");
	snmpd_unregister_config_handler("objectCreationRecordTable");
	snmpd_unregister_config_handler("objectDeletionRecordTable");
	snmpd_unregister_config_handler("relationshipChangeRecordTable");
	snmpd_unregister_config_handler("securityAlarmReportRecordTable");
	snmpd_unregister_config_handler("stateChangeRecordTable");

	/* place any other de-initialization junk you need here */
	DEBUGMSGTL(("smiMIB", "done.\n"));
}

int
term_smiMIB(int majorID, int minorID, void *serverarg, void *clientarg)
{
	deinit_smiMIB();
	return 0;
}

/**
 * @fn struct smiMIB_data *smiMIB_create(void)
 * @brief create a fresh data structure representing scalars in smiMIB.
 * Creates a new smiMIB_data structure by allocating dynamic memory for the structure and
 * initializing the default values of scalars in smiMIB.
 */
struct smiMIB_data *
smiMIB_create(void)
{
	struct smiMIB_data *StorageNew = SNMP_MALLOC_STRUCT(smiMIB_data);

	DEBUGMSGTL(("smiMIB", "creating scalars...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default scalar values here into StorageNew */

	}
	DEBUGMSGTL(("smiMIB", "done.\n"));
	return (StorageNew);
}

/**
 * @fn int smiMIB_destroy(struct smiMIB_data **thedata)
 * @param thedata pointer to the data structure in smiMIB.
 * @brief delete a scalars structure from smiMIB.
 * Frees scalars that were previously removed from smiMIB.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
smiMIB_destroy(struct smiMIB_data **thedata)
{
	struct smiMIB_data *StorageDel;

	DEBUGMSGTL(("smiMIB", "deleting scalars...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("smiMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int smiMIB_add(struct smiMIB_data *thedata)
 * @param thedata the structure representing smiMIB scalars.
 * @brief adds node to the smiMIB scalar data set.
 * Adds a scalar structure to the smiMIB data set.  Note that this function is necessary even
 * when the scalar values are not peristent.
 */
int
smiMIB_add(struct smiMIB_data *thedata)
{
	DEBUGMSGTL(("smiMIB", "adding data...  "));
	smiMIBStorage = thedata;
	DEBUGMSGTL(("smiMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_smiMIB(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for smiMIB entries.
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case smiMIB).  This routine is invoked by
 * UCD-SNMP to read the values of scalars in the MIB from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the MIB.  If there are no configured entries
 * in the configuration MIB, this function will simply not be called.
 */
void
parse_smiMIB(const char *token, char *line)
{
	size_t tmpsize;
	struct smiMIB_data *StorageTmp = smiMIB_create();

	DEBUGMSGTL(("smiMIB", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual scalars that are not persistent */
	smiMIB_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("smiMIB", "done.\n"));
}

/*
 * store_smiMIB(): stores .conf file entries needed to configure the mib.
 */
int
store_smiMIB(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct smiMIB_data *StorageTmp;

	DEBUGMSGTL(("smiMIB", "storing data...  "));
	refresh_smiMIB();
	if ((StorageTmp = smiMIBStorage) == NULL) {
		DEBUGMSGTL(("smiMIB", "error.\n"));
		return SNMPERR_GENERR;
	}
	(void) tmpsize;
	/* XXX: comment entire section if no scalars are persistent */
	{
		memset(line, 0, sizeof(line));
		strcat(line, "smiMIB ");
		cptr = line + strlen(line);
		/* XXX: remove individual scalars that are not persistent */
		snmpd_store_config(line);
	}
	DEBUGMSGTL(("smiMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void refresh_smiMIB(void)
 * @brief refresh the scalar values of smiMIB.
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a scalar has been requested).
 */
void
refresh_smiMIB(void)
{
	if (smiMIBStorage == NULL) {
		struct smiMIB_data *StorageNew;

		if ((StorageNew = smiMIB_create()) == NULL)
			return;
		smiMIBStorage = StorageNew;
		smiMIB_refresh = 1;
	}
	if (smiMIB_refresh == 0)
		return;
	smiMIB_refresh = 0;
	/* XXX: Update scalars as required here... */
}

/**
 * @fn u_char * var_smiMIB(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @param vp a pointer to the entry in the variables table for the requested variable.
 * @param name the object identifier for which to find.
 * @param length the length of the object identifier.
 * @param exact whether the name is exact.
 * @param var_len a pointer to the length of the representation of the object.
 * @param write_method a pointer to a write method for the object.
 * @brief locate variables in smiMIB.
 * This function returns a pointer to a memory area that is static across the request that contains
 * the UCD-SNMP representation of the scalar (so that it may be used to read from for a GET,
 * GET-NEXT or GET-BULK request).  This returned pointer may be NULL, in which case the function is
 * telling UCD-SNMP that the scalar does not exist for reading; however, if write_method is
 * overwritten with a non-NULL value, the function is telling UCD-SNMP that the scalar exists for
 * writing.  Write-only objects can be effected in this way.
 */
u_char *
var_smiMIB(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct smiMIB_data *StorageTmp;

	if (header_generic(vp, name, length, exact, var_len, write_method) == MATCH_FAILED)
		return NULL;
	/* Refresh the MIB values if required. */
	refresh_smiMIB();
	if ((StorageTmp = smiMIBStorage) == NULL)
		return NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	default:
		ERROR_MSG("");
	}
	return NULL;
}

/**
 * @fn struct discriminatorTable_data *discriminatorTable_create(void)
 * @brief create a fresh data structure representing a new row in the discriminatorTable table.
 * Creates a new discriminatorTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct discriminatorTable_data *
discriminatorTable_create(void)
{
	struct discriminatorTable_data *StorageNew = SNMP_MALLOC_STRUCT(discriminatorTable_data);

	DEBUGMSGTL(("discriminatorTable", "creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->discriminatorEntryStatus = RS_NOTREADY;
	}
	DEBUGMSGTL(("discriminatorTable", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct discriminatorTable_data *discriminatorTable_duplicate(struct discriminatorTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicat a row structure for a table.
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct discriminatorTable_data *
discriminatorTable_duplicate(struct discriminatorTable_data *thedata)
{
	struct discriminatorTable_data *StorageNew = SNMP_MALLOC_STRUCT(discriminatorTable_data);

	DEBUGMSGTL(("discriminatorTable", "duplicating row...  "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("discriminatorTable", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	discriminatorTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int discriminatorTable_destroy(struct discriminatorTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
discriminatorTable_destroy(struct discriminatorTable_data **thedata)
{
	struct discriminatorTable_data *StorageDel;

	DEBUGMSGTL(("discriminatorTable", "deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->discriminatorId);
		StorageDel->discriminatorIdLen = 0;
		SNMP_FREE(StorageDel->discriminatorConstruct);
		StorageDel->discriminatorConstructLen = 0;
		SNMP_FREE(StorageDel->discriminatorAvailabilityStatus);
		StorageDel->discriminatorAvailabilityStatusLen = 0;
		SNMP_FREE(StorageDel->discriminatorIntervalsOfDay);
		StorageDel->discriminatorIntervalsOfDayLen = 0;
		SNMP_FREE(StorageDel->discriminatorWeekMask);
		StorageDel->discriminatorWeekMaskLen = 0;
		SNMP_FREE(StorageDel->discriminatorSchedulerName);
		StorageDel->discriminatorSchedulerNameLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("discriminatorTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int discriminatorTable_add(struct discriminatorTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the discriminatorTable table data set.
 * Adds a table row structure to the discriminatorTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
discriminatorTable_add(struct discriminatorTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("discriminatorTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index the data */
	/* discriminatorId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->discriminatorId, thedata->discriminatorIdLen);
	header_complex_add_data(&discriminatorTableStorage, vars, thedata);
	DEBUGMSGTL(("discriminatorTable", "registered an entry\n"));
	DEBUGMSGTL(("discriminatorTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int discriminatorTable_del(struct discriminatorTable_data *thedata)
 * @brief delete a row structure from a table.
 * @param thedata pointer to the extracted or existing data structure in the table.
 * Deletes a table row structure from the discriminatorTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
discriminatorTable_del(struct discriminatorTable_data *thedata)
{
	struct discriminatorTable_data *StorageDel;

	DEBUGMSGTL(("discriminatorTable", "deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(discriminatorTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&discriminatorTableStorage, hciptr);
	}
	DEBUGMSGTL(("discriminatorTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_discriminatorTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for discriminatorTable entries.
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case discriminatorTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_discriminatorTable(const char *token, char *line)
{
	size_t tmpsize;
	struct discriminatorTable_data *StorageTmp = discriminatorTable_create();

	DEBUGMSGTL(("discriminatorTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->discriminatorId, &StorageTmp->discriminatorIdLen);
	if (StorageTmp->discriminatorId == NULL) {
		config_perror("invalid specification for discriminatorId");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->discriminatorConstruct, &StorageTmp->discriminatorConstructLen);
	if (StorageTmp->discriminatorConstruct == NULL) {
		config_perror("invalid specification for discriminatorConstruct");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->discriminatorAdministrativeState, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->discriminatorOperationalState, &tmpsize);
	line = read_config_read_data(ASN_BIT_STR, line, &StorageTmp->discriminatorAvailabilityStatus, &StorageTmp->discriminatorAvailabilityStatusLen);
	if (StorageTmp->discriminatorAvailabilityStatus == NULL) {
		config_perror("invalid specification for discriminatorAvailabilityStatus");
		return;
	}
	line = read_config_read_data(ASN_TIMETICKS, line, &StorageTmp->discriminatorStartTime, &tmpsize);
	line = read_config_read_data(ASN_TIMETICKS, line, &StorageTmp->discriminatorStopTime, &tmpsize);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->discriminatorIntervalsOfDay, &StorageTmp->discriminatorIntervalsOfDayLen);
	if (StorageTmp->discriminatorIntervalsOfDay == NULL) {
		config_perror("invalid specification for discriminatorIntervalsOfDay");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->discriminatorWeekMask, &StorageTmp->discriminatorWeekMaskLen);
	if (StorageTmp->discriminatorWeekMask == NULL) {
		config_perror("invalid specification for discriminatorWeekMask");
		return;
	}
	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->discriminatorSchedulerName, &StorageTmp->discriminatorSchedulerNameLen);
	if (StorageTmp->discriminatorSchedulerName == NULL) {
		config_perror("invalid specification for discriminatorSchedulerName");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->discriminatorEntryStatus, &tmpsize);
	discriminatorTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("discriminatorTable", "done.\n"));
}

/*
 * store_discriminatorTable(): store configuraiton file for discriminatorTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_discriminatorTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct discriminatorTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("discriminatorTable", "storing data...  "));
	refresh_discriminatorTable();
	(void) tmpsize;
	for (hcindex = discriminatorTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct discriminatorTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "discriminatorTable ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->discriminatorId, &StorageTmp->discriminatorIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->discriminatorConstruct, &StorageTmp->discriminatorConstructLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->discriminatorAdministrativeState, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->discriminatorOperationalState, &tmpsize);
			cptr = read_config_store_data(ASN_BIT_STR, cptr, &StorageTmp->discriminatorAvailabilityStatus, &StorageTmp->discriminatorAvailabilityStatusLen);
			cptr = read_config_store_data(ASN_TIMETICKS, cptr, &StorageTmp->discriminatorStartTime, &tmpsize);
			cptr = read_config_store_data(ASN_TIMETICKS, cptr, &StorageTmp->discriminatorStopTime, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->discriminatorIntervalsOfDay, &StorageTmp->discriminatorIntervalsOfDayLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->discriminatorWeekMask, &StorageTmp->discriminatorWeekMaskLen);
			cptr = read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->discriminatorSchedulerName, &StorageTmp->discriminatorSchedulerNameLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->discriminatorEntryStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("discriminatorTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct eventForwardingDiscriminatorTable_data *eventForwardingDiscriminatorTable_create(void)
 * @brief create a fresh data structure representing a new row in the eventForwardingDiscriminatorTable table.
 * Creates a new eventForwardingDiscriminatorTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct eventForwardingDiscriminatorTable_data *
eventForwardingDiscriminatorTable_create(void)
{
	struct eventForwardingDiscriminatorTable_data *StorageNew = SNMP_MALLOC_STRUCT(eventForwardingDiscriminatorTable_data);

	DEBUGMSGTL(("eventForwardingDiscriminatorTable", "creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->eventForwardingDiscriminatorEntryStatus = RS_NOTREADY;
	}
	DEBUGMSGTL(("eventForwardingDiscriminatorTable", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct eventForwardingDiscriminatorTable_data *eventForwardingDiscriminatorTable_duplicate(struct eventForwardingDiscriminatorTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicat a row structure for a table.
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct eventForwardingDiscriminatorTable_data *
eventForwardingDiscriminatorTable_duplicate(struct eventForwardingDiscriminatorTable_data *thedata)
{
	struct eventForwardingDiscriminatorTable_data *StorageNew = SNMP_MALLOC_STRUCT(eventForwardingDiscriminatorTable_data);

	DEBUGMSGTL(("eventForwardingDiscriminatorTable", "duplicating row...  "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("eventForwardingDiscriminatorTable", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	eventForwardingDiscriminatorTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int eventForwardingDiscriminatorTable_destroy(struct eventForwardingDiscriminatorTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
eventForwardingDiscriminatorTable_destroy(struct eventForwardingDiscriminatorTable_data **thedata)
{
	struct eventForwardingDiscriminatorTable_data *StorageDel;

	DEBUGMSGTL(("eventForwardingDiscriminatorTable", "deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->discriminatorId);
		StorageDel->discriminatorIdLen = 0;
		SNMP_FREE(StorageDel->destination);
		StorageDel->destinationLen = 0;
		SNMP_FREE(StorageDel->activeDestination);
		StorageDel->activeDestinationLen = 0;
		SNMP_FREE(StorageDel->backUpDestinationList);
		StorageDel->backUpDestinationListLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("eventForwardingDiscriminatorTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int eventForwardingDiscriminatorTable_add(struct eventForwardingDiscriminatorTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the eventForwardingDiscriminatorTable table data set.
 * Adds a table row structure to the eventForwardingDiscriminatorTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
eventForwardingDiscriminatorTable_add(struct eventForwardingDiscriminatorTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("eventForwardingDiscriminatorTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index the data */
	/* discriminatorId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->discriminatorId, thedata->discriminatorIdLen);
	header_complex_add_data(&eventForwardingDiscriminatorTableStorage, vars, thedata);
	DEBUGMSGTL(("eventForwardingDiscriminatorTable", "registered an entry\n"));
	DEBUGMSGTL(("eventForwardingDiscriminatorTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int eventForwardingDiscriminatorTable_del(struct eventForwardingDiscriminatorTable_data *thedata)
 * @brief delete a row structure from a table.
 * @param thedata pointer to the extracted or existing data structure in the table.
 * Deletes a table row structure from the eventForwardingDiscriminatorTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
eventForwardingDiscriminatorTable_del(struct eventForwardingDiscriminatorTable_data *thedata)
{
	struct eventForwardingDiscriminatorTable_data *StorageDel;

	DEBUGMSGTL(("eventForwardingDiscriminatorTable", "deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(eventForwardingDiscriminatorTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&eventForwardingDiscriminatorTableStorage, hciptr);
	}
	DEBUGMSGTL(("eventForwardingDiscriminatorTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_eventForwardingDiscriminatorTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for eventForwardingDiscriminatorTable entries.
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case eventForwardingDiscriminatorTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_eventForwardingDiscriminatorTable(const char *token, char *line)
{
	size_t tmpsize;
	struct eventForwardingDiscriminatorTable_data *StorageTmp = eventForwardingDiscriminatorTable_create();

	DEBUGMSGTL(("eventForwardingDiscriminatorTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->discriminatorId, &StorageTmp->discriminatorIdLen);
	if (StorageTmp->discriminatorId == NULL) {
		config_perror("invalid specification for discriminatorId");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->destination, &StorageTmp->destinationLen);
	if (StorageTmp->destination == NULL) {
		config_perror("invalid specification for destination");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->activeDestination, &StorageTmp->activeDestinationLen);
	if (StorageTmp->activeDestination == NULL) {
		config_perror("invalid specification for activeDestination");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->backUpDestinationList, &StorageTmp->backUpDestinationListLen);
	if (StorageTmp->backUpDestinationList == NULL) {
		config_perror("invalid specification for backUpDestinationList");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->confirmedMode, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->eventForwardingDiscriminatorEntryStatus, &tmpsize);
	eventForwardingDiscriminatorTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("eventForwardingDiscriminatorTable", "done.\n"));
}

/*
 * store_eventForwardingDiscriminatorTable(): store configuraiton file for eventForwardingDiscriminatorTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_eventForwardingDiscriminatorTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct eventForwardingDiscriminatorTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("eventForwardingDiscriminatorTable", "storing data...  "));
	refresh_eventForwardingDiscriminatorTable();
	(void) tmpsize;
	for (hcindex = eventForwardingDiscriminatorTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct eventForwardingDiscriminatorTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "eventForwardingDiscriminatorTable ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->discriminatorId, &StorageTmp->discriminatorIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->destination, &StorageTmp->destinationLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->activeDestination, &StorageTmp->activeDestinationLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->backUpDestinationList, &StorageTmp->backUpDestinationListLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->confirmedMode, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->eventForwardingDiscriminatorEntryStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("eventForwardingDiscriminatorTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct logTable_data *logTable_create(void)
 * @brief create a fresh data structure representing a new row in the logTable table.
 * Creates a new logTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct logTable_data *
logTable_create(void)
{
	struct logTable_data *StorageNew = SNMP_MALLOC_STRUCT(logTable_data);

	DEBUGMSGTL(("logTable", "creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->logAdministrativeState = 1;
		/* StorageNew->capacityAlarmThreshold = (uint8_t *) strdup(\"\x64\"); *//* DEFVAL \"\x64\" */
		/* StorageNew->capacityAlarmThresholdLen = strlen(\"\x64\"); *//* DEFVAL \"\x64\" */
		StorageNew->logStopTime = 6983120;
		/* StorageNew->logIntervalsOfDay = (uint8_t *) strdup(\"\x00\x00\x17\x3B\"); *//* DEFVAL \"\x00\x00\x17\x3B\" */
		/* StorageNew->logIntervalsOfDayLen = strlen(\"\x00\x00\x17\x3B\"); *//* DEFVAL \"\x00\x00\x17\x3B\" */
		/* StorageNew->logWeekMask = (uint8_t *) strdup(\"\x7F\x01\x00\x00\x17\x3B\"); *//* DEFVAL \"\x7F\x01\x00\x00\x17\x3B\" */
		/* StorageNew->logWeekMaskLen = strlen(\"\x7F\x01\x00\x00\x17\x3B\"); *//* DEFVAL \"\x7F\x01\x00\x00\x17\x3B\" */
		/* StorageNew->logSchedulerName = NULL; *//* DEFVAL zeroDotZero */
		/* StorageNew->logSchedulerName = 0; *//* DEFVAL zeroDotZero */
		StorageNew->logEntryStatus = RS_NOTREADY;
	}
	DEBUGMSGTL(("logTable", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct logTable_data *logTable_duplicate(struct logTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicat a row structure for a table.
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct logTable_data *
logTable_duplicate(struct logTable_data *thedata)
{
	struct logTable_data *StorageNew = SNMP_MALLOC_STRUCT(logTable_data);

	DEBUGMSGTL(("logTable", "duplicating row...  "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("logTable", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	logTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int logTable_destroy(struct logTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
logTable_destroy(struct logTable_data **thedata)
{
	struct logTable_data *StorageDel;

	DEBUGMSGTL(("logTable", "deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->logId);
		StorageDel->logIdLen = 0;
		SNMP_FREE(StorageDel->logDiscriminatorConstruct);
		StorageDel->logDiscriminatorConstructLen = 0;
		SNMP_FREE(StorageDel->logAvailabilityStatus);
		StorageDel->logAvailabilityStatusLen = 0;
		SNMP_FREE(StorageDel->capacityAlarmThreshold);
		StorageDel->capacityAlarmThresholdLen = 0;
		SNMP_FREE(StorageDel->logIntervalsOfDay);
		StorageDel->logIntervalsOfDayLen = 0;
		SNMP_FREE(StorageDel->logWeekMask);
		StorageDel->logWeekMaskLen = 0;
		SNMP_FREE(StorageDel->logSchedulerName);
		StorageDel->logSchedulerNameLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("logTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int logTable_add(struct logTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the logTable table data set.
 * Adds a table row structure to the logTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
logTable_add(struct logTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("logTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index the data */
	/* logId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->logId, thedata->logIdLen);
	header_complex_add_data(&logTableStorage, vars, thedata);
	DEBUGMSGTL(("logTable", "registered an entry\n"));
	DEBUGMSGTL(("logTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int logTable_del(struct logTable_data *thedata)
 * @brief delete a row structure from a table.
 * @param thedata pointer to the extracted or existing data structure in the table.
 * Deletes a table row structure from the logTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
logTable_del(struct logTable_data *thedata)
{
	struct logTable_data *StorageDel;

	DEBUGMSGTL(("logTable", "deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(logTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&logTableStorage, hciptr);
	}
	DEBUGMSGTL(("logTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_logTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for logTable entries.
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case logTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_logTable(const char *token, char *line)
{
	size_t tmpsize;
	struct logTable_data *StorageTmp = logTable_create();

	DEBUGMSGTL(("logTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->logId, &StorageTmp->logIdLen);
	if (StorageTmp->logId == NULL) {
		config_perror("invalid specification for logId");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->logDiscriminatorConstruct, &StorageTmp->logDiscriminatorConstructLen);
	if (StorageTmp->logDiscriminatorConstruct == NULL) {
		config_perror("invalid specification for logDiscriminatorConstruct");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->logAdministrativeState, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->logOperationalState, &tmpsize);
	line = read_config_read_data(ASN_BIT_STR, line, &StorageTmp->logAvailabilityStatus, &StorageTmp->logAvailabilityStatusLen);
	if (StorageTmp->logAvailabilityStatus == NULL) {
		config_perror("invalid specification for logAvailabilityStatus");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->logFullAction, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->maxLogSize, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->currentLogSize, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->numberOfRecords, &tmpsize);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->capacityAlarmThreshold, &StorageTmp->capacityAlarmThresholdLen);
	if (StorageTmp->capacityAlarmThreshold == NULL) {
		config_perror("invalid specification for capacityAlarmThreshold");
		return;
	}
	line = read_config_read_data(ASN_TIMETICKS, line, &StorageTmp->logStartTime, &tmpsize);
	line = read_config_read_data(ASN_TIMETICKS, line, &StorageTmp->logStopTime, &tmpsize);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->logIntervalsOfDay, &StorageTmp->logIntervalsOfDayLen);
	if (StorageTmp->logIntervalsOfDay == NULL) {
		config_perror("invalid specification for logIntervalsOfDay");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->logWeekMask, &StorageTmp->logWeekMaskLen);
	if (StorageTmp->logWeekMask == NULL) {
		config_perror("invalid specification for logWeekMask");
		return;
	}
	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->logSchedulerName, &StorageTmp->logSchedulerNameLen);
	if (StorageTmp->logSchedulerName == NULL) {
		config_perror("invalid specification for logSchedulerName");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->logEntryStatus, &tmpsize);
	logTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("logTable", "done.\n"));
}

/*
 * store_logTable(): store configuraiton file for logTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_logTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct logTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("logTable", "storing data...  "));
	refresh_logTable();
	(void) tmpsize;
	for (hcindex = logTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct logTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "logTable ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->logId, &StorageTmp->logIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->logDiscriminatorConstruct, &StorageTmp->logDiscriminatorConstructLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->logAdministrativeState, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->logOperationalState, &tmpsize);
			cptr = read_config_store_data(ASN_BIT_STR, cptr, &StorageTmp->logAvailabilityStatus, &StorageTmp->logAvailabilityStatusLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->logFullAction, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->maxLogSize, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->currentLogSize, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->numberOfRecords, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->capacityAlarmThreshold, &StorageTmp->capacityAlarmThresholdLen);
			cptr = read_config_store_data(ASN_TIMETICKS, cptr, &StorageTmp->logStartTime, &tmpsize);
			cptr = read_config_store_data(ASN_TIMETICKS, cptr, &StorageTmp->logStopTime, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->logIntervalsOfDay, &StorageTmp->logIntervalsOfDayLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->logWeekMask, &StorageTmp->logWeekMaskLen);
			cptr = read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->logSchedulerName, &StorageTmp->logSchedulerNameLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->logEntryStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("logTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct logRecordTable_data *logRecordTable_create(void)
 * @brief create a fresh data structure representing a new row in the logRecordTable table.
 * Creates a new logRecordTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct logRecordTable_data *
logRecordTable_create(void)
{
	struct logRecordTable_data *StorageNew = SNMP_MALLOC_STRUCT(logRecordTable_data);

	DEBUGMSGTL(("logRecordTable", "creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->logRecordEntryStatus = RS_NOTREADY;
	}
	DEBUGMSGTL(("logRecordTable", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct logRecordTable_data *logRecordTable_duplicate(struct logRecordTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicat a row structure for a table.
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct logRecordTable_data *
logRecordTable_duplicate(struct logRecordTable_data *thedata)
{
	struct logRecordTable_data *StorageNew = SNMP_MALLOC_STRUCT(logRecordTable_data);

	DEBUGMSGTL(("logRecordTable", "duplicating row...  "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("logRecordTable", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	logRecordTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int logRecordTable_destroy(struct logRecordTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
logRecordTable_destroy(struct logRecordTable_data **thedata)
{
	struct logRecordTable_data *StorageDel;

	DEBUGMSGTL(("logRecordTable", "deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->logId);
		StorageDel->logIdLen = 0;
		SNMP_FREE(StorageDel->logRecordId);
		StorageDel->logRecordIdLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("logRecordTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int logRecordTable_add(struct logRecordTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the logRecordTable table data set.
 * Adds a table row structure to the logRecordTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
logRecordTable_add(struct logRecordTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("logRecordTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index the data */
	/* logId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->logId, thedata->logIdLen);
	/* logRecordId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->logRecordId, thedata->logRecordIdLen);
	header_complex_add_data(&logRecordTableStorage, vars, thedata);
	DEBUGMSGTL(("logRecordTable", "registered an entry\n"));
	DEBUGMSGTL(("logRecordTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int logRecordTable_del(struct logRecordTable_data *thedata)
 * @brief delete a row structure from a table.
 * @param thedata pointer to the extracted or existing data structure in the table.
 * Deletes a table row structure from the logRecordTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
logRecordTable_del(struct logRecordTable_data *thedata)
{
	struct logRecordTable_data *StorageDel;

	DEBUGMSGTL(("logRecordTable", "deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(logRecordTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&logRecordTableStorage, hciptr);
	}
	DEBUGMSGTL(("logRecordTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_logRecordTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for logRecordTable entries.
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case logRecordTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_logRecordTable(const char *token, char *line)
{
	size_t tmpsize;
	struct logRecordTable_data *StorageTmp = logRecordTable_create();

	DEBUGMSGTL(("logRecordTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->logId, &StorageTmp->logIdLen);
	if (StorageTmp->logId == NULL) {
		config_perror("invalid specification for logId");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->logRecordId, &StorageTmp->logRecordIdLen);
	if (StorageTmp->logRecordId == NULL) {
		config_perror("invalid specification for logRecordId");
		return;
	}
	line = read_config_read_data(ASN_TIMETICKS, line, &StorageTmp->loggingTime, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->logRecordEntryStatus, &tmpsize);
	logRecordTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("logRecordTable", "done.\n"));
}

/*
 * store_logRecordTable(): store configuraiton file for logRecordTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_logRecordTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct logRecordTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("logRecordTable", "storing data...  "));
	refresh_logRecordTable();
	(void) tmpsize;
	for (hcindex = logRecordTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct logRecordTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "logRecordTable ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->logId, &StorageTmp->logIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->logRecordId, &StorageTmp->logRecordIdLen);
			cptr = read_config_store_data(ASN_TIMETICKS, cptr, &StorageTmp->loggingTime, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->logRecordEntryStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("logRecordTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct eventLogRecordTable_data *eventLogRecordTable_create(void)
 * @brief create a fresh data structure representing a new row in the eventLogRecordTable table.
 * Creates a new eventLogRecordTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct eventLogRecordTable_data *
eventLogRecordTable_create(void)
{
	struct eventLogRecordTable_data *StorageNew = SNMP_MALLOC_STRUCT(eventLogRecordTable_data);

	DEBUGMSGTL(("eventLogRecordTable", "creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */

	}
	DEBUGMSGTL(("eventLogRecordTable", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct eventLogRecordTable_data *eventLogRecordTable_duplicate(struct eventLogRecordTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicat a row structure for a table.
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct eventLogRecordTable_data *
eventLogRecordTable_duplicate(struct eventLogRecordTable_data *thedata)
{
	struct eventLogRecordTable_data *StorageNew = SNMP_MALLOC_STRUCT(eventLogRecordTable_data);

	DEBUGMSGTL(("eventLogRecordTable", "duplicating row...  "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("eventLogRecordTable", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	eventLogRecordTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int eventLogRecordTable_destroy(struct eventLogRecordTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
eventLogRecordTable_destroy(struct eventLogRecordTable_data **thedata)
{
	struct eventLogRecordTable_data *StorageDel;

	DEBUGMSGTL(("eventLogRecordTable", "deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->logId);
		StorageDel->logIdLen = 0;
		SNMP_FREE(StorageDel->logRecordId);
		StorageDel->logRecordIdLen = 0;
		SNMP_FREE(StorageDel->managedObjectClass);
		StorageDel->managedObjectClassLen = 0;
		SNMP_FREE(StorageDel->managedObjectInstance);
		StorageDel->managedObjectInstanceLen = 0;
		SNMP_FREE(StorageDel->eventType);
		StorageDel->eventTypeLen = 0;
		SNMP_FREE(StorageDel->eventCorrelatedNotifications);
		StorageDel->eventCorrelatedNotificationsLen = 0;
		SNMP_FREE(StorageDel->eventAdditionalText);
		StorageDel->eventAdditionalTextLen = 0;
		SNMP_FREE(StorageDel->eventAdditionalInformation);
		StorageDel->eventAdditionalInformationLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("eventLogRecordTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int eventLogRecordTable_add(struct eventLogRecordTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the eventLogRecordTable table data set.
 * Adds a table row structure to the eventLogRecordTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
eventLogRecordTable_add(struct eventLogRecordTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("eventLogRecordTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index the data */
	/* logId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->logId, thedata->logIdLen);
	/* logRecordId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->logRecordId, thedata->logRecordIdLen);
	header_complex_add_data(&eventLogRecordTableStorage, vars, thedata);
	DEBUGMSGTL(("eventLogRecordTable", "registered an entry\n"));
	DEBUGMSGTL(("eventLogRecordTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int eventLogRecordTable_del(struct eventLogRecordTable_data *thedata)
 * @brief delete a row structure from a table.
 * @param thedata pointer to the extracted or existing data structure in the table.
 * Deletes a table row structure from the eventLogRecordTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
eventLogRecordTable_del(struct eventLogRecordTable_data *thedata)
{
	struct eventLogRecordTable_data *StorageDel;

	DEBUGMSGTL(("eventLogRecordTable", "deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(eventLogRecordTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&eventLogRecordTableStorage, hciptr);
	}
	DEBUGMSGTL(("eventLogRecordTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_eventLogRecordTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for eventLogRecordTable entries.
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case eventLogRecordTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_eventLogRecordTable(const char *token, char *line)
{
	size_t tmpsize;
	struct eventLogRecordTable_data *StorageTmp = eventLogRecordTable_create();

	DEBUGMSGTL(("eventLogRecordTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->logId, &StorageTmp->logIdLen);
	if (StorageTmp->logId == NULL) {
		config_perror("invalid specification for logId");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->logRecordId, &StorageTmp->logRecordIdLen);
	if (StorageTmp->logRecordId == NULL) {
		config_perror("invalid specification for logRecordId");
		return;
	}
	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->managedObjectClass, &StorageTmp->managedObjectClassLen);
	if (StorageTmp->managedObjectClass == NULL) {
		config_perror("invalid specification for managedObjectClass");
		return;
	}
	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->managedObjectInstance, &StorageTmp->managedObjectInstanceLen);
	if (StorageTmp->managedObjectInstance == NULL) {
		config_perror("invalid specification for managedObjectInstance");
		return;
	}
	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->eventType, &StorageTmp->eventTypeLen);
	if (StorageTmp->eventType == NULL) {
		config_perror("invalid specification for eventType");
		return;
	}
	line = read_config_read_data(ASN_TIMETICKS, line, &StorageTmp->eventTimes, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->eventNotificationIdentifier, &tmpsize);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->eventCorrelatedNotifications, &StorageTmp->eventCorrelatedNotificationsLen);
	if (StorageTmp->eventCorrelatedNotifications == NULL) {
		config_perror("invalid specification for eventCorrelatedNotifications");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->eventAdditionalText, &StorageTmp->eventAdditionalTextLen);
	if (StorageTmp->eventAdditionalText == NULL) {
		config_perror("invalid specification for eventAdditionalText");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->eventAdditionalInformation, &StorageTmp->eventAdditionalInformationLen);
	if (StorageTmp->eventAdditionalInformation == NULL) {
		config_perror("invalid specification for eventAdditionalInformation");
		return;
	}
	eventLogRecordTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("eventLogRecordTable", "done.\n"));
}

/*
 * store_eventLogRecordTable(): store configuraiton file for eventLogRecordTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_eventLogRecordTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct eventLogRecordTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("eventLogRecordTable", "storing data...  "));
	refresh_eventLogRecordTable();
	(void) tmpsize;
	for (hcindex = eventLogRecordTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct eventLogRecordTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "eventLogRecordTable ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->logId, &StorageTmp->logIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->logRecordId, &StorageTmp->logRecordIdLen);
			cptr = read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->managedObjectClass, &StorageTmp->managedObjectClassLen);
			cptr = read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->managedObjectInstance, &StorageTmp->managedObjectInstanceLen);
			cptr = read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->eventType, &StorageTmp->eventTypeLen);
			cptr = read_config_store_data(ASN_TIMETICKS, cptr, &StorageTmp->eventTimes, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->eventNotificationIdentifier, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->eventCorrelatedNotifications, &StorageTmp->eventCorrelatedNotificationsLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->eventAdditionalText, &StorageTmp->eventAdditionalTextLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->eventAdditionalInformation, &StorageTmp->eventAdditionalInformationLen);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("eventLogRecordTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct alarmRecordTable_data *alarmRecordTable_create(void)
 * @brief create a fresh data structure representing a new row in the alarmRecordTable table.
 * Creates a new alarmRecordTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct alarmRecordTable_data *
alarmRecordTable_create(void)
{
	struct alarmRecordTable_data *StorageNew = SNMP_MALLOC_STRUCT(alarmRecordTable_data);

	DEBUGMSGTL(("alarmRecordTable", "creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		/* StorageNew->alarmRecordSpecificProblems = NULL; *//* DEFVAL zeroDotZero */
		/* StorageNew->alarmRecordSpecificProblems = 0; *//* DEFVAL zeroDotZero */
		/* StorageNew->alarmRecordBackUpObject = NULL; *//* DEFVAL zeroDotZero */
		/* StorageNew->alarmRecordBackUpObject = 0; *//* DEFVAL zeroDotZero */
		/* StorageNew->alarmRecordProposedRepairActions = NULL; *//* DEFVAL zeroDotZero */
		/* StorageNew->alarmRecordProposedRepairActions = 0; *//* DEFVAL zeroDotZero */

	}
	DEBUGMSGTL(("alarmRecordTable", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct alarmRecordTable_data *alarmRecordTable_duplicate(struct alarmRecordTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicat a row structure for a table.
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct alarmRecordTable_data *
alarmRecordTable_duplicate(struct alarmRecordTable_data *thedata)
{
	struct alarmRecordTable_data *StorageNew = SNMP_MALLOC_STRUCT(alarmRecordTable_data);

	DEBUGMSGTL(("alarmRecordTable", "duplicating row...  "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("alarmRecordTable", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	alarmRecordTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int alarmRecordTable_destroy(struct alarmRecordTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
alarmRecordTable_destroy(struct alarmRecordTable_data **thedata)
{
	struct alarmRecordTable_data *StorageDel;

	DEBUGMSGTL(("alarmRecordTable", "deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->logId);
		StorageDel->logIdLen = 0;
		SNMP_FREE(StorageDel->logRecordId);
		StorageDel->logRecordIdLen = 0;
		SNMP_FREE(StorageDel->alarmRecordProbableCause);
		StorageDel->alarmRecordProbableCauseLen = 0;
		SNMP_FREE(StorageDel->alarmRecordSpecificProblems);
		StorageDel->alarmRecordSpecificProblemsLen = 0;
		SNMP_FREE(StorageDel->alarmRecordBackUpObject);
		StorageDel->alarmRecordBackUpObjectLen = 0;
		SNMP_FREE(StorageDel->alarmRecordThresholdInfo);
		StorageDel->alarmRecordThresholdInfoLen = 0;
		SNMP_FREE(StorageDel->alarmRecordStateChangeDefinition);
		StorageDel->alarmRecordStateChangeDefinitionLen = 0;
		SNMP_FREE(StorageDel->alarmRecordMonitoredAttributes);
		StorageDel->alarmRecordMonitoredAttributesLen = 0;
		SNMP_FREE(StorageDel->alarmRecordProposedRepairActions);
		StorageDel->alarmRecordProposedRepairActionsLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("alarmRecordTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int alarmRecordTable_add(struct alarmRecordTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the alarmRecordTable table data set.
 * Adds a table row structure to the alarmRecordTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
alarmRecordTable_add(struct alarmRecordTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("alarmRecordTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index the data */
	/* logId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->logId, thedata->logIdLen);
	/* logRecordId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->logRecordId, thedata->logRecordIdLen);
	header_complex_add_data(&alarmRecordTableStorage, vars, thedata);
	DEBUGMSGTL(("alarmRecordTable", "registered an entry\n"));
	DEBUGMSGTL(("alarmRecordTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int alarmRecordTable_del(struct alarmRecordTable_data *thedata)
 * @brief delete a row structure from a table.
 * @param thedata pointer to the extracted or existing data structure in the table.
 * Deletes a table row structure from the alarmRecordTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
alarmRecordTable_del(struct alarmRecordTable_data *thedata)
{
	struct alarmRecordTable_data *StorageDel;

	DEBUGMSGTL(("alarmRecordTable", "deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(alarmRecordTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&alarmRecordTableStorage, hciptr);
	}
	DEBUGMSGTL(("alarmRecordTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_alarmRecordTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for alarmRecordTable entries.
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case alarmRecordTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_alarmRecordTable(const char *token, char *line)
{
	size_t tmpsize;
	struct alarmRecordTable_data *StorageTmp = alarmRecordTable_create();

	DEBUGMSGTL(("alarmRecordTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->logId, &StorageTmp->logIdLen);
	if (StorageTmp->logId == NULL) {
		config_perror("invalid specification for logId");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->logRecordId, &StorageTmp->logRecordIdLen);
	if (StorageTmp->logRecordId == NULL) {
		config_perror("invalid specification for logRecordId");
		return;
	}
	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->alarmRecordProbableCause, &StorageTmp->alarmRecordProbableCauseLen);
	if (StorageTmp->alarmRecordProbableCause == NULL) {
		config_perror("invalid specification for alarmRecordProbableCause");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->alarmRecordPerceivedSeverity, &tmpsize);
	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->alarmRecordSpecificProblems, &StorageTmp->alarmRecordSpecificProblemsLen);
	if (StorageTmp->alarmRecordSpecificProblems == NULL) {
		config_perror("invalid specification for alarmRecordSpecificProblems");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->alarmRecordBackedUpStatus, &tmpsize);
	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->alarmRecordBackUpObject, &StorageTmp->alarmRecordBackUpObjectLen);
	if (StorageTmp->alarmRecordBackUpObject == NULL) {
		config_perror("invalid specification for alarmRecordBackUpObject");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->alarmRecordTrendIndication, &tmpsize);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->alarmRecordThresholdInfo, &StorageTmp->alarmRecordThresholdInfoLen);
	if (StorageTmp->alarmRecordThresholdInfo == NULL) {
		config_perror("invalid specification for alarmRecordThresholdInfo");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->alarmRecordStateChangeDefinition, &StorageTmp->alarmRecordStateChangeDefinitionLen);
	if (StorageTmp->alarmRecordStateChangeDefinition == NULL) {
		config_perror("invalid specification for alarmRecordStateChangeDefinition");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->alarmRecordMonitoredAttributes, &StorageTmp->alarmRecordMonitoredAttributesLen);
	if (StorageTmp->alarmRecordMonitoredAttributes == NULL) {
		config_perror("invalid specification for alarmRecordMonitoredAttributes");
		return;
	}
	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->alarmRecordProposedRepairActions, &StorageTmp->alarmRecordProposedRepairActionsLen);
	if (StorageTmp->alarmRecordProposedRepairActions == NULL) {
		config_perror("invalid specification for alarmRecordProposedRepairActions");
		return;
	}
	alarmRecordTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("alarmRecordTable", "done.\n"));
}

/*
 * store_alarmRecordTable(): store configuraiton file for alarmRecordTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_alarmRecordTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct alarmRecordTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("alarmRecordTable", "storing data...  "));
	refresh_alarmRecordTable();
	(void) tmpsize;
	for (hcindex = alarmRecordTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct alarmRecordTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "alarmRecordTable ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->logId, &StorageTmp->logIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->logRecordId, &StorageTmp->logRecordIdLen);
			cptr = read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->alarmRecordProbableCause, &StorageTmp->alarmRecordProbableCauseLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->alarmRecordPerceivedSeverity, &tmpsize);
			cptr = read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->alarmRecordSpecificProblems, &StorageTmp->alarmRecordSpecificProblemsLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->alarmRecordBackedUpStatus, &tmpsize);
			cptr = read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->alarmRecordBackUpObject, &StorageTmp->alarmRecordBackUpObjectLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->alarmRecordTrendIndication, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->alarmRecordThresholdInfo, &StorageTmp->alarmRecordThresholdInfoLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->alarmRecordStateChangeDefinition, &StorageTmp->alarmRecordStateChangeDefinitionLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->alarmRecordMonitoredAttributes, &StorageTmp->alarmRecordMonitoredAttributesLen);
			cptr = read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->alarmRecordProposedRepairActions, &StorageTmp->alarmRecordProposedRepairActionsLen);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("alarmRecordTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct attributeValueChangeRecordTable_data *attributeValueChangeRecordTable_create(void)
 * @brief create a fresh data structure representing a new row in the attributeValueChangeRecordTable table.
 * Creates a new attributeValueChangeRecordTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct attributeValueChangeRecordTable_data *
attributeValueChangeRecordTable_create(void)
{
	struct attributeValueChangeRecordTable_data *StorageNew = SNMP_MALLOC_STRUCT(attributeValueChangeRecordTable_data);

	DEBUGMSGTL(("attributeValueChangeRecordTable", "creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */

	}
	DEBUGMSGTL(("attributeValueChangeRecordTable", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct attributeValueChangeRecordTable_data *attributeValueChangeRecordTable_duplicate(struct attributeValueChangeRecordTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicat a row structure for a table.
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct attributeValueChangeRecordTable_data *
attributeValueChangeRecordTable_duplicate(struct attributeValueChangeRecordTable_data *thedata)
{
	struct attributeValueChangeRecordTable_data *StorageNew = SNMP_MALLOC_STRUCT(attributeValueChangeRecordTable_data);

	DEBUGMSGTL(("attributeValueChangeRecordTable", "duplicating row...  "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("attributeValueChangeRecordTable", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	attributeValueChangeRecordTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int attributeValueChangeRecordTable_destroy(struct attributeValueChangeRecordTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
attributeValueChangeRecordTable_destroy(struct attributeValueChangeRecordTable_data **thedata)
{
	struct attributeValueChangeRecordTable_data *StorageDel;

	DEBUGMSGTL(("attributeValueChangeRecordTable", "deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->logId);
		StorageDel->logIdLen = 0;
		SNMP_FREE(StorageDel->logRecordId);
		StorageDel->logRecordIdLen = 0;
		SNMP_FREE(StorageDel->attributeValueChangeRecordDefinition);
		StorageDel->attributeValueChangeRecordDefinitionLen = 0;
		SNMP_FREE(StorageDel->attributeValueChangeAttributeIdentifierList);
		StorageDel->attributeValueChangeAttributeIdentifierListLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("attributeValueChangeRecordTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int attributeValueChangeRecordTable_add(struct attributeValueChangeRecordTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the attributeValueChangeRecordTable table data set.
 * Adds a table row structure to the attributeValueChangeRecordTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
attributeValueChangeRecordTable_add(struct attributeValueChangeRecordTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("attributeValueChangeRecordTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index the data */
	/* logId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->logId, thedata->logIdLen);
	/* logRecordId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->logRecordId, thedata->logRecordIdLen);
	header_complex_add_data(&attributeValueChangeRecordTableStorage, vars, thedata);
	DEBUGMSGTL(("attributeValueChangeRecordTable", "registered an entry\n"));
	DEBUGMSGTL(("attributeValueChangeRecordTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int attributeValueChangeRecordTable_del(struct attributeValueChangeRecordTable_data *thedata)
 * @brief delete a row structure from a table.
 * @param thedata pointer to the extracted or existing data structure in the table.
 * Deletes a table row structure from the attributeValueChangeRecordTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
attributeValueChangeRecordTable_del(struct attributeValueChangeRecordTable_data *thedata)
{
	struct attributeValueChangeRecordTable_data *StorageDel;

	DEBUGMSGTL(("attributeValueChangeRecordTable", "deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(attributeValueChangeRecordTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&attributeValueChangeRecordTableStorage, hciptr);
	}
	DEBUGMSGTL(("attributeValueChangeRecordTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_attributeValueChangeRecordTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for attributeValueChangeRecordTable entries.
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case attributeValueChangeRecordTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_attributeValueChangeRecordTable(const char *token, char *line)
{
	size_t tmpsize;
	struct attributeValueChangeRecordTable_data *StorageTmp = attributeValueChangeRecordTable_create();

	DEBUGMSGTL(("attributeValueChangeRecordTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->logId, &StorageTmp->logIdLen);
	if (StorageTmp->logId == NULL) {
		config_perror("invalid specification for logId");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->logRecordId, &StorageTmp->logRecordIdLen);
	if (StorageTmp->logRecordId == NULL) {
		config_perror("invalid specification for logRecordId");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->attributeValueChangeRecordDefinition, &StorageTmp->attributeValueChangeRecordDefinitionLen);
	if (StorageTmp->attributeValueChangeRecordDefinition == NULL) {
		config_perror("invalid specification for attributeValueChangeRecordDefinition");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->attributeValueChangeSourceIndicator, &tmpsize);
	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->attributeValueChangeAttributeIdentifierList, &StorageTmp->attributeValueChangeAttributeIdentifierListLen);
	if (StorageTmp->attributeValueChangeAttributeIdentifierList == NULL) {
		config_perror("invalid specification for attributeValueChangeAttributeIdentifierList");
		return;
	}
	attributeValueChangeRecordTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("attributeValueChangeRecordTable", "done.\n"));
}

/*
 * store_attributeValueChangeRecordTable(): store configuraiton file for attributeValueChangeRecordTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_attributeValueChangeRecordTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct attributeValueChangeRecordTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("attributeValueChangeRecordTable", "storing data...  "));
	refresh_attributeValueChangeRecordTable();
	(void) tmpsize;
	for (hcindex = attributeValueChangeRecordTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct attributeValueChangeRecordTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "attributeValueChangeRecordTable ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->logId, &StorageTmp->logIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->logRecordId, &StorageTmp->logRecordIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->attributeValueChangeRecordDefinition, &StorageTmp->attributeValueChangeRecordDefinitionLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->attributeValueChangeSourceIndicator, &tmpsize);
			cptr = read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->attributeValueChangeAttributeIdentifierList, &StorageTmp->attributeValueChangeAttributeIdentifierListLen);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("attributeValueChangeRecordTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct objectCreationRecordTable_data *objectCreationRecordTable_create(void)
 * @brief create a fresh data structure representing a new row in the objectCreationRecordTable table.
 * Creates a new objectCreationRecordTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct objectCreationRecordTable_data *
objectCreationRecordTable_create(void)
{
	struct objectCreationRecordTable_data *StorageNew = SNMP_MALLOC_STRUCT(objectCreationRecordTable_data);

	DEBUGMSGTL(("objectCreationRecordTable", "creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */

	}
	DEBUGMSGTL(("objectCreationRecordTable", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct objectCreationRecordTable_data *objectCreationRecordTable_duplicate(struct objectCreationRecordTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicat a row structure for a table.
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct objectCreationRecordTable_data *
objectCreationRecordTable_duplicate(struct objectCreationRecordTable_data *thedata)
{
	struct objectCreationRecordTable_data *StorageNew = SNMP_MALLOC_STRUCT(objectCreationRecordTable_data);

	DEBUGMSGTL(("objectCreationRecordTable", "duplicating row...  "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("objectCreationRecordTable", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	objectCreationRecordTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int objectCreationRecordTable_destroy(struct objectCreationRecordTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
objectCreationRecordTable_destroy(struct objectCreationRecordTable_data **thedata)
{
	struct objectCreationRecordTable_data *StorageDel;

	DEBUGMSGTL(("objectCreationRecordTable", "deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->logId);
		StorageDel->logIdLen = 0;
		SNMP_FREE(StorageDel->logRecordId);
		StorageDel->logRecordIdLen = 0;
		SNMP_FREE(StorageDel->objectCreationAttributeIdentifierList);
		StorageDel->objectCreationAttributeIdentifierListLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("objectCreationRecordTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int objectCreationRecordTable_add(struct objectCreationRecordTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the objectCreationRecordTable table data set.
 * Adds a table row structure to the objectCreationRecordTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
objectCreationRecordTable_add(struct objectCreationRecordTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("objectCreationRecordTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index the data */
	/* logId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->logId, thedata->logIdLen);
	/* logRecordId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->logRecordId, thedata->logRecordIdLen);
	header_complex_add_data(&objectCreationRecordTableStorage, vars, thedata);
	DEBUGMSGTL(("objectCreationRecordTable", "registered an entry\n"));
	DEBUGMSGTL(("objectCreationRecordTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int objectCreationRecordTable_del(struct objectCreationRecordTable_data *thedata)
 * @brief delete a row structure from a table.
 * @param thedata pointer to the extracted or existing data structure in the table.
 * Deletes a table row structure from the objectCreationRecordTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
objectCreationRecordTable_del(struct objectCreationRecordTable_data *thedata)
{
	struct objectCreationRecordTable_data *StorageDel;

	DEBUGMSGTL(("objectCreationRecordTable", "deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(objectCreationRecordTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&objectCreationRecordTableStorage, hciptr);
	}
	DEBUGMSGTL(("objectCreationRecordTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_objectCreationRecordTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for objectCreationRecordTable entries.
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case objectCreationRecordTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_objectCreationRecordTable(const char *token, char *line)
{
	size_t tmpsize;
	struct objectCreationRecordTable_data *StorageTmp = objectCreationRecordTable_create();

	DEBUGMSGTL(("objectCreationRecordTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->logId, &StorageTmp->logIdLen);
	if (StorageTmp->logId == NULL) {
		config_perror("invalid specification for logId");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->logRecordId, &StorageTmp->logRecordIdLen);
	if (StorageTmp->logRecordId == NULL) {
		config_perror("invalid specification for logRecordId");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->objectCreationSourceIndicator, &tmpsize);
	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->objectCreationAttributeIdentifierList, &StorageTmp->objectCreationAttributeIdentifierListLen);
	if (StorageTmp->objectCreationAttributeIdentifierList == NULL) {
		config_perror("invalid specification for objectCreationAttributeIdentifierList");
		return;
	}
	objectCreationRecordTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("objectCreationRecordTable", "done.\n"));
}

/*
 * store_objectCreationRecordTable(): store configuraiton file for objectCreationRecordTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_objectCreationRecordTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct objectCreationRecordTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("objectCreationRecordTable", "storing data...  "));
	refresh_objectCreationRecordTable();
	(void) tmpsize;
	for (hcindex = objectCreationRecordTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct objectCreationRecordTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "objectCreationRecordTable ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->logId, &StorageTmp->logIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->logRecordId, &StorageTmp->logRecordIdLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->objectCreationSourceIndicator, &tmpsize);
			cptr = read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->objectCreationAttributeIdentifierList, &StorageTmp->objectCreationAttributeIdentifierListLen);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("objectCreationRecordTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct objectDeletionRecordTable_data *objectDeletionRecordTable_create(void)
 * @brief create a fresh data structure representing a new row in the objectDeletionRecordTable table.
 * Creates a new objectDeletionRecordTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct objectDeletionRecordTable_data *
objectDeletionRecordTable_create(void)
{
	struct objectDeletionRecordTable_data *StorageNew = SNMP_MALLOC_STRUCT(objectDeletionRecordTable_data);

	DEBUGMSGTL(("objectDeletionRecordTable", "creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */

	}
	DEBUGMSGTL(("objectDeletionRecordTable", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct objectDeletionRecordTable_data *objectDeletionRecordTable_duplicate(struct objectDeletionRecordTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicat a row structure for a table.
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct objectDeletionRecordTable_data *
objectDeletionRecordTable_duplicate(struct objectDeletionRecordTable_data *thedata)
{
	struct objectDeletionRecordTable_data *StorageNew = SNMP_MALLOC_STRUCT(objectDeletionRecordTable_data);

	DEBUGMSGTL(("objectDeletionRecordTable", "duplicating row...  "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("objectDeletionRecordTable", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	objectDeletionRecordTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int objectDeletionRecordTable_destroy(struct objectDeletionRecordTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
objectDeletionRecordTable_destroy(struct objectDeletionRecordTable_data **thedata)
{
	struct objectDeletionRecordTable_data *StorageDel;

	DEBUGMSGTL(("objectDeletionRecordTable", "deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->logId);
		StorageDel->logIdLen = 0;
		SNMP_FREE(StorageDel->logRecordId);
		StorageDel->logRecordIdLen = 0;
		SNMP_FREE(StorageDel->objectDeletionAttributeIdentifierList);
		StorageDel->objectDeletionAttributeIdentifierListLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("objectDeletionRecordTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int objectDeletionRecordTable_add(struct objectDeletionRecordTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the objectDeletionRecordTable table data set.
 * Adds a table row structure to the objectDeletionRecordTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
objectDeletionRecordTable_add(struct objectDeletionRecordTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("objectDeletionRecordTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index the data */
	/* logId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->logId, thedata->logIdLen);
	/* logRecordId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->logRecordId, thedata->logRecordIdLen);
	header_complex_add_data(&objectDeletionRecordTableStorage, vars, thedata);
	DEBUGMSGTL(("objectDeletionRecordTable", "registered an entry\n"));
	DEBUGMSGTL(("objectDeletionRecordTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int objectDeletionRecordTable_del(struct objectDeletionRecordTable_data *thedata)
 * @brief delete a row structure from a table.
 * @param thedata pointer to the extracted or existing data structure in the table.
 * Deletes a table row structure from the objectDeletionRecordTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
objectDeletionRecordTable_del(struct objectDeletionRecordTable_data *thedata)
{
	struct objectDeletionRecordTable_data *StorageDel;

	DEBUGMSGTL(("objectDeletionRecordTable", "deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(objectDeletionRecordTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&objectDeletionRecordTableStorage, hciptr);
	}
	DEBUGMSGTL(("objectDeletionRecordTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_objectDeletionRecordTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for objectDeletionRecordTable entries.
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case objectDeletionRecordTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_objectDeletionRecordTable(const char *token, char *line)
{
	size_t tmpsize;
	struct objectDeletionRecordTable_data *StorageTmp = objectDeletionRecordTable_create();

	DEBUGMSGTL(("objectDeletionRecordTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->logId, &StorageTmp->logIdLen);
	if (StorageTmp->logId == NULL) {
		config_perror("invalid specification for logId");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->logRecordId, &StorageTmp->logRecordIdLen);
	if (StorageTmp->logRecordId == NULL) {
		config_perror("invalid specification for logRecordId");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->objectDeletionSourceIndicator, &tmpsize);
	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->objectDeletionAttributeIdentifierList, &StorageTmp->objectDeletionAttributeIdentifierListLen);
	if (StorageTmp->objectDeletionAttributeIdentifierList == NULL) {
		config_perror("invalid specification for objectDeletionAttributeIdentifierList");
		return;
	}
	objectDeletionRecordTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("objectDeletionRecordTable", "done.\n"));
}

/*
 * store_objectDeletionRecordTable(): store configuraiton file for objectDeletionRecordTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_objectDeletionRecordTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct objectDeletionRecordTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("objectDeletionRecordTable", "storing data...  "));
	refresh_objectDeletionRecordTable();
	(void) tmpsize;
	for (hcindex = objectDeletionRecordTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct objectDeletionRecordTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "objectDeletionRecordTable ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->logId, &StorageTmp->logIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->logRecordId, &StorageTmp->logRecordIdLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->objectDeletionSourceIndicator, &tmpsize);
			cptr = read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->objectDeletionAttributeIdentifierList, &StorageTmp->objectDeletionAttributeIdentifierListLen);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("objectDeletionRecordTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct relationshipChangeRecordTable_data *relationshipChangeRecordTable_create(void)
 * @brief create a fresh data structure representing a new row in the relationshipChangeRecordTable table.
 * Creates a new relationshipChangeRecordTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct relationshipChangeRecordTable_data *
relationshipChangeRecordTable_create(void)
{
	struct relationshipChangeRecordTable_data *StorageNew = SNMP_MALLOC_STRUCT(relationshipChangeRecordTable_data);

	DEBUGMSGTL(("relationshipChangeRecordTable", "creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */

	}
	DEBUGMSGTL(("relationshipChangeRecordTable", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct relationshipChangeRecordTable_data *relationshipChangeRecordTable_duplicate(struct relationshipChangeRecordTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicat a row structure for a table.
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct relationshipChangeRecordTable_data *
relationshipChangeRecordTable_duplicate(struct relationshipChangeRecordTable_data *thedata)
{
	struct relationshipChangeRecordTable_data *StorageNew = SNMP_MALLOC_STRUCT(relationshipChangeRecordTable_data);

	DEBUGMSGTL(("relationshipChangeRecordTable", "duplicating row...  "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("relationshipChangeRecordTable", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	relationshipChangeRecordTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int relationshipChangeRecordTable_destroy(struct relationshipChangeRecordTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
relationshipChangeRecordTable_destroy(struct relationshipChangeRecordTable_data **thedata)
{
	struct relationshipChangeRecordTable_data *StorageDel;

	DEBUGMSGTL(("relationshipChangeRecordTable", "deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->logId);
		StorageDel->logIdLen = 0;
		SNMP_FREE(StorageDel->logRecordId);
		StorageDel->logRecordIdLen = 0;
		SNMP_FREE(StorageDel->relationshipChangeRecordDefinition);
		StorageDel->relationshipChangeRecordDefinitionLen = 0;
		SNMP_FREE(StorageDel->relationshipChangeAttributeIdentifierList);
		StorageDel->relationshipChangeAttributeIdentifierListLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("relationshipChangeRecordTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int relationshipChangeRecordTable_add(struct relationshipChangeRecordTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the relationshipChangeRecordTable table data set.
 * Adds a table row structure to the relationshipChangeRecordTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
relationshipChangeRecordTable_add(struct relationshipChangeRecordTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("relationshipChangeRecordTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index the data */
	/* logId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->logId, thedata->logIdLen);
	/* logRecordId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->logRecordId, thedata->logRecordIdLen);
	header_complex_add_data(&relationshipChangeRecordTableStorage, vars, thedata);
	DEBUGMSGTL(("relationshipChangeRecordTable", "registered an entry\n"));
	DEBUGMSGTL(("relationshipChangeRecordTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int relationshipChangeRecordTable_del(struct relationshipChangeRecordTable_data *thedata)
 * @brief delete a row structure from a table.
 * @param thedata pointer to the extracted or existing data structure in the table.
 * Deletes a table row structure from the relationshipChangeRecordTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
relationshipChangeRecordTable_del(struct relationshipChangeRecordTable_data *thedata)
{
	struct relationshipChangeRecordTable_data *StorageDel;

	DEBUGMSGTL(("relationshipChangeRecordTable", "deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(relationshipChangeRecordTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&relationshipChangeRecordTableStorage, hciptr);
	}
	DEBUGMSGTL(("relationshipChangeRecordTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_relationshipChangeRecordTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for relationshipChangeRecordTable entries.
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case relationshipChangeRecordTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_relationshipChangeRecordTable(const char *token, char *line)
{
	size_t tmpsize;
	struct relationshipChangeRecordTable_data *StorageTmp = relationshipChangeRecordTable_create();

	DEBUGMSGTL(("relationshipChangeRecordTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->logId, &StorageTmp->logIdLen);
	if (StorageTmp->logId == NULL) {
		config_perror("invalid specification for logId");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->logRecordId, &StorageTmp->logRecordIdLen);
	if (StorageTmp->logRecordId == NULL) {
		config_perror("invalid specification for logRecordId");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->relationshipChangeRecordDefinition, &StorageTmp->relationshipChangeRecordDefinitionLen);
	if (StorageTmp->relationshipChangeRecordDefinition == NULL) {
		config_perror("invalid specification for relationshipChangeRecordDefinition");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->relationshipChangeSourceIndicator, &tmpsize);
	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->relationshipChangeAttributeIdentifierList, &StorageTmp->relationshipChangeAttributeIdentifierListLen);
	if (StorageTmp->relationshipChangeAttributeIdentifierList == NULL) {
		config_perror("invalid specification for relationshipChangeAttributeIdentifierList");
		return;
	}
	relationshipChangeRecordTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("relationshipChangeRecordTable", "done.\n"));
}

/*
 * store_relationshipChangeRecordTable(): store configuraiton file for relationshipChangeRecordTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_relationshipChangeRecordTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct relationshipChangeRecordTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("relationshipChangeRecordTable", "storing data...  "));
	refresh_relationshipChangeRecordTable();
	(void) tmpsize;
	for (hcindex = relationshipChangeRecordTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct relationshipChangeRecordTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "relationshipChangeRecordTable ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->logId, &StorageTmp->logIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->logRecordId, &StorageTmp->logRecordIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->relationshipChangeRecordDefinition, &StorageTmp->relationshipChangeRecordDefinitionLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->relationshipChangeSourceIndicator, &tmpsize);
			cptr = read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->relationshipChangeAttributeIdentifierList, &StorageTmp->relationshipChangeAttributeIdentifierListLen);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("relationshipChangeRecordTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct securityAlarmReportRecordTable_data *securityAlarmReportRecordTable_create(void)
 * @brief create a fresh data structure representing a new row in the securityAlarmReportRecordTable table.
 * Creates a new securityAlarmReportRecordTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct securityAlarmReportRecordTable_data *
securityAlarmReportRecordTable_create(void)
{
	struct securityAlarmReportRecordTable_data *StorageNew = SNMP_MALLOC_STRUCT(securityAlarmReportRecordTable_data);

	DEBUGMSGTL(("securityAlarmReportRecordTable", "creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */

	}
	DEBUGMSGTL(("securityAlarmReportRecordTable", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct securityAlarmReportRecordTable_data *securityAlarmReportRecordTable_duplicate(struct securityAlarmReportRecordTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicat a row structure for a table.
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct securityAlarmReportRecordTable_data *
securityAlarmReportRecordTable_duplicate(struct securityAlarmReportRecordTable_data *thedata)
{
	struct securityAlarmReportRecordTable_data *StorageNew = SNMP_MALLOC_STRUCT(securityAlarmReportRecordTable_data);

	DEBUGMSGTL(("securityAlarmReportRecordTable", "duplicating row...  "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("securityAlarmReportRecordTable", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	securityAlarmReportRecordTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int securityAlarmReportRecordTable_destroy(struct securityAlarmReportRecordTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
securityAlarmReportRecordTable_destroy(struct securityAlarmReportRecordTable_data **thedata)
{
	struct securityAlarmReportRecordTable_data *StorageDel;

	DEBUGMSGTL(("securityAlarmReportRecordTable", "deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->logId);
		StorageDel->logIdLen = 0;
		SNMP_FREE(StorageDel->logRecordId);
		StorageDel->logRecordIdLen = 0;
		SNMP_FREE(StorageDel->securityAlarmRecordCause);
		StorageDel->securityAlarmRecordCauseLen = 0;
		SNMP_FREE(StorageDel->securityAlarmRecordDetector);
		StorageDel->securityAlarmRecordDetectorLen = 0;
		SNMP_FREE(StorageDel->securityServiceUser);
		StorageDel->securityServiceUserLen = 0;
		SNMP_FREE(StorageDel->securityServiceProvider);
		StorageDel->securityServiceProviderLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("securityAlarmReportRecordTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int securityAlarmReportRecordTable_add(struct securityAlarmReportRecordTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the securityAlarmReportRecordTable table data set.
 * Adds a table row structure to the securityAlarmReportRecordTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
securityAlarmReportRecordTable_add(struct securityAlarmReportRecordTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("securityAlarmReportRecordTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index the data */
	/* logId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->logId, thedata->logIdLen);
	/* logRecordId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->logRecordId, thedata->logRecordIdLen);
	header_complex_add_data(&securityAlarmReportRecordTableStorage, vars, thedata);
	DEBUGMSGTL(("securityAlarmReportRecordTable", "registered an entry\n"));
	DEBUGMSGTL(("securityAlarmReportRecordTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int securityAlarmReportRecordTable_del(struct securityAlarmReportRecordTable_data *thedata)
 * @brief delete a row structure from a table.
 * @param thedata pointer to the extracted or existing data structure in the table.
 * Deletes a table row structure from the securityAlarmReportRecordTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
securityAlarmReportRecordTable_del(struct securityAlarmReportRecordTable_data *thedata)
{
	struct securityAlarmReportRecordTable_data *StorageDel;

	DEBUGMSGTL(("securityAlarmReportRecordTable", "deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(securityAlarmReportRecordTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&securityAlarmReportRecordTableStorage, hciptr);
	}
	DEBUGMSGTL(("securityAlarmReportRecordTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_securityAlarmReportRecordTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for securityAlarmReportRecordTable entries.
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case securityAlarmReportRecordTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_securityAlarmReportRecordTable(const char *token, char *line)
{
	size_t tmpsize;
	struct securityAlarmReportRecordTable_data *StorageTmp = securityAlarmReportRecordTable_create();

	DEBUGMSGTL(("securityAlarmReportRecordTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->logId, &StorageTmp->logIdLen);
	if (StorageTmp->logId == NULL) {
		config_perror("invalid specification for logId");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->logRecordId, &StorageTmp->logRecordIdLen);
	if (StorageTmp->logRecordId == NULL) {
		config_perror("invalid specification for logRecordId");
		return;
	}
	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->securityAlarmRecordCause, &StorageTmp->securityAlarmRecordCauseLen);
	if (StorageTmp->securityAlarmRecordCause == NULL) {
		config_perror("invalid specification for securityAlarmRecordCause");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->securityAlarmRecordSeverity, &tmpsize);
	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->securityAlarmRecordDetector, &StorageTmp->securityAlarmRecordDetectorLen);
	if (StorageTmp->securityAlarmRecordDetector == NULL) {
		config_perror("invalid specification for securityAlarmRecordDetector");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->securityServiceUser, &StorageTmp->securityServiceUserLen);
	if (StorageTmp->securityServiceUser == NULL) {
		config_perror("invalid specification for securityServiceUser");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->securityServiceProvider, &StorageTmp->securityServiceProviderLen);
	if (StorageTmp->securityServiceProvider == NULL) {
		config_perror("invalid specification for securityServiceProvider");
		return;
	}
	securityAlarmReportRecordTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("securityAlarmReportRecordTable", "done.\n"));
}

/*
 * store_securityAlarmReportRecordTable(): store configuraiton file for securityAlarmReportRecordTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_securityAlarmReportRecordTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct securityAlarmReportRecordTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("securityAlarmReportRecordTable", "storing data...  "));
	refresh_securityAlarmReportRecordTable();
	(void) tmpsize;
	for (hcindex = securityAlarmReportRecordTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct securityAlarmReportRecordTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "securityAlarmReportRecordTable ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->logId, &StorageTmp->logIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->logRecordId, &StorageTmp->logRecordIdLen);
			cptr = read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->securityAlarmRecordCause, &StorageTmp->securityAlarmRecordCauseLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->securityAlarmRecordSeverity, &tmpsize);
			cptr = read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->securityAlarmRecordDetector, &StorageTmp->securityAlarmRecordDetectorLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->securityServiceUser, &StorageTmp->securityServiceUserLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->securityServiceProvider, &StorageTmp->securityServiceProviderLen);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("securityAlarmReportRecordTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct stateChangeRecordTable_data *stateChangeRecordTable_create(void)
 * @brief create a fresh data structure representing a new row in the stateChangeRecordTable table.
 * Creates a new stateChangeRecordTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct stateChangeRecordTable_data *
stateChangeRecordTable_create(void)
{
	struct stateChangeRecordTable_data *StorageNew = SNMP_MALLOC_STRUCT(stateChangeRecordTable_data);

	DEBUGMSGTL(("stateChangeRecordTable", "creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */

	}
	DEBUGMSGTL(("stateChangeRecordTable", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct stateChangeRecordTable_data *stateChangeRecordTable_duplicate(struct stateChangeRecordTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicat a row structure for a table.
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct stateChangeRecordTable_data *
stateChangeRecordTable_duplicate(struct stateChangeRecordTable_data *thedata)
{
	struct stateChangeRecordTable_data *StorageNew = SNMP_MALLOC_STRUCT(stateChangeRecordTable_data);

	DEBUGMSGTL(("stateChangeRecordTable", "duplicating row...  "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("stateChangeRecordTable", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	stateChangeRecordTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int stateChangeRecordTable_destroy(struct stateChangeRecordTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
stateChangeRecordTable_destroy(struct stateChangeRecordTable_data **thedata)
{
	struct stateChangeRecordTable_data *StorageDel;

	DEBUGMSGTL(("stateChangeRecordTable", "deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->logId);
		StorageDel->logIdLen = 0;
		SNMP_FREE(StorageDel->logRecordId);
		StorageDel->logRecordIdLen = 0;
		SNMP_FREE(StorageDel->stateChangeRecordDefinition);
		StorageDel->stateChangeRecordDefinitionLen = 0;
		SNMP_FREE(StorageDel->stateChangeAttributeIdentifierList);
		StorageDel->stateChangeAttributeIdentifierListLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("stateChangeRecordTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int stateChangeRecordTable_add(struct stateChangeRecordTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the stateChangeRecordTable table data set.
 * Adds a table row structure to the stateChangeRecordTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
stateChangeRecordTable_add(struct stateChangeRecordTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("stateChangeRecordTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index the data */
	/* logId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->logId, thedata->logIdLen);
	/* logRecordId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->logRecordId, thedata->logRecordIdLen);
	header_complex_add_data(&stateChangeRecordTableStorage, vars, thedata);
	DEBUGMSGTL(("stateChangeRecordTable", "registered an entry\n"));
	DEBUGMSGTL(("stateChangeRecordTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int stateChangeRecordTable_del(struct stateChangeRecordTable_data *thedata)
 * @brief delete a row structure from a table.
 * @param thedata pointer to the extracted or existing data structure in the table.
 * Deletes a table row structure from the stateChangeRecordTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
stateChangeRecordTable_del(struct stateChangeRecordTable_data *thedata)
{
	struct stateChangeRecordTable_data *StorageDel;

	DEBUGMSGTL(("stateChangeRecordTable", "deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(stateChangeRecordTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&stateChangeRecordTableStorage, hciptr);
	}
	DEBUGMSGTL(("stateChangeRecordTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_stateChangeRecordTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for stateChangeRecordTable entries.
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case stateChangeRecordTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_stateChangeRecordTable(const char *token, char *line)
{
	size_t tmpsize;
	struct stateChangeRecordTable_data *StorageTmp = stateChangeRecordTable_create();

	DEBUGMSGTL(("stateChangeRecordTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->logId, &StorageTmp->logIdLen);
	if (StorageTmp->logId == NULL) {
		config_perror("invalid specification for logId");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->logRecordId, &StorageTmp->logRecordIdLen);
	if (StorageTmp->logRecordId == NULL) {
		config_perror("invalid specification for logRecordId");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->stateChangeRecordDefinition, &StorageTmp->stateChangeRecordDefinitionLen);
	if (StorageTmp->stateChangeRecordDefinition == NULL) {
		config_perror("invalid specification for stateChangeRecordDefinition");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->stateChangeSourceIndicator, &tmpsize);
	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->stateChangeAttributeIdentifierList, &StorageTmp->stateChangeAttributeIdentifierListLen);
	if (StorageTmp->stateChangeAttributeIdentifierList == NULL) {
		config_perror("invalid specification for stateChangeAttributeIdentifierList");
		return;
	}
	stateChangeRecordTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("stateChangeRecordTable", "done.\n"));
}

/*
 * store_stateChangeRecordTable(): store configuraiton file for stateChangeRecordTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_stateChangeRecordTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct stateChangeRecordTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("stateChangeRecordTable", "storing data...  "));
	refresh_stateChangeRecordTable();
	(void) tmpsize;
	for (hcindex = stateChangeRecordTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct stateChangeRecordTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "stateChangeRecordTable ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->logId, &StorageTmp->logIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->logRecordId, &StorageTmp->logRecordIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->stateChangeRecordDefinition, &StorageTmp->stateChangeRecordDefinitionLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->stateChangeSourceIndicator, &tmpsize);
			cptr = read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->stateChangeAttributeIdentifierList, &StorageTmp->stateChangeAttributeIdentifierListLen);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("stateChangeRecordTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void refresh_discriminatorTable(void)
 * @brief refresh the scalar values of the discriminatorTable.
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_discriminatorTable(void)
{
	if (discriminatorTable_refresh == 0)
		return;
	discriminatorTable_refresh = 0;
	/* XXX: Here, update the table as required... */
}

/**
 * @fn void refresh_discriminatorTable_row(struct discriminatorTable_data *StorageTmp)
 * @brief refresh the contents of the discriminatorTable row.
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
void
refresh_discriminatorTable_row(struct discriminatorTable_data *StorageTmp)
{
	if (StorageTmp->discriminatorTable_request == sa_request)
		return;
	StorageTmp->discriminatorTable_request = sa_request;
}

/**
 * @fn u_char *var_discriminatorTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in discriminatorTable.
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_smiMIB above.
 */
u_char *
var_discriminatorTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct discriminatorTable_data *StorageTmp = NULL;

	DEBUGMSGTL(("smiMIB", "var_discriminatorTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_discriminatorTable();
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	if ((StorageTmp = header_complex(discriminatorTableStorage, vp, name, length, exact, var_len, write_method)) == NULL)
		return NULL;
	refresh_discriminatorTable_row(StorageTmp);
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case DISCRIMINATORCONSTRUCT:
		*write_method = write_discriminatorConstruct;
		*var_len = StorageTmp->discriminatorConstructLen;
		return (u_char *) StorageTmp->discriminatorConstruct;
	case DISCRIMINATORADMINISTRATIVESTATE:
		*write_method = write_discriminatorAdministrativeState;
		*var_len = sizeof(StorageTmp->discriminatorAdministrativeState);
		return (u_char *) &StorageTmp->discriminatorAdministrativeState;
	case DISCRIMINATOROPERATIONALSTATE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->discriminatorOperationalState);
		return (u_char *) &StorageTmp->discriminatorOperationalState;
	case DISCRIMINATORAVAILABILITYSTATUS:
		*write_method = NULL;	/* read-only */
		*var_len = StorageTmp->discriminatorAvailabilityStatusLen;
		return (u_char *) StorageTmp->discriminatorAvailabilityStatus;
	case DISCRIMINATORSTARTTIME:
		*write_method = write_discriminatorStartTime;
		*var_len = sizeof(StorageTmp->discriminatorStartTime);
		return (u_char *) &StorageTmp->discriminatorStartTime;
	case DISCRIMINATORSTOPTIME:
		*write_method = write_discriminatorStopTime;
		*var_len = sizeof(StorageTmp->discriminatorStopTime);
		return (u_char *) &StorageTmp->discriminatorStopTime;
	case DISCRIMINATORINTERVALSOFDAY:
		*write_method = write_discriminatorIntervalsOfDay;
		*var_len = StorageTmp->discriminatorIntervalsOfDayLen;
		return (u_char *) StorageTmp->discriminatorIntervalsOfDay;
	case DISCRIMINATORWEEKMASK:
		*write_method = write_discriminatorWeekMask;
		*var_len = StorageTmp->discriminatorWeekMaskLen;
		return (u_char *) StorageTmp->discriminatorWeekMask;
	case DISCRIMINATORSCHEDULERNAME:
		*write_method = write_discriminatorSchedulerName;
		*var_len = StorageTmp->discriminatorSchedulerNameLen;
		return (u_char *) StorageTmp->discriminatorSchedulerName;
	case DISCRIMINATORENTRYSTATUS:
		*write_method = write_discriminatorEntryStatus;
		*var_len = sizeof(StorageTmp->discriminatorEntryStatus);
		return (u_char *) &StorageTmp->discriminatorEntryStatus;
	default:
		ERROR_MSG("");
	}
	return NULL;
}

/**
 * @fn void refresh_eventForwardingDiscriminatorTable(void)
 * @brief refresh the scalar values of the eventForwardingDiscriminatorTable.
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_eventForwardingDiscriminatorTable(void)
{
	if (eventForwardingDiscriminatorTable_refresh == 0)
		return;
	eventForwardingDiscriminatorTable_refresh = 0;
	/* XXX: Here, update the table as required... */
}

/**
 * @fn void refresh_eventForwardingDiscriminatorTable_row(struct eventForwardingDiscriminatorTable_data *StorageTmp)
 * @brief refresh the contents of the eventForwardingDiscriminatorTable row.
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
void
refresh_eventForwardingDiscriminatorTable_row(struct eventForwardingDiscriminatorTable_data *StorageTmp)
{
	if (StorageTmp->eventForwardingDiscriminatorTable_request == sa_request)
		return;
	StorageTmp->eventForwardingDiscriminatorTable_request = sa_request;
}

/**
 * @fn u_char *var_eventForwardingDiscriminatorTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in eventForwardingDiscriminatorTable.
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_smiMIB above.
 */
u_char *
var_eventForwardingDiscriminatorTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct eventForwardingDiscriminatorTable_data *StorageTmp = NULL;

	DEBUGMSGTL(("smiMIB", "var_eventForwardingDiscriminatorTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_eventForwardingDiscriminatorTable();
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	if ((StorageTmp = header_complex(eventForwardingDiscriminatorTableStorage, vp, name, length, exact, var_len, write_method)) == NULL)
		return NULL;
	refresh_eventForwardingDiscriminatorTable_row(StorageTmp);
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case DESTINATION:
		*write_method = write_destination;
		*var_len = StorageTmp->destinationLen;
		return (u_char *) StorageTmp->destination;
	case ACTIVEDESTINATION:
		*write_method = write_activeDestination;
		*var_len = StorageTmp->activeDestinationLen;
		return (u_char *) StorageTmp->activeDestination;
	case BACKUPDESTINATIONLIST:
		*write_method = write_backUpDestinationList;
		*var_len = StorageTmp->backUpDestinationListLen;
		return (u_char *) StorageTmp->backUpDestinationList;
	case CONFIRMEDMODE:
		*write_method = write_confirmedMode;
		*var_len = sizeof(StorageTmp->confirmedMode);
		return (u_char *) &StorageTmp->confirmedMode;
	case EVENTFORWARDINGDISCRIMINATORENTRYSTATUS:
		*write_method = write_eventForwardingDiscriminatorEntryStatus;
		*var_len = sizeof(StorageTmp->eventForwardingDiscriminatorEntryStatus);
		return (u_char *) &StorageTmp->eventForwardingDiscriminatorEntryStatus;
	default:
		ERROR_MSG("");
	}
	return NULL;
}

/**
 * @fn void refresh_logTable(void)
 * @brief refresh the scalar values of the logTable.
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_logTable(void)
{
	if (logTable_refresh == 0)
		return;
	logTable_refresh = 0;
	/* XXX: Here, update the table as required... */
}

/**
 * @fn void refresh_logTable_row(struct logTable_data *StorageTmp)
 * @brief refresh the contents of the logTable row.
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
void
refresh_logTable_row(struct logTable_data *StorageTmp)
{
	if (StorageTmp->logTable_request == sa_request)
		return;
	StorageTmp->logTable_request = sa_request;
}

/**
 * @fn u_char *var_logTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in logTable.
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_smiMIB above.
 */
u_char *
var_logTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct logTable_data *StorageTmp = NULL;

	DEBUGMSGTL(("smiMIB", "var_logTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_logTable();
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	if ((StorageTmp = header_complex(logTableStorage, vp, name, length, exact, var_len, write_method)) == NULL)
		return NULL;
	refresh_logTable_row(StorageTmp);
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case LOGDISCRIMINATORCONSTRUCT:
		*write_method = write_logDiscriminatorConstruct;
		*var_len = StorageTmp->logDiscriminatorConstructLen;
		return (u_char *) StorageTmp->logDiscriminatorConstruct;
	case LOGADMINISTRATIVESTATE:
		*write_method = write_logAdministrativeState;
		*var_len = sizeof(StorageTmp->logAdministrativeState);
		return (u_char *) &StorageTmp->logAdministrativeState;
	case LOGOPERATIONALSTATE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->logOperationalState);
		return (u_char *) &StorageTmp->logOperationalState;
	case LOGAVAILABILITYSTATUS:
		*write_method = write_logAvailabilityStatus;
		*var_len = StorageTmp->logAvailabilityStatusLen;
		return (u_char *) StorageTmp->logAvailabilityStatus;
	case LOGFULLACTION:
		*write_method = write_logFullAction;
		*var_len = sizeof(StorageTmp->logFullAction);
		return (u_char *) &StorageTmp->logFullAction;
	case MAXLOGSIZE:
		*write_method = write_maxLogSize;
		*var_len = sizeof(StorageTmp->maxLogSize);
		return (u_char *) &StorageTmp->maxLogSize;
	case CURRENTLOGSIZE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->currentLogSize);
		return (u_char *) &StorageTmp->currentLogSize;
	case NUMBEROFRECORDS:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->numberOfRecords);
		return (u_char *) &StorageTmp->numberOfRecords;
	case CAPACITYALARMTHRESHOLD:
		*write_method = write_capacityAlarmThreshold;
		*var_len = StorageTmp->capacityAlarmThresholdLen;
		return (u_char *) StorageTmp->capacityAlarmThreshold;
	case LOGSTARTTIME:
		*write_method = write_logStartTime;
		*var_len = sizeof(StorageTmp->logStartTime);
		return (u_char *) &StorageTmp->logStartTime;
	case LOGSTOPTIME:
		*write_method = write_logStopTime;
		*var_len = sizeof(StorageTmp->logStopTime);
		return (u_char *) &StorageTmp->logStopTime;
	case LOGINTERVALSOFDAY:
		*write_method = write_logIntervalsOfDay;
		*var_len = StorageTmp->logIntervalsOfDayLen;
		return (u_char *) StorageTmp->logIntervalsOfDay;
	case LOGWEEKMASK:
		*write_method = write_logWeekMask;
		*var_len = StorageTmp->logWeekMaskLen;
		return (u_char *) StorageTmp->logWeekMask;
	case LOGSCHEDULERNAME:
		*write_method = write_logSchedulerName;
		*var_len = StorageTmp->logSchedulerNameLen;
		return (u_char *) StorageTmp->logSchedulerName;
	case LOGENTRYSTATUS:
		*write_method = write_logEntryStatus;
		*var_len = sizeof(StorageTmp->logEntryStatus);
		return (u_char *) &StorageTmp->logEntryStatus;
	default:
		ERROR_MSG("");
	}
	return NULL;
}

/**
 * @fn void refresh_logRecordTable(void)
 * @brief refresh the scalar values of the logRecordTable.
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_logRecordTable(void)
{
	if (logRecordTable_refresh == 0)
		return;
	logRecordTable_refresh = 0;
	/* XXX: Here, update the table as required... */
}

/**
 * @fn void refresh_logRecordTable_row(struct logRecordTable_data *StorageTmp)
 * @brief refresh the contents of the logRecordTable row.
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
void
refresh_logRecordTable_row(struct logRecordTable_data *StorageTmp)
{
	if (StorageTmp->logRecordTable_request == sa_request)
		return;
	StorageTmp->logRecordTable_request = sa_request;
}

/**
 * @fn u_char *var_logRecordTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in logRecordTable.
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_smiMIB above.
 */
u_char *
var_logRecordTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct logRecordTable_data *StorageTmp = NULL;

	DEBUGMSGTL(("smiMIB", "var_logRecordTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_logRecordTable();
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	if ((StorageTmp = header_complex(logRecordTableStorage, vp, name, length, exact, var_len, write_method)) == NULL)
		return NULL;
	refresh_logRecordTable_row(StorageTmp);
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case LOGGINGTIME:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->loggingTime);
		return (u_char *) &StorageTmp->loggingTime;
	case LOGRECORDENTRYSTATUS:
		*write_method = write_logRecordEntryStatus;
		*var_len = sizeof(StorageTmp->logRecordEntryStatus);
		return (u_char *) &StorageTmp->logRecordEntryStatus;
	default:
		ERROR_MSG("");
	}
	return NULL;
}

/**
 * @fn void refresh_eventLogRecordTable(void)
 * @brief refresh the scalar values of the eventLogRecordTable.
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_eventLogRecordTable(void)
{
	if (eventLogRecordTable_refresh == 0)
		return;
	eventLogRecordTable_refresh = 0;
	/* XXX: Here, update the table as required... */
}

/**
 * @fn void refresh_eventLogRecordTable_row(struct eventLogRecordTable_data *StorageTmp)
 * @brief refresh the contents of the eventLogRecordTable row.
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
void
refresh_eventLogRecordTable_row(struct eventLogRecordTable_data *StorageTmp)
{
	if (StorageTmp->eventLogRecordTable_request == sa_request)
		return;
	StorageTmp->eventLogRecordTable_request = sa_request;
}

/**
 * @fn u_char *var_eventLogRecordTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in eventLogRecordTable.
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_smiMIB above.
 */
u_char *
var_eventLogRecordTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct eventLogRecordTable_data *StorageTmp = NULL;

	DEBUGMSGTL(("smiMIB", "var_eventLogRecordTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_eventLogRecordTable();
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	if ((StorageTmp = header_complex(eventLogRecordTableStorage, vp, name, length, exact, var_len, write_method)) == NULL)
		return NULL;
	refresh_eventLogRecordTable_row(StorageTmp);
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case MANAGEDOBJECTCLASS:
		*write_method = NULL;	/* read-only */
		*var_len = StorageTmp->managedObjectClassLen;
		return (u_char *) StorageTmp->managedObjectClass;
	case MANAGEDOBJECTINSTANCE:
		*write_method = NULL;	/* read-only */
		*var_len = StorageTmp->managedObjectInstanceLen;
		return (u_char *) StorageTmp->managedObjectInstance;
	case EVENTTYPE:
		*write_method = NULL;	/* read-only */
		*var_len = StorageTmp->eventTypeLen;
		return (u_char *) StorageTmp->eventType;
	case EVENTTIMES:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->eventTimes);
		return (u_char *) &StorageTmp->eventTimes;
	case EVENTNOTIFICATIONIDENTIFIER:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->eventNotificationIdentifier);
		return (u_char *) &StorageTmp->eventNotificationIdentifier;
	case EVENTCORRELATEDNOTIFICATIONS:
		*write_method = NULL;	/* read-only */
		*var_len = StorageTmp->eventCorrelatedNotificationsLen;
		return (u_char *) StorageTmp->eventCorrelatedNotifications;
	case EVENTADDITIONALTEXT:
		*write_method = NULL;	/* read-only */
		*var_len = StorageTmp->eventAdditionalTextLen;
		return (u_char *) StorageTmp->eventAdditionalText;
	case EVENTADDITIONALINFORMATION:
		*write_method = NULL;	/* read-only */
		*var_len = StorageTmp->eventAdditionalInformationLen;
		return (u_char *) StorageTmp->eventAdditionalInformation;
	default:
		ERROR_MSG("");
	}
	return NULL;
}

/**
 * @fn void refresh_alarmRecordTable(void)
 * @brief refresh the scalar values of the alarmRecordTable.
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_alarmRecordTable(void)
{
	if (alarmRecordTable_refresh == 0)
		return;
	alarmRecordTable_refresh = 0;
	/* XXX: Here, update the table as required... */
}

/**
 * @fn void refresh_alarmRecordTable_row(struct alarmRecordTable_data *StorageTmp)
 * @brief refresh the contents of the alarmRecordTable row.
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
void
refresh_alarmRecordTable_row(struct alarmRecordTable_data *StorageTmp)
{
	if (StorageTmp->alarmRecordTable_request == sa_request)
		return;
	StorageTmp->alarmRecordTable_request = sa_request;
}

/**
 * @fn u_char *var_alarmRecordTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in alarmRecordTable.
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_smiMIB above.
 */
u_char *
var_alarmRecordTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct alarmRecordTable_data *StorageTmp = NULL;

	DEBUGMSGTL(("smiMIB", "var_alarmRecordTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_alarmRecordTable();
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	if ((StorageTmp = header_complex(alarmRecordTableStorage, vp, name, length, exact, var_len, write_method)) == NULL)
		return NULL;
	refresh_alarmRecordTable_row(StorageTmp);
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case ALARMRECORDPROBABLECAUSE:
		*write_method = NULL;	/* read-only */
		*var_len = StorageTmp->alarmRecordProbableCauseLen;
		return (u_char *) StorageTmp->alarmRecordProbableCause;
	case ALARMRECORDPERCEIVEDSEVERITY:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->alarmRecordPerceivedSeverity);
		return (u_char *) &StorageTmp->alarmRecordPerceivedSeverity;
	case ALARMRECORDSPECIFICPROBLEMS:
		*write_method = NULL;	/* read-only */
		*var_len = StorageTmp->alarmRecordSpecificProblemsLen;
		return (u_char *) StorageTmp->alarmRecordSpecificProblems;
	case ALARMRECORDBACKEDUPSTATUS:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->alarmRecordBackedUpStatus);
		return (u_char *) &StorageTmp->alarmRecordBackedUpStatus;
	case ALARMRECORDBACKUPOBJECT:
		*write_method = NULL;	/* read-only */
		*var_len = StorageTmp->alarmRecordBackUpObjectLen;
		return (u_char *) StorageTmp->alarmRecordBackUpObject;
	case ALARMRECORDTRENDINDICATION:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->alarmRecordTrendIndication);
		return (u_char *) &StorageTmp->alarmRecordTrendIndication;
	case ALARMRECORDTHRESHOLDINFO:
		*write_method = NULL;	/* read-only */
		*var_len = StorageTmp->alarmRecordThresholdInfoLen;
		return (u_char *) StorageTmp->alarmRecordThresholdInfo;
	case ALARMRECORDSTATECHANGEDEFINITION:
		*write_method = NULL;	/* read-only */
		*var_len = StorageTmp->alarmRecordStateChangeDefinitionLen;
		return (u_char *) StorageTmp->alarmRecordStateChangeDefinition;
	case ALARMRECORDMONITOREDATTRIBUTES:
		*write_method = NULL;	/* read-only */
		*var_len = StorageTmp->alarmRecordMonitoredAttributesLen;
		return (u_char *) StorageTmp->alarmRecordMonitoredAttributes;
	case ALARMRECORDPROPOSEDREPAIRACTIONS:
		*write_method = NULL;	/* read-only */
		*var_len = StorageTmp->alarmRecordProposedRepairActionsLen;
		return (u_char *) StorageTmp->alarmRecordProposedRepairActions;
	default:
		ERROR_MSG("");
	}
	return NULL;
}

/**
 * @fn void refresh_attributeValueChangeRecordTable(void)
 * @brief refresh the scalar values of the attributeValueChangeRecordTable.
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_attributeValueChangeRecordTable(void)
{
	if (attributeValueChangeRecordTable_refresh == 0)
		return;
	attributeValueChangeRecordTable_refresh = 0;
	/* XXX: Here, update the table as required... */
}

/**
 * @fn void refresh_attributeValueChangeRecordTable_row(struct attributeValueChangeRecordTable_data *StorageTmp)
 * @brief refresh the contents of the attributeValueChangeRecordTable row.
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
void
refresh_attributeValueChangeRecordTable_row(struct attributeValueChangeRecordTable_data *StorageTmp)
{
	if (StorageTmp->attributeValueChangeRecordTable_request == sa_request)
		return;
	StorageTmp->attributeValueChangeRecordTable_request = sa_request;
}

/**
 * @fn u_char *var_attributeValueChangeRecordTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in attributeValueChangeRecordTable.
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_smiMIB above.
 */
u_char *
var_attributeValueChangeRecordTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct attributeValueChangeRecordTable_data *StorageTmp = NULL;

	DEBUGMSGTL(("smiMIB", "var_attributeValueChangeRecordTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_attributeValueChangeRecordTable();
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	if ((StorageTmp = header_complex(attributeValueChangeRecordTableStorage, vp, name, length, exact, var_len, write_method)) == NULL)
		return NULL;
	refresh_attributeValueChangeRecordTable_row(StorageTmp);
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case ATTRIBUTEVALUECHANGERECORDDEFINITION:
		*write_method = NULL;	/* read-only */
		*var_len = StorageTmp->attributeValueChangeRecordDefinitionLen;
		return (u_char *) StorageTmp->attributeValueChangeRecordDefinition;
	case ATTRIBUTEVALUECHANGESOURCEINDICATOR:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->attributeValueChangeSourceIndicator);
		return (u_char *) &StorageTmp->attributeValueChangeSourceIndicator;
	case ATTRIBUTEVALUECHANGEATTRIBUTEIDENTIFIERLIST:
		*write_method = NULL;	/* read-only */
		*var_len = StorageTmp->attributeValueChangeAttributeIdentifierListLen;
		return (u_char *) StorageTmp->attributeValueChangeAttributeIdentifierList;
	default:
		ERROR_MSG("");
	}
	return NULL;
}

/**
 * @fn void refresh_objectCreationRecordTable(void)
 * @brief refresh the scalar values of the objectCreationRecordTable.
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_objectCreationRecordTable(void)
{
	if (objectCreationRecordTable_refresh == 0)
		return;
	objectCreationRecordTable_refresh = 0;
	/* XXX: Here, update the table as required... */
}

/**
 * @fn void refresh_objectCreationRecordTable_row(struct objectCreationRecordTable_data *StorageTmp)
 * @brief refresh the contents of the objectCreationRecordTable row.
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
void
refresh_objectCreationRecordTable_row(struct objectCreationRecordTable_data *StorageTmp)
{
	if (StorageTmp->objectCreationRecordTable_request == sa_request)
		return;
	StorageTmp->objectCreationRecordTable_request = sa_request;
}

/**
 * @fn u_char *var_objectCreationRecordTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in objectCreationRecordTable.
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_smiMIB above.
 */
u_char *
var_objectCreationRecordTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct objectCreationRecordTable_data *StorageTmp = NULL;

	DEBUGMSGTL(("smiMIB", "var_objectCreationRecordTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_objectCreationRecordTable();
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	if ((StorageTmp = header_complex(objectCreationRecordTableStorage, vp, name, length, exact, var_len, write_method)) == NULL)
		return NULL;
	refresh_objectCreationRecordTable_row(StorageTmp);
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case OBJECTCREATIONSOURCEINDICATOR:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->objectCreationSourceIndicator);
		return (u_char *) &StorageTmp->objectCreationSourceIndicator;
	case OBJECTCREATIONATTRIBUTEIDENTIFIERLIST:
		*write_method = NULL;	/* read-only */
		*var_len = StorageTmp->objectCreationAttributeIdentifierListLen;
		return (u_char *) StorageTmp->objectCreationAttributeIdentifierList;
	default:
		ERROR_MSG("");
	}
	return NULL;
}

/**
 * @fn void refresh_objectDeletionRecordTable(void)
 * @brief refresh the scalar values of the objectDeletionRecordTable.
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_objectDeletionRecordTable(void)
{
	if (objectDeletionRecordTable_refresh == 0)
		return;
	objectDeletionRecordTable_refresh = 0;
	/* XXX: Here, update the table as required... */
}

/**
 * @fn void refresh_objectDeletionRecordTable_row(struct objectDeletionRecordTable_data *StorageTmp)
 * @brief refresh the contents of the objectDeletionRecordTable row.
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
void
refresh_objectDeletionRecordTable_row(struct objectDeletionRecordTable_data *StorageTmp)
{
	if (StorageTmp->objectDeletionRecordTable_request == sa_request)
		return;
	StorageTmp->objectDeletionRecordTable_request = sa_request;
}

/**
 * @fn u_char *var_objectDeletionRecordTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in objectDeletionRecordTable.
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_smiMIB above.
 */
u_char *
var_objectDeletionRecordTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct objectDeletionRecordTable_data *StorageTmp = NULL;

	DEBUGMSGTL(("smiMIB", "var_objectDeletionRecordTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_objectDeletionRecordTable();
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	if ((StorageTmp = header_complex(objectDeletionRecordTableStorage, vp, name, length, exact, var_len, write_method)) == NULL)
		return NULL;
	refresh_objectDeletionRecordTable_row(StorageTmp);
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case OBJECTDELETIONSOURCEINDICATOR:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->objectDeletionSourceIndicator);
		return (u_char *) &StorageTmp->objectDeletionSourceIndicator;
	case OBJECTDELETIONATTRIBUTEIDENTIFIERLIST:
		*write_method = NULL;	/* read-only */
		*var_len = StorageTmp->objectDeletionAttributeIdentifierListLen;
		return (u_char *) StorageTmp->objectDeletionAttributeIdentifierList;
	default:
		ERROR_MSG("");
	}
	return NULL;
}

/**
 * @fn void refresh_relationshipChangeRecordTable(void)
 * @brief refresh the scalar values of the relationshipChangeRecordTable.
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_relationshipChangeRecordTable(void)
{
	if (relationshipChangeRecordTable_refresh == 0)
		return;
	relationshipChangeRecordTable_refresh = 0;
	/* XXX: Here, update the table as required... */
}

/**
 * @fn void refresh_relationshipChangeRecordTable_row(struct relationshipChangeRecordTable_data *StorageTmp)
 * @brief refresh the contents of the relationshipChangeRecordTable row.
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
void
refresh_relationshipChangeRecordTable_row(struct relationshipChangeRecordTable_data *StorageTmp)
{
	if (StorageTmp->relationshipChangeRecordTable_request == sa_request)
		return;
	StorageTmp->relationshipChangeRecordTable_request = sa_request;
}

/**
 * @fn u_char *var_relationshipChangeRecordTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in relationshipChangeRecordTable.
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_smiMIB above.
 */
u_char *
var_relationshipChangeRecordTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct relationshipChangeRecordTable_data *StorageTmp = NULL;

	DEBUGMSGTL(("smiMIB", "var_relationshipChangeRecordTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_relationshipChangeRecordTable();
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	if ((StorageTmp = header_complex(relationshipChangeRecordTableStorage, vp, name, length, exact, var_len, write_method)) == NULL)
		return NULL;
	refresh_relationshipChangeRecordTable_row(StorageTmp);
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case RELATIONSHIPCHANGERECORDDEFINITION:
		*write_method = NULL;	/* read-only */
		*var_len = StorageTmp->relationshipChangeRecordDefinitionLen;
		return (u_char *) StorageTmp->relationshipChangeRecordDefinition;
	case RELATIONSHIPCHANGESOURCEINDICATOR:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->relationshipChangeSourceIndicator);
		return (u_char *) &StorageTmp->relationshipChangeSourceIndicator;
	case RELATIONSHIPCHANGEATTRIBUTEIDENTIFIERLIST:
		*write_method = NULL;	/* read-only */
		*var_len = StorageTmp->relationshipChangeAttributeIdentifierListLen;
		return (u_char *) StorageTmp->relationshipChangeAttributeIdentifierList;
	default:
		ERROR_MSG("");
	}
	return NULL;
}

/**
 * @fn void refresh_securityAlarmReportRecordTable(void)
 * @brief refresh the scalar values of the securityAlarmReportRecordTable.
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_securityAlarmReportRecordTable(void)
{
	if (securityAlarmReportRecordTable_refresh == 0)
		return;
	securityAlarmReportRecordTable_refresh = 0;
	/* XXX: Here, update the table as required... */
}

/**
 * @fn void refresh_securityAlarmReportRecordTable_row(struct securityAlarmReportRecordTable_data *StorageTmp)
 * @brief refresh the contents of the securityAlarmReportRecordTable row.
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
void
refresh_securityAlarmReportRecordTable_row(struct securityAlarmReportRecordTable_data *StorageTmp)
{
	if (StorageTmp->securityAlarmReportRecordTable_request == sa_request)
		return;
	StorageTmp->securityAlarmReportRecordTable_request = sa_request;
}

/**
 * @fn u_char *var_securityAlarmReportRecordTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in securityAlarmReportRecordTable.
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_smiMIB above.
 */
u_char *
var_securityAlarmReportRecordTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct securityAlarmReportRecordTable_data *StorageTmp = NULL;

	DEBUGMSGTL(("smiMIB", "var_securityAlarmReportRecordTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_securityAlarmReportRecordTable();
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	if ((StorageTmp = header_complex(securityAlarmReportRecordTableStorage, vp, name, length, exact, var_len, write_method)) == NULL)
		return NULL;
	refresh_securityAlarmReportRecordTable_row(StorageTmp);
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case SECURITYALARMRECORDCAUSE:
		*write_method = NULL;	/* read-only */
		*var_len = StorageTmp->securityAlarmRecordCauseLen;
		return (u_char *) StorageTmp->securityAlarmRecordCause;
	case SECURITYALARMRECORDSEVERITY:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->securityAlarmRecordSeverity);
		return (u_char *) &StorageTmp->securityAlarmRecordSeverity;
	case SECURITYALARMRECORDDETECTOR:
		*write_method = NULL;	/* read-only */
		*var_len = StorageTmp->securityAlarmRecordDetectorLen;
		return (u_char *) StorageTmp->securityAlarmRecordDetector;
	case SECURITYSERVICEUSER:
		*write_method = NULL;	/* read-only */
		*var_len = StorageTmp->securityServiceUserLen;
		return (u_char *) StorageTmp->securityServiceUser;
	case SECURITYSERVICEPROVIDER:
		*write_method = NULL;	/* read-only */
		*var_len = StorageTmp->securityServiceProviderLen;
		return (u_char *) StorageTmp->securityServiceProvider;
	default:
		ERROR_MSG("");
	}
	return NULL;
}

/**
 * @fn void refresh_stateChangeRecordTable(void)
 * @brief refresh the scalar values of the stateChangeRecordTable.
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_stateChangeRecordTable(void)
{
	if (stateChangeRecordTable_refresh == 0)
		return;
	stateChangeRecordTable_refresh = 0;
	/* XXX: Here, update the table as required... */
}

/**
 * @fn void refresh_stateChangeRecordTable_row(struct stateChangeRecordTable_data *StorageTmp)
 * @brief refresh the contents of the stateChangeRecordTable row.
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
void
refresh_stateChangeRecordTable_row(struct stateChangeRecordTable_data *StorageTmp)
{
	if (StorageTmp->stateChangeRecordTable_request == sa_request)
		return;
	StorageTmp->stateChangeRecordTable_request = sa_request;
}

/**
 * @fn u_char *var_stateChangeRecordTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in stateChangeRecordTable.
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_smiMIB above.
 */
u_char *
var_stateChangeRecordTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct stateChangeRecordTable_data *StorageTmp = NULL;

	DEBUGMSGTL(("smiMIB", "var_stateChangeRecordTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_stateChangeRecordTable();
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	if ((StorageTmp = header_complex(stateChangeRecordTableStorage, vp, name, length, exact, var_len, write_method)) == NULL)
		return NULL;
	refresh_stateChangeRecordTable_row(StorageTmp);
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case STATECHANGERECORDDEFINITION:
		*write_method = NULL;	/* read-only */
		*var_len = StorageTmp->stateChangeRecordDefinitionLen;
		return (u_char *) StorageTmp->stateChangeRecordDefinition;
	case STATECHANGESOURCEINDICATOR:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->stateChangeSourceIndicator);
		return (u_char *) &StorageTmp->stateChangeSourceIndicator;
	case STATECHANGEATTRIBUTEIDENTIFIERLIST:
		*write_method = NULL;	/* read-only */
		*var_len = StorageTmp->stateChangeAttributeIdentifierListLen;
		return (u_char *) StorageTmp->stateChangeAttributeIdentifierList;
	default:
		ERROR_MSG("");
	}
	return NULL;
}

/**
 * @fn int write_discriminatorConstruct(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_discriminatorConstruct(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static uint8_t *old_value;
	struct discriminatorTable_data *StorageTmp = NULL;
	size_t newlen = name_len - (sizeof(smiMIB_variables_oid) / sizeof(oid) + 7 - 1);
	static size_t old_length = 0;
	static uint8_t *string = NULL;

	DEBUGMSGTL(("smiMIB", "write_discriminatorConstruct entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(discriminatorTableStorage, NULL, &name[sizeof(smiMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->discriminatorEntryStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to discriminatorConstruct: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to discriminatorConstruct not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (2 > var_val_len || var_val_len > SPRINT_MAX_LEN) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to discriminatorConstruct: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((string = malloc(var_val_len)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		break;
	case FREE:		/* Release any resources that have been allocated */
		SNMP_FREE(string);
		break;
	case ACTION:		/* The variable has been stored in string for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in the 
				   UNDO case */
		old_value = StorageTmp->discriminatorConstruct;
		old_length = StorageTmp->discriminatorConstructLen;
		StorageTmp->discriminatorConstruct = string;
		StorageTmp->discriminatorConstructLen = var_val_len;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->discriminatorConstruct = old_value;
		StorageTmp->discriminatorConstructLen = old_length;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(old_value);
		old_length = 0;
		string = NULL;
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_discriminatorAdministrativeState(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_discriminatorAdministrativeState(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct discriminatorTable_data *StorageTmp = NULL;
	size_t newlen = name_len - (sizeof(smiMIB_variables_oid) / sizeof(oid) + 7 - 1);
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("smiMIB", "write_discriminatorAdministrativeState entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(discriminatorTableStorage, NULL, &name[sizeof(smiMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->discriminatorEntryStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to discriminatorAdministrativeState: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to discriminatorAdministrativeState not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to discriminatorAdministrativeState: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case DISCRIMINATORADMINISTRATIVESTATE_LOCKED:
		case DISCRIMINATORADMINISTRATIVESTATE_UNLOCKED:
		case DISCRIMINATORADMINISTRATIVESTATE_SHUTTINGDOWN:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to discriminatorAdministrativeState: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in set_value for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in
				   the UNDO case */
		old_value = StorageTmp->discriminatorAdministrativeState;
		StorageTmp->discriminatorAdministrativeState = set_value;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->discriminatorAdministrativeState = old_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_discriminatorStartTime(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_discriminatorStartTime(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct discriminatorTable_data *StorageTmp = NULL;
	size_t newlen = name_len - (sizeof(smiMIB_variables_oid) / sizeof(oid) + 7 - 1);
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("smiMIB", "write_discriminatorStartTime entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(discriminatorTableStorage, NULL, &name[sizeof(smiMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->discriminatorEntryStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to discriminatorStartTime: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_TIMETICKS) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to discriminatorStartTime not ASN_TIMETICKS\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to discriminatorStartTime: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in set_value for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in
				   the UNDO case */
		old_value = StorageTmp->discriminatorStartTime;
		StorageTmp->discriminatorStartTime = set_value;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->discriminatorStartTime = old_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_discriminatorStopTime(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_discriminatorStopTime(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct discriminatorTable_data *StorageTmp = NULL;
	size_t newlen = name_len - (sizeof(smiMIB_variables_oid) / sizeof(oid) + 7 - 1);
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("smiMIB", "write_discriminatorStopTime entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(discriminatorTableStorage, NULL, &name[sizeof(smiMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->discriminatorEntryStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to discriminatorStopTime: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_TIMETICKS) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to discriminatorStopTime not ASN_TIMETICKS\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to discriminatorStopTime: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in set_value for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in
				   the UNDO case */
		old_value = StorageTmp->discriminatorStopTime;
		StorageTmp->discriminatorStopTime = set_value;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->discriminatorStopTime = old_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_discriminatorIntervalsOfDay(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_discriminatorIntervalsOfDay(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static uint8_t *old_value;
	struct discriminatorTable_data *StorageTmp = NULL;
	size_t newlen = name_len - (sizeof(smiMIB_variables_oid) / sizeof(oid) + 7 - 1);
	static size_t old_length = 0;
	static uint8_t *string = NULL;

	DEBUGMSGTL(("smiMIB", "write_discriminatorIntervalsOfDay entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(discriminatorTableStorage, NULL, &name[sizeof(smiMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->discriminatorEntryStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to discriminatorIntervalsOfDay: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to discriminatorIntervalsOfDay not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (2 > var_val_len || var_val_len > SPRINT_MAX_LEN) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to discriminatorIntervalsOfDay: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((string = malloc(var_val_len)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		break;
	case FREE:		/* Release any resources that have been allocated */
		SNMP_FREE(string);
		break;
	case ACTION:		/* The variable has been stored in string for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in the 
				   UNDO case */
		old_value = StorageTmp->discriminatorIntervalsOfDay;
		old_length = StorageTmp->discriminatorIntervalsOfDayLen;
		StorageTmp->discriminatorIntervalsOfDay = string;
		StorageTmp->discriminatorIntervalsOfDayLen = var_val_len;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->discriminatorIntervalsOfDay = old_value;
		StorageTmp->discriminatorIntervalsOfDayLen = old_length;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(old_value);
		old_length = 0;
		string = NULL;
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_discriminatorWeekMask(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_discriminatorWeekMask(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static uint8_t *old_value;
	struct discriminatorTable_data *StorageTmp = NULL;
	size_t newlen = name_len - (sizeof(smiMIB_variables_oid) / sizeof(oid) + 7 - 1);
	static size_t old_length = 0;
	static uint8_t *string = NULL;

	DEBUGMSGTL(("smiMIB", "write_discriminatorWeekMask entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(discriminatorTableStorage, NULL, &name[sizeof(smiMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->discriminatorEntryStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to discriminatorWeekMask: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to discriminatorWeekMask not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (2 > var_val_len || var_val_len > SPRINT_MAX_LEN) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to discriminatorWeekMask: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((string = malloc(var_val_len)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		break;
	case FREE:		/* Release any resources that have been allocated */
		SNMP_FREE(string);
		break;
	case ACTION:		/* The variable has been stored in string for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in the 
				   UNDO case */
		old_value = StorageTmp->discriminatorWeekMask;
		old_length = StorageTmp->discriminatorWeekMaskLen;
		StorageTmp->discriminatorWeekMask = string;
		StorageTmp->discriminatorWeekMaskLen = var_val_len;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->discriminatorWeekMask = old_value;
		StorageTmp->discriminatorWeekMaskLen = old_length;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(old_value);
		old_length = 0;
		string = NULL;
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_discriminatorSchedulerName(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_discriminatorSchedulerName(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static oid *old_value;
	struct discriminatorTable_data *StorageTmp = NULL;
	size_t newlen = name_len - (sizeof(smiMIB_variables_oid) / sizeof(oid) + 7 - 1);
	static size_t old_length = 0;
	static oid *objid = NULL;

	DEBUGMSGTL(("smiMIB", "write_discriminatorSchedulerName entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(discriminatorTableStorage, NULL, &name[sizeof(smiMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->discriminatorEntryStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to discriminatorSchedulerName: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OBJECT_ID) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to discriminatorSchedulerName not ASN_OBJECT_ID\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (MIN_OID_LEN * sizeof(oid) > var_val_len || var_val_len > MAX_OID_LEN * sizeof(oid)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to discriminatorSchedulerName: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((objid = snmp_duplicate_objid((void *) var_val, var_val_len / sizeof(oid))) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		break;
	case FREE:		/* Release any resources that have been allocated */
		SNMP_FREE(objid);
		break;
	case ACTION:		/* The variable has been stored in objid for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in the
				   UNDO case */
		old_value = StorageTmp->discriminatorSchedulerName;
		old_length = StorageTmp->discriminatorSchedulerNameLen;
		StorageTmp->discriminatorSchedulerName = objid;
		StorageTmp->discriminatorSchedulerNameLen = var_val_len / sizeof(oid);
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->discriminatorSchedulerName = old_value;
		StorageTmp->discriminatorSchedulerNameLen = old_length;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(old_value);
		old_length = 0;
		objid = NULL;
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_destination(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_destination(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static uint8_t *old_value;
	struct eventForwardingDiscriminatorTable_data *StorageTmp = NULL;
	size_t newlen = name_len - (sizeof(smiMIB_variables_oid) / sizeof(oid) + 7 - 1);
	static size_t old_length = 0;
	static uint8_t *string = NULL;

	DEBUGMSGTL(("smiMIB", "write_destination entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(eventForwardingDiscriminatorTableStorage, NULL, &name[sizeof(smiMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->eventForwardingDiscriminatorEntryStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to destination: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to destination not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (2 > var_val_len || var_val_len > SPRINT_MAX_LEN) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to destination: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((string = malloc(var_val_len)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		break;
	case FREE:		/* Release any resources that have been allocated */
		SNMP_FREE(string);
		break;
	case ACTION:		/* The variable has been stored in string for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in the 
				   UNDO case */
		old_value = StorageTmp->destination;
		old_length = StorageTmp->destinationLen;
		StorageTmp->destination = string;
		StorageTmp->destinationLen = var_val_len;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->destination = old_value;
		StorageTmp->destinationLen = old_length;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(old_value);
		old_length = 0;
		string = NULL;
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_activeDestination(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_activeDestination(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static uint8_t *old_value;
	struct eventForwardingDiscriminatorTable_data *StorageTmp = NULL;
	size_t newlen = name_len - (sizeof(smiMIB_variables_oid) / sizeof(oid) + 7 - 1);
	static size_t old_length = 0;
	static uint8_t *string = NULL;

	DEBUGMSGTL(("smiMIB", "write_activeDestination entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(eventForwardingDiscriminatorTableStorage, NULL, &name[sizeof(smiMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->eventForwardingDiscriminatorEntryStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to activeDestination: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to activeDestination not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (2 > var_val_len || var_val_len > SPRINT_MAX_LEN) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to activeDestination: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((string = malloc(var_val_len)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		break;
	case FREE:		/* Release any resources that have been allocated */
		SNMP_FREE(string);
		break;
	case ACTION:		/* The variable has been stored in string for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in the 
				   UNDO case */
		old_value = StorageTmp->activeDestination;
		old_length = StorageTmp->activeDestinationLen;
		StorageTmp->activeDestination = string;
		StorageTmp->activeDestinationLen = var_val_len;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->activeDestination = old_value;
		StorageTmp->activeDestinationLen = old_length;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(old_value);
		old_length = 0;
		string = NULL;
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_backUpDestinationList(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_backUpDestinationList(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static uint8_t *old_value;
	struct eventForwardingDiscriminatorTable_data *StorageTmp = NULL;
	size_t newlen = name_len - (sizeof(smiMIB_variables_oid) / sizeof(oid) + 7 - 1);
	static size_t old_length = 0;
	static uint8_t *string = NULL;

	DEBUGMSGTL(("smiMIB", "write_backUpDestinationList entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(eventForwardingDiscriminatorTableStorage, NULL, &name[sizeof(smiMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->eventForwardingDiscriminatorEntryStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to backUpDestinationList: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to backUpDestinationList not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (2 > var_val_len || var_val_len > SPRINT_MAX_LEN) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to backUpDestinationList: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((string = malloc(var_val_len)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		break;
	case FREE:		/* Release any resources that have been allocated */
		SNMP_FREE(string);
		break;
	case ACTION:		/* The variable has been stored in string for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in the 
				   UNDO case */
		old_value = StorageTmp->backUpDestinationList;
		old_length = StorageTmp->backUpDestinationListLen;
		StorageTmp->backUpDestinationList = string;
		StorageTmp->backUpDestinationListLen = var_val_len;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->backUpDestinationList = old_value;
		StorageTmp->backUpDestinationListLen = old_length;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(old_value);
		old_length = 0;
		string = NULL;
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_confirmedMode(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_confirmedMode(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct eventForwardingDiscriminatorTable_data *StorageTmp = NULL;
	size_t newlen = name_len - (sizeof(smiMIB_variables_oid) / sizeof(oid) + 7 - 1);
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("smiMIB", "write_confirmedMode entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(eventForwardingDiscriminatorTableStorage, NULL, &name[sizeof(smiMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->eventForwardingDiscriminatorEntryStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to confirmedMode: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to confirmedMode not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to confirmedMode: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to confirmedMode: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in set_value for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in
				   the UNDO case */
		old_value = StorageTmp->confirmedMode;
		StorageTmp->confirmedMode = set_value;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->confirmedMode = old_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_logDiscriminatorConstruct(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_logDiscriminatorConstruct(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static uint8_t *old_value;
	struct logTable_data *StorageTmp = NULL;
	size_t newlen = name_len - (sizeof(smiMIB_variables_oid) / sizeof(oid) + 7 - 1);
	static size_t old_length = 0;
	static uint8_t *string = NULL;

	DEBUGMSGTL(("smiMIB", "write_logDiscriminatorConstruct entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(logTableStorage, NULL, &name[sizeof(smiMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->logEntryStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to logDiscriminatorConstruct: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to logDiscriminatorConstruct not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (2 > var_val_len || var_val_len > SPRINT_MAX_LEN) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to logDiscriminatorConstruct: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((string = malloc(var_val_len)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		break;
	case FREE:		/* Release any resources that have been allocated */
		SNMP_FREE(string);
		break;
	case ACTION:		/* The variable has been stored in string for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in the 
				   UNDO case */
		old_value = StorageTmp->logDiscriminatorConstruct;
		old_length = StorageTmp->logDiscriminatorConstructLen;
		StorageTmp->logDiscriminatorConstruct = string;
		StorageTmp->logDiscriminatorConstructLen = var_val_len;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->logDiscriminatorConstruct = old_value;
		StorageTmp->logDiscriminatorConstructLen = old_length;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(old_value);
		old_length = 0;
		string = NULL;
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_logAdministrativeState(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_logAdministrativeState(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct logTable_data *StorageTmp = NULL;
	size_t newlen = name_len - (sizeof(smiMIB_variables_oid) / sizeof(oid) + 7 - 1);
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("smiMIB", "write_logAdministrativeState entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(logTableStorage, NULL, &name[sizeof(smiMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->logEntryStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to logAdministrativeState: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to logAdministrativeState not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to logAdministrativeState: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 1 */
		switch (set_value) {
		case LOGADMINISTRATIVESTATE_LOCKED:
		case LOGADMINISTRATIVESTATE_UNLOCKED:
		case LOGADMINISTRATIVESTATE_SHUTTINGDOWN:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to logAdministrativeState: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in set_value for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in
				   the UNDO case */
		old_value = StorageTmp->logAdministrativeState;
		StorageTmp->logAdministrativeState = set_value;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->logAdministrativeState = old_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_logAvailabilityStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_logAvailabilityStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static uint8_t *old_value;
	struct logTable_data *StorageTmp = NULL;
	size_t newlen = name_len - (sizeof(smiMIB_variables_oid) / sizeof(oid) + 7 - 1);
	static size_t old_length = 0;
	static uint8_t *string = NULL;

	DEBUGMSGTL(("smiMIB", "write_logAvailabilityStatus entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(logTableStorage, NULL, &name[sizeof(smiMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->logEntryStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to logAvailabilityStatus: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_BIT_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to logAvailabilityStatus not ASN_BIT_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (2 > var_val_len || var_val_len > SPRINT_MAX_LEN || var_val_len != ((8 >> 3) + 2)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to logAvailabilityStatus: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		if (var_val_len < 1 || *var_val != (8 - (8 & 0x07))) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to logAvailabilityStatus: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((string = malloc(var_val_len)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		break;
	case FREE:		/* Release any resources that have been allocated */
		SNMP_FREE(string);
		break;
	case ACTION:		/* The variable has been stored in string for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in the 
				   UNDO case */
		old_value = StorageTmp->logAvailabilityStatus;
		old_length = StorageTmp->logAvailabilityStatusLen;
		StorageTmp->logAvailabilityStatus = string;
		StorageTmp->logAvailabilityStatusLen = var_val_len;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->logAvailabilityStatus = old_value;
		StorageTmp->logAvailabilityStatusLen = old_length;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(old_value);
		old_length = 0;
		string = NULL;
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_logFullAction(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_logFullAction(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct logTable_data *StorageTmp = NULL;
	size_t newlen = name_len - (sizeof(smiMIB_variables_oid) / sizeof(oid) + 7 - 1);
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("smiMIB", "write_logFullAction entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(logTableStorage, NULL, &name[sizeof(smiMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->logEntryStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to logFullAction: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to logFullAction not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to logFullAction: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 0 */
		switch (set_value) {
		case LOGFULLACTION_WRAP:
		case LOGFULLACTION_HALT:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to logFullAction: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in set_value for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in
				   the UNDO case */
		old_value = StorageTmp->logFullAction;
		StorageTmp->logFullAction = set_value;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->logFullAction = old_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_maxLogSize(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_maxLogSize(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static ulong old_value;
	struct logTable_data *StorageTmp = NULL;
	size_t newlen = name_len - (sizeof(smiMIB_variables_oid) / sizeof(oid) + 7 - 1);
	ulong set_value = *((ulong *) var_val);

	DEBUGMSGTL(("smiMIB", "write_maxLogSize entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(logTableStorage, NULL, &name[sizeof(smiMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->logEntryStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to maxLogSize: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to maxLogSize not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to maxLogSize: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 0 */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in set_value for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in
				   the UNDO case */
		old_value = StorageTmp->maxLogSize;
		StorageTmp->maxLogSize = set_value;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->maxLogSize = old_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_capacityAlarmThreshold(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_capacityAlarmThreshold(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static uint8_t *old_value;
	struct logTable_data *StorageTmp = NULL;
	size_t newlen = name_len - (sizeof(smiMIB_variables_oid) / sizeof(oid) + 7 - 1);
	static size_t old_length = 0;
	static uint8_t *string = NULL;

	DEBUGMSGTL(("smiMIB", "write_capacityAlarmThreshold entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(logTableStorage, NULL, &name[sizeof(smiMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->logEntryStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to capacityAlarmThreshold: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to capacityAlarmThreshold not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (2 > var_val_len || var_val_len > SPRINT_MAX_LEN) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to capacityAlarmThreshold: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value \"\x64\" */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((string = malloc(var_val_len)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		break;
	case FREE:		/* Release any resources that have been allocated */
		SNMP_FREE(string);
		break;
	case ACTION:		/* The variable has been stored in string for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in the 
				   UNDO case */
		old_value = StorageTmp->capacityAlarmThreshold;
		old_length = StorageTmp->capacityAlarmThresholdLen;
		StorageTmp->capacityAlarmThreshold = string;
		StorageTmp->capacityAlarmThresholdLen = var_val_len;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->capacityAlarmThreshold = old_value;
		StorageTmp->capacityAlarmThresholdLen = old_length;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(old_value);
		old_length = 0;
		string = NULL;
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_logStartTime(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_logStartTime(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct logTable_data *StorageTmp = NULL;
	size_t newlen = name_len - (sizeof(smiMIB_variables_oid) / sizeof(oid) + 7 - 1);
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("smiMIB", "write_logStartTime entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(logTableStorage, NULL, &name[sizeof(smiMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->logEntryStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to logStartTime: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_TIMETICKS) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to logStartTime not ASN_TIMETICKS\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to logStartTime: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in set_value for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in
				   the UNDO case */
		old_value = StorageTmp->logStartTime;
		StorageTmp->logStartTime = set_value;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->logStartTime = old_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_logStopTime(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_logStopTime(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct logTable_data *StorageTmp = NULL;
	size_t newlen = name_len - (sizeof(smiMIB_variables_oid) / sizeof(oid) + 7 - 1);
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("smiMIB", "write_logStopTime entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(logTableStorage, NULL, &name[sizeof(smiMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->logEntryStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to logStopTime: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_TIMETICKS) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to logStopTime not ASN_TIMETICKS\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to logStopTime: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 6983120 */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in set_value for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in
				   the UNDO case */
		old_value = StorageTmp->logStopTime;
		StorageTmp->logStopTime = set_value;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->logStopTime = old_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_logIntervalsOfDay(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_logIntervalsOfDay(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static uint8_t *old_value;
	struct logTable_data *StorageTmp = NULL;
	size_t newlen = name_len - (sizeof(smiMIB_variables_oid) / sizeof(oid) + 7 - 1);
	static size_t old_length = 0;
	static uint8_t *string = NULL;

	DEBUGMSGTL(("smiMIB", "write_logIntervalsOfDay entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(logTableStorage, NULL, &name[sizeof(smiMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->logEntryStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to logIntervalsOfDay: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to logIntervalsOfDay not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (2 > var_val_len || var_val_len > SPRINT_MAX_LEN) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to logIntervalsOfDay: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value \"\x00\x00\x17\x3B\" */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((string = malloc(var_val_len)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		break;
	case FREE:		/* Release any resources that have been allocated */
		SNMP_FREE(string);
		break;
	case ACTION:		/* The variable has been stored in string for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in the 
				   UNDO case */
		old_value = StorageTmp->logIntervalsOfDay;
		old_length = StorageTmp->logIntervalsOfDayLen;
		StorageTmp->logIntervalsOfDay = string;
		StorageTmp->logIntervalsOfDayLen = var_val_len;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->logIntervalsOfDay = old_value;
		StorageTmp->logIntervalsOfDayLen = old_length;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(old_value);
		old_length = 0;
		string = NULL;
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_logWeekMask(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_logWeekMask(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static uint8_t *old_value;
	struct logTable_data *StorageTmp = NULL;
	size_t newlen = name_len - (sizeof(smiMIB_variables_oid) / sizeof(oid) + 7 - 1);
	static size_t old_length = 0;
	static uint8_t *string = NULL;

	DEBUGMSGTL(("smiMIB", "write_logWeekMask entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(logTableStorage, NULL, &name[sizeof(smiMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->logEntryStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to logWeekMask: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to logWeekMask not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (2 > var_val_len || var_val_len > SPRINT_MAX_LEN) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to logWeekMask: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value \"\x7F\x01\x00\x00\x17\x3B\" */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((string = malloc(var_val_len)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		break;
	case FREE:		/* Release any resources that have been allocated */
		SNMP_FREE(string);
		break;
	case ACTION:		/* The variable has been stored in string for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in the 
				   UNDO case */
		old_value = StorageTmp->logWeekMask;
		old_length = StorageTmp->logWeekMaskLen;
		StorageTmp->logWeekMask = string;
		StorageTmp->logWeekMaskLen = var_val_len;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->logWeekMask = old_value;
		StorageTmp->logWeekMaskLen = old_length;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(old_value);
		old_length = 0;
		string = NULL;
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_logSchedulerName(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_logSchedulerName(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static oid *old_value;
	struct logTable_data *StorageTmp = NULL;
	size_t newlen = name_len - (sizeof(smiMIB_variables_oid) / sizeof(oid) + 7 - 1);
	static size_t old_length = 0;
	static oid *objid = NULL;

	DEBUGMSGTL(("smiMIB", "write_logSchedulerName entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(logTableStorage, NULL, &name[sizeof(smiMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->logEntryStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to logSchedulerName: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OBJECT_ID) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to logSchedulerName not ASN_OBJECT_ID\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (MIN_OID_LEN * sizeof(oid) > var_val_len || var_val_len > MAX_OID_LEN * sizeof(oid)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to logSchedulerName: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value zeroDotZero */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((objid = snmp_duplicate_objid((void *) var_val, var_val_len / sizeof(oid))) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		break;
	case FREE:		/* Release any resources that have been allocated */
		SNMP_FREE(objid);
		break;
	case ACTION:		/* The variable has been stored in objid for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in the
				   UNDO case */
		old_value = StorageTmp->logSchedulerName;
		old_length = StorageTmp->logSchedulerNameLen;
		StorageTmp->logSchedulerName = objid;
		StorageTmp->logSchedulerNameLen = var_val_len / sizeof(oid);
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->logSchedulerName = old_value;
		StorageTmp->logSchedulerNameLen = old_length;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(old_value);
		old_length = 0;
		objid = NULL;
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_discriminatorEntryStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_discriminatorEntryStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct discriminatorTable_data *StorageTmp = NULL;
	static struct discriminatorTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - (sizeof(discriminatorTable_variables_oid) / sizeof(oid) + 3 - 1);
	static int old_value;
	int set_value;
	static struct variable_list *vars, *vp;

	StorageTmp = header_complex(discriminatorTableStorage, NULL, &name[sizeof(discriminatorTable_variables_oid) / sizeof(oid) + 3 - 1], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to discriminatorEntryStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to discriminatorEntryStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->discriminatorEntryStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->discriminatorTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_DESTROY:
			/* destroying existent or non-existent row is ok */
			if (StorageTmp == NULL)
				break;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->discriminatorTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			vars = NULL;
			/* discriminatorId */
			snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0);
			if (header_complex_parse_oid(&(name[sizeof(discriminatorTable_variables_oid) / sizeof(oid) + 2]), newlen, vars) != SNMPERR_SUCCESS) {
				/* XXX: free, zero vars */
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			if ((StorageNew = discriminatorTable_create()) == NULL) {
				/* XXX: free, zero vars */
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			memdup((void *) &StorageNew->discriminatorId, vp->val.string, vp->val_len);
			StorageNew->discriminatorIdLen = vp->val_len;
			vp = vp->next_variable;
			/* XXX: free, zero vars, no longer needed? */
			break;
		}
		break;
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			discriminatorTable_destroy(&StorageNew);
			/* XXX: free, zero vars */
			break;
		}
		break;
	case ACTION:
		/* The variable has been stored in set_value for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* row creation, so add it */
			discriminatorTable_add(StorageNew);
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* set the flag? */
			old_value = StorageTmp->discriminatorEntryStatus;
			StorageTmp->discriminatorEntryStatus = set_value;
			break;
		case RS_DESTROY:
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				discriminatorTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* row creation, so remove it again */
			discriminatorTable_del(StorageNew);
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* restore state */
			StorageTmp->discriminatorEntryStatus = old_value;
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			discriminatorTable_add(StorageDel);
			break;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* row creation, set final state */
			/* XXX: check if row is ready, otherwise leave at RS_NOTREADY */
			StorageTmp->discriminatorEntryStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			/* row creation, set final state */
			/* XXX: check if row is ready, otherwise leave at RS_NOTREADY */
			StorageTmp->discriminatorEntryStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* state change already performed */
			break;
		case RS_DESTROY:
			/* row deletion, free it its dead */
			discriminatorTable_destroy(&StorageDel);
			/* discriminatorTable_destroy() can handle NULL pointers. */
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_eventForwardingDiscriminatorEntryStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_eventForwardingDiscriminatorEntryStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct eventForwardingDiscriminatorTable_data *StorageTmp = NULL;
	static struct eventForwardingDiscriminatorTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - (sizeof(eventForwardingDiscriminatorTable_variables_oid) / sizeof(oid) + 3 - 1);
	static int old_value;
	int set_value;
	static struct variable_list *vars, *vp;

	StorageTmp = header_complex(eventForwardingDiscriminatorTableStorage, NULL, &name[sizeof(eventForwardingDiscriminatorTable_variables_oid) / sizeof(oid) + 3 - 1], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to eventForwardingDiscriminatorEntryStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to eventForwardingDiscriminatorEntryStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->eventForwardingDiscriminatorEntryStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->eventForwardingDiscriminatorTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_DESTROY:
			/* destroying existent or non-existent row is ok */
			if (StorageTmp == NULL)
				break;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->eventForwardingDiscriminatorTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			vars = NULL;
			/* discriminatorId */
			snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0);
			if (header_complex_parse_oid(&(name[sizeof(eventForwardingDiscriminatorTable_variables_oid) / sizeof(oid) + 2]), newlen, vars) != SNMPERR_SUCCESS) {
				/* XXX: free, zero vars */
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			if ((StorageNew = eventForwardingDiscriminatorTable_create()) == NULL) {
				/* XXX: free, zero vars */
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			memdup((void *) &StorageNew->discriminatorId, vp->val.string, vp->val_len);
			StorageNew->discriminatorIdLen = vp->val_len;
			vp = vp->next_variable;
			/* XXX: free, zero vars, no longer needed? */
			break;
		}
		break;
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			eventForwardingDiscriminatorTable_destroy(&StorageNew);
			/* XXX: free, zero vars */
			break;
		}
		break;
	case ACTION:
		/* The variable has been stored in set_value for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* row creation, so add it */
			eventForwardingDiscriminatorTable_add(StorageNew);
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* set the flag? */
			old_value = StorageTmp->eventForwardingDiscriminatorEntryStatus;
			StorageTmp->eventForwardingDiscriminatorEntryStatus = set_value;
			break;
		case RS_DESTROY:
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				eventForwardingDiscriminatorTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* row creation, so remove it again */
			eventForwardingDiscriminatorTable_del(StorageNew);
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* restore state */
			StorageTmp->eventForwardingDiscriminatorEntryStatus = old_value;
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			eventForwardingDiscriminatorTable_add(StorageDel);
			break;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* row creation, set final state */
			/* XXX: check if row is ready, otherwise leave at RS_NOTREADY */
			StorageTmp->eventForwardingDiscriminatorEntryStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			/* row creation, set final state */
			/* XXX: check if row is ready, otherwise leave at RS_NOTREADY */
			StorageTmp->eventForwardingDiscriminatorEntryStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* state change already performed */
			break;
		case RS_DESTROY:
			/* row deletion, free it its dead */
			eventForwardingDiscriminatorTable_destroy(&StorageDel);
			/* eventForwardingDiscriminatorTable_destroy() can handle NULL pointers. */
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_logEntryStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_logEntryStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct logTable_data *StorageTmp = NULL;
	static struct logTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - (sizeof(logTable_variables_oid) / sizeof(oid) + 3 - 1);
	static int old_value;
	int set_value;
	static struct variable_list *vars, *vp;

	StorageTmp = header_complex(logTableStorage, NULL, &name[sizeof(logTable_variables_oid) / sizeof(oid) + 3 - 1], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to logEntryStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to logEntryStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->logEntryStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->logTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_DESTROY:
			/* destroying existent or non-existent row is ok */
			if (StorageTmp == NULL)
				break;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->logTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			vars = NULL;
			/* logId */
			snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0);
			if (header_complex_parse_oid(&(name[sizeof(logTable_variables_oid) / sizeof(oid) + 2]), newlen, vars) != SNMPERR_SUCCESS) {
				/* XXX: free, zero vars */
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			if ((StorageNew = logTable_create()) == NULL) {
				/* XXX: free, zero vars */
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			memdup((void *) &StorageNew->logId, vp->val.string, vp->val_len);
			StorageNew->logIdLen = vp->val_len;
			vp = vp->next_variable;
			/* XXX: free, zero vars, no longer needed? */
			break;
		}
		break;
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			logTable_destroy(&StorageNew);
			/* XXX: free, zero vars */
			break;
		}
		break;
	case ACTION:
		/* The variable has been stored in set_value for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* row creation, so add it */
			logTable_add(StorageNew);
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* set the flag? */
			old_value = StorageTmp->logEntryStatus;
			StorageTmp->logEntryStatus = set_value;
			break;
		case RS_DESTROY:
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				logTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* row creation, so remove it again */
			logTable_del(StorageNew);
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* restore state */
			StorageTmp->logEntryStatus = old_value;
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			logTable_add(StorageDel);
			break;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* row creation, set final state */
			/* XXX: check if row is ready, otherwise leave at RS_NOTREADY */
			StorageTmp->logEntryStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			/* row creation, set final state */
			/* XXX: check if row is ready, otherwise leave at RS_NOTREADY */
			StorageTmp->logEntryStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* state change already performed */
			break;
		case RS_DESTROY:
			/* row deletion, free it its dead */
			logTable_destroy(&StorageDel);
			/* logTable_destroy() can handle NULL pointers. */
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_logRecordEntryStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_logRecordEntryStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct logRecordTable_data *StorageTmp = NULL;
	static struct logRecordTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - (sizeof(logRecordTable_variables_oid) / sizeof(oid) + 3 - 1);
	static int old_value;
	int set_value;
	static struct variable_list *vars, *vp;

	StorageTmp = header_complex(logRecordTableStorage, NULL, &name[sizeof(logRecordTable_variables_oid) / sizeof(oid) + 3 - 1], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to logRecordEntryStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to logRecordEntryStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->logRecordEntryStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->logRecordTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_DESTROY:
			/* destroying existent or non-existent row is ok */
			if (StorageTmp == NULL)
				break;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->logRecordTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			vars = NULL;
			/* logId */
			snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0);
			/* logRecordId */
			snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0);
			if (header_complex_parse_oid(&(name[sizeof(logRecordTable_variables_oid) / sizeof(oid) + 2]), newlen, vars) != SNMPERR_SUCCESS) {
				/* XXX: free, zero vars */
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			if ((StorageNew = logRecordTable_create()) == NULL) {
				/* XXX: free, zero vars */
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			memdup((void *) &StorageNew->logId, vp->val.string, vp->val_len);
			StorageNew->logIdLen = vp->val_len;
			vp = vp->next_variable;
			memdup((void *) &StorageNew->logRecordId, vp->val.string, vp->val_len);
			StorageNew->logRecordIdLen = vp->val_len;
			vp = vp->next_variable;
			/* XXX: free, zero vars, no longer needed? */
			break;
		}
		break;
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			logRecordTable_destroy(&StorageNew);
			/* XXX: free, zero vars */
			break;
		}
		break;
	case ACTION:
		/* The variable has been stored in set_value for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* row creation, so add it */
			logRecordTable_add(StorageNew);
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* set the flag? */
			old_value = StorageTmp->logRecordEntryStatus;
			StorageTmp->logRecordEntryStatus = set_value;
			break;
		case RS_DESTROY:
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				logRecordTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* row creation, so remove it again */
			logRecordTable_del(StorageNew);
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* restore state */
			StorageTmp->logRecordEntryStatus = old_value;
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			logRecordTable_add(StorageDel);
			break;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* row creation, set final state */
			/* XXX: check if row is ready, otherwise leave at RS_NOTREADY */
			StorageTmp->logRecordEntryStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			/* row creation, set final state */
			/* XXX: check if row is ready, otherwise leave at RS_NOTREADY */
			StorageTmp->logRecordEntryStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* state change already performed */
			break;
		case RS_DESTROY:
			/* row deletion, free it its dead */
			logRecordTable_destroy(&StorageDel);
			/* logRecordTable_destroy() can handle NULL pointers. */
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

void
send_attributeValueChange_v2trap(struct variable_list *vars)
{
	struct variable_list trap;

	trap.next_variable = vars;
	trap.name = snmpTrapOID_oid;
	trap.name_length = sizeof(snmpTrapOID_oid) / sizeof(oid);
	trap.type = ASN_OBJECT_ID;
	trap.val.objid = attributeValueChange_oid;
	trap.val_len = sizeof(attributeValueChange_oid);
	trap.index = 0;
	send_v2trap(&trap);
}

void
send_communicationsAlarm_v2trap(struct variable_list *vars)
{
	struct variable_list trap;

	trap.next_variable = vars;
	trap.name = snmpTrapOID_oid;
	trap.name_length = sizeof(snmpTrapOID_oid) / sizeof(oid);
	trap.type = ASN_OBJECT_ID;
	trap.val.objid = communicationsAlarm_oid;
	trap.val_len = sizeof(communicationsAlarm_oid);
	trap.index = 0;
	send_v2trap(&trap);
}

void
send_environmentalAlarm_v2trap(struct variable_list *vars)
{
	struct variable_list trap;

	trap.next_variable = vars;
	trap.name = snmpTrapOID_oid;
	trap.name_length = sizeof(snmpTrapOID_oid) / sizeof(oid);
	trap.type = ASN_OBJECT_ID;
	trap.val.objid = environmentalAlarm_oid;
	trap.val_len = sizeof(environmentalAlarm_oid);
	trap.index = 0;
	send_v2trap(&trap);
}

void
send_equipmentAlarm_v2trap(struct variable_list *vars)
{
	struct variable_list trap;

	trap.next_variable = vars;
	trap.name = snmpTrapOID_oid;
	trap.name_length = sizeof(snmpTrapOID_oid) / sizeof(oid);
	trap.type = ASN_OBJECT_ID;
	trap.val.objid = equipmentAlarm_oid;
	trap.val_len = sizeof(equipmentAlarm_oid);
	trap.index = 0;
	send_v2trap(&trap);
}

void
send_integrityViolation_v2trap(struct variable_list *vars)
{
	struct variable_list trap;

	trap.next_variable = vars;
	trap.name = snmpTrapOID_oid;
	trap.name_length = sizeof(snmpTrapOID_oid) / sizeof(oid);
	trap.type = ASN_OBJECT_ID;
	trap.val.objid = integrityViolation_oid;
	trap.val_len = sizeof(integrityViolation_oid);
	trap.index = 0;
	send_v2trap(&trap);
}

void
send_objectCreation_v2trap(struct variable_list *vars)
{
	struct variable_list trap;

	trap.next_variable = vars;
	trap.name = snmpTrapOID_oid;
	trap.name_length = sizeof(snmpTrapOID_oid) / sizeof(oid);
	trap.type = ASN_OBJECT_ID;
	trap.val.objid = objectCreation_oid;
	trap.val_len = sizeof(objectCreation_oid);
	trap.index = 0;
	send_v2trap(&trap);
}

void
send_objectDeletion_v2trap(struct variable_list *vars)
{
	struct variable_list trap;

	trap.next_variable = vars;
	trap.name = snmpTrapOID_oid;
	trap.name_length = sizeof(snmpTrapOID_oid) / sizeof(oid);
	trap.type = ASN_OBJECT_ID;
	trap.val.objid = objectDeletion_oid;
	trap.val_len = sizeof(objectDeletion_oid);
	trap.index = 0;
	send_v2trap(&trap);
}

void
send_operationalViolation_v2trap(struct variable_list *vars)
{
	struct variable_list trap;

	trap.next_variable = vars;
	trap.name = snmpTrapOID_oid;
	trap.name_length = sizeof(snmpTrapOID_oid) / sizeof(oid);
	trap.type = ASN_OBJECT_ID;
	trap.val.objid = operationalViolation_oid;
	trap.val_len = sizeof(operationalViolation_oid);
	trap.index = 0;
	send_v2trap(&trap);
}

void
send_physicalViolation_v2trap(struct variable_list *vars)
{
	struct variable_list trap;

	trap.next_variable = vars;
	trap.name = snmpTrapOID_oid;
	trap.name_length = sizeof(snmpTrapOID_oid) / sizeof(oid);
	trap.type = ASN_OBJECT_ID;
	trap.val.objid = physicalViolation_oid;
	trap.val_len = sizeof(physicalViolation_oid);
	trap.index = 0;
	send_v2trap(&trap);
}

void
send_processingErrorAlarm_v2trap(struct variable_list *vars)
{
	struct variable_list trap;

	trap.next_variable = vars;
	trap.name = snmpTrapOID_oid;
	trap.name_length = sizeof(snmpTrapOID_oid) / sizeof(oid);
	trap.type = ASN_OBJECT_ID;
	trap.val.objid = processingErrorAlarm_oid;
	trap.val_len = sizeof(processingErrorAlarm_oid);
	trap.index = 0;
	send_v2trap(&trap);
}

void
send_qualityofServiceAlarm_v2trap(struct variable_list *vars)
{
	struct variable_list trap;

	trap.next_variable = vars;
	trap.name = snmpTrapOID_oid;
	trap.name_length = sizeof(snmpTrapOID_oid) / sizeof(oid);
	trap.type = ASN_OBJECT_ID;
	trap.val.objid = qualityofServiceAlarm_oid;
	trap.val_len = sizeof(qualityofServiceAlarm_oid);
	trap.index = 0;
	send_v2trap(&trap);
}

void
send_relationshipChange_v2trap(struct variable_list *vars)
{
	struct variable_list trap;

	trap.next_variable = vars;
	trap.name = snmpTrapOID_oid;
	trap.name_length = sizeof(snmpTrapOID_oid) / sizeof(oid);
	trap.type = ASN_OBJECT_ID;
	trap.val.objid = relationshipChange_oid;
	trap.val_len = sizeof(relationshipChange_oid);
	trap.index = 0;
	send_v2trap(&trap);
}

void
send_securityServiceOrMechanismViolation_v2trap(struct variable_list *vars)
{
	struct variable_list trap;

	trap.next_variable = vars;
	trap.name = snmpTrapOID_oid;
	trap.name_length = sizeof(snmpTrapOID_oid) / sizeof(oid);
	trap.type = ASN_OBJECT_ID;
	trap.val.objid = securityServiceOrMechanismViolation_oid;
	trap.val_len = sizeof(securityServiceOrMechanismViolation_oid);
	trap.index = 0;
	send_v2trap(&trap);
}

void
send_stateChange_v2trap(struct variable_list *vars)
{
	struct variable_list trap;

	trap.next_variable = vars;
	trap.name = snmpTrapOID_oid;
	trap.name_length = sizeof(snmpTrapOID_oid) / sizeof(oid);
	trap.type = ASN_OBJECT_ID;
	trap.val.objid = stateChange_oid;
	trap.val_len = sizeof(stateChange_oid);
	trap.index = 0;
	send_v2trap(&trap);
}

void
send_timeDomainViolation_v2trap(struct variable_list *vars)
{
	struct variable_list trap;

	trap.next_variable = vars;
	trap.name = snmpTrapOID_oid;
	trap.name_length = sizeof(snmpTrapOID_oid) / sizeof(oid);
	trap.type = ASN_OBJECT_ID;
	trap.val.objid = timeDomainViolation_oid;
	trap.val_len = sizeof(timeDomainViolation_oid);
	trap.index = 0;
	send_v2trap(&trap);
}

void
send_communicationsInformation_v2trap(struct variable_list *vars)
{
	struct variable_list trap;

	trap.next_variable = vars;
	trap.name = snmpTrapOID_oid;
	trap.name_length = sizeof(snmpTrapOID_oid) / sizeof(oid);
	trap.type = ASN_OBJECT_ID;
	trap.val.objid = communicationsInformation_oid;
	trap.val_len = sizeof(communicationsInformation_oid);
	trap.index = 0;
	send_v2trap(&trap);
}
