/*****************************************************************************

 @(#) $RCSfile: gmiMIB.c,v $ $Name:  $($Revision: 0.9.2.2 $) $Date: 2009-01-10 17:09:13 $

 -----------------------------------------------------------------------------

 Copyright (c) 2008-2009  Monavacon Limited <http://www.monavacon.com/>
 Copyright (c) 2001-2008  OpenSS7 Corporation <http://www.openss7.com/>
 Copyright (c) 1997-2001  Brian F. G. Bidulock <bidulock@openss7.org>

 All Rights Reserved.

 This program is free software: you can redistribute it and/or modify it under
 the terms of the GNU Affero General Public License as published by the Free
 Software Foundation, version 3 of the license.

 This program is distributed in the hope that it will be useful, but WITHOUT
 ANY WARRANTY; without even the implied warranty of MERCHANTABILITY or FITNESS
 FOR A PARTICULAR PURPOSE.  See the GNU Affero General Public License for more
 details.

 You should have received a copy of the GNU Affero General Public License
 along with this program.  If not, see <http://www.gnu.org/licenses/>, or
 write to the Free Software Foundation, Inc., 675 Mass Ave, Cambridge, MA
 02139, USA.

 -----------------------------------------------------------------------------

 U.S. GOVERNMENT RESTRICTED RIGHTS.  If you are licensing this Software on
 behalf of the U.S. Government ("Government"), the following provisions apply
 to you.  If the Software is supplied by the Department of Defense ("DoD"), it
 is classified as "Commercial Computer Software" under paragraph 252.227-7014
 of the DoD Supplement to the Federal Acquisition Regulations ("DFARS") (or any
 successor regulations) and the Government is acquiring only the license rights
 granted herein (the license rights customarily provided to non-Government
 users).  If the Software is supplied to any unit or agency of the Government
 other than DoD, it is classified as "Restricted Computer Software" and the
 Government's rights in the Software are defined in paragraph 52.227-19 of the
 Federal Acquisition Regulations ("FAR") (or any successor regulations) or, in
 the cases of NASA, in paragraph 18.52.227-86 of the NASA Supplement to the FAR
 (or any successor regulations).

 -----------------------------------------------------------------------------

 Commercial licensing and support of this software is available from OpenSS7
 Corporation at a fee.  See http://www.openss7.com/

 -----------------------------------------------------------------------------

 Last Modified $Date: 2009-01-10 17:09:13 $ by $Author: brian $

 -----------------------------------------------------------------------------

 $Log: gmiMIB.c,v $
 Revision 0.9.2.2  2009-01-10 17:09:13  brian
 - updated agents

 Revision 0.9.2.1  2009-01-04 13:16:00  brian
 - updated agents

 *****************************************************************************/

#ident "@(#) $RCSfile: gmiMIB.c,v $ $Name:  $($Revision: 0.9.2.2 $) $Date: 2009-01-10 17:09:13 $"

static char const ident[] = "$RCSfile: gmiMIB.c,v $ $Name:  $($Revision: 0.9.2.2 $) $Date: 2009-01-10 17:09:13 $";

/* This file was generated by mib2c and is intended for use as
   a mib module for the ucd-snmp snmpd agent. */
#include <ucd-snmp/ucd-snmp-config.h>
#include <ucd-snmp/ucd-snmp-includes.h>
#include <ucd-snmp/ucd-snmp-agent-includes.h>
#include <ucd-snmp/callback.h>
#include <ucd-snmp/snmp-tc.h>
#include <ucd-snmp/default_store.h>
#include <ucd-snmp/snmp_alarm.h>
/* The following header files are mangled in most recent net-snmp releases so
 * the versions from UCD-SNMP 4.2.5 are included here.  */
#if defined HAVE_LIBNETSNMP
#else				/* defined HAVE_LIBNETSNMP */
#endif				/* defined HAVE_LIBNETSNMP */
/* These are messed up on both. */
#include "ds_agent.h"
#ifdef HAVE_UCD_SNMP_UTIL_FUNCS_H
#include <ucd-snmp/util_funcs.h>
/* Many recent net-snmp UCD compatible headers do not declare header_generic. */
int header_generic(struct variable *, oid *, size_t *, int, size_t *, WriteMethod **);
#else				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#include "util_funcs.h"
#endif				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#ifdef HAVE_UCD_SNMP_HEADER_COMPLEX_H
#include <ucd-snmp/header_complex.h>
#else				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
#include "header_complex.h"
#endif				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
/* This one is the other way around: it is just fine for net-snmp, but
 * ucd-snmp does not provide the header file at all.  */
#ifdef HAVE_UCD_SNMP_MIB_MODULES_H
#include <ucd-snmp/mib_modules.h>
#else				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#ifdef HAVE_NET_SNMP_AGENT_MIB_MODULES_H
#include <net-snmp/agent/mib_modules.h>
#else				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#include "mib_modules.h"
#endif				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#endif				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#include <stdint.h>
#include <signal.h>
#include <sys/stat.h>		/* for struct stat, fstat() */
#include <sys/types.h>
#include <unistd.h>
#include <stdlib.h>
#include <pwd.h>		/* for getpwuid() getpwnam() */
#include <grp.h>		/* for getgrgid() getgrnam() */
#include <libgen.h>		/* for basename() */
#include <fcntl.h>		/* for O_CREAT */
#include <sys/sysctl.h>		/* for sysctl */
#include <errno.h>
#include <string.h>
#ifdef _GNU_SOURCE
#include <getopt.h>
#endif
#include "gmiMIB.h"
extern const char sa_program[];

#define MY_FACILITY(__pri)	(LOG_DAEMON|(__pri))
#undef MASTER
#if !defined MODULE
extern int sa_dump;			/* default packet dump */
extern int sa_debug;			/* default no debug */
extern int sa_nomead;			/* default daemon mode */
extern int sa_output;			/* default normal output */
extern int sa_agentx;			/* default agentx mode */
extern int sa_alarms;			/* default application alarms */
extern int sa_logaddr;			/* log addresses */
extern int sa_logfillog;		/* log to sa_logfile */
extern int sa_logstderr;		/* log to standard error */
extern int sa_logstdout;		/* log to standard output */
extern int sa_logsyslog;		/* log to system logs */
extern int sa_logcallog;		/* log to callback logs */
extern int sa_appendlog;		/* append to log file without truncating */
extern char sa_logfile[256];
extern char sa_pidfile[256];
extern char sa_sysctlf[256];

/* file stream for log file */
extern FILE *stdlog;
#endif				/* !defined MODULE */
extern int sa_fclose;			/* default close files between requests */
extern int sa_fd;			/* file descriptor for MIB use */
extern int sa_readfd;			/* file descriptor for autonomnous events */
extern int sa_changed;			/* indication to reread MIB configuration */
extern int sa_stats_refresh;		/* indications that statistics, the mib or its tables need to be refreshed */
extern int sa_request;			/* request number for per-request actions */
volatile int gmiMIB_refresh = 1;
volatile int applicationProcessTable_refresh = 1;
volatile int communicationsEntityTable_refresh = 1;
volatile int communicationsInformationRecordTable_refresh = 1;
volatile int clProtocolMachineTable_refresh = 1;
volatile int coProtocolMachineTable_refresh = 1;
volatile int sap1Table_refresh = 1;
volatile int sap2Table_refresh = 1;
volatile int singlePeerConnectionTable_refresh = 1;
volatile int subsystemTable_refresh = 1;

/*
 * gmiMIB_variables_oid: object identifier for gmiMIB
 * This is the top level oid that we want to register under.  This is essentially a prefix, with the
 * suffix appearing in the variable below.
 */
oid gmiMIB_variables_oid[10] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 5 };

/*
 * Oids for use in notifications defined in this MIB.
 */
oid communicationsInformation_oid[11] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 5, 0 };

/*
 * Oids accessible only for notify defined in this MIB.
 */
oid tmnInformationType_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 5, 1, 3 };
oid tmnInformationData_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 5, 1, 3 };

/*
 * Other oids defined in this MIB.
 */
oid communicationsInformationType_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 5, 1, 4 };
oid gmiBasicCompliance_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 5, 2, 1 };
oid gmiApplicationProcessP1Package_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 5, 2, 2 };
oid gmiCommunicationsEntityP1Package_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 5, 2, 2 };
oid gmiCommunicationsInformationRecordP1Package_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 5, 2, 2 };
oid gmiInformationDataPackagePackage_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 5, 2, 2 };
oid gmiClProtocolMachineP1Package_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 5, 2, 2 };
oid gmiClProtocolMachineP2Package_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 5, 2, 2 };
oid gmiCoProtocolMachineP1Package_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 5, 2, 2 };
oid gmiSap1P1Package_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 5, 2, 2 };
oid gmiSap2P1Package_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 5, 2, 2 };
oid gmiSinglePeerConnectionP1Package_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 5, 2, 2 };
oid gmiSinglePeerConnectionP2Package_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 5, 2, 2 };
oid gmiSubsystemP1Package_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 5, 2, 2 };
oid gmiParametersGroup_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 5, 2, 2 };
oid gmiNotificationsGroup_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 3, 5, 2, 2 };
static const oid zeroDotZero_oid[2] = { 0, 0 };
static oid snmpTrapOID_oid[11] = { 1, 3, 6, 1, 6, 3, 1, 1, 4, 1, 0 };

/*
 * variable7 gmiMIB_variables: tree for gmiMIB
 * This variable defines function callbacks and type return information for the gmiMIB mib section
 */
struct variable7 gmiMIB_variables[] = {
	/* magic number, variable type, ro/rw, callback fn, L, oidsuffix */
#define   APPLICATIONPROCESSTITLE  1
	{(u_char) APPLICATIONPROCESSTITLE, ASN_OBJECT_ID, RONLY, var_applicationProcessTable, 6, {1, 2, 1, 1, 1, 2}},
#define   SUPPORTENTITYNAMES    2
	{(u_char) SUPPORTENTITYNAMES, ASN_OCTET_STR, RONLY, var_applicationProcessTable, 6, {1, 2, 1, 1, 1, 3}},
#define   APPLICATIONPROCESSOPERATIONALSTATE  3
	{(u_char) APPLICATIONPROCESSOPERATIONALSTATE, ASN_INTEGER, RONLY, var_applicationProcessTable, 6, {1, 2, 1, 1, 1, 4}},
#define   SUBSYSTEMID           4
	{(u_char) SUBSYSTEMID, ASN_OCTET_STR, RONLY, var_communicationsEntityTable, 6, {1, 2, 9, 1, 1, 1}},
#define   LOCALSAPNAMES         5
	{(u_char) LOCALSAPNAMES, ASN_OCTET_STR, RONLY, var_communicationsEntityTable, 6, {1, 2, 2, 1, 1, 2}},
#define   COMMUNICATIONSENTITYOPERATIONALSTATE  6
	{(u_char) COMMUNICATIONSENTITYOPERATIONALSTATE, ASN_INTEGER, RONLY, var_communicationsEntityTable, 6, {1, 2, 2, 1, 1, 3}},
#define   INFORMATIONTYPE       7
	{(u_char) INFORMATIONTYPE, ASN_OBJECT_ID, RONLY, var_communicationsInformationRecordTable, 6, {1, 2, 3, 1, 1, 1}},
#define   INFORMATIONDATA       8
	{(u_char) INFORMATIONDATA, ASN_OCTET_STR, RONLY, var_communicationsInformationRecordTable, 6, {1, 2, 3, 1, 1, 2}},
#define   SUBSYSTEMID           9
	{(u_char) SUBSYSTEMID, ASN_OCTET_STR, RONLY, var_clProtocolMachineTable, 6, {1, 2, 9, 1, 1, 1}},
#define   CLPROTOCOLMACHINEOPERATIONALSTATE  10
	{(u_char) CLPROTOCOLMACHINEOPERATIONALSTATE, ASN_INTEGER, RONLY, var_clProtocolMachineTable, 6, {1, 2, 4, 1, 1, 2}},
#define   TOTALREMOTESAPS       11
	{(u_char) TOTALREMOTESAPS, ASN_COUNTER64, RONLY, var_clProtocolMachineTable, 6, {1, 2, 4, 1, 1, 3}},
#define   SUBSYSTEMID           12
	{(u_char) SUBSYSTEMID, ASN_OCTET_STR, RONLY, var_coProtocolMachineTable, 6, {1, 2, 9, 1, 1, 1}},
#define   COPROTOCOLMACHINEOPERATIONALSTATE  13
	{(u_char) COPROTOCOLMACHINEOPERATIONALSTATE, ASN_INTEGER, RONLY, var_coProtocolMachineTable, 6, {1, 2, 5, 1, 1, 2}},
#define   SUBSYSTEMID           14
	{(u_char) SUBSYSTEMID, ASN_OCTET_STR, RONLY, var_sap1Table, 6, {1, 2, 9, 1, 1, 1}},
#define   SAP1ADDRESS           15
	{(u_char) SAP1ADDRESS, ASN_UNSIGNED, RONLY, var_sap1Table, 6, {1, 2, 6, 1, 1, 2}},
#define   SAP1USERENTITYNAMES   16
	{(u_char) SAP1USERENTITYNAMES, ASN_OCTET_STR, RONLY, var_sap1Table, 6, {1, 2, 6, 1, 1, 3}},
#define   SUBSYSTEMID           17
	{(u_char) SUBSYSTEMID, ASN_OCTET_STR, RONLY, var_sap2Table, 6, {1, 2, 9, 1, 1, 1}},
#define   SAP2ADDRESS           18
	{(u_char) SAP2ADDRESS, ASN_OPAQUE, RONLY, var_sap2Table, 6, {1, 2, 7, 1, 1, 2}},
#define   SAP2USERENTITYNAMES   19
	{(u_char) SAP2USERENTITYNAMES, ASN_OCTET_STR, RONLY, var_sap2Table, 6, {1, 2, 7, 1, 1, 3}},
#define   SAP2PROVIDERENTITYNAMES  20
	{(u_char) SAP2PROVIDERENTITYNAMES, ASN_OCTET_STR, RONLY, var_sap2Table, 6, {1, 2, 7, 1, 1, 4}},
#define   SUBSYSTEMID           21
	{(u_char) SUBSYSTEMID, ASN_OCTET_STR, RONLY, var_singlePeerConnectionTable, 6, {1, 2, 9, 1, 1, 1}},
#define   UNDERLYINGCONNECTIONNAMES  22
	{(u_char) UNDERLYINGCONNECTIONNAMES, ASN_OCTET_STR, RONLY, var_singlePeerConnectionTable, 6, {1, 2, 8, 1, 1, 2}},
#define   SUPPORTEDCONNECTIONNAMES  23
	{(u_char) SUPPORTEDCONNECTIONNAMES, ASN_OCTET_STR, RONLY, var_singlePeerConnectionTable, 6, {1, 2, 8, 1, 1, 3}},
#define   SUBSYSTEMID           24
	{(u_char) SUBSYSTEMID, ASN_OCTET_STR, RONLY, var_subsystemTable, 6, {1, 2, 9, 1, 1, 1}},
};

/* (L = length of the oidsuffix) */
struct gmiMIB_data *gmiMIBStorage = NULL;

/* global storage of our data, saved in and configured by header_complex() */
struct header_complex_index *applicationProcessTableStorage = NULL;
struct header_complex_index *communicationsEntityTableStorage = NULL;
struct header_complex_index *communicationsInformationRecordTableStorage = NULL;
struct header_complex_index *clProtocolMachineTableStorage = NULL;
struct header_complex_index *coProtocolMachineTableStorage = NULL;
struct header_complex_index *sap1TableStorage = NULL;
struct header_complex_index *sap2TableStorage = NULL;
struct header_complex_index *singlePeerConnectionTableStorage = NULL;
struct header_complex_index *subsystemTableStorage = NULL;

#if defined MODULE
void (*gmiMIBold_signal_handler) (int) = NULL;	/* save old signal handler just in case */
void gmiMIB_loop_handler(int);
void gmiMIB_fd_handler(int, void *);
#endif				/* defined MOUDLE */
/**
 * @fn void init_gmiMIB(void)
 * @brief gmiMIB initialization routine.
 *
 * This is called when the agent starts up.  At a minimum, registration of the MIB variables
 * structure (gmiMIB_variables) should take place here.  By default the function also
 * registers the configuration handler and configuration store callbacks.
 *
 * Additional registrations that may be considered here are calls to regsiter_readfd(),
 * register_writefd() and register_exceptfd() for hooking into the snmpd event loop, but only when
 * used as a loadable module.  By default this function establishes a single file descriptor to
 * read, or upon which to handle exceptions.  Note that the snmpd only supports a maximum of 32
 * extneral file descriptors, so these should be used sparingly.
 *
 * When running as a loadable module, it is also necessary to hook into the snmpd event loop so that
 * the current request number can be deteremined.  This is accomplished by using a trick of the
 * external_signal_scheduled and external_signal_handler mechanism which is called on each event
 * loop when external_signal_scheduled is non-zero.  This is used to increment the sa_request value
 * on each snmpd event loop interation so that calls to MIB tree functions can determine whether
 * they belong to a fresh request or not (primarily for cacheing and possibly to clean up non-polled
 * file descriptors).
 */
void
init_gmiMIB(void)
{
	(void) snmpTrapOID_oid;
	DEBUGMSGTL(("gmiMIB", "initializing...  "));
	/* register ourselves with the agent to handle our mib tree */
	REGISTER_MIB("gmiMIB", gmiMIB_variables, variable7, gmiMIB_variables_oid);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_SHUTDOWN, term_gmiMIB, NULL);
	/* register our config handler(s) to deal with registrations */
	snmpd_register_config_handler("gmiMIB", parse_gmiMIB, NULL, "HELP STRING");
	snmpd_register_config_handler("applicationProcessTable", parse_applicationProcessTable, NULL, "HELP STRING");
	snmpd_register_config_handler("communicationsEntityTable", parse_communicationsEntityTable, NULL, "HELP STRING");
	snmpd_register_config_handler("communicationsInformationRecordTable", parse_communicationsInformationRecordTable, NULL, "HELP STRING");
	snmpd_register_config_handler("clProtocolMachineTable", parse_clProtocolMachineTable, NULL, "HELP STRING");
	snmpd_register_config_handler("coProtocolMachineTable", parse_coProtocolMachineTable, NULL, "HELP STRING");
	snmpd_register_config_handler("sap1Table", parse_sap1Table, NULL, "HELP STRING");
	snmpd_register_config_handler("sap2Table", parse_sap2Table, NULL, "HELP STRING");
	snmpd_register_config_handler("singlePeerConnectionTable", parse_singlePeerConnectionTable, NULL, "HELP STRING");
	snmpd_register_config_handler("subsystemTable", parse_subsystemTable, NULL, "HELP STRING");

	/* we need to be called back later to store our data */
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_gmiMIB, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_applicationProcessTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_communicationsEntityTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_communicationsInformationRecordTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_clProtocolMachineTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_coProtocolMachineTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_sap1Table, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_sap2Table, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_singlePeerConnectionTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_subsystemTable, NULL);

	/* place any other initialization junk you need here */
#if defined MODULE
	if (sa_readfd != 0) {
		register_readfd(sa_readfd, gmiMIB_fd_handler, (void *) 0);
		register_exceptfd(sa_readfd, gmiMIB_fd_handler, (void *) 1);
	}
#if defined MASTER
	gmiMIBold_signal_handler = external_signal_handler[0];
	external_signal_handler[0] = &gmiMIB_loop_handler;
#endif				/* defined MASTER */
#endif				/* defined MODULE */
	DEBUGMSGTL(("gmiMIB", "done.\n"));
}

/**
 * @fn void deinit_gmiMIB(void)
 * @brief deinitialization routine.
 *
 * This is called before the agent is unloaded.  At a minimum, deregistration of the MIB variables
 * structure (gmiMIB_variables) should take place here.  By default, the function also
 * deregisters the the configuration file handlers for the MIB variables and table rows.
 *
 * Additional deregistrations that may be required here are calls to unregister_readfd(),
 * unregister_writefd() and unregsiter_exceptfd() for unhooking from the snmpd event loop, but only
 * when used as a loadable module.  By default if a read file descriptor exists, it is unregistered.
 */
void
deinit_gmiMIB(void)
{
	DEBUGMSGTL(("gmiMIB", "deinitializating...  "));
#if defined MODULE
#if defined MASTER
	external_signal_handler[0] = gmiMIBold_signal_handler;
#endif				/* defined MASTER */
	if (sa_readfd != 0) {
		unregister_exceptfd(sa_readfd);
		unregister_readfd(sa_readfd);
		close(sa_readfd);
		sa_readfd = 0;
	}
#endif				/* defined MODULE */
	unregister_mib(gmiMIB_variables_oid, sizeof(gmiMIB_variables_oid) / sizeof(oid));
	snmpd_unregister_config_handler("gmiMIB");
	snmpd_unregister_config_handler("applicationProcessTable");
	snmpd_unregister_config_handler("communicationsEntityTable");
	snmpd_unregister_config_handler("communicationsInformationRecordTable");
	snmpd_unregister_config_handler("clProtocolMachineTable");
	snmpd_unregister_config_handler("coProtocolMachineTable");
	snmpd_unregister_config_handler("sap1Table");
	snmpd_unregister_config_handler("sap2Table");
	snmpd_unregister_config_handler("singlePeerConnectionTable");
	snmpd_unregister_config_handler("subsystemTable");

	/* place any other de-initialization junk you need here */
	DEBUGMSGTL(("gmiMIB", "done.\n"));
}

int
term_gmiMIB(int majorID, int minorID, void *serverarg, void *clientarg)
{
	deinit_gmiMIB();
	return 0;
}

/**
 * @fn struct gmiMIB_data *gmiMIB_create(void)
 * @brief create a fresh data structure representing scalars in gmiMIB.
 *
 * Creates a new gmiMIB_data structure by allocating dynamic memory for the structure and
 * initializing the default values of scalars in gmiMIB.
 */
struct gmiMIB_data *
gmiMIB_create(void)
{
	struct gmiMIB_data *StorageNew = SNMP_MALLOC_STRUCT(gmiMIB_data);

	DEBUGMSGTL(("gmiMIB", "creating scalars...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default scalar values here into StorageNew */

	}
	DEBUGMSGTL(("gmiMIB", "done.\n"));
	return (StorageNew);
}

/**
 * @fn int gmiMIB_destroy(struct gmiMIB_data **thedata)
 * @param thedata pointer to the data structure in gmiMIB.
 * @brief delete a scalars structure from gmiMIB.
 *
 * Frees scalars that were previously removed from gmiMIB.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
gmiMIB_destroy(struct gmiMIB_data **thedata)
{
	struct gmiMIB_data *StorageDel;

	DEBUGMSGTL(("gmiMIB", "deleting scalars...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("gmiMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int gmiMIB_add(struct gmiMIB_data *thedata)
 * @param thedata the structure representing gmiMIB scalars.
 * @brief adds node to the gmiMIB scalar data set.
 *
 * Adds a scalar structure to the gmiMIB data set.  Note that this function is necessary even
 * when the scalar values are not peristent.
 */
int
gmiMIB_add(struct gmiMIB_data *thedata)
{
	DEBUGMSGTL(("gmiMIB", "adding data...  "));
	gmiMIBStorage = thedata;
	DEBUGMSGTL(("gmiMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_gmiMIB(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for gmiMIB entries.
 *
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case gmiMIB).  This routine is invoked by
 * UCD-SNMP to read the values of scalars in the MIB from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the MIB.  If there are no configured entries
 * in the configuration MIB, this function will simply not be called.
 */
void
parse_gmiMIB(const char *token, char *line)
{
	size_t tmpsize;
	struct gmiMIB_data *StorageTmp = gmiMIB_create();

	DEBUGMSGTL(("gmiMIB", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual scalars that are not persistent */
	gmiMIB_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("gmiMIB", "done.\n"));
}

/*
 * store_gmiMIB(): stores .conf file entries needed to configure the mib.
 */
int
store_gmiMIB(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct gmiMIB_data *StorageTmp;

	DEBUGMSGTL(("gmiMIB", "storing data...  "));
	refresh_gmiMIB();
	if ((StorageTmp = gmiMIBStorage) == NULL) {
		DEBUGMSGTL(("gmiMIB", "error.\n"));
		return SNMPERR_GENERR;
	}
	(void) tmpsize;
	/* XXX: comment entire section if no scalars are persistent */
	{
		memset(line, 0, sizeof(line));
		strcat(line, "gmiMIB ");
		cptr = line + strlen(line);
		/* XXX: remove individual scalars that are not persistent */
		snmpd_store_config(line);
	}
	DEBUGMSGTL(("gmiMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void refresh_gmiMIB(void)
 * @brief refresh the scalar values of gmiMIB.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a scalar has been requested).
 */
void
refresh_gmiMIB(void)
{
	if (gmiMIBStorage == NULL) {
		struct gmiMIB_data *StorageNew;

		if ((StorageNew = gmiMIB_create()) == NULL)
			return;
		gmiMIBStorage = StorageNew;
		gmiMIB_refresh = 1;
	}
	if (gmiMIB_refresh == 0)
		return;
	gmiMIB_refresh = 0;
	/* XXX: Update scalars as required here... */
}

/**
 * @fn u_char * var_gmiMIB(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @param vp a pointer to the entry in the variables table for the requested variable.
 * @param name the object identifier for which to find.
 * @param length the length of the object identifier.
 * @param exact whether the name is exact.
 * @param var_len a pointer to the length of the representation of the object.
 * @param write_method a pointer to a write method for the object.
 * @brief locate variables in gmiMIB.
 *
 * This function returns a pointer to a memory area that is static across the request that contains
 * the UCD-SNMP representation of the scalar (so that it may be used to read from for a GET,
 * GET-NEXT or GET-BULK request).  This returned pointer may be NULL, in which case the function is
 * telling UCD-SNMP that the scalar does not exist for reading; however, if write_method is
 * overwritten with a non-NULL value, the function is telling UCD-SNMP that the scalar exists for
 * writing.  Write-only objects can be effected in this way.
 */
u_char *
var_gmiMIB(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct gmiMIB_data *StorageTmp;
	u_char *rval;

	if (header_generic(vp, name, length, exact, var_len, write_method) == MATCH_FAILED)
		return NULL;
	/* Refresh the MIB values if required. */
	refresh_gmiMIB();
	if ((StorageTmp = gmiMIBStorage) == NULL)
		return NULL;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn struct applicationProcessTable_data *applicationProcessTable_create(void)
 * @brief create a fresh data structure representing a new row in the applicationProcessTable table.
 *
 * Creates a new applicationProcessTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct applicationProcessTable_data *
applicationProcessTable_create(void)
{
	struct applicationProcessTable_data *StorageNew = SNMP_MALLOC_STRUCT(applicationProcessTable_data);

	DEBUGMSGTL(("applicationProcessTable", "creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->applicationProcessTitle = snmp_duplicate_objid(zeroDotZero_oid, 2))) {
			StorageNew->applicationProcessTitleLen = 2;
		}
		if ((StorageNew->supportEntityNames = (uint8_t *) strdup("")) != NULL) {
			StorageNew->supportEntityNamesLen = strlen("");
		}
		StorageNew->applicationProcessOperationalState = 0;

	}
	DEBUGMSGTL(("applicationProcessTable", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct applicationProcessTable_data *applicationProcessTable_duplicate(struct applicationProcessTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicat a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct applicationProcessTable_data *
applicationProcessTable_duplicate(struct applicationProcessTable_data *thedata)
{
	struct applicationProcessTable_data *StorageNew = SNMP_MALLOC_STRUCT(applicationProcessTable_data);

	DEBUGMSGTL(("applicationProcessTable", "duplicating row...  "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("applicationProcessTable", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	applicationProcessTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int applicationProcessTable_destroy(struct applicationProcessTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
applicationProcessTable_destroy(struct applicationProcessTable_data **thedata)
{
	struct applicationProcessTable_data *StorageDel;

	DEBUGMSGTL(("applicationProcessTable", "deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->applicationProcessId);
		StorageDel->applicationProcessIdLen = 0;
		SNMP_FREE(StorageDel->applicationProcessTitle);
		StorageDel->applicationProcessTitleLen = 0;
		SNMP_FREE(StorageDel->supportEntityNames);
		StorageDel->supportEntityNamesLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("applicationProcessTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int applicationProcessTable_add(struct applicationProcessTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the applicationProcessTable table data set.
 *
 * Adds a table row structure to the applicationProcessTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
applicationProcessTable_add(struct applicationProcessTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("applicationProcessTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index the data */
	/* applicationProcessId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->applicationProcessId, thedata->applicationProcessIdLen);
	header_complex_add_data(&applicationProcessTableStorage, vars, thedata);
	DEBUGMSGTL(("applicationProcessTable", "registered an entry\n"));
	DEBUGMSGTL(("applicationProcessTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int applicationProcessTable_del(struct applicationProcessTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the applicationProcessTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
applicationProcessTable_del(struct applicationProcessTable_data *thedata)
{
	struct applicationProcessTable_data *StorageDel;

	DEBUGMSGTL(("applicationProcessTable", "deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(applicationProcessTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&applicationProcessTableStorage, hciptr);
	}
	DEBUGMSGTL(("applicationProcessTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_applicationProcessTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for applicationProcessTable entries.
 *
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case applicationProcessTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_applicationProcessTable(const char *token, char *line)
{
	size_t tmpsize;
	struct applicationProcessTable_data *StorageTmp = applicationProcessTable_create();

	DEBUGMSGTL(("applicationProcessTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->applicationProcessId, &StorageTmp->applicationProcessIdLen);
	if (StorageTmp->applicationProcessId == NULL) {
		config_perror("invalid specification for applicationProcessId");
		return;
	}
	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->applicationProcessTitle, &StorageTmp->applicationProcessTitleLen);
	if (StorageTmp->applicationProcessTitle == NULL) {
		config_perror("invalid specification for applicationProcessTitle");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->supportEntityNames, &StorageTmp->supportEntityNamesLen);
	if (StorageTmp->supportEntityNames == NULL) {
		config_perror("invalid specification for supportEntityNames");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->applicationProcessOperationalState, &tmpsize);
	applicationProcessTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("applicationProcessTable", "done.\n"));
}

/*
 * store_applicationProcessTable(): store configuraiton file for applicationProcessTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_applicationProcessTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct applicationProcessTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("applicationProcessTable", "storing data...  "));
	refresh_applicationProcessTable();
	(void) tmpsize;
	for (hcindex = applicationProcessTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct applicationProcessTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "applicationProcessTable ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->applicationProcessId, &StorageTmp->applicationProcessIdLen);
			cptr = read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->applicationProcessTitle, &StorageTmp->applicationProcessTitleLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->supportEntityNames, &StorageTmp->supportEntityNamesLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->applicationProcessOperationalState, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("applicationProcessTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct communicationsEntityTable_data *communicationsEntityTable_create(void)
 * @brief create a fresh data structure representing a new row in the communicationsEntityTable table.
 *
 * Creates a new communicationsEntityTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct communicationsEntityTable_data *
communicationsEntityTable_create(void)
{
	struct communicationsEntityTable_data *StorageNew = SNMP_MALLOC_STRUCT(communicationsEntityTable_data);

	DEBUGMSGTL(("communicationsEntityTable", "creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->subsystemId = (uint8_t *) strdup("")) != NULL) {
			StorageNew->subsystemIdLen = strlen("");
		}
		if ((StorageNew->localSapNames = (uint8_t *) strdup("")) != NULL) {
			StorageNew->localSapNamesLen = strlen("");
		}
		StorageNew->communicationsEntityOperationalState = 0;

	}
	DEBUGMSGTL(("communicationsEntityTable", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct communicationsEntityTable_data *communicationsEntityTable_duplicate(struct communicationsEntityTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicat a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct communicationsEntityTable_data *
communicationsEntityTable_duplicate(struct communicationsEntityTable_data *thedata)
{
	struct communicationsEntityTable_data *StorageNew = SNMP_MALLOC_STRUCT(communicationsEntityTable_data);

	DEBUGMSGTL(("communicationsEntityTable", "duplicating row...  "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("communicationsEntityTable", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	communicationsEntityTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int communicationsEntityTable_destroy(struct communicationsEntityTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
communicationsEntityTable_destroy(struct communicationsEntityTable_data **thedata)
{
	struct communicationsEntityTable_data *StorageDel;

	DEBUGMSGTL(("communicationsEntityTable", "deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->subsystemId);
		StorageDel->subsystemIdLen = 0;
		SNMP_FREE(StorageDel->communicationsEntityId);
		StorageDel->communicationsEntityIdLen = 0;
		SNMP_FREE(StorageDel->localSapNames);
		StorageDel->localSapNamesLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("communicationsEntityTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int communicationsEntityTable_add(struct communicationsEntityTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the communicationsEntityTable table data set.
 *
 * Adds a table row structure to the communicationsEntityTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
communicationsEntityTable_add(struct communicationsEntityTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("communicationsEntityTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index the data */
	/* subsystemId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->subsystemId, thedata->subsystemIdLen);
	/* communicationsEntityId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->communicationsEntityId, thedata->communicationsEntityIdLen);
	header_complex_add_data(&communicationsEntityTableStorage, vars, thedata);
	DEBUGMSGTL(("communicationsEntityTable", "registered an entry\n"));
	DEBUGMSGTL(("communicationsEntityTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int communicationsEntityTable_del(struct communicationsEntityTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the communicationsEntityTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
communicationsEntityTable_del(struct communicationsEntityTable_data *thedata)
{
	struct communicationsEntityTable_data *StorageDel;

	DEBUGMSGTL(("communicationsEntityTable", "deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(communicationsEntityTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&communicationsEntityTableStorage, hciptr);
	}
	DEBUGMSGTL(("communicationsEntityTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_communicationsEntityTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for communicationsEntityTable entries.
 *
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case communicationsEntityTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_communicationsEntityTable(const char *token, char *line)
{
	size_t tmpsize;
	struct communicationsEntityTable_data *StorageTmp = communicationsEntityTable_create();

	DEBUGMSGTL(("communicationsEntityTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->subsystemId, &StorageTmp->subsystemIdLen);
	if (StorageTmp->subsystemId == NULL) {
		config_perror("invalid specification for subsystemId");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->communicationsEntityId, &StorageTmp->communicationsEntityIdLen);
	if (StorageTmp->communicationsEntityId == NULL) {
		config_perror("invalid specification for communicationsEntityId");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->localSapNames, &StorageTmp->localSapNamesLen);
	if (StorageTmp->localSapNames == NULL) {
		config_perror("invalid specification for localSapNames");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->communicationsEntityOperationalState, &tmpsize);
	communicationsEntityTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("communicationsEntityTable", "done.\n"));
}

/*
 * store_communicationsEntityTable(): store configuraiton file for communicationsEntityTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_communicationsEntityTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct communicationsEntityTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("communicationsEntityTable", "storing data...  "));
	refresh_communicationsEntityTable();
	(void) tmpsize;
	for (hcindex = communicationsEntityTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct communicationsEntityTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "communicationsEntityTable ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->subsystemId, &StorageTmp->subsystemIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->communicationsEntityId, &StorageTmp->communicationsEntityIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->localSapNames, &StorageTmp->localSapNamesLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->communicationsEntityOperationalState, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("communicationsEntityTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct communicationsInformationRecordTable_data *communicationsInformationRecordTable_create(void)
 * @brief create a fresh data structure representing a new row in the communicationsInformationRecordTable table.
 *
 * Creates a new communicationsInformationRecordTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct communicationsInformationRecordTable_data *
communicationsInformationRecordTable_create(void)
{
	struct communicationsInformationRecordTable_data *StorageNew = SNMP_MALLOC_STRUCT(communicationsInformationRecordTable_data);

	DEBUGMSGTL(("communicationsInformationRecordTable", "creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->logId = (uint8_t *) strdup("")) != NULL) {
			StorageNew->logIdLen = strlen("");
		}
		if ((StorageNew->logRecordId = (uint8_t *) strdup("")) != NULL) {
			StorageNew->logRecordIdLen = strlen("");
		}
		if ((StorageNew->informationType = snmp_duplicate_objid(zeroDotZero_oid, 2))) {
			StorageNew->informationTypeLen = 2;
		}
		if ((StorageNew->informationData = (uint8_t *) strdup("")) != NULL) {
			StorageNew->informationDataLen = strlen("");
		}

	}
	DEBUGMSGTL(("communicationsInformationRecordTable", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct communicationsInformationRecordTable_data *communicationsInformationRecordTable_duplicate(struct communicationsInformationRecordTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicat a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct communicationsInformationRecordTable_data *
communicationsInformationRecordTable_duplicate(struct communicationsInformationRecordTable_data *thedata)
{
	struct communicationsInformationRecordTable_data *StorageNew = SNMP_MALLOC_STRUCT(communicationsInformationRecordTable_data);

	DEBUGMSGTL(("communicationsInformationRecordTable", "duplicating row...  "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("communicationsInformationRecordTable", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	communicationsInformationRecordTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int communicationsInformationRecordTable_destroy(struct communicationsInformationRecordTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
communicationsInformationRecordTable_destroy(struct communicationsInformationRecordTable_data **thedata)
{
	struct communicationsInformationRecordTable_data *StorageDel;

	DEBUGMSGTL(("communicationsInformationRecordTable", "deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->logId);
		StorageDel->logIdLen = 0;
		SNMP_FREE(StorageDel->logRecordId);
		StorageDel->logRecordIdLen = 0;
		SNMP_FREE(StorageDel->informationType);
		StorageDel->informationTypeLen = 0;
		SNMP_FREE(StorageDel->informationData);
		StorageDel->informationDataLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("communicationsInformationRecordTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int communicationsInformationRecordTable_add(struct communicationsInformationRecordTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the communicationsInformationRecordTable table data set.
 *
 * Adds a table row structure to the communicationsInformationRecordTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
communicationsInformationRecordTable_add(struct communicationsInformationRecordTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("communicationsInformationRecordTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index the data */
	/* logId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->logId, thedata->logIdLen);
	/* logRecordId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->logRecordId, thedata->logRecordIdLen);
	header_complex_add_data(&communicationsInformationRecordTableStorage, vars, thedata);
	DEBUGMSGTL(("communicationsInformationRecordTable", "registered an entry\n"));
	DEBUGMSGTL(("communicationsInformationRecordTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int communicationsInformationRecordTable_del(struct communicationsInformationRecordTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the communicationsInformationRecordTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
communicationsInformationRecordTable_del(struct communicationsInformationRecordTable_data *thedata)
{
	struct communicationsInformationRecordTable_data *StorageDel;

	DEBUGMSGTL(("communicationsInformationRecordTable", "deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(communicationsInformationRecordTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&communicationsInformationRecordTableStorage, hciptr);
	}
	DEBUGMSGTL(("communicationsInformationRecordTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_communicationsInformationRecordTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for communicationsInformationRecordTable entries.
 *
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case communicationsInformationRecordTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_communicationsInformationRecordTable(const char *token, char *line)
{
	size_t tmpsize;
	struct communicationsInformationRecordTable_data *StorageTmp = communicationsInformationRecordTable_create();

	DEBUGMSGTL(("communicationsInformationRecordTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->logId, &StorageTmp->logIdLen);
	if (StorageTmp->logId == NULL) {
		config_perror("invalid specification for logId");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->logRecordId, &StorageTmp->logRecordIdLen);
	if (StorageTmp->logRecordId == NULL) {
		config_perror("invalid specification for logRecordId");
		return;
	}
	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->informationType, &StorageTmp->informationTypeLen);
	if (StorageTmp->informationType == NULL) {
		config_perror("invalid specification for informationType");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->informationData, &StorageTmp->informationDataLen);
	if (StorageTmp->informationData == NULL) {
		config_perror("invalid specification for informationData");
		return;
	}
	communicationsInformationRecordTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("communicationsInformationRecordTable", "done.\n"));
}

/*
 * store_communicationsInformationRecordTable(): store configuraiton file for communicationsInformationRecordTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_communicationsInformationRecordTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct communicationsInformationRecordTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("communicationsInformationRecordTable", "storing data...  "));
	refresh_communicationsInformationRecordTable();
	(void) tmpsize;
	for (hcindex = communicationsInformationRecordTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct communicationsInformationRecordTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "communicationsInformationRecordTable ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->logId, &StorageTmp->logIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->logRecordId, &StorageTmp->logRecordIdLen);
			cptr = read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->informationType, &StorageTmp->informationTypeLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->informationData, &StorageTmp->informationDataLen);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("communicationsInformationRecordTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct clProtocolMachineTable_data *clProtocolMachineTable_create(void)
 * @brief create a fresh data structure representing a new row in the clProtocolMachineTable table.
 *
 * Creates a new clProtocolMachineTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct clProtocolMachineTable_data *
clProtocolMachineTable_create(void)
{
	struct clProtocolMachineTable_data *StorageNew = SNMP_MALLOC_STRUCT(clProtocolMachineTable_data);

	DEBUGMSGTL(("clProtocolMachineTable", "creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->subsystemId = (uint8_t *) strdup("")) != NULL) {
			StorageNew->subsystemIdLen = strlen("");
		}
		if ((StorageNew->communicationsEntityId = (uint8_t *) strdup("")) != NULL) {
			StorageNew->communicationsEntityIdLen = strlen("");
		}
		StorageNew->clProtocolMachineOperationalState = 0;
		StorageNew->totalRemoteSAPs = 0;

	}
	DEBUGMSGTL(("clProtocolMachineTable", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct clProtocolMachineTable_data *clProtocolMachineTable_duplicate(struct clProtocolMachineTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicat a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct clProtocolMachineTable_data *
clProtocolMachineTable_duplicate(struct clProtocolMachineTable_data *thedata)
{
	struct clProtocolMachineTable_data *StorageNew = SNMP_MALLOC_STRUCT(clProtocolMachineTable_data);

	DEBUGMSGTL(("clProtocolMachineTable", "duplicating row...  "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("clProtocolMachineTable", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	clProtocolMachineTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int clProtocolMachineTable_destroy(struct clProtocolMachineTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
clProtocolMachineTable_destroy(struct clProtocolMachineTable_data **thedata)
{
	struct clProtocolMachineTable_data *StorageDel;

	DEBUGMSGTL(("clProtocolMachineTable", "deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->subsystemId);
		StorageDel->subsystemIdLen = 0;
		SNMP_FREE(StorageDel->communicationsEntityId);
		StorageDel->communicationsEntityIdLen = 0;
		SNMP_FREE(StorageDel->clProtocolMachineId);
		StorageDel->clProtocolMachineIdLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("clProtocolMachineTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int clProtocolMachineTable_add(struct clProtocolMachineTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the clProtocolMachineTable table data set.
 *
 * Adds a table row structure to the clProtocolMachineTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
clProtocolMachineTable_add(struct clProtocolMachineTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("clProtocolMachineTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index the data */
	/* subsystemId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->subsystemId, thedata->subsystemIdLen);
	/* communicationsEntityId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->communicationsEntityId, thedata->communicationsEntityIdLen);
	/* clProtocolMachineId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->clProtocolMachineId, thedata->clProtocolMachineIdLen);
	header_complex_add_data(&clProtocolMachineTableStorage, vars, thedata);
	DEBUGMSGTL(("clProtocolMachineTable", "registered an entry\n"));
	DEBUGMSGTL(("clProtocolMachineTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int clProtocolMachineTable_del(struct clProtocolMachineTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the clProtocolMachineTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
clProtocolMachineTable_del(struct clProtocolMachineTable_data *thedata)
{
	struct clProtocolMachineTable_data *StorageDel;

	DEBUGMSGTL(("clProtocolMachineTable", "deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(clProtocolMachineTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&clProtocolMachineTableStorage, hciptr);
	}
	DEBUGMSGTL(("clProtocolMachineTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_clProtocolMachineTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for clProtocolMachineTable entries.
 *
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case clProtocolMachineTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_clProtocolMachineTable(const char *token, char *line)
{
	size_t tmpsize;
	struct clProtocolMachineTable_data *StorageTmp = clProtocolMachineTable_create();

	DEBUGMSGTL(("clProtocolMachineTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->subsystemId, &StorageTmp->subsystemIdLen);
	if (StorageTmp->subsystemId == NULL) {
		config_perror("invalid specification for subsystemId");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->communicationsEntityId, &StorageTmp->communicationsEntityIdLen);
	if (StorageTmp->communicationsEntityId == NULL) {
		config_perror("invalid specification for communicationsEntityId");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->clProtocolMachineId, &StorageTmp->clProtocolMachineIdLen);
	if (StorageTmp->clProtocolMachineId == NULL) {
		config_perror("invalid specification for clProtocolMachineId");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->clProtocolMachineOperationalState, &tmpsize);
	line = read_config_read_data(ASN_COUNTER64, line, &StorageTmp->totalRemoteSAPs, &tmpsize);
	clProtocolMachineTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("clProtocolMachineTable", "done.\n"));
}

/*
 * store_clProtocolMachineTable(): store configuraiton file for clProtocolMachineTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_clProtocolMachineTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct clProtocolMachineTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("clProtocolMachineTable", "storing data...  "));
	refresh_clProtocolMachineTable();
	(void) tmpsize;
	for (hcindex = clProtocolMachineTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct clProtocolMachineTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "clProtocolMachineTable ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->subsystemId, &StorageTmp->subsystemIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->communicationsEntityId, &StorageTmp->communicationsEntityIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->clProtocolMachineId, &StorageTmp->clProtocolMachineIdLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->clProtocolMachineOperationalState, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER64, cptr, &StorageTmp->totalRemoteSAPs, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("clProtocolMachineTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct coProtocolMachineTable_data *coProtocolMachineTable_create(void)
 * @brief create a fresh data structure representing a new row in the coProtocolMachineTable table.
 *
 * Creates a new coProtocolMachineTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct coProtocolMachineTable_data *
coProtocolMachineTable_create(void)
{
	struct coProtocolMachineTable_data *StorageNew = SNMP_MALLOC_STRUCT(coProtocolMachineTable_data);

	DEBUGMSGTL(("coProtocolMachineTable", "creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->subsystemId = (uint8_t *) strdup("")) != NULL) {
			StorageNew->subsystemIdLen = strlen("");
		}
		if ((StorageNew->communicationsEntityId = (uint8_t *) strdup("")) != NULL) {
			StorageNew->communicationsEntityIdLen = strlen("");
		}
		StorageNew->coProtocolMachineOperationalState = 0;

	}
	DEBUGMSGTL(("coProtocolMachineTable", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct coProtocolMachineTable_data *coProtocolMachineTable_duplicate(struct coProtocolMachineTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicat a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct coProtocolMachineTable_data *
coProtocolMachineTable_duplicate(struct coProtocolMachineTable_data *thedata)
{
	struct coProtocolMachineTable_data *StorageNew = SNMP_MALLOC_STRUCT(coProtocolMachineTable_data);

	DEBUGMSGTL(("coProtocolMachineTable", "duplicating row...  "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("coProtocolMachineTable", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	coProtocolMachineTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int coProtocolMachineTable_destroy(struct coProtocolMachineTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
coProtocolMachineTable_destroy(struct coProtocolMachineTable_data **thedata)
{
	struct coProtocolMachineTable_data *StorageDel;

	DEBUGMSGTL(("coProtocolMachineTable", "deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->subsystemId);
		StorageDel->subsystemIdLen = 0;
		SNMP_FREE(StorageDel->communicationsEntityId);
		StorageDel->communicationsEntityIdLen = 0;
		SNMP_FREE(StorageDel->coProtocolMachineId);
		StorageDel->coProtocolMachineIdLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("coProtocolMachineTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int coProtocolMachineTable_add(struct coProtocolMachineTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the coProtocolMachineTable table data set.
 *
 * Adds a table row structure to the coProtocolMachineTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
coProtocolMachineTable_add(struct coProtocolMachineTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("coProtocolMachineTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index the data */
	/* subsystemId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->subsystemId, thedata->subsystemIdLen);
	/* communicationsEntityId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->communicationsEntityId, thedata->communicationsEntityIdLen);
	/* coProtocolMachineId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->coProtocolMachineId, thedata->coProtocolMachineIdLen);
	header_complex_add_data(&coProtocolMachineTableStorage, vars, thedata);
	DEBUGMSGTL(("coProtocolMachineTable", "registered an entry\n"));
	DEBUGMSGTL(("coProtocolMachineTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int coProtocolMachineTable_del(struct coProtocolMachineTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the coProtocolMachineTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
coProtocolMachineTable_del(struct coProtocolMachineTable_data *thedata)
{
	struct coProtocolMachineTable_data *StorageDel;

	DEBUGMSGTL(("coProtocolMachineTable", "deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(coProtocolMachineTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&coProtocolMachineTableStorage, hciptr);
	}
	DEBUGMSGTL(("coProtocolMachineTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_coProtocolMachineTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for coProtocolMachineTable entries.
 *
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case coProtocolMachineTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_coProtocolMachineTable(const char *token, char *line)
{
	size_t tmpsize;
	struct coProtocolMachineTable_data *StorageTmp = coProtocolMachineTable_create();

	DEBUGMSGTL(("coProtocolMachineTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->subsystemId, &StorageTmp->subsystemIdLen);
	if (StorageTmp->subsystemId == NULL) {
		config_perror("invalid specification for subsystemId");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->communicationsEntityId, &StorageTmp->communicationsEntityIdLen);
	if (StorageTmp->communicationsEntityId == NULL) {
		config_perror("invalid specification for communicationsEntityId");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->coProtocolMachineId, &StorageTmp->coProtocolMachineIdLen);
	if (StorageTmp->coProtocolMachineId == NULL) {
		config_perror("invalid specification for coProtocolMachineId");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->coProtocolMachineOperationalState, &tmpsize);
	coProtocolMachineTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("coProtocolMachineTable", "done.\n"));
}

/*
 * store_coProtocolMachineTable(): store configuraiton file for coProtocolMachineTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_coProtocolMachineTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct coProtocolMachineTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("coProtocolMachineTable", "storing data...  "));
	refresh_coProtocolMachineTable();
	(void) tmpsize;
	for (hcindex = coProtocolMachineTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct coProtocolMachineTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "coProtocolMachineTable ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->subsystemId, &StorageTmp->subsystemIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->communicationsEntityId, &StorageTmp->communicationsEntityIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->coProtocolMachineId, &StorageTmp->coProtocolMachineIdLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->coProtocolMachineOperationalState, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("coProtocolMachineTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct sap1Table_data *sap1Table_create(void)
 * @brief create a fresh data structure representing a new row in the sap1Table table.
 *
 * Creates a new sap1Table_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct sap1Table_data *
sap1Table_create(void)
{
	struct sap1Table_data *StorageNew = SNMP_MALLOC_STRUCT(sap1Table_data);

	DEBUGMSGTL(("sap1Table", "creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->subsystemId = (uint8_t *) strdup("")) != NULL) {
			StorageNew->subsystemIdLen = strlen("");
		}
		if ((StorageNew->communicationsEntityId = (uint8_t *) strdup("")) != NULL) {
			StorageNew->communicationsEntityIdLen = strlen("");
		}
		StorageNew->sap1Address = 0;
		if ((StorageNew->sap1UserEntityNames = (uint8_t *) strdup("")) != NULL) {
			StorageNew->sap1UserEntityNamesLen = strlen("");
		}

	}
	DEBUGMSGTL(("sap1Table", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct sap1Table_data *sap1Table_duplicate(struct sap1Table_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicat a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct sap1Table_data *
sap1Table_duplicate(struct sap1Table_data *thedata)
{
	struct sap1Table_data *StorageNew = SNMP_MALLOC_STRUCT(sap1Table_data);

	DEBUGMSGTL(("sap1Table", "duplicating row...  "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("sap1Table", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	sap1Table_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int sap1Table_destroy(struct sap1Table_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
sap1Table_destroy(struct sap1Table_data **thedata)
{
	struct sap1Table_data *StorageDel;

	DEBUGMSGTL(("sap1Table", "deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->subsystemId);
		StorageDel->subsystemIdLen = 0;
		SNMP_FREE(StorageDel->communicationsEntityId);
		StorageDel->communicationsEntityIdLen = 0;
		SNMP_FREE(StorageDel->sap1Id);
		StorageDel->sap1IdLen = 0;
		SNMP_FREE(StorageDel->sap1UserEntityNames);
		StorageDel->sap1UserEntityNamesLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("sap1Table", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int sap1Table_add(struct sap1Table_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the sap1Table table data set.
 *
 * Adds a table row structure to the sap1Table table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
sap1Table_add(struct sap1Table_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("sap1Table", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index the data */
	/* subsystemId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->subsystemId, thedata->subsystemIdLen);
	/* communicationsEntityId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->communicationsEntityId, thedata->communicationsEntityIdLen);
	/* sap1Id */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->sap1Id, thedata->sap1IdLen);
	header_complex_add_data(&sap1TableStorage, vars, thedata);
	DEBUGMSGTL(("sap1Table", "registered an entry\n"));
	DEBUGMSGTL(("sap1Table", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int sap1Table_del(struct sap1Table_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the sap1Table table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
sap1Table_del(struct sap1Table_data *thedata)
{
	struct sap1Table_data *StorageDel;

	DEBUGMSGTL(("sap1Table", "deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(sap1TableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&sap1TableStorage, hciptr);
	}
	DEBUGMSGTL(("sap1Table", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_sap1Table(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for sap1Table entries.
 *
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case sap1Table).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_sap1Table(const char *token, char *line)
{
	size_t tmpsize;
	struct sap1Table_data *StorageTmp = sap1Table_create();

	DEBUGMSGTL(("sap1Table", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->subsystemId, &StorageTmp->subsystemIdLen);
	if (StorageTmp->subsystemId == NULL) {
		config_perror("invalid specification for subsystemId");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->communicationsEntityId, &StorageTmp->communicationsEntityIdLen);
	if (StorageTmp->communicationsEntityId == NULL) {
		config_perror("invalid specification for communicationsEntityId");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->sap1Id, &StorageTmp->sap1IdLen);
	if (StorageTmp->sap1Id == NULL) {
		config_perror("invalid specification for sap1Id");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->sap1Address, &tmpsize);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->sap1UserEntityNames, &StorageTmp->sap1UserEntityNamesLen);
	if (StorageTmp->sap1UserEntityNames == NULL) {
		config_perror("invalid specification for sap1UserEntityNames");
		return;
	}
	sap1Table_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("sap1Table", "done.\n"));
}

/*
 * store_sap1Table(): store configuraiton file for sap1Table
 * stores .conf file entries needed to configure the mib.
 */
int
store_sap1Table(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct sap1Table_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("sap1Table", "storing data...  "));
	refresh_sap1Table();
	(void) tmpsize;
	for (hcindex = sap1TableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct sap1Table_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "sap1Table ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->subsystemId, &StorageTmp->subsystemIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->communicationsEntityId, &StorageTmp->communicationsEntityIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->sap1Id, &StorageTmp->sap1IdLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->sap1Address, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->sap1UserEntityNames, &StorageTmp->sap1UserEntityNamesLen);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("sap1Table", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct sap2Table_data *sap2Table_create(void)
 * @brief create a fresh data structure representing a new row in the sap2Table table.
 *
 * Creates a new sap2Table_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct sap2Table_data *
sap2Table_create(void)
{
	struct sap2Table_data *StorageNew = SNMP_MALLOC_STRUCT(sap2Table_data);

	DEBUGMSGTL(("sap2Table", "creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->subsystemId = (uint8_t *) strdup("")) != NULL) {
			StorageNew->subsystemIdLen = strlen("");
		}
		if ((StorageNew->sap2Address = (uint8_t *) strdup("")) != NULL) {
			StorageNew->sap2AddressLen = strlen("");
		}
		if ((StorageNew->sap2UserEntityNames = (uint8_t *) strdup("")) != NULL) {
			StorageNew->sap2UserEntityNamesLen = strlen("");
		}
		if ((StorageNew->sap2providerEntityNames = (uint8_t *) strdup("")) != NULL) {
			StorageNew->sap2providerEntityNamesLen = strlen("");
		}

	}
	DEBUGMSGTL(("sap2Table", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct sap2Table_data *sap2Table_duplicate(struct sap2Table_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicat a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct sap2Table_data *
sap2Table_duplicate(struct sap2Table_data *thedata)
{
	struct sap2Table_data *StorageNew = SNMP_MALLOC_STRUCT(sap2Table_data);

	DEBUGMSGTL(("sap2Table", "duplicating row...  "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("sap2Table", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	sap2Table_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int sap2Table_destroy(struct sap2Table_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
sap2Table_destroy(struct sap2Table_data **thedata)
{
	struct sap2Table_data *StorageDel;

	DEBUGMSGTL(("sap2Table", "deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->subsystemId);
		StorageDel->subsystemIdLen = 0;
		SNMP_FREE(StorageDel->sap2Id);
		StorageDel->sap2IdLen = 0;
		SNMP_FREE(StorageDel->sap2UserEntityNames);
		StorageDel->sap2UserEntityNamesLen = 0;
		SNMP_FREE(StorageDel->sap2providerEntityNames);
		StorageDel->sap2providerEntityNamesLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("sap2Table", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int sap2Table_add(struct sap2Table_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the sap2Table table data set.
 *
 * Adds a table row structure to the sap2Table table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
sap2Table_add(struct sap2Table_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("sap2Table", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index the data */
	/* subsystemId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->subsystemId, thedata->subsystemIdLen);
	/* sap2Id */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->sap2Id, thedata->sap2IdLen);
	header_complex_add_data(&sap2TableStorage, vars, thedata);
	DEBUGMSGTL(("sap2Table", "registered an entry\n"));
	DEBUGMSGTL(("sap2Table", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int sap2Table_del(struct sap2Table_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the sap2Table table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
sap2Table_del(struct sap2Table_data *thedata)
{
	struct sap2Table_data *StorageDel;

	DEBUGMSGTL(("sap2Table", "deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(sap2TableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&sap2TableStorage, hciptr);
	}
	DEBUGMSGTL(("sap2Table", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_sap2Table(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for sap2Table entries.
 *
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case sap2Table).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_sap2Table(const char *token, char *line)
{
	size_t tmpsize;
	struct sap2Table_data *StorageTmp = sap2Table_create();

	DEBUGMSGTL(("sap2Table", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->subsystemId, &StorageTmp->subsystemIdLen);
	if (StorageTmp->subsystemId == NULL) {
		config_perror("invalid specification for subsystemId");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->sap2Id, &StorageTmp->sap2IdLen);
	if (StorageTmp->sap2Id == NULL) {
		config_perror("invalid specification for sap2Id");
		return;
	}
	line = read_config_read_data(ASN_OPAQUE, line, &StorageTmp->sap2Address, &StorageTmp->sap2AddressLen);
	if (StorageTmp->sap2Address == NULL) {
		config_perror("invalid specification for sap2Address");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->sap2UserEntityNames, &StorageTmp->sap2UserEntityNamesLen);
	if (StorageTmp->sap2UserEntityNames == NULL) {
		config_perror("invalid specification for sap2UserEntityNames");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->sap2providerEntityNames, &StorageTmp->sap2providerEntityNamesLen);
	if (StorageTmp->sap2providerEntityNames == NULL) {
		config_perror("invalid specification for sap2providerEntityNames");
		return;
	}
	sap2Table_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("sap2Table", "done.\n"));
}

/*
 * store_sap2Table(): store configuraiton file for sap2Table
 * stores .conf file entries needed to configure the mib.
 */
int
store_sap2Table(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct sap2Table_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("sap2Table", "storing data...  "));
	refresh_sap2Table();
	(void) tmpsize;
	for (hcindex = sap2TableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct sap2Table_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "sap2Table ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->subsystemId, &StorageTmp->subsystemIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->sap2Id, &StorageTmp->sap2IdLen);
			cptr = read_config_store_data(ASN_OPAQUE, cptr, &StorageTmp->sap2Address, &StorageTmp->sap2AddressLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->sap2UserEntityNames, &StorageTmp->sap2UserEntityNamesLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->sap2providerEntityNames, &StorageTmp->sap2providerEntityNamesLen);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("sap2Table", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct singlePeerConnectionTable_data *singlePeerConnectionTable_create(void)
 * @brief create a fresh data structure representing a new row in the singlePeerConnectionTable table.
 *
 * Creates a new singlePeerConnectionTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct singlePeerConnectionTable_data *
singlePeerConnectionTable_create(void)
{
	struct singlePeerConnectionTable_data *StorageNew = SNMP_MALLOC_STRUCT(singlePeerConnectionTable_data);

	DEBUGMSGTL(("singlePeerConnectionTable", "creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->subsystemId = (uint8_t *) strdup("")) != NULL) {
			StorageNew->subsystemIdLen = strlen("");
		}
		if ((StorageNew->communicationsEntityId = (uint8_t *) strdup("")) != NULL) {
			StorageNew->communicationsEntityIdLen = strlen("");
		}
		if ((StorageNew->coProtocolMachineId = (uint8_t *) strdup("")) != NULL) {
			StorageNew->coProtocolMachineIdLen = strlen("");
		}
		if ((StorageNew->underlyingConnectionNames = (uint8_t *) strdup("")) != NULL) {
			StorageNew->underlyingConnectionNamesLen = strlen("");
		}
		if ((StorageNew->supportedConnectionNames = (uint8_t *) strdup("")) != NULL) {
			StorageNew->supportedConnectionNamesLen = strlen("");
		}

	}
	DEBUGMSGTL(("singlePeerConnectionTable", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct singlePeerConnectionTable_data *singlePeerConnectionTable_duplicate(struct singlePeerConnectionTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicat a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct singlePeerConnectionTable_data *
singlePeerConnectionTable_duplicate(struct singlePeerConnectionTable_data *thedata)
{
	struct singlePeerConnectionTable_data *StorageNew = SNMP_MALLOC_STRUCT(singlePeerConnectionTable_data);

	DEBUGMSGTL(("singlePeerConnectionTable", "duplicating row...  "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("singlePeerConnectionTable", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	singlePeerConnectionTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int singlePeerConnectionTable_destroy(struct singlePeerConnectionTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
singlePeerConnectionTable_destroy(struct singlePeerConnectionTable_data **thedata)
{
	struct singlePeerConnectionTable_data *StorageDel;

	DEBUGMSGTL(("singlePeerConnectionTable", "deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->subsystemId);
		StorageDel->subsystemIdLen = 0;
		SNMP_FREE(StorageDel->communicationsEntityId);
		StorageDel->communicationsEntityIdLen = 0;
		SNMP_FREE(StorageDel->coProtocolMachineId);
		StorageDel->coProtocolMachineIdLen = 0;
		SNMP_FREE(StorageDel->connectionId);
		StorageDel->connectionIdLen = 0;
		SNMP_FREE(StorageDel->underlyingConnectionNames);
		StorageDel->underlyingConnectionNamesLen = 0;
		SNMP_FREE(StorageDel->supportedConnectionNames);
		StorageDel->supportedConnectionNamesLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("singlePeerConnectionTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int singlePeerConnectionTable_add(struct singlePeerConnectionTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the singlePeerConnectionTable table data set.
 *
 * Adds a table row structure to the singlePeerConnectionTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
singlePeerConnectionTable_add(struct singlePeerConnectionTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("singlePeerConnectionTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index the data */
	/* subsystemId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->subsystemId, thedata->subsystemIdLen);
	/* communicationsEntityId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->communicationsEntityId, thedata->communicationsEntityIdLen);
	/* coProtocolMachineId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->coProtocolMachineId, thedata->coProtocolMachineIdLen);
	/* connectionId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->connectionId, thedata->connectionIdLen);
	header_complex_add_data(&singlePeerConnectionTableStorage, vars, thedata);
	DEBUGMSGTL(("singlePeerConnectionTable", "registered an entry\n"));
	DEBUGMSGTL(("singlePeerConnectionTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int singlePeerConnectionTable_del(struct singlePeerConnectionTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the singlePeerConnectionTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
singlePeerConnectionTable_del(struct singlePeerConnectionTable_data *thedata)
{
	struct singlePeerConnectionTable_data *StorageDel;

	DEBUGMSGTL(("singlePeerConnectionTable", "deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(singlePeerConnectionTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&singlePeerConnectionTableStorage, hciptr);
	}
	DEBUGMSGTL(("singlePeerConnectionTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_singlePeerConnectionTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for singlePeerConnectionTable entries.
 *
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case singlePeerConnectionTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_singlePeerConnectionTable(const char *token, char *line)
{
	size_t tmpsize;
	struct singlePeerConnectionTable_data *StorageTmp = singlePeerConnectionTable_create();

	DEBUGMSGTL(("singlePeerConnectionTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->subsystemId, &StorageTmp->subsystemIdLen);
	if (StorageTmp->subsystemId == NULL) {
		config_perror("invalid specification for subsystemId");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->communicationsEntityId, &StorageTmp->communicationsEntityIdLen);
	if (StorageTmp->communicationsEntityId == NULL) {
		config_perror("invalid specification for communicationsEntityId");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->coProtocolMachineId, &StorageTmp->coProtocolMachineIdLen);
	if (StorageTmp->coProtocolMachineId == NULL) {
		config_perror("invalid specification for coProtocolMachineId");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->connectionId, &StorageTmp->connectionIdLen);
	if (StorageTmp->connectionId == NULL) {
		config_perror("invalid specification for connectionId");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->underlyingConnectionNames, &StorageTmp->underlyingConnectionNamesLen);
	if (StorageTmp->underlyingConnectionNames == NULL) {
		config_perror("invalid specification for underlyingConnectionNames");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->supportedConnectionNames, &StorageTmp->supportedConnectionNamesLen);
	if (StorageTmp->supportedConnectionNames == NULL) {
		config_perror("invalid specification for supportedConnectionNames");
		return;
	}
	singlePeerConnectionTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("singlePeerConnectionTable", "done.\n"));
}

/*
 * store_singlePeerConnectionTable(): store configuraiton file for singlePeerConnectionTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_singlePeerConnectionTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct singlePeerConnectionTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("singlePeerConnectionTable", "storing data...  "));
	refresh_singlePeerConnectionTable();
	(void) tmpsize;
	for (hcindex = singlePeerConnectionTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct singlePeerConnectionTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "singlePeerConnectionTable ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->subsystemId, &StorageTmp->subsystemIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->communicationsEntityId, &StorageTmp->communicationsEntityIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->coProtocolMachineId, &StorageTmp->coProtocolMachineIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->connectionId, &StorageTmp->connectionIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->underlyingConnectionNames, &StorageTmp->underlyingConnectionNamesLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->supportedConnectionNames, &StorageTmp->supportedConnectionNamesLen);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("singlePeerConnectionTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct subsystemTable_data *subsystemTable_create(void)
 * @brief create a fresh data structure representing a new row in the subsystemTable table.
 *
 * Creates a new subsystemTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct subsystemTable_data *
subsystemTable_create(void)
{
	struct subsystemTable_data *StorageNew = SNMP_MALLOC_STRUCT(subsystemTable_data);

	DEBUGMSGTL(("subsystemTable", "creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */

	}
	DEBUGMSGTL(("subsystemTable", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct subsystemTable_data *subsystemTable_duplicate(struct subsystemTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicat a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct subsystemTable_data *
subsystemTable_duplicate(struct subsystemTable_data *thedata)
{
	struct subsystemTable_data *StorageNew = SNMP_MALLOC_STRUCT(subsystemTable_data);

	DEBUGMSGTL(("subsystemTable", "duplicating row...  "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("subsystemTable", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	subsystemTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int subsystemTable_destroy(struct subsystemTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
subsystemTable_destroy(struct subsystemTable_data **thedata)
{
	struct subsystemTable_data *StorageDel;

	DEBUGMSGTL(("subsystemTable", "deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->subsystemId);
		StorageDel->subsystemIdLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("subsystemTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int subsystemTable_add(struct subsystemTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the subsystemTable table data set.
 *
 * Adds a table row structure to the subsystemTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
subsystemTable_add(struct subsystemTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("subsystemTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index the data */
	/* subsystemId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->subsystemId, thedata->subsystemIdLen);
	header_complex_add_data(&subsystemTableStorage, vars, thedata);
	DEBUGMSGTL(("subsystemTable", "registered an entry\n"));
	DEBUGMSGTL(("subsystemTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int subsystemTable_del(struct subsystemTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the subsystemTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
subsystemTable_del(struct subsystemTable_data *thedata)
{
	struct subsystemTable_data *StorageDel;

	DEBUGMSGTL(("subsystemTable", "deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(subsystemTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&subsystemTableStorage, hciptr);
	}
	DEBUGMSGTL(("subsystemTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_subsystemTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for subsystemTable entries.
 *
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case subsystemTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_subsystemTable(const char *token, char *line)
{
	size_t tmpsize;
	struct subsystemTable_data *StorageTmp = subsystemTable_create();

	DEBUGMSGTL(("subsystemTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->subsystemId, &StorageTmp->subsystemIdLen);
	if (StorageTmp->subsystemId == NULL) {
		config_perror("invalid specification for subsystemId");
		return;
	}
	subsystemTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("subsystemTable", "done.\n"));
}

/*
 * store_subsystemTable(): store configuraiton file for subsystemTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_subsystemTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct subsystemTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("subsystemTable", "storing data...  "));
	refresh_subsystemTable();
	(void) tmpsize;
	for (hcindex = subsystemTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct subsystemTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "subsystemTable ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->subsystemId, &StorageTmp->subsystemIdLen);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("subsystemTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void refresh_applicationProcessTable(void)
 * @brief refresh the scalar values of the applicationProcessTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_applicationProcessTable(void)
{
	if (applicationProcessTable_refresh == 0)
		return;
	applicationProcessTable_refresh = 0;
	/* XXX: Here, update the table as required... */
}

/**
 * @fn void refresh_applicationProcessTable_row(struct applicationProcessTable_data *StorageTmp)
 * @brief refresh the contents of the applicationProcessTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
void
refresh_applicationProcessTable_row(struct applicationProcessTable_data *StorageTmp)
{
	if (!StorageTmp || StorageTmp->applicationProcessTable_request == sa_request)
		return;
	StorageTmp->applicationProcessTable_request = sa_request;
}

/**
 * @fn u_char *var_applicationProcessTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in applicationProcessTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_gmiMIB above.
 */
u_char *
var_applicationProcessTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct applicationProcessTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("gmiMIB", "var_applicationProcessTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_applicationProcessTable();
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	StorageTmp = header_complex(applicationProcessTableStorage, vp, name, length, exact, var_len, write_method);
	refresh_applicationProcessTable_row(StorageTmp);
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) APPLICATIONPROCESSTITLE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->applicationProcessTitleLen * sizeof(oid);
		rval = (u_char *) StorageTmp->applicationProcessTitle;
		break;
	case (u_char) SUPPORTENTITYNAMES:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->supportEntityNamesLen;
		rval = (u_char *) StorageTmp->supportEntityNames;
		break;
	case (u_char) APPLICATIONPROCESSOPERATIONALSTATE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->applicationProcessOperationalState);
		rval = (u_char *) &StorageTmp->applicationProcessOperationalState;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn void refresh_communicationsEntityTable(void)
 * @brief refresh the scalar values of the communicationsEntityTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_communicationsEntityTable(void)
{
	if (communicationsEntityTable_refresh == 0)
		return;
	communicationsEntityTable_refresh = 0;
	/* XXX: Here, update the table as required... */
}

/**
 * @fn void refresh_communicationsEntityTable_row(struct communicationsEntityTable_data *StorageTmp)
 * @brief refresh the contents of the communicationsEntityTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
void
refresh_communicationsEntityTable_row(struct communicationsEntityTable_data *StorageTmp)
{
	if (!StorageTmp || StorageTmp->communicationsEntityTable_request == sa_request)
		return;
	StorageTmp->communicationsEntityTable_request = sa_request;
}

/**
 * @fn u_char *var_communicationsEntityTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in communicationsEntityTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_gmiMIB above.
 */
u_char *
var_communicationsEntityTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct communicationsEntityTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("gmiMIB", "var_communicationsEntityTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_communicationsEntityTable();
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	StorageTmp = header_complex(communicationsEntityTableStorage, vp, name, length, exact, var_len, write_method);
	refresh_communicationsEntityTable_row(StorageTmp);
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) LOCALSAPNAMES:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->localSapNamesLen;
		rval = (u_char *) StorageTmp->localSapNames;
		break;
	case (u_char) COMMUNICATIONSENTITYOPERATIONALSTATE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->communicationsEntityOperationalState);
		rval = (u_char *) &StorageTmp->communicationsEntityOperationalState;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn void refresh_communicationsInformationRecordTable(void)
 * @brief refresh the scalar values of the communicationsInformationRecordTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_communicationsInformationRecordTable(void)
{
	if (communicationsInformationRecordTable_refresh == 0)
		return;
	communicationsInformationRecordTable_refresh = 0;
	/* XXX: Here, update the table as required... */
}

/**
 * @fn void refresh_communicationsInformationRecordTable_row(struct communicationsInformationRecordTable_data *StorageTmp)
 * @brief refresh the contents of the communicationsInformationRecordTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
void
refresh_communicationsInformationRecordTable_row(struct communicationsInformationRecordTable_data *StorageTmp)
{
	if (!StorageTmp || StorageTmp->communicationsInformationRecordTable_request == sa_request)
		return;
	StorageTmp->communicationsInformationRecordTable_request = sa_request;
}

/**
 * @fn u_char *var_communicationsInformationRecordTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in communicationsInformationRecordTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_gmiMIB above.
 */
u_char *
var_communicationsInformationRecordTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct communicationsInformationRecordTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("gmiMIB", "var_communicationsInformationRecordTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_communicationsInformationRecordTable();
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	StorageTmp = header_complex(communicationsInformationRecordTableStorage, vp, name, length, exact, var_len, write_method);
	refresh_communicationsInformationRecordTable_row(StorageTmp);
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) INFORMATIONTYPE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->informationTypeLen * sizeof(oid);
		rval = (u_char *) StorageTmp->informationType;
		break;
	case (u_char) INFORMATIONDATA:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->informationDataLen;
		rval = (u_char *) StorageTmp->informationData;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn void refresh_clProtocolMachineTable(void)
 * @brief refresh the scalar values of the clProtocolMachineTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_clProtocolMachineTable(void)
{
	if (clProtocolMachineTable_refresh == 0)
		return;
	clProtocolMachineTable_refresh = 0;
	/* XXX: Here, update the table as required... */
}

/**
 * @fn void refresh_clProtocolMachineTable_row(struct clProtocolMachineTable_data *StorageTmp)
 * @brief refresh the contents of the clProtocolMachineTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
void
refresh_clProtocolMachineTable_row(struct clProtocolMachineTable_data *StorageTmp)
{
	if (!StorageTmp || StorageTmp->clProtocolMachineTable_request == sa_request)
		return;
	StorageTmp->clProtocolMachineTable_request = sa_request;
}

/**
 * @fn u_char *var_clProtocolMachineTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in clProtocolMachineTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_gmiMIB above.
 */
u_char *
var_clProtocolMachineTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct clProtocolMachineTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("gmiMIB", "var_clProtocolMachineTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_clProtocolMachineTable();
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	StorageTmp = header_complex(clProtocolMachineTableStorage, vp, name, length, exact, var_len, write_method);
	refresh_clProtocolMachineTable_row(StorageTmp);
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) CLPROTOCOLMACHINEOPERATIONALSTATE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->clProtocolMachineOperationalState);
		rval = (u_char *) &StorageTmp->clProtocolMachineOperationalState;
		break;
	case (u_char) TOTALREMOTESAPS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->totalRemoteSAPs);
		rval = (u_char *) &StorageTmp->totalRemoteSAPs;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn void refresh_coProtocolMachineTable(void)
 * @brief refresh the scalar values of the coProtocolMachineTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_coProtocolMachineTable(void)
{
	if (coProtocolMachineTable_refresh == 0)
		return;
	coProtocolMachineTable_refresh = 0;
	/* XXX: Here, update the table as required... */
}

/**
 * @fn void refresh_coProtocolMachineTable_row(struct coProtocolMachineTable_data *StorageTmp)
 * @brief refresh the contents of the coProtocolMachineTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
void
refresh_coProtocolMachineTable_row(struct coProtocolMachineTable_data *StorageTmp)
{
	if (!StorageTmp || StorageTmp->coProtocolMachineTable_request == sa_request)
		return;
	StorageTmp->coProtocolMachineTable_request = sa_request;
}

/**
 * @fn u_char *var_coProtocolMachineTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in coProtocolMachineTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_gmiMIB above.
 */
u_char *
var_coProtocolMachineTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct coProtocolMachineTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("gmiMIB", "var_coProtocolMachineTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_coProtocolMachineTable();
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	StorageTmp = header_complex(coProtocolMachineTableStorage, vp, name, length, exact, var_len, write_method);
	refresh_coProtocolMachineTable_row(StorageTmp);
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) COPROTOCOLMACHINEOPERATIONALSTATE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->coProtocolMachineOperationalState);
		rval = (u_char *) &StorageTmp->coProtocolMachineOperationalState;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn void refresh_sap1Table(void)
 * @brief refresh the scalar values of the sap1Table.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_sap1Table(void)
{
	if (sap1Table_refresh == 0)
		return;
	sap1Table_refresh = 0;
	/* XXX: Here, update the table as required... */
}

/**
 * @fn void refresh_sap1Table_row(struct sap1Table_data *StorageTmp)
 * @brief refresh the contents of the sap1Table row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
void
refresh_sap1Table_row(struct sap1Table_data *StorageTmp)
{
	if (!StorageTmp || StorageTmp->sap1Table_request == sa_request)
		return;
	StorageTmp->sap1Table_request = sa_request;
}

/**
 * @fn u_char *var_sap1Table(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in sap1Table.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_gmiMIB above.
 */
u_char *
var_sap1Table(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct sap1Table_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("gmiMIB", "var_sap1Table: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_sap1Table();
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	StorageTmp = header_complex(sap1TableStorage, vp, name, length, exact, var_len, write_method);
	refresh_sap1Table_row(StorageTmp);
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) SAP1ADDRESS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->sap1Address);
		rval = (u_char *) &StorageTmp->sap1Address;
		break;
	case (u_char) SAP1USERENTITYNAMES:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->sap1UserEntityNamesLen;
		rval = (u_char *) StorageTmp->sap1UserEntityNames;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn void refresh_sap2Table(void)
 * @brief refresh the scalar values of the sap2Table.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_sap2Table(void)
{
	if (sap2Table_refresh == 0)
		return;
	sap2Table_refresh = 0;
	/* XXX: Here, update the table as required... */
}

/**
 * @fn void refresh_sap2Table_row(struct sap2Table_data *StorageTmp)
 * @brief refresh the contents of the sap2Table row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
void
refresh_sap2Table_row(struct sap2Table_data *StorageTmp)
{
	if (!StorageTmp || StorageTmp->sap2Table_request == sa_request)
		return;
	StorageTmp->sap2Table_request = sa_request;
}

/**
 * @fn u_char *var_sap2Table(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in sap2Table.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_gmiMIB above.
 */
u_char *
var_sap2Table(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct sap2Table_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("gmiMIB", "var_sap2Table: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_sap2Table();
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	StorageTmp = header_complex(sap2TableStorage, vp, name, length, exact, var_len, write_method);
	refresh_sap2Table_row(StorageTmp);
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) SAP2ADDRESS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->sap2AddressLen;
		rval = (u_char *) StorageTmp->sap2Address;
		break;
	case (u_char) SAP2USERENTITYNAMES:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->sap2UserEntityNamesLen;
		rval = (u_char *) StorageTmp->sap2UserEntityNames;
		break;
	case (u_char) SAP2PROVIDERENTITYNAMES:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->sap2providerEntityNamesLen;
		rval = (u_char *) StorageTmp->sap2providerEntityNames;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn void refresh_singlePeerConnectionTable(void)
 * @brief refresh the scalar values of the singlePeerConnectionTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_singlePeerConnectionTable(void)
{
	if (singlePeerConnectionTable_refresh == 0)
		return;
	singlePeerConnectionTable_refresh = 0;
	/* XXX: Here, update the table as required... */
}

/**
 * @fn void refresh_singlePeerConnectionTable_row(struct singlePeerConnectionTable_data *StorageTmp)
 * @brief refresh the contents of the singlePeerConnectionTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
void
refresh_singlePeerConnectionTable_row(struct singlePeerConnectionTable_data *StorageTmp)
{
	if (!StorageTmp || StorageTmp->singlePeerConnectionTable_request == sa_request)
		return;
	StorageTmp->singlePeerConnectionTable_request = sa_request;
}

/**
 * @fn u_char *var_singlePeerConnectionTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in singlePeerConnectionTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_gmiMIB above.
 */
u_char *
var_singlePeerConnectionTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct singlePeerConnectionTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("gmiMIB", "var_singlePeerConnectionTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_singlePeerConnectionTable();
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	StorageTmp = header_complex(singlePeerConnectionTableStorage, vp, name, length, exact, var_len, write_method);
	refresh_singlePeerConnectionTable_row(StorageTmp);
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) UNDERLYINGCONNECTIONNAMES:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->underlyingConnectionNamesLen;
		rval = (u_char *) StorageTmp->underlyingConnectionNames;
		break;
	case (u_char) SUPPORTEDCONNECTIONNAMES:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->supportedConnectionNamesLen;
		rval = (u_char *) StorageTmp->supportedConnectionNames;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn void refresh_subsystemTable(void)
 * @brief refresh the scalar values of the subsystemTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_subsystemTable(void)
{
	if (subsystemTable_refresh == 0)
		return;
	subsystemTable_refresh = 0;
	/* XXX: Here, update the table as required... */
}

/**
 * @fn void refresh_subsystemTable_row(struct subsystemTable_data *StorageTmp)
 * @brief refresh the contents of the subsystemTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
void
refresh_subsystemTable_row(struct subsystemTable_data *StorageTmp)
{
	if (!StorageTmp || StorageTmp->subsystemTable_request == sa_request)
		return;
	StorageTmp->subsystemTable_request = sa_request;
}

/**
 * @fn u_char *var_subsystemTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in subsystemTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_gmiMIB above.
 */
u_char *
var_subsystemTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct subsystemTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("gmiMIB", "var_subsystemTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_subsystemTable();
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	StorageTmp = header_complex(subsystemTableStorage, vp, name, length, exact, var_len, write_method);
	refresh_subsystemTable_row(StorageTmp);
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) SUBSYSTEMID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->subsystemIdLen;
		rval = (u_char *) StorageTmp->subsystemId;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int applicationProcessTable_consistent(struct applicationProcessTable_data *thedata)
 * @param thedata the row data to check for consistency.
 * @brief check the internal consistency of a table row.
 *
 * This function checks the internal consistency of a table row for the applicationProcessTable table.  If the
 * table row is internally consistent, then this function returns true (1), otherwise the function
 * returns false (0) and it will not be possible to activate the row until the row's internal
 * consistency is corrected.
 */
int
applicationProcessTable_consistent(struct applicationProcessTable_data *thedata)
{
	/* XXX: check row consistency return true(1) if consistent, or false(0) if not. */
	return (1);
}

/**
 * @fn int communicationsEntityTable_consistent(struct communicationsEntityTable_data *thedata)
 * @param thedata the row data to check for consistency.
 * @brief check the internal consistency of a table row.
 *
 * This function checks the internal consistency of a table row for the communicationsEntityTable table.  If the
 * table row is internally consistent, then this function returns true (1), otherwise the function
 * returns false (0) and it will not be possible to activate the row until the row's internal
 * consistency is corrected.
 */
int
communicationsEntityTable_consistent(struct communicationsEntityTable_data *thedata)
{
	/* XXX: check row consistency return true(1) if consistent, or false(0) if not. */
	return (1);
}

/**
 * @fn int communicationsInformationRecordTable_consistent(struct communicationsInformationRecordTable_data *thedata)
 * @param thedata the row data to check for consistency.
 * @brief check the internal consistency of a table row.
 *
 * This function checks the internal consistency of a table row for the communicationsInformationRecordTable table.  If the
 * table row is internally consistent, then this function returns true (1), otherwise the function
 * returns false (0) and it will not be possible to activate the row until the row's internal
 * consistency is corrected.
 */
int
communicationsInformationRecordTable_consistent(struct communicationsInformationRecordTable_data *thedata)
{
	/* XXX: check row consistency return true(1) if consistent, or false(0) if not. */
	return (1);
}

/**
 * @fn int clProtocolMachineTable_consistent(struct clProtocolMachineTable_data *thedata)
 * @param thedata the row data to check for consistency.
 * @brief check the internal consistency of a table row.
 *
 * This function checks the internal consistency of a table row for the clProtocolMachineTable table.  If the
 * table row is internally consistent, then this function returns true (1), otherwise the function
 * returns false (0) and it will not be possible to activate the row until the row's internal
 * consistency is corrected.
 */
int
clProtocolMachineTable_consistent(struct clProtocolMachineTable_data *thedata)
{
	/* XXX: check row consistency return true(1) if consistent, or false(0) if not. */
	return (1);
}

/**
 * @fn int coProtocolMachineTable_consistent(struct coProtocolMachineTable_data *thedata)
 * @param thedata the row data to check for consistency.
 * @brief check the internal consistency of a table row.
 *
 * This function checks the internal consistency of a table row for the coProtocolMachineTable table.  If the
 * table row is internally consistent, then this function returns true (1), otherwise the function
 * returns false (0) and it will not be possible to activate the row until the row's internal
 * consistency is corrected.
 */
int
coProtocolMachineTable_consistent(struct coProtocolMachineTable_data *thedata)
{
	/* XXX: check row consistency return true(1) if consistent, or false(0) if not. */
	return (1);
}

/**
 * @fn int sap1Table_consistent(struct sap1Table_data *thedata)
 * @param thedata the row data to check for consistency.
 * @brief check the internal consistency of a table row.
 *
 * This function checks the internal consistency of a table row for the sap1Table table.  If the
 * table row is internally consistent, then this function returns true (1), otherwise the function
 * returns false (0) and it will not be possible to activate the row until the row's internal
 * consistency is corrected.
 */
int
sap1Table_consistent(struct sap1Table_data *thedata)
{
	/* XXX: check row consistency return true(1) if consistent, or false(0) if not. */
	return (1);
}

/**
 * @fn int sap2Table_consistent(struct sap2Table_data *thedata)
 * @param thedata the row data to check for consistency.
 * @brief check the internal consistency of a table row.
 *
 * This function checks the internal consistency of a table row for the sap2Table table.  If the
 * table row is internally consistent, then this function returns true (1), otherwise the function
 * returns false (0) and it will not be possible to activate the row until the row's internal
 * consistency is corrected.
 */
int
sap2Table_consistent(struct sap2Table_data *thedata)
{
	/* XXX: check row consistency return true(1) if consistent, or false(0) if not. */
	return (1);
}

/**
 * @fn int singlePeerConnectionTable_consistent(struct singlePeerConnectionTable_data *thedata)
 * @param thedata the row data to check for consistency.
 * @brief check the internal consistency of a table row.
 *
 * This function checks the internal consistency of a table row for the singlePeerConnectionTable table.  If the
 * table row is internally consistent, then this function returns true (1), otherwise the function
 * returns false (0) and it will not be possible to activate the row until the row's internal
 * consistency is corrected.
 */
int
singlePeerConnectionTable_consistent(struct singlePeerConnectionTable_data *thedata)
{
	/* XXX: check row consistency return true(1) if consistent, or false(0) if not. */
	return (1);
}

/**
 * @fn int subsystemTable_consistent(struct subsystemTable_data *thedata)
 * @param thedata the row data to check for consistency.
 * @brief check the internal consistency of a table row.
 *
 * This function checks the internal consistency of a table row for the subsystemTable table.  If the
 * table row is internally consistent, then this function returns true (1), otherwise the function
 * returns false (0) and it will not be possible to activate the row until the row's internal
 * consistency is corrected.
 */
int
subsystemTable_consistent(struct subsystemTable_data *thedata)
{
	/* XXX: check row consistency return true(1) if consistent, or false(0) if not. */
	return (1);
}

void
send_communicationsInformation_v2trap(struct variable_list *vars)
{
	struct variable_list trap;

	trap.next_variable = vars;
	trap.name = snmpTrapOID_oid;
	trap.name_length = sizeof(snmpTrapOID_oid) / sizeof(oid);
	trap.type = ASN_OBJECT_ID;
	trap.val.objid = communicationsInformation_oid;
	trap.val_len = sizeof(communicationsInformation_oid);
	trap.index = 0;
	send_v2trap(&trap);
}

#if defined MODULE
#if defined MASTER
/**
 * @fn void gmiMIB_loop_handler(int dummy)
 * @param dummy signal number (always zero (0))
 * @brief handle event loop interation.
 *
 * This function is registered so that, when operating as a module, snmpd will call it one per event
 * loop interation.  This function is called before the next requst is processed and after the
 * previous request is processed.  Two things are done here:  1) The file descriptor that is used to
 * synchronize the agent with (pseudo-)device drivers is closed.  (Another approach, instead of
 * closing each time, would be to restart a timer each time that a request is made (loop is
 * performed) and if it expires, close the file descriptor).  2) The request number is incremented.
 * Although a request is not generated for each loop of the snmp event loop, it is true that a new
 * request cannot be generated without performing a loop.  Therefore, the sa_request is not the
 * request number but it is a temporally unique identifier for a request.
 */
void
gmiMIB_loop_handler(int dummy)
{
	if (external_signal_scheduled[dummy] == 0)
		external_signal_scheduled[dummy]--;
	/* close files after each request */
	if (sa_fclose && sa_fd != 0) {
		close(sa_fd);
		sa_fd = 0;
	}
	/* prepare for next request */
	sa_request++;
}
#endif				/* defined MASTER */
/**
 * @fn void gmiMIB_fd_handler(int fd, void *dummy)
 * @param fd file descriptor to read.
 * @param dummy client data passed to registration function (always NULL).
 * @brief handle read event on file descriptor.
 *
 * This read file descriptor handler is normally used for (pseudo-)device drivers that generate
 * statistical collection interval events, alarm events, or other operational measurement events, by
 * placing a message on the read queue of the "event handling" Stream.  Normally this routine
 * would adjust counts in some table or scalars, generate SNMP traps representing on-occurence
 * events, first and interval events, and alarm indications.
 */
void
gmiMIB_fd_handler(int fd, void *dummy)
{
	/* XXX: place actions to handle sa_readfd here... */
	return;
}
#endif				/* defined MOUDLE */
#if defined MASTER
const char sa_program[] = "gmimib";
int sa_fclose = 1;			/* default close files between requests */
int sa_fd = 0;				/* file descriptor for MIB use */
int sa_readfd = 0;			/* file descriptor for autonomnous events */
int sa_changed = 1;			/* indication to reread MIB configuration */
int sa_stats_refresh = 1;		/* indications that statistics, the mib or its tables need to be refreshed */
int sa_request = 1;			/* request number for per-request actions */
#endif				/* defined MASTER */
#if defined MASTER
#if !defined MODULE
int sa_dump = 0;			/* default packet dump */
int sa_debug = 0;			/* default no debug */
int sa_nomead = 1;			/* default daemon mode */
int sa_output = 1;			/* default normal output */
int sa_agentx = 1;			/* default agentx mode */
int sa_alarms = 1;			/* default application alarms */
int sa_logaddr = 0;			/* log addresses */
int sa_logfillog = 0;			/* log to sa_logfile */
int sa_logstderr = 0;			/* log to standard error */
int sa_logstdout = 0;			/* log to standard output */
int sa_logsyslog = 0;			/* log to system logs */
int sa_logcallog = 0;			/* log to callback logs */
int sa_appendlog = 0;			/* append to log file without truncating */
char sa_logfile[256] = "/var/log/gmimib.log";
char sa_pidfile[256] = "/var/run/gmimib.pid";
char sa_sysctlf[256] = "/etc/gmimib.conf";
int allow_severity = LOG_ERR;
int deny_severity = LOG_ERR;

/* file stream for log file */
FILE *stdlog = NULL;
static void
sa_version(int argc, char *argv[])
{
	if (!sa_output && !sa_debug)
		return;
	fprintf(stdout, "\
%2$s\n\
Copyright (c) 2008-2009  Monavacon Limited.  All Rights Reserved.\n\
Distributed under Affero GPL Version 3, included here by reference.\n\
See `%1$s --copying' for copying permissions.\n\
", argv[0], ident);
}
static void
sa_usage(int argc, char *argv[])
{
	if (!sa_output && !sa_debug)
		return;
	fprintf(stderr, "\
Usage:\n\
    %1$s [general-options] [options] [arguments]\n\
    %1$s {-H|--help-directives}\n\
    %1$s {-h|--help}\n\
    %1$s {-V|--version}\n\
    %1$s {-C|--copying}\n\
", argv[0]);
}
static void
sa_help(int argc, char *argv[])
{
	if (!sa_output && !sa_debug)
		return;
	fprintf(stdout, "\
Usage:\n\
    %1$s [general-options] [options] [arguments]\n\
    %1$s {-h|--help}\n\
    %1$s {-V|--version}\n\
    %1$s {-C|--copying}\n\
Arguments:\n\
    None.\n\
Options:\n\
    -a, --log-addresses\n\
        log addresses of connecting management stations.\n\
    -A, --append\n\
        append to logfiles without truncating.\n\
    -c, --config-file CONFIGFILE\n\
        use configuration file CONFIGFILE.\n\
    -C, --config-only\n\
        only load configuration given by -c option.\n\
    -d, --dump\n\
        dump sent and received PDUs.\n\
    -D, --debug [LEVEL]\n\
        set debugging verbosity to LEVEL.\n\
    -D, --debug-tokens [TOKEN[,TOKEN]*]\n\
        debug specified TOKEN's.\n\
    -f, --dont-fork\n\
        run in the foreground.\n\
    -g, --gid, --groupid GID\n\
        become group GID after listening.\n\
    -h, --help, -?, --?\n\
        print usage information and exit.\n\
    -H, --help-directives\n\
        print config directives and exit.\n\
    -I, --initialize [-]MODULE[,MODULE]*\n\
        initialize (or not, '-') these MODULE's.\n\
    -k, --keep-open\n\
        keep system files open between requests.\n\
    -l, --log-file [LOGFILE]\n\
        log to log file name LOGFILE.  [default: /var/log/gmimib.log]\n\
    -L, --log-stderr\n\
        log to controlling terminal standard error.\n\
    -m, --mibs [+]MIB[,MIB]*\n\
        load these (additional '+') MIBs.\n\
    -M, --master\n\
        run as SNMP master instead of AgentX sub-agent.\n\
    -M, --mibdirs [+]MIBDIR[:MIBDIR]*\n\
        search these (additional, '+') colon separated directories for MIBs.\n\
    -n, --nodaemon\n\
        run in the foreground.\n\
    -n, --name NAME\n\
        use NAME for configuration file base.  [default: gmimib]\n\
    -p, --port PORTNUM\n\
        listen on port number PORTNUM.  [default: 161]\n\
    -p, --pidfile PIDFILE\n\
        write daemon pid to PIDFILE.  [default: /var/run/gmimib.pid]\n\
    -P, --pidfile PIDFILE\n\
        write daemon pid to PIDFILE.  [default: /var/run/gmimib.pid]\n\
    -q, --quiet\n\
        suppress normal output.\n\
    -q, --quick\n\
        abbreviate output for machine readability.\n\
    -r, --noroot\n\
        do not require root privilege.\n\
    -s, --log-syslog\n\
        log to system logs.\n\
    -S, --sysctl-file FILENAME\n\
        write sysctl config file FILENAME.  [default: /etc/streams.conf]\n\
    -t, --agent-alarms\n\
        agent blocks {SIGALARM}.\n\
    -T, --transport [TRANSPORT]\n\
        default transport TRANSPORT.  [default: udp]\n\
    -u, --uid, --userid UID\n\
        become user UID after listening.\n\
    -U, --dont-remove-pidfile\n\
        do not remove PIDFILE when shutting down.\n\
    -v, --version\n\
        print version information and exit.\n\
    -V, --verbose [LEVEL]\n\
        be verbose to LEVEL.  [default: 1]\n\
    -x, --agentx-socket [SOCKET]\n\
        master AgentX on SOCKET.  [default: /var/agentx/master]\n\
    -X, --agentx\n\
        run as AgentX sub-agent instead of master (the default).\n\
    -y, --copying\n\
        print copying information and exit.\n\
", argv[0]);
}
static void
sa_copying(int argc, char *argv[])
{
	if (!sa_output && !sa_debug)
		return;
	fprintf(stdout, "\
--------------------------------------------------------------------------------\n\
%1$s\n\
--------------------------------------------------------------------------------\n\
Copyright (c) 2008-2009  Monavacon Limited <http://www.monavacon.com>\n\
Copyright (c) 2001-2008  OpenSS7 Corporation <http://www.openss7.com>\n\
Copyright (c) 1997-2000  Brian F. G. Bidulock <bidulock@openss7.org>\n\
\n\
All Rights Reserved.\n\
--------------------------------------------------------------------------------\n\
This program is free software; you can  redistribute  it and/or modify  it under\n\
the terms of the GNU Affero General Public License as published by the Free\n\
Software Foundation; Version 3 of the License.\n\
\n\
This program is distributed in the hope that it will  be useful, but WITHOUT ANY\n\
WARRANTY; without even  the implied warranty of MERCHANTABILITY or FITNESS FOR A\n\
PARTICULAR PURPOSE.  See the GNU Affero General Public License for more details.\n\
\n\
You should have received a copy of the GNU  Affero  General Public License along\n\
with this program.   If not, see <http://www.gnu.org/licenses/>, or write to the\n\
Free Software Foundation, Inc., 675 Mass Ave, Cambridge, MA 02139, USA.\n\
--------------------------------------------------------------------------------\n\
U.S. GOVERNMENT RESTRICTED RIGHTS.  If you are licensing this Software on behalf\n\
of the U.S. Government (\"Government\"), the following provisions apply to you. If\n\
the Software is supplied by the  Department of Defense (\"DoD\"), it is classified\n\
as \"Commercial  Computer  Software\"  under  paragraph  252.227-7014  of the  DoD\n\
Supplement  to the  Federal Acquisition Regulations  (\"DFARS\") (or any successor\n\
regulations) and the  Government  is acquiring  only the  license rights granted\n\
herein (the license rights customarily provided to non-Government users). If the\n\
Software is supplied to any unit or agency of the Government  other than DoD, it\n\
is  classified as  \"Restricted Computer Software\" and the Government's rights in\n\
the Software  are defined  in  paragraph 52.227-19  of the  Federal  Acquisition\n\
Regulations (\"FAR\")  (or any successor regulations) or, in the cases of NASA, in\n\
paragraph  18.52.227-86 of  the  NASA  Supplement  to the FAR (or any  successor\n\
regulations).\n\
--------------------------------------------------------------------------------\n\
Commercial  licensing  and  support of this  software is  available from OpenSS7\n\
Corporation at a fee.  See http://www.openss7.com/\n\
--------------------------------------------------------------------------------\n\
", ident);
}

void
sa_help_directives(int argc, char *argv[])
{
	ds_set_boolean(DS_APPLICATION_ID, DS_AGENT_NO_ROOT_ACCESS, 1);
	init_agent("gmiMIB");
	// init_mib_modules();
	init_mib();
	init_snmp("gmiMIB");
	snmp_log(MY_FACILITY(LOG_INFO), "Configuration directives understood:\n");
	/* Unfortunately, read_config_print_usage() uses snmp_log(), meaning that it can only be writen to standard error and not standard output. */
	read_config_print_usage("    ");
}
static int
sa_sig_register(int signum, RETSIGTYPE(*handler) (int))
{
	sigset_t mask;
	struct sigaction act;

	act.sa_handler = handler ? handler : SIG_DFL;
	act.sa_flags = handler ? SA_RESTART : 0;
	sigemptyset(&act.sa_mask);
	if (sigaction(signum, &act, NULL))
		return (-1);
	sigemptyset(&mask);
	sigaddset(&mask, signum);
	sigprocmask(handler ? SIG_UNBLOCK : SIG_BLOCK, &mask, NULL);
	return (0);
}
static int sa_alm_signal = 0;
static int sa_pol_signal = 0;
static int sa_hup_signal = 0;
static int sa_int_signal = 0;
static int sa_trm_signal = 0;
static int sa_alm_handle = 0;
void
sa_alm_callback(uint req, void *arg)
{
	if (req == sa_alm_handle)
		sa_alm_handle = 0;
	sa_alm_signal = 1;
	return;
}

static RETSIGTYPE
sa_alm_handler(int signum)
{
	sa_alm_signal = 1;
	return (RETSIGTYPE) (0);
}
static void
sa_snmp_alm_handler(uint reg, void *clientarg)
{
	sa_alm_signal = 1;
	return;
}
static int
sa_alm_catch(void)
{
	if (sa_alarms)
		return sa_sig_register(SIGALRM, &sa_alm_handler);
	return (-1);
}
static int
sa_alm_block(void)
{
	if (sa_alarms)
		return sa_sig_register(SIGALRM, NULL);
	if (sa_alm_handle) {
		uint handle = sa_alm_handle;

		sa_alm_handle = 0;
		snmp_alarm_unregister(handle);
	}
	return (0);
}
static int
sa_alm_action(void)
{
	sa_alm_signal = 0;
	return (0);
}

static RETSIGTYPE
sa_pol_handler(int signum)
{
	sa_pol_signal = 1;
	return (RETSIGTYPE) (0);
}
static int
sa_pol_catch(void)
{
	return sa_sig_register(SIGPOLL, &sa_pol_handler);
}
static int
sa_pol_block(void)
{
	return sa_sig_register(SIGPOLL, NULL);
}

/*
 * Both the sc(4) module and sad(4) driver issue an M_PCSIG message with
 * SIGPOLL to the stream head whenever the STREAMS configuration or autopush
 * configuration changes, indicating to the agent which has the sc(4) or
 * sad(4) Stream open that it is necessary to reread information from the
 * kernel.  This fact is merely recorded, as this information is not read each
 * time that a configuration change occurs, but only after a request from some
 * portion of that information occurs. This condition is also set when the
 * sc(4) and sad(4) Streams are first opened. The SIGPOLL will also deliver in
 * siginfo the file descriptor issuing the signal, so we could distiguish
 * between sc(4) and sad(4) signals, but since one can be pushed over the
 * other, there is little point in distinguishing.
 *
 * sc(4) or sad(4) also should be modified to provide the general streams
 * statistics supported here; even though they are available through the /proc
 * filesystem on Linux Fast-STREAMS.
 */
static int
sa_pol_action(void)
{
	sa_pol_signal = 0;
	snmp_log(MY_FACILITY(LOG_INFO), "%s: Caught SIGPOLL, will re-read data structures", sa_program);
	sa_changed = 1;
	return (0);
}

static RETSIGTYPE
sa_hup_handler(int signum)
{
	sa_hup_signal = 1;
	return (RETSIGTYPE) (0);
}
static int
sa_hup_catch(void)
{
	if (sa_agentx)
		return sa_sig_register(SIGHUP, &sa_hup_handler);
	return (-1);
}
static int
sa_hup_block(void)
{
	return sa_sig_register(SIGHUP, NULL);
}
static int
sa_hup_action(void)
{
	/* There are several times that we might be sent a SIGHUP.  We might be sent a SIGHUP by logrotate asking us to close and reopen our log files. */
	sa_hup_signal = 0;
	snmp_log(MY_FACILITY(LOG_WARNING), "Caught SIGHUP, reopening files.");
	if (sa_output > 1)
		snmp_log(MY_FACILITY(LOG_NOTICE), "Reopening output file %s", sa_logfile);
	if (sa_logfillog != 0) {
		fflush(stdlog);
		fclose(stdlog);
		snmp_disable_filelog();
		if ((stdlog = freopen(sa_logfile, sa_appendlog ? "a" : "w", stdlog)) == NULL) {
			/* I hope we have another log sink. */
			snmp_log(MY_FACILITY(LOG_ERR), "%s", strerror(errno));
			snmp_log(MY_FACILITY(LOG_ERR), "Could not reopen log file %s", sa_logfile);
		}
		snmp_enable_filelog(sa_logfile, sa_appendlog);
	}
	return (0);
}

static RETSIGTYPE
sa_int_handler(int signum)
{
	sa_int_signal = 1;
	return (RETSIGTYPE) (0);
}
static int
sa_int_catch(void)
{
	return sa_sig_register(SIGINT, &sa_int_handler);
}
static int
sa_int_block(void)
{
	return sa_sig_register(SIGINT, NULL);
}
static void sa_exit(int retval);
static int
sa_int_action(void)
{
	sa_int_signal = 0;
	snmp_log(MY_FACILITY(LOG_WARNING), "%s: Caught SIGINT, shutting down", sa_program);
	sa_exit(0);
	return (0);		/* should be no return */
}

static RETSIGTYPE
sa_trm_handler(int signum)
{
	sa_trm_signal = 1;
	return (RETSIGTYPE) (0);
}
static int
sa_trm_catch(void)
{
	return sa_sig_register(SIGTERM, &sa_trm_handler);
}
static int
sa_trm_block(void)
{
	return sa_sig_register(SIGTERM, NULL);
}
static void sa_exit(int retval);
static int
sa_trm_action(void)
{
	sa_trm_signal = 0;
	snmp_log(MY_FACILITY(LOG_WARNING), "%s: Caught SIGTERM, shutting down", sa_program);
	sa_exit(0);
	return (0);		/* should be no return */
}
static void
sa_sig_catch(void)
{
	sa_alm_catch();
	sa_pol_catch();
	sa_hup_catch();
	sa_int_catch();
	sa_trm_catch();
}
static void
sa_sig_block(void)
{
	sa_alm_block();
	sa_pol_block();
	sa_hup_block();
	sa_int_block();
	sa_trm_block();
}

int
sa_start_timer(long duration)
{
	if (sa_alarms) {
		struct itimerval setting = {
			{0, 0},
			{duration / 1000, (duration % 1000) * 1000}
		};
		if (sa_alm_catch())
			return (-1);
		if (setitimer(ITIMER_REAL, &setting, NULL))
			return (-1);
		sa_alm_signal = 0;
		return (0);
	} else {
#if defined NETSNMP_DS_APPLICATION_ID
		struct timeval setting = {
			duration / 1000, (duration % 1000) * 1000
		};
		sa_alm_handle = snmp_alarm_register_hr(setting, 0, sa_snmp_alm_handler, NULL);
#else
		sa_alm_handle = snmp_alarm_register((duration + 999) / 1000, 0, sa_snmp_alm_handler, NULL);
#endif
		return (sa_alm_handle ? 0 : -1);
	}
}
static void
sa_exit(int retval)
{
	if (retval)
		snmp_log(MY_FACILITY(LOG_ERR), "%s: Exiting %d", sa_program, retval);
	else
		snmp_log(MY_FACILITY(LOG_NOTICE), "%s: Exiting %d", sa_program, retval);
	fflush(stdout);
	fflush(stderr);
	sa_sig_block();
	closelog();
	exit(retval);
}
static void
sa_init_logging(int argc, char *argv[])
{
	static char progname[256];

	/* The purpose of this function is to bring logging up before forking (and while still in the foreground) so that we can use the snmp_log() function before and during forking if necessary.
	   Note that the default configuration for snmp_log() is to send all logs to standard error. */
	strncpy(progname, basename(argv[0]), sizeof(progname));
	snmp_disable_log();
	if (sa_logfillog) {
		snmp_enable_filelog(sa_logfile, sa_appendlog);
	}
	if (sa_logstderr | sa_logstdout) {
#if defined LOG_PERROR
		/* Note that when we have Linux LOG_PERROR, and logs go both to syslog and stderr, it is better to use the LOG_PERROR than to use snmp_log()'s print to stderr, as the former is better 
		   formated. */
		if (!sa_logsyslog)
			snmp_enable_stderrlog();
#else				/* defined LOG_PERROR */
		snmp_enable_stderrlog();
#endif				/* defined LOG_PERROR */
	}
	if (sa_logsyslog) {
#if !defined HAVE_SNMP_ENABLE_SYSLOG_IDENT
		snmp_enable_syslog();
#else				/* !defined HAVE_SNMP_ENABLE_SYSLOG_IDENT */
		snmp_enable_syslog_ident("gmiMIB", LOG_DAEMON);
#endif				/* !defined HAVE_SNMP_ENABLE_SYSLOG_IDENT */
		/* Note that the way that snmp sets up the logger is not really the way we want it, so close the log and reopen it the way we want. */
		closelog();
#if defined LOG_PERROR
		openlog("gmiMIB", LOG_PID | LOG_CONS | LOG_NDELAY | (sa_logstderr ? LOG_PERROR : 0), MY_FACILITY(0));
#else				/* defined LOG_PERROR */
		openlog("gmiMIB", LOG_PID | LOG_CONS | LOG_NDELAY, MY_FACILITY(0));
#endif				/* defined LOG_PERROR */
	}
	if (sa_logcallog) {
		snmp_enable_calllog();
	}
}
static void
sa_enter(int argc, char *argv[])
{
	if (sa_nomead) {
		pid_t pid;

		if ((pid = fork()) < 0) {
			perror(argv[0]);
			exit(2);
		} else if (pid != 0) {
			/* parent exits */
			exit(0);
		}
		setsid();	/* become a session leader */
		/* fork once more for SVR4 */
		if ((pid = fork()) < 0) {
			perror(argv[0]);
			exit(2);
		} else if (pid != 0) {
			/* parent responsible for writing pid file */
			if (sa_nomead || sa_pidfile[0] != '\0') {
				FILE *pidf;

				/* initialize default filename */
				if (sa_pidfile[0] == '\0')
					snprintf(sa_pidfile, sizeof(sa_pidfile), "/var/run/%s.pid", sa_program);
				if (sa_output > 1) {
					snmp_log(MY_FACILITY(LOG_NOTICE), "%s: Writing daemon pid to file %s", sa_program, sa_pidfile);
				}
				if ((pidf = fopen(sa_pidfile, "w+"))) {
					fprintf(pidf, "%d", (int) pid);
					fflush(pidf);
					fclose(pidf);
				} else {
					snmp_log(MY_FACILITY(LOG_ERR), "%s: %m", sa_program);
					snmp_log(MY_FACILITY(LOG_ERR), "%s: Could not write pid to file %s", sa_program, sa_pidfile);
					sa_exit(2);
					/* no return */
				}
			}
			/* parent exits */
			exit(0);
		}
		/* child continues */
		/* release current directory */
		if (chdir("/") < 0) {
			perror(argv[0]);
			exit(2);
		}
		umask(0);	/* clear file creation mask */
		/* rearrange file streams */
		fclose(stdin);
	}
	/* continue as foreground or background */
	sa_init_logging(argc, argv);
	sa_sig_catch();
	snmp_log(MY_FACILITY(LOG_NOTICE), "%s: Startup complete.", sa_program);
}
static void
sa_mloop(int argc, char *argv[])
{
	if (sa_agentx) {
		if (sa_debug)
			snmp_log(MY_FACILITY(LOG_DEBUG), "%s: running as AgentX client\n", argv[0]);
		/* run as an AgentX client */
		ds_set_boolean(DS_APPLICATION_ID, DS_AGENT_ROLE, 1);
	} else {
		if (sa_debug)
			snmp_log(MY_FACILITY(LOG_DEBUG), "%s: running as SNMP master agent\n", argv[0]);
		/* run as SNMP master */
		ds_set_boolean(DS_APPLICATION_ID, DS_AGENT_ROLE, 0);
	}
	if (sa_alarms) {
		if (sa_debug)
			snmp_log(MY_FACILITY(LOG_DEBUG), "%s: using application alarms\n", argv[0]);
		/* use application alarms */
		ds_set_boolean(DS_LIBRARY_ID, DS_LIB_ALARM_DONT_USE_SIG, 1);
	}
	/* initialize agent */
	init_agent("gmiMIB");
	/* initialize MIB */
	init_gmiMIB();
	/* initialize SNMP */
	init_snmp("gmiMIB");
	if (!sa_agentx) {
		if (sa_debug)
			snmp_log(MY_FACILITY(LOG_DEBUG), "%s: running as SNMP master\n", argv[0]);
#if !defined NETSNMP_DS_APPLICATION_ID
		init_master_agent(710, NULL, NULL);
#else
		init_master_agent();
#endif
	}
	for (;;) {
		int retval;

		/* to use select or poll you need to use the snmp_select_info() to obtain the fd of the agentx socket and add it to the fdset. */
		/* note that SIGALRM is used by snmp: use the snmp_alarm() api instead */
#if 0
		if (snmp_select() == 0) {
			if (sa_alarms == 0)
				run_alarms();
		}
#endif
		retval = agent_check_and_process(1);	/* 0 == don't block */
		if (retval == 0) {
			/* alarm occurred, alarm conditions checked */
		} else if (retval == -1) {
			/* error (or signal) ocurred */
			if (sa_alm_signal) {
				sa_alm_action();
			}
			if (sa_pol_signal) {
				sa_pol_action();
			}
			if (sa_hup_signal) {
				sa_hup_action();
			}
			if (sa_int_signal) {
				if (sa_debug)
					snmp_log(MY_FACILITY(LOG_DEBUG), "%s: shutting down\n", argv[0]);
				snmp_shutdown("gmiMIB");
				sa_int_action();	/* no return */
			}
			if (sa_trm_signal) {
				if (sa_debug)
					snmp_log(MY_FACILITY(LOG_DEBUG), "%s: shutting down\n", argv[0]);
				snmp_shutdown("gmiMIB");
				sa_trm_action();	/* no return */
			}
		} else if (retval > 0) {
			/* processed packets */
			if (sa_fclose) {
				/* close files after each request */
				if (sa_fd != 0) {
					int fd = sa_fd;

					sa_fd = 0;
					close(fd);
				}
			}
			sa_stats_refresh = 1;
			sa_request++;
		}
	}
	if (sa_debug)
		snmp_log(MY_FACILITY(LOG_DEBUG), "%s: shutting down\n", argv[0]);
	snmp_shutdown("gmiMIB");
}

int
main(int argc, char *argv[])
{
	for (;;) {
		int c, val, fd;
		char *cptr;
		struct passwd *pw;
		struct group *gr;
		struct stat st;

#if defined _GNU_SOURCE
		int option_index = 0;
                /* *INDENT-OFF* */
                static struct option long_options[] = {
                        {"log-addresses",	no_argument,		NULL, 'a'},
                        {"append",		no_argument,		NULL, 'A'},
                        {"config-file",		required_argument,	NULL, 'c'},
                        {"no-configs",		no_argument,		NULL, 'C'},
                        {"dump",		no_argument,		NULL, 'd'},
                        {"debug",		optional_argument,	NULL, 'D'},
                        {"debug-tokens",	optional_argument,	NULL, 'D'},
                        {"dont-fork",		no_argument,		NULL, 'f'},
                        {"gid",			required_argument,	NULL, 'g'},
                        {"groupid",		required_argument,	NULL, 'g'},
                        {"help",		no_argument,		NULL, 'h'},
                        {"?",			no_argument,		NULL, 'h'},
                        {"help-directives",	no_argument,		NULL, 'H'},
                        {"initialize",		required_argument,	NULL, 'I'},
                        {"init-modules",	required_argument,	NULL, 'I'},
                        {"keep-open",		no_argument,		NULL, 'k'},
                        {"log-file",		optional_argument,	NULL, 'l'},
                        {"logfile",		optional_argument,	NULL, 'l'},
                        {"Lf",			optional_argument,	NULL, 'l'},
                        {"LF",			required_argument,	NULL, 'l'},
                        {"log-stderr",		no_argument,		NULL, 'L'},
                        {"Le",			no_argument,		NULL, 'L'},
                        {"LE",			required_argument,	NULL, 'L'},
                        {"mibs",		required_argument,	NULL, 'm'},
                        {"master",		no_argument,		NULL, 'M'},
                        {"mibdirs",		required_argument,	NULL, 'M'},
                        {"nodaemon",		no_argument,		NULL, 'n'},
                        {"name",		required_argument,	NULL, 'n'},
                        {"dry-run",		no_argument,		NULL, 'N'},
                        {"log-stdout",		no_argument,		NULL, 'o'},
                        {"Lo",			no_argument,		NULL, 'o'},
                        {"LO",			required_argument,	NULL, 'o'},
                        {"port",		required_argument,	NULL, 'p'},
                        {"pidfile",		required_argument,	NULL, 'P'},
                        {"quiet",		no_argument,		NULL, 'q'},
                        {"quick",		no_argument,		NULL, 'q'},
                        {"noroot",		no_argument,		NULL, 'r'},
                        {"log-syslog",		no_argument,		NULL, 's'},
                        {"Ls",			no_argument,		NULL, 's'},
                        {"LS",			required_argument,	NULL, 's'},
                        {"syslog",		no_argument,		NULL, 's'},
                        {"sysctl-file",		required_argument,	NULL, 'S'},
                        {"agent-alarms",	no_argument,		NULL, 't'},
                        {"transport",		optional_argument,	NULL, 'T'},
                        {"uid",			required_argument,	NULL, 'u'},
                        {"userid",		required_argument,	NULL, 'u'},
                        {"dont-remove-pidfile",	no_argument,		NULL, 'U'},
                        {"leave-pidfile",	no_argument,		NULL, 'U'},
                        {"version",		no_argument,		NULL, 'v'},
                        {"verbose",		optional_argument,	NULL, 'V'},
                        {"agentx-socket",	required_argument,	NULL, 'x'},
                        {"agentx",		no_argument,		NULL, 'X'},
                        {"copying",		no_argument,		NULL, 'y'},
#if 0
                        {"directory",		required_argument,	NULL, 'd'},
                        {"basename",		required_argument,	NULL, 'b'},
                        {"outfile",		required_argument,	NULL, 'o'},
                        {"errfile",		required_argument,	NULL, 'e'},
#endif
                        { 0, }
                };
                /* *INDENT-ON* */

		c = getopt_long_only(argc, argv, ":aAc:CdD::fg:hHI:kl::L::m:M::n::o::p:P:qrs::S:tT::u:UvV::x:Xy", long_options, &option_index);
#else				/* defined _GNU_SOURCE */
		c = getopt(argc, argv, ":aAc:CdD::fg:hHI:kl::L::m:M::n::o::p:P:qrs::S:tT::u:UvV::x:Xy");
#endif				/* defined _GNU_SOURCE */
		if (c == -1) {
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: done options processing\n", argv[0]);
			break;
		}
		switch (c) {
		case 0:
			goto bad_usage;
		case 'a':	/* -a, --log-addresses */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: logging addresses\n", argv[0]);
			sa_logaddr++;
			break;
		case 'A':	/* -A, --append */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: will not truncate logfile\n", argv[0]);
#if defined NETSNMP_DS_LIB_APPEND_LOGFILES
			ds_set_boolean(DS_LIBRARY_ID, NETSNMP_DS_LIB_APPEND_LOGFILES, 1);
#endif				/* defined NETSNMP_DS_LIB_APPEND_LOGFILES */
			sa_appendlog = 1;
			break;
		case 'c':	/* -c, --config-file CONFIGFILE */
			if (optarg == NULL)
				goto bad_option;
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: using configuration file %s\n", argv[0], optarg);
			ds_set_string(DS_LIBRARY_ID, DS_LIB_OPTIONALCONFIG, optarg);
			break;
		case 'C':	/* -C, --no-configs */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: not reading default config files\n", argv[0]);
			ds_set_boolean(DS_LIBRARY_ID, DS_LIB_DONT_READ_CONFIGS, 1);
			break;
		case 'd':	/* -d, --dump */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: setting packet dump\n", argv[0]);
			sa_dump = 1;
			// snmp_set_dump_packet(sa_dump);
			ds_set_boolean(DS_LIBRARY_ID, DS_LIB_DUMP_PACKET, sa_dump);
			ds_set_boolean(DS_APPLICATION_ID, DS_AGENT_VERBOSE, sa_dump);
			break;
		case 'D':	/* -D, --debug [LEVEL], --debug-tokens [TOKENS] */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: increasing debug verbosity\n", argv[0]);
			if (optarg == NULL) {
				/* no option: must be -D, --debug */
				sa_debug++;
				if (sa_debug)
					snmp_log(MY_FACILITY(LOG_DEBUG), "%s: debug level is now %d\n", argv[0], sa_debug);
				if (sa_debug)
					snmp_log(MY_FACILITY(LOG_DEBUG), "%s: debugging all tokens\n", argv[0]);
				if (sa_debug)
					debug_register_tokens("ALL");
			} else {
				cptr = optarg;
				if ((val = strtol(optarg, &cptr, 0)) < 0)
					goto bad_option;
				if (*cptr == '\0') {
					/* it is just a number, must be -D, --debug [LEVEL] */
					sa_debug = val;
					if (sa_debug)
						snmp_log(MY_FACILITY(LOG_DEBUG), "%s: debug level is now %d\n", argv[0], sa_debug);
				} else {
					/* not a number, must be -D, --debug-tokens TOKENS */
					if (sa_debug)
						snmp_log(MY_FACILITY(LOG_DEBUG), "%s: debugging tokens %s\n", argv[0], optarg);
					debug_register_tokens(optarg);
				}
			}
			break;
		case 'f':	/* -f, --dont-fork */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: suppressing daemon mode\n", argv[0]);
			sa_nomead = 0;
			break;
		case 'u':	/* -u, --uid, --userid UID */
			cptr = optarg;
			if ((val = strtol(optarg, &cptr, 0)) < 0)
				goto bad_option;
			/* UID can be name or number */
			if ((pw = (*cptr == '\0') ? getpwuid((uid_t) val) : getpwnam(optarg)) == NULL)
				goto bad_option;
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: will run as uid %s(%d)\n", argv[0], pw->pw_name, pw->pw_uid);
			ds_set_int(DS_APPLICATION_ID, DS_AGENT_USERID, pw->pw_uid);
			break;
		case 'g':	/* -g, --gid, --groupdid GID */
			cptr = optarg;
			if ((val = strtol(optarg, &cptr, 0)) < 0)
				goto bad_option;
			/* GID can be name or number */
			if ((gr = (*cptr == '\0') ? getgrgid((gid_t) val) : getgrnam(optarg)) == NULL)
				goto bad_option;
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: will run as gid %s(%d)\n", argv[0], gr->gr_name, gr->gr_gid);
			ds_set_int(DS_APPLICATION_ID, DS_AGENT_GROUPID, gr->gr_gid);
			break;
		case 'h':	/* -h, --help, -?, --? */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: printing help message\n", argv[0]);
			sa_help(argc, argv);
			exit(0);
		case 'H':	/* -H, --help-directives */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: printing config directives\n", argv[0]);
			sa_help_directives(argc, argv);
			exit(0);
		case 'I':	/* -I, --init-modules, --initialize MODULE[{,| |:}MODULE]* */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: will initialize modules: %s\n", argv[0], optarg);
			add_to_init_list(optarg);
			break;
		case 'k':	/* -k, --keep-open */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: keeping files open\n", argv[0]);
			sa_fclose = 0;
			break;
		case 'l':	/* -l, --log-file, --logfile, -Lf, -LF p1[-p2] [LOGFILE] */
			if (optarg != NULL)
				strncpy(sa_logfile, optarg, sizeof(sa_logfile));
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: will log to file %s\n", argv[0], sa_logfile);
			sa_logfillog = 1;
			break;
		case 'L':	/* -L, --log-stderr, -Le, -LE p1[-p2] */
			/* Note that the recent NET-SNMP version of this option is far more complicated: -Le is the same as the old version of the option; -Lf LOGFILE is like the -l option; -Ls is
			   like the -s option; -Lo logs messages to standard output; -LX p1[-p2] [LOGFILE], where X = E, F, S or O, logs priority p1 and above to X, or p1 thru p2 to X. */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: logging to standard error\n", argv[0]);
			sa_logstderr = 1;
			break;
		case 'm':	/* -m, --mibs MIBS */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: using MIBS %s\n", argv[0], optarg);
			break;
		case 'M':	/* -M, --master or -M, --mibdirs MIBDIRS */
			if (optarg) {
				/* -M, --mibdirs MIBDIRS */
				if (sa_debug)
					snmp_log(MY_FACILITY(LOG_DEBUG), "%s: using MIBDIRS %s\n", argv[0], optarg);
			} else {
				/* -M, --master */
				if (sa_debug)
					snmp_log(MY_FACILITY(LOG_DEBUG), "%s: setting SNMP master\n", argv[0]);
				sa_agentx = 0;
				ds_set_boolean(DS_APPLICATION_ID, DS_AGENT_ROLE, 0);
			}
			break;
		case 'n':	/* -n, --nodaemon or -n, --name NAME */
			if (optarg) {
				/* -n, --name NAME */
				if (sa_debug)
					snmp_log(MY_FACILITY(LOG_DEBUG), "%s: using name %s\n", argv[0], optarg);
				ds_set_string(DS_APPLICATION_ID, DS_AGENT_PROGNAME, optarg);
			} else {
				/* -n, --nodaemon */
				if (sa_debug)
					snmp_log(MY_FACILITY(LOG_DEBUG), "%s: suppressing deamon mode\n", argv[0]);
				sa_nomead = 0;
				ds_set_string(DS_APPLICATION_ID, DS_AGENT_PROGNAME, basename(argv[0]));
			}
			break;
		case 'N':	/* -N, --dry-run */
#if defined NETSNMP_DS_AGENT_QUIT_IMMEDIATELY
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: setting for dry-runs startup\n", argv[0]);
			ds_set_boolean(DS_APPLICATION_ID, NETSNMP_DS_AGENT_QUIT_IMMEDIATELY, 1);
			break;
#else				/* defined NETSNMP_DS_AGENT_QUIT_IMMEDIATELY */
			snmp_log(MY_FACILITY(LOG_DEBUG), "%s: -N option not supported\n", argv[0]);
			goto bad_option;
#endif				/* defined NETSNMP_DS_AGENT_QUIT_IMMEDIATELY */
		case 'o':	/* -o, --log-stdout, -Lo, -LO p1[-p2] */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: logging to stdout\n", argv[0]);
			sa_logstdout = 1;
			break;
		case 'p':	/* -p, --port PORTNUM or -p, --pidfile PIDFILE */
			cptr = optarg;
			if ((val = strtol(optarg, &cptr, 0)) < 0 || val > 16383)
				goto bad_option;
			if (*cptr == '\0') {
				char buf[4096];

				/* -p, --port PORTNUM */
				if ((cptr = ds_get_string(DS_APPLICATION_ID, DS_AGENT_PORTS)))
					snprintf(buf, sizeof(buf), "%s,%s", cptr, optarg);
				else
					strncpy(buf, optarg, sizeof(buf));
				ds_set_string(DS_APPLICATION_ID, DS_AGENT_PORTS, buf);
				break;
			}
			/* fall through */
		case 'P':	/* -p, -P, --pidfile PIDFILE */
			if (optarg) {
				/* either it exists */
				if (stat(optarg, &st) == -1) {
					/* or we can create it */
					if ((fd = open(optarg, O_CREAT, 0600)) == -1) {
						perror(argv[0]);
						goto bad_option;
					}
					close(fd);
				}
				if (sa_debug)
					snmp_log(MY_FACILITY(LOG_DEBUG), "%s: setting pid file to %s\n", argv[0], optarg);
				strncpy(sa_pidfile, optarg, sizeof(sa_pidfile));
			}
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: using pidfile %s\n", argv[0], sa_pidfile);
			break;
		case 'q':	/* -q, --quiet, --quick */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: suppressing normal output\n", argv[0]);
			sa_debug = 0;
			sa_output = 0;
			ds_set_boolean(DS_APPLICATION_ID, DS_AGENT_VERBOSE, 0);
			// snmp_set_quick_print();
			ds_set_boolean(DS_LIBRARY_ID, DS_LIB_QUICK_PRINT, 1);
			break;
		case 'r':	/* -r, --noroot */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: setting for non-root access\n", argv[0]);
			ds_set_boolean(DS_APPLICATION_ID, DS_AGENT_NO_ROOT_ACCESS, 1);
			break;
		case 's':	/* -s, --log-syslog, -Ls, -LS p1[-p2] */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: logging to system logs\n", argv[0]);
			sa_logsyslog = 1;
			break;
		case 'S':	/* -S, -sysctl-file FILENAME */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: using %s for backing\n", argv[0], optarg);
			strncpy(sa_sysctlf, optarg, sizeof(sa_sysctlf));
			break;
		case 't':	/* -t, --agent-alarms */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: setting agent alarms\n", argv[0]);
			sa_alarms = 0;
			ds_set_boolean(DS_LIBRARY_ID, DS_LIB_ALARM_DONT_USE_SIG, 1);
			break;
		case 'T':	/* -T, --transport [TRANSPORT] */
			if (optarg == NULL)
				goto udp_transport;
			if (!strcasecmp("TCP", optarg)) {
				if (sa_debug)
					snmp_log(MY_FACILITY(LOG_DEBUG), "%s: setting default transport to TCP\n", argv[0]);
				val = ds_get_int(DS_APPLICATION_ID, DS_AGENT_FLAGS);
				val |= SNMP_FLAGS_STREAM_SOCKET;
				ds_set_int(DS_APPLICATION_ID, DS_AGENT_FLAGS, val);
			} else if (!strcasecmp("UDP", optarg)) {
			      udp_transport:
				if (sa_debug)
					snmp_log(MY_FACILITY(LOG_DEBUG), "%s: setting default transport to UDP\n", argv[0]);
				val = ds_get_int(DS_APPLICATION_ID, DS_AGENT_FLAGS);
				val &= ~SNMP_FLAGS_STREAM_SOCKET;
				ds_set_int(DS_APPLICATION_ID, DS_AGENT_FLAGS, val);
			} else
				goto bad_option;
			break;
		case 'U':
#if defined NETSNMP_DS_AGENT_LEAVE_PIDFILE
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: will leave pidfile after shutdown\n", argv[0]);
			ds_set_boolean(DS_APPLICATION_ID, NETSNMP_DS_AGENT_LEAVE_PIDFILE, 1);
#else
			snmp_log(MY_FACILITY(LOG_DEBUG), "%s: -U option not supported\n");
			goto bad_option;
#endif				/* defined NETSNMP_DS_AGENT_LEAVE_PIDFILE */
			break;
		case 'v':	/* -v, --version */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: printing version message\n", argv[0]);
			sa_version(argc, argv);
			exit(0);
		case 'V':	/* -V, --verbose [LEVEL] */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: increasing output verbosity\n", argv[0]);
			if (optarg == NULL) {
				sa_output++;
			} else {
				if ((val = strtol(optarg, NULL, 0)) < 0)
					goto bad_option;
				sa_output = val;
			}
			if (sa_output > 1)
				ds_set_boolean(DS_APPLICATION_ID, DS_AGENT_VERBOSE, 1);
			else
				ds_set_boolean(DS_APPLICATION_ID, DS_AGENT_VERBOSE, 0);
			break;
		case 'x':	/* -x, --agentx-socket SOCKET */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: setting AgentX socket to %s\n", argv[0], optarg);
			ds_set_string(DS_APPLICATION_ID, DS_AGENT_X_SOCKET, optarg);
			// ds_set_boolean(DS_APPLICATION_ID, DS_AGENT_AGENTX_MASTER, 1);
			break;
		case 'X':	/* -X, --agentx */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: setting AgentX sub-agent\n", argv[0]);
			sa_agentx = 1;
			ds_set_boolean(DS_APPLICATION_ID, DS_AGENT_ROLE, 1);
			break;
		case 'y':	/* -y, --copying */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: printing copying message\n", argv[0]);
			sa_copying(argc, argv);
			exit(0);
		case '?':
		case ':':
		default:
		      bad_option:
			optind--;
			goto bad_nonopt;
		      bad_nonopt:
			if (sa_output || sa_debug) {
				if (optind < argc) {
					fprintf(stderr, "%s: syntax error near '", argv[0]);
					while (optind < argc)
						fprintf(stderr, "%s ", argv[optind++]);
					fprintf(stderr, "'\n");
				} else {
					fprintf(stderr, "%s: missing option or argument", argv[0]);
					fprintf(stderr, "\n");
				}
				fflush(stderr);
			      bad_usage:
				sa_usage(argc, argv);
			}
			exit(2);
		}
	}
	if (optind < argc) {
		if (sa_debug)
			snmp_log(MY_FACILITY(LOG_DEBUG), "%s: excess non-option arguments\n", argv[0]);
		goto bad_nonopt;
	}
	sa_enter(argc, argv);	/* daemonize if necessary */
	sa_mloop(argc, argv);	/* execute main loop */
	exit(0);
}
#endif				/* !defined MODULE */
#endif				/* defined MASTER */
