/*****************************************************************************

 @(#) $RCSfile: cdr2MIB.c,v $ $Name:  $($Revision: 0.9.2.9 $) $Date: 2009-01-14 14:29:57 $

 -----------------------------------------------------------------------------

 Copyright (c) 2008-2009  Monavacon Limited <http://www.monavacon.com/>
 Copyright (c) 2001-2008  OpenSS7 Corporation <http://www.openss7.com/>
 Copyright (c) 1997-2001  Brian F. G. Bidulock <bidulock@openss7.org>

 All Rights Reserved.

 This program is free software: you can redistribute it and/or modify it under
 the terms of the GNU Affero General Public License as published by the Free
 Software Foundation, version 3 of the license.

 This program is distributed in the hope that it will be useful, but WITHOUT
 ANY WARRANTY; without even the implied warranty of MERCHANTABILITY or FITNESS
 FOR A PARTICULAR PURPOSE.  See the GNU Affero General Public License for more
 details.

 You should have received a copy of the GNU Affero General Public License
 along with this program.  If not, see <http://www.gnu.org/licenses/>, or
 write to the Free Software Foundation, Inc., 675 Mass Ave, Cambridge, MA
 02139, USA.

 -----------------------------------------------------------------------------

 U.S. GOVERNMENT RESTRICTED RIGHTS.  If you are licensing this Software on
 behalf of the U.S. Government ("Government"), the following provisions apply
 to you.  If the Software is supplied by the Department of Defense ("DoD"), it
 is classified as "Commercial Computer Software" under paragraph 252.227-7014
 of the DoD Supplement to the Federal Acquisition Regulations ("DFARS") (or any
 successor regulations) and the Government is acquiring only the license rights
 granted herein (the license rights customarily provided to non-Government
 users).  If the Software is supplied to any unit or agency of the Government
 other than DoD, it is classified as "Restricted Computer Software" and the
 Government's rights in the Software are defined in paragraph 52.227-19 of the
 Federal Acquisition Regulations ("FAR") (or any successor regulations) or, in
 the cases of NASA, in paragraph 18.52.227-86 of the NASA Supplement to the FAR
 (or any successor regulations).

 -----------------------------------------------------------------------------

 Commercial licensing and support of this software is available from OpenSS7
 Corporation at a fee.  See http://www.openss7.com/

 -----------------------------------------------------------------------------

 Last Modified $Date: 2009-01-14 14:29:57 $ by $Author: brian $

 -----------------------------------------------------------------------------

 $Log: cdr2MIB.c,v $
 Revision 0.9.2.9  2009-01-14 14:29:57  brian
 - working up agents

 Revision 0.9.2.8  2009-01-10 17:09:12  brian
 - updated agents

 Revision 0.9.2.7  2009-01-04 13:16:00  brian
 - updated agents

 Revision 0.9.2.6  2009-01-03 10:51:01  brian
 - updated agent

 Revision 0.9.2.5  2009-01-02 15:46:39  brian
 - corrections

 Revision 0.9.2.4  2009-01-02 15:04:10  brian
 - updated agents

 *****************************************************************************/

#ident "@(#) $RCSfile: cdr2MIB.c,v $ $Name:  $($Revision: 0.9.2.9 $) $Date: 2009-01-14 14:29:57 $"

static char const ident[] = "$RCSfile: cdr2MIB.c,v $ $Name:  $($Revision: 0.9.2.9 $) $Date: 2009-01-14 14:29:57 $";

/* This file was generated by mib2c and is intended for use as
   a mib module for the ucd-snmp snmpd agent. */
#include <ucd-snmp/ucd-snmp-config.h>
#include <ucd-snmp/ucd-snmp-includes.h>
#include <ucd-snmp/ucd-snmp-agent-includes.h>
#include <ucd-snmp/callback.h>
#include <ucd-snmp/snmp-tc.h>
#include <ucd-snmp/default_store.h>
#include <ucd-snmp/snmp_alarm.h>
/* The following header files are mangled in most recent net-snmp releases so
 * the versions from UCD-SNMP 4.2.5 are included here.  */
#if defined HAVE_LIBNETSNMP
#else				/* defined HAVE_LIBNETSNMP */
#endif				/* defined HAVE_LIBNETSNMP */
/* These are messed up on both. */
#include "ds_agent.h"
#ifdef HAVE_UCD_SNMP_UTIL_FUNCS_H
#include <ucd-snmp/util_funcs.h>
/* Many recent net-snmp UCD compatible headers do not declare header_generic. */
int header_generic(struct variable *, oid *, size_t *, int, size_t *, WriteMethod **);
#else				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#include "util_funcs.h"
#endif				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#ifdef HAVE_UCD_SNMP_HEADER_COMPLEX_H
#include <ucd-snmp/header_complex.h>
#else				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
#include "header_complex.h"
#endif				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
/* This one is the other way around: it is just fine for net-snmp, but
 * ucd-snmp does not provide the header file at all.  */
#ifdef HAVE_UCD_SNMP_MIB_MODULES_H
#include <ucd-snmp/mib_modules.h>
#else				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#ifdef HAVE_NET_SNMP_AGENT_MIB_MODULES_H
#include <net-snmp/agent/mib_modules.h>
#else				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#include "mib_modules.h"
#endif				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#endif				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#include <stdint.h>
#include <signal.h>
#include <sys/stat.h>		/* for struct stat, fstat() */
#include <sys/types.h>
#include <unistd.h>
#include <stdlib.h>
#include <pwd.h>		/* for getpwuid() getpwnam() */
#include <grp.h>		/* for getgrgid() getgrnam() */
#include <libgen.h>		/* for basename() */
#include <fcntl.h>		/* for O_CREAT */
#include <sys/sysctl.h>		/* for sysctl */
#include <errno.h>
#include <string.h>
#ifdef _GNU_SOURCE
#include <getopt.h>
#endif
#include "cdr2MIB.h"
extern const char sa_program[];

#define MY_FACILITY(__pri)	(LOG_DAEMON|(__pri))
#define MASTER 1
#if !defined MODULE
extern int sa_dump;			/* default packet dump */
extern int sa_debug;			/* default no debug */
extern int sa_nomead;			/* default daemon mode */
extern int sa_output;			/* default normal output */
extern int sa_agentx;			/* default agentx mode */
extern int sa_alarms;			/* default application alarms */
extern int sa_logaddr;			/* log addresses */
extern int sa_logfillog;		/* log to sa_logfile */
extern int sa_logstderr;		/* log to standard error */
extern int sa_logstdout;		/* log to standard output */
extern int sa_logsyslog;		/* log to system logs */
extern int sa_logcallog;		/* log to callback logs */
extern int sa_appendlog;		/* append to log file without truncating */
extern char sa_logfile[256];
extern char sa_pidfile[256];
extern char sa_sysctlf[256];

/* file stream for log file */
extern FILE *stdlog;
#endif				/* !defined MODULE */
extern int sa_fclose;			/* default close files between requests */
static int my_fd;			/* file descriptor for this MIB's use */
extern int sa_fd;			/* file descriptor for MIB use */
extern int sa_readfd;			/* file descriptor for autonomnous events */
extern int sa_changed;			/* indication to reread MIB configuration */
extern int sa_stats_refresh;		/* indications that statistics, the mib or its tables need to be refreshed */
extern int sa_request;			/* request number for per-request actions */
volatile int cdr2MIB_refresh = 1;
volatile int callDetailDataTable_refresh = 1;
volatile int simpleUsageMeteringControlTable_refresh = 1;
volatile int configurableSimpleUsageMeteringControlTable_refresh = 1;
volatile int blockGeneratingLogTable_refresh = 1;
volatile int fileGeneratingLogTable_refresh = 1;
volatile int callDetailLogRecordTable_refresh = 1;
volatile int configurableSimpleUsageMeteringControlR2Table_refresh = 1;
volatile int blockGeneratingLogR2Table_refresh = 1;
volatile int callDetailLogRecordR2Table_refresh = 1;

/*
 * cdr2MIB_variables_oid: object identifier for cdr2MIB
 * This is the top level oid that we want to register under.  This is essentially a prefix, with the
 * suffix appearing in the variable below.
 */
oid cdr2MIB_variables_oid[10] = { 1, 3, 6, 1, 4, 1, 29591, 17, 825, 2 };
oid simpleUsageMeteringControlTable_variables_oid[14] = { 1, 3, 6, 1, 4, 1, 29591, 17, 825, 2, 1, 3, 1, 1 };
oid configurableSimpleUsageMeteringControlTable_variables_oid[14] = { 1, 3, 6, 1, 4, 1, 29591, 17, 825, 2, 1, 4, 1, 1 };
oid blockGeneratingLogTable_variables_oid[14] = { 1, 3, 6, 1, 4, 1, 29591, 17, 825, 2, 1, 5, 1, 1 };
oid fileGeneratingLogTable_variables_oid[14] = { 1, 3, 6, 1, 4, 1, 29591, 17, 825, 2, 1, 6, 1, 1 };
oid configurableSimpleUsageMeteringControlR2Table_variables_oid[14] = { 1, 3, 6, 1, 4, 1, 29591, 17, 825, 2, 1, 8, 1, 1 };

/*
 * Oids for use in notifications defined in this MIB.
 */
oid blockRecordNotification_oid[11] = { 1, 3, 6, 1, 4, 1, 29591, 17, 825, 2, 0 };
oid callDetailRecordNotification_oid[11] = { 1, 3, 6, 1, 4, 1, 29591, 17, 825, 2, 0 };
oid fileCreationNotification_oid[11] = { 1, 3, 6, 1, 4, 1, 29591, 17, 825, 2, 0 };
oid blockRecordNotificationR2_oid[11] = { 1, 3, 6, 1, 4, 1, 29591, 17, 825, 2, 0 };
oid callDetailRecordNotificationR2_oid[11] = { 1, 3, 6, 1, 4, 1, 29591, 17, 825, 2, 0 };

/*
 * Oids accessible only for notify defined in this MIB.
 */
oid fileName_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 17, 825, 2, 1, 1 };
oid reasonForOutput_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 17, 825, 2, 1, 1 };
oid sequenceNumber_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 17, 825, 2, 1, 1 };
oid callRecord_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 17, 825, 2, 1, 1 };

/*
 * Other oids defined in this MIB.
 */
oid cdrParameterGroup_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 17, 825, 2, 2, 2 };
oid cdrNotificationGroup_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 17, 825, 2, 2, 2 };
oid cdr1NotificationGroup_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 17, 825, 2, 2, 2 };
oid cdr2NotificationGroup_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 17, 825, 2, 2, 2 };
oid cdr2MandatoryCallRecordFields_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 17, 825, 2, 2, 2 };
oid cdrMandatoryGroups_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 17, 825, 2, 2, 2 };
oid cdr1ConditionalGroups_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 17, 825, 2, 2, 2 };
oid cdr2MandatoryGroups_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 17, 825, 2, 2, 2 };
oid cd1RecordFormatGroup_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 17, 825, 2, 2, 2 };
oid cdr2ConditionalFieldsGroup_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 17, 825, 2, 2, 2 };
oid cdr2Conformances_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 17, 825, 2, 2, 2 };
static const oid zeroDotZero_oid[2] = { 0, 0 };
static oid snmpTrapOID_oid[11] = { 1, 3, 6, 1, 6, 3, 1, 1, 4, 1, 0 };

/*
 * variable7 cdr2MIB_variables: tree for cdr2MIB
 * This variable defines function callbacks and type return information for the cdr2MIB mib section
 */
struct variable7 cdr2MIB_variables[] = {
	/* magic number, variable type, ro/rw, callback fn, L, oidsuffix */
#define   CREATIONTRIGGERLIST   2
	{(u_char) CREATIONTRIGGERLIST, ASN_BIT_STR, RWRITE, var_simpleUsageMeteringControlTable, 5, {1, 3, 1, 1, 1}},
#define   SIMPLEUSAGEMETERINGCONTROLENTRYSTATUS  3
	{(u_char) SIMPLEUSAGEMETERINGCONTROLENTRYSTATUS, ASN_INTEGER, RWRITE, var_simpleUsageMeteringControlTable, 5, {1, 3, 1, 1, 2}},
#define   SAMPLINGRATE          4
	{(u_char) SAMPLINGRATE, ASN_INTEGER, RWRITE, var_configurableSimpleUsageMeteringControlTable, 5, {1, 4, 1, 1, 1}},
#define   CONFIGURATIONMASK     5
	{(u_char) CONFIGURATIONMASK, ASN_BIT_STR, RWRITE, var_configurableSimpleUsageMeteringControlTable, 5, {1, 4, 1, 1, 2}},
#define   CONFIGURATIONROWSTATUS  6
	{(u_char) CONFIGURATIONROWSTATUS, ASN_INTEGER, RWRITE, var_configurableSimpleUsageMeteringControlTable, 5, {1, 4, 1, 1, 3}},
#define   BLOCKGENERATINGLOGMAXBLOCKSIZE  7
	{(u_char) BLOCKGENERATINGLOGMAXBLOCKSIZE, ASN_INTEGER, RWRITE, var_blockGeneratingLogTable, 5, {1, 5, 1, 1, 1}},
#define   BLOCKGENERATINGLOGMAXTIMEINTERVAL  8
	{(u_char) BLOCKGENERATINGLOGMAXTIMEINTERVAL, ASN_INTEGER, RWRITE, var_blockGeneratingLogTable, 5, {1, 5, 1, 1, 2}},
#define   BLOCKGENERATINGLOGSTORAGETYPE  9
	{(u_char) BLOCKGENERATINGLOGSTORAGETYPE, ASN_INTEGER, RWRITE, var_blockGeneratingLogTable, 5, {1, 5, 1, 1, 3}},
#define   BLOCKGENERATINGLOGROWSTATUS  10
	{(u_char) BLOCKGENERATINGLOGROWSTATUS, ASN_INTEGER, RWRITE, var_blockGeneratingLogTable, 5, {1, 5, 1, 1, 4}},
#define   TIMESOFDAY            11
	{(u_char) TIMESOFDAY, ASN_OCTET_STR, RWRITE, var_fileGeneratingLogTable, 5, {1, 6, 1, 1, 1}},
#define   PERIODICTRIGGER       12
	{(u_char) PERIODICTRIGGER, ASN_INTEGER, RWRITE, var_fileGeneratingLogTable, 5, {1, 6, 1, 1, 2}},
#define   FILEGENERATINGLOGSTORAGETYPE  13
	{(u_char) FILEGENERATINGLOGSTORAGETYPE, ASN_INTEGER, RWRITE, var_fileGeneratingLogTable, 5, {1, 6, 1, 1, 3}},
#define   FILEGENERATINGLOGENTRYSTATUS  14
	{(u_char) FILEGENERATINGLOGENTRYSTATUS, ASN_INTEGER, RWRITE, var_fileGeneratingLogTable, 5, {1, 6, 1, 1, 4}},
#define   NETWORKPROVIDERID     15
	{(u_char) NETWORKPROVIDERID, ASN_OCTET_STR, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 1}},
#define   RECORDTYPE            16
	{(u_char) RECORDTYPE, ASN_INTEGER, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 2}},
#define   SEIZURETIME           17
	{(u_char) SEIZURETIME, ASN_OCTET_STR, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 3}},
#define   ANSWERTIME            18
	{(u_char) ANSWERTIME, ASN_OCTET_STR, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 4}},
#define   PARTIALTIME           19
	{(u_char) PARTIALTIME, ASN_OCTET_STR, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 5}},
#define   EVENTTIME             20
	{(u_char) EVENTTIME, ASN_OCTET_STR, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 6}},
#define   CALLINGPARTYNUMBER    21
	{(u_char) CALLINGPARTYNUMBER, ASN_OCTET_STR, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 7}},
#define   CALLEDPARTYNUMBER     22
	{(u_char) CALLEDPARTYNUMBER, ASN_OCTET_STR, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 8}},
#define   REDIRECTINGNUMBER     23
	{(u_char) REDIRECTINGNUMBER, ASN_OCTET_STR, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 9}},
#define   REDIRECTIONNUMBER     24
	{(u_char) REDIRECTIONNUMBER, ASN_OCTET_STR, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 10}},
#define   ORIGINALCALLEDNUMBER  25
	{(u_char) ORIGINALCALLEDNUMBER, ASN_OCTET_STR, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 11}},
#define   CALLINGPARTYNUMBERNOTSCREENED  26
	{(u_char) CALLINGPARTYNUMBERNOTSCREENED, ASN_OCTET_STR, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 12}},
#define   OPERATORSPECIFIC1NUMBER  27
	{(u_char) OPERATORSPECIFIC1NUMBER, ASN_OCTET_STR, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 13}},
#define   OPERATORSPECIFIC2NUMBER  28
	{(u_char) OPERATORSPECIFIC2NUMBER, ASN_OCTET_STR, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 14}},
#define   OPERATORSPECIFIC3NUMBER  29
	{(u_char) OPERATORSPECIFIC3NUMBER, ASN_OCTET_STR, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 15}},
#define   BEARERSERVICE         30
	{(u_char) BEARERSERVICE, ASN_OCTET_STR, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 16}},
#define   PARTICIPANTSERVICEUSER  31
	{(u_char) PARTICIPANTSERVICEUSER, ASN_INTEGER, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 17}},
#define   CALLIDENTIFICATIONNUMBER  32
	{(u_char) CALLIDENTIFICATIONNUMBER, ASN_OCTET_STR, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 18}},
#define   SUPPLEMENTARYSERVICES  33
	{(u_char) SUPPLEMENTARYSERVICES, ASN_OCTET_STR, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 19}},
#define   IMMEDIATENOTIFICATIONFORUSAGEMETERING  34
	{(u_char) IMMEDIATENOTIFICATIONFORUSAGEMETERING, ASN_INTEGER, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 20}},
#define   CAUSE                 35
	{(u_char) CAUSE, ASN_OCTET_STR, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 21}},
#define   PERSONALUSERID        36
	{(u_char) PERSONALUSERID, ASN_OCTET_STR, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 22}},
#define   CHARGEDPARTICIPANT    37
	{(u_char) CHARGEDPARTICIPANT, ASN_INTEGER, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 23}},
#define   CHARGEDDIRECTORYNUMBER  38
	{(u_char) CHARGEDDIRECTORYNUMBER, ASN_OCTET_STR, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 24}},
#define   PERCENTAGETOBEBILLED  39
	{(u_char) PERCENTAGETOBEBILLED, ASN_INTEGER, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 25}},
#define   ACCOUNTCODEINPUT      40
	{(u_char) ACCOUNTCODEINPUT, ASN_OCTET_STR, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 26}},
#define   INSERVICECODE         41
	{(u_char) INSERVICECODE, ASN_OCTET_STR, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 27}},
#define   QUEUETIMESTAMP        42
	{(u_char) QUEUETIMESTAMP, ASN_OCTET_STR, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 28}},
#define   QUEUEDURATION         43
	{(u_char) QUEUEDURATION, ASN_INTEGER, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 29}},
#define   SERVICESPECIFICININFORMATION  44
	{(u_char) SERVICESPECIFICININFORMATION, ASN_OCTET_STR, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 30}},
#define   PARTIALRECORDNUMBER   45
	{(u_char) PARTIALRECORDNUMBER, ASN_INTEGER, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 31}},
#define   PARTIALRECORDREASON   46
	{(u_char) PARTIALRECORDREASON, ASN_INTEGER, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 32}},
#define   EXCHANGEINFO          47
	{(u_char) EXCHANGEINFO, ASN_OCTET_STR, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 33}},
#define   RELATEDCALLNUMBER     48
	{(u_char) RELATEDCALLNUMBER, ASN_OCTET_STR, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 34}},
#define   CDRPURPOSE            49
	{(u_char) CDRPURPOSE, ASN_BIT_STR, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 35}},
#define   PHYSICALLINECODE      50
	{(u_char) PHYSICALLINECODE, ASN_OCTET_STR, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 36}},
#define   RECEIVEDDIGITS        51
	{(u_char) RECEIVEDDIGITS, ASN_OCTET_STR, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 37}},
#define   OPERATORSPECIFIC1ADDITIONALNUMBER  52
	{(u_char) OPERATORSPECIFIC1ADDITIONALNUMBER, ASN_OCTET_STR, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 38}},
#define   OPERATORSPECIFIC2ADDITIONALNUMBER  53
	{(u_char) OPERATORSPECIFIC2ADDITIONALNUMBER, ASN_OCTET_STR, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 39}},
#define   OPERATORSPECIFIC3ADDITIONALNUMBER  54
	{(u_char) OPERATORSPECIFIC3ADDITIONALNUMBER, ASN_OCTET_STR, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 40}},
#define   CALLINGPARTYCATEGORY  55
	{(u_char) CALLINGPARTYCATEGORY, ASN_OCTET_STR, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 41}},
#define   CALLINGPARTYTYPE      56
	{(u_char) CALLINGPARTYTYPE, ASN_INTEGER, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 42}},
#define   CHARGEINFORMATION     57
	{(u_char) CHARGEINFORMATION, ASN_OCTET_STR, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 43}},
#define   PROGRESS              58
	{(u_char) PROGRESS, ASN_OCTET_STR, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 44}},
#define   ACCESSDELIVERY        59
	{(u_char) ACCESSDELIVERY, ASN_BIT_STR, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 45}},
#define   TRUNKGROUPOUTGOING    60
	{(u_char) TRUNKGROUPOUTGOING, ASN_OCTET_STR, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 46}},
#define   TRUNKGROUPINCOMING    61
	{(u_char) TRUNKGROUPINCOMING, ASN_OCTET_STR, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 47}},
#define   FALLBACKBEARERSERVICE  62
	{(u_char) FALLBACKBEARERSERVICE, ASN_OCTET_STR, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 48}},
#define   TELESERVICE           63
	{(u_char) TELESERVICE, ASN_OCTET_STR, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 49}},
#define   CONVERSATIONTIME      64
	{(u_char) CONVERSATIONTIME, ASN_INTEGER, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 50}},
#define   DURATIONTIMEACM       65
	{(u_char) DURATIONTIMEACM, ASN_INTEGER, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 51}},
#define   DURATIONTIMEBANSWER   66
	{(u_char) DURATIONTIMEBANSWER, ASN_INTEGER, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 52}},
#define   DURATIONTIMENOBANSWER  67
	{(u_char) DURATIONTIMENOBANSWER, ASN_INTEGER, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 53}},
#define   UUINFO                68
	{(u_char) UUINFO, ASN_OCTET_STR, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 54}},
#define   STANDARDEXTENSIONS    69
	{(u_char) STANDARDEXTENSIONS, ASN_OCTET_STR, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 55}},
#define   RECORDEXTENSIONS      70
	{(u_char) RECORDEXTENSIONS, ASN_OCTET_STR, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 56}},
#define   BPARTYCATEGORY        71
	{(u_char) BPARTYCATEGORY, ASN_OCTET_STR, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 57}},
#define   ISUPPREFERRED         72
	{(u_char) ISUPPREFERRED, ASN_INTEGER, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 58}},
#define   NETWORKMANAGEMENTCONTROLS  73
	{(u_char) NETWORKMANAGEMENTCONTROLS, ASN_INTEGER, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 59}},
#define   GLARE                 74
	{(u_char) GLARE, ASN_INTEGER, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 60}},
#define   RECORDID              75
	{(u_char) RECORDID, ASN_INTEGER, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 61}},
#define   DATAVALIDITY          76
	{(u_char) DATAVALIDITY, ASN_INTEGER, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 62}},
#define   CALLSTATUS            77
	{(u_char) CALLSTATUS, ASN_INTEGER, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 63}},
#define   CARRIERID             78
	{(u_char) CARRIERID, ASN_OCTET_STR, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 64}},
#define   DPC                   79
	{(u_char) DPC, ASN_OCTET_STR, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 65}},
#define   OPC                   80
	{(u_char) OPC, ASN_OCTET_STR, RONLY, var_callDetailLogRecordTable, 5, {1, 7, 1, 1, 66}},
#define   CONFIGURATIONR2MASK   81
	{(u_char) CONFIGURATIONR2MASK, ASN_BIT_STR, RWRITE, var_configurableSimpleUsageMeteringControlR2Table, 5, {1, 8, 1, 1, 1}},
#define   CONFIGURATIONR2STATUS  82
	{(u_char) CONFIGURATIONR2STATUS, ASN_INTEGER, RWRITE, var_configurableSimpleUsageMeteringControlR2Table, 5, {1, 8, 1, 1, 2}},
#define   BLOCKGENERATINGLOGVERSION  83
	{(u_char) BLOCKGENERATINGLOGVERSION, ASN_INTEGER, RWRITE, var_blockGeneratingLogR2Table, 5, {1, 10, 1, 1, 1}},
#define   SEQUENCEID            84
	{(u_char) SEQUENCEID, ASN_COUNTER64, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 1}},
#define   ZONEID                85
	{(u_char) ZONEID, ASN_COUNTER64, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 2}},
#define   RECSEQID              86
	{(u_char) RECSEQID, ASN_COUNTER64, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 3}},
#define   FILESEQID             87
	{(u_char) FILESEQID, ASN_COUNTER64, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 4}},
#define   CALLID                88
	{(u_char) CALLID, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 5}},
#define   LASTUPDATE            89
	{(u_char) LASTUPDATE, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 6}},
#define   EVENTORDER            90
	{(u_char) EVENTORDER, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 7}},
#define   STATUS                91
	{(u_char) STATUS, ASN_INTEGER, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 8}},
#define   CALLTYPE              92
	{(u_char) CALLTYPE, ASN_INTEGER, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 9}},
#define   CLNGPTYNBR            93
	{(u_char) CLNGPTYNBR, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 10}},
#define   CHARGENBR             94
	{(u_char) CHARGENBR, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 11}},
#define   CLLDPTYNBR            95
	{(u_char) CLLDPTYNBR, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 12}},
#define   CLLDPTYNOA            96
	{(u_char) CLLDPTYNOA, ASN_INTEGER, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 13}},
#define   ORIGLNINFO            97
	{(u_char) ORIGLNINFO, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 14}},
#define   INGLRN                98
	{(u_char) INGLRN, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 15}},
#define   INGCIC                99
	{(u_char) INGCIC, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 16}},
#define   INGCSI                100
	{(u_char) INGCSI, ASN_INTEGER, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 17}},
#define   INGCCEID              101
	{(u_char) INGCCEID, ASN_UNSIGNED, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 18}},
#define   INGTGPROT             102
	{(u_char) INGTGPROT, ASN_INTEGER, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 19}},
#define   INGTGTYPE             103
	{(u_char) INGTGTYPE, ASN_INTEGER, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 20}},
#define   INGTGID               104
	{(u_char) INGTGID, ASN_UNSIGNED, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 21}},
#define   INGCALLSTART          105
	{(u_char) INGCALLSTART, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 22}},
#define   INGGATEID             106
	{(u_char) INGGATEID, ASN_UNSIGNED, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 23}},
#define   INGCARDID             107
	{(u_char) INGCARDID, ASN_UNSIGNED, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 24}},
#define   INGSPANID             108
	{(u_char) INGSPANID, ASN_UNSIGNED, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 25}},
#define   INGCHANID             109
	{(u_char) INGCHANID, ASN_UNSIGNED, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 26}},
#define   INGISDNDCHAN          110
	{(u_char) INGISDNDCHAN, ASN_UNSIGNED, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 27}},
#define   INGISDNCRN            111
	{(u_char) INGISDNCRN, ASN_UNSIGNED, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 28}},
#define   INGCREATCONNCPLT      112
	{(u_char) INGCREATCONNCPLT, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 29}},
#define   INGADDRCPLT           113
	{(u_char) INGADDRCPLT, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 30}},
#define   INGCALLANS            114
	{(u_char) INGCALLANS, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 31}},
#define   TRANSNBR              115
	{(u_char) TRANSNBR, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 32}},
#define   TERMLRN               116
	{(u_char) TERMLRN, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 33}},
#define   TRANSCIC              117
	{(u_char) TRANSCIC, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 34}},
#define   INGCALLRLS            118
	{(u_char) INGCALLRLS, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 35}},
#define   INGRLSCAUSE           119
	{(u_char) INGRLSCAUSE, ASN_UNSIGNED, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 36}},
#define   EGRCCEID              120
	{(u_char) EGRCCEID, ASN_UNSIGNED, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 37}},
#define   EGRTGPROT             121
	{(u_char) EGRTGPROT, ASN_INTEGER, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 38}},
#define   EGRTGTYPE             122
	{(u_char) EGRTGTYPE, ASN_INTEGER, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 39}},
#define   EGRTGID               123
	{(u_char) EGRTGID, ASN_UNSIGNED, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 40}},
#define   EGRCALLSTART          124
	{(u_char) EGRCALLSTART, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 41}},
#define   EGRGATEID             125
	{(u_char) EGRGATEID, ASN_UNSIGNED, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 42}},
#define   EGRCARDID             126
	{(u_char) EGRCARDID, ASN_UNSIGNED, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 43}},
#define   EGRSPANID             127
	{(u_char) EGRSPANID, ASN_UNSIGNED, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 44}},
#define   EGRCHANID             128
	{(u_char) EGRCHANID, ASN_UNSIGNED, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 45}},
#define   EGRISDNDCHAN          129
	{(u_char) EGRISDNDCHAN, ASN_UNSIGNED, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 46}},
#define   EGRISDNCRN            130
	{(u_char) EGRISDNCRN, ASN_UNSIGNED, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 47}},
#define   EGRCREATCONNCPLT      131
	{(u_char) EGRCREATCONNCPLT, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 48}},
#define   EGRADDRCPLT           132
	{(u_char) EGRADDRCPLT, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 49}},
#define   EGRCALLANS            133
	{(u_char) EGRCALLANS, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 50}},
#define   EGRCALLRLS            134
	{(u_char) EGRCALLRLS, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 51}},
#define   EGRRLSCAUSE           135
	{(u_char) EGRRLSCAUSE, ASN_UNSIGNED, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 52}},
#define   CHKPTFIRST            136
	{(u_char) CHKPTFIRST, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 53}},
#define   CHKPTLAST             137
	{(u_char) CHKPTLAST, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 54}},
#define   INGGATENAME           138
	{(u_char) INGGATENAME, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 55}},
#define   EGRGATENAME           139
	{(u_char) EGRGATENAME, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 56}},
#define   INGTGNAME             140
	{(u_char) INGTGNAME, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 57}},
#define   EGRTGNAME             141
	{(u_char) EGRTGNAME, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 58}},
#define   ORIGGATEIP            142
	{(u_char) ORIGGATEIP, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 59}},
#define   TERMGATEIP            143
	{(u_char) TERMGATEIP, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 60}},
#define   H323CONFID            144
	{(u_char) H323CONFID, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 61}},
#define   INGCARDPORT           145
	{(u_char) INGCARDPORT, ASN_UNSIGNED, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 62}},
#define   INGCARDPATH           146
	{(u_char) INGCARDPATH, ASN_UNSIGNED, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 63}},
#define   EGRCARDPORT           147
	{(u_char) EGRCARDPORT, ASN_UNSIGNED, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 64}},
#define   EGRCARDPATH           148
	{(u_char) EGRCARDPATH, ASN_UNSIGNED, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 65}},
#define   INGTG                 149
	{(u_char) INGTG, ASN_UNSIGNED, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 66}},
#define   EGRTG                 150
	{(u_char) EGRTG, ASN_UNSIGNED, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 67}},
#define   DIALEDNBR             151
	{(u_char) DIALEDNBR, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 68}},
#define   DIALEDNOA             152
	{(u_char) DIALEDNOA, ASN_INTEGER, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 69}},
#define   REDIRNBR              153
	{(u_char) REDIRNBR, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 70}},
#define   REDIRNOA              154
	{(u_char) REDIRNOA, ASN_INTEGER, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 71}},
#define   REDIRIND              155
	{(u_char) REDIRIND, ASN_INTEGER, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 72}},
#define   REDIRRSN              156
	{(u_char) REDIRRSN, ASN_INTEGER, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 73}},
#define   CALLEDNBR             157
	{(u_char) CALLEDNBR, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 74}},
#define   CALLEDNOA             158
	{(u_char) CALLEDNOA, ASN_INTEGER, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 75}},
#define   REDIRRSNORIG          159
	{(u_char) REDIRRSNORIG, ASN_INTEGER, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 76}},
#define   REDIRCNTR             160
	{(u_char) REDIRCNTR, ASN_UNSIGNED, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 77}},
#define   REDIRPRSNTNIND        161
	{(u_char) REDIRPRSNTNIND, ASN_INTEGER, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 78}},
#define   INGJURIS              162
	{(u_char) INGJURIS, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 79}},
#define   EGRJURIS              163
	{(u_char) EGRJURIS, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 80}},
#define   INGTRUNKBEARCAP       164
	{(u_char) INGTRUNKBEARCAP, ASN_INTEGER, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 81}},
#define   EGRTRUNKBEARCAP       165
	{(u_char) EGRTRUNKBEARCAP, ASN_INTEGER, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 82}},
#define   TRANSNETSELCARR       166
	{(u_char) TRANSNETSELCARR, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 83}},
#define   CLNGPTYNOA            167
	{(u_char) CLNGPTYNOA, ASN_UNSIGNED, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 84}},
#define   INGRLSCAUSEINTRNL     168
	{(u_char) INGRLSCAUSEINTRNL, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 85}},
#define   EGRRLSCAUSEINTRNL     169
	{(u_char) EGRRLSCAUSEINTRNL, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 86}},
#define   EGRCLLDNBR            170
	{(u_char) EGRCLLDNBR, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 87}},
#define   EGRCLLDNOA            171
	{(u_char) EGRCLLDNOA, ASN_INTEGER, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 88}},
#define   EGRCNNDNBR            172
	{(u_char) EGRCNNDNBR, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 89}},
#define   EGRCNNDNOA            173
	{(u_char) EGRCNNDNOA, ASN_INTEGER, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 90}},
#define   CLNGPTYPRSNTNIND      174
	{(u_char) CLNGPTYPRSNTNIND, ASN_INTEGER, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 91}},
#define   INGIRI                175
	{(u_char) INGIRI, ASN_INTEGER, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 92}},
#define   EGRORI                176
	{(u_char) EGRORI, ASN_INTEGER, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 93}},
#define   INGCALLIDEXTRNL       177
	{(u_char) INGCALLIDEXTRNL, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 94}},
#define   EGRCALLIDEXTRNL       178
	{(u_char) EGRCALLIDEXTRNL, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 95}},
#define   INGCHARGEINFO         179
	{(u_char) INGCHARGEINFO, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 96}},
#define   EGRCHARGEINFO         180
	{(u_char) EGRCHARGEINFO, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 97}},
#define   INGPRTLIND            181
	{(u_char) INGPRTLIND, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 98}},
#define   INGNATFWDCLIBLKIND    182
	{(u_char) INGNATFWDCLIBLKIND, ASN_INTEGER, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 99}},
#define   INGNATFWDNTAIND       183
	{(u_char) INGNATFWDNTAIND, ASN_INTEGER, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 100}},
#define   INGLASTDVRTLNDIGS     184
	{(u_char) INGLASTDVRTLNDIGS, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 101}},
#define   INGLASTDVRTLNNOA      185
	{(u_char) INGLASTDVRTLNNOA, ASN_INTEGER, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 102}},
#define   J7INGADDLPTYCAT       186
	{(u_char) J7INGADDLPTYCAT, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 103}},
#define   J7INGCHGAREAINFO      187
	{(u_char) J7INGCHGAREAINFO, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 104}},
#define   J7INGFWDCALLIND       188
	{(u_char) J7INGFWDCALLIND, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 105}},
#define   J7EGRADDLPTYCAT       189
	{(u_char) J7EGRADDLPTYCAT, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 106}},
#define   J7EGRCHGAREAINFO      190
	{(u_char) J7EGRCHGAREAINFO, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 107}},
#define   J7EGRBKWCALLIND       191
	{(u_char) J7EGRBKWCALLIND, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 108}},
#define   J7CARRINFOXFER        192
	{(u_char) J7CARRINFOXFER, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 109}},
#define   INGSS7GNRCPARM        193
	{(u_char) INGSS7GNRCPARM, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 110}},
#define   EGRSS7GNRCPARM        194
	{(u_char) EGRSS7GNRCPARM, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 111}},
#define   INGPKTSSENT           195
	{(u_char) INGPKTSSENT, ASN_UNSIGNED, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 112}},
#define   INGPKTSRCVD           196
	{(u_char) INGPKTSRCVD, ASN_UNSIGNED, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 113}},
#define   INGPKTSLOST           197
	{(u_char) INGPKTSLOST, ASN_UNSIGNED, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 114}},
#define   INGPKTSXFER           198
	{(u_char) INGPKTSXFER, ASN_UNSIGNED, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 115}},
#define   INGJITTER             199
	{(u_char) INGJITTER, ASN_UNSIGNED, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 116}},
#define   INGLTNCY              200
	{(u_char) INGLTNCY, ASN_UNSIGNED, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 117}},
#define   EGRPKTSSENT           201
	{(u_char) EGRPKTSSENT, ASN_UNSIGNED, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 118}},
#define   EGRPKTSRCVD           202
	{(u_char) EGRPKTSRCVD, ASN_UNSIGNED, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 119}},
#define   EGRPKTSLOST           203
	{(u_char) EGRPKTSLOST, ASN_UNSIGNED, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 120}},
#define   EGRPKTSXFER           204
	{(u_char) EGRPKTSXFER, ASN_UNSIGNED, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 121}},
#define   EGRJITTER             205
	{(u_char) EGRJITTER, ASN_UNSIGNED, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 122}},
#define   EGRLTNCY              206
	{(u_char) EGRLTNCY, ASN_UNSIGNED, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 123}},
#define   INGCRCTID             207
	{(u_char) INGCRCTID, ASN_UNSIGNED, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 124}},
#define   EGRCRCTID             208
	{(u_char) EGRCRCTID, ASN_UNSIGNED, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 125}},
#define   INGCODEC              209
	{(u_char) INGCODEC, ASN_UNSIGNED, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 126}},
#define   EGRCODEC              210
	{(u_char) EGRCODEC, ASN_UNSIGNED, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 127}},
#define   INGLOCGATEID          211
	{(u_char) INGLOCGATEID, ASN_UNSIGNED, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 128}},
#define   EGRLOCGATEID          212
	{(u_char) EGRLOCGATEID, ASN_UNSIGNED, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 129}},
#define   INGCASCRCTSZR         213
	{(u_char) INGCASCRCTSZR, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 130}},
#define   EGRCASCRCTSZR         214
	{(u_char) EGRCASCRCTSZR, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 131}},
#define   INGZZ                 215
	{(u_char) INGZZ, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 132}},
#define   EGRZZ                 216
	{(u_char) EGRZZ, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 133}},
#define   INGCTRYADDRTYPE       217
	{(u_char) INGCTRYADDRTYPE, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 134}},
#define   EGRCTRYADDRTYPE       218
	{(u_char) EGRCTRYADDRTYPE, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 135}},
#define   INGPARTITION          219
	{(u_char) INGPARTITION, ASN_UNSIGNED, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 136}},
#define   EGRPARTITION          220
	{(u_char) EGRPARTITION, ASN_UNSIGNED, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 137}},
#define   INGCLNGPTYCAT         221
	{(u_char) INGCLNGPTYCAT, ASN_UNSIGNED, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 138}},
#define   INGRLSCAUSELCTN       222
	{(u_char) INGRLSCAUSELCTN, ASN_UNSIGNED, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 139}},
#define   EGRRLSCAUSELCTN       223
	{(u_char) EGRRLSCAUSELCTN, ASN_UNSIGNED, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 140}},
#define   CLLDPTYCAT            224
	{(u_char) CLLDPTYCAT, ASN_UNSIGNED, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 141}},
#define   CALLDURATION          225
	{(u_char) CALLDURATION, ASN_UNSIGNED, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 142}},
#define   INGRLSDRCTN           226
	{(u_char) INGRLSDRCTN, ASN_UNSIGNED, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 143}},
#define   EGRRLSDRCTN           227
	{(u_char) EGRRLSDRCTN, ASN_UNSIGNED, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 144}},
#define   INGANSLCTIM           228
	{(u_char) INGANSLCTIM, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 145}},
#define   EGRANSLCTIM           229
	{(u_char) EGRANSLCTIM, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 146}},
#define   INGHLCCHRSID          230
	{(u_char) INGHLCCHRSID, ASN_UNSIGNED, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 147}},
#define   EGRHLCCHRSID          231
	{(u_char) EGRHLCCHRSID, ASN_UNSIGNED, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 148}},
#define   INGLLCXFERCAP         232
	{(u_char) INGLLCXFERCAP, ASN_UNSIGNED, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 149}},
#define   EGRLLCXFERCAP         233
	{(u_char) EGRLLCXFERCAP, ASN_UNSIGNED, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 150}},
#define   ROUTELIST             234
	{(u_char) ROUTELIST, ASN_OCTET_STR, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 151}},
#define   INGPOINTCODE          235
	{(u_char) INGPOINTCODE, ASN_UNSIGNED, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 152}},
#define   EGRPOINTCODE          236
	{(u_char) EGRPOINTCODE, ASN_UNSIGNED, RONLY, var_callDetailLogRecordR2Table, 5, {1, 11, 1, 1, 153}},
};

/* (L = length of the oidsuffix) */
struct cdr2MIB_data *cdr2MIBStorage = NULL;

/* global storage of our data, saved in and configured by header_complex() */
struct header_complex_index *callDetailDataTableStorage = NULL;
struct header_complex_index *simpleUsageMeteringControlTableStorage = NULL;
struct header_complex_index *configurableSimpleUsageMeteringControlTableStorage = NULL;
struct header_complex_index *blockGeneratingLogTableStorage = NULL;
struct header_complex_index *fileGeneratingLogTableStorage = NULL;
struct header_complex_index *callDetailLogRecordTableStorage = NULL;
struct header_complex_index *configurableSimpleUsageMeteringControlR2TableStorage = NULL;
struct header_complex_index *blockGeneratingLogR2TableStorage = NULL;
struct header_complex_index *callDetailLogRecordR2TableStorage = NULL;

#if defined MODULE
void (*cdr2MIBold_signal_handler) (int) = NULL;	/* save old signal handler just in case */
void cdr2MIB_loop_handler(int);
void cdr2MIB_fd_handler(int, void *);
#endif				/* defined MOUDLE */
/**
 * @fn void init_cdr2MIB(void)
 * @brief cdr2MIB initialization routine.
 *
 * This is called when the agent starts up.  At a minimum, registration of the MIB variables
 * structure (cdr2MIB_variables) should take place here.  By default the function also
 * registers the configuration handler and configuration store callbacks.
 *
 * Additional registrations that may be considered here are calls to regsiter_readfd(),
 * register_writefd() and register_exceptfd() for hooking into the snmpd event loop, but only when
 * used as a loadable module.  By default this function establishes a single file descriptor to
 * read, or upon which to handle exceptions.  Note that the snmpd only supports a maximum of 32
 * extneral file descriptors, so these should be used sparingly.
 *
 * When running as a loadable module, it is also necessary to hook into the snmpd event loop so that
 * the current request number can be deteremined.  This is accomplished by using a trick of the
 * external_signal_scheduled and external_signal_handler mechanism which is called on each event
 * loop when external_signal_scheduled is non-zero.  This is used to increment the sa_request value
 * on each snmpd event loop interation so that calls to MIB tree functions can determine whether
 * they belong to a fresh request or not (primarily for cacheing and possibly to clean up non-polled
 * file descriptors).
 */
void
init_cdr2MIB(void)
{
	(void) my_fd;
	(void) snmpTrapOID_oid;
	DEBUGMSGTL(("cdr2MIB", "initializing...  "));
	/* register ourselves with the agent to handle our mib tree */
	REGISTER_MIB("cdr2MIB", cdr2MIB_variables, variable7, cdr2MIB_variables_oid);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_SHUTDOWN, term_cdr2MIB, NULL);
	/* register our config handler(s) to deal with registrations */
	snmpd_register_config_handler("cdr2MIB", parse_cdr2MIB, NULL, "HELP STRING");
	snmpd_register_config_handler("callDetailDataTable", parse_callDetailDataTable, NULL, "HELP STRING");
	snmpd_register_config_handler("simpleUsageMeteringControlTable", parse_simpleUsageMeteringControlTable, NULL, "HELP STRING");
	snmpd_register_config_handler("configurableSimpleUsageMeteringControlTable", parse_configurableSimpleUsageMeteringControlTable, NULL, "HELP STRING");
	snmpd_register_config_handler("blockGeneratingLogTable", parse_blockGeneratingLogTable, NULL, "HELP STRING");
	snmpd_register_config_handler("fileGeneratingLogTable", parse_fileGeneratingLogTable, NULL, "HELP STRING");
	snmpd_register_config_handler("callDetailLogRecordTable", parse_callDetailLogRecordTable, NULL, "HELP STRING");
	snmpd_register_config_handler("configurableSimpleUsageMeteringControlR2Table", parse_configurableSimpleUsageMeteringControlR2Table, NULL, "HELP STRING");
	snmpd_register_config_handler("blockGeneratingLogR2Table", parse_blockGeneratingLogR2Table, NULL, "HELP STRING");
	snmpd_register_config_handler("callDetailLogRecordR2Table", parse_callDetailLogRecordR2Table, NULL, "HELP STRING");

	/* we need to be called back later to store our data */
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_cdr2MIB, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_callDetailDataTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_simpleUsageMeteringControlTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_configurableSimpleUsageMeteringControlTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_blockGeneratingLogTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_fileGeneratingLogTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_callDetailLogRecordTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_configurableSimpleUsageMeteringControlR2Table, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_blockGeneratingLogR2Table, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_callDetailLogRecordR2Table, NULL);

	/* place any other initialization junk you need here */
#if defined MODULE
	if (sa_readfd != 0) {
		register_readfd(sa_readfd, cdr2MIB_fd_handler, (void *) 0);
		register_exceptfd(sa_readfd, cdr2MIB_fd_handler, (void *) 1);
	}
#if defined MASTER
	cdr2MIBold_signal_handler = external_signal_handler[0];
	external_signal_handler[0] = &cdr2MIB_loop_handler;
#endif				/* defined MASTER */
#endif				/* defined MODULE */
	DEBUGMSGTL(("cdr2MIB", "done.\n"));
}

/**
 * @fn void deinit_cdr2MIB(void)
 * @brief deinitialization routine.
 *
 * This is called before the agent is unloaded.  At a minimum, deregistration of the MIB variables
 * structure (cdr2MIB_variables) should take place here.  By default, the function also
 * deregisters the the configuration file handlers for the MIB variables and table rows.
 *
 * Additional deregistrations that may be required here are calls to unregister_readfd(),
 * unregister_writefd() and unregsiter_exceptfd() for unhooking from the snmpd event loop, but only
 * when used as a loadable module.  By default if a read file descriptor exists, it is unregistered.
 */
void
deinit_cdr2MIB(void)
{
	DEBUGMSGTL(("cdr2MIB", "deinitializating...  "));
#if defined MODULE
#if defined MASTER
	external_signal_handler[0] = cdr2MIBold_signal_handler;
#endif				/* defined MASTER */
	if (sa_readfd != 0) {
		unregister_exceptfd(sa_readfd);
		unregister_readfd(sa_readfd);
		close(sa_readfd);
		sa_readfd = 0;
	}
#endif				/* defined MODULE */
	unregister_mib(cdr2MIB_variables_oid, sizeof(cdr2MIB_variables_oid) / sizeof(oid));
	snmpd_unregister_config_handler("cdr2MIB");
	snmpd_unregister_config_handler("callDetailDataTable");
	snmpd_unregister_config_handler("simpleUsageMeteringControlTable");
	snmpd_unregister_config_handler("configurableSimpleUsageMeteringControlTable");
	snmpd_unregister_config_handler("blockGeneratingLogTable");
	snmpd_unregister_config_handler("fileGeneratingLogTable");
	snmpd_unregister_config_handler("callDetailLogRecordTable");
	snmpd_unregister_config_handler("configurableSimpleUsageMeteringControlR2Table");
	snmpd_unregister_config_handler("blockGeneratingLogR2Table");
	snmpd_unregister_config_handler("callDetailLogRecordR2Table");

	/* place any other de-initialization junk you need here */
	DEBUGMSGTL(("cdr2MIB", "done.\n"));
}

int
term_cdr2MIB(int majorID, int minorID, void *serverarg, void *clientarg)
{
	deinit_cdr2MIB();
	return 0;
}

/**
 * @fn struct cdr2MIB_data *cdr2MIB_create(void)
 * @brief create a fresh data structure representing scalars in cdr2MIB.
 *
 * Creates a new cdr2MIB_data structure by allocating dynamic memory for the structure and
 * initializing the default values of scalars in cdr2MIB.
 */
struct cdr2MIB_data *
cdr2MIB_create(void)
{
	struct cdr2MIB_data *StorageNew = SNMP_MALLOC_STRUCT(cdr2MIB_data);

	DEBUGMSGTL(("cdr2MIB", "creating scalars...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default scalar values here into StorageNew */

	}
	DEBUGMSGTL(("cdr2MIB", "done.\n"));
	return (StorageNew);
}

/**
 * @fn int cdr2MIB_destroy(struct cdr2MIB_data **thedata)
 * @param thedata pointer to the data structure in cdr2MIB.
 * @brief delete a scalars structure from cdr2MIB.
 *
 * Frees scalars that were previously removed from cdr2MIB.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
cdr2MIB_destroy(struct cdr2MIB_data **thedata)
{
	struct cdr2MIB_data *StorageDel;

	DEBUGMSGTL(("cdr2MIB", "deleting scalars...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("cdr2MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int cdr2MIB_add(struct cdr2MIB_data *thedata)
 * @param thedata the structure representing cdr2MIB scalars.
 * @brief adds node to the cdr2MIB scalar data set.
 *
 * Adds a scalar structure to the cdr2MIB data set.  Note that this function is necessary even
 * when the scalar values are not peristent.
 */
int
cdr2MIB_add(struct cdr2MIB_data *thedata)
{
	DEBUGMSGTL(("cdr2MIB", "adding data...  "));
	cdr2MIBStorage = thedata;
	DEBUGMSGTL(("cdr2MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_cdr2MIB(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for cdr2MIB entries.
 *
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case cdr2MIB).  This routine is invoked by
 * UCD-SNMP to read the values of scalars in the MIB from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the MIB.  If there are no configured entries
 * in the configuration MIB, this function will simply not be called.
 */
void
parse_cdr2MIB(const char *token, char *line)
{
	size_t tmpsize;
	struct cdr2MIB_data *StorageTmp = cdr2MIB_create();

	DEBUGMSGTL(("cdr2MIB", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual scalars that are not persistent */
	cdr2MIB_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("cdr2MIB", "done.\n"));
}

/*
 * store_cdr2MIB(): stores .conf file entries needed to configure the mib.
 */
int
store_cdr2MIB(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct cdr2MIB_data *StorageTmp;

	DEBUGMSGTL(("cdr2MIB", "storing data...  "));
	refresh_cdr2MIB(1);
	if ((StorageTmp = cdr2MIBStorage) == NULL) {
		DEBUGMSGTL(("cdr2MIB", "error.\n"));
		return SNMPERR_GENERR;
	}
	(void) tmpsize;
	/* XXX: comment entire section if no scalars are persistent */
	{
		memset(line, 0, sizeof(line));
		strcat(line, "cdr2MIB ");
		cptr = line + strlen(line);
		/* XXX: remove individual scalars that are not persistent */
		snmpd_store_config(line);
	}
	DEBUGMSGTL(("cdr2MIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void refresh_cdr2MIB(int force)
 * @param force forced refresh when non-zero.
 * @brief refresh the scalar values of cdr2MIB.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a scalar has been requested).
 */
void
refresh_cdr2MIB(int force)
{
	if (cdr2MIBStorage == NULL) {
		struct cdr2MIB_data *StorageNew;

		if ((StorageNew = cdr2MIB_create()) == NULL)
			return;
		cdr2MIBStorage = StorageNew;
		cdr2MIB_refresh = 1;
	}
	if (!force && cdr2MIB_refresh == 0)
		return;
	cdr2MIB_refresh = 0;
	/* XXX: Update scalars as required here... */
}

/**
 * @fn u_char * var_cdr2MIB(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @param vp a pointer to the entry in the variables table for the requested variable.
 * @param name the object identifier for which to find.
 * @param length the length of the object identifier.
 * @param exact whether the name is exact.
 * @param var_len a pointer to the length of the representation of the object.
 * @param write_method a pointer to a write method for the object.
 * @brief locate variables in cdr2MIB.
 *
 * This function returns a pointer to a memory area that is static across the request that contains
 * the UCD-SNMP representation of the scalar (so that it may be used to read from for a GET,
 * GET-NEXT or GET-BULK request).  This returned pointer may be NULL, in which case the function is
 * telling UCD-SNMP that the scalar does not exist for reading; however, if write_method is
 * overwritten with a non-NULL value, the function is telling UCD-SNMP that the scalar exists for
 * writing.  Write-only objects can be effected in this way.
 */
u_char *
var_cdr2MIB(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct cdr2MIB_data *StorageTmp;
	u_char *rval;

	if (header_generic(vp, name, length, exact, var_len, write_method) == MATCH_FAILED)
		return NULL;
	/* Refresh the MIB values if required. */
	refresh_cdr2MIB(0);
	if ((StorageTmp = cdr2MIBStorage) == NULL)
		return NULL;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn struct callDetailDataTable_data *callDetailDataTable_create(void)
 * @brief create a fresh data structure representing a new row in the callDetailDataTable table.
 *
 * Creates a new callDetailDataTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct callDetailDataTable_data *
callDetailDataTable_create(void)
{
	struct callDetailDataTable_data *StorageNew = SNMP_MALLOC_STRUCT(callDetailDataTable_data);

	DEBUGMSGTL(("callDetailDataTable", "creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */

	}
	DEBUGMSGTL(("callDetailDataTable", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct callDetailDataTable_data *callDetailDataTable_duplicate(struct callDetailDataTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicat a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct callDetailDataTable_data *
callDetailDataTable_duplicate(struct callDetailDataTable_data *thedata)
{
	struct callDetailDataTable_data *StorageNew = SNMP_MALLOC_STRUCT(callDetailDataTable_data);

	DEBUGMSGTL(("callDetailDataTable", "duplicating row...  "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("callDetailDataTable", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	callDetailDataTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int callDetailDataTable_destroy(struct callDetailDataTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
callDetailDataTable_destroy(struct callDetailDataTable_data **thedata)
{
	struct callDetailDataTable_data *StorageDel;

	DEBUGMSGTL(("callDetailDataTable", "deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->callDetailDataId);
		StorageDel->callDetailDataIdLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("callDetailDataTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int callDetailDataTable_add(struct callDetailDataTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the callDetailDataTable table data set.
 *
 * Adds a table row structure to the callDetailDataTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
callDetailDataTable_add(struct callDetailDataTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("callDetailDataTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index the data */
	/* callDetailDataId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->callDetailDataId, thedata->callDetailDataIdLen);
	header_complex_add_data(&callDetailDataTableStorage, vars, thedata);
	DEBUGMSGTL(("callDetailDataTable", "registered an entry\n"));
	DEBUGMSGTL(("callDetailDataTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int callDetailDataTable_del(struct callDetailDataTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the callDetailDataTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
callDetailDataTable_del(struct callDetailDataTable_data *thedata)
{
	struct callDetailDataTable_data *StorageDel;

	DEBUGMSGTL(("callDetailDataTable", "deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(callDetailDataTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&callDetailDataTableStorage, hciptr);
	}
	DEBUGMSGTL(("callDetailDataTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_callDetailDataTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for callDetailDataTable entries.
 *
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case callDetailDataTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_callDetailDataTable(const char *token, char *line)
{
	size_t tmpsize;
	struct callDetailDataTable_data *StorageTmp = callDetailDataTable_create();

	DEBUGMSGTL(("callDetailDataTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->callDetailDataId, &StorageTmp->callDetailDataIdLen);
	if (StorageTmp->callDetailDataId == NULL) {
		config_perror("invalid specification for callDetailDataId");
		return;
	}
	callDetailDataTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("callDetailDataTable", "done.\n"));
}

/*
 * store_callDetailDataTable(): store configuraiton file for callDetailDataTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_callDetailDataTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct callDetailDataTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("callDetailDataTable", "storing data...  "));
	refresh_callDetailDataTable(1);
	(void) tmpsize;
	for (hcindex = callDetailDataTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct callDetailDataTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "callDetailDataTable ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->callDetailDataId, &StorageTmp->callDetailDataIdLen);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("callDetailDataTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct simpleUsageMeteringControlTable_data *simpleUsageMeteringControlTable_create(void)
 * @brief create a fresh data structure representing a new row in the simpleUsageMeteringControlTable table.
 *
 * Creates a new simpleUsageMeteringControlTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct simpleUsageMeteringControlTable_data *
simpleUsageMeteringControlTable_create(void)
{
	struct simpleUsageMeteringControlTable_data *StorageNew = SNMP_MALLOC_STRUCT(simpleUsageMeteringControlTable_data);

	DEBUGMSGTL(("simpleUsageMeteringControlTable", "creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->controlObjectId = (uint8_t *) strdup("")) != NULL) {
			StorageNew->controlObjectIdLen = strlen("");
		}
		if (memdup((u_char **) &StorageNew->creationTriggerList, (u_char *) "\x00", 1) == SNMPERR_SUCCESS) {
			StorageNew->creationTriggerListLen = 1;
		}
		StorageNew->simpleUsageMeteringControlEntryStatus = 0;
		StorageNew->simpleUsageMeteringControlEntryStatus = RS_NOTREADY;
	}
	DEBUGMSGTL(("simpleUsageMeteringControlTable", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct simpleUsageMeteringControlTable_data *simpleUsageMeteringControlTable_duplicate(struct simpleUsageMeteringControlTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicat a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct simpleUsageMeteringControlTable_data *
simpleUsageMeteringControlTable_duplicate(struct simpleUsageMeteringControlTable_data *thedata)
{
	struct simpleUsageMeteringControlTable_data *StorageNew = SNMP_MALLOC_STRUCT(simpleUsageMeteringControlTable_data);

	DEBUGMSGTL(("simpleUsageMeteringControlTable", "duplicating row...  "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("simpleUsageMeteringControlTable", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	simpleUsageMeteringControlTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int simpleUsageMeteringControlTable_destroy(struct simpleUsageMeteringControlTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
simpleUsageMeteringControlTable_destroy(struct simpleUsageMeteringControlTable_data **thedata)
{
	struct simpleUsageMeteringControlTable_data *StorageDel;

	DEBUGMSGTL(("simpleUsageMeteringControlTable", "deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->controlObjectId);
		StorageDel->controlObjectIdLen = 0;
		SNMP_FREE(StorageDel->creationTriggerList);
		StorageDel->creationTriggerListLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("simpleUsageMeteringControlTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int simpleUsageMeteringControlTable_add(struct simpleUsageMeteringControlTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the simpleUsageMeteringControlTable table data set.
 *
 * Adds a table row structure to the simpleUsageMeteringControlTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
simpleUsageMeteringControlTable_add(struct simpleUsageMeteringControlTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("simpleUsageMeteringControlTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index the data */
	/* controlObjectId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->controlObjectId, thedata->controlObjectIdLen);
	header_complex_add_data(&simpleUsageMeteringControlTableStorage, vars, thedata);
	DEBUGMSGTL(("simpleUsageMeteringControlTable", "registered an entry\n"));
	DEBUGMSGTL(("simpleUsageMeteringControlTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int simpleUsageMeteringControlTable_del(struct simpleUsageMeteringControlTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the simpleUsageMeteringControlTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
simpleUsageMeteringControlTable_del(struct simpleUsageMeteringControlTable_data *thedata)
{
	struct simpleUsageMeteringControlTable_data *StorageDel;

	DEBUGMSGTL(("simpleUsageMeteringControlTable", "deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(simpleUsageMeteringControlTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&simpleUsageMeteringControlTableStorage, hciptr);
	}
	DEBUGMSGTL(("simpleUsageMeteringControlTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_simpleUsageMeteringControlTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for simpleUsageMeteringControlTable entries.
 *
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case simpleUsageMeteringControlTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_simpleUsageMeteringControlTable(const char *token, char *line)
{
	size_t tmpsize;
	struct simpleUsageMeteringControlTable_data *StorageTmp = simpleUsageMeteringControlTable_create();

	DEBUGMSGTL(("simpleUsageMeteringControlTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->controlObjectId, &StorageTmp->controlObjectIdLen);
	if (StorageTmp->controlObjectId == NULL) {
		config_perror("invalid specification for controlObjectId");
		return;
	}
	line = read_config_read_data(ASN_BIT_STR, line, &StorageTmp->creationTriggerList, &StorageTmp->creationTriggerListLen);
	if (StorageTmp->creationTriggerList == NULL) {
		config_perror("invalid specification for creationTriggerList");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->simpleUsageMeteringControlEntryStatus, &tmpsize);
	simpleUsageMeteringControlTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("simpleUsageMeteringControlTable", "done.\n"));
}

/*
 * store_simpleUsageMeteringControlTable(): store configuraiton file for simpleUsageMeteringControlTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_simpleUsageMeteringControlTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct simpleUsageMeteringControlTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("simpleUsageMeteringControlTable", "storing data...  "));
	refresh_simpleUsageMeteringControlTable(1);
	(void) tmpsize;
	for (hcindex = simpleUsageMeteringControlTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct simpleUsageMeteringControlTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "simpleUsageMeteringControlTable ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->controlObjectId, &StorageTmp->controlObjectIdLen);
			cptr = read_config_store_data(ASN_BIT_STR, cptr, &StorageTmp->creationTriggerList, &StorageTmp->creationTriggerListLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->simpleUsageMeteringControlEntryStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("simpleUsageMeteringControlTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct configurableSimpleUsageMeteringControlTable_data *configurableSimpleUsageMeteringControlTable_create(void)
 * @brief create a fresh data structure representing a new row in the configurableSimpleUsageMeteringControlTable table.
 *
 * Creates a new configurableSimpleUsageMeteringControlTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct configurableSimpleUsageMeteringControlTable_data *
configurableSimpleUsageMeteringControlTable_create(void)
{
	struct configurableSimpleUsageMeteringControlTable_data *StorageNew = SNMP_MALLOC_STRUCT(configurableSimpleUsageMeteringControlTable_data);

	DEBUGMSGTL(("configurableSimpleUsageMeteringControlTable", "creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->controlObjectId = (uint8_t *) strdup("")) != NULL) {
			StorageNew->controlObjectIdLen = strlen("");
		}
		StorageNew->samplingRate = 0;
		if (memdup((u_char **) &StorageNew->configurationMask, (u_char *) "\x00\x00", 2) == SNMPERR_SUCCESS) {
			StorageNew->configurationMaskLen = 2;
		}
		StorageNew->configurationRowStatus = 0;
		StorageNew->configurationRowStatus = RS_NOTREADY;
	}
	DEBUGMSGTL(("configurableSimpleUsageMeteringControlTable", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct configurableSimpleUsageMeteringControlTable_data *configurableSimpleUsageMeteringControlTable_duplicate(struct configurableSimpleUsageMeteringControlTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicat a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct configurableSimpleUsageMeteringControlTable_data *
configurableSimpleUsageMeteringControlTable_duplicate(struct configurableSimpleUsageMeteringControlTable_data *thedata)
{
	struct configurableSimpleUsageMeteringControlTable_data *StorageNew = SNMP_MALLOC_STRUCT(configurableSimpleUsageMeteringControlTable_data);

	DEBUGMSGTL(("configurableSimpleUsageMeteringControlTable", "duplicating row...  "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("configurableSimpleUsageMeteringControlTable", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	configurableSimpleUsageMeteringControlTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int configurableSimpleUsageMeteringControlTable_destroy(struct configurableSimpleUsageMeteringControlTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
configurableSimpleUsageMeteringControlTable_destroy(struct configurableSimpleUsageMeteringControlTable_data **thedata)
{
	struct configurableSimpleUsageMeteringControlTable_data *StorageDel;

	DEBUGMSGTL(("configurableSimpleUsageMeteringControlTable", "deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->controlObjectId);
		StorageDel->controlObjectIdLen = 0;
		SNMP_FREE(StorageDel->configurationMask);
		StorageDel->configurationMaskLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("configurableSimpleUsageMeteringControlTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int configurableSimpleUsageMeteringControlTable_add(struct configurableSimpleUsageMeteringControlTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the configurableSimpleUsageMeteringControlTable table data set.
 *
 * Adds a table row structure to the configurableSimpleUsageMeteringControlTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
configurableSimpleUsageMeteringControlTable_add(struct configurableSimpleUsageMeteringControlTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("configurableSimpleUsageMeteringControlTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index the data */
	/* controlObjectId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->controlObjectId, thedata->controlObjectIdLen);
	header_complex_add_data(&configurableSimpleUsageMeteringControlTableStorage, vars, thedata);
	DEBUGMSGTL(("configurableSimpleUsageMeteringControlTable", "registered an entry\n"));
	DEBUGMSGTL(("configurableSimpleUsageMeteringControlTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int configurableSimpleUsageMeteringControlTable_del(struct configurableSimpleUsageMeteringControlTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the configurableSimpleUsageMeteringControlTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
configurableSimpleUsageMeteringControlTable_del(struct configurableSimpleUsageMeteringControlTable_data *thedata)
{
	struct configurableSimpleUsageMeteringControlTable_data *StorageDel;

	DEBUGMSGTL(("configurableSimpleUsageMeteringControlTable", "deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(configurableSimpleUsageMeteringControlTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&configurableSimpleUsageMeteringControlTableStorage, hciptr);
	}
	DEBUGMSGTL(("configurableSimpleUsageMeteringControlTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_configurableSimpleUsageMeteringControlTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for configurableSimpleUsageMeteringControlTable entries.
 *
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case configurableSimpleUsageMeteringControlTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_configurableSimpleUsageMeteringControlTable(const char *token, char *line)
{
	size_t tmpsize;
	struct configurableSimpleUsageMeteringControlTable_data *StorageTmp = configurableSimpleUsageMeteringControlTable_create();

	DEBUGMSGTL(("configurableSimpleUsageMeteringControlTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->controlObjectId, &StorageTmp->controlObjectIdLen);
	if (StorageTmp->controlObjectId == NULL) {
		config_perror("invalid specification for controlObjectId");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->samplingRate, &tmpsize);
	line = read_config_read_data(ASN_BIT_STR, line, &StorageTmp->configurationMask, &StorageTmp->configurationMaskLen);
	if (StorageTmp->configurationMask == NULL) {
		config_perror("invalid specification for configurationMask");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->configurationRowStatus, &tmpsize);
	configurableSimpleUsageMeteringControlTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("configurableSimpleUsageMeteringControlTable", "done.\n"));
}

/*
 * store_configurableSimpleUsageMeteringControlTable(): store configuraiton file for configurableSimpleUsageMeteringControlTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_configurableSimpleUsageMeteringControlTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct configurableSimpleUsageMeteringControlTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("configurableSimpleUsageMeteringControlTable", "storing data...  "));
	refresh_configurableSimpleUsageMeteringControlTable(1);
	(void) tmpsize;
	for (hcindex = configurableSimpleUsageMeteringControlTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct configurableSimpleUsageMeteringControlTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "configurableSimpleUsageMeteringControlTable ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->controlObjectId, &StorageTmp->controlObjectIdLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->samplingRate, &tmpsize);
			cptr = read_config_store_data(ASN_BIT_STR, cptr, &StorageTmp->configurationMask, &StorageTmp->configurationMaskLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->configurationRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("configurableSimpleUsageMeteringControlTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct blockGeneratingLogTable_data *blockGeneratingLogTable_create(void)
 * @brief create a fresh data structure representing a new row in the blockGeneratingLogTable table.
 *
 * Creates a new blockGeneratingLogTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct blockGeneratingLogTable_data *
blockGeneratingLogTable_create(void)
{
	struct blockGeneratingLogTable_data *StorageNew = SNMP_MALLOC_STRUCT(blockGeneratingLogTable_data);

	DEBUGMSGTL(("blockGeneratingLogTable", "creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->logId = (uint8_t *) strdup("")) != NULL) {
			StorageNew->logIdLen = strlen("");
		}
		StorageNew->blockGeneratingLogMaxBlockSize = 0;
		StorageNew->blockGeneratingLogMaxTimeInterval = 0;
		StorageNew->blockGeneratingLogStorageType = 0;
		StorageNew->blockGeneratingLogRowStatus = 0;
		StorageNew->blockGeneratingLogRowStatus = RS_NOTREADY;
	}
	DEBUGMSGTL(("blockGeneratingLogTable", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct blockGeneratingLogTable_data *blockGeneratingLogTable_duplicate(struct blockGeneratingLogTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicat a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct blockGeneratingLogTable_data *
blockGeneratingLogTable_duplicate(struct blockGeneratingLogTable_data *thedata)
{
	struct blockGeneratingLogTable_data *StorageNew = SNMP_MALLOC_STRUCT(blockGeneratingLogTable_data);

	DEBUGMSGTL(("blockGeneratingLogTable", "duplicating row...  "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("blockGeneratingLogTable", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	blockGeneratingLogTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int blockGeneratingLogTable_destroy(struct blockGeneratingLogTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
blockGeneratingLogTable_destroy(struct blockGeneratingLogTable_data **thedata)
{
	struct blockGeneratingLogTable_data *StorageDel;

	DEBUGMSGTL(("blockGeneratingLogTable", "deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->logId);
		StorageDel->logIdLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("blockGeneratingLogTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int blockGeneratingLogTable_add(struct blockGeneratingLogTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the blockGeneratingLogTable table data set.
 *
 * Adds a table row structure to the blockGeneratingLogTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
blockGeneratingLogTable_add(struct blockGeneratingLogTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("blockGeneratingLogTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index the data */
	/* logId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->logId, thedata->logIdLen);
	header_complex_add_data(&blockGeneratingLogTableStorage, vars, thedata);
	DEBUGMSGTL(("blockGeneratingLogTable", "registered an entry\n"));
	DEBUGMSGTL(("blockGeneratingLogTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int blockGeneratingLogTable_del(struct blockGeneratingLogTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the blockGeneratingLogTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
blockGeneratingLogTable_del(struct blockGeneratingLogTable_data *thedata)
{
	struct blockGeneratingLogTable_data *StorageDel;

	DEBUGMSGTL(("blockGeneratingLogTable", "deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(blockGeneratingLogTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&blockGeneratingLogTableStorage, hciptr);
	}
	DEBUGMSGTL(("blockGeneratingLogTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_blockGeneratingLogTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for blockGeneratingLogTable entries.
 *
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case blockGeneratingLogTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_blockGeneratingLogTable(const char *token, char *line)
{
	size_t tmpsize;
	struct blockGeneratingLogTable_data *StorageTmp = blockGeneratingLogTable_create();

	DEBUGMSGTL(("blockGeneratingLogTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->logId, &StorageTmp->logIdLen);
	if (StorageTmp->logId == NULL) {
		config_perror("invalid specification for logId");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->blockGeneratingLogMaxBlockSize, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->blockGeneratingLogMaxTimeInterval, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->blockGeneratingLogStorageType, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->blockGeneratingLogRowStatus, &tmpsize);
	blockGeneratingLogTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("blockGeneratingLogTable", "done.\n"));
}

/*
 * store_blockGeneratingLogTable(): store configuraiton file for blockGeneratingLogTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_blockGeneratingLogTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct blockGeneratingLogTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("blockGeneratingLogTable", "storing data...  "));
	refresh_blockGeneratingLogTable(1);
	(void) tmpsize;
	for (hcindex = blockGeneratingLogTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct blockGeneratingLogTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		if (StorageTmp->blockGeneratingLogStorageType == ST_NONVOLATILE) {
			memset(line, 0, sizeof(line));
			strcat(line, "blockGeneratingLogTable ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->logId, &StorageTmp->logIdLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->blockGeneratingLogMaxBlockSize, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->blockGeneratingLogMaxTimeInterval, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->blockGeneratingLogStorageType, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->blockGeneratingLogRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("blockGeneratingLogTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct fileGeneratingLogTable_data *fileGeneratingLogTable_create(void)
 * @brief create a fresh data structure representing a new row in the fileGeneratingLogTable table.
 *
 * Creates a new fileGeneratingLogTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct fileGeneratingLogTable_data *
fileGeneratingLogTable_create(void)
{
	struct fileGeneratingLogTable_data *StorageNew = SNMP_MALLOC_STRUCT(fileGeneratingLogTable_data);

	DEBUGMSGTL(("fileGeneratingLogTable", "creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->logId = (uint8_t *) strdup("")) != NULL) {
			StorageNew->logIdLen = strlen("");
		}
		if ((StorageNew->timesOfDay = (uint8_t *) strdup("")) != NULL) {
			StorageNew->timesOfDayLen = strlen("");
		}
		StorageNew->periodicTrigger = 0;
		StorageNew->fileGeneratingLogStorageType = ST_NONVOLATILE;
		StorageNew->fileGeneratingLogEntryStatus = 0;
		StorageNew->fileGeneratingLogEntryStatus = RS_NOTREADY;
	}
	DEBUGMSGTL(("fileGeneratingLogTable", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct fileGeneratingLogTable_data *fileGeneratingLogTable_duplicate(struct fileGeneratingLogTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicat a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct fileGeneratingLogTable_data *
fileGeneratingLogTable_duplicate(struct fileGeneratingLogTable_data *thedata)
{
	struct fileGeneratingLogTable_data *StorageNew = SNMP_MALLOC_STRUCT(fileGeneratingLogTable_data);

	DEBUGMSGTL(("fileGeneratingLogTable", "duplicating row...  "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("fileGeneratingLogTable", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	fileGeneratingLogTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int fileGeneratingLogTable_destroy(struct fileGeneratingLogTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
fileGeneratingLogTable_destroy(struct fileGeneratingLogTable_data **thedata)
{
	struct fileGeneratingLogTable_data *StorageDel;

	DEBUGMSGTL(("fileGeneratingLogTable", "deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->logId);
		StorageDel->logIdLen = 0;
		SNMP_FREE(StorageDel->timesOfDay);
		StorageDel->timesOfDayLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("fileGeneratingLogTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int fileGeneratingLogTable_add(struct fileGeneratingLogTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the fileGeneratingLogTable table data set.
 *
 * Adds a table row structure to the fileGeneratingLogTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
fileGeneratingLogTable_add(struct fileGeneratingLogTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("fileGeneratingLogTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index the data */
	/* logId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->logId, thedata->logIdLen);
	header_complex_add_data(&fileGeneratingLogTableStorage, vars, thedata);
	DEBUGMSGTL(("fileGeneratingLogTable", "registered an entry\n"));
	DEBUGMSGTL(("fileGeneratingLogTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int fileGeneratingLogTable_del(struct fileGeneratingLogTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the fileGeneratingLogTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
fileGeneratingLogTable_del(struct fileGeneratingLogTable_data *thedata)
{
	struct fileGeneratingLogTable_data *StorageDel;

	DEBUGMSGTL(("fileGeneratingLogTable", "deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(fileGeneratingLogTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&fileGeneratingLogTableStorage, hciptr);
	}
	DEBUGMSGTL(("fileGeneratingLogTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_fileGeneratingLogTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for fileGeneratingLogTable entries.
 *
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case fileGeneratingLogTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_fileGeneratingLogTable(const char *token, char *line)
{
	size_t tmpsize;
	struct fileGeneratingLogTable_data *StorageTmp = fileGeneratingLogTable_create();

	DEBUGMSGTL(("fileGeneratingLogTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->logId, &StorageTmp->logIdLen);
	if (StorageTmp->logId == NULL) {
		config_perror("invalid specification for logId");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->timesOfDay, &StorageTmp->timesOfDayLen);
	if (StorageTmp->timesOfDay == NULL) {
		config_perror("invalid specification for timesOfDay");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->periodicTrigger, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->fileGeneratingLogStorageType, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->fileGeneratingLogEntryStatus, &tmpsize);
	fileGeneratingLogTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("fileGeneratingLogTable", "done.\n"));
}

/*
 * store_fileGeneratingLogTable(): store configuraiton file for fileGeneratingLogTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_fileGeneratingLogTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct fileGeneratingLogTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("fileGeneratingLogTable", "storing data...  "));
	refresh_fileGeneratingLogTable(1);
	(void) tmpsize;
	for (hcindex = fileGeneratingLogTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct fileGeneratingLogTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		if (StorageTmp->fileGeneratingLogStorageType == ST_NONVOLATILE) {
			memset(line, 0, sizeof(line));
			strcat(line, "fileGeneratingLogTable ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->logId, &StorageTmp->logIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->timesOfDay, &StorageTmp->timesOfDayLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->periodicTrigger, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->fileGeneratingLogStorageType, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->fileGeneratingLogEntryStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("fileGeneratingLogTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct callDetailLogRecordTable_data *callDetailLogRecordTable_create(void)
 * @brief create a fresh data structure representing a new row in the callDetailLogRecordTable table.
 *
 * Creates a new callDetailLogRecordTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct callDetailLogRecordTable_data *
callDetailLogRecordTable_create(void)
{
	struct callDetailLogRecordTable_data *StorageNew = SNMP_MALLOC_STRUCT(callDetailLogRecordTable_data);

	DEBUGMSGTL(("callDetailLogRecordTable", "creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->logId = (uint8_t *) strdup("")) != NULL) {
			StorageNew->logIdLen = strlen("");
		}
		if ((StorageNew->logRecordId = (uint8_t *) strdup("")) != NULL) {
			StorageNew->logRecordIdLen = strlen("");
		}
		if ((StorageNew->networkProviderId = (uint8_t *) strdup("")) != NULL) {
			StorageNew->networkProviderIdLen = strlen("");
		}
		StorageNew->recordType = 0;
		if ((StorageNew->seizureTime = (uint8_t *) strdup("")) != NULL) {
			StorageNew->seizureTimeLen = strlen("");
		}
		if ((StorageNew->answerTime = (uint8_t *) strdup("")) != NULL) {
			StorageNew->answerTimeLen = strlen("");
		}
		if ((StorageNew->partialTime = (uint8_t *) strdup("")) != NULL) {
			StorageNew->partialTimeLen = strlen("");
		}
		if ((StorageNew->eventTime = (uint8_t *) strdup("")) != NULL) {
			StorageNew->eventTimeLen = strlen("");
		}
		if ((StorageNew->callingPartyNumber = (uint8_t *) strdup("")) != NULL) {
			StorageNew->callingPartyNumberLen = strlen("");
		}
		if ((StorageNew->calledPartyNumber = (uint8_t *) strdup("")) != NULL) {
			StorageNew->calledPartyNumberLen = strlen("");
		}
		if ((StorageNew->redirectingNumber = (uint8_t *) strdup("")) != NULL) {
			StorageNew->redirectingNumberLen = strlen("");
		}
		if ((StorageNew->redirectionNumber = (uint8_t *) strdup("")) != NULL) {
			StorageNew->redirectionNumberLen = strlen("");
		}
		if ((StorageNew->originalCalledNumber = (uint8_t *) strdup("")) != NULL) {
			StorageNew->originalCalledNumberLen = strlen("");
		}
		if ((StorageNew->callingPartyNumberNotScreened = (uint8_t *) strdup("")) != NULL) {
			StorageNew->callingPartyNumberNotScreenedLen = strlen("");
		}
		if ((StorageNew->operatorSpecific1Number = (uint8_t *) strdup("")) != NULL) {
			StorageNew->operatorSpecific1NumberLen = strlen("");
		}
		if ((StorageNew->operatorSpecific2Number = (uint8_t *) strdup("")) != NULL) {
			StorageNew->operatorSpecific2NumberLen = strlen("");
		}
		if ((StorageNew->operatorSpecific3Number = (uint8_t *) strdup("")) != NULL) {
			StorageNew->operatorSpecific3NumberLen = strlen("");
		}
		if ((StorageNew->bearerService = (uint8_t *) strdup("")) != NULL) {
			StorageNew->bearerServiceLen = strlen("");
		}
		StorageNew->participantServiceUser = 0;
		if ((StorageNew->callIdentificationNumber = (uint8_t *) strdup("")) != NULL) {
			StorageNew->callIdentificationNumberLen = strlen("");
		}
		if ((StorageNew->supplementaryServices = (uint8_t *) strdup("")) != NULL) {
			StorageNew->supplementaryServicesLen = strlen("");
		}
		StorageNew->immediateNotificationForUsageMetering = 0;
		if ((StorageNew->cause = (uint8_t *) strdup("")) != NULL) {
			StorageNew->causeLen = strlen("");
		}
		if ((StorageNew->personalUserId = (uint8_t *) strdup("")) != NULL) {
			StorageNew->personalUserIdLen = strlen("");
		}
		StorageNew->chargedParticipant = 0;
		if ((StorageNew->chargedDirectoryNumber = (uint8_t *) strdup("")) != NULL) {
			StorageNew->chargedDirectoryNumberLen = strlen("");
		}
		StorageNew->percentageToBeBilled = 0;
		if ((StorageNew->accountCodeInput = (uint8_t *) strdup("")) != NULL) {
			StorageNew->accountCodeInputLen = strlen("");
		}
		if ((StorageNew->iNServiceCode = (uint8_t *) strdup("")) != NULL) {
			StorageNew->iNServiceCodeLen = strlen("");
		}
		if ((StorageNew->queueTimeStamp = (uint8_t *) strdup("")) != NULL) {
			StorageNew->queueTimeStampLen = strlen("");
		}
		StorageNew->queueDuration = 0;
		if ((StorageNew->serviceSpecificINInformation = (uint8_t *) strdup("")) != NULL) {
			StorageNew->serviceSpecificINInformationLen = strlen("");
		}
		StorageNew->partialRecordNumber = 0;
		StorageNew->partialRecordReason = 0;
		if ((StorageNew->exchangeInfo = (uint8_t *) strdup("")) != NULL) {
			StorageNew->exchangeInfoLen = strlen("");
		}
		if ((StorageNew->relatedCallNumber = (uint8_t *) strdup("")) != NULL) {
			StorageNew->relatedCallNumberLen = strlen("");
		}
		if (memdup((u_char **) &StorageNew->cDRPurpose, (u_char *) "\x00", 1) == SNMPERR_SUCCESS) {
			StorageNew->cDRPurposeLen = 1;
		}
		if ((StorageNew->physicalLineCode = (uint8_t *) strdup("")) != NULL) {
			StorageNew->physicalLineCodeLen = strlen("");
		}
		if ((StorageNew->receivedDigits = (uint8_t *) strdup("")) != NULL) {
			StorageNew->receivedDigitsLen = strlen("");
		}
		if ((StorageNew->operatorSpecific1AdditionalNumber = (uint8_t *) strdup("")) != NULL) {
			StorageNew->operatorSpecific1AdditionalNumberLen = strlen("");
		}
		if ((StorageNew->operatorSpecific2AdditionalNumber = (uint8_t *) strdup("")) != NULL) {
			StorageNew->operatorSpecific2AdditionalNumberLen = strlen("");
		}
		if ((StorageNew->operatorSpecific3AdditionalNumber = (uint8_t *) strdup("")) != NULL) {
			StorageNew->operatorSpecific3AdditionalNumberLen = strlen("");
		}
		if ((StorageNew->callingPartyCategory = (uint8_t *) strdup("")) != NULL) {
			StorageNew->callingPartyCategoryLen = strlen("");
		}
		StorageNew->callingPartyType = 0;
		if ((StorageNew->chargeInformation = (uint8_t *) strdup("")) != NULL) {
			StorageNew->chargeInformationLen = strlen("");
		}
		if ((StorageNew->progress = (uint8_t *) strdup("")) != NULL) {
			StorageNew->progressLen = strlen("");
		}
		if (memdup((u_char **) &StorageNew->accessDelivery, (u_char *) "\x00", 1) == SNMPERR_SUCCESS) {
			StorageNew->accessDeliveryLen = 1;
		}
		if ((StorageNew->trunkGroupOutgoing = (uint8_t *) strdup("")) != NULL) {
			StorageNew->trunkGroupOutgoingLen = strlen("");
		}
		if ((StorageNew->trunkGroupIncoming = (uint8_t *) strdup("")) != NULL) {
			StorageNew->trunkGroupIncomingLen = strlen("");
		}
		if ((StorageNew->fallbackBearerService = (uint8_t *) strdup("")) != NULL) {
			StorageNew->fallbackBearerServiceLen = strlen("");
		}
		if ((StorageNew->teleservice = (uint8_t *) strdup("")) != NULL) {
			StorageNew->teleserviceLen = strlen("");
		}
		StorageNew->conversationTime = 0;
		StorageNew->durationTimeACM = 0;
		StorageNew->durationTimeBAnswer = 0;
		StorageNew->durationTimeNoBAnswer = 0;
		if ((StorageNew->uUInfo = (uint8_t *) strdup("")) != NULL) {
			StorageNew->uUInfoLen = strlen("");
		}
		if ((StorageNew->standardExtensions = (uint8_t *) strdup("")) != NULL) {
			StorageNew->standardExtensionsLen = strlen("");
		}
		if ((StorageNew->recordExtensions = (uint8_t *) strdup("")) != NULL) {
			StorageNew->recordExtensionsLen = strlen("");
		}
		if ((StorageNew->bPartyCategory = (uint8_t *) strdup("")) != NULL) {
			StorageNew->bPartyCategoryLen = strlen("");
		}
		StorageNew->iSUPPreferred = 0;
		StorageNew->networkManagementControls = 0;
		StorageNew->glare = 0;
		StorageNew->recordId = 0;
		StorageNew->dataValidity = 0;
		StorageNew->callStatus = 0;
		if ((StorageNew->carrierId = (uint8_t *) strdup("")) != NULL) {
			StorageNew->carrierIdLen = strlen("");
		}
		if ((StorageNew->dPC = (uint8_t *) strdup("")) != NULL) {
			StorageNew->dPCLen = strlen("");
		}
		if ((StorageNew->oPC = (uint8_t *) strdup("")) != NULL) {
			StorageNew->oPCLen = strlen("");
		}

	}
	DEBUGMSGTL(("callDetailLogRecordTable", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct callDetailLogRecordTable_data *callDetailLogRecordTable_duplicate(struct callDetailLogRecordTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicat a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct callDetailLogRecordTable_data *
callDetailLogRecordTable_duplicate(struct callDetailLogRecordTable_data *thedata)
{
	struct callDetailLogRecordTable_data *StorageNew = SNMP_MALLOC_STRUCT(callDetailLogRecordTable_data);

	DEBUGMSGTL(("callDetailLogRecordTable", "duplicating row...  "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("callDetailLogRecordTable", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	callDetailLogRecordTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int callDetailLogRecordTable_destroy(struct callDetailLogRecordTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
callDetailLogRecordTable_destroy(struct callDetailLogRecordTable_data **thedata)
{
	struct callDetailLogRecordTable_data *StorageDel;

	DEBUGMSGTL(("callDetailLogRecordTable", "deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->logId);
		StorageDel->logIdLen = 0;
		SNMP_FREE(StorageDel->logRecordId);
		StorageDel->logRecordIdLen = 0;
		SNMP_FREE(StorageDel->networkProviderId);
		StorageDel->networkProviderIdLen = 0;
		SNMP_FREE(StorageDel->seizureTime);
		StorageDel->seizureTimeLen = 0;
		SNMP_FREE(StorageDel->answerTime);
		StorageDel->answerTimeLen = 0;
		SNMP_FREE(StorageDel->partialTime);
		StorageDel->partialTimeLen = 0;
		SNMP_FREE(StorageDel->eventTime);
		StorageDel->eventTimeLen = 0;
		SNMP_FREE(StorageDel->callingPartyNumber);
		StorageDel->callingPartyNumberLen = 0;
		SNMP_FREE(StorageDel->calledPartyNumber);
		StorageDel->calledPartyNumberLen = 0;
		SNMP_FREE(StorageDel->redirectingNumber);
		StorageDel->redirectingNumberLen = 0;
		SNMP_FREE(StorageDel->redirectionNumber);
		StorageDel->redirectionNumberLen = 0;
		SNMP_FREE(StorageDel->originalCalledNumber);
		StorageDel->originalCalledNumberLen = 0;
		SNMP_FREE(StorageDel->callingPartyNumberNotScreened);
		StorageDel->callingPartyNumberNotScreenedLen = 0;
		SNMP_FREE(StorageDel->operatorSpecific1Number);
		StorageDel->operatorSpecific1NumberLen = 0;
		SNMP_FREE(StorageDel->operatorSpecific2Number);
		StorageDel->operatorSpecific2NumberLen = 0;
		SNMP_FREE(StorageDel->operatorSpecific3Number);
		StorageDel->operatorSpecific3NumberLen = 0;
		SNMP_FREE(StorageDel->bearerService);
		StorageDel->bearerServiceLen = 0;
		SNMP_FREE(StorageDel->callIdentificationNumber);
		StorageDel->callIdentificationNumberLen = 0;
		SNMP_FREE(StorageDel->supplementaryServices);
		StorageDel->supplementaryServicesLen = 0;
		SNMP_FREE(StorageDel->cause);
		StorageDel->causeLen = 0;
		SNMP_FREE(StorageDel->personalUserId);
		StorageDel->personalUserIdLen = 0;
		SNMP_FREE(StorageDel->chargedDirectoryNumber);
		StorageDel->chargedDirectoryNumberLen = 0;
		SNMP_FREE(StorageDel->accountCodeInput);
		StorageDel->accountCodeInputLen = 0;
		SNMP_FREE(StorageDel->iNServiceCode);
		StorageDel->iNServiceCodeLen = 0;
		SNMP_FREE(StorageDel->queueTimeStamp);
		StorageDel->queueTimeStampLen = 0;
		SNMP_FREE(StorageDel->serviceSpecificINInformation);
		StorageDel->serviceSpecificINInformationLen = 0;
		SNMP_FREE(StorageDel->exchangeInfo);
		StorageDel->exchangeInfoLen = 0;
		SNMP_FREE(StorageDel->relatedCallNumber);
		StorageDel->relatedCallNumberLen = 0;
		SNMP_FREE(StorageDel->cDRPurpose);
		StorageDel->cDRPurposeLen = 0;
		SNMP_FREE(StorageDel->physicalLineCode);
		StorageDel->physicalLineCodeLen = 0;
		SNMP_FREE(StorageDel->receivedDigits);
		StorageDel->receivedDigitsLen = 0;
		SNMP_FREE(StorageDel->operatorSpecific1AdditionalNumber);
		StorageDel->operatorSpecific1AdditionalNumberLen = 0;
		SNMP_FREE(StorageDel->operatorSpecific2AdditionalNumber);
		StorageDel->operatorSpecific2AdditionalNumberLen = 0;
		SNMP_FREE(StorageDel->operatorSpecific3AdditionalNumber);
		StorageDel->operatorSpecific3AdditionalNumberLen = 0;
		SNMP_FREE(StorageDel->callingPartyCategory);
		StorageDel->callingPartyCategoryLen = 0;
		SNMP_FREE(StorageDel->chargeInformation);
		StorageDel->chargeInformationLen = 0;
		SNMP_FREE(StorageDel->progress);
		StorageDel->progressLen = 0;
		SNMP_FREE(StorageDel->accessDelivery);
		StorageDel->accessDeliveryLen = 0;
		SNMP_FREE(StorageDel->trunkGroupOutgoing);
		StorageDel->trunkGroupOutgoingLen = 0;
		SNMP_FREE(StorageDel->trunkGroupIncoming);
		StorageDel->trunkGroupIncomingLen = 0;
		SNMP_FREE(StorageDel->fallbackBearerService);
		StorageDel->fallbackBearerServiceLen = 0;
		SNMP_FREE(StorageDel->teleservice);
		StorageDel->teleserviceLen = 0;
		SNMP_FREE(StorageDel->uUInfo);
		StorageDel->uUInfoLen = 0;
		SNMP_FREE(StorageDel->standardExtensions);
		StorageDel->standardExtensionsLen = 0;
		SNMP_FREE(StorageDel->recordExtensions);
		StorageDel->recordExtensionsLen = 0;
		SNMP_FREE(StorageDel->bPartyCategory);
		StorageDel->bPartyCategoryLen = 0;
		SNMP_FREE(StorageDel->carrierId);
		StorageDel->carrierIdLen = 0;
		SNMP_FREE(StorageDel->dPC);
		StorageDel->dPCLen = 0;
		SNMP_FREE(StorageDel->oPC);
		StorageDel->oPCLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("callDetailLogRecordTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int callDetailLogRecordTable_add(struct callDetailLogRecordTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the callDetailLogRecordTable table data set.
 *
 * Adds a table row structure to the callDetailLogRecordTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
callDetailLogRecordTable_add(struct callDetailLogRecordTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("callDetailLogRecordTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index the data */
	/* logId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->logId, thedata->logIdLen);
	/* logRecordId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->logRecordId, thedata->logRecordIdLen);
	header_complex_add_data(&callDetailLogRecordTableStorage, vars, thedata);
	DEBUGMSGTL(("callDetailLogRecordTable", "registered an entry\n"));
	DEBUGMSGTL(("callDetailLogRecordTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int callDetailLogRecordTable_del(struct callDetailLogRecordTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the callDetailLogRecordTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
callDetailLogRecordTable_del(struct callDetailLogRecordTable_data *thedata)
{
	struct callDetailLogRecordTable_data *StorageDel;

	DEBUGMSGTL(("callDetailLogRecordTable", "deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(callDetailLogRecordTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&callDetailLogRecordTableStorage, hciptr);
	}
	DEBUGMSGTL(("callDetailLogRecordTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_callDetailLogRecordTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for callDetailLogRecordTable entries.
 *
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case callDetailLogRecordTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_callDetailLogRecordTable(const char *token, char *line)
{
	size_t tmpsize;
	struct callDetailLogRecordTable_data *StorageTmp = callDetailLogRecordTable_create();

	DEBUGMSGTL(("callDetailLogRecordTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->logId, &StorageTmp->logIdLen);
	if (StorageTmp->logId == NULL) {
		config_perror("invalid specification for logId");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->logRecordId, &StorageTmp->logRecordIdLen);
	if (StorageTmp->logRecordId == NULL) {
		config_perror("invalid specification for logRecordId");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->networkProviderId, &StorageTmp->networkProviderIdLen);
	if (StorageTmp->networkProviderId == NULL) {
		config_perror("invalid specification for networkProviderId");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->recordType, &tmpsize);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->seizureTime, &StorageTmp->seizureTimeLen);
	if (StorageTmp->seizureTime == NULL) {
		config_perror("invalid specification for seizureTime");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->answerTime, &StorageTmp->answerTimeLen);
	if (StorageTmp->answerTime == NULL) {
		config_perror("invalid specification for answerTime");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->partialTime, &StorageTmp->partialTimeLen);
	if (StorageTmp->partialTime == NULL) {
		config_perror("invalid specification for partialTime");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->eventTime, &StorageTmp->eventTimeLen);
	if (StorageTmp->eventTime == NULL) {
		config_perror("invalid specification for eventTime");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->callingPartyNumber, &StorageTmp->callingPartyNumberLen);
	if (StorageTmp->callingPartyNumber == NULL) {
		config_perror("invalid specification for callingPartyNumber");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->calledPartyNumber, &StorageTmp->calledPartyNumberLen);
	if (StorageTmp->calledPartyNumber == NULL) {
		config_perror("invalid specification for calledPartyNumber");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->redirectingNumber, &StorageTmp->redirectingNumberLen);
	if (StorageTmp->redirectingNumber == NULL) {
		config_perror("invalid specification for redirectingNumber");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->redirectionNumber, &StorageTmp->redirectionNumberLen);
	if (StorageTmp->redirectionNumber == NULL) {
		config_perror("invalid specification for redirectionNumber");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->originalCalledNumber, &StorageTmp->originalCalledNumberLen);
	if (StorageTmp->originalCalledNumber == NULL) {
		config_perror("invalid specification for originalCalledNumber");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->callingPartyNumberNotScreened, &StorageTmp->callingPartyNumberNotScreenedLen);
	if (StorageTmp->callingPartyNumberNotScreened == NULL) {
		config_perror("invalid specification for callingPartyNumberNotScreened");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->operatorSpecific1Number, &StorageTmp->operatorSpecific1NumberLen);
	if (StorageTmp->operatorSpecific1Number == NULL) {
		config_perror("invalid specification for operatorSpecific1Number");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->operatorSpecific2Number, &StorageTmp->operatorSpecific2NumberLen);
	if (StorageTmp->operatorSpecific2Number == NULL) {
		config_perror("invalid specification for operatorSpecific2Number");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->operatorSpecific3Number, &StorageTmp->operatorSpecific3NumberLen);
	if (StorageTmp->operatorSpecific3Number == NULL) {
		config_perror("invalid specification for operatorSpecific3Number");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->bearerService, &StorageTmp->bearerServiceLen);
	if (StorageTmp->bearerService == NULL) {
		config_perror("invalid specification for bearerService");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->participantServiceUser, &tmpsize);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->callIdentificationNumber, &StorageTmp->callIdentificationNumberLen);
	if (StorageTmp->callIdentificationNumber == NULL) {
		config_perror("invalid specification for callIdentificationNumber");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->supplementaryServices, &StorageTmp->supplementaryServicesLen);
	if (StorageTmp->supplementaryServices == NULL) {
		config_perror("invalid specification for supplementaryServices");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->immediateNotificationForUsageMetering, &tmpsize);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->cause, &StorageTmp->causeLen);
	if (StorageTmp->cause == NULL) {
		config_perror("invalid specification for cause");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->personalUserId, &StorageTmp->personalUserIdLen);
	if (StorageTmp->personalUserId == NULL) {
		config_perror("invalid specification for personalUserId");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->chargedParticipant, &tmpsize);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->chargedDirectoryNumber, &StorageTmp->chargedDirectoryNumberLen);
	if (StorageTmp->chargedDirectoryNumber == NULL) {
		config_perror("invalid specification for chargedDirectoryNumber");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->percentageToBeBilled, &tmpsize);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->accountCodeInput, &StorageTmp->accountCodeInputLen);
	if (StorageTmp->accountCodeInput == NULL) {
		config_perror("invalid specification for accountCodeInput");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->iNServiceCode, &StorageTmp->iNServiceCodeLen);
	if (StorageTmp->iNServiceCode == NULL) {
		config_perror("invalid specification for iNServiceCode");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->queueTimeStamp, &StorageTmp->queueTimeStampLen);
	if (StorageTmp->queueTimeStamp == NULL) {
		config_perror("invalid specification for queueTimeStamp");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->queueDuration, &tmpsize);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->serviceSpecificINInformation, &StorageTmp->serviceSpecificINInformationLen);
	if (StorageTmp->serviceSpecificINInformation == NULL) {
		config_perror("invalid specification for serviceSpecificINInformation");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->partialRecordNumber, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->partialRecordReason, &tmpsize);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->exchangeInfo, &StorageTmp->exchangeInfoLen);
	if (StorageTmp->exchangeInfo == NULL) {
		config_perror("invalid specification for exchangeInfo");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->relatedCallNumber, &StorageTmp->relatedCallNumberLen);
	if (StorageTmp->relatedCallNumber == NULL) {
		config_perror("invalid specification for relatedCallNumber");
		return;
	}
	line = read_config_read_data(ASN_BIT_STR, line, &StorageTmp->cDRPurpose, &StorageTmp->cDRPurposeLen);
	if (StorageTmp->cDRPurpose == NULL) {
		config_perror("invalid specification for cDRPurpose");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->physicalLineCode, &StorageTmp->physicalLineCodeLen);
	if (StorageTmp->physicalLineCode == NULL) {
		config_perror("invalid specification for physicalLineCode");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->receivedDigits, &StorageTmp->receivedDigitsLen);
	if (StorageTmp->receivedDigits == NULL) {
		config_perror("invalid specification for receivedDigits");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->operatorSpecific1AdditionalNumber, &StorageTmp->operatorSpecific1AdditionalNumberLen);
	if (StorageTmp->operatorSpecific1AdditionalNumber == NULL) {
		config_perror("invalid specification for operatorSpecific1AdditionalNumber");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->operatorSpecific2AdditionalNumber, &StorageTmp->operatorSpecific2AdditionalNumberLen);
	if (StorageTmp->operatorSpecific2AdditionalNumber == NULL) {
		config_perror("invalid specification for operatorSpecific2AdditionalNumber");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->operatorSpecific3AdditionalNumber, &StorageTmp->operatorSpecific3AdditionalNumberLen);
	if (StorageTmp->operatorSpecific3AdditionalNumber == NULL) {
		config_perror("invalid specification for operatorSpecific3AdditionalNumber");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->callingPartyCategory, &StorageTmp->callingPartyCategoryLen);
	if (StorageTmp->callingPartyCategory == NULL) {
		config_perror("invalid specification for callingPartyCategory");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->callingPartyType, &tmpsize);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->chargeInformation, &StorageTmp->chargeInformationLen);
	if (StorageTmp->chargeInformation == NULL) {
		config_perror("invalid specification for chargeInformation");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->progress, &StorageTmp->progressLen);
	if (StorageTmp->progress == NULL) {
		config_perror("invalid specification for progress");
		return;
	}
	line = read_config_read_data(ASN_BIT_STR, line, &StorageTmp->accessDelivery, &StorageTmp->accessDeliveryLen);
	if (StorageTmp->accessDelivery == NULL) {
		config_perror("invalid specification for accessDelivery");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->trunkGroupOutgoing, &StorageTmp->trunkGroupOutgoingLen);
	if (StorageTmp->trunkGroupOutgoing == NULL) {
		config_perror("invalid specification for trunkGroupOutgoing");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->trunkGroupIncoming, &StorageTmp->trunkGroupIncomingLen);
	if (StorageTmp->trunkGroupIncoming == NULL) {
		config_perror("invalid specification for trunkGroupIncoming");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->fallbackBearerService, &StorageTmp->fallbackBearerServiceLen);
	if (StorageTmp->fallbackBearerService == NULL) {
		config_perror("invalid specification for fallbackBearerService");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->teleservice, &StorageTmp->teleserviceLen);
	if (StorageTmp->teleservice == NULL) {
		config_perror("invalid specification for teleservice");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->conversationTime, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->durationTimeACM, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->durationTimeBAnswer, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->durationTimeNoBAnswer, &tmpsize);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->uUInfo, &StorageTmp->uUInfoLen);
	if (StorageTmp->uUInfo == NULL) {
		config_perror("invalid specification for uUInfo");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->standardExtensions, &StorageTmp->standardExtensionsLen);
	if (StorageTmp->standardExtensions == NULL) {
		config_perror("invalid specification for standardExtensions");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->recordExtensions, &StorageTmp->recordExtensionsLen);
	if (StorageTmp->recordExtensions == NULL) {
		config_perror("invalid specification for recordExtensions");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->bPartyCategory, &StorageTmp->bPartyCategoryLen);
	if (StorageTmp->bPartyCategory == NULL) {
		config_perror("invalid specification for bPartyCategory");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->iSUPPreferred, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->networkManagementControls, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->glare, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->recordId, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->dataValidity, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->callStatus, &tmpsize);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->carrierId, &StorageTmp->carrierIdLen);
	if (StorageTmp->carrierId == NULL) {
		config_perror("invalid specification for carrierId");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->dPC, &StorageTmp->dPCLen);
	if (StorageTmp->dPC == NULL) {
		config_perror("invalid specification for dPC");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->oPC, &StorageTmp->oPCLen);
	if (StorageTmp->oPC == NULL) {
		config_perror("invalid specification for oPC");
		return;
	}
	callDetailLogRecordTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("callDetailLogRecordTable", "done.\n"));
}

/*
 * store_callDetailLogRecordTable(): store configuraiton file for callDetailLogRecordTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_callDetailLogRecordTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct callDetailLogRecordTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("callDetailLogRecordTable", "storing data...  "));
	refresh_callDetailLogRecordTable(1);
	(void) tmpsize;
	for (hcindex = callDetailLogRecordTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct callDetailLogRecordTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "callDetailLogRecordTable ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->logId, &StorageTmp->logIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->logRecordId, &StorageTmp->logRecordIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->networkProviderId, &StorageTmp->networkProviderIdLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->recordType, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->seizureTime, &StorageTmp->seizureTimeLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->answerTime, &StorageTmp->answerTimeLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->partialTime, &StorageTmp->partialTimeLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->eventTime, &StorageTmp->eventTimeLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->callingPartyNumber, &StorageTmp->callingPartyNumberLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->calledPartyNumber, &StorageTmp->calledPartyNumberLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->redirectingNumber, &StorageTmp->redirectingNumberLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->redirectionNumber, &StorageTmp->redirectionNumberLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->originalCalledNumber, &StorageTmp->originalCalledNumberLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->callingPartyNumberNotScreened, &StorageTmp->callingPartyNumberNotScreenedLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->operatorSpecific1Number, &StorageTmp->operatorSpecific1NumberLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->operatorSpecific2Number, &StorageTmp->operatorSpecific2NumberLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->operatorSpecific3Number, &StorageTmp->operatorSpecific3NumberLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->bearerService, &StorageTmp->bearerServiceLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->participantServiceUser, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->callIdentificationNumber, &StorageTmp->callIdentificationNumberLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->supplementaryServices, &StorageTmp->supplementaryServicesLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->immediateNotificationForUsageMetering, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->cause, &StorageTmp->causeLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->personalUserId, &StorageTmp->personalUserIdLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->chargedParticipant, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->chargedDirectoryNumber, &StorageTmp->chargedDirectoryNumberLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->percentageToBeBilled, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->accountCodeInput, &StorageTmp->accountCodeInputLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->iNServiceCode, &StorageTmp->iNServiceCodeLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->queueTimeStamp, &StorageTmp->queueTimeStampLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->queueDuration, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->serviceSpecificINInformation, &StorageTmp->serviceSpecificINInformationLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->partialRecordNumber, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->partialRecordReason, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->exchangeInfo, &StorageTmp->exchangeInfoLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->relatedCallNumber, &StorageTmp->relatedCallNumberLen);
			cptr = read_config_store_data(ASN_BIT_STR, cptr, &StorageTmp->cDRPurpose, &StorageTmp->cDRPurposeLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->physicalLineCode, &StorageTmp->physicalLineCodeLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->receivedDigits, &StorageTmp->receivedDigitsLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->operatorSpecific1AdditionalNumber, &StorageTmp->operatorSpecific1AdditionalNumberLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->operatorSpecific2AdditionalNumber, &StorageTmp->operatorSpecific2AdditionalNumberLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->operatorSpecific3AdditionalNumber, &StorageTmp->operatorSpecific3AdditionalNumberLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->callingPartyCategory, &StorageTmp->callingPartyCategoryLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->callingPartyType, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->chargeInformation, &StorageTmp->chargeInformationLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->progress, &StorageTmp->progressLen);
			cptr = read_config_store_data(ASN_BIT_STR, cptr, &StorageTmp->accessDelivery, &StorageTmp->accessDeliveryLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->trunkGroupOutgoing, &StorageTmp->trunkGroupOutgoingLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->trunkGroupIncoming, &StorageTmp->trunkGroupIncomingLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->fallbackBearerService, &StorageTmp->fallbackBearerServiceLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->teleservice, &StorageTmp->teleserviceLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->conversationTime, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->durationTimeACM, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->durationTimeBAnswer, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->durationTimeNoBAnswer, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->uUInfo, &StorageTmp->uUInfoLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->standardExtensions, &StorageTmp->standardExtensionsLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->recordExtensions, &StorageTmp->recordExtensionsLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->bPartyCategory, &StorageTmp->bPartyCategoryLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->iSUPPreferred, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->networkManagementControls, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->glare, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->recordId, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->dataValidity, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->callStatus, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->carrierId, &StorageTmp->carrierIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->dPC, &StorageTmp->dPCLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->oPC, &StorageTmp->oPCLen);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("callDetailLogRecordTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct configurableSimpleUsageMeteringControlR2Table_data *configurableSimpleUsageMeteringControlR2Table_create(void)
 * @brief create a fresh data structure representing a new row in the configurableSimpleUsageMeteringControlR2Table table.
 *
 * Creates a new configurableSimpleUsageMeteringControlR2Table_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct configurableSimpleUsageMeteringControlR2Table_data *
configurableSimpleUsageMeteringControlR2Table_create(void)
{
	struct configurableSimpleUsageMeteringControlR2Table_data *StorageNew = SNMP_MALLOC_STRUCT(configurableSimpleUsageMeteringControlR2Table_data);

	DEBUGMSGTL(("configurableSimpleUsageMeteringControlR2Table", "creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->controlObjectId = (uint8_t *) strdup("")) != NULL) {
			StorageNew->controlObjectIdLen = strlen("");
		}
		if (memdup((u_char **) &StorageNew->configurationR2Mask, (u_char *) "\x00", 1) == SNMPERR_SUCCESS) {
			StorageNew->configurationR2MaskLen = 1;
		}
		StorageNew->configurationR2Status = 0;
		StorageNew->configurationR2Status = RS_NOTREADY;
	}
	DEBUGMSGTL(("configurableSimpleUsageMeteringControlR2Table", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct configurableSimpleUsageMeteringControlR2Table_data *configurableSimpleUsageMeteringControlR2Table_duplicate(struct configurableSimpleUsageMeteringControlR2Table_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicat a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct configurableSimpleUsageMeteringControlR2Table_data *
configurableSimpleUsageMeteringControlR2Table_duplicate(struct configurableSimpleUsageMeteringControlR2Table_data *thedata)
{
	struct configurableSimpleUsageMeteringControlR2Table_data *StorageNew = SNMP_MALLOC_STRUCT(configurableSimpleUsageMeteringControlR2Table_data);

	DEBUGMSGTL(("configurableSimpleUsageMeteringControlR2Table", "duplicating row...  "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("configurableSimpleUsageMeteringControlR2Table", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	configurableSimpleUsageMeteringControlR2Table_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int configurableSimpleUsageMeteringControlR2Table_destroy(struct configurableSimpleUsageMeteringControlR2Table_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
configurableSimpleUsageMeteringControlR2Table_destroy(struct configurableSimpleUsageMeteringControlR2Table_data **thedata)
{
	struct configurableSimpleUsageMeteringControlR2Table_data *StorageDel;

	DEBUGMSGTL(("configurableSimpleUsageMeteringControlR2Table", "deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->controlObjectId);
		StorageDel->controlObjectIdLen = 0;
		SNMP_FREE(StorageDel->configurationR2Mask);
		StorageDel->configurationR2MaskLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("configurableSimpleUsageMeteringControlR2Table", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int configurableSimpleUsageMeteringControlR2Table_add(struct configurableSimpleUsageMeteringControlR2Table_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the configurableSimpleUsageMeteringControlR2Table table data set.
 *
 * Adds a table row structure to the configurableSimpleUsageMeteringControlR2Table table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
configurableSimpleUsageMeteringControlR2Table_add(struct configurableSimpleUsageMeteringControlR2Table_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("configurableSimpleUsageMeteringControlR2Table", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index the data */
	/* controlObjectId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->controlObjectId, thedata->controlObjectIdLen);
	header_complex_add_data(&configurableSimpleUsageMeteringControlR2TableStorage, vars, thedata);
	DEBUGMSGTL(("configurableSimpleUsageMeteringControlR2Table", "registered an entry\n"));
	DEBUGMSGTL(("configurableSimpleUsageMeteringControlR2Table", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int configurableSimpleUsageMeteringControlR2Table_del(struct configurableSimpleUsageMeteringControlR2Table_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the configurableSimpleUsageMeteringControlR2Table table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
configurableSimpleUsageMeteringControlR2Table_del(struct configurableSimpleUsageMeteringControlR2Table_data *thedata)
{
	struct configurableSimpleUsageMeteringControlR2Table_data *StorageDel;

	DEBUGMSGTL(("configurableSimpleUsageMeteringControlR2Table", "deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(configurableSimpleUsageMeteringControlR2TableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&configurableSimpleUsageMeteringControlR2TableStorage, hciptr);
	}
	DEBUGMSGTL(("configurableSimpleUsageMeteringControlR2Table", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_configurableSimpleUsageMeteringControlR2Table(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for configurableSimpleUsageMeteringControlR2Table entries.
 *
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case configurableSimpleUsageMeteringControlR2Table).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_configurableSimpleUsageMeteringControlR2Table(const char *token, char *line)
{
	size_t tmpsize;
	struct configurableSimpleUsageMeteringControlR2Table_data *StorageTmp = configurableSimpleUsageMeteringControlR2Table_create();

	DEBUGMSGTL(("configurableSimpleUsageMeteringControlR2Table", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->controlObjectId, &StorageTmp->controlObjectIdLen);
	if (StorageTmp->controlObjectId == NULL) {
		config_perror("invalid specification for controlObjectId");
		return;
	}
	line = read_config_read_data(ASN_BIT_STR, line, &StorageTmp->configurationR2Mask, &StorageTmp->configurationR2MaskLen);
	if (StorageTmp->configurationR2Mask == NULL) {
		config_perror("invalid specification for configurationR2Mask");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->configurationR2Status, &tmpsize);
	configurableSimpleUsageMeteringControlR2Table_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("configurableSimpleUsageMeteringControlR2Table", "done.\n"));
}

/*
 * store_configurableSimpleUsageMeteringControlR2Table(): store configuraiton file for configurableSimpleUsageMeteringControlR2Table
 * stores .conf file entries needed to configure the mib.
 */
int
store_configurableSimpleUsageMeteringControlR2Table(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct configurableSimpleUsageMeteringControlR2Table_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("configurableSimpleUsageMeteringControlR2Table", "storing data...  "));
	refresh_configurableSimpleUsageMeteringControlR2Table(1);
	(void) tmpsize;
	for (hcindex = configurableSimpleUsageMeteringControlR2TableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct configurableSimpleUsageMeteringControlR2Table_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "configurableSimpleUsageMeteringControlR2Table ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->controlObjectId, &StorageTmp->controlObjectIdLen);
			cptr = read_config_store_data(ASN_BIT_STR, cptr, &StorageTmp->configurationR2Mask, &StorageTmp->configurationR2MaskLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->configurationR2Status, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("configurableSimpleUsageMeteringControlR2Table", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct blockGeneratingLogR2Table_data *blockGeneratingLogR2Table_create(void)
 * @brief create a fresh data structure representing a new row in the blockGeneratingLogR2Table table.
 *
 * Creates a new blockGeneratingLogR2Table_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct blockGeneratingLogR2Table_data *
blockGeneratingLogR2Table_create(void)
{
	struct blockGeneratingLogR2Table_data *StorageNew = SNMP_MALLOC_STRUCT(blockGeneratingLogR2Table_data);

	DEBUGMSGTL(("blockGeneratingLogR2Table", "creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->logId = (uint8_t *) strdup("")) != NULL) {
			StorageNew->logIdLen = strlen("");
		}
		StorageNew->blockGeneratingLogVersion = BLOCKGENERATINGLOGVERSION_VERSION1;

	}
	DEBUGMSGTL(("blockGeneratingLogR2Table", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct blockGeneratingLogR2Table_data *blockGeneratingLogR2Table_duplicate(struct blockGeneratingLogR2Table_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicat a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct blockGeneratingLogR2Table_data *
blockGeneratingLogR2Table_duplicate(struct blockGeneratingLogR2Table_data *thedata)
{
	struct blockGeneratingLogR2Table_data *StorageNew = SNMP_MALLOC_STRUCT(blockGeneratingLogR2Table_data);

	DEBUGMSGTL(("blockGeneratingLogR2Table", "duplicating row...  "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("blockGeneratingLogR2Table", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	blockGeneratingLogR2Table_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int blockGeneratingLogR2Table_destroy(struct blockGeneratingLogR2Table_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
blockGeneratingLogR2Table_destroy(struct blockGeneratingLogR2Table_data **thedata)
{
	struct blockGeneratingLogR2Table_data *StorageDel;

	DEBUGMSGTL(("blockGeneratingLogR2Table", "deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->logId);
		StorageDel->logIdLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("blockGeneratingLogR2Table", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int blockGeneratingLogR2Table_add(struct blockGeneratingLogR2Table_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the blockGeneratingLogR2Table table data set.
 *
 * Adds a table row structure to the blockGeneratingLogR2Table table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
blockGeneratingLogR2Table_add(struct blockGeneratingLogR2Table_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("blockGeneratingLogR2Table", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index the data */
	/* logId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->logId, thedata->logIdLen);
	header_complex_add_data(&blockGeneratingLogR2TableStorage, vars, thedata);
	DEBUGMSGTL(("blockGeneratingLogR2Table", "registered an entry\n"));
	DEBUGMSGTL(("blockGeneratingLogR2Table", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int blockGeneratingLogR2Table_del(struct blockGeneratingLogR2Table_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the blockGeneratingLogR2Table table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
blockGeneratingLogR2Table_del(struct blockGeneratingLogR2Table_data *thedata)
{
	struct blockGeneratingLogR2Table_data *StorageDel;

	DEBUGMSGTL(("blockGeneratingLogR2Table", "deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(blockGeneratingLogR2TableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&blockGeneratingLogR2TableStorage, hciptr);
	}
	DEBUGMSGTL(("blockGeneratingLogR2Table", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_blockGeneratingLogR2Table(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for blockGeneratingLogR2Table entries.
 *
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case blockGeneratingLogR2Table).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_blockGeneratingLogR2Table(const char *token, char *line)
{
	size_t tmpsize;
	struct blockGeneratingLogR2Table_data *StorageTmp = blockGeneratingLogR2Table_create();

	DEBUGMSGTL(("blockGeneratingLogR2Table", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->logId, &StorageTmp->logIdLen);
	if (StorageTmp->logId == NULL) {
		config_perror("invalid specification for logId");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->blockGeneratingLogVersion, &tmpsize);
	blockGeneratingLogR2Table_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("blockGeneratingLogR2Table", "done.\n"));
}

/*
 * store_blockGeneratingLogR2Table(): store configuraiton file for blockGeneratingLogR2Table
 * stores .conf file entries needed to configure the mib.
 */
int
store_blockGeneratingLogR2Table(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct blockGeneratingLogR2Table_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("blockGeneratingLogR2Table", "storing data...  "));
	refresh_blockGeneratingLogR2Table(1);
	(void) tmpsize;
	for (hcindex = blockGeneratingLogR2TableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct blockGeneratingLogR2Table_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "blockGeneratingLogR2Table ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->logId, &StorageTmp->logIdLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->blockGeneratingLogVersion, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("blockGeneratingLogR2Table", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct callDetailLogRecordR2Table_data *callDetailLogRecordR2Table_create(void)
 * @brief create a fresh data structure representing a new row in the callDetailLogRecordR2Table table.
 *
 * Creates a new callDetailLogRecordR2Table_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct callDetailLogRecordR2Table_data *
callDetailLogRecordR2Table_create(void)
{
	struct callDetailLogRecordR2Table_data *StorageNew = SNMP_MALLOC_STRUCT(callDetailLogRecordR2Table_data);

	DEBUGMSGTL(("callDetailLogRecordR2Table", "creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->logId = (uint8_t *) strdup("")) != NULL) {
			StorageNew->logIdLen = strlen("");
		}
		if ((StorageNew->logRecordId = (uint8_t *) strdup("")) != NULL) {
			StorageNew->logRecordIdLen = strlen("");
		}
		StorageNew->sequenceId = (struct counter64) {
		0, 0};
		StorageNew->zoneId = (struct counter64) {
		0, 0};
		StorageNew->recSeqId = (struct counter64) {
		0, 0};
		StorageNew->fileSeqId = (struct counter64) {
		0, 0};
		if ((StorageNew->callId = (uint8_t *) strdup("")) != NULL) {
			StorageNew->callIdLen = strlen("");
		}
		if ((StorageNew->lastUpdate = (uint8_t *) strdup("")) != NULL) {
			StorageNew->lastUpdateLen = strlen("");
		}
		if ((StorageNew->eventOrder = (uint8_t *) strdup("")) != NULL) {
			StorageNew->eventOrderLen = strlen("");
		}
		StorageNew->status = 0;
		StorageNew->callType = 0;
		if ((StorageNew->clngPtyNbr = (uint8_t *) strdup("")) != NULL) {
			StorageNew->clngPtyNbrLen = strlen("");
		}
		if ((StorageNew->chargeNbr = (uint8_t *) strdup("")) != NULL) {
			StorageNew->chargeNbrLen = strlen("");
		}
		if ((StorageNew->clldPtyNbr = (uint8_t *) strdup("")) != NULL) {
			StorageNew->clldPtyNbrLen = strlen("");
		}
		StorageNew->clldPtyNoa = 0;
		if ((StorageNew->origLnInfo = (uint8_t *) strdup("")) != NULL) {
			StorageNew->origLnInfoLen = strlen("");
		}
		if ((StorageNew->ingLrn = (uint8_t *) strdup("")) != NULL) {
			StorageNew->ingLrnLen = strlen("");
		}
		if ((StorageNew->ingCic = (uint8_t *) strdup("")) != NULL) {
			StorageNew->ingCicLen = strlen("");
		}
		StorageNew->ingCsi = 0;
		StorageNew->ingCceId = 0;
		StorageNew->ingTgProt = 0;
		StorageNew->ingTgType = 0;
		StorageNew->ingTgId = 0;
		if ((StorageNew->ingCallStart = (uint8_t *) strdup("")) != NULL) {
			StorageNew->ingCallStartLen = strlen("");
		}
		StorageNew->ingGateId = 0;
		StorageNew->ingCardId = 0;
		StorageNew->ingSpanId = 0;
		StorageNew->ingChanId = 0;
		StorageNew->ingIsdnDChan = 0;
		StorageNew->ingIsdnCrn = 0;
		if ((StorageNew->ingCreatConnCplt = (uint8_t *) strdup("")) != NULL) {
			StorageNew->ingCreatConnCpltLen = strlen("");
		}
		if ((StorageNew->ingAddrCplt = (uint8_t *) strdup("")) != NULL) {
			StorageNew->ingAddrCpltLen = strlen("");
		}
		if ((StorageNew->ingCallAns = (uint8_t *) strdup("")) != NULL) {
			StorageNew->ingCallAnsLen = strlen("");
		}
		if ((StorageNew->transNbr = (uint8_t *) strdup("")) != NULL) {
			StorageNew->transNbrLen = strlen("");
		}
		if ((StorageNew->termLrn = (uint8_t *) strdup("")) != NULL) {
			StorageNew->termLrnLen = strlen("");
		}
		if ((StorageNew->transCic = (uint8_t *) strdup("")) != NULL) {
			StorageNew->transCicLen = strlen("");
		}
		if ((StorageNew->ingCallRls = (uint8_t *) strdup("")) != NULL) {
			StorageNew->ingCallRlsLen = strlen("");
		}
		StorageNew->ingRlsCause = 0;
		StorageNew->egrCceId = 0;
		StorageNew->egrTgProt = 0;
		StorageNew->egrTgType = 0;
		StorageNew->egrTgId = 0;
		if ((StorageNew->egrCallStart = (uint8_t *) strdup("")) != NULL) {
			StorageNew->egrCallStartLen = strlen("");
		}
		StorageNew->egrGateId = 0;
		StorageNew->egrCardId = 0;
		StorageNew->egrSpanId = 0;
		StorageNew->egrChanId = 0;
		StorageNew->egrIsdnDChan = 0;
		StorageNew->egrIsdnCrn = 0;
		if ((StorageNew->egrCreatConnCplt = (uint8_t *) strdup("")) != NULL) {
			StorageNew->egrCreatConnCpltLen = strlen("");
		}
		if ((StorageNew->egrAddrCplt = (uint8_t *) strdup("")) != NULL) {
			StorageNew->egrAddrCpltLen = strlen("");
		}
		if ((StorageNew->egrCallAns = (uint8_t *) strdup("")) != NULL) {
			StorageNew->egrCallAnsLen = strlen("");
		}
		if ((StorageNew->egrCallRls = (uint8_t *) strdup("")) != NULL) {
			StorageNew->egrCallRlsLen = strlen("");
		}
		StorageNew->egrRlsCause = 0;
		if ((StorageNew->chkptFirst = (uint8_t *) strdup("")) != NULL) {
			StorageNew->chkptFirstLen = strlen("");
		}
		if ((StorageNew->chkptLast = (uint8_t *) strdup("")) != NULL) {
			StorageNew->chkptLastLen = strlen("");
		}
		if ((StorageNew->ingGateName = (uint8_t *) strdup("")) != NULL) {
			StorageNew->ingGateNameLen = strlen("");
		}
		if ((StorageNew->egrGateName = (uint8_t *) strdup("")) != NULL) {
			StorageNew->egrGateNameLen = strlen("");
		}
		if ((StorageNew->ingTgName = (uint8_t *) strdup("")) != NULL) {
			StorageNew->ingTgNameLen = strlen("");
		}
		if ((StorageNew->egrTgName = (uint8_t *) strdup("")) != NULL) {
			StorageNew->egrTgNameLen = strlen("");
		}
		if ((StorageNew->origGateIp = (uint8_t *) strdup("")) != NULL) {
			StorageNew->origGateIpLen = strlen("");
		}
		if ((StorageNew->termGateIp = (uint8_t *) strdup("")) != NULL) {
			StorageNew->termGateIpLen = strlen("");
		}
		if ((StorageNew->h323ConfId = (uint8_t *) strdup("")) != NULL) {
			StorageNew->h323ConfIdLen = strlen("");
		}
		StorageNew->ingCardPort = 0;
		StorageNew->ingCardPath = 0;
		StorageNew->egrCardPort = 0;
		StorageNew->egrCardPath = 0;
		StorageNew->ingTg = 0;
		StorageNew->egrTg = 0;
		if ((StorageNew->dialedNbr = (uint8_t *) strdup("")) != NULL) {
			StorageNew->dialedNbrLen = strlen("");
		}
		StorageNew->dialedNoa = 0;
		if ((StorageNew->redirNbr = (uint8_t *) strdup("")) != NULL) {
			StorageNew->redirNbrLen = strlen("");
		}
		StorageNew->redirNoa = 0;
		StorageNew->redirInd = 0;
		StorageNew->redirRsn = 0;
		if ((StorageNew->calledNbr = (uint8_t *) strdup("")) != NULL) {
			StorageNew->calledNbrLen = strlen("");
		}
		StorageNew->calledNoa = 0;
		StorageNew->redirRsnOrig = 0;
		StorageNew->redirCntr = 0;
		StorageNew->redirPrsntnInd = 0;
		if ((StorageNew->ingJuris = (uint8_t *) strdup("")) != NULL) {
			StorageNew->ingJurisLen = strlen("");
		}
		if ((StorageNew->egrJuris = (uint8_t *) strdup("")) != NULL) {
			StorageNew->egrJurisLen = strlen("");
		}
		StorageNew->ingTrunkBearCap = 0;
		StorageNew->egrTrunkBearCap = 0;
		if ((StorageNew->transNetSelCarr = (uint8_t *) strdup("")) != NULL) {
			StorageNew->transNetSelCarrLen = strlen("");
		}
		StorageNew->clngPtyNoa = 0;
		if ((StorageNew->ingRlsCauseIntrnl = (uint8_t *) strdup("")) != NULL) {
			StorageNew->ingRlsCauseIntrnlLen = strlen("");
		}
		if ((StorageNew->egrRlsCauseIntrnl = (uint8_t *) strdup("")) != NULL) {
			StorageNew->egrRlsCauseIntrnlLen = strlen("");
		}
		if ((StorageNew->egrClldNbr = (uint8_t *) strdup("")) != NULL) {
			StorageNew->egrClldNbrLen = strlen("");
		}
		StorageNew->egrClldNoa = 0;
		if ((StorageNew->egrCnndNbr = (uint8_t *) strdup("")) != NULL) {
			StorageNew->egrCnndNbrLen = strlen("");
		}
		StorageNew->egrCnndNoa = 0;
		StorageNew->clngPtyPrsntnInd = 0;
		StorageNew->ingIri = 0;
		StorageNew->egrOri = 0;
		if ((StorageNew->ingCallidExtrnl = (uint8_t *) strdup("")) != NULL) {
			StorageNew->ingCallidExtrnlLen = strlen("");
		}
		if ((StorageNew->egrCallidExtrnl = (uint8_t *) strdup("")) != NULL) {
			StorageNew->egrCallidExtrnlLen = strlen("");
		}
		if ((StorageNew->ingChargeInfo = (uint8_t *) strdup("")) != NULL) {
			StorageNew->ingChargeInfoLen = strlen("");
		}
		if ((StorageNew->egrChargeInfo = (uint8_t *) strdup("")) != NULL) {
			StorageNew->egrChargeInfoLen = strlen("");
		}
		if ((StorageNew->ingPrtlInd = (uint8_t *) strdup("")) != NULL) {
			StorageNew->ingPrtlIndLen = strlen("");
		}
		StorageNew->ingNatfwdCliblkInd = 0;
		StorageNew->ingNatfwdNtaInd = 0;
		if ((StorageNew->ingLastDvrtLnDigs = (uint8_t *) strdup("")) != NULL) {
			StorageNew->ingLastDvrtLnDigsLen = strlen("");
		}
		StorageNew->ingLastDvrtLnNoa = 0;
		if ((StorageNew->j7IngAddlPtyCat = (uint8_t *) strdup("")) != NULL) {
			StorageNew->j7IngAddlPtyCatLen = strlen("");
		}
		if ((StorageNew->j7IngChgAreaInfo = (uint8_t *) strdup("")) != NULL) {
			StorageNew->j7IngChgAreaInfoLen = strlen("");
		}
		if ((StorageNew->j7IngFwdCallInd = (uint8_t *) strdup("")) != NULL) {
			StorageNew->j7IngFwdCallIndLen = strlen("");
		}
		if ((StorageNew->j7EgrAddlPtyCat = (uint8_t *) strdup("")) != NULL) {
			StorageNew->j7EgrAddlPtyCatLen = strlen("");
		}
		if ((StorageNew->j7EgrChgAreaInfo = (uint8_t *) strdup("")) != NULL) {
			StorageNew->j7EgrChgAreaInfoLen = strlen("");
		}
		if ((StorageNew->j7EgrBkwCallInd = (uint8_t *) strdup("")) != NULL) {
			StorageNew->j7EgrBkwCallIndLen = strlen("");
		}
		if ((StorageNew->j7CarrInfoXfer = (uint8_t *) strdup("")) != NULL) {
			StorageNew->j7CarrInfoXferLen = strlen("");
		}
		if ((StorageNew->ingSs7GnrcParm = (uint8_t *) strdup("")) != NULL) {
			StorageNew->ingSs7GnrcParmLen = strlen("");
		}
		if ((StorageNew->egrSs7GnrcParm = (uint8_t *) strdup("")) != NULL) {
			StorageNew->egrSs7GnrcParmLen = strlen("");
		}
		StorageNew->ingPktsSent = 0;
		StorageNew->ingPktsRcvd = 0;
		StorageNew->ingPktsLost = 0;
		StorageNew->ingPktsXfer = 0;
		StorageNew->ingJitter = 0;
		StorageNew->ingLtncy = 0;
		StorageNew->egrPktsSent = 0;
		StorageNew->egrPktsRcvd = 0;
		StorageNew->egrPktsLost = 0;
		StorageNew->egrPktsXfer = 0;
		StorageNew->egrJitter = 0;
		StorageNew->egrLtncy = 0;
		StorageNew->ingCrctId = 0;
		StorageNew->egrCrctId = 0;
		StorageNew->ingCodec = 0;
		StorageNew->egrCodec = 0;
		StorageNew->ingLocGateId = 0;
		StorageNew->egrLocGateId = 0;
		if ((StorageNew->ingCasCrctSzr = (uint8_t *) strdup("")) != NULL) {
			StorageNew->ingCasCrctSzrLen = strlen("");
		}
		if ((StorageNew->egrCasCrctSzr = (uint8_t *) strdup("")) != NULL) {
			StorageNew->egrCasCrctSzrLen = strlen("");
		}
		if ((StorageNew->ingZz = (uint8_t *) strdup("")) != NULL) {
			StorageNew->ingZzLen = strlen("");
		}
		if ((StorageNew->egrZz = (uint8_t *) strdup("")) != NULL) {
			StorageNew->egrZzLen = strlen("");
		}
		if ((StorageNew->ingCtryAddrType = (uint8_t *) strdup("")) != NULL) {
			StorageNew->ingCtryAddrTypeLen = strlen("");
		}
		if ((StorageNew->egrCtryAddrType = (uint8_t *) strdup("")) != NULL) {
			StorageNew->egrCtryAddrTypeLen = strlen("");
		}
		StorageNew->ingPartition = 0;
		StorageNew->egrPartition = 0;
		StorageNew->ingClngPtyCat = 0;
		StorageNew->ingRlsCauseLctn = 0;
		StorageNew->egrRlsCauseLctn = 0;
		StorageNew->clldPtyCat = 0;
		StorageNew->callDuration = 0;
		StorageNew->ingRlsDrctn = 0;
		StorageNew->egrRlsDrctn = 0;
		if ((StorageNew->ingAnsLctim = (uint8_t *) strdup("")) != NULL) {
			StorageNew->ingAnsLctimLen = strlen("");
		}
		if ((StorageNew->egrAnsLctim = (uint8_t *) strdup("")) != NULL) {
			StorageNew->egrAnsLctimLen = strlen("");
		}
		StorageNew->ingHlcChrsId = 0;
		StorageNew->egrHlcChrsId = 0;
		StorageNew->ingLlcXferCap = 0;
		StorageNew->egrLlcXferCap = 0;
		if ((StorageNew->routeList = (uint8_t *) strdup("")) != NULL) {
			StorageNew->routeListLen = strlen("");
		}
		StorageNew->ingPointCode = 0;
		StorageNew->egrPointCode = 0;

	}
	DEBUGMSGTL(("callDetailLogRecordR2Table", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct callDetailLogRecordR2Table_data *callDetailLogRecordR2Table_duplicate(struct callDetailLogRecordR2Table_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicat a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct callDetailLogRecordR2Table_data *
callDetailLogRecordR2Table_duplicate(struct callDetailLogRecordR2Table_data *thedata)
{
	struct callDetailLogRecordR2Table_data *StorageNew = SNMP_MALLOC_STRUCT(callDetailLogRecordR2Table_data);

	DEBUGMSGTL(("callDetailLogRecordR2Table", "duplicating row...  "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("callDetailLogRecordR2Table", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	callDetailLogRecordR2Table_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int callDetailLogRecordR2Table_destroy(struct callDetailLogRecordR2Table_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
callDetailLogRecordR2Table_destroy(struct callDetailLogRecordR2Table_data **thedata)
{
	struct callDetailLogRecordR2Table_data *StorageDel;

	DEBUGMSGTL(("callDetailLogRecordR2Table", "deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->logId);
		StorageDel->logIdLen = 0;
		SNMP_FREE(StorageDel->logRecordId);
		StorageDel->logRecordIdLen = 0;
		SNMP_FREE(StorageDel->callId);
		StorageDel->callIdLen = 0;
		SNMP_FREE(StorageDel->lastUpdate);
		StorageDel->lastUpdateLen = 0;
		SNMP_FREE(StorageDel->eventOrder);
		StorageDel->eventOrderLen = 0;
		SNMP_FREE(StorageDel->clngPtyNbr);
		StorageDel->clngPtyNbrLen = 0;
		SNMP_FREE(StorageDel->chargeNbr);
		StorageDel->chargeNbrLen = 0;
		SNMP_FREE(StorageDel->clldPtyNbr);
		StorageDel->clldPtyNbrLen = 0;
		SNMP_FREE(StorageDel->origLnInfo);
		StorageDel->origLnInfoLen = 0;
		SNMP_FREE(StorageDel->ingLrn);
		StorageDel->ingLrnLen = 0;
		SNMP_FREE(StorageDel->ingCic);
		StorageDel->ingCicLen = 0;
		SNMP_FREE(StorageDel->ingCallStart);
		StorageDel->ingCallStartLen = 0;
		SNMP_FREE(StorageDel->ingCreatConnCplt);
		StorageDel->ingCreatConnCpltLen = 0;
		SNMP_FREE(StorageDel->ingAddrCplt);
		StorageDel->ingAddrCpltLen = 0;
		SNMP_FREE(StorageDel->ingCallAns);
		StorageDel->ingCallAnsLen = 0;
		SNMP_FREE(StorageDel->transNbr);
		StorageDel->transNbrLen = 0;
		SNMP_FREE(StorageDel->termLrn);
		StorageDel->termLrnLen = 0;
		SNMP_FREE(StorageDel->transCic);
		StorageDel->transCicLen = 0;
		SNMP_FREE(StorageDel->ingCallRls);
		StorageDel->ingCallRlsLen = 0;
		SNMP_FREE(StorageDel->egrCallStart);
		StorageDel->egrCallStartLen = 0;
		SNMP_FREE(StorageDel->egrCreatConnCplt);
		StorageDel->egrCreatConnCpltLen = 0;
		SNMP_FREE(StorageDel->egrAddrCplt);
		StorageDel->egrAddrCpltLen = 0;
		SNMP_FREE(StorageDel->egrCallAns);
		StorageDel->egrCallAnsLen = 0;
		SNMP_FREE(StorageDel->egrCallRls);
		StorageDel->egrCallRlsLen = 0;
		SNMP_FREE(StorageDel->chkptFirst);
		StorageDel->chkptFirstLen = 0;
		SNMP_FREE(StorageDel->chkptLast);
		StorageDel->chkptLastLen = 0;
		SNMP_FREE(StorageDel->ingGateName);
		StorageDel->ingGateNameLen = 0;
		SNMP_FREE(StorageDel->egrGateName);
		StorageDel->egrGateNameLen = 0;
		SNMP_FREE(StorageDel->ingTgName);
		StorageDel->ingTgNameLen = 0;
		SNMP_FREE(StorageDel->egrTgName);
		StorageDel->egrTgNameLen = 0;
		SNMP_FREE(StorageDel->origGateIp);
		StorageDel->origGateIpLen = 0;
		SNMP_FREE(StorageDel->termGateIp);
		StorageDel->termGateIpLen = 0;
		SNMP_FREE(StorageDel->h323ConfId);
		StorageDel->h323ConfIdLen = 0;
		SNMP_FREE(StorageDel->dialedNbr);
		StorageDel->dialedNbrLen = 0;
		SNMP_FREE(StorageDel->redirNbr);
		StorageDel->redirNbrLen = 0;
		SNMP_FREE(StorageDel->calledNbr);
		StorageDel->calledNbrLen = 0;
		SNMP_FREE(StorageDel->ingJuris);
		StorageDel->ingJurisLen = 0;
		SNMP_FREE(StorageDel->egrJuris);
		StorageDel->egrJurisLen = 0;
		SNMP_FREE(StorageDel->transNetSelCarr);
		StorageDel->transNetSelCarrLen = 0;
		SNMP_FREE(StorageDel->ingRlsCauseIntrnl);
		StorageDel->ingRlsCauseIntrnlLen = 0;
		SNMP_FREE(StorageDel->egrRlsCauseIntrnl);
		StorageDel->egrRlsCauseIntrnlLen = 0;
		SNMP_FREE(StorageDel->egrClldNbr);
		StorageDel->egrClldNbrLen = 0;
		SNMP_FREE(StorageDel->egrCnndNbr);
		StorageDel->egrCnndNbrLen = 0;
		SNMP_FREE(StorageDel->ingCallidExtrnl);
		StorageDel->ingCallidExtrnlLen = 0;
		SNMP_FREE(StorageDel->egrCallidExtrnl);
		StorageDel->egrCallidExtrnlLen = 0;
		SNMP_FREE(StorageDel->ingChargeInfo);
		StorageDel->ingChargeInfoLen = 0;
		SNMP_FREE(StorageDel->egrChargeInfo);
		StorageDel->egrChargeInfoLen = 0;
		SNMP_FREE(StorageDel->ingPrtlInd);
		StorageDel->ingPrtlIndLen = 0;
		SNMP_FREE(StorageDel->ingLastDvrtLnDigs);
		StorageDel->ingLastDvrtLnDigsLen = 0;
		SNMP_FREE(StorageDel->j7IngAddlPtyCat);
		StorageDel->j7IngAddlPtyCatLen = 0;
		SNMP_FREE(StorageDel->j7IngChgAreaInfo);
		StorageDel->j7IngChgAreaInfoLen = 0;
		SNMP_FREE(StorageDel->j7IngFwdCallInd);
		StorageDel->j7IngFwdCallIndLen = 0;
		SNMP_FREE(StorageDel->j7EgrAddlPtyCat);
		StorageDel->j7EgrAddlPtyCatLen = 0;
		SNMP_FREE(StorageDel->j7EgrChgAreaInfo);
		StorageDel->j7EgrChgAreaInfoLen = 0;
		SNMP_FREE(StorageDel->j7EgrBkwCallInd);
		StorageDel->j7EgrBkwCallIndLen = 0;
		SNMP_FREE(StorageDel->j7CarrInfoXfer);
		StorageDel->j7CarrInfoXferLen = 0;
		SNMP_FREE(StorageDel->ingSs7GnrcParm);
		StorageDel->ingSs7GnrcParmLen = 0;
		SNMP_FREE(StorageDel->egrSs7GnrcParm);
		StorageDel->egrSs7GnrcParmLen = 0;
		SNMP_FREE(StorageDel->ingCasCrctSzr);
		StorageDel->ingCasCrctSzrLen = 0;
		SNMP_FREE(StorageDel->egrCasCrctSzr);
		StorageDel->egrCasCrctSzrLen = 0;
		SNMP_FREE(StorageDel->ingZz);
		StorageDel->ingZzLen = 0;
		SNMP_FREE(StorageDel->egrZz);
		StorageDel->egrZzLen = 0;
		SNMP_FREE(StorageDel->ingCtryAddrType);
		StorageDel->ingCtryAddrTypeLen = 0;
		SNMP_FREE(StorageDel->egrCtryAddrType);
		StorageDel->egrCtryAddrTypeLen = 0;
		SNMP_FREE(StorageDel->ingAnsLctim);
		StorageDel->ingAnsLctimLen = 0;
		SNMP_FREE(StorageDel->egrAnsLctim);
		StorageDel->egrAnsLctimLen = 0;
		SNMP_FREE(StorageDel->routeList);
		StorageDel->routeListLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("callDetailLogRecordR2Table", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int callDetailLogRecordR2Table_add(struct callDetailLogRecordR2Table_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the callDetailLogRecordR2Table table data set.
 *
 * Adds a table row structure to the callDetailLogRecordR2Table table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
callDetailLogRecordR2Table_add(struct callDetailLogRecordR2Table_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("callDetailLogRecordR2Table", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index the data */
	/* logId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->logId, thedata->logIdLen);
	/* logRecordId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->logRecordId, thedata->logRecordIdLen);
	header_complex_add_data(&callDetailLogRecordR2TableStorage, vars, thedata);
	DEBUGMSGTL(("callDetailLogRecordR2Table", "registered an entry\n"));
	DEBUGMSGTL(("callDetailLogRecordR2Table", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int callDetailLogRecordR2Table_del(struct callDetailLogRecordR2Table_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the callDetailLogRecordR2Table table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
callDetailLogRecordR2Table_del(struct callDetailLogRecordR2Table_data *thedata)
{
	struct callDetailLogRecordR2Table_data *StorageDel;

	DEBUGMSGTL(("callDetailLogRecordR2Table", "deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(callDetailLogRecordR2TableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&callDetailLogRecordR2TableStorage, hciptr);
	}
	DEBUGMSGTL(("callDetailLogRecordR2Table", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_callDetailLogRecordR2Table(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for callDetailLogRecordR2Table entries.
 *
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case callDetailLogRecordR2Table).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_callDetailLogRecordR2Table(const char *token, char *line)
{
	size_t tmpsize;
	struct callDetailLogRecordR2Table_data *StorageTmp = callDetailLogRecordR2Table_create();

	DEBUGMSGTL(("callDetailLogRecordR2Table", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->logId, &StorageTmp->logIdLen);
	if (StorageTmp->logId == NULL) {
		config_perror("invalid specification for logId");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->logRecordId, &StorageTmp->logRecordIdLen);
	if (StorageTmp->logRecordId == NULL) {
		config_perror("invalid specification for logRecordId");
		return;
	}
	line = read_config_read_data(ASN_COUNTER64, line, &StorageTmp->sequenceId, &tmpsize);
	line = read_config_read_data(ASN_COUNTER64, line, &StorageTmp->zoneId, &tmpsize);
	line = read_config_read_data(ASN_COUNTER64, line, &StorageTmp->recSeqId, &tmpsize);
	line = read_config_read_data(ASN_COUNTER64, line, &StorageTmp->fileSeqId, &tmpsize);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->callId, &StorageTmp->callIdLen);
	if (StorageTmp->callId == NULL) {
		config_perror("invalid specification for callId");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->lastUpdate, &StorageTmp->lastUpdateLen);
	if (StorageTmp->lastUpdate == NULL) {
		config_perror("invalid specification for lastUpdate");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->eventOrder, &StorageTmp->eventOrderLen);
	if (StorageTmp->eventOrder == NULL) {
		config_perror("invalid specification for eventOrder");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->status, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->callType, &tmpsize);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->clngPtyNbr, &StorageTmp->clngPtyNbrLen);
	if (StorageTmp->clngPtyNbr == NULL) {
		config_perror("invalid specification for clngPtyNbr");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->chargeNbr, &StorageTmp->chargeNbrLen);
	if (StorageTmp->chargeNbr == NULL) {
		config_perror("invalid specification for chargeNbr");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->clldPtyNbr, &StorageTmp->clldPtyNbrLen);
	if (StorageTmp->clldPtyNbr == NULL) {
		config_perror("invalid specification for clldPtyNbr");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->clldPtyNoa, &tmpsize);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->origLnInfo, &StorageTmp->origLnInfoLen);
	if (StorageTmp->origLnInfo == NULL) {
		config_perror("invalid specification for origLnInfo");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ingLrn, &StorageTmp->ingLrnLen);
	if (StorageTmp->ingLrn == NULL) {
		config_perror("invalid specification for ingLrn");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ingCic, &StorageTmp->ingCicLen);
	if (StorageTmp->ingCic == NULL) {
		config_perror("invalid specification for ingCic");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ingCsi, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ingCceId, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ingTgProt, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ingTgType, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ingTgId, &tmpsize);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ingCallStart, &StorageTmp->ingCallStartLen);
	if (StorageTmp->ingCallStart == NULL) {
		config_perror("invalid specification for ingCallStart");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ingGateId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ingCardId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ingSpanId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ingChanId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ingIsdnDChan, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ingIsdnCrn, &tmpsize);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ingCreatConnCplt, &StorageTmp->ingCreatConnCpltLen);
	if (StorageTmp->ingCreatConnCplt == NULL) {
		config_perror("invalid specification for ingCreatConnCplt");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ingAddrCplt, &StorageTmp->ingAddrCpltLen);
	if (StorageTmp->ingAddrCplt == NULL) {
		config_perror("invalid specification for ingAddrCplt");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ingCallAns, &StorageTmp->ingCallAnsLen);
	if (StorageTmp->ingCallAns == NULL) {
		config_perror("invalid specification for ingCallAns");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->transNbr, &StorageTmp->transNbrLen);
	if (StorageTmp->transNbr == NULL) {
		config_perror("invalid specification for transNbr");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->termLrn, &StorageTmp->termLrnLen);
	if (StorageTmp->termLrn == NULL) {
		config_perror("invalid specification for termLrn");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->transCic, &StorageTmp->transCicLen);
	if (StorageTmp->transCic == NULL) {
		config_perror("invalid specification for transCic");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ingCallRls, &StorageTmp->ingCallRlsLen);
	if (StorageTmp->ingCallRls == NULL) {
		config_perror("invalid specification for ingCallRls");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ingRlsCause, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->egrCceId, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->egrTgProt, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->egrTgType, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->egrTgId, &tmpsize);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->egrCallStart, &StorageTmp->egrCallStartLen);
	if (StorageTmp->egrCallStart == NULL) {
		config_perror("invalid specification for egrCallStart");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->egrGateId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->egrCardId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->egrSpanId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->egrChanId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->egrIsdnDChan, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->egrIsdnCrn, &tmpsize);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->egrCreatConnCplt, &StorageTmp->egrCreatConnCpltLen);
	if (StorageTmp->egrCreatConnCplt == NULL) {
		config_perror("invalid specification for egrCreatConnCplt");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->egrAddrCplt, &StorageTmp->egrAddrCpltLen);
	if (StorageTmp->egrAddrCplt == NULL) {
		config_perror("invalid specification for egrAddrCplt");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->egrCallAns, &StorageTmp->egrCallAnsLen);
	if (StorageTmp->egrCallAns == NULL) {
		config_perror("invalid specification for egrCallAns");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->egrCallRls, &StorageTmp->egrCallRlsLen);
	if (StorageTmp->egrCallRls == NULL) {
		config_perror("invalid specification for egrCallRls");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->egrRlsCause, &tmpsize);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->chkptFirst, &StorageTmp->chkptFirstLen);
	if (StorageTmp->chkptFirst == NULL) {
		config_perror("invalid specification for chkptFirst");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->chkptLast, &StorageTmp->chkptLastLen);
	if (StorageTmp->chkptLast == NULL) {
		config_perror("invalid specification for chkptLast");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ingGateName, &StorageTmp->ingGateNameLen);
	if (StorageTmp->ingGateName == NULL) {
		config_perror("invalid specification for ingGateName");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->egrGateName, &StorageTmp->egrGateNameLen);
	if (StorageTmp->egrGateName == NULL) {
		config_perror("invalid specification for egrGateName");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ingTgName, &StorageTmp->ingTgNameLen);
	if (StorageTmp->ingTgName == NULL) {
		config_perror("invalid specification for ingTgName");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->egrTgName, &StorageTmp->egrTgNameLen);
	if (StorageTmp->egrTgName == NULL) {
		config_perror("invalid specification for egrTgName");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->origGateIp, &StorageTmp->origGateIpLen);
	if (StorageTmp->origGateIp == NULL) {
		config_perror("invalid specification for origGateIp");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->termGateIp, &StorageTmp->termGateIpLen);
	if (StorageTmp->termGateIp == NULL) {
		config_perror("invalid specification for termGateIp");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->h323ConfId, &StorageTmp->h323ConfIdLen);
	if (StorageTmp->h323ConfId == NULL) {
		config_perror("invalid specification for h323ConfId");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ingCardPort, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ingCardPath, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->egrCardPort, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->egrCardPath, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ingTg, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->egrTg, &tmpsize);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->dialedNbr, &StorageTmp->dialedNbrLen);
	if (StorageTmp->dialedNbr == NULL) {
		config_perror("invalid specification for dialedNbr");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->dialedNoa, &tmpsize);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->redirNbr, &StorageTmp->redirNbrLen);
	if (StorageTmp->redirNbr == NULL) {
		config_perror("invalid specification for redirNbr");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->redirNoa, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->redirInd, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->redirRsn, &tmpsize);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->calledNbr, &StorageTmp->calledNbrLen);
	if (StorageTmp->calledNbr == NULL) {
		config_perror("invalid specification for calledNbr");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->calledNoa, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->redirRsnOrig, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->redirCntr, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->redirPrsntnInd, &tmpsize);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ingJuris, &StorageTmp->ingJurisLen);
	if (StorageTmp->ingJuris == NULL) {
		config_perror("invalid specification for ingJuris");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->egrJuris, &StorageTmp->egrJurisLen);
	if (StorageTmp->egrJuris == NULL) {
		config_perror("invalid specification for egrJuris");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ingTrunkBearCap, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->egrTrunkBearCap, &tmpsize);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->transNetSelCarr, &StorageTmp->transNetSelCarrLen);
	if (StorageTmp->transNetSelCarr == NULL) {
		config_perror("invalid specification for transNetSelCarr");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->clngPtyNoa, &tmpsize);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ingRlsCauseIntrnl, &StorageTmp->ingRlsCauseIntrnlLen);
	if (StorageTmp->ingRlsCauseIntrnl == NULL) {
		config_perror("invalid specification for ingRlsCauseIntrnl");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->egrRlsCauseIntrnl, &StorageTmp->egrRlsCauseIntrnlLen);
	if (StorageTmp->egrRlsCauseIntrnl == NULL) {
		config_perror("invalid specification for egrRlsCauseIntrnl");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->egrClldNbr, &StorageTmp->egrClldNbrLen);
	if (StorageTmp->egrClldNbr == NULL) {
		config_perror("invalid specification for egrClldNbr");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->egrClldNoa, &tmpsize);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->egrCnndNbr, &StorageTmp->egrCnndNbrLen);
	if (StorageTmp->egrCnndNbr == NULL) {
		config_perror("invalid specification for egrCnndNbr");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->egrCnndNoa, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->clngPtyPrsntnInd, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ingIri, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->egrOri, &tmpsize);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ingCallidExtrnl, &StorageTmp->ingCallidExtrnlLen);
	if (StorageTmp->ingCallidExtrnl == NULL) {
		config_perror("invalid specification for ingCallidExtrnl");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->egrCallidExtrnl, &StorageTmp->egrCallidExtrnlLen);
	if (StorageTmp->egrCallidExtrnl == NULL) {
		config_perror("invalid specification for egrCallidExtrnl");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ingChargeInfo, &StorageTmp->ingChargeInfoLen);
	if (StorageTmp->ingChargeInfo == NULL) {
		config_perror("invalid specification for ingChargeInfo");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->egrChargeInfo, &StorageTmp->egrChargeInfoLen);
	if (StorageTmp->egrChargeInfo == NULL) {
		config_perror("invalid specification for egrChargeInfo");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ingPrtlInd, &StorageTmp->ingPrtlIndLen);
	if (StorageTmp->ingPrtlInd == NULL) {
		config_perror("invalid specification for ingPrtlInd");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ingNatfwdCliblkInd, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ingNatfwdNtaInd, &tmpsize);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ingLastDvrtLnDigs, &StorageTmp->ingLastDvrtLnDigsLen);
	if (StorageTmp->ingLastDvrtLnDigs == NULL) {
		config_perror("invalid specification for ingLastDvrtLnDigs");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ingLastDvrtLnNoa, &tmpsize);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->j7IngAddlPtyCat, &StorageTmp->j7IngAddlPtyCatLen);
	if (StorageTmp->j7IngAddlPtyCat == NULL) {
		config_perror("invalid specification for j7IngAddlPtyCat");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->j7IngChgAreaInfo, &StorageTmp->j7IngChgAreaInfoLen);
	if (StorageTmp->j7IngChgAreaInfo == NULL) {
		config_perror("invalid specification for j7IngChgAreaInfo");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->j7IngFwdCallInd, &StorageTmp->j7IngFwdCallIndLen);
	if (StorageTmp->j7IngFwdCallInd == NULL) {
		config_perror("invalid specification for j7IngFwdCallInd");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->j7EgrAddlPtyCat, &StorageTmp->j7EgrAddlPtyCatLen);
	if (StorageTmp->j7EgrAddlPtyCat == NULL) {
		config_perror("invalid specification for j7EgrAddlPtyCat");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->j7EgrChgAreaInfo, &StorageTmp->j7EgrChgAreaInfoLen);
	if (StorageTmp->j7EgrChgAreaInfo == NULL) {
		config_perror("invalid specification for j7EgrChgAreaInfo");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->j7EgrBkwCallInd, &StorageTmp->j7EgrBkwCallIndLen);
	if (StorageTmp->j7EgrBkwCallInd == NULL) {
		config_perror("invalid specification for j7EgrBkwCallInd");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->j7CarrInfoXfer, &StorageTmp->j7CarrInfoXferLen);
	if (StorageTmp->j7CarrInfoXfer == NULL) {
		config_perror("invalid specification for j7CarrInfoXfer");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ingSs7GnrcParm, &StorageTmp->ingSs7GnrcParmLen);
	if (StorageTmp->ingSs7GnrcParm == NULL) {
		config_perror("invalid specification for ingSs7GnrcParm");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->egrSs7GnrcParm, &StorageTmp->egrSs7GnrcParmLen);
	if (StorageTmp->egrSs7GnrcParm == NULL) {
		config_perror("invalid specification for egrSs7GnrcParm");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ingPktsSent, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ingPktsRcvd, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ingPktsLost, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ingPktsXfer, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ingJitter, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ingLtncy, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->egrPktsSent, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->egrPktsRcvd, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->egrPktsLost, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->egrPktsXfer, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->egrJitter, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->egrLtncy, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ingCrctId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->egrCrctId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ingCodec, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->egrCodec, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ingLocGateId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->egrLocGateId, &tmpsize);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ingCasCrctSzr, &StorageTmp->ingCasCrctSzrLen);
	if (StorageTmp->ingCasCrctSzr == NULL) {
		config_perror("invalid specification for ingCasCrctSzr");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->egrCasCrctSzr, &StorageTmp->egrCasCrctSzrLen);
	if (StorageTmp->egrCasCrctSzr == NULL) {
		config_perror("invalid specification for egrCasCrctSzr");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ingZz, &StorageTmp->ingZzLen);
	if (StorageTmp->ingZz == NULL) {
		config_perror("invalid specification for ingZz");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->egrZz, &StorageTmp->egrZzLen);
	if (StorageTmp->egrZz == NULL) {
		config_perror("invalid specification for egrZz");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ingCtryAddrType, &StorageTmp->ingCtryAddrTypeLen);
	if (StorageTmp->ingCtryAddrType == NULL) {
		config_perror("invalid specification for ingCtryAddrType");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->egrCtryAddrType, &StorageTmp->egrCtryAddrTypeLen);
	if (StorageTmp->egrCtryAddrType == NULL) {
		config_perror("invalid specification for egrCtryAddrType");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ingPartition, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->egrPartition, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ingClngPtyCat, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ingRlsCauseLctn, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->egrRlsCauseLctn, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->clldPtyCat, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->callDuration, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ingRlsDrctn, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->egrRlsDrctn, &tmpsize);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->ingAnsLctim, &StorageTmp->ingAnsLctimLen);
	if (StorageTmp->ingAnsLctim == NULL) {
		config_perror("invalid specification for ingAnsLctim");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->egrAnsLctim, &StorageTmp->egrAnsLctimLen);
	if (StorageTmp->egrAnsLctim == NULL) {
		config_perror("invalid specification for egrAnsLctim");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ingHlcChrsId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->egrHlcChrsId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ingLlcXferCap, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->egrLlcXferCap, &tmpsize);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->routeList, &StorageTmp->routeListLen);
	if (StorageTmp->routeList == NULL) {
		config_perror("invalid specification for routeList");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->ingPointCode, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->egrPointCode, &tmpsize);
	callDetailLogRecordR2Table_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("callDetailLogRecordR2Table", "done.\n"));
}

/*
 * store_callDetailLogRecordR2Table(): store configuraiton file for callDetailLogRecordR2Table
 * stores .conf file entries needed to configure the mib.
 */
int
store_callDetailLogRecordR2Table(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct callDetailLogRecordR2Table_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("callDetailLogRecordR2Table", "storing data...  "));
	refresh_callDetailLogRecordR2Table(1);
	(void) tmpsize;
	for (hcindex = callDetailLogRecordR2TableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct callDetailLogRecordR2Table_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "callDetailLogRecordR2Table ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->logId, &StorageTmp->logIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->logRecordId, &StorageTmp->logRecordIdLen);
			cptr = read_config_store_data(ASN_COUNTER64, cptr, &StorageTmp->sequenceId, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER64, cptr, &StorageTmp->zoneId, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER64, cptr, &StorageTmp->recSeqId, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER64, cptr, &StorageTmp->fileSeqId, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->callId, &StorageTmp->callIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->lastUpdate, &StorageTmp->lastUpdateLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->eventOrder, &StorageTmp->eventOrderLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->status, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->callType, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->clngPtyNbr, &StorageTmp->clngPtyNbrLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->chargeNbr, &StorageTmp->chargeNbrLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->clldPtyNbr, &StorageTmp->clldPtyNbrLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->clldPtyNoa, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->origLnInfo, &StorageTmp->origLnInfoLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ingLrn, &StorageTmp->ingLrnLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ingCic, &StorageTmp->ingCicLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ingCsi, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ingCceId, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ingTgProt, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ingTgType, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ingTgId, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ingCallStart, &StorageTmp->ingCallStartLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ingGateId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ingCardId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ingSpanId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ingChanId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ingIsdnDChan, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ingIsdnCrn, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ingCreatConnCplt, &StorageTmp->ingCreatConnCpltLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ingAddrCplt, &StorageTmp->ingAddrCpltLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ingCallAns, &StorageTmp->ingCallAnsLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->transNbr, &StorageTmp->transNbrLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->termLrn, &StorageTmp->termLrnLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->transCic, &StorageTmp->transCicLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ingCallRls, &StorageTmp->ingCallRlsLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ingRlsCause, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->egrCceId, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->egrTgProt, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->egrTgType, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->egrTgId, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->egrCallStart, &StorageTmp->egrCallStartLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->egrGateId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->egrCardId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->egrSpanId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->egrChanId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->egrIsdnDChan, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->egrIsdnCrn, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->egrCreatConnCplt, &StorageTmp->egrCreatConnCpltLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->egrAddrCplt, &StorageTmp->egrAddrCpltLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->egrCallAns, &StorageTmp->egrCallAnsLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->egrCallRls, &StorageTmp->egrCallRlsLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->egrRlsCause, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->chkptFirst, &StorageTmp->chkptFirstLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->chkptLast, &StorageTmp->chkptLastLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ingGateName, &StorageTmp->ingGateNameLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->egrGateName, &StorageTmp->egrGateNameLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ingTgName, &StorageTmp->ingTgNameLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->egrTgName, &StorageTmp->egrTgNameLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->origGateIp, &StorageTmp->origGateIpLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->termGateIp, &StorageTmp->termGateIpLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->h323ConfId, &StorageTmp->h323ConfIdLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ingCardPort, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ingCardPath, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->egrCardPort, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->egrCardPath, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ingTg, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->egrTg, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->dialedNbr, &StorageTmp->dialedNbrLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->dialedNoa, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->redirNbr, &StorageTmp->redirNbrLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->redirNoa, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->redirInd, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->redirRsn, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->calledNbr, &StorageTmp->calledNbrLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->calledNoa, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->redirRsnOrig, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->redirCntr, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->redirPrsntnInd, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ingJuris, &StorageTmp->ingJurisLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->egrJuris, &StorageTmp->egrJurisLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ingTrunkBearCap, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->egrTrunkBearCap, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->transNetSelCarr, &StorageTmp->transNetSelCarrLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->clngPtyNoa, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ingRlsCauseIntrnl, &StorageTmp->ingRlsCauseIntrnlLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->egrRlsCauseIntrnl, &StorageTmp->egrRlsCauseIntrnlLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->egrClldNbr, &StorageTmp->egrClldNbrLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->egrClldNoa, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->egrCnndNbr, &StorageTmp->egrCnndNbrLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->egrCnndNoa, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->clngPtyPrsntnInd, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ingIri, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->egrOri, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ingCallidExtrnl, &StorageTmp->ingCallidExtrnlLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->egrCallidExtrnl, &StorageTmp->egrCallidExtrnlLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ingChargeInfo, &StorageTmp->ingChargeInfoLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->egrChargeInfo, &StorageTmp->egrChargeInfoLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ingPrtlInd, &StorageTmp->ingPrtlIndLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ingNatfwdCliblkInd, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ingNatfwdNtaInd, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ingLastDvrtLnDigs, &StorageTmp->ingLastDvrtLnDigsLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ingLastDvrtLnNoa, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->j7IngAddlPtyCat, &StorageTmp->j7IngAddlPtyCatLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->j7IngChgAreaInfo, &StorageTmp->j7IngChgAreaInfoLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->j7IngFwdCallInd, &StorageTmp->j7IngFwdCallIndLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->j7EgrAddlPtyCat, &StorageTmp->j7EgrAddlPtyCatLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->j7EgrChgAreaInfo, &StorageTmp->j7EgrChgAreaInfoLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->j7EgrBkwCallInd, &StorageTmp->j7EgrBkwCallIndLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->j7CarrInfoXfer, &StorageTmp->j7CarrInfoXferLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ingSs7GnrcParm, &StorageTmp->ingSs7GnrcParmLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->egrSs7GnrcParm, &StorageTmp->egrSs7GnrcParmLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ingPktsSent, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ingPktsRcvd, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ingPktsLost, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ingPktsXfer, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ingJitter, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ingLtncy, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->egrPktsSent, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->egrPktsRcvd, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->egrPktsLost, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->egrPktsXfer, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->egrJitter, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->egrLtncy, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ingCrctId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->egrCrctId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ingCodec, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->egrCodec, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ingLocGateId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->egrLocGateId, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ingCasCrctSzr, &StorageTmp->ingCasCrctSzrLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->egrCasCrctSzr, &StorageTmp->egrCasCrctSzrLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ingZz, &StorageTmp->ingZzLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->egrZz, &StorageTmp->egrZzLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ingCtryAddrType, &StorageTmp->ingCtryAddrTypeLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->egrCtryAddrType, &StorageTmp->egrCtryAddrTypeLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ingPartition, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->egrPartition, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ingClngPtyCat, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ingRlsCauseLctn, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->egrRlsCauseLctn, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->clldPtyCat, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->callDuration, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ingRlsDrctn, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->egrRlsDrctn, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->ingAnsLctim, &StorageTmp->ingAnsLctimLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->egrAnsLctim, &StorageTmp->egrAnsLctimLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ingHlcChrsId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->egrHlcChrsId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ingLlcXferCap, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->egrLlcXferCap, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->routeList, &StorageTmp->routeListLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->ingPointCode, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->egrPointCode, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("callDetailLogRecordR2Table", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void refresh_callDetailDataTable_row(struct callDetailDataTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the callDetailDataTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
void
refresh_callDetailDataTable_row(struct callDetailDataTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->callDetailDataTable_request == sa_request))
		return;
	StorageTmp->callDetailDataTable_request = sa_request;
}

/**
 * @fn void refresh_callDetailDataTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the callDetailDataTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_callDetailDataTable(int force)
{
	if (!force && callDetailDataTable_refresh == 0)
		return;
	callDetailDataTable_refresh = 0;
	/* XXX: Here, update the table as required... */
}

/**
 * @fn u_char *var_callDetailDataTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in callDetailDataTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_cdr2MIB above.
 */
u_char *
var_callDetailDataTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct callDetailDataTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("cdr2MIB", "var_callDetailDataTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_callDetailDataTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	StorageTmp = header_complex(callDetailDataTableStorage, vp, name, length, exact, var_len, write_method);
	refresh_callDetailDataTable_row(StorageTmp, 0);
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn void refresh_simpleUsageMeteringControlTable_row(struct simpleUsageMeteringControlTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the simpleUsageMeteringControlTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
void
refresh_simpleUsageMeteringControlTable_row(struct simpleUsageMeteringControlTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->simpleUsageMeteringControlTable_request == sa_request))
		return;
	StorageTmp->simpleUsageMeteringControlTable_request = sa_request;
}

/**
 * @fn void refresh_simpleUsageMeteringControlTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the simpleUsageMeteringControlTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_simpleUsageMeteringControlTable(int force)
{
	if (!force && simpleUsageMeteringControlTable_refresh == 0)
		return;
	simpleUsageMeteringControlTable_refresh = 0;
	/* XXX: Here, update the table as required... */
}

/**
 * @fn u_char *var_simpleUsageMeteringControlTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in simpleUsageMeteringControlTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_cdr2MIB above.
 */
u_char *
var_simpleUsageMeteringControlTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct simpleUsageMeteringControlTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("cdr2MIB", "var_simpleUsageMeteringControlTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_simpleUsageMeteringControlTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	StorageTmp = header_complex(simpleUsageMeteringControlTableStorage, vp, name, length, exact, var_len, write_method);
	refresh_simpleUsageMeteringControlTable_row(StorageTmp, 0);
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) CREATIONTRIGGERLIST:	/* ReadWrite */
		*write_method = write_creationTriggerList;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->creationTriggerListLen;
		rval = (u_char *) StorageTmp->creationTriggerList;
		break;
	case (u_char) SIMPLEUSAGEMETERINGCONTROLENTRYSTATUS:	/* ReadWrite */
		*write_method = write_simpleUsageMeteringControlEntryStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->simpleUsageMeteringControlEntryStatus);
		rval = (u_char *) &StorageTmp->simpleUsageMeteringControlEntryStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn void refresh_configurableSimpleUsageMeteringControlTable_row(struct configurableSimpleUsageMeteringControlTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the configurableSimpleUsageMeteringControlTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
void
refresh_configurableSimpleUsageMeteringControlTable_row(struct configurableSimpleUsageMeteringControlTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->configurableSimpleUsageMeteringControlTable_request == sa_request))
		return;
	StorageTmp->configurableSimpleUsageMeteringControlTable_request = sa_request;
}

/**
 * @fn void refresh_configurableSimpleUsageMeteringControlTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the configurableSimpleUsageMeteringControlTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_configurableSimpleUsageMeteringControlTable(int force)
{
	if (!force && configurableSimpleUsageMeteringControlTable_refresh == 0)
		return;
	configurableSimpleUsageMeteringControlTable_refresh = 0;
	/* XXX: Here, update the table as required... */
}

/**
 * @fn u_char *var_configurableSimpleUsageMeteringControlTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in configurableSimpleUsageMeteringControlTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_cdr2MIB above.
 */
u_char *
var_configurableSimpleUsageMeteringControlTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct configurableSimpleUsageMeteringControlTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("cdr2MIB", "var_configurableSimpleUsageMeteringControlTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_configurableSimpleUsageMeteringControlTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	StorageTmp = header_complex(configurableSimpleUsageMeteringControlTableStorage, vp, name, length, exact, var_len, write_method);
	refresh_configurableSimpleUsageMeteringControlTable_row(StorageTmp, 0);
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) SAMPLINGRATE:	/* Create */
		*write_method = write_samplingRate;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->samplingRate);
		rval = (u_char *) &StorageTmp->samplingRate;
		break;
	case (u_char) CONFIGURATIONMASK:	/* Create */
		*write_method = write_configurationMask;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->configurationMaskLen;
		rval = (u_char *) StorageTmp->configurationMask;
		break;
	case (u_char) CONFIGURATIONROWSTATUS:	/* Create */
		*write_method = write_configurationRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->configurationRowStatus);
		rval = (u_char *) &StorageTmp->configurationRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn void refresh_blockGeneratingLogTable_row(struct blockGeneratingLogTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the blockGeneratingLogTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
void
refresh_blockGeneratingLogTable_row(struct blockGeneratingLogTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->blockGeneratingLogTable_request == sa_request))
		return;
	StorageTmp->blockGeneratingLogTable_request = sa_request;
}

/**
 * @fn void refresh_blockGeneratingLogTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the blockGeneratingLogTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_blockGeneratingLogTable(int force)
{
	if (!force && blockGeneratingLogTable_refresh == 0)
		return;
	blockGeneratingLogTable_refresh = 0;
	/* XXX: Here, update the table as required... */
}

/**
 * @fn u_char *var_blockGeneratingLogTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in blockGeneratingLogTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_cdr2MIB above.
 */
u_char *
var_blockGeneratingLogTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct blockGeneratingLogTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("cdr2MIB", "var_blockGeneratingLogTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_blockGeneratingLogTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	StorageTmp = header_complex(blockGeneratingLogTableStorage, vp, name, length, exact, var_len, write_method);
	refresh_blockGeneratingLogTable_row(StorageTmp, 0);
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) BLOCKGENERATINGLOGMAXBLOCKSIZE:	/* Create */
		*write_method = write_blockGeneratingLogMaxBlockSize;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->blockGeneratingLogMaxBlockSize);
		rval = (u_char *) &StorageTmp->blockGeneratingLogMaxBlockSize;
		break;
	case (u_char) BLOCKGENERATINGLOGMAXTIMEINTERVAL:	/* Create */
		*write_method = write_blockGeneratingLogMaxTimeInterval;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->blockGeneratingLogMaxTimeInterval);
		rval = (u_char *) &StorageTmp->blockGeneratingLogMaxTimeInterval;
		break;
	case (u_char) BLOCKGENERATINGLOGSTORAGETYPE:	/* Create */
		*write_method = write_blockGeneratingLogStorageType;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->blockGeneratingLogStorageType);
		rval = (u_char *) &StorageTmp->blockGeneratingLogStorageType;
		break;
	case (u_char) BLOCKGENERATINGLOGROWSTATUS:	/* Create */
		*write_method = write_blockGeneratingLogRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->blockGeneratingLogRowStatus);
		rval = (u_char *) &StorageTmp->blockGeneratingLogRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn void refresh_fileGeneratingLogTable_row(struct fileGeneratingLogTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the fileGeneratingLogTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
void
refresh_fileGeneratingLogTable_row(struct fileGeneratingLogTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->fileGeneratingLogTable_request == sa_request))
		return;
	StorageTmp->fileGeneratingLogTable_request = sa_request;
}

/**
 * @fn void refresh_fileGeneratingLogTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the fileGeneratingLogTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_fileGeneratingLogTable(int force)
{
	if (!force && fileGeneratingLogTable_refresh == 0)
		return;
	fileGeneratingLogTable_refresh = 0;
	/* XXX: Here, update the table as required... */
}

/**
 * @fn u_char *var_fileGeneratingLogTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in fileGeneratingLogTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_cdr2MIB above.
 */
u_char *
var_fileGeneratingLogTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct fileGeneratingLogTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("cdr2MIB", "var_fileGeneratingLogTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_fileGeneratingLogTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	StorageTmp = header_complex(fileGeneratingLogTableStorage, vp, name, length, exact, var_len, write_method);
	refresh_fileGeneratingLogTable_row(StorageTmp, 0);
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) TIMESOFDAY:	/* Create */
		*write_method = write_timesOfDay;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->timesOfDayLen;
		rval = (u_char *) StorageTmp->timesOfDay;
		break;
	case (u_char) PERIODICTRIGGER:	/* Create */
		*write_method = write_periodicTrigger;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->periodicTrigger);
		rval = (u_char *) &StorageTmp->periodicTrigger;
		break;
	case (u_char) FILEGENERATINGLOGSTORAGETYPE:	/* Create */
		*write_method = write_fileGeneratingLogStorageType;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->fileGeneratingLogStorageType);
		rval = (u_char *) &StorageTmp->fileGeneratingLogStorageType;
		break;
	case (u_char) FILEGENERATINGLOGENTRYSTATUS:	/* Create */
		*write_method = write_fileGeneratingLogEntryStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->fileGeneratingLogEntryStatus);
		rval = (u_char *) &StorageTmp->fileGeneratingLogEntryStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn void refresh_callDetailLogRecordTable_row(struct callDetailLogRecordTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the callDetailLogRecordTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
void
refresh_callDetailLogRecordTable_row(struct callDetailLogRecordTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->callDetailLogRecordTable_request == sa_request))
		return;
	StorageTmp->callDetailLogRecordTable_request = sa_request;
}

/**
 * @fn void refresh_callDetailLogRecordTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the callDetailLogRecordTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_callDetailLogRecordTable(int force)
{
	if (!force && callDetailLogRecordTable_refresh == 0)
		return;
	callDetailLogRecordTable_refresh = 0;
	/* XXX: Here, update the table as required... */
}

/**
 * @fn u_char *var_callDetailLogRecordTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in callDetailLogRecordTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_cdr2MIB above.
 */
u_char *
var_callDetailLogRecordTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct callDetailLogRecordTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("cdr2MIB", "var_callDetailLogRecordTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_callDetailLogRecordTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	StorageTmp = header_complex(callDetailLogRecordTableStorage, vp, name, length, exact, var_len, write_method);
	refresh_callDetailLogRecordTable_row(StorageTmp, 0);
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) NETWORKPROVIDERID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->networkProviderIdLen;
		rval = (u_char *) StorageTmp->networkProviderId;
		break;
	case (u_char) RECORDTYPE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->recordType);
		rval = (u_char *) &StorageTmp->recordType;
		break;
	case (u_char) SEIZURETIME:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->seizureTimeLen;
		rval = (u_char *) StorageTmp->seizureTime;
		break;
	case (u_char) ANSWERTIME:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->answerTimeLen;
		rval = (u_char *) StorageTmp->answerTime;
		break;
	case (u_char) PARTIALTIME:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->partialTimeLen;
		rval = (u_char *) StorageTmp->partialTime;
		break;
	case (u_char) EVENTTIME:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->eventTimeLen;
		rval = (u_char *) StorageTmp->eventTime;
		break;
	case (u_char) CALLINGPARTYNUMBER:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->callingPartyNumberLen;
		rval = (u_char *) StorageTmp->callingPartyNumber;
		break;
	case (u_char) CALLEDPARTYNUMBER:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->calledPartyNumberLen;
		rval = (u_char *) StorageTmp->calledPartyNumber;
		break;
	case (u_char) REDIRECTINGNUMBER:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->redirectingNumberLen;
		rval = (u_char *) StorageTmp->redirectingNumber;
		break;
	case (u_char) REDIRECTIONNUMBER:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->redirectionNumberLen;
		rval = (u_char *) StorageTmp->redirectionNumber;
		break;
	case (u_char) ORIGINALCALLEDNUMBER:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->originalCalledNumberLen;
		rval = (u_char *) StorageTmp->originalCalledNumber;
		break;
	case (u_char) CALLINGPARTYNUMBERNOTSCREENED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->callingPartyNumberNotScreenedLen;
		rval = (u_char *) StorageTmp->callingPartyNumberNotScreened;
		break;
	case (u_char) OPERATORSPECIFIC1NUMBER:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->operatorSpecific1NumberLen;
		rval = (u_char *) StorageTmp->operatorSpecific1Number;
		break;
	case (u_char) OPERATORSPECIFIC2NUMBER:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->operatorSpecific2NumberLen;
		rval = (u_char *) StorageTmp->operatorSpecific2Number;
		break;
	case (u_char) OPERATORSPECIFIC3NUMBER:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->operatorSpecific3NumberLen;
		rval = (u_char *) StorageTmp->operatorSpecific3Number;
		break;
	case (u_char) BEARERSERVICE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->bearerServiceLen;
		rval = (u_char *) StorageTmp->bearerService;
		break;
	case (u_char) PARTICIPANTSERVICEUSER:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->participantServiceUser);
		rval = (u_char *) &StorageTmp->participantServiceUser;
		break;
	case (u_char) CALLIDENTIFICATIONNUMBER:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->callIdentificationNumberLen;
		rval = (u_char *) StorageTmp->callIdentificationNumber;
		break;
	case (u_char) SUPPLEMENTARYSERVICES:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->supplementaryServicesLen;
		rval = (u_char *) StorageTmp->supplementaryServices;
		break;
	case (u_char) IMMEDIATENOTIFICATIONFORUSAGEMETERING:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->immediateNotificationForUsageMetering);
		rval = (u_char *) &StorageTmp->immediateNotificationForUsageMetering;
		break;
	case (u_char) CAUSE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->causeLen;
		rval = (u_char *) StorageTmp->cause;
		break;
	case (u_char) PERSONALUSERID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->personalUserIdLen;
		rval = (u_char *) StorageTmp->personalUserId;
		break;
	case (u_char) CHARGEDPARTICIPANT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->chargedParticipant);
		rval = (u_char *) &StorageTmp->chargedParticipant;
		break;
	case (u_char) CHARGEDDIRECTORYNUMBER:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->chargedDirectoryNumberLen;
		rval = (u_char *) StorageTmp->chargedDirectoryNumber;
		break;
	case (u_char) PERCENTAGETOBEBILLED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->percentageToBeBilled);
		rval = (u_char *) &StorageTmp->percentageToBeBilled;
		break;
	case (u_char) ACCOUNTCODEINPUT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->accountCodeInputLen;
		rval = (u_char *) StorageTmp->accountCodeInput;
		break;
	case (u_char) INSERVICECODE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->iNServiceCodeLen;
		rval = (u_char *) StorageTmp->iNServiceCode;
		break;
	case (u_char) QUEUETIMESTAMP:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->queueTimeStampLen;
		rval = (u_char *) StorageTmp->queueTimeStamp;
		break;
	case (u_char) QUEUEDURATION:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->queueDuration);
		rval = (u_char *) &StorageTmp->queueDuration;
		break;
	case (u_char) SERVICESPECIFICININFORMATION:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->serviceSpecificINInformationLen;
		rval = (u_char *) StorageTmp->serviceSpecificINInformation;
		break;
	case (u_char) PARTIALRECORDNUMBER:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->partialRecordNumber);
		rval = (u_char *) &StorageTmp->partialRecordNumber;
		break;
	case (u_char) PARTIALRECORDREASON:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->partialRecordReason);
		rval = (u_char *) &StorageTmp->partialRecordReason;
		break;
	case (u_char) EXCHANGEINFO:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->exchangeInfoLen;
		rval = (u_char *) StorageTmp->exchangeInfo;
		break;
	case (u_char) RELATEDCALLNUMBER:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->relatedCallNumberLen;
		rval = (u_char *) StorageTmp->relatedCallNumber;
		break;
	case (u_char) CDRPURPOSE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->cDRPurposeLen;
		rval = (u_char *) StorageTmp->cDRPurpose;
		break;
	case (u_char) PHYSICALLINECODE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->physicalLineCodeLen;
		rval = (u_char *) StorageTmp->physicalLineCode;
		break;
	case (u_char) RECEIVEDDIGITS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->receivedDigitsLen;
		rval = (u_char *) StorageTmp->receivedDigits;
		break;
	case (u_char) OPERATORSPECIFIC1ADDITIONALNUMBER:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->operatorSpecific1AdditionalNumberLen;
		rval = (u_char *) StorageTmp->operatorSpecific1AdditionalNumber;
		break;
	case (u_char) OPERATORSPECIFIC2ADDITIONALNUMBER:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->operatorSpecific2AdditionalNumberLen;
		rval = (u_char *) StorageTmp->operatorSpecific2AdditionalNumber;
		break;
	case (u_char) OPERATORSPECIFIC3ADDITIONALNUMBER:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->operatorSpecific3AdditionalNumberLen;
		rval = (u_char *) StorageTmp->operatorSpecific3AdditionalNumber;
		break;
	case (u_char) CALLINGPARTYCATEGORY:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->callingPartyCategoryLen;
		rval = (u_char *) StorageTmp->callingPartyCategory;
		break;
	case (u_char) CALLINGPARTYTYPE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->callingPartyType);
		rval = (u_char *) &StorageTmp->callingPartyType;
		break;
	case (u_char) CHARGEINFORMATION:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->chargeInformationLen;
		rval = (u_char *) StorageTmp->chargeInformation;
		break;
	case (u_char) PROGRESS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->progressLen;
		rval = (u_char *) StorageTmp->progress;
		break;
	case (u_char) ACCESSDELIVERY:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->accessDeliveryLen;
		rval = (u_char *) StorageTmp->accessDelivery;
		break;
	case (u_char) TRUNKGROUPOUTGOING:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->trunkGroupOutgoingLen;
		rval = (u_char *) StorageTmp->trunkGroupOutgoing;
		break;
	case (u_char) TRUNKGROUPINCOMING:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->trunkGroupIncomingLen;
		rval = (u_char *) StorageTmp->trunkGroupIncoming;
		break;
	case (u_char) FALLBACKBEARERSERVICE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->fallbackBearerServiceLen;
		rval = (u_char *) StorageTmp->fallbackBearerService;
		break;
	case (u_char) TELESERVICE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->teleserviceLen;
		rval = (u_char *) StorageTmp->teleservice;
		break;
	case (u_char) CONVERSATIONTIME:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->conversationTime);
		rval = (u_char *) &StorageTmp->conversationTime;
		break;
	case (u_char) DURATIONTIMEACM:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->durationTimeACM);
		rval = (u_char *) &StorageTmp->durationTimeACM;
		break;
	case (u_char) DURATIONTIMEBANSWER:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->durationTimeBAnswer);
		rval = (u_char *) &StorageTmp->durationTimeBAnswer;
		break;
	case (u_char) DURATIONTIMENOBANSWER:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->durationTimeNoBAnswer);
		rval = (u_char *) &StorageTmp->durationTimeNoBAnswer;
		break;
	case (u_char) UUINFO:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->uUInfoLen;
		rval = (u_char *) StorageTmp->uUInfo;
		break;
	case (u_char) STANDARDEXTENSIONS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->standardExtensionsLen;
		rval = (u_char *) StorageTmp->standardExtensions;
		break;
	case (u_char) RECORDEXTENSIONS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->recordExtensionsLen;
		rval = (u_char *) StorageTmp->recordExtensions;
		break;
	case (u_char) BPARTYCATEGORY:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->bPartyCategoryLen;
		rval = (u_char *) StorageTmp->bPartyCategory;
		break;
	case (u_char) ISUPPREFERRED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->iSUPPreferred);
		rval = (u_char *) &StorageTmp->iSUPPreferred;
		break;
	case (u_char) NETWORKMANAGEMENTCONTROLS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->networkManagementControls);
		rval = (u_char *) &StorageTmp->networkManagementControls;
		break;
	case (u_char) GLARE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->glare);
		rval = (u_char *) &StorageTmp->glare;
		break;
	case (u_char) RECORDID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->recordId);
		rval = (u_char *) &StorageTmp->recordId;
		break;
	case (u_char) DATAVALIDITY:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dataValidity);
		rval = (u_char *) &StorageTmp->dataValidity;
		break;
	case (u_char) CALLSTATUS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->callStatus);
		rval = (u_char *) &StorageTmp->callStatus;
		break;
	case (u_char) CARRIERID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->carrierIdLen;
		rval = (u_char *) StorageTmp->carrierId;
		break;
	case (u_char) DPC:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->dPCLen;
		rval = (u_char *) StorageTmp->dPC;
		break;
	case (u_char) OPC:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->oPCLen;
		rval = (u_char *) StorageTmp->oPC;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn void refresh_configurableSimpleUsageMeteringControlR2Table_row(struct configurableSimpleUsageMeteringControlR2Table_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the configurableSimpleUsageMeteringControlR2Table row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
void
refresh_configurableSimpleUsageMeteringControlR2Table_row(struct configurableSimpleUsageMeteringControlR2Table_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->configurableSimpleUsageMeteringControlR2Table_request == sa_request))
		return;
	StorageTmp->configurableSimpleUsageMeteringControlR2Table_request = sa_request;
}

/**
 * @fn void refresh_configurableSimpleUsageMeteringControlR2Table(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the configurableSimpleUsageMeteringControlR2Table.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_configurableSimpleUsageMeteringControlR2Table(int force)
{
	if (!force && configurableSimpleUsageMeteringControlR2Table_refresh == 0)
		return;
	configurableSimpleUsageMeteringControlR2Table_refresh = 0;
	/* XXX: Here, update the table as required... */
}

/**
 * @fn u_char *var_configurableSimpleUsageMeteringControlR2Table(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in configurableSimpleUsageMeteringControlR2Table.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_cdr2MIB above.
 */
u_char *
var_configurableSimpleUsageMeteringControlR2Table(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct configurableSimpleUsageMeteringControlR2Table_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("cdr2MIB", "var_configurableSimpleUsageMeteringControlR2Table: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_configurableSimpleUsageMeteringControlR2Table(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	StorageTmp = header_complex(configurableSimpleUsageMeteringControlR2TableStorage, vp, name, length, exact, var_len, write_method);
	refresh_configurableSimpleUsageMeteringControlR2Table_row(StorageTmp, 0);
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) CONFIGURATIONR2MASK:	/* Create */
		*write_method = write_configurationR2Mask;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->configurationR2MaskLen;
		rval = (u_char *) StorageTmp->configurationR2Mask;
		break;
	case (u_char) CONFIGURATIONR2STATUS:	/* Create */
		*write_method = write_configurationR2Status;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->configurationR2Status);
		rval = (u_char *) &StorageTmp->configurationR2Status;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn void refresh_blockGeneratingLogR2Table_row(struct blockGeneratingLogR2Table_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the blockGeneratingLogR2Table row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
void
refresh_blockGeneratingLogR2Table_row(struct blockGeneratingLogR2Table_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->blockGeneratingLogR2Table_request == sa_request))
		return;
	StorageTmp->blockGeneratingLogR2Table_request = sa_request;
}

/**
 * @fn void refresh_blockGeneratingLogR2Table(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the blockGeneratingLogR2Table.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_blockGeneratingLogR2Table(int force)
{
	if (!force && blockGeneratingLogR2Table_refresh == 0)
		return;
	blockGeneratingLogR2Table_refresh = 0;
	/* XXX: Here, update the table as required... */
}

/**
 * @fn u_char *var_blockGeneratingLogR2Table(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in blockGeneratingLogR2Table.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_cdr2MIB above.
 */
u_char *
var_blockGeneratingLogR2Table(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct blockGeneratingLogR2Table_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("cdr2MIB", "var_blockGeneratingLogR2Table: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_blockGeneratingLogR2Table(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	StorageTmp = header_complex(blockGeneratingLogR2TableStorage, vp, name, length, exact, var_len, write_method);
	refresh_blockGeneratingLogR2Table_row(StorageTmp, 0);
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) BLOCKGENERATINGLOGVERSION:	/* ReadWrite */
		*write_method = write_blockGeneratingLogVersion;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->blockGeneratingLogVersion);
		rval = (u_char *) &StorageTmp->blockGeneratingLogVersion;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn void refresh_callDetailLogRecordR2Table_row(struct callDetailLogRecordR2Table_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the callDetailLogRecordR2Table row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
void
refresh_callDetailLogRecordR2Table_row(struct callDetailLogRecordR2Table_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->callDetailLogRecordR2Table_request == sa_request))
		return;
	StorageTmp->callDetailLogRecordR2Table_request = sa_request;
}

/**
 * @fn void refresh_callDetailLogRecordR2Table(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the callDetailLogRecordR2Table.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_callDetailLogRecordR2Table(int force)
{
	if (!force && callDetailLogRecordR2Table_refresh == 0)
		return;
	callDetailLogRecordR2Table_refresh = 0;
	/* XXX: Here, update the table as required... */
}

/**
 * @fn u_char *var_callDetailLogRecordR2Table(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in callDetailLogRecordR2Table.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_cdr2MIB above.
 */
u_char *
var_callDetailLogRecordR2Table(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct callDetailLogRecordR2Table_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("cdr2MIB", "var_callDetailLogRecordR2Table: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_callDetailLogRecordR2Table(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	StorageTmp = header_complex(callDetailLogRecordR2TableStorage, vp, name, length, exact, var_len, write_method);
	refresh_callDetailLogRecordR2Table_row(StorageTmp, 0);
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) SEQUENCEID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->sequenceId);
		rval = (u_char *) &StorageTmp->sequenceId;
		break;
	case (u_char) ZONEID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->zoneId);
		rval = (u_char *) &StorageTmp->zoneId;
		break;
	case (u_char) RECSEQID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->recSeqId);
		rval = (u_char *) &StorageTmp->recSeqId;
		break;
	case (u_char) FILESEQID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->fileSeqId);
		rval = (u_char *) &StorageTmp->fileSeqId;
		break;
	case (u_char) CALLID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->callIdLen;
		rval = (u_char *) StorageTmp->callId;
		break;
	case (u_char) LASTUPDATE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->lastUpdateLen;
		rval = (u_char *) StorageTmp->lastUpdate;
		break;
	case (u_char) EVENTORDER:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->eventOrderLen;
		rval = (u_char *) StorageTmp->eventOrder;
		break;
	case (u_char) STATUS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->status);
		rval = (u_char *) &StorageTmp->status;
		break;
	case (u_char) CALLTYPE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->callType);
		rval = (u_char *) &StorageTmp->callType;
		break;
	case (u_char) CLNGPTYNBR:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->clngPtyNbrLen;
		rval = (u_char *) StorageTmp->clngPtyNbr;
		break;
	case (u_char) CHARGENBR:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->chargeNbrLen;
		rval = (u_char *) StorageTmp->chargeNbr;
		break;
	case (u_char) CLLDPTYNBR:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->clldPtyNbrLen;
		rval = (u_char *) StorageTmp->clldPtyNbr;
		break;
	case (u_char) CLLDPTYNOA:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->clldPtyNoa);
		rval = (u_char *) &StorageTmp->clldPtyNoa;
		break;
	case (u_char) ORIGLNINFO:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->origLnInfoLen;
		rval = (u_char *) StorageTmp->origLnInfo;
		break;
	case (u_char) INGLRN:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ingLrnLen;
		rval = (u_char *) StorageTmp->ingLrn;
		break;
	case (u_char) INGCIC:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ingCicLen;
		rval = (u_char *) StorageTmp->ingCic;
		break;
	case (u_char) INGCSI:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ingCsi);
		rval = (u_char *) &StorageTmp->ingCsi;
		break;
	case (u_char) INGCCEID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ingCceId);
		rval = (u_char *) &StorageTmp->ingCceId;
		break;
	case (u_char) INGTGPROT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ingTgProt);
		rval = (u_char *) &StorageTmp->ingTgProt;
		break;
	case (u_char) INGTGTYPE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ingTgType);
		rval = (u_char *) &StorageTmp->ingTgType;
		break;
	case (u_char) INGTGID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ingTgId);
		rval = (u_char *) &StorageTmp->ingTgId;
		break;
	case (u_char) INGCALLSTART:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ingCallStartLen;
		rval = (u_char *) StorageTmp->ingCallStart;
		break;
	case (u_char) INGGATEID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ingGateId);
		rval = (u_char *) &StorageTmp->ingGateId;
		break;
	case (u_char) INGCARDID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ingCardId);
		rval = (u_char *) &StorageTmp->ingCardId;
		break;
	case (u_char) INGSPANID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ingSpanId);
		rval = (u_char *) &StorageTmp->ingSpanId;
		break;
	case (u_char) INGCHANID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ingChanId);
		rval = (u_char *) &StorageTmp->ingChanId;
		break;
	case (u_char) INGISDNDCHAN:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ingIsdnDChan);
		rval = (u_char *) &StorageTmp->ingIsdnDChan;
		break;
	case (u_char) INGISDNCRN:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ingIsdnCrn);
		rval = (u_char *) &StorageTmp->ingIsdnCrn;
		break;
	case (u_char) INGCREATCONNCPLT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ingCreatConnCpltLen;
		rval = (u_char *) StorageTmp->ingCreatConnCplt;
		break;
	case (u_char) INGADDRCPLT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ingAddrCpltLen;
		rval = (u_char *) StorageTmp->ingAddrCplt;
		break;
	case (u_char) INGCALLANS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ingCallAnsLen;
		rval = (u_char *) StorageTmp->ingCallAns;
		break;
	case (u_char) TRANSNBR:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->transNbrLen;
		rval = (u_char *) StorageTmp->transNbr;
		break;
	case (u_char) TERMLRN:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->termLrnLen;
		rval = (u_char *) StorageTmp->termLrn;
		break;
	case (u_char) TRANSCIC:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->transCicLen;
		rval = (u_char *) StorageTmp->transCic;
		break;
	case (u_char) INGCALLRLS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ingCallRlsLen;
		rval = (u_char *) StorageTmp->ingCallRls;
		break;
	case (u_char) INGRLSCAUSE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ingRlsCause);
		rval = (u_char *) &StorageTmp->ingRlsCause;
		break;
	case (u_char) EGRCCEID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->egrCceId);
		rval = (u_char *) &StorageTmp->egrCceId;
		break;
	case (u_char) EGRTGPROT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->egrTgProt);
		rval = (u_char *) &StorageTmp->egrTgProt;
		break;
	case (u_char) EGRTGTYPE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->egrTgType);
		rval = (u_char *) &StorageTmp->egrTgType;
		break;
	case (u_char) EGRTGID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->egrTgId);
		rval = (u_char *) &StorageTmp->egrTgId;
		break;
	case (u_char) EGRCALLSTART:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->egrCallStartLen;
		rval = (u_char *) StorageTmp->egrCallStart;
		break;
	case (u_char) EGRGATEID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->egrGateId);
		rval = (u_char *) &StorageTmp->egrGateId;
		break;
	case (u_char) EGRCARDID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->egrCardId);
		rval = (u_char *) &StorageTmp->egrCardId;
		break;
	case (u_char) EGRSPANID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->egrSpanId);
		rval = (u_char *) &StorageTmp->egrSpanId;
		break;
	case (u_char) EGRCHANID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->egrChanId);
		rval = (u_char *) &StorageTmp->egrChanId;
		break;
	case (u_char) EGRISDNDCHAN:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->egrIsdnDChan);
		rval = (u_char *) &StorageTmp->egrIsdnDChan;
		break;
	case (u_char) EGRISDNCRN:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->egrIsdnCrn);
		rval = (u_char *) &StorageTmp->egrIsdnCrn;
		break;
	case (u_char) EGRCREATCONNCPLT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->egrCreatConnCpltLen;
		rval = (u_char *) StorageTmp->egrCreatConnCplt;
		break;
	case (u_char) EGRADDRCPLT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->egrAddrCpltLen;
		rval = (u_char *) StorageTmp->egrAddrCplt;
		break;
	case (u_char) EGRCALLANS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->egrCallAnsLen;
		rval = (u_char *) StorageTmp->egrCallAns;
		break;
	case (u_char) EGRCALLRLS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->egrCallRlsLen;
		rval = (u_char *) StorageTmp->egrCallRls;
		break;
	case (u_char) EGRRLSCAUSE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->egrRlsCause);
		rval = (u_char *) &StorageTmp->egrRlsCause;
		break;
	case (u_char) CHKPTFIRST:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->chkptFirstLen;
		rval = (u_char *) StorageTmp->chkptFirst;
		break;
	case (u_char) CHKPTLAST:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->chkptLastLen;
		rval = (u_char *) StorageTmp->chkptLast;
		break;
	case (u_char) INGGATENAME:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ingGateNameLen;
		rval = (u_char *) StorageTmp->ingGateName;
		break;
	case (u_char) EGRGATENAME:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->egrGateNameLen;
		rval = (u_char *) StorageTmp->egrGateName;
		break;
	case (u_char) INGTGNAME:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ingTgNameLen;
		rval = (u_char *) StorageTmp->ingTgName;
		break;
	case (u_char) EGRTGNAME:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->egrTgNameLen;
		rval = (u_char *) StorageTmp->egrTgName;
		break;
	case (u_char) ORIGGATEIP:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->origGateIpLen;
		rval = (u_char *) StorageTmp->origGateIp;
		break;
	case (u_char) TERMGATEIP:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->termGateIpLen;
		rval = (u_char *) StorageTmp->termGateIp;
		break;
	case (u_char) H323CONFID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->h323ConfIdLen;
		rval = (u_char *) StorageTmp->h323ConfId;
		break;
	case (u_char) INGCARDPORT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ingCardPort);
		rval = (u_char *) &StorageTmp->ingCardPort;
		break;
	case (u_char) INGCARDPATH:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ingCardPath);
		rval = (u_char *) &StorageTmp->ingCardPath;
		break;
	case (u_char) EGRCARDPORT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->egrCardPort);
		rval = (u_char *) &StorageTmp->egrCardPort;
		break;
	case (u_char) EGRCARDPATH:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->egrCardPath);
		rval = (u_char *) &StorageTmp->egrCardPath;
		break;
	case (u_char) INGTG:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ingTg);
		rval = (u_char *) &StorageTmp->ingTg;
		break;
	case (u_char) EGRTG:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->egrTg);
		rval = (u_char *) &StorageTmp->egrTg;
		break;
	case (u_char) DIALEDNBR:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->dialedNbrLen;
		rval = (u_char *) StorageTmp->dialedNbr;
		break;
	case (u_char) DIALEDNOA:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dialedNoa);
		rval = (u_char *) &StorageTmp->dialedNoa;
		break;
	case (u_char) REDIRNBR:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->redirNbrLen;
		rval = (u_char *) StorageTmp->redirNbr;
		break;
	case (u_char) REDIRNOA:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->redirNoa);
		rval = (u_char *) &StorageTmp->redirNoa;
		break;
	case (u_char) REDIRIND:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->redirInd);
		rval = (u_char *) &StorageTmp->redirInd;
		break;
	case (u_char) REDIRRSN:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->redirRsn);
		rval = (u_char *) &StorageTmp->redirRsn;
		break;
	case (u_char) CALLEDNBR:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->calledNbrLen;
		rval = (u_char *) StorageTmp->calledNbr;
		break;
	case (u_char) CALLEDNOA:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->calledNoa);
		rval = (u_char *) &StorageTmp->calledNoa;
		break;
	case (u_char) REDIRRSNORIG:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->redirRsnOrig);
		rval = (u_char *) &StorageTmp->redirRsnOrig;
		break;
	case (u_char) REDIRCNTR:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->redirCntr);
		rval = (u_char *) &StorageTmp->redirCntr;
		break;
	case (u_char) REDIRPRSNTNIND:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->redirPrsntnInd);
		rval = (u_char *) &StorageTmp->redirPrsntnInd;
		break;
	case (u_char) INGJURIS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ingJurisLen;
		rval = (u_char *) StorageTmp->ingJuris;
		break;
	case (u_char) EGRJURIS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->egrJurisLen;
		rval = (u_char *) StorageTmp->egrJuris;
		break;
	case (u_char) INGTRUNKBEARCAP:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ingTrunkBearCap);
		rval = (u_char *) &StorageTmp->ingTrunkBearCap;
		break;
	case (u_char) EGRTRUNKBEARCAP:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->egrTrunkBearCap);
		rval = (u_char *) &StorageTmp->egrTrunkBearCap;
		break;
	case (u_char) TRANSNETSELCARR:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->transNetSelCarrLen;
		rval = (u_char *) StorageTmp->transNetSelCarr;
		break;
	case (u_char) CLNGPTYNOA:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->clngPtyNoa);
		rval = (u_char *) &StorageTmp->clngPtyNoa;
		break;
	case (u_char) INGRLSCAUSEINTRNL:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ingRlsCauseIntrnlLen;
		rval = (u_char *) StorageTmp->ingRlsCauseIntrnl;
		break;
	case (u_char) EGRRLSCAUSEINTRNL:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->egrRlsCauseIntrnlLen;
		rval = (u_char *) StorageTmp->egrRlsCauseIntrnl;
		break;
	case (u_char) EGRCLLDNBR:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->egrClldNbrLen;
		rval = (u_char *) StorageTmp->egrClldNbr;
		break;
	case (u_char) EGRCLLDNOA:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->egrClldNoa);
		rval = (u_char *) &StorageTmp->egrClldNoa;
		break;
	case (u_char) EGRCNNDNBR:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->egrCnndNbrLen;
		rval = (u_char *) StorageTmp->egrCnndNbr;
		break;
	case (u_char) EGRCNNDNOA:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->egrCnndNoa);
		rval = (u_char *) &StorageTmp->egrCnndNoa;
		break;
	case (u_char) CLNGPTYPRSNTNIND:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->clngPtyPrsntnInd);
		rval = (u_char *) &StorageTmp->clngPtyPrsntnInd;
		break;
	case (u_char) INGIRI:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ingIri);
		rval = (u_char *) &StorageTmp->ingIri;
		break;
	case (u_char) EGRORI:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->egrOri);
		rval = (u_char *) &StorageTmp->egrOri;
		break;
	case (u_char) INGCALLIDEXTRNL:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ingCallidExtrnlLen;
		rval = (u_char *) StorageTmp->ingCallidExtrnl;
		break;
	case (u_char) EGRCALLIDEXTRNL:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->egrCallidExtrnlLen;
		rval = (u_char *) StorageTmp->egrCallidExtrnl;
		break;
	case (u_char) INGCHARGEINFO:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ingChargeInfoLen;
		rval = (u_char *) StorageTmp->ingChargeInfo;
		break;
	case (u_char) EGRCHARGEINFO:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->egrChargeInfoLen;
		rval = (u_char *) StorageTmp->egrChargeInfo;
		break;
	case (u_char) INGPRTLIND:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ingPrtlIndLen;
		rval = (u_char *) StorageTmp->ingPrtlInd;
		break;
	case (u_char) INGNATFWDCLIBLKIND:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ingNatfwdCliblkInd);
		rval = (u_char *) &StorageTmp->ingNatfwdCliblkInd;
		break;
	case (u_char) INGNATFWDNTAIND:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ingNatfwdNtaInd);
		rval = (u_char *) &StorageTmp->ingNatfwdNtaInd;
		break;
	case (u_char) INGLASTDVRTLNDIGS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ingLastDvrtLnDigsLen;
		rval = (u_char *) StorageTmp->ingLastDvrtLnDigs;
		break;
	case (u_char) INGLASTDVRTLNNOA:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ingLastDvrtLnNoa);
		rval = (u_char *) &StorageTmp->ingLastDvrtLnNoa;
		break;
	case (u_char) J7INGADDLPTYCAT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->j7IngAddlPtyCatLen;
		rval = (u_char *) StorageTmp->j7IngAddlPtyCat;
		break;
	case (u_char) J7INGCHGAREAINFO:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->j7IngChgAreaInfoLen;
		rval = (u_char *) StorageTmp->j7IngChgAreaInfo;
		break;
	case (u_char) J7INGFWDCALLIND:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->j7IngFwdCallIndLen;
		rval = (u_char *) StorageTmp->j7IngFwdCallInd;
		break;
	case (u_char) J7EGRADDLPTYCAT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->j7EgrAddlPtyCatLen;
		rval = (u_char *) StorageTmp->j7EgrAddlPtyCat;
		break;
	case (u_char) J7EGRCHGAREAINFO:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->j7EgrChgAreaInfoLen;
		rval = (u_char *) StorageTmp->j7EgrChgAreaInfo;
		break;
	case (u_char) J7EGRBKWCALLIND:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->j7EgrBkwCallIndLen;
		rval = (u_char *) StorageTmp->j7EgrBkwCallInd;
		break;
	case (u_char) J7CARRINFOXFER:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->j7CarrInfoXferLen;
		rval = (u_char *) StorageTmp->j7CarrInfoXfer;
		break;
	case (u_char) INGSS7GNRCPARM:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ingSs7GnrcParmLen;
		rval = (u_char *) StorageTmp->ingSs7GnrcParm;
		break;
	case (u_char) EGRSS7GNRCPARM:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->egrSs7GnrcParmLen;
		rval = (u_char *) StorageTmp->egrSs7GnrcParm;
		break;
	case (u_char) INGPKTSSENT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ingPktsSent);
		rval = (u_char *) &StorageTmp->ingPktsSent;
		break;
	case (u_char) INGPKTSRCVD:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ingPktsRcvd);
		rval = (u_char *) &StorageTmp->ingPktsRcvd;
		break;
	case (u_char) INGPKTSLOST:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ingPktsLost);
		rval = (u_char *) &StorageTmp->ingPktsLost;
		break;
	case (u_char) INGPKTSXFER:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ingPktsXfer);
		rval = (u_char *) &StorageTmp->ingPktsXfer;
		break;
	case (u_char) INGJITTER:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ingJitter);
		rval = (u_char *) &StorageTmp->ingJitter;
		break;
	case (u_char) INGLTNCY:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ingLtncy);
		rval = (u_char *) &StorageTmp->ingLtncy;
		break;
	case (u_char) EGRPKTSSENT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->egrPktsSent);
		rval = (u_char *) &StorageTmp->egrPktsSent;
		break;
	case (u_char) EGRPKTSRCVD:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->egrPktsRcvd);
		rval = (u_char *) &StorageTmp->egrPktsRcvd;
		break;
	case (u_char) EGRPKTSLOST:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->egrPktsLost);
		rval = (u_char *) &StorageTmp->egrPktsLost;
		break;
	case (u_char) EGRPKTSXFER:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->egrPktsXfer);
		rval = (u_char *) &StorageTmp->egrPktsXfer;
		break;
	case (u_char) EGRJITTER:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->egrJitter);
		rval = (u_char *) &StorageTmp->egrJitter;
		break;
	case (u_char) EGRLTNCY:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->egrLtncy);
		rval = (u_char *) &StorageTmp->egrLtncy;
		break;
	case (u_char) INGCRCTID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ingCrctId);
		rval = (u_char *) &StorageTmp->ingCrctId;
		break;
	case (u_char) EGRCRCTID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->egrCrctId);
		rval = (u_char *) &StorageTmp->egrCrctId;
		break;
	case (u_char) INGCODEC:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ingCodec);
		rval = (u_char *) &StorageTmp->ingCodec;
		break;
	case (u_char) EGRCODEC:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->egrCodec);
		rval = (u_char *) &StorageTmp->egrCodec;
		break;
	case (u_char) INGLOCGATEID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ingLocGateId);
		rval = (u_char *) &StorageTmp->ingLocGateId;
		break;
	case (u_char) EGRLOCGATEID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->egrLocGateId);
		rval = (u_char *) &StorageTmp->egrLocGateId;
		break;
	case (u_char) INGCASCRCTSZR:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ingCasCrctSzrLen;
		rval = (u_char *) StorageTmp->ingCasCrctSzr;
		break;
	case (u_char) EGRCASCRCTSZR:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->egrCasCrctSzrLen;
		rval = (u_char *) StorageTmp->egrCasCrctSzr;
		break;
	case (u_char) INGZZ:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ingZzLen;
		rval = (u_char *) StorageTmp->ingZz;
		break;
	case (u_char) EGRZZ:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->egrZzLen;
		rval = (u_char *) StorageTmp->egrZz;
		break;
	case (u_char) INGCTRYADDRTYPE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ingCtryAddrTypeLen;
		rval = (u_char *) StorageTmp->ingCtryAddrType;
		break;
	case (u_char) EGRCTRYADDRTYPE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->egrCtryAddrTypeLen;
		rval = (u_char *) StorageTmp->egrCtryAddrType;
		break;
	case (u_char) INGPARTITION:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ingPartition);
		rval = (u_char *) &StorageTmp->ingPartition;
		break;
	case (u_char) EGRPARTITION:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->egrPartition);
		rval = (u_char *) &StorageTmp->egrPartition;
		break;
	case (u_char) INGCLNGPTYCAT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ingClngPtyCat);
		rval = (u_char *) &StorageTmp->ingClngPtyCat;
		break;
	case (u_char) INGRLSCAUSELCTN:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ingRlsCauseLctn);
		rval = (u_char *) &StorageTmp->ingRlsCauseLctn;
		break;
	case (u_char) EGRRLSCAUSELCTN:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->egrRlsCauseLctn);
		rval = (u_char *) &StorageTmp->egrRlsCauseLctn;
		break;
	case (u_char) CLLDPTYCAT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->clldPtyCat);
		rval = (u_char *) &StorageTmp->clldPtyCat;
		break;
	case (u_char) CALLDURATION:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->callDuration);
		rval = (u_char *) &StorageTmp->callDuration;
		break;
	case (u_char) INGRLSDRCTN:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ingRlsDrctn);
		rval = (u_char *) &StorageTmp->ingRlsDrctn;
		break;
	case (u_char) EGRRLSDRCTN:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->egrRlsDrctn);
		rval = (u_char *) &StorageTmp->egrRlsDrctn;
		break;
	case (u_char) INGANSLCTIM:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->ingAnsLctimLen;
		rval = (u_char *) StorageTmp->ingAnsLctim;
		break;
	case (u_char) EGRANSLCTIM:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->egrAnsLctimLen;
		rval = (u_char *) StorageTmp->egrAnsLctim;
		break;
	case (u_char) INGHLCCHRSID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ingHlcChrsId);
		rval = (u_char *) &StorageTmp->ingHlcChrsId;
		break;
	case (u_char) EGRHLCCHRSID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->egrHlcChrsId);
		rval = (u_char *) &StorageTmp->egrHlcChrsId;
		break;
	case (u_char) INGLLCXFERCAP:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ingLlcXferCap);
		rval = (u_char *) &StorageTmp->ingLlcXferCap;
		break;
	case (u_char) EGRLLCXFERCAP:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->egrLlcXferCap);
		rval = (u_char *) &StorageTmp->egrLlcXferCap;
		break;
	case (u_char) ROUTELIST:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->routeListLen;
		rval = (u_char *) StorageTmp->routeList;
		break;
	case (u_char) INGPOINTCODE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->ingPointCode);
		rval = (u_char *) &StorageTmp->ingPointCode;
		break;
	case (u_char) EGRPOINTCODE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->egrPointCode);
		rval = (u_char *) &StorageTmp->egrPointCode;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int write_creationTriggerList(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_creationTriggerList(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static uint8_t *old_value;
	struct simpleUsageMeteringControlTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	static size_t old_length = 0;
	static uint8_t *string = NULL;

	DEBUGMSGTL(("cdr2MIB", "write_creationTriggerList entering action=%d...  \n", action));
	StorageTmp = header_complex(simpleUsageMeteringControlTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);

	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->simpleUsageMeteringControlEntryStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to creationTriggerList: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_BIT_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to creationTriggerList not ASN_BIT_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (1 > var_val_len || var_val_len > SPRINT_MAX_LEN || var_val_len != 1) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to creationTriggerList: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		break;
	case FREE:		/* Release any resources that have been allocated */
		SNMP_FREE(string);
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->creationTriggerList for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		old_value = StorageTmp->creationTriggerList;
		old_length = StorageTmp->creationTriggerListLen;
		StorageTmp->creationTriggerList = string;
		StorageTmp->creationTriggerListLen = var_val_len;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->creationTriggerList = old_value;
		StorageTmp->creationTriggerListLen = old_length;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(old_value);
		old_length = 0;
		string = NULL;
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_samplingRate(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_samplingRate(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct configurableSimpleUsageMeteringControlTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("cdr2MIB", "write_samplingRate entering action=%d...  \n", action));
	StorageTmp = header_complex(configurableSimpleUsageMeteringControlTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);

	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->configurationRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to samplingRate: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to samplingRate not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to samplingRate: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->samplingRate for you to use, and you have just been asked to do something with it.  Note that anything done here must be 
				   reversable in the UNDO case */
		old_value = StorageTmp->samplingRate;
		StorageTmp->samplingRate = set_value;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->samplingRate = old_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_configurationMask(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_configurationMask(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static uint8_t *old_value;
	struct configurableSimpleUsageMeteringControlTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	static size_t old_length = 0;
	static uint8_t *string = NULL;

	DEBUGMSGTL(("cdr2MIB", "write_configurationMask entering action=%d...  \n", action));
	StorageTmp = header_complex(configurableSimpleUsageMeteringControlTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);

	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->configurationRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to configurationMask: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_BIT_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to configurationMask not ASN_BIT_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (1 > var_val_len || var_val_len > SPRINT_MAX_LEN || var_val_len != 2) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to configurationMask: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		break;
	case FREE:		/* Release any resources that have been allocated */
		SNMP_FREE(string);
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->configurationMask for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		old_value = StorageTmp->configurationMask;
		old_length = StorageTmp->configurationMaskLen;
		StorageTmp->configurationMask = string;
		StorageTmp->configurationMaskLen = var_val_len;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->configurationMask = old_value;
		StorageTmp->configurationMaskLen = old_length;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(old_value);
		old_length = 0;
		string = NULL;
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_blockGeneratingLogMaxBlockSize(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_blockGeneratingLogMaxBlockSize(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct blockGeneratingLogTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("cdr2MIB", "write_blockGeneratingLogMaxBlockSize entering action=%d...  \n", action));
	StorageTmp = header_complex(blockGeneratingLogTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);

	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->blockGeneratingLogRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to blockGeneratingLogMaxBlockSize: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to blockGeneratingLogMaxBlockSize not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to blockGeneratingLogMaxBlockSize: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..32767 */
		if ((0 > set_value || set_value > 32767)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to blockGeneratingLogMaxBlockSize: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->blockGeneratingLogMaxBlockSize for you to use, and you have just been asked to do something with it.  Note that anything 
				   done here must be reversable in the UNDO case */
		old_value = StorageTmp->blockGeneratingLogMaxBlockSize;
		StorageTmp->blockGeneratingLogMaxBlockSize = set_value;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->blockGeneratingLogMaxBlockSize = old_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_blockGeneratingLogMaxTimeInterval(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_blockGeneratingLogMaxTimeInterval(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct blockGeneratingLogTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("cdr2MIB", "write_blockGeneratingLogMaxTimeInterval entering action=%d...  \n", action));
	StorageTmp = header_complex(blockGeneratingLogTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);

	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->blockGeneratingLogRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to blockGeneratingLogMaxTimeInterval: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to blockGeneratingLogMaxTimeInterval not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to blockGeneratingLogMaxTimeInterval: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..32767 */
		if ((0 > set_value || set_value > 32767)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to blockGeneratingLogMaxTimeInterval: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->blockGeneratingLogMaxTimeInterval for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		old_value = StorageTmp->blockGeneratingLogMaxTimeInterval;
		StorageTmp->blockGeneratingLogMaxTimeInterval = set_value;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->blockGeneratingLogMaxTimeInterval = old_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_blockGeneratingLogStorageType(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_blockGeneratingLogStorageType(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct blockGeneratingLogTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("cdr2MIB", "write_blockGeneratingLogStorageType entering action=%d...  \n", action));
	StorageTmp = header_complex(blockGeneratingLogTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);

	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->blockGeneratingLogRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to blockGeneratingLogStorageType: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to blockGeneratingLogStorageType not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to blockGeneratingLogStorageType: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case ST_OTHER:
		case ST_VOLATILE:
		case ST_NONVOLATILE:
		case ST_PERMANENT:
		case ST_READONLY:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to blockGeneratingLogStorageType: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->blockGeneratingLogStorageType for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		old_value = StorageTmp->blockGeneratingLogStorageType;
		StorageTmp->blockGeneratingLogStorageType = set_value;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->blockGeneratingLogStorageType = old_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_timesOfDay(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_timesOfDay(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static uint8_t *old_value;
	struct fileGeneratingLogTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	static size_t old_length = 0;
	static uint8_t *string = NULL;

	DEBUGMSGTL(("cdr2MIB", "write_timesOfDay entering action=%d...  \n", action));
	StorageTmp = header_complex(fileGeneratingLogTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);

	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->fileGeneratingLogEntryStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to timesOfDay: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to timesOfDay not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 0..0 2..2 */
		if (var_val_len > SPRINT_MAX_LEN || (var_val_len != 0 && var_val_len != 2)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to timesOfDay: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		break;
	case FREE:		/* Release any resources that have been allocated */
		SNMP_FREE(string);
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->timesOfDay for you to use, and you have just been asked to do something with it.  Note that anything done here must be
				   reversable in the UNDO case */
		old_value = StorageTmp->timesOfDay;
		old_length = StorageTmp->timesOfDayLen;
		StorageTmp->timesOfDay = string;
		StorageTmp->timesOfDayLen = var_val_len;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->timesOfDay = old_value;
		StorageTmp->timesOfDayLen = old_length;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(old_value);
		old_length = 0;
		string = NULL;
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_periodicTrigger(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_periodicTrigger(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct fileGeneratingLogTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("cdr2MIB", "write_periodicTrigger entering action=%d...  \n", action));
	StorageTmp = header_complex(fileGeneratingLogTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);

	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->fileGeneratingLogEntryStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to periodicTrigger: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to periodicTrigger not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to periodicTrigger: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 0 */
		/* Note: ranges 0..512 */
		if ((0 > set_value || set_value > 512)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to periodicTrigger: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->periodicTrigger for you to use, and you have just been asked to do something with it.  Note that anything done here must 
				   be reversable in the UNDO case */
		old_value = StorageTmp->periodicTrigger;
		StorageTmp->periodicTrigger = set_value;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->periodicTrigger = old_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_fileGeneratingLogStorageType(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_fileGeneratingLogStorageType(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct fileGeneratingLogTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("cdr2MIB", "write_fileGeneratingLogStorageType entering action=%d...  \n", action));
	StorageTmp = header_complex(fileGeneratingLogTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);

	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->fileGeneratingLogEntryStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to fileGeneratingLogStorageType: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to fileGeneratingLogStorageType not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to fileGeneratingLogStorageType: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value nonVolatile */
		switch (set_value) {
		case ST_OTHER:
		case ST_VOLATILE:
		case ST_NONVOLATILE:
		case ST_PERMANENT:
		case ST_READONLY:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to fileGeneratingLogStorageType: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->fileGeneratingLogStorageType for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		old_value = StorageTmp->fileGeneratingLogStorageType;
		StorageTmp->fileGeneratingLogStorageType = set_value;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->fileGeneratingLogStorageType = old_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_configurationR2Mask(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_configurationR2Mask(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static uint8_t *old_value;
	struct configurableSimpleUsageMeteringControlR2Table_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	static size_t old_length = 0;
	static uint8_t *string = NULL;

	DEBUGMSGTL(("cdr2MIB", "write_configurationR2Mask entering action=%d...  \n", action));
	StorageTmp = header_complex(configurableSimpleUsageMeteringControlR2TableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);

	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->configurationR2Status) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to configurationR2Mask: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_BIT_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to configurationR2Mask not ASN_BIT_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (1 > var_val_len || var_val_len > SPRINT_MAX_LEN) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to configurationR2Mask: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		break;
	case FREE:		/* Release any resources that have been allocated */
		SNMP_FREE(string);
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->configurationR2Mask for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		old_value = StorageTmp->configurationR2Mask;
		old_length = StorageTmp->configurationR2MaskLen;
		StorageTmp->configurationR2Mask = string;
		StorageTmp->configurationR2MaskLen = var_val_len;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->configurationR2Mask = old_value;
		StorageTmp->configurationR2MaskLen = old_length;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(old_value);
		old_length = 0;
		string = NULL;
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_blockGeneratingLogVersion(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_blockGeneratingLogVersion(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct blockGeneratingLogR2Table_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("cdr2MIB", "write_blockGeneratingLogVersion entering action=%d...  \n", action));
	StorageTmp = header_complex(blockGeneratingLogR2TableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to blockGeneratingLogVersion not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to blockGeneratingLogVersion: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value version1 */
		switch (set_value) {
		case BLOCKGENERATINGLOGVERSION_VERSION1:
		case BLOCKGENERATINGLOGVERSION_VERSION2:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to blockGeneratingLogVersion: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->blockGeneratingLogVersion for you to use, and you have just been asked to do something with it.  Note that anything done 
				   here must be reversable in the UNDO case */
		old_value = StorageTmp->blockGeneratingLogVersion;
		StorageTmp->blockGeneratingLogVersion = set_value;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->blockGeneratingLogVersion = old_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int callDetailDataTable_consistent(struct callDetailDataTable_data *thedata)
 * @param thedata the row data to check for consistency.
 * @brief check the internal consistency of a table row.
 *
 * This function checks the internal consistency of a table row for the callDetailDataTable table.  If the
 * table row is internally consistent, then this function returns true (1), otherwise the function
 * returns false (0) and it will not be possible to activate the row until the row's internal
 * consistency is corrected.
 */
int
callDetailDataTable_consistent(struct callDetailDataTable_data *thedata)
{
	/* XXX: check row consistency return true(1) if consistent, or false(0) if not. */
	return (1);
}

/**
 * @fn int simpleUsageMeteringControlTable_consistent(struct simpleUsageMeteringControlTable_data *thedata)
 * @param thedata the row data to check for consistency.
 * @brief check the internal consistency of a table row.
 *
 * This function checks the internal consistency of a table row for the simpleUsageMeteringControlTable table.  If the
 * table row is internally consistent, then this function returns true (1), otherwise the function
 * returns false (0) and it will not be possible to activate the row until the row's internal
 * consistency is corrected.
 */
int
simpleUsageMeteringControlTable_consistent(struct simpleUsageMeteringControlTable_data *thedata)
{
	/* XXX: check row consistency return true(1) if consistent, or false(0) if not. */
	return (1);
}

/**
 * @fn int configurableSimpleUsageMeteringControlTable_consistent(struct configurableSimpleUsageMeteringControlTable_data *thedata)
 * @param thedata the row data to check for consistency.
 * @brief check the internal consistency of a table row.
 *
 * This function checks the internal consistency of a table row for the configurableSimpleUsageMeteringControlTable table.  If the
 * table row is internally consistent, then this function returns true (1), otherwise the function
 * returns false (0) and it will not be possible to activate the row until the row's internal
 * consistency is corrected.
 */
int
configurableSimpleUsageMeteringControlTable_consistent(struct configurableSimpleUsageMeteringControlTable_data *thedata)
{
	/* XXX: check row consistency return true(1) if consistent, or false(0) if not. */
	return (1);
}

/**
 * @fn int blockGeneratingLogTable_consistent(struct blockGeneratingLogTable_data *thedata)
 * @param thedata the row data to check for consistency.
 * @brief check the internal consistency of a table row.
 *
 * This function checks the internal consistency of a table row for the blockGeneratingLogTable table.  If the
 * table row is internally consistent, then this function returns true (1), otherwise the function
 * returns false (0) and it will not be possible to activate the row until the row's internal
 * consistency is corrected.
 */
int
blockGeneratingLogTable_consistent(struct blockGeneratingLogTable_data *thedata)
{
	/* XXX: check row consistency return true(1) if consistent, or false(0) if not. */
	return (1);
}

/**
 * @fn int fileGeneratingLogTable_consistent(struct fileGeneratingLogTable_data *thedata)
 * @param thedata the row data to check for consistency.
 * @brief check the internal consistency of a table row.
 *
 * This function checks the internal consistency of a table row for the fileGeneratingLogTable table.  If the
 * table row is internally consistent, then this function returns true (1), otherwise the function
 * returns false (0) and it will not be possible to activate the row until the row's internal
 * consistency is corrected.
 */
int
fileGeneratingLogTable_consistent(struct fileGeneratingLogTable_data *thedata)
{
	/* XXX: check row consistency return true(1) if consistent, or false(0) if not. */
	return (1);
}

/**
 * @fn int callDetailLogRecordTable_consistent(struct callDetailLogRecordTable_data *thedata)
 * @param thedata the row data to check for consistency.
 * @brief check the internal consistency of a table row.
 *
 * This function checks the internal consistency of a table row for the callDetailLogRecordTable table.  If the
 * table row is internally consistent, then this function returns true (1), otherwise the function
 * returns false (0) and it will not be possible to activate the row until the row's internal
 * consistency is corrected.
 */
int
callDetailLogRecordTable_consistent(struct callDetailLogRecordTable_data *thedata)
{
	/* XXX: check row consistency return true(1) if consistent, or false(0) if not. */
	return (1);
}

/**
 * @fn int configurableSimpleUsageMeteringControlR2Table_consistent(struct configurableSimpleUsageMeteringControlR2Table_data *thedata)
 * @param thedata the row data to check for consistency.
 * @brief check the internal consistency of a table row.
 *
 * This function checks the internal consistency of a table row for the configurableSimpleUsageMeteringControlR2Table table.  If the
 * table row is internally consistent, then this function returns true (1), otherwise the function
 * returns false (0) and it will not be possible to activate the row until the row's internal
 * consistency is corrected.
 */
int
configurableSimpleUsageMeteringControlR2Table_consistent(struct configurableSimpleUsageMeteringControlR2Table_data *thedata)
{
	/* XXX: check row consistency return true(1) if consistent, or false(0) if not. */
	return (1);
}

/**
 * @fn int blockGeneratingLogR2Table_consistent(struct blockGeneratingLogR2Table_data *thedata)
 * @param thedata the row data to check for consistency.
 * @brief check the internal consistency of a table row.
 *
 * This function checks the internal consistency of a table row for the blockGeneratingLogR2Table table.  If the
 * table row is internally consistent, then this function returns true (1), otherwise the function
 * returns false (0) and it will not be possible to activate the row until the row's internal
 * consistency is corrected.
 */
int
blockGeneratingLogR2Table_consistent(struct blockGeneratingLogR2Table_data *thedata)
{
	/* XXX: check row consistency return true(1) if consistent, or false(0) if not. */
	return (1);
}

/**
 * @fn int callDetailLogRecordR2Table_consistent(struct callDetailLogRecordR2Table_data *thedata)
 * @param thedata the row data to check for consistency.
 * @brief check the internal consistency of a table row.
 *
 * This function checks the internal consistency of a table row for the callDetailLogRecordR2Table table.  If the
 * table row is internally consistent, then this function returns true (1), otherwise the function
 * returns false (0) and it will not be possible to activate the row until the row's internal
 * consistency is corrected.
 */
int
callDetailLogRecordR2Table_consistent(struct callDetailLogRecordR2Table_data *thedata)
{
	/* XXX: check row consistency return true(1) if consistent, or false(0) if not. */
	return (1);
}

/**
 * @fn int write_simpleUsageMeteringControlEntryStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_simpleUsageMeteringControlEntryStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct simpleUsageMeteringControlTable_data *StorageTmp = NULL;
	static struct simpleUsageMeteringControlTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 15;
	static int old_value;
	int set_value;
	static struct variable_list *vars, *vp;

	StorageTmp = header_complex(simpleUsageMeteringControlTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to simpleUsageMeteringControlEntryStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to simpleUsageMeteringControlEntryStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->simpleUsageMeteringControlEntryStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->simpleUsageMeteringControlTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_DESTROY:
			/* destroying existent or non-existent row is ok */
			if (StorageTmp == NULL)
				break;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->simpleUsageMeteringControlTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			vars = NULL;
			/* controlObjectId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[15]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			if ((StorageNew = simpleUsageMeteringControlTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			vp = vars;
			memdup((void *) &StorageNew->controlObjectId, vp->val.string, vp->val_len);
			StorageNew->controlObjectIdLen = vp->val_len;
			vp = vp->next_variable;
			header_complex_add_data(&simpleUsageMeteringControlTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_DESTROY:
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				simpleUsageMeteringControlTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		}
		break;
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			simpleUsageMeteringControlTable_del(StorageNew);
			simpleUsageMeteringControlTable_destroy(&StorageNew);
			/* XXX: free, zero vars */
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			simpleUsageMeteringControlTable_add(StorageDel);
			break;
		}
		break;
	case ACTION:
		/* The variable has been stored in set_value for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in the UNDO case */
		switch (set_value) {
		case RS_ACTIVE:
			old_value = StorageTmp->simpleUsageMeteringControlEntryStatus;
			StorageTmp->simpleUsageMeteringControlEntryStatus = set_value;
			if (old_value != RS_ACTIVE) {
				/* check that activation is possible */
				if (!simpleUsageMeteringControlTable_consistent(StorageTmp)) {
					StorageTmp->simpleUsageMeteringControlEntryStatus = old_value;
					return SNMP_ERR_INCONSISTENTVALUE;
				}
			}
			break;
		case RS_NOTINSERVICE:
			/* set the flag? */
			old_value = StorageTmp->simpleUsageMeteringControlEntryStatus;
			StorageTmp->simpleUsageMeteringControlEntryStatus = set_value;
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* restore state */
			StorageTmp->simpleUsageMeteringControlEntryStatus = old_value;
			break;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* row creation, set final state */
			/* check if row is ready, otherwise leave at RS_NOTREADY */
			if (simpleUsageMeteringControlTable_consistent(StorageNew)) {
				/* XXX: commit creation to underlying device */
				/* XXX: activate with underlying device */
				StorageNew->simpleUsageMeteringControlEntryStatus = RS_ACTIVE;
			}
			break;
		case RS_CREATEANDWAIT:
			/* row creation, set final state */
			/* check if row is ready, otherwise leave at RS_NOTREADY */
			if (simpleUsageMeteringControlTable_consistent(StorageNew)) {
				/* XXX: commit creation to underlying device, inactive */
				StorageNew->simpleUsageMeteringControlEntryStatus = RS_NOTINSERVICE;
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (old_value != set_value) {
				switch (set_value) {
				case RS_ACTIVE:
					/* XXX: activate with underlying device */
					break;
				case RS_NOTINSERVICE:
					/* XXX: deactivate with underlying device */
					break;
				}
			}
			break;
		case RS_DESTROY:
			/* row deletion, free it its dead */
			simpleUsageMeteringControlTable_destroy(&StorageDel);
			/* simpleUsageMeteringControlTable_destroy() can handle NULL pointers. */
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_configurationRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_configurationRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct configurableSimpleUsageMeteringControlTable_data *StorageTmp = NULL;
	static struct configurableSimpleUsageMeteringControlTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 15;
	static int old_value;
	int set_value;
	static struct variable_list *vars, *vp;

	StorageTmp = header_complex(configurableSimpleUsageMeteringControlTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to configurationRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to configurationRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->configurationRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->configurableSimpleUsageMeteringControlTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_DESTROY:
			/* destroying existent or non-existent row is ok */
			if (StorageTmp == NULL)
				break;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->configurableSimpleUsageMeteringControlTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			vars = NULL;
			/* controlObjectId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[15]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			if ((StorageNew = configurableSimpleUsageMeteringControlTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			vp = vars;
			memdup((void *) &StorageNew->controlObjectId, vp->val.string, vp->val_len);
			StorageNew->controlObjectIdLen = vp->val_len;
			vp = vp->next_variable;
			header_complex_add_data(&configurableSimpleUsageMeteringControlTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_DESTROY:
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				configurableSimpleUsageMeteringControlTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		}
		break;
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			configurableSimpleUsageMeteringControlTable_del(StorageNew);
			configurableSimpleUsageMeteringControlTable_destroy(&StorageNew);
			/* XXX: free, zero vars */
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			configurableSimpleUsageMeteringControlTable_add(StorageDel);
			break;
		}
		break;
	case ACTION:
		/* The variable has been stored in set_value for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in the UNDO case */
		switch (set_value) {
		case RS_ACTIVE:
			old_value = StorageTmp->configurationRowStatus;
			StorageTmp->configurationRowStatus = set_value;
			if (old_value != RS_ACTIVE) {
				/* check that activation is possible */
				if (!configurableSimpleUsageMeteringControlTable_consistent(StorageTmp)) {
					StorageTmp->configurationRowStatus = old_value;
					return SNMP_ERR_INCONSISTENTVALUE;
				}
			}
			break;
		case RS_NOTINSERVICE:
			/* set the flag? */
			old_value = StorageTmp->configurationRowStatus;
			StorageTmp->configurationRowStatus = set_value;
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* restore state */
			StorageTmp->configurationRowStatus = old_value;
			break;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* row creation, set final state */
			/* check if row is ready, otherwise leave at RS_NOTREADY */
			if (configurableSimpleUsageMeteringControlTable_consistent(StorageNew)) {
				/* XXX: commit creation to underlying device */
				/* XXX: activate with underlying device */
				StorageNew->configurationRowStatus = RS_ACTIVE;
			}
			break;
		case RS_CREATEANDWAIT:
			/* row creation, set final state */
			/* check if row is ready, otherwise leave at RS_NOTREADY */
			if (configurableSimpleUsageMeteringControlTable_consistent(StorageNew)) {
				/* XXX: commit creation to underlying device, inactive */
				StorageNew->configurationRowStatus = RS_NOTINSERVICE;
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (old_value != set_value) {
				switch (set_value) {
				case RS_ACTIVE:
					/* XXX: activate with underlying device */
					break;
				case RS_NOTINSERVICE:
					/* XXX: deactivate with underlying device */
					break;
				}
			}
			break;
		case RS_DESTROY:
			/* row deletion, free it its dead */
			configurableSimpleUsageMeteringControlTable_destroy(&StorageDel);
			/* configurableSimpleUsageMeteringControlTable_destroy() can handle NULL pointers. */
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_blockGeneratingLogRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_blockGeneratingLogRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct blockGeneratingLogTable_data *StorageTmp = NULL;
	static struct blockGeneratingLogTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 15;
	static int old_value;
	int set_value;
	static struct variable_list *vars, *vp;

	StorageTmp = header_complex(blockGeneratingLogTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to blockGeneratingLogRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to blockGeneratingLogRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->blockGeneratingLogRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->blockGeneratingLogTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_DESTROY:
			/* destroying existent or non-existent row is ok */
			if (StorageTmp == NULL)
				break;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->blockGeneratingLogTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			vars = NULL;
			/* logId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[15]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			if ((StorageNew = blockGeneratingLogTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			vp = vars;
			memdup((void *) &StorageNew->logId, vp->val.string, vp->val_len);
			StorageNew->logIdLen = vp->val_len;
			vp = vp->next_variable;
			header_complex_add_data(&blockGeneratingLogTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_DESTROY:
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				blockGeneratingLogTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		}
		break;
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			blockGeneratingLogTable_del(StorageNew);
			blockGeneratingLogTable_destroy(&StorageNew);
			/* XXX: free, zero vars */
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			blockGeneratingLogTable_add(StorageDel);
			break;
		}
		break;
	case ACTION:
		/* The variable has been stored in set_value for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in the UNDO case */
		switch (set_value) {
		case RS_ACTIVE:
			old_value = StorageTmp->blockGeneratingLogRowStatus;
			StorageTmp->blockGeneratingLogRowStatus = set_value;
			if (old_value != RS_ACTIVE) {
				/* check that activation is possible */
				if (!blockGeneratingLogTable_consistent(StorageTmp)) {
					StorageTmp->blockGeneratingLogRowStatus = old_value;
					return SNMP_ERR_INCONSISTENTVALUE;
				}
			}
			break;
		case RS_NOTINSERVICE:
			/* set the flag? */
			old_value = StorageTmp->blockGeneratingLogRowStatus;
			StorageTmp->blockGeneratingLogRowStatus = set_value;
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* restore state */
			StorageTmp->blockGeneratingLogRowStatus = old_value;
			break;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* row creation, set final state */
			/* check if row is ready, otherwise leave at RS_NOTREADY */
			if (blockGeneratingLogTable_consistent(StorageNew)) {
				/* XXX: commit creation to underlying device */
				/* XXX: activate with underlying device */
				StorageNew->blockGeneratingLogRowStatus = RS_ACTIVE;
			}
			break;
		case RS_CREATEANDWAIT:
			/* row creation, set final state */
			/* check if row is ready, otherwise leave at RS_NOTREADY */
			if (blockGeneratingLogTable_consistent(StorageNew)) {
				/* XXX: commit creation to underlying device, inactive */
				StorageNew->blockGeneratingLogRowStatus = RS_NOTINSERVICE;
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (old_value != set_value) {
				switch (set_value) {
				case RS_ACTIVE:
					/* XXX: activate with underlying device */
					break;
				case RS_NOTINSERVICE:
					/* XXX: deactivate with underlying device */
					break;
				}
			}
			break;
		case RS_DESTROY:
			/* row deletion, free it its dead */
			blockGeneratingLogTable_destroy(&StorageDel);
			/* blockGeneratingLogTable_destroy() can handle NULL pointers. */
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_fileGeneratingLogEntryStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_fileGeneratingLogEntryStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct fileGeneratingLogTable_data *StorageTmp = NULL;
	static struct fileGeneratingLogTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 15;
	static int old_value;
	int set_value;
	static struct variable_list *vars, *vp;

	StorageTmp = header_complex(fileGeneratingLogTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to fileGeneratingLogEntryStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to fileGeneratingLogEntryStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->fileGeneratingLogEntryStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->fileGeneratingLogTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_DESTROY:
			/* destroying existent or non-existent row is ok */
			if (StorageTmp == NULL)
				break;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->fileGeneratingLogTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			vars = NULL;
			/* logId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[15]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			if ((StorageNew = fileGeneratingLogTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			vp = vars;
			memdup((void *) &StorageNew->logId, vp->val.string, vp->val_len);
			StorageNew->logIdLen = vp->val_len;
			vp = vp->next_variable;
			header_complex_add_data(&fileGeneratingLogTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_DESTROY:
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				fileGeneratingLogTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		}
		break;
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			fileGeneratingLogTable_del(StorageNew);
			fileGeneratingLogTable_destroy(&StorageNew);
			/* XXX: free, zero vars */
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			fileGeneratingLogTable_add(StorageDel);
			break;
		}
		break;
	case ACTION:
		/* The variable has been stored in set_value for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in the UNDO case */
		switch (set_value) {
		case RS_ACTIVE:
			old_value = StorageTmp->fileGeneratingLogEntryStatus;
			StorageTmp->fileGeneratingLogEntryStatus = set_value;
			if (old_value != RS_ACTIVE) {
				/* check that activation is possible */
				if (!fileGeneratingLogTable_consistent(StorageTmp)) {
					StorageTmp->fileGeneratingLogEntryStatus = old_value;
					return SNMP_ERR_INCONSISTENTVALUE;
				}
			}
			break;
		case RS_NOTINSERVICE:
			/* set the flag? */
			old_value = StorageTmp->fileGeneratingLogEntryStatus;
			StorageTmp->fileGeneratingLogEntryStatus = set_value;
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* restore state */
			StorageTmp->fileGeneratingLogEntryStatus = old_value;
			break;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* row creation, set final state */
			/* check if row is ready, otherwise leave at RS_NOTREADY */
			if (fileGeneratingLogTable_consistent(StorageNew)) {
				/* XXX: commit creation to underlying device */
				/* XXX: activate with underlying device */
				StorageNew->fileGeneratingLogEntryStatus = RS_ACTIVE;
			}
			break;
		case RS_CREATEANDWAIT:
			/* row creation, set final state */
			/* check if row is ready, otherwise leave at RS_NOTREADY */
			if (fileGeneratingLogTable_consistent(StorageNew)) {
				/* XXX: commit creation to underlying device, inactive */
				StorageNew->fileGeneratingLogEntryStatus = RS_NOTINSERVICE;
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (old_value != set_value) {
				switch (set_value) {
				case RS_ACTIVE:
					/* XXX: activate with underlying device */
					break;
				case RS_NOTINSERVICE:
					/* XXX: deactivate with underlying device */
					break;
				}
			}
			break;
		case RS_DESTROY:
			/* row deletion, free it its dead */
			fileGeneratingLogTable_destroy(&StorageDel);
			/* fileGeneratingLogTable_destroy() can handle NULL pointers. */
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_configurationR2Status(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_configurationR2Status(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct configurableSimpleUsageMeteringControlR2Table_data *StorageTmp = NULL;
	static struct configurableSimpleUsageMeteringControlR2Table_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 15;
	static int old_value;
	int set_value;
	static struct variable_list *vars, *vp;

	StorageTmp = header_complex(configurableSimpleUsageMeteringControlR2TableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to configurationR2Status not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to configurationR2Status: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->configurationR2Status == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->configurableSimpleUsageMeteringControlR2Table_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_DESTROY:
			/* destroying existent or non-existent row is ok */
			if (StorageTmp == NULL)
				break;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->configurableSimpleUsageMeteringControlR2Table_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			vars = NULL;
			/* controlObjectId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[15]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			if ((StorageNew = configurableSimpleUsageMeteringControlR2Table_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			vp = vars;
			memdup((void *) &StorageNew->controlObjectId, vp->val.string, vp->val_len);
			StorageNew->controlObjectIdLen = vp->val_len;
			vp = vp->next_variable;
			header_complex_add_data(&configurableSimpleUsageMeteringControlR2TableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_DESTROY:
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				configurableSimpleUsageMeteringControlR2Table_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		}
		break;
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			configurableSimpleUsageMeteringControlR2Table_del(StorageNew);
			configurableSimpleUsageMeteringControlR2Table_destroy(&StorageNew);
			/* XXX: free, zero vars */
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			configurableSimpleUsageMeteringControlR2Table_add(StorageDel);
			break;
		}
		break;
	case ACTION:
		/* The variable has been stored in set_value for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in the UNDO case */
		switch (set_value) {
		case RS_ACTIVE:
			old_value = StorageTmp->configurationR2Status;
			StorageTmp->configurationR2Status = set_value;
			if (old_value != RS_ACTIVE) {
				/* check that activation is possible */
				if (!configurableSimpleUsageMeteringControlR2Table_consistent(StorageTmp)) {
					StorageTmp->configurationR2Status = old_value;
					return SNMP_ERR_INCONSISTENTVALUE;
				}
			}
			break;
		case RS_NOTINSERVICE:
			/* set the flag? */
			old_value = StorageTmp->configurationR2Status;
			StorageTmp->configurationR2Status = set_value;
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* restore state */
			StorageTmp->configurationR2Status = old_value;
			break;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* row creation, set final state */
			/* check if row is ready, otherwise leave at RS_NOTREADY */
			if (configurableSimpleUsageMeteringControlR2Table_consistent(StorageNew)) {
				/* XXX: commit creation to underlying device */
				/* XXX: activate with underlying device */
				StorageNew->configurationR2Status = RS_ACTIVE;
			}
			break;
		case RS_CREATEANDWAIT:
			/* row creation, set final state */
			/* check if row is ready, otherwise leave at RS_NOTREADY */
			if (configurableSimpleUsageMeteringControlR2Table_consistent(StorageNew)) {
				/* XXX: commit creation to underlying device, inactive */
				StorageNew->configurationR2Status = RS_NOTINSERVICE;
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (old_value != set_value) {
				switch (set_value) {
				case RS_ACTIVE:
					/* XXX: activate with underlying device */
					break;
				case RS_NOTINSERVICE:
					/* XXX: deactivate with underlying device */
					break;
				}
			}
			break;
		case RS_DESTROY:
			/* row deletion, free it its dead */
			configurableSimpleUsageMeteringControlR2Table_destroy(&StorageDel);
			/* configurableSimpleUsageMeteringControlR2Table_destroy() can handle NULL pointers. */
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

void
send_blockRecordNotification_v2trap(struct variable_list *vars)
{
	struct variable_list trap;

	trap.next_variable = vars;
	trap.name = snmpTrapOID_oid;
	trap.name_length = sizeof(snmpTrapOID_oid) / sizeof(oid);
	trap.type = ASN_OBJECT_ID;
	trap.val.objid = blockRecordNotification_oid;
	trap.val_len = sizeof(blockRecordNotification_oid);
	trap.index = 0;
	send_v2trap(&trap);
}

void
send_callDetailRecordNotification_v2trap(struct variable_list *vars)
{
	struct variable_list trap;

	trap.next_variable = vars;
	trap.name = snmpTrapOID_oid;
	trap.name_length = sizeof(snmpTrapOID_oid) / sizeof(oid);
	trap.type = ASN_OBJECT_ID;
	trap.val.objid = callDetailRecordNotification_oid;
	trap.val_len = sizeof(callDetailRecordNotification_oid);
	trap.index = 0;
	send_v2trap(&trap);
}

void
send_fileCreationNotification_v2trap(struct variable_list *vars)
{
	struct variable_list trap;

	trap.next_variable = vars;
	trap.name = snmpTrapOID_oid;
	trap.name_length = sizeof(snmpTrapOID_oid) / sizeof(oid);
	trap.type = ASN_OBJECT_ID;
	trap.val.objid = fileCreationNotification_oid;
	trap.val_len = sizeof(fileCreationNotification_oid);
	trap.index = 0;
	send_v2trap(&trap);
}

void
send_blockRecordNotificationR2_v2trap(struct variable_list *vars)
{
	struct variable_list trap;

	trap.next_variable = vars;
	trap.name = snmpTrapOID_oid;
	trap.name_length = sizeof(snmpTrapOID_oid) / sizeof(oid);
	trap.type = ASN_OBJECT_ID;
	trap.val.objid = blockRecordNotificationR2_oid;
	trap.val_len = sizeof(blockRecordNotificationR2_oid);
	trap.index = 0;
	send_v2trap(&trap);
}

void
send_callDetailRecordNotificationR2_v2trap(struct variable_list *vars)
{
	struct variable_list trap;

	trap.next_variable = vars;
	trap.name = snmpTrapOID_oid;
	trap.name_length = sizeof(snmpTrapOID_oid) / sizeof(oid);
	trap.type = ASN_OBJECT_ID;
	trap.val.objid = callDetailRecordNotificationR2_oid;
	trap.val_len = sizeof(callDetailRecordNotificationR2_oid);
	trap.index = 0;
	send_v2trap(&trap);
}

#if defined MODULE
#if defined MASTER
/**
 * @fn void cdr2MIB_loop_handler(int dummy)
 * @param dummy signal number (always zero (0))
 * @brief handle event loop interation.
 *
 * This function is registered so that, when operating as a module, snmpd will call it one per event
 * loop interation.  This function is called before the next requst is processed and after the
 * previous request is processed.  Two things are done here:  1) The file descriptor that is used to
 * synchronize the agent with (pseudo-)device drivers is closed.  (Another approach, instead of
 * closing each time, would be to restart a timer each time that a request is made (loop is
 * performed) and if it expires, close the file descriptor).  2) The request number is incremented.
 * Although a request is not generated for each loop of the snmp event loop, it is true that a new
 * request cannot be generated without performing a loop.  Therefore, the sa_request is not the
 * request number but it is a temporally unique identifier for a request.
 */
void
cdr2MIB_loop_handler(int dummy)
{
	if (external_signal_scheduled[dummy] == 0)
		external_signal_scheduled[dummy]--;
	/* close files after each request */
	if (sa_fclose) {
		if (sa_fd != 0) {
			close(sa_fd);
			sa_fd = 0;
		}
		if (my_fd != 0) {
			close(my_fd);
			my_fd = 0;
		}
	}
	/* prepare for next request */
	sa_request++;
}
#endif				/* defined MASTER */
/**
 * @fn void cdr2MIB_fd_handler(int fd, void *dummy)
 * @param fd file descriptor to read.
 * @param dummy client data passed to registration function (always NULL).
 * @brief handle read event on file descriptor.
 *
 * This read file descriptor handler is normally used for (pseudo-)device drivers that generate
 * statistical collection interval events, alarm events, or other operational measurement events, by
 * placing a message on the read queue of the "event handling" Stream.  Normally this routine
 * would adjust counts in some table or scalars, generate SNMP traps representing on-occurence
 * events, first and interval events, and alarm indications.
 */
void
cdr2MIB_fd_handler(int fd, void *dummy)
{
	/* XXX: place actions to handle sa_readfd here... */
	return;
}
#endif				/* defined MOUDLE */
#if defined MASTER
const char sa_program[] = "cdr2mib";
int sa_fclose = 1;			/* default close files between requests */
int sa_fd = 0;				/* file descriptor for MIB use */
int sa_readfd = 0;			/* file descriptor for autonomnous events */
int sa_changed = 1;			/* indication to reread MIB configuration */
int sa_stats_refresh = 1;		/* indications that statistics, the mib or its tables need to be refreshed */
int sa_request = 1;			/* request number for per-request actions */
#endif				/* defined MASTER */
#if defined MASTER
#if !defined MODULE
int sa_dump = 0;			/* default packet dump */
int sa_debug = 0;			/* default no debug */
int sa_nomead = 1;			/* default daemon mode */
int sa_output = 1;			/* default normal output */
int sa_agentx = 1;			/* default agentx mode */
int sa_alarms = 1;			/* default application alarms */
int sa_logaddr = 0;			/* log addresses */
int sa_logfillog = 0;			/* log to sa_logfile */
int sa_logstderr = 0;			/* log to standard error */
int sa_logstdout = 0;			/* log to standard output */
int sa_logsyslog = 0;			/* log to system logs */
int sa_logcallog = 0;			/* log to callback logs */
int sa_appendlog = 0;			/* append to log file without truncating */
char sa_logfile[256] = "/var/log/cdr2mib.log";
char sa_pidfile[256] = "/var/run/cdr2mib.pid";
char sa_sysctlf[256] = "/etc/cdr2mib.conf";
int allow_severity = LOG_ERR;
int deny_severity = LOG_ERR;

/* file stream for log file */
FILE *stdlog = NULL;
static void
sa_version(int argc, char *argv[])
{
	if (!sa_output && !sa_debug)
		return;
	fprintf(stdout, "\
%2$s\n\
Copyright (c) 2008-2009  Monavacon Limited.  All Rights Reserved.\n\
Distributed under Affero GPL Version 3, included here by reference.\n\
See `%1$s --copying' for copying permissions.\n\
", argv[0], ident);
}
static void
sa_usage(int argc, char *argv[])
{
	if (!sa_output && !sa_debug)
		return;
	fprintf(stderr, "\
Usage:\n\
    %1$s [general-options] [options] [arguments]\n\
    %1$s {-H|--help-directives}\n\
    %1$s {-h|--help}\n\
    %1$s {-V|--version}\n\
    %1$s {-C|--copying}\n\
", argv[0]);
}
static void
sa_help(int argc, char *argv[])
{
	if (!sa_output && !sa_debug)
		return;
	fprintf(stdout, "\
Usage:\n\
    %1$s [general-options] [options] [arguments]\n\
    %1$s {-h|--help}\n\
    %1$s {-V|--version}\n\
    %1$s {-C|--copying}\n\
Arguments:\n\
    None.\n\
Options:\n\
    -a, --log-addresses\n\
        log addresses of connecting management stations.\n\
    -A, --append\n\
        append to logfiles without truncating.\n\
    -c, --config-file CONFIGFILE\n\
        use configuration file CONFIGFILE.\n\
    -C, --config-only\n\
        only load configuration given by -c option.\n\
    -d, --dump\n\
        dump sent and received PDUs.\n\
    -D, --debug [LEVEL]\n\
        set debugging verbosity to LEVEL.\n\
    -D, --debug-tokens [TOKEN[,TOKEN]*]\n\
        debug specified TOKEN's.\n\
    -f, --dont-fork\n\
        run in the foreground.\n\
    -g, --gid, --groupid GID\n\
        become group GID after listening.\n\
    -h, --help, -?, --?\n\
        print usage information and exit.\n\
    -H, --help-directives\n\
        print config directives and exit.\n\
    -I, --initialize [-]MODULE[,MODULE]*\n\
        initialize (or not, '-') these MODULE's.\n\
    -k, --keep-open\n\
        keep system files open between requests.\n\
    -l, --log-file [LOGFILE]\n\
        log to log file name LOGFILE.  [default: /var/log/cdr2mib.log]\n\
    -L, --log-stderr\n\
        log to controlling terminal standard error.\n\
    -m, --mibs [+]MIB[,MIB]*\n\
        load these (additional '+') MIBs.\n\
    -M, --master\n\
        run as SNMP master instead of AgentX sub-agent.\n\
    -M, --mibdirs [+]MIBDIR[:MIBDIR]*\n\
        search these (additional, '+') colon separated directories for MIBs.\n\
    -n, --nodaemon\n\
        run in the foreground.\n\
    -n, --name NAME\n\
        use NAME for configuration file base.  [default: cdr2mib]\n\
    -p, --port PORTNUM\n\
        listen on port number PORTNUM.  [default: 161]\n\
    -p, --pidfile PIDFILE\n\
        write daemon pid to PIDFILE.  [default: /var/run/cdr2mib.pid]\n\
    -P, --pidfile PIDFILE\n\
        write daemon pid to PIDFILE.  [default: /var/run/cdr2mib.pid]\n\
    -q, --quiet\n\
        suppress normal output.\n\
    -q, --quick\n\
        abbreviate output for machine readability.\n\
    -r, --noroot\n\
        do not require root privilege.\n\
    -s, --log-syslog\n\
        log to system logs.\n\
    -S, --sysctl-file FILENAME\n\
        write sysctl config file FILENAME.  [default: /etc/streams.conf]\n\
    -t, --agent-alarms\n\
        agent blocks {SIGALARM}.\n\
    -T, --transport [TRANSPORT]\n\
        default transport TRANSPORT.  [default: udp]\n\
    -u, --uid, --userid UID\n\
        become user UID after listening.\n\
    -U, --dont-remove-pidfile\n\
        do not remove PIDFILE when shutting down.\n\
    -v, --version\n\
        print version information and exit.\n\
    -V, --verbose [LEVEL]\n\
        be verbose to LEVEL.  [default: 1]\n\
    -x, --agentx-socket [SOCKET]\n\
        master AgentX on SOCKET.  [default: /var/agentx/master]\n\
    -X, --agentx\n\
        run as AgentX sub-agent instead of master (the default).\n\
    -y, --copying\n\
        print copying information and exit.\n\
", argv[0]);
}
static void
sa_copying(int argc, char *argv[])
{
	if (!sa_output && !sa_debug)
		return;
	fprintf(stdout, "\
--------------------------------------------------------------------------------\n\
%1$s\n\
--------------------------------------------------------------------------------\n\
Copyright (c) 2008-2009  Monavacon Limited <http://www.monavacon.com>\n\
Copyright (c) 2001-2008  OpenSS7 Corporation <http://www.openss7.com>\n\
Copyright (c) 1997-2000  Brian F. G. Bidulock <bidulock@openss7.org>\n\
\n\
All Rights Reserved.\n\
--------------------------------------------------------------------------------\n\
This program is free software; you can  redistribute  it and/or modify  it under\n\
the terms of the GNU Affero General Public License as published by the Free\n\
Software Foundation; Version 3 of the License.\n\
\n\
This program is distributed in the hope that it will  be useful, but WITHOUT ANY\n\
WARRANTY; without even  the implied warranty of MERCHANTABILITY or FITNESS FOR A\n\
PARTICULAR PURPOSE.  See the GNU Affero General Public License for more details.\n\
\n\
You should have received a copy of the GNU  Affero  General Public License along\n\
with this program.   If not, see <http://www.gnu.org/licenses/>, or write to the\n\
Free Software Foundation, Inc., 675 Mass Ave, Cambridge, MA 02139, USA.\n\
--------------------------------------------------------------------------------\n\
U.S. GOVERNMENT RESTRICTED RIGHTS.  If you are licensing this Software on behalf\n\
of the U.S. Government (\"Government\"), the following provisions apply to you. If\n\
the Software is supplied by the  Department of Defense (\"DoD\"), it is classified\n\
as \"Commercial  Computer  Software\"  under  paragraph  252.227-7014  of the  DoD\n\
Supplement  to the  Federal Acquisition Regulations  (\"DFARS\") (or any successor\n\
regulations) and the  Government  is acquiring  only the  license rights granted\n\
herein (the license rights customarily provided to non-Government users). If the\n\
Software is supplied to any unit or agency of the Government  other than DoD, it\n\
is  classified as  \"Restricted Computer Software\" and the Government's rights in\n\
the Software  are defined  in  paragraph 52.227-19  of the  Federal  Acquisition\n\
Regulations (\"FAR\")  (or any successor regulations) or, in the cases of NASA, in\n\
paragraph  18.52.227-86 of  the  NASA  Supplement  to the FAR (or any  successor\n\
regulations).\n\
--------------------------------------------------------------------------------\n\
Commercial  licensing  and  support of this  software is  available from OpenSS7\n\
Corporation at a fee.  See http://www.openss7.com/\n\
--------------------------------------------------------------------------------\n\
", ident);
}

void
sa_help_directives(int argc, char *argv[])
{
	ds_set_boolean(DS_APPLICATION_ID, DS_AGENT_NO_ROOT_ACCESS, 1);
	init_agent("cdr2MIB");
	// init_mib_modules();
	init_mib();
	init_snmp("cdr2MIB");
	snmp_log(MY_FACILITY(LOG_INFO), "Configuration directives understood:\n");
	/* Unfortunately, read_config_print_usage() uses snmp_log(), meaning that it can only be writen to standard error and not standard output. */
	read_config_print_usage("    ");
}
static int
sa_sig_register(int signum, RETSIGTYPE(*handler) (int))
{
	sigset_t mask;
	struct sigaction act;

	act.sa_handler = handler ? handler : SIG_DFL;
	act.sa_flags = handler ? SA_RESTART : 0;
	sigemptyset(&act.sa_mask);
	if (sigaction(signum, &act, NULL))
		return (-1);
	sigemptyset(&mask);
	sigaddset(&mask, signum);
	sigprocmask(handler ? SIG_UNBLOCK : SIG_BLOCK, &mask, NULL);
	return (0);
}
static int sa_alm_signal = 0;
static int sa_pol_signal = 0;
static int sa_hup_signal = 0;
static int sa_int_signal = 0;
static int sa_trm_signal = 0;
static int sa_alm_handle = 0;
void
sa_alm_callback(uint req, void *arg)
{
	if (req == sa_alm_handle)
		sa_alm_handle = 0;
	sa_alm_signal = 1;
	return;
}

static RETSIGTYPE
sa_alm_handler(int signum)
{
	sa_alm_signal = 1;
	return (RETSIGTYPE) (0);
}
static void
sa_snmp_alm_handler(uint reg, void *clientarg)
{
	sa_alm_signal = 1;
	return;
}
static int
sa_alm_catch(void)
{
	if (sa_alarms)
		return sa_sig_register(SIGALRM, &sa_alm_handler);
	return (-1);
}
static int
sa_alm_block(void)
{
	if (sa_alarms)
		return sa_sig_register(SIGALRM, NULL);
	if (sa_alm_handle) {
		uint handle = sa_alm_handle;

		sa_alm_handle = 0;
		snmp_alarm_unregister(handle);
	}
	return (0);
}
static int
sa_alm_action(void)
{
	sa_alm_signal = 0;
	return (0);
}

static RETSIGTYPE
sa_pol_handler(int signum)
{
	sa_pol_signal = 1;
	return (RETSIGTYPE) (0);
}
static int
sa_pol_catch(void)
{
	return sa_sig_register(SIGPOLL, &sa_pol_handler);
}
static int
sa_pol_block(void)
{
	return sa_sig_register(SIGPOLL, NULL);
}

/*
 * Both the sc(4) module and sad(4) driver issue an M_PCSIG message with
 * SIGPOLL to the stream head whenever the STREAMS configuration or autopush
 * configuration changes, indicating to the agent which has the sc(4) or
 * sad(4) Stream open that it is necessary to reread information from the
 * kernel.  This fact is merely recorded, as this information is not read each
 * time that a configuration change occurs, but only after a request from some
 * portion of that information occurs. This condition is also set when the
 * sc(4) and sad(4) Streams are first opened. The SIGPOLL will also deliver in
 * siginfo the file descriptor issuing the signal, so we could distiguish
 * between sc(4) and sad(4) signals, but since one can be pushed over the
 * other, there is little point in distinguishing.
 *
 * sc(4) or sad(4) also should be modified to provide the general streams
 * statistics supported here; even though they are available through the /proc
 * filesystem on Linux Fast-STREAMS.
 */
static int
sa_pol_action(void)
{
	sa_pol_signal = 0;
	snmp_log(MY_FACILITY(LOG_INFO), "%s: Caught SIGPOLL, will re-read data structures", sa_program);
	sa_changed = 1;
	return (0);
}

static RETSIGTYPE
sa_hup_handler(int signum)
{
	sa_hup_signal = 1;
	return (RETSIGTYPE) (0);
}
static int
sa_hup_catch(void)
{
	if (sa_agentx)
		return sa_sig_register(SIGHUP, &sa_hup_handler);
	return (-1);
}
static int
sa_hup_block(void)
{
	return sa_sig_register(SIGHUP, NULL);
}
static int
sa_hup_action(void)
{
	/* There are several times that we might be sent a SIGHUP.  We might be sent a SIGHUP by logrotate asking us to close and reopen our log files. */
	sa_hup_signal = 0;
	snmp_log(MY_FACILITY(LOG_WARNING), "Caught SIGHUP, reopening files.");
	if (sa_output > 1)
		snmp_log(MY_FACILITY(LOG_NOTICE), "Reopening output file %s", sa_logfile);
	if (sa_logfillog != 0) {
		fflush(stdlog);
		fclose(stdlog);
		snmp_disable_filelog();
		if ((stdlog = freopen(sa_logfile, sa_appendlog ? "a" : "w", stdlog)) == NULL) {
			/* I hope we have another log sink. */
			snmp_log(MY_FACILITY(LOG_ERR), "%s", strerror(errno));
			snmp_log(MY_FACILITY(LOG_ERR), "Could not reopen log file %s", sa_logfile);
		}
		snmp_enable_filelog(sa_logfile, sa_appendlog);
	}
	return (0);
}

static RETSIGTYPE
sa_int_handler(int signum)
{
	sa_int_signal = 1;
	return (RETSIGTYPE) (0);
}
static int
sa_int_catch(void)
{
	return sa_sig_register(SIGINT, &sa_int_handler);
}
static int
sa_int_block(void)
{
	return sa_sig_register(SIGINT, NULL);
}
static void sa_exit(int retval);
static int
sa_int_action(void)
{
	sa_int_signal = 0;
	snmp_log(MY_FACILITY(LOG_WARNING), "%s: Caught SIGINT, shutting down", sa_program);
	sa_exit(0);
	return (0);		/* should be no return */
}

static RETSIGTYPE
sa_trm_handler(int signum)
{
	sa_trm_signal = 1;
	return (RETSIGTYPE) (0);
}
static int
sa_trm_catch(void)
{
	return sa_sig_register(SIGTERM, &sa_trm_handler);
}
static int
sa_trm_block(void)
{
	return sa_sig_register(SIGTERM, NULL);
}
static void sa_exit(int retval);
static int
sa_trm_action(void)
{
	sa_trm_signal = 0;
	snmp_log(MY_FACILITY(LOG_WARNING), "%s: Caught SIGTERM, shutting down", sa_program);
	sa_exit(0);
	return (0);		/* should be no return */
}
static void
sa_sig_catch(void)
{
	sa_alm_catch();
	sa_pol_catch();
	sa_hup_catch();
	sa_int_catch();
	sa_trm_catch();
}
static void
sa_sig_block(void)
{
	sa_alm_block();
	sa_pol_block();
	sa_hup_block();
	sa_int_block();
	sa_trm_block();
}

int
sa_start_timer(long duration)
{
	if (sa_alarms) {
		struct itimerval setting = {
			{0, 0},
			{duration / 1000, (duration % 1000) * 1000}
		};
		if (sa_alm_catch())
			return (-1);
		if (setitimer(ITIMER_REAL, &setting, NULL))
			return (-1);
		sa_alm_signal = 0;
		return (0);
	} else {
#if defined NETSNMP_DS_APPLICATION_ID
		struct timeval setting = {
			duration / 1000, (duration % 1000) * 1000
		};
		sa_alm_handle = snmp_alarm_register_hr(setting, 0, sa_snmp_alm_handler, NULL);
#else
		sa_alm_handle = snmp_alarm_register((duration + 999) / 1000, 0, sa_snmp_alm_handler, NULL);
#endif
		return (sa_alm_handle ? 0 : -1);
	}
}
static void
sa_exit(int retval)
{
	if (retval)
		snmp_log(MY_FACILITY(LOG_ERR), "%s: Exiting %d", sa_program, retval);
	else
		snmp_log(MY_FACILITY(LOG_NOTICE), "%s: Exiting %d", sa_program, retval);
	fflush(stdout);
	fflush(stderr);
	sa_sig_block();
	closelog();
	exit(retval);
}
static void
sa_init_logging(int argc, char *argv[])
{
	static char progname[256];

	/* The purpose of this function is to bring logging up before forking (and while still in the foreground) so that we can use the snmp_log() function before and during forking if necessary.
	   Note that the default configuration for snmp_log() is to send all logs to standard error. */
	strncpy(progname, basename(argv[0]), sizeof(progname));
	snmp_disable_log();
	if (sa_logfillog) {
		snmp_enable_filelog(sa_logfile, sa_appendlog);
	}
	if (sa_logstderr | sa_logstdout) {
#if defined LOG_PERROR
		/* Note that when we have Linux LOG_PERROR, and logs go both to syslog and stderr, it is better to use the LOG_PERROR than to use snmp_log()'s print to stderr, as the former is better 
		   formated. */
		if (!sa_logsyslog)
			snmp_enable_stderrlog();
#else				/* defined LOG_PERROR */
		snmp_enable_stderrlog();
#endif				/* defined LOG_PERROR */
	}
	if (sa_logsyslog) {
#if !defined HAVE_SNMP_ENABLE_SYSLOG_IDENT
		snmp_enable_syslog();
#else				/* !defined HAVE_SNMP_ENABLE_SYSLOG_IDENT */
		snmp_enable_syslog_ident("cdr2MIB", LOG_DAEMON);
#endif				/* !defined HAVE_SNMP_ENABLE_SYSLOG_IDENT */
		/* Note that the way that snmp sets up the logger is not really the way we want it, so close the log and reopen it the way we want. */
		closelog();
#if defined LOG_PERROR
		openlog("cdr2MIB", LOG_PID | LOG_CONS | LOG_NDELAY | (sa_logstderr ? LOG_PERROR : 0), MY_FACILITY(0));
#else				/* defined LOG_PERROR */
		openlog("cdr2MIB", LOG_PID | LOG_CONS | LOG_NDELAY, MY_FACILITY(0));
#endif				/* defined LOG_PERROR */
	}
	if (sa_logcallog) {
		snmp_enable_calllog();
	}
}
static void
sa_enter(int argc, char *argv[])
{
	if (sa_nomead) {
		pid_t pid;

		if ((pid = fork()) < 0) {
			perror(argv[0]);
			exit(2);
		} else if (pid != 0) {
			/* parent exits */
			exit(0);
		}
		setsid();	/* become a session leader */
		/* fork once more for SVR4 */
		if ((pid = fork()) < 0) {
			perror(argv[0]);
			exit(2);
		} else if (pid != 0) {
			/* parent responsible for writing pid file */
			if (sa_nomead || sa_pidfile[0] != '\0') {
				FILE *pidf;

				/* initialize default filename */
				if (sa_pidfile[0] == '\0')
					snprintf(sa_pidfile, sizeof(sa_pidfile), "/var/run/%s.pid", sa_program);
				if (sa_output > 1) {
					snmp_log(MY_FACILITY(LOG_NOTICE), "%s: Writing daemon pid to file %s", sa_program, sa_pidfile);
				}
				if ((pidf = fopen(sa_pidfile, "w+"))) {
					fprintf(pidf, "%d", (int) pid);
					fflush(pidf);
					fclose(pidf);
				} else {
					snmp_log(MY_FACILITY(LOG_ERR), "%s: %m", sa_program);
					snmp_log(MY_FACILITY(LOG_ERR), "%s: Could not write pid to file %s", sa_program, sa_pidfile);
					sa_exit(2);
					/* no return */
				}
			}
			/* parent exits */
			exit(0);
		}
		/* child continues */
		/* release current directory */
		if (chdir("/") < 0) {
			perror(argv[0]);
			exit(2);
		}
		umask(0);	/* clear file creation mask */
		/* rearrange file streams */
		fclose(stdin);
	}
	/* continue as foreground or background */
	sa_init_logging(argc, argv);
	sa_sig_catch();
	snmp_log(MY_FACILITY(LOG_NOTICE), "%s: Startup complete.", sa_program);
}
static void
sa_mloop(int argc, char *argv[])
{
	if (sa_agentx) {
		if (sa_debug)
			snmp_log(MY_FACILITY(LOG_DEBUG), "%s: running as AgentX client\n", argv[0]);
		/* run as an AgentX client */
		ds_set_boolean(DS_APPLICATION_ID, DS_AGENT_ROLE, 1);
	} else {
		if (sa_debug)
			snmp_log(MY_FACILITY(LOG_DEBUG), "%s: running as SNMP master agent\n", argv[0]);
		/* run as SNMP master */
		ds_set_boolean(DS_APPLICATION_ID, DS_AGENT_ROLE, 0);
	}
	if (sa_alarms) {
		if (sa_debug)
			snmp_log(MY_FACILITY(LOG_DEBUG), "%s: using application alarms\n", argv[0]);
		/* use application alarms */
		ds_set_boolean(DS_LIBRARY_ID, DS_LIB_ALARM_DONT_USE_SIG, 1);
	}
	/* initialize agent */
	init_agent("cdr2MIB");
	/* initialize MIB */
	init_cdr2MIB();
	/* initialize SNMP */
	init_snmp("cdr2MIB");
	if (!sa_agentx) {
		if (sa_debug)
			snmp_log(MY_FACILITY(LOG_DEBUG), "%s: running as SNMP master\n", argv[0]);
#if !defined NETSNMP_DS_APPLICATION_ID
		init_master_agent(710, NULL, NULL);
#else
		init_master_agent();
#endif
	}
	for (;;) {
		int retval;

		/* to use select or poll you need to use the snmp_select_info() to obtain the fd of the agentx socket and add it to the fdset. */
		/* note that SIGALRM is used by snmp: use the snmp_alarm() api instead */
#if 0
		if (snmp_select() == 0) {
			if (sa_alarms == 0)
				run_alarms();
		}
#endif
		retval = agent_check_and_process(1);	/* 0 == don't block */
		if (retval == 0) {
			/* alarm occurred, alarm conditions checked */
		} else if (retval == -1) {
			/* error (or signal) ocurred */
			if (sa_alm_signal) {
				sa_alm_action();
			}
			if (sa_pol_signal) {
				sa_pol_action();
			}
			if (sa_hup_signal) {
				sa_hup_action();
			}
			if (sa_int_signal) {
				if (sa_debug)
					snmp_log(MY_FACILITY(LOG_DEBUG), "%s: shutting down\n", argv[0]);
				snmp_shutdown("cdr2MIB");
				sa_int_action();	/* no return */
			}
			if (sa_trm_signal) {
				if (sa_debug)
					snmp_log(MY_FACILITY(LOG_DEBUG), "%s: shutting down\n", argv[0]);
				snmp_shutdown("cdr2MIB");
				sa_trm_action();	/* no return */
			}
		} else if (retval > 0) {
			/* processed packets */
			if (sa_fclose) {
				/* close files after each request */
				if (sa_fd != 0) {
					int fd = sa_fd;

					sa_fd = 0;
					close(fd);
				}
			}
			sa_stats_refresh = 1;
			sa_request++;
		}
	}
	if (sa_debug)
		snmp_log(MY_FACILITY(LOG_DEBUG), "%s: shutting down\n", argv[0]);
	snmp_shutdown("cdr2MIB");
}

int
main(int argc, char *argv[])
{
	for (;;) {
		int c, val, fd;
		char *cptr;
		struct passwd *pw;
		struct group *gr;
		struct stat st;

#if defined _GNU_SOURCE
		int option_index = 0;
                /* *INDENT-OFF* */
                static struct option long_options[] = {
                        {"log-addresses",	no_argument,		NULL, 'a'},
                        {"append",		no_argument,		NULL, 'A'},
                        {"config-file",		required_argument,	NULL, 'c'},
                        {"no-configs",		no_argument,		NULL, 'C'},
                        {"dump",		no_argument,		NULL, 'd'},
                        {"debug",		optional_argument,	NULL, 'D'},
                        {"debug-tokens",	optional_argument,	NULL, 'D'},
                        {"dont-fork",		no_argument,		NULL, 'f'},
                        {"gid",			required_argument,	NULL, 'g'},
                        {"groupid",		required_argument,	NULL, 'g'},
                        {"help",		no_argument,		NULL, 'h'},
                        {"?",			no_argument,		NULL, 'h'},
                        {"help-directives",	no_argument,		NULL, 'H'},
                        {"initialize",		required_argument,	NULL, 'I'},
                        {"init-modules",	required_argument,	NULL, 'I'},
                        {"keep-open",		no_argument,		NULL, 'k'},
                        {"log-file",		optional_argument,	NULL, 'l'},
                        {"logfile",		optional_argument,	NULL, 'l'},
                        {"Lf",			optional_argument,	NULL, 'l'},
                        {"LF",			required_argument,	NULL, 'l'},
                        {"log-stderr",		no_argument,		NULL, 'L'},
                        {"Le",			no_argument,		NULL, 'L'},
                        {"LE",			required_argument,	NULL, 'L'},
                        {"mibs",		required_argument,	NULL, 'm'},
                        {"master",		no_argument,		NULL, 'M'},
                        {"mibdirs",		required_argument,	NULL, 'M'},
                        {"nodaemon",		no_argument,		NULL, 'n'},
                        {"name",		required_argument,	NULL, 'n'},
                        {"dry-run",		no_argument,		NULL, 'N'},
                        {"log-stdout",		no_argument,		NULL, 'o'},
                        {"Lo",			no_argument,		NULL, 'o'},
                        {"LO",			required_argument,	NULL, 'o'},
                        {"port",		required_argument,	NULL, 'p'},
                        {"pidfile",		required_argument,	NULL, 'P'},
                        {"quiet",		no_argument,		NULL, 'q'},
                        {"quick",		no_argument,		NULL, 'q'},
                        {"noroot",		no_argument,		NULL, 'r'},
                        {"log-syslog",		no_argument,		NULL, 's'},
                        {"Ls",			no_argument,		NULL, 's'},
                        {"LS",			required_argument,	NULL, 's'},
                        {"syslog",		no_argument,		NULL, 's'},
                        {"sysctl-file",		required_argument,	NULL, 'S'},
                        {"agent-alarms",	no_argument,		NULL, 't'},
                        {"transport",		optional_argument,	NULL, 'T'},
                        {"uid",			required_argument,	NULL, 'u'},
                        {"userid",		required_argument,	NULL, 'u'},
                        {"dont-remove-pidfile",	no_argument,		NULL, 'U'},
                        {"leave-pidfile",	no_argument,		NULL, 'U'},
                        {"version",		no_argument,		NULL, 'v'},
                        {"verbose",		optional_argument,	NULL, 'V'},
                        {"agentx-socket",	required_argument,	NULL, 'x'},
                        {"agentx",		no_argument,		NULL, 'X'},
                        {"copying",		no_argument,		NULL, 'y'},
#if 0
                        {"directory",		required_argument,	NULL, 'd'},
                        {"basename",		required_argument,	NULL, 'b'},
                        {"outfile",		required_argument,	NULL, 'o'},
                        {"errfile",		required_argument,	NULL, 'e'},
#endif
                        { 0, }
                };
                /* *INDENT-ON* */

		c = getopt_long_only(argc, argv, ":aAc:CdD::fg:hHI:kl::L::m:M::n::o::p:P:qrs::S:tT::u:UvV::x:Xy", long_options, &option_index);
#else				/* defined _GNU_SOURCE */
		c = getopt(argc, argv, ":aAc:CdD::fg:hHI:kl::L::m:M::n::o::p:P:qrs::S:tT::u:UvV::x:Xy");
#endif				/* defined _GNU_SOURCE */
		if (c == -1) {
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: done options processing\n", argv[0]);
			break;
		}
		switch (c) {
		case 0:
			goto bad_usage;
		case 'a':	/* -a, --log-addresses */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: logging addresses\n", argv[0]);
			sa_logaddr++;
			break;
		case 'A':	/* -A, --append */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: will not truncate logfile\n", argv[0]);
#if defined NETSNMP_DS_LIB_APPEND_LOGFILES
			ds_set_boolean(DS_LIBRARY_ID, NETSNMP_DS_LIB_APPEND_LOGFILES, 1);
#endif				/* defined NETSNMP_DS_LIB_APPEND_LOGFILES */
			sa_appendlog = 1;
			break;
		case 'c':	/* -c, --config-file CONFIGFILE */
			if (optarg == NULL)
				goto bad_option;
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: using configuration file %s\n", argv[0], optarg);
			ds_set_string(DS_LIBRARY_ID, DS_LIB_OPTIONALCONFIG, optarg);
			break;
		case 'C':	/* -C, --no-configs */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: not reading default config files\n", argv[0]);
			ds_set_boolean(DS_LIBRARY_ID, DS_LIB_DONT_READ_CONFIGS, 1);
			break;
		case 'd':	/* -d, --dump */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: setting packet dump\n", argv[0]);
			sa_dump = 1;
			// snmp_set_dump_packet(sa_dump);
			ds_set_boolean(DS_LIBRARY_ID, DS_LIB_DUMP_PACKET, sa_dump);
			ds_set_boolean(DS_APPLICATION_ID, DS_AGENT_VERBOSE, sa_dump);
			break;
		case 'D':	/* -D, --debug [LEVEL], --debug-tokens [TOKENS] */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: increasing debug verbosity\n", argv[0]);
			if (optarg == NULL) {
				/* no option: must be -D, --debug */
				sa_debug++;
				if (sa_debug)
					snmp_log(MY_FACILITY(LOG_DEBUG), "%s: debug level is now %d\n", argv[0], sa_debug);
				if (sa_debug)
					snmp_log(MY_FACILITY(LOG_DEBUG), "%s: debugging all tokens\n", argv[0]);
				if (sa_debug)
					debug_register_tokens("ALL");
			} else {
				cptr = optarg;
				if ((val = strtol(optarg, &cptr, 0)) < 0)
					goto bad_option;
				if (*cptr == '\0') {
					/* it is just a number, must be -D, --debug [LEVEL] */
					sa_debug = val;
					if (sa_debug)
						snmp_log(MY_FACILITY(LOG_DEBUG), "%s: debug level is now %d\n", argv[0], sa_debug);
				} else {
					/* not a number, must be -D, --debug-tokens TOKENS */
					if (sa_debug)
						snmp_log(MY_FACILITY(LOG_DEBUG), "%s: debugging tokens %s\n", argv[0], optarg);
					debug_register_tokens(optarg);
				}
			}
			break;
		case 'f':	/* -f, --dont-fork */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: suppressing daemon mode\n", argv[0]);
			sa_nomead = 0;
			break;
		case 'u':	/* -u, --uid, --userid UID */
			cptr = optarg;
			if ((val = strtol(optarg, &cptr, 0)) < 0)
				goto bad_option;
			/* UID can be name or number */
			if ((pw = (*cptr == '\0') ? getpwuid((uid_t) val) : getpwnam(optarg)) == NULL)
				goto bad_option;
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: will run as uid %s(%d)\n", argv[0], pw->pw_name, pw->pw_uid);
			ds_set_int(DS_APPLICATION_ID, DS_AGENT_USERID, pw->pw_uid);
			break;
		case 'g':	/* -g, --gid, --groupdid GID */
			cptr = optarg;
			if ((val = strtol(optarg, &cptr, 0)) < 0)
				goto bad_option;
			/* GID can be name or number */
			if ((gr = (*cptr == '\0') ? getgrgid((gid_t) val) : getgrnam(optarg)) == NULL)
				goto bad_option;
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: will run as gid %s(%d)\n", argv[0], gr->gr_name, gr->gr_gid);
			ds_set_int(DS_APPLICATION_ID, DS_AGENT_GROUPID, gr->gr_gid);
			break;
		case 'h':	/* -h, --help, -?, --? */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: printing help message\n", argv[0]);
			sa_help(argc, argv);
			exit(0);
		case 'H':	/* -H, --help-directives */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: printing config directives\n", argv[0]);
			sa_help_directives(argc, argv);
			exit(0);
		case 'I':	/* -I, --init-modules, --initialize MODULE[{,| |:}MODULE]* */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: will initialize modules: %s\n", argv[0], optarg);
			add_to_init_list(optarg);
			break;
		case 'k':	/* -k, --keep-open */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: keeping files open\n", argv[0]);
			sa_fclose = 0;
			break;
		case 'l':	/* -l, --log-file, --logfile, -Lf, -LF p1[-p2] [LOGFILE] */
			if (optarg != NULL)
				strncpy(sa_logfile, optarg, sizeof(sa_logfile));
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: will log to file %s\n", argv[0], sa_logfile);
			sa_logfillog = 1;
			break;
		case 'L':	/* -L, --log-stderr, -Le, -LE p1[-p2] */
			/* Note that the recent NET-SNMP version of this option is far more complicated: -Le is the same as the old version of the option; -Lf LOGFILE is like the -l option; -Ls is
			   like the -s option; -Lo logs messages to standard output; -LX p1[-p2] [LOGFILE], where X = E, F, S or O, logs priority p1 and above to X, or p1 thru p2 to X. */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: logging to standard error\n", argv[0]);
			sa_logstderr = 1;
			break;
		case 'm':	/* -m, --mibs MIBS */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: using MIBS %s\n", argv[0], optarg);
			break;
		case 'M':	/* -M, --master or -M, --mibdirs MIBDIRS */
			if (optarg) {
				/* -M, --mibdirs MIBDIRS */
				if (sa_debug)
					snmp_log(MY_FACILITY(LOG_DEBUG), "%s: using MIBDIRS %s\n", argv[0], optarg);
			} else {
				/* -M, --master */
				if (sa_debug)
					snmp_log(MY_FACILITY(LOG_DEBUG), "%s: setting SNMP master\n", argv[0]);
				sa_agentx = 0;
				ds_set_boolean(DS_APPLICATION_ID, DS_AGENT_ROLE, 0);
			}
			break;
		case 'n':	/* -n, --nodaemon or -n, --name NAME */
			if (optarg) {
				/* -n, --name NAME */
				if (sa_debug)
					snmp_log(MY_FACILITY(LOG_DEBUG), "%s: using name %s\n", argv[0], optarg);
				ds_set_string(DS_APPLICATION_ID, DS_AGENT_PROGNAME, optarg);
			} else {
				/* -n, --nodaemon */
				if (sa_debug)
					snmp_log(MY_FACILITY(LOG_DEBUG), "%s: suppressing deamon mode\n", argv[0]);
				sa_nomead = 0;
				ds_set_string(DS_APPLICATION_ID, DS_AGENT_PROGNAME, basename(argv[0]));
			}
			break;
		case 'N':	/* -N, --dry-run */
#if defined NETSNMP_DS_AGENT_QUIT_IMMEDIATELY
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: setting for dry-runs startup\n", argv[0]);
			ds_set_boolean(DS_APPLICATION_ID, NETSNMP_DS_AGENT_QUIT_IMMEDIATELY, 1);
			break;
#else				/* defined NETSNMP_DS_AGENT_QUIT_IMMEDIATELY */
			snmp_log(MY_FACILITY(LOG_DEBUG), "%s: -N option not supported\n", argv[0]);
			goto bad_option;
#endif				/* defined NETSNMP_DS_AGENT_QUIT_IMMEDIATELY */
		case 'o':	/* -o, --log-stdout, -Lo, -LO p1[-p2] */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: logging to stdout\n", argv[0]);
			sa_logstdout = 1;
			break;
		case 'p':	/* -p, --port PORTNUM or -p, --pidfile PIDFILE */
			cptr = optarg;
			if ((val = strtol(optarg, &cptr, 0)) < 0 || val > 16383)
				goto bad_option;
			if (*cptr == '\0') {
				char buf[4096];

				/* -p, --port PORTNUM */
				if ((cptr = ds_get_string(DS_APPLICATION_ID, DS_AGENT_PORTS)))
					snprintf(buf, sizeof(buf), "%s,%s", cptr, optarg);
				else
					strncpy(buf, optarg, sizeof(buf));
				ds_set_string(DS_APPLICATION_ID, DS_AGENT_PORTS, buf);
				break;
			}
			/* fall through */
		case 'P':	/* -p, -P, --pidfile PIDFILE */
			if (optarg) {
				/* either it exists */
				if (stat(optarg, &st) == -1) {
					/* or we can create it */
					if ((fd = open(optarg, O_CREAT, 0600)) == -1) {
						perror(argv[0]);
						goto bad_option;
					}
					close(fd);
				}
				if (sa_debug)
					snmp_log(MY_FACILITY(LOG_DEBUG), "%s: setting pid file to %s\n", argv[0], optarg);
				strncpy(sa_pidfile, optarg, sizeof(sa_pidfile));
			}
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: using pidfile %s\n", argv[0], sa_pidfile);
			break;
		case 'q':	/* -q, --quiet, --quick */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: suppressing normal output\n", argv[0]);
			sa_debug = 0;
			sa_output = 0;
			ds_set_boolean(DS_APPLICATION_ID, DS_AGENT_VERBOSE, 0);
			// snmp_set_quick_print();
			ds_set_boolean(DS_LIBRARY_ID, DS_LIB_QUICK_PRINT, 1);
			break;
		case 'r':	/* -r, --noroot */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: setting for non-root access\n", argv[0]);
			ds_set_boolean(DS_APPLICATION_ID, DS_AGENT_NO_ROOT_ACCESS, 1);
			break;
		case 's':	/* -s, --log-syslog, -Ls, -LS p1[-p2] */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: logging to system logs\n", argv[0]);
			sa_logsyslog = 1;
			break;
		case 'S':	/* -S, -sysctl-file FILENAME */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: using %s for backing\n", argv[0], optarg);
			strncpy(sa_sysctlf, optarg, sizeof(sa_sysctlf));
			break;
		case 't':	/* -t, --agent-alarms */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: setting agent alarms\n", argv[0]);
			sa_alarms = 0;
			ds_set_boolean(DS_LIBRARY_ID, DS_LIB_ALARM_DONT_USE_SIG, 1);
			break;
		case 'T':	/* -T, --transport [TRANSPORT] */
			if (optarg == NULL)
				goto udp_transport;
			if (!strcasecmp("TCP", optarg)) {
				if (sa_debug)
					snmp_log(MY_FACILITY(LOG_DEBUG), "%s: setting default transport to TCP\n", argv[0]);
				val = ds_get_int(DS_APPLICATION_ID, DS_AGENT_FLAGS);
				val |= SNMP_FLAGS_STREAM_SOCKET;
				ds_set_int(DS_APPLICATION_ID, DS_AGENT_FLAGS, val);
			} else if (!strcasecmp("UDP", optarg)) {
			      udp_transport:
				if (sa_debug)
					snmp_log(MY_FACILITY(LOG_DEBUG), "%s: setting default transport to UDP\n", argv[0]);
				val = ds_get_int(DS_APPLICATION_ID, DS_AGENT_FLAGS);
				val &= ~SNMP_FLAGS_STREAM_SOCKET;
				ds_set_int(DS_APPLICATION_ID, DS_AGENT_FLAGS, val);
			} else
				goto bad_option;
			break;
		case 'U':
#if defined NETSNMP_DS_AGENT_LEAVE_PIDFILE
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: will leave pidfile after shutdown\n", argv[0]);
			ds_set_boolean(DS_APPLICATION_ID, NETSNMP_DS_AGENT_LEAVE_PIDFILE, 1);
#else
			snmp_log(MY_FACILITY(LOG_DEBUG), "%s: -U option not supported\n");
			goto bad_option;
#endif				/* defined NETSNMP_DS_AGENT_LEAVE_PIDFILE */
			break;
		case 'v':	/* -v, --version */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: printing version message\n", argv[0]);
			sa_version(argc, argv);
			exit(0);
		case 'V':	/* -V, --verbose [LEVEL] */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: increasing output verbosity\n", argv[0]);
			if (optarg == NULL) {
				sa_output++;
			} else {
				if ((val = strtol(optarg, NULL, 0)) < 0)
					goto bad_option;
				sa_output = val;
			}
			if (sa_output > 1)
				ds_set_boolean(DS_APPLICATION_ID, DS_AGENT_VERBOSE, 1);
			else
				ds_set_boolean(DS_APPLICATION_ID, DS_AGENT_VERBOSE, 0);
			break;
		case 'x':	/* -x, --agentx-socket SOCKET */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: setting AgentX socket to %s\n", argv[0], optarg);
			ds_set_string(DS_APPLICATION_ID, DS_AGENT_X_SOCKET, optarg);
			// ds_set_boolean(DS_APPLICATION_ID, DS_AGENT_AGENTX_MASTER, 1);
			break;
		case 'X':	/* -X, --agentx */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: setting AgentX sub-agent\n", argv[0]);
			sa_agentx = 1;
			ds_set_boolean(DS_APPLICATION_ID, DS_AGENT_ROLE, 1);
			break;
		case 'y':	/* -y, --copying */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: printing copying message\n", argv[0]);
			sa_copying(argc, argv);
			exit(0);
		case '?':
		case ':':
		default:
		      bad_option:
			optind--;
			goto bad_nonopt;
		      bad_nonopt:
			if (sa_output || sa_debug) {
				if (optind < argc) {
					fprintf(stderr, "%s: syntax error near '", argv[0]);
					while (optind < argc)
						fprintf(stderr, "%s ", argv[optind++]);
					fprintf(stderr, "'\n");
				} else {
					fprintf(stderr, "%s: missing option or argument", argv[0]);
					fprintf(stderr, "\n");
				}
				fflush(stderr);
			      bad_usage:
				sa_usage(argc, argv);
			}
			exit(2);
		}
	}
	if (optind < argc) {
		if (sa_debug)
			snmp_log(MY_FACILITY(LOG_DEBUG), "%s: excess non-option arguments\n", argv[0]);
		goto bad_nonopt;
	}
	sa_enter(argc, argv);	/* daemonize if necessary */
	sa_mloop(argc, argv);	/* execute main loop */
	exit(0);
}
#endif				/* !defined MODULE */
#endif				/* defined MASTER */
