/* This file was generated by mib2c and is intended for use as
   a mib module for the ucd-snmp snmpd agent. */

#include <ucd-snmp/ucd-snmp-config.h>
#include <ucd-snmp/ucd-snmp-includes.h>
#include <ucd-snmp/ucd-snmp-agent-includes.h>
#include <ucd-snmp/callback.h>
#include <ucd-snmp/snmp-tc.h>
#include <ucd-snmp/default_store.h>
#include <ucd-snmp/snmp_alarm.h>

/* The following header files are mangled in most recent net-snmp releases so
 * the versions from UCD-SNMP 4.2.5 are included here.  */
#if defined HAVE_LIBNETSNMP
#else				/* defined HAVE_LIBNETSNMP */
#endif				/* defined HAVE_LIBNETSNMP */

/* These are messed up on both. */
#include "ds_agent.h"

#ifdef HAVE_UCD_SNMP_UTIL_FUNCS_H
#include <ucd-snmp/util_funcs.h>
/* Many recent net-snmp UCD compatible headers do not declard header_generic. */
int header_generic(struct variable *, oid *, size_t *, int, size_t *, WriteMethod **);
#else				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#include "util_funcs.h"
#endif				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */

#ifdef HAVE_UCD_SNMP_HEADER_COMPLEX_H
#include <ucd-snmp/header_complex.h>
#else				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
#include "header_complex.h"
#endif				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */

/* This one is the other way around: it is just fine for net-snmp, but
 * ucd-snmp does not provide the header file at all.  */
#ifdef HAVE_UCD_SNMP_MIB_MODULES_H
#include <ucd-snmp/mib_modules.h>
#else				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#ifdef HAVE_NET_SNMP_AGENT_MIB_MODULES_H
#include <net-snmp/agent/mib_modules.h>
#else				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#include "mib_modules.h"
#endif				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#endif				/* HAVE_UCD_SNMP_MIB_MODULES_H */

#include "mtpOmMIB_openss7.h"

#include <stdint.h>
#include <signal.h>
#include <sys/stat.h>		/* for struct stat, fstat() */
#include <sys/types.h>
#include <unistd.h>
#include <stdlib.h>
#include <pwd.h>		/* for getpwuid() getpwnam() */
#include <grp.h>		/* for getgrgid() getgrnam() */
#include <libgen.h>		/* for basename() */
#include <fcntl.h>		/* for O_CREAT */

#include <sys/sysctl.h>		/* for sysctl */
#include <errno.h>
#include <string.h>

#ifdef _GNU_SOURCE
#include <getopt.h>
#endif

static const char sa_program[] = "mtpommib";

#define MY_FACILITY(__pri)	(LOG_DAEMON|(__pri))

#if !defined MODULE
static int sa_dump = 0;			/* default packet dump */
static int sa_debug = 0;		/* default no debug */
static int sa_nomead = 1;		/* default daemon mode */
static int sa_output = 1;		/* default normal output */
static int sa_agentx = 1;		/* default agentx mode */
static int sa_alarms = 1;		/* default application alarms */
static int sa_fclose = 1;		/* default close files between requests */

static int sa_logaddr = 0;		/* log addresses */
static int sa_logfillog = 0;		/* log to sa_logfile */
static int sa_logstderr = 0;		/* log to standard error */
static int sa_logstdout = 0;		/* log to standard output */
static int sa_logsyslog = 0;		/* log to system logs */
static int sa_logcallog = 0;		/* log to callback logs */
static int sa_appendlog = 0;		/* append to log file without truncating */

static char sa_logfile[256] = "/var/log/mtpommib.log";
static char sa_pidfile[256] = "/var/run/mtpommib.pid";
static char sa_sysctlf[256] = "/etc/mtpommib.conf";

int allow_severity = LOG_ERR;
int deny_severity = LOG_ERR;

/* file stream for log file */
static FILE *stdlog = NULL;

/* file descriptor for MIB use */
static int sa_fd = 0;

/* indication to reread MIB configuration */
static int sa_changed = 1;

/* indications that statistics, the mib or its tables need to be refreshed */
static int sa_stats_refresh = 1;
#endif				/* !defined MODULE */

/* request number for per-request actions */
static int sa_request = 1;

static volatile int mtpOmMIB_refresh = 1;
static volatile int mtpOmHandledMSUsOpcDpcSioTable_refresh = 1;
static volatile int mtpOmHandledOctetsOpcDpcSioTable_refresh = 1;
static volatile int mtpOmHandledOctetsSIOTable_refresh = 1;
static volatile int mtpOmOblSpUtilizationTable_refresh = 1;
static volatile int mtpOmAllSpUtilizationTable_refresh = 1;
static volatile int mtpOmReceivedOctetsOPCTable_refresh = 1;
static volatile int mtpOmReceivedOctetsOpcSioTable_refresh = 1;
static volatile int mtpOmSpDataTable_refresh = 1;
static volatile int mtpOmTransmittedOctetsDPCTable_refresh = 1;
static volatile int mtpOmTransmittedOctetsDPCTable_refresh = 1;
static volatile int mtpOmTransmittedOctetsDPCTable_refresh = 1;
static volatile int mtpOmTransmittedOctetsDpcSioTable_refresh = 1;
static volatile int mtpOmSignRsInformationTable_refresh = 1;
static volatile int mtpOmSlsDurationTable_refresh = 1;
static volatile int mtpOmAll5And30MinSlDurationTable_refresh = 1;
static volatile int mtpOmOblSlDurationTable_refresh = 1;
static volatile int mtpOmAll30MinSlDurationTable_refresh = 1;
static volatile int mtpOmOblSlUtilizationTable_refresh = 1;
static volatile int mtpOmAllSlUtilizationTable_refresh = 1;
static volatile int mtpOmSlCongestionTable_refresh = 1;

/* 
 * mtpOmMIB_variables_oid:
 *   this is the top level oid that we want to register under.  This
 *   is essentially a prefix, with the suffix appearing in the
 *   variable below.
 */

oid mtpOmMIB_variables_oid[] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 1 };
oid mtpOmHandledMSUsOpcDpcSioTable_variables_oid[] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 1, 1, 1, 1, 1 };
oid mtpOmHandledOctetsOpcDpcSioTable_variables_oid[] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 1, 1, 1, 2, 1 };
oid mtpOmAllSpUtilizationTable_variables_oid[] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 1, 1, 1, 5, 1 };
oid mtpOmReceivedOctetsOPCTable_variables_oid[] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 1, 1, 1, 6, 1 };
oid mtpOmReceivedOctetsOpcSioTable_variables_oid[] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 1, 1, 1, 7, 1 };
oid mtpOmTransmittedOctetsDPCTable_variables_oid[] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 1, 1, 1, 9 };
oid mtpOmTransmittedOctetsDpcSioTable_variables_oid[] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 1, 1, 1, 10, 1 };
oid mtpOmAll5And30MinSlDurationTable_variables_oid[] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 1, 1, 1, 13, 1 };
oid mtpOmAll30MinSlDurationTable_variables_oid[] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 1, 1, 1, 15, 1 };
oid mtpOmAllSlUtilizationTable_variables_oid[] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 1, 1, 1, 17, 1 };
oid mtpOmSlCongestionTable_variables_oid[] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 1, 1, 1, 18, 1 };

/* 
 * variable7 mtpOmMIB_variables:
 *   this variable defines function callbacks and type return information 
 *   for the mtpOmMIB mib section 
 */

struct variable7 mtpOmMIB_variables[] = {
/*  magic number        , variable type , ro/rw , callback fn  , L, oidsuffix */
#define   MTPOMHANDLEDMSUSOPCDPCSIO  6
	{MTPOMHANDLEDMSUSOPCDPCSIO, ASN_GAUGE, RONLY, var_mtpOmHandledMSUsOpcDpcSioTable, 5, {1, 1, 1, 1, 4}},
#define   MTPOMHANDLEDMSUSOPCDPCSIOROWSTATUS  7
	{MTPOMHANDLEDMSUSOPCDPCSIOROWSTATUS, ASN_INTEGER, RWRITE, var_mtpOmHandledMSUsOpcDpcSioTable, 5, {1, 1, 1, 1, 5}},
#define   MTPOMHANDLEDOCTETSOPCDPCSIOINTERVAL  13
	{MTPOMHANDLEDOCTETSOPCDPCSIOINTERVAL, ASN_INTEGER, RWRITE, var_mtpOmHandledOctetsOpcDpcSioTable, 5, {1, 1, 2, 1, 1}},
#define   MTPOMHANDLEDOCTETSOPCDPCSIO  14
	{MTPOMHANDLEDOCTETSOPCDPCSIO, ASN_GAUGE, RONLY, var_mtpOmHandledOctetsOpcDpcSioTable, 5, {1, 1, 2, 1, 2}},
#define   MTPOMHANDLEDOCTETSOPCDPCSIOROWSTATUS  15
	{MTPOMHANDLEDOCTETSOPCDPCSIOROWSTATUS, ASN_INTEGER, RWRITE, var_mtpOmHandledOctetsOpcDpcSioTable, 5, {1, 1, 2, 1, 3}},
#define   MTPOMHANDLEDOCTETSSIO  19
	{MTPOMHANDLEDOCTETSSIO, ASN_GAUGE, RONLY, var_mtpOmHandledOctetsSIOTable, 5, {1, 1, 3, 1, 1}},
#define   MTPOMADJACENTINACCESSIBLEDURATION  22
	{MTPOMADJACENTINACCESSIBLEDURATION, ASN_INTEGER, RONLY, var_mtpOmOblSpUtilizationTable, 5, {1, 1, 4, 1, 1}},
#define   MTPOMADJACENTINACCESSIBLEEVENTS  25
	{MTPOMADJACENTINACCESSIBLEEVENTS, ASN_GAUGE, RONLY, var_mtpOmAllSpUtilizationTable, 5, {1, 1, 5, 1, 1}},
#define   MTPOMALLSPUTILIZATIONROWSTATUS  26
	{MTPOMALLSPUTILIZATIONROWSTATUS, ASN_INTEGER, RWRITE, var_mtpOmAllSpUtilizationTable, 5, {1, 1, 5, 1, 2}},
#define   MTPOMRECEIVEDOCTETSOPCINTERVAL  30
	{MTPOMRECEIVEDOCTETSOPCINTERVAL, ASN_INTEGER, RWRITE, var_mtpOmReceivedOctetsOPCTable, 5, {1, 1, 6, 1, 1}},
#define   MTPOMRECEIVEDOCTETSOPC  31
	{MTPOMRECEIVEDOCTETSOPC, ASN_GAUGE, RONLY, var_mtpOmReceivedOctetsOPCTable, 5, {1, 1, 6, 1, 2}},
#define   MTPOMRECEIVEDOCTETSOPCROWSTATUS  32
	{MTPOMRECEIVEDOCTETSOPCROWSTATUS, ASN_INTEGER, RWRITE, var_mtpOmReceivedOctetsOPCTable, 5, {1, 1, 6, 1, 3}},
#define   MTPOMRECEIVEDOCTETSOPCSIOINTERVAL  37
	{MTPOMRECEIVEDOCTETSOPCSIOINTERVAL, ASN_INTEGER, RWRITE, var_mtpOmReceivedOctetsOpcSioTable, 5, {1, 1, 7, 1, 1}},
#define   MTPOMRECEIVEDOCTETSOPCSIO  38
	{MTPOMRECEIVEDOCTETSOPCSIO, ASN_GAUGE, RONLY, var_mtpOmReceivedOctetsOpcSioTable, 5, {1, 1, 7, 1, 2}},
#define   MTPOMRECEIVEDOCTETSOPCSIOROWSTATUS  39
	{MTPOMRECEIVEDOCTETSOPCSIOROWSTATUS, ASN_INTEGER, RWRITE, var_mtpOmReceivedOctetsOpcSioTable, 5, {1, 1, 7, 1, 3}},
#define   MTPOMSPDISCARDEDMSUS  44
	{MTPOMSPDISCARDEDMSUS, ASN_GAUGE, RONLY, var_mtpOmSpDataTable, 5, {1, 1, 8, 1, 3}},
#define   MTPOMSPTRANSMITTEDUPUNAVAILABLE  45
	{MTPOMSPTRANSMITTEDUPUNAVAILABLE, ASN_GAUGE, RONLY, var_mtpOmSpDataTable, 5, {1, 1, 8, 1, 4}},
#define   MTPOMSPRECEIVEDUPUNAVAILABLE  46
	{MTPOMSPRECEIVEDUPUNAVAILABLE, ASN_GAUGE, RONLY, var_mtpOmSpDataTable, 5, {1, 1, 8, 1, 5}},
#define   MTPOMSPRECEIVEDTFC    47
	{MTPOMSPRECEIVEDTFC, ASN_GAUGE, RONLY, var_mtpOmSpDataTable, 5, {1, 1, 8, 1, 6}},
#define   MTPOMSPRECEIVEDTFCLEVEL0  48
	{MTPOMSPRECEIVEDTFCLEVEL0, ASN_GAUGE, RONLY, var_mtpOmSpDataTable, 5, {1, 1, 8, 1, 7}},
#define   MTPOMSPRECEIVEDTFCLEVEL1  49
	{MTPOMSPRECEIVEDTFCLEVEL1, ASN_GAUGE, RONLY, var_mtpOmSpDataTable, 5, {1, 1, 8, 1, 8}},
#define   MTPOMSPRECEIVEDTFCLEVEL2  50
	{MTPOMSPRECEIVEDTFCLEVEL2, ASN_GAUGE, RONLY, var_mtpOmSpDataTable, 5, {1, 1, 8, 1, 9}},
#define   MTPOMSPRECEIVEDTFCLEVEL3  51
	{MTPOMSPRECEIVEDTFCLEVEL3, ASN_GAUGE, RONLY, var_mtpOmSpDataTable, 5, {1, 1, 8, 1, 10}},
#define   MTPOMTRANSMITTEDOCTETSDPCROWSTATUS  57
	{MTPOMTRANSMITTEDOCTETSDPCROWSTATUS, ASN_INTEGER, RONLY, var_mtpOmTransmittedOctetsDPCTable, 4, {1, 1, 9, 2}},
#define   MTPOMTRANSMITTEDOCTETSDPCSIO  62
	{MTPOMTRANSMITTEDOCTETSDPCSIO, ASN_GAUGE, RONLY, var_mtpOmTransmittedOctetsDpcSioTable, 5, {1, 1, 10, 1, 1}},
#define   MTPOMTRANSMITTEDOCTETSDPCSIOROWSTATUS  63
	{MTPOMTRANSMITTEDOCTETSDPCSIOROWSTATUS, ASN_INTEGER, RWRITE, var_mtpOmTransmittedOctetsDpcSioTable, 5, {1, 1, 10, 1, 2}},
#define   MTPOMROUTESETUNAVAILABLE  66
	{MTPOMROUTESETUNAVAILABLE, ASN_GAUGE, RONLY, var_mtpOmSignRsInformationTable, 5, {1, 1, 11, 1, 1}},
#define   MTPOMROUTESETUNAVAILABLEDURATION  67
	{MTPOMROUTESETUNAVAILABLEDURATION, ASN_INTEGER, RONLY, var_mtpOmSignRsInformationTable, 5, {1, 1, 11, 1, 2}},
#define   MTPOMSLSUNAVAILABLE   70
	{MTPOMSLSUNAVAILABLE, ASN_INTEGER, RONLY, var_mtpOmSlsDurationTable, 5, {1, 1, 12, 1, 1}},
#define   MTPOMALL5AND30MINSLDURATIONINTERVAL  73
	{MTPOMALL5AND30MINSLDURATIONINTERVAL, ASN_INTEGER, RWRITE, var_mtpOmAll5And30MinSlDurationTable, 5, {1, 1, 13, 1, 1}},
#define   MTPOMLOCALMGNTINHIBIT  74
	{MTPOMLOCALMGNTINHIBIT, ASN_INTEGER, RONLY, var_mtpOmAll5And30MinSlDurationTable, 5, {1, 1, 13, 1, 2}},
#define   MTPOMLOCALMGNTUNINHIBIT  75
	{MTPOMLOCALMGNTUNINHIBIT, ASN_INTEGER, RONLY, var_mtpOmAll5And30MinSlDurationTable, 5, {1, 1, 13, 1, 3}},
#define   MTPOMLOCALBUSY        76
	{MTPOMLOCALBUSY, ASN_GAUGE, RONLY, var_mtpOmAll5And30MinSlDurationTable, 5, {1, 1, 13, 1, 4}},
#define   MTPOMALL5AND30MINSLDURATIONROWSTATUS  77
	{MTPOMALL5AND30MINSLDURATIONROWSTATUS, ASN_INTEGER, RWRITE, var_mtpOmAll5And30MinSlDurationTable, 5, {1, 1, 13, 1, 5}},
#define   MTPOMSLUNAVAILABILITYDURATION  80
	{MTPOMSLUNAVAILABILITYDURATION, ASN_INTEGER, RONLY, var_mtpOmOblSlDurationTable, 5, {1, 1, 14, 1, 1}},
#define   MTPOMSLINSERVICEDURATION  81
	{MTPOMSLINSERVICEDURATION, ASN_INTEGER, RONLY, var_mtpOmOblSlDurationTable, 5, {1, 1, 14, 1, 2}},
#define   MTPOMALL30MINSLDURATIONINTERVAL  84
	{MTPOMALL30MINSLDURATIONINTERVAL, ASN_INTEGER, RWRITE, var_mtpOmAll30MinSlDurationTable, 5, {1, 1, 15, 1, 1}},
#define   MTPOMSLLOCALINHIBITION  85
	{MTPOMSLLOCALINHIBITION, ASN_INTEGER, RONLY, var_mtpOmAll30MinSlDurationTable, 5, {1, 1, 15, 1, 2}},
#define   MTPOMSLREMOTEINHIBITION  86
	{MTPOMSLREMOTEINHIBITION, ASN_INTEGER, RONLY, var_mtpOmAll30MinSlDurationTable, 5, {1, 1, 15, 1, 3}},
#define   MTPOMSLFAILED         87
	{MTPOMSLFAILED, ASN_INTEGER, RONLY, var_mtpOmAll30MinSlDurationTable, 5, {1, 1, 15, 1, 4}},
#define   MTPOMSLREMOTEPROCOUTAGE  88
	{MTPOMSLREMOTEPROCOUTAGE, ASN_INTEGER, RONLY, var_mtpOmAll30MinSlDurationTable, 5, {1, 1, 15, 1, 5}},
#define   MTPOMALL30MINSLDURATIONROWSTATUS  89
	{MTPOMALL30MINSLDURATIONROWSTATUS, ASN_INTEGER, RWRITE, var_mtpOmAll30MinSlDurationTable, 5, {1, 1, 15, 1, 6}},
#define   MTPOMSLSIGNUNITSRECEIVED  92
	{MTPOMSLSIGNUNITSRECEIVED, ASN_GAUGE, RONLY, var_mtpOmOblSlUtilizationTable, 5, {1, 1, 16, 1, 1}},
#define   MTPOMSLTRANSMITTEDOCTETSSIFSIO  93
	{MTPOMSLTRANSMITTEDOCTETSSIFSIO, ASN_GAUGE, RONLY, var_mtpOmOblSlUtilizationTable, 5, {1, 1, 16, 1, 2}},
#define   MTPOMSLRECEIVEDOCTETSSIFSIO  94
	{MTPOMSLRECEIVEDOCTETSSIFSIO, ASN_GAUGE, RONLY, var_mtpOmOblSlUtilizationTable, 5, {1, 1, 16, 1, 3}},
#define   MTPOMSLDISCARDEDMSUS  95
	{MTPOMSLDISCARDEDMSUS, ASN_GAUGE, RONLY, var_mtpOmOblSlUtilizationTable, 5, {1, 1, 16, 1, 4}},
#define   MTPOMSLALIGNMENT      98
	{MTPOMSLALIGNMENT, ASN_GAUGE, RONLY, var_mtpOmAllSlUtilizationTable, 5, {1, 1, 17, 1, 1}},
#define   MTPOMSLNEGACKRECEIVED  99
	{MTPOMSLNEGACKRECEIVED, ASN_GAUGE, RONLY, var_mtpOmAllSlUtilizationTable, 5, {1, 1, 17, 1, 2}},
#define   MTPOMSLRETRANSMITTEDOCTETS  100
	{MTPOMSLRETRANSMITTEDOCTETS, ASN_GAUGE, RONLY, var_mtpOmAllSlUtilizationTable, 5, {1, 1, 17, 1, 3}},
#define   MTPOMSLTRANSMITTEDMSUS  101
	{MTPOMSLTRANSMITTEDMSUS, ASN_GAUGE, RONLY, var_mtpOmAllSlUtilizationTable, 5, {1, 1, 17, 1, 4}},
#define   MTPOMSLRECEIVEDMSUS   102
	{MTPOMSLRECEIVEDMSUS, ASN_GAUGE, RONLY, var_mtpOmAllSlUtilizationTable, 5, {1, 1, 17, 1, 5}},
#define   MTPOMALLSLUTILIZATIONROWSTATUS  103
	{MTPOMALLSLUTILIZATIONROWSTATUS, ASN_INTEGER, RWRITE, var_mtpOmAllSlUtilizationTable, 5, {1, 1, 17, 1, 6}},
#define   MTPOMSLCONGESTIONFIRST  107
	{MTPOMSLCONGESTIONFIRST, ASN_INTEGER, RWRITE, var_mtpOmSlCongestionTable, 5, {1, 1, 18, 1, 1}},
#define   MTPOMSLCONGESTIONINTERVAL  108
	{MTPOMSLCONGESTIONINTERVAL, ASN_INTEGER, RWRITE, var_mtpOmSlCongestionTable, 5, {1, 1, 18, 1, 2}},
#define   MTPOMSLCONGESTIONSTARTS  109
	{MTPOMSLCONGESTIONSTARTS, ASN_GAUGE, RONLY, var_mtpOmSlCongestionTable, 5, {1, 1, 18, 1, 3}},
#define   MTPOMSLCONGESTIONSTOPS  110
	{MTPOMSLCONGESTIONSTOPS, ASN_GAUGE, RONLY, var_mtpOmSlCongestionTable, 5, {1, 1, 18, 1, 4}},
#define   MTPOMSLCONGESTIONEVENTSMSULOSS  111
	{MTPOMSLCONGESTIONEVENTSMSULOSS, ASN_GAUGE, RONLY, var_mtpOmSlCongestionTable, 5, {1, 1, 18, 1, 5}},
#define   MTPOMSLCONGESTIONROWSTATUS  112
	{MTPOMSLCONGESTIONROWSTATUS, ASN_INTEGER, RWRITE, var_mtpOmSlCongestionTable, 5, {1, 1, 18, 1, 6}},

};

/*    (L = length of the oidsuffix) */

static struct mtpOmMIB_data *mtpOmMIBStorage = NULL;

/* global storage of our data, saved in and configured by header_complex() */
static struct header_complex_index *mtpOmHandledMSUsOpcDpcSioTableStorage = NULL;
static struct header_complex_index *mtpOmHandledOctetsOpcDpcSioTableStorage = NULL;
static struct header_complex_index *mtpOmHandledOctetsSIOTableStorage = NULL;
static struct header_complex_index *mtpOmOblSpUtilizationTableStorage = NULL;
static struct header_complex_index *mtpOmAllSpUtilizationTableStorage = NULL;
static struct header_complex_index *mtpOmReceivedOctetsOPCTableStorage = NULL;
static struct header_complex_index *mtpOmReceivedOctetsOpcSioTableStorage = NULL;
static struct header_complex_index *mtpOmSpDataTableStorage = NULL;
static struct header_complex_index *mtpOmTransmittedOctetsDPCTableStorage = NULL;
static struct header_complex_index *mtpOmTransmittedOctetsDPCTableStorage = NULL;
static struct header_complex_index *mtpOmTransmittedOctetsDPCTableStorage = NULL;
static struct header_complex_index *mtpOmTransmittedOctetsDpcSioTableStorage = NULL;
static struct header_complex_index *mtpOmSignRsInformationTableStorage = NULL;
static struct header_complex_index *mtpOmSlsDurationTableStorage = NULL;
static struct header_complex_index *mtpOmAll5And30MinSlDurationTableStorage = NULL;
static struct header_complex_index *mtpOmOblSlDurationTableStorage = NULL;
static struct header_complex_index *mtpOmAll30MinSlDurationTableStorage = NULL;
static struct header_complex_index *mtpOmOblSlUtilizationTableStorage = NULL;
static struct header_complex_index *mtpOmAllSlUtilizationTableStorage = NULL;
static struct header_complex_index *mtpOmSlCongestionTableStorage = NULL;

/*
 * init_mtpOmMIB():
 *   Initialization routine.  This is called when the agent starts up.
 *   At a minimum, registration of your variables should take place here.
 */
void
init_mtpOmMIB(void)
{
	DEBUGMSGTL(("mtpOmMIB", "initializing...  "));

	/* register ourselves with the agent to handle our mib tree */
	REGISTER_MIB("mtpOmMIB", mtpOmMIB_variables, variable7, mtpOmMIB_variables_oid);

	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_SHUTDOWN, term_mtpOmMIB, NULL);

	/* register our config handler(s) to deal with registrations */
	snmpd_register_config_handler("mtpOmMIB", parse_mtpOmMIB, NULL, "HELP STRING");
	snmpd_register_config_handler("mtpOmHandledMSUsOpcDpcSioTable", parse_mtpOmHandledMSUsOpcDpcSioTable, NULL, "HELP STRING");
	snmpd_register_config_handler("mtpOmHandledOctetsOpcDpcSioTable", parse_mtpOmHandledOctetsOpcDpcSioTable, NULL, "HELP STRING");
	snmpd_register_config_handler("mtpOmHandledOctetsSIOTable", parse_mtpOmHandledOctetsSIOTable, NULL, "HELP STRING");
	snmpd_register_config_handler("mtpOmOblSpUtilizationTable", parse_mtpOmOblSpUtilizationTable, NULL, "HELP STRING");
	snmpd_register_config_handler("mtpOmAllSpUtilizationTable", parse_mtpOmAllSpUtilizationTable, NULL, "HELP STRING");
	snmpd_register_config_handler("mtpOmReceivedOctetsOPCTable", parse_mtpOmReceivedOctetsOPCTable, NULL, "HELP STRING");
	snmpd_register_config_handler("mtpOmReceivedOctetsOpcSioTable", parse_mtpOmReceivedOctetsOpcSioTable, NULL, "HELP STRING");
	snmpd_register_config_handler("mtpOmSpDataTable", parse_mtpOmSpDataTable, NULL, "HELP STRING");
	snmpd_register_config_handler("mtpOmTransmittedOctetsDPCTable", parse_mtpOmTransmittedOctetsDPCTable, NULL, "HELP STRING");
	snmpd_register_config_handler("mtpOmTransmittedOctetsDPCTable", parse_mtpOmTransmittedOctetsDPCTable, NULL, "HELP STRING");
	snmpd_register_config_handler("mtpOmTransmittedOctetsDPCTable", parse_mtpOmTransmittedOctetsDPCTable, NULL, "HELP STRING");
	snmpd_register_config_handler("mtpOmTransmittedOctetsDpcSioTable", parse_mtpOmTransmittedOctetsDpcSioTable, NULL, "HELP STRING");
	snmpd_register_config_handler("mtpOmSignRsInformationTable", parse_mtpOmSignRsInformationTable, NULL, "HELP STRING");
	snmpd_register_config_handler("mtpOmSlsDurationTable", parse_mtpOmSlsDurationTable, NULL, "HELP STRING");
	snmpd_register_config_handler("mtpOmAll5And30MinSlDurationTable", parse_mtpOmAll5And30MinSlDurationTable, NULL, "HELP STRING");
	snmpd_register_config_handler("mtpOmOblSlDurationTable", parse_mtpOmOblSlDurationTable, NULL, "HELP STRING");
	snmpd_register_config_handler("mtpOmAll30MinSlDurationTable", parse_mtpOmAll30MinSlDurationTable, NULL, "HELP STRING");
	snmpd_register_config_handler("mtpOmOblSlUtilizationTable", parse_mtpOmOblSlUtilizationTable, NULL, "HELP STRING");
	snmpd_register_config_handler("mtpOmAllSlUtilizationTable", parse_mtpOmAllSlUtilizationTable, NULL, "HELP STRING");
	snmpd_register_config_handler("mtpOmSlCongestionTable", parse_mtpOmSlCongestionTable, NULL, "HELP STRING");

	/* we need to be called back later to store our data */
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_mtpOmMIB, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_mtpOmHandledMSUsOpcDpcSioTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_mtpOmHandledOctetsOpcDpcSioTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_mtpOmHandledOctetsSIOTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_mtpOmOblSpUtilizationTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_mtpOmAllSpUtilizationTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_mtpOmReceivedOctetsOPCTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_mtpOmReceivedOctetsOpcSioTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_mtpOmSpDataTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_mtpOmTransmittedOctetsDPCTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_mtpOmTransmittedOctetsDPCTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_mtpOmTransmittedOctetsDPCTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_mtpOmTransmittedOctetsDpcSioTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_mtpOmSignRsInformationTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_mtpOmSlsDurationTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_mtpOmAll5And30MinSlDurationTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_mtpOmOblSlDurationTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_mtpOmAll30MinSlDurationTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_mtpOmOblSlUtilizationTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_mtpOmAllSlUtilizationTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_mtpOmSlCongestionTable, NULL);

	/* place any other initialization junk you need here */

	DEBUGMSGTL(("mtpOmMIB", "done.\n"));
}

/*
 * deinit_mtpOmMIB():
 *   Deinitialization routine.  This is called before the agent is unloaded.
 *   At a minimum, deregistration of your variables should take place here.
 */
void
deinit_mtpOmMIB(void)
{
	DEBUGMSGTL(("mtpOmMIB", "deinitializating...  "));

	unregister_mib(mtpOmMIB_variables_oid, sizeof(mtpOmMIB_variables_oid) / sizeof(oid));
	snmpd_unregister_config_handler("mtpOmMIB");
	snmpd_unregister_config_handler("mtpOmHandledMSUsOpcDpcSioTable");
	snmpd_unregister_config_handler("mtpOmHandledOctetsOpcDpcSioTable");
	snmpd_unregister_config_handler("mtpOmHandledOctetsSIOTable");
	snmpd_unregister_config_handler("mtpOmOblSpUtilizationTable");
	snmpd_unregister_config_handler("mtpOmAllSpUtilizationTable");
	snmpd_unregister_config_handler("mtpOmReceivedOctetsOPCTable");
	snmpd_unregister_config_handler("mtpOmReceivedOctetsOpcSioTable");
	snmpd_unregister_config_handler("mtpOmSpDataTable");
	snmpd_unregister_config_handler("mtpOmTransmittedOctetsDPCTable");
	snmpd_unregister_config_handler("mtpOmTransmittedOctetsDPCTable");
	snmpd_unregister_config_handler("mtpOmTransmittedOctetsDPCTable");
	snmpd_unregister_config_handler("mtpOmTransmittedOctetsDpcSioTable");
	snmpd_unregister_config_handler("mtpOmSignRsInformationTable");
	snmpd_unregister_config_handler("mtpOmSlsDurationTable");
	snmpd_unregister_config_handler("mtpOmAll5And30MinSlDurationTable");
	snmpd_unregister_config_handler("mtpOmOblSlDurationTable");
	snmpd_unregister_config_handler("mtpOmAll30MinSlDurationTable");
	snmpd_unregister_config_handler("mtpOmOblSlUtilizationTable");
	snmpd_unregister_config_handler("mtpOmAllSlUtilizationTable");
	snmpd_unregister_config_handler("mtpOmSlCongestionTable");

	/* place any other de-initialization junk you need here */

	DEBUGMSGTL(("mtpOmMIB", "done.\n"));
}

int
term_mtpOmMIB(int majorID, int minorID, void *serverarg, void *clientarg)
{
	deinit_mtpOmMIB();
	return 0;
}

/*
 * mtpOmMIB_add(): adds a structure node for scalars
 */
int
mtpOmMIB_add(struct mtpOmMIB_data *thedata)
{
	DEBUGMSGTL(("mtpOmMIB", "adding data...  "));

	mtpOmMIBStorage = thedata;

	DEBUGMSGTL(("mtpOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/*
 * parse_mtpOmMIB():
 *   parses .conf file entries needed to configure the mib.
 */
void
parse_mtpOmMIB(const char *token, char *line)
{
	size_t tmpint;
	struct mtpOmMIB_data *StorageTmp = SNMP_MALLOC_STRUCT(mtpOmMIB_data);

	DEBUGMSGTL(("mtpOmMIB", "parsing config...  "));

	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}

	mtpOmMIB_add(StorageTmp);

	(void) tmpint;

	DEBUGMSGTL(("mtpOmMIB", "done.\n"));
}

/*
 * store_mtpOmMIB():
 *   stores .conf file entries needed to configure the mib.
 */
int
store_mtpOmMIB(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpint;
	struct mtpOmMIB_data *StorageTmp;

	DEBUGMSGTL(("mtpOmMIB", "storing data...  "));

	refresh_mtpOmMIB();

	if ((StorageTmp = mtpOmMIBStorage) == NULL) {
		DEBUGMSGTL(("mtpOmMIB", "error.\n"));
		return SNMPERR_GENERR;
	}

	(void) tmpint;

	/* XXX: if (StorageTmp->mtpOmMIBStorageType == ST_NONVOLATILE) { */

	memset(line, 0, sizeof(line));
	strcat(line, "mtpOmMIB ");
	cptr = line + strlen(line);

	snmpd_store_config(line);
	/* } */
	DEBUGMSGTL(("mtpOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * refresh_mtpOmMIB: refresh mtpOmMIB scalars
 *
 * Normally, the values retrieved from the operating system are cached.
 * When the agent receives a SIGPOLL from an open STREAMS configuration
 * or administrative driver Stream, the STREAMS subsystem indicates to
 * the agent that the cache has been invalidated and that it should
 * reread scalars and tables from the STREAMS susbystem.  This function
 * is used when the agent starts for the first time, or after a SIGPOLL
 * has been received (and a scalar has been requested).
 */
void
refresh_mtpOmMIB(void)
{
	if (mtpOmMIBStorage == NULL) {
		if ((mtpOmMIBStorage = SNMP_MALLOC_STRUCT(mtpOmMIB_data)) == NULL)
			return;

		/* Update scalar defaults as required here... */

		mtpOmMIB_refresh = 1;
	}

	if (mtpOmMIB_refresh == 0)
		return;
	mtpOmMIB_refresh = 0;

	/* Update scalars as required here... */

}

/*
 * var_mtpOmMIB():
 *   This function is called every time the agent gets a request for
 *   a scalar variable that might be found within your mib section
 *   registered above.  It is up to you to do the right thing and
 *   return the correct value.
 *     You should also correct the value of "var_len" if necessary.
 *
 *   Please see the documentation for more information about writing
 *   module extensions, and check out the examples in the examples
 *   and mibII directories.
 */
u_char *
var_mtpOmMIB(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct mtpOmMIB_data *StorageTmp;

	if (header_generic(vp, name, length, exact, var_len, write_method)
	    == MATCH_FAILED)
		return NULL;

	/* Refresh the MIB values if required. */
	refresh_mtpOmMIB();

	if ((StorageTmp = mtpOmMIBStorage) == NULL)
		return NULL;

	/* This is where we do the value assignments for the mib results. */

	switch (vp->magic) {

	default:
		ERROR_MSG("");
	}
	return NULL;
}

/* 
 * mtpOmHandledMSUsOpcDpcSioTable_add(): adds a structure node to our data set 
 */
int
mtpOmHandledMSUsOpcDpcSioTable_add(struct mtpOmHandledMSUsOpcDpcSioTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("mtpOmHandledMSUsOpcDpcSioTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index
	   the data */

	/* mtpOmOpc */
	snmp_varlist_add_variable(&vars, NULL, 0,, (u_char *) thedata->mtpOmOpc, thedata->mtpOmOpc);
	/* mtpOmDpc */
	snmp_varlist_add_variable(&vars, NULL, 0,, (u_char *) thedata->mtpOmDpc, thedata->mtpOmDpc);
	/* mtpOmSio */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->mtpOmSio, thedata->mtpOmSioLen);

	header_complex_add_data(&mtpOmHandledMSUsOpcDpcSioTableStorage, vars, thedata);
	DEBUGMSGTL(("mtpOmHandledMSUsOpcDpcSioTable", "registered an entry\n"));

	DEBUGMSGTL(("mtpOmHandledMSUsOpcDpcSioTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/*
 * parse_mtpOmHandledMSUsOpcDpcSioTable():
 *   parses .conf file entries needed to configure the mib.
 */
void
parse_mtpOmHandledMSUsOpcDpcSioTable(const char *token, char *line)
{
	size_t tmpint;
	struct mtpOmHandledMSUsOpcDpcSioTable_data *StorageTmp = SNMP_MALLOC_STRUCT(mtpOmHandledMSUsOpcDpcSioTable_data);

	DEBUGMSGTL(("mtpOmHandledMSUsOpcDpcSioTable", "parsing config...  "));

	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}

	line = read_config_read_data(, line, &StorageTmp->mtpOmOpc, &);
	line = read_config_read_data(, line, &StorageTmp->mtpOmDpc, &);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->mtpOmSio, &StorageTmp->mtpOmSioLen);
	if (StorageTmp->mtpOmSio == NULL) {
		config_perror("invalid specification for mtpOmSio");
		return;
	}

	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->mtpOmHandledMSUsOpcDpcSio, &tmpint);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->mtpOmHandledMSUsOpcDpcSioRowStatus, &tmpint);

	mtpOmHandledMSUsOpcDpcSioTable_add(StorageTmp);

	DEBUGMSGTL(("mtpOmHandledMSUsOpcDpcSioTable", "done.\n"));
}

/*
 * store_mtpOmHandledMSUsOpcDpcSioTable():
 *   stores .conf file entries needed to configure the mib.
 */
int
store_mtpOmHandledMSUsOpcDpcSioTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpint;
	struct mtpOmHandledMSUsOpcDpcSioTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("mtpOmHandledMSUsOpcDpcSioTable", "storing data...  "));

	refresh_mtpOmHandledMSUsOpcDpcSioTable();

	for (hcindex = mtpOmHandledMSUsOpcDpcSioTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct mtpOmHandledMSUsOpcDpcSioTable_data *) hcindex->data;

/*   XXX:  if (StorageTmp->mtpOmHandledMSUsOpcDpcSioTableStorageType == ST_NONVOLATILE) { */

		memset(line, 0, sizeof(line));
		strcat(line, "mtpOmHandledMSUsOpcDpcSioTable ");
		cptr = line + strlen(line);

		cptr = read_config_store_data(, cptr, &StorageTmp->mtpOmOpc, &);
		cptr = read_config_store_data(, cptr, &StorageTmp->mtpOmDpc, &);
		cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->mtpOmSio, &StorageTmp->mtpOmSioLen);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->mtpOmHandledMSUsOpcDpcSio, &tmpint);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->mtpOmHandledMSUsOpcDpcSioRowStatus, &tmpint);

		snmpd_store_config(line);
/*   } */
	}
	DEBUGMSGTL(("mtpOmHandledMSUsOpcDpcSioTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/* 
 * mtpOmHandledOctetsOpcDpcSioTable_add(): adds a structure node to our data set 
 */
int
mtpOmHandledOctetsOpcDpcSioTable_add(struct mtpOmHandledOctetsOpcDpcSioTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("mtpOmHandledOctetsOpcDpcSioTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index
	   the data */

	/* mtpOmOpc */
	snmp_varlist_add_variable(&vars, NULL, 0,, (u_char *) thedata->mtpOmOpc, thedata->mtpOmOpc);
	/* mtpOmDpc */
	snmp_varlist_add_variable(&vars, NULL, 0,, (u_char *) thedata->mtpOmDpc, thedata->mtpOmDpc);
	/* mtpOmSio */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->mtpOmSio, thedata->mtpOmSioLen);

	header_complex_add_data(&mtpOmHandledOctetsOpcDpcSioTableStorage, vars, thedata);
	DEBUGMSGTL(("mtpOmHandledOctetsOpcDpcSioTable", "registered an entry\n"));

	DEBUGMSGTL(("mtpOmHandledOctetsOpcDpcSioTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/*
 * parse_mtpOmHandledOctetsOpcDpcSioTable():
 *   parses .conf file entries needed to configure the mib.
 */
void
parse_mtpOmHandledOctetsOpcDpcSioTable(const char *token, char *line)
{
	size_t tmpint;
	struct mtpOmHandledOctetsOpcDpcSioTable_data *StorageTmp = SNMP_MALLOC_STRUCT(mtpOmHandledOctetsOpcDpcSioTable_data);

	DEBUGMSGTL(("mtpOmHandledOctetsOpcDpcSioTable", "parsing config...  "));

	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}

	line = read_config_read_data(, line, &StorageTmp->mtpOmOpc, &);
	line = read_config_read_data(, line, &StorageTmp->mtpOmDpc, &);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->mtpOmSio, &StorageTmp->mtpOmSioLen);
	if (StorageTmp->mtpOmSio == NULL) {
		config_perror("invalid specification for mtpOmSio");
		return;
	}

	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->mtpOmHandledOctetsOpcDpcSioInterval, &tmpint);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->mtpOmHandledOctetsOpcDpcSio, &tmpint);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->mtpOmHandledOctetsOpcDpcSioRowStatus, &tmpint);

	mtpOmHandledOctetsOpcDpcSioTable_add(StorageTmp);

	DEBUGMSGTL(("mtpOmHandledOctetsOpcDpcSioTable", "done.\n"));
}

/*
 * store_mtpOmHandledOctetsOpcDpcSioTable():
 *   stores .conf file entries needed to configure the mib.
 */
int
store_mtpOmHandledOctetsOpcDpcSioTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpint;
	struct mtpOmHandledOctetsOpcDpcSioTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("mtpOmHandledOctetsOpcDpcSioTable", "storing data...  "));

	refresh_mtpOmHandledOctetsOpcDpcSioTable();

	for (hcindex = mtpOmHandledOctetsOpcDpcSioTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct mtpOmHandledOctetsOpcDpcSioTable_data *) hcindex->data;

/*   XXX:  if (StorageTmp->mtpOmHandledOctetsOpcDpcSioTableStorageType == ST_NONVOLATILE) { */

		memset(line, 0, sizeof(line));
		strcat(line, "mtpOmHandledOctetsOpcDpcSioTable ");
		cptr = line + strlen(line);

		cptr = read_config_store_data(, cptr, &StorageTmp->mtpOmOpc, &);
		cptr = read_config_store_data(, cptr, &StorageTmp->mtpOmDpc, &);
		cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->mtpOmSio, &StorageTmp->mtpOmSioLen);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->mtpOmHandledOctetsOpcDpcSioInterval, &tmpint);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->mtpOmHandledOctetsOpcDpcSio, &tmpint);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->mtpOmHandledOctetsOpcDpcSioRowStatus, &tmpint);

		snmpd_store_config(line);
/*   } */
	}
	DEBUGMSGTL(("mtpOmHandledOctetsOpcDpcSioTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/* 
 * mtpOmHandledOctetsSIOTable_add(): adds a structure node to our data set 
 */
int
mtpOmHandledOctetsSIOTable_add(struct mtpOmHandledOctetsSIOTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("mtpOmHandledOctetsSIOTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index
	   the data */

	/* mtpOmSio */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->mtpOmSio, thedata->mtpOmSioLen);

	header_complex_add_data(&mtpOmHandledOctetsSIOTableStorage, vars, thedata);
	DEBUGMSGTL(("mtpOmHandledOctetsSIOTable", "registered an entry\n"));

	DEBUGMSGTL(("mtpOmHandledOctetsSIOTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/*
 * parse_mtpOmHandledOctetsSIOTable():
 *   parses .conf file entries needed to configure the mib.
 */
void
parse_mtpOmHandledOctetsSIOTable(const char *token, char *line)
{
	size_t tmpint;
	struct mtpOmHandledOctetsSIOTable_data *StorageTmp = SNMP_MALLOC_STRUCT(mtpOmHandledOctetsSIOTable_data);

	DEBUGMSGTL(("mtpOmHandledOctetsSIOTable", "parsing config...  "));

	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}

	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->mtpOmSio, &StorageTmp->mtpOmSioLen);
	if (StorageTmp->mtpOmSio == NULL) {
		config_perror("invalid specification for mtpOmSio");
		return;
	}

	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->mtpOmHandledOctetsSIO, &tmpint);

	mtpOmHandledOctetsSIOTable_add(StorageTmp);

	DEBUGMSGTL(("mtpOmHandledOctetsSIOTable", "done.\n"));
}

/*
 * store_mtpOmHandledOctetsSIOTable():
 *   stores .conf file entries needed to configure the mib.
 */
int
store_mtpOmHandledOctetsSIOTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpint;
	struct mtpOmHandledOctetsSIOTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("mtpOmHandledOctetsSIOTable", "storing data...  "));

	refresh_mtpOmHandledOctetsSIOTable();

	for (hcindex = mtpOmHandledOctetsSIOTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct mtpOmHandledOctetsSIOTable_data *) hcindex->data;

/*   XXX:  if (StorageTmp->mtpOmHandledOctetsSIOTableStorageType == ST_NONVOLATILE) { */

		memset(line, 0, sizeof(line));
		strcat(line, "mtpOmHandledOctetsSIOTable ");
		cptr = line + strlen(line);

		cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->mtpOmSio, &StorageTmp->mtpOmSioLen);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->mtpOmHandledOctetsSIO, &tmpint);

		snmpd_store_config(line);
/*   } */
	}
	DEBUGMSGTL(("mtpOmHandledOctetsSIOTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/* 
 * mtpOmOblSpUtilizationTable_add(): adds a structure node to our data set 
 */
int
mtpOmOblSpUtilizationTable_add(struct mtpOmOblSpUtilizationTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("mtpOmOblSpUtilizationTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index
	   the data */

	header_complex_add_data(&mtpOmOblSpUtilizationTableStorage, vars, thedata);
	DEBUGMSGTL(("mtpOmOblSpUtilizationTable", "registered an entry\n"));

	DEBUGMSGTL(("mtpOmOblSpUtilizationTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/*
 * parse_mtpOmOblSpUtilizationTable():
 *   parses .conf file entries needed to configure the mib.
 */
void
parse_mtpOmOblSpUtilizationTable(const char *token, char *line)
{
	size_t tmpint;
	struct mtpOmOblSpUtilizationTable_data *StorageTmp = SNMP_MALLOC_STRUCT(mtpOmOblSpUtilizationTable_data);

	DEBUGMSGTL(("mtpOmOblSpUtilizationTable", "parsing config...  "));

	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}

	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->mtpOmAdjacentInaccessibleDuration, &tmpint);

	mtpOmOblSpUtilizationTable_add(StorageTmp);

	DEBUGMSGTL(("mtpOmOblSpUtilizationTable", "done.\n"));
}

/*
 * store_mtpOmOblSpUtilizationTable():
 *   stores .conf file entries needed to configure the mib.
 */
int
store_mtpOmOblSpUtilizationTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpint;
	struct mtpOmOblSpUtilizationTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("mtpOmOblSpUtilizationTable", "storing data...  "));

	refresh_mtpOmOblSpUtilizationTable();

	for (hcindex = mtpOmOblSpUtilizationTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct mtpOmOblSpUtilizationTable_data *) hcindex->data;

/*   XXX:  if (StorageTmp->mtpOmOblSpUtilizationTableStorageType == ST_NONVOLATILE) { */

		memset(line, 0, sizeof(line));
		strcat(line, "mtpOmOblSpUtilizationTable ");
		cptr = line + strlen(line);

		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->mtpOmAdjacentInaccessibleDuration, &tmpint);

		snmpd_store_config(line);
/*   } */
	}
	DEBUGMSGTL(("mtpOmOblSpUtilizationTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/* 
 * mtpOmAllSpUtilizationTable_add(): adds a structure node to our data set 
 */
int
mtpOmAllSpUtilizationTable_add(struct mtpOmAllSpUtilizationTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("mtpOmAllSpUtilizationTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index
	   the data */

	header_complex_add_data(&mtpOmAllSpUtilizationTableStorage, vars, thedata);
	DEBUGMSGTL(("mtpOmAllSpUtilizationTable", "registered an entry\n"));

	DEBUGMSGTL(("mtpOmAllSpUtilizationTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/*
 * parse_mtpOmAllSpUtilizationTable():
 *   parses .conf file entries needed to configure the mib.
 */
void
parse_mtpOmAllSpUtilizationTable(const char *token, char *line)
{
	size_t tmpint;
	struct mtpOmAllSpUtilizationTable_data *StorageTmp = SNMP_MALLOC_STRUCT(mtpOmAllSpUtilizationTable_data);

	DEBUGMSGTL(("mtpOmAllSpUtilizationTable", "parsing config...  "));

	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}

	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->mtpOmAdjacentInaccessibleEvents, &tmpint);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->mtpOmAllSpUtilizationRowStatus, &tmpint);

	mtpOmAllSpUtilizationTable_add(StorageTmp);

	DEBUGMSGTL(("mtpOmAllSpUtilizationTable", "done.\n"));
}

/*
 * store_mtpOmAllSpUtilizationTable():
 *   stores .conf file entries needed to configure the mib.
 */
int
store_mtpOmAllSpUtilizationTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpint;
	struct mtpOmAllSpUtilizationTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("mtpOmAllSpUtilizationTable", "storing data...  "));

	refresh_mtpOmAllSpUtilizationTable();

	for (hcindex = mtpOmAllSpUtilizationTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct mtpOmAllSpUtilizationTable_data *) hcindex->data;

/*   XXX:  if (StorageTmp->mtpOmAllSpUtilizationTableStorageType == ST_NONVOLATILE) { */

		memset(line, 0, sizeof(line));
		strcat(line, "mtpOmAllSpUtilizationTable ");
		cptr = line + strlen(line);

		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->mtpOmAdjacentInaccessibleEvents, &tmpint);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->mtpOmAllSpUtilizationRowStatus, &tmpint);

		snmpd_store_config(line);
/*   } */
	}
	DEBUGMSGTL(("mtpOmAllSpUtilizationTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/* 
 * mtpOmReceivedOctetsOPCTable_add(): adds a structure node to our data set 
 */
int
mtpOmReceivedOctetsOPCTable_add(struct mtpOmReceivedOctetsOPCTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("mtpOmReceivedOctetsOPCTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index
	   the data */

	/* mtpOmOpc */
	snmp_varlist_add_variable(&vars, NULL, 0,, (u_char *) thedata->mtpOmOpc, thedata->mtpOmOpc);

	header_complex_add_data(&mtpOmReceivedOctetsOPCTableStorage, vars, thedata);
	DEBUGMSGTL(("mtpOmReceivedOctetsOPCTable", "registered an entry\n"));

	DEBUGMSGTL(("mtpOmReceivedOctetsOPCTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/*
 * parse_mtpOmReceivedOctetsOPCTable():
 *   parses .conf file entries needed to configure the mib.
 */
void
parse_mtpOmReceivedOctetsOPCTable(const char *token, char *line)
{
	size_t tmpint;
	struct mtpOmReceivedOctetsOPCTable_data *StorageTmp = SNMP_MALLOC_STRUCT(mtpOmReceivedOctetsOPCTable_data);

	DEBUGMSGTL(("mtpOmReceivedOctetsOPCTable", "parsing config...  "));

	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}

	line = read_config_read_data(, line, &StorageTmp->mtpOmOpc, &);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->mtpOmReceivedOctetsOPCInterval, &tmpint);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->mtpOmReceivedOctetsOPC, &tmpint);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->mtpOmReceivedOctetsOPCRowStatus, &tmpint);

	mtpOmReceivedOctetsOPCTable_add(StorageTmp);

	DEBUGMSGTL(("mtpOmReceivedOctetsOPCTable", "done.\n"));
}

/*
 * store_mtpOmReceivedOctetsOPCTable():
 *   stores .conf file entries needed to configure the mib.
 */
int
store_mtpOmReceivedOctetsOPCTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpint;
	struct mtpOmReceivedOctetsOPCTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("mtpOmReceivedOctetsOPCTable", "storing data...  "));

	refresh_mtpOmReceivedOctetsOPCTable();

	for (hcindex = mtpOmReceivedOctetsOPCTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct mtpOmReceivedOctetsOPCTable_data *) hcindex->data;

/*   XXX:  if (StorageTmp->mtpOmReceivedOctetsOPCTableStorageType == ST_NONVOLATILE) { */

		memset(line, 0, sizeof(line));
		strcat(line, "mtpOmReceivedOctetsOPCTable ");
		cptr = line + strlen(line);

		cptr = read_config_store_data(, cptr, &StorageTmp->mtpOmOpc, &);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->mtpOmReceivedOctetsOPCInterval, &tmpint);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->mtpOmReceivedOctetsOPC, &tmpint);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->mtpOmReceivedOctetsOPCRowStatus, &tmpint);

		snmpd_store_config(line);
/*   } */
	}
	DEBUGMSGTL(("mtpOmReceivedOctetsOPCTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/* 
 * mtpOmReceivedOctetsOpcSioTable_add(): adds a structure node to our data set 
 */
int
mtpOmReceivedOctetsOpcSioTable_add(struct mtpOmReceivedOctetsOpcSioTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("mtpOmReceivedOctetsOpcSioTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index
	   the data */

	/* mtpOmOpc */
	snmp_varlist_add_variable(&vars, NULL, 0,, (u_char *) thedata->mtpOmOpc, thedata->mtpOmOpc);
	/* mtpOmSio */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->mtpOmSio, thedata->mtpOmSioLen);

	header_complex_add_data(&mtpOmReceivedOctetsOpcSioTableStorage, vars, thedata);
	DEBUGMSGTL(("mtpOmReceivedOctetsOpcSioTable", "registered an entry\n"));

	DEBUGMSGTL(("mtpOmReceivedOctetsOpcSioTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/*
 * parse_mtpOmReceivedOctetsOpcSioTable():
 *   parses .conf file entries needed to configure the mib.
 */
void
parse_mtpOmReceivedOctetsOpcSioTable(const char *token, char *line)
{
	size_t tmpint;
	struct mtpOmReceivedOctetsOpcSioTable_data *StorageTmp = SNMP_MALLOC_STRUCT(mtpOmReceivedOctetsOpcSioTable_data);

	DEBUGMSGTL(("mtpOmReceivedOctetsOpcSioTable", "parsing config...  "));

	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}

	line = read_config_read_data(, line, &StorageTmp->mtpOmOpc, &);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->mtpOmSio, &StorageTmp->mtpOmSioLen);
	if (StorageTmp->mtpOmSio == NULL) {
		config_perror("invalid specification for mtpOmSio");
		return;
	}

	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->mtpOmReceivedOctetsOpcSioInterval, &tmpint);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->mtpOmReceivedOctetsOpcSio, &tmpint);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->mtpOmReceivedOctetsOpcSioRowStatus, &tmpint);

	mtpOmReceivedOctetsOpcSioTable_add(StorageTmp);

	DEBUGMSGTL(("mtpOmReceivedOctetsOpcSioTable", "done.\n"));
}

/*
 * store_mtpOmReceivedOctetsOpcSioTable():
 *   stores .conf file entries needed to configure the mib.
 */
int
store_mtpOmReceivedOctetsOpcSioTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpint;
	struct mtpOmReceivedOctetsOpcSioTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("mtpOmReceivedOctetsOpcSioTable", "storing data...  "));

	refresh_mtpOmReceivedOctetsOpcSioTable();

	for (hcindex = mtpOmReceivedOctetsOpcSioTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct mtpOmReceivedOctetsOpcSioTable_data *) hcindex->data;

/*   XXX:  if (StorageTmp->mtpOmReceivedOctetsOpcSioTableStorageType == ST_NONVOLATILE) { */

		memset(line, 0, sizeof(line));
		strcat(line, "mtpOmReceivedOctetsOpcSioTable ");
		cptr = line + strlen(line);

		cptr = read_config_store_data(, cptr, &StorageTmp->mtpOmOpc, &);
		cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->mtpOmSio, &StorageTmp->mtpOmSioLen);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->mtpOmReceivedOctetsOpcSioInterval, &tmpint);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->mtpOmReceivedOctetsOpcSio, &tmpint);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->mtpOmReceivedOctetsOpcSioRowStatus, &tmpint);

		snmpd_store_config(line);
/*   } */
	}
	DEBUGMSGTL(("mtpOmReceivedOctetsOpcSioTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/* 
 * mtpOmSpDataTable_add(): adds a structure node to our data set 
 */
int
mtpOmSpDataTable_add(struct mtpOmSpDataTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("mtpOmSpDataTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index
	   the data */

	/* mtpOmUserPart */
	snmp_varlist_add_variable(&vars, NULL, 0,, (u_char *) thedata->mtpOmUserPart, thedata->mtpOmUserPart);
	/* mtpOmCongestionLevel */
	snmp_varlist_add_variable(&vars, NULL, 0,, (u_char *) thedata->mtpOmCongestionLevel, thedata->mtpOmCongestionLevel);

	header_complex_add_data(&mtpOmSpDataTableStorage, vars, thedata);
	DEBUGMSGTL(("mtpOmSpDataTable", "registered an entry\n"));

	DEBUGMSGTL(("mtpOmSpDataTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/*
 * parse_mtpOmSpDataTable():
 *   parses .conf file entries needed to configure the mib.
 */
void
parse_mtpOmSpDataTable(const char *token, char *line)
{
	size_t tmpint;
	struct mtpOmSpDataTable_data *StorageTmp = SNMP_MALLOC_STRUCT(mtpOmSpDataTable_data);

	DEBUGMSGTL(("mtpOmSpDataTable", "parsing config...  "));

	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}

	line = read_config_read_data(, line, &StorageTmp->mtpOmUserPart, &);
	line = read_config_read_data(, line, &StorageTmp->mtpOmCongestionLevel, &);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->mtpOmSpDiscardedMSUs, &tmpint);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->mtpOmSpTransmittedUPUnavailable, &tmpint);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->mtpOmSpReceivedUPUnavailable, &tmpint);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->mtpOmSpReceivedTFC, &tmpint);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->mtpOmSpReceivedTFCLevel0, &tmpint);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->mtpOmSpReceivedTFCLevel1, &tmpint);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->mtpOmSpReceivedTFCLevel2, &tmpint);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->mtpOmSpReceivedTFCLevel3, &tmpint);

	mtpOmSpDataTable_add(StorageTmp);

	DEBUGMSGTL(("mtpOmSpDataTable", "done.\n"));
}

/*
 * store_mtpOmSpDataTable():
 *   stores .conf file entries needed to configure the mib.
 */
int
store_mtpOmSpDataTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpint;
	struct mtpOmSpDataTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("mtpOmSpDataTable", "storing data...  "));

	refresh_mtpOmSpDataTable();

	for (hcindex = mtpOmSpDataTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct mtpOmSpDataTable_data *) hcindex->data;

/*   XXX:  if (StorageTmp->mtpOmSpDataTableStorageType == ST_NONVOLATILE) { */

		memset(line, 0, sizeof(line));
		strcat(line, "mtpOmSpDataTable ");
		cptr = line + strlen(line);

		cptr = read_config_store_data(, cptr, &StorageTmp->mtpOmUserPart, &);
		cptr = read_config_store_data(, cptr, &StorageTmp->mtpOmCongestionLevel, &);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->mtpOmSpDiscardedMSUs, &tmpint);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->mtpOmSpTransmittedUPUnavailable, &tmpint);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->mtpOmSpReceivedUPUnavailable, &tmpint);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->mtpOmSpReceivedTFC, &tmpint);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->mtpOmSpReceivedTFCLevel0, &tmpint);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->mtpOmSpReceivedTFCLevel1, &tmpint);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->mtpOmSpReceivedTFCLevel2, &tmpint);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->mtpOmSpReceivedTFCLevel3, &tmpint);

		snmpd_store_config(line);
/*   } */
	}
	DEBUGMSGTL(("mtpOmSpDataTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/* 
 * mtpOmTransmittedOctetsDPCTable_add(): adds a structure node to our data set 
 */
int
mtpOmTransmittedOctetsDPCTable_add(struct mtpOmTransmittedOctetsDPCTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("mtpOmTransmittedOctetsDPCTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index
	   the data */

	/* mtpOmDpc */
	snmp_varlist_add_variable(&vars, NULL, 0,, (u_char *) thedata->mtpOmDpc, thedata->mtpOmDpc);
	/* mtpOmDpc */
	snmp_varlist_add_variable(&vars, NULL, 0,, (u_char *) thedata->mtpOmDpc, thedata->mtpOmDpc);

	header_complex_add_data(&mtpOmTransmittedOctetsDPCTableStorage, vars, thedata);
	DEBUGMSGTL(("mtpOmTransmittedOctetsDPCTable", "registered an entry\n"));

	DEBUGMSGTL(("mtpOmTransmittedOctetsDPCTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/*
 * parse_mtpOmTransmittedOctetsDPCTable():
 *   parses .conf file entries needed to configure the mib.
 */
void
parse_mtpOmTransmittedOctetsDPCTable(const char *token, char *line)
{
	size_t tmpint;
	struct mtpOmTransmittedOctetsDPCTable_data *StorageTmp = SNMP_MALLOC_STRUCT(mtpOmTransmittedOctetsDPCTable_data);

	DEBUGMSGTL(("mtpOmTransmittedOctetsDPCTable", "parsing config...  "));

	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}

	line = read_config_read_data(, line, &StorageTmp->mtpOmDpc, &);
	line = read_config_read_data(, line, &StorageTmp->mtpOmDpc, &);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->mtpOmTransmittedOctetsDPCRowStatus, &tmpint);

	mtpOmTransmittedOctetsDPCTable_add(StorageTmp);

	DEBUGMSGTL(("mtpOmTransmittedOctetsDPCTable", "done.\n"));
}

/*
 * store_mtpOmTransmittedOctetsDPCTable():
 *   stores .conf file entries needed to configure the mib.
 */
int
store_mtpOmTransmittedOctetsDPCTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpint;
	struct mtpOmTransmittedOctetsDPCTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("mtpOmTransmittedOctetsDPCTable", "storing data...  "));

	refresh_mtpOmTransmittedOctetsDPCTable();

	for (hcindex = mtpOmTransmittedOctetsDPCTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct mtpOmTransmittedOctetsDPCTable_data *) hcindex->data;

/*   XXX:  if (StorageTmp->mtpOmTransmittedOctetsDPCTableStorageType == ST_NONVOLATILE) { */

		memset(line, 0, sizeof(line));
		strcat(line, "mtpOmTransmittedOctetsDPCTable ");
		cptr = line + strlen(line);

		cptr = read_config_store_data(, cptr, &StorageTmp->mtpOmDpc, &);
		cptr = read_config_store_data(, cptr, &StorageTmp->mtpOmDpc, &);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->mtpOmTransmittedOctetsDPCRowStatus, &tmpint);

		snmpd_store_config(line);
/*   } */
	}
	DEBUGMSGTL(("mtpOmTransmittedOctetsDPCTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/* 
 * mtpOmTransmittedOctetsDPCTable_add(): adds a structure node to our data set 
 */
int
mtpOmTransmittedOctetsDPCTable_add(struct mtpOmTransmittedOctetsDPCTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("mtpOmTransmittedOctetsDPCTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index
	   the data */

	/* mtpOmDpc */
	snmp_varlist_add_variable(&vars, NULL, 0,, (u_char *) thedata->mtpOmDpc, thedata->mtpOmDpc);
	/* mtpOmDpc */
	snmp_varlist_add_variable(&vars, NULL, 0,, (u_char *) thedata->mtpOmDpc, thedata->mtpOmDpc);

	header_complex_add_data(&mtpOmTransmittedOctetsDPCTableStorage, vars, thedata);
	DEBUGMSGTL(("mtpOmTransmittedOctetsDPCTable", "registered an entry\n"));

	DEBUGMSGTL(("mtpOmTransmittedOctetsDPCTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/*
 * parse_mtpOmTransmittedOctetsDPCTable():
 *   parses .conf file entries needed to configure the mib.
 */
void
parse_mtpOmTransmittedOctetsDPCTable(const char *token, char *line)
{
	size_t tmpint;
	struct mtpOmTransmittedOctetsDPCTable_data *StorageTmp = SNMP_MALLOC_STRUCT(mtpOmTransmittedOctetsDPCTable_data);

	DEBUGMSGTL(("mtpOmTransmittedOctetsDPCTable", "parsing config...  "));

	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}

	line = read_config_read_data(, line, &StorageTmp->mtpOmDpc, &);
	line = read_config_read_data(, line, &StorageTmp->mtpOmDpc, &);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->mtpOmTransmittedOctetsDPCRowStatus, &tmpint);

	mtpOmTransmittedOctetsDPCTable_add(StorageTmp);

	DEBUGMSGTL(("mtpOmTransmittedOctetsDPCTable", "done.\n"));
}

/*
 * store_mtpOmTransmittedOctetsDPCTable():
 *   stores .conf file entries needed to configure the mib.
 */
int
store_mtpOmTransmittedOctetsDPCTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpint;
	struct mtpOmTransmittedOctetsDPCTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("mtpOmTransmittedOctetsDPCTable", "storing data...  "));

	refresh_mtpOmTransmittedOctetsDPCTable();

	for (hcindex = mtpOmTransmittedOctetsDPCTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct mtpOmTransmittedOctetsDPCTable_data *) hcindex->data;

/*   XXX:  if (StorageTmp->mtpOmTransmittedOctetsDPCTableStorageType == ST_NONVOLATILE) { */

		memset(line, 0, sizeof(line));
		strcat(line, "mtpOmTransmittedOctetsDPCTable ");
		cptr = line + strlen(line);

		cptr = read_config_store_data(, cptr, &StorageTmp->mtpOmDpc, &);
		cptr = read_config_store_data(, cptr, &StorageTmp->mtpOmDpc, &);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->mtpOmTransmittedOctetsDPCRowStatus, &tmpint);

		snmpd_store_config(line);
/*   } */
	}
	DEBUGMSGTL(("mtpOmTransmittedOctetsDPCTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/* 
 * mtpOmTransmittedOctetsDPCTable_add(): adds a structure node to our data set 
 */
int
mtpOmTransmittedOctetsDPCTable_add(struct mtpOmTransmittedOctetsDPCTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("mtpOmTransmittedOctetsDPCTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index
	   the data */

	/* mtpOmDpc */
	snmp_varlist_add_variable(&vars, NULL, 0,, (u_char *) thedata->mtpOmDpc, thedata->mtpOmDpc);
	/* mtpOmDpc */
	snmp_varlist_add_variable(&vars, NULL, 0,, (u_char *) thedata->mtpOmDpc, thedata->mtpOmDpc);

	header_complex_add_data(&mtpOmTransmittedOctetsDPCTableStorage, vars, thedata);
	DEBUGMSGTL(("mtpOmTransmittedOctetsDPCTable", "registered an entry\n"));

	DEBUGMSGTL(("mtpOmTransmittedOctetsDPCTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/*
 * parse_mtpOmTransmittedOctetsDPCTable():
 *   parses .conf file entries needed to configure the mib.
 */
void
parse_mtpOmTransmittedOctetsDPCTable(const char *token, char *line)
{
	size_t tmpint;
	struct mtpOmTransmittedOctetsDPCTable_data *StorageTmp = SNMP_MALLOC_STRUCT(mtpOmTransmittedOctetsDPCTable_data);

	DEBUGMSGTL(("mtpOmTransmittedOctetsDPCTable", "parsing config...  "));

	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}

	line = read_config_read_data(, line, &StorageTmp->mtpOmDpc, &);
	line = read_config_read_data(, line, &StorageTmp->mtpOmDpc, &);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->mtpOmTransmittedOctetsDPCRowStatus, &tmpint);

	mtpOmTransmittedOctetsDPCTable_add(StorageTmp);

	DEBUGMSGTL(("mtpOmTransmittedOctetsDPCTable", "done.\n"));
}

/*
 * store_mtpOmTransmittedOctetsDPCTable():
 *   stores .conf file entries needed to configure the mib.
 */
int
store_mtpOmTransmittedOctetsDPCTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpint;
	struct mtpOmTransmittedOctetsDPCTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("mtpOmTransmittedOctetsDPCTable", "storing data...  "));

	refresh_mtpOmTransmittedOctetsDPCTable();

	for (hcindex = mtpOmTransmittedOctetsDPCTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct mtpOmTransmittedOctetsDPCTable_data *) hcindex->data;

/*   XXX:  if (StorageTmp->mtpOmTransmittedOctetsDPCTableStorageType == ST_NONVOLATILE) { */

		memset(line, 0, sizeof(line));
		strcat(line, "mtpOmTransmittedOctetsDPCTable ");
		cptr = line + strlen(line);

		cptr = read_config_store_data(, cptr, &StorageTmp->mtpOmDpc, &);
		cptr = read_config_store_data(, cptr, &StorageTmp->mtpOmDpc, &);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->mtpOmTransmittedOctetsDPCRowStatus, &tmpint);

		snmpd_store_config(line);
/*   } */
	}
	DEBUGMSGTL(("mtpOmTransmittedOctetsDPCTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/* 
 * mtpOmTransmittedOctetsDpcSioTable_add(): adds a structure node to our data set 
 */
int
mtpOmTransmittedOctetsDpcSioTable_add(struct mtpOmTransmittedOctetsDpcSioTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("mtpOmTransmittedOctetsDpcSioTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index
	   the data */

	/* mtpOmDpc */
	snmp_varlist_add_variable(&vars, NULL, 0,, (u_char *) thedata->mtpOmDpc, thedata->mtpOmDpc);
	/* mtpOmSio */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->mtpOmSio, thedata->mtpOmSioLen);

	header_complex_add_data(&mtpOmTransmittedOctetsDpcSioTableStorage, vars, thedata);
	DEBUGMSGTL(("mtpOmTransmittedOctetsDpcSioTable", "registered an entry\n"));

	DEBUGMSGTL(("mtpOmTransmittedOctetsDpcSioTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/*
 * parse_mtpOmTransmittedOctetsDpcSioTable():
 *   parses .conf file entries needed to configure the mib.
 */
void
parse_mtpOmTransmittedOctetsDpcSioTable(const char *token, char *line)
{
	size_t tmpint;
	struct mtpOmTransmittedOctetsDpcSioTable_data *StorageTmp = SNMP_MALLOC_STRUCT(mtpOmTransmittedOctetsDpcSioTable_data);

	DEBUGMSGTL(("mtpOmTransmittedOctetsDpcSioTable", "parsing config...  "));

	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}

	line = read_config_read_data(, line, &StorageTmp->mtpOmDpc, &);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->mtpOmSio, &StorageTmp->mtpOmSioLen);
	if (StorageTmp->mtpOmSio == NULL) {
		config_perror("invalid specification for mtpOmSio");
		return;
	}

	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->mtpOmTransmittedOctetsDpcSio, &tmpint);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->mtpOmTransmittedOctetsDpcSioRowStatus, &tmpint);

	mtpOmTransmittedOctetsDpcSioTable_add(StorageTmp);

	DEBUGMSGTL(("mtpOmTransmittedOctetsDpcSioTable", "done.\n"));
}

/*
 * store_mtpOmTransmittedOctetsDpcSioTable():
 *   stores .conf file entries needed to configure the mib.
 */
int
store_mtpOmTransmittedOctetsDpcSioTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpint;
	struct mtpOmTransmittedOctetsDpcSioTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("mtpOmTransmittedOctetsDpcSioTable", "storing data...  "));

	refresh_mtpOmTransmittedOctetsDpcSioTable();

	for (hcindex = mtpOmTransmittedOctetsDpcSioTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct mtpOmTransmittedOctetsDpcSioTable_data *) hcindex->data;

/*   XXX:  if (StorageTmp->mtpOmTransmittedOctetsDpcSioTableStorageType == ST_NONVOLATILE) { */

		memset(line, 0, sizeof(line));
		strcat(line, "mtpOmTransmittedOctetsDpcSioTable ");
		cptr = line + strlen(line);

		cptr = read_config_store_data(, cptr, &StorageTmp->mtpOmDpc, &);
		cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->mtpOmSio, &StorageTmp->mtpOmSioLen);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->mtpOmTransmittedOctetsDpcSio, &tmpint);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->mtpOmTransmittedOctetsDpcSioRowStatus, &tmpint);

		snmpd_store_config(line);
/*   } */
	}
	DEBUGMSGTL(("mtpOmTransmittedOctetsDpcSioTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/* 
 * mtpOmSignRsInformationTable_add(): adds a structure node to our data set 
 */
int
mtpOmSignRsInformationTable_add(struct mtpOmSignRsInformationTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("mtpOmSignRsInformationTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index
	   the data */

	header_complex_add_data(&mtpOmSignRsInformationTableStorage, vars, thedata);
	DEBUGMSGTL(("mtpOmSignRsInformationTable", "registered an entry\n"));

	DEBUGMSGTL(("mtpOmSignRsInformationTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/*
 * parse_mtpOmSignRsInformationTable():
 *   parses .conf file entries needed to configure the mib.
 */
void
parse_mtpOmSignRsInformationTable(const char *token, char *line)
{
	size_t tmpint;
	struct mtpOmSignRsInformationTable_data *StorageTmp = SNMP_MALLOC_STRUCT(mtpOmSignRsInformationTable_data);

	DEBUGMSGTL(("mtpOmSignRsInformationTable", "parsing config...  "));

	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}

	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->mtpOmRouteSetUnavailable, &tmpint);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->mtpOmRouteSetUnavailableDuration, &tmpint);

	mtpOmSignRsInformationTable_add(StorageTmp);

	DEBUGMSGTL(("mtpOmSignRsInformationTable", "done.\n"));
}

/*
 * store_mtpOmSignRsInformationTable():
 *   stores .conf file entries needed to configure the mib.
 */
int
store_mtpOmSignRsInformationTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpint;
	struct mtpOmSignRsInformationTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("mtpOmSignRsInformationTable", "storing data...  "));

	refresh_mtpOmSignRsInformationTable();

	for (hcindex = mtpOmSignRsInformationTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct mtpOmSignRsInformationTable_data *) hcindex->data;

/*   XXX:  if (StorageTmp->mtpOmSignRsInformationTableStorageType == ST_NONVOLATILE) { */

		memset(line, 0, sizeof(line));
		strcat(line, "mtpOmSignRsInformationTable ");
		cptr = line + strlen(line);

		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->mtpOmRouteSetUnavailable, &tmpint);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->mtpOmRouteSetUnavailableDuration, &tmpint);

		snmpd_store_config(line);
/*   } */
	}
	DEBUGMSGTL(("mtpOmSignRsInformationTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/* 
 * mtpOmSlsDurationTable_add(): adds a structure node to our data set 
 */
int
mtpOmSlsDurationTable_add(struct mtpOmSlsDurationTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("mtpOmSlsDurationTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index
	   the data */

	header_complex_add_data(&mtpOmSlsDurationTableStorage, vars, thedata);
	DEBUGMSGTL(("mtpOmSlsDurationTable", "registered an entry\n"));

	DEBUGMSGTL(("mtpOmSlsDurationTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/*
 * parse_mtpOmSlsDurationTable():
 *   parses .conf file entries needed to configure the mib.
 */
void
parse_mtpOmSlsDurationTable(const char *token, char *line)
{
	size_t tmpint;
	struct mtpOmSlsDurationTable_data *StorageTmp = SNMP_MALLOC_STRUCT(mtpOmSlsDurationTable_data);

	DEBUGMSGTL(("mtpOmSlsDurationTable", "parsing config...  "));

	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}

	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->mtpOmSlsUnavailable, &tmpint);

	mtpOmSlsDurationTable_add(StorageTmp);

	DEBUGMSGTL(("mtpOmSlsDurationTable", "done.\n"));
}

/*
 * store_mtpOmSlsDurationTable():
 *   stores .conf file entries needed to configure the mib.
 */
int
store_mtpOmSlsDurationTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpint;
	struct mtpOmSlsDurationTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("mtpOmSlsDurationTable", "storing data...  "));

	refresh_mtpOmSlsDurationTable();

	for (hcindex = mtpOmSlsDurationTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct mtpOmSlsDurationTable_data *) hcindex->data;

/*   XXX:  if (StorageTmp->mtpOmSlsDurationTableStorageType == ST_NONVOLATILE) { */

		memset(line, 0, sizeof(line));
		strcat(line, "mtpOmSlsDurationTable ");
		cptr = line + strlen(line);

		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->mtpOmSlsUnavailable, &tmpint);

		snmpd_store_config(line);
/*   } */
	}
	DEBUGMSGTL(("mtpOmSlsDurationTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/* 
 * mtpOmAll5And30MinSlDurationTable_add(): adds a structure node to our data set 
 */
int
mtpOmAll5And30MinSlDurationTable_add(struct mtpOmAll5And30MinSlDurationTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("mtpOmAll5And30MinSlDurationTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index
	   the data */

	header_complex_add_data(&mtpOmAll5And30MinSlDurationTableStorage, vars, thedata);
	DEBUGMSGTL(("mtpOmAll5And30MinSlDurationTable", "registered an entry\n"));

	DEBUGMSGTL(("mtpOmAll5And30MinSlDurationTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/*
 * parse_mtpOmAll5And30MinSlDurationTable():
 *   parses .conf file entries needed to configure the mib.
 */
void
parse_mtpOmAll5And30MinSlDurationTable(const char *token, char *line)
{
	size_t tmpint;
	struct mtpOmAll5And30MinSlDurationTable_data *StorageTmp = SNMP_MALLOC_STRUCT(mtpOmAll5And30MinSlDurationTable_data);

	DEBUGMSGTL(("mtpOmAll5And30MinSlDurationTable", "parsing config...  "));

	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}

	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->mtpOmAll5And30MinSlDurationInterval, &tmpint);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->mtpOmLocalMgntInhibit, &tmpint);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->mtpOmLocalMgntUninhibit, &tmpint);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->mtpOmLocalBusy, &tmpint);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->mtpOmAll5And30MinSlDurationRowStatus, &tmpint);

	mtpOmAll5And30MinSlDurationTable_add(StorageTmp);

	DEBUGMSGTL(("mtpOmAll5And30MinSlDurationTable", "done.\n"));
}

/*
 * store_mtpOmAll5And30MinSlDurationTable():
 *   stores .conf file entries needed to configure the mib.
 */
int
store_mtpOmAll5And30MinSlDurationTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpint;
	struct mtpOmAll5And30MinSlDurationTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("mtpOmAll5And30MinSlDurationTable", "storing data...  "));

	refresh_mtpOmAll5And30MinSlDurationTable();

	for (hcindex = mtpOmAll5And30MinSlDurationTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct mtpOmAll5And30MinSlDurationTable_data *) hcindex->data;

/*   XXX:  if (StorageTmp->mtpOmAll5And30MinSlDurationTableStorageType == ST_NONVOLATILE) { */

		memset(line, 0, sizeof(line));
		strcat(line, "mtpOmAll5And30MinSlDurationTable ");
		cptr = line + strlen(line);

		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->mtpOmAll5And30MinSlDurationInterval, &tmpint);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->mtpOmLocalMgntInhibit, &tmpint);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->mtpOmLocalMgntUninhibit, &tmpint);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->mtpOmLocalBusy, &tmpint);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->mtpOmAll5And30MinSlDurationRowStatus, &tmpint);

		snmpd_store_config(line);
/*   } */
	}
	DEBUGMSGTL(("mtpOmAll5And30MinSlDurationTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/* 
 * mtpOmOblSlDurationTable_add(): adds a structure node to our data set 
 */
int
mtpOmOblSlDurationTable_add(struct mtpOmOblSlDurationTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("mtpOmOblSlDurationTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index
	   the data */

	header_complex_add_data(&mtpOmOblSlDurationTableStorage, vars, thedata);
	DEBUGMSGTL(("mtpOmOblSlDurationTable", "registered an entry\n"));

	DEBUGMSGTL(("mtpOmOblSlDurationTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/*
 * parse_mtpOmOblSlDurationTable():
 *   parses .conf file entries needed to configure the mib.
 */
void
parse_mtpOmOblSlDurationTable(const char *token, char *line)
{
	size_t tmpint;
	struct mtpOmOblSlDurationTable_data *StorageTmp = SNMP_MALLOC_STRUCT(mtpOmOblSlDurationTable_data);

	DEBUGMSGTL(("mtpOmOblSlDurationTable", "parsing config...  "));

	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}

	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->mtpOmSlUnavailabilityDuration, &tmpint);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->mtpOmSlInServiceDuration, &tmpint);

	mtpOmOblSlDurationTable_add(StorageTmp);

	DEBUGMSGTL(("mtpOmOblSlDurationTable", "done.\n"));
}

/*
 * store_mtpOmOblSlDurationTable():
 *   stores .conf file entries needed to configure the mib.
 */
int
store_mtpOmOblSlDurationTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpint;
	struct mtpOmOblSlDurationTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("mtpOmOblSlDurationTable", "storing data...  "));

	refresh_mtpOmOblSlDurationTable();

	for (hcindex = mtpOmOblSlDurationTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct mtpOmOblSlDurationTable_data *) hcindex->data;

/*   XXX:  if (StorageTmp->mtpOmOblSlDurationTableStorageType == ST_NONVOLATILE) { */

		memset(line, 0, sizeof(line));
		strcat(line, "mtpOmOblSlDurationTable ");
		cptr = line + strlen(line);

		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->mtpOmSlUnavailabilityDuration, &tmpint);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->mtpOmSlInServiceDuration, &tmpint);

		snmpd_store_config(line);
/*   } */
	}
	DEBUGMSGTL(("mtpOmOblSlDurationTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/* 
 * mtpOmAll30MinSlDurationTable_add(): adds a structure node to our data set 
 */
int
mtpOmAll30MinSlDurationTable_add(struct mtpOmAll30MinSlDurationTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("mtpOmAll30MinSlDurationTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index
	   the data */

	header_complex_add_data(&mtpOmAll30MinSlDurationTableStorage, vars, thedata);
	DEBUGMSGTL(("mtpOmAll30MinSlDurationTable", "registered an entry\n"));

	DEBUGMSGTL(("mtpOmAll30MinSlDurationTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/*
 * parse_mtpOmAll30MinSlDurationTable():
 *   parses .conf file entries needed to configure the mib.
 */
void
parse_mtpOmAll30MinSlDurationTable(const char *token, char *line)
{
	size_t tmpint;
	struct mtpOmAll30MinSlDurationTable_data *StorageTmp = SNMP_MALLOC_STRUCT(mtpOmAll30MinSlDurationTable_data);

	DEBUGMSGTL(("mtpOmAll30MinSlDurationTable", "parsing config...  "));

	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}

	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->mtpOmAll30MinSlDurationInterval, &tmpint);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->mtpOmSlLocalInhibition, &tmpint);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->mtpOmSlRemoteInhibition, &tmpint);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->mtpOmSlFailed, &tmpint);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->mtpOmSlRemoteProcOutage, &tmpint);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->mtpOmAll30MinSlDurationRowStatus, &tmpint);

	mtpOmAll30MinSlDurationTable_add(StorageTmp);

	DEBUGMSGTL(("mtpOmAll30MinSlDurationTable", "done.\n"));
}

/*
 * store_mtpOmAll30MinSlDurationTable():
 *   stores .conf file entries needed to configure the mib.
 */
int
store_mtpOmAll30MinSlDurationTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpint;
	struct mtpOmAll30MinSlDurationTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("mtpOmAll30MinSlDurationTable", "storing data...  "));

	refresh_mtpOmAll30MinSlDurationTable();

	for (hcindex = mtpOmAll30MinSlDurationTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct mtpOmAll30MinSlDurationTable_data *) hcindex->data;

/*   XXX:  if (StorageTmp->mtpOmAll30MinSlDurationTableStorageType == ST_NONVOLATILE) { */

		memset(line, 0, sizeof(line));
		strcat(line, "mtpOmAll30MinSlDurationTable ");
		cptr = line + strlen(line);

		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->mtpOmAll30MinSlDurationInterval, &tmpint);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->mtpOmSlLocalInhibition, &tmpint);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->mtpOmSlRemoteInhibition, &tmpint);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->mtpOmSlFailed, &tmpint);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->mtpOmSlRemoteProcOutage, &tmpint);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->mtpOmAll30MinSlDurationRowStatus, &tmpint);

		snmpd_store_config(line);
/*   } */
	}
	DEBUGMSGTL(("mtpOmAll30MinSlDurationTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/* 
 * mtpOmOblSlUtilizationTable_add(): adds a structure node to our data set 
 */
int
mtpOmOblSlUtilizationTable_add(struct mtpOmOblSlUtilizationTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("mtpOmOblSlUtilizationTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index
	   the data */

	header_complex_add_data(&mtpOmOblSlUtilizationTableStorage, vars, thedata);
	DEBUGMSGTL(("mtpOmOblSlUtilizationTable", "registered an entry\n"));

	DEBUGMSGTL(("mtpOmOblSlUtilizationTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/*
 * parse_mtpOmOblSlUtilizationTable():
 *   parses .conf file entries needed to configure the mib.
 */
void
parse_mtpOmOblSlUtilizationTable(const char *token, char *line)
{
	size_t tmpint;
	struct mtpOmOblSlUtilizationTable_data *StorageTmp = SNMP_MALLOC_STRUCT(mtpOmOblSlUtilizationTable_data);

	DEBUGMSGTL(("mtpOmOblSlUtilizationTable", "parsing config...  "));

	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}

	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->mtpOmSlSignUnitsReceived, &tmpint);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->mtpOmSlTransmittedOctetsSIFSIO, &tmpint);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->mtpOmSlReceivedOctetsSIFSIO, &tmpint);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->mtpOmSlDiscardedMSUs, &tmpint);

	mtpOmOblSlUtilizationTable_add(StorageTmp);

	DEBUGMSGTL(("mtpOmOblSlUtilizationTable", "done.\n"));
}

/*
 * store_mtpOmOblSlUtilizationTable():
 *   stores .conf file entries needed to configure the mib.
 */
int
store_mtpOmOblSlUtilizationTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpint;
	struct mtpOmOblSlUtilizationTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("mtpOmOblSlUtilizationTable", "storing data...  "));

	refresh_mtpOmOblSlUtilizationTable();

	for (hcindex = mtpOmOblSlUtilizationTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct mtpOmOblSlUtilizationTable_data *) hcindex->data;

/*   XXX:  if (StorageTmp->mtpOmOblSlUtilizationTableStorageType == ST_NONVOLATILE) { */

		memset(line, 0, sizeof(line));
		strcat(line, "mtpOmOblSlUtilizationTable ");
		cptr = line + strlen(line);

		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->mtpOmSlSignUnitsReceived, &tmpint);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->mtpOmSlTransmittedOctetsSIFSIO, &tmpint);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->mtpOmSlReceivedOctetsSIFSIO, &tmpint);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->mtpOmSlDiscardedMSUs, &tmpint);

		snmpd_store_config(line);
/*   } */
	}
	DEBUGMSGTL(("mtpOmOblSlUtilizationTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/* 
 * mtpOmAllSlUtilizationTable_add(): adds a structure node to our data set 
 */
int
mtpOmAllSlUtilizationTable_add(struct mtpOmAllSlUtilizationTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("mtpOmAllSlUtilizationTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index
	   the data */

	header_complex_add_data(&mtpOmAllSlUtilizationTableStorage, vars, thedata);
	DEBUGMSGTL(("mtpOmAllSlUtilizationTable", "registered an entry\n"));

	DEBUGMSGTL(("mtpOmAllSlUtilizationTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/*
 * parse_mtpOmAllSlUtilizationTable():
 *   parses .conf file entries needed to configure the mib.
 */
void
parse_mtpOmAllSlUtilizationTable(const char *token, char *line)
{
	size_t tmpint;
	struct mtpOmAllSlUtilizationTable_data *StorageTmp = SNMP_MALLOC_STRUCT(mtpOmAllSlUtilizationTable_data);

	DEBUGMSGTL(("mtpOmAllSlUtilizationTable", "parsing config...  "));

	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}

	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->mtpOmSlAlignment, &tmpint);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->mtpOmSlNegAckReceived, &tmpint);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->mtpOmSlRetransmittedOctets, &tmpint);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->mtpOmSlTransmittedMSUs, &tmpint);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->mtpOmSlReceivedMSUs, &tmpint);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->mtpOmAllSlUtilizationRowStatus, &tmpint);

	mtpOmAllSlUtilizationTable_add(StorageTmp);

	DEBUGMSGTL(("mtpOmAllSlUtilizationTable", "done.\n"));
}

/*
 * store_mtpOmAllSlUtilizationTable():
 *   stores .conf file entries needed to configure the mib.
 */
int
store_mtpOmAllSlUtilizationTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpint;
	struct mtpOmAllSlUtilizationTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("mtpOmAllSlUtilizationTable", "storing data...  "));

	refresh_mtpOmAllSlUtilizationTable();

	for (hcindex = mtpOmAllSlUtilizationTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct mtpOmAllSlUtilizationTable_data *) hcindex->data;

/*   XXX:  if (StorageTmp->mtpOmAllSlUtilizationTableStorageType == ST_NONVOLATILE) { */

		memset(line, 0, sizeof(line));
		strcat(line, "mtpOmAllSlUtilizationTable ");
		cptr = line + strlen(line);

		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->mtpOmSlAlignment, &tmpint);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->mtpOmSlNegAckReceived, &tmpint);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->mtpOmSlRetransmittedOctets, &tmpint);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->mtpOmSlTransmittedMSUs, &tmpint);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->mtpOmSlReceivedMSUs, &tmpint);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->mtpOmAllSlUtilizationRowStatus, &tmpint);

		snmpd_store_config(line);
/*   } */
	}
	DEBUGMSGTL(("mtpOmAllSlUtilizationTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/* 
 * mtpOmSlCongestionTable_add(): adds a structure node to our data set 
 */
int
mtpOmSlCongestionTable_add(struct mtpOmSlCongestionTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("mtpOmSlCongestionTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index
	   the data */

	/* mtpOmCongestionLevel */
	snmp_varlist_add_variable(&vars, NULL, 0,, (u_char *) thedata->mtpOmCongestionLevel, thedata->mtpOmCongestionLevel);

	header_complex_add_data(&mtpOmSlCongestionTableStorage, vars, thedata);
	DEBUGMSGTL(("mtpOmSlCongestionTable", "registered an entry\n"));

	DEBUGMSGTL(("mtpOmSlCongestionTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/*
 * parse_mtpOmSlCongestionTable():
 *   parses .conf file entries needed to configure the mib.
 */
void
parse_mtpOmSlCongestionTable(const char *token, char *line)
{
	size_t tmpint;
	struct mtpOmSlCongestionTable_data *StorageTmp = SNMP_MALLOC_STRUCT(mtpOmSlCongestionTable_data);

	DEBUGMSGTL(("mtpOmSlCongestionTable", "parsing config...  "));

	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}

	line = read_config_read_data(, line, &StorageTmp->mtpOmCongestionLevel, &);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->mtpOmSlCongestionFirst, &tmpint);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->mtpOmSlCongestionInterval, &tmpint);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->mtpOmSlCongestionStarts, &tmpint);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->mtpOmSlCongestionStops, &tmpint);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->mtpOmSlCongestionEventsMSULoss, &tmpint);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->mtpOmSlCongestionRowStatus, &tmpint);

	mtpOmSlCongestionTable_add(StorageTmp);

	DEBUGMSGTL(("mtpOmSlCongestionTable", "done.\n"));
}

/*
 * store_mtpOmSlCongestionTable():
 *   stores .conf file entries needed to configure the mib.
 */
int
store_mtpOmSlCongestionTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpint;
	struct mtpOmSlCongestionTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("mtpOmSlCongestionTable", "storing data...  "));

	refresh_mtpOmSlCongestionTable();

	for (hcindex = mtpOmSlCongestionTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct mtpOmSlCongestionTable_data *) hcindex->data;

/*   XXX:  if (StorageTmp->mtpOmSlCongestionTableStorageType == ST_NONVOLATILE) { */

		memset(line, 0, sizeof(line));
		strcat(line, "mtpOmSlCongestionTable ");
		cptr = line + strlen(line);

		cptr = read_config_store_data(, cptr, &StorageTmp->mtpOmCongestionLevel, &);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->mtpOmSlCongestionFirst, &tmpint);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->mtpOmSlCongestionInterval, &tmpint);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->mtpOmSlCongestionStarts, &tmpint);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->mtpOmSlCongestionStops, &tmpint);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->mtpOmSlCongestionEventsMSULoss, &tmpint);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->mtpOmSlCongestionRowStatus, &tmpint);

		snmpd_store_config(line);
/*   } */
	}
	DEBUGMSGTL(("mtpOmSlCongestionTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * refresh_mtpOmHandledMSUsOpcDpcSioTable(): refresh mtpOmHandledMSUsOpcDpcSioTable
 *
 * Normally the values retrieved from the operating system are cached.
 * When the agent receives a SIGPOLL from an open STREAMS configuration
 * or administrative driver Stream, the STREAMS subsystem indicates to
 * the agent that the cache has been invalidated and that it should
 * reread tables from the STREAMS subsystem.  This function is used when
 * the agent start for the first time, or after a SIGPOLL has been
 * received (and a row or column has been requested).
 */
void
refresh_mtpOmHandledMSUsOpcDpcSioTable(void)
{
	if (mtpOmHandledMSUsOpcDpcSioTable_refresh == 0)
		return;
	mtpOmHandledMSUsOpcDpcSioTable_refresh = 0;

	/* Here, update the table as required... */

}

/**
 * refresh_mtpOmHandledMSUsOpcDpcSioTable_row(): refresh mtpOmHandledMSUsOpcDpcSioTable row
 *
 * Normally the values retrieved from the operating system are cached.
 * However, if a row contains temporal values, such as statistics
 * counters, gauges, timestamps, or other transient columns, it may be
 * necessary to refresh the row on some other basis, but normally only
 * once per request.
 */
void
refresh_mtpOmHandledMSUsOpcDpcSioTable_row(struct mtpOmHandledMSUsOpcDpcSioTable_data *StorageTmp)
{
	if (StorageTmp->mtpOmHandledMSUsOpcDpcSioTable_request == sa_request)
		return;
	StorageTmp->mtpOmHandledMSUsOpcDpcSioTable_request = sa_request;
}

/*
 * var_mtpOmHandledMSUsOpcDpcSioTable():
 *   Handle this table separately from the scalar value case.
 *   The workings of this are basically the same as for var_mtpOmMIB above.
 */
u_char *
var_mtpOmHandledMSUsOpcDpcSioTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{

	struct mtpOmHandledMSUsOpcDpcSioTable_data *StorageTmp = NULL;

	DEBUGMSGTL(("mtpOmMIB", "var_mtpOmHandledMSUsOpcDpcSioTable: Entering...  \n"));

	/* Make sure that the storage data does not need to be refreshed before checking the
	   header. */
	refresh_mtpOmHandledMSUsOpcDpcSioTable();

	/* This assumes you have registered all your data properly with header_complex_add()
	   somewhere before this. */
	if ((StorageTmp = header_complex(mtpOmHandledMSUsOpcDpcSioTableStorage, vp, name, length, exact, var_len, write_method)) == NULL)
		return NULL;

	refresh_mtpOmHandledMSUsOpcDpcSioTable_row(StorageTmp);

	/* This is where we do the value assignments for the mib results. */

	switch (vp->magic) {

	case MTPOMHANDLEDMSUSOPCDPCSIO:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->mtpOmHandledMSUsOpcDpcSio);
		return (u_char *) &StorageTmp->mtpOmHandledMSUsOpcDpcSio;

	case MTPOMHANDLEDMSUSOPCDPCSIOROWSTATUS:
		*write_method = write_mtpOmHandledMSUsOpcDpcSioRowStatus;
		*var_len = sizeof(StorageTmp->mtpOmHandledMSUsOpcDpcSioRowStatus);
		return (u_char *) &StorageTmp->mtpOmHandledMSUsOpcDpcSioRowStatus;

	default:
		ERROR_MSG("");
	}
	return NULL;
}

/**
 * refresh_mtpOmHandledOctetsOpcDpcSioTable(): refresh mtpOmHandledOctetsOpcDpcSioTable
 *
 * Normally the values retrieved from the operating system are cached.
 * When the agent receives a SIGPOLL from an open STREAMS configuration
 * or administrative driver Stream, the STREAMS subsystem indicates to
 * the agent that the cache has been invalidated and that it should
 * reread tables from the STREAMS subsystem.  This function is used when
 * the agent start for the first time, or after a SIGPOLL has been
 * received (and a row or column has been requested).
 */
void
refresh_mtpOmHandledOctetsOpcDpcSioTable(void)
{
	if (mtpOmHandledOctetsOpcDpcSioTable_refresh == 0)
		return;
	mtpOmHandledOctetsOpcDpcSioTable_refresh = 0;

	/* Here, update the table as required... */

}

/**
 * refresh_mtpOmHandledOctetsOpcDpcSioTable_row(): refresh mtpOmHandledOctetsOpcDpcSioTable row
 *
 * Normally the values retrieved from the operating system are cached.
 * However, if a row contains temporal values, such as statistics
 * counters, gauges, timestamps, or other transient columns, it may be
 * necessary to refresh the row on some other basis, but normally only
 * once per request.
 */
void
refresh_mtpOmHandledOctetsOpcDpcSioTable_row(struct mtpOmHandledOctetsOpcDpcSioTable_data *StorageTmp)
{
	if (StorageTmp->mtpOmHandledOctetsOpcDpcSioTable_request == sa_request)
		return;
	StorageTmp->mtpOmHandledOctetsOpcDpcSioTable_request = sa_request;
}

/*
 * var_mtpOmHandledOctetsOpcDpcSioTable():
 *   Handle this table separately from the scalar value case.
 *   The workings of this are basically the same as for var_mtpOmMIB above.
 */
u_char *
var_mtpOmHandledOctetsOpcDpcSioTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{

	struct mtpOmHandledOctetsOpcDpcSioTable_data *StorageTmp = NULL;

	DEBUGMSGTL(("mtpOmMIB", "var_mtpOmHandledOctetsOpcDpcSioTable: Entering...  \n"));

	/* Make sure that the storage data does not need to be refreshed before checking the
	   header. */
	refresh_mtpOmHandledOctetsOpcDpcSioTable();

	/* This assumes you have registered all your data properly with header_complex_add()
	   somewhere before this. */
	if ((StorageTmp = header_complex(mtpOmHandledOctetsOpcDpcSioTableStorage, vp, name, length, exact, var_len, write_method)) == NULL)
		return NULL;

	refresh_mtpOmHandledOctetsOpcDpcSioTable_row(StorageTmp);

	/* This is where we do the value assignments for the mib results. */

	switch (vp->magic) {

	case MTPOMHANDLEDOCTETSOPCDPCSIOINTERVAL:
		*write_method = write_mtpOmHandledOctetsOpcDpcSioInterval;
		*var_len = sizeof(StorageTmp->mtpOmHandledOctetsOpcDpcSioInterval);
		return (u_char *) &StorageTmp->mtpOmHandledOctetsOpcDpcSioInterval;

	case MTPOMHANDLEDOCTETSOPCDPCSIO:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->mtpOmHandledOctetsOpcDpcSio);
		return (u_char *) &StorageTmp->mtpOmHandledOctetsOpcDpcSio;

	case MTPOMHANDLEDOCTETSOPCDPCSIOROWSTATUS:
		*write_method = write_mtpOmHandledOctetsOpcDpcSioRowStatus;
		*var_len = sizeof(StorageTmp->mtpOmHandledOctetsOpcDpcSioRowStatus);
		return (u_char *) &StorageTmp->mtpOmHandledOctetsOpcDpcSioRowStatus;

	default:
		ERROR_MSG("");
	}
	return NULL;
}

/**
 * refresh_mtpOmHandledOctetsSIOTable(): refresh mtpOmHandledOctetsSIOTable
 *
 * Normally the values retrieved from the operating system are cached.
 * When the agent receives a SIGPOLL from an open STREAMS configuration
 * or administrative driver Stream, the STREAMS subsystem indicates to
 * the agent that the cache has been invalidated and that it should
 * reread tables from the STREAMS subsystem.  This function is used when
 * the agent start for the first time, or after a SIGPOLL has been
 * received (and a row or column has been requested).
 */
void
refresh_mtpOmHandledOctetsSIOTable(void)
{
	if (mtpOmHandledOctetsSIOTable_refresh == 0)
		return;
	mtpOmHandledOctetsSIOTable_refresh = 0;

	/* Here, update the table as required... */

}

/**
 * refresh_mtpOmHandledOctetsSIOTable_row(): refresh mtpOmHandledOctetsSIOTable row
 *
 * Normally the values retrieved from the operating system are cached.
 * However, if a row contains temporal values, such as statistics
 * counters, gauges, timestamps, or other transient columns, it may be
 * necessary to refresh the row on some other basis, but normally only
 * once per request.
 */
void
refresh_mtpOmHandledOctetsSIOTable_row(struct mtpOmHandledOctetsSIOTable_data *StorageTmp)
{
	if (StorageTmp->mtpOmHandledOctetsSIOTable_request == sa_request)
		return;
	StorageTmp->mtpOmHandledOctetsSIOTable_request = sa_request;
}

/*
 * var_mtpOmHandledOctetsSIOTable():
 *   Handle this table separately from the scalar value case.
 *   The workings of this are basically the same as for var_mtpOmMIB above.
 */
u_char *
var_mtpOmHandledOctetsSIOTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{

	struct mtpOmHandledOctetsSIOTable_data *StorageTmp = NULL;

	DEBUGMSGTL(("mtpOmMIB", "var_mtpOmHandledOctetsSIOTable: Entering...  \n"));

	/* Make sure that the storage data does not need to be refreshed before checking the
	   header. */
	refresh_mtpOmHandledOctetsSIOTable();

	/* This assumes you have registered all your data properly with header_complex_add()
	   somewhere before this. */
	if ((StorageTmp = header_complex(mtpOmHandledOctetsSIOTableStorage, vp, name, length, exact, var_len, write_method)) == NULL)
		return NULL;

	refresh_mtpOmHandledOctetsSIOTable_row(StorageTmp);

	/* This is where we do the value assignments for the mib results. */

	switch (vp->magic) {

	case MTPOMHANDLEDOCTETSSIO:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->mtpOmHandledOctetsSIO);
		return (u_char *) &StorageTmp->mtpOmHandledOctetsSIO;

	default:
		ERROR_MSG("");
	}
	return NULL;
}

/**
 * refresh_mtpOmOblSpUtilizationTable(): refresh mtpOmOblSpUtilizationTable
 *
 * Normally the values retrieved from the operating system are cached.
 * When the agent receives a SIGPOLL from an open STREAMS configuration
 * or administrative driver Stream, the STREAMS subsystem indicates to
 * the agent that the cache has been invalidated and that it should
 * reread tables from the STREAMS subsystem.  This function is used when
 * the agent start for the first time, or after a SIGPOLL has been
 * received (and a row or column has been requested).
 */
void
refresh_mtpOmOblSpUtilizationTable(void)
{
	if (mtpOmOblSpUtilizationTable_refresh == 0)
		return;
	mtpOmOblSpUtilizationTable_refresh = 0;

	/* Here, update the table as required... */

}

/**
 * refresh_mtpOmOblSpUtilizationTable_row(): refresh mtpOmOblSpUtilizationTable row
 *
 * Normally the values retrieved from the operating system are cached.
 * However, if a row contains temporal values, such as statistics
 * counters, gauges, timestamps, or other transient columns, it may be
 * necessary to refresh the row on some other basis, but normally only
 * once per request.
 */
void
refresh_mtpOmOblSpUtilizationTable_row(struct mtpOmOblSpUtilizationTable_data *StorageTmp)
{
	if (StorageTmp->mtpOmOblSpUtilizationTable_request == sa_request)
		return;
	StorageTmp->mtpOmOblSpUtilizationTable_request = sa_request;
}

/*
 * var_mtpOmOblSpUtilizationTable():
 *   Handle this table separately from the scalar value case.
 *   The workings of this are basically the same as for var_mtpOmMIB above.
 */
u_char *
var_mtpOmOblSpUtilizationTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{

	struct mtpOmOblSpUtilizationTable_data *StorageTmp = NULL;

	DEBUGMSGTL(("mtpOmMIB", "var_mtpOmOblSpUtilizationTable: Entering...  \n"));

	/* Make sure that the storage data does not need to be refreshed before checking the
	   header. */
	refresh_mtpOmOblSpUtilizationTable();

	/* This assumes you have registered all your data properly with header_complex_add()
	   somewhere before this. */
	if ((StorageTmp = header_complex(mtpOmOblSpUtilizationTableStorage, vp, name, length, exact, var_len, write_method)) == NULL)
		return NULL;

	refresh_mtpOmOblSpUtilizationTable_row(StorageTmp);

	/* This is where we do the value assignments for the mib results. */

	switch (vp->magic) {

	case MTPOMADJACENTINACCESSIBLEDURATION:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->mtpOmAdjacentInaccessibleDuration);
		return (u_char *) &StorageTmp->mtpOmAdjacentInaccessibleDuration;

	default:
		ERROR_MSG("");
	}
	return NULL;
}

/**
 * refresh_mtpOmAllSpUtilizationTable(): refresh mtpOmAllSpUtilizationTable
 *
 * Normally the values retrieved from the operating system are cached.
 * When the agent receives a SIGPOLL from an open STREAMS configuration
 * or administrative driver Stream, the STREAMS subsystem indicates to
 * the agent that the cache has been invalidated and that it should
 * reread tables from the STREAMS subsystem.  This function is used when
 * the agent start for the first time, or after a SIGPOLL has been
 * received (and a row or column has been requested).
 */
void
refresh_mtpOmAllSpUtilizationTable(void)
{
	if (mtpOmAllSpUtilizationTable_refresh == 0)
		return;
	mtpOmAllSpUtilizationTable_refresh = 0;

	/* Here, update the table as required... */

}

/**
 * refresh_mtpOmAllSpUtilizationTable_row(): refresh mtpOmAllSpUtilizationTable row
 *
 * Normally the values retrieved from the operating system are cached.
 * However, if a row contains temporal values, such as statistics
 * counters, gauges, timestamps, or other transient columns, it may be
 * necessary to refresh the row on some other basis, but normally only
 * once per request.
 */
void
refresh_mtpOmAllSpUtilizationTable_row(struct mtpOmAllSpUtilizationTable_data *StorageTmp)
{
	if (StorageTmp->mtpOmAllSpUtilizationTable_request == sa_request)
		return;
	StorageTmp->mtpOmAllSpUtilizationTable_request = sa_request;
}

/*
 * var_mtpOmAllSpUtilizationTable():
 *   Handle this table separately from the scalar value case.
 *   The workings of this are basically the same as for var_mtpOmMIB above.
 */
u_char *
var_mtpOmAllSpUtilizationTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{

	struct mtpOmAllSpUtilizationTable_data *StorageTmp = NULL;

	DEBUGMSGTL(("mtpOmMIB", "var_mtpOmAllSpUtilizationTable: Entering...  \n"));

	/* Make sure that the storage data does not need to be refreshed before checking the
	   header. */
	refresh_mtpOmAllSpUtilizationTable();

	/* This assumes you have registered all your data properly with header_complex_add()
	   somewhere before this. */
	if ((StorageTmp = header_complex(mtpOmAllSpUtilizationTableStorage, vp, name, length, exact, var_len, write_method)) == NULL)
		return NULL;

	refresh_mtpOmAllSpUtilizationTable_row(StorageTmp);

	/* This is where we do the value assignments for the mib results. */

	switch (vp->magic) {

	case MTPOMADJACENTINACCESSIBLEEVENTS:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->mtpOmAdjacentInaccessibleEvents);
		return (u_char *) &StorageTmp->mtpOmAdjacentInaccessibleEvents;

	case MTPOMALLSPUTILIZATIONROWSTATUS:
		*write_method = write_mtpOmAllSpUtilizationRowStatus;
		*var_len = sizeof(StorageTmp->mtpOmAllSpUtilizationRowStatus);
		return (u_char *) &StorageTmp->mtpOmAllSpUtilizationRowStatus;

	default:
		ERROR_MSG("");
	}
	return NULL;
}

/**
 * refresh_mtpOmReceivedOctetsOPCTable(): refresh mtpOmReceivedOctetsOPCTable
 *
 * Normally the values retrieved from the operating system are cached.
 * When the agent receives a SIGPOLL from an open STREAMS configuration
 * or administrative driver Stream, the STREAMS subsystem indicates to
 * the agent that the cache has been invalidated and that it should
 * reread tables from the STREAMS subsystem.  This function is used when
 * the agent start for the first time, or after a SIGPOLL has been
 * received (and a row or column has been requested).
 */
void
refresh_mtpOmReceivedOctetsOPCTable(void)
{
	if (mtpOmReceivedOctetsOPCTable_refresh == 0)
		return;
	mtpOmReceivedOctetsOPCTable_refresh = 0;

	/* Here, update the table as required... */

}

/**
 * refresh_mtpOmReceivedOctetsOPCTable_row(): refresh mtpOmReceivedOctetsOPCTable row
 *
 * Normally the values retrieved from the operating system are cached.
 * However, if a row contains temporal values, such as statistics
 * counters, gauges, timestamps, or other transient columns, it may be
 * necessary to refresh the row on some other basis, but normally only
 * once per request.
 */
void
refresh_mtpOmReceivedOctetsOPCTable_row(struct mtpOmReceivedOctetsOPCTable_data *StorageTmp)
{
	if (StorageTmp->mtpOmReceivedOctetsOPCTable_request == sa_request)
		return;
	StorageTmp->mtpOmReceivedOctetsOPCTable_request = sa_request;
}

/*
 * var_mtpOmReceivedOctetsOPCTable():
 *   Handle this table separately from the scalar value case.
 *   The workings of this are basically the same as for var_mtpOmMIB above.
 */
u_char *
var_mtpOmReceivedOctetsOPCTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{

	struct mtpOmReceivedOctetsOPCTable_data *StorageTmp = NULL;

	DEBUGMSGTL(("mtpOmMIB", "var_mtpOmReceivedOctetsOPCTable: Entering...  \n"));

	/* Make sure that the storage data does not need to be refreshed before checking the
	   header. */
	refresh_mtpOmReceivedOctetsOPCTable();

	/* This assumes you have registered all your data properly with header_complex_add()
	   somewhere before this. */
	if ((StorageTmp = header_complex(mtpOmReceivedOctetsOPCTableStorage, vp, name, length, exact, var_len, write_method)) == NULL)
		return NULL;

	refresh_mtpOmReceivedOctetsOPCTable_row(StorageTmp);

	/* This is where we do the value assignments for the mib results. */

	switch (vp->magic) {

	case MTPOMRECEIVEDOCTETSOPCINTERVAL:
		*write_method = write_mtpOmReceivedOctetsOPCInterval;
		*var_len = sizeof(StorageTmp->mtpOmReceivedOctetsOPCInterval);
		return (u_char *) &StorageTmp->mtpOmReceivedOctetsOPCInterval;

	case MTPOMRECEIVEDOCTETSOPC:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->mtpOmReceivedOctetsOPC);
		return (u_char *) &StorageTmp->mtpOmReceivedOctetsOPC;

	case MTPOMRECEIVEDOCTETSOPCROWSTATUS:
		*write_method = write_mtpOmReceivedOctetsOPCRowStatus;
		*var_len = sizeof(StorageTmp->mtpOmReceivedOctetsOPCRowStatus);
		return (u_char *) &StorageTmp->mtpOmReceivedOctetsOPCRowStatus;

	default:
		ERROR_MSG("");
	}
	return NULL;
}

/**
 * refresh_mtpOmReceivedOctetsOpcSioTable(): refresh mtpOmReceivedOctetsOpcSioTable
 *
 * Normally the values retrieved from the operating system are cached.
 * When the agent receives a SIGPOLL from an open STREAMS configuration
 * or administrative driver Stream, the STREAMS subsystem indicates to
 * the agent that the cache has been invalidated and that it should
 * reread tables from the STREAMS subsystem.  This function is used when
 * the agent start for the first time, or after a SIGPOLL has been
 * received (and a row or column has been requested).
 */
void
refresh_mtpOmReceivedOctetsOpcSioTable(void)
{
	if (mtpOmReceivedOctetsOpcSioTable_refresh == 0)
		return;
	mtpOmReceivedOctetsOpcSioTable_refresh = 0;

	/* Here, update the table as required... */

}

/**
 * refresh_mtpOmReceivedOctetsOpcSioTable_row(): refresh mtpOmReceivedOctetsOpcSioTable row
 *
 * Normally the values retrieved from the operating system are cached.
 * However, if a row contains temporal values, such as statistics
 * counters, gauges, timestamps, or other transient columns, it may be
 * necessary to refresh the row on some other basis, but normally only
 * once per request.
 */
void
refresh_mtpOmReceivedOctetsOpcSioTable_row(struct mtpOmReceivedOctetsOpcSioTable_data *StorageTmp)
{
	if (StorageTmp->mtpOmReceivedOctetsOpcSioTable_request == sa_request)
		return;
	StorageTmp->mtpOmReceivedOctetsOpcSioTable_request = sa_request;
}

/*
 * var_mtpOmReceivedOctetsOpcSioTable():
 *   Handle this table separately from the scalar value case.
 *   The workings of this are basically the same as for var_mtpOmMIB above.
 */
u_char *
var_mtpOmReceivedOctetsOpcSioTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{

	struct mtpOmReceivedOctetsOpcSioTable_data *StorageTmp = NULL;

	DEBUGMSGTL(("mtpOmMIB", "var_mtpOmReceivedOctetsOpcSioTable: Entering...  \n"));

	/* Make sure that the storage data does not need to be refreshed before checking the
	   header. */
	refresh_mtpOmReceivedOctetsOpcSioTable();

	/* This assumes you have registered all your data properly with header_complex_add()
	   somewhere before this. */
	if ((StorageTmp = header_complex(mtpOmReceivedOctetsOpcSioTableStorage, vp, name, length, exact, var_len, write_method)) == NULL)
		return NULL;

	refresh_mtpOmReceivedOctetsOpcSioTable_row(StorageTmp);

	/* This is where we do the value assignments for the mib results. */

	switch (vp->magic) {

	case MTPOMRECEIVEDOCTETSOPCSIOINTERVAL:
		*write_method = write_mtpOmReceivedOctetsOpcSioInterval;
		*var_len = sizeof(StorageTmp->mtpOmReceivedOctetsOpcSioInterval);
		return (u_char *) &StorageTmp->mtpOmReceivedOctetsOpcSioInterval;

	case MTPOMRECEIVEDOCTETSOPCSIO:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->mtpOmReceivedOctetsOpcSio);
		return (u_char *) &StorageTmp->mtpOmReceivedOctetsOpcSio;

	case MTPOMRECEIVEDOCTETSOPCSIOROWSTATUS:
		*write_method = write_mtpOmReceivedOctetsOpcSioRowStatus;
		*var_len = sizeof(StorageTmp->mtpOmReceivedOctetsOpcSioRowStatus);
		return (u_char *) &StorageTmp->mtpOmReceivedOctetsOpcSioRowStatus;

	default:
		ERROR_MSG("");
	}
	return NULL;
}

/**
 * refresh_mtpOmSpDataTable(): refresh mtpOmSpDataTable
 *
 * Normally the values retrieved from the operating system are cached.
 * When the agent receives a SIGPOLL from an open STREAMS configuration
 * or administrative driver Stream, the STREAMS subsystem indicates to
 * the agent that the cache has been invalidated and that it should
 * reread tables from the STREAMS subsystem.  This function is used when
 * the agent start for the first time, or after a SIGPOLL has been
 * received (and a row or column has been requested).
 */
void
refresh_mtpOmSpDataTable(void)
{
	if (mtpOmSpDataTable_refresh == 0)
		return;
	mtpOmSpDataTable_refresh = 0;

	/* Here, update the table as required... */

}

/**
 * refresh_mtpOmSpDataTable_row(): refresh mtpOmSpDataTable row
 *
 * Normally the values retrieved from the operating system are cached.
 * However, if a row contains temporal values, such as statistics
 * counters, gauges, timestamps, or other transient columns, it may be
 * necessary to refresh the row on some other basis, but normally only
 * once per request.
 */
void
refresh_mtpOmSpDataTable_row(struct mtpOmSpDataTable_data *StorageTmp)
{
	if (StorageTmp->mtpOmSpDataTable_request == sa_request)
		return;
	StorageTmp->mtpOmSpDataTable_request = sa_request;
}

/*
 * var_mtpOmSpDataTable():
 *   Handle this table separately from the scalar value case.
 *   The workings of this are basically the same as for var_mtpOmMIB above.
 */
u_char *
var_mtpOmSpDataTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{

	struct mtpOmSpDataTable_data *StorageTmp = NULL;

	DEBUGMSGTL(("mtpOmMIB", "var_mtpOmSpDataTable: Entering...  \n"));

	/* Make sure that the storage data does not need to be refreshed before checking the
	   header. */
	refresh_mtpOmSpDataTable();

	/* This assumes you have registered all your data properly with header_complex_add()
	   somewhere before this. */
	if ((StorageTmp = header_complex(mtpOmSpDataTableStorage, vp, name, length, exact, var_len, write_method)) == NULL)
		return NULL;

	refresh_mtpOmSpDataTable_row(StorageTmp);

	/* This is where we do the value assignments for the mib results. */

	switch (vp->magic) {

	case MTPOMSPDISCARDEDMSUS:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->mtpOmSpDiscardedMSUs);
		return (u_char *) &StorageTmp->mtpOmSpDiscardedMSUs;

	case MTPOMSPTRANSMITTEDUPUNAVAILABLE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->mtpOmSpTransmittedUPUnavailable);
		return (u_char *) &StorageTmp->mtpOmSpTransmittedUPUnavailable;

	case MTPOMSPRECEIVEDUPUNAVAILABLE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->mtpOmSpReceivedUPUnavailable);
		return (u_char *) &StorageTmp->mtpOmSpReceivedUPUnavailable;

	case MTPOMSPRECEIVEDTFC:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->mtpOmSpReceivedTFC);
		return (u_char *) &StorageTmp->mtpOmSpReceivedTFC;

	case MTPOMSPRECEIVEDTFCLEVEL0:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->mtpOmSpReceivedTFCLevel0);
		return (u_char *) &StorageTmp->mtpOmSpReceivedTFCLevel0;

	case MTPOMSPRECEIVEDTFCLEVEL1:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->mtpOmSpReceivedTFCLevel1);
		return (u_char *) &StorageTmp->mtpOmSpReceivedTFCLevel1;

	case MTPOMSPRECEIVEDTFCLEVEL2:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->mtpOmSpReceivedTFCLevel2);
		return (u_char *) &StorageTmp->mtpOmSpReceivedTFCLevel2;

	case MTPOMSPRECEIVEDTFCLEVEL3:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->mtpOmSpReceivedTFCLevel3);
		return (u_char *) &StorageTmp->mtpOmSpReceivedTFCLevel3;

	default:
		ERROR_MSG("");
	}
	return NULL;
}

/**
 * refresh_mtpOmTransmittedOctetsDPCTable(): refresh mtpOmTransmittedOctetsDPCTable
 *
 * Normally the values retrieved from the operating system are cached.
 * When the agent receives a SIGPOLL from an open STREAMS configuration
 * or administrative driver Stream, the STREAMS subsystem indicates to
 * the agent that the cache has been invalidated and that it should
 * reread tables from the STREAMS subsystem.  This function is used when
 * the agent start for the first time, or after a SIGPOLL has been
 * received (and a row or column has been requested).
 */
void
refresh_mtpOmTransmittedOctetsDPCTable(void)
{
	if (mtpOmTransmittedOctetsDPCTable_refresh == 0)
		return;
	mtpOmTransmittedOctetsDPCTable_refresh = 0;

	/* Here, update the table as required... */

}

/**
 * refresh_mtpOmTransmittedOctetsDPCTable_row(): refresh mtpOmTransmittedOctetsDPCTable row
 *
 * Normally the values retrieved from the operating system are cached.
 * However, if a row contains temporal values, such as statistics
 * counters, gauges, timestamps, or other transient columns, it may be
 * necessary to refresh the row on some other basis, but normally only
 * once per request.
 */
void
refresh_mtpOmTransmittedOctetsDPCTable_row(struct mtpOmTransmittedOctetsDPCTable_data *StorageTmp)
{
	if (StorageTmp->mtpOmTransmittedOctetsDPCTable_request == sa_request)
		return;
	StorageTmp->mtpOmTransmittedOctetsDPCTable_request = sa_request;
}

/*
 * var_mtpOmTransmittedOctetsDPCTable():
 *   Handle this table separately from the scalar value case.
 *   The workings of this are basically the same as for var_mtpOmMIB above.
 */
u_char *
var_mtpOmTransmittedOctetsDPCTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{

	struct mtpOmTransmittedOctetsDPCTable_data *StorageTmp = NULL;

	DEBUGMSGTL(("mtpOmMIB", "var_mtpOmTransmittedOctetsDPCTable: Entering...  \n"));

	/* Make sure that the storage data does not need to be refreshed before checking the
	   header. */
	refresh_mtpOmTransmittedOctetsDPCTable();

	/* This assumes you have registered all your data properly with header_complex_add()
	   somewhere before this. */
	if ((StorageTmp = header_complex(mtpOmTransmittedOctetsDPCTableStorage, vp, name, length, exact, var_len, write_method)) == NULL)
		return NULL;

	refresh_mtpOmTransmittedOctetsDPCTable_row(StorageTmp);

	/* This is where we do the value assignments for the mib results. */

	switch (vp->magic) {

	case MTPOMTRANSMITTEDOCTETSDPCROWSTATUS:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->mtpOmTransmittedOctetsDPCRowStatus);
		return (u_char *) &StorageTmp->mtpOmTransmittedOctetsDPCRowStatus;

	default:
		ERROR_MSG("");
	}
	return NULL;
}

/**
 * refresh_mtpOmTransmittedOctetsDPCTable(): refresh mtpOmTransmittedOctetsDPCTable
 *
 * Normally the values retrieved from the operating system are cached.
 * When the agent receives a SIGPOLL from an open STREAMS configuration
 * or administrative driver Stream, the STREAMS subsystem indicates to
 * the agent that the cache has been invalidated and that it should
 * reread tables from the STREAMS subsystem.  This function is used when
 * the agent start for the first time, or after a SIGPOLL has been
 * received (and a row or column has been requested).
 */
void
refresh_mtpOmTransmittedOctetsDPCTable(void)
{
	if (mtpOmTransmittedOctetsDPCTable_refresh == 0)
		return;
	mtpOmTransmittedOctetsDPCTable_refresh = 0;

	/* Here, update the table as required... */

}

/**
 * refresh_mtpOmTransmittedOctetsDPCTable_row(): refresh mtpOmTransmittedOctetsDPCTable row
 *
 * Normally the values retrieved from the operating system are cached.
 * However, if a row contains temporal values, such as statistics
 * counters, gauges, timestamps, or other transient columns, it may be
 * necessary to refresh the row on some other basis, but normally only
 * once per request.
 */
void
refresh_mtpOmTransmittedOctetsDPCTable_row(struct mtpOmTransmittedOctetsDPCTable_data *StorageTmp)
{
	if (StorageTmp->mtpOmTransmittedOctetsDPCTable_request == sa_request)
		return;
	StorageTmp->mtpOmTransmittedOctetsDPCTable_request = sa_request;
}

/*
 * var_mtpOmTransmittedOctetsDPCTable():
 *   Handle this table separately from the scalar value case.
 *   The workings of this are basically the same as for var_mtpOmMIB above.
 */
u_char *
var_mtpOmTransmittedOctetsDPCTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{

	struct mtpOmTransmittedOctetsDPCTable_data *StorageTmp = NULL;

	DEBUGMSGTL(("mtpOmMIB", "var_mtpOmTransmittedOctetsDPCTable: Entering...  \n"));

	/* Make sure that the storage data does not need to be refreshed before checking the
	   header. */
	refresh_mtpOmTransmittedOctetsDPCTable();

	/* This assumes you have registered all your data properly with header_complex_add()
	   somewhere before this. */
	if ((StorageTmp = header_complex(mtpOmTransmittedOctetsDPCTableStorage, vp, name, length, exact, var_len, write_method)) == NULL)
		return NULL;

	refresh_mtpOmTransmittedOctetsDPCTable_row(StorageTmp);

	/* This is where we do the value assignments for the mib results. */

	switch (vp->magic) {

	case MTPOMTRANSMITTEDOCTETSDPCROWSTATUS:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->mtpOmTransmittedOctetsDPCRowStatus);
		return (u_char *) &StorageTmp->mtpOmTransmittedOctetsDPCRowStatus;

	default:
		ERROR_MSG("");
	}
	return NULL;
}

/**
 * refresh_mtpOmTransmittedOctetsDPCTable(): refresh mtpOmTransmittedOctetsDPCTable
 *
 * Normally the values retrieved from the operating system are cached.
 * When the agent receives a SIGPOLL from an open STREAMS configuration
 * or administrative driver Stream, the STREAMS subsystem indicates to
 * the agent that the cache has been invalidated and that it should
 * reread tables from the STREAMS subsystem.  This function is used when
 * the agent start for the first time, or after a SIGPOLL has been
 * received (and a row or column has been requested).
 */
void
refresh_mtpOmTransmittedOctetsDPCTable(void)
{
	if (mtpOmTransmittedOctetsDPCTable_refresh == 0)
		return;
	mtpOmTransmittedOctetsDPCTable_refresh = 0;

	/* Here, update the table as required... */

}

/**
 * refresh_mtpOmTransmittedOctetsDPCTable_row(): refresh mtpOmTransmittedOctetsDPCTable row
 *
 * Normally the values retrieved from the operating system are cached.
 * However, if a row contains temporal values, such as statistics
 * counters, gauges, timestamps, or other transient columns, it may be
 * necessary to refresh the row on some other basis, but normally only
 * once per request.
 */
void
refresh_mtpOmTransmittedOctetsDPCTable_row(struct mtpOmTransmittedOctetsDPCTable_data *StorageTmp)
{
	if (StorageTmp->mtpOmTransmittedOctetsDPCTable_request == sa_request)
		return;
	StorageTmp->mtpOmTransmittedOctetsDPCTable_request = sa_request;
}

/*
 * var_mtpOmTransmittedOctetsDPCTable():
 *   Handle this table separately from the scalar value case.
 *   The workings of this are basically the same as for var_mtpOmMIB above.
 */
u_char *
var_mtpOmTransmittedOctetsDPCTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{

	struct mtpOmTransmittedOctetsDPCTable_data *StorageTmp = NULL;

	DEBUGMSGTL(("mtpOmMIB", "var_mtpOmTransmittedOctetsDPCTable: Entering...  \n"));

	/* Make sure that the storage data does not need to be refreshed before checking the
	   header. */
	refresh_mtpOmTransmittedOctetsDPCTable();

	/* This assumes you have registered all your data properly with header_complex_add()
	   somewhere before this. */
	if ((StorageTmp = header_complex(mtpOmTransmittedOctetsDPCTableStorage, vp, name, length, exact, var_len, write_method)) == NULL)
		return NULL;

	refresh_mtpOmTransmittedOctetsDPCTable_row(StorageTmp);

	/* This is where we do the value assignments for the mib results. */

	switch (vp->magic) {

	case MTPOMTRANSMITTEDOCTETSDPCROWSTATUS:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->mtpOmTransmittedOctetsDPCRowStatus);
		return (u_char *) &StorageTmp->mtpOmTransmittedOctetsDPCRowStatus;

	default:
		ERROR_MSG("");
	}
	return NULL;
}

/**
 * refresh_mtpOmTransmittedOctetsDpcSioTable(): refresh mtpOmTransmittedOctetsDpcSioTable
 *
 * Normally the values retrieved from the operating system are cached.
 * When the agent receives a SIGPOLL from an open STREAMS configuration
 * or administrative driver Stream, the STREAMS subsystem indicates to
 * the agent that the cache has been invalidated and that it should
 * reread tables from the STREAMS subsystem.  This function is used when
 * the agent start for the first time, or after a SIGPOLL has been
 * received (and a row or column has been requested).
 */
void
refresh_mtpOmTransmittedOctetsDpcSioTable(void)
{
	if (mtpOmTransmittedOctetsDpcSioTable_refresh == 0)
		return;
	mtpOmTransmittedOctetsDpcSioTable_refresh = 0;

	/* Here, update the table as required... */

}

/**
 * refresh_mtpOmTransmittedOctetsDpcSioTable_row(): refresh mtpOmTransmittedOctetsDpcSioTable row
 *
 * Normally the values retrieved from the operating system are cached.
 * However, if a row contains temporal values, such as statistics
 * counters, gauges, timestamps, or other transient columns, it may be
 * necessary to refresh the row on some other basis, but normally only
 * once per request.
 */
void
refresh_mtpOmTransmittedOctetsDpcSioTable_row(struct mtpOmTransmittedOctetsDpcSioTable_data *StorageTmp)
{
	if (StorageTmp->mtpOmTransmittedOctetsDpcSioTable_request == sa_request)
		return;
	StorageTmp->mtpOmTransmittedOctetsDpcSioTable_request = sa_request;
}

/*
 * var_mtpOmTransmittedOctetsDpcSioTable():
 *   Handle this table separately from the scalar value case.
 *   The workings of this are basically the same as for var_mtpOmMIB above.
 */
u_char *
var_mtpOmTransmittedOctetsDpcSioTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{

	struct mtpOmTransmittedOctetsDpcSioTable_data *StorageTmp = NULL;

	DEBUGMSGTL(("mtpOmMIB", "var_mtpOmTransmittedOctetsDpcSioTable: Entering...  \n"));

	/* Make sure that the storage data does not need to be refreshed before checking the
	   header. */
	refresh_mtpOmTransmittedOctetsDpcSioTable();

	/* This assumes you have registered all your data properly with header_complex_add()
	   somewhere before this. */
	if ((StorageTmp = header_complex(mtpOmTransmittedOctetsDpcSioTableStorage, vp, name, length, exact, var_len, write_method)) == NULL)
		return NULL;

	refresh_mtpOmTransmittedOctetsDpcSioTable_row(StorageTmp);

	/* This is where we do the value assignments for the mib results. */

	switch (vp->magic) {

	case MTPOMTRANSMITTEDOCTETSDPCSIO:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->mtpOmTransmittedOctetsDpcSio);
		return (u_char *) &StorageTmp->mtpOmTransmittedOctetsDpcSio;

	case MTPOMTRANSMITTEDOCTETSDPCSIOROWSTATUS:
		*write_method = write_mtpOmTransmittedOctetsDpcSioRowStatus;
		*var_len = sizeof(StorageTmp->mtpOmTransmittedOctetsDpcSioRowStatus);
		return (u_char *) &StorageTmp->mtpOmTransmittedOctetsDpcSioRowStatus;

	default:
		ERROR_MSG("");
	}
	return NULL;
}

/**
 * refresh_mtpOmSignRsInformationTable(): refresh mtpOmSignRsInformationTable
 *
 * Normally the values retrieved from the operating system are cached.
 * When the agent receives a SIGPOLL from an open STREAMS configuration
 * or administrative driver Stream, the STREAMS subsystem indicates to
 * the agent that the cache has been invalidated and that it should
 * reread tables from the STREAMS subsystem.  This function is used when
 * the agent start for the first time, or after a SIGPOLL has been
 * received (and a row or column has been requested).
 */
void
refresh_mtpOmSignRsInformationTable(void)
{
	if (mtpOmSignRsInformationTable_refresh == 0)
		return;
	mtpOmSignRsInformationTable_refresh = 0;

	/* Here, update the table as required... */

}

/**
 * refresh_mtpOmSignRsInformationTable_row(): refresh mtpOmSignRsInformationTable row
 *
 * Normally the values retrieved from the operating system are cached.
 * However, if a row contains temporal values, such as statistics
 * counters, gauges, timestamps, or other transient columns, it may be
 * necessary to refresh the row on some other basis, but normally only
 * once per request.
 */
void
refresh_mtpOmSignRsInformationTable_row(struct mtpOmSignRsInformationTable_data *StorageTmp)
{
	if (StorageTmp->mtpOmSignRsInformationTable_request == sa_request)
		return;
	StorageTmp->mtpOmSignRsInformationTable_request = sa_request;
}

/*
 * var_mtpOmSignRsInformationTable():
 *   Handle this table separately from the scalar value case.
 *   The workings of this are basically the same as for var_mtpOmMIB above.
 */
u_char *
var_mtpOmSignRsInformationTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{

	struct mtpOmSignRsInformationTable_data *StorageTmp = NULL;

	DEBUGMSGTL(("mtpOmMIB", "var_mtpOmSignRsInformationTable: Entering...  \n"));

	/* Make sure that the storage data does not need to be refreshed before checking the
	   header. */
	refresh_mtpOmSignRsInformationTable();

	/* This assumes you have registered all your data properly with header_complex_add()
	   somewhere before this. */
	if ((StorageTmp = header_complex(mtpOmSignRsInformationTableStorage, vp, name, length, exact, var_len, write_method)) == NULL)
		return NULL;

	refresh_mtpOmSignRsInformationTable_row(StorageTmp);

	/* This is where we do the value assignments for the mib results. */

	switch (vp->magic) {

	case MTPOMROUTESETUNAVAILABLE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->mtpOmRouteSetUnavailable);
		return (u_char *) &StorageTmp->mtpOmRouteSetUnavailable;

	case MTPOMROUTESETUNAVAILABLEDURATION:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->mtpOmRouteSetUnavailableDuration);
		return (u_char *) &StorageTmp->mtpOmRouteSetUnavailableDuration;

	default:
		ERROR_MSG("");
	}
	return NULL;
}

/**
 * refresh_mtpOmSlsDurationTable(): refresh mtpOmSlsDurationTable
 *
 * Normally the values retrieved from the operating system are cached.
 * When the agent receives a SIGPOLL from an open STREAMS configuration
 * or administrative driver Stream, the STREAMS subsystem indicates to
 * the agent that the cache has been invalidated and that it should
 * reread tables from the STREAMS subsystem.  This function is used when
 * the agent start for the first time, or after a SIGPOLL has been
 * received (and a row or column has been requested).
 */
void
refresh_mtpOmSlsDurationTable(void)
{
	if (mtpOmSlsDurationTable_refresh == 0)
		return;
	mtpOmSlsDurationTable_refresh = 0;

	/* Here, update the table as required... */

}

/**
 * refresh_mtpOmSlsDurationTable_row(): refresh mtpOmSlsDurationTable row
 *
 * Normally the values retrieved from the operating system are cached.
 * However, if a row contains temporal values, such as statistics
 * counters, gauges, timestamps, or other transient columns, it may be
 * necessary to refresh the row on some other basis, but normally only
 * once per request.
 */
void
refresh_mtpOmSlsDurationTable_row(struct mtpOmSlsDurationTable_data *StorageTmp)
{
	if (StorageTmp->mtpOmSlsDurationTable_request == sa_request)
		return;
	StorageTmp->mtpOmSlsDurationTable_request = sa_request;
}

/*
 * var_mtpOmSlsDurationTable():
 *   Handle this table separately from the scalar value case.
 *   The workings of this are basically the same as for var_mtpOmMIB above.
 */
u_char *
var_mtpOmSlsDurationTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{

	struct mtpOmSlsDurationTable_data *StorageTmp = NULL;

	DEBUGMSGTL(("mtpOmMIB", "var_mtpOmSlsDurationTable: Entering...  \n"));

	/* Make sure that the storage data does not need to be refreshed before checking the
	   header. */
	refresh_mtpOmSlsDurationTable();

	/* This assumes you have registered all your data properly with header_complex_add()
	   somewhere before this. */
	if ((StorageTmp = header_complex(mtpOmSlsDurationTableStorage, vp, name, length, exact, var_len, write_method)) == NULL)
		return NULL;

	refresh_mtpOmSlsDurationTable_row(StorageTmp);

	/* This is where we do the value assignments for the mib results. */

	switch (vp->magic) {

	case MTPOMSLSUNAVAILABLE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->mtpOmSlsUnavailable);
		return (u_char *) &StorageTmp->mtpOmSlsUnavailable;

	default:
		ERROR_MSG("");
	}
	return NULL;
}

/**
 * refresh_mtpOmAll5And30MinSlDurationTable(): refresh mtpOmAll5And30MinSlDurationTable
 *
 * Normally the values retrieved from the operating system are cached.
 * When the agent receives a SIGPOLL from an open STREAMS configuration
 * or administrative driver Stream, the STREAMS subsystem indicates to
 * the agent that the cache has been invalidated and that it should
 * reread tables from the STREAMS subsystem.  This function is used when
 * the agent start for the first time, or after a SIGPOLL has been
 * received (and a row or column has been requested).
 */
void
refresh_mtpOmAll5And30MinSlDurationTable(void)
{
	if (mtpOmAll5And30MinSlDurationTable_refresh == 0)
		return;
	mtpOmAll5And30MinSlDurationTable_refresh = 0;

	/* Here, update the table as required... */

}

/**
 * refresh_mtpOmAll5And30MinSlDurationTable_row(): refresh mtpOmAll5And30MinSlDurationTable row
 *
 * Normally the values retrieved from the operating system are cached.
 * However, if a row contains temporal values, such as statistics
 * counters, gauges, timestamps, or other transient columns, it may be
 * necessary to refresh the row on some other basis, but normally only
 * once per request.
 */
void
refresh_mtpOmAll5And30MinSlDurationTable_row(struct mtpOmAll5And30MinSlDurationTable_data *StorageTmp)
{
	if (StorageTmp->mtpOmAll5And30MinSlDurationTable_request == sa_request)
		return;
	StorageTmp->mtpOmAll5And30MinSlDurationTable_request = sa_request;
}

/*
 * var_mtpOmAll5And30MinSlDurationTable():
 *   Handle this table separately from the scalar value case.
 *   The workings of this are basically the same as for var_mtpOmMIB above.
 */
u_char *
var_mtpOmAll5And30MinSlDurationTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{

	struct mtpOmAll5And30MinSlDurationTable_data *StorageTmp = NULL;

	DEBUGMSGTL(("mtpOmMIB", "var_mtpOmAll5And30MinSlDurationTable: Entering...  \n"));

	/* Make sure that the storage data does not need to be refreshed before checking the
	   header. */
	refresh_mtpOmAll5And30MinSlDurationTable();

	/* This assumes you have registered all your data properly with header_complex_add()
	   somewhere before this. */
	if ((StorageTmp = header_complex(mtpOmAll5And30MinSlDurationTableStorage, vp, name, length, exact, var_len, write_method)) == NULL)
		return NULL;

	refresh_mtpOmAll5And30MinSlDurationTable_row(StorageTmp);

	/* This is where we do the value assignments for the mib results. */

	switch (vp->magic) {

	case MTPOMALL5AND30MINSLDURATIONINTERVAL:
		*write_method = write_mtpOmAll5And30MinSlDurationInterval;
		*var_len = sizeof(StorageTmp->mtpOmAll5And30MinSlDurationInterval);
		return (u_char *) &StorageTmp->mtpOmAll5And30MinSlDurationInterval;

	case MTPOMLOCALMGNTINHIBIT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->mtpOmLocalMgntInhibit);
		return (u_char *) &StorageTmp->mtpOmLocalMgntInhibit;

	case MTPOMLOCALMGNTUNINHIBIT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->mtpOmLocalMgntUninhibit);
		return (u_char *) &StorageTmp->mtpOmLocalMgntUninhibit;

	case MTPOMLOCALBUSY:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->mtpOmLocalBusy);
		return (u_char *) &StorageTmp->mtpOmLocalBusy;

	case MTPOMALL5AND30MINSLDURATIONROWSTATUS:
		*write_method = write_mtpOmAll5And30MinSlDurationRowStatus;
		*var_len = sizeof(StorageTmp->mtpOmAll5And30MinSlDurationRowStatus);
		return (u_char *) &StorageTmp->mtpOmAll5And30MinSlDurationRowStatus;

	default:
		ERROR_MSG("");
	}
	return NULL;
}

/**
 * refresh_mtpOmOblSlDurationTable(): refresh mtpOmOblSlDurationTable
 *
 * Normally the values retrieved from the operating system are cached.
 * When the agent receives a SIGPOLL from an open STREAMS configuration
 * or administrative driver Stream, the STREAMS subsystem indicates to
 * the agent that the cache has been invalidated and that it should
 * reread tables from the STREAMS subsystem.  This function is used when
 * the agent start for the first time, or after a SIGPOLL has been
 * received (and a row or column has been requested).
 */
void
refresh_mtpOmOblSlDurationTable(void)
{
	if (mtpOmOblSlDurationTable_refresh == 0)
		return;
	mtpOmOblSlDurationTable_refresh = 0;

	/* Here, update the table as required... */

}

/**
 * refresh_mtpOmOblSlDurationTable_row(): refresh mtpOmOblSlDurationTable row
 *
 * Normally the values retrieved from the operating system are cached.
 * However, if a row contains temporal values, such as statistics
 * counters, gauges, timestamps, or other transient columns, it may be
 * necessary to refresh the row on some other basis, but normally only
 * once per request.
 */
void
refresh_mtpOmOblSlDurationTable_row(struct mtpOmOblSlDurationTable_data *StorageTmp)
{
	if (StorageTmp->mtpOmOblSlDurationTable_request == sa_request)
		return;
	StorageTmp->mtpOmOblSlDurationTable_request = sa_request;
}

/*
 * var_mtpOmOblSlDurationTable():
 *   Handle this table separately from the scalar value case.
 *   The workings of this are basically the same as for var_mtpOmMIB above.
 */
u_char *
var_mtpOmOblSlDurationTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{

	struct mtpOmOblSlDurationTable_data *StorageTmp = NULL;

	DEBUGMSGTL(("mtpOmMIB", "var_mtpOmOblSlDurationTable: Entering...  \n"));

	/* Make sure that the storage data does not need to be refreshed before checking the
	   header. */
	refresh_mtpOmOblSlDurationTable();

	/* This assumes you have registered all your data properly with header_complex_add()
	   somewhere before this. */
	if ((StorageTmp = header_complex(mtpOmOblSlDurationTableStorage, vp, name, length, exact, var_len, write_method)) == NULL)
		return NULL;

	refresh_mtpOmOblSlDurationTable_row(StorageTmp);

	/* This is where we do the value assignments for the mib results. */

	switch (vp->magic) {

	case MTPOMSLUNAVAILABILITYDURATION:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->mtpOmSlUnavailabilityDuration);
		return (u_char *) &StorageTmp->mtpOmSlUnavailabilityDuration;

	case MTPOMSLINSERVICEDURATION:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->mtpOmSlInServiceDuration);
		return (u_char *) &StorageTmp->mtpOmSlInServiceDuration;

	default:
		ERROR_MSG("");
	}
	return NULL;
}

/**
 * refresh_mtpOmAll30MinSlDurationTable(): refresh mtpOmAll30MinSlDurationTable
 *
 * Normally the values retrieved from the operating system are cached.
 * When the agent receives a SIGPOLL from an open STREAMS configuration
 * or administrative driver Stream, the STREAMS subsystem indicates to
 * the agent that the cache has been invalidated and that it should
 * reread tables from the STREAMS subsystem.  This function is used when
 * the agent start for the first time, or after a SIGPOLL has been
 * received (and a row or column has been requested).
 */
void
refresh_mtpOmAll30MinSlDurationTable(void)
{
	if (mtpOmAll30MinSlDurationTable_refresh == 0)
		return;
	mtpOmAll30MinSlDurationTable_refresh = 0;

	/* Here, update the table as required... */

}

/**
 * refresh_mtpOmAll30MinSlDurationTable_row(): refresh mtpOmAll30MinSlDurationTable row
 *
 * Normally the values retrieved from the operating system are cached.
 * However, if a row contains temporal values, such as statistics
 * counters, gauges, timestamps, or other transient columns, it may be
 * necessary to refresh the row on some other basis, but normally only
 * once per request.
 */
void
refresh_mtpOmAll30MinSlDurationTable_row(struct mtpOmAll30MinSlDurationTable_data *StorageTmp)
{
	if (StorageTmp->mtpOmAll30MinSlDurationTable_request == sa_request)
		return;
	StorageTmp->mtpOmAll30MinSlDurationTable_request = sa_request;
}

/*
 * var_mtpOmAll30MinSlDurationTable():
 *   Handle this table separately from the scalar value case.
 *   The workings of this are basically the same as for var_mtpOmMIB above.
 */
u_char *
var_mtpOmAll30MinSlDurationTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{

	struct mtpOmAll30MinSlDurationTable_data *StorageTmp = NULL;

	DEBUGMSGTL(("mtpOmMIB", "var_mtpOmAll30MinSlDurationTable: Entering...  \n"));

	/* Make sure that the storage data does not need to be refreshed before checking the
	   header. */
	refresh_mtpOmAll30MinSlDurationTable();

	/* This assumes you have registered all your data properly with header_complex_add()
	   somewhere before this. */
	if ((StorageTmp = header_complex(mtpOmAll30MinSlDurationTableStorage, vp, name, length, exact, var_len, write_method)) == NULL)
		return NULL;

	refresh_mtpOmAll30MinSlDurationTable_row(StorageTmp);

	/* This is where we do the value assignments for the mib results. */

	switch (vp->magic) {

	case MTPOMALL30MINSLDURATIONINTERVAL:
		*write_method = write_mtpOmAll30MinSlDurationInterval;
		*var_len = sizeof(StorageTmp->mtpOmAll30MinSlDurationInterval);
		return (u_char *) &StorageTmp->mtpOmAll30MinSlDurationInterval;

	case MTPOMSLLOCALINHIBITION:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->mtpOmSlLocalInhibition);
		return (u_char *) &StorageTmp->mtpOmSlLocalInhibition;

	case MTPOMSLREMOTEINHIBITION:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->mtpOmSlRemoteInhibition);
		return (u_char *) &StorageTmp->mtpOmSlRemoteInhibition;

	case MTPOMSLFAILED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->mtpOmSlFailed);
		return (u_char *) &StorageTmp->mtpOmSlFailed;

	case MTPOMSLREMOTEPROCOUTAGE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->mtpOmSlRemoteProcOutage);
		return (u_char *) &StorageTmp->mtpOmSlRemoteProcOutage;

	case MTPOMALL30MINSLDURATIONROWSTATUS:
		*write_method = write_mtpOmAll30MinSlDurationRowStatus;
		*var_len = sizeof(StorageTmp->mtpOmAll30MinSlDurationRowStatus);
		return (u_char *) &StorageTmp->mtpOmAll30MinSlDurationRowStatus;

	default:
		ERROR_MSG("");
	}
	return NULL;
}

/**
 * refresh_mtpOmOblSlUtilizationTable(): refresh mtpOmOblSlUtilizationTable
 *
 * Normally the values retrieved from the operating system are cached.
 * When the agent receives a SIGPOLL from an open STREAMS configuration
 * or administrative driver Stream, the STREAMS subsystem indicates to
 * the agent that the cache has been invalidated and that it should
 * reread tables from the STREAMS subsystem.  This function is used when
 * the agent start for the first time, or after a SIGPOLL has been
 * received (and a row or column has been requested).
 */
void
refresh_mtpOmOblSlUtilizationTable(void)
{
	if (mtpOmOblSlUtilizationTable_refresh == 0)
		return;
	mtpOmOblSlUtilizationTable_refresh = 0;

	/* Here, update the table as required... */

}

/**
 * refresh_mtpOmOblSlUtilizationTable_row(): refresh mtpOmOblSlUtilizationTable row
 *
 * Normally the values retrieved from the operating system are cached.
 * However, if a row contains temporal values, such as statistics
 * counters, gauges, timestamps, or other transient columns, it may be
 * necessary to refresh the row on some other basis, but normally only
 * once per request.
 */
void
refresh_mtpOmOblSlUtilizationTable_row(struct mtpOmOblSlUtilizationTable_data *StorageTmp)
{
	if (StorageTmp->mtpOmOblSlUtilizationTable_request == sa_request)
		return;
	StorageTmp->mtpOmOblSlUtilizationTable_request = sa_request;
}

/*
 * var_mtpOmOblSlUtilizationTable():
 *   Handle this table separately from the scalar value case.
 *   The workings of this are basically the same as for var_mtpOmMIB above.
 */
u_char *
var_mtpOmOblSlUtilizationTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{

	struct mtpOmOblSlUtilizationTable_data *StorageTmp = NULL;

	DEBUGMSGTL(("mtpOmMIB", "var_mtpOmOblSlUtilizationTable: Entering...  \n"));

	/* Make sure that the storage data does not need to be refreshed before checking the
	   header. */
	refresh_mtpOmOblSlUtilizationTable();

	/* This assumes you have registered all your data properly with header_complex_add()
	   somewhere before this. */
	if ((StorageTmp = header_complex(mtpOmOblSlUtilizationTableStorage, vp, name, length, exact, var_len, write_method)) == NULL)
		return NULL;

	refresh_mtpOmOblSlUtilizationTable_row(StorageTmp);

	/* This is where we do the value assignments for the mib results. */

	switch (vp->magic) {

	case MTPOMSLSIGNUNITSRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->mtpOmSlSignUnitsReceived);
		return (u_char *) &StorageTmp->mtpOmSlSignUnitsReceived;

	case MTPOMSLTRANSMITTEDOCTETSSIFSIO:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->mtpOmSlTransmittedOctetsSIFSIO);
		return (u_char *) &StorageTmp->mtpOmSlTransmittedOctetsSIFSIO;

	case MTPOMSLRECEIVEDOCTETSSIFSIO:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->mtpOmSlReceivedOctetsSIFSIO);
		return (u_char *) &StorageTmp->mtpOmSlReceivedOctetsSIFSIO;

	case MTPOMSLDISCARDEDMSUS:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->mtpOmSlDiscardedMSUs);
		return (u_char *) &StorageTmp->mtpOmSlDiscardedMSUs;

	default:
		ERROR_MSG("");
	}
	return NULL;
}

/**
 * refresh_mtpOmAllSlUtilizationTable(): refresh mtpOmAllSlUtilizationTable
 *
 * Normally the values retrieved from the operating system are cached.
 * When the agent receives a SIGPOLL from an open STREAMS configuration
 * or administrative driver Stream, the STREAMS subsystem indicates to
 * the agent that the cache has been invalidated and that it should
 * reread tables from the STREAMS subsystem.  This function is used when
 * the agent start for the first time, or after a SIGPOLL has been
 * received (and a row or column has been requested).
 */
void
refresh_mtpOmAllSlUtilizationTable(void)
{
	if (mtpOmAllSlUtilizationTable_refresh == 0)
		return;
	mtpOmAllSlUtilizationTable_refresh = 0;

	/* Here, update the table as required... */

}

/**
 * refresh_mtpOmAllSlUtilizationTable_row(): refresh mtpOmAllSlUtilizationTable row
 *
 * Normally the values retrieved from the operating system are cached.
 * However, if a row contains temporal values, such as statistics
 * counters, gauges, timestamps, or other transient columns, it may be
 * necessary to refresh the row on some other basis, but normally only
 * once per request.
 */
void
refresh_mtpOmAllSlUtilizationTable_row(struct mtpOmAllSlUtilizationTable_data *StorageTmp)
{
	if (StorageTmp->mtpOmAllSlUtilizationTable_request == sa_request)
		return;
	StorageTmp->mtpOmAllSlUtilizationTable_request = sa_request;
}

/*
 * var_mtpOmAllSlUtilizationTable():
 *   Handle this table separately from the scalar value case.
 *   The workings of this are basically the same as for var_mtpOmMIB above.
 */
u_char *
var_mtpOmAllSlUtilizationTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{

	struct mtpOmAllSlUtilizationTable_data *StorageTmp = NULL;

	DEBUGMSGTL(("mtpOmMIB", "var_mtpOmAllSlUtilizationTable: Entering...  \n"));

	/* Make sure that the storage data does not need to be refreshed before checking the
	   header. */
	refresh_mtpOmAllSlUtilizationTable();

	/* This assumes you have registered all your data properly with header_complex_add()
	   somewhere before this. */
	if ((StorageTmp = header_complex(mtpOmAllSlUtilizationTableStorage, vp, name, length, exact, var_len, write_method)) == NULL)
		return NULL;

	refresh_mtpOmAllSlUtilizationTable_row(StorageTmp);

	/* This is where we do the value assignments for the mib results. */

	switch (vp->magic) {

	case MTPOMSLALIGNMENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->mtpOmSlAlignment);
		return (u_char *) &StorageTmp->mtpOmSlAlignment;

	case MTPOMSLNEGACKRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->mtpOmSlNegAckReceived);
		return (u_char *) &StorageTmp->mtpOmSlNegAckReceived;

	case MTPOMSLRETRANSMITTEDOCTETS:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->mtpOmSlRetransmittedOctets);
		return (u_char *) &StorageTmp->mtpOmSlRetransmittedOctets;

	case MTPOMSLTRANSMITTEDMSUS:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->mtpOmSlTransmittedMSUs);
		return (u_char *) &StorageTmp->mtpOmSlTransmittedMSUs;

	case MTPOMSLRECEIVEDMSUS:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->mtpOmSlReceivedMSUs);
		return (u_char *) &StorageTmp->mtpOmSlReceivedMSUs;

	case MTPOMALLSLUTILIZATIONROWSTATUS:
		*write_method = write_mtpOmAllSlUtilizationRowStatus;
		*var_len = sizeof(StorageTmp->mtpOmAllSlUtilizationRowStatus);
		return (u_char *) &StorageTmp->mtpOmAllSlUtilizationRowStatus;

	default:
		ERROR_MSG("");
	}
	return NULL;
}

/**
 * refresh_mtpOmSlCongestionTable(): refresh mtpOmSlCongestionTable
 *
 * Normally the values retrieved from the operating system are cached.
 * When the agent receives a SIGPOLL from an open STREAMS configuration
 * or administrative driver Stream, the STREAMS subsystem indicates to
 * the agent that the cache has been invalidated and that it should
 * reread tables from the STREAMS subsystem.  This function is used when
 * the agent start for the first time, or after a SIGPOLL has been
 * received (and a row or column has been requested).
 */
void
refresh_mtpOmSlCongestionTable(void)
{
	if (mtpOmSlCongestionTable_refresh == 0)
		return;
	mtpOmSlCongestionTable_refresh = 0;

	/* Here, update the table as required... */

}

/**
 * refresh_mtpOmSlCongestionTable_row(): refresh mtpOmSlCongestionTable row
 *
 * Normally the values retrieved from the operating system are cached.
 * However, if a row contains temporal values, such as statistics
 * counters, gauges, timestamps, or other transient columns, it may be
 * necessary to refresh the row on some other basis, but normally only
 * once per request.
 */
void
refresh_mtpOmSlCongestionTable_row(struct mtpOmSlCongestionTable_data *StorageTmp)
{
	if (StorageTmp->mtpOmSlCongestionTable_request == sa_request)
		return;
	StorageTmp->mtpOmSlCongestionTable_request = sa_request;
}

/*
 * var_mtpOmSlCongestionTable():
 *   Handle this table separately from the scalar value case.
 *   The workings of this are basically the same as for var_mtpOmMIB above.
 */
u_char *
var_mtpOmSlCongestionTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{

	struct mtpOmSlCongestionTable_data *StorageTmp = NULL;

	DEBUGMSGTL(("mtpOmMIB", "var_mtpOmSlCongestionTable: Entering...  \n"));

	/* Make sure that the storage data does not need to be refreshed before checking the
	   header. */
	refresh_mtpOmSlCongestionTable();

	/* This assumes you have registered all your data properly with header_complex_add()
	   somewhere before this. */
	if ((StorageTmp = header_complex(mtpOmSlCongestionTableStorage, vp, name, length, exact, var_len, write_method)) == NULL)
		return NULL;

	refresh_mtpOmSlCongestionTable_row(StorageTmp);

	/* This is where we do the value assignments for the mib results. */

	switch (vp->magic) {

	case MTPOMSLCONGESTIONFIRST:
		*write_method = write_mtpOmSlCongestionFirst;
		*var_len = sizeof(StorageTmp->mtpOmSlCongestionFirst);
		return (u_char *) &StorageTmp->mtpOmSlCongestionFirst;

	case MTPOMSLCONGESTIONINTERVAL:
		*write_method = write_mtpOmSlCongestionInterval;
		*var_len = sizeof(StorageTmp->mtpOmSlCongestionInterval);
		return (u_char *) &StorageTmp->mtpOmSlCongestionInterval;

	case MTPOMSLCONGESTIONSTARTS:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->mtpOmSlCongestionStarts);
		return (u_char *) &StorageTmp->mtpOmSlCongestionStarts;

	case MTPOMSLCONGESTIONSTOPS:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->mtpOmSlCongestionStops);
		return (u_char *) &StorageTmp->mtpOmSlCongestionStops;

	case MTPOMSLCONGESTIONEVENTSMSULOSS:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->mtpOmSlCongestionEventsMSULoss);
		return (u_char *) &StorageTmp->mtpOmSlCongestionEventsMSULoss;

	case MTPOMSLCONGESTIONROWSTATUS:
		*write_method = write_mtpOmSlCongestionRowStatus;
		*var_len = sizeof(StorageTmp->mtpOmSlCongestionRowStatus);
		return (u_char *) &StorageTmp->mtpOmSlCongestionRowStatus;

	default:
		ERROR_MSG("");
	}
	return NULL;
}

int
write_mtpOmHandledOctetsOpcDpcSioInterval(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long tmpvar;
	struct mtpOmHandledOctetsOpcDpcSioTable_data *StorageTmp = NULL;
	static size_t tmplen;
	size_t newlen = name_len - (sizeof(mtpOmMIB_variables_oid) / sizeof(oid) + 6 - 1);

	(void) tmplen;		/* not always used */
	DEBUGMSGTL(("mtpOmMIB", "write_mtpOmHandledOctetsOpcDpcSioInterval entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(mtpOmHandledOctetsOpcDpcSioTableStorage, NULL, &name[sizeof(mtpOmMIB_variables_oid) / sizeof(oid) + 6 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */

	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to mtpOmHandledOctetsOpcDpcSioInterval not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		break;

	case RESERVE2:
		/* memory reseveration, final preparation... */
		break;

	case FREE:
		/* Release any resources that have been allocated */
		break;

	case ACTION:
		/* The variable has been stored in long_ret for you to use, and you have just been
		   asked to do something with it.  Note that anything done here must be reversable
		   in the UNDO case */
		tmpvar = StorageTmp->mtpOmHandledOctetsOpcDpcSioInterval;
		StorageTmp->mtpOmHandledOctetsOpcDpcSioInterval = *((long *) var_val);
		break;

	case UNDO:
		/* Back out any changes made in the ACTION case */
		StorageTmp->mtpOmHandledOctetsOpcDpcSioInterval = tmpvar;
		break;

	case COMMIT:
	{
		/* Things are working well, so it's now safe to make the change permanently.  Make
		   sure that anything done here can't fail! */

		break;
	}
	}
	return SNMP_ERR_NOERROR;
}

int
write_mtpOmReceivedOctetsOPCInterval(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long tmpvar;
	struct mtpOmReceivedOctetsOPCTable_data *StorageTmp = NULL;
	static size_t tmplen;
	size_t newlen = name_len - (sizeof(mtpOmMIB_variables_oid) / sizeof(oid) + 6 - 1);

	(void) tmplen;		/* not always used */
	DEBUGMSGTL(("mtpOmMIB", "write_mtpOmReceivedOctetsOPCInterval entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(mtpOmReceivedOctetsOPCTableStorage, NULL, &name[sizeof(mtpOmMIB_variables_oid) / sizeof(oid) + 6 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */

	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to mtpOmReceivedOctetsOPCInterval not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		break;

	case RESERVE2:
		/* memory reseveration, final preparation... */
		break;

	case FREE:
		/* Release any resources that have been allocated */
		break;

	case ACTION:
		/* The variable has been stored in long_ret for you to use, and you have just been
		   asked to do something with it.  Note that anything done here must be reversable
		   in the UNDO case */
		tmpvar = StorageTmp->mtpOmReceivedOctetsOPCInterval;
		StorageTmp->mtpOmReceivedOctetsOPCInterval = *((long *) var_val);
		break;

	case UNDO:
		/* Back out any changes made in the ACTION case */
		StorageTmp->mtpOmReceivedOctetsOPCInterval = tmpvar;
		break;

	case COMMIT:
	{
		/* Things are working well, so it's now safe to make the change permanently.  Make
		   sure that anything done here can't fail! */

		break;
	}
	}
	return SNMP_ERR_NOERROR;
}

int
write_mtpOmReceivedOctetsOpcSioInterval(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long tmpvar;
	struct mtpOmReceivedOctetsOpcSioTable_data *StorageTmp = NULL;
	static size_t tmplen;
	size_t newlen = name_len - (sizeof(mtpOmMIB_variables_oid) / sizeof(oid) + 6 - 1);

	(void) tmplen;		/* not always used */
	DEBUGMSGTL(("mtpOmMIB", "write_mtpOmReceivedOctetsOpcSioInterval entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(mtpOmReceivedOctetsOpcSioTableStorage, NULL, &name[sizeof(mtpOmMIB_variables_oid) / sizeof(oid) + 6 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */

	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to mtpOmReceivedOctetsOpcSioInterval not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		break;

	case RESERVE2:
		/* memory reseveration, final preparation... */
		break;

	case FREE:
		/* Release any resources that have been allocated */
		break;

	case ACTION:
		/* The variable has been stored in long_ret for you to use, and you have just been
		   asked to do something with it.  Note that anything done here must be reversable
		   in the UNDO case */
		tmpvar = StorageTmp->mtpOmReceivedOctetsOpcSioInterval;
		StorageTmp->mtpOmReceivedOctetsOpcSioInterval = *((long *) var_val);
		break;

	case UNDO:
		/* Back out any changes made in the ACTION case */
		StorageTmp->mtpOmReceivedOctetsOpcSioInterval = tmpvar;
		break;

	case COMMIT:
	{
		/* Things are working well, so it's now safe to make the change permanently.  Make
		   sure that anything done here can't fail! */

		break;
	}
	}
	return SNMP_ERR_NOERROR;
}

int
write_mtpOmAll5And30MinSlDurationInterval(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long tmpvar;
	struct mtpOmAll5And30MinSlDurationTable_data *StorageTmp = NULL;
	static size_t tmplen;
	size_t newlen = name_len - (sizeof(mtpOmMIB_variables_oid) / sizeof(oid) + 6 - 1);

	(void) tmplen;		/* not always used */
	DEBUGMSGTL(("mtpOmMIB", "write_mtpOmAll5And30MinSlDurationInterval entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(mtpOmAll5And30MinSlDurationTableStorage, NULL, &name[sizeof(mtpOmMIB_variables_oid) / sizeof(oid) + 6 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */

	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to mtpOmAll5And30MinSlDurationInterval not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		break;

	case RESERVE2:
		/* memory reseveration, final preparation... */
		break;

	case FREE:
		/* Release any resources that have been allocated */
		break;

	case ACTION:
		/* The variable has been stored in long_ret for you to use, and you have just been
		   asked to do something with it.  Note that anything done here must be reversable
		   in the UNDO case */
		tmpvar = StorageTmp->mtpOmAll5And30MinSlDurationInterval;
		StorageTmp->mtpOmAll5And30MinSlDurationInterval = *((long *) var_val);
		break;

	case UNDO:
		/* Back out any changes made in the ACTION case */
		StorageTmp->mtpOmAll5And30MinSlDurationInterval = tmpvar;
		break;

	case COMMIT:
	{
		/* Things are working well, so it's now safe to make the change permanently.  Make
		   sure that anything done here can't fail! */

		break;
	}
	}
	return SNMP_ERR_NOERROR;
}

int
write_mtpOmAll30MinSlDurationInterval(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long tmpvar;
	struct mtpOmAll30MinSlDurationTable_data *StorageTmp = NULL;
	static size_t tmplen;
	size_t newlen = name_len - (sizeof(mtpOmMIB_variables_oid) / sizeof(oid) + 6 - 1);

	(void) tmplen;		/* not always used */
	DEBUGMSGTL(("mtpOmMIB", "write_mtpOmAll30MinSlDurationInterval entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(mtpOmAll30MinSlDurationTableStorage, NULL, &name[sizeof(mtpOmMIB_variables_oid) / sizeof(oid) + 6 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */

	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to mtpOmAll30MinSlDurationInterval not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		break;

	case RESERVE2:
		/* memory reseveration, final preparation... */
		break;

	case FREE:
		/* Release any resources that have been allocated */
		break;

	case ACTION:
		/* The variable has been stored in long_ret for you to use, and you have just been
		   asked to do something with it.  Note that anything done here must be reversable
		   in the UNDO case */
		tmpvar = StorageTmp->mtpOmAll30MinSlDurationInterval;
		StorageTmp->mtpOmAll30MinSlDurationInterval = *((long *) var_val);
		break;

	case UNDO:
		/* Back out any changes made in the ACTION case */
		StorageTmp->mtpOmAll30MinSlDurationInterval = tmpvar;
		break;

	case COMMIT:
	{
		/* Things are working well, so it's now safe to make the change permanently.  Make
		   sure that anything done here can't fail! */

		break;
	}
	}
	return SNMP_ERR_NOERROR;
}

int
write_mtpOmSlCongestionFirst(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long tmpvar;
	struct mtpOmSlCongestionTable_data *StorageTmp = NULL;
	static size_t tmplen;
	size_t newlen = name_len - (sizeof(mtpOmMIB_variables_oid) / sizeof(oid) + 6 - 1);

	(void) tmplen;		/* not always used */
	DEBUGMSGTL(("mtpOmMIB", "write_mtpOmSlCongestionFirst entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(mtpOmSlCongestionTableStorage, NULL, &name[sizeof(mtpOmMIB_variables_oid) / sizeof(oid) + 6 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */

	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to mtpOmSlCongestionFirst not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		break;

	case RESERVE2:
		/* memory reseveration, final preparation... */
		break;

	case FREE:
		/* Release any resources that have been allocated */
		break;

	case ACTION:
		/* The variable has been stored in long_ret for you to use, and you have just been
		   asked to do something with it.  Note that anything done here must be reversable
		   in the UNDO case */
		tmpvar = StorageTmp->mtpOmSlCongestionFirst;
		StorageTmp->mtpOmSlCongestionFirst = *((long *) var_val);
		break;

	case UNDO:
		/* Back out any changes made in the ACTION case */
		StorageTmp->mtpOmSlCongestionFirst = tmpvar;
		break;

	case COMMIT:
	{
		/* Things are working well, so it's now safe to make the change permanently.  Make
		   sure that anything done here can't fail! */

		break;
	}
	}
	return SNMP_ERR_NOERROR;
}

int
write_mtpOmSlCongestionInterval(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long tmpvar;
	struct mtpOmSlCongestionTable_data *StorageTmp = NULL;
	static size_t tmplen;
	size_t newlen = name_len - (sizeof(mtpOmMIB_variables_oid) / sizeof(oid) + 6 - 1);

	(void) tmplen;		/* not always used */
	DEBUGMSGTL(("mtpOmMIB", "write_mtpOmSlCongestionInterval entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(mtpOmSlCongestionTableStorage, NULL, &name[sizeof(mtpOmMIB_variables_oid) / sizeof(oid) + 6 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */

	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to mtpOmSlCongestionInterval not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		break;

	case RESERVE2:
		/* memory reseveration, final preparation... */
		break;

	case FREE:
		/* Release any resources that have been allocated */
		break;

	case ACTION:
		/* The variable has been stored in long_ret for you to use, and you have just been
		   asked to do something with it.  Note that anything done here must be reversable
		   in the UNDO case */
		tmpvar = StorageTmp->mtpOmSlCongestionInterval;
		StorageTmp->mtpOmSlCongestionInterval = *((long *) var_val);
		break;

	case UNDO:
		/* Back out any changes made in the ACTION case */
		StorageTmp->mtpOmSlCongestionInterval = tmpvar;
		break;

	case COMMIT:
	{
		/* Things are working well, so it's now safe to make the change permanently.  Make
		   sure that anything done here can't fail! */

		break;
	}
	}
	return SNMP_ERR_NOERROR;
}

int
write_mtpOmHandledMSUsOpcDpcSioRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct mtpOmHandledMSUsOpcDpcSioTable_data *StorageTmp = NULL;
	static struct mtpOmHandledMSUsOpcDpcSioTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - (sizeof(mtpOmHandledMSUsOpcDpcSioTable_variables_oid) / sizeof(oid) + 3 - 1);
	static int old_value;
	int set_value;
	static struct variable_list *vars, *vp;
	struct header_complex_index *hciptr;

	StorageTmp = header_complex(mtpOmHandledMSUsOpcDpcSioTableStorage, NULL, &name[sizeof(mtpOmHandledMSUsOpcDpcSioTable_variables_oid) / sizeof(oid) + 3 - 1], &newlen, 1, NULL, NULL);

	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to mtpOmHandledMSUsOpcDpcSioRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((int *) var_val);

	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY)
		return SNMP_ERR_INCONSISTENTVALUE;

	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		if (StorageTmp == NULL) {
			/* create the row now? */

			/* ditch illegal values now */
			if (set_value == RS_ACTIVE || set_value == RS_NOTINSERVICE)
				return SNMP_ERR_INCONSISTENTVALUE;

			/* destroying a non-existent row is actually legal */
			if (set_value == RS_DESTROY) {
				return SNMP_ERR_NOERROR;
			}

			/* illegal creation values */
			if (set_value == RS_ACTIVE || set_value == RS_NOTINSERVICE) {
				return SNMP_ERR_INCONSISTENTVALUE;
			}
		} else {
			/* row exists.  Check for a valid state change */
			if (set_value == RS_CREATEANDGO || set_value == RS_CREATEANDWAIT) {
				/* can't create a row that exists */
				return SNMP_ERR_INCONSISTENTVALUE;
			}
			/* XXX: interaction with row storage type needed */
		}
		break;

	case RESERVE2:
		/* memory reseveration, final preparation... */
		if (StorageTmp == NULL) {
			/* creation */
			vars = NULL;

			snmp_varlist_add_variable(&vars, NULL, 0,, NULL, 0);	/* mtpOmOpc */
			snmp_varlist_add_variable(&vars, NULL, 0,, NULL, 0);	/* mtpOmDpc */
			snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0);	/* mtpOmSio 
												 */

			if (header_complex_parse_oid(&(name[sizeof(mtpOmHandledMSUsOpcDpcSioTable_variables_oid) / sizeof(oid) + 2]), newlen, vars) != SNMPERR_SUCCESS) {
				/* XXX: free, zero vars */
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;

			StorageNew = SNMP_MALLOC_STRUCT(mtpOmHandledMSUsOpcDpcSioTable_data);

			vp = vp->next_variable;

			vp = vp->next_variable;
			memdup((void *) &StorageNew->mtpOmSio, vp->val.string, vp->val_len);
			StorageNew->mtpOmSioLen = vp->val_len;
			vp = vp->next_variable;

			/* XXX: fill in default row values here into StorageNew */

			StorageNew->mtpOmHandledMSUsOpcDpcSioRowStatus = set_value;
			/* XXX: free, zero vars, no longer needed? */
		}

		break;

	case FREE:
		/* XXX: free, zero vars */
		/* Release any resources that have been allocated */
		break;

	case ACTION:
		/* The variable has been stored in set_value for you to use, and you have just been 
		   asked to do something with it.  Note that anything done here must be reversable
		   in the UNDO case */

		if (StorageTmp == NULL) {
			/* row creation, so add it */
			if (StorageNew != NULL)
				mtpOmHandledMSUsOpcDpcSioTable_add(StorageNew);
			/* XXX: ack, and if it is NULL? */
		} else if (set_value != RS_DESTROY) {
			/* set the flag? */
			old_value = StorageTmp->mtpOmHandledMSUsOpcDpcSioRowStatus;
			StorageTmp->mtpOmHandledMSUsOpcDpcSioRowStatus = *((int *) var_val);
		} else {
			/* destroy...  extract it for now */
			hciptr = header_complex_find_entry(mtpOmHandledMSUsOpcDpcSioTableStorage, StorageTmp);
			StorageDel = header_complex_extract_entry(&mtpOmHandledMSUsOpcDpcSioTableStorage, hciptr);
		}
		break;

	case UNDO:
		/* Back out any changes made in the ACTION case */
		if (StorageTmp == NULL) {
			/* row creation, so remove it again */
			hciptr = header_complex_find_entry(mtpOmHandledMSUsOpcDpcSioTableStorage, StorageTmp);
			StorageDel = header_complex_extract_entry(&mtpOmHandledMSUsOpcDpcSioTableStorage, hciptr);
			/* XXX: free it */
		} else if (StorageDel != NULL) {
			/* row deletion, so add it again */
			mtpOmHandledMSUsOpcDpcSioTable_add(StorageDel);
		} else {
			StorageTmp->mtpOmHandledMSUsOpcDpcSioRowStatus = old_value;
		}
		break;

	case COMMIT:
	{
		/* Things are working well, so it's now safe to make the change permanently.  Make
		   sure that anything done here can't fail! */
		if (StorageDel != NULL) {
			StorageDel = 0;
			/* XXX: free it, its dead */
		} else {
			if (StorageTmp && StorageTmp->mtpOmHandledMSUsOpcDpcSioRowStatus == RS_CREATEANDGO) {
				StorageTmp->mtpOmHandledMSUsOpcDpcSioRowStatus = RS_ACTIVE;
			} else if (StorageTmp && StorageTmp->mtpOmHandledMSUsOpcDpcSioRowStatus == RS_CREATEANDWAIT) {
				StorageTmp->mtpOmHandledMSUsOpcDpcSioRowStatus = RS_NOTINSERVICE;
			}
		}
		break;
	}
	}
	return SNMP_ERR_NOERROR;
}

int
write_mtpOmHandledOctetsOpcDpcSioRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct mtpOmHandledOctetsOpcDpcSioTable_data *StorageTmp = NULL;
	static struct mtpOmHandledOctetsOpcDpcSioTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - (sizeof(mtpOmHandledOctetsOpcDpcSioTable_variables_oid) / sizeof(oid) + 3 - 1);
	static int old_value;
	int set_value;
	static struct variable_list *vars, *vp;
	struct header_complex_index *hciptr;

	StorageTmp = header_complex(mtpOmHandledOctetsOpcDpcSioTableStorage, NULL, &name[sizeof(mtpOmHandledOctetsOpcDpcSioTable_variables_oid) / sizeof(oid) + 3 - 1], &newlen, 1, NULL, NULL);

	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to mtpOmHandledOctetsOpcDpcSioRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((int *) var_val);

	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY)
		return SNMP_ERR_INCONSISTENTVALUE;

	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		if (StorageTmp == NULL) {
			/* create the row now? */

			/* ditch illegal values now */
			if (set_value == RS_ACTIVE || set_value == RS_NOTINSERVICE)
				return SNMP_ERR_INCONSISTENTVALUE;

			/* destroying a non-existent row is actually legal */
			if (set_value == RS_DESTROY) {
				return SNMP_ERR_NOERROR;
			}

			/* illegal creation values */
			if (set_value == RS_ACTIVE || set_value == RS_NOTINSERVICE) {
				return SNMP_ERR_INCONSISTENTVALUE;
			}
		} else {
			/* row exists.  Check for a valid state change */
			if (set_value == RS_CREATEANDGO || set_value == RS_CREATEANDWAIT) {
				/* can't create a row that exists */
				return SNMP_ERR_INCONSISTENTVALUE;
			}
			/* XXX: interaction with row storage type needed */
		}
		break;

	case RESERVE2:
		/* memory reseveration, final preparation... */
		if (StorageTmp == NULL) {
			/* creation */
			vars = NULL;

			snmp_varlist_add_variable(&vars, NULL, 0,, NULL, 0);	/* mtpOmOpc */
			snmp_varlist_add_variable(&vars, NULL, 0,, NULL, 0);	/* mtpOmDpc */
			snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0);	/* mtpOmSio 
												 */

			if (header_complex_parse_oid(&(name[sizeof(mtpOmHandledOctetsOpcDpcSioTable_variables_oid) / sizeof(oid) + 2]), newlen, vars) != SNMPERR_SUCCESS) {
				/* XXX: free, zero vars */
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;

			StorageNew = SNMP_MALLOC_STRUCT(mtpOmHandledOctetsOpcDpcSioTable_data);

			vp = vp->next_variable;

			vp = vp->next_variable;
			memdup((void *) &StorageNew->mtpOmSio, vp->val.string, vp->val_len);
			StorageNew->mtpOmSioLen = vp->val_len;
			vp = vp->next_variable;

			/* XXX: fill in default row values here into StorageNew */

			StorageNew->mtpOmHandledOctetsOpcDpcSioInterval = 180000;

			StorageNew->mtpOmHandledOctetsOpcDpcSioRowStatus = set_value;
			/* XXX: free, zero vars, no longer needed? */
		}

		break;

	case FREE:
		/* XXX: free, zero vars */
		/* Release any resources that have been allocated */
		break;

	case ACTION:
		/* The variable has been stored in set_value for you to use, and you have just been 
		   asked to do something with it.  Note that anything done here must be reversable
		   in the UNDO case */

		if (StorageTmp == NULL) {
			/* row creation, so add it */
			if (StorageNew != NULL)
				mtpOmHandledOctetsOpcDpcSioTable_add(StorageNew);
			/* XXX: ack, and if it is NULL? */
		} else if (set_value != RS_DESTROY) {
			/* set the flag? */
			old_value = StorageTmp->mtpOmHandledOctetsOpcDpcSioRowStatus;
			StorageTmp->mtpOmHandledOctetsOpcDpcSioRowStatus = *((int *) var_val);
		} else {
			/* destroy...  extract it for now */
			hciptr = header_complex_find_entry(mtpOmHandledOctetsOpcDpcSioTableStorage, StorageTmp);
			StorageDel = header_complex_extract_entry(&mtpOmHandledOctetsOpcDpcSioTableStorage, hciptr);
		}
		break;

	case UNDO:
		/* Back out any changes made in the ACTION case */
		if (StorageTmp == NULL) {
			/* row creation, so remove it again */
			hciptr = header_complex_find_entry(mtpOmHandledOctetsOpcDpcSioTableStorage, StorageTmp);
			StorageDel = header_complex_extract_entry(&mtpOmHandledOctetsOpcDpcSioTableStorage, hciptr);
			/* XXX: free it */
		} else if (StorageDel != NULL) {
			/* row deletion, so add it again */
			mtpOmHandledOctetsOpcDpcSioTable_add(StorageDel);
		} else {
			StorageTmp->mtpOmHandledOctetsOpcDpcSioRowStatus = old_value;
		}
		break;

	case COMMIT:
	{
		/* Things are working well, so it's now safe to make the change permanently.  Make
		   sure that anything done here can't fail! */
		if (StorageDel != NULL) {
			StorageDel = 0;
			/* XXX: free it, its dead */
		} else {
			if (StorageTmp && StorageTmp->mtpOmHandledOctetsOpcDpcSioRowStatus == RS_CREATEANDGO) {
				StorageTmp->mtpOmHandledOctetsOpcDpcSioRowStatus = RS_ACTIVE;
			} else if (StorageTmp && StorageTmp->mtpOmHandledOctetsOpcDpcSioRowStatus == RS_CREATEANDWAIT) {
				StorageTmp->mtpOmHandledOctetsOpcDpcSioRowStatus = RS_NOTINSERVICE;
			}
		}
		break;
	}
	}
	return SNMP_ERR_NOERROR;
}

int
write_mtpOmAllSpUtilizationRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct mtpOmAllSpUtilizationTable_data *StorageTmp = NULL;
	static struct mtpOmAllSpUtilizationTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - (sizeof(mtpOmAllSpUtilizationTable_variables_oid) / sizeof(oid) + 3 - 1);
	static int old_value;
	int set_value;
	static struct variable_list *vars, *vp;
	struct header_complex_index *hciptr;

	StorageTmp = header_complex(mtpOmAllSpUtilizationTableStorage, NULL, &name[sizeof(mtpOmAllSpUtilizationTable_variables_oid) / sizeof(oid) + 3 - 1], &newlen, 1, NULL, NULL);

	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to mtpOmAllSpUtilizationRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((int *) var_val);

	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY)
		return SNMP_ERR_INCONSISTENTVALUE;

	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		if (StorageTmp == NULL) {
			/* create the row now? */

			/* ditch illegal values now */
			if (set_value == RS_ACTIVE || set_value == RS_NOTINSERVICE)
				return SNMP_ERR_INCONSISTENTVALUE;

			/* destroying a non-existent row is actually legal */
			if (set_value == RS_DESTROY) {
				return SNMP_ERR_NOERROR;
			}

			/* illegal creation values */
			if (set_value == RS_ACTIVE || set_value == RS_NOTINSERVICE) {
				return SNMP_ERR_INCONSISTENTVALUE;
			}
		} else {
			/* row exists.  Check for a valid state change */
			if (set_value == RS_CREATEANDGO || set_value == RS_CREATEANDWAIT) {
				/* can't create a row that exists */
				return SNMP_ERR_INCONSISTENTVALUE;
			}
			/* XXX: interaction with row storage type needed */
		}
		break;

	case RESERVE2:
		/* memory reseveration, final preparation... */
		if (StorageTmp == NULL) {
			/* creation */
			vars = NULL;

			if (header_complex_parse_oid(&(name[sizeof(mtpOmAllSpUtilizationTable_variables_oid) / sizeof(oid) + 2]), newlen, vars) != SNMPERR_SUCCESS) {
				/* XXX: free, zero vars */
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;

			StorageNew = SNMP_MALLOC_STRUCT(mtpOmAllSpUtilizationTable_data);

			/* XXX: fill in default row values here into StorageNew */

			StorageNew->mtpOmAllSpUtilizationRowStatus = set_value;
			/* XXX: free, zero vars, no longer needed? */
		}

		break;

	case FREE:
		/* XXX: free, zero vars */
		/* Release any resources that have been allocated */
		break;

	case ACTION:
		/* The variable has been stored in set_value for you to use, and you have just been 
		   asked to do something with it.  Note that anything done here must be reversable
		   in the UNDO case */

		if (StorageTmp == NULL) {
			/* row creation, so add it */
			if (StorageNew != NULL)
				mtpOmAllSpUtilizationTable_add(StorageNew);
			/* XXX: ack, and if it is NULL? */
		} else if (set_value != RS_DESTROY) {
			/* set the flag? */
			old_value = StorageTmp->mtpOmAllSpUtilizationRowStatus;
			StorageTmp->mtpOmAllSpUtilizationRowStatus = *((int *) var_val);
		} else {
			/* destroy...  extract it for now */
			hciptr = header_complex_find_entry(mtpOmAllSpUtilizationTableStorage, StorageTmp);
			StorageDel = header_complex_extract_entry(&mtpOmAllSpUtilizationTableStorage, hciptr);
		}
		break;

	case UNDO:
		/* Back out any changes made in the ACTION case */
		if (StorageTmp == NULL) {
			/* row creation, so remove it again */
			hciptr = header_complex_find_entry(mtpOmAllSpUtilizationTableStorage, StorageTmp);
			StorageDel = header_complex_extract_entry(&mtpOmAllSpUtilizationTableStorage, hciptr);
			/* XXX: free it */
		} else if (StorageDel != NULL) {
			/* row deletion, so add it again */
			mtpOmAllSpUtilizationTable_add(StorageDel);
		} else {
			StorageTmp->mtpOmAllSpUtilizationRowStatus = old_value;
		}
		break;

	case COMMIT:
	{
		/* Things are working well, so it's now safe to make the change permanently.  Make
		   sure that anything done here can't fail! */
		if (StorageDel != NULL) {
			StorageDel = 0;
			/* XXX: free it, its dead */
		} else {
			if (StorageTmp && StorageTmp->mtpOmAllSpUtilizationRowStatus == RS_CREATEANDGO) {
				StorageTmp->mtpOmAllSpUtilizationRowStatus = RS_ACTIVE;
			} else if (StorageTmp && StorageTmp->mtpOmAllSpUtilizationRowStatus == RS_CREATEANDWAIT) {
				StorageTmp->mtpOmAllSpUtilizationRowStatus = RS_NOTINSERVICE;
			}
		}
		break;
	}
	}
	return SNMP_ERR_NOERROR;
}

int
write_mtpOmReceivedOctetsOPCRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct mtpOmReceivedOctetsOPCTable_data *StorageTmp = NULL;
	static struct mtpOmReceivedOctetsOPCTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - (sizeof(mtpOmReceivedOctetsOPCTable_variables_oid) / sizeof(oid) + 3 - 1);
	static int old_value;
	int set_value;
	static struct variable_list *vars, *vp;
	struct header_complex_index *hciptr;

	StorageTmp = header_complex(mtpOmReceivedOctetsOPCTableStorage, NULL, &name[sizeof(mtpOmReceivedOctetsOPCTable_variables_oid) / sizeof(oid) + 3 - 1], &newlen, 1, NULL, NULL);

	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to mtpOmReceivedOctetsOPCRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((int *) var_val);

	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY)
		return SNMP_ERR_INCONSISTENTVALUE;

	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		if (StorageTmp == NULL) {
			/* create the row now? */

			/* ditch illegal values now */
			if (set_value == RS_ACTIVE || set_value == RS_NOTINSERVICE)
				return SNMP_ERR_INCONSISTENTVALUE;

			/* destroying a non-existent row is actually legal */
			if (set_value == RS_DESTROY) {
				return SNMP_ERR_NOERROR;
			}

			/* illegal creation values */
			if (set_value == RS_ACTIVE || set_value == RS_NOTINSERVICE) {
				return SNMP_ERR_INCONSISTENTVALUE;
			}
		} else {
			/* row exists.  Check for a valid state change */
			if (set_value == RS_CREATEANDGO || set_value == RS_CREATEANDWAIT) {
				/* can't create a row that exists */
				return SNMP_ERR_INCONSISTENTVALUE;
			}
			/* XXX: interaction with row storage type needed */
		}
		break;

	case RESERVE2:
		/* memory reseveration, final preparation... */
		if (StorageTmp == NULL) {
			/* creation */
			vars = NULL;

			snmp_varlist_add_variable(&vars, NULL, 0,, NULL, 0);	/* mtpOmOpc */

			if (header_complex_parse_oid(&(name[sizeof(mtpOmReceivedOctetsOPCTable_variables_oid) / sizeof(oid) + 2]), newlen, vars) != SNMPERR_SUCCESS) {
				/* XXX: free, zero vars */
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;

			StorageNew = SNMP_MALLOC_STRUCT(mtpOmReceivedOctetsOPCTable_data);

			vp = vp->next_variable;

			/* XXX: fill in default row values here into StorageNew */

			StorageNew->mtpOmReceivedOctetsOPCRowStatus = set_value;
			/* XXX: free, zero vars, no longer needed? */
		}

		break;

	case FREE:
		/* XXX: free, zero vars */
		/* Release any resources that have been allocated */
		break;

	case ACTION:
		/* The variable has been stored in set_value for you to use, and you have just been 
		   asked to do something with it.  Note that anything done here must be reversable
		   in the UNDO case */

		if (StorageTmp == NULL) {
			/* row creation, so add it */
			if (StorageNew != NULL)
				mtpOmReceivedOctetsOPCTable_add(StorageNew);
			/* XXX: ack, and if it is NULL? */
		} else if (set_value != RS_DESTROY) {
			/* set the flag? */
			old_value = StorageTmp->mtpOmReceivedOctetsOPCRowStatus;
			StorageTmp->mtpOmReceivedOctetsOPCRowStatus = *((int *) var_val);
		} else {
			/* destroy...  extract it for now */
			hciptr = header_complex_find_entry(mtpOmReceivedOctetsOPCTableStorage, StorageTmp);
			StorageDel = header_complex_extract_entry(&mtpOmReceivedOctetsOPCTableStorage, hciptr);
		}
		break;

	case UNDO:
		/* Back out any changes made in the ACTION case */
		if (StorageTmp == NULL) {
			/* row creation, so remove it again */
			hciptr = header_complex_find_entry(mtpOmReceivedOctetsOPCTableStorage, StorageTmp);
			StorageDel = header_complex_extract_entry(&mtpOmReceivedOctetsOPCTableStorage, hciptr);
			/* XXX: free it */
		} else if (StorageDel != NULL) {
			/* row deletion, so add it again */
			mtpOmReceivedOctetsOPCTable_add(StorageDel);
		} else {
			StorageTmp->mtpOmReceivedOctetsOPCRowStatus = old_value;
		}
		break;

	case COMMIT:
	{
		/* Things are working well, so it's now safe to make the change permanently.  Make
		   sure that anything done here can't fail! */
		if (StorageDel != NULL) {
			StorageDel = 0;
			/* XXX: free it, its dead */
		} else {
			if (StorageTmp && StorageTmp->mtpOmReceivedOctetsOPCRowStatus == RS_CREATEANDGO) {
				StorageTmp->mtpOmReceivedOctetsOPCRowStatus = RS_ACTIVE;
			} else if (StorageTmp && StorageTmp->mtpOmReceivedOctetsOPCRowStatus == RS_CREATEANDWAIT) {
				StorageTmp->mtpOmReceivedOctetsOPCRowStatus = RS_NOTINSERVICE;
			}
		}
		break;
	}
	}
	return SNMP_ERR_NOERROR;
}

int
write_mtpOmReceivedOctetsOpcSioRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct mtpOmReceivedOctetsOpcSioTable_data *StorageTmp = NULL;
	static struct mtpOmReceivedOctetsOpcSioTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - (sizeof(mtpOmReceivedOctetsOpcSioTable_variables_oid) / sizeof(oid) + 3 - 1);
	static int old_value;
	int set_value;
	static struct variable_list *vars, *vp;
	struct header_complex_index *hciptr;

	StorageTmp = header_complex(mtpOmReceivedOctetsOpcSioTableStorage, NULL, &name[sizeof(mtpOmReceivedOctetsOpcSioTable_variables_oid) / sizeof(oid) + 3 - 1], &newlen, 1, NULL, NULL);

	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to mtpOmReceivedOctetsOpcSioRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((int *) var_val);

	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY)
		return SNMP_ERR_INCONSISTENTVALUE;

	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		if (StorageTmp == NULL) {
			/* create the row now? */

			/* ditch illegal values now */
			if (set_value == RS_ACTIVE || set_value == RS_NOTINSERVICE)
				return SNMP_ERR_INCONSISTENTVALUE;

			/* destroying a non-existent row is actually legal */
			if (set_value == RS_DESTROY) {
				return SNMP_ERR_NOERROR;
			}

			/* illegal creation values */
			if (set_value == RS_ACTIVE || set_value == RS_NOTINSERVICE) {
				return SNMP_ERR_INCONSISTENTVALUE;
			}
		} else {
			/* row exists.  Check for a valid state change */
			if (set_value == RS_CREATEANDGO || set_value == RS_CREATEANDWAIT) {
				/* can't create a row that exists */
				return SNMP_ERR_INCONSISTENTVALUE;
			}
			/* XXX: interaction with row storage type needed */
		}
		break;

	case RESERVE2:
		/* memory reseveration, final preparation... */
		if (StorageTmp == NULL) {
			/* creation */
			vars = NULL;

			snmp_varlist_add_variable(&vars, NULL, 0,, NULL, 0);	/* mtpOmOpc */
			snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0);	/* mtpOmSio 
												 */

			if (header_complex_parse_oid(&(name[sizeof(mtpOmReceivedOctetsOpcSioTable_variables_oid) / sizeof(oid) + 2]), newlen, vars) != SNMPERR_SUCCESS) {
				/* XXX: free, zero vars */
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;

			StorageNew = SNMP_MALLOC_STRUCT(mtpOmReceivedOctetsOpcSioTable_data);

			vp = vp->next_variable;
			memdup((void *) &StorageNew->mtpOmSio, vp->val.string, vp->val_len);
			StorageNew->mtpOmSioLen = vp->val_len;
			vp = vp->next_variable;

			/* XXX: fill in default row values here into StorageNew */

			StorageNew->mtpOmReceivedOctetsOpcSioInterval = 180000;

			StorageNew->mtpOmReceivedOctetsOpcSioRowStatus = set_value;
			/* XXX: free, zero vars, no longer needed? */
		}

		break;

	case FREE:
		/* XXX: free, zero vars */
		/* Release any resources that have been allocated */
		break;

	case ACTION:
		/* The variable has been stored in set_value for you to use, and you have just been 
		   asked to do something with it.  Note that anything done here must be reversable
		   in the UNDO case */

		if (StorageTmp == NULL) {
			/* row creation, so add it */
			if (StorageNew != NULL)
				mtpOmReceivedOctetsOpcSioTable_add(StorageNew);
			/* XXX: ack, and if it is NULL? */
		} else if (set_value != RS_DESTROY) {
			/* set the flag? */
			old_value = StorageTmp->mtpOmReceivedOctetsOpcSioRowStatus;
			StorageTmp->mtpOmReceivedOctetsOpcSioRowStatus = *((int *) var_val);
		} else {
			/* destroy...  extract it for now */
			hciptr = header_complex_find_entry(mtpOmReceivedOctetsOpcSioTableStorage, StorageTmp);
			StorageDel = header_complex_extract_entry(&mtpOmReceivedOctetsOpcSioTableStorage, hciptr);
		}
		break;

	case UNDO:
		/* Back out any changes made in the ACTION case */
		if (StorageTmp == NULL) {
			/* row creation, so remove it again */
			hciptr = header_complex_find_entry(mtpOmReceivedOctetsOpcSioTableStorage, StorageTmp);
			StorageDel = header_complex_extract_entry(&mtpOmReceivedOctetsOpcSioTableStorage, hciptr);
			/* XXX: free it */
		} else if (StorageDel != NULL) {
			/* row deletion, so add it again */
			mtpOmReceivedOctetsOpcSioTable_add(StorageDel);
		} else {
			StorageTmp->mtpOmReceivedOctetsOpcSioRowStatus = old_value;
		}
		break;

	case COMMIT:
	{
		/* Things are working well, so it's now safe to make the change permanently.  Make
		   sure that anything done here can't fail! */
		if (StorageDel != NULL) {
			StorageDel = 0;
			/* XXX: free it, its dead */
		} else {
			if (StorageTmp && StorageTmp->mtpOmReceivedOctetsOpcSioRowStatus == RS_CREATEANDGO) {
				StorageTmp->mtpOmReceivedOctetsOpcSioRowStatus = RS_ACTIVE;
			} else if (StorageTmp && StorageTmp->mtpOmReceivedOctetsOpcSioRowStatus == RS_CREATEANDWAIT) {
				StorageTmp->mtpOmReceivedOctetsOpcSioRowStatus = RS_NOTINSERVICE;
			}
		}
		break;
	}
	}
	return SNMP_ERR_NOERROR;
}

int
write_mtpOmTransmittedOctetsDPCRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct mtpOmTransmittedOctetsDPCTable_data *StorageTmp = NULL;
	static struct mtpOmTransmittedOctetsDPCTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - (sizeof(mtpOmTransmittedOctetsDPCTable_variables_oid) / sizeof(oid) + 3 - 1);
	static int old_value;
	int set_value;
	static struct variable_list *vars, *vp;
	struct header_complex_index *hciptr;

	StorageTmp = header_complex(mtpOmTransmittedOctetsDPCTableStorage, NULL, &name[sizeof(mtpOmTransmittedOctetsDPCTable_variables_oid) / sizeof(oid) + 3 - 1], &newlen, 1, NULL, NULL);

	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to mtpOmTransmittedOctetsDPCRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((int *) var_val);

	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY)
		return SNMP_ERR_INCONSISTENTVALUE;

	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		if (StorageTmp == NULL) {
			/* create the row now? */

			/* ditch illegal values now */
			if (set_value == RS_ACTIVE || set_value == RS_NOTINSERVICE)
				return SNMP_ERR_INCONSISTENTVALUE;

			/* destroying a non-existent row is actually legal */
			if (set_value == RS_DESTROY) {
				return SNMP_ERR_NOERROR;
			}

			/* illegal creation values */
			if (set_value == RS_ACTIVE || set_value == RS_NOTINSERVICE) {
				return SNMP_ERR_INCONSISTENTVALUE;
			}
		} else {
			/* row exists.  Check for a valid state change */
			if (set_value == RS_CREATEANDGO || set_value == RS_CREATEANDWAIT) {
				/* can't create a row that exists */
				return SNMP_ERR_INCONSISTENTVALUE;
			}
			/* XXX: interaction with row storage type needed */
		}
		break;

	case RESERVE2:
		/* memory reseveration, final preparation... */
		if (StorageTmp == NULL) {
			/* creation */
			vars = NULL;

			snmp_varlist_add_variable(&vars, NULL, 0,, NULL, 0);	/* mtpOmDpc */
			snmp_varlist_add_variable(&vars, NULL, 0,, NULL, 0);	/* mtpOmDpc */

			if (header_complex_parse_oid(&(name[sizeof(mtpOmTransmittedOctetsDPCTable_variables_oid) / sizeof(oid) + 2]), newlen, vars) != SNMPERR_SUCCESS) {
				/* XXX: free, zero vars */
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;

			StorageNew = SNMP_MALLOC_STRUCT(mtpOmTransmittedOctetsDPCTable_data);

			vp = vp->next_variable;

			vp = vp->next_variable;

			/* XXX: fill in default row values here into StorageNew */

			StorageNew->mtpOmTransmittedOctetsDPCRowStatus = set_value;
			/* XXX: free, zero vars, no longer needed? */
		}

		break;

	case FREE:
		/* XXX: free, zero vars */
		/* Release any resources that have been allocated */
		break;

	case ACTION:
		/* The variable has been stored in set_value for you to use, and you have just been 
		   asked to do something with it.  Note that anything done here must be reversable
		   in the UNDO case */

		if (StorageTmp == NULL) {
			/* row creation, so add it */
			if (StorageNew != NULL)
				mtpOmTransmittedOctetsDPCTable_add(StorageNew);
			/* XXX: ack, and if it is NULL? */
		} else if (set_value != RS_DESTROY) {
			/* set the flag? */
			old_value = StorageTmp->mtpOmTransmittedOctetsDPCRowStatus;
			StorageTmp->mtpOmTransmittedOctetsDPCRowStatus = *((int *) var_val);
		} else {
			/* destroy...  extract it for now */
			hciptr = header_complex_find_entry(mtpOmTransmittedOctetsDPCTableStorage, StorageTmp);
			StorageDel = header_complex_extract_entry(&mtpOmTransmittedOctetsDPCTableStorage, hciptr);
		}
		break;

	case UNDO:
		/* Back out any changes made in the ACTION case */
		if (StorageTmp == NULL) {
			/* row creation, so remove it again */
			hciptr = header_complex_find_entry(mtpOmTransmittedOctetsDPCTableStorage, StorageTmp);
			StorageDel = header_complex_extract_entry(&mtpOmTransmittedOctetsDPCTableStorage, hciptr);
			/* XXX: free it */
		} else if (StorageDel != NULL) {
			/* row deletion, so add it again */
			mtpOmTransmittedOctetsDPCTable_add(StorageDel);
		} else {
			StorageTmp->mtpOmTransmittedOctetsDPCRowStatus = old_value;
		}
		break;

	case COMMIT:
	{
		/* Things are working well, so it's now safe to make the change permanently.  Make
		   sure that anything done here can't fail! */
		if (StorageDel != NULL) {
			StorageDel = 0;
			/* XXX: free it, its dead */
		} else {
			if (StorageTmp && StorageTmp->mtpOmTransmittedOctetsDPCRowStatus == RS_CREATEANDGO) {
				StorageTmp->mtpOmTransmittedOctetsDPCRowStatus = RS_ACTIVE;
			} else if (StorageTmp && StorageTmp->mtpOmTransmittedOctetsDPCRowStatus == RS_CREATEANDWAIT) {
				StorageTmp->mtpOmTransmittedOctetsDPCRowStatus = RS_NOTINSERVICE;
			}
		}
		break;
	}
	}
	return SNMP_ERR_NOERROR;
}

int
write_mtpOmTransmittedOctetsDpcSioRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct mtpOmTransmittedOctetsDpcSioTable_data *StorageTmp = NULL;
	static struct mtpOmTransmittedOctetsDpcSioTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - (sizeof(mtpOmTransmittedOctetsDpcSioTable_variables_oid) / sizeof(oid) + 3 - 1);
	static int old_value;
	int set_value;
	static struct variable_list *vars, *vp;
	struct header_complex_index *hciptr;

	StorageTmp = header_complex(mtpOmTransmittedOctetsDpcSioTableStorage, NULL, &name[sizeof(mtpOmTransmittedOctetsDpcSioTable_variables_oid) / sizeof(oid) + 3 - 1], &newlen, 1, NULL, NULL);

	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to mtpOmTransmittedOctetsDpcSioRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((int *) var_val);

	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY)
		return SNMP_ERR_INCONSISTENTVALUE;

	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		if (StorageTmp == NULL) {
			/* create the row now? */

			/* ditch illegal values now */
			if (set_value == RS_ACTIVE || set_value == RS_NOTINSERVICE)
				return SNMP_ERR_INCONSISTENTVALUE;

			/* destroying a non-existent row is actually legal */
			if (set_value == RS_DESTROY) {
				return SNMP_ERR_NOERROR;
			}

			/* illegal creation values */
			if (set_value == RS_ACTIVE || set_value == RS_NOTINSERVICE) {
				return SNMP_ERR_INCONSISTENTVALUE;
			}
		} else {
			/* row exists.  Check for a valid state change */
			if (set_value == RS_CREATEANDGO || set_value == RS_CREATEANDWAIT) {
				/* can't create a row that exists */
				return SNMP_ERR_INCONSISTENTVALUE;
			}
			/* XXX: interaction with row storage type needed */
		}
		break;

	case RESERVE2:
		/* memory reseveration, final preparation... */
		if (StorageTmp == NULL) {
			/* creation */
			vars = NULL;

			snmp_varlist_add_variable(&vars, NULL, 0,, NULL, 0);	/* mtpOmDpc */
			snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0);	/* mtpOmSio 
												 */

			if (header_complex_parse_oid(&(name[sizeof(mtpOmTransmittedOctetsDpcSioTable_variables_oid) / sizeof(oid) + 2]), newlen, vars) != SNMPERR_SUCCESS) {
				/* XXX: free, zero vars */
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;

			StorageNew = SNMP_MALLOC_STRUCT(mtpOmTransmittedOctetsDpcSioTable_data);

			vp = vp->next_variable;
			memdup((void *) &StorageNew->mtpOmSio, vp->val.string, vp->val_len);
			StorageNew->mtpOmSioLen = vp->val_len;
			vp = vp->next_variable;

			/* XXX: fill in default row values here into StorageNew */

			StorageNew->mtpOmTransmittedOctetsDpcSioRowStatus = set_value;
			/* XXX: free, zero vars, no longer needed? */
		}

		break;

	case FREE:
		/* XXX: free, zero vars */
		/* Release any resources that have been allocated */
		break;

	case ACTION:
		/* The variable has been stored in set_value for you to use, and you have just been 
		   asked to do something with it.  Note that anything done here must be reversable
		   in the UNDO case */

		if (StorageTmp == NULL) {
			/* row creation, so add it */
			if (StorageNew != NULL)
				mtpOmTransmittedOctetsDpcSioTable_add(StorageNew);
			/* XXX: ack, and if it is NULL? */
		} else if (set_value != RS_DESTROY) {
			/* set the flag? */
			old_value = StorageTmp->mtpOmTransmittedOctetsDpcSioRowStatus;
			StorageTmp->mtpOmTransmittedOctetsDpcSioRowStatus = *((int *) var_val);
		} else {
			/* destroy...  extract it for now */
			hciptr = header_complex_find_entry(mtpOmTransmittedOctetsDpcSioTableStorage, StorageTmp);
			StorageDel = header_complex_extract_entry(&mtpOmTransmittedOctetsDpcSioTableStorage, hciptr);
		}
		break;

	case UNDO:
		/* Back out any changes made in the ACTION case */
		if (StorageTmp == NULL) {
			/* row creation, so remove it again */
			hciptr = header_complex_find_entry(mtpOmTransmittedOctetsDpcSioTableStorage, StorageTmp);
			StorageDel = header_complex_extract_entry(&mtpOmTransmittedOctetsDpcSioTableStorage, hciptr);
			/* XXX: free it */
		} else if (StorageDel != NULL) {
			/* row deletion, so add it again */
			mtpOmTransmittedOctetsDpcSioTable_add(StorageDel);
		} else {
			StorageTmp->mtpOmTransmittedOctetsDpcSioRowStatus = old_value;
		}
		break;

	case COMMIT:
	{
		/* Things are working well, so it's now safe to make the change permanently.  Make
		   sure that anything done here can't fail! */
		if (StorageDel != NULL) {
			StorageDel = 0;
			/* XXX: free it, its dead */
		} else {
			if (StorageTmp && StorageTmp->mtpOmTransmittedOctetsDpcSioRowStatus == RS_CREATEANDGO) {
				StorageTmp->mtpOmTransmittedOctetsDpcSioRowStatus = RS_ACTIVE;
			} else if (StorageTmp && StorageTmp->mtpOmTransmittedOctetsDpcSioRowStatus == RS_CREATEANDWAIT) {
				StorageTmp->mtpOmTransmittedOctetsDpcSioRowStatus = RS_NOTINSERVICE;
			}
		}
		break;
	}
	}
	return SNMP_ERR_NOERROR;
}

int
write_mtpOmAll5And30MinSlDurationRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct mtpOmAll5And30MinSlDurationTable_data *StorageTmp = NULL;
	static struct mtpOmAll5And30MinSlDurationTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - (sizeof(mtpOmAll5And30MinSlDurationTable_variables_oid) / sizeof(oid) + 3 - 1);
	static int old_value;
	int set_value;
	static struct variable_list *vars, *vp;
	struct header_complex_index *hciptr;

	StorageTmp = header_complex(mtpOmAll5And30MinSlDurationTableStorage, NULL, &name[sizeof(mtpOmAll5And30MinSlDurationTable_variables_oid) / sizeof(oid) + 3 - 1], &newlen, 1, NULL, NULL);

	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to mtpOmAll5And30MinSlDurationRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((int *) var_val);

	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY)
		return SNMP_ERR_INCONSISTENTVALUE;

	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		if (StorageTmp == NULL) {
			/* create the row now? */

			/* ditch illegal values now */
			if (set_value == RS_ACTIVE || set_value == RS_NOTINSERVICE)
				return SNMP_ERR_INCONSISTENTVALUE;

			/* destroying a non-existent row is actually legal */
			if (set_value == RS_DESTROY) {
				return SNMP_ERR_NOERROR;
			}

			/* illegal creation values */
			if (set_value == RS_ACTIVE || set_value == RS_NOTINSERVICE) {
				return SNMP_ERR_INCONSISTENTVALUE;
			}
		} else {
			/* row exists.  Check for a valid state change */
			if (set_value == RS_CREATEANDGO || set_value == RS_CREATEANDWAIT) {
				/* can't create a row that exists */
				return SNMP_ERR_INCONSISTENTVALUE;
			}
			/* XXX: interaction with row storage type needed */
		}
		break;

	case RESERVE2:
		/* memory reseveration, final preparation... */
		if (StorageTmp == NULL) {
			/* creation */
			vars = NULL;

			if (header_complex_parse_oid(&(name[sizeof(mtpOmAll5And30MinSlDurationTable_variables_oid) / sizeof(oid) + 2]), newlen, vars) != SNMPERR_SUCCESS) {
				/* XXX: free, zero vars */
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;

			StorageNew = SNMP_MALLOC_STRUCT(mtpOmAll5And30MinSlDurationTable_data);

			/* XXX: fill in default row values here into StorageNew */

			StorageNew->mtpOmAll5And30MinSlDurationRowStatus = set_value;
			/* XXX: free, zero vars, no longer needed? */
		}

		break;

	case FREE:
		/* XXX: free, zero vars */
		/* Release any resources that have been allocated */
		break;

	case ACTION:
		/* The variable has been stored in set_value for you to use, and you have just been 
		   asked to do something with it.  Note that anything done here must be reversable
		   in the UNDO case */

		if (StorageTmp == NULL) {
			/* row creation, so add it */
			if (StorageNew != NULL)
				mtpOmAll5And30MinSlDurationTable_add(StorageNew);
			/* XXX: ack, and if it is NULL? */
		} else if (set_value != RS_DESTROY) {
			/* set the flag? */
			old_value = StorageTmp->mtpOmAll5And30MinSlDurationRowStatus;
			StorageTmp->mtpOmAll5And30MinSlDurationRowStatus = *((int *) var_val);
		} else {
			/* destroy...  extract it for now */
			hciptr = header_complex_find_entry(mtpOmAll5And30MinSlDurationTableStorage, StorageTmp);
			StorageDel = header_complex_extract_entry(&mtpOmAll5And30MinSlDurationTableStorage, hciptr);
		}
		break;

	case UNDO:
		/* Back out any changes made in the ACTION case */
		if (StorageTmp == NULL) {
			/* row creation, so remove it again */
			hciptr = header_complex_find_entry(mtpOmAll5And30MinSlDurationTableStorage, StorageTmp);
			StorageDel = header_complex_extract_entry(&mtpOmAll5And30MinSlDurationTableStorage, hciptr);
			/* XXX: free it */
		} else if (StorageDel != NULL) {
			/* row deletion, so add it again */
			mtpOmAll5And30MinSlDurationTable_add(StorageDel);
		} else {
			StorageTmp->mtpOmAll5And30MinSlDurationRowStatus = old_value;
		}
		break;

	case COMMIT:
	{
		/* Things are working well, so it's now safe to make the change permanently.  Make
		   sure that anything done here can't fail! */
		if (StorageDel != NULL) {
			StorageDel = 0;
			/* XXX: free it, its dead */
		} else {
			if (StorageTmp && StorageTmp->mtpOmAll5And30MinSlDurationRowStatus == RS_CREATEANDGO) {
				StorageTmp->mtpOmAll5And30MinSlDurationRowStatus = RS_ACTIVE;
			} else if (StorageTmp && StorageTmp->mtpOmAll5And30MinSlDurationRowStatus == RS_CREATEANDWAIT) {
				StorageTmp->mtpOmAll5And30MinSlDurationRowStatus = RS_NOTINSERVICE;
			}
		}
		break;
	}
	}
	return SNMP_ERR_NOERROR;
}

int
write_mtpOmAll30MinSlDurationRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct mtpOmAll30MinSlDurationTable_data *StorageTmp = NULL;
	static struct mtpOmAll30MinSlDurationTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - (sizeof(mtpOmAll30MinSlDurationTable_variables_oid) / sizeof(oid) + 3 - 1);
	static int old_value;
	int set_value;
	static struct variable_list *vars, *vp;
	struct header_complex_index *hciptr;

	StorageTmp = header_complex(mtpOmAll30MinSlDurationTableStorage, NULL, &name[sizeof(mtpOmAll30MinSlDurationTable_variables_oid) / sizeof(oid) + 3 - 1], &newlen, 1, NULL, NULL);

	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to mtpOmAll30MinSlDurationRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((int *) var_val);

	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY)
		return SNMP_ERR_INCONSISTENTVALUE;

	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		if (StorageTmp == NULL) {
			/* create the row now? */

			/* ditch illegal values now */
			if (set_value == RS_ACTIVE || set_value == RS_NOTINSERVICE)
				return SNMP_ERR_INCONSISTENTVALUE;

			/* destroying a non-existent row is actually legal */
			if (set_value == RS_DESTROY) {
				return SNMP_ERR_NOERROR;
			}

			/* illegal creation values */
			if (set_value == RS_ACTIVE || set_value == RS_NOTINSERVICE) {
				return SNMP_ERR_INCONSISTENTVALUE;
			}
		} else {
			/* row exists.  Check for a valid state change */
			if (set_value == RS_CREATEANDGO || set_value == RS_CREATEANDWAIT) {
				/* can't create a row that exists */
				return SNMP_ERR_INCONSISTENTVALUE;
			}
			/* XXX: interaction with row storage type needed */
		}
		break;

	case RESERVE2:
		/* memory reseveration, final preparation... */
		if (StorageTmp == NULL) {
			/* creation */
			vars = NULL;

			if (header_complex_parse_oid(&(name[sizeof(mtpOmAll30MinSlDurationTable_variables_oid) / sizeof(oid) + 2]), newlen, vars) != SNMPERR_SUCCESS) {
				/* XXX: free, zero vars */
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;

			StorageNew = SNMP_MALLOC_STRUCT(mtpOmAll30MinSlDurationTable_data);

			/* XXX: fill in default row values here into StorageNew */

			StorageNew->mtpOmAll30MinSlDurationInterval = 180000;

			StorageNew->mtpOmAll30MinSlDurationRowStatus = set_value;
			/* XXX: free, zero vars, no longer needed? */
		}

		break;

	case FREE:
		/* XXX: free, zero vars */
		/* Release any resources that have been allocated */
		break;

	case ACTION:
		/* The variable has been stored in set_value for you to use, and you have just been 
		   asked to do something with it.  Note that anything done here must be reversable
		   in the UNDO case */

		if (StorageTmp == NULL) {
			/* row creation, so add it */
			if (StorageNew != NULL)
				mtpOmAll30MinSlDurationTable_add(StorageNew);
			/* XXX: ack, and if it is NULL? */
		} else if (set_value != RS_DESTROY) {
			/* set the flag? */
			old_value = StorageTmp->mtpOmAll30MinSlDurationRowStatus;
			StorageTmp->mtpOmAll30MinSlDurationRowStatus = *((int *) var_val);
		} else {
			/* destroy...  extract it for now */
			hciptr = header_complex_find_entry(mtpOmAll30MinSlDurationTableStorage, StorageTmp);
			StorageDel = header_complex_extract_entry(&mtpOmAll30MinSlDurationTableStorage, hciptr);
		}
		break;

	case UNDO:
		/* Back out any changes made in the ACTION case */
		if (StorageTmp == NULL) {
			/* row creation, so remove it again */
			hciptr = header_complex_find_entry(mtpOmAll30MinSlDurationTableStorage, StorageTmp);
			StorageDel = header_complex_extract_entry(&mtpOmAll30MinSlDurationTableStorage, hciptr);
			/* XXX: free it */
		} else if (StorageDel != NULL) {
			/* row deletion, so add it again */
			mtpOmAll30MinSlDurationTable_add(StorageDel);
		} else {
			StorageTmp->mtpOmAll30MinSlDurationRowStatus = old_value;
		}
		break;

	case COMMIT:
	{
		/* Things are working well, so it's now safe to make the change permanently.  Make
		   sure that anything done here can't fail! */
		if (StorageDel != NULL) {
			StorageDel = 0;
			/* XXX: free it, its dead */
		} else {
			if (StorageTmp && StorageTmp->mtpOmAll30MinSlDurationRowStatus == RS_CREATEANDGO) {
				StorageTmp->mtpOmAll30MinSlDurationRowStatus = RS_ACTIVE;
			} else if (StorageTmp && StorageTmp->mtpOmAll30MinSlDurationRowStatus == RS_CREATEANDWAIT) {
				StorageTmp->mtpOmAll30MinSlDurationRowStatus = RS_NOTINSERVICE;
			}
		}
		break;
	}
	}
	return SNMP_ERR_NOERROR;
}

int
write_mtpOmAllSlUtilizationRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct mtpOmAllSlUtilizationTable_data *StorageTmp = NULL;
	static struct mtpOmAllSlUtilizationTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - (sizeof(mtpOmAllSlUtilizationTable_variables_oid) / sizeof(oid) + 3 - 1);
	static int old_value;
	int set_value;
	static struct variable_list *vars, *vp;
	struct header_complex_index *hciptr;

	StorageTmp = header_complex(mtpOmAllSlUtilizationTableStorage, NULL, &name[sizeof(mtpOmAllSlUtilizationTable_variables_oid) / sizeof(oid) + 3 - 1], &newlen, 1, NULL, NULL);

	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to mtpOmAllSlUtilizationRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((int *) var_val);

	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY)
		return SNMP_ERR_INCONSISTENTVALUE;

	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		if (StorageTmp == NULL) {
			/* create the row now? */

			/* ditch illegal values now */
			if (set_value == RS_ACTIVE || set_value == RS_NOTINSERVICE)
				return SNMP_ERR_INCONSISTENTVALUE;

			/* destroying a non-existent row is actually legal */
			if (set_value == RS_DESTROY) {
				return SNMP_ERR_NOERROR;
			}

			/* illegal creation values */
			if (set_value == RS_ACTIVE || set_value == RS_NOTINSERVICE) {
				return SNMP_ERR_INCONSISTENTVALUE;
			}
		} else {
			/* row exists.  Check for a valid state change */
			if (set_value == RS_CREATEANDGO || set_value == RS_CREATEANDWAIT) {
				/* can't create a row that exists */
				return SNMP_ERR_INCONSISTENTVALUE;
			}
			/* XXX: interaction with row storage type needed */
		}
		break;

	case RESERVE2:
		/* memory reseveration, final preparation... */
		if (StorageTmp == NULL) {
			/* creation */
			vars = NULL;

			if (header_complex_parse_oid(&(name[sizeof(mtpOmAllSlUtilizationTable_variables_oid) / sizeof(oid) + 2]), newlen, vars) != SNMPERR_SUCCESS) {
				/* XXX: free, zero vars */
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;

			StorageNew = SNMP_MALLOC_STRUCT(mtpOmAllSlUtilizationTable_data);

			/* XXX: fill in default row values here into StorageNew */

			StorageNew->mtpOmAllSlUtilizationRowStatus = set_value;
			/* XXX: free, zero vars, no longer needed? */
		}

		break;

	case FREE:
		/* XXX: free, zero vars */
		/* Release any resources that have been allocated */
		break;

	case ACTION:
		/* The variable has been stored in set_value for you to use, and you have just been 
		   asked to do something with it.  Note that anything done here must be reversable
		   in the UNDO case */

		if (StorageTmp == NULL) {
			/* row creation, so add it */
			if (StorageNew != NULL)
				mtpOmAllSlUtilizationTable_add(StorageNew);
			/* XXX: ack, and if it is NULL? */
		} else if (set_value != RS_DESTROY) {
			/* set the flag? */
			old_value = StorageTmp->mtpOmAllSlUtilizationRowStatus;
			StorageTmp->mtpOmAllSlUtilizationRowStatus = *((int *) var_val);
		} else {
			/* destroy...  extract it for now */
			hciptr = header_complex_find_entry(mtpOmAllSlUtilizationTableStorage, StorageTmp);
			StorageDel = header_complex_extract_entry(&mtpOmAllSlUtilizationTableStorage, hciptr);
		}
		break;

	case UNDO:
		/* Back out any changes made in the ACTION case */
		if (StorageTmp == NULL) {
			/* row creation, so remove it again */
			hciptr = header_complex_find_entry(mtpOmAllSlUtilizationTableStorage, StorageTmp);
			StorageDel = header_complex_extract_entry(&mtpOmAllSlUtilizationTableStorage, hciptr);
			/* XXX: free it */
		} else if (StorageDel != NULL) {
			/* row deletion, so add it again */
			mtpOmAllSlUtilizationTable_add(StorageDel);
		} else {
			StorageTmp->mtpOmAllSlUtilizationRowStatus = old_value;
		}
		break;

	case COMMIT:
	{
		/* Things are working well, so it's now safe to make the change permanently.  Make
		   sure that anything done here can't fail! */
		if (StorageDel != NULL) {
			StorageDel = 0;
			/* XXX: free it, its dead */
		} else {
			if (StorageTmp && StorageTmp->mtpOmAllSlUtilizationRowStatus == RS_CREATEANDGO) {
				StorageTmp->mtpOmAllSlUtilizationRowStatus = RS_ACTIVE;
			} else if (StorageTmp && StorageTmp->mtpOmAllSlUtilizationRowStatus == RS_CREATEANDWAIT) {
				StorageTmp->mtpOmAllSlUtilizationRowStatus = RS_NOTINSERVICE;
			}
		}
		break;
	}
	}
	return SNMP_ERR_NOERROR;
}

int
write_mtpOmSlCongestionRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct mtpOmSlCongestionTable_data *StorageTmp = NULL;
	static struct mtpOmSlCongestionTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - (sizeof(mtpOmSlCongestionTable_variables_oid) / sizeof(oid) + 3 - 1);
	static int old_value;
	int set_value;
	static struct variable_list *vars, *vp;
	struct header_complex_index *hciptr;

	StorageTmp = header_complex(mtpOmSlCongestionTableStorage, NULL, &name[sizeof(mtpOmSlCongestionTable_variables_oid) / sizeof(oid) + 3 - 1], &newlen, 1, NULL, NULL);

	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to mtpOmSlCongestionRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((int *) var_val);

	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY)
		return SNMP_ERR_INCONSISTENTVALUE;

	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		if (StorageTmp == NULL) {
			/* create the row now? */

			/* ditch illegal values now */
			if (set_value == RS_ACTIVE || set_value == RS_NOTINSERVICE)
				return SNMP_ERR_INCONSISTENTVALUE;

			/* destroying a non-existent row is actually legal */
			if (set_value == RS_DESTROY) {
				return SNMP_ERR_NOERROR;
			}

			/* illegal creation values */
			if (set_value == RS_ACTIVE || set_value == RS_NOTINSERVICE) {
				return SNMP_ERR_INCONSISTENTVALUE;
			}
		} else {
			/* row exists.  Check for a valid state change */
			if (set_value == RS_CREATEANDGO || set_value == RS_CREATEANDWAIT) {
				/* can't create a row that exists */
				return SNMP_ERR_INCONSISTENTVALUE;
			}
			/* XXX: interaction with row storage type needed */
		}
		break;

	case RESERVE2:
		/* memory reseveration, final preparation... */
		if (StorageTmp == NULL) {
			/* creation */
			vars = NULL;

			snmp_varlist_add_variable(&vars, NULL, 0,, NULL, 0);	/* mtpOmCongestionLevel 
										 */

			if (header_complex_parse_oid(&(name[sizeof(mtpOmSlCongestionTable_variables_oid) / sizeof(oid) + 2]), newlen, vars) != SNMPERR_SUCCESS) {
				/* XXX: free, zero vars */
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;

			StorageNew = SNMP_MALLOC_STRUCT(mtpOmSlCongestionTable_data);

			vp = vp->next_variable;

			/* XXX: fill in default row values here into StorageNew */

			StorageNew->mtpOmSlCongestionFirst = 2;
			StorageNew->mtpOmSlCongestionInterval = 180000;

			StorageNew->mtpOmSlCongestionRowStatus = set_value;
			/* XXX: free, zero vars, no longer needed? */
		}

		break;

	case FREE:
		/* XXX: free, zero vars */
		/* Release any resources that have been allocated */
		break;

	case ACTION:
		/* The variable has been stored in set_value for you to use, and you have just been 
		   asked to do something with it.  Note that anything done here must be reversable
		   in the UNDO case */

		if (StorageTmp == NULL) {
			/* row creation, so add it */
			if (StorageNew != NULL)
				mtpOmSlCongestionTable_add(StorageNew);
			/* XXX: ack, and if it is NULL? */
		} else if (set_value != RS_DESTROY) {
			/* set the flag? */
			old_value = StorageTmp->mtpOmSlCongestionRowStatus;
			StorageTmp->mtpOmSlCongestionRowStatus = *((int *) var_val);
		} else {
			/* destroy...  extract it for now */
			hciptr = header_complex_find_entry(mtpOmSlCongestionTableStorage, StorageTmp);
			StorageDel = header_complex_extract_entry(&mtpOmSlCongestionTableStorage, hciptr);
		}
		break;

	case UNDO:
		/* Back out any changes made in the ACTION case */
		if (StorageTmp == NULL) {
			/* row creation, so remove it again */
			hciptr = header_complex_find_entry(mtpOmSlCongestionTableStorage, StorageTmp);
			StorageDel = header_complex_extract_entry(&mtpOmSlCongestionTableStorage, hciptr);
			/* XXX: free it */
		} else if (StorageDel != NULL) {
			/* row deletion, so add it again */
			mtpOmSlCongestionTable_add(StorageDel);
		} else {
			StorageTmp->mtpOmSlCongestionRowStatus = old_value;
		}
		break;

	case COMMIT:
	{
		/* Things are working well, so it's now safe to make the change permanently.  Make
		   sure that anything done here can't fail! */
		if (StorageDel != NULL) {
			StorageDel = 0;
			/* XXX: free it, its dead */
		} else {
			if (StorageTmp && StorageTmp->mtpOmSlCongestionRowStatus == RS_CREATEANDGO) {
				StorageTmp->mtpOmSlCongestionRowStatus = RS_ACTIVE;
			} else if (StorageTmp && StorageTmp->mtpOmSlCongestionRowStatus == RS_CREATEANDWAIT) {
				StorageTmp->mtpOmSlCongestionRowStatus = RS_NOTINSERVICE;
			}
		}
		break;
	}
	}
	return SNMP_ERR_NOERROR;
}

#if !defined MODULE
static void
sa_version(int argc, char *argv[])
{
	if (!sa_output && !sa_debug)
		return;
	fprintf(stdout, "\
%2$s\n\
Copyright (c) 2001-2008  OpenSS7 Corporation.  All Rights Reserved.\n\
Distributed under Affero GPL Version 3, included here by reference.\n\
See `%1$s --copying' for copying permissions.\n\
", argv[0], ident);
}

static void
sa_usage(int argc, char *argv[])
{
	if (!sa_output && !sa_debug)
		return;
	fprintf(stderr, "\
Usage:\n\
    %1$s [general-options] [options] [arguments]\n\
    %1$s {-H|--help-directives}\n\
    %1$s {-h|--help}\n\
    %1$s {-V|--version}\n\
    %1$s {-C|--copying}\n\
", argv[0]);
}

static void
sa_help(int argc, char *argv[])
{
	if (!sa_output && !sa_debug)
		return;
	fprintf(stdout, "\
Usage:\n\
    %1$s [general-options] [options] [arguments]\n\
    %1$s {-h|--help}\n\
    %1$s {-V|--version}\n\
    %1$s {-C|--copying}\n\
Arguments:\n\
    None.\n\
Options:\n\
    -a, --log-addresses\n\
        log addresses of connecting management stations.\n\
    -A, --append\n\
        append to logfiles without truncating.\n\
    -c, --config-file CONFIGFILE\n\
        use configuration file CONFIGFILE.\n\
    -C, --config-only\n\
        only load configuration given by -c option.\n\
    -d, --dump\n\
        dump sent and received PDUs.\n\
    -D, --debug [LEVEL]\n\
        set debugging verbosity to LEVEL.\n\
    -D, --debug-tokens [TOKEN[,TOKEN]*]\n\
        debug specified TOKEN's.\n\
    -f, --dont-fork\n\
        run in the foreground.\n\
    -g, --gid, --groupid GID\n\
        become group GID after listening.\n\
    -h, --help, -?, --?\n\
        print usage information and exit.\n\
    -H, --help-directives\n\
        print config directives and exit.\n\
    -I, --initialize [-]MODULE[,MODULE]*\n\
        initialize (or not, '-') these MODULE's.\n\
    -k, --keep-open\n\
        keep system files open between requests.\n\
    -l, --log-file [LOGFILE]\n\
        log to log file name LOGFILE.  [default: /var/log/mtpommib.log]\n\
    -L, --log-stderr\n\
        log to controlling terminal standard error.\n\
    -m, --mibs [+]MIB[,MIB]*\n\
        load these (additional '+') MIBs.\n\
    -M, --master\n\
        run as SNMP master instead of AgentX sub-agent.\n\
    -M, --mibdirs [+]MIBDIR[:MIBDIR]*\n\
        search these (additional, '+') colon separated directories for MIBs.\n\
    -n, --nodaemon\n\
        run in the foreground.\n\
    -n, --name NAME\n\
        use NAME for configuration file base.  [default: mtpommib]\n\
    -p, --port PORTNUM\n\
        listen on port number PORTNUM.  [default: 161]\n\
    -p, --pidfile PIDFILE\n\
        write daemon pid to PIDFILE.  [default: /var/run/mtpommib.pid]\n\
    -P, --pidfile PIDFILE\n\
        write daemon pid to PIDFILE.  [default: /var/run/mtpommib.pid]\n\
    -q, --quiet\n\
        suppress normal output.\n\
    -q, --quick\n\
        abbreviate output for machine readability.\n\
    -r, --noroot\n\
        do not require root privilege.\n\
    -s, --log-syslog\n\
        log to system logs.\n\
    -S, --sysctl-file FILENAME\n\
        write sysctl config file FILENAME.  [default: /etc/streams.conf]\n\
    -t, --agent-alarms\n\
        agent blocks {SIGALARM}.\n\
    -T, --transport [TRANSPORT]\n\
        default transport TRANSPORT.  [default: udp]\n\
    -u, --uid, --userid UID\n\
        become user UID after listening.\n\
    -U, --dont-remove-pidfile\n\
        do not remove PIDFILE when shutting down.\n\
    -v, --version\n\
        print version information and exit.\n\
    -V, --verbose [LEVEL]\n\
        be verbose to LEVEL.  [default: 1]\n\
    -x, --agentx-socket [SOCKET]\n\
        master AgentX on SOCKET.  [default: /var/agentx/master]\n\
    -X, --agentx\n\
        run as AgentX sub-agent instead of master (the default).\n\
    -y, --copying\n\
        print copying information and exit.\n\
", argv[0]);
}

static void
sa_copying(int argc, char *argv[])
{
	if (!sa_output && !sa_debug)
		return;
	fprintf(stdout, "\
--------------------------------------------------------------------------------\n\
%1$s\n\
--------------------------------------------------------------------------------\n\
Copyright (c) 2001-2008  OpenSS7 Corporation <http://www.openss7.com>\n\
Copyright (c) 1997-2000  Brian F. G. Bidulock <bidulock@openss7.org>\n\
\n\
All Rights Reserved.\n\
--------------------------------------------------------------------------------\n\
This program is free software; you can  redistribute  it and/or modify  it under\n\
the terms of the GNU Affero General Public License as published by the Free\n\
Software Foundation; Version 3 of the License.\n\
\n\
This program is distributed in the hope that it will  be useful, but WITHOUT ANY\n\
WARRANTY; without even  the implied warranty of MERCHANTABILITY or FITNESS FOR A\n\
PARTICULAR PURPOSE.  See the GNU Affero General Public License for more details.\n\
\n\
You should have received a copy of the GNU  Affero  General Public License along\n\
with this program.   If not, see <http://www.gnu.org/licenses/>, or write to the\n\
Free Software Foundation, Inc., 675 Mass Ave, Cambridge, MA 02139, USA.\n\
--------------------------------------------------------------------------------\n\
U.S. GOVERNMENT RESTRICTED RIGHTS.  If you are licensing this Software on behalf\n\
of the U.S. Government (\"Government\"), the following provisions apply to you. If\n\
the Software is supplied by the  Department of Defense (\"DoD\"), it is classified\n\
as \"Commercial  Computer  Software\"  under  paragraph  252.227-7014  of the  DoD\n\
Supplement  to the  Federal Acquisition Regulations  (\"DFARS\") (or any successor\n\
regulations) and the  Government  is acquiring  only the  license rights granted\n\
herein (the license rights customarily provided to non-Government users). If the\n\
Software is supplied to any unit or agency of the Government  other than DoD, it\n\
is  classified as  \"Restricted Computer Software\" and the Government's rights in\n\
the Software  are defined  in  paragraph 52.227-19  of the  Federal  Acquisition\n\
Regulations (\"FAR\")  (or any successor regulations) or, in the cases of NASA, in\n\
paragraph  18.52.227-86 of  the  NASA  Supplement  to the FAR (or any  successor\n\
regulations).\n\
--------------------------------------------------------------------------------\n\
Commercial  licensing  and  support of this  software is  available from OpenSS7\n\
Corporation at a fee.  See http://www.openss7.com/\n\
--------------------------------------------------------------------------------\n\
", ident);
}

void
sa_help_directives(int argc, char *argv[])
{
	ds_set_boolean(DS_APPLICATION_ID, DS_AGENT_NO_ROOT_ACCESS, 1);
	init_agent("mtpOmMIB");
	// init_mib_modules();
	init_mib();
	init_snmp("mtpOmMIB");
	snmp_log(MY_FACILITY(LOG_INFO), "Configuration directives understood:\n");
	/* Unfortunately, read_config_print_usage() uses snmp_log(), meaning that it can only be
	   writen to standard error and not standard output. */
	read_config_print_usage("    ");
}

static int
sa_sig_register(int signum, RETSIGTYPE(*handler) (int))
{
	sigset_t mask;
	struct sigaction act;

	act.sa_handler = handler ? handler : SIG_DFL;
	act.sa_flags = handler ? SA_RESTART : 0;
	sigemptyset(&act.sa_mask);
	if (sigaction(signum, &act, NULL))
		return (-1);
	sigemptyset(&mask);
	sigaddset(&mask, signum);
	sigprocmask(handler ? SIG_UNBLOCK : SIG_BLOCK, &mask, NULL);
	return (0);
}

static int sa_alm_signal = 0;
static int sa_pol_signal = 0;
static int sa_hup_signal = 0;
static int sa_int_signal = 0;
static int sa_trm_signal = 0;

static int sa_alm_handle = 0;

void
sa_alm_callback(unsigned int req, void *arg)
{
	if (req == sa_alm_handle)
		sa_alm_handle = 0;
	sa_alm_signal = 1;
	return;
}

static RETSIGTYPE
sa_alm_handler(int signum)
{
	sa_alm_signal = 1;
	return (RETSIGTYPE) (0);
}

static void
sa_snmp_alm_handler(unsigned int reg, void *clientarg)
{
	sa_alm_signal = 1;
	return;
}

static int
sa_alm_catch(void)
{
	if (sa_alarms)
		return sa_sig_register(SIGALRM, &sa_alm_handler);
	return (-1);
}

static int
sa_alm_block(void)
{
	if (sa_alarms)
		return sa_sig_register(SIGALRM, NULL);
	if (sa_alm_handle) {
		unsigned int handle = sa_alm_handle;

		sa_alm_handle = 0;
		snmp_alarm_unregister(handle);
	}
	return (0);
}

static int
sa_alm_action(void)
{
	sa_alm_signal = 0;
	return (0);
}

static RETSIGTYPE
sa_pol_handler(int signum)
{
	sa_pol_signal = 1;
	return (RETSIGTYPE) (0);
}

static int
sa_pol_catch(void)
{
	return sa_sig_register(SIGPOLL, &sa_pol_handler);
}

static int
sa_pol_block(void)
{
	return sa_sig_register(SIGPOLL, NULL);
}

/*
 * Both the sc(4) module and sad(4) driver issue an M_PCSIG message with
 * SIGPOLL to the stream head whenever the STREAMS configuration or autopush
 * configuration changes, indicating to the agent which has the sc(4) or
 * sad(4) Stream open that it is necessary to reread information from the
 * kernel.  This fact is merely recorded, as this information is not read each
 * time that a configuration change occurs, but only after a request from some
 * portion of that information occurs. This condition is also set when the
 * sc(4) and sad(4) Streams are first opened. The SIGPOLL will also deliver in
 * siginfo the file descriptor issuing the signal, so we could distiguish
 * between sc(4) and sad(4) signals, but since one can be pushed over the
 * other, there is little point in distinguishing.
 *
 * sc(4) or sad(4) also should be modified to provide the general streams
 * statistics supported here; even though they are available through the /proc
 * filesystem on Linux Fast-STREAMS.
 */
static int
sa_pol_action(void)
{
	sa_pol_signal = 0;
	snmp_log(MY_FACILITY(LOG_INFO), "%s: Caught SIGPOLL, will re-read data structures", sa_program);
	sa_changed = 1;
	return (0);
}

static RETSIGTYPE
sa_hup_handler(int signum)
{
	sa_hup_signal = 1;
	return (RETSIGTYPE) (0);
}

static int
sa_hup_catch(void)
{
	if (sa_agentx)
		return sa_sig_register(SIGHUP, &sa_hup_handler);
	return (-1);
}

static int
sa_hup_block(void)
{
	return sa_sig_register(SIGHUP, NULL);
}

static int
sa_hup_action(void)
{
	/* There are several times that we might be sent a SIGHUP.  We might be sent a SIGHUP by
	   logrotate asking us to close and reopen our log files. */
	sa_hup_signal = 0;
	snmp_log(MY_FACILITY(LOG_WARNING), "Caught SIGHUP, reopening files.");
	if (sa_output > 1)
		snmp_log(MY_FACILITY(LOG_NOTICE), "Reopening output file %s", sa_logfile);
	if (sa_logfillog != 0) {
		fflush(stdlog);
		fclose(stdlog);
		snmp_disable_filelog();
		if ((stdlog = freopen(sa_logfile, sa_appendlog ? "a" : "w", stdlog)) == NULL) {
			/* I hope we have another log sink. */
			snmp_log(MY_FACILITY(LOG_ERR), "%s", strerror(errno));
			snmp_log(MY_FACILITY(LOG_ERR), "Could not reopen log file %s", sa_logfile);
		}
		snmp_enable_filelog(sa_logfile, sa_appendlog);
	}
	return (0);
}

static RETSIGTYPE
sa_int_handler(int signum)
{
	sa_int_signal = 1;
	return (RETSIGTYPE) (0);
}

static int
sa_int_catch(void)
{
	return sa_sig_register(SIGINT, &sa_int_handler);
}

static int
sa_int_block(void)
{
	return sa_sig_register(SIGINT, NULL);
}

static void sa_exit(int retval);

static int
sa_int_action(void)
{
	sa_int_signal = 0;
	snmp_log(MY_FACILITY(LOG_WARNING), "%s: Caught SIGINT, shutting down", sa_program);
	sa_exit(0);
	return (0);		/* should be no return */
}

static RETSIGTYPE
sa_trm_handler(int signum)
{
	sa_trm_signal = 1;
	return (RETSIGTYPE) (0);
}

static int
sa_trm_catch(void)
{
	return sa_sig_register(SIGTERM, &sa_trm_handler);
}

static int
sa_trm_block(void)
{
	return sa_sig_register(SIGTERM, NULL);
}

static void sa_exit(int retval);

static int
sa_trm_action(void)
{
	sa_trm_signal = 0;
	snmp_log(MY_FACILITY(LOG_WARNING), "%s: Caught SIGTERM, shutting down", sa_program);
	sa_exit(0);
	return (0);		/* should be no return */
}

static void
sa_sig_catch(void)
{
	sa_alm_catch();
	sa_pol_catch();
	sa_hup_catch();
	sa_int_catch();
	sa_trm_catch();
}

static void
sa_sig_block(void)
{
	sa_alm_block();
	sa_pol_block();
	sa_hup_block();
	sa_int_block();
	sa_trm_block();
}

int
sa_start_timer(long duration)
{
	if (sa_alarms) {
		struct itimerval setting = {
			{0, 0},
			{duration / 1000, (duration % 1000) * 1000}
		};

		if (sa_alm_catch())
			return (-1);
		if (setitimer(ITIMER_REAL, &setting, NULL))
			return (-1);
		sa_alm_signal = 0;
		return (0);
	} else {
#if defined NETSNMP_DS_APPLICATION_ID
		struct timeval setting = {
			duration / 1000, (duration % 1000) * 1000
		};

		sa_alm_handle = snmp_alarm_register_hr(setting, 0, sa_snmp_alm_handler, NULL);
#else
		sa_alm_handle = snmp_alarm_register((duration + 999) / 1000, 0, sa_snmp_alm_handler, NULL);
#endif
		return (sa_alm_handle ? 0 : -1);
	}
}

static void
sa_exit(int retval)
{
	if (retval)
		snmp_log(MY_FACILITY(LOG_ERR), "%s: Exiting %d", sa_program, retval);
	else
		snmp_log(MY_FACILITY(LOG_NOTICE), "%s: Exiting %d", sa_program, retval);
	fflush(stdout);
	fflush(stderr);
	sa_sig_block();
	closelog();
	exit(retval);
}

static void
sa_init_logging(int argc, char *argv[])
{
	static char progname[256];

	/* The purpose of this function is to bring logging up before forking (and while still in
	   the foreground) so that we can use the snmp_log() function before and during forking if
	   necessary.  Note that the default configuration for snmp_log() is to send all logs to
	   standard error. */
	strncpy(progname, basename(argv[0]), sizeof(progname));

	snmp_disable_log();
	if (sa_logfillog) {
		snmp_enable_filelog(sa_logfile, sa_appendlog);
	}
	if (sa_logstderr | sa_logstdout) {
#if defined LOG_PERROR
		/* Note that when we have Linux LOG_PERROR, and logs go both to syslog and stderr,
		   it is better to use the LOG_PERROR than to use snmp_log()'s print to stderr, as
		   the former is better formated. */
		if (!sa_logsyslog)
			snmp_enable_stderrlog();
#else				/* defined LOG_PERROR */
		snmp_enable_stderrlog();
#endif				/* defined LOG_PERROR */
	}
	if (sa_logsyslog) {
#if !defined HAVE_SNMP_ENABLE_SYSLOG_IDENT
		snmp_enable_syslog();
#else				/* !defined HAVE_SNMP_ENABLE_SYSLOG_IDENT */
		snmp_enable_syslog_ident("mtpOmMIB", LOG_DAEMON);
#endif				/* !defined HAVE_SNMP_ENABLE_SYSLOG_IDENT */
		/* Note that the way that snmp sets up the logger is not really the way we want it,
		   so close the log and reopen it the way we want. */
		closelog();
#if defined LOG_PERROR
		openlog("mtpOmMIB", LOG_PID | LOG_CONS | LOG_NDELAY | (sa_logstderr ? LOG_PERROR : 0), MY_FACILITY(0));
#else				/* defined LOG_PERROR */
		openlog("mtpOmMIB", LOG_PID | LOG_CONS | LOG_NDELAY, MY_FACILITY(0));
#endif				/* defined LOG_PERROR */
	}
	if (sa_logcallog) {
		snmp_enable_calllog();
	}
}

static void
sa_enter(int argc, char *argv[])
{
	if (sa_nomead) {
		pid_t pid;

		if ((pid = fork()) < 0) {
			perror(argv[0]);
			exit(2);
		} else if (pid != 0) {
			/* parent exits */
			exit(0);
		}
		setsid();	/* become a session leader */
		/* fork once more for SVR4 */
		if ((pid = fork()) < 0) {
			perror(argv[0]);
			exit(2);
		} else if (pid != 0) {
			/* parent responsible for writing pid file */
			if (sa_nomead || sa_pidfile[0] != '\0') {
				FILE *pidf;

				/* initialize default filename */
				if (sa_pidfile[0] == '\0')
					snprintf(sa_pidfile, sizeof(sa_pidfile), "/var/run/%s.pid", sa_program);
				if (sa_output > 1) {
					snmp_log(MY_FACILITY(LOG_NOTICE), "%s: Writing daemon pid to file %s", sa_program, sa_pidfile);
				}
				if ((pidf = fopen(sa_pidfile, "w+"))) {
					fprintf(pidf, "%d", (int) pid);
					fflush(pidf);
					fclose(pidf);
				} else {
					snmp_log(MY_FACILITY(LOG_ERR), "%s: %m", sa_program);
					snmp_log(MY_FACILITY(LOG_ERR), "%s: Could not write pid to file %s", sa_program, sa_pidfile);
					sa_exit(2);
					/* no return */
				}
			}
			/* parent exits */
			exit(0);
		}
		/* child continues */
		/* release current directory */
		if (chdir("/") < 0) {
			perror(argv[0]);
			exit(2);
		}
		umask(0);	/* clear file creation mask */
		/* rearrange file streams */
		fclose(stdin);
	}
	/* continue as foreground or background */
	sa_init_logging(argc, argv);
	sa_sig_catch();
	snmp_log(MY_FACILITY(LOG_NOTICE), "%s: Startup complete.", sa_program);
}

static void
sa_mloop(int argc, char *argv[])
{
	if (sa_agentx) {
		if (sa_debug)
			snmp_log(MY_FACILITY(LOG_DEBUG), "%s: running as AgentX client\n", argv[0]);
		/* run as an AgentX client */
		ds_set_boolean(DS_APPLICATION_ID, DS_AGENT_ROLE, 1);
	} else {
		if (sa_debug)
			snmp_log(MY_FACILITY(LOG_DEBUG), "%s: running as SNMP master agent\n", argv[0]);
		/* run as SNMP master */
		ds_set_boolean(DS_APPLICATION_ID, DS_AGENT_ROLE, 0);
	}

	if (sa_alarms) {
		if (sa_debug)
			snmp_log(MY_FACILITY(LOG_DEBUG), "%s: using application alarms\n", argv[0]);
		/* use application alarms */
		ds_set_boolean(DS_LIBRARY_ID, DS_LIB_ALARM_DONT_USE_SIG, 1);
	}

	/* initialize agent */
	init_agent("mtpOmMIB");

	/* initialize MIB */
	init_mtpOmMIB();

	/* initialize SNMP */
	init_snmp("mtpOmMIB");

	if (!sa_agentx) {
		if (sa_debug)
			snmp_log(MY_FACILITY(LOG_DEBUG), "%s: running as SNMP master\n", argv[0]);
#if !defined NETSNMP_DS_APPLICATION_ID
		init_master_agent(710, NULL, NULL);
#else
		init_master_agent();
#endif
	}

	for (;;) {
		int retval;

		/* to use select or poll you need to use the snmp_select_info() to obtain the fd of 
		   the agentx socket and add it to the fdset. */
		/* note that SIGALRM is used by snmp: use the snmp_alarm() api instead */

#if 0
		if (snmp_select() == 0) {
			if (sa_alarms == 0)
				run_alarms();
		}
#endif
		retval = agent_check_and_process(1);	/* 0 == don't block */

		if (retval == 0) {
			/* alarm occurred, alarm conditions checked */
		} else if (retval == -1) {
			/* error (or signal) ocurred */
			if (sa_alm_signal) {
				sa_alm_action();
			}
			if (sa_pol_signal) {
				sa_pol_action();
			}
			if (sa_hup_signal) {
				sa_hup_action();
			}
			if (sa_int_signal) {
				if (sa_debug)
					snmp_log(MY_FACILITY(LOG_DEBUG), "%s: shutting down\n", argv[0]);
				snmp_shutdown("mtpOmMIB");
				sa_int_action();	/* no return */
			}
			if (sa_trm_signal) {
				if (sa_debug)
					snmp_log(MY_FACILITY(LOG_DEBUG), "%s: shutting down\n", argv[0]);
				snmp_shutdown("mtpOmMIB");
				sa_trm_action();	/* no return */
			}
		} else if (retval > 0) {
			/* processed packets */
			if (sa_fclose) {
				/* close files after each request */
				if (sa_fd != 0) {
					int fd = sa_fd;

					sa_fd = 0;
					close(fd);
				}
			}
			sa_stats_refresh = 1;
			sa_request++;
		}
	}

	if (sa_debug)
		snmp_log(MY_FACILITY(LOG_DEBUG), "%s: shutting down\n", argv[0]);
	snmp_shutdown("mtpOmMIB");
}

int
main(int argc, char *argv[])
{
	for (;;) {
		int c, val, fd;
		char *cptr;
		struct passwd *pw;
		struct group *gr;
		struct stat st;

#if defined _GNU_SOURCE
		int option_index = 0;
                /* *INDENT-OFF* */
                static struct option long_options[] = {
                        {"log-addresses",	no_argument,		NULL, 'a'},
                        {"append",		no_argument,		NULL, 'A'},
                        {"config-file",		required_argument,	NULL, 'c'},
                        {"no-configs",		no_argument,		NULL, 'C'},
                        {"dump",		no_argument,		NULL, 'd'},
                        {"debug",		optional_argument,	NULL, 'D'},
                        {"debug-tokens",	optional_argument,	NULL, 'D'},
                        {"dont-fork",		no_argument,		NULL, 'f'},
                        {"gid",			required_argument,	NULL, 'g'},
                        {"groupid",		required_argument,	NULL, 'g'},
                        {"help",		no_argument,		NULL, 'h'},
                        {"?",			no_argument,		NULL, 'h'},
                        {"help-directives",	no_argument,		NULL, 'H'},
                        {"initialize",		required_argument,	NULL, 'I'},
                        {"init-modules",	required_argument,	NULL, 'I'},
                        {"keep-open",		no_argument,		NULL, 'k'},
                        {"log-file",		optional_argument,	NULL, 'l'},
                        {"logfile",		optional_argument,	NULL, 'l'},
                        {"Lf",			optional_argument,	NULL, 'l'},
                        {"LF",			required_argument,	NULL, 'l'},
                        {"log-stderr",		no_argument,		NULL, 'L'},
                        {"Le",			no_argument,		NULL, 'L'},
                        {"LE",			required_argument,	NULL, 'L'},
                        {"mibs",		required_argument,	NULL, 'm'},
                        {"master",		no_argument,		NULL, 'M'},
                        {"mibdirs",		required_argument,	NULL, 'M'},
                        {"nodaemon",		no_argument,		NULL, 'n'},
                        {"name",		required_argument,	NULL, 'n'},
                        {"dry-run",		no_argument,		NULL, 'N'},
                        {"log-stdout",		no_argument,		NULL, 'o'},
                        {"Lo",			no_argument,		NULL, 'o'},
                        {"LO",			required_argument,	NULL, 'o'},
                        {"port",		required_argument,	NULL, 'p'},
                        {"pidfile",		required_argument,	NULL, 'P'},
                        {"quiet",		no_argument,		NULL, 'q'},
                        {"quick",		no_argument,		NULL, 'q'},
                        {"noroot",		no_argument,		NULL, 'r'},
                        {"log-syslog",		no_argument,		NULL, 's'},
                        {"Ls",			no_argument,		NULL, 's'},
                        {"LS",			required_argument,	NULL, 's'},
                        {"syslog",		no_argument,		NULL, 's'},
                        {"sysctl-file",		required_argument,	NULL, 'S'},
                        {"agent-alarms",	no_argument,		NULL, 't'},
                        {"transport",		optional_argument,	NULL, 'T'},
                        {"uid",			required_argument,	NULL, 'u'},
                        {"userid",		required_argument,	NULL, 'u'},
                        {"dont-remove-pidfile",	no_argument,		NULL, 'U'},
                        {"leave-pidfile",	no_argument,		NULL, 'U'},
                        {"version",		no_argument,		NULL, 'v'},
                        {"verbose",		optional_argument,	NULL, 'V'},
                        {"agentx-socket",	required_argument,	NULL, 'x'},
                        {"agentx",		no_argument,		NULL, 'X'},
                        {"copying",		no_argument,		NULL, 'y'},
#if 0
                        {"directory",		required_argument,	NULL, 'd'},
                        {"basename",		required_argument,	NULL, 'b'},
                        {"outfile",		required_argument,	NULL, 'o'},
                        {"errfile",		required_argument,	NULL, 'e'},
#endif
                        { 0, }
                };
                /* *INDENT-ON* */

		c = getopt_long_only(argc, argv, ":aAc:CdD::fg:hHI:kl::L::m:M::n::o::p:P:qrs::S:tT::u:UvV::x:Xy", long_options, &option_index);
#else				/* defined _GNU_SOURCE */
		c = getopt(argc, argv, ":aAc:CdD::fg:hHI:kl::L::m:M::n::o::p:P:qrs::S:tT::u:UvV::x:Xy");
#endif				/* defined _GNU_SOURCE */
		if (c == -1) {
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: done options processing\n", argv[0]);
			break;
		}
		switch (c) {
		case 0:
			goto bad_usage;
		case 'a':	/* -a, --log-addresses */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: logging addresses\n", argv[0]);
			sa_logaddr++;
			break;
		case 'A':	/* -A, --append */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: will not truncate logfile\n", argv[0]);
#if defined NETSNMP_DS_LIB_APPEND_LOGFILES
			ds_set_boolean(DS_LIBRARY_ID, NETSNMP_DS_LIB_APPEND_LOGFILES, 1);
#endif				/* defined NETSNMP_DS_LIB_APPEND_LOGFILES */
			sa_appendlog = 1;
			break;
		case 'c':	/* -c, --config-file CONFIGFILE */
			if (optarg == NULL)
				goto bad_option;
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: using configuration file %s\n", argv[0], optarg);
			ds_set_string(DS_LIBRARY_ID, DS_LIB_OPTIONALCONFIG, optarg);
			break;
		case 'C':	/* -C, --no-configs */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: not reading default config files\n", argv[0]);
			ds_set_boolean(DS_LIBRARY_ID, DS_LIB_DONT_READ_CONFIGS, 1);
			break;
		case 'd':	/* -d, --dump */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: setting packet dump\n", argv[0]);
			sa_dump = 1;
			// snmp_set_dump_packet(sa_dump);
			ds_set_boolean(DS_LIBRARY_ID, DS_LIB_DUMP_PACKET, sa_dump);
			ds_set_boolean(DS_APPLICATION_ID, DS_AGENT_VERBOSE, sa_dump);
			break;
		case 'D':	/* -D, --debug [LEVEL], --debug-tokens [TOKENS] */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: increasing debug verbosity\n", argv[0]);
			if (optarg == NULL) {
				/* no option: must be -D, --debug */
				sa_debug++;
				if (sa_debug)
					snmp_log(MY_FACILITY(LOG_DEBUG), "%s: debug level is now %d\n", argv[0], sa_debug);
				if (sa_debug)
					snmp_log(MY_FACILITY(LOG_DEBUG), "%s: debugging all tokens\n", argv[0]);
				if (sa_debug)
					debug_register_tokens("ALL");
			} else {
				cptr = optarg;
				if ((val = strtol(optarg, &cptr, 0)) < 0)
					goto bad_option;
				if (*cptr == '\0') {
					/* it is just a number, must be -D, --debug [LEVEL] */
					sa_debug = val;
					if (sa_debug)
						snmp_log(MY_FACILITY(LOG_DEBUG), "%s: debug level is now %d\n", argv[0], sa_debug);
				} else {
					/* not a number, must be -D, --debug-tokens TOKENS */
					if (sa_debug)
						snmp_log(MY_FACILITY(LOG_DEBUG), "%s: debugging tokens %s\n", argv[0], optarg);
					debug_register_tokens(optarg);
				}
			}
			break;
		case 'f':	/* -f, --dont-fork */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: suppressing daemon mode\n", argv[0]);
			sa_nomead = 0;
			break;
		case 'u':	/* -u, --uid, --userid UID */
			cptr = optarg;
			if ((val = strtol(optarg, &cptr, 0)) < 0)
				goto bad_option;
			/* UID can be name or number */
			if ((pw = (*cptr == '\0') ? getpwuid((uid_t) val) : getpwnam(optarg)) == NULL)
				goto bad_option;
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: will run as uid %s(%d)\n", argv[0], pw->pw_name, pw->pw_uid);
			ds_set_int(DS_APPLICATION_ID, DS_AGENT_USERID, pw->pw_uid);
			break;
		case 'g':	/* -g, --gid, --groupdid GID */
			cptr = optarg;
			if ((val = strtol(optarg, &cptr, 0)) < 0)
				goto bad_option;
			/* GID can be name or number */
			if ((gr = (*cptr == '\0') ? getgrgid((gid_t) val) : getgrnam(optarg)) == NULL)
				goto bad_option;
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: will run as gid %s(%d)\n", argv[0], gr->gr_name, gr->gr_gid);
			ds_set_int(DS_APPLICATION_ID, DS_AGENT_GROUPID, gr->gr_gid);
			break;
		case 'h':	/* -h, --help, -?, --? */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: printing help message\n", argv[0]);
			sa_help(argc, argv);
			exit(0);
		case 'H':	/* -H, --help-directives */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: printing config directives\n", argv[0]);
			sa_help_directives(argc, argv);
			exit(0);
		case 'I':	/* -I, --init-modules, --initialize MODULE[{,| |:}MODULE]* */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: will initialize modules: %s\n", argv[0], optarg);
			add_to_init_list(optarg);
			break;
		case 'k':	/* -k, --keep-open */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: keeping files open\n", argv[0]);
			sa_fclose = 0;
			break;
		case 'l':	/* -l, --log-file, --logfile, -Lf, -LF p1[-p2] [LOGFILE] */
			if (optarg != NULL)
				strncpy(sa_logfile, optarg, sizeof(sa_logfile));
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: will log to file %s\n", argv[0], sa_logfile);
			sa_logfillog = 1;
			break;
		case 'L':	/* -L, --log-stderr, -Le, -LE p1[-p2] */
			/* Note that the recent NET-SNMP version of this option is far more
			   complicated: -Le is the same as the old version of the option; -Lf
			   LOGFILE is like the -l option; -Ls is like the -s option; -Lo logs
			   messages to standard output; -LX p1[-p2] [LOGFILE], where X = E, F, S or 
			   O, logs priority p1 and above to X, or p1 thru p2 to X. */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: logging to standard error\n", argv[0]);
			sa_logstderr = 1;
			break;
		case 'm':	/* -m, --mibs MIBS */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: using MIBS %s\n", argv[0], optarg);
			break;
		case 'M':	/* -M, --master or -M, --mibdirs MIBDIRS */
			if (optarg) {
				/* -M, --mibdirs MIBDIRS */
				if (sa_debug)
					snmp_log(MY_FACILITY(LOG_DEBUG), "%s: using MIBDIRS %s\n", argv[0], optarg);
			} else {
				/* -M, --master */
				if (sa_debug)
					snmp_log(MY_FACILITY(LOG_DEBUG), "%s: setting SNMP master\n", argv[0]);
				sa_agentx = 0;
				ds_set_boolean(DS_APPLICATION_ID, DS_AGENT_ROLE, 0);
			}
			break;
		case 'n':	/* -n, --nodaemon or -n, --name NAME */
			if (optarg) {
				/* -n, --name NAME */
				if (sa_debug)
					snmp_log(MY_FACILITY(LOG_DEBUG), "%s: using name %s\n", argv[0], optarg);
				ds_set_string(DS_APPLICATION_ID, DS_AGENT_PROGNAME, optarg);
			} else {
				/* -n, --nodaemon */
				if (sa_debug)
					snmp_log(MY_FACILITY(LOG_DEBUG), "%s: suppressing deamon mode\n", argv[0]);
				sa_nomead = 0;
				ds_set_string(DS_APPLICATION_ID, DS_AGENT_PROGNAME, basename(argv[0]));
			}
			break;
		case 'N':	/* -N, --dry-run */
#if defined NETSNMP_DS_AGENT_QUIT_IMMEDIATELY
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: setting for dry-runs startup\n", argv[0]);
			ds_set_boolean(DS_APPLICATION_ID, NETSNMP_DS_AGENT_QUIT_IMMEDIATELY, 1);
			break;
#else				/* defined NETSNMP_DS_AGENT_QUIT_IMMEDIATELY */
			snmp_log(MY_FACILITY(LOG_DEBUG), "%s: -N option not supported\n", argv[0]);
			goto bad_option;
#endif				/* defined NETSNMP_DS_AGENT_QUIT_IMMEDIATELY */
		case 'o':	/* -o, --log-stdout, -Lo, -LO p1[-p2] */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: logging to stdout\n", argv[0]);
			sa_logstdout = 1;
			break;
		case 'p':	/* -p, --port PORTNUM or -p, --pidfile PIDFILE */
			cptr = optarg;
			if ((val = strtol(optarg, &cptr, 0)) < 0 || val > 16383)
				goto bad_option;
			if (*cptr == '\0') {
				char buf[4096];

				/* -p, --port PORTNUM */
				if ((cptr = ds_get_string(DS_APPLICATION_ID, DS_AGENT_PORTS)))
					snprintf(buf, sizeof(buf), "%s,%s", cptr, optarg);
				else
					strncpy(buf, optarg, sizeof(buf));
				ds_set_string(DS_APPLICATION_ID, DS_AGENT_PORTS, buf);
				break;
			}
			/* fall through */
		case 'P':	/* -p, -P, --pidfile PIDFILE */
			if (optarg) {
				/* either it exists */
				if (stat(optarg, &st) == -1) {
					/* or we can create it */
					if ((fd = open(optarg, O_CREAT, 0600)) == -1) {
						perror(argv[0]);
						goto bad_option;
					}
					close(fd);
				}
				if (sa_debug)
					snmp_log(MY_FACILITY(LOG_DEBUG), "%s: setting pid file to %s\n", argv[0], optarg);
				strncpy(sa_pidfile, optarg, sizeof(sa_pidfile));
			}
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: using pidfile %s\n", argv[0], sa_pidfile);
			break;
		case 'q':	/* -q, --quiet, --quick */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: suppressing normal output\n", argv[0]);
			sa_debug = 0;
			sa_output = 0;
			ds_set_boolean(DS_APPLICATION_ID, DS_AGENT_VERBOSE, 0);
			// snmp_set_quick_print();
			ds_set_boolean(DS_LIBRARY_ID, DS_LIB_QUICK_PRINT, 1);
			break;
		case 'r':	/* -r, --noroot */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: setting for non-root access\n", argv[0]);
			ds_set_boolean(DS_APPLICATION_ID, DS_AGENT_NO_ROOT_ACCESS, 1);
			break;
		case 's':	/* -s, --log-syslog, -Ls, -LS p1[-p2] */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: logging to system logs\n", argv[0]);
			sa_logsyslog = 1;
			break;
		case 'S':	/* -S, -sysctl-file FILENAME */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: using %s for backing\n", argv[0], optarg);
			strncpy(sa_sysctlf, optarg, sizeof(sa_sysctlf));
			break;
		case 't':	/* -t, --agent-alarms */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: setting agent alarms\n", argv[0]);
			sa_alarms = 0;
			ds_set_boolean(DS_LIBRARY_ID, DS_LIB_ALARM_DONT_USE_SIG, 1);
			break;
		case 'T':	/* -T, --transport [TRANSPORT] */
			if (optarg == NULL)
				goto udp_transport;
			if (!strcasecmp("TCP", optarg)) {
				if (sa_debug)
					snmp_log(MY_FACILITY(LOG_DEBUG), "%s: setting default transport to TCP\n", argv[0]);
				val = ds_get_int(DS_APPLICATION_ID, DS_AGENT_FLAGS);
				val |= SNMP_FLAGS_STREAM_SOCKET;
				ds_set_int(DS_APPLICATION_ID, DS_AGENT_FLAGS, val);
			} else if (!strcasecmp("UDP", optarg)) {
			      udp_transport:
				if (sa_debug)
					snmp_log(MY_FACILITY(LOG_DEBUG), "%s: setting default transport to UDP\n", argv[0]);
				val = ds_get_int(DS_APPLICATION_ID, DS_AGENT_FLAGS);
				val &= ~SNMP_FLAGS_STREAM_SOCKET;
				ds_set_int(DS_APPLICATION_ID, DS_AGENT_FLAGS, val);
			} else
				goto bad_option;
			break;
		case 'U':
#if defined NETSNMP_DS_AGENT_LEAVE_PIDFILE
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: will leave pidfile after shutdown\n", argv[0]);
			ds_set_boolean(DS_APPLICATION_ID, NETSNMP_DS_AGENT_LEAVE_PIDFILE, 1);
#else
			snmp_log(MY_FACILITY(LOG_DEBUG), "%s: -U option not supported\n");
			goto bad_option;
#endif				/* defined NETSNMP_DS_AGENT_LEAVE_PIDFILE */
			break;
		case 'v':	/* -v, --version */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: printing version message\n", argv[0]);
			sa_version(argc, argv);
			exit(0);
		case 'V':	/* -V, --verbose [LEVEL] */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: increasing output verbosity\n", argv[0]);
			if (optarg == NULL) {
				sa_output++;
			} else {
				if ((val = strtol(optarg, NULL, 0)) < 0)
					goto bad_option;
				sa_output = val;
			}
			if (sa_output > 1)
				ds_set_boolean(DS_APPLICATION_ID, DS_AGENT_VERBOSE, 1);
			else
				ds_set_boolean(DS_APPLICATION_ID, DS_AGENT_VERBOSE, 0);
			break;
		case 'x':	/* -x, --agentx-socket SOCKET */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: setting AgentX socket to %s\n", argv[0], optarg);
			ds_set_string(DS_APPLICATION_ID, DS_AGENT_X_SOCKET, optarg);
			// ds_set_boolean(DS_APPLICATION_ID, DS_AGENT_AGENTX_MASTER, 1);
			break;
		case 'X':	/* -X, --agentx */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: setting AgentX sub-agent\n", argv[0]);
			sa_agentx = 1;
			ds_set_boolean(DS_APPLICATION_ID, DS_AGENT_ROLE, 1);
			break;
		case 'y':	/* -y, --copying */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: printing copying message\n", argv[0]);
			sa_copying(argc, argv);
			exit(0);
		case '?':
		case ':':
		default:
		      bad_option:
			optind--;
			goto bad_nonopt;
		      bad_nonopt:
			if (sa_output || sa_debug) {
				if (optind < argc) {
					fprintf(stderr, "%s: syntax error near '", argv[0]);
					while (optind < argc)
						fprintf(stderr, "%s ", argv[optind++]);
					fprintf(stderr, "'\n");
				} else {
					fprintf(stderr, "%s: missing option or argument", argv[0]);
					fprintf(stderr, "\n");
				}
				fflush(stderr);
			      bad_usage:
				sa_usage(argc, argv);
			}
			exit(2);
		}
	}
	if (optind < argc) {
		if (sa_debug)
			snmp_log(MY_FACILITY(LOG_DEBUG), "%s: excess non-option arguments\n", argv[0]);
		goto bad_nonopt;
	}
	sa_enter(argc, argv);	/* daemonize if necessary */
	sa_mloop(argc, argv);	/* execute main loop */
	exit(0);
}
#endif				/* !defined MODULE */

