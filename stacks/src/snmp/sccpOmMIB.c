/*****************************************************************************

 @(#) $RCSfile: sccpOmMIB.c,v $ $Name:  $($Revision: 0.9.2.8 $) $Date: 2009-01-04 13:16:05 $

 -----------------------------------------------------------------------------

 Copyright (c) 2008-2009  Monavacom Limited <http://www.monavacom.com/>
 Copyright (c) 2001-2008  OpenSS7 Corporation <http://www.openss7.com/>
 Copyright (c) 1997-2001  Brian F. G. Bidulock <bidulock@openss7.org>

 All Rights Reserved.

 This program is free software: you can redistribute it and/or modify it under
 the terms of the GNU Affero General Public License as published by the Free
 Software Foundation, version 3 of the license.

 This program is distributed in the hope that it will be useful, but WITHOUT
 ANY WARRANTY; without even the implied warranty of MERCHANTABILITY or FITNESS
 FOR A PARTICULAR PURPOSE.  See the GNU Affero General Public License for more
 details.

 You should have received a copy of the GNU Affero General Public License
 along with this program.  If not, see <http://www.gnu.org/licenses/>, or
 write to the Free Software Foundation, Inc., 675 Mass Ave, Cambridge, MA
 02139, USA.

 -----------------------------------------------------------------------------

 U.S. GOVERNMENT RESTRICTED RIGHTS.  If you are licensing this Software on
 behalf of the U.S. Government ("Government"), the following provisions apply
 to you.  If the Software is supplied by the Department of Defense ("DoD"), it
 is classified as "Commercial Computer Software" under paragraph 252.227-7014
 of the DoD Supplement to the Federal Acquisition Regulations ("DFARS") (or any
 successor regulations) and the Government is acquiring only the license rights
 granted herein (the license rights customarily provided to non-Government
 users).  If the Software is supplied to any unit or agency of the Government
 other than DoD, it is classified as "Restricted Computer Software" and the
 Government's rights in the Software are defined in paragraph 52.227-19 of the
 Federal Acquisition Regulations ("FAR") (or any successor regulations) or, in
 the cases of NASA, in paragraph 18.52.227-86 of the NASA Supplement to the FAR
 (or any successor regulations).

 -----------------------------------------------------------------------------

 Commercial licensing and support of this software is available from OpenSS7
 Corporation at a fee.  See http://www.openss7.com/

 -----------------------------------------------------------------------------

 Last Modified $Date: 2009-01-04 13:16:05 $ by $Author: brian $

 -----------------------------------------------------------------------------

 $Log: sccpOmMIB.c,v $
 Revision 0.9.2.8  2009-01-04 13:16:05  brian
 - updated agents

 Revision 0.9.2.7  2009-01-03 10:51:06  brian
 - updated agent

 Revision 0.9.2.6  2009-01-02 15:46:43  brian
 - corrections

 Revision 0.9.2.5  2009-01-02 15:04:15  brian
 - updated agents

 Revision 0.9.2.4  2008-12-31 16:04:47  brian
 - updated mibs

 Revision 0.9.2.3  2008-12-27 15:29:47  brian
 - split MTP and MTP OM mib

 Revision 0.9.2.2  2008-12-24 13:08:25  brian
 - building ss7confd

 *****************************************************************************/

#ident "@(#) $RCSfile: sccpOmMIB.c,v $ $Name:  $($Revision: 0.9.2.8 $) $Date: 2009-01-04 13:16:05 $"

static char const ident[] = "$RCSfile: sccpOmMIB.c,v $ $Name:  $($Revision: 0.9.2.8 $) $Date: 2009-01-04 13:16:05 $";

/* This file was generated by mib2c and is intended for use as
   a mib module for the ucd-snmp snmpd agent. */
#include <ucd-snmp/ucd-snmp-config.h>
#include <ucd-snmp/ucd-snmp-includes.h>
#include <ucd-snmp/ucd-snmp-agent-includes.h>
#include <ucd-snmp/callback.h>
#include <ucd-snmp/snmp-tc.h>
#include <ucd-snmp/default_store.h>
#include <ucd-snmp/snmp_alarm.h>
/* The following header files are mangled in most recent net-snmp releases so
 * the versions from UCD-SNMP 4.2.5 are included here.  */
#if defined HAVE_LIBNETSNMP
#else				/* defined HAVE_LIBNETSNMP */
#endif				/* defined HAVE_LIBNETSNMP */
/* These are messed up on both. */
#include "ds_agent.h"
#ifdef HAVE_UCD_SNMP_UTIL_FUNCS_H
#include <ucd-snmp/util_funcs.h>
/* Many recent net-snmp UCD compatible headers do not declare header_generic. */
int header_generic(struct variable *, oid *, size_t *, int, size_t *, WriteMethod **);
#else				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#include "util_funcs.h"
#endif				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#ifdef HAVE_UCD_SNMP_HEADER_COMPLEX_H
#include <ucd-snmp/header_complex.h>
#else				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
#include "header_complex.h"
#endif				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
/* This one is the other way around: it is just fine for net-snmp, but
 * ucd-snmp does not provide the header file at all.  */
#ifdef HAVE_UCD_SNMP_MIB_MODULES_H
#include <ucd-snmp/mib_modules.h>
#else				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#ifdef HAVE_NET_SNMP_AGENT_MIB_MODULES_H
#include <net-snmp/agent/mib_modules.h>
#else				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#include "mib_modules.h"
#endif				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#endif				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#include <stdint.h>
#include <signal.h>
#include <sys/stat.h>		/* for struct stat, fstat() */
#include <sys/types.h>
#include <unistd.h>
#include <stdlib.h>
#include <pwd.h>		/* for getpwuid() getpwnam() */
#include <grp.h>		/* for getgrgid() getgrnam() */
#include <libgen.h>		/* for basename() */
#include <fcntl.h>		/* for O_CREAT */
#include <sys/sysctl.h>		/* for sysctl */
#include <errno.h>
#include <string.h>
#ifdef _GNU_SOURCE
#include <getopt.h>
#endif
#include "ss7MIB.h"
#include "x400pMIB.h"
#include "mtpMIB.h"
#include "mtpOmMIB.h"
#include "sccpMIB.h"
#include "sccpOmMIB.h"
extern const char sa_program[];

#define MY_FACILITY(__pri)	(LOG_DAEMON|(__pri))
#undef MASTER
#if !defined MODULE
extern int sa_dump;			/* default packet dump */
extern int sa_debug;			/* default no debug */
extern int sa_nomead;			/* default daemon mode */
extern int sa_output;			/* default normal output */
extern int sa_agentx;			/* default agentx mode */
extern int sa_alarms;			/* default application alarms */
extern int sa_logaddr;			/* log addresses */
extern int sa_logfillog;		/* log to sa_logfile */
extern int sa_logstderr;		/* log to standard error */
extern int sa_logstdout;		/* log to standard output */
extern int sa_logsyslog;		/* log to system logs */
extern int sa_logcallog;		/* log to callback logs */
extern int sa_appendlog;		/* append to log file without truncating */
extern char sa_logfile[256];
extern char sa_pidfile[256];
extern char sa_sysctlf[256];

/* file stream for log file */
extern FILE *stdlog;
#endif				/* !defined MODULE */
extern int sa_fclose;			/* default close files between requests */
extern int sa_fd;			/* file descriptor for MIB use */
extern int sa_readfd;			/* file descriptor for autonomnous events */
extern int sa_changed;			/* indication to reread MIB configuration */
extern int sa_stats_refresh;		/* indications that statistics, the mib or its tables need to be refreshed */
extern int sa_request;			/* request number for per-request actions */
volatile int sccpOmMIB_refresh = 1;
volatile int sccpOmErrorsTable_refresh = 1;
volatile int sccpOmMessageTable_refresh = 1;
volatile int sccpOmAccessibilityTable_refresh = 1;
volatile int sccpOmUtilizationTable_refresh = 1;
volatile int sccpOm5MinHistoryTable_refresh = 1;
volatile int sccpOm15MinHistoryTable_refresh = 1;
volatile int sccpOm5MinSsnHistoryTable_refresh = 1;
volatile int sccpOm15MinSsnHistoryTable_refresh = 1;

/*
 * sccpOmMIB_variables_oid: object identifier for sccpOmMIB
 * This is the top level oid that we want to register under.  This is essentially a prefix, with the
 * suffix appearing in the variable below.
 */
oid sccpOmMIB_variables_oid[] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 2 };

/*
 * Oids for use in notifications defined in this MIB.
 */
oid sccpOm5minReport_oid[11] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 2, 0 };
oid sccpOm15minReport_oid[11] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 2, 0 };
oid sccpOm5minSsnReport_oid[11] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 2, 0 };
oid sccpOm15minSsnReport_oid[11] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 2, 0 };

/*
 * Oids accessible only for notify defined in this MIB.
 */
oid sccpOmDuration_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 2, 1, 3 };
oid sccpOmSsn_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 2, 1, 3 };
oid sccpOmSyntaxErrorType_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 2, 1, 3 };

/*
 * Other oids defined in this MIB.
 */
#if 0
oid sccpOmMIBCompliance_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 2, 2, 1 };
oid sccpOmObligatoryGroup_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 2, 2, 2 };
oid sccpOmRoutingGroup_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 2, 2, 2 };
oid sccpOmOblReassemblyGroup_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 2, 2, 2 };
oid sccpOmAllReassemblyGroup_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 2, 2, 2 };
oid sccpOmHopCounterGroup_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 2, 2, 2 };
oid sccpOmReplicatedGroup_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 2, 2, 2 };
oid sccpOmOblConnectionOrientedGroup_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 2, 2, 2 };
oid sccpOmAllConnectionOrientedGroup_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 2, 2, 2 };
oid sccpOmOblProtocolClass3Group_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 2, 2, 2 };
oid sccpOmAllProtocolClass3Group_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 2, 2, 2 };
oid sccpOmSccpManagementGroup_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 2, 2, 2 };
oid sccpOmGTTGroup_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 2, 2, 2 };
oid sccpOmNonObligatoryGroup_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 2, 2, 2 };
oid sccpOmSccpUnavailableGroup_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 2, 2, 2 };
#endif
static const oid zeroDotZero_oid[2] = { 0, 0 };
static oid snmpTrapOID_oid[11] = { 1, 3, 6, 1, 6, 3, 1, 1, 4, 1, 0 };

/*
 * variable7 sccpOmMIB_variables: tree for sccpOmMIB
 * This variable defines function callbacks and type return information for the sccpOmMIB mib section
 */
struct variable7 sccpOmMIB_variables[] = {
	/* magic number, variable type, ro/rw, callback fn, L, oidsuffix */
#define   SCCPOMNOTRANSLATORFORADDRESS  (4 % 256)
	{SCCPOMNOTRANSLATORFORADDRESS, ASN_COUNTER, RONLY, var_sccpOmErrorsTable, 5, {1, 1, 1, 1, 1}},
#define   SCCPOMNORULEFORADDRESS  (5 % 256)
	{SCCPOMNORULEFORADDRESS, ASN_COUNTER, RONLY, var_sccpOmErrorsTable, 5, {1, 1, 1, 1, 2}},
#define   SCCPOMPOINTCODENOTAVAILABLE  (6 % 256)
	{SCCPOMPOINTCODENOTAVAILABLE, ASN_COUNTER, RONLY, var_sccpOmErrorsTable, 5, {1, 1, 1, 1, 3}},
#define   SCCPOMPOINTCODECONGESTED  (7 % 256)
	{SCCPOMPOINTCODECONGESTED, ASN_COUNTER, RONLY, var_sccpOmErrorsTable, 5, {1, 1, 1, 1, 4}},
#define   SCCPOMSUBSYSTEMUNAVAILABLE  (8 % 256)
	{SCCPOMSUBSYSTEMUNAVAILABLE, ASN_COUNTER, RONLY, var_sccpOmErrorsTable, 5, {1, 1, 1, 1, 5}},
#define   SCCPOMSUBSYSTEMCONGESTED  (9 % 256)
	{SCCPOMSUBSYSTEMCONGESTED, ASN_COUNTER, RONLY, var_sccpOmErrorsTable, 5, {1, 1, 1, 1, 6}},
#define   SCCPOMUNEQUIPPEDSUBSYSTEM  (10 % 256)
	{SCCPOMUNEQUIPPEDSUBSYSTEM, ASN_COUNTER, RONLY, var_sccpOmErrorsTable, 5, {1, 1, 1, 1, 7}},
#define   SCCPOMSYNTAXERRORDETECTED  (11 % 256)
	{SCCPOMSYNTAXERRORDETECTED, ASN_COUNTER, RONLY, var_sccpOmErrorsTable, 5, {1, 1, 1, 1, 8}},
#define   SCCPOMROUTINGFAILURE  (12 % 256)
	{SCCPOMROUTINGFAILURE, ASN_COUNTER, RONLY, var_sccpOmErrorsTable, 5, {1, 1, 1, 1, 9}},
#define   SCCPOMREASSEMBLYTIMEOUT  (13 % 256)
	{SCCPOMREASSEMBLYTIMEOUT, ASN_COUNTER, RONLY, var_sccpOmErrorsTable, 5, {1, 1, 1, 1, 10}},
#define   SCCPOMSEGMENTOUTOFORDER  (14 % 256)
	{SCCPOMSEGMENTOUTOFORDER, ASN_COUNTER, RONLY, var_sccpOmErrorsTable, 5, {1, 1, 1, 1, 11}},
#define   SCCPOMNOREASSEMBLYSPACE  (15 % 256)
	{SCCPOMNOREASSEMBLYSPACE, ASN_COUNTER, RONLY, var_sccpOmErrorsTable, 5, {1, 1, 1, 1, 12}},
#define   SCCPOMHOPCOUNTERVIOLATION  (16 % 256)
	{SCCPOMHOPCOUNTERVIOLATION, ASN_COUNTER, RONLY, var_sccpOmErrorsTable, 5, {1, 1, 1, 1, 13}},
#define   SCCPOMTOOLARGEFORSEGMENTATION  (17 % 256)
	{SCCPOMTOOLARGEFORSEGMENTATION, ASN_COUNTER, RONLY, var_sccpOmErrorsTable, 5, {1, 1, 1, 1, 14}},
#define   SCCPOMRELEASEFAILURE  (18 % 256)
	{SCCPOMRELEASEFAILURE, ASN_COUNTER, RONLY, var_sccpOmErrorsTable, 5, {1, 1, 1, 1, 15}},
#define   SCCPOMINACTIVITYTIMEREXPIRY  (19 % 256)
	{SCCPOMINACTIVITYTIMEREXPIRY, ASN_COUNTER, RONLY, var_sccpOmErrorsTable, 5, {1, 1, 1, 1, 16}},
#define   SCCPOMPROVIDERINITIATEDRESET  (20 % 256)
	{SCCPOMPROVIDERINITIATEDRESET, ASN_COUNTER, RONLY, var_sccpOmErrorsTable, 5, {1, 1, 1, 1, 17}},
#define   SCCPOMPROVIDERINITIATEDRELEASE  (21 % 256)
	{SCCPOMPROVIDERINITIATEDRELEASE, ASN_COUNTER, RONLY, var_sccpOmErrorsTable, 5, {1, 1, 1, 1, 18}},
#define   SCCPOMNOSEGMENTATIONSUPPORT  (22 % 256)
	{SCCPOMNOSEGMENTATIONSUPPORT, ASN_COUNTER, RONLY, var_sccpOmErrorsTable, 5, {1, 1, 1, 1, 19}},
#define   SCCPOMSEGMENTATIONFAILURE  (23 % 256)
	{SCCPOMSEGMENTATIONFAILURE, ASN_COUNTER, RONLY, var_sccpOmErrorsTable, 5, {1, 1, 1, 1, 20}},
#define   SCCPOMREASSEMBLYFAILURE  (24 % 256)
	{SCCPOMREASSEMBLYFAILURE, ASN_COUNTER, RONLY, var_sccpOmErrorsTable, 5, {1, 1, 1, 1, 21}},
#define   SCCPOMERRORS5MINVALIDINTERVALS  (25 % 256)
	{SCCPOMERRORS5MINVALIDINTERVALS, ASN_INTEGER, RONLY, var_sccpOmErrorsTable, 5, {1, 1, 1, 1, 22}},
#define   SCCPOMERRORS15MINVALIDINTERVALS  (26 % 256)
	{SCCPOMERRORS15MINVALIDINTERVALS, ASN_INTEGER, RONLY, var_sccpOmErrorsTable, 5, {1, 1, 1, 1, 23}},
#define   SCCPOMTOTALMESSAGESHANDLED  (30 % 256)
	{SCCPOMTOTALMESSAGESHANDLED, ASN_COUNTER, RONLY, var_sccpOmMessageTable, 5, {1, 1, 2, 1, 1}},
#define   SCCPOMTOTALMESSAGESLOCAL  (31 % 256)
	{SCCPOMTOTALMESSAGESLOCAL, ASN_COUNTER, RONLY, var_sccpOmMessageTable, 5, {1, 1, 2, 1, 2}},
#define   SCCPOMTOTALMESSAGESGTT  (32 % 256)
	{SCCPOMTOTALMESSAGESGTT, ASN_COUNTER, RONLY, var_sccpOmMessageTable, 5, {1, 1, 2, 1, 3}},
#define   SCCPOMUDTMESSAGESSENT  (33 % 256)
	{SCCPOMUDTMESSAGESSENT, ASN_COUNTER, RONLY, var_sccpOmMessageTable, 5, {1, 1, 2, 1, 4}},
#define   SCCPOMUDTSMESSAGESSENT  (34 % 256)
	{SCCPOMUDTSMESSAGESSENT, ASN_COUNTER, RONLY, var_sccpOmMessageTable, 5, {1, 1, 2, 1, 5}},
#define   SCCPOMUDTMESSAGESRECEIVED  (35 % 256)
	{SCCPOMUDTMESSAGESRECEIVED, ASN_COUNTER, RONLY, var_sccpOmMessageTable, 5, {1, 1, 2, 1, 6}},
#define   SCCPOMUDTSMESSAGESRECEIVED  (36 % 256)
	{SCCPOMUDTSMESSAGESRECEIVED, ASN_COUNTER, RONLY, var_sccpOmMessageTable, 5, {1, 1, 2, 1, 7}},
#define   SCCPOMCRMESSAGESSENT  (37 % 256)
	{SCCPOMCRMESSAGESSENT, ASN_COUNTER, RONLY, var_sccpOmMessageTable, 5, {1, 1, 2, 1, 8}},
#define   SCCPOMCREFMESSAGESSENT  (38 % 256)
	{SCCPOMCREFMESSAGESSENT, ASN_COUNTER, RONLY, var_sccpOmMessageTable, 5, {1, 1, 2, 1, 9}},
#define   SCCPOMCRMESSAGESRECEIVED  (39 % 256)
	{SCCPOMCRMESSAGESRECEIVED, ASN_COUNTER, RONLY, var_sccpOmMessageTable, 5, {1, 1, 2, 1, 10}},
#define   SCCPOMCREFMESSAGESRECEIVED  (40 % 256)
	{SCCPOMCREFMESSAGESRECEIVED, ASN_COUNTER, RONLY, var_sccpOmMessageTable, 5, {1, 1, 2, 1, 11}},
#define   SCCPOMRSRMESSAGESSENT  (41 % 256)
	{SCCPOMRSRMESSAGESSENT, ASN_COUNTER, RONLY, var_sccpOmMessageTable, 5, {1, 1, 2, 1, 12}},
#define   SCCPOMRSRMESSAGESRECEIVED  (42 % 256)
	{SCCPOMRSRMESSAGESRECEIVED, ASN_COUNTER, RONLY, var_sccpOmMessageTable, 5, {1, 1, 2, 1, 13}},
#define   SCCPOMERRMESSAGESSENT  (43 % 256)
	{SCCPOMERRMESSAGESSENT, ASN_COUNTER, RONLY, var_sccpOmMessageTable, 5, {1, 1, 2, 1, 14}},
#define   SCCPOMERRMESSAGESRECEIVED  (44 % 256)
	{SCCPOMERRMESSAGESRECEIVED, ASN_COUNTER, RONLY, var_sccpOmMessageTable, 5, {1, 1, 2, 1, 15}},
#define   SCCPOMXUDTMESSAGESSENT  (45 % 256)
	{SCCPOMXUDTMESSAGESSENT, ASN_COUNTER, RONLY, var_sccpOmMessageTable, 5, {1, 1, 2, 1, 16}},
#define   SCCPOMXUDTSMESSAGESSENT  (46 % 256)
	{SCCPOMXUDTSMESSAGESSENT, ASN_COUNTER, RONLY, var_sccpOmMessageTable, 5, {1, 1, 2, 1, 17}},
#define   SCCPOMXUDTMESSAGESRECEIVED  (47 % 256)
	{SCCPOMXUDTMESSAGESRECEIVED, ASN_COUNTER, RONLY, var_sccpOmMessageTable, 5, {1, 1, 2, 1, 18}},
#define   SCCPOMXUDTSMESSAGESRECEIVED  (48 % 256)
	{SCCPOMXUDTSMESSAGESRECEIVED, ASN_COUNTER, RONLY, var_sccpOmMessageTable, 5, {1, 1, 2, 1, 19}},
#define   SCCPOMLUDTMESSAGESSENT  (49 % 256)
	{SCCPOMLUDTMESSAGESSENT, ASN_COUNTER, RONLY, var_sccpOmMessageTable, 5, {1, 1, 2, 1, 20}},
#define   SCCPOMLUDTSMESSAGESSENT  (50 % 256)
	{SCCPOMLUDTSMESSAGESSENT, ASN_COUNTER, RONLY, var_sccpOmMessageTable, 5, {1, 1, 2, 1, 21}},
#define   SCCPOMLUDTMESSAGESRECEIVED  (51 % 256)
	{SCCPOMLUDTMESSAGESRECEIVED, ASN_COUNTER, RONLY, var_sccpOmMessageTable, 5, {1, 1, 2, 1, 22}},
#define   SCCPOMLUDTSMESSAGESRECEIVED  (52 % 256)
	{SCCPOMLUDTSMESSAGESRECEIVED, ASN_COUNTER, RONLY, var_sccpOmMessageTable, 5, {1, 1, 2, 1, 23}},
#define   SCCPOMMESSAGE5MINVALIDINTERVALS  (53 % 256)
	{SCCPOMMESSAGE5MINVALIDINTERVALS, ASN_INTEGER, RONLY, var_sccpOmMessageTable, 5, {1, 1, 2, 1, 24}},
#define   SCCPOMMESSAGE15MINVALIDINTERVALS  (54 % 256)
	{SCCPOMMESSAGE15MINVALIDINTERVALS, ASN_INTEGER, RONLY, var_sccpOmMessageTable, 5, {1, 1, 2, 1, 25}},
#define   SCCPOMSSCMESSAGERECEIVED  (58 % 256)
	{SCCPOMSSCMESSAGERECEIVED, ASN_COUNTER, RONLY, var_sccpOmAccessibilityTable, 5, {1, 1, 3, 1, 1}},
#define   SCCPOMSSPMESSAGERECEIVED  (59 % 256)
	{SCCPOMSSPMESSAGERECEIVED, ASN_COUNTER, RONLY, var_sccpOmAccessibilityTable, 5, {1, 1, 3, 1, 2}},
#define   SCCPOMSCCPUNAVAILABLEDURATION  (60 % 256)
	{SCCPOMSCCPUNAVAILABLEDURATION, ASN_UNSIGNED, RONLY, var_sccpOmAccessibilityTable, 5, {1, 1, 3, 1, 3}},
#define   SCCPOMACCESSIBILITY5MINVALIDINTERVALS  (61 % 256)
	{SCCPOMACCESSIBILITY5MINVALIDINTERVALS, ASN_INTEGER, RONLY, var_sccpOmAccessibilityTable, 5, {1, 1, 3, 1, 4}},
#define   SCCPOMACCESSIBILITY15MINVALIDINTERVALS  (62 % 256)
	{SCCPOMACCESSIBILITY15MINVALIDINTERVALS, ASN_INTEGER, RONLY, var_sccpOmAccessibilityTable, 5, {1, 1, 3, 1, 5}},
#define   SCCPOMLXUDTMESSAGESORIGCLASS0  (67 % 256)
	{SCCPOMLXUDTMESSAGESORIGCLASS0, ASN_COUNTER, RONLY, var_sccpOmUtilizationTable, 5, {1, 1, 4, 1, 1}},
#define   SCCPOMLXUDTMESSAGESORIGCLASS1  (68 % 256)
	{SCCPOMLXUDTMESSAGESORIGCLASS1, ASN_COUNTER, RONLY, var_sccpOmUtilizationTable, 5, {1, 1, 4, 1, 2}},
#define   SCCPOMLXUDTMESSAGESTERMCLASS0  (69 % 256)
	{SCCPOMLXUDTMESSAGESTERMCLASS0, ASN_COUNTER, RONLY, var_sccpOmUtilizationTable, 5, {1, 1, 4, 1, 3}},
#define   SCCPOMLXUDTMESSAGESTERMCLASS1  (70 % 256)
	{SCCPOMLXUDTMESSAGESTERMCLASS1, ASN_COUNTER, RONLY, var_sccpOmUtilizationTable, 5, {1, 1, 4, 1, 4}},
#define   SCCPOMMESSAGESSENTTOBACKUP  (71 % 256)
	{SCCPOMMESSAGESSENTTOBACKUP, ASN_COUNTER, RONLY, var_sccpOmUtilizationTable, 5, {1, 1, 4, 1, 5}},
#define   SCCPOMDT1MESSAGESRECEIVED  (72 % 256)
	{SCCPOMDT1MESSAGESRECEIVED, ASN_COUNTER, RONLY, var_sccpOmUtilizationTable, 5, {1, 1, 4, 1, 6}},
#define   SCCPOMDT1MESSAGESSENT  (73 % 256)
	{SCCPOMDT1MESSAGESSENT, ASN_COUNTER, RONLY, var_sccpOmUtilizationTable, 5, {1, 1, 4, 1, 7}},
#define   SCCPOMDT2MESSAGESRECEIVED  (74 % 256)
	{SCCPOMDT2MESSAGESRECEIVED, ASN_COUNTER, RONLY, var_sccpOmUtilizationTable, 5, {1, 1, 4, 1, 8}},
#define   SCCPOMDT2MESSAGESSENT  (75 % 256)
	{SCCPOMDT2MESSAGESSENT, ASN_COUNTER, RONLY, var_sccpOmUtilizationTable, 5, {1, 1, 4, 1, 9}},
#define   SCCPOMEDMESSAGESSENT  (76 % 256)
	{SCCPOMEDMESSAGESSENT, ASN_COUNTER, RONLY, var_sccpOmUtilizationTable, 5, {1, 1, 4, 1, 10}},
#define   SCCPOMEDMESSAGESRECEIVED  (77 % 256)
	{SCCPOMEDMESSAGESRECEIVED, ASN_COUNTER, RONLY, var_sccpOmUtilizationTable, 5, {1, 1, 4, 1, 11}},
#define   SCCPOMUTILIZATION5MINVALIDINTERVALS  (78 % 256)
	{SCCPOMUTILIZATION5MINVALIDINTERVALS, ASN_INTEGER, RONLY, var_sccpOmUtilizationTable, 5, {1, 1, 4, 1, 12}},
#define   SCCPOMUTILIZATION15MINVALIDINTERVALS  (79 % 256)
	{SCCPOMUTILIZATION15MINVALIDINTERVALS, ASN_INTEGER, RONLY, var_sccpOmUtilizationTable, 5, {1, 1, 4, 1, 13}},
#define   SCCPOM5MINNOTRANSLATORFORADDRESS  (84 % 256)
	{SCCPOM5MINNOTRANSLATORFORADDRESS, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 1}},
#define   SCCPOM5MINNORULEFORADDRESS  (85 % 256)
	{SCCPOM5MINNORULEFORADDRESS, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 2}},
#define   SCCPOM5MINPOINTCODENOTAVAILABLE  (86 % 256)
	{SCCPOM5MINPOINTCODENOTAVAILABLE, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 3}},
#define   SCCPOM5MINPOINTCODECONGESTED  (87 % 256)
	{SCCPOM5MINPOINTCODECONGESTED, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 4}},
#define   SCCPOM5MINSUBSYSTEMUNAVAILABLE  (88 % 256)
	{SCCPOM5MINSUBSYSTEMUNAVAILABLE, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 5}},
#define   SCCPOM5MINSUBSYSTEMCONGESTED  (89 % 256)
	{SCCPOM5MINSUBSYSTEMCONGESTED, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 6}},
#define   SCCPOM5MINUNEQUIPPEDSUBSYSTEM  (90 % 256)
	{SCCPOM5MINUNEQUIPPEDSUBSYSTEM, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 7}},
#define   SCCPOM5MINSYNTAXERRORDETECTED  (91 % 256)
	{SCCPOM5MINSYNTAXERRORDETECTED, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 8}},
#define   SCCPOM5MINROUTINGFAILURE  (92 % 256)
	{SCCPOM5MINROUTINGFAILURE, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 9}},
#define   SCCPOM5MINREASSEMBLYTIMEOUT  (93 % 256)
	{SCCPOM5MINREASSEMBLYTIMEOUT, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 10}},
#define   SCCPOM5MINSEGMENTOUTOFORDER  (94 % 256)
	{SCCPOM5MINSEGMENTOUTOFORDER, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 11}},
#define   SCCPOM5MINNOREASSEMBLYSPACE  (95 % 256)
	{SCCPOM5MINNOREASSEMBLYSPACE, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 12}},
#define   SCCPOM5MINHOPCOUNTERVIOLATION  (96 % 256)
	{SCCPOM5MINHOPCOUNTERVIOLATION, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 13}},
#define   SCCPOM5MINTOOLARGEFORSEGMENTATION  (97 % 256)
	{SCCPOM5MINTOOLARGEFORSEGMENTATION, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 14}},
#define   SCCPOM5MINRELEASEFAILURE  (98 % 256)
	{SCCPOM5MINRELEASEFAILURE, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 15}},
#define   SCCPOM5MININACTIVITYTIMEREXPIRY  (99 % 256)
	{SCCPOM5MININACTIVITYTIMEREXPIRY, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 16}},
#define   SCCPOM5MINPROVIDERINITIATEDRESET  (100 % 256)
	{SCCPOM5MINPROVIDERINITIATEDRESET, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 17}},
#define   SCCPOM5MINPROVIDERINITIATEDRELEASE  (101 % 256)
	{SCCPOM5MINPROVIDERINITIATEDRELEASE, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 18}},
#define   SCCPOM5MINNOSEGMENTATIONSUPPORT  (102 % 256)
	{SCCPOM5MINNOSEGMENTATIONSUPPORT, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 19}},
#define   SCCPOM5MINSEGMENTATIONFAILURE  (103 % 256)
	{SCCPOM5MINSEGMENTATIONFAILURE, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 20}},
#define   SCCPOM5MINREASSEMBLYFAILURE  (104 % 256)
	{SCCPOM5MINREASSEMBLYFAILURE, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 21}},
#define   SCCPOM5MINTOTALMESSAGESHANDLED  (105 % 256)
	{SCCPOM5MINTOTALMESSAGESHANDLED, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 22}},
#define   SCCPOM5MINTOTALMESSAGESLOCAL  (106 % 256)
	{SCCPOM5MINTOTALMESSAGESLOCAL, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 23}},
#define   SCCPOM5MINTOTALMESSAGESGTT  (107 % 256)
	{SCCPOM5MINTOTALMESSAGESGTT, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 24}},
#define   SCCPOM5MINUDTMESSAGESSENT  (108 % 256)
	{SCCPOM5MINUDTMESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 25}},
#define   SCCPOM5MINUDTSMESSAGESSENT  (109 % 256)
	{SCCPOM5MINUDTSMESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 26}},
#define   SCCPOM5MINUDTMESSAGESRECEIVED  (110 % 256)
	{SCCPOM5MINUDTMESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 27}},
#define   SCCPOM5MINUDTSMESSAGESRECEIVED  (111 % 256)
	{SCCPOM5MINUDTSMESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 28}},
#define   SCCPOM5MINCRMESSAGESSENT  (112 % 256)
	{SCCPOM5MINCRMESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 29}},
#define   SCCPOM5MINCREFMESSAGESSENT  (113 % 256)
	{SCCPOM5MINCREFMESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 30}},
#define   SCCPOM5MINCRMESSAGESRECEIVED  (114 % 256)
	{SCCPOM5MINCRMESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 31}},
#define   SCCPOM5MINCREFMESSAGESRECEIVED  (115 % 256)
	{SCCPOM5MINCREFMESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 32}},
#define   SCCPOM5MINRSRMESSAGESSENT  (116 % 256)
	{SCCPOM5MINRSRMESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 33}},
#define   SCCPOM5MINRSRMESSAGESRECEIVED  (117 % 256)
	{SCCPOM5MINRSRMESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 34}},
#define   SCCPOM5MINERRMESSAGESSENT  (118 % 256)
	{SCCPOM5MINERRMESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 35}},
#define   SCCPOM5MINERRMESSAGESRECEIVED  (119 % 256)
	{SCCPOM5MINERRMESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 36}},
#define   SCCPOM5MINXUDTMESSAGESSENT  (120 % 256)
	{SCCPOM5MINXUDTMESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 37}},
#define   SCCPOM5MINXUDTSMESSAGESSENT  (121 % 256)
	{SCCPOM5MINXUDTSMESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 38}},
#define   SCCPOM5MINXUDTMESSAGESRECEIVED  (122 % 256)
	{SCCPOM5MINXUDTMESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 39}},
#define   SCCPOM5MINXUDTSMESSAGESRECEIVED  (123 % 256)
	{SCCPOM5MINXUDTSMESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 40}},
#define   SCCPOM5MINLUDTMESSAGESSENT  (124 % 256)
	{SCCPOM5MINLUDTMESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 41}},
#define   SCCPOM5MINLUDTSMESSAGESSENT  (125 % 256)
	{SCCPOM5MINLUDTSMESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 42}},
#define   SCCPOM5MINLUDTMESSAGESRECEIVED  (126 % 256)
	{SCCPOM5MINLUDTMESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 43}},
#define   SCCPOM5MINLUDTSMESSAGESRECEIVED  (127 % 256)
	{SCCPOM5MINLUDTSMESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 44}},
#define   SCCPOM5MINSSCMESSAGERECEIVED  (128 % 256)
	{SCCPOM5MINSSCMESSAGERECEIVED, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 45}},
#define   SCCPOM5MINSSPMESSAGERECEIVED  (129 % 256)
	{SCCPOM5MINSSPMESSAGERECEIVED, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 46}},
#define   SCCPOM15MINNOTRANSLATORFORADDRESS  (134 % 256)
	{SCCPOM15MINNOTRANSLATORFORADDRESS, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 1}},
#define   SCCPOM15MINNORULEFORADDRESS  (135 % 256)
	{SCCPOM15MINNORULEFORADDRESS, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 2}},
#define   SCCPOM15MINPOINTCODENOTAVAILABLE  (136 % 256)
	{SCCPOM15MINPOINTCODENOTAVAILABLE, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 3}},
#define   SCCPOM15MINPOINTCODECONGESTED  (137 % 256)
	{SCCPOM15MINPOINTCODECONGESTED, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 4}},
#define   SCCPOM15MINSUBSYSTEMUNAVAILABLE  (138 % 256)
	{SCCPOM15MINSUBSYSTEMUNAVAILABLE, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 5}},
#define   SCCPOM15MINSUBSYSTEMCONGESTED  (139 % 256)
	{SCCPOM15MINSUBSYSTEMCONGESTED, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 6}},
#define   SCCPOM15MINUNEQUIPPEDSUBSYSTEM  (140 % 256)
	{SCCPOM15MINUNEQUIPPEDSUBSYSTEM, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 7}},
#define   SCCPOM15MINSYNTAXERRORDETECTED  (141 % 256)
	{SCCPOM15MINSYNTAXERRORDETECTED, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 8}},
#define   SCCPOM15MINROUTINGFAILURE  (142 % 256)
	{SCCPOM15MINROUTINGFAILURE, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 9}},
#define   SCCPOM15MINREASSEMBLYTIMEOUT  (143 % 256)
	{SCCPOM15MINREASSEMBLYTIMEOUT, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 10}},
#define   SCCPOM15MINSEGMENTOUTOFORDER  (144 % 256)
	{SCCPOM15MINSEGMENTOUTOFORDER, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 11}},
#define   SCCPOM15MINNOREASSEMBLYSPACE  (145 % 256)
	{SCCPOM15MINNOREASSEMBLYSPACE, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 12}},
#define   SCCPOM15MINHOPCOUNTERVIOLATION  (146 % 256)
	{SCCPOM15MINHOPCOUNTERVIOLATION, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 13}},
#define   SCCPOM15MINTOOLARGEFORSEGMENTATION  (147 % 256)
	{SCCPOM15MINTOOLARGEFORSEGMENTATION, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 14}},
#define   SCCPOM15MINRELEASEFAILURE  (148 % 256)
	{SCCPOM15MINRELEASEFAILURE, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 15}},
#define   SCCPOM15MININACTIVITYTIMEREXPIRY  (149 % 256)
	{SCCPOM15MININACTIVITYTIMEREXPIRY, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 16}},
#define   SCCPOM15MINPROVIDERINITIATEDRESET  (150 % 256)
	{SCCPOM15MINPROVIDERINITIATEDRESET, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 17}},
#define   SCCPOM15MINPROVIDERINITIATEDRELEASE  (151 % 256)
	{SCCPOM15MINPROVIDERINITIATEDRELEASE, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 18}},
#define   SCCPOM15MINNOSEGMENTATIONSUPPORT  (152 % 256)
	{SCCPOM15MINNOSEGMENTATIONSUPPORT, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 19}},
#define   SCCPOM15MINSEGMENTATIONFAILURE  (153 % 256)
	{SCCPOM15MINSEGMENTATIONFAILURE, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 20}},
#define   SCCPOM15MINREASSEMBLYFAILURE  (154 % 256)
	{SCCPOM15MINREASSEMBLYFAILURE, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 21}},
#define   SCCPOM15MINTOTALMESSAGESHANDLED  (155 % 256)
	{SCCPOM15MINTOTALMESSAGESHANDLED, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 22}},
#define   SCCPOM15MINTOTALMESSAGESLOCAL  (156 % 256)
	{SCCPOM15MINTOTALMESSAGESLOCAL, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 23}},
#define   SCCPOM15MINTOTALMESSAGESGTT  (157 % 256)
	{SCCPOM15MINTOTALMESSAGESGTT, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 24}},
#define   SCCPOM15MINUDTMESSAGESSENT  (158 % 256)
	{SCCPOM15MINUDTMESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 25}},
#define   SCCPOM15MINUDTSMESSAGESSENT  (159 % 256)
	{SCCPOM15MINUDTSMESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 26}},
#define   SCCPOM15MINUDTMESSAGESRECEIVED  (160 % 256)
	{SCCPOM15MINUDTMESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 27}},
#define   SCCPOM15MINUDTSMESSAGESRECEIVED  (161 % 256)
	{SCCPOM15MINUDTSMESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 28}},
#define   SCCPOM15MINCRMESSAGESSENT  (162 % 256)
	{SCCPOM15MINCRMESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 29}},
#define   SCCPOM15MINCREFMESSAGESSENT  (163 % 256)
	{SCCPOM15MINCREFMESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 30}},
#define   SCCPOM15MINCRMESSAGESRECEIVED  (164 % 256)
	{SCCPOM15MINCRMESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 31}},
#define   SCCPOM15MINCREFMESSAGESRECEIVED  (165 % 256)
	{SCCPOM15MINCREFMESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 32}},
#define   SCCPOM15MINRSRMESSAGESSENT  (166 % 256)
	{SCCPOM15MINRSRMESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 33}},
#define   SCCPOM15MINRSRMESSAGESRECEIVED  (167 % 256)
	{SCCPOM15MINRSRMESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 34}},
#define   SCCPOM15MINERRMESSAGESSENT  (168 % 256)
	{SCCPOM15MINERRMESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 35}},
#define   SCCPOM15MINERRMESSAGESRECEIVED  (169 % 256)
	{SCCPOM15MINERRMESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 36}},
#define   SCCPOM15MINXUDTMESSAGESSENT  (170 % 256)
	{SCCPOM15MINXUDTMESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 37}},
#define   SCCPOM15MINXUDTSMESSAGESSENT  (171 % 256)
	{SCCPOM15MINXUDTSMESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 38}},
#define   SCCPOM15MINXUDTMESSAGESRECEIVED  (172 % 256)
	{SCCPOM15MINXUDTMESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 39}},
#define   SCCPOM15MINXUDTSMESSAGESRECEIVED  (173 % 256)
	{SCCPOM15MINXUDTSMESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 40}},
#define   SCCPOM15MINLUDTMESSAGESSENT  (174 % 256)
	{SCCPOM15MINLUDTMESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 41}},
#define   SCCPOM15MINLUDTSMESSAGESSENT  (175 % 256)
	{SCCPOM15MINLUDTSMESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 42}},
#define   SCCPOM15MINLUDTMESSAGESRECEIVED  (176 % 256)
	{SCCPOM15MINLUDTMESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 43}},
#define   SCCPOM15MINLUDTSMESSAGESRECEIVED  (177 % 256)
	{SCCPOM15MINLUDTSMESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 44}},
#define   SCCPOM15MINSCCPUNAVAILABLEDURATION  (178 % 256)
	{SCCPOM15MINSCCPUNAVAILABLEDURATION, ASN_UNSIGNED, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 45}},
#define   SCCPOM5MINLXUDTMESSAGESORIGCLASS0  (184 % 256)
	{SCCPOM5MINLXUDTMESSAGESORIGCLASS0, ASN_GAUGE, RONLY, var_sccpOm5MinSsnHistoryTable, 5, {1, 1, 7, 1, 1}},
#define   SCCPOM5MINLXUDTMESSAGESORIGCLASS1  (185 % 256)
	{SCCPOM5MINLXUDTMESSAGESORIGCLASS1, ASN_GAUGE, RONLY, var_sccpOm5MinSsnHistoryTable, 5, {1, 1, 7, 1, 2}},
#define   SCCPOM5MINLXUDTMESSAGESTERMCLASS0  (186 % 256)
	{SCCPOM5MINLXUDTMESSAGESTERMCLASS0, ASN_GAUGE, RONLY, var_sccpOm5MinSsnHistoryTable, 5, {1, 1, 7, 1, 3}},
#define   SCCPOM5MINLXUDTMESSAGESTERMCLASS1  (187 % 256)
	{SCCPOM5MINLXUDTMESSAGESTERMCLASS1, ASN_GAUGE, RONLY, var_sccpOm5MinSsnHistoryTable, 5, {1, 1, 7, 1, 4}},
#define   SCCPOM5MINMESSAGESSENTTOBACKUP  (188 % 256)
	{SCCPOM5MINMESSAGESSENTTOBACKUP, ASN_GAUGE, RONLY, var_sccpOm5MinSsnHistoryTable, 5, {1, 1, 7, 1, 5}},
#define   SCCPOM5MINDT1MESSAGESRECEIVED  (189 % 256)
	{SCCPOM5MINDT1MESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOm5MinSsnHistoryTable, 5, {1, 1, 7, 1, 6}},
#define   SCCPOM5MINDT1MESSAGESSENT  (190 % 256)
	{SCCPOM5MINDT1MESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOm5MinSsnHistoryTable, 5, {1, 1, 7, 1, 7}},
#define   SCCPOM5MINDT2MESSAGESRECEIVED  (191 % 256)
	{SCCPOM5MINDT2MESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOm5MinSsnHistoryTable, 5, {1, 1, 7, 1, 8}},
#define   SCCPOM5MINDT2MESSAGESSENT  (192 % 256)
	{SCCPOM5MINDT2MESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOm5MinSsnHistoryTable, 5, {1, 1, 7, 1, 9}},
#define   SCCPOM5MINEDMESSAGESSENT  (193 % 256)
	{SCCPOM5MINEDMESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOm5MinSsnHistoryTable, 5, {1, 1, 7, 1, 10}},
#define   SCCPOM5MINEDMESSAGESRECEIVED  (194 % 256)
	{SCCPOM5MINEDMESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOm5MinSsnHistoryTable, 5, {1, 1, 7, 1, 11}},
#define   SCCPOM15MINLXUDTMESSAGESORIGCLASS0  (200 % 256)
	{SCCPOM15MINLXUDTMESSAGESORIGCLASS0, ASN_GAUGE, RONLY, var_sccpOm15MinSsnHistoryTable, 5, {1, 1, 8, 1, 1}},
#define   SCCPOM15MINLXUDTMESSAGESORIGCLASS1  (201 % 256)
	{SCCPOM15MINLXUDTMESSAGESORIGCLASS1, ASN_GAUGE, RONLY, var_sccpOm15MinSsnHistoryTable, 5, {1, 1, 8, 1, 2}},
#define   SCCPOM15MINLXUDTMESSAGESTERMCLASS0  (202 % 256)
	{SCCPOM15MINLXUDTMESSAGESTERMCLASS0, ASN_GAUGE, RONLY, var_sccpOm15MinSsnHistoryTable, 5, {1, 1, 8, 1, 3}},
#define   SCCPOM15MINLXUDTMESSAGESTERMCLASS1  (203 % 256)
	{SCCPOM15MINLXUDTMESSAGESTERMCLASS1, ASN_GAUGE, RONLY, var_sccpOm15MinSsnHistoryTable, 5, {1, 1, 8, 1, 4}},
#define   SCCPOM15MINMESSAGESSENTTOBACKUP  (204 % 256)
	{SCCPOM15MINMESSAGESSENTTOBACKUP, ASN_GAUGE, RONLY, var_sccpOm15MinSsnHistoryTable, 5, {1, 1, 8, 1, 5}},
#define   SCCPOM15MINDT1MESSAGESRECEIVED  (205 % 256)
	{SCCPOM15MINDT1MESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOm15MinSsnHistoryTable, 5, {1, 1, 8, 1, 6}},
#define   SCCPOM15MINDT1MESSAGESSENT  (206 % 256)
	{SCCPOM15MINDT1MESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOm15MinSsnHistoryTable, 5, {1, 1, 8, 1, 7}},
#define   SCCPOM15MINDT2MESSAGESRECEIVED  (207 % 256)
	{SCCPOM15MINDT2MESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOm15MinSsnHistoryTable, 5, {1, 1, 8, 1, 8}},
#define   SCCPOM15MINDT2MESSAGESSENT  (208 % 256)
	{SCCPOM15MINDT2MESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOm15MinSsnHistoryTable, 5, {1, 1, 8, 1, 9}},
#define   SCCPOM15MINEDMESSAGESSENT  (209 % 256)
	{SCCPOM15MINEDMESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOm15MinSsnHistoryTable, 5, {1, 1, 8, 1, 10}},
#define   SCCPOM15MINEDMESSAGESRECEIVED  (210 % 256)
	{SCCPOM15MINEDMESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOm15MinSsnHistoryTable, 5, {1, 1, 8, 1, 11}},
#define   SCCPOM1STANDINTERVALACTIVATE  (211 % 256)
	{SCCPOM1STANDINTERVALACTIVATE, ASN_OBJECT_ID, RWRITE, var_sccpOmMIB, 3, {1, 2, 1}},
#define   SCCPOM1STANDINTERVALDEACTIVATE  (212 % 256)
	{SCCPOM1STANDINTERVALDEACTIVATE, ASN_OBJECT_ID, RWRITE, var_sccpOmMIB, 3, {1, 2, 2}},
#define   SCCPOM5MINACTIVATE    (213 % 256)
	{SCCPOM5MINACTIVATE, ASN_OBJECT_ID, RWRITE, var_sccpOmMIB, 3, {1, 2, 3}},
#define   SCCPOM5MINDEAACTIVATE  (214 % 256)
	{SCCPOM5MINDEAACTIVATE, ASN_OBJECT_ID, RWRITE, var_sccpOmMIB, 3, {1, 2, 4}},
#define   SCCPOM15MINACTIVATE   (215 % 256)
	{SCCPOM15MINACTIVATE, ASN_OBJECT_ID, RWRITE, var_sccpOmMIB, 3, {1, 2, 5}},
#define   SCCPOM15MINDEAACTIVATE  (216 % 256)
	{SCCPOM15MINDEAACTIVATE, ASN_OBJECT_ID, RWRITE, var_sccpOmMIB, 3, {1, 2, 6}},
#define   SCCPOMDISCONTINUITYTIME  (218 % 256)
	{SCCPOMDISCONTINUITYTIME, ASN_TIMETICKS, RONLY, var_sccpOmMIB, 3, {1, 2, 8}},
#define   SCCPOMTIMESTAMP       (219 % 256)
	{SCCPOMTIMESTAMP, ASN_TIMETICKS, RONLY, var_sccpOmMIB, 3, {1, 2, 9}},
#define   SCCPOM5MINMAXINTERVALS  (220 % 256)
	{SCCPOM5MINMAXINTERVALS, ASN_UNSIGNED, RWRITE, var_sccpOmMIB, 3, {1, 2, 10}},
#define   SCCPOM15MINMAXINTERVALS  (221 % 256)
	{SCCPOM15MINMAXINTERVALS, ASN_UNSIGNED, RWRITE, var_sccpOmMIB, 3, {1, 2, 11}},
#define   SCCPOMPDEFAULT        (222 % 256)
	{SCCPOMPDEFAULT, ASN_INTEGER, RWRITE, var_sccpOmMIB, 3, {1, 2, 12}},
#define   SCCPOMNROFSUBLEVELSDEFAULT  (223 % 256)
	{SCCPOMNROFSUBLEVELSDEFAULT, ASN_INTEGER, RWRITE, var_sccpOmMIB, 3, {1, 2, 13}},
#define   SCCPOMNROFRESTRICTIONLEVELSDEFAULT  (224 % 256)
	{SCCPOMNROFRESTRICTIONLEVELSDEFAULT, ASN_INTEGER, RWRITE, var_sccpOmMIB, 3, {1, 2, 14}},
};

/* (L = length of the oidsuffix) */
struct sccpOmMIB_data *sccpOmMIBStorage = NULL;

/* global storage of our data, saved in and configured by header_complex() */
struct header_complex_index *sccpOmErrorsTableStorage = NULL;
struct header_complex_index *sccpOmMessageTableStorage = NULL;
struct header_complex_index *sccpOmAccessibilityTableStorage = NULL;
struct header_complex_index *sccpOmUtilizationTableStorage = NULL;
struct header_complex_index *sccpOm5MinHistoryTableStorage = NULL;
struct header_complex_index *sccpOm15MinHistoryTableStorage = NULL;
struct header_complex_index *sccpOm5MinSsnHistoryTableStorage = NULL;
struct header_complex_index *sccpOm15MinSsnHistoryTableStorage = NULL;

#if defined MODULE
void (*sccpOmMIBold_signal_handler) (int) = NULL;	/* save old signal handler just in case */
void sccpOmMIB_loop_handler(int);
void sccpOmMIB_fd_handler(int, void *);
#endif				/* defined MOUDLE */
/**
 * @fn void init_sccpOmMIB(void)
 * @brief sccpOmMIB initialization routine.
 *
 * This is called when the agent starts up.  At a minimum, registration of the MIB variables
 * structure (sccpOmMIB_variables) should take place here.  By default the function also
 * registers the configuration handler and configuration store callbacks.
 *
 * Additional registrations that may be considered here are calls to regsiter_readfd(),
 * register_writefd() and register_exceptfd() for hooking into the snmpd event loop, but only when
 * used as a loadable module.  By default this function establishes a single file descriptor to
 * read, or upon which to handle exceptions.  Note that the snmpd only supports a maximum of 32
 * extneral file descriptors, so these should be used sparingly.
 *
 * When running as a loadable module, it is also necessary to hook into the snmpd event loop so that
 * the current request number can be deteremined.  This is accomplished by using a trick of the
 * external_signal_scheduled and external_signal_handler mechanism which is called on each event
 * loop when external_signal_scheduled is non-zero.  This is used to increment the sa_request value
 * on each snmpd event loop interation so that calls to MIB tree functions can determine whether
 * they belong to a fresh request or not (primarily for cacheing and possibly to clean up non-polled
 * file descriptors).
 */
void
init_sccpOmMIB(void)
{
	(void) snmpTrapOID_oid;
	DEBUGMSGTL(("sccpOmMIB", "initializing...  "));
	/* register ourselves with the agent to handle our mib tree */
	REGISTER_MIB("sccpOmMIB", sccpOmMIB_variables, variable7, sccpOmMIB_variables_oid);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_SHUTDOWN, term_sccpOmMIB, NULL);
	/* register our config handler(s) to deal with registrations */
	snmpd_register_config_handler("sccpOmMIB", parse_sccpOmMIB, NULL, "HELP STRING");
	snmpd_register_config_handler("sccpOmErrorsTable", parse_sccpOmErrorsTable, NULL, "HELP STRING");
	snmpd_register_config_handler("sccpOmMessageTable", parse_sccpOmMessageTable, NULL, "HELP STRING");
	snmpd_register_config_handler("sccpOmAccessibilityTable", parse_sccpOmAccessibilityTable, NULL, "HELP STRING");
	snmpd_register_config_handler("sccpOmUtilizationTable", parse_sccpOmUtilizationTable, NULL, "HELP STRING");
	snmpd_register_config_handler("sccpOm5MinHistoryTable", parse_sccpOm5MinHistoryTable, NULL, "HELP STRING");
	snmpd_register_config_handler("sccpOm15MinHistoryTable", parse_sccpOm15MinHistoryTable, NULL, "HELP STRING");
	snmpd_register_config_handler("sccpOm5MinSsnHistoryTable", parse_sccpOm5MinSsnHistoryTable, NULL, "HELP STRING");
	snmpd_register_config_handler("sccpOm15MinSsnHistoryTable", parse_sccpOm15MinSsnHistoryTable, NULL, "HELP STRING");

	/* we need to be called back later to store our data */
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_sccpOmMIB, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_sccpOmErrorsTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_sccpOmMessageTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_sccpOmAccessibilityTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_sccpOmUtilizationTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_sccpOm5MinHistoryTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_sccpOm15MinHistoryTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_sccpOm5MinSsnHistoryTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_sccpOm15MinSsnHistoryTable, NULL);

	/* place any other initialization junk you need here */
#if defined MODULE
	if (sa_readfd != 0) {
		register_readfd(sa_readfd, sccpOmMIB_fd_handler, (void *) 0);
		register_exceptfd(sa_readfd, sccpOmMIB_fd_handler, (void *) 1);
	}
#if defined MASTER
	sccpOmMIBold_signal_handler = external_signal_handler[0];
	external_signal_handler[0] = &sccpOmMIB_loop_handler;
#endif				/* defined MASTER */
#endif				/* defined MODULE */
	DEBUGMSGTL(("sccpOmMIB", "done.\n"));
}

/**
 * @fn void deinit_sccpOmMIB(void)
 * @brief deinitialization routine.
 *
 * This is called before the agent is unloaded.  At a minimum, deregistration of the MIB variables
 * structure (sccpOmMIB_variables) should take place here.  By default, the function also
 * deregisters the the configuration file handlers for the MIB variables and table rows.
 *
 * Additional deregistrations that may be required here are calls to unregister_readfd(),
 * unregister_writefd() and unregsiter_exceptfd() for unhooking from the snmpd event loop, but only
 * when used as a loadable module.  By default if a read file descriptor exists, it is unregistered.
 */
void
deinit_sccpOmMIB(void)
{
	DEBUGMSGTL(("sccpOmMIB", "deinitializating...  "));
#if defined MODULE
#if defined MASTER
	external_signal_handler[0] = sccpOmMIBold_signal_handler;
#endif				/* defined MASTER */
	if (sa_readfd != 0) {
		unregister_exceptfd(sa_readfd);
		unregister_readfd(sa_readfd);
		close(sa_readfd);
		sa_readfd = 0;
	}
#endif				/* defined MODULE */
	unregister_mib(sccpOmMIB_variables_oid, sizeof(sccpOmMIB_variables_oid) / sizeof(oid));
	snmpd_unregister_config_handler("sccpOmMIB");
	snmpd_unregister_config_handler("sccpOmErrorsTable");
	snmpd_unregister_config_handler("sccpOmMessageTable");
	snmpd_unregister_config_handler("sccpOmAccessibilityTable");
	snmpd_unregister_config_handler("sccpOmUtilizationTable");
	snmpd_unregister_config_handler("sccpOm5MinHistoryTable");
	snmpd_unregister_config_handler("sccpOm15MinHistoryTable");
	snmpd_unregister_config_handler("sccpOm5MinSsnHistoryTable");
	snmpd_unregister_config_handler("sccpOm15MinSsnHistoryTable");

	/* place any other de-initialization junk you need here */
	DEBUGMSGTL(("sccpOmMIB", "done.\n"));
}

int
term_sccpOmMIB(int majorID, int minorID, void *serverarg, void *clientarg)
{
	deinit_sccpOmMIB();
	return 0;
}

/**
 * @fn struct sccpOmMIB_data *sccpOmMIB_create(void)
 * @brief create a fresh data structure representing scalars in sccpOmMIB.
 *
 * Creates a new sccpOmMIB_data structure by allocating dynamic memory for the structure and
 * initializing the default values of scalars in sccpOmMIB.
 */
struct sccpOmMIB_data *
sccpOmMIB_create(void)
{
	struct sccpOmMIB_data *StorageNew = SNMP_MALLOC_STRUCT(sccpOmMIB_data);

	DEBUGMSGTL(("sccpOmMIB", "creating scalars...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default scalar values here into StorageNew */
		StorageNew->sccpOm5MinMaxIntervals = 96;
		StorageNew->sccpOm15MinMaxIntervals = 96;
		StorageNew->sccpOmpDefault = 8;
		StorageNew->sccpOmNrOfSubLevelsDefault = 4;
		StorageNew->sccpOmNrOfRestrictionLevelsDefault = 8;

	}
	DEBUGMSGTL(("sccpOmMIB", "done.\n"));
	return (StorageNew);
}

/**
 * @fn int sccpOmMIB_destroy(struct sccpOmMIB_data **thedata)
 * @param thedata pointer to the data structure in sccpOmMIB.
 * @brief delete a scalars structure from sccpOmMIB.
 *
 * Frees scalars that were previously removed from sccpOmMIB.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
sccpOmMIB_destroy(struct sccpOmMIB_data **thedata)
{
	struct sccpOmMIB_data *StorageDel;

	DEBUGMSGTL(("sccpOmMIB", "deleting scalars...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->sccpOm1stAndIntervalActivate);
		StorageDel->sccpOm1stAndIntervalActivateLen = 0;
		SNMP_FREE(StorageDel->sccpOm1stAndIntervalDeactivate);
		StorageDel->sccpOm1stAndIntervalDeactivateLen = 0;
		SNMP_FREE(StorageDel->sccpOm5MinActivate);
		StorageDel->sccpOm5MinActivateLen = 0;
		SNMP_FREE(StorageDel->sccpOm5MinDeaActivate);
		StorageDel->sccpOm5MinDeaActivateLen = 0;
		SNMP_FREE(StorageDel->sccpOm15MinActivate);
		StorageDel->sccpOm15MinActivateLen = 0;
		SNMP_FREE(StorageDel->sccpOm15MinDeaActivate);
		StorageDel->sccpOm15MinDeaActivateLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("sccpOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int sccpOmMIB_add(struct sccpOmMIB_data *thedata)
 * @param thedata the structure representing sccpOmMIB scalars.
 * @brief adds node to the sccpOmMIB scalar data set.
 *
 * Adds a scalar structure to the sccpOmMIB data set.  Note that this function is necessary even
 * when the scalar values are not peristent.
 */
int
sccpOmMIB_add(struct sccpOmMIB_data *thedata)
{
	DEBUGMSGTL(("sccpOmMIB", "adding data...  "));
	sccpOmMIBStorage = thedata;
	DEBUGMSGTL(("sccpOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_sccpOmMIB(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for sccpOmMIB entries.
 *
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case sccpOmMIB).  This routine is invoked by
 * UCD-SNMP to read the values of scalars in the MIB from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the MIB.  If there are no configured entries
 * in the configuration MIB, this function will simply not be called.
 */
void
parse_sccpOmMIB(const char *token, char *line)
{
	size_t tmpsize;
	struct sccpOmMIB_data *StorageTmp = sccpOmMIB_create();

	DEBUGMSGTL(("sccpOmMIB", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual scalars that are not persistent */
#if 0
	line = read_config_read_data(ASN_TIMETICKS, line, &StorageTmp->sccpOmDiscontinuityTime, &tmpsize);
	line = read_config_read_data(ASN_TIMETICKS, line, &StorageTmp->sccpOmTimeStamp, &tmpsize);
#endif
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->sccpOm5MinMaxIntervals, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->sccpOm15MinMaxIntervals, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->sccpOmpDefault, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->sccpOmNrOfSubLevelsDefault, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->sccpOmNrOfRestrictionLevelsDefault, &tmpsize);
	sccpOmMIB_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("sccpOmMIB", "done.\n"));
}

/*
 * store_sccpOmMIB(): stores .conf file entries needed to configure the mib.
 */
int
store_sccpOmMIB(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct sccpOmMIB_data *StorageTmp;

	DEBUGMSGTL(("sccpOmMIB", "storing data...  "));
	refresh_sccpOmMIB();
	if ((StorageTmp = sccpOmMIBStorage) == NULL) {
		DEBUGMSGTL(("sccpOmMIB", "error.\n"));
		return SNMPERR_GENERR;
	}
	(void) tmpsize;
	/* XXX: comment entire section if no scalars are persistent */
	{
		memset(line, 0, sizeof(line));
		strcat(line, "sccpOmMIB ");
		cptr = line + strlen(line);
		/* XXX: remove individual scalars that are not persistent */
#if 0
		cptr = read_config_store_data(ASN_TIMETICKS, cptr, &StorageTmp->sccpOmDiscontinuityTime, &tmpsize);
		cptr = read_config_store_data(ASN_TIMETICKS, cptr, &StorageTmp->sccpOmTimeStamp, &tmpsize);
#endif
		cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->sccpOm5MinMaxIntervals, &tmpsize);
		cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->sccpOm15MinMaxIntervals, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->sccpOmpDefault, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->sccpOmNrOfSubLevelsDefault, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->sccpOmNrOfRestrictionLevelsDefault, &tmpsize);
		snmpd_store_config(line);
	}
	DEBUGMSGTL(("sccpOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void refresh_sccpOmMIB(void)
 * @brief refresh the scalar values of sccpOmMIB.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a scalar has been requested).
 */
void
refresh_sccpOmMIB(void)
{
	if (sccpOmMIBStorage == NULL) {
		struct sccpOmMIB_data *StorageNew;

		if ((StorageNew = sccpOmMIB_create()) == NULL)
			return;
		sccpOmMIBStorage = StorageNew;
		sccpOmMIB_refresh = 1;
	}
	if (sccpOmMIB_refresh == 0)
		return;
	sccpOmMIB_refresh = 0;
	/* XXX: Update scalars as required here... */
}

/**
 * @fn u_char * var_sccpOmMIB(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @param vp a pointer to the entry in the variables table for the requested variable.
 * @param name the object identifier for which to find.
 * @param length the length of the object identifier.
 * @param exact whether the name is exact.
 * @param var_len a pointer to the length of the representation of the object.
 * @param write_method a pointer to a write method for the object.
 * @brief locate variables in sccpOmMIB.
 *
 * This function returns a pointer to a memory area that is static across the request that contains
 * the UCD-SNMP representation of the scalar (so that it may be used to read from for a GET,
 * GET-NEXT or GET-BULK request).  This returned pointer may be NULL, in which case the function is
 * telling UCD-SNMP that the scalar does not exist for reading; however, if write_method is
 * overwritten with a non-NULL value, the function is telling UCD-SNMP that the scalar exists for
 * writing.  Write-only objects can be effected in this way.
 */
u_char *
var_sccpOmMIB(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct sccpOmMIB_data *StorageTmp;

	if (header_generic(vp, name, length, exact, var_len, write_method) == MATCH_FAILED)
		return NULL;
	/* Refresh the MIB values if required. */
	refresh_sccpOmMIB();
	if ((StorageTmp = sccpOmMIBStorage) == NULL)
		return NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case SCCPOM1STANDINTERVALACTIVATE:
		*write_method = write_sccpOm1stAndIntervalActivate;
		*var_len = 0;	/* write-only */
		return (u_char *) NULL;	/* write-only */
	case SCCPOM1STANDINTERVALDEACTIVATE:
		*write_method = write_sccpOm1stAndIntervalDeactivate;
		*var_len = 0;	/* write-only */
		return (u_char *) NULL;	/* write-only */
	case SCCPOM5MINACTIVATE:
		*write_method = write_sccpOm5MinActivate;
		*var_len = 0;	/* write-only */
		return (u_char *) NULL;	/* write-only */
	case SCCPOM5MINDEAACTIVATE:
		*write_method = write_sccpOm5MinDeaActivate;
		*var_len = 0;	/* write-only */
		return (u_char *) NULL;	/* write-only */
	case SCCPOM15MINACTIVATE:
		*write_method = write_sccpOm15MinActivate;
		*var_len = 0;	/* write-only */
		return (u_char *) NULL;	/* write-only */
	case SCCPOM15MINDEAACTIVATE:
		*write_method = write_sccpOm15MinDeaActivate;
		*var_len = 0;	/* write-only */
		return (u_char *) NULL;	/* write-only */
	case SCCPOMDISCONTINUITYTIME:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmDiscontinuityTime);
		return (u_char *) &StorageTmp->sccpOmDiscontinuityTime;
	case SCCPOMTIMESTAMP:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmTimeStamp);
		return (u_char *) &StorageTmp->sccpOmTimeStamp;
	case SCCPOM5MINMAXINTERVALS:
		*write_method = write_sccpOm5MinMaxIntervals;
		*var_len = sizeof(StorageTmp->sccpOm5MinMaxIntervals);
		return (u_char *) &StorageTmp->sccpOm5MinMaxIntervals;
	case SCCPOM15MINMAXINTERVALS:
		*write_method = write_sccpOm15MinMaxIntervals;
		*var_len = sizeof(StorageTmp->sccpOm15MinMaxIntervals);
		return (u_char *) &StorageTmp->sccpOm15MinMaxIntervals;
	case SCCPOMPDEFAULT:
		*write_method = write_sccpOmpDefault;
		*var_len = sizeof(StorageTmp->sccpOmpDefault);
		return (u_char *) &StorageTmp->sccpOmpDefault;
	case SCCPOMNROFSUBLEVELSDEFAULT:
		*write_method = write_sccpOmNrOfSubLevelsDefault;
		*var_len = sizeof(StorageTmp->sccpOmNrOfSubLevelsDefault);
		return (u_char *) &StorageTmp->sccpOmNrOfSubLevelsDefault;
	case SCCPOMNROFRESTRICTIONLEVELSDEFAULT:
		*write_method = write_sccpOmNrOfRestrictionLevelsDefault;
		*var_len = sizeof(StorageTmp->sccpOmNrOfRestrictionLevelsDefault);
		return (u_char *) &StorageTmp->sccpOmNrOfRestrictionLevelsDefault;
	default:
		ERROR_MSG("");
	}
	return NULL;
}

/**
 * @fn struct sccpOmErrorsTable_data *sccpOmErrorsTable_create(void)
 * @brief create a fresh data structure representing a new row in the sccpOmErrorsTable table.
 *
 * Creates a new sccpOmErrorsTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct sccpOmErrorsTable_data *
sccpOmErrorsTable_create(void)
{
	struct sccpOmErrorsTable_data *StorageNew = SNMP_MALLOC_STRUCT(sccpOmErrorsTable_data);

	DEBUGMSGTL(("sccpOmErrorsTable", "creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */

	}
	DEBUGMSGTL(("sccpOmErrorsTable", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct sccpOmErrorsTable_data *sccpOmErrorsTable_duplicate(struct sccpOmErrorsTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicat a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct sccpOmErrorsTable_data *
sccpOmErrorsTable_duplicate(struct sccpOmErrorsTable_data *thedata)
{
	struct sccpOmErrorsTable_data *StorageNew = SNMP_MALLOC_STRUCT(sccpOmErrorsTable_data);

	DEBUGMSGTL(("sccpOmErrorsTable", "duplicating row...  "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("sccpOmErrorsTable", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	sccpOmErrorsTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int sccpOmErrorsTable_destroy(struct sccpOmErrorsTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
sccpOmErrorsTable_destroy(struct sccpOmErrorsTable_data **thedata)
{
	struct sccpOmErrorsTable_data *StorageDel;

	DEBUGMSGTL(("sccpOmErrorsTable", "deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("sccpOmErrorsTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int sccpOmErrorsTable_add(struct sccpOmErrorsTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the sccpOmErrorsTable table data set.
 *
 * Adds a table row structure to the sccpOmErrorsTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
sccpOmErrorsTable_add(struct sccpOmErrorsTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("sccpOmErrorsTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index the data */
	/* sccpNetworkEntityId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->sccpNetworkEntityId, sizeof(thedata->sccpNetworkEntityId));
	header_complex_add_data(&sccpOmErrorsTableStorage, vars, thedata);
	DEBUGMSGTL(("sccpOmErrorsTable", "registered an entry\n"));
	DEBUGMSGTL(("sccpOmErrorsTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int sccpOmErrorsTable_del(struct sccpOmErrorsTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the sccpOmErrorsTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
sccpOmErrorsTable_del(struct sccpOmErrorsTable_data *thedata)
{
	struct sccpOmErrorsTable_data *StorageDel;

	DEBUGMSGTL(("sccpOmErrorsTable", "deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(sccpOmErrorsTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&sccpOmErrorsTableStorage, hciptr);
	}
	DEBUGMSGTL(("sccpOmErrorsTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_sccpOmErrorsTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for sccpOmErrorsTable entries.
 *
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case sccpOmErrorsTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_sccpOmErrorsTable(const char *token, char *line)
{
	size_t tmpsize;
	struct sccpOmErrorsTable_data *StorageTmp = sccpOmErrorsTable_create();

	DEBUGMSGTL(("sccpOmErrorsTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->sccpNetworkEntityId, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->sccpOmNoTranslatorForAddress, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->sccpOmNoRuleForAddress, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->sccpOmPointCodeNotAvailable, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->sccpOmPointCodeCongested, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->sccpOmSubsystemUnavailable, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->sccpOmSubsystemCongested, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->sccpOmUnequippedSubsystem, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->sccpOmSyntaxErrorDetected, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->sccpOmRoutingFailure, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->sccpOmReassemblyTimeOut, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->sccpOmSegmentOutOfOrder, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->sccpOmNoReassemblySpace, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->sccpOmHopCounterViolation, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->sccpOmTooLargeForSegmentation, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->sccpOmReleaseFailure, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->sccpOmInactivityTimerExpiry, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->sccpOmProviderInitiatedReset, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->sccpOmProviderInitiatedRelease, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->sccpOmNoSegmentationSupport, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->sccpOmSegmentationFailure, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->sccpOmReassemblyFailure, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->sccpOmErrors5MinValidIntervals, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->sccpOmErrors15MinValidIntervals, &tmpsize);
	sccpOmErrorsTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("sccpOmErrorsTable", "done.\n"));
}

/*
 * store_sccpOmErrorsTable(): store configuraiton file for sccpOmErrorsTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_sccpOmErrorsTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct sccpOmErrorsTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("sccpOmErrorsTable", "storing data...  "));
	refresh_sccpOmErrorsTable();
	(void) tmpsize;
	for (hcindex = sccpOmErrorsTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct sccpOmErrorsTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "sccpOmErrorsTable ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->sccpNetworkEntityId, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->sccpOmNoTranslatorForAddress, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->sccpOmNoRuleForAddress, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->sccpOmPointCodeNotAvailable, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->sccpOmPointCodeCongested, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->sccpOmSubsystemUnavailable, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->sccpOmSubsystemCongested, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->sccpOmUnequippedSubsystem, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->sccpOmSyntaxErrorDetected, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->sccpOmRoutingFailure, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->sccpOmReassemblyTimeOut, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->sccpOmSegmentOutOfOrder, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->sccpOmNoReassemblySpace, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->sccpOmHopCounterViolation, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->sccpOmTooLargeForSegmentation, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->sccpOmReleaseFailure, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->sccpOmInactivityTimerExpiry, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->sccpOmProviderInitiatedReset, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->sccpOmProviderInitiatedRelease, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->sccpOmNoSegmentationSupport, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->sccpOmSegmentationFailure, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->sccpOmReassemblyFailure, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->sccpOmErrors5MinValidIntervals, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->sccpOmErrors15MinValidIntervals, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("sccpOmErrorsTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct sccpOmMessageTable_data *sccpOmMessageTable_create(void)
 * @brief create a fresh data structure representing a new row in the sccpOmMessageTable table.
 *
 * Creates a new sccpOmMessageTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct sccpOmMessageTable_data *
sccpOmMessageTable_create(void)
{
	struct sccpOmMessageTable_data *StorageNew = SNMP_MALLOC_STRUCT(sccpOmMessageTable_data);

	DEBUGMSGTL(("sccpOmMessageTable", "creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */

	}
	DEBUGMSGTL(("sccpOmMessageTable", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct sccpOmMessageTable_data *sccpOmMessageTable_duplicate(struct sccpOmMessageTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicat a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct sccpOmMessageTable_data *
sccpOmMessageTable_duplicate(struct sccpOmMessageTable_data *thedata)
{
	struct sccpOmMessageTable_data *StorageNew = SNMP_MALLOC_STRUCT(sccpOmMessageTable_data);

	DEBUGMSGTL(("sccpOmMessageTable", "duplicating row...  "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("sccpOmMessageTable", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	sccpOmMessageTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int sccpOmMessageTable_destroy(struct sccpOmMessageTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
sccpOmMessageTable_destroy(struct sccpOmMessageTable_data **thedata)
{
	struct sccpOmMessageTable_data *StorageDel;

	DEBUGMSGTL(("sccpOmMessageTable", "deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("sccpOmMessageTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int sccpOmMessageTable_add(struct sccpOmMessageTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the sccpOmMessageTable table data set.
 *
 * Adds a table row structure to the sccpOmMessageTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
sccpOmMessageTable_add(struct sccpOmMessageTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("sccpOmMessageTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index the data */
	/* sccpNetworkEntityId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->sccpNetworkEntityId, sizeof(thedata->sccpNetworkEntityId));
	header_complex_add_data(&sccpOmMessageTableStorage, vars, thedata);
	DEBUGMSGTL(("sccpOmMessageTable", "registered an entry\n"));
	DEBUGMSGTL(("sccpOmMessageTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int sccpOmMessageTable_del(struct sccpOmMessageTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the sccpOmMessageTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
sccpOmMessageTable_del(struct sccpOmMessageTable_data *thedata)
{
	struct sccpOmMessageTable_data *StorageDel;

	DEBUGMSGTL(("sccpOmMessageTable", "deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(sccpOmMessageTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&sccpOmMessageTableStorage, hciptr);
	}
	DEBUGMSGTL(("sccpOmMessageTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_sccpOmMessageTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for sccpOmMessageTable entries.
 *
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case sccpOmMessageTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_sccpOmMessageTable(const char *token, char *line)
{
	size_t tmpsize;
	struct sccpOmMessageTable_data *StorageTmp = sccpOmMessageTable_create();

	DEBUGMSGTL(("sccpOmMessageTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->sccpNetworkEntityId, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->sccpOmTotalMessagesHandled, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->sccpOmTotalMessagesLocal, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->sccpOmTotalMessagesGTT, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->sccpOmUDTMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->sccpOmUDTSMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->sccpOmUDTMessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->sccpOmUDTSMessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->sccpOmCRMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->sccpOmCREFMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->sccpOmCRMessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->sccpOmCREFMessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->sccpOmRSRMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->sccpOmRSRMessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->sccpOmERRMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->sccpOmERRMessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->sccpOmXUDTMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->sccpOmXUDTSMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->sccpOmXUDTMessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->sccpOmXUDTSMessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->sccpOmLUDTMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->sccpOmLUDTSMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->sccpOmLUDTMessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->sccpOmLUDTSMessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->sccpOmMessage5MinValidIntervals, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->sccpOmMessage15MinValidIntervals, &tmpsize);
	sccpOmMessageTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("sccpOmMessageTable", "done.\n"));
}

/*
 * store_sccpOmMessageTable(): store configuraiton file for sccpOmMessageTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_sccpOmMessageTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct sccpOmMessageTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("sccpOmMessageTable", "storing data...  "));
	refresh_sccpOmMessageTable();
	(void) tmpsize;
	for (hcindex = sccpOmMessageTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct sccpOmMessageTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "sccpOmMessageTable ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->sccpNetworkEntityId, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->sccpOmTotalMessagesHandled, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->sccpOmTotalMessagesLocal, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->sccpOmTotalMessagesGTT, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->sccpOmUDTMessagesSent, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->sccpOmUDTSMessagesSent, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->sccpOmUDTMessagesReceived, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->sccpOmUDTSMessagesReceived, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->sccpOmCRMessagesSent, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->sccpOmCREFMessagesSent, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->sccpOmCRMessagesReceived, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->sccpOmCREFMessagesReceived, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->sccpOmRSRMessagesSent, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->sccpOmRSRMessagesReceived, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->sccpOmERRMessagesSent, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->sccpOmERRMessagesReceived, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->sccpOmXUDTMessagesSent, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->sccpOmXUDTSMessagesSent, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->sccpOmXUDTMessagesReceived, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->sccpOmXUDTSMessagesReceived, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->sccpOmLUDTMessagesSent, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->sccpOmLUDTSMessagesSent, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->sccpOmLUDTMessagesReceived, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->sccpOmLUDTSMessagesReceived, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->sccpOmMessage5MinValidIntervals, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->sccpOmMessage15MinValidIntervals, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("sccpOmMessageTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct sccpOmAccessibilityTable_data *sccpOmAccessibilityTable_create(void)
 * @brief create a fresh data structure representing a new row in the sccpOmAccessibilityTable table.
 *
 * Creates a new sccpOmAccessibilityTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct sccpOmAccessibilityTable_data *
sccpOmAccessibilityTable_create(void)
{
	struct sccpOmAccessibilityTable_data *StorageNew = SNMP_MALLOC_STRUCT(sccpOmAccessibilityTable_data);

	DEBUGMSGTL(("sccpOmAccessibilityTable", "creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */

	}
	DEBUGMSGTL(("sccpOmAccessibilityTable", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct sccpOmAccessibilityTable_data *sccpOmAccessibilityTable_duplicate(struct sccpOmAccessibilityTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicat a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct sccpOmAccessibilityTable_data *
sccpOmAccessibilityTable_duplicate(struct sccpOmAccessibilityTable_data *thedata)
{
	struct sccpOmAccessibilityTable_data *StorageNew = SNMP_MALLOC_STRUCT(sccpOmAccessibilityTable_data);

	DEBUGMSGTL(("sccpOmAccessibilityTable", "duplicating row...  "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("sccpOmAccessibilityTable", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	sccpOmAccessibilityTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int sccpOmAccessibilityTable_destroy(struct sccpOmAccessibilityTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
sccpOmAccessibilityTable_destroy(struct sccpOmAccessibilityTable_data **thedata)
{
	struct sccpOmAccessibilityTable_data *StorageDel;

	DEBUGMSGTL(("sccpOmAccessibilityTable", "deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("sccpOmAccessibilityTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int sccpOmAccessibilityTable_add(struct sccpOmAccessibilityTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the sccpOmAccessibilityTable table data set.
 *
 * Adds a table row structure to the sccpOmAccessibilityTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
sccpOmAccessibilityTable_add(struct sccpOmAccessibilityTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("sccpOmAccessibilityTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index the data */
	/* sccpNetworkEntityId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->sccpNetworkEntityId, sizeof(thedata->sccpNetworkEntityId));
	header_complex_add_data(&sccpOmAccessibilityTableStorage, vars, thedata);
	DEBUGMSGTL(("sccpOmAccessibilityTable", "registered an entry\n"));
	DEBUGMSGTL(("sccpOmAccessibilityTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int sccpOmAccessibilityTable_del(struct sccpOmAccessibilityTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the sccpOmAccessibilityTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
sccpOmAccessibilityTable_del(struct sccpOmAccessibilityTable_data *thedata)
{
	struct sccpOmAccessibilityTable_data *StorageDel;

	DEBUGMSGTL(("sccpOmAccessibilityTable", "deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(sccpOmAccessibilityTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&sccpOmAccessibilityTableStorage, hciptr);
	}
	DEBUGMSGTL(("sccpOmAccessibilityTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_sccpOmAccessibilityTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for sccpOmAccessibilityTable entries.
 *
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case sccpOmAccessibilityTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_sccpOmAccessibilityTable(const char *token, char *line)
{
	size_t tmpsize;
	struct sccpOmAccessibilityTable_data *StorageTmp = sccpOmAccessibilityTable_create();

	DEBUGMSGTL(("sccpOmAccessibilityTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->sccpNetworkEntityId, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->sccpOmSSCMessageReceived, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->sccpOmSSPMessageReceived, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->sccpOmSccpUnavailableDuration, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->sccpOmAccessibility5MinValidIntervals, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->sccpOmAccessibility15MinValidIntervals, &tmpsize);
	sccpOmAccessibilityTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("sccpOmAccessibilityTable", "done.\n"));
}

/*
 * store_sccpOmAccessibilityTable(): store configuraiton file for sccpOmAccessibilityTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_sccpOmAccessibilityTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct sccpOmAccessibilityTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("sccpOmAccessibilityTable", "storing data...  "));
	refresh_sccpOmAccessibilityTable();
	(void) tmpsize;
	for (hcindex = sccpOmAccessibilityTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct sccpOmAccessibilityTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "sccpOmAccessibilityTable ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->sccpNetworkEntityId, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->sccpOmSSCMessageReceived, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->sccpOmSSPMessageReceived, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->sccpOmSccpUnavailableDuration, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->sccpOmAccessibility5MinValidIntervals, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->sccpOmAccessibility15MinValidIntervals, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("sccpOmAccessibilityTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct sccpOmUtilizationTable_data *sccpOmUtilizationTable_create(void)
 * @brief create a fresh data structure representing a new row in the sccpOmUtilizationTable table.
 *
 * Creates a new sccpOmUtilizationTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct sccpOmUtilizationTable_data *
sccpOmUtilizationTable_create(void)
{
	struct sccpOmUtilizationTable_data *StorageNew = SNMP_MALLOC_STRUCT(sccpOmUtilizationTable_data);

	DEBUGMSGTL(("sccpOmUtilizationTable", "creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */

	}
	DEBUGMSGTL(("sccpOmUtilizationTable", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct sccpOmUtilizationTable_data *sccpOmUtilizationTable_duplicate(struct sccpOmUtilizationTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicat a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct sccpOmUtilizationTable_data *
sccpOmUtilizationTable_duplicate(struct sccpOmUtilizationTable_data *thedata)
{
	struct sccpOmUtilizationTable_data *StorageNew = SNMP_MALLOC_STRUCT(sccpOmUtilizationTable_data);

	DEBUGMSGTL(("sccpOmUtilizationTable", "duplicating row...  "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("sccpOmUtilizationTable", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	sccpOmUtilizationTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int sccpOmUtilizationTable_destroy(struct sccpOmUtilizationTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
sccpOmUtilizationTable_destroy(struct sccpOmUtilizationTable_data **thedata)
{
	struct sccpOmUtilizationTable_data *StorageDel;

	DEBUGMSGTL(("sccpOmUtilizationTable", "deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("sccpOmUtilizationTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int sccpOmUtilizationTable_add(struct sccpOmUtilizationTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the sccpOmUtilizationTable table data set.
 *
 * Adds a table row structure to the sccpOmUtilizationTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
sccpOmUtilizationTable_add(struct sccpOmUtilizationTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("sccpOmUtilizationTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index the data */
	/* sccpNetworkEntityId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->sccpNetworkEntityId, sizeof(thedata->sccpNetworkEntityId));
	/* sccpOmSsn */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->sccpOmSsn, sizeof(thedata->sccpOmSsn));
	header_complex_add_data(&sccpOmUtilizationTableStorage, vars, thedata);
	DEBUGMSGTL(("sccpOmUtilizationTable", "registered an entry\n"));
	DEBUGMSGTL(("sccpOmUtilizationTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int sccpOmUtilizationTable_del(struct sccpOmUtilizationTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the sccpOmUtilizationTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
sccpOmUtilizationTable_del(struct sccpOmUtilizationTable_data *thedata)
{
	struct sccpOmUtilizationTable_data *StorageDel;

	DEBUGMSGTL(("sccpOmUtilizationTable", "deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(sccpOmUtilizationTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&sccpOmUtilizationTableStorage, hciptr);
	}
	DEBUGMSGTL(("sccpOmUtilizationTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_sccpOmUtilizationTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for sccpOmUtilizationTable entries.
 *
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case sccpOmUtilizationTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_sccpOmUtilizationTable(const char *token, char *line)
{
	size_t tmpsize;
	struct sccpOmUtilizationTable_data *StorageTmp = sccpOmUtilizationTable_create();

	DEBUGMSGTL(("sccpOmUtilizationTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->sccpNetworkEntityId, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->sccpOmSsn, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->sccpOmLXUDTMessagesOrigClass0, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->sccpOmLXUDTMessagesOrigClass1, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->sccpOmLXUDTMessagesTermClass0, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->sccpOmLXUDTMessagesTermClass1, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->sccpOmMessagesSentToBackup, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->sccpOmDT1MessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->sccpOmDT1MessagesSent, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->sccpOmDT2MessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->sccpOmDT2MessagesSent, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->sccpOmEDMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->sccpOmEDMessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->sccpOmUtilization5MinValidIntervals, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->sccpOmUtilization15MinValidIntervals, &tmpsize);
	sccpOmUtilizationTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("sccpOmUtilizationTable", "done.\n"));
}

/*
 * store_sccpOmUtilizationTable(): store configuraiton file for sccpOmUtilizationTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_sccpOmUtilizationTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct sccpOmUtilizationTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("sccpOmUtilizationTable", "storing data...  "));
	refresh_sccpOmUtilizationTable();
	(void) tmpsize;
	for (hcindex = sccpOmUtilizationTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct sccpOmUtilizationTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "sccpOmUtilizationTable ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->sccpNetworkEntityId, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->sccpOmSsn, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->sccpOmLXUDTMessagesOrigClass0, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->sccpOmLXUDTMessagesOrigClass1, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->sccpOmLXUDTMessagesTermClass0, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->sccpOmLXUDTMessagesTermClass1, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->sccpOmMessagesSentToBackup, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->sccpOmDT1MessagesReceived, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->sccpOmDT1MessagesSent, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->sccpOmDT2MessagesReceived, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->sccpOmDT2MessagesSent, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->sccpOmEDMessagesSent, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->sccpOmEDMessagesReceived, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->sccpOmUtilization5MinValidIntervals, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->sccpOmUtilization15MinValidIntervals, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("sccpOmUtilizationTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct sccpOm5MinHistoryTable_data *sccpOm5MinHistoryTable_create(void)
 * @brief create a fresh data structure representing a new row in the sccpOm5MinHistoryTable table.
 *
 * Creates a new sccpOm5MinHistoryTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct sccpOm5MinHistoryTable_data *
sccpOm5MinHistoryTable_create(void)
{
	struct sccpOm5MinHistoryTable_data *StorageNew = SNMP_MALLOC_STRUCT(sccpOm5MinHistoryTable_data);

	DEBUGMSGTL(("sccpOm5MinHistoryTable", "creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */

	}
	DEBUGMSGTL(("sccpOm5MinHistoryTable", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct sccpOm5MinHistoryTable_data *sccpOm5MinHistoryTable_duplicate(struct sccpOm5MinHistoryTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicat a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct sccpOm5MinHistoryTable_data *
sccpOm5MinHistoryTable_duplicate(struct sccpOm5MinHistoryTable_data *thedata)
{
	struct sccpOm5MinHistoryTable_data *StorageNew = SNMP_MALLOC_STRUCT(sccpOm5MinHistoryTable_data);

	DEBUGMSGTL(("sccpOm5MinHistoryTable", "duplicating row...  "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("sccpOm5MinHistoryTable", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	sccpOm5MinHistoryTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int sccpOm5MinHistoryTable_destroy(struct sccpOm5MinHistoryTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
sccpOm5MinHistoryTable_destroy(struct sccpOm5MinHistoryTable_data **thedata)
{
	struct sccpOm5MinHistoryTable_data *StorageDel;

	DEBUGMSGTL(("sccpOm5MinHistoryTable", "deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("sccpOm5MinHistoryTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int sccpOm5MinHistoryTable_add(struct sccpOm5MinHistoryTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the sccpOm5MinHistoryTable table data set.
 *
 * Adds a table row structure to the sccpOm5MinHistoryTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
sccpOm5MinHistoryTable_add(struct sccpOm5MinHistoryTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("sccpOm5MinHistoryTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index the data */
	/* sccpNetworkEntityId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->sccpNetworkEntityId, sizeof(thedata->sccpNetworkEntityId));
	/* sccpOmInterval */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->sccpOmInterval, sizeof(thedata->sccpOmInterval));
	header_complex_add_data(&sccpOm5MinHistoryTableStorage, vars, thedata);
	DEBUGMSGTL(("sccpOm5MinHistoryTable", "registered an entry\n"));
	DEBUGMSGTL(("sccpOm5MinHistoryTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int sccpOm5MinHistoryTable_del(struct sccpOm5MinHistoryTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the sccpOm5MinHistoryTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
sccpOm5MinHistoryTable_del(struct sccpOm5MinHistoryTable_data *thedata)
{
	struct sccpOm5MinHistoryTable_data *StorageDel;

	DEBUGMSGTL(("sccpOm5MinHistoryTable", "deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(sccpOm5MinHistoryTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&sccpOm5MinHistoryTableStorage, hciptr);
	}
	DEBUGMSGTL(("sccpOm5MinHistoryTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_sccpOm5MinHistoryTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for sccpOm5MinHistoryTable entries.
 *
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case sccpOm5MinHistoryTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_sccpOm5MinHistoryTable(const char *token, char *line)
{
	size_t tmpsize;
	struct sccpOm5MinHistoryTable_data *StorageTmp = sccpOm5MinHistoryTable_create();

	DEBUGMSGTL(("sccpOm5MinHistoryTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->sccpNetworkEntityId, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->sccpOmInterval, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinNoTranslatorForAddress, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinNoRuleForAddress, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinPointCodeNotAvailable, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinPointCodeCongested, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinSubsystemUnavailable, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinSubsystemCongested, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinUnequippedSubsystem, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinSyntaxErrorDetected, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinRoutingFailure, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinReassemblyTimeOut, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinSegmentOutOfOrder, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinNoReassemblySpace, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinHopCounterViolation, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinTooLargeForSegmentation, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinReleaseFailure, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinInactivityTimerExpiry, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinProviderInitiatedReset, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinProviderInitiatedRelease, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinNoSegmentationSupport, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinSegmentationFailure, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinReassemblyFailure, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinTotalMessagesHandled, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinTotalMessagesLocal, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinTotalMessagesGTT, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinUDTMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinUDTSMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinUDTMessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinUDTSMessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinCRMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinCREFMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinCRMessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinCREFMessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinRSRMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinRSRMessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinERRMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinERRMessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinXUDTMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinXUDTSMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinXUDTMessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinXUDTSMessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinLUDTMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinLUDTSMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinLUDTMessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinLUDTSMessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinSSCMessageReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinSSPMessageReceived, &tmpsize);
	sccpOm5MinHistoryTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("sccpOm5MinHistoryTable", "done.\n"));
}

/*
 * store_sccpOm5MinHistoryTable(): store configuraiton file for sccpOm5MinHistoryTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_sccpOm5MinHistoryTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct sccpOm5MinHistoryTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("sccpOm5MinHistoryTable", "storing data...  "));
	refresh_sccpOm5MinHistoryTable();
	(void) tmpsize;
	for (hcindex = sccpOm5MinHistoryTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct sccpOm5MinHistoryTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "sccpOm5MinHistoryTable ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->sccpNetworkEntityId, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->sccpOmInterval, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinNoTranslatorForAddress, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinNoRuleForAddress, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinPointCodeNotAvailable, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinPointCodeCongested, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinSubsystemUnavailable, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinSubsystemCongested, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinUnequippedSubsystem, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinSyntaxErrorDetected, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinRoutingFailure, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinReassemblyTimeOut, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinSegmentOutOfOrder, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinNoReassemblySpace, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinHopCounterViolation, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinTooLargeForSegmentation, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinReleaseFailure, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinInactivityTimerExpiry, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinProviderInitiatedReset, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinProviderInitiatedRelease, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinNoSegmentationSupport, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinSegmentationFailure, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinReassemblyFailure, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinTotalMessagesHandled, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinTotalMessagesLocal, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinTotalMessagesGTT, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinUDTMessagesSent, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinUDTSMessagesSent, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinUDTMessagesReceived, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinUDTSMessagesReceived, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinCRMessagesSent, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinCREFMessagesSent, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinCRMessagesReceived, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinCREFMessagesReceived, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinRSRMessagesSent, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinRSRMessagesReceived, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinERRMessagesSent, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinERRMessagesReceived, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinXUDTMessagesSent, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinXUDTSMessagesSent, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinXUDTMessagesReceived, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinXUDTSMessagesReceived, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinLUDTMessagesSent, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinLUDTSMessagesSent, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinLUDTMessagesReceived, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinLUDTSMessagesReceived, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinSSCMessageReceived, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinSSPMessageReceived, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("sccpOm5MinHistoryTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct sccpOm15MinHistoryTable_data *sccpOm15MinHistoryTable_create(void)
 * @brief create a fresh data structure representing a new row in the sccpOm15MinHistoryTable table.
 *
 * Creates a new sccpOm15MinHistoryTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct sccpOm15MinHistoryTable_data *
sccpOm15MinHistoryTable_create(void)
{
	struct sccpOm15MinHistoryTable_data *StorageNew = SNMP_MALLOC_STRUCT(sccpOm15MinHistoryTable_data);

	DEBUGMSGTL(("sccpOm15MinHistoryTable", "creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */

	}
	DEBUGMSGTL(("sccpOm15MinHistoryTable", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct sccpOm15MinHistoryTable_data *sccpOm15MinHistoryTable_duplicate(struct sccpOm15MinHistoryTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicat a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct sccpOm15MinHistoryTable_data *
sccpOm15MinHistoryTable_duplicate(struct sccpOm15MinHistoryTable_data *thedata)
{
	struct sccpOm15MinHistoryTable_data *StorageNew = SNMP_MALLOC_STRUCT(sccpOm15MinHistoryTable_data);

	DEBUGMSGTL(("sccpOm15MinHistoryTable", "duplicating row...  "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("sccpOm15MinHistoryTable", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	sccpOm15MinHistoryTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int sccpOm15MinHistoryTable_destroy(struct sccpOm15MinHistoryTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
sccpOm15MinHistoryTable_destroy(struct sccpOm15MinHistoryTable_data **thedata)
{
	struct sccpOm15MinHistoryTable_data *StorageDel;

	DEBUGMSGTL(("sccpOm15MinHistoryTable", "deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("sccpOm15MinHistoryTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int sccpOm15MinHistoryTable_add(struct sccpOm15MinHistoryTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the sccpOm15MinHistoryTable table data set.
 *
 * Adds a table row structure to the sccpOm15MinHistoryTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
sccpOm15MinHistoryTable_add(struct sccpOm15MinHistoryTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("sccpOm15MinHistoryTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index the data */
	/* sccpNetworkEntityId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->sccpNetworkEntityId, sizeof(thedata->sccpNetworkEntityId));
	/* sccpOmInterval */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->sccpOmInterval, sizeof(thedata->sccpOmInterval));
	header_complex_add_data(&sccpOm15MinHistoryTableStorage, vars, thedata);
	DEBUGMSGTL(("sccpOm15MinHistoryTable", "registered an entry\n"));
	DEBUGMSGTL(("sccpOm15MinHistoryTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int sccpOm15MinHistoryTable_del(struct sccpOm15MinHistoryTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the sccpOm15MinHistoryTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
sccpOm15MinHistoryTable_del(struct sccpOm15MinHistoryTable_data *thedata)
{
	struct sccpOm15MinHistoryTable_data *StorageDel;

	DEBUGMSGTL(("sccpOm15MinHistoryTable", "deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(sccpOm15MinHistoryTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&sccpOm15MinHistoryTableStorage, hciptr);
	}
	DEBUGMSGTL(("sccpOm15MinHistoryTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_sccpOm15MinHistoryTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for sccpOm15MinHistoryTable entries.
 *
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case sccpOm15MinHistoryTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_sccpOm15MinHistoryTable(const char *token, char *line)
{
	size_t tmpsize;
	struct sccpOm15MinHistoryTable_data *StorageTmp = sccpOm15MinHistoryTable_create();

	DEBUGMSGTL(("sccpOm15MinHistoryTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->sccpNetworkEntityId, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->sccpOmInterval, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinNoTranslatorForAddress, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinNoRuleForAddress, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinPointCodeNotAvailable, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinPointCodeCongested, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinSubsystemUnavailable, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinSubsystemCongested, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinUnequippedSubsystem, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinSyntaxErrorDetected, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinRoutingFailure, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinReassemblyTimeOut, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinSegmentOutOfOrder, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinNoReassemblySpace, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinHopCounterViolation, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinTooLargeForSegmentation, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinReleaseFailure, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinInactivityTimerExpiry, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinProviderInitiatedReset, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinProviderInitiatedRelease, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinNoSegmentationSupport, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinSegmentationFailure, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinReassemblyFailure, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinTotalMessagesHandled, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinTotalMessagesLocal, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinTotalMessagesGTT, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinUDTMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinUDTSMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinUDTMessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinUDTSMessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinCRMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinCREFMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinCRMessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinCREFMessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinRSRMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinRSRMessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinERRMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinERRMessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinXUDTMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinXUDTSMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinXUDTMessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinXUDTSMessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinLUDTMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinLUDTSMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinLUDTMessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinLUDTSMessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->sccpOm15MinSccpUnavailableDuration, &tmpsize);
	sccpOm15MinHistoryTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("sccpOm15MinHistoryTable", "done.\n"));
}

/*
 * store_sccpOm15MinHistoryTable(): store configuraiton file for sccpOm15MinHistoryTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_sccpOm15MinHistoryTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct sccpOm15MinHistoryTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("sccpOm15MinHistoryTable", "storing data...  "));
	refresh_sccpOm15MinHistoryTable();
	(void) tmpsize;
	for (hcindex = sccpOm15MinHistoryTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct sccpOm15MinHistoryTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "sccpOm15MinHistoryTable ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->sccpNetworkEntityId, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->sccpOmInterval, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinNoTranslatorForAddress, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinNoRuleForAddress, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinPointCodeNotAvailable, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinPointCodeCongested, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinSubsystemUnavailable, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinSubsystemCongested, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinUnequippedSubsystem, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinSyntaxErrorDetected, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinRoutingFailure, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinReassemblyTimeOut, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinSegmentOutOfOrder, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinNoReassemblySpace, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinHopCounterViolation, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinTooLargeForSegmentation, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinReleaseFailure, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinInactivityTimerExpiry, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinProviderInitiatedReset, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinProviderInitiatedRelease, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinNoSegmentationSupport, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinSegmentationFailure, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinReassemblyFailure, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinTotalMessagesHandled, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinTotalMessagesLocal, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinTotalMessagesGTT, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinUDTMessagesSent, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinUDTSMessagesSent, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinUDTMessagesReceived, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinUDTSMessagesReceived, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinCRMessagesSent, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinCREFMessagesSent, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinCRMessagesReceived, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinCREFMessagesReceived, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinRSRMessagesSent, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinRSRMessagesReceived, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinERRMessagesSent, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinERRMessagesReceived, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinXUDTMessagesSent, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinXUDTSMessagesSent, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinXUDTMessagesReceived, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinXUDTSMessagesReceived, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinLUDTMessagesSent, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinLUDTSMessagesSent, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinLUDTMessagesReceived, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinLUDTSMessagesReceived, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->sccpOm15MinSccpUnavailableDuration, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("sccpOm15MinHistoryTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct sccpOm5MinSsnHistoryTable_data *sccpOm5MinSsnHistoryTable_create(void)
 * @brief create a fresh data structure representing a new row in the sccpOm5MinSsnHistoryTable table.
 *
 * Creates a new sccpOm5MinSsnHistoryTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct sccpOm5MinSsnHistoryTable_data *
sccpOm5MinSsnHistoryTable_create(void)
{
	struct sccpOm5MinSsnHistoryTable_data *StorageNew = SNMP_MALLOC_STRUCT(sccpOm5MinSsnHistoryTable_data);

	DEBUGMSGTL(("sccpOm5MinSsnHistoryTable", "creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */

	}
	DEBUGMSGTL(("sccpOm5MinSsnHistoryTable", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct sccpOm5MinSsnHistoryTable_data *sccpOm5MinSsnHistoryTable_duplicate(struct sccpOm5MinSsnHistoryTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicat a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct sccpOm5MinSsnHistoryTable_data *
sccpOm5MinSsnHistoryTable_duplicate(struct sccpOm5MinSsnHistoryTable_data *thedata)
{
	struct sccpOm5MinSsnHistoryTable_data *StorageNew = SNMP_MALLOC_STRUCT(sccpOm5MinSsnHistoryTable_data);

	DEBUGMSGTL(("sccpOm5MinSsnHistoryTable", "duplicating row...  "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("sccpOm5MinSsnHistoryTable", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	sccpOm5MinSsnHistoryTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int sccpOm5MinSsnHistoryTable_destroy(struct sccpOm5MinSsnHistoryTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
sccpOm5MinSsnHistoryTable_destroy(struct sccpOm5MinSsnHistoryTable_data **thedata)
{
	struct sccpOm5MinSsnHistoryTable_data *StorageDel;

	DEBUGMSGTL(("sccpOm5MinSsnHistoryTable", "deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("sccpOm5MinSsnHistoryTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int sccpOm5MinSsnHistoryTable_add(struct sccpOm5MinSsnHistoryTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the sccpOm5MinSsnHistoryTable table data set.
 *
 * Adds a table row structure to the sccpOm5MinSsnHistoryTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
sccpOm5MinSsnHistoryTable_add(struct sccpOm5MinSsnHistoryTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("sccpOm5MinSsnHistoryTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index the data */
	/* sccpNetworkEntityId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->sccpNetworkEntityId, sizeof(thedata->sccpNetworkEntityId));
	/* sccpOmSsn */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->sccpOmSsn, sizeof(thedata->sccpOmSsn));
	/* sccpOmInterval */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->sccpOmInterval, sizeof(thedata->sccpOmInterval));
	header_complex_add_data(&sccpOm5MinSsnHistoryTableStorage, vars, thedata);
	DEBUGMSGTL(("sccpOm5MinSsnHistoryTable", "registered an entry\n"));
	DEBUGMSGTL(("sccpOm5MinSsnHistoryTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int sccpOm5MinSsnHistoryTable_del(struct sccpOm5MinSsnHistoryTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the sccpOm5MinSsnHistoryTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
sccpOm5MinSsnHistoryTable_del(struct sccpOm5MinSsnHistoryTable_data *thedata)
{
	struct sccpOm5MinSsnHistoryTable_data *StorageDel;

	DEBUGMSGTL(("sccpOm5MinSsnHistoryTable", "deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(sccpOm5MinSsnHistoryTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&sccpOm5MinSsnHistoryTableStorage, hciptr);
	}
	DEBUGMSGTL(("sccpOm5MinSsnHistoryTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_sccpOm5MinSsnHistoryTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for sccpOm5MinSsnHistoryTable entries.
 *
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case sccpOm5MinSsnHistoryTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_sccpOm5MinSsnHistoryTable(const char *token, char *line)
{
	size_t tmpsize;
	struct sccpOm5MinSsnHistoryTable_data *StorageTmp = sccpOm5MinSsnHistoryTable_create();

	DEBUGMSGTL(("sccpOm5MinSsnHistoryTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->sccpNetworkEntityId, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->sccpOmSsn, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->sccpOmInterval, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinLXUDTMessagesOrigClass0, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinLXUDTMessagesOrigClass1, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinLXUDTMessagesTermClass0, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinLXUDTMessagesTermClass1, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinMessagesSentToBackup, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinDT1MessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinDT1MessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinDT2MessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinDT2MessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinEDMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinEDMessagesReceived, &tmpsize);
	sccpOm5MinSsnHistoryTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("sccpOm5MinSsnHistoryTable", "done.\n"));
}

/*
 * store_sccpOm5MinSsnHistoryTable(): store configuraiton file for sccpOm5MinSsnHistoryTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_sccpOm5MinSsnHistoryTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct sccpOm5MinSsnHistoryTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("sccpOm5MinSsnHistoryTable", "storing data...  "));
	refresh_sccpOm5MinSsnHistoryTable();
	(void) tmpsize;
	for (hcindex = sccpOm5MinSsnHistoryTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct sccpOm5MinSsnHistoryTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "sccpOm5MinSsnHistoryTable ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->sccpNetworkEntityId, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->sccpOmSsn, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->sccpOmInterval, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinLXUDTMessagesOrigClass0, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinLXUDTMessagesOrigClass1, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinLXUDTMessagesTermClass0, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinLXUDTMessagesTermClass1, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinMessagesSentToBackup, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinDT1MessagesReceived, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinDT1MessagesSent, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinDT2MessagesReceived, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinDT2MessagesSent, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinEDMessagesSent, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinEDMessagesReceived, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("sccpOm5MinSsnHistoryTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct sccpOm15MinSsnHistoryTable_data *sccpOm15MinSsnHistoryTable_create(void)
 * @brief create a fresh data structure representing a new row in the sccpOm15MinSsnHistoryTable table.
 *
 * Creates a new sccpOm15MinSsnHistoryTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct sccpOm15MinSsnHistoryTable_data *
sccpOm15MinSsnHistoryTable_create(void)
{
	struct sccpOm15MinSsnHistoryTable_data *StorageNew = SNMP_MALLOC_STRUCT(sccpOm15MinSsnHistoryTable_data);

	DEBUGMSGTL(("sccpOm15MinSsnHistoryTable", "creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */

	}
	DEBUGMSGTL(("sccpOm15MinSsnHistoryTable", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct sccpOm15MinSsnHistoryTable_data *sccpOm15MinSsnHistoryTable_duplicate(struct sccpOm15MinSsnHistoryTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicat a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct sccpOm15MinSsnHistoryTable_data *
sccpOm15MinSsnHistoryTable_duplicate(struct sccpOm15MinSsnHistoryTable_data *thedata)
{
	struct sccpOm15MinSsnHistoryTable_data *StorageNew = SNMP_MALLOC_STRUCT(sccpOm15MinSsnHistoryTable_data);

	DEBUGMSGTL(("sccpOm15MinSsnHistoryTable", "duplicating row...  "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("sccpOm15MinSsnHistoryTable", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	sccpOm15MinSsnHistoryTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int sccpOm15MinSsnHistoryTable_destroy(struct sccpOm15MinSsnHistoryTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
sccpOm15MinSsnHistoryTable_destroy(struct sccpOm15MinSsnHistoryTable_data **thedata)
{
	struct sccpOm15MinSsnHistoryTable_data *StorageDel;

	DEBUGMSGTL(("sccpOm15MinSsnHistoryTable", "deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("sccpOm15MinSsnHistoryTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int sccpOm15MinSsnHistoryTable_add(struct sccpOm15MinSsnHistoryTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the sccpOm15MinSsnHistoryTable table data set.
 *
 * Adds a table row structure to the sccpOm15MinSsnHistoryTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
sccpOm15MinSsnHistoryTable_add(struct sccpOm15MinSsnHistoryTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("sccpOm15MinSsnHistoryTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index the data */
	/* sccpNetworkEntityId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->sccpNetworkEntityId, sizeof(thedata->sccpNetworkEntityId));
	/* sccpOmSsn */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->sccpOmSsn, sizeof(thedata->sccpOmSsn));
	/* sccpOmInterval */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->sccpOmInterval, sizeof(thedata->sccpOmInterval));
	header_complex_add_data(&sccpOm15MinSsnHistoryTableStorage, vars, thedata);
	DEBUGMSGTL(("sccpOm15MinSsnHistoryTable", "registered an entry\n"));
	DEBUGMSGTL(("sccpOm15MinSsnHistoryTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int sccpOm15MinSsnHistoryTable_del(struct sccpOm15MinSsnHistoryTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the sccpOm15MinSsnHistoryTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
sccpOm15MinSsnHistoryTable_del(struct sccpOm15MinSsnHistoryTable_data *thedata)
{
	struct sccpOm15MinSsnHistoryTable_data *StorageDel;

	DEBUGMSGTL(("sccpOm15MinSsnHistoryTable", "deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(sccpOm15MinSsnHistoryTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&sccpOm15MinSsnHistoryTableStorage, hciptr);
	}
	DEBUGMSGTL(("sccpOm15MinSsnHistoryTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_sccpOm15MinSsnHistoryTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for sccpOm15MinSsnHistoryTable entries.
 *
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case sccpOm15MinSsnHistoryTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_sccpOm15MinSsnHistoryTable(const char *token, char *line)
{
	size_t tmpsize;
	struct sccpOm15MinSsnHistoryTable_data *StorageTmp = sccpOm15MinSsnHistoryTable_create();

	DEBUGMSGTL(("sccpOm15MinSsnHistoryTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->sccpNetworkEntityId, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->sccpOmSsn, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->sccpOmInterval, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinLXUDTMessagesOrigClass0, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinLXUDTMessagesOrigClass1, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinLXUDTMessagesTermClass0, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinLXUDTMessagesTermClass1, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinMessagesSentToBackup, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinDT1MessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinDT1MessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinDT2MessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinDT2MessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinEDMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinEDMessagesReceived, &tmpsize);
	sccpOm15MinSsnHistoryTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("sccpOm15MinSsnHistoryTable", "done.\n"));
}

/*
 * store_sccpOm15MinSsnHistoryTable(): store configuraiton file for sccpOm15MinSsnHistoryTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_sccpOm15MinSsnHistoryTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct sccpOm15MinSsnHistoryTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("sccpOm15MinSsnHistoryTable", "storing data...  "));
	refresh_sccpOm15MinSsnHistoryTable();
	(void) tmpsize;
	for (hcindex = sccpOm15MinSsnHistoryTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct sccpOm15MinSsnHistoryTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "sccpOm15MinSsnHistoryTable ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->sccpNetworkEntityId, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->sccpOmSsn, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->sccpOmInterval, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinLXUDTMessagesOrigClass0, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinLXUDTMessagesOrigClass1, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinLXUDTMessagesTermClass0, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinLXUDTMessagesTermClass1, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinMessagesSentToBackup, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinDT1MessagesReceived, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinDT1MessagesSent, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinDT2MessagesReceived, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinDT2MessagesSent, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinEDMessagesSent, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinEDMessagesReceived, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("sccpOm15MinSsnHistoryTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void refresh_sccpOmErrorsTable(void)
 * @brief refresh the scalar values of the sccpOmErrorsTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_sccpOmErrorsTable(void)
{
	if (sccpOmErrorsTable_refresh == 0)
		return;
	sccpOmErrorsTable_refresh = 0;
	/* XXX: Here, update the table as required... */
}

/**
 * @fn void refresh_sccpOmErrorsTable_row(struct sccpOmErrorsTable_data *StorageTmp)
 * @brief refresh the contents of the sccpOmErrorsTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
void
refresh_sccpOmErrorsTable_row(struct sccpOmErrorsTable_data *StorageTmp)
{
	if (StorageTmp->sccpOmErrorsTable_request == sa_request)
		return;
	StorageTmp->sccpOmErrorsTable_request = sa_request;
}

/**
 * @fn u_char *var_sccpOmErrorsTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in sccpOmErrorsTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_sccpOmMIB above.
 */
u_char *
var_sccpOmErrorsTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct sccpOmErrorsTable_data *StorageTmp = NULL;

	DEBUGMSGTL(("sccpOmMIB", "var_sccpOmErrorsTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_sccpOmErrorsTable();
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	if ((StorageTmp = header_complex(sccpOmErrorsTableStorage, vp, name, length, exact, var_len, write_method)) == NULL)
		return NULL;
	refresh_sccpOmErrorsTable_row(StorageTmp);
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case SCCPOMNOTRANSLATORFORADDRESS:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmNoTranslatorForAddress);
		return (u_char *) &StorageTmp->sccpOmNoTranslatorForAddress;
	case SCCPOMNORULEFORADDRESS:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmNoRuleForAddress);
		return (u_char *) &StorageTmp->sccpOmNoRuleForAddress;
	case SCCPOMPOINTCODENOTAVAILABLE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmPointCodeNotAvailable);
		return (u_char *) &StorageTmp->sccpOmPointCodeNotAvailable;
	case SCCPOMPOINTCODECONGESTED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmPointCodeCongested);
		return (u_char *) &StorageTmp->sccpOmPointCodeCongested;
	case SCCPOMSUBSYSTEMUNAVAILABLE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmSubsystemUnavailable);
		return (u_char *) &StorageTmp->sccpOmSubsystemUnavailable;
	case SCCPOMSUBSYSTEMCONGESTED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmSubsystemCongested);
		return (u_char *) &StorageTmp->sccpOmSubsystemCongested;
	case SCCPOMUNEQUIPPEDSUBSYSTEM:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmUnequippedSubsystem);
		return (u_char *) &StorageTmp->sccpOmUnequippedSubsystem;
	case SCCPOMSYNTAXERRORDETECTED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmSyntaxErrorDetected);
		return (u_char *) &StorageTmp->sccpOmSyntaxErrorDetected;
	case SCCPOMROUTINGFAILURE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmRoutingFailure);
		return (u_char *) &StorageTmp->sccpOmRoutingFailure;
	case SCCPOMREASSEMBLYTIMEOUT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmReassemblyTimeOut);
		return (u_char *) &StorageTmp->sccpOmReassemblyTimeOut;
	case SCCPOMSEGMENTOUTOFORDER:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmSegmentOutOfOrder);
		return (u_char *) &StorageTmp->sccpOmSegmentOutOfOrder;
	case SCCPOMNOREASSEMBLYSPACE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmNoReassemblySpace);
		return (u_char *) &StorageTmp->sccpOmNoReassemblySpace;
	case SCCPOMHOPCOUNTERVIOLATION:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmHopCounterViolation);
		return (u_char *) &StorageTmp->sccpOmHopCounterViolation;
	case SCCPOMTOOLARGEFORSEGMENTATION:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmTooLargeForSegmentation);
		return (u_char *) &StorageTmp->sccpOmTooLargeForSegmentation;
	case SCCPOMRELEASEFAILURE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmReleaseFailure);
		return (u_char *) &StorageTmp->sccpOmReleaseFailure;
	case SCCPOMINACTIVITYTIMEREXPIRY:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmInactivityTimerExpiry);
		return (u_char *) &StorageTmp->sccpOmInactivityTimerExpiry;
	case SCCPOMPROVIDERINITIATEDRESET:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmProviderInitiatedReset);
		return (u_char *) &StorageTmp->sccpOmProviderInitiatedReset;
	case SCCPOMPROVIDERINITIATEDRELEASE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmProviderInitiatedRelease);
		return (u_char *) &StorageTmp->sccpOmProviderInitiatedRelease;
	case SCCPOMNOSEGMENTATIONSUPPORT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmNoSegmentationSupport);
		return (u_char *) &StorageTmp->sccpOmNoSegmentationSupport;
	case SCCPOMSEGMENTATIONFAILURE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmSegmentationFailure);
		return (u_char *) &StorageTmp->sccpOmSegmentationFailure;
	case SCCPOMREASSEMBLYFAILURE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmReassemblyFailure);
		return (u_char *) &StorageTmp->sccpOmReassemblyFailure;
	case SCCPOMERRORS5MINVALIDINTERVALS:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmErrors5MinValidIntervals);
		return (u_char *) &StorageTmp->sccpOmErrors5MinValidIntervals;
	case SCCPOMERRORS15MINVALIDINTERVALS:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmErrors15MinValidIntervals);
		return (u_char *) &StorageTmp->sccpOmErrors15MinValidIntervals;
	default:
		ERROR_MSG("");
	}
	return NULL;
}

/**
 * @fn void refresh_sccpOmMessageTable(void)
 * @brief refresh the scalar values of the sccpOmMessageTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_sccpOmMessageTable(void)
{
	if (sccpOmMessageTable_refresh == 0)
		return;
	sccpOmMessageTable_refresh = 0;
	/* XXX: Here, update the table as required... */
}

/**
 * @fn void refresh_sccpOmMessageTable_row(struct sccpOmMessageTable_data *StorageTmp)
 * @brief refresh the contents of the sccpOmMessageTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
void
refresh_sccpOmMessageTable_row(struct sccpOmMessageTable_data *StorageTmp)
{
	if (StorageTmp->sccpOmMessageTable_request == sa_request)
		return;
	StorageTmp->sccpOmMessageTable_request = sa_request;
}

/**
 * @fn u_char *var_sccpOmMessageTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in sccpOmMessageTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_sccpOmMIB above.
 */
u_char *
var_sccpOmMessageTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct sccpOmMessageTable_data *StorageTmp = NULL;

	DEBUGMSGTL(("sccpOmMIB", "var_sccpOmMessageTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_sccpOmMessageTable();
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	if ((StorageTmp = header_complex(sccpOmMessageTableStorage, vp, name, length, exact, var_len, write_method)) == NULL)
		return NULL;
	refresh_sccpOmMessageTable_row(StorageTmp);
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case SCCPOMTOTALMESSAGESHANDLED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmTotalMessagesHandled);
		return (u_char *) &StorageTmp->sccpOmTotalMessagesHandled;
	case SCCPOMTOTALMESSAGESLOCAL:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmTotalMessagesLocal);
		return (u_char *) &StorageTmp->sccpOmTotalMessagesLocal;
	case SCCPOMTOTALMESSAGESGTT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmTotalMessagesGTT);
		return (u_char *) &StorageTmp->sccpOmTotalMessagesGTT;
	case SCCPOMUDTMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmUDTMessagesSent);
		return (u_char *) &StorageTmp->sccpOmUDTMessagesSent;
	case SCCPOMUDTSMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmUDTSMessagesSent);
		return (u_char *) &StorageTmp->sccpOmUDTSMessagesSent;
	case SCCPOMUDTMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmUDTMessagesReceived);
		return (u_char *) &StorageTmp->sccpOmUDTMessagesReceived;
	case SCCPOMUDTSMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmUDTSMessagesReceived);
		return (u_char *) &StorageTmp->sccpOmUDTSMessagesReceived;
	case SCCPOMCRMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmCRMessagesSent);
		return (u_char *) &StorageTmp->sccpOmCRMessagesSent;
	case SCCPOMCREFMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmCREFMessagesSent);
		return (u_char *) &StorageTmp->sccpOmCREFMessagesSent;
	case SCCPOMCRMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmCRMessagesReceived);
		return (u_char *) &StorageTmp->sccpOmCRMessagesReceived;
	case SCCPOMCREFMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmCREFMessagesReceived);
		return (u_char *) &StorageTmp->sccpOmCREFMessagesReceived;
	case SCCPOMRSRMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmRSRMessagesSent);
		return (u_char *) &StorageTmp->sccpOmRSRMessagesSent;
	case SCCPOMRSRMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmRSRMessagesReceived);
		return (u_char *) &StorageTmp->sccpOmRSRMessagesReceived;
	case SCCPOMERRMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmERRMessagesSent);
		return (u_char *) &StorageTmp->sccpOmERRMessagesSent;
	case SCCPOMERRMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmERRMessagesReceived);
		return (u_char *) &StorageTmp->sccpOmERRMessagesReceived;
	case SCCPOMXUDTMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmXUDTMessagesSent);
		return (u_char *) &StorageTmp->sccpOmXUDTMessagesSent;
	case SCCPOMXUDTSMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmXUDTSMessagesSent);
		return (u_char *) &StorageTmp->sccpOmXUDTSMessagesSent;
	case SCCPOMXUDTMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmXUDTMessagesReceived);
		return (u_char *) &StorageTmp->sccpOmXUDTMessagesReceived;
	case SCCPOMXUDTSMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmXUDTSMessagesReceived);
		return (u_char *) &StorageTmp->sccpOmXUDTSMessagesReceived;
	case SCCPOMLUDTMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmLUDTMessagesSent);
		return (u_char *) &StorageTmp->sccpOmLUDTMessagesSent;
	case SCCPOMLUDTSMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmLUDTSMessagesSent);
		return (u_char *) &StorageTmp->sccpOmLUDTSMessagesSent;
	case SCCPOMLUDTMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmLUDTMessagesReceived);
		return (u_char *) &StorageTmp->sccpOmLUDTMessagesReceived;
	case SCCPOMLUDTSMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmLUDTSMessagesReceived);
		return (u_char *) &StorageTmp->sccpOmLUDTSMessagesReceived;
	case SCCPOMMESSAGE5MINVALIDINTERVALS:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmMessage5MinValidIntervals);
		return (u_char *) &StorageTmp->sccpOmMessage5MinValidIntervals;
	case SCCPOMMESSAGE15MINVALIDINTERVALS:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmMessage15MinValidIntervals);
		return (u_char *) &StorageTmp->sccpOmMessage15MinValidIntervals;
	default:
		ERROR_MSG("");
	}
	return NULL;
}

/**
 * @fn void refresh_sccpOmAccessibilityTable(void)
 * @brief refresh the scalar values of the sccpOmAccessibilityTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_sccpOmAccessibilityTable(void)
{
	if (sccpOmAccessibilityTable_refresh == 0)
		return;
	sccpOmAccessibilityTable_refresh = 0;
	/* XXX: Here, update the table as required... */
}

/**
 * @fn void refresh_sccpOmAccessibilityTable_row(struct sccpOmAccessibilityTable_data *StorageTmp)
 * @brief refresh the contents of the sccpOmAccessibilityTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
void
refresh_sccpOmAccessibilityTable_row(struct sccpOmAccessibilityTable_data *StorageTmp)
{
	if (StorageTmp->sccpOmAccessibilityTable_request == sa_request)
		return;
	StorageTmp->sccpOmAccessibilityTable_request = sa_request;
}

/**
 * @fn u_char *var_sccpOmAccessibilityTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in sccpOmAccessibilityTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_sccpOmMIB above.
 */
u_char *
var_sccpOmAccessibilityTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct sccpOmAccessibilityTable_data *StorageTmp = NULL;

	DEBUGMSGTL(("sccpOmMIB", "var_sccpOmAccessibilityTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_sccpOmAccessibilityTable();
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	if ((StorageTmp = header_complex(sccpOmAccessibilityTableStorage, vp, name, length, exact, var_len, write_method)) == NULL)
		return NULL;
	refresh_sccpOmAccessibilityTable_row(StorageTmp);
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case SCCPOMSSCMESSAGERECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmSSCMessageReceived);
		return (u_char *) &StorageTmp->sccpOmSSCMessageReceived;
	case SCCPOMSSPMESSAGERECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmSSPMessageReceived);
		return (u_char *) &StorageTmp->sccpOmSSPMessageReceived;
	case SCCPOMSCCPUNAVAILABLEDURATION:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmSccpUnavailableDuration);
		return (u_char *) &StorageTmp->sccpOmSccpUnavailableDuration;
	case SCCPOMACCESSIBILITY5MINVALIDINTERVALS:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmAccessibility5MinValidIntervals);
		return (u_char *) &StorageTmp->sccpOmAccessibility5MinValidIntervals;
	case SCCPOMACCESSIBILITY15MINVALIDINTERVALS:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmAccessibility15MinValidIntervals);
		return (u_char *) &StorageTmp->sccpOmAccessibility15MinValidIntervals;
	default:
		ERROR_MSG("");
	}
	return NULL;
}

/**
 * @fn void refresh_sccpOmUtilizationTable(void)
 * @brief refresh the scalar values of the sccpOmUtilizationTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_sccpOmUtilizationTable(void)
{
	if (sccpOmUtilizationTable_refresh == 0)
		return;
	sccpOmUtilizationTable_refresh = 0;
	/* XXX: Here, update the table as required... */
}

/**
 * @fn void refresh_sccpOmUtilizationTable_row(struct sccpOmUtilizationTable_data *StorageTmp)
 * @brief refresh the contents of the sccpOmUtilizationTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
void
refresh_sccpOmUtilizationTable_row(struct sccpOmUtilizationTable_data *StorageTmp)
{
	if (StorageTmp->sccpOmUtilizationTable_request == sa_request)
		return;
	StorageTmp->sccpOmUtilizationTable_request = sa_request;
}

/**
 * @fn u_char *var_sccpOmUtilizationTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in sccpOmUtilizationTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_sccpOmMIB above.
 */
u_char *
var_sccpOmUtilizationTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct sccpOmUtilizationTable_data *StorageTmp = NULL;

	DEBUGMSGTL(("sccpOmMIB", "var_sccpOmUtilizationTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_sccpOmUtilizationTable();
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	if ((StorageTmp = header_complex(sccpOmUtilizationTableStorage, vp, name, length, exact, var_len, write_method)) == NULL)
		return NULL;
	refresh_sccpOmUtilizationTable_row(StorageTmp);
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case SCCPOMLXUDTMESSAGESORIGCLASS0:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmLXUDTMessagesOrigClass0);
		return (u_char *) &StorageTmp->sccpOmLXUDTMessagesOrigClass0;
	case SCCPOMLXUDTMESSAGESORIGCLASS1:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmLXUDTMessagesOrigClass1);
		return (u_char *) &StorageTmp->sccpOmLXUDTMessagesOrigClass1;
	case SCCPOMLXUDTMESSAGESTERMCLASS0:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmLXUDTMessagesTermClass0);
		return (u_char *) &StorageTmp->sccpOmLXUDTMessagesTermClass0;
	case SCCPOMLXUDTMESSAGESTERMCLASS1:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmLXUDTMessagesTermClass1);
		return (u_char *) &StorageTmp->sccpOmLXUDTMessagesTermClass1;
	case SCCPOMMESSAGESSENTTOBACKUP:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmMessagesSentToBackup);
		return (u_char *) &StorageTmp->sccpOmMessagesSentToBackup;
	case SCCPOMDT1MESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmDT1MessagesReceived);
		return (u_char *) &StorageTmp->sccpOmDT1MessagesReceived;
	case SCCPOMDT1MESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmDT1MessagesSent);
		return (u_char *) &StorageTmp->sccpOmDT1MessagesSent;
	case SCCPOMDT2MESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmDT2MessagesReceived);
		return (u_char *) &StorageTmp->sccpOmDT2MessagesReceived;
	case SCCPOMDT2MESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmDT2MessagesSent);
		return (u_char *) &StorageTmp->sccpOmDT2MessagesSent;
	case SCCPOMEDMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmEDMessagesSent);
		return (u_char *) &StorageTmp->sccpOmEDMessagesSent;
	case SCCPOMEDMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmEDMessagesReceived);
		return (u_char *) &StorageTmp->sccpOmEDMessagesReceived;
	case SCCPOMUTILIZATION5MINVALIDINTERVALS:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmUtilization5MinValidIntervals);
		return (u_char *) &StorageTmp->sccpOmUtilization5MinValidIntervals;
	case SCCPOMUTILIZATION15MINVALIDINTERVALS:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmUtilization15MinValidIntervals);
		return (u_char *) &StorageTmp->sccpOmUtilization15MinValidIntervals;
	default:
		ERROR_MSG("");
	}
	return NULL;
}

/**
 * @fn void refresh_sccpOm5MinHistoryTable(void)
 * @brief refresh the scalar values of the sccpOm5MinHistoryTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_sccpOm5MinHistoryTable(void)
{
	if (sccpOm5MinHistoryTable_refresh == 0)
		return;
	sccpOm5MinHistoryTable_refresh = 0;
	/* XXX: Here, update the table as required... */
}

/**
 * @fn void refresh_sccpOm5MinHistoryTable_row(struct sccpOm5MinHistoryTable_data *StorageTmp)
 * @brief refresh the contents of the sccpOm5MinHistoryTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
void
refresh_sccpOm5MinHistoryTable_row(struct sccpOm5MinHistoryTable_data *StorageTmp)
{
	if (StorageTmp->sccpOm5MinHistoryTable_request == sa_request)
		return;
	StorageTmp->sccpOm5MinHistoryTable_request = sa_request;
}

/**
 * @fn u_char *var_sccpOm5MinHistoryTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in sccpOm5MinHistoryTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_sccpOmMIB above.
 */
u_char *
var_sccpOm5MinHistoryTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct sccpOm5MinHistoryTable_data *StorageTmp = NULL;

	DEBUGMSGTL(("sccpOmMIB", "var_sccpOm5MinHistoryTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_sccpOm5MinHistoryTable();
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	if ((StorageTmp = header_complex(sccpOm5MinHistoryTableStorage, vp, name, length, exact, var_len, write_method)) == NULL)
		return NULL;
	refresh_sccpOm5MinHistoryTable_row(StorageTmp);
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case SCCPOM5MINNOTRANSLATORFORADDRESS:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinNoTranslatorForAddress);
		return (u_char *) &StorageTmp->sccpOm5MinNoTranslatorForAddress;
	case SCCPOM5MINNORULEFORADDRESS:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinNoRuleForAddress);
		return (u_char *) &StorageTmp->sccpOm5MinNoRuleForAddress;
	case SCCPOM5MINPOINTCODENOTAVAILABLE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinPointCodeNotAvailable);
		return (u_char *) &StorageTmp->sccpOm5MinPointCodeNotAvailable;
	case SCCPOM5MINPOINTCODECONGESTED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinPointCodeCongested);
		return (u_char *) &StorageTmp->sccpOm5MinPointCodeCongested;
	case SCCPOM5MINSUBSYSTEMUNAVAILABLE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinSubsystemUnavailable);
		return (u_char *) &StorageTmp->sccpOm5MinSubsystemUnavailable;
	case SCCPOM5MINSUBSYSTEMCONGESTED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinSubsystemCongested);
		return (u_char *) &StorageTmp->sccpOm5MinSubsystemCongested;
	case SCCPOM5MINUNEQUIPPEDSUBSYSTEM:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinUnequippedSubsystem);
		return (u_char *) &StorageTmp->sccpOm5MinUnequippedSubsystem;
	case SCCPOM5MINSYNTAXERRORDETECTED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinSyntaxErrorDetected);
		return (u_char *) &StorageTmp->sccpOm5MinSyntaxErrorDetected;
	case SCCPOM5MINROUTINGFAILURE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinRoutingFailure);
		return (u_char *) &StorageTmp->sccpOm5MinRoutingFailure;
	case SCCPOM5MINREASSEMBLYTIMEOUT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinReassemblyTimeOut);
		return (u_char *) &StorageTmp->sccpOm5MinReassemblyTimeOut;
	case SCCPOM5MINSEGMENTOUTOFORDER:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinSegmentOutOfOrder);
		return (u_char *) &StorageTmp->sccpOm5MinSegmentOutOfOrder;
	case SCCPOM5MINNOREASSEMBLYSPACE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinNoReassemblySpace);
		return (u_char *) &StorageTmp->sccpOm5MinNoReassemblySpace;
	case SCCPOM5MINHOPCOUNTERVIOLATION:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinHopCounterViolation);
		return (u_char *) &StorageTmp->sccpOm5MinHopCounterViolation;
	case SCCPOM5MINTOOLARGEFORSEGMENTATION:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinTooLargeForSegmentation);
		return (u_char *) &StorageTmp->sccpOm5MinTooLargeForSegmentation;
	case SCCPOM5MINRELEASEFAILURE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinReleaseFailure);
		return (u_char *) &StorageTmp->sccpOm5MinReleaseFailure;
	case SCCPOM5MININACTIVITYTIMEREXPIRY:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinInactivityTimerExpiry);
		return (u_char *) &StorageTmp->sccpOm5MinInactivityTimerExpiry;
	case SCCPOM5MINPROVIDERINITIATEDRESET:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinProviderInitiatedReset);
		return (u_char *) &StorageTmp->sccpOm5MinProviderInitiatedReset;
	case SCCPOM5MINPROVIDERINITIATEDRELEASE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinProviderInitiatedRelease);
		return (u_char *) &StorageTmp->sccpOm5MinProviderInitiatedRelease;
	case SCCPOM5MINNOSEGMENTATIONSUPPORT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinNoSegmentationSupport);
		return (u_char *) &StorageTmp->sccpOm5MinNoSegmentationSupport;
	case SCCPOM5MINSEGMENTATIONFAILURE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinSegmentationFailure);
		return (u_char *) &StorageTmp->sccpOm5MinSegmentationFailure;
	case SCCPOM5MINREASSEMBLYFAILURE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinReassemblyFailure);
		return (u_char *) &StorageTmp->sccpOm5MinReassemblyFailure;
	case SCCPOM5MINTOTALMESSAGESHANDLED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinTotalMessagesHandled);
		return (u_char *) &StorageTmp->sccpOm5MinTotalMessagesHandled;
	case SCCPOM5MINTOTALMESSAGESLOCAL:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinTotalMessagesLocal);
		return (u_char *) &StorageTmp->sccpOm5MinTotalMessagesLocal;
	case SCCPOM5MINTOTALMESSAGESGTT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinTotalMessagesGTT);
		return (u_char *) &StorageTmp->sccpOm5MinTotalMessagesGTT;
	case SCCPOM5MINUDTMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinUDTMessagesSent);
		return (u_char *) &StorageTmp->sccpOm5MinUDTMessagesSent;
	case SCCPOM5MINUDTSMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinUDTSMessagesSent);
		return (u_char *) &StorageTmp->sccpOm5MinUDTSMessagesSent;
	case SCCPOM5MINUDTMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinUDTMessagesReceived);
		return (u_char *) &StorageTmp->sccpOm5MinUDTMessagesReceived;
	case SCCPOM5MINUDTSMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinUDTSMessagesReceived);
		return (u_char *) &StorageTmp->sccpOm5MinUDTSMessagesReceived;
	case SCCPOM5MINCRMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinCRMessagesSent);
		return (u_char *) &StorageTmp->sccpOm5MinCRMessagesSent;
	case SCCPOM5MINCREFMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinCREFMessagesSent);
		return (u_char *) &StorageTmp->sccpOm5MinCREFMessagesSent;
	case SCCPOM5MINCRMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinCRMessagesReceived);
		return (u_char *) &StorageTmp->sccpOm5MinCRMessagesReceived;
	case SCCPOM5MINCREFMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinCREFMessagesReceived);
		return (u_char *) &StorageTmp->sccpOm5MinCREFMessagesReceived;
	case SCCPOM5MINRSRMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinRSRMessagesSent);
		return (u_char *) &StorageTmp->sccpOm5MinRSRMessagesSent;
	case SCCPOM5MINRSRMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinRSRMessagesReceived);
		return (u_char *) &StorageTmp->sccpOm5MinRSRMessagesReceived;
	case SCCPOM5MINERRMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinERRMessagesSent);
		return (u_char *) &StorageTmp->sccpOm5MinERRMessagesSent;
	case SCCPOM5MINERRMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinERRMessagesReceived);
		return (u_char *) &StorageTmp->sccpOm5MinERRMessagesReceived;
	case SCCPOM5MINXUDTMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinXUDTMessagesSent);
		return (u_char *) &StorageTmp->sccpOm5MinXUDTMessagesSent;
	case SCCPOM5MINXUDTSMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinXUDTSMessagesSent);
		return (u_char *) &StorageTmp->sccpOm5MinXUDTSMessagesSent;
	case SCCPOM5MINXUDTMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinXUDTMessagesReceived);
		return (u_char *) &StorageTmp->sccpOm5MinXUDTMessagesReceived;
	case SCCPOM5MINXUDTSMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinXUDTSMessagesReceived);
		return (u_char *) &StorageTmp->sccpOm5MinXUDTSMessagesReceived;
	case SCCPOM5MINLUDTMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinLUDTMessagesSent);
		return (u_char *) &StorageTmp->sccpOm5MinLUDTMessagesSent;
	case SCCPOM5MINLUDTSMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinLUDTSMessagesSent);
		return (u_char *) &StorageTmp->sccpOm5MinLUDTSMessagesSent;
	case SCCPOM5MINLUDTMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinLUDTMessagesReceived);
		return (u_char *) &StorageTmp->sccpOm5MinLUDTMessagesReceived;
	case SCCPOM5MINLUDTSMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinLUDTSMessagesReceived);
		return (u_char *) &StorageTmp->sccpOm5MinLUDTSMessagesReceived;
	case SCCPOM5MINSSCMESSAGERECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinSSCMessageReceived);
		return (u_char *) &StorageTmp->sccpOm5MinSSCMessageReceived;
	case SCCPOM5MINSSPMESSAGERECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinSSPMessageReceived);
		return (u_char *) &StorageTmp->sccpOm5MinSSPMessageReceived;
	default:
		ERROR_MSG("");
	}
	return NULL;
}

/**
 * @fn void refresh_sccpOm15MinHistoryTable(void)
 * @brief refresh the scalar values of the sccpOm15MinHistoryTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_sccpOm15MinHistoryTable(void)
{
	if (sccpOm15MinHistoryTable_refresh == 0)
		return;
	sccpOm15MinHistoryTable_refresh = 0;
	/* XXX: Here, update the table as required... */
}

/**
 * @fn void refresh_sccpOm15MinHistoryTable_row(struct sccpOm15MinHistoryTable_data *StorageTmp)
 * @brief refresh the contents of the sccpOm15MinHistoryTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
void
refresh_sccpOm15MinHistoryTable_row(struct sccpOm15MinHistoryTable_data *StorageTmp)
{
	if (StorageTmp->sccpOm15MinHistoryTable_request == sa_request)
		return;
	StorageTmp->sccpOm15MinHistoryTable_request = sa_request;
}

/**
 * @fn u_char *var_sccpOm15MinHistoryTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in sccpOm15MinHistoryTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_sccpOmMIB above.
 */
u_char *
var_sccpOm15MinHistoryTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct sccpOm15MinHistoryTable_data *StorageTmp = NULL;

	DEBUGMSGTL(("sccpOmMIB", "var_sccpOm15MinHistoryTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_sccpOm15MinHistoryTable();
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	if ((StorageTmp = header_complex(sccpOm15MinHistoryTableStorage, vp, name, length, exact, var_len, write_method)) == NULL)
		return NULL;
	refresh_sccpOm15MinHistoryTable_row(StorageTmp);
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case SCCPOM15MINNOTRANSLATORFORADDRESS:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinNoTranslatorForAddress);
		return (u_char *) &StorageTmp->sccpOm15MinNoTranslatorForAddress;
	case SCCPOM15MINNORULEFORADDRESS:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinNoRuleForAddress);
		return (u_char *) &StorageTmp->sccpOm15MinNoRuleForAddress;
	case SCCPOM15MINPOINTCODENOTAVAILABLE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinPointCodeNotAvailable);
		return (u_char *) &StorageTmp->sccpOm15MinPointCodeNotAvailable;
	case SCCPOM15MINPOINTCODECONGESTED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinPointCodeCongested);
		return (u_char *) &StorageTmp->sccpOm15MinPointCodeCongested;
	case SCCPOM15MINSUBSYSTEMUNAVAILABLE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinSubsystemUnavailable);
		return (u_char *) &StorageTmp->sccpOm15MinSubsystemUnavailable;
	case SCCPOM15MINSUBSYSTEMCONGESTED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinSubsystemCongested);
		return (u_char *) &StorageTmp->sccpOm15MinSubsystemCongested;
	case SCCPOM15MINUNEQUIPPEDSUBSYSTEM:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinUnequippedSubsystem);
		return (u_char *) &StorageTmp->sccpOm15MinUnequippedSubsystem;
	case SCCPOM15MINSYNTAXERRORDETECTED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinSyntaxErrorDetected);
		return (u_char *) &StorageTmp->sccpOm15MinSyntaxErrorDetected;
	case SCCPOM15MINROUTINGFAILURE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinRoutingFailure);
		return (u_char *) &StorageTmp->sccpOm15MinRoutingFailure;
	case SCCPOM15MINREASSEMBLYTIMEOUT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinReassemblyTimeOut);
		return (u_char *) &StorageTmp->sccpOm15MinReassemblyTimeOut;
	case SCCPOM15MINSEGMENTOUTOFORDER:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinSegmentOutOfOrder);
		return (u_char *) &StorageTmp->sccpOm15MinSegmentOutOfOrder;
	case SCCPOM15MINNOREASSEMBLYSPACE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinNoReassemblySpace);
		return (u_char *) &StorageTmp->sccpOm15MinNoReassemblySpace;
	case SCCPOM15MINHOPCOUNTERVIOLATION:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinHopCounterViolation);
		return (u_char *) &StorageTmp->sccpOm15MinHopCounterViolation;
	case SCCPOM15MINTOOLARGEFORSEGMENTATION:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinTooLargeForSegmentation);
		return (u_char *) &StorageTmp->sccpOm15MinTooLargeForSegmentation;
	case SCCPOM15MINRELEASEFAILURE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinReleaseFailure);
		return (u_char *) &StorageTmp->sccpOm15MinReleaseFailure;
	case SCCPOM15MININACTIVITYTIMEREXPIRY:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinInactivityTimerExpiry);
		return (u_char *) &StorageTmp->sccpOm15MinInactivityTimerExpiry;
	case SCCPOM15MINPROVIDERINITIATEDRESET:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinProviderInitiatedReset);
		return (u_char *) &StorageTmp->sccpOm15MinProviderInitiatedReset;
	case SCCPOM15MINPROVIDERINITIATEDRELEASE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinProviderInitiatedRelease);
		return (u_char *) &StorageTmp->sccpOm15MinProviderInitiatedRelease;
	case SCCPOM15MINNOSEGMENTATIONSUPPORT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinNoSegmentationSupport);
		return (u_char *) &StorageTmp->sccpOm15MinNoSegmentationSupport;
	case SCCPOM15MINSEGMENTATIONFAILURE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinSegmentationFailure);
		return (u_char *) &StorageTmp->sccpOm15MinSegmentationFailure;
	case SCCPOM15MINREASSEMBLYFAILURE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinReassemblyFailure);
		return (u_char *) &StorageTmp->sccpOm15MinReassemblyFailure;
	case SCCPOM15MINTOTALMESSAGESHANDLED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinTotalMessagesHandled);
		return (u_char *) &StorageTmp->sccpOm15MinTotalMessagesHandled;
	case SCCPOM15MINTOTALMESSAGESLOCAL:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinTotalMessagesLocal);
		return (u_char *) &StorageTmp->sccpOm15MinTotalMessagesLocal;
	case SCCPOM15MINTOTALMESSAGESGTT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinTotalMessagesGTT);
		return (u_char *) &StorageTmp->sccpOm15MinTotalMessagesGTT;
	case SCCPOM15MINUDTMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinUDTMessagesSent);
		return (u_char *) &StorageTmp->sccpOm15MinUDTMessagesSent;
	case SCCPOM15MINUDTSMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinUDTSMessagesSent);
		return (u_char *) &StorageTmp->sccpOm15MinUDTSMessagesSent;
	case SCCPOM15MINUDTMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinUDTMessagesReceived);
		return (u_char *) &StorageTmp->sccpOm15MinUDTMessagesReceived;
	case SCCPOM15MINUDTSMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinUDTSMessagesReceived);
		return (u_char *) &StorageTmp->sccpOm15MinUDTSMessagesReceived;
	case SCCPOM15MINCRMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinCRMessagesSent);
		return (u_char *) &StorageTmp->sccpOm15MinCRMessagesSent;
	case SCCPOM15MINCREFMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinCREFMessagesSent);
		return (u_char *) &StorageTmp->sccpOm15MinCREFMessagesSent;
	case SCCPOM15MINCRMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinCRMessagesReceived);
		return (u_char *) &StorageTmp->sccpOm15MinCRMessagesReceived;
	case SCCPOM15MINCREFMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinCREFMessagesReceived);
		return (u_char *) &StorageTmp->sccpOm15MinCREFMessagesReceived;
	case SCCPOM15MINRSRMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinRSRMessagesSent);
		return (u_char *) &StorageTmp->sccpOm15MinRSRMessagesSent;
	case SCCPOM15MINRSRMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinRSRMessagesReceived);
		return (u_char *) &StorageTmp->sccpOm15MinRSRMessagesReceived;
	case SCCPOM15MINERRMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinERRMessagesSent);
		return (u_char *) &StorageTmp->sccpOm15MinERRMessagesSent;
	case SCCPOM15MINERRMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinERRMessagesReceived);
		return (u_char *) &StorageTmp->sccpOm15MinERRMessagesReceived;
	case SCCPOM15MINXUDTMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinXUDTMessagesSent);
		return (u_char *) &StorageTmp->sccpOm15MinXUDTMessagesSent;
	case SCCPOM15MINXUDTSMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinXUDTSMessagesSent);
		return (u_char *) &StorageTmp->sccpOm15MinXUDTSMessagesSent;
	case SCCPOM15MINXUDTMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinXUDTMessagesReceived);
		return (u_char *) &StorageTmp->sccpOm15MinXUDTMessagesReceived;
	case SCCPOM15MINXUDTSMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinXUDTSMessagesReceived);
		return (u_char *) &StorageTmp->sccpOm15MinXUDTSMessagesReceived;
	case SCCPOM15MINLUDTMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinLUDTMessagesSent);
		return (u_char *) &StorageTmp->sccpOm15MinLUDTMessagesSent;
	case SCCPOM15MINLUDTSMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinLUDTSMessagesSent);
		return (u_char *) &StorageTmp->sccpOm15MinLUDTSMessagesSent;
	case SCCPOM15MINLUDTMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinLUDTMessagesReceived);
		return (u_char *) &StorageTmp->sccpOm15MinLUDTMessagesReceived;
	case SCCPOM15MINLUDTSMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinLUDTSMessagesReceived);
		return (u_char *) &StorageTmp->sccpOm15MinLUDTSMessagesReceived;
	case SCCPOM15MINSCCPUNAVAILABLEDURATION:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinSccpUnavailableDuration);
		return (u_char *) &StorageTmp->sccpOm15MinSccpUnavailableDuration;
	default:
		ERROR_MSG("");
	}
	return NULL;
}

/**
 * @fn void refresh_sccpOm5MinSsnHistoryTable(void)
 * @brief refresh the scalar values of the sccpOm5MinSsnHistoryTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_sccpOm5MinSsnHistoryTable(void)
{
	if (sccpOm5MinSsnHistoryTable_refresh == 0)
		return;
	sccpOm5MinSsnHistoryTable_refresh = 0;
	/* XXX: Here, update the table as required... */
}

/**
 * @fn void refresh_sccpOm5MinSsnHistoryTable_row(struct sccpOm5MinSsnHistoryTable_data *StorageTmp)
 * @brief refresh the contents of the sccpOm5MinSsnHistoryTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
void
refresh_sccpOm5MinSsnHistoryTable_row(struct sccpOm5MinSsnHistoryTable_data *StorageTmp)
{
	if (StorageTmp->sccpOm5MinSsnHistoryTable_request == sa_request)
		return;
	StorageTmp->sccpOm5MinSsnHistoryTable_request = sa_request;
}

/**
 * @fn u_char *var_sccpOm5MinSsnHistoryTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in sccpOm5MinSsnHistoryTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_sccpOmMIB above.
 */
u_char *
var_sccpOm5MinSsnHistoryTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct sccpOm5MinSsnHistoryTable_data *StorageTmp = NULL;

	DEBUGMSGTL(("sccpOmMIB", "var_sccpOm5MinSsnHistoryTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_sccpOm5MinSsnHistoryTable();
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	if ((StorageTmp = header_complex(sccpOm5MinSsnHistoryTableStorage, vp, name, length, exact, var_len, write_method)) == NULL)
		return NULL;
	refresh_sccpOm5MinSsnHistoryTable_row(StorageTmp);
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case SCCPOM5MINLXUDTMESSAGESORIGCLASS0:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinLXUDTMessagesOrigClass0);
		return (u_char *) &StorageTmp->sccpOm5MinLXUDTMessagesOrigClass0;
	case SCCPOM5MINLXUDTMESSAGESORIGCLASS1:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinLXUDTMessagesOrigClass1);
		return (u_char *) &StorageTmp->sccpOm5MinLXUDTMessagesOrigClass1;
	case SCCPOM5MINLXUDTMESSAGESTERMCLASS0:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinLXUDTMessagesTermClass0);
		return (u_char *) &StorageTmp->sccpOm5MinLXUDTMessagesTermClass0;
	case SCCPOM5MINLXUDTMESSAGESTERMCLASS1:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinLXUDTMessagesTermClass1);
		return (u_char *) &StorageTmp->sccpOm5MinLXUDTMessagesTermClass1;
	case SCCPOM5MINMESSAGESSENTTOBACKUP:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinMessagesSentToBackup);
		return (u_char *) &StorageTmp->sccpOm5MinMessagesSentToBackup;
	case SCCPOM5MINDT1MESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinDT1MessagesReceived);
		return (u_char *) &StorageTmp->sccpOm5MinDT1MessagesReceived;
	case SCCPOM5MINDT1MESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinDT1MessagesSent);
		return (u_char *) &StorageTmp->sccpOm5MinDT1MessagesSent;
	case SCCPOM5MINDT2MESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinDT2MessagesReceived);
		return (u_char *) &StorageTmp->sccpOm5MinDT2MessagesReceived;
	case SCCPOM5MINDT2MESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinDT2MessagesSent);
		return (u_char *) &StorageTmp->sccpOm5MinDT2MessagesSent;
	case SCCPOM5MINEDMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinEDMessagesSent);
		return (u_char *) &StorageTmp->sccpOm5MinEDMessagesSent;
	case SCCPOM5MINEDMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinEDMessagesReceived);
		return (u_char *) &StorageTmp->sccpOm5MinEDMessagesReceived;
	default:
		ERROR_MSG("");
	}
	return NULL;
}

/**
 * @fn void refresh_sccpOm15MinSsnHistoryTable(void)
 * @brief refresh the scalar values of the sccpOm15MinSsnHistoryTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_sccpOm15MinSsnHistoryTable(void)
{
	if (sccpOm15MinSsnHistoryTable_refresh == 0)
		return;
	sccpOm15MinSsnHistoryTable_refresh = 0;
	/* XXX: Here, update the table as required... */
}

/**
 * @fn void refresh_sccpOm15MinSsnHistoryTable_row(struct sccpOm15MinSsnHistoryTable_data *StorageTmp)
 * @brief refresh the contents of the sccpOm15MinSsnHistoryTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
void
refresh_sccpOm15MinSsnHistoryTable_row(struct sccpOm15MinSsnHistoryTable_data *StorageTmp)
{
	if (StorageTmp->sccpOm15MinSsnHistoryTable_request == sa_request)
		return;
	StorageTmp->sccpOm15MinSsnHistoryTable_request = sa_request;
}

/**
 * @fn u_char *var_sccpOm15MinSsnHistoryTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in sccpOm15MinSsnHistoryTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_sccpOmMIB above.
 */
u_char *
var_sccpOm15MinSsnHistoryTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct sccpOm15MinSsnHistoryTable_data *StorageTmp = NULL;

	DEBUGMSGTL(("sccpOmMIB", "var_sccpOm15MinSsnHistoryTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_sccpOm15MinSsnHistoryTable();
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	if ((StorageTmp = header_complex(sccpOm15MinSsnHistoryTableStorage, vp, name, length, exact, var_len, write_method)) == NULL)
		return NULL;
	refresh_sccpOm15MinSsnHistoryTable_row(StorageTmp);
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case SCCPOM15MINLXUDTMESSAGESORIGCLASS0:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinLXUDTMessagesOrigClass0);
		return (u_char *) &StorageTmp->sccpOm15MinLXUDTMessagesOrigClass0;
	case SCCPOM15MINLXUDTMESSAGESORIGCLASS1:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinLXUDTMessagesOrigClass1);
		return (u_char *) &StorageTmp->sccpOm15MinLXUDTMessagesOrigClass1;
	case SCCPOM15MINLXUDTMESSAGESTERMCLASS0:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinLXUDTMessagesTermClass0);
		return (u_char *) &StorageTmp->sccpOm15MinLXUDTMessagesTermClass0;
	case SCCPOM15MINLXUDTMESSAGESTERMCLASS1:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinLXUDTMessagesTermClass1);
		return (u_char *) &StorageTmp->sccpOm15MinLXUDTMessagesTermClass1;
	case SCCPOM15MINMESSAGESSENTTOBACKUP:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinMessagesSentToBackup);
		return (u_char *) &StorageTmp->sccpOm15MinMessagesSentToBackup;
	case SCCPOM15MINDT1MESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinDT1MessagesReceived);
		return (u_char *) &StorageTmp->sccpOm15MinDT1MessagesReceived;
	case SCCPOM15MINDT1MESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinDT1MessagesSent);
		return (u_char *) &StorageTmp->sccpOm15MinDT1MessagesSent;
	case SCCPOM15MINDT2MESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinDT2MessagesReceived);
		return (u_char *) &StorageTmp->sccpOm15MinDT2MessagesReceived;
	case SCCPOM15MINDT2MESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinDT2MessagesSent);
		return (u_char *) &StorageTmp->sccpOm15MinDT2MessagesSent;
	case SCCPOM15MINEDMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinEDMessagesSent);
		return (u_char *) &StorageTmp->sccpOm15MinEDMessagesSent;
	case SCCPOM15MINEDMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinEDMessagesReceived);
		return (u_char *) &StorageTmp->sccpOm15MinEDMessagesReceived;
	default:
		ERROR_MSG("");
	}
	return NULL;
}

/**
 * @fn int write_sccpOm1stAndIntervalActivate(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_sccpOm1stAndIntervalActivate(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static oid *old_value;
	struct sccpOmMIB_data *StorageTmp = NULL;
	static size_t old_length = 0;
	static oid *objid = NULL;

	DEBUGMSGTL(("sccpOmMIB", "write_sccpOm1stAndIntervalActivate entering action=%d...  \n", action));
	if ((StorageTmp = sccpOmMIBStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_OBJECT_ID) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sccpOm1stAndIntervalActivate not ASN_OBJECT_ID\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (MIN_OID_LEN * sizeof(oid) > var_val_len || var_val_len > MAX_OID_LEN * sizeof(oid)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sccpOm1stAndIntervalActivate: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((objid = snmp_duplicate_objid((void *) var_val, var_val_len / sizeof(oid))) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		break;
	case FREE:		/* Release any resources that have been allocated */
		SNMP_FREE(objid);
		break;
	case ACTION:		/* The variable has been stored in objid for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in the
				   UNDO case */
		old_value = StorageTmp->sccpOm1stAndIntervalActivate;
		old_length = StorageTmp->sccpOm1stAndIntervalActivateLen;
		StorageTmp->sccpOm1stAndIntervalActivate = objid;
		StorageTmp->sccpOm1stAndIntervalActivateLen = var_val_len / sizeof(oid);
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->sccpOm1stAndIntervalActivate = old_value;
		StorageTmp->sccpOm1stAndIntervalActivateLen = old_length;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(old_value);
		old_length = 0;
		objid = NULL;
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_sccpOm1stAndIntervalDeactivate(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_sccpOm1stAndIntervalDeactivate(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static oid *old_value;
	struct sccpOmMIB_data *StorageTmp = NULL;
	static size_t old_length = 0;
	static oid *objid = NULL;

	DEBUGMSGTL(("sccpOmMIB", "write_sccpOm1stAndIntervalDeactivate entering action=%d...  \n", action));
	if ((StorageTmp = sccpOmMIBStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_OBJECT_ID) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sccpOm1stAndIntervalDeactivate not ASN_OBJECT_ID\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (MIN_OID_LEN * sizeof(oid) > var_val_len || var_val_len > MAX_OID_LEN * sizeof(oid)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sccpOm1stAndIntervalDeactivate: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((objid = snmp_duplicate_objid((void *) var_val, var_val_len / sizeof(oid))) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		break;
	case FREE:		/* Release any resources that have been allocated */
		SNMP_FREE(objid);
		break;
	case ACTION:		/* The variable has been stored in objid for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in the
				   UNDO case */
		old_value = StorageTmp->sccpOm1stAndIntervalDeactivate;
		old_length = StorageTmp->sccpOm1stAndIntervalDeactivateLen;
		StorageTmp->sccpOm1stAndIntervalDeactivate = objid;
		StorageTmp->sccpOm1stAndIntervalDeactivateLen = var_val_len / sizeof(oid);
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->sccpOm1stAndIntervalDeactivate = old_value;
		StorageTmp->sccpOm1stAndIntervalDeactivateLen = old_length;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(old_value);
		old_length = 0;
		objid = NULL;
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_sccpOm5MinActivate(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_sccpOm5MinActivate(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static oid *old_value;
	struct sccpOmMIB_data *StorageTmp = NULL;
	static size_t old_length = 0;
	static oid *objid = NULL;

	DEBUGMSGTL(("sccpOmMIB", "write_sccpOm5MinActivate entering action=%d...  \n", action));
	if ((StorageTmp = sccpOmMIBStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_OBJECT_ID) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sccpOm5MinActivate not ASN_OBJECT_ID\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (MIN_OID_LEN * sizeof(oid) > var_val_len || var_val_len > MAX_OID_LEN * sizeof(oid)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sccpOm5MinActivate: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((objid = snmp_duplicate_objid((void *) var_val, var_val_len / sizeof(oid))) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		break;
	case FREE:		/* Release any resources that have been allocated */
		SNMP_FREE(objid);
		break;
	case ACTION:		/* The variable has been stored in objid for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in the
				   UNDO case */
		old_value = StorageTmp->sccpOm5MinActivate;
		old_length = StorageTmp->sccpOm5MinActivateLen;
		StorageTmp->sccpOm5MinActivate = objid;
		StorageTmp->sccpOm5MinActivateLen = var_val_len / sizeof(oid);
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->sccpOm5MinActivate = old_value;
		StorageTmp->sccpOm5MinActivateLen = old_length;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(old_value);
		old_length = 0;
		objid = NULL;
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_sccpOm5MinDeaActivate(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_sccpOm5MinDeaActivate(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static oid *old_value;
	struct sccpOmMIB_data *StorageTmp = NULL;
	static size_t old_length = 0;
	static oid *objid = NULL;

	DEBUGMSGTL(("sccpOmMIB", "write_sccpOm5MinDeaActivate entering action=%d...  \n", action));
	if ((StorageTmp = sccpOmMIBStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_OBJECT_ID) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sccpOm5MinDeaActivate not ASN_OBJECT_ID\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (MIN_OID_LEN * sizeof(oid) > var_val_len || var_val_len > MAX_OID_LEN * sizeof(oid)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sccpOm5MinDeaActivate: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((objid = snmp_duplicate_objid((void *) var_val, var_val_len / sizeof(oid))) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		break;
	case FREE:		/* Release any resources that have been allocated */
		SNMP_FREE(objid);
		break;
	case ACTION:		/* The variable has been stored in objid for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in the
				   UNDO case */
		old_value = StorageTmp->sccpOm5MinDeaActivate;
		old_length = StorageTmp->sccpOm5MinDeaActivateLen;
		StorageTmp->sccpOm5MinDeaActivate = objid;
		StorageTmp->sccpOm5MinDeaActivateLen = var_val_len / sizeof(oid);
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->sccpOm5MinDeaActivate = old_value;
		StorageTmp->sccpOm5MinDeaActivateLen = old_length;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(old_value);
		old_length = 0;
		objid = NULL;
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_sccpOm15MinActivate(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_sccpOm15MinActivate(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static oid *old_value;
	struct sccpOmMIB_data *StorageTmp = NULL;
	static size_t old_length = 0;
	static oid *objid = NULL;

	DEBUGMSGTL(("sccpOmMIB", "write_sccpOm15MinActivate entering action=%d...  \n", action));
	if ((StorageTmp = sccpOmMIBStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_OBJECT_ID) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sccpOm15MinActivate not ASN_OBJECT_ID\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (MIN_OID_LEN * sizeof(oid) > var_val_len || var_val_len > MAX_OID_LEN * sizeof(oid)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sccpOm15MinActivate: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((objid = snmp_duplicate_objid((void *) var_val, var_val_len / sizeof(oid))) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		break;
	case FREE:		/* Release any resources that have been allocated */
		SNMP_FREE(objid);
		break;
	case ACTION:		/* The variable has been stored in objid for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in the
				   UNDO case */
		old_value = StorageTmp->sccpOm15MinActivate;
		old_length = StorageTmp->sccpOm15MinActivateLen;
		StorageTmp->sccpOm15MinActivate = objid;
		StorageTmp->sccpOm15MinActivateLen = var_val_len / sizeof(oid);
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->sccpOm15MinActivate = old_value;
		StorageTmp->sccpOm15MinActivateLen = old_length;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(old_value);
		old_length = 0;
		objid = NULL;
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_sccpOm15MinDeaActivate(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_sccpOm15MinDeaActivate(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static oid *old_value;
	struct sccpOmMIB_data *StorageTmp = NULL;
	static size_t old_length = 0;
	static oid *objid = NULL;

	DEBUGMSGTL(("sccpOmMIB", "write_sccpOm15MinDeaActivate entering action=%d...  \n", action));
	if ((StorageTmp = sccpOmMIBStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_OBJECT_ID) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sccpOm15MinDeaActivate not ASN_OBJECT_ID\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (MIN_OID_LEN * sizeof(oid) > var_val_len || var_val_len > MAX_OID_LEN * sizeof(oid)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sccpOm15MinDeaActivate: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((objid = snmp_duplicate_objid((void *) var_val, var_val_len / sizeof(oid))) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		break;
	case FREE:		/* Release any resources that have been allocated */
		SNMP_FREE(objid);
		break;
	case ACTION:		/* The variable has been stored in objid for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in the
				   UNDO case */
		old_value = StorageTmp->sccpOm15MinDeaActivate;
		old_length = StorageTmp->sccpOm15MinDeaActivateLen;
		StorageTmp->sccpOm15MinDeaActivate = objid;
		StorageTmp->sccpOm15MinDeaActivateLen = var_val_len / sizeof(oid);
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->sccpOm15MinDeaActivate = old_value;
		StorageTmp->sccpOm15MinDeaActivateLen = old_length;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(old_value);
		old_length = 0;
		objid = NULL;
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_sccpOm5MinMaxIntervals(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_sccpOm5MinMaxIntervals(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static ulong old_value;
	struct sccpOmMIB_data *StorageTmp = NULL;
	ulong set_value = *((ulong *) var_val);

	DEBUGMSGTL(("sccpOmMIB", "write_sccpOm5MinMaxIntervals entering action=%d...  \n", action));
	if ((StorageTmp = sccpOmMIBStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sccpOm5MinMaxIntervals not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sccpOm5MinMaxIntervals: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 96 */
		/* Note: ranges 0..288 */
		if ((0 > set_value || set_value > 288)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sccpOm5MinMaxIntervals: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in set_value for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in
				   the UNDO case */
		old_value = StorageTmp->sccpOm5MinMaxIntervals;
		StorageTmp->sccpOm5MinMaxIntervals = set_value;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->sccpOm5MinMaxIntervals = old_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_sccpOm15MinMaxIntervals(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_sccpOm15MinMaxIntervals(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static ulong old_value;
	struct sccpOmMIB_data *StorageTmp = NULL;
	ulong set_value = *((ulong *) var_val);

	DEBUGMSGTL(("sccpOmMIB", "write_sccpOm15MinMaxIntervals entering action=%d...  \n", action));
	if ((StorageTmp = sccpOmMIBStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sccpOm15MinMaxIntervals not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sccpOm15MinMaxIntervals: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 96 */
		/* Note: ranges 0..672 */
		if ((0 > set_value || set_value > 672)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sccpOm15MinMaxIntervals: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in set_value for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in
				   the UNDO case */
		old_value = StorageTmp->sccpOm15MinMaxIntervals;
		StorageTmp->sccpOm15MinMaxIntervals = set_value;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->sccpOm15MinMaxIntervals = old_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_sccpOmpDefault(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_sccpOmpDefault(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct sccpOmMIB_data *StorageTmp = NULL;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("sccpOmMIB", "write_sccpOmpDefault entering action=%d...  \n", action));
	if ((StorageTmp = sccpOmMIBStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sccpOmpDefault not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sccpOmpDefault: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 8 */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in set_value for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in
				   the UNDO case */
		old_value = StorageTmp->sccpOmpDefault;
		StorageTmp->sccpOmpDefault = set_value;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->sccpOmpDefault = old_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_sccpOmNrOfSubLevelsDefault(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_sccpOmNrOfSubLevelsDefault(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct sccpOmMIB_data *StorageTmp = NULL;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("sccpOmMIB", "write_sccpOmNrOfSubLevelsDefault entering action=%d...  \n", action));
	if ((StorageTmp = sccpOmMIBStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sccpOmNrOfSubLevelsDefault not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sccpOmNrOfSubLevelsDefault: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 4 */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in set_value for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in
				   the UNDO case */
		old_value = StorageTmp->sccpOmNrOfSubLevelsDefault;
		StorageTmp->sccpOmNrOfSubLevelsDefault = set_value;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->sccpOmNrOfSubLevelsDefault = old_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_sccpOmNrOfRestrictionLevelsDefault(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_sccpOmNrOfRestrictionLevelsDefault(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct sccpOmMIB_data *StorageTmp = NULL;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("sccpOmMIB", "write_sccpOmNrOfRestrictionLevelsDefault entering action=%d...  \n", action));
	if ((StorageTmp = sccpOmMIBStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sccpOmNrOfRestrictionLevelsDefault not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sccpOmNrOfRestrictionLevelsDefault: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 8 */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in set_value for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in
				   the UNDO case */
		old_value = StorageTmp->sccpOmNrOfRestrictionLevelsDefault;
		StorageTmp->sccpOmNrOfRestrictionLevelsDefault = set_value;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->sccpOmNrOfRestrictionLevelsDefault = old_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	}
	return SNMP_ERR_NOERROR;
}

void
send_sccpOm5minReport_v2trap(struct variable_list *vars)
{
	struct variable_list trap;

	trap.next_variable = vars;
	trap.name = snmpTrapOID_oid;
	trap.name_length = sizeof(snmpTrapOID_oid) / sizeof(oid);
	trap.type = ASN_OBJECT_ID;
	trap.val.objid = sccpOm5minReport_oid;
	trap.val_len = sizeof(sccpOm5minReport_oid);
	trap.index = 0;
	send_v2trap(&trap);
}

void
send_sccpOm15minReport_v2trap(struct variable_list *vars)
{
	struct variable_list trap;

	trap.next_variable = vars;
	trap.name = snmpTrapOID_oid;
	trap.name_length = sizeof(snmpTrapOID_oid) / sizeof(oid);
	trap.type = ASN_OBJECT_ID;
	trap.val.objid = sccpOm15minReport_oid;
	trap.val_len = sizeof(sccpOm15minReport_oid);
	trap.index = 0;
	send_v2trap(&trap);
}

void
send_sccpOm5minSsnReport_v2trap(struct variable_list *vars)
{
	struct variable_list trap;

	trap.next_variable = vars;
	trap.name = snmpTrapOID_oid;
	trap.name_length = sizeof(snmpTrapOID_oid) / sizeof(oid);
	trap.type = ASN_OBJECT_ID;
	trap.val.objid = sccpOm5minSsnReport_oid;
	trap.val_len = sizeof(sccpOm5minSsnReport_oid);
	trap.index = 0;
	send_v2trap(&trap);
}

void
send_sccpOm15minSsnReport_v2trap(struct variable_list *vars)
{
	struct variable_list trap;

	trap.next_variable = vars;
	trap.name = snmpTrapOID_oid;
	trap.name_length = sizeof(snmpTrapOID_oid) / sizeof(oid);
	trap.type = ASN_OBJECT_ID;
	trap.val.objid = sccpOm15minSsnReport_oid;
	trap.val_len = sizeof(sccpOm15minSsnReport_oid);
	trap.index = 0;
	send_v2trap(&trap);
}

#if defined MODULE
#if defined MASTER
/**
 * @fn void sccpOmMIB_loop_handler(int dummy)
 * @param dummy signal number (always zero (0))
 * @brief handle event loop interation.
 *
 * This function is registered so that, when operating as a module, snmpd will call it one per event
 * loop interation.  This function is called before the next requst is processed and after the
 * previous request is processed.  Two things are done here:  1) The file descriptor that is used to
 * synchronize the agent with (pseudo-)device drivers is closed.  (Another approach, instead of
 * closing each time, would be to restart a timer each time that a request is made (loop is
 * performed) and if it expires, close the file descriptor).  2) The request number is incremented.
 * Although a request is not generated for each loop of the snmp event loop, it is true that a new
 * request cannot be generated without performing a loop.  Therefore, the sa_request is not the
 * request number but it is a temporally unique identifier for a request.
 */
void
sccpOmMIB_loop_handler(int dummy)
{
	if (external_signal_scheduled[dummy] == 0)
		external_signal_scheduled[dummy]--;
	/* close files after each request */
	if (sa_fclose && sa_fd != 0) {
		close(sa_fd);
		sa_fd = 0;
	}
	/* prepare for next request */
	sa_request++;
}
#endif				/* defined MASTER */
/**
 * @fn void sccpOmMIB_readfd_handler(int fd, void *dummy)
 * @param fd file descriptor to read.
 * @param dummy client data passed to registration function (always NULL).
 * @brief handle read event on file descriptor.
 *
 * This read file descriptor handler is normally used for (pseudo-)device drivers that generate
 * statistical collection interval events, alarm events, or other operational measurement events, by
 * placing a message on the read queue of the "event handling" Stream.  Normally this routine
 * would adjust counts in some table or scalars, generate SNMP traps representing on-occurence
 * events, first and interval events, and alarm indications.
 */
void
sccpOmMIB_readfd_handler(int fd, void *dummy)
{
	/* XXX: place actions to handle sa_readfd here... */
	return;
}
#endif				/* defined MOUDLE */
#if defined MASTER
const char sa_program[] = "sccpommib";
int sa_fclose = 1;			/* default close files between requests */
int sa_fd = 0;				/* file descriptor for MIB use */
int sa_readfd = 0;			/* file descriptor for autonomnous events */
int sa_changed = 1;			/* indication to reread MIB configuration */
int sa_stats_refresh = 1;		/* indications that statistics, the mib or its tables need to be refreshed */
int sa_request = 1;			/* request number for per-request actions */
#endif				/* defined MASTER */
#if defined MASTER
#if !defined MODULE
int sa_dump = 0;			/* default packet dump */
int sa_debug = 0;			/* default no debug */
int sa_nomead = 1;			/* default daemon mode */
int sa_output = 1;			/* default normal output */
int sa_agentx = 1;			/* default agentx mode */
int sa_alarms = 1;			/* default application alarms */
int sa_logaddr = 0;			/* log addresses */
int sa_logfillog = 0;			/* log to sa_logfile */
int sa_logstderr = 0;			/* log to standard error */
int sa_logstdout = 0;			/* log to standard output */
int sa_logsyslog = 0;			/* log to system logs */
int sa_logcallog = 0;			/* log to callback logs */
int sa_appendlog = 0;			/* append to log file without truncating */
char sa_logfile[256] = "/var/log/sccpommib.log";
char sa_pidfile[256] = "/var/run/sccpommib.pid";
char sa_sysctlf[256] = "/etc/sccpommib.conf";
int allow_severity = LOG_ERR;
int deny_severity = LOG_ERR;

/* file stream for log file */
FILE *stdlog = NULL;
static void
sa_version(int argc, char *argv[])
{
	if (!sa_output && !sa_debug)
		return;
	fprintf(stdout, "\
%2$s\n\
Copyright (c) 2008-2009  Monavacom Limited.  All Rights Reserved.\n\
Distributed under Affero GPL Version 3, included here by reference.\n\
See `%1$s --copying' for copying permissions.\n\
", argv[0], ident);
}
static void
sa_usage(int argc, char *argv[])
{
	if (!sa_output && !sa_debug)
		return;
	fprintf(stderr, "\
Usage:\n\
    %1$s [general-options] [options] [arguments]\n\
    %1$s {-H|--help-directives}\n\
    %1$s {-h|--help}\n\
    %1$s {-V|--version}\n\
    %1$s {-C|--copying}\n\
", argv[0]);
}
static void
sa_help(int argc, char *argv[])
{
	if (!sa_output && !sa_debug)
		return;
	fprintf(stdout, "\
Usage:\n\
    %1$s [general-options] [options] [arguments]\n\
    %1$s {-h|--help}\n\
    %1$s {-V|--version}\n\
    %1$s {-C|--copying}\n\
Arguments:\n\
    None.\n\
Options:\n\
    -a, --log-addresses\n\
        log addresses of connecting management stations.\n\
    -A, --append\n\
        append to logfiles without truncating.\n\
    -c, --config-file CONFIGFILE\n\
        use configuration file CONFIGFILE.\n\
    -C, --config-only\n\
        only load configuration given by -c option.\n\
    -d, --dump\n\
        dump sent and received PDUs.\n\
    -D, --debug [LEVEL]\n\
        set debugging verbosity to LEVEL.\n\
    -D, --debug-tokens [TOKEN[,TOKEN]*]\n\
        debug specified TOKEN's.\n\
    -f, --dont-fork\n\
        run in the foreground.\n\
    -g, --gid, --groupid GID\n\
        become group GID after listening.\n\
    -h, --help, -?, --?\n\
        print usage information and exit.\n\
    -H, --help-directives\n\
        print config directives and exit.\n\
    -I, --initialize [-]MODULE[,MODULE]*\n\
        initialize (or not, '-') these MODULE's.\n\
    -k, --keep-open\n\
        keep system files open between requests.\n\
    -l, --log-file [LOGFILE]\n\
        log to log file name LOGFILE.  [default: /var/log/sccpommib.log]\n\
    -L, --log-stderr\n\
        log to controlling terminal standard error.\n\
    -m, --mibs [+]MIB[,MIB]*\n\
        load these (additional '+') MIBs.\n\
    -M, --master\n\
        run as SNMP master instead of AgentX sub-agent.\n\
    -M, --mibdirs [+]MIBDIR[:MIBDIR]*\n\
        search these (additional, '+') colon separated directories for MIBs.\n\
    -n, --nodaemon\n\
        run in the foreground.\n\
    -n, --name NAME\n\
        use NAME for configuration file base.  [default: sccpommib]\n\
    -p, --port PORTNUM\n\
        listen on port number PORTNUM.  [default: 161]\n\
    -p, --pidfile PIDFILE\n\
        write daemon pid to PIDFILE.  [default: /var/run/sccpommib.pid]\n\
    -P, --pidfile PIDFILE\n\
        write daemon pid to PIDFILE.  [default: /var/run/sccpommib.pid]\n\
    -q, --quiet\n\
        suppress normal output.\n\
    -q, --quick\n\
        abbreviate output for machine readability.\n\
    -r, --noroot\n\
        do not require root privilege.\n\
    -s, --log-syslog\n\
        log to system logs.\n\
    -S, --sysctl-file FILENAME\n\
        write sysctl config file FILENAME.  [default: /etc/streams.conf]\n\
    -t, --agent-alarms\n\
        agent blocks {SIGALARM}.\n\
    -T, --transport [TRANSPORT]\n\
        default transport TRANSPORT.  [default: udp]\n\
    -u, --uid, --userid UID\n\
        become user UID after listening.\n\
    -U, --dont-remove-pidfile\n\
        do not remove PIDFILE when shutting down.\n\
    -v, --version\n\
        print version information and exit.\n\
    -V, --verbose [LEVEL]\n\
        be verbose to LEVEL.  [default: 1]\n\
    -x, --agentx-socket [SOCKET]\n\
        master AgentX on SOCKET.  [default: /var/agentx/master]\n\
    -X, --agentx\n\
        run as AgentX sub-agent instead of master (the default).\n\
    -y, --copying\n\
        print copying information and exit.\n\
", argv[0]);
}
static void
sa_copying(int argc, char *argv[])
{
	if (!sa_output && !sa_debug)
		return;
	fprintf(stdout, "\
--------------------------------------------------------------------------------\n\
%1$s\n\
--------------------------------------------------------------------------------\n\
Copyright (c) 2008-2009  Monavacom Limited <http://www.monavacom.com>\n\
Copyright (c) 2001-2008  OpenSS7 Corporation <http://www.openss7.com>\n\
Copyright (c) 1997-2000  Brian F. G. Bidulock <bidulock@openss7.org>\n\
\n\
All Rights Reserved.\n\
--------------------------------------------------------------------------------\n\
This program is free software; you can  redistribute  it and/or modify  it under\n\
the terms of the GNU Affero General Public License as published by the Free\n\
Software Foundation; Version 3 of the License.\n\
\n\
This program is distributed in the hope that it will  be useful, but WITHOUT ANY\n\
WARRANTY; without even  the implied warranty of MERCHANTABILITY or FITNESS FOR A\n\
PARTICULAR PURPOSE.  See the GNU Affero General Public License for more details.\n\
\n\
You should have received a copy of the GNU  Affero  General Public License along\n\
with this program.   If not, see <http://www.gnu.org/licenses/>, or write to the\n\
Free Software Foundation, Inc., 675 Mass Ave, Cambridge, MA 02139, USA.\n\
--------------------------------------------------------------------------------\n\
U.S. GOVERNMENT RESTRICTED RIGHTS.  If you are licensing this Software on behalf\n\
of the U.S. Government (\"Government\"), the following provisions apply to you. If\n\
the Software is supplied by the  Department of Defense (\"DoD\"), it is classified\n\
as \"Commercial  Computer  Software\"  under  paragraph  252.227-7014  of the  DoD\n\
Supplement  to the  Federal Acquisition Regulations  (\"DFARS\") (or any successor\n\
regulations) and the  Government  is acquiring  only the  license rights granted\n\
herein (the license rights customarily provided to non-Government users). If the\n\
Software is supplied to any unit or agency of the Government  other than DoD, it\n\
is  classified as  \"Restricted Computer Software\" and the Government's rights in\n\
the Software  are defined  in  paragraph 52.227-19  of the  Federal  Acquisition\n\
Regulations (\"FAR\")  (or any successor regulations) or, in the cases of NASA, in\n\
paragraph  18.52.227-86 of  the  NASA  Supplement  to the FAR (or any  successor\n\
regulations).\n\
--------------------------------------------------------------------------------\n\
Commercial  licensing  and  support of this  software is  available from OpenSS7\n\
Corporation at a fee.  See http://www.openss7.com/\n\
--------------------------------------------------------------------------------\n\
", ident);
}

void
sa_help_directives(int argc, char *argv[])
{
	ds_set_boolean(DS_APPLICATION_ID, DS_AGENT_NO_ROOT_ACCESS, 1);
	init_agent("sccpOmMIB");
	// init_mib_modules();
	init_mib();
	init_snmp("sccpOmMIB");
	snmp_log(MY_FACILITY(LOG_INFO), "Configuration directives understood:\n");
	/* Unfortunately, read_config_print_usage() uses snmp_log(), meaning that it can only be writen to standard error and not standard output. */
	read_config_print_usage("    ");
}
static int
sa_sig_register(int signum, RETSIGTYPE(*handler) (int))
{
	sigset_t mask;
	struct sigaction act;

	act.sa_handler = handler ? handler : SIG_DFL;
	act.sa_flags = handler ? SA_RESTART : 0;
	sigemptyset(&act.sa_mask);
	if (sigaction(signum, &act, NULL))
		return (-1);
	sigemptyset(&mask);
	sigaddset(&mask, signum);
	sigprocmask(handler ? SIG_UNBLOCK : SIG_BLOCK, &mask, NULL);
	return (0);
}
static int sa_alm_signal = 0;
static int sa_pol_signal = 0;
static int sa_hup_signal = 0;
static int sa_int_signal = 0;
static int sa_trm_signal = 0;
static int sa_alm_handle = 0;
void
sa_alm_callback(uint req, void *arg)
{
	if (req == sa_alm_handle)
		sa_alm_handle = 0;
	sa_alm_signal = 1;
	return;
}

static RETSIGTYPE
sa_alm_handler(int signum)
{
	sa_alm_signal = 1;
	return (RETSIGTYPE) (0);
}
static void
sa_snmp_alm_handler(uint reg, void *clientarg)
{
	sa_alm_signal = 1;
	return;
}
static int
sa_alm_catch(void)
{
	if (sa_alarms)
		return sa_sig_register(SIGALRM, &sa_alm_handler);
	return (-1);
}
static int
sa_alm_block(void)
{
	if (sa_alarms)
		return sa_sig_register(SIGALRM, NULL);
	if (sa_alm_handle) {
		uint handle = sa_alm_handle;

		sa_alm_handle = 0;
		snmp_alarm_unregister(handle);
	}
	return (0);
}
static int
sa_alm_action(void)
{
	sa_alm_signal = 0;
	return (0);
}

static RETSIGTYPE
sa_pol_handler(int signum)
{
	sa_pol_signal = 1;
	return (RETSIGTYPE) (0);
}
static int
sa_pol_catch(void)
{
	return sa_sig_register(SIGPOLL, &sa_pol_handler);
}
static int
sa_pol_block(void)
{
	return sa_sig_register(SIGPOLL, NULL);
}

/*
 * Both the sc(4) module and sad(4) driver issue an M_PCSIG message with
 * SIGPOLL to the stream head whenever the STREAMS configuration or autopush
 * configuration changes, indicating to the agent which has the sc(4) or
 * sad(4) Stream open that it is necessary to reread information from the
 * kernel.  This fact is merely recorded, as this information is not read each
 * time that a configuration change occurs, but only after a request from some
 * portion of that information occurs. This condition is also set when the
 * sc(4) and sad(4) Streams are first opened. The SIGPOLL will also deliver in
 * siginfo the file descriptor issuing the signal, so we could distiguish
 * between sc(4) and sad(4) signals, but since one can be pushed over the
 * other, there is little point in distinguishing.
 *
 * sc(4) or sad(4) also should be modified to provide the general streams
 * statistics supported here; even though they are available through the /proc
 * filesystem on Linux Fast-STREAMS.
 */
static int
sa_pol_action(void)
{
	sa_pol_signal = 0;
	snmp_log(MY_FACILITY(LOG_INFO), "%s: Caught SIGPOLL, will re-read data structures", sa_program);
	sa_changed = 1;
	return (0);
}

static RETSIGTYPE
sa_hup_handler(int signum)
{
	sa_hup_signal = 1;
	return (RETSIGTYPE) (0);
}
static int
sa_hup_catch(void)
{
	if (sa_agentx)
		return sa_sig_register(SIGHUP, &sa_hup_handler);
	return (-1);
}
static int
sa_hup_block(void)
{
	return sa_sig_register(SIGHUP, NULL);
}
static int
sa_hup_action(void)
{
	/* There are several times that we might be sent a SIGHUP.  We might be sent a SIGHUP by logrotate asking us to close and reopen our log files. */
	sa_hup_signal = 0;
	snmp_log(MY_FACILITY(LOG_WARNING), "Caught SIGHUP, reopening files.");
	if (sa_output > 1)
		snmp_log(MY_FACILITY(LOG_NOTICE), "Reopening output file %s", sa_logfile);
	if (sa_logfillog != 0) {
		fflush(stdlog);
		fclose(stdlog);
		snmp_disable_filelog();
		if ((stdlog = freopen(sa_logfile, sa_appendlog ? "a" : "w", stdlog)) == NULL) {
			/* I hope we have another log sink. */
			snmp_log(MY_FACILITY(LOG_ERR), "%s", strerror(errno));
			snmp_log(MY_FACILITY(LOG_ERR), "Could not reopen log file %s", sa_logfile);
		}
		snmp_enable_filelog(sa_logfile, sa_appendlog);
	}
	return (0);
}

static RETSIGTYPE
sa_int_handler(int signum)
{
	sa_int_signal = 1;
	return (RETSIGTYPE) (0);
}
static int
sa_int_catch(void)
{
	return sa_sig_register(SIGINT, &sa_int_handler);
}
static int
sa_int_block(void)
{
	return sa_sig_register(SIGINT, NULL);
}
static void sa_exit(int retval);
static int
sa_int_action(void)
{
	sa_int_signal = 0;
	snmp_log(MY_FACILITY(LOG_WARNING), "%s: Caught SIGINT, shutting down", sa_program);
	sa_exit(0);
	return (0);		/* should be no return */
}

static RETSIGTYPE
sa_trm_handler(int signum)
{
	sa_trm_signal = 1;
	return (RETSIGTYPE) (0);
}
static int
sa_trm_catch(void)
{
	return sa_sig_register(SIGTERM, &sa_trm_handler);
}
static int
sa_trm_block(void)
{
	return sa_sig_register(SIGTERM, NULL);
}
static void sa_exit(int retval);
static int
sa_trm_action(void)
{
	sa_trm_signal = 0;
	snmp_log(MY_FACILITY(LOG_WARNING), "%s: Caught SIGTERM, shutting down", sa_program);
	sa_exit(0);
	return (0);		/* should be no return */
}
static void
sa_sig_catch(void)
{
	sa_alm_catch();
	sa_pol_catch();
	sa_hup_catch();
	sa_int_catch();
	sa_trm_catch();
}
static void
sa_sig_block(void)
{
	sa_alm_block();
	sa_pol_block();
	sa_hup_block();
	sa_int_block();
	sa_trm_block();
}

int
sa_start_timer(long duration)
{
	if (sa_alarms) {
		struct itimerval setting = {
			{0, 0},
			{duration / 1000, (duration % 1000) * 1000}
		};
		if (sa_alm_catch())
			return (-1);
		if (setitimer(ITIMER_REAL, &setting, NULL))
			return (-1);
		sa_alm_signal = 0;
		return (0);
	} else {
#if defined NETSNMP_DS_APPLICATION_ID
		struct timeval setting = {
			duration / 1000, (duration % 1000) * 1000
		};
		sa_alm_handle = snmp_alarm_register_hr(setting, 0, sa_snmp_alm_handler, NULL);
#else
		sa_alm_handle = snmp_alarm_register((duration + 999) / 1000, 0, sa_snmp_alm_handler, NULL);
#endif
		return (sa_alm_handle ? 0 : -1);
	}
}
static void
sa_exit(int retval)
{
	if (retval)
		snmp_log(MY_FACILITY(LOG_ERR), "%s: Exiting %d", sa_program, retval);
	else
		snmp_log(MY_FACILITY(LOG_NOTICE), "%s: Exiting %d", sa_program, retval);
	fflush(stdout);
	fflush(stderr);
	sa_sig_block();
	closelog();
	exit(retval);
}
static void
sa_init_logging(int argc, char *argv[])
{
	static char progname[256];

	/* The purpose of this function is to bring logging up before forking (and while still in the foreground) so that we can use the snmp_log() function before and during forking if necessary.
	   Note that the default configuration for snmp_log() is to send all logs to standard error. */
	strncpy(progname, basename(argv[0]), sizeof(progname));
	snmp_disable_log();
	if (sa_logfillog) {
		snmp_enable_filelog(sa_logfile, sa_appendlog);
	}
	if (sa_logstderr | sa_logstdout) {
#if defined LOG_PERROR
		/* Note that when we have Linux LOG_PERROR, and logs go both to syslog and stderr, it is better to use the LOG_PERROR than to use snmp_log()'s print to stderr, as the former is better 
		   formated. */
		if (!sa_logsyslog)
			snmp_enable_stderrlog();
#else				/* defined LOG_PERROR */
		snmp_enable_stderrlog();
#endif				/* defined LOG_PERROR */
	}
	if (sa_logsyslog) {
#if !defined HAVE_SNMP_ENABLE_SYSLOG_IDENT
		snmp_enable_syslog();
#else				/* !defined HAVE_SNMP_ENABLE_SYSLOG_IDENT */
		snmp_enable_syslog_ident("sccpOmMIB", LOG_DAEMON);
#endif				/* !defined HAVE_SNMP_ENABLE_SYSLOG_IDENT */
		/* Note that the way that snmp sets up the logger is not really the way we want it, so close the log and reopen it the way we want. */
		closelog();
#if defined LOG_PERROR
		openlog("sccpOmMIB", LOG_PID | LOG_CONS | LOG_NDELAY | (sa_logstderr ? LOG_PERROR : 0), MY_FACILITY(0));
#else				/* defined LOG_PERROR */
		openlog("sccpOmMIB", LOG_PID | LOG_CONS | LOG_NDELAY, MY_FACILITY(0));
#endif				/* defined LOG_PERROR */
	}
	if (sa_logcallog) {
		snmp_enable_calllog();
	}
}
static void
sa_enter(int argc, char *argv[])
{
	if (sa_nomead) {
		pid_t pid;

		if ((pid = fork()) < 0) {
			perror(argv[0]);
			exit(2);
		} else if (pid != 0) {
			/* parent exits */
			exit(0);
		}
		setsid();	/* become a session leader */
		/* fork once more for SVR4 */
		if ((pid = fork()) < 0) {
			perror(argv[0]);
			exit(2);
		} else if (pid != 0) {
			/* parent responsible for writing pid file */
			if (sa_nomead || sa_pidfile[0] != '\0') {
				FILE *pidf;

				/* initialize default filename */
				if (sa_pidfile[0] == '\0')
					snprintf(sa_pidfile, sizeof(sa_pidfile), "/var/run/%s.pid", sa_program);
				if (sa_output > 1) {
					snmp_log(MY_FACILITY(LOG_NOTICE), "%s: Writing daemon pid to file %s", sa_program, sa_pidfile);
				}
				if ((pidf = fopen(sa_pidfile, "w+"))) {
					fprintf(pidf, "%d", (int) pid);
					fflush(pidf);
					fclose(pidf);
				} else {
					snmp_log(MY_FACILITY(LOG_ERR), "%s: %m", sa_program);
					snmp_log(MY_FACILITY(LOG_ERR), "%s: Could not write pid to file %s", sa_program, sa_pidfile);
					sa_exit(2);
					/* no return */
				}
			}
			/* parent exits */
			exit(0);
		}
		/* child continues */
		/* release current directory */
		if (chdir("/") < 0) {
			perror(argv[0]);
			exit(2);
		}
		umask(0);	/* clear file creation mask */
		/* rearrange file streams */
		fclose(stdin);
	}
	/* continue as foreground or background */
	sa_init_logging(argc, argv);
	sa_sig_catch();
	snmp_log(MY_FACILITY(LOG_NOTICE), "%s: Startup complete.", sa_program);
}
static void
sa_mloop(int argc, char *argv[])
{
	if (sa_agentx) {
		if (sa_debug)
			snmp_log(MY_FACILITY(LOG_DEBUG), "%s: running as AgentX client\n", argv[0]);
		/* run as an AgentX client */
		ds_set_boolean(DS_APPLICATION_ID, DS_AGENT_ROLE, 1);
	} else {
		if (sa_debug)
			snmp_log(MY_FACILITY(LOG_DEBUG), "%s: running as SNMP master agent\n", argv[0]);
		/* run as SNMP master */
		ds_set_boolean(DS_APPLICATION_ID, DS_AGENT_ROLE, 0);
	}
	if (sa_alarms) {
		if (sa_debug)
			snmp_log(MY_FACILITY(LOG_DEBUG), "%s: using application alarms\n", argv[0]);
		/* use application alarms */
		ds_set_boolean(DS_LIBRARY_ID, DS_LIB_ALARM_DONT_USE_SIG, 1);
	}
	/* initialize agent */
	init_agent("sccpOmMIB");
	/* initialize MIB */
	init_sccpOmMIB();
	/* initialize SNMP */
	init_snmp("sccpOmMIB");
	if (!sa_agentx) {
		if (sa_debug)
			snmp_log(MY_FACILITY(LOG_DEBUG), "%s: running as SNMP master\n", argv[0]);
#if !defined NETSNMP_DS_APPLICATION_ID
		init_master_agent(710, NULL, NULL);
#else
		init_master_agent();
#endif
	}
	for (;;) {
		int retval;

		/* to use select or poll you need to use the snmp_select_info() to obtain the fd of the agentx socket and add it to the fdset. */
		/* note that SIGALRM is used by snmp: use the snmp_alarm() api instead */
#if 0
		if (snmp_select() == 0) {
			if (sa_alarms == 0)
				run_alarms();
		}
#endif
		retval = agent_check_and_process(1);	/* 0 == don't block */
		if (retval == 0) {
			/* alarm occurred, alarm conditions checked */
		} else if (retval == -1) {
			/* error (or signal) ocurred */
			if (sa_alm_signal) {
				sa_alm_action();
			}
			if (sa_pol_signal) {
				sa_pol_action();
			}
			if (sa_hup_signal) {
				sa_hup_action();
			}
			if (sa_int_signal) {
				if (sa_debug)
					snmp_log(MY_FACILITY(LOG_DEBUG), "%s: shutting down\n", argv[0]);
				snmp_shutdown("sccpOmMIB");
				sa_int_action();	/* no return */
			}
			if (sa_trm_signal) {
				if (sa_debug)
					snmp_log(MY_FACILITY(LOG_DEBUG), "%s: shutting down\n", argv[0]);
				snmp_shutdown("sccpOmMIB");
				sa_trm_action();	/* no return */
			}
		} else if (retval > 0) {
			/* processed packets */
			if (sa_fclose) {
				/* close files after each request */
				if (sa_fd != 0) {
					int fd = sa_fd;

					sa_fd = 0;
					close(fd);
				}
			}
			sa_stats_refresh = 1;
			sa_request++;
		}
	}
	if (sa_debug)
		snmp_log(MY_FACILITY(LOG_DEBUG), "%s: shutting down\n", argv[0]);
	snmp_shutdown("sccpOmMIB");
}

int
main(int argc, char *argv[])
{
	for (;;) {
		int c, val, fd;
		char *cptr;
		struct passwd *pw;
		struct group *gr;
		struct stat st;

#if defined _GNU_SOURCE
		int option_index = 0;
                /* *INDENT-OFF* */
                static struct option long_options[] = {
                        {"log-addresses",	no_argument,		NULL, 'a'},
                        {"append",		no_argument,		NULL, 'A'},
                        {"config-file",		required_argument,	NULL, 'c'},
                        {"no-configs",		no_argument,		NULL, 'C'},
                        {"dump",		no_argument,		NULL, 'd'},
                        {"debug",		optional_argument,	NULL, 'D'},
                        {"debug-tokens",	optional_argument,	NULL, 'D'},
                        {"dont-fork",		no_argument,		NULL, 'f'},
                        {"gid",			required_argument,	NULL, 'g'},
                        {"groupid",		required_argument,	NULL, 'g'},
                        {"help",		no_argument,		NULL, 'h'},
                        {"?",			no_argument,		NULL, 'h'},
                        {"help-directives",	no_argument,		NULL, 'H'},
                        {"initialize",		required_argument,	NULL, 'I'},
                        {"init-modules",	required_argument,	NULL, 'I'},
                        {"keep-open",		no_argument,		NULL, 'k'},
                        {"log-file",		optional_argument,	NULL, 'l'},
                        {"logfile",		optional_argument,	NULL, 'l'},
                        {"Lf",			optional_argument,	NULL, 'l'},
                        {"LF",			required_argument,	NULL, 'l'},
                        {"log-stderr",		no_argument,		NULL, 'L'},
                        {"Le",			no_argument,		NULL, 'L'},
                        {"LE",			required_argument,	NULL, 'L'},
                        {"mibs",		required_argument,	NULL, 'm'},
                        {"master",		no_argument,		NULL, 'M'},
                        {"mibdirs",		required_argument,	NULL, 'M'},
                        {"nodaemon",		no_argument,		NULL, 'n'},
                        {"name",		required_argument,	NULL, 'n'},
                        {"dry-run",		no_argument,		NULL, 'N'},
                        {"log-stdout",		no_argument,		NULL, 'o'},
                        {"Lo",			no_argument,		NULL, 'o'},
                        {"LO",			required_argument,	NULL, 'o'},
                        {"port",		required_argument,	NULL, 'p'},
                        {"pidfile",		required_argument,	NULL, 'P'},
                        {"quiet",		no_argument,		NULL, 'q'},
                        {"quick",		no_argument,		NULL, 'q'},
                        {"noroot",		no_argument,		NULL, 'r'},
                        {"log-syslog",		no_argument,		NULL, 's'},
                        {"Ls",			no_argument,		NULL, 's'},
                        {"LS",			required_argument,	NULL, 's'},
                        {"syslog",		no_argument,		NULL, 's'},
                        {"sysctl-file",		required_argument,	NULL, 'S'},
                        {"agent-alarms",	no_argument,		NULL, 't'},
                        {"transport",		optional_argument,	NULL, 'T'},
                        {"uid",			required_argument,	NULL, 'u'},
                        {"userid",		required_argument,	NULL, 'u'},
                        {"dont-remove-pidfile",	no_argument,		NULL, 'U'},
                        {"leave-pidfile",	no_argument,		NULL, 'U'},
                        {"version",		no_argument,		NULL, 'v'},
                        {"verbose",		optional_argument,	NULL, 'V'},
                        {"agentx-socket",	required_argument,	NULL, 'x'},
                        {"agentx",		no_argument,		NULL, 'X'},
                        {"copying",		no_argument,		NULL, 'y'},
#if 0
                        {"directory",		required_argument,	NULL, 'd'},
                        {"basename",		required_argument,	NULL, 'b'},
                        {"outfile",		required_argument,	NULL, 'o'},
                        {"errfile",		required_argument,	NULL, 'e'},
#endif
                        { 0, }
                };
                /* *INDENT-ON* */

		c = getopt_long_only(argc, argv, ":aAc:CdD::fg:hHI:kl::L::m:M::n::o::p:P:qrs::S:tT::u:UvV::x:Xy", long_options, &option_index);
#else				/* defined _GNU_SOURCE */
		c = getopt(argc, argv, ":aAc:CdD::fg:hHI:kl::L::m:M::n::o::p:P:qrs::S:tT::u:UvV::x:Xy");
#endif				/* defined _GNU_SOURCE */
		if (c == -1) {
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: done options processing\n", argv[0]);
			break;
		}
		switch (c) {
		case 0:
			goto bad_usage;
		case 'a':	/* -a, --log-addresses */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: logging addresses\n", argv[0]);
			sa_logaddr++;
			break;
		case 'A':	/* -A, --append */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: will not truncate logfile\n", argv[0]);
#if defined NETSNMP_DS_LIB_APPEND_LOGFILES
			ds_set_boolean(DS_LIBRARY_ID, NETSNMP_DS_LIB_APPEND_LOGFILES, 1);
#endif				/* defined NETSNMP_DS_LIB_APPEND_LOGFILES */
			sa_appendlog = 1;
			break;
		case 'c':	/* -c, --config-file CONFIGFILE */
			if (optarg == NULL)
				goto bad_option;
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: using configuration file %s\n", argv[0], optarg);
			ds_set_string(DS_LIBRARY_ID, DS_LIB_OPTIONALCONFIG, optarg);
			break;
		case 'C':	/* -C, --no-configs */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: not reading default config files\n", argv[0]);
			ds_set_boolean(DS_LIBRARY_ID, DS_LIB_DONT_READ_CONFIGS, 1);
			break;
		case 'd':	/* -d, --dump */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: setting packet dump\n", argv[0]);
			sa_dump = 1;
			// snmp_set_dump_packet(sa_dump);
			ds_set_boolean(DS_LIBRARY_ID, DS_LIB_DUMP_PACKET, sa_dump);
			ds_set_boolean(DS_APPLICATION_ID, DS_AGENT_VERBOSE, sa_dump);
			break;
		case 'D':	/* -D, --debug [LEVEL], --debug-tokens [TOKENS] */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: increasing debug verbosity\n", argv[0]);
			if (optarg == NULL) {
				/* no option: must be -D, --debug */
				sa_debug++;
				if (sa_debug)
					snmp_log(MY_FACILITY(LOG_DEBUG), "%s: debug level is now %d\n", argv[0], sa_debug);
				if (sa_debug)
					snmp_log(MY_FACILITY(LOG_DEBUG), "%s: debugging all tokens\n", argv[0]);
				if (sa_debug)
					debug_register_tokens("ALL");
			} else {
				cptr = optarg;
				if ((val = strtol(optarg, &cptr, 0)) < 0)
					goto bad_option;
				if (*cptr == '\0') {
					/* it is just a number, must be -D, --debug [LEVEL] */
					sa_debug = val;
					if (sa_debug)
						snmp_log(MY_FACILITY(LOG_DEBUG), "%s: debug level is now %d\n", argv[0], sa_debug);
				} else {
					/* not a number, must be -D, --debug-tokens TOKENS */
					if (sa_debug)
						snmp_log(MY_FACILITY(LOG_DEBUG), "%s: debugging tokens %s\n", argv[0], optarg);
					debug_register_tokens(optarg);
				}
			}
			break;
		case 'f':	/* -f, --dont-fork */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: suppressing daemon mode\n", argv[0]);
			sa_nomead = 0;
			break;
		case 'u':	/* -u, --uid, --userid UID */
			cptr = optarg;
			if ((val = strtol(optarg, &cptr, 0)) < 0)
				goto bad_option;
			/* UID can be name or number */
			if ((pw = (*cptr == '\0') ? getpwuid((uid_t) val) : getpwnam(optarg)) == NULL)
				goto bad_option;
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: will run as uid %s(%d)\n", argv[0], pw->pw_name, pw->pw_uid);
			ds_set_int(DS_APPLICATION_ID, DS_AGENT_USERID, pw->pw_uid);
			break;
		case 'g':	/* -g, --gid, --groupdid GID */
			cptr = optarg;
			if ((val = strtol(optarg, &cptr, 0)) < 0)
				goto bad_option;
			/* GID can be name or number */
			if ((gr = (*cptr == '\0') ? getgrgid((gid_t) val) : getgrnam(optarg)) == NULL)
				goto bad_option;
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: will run as gid %s(%d)\n", argv[0], gr->gr_name, gr->gr_gid);
			ds_set_int(DS_APPLICATION_ID, DS_AGENT_GROUPID, gr->gr_gid);
			break;
		case 'h':	/* -h, --help, -?, --? */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: printing help message\n", argv[0]);
			sa_help(argc, argv);
			exit(0);
		case 'H':	/* -H, --help-directives */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: printing config directives\n", argv[0]);
			sa_help_directives(argc, argv);
			exit(0);
		case 'I':	/* -I, --init-modules, --initialize MODULE[{,| |:}MODULE]* */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: will initialize modules: %s\n", argv[0], optarg);
			add_to_init_list(optarg);
			break;
		case 'k':	/* -k, --keep-open */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: keeping files open\n", argv[0]);
			sa_fclose = 0;
			break;
		case 'l':	/* -l, --log-file, --logfile, -Lf, -LF p1[-p2] [LOGFILE] */
			if (optarg != NULL)
				strncpy(sa_logfile, optarg, sizeof(sa_logfile));
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: will log to file %s\n", argv[0], sa_logfile);
			sa_logfillog = 1;
			break;
		case 'L':	/* -L, --log-stderr, -Le, -LE p1[-p2] */
			/* Note that the recent NET-SNMP version of this option is far more complicated: -Le is the same as the old version of the option; -Lf LOGFILE is like the -l option; -Ls is
			   like the -s option; -Lo logs messages to standard output; -LX p1[-p2] [LOGFILE], where X = E, F, S or O, logs priority p1 and above to X, or p1 thru p2 to X. */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: logging to standard error\n", argv[0]);
			sa_logstderr = 1;
			break;
		case 'm':	/* -m, --mibs MIBS */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: using MIBS %s\n", argv[0], optarg);
			break;
		case 'M':	/* -M, --master or -M, --mibdirs MIBDIRS */
			if (optarg) {
				/* -M, --mibdirs MIBDIRS */
				if (sa_debug)
					snmp_log(MY_FACILITY(LOG_DEBUG), "%s: using MIBDIRS %s\n", argv[0], optarg);
			} else {
				/* -M, --master */
				if (sa_debug)
					snmp_log(MY_FACILITY(LOG_DEBUG), "%s: setting SNMP master\n", argv[0]);
				sa_agentx = 0;
				ds_set_boolean(DS_APPLICATION_ID, DS_AGENT_ROLE, 0);
			}
			break;
		case 'n':	/* -n, --nodaemon or -n, --name NAME */
			if (optarg) {
				/* -n, --name NAME */
				if (sa_debug)
					snmp_log(MY_FACILITY(LOG_DEBUG), "%s: using name %s\n", argv[0], optarg);
				ds_set_string(DS_APPLICATION_ID, DS_AGENT_PROGNAME, optarg);
			} else {
				/* -n, --nodaemon */
				if (sa_debug)
					snmp_log(MY_FACILITY(LOG_DEBUG), "%s: suppressing deamon mode\n", argv[0]);
				sa_nomead = 0;
				ds_set_string(DS_APPLICATION_ID, DS_AGENT_PROGNAME, basename(argv[0]));
			}
			break;
		case 'N':	/* -N, --dry-run */
#if defined NETSNMP_DS_AGENT_QUIT_IMMEDIATELY
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: setting for dry-runs startup\n", argv[0]);
			ds_set_boolean(DS_APPLICATION_ID, NETSNMP_DS_AGENT_QUIT_IMMEDIATELY, 1);
			break;
#else				/* defined NETSNMP_DS_AGENT_QUIT_IMMEDIATELY */
			snmp_log(MY_FACILITY(LOG_DEBUG), "%s: -N option not supported\n", argv[0]);
			goto bad_option;
#endif				/* defined NETSNMP_DS_AGENT_QUIT_IMMEDIATELY */
		case 'o':	/* -o, --log-stdout, -Lo, -LO p1[-p2] */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: logging to stdout\n", argv[0]);
			sa_logstdout = 1;
			break;
		case 'p':	/* -p, --port PORTNUM or -p, --pidfile PIDFILE */
			cptr = optarg;
			if ((val = strtol(optarg, &cptr, 0)) < 0 || val > 16383)
				goto bad_option;
			if (*cptr == '\0') {
				char buf[4096];

				/* -p, --port PORTNUM */
				if ((cptr = ds_get_string(DS_APPLICATION_ID, DS_AGENT_PORTS)))
					snprintf(buf, sizeof(buf), "%s,%s", cptr, optarg);
				else
					strncpy(buf, optarg, sizeof(buf));
				ds_set_string(DS_APPLICATION_ID, DS_AGENT_PORTS, buf);
				break;
			}
			/* fall through */
		case 'P':	/* -p, -P, --pidfile PIDFILE */
			if (optarg) {
				/* either it exists */
				if (stat(optarg, &st) == -1) {
					/* or we can create it */
					if ((fd = open(optarg, O_CREAT, 0600)) == -1) {
						perror(argv[0]);
						goto bad_option;
					}
					close(fd);
				}
				if (sa_debug)
					snmp_log(MY_FACILITY(LOG_DEBUG), "%s: setting pid file to %s\n", argv[0], optarg);
				strncpy(sa_pidfile, optarg, sizeof(sa_pidfile));
			}
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: using pidfile %s\n", argv[0], sa_pidfile);
			break;
		case 'q':	/* -q, --quiet, --quick */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: suppressing normal output\n", argv[0]);
			sa_debug = 0;
			sa_output = 0;
			ds_set_boolean(DS_APPLICATION_ID, DS_AGENT_VERBOSE, 0);
			// snmp_set_quick_print();
			ds_set_boolean(DS_LIBRARY_ID, DS_LIB_QUICK_PRINT, 1);
			break;
		case 'r':	/* -r, --noroot */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: setting for non-root access\n", argv[0]);
			ds_set_boolean(DS_APPLICATION_ID, DS_AGENT_NO_ROOT_ACCESS, 1);
			break;
		case 's':	/* -s, --log-syslog, -Ls, -LS p1[-p2] */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: logging to system logs\n", argv[0]);
			sa_logsyslog = 1;
			break;
		case 'S':	/* -S, -sysctl-file FILENAME */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: using %s for backing\n", argv[0], optarg);
			strncpy(sa_sysctlf, optarg, sizeof(sa_sysctlf));
			break;
		case 't':	/* -t, --agent-alarms */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: setting agent alarms\n", argv[0]);
			sa_alarms = 0;
			ds_set_boolean(DS_LIBRARY_ID, DS_LIB_ALARM_DONT_USE_SIG, 1);
			break;
		case 'T':	/* -T, --transport [TRANSPORT] */
			if (optarg == NULL)
				goto udp_transport;
			if (!strcasecmp("TCP", optarg)) {
				if (sa_debug)
					snmp_log(MY_FACILITY(LOG_DEBUG), "%s: setting default transport to TCP\n", argv[0]);
				val = ds_get_int(DS_APPLICATION_ID, DS_AGENT_FLAGS);
				val |= SNMP_FLAGS_STREAM_SOCKET;
				ds_set_int(DS_APPLICATION_ID, DS_AGENT_FLAGS, val);
			} else if (!strcasecmp("UDP", optarg)) {
			      udp_transport:
				if (sa_debug)
					snmp_log(MY_FACILITY(LOG_DEBUG), "%s: setting default transport to UDP\n", argv[0]);
				val = ds_get_int(DS_APPLICATION_ID, DS_AGENT_FLAGS);
				val &= ~SNMP_FLAGS_STREAM_SOCKET;
				ds_set_int(DS_APPLICATION_ID, DS_AGENT_FLAGS, val);
			} else
				goto bad_option;
			break;
		case 'U':
#if defined NETSNMP_DS_AGENT_LEAVE_PIDFILE
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: will leave pidfile after shutdown\n", argv[0]);
			ds_set_boolean(DS_APPLICATION_ID, NETSNMP_DS_AGENT_LEAVE_PIDFILE, 1);
#else
			snmp_log(MY_FACILITY(LOG_DEBUG), "%s: -U option not supported\n");
			goto bad_option;
#endif				/* defined NETSNMP_DS_AGENT_LEAVE_PIDFILE */
			break;
		case 'v':	/* -v, --version */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: printing version message\n", argv[0]);
			sa_version(argc, argv);
			exit(0);
		case 'V':	/* -V, --verbose [LEVEL] */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: increasing output verbosity\n", argv[0]);
			if (optarg == NULL) {
				sa_output++;
			} else {
				if ((val = strtol(optarg, NULL, 0)) < 0)
					goto bad_option;
				sa_output = val;
			}
			if (sa_output > 1)
				ds_set_boolean(DS_APPLICATION_ID, DS_AGENT_VERBOSE, 1);
			else
				ds_set_boolean(DS_APPLICATION_ID, DS_AGENT_VERBOSE, 0);
			break;
		case 'x':	/* -x, --agentx-socket SOCKET */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: setting AgentX socket to %s\n", argv[0], optarg);
			ds_set_string(DS_APPLICATION_ID, DS_AGENT_X_SOCKET, optarg);
			// ds_set_boolean(DS_APPLICATION_ID, DS_AGENT_AGENTX_MASTER, 1);
			break;
		case 'X':	/* -X, --agentx */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: setting AgentX sub-agent\n", argv[0]);
			sa_agentx = 1;
			ds_set_boolean(DS_APPLICATION_ID, DS_AGENT_ROLE, 1);
			break;
		case 'y':	/* -y, --copying */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: printing copying message\n", argv[0]);
			sa_copying(argc, argv);
			exit(0);
		case '?':
		case ':':
		default:
		      bad_option:
			optind--;
			goto bad_nonopt;
		      bad_nonopt:
			if (sa_output || sa_debug) {
				if (optind < argc) {
					fprintf(stderr, "%s: syntax error near '", argv[0]);
					while (optind < argc)
						fprintf(stderr, "%s ", argv[optind++]);
					fprintf(stderr, "'\n");
				} else {
					fprintf(stderr, "%s: missing option or argument", argv[0]);
					fprintf(stderr, "\n");
				}
				fflush(stderr);
			      bad_usage:
				sa_usage(argc, argv);
			}
			exit(2);
		}
	}
	if (optind < argc) {
		if (sa_debug)
			snmp_log(MY_FACILITY(LOG_DEBUG), "%s: excess non-option arguments\n", argv[0]);
		goto bad_nonopt;
	}
	sa_enter(argc, argv);	/* daemonize if necessary */
	sa_mloop(argc, argv);	/* execute main loop */
	exit(0);
}
#endif				/* !defined MODULE */
#endif				/* defined MASTER */
