/*****************************************************************************

 @(#) $RCSfile: sccpOmMIB.c,v $ $Name:  $($Revision: 0.9.2.3 $) $Date: 2008-12-27 15:29:47 $

 -----------------------------------------------------------------------------

 Copyright (c) 2001-2008  OpenSS7 Corporation <http://www.openss7.com/>
 Copyright (c) 1997-2000  Brian F. G. Bidulock <bidulock@openss7.org>

 All Rights Reserved.

 This program is free software: you can redistribute it and/or modify it under
 the terms of the GNU Affero General Public License as published by the Free
 Software Foundation, version 3 of the license.

 This program is distributed in the hope that it will be useful, but WITHOUT
 ANY WARRANTY; without even the implied warranty of MERCHANTABILITY or FITNESS
 FOR A PARTICULAR PURPOSE.  See the GNU Affero General Public License for more
 details.

 You should have received a copy of the GNU Affero General Public License
 along with this program.  If not, see <http://www.gnu.org/licenses/>, or
 write to the Free Software Foundation, Inc., 675 Mass Ave, Cambridge, MA
 02139, USA.

 -----------------------------------------------------------------------------

 U.S. GOVERNMENT RESTRICTED RIGHTS.  If you are licensing this Software on
 behalf of the U.S. Government ("Government"), the following provisions apply
 to you.  If the Software is supplied by the Department of Defense ("DoD"), it
 is classified as "Commercial Computer Software" under paragraph 252.227-7014
 of the DoD Supplement to the Federal Acquisition Regulations ("DFARS") (or any
 successor regulations) and the Government is acquiring only the license rights
 granted herein (the license rights customarily provided to non-Government
 users).  If the Software is supplied to any unit or agency of the Government
 other than DoD, it is classified as "Restricted Computer Software" and the
 Government's rights in the Software are defined in paragraph 52.227-19 of the
 Federal Acquisition Regulations ("FAR") (or any successor regulations) or, in
 the cases of NASA, in paragraph 18.52.227-86 of the NASA Supplement to the FAR
 (or any successor regulations).

 -----------------------------------------------------------------------------

 Commercial licensing and support of this software is available from OpenSS7
 Corporation at a fee.  See http://www.openss7.com/

 -----------------------------------------------------------------------------

 Last Modified $Date: 2008-12-27 15:29:47 $ by $Author: brian $

 -----------------------------------------------------------------------------

 $Log: sccpOmMIB.c,v $
 Revision 0.9.2.3  2008-12-27 15:29:47  brian
 - split MTP and MTP OM mib

 Revision 0.9.2.2  2008-12-24 13:08:25  brian
 - building ss7confd

 *****************************************************************************/

#ident "@(#) $RCSfile: sccpOmMIB.c,v $ $Name:  $($Revision: 0.9.2.3 $) $Date: 2008-12-27 15:29:47 $"

static char const ident[] = "$RCSfile: sccpOmMIB.c,v $ $Name:  $($Revision: 0.9.2.3 $) $Date: 2008-12-27 15:29:47 $";

/* This file was generated by mib2c and is intended for use as
   a mib module for the ucd-snmp snmpd agent. */
#include <ucd-snmp/ucd-snmp-config.h>
#include <ucd-snmp/ucd-snmp-includes.h>
#include <ucd-snmp/ucd-snmp-agent-includes.h>
#include <ucd-snmp/callback.h>
#include <ucd-snmp/snmp-tc.h>
#include <ucd-snmp/default_store.h>
#include <ucd-snmp/snmp_alarm.h>
/* The following header files are mangled in most recent net-snmp releases so
 * the versions from UCD-SNMP 4.2.5 are included here.  */
#if defined HAVE_LIBNETSNMP
#else				/* defined HAVE_LIBNETSNMP */
#endif				/* defined HAVE_LIBNETSNMP */
/* These are messed up on both. */
#include "ds_agent.h"
#ifdef HAVE_UCD_SNMP_UTIL_FUNCS_H
#include <ucd-snmp/util_funcs.h>
/* Many recent net-snmp UCD compatible headers do not declard header_generic. */
int header_generic(struct variable *, oid *, size_t *, int, size_t *, WriteMethod **);
#else				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#include "util_funcs.h"
#endif				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#ifdef HAVE_UCD_SNMP_HEADER_COMPLEX_H
#include <ucd-snmp/header_complex.h>
#else				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
#include "header_complex.h"
#endif				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
/* This one is the other way around: it is just fine for net-snmp, but
 * ucd-snmp does not provide the header file at all.  */
#ifdef HAVE_UCD_SNMP_MIB_MODULES_H
#include <ucd-snmp/mib_modules.h>
#else				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#ifdef HAVE_NET_SNMP_AGENT_MIB_MODULES_H
#include <net-snmp/agent/mib_modules.h>
#else				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#include "mib_modules.h"
#endif				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#endif				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#include <stdint.h>
#include <signal.h>
#include <sys/stat.h>		/* for struct stat, fstat() */
#include <sys/types.h>
#include <unistd.h>
#include <stdlib.h>
#include <pwd.h>		/* for getpwuid() getpwnam() */
#include <grp.h>		/* for getgrgid() getgrnam() */
#include <libgen.h>		/* for basename() */
#include <fcntl.h>		/* for O_CREAT */
#include <sys/sysctl.h>		/* for sysctl */
#include <errno.h>
#include <string.h>
#ifdef _GNU_SOURCE
#include <getopt.h>
#endif
#include "ss7MIB.h"
#include "x400pMIB.h"
#include "mtpMIB.h"
#include "mtpOmMIB.h"
#include "sccpMIB.h"
#include "sccpOmMIB.h"
extern const char sa_program[];

#define MY_FACILITY(__pri)	(LOG_DAEMON|(__pri))
#if !defined MODULE
extern int sa_dump;			/* default packet dump */
extern int sa_debug;			/* default no debug */
extern int sa_nomead;			/* default daemon mode */
extern int sa_output;			/* default normal output */
extern int sa_agentx;			/* default agentx mode */
extern int sa_alarms;			/* default application alarms */
extern int sa_fclose;			/* default close files between requests */
extern int sa_logaddr;			/* log addresses */
extern int sa_logfillog;		/* log to sa_logfile */
extern int sa_logstderr;		/* log to standard error */
extern int sa_logstdout;		/* log to standard output */
extern int sa_logsyslog;		/* log to system logs */
extern int sa_logcallog;		/* log to callback logs */
extern int sa_appendlog;		/* append to log file without truncating */
extern char sa_logfile[256];
extern char sa_pidfile[256];
extern char sa_sysctlf[256];

/* file stream for log file */
extern FILE *stdlog;

/* file descriptor for MIB use */
extern int sa_fd;

/* indication to reread MIB configuration */
extern int sa_changed;

/* indications that statistics, the mib or its tables need to be refreshed */
extern int sa_stats_refresh;
#endif				/* !defined MODULE */
/* request number for per-request actions */
extern int sa_request;
volatile int sccpOmMIB_refresh = 1;
volatile int sccpOmErrorsTable_refresh = 1;
volatile int sccpOmMessageTable_refresh = 1;
volatile int sccpOmAccessibilityTable_refresh = 1;
volatile int sccpOmUtilizationTable_refresh = 1;
volatile int sccpOm5MinHistoryTable_refresh = 1;
volatile int sccpOm15MinHistoryTable_refresh = 1;
volatile int sccpOm5MinSsnHistoryTable_refresh = 1;
volatile int sccpOm15MinSsnHistoryTable_refresh = 1;

/*
 * sccpOmMIB_variables_oid:
 *   this is the top level oid that we want to register under.  This
 *   is essentially a prefix, with the suffix appearing in the
 *   variable below.
 */
oid sccpOmMIB_variables_oid[] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 2 };

/*
 * variable7 sccpOmMIB_variables:
 *   this variable defines function callbacks and type return information
 *   for the sccpOmMIB mib section
 */
struct variable7 sccpOmMIB_variables[] = {
/*  magic number        , variable type , ro/rw , callback fn  , L, oidsuffix */
#define   SCCPOMNOTRANSLATORFORADDRESS  (4 % 256)
	{SCCPOMNOTRANSLATORFORADDRESS, ASN_GAUGE, RONLY, var_sccpOmErrorsTable, 5, {1, 1, 1, 1, 1}},
#define   SCCPOMNORULEFORADDRESS  (5 % 256)
	{SCCPOMNORULEFORADDRESS, ASN_GAUGE, RONLY, var_sccpOmErrorsTable, 5, {1, 1, 1, 1, 2}},
#define   SCCPOMPOINTCODENOTAVAILABLE  (6 % 256)
	{SCCPOMPOINTCODENOTAVAILABLE, ASN_GAUGE, RONLY, var_sccpOmErrorsTable, 5, {1, 1, 1, 1, 3}},
#define   SCCPOMPOINTCODECONGESTED  (7 % 256)
	{SCCPOMPOINTCODECONGESTED, ASN_GAUGE, RONLY, var_sccpOmErrorsTable, 5, {1, 1, 1, 1, 4}},
#define   SCCPOMSUBSYSTEMUNAVAILABLE  (8 % 256)
	{SCCPOMSUBSYSTEMUNAVAILABLE, ASN_GAUGE, RONLY, var_sccpOmErrorsTable, 5, {1, 1, 1, 1, 5}},
#define   SCCPOMSUBSYSTEMCONGESTED  (9 % 256)
	{SCCPOMSUBSYSTEMCONGESTED, ASN_GAUGE, RONLY, var_sccpOmErrorsTable, 5, {1, 1, 1, 1, 6}},
#define   SCCPOMUNEQUIPPEDSUBSYSTEM  (10 % 256)
	{SCCPOMUNEQUIPPEDSUBSYSTEM, ASN_GAUGE, RONLY, var_sccpOmErrorsTable, 5, {1, 1, 1, 1, 7}},
#define   SCCPOMSYNTAXERRORDETECTED  (11 % 256)
	{SCCPOMSYNTAXERRORDETECTED, ASN_GAUGE, RONLY, var_sccpOmErrorsTable, 5, {1, 1, 1, 1, 8}},
#define   SCCPOMROUTINGFAILURE  (12 % 256)
	{SCCPOMROUTINGFAILURE, ASN_GAUGE, RONLY, var_sccpOmErrorsTable, 5, {1, 1, 1, 1, 9}},
#define   SCCPOMREASSEMBLYTIMEOUT  (13 % 256)
	{SCCPOMREASSEMBLYTIMEOUT, ASN_GAUGE, RONLY, var_sccpOmErrorsTable, 5, {1, 1, 1, 1, 10}},
#define   SCCPOMSEGMENTOUTOFORDER  (14 % 256)
	{SCCPOMSEGMENTOUTOFORDER, ASN_GAUGE, RONLY, var_sccpOmErrorsTable, 5, {1, 1, 1, 1, 11}},
#define   SCCPOMNOREASSEMBLYSPACE  (15 % 256)
	{SCCPOMNOREASSEMBLYSPACE, ASN_GAUGE, RONLY, var_sccpOmErrorsTable, 5, {1, 1, 1, 1, 12}},
#define   SCCPOMHOPCOUNTERVIOLATION  (16 % 256)
	{SCCPOMHOPCOUNTERVIOLATION, ASN_GAUGE, RONLY, var_sccpOmErrorsTable, 5, {1, 1, 1, 1, 13}},
#define   SCCPOMTOOLARGEFORSEGMENTATION  (17 % 256)
	{SCCPOMTOOLARGEFORSEGMENTATION, ASN_GAUGE, RONLY, var_sccpOmErrorsTable, 5, {1, 1, 1, 1, 14}},
#define   SCCPOMRELEASEFAILURE  (18 % 256)
	{SCCPOMRELEASEFAILURE, ASN_GAUGE, RONLY, var_sccpOmErrorsTable, 5, {1, 1, 1, 1, 15}},
#define   SCCPOMINACTIVITYTIMEREXPIRY  (19 % 256)
	{SCCPOMINACTIVITYTIMEREXPIRY, ASN_GAUGE, RONLY, var_sccpOmErrorsTable, 5, {1, 1, 1, 1, 16}},
#define   SCCPOMPROVIDERINITIATEDRESET  (20 % 256)
	{SCCPOMPROVIDERINITIATEDRESET, ASN_GAUGE, RONLY, var_sccpOmErrorsTable, 5, {1, 1, 1, 1, 17}},
#define   SCCPOMPROVIDERINITIATEDRELEASE  (21 % 256)
	{SCCPOMPROVIDERINITIATEDRELEASE, ASN_GAUGE, RONLY, var_sccpOmErrorsTable, 5, {1, 1, 1, 1, 18}},
#define   SCCPOMNOSEGMENTATIONSUPPORT  (22 % 256)
	{SCCPOMNOSEGMENTATIONSUPPORT, ASN_GAUGE, RONLY, var_sccpOmErrorsTable, 5, {1, 1, 1, 1, 19}},
#define   SCCPOMSEGMENTATIONFAILURE  (23 % 256)
	{SCCPOMSEGMENTATIONFAILURE, ASN_GAUGE, RONLY, var_sccpOmErrorsTable, 5, {1, 1, 1, 1, 20}},
#define   SCCPOMREASSEMBLYFAILURE  (24 % 256)
	{SCCPOMREASSEMBLYFAILURE, ASN_GAUGE, RONLY, var_sccpOmErrorsTable, 5, {1, 1, 1, 1, 21}},
#define   SCCPOMERRORS5MINVALIDINTERVALS  (25 % 256)
	{SCCPOMERRORS5MINVALIDINTERVALS, ASN_INTEGER, RONLY, var_sccpOmErrorsTable, 5, {1, 1, 1, 1, 22}},
#define   SCCPOMERRORS15MINVALIDINTERVALS  (26 % 256)
	{SCCPOMERRORS15MINVALIDINTERVALS, ASN_INTEGER, RONLY, var_sccpOmErrorsTable, 5, {1, 1, 1, 1, 23}},
#define   SCCPOMTOTALMESSAGESHANDLED  (30 % 256)
	{SCCPOMTOTALMESSAGESHANDLED, ASN_GAUGE, RONLY, var_sccpOmMessageTable, 5, {1, 1, 2, 1, 1}},
#define   SCCPOMTOTALMESSAGESLOCAL  (31 % 256)
	{SCCPOMTOTALMESSAGESLOCAL, ASN_GAUGE, RONLY, var_sccpOmMessageTable, 5, {1, 1, 2, 1, 2}},
#define   SCCPOMTOTALMESSAGESGTT  (32 % 256)
	{SCCPOMTOTALMESSAGESGTT, ASN_GAUGE, RONLY, var_sccpOmMessageTable, 5, {1, 1, 2, 1, 3}},
#define   SCCPOMUDTMESSAGESSENT  (33 % 256)
	{SCCPOMUDTMESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOmMessageTable, 5, {1, 1, 2, 1, 4}},
#define   SCCPOMUDTSMESSAGESSENT  (34 % 256)
	{SCCPOMUDTSMESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOmMessageTable, 5, {1, 1, 2, 1, 5}},
#define   SCCPOMUDTMESSAGESRECEIVED  (35 % 256)
	{SCCPOMUDTMESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOmMessageTable, 5, {1, 1, 2, 1, 6}},
#define   SCCPOMUDTSMESSAGESRECEIVED  (36 % 256)
	{SCCPOMUDTSMESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOmMessageTable, 5, {1, 1, 2, 1, 7}},
#define   SCCPOMCRMESSAGESSENT  (37 % 256)
	{SCCPOMCRMESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOmMessageTable, 5, {1, 1, 2, 1, 8}},
#define   SCCPOMCREFMESSAGESSENT  (38 % 256)
	{SCCPOMCREFMESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOmMessageTable, 5, {1, 1, 2, 1, 9}},
#define   SCCPOMCRMESSAGESRECEIVED  (39 % 256)
	{SCCPOMCRMESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOmMessageTable, 5, {1, 1, 2, 1, 10}},
#define   SCCPOMCREFMESSAGESRECEIVED  (40 % 256)
	{SCCPOMCREFMESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOmMessageTable, 5, {1, 1, 2, 1, 11}},
#define   SCCPOMRSRMESSAGESSENT  (41 % 256)
	{SCCPOMRSRMESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOmMessageTable, 5, {1, 1, 2, 1, 12}},
#define   SCCPOMRSRMESSAGESRECEIVED  (42 % 256)
	{SCCPOMRSRMESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOmMessageTable, 5, {1, 1, 2, 1, 13}},
#define   SCCPOMERRMESSAGESSENT  (43 % 256)
	{SCCPOMERRMESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOmMessageTable, 5, {1, 1, 2, 1, 14}},
#define   SCCPOMERRMESSAGESRECEIVED  (44 % 256)
	{SCCPOMERRMESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOmMessageTable, 5, {1, 1, 2, 1, 15}},
#define   SCCPOMXUDTMESSAGESSENT  (45 % 256)
	{SCCPOMXUDTMESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOmMessageTable, 5, {1, 1, 2, 1, 16}},
#define   SCCPOMXUDTSMESSAGESSENT  (46 % 256)
	{SCCPOMXUDTSMESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOmMessageTable, 5, {1, 1, 2, 1, 17}},
#define   SCCPOMXUDTMESSAGESRECEIVED  (47 % 256)
	{SCCPOMXUDTMESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOmMessageTable, 5, {1, 1, 2, 1, 18}},
#define   SCCPOMXUDTSMESSAGESRECEIVED  (48 % 256)
	{SCCPOMXUDTSMESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOmMessageTable, 5, {1, 1, 2, 1, 19}},
#define   SCCPOMLUDTMESSAGESSENT  (49 % 256)
	{SCCPOMLUDTMESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOmMessageTable, 5, {1, 1, 2, 1, 20}},
#define   SCCPOMLUDTSMESSAGESSENT  (50 % 256)
	{SCCPOMLUDTSMESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOmMessageTable, 5, {1, 1, 2, 1, 21}},
#define   SCCPOMLUDTMESSAGESRECEIVED  (51 % 256)
	{SCCPOMLUDTMESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOmMessageTable, 5, {1, 1, 2, 1, 22}},
#define   SCCPOMLUDTSMESSAGESRECEIVED  (52 % 256)
	{SCCPOMLUDTSMESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOmMessageTable, 5, {1, 1, 2, 1, 23}},
#define   SCCPOMMESSAGE5MINVALIDINTERVALS  (53 % 256)
	{SCCPOMMESSAGE5MINVALIDINTERVALS, ASN_INTEGER, RONLY, var_sccpOmMessageTable, 5, {1, 1, 2, 1, 24}},
#define   SCCPOMMESSAGE15MINVALIDINTERVALS  (54 % 256)
	{SCCPOMMESSAGE15MINVALIDINTERVALS, ASN_INTEGER, RONLY, var_sccpOmMessageTable, 5, {1, 1, 2, 1, 25}},
#define   SCCPOMSSCMESSAGERECEIVED  (58 % 256)
	{SCCPOMSSCMESSAGERECEIVED, ASN_GAUGE, RONLY, var_sccpOmAccessibilityTable, 5, {1, 1, 3, 1, 1}},
#define   SCCPOMSSPMESSAGERECEIVED  (59 % 256)
	{SCCPOMSSPMESSAGERECEIVED, ASN_GAUGE, RONLY, var_sccpOmAccessibilityTable, 5, {1, 1, 3, 1, 2}},
#define   SCCPOMSCCPUNAVAILABLEDURATION  (60 % 256)
	{SCCPOMSCCPUNAVAILABLEDURATION, ASN_UNSIGNED, RONLY, var_sccpOmAccessibilityTable, 5, {1, 1, 3, 1, 3}},
#define   SCCPOMACCESSIBILITY5MINVALIDINTERVALS  (61 % 256)
	{SCCPOMACCESSIBILITY5MINVALIDINTERVALS, ASN_INTEGER, RONLY, var_sccpOmAccessibilityTable, 5, {1, 1, 3, 1, 4}},
#define   SCCPOMACCESSIBILITY15MINVALIDINTERVALS  (62 % 256)
	{SCCPOMACCESSIBILITY15MINVALIDINTERVALS, ASN_INTEGER, RONLY, var_sccpOmAccessibilityTable, 5, {1, 1, 3, 1, 5}},
#define   SCCPOMLXUDTMESSAGESORIGCLASS0  (66 % 256)
	{SCCPOMLXUDTMESSAGESORIGCLASS0, ASN_GAUGE, RONLY, var_sccpOmUtilizationTable, 5, {1, 1, 4, 1, 1}},
#define   SCCPOMLXUDTMESSAGESORIGCLASS1  (67 % 256)
	{SCCPOMLXUDTMESSAGESORIGCLASS1, ASN_GAUGE, RONLY, var_sccpOmUtilizationTable, 5, {1, 1, 4, 1, 2}},
#define   SCCPOMLXUDTMESSAGESTERMCLASS0  (68 % 256)
	{SCCPOMLXUDTMESSAGESTERMCLASS0, ASN_GAUGE, RONLY, var_sccpOmUtilizationTable, 5, {1, 1, 4, 1, 3}},
#define   SCCPOMLXUDTMESSAGESTERMCLASS1  (69 % 256)
	{SCCPOMLXUDTMESSAGESTERMCLASS1, ASN_GAUGE, RONLY, var_sccpOmUtilizationTable, 5, {1, 1, 4, 1, 4}},
#define   SCCPOMMESSAGESSENTTOBACKUP  (70 % 256)
	{SCCPOMMESSAGESSENTTOBACKUP, ASN_GAUGE, RONLY, var_sccpOmUtilizationTable, 5, {1, 1, 4, 1, 5}},
#define   SCCPOMDT1MESSAGESRECEIVED  (71 % 256)
	{SCCPOMDT1MESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOmUtilizationTable, 5, {1, 1, 4, 1, 6}},
#define   SCCPOMDT1MESSAGESSENT  (72 % 256)
	{SCCPOMDT1MESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOmUtilizationTable, 5, {1, 1, 4, 1, 7}},
#define   SCCPOMDT2MESSAGESRECEIVED  (73 % 256)
	{SCCPOMDT2MESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOmUtilizationTable, 5, {1, 1, 4, 1, 8}},
#define   SCCPOMDT2MESSAGESSENT  (74 % 256)
	{SCCPOMDT2MESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOmUtilizationTable, 5, {1, 1, 4, 1, 9}},
#define   SCCPOMEDMESSAGESSENT  (75 % 256)
	{SCCPOMEDMESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOmUtilizationTable, 5, {1, 1, 4, 1, 10}},
#define   SCCPOMEDMESSAGESRECEIVED  (76 % 256)
	{SCCPOMEDMESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOmUtilizationTable, 5, {1, 1, 4, 1, 11}},
#define   SCCPOMUTILIZATION5MINVALIDINTERVALS  (77 % 256)
	{SCCPOMUTILIZATION5MINVALIDINTERVALS, ASN_INTEGER, RONLY, var_sccpOmUtilizationTable, 5, {1, 1, 4, 1, 12}},
#define   SCCPOMUTILIZATION15MINVALIDINTERVALS  (78 % 256)
	{SCCPOMUTILIZATION15MINVALIDINTERVALS, ASN_INTEGER, RONLY, var_sccpOmUtilizationTable, 5, {1, 1, 4, 1, 13}},
#define   SCCPOM5MINNOTRANSLATORFORADDRESS  (83 % 256)
	{SCCPOM5MINNOTRANSLATORFORADDRESS, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 1}},
#define   SCCPOM5MINNORULEFORADDRESS  (84 % 256)
	{SCCPOM5MINNORULEFORADDRESS, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 2}},
#define   SCCPOM5MINPOINTCODENOTAVAILABLE  (85 % 256)
	{SCCPOM5MINPOINTCODENOTAVAILABLE, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 3}},
#define   SCCPOM5MINPOINTCODECONGESTED  (86 % 256)
	{SCCPOM5MINPOINTCODECONGESTED, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 4}},
#define   SCCPOM5MINSUBSYSTEMUNAVAILABLE  (87 % 256)
	{SCCPOM5MINSUBSYSTEMUNAVAILABLE, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 5}},
#define   SCCPOM5MINSUBSYSTEMCONGESTED  (88 % 256)
	{SCCPOM5MINSUBSYSTEMCONGESTED, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 6}},
#define   SCCPOM5MINUNEQUIPPEDSUBSYSTEM  (89 % 256)
	{SCCPOM5MINUNEQUIPPEDSUBSYSTEM, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 7}},
#define   SCCPOM5MINSYNTAXERRORDETECTED  (90 % 256)
	{SCCPOM5MINSYNTAXERRORDETECTED, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 8}},
#define   SCCPOM5MINROUTINGFAILURE  (91 % 256)
	{SCCPOM5MINROUTINGFAILURE, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 9}},
#define   SCCPOM5MINREASSEMBLYTIMEOUT  (92 % 256)
	{SCCPOM5MINREASSEMBLYTIMEOUT, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 10}},
#define   SCCPOM5MINSEGMENTOUTOFORDER  (93 % 256)
	{SCCPOM5MINSEGMENTOUTOFORDER, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 11}},
#define   SCCPOM5MINNOREASSEMBLYSPACE  (94 % 256)
	{SCCPOM5MINNOREASSEMBLYSPACE, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 12}},
#define   SCCPOM5MINHOPCOUNTERVIOLATION  (95 % 256)
	{SCCPOM5MINHOPCOUNTERVIOLATION, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 13}},
#define   SCCPOM5MINTOOLARGEFORSEGMENTATION  (96 % 256)
	{SCCPOM5MINTOOLARGEFORSEGMENTATION, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 14}},
#define   SCCPOM5MINRELEASEFAILURE  (97 % 256)
	{SCCPOM5MINRELEASEFAILURE, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 15}},
#define   SCCPOM5MININACTIVITYTIMEREXPIRY  (98 % 256)
	{SCCPOM5MININACTIVITYTIMEREXPIRY, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 16}},
#define   SCCPOM5MINPROVIDERINITIATEDRESET  (99 % 256)
	{SCCPOM5MINPROVIDERINITIATEDRESET, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 17}},
#define   SCCPOM5MINPROVIDERINITIATEDRELEASE  (100 % 256)
	{SCCPOM5MINPROVIDERINITIATEDRELEASE, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 18}},
#define   SCCPOM5MINNOSEGMENTATIONSUPPORT  (101 % 256)
	{SCCPOM5MINNOSEGMENTATIONSUPPORT, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 19}},
#define   SCCPOM5MINSEGMENTATIONFAILURE  (102 % 256)
	{SCCPOM5MINSEGMENTATIONFAILURE, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 20}},
#define   SCCPOM5MINREASSEMBLYFAILURE  (103 % 256)
	{SCCPOM5MINREASSEMBLYFAILURE, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 21}},
#define   SCCPOM5MINTOTALMESSAGESHANDLED  (104 % 256)
	{SCCPOM5MINTOTALMESSAGESHANDLED, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 22}},
#define   SCCPOM5MINTOTALMESSAGESLOCAL  (105 % 256)
	{SCCPOM5MINTOTALMESSAGESLOCAL, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 23}},
#define   SCCPOM5MINTOTALMESSAGESGTT  (106 % 256)
	{SCCPOM5MINTOTALMESSAGESGTT, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 24}},
#define   SCCPOM5MINUDTMESSAGESSENT  (107 % 256)
	{SCCPOM5MINUDTMESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 25}},
#define   SCCPOM5MINUDTSMESSAGESSENT  (108 % 256)
	{SCCPOM5MINUDTSMESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 26}},
#define   SCCPOM5MINUDTMESSAGESRECEIVED  (109 % 256)
	{SCCPOM5MINUDTMESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 27}},
#define   SCCPOM5MINUDTSMESSAGESRECEIVED  (110 % 256)
	{SCCPOM5MINUDTSMESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 28}},
#define   SCCPOM5MINCRMESSAGESSENT  (111 % 256)
	{SCCPOM5MINCRMESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 29}},
#define   SCCPOM5MINCREFMESSAGESSENT  (112 % 256)
	{SCCPOM5MINCREFMESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 30}},
#define   SCCPOM5MINCRMESSAGESRECEIVED  (113 % 256)
	{SCCPOM5MINCRMESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 31}},
#define   SCCPOM5MINCREFMESSAGESRECEIVED  (114 % 256)
	{SCCPOM5MINCREFMESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 32}},
#define   SCCPOM5MINRSRMESSAGESSENT  (115 % 256)
	{SCCPOM5MINRSRMESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 33}},
#define   SCCPOM5MINRSRMESSAGESRECEIVED  (116 % 256)
	{SCCPOM5MINRSRMESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 34}},
#define   SCCPOM5MINERRMESSAGESSENT  (117 % 256)
	{SCCPOM5MINERRMESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 35}},
#define   SCCPOM5MINERRMESSAGESRECEIVED  (118 % 256)
	{SCCPOM5MINERRMESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 36}},
#define   SCCPOM5MINXUDTMESSAGESSENT  (119 % 256)
	{SCCPOM5MINXUDTMESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 37}},
#define   SCCPOM5MINXUDTSMESSAGESSENT  (120 % 256)
	{SCCPOM5MINXUDTSMESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 38}},
#define   SCCPOM5MINXUDTMESSAGESRECEIVED  (121 % 256)
	{SCCPOM5MINXUDTMESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 39}},
#define   SCCPOM5MINXUDTSMESSAGESRECEIVED  (122 % 256)
	{SCCPOM5MINXUDTSMESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 40}},
#define   SCCPOM5MINLUDTMESSAGESSENT  (123 % 256)
	{SCCPOM5MINLUDTMESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 41}},
#define   SCCPOM5MINLUDTSMESSAGESSENT  (124 % 256)
	{SCCPOM5MINLUDTSMESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 42}},
#define   SCCPOM5MINLUDTMESSAGESRECEIVED  (125 % 256)
	{SCCPOM5MINLUDTMESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 43}},
#define   SCCPOM5MINLUDTSMESSAGESRECEIVED  (126 % 256)
	{SCCPOM5MINLUDTSMESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 44}},
#define   SCCPOM5MINSSCMESSAGERECEIVED  (127 % 256)
	{SCCPOM5MINSSCMESSAGERECEIVED, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 45}},
#define   SCCPOM5MINSSPMESSAGERECEIVED  (128 % 256)
	{SCCPOM5MINSSPMESSAGERECEIVED, ASN_GAUGE, RONLY, var_sccpOm5MinHistoryTable, 5, {1, 1, 5, 1, 46}},
#define   SCCPOM15MINNOTRANSLATORFORADDRESS  (133 % 256)
	{SCCPOM15MINNOTRANSLATORFORADDRESS, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 1}},
#define   SCCPOM15MINNORULEFORADDRESS  (134 % 256)
	{SCCPOM15MINNORULEFORADDRESS, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 2}},
#define   SCCPOM15MINPOINTCODENOTAVAILABLE  (135 % 256)
	{SCCPOM15MINPOINTCODENOTAVAILABLE, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 3}},
#define   SCCPOM15MINPOINTCODECONGESTED  (136 % 256)
	{SCCPOM15MINPOINTCODECONGESTED, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 4}},
#define   SCCPOM15MINSUBSYSTEMUNAVAILABLE  (137 % 256)
	{SCCPOM15MINSUBSYSTEMUNAVAILABLE, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 5}},
#define   SCCPOM15MINSUBSYSTEMCONGESTED  (138 % 256)
	{SCCPOM15MINSUBSYSTEMCONGESTED, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 6}},
#define   SCCPOM15MINUNEQUIPPEDSUBSYSTEM  (139 % 256)
	{SCCPOM15MINUNEQUIPPEDSUBSYSTEM, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 7}},
#define   SCCPOM15MINSYNTAXERRORDETECTED  (140 % 256)
	{SCCPOM15MINSYNTAXERRORDETECTED, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 8}},
#define   SCCPOM15MINROUTINGFAILURE  (141 % 256)
	{SCCPOM15MINROUTINGFAILURE, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 9}},
#define   SCCPOM15MINREASSEMBLYTIMEOUT  (142 % 256)
	{SCCPOM15MINREASSEMBLYTIMEOUT, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 10}},
#define   SCCPOM15MINSEGMENTOUTOFORDER  (143 % 256)
	{SCCPOM15MINSEGMENTOUTOFORDER, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 11}},
#define   SCCPOM15MINNOREASSEMBLYSPACE  (144 % 256)
	{SCCPOM15MINNOREASSEMBLYSPACE, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 12}},
#define   SCCPOM15MINHOPCOUNTERVIOLATION  (145 % 256)
	{SCCPOM15MINHOPCOUNTERVIOLATION, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 13}},
#define   SCCPOM15MINTOOLARGEFORSEGMENTATION  (146 % 256)
	{SCCPOM15MINTOOLARGEFORSEGMENTATION, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 14}},
#define   SCCPOM15MINRELEASEFAILURE  (147 % 256)
	{SCCPOM15MINRELEASEFAILURE, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 15}},
#define   SCCPOM15MININACTIVITYTIMEREXPIRY  (148 % 256)
	{SCCPOM15MININACTIVITYTIMEREXPIRY, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 16}},
#define   SCCPOM15MINPROVIDERINITIATEDRESET  (149 % 256)
	{SCCPOM15MINPROVIDERINITIATEDRESET, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 17}},
#define   SCCPOM15MINPROVIDERINITIATEDRELEASE  (150 % 256)
	{SCCPOM15MINPROVIDERINITIATEDRELEASE, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 18}},
#define   SCCPOM15MINNOSEGMENTATIONSUPPORT  (151 % 256)
	{SCCPOM15MINNOSEGMENTATIONSUPPORT, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 19}},
#define   SCCPOM15MINSEGMENTATIONFAILURE  (152 % 256)
	{SCCPOM15MINSEGMENTATIONFAILURE, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 20}},
#define   SCCPOM15MINREASSEMBLYFAILURE  (153 % 256)
	{SCCPOM15MINREASSEMBLYFAILURE, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 21}},
#define   SCCPOM15MINTOTALMESSAGESHANDLED  (154 % 256)
	{SCCPOM15MINTOTALMESSAGESHANDLED, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 22}},
#define   SCCPOM15MINTOTALMESSAGESLOCAL  (155 % 256)
	{SCCPOM15MINTOTALMESSAGESLOCAL, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 23}},
#define   SCCPOM15MINTOTALMESSAGESGTT  (156 % 256)
	{SCCPOM15MINTOTALMESSAGESGTT, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 24}},
#define   SCCPOM15MINUDTMESSAGESSENT  (157 % 256)
	{SCCPOM15MINUDTMESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 25}},
#define   SCCPOM15MINUDTSMESSAGESSENT  (158 % 256)
	{SCCPOM15MINUDTSMESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 26}},
#define   SCCPOM15MINUDTMESSAGESRECEIVED  (159 % 256)
	{SCCPOM15MINUDTMESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 27}},
#define   SCCPOM15MINUDTSMESSAGESRECEIVED  (160 % 256)
	{SCCPOM15MINUDTSMESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 28}},
#define   SCCPOM15MINCRMESSAGESSENT  (161 % 256)
	{SCCPOM15MINCRMESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 29}},
#define   SCCPOM15MINCREFMESSAGESSENT  (162 % 256)
	{SCCPOM15MINCREFMESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 30}},
#define   SCCPOM15MINCRMESSAGESRECEIVED  (163 % 256)
	{SCCPOM15MINCRMESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 31}},
#define   SCCPOM15MINCREFMESSAGESRECEIVED  (164 % 256)
	{SCCPOM15MINCREFMESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 32}},
#define   SCCPOM15MINRSRMESSAGESSENT  (165 % 256)
	{SCCPOM15MINRSRMESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 33}},
#define   SCCPOM15MINRSRMESSAGESRECEIVED  (166 % 256)
	{SCCPOM15MINRSRMESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 34}},
#define   SCCPOM15MINERRMESSAGESSENT  (167 % 256)
	{SCCPOM15MINERRMESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 35}},
#define   SCCPOM15MINERRMESSAGESRECEIVED  (168 % 256)
	{SCCPOM15MINERRMESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 36}},
#define   SCCPOM15MINXUDTMESSAGESSENT  (169 % 256)
	{SCCPOM15MINXUDTMESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 37}},
#define   SCCPOM15MINXUDTSMESSAGESSENT  (170 % 256)
	{SCCPOM15MINXUDTSMESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 38}},
#define   SCCPOM15MINXUDTMESSAGESRECEIVED  (171 % 256)
	{SCCPOM15MINXUDTMESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 39}},
#define   SCCPOM15MINXUDTSMESSAGESRECEIVED  (172 % 256)
	{SCCPOM15MINXUDTSMESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 40}},
#define   SCCPOM15MINLUDTMESSAGESSENT  (173 % 256)
	{SCCPOM15MINLUDTMESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 41}},
#define   SCCPOM15MINLUDTSMESSAGESSENT  (174 % 256)
	{SCCPOM15MINLUDTSMESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 42}},
#define   SCCPOM15MINLUDTMESSAGESRECEIVED  (175 % 256)
	{SCCPOM15MINLUDTMESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 43}},
#define   SCCPOM15MINLUDTSMESSAGESRECEIVED  (176 % 256)
	{SCCPOM15MINLUDTSMESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 44}},
#define   SCCPOM15MINSCCPUNAVAILABLEDURATION  (177 % 256)
	{SCCPOM15MINSCCPUNAVAILABLEDURATION, ASN_UNSIGNED, RONLY, var_sccpOm15MinHistoryTable, 5, {1, 1, 6, 1, 45}},
#define   SCCPOM5MINLXUDTMESSAGESORIGCLASS0  (182 % 256)
	{SCCPOM5MINLXUDTMESSAGESORIGCLASS0, ASN_GAUGE, RONLY, var_sccpOm5MinSsnHistoryTable, 5, {1, 1, 7, 1, 1}},
#define   SCCPOM5MINLXUDTMESSAGESORIGCLASS1  (183 % 256)
	{SCCPOM5MINLXUDTMESSAGESORIGCLASS1, ASN_GAUGE, RONLY, var_sccpOm5MinSsnHistoryTable, 5, {1, 1, 7, 1, 2}},
#define   SCCPOM5MINLXUDTMESSAGESTERMCLASS0  (184 % 256)
	{SCCPOM5MINLXUDTMESSAGESTERMCLASS0, ASN_GAUGE, RONLY, var_sccpOm5MinSsnHistoryTable, 5, {1, 1, 7, 1, 3}},
#define   SCCPOM5MINLXUDTMESSAGESTERMCLASS1  (185 % 256)
	{SCCPOM5MINLXUDTMESSAGESTERMCLASS1, ASN_GAUGE, RONLY, var_sccpOm5MinSsnHistoryTable, 5, {1, 1, 7, 1, 4}},
#define   SCCPOM5MINMESSAGESSENTTOBACKUP  (186 % 256)
	{SCCPOM5MINMESSAGESSENTTOBACKUP, ASN_GAUGE, RONLY, var_sccpOm5MinSsnHistoryTable, 5, {1, 1, 7, 1, 5}},
#define   SCCPOM5MINDT1MESSAGESRECEIVED  (187 % 256)
	{SCCPOM5MINDT1MESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOm5MinSsnHistoryTable, 5, {1, 1, 7, 1, 6}},
#define   SCCPOM5MINDT1MESSAGESSENT  (188 % 256)
	{SCCPOM5MINDT1MESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOm5MinSsnHistoryTable, 5, {1, 1, 7, 1, 7}},
#define   SCCPOM5MINDT2MESSAGESRECEIVED  (189 % 256)
	{SCCPOM5MINDT2MESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOm5MinSsnHistoryTable, 5, {1, 1, 7, 1, 8}},
#define   SCCPOM5MINDT2MESSAGESSENT  (190 % 256)
	{SCCPOM5MINDT2MESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOm5MinSsnHistoryTable, 5, {1, 1, 7, 1, 9}},
#define   SCCPOM5MINEDMESSAGESSENT  (191 % 256)
	{SCCPOM5MINEDMESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOm5MinSsnHistoryTable, 5, {1, 1, 7, 1, 10}},
#define   SCCPOM5MINEDMESSAGESRECEIVED  (192 % 256)
	{SCCPOM5MINEDMESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOm5MinSsnHistoryTable, 5, {1, 1, 7, 1, 11}},
#define   SCCPOM15MINLXUDTMESSAGESORIGCLASS0  (197 % 256)
	{SCCPOM15MINLXUDTMESSAGESORIGCLASS0, ASN_GAUGE, RONLY, var_sccpOm15MinSsnHistoryTable, 5, {1, 1, 8, 1, 1}},
#define   SCCPOM15MINLXUDTMESSAGESORIGCLASS1  (198 % 256)
	{SCCPOM15MINLXUDTMESSAGESORIGCLASS1, ASN_GAUGE, RONLY, var_sccpOm15MinSsnHistoryTable, 5, {1, 1, 8, 1, 2}},
#define   SCCPOM15MINLXUDTMESSAGESTERMCLASS0  (199 % 256)
	{SCCPOM15MINLXUDTMESSAGESTERMCLASS0, ASN_GAUGE, RONLY, var_sccpOm15MinSsnHistoryTable, 5, {1, 1, 8, 1, 3}},
#define   SCCPOM15MINLXUDTMESSAGESTERMCLASS1  (200 % 256)
	{SCCPOM15MINLXUDTMESSAGESTERMCLASS1, ASN_GAUGE, RONLY, var_sccpOm15MinSsnHistoryTable, 5, {1, 1, 8, 1, 4}},
#define   SCCPOM15MINMESSAGESSENTTOBACKUP  (201 % 256)
	{SCCPOM15MINMESSAGESSENTTOBACKUP, ASN_GAUGE, RONLY, var_sccpOm15MinSsnHistoryTable, 5, {1, 1, 8, 1, 5}},
#define   SCCPOM15MINDT1MESSAGESRECEIVED  (202 % 256)
	{SCCPOM15MINDT1MESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOm15MinSsnHistoryTable, 5, {1, 1, 8, 1, 6}},
#define   SCCPOM15MINDT1MESSAGESSENT  (203 % 256)
	{SCCPOM15MINDT1MESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOm15MinSsnHistoryTable, 5, {1, 1, 8, 1, 7}},
#define   SCCPOM15MINDT2MESSAGESRECEIVED  (204 % 256)
	{SCCPOM15MINDT2MESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOm15MinSsnHistoryTable, 5, {1, 1, 8, 1, 8}},
#define   SCCPOM15MINDT2MESSAGESSENT  (205 % 256)
	{SCCPOM15MINDT2MESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOm15MinSsnHistoryTable, 5, {1, 1, 8, 1, 9}},
#define   SCCPOM15MINEDMESSAGESSENT  (206 % 256)
	{SCCPOM15MINEDMESSAGESSENT, ASN_GAUGE, RONLY, var_sccpOm15MinSsnHistoryTable, 5, {1, 1, 8, 1, 10}},
#define   SCCPOM15MINEDMESSAGESRECEIVED  (207 % 256)
	{SCCPOM15MINEDMESSAGESRECEIVED, ASN_GAUGE, RONLY, var_sccpOm15MinSsnHistoryTable, 5, {1, 1, 8, 1, 11}},
#define   SCCPOM1STANDINTERVALACTIVATE  (208 % 256)
	{SCCPOM1STANDINTERVALACTIVATE, ASN_OBJECT_ID, RWRITE, var_sccpOmMIB, 3, {1, 2, 1}},
#define   SCCPOM1STANDINTERVALDEACTIVATE  (209 % 256)
	{SCCPOM1STANDINTERVALDEACTIVATE, ASN_OBJECT_ID, RWRITE, var_sccpOmMIB, 3, {1, 2, 2}},
#define   SCCPOM5MINACTIVATE    (210 % 256)
	{SCCPOM5MINACTIVATE, ASN_OBJECT_ID, RWRITE, var_sccpOmMIB, 3, {1, 2, 3}},
#define   SCCPOM5MINDEAACTIVATE  (211 % 256)
	{SCCPOM5MINDEAACTIVATE, ASN_OBJECT_ID, RWRITE, var_sccpOmMIB, 3, {1, 2, 4}},
#define   SCCPOM15MINACTIVATE   (212 % 256)
	{SCCPOM15MINACTIVATE, ASN_OBJECT_ID, RWRITE, var_sccpOmMIB, 3, {1, 2, 5}},
#define   SCCPOM15MINDEAACTIVATE  (213 % 256)
	{SCCPOM15MINDEAACTIVATE, ASN_OBJECT_ID, RWRITE, var_sccpOmMIB, 3, {1, 2, 6}},
#define   SCCPOMDISCONTINUITYTIME  (215 % 256)
	{SCCPOMDISCONTINUITYTIME, ASN_TIMETICKS, RONLY, var_sccpOmMIB, 3, {1, 2, 8}},
#define   SCCPOMTIMESTAMP       (216 % 256)
	{SCCPOMTIMESTAMP, ASN_TIMETICKS, RONLY, var_sccpOmMIB, 3, {1, 2, 9}},
#define   SCCPOM5MINMAXINTERVALS  (217 % 256)
	{SCCPOM5MINMAXINTERVALS, ASN_UNSIGNED, RWRITE, var_sccpOmMIB, 3, {1, 2, 10}},
#define   SCCPOM15MINMAXINTERVALS  (218 % 256)
	{SCCPOM15MINMAXINTERVALS, ASN_UNSIGNED, RWRITE, var_sccpOmMIB, 3, {1, 2, 11}},
#define   SCCPOMPDEFAULT        (219 % 256)
	{SCCPOMPDEFAULT, ASN_INTEGER, RWRITE, var_sccpOmMIB, 3, {1, 2, 12}},
#define   SCCPOMNROFSUBLEVELSDEFAULT  (220 % 256)
	{SCCPOMNROFSUBLEVELSDEFAULT, ASN_INTEGER, RWRITE, var_sccpOmMIB, 3, {1, 2, 13}},
#define   SCCPOMNROFRESTRICTIONLEVELSDEFAULT  (221 % 256)
	{SCCPOMNROFRESTRICTIONLEVELSDEFAULT, ASN_INTEGER, RWRITE, var_sccpOmMIB, 3, {1, 2, 14}},

};

/*    (L = length of the oidsuffix) */
struct sccpOmMIB_data *sccpOmMIBStorage = NULL;

/* global storage of our data, saved in and configured by header_complex() */
struct header_complex_index *sccpOmErrorsTableStorage = NULL;
struct header_complex_index *sccpOmMessageTableStorage = NULL;
struct header_complex_index *sccpOmAccessibilityTableStorage = NULL;
struct header_complex_index *sccpOmUtilizationTableStorage = NULL;
struct header_complex_index *sccpOm5MinHistoryTableStorage = NULL;
struct header_complex_index *sccpOm15MinHistoryTableStorage = NULL;
struct header_complex_index *sccpOm5MinSsnHistoryTableStorage = NULL;
struct header_complex_index *sccpOm15MinSsnHistoryTableStorage = NULL;

/*
 * init_sccpOmMIB():
 *   Initialization routine.  This is called when the agent starts up.
 *   At a minimum, registration of your variables should take place here.
 */
void
init_sccpOmMIB(void)
{
	DEBUGMSGTL(("sccpOmMIB", "initializing...  "));
	/* register ourselves with the agent to handle our mib tree */
	REGISTER_MIB("sccpOmMIB", sccpOmMIB_variables, variable7, sccpOmMIB_variables_oid);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_SHUTDOWN, term_sccpOmMIB, NULL);
	/* register our config handler(s) to deal with registrations */
	snmpd_register_config_handler("sccpOmMIB", parse_sccpOmMIB, NULL, "HELP STRING");
	snmpd_register_config_handler("sccpOmErrorsTable", parse_sccpOmErrorsTable, NULL, "HELP STRING");
	snmpd_register_config_handler("sccpOmMessageTable", parse_sccpOmMessageTable, NULL, "HELP STRING");
	snmpd_register_config_handler("sccpOmAccessibilityTable", parse_sccpOmAccessibilityTable, NULL, "HELP STRING");
	snmpd_register_config_handler("sccpOmUtilizationTable", parse_sccpOmUtilizationTable, NULL, "HELP STRING");
	snmpd_register_config_handler("sccpOm5MinHistoryTable", parse_sccpOm5MinHistoryTable, NULL, "HELP STRING");
	snmpd_register_config_handler("sccpOm15MinHistoryTable", parse_sccpOm15MinHistoryTable, NULL, "HELP STRING");
	snmpd_register_config_handler("sccpOm5MinSsnHistoryTable", parse_sccpOm5MinSsnHistoryTable, NULL, "HELP STRING");
	snmpd_register_config_handler("sccpOm15MinSsnHistoryTable", parse_sccpOm15MinSsnHistoryTable, NULL, "HELP STRING");

	/* we need to be called back later to store our data */
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_sccpOmMIB, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_sccpOmErrorsTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_sccpOmMessageTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_sccpOmAccessibilityTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_sccpOmUtilizationTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_sccpOm5MinHistoryTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_sccpOm15MinHistoryTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_sccpOm5MinSsnHistoryTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_sccpOm15MinSsnHistoryTable, NULL);

	/* place any other initialization junk you need here */
	DEBUGMSGTL(("sccpOmMIB", "done.\n"));
}

/*
 * deinit_sccpOmMIB():
 *   Deinitialization routine.  This is called before the agent is unloaded.
 *   At a minimum, deregistration of your variables should take place here.
 */
void
deinit_sccpOmMIB(void)
{
	DEBUGMSGTL(("sccpOmMIB", "deinitializating...  "));
	unregister_mib(sccpOmMIB_variables_oid, sizeof(sccpOmMIB_variables_oid) / sizeof(oid));
	snmpd_unregister_config_handler("sccpOmMIB");
	snmpd_unregister_config_handler("sccpOmErrorsTable");
	snmpd_unregister_config_handler("sccpOmMessageTable");
	snmpd_unregister_config_handler("sccpOmAccessibilityTable");
	snmpd_unregister_config_handler("sccpOmUtilizationTable");
	snmpd_unregister_config_handler("sccpOm5MinHistoryTable");
	snmpd_unregister_config_handler("sccpOm15MinHistoryTable");
	snmpd_unregister_config_handler("sccpOm5MinSsnHistoryTable");
	snmpd_unregister_config_handler("sccpOm15MinSsnHistoryTable");

	/* place any other de-initialization junk you need here */
	DEBUGMSGTL(("sccpOmMIB", "done.\n"));
}

int
term_sccpOmMIB(int majorID, int minorID, void *serverarg, void *clientarg)
{
	deinit_sccpOmMIB();
	return 0;
}

/*
 * sccpOmMIB_add(): adds a structure node for scalars
 */
int
sccpOmMIB_add(struct sccpOmMIB_data *thedata)
{
	DEBUGMSGTL(("sccpOmMIB", "adding data...  "));
	sccpOmMIBStorage = thedata;
	DEBUGMSGTL(("sccpOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/*
 * parse_sccpOmMIB():
 *   parses .conf file entries needed to configure the mib.
 */
void
parse_sccpOmMIB(const char *token, char *line)
{
	size_t tmpsize;
	struct sccpOmMIB_data *StorageTmp = SNMP_MALLOC_STRUCT(sccpOmMIB_data);

	DEBUGMSGTL(("sccpOmMIB", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->sccpOm1stAndIntervalActivate, &tmpsize);
	StorageTmp->sccpOm1stAndIntervalActivateLen = tmpsize;
	if (StorageTmp->sccpOm1stAndIntervalActivate == NULL) {
		config_perror("invalid specification for sccpOm1stAndIntervalActivate");
		return;
	}

	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->sccpOm1stAndIntervalDeactivate, &tmpsize);
	StorageTmp->sccpOm1stAndIntervalDeactivateLen = tmpsize;
	if (StorageTmp->sccpOm1stAndIntervalDeactivate == NULL) {
		config_perror("invalid specification for sccpOm1stAndIntervalDeactivate");
		return;
	}

	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->sccpOm5MinActivate, &tmpsize);
	StorageTmp->sccpOm5MinActivateLen = tmpsize;
	if (StorageTmp->sccpOm5MinActivate == NULL) {
		config_perror("invalid specification for sccpOm5MinActivate");
		return;
	}

	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->sccpOm5MinDeaActivate, &tmpsize);
	StorageTmp->sccpOm5MinDeaActivateLen = tmpsize;
	if (StorageTmp->sccpOm5MinDeaActivate == NULL) {
		config_perror("invalid specification for sccpOm5MinDeaActivate");
		return;
	}

	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->sccpOm15MinActivate, &tmpsize);
	StorageTmp->sccpOm15MinActivateLen = tmpsize;
	if (StorageTmp->sccpOm15MinActivate == NULL) {
		config_perror("invalid specification for sccpOm15MinActivate");
		return;
	}

	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->sccpOm15MinDeaActivate, &tmpsize);
	StorageTmp->sccpOm15MinDeaActivateLen = tmpsize;
	if (StorageTmp->sccpOm15MinDeaActivate == NULL) {
		config_perror("invalid specification for sccpOm15MinDeaActivate");
		return;
	}

	line = read_config_read_data(ASN_TIMETICKS, line, &StorageTmp->sccpOmDiscontinuityTime, &tmpsize);
	line = read_config_read_data(ASN_TIMETICKS, line, &StorageTmp->sccpOmTimeStamp, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->sccpOm5MinMaxIntervals, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->sccpOm15MinMaxIntervals, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->sccpOmpDefault, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->sccpOmNrOfSubLevelsDefault, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->sccpOmNrOfRestrictionLevelsDefault, &tmpsize);

	sccpOmMIB_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("sccpOmMIB", "done.\n"));
}

/*
 * store_sccpOmMIB():
 *   stores .conf file entries needed to configure the mib.
 */
int
store_sccpOmMIB(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct sccpOmMIB_data *StorageTmp;

	DEBUGMSGTL(("sccpOmMIB", "storing data...  "));
	refresh_sccpOmMIB();
	if ((StorageTmp = sccpOmMIBStorage) == NULL) {
		DEBUGMSGTL(("sccpOmMIB", "error.\n"));
		return SNMPERR_GENERR;
	}
	(void) tmpsize;
	/* XXX: if (StorageTmp->sccpOmMIBStorageType == ST_NONVOLATILE) { */
	memset(line, 0, sizeof(line));
	strcat(line, "sccpOmMIB ");
	cptr = line + strlen(line);
	tmpsize = StorageTmp->sccpOm1stAndIntervalActivateLen;
	cptr = read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->sccpOm1stAndIntervalActivate, &tmpsize);
	tmpsize = StorageTmp->sccpOm1stAndIntervalDeactivateLen;
	cptr = read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->sccpOm1stAndIntervalDeactivate, &tmpsize);
	tmpsize = StorageTmp->sccpOm5MinActivateLen;
	cptr = read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->sccpOm5MinActivate, &tmpsize);
	tmpsize = StorageTmp->sccpOm5MinDeaActivateLen;
	cptr = read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->sccpOm5MinDeaActivate, &tmpsize);
	tmpsize = StorageTmp->sccpOm15MinActivateLen;
	cptr = read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->sccpOm15MinActivate, &tmpsize);
	tmpsize = StorageTmp->sccpOm15MinDeaActivateLen;
	cptr = read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->sccpOm15MinDeaActivate, &tmpsize);
	cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->sccpOmInterval, &tmpsize);
	cptr = read_config_store_data(ASN_TIMETICKS, cptr, &StorageTmp->sccpOmDiscontinuityTime, &tmpsize);
	cptr = read_config_store_data(ASN_TIMETICKS, cptr, &StorageTmp->sccpOmTimeStamp, &tmpsize);
	cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->sccpOm5MinMaxIntervals, &tmpsize);
	cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->sccpOm15MinMaxIntervals, &tmpsize);
	cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->sccpOmpDefault, &tmpsize);
	cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->sccpOmNrOfSubLevelsDefault, &tmpsize);
	cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->sccpOmNrOfRestrictionLevelsDefault, &tmpsize);

	snmpd_store_config(line);
	/* } */
	DEBUGMSGTL(("sccpOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * refresh_sccpOmMIB: refresh sccpOmMIB scalars
 *
 * Normally, the values retrieved from the operating system are cached.
 * When the agent receives a SIGPOLL from an open STREAMS configuration
 * or administrative driver Stream, the STREAMS subsystem indicates to
 * the agent that the cache has been invalidated and that it should
 * reread scalars and tables from the STREAMS susbystem.  This function
 * is used when the agent starts for the first time, or after a SIGPOLL
 * has been received (and a scalar has been requested).
 */
void
refresh_sccpOmMIB(void)
{
	if (sccpOmMIBStorage == NULL) {
		struct sccpOmMIB_data *StorageNew;

		if ((StorageNew = SNMP_MALLOC_STRUCT(sccpOmMIB_data)) == NULL)
			return;
		/* Update scalar defaults as required here... */
		StorageNew->sccpOm5MinMaxIntervals = 96;
		StorageNew->sccpOm15MinMaxIntervals = 96;
		StorageNew->sccpOmpDefault = 8;
		StorageNew->sccpOmNrOfSubLevelsDefault = 4;
		StorageNew->sccpOmNrOfRestrictionLevelsDefault = 8;

		sccpOmMIBStorage = StorageNew;
		sccpOmMIB_refresh = 1;
	}
	if (sccpOmMIB_refresh == 0)
		return;
	sccpOmMIB_refresh = 0;
	/* Update scalars as required here... */
}

/*
 * var_sccpOmMIB():
 *   This function is called every time the agent gets a request for
 *   a scalar variable that might be found within your mib section
 *   registered above.  It is up to you to do the right thing and
 *   return the correct value.
 *     You should also correct the value of "var_len" if necessary.
 *
 *   Please see the documentation for more information about writing
 *   module extensions, and check out the examples in the examples
 *   and mibII directories.
 */
uint8_t *
var_sccpOmMIB(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct sccpOmMIB_data *StorageTmp;

	if (header_generic(vp, name, length, exact, var_len, write_method)
	    == MATCH_FAILED)
		return NULL;
	/* Refresh the MIB values if required. */
	refresh_sccpOmMIB();
	if ((StorageTmp = sccpOmMIBStorage) == NULL)
		return NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {

	case SCCPOM1STANDINTERVALACTIVATE:
		*write_method = write_sccpOm1stAndIntervalActivate;
		*var_len = StorageTmp->sccpOm1stAndIntervalActivateLen;
		return (uint8_t *) StorageTmp->sccpOm1stAndIntervalActivate;
	case SCCPOM1STANDINTERVALDEACTIVATE:
		*write_method = write_sccpOm1stAndIntervalDeactivate;
		*var_len = StorageTmp->sccpOm1stAndIntervalDeactivateLen;
		return (uint8_t *) StorageTmp->sccpOm1stAndIntervalDeactivate;
	case SCCPOM5MINACTIVATE:
		*write_method = write_sccpOm5MinActivate;
		*var_len = StorageTmp->sccpOm5MinActivateLen;
		return (uint8_t *) StorageTmp->sccpOm5MinActivate;
	case SCCPOM5MINDEAACTIVATE:
		*write_method = write_sccpOm5MinDeaActivate;
		*var_len = StorageTmp->sccpOm5MinDeaActivateLen;
		return (uint8_t *) StorageTmp->sccpOm5MinDeaActivate;
	case SCCPOM15MINACTIVATE:
		*write_method = write_sccpOm15MinActivate;
		*var_len = StorageTmp->sccpOm15MinActivateLen;
		return (uint8_t *) StorageTmp->sccpOm15MinActivate;
	case SCCPOM15MINDEAACTIVATE:
		*write_method = write_sccpOm15MinDeaActivate;
		*var_len = StorageTmp->sccpOm15MinDeaActivateLen;
		return (uint8_t *) StorageTmp->sccpOm15MinDeaActivate;
	case SCCPOMDISCONTINUITYTIME:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmDiscontinuityTime);
		return (uint8_t *) &StorageTmp->sccpOmDiscontinuityTime;
	case SCCPOMTIMESTAMP:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmTimeStamp);
		return (uint8_t *) &StorageTmp->sccpOmTimeStamp;
	case SCCPOM5MINMAXINTERVALS:
		*write_method = write_sccpOm5MinMaxIntervals;
		*var_len = sizeof(StorageTmp->sccpOm5MinMaxIntervals);
		return (uint8_t *) &StorageTmp->sccpOm5MinMaxIntervals;
	case SCCPOM15MINMAXINTERVALS:
		*write_method = write_sccpOm15MinMaxIntervals;
		*var_len = sizeof(StorageTmp->sccpOm15MinMaxIntervals);
		return (uint8_t *) &StorageTmp->sccpOm15MinMaxIntervals;
	case SCCPOMPDEFAULT:
		*write_method = write_sccpOmpDefault;
		*var_len = sizeof(StorageTmp->sccpOmpDefault);
		return (uint8_t *) &StorageTmp->sccpOmpDefault;
	case SCCPOMNROFSUBLEVELSDEFAULT:
		*write_method = write_sccpOmNrOfSubLevelsDefault;
		*var_len = sizeof(StorageTmp->sccpOmNrOfSubLevelsDefault);
		return (uint8_t *) &StorageTmp->sccpOmNrOfSubLevelsDefault;
	case SCCPOMNROFRESTRICTIONLEVELSDEFAULT:
		*write_method = write_sccpOmNrOfRestrictionLevelsDefault;
		*var_len = sizeof(StorageTmp->sccpOmNrOfRestrictionLevelsDefault);
		return (uint8_t *) &StorageTmp->sccpOmNrOfRestrictionLevelsDefault;

	default:
		ERROR_MSG("");
	}
	return NULL;
}

/*
 * sccpOmErrorsTable_add(): adds a structure node to our data set
 */
int
sccpOmErrorsTable_add(struct sccpOmErrorsTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("sccpOmErrorsTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index
	   the data */
	/* sccpNetworkEntityId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_UINTEGER, (uint8_t *) &thedata->sccpNetworkEntityId, sizeof(thedata->sccpNetworkEntityId));

	header_complex_add_data(&sccpOmErrorsTableStorage, vars, thedata);
	DEBUGMSGTL(("sccpOmErrorsTable", "registered an entry\n"));
	DEBUGMSGTL(("sccpOmErrorsTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/*
 * parse_sccpOmErrorsTable():
 *   parses .conf file entries needed to configure the mib.
 */
void
parse_sccpOmErrorsTable(const char *token, char *line)
{
	size_t tmpsize;
	struct sccpOmErrorsTable_data *StorageTmp = SNMP_MALLOC_STRUCT(sccpOmErrorsTable_data);

	DEBUGMSGTL(("sccpOmErrorsTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	line = read_config_read_data(ASN_UINTEGER, line, &StorageTmp->sccpNetworkEntityId, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOmNoTranslatorForAddress, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOmNoRuleForAddress, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOmPointCodeNotAvailable, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOmPointCodeCongested, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOmSubsystemUnavailable, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOmSubsystemCongested, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOmUnequippedSubsystem, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOmSyntaxErrorDetected, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOmRoutingFailure, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOmReassemblyTimeOut, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOmSegmentOutOfOrder, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOmNoReassemblySpace, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOmHopCounterViolation, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOmTooLargeForSegmentation, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOmReleaseFailure, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOmInactivityTimerExpiry, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOmProviderInitiatedReset, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOmProviderInitiatedRelease, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOmNoSegmentationSupport, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOmSegmentationFailure, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOmReassemblyFailure, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->sccpOmErrors5MinValidIntervals, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->sccpOmErrors15MinValidIntervals, &tmpsize);

	sccpOmErrorsTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("sccpOmErrorsTable", "done.\n"));
}

/*
 * store_sccpOmErrorsTable():
 *   stores .conf file entries needed to configure the mib.
 */
int
store_sccpOmErrorsTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct sccpOmErrorsTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("sccpOmErrorsTable", "storing data...  "));
	refresh_sccpOmErrorsTable();
	(void) tmpsize;
	for (hcindex = sccpOmErrorsTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct sccpOmErrorsTable_data *) hcindex->data;
/*   XXX:  if (StorageTmp->sccpOmErrorsTableStorageType == ST_NONVOLATILE) { */
		memset(line, 0, sizeof(line));
		strcat(line, "sccpOmErrorsTable ");
		cptr = line + strlen(line);
		cptr = read_config_store_data(ASN_UINTEGER, cptr, &StorageTmp->sccpNetworkEntityId, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOmNoTranslatorForAddress, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOmNoRuleForAddress, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOmPointCodeNotAvailable, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOmPointCodeCongested, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOmSubsystemUnavailable, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOmSubsystemCongested, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOmUnequippedSubsystem, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOmSyntaxErrorDetected, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOmRoutingFailure, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOmReassemblyTimeOut, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOmSegmentOutOfOrder, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOmNoReassemblySpace, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOmHopCounterViolation, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOmTooLargeForSegmentation, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOmReleaseFailure, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOmInactivityTimerExpiry, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOmProviderInitiatedReset, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOmProviderInitiatedRelease, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOmNoSegmentationSupport, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOmSegmentationFailure, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOmReassemblyFailure, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->sccpOmErrors5MinValidIntervals, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->sccpOmErrors15MinValidIntervals, &tmpsize);

		snmpd_store_config(line);
/*   } */
	}
	DEBUGMSGTL(("sccpOmErrorsTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/*
 * sccpOmMessageTable_add(): adds a structure node to our data set
 */
int
sccpOmMessageTable_add(struct sccpOmMessageTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("sccpOmMessageTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index
	   the data */
	/* sccpNetworkEntityId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_UINTEGER, (uint8_t *) &thedata->sccpNetworkEntityId, sizeof(thedata->sccpNetworkEntityId));

	header_complex_add_data(&sccpOmMessageTableStorage, vars, thedata);
	DEBUGMSGTL(("sccpOmMessageTable", "registered an entry\n"));
	DEBUGMSGTL(("sccpOmMessageTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/*
 * parse_sccpOmMessageTable():
 *   parses .conf file entries needed to configure the mib.
 */
void
parse_sccpOmMessageTable(const char *token, char *line)
{
	size_t tmpsize;
	struct sccpOmMessageTable_data *StorageTmp = SNMP_MALLOC_STRUCT(sccpOmMessageTable_data);

	DEBUGMSGTL(("sccpOmMessageTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	line = read_config_read_data(ASN_UINTEGER, line, &StorageTmp->sccpNetworkEntityId, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOmTotalMessagesHandled, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOmTotalMessagesLocal, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOmTotalMessagesGTT, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOmUDTMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOmUDTSMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOmUDTMessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOmUDTSMessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOmCRMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOmCREFMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOmCRMessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOmCREFMessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOmRSRMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOmRSRMessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOmERRMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOmERRMessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOmXUDTMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOmXUDTSMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOmXUDTMessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOmXUDTSMessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOmLUDTMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOmLUDTSMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOmLUDTMessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOmLUDTSMessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->sccpOmMessage5MinValidIntervals, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->sccpOmMessage15MinValidIntervals, &tmpsize);

	sccpOmMessageTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("sccpOmMessageTable", "done.\n"));
}

/*
 * store_sccpOmMessageTable():
 *   stores .conf file entries needed to configure the mib.
 */
int
store_sccpOmMessageTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct sccpOmMessageTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("sccpOmMessageTable", "storing data...  "));
	refresh_sccpOmMessageTable();
	(void) tmpsize;
	for (hcindex = sccpOmMessageTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct sccpOmMessageTable_data *) hcindex->data;
/*   XXX:  if (StorageTmp->sccpOmMessageTableStorageType == ST_NONVOLATILE) { */
		memset(line, 0, sizeof(line));
		strcat(line, "sccpOmMessageTable ");
		cptr = line + strlen(line);
		cptr = read_config_store_data(ASN_UINTEGER, cptr, &StorageTmp->sccpNetworkEntityId, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOmTotalMessagesHandled, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOmTotalMessagesLocal, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOmTotalMessagesGTT, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOmUDTMessagesSent, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOmUDTSMessagesSent, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOmUDTMessagesReceived, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOmUDTSMessagesReceived, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOmCRMessagesSent, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOmCREFMessagesSent, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOmCRMessagesReceived, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOmCREFMessagesReceived, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOmRSRMessagesSent, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOmRSRMessagesReceived, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOmERRMessagesSent, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOmERRMessagesReceived, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOmXUDTMessagesSent, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOmXUDTSMessagesSent, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOmXUDTMessagesReceived, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOmXUDTSMessagesReceived, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOmLUDTMessagesSent, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOmLUDTSMessagesSent, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOmLUDTMessagesReceived, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOmLUDTSMessagesReceived, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->sccpOmMessage5MinValidIntervals, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->sccpOmMessage15MinValidIntervals, &tmpsize);

		snmpd_store_config(line);
/*   } */
	}
	DEBUGMSGTL(("sccpOmMessageTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/*
 * sccpOmAccessibilityTable_add(): adds a structure node to our data set
 */
int
sccpOmAccessibilityTable_add(struct sccpOmAccessibilityTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("sccpOmAccessibilityTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index
	   the data */
	/* sccpNetworkEntityId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_UINTEGER, (uint8_t *) &thedata->sccpNetworkEntityId, sizeof(thedata->sccpNetworkEntityId));

	header_complex_add_data(&sccpOmAccessibilityTableStorage, vars, thedata);
	DEBUGMSGTL(("sccpOmAccessibilityTable", "registered an entry\n"));
	DEBUGMSGTL(("sccpOmAccessibilityTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/*
 * parse_sccpOmAccessibilityTable():
 *   parses .conf file entries needed to configure the mib.
 */
void
parse_sccpOmAccessibilityTable(const char *token, char *line)
{
	size_t tmpsize;
	struct sccpOmAccessibilityTable_data *StorageTmp = SNMP_MALLOC_STRUCT(sccpOmAccessibilityTable_data);

	DEBUGMSGTL(("sccpOmAccessibilityTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	line = read_config_read_data(ASN_UINTEGER, line, &StorageTmp->sccpNetworkEntityId, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOmSSCMessageReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOmSSPMessageReceived, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->sccpOmSccpUnavailableDuration, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->sccpOmAccessibility5MinValidIntervals, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->sccpOmAccessibility15MinValidIntervals, &tmpsize);

	sccpOmAccessibilityTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("sccpOmAccessibilityTable", "done.\n"));
}

/*
 * store_sccpOmAccessibilityTable():
 *   stores .conf file entries needed to configure the mib.
 */
int
store_sccpOmAccessibilityTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct sccpOmAccessibilityTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("sccpOmAccessibilityTable", "storing data...  "));
	refresh_sccpOmAccessibilityTable();
	(void) tmpsize;
	for (hcindex = sccpOmAccessibilityTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct sccpOmAccessibilityTable_data *) hcindex->data;
/*   XXX:  if (StorageTmp->sccpOmAccessibilityTableStorageType == ST_NONVOLATILE) { */
		memset(line, 0, sizeof(line));
		strcat(line, "sccpOmAccessibilityTable ");
		cptr = line + strlen(line);
		cptr = read_config_store_data(ASN_UINTEGER, cptr, &StorageTmp->sccpNetworkEntityId, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOmSSCMessageReceived, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOmSSPMessageReceived, &tmpsize);
		cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->sccpOmSccpUnavailableDuration, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->sccpOmAccessibility5MinValidIntervals, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->sccpOmAccessibility15MinValidIntervals, &tmpsize);

		snmpd_store_config(line);
/*   } */
	}
	DEBUGMSGTL(("sccpOmAccessibilityTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/*
 * sccpOmUtilizationTable_add(): adds a structure node to our data set
 */
int
sccpOmUtilizationTable_add(struct sccpOmUtilizationTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("sccpOmUtilizationTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index
	   the data */
	/* sccpNetworkEntityId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_UINTEGER, (uint8_t *) &thedata->sccpNetworkEntityId, sizeof(thedata->sccpNetworkEntityId));

	header_complex_add_data(&sccpOmUtilizationTableStorage, vars, thedata);
	DEBUGMSGTL(("sccpOmUtilizationTable", "registered an entry\n"));
	DEBUGMSGTL(("sccpOmUtilizationTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/*
 * parse_sccpOmUtilizationTable():
 *   parses .conf file entries needed to configure the mib.
 */
void
parse_sccpOmUtilizationTable(const char *token, char *line)
{
	size_t tmpsize;
	struct sccpOmUtilizationTable_data *StorageTmp = SNMP_MALLOC_STRUCT(sccpOmUtilizationTable_data);

	DEBUGMSGTL(("sccpOmUtilizationTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	line = read_config_read_data(ASN_UINTEGER, line, &StorageTmp->sccpNetworkEntityId, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOmLXUDTMessagesOrigClass0, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOmLXUDTMessagesOrigClass1, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOmLXUDTMessagesTermClass0, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOmLXUDTMessagesTermClass1, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOmMessagesSentToBackup, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOmDT1MessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOmDT1MessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOmDT2MessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOmDT2MessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOmEDMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOmEDMessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->sccpOmUtilization5MinValidIntervals, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->sccpOmUtilization15MinValidIntervals, &tmpsize);

	sccpOmUtilizationTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("sccpOmUtilizationTable", "done.\n"));
}

/*
 * store_sccpOmUtilizationTable():
 *   stores .conf file entries needed to configure the mib.
 */
int
store_sccpOmUtilizationTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct sccpOmUtilizationTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("sccpOmUtilizationTable", "storing data...  "));
	refresh_sccpOmUtilizationTable();
	(void) tmpsize;
	for (hcindex = sccpOmUtilizationTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct sccpOmUtilizationTable_data *) hcindex->data;
/*   XXX:  if (StorageTmp->sccpOmUtilizationTableStorageType == ST_NONVOLATILE) { */
		memset(line, 0, sizeof(line));
		strcat(line, "sccpOmUtilizationTable ");
		cptr = line + strlen(line);
		cptr = read_config_store_data(ASN_UINTEGER, cptr, &StorageTmp->sccpNetworkEntityId, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOmLXUDTMessagesOrigClass0, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOmLXUDTMessagesOrigClass1, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOmLXUDTMessagesTermClass0, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOmLXUDTMessagesTermClass1, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOmMessagesSentToBackup, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOmDT1MessagesReceived, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOmDT1MessagesSent, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOmDT2MessagesReceived, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOmDT2MessagesSent, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOmEDMessagesSent, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOmEDMessagesReceived, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->sccpOmUtilization5MinValidIntervals, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->sccpOmUtilization15MinValidIntervals, &tmpsize);

		snmpd_store_config(line);
/*   } */
	}
	DEBUGMSGTL(("sccpOmUtilizationTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/*
 * sccpOm5MinHistoryTable_add(): adds a structure node to our data set
 */
int
sccpOm5MinHistoryTable_add(struct sccpOm5MinHistoryTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("sccpOm5MinHistoryTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index
	   the data */
	/* sccpNetworkEntityId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_UINTEGER, (uint8_t *) &thedata->sccpNetworkEntityId, sizeof(thedata->sccpNetworkEntityId));
	/* sccpOmInterval */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (uint8_t *) &thedata->sccpOmInterval, sizeof(thedata->sccpOmInterval));

	header_complex_add_data(&sccpOm5MinHistoryTableStorage, vars, thedata);
	DEBUGMSGTL(("sccpOm5MinHistoryTable", "registered an entry\n"));
	DEBUGMSGTL(("sccpOm5MinHistoryTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/*
 * parse_sccpOm5MinHistoryTable():
 *   parses .conf file entries needed to configure the mib.
 */
void
parse_sccpOm5MinHistoryTable(const char *token, char *line)
{
	size_t tmpsize;
	struct sccpOm5MinHistoryTable_data *StorageTmp = SNMP_MALLOC_STRUCT(sccpOm5MinHistoryTable_data);

	DEBUGMSGTL(("sccpOm5MinHistoryTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	line = read_config_read_data(ASN_UINTEGER, line, &StorageTmp->sccpNetworkEntityId, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->sccpOmInterval, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinNoTranslatorForAddress, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinNoRuleForAddress, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinPointCodeNotAvailable, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinPointCodeCongested, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinSubsystemUnavailable, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinSubsystemCongested, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinUnequippedSubsystem, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinSyntaxErrorDetected, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinRoutingFailure, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinReassemblyTimeOut, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinSegmentOutOfOrder, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinNoReassemblySpace, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinHopCounterViolation, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinTooLargeForSegmentation, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinReleaseFailure, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinInactivityTimerExpiry, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinProviderInitiatedReset, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinProviderInitiatedRelease, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinNoSegmentationSupport, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinSegmentationFailure, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinReassemblyFailure, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinTotalMessagesHandled, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinTotalMessagesLocal, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinTotalMessagesGTT, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinUDTMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinUDTSMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinUDTMessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinUDTSMessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinCRMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinCREFMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinCRMessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinCREFMessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinRSRMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinRSRMessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinERRMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinERRMessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinXUDTMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinXUDTSMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinXUDTMessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinXUDTSMessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinLUDTMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinLUDTSMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinLUDTMessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinLUDTSMessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinSSCMessageReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinSSPMessageReceived, &tmpsize);

	sccpOm5MinHistoryTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("sccpOm5MinHistoryTable", "done.\n"));
}

/*
 * store_sccpOm5MinHistoryTable():
 *   stores .conf file entries needed to configure the mib.
 */
int
store_sccpOm5MinHistoryTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct sccpOm5MinHistoryTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("sccpOm5MinHistoryTable", "storing data...  "));
	refresh_sccpOm5MinHistoryTable();
	(void) tmpsize;
	for (hcindex = sccpOm5MinHistoryTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct sccpOm5MinHistoryTable_data *) hcindex->data;
/*   XXX:  if (StorageTmp->sccpOm5MinHistoryTableStorageType == ST_NONVOLATILE) { */
		memset(line, 0, sizeof(line));
		strcat(line, "sccpOm5MinHistoryTable ");
		cptr = line + strlen(line);
		cptr = read_config_store_data(ASN_UINTEGER, cptr, &StorageTmp->sccpNetworkEntityId, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->sccpOmInterval, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinNoTranslatorForAddress, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinNoRuleForAddress, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinPointCodeNotAvailable, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinPointCodeCongested, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinSubsystemUnavailable, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinSubsystemCongested, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinUnequippedSubsystem, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinSyntaxErrorDetected, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinRoutingFailure, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinReassemblyTimeOut, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinSegmentOutOfOrder, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinNoReassemblySpace, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinHopCounterViolation, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinTooLargeForSegmentation, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinReleaseFailure, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinInactivityTimerExpiry, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinProviderInitiatedReset, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinProviderInitiatedRelease, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinNoSegmentationSupport, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinSegmentationFailure, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinReassemblyFailure, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinTotalMessagesHandled, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinTotalMessagesLocal, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinTotalMessagesGTT, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinUDTMessagesSent, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinUDTSMessagesSent, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinUDTMessagesReceived, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinUDTSMessagesReceived, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinCRMessagesSent, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinCREFMessagesSent, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinCRMessagesReceived, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinCREFMessagesReceived, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinRSRMessagesSent, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinRSRMessagesReceived, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinERRMessagesSent, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinERRMessagesReceived, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinXUDTMessagesSent, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinXUDTSMessagesSent, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinXUDTMessagesReceived, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinXUDTSMessagesReceived, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinLUDTMessagesSent, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinLUDTSMessagesSent, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinLUDTMessagesReceived, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinLUDTSMessagesReceived, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinSSCMessageReceived, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinSSPMessageReceived, &tmpsize);

		snmpd_store_config(line);
/*   } */
	}
	DEBUGMSGTL(("sccpOm5MinHistoryTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/*
 * sccpOm15MinHistoryTable_add(): adds a structure node to our data set
 */
int
sccpOm15MinHistoryTable_add(struct sccpOm15MinHistoryTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("sccpOm15MinHistoryTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index
	   the data */
	/* sccpNetworkEntityId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_UINTEGER, (uint8_t *) &thedata->sccpNetworkEntityId, sizeof(thedata->sccpNetworkEntityId));
	/* sccpOmInterval */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (uint8_t *) &thedata->sccpOmInterval, sizeof(thedata->sccpOmInterval));

	header_complex_add_data(&sccpOm15MinHistoryTableStorage, vars, thedata);
	DEBUGMSGTL(("sccpOm15MinHistoryTable", "registered an entry\n"));
	DEBUGMSGTL(("sccpOm15MinHistoryTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/*
 * parse_sccpOm15MinHistoryTable():
 *   parses .conf file entries needed to configure the mib.
 */
void
parse_sccpOm15MinHistoryTable(const char *token, char *line)
{
	size_t tmpsize;
	struct sccpOm15MinHistoryTable_data *StorageTmp = SNMP_MALLOC_STRUCT(sccpOm15MinHistoryTable_data);

	DEBUGMSGTL(("sccpOm15MinHistoryTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	line = read_config_read_data(ASN_UINTEGER, line, &StorageTmp->sccpNetworkEntityId, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->sccpOmInterval, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinNoTranslatorForAddress, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinNoRuleForAddress, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinPointCodeNotAvailable, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinPointCodeCongested, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinSubsystemUnavailable, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinSubsystemCongested, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinUnequippedSubsystem, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinSyntaxErrorDetected, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinRoutingFailure, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinReassemblyTimeOut, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinSegmentOutOfOrder, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinNoReassemblySpace, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinHopCounterViolation, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinTooLargeForSegmentation, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinReleaseFailure, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinInactivityTimerExpiry, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinProviderInitiatedReset, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinProviderInitiatedRelease, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinNoSegmentationSupport, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinSegmentationFailure, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinReassemblyFailure, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinTotalMessagesHandled, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinTotalMessagesLocal, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinTotalMessagesGTT, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinUDTMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinUDTSMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinUDTMessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinUDTSMessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinCRMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinCREFMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinCRMessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinCREFMessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinRSRMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinRSRMessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinERRMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinERRMessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinXUDTMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinXUDTSMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinXUDTMessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinXUDTSMessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinLUDTMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinLUDTSMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinLUDTMessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinLUDTSMessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->sccpOm15MinSccpUnavailableDuration, &tmpsize);

	sccpOm15MinHistoryTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("sccpOm15MinHistoryTable", "done.\n"));
}

/*
 * store_sccpOm15MinHistoryTable():
 *   stores .conf file entries needed to configure the mib.
 */
int
store_sccpOm15MinHistoryTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct sccpOm15MinHistoryTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("sccpOm15MinHistoryTable", "storing data...  "));
	refresh_sccpOm15MinHistoryTable();
	(void) tmpsize;
	for (hcindex = sccpOm15MinHistoryTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct sccpOm15MinHistoryTable_data *) hcindex->data;
/*   XXX:  if (StorageTmp->sccpOm15MinHistoryTableStorageType == ST_NONVOLATILE) { */
		memset(line, 0, sizeof(line));
		strcat(line, "sccpOm15MinHistoryTable ");
		cptr = line + strlen(line);
		cptr = read_config_store_data(ASN_UINTEGER, cptr, &StorageTmp->sccpNetworkEntityId, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->sccpOmInterval, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinNoTranslatorForAddress, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinNoRuleForAddress, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinPointCodeNotAvailable, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinPointCodeCongested, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinSubsystemUnavailable, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinSubsystemCongested, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinUnequippedSubsystem, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinSyntaxErrorDetected, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinRoutingFailure, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinReassemblyTimeOut, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinSegmentOutOfOrder, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinNoReassemblySpace, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinHopCounterViolation, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinTooLargeForSegmentation, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinReleaseFailure, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinInactivityTimerExpiry, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinProviderInitiatedReset, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinProviderInitiatedRelease, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinNoSegmentationSupport, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinSegmentationFailure, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinReassemblyFailure, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinTotalMessagesHandled, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinTotalMessagesLocal, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinTotalMessagesGTT, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinUDTMessagesSent, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinUDTSMessagesSent, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinUDTMessagesReceived, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinUDTSMessagesReceived, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinCRMessagesSent, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinCREFMessagesSent, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinCRMessagesReceived, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinCREFMessagesReceived, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinRSRMessagesSent, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinRSRMessagesReceived, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinERRMessagesSent, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinERRMessagesReceived, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinXUDTMessagesSent, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinXUDTSMessagesSent, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinXUDTMessagesReceived, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinXUDTSMessagesReceived, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinLUDTMessagesSent, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinLUDTSMessagesSent, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinLUDTMessagesReceived, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinLUDTSMessagesReceived, &tmpsize);
		cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->sccpOm15MinSccpUnavailableDuration, &tmpsize);

		snmpd_store_config(line);
/*   } */
	}
	DEBUGMSGTL(("sccpOm15MinHistoryTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/*
 * sccpOm5MinSsnHistoryTable_add(): adds a structure node to our data set
 */
int
sccpOm5MinSsnHistoryTable_add(struct sccpOm5MinSsnHistoryTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("sccpOm5MinSsnHistoryTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index
	   the data */
	/* sccpNetworkEntityId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_UINTEGER, (uint8_t *) &thedata->sccpNetworkEntityId, sizeof(thedata->sccpNetworkEntityId));
	/* sccpOmInterval */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (uint8_t *) &thedata->sccpOmInterval, sizeof(thedata->sccpOmInterval));

	header_complex_add_data(&sccpOm5MinSsnHistoryTableStorage, vars, thedata);
	DEBUGMSGTL(("sccpOm5MinSsnHistoryTable", "registered an entry\n"));
	DEBUGMSGTL(("sccpOm5MinSsnHistoryTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/*
 * parse_sccpOm5MinSsnHistoryTable():
 *   parses .conf file entries needed to configure the mib.
 */
void
parse_sccpOm5MinSsnHistoryTable(const char *token, char *line)
{
	size_t tmpsize;
	struct sccpOm5MinSsnHistoryTable_data *StorageTmp = SNMP_MALLOC_STRUCT(sccpOm5MinSsnHistoryTable_data);

	DEBUGMSGTL(("sccpOm5MinSsnHistoryTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	line = read_config_read_data(ASN_UINTEGER, line, &StorageTmp->sccpNetworkEntityId, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->sccpOmInterval, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinLXUDTMessagesOrigClass0, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinLXUDTMessagesOrigClass1, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinLXUDTMessagesTermClass0, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinLXUDTMessagesTermClass1, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinMessagesSentToBackup, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinDT1MessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinDT1MessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinDT2MessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinDT2MessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinEDMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm5MinEDMessagesReceived, &tmpsize);

	sccpOm5MinSsnHistoryTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("sccpOm5MinSsnHistoryTable", "done.\n"));
}

/*
 * store_sccpOm5MinSsnHistoryTable():
 *   stores .conf file entries needed to configure the mib.
 */
int
store_sccpOm5MinSsnHistoryTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct sccpOm5MinSsnHistoryTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("sccpOm5MinSsnHistoryTable", "storing data...  "));
	refresh_sccpOm5MinSsnHistoryTable();
	(void) tmpsize;
	for (hcindex = sccpOm5MinSsnHistoryTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct sccpOm5MinSsnHistoryTable_data *) hcindex->data;
/*   XXX:  if (StorageTmp->sccpOm5MinSsnHistoryTableStorageType == ST_NONVOLATILE) { */
		memset(line, 0, sizeof(line));
		strcat(line, "sccpOm5MinSsnHistoryTable ");
		cptr = line + strlen(line);
		cptr = read_config_store_data(ASN_UINTEGER, cptr, &StorageTmp->sccpNetworkEntityId, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->sccpOmInterval, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinLXUDTMessagesOrigClass0, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinLXUDTMessagesOrigClass1, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinLXUDTMessagesTermClass0, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinLXUDTMessagesTermClass1, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinMessagesSentToBackup, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinDT1MessagesReceived, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinDT1MessagesSent, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinDT2MessagesReceived, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinDT2MessagesSent, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinEDMessagesSent, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm5MinEDMessagesReceived, &tmpsize);

		snmpd_store_config(line);
/*   } */
	}
	DEBUGMSGTL(("sccpOm5MinSsnHistoryTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/*
 * sccpOm15MinSsnHistoryTable_add(): adds a structure node to our data set
 */
int
sccpOm15MinSsnHistoryTable_add(struct sccpOm15MinSsnHistoryTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("sccpOm15MinSsnHistoryTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index
	   the data */
	/* sccpNetworkEntityId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_UINTEGER, (uint8_t *) &thedata->sccpNetworkEntityId, sizeof(thedata->sccpNetworkEntityId));
	/* sccpOmInterval */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (uint8_t *) &thedata->sccpOmInterval, sizeof(thedata->sccpOmInterval));

	header_complex_add_data(&sccpOm15MinSsnHistoryTableStorage, vars, thedata);
	DEBUGMSGTL(("sccpOm15MinSsnHistoryTable", "registered an entry\n"));
	DEBUGMSGTL(("sccpOm15MinSsnHistoryTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/*
 * parse_sccpOm15MinSsnHistoryTable():
 *   parses .conf file entries needed to configure the mib.
 */
void
parse_sccpOm15MinSsnHistoryTable(const char *token, char *line)
{
	size_t tmpsize;
	struct sccpOm15MinSsnHistoryTable_data *StorageTmp = SNMP_MALLOC_STRUCT(sccpOm15MinSsnHistoryTable_data);

	DEBUGMSGTL(("sccpOm15MinSsnHistoryTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	line = read_config_read_data(ASN_UINTEGER, line, &StorageTmp->sccpNetworkEntityId, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->sccpOmInterval, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinLXUDTMessagesOrigClass0, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinLXUDTMessagesOrigClass1, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinLXUDTMessagesTermClass0, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinLXUDTMessagesTermClass1, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinMessagesSentToBackup, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinDT1MessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinDT1MessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinDT2MessagesReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinDT2MessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinEDMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->sccpOm15MinEDMessagesReceived, &tmpsize);

	sccpOm15MinSsnHistoryTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("sccpOm15MinSsnHistoryTable", "done.\n"));
}

/*
 * store_sccpOm15MinSsnHistoryTable():
 *   stores .conf file entries needed to configure the mib.
 */
int
store_sccpOm15MinSsnHistoryTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct sccpOm15MinSsnHistoryTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("sccpOm15MinSsnHistoryTable", "storing data...  "));
	refresh_sccpOm15MinSsnHistoryTable();
	(void) tmpsize;
	for (hcindex = sccpOm15MinSsnHistoryTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct sccpOm15MinSsnHistoryTable_data *) hcindex->data;
/*   XXX:  if (StorageTmp->sccpOm15MinSsnHistoryTableStorageType == ST_NONVOLATILE) { */
		memset(line, 0, sizeof(line));
		strcat(line, "sccpOm15MinSsnHistoryTable ");
		cptr = line + strlen(line);
		cptr = read_config_store_data(ASN_UINTEGER, cptr, &StorageTmp->sccpNetworkEntityId, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->sccpOmInterval, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinLXUDTMessagesOrigClass0, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinLXUDTMessagesOrigClass1, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinLXUDTMessagesTermClass0, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinLXUDTMessagesTermClass1, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinMessagesSentToBackup, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinDT1MessagesReceived, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinDT1MessagesSent, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinDT2MessagesReceived, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinDT2MessagesSent, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinEDMessagesSent, &tmpsize);
		cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->sccpOm15MinEDMessagesReceived, &tmpsize);

		snmpd_store_config(line);
/*   } */
	}
	DEBUGMSGTL(("sccpOm15MinSsnHistoryTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * refresh_sccpOmErrorsTable(): refresh sccpOmErrorsTable
 *
 * Normally the values retrieved from the operating system are cached.
 * When the agent receives a SIGPOLL from an open STREAMS configuration
 * or administrative driver Stream, the STREAMS subsystem indicates to
 * the agent that the cache has been invalidated and that it should
 * reread tables from the STREAMS subsystem.  This function is used when
 * the agent start for the first time, or after a SIGPOLL has been
 * received (and a row or column has been requested).
 */
void
refresh_sccpOmErrorsTable(void)
{
	if (sccpOmErrorsTable_refresh == 0)
		return;
	sccpOmErrorsTable_refresh = 0;
	/* Here, update the table as required... */
}

/**
 * refresh_sccpOmErrorsTable_row(): refresh sccpOmErrorsTable row
 *
 * Normally the values retrieved from the operating system are cached.
 * However, if a row contains temporal values, such as statistics
 * counters, gauges, timestamps, or other transient columns, it may be
 * necessary to refresh the row on some other basis, but normally only
 * once per request.
 */
void
refresh_sccpOmErrorsTable_row(struct sccpOmErrorsTable_data *StorageTmp)
{
	if (StorageTmp->sccpOmErrorsTable_request == sa_request)
		return;
	StorageTmp->sccpOmErrorsTable_request = sa_request;
}

/*
 * var_sccpOmErrorsTable():
 *   Handle this table separately from the scalar value case.
 *   The workings of this are basically the same as for var_sccpOmMIB above.
 */
uint8_t *
var_sccpOmErrorsTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct sccpOmErrorsTable_data *StorageTmp = NULL;

	DEBUGMSGTL(("sccpOmMIB", "var_sccpOmErrorsTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the
	   header. */
	refresh_sccpOmErrorsTable();
	/* This assumes you have registered all your data properly with header_complex_add()
	   somewhere before this. */
	if ((StorageTmp = header_complex(sccpOmErrorsTableStorage, vp, name, length, exact, var_len, write_method)) == NULL)
		return NULL;
	refresh_sccpOmErrorsTable_row(StorageTmp);
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {

	case SCCPOMNOTRANSLATORFORADDRESS:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmNoTranslatorForAddress);
		return (uint8_t *) &StorageTmp->sccpOmNoTranslatorForAddress;
	case SCCPOMNORULEFORADDRESS:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmNoRuleForAddress);
		return (uint8_t *) &StorageTmp->sccpOmNoRuleForAddress;
	case SCCPOMPOINTCODENOTAVAILABLE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmPointCodeNotAvailable);
		return (uint8_t *) &StorageTmp->sccpOmPointCodeNotAvailable;
	case SCCPOMPOINTCODECONGESTED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmPointCodeCongested);
		return (uint8_t *) &StorageTmp->sccpOmPointCodeCongested;
	case SCCPOMSUBSYSTEMUNAVAILABLE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmSubsystemUnavailable);
		return (uint8_t *) &StorageTmp->sccpOmSubsystemUnavailable;
	case SCCPOMSUBSYSTEMCONGESTED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmSubsystemCongested);
		return (uint8_t *) &StorageTmp->sccpOmSubsystemCongested;
	case SCCPOMUNEQUIPPEDSUBSYSTEM:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmUnequippedSubsystem);
		return (uint8_t *) &StorageTmp->sccpOmUnequippedSubsystem;
	case SCCPOMSYNTAXERRORDETECTED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmSyntaxErrorDetected);
		return (uint8_t *) &StorageTmp->sccpOmSyntaxErrorDetected;
	case SCCPOMROUTINGFAILURE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmRoutingFailure);
		return (uint8_t *) &StorageTmp->sccpOmRoutingFailure;
	case SCCPOMREASSEMBLYTIMEOUT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmReassemblyTimeOut);
		return (uint8_t *) &StorageTmp->sccpOmReassemblyTimeOut;
	case SCCPOMSEGMENTOUTOFORDER:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmSegmentOutOfOrder);
		return (uint8_t *) &StorageTmp->sccpOmSegmentOutOfOrder;
	case SCCPOMNOREASSEMBLYSPACE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmNoReassemblySpace);
		return (uint8_t *) &StorageTmp->sccpOmNoReassemblySpace;
	case SCCPOMHOPCOUNTERVIOLATION:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmHopCounterViolation);
		return (uint8_t *) &StorageTmp->sccpOmHopCounterViolation;
	case SCCPOMTOOLARGEFORSEGMENTATION:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmTooLargeForSegmentation);
		return (uint8_t *) &StorageTmp->sccpOmTooLargeForSegmentation;
	case SCCPOMRELEASEFAILURE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmReleaseFailure);
		return (uint8_t *) &StorageTmp->sccpOmReleaseFailure;
	case SCCPOMINACTIVITYTIMEREXPIRY:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmInactivityTimerExpiry);
		return (uint8_t *) &StorageTmp->sccpOmInactivityTimerExpiry;
	case SCCPOMPROVIDERINITIATEDRESET:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmProviderInitiatedReset);
		return (uint8_t *) &StorageTmp->sccpOmProviderInitiatedReset;
	case SCCPOMPROVIDERINITIATEDRELEASE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmProviderInitiatedRelease);
		return (uint8_t *) &StorageTmp->sccpOmProviderInitiatedRelease;
	case SCCPOMNOSEGMENTATIONSUPPORT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmNoSegmentationSupport);
		return (uint8_t *) &StorageTmp->sccpOmNoSegmentationSupport;
	case SCCPOMSEGMENTATIONFAILURE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmSegmentationFailure);
		return (uint8_t *) &StorageTmp->sccpOmSegmentationFailure;
	case SCCPOMREASSEMBLYFAILURE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmReassemblyFailure);
		return (uint8_t *) &StorageTmp->sccpOmReassemblyFailure;
	case SCCPOMERRORS5MINVALIDINTERVALS:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmErrors5MinValidIntervals);
		return (uint8_t *) &StorageTmp->sccpOmErrors5MinValidIntervals;
	case SCCPOMERRORS15MINVALIDINTERVALS:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmErrors15MinValidIntervals);
		return (uint8_t *) &StorageTmp->sccpOmErrors15MinValidIntervals;

	default:
		ERROR_MSG("");
	}
	return NULL;
}

/**
 * refresh_sccpOmMessageTable(): refresh sccpOmMessageTable
 *
 * Normally the values retrieved from the operating system are cached.
 * When the agent receives a SIGPOLL from an open STREAMS configuration
 * or administrative driver Stream, the STREAMS subsystem indicates to
 * the agent that the cache has been invalidated and that it should
 * reread tables from the STREAMS subsystem.  This function is used when
 * the agent start for the first time, or after a SIGPOLL has been
 * received (and a row or column has been requested).
 */
void
refresh_sccpOmMessageTable(void)
{
	if (sccpOmMessageTable_refresh == 0)
		return;
	sccpOmMessageTable_refresh = 0;
	/* Here, update the table as required... */
}

/**
 * refresh_sccpOmMessageTable_row(): refresh sccpOmMessageTable row
 *
 * Normally the values retrieved from the operating system are cached.
 * However, if a row contains temporal values, such as statistics
 * counters, gauges, timestamps, or other transient columns, it may be
 * necessary to refresh the row on some other basis, but normally only
 * once per request.
 */
void
refresh_sccpOmMessageTable_row(struct sccpOmMessageTable_data *StorageTmp)
{
	if (StorageTmp->sccpOmMessageTable_request == sa_request)
		return;
	StorageTmp->sccpOmMessageTable_request = sa_request;
}

/*
 * var_sccpOmMessageTable():
 *   Handle this table separately from the scalar value case.
 *   The workings of this are basically the same as for var_sccpOmMIB above.
 */
uint8_t *
var_sccpOmMessageTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct sccpOmMessageTable_data *StorageTmp = NULL;

	DEBUGMSGTL(("sccpOmMIB", "var_sccpOmMessageTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the
	   header. */
	refresh_sccpOmMessageTable();
	/* This assumes you have registered all your data properly with header_complex_add()
	   somewhere before this. */
	if ((StorageTmp = header_complex(sccpOmMessageTableStorage, vp, name, length, exact, var_len, write_method)) == NULL)
		return NULL;
	refresh_sccpOmMessageTable_row(StorageTmp);
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {

	case SCCPOMTOTALMESSAGESHANDLED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmTotalMessagesHandled);
		return (uint8_t *) &StorageTmp->sccpOmTotalMessagesHandled;
	case SCCPOMTOTALMESSAGESLOCAL:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmTotalMessagesLocal);
		return (uint8_t *) &StorageTmp->sccpOmTotalMessagesLocal;
	case SCCPOMTOTALMESSAGESGTT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmTotalMessagesGTT);
		return (uint8_t *) &StorageTmp->sccpOmTotalMessagesGTT;
	case SCCPOMUDTMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmUDTMessagesSent);
		return (uint8_t *) &StorageTmp->sccpOmUDTMessagesSent;
	case SCCPOMUDTSMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmUDTSMessagesSent);
		return (uint8_t *) &StorageTmp->sccpOmUDTSMessagesSent;
	case SCCPOMUDTMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmUDTMessagesReceived);
		return (uint8_t *) &StorageTmp->sccpOmUDTMessagesReceived;
	case SCCPOMUDTSMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmUDTSMessagesReceived);
		return (uint8_t *) &StorageTmp->sccpOmUDTSMessagesReceived;
	case SCCPOMCRMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmCRMessagesSent);
		return (uint8_t *) &StorageTmp->sccpOmCRMessagesSent;
	case SCCPOMCREFMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmCREFMessagesSent);
		return (uint8_t *) &StorageTmp->sccpOmCREFMessagesSent;
	case SCCPOMCRMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmCRMessagesReceived);
		return (uint8_t *) &StorageTmp->sccpOmCRMessagesReceived;
	case SCCPOMCREFMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmCREFMessagesReceived);
		return (uint8_t *) &StorageTmp->sccpOmCREFMessagesReceived;
	case SCCPOMRSRMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmRSRMessagesSent);
		return (uint8_t *) &StorageTmp->sccpOmRSRMessagesSent;
	case SCCPOMRSRMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmRSRMessagesReceived);
		return (uint8_t *) &StorageTmp->sccpOmRSRMessagesReceived;
	case SCCPOMERRMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmERRMessagesSent);
		return (uint8_t *) &StorageTmp->sccpOmERRMessagesSent;
	case SCCPOMERRMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmERRMessagesReceived);
		return (uint8_t *) &StorageTmp->sccpOmERRMessagesReceived;
	case SCCPOMXUDTMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmXUDTMessagesSent);
		return (uint8_t *) &StorageTmp->sccpOmXUDTMessagesSent;
	case SCCPOMXUDTSMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmXUDTSMessagesSent);
		return (uint8_t *) &StorageTmp->sccpOmXUDTSMessagesSent;
	case SCCPOMXUDTMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmXUDTMessagesReceived);
		return (uint8_t *) &StorageTmp->sccpOmXUDTMessagesReceived;
	case SCCPOMXUDTSMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmXUDTSMessagesReceived);
		return (uint8_t *) &StorageTmp->sccpOmXUDTSMessagesReceived;
	case SCCPOMLUDTMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmLUDTMessagesSent);
		return (uint8_t *) &StorageTmp->sccpOmLUDTMessagesSent;
	case SCCPOMLUDTSMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmLUDTSMessagesSent);
		return (uint8_t *) &StorageTmp->sccpOmLUDTSMessagesSent;
	case SCCPOMLUDTMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmLUDTMessagesReceived);
		return (uint8_t *) &StorageTmp->sccpOmLUDTMessagesReceived;
	case SCCPOMLUDTSMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmLUDTSMessagesReceived);
		return (uint8_t *) &StorageTmp->sccpOmLUDTSMessagesReceived;
	case SCCPOMMESSAGE5MINVALIDINTERVALS:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmMessage5MinValidIntervals);
		return (uint8_t *) &StorageTmp->sccpOmMessage5MinValidIntervals;
	case SCCPOMMESSAGE15MINVALIDINTERVALS:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmMessage15MinValidIntervals);
		return (uint8_t *) &StorageTmp->sccpOmMessage15MinValidIntervals;

	default:
		ERROR_MSG("");
	}
	return NULL;
}

/**
 * refresh_sccpOmAccessibilityTable(): refresh sccpOmAccessibilityTable
 *
 * Normally the values retrieved from the operating system are cached.
 * When the agent receives a SIGPOLL from an open STREAMS configuration
 * or administrative driver Stream, the STREAMS subsystem indicates to
 * the agent that the cache has been invalidated and that it should
 * reread tables from the STREAMS subsystem.  This function is used when
 * the agent start for the first time, or after a SIGPOLL has been
 * received (and a row or column has been requested).
 */
void
refresh_sccpOmAccessibilityTable(void)
{
	if (sccpOmAccessibilityTable_refresh == 0)
		return;
	sccpOmAccessibilityTable_refresh = 0;
	/* Here, update the table as required... */
}

/**
 * refresh_sccpOmAccessibilityTable_row(): refresh sccpOmAccessibilityTable row
 *
 * Normally the values retrieved from the operating system are cached.
 * However, if a row contains temporal values, such as statistics
 * counters, gauges, timestamps, or other transient columns, it may be
 * necessary to refresh the row on some other basis, but normally only
 * once per request.
 */
void
refresh_sccpOmAccessibilityTable_row(struct sccpOmAccessibilityTable_data *StorageTmp)
{
	if (StorageTmp->sccpOmAccessibilityTable_request == sa_request)
		return;
	StorageTmp->sccpOmAccessibilityTable_request = sa_request;
}

/*
 * var_sccpOmAccessibilityTable():
 *   Handle this table separately from the scalar value case.
 *   The workings of this are basically the same as for var_sccpOmMIB above.
 */
uint8_t *
var_sccpOmAccessibilityTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct sccpOmAccessibilityTable_data *StorageTmp = NULL;

	DEBUGMSGTL(("sccpOmMIB", "var_sccpOmAccessibilityTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the
	   header. */
	refresh_sccpOmAccessibilityTable();
	/* This assumes you have registered all your data properly with header_complex_add()
	   somewhere before this. */
	if ((StorageTmp = header_complex(sccpOmAccessibilityTableStorage, vp, name, length, exact, var_len, write_method)) == NULL)
		return NULL;
	refresh_sccpOmAccessibilityTable_row(StorageTmp);
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {

	case SCCPOMSSCMESSAGERECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmSSCMessageReceived);
		return (uint8_t *) &StorageTmp->sccpOmSSCMessageReceived;
	case SCCPOMSSPMESSAGERECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmSSPMessageReceived);
		return (uint8_t *) &StorageTmp->sccpOmSSPMessageReceived;
	case SCCPOMSCCPUNAVAILABLEDURATION:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmSccpUnavailableDuration);
		return (uint8_t *) &StorageTmp->sccpOmSccpUnavailableDuration;
	case SCCPOMACCESSIBILITY5MINVALIDINTERVALS:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmAccessibility5MinValidIntervals);
		return (uint8_t *) &StorageTmp->sccpOmAccessibility5MinValidIntervals;
	case SCCPOMACCESSIBILITY15MINVALIDINTERVALS:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmAccessibility15MinValidIntervals);
		return (uint8_t *) &StorageTmp->sccpOmAccessibility15MinValidIntervals;

	default:
		ERROR_MSG("");
	}
	return NULL;
}

/**
 * refresh_sccpOmUtilizationTable(): refresh sccpOmUtilizationTable
 *
 * Normally the values retrieved from the operating system are cached.
 * When the agent receives a SIGPOLL from an open STREAMS configuration
 * or administrative driver Stream, the STREAMS subsystem indicates to
 * the agent that the cache has been invalidated and that it should
 * reread tables from the STREAMS subsystem.  This function is used when
 * the agent start for the first time, or after a SIGPOLL has been
 * received (and a row or column has been requested).
 */
void
refresh_sccpOmUtilizationTable(void)
{
	if (sccpOmUtilizationTable_refresh == 0)
		return;
	sccpOmUtilizationTable_refresh = 0;
	/* Here, update the table as required... */
}

/**
 * refresh_sccpOmUtilizationTable_row(): refresh sccpOmUtilizationTable row
 *
 * Normally the values retrieved from the operating system are cached.
 * However, if a row contains temporal values, such as statistics
 * counters, gauges, timestamps, or other transient columns, it may be
 * necessary to refresh the row on some other basis, but normally only
 * once per request.
 */
void
refresh_sccpOmUtilizationTable_row(struct sccpOmUtilizationTable_data *StorageTmp)
{
	if (StorageTmp->sccpOmUtilizationTable_request == sa_request)
		return;
	StorageTmp->sccpOmUtilizationTable_request = sa_request;
}

/*
 * var_sccpOmUtilizationTable():
 *   Handle this table separately from the scalar value case.
 *   The workings of this are basically the same as for var_sccpOmMIB above.
 */
uint8_t *
var_sccpOmUtilizationTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct sccpOmUtilizationTable_data *StorageTmp = NULL;

	DEBUGMSGTL(("sccpOmMIB", "var_sccpOmUtilizationTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the
	   header. */
	refresh_sccpOmUtilizationTable();
	/* This assumes you have registered all your data properly with header_complex_add()
	   somewhere before this. */
	if ((StorageTmp = header_complex(sccpOmUtilizationTableStorage, vp, name, length, exact, var_len, write_method)) == NULL)
		return NULL;
	refresh_sccpOmUtilizationTable_row(StorageTmp);
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {

	case SCCPOMLXUDTMESSAGESORIGCLASS0:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmLXUDTMessagesOrigClass0);
		return (uint8_t *) &StorageTmp->sccpOmLXUDTMessagesOrigClass0;
	case SCCPOMLXUDTMESSAGESORIGCLASS1:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmLXUDTMessagesOrigClass1);
		return (uint8_t *) &StorageTmp->sccpOmLXUDTMessagesOrigClass1;
	case SCCPOMLXUDTMESSAGESTERMCLASS0:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmLXUDTMessagesTermClass0);
		return (uint8_t *) &StorageTmp->sccpOmLXUDTMessagesTermClass0;
	case SCCPOMLXUDTMESSAGESTERMCLASS1:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmLXUDTMessagesTermClass1);
		return (uint8_t *) &StorageTmp->sccpOmLXUDTMessagesTermClass1;
	case SCCPOMMESSAGESSENTTOBACKUP:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmMessagesSentToBackup);
		return (uint8_t *) &StorageTmp->sccpOmMessagesSentToBackup;
	case SCCPOMDT1MESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmDT1MessagesReceived);
		return (uint8_t *) &StorageTmp->sccpOmDT1MessagesReceived;
	case SCCPOMDT1MESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmDT1MessagesSent);
		return (uint8_t *) &StorageTmp->sccpOmDT1MessagesSent;
	case SCCPOMDT2MESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmDT2MessagesReceived);
		return (uint8_t *) &StorageTmp->sccpOmDT2MessagesReceived;
	case SCCPOMDT2MESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmDT2MessagesSent);
		return (uint8_t *) &StorageTmp->sccpOmDT2MessagesSent;
	case SCCPOMEDMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmEDMessagesSent);
		return (uint8_t *) &StorageTmp->sccpOmEDMessagesSent;
	case SCCPOMEDMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmEDMessagesReceived);
		return (uint8_t *) &StorageTmp->sccpOmEDMessagesReceived;
	case SCCPOMUTILIZATION5MINVALIDINTERVALS:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmUtilization5MinValidIntervals);
		return (uint8_t *) &StorageTmp->sccpOmUtilization5MinValidIntervals;
	case SCCPOMUTILIZATION15MINVALIDINTERVALS:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOmUtilization15MinValidIntervals);
		return (uint8_t *) &StorageTmp->sccpOmUtilization15MinValidIntervals;

	default:
		ERROR_MSG("");
	}
	return NULL;
}

/**
 * refresh_sccpOm5MinHistoryTable(): refresh sccpOm5MinHistoryTable
 *
 * Normally the values retrieved from the operating system are cached.
 * When the agent receives a SIGPOLL from an open STREAMS configuration
 * or administrative driver Stream, the STREAMS subsystem indicates to
 * the agent that the cache has been invalidated and that it should
 * reread tables from the STREAMS subsystem.  This function is used when
 * the agent start for the first time, or after a SIGPOLL has been
 * received (and a row or column has been requested).
 */
void
refresh_sccpOm5MinHistoryTable(void)
{
	if (sccpOm5MinHistoryTable_refresh == 0)
		return;
	sccpOm5MinHistoryTable_refresh = 0;
	/* Here, update the table as required... */
}

/**
 * refresh_sccpOm5MinHistoryTable_row(): refresh sccpOm5MinHistoryTable row
 *
 * Normally the values retrieved from the operating system are cached.
 * However, if a row contains temporal values, such as statistics
 * counters, gauges, timestamps, or other transient columns, it may be
 * necessary to refresh the row on some other basis, but normally only
 * once per request.
 */
void
refresh_sccpOm5MinHistoryTable_row(struct sccpOm5MinHistoryTable_data *StorageTmp)
{
	if (StorageTmp->sccpOm5MinHistoryTable_request == sa_request)
		return;
	StorageTmp->sccpOm5MinHistoryTable_request = sa_request;
}

/*
 * var_sccpOm5MinHistoryTable():
 *   Handle this table separately from the scalar value case.
 *   The workings of this are basically the same as for var_sccpOmMIB above.
 */
uint8_t *
var_sccpOm5MinHistoryTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct sccpOm5MinHistoryTable_data *StorageTmp = NULL;

	DEBUGMSGTL(("sccpOmMIB", "var_sccpOm5MinHistoryTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the
	   header. */
	refresh_sccpOm5MinHistoryTable();
	/* This assumes you have registered all your data properly with header_complex_add()
	   somewhere before this. */
	if ((StorageTmp = header_complex(sccpOm5MinHistoryTableStorage, vp, name, length, exact, var_len, write_method)) == NULL)
		return NULL;
	refresh_sccpOm5MinHistoryTable_row(StorageTmp);
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {

	case SCCPOM5MINNOTRANSLATORFORADDRESS:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinNoTranslatorForAddress);
		return (uint8_t *) &StorageTmp->sccpOm5MinNoTranslatorForAddress;
	case SCCPOM5MINNORULEFORADDRESS:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinNoRuleForAddress);
		return (uint8_t *) &StorageTmp->sccpOm5MinNoRuleForAddress;
	case SCCPOM5MINPOINTCODENOTAVAILABLE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinPointCodeNotAvailable);
		return (uint8_t *) &StorageTmp->sccpOm5MinPointCodeNotAvailable;
	case SCCPOM5MINPOINTCODECONGESTED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinPointCodeCongested);
		return (uint8_t *) &StorageTmp->sccpOm5MinPointCodeCongested;
	case SCCPOM5MINSUBSYSTEMUNAVAILABLE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinSubsystemUnavailable);
		return (uint8_t *) &StorageTmp->sccpOm5MinSubsystemUnavailable;
	case SCCPOM5MINSUBSYSTEMCONGESTED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinSubsystemCongested);
		return (uint8_t *) &StorageTmp->sccpOm5MinSubsystemCongested;
	case SCCPOM5MINUNEQUIPPEDSUBSYSTEM:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinUnequippedSubsystem);
		return (uint8_t *) &StorageTmp->sccpOm5MinUnequippedSubsystem;
	case SCCPOM5MINSYNTAXERRORDETECTED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinSyntaxErrorDetected);
		return (uint8_t *) &StorageTmp->sccpOm5MinSyntaxErrorDetected;
	case SCCPOM5MINROUTINGFAILURE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinRoutingFailure);
		return (uint8_t *) &StorageTmp->sccpOm5MinRoutingFailure;
	case SCCPOM5MINREASSEMBLYTIMEOUT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinReassemblyTimeOut);
		return (uint8_t *) &StorageTmp->sccpOm5MinReassemblyTimeOut;
	case SCCPOM5MINSEGMENTOUTOFORDER:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinSegmentOutOfOrder);
		return (uint8_t *) &StorageTmp->sccpOm5MinSegmentOutOfOrder;
	case SCCPOM5MINNOREASSEMBLYSPACE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinNoReassemblySpace);
		return (uint8_t *) &StorageTmp->sccpOm5MinNoReassemblySpace;
	case SCCPOM5MINHOPCOUNTERVIOLATION:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinHopCounterViolation);
		return (uint8_t *) &StorageTmp->sccpOm5MinHopCounterViolation;
	case SCCPOM5MINTOOLARGEFORSEGMENTATION:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinTooLargeForSegmentation);
		return (uint8_t *) &StorageTmp->sccpOm5MinTooLargeForSegmentation;
	case SCCPOM5MINRELEASEFAILURE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinReleaseFailure);
		return (uint8_t *) &StorageTmp->sccpOm5MinReleaseFailure;
	case SCCPOM5MININACTIVITYTIMEREXPIRY:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinInactivityTimerExpiry);
		return (uint8_t *) &StorageTmp->sccpOm5MinInactivityTimerExpiry;
	case SCCPOM5MINPROVIDERINITIATEDRESET:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinProviderInitiatedReset);
		return (uint8_t *) &StorageTmp->sccpOm5MinProviderInitiatedReset;
	case SCCPOM5MINPROVIDERINITIATEDRELEASE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinProviderInitiatedRelease);
		return (uint8_t *) &StorageTmp->sccpOm5MinProviderInitiatedRelease;
	case SCCPOM5MINNOSEGMENTATIONSUPPORT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinNoSegmentationSupport);
		return (uint8_t *) &StorageTmp->sccpOm5MinNoSegmentationSupport;
	case SCCPOM5MINSEGMENTATIONFAILURE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinSegmentationFailure);
		return (uint8_t *) &StorageTmp->sccpOm5MinSegmentationFailure;
	case SCCPOM5MINREASSEMBLYFAILURE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinReassemblyFailure);
		return (uint8_t *) &StorageTmp->sccpOm5MinReassemblyFailure;
	case SCCPOM5MINTOTALMESSAGESHANDLED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinTotalMessagesHandled);
		return (uint8_t *) &StorageTmp->sccpOm5MinTotalMessagesHandled;
	case SCCPOM5MINTOTALMESSAGESLOCAL:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinTotalMessagesLocal);
		return (uint8_t *) &StorageTmp->sccpOm5MinTotalMessagesLocal;
	case SCCPOM5MINTOTALMESSAGESGTT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinTotalMessagesGTT);
		return (uint8_t *) &StorageTmp->sccpOm5MinTotalMessagesGTT;
	case SCCPOM5MINUDTMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinUDTMessagesSent);
		return (uint8_t *) &StorageTmp->sccpOm5MinUDTMessagesSent;
	case SCCPOM5MINUDTSMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinUDTSMessagesSent);
		return (uint8_t *) &StorageTmp->sccpOm5MinUDTSMessagesSent;
	case SCCPOM5MINUDTMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinUDTMessagesReceived);
		return (uint8_t *) &StorageTmp->sccpOm5MinUDTMessagesReceived;
	case SCCPOM5MINUDTSMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinUDTSMessagesReceived);
		return (uint8_t *) &StorageTmp->sccpOm5MinUDTSMessagesReceived;
	case SCCPOM5MINCRMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinCRMessagesSent);
		return (uint8_t *) &StorageTmp->sccpOm5MinCRMessagesSent;
	case SCCPOM5MINCREFMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinCREFMessagesSent);
		return (uint8_t *) &StorageTmp->sccpOm5MinCREFMessagesSent;
	case SCCPOM5MINCRMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinCRMessagesReceived);
		return (uint8_t *) &StorageTmp->sccpOm5MinCRMessagesReceived;
	case SCCPOM5MINCREFMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinCREFMessagesReceived);
		return (uint8_t *) &StorageTmp->sccpOm5MinCREFMessagesReceived;
	case SCCPOM5MINRSRMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinRSRMessagesSent);
		return (uint8_t *) &StorageTmp->sccpOm5MinRSRMessagesSent;
	case SCCPOM5MINRSRMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinRSRMessagesReceived);
		return (uint8_t *) &StorageTmp->sccpOm5MinRSRMessagesReceived;
	case SCCPOM5MINERRMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinERRMessagesSent);
		return (uint8_t *) &StorageTmp->sccpOm5MinERRMessagesSent;
	case SCCPOM5MINERRMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinERRMessagesReceived);
		return (uint8_t *) &StorageTmp->sccpOm5MinERRMessagesReceived;
	case SCCPOM5MINXUDTMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinXUDTMessagesSent);
		return (uint8_t *) &StorageTmp->sccpOm5MinXUDTMessagesSent;
	case SCCPOM5MINXUDTSMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinXUDTSMessagesSent);
		return (uint8_t *) &StorageTmp->sccpOm5MinXUDTSMessagesSent;
	case SCCPOM5MINXUDTMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinXUDTMessagesReceived);
		return (uint8_t *) &StorageTmp->sccpOm5MinXUDTMessagesReceived;
	case SCCPOM5MINXUDTSMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinXUDTSMessagesReceived);
		return (uint8_t *) &StorageTmp->sccpOm5MinXUDTSMessagesReceived;
	case SCCPOM5MINLUDTMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinLUDTMessagesSent);
		return (uint8_t *) &StorageTmp->sccpOm5MinLUDTMessagesSent;
	case SCCPOM5MINLUDTSMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinLUDTSMessagesSent);
		return (uint8_t *) &StorageTmp->sccpOm5MinLUDTSMessagesSent;
	case SCCPOM5MINLUDTMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinLUDTMessagesReceived);
		return (uint8_t *) &StorageTmp->sccpOm5MinLUDTMessagesReceived;
	case SCCPOM5MINLUDTSMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinLUDTSMessagesReceived);
		return (uint8_t *) &StorageTmp->sccpOm5MinLUDTSMessagesReceived;
	case SCCPOM5MINSSCMESSAGERECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinSSCMessageReceived);
		return (uint8_t *) &StorageTmp->sccpOm5MinSSCMessageReceived;
	case SCCPOM5MINSSPMESSAGERECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinSSPMessageReceived);
		return (uint8_t *) &StorageTmp->sccpOm5MinSSPMessageReceived;

	default:
		ERROR_MSG("");
	}
	return NULL;
}

/**
 * refresh_sccpOm15MinHistoryTable(): refresh sccpOm15MinHistoryTable
 *
 * Normally the values retrieved from the operating system are cached.
 * When the agent receives a SIGPOLL from an open STREAMS configuration
 * or administrative driver Stream, the STREAMS subsystem indicates to
 * the agent that the cache has been invalidated and that it should
 * reread tables from the STREAMS subsystem.  This function is used when
 * the agent start for the first time, or after a SIGPOLL has been
 * received (and a row or column has been requested).
 */
void
refresh_sccpOm15MinHistoryTable(void)
{
	if (sccpOm15MinHistoryTable_refresh == 0)
		return;
	sccpOm15MinHistoryTable_refresh = 0;
	/* Here, update the table as required... */
}

/**
 * refresh_sccpOm15MinHistoryTable_row(): refresh sccpOm15MinHistoryTable row
 *
 * Normally the values retrieved from the operating system are cached.
 * However, if a row contains temporal values, such as statistics
 * counters, gauges, timestamps, or other transient columns, it may be
 * necessary to refresh the row on some other basis, but normally only
 * once per request.
 */
void
refresh_sccpOm15MinHistoryTable_row(struct sccpOm15MinHistoryTable_data *StorageTmp)
{
	if (StorageTmp->sccpOm15MinHistoryTable_request == sa_request)
		return;
	StorageTmp->sccpOm15MinHistoryTable_request = sa_request;
}

/*
 * var_sccpOm15MinHistoryTable():
 *   Handle this table separately from the scalar value case.
 *   The workings of this are basically the same as for var_sccpOmMIB above.
 */
uint8_t *
var_sccpOm15MinHistoryTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct sccpOm15MinHistoryTable_data *StorageTmp = NULL;

	DEBUGMSGTL(("sccpOmMIB", "var_sccpOm15MinHistoryTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the
	   header. */
	refresh_sccpOm15MinHistoryTable();
	/* This assumes you have registered all your data properly with header_complex_add()
	   somewhere before this. */
	if ((StorageTmp = header_complex(sccpOm15MinHistoryTableStorage, vp, name, length, exact, var_len, write_method)) == NULL)
		return NULL;
	refresh_sccpOm15MinHistoryTable_row(StorageTmp);
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {

	case SCCPOM15MINNOTRANSLATORFORADDRESS:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinNoTranslatorForAddress);
		return (uint8_t *) &StorageTmp->sccpOm15MinNoTranslatorForAddress;
	case SCCPOM15MINNORULEFORADDRESS:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinNoRuleForAddress);
		return (uint8_t *) &StorageTmp->sccpOm15MinNoRuleForAddress;
	case SCCPOM15MINPOINTCODENOTAVAILABLE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinPointCodeNotAvailable);
		return (uint8_t *) &StorageTmp->sccpOm15MinPointCodeNotAvailable;
	case SCCPOM15MINPOINTCODECONGESTED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinPointCodeCongested);
		return (uint8_t *) &StorageTmp->sccpOm15MinPointCodeCongested;
	case SCCPOM15MINSUBSYSTEMUNAVAILABLE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinSubsystemUnavailable);
		return (uint8_t *) &StorageTmp->sccpOm15MinSubsystemUnavailable;
	case SCCPOM15MINSUBSYSTEMCONGESTED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinSubsystemCongested);
		return (uint8_t *) &StorageTmp->sccpOm15MinSubsystemCongested;
	case SCCPOM15MINUNEQUIPPEDSUBSYSTEM:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinUnequippedSubsystem);
		return (uint8_t *) &StorageTmp->sccpOm15MinUnequippedSubsystem;
	case SCCPOM15MINSYNTAXERRORDETECTED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinSyntaxErrorDetected);
		return (uint8_t *) &StorageTmp->sccpOm15MinSyntaxErrorDetected;
	case SCCPOM15MINROUTINGFAILURE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinRoutingFailure);
		return (uint8_t *) &StorageTmp->sccpOm15MinRoutingFailure;
	case SCCPOM15MINREASSEMBLYTIMEOUT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinReassemblyTimeOut);
		return (uint8_t *) &StorageTmp->sccpOm15MinReassemblyTimeOut;
	case SCCPOM15MINSEGMENTOUTOFORDER:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinSegmentOutOfOrder);
		return (uint8_t *) &StorageTmp->sccpOm15MinSegmentOutOfOrder;
	case SCCPOM15MINNOREASSEMBLYSPACE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinNoReassemblySpace);
		return (uint8_t *) &StorageTmp->sccpOm15MinNoReassemblySpace;
	case SCCPOM15MINHOPCOUNTERVIOLATION:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinHopCounterViolation);
		return (uint8_t *) &StorageTmp->sccpOm15MinHopCounterViolation;
	case SCCPOM15MINTOOLARGEFORSEGMENTATION:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinTooLargeForSegmentation);
		return (uint8_t *) &StorageTmp->sccpOm15MinTooLargeForSegmentation;
	case SCCPOM15MINRELEASEFAILURE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinReleaseFailure);
		return (uint8_t *) &StorageTmp->sccpOm15MinReleaseFailure;
	case SCCPOM15MININACTIVITYTIMEREXPIRY:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinInactivityTimerExpiry);
		return (uint8_t *) &StorageTmp->sccpOm15MinInactivityTimerExpiry;
	case SCCPOM15MINPROVIDERINITIATEDRESET:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinProviderInitiatedReset);
		return (uint8_t *) &StorageTmp->sccpOm15MinProviderInitiatedReset;
	case SCCPOM15MINPROVIDERINITIATEDRELEASE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinProviderInitiatedRelease);
		return (uint8_t *) &StorageTmp->sccpOm15MinProviderInitiatedRelease;
	case SCCPOM15MINNOSEGMENTATIONSUPPORT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinNoSegmentationSupport);
		return (uint8_t *) &StorageTmp->sccpOm15MinNoSegmentationSupport;
	case SCCPOM15MINSEGMENTATIONFAILURE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinSegmentationFailure);
		return (uint8_t *) &StorageTmp->sccpOm15MinSegmentationFailure;
	case SCCPOM15MINREASSEMBLYFAILURE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinReassemblyFailure);
		return (uint8_t *) &StorageTmp->sccpOm15MinReassemblyFailure;
	case SCCPOM15MINTOTALMESSAGESHANDLED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinTotalMessagesHandled);
		return (uint8_t *) &StorageTmp->sccpOm15MinTotalMessagesHandled;
	case SCCPOM15MINTOTALMESSAGESLOCAL:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinTotalMessagesLocal);
		return (uint8_t *) &StorageTmp->sccpOm15MinTotalMessagesLocal;
	case SCCPOM15MINTOTALMESSAGESGTT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinTotalMessagesGTT);
		return (uint8_t *) &StorageTmp->sccpOm15MinTotalMessagesGTT;
	case SCCPOM15MINUDTMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinUDTMessagesSent);
		return (uint8_t *) &StorageTmp->sccpOm15MinUDTMessagesSent;
	case SCCPOM15MINUDTSMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinUDTSMessagesSent);
		return (uint8_t *) &StorageTmp->sccpOm15MinUDTSMessagesSent;
	case SCCPOM15MINUDTMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinUDTMessagesReceived);
		return (uint8_t *) &StorageTmp->sccpOm15MinUDTMessagesReceived;
	case SCCPOM15MINUDTSMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinUDTSMessagesReceived);
		return (uint8_t *) &StorageTmp->sccpOm15MinUDTSMessagesReceived;
	case SCCPOM15MINCRMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinCRMessagesSent);
		return (uint8_t *) &StorageTmp->sccpOm15MinCRMessagesSent;
	case SCCPOM15MINCREFMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinCREFMessagesSent);
		return (uint8_t *) &StorageTmp->sccpOm15MinCREFMessagesSent;
	case SCCPOM15MINCRMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinCRMessagesReceived);
		return (uint8_t *) &StorageTmp->sccpOm15MinCRMessagesReceived;
	case SCCPOM15MINCREFMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinCREFMessagesReceived);
		return (uint8_t *) &StorageTmp->sccpOm15MinCREFMessagesReceived;
	case SCCPOM15MINRSRMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinRSRMessagesSent);
		return (uint8_t *) &StorageTmp->sccpOm15MinRSRMessagesSent;
	case SCCPOM15MINRSRMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinRSRMessagesReceived);
		return (uint8_t *) &StorageTmp->sccpOm15MinRSRMessagesReceived;
	case SCCPOM15MINERRMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinERRMessagesSent);
		return (uint8_t *) &StorageTmp->sccpOm15MinERRMessagesSent;
	case SCCPOM15MINERRMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinERRMessagesReceived);
		return (uint8_t *) &StorageTmp->sccpOm15MinERRMessagesReceived;
	case SCCPOM15MINXUDTMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinXUDTMessagesSent);
		return (uint8_t *) &StorageTmp->sccpOm15MinXUDTMessagesSent;
	case SCCPOM15MINXUDTSMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinXUDTSMessagesSent);
		return (uint8_t *) &StorageTmp->sccpOm15MinXUDTSMessagesSent;
	case SCCPOM15MINXUDTMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinXUDTMessagesReceived);
		return (uint8_t *) &StorageTmp->sccpOm15MinXUDTMessagesReceived;
	case SCCPOM15MINXUDTSMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinXUDTSMessagesReceived);
		return (uint8_t *) &StorageTmp->sccpOm15MinXUDTSMessagesReceived;
	case SCCPOM15MINLUDTMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinLUDTMessagesSent);
		return (uint8_t *) &StorageTmp->sccpOm15MinLUDTMessagesSent;
	case SCCPOM15MINLUDTSMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinLUDTSMessagesSent);
		return (uint8_t *) &StorageTmp->sccpOm15MinLUDTSMessagesSent;
	case SCCPOM15MINLUDTMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinLUDTMessagesReceived);
		return (uint8_t *) &StorageTmp->sccpOm15MinLUDTMessagesReceived;
	case SCCPOM15MINLUDTSMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinLUDTSMessagesReceived);
		return (uint8_t *) &StorageTmp->sccpOm15MinLUDTSMessagesReceived;
	case SCCPOM15MINSCCPUNAVAILABLEDURATION:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinSccpUnavailableDuration);
		return (uint8_t *) &StorageTmp->sccpOm15MinSccpUnavailableDuration;

	default:
		ERROR_MSG("");
	}
	return NULL;
}

/**
 * refresh_sccpOm5MinSsnHistoryTable(): refresh sccpOm5MinSsnHistoryTable
 *
 * Normally the values retrieved from the operating system are cached.
 * When the agent receives a SIGPOLL from an open STREAMS configuration
 * or administrative driver Stream, the STREAMS subsystem indicates to
 * the agent that the cache has been invalidated and that it should
 * reread tables from the STREAMS subsystem.  This function is used when
 * the agent start for the first time, or after a SIGPOLL has been
 * received (and a row or column has been requested).
 */
void
refresh_sccpOm5MinSsnHistoryTable(void)
{
	if (sccpOm5MinSsnHistoryTable_refresh == 0)
		return;
	sccpOm5MinSsnHistoryTable_refresh = 0;
	/* Here, update the table as required... */
}

/**
 * refresh_sccpOm5MinSsnHistoryTable_row(): refresh sccpOm5MinSsnHistoryTable row
 *
 * Normally the values retrieved from the operating system are cached.
 * However, if a row contains temporal values, such as statistics
 * counters, gauges, timestamps, or other transient columns, it may be
 * necessary to refresh the row on some other basis, but normally only
 * once per request.
 */
void
refresh_sccpOm5MinSsnHistoryTable_row(struct sccpOm5MinSsnHistoryTable_data *StorageTmp)
{
	if (StorageTmp->sccpOm5MinSsnHistoryTable_request == sa_request)
		return;
	StorageTmp->sccpOm5MinSsnHistoryTable_request = sa_request;
}

/*
 * var_sccpOm5MinSsnHistoryTable():
 *   Handle this table separately from the scalar value case.
 *   The workings of this are basically the same as for var_sccpOmMIB above.
 */
uint8_t *
var_sccpOm5MinSsnHistoryTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct sccpOm5MinSsnHistoryTable_data *StorageTmp = NULL;

	DEBUGMSGTL(("sccpOmMIB", "var_sccpOm5MinSsnHistoryTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the
	   header. */
	refresh_sccpOm5MinSsnHistoryTable();
	/* This assumes you have registered all your data properly with header_complex_add()
	   somewhere before this. */
	if ((StorageTmp = header_complex(sccpOm5MinSsnHistoryTableStorage, vp, name, length, exact, var_len, write_method)) == NULL)
		return NULL;
	refresh_sccpOm5MinSsnHistoryTable_row(StorageTmp);
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {

	case SCCPOM5MINLXUDTMESSAGESORIGCLASS0:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinLXUDTMessagesOrigClass0);
		return (uint8_t *) &StorageTmp->sccpOm5MinLXUDTMessagesOrigClass0;
	case SCCPOM5MINLXUDTMESSAGESORIGCLASS1:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinLXUDTMessagesOrigClass1);
		return (uint8_t *) &StorageTmp->sccpOm5MinLXUDTMessagesOrigClass1;
	case SCCPOM5MINLXUDTMESSAGESTERMCLASS0:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinLXUDTMessagesTermClass0);
		return (uint8_t *) &StorageTmp->sccpOm5MinLXUDTMessagesTermClass0;
	case SCCPOM5MINLXUDTMESSAGESTERMCLASS1:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinLXUDTMessagesTermClass1);
		return (uint8_t *) &StorageTmp->sccpOm5MinLXUDTMessagesTermClass1;
	case SCCPOM5MINMESSAGESSENTTOBACKUP:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinMessagesSentToBackup);
		return (uint8_t *) &StorageTmp->sccpOm5MinMessagesSentToBackup;
	case SCCPOM5MINDT1MESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinDT1MessagesReceived);
		return (uint8_t *) &StorageTmp->sccpOm5MinDT1MessagesReceived;
	case SCCPOM5MINDT1MESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinDT1MessagesSent);
		return (uint8_t *) &StorageTmp->sccpOm5MinDT1MessagesSent;
	case SCCPOM5MINDT2MESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinDT2MessagesReceived);
		return (uint8_t *) &StorageTmp->sccpOm5MinDT2MessagesReceived;
	case SCCPOM5MINDT2MESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinDT2MessagesSent);
		return (uint8_t *) &StorageTmp->sccpOm5MinDT2MessagesSent;
	case SCCPOM5MINEDMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinEDMessagesSent);
		return (uint8_t *) &StorageTmp->sccpOm5MinEDMessagesSent;
	case SCCPOM5MINEDMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm5MinEDMessagesReceived);
		return (uint8_t *) &StorageTmp->sccpOm5MinEDMessagesReceived;

	default:
		ERROR_MSG("");
	}
	return NULL;
}

/**
 * refresh_sccpOm15MinSsnHistoryTable(): refresh sccpOm15MinSsnHistoryTable
 *
 * Normally the values retrieved from the operating system are cached.
 * When the agent receives a SIGPOLL from an open STREAMS configuration
 * or administrative driver Stream, the STREAMS subsystem indicates to
 * the agent that the cache has been invalidated and that it should
 * reread tables from the STREAMS subsystem.  This function is used when
 * the agent start for the first time, or after a SIGPOLL has been
 * received (and a row or column has been requested).
 */
void
refresh_sccpOm15MinSsnHistoryTable(void)
{
	if (sccpOm15MinSsnHistoryTable_refresh == 0)
		return;
	sccpOm15MinSsnHistoryTable_refresh = 0;
	/* Here, update the table as required... */
}

/**
 * refresh_sccpOm15MinSsnHistoryTable_row(): refresh sccpOm15MinSsnHistoryTable row
 *
 * Normally the values retrieved from the operating system are cached.
 * However, if a row contains temporal values, such as statistics
 * counters, gauges, timestamps, or other transient columns, it may be
 * necessary to refresh the row on some other basis, but normally only
 * once per request.
 */
void
refresh_sccpOm15MinSsnHistoryTable_row(struct sccpOm15MinSsnHistoryTable_data *StorageTmp)
{
	if (StorageTmp->sccpOm15MinSsnHistoryTable_request == sa_request)
		return;
	StorageTmp->sccpOm15MinSsnHistoryTable_request = sa_request;
}

/*
 * var_sccpOm15MinSsnHistoryTable():
 *   Handle this table separately from the scalar value case.
 *   The workings of this are basically the same as for var_sccpOmMIB above.
 */
uint8_t *
var_sccpOm15MinSsnHistoryTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct sccpOm15MinSsnHistoryTable_data *StorageTmp = NULL;

	DEBUGMSGTL(("sccpOmMIB", "var_sccpOm15MinSsnHistoryTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the
	   header. */
	refresh_sccpOm15MinSsnHistoryTable();
	/* This assumes you have registered all your data properly with header_complex_add()
	   somewhere before this. */
	if ((StorageTmp = header_complex(sccpOm15MinSsnHistoryTableStorage, vp, name, length, exact, var_len, write_method)) == NULL)
		return NULL;
	refresh_sccpOm15MinSsnHistoryTable_row(StorageTmp);
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {

	case SCCPOM15MINLXUDTMESSAGESORIGCLASS0:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinLXUDTMessagesOrigClass0);
		return (uint8_t *) &StorageTmp->sccpOm15MinLXUDTMessagesOrigClass0;
	case SCCPOM15MINLXUDTMESSAGESORIGCLASS1:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinLXUDTMessagesOrigClass1);
		return (uint8_t *) &StorageTmp->sccpOm15MinLXUDTMessagesOrigClass1;
	case SCCPOM15MINLXUDTMESSAGESTERMCLASS0:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinLXUDTMessagesTermClass0);
		return (uint8_t *) &StorageTmp->sccpOm15MinLXUDTMessagesTermClass0;
	case SCCPOM15MINLXUDTMESSAGESTERMCLASS1:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinLXUDTMessagesTermClass1);
		return (uint8_t *) &StorageTmp->sccpOm15MinLXUDTMessagesTermClass1;
	case SCCPOM15MINMESSAGESSENTTOBACKUP:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinMessagesSentToBackup);
		return (uint8_t *) &StorageTmp->sccpOm15MinMessagesSentToBackup;
	case SCCPOM15MINDT1MESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinDT1MessagesReceived);
		return (uint8_t *) &StorageTmp->sccpOm15MinDT1MessagesReceived;
	case SCCPOM15MINDT1MESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinDT1MessagesSent);
		return (uint8_t *) &StorageTmp->sccpOm15MinDT1MessagesSent;
	case SCCPOM15MINDT2MESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinDT2MessagesReceived);
		return (uint8_t *) &StorageTmp->sccpOm15MinDT2MessagesReceived;
	case SCCPOM15MINDT2MESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinDT2MessagesSent);
		return (uint8_t *) &StorageTmp->sccpOm15MinDT2MessagesSent;
	case SCCPOM15MINEDMESSAGESSENT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinEDMessagesSent);
		return (uint8_t *) &StorageTmp->sccpOm15MinEDMessagesSent;
	case SCCPOM15MINEDMESSAGESRECEIVED:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->sccpOm15MinEDMessagesReceived);
		return (uint8_t *) &StorageTmp->sccpOm15MinEDMessagesReceived;

	default:
		ERROR_MSG("");
	}
	return NULL;
}

int
write_sccpOm1stAndIntervalActivate(int action, uint8_t *var_val, uint8_t var_val_type, size_t var_val_len, uint8_t *statP, oid * name, size_t name_len)
{
	static oid *tmpvar;
	struct sccpOmMIB_data *StorageTmp = NULL;
	static size_t tmplen;

	(void) tmplen;		/* not always used */
	DEBUGMSGTL(("sccpOmMIB", "write_sccpOm1stAndIntervalActivate entering action=%d...  \n", action));
	if ((StorageTmp = sccpOmMIBStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_OBJECT_ID) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sccpOm1stAndIntervalActivate not ASN_OBJECT_ID\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(oid *)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sccpOm1stAndIntervalActivate: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: no default */
		/* Note: ranges (none) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in objid for you to use, and you
				   have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		tmpvar = StorageTmp->sccpOm1stAndIntervalActivate;
		tmplen = StorageTmp->sccpOm1stAndIntervalActivateLen;
		memdup((void *) &StorageTmp->sccpOm1stAndIntervalActivate, var_val, var_val_len);
		StorageTmp->sccpOm1stAndIntervalActivateLen = var_val_len / sizeof(oid);
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		SNMP_FREE(StorageTmp->sccpOm1stAndIntervalActivate);
		StorageTmp->sccpOm1stAndIntervalActivate = tmpvar;
		StorageTmp->sccpOm1stAndIntervalActivateLen = tmplen;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(tmpvar);
		break;
	}
	return SNMP_ERR_NOERROR;
}

int
write_sccpOm1stAndIntervalDeactivate(int action, uint8_t *var_val, uint8_t var_val_type, size_t var_val_len, uint8_t *statP, oid * name, size_t name_len)
{
	static oid *tmpvar;
	struct sccpOmMIB_data *StorageTmp = NULL;
	static size_t tmplen;

	(void) tmplen;		/* not always used */
	DEBUGMSGTL(("sccpOmMIB", "write_sccpOm1stAndIntervalDeactivate entering action=%d...  \n", action));
	if ((StorageTmp = sccpOmMIBStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_OBJECT_ID) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sccpOm1stAndIntervalDeactivate not ASN_OBJECT_ID\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(oid *)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sccpOm1stAndIntervalDeactivate: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: no default */
		/* Note: ranges (none) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in objid for you to use, and you
				   have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		tmpvar = StorageTmp->sccpOm1stAndIntervalDeactivate;
		tmplen = StorageTmp->sccpOm1stAndIntervalDeactivateLen;
		memdup((void *) &StorageTmp->sccpOm1stAndIntervalDeactivate, var_val, var_val_len);
		StorageTmp->sccpOm1stAndIntervalDeactivateLen = var_val_len / sizeof(oid);
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		SNMP_FREE(StorageTmp->sccpOm1stAndIntervalDeactivate);
		StorageTmp->sccpOm1stAndIntervalDeactivate = tmpvar;
		StorageTmp->sccpOm1stAndIntervalDeactivateLen = tmplen;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(tmpvar);
		break;
	}
	return SNMP_ERR_NOERROR;
}

int
write_sccpOm5MinActivate(int action, uint8_t *var_val, uint8_t var_val_type, size_t var_val_len, uint8_t *statP, oid * name, size_t name_len)
{
	static oid *tmpvar;
	struct sccpOmMIB_data *StorageTmp = NULL;
	static size_t tmplen;

	(void) tmplen;		/* not always used */
	DEBUGMSGTL(("sccpOmMIB", "write_sccpOm5MinActivate entering action=%d...  \n", action));
	if ((StorageTmp = sccpOmMIBStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_OBJECT_ID) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sccpOm5MinActivate not ASN_OBJECT_ID\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(oid *)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sccpOm5MinActivate: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: no default */
		/* Note: ranges (none) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in objid for you to use, and you
				   have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		tmpvar = StorageTmp->sccpOm5MinActivate;
		tmplen = StorageTmp->sccpOm5MinActivateLen;
		memdup((void *) &StorageTmp->sccpOm5MinActivate, var_val, var_val_len);
		StorageTmp->sccpOm5MinActivateLen = var_val_len / sizeof(oid);
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		SNMP_FREE(StorageTmp->sccpOm5MinActivate);
		StorageTmp->sccpOm5MinActivate = tmpvar;
		StorageTmp->sccpOm5MinActivateLen = tmplen;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(tmpvar);
		break;
	}
	return SNMP_ERR_NOERROR;
}

int
write_sccpOm5MinDeaActivate(int action, uint8_t *var_val, uint8_t var_val_type, size_t var_val_len, uint8_t *statP, oid * name, size_t name_len)
{
	static oid *tmpvar;
	struct sccpOmMIB_data *StorageTmp = NULL;
	static size_t tmplen;

	(void) tmplen;		/* not always used */
	DEBUGMSGTL(("sccpOmMIB", "write_sccpOm5MinDeaActivate entering action=%d...  \n", action));
	if ((StorageTmp = sccpOmMIBStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_OBJECT_ID) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sccpOm5MinDeaActivate not ASN_OBJECT_ID\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(oid *)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sccpOm5MinDeaActivate: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: no default */
		/* Note: ranges (none) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in objid for you to use, and you
				   have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		tmpvar = StorageTmp->sccpOm5MinDeaActivate;
		tmplen = StorageTmp->sccpOm5MinDeaActivateLen;
		memdup((void *) &StorageTmp->sccpOm5MinDeaActivate, var_val, var_val_len);
		StorageTmp->sccpOm5MinDeaActivateLen = var_val_len / sizeof(oid);
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		SNMP_FREE(StorageTmp->sccpOm5MinDeaActivate);
		StorageTmp->sccpOm5MinDeaActivate = tmpvar;
		StorageTmp->sccpOm5MinDeaActivateLen = tmplen;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(tmpvar);
		break;
	}
	return SNMP_ERR_NOERROR;
}

int
write_sccpOm15MinActivate(int action, uint8_t *var_val, uint8_t var_val_type, size_t var_val_len, uint8_t *statP, oid * name, size_t name_len)
{
	static oid *tmpvar;
	struct sccpOmMIB_data *StorageTmp = NULL;
	static size_t tmplen;

	(void) tmplen;		/* not always used */
	DEBUGMSGTL(("sccpOmMIB", "write_sccpOm15MinActivate entering action=%d...  \n", action));
	if ((StorageTmp = sccpOmMIBStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_OBJECT_ID) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sccpOm15MinActivate not ASN_OBJECT_ID\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(oid *)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sccpOm15MinActivate: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: no default */
		/* Note: ranges (none) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in objid for you to use, and you
				   have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		tmpvar = StorageTmp->sccpOm15MinActivate;
		tmplen = StorageTmp->sccpOm15MinActivateLen;
		memdup((void *) &StorageTmp->sccpOm15MinActivate, var_val, var_val_len);
		StorageTmp->sccpOm15MinActivateLen = var_val_len / sizeof(oid);
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		SNMP_FREE(StorageTmp->sccpOm15MinActivate);
		StorageTmp->sccpOm15MinActivate = tmpvar;
		StorageTmp->sccpOm15MinActivateLen = tmplen;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(tmpvar);
		break;
	}
	return SNMP_ERR_NOERROR;
}

int
write_sccpOm15MinDeaActivate(int action, uint8_t *var_val, uint8_t var_val_type, size_t var_val_len, uint8_t *statP, oid * name, size_t name_len)
{
	static oid *tmpvar;
	struct sccpOmMIB_data *StorageTmp = NULL;
	static size_t tmplen;

	(void) tmplen;		/* not always used */
	DEBUGMSGTL(("sccpOmMIB", "write_sccpOm15MinDeaActivate entering action=%d...  \n", action));
	if ((StorageTmp = sccpOmMIBStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_OBJECT_ID) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sccpOm15MinDeaActivate not ASN_OBJECT_ID\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(oid *)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sccpOm15MinDeaActivate: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: no default */
		/* Note: ranges (none) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in objid for you to use, and you
				   have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		tmpvar = StorageTmp->sccpOm15MinDeaActivate;
		tmplen = StorageTmp->sccpOm15MinDeaActivateLen;
		memdup((void *) &StorageTmp->sccpOm15MinDeaActivate, var_val, var_val_len);
		StorageTmp->sccpOm15MinDeaActivateLen = var_val_len / sizeof(oid);
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		SNMP_FREE(StorageTmp->sccpOm15MinDeaActivate);
		StorageTmp->sccpOm15MinDeaActivate = tmpvar;
		StorageTmp->sccpOm15MinDeaActivateLen = tmplen;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(tmpvar);
		break;
	}
	return SNMP_ERR_NOERROR;
}

int
write_sccpOm5MinMaxIntervals(int action, uint8_t *var_val, uint8_t var_val_type, size_t var_val_len, uint8_t *statP, oid * name, size_t name_len)
{
	static uint32_t tmpvar;
	struct sccpOmMIB_data *StorageTmp = NULL;
	static size_t tmplen;

	(void) tmplen;		/* not always used */
	DEBUGMSGTL(("sccpOmMIB", "write_sccpOm5MinMaxIntervals entering action=%d...  \n", action));
	if ((StorageTmp = sccpOmMIBStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sccpOm5MinMaxIntervals not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sccpOm5MinMaxIntervals: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 96 */
		/* Note: ranges 0..288 */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in ulong_ret for you to use, and
				   you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		tmpvar = StorageTmp->sccpOm5MinMaxIntervals;
		StorageTmp->sccpOm5MinMaxIntervals = *((ulong *) var_val);
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->sccpOm5MinMaxIntervals = tmpvar;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */

		break;
	}
	return SNMP_ERR_NOERROR;
}

int
write_sccpOm15MinMaxIntervals(int action, uint8_t *var_val, uint8_t var_val_type, size_t var_val_len, uint8_t *statP, oid * name, size_t name_len)
{
	static uint32_t tmpvar;
	struct sccpOmMIB_data *StorageTmp = NULL;
	static size_t tmplen;

	(void) tmplen;		/* not always used */
	DEBUGMSGTL(("sccpOmMIB", "write_sccpOm15MinMaxIntervals entering action=%d...  \n", action));
	if ((StorageTmp = sccpOmMIBStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sccpOm15MinMaxIntervals not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sccpOm15MinMaxIntervals: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 96 */
		/* Note: ranges 0..672 */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in ulong_ret for you to use, and
				   you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		tmpvar = StorageTmp->sccpOm15MinMaxIntervals;
		StorageTmp->sccpOm15MinMaxIntervals = *((ulong *) var_val);
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->sccpOm15MinMaxIntervals = tmpvar;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */

		break;
	}
	return SNMP_ERR_NOERROR;
}

int
write_sccpOmpDefault(int action, uint8_t *var_val, uint8_t var_val_type, size_t var_val_len, uint8_t *statP, oid * name, size_t name_len)
{
	static int32_t tmpvar;
	struct sccpOmMIB_data *StorageTmp = NULL;
	static size_t tmplen;

	(void) tmplen;		/* not always used */
	DEBUGMSGTL(("sccpOmMIB", "write_sccpOmpDefault entering action=%d...  \n", action));
	if ((StorageTmp = sccpOmMIBStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sccpOmpDefault not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sccpOmpDefault: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 8 */
		/* Note: ranges (none) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in long_ret for you to use, and you 
				   have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		tmpvar = StorageTmp->sccpOmpDefault;
		StorageTmp->sccpOmpDefault = *((long *) var_val);
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->sccpOmpDefault = tmpvar;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */

		break;
	}
	return SNMP_ERR_NOERROR;
}

int
write_sccpOmNrOfSubLevelsDefault(int action, uint8_t *var_val, uint8_t var_val_type, size_t var_val_len, uint8_t *statP, oid * name, size_t name_len)
{
	static int32_t tmpvar;
	struct sccpOmMIB_data *StorageTmp = NULL;
	static size_t tmplen;

	(void) tmplen;		/* not always used */
	DEBUGMSGTL(("sccpOmMIB", "write_sccpOmNrOfSubLevelsDefault entering action=%d...  \n", action));
	if ((StorageTmp = sccpOmMIBStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sccpOmNrOfSubLevelsDefault not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sccpOmNrOfSubLevelsDefault: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 4 */
		/* Note: ranges (none) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in long_ret for you to use, and you 
				   have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		tmpvar = StorageTmp->sccpOmNrOfSubLevelsDefault;
		StorageTmp->sccpOmNrOfSubLevelsDefault = *((long *) var_val);
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->sccpOmNrOfSubLevelsDefault = tmpvar;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */

		break;
	}
	return SNMP_ERR_NOERROR;
}

int
write_sccpOmNrOfRestrictionLevelsDefault(int action, uint8_t *var_val, uint8_t var_val_type, size_t var_val_len, uint8_t *statP, oid * name, size_t name_len)
{
	static int32_t tmpvar;
	struct sccpOmMIB_data *StorageTmp = NULL;
	static size_t tmplen;

	(void) tmplen;		/* not always used */
	DEBUGMSGTL(("sccpOmMIB", "write_sccpOmNrOfRestrictionLevelsDefault entering action=%d...  \n", action));
	if ((StorageTmp = sccpOmMIBStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sccpOmNrOfRestrictionLevelsDefault not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sccpOmNrOfRestrictionLevelsDefault: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 8 */
		/* Note: ranges (none) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in long_ret for you to use, and you 
				   have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		tmpvar = StorageTmp->sccpOmNrOfRestrictionLevelsDefault;
		StorageTmp->sccpOmNrOfRestrictionLevelsDefault = *((long *) var_val);
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->sccpOmNrOfRestrictionLevelsDefault = tmpvar;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */

		break;
	}
	return SNMP_ERR_NOERROR;
}
