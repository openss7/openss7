/*****************************************************************************

 @(#) $RCSfile: x400pMIB.c,v $ $Name:  $($Revision: 0.9.2.3 $) $Date: 2008-12-27 15:29:48 $

 -----------------------------------------------------------------------------

 Copyright (c) 2001-2008  OpenSS7 Corporation <http://www.openss7.com/>
 Copyright (c) 1997-2000  Brian F. G. Bidulock <bidulock@openss7.org>

 All Rights Reserved.

 This program is free software: you can redistribute it and/or modify it under
 the terms of the GNU Affero General Public License as published by the Free
 Software Foundation, version 3 of the license.

 This program is distributed in the hope that it will be useful, but WITHOUT
 ANY WARRANTY; without even the implied warranty of MERCHANTABILITY or FITNESS
 FOR A PARTICULAR PURPOSE.  See the GNU Affero General Public License for more
 details.

 You should have received a copy of the GNU Affero General Public License
 along with this program.  If not, see <http://www.gnu.org/licenses/>, or
 write to the Free Software Foundation, Inc., 675 Mass Ave, Cambridge, MA
 02139, USA.

 -----------------------------------------------------------------------------

 U.S. GOVERNMENT RESTRICTED RIGHTS.  If you are licensing this Software on
 behalf of the U.S. Government ("Government"), the following provisions apply
 to you.  If the Software is supplied by the Department of Defense ("DoD"), it
 is classified as "Commercial Computer Software" under paragraph 252.227-7014
 of the DoD Supplement to the Federal Acquisition Regulations ("DFARS") (or any
 successor regulations) and the Government is acquiring only the license rights
 granted herein (the license rights customarily provided to non-Government
 users).  If the Software is supplied to any unit or agency of the Government
 other than DoD, it is classified as "Restricted Computer Software" and the
 Government's rights in the Software are defined in paragraph 52.227-19 of the
 Federal Acquisition Regulations ("FAR") (or any successor regulations) or, in
 the cases of NASA, in paragraph 18.52.227-86 of the NASA Supplement to the FAR
 (or any successor regulations).

 -----------------------------------------------------------------------------

 Commercial licensing and support of this software is available from OpenSS7
 Corporation at a fee.  See http://www.openss7.com/

 -----------------------------------------------------------------------------

 Last Modified $Date: 2008-12-27 15:29:48 $ by $Author: brian $

 -----------------------------------------------------------------------------

 $Log: x400pMIB.c,v $
 Revision 0.9.2.3  2008-12-27 15:29:48  brian
 - split MTP and MTP OM mib

 Revision 0.9.2.2  2008-12-24 13:08:26  brian
 - building ss7confd

 *****************************************************************************/

#ident "@(#) $RCSfile: x400pMIB.c,v $ $Name:  $($Revision: 0.9.2.3 $) $Date: 2008-12-27 15:29:48 $"

static char const ident[] = "$RCSfile: x400pMIB.c,v $ $Name:  $($Revision: 0.9.2.3 $) $Date: 2008-12-27 15:29:48 $";

/* This file was generated by mib2c and is intended for use as
   a mib module for the ucd-snmp snmpd agent. */
#include <ucd-snmp/ucd-snmp-config.h>
#include <ucd-snmp/ucd-snmp-includes.h>
#include <ucd-snmp/ucd-snmp-agent-includes.h>
#include <ucd-snmp/callback.h>
#include <ucd-snmp/snmp-tc.h>
#include <ucd-snmp/default_store.h>
#include <ucd-snmp/snmp_alarm.h>
/* The following header files are mangled in most recent net-snmp releases so
 * the versions from UCD-SNMP 4.2.5 are included here.  */
#if defined HAVE_LIBNETSNMP
#else				/* defined HAVE_LIBNETSNMP */
#endif				/* defined HAVE_LIBNETSNMP */
/* These are messed up on both. */
#include "ds_agent.h"
#ifdef HAVE_UCD_SNMP_UTIL_FUNCS_H
#include <ucd-snmp/util_funcs.h>
/* Many recent net-snmp UCD compatible headers do not declard header_generic. */
int header_generic(struct variable *, oid *, size_t *, int, size_t *, WriteMethod **);
#else				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#include "util_funcs.h"
#endif				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#ifdef HAVE_UCD_SNMP_HEADER_COMPLEX_H
#include <ucd-snmp/header_complex.h>
#else				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
#include "header_complex.h"
#endif				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
/* This one is the other way around: it is just fine for net-snmp, but
 * ucd-snmp does not provide the header file at all.  */
#ifdef HAVE_UCD_SNMP_MIB_MODULES_H
#include <ucd-snmp/mib_modules.h>
#else				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#ifdef HAVE_NET_SNMP_AGENT_MIB_MODULES_H
#include <net-snmp/agent/mib_modules.h>
#else				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#include "mib_modules.h"
#endif				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#endif				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#include <stdint.h>
#include <signal.h>
#include <sys/stat.h>		/* for struct stat, fstat() */
#include <sys/types.h>
#include <unistd.h>
#include <stdlib.h>
#include <pwd.h>		/* for getpwuid() getpwnam() */
#include <grp.h>		/* for getgrgid() getgrnam() */
#include <libgen.h>		/* for basename() */
#include <fcntl.h>		/* for O_CREAT */
#include <sys/sysctl.h>		/* for sysctl */
#include <errno.h>
#include <string.h>
#ifdef _GNU_SOURCE
#include <getopt.h>
#endif
#include "ss7MIB.h"
#include "x400pMIB.h"
#include "mtpMIB.h"
#include "mtpOmMIB.h"
#include "sccpMIB.h"
#include "sccpOmMIB.h"
extern const char sa_program[];

#define MY_FACILITY(__pri)	(LOG_DAEMON|(__pri))
#if !defined MODULE
extern int sa_dump;			/* default packet dump */
extern int sa_debug;			/* default no debug */
extern int sa_nomead;			/* default daemon mode */
extern int sa_output;			/* default normal output */
extern int sa_agentx;			/* default agentx mode */
extern int sa_alarms;			/* default application alarms */
extern int sa_fclose;			/* default close files between requests */
extern int sa_logaddr;			/* log addresses */
extern int sa_logfillog;		/* log to sa_logfile */
extern int sa_logstderr;		/* log to standard error */
extern int sa_logstdout;		/* log to standard output */
extern int sa_logsyslog;		/* log to system logs */
extern int sa_logcallog;		/* log to callback logs */
extern int sa_appendlog;		/* append to log file without truncating */
extern char sa_logfile[256];
extern char sa_pidfile[256];
extern char sa_sysctlf[256];

/* file stream for log file */
extern FILE *stdlog;

/* file descriptor for MIB use */
extern int sa_fd;

/* indication to reread MIB configuration */
extern int sa_changed;

/* indications that statistics, the mib or its tables need to be refreshed */
extern int sa_stats_refresh;
#endif				/* !defined MODULE */
/* request number for per-request actions */
extern int sa_request;
volatile int x400pMIB_refresh = 1;
volatile int x400pSyncTable_refresh = 1;
volatile int x400pCardTable_refresh = 1;
volatile int x400pSpanTable_refresh = 1;
volatile int x400pChanTable_refresh = 1;

/*
 * x400pMIB_variables_oid:
 *   this is the top level oid that we want to register under.  This
 *   is essentially a prefix, with the suffix appearing in the
 *   variable below.
 */
oid x400pMIB_variables_oid[] = { 1, 3, 6, 1, 4, 1, 29591, 1, 400 };
oid x400pSyncTable_variables_oid[] = { 1, 3, 6, 1, 4, 1, 29591, 1, 400, 1, 1, 1, 1, 1 };
oid x400pCardTable_variables_oid[] = { 1, 3, 6, 1, 4, 1, 29591, 1, 400, 1, 1, 2, 2, 1 };

/*
 * variable7 x400pMIB_variables:
 *   this variable defines function callbacks and type return information
 *   for the x400pMIB mib section
 */
struct variable7 x400pMIB_variables[] = {
/*  magic number        , variable type , ro/rw , callback fn  , L, oidsuffix */
#define   X400PSYNCSPANID       (5 % 256)
	{X400PSYNCSPANID, ASN_OCTET_STR, RWRITE, var_x400pSyncTable, 6, {1, 1, 1, 1, 1, 3}},
#define   X400PSYNCROWSTATUS    (6 % 256)
	{X400PSYNCROWSTATUS, ASN_INTEGER, RWRITE, var_x400pSyncTable, 6, {1, 1, 1, 1, 1, 4}},
#define   X400PCARDNEXTINDEX    (7 % 256)
	{X400PCARDNEXTINDEX, ASN_UNSIGNED, RONLY, var_x400pMIB, 4, {1, 1, 2, 1}},
#define   X400PCARDTYPE         (11 % 256)
	{X400PCARDTYPE, ASN_INTEGER, RONLY, var_x400pCardTable, 6, {1, 1, 2, 2, 1, 2}},
#define   X400PCARDIDENTIFIER   (12 % 256)
	{X400PCARDIDENTIFIER, ASN_INTEGER, RONLY, var_x400pCardTable, 6, {1, 1, 2, 2, 1, 3}},
#define   X400PCARDREVISION     (13 % 256)
	{X400PCARDREVISION, ASN_OCTET_STR, RONLY, var_x400pCardTable, 6, {1, 1, 2, 2, 1, 4}},
#define   X400PCARDCHIPTYPE     (14 % 256)
	{X400PCARDCHIPTYPE, ASN_INTEGER, RONLY, var_x400pCardTable, 6, {1, 1, 2, 2, 1, 5}},
#define   X400PCARDCHIPREVISION  (15 % 256)
	{X400PCARDCHIPREVISION, ASN_OCTET_STR, RONLY, var_x400pCardTable, 6, {1, 1, 2, 2, 1, 6}},
#define   X400PCARDPCIBUS       (16 % 256)
	{X400PCARDPCIBUS, ASN_INTEGER, RONLY, var_x400pCardTable, 6, {1, 1, 2, 2, 1, 7}},
#define   X400PCARDPCISLOT      (17 % 256)
	{X400PCARDPCISLOT, ASN_INTEGER, RONLY, var_x400pCardTable, 6, {1, 1, 2, 2, 1, 8}},
#define   X400PCARDPCIIRQ       (18 % 256)
	{X400PCARDPCIIRQ, ASN_INTEGER, RONLY, var_x400pCardTable, 6, {1, 1, 2, 2, 1, 9}},
#define   X400PCARDSPANTYPE     (19 % 256)
	{X400PCARDSPANTYPE, ASN_INTEGER, RWRITE, var_x400pCardTable, 6, {1, 1, 2, 2, 1, 10}},
#define   X400PCARDMODE         (20 % 256)
	{X400PCARDMODE, ASN_BIT_STR, RWRITE, var_x400pCardTable, 6, {1, 1, 2, 2, 1, 11}},
#define   X400PCARDSYNCMASTER   (21 % 256)
	{X400PCARDSYNCMASTER, ASN_INTEGER, RWRITE, var_x400pCardTable, 6, {1, 1, 2, 2, 1, 12}},
#define   X400PCARDSYNCSOURCE   (22 % 256)
	{X400PCARDSYNCSOURCE, ASN_INTEGER, RWRITE, var_x400pCardTable, 6, {1, 1, 2, 2, 1, 13}},
#define   X400PCARDSYNCGROUP    (23 % 256)
	{X400PCARDSYNCGROUP, ASN_INTEGER, RWRITE, var_x400pCardTable, 6, {1, 1, 2, 2, 1, 14}},
#define   X400PCARDADMINISTRATIVESTATE  (24 % 256)
	{X400PCARDADMINISTRATIVESTATE, ASN_INTEGER, RWRITE, var_x400pCardTable, 6, {1, 1, 2, 2, 1, 15}},
#define   X400PCARDOPERATIONALSTATE  (25 % 256)
	{X400PCARDOPERATIONALSTATE, ASN_INTEGER, RONLY, var_x400pCardTable, 6, {1, 1, 2, 2, 1, 16}},
#define   X400PCARDUSAGESTATE   (26 % 256)
	{X400PCARDUSAGESTATE, ASN_INTEGER, RONLY, var_x400pCardTable, 6, {1, 1, 2, 2, 1, 17}},
#define   X400PCARDALARMSTATUS  (27 % 256)
	{X400PCARDALARMSTATUS, ASN_BIT_STR, RWRITE, var_x400pCardTable, 6, {1, 1, 2, 2, 1, 18}},
#define   X400PCARDPROCEDURALSTATUS  (28 % 256)
	{X400PCARDPROCEDURALSTATUS, ASN_BIT_STR, RONLY, var_x400pCardTable, 6, {1, 1, 2, 2, 1, 19}},
#define   X400PCARDAVAILABILITYSTATUS  (29 % 256)
	{X400PCARDAVAILABILITYSTATUS, ASN_BIT_STR, RONLY, var_x400pCardTable, 6, {1, 1, 2, 2, 1, 20}},
#define   X400PCARDCONTROLSTATUS  (30 % 256)
	{X400PCARDCONTROLSTATUS, ASN_BIT_STR, RWRITE, var_x400pCardTable, 6, {1, 1, 2, 2, 1, 21}},
#define   X400PCARDUNKNOWNSTATUS  (31 % 256)
	{X400PCARDUNKNOWNSTATUS, ASN_INTEGER, RONLY, var_x400pCardTable, 6, {1, 1, 2, 2, 1, 22}},
#define   X400PCARDSTANDBYSTATUS  (32 % 256)
	{X400PCARDSTANDBYSTATUS, ASN_INTEGER, RONLY, var_x400pCardTable, 6, {1, 1, 2, 2, 1, 23}},
#define   X400PCARDSYNCSPANID   (33 % 256)
	{X400PCARDSYNCSPANID, ASN_OCTET_STR, RONLY, var_x400pCardTable, 6, {1, 1, 2, 2, 1, 24}},
#define   X400PCARDSYNCTRANSITIONS  (34 % 256)
	{X400PCARDSYNCTRANSITIONS, ASN_COUNTER, RONLY, var_x400pCardTable, 6, {1, 1, 2, 2, 1, 25}},
#define   X400PCARDNAME         (35 % 256)
	{X400PCARDNAME, ASN_OCTET_STR, RONLY, var_x400pCardTable, 6, {1, 1, 2, 2, 1, 26}},
#define   X400PCARDSTATUS       (36 % 256)
	{X400PCARDSTATUS, ASN_INTEGER, RONLY, var_x400pCardTable, 6, {1, 1, 2, 2, 1, 27}},
#define   X400PSPANOPERATIONALSTATE  (41 % 256)
	{X400PSPANOPERATIONALSTATE, ASN_INTEGER, RONLY, var_x400pSpanTable, 6, {1, 1, 3, 1, 1, 2}},
#define   X400PSPANUSAGESTATE   (42 % 256)
	{X400PSPANUSAGESTATE, ASN_INTEGER, RONLY, var_x400pSpanTable, 6, {1, 1, 3, 1, 1, 3}},
#define   X400PSPANADMINISTRATIVESTATE  (43 % 256)
	{X400PSPANADMINISTRATIVESTATE, ASN_INTEGER, RWRITE, var_x400pSpanTable, 6, {1, 1, 3, 1, 1, 4}},
#define   X400PSPANALARMSTATUS  (44 % 256)
	{X400PSPANALARMSTATUS, ASN_BIT_STR, RWRITE, var_x400pSpanTable, 6, {1, 1, 3, 1, 1, 5}},
#define   X400PSPANPROCEDURALSTATUS  (45 % 256)
	{X400PSPANPROCEDURALSTATUS, ASN_BIT_STR, RONLY, var_x400pSpanTable, 6, {1, 1, 3, 1, 1, 6}},
#define   X400PSPANAVAILABILITYSTATUS  (46 % 256)
	{X400PSPANAVAILABILITYSTATUS, ASN_BIT_STR, RONLY, var_x400pSpanTable, 6, {1, 1, 3, 1, 1, 7}},
#define   X400PSPANCONTROLSTATUS  (47 % 256)
	{X400PSPANCONTROLSTATUS, ASN_BIT_STR, RONLY, var_x400pSpanTable, 6, {1, 1, 3, 1, 1, 8}},
#define   X400PSPANSTANDBYSTATUS  (48 % 256)
	{X400PSPANSTANDBYSTATUS, ASN_INTEGER, RONLY, var_x400pSpanTable, 6, {1, 1, 3, 1, 1, 9}},
#define   X400PSPANUNKNOWNSTATUS  (49 % 256)
	{X400PSPANUNKNOWNSTATUS, ASN_INTEGER, RONLY, var_x400pSpanTable, 6, {1, 1, 3, 1, 1, 10}},
#define   X400PSPANTYPE         (50 % 256)
	{X400PSPANTYPE, ASN_INTEGER, RWRITE, var_x400pSpanTable, 6, {1, 1, 3, 1, 1, 11}},
#define   X400PSPANRATE         (51 % 256)
	{X400PSPANRATE, ASN_INTEGER, RONLY, var_x400pSpanTable, 6, {1, 1, 3, 1, 1, 12}},
#define   X400PSPANMODE         (52 % 256)
	{X400PSPANMODE, ASN_INTEGER, RONLY, var_x400pSpanTable, 6, {1, 1, 3, 1, 1, 13}},
#define   X400PSPANCRC          (53 % 256)
	{X400PSPANCRC, ASN_INTEGER, RWRITE, var_x400pSpanTable, 6, {1, 1, 3, 1, 1, 14}},
#define   X400PSPANCLOCKING     (54 % 256)
	{X400PSPANCLOCKING, ASN_INTEGER, RWRITE, var_x400pSpanTable, 6, {1, 1, 3, 1, 1, 15}},
#define   X400PSPANCODING       (55 % 256)
	{X400PSPANCODING, ASN_INTEGER, RWRITE, var_x400pSpanTable, 6, {1, 1, 3, 1, 1, 16}},
#define   X400PSPANFRAMING      (56 % 256)
	{X400PSPANFRAMING, ASN_INTEGER, RWRITE, var_x400pSpanTable, 6, {1, 1, 3, 1, 1, 17}},
#define   X400PSPANLINEBUILDOUT  (57 % 256)
	{X400PSPANLINEBUILDOUT, ASN_INTEGER, RWRITE, var_x400pSpanTable, 6, {1, 1, 3, 1, 1, 18}},
#define   X400PSPANTXLEVEL      (58 % 256)
	{X400PSPANTXLEVEL, ASN_INTEGER, RWRITE, var_x400pSpanTable, 6, {1, 1, 3, 1, 1, 19}},
#define   X400PSPANRXLEVEL      (59 % 256)
	{X400PSPANRXLEVEL, ASN_INTEGER, RWRITE, var_x400pSpanTable, 6, {1, 1, 3, 1, 1, 20}},
#define   X400PCHANOPERATIONALSTATE  (65 % 256)
	{X400PCHANOPERATIONALSTATE, ASN_INTEGER, RONLY, var_x400pChanTable, 6, {1, 1, 4, 1, 1, 2}},
#define   X400PCHANUSAGESTATE   (66 % 256)
	{X400PCHANUSAGESTATE, ASN_INTEGER, RONLY, var_x400pChanTable, 6, {1, 1, 4, 1, 1, 3}},
#define   X400PCHANADMINISTATIVESTATE  (67 % 256)
	{X400PCHANADMINISTATIVESTATE, ASN_INTEGER, RWRITE, var_x400pChanTable, 6, {1, 1, 4, 1, 1, 4}},
#define   X400PCHANALARMSTATUS  (68 % 256)
	{X400PCHANALARMSTATUS, ASN_BIT_STR, RWRITE, var_x400pChanTable, 6, {1, 1, 4, 1, 1, 5}},
#define   X400PCHANPROCEDURALSTATUS  (69 % 256)
	{X400PCHANPROCEDURALSTATUS, ASN_BIT_STR, RONLY, var_x400pChanTable, 6, {1, 1, 4, 1, 1, 6}},
#define   X400PCHANAVAILABILITYSTATUS  (70 % 256)
	{X400PCHANAVAILABILITYSTATUS, ASN_BIT_STR, RONLY, var_x400pChanTable, 6, {1, 1, 4, 1, 1, 7}},
#define   X400PCHANCONTROLSTATUS  (71 % 256)
	{X400PCHANCONTROLSTATUS, ASN_BIT_STR, RONLY, var_x400pChanTable, 6, {1, 1, 4, 1, 1, 8}},
#define   X400PCHANSTANDBYSTATUS  (72 % 256)
	{X400PCHANSTANDBYSTATUS, ASN_INTEGER, RONLY, var_x400pChanTable, 6, {1, 1, 4, 1, 1, 9}},
#define   X400PCHANTYPE         (73 % 256)
	{X400PCHANTYPE, ASN_INTEGER, RWRITE, var_x400pChanTable, 6, {1, 1, 4, 1, 1, 10}},
#define   X400PCHANRATE         (74 % 256)
	{X400PCHANRATE, ASN_INTEGER, RWRITE, var_x400pChanTable, 6, {1, 1, 4, 1, 1, 11}},
#define   X400PCHANMODE         (75 % 256)
	{X400PCHANMODE, ASN_INTEGER, RWRITE, var_x400pChanTable, 6, {1, 1, 4, 1, 1, 12}},

};

/*    (L = length of the oidsuffix) */
struct x400pMIB_data *x400pMIBStorage = NULL;

/* global storage of our data, saved in and configured by header_complex() */
struct header_complex_index *x400pSyncTableStorage = NULL;
struct header_complex_index *x400pCardTableStorage = NULL;
struct header_complex_index *x400pSpanTableStorage = NULL;
struct header_complex_index *x400pChanTableStorage = NULL;

/*
 * init_x400pMIB():
 *   Initialization routine.  This is called when the agent starts up.
 *   At a minimum, registration of your variables should take place here.
 */
void
init_x400pMIB(void)
{
	DEBUGMSGTL(("x400pMIB", "initializing...  "));
	/* register ourselves with the agent to handle our mib tree */
	REGISTER_MIB("x400pMIB", x400pMIB_variables, variable7, x400pMIB_variables_oid);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_SHUTDOWN, term_x400pMIB, NULL);
	/* register our config handler(s) to deal with registrations */
	snmpd_register_config_handler("x400pMIB", parse_x400pMIB, NULL, "HELP STRING");
	snmpd_register_config_handler("x400pSyncTable", parse_x400pSyncTable, NULL, "HELP STRING");
	snmpd_register_config_handler("x400pCardTable", parse_x400pCardTable, NULL, "HELP STRING");
	snmpd_register_config_handler("x400pSpanTable", parse_x400pSpanTable, NULL, "HELP STRING");
	snmpd_register_config_handler("x400pChanTable", parse_x400pChanTable, NULL, "HELP STRING");

	/* we need to be called back later to store our data */
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_x400pMIB, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_x400pSyncTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_x400pCardTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_x400pSpanTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_x400pChanTable, NULL);

	/* place any other initialization junk you need here */
	DEBUGMSGTL(("x400pMIB", "done.\n"));
}

/*
 * deinit_x400pMIB():
 *   Deinitialization routine.  This is called before the agent is unloaded.
 *   At a minimum, deregistration of your variables should take place here.
 */
void
deinit_x400pMIB(void)
{
	DEBUGMSGTL(("x400pMIB", "deinitializating...  "));
	unregister_mib(x400pMIB_variables_oid, sizeof(x400pMIB_variables_oid) / sizeof(oid));
	snmpd_unregister_config_handler("x400pMIB");
	snmpd_unregister_config_handler("x400pSyncTable");
	snmpd_unregister_config_handler("x400pCardTable");
	snmpd_unregister_config_handler("x400pSpanTable");
	snmpd_unregister_config_handler("x400pChanTable");

	/* place any other de-initialization junk you need here */
	DEBUGMSGTL(("x400pMIB", "done.\n"));
}

int
term_x400pMIB(int majorID, int minorID, void *serverarg, void *clientarg)
{
	deinit_x400pMIB();
	return 0;
}

/*
 * x400pMIB_add(): adds a structure node for scalars
 */
int
x400pMIB_add(struct x400pMIB_data *thedata)
{
	DEBUGMSGTL(("x400pMIB", "adding data...  "));
	x400pMIBStorage = thedata;
	DEBUGMSGTL(("x400pMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/*
 * parse_x400pMIB():
 *   parses .conf file entries needed to configure the mib.
 */
void
parse_x400pMIB(const char *token, char *line)
{
	size_t tmpsize;
	struct x400pMIB_data *StorageTmp = SNMP_MALLOC_STRUCT(x400pMIB_data);

	DEBUGMSGTL(("x400pMIB", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->x400pCardNextIndex, &tmpsize);

	x400pMIB_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("x400pMIB", "done.\n"));
}

/*
 * store_x400pMIB():
 *   stores .conf file entries needed to configure the mib.
 */
int
store_x400pMIB(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct x400pMIB_data *StorageTmp;

	DEBUGMSGTL(("x400pMIB", "storing data...  "));
	refresh_x400pMIB();
	if ((StorageTmp = x400pMIBStorage) == NULL) {
		DEBUGMSGTL(("x400pMIB", "error.\n"));
		return SNMPERR_GENERR;
	}
	(void) tmpsize;
	/* XXX: if (StorageTmp->x400pMIBStorageType == ST_NONVOLATILE) { */
	memset(line, 0, sizeof(line));
	strcat(line, "x400pMIB ");
	cptr = line + strlen(line);
	cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->x400pCardNextIndex, &tmpsize);

	snmpd_store_config(line);
	/* } */
	DEBUGMSGTL(("x400pMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * refresh_x400pMIB: refresh x400pMIB scalars
 *
 * Normally, the values retrieved from the operating system are cached.
 * When the agent receives a SIGPOLL from an open STREAMS configuration
 * or administrative driver Stream, the STREAMS subsystem indicates to
 * the agent that the cache has been invalidated and that it should
 * reread scalars and tables from the STREAMS susbystem.  This function
 * is used when the agent starts for the first time, or after a SIGPOLL
 * has been received (and a scalar has been requested).
 */
void
refresh_x400pMIB(void)
{
	if (x400pMIBStorage == NULL) {
		struct x400pMIB_data *StorageNew;

		if ((StorageNew = SNMP_MALLOC_STRUCT(x400pMIB_data)) == NULL)
			return;
		/* Update scalar defaults as required here... */

		x400pMIBStorage = StorageNew;
		x400pMIB_refresh = 1;
	}
	if (x400pMIB_refresh == 0)
		return;
	x400pMIB_refresh = 0;
	/* Update scalars as required here... */
}

/*
 * var_x400pMIB():
 *   This function is called every time the agent gets a request for
 *   a scalar variable that might be found within your mib section
 *   registered above.  It is up to you to do the right thing and
 *   return the correct value.
 *     You should also correct the value of "var_len" if necessary.
 *
 *   Please see the documentation for more information about writing
 *   module extensions, and check out the examples in the examples
 *   and mibII directories.
 */
uint8_t *
var_x400pMIB(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct x400pMIB_data *StorageTmp;

	if (header_generic(vp, name, length, exact, var_len, write_method)
	    == MATCH_FAILED)
		return NULL;
	/* Refresh the MIB values if required. */
	refresh_x400pMIB();
	if ((StorageTmp = x400pMIBStorage) == NULL)
		return NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {

	case X400PCARDNEXTINDEX:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->x400pCardNextIndex);
		return (uint8_t *) &StorageTmp->x400pCardNextIndex;

	default:
		ERROR_MSG("");
	}
	return NULL;
}

/*
 * x400pSyncTable_add(): adds a structure node to our data set
 */
int
x400pSyncTable_add(struct x400pSyncTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("x400pSyncTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index
	   the data */
	/* x400pSyncGroup */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (uint8_t *) &thedata->x400pSyncGroup, sizeof(thedata->x400pSyncGroup));
	/* x400pSyncIndex */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (uint8_t *) &thedata->x400pSyncIndex, sizeof(thedata->x400pSyncIndex));

	header_complex_add_data(&x400pSyncTableStorage, vars, thedata);
	DEBUGMSGTL(("x400pSyncTable", "registered an entry\n"));
	DEBUGMSGTL(("x400pSyncTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/*
 * parse_x400pSyncTable():
 *   parses .conf file entries needed to configure the mib.
 */
void
parse_x400pSyncTable(const char *token, char *line)
{
	size_t tmpsize;
	struct x400pSyncTable_data *StorageTmp = SNMP_MALLOC_STRUCT(x400pSyncTable_data);

	DEBUGMSGTL(("x400pSyncTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pSyncGroup, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pSyncIndex, &tmpsize);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->x400pSyncSpanId, &tmpsize);
	StorageTmp->x400pSyncSpanIdLen = tmpsize;
	if (StorageTmp->x400pSyncSpanId == NULL) {
		config_perror("invalid specification for x400pSyncSpanId");
		return;
	}

	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pSyncRowStatus, &tmpsize);

	x400pSyncTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("x400pSyncTable", "done.\n"));
}

/*
 * store_x400pSyncTable():
 *   stores .conf file entries needed to configure the mib.
 */
int
store_x400pSyncTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct x400pSyncTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("x400pSyncTable", "storing data...  "));
	refresh_x400pSyncTable();
	(void) tmpsize;
	for (hcindex = x400pSyncTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct x400pSyncTable_data *) hcindex->data;
/*   XXX:  if (StorageTmp->x400pSyncTableStorageType == ST_NONVOLATILE) { */
		memset(line, 0, sizeof(line));
		strcat(line, "x400pSyncTable ");
		cptr = line + strlen(line);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pSyncGroup, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pSyncIndex, &tmpsize);
		tmpsize = StorageTmp->x400pSyncSpanIdLen;
		cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->x400pSyncSpanId, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pSyncRowStatus, &tmpsize);

		snmpd_store_config(line);
/*   } */
	}
	DEBUGMSGTL(("x400pSyncTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/*
 * x400pCardTable_add(): adds a structure node to our data set
 */
int
x400pCardTable_add(struct x400pCardTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("x400pCardTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index
	   the data */
	/* x400pCardIndex */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (uint8_t *) &thedata->x400pCardIndex, sizeof(thedata->x400pCardIndex));

	header_complex_add_data(&x400pCardTableStorage, vars, thedata);
	DEBUGMSGTL(("x400pCardTable", "registered an entry\n"));
	DEBUGMSGTL(("x400pCardTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/*
 * parse_x400pCardTable():
 *   parses .conf file entries needed to configure the mib.
 */
void
parse_x400pCardTable(const char *token, char *line)
{
	size_t tmpsize;
	struct x400pCardTable_data *StorageTmp = SNMP_MALLOC_STRUCT(x400pCardTable_data);

	DEBUGMSGTL(("x400pCardTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pCardIndex, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pCardType, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pCardIdentifier, &tmpsize);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->x400pCardRevision, &tmpsize);
	StorageTmp->x400pCardRevisionLen = tmpsize;
	if (StorageTmp->x400pCardRevision == NULL) {
		config_perror("invalid specification for x400pCardRevision");
		return;
	}

	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pCardChipType, &tmpsize);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->x400pCardChipRevision, &tmpsize);
	StorageTmp->x400pCardChipRevisionLen = tmpsize;
	if (StorageTmp->x400pCardChipRevision == NULL) {
		config_perror("invalid specification for x400pCardChipRevision");
		return;
	}

	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pCardPciBus, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pCardPciSlot, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pCardPciIrq, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pCardSpanType, &tmpsize);
	line = read_config_read_data(ASN_BIT_STR, line, &StorageTmp->x400pCardMode, &tmpsize);
	StorageTmp->x400pCardModeLen = tmpsize;
	if (StorageTmp->x400pCardMode == NULL) {
		config_perror("invalid specification for x400pCardMode");
		return;
	}

	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pCardSyncMaster, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pCardSyncSource, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pCardSyncGroup, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pCardAdministrativeState, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pCardOperationalState, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pCardUsageState, &tmpsize);
	line = read_config_read_data(ASN_BIT_STR, line, &StorageTmp->x400pCardAlarmStatus, &tmpsize);
	StorageTmp->x400pCardAlarmStatusLen = tmpsize;
	if (StorageTmp->x400pCardAlarmStatus == NULL) {
		config_perror("invalid specification for x400pCardAlarmStatus");
		return;
	}

	line = read_config_read_data(ASN_BIT_STR, line, &StorageTmp->x400pCardProceduralStatus, &tmpsize);
	StorageTmp->x400pCardProceduralStatusLen = tmpsize;
	if (StorageTmp->x400pCardProceduralStatus == NULL) {
		config_perror("invalid specification for x400pCardProceduralStatus");
		return;
	}

	line = read_config_read_data(ASN_BIT_STR, line, &StorageTmp->x400pCardAvailabilityStatus, &tmpsize);
	StorageTmp->x400pCardAvailabilityStatusLen = tmpsize;
	if (StorageTmp->x400pCardAvailabilityStatus == NULL) {
		config_perror("invalid specification for x400pCardAvailabilityStatus");
		return;
	}

	line = read_config_read_data(ASN_BIT_STR, line, &StorageTmp->x400pCardControlStatus, &tmpsize);
	StorageTmp->x400pCardControlStatusLen = tmpsize;
	if (StorageTmp->x400pCardControlStatus == NULL) {
		config_perror("invalid specification for x400pCardControlStatus");
		return;
	}

	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pCardUnknownStatus, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pCardStandbyStatus, &tmpsize);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->x400pCardSyncSpanId, &tmpsize);
	StorageTmp->x400pCardSyncSpanIdLen = tmpsize;
	if (StorageTmp->x400pCardSyncSpanId == NULL) {
		config_perror("invalid specification for x400pCardSyncSpanId");
		return;
	}

	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->x400pCardSyncTransitions, &tmpsize);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->x400pCardName, &tmpsize);
	StorageTmp->x400pCardNameLen = tmpsize;
	if (StorageTmp->x400pCardName == NULL) {
		config_perror("invalid specification for x400pCardName");
		return;
	}

	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pCardStatus, &tmpsize);

	x400pCardTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("x400pCardTable", "done.\n"));
}

/*
 * store_x400pCardTable():
 *   stores .conf file entries needed to configure the mib.
 */
int
store_x400pCardTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct x400pCardTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("x400pCardTable", "storing data...  "));
	refresh_x400pCardTable();
	(void) tmpsize;
	for (hcindex = x400pCardTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct x400pCardTable_data *) hcindex->data;
/*   XXX:  if (StorageTmp->x400pCardTableStorageType == ST_NONVOLATILE) { */
		memset(line, 0, sizeof(line));
		strcat(line, "x400pCardTable ");
		cptr = line + strlen(line);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pCardIndex, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pCardType, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pCardIdentifier, &tmpsize);
		tmpsize = StorageTmp->x400pCardRevisionLen;
		cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->x400pCardRevision, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pCardChipType, &tmpsize);
		tmpsize = StorageTmp->x400pCardChipRevisionLen;
		cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->x400pCardChipRevision, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pCardPciBus, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pCardPciSlot, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pCardPciIrq, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pCardSpanType, &tmpsize);
		tmpsize = StorageTmp->x400pCardModeLen;
		cptr = read_config_store_data(ASN_BIT_STR, cptr, &StorageTmp->x400pCardMode, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pCardSyncMaster, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pCardSyncSource, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pCardSyncGroup, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pCardAdministrativeState, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pCardOperationalState, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pCardUsageState, &tmpsize);
		tmpsize = StorageTmp->x400pCardAlarmStatusLen;
		cptr = read_config_store_data(ASN_BIT_STR, cptr, &StorageTmp->x400pCardAlarmStatus, &tmpsize);
		tmpsize = StorageTmp->x400pCardProceduralStatusLen;
		cptr = read_config_store_data(ASN_BIT_STR, cptr, &StorageTmp->x400pCardProceduralStatus, &tmpsize);
		tmpsize = StorageTmp->x400pCardAvailabilityStatusLen;
		cptr = read_config_store_data(ASN_BIT_STR, cptr, &StorageTmp->x400pCardAvailabilityStatus, &tmpsize);
		tmpsize = StorageTmp->x400pCardControlStatusLen;
		cptr = read_config_store_data(ASN_BIT_STR, cptr, &StorageTmp->x400pCardControlStatus, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pCardUnknownStatus, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pCardStandbyStatus, &tmpsize);
		tmpsize = StorageTmp->x400pCardSyncSpanIdLen;
		cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->x400pCardSyncSpanId, &tmpsize);
		cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->x400pCardSyncTransitions, &tmpsize);
		tmpsize = StorageTmp->x400pCardNameLen;
		cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->x400pCardName, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pCardStatus, &tmpsize);

		snmpd_store_config(line);
/*   } */
	}
	DEBUGMSGTL(("x400pCardTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/*
 * x400pSpanTable_add(): adds a structure node to our data set
 */
int
x400pSpanTable_add(struct x400pSpanTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("x400pSpanTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index
	   the data */
	/* x400pCardIndex */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (uint8_t *) &thedata->x400pCardIndex, sizeof(thedata->x400pCardIndex));
	/* x400pSpanIndex */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (uint8_t *) &thedata->x400pSpanIndex, sizeof(thedata->x400pSpanIndex));

	header_complex_add_data(&x400pSpanTableStorage, vars, thedata);
	DEBUGMSGTL(("x400pSpanTable", "registered an entry\n"));
	DEBUGMSGTL(("x400pSpanTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/*
 * parse_x400pSpanTable():
 *   parses .conf file entries needed to configure the mib.
 */
void
parse_x400pSpanTable(const char *token, char *line)
{
	size_t tmpsize;
	struct x400pSpanTable_data *StorageTmp = SNMP_MALLOC_STRUCT(x400pSpanTable_data);

	DEBUGMSGTL(("x400pSpanTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pCardIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->x400pSpanIndex, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pSpanOperationalState, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pSpanUsageState, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pSpanAdministrativeState, &tmpsize);
	line = read_config_read_data(ASN_BIT_STR, line, &StorageTmp->x400pSpanAlarmStatus, &tmpsize);
	StorageTmp->x400pSpanAlarmStatusLen = tmpsize;
	if (StorageTmp->x400pSpanAlarmStatus == NULL) {
		config_perror("invalid specification for x400pSpanAlarmStatus");
		return;
	}

	line = read_config_read_data(ASN_BIT_STR, line, &StorageTmp->x400pSpanProceduralStatus, &tmpsize);
	StorageTmp->x400pSpanProceduralStatusLen = tmpsize;
	if (StorageTmp->x400pSpanProceduralStatus == NULL) {
		config_perror("invalid specification for x400pSpanProceduralStatus");
		return;
	}

	line = read_config_read_data(ASN_BIT_STR, line, &StorageTmp->x400pSpanAvailabilityStatus, &tmpsize);
	StorageTmp->x400pSpanAvailabilityStatusLen = tmpsize;
	if (StorageTmp->x400pSpanAvailabilityStatus == NULL) {
		config_perror("invalid specification for x400pSpanAvailabilityStatus");
		return;
	}

	line = read_config_read_data(ASN_BIT_STR, line, &StorageTmp->x400pSpanControlStatus, &tmpsize);
	StorageTmp->x400pSpanControlStatusLen = tmpsize;
	if (StorageTmp->x400pSpanControlStatus == NULL) {
		config_perror("invalid specification for x400pSpanControlStatus");
		return;
	}

	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pSpanStandbyStatus, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pSpanUnknownStatus, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pSpanType, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pSpanRate, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pSpanMode, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pSpanCrc, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pSpanClocking, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pSpanCoding, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pSpanFraming, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pSpanLineBuildout, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pSpanTxLevel, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pSpanRxLevel, &tmpsize);

	x400pSpanTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("x400pSpanTable", "done.\n"));
}

/*
 * store_x400pSpanTable():
 *   stores .conf file entries needed to configure the mib.
 */
int
store_x400pSpanTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct x400pSpanTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("x400pSpanTable", "storing data...  "));
	refresh_x400pSpanTable();
	(void) tmpsize;
	for (hcindex = x400pSpanTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct x400pSpanTable_data *) hcindex->data;
/*   XXX:  if (StorageTmp->x400pSpanTableStorageType == ST_NONVOLATILE) { */
		memset(line, 0, sizeof(line));
		strcat(line, "x400pSpanTable ");
		cptr = line + strlen(line);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pCardIndex, &tmpsize);
		cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->x400pSpanIndex, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pSpanOperationalState, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pSpanUsageState, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pSpanAdministrativeState, &tmpsize);
		tmpsize = StorageTmp->x400pSpanAlarmStatusLen;
		cptr = read_config_store_data(ASN_BIT_STR, cptr, &StorageTmp->x400pSpanAlarmStatus, &tmpsize);
		tmpsize = StorageTmp->x400pSpanProceduralStatusLen;
		cptr = read_config_store_data(ASN_BIT_STR, cptr, &StorageTmp->x400pSpanProceduralStatus, &tmpsize);
		tmpsize = StorageTmp->x400pSpanAvailabilityStatusLen;
		cptr = read_config_store_data(ASN_BIT_STR, cptr, &StorageTmp->x400pSpanAvailabilityStatus, &tmpsize);
		tmpsize = StorageTmp->x400pSpanControlStatusLen;
		cptr = read_config_store_data(ASN_BIT_STR, cptr, &StorageTmp->x400pSpanControlStatus, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pSpanStandbyStatus, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pSpanUnknownStatus, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pSpanType, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pSpanRate, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pSpanMode, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pSpanCrc, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pSpanClocking, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pSpanCoding, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pSpanFraming, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pSpanLineBuildout, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pSpanTxLevel, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pSpanRxLevel, &tmpsize);

		snmpd_store_config(line);
/*   } */
	}
	DEBUGMSGTL(("x400pSpanTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/*
 * x400pChanTable_add(): adds a structure node to our data set
 */
int
x400pChanTable_add(struct x400pChanTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("x400pChanTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index
	   the data */
	/* x400pCardIndex */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (uint8_t *) &thedata->x400pCardIndex, sizeof(thedata->x400pCardIndex));
	/* x400pSpanIndex */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (uint8_t *) &thedata->x400pSpanIndex, sizeof(thedata->x400pSpanIndex));
	/* x400pChanIndex */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (uint8_t *) &thedata->x400pChanIndex, sizeof(thedata->x400pChanIndex));

	header_complex_add_data(&x400pChanTableStorage, vars, thedata);
	DEBUGMSGTL(("x400pChanTable", "registered an entry\n"));
	DEBUGMSGTL(("x400pChanTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/*
 * parse_x400pChanTable():
 *   parses .conf file entries needed to configure the mib.
 */
void
parse_x400pChanTable(const char *token, char *line)
{
	size_t tmpsize;
	struct x400pChanTable_data *StorageTmp = SNMP_MALLOC_STRUCT(x400pChanTable_data);

	DEBUGMSGTL(("x400pChanTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pCardIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->x400pSpanIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->x400pChanIndex, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pChanOperationalState, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pChanUsageState, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pChanAdministativeState, &tmpsize);
	line = read_config_read_data(ASN_BIT_STR, line, &StorageTmp->x400pChanAlarmStatus, &tmpsize);
	StorageTmp->x400pChanAlarmStatusLen = tmpsize;
	if (StorageTmp->x400pChanAlarmStatus == NULL) {
		config_perror("invalid specification for x400pChanAlarmStatus");
		return;
	}

	line = read_config_read_data(ASN_BIT_STR, line, &StorageTmp->x400pChanProceduralStatus, &tmpsize);
	StorageTmp->x400pChanProceduralStatusLen = tmpsize;
	if (StorageTmp->x400pChanProceduralStatus == NULL) {
		config_perror("invalid specification for x400pChanProceduralStatus");
		return;
	}

	line = read_config_read_data(ASN_BIT_STR, line, &StorageTmp->x400pChanAvailabilityStatus, &tmpsize);
	StorageTmp->x400pChanAvailabilityStatusLen = tmpsize;
	if (StorageTmp->x400pChanAvailabilityStatus == NULL) {
		config_perror("invalid specification for x400pChanAvailabilityStatus");
		return;
	}

	line = read_config_read_data(ASN_BIT_STR, line, &StorageTmp->x400pChanControlStatus, &tmpsize);
	StorageTmp->x400pChanControlStatusLen = tmpsize;
	if (StorageTmp->x400pChanControlStatus == NULL) {
		config_perror("invalid specification for x400pChanControlStatus");
		return;
	}

	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pChanStandbyStatus, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pChanType, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pChanRate, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pChanMode, &tmpsize);

	x400pChanTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("x400pChanTable", "done.\n"));
}

/*
 * store_x400pChanTable():
 *   stores .conf file entries needed to configure the mib.
 */
int
store_x400pChanTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct x400pChanTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("x400pChanTable", "storing data...  "));
	refresh_x400pChanTable();
	(void) tmpsize;
	for (hcindex = x400pChanTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct x400pChanTable_data *) hcindex->data;
/*   XXX:  if (StorageTmp->x400pChanTableStorageType == ST_NONVOLATILE) { */
		memset(line, 0, sizeof(line));
		strcat(line, "x400pChanTable ");
		cptr = line + strlen(line);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pCardIndex, &tmpsize);
		cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->x400pSpanIndex, &tmpsize);
		cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->x400pChanIndex, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pChanOperationalState, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pChanUsageState, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pChanAdministativeState, &tmpsize);
		tmpsize = StorageTmp->x400pChanAlarmStatusLen;
		cptr = read_config_store_data(ASN_BIT_STR, cptr, &StorageTmp->x400pChanAlarmStatus, &tmpsize);
		tmpsize = StorageTmp->x400pChanProceduralStatusLen;
		cptr = read_config_store_data(ASN_BIT_STR, cptr, &StorageTmp->x400pChanProceduralStatus, &tmpsize);
		tmpsize = StorageTmp->x400pChanAvailabilityStatusLen;
		cptr = read_config_store_data(ASN_BIT_STR, cptr, &StorageTmp->x400pChanAvailabilityStatus, &tmpsize);
		tmpsize = StorageTmp->x400pChanControlStatusLen;
		cptr = read_config_store_data(ASN_BIT_STR, cptr, &StorageTmp->x400pChanControlStatus, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pChanStandbyStatus, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pChanType, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pChanRate, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pChanMode, &tmpsize);

		snmpd_store_config(line);
/*   } */
	}
	DEBUGMSGTL(("x400pChanTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * refresh_x400pSyncTable(): refresh x400pSyncTable
 *
 * Normally the values retrieved from the operating system are cached.
 * When the agent receives a SIGPOLL from an open STREAMS configuration
 * or administrative driver Stream, the STREAMS subsystem indicates to
 * the agent that the cache has been invalidated and that it should
 * reread tables from the STREAMS subsystem.  This function is used when
 * the agent start for the first time, or after a SIGPOLL has been
 * received (and a row or column has been requested).
 */
void
refresh_x400pSyncTable(void)
{
	if (x400pSyncTable_refresh == 0)
		return;
	x400pSyncTable_refresh = 0;
	/* Here, update the table as required... */
}

/**
 * refresh_x400pSyncTable_row(): refresh x400pSyncTable row
 *
 * Normally the values retrieved from the operating system are cached.
 * However, if a row contains temporal values, such as statistics
 * counters, gauges, timestamps, or other transient columns, it may be
 * necessary to refresh the row on some other basis, but normally only
 * once per request.
 */
void
refresh_x400pSyncTable_row(struct x400pSyncTable_data *StorageTmp)
{
	if (StorageTmp->x400pSyncTable_request == sa_request)
		return;
	StorageTmp->x400pSyncTable_request = sa_request;
}

/*
 * var_x400pSyncTable():
 *   Handle this table separately from the scalar value case.
 *   The workings of this are basically the same as for var_x400pMIB above.
 */
uint8_t *
var_x400pSyncTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct x400pSyncTable_data *StorageTmp = NULL;

	DEBUGMSGTL(("x400pMIB", "var_x400pSyncTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the
	   header. */
	refresh_x400pSyncTable();
	/* This assumes you have registered all your data properly with header_complex_add()
	   somewhere before this. */
	if ((StorageTmp = header_complex(x400pSyncTableStorage, vp, name, length, exact, var_len, write_method)) == NULL)
		return NULL;
	refresh_x400pSyncTable_row(StorageTmp);
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {

	case X400PSYNCSPANID:
		*write_method = write_x400pSyncSpanId;
		*var_len = StorageTmp->x400pSyncSpanIdLen;
		return (uint8_t *) StorageTmp->x400pSyncSpanId;
	case X400PSYNCROWSTATUS:
		*write_method = write_x400pSyncRowStatus;
		*var_len = sizeof(StorageTmp->x400pSyncRowStatus);
		return (uint8_t *) &StorageTmp->x400pSyncRowStatus;

	default:
		ERROR_MSG("");
	}
	return NULL;
}

/**
 * refresh_x400pCardTable(): refresh x400pCardTable
 *
 * Normally the values retrieved from the operating system are cached.
 * When the agent receives a SIGPOLL from an open STREAMS configuration
 * or administrative driver Stream, the STREAMS subsystem indicates to
 * the agent that the cache has been invalidated and that it should
 * reread tables from the STREAMS subsystem.  This function is used when
 * the agent start for the first time, or after a SIGPOLL has been
 * received (and a row or column has been requested).
 */
void
refresh_x400pCardTable(void)
{
	if (x400pCardTable_refresh == 0)
		return;
	x400pCardTable_refresh = 0;
	/* Here, update the table as required... */
}

/**
 * refresh_x400pCardTable_row(): refresh x400pCardTable row
 *
 * Normally the values retrieved from the operating system are cached.
 * However, if a row contains temporal values, such as statistics
 * counters, gauges, timestamps, or other transient columns, it may be
 * necessary to refresh the row on some other basis, but normally only
 * once per request.
 */
void
refresh_x400pCardTable_row(struct x400pCardTable_data *StorageTmp)
{
	if (StorageTmp->x400pCardTable_request == sa_request)
		return;
	StorageTmp->x400pCardTable_request = sa_request;
}

/*
 * var_x400pCardTable():
 *   Handle this table separately from the scalar value case.
 *   The workings of this are basically the same as for var_x400pMIB above.
 */
uint8_t *
var_x400pCardTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct x400pCardTable_data *StorageTmp = NULL;

	DEBUGMSGTL(("x400pMIB", "var_x400pCardTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the
	   header. */
	refresh_x400pCardTable();
	/* This assumes you have registered all your data properly with header_complex_add()
	   somewhere before this. */
	if ((StorageTmp = header_complex(x400pCardTableStorage, vp, name, length, exact, var_len, write_method)) == NULL)
		return NULL;
	refresh_x400pCardTable_row(StorageTmp);
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {

	case X400PCARDTYPE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->x400pCardType);
		return (uint8_t *) &StorageTmp->x400pCardType;
	case X400PCARDIDENTIFIER:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->x400pCardIdentifier);
		return (uint8_t *) &StorageTmp->x400pCardIdentifier;
	case X400PCARDREVISION:
		*write_method = NULL;	/* read-only */
		*var_len = StorageTmp->x400pCardRevisionLen;
		return (uint8_t *) StorageTmp->x400pCardRevision;
	case X400PCARDCHIPTYPE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->x400pCardChipType);
		return (uint8_t *) &StorageTmp->x400pCardChipType;
	case X400PCARDCHIPREVISION:
		*write_method = NULL;	/* read-only */
		*var_len = StorageTmp->x400pCardChipRevisionLen;
		return (uint8_t *) StorageTmp->x400pCardChipRevision;
	case X400PCARDPCIBUS:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->x400pCardPciBus);
		return (uint8_t *) &StorageTmp->x400pCardPciBus;
	case X400PCARDPCISLOT:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->x400pCardPciSlot);
		return (uint8_t *) &StorageTmp->x400pCardPciSlot;
	case X400PCARDPCIIRQ:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->x400pCardPciIrq);
		return (uint8_t *) &StorageTmp->x400pCardPciIrq;
	case X400PCARDSPANTYPE:
		*write_method = write_x400pCardSpanType;
		*var_len = sizeof(StorageTmp->x400pCardSpanType);
		return (uint8_t *) &StorageTmp->x400pCardSpanType;
	case X400PCARDMODE:
		*write_method = write_x400pCardMode;
		*var_len = StorageTmp->x400pCardModeLen;
		return (uint8_t *) StorageTmp->x400pCardMode;
	case X400PCARDSYNCMASTER:
		*write_method = write_x400pCardSyncMaster;
		*var_len = sizeof(StorageTmp->x400pCardSyncMaster);
		return (uint8_t *) &StorageTmp->x400pCardSyncMaster;
	case X400PCARDSYNCSOURCE:
		*write_method = write_x400pCardSyncSource;
		*var_len = sizeof(StorageTmp->x400pCardSyncSource);
		return (uint8_t *) &StorageTmp->x400pCardSyncSource;
	case X400PCARDSYNCGROUP:
		*write_method = write_x400pCardSyncGroup;
		*var_len = sizeof(StorageTmp->x400pCardSyncGroup);
		return (uint8_t *) &StorageTmp->x400pCardSyncGroup;
	case X400PCARDADMINISTRATIVESTATE:
		*write_method = write_x400pCardAdministrativeState;
		*var_len = sizeof(StorageTmp->x400pCardAdministrativeState);
		return (uint8_t *) &StorageTmp->x400pCardAdministrativeState;
	case X400PCARDOPERATIONALSTATE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->x400pCardOperationalState);
		return (uint8_t *) &StorageTmp->x400pCardOperationalState;
	case X400PCARDUSAGESTATE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->x400pCardUsageState);
		return (uint8_t *) &StorageTmp->x400pCardUsageState;
	case X400PCARDALARMSTATUS:
		*write_method = write_x400pCardAlarmStatus;
		*var_len = StorageTmp->x400pCardAlarmStatusLen;
		return (uint8_t *) StorageTmp->x400pCardAlarmStatus;
	case X400PCARDPROCEDURALSTATUS:
		*write_method = NULL;	/* read-only */
		*var_len = StorageTmp->x400pCardProceduralStatusLen;
		return (uint8_t *) StorageTmp->x400pCardProceduralStatus;
	case X400PCARDAVAILABILITYSTATUS:
		*write_method = NULL;	/* read-only */
		*var_len = StorageTmp->x400pCardAvailabilityStatusLen;
		return (uint8_t *) StorageTmp->x400pCardAvailabilityStatus;
	case X400PCARDCONTROLSTATUS:
		*write_method = write_x400pCardControlStatus;
		*var_len = StorageTmp->x400pCardControlStatusLen;
		return (uint8_t *) StorageTmp->x400pCardControlStatus;
	case X400PCARDUNKNOWNSTATUS:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->x400pCardUnknownStatus);
		return (uint8_t *) &StorageTmp->x400pCardUnknownStatus;
	case X400PCARDSTANDBYSTATUS:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->x400pCardStandbyStatus);
		return (uint8_t *) &StorageTmp->x400pCardStandbyStatus;
	case X400PCARDSYNCSPANID:
		*write_method = NULL;	/* read-only */
		*var_len = StorageTmp->x400pCardSyncSpanIdLen;
		return (uint8_t *) StorageTmp->x400pCardSyncSpanId;
	case X400PCARDSYNCTRANSITIONS:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->x400pCardSyncTransitions);
		return (uint8_t *) &StorageTmp->x400pCardSyncTransitions;
	case X400PCARDNAME:
		*write_method = NULL;	/* read-only */
		*var_len = StorageTmp->x400pCardNameLen;
		return (uint8_t *) StorageTmp->x400pCardName;
	case X400PCARDSTATUS:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->x400pCardStatus);
		return (uint8_t *) &StorageTmp->x400pCardStatus;

	default:
		ERROR_MSG("");
	}
	return NULL;
}

/**
 * refresh_x400pSpanTable(): refresh x400pSpanTable
 *
 * Normally the values retrieved from the operating system are cached.
 * When the agent receives a SIGPOLL from an open STREAMS configuration
 * or administrative driver Stream, the STREAMS subsystem indicates to
 * the agent that the cache has been invalidated and that it should
 * reread tables from the STREAMS subsystem.  This function is used when
 * the agent start for the first time, or after a SIGPOLL has been
 * received (and a row or column has been requested).
 */
void
refresh_x400pSpanTable(void)
{
	if (x400pSpanTable_refresh == 0)
		return;
	x400pSpanTable_refresh = 0;
	/* Here, update the table as required... */
}

/**
 * refresh_x400pSpanTable_row(): refresh x400pSpanTable row
 *
 * Normally the values retrieved from the operating system are cached.
 * However, if a row contains temporal values, such as statistics
 * counters, gauges, timestamps, or other transient columns, it may be
 * necessary to refresh the row on some other basis, but normally only
 * once per request.
 */
void
refresh_x400pSpanTable_row(struct x400pSpanTable_data *StorageTmp)
{
	if (StorageTmp->x400pSpanTable_request == sa_request)
		return;
	StorageTmp->x400pSpanTable_request = sa_request;
}

/*
 * var_x400pSpanTable():
 *   Handle this table separately from the scalar value case.
 *   The workings of this are basically the same as for var_x400pMIB above.
 */
uint8_t *
var_x400pSpanTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct x400pSpanTable_data *StorageTmp = NULL;

	DEBUGMSGTL(("x400pMIB", "var_x400pSpanTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the
	   header. */
	refresh_x400pSpanTable();
	/* This assumes you have registered all your data properly with header_complex_add()
	   somewhere before this. */
	if ((StorageTmp = header_complex(x400pSpanTableStorage, vp, name, length, exact, var_len, write_method)) == NULL)
		return NULL;
	refresh_x400pSpanTable_row(StorageTmp);
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {

	case X400PSPANOPERATIONALSTATE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->x400pSpanOperationalState);
		return (uint8_t *) &StorageTmp->x400pSpanOperationalState;
	case X400PSPANUSAGESTATE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->x400pSpanUsageState);
		return (uint8_t *) &StorageTmp->x400pSpanUsageState;
	case X400PSPANADMINISTRATIVESTATE:
		*write_method = write_x400pSpanAdministrativeState;
		*var_len = sizeof(StorageTmp->x400pSpanAdministrativeState);
		return (uint8_t *) &StorageTmp->x400pSpanAdministrativeState;
	case X400PSPANALARMSTATUS:
		*write_method = write_x400pSpanAlarmStatus;
		*var_len = StorageTmp->x400pSpanAlarmStatusLen;
		return (uint8_t *) StorageTmp->x400pSpanAlarmStatus;
	case X400PSPANPROCEDURALSTATUS:
		*write_method = NULL;	/* read-only */
		*var_len = StorageTmp->x400pSpanProceduralStatusLen;
		return (uint8_t *) StorageTmp->x400pSpanProceduralStatus;
	case X400PSPANAVAILABILITYSTATUS:
		*write_method = NULL;	/* read-only */
		*var_len = StorageTmp->x400pSpanAvailabilityStatusLen;
		return (uint8_t *) StorageTmp->x400pSpanAvailabilityStatus;
	case X400PSPANCONTROLSTATUS:
		*write_method = NULL;	/* read-only */
		*var_len = StorageTmp->x400pSpanControlStatusLen;
		return (uint8_t *) StorageTmp->x400pSpanControlStatus;
	case X400PSPANSTANDBYSTATUS:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->x400pSpanStandbyStatus);
		return (uint8_t *) &StorageTmp->x400pSpanStandbyStatus;
	case X400PSPANUNKNOWNSTATUS:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->x400pSpanUnknownStatus);
		return (uint8_t *) &StorageTmp->x400pSpanUnknownStatus;
	case X400PSPANTYPE:
		*write_method = write_x400pSpanType;
		*var_len = sizeof(StorageTmp->x400pSpanType);
		return (uint8_t *) &StorageTmp->x400pSpanType;
	case X400PSPANRATE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->x400pSpanRate);
		return (uint8_t *) &StorageTmp->x400pSpanRate;
	case X400PSPANMODE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->x400pSpanMode);
		return (uint8_t *) &StorageTmp->x400pSpanMode;
	case X400PSPANCRC:
		*write_method = write_x400pSpanCrc;
		*var_len = sizeof(StorageTmp->x400pSpanCrc);
		return (uint8_t *) &StorageTmp->x400pSpanCrc;
	case X400PSPANCLOCKING:
		*write_method = write_x400pSpanClocking;
		*var_len = sizeof(StorageTmp->x400pSpanClocking);
		return (uint8_t *) &StorageTmp->x400pSpanClocking;
	case X400PSPANCODING:
		*write_method = write_x400pSpanCoding;
		*var_len = sizeof(StorageTmp->x400pSpanCoding);
		return (uint8_t *) &StorageTmp->x400pSpanCoding;
	case X400PSPANFRAMING:
		*write_method = write_x400pSpanFraming;
		*var_len = sizeof(StorageTmp->x400pSpanFraming);
		return (uint8_t *) &StorageTmp->x400pSpanFraming;
	case X400PSPANLINEBUILDOUT:
		*write_method = write_x400pSpanLineBuildout;
		*var_len = sizeof(StorageTmp->x400pSpanLineBuildout);
		return (uint8_t *) &StorageTmp->x400pSpanLineBuildout;
	case X400PSPANTXLEVEL:
		*write_method = write_x400pSpanTxLevel;
		*var_len = sizeof(StorageTmp->x400pSpanTxLevel);
		return (uint8_t *) &StorageTmp->x400pSpanTxLevel;
	case X400PSPANRXLEVEL:
		*write_method = write_x400pSpanRxLevel;
		*var_len = sizeof(StorageTmp->x400pSpanRxLevel);
		return (uint8_t *) &StorageTmp->x400pSpanRxLevel;

	default:
		ERROR_MSG("");
	}
	return NULL;
}

/**
 * refresh_x400pChanTable(): refresh x400pChanTable
 *
 * Normally the values retrieved from the operating system are cached.
 * When the agent receives a SIGPOLL from an open STREAMS configuration
 * or administrative driver Stream, the STREAMS subsystem indicates to
 * the agent that the cache has been invalidated and that it should
 * reread tables from the STREAMS subsystem.  This function is used when
 * the agent start for the first time, or after a SIGPOLL has been
 * received (and a row or column has been requested).
 */
void
refresh_x400pChanTable(void)
{
	if (x400pChanTable_refresh == 0)
		return;
	x400pChanTable_refresh = 0;
	/* Here, update the table as required... */
}

/**
 * refresh_x400pChanTable_row(): refresh x400pChanTable row
 *
 * Normally the values retrieved from the operating system are cached.
 * However, if a row contains temporal values, such as statistics
 * counters, gauges, timestamps, or other transient columns, it may be
 * necessary to refresh the row on some other basis, but normally only
 * once per request.
 */
void
refresh_x400pChanTable_row(struct x400pChanTable_data *StorageTmp)
{
	if (StorageTmp->x400pChanTable_request == sa_request)
		return;
	StorageTmp->x400pChanTable_request = sa_request;
}

/*
 * var_x400pChanTable():
 *   Handle this table separately from the scalar value case.
 *   The workings of this are basically the same as for var_x400pMIB above.
 */
uint8_t *
var_x400pChanTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct x400pChanTable_data *StorageTmp = NULL;

	DEBUGMSGTL(("x400pMIB", "var_x400pChanTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the
	   header. */
	refresh_x400pChanTable();
	/* This assumes you have registered all your data properly with header_complex_add()
	   somewhere before this. */
	if ((StorageTmp = header_complex(x400pChanTableStorage, vp, name, length, exact, var_len, write_method)) == NULL)
		return NULL;
	refresh_x400pChanTable_row(StorageTmp);
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {

	case X400PCHANOPERATIONALSTATE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->x400pChanOperationalState);
		return (uint8_t *) &StorageTmp->x400pChanOperationalState;
	case X400PCHANUSAGESTATE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->x400pChanUsageState);
		return (uint8_t *) &StorageTmp->x400pChanUsageState;
	case X400PCHANADMINISTATIVESTATE:
		*write_method = write_x400pChanAdministativeState;
		*var_len = sizeof(StorageTmp->x400pChanAdministativeState);
		return (uint8_t *) &StorageTmp->x400pChanAdministativeState;
	case X400PCHANALARMSTATUS:
		*write_method = write_x400pChanAlarmStatus;
		*var_len = StorageTmp->x400pChanAlarmStatusLen;
		return (uint8_t *) StorageTmp->x400pChanAlarmStatus;
	case X400PCHANPROCEDURALSTATUS:
		*write_method = NULL;	/* read-only */
		*var_len = StorageTmp->x400pChanProceduralStatusLen;
		return (uint8_t *) StorageTmp->x400pChanProceduralStatus;
	case X400PCHANAVAILABILITYSTATUS:
		*write_method = NULL;	/* read-only */
		*var_len = StorageTmp->x400pChanAvailabilityStatusLen;
		return (uint8_t *) StorageTmp->x400pChanAvailabilityStatus;
	case X400PCHANCONTROLSTATUS:
		*write_method = NULL;	/* read-only */
		*var_len = StorageTmp->x400pChanControlStatusLen;
		return (uint8_t *) StorageTmp->x400pChanControlStatus;
	case X400PCHANSTANDBYSTATUS:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->x400pChanStandbyStatus);
		return (uint8_t *) &StorageTmp->x400pChanStandbyStatus;
	case X400PCHANTYPE:
		*write_method = write_x400pChanType;
		*var_len = sizeof(StorageTmp->x400pChanType);
		return (uint8_t *) &StorageTmp->x400pChanType;
	case X400PCHANRATE:
		*write_method = write_x400pChanRate;
		*var_len = sizeof(StorageTmp->x400pChanRate);
		return (uint8_t *) &StorageTmp->x400pChanRate;
	case X400PCHANMODE:
		*write_method = write_x400pChanMode;
		*var_len = sizeof(StorageTmp->x400pChanMode);
		return (uint8_t *) &StorageTmp->x400pChanMode;

	default:
		ERROR_MSG("");
	}
	return NULL;
}

int
write_x400pSyncSpanId(int action, uint8_t *var_val, uint8_t var_val_type, size_t var_val_len, uint8_t *statP, oid * name, size_t name_len)
{
	static char *tmpvar;
	struct x400pSyncTable_data *StorageTmp = NULL;
	static size_t tmplen;
	size_t newlen = name_len - (sizeof(x400pMIB_variables_oid) / sizeof(oid) + 7 - 1);

	(void) tmplen;		/* not always used */
	DEBUGMSGTL(("x400pMIB", "write_x400pSyncSpanId entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(x400pSyncTableStorage, NULL, &name[sizeof(x400pMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSyncSpanId not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in string for you to use, and you
				   have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		tmpvar = StorageTmp->x400pSyncSpanId;
		tmplen = StorageTmp->x400pSyncSpanIdLen;
		memdup((void *) &StorageTmp->x400pSyncSpanId, var_val, var_val_len);
		StorageTmp->x400pSyncSpanIdLen = var_val_len;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		SNMP_FREE(StorageTmp->x400pSyncSpanId);
		StorageTmp->x400pSyncSpanId = tmpvar;
		StorageTmp->x400pSyncSpanIdLen = tmplen;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(tmpvar);
		break;
	}
	return SNMP_ERR_NOERROR;
}

int
write_x400pCardSpanType(int action, uint8_t *var_val, uint8_t var_val_type, size_t var_val_len, uint8_t *statP, oid * name, size_t name_len)
{
	static int32_t tmpvar;
	struct x400pCardTable_data *StorageTmp = NULL;
	static size_t tmplen;
	size_t newlen = name_len - (sizeof(x400pMIB_variables_oid) / sizeof(oid) + 7 - 1);

	(void) tmplen;		/* not always used */
	DEBUGMSGTL(("x400pMIB", "write_x400pCardSpanType entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(x400pCardTableStorage, NULL, &name[sizeof(x400pMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pCardSpanType not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in long_ret for you to use, and you 
				   have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		tmpvar = StorageTmp->x400pCardSpanType;
		StorageTmp->x400pCardSpanType = *((long *) var_val);
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->x400pCardSpanType = tmpvar;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */

		break;
	}
	return SNMP_ERR_NOERROR;
}

int
write_x400pCardMode(int action, uint8_t *var_val, uint8_t var_val_type, size_t var_val_len, uint8_t *statP, oid * name, size_t name_len)
{
	static char *tmpvar;
	struct x400pCardTable_data *StorageTmp = NULL;
	static size_t tmplen;
	size_t newlen = name_len - (sizeof(x400pMIB_variables_oid) / sizeof(oid) + 7 - 1);

	(void) tmplen;		/* not always used */
	DEBUGMSGTL(("x400pMIB", "write_x400pCardMode entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(x400pCardTableStorage, NULL, &name[sizeof(x400pMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_BIT_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pCardMode not ASN_BIT_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in string for you to use, and you
				   have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		tmpvar = StorageTmp->x400pCardMode;
		tmplen = StorageTmp->x400pCardModeLen;
		memdup((void *) &StorageTmp->x400pCardMode, var_val, var_val_len);
		StorageTmp->x400pCardModeLen = var_val_len;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		SNMP_FREE(StorageTmp->x400pCardMode);
		StorageTmp->x400pCardMode = tmpvar;
		StorageTmp->x400pCardModeLen = tmplen;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(tmpvar);
		break;
	}
	return SNMP_ERR_NOERROR;
}

int
write_x400pCardSyncMaster(int action, uint8_t *var_val, uint8_t var_val_type, size_t var_val_len, uint8_t *statP, oid * name, size_t name_len)
{
	static int32_t tmpvar;
	struct x400pCardTable_data *StorageTmp = NULL;
	static size_t tmplen;
	size_t newlen = name_len - (sizeof(x400pMIB_variables_oid) / sizeof(oid) + 7 - 1);

	(void) tmplen;		/* not always used */
	DEBUGMSGTL(("x400pMIB", "write_x400pCardSyncMaster entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(x400pCardTableStorage, NULL, &name[sizeof(x400pMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pCardSyncMaster not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in long_ret for you to use, and you 
				   have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		tmpvar = StorageTmp->x400pCardSyncMaster;
		StorageTmp->x400pCardSyncMaster = *((long *) var_val);
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->x400pCardSyncMaster = tmpvar;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */

		break;
	}
	return SNMP_ERR_NOERROR;
}

int
write_x400pCardSyncSource(int action, uint8_t *var_val, uint8_t var_val_type, size_t var_val_len, uint8_t *statP, oid * name, size_t name_len)
{
	static int32_t tmpvar;
	struct x400pCardTable_data *StorageTmp = NULL;
	static size_t tmplen;
	size_t newlen = name_len - (sizeof(x400pMIB_variables_oid) / sizeof(oid) + 7 - 1);

	(void) tmplen;		/* not always used */
	DEBUGMSGTL(("x400pMIB", "write_x400pCardSyncSource entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(x400pCardTableStorage, NULL, &name[sizeof(x400pMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pCardSyncSource not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in long_ret for you to use, and you 
				   have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		tmpvar = StorageTmp->x400pCardSyncSource;
		StorageTmp->x400pCardSyncSource = *((long *) var_val);
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->x400pCardSyncSource = tmpvar;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */

		break;
	}
	return SNMP_ERR_NOERROR;
}

int
write_x400pCardSyncGroup(int action, uint8_t *var_val, uint8_t var_val_type, size_t var_val_len, uint8_t *statP, oid * name, size_t name_len)
{
	static int32_t tmpvar;
	struct x400pCardTable_data *StorageTmp = NULL;
	static size_t tmplen;
	size_t newlen = name_len - (sizeof(x400pMIB_variables_oid) / sizeof(oid) + 7 - 1);

	(void) tmplen;		/* not always used */
	DEBUGMSGTL(("x400pMIB", "write_x400pCardSyncGroup entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(x400pCardTableStorage, NULL, &name[sizeof(x400pMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pCardSyncGroup not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in long_ret for you to use, and you 
				   have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		tmpvar = StorageTmp->x400pCardSyncGroup;
		StorageTmp->x400pCardSyncGroup = *((long *) var_val);
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->x400pCardSyncGroup = tmpvar;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */

		break;
	}
	return SNMP_ERR_NOERROR;
}

int
write_x400pCardAdministrativeState(int action, uint8_t *var_val, uint8_t var_val_type, size_t var_val_len, uint8_t *statP, oid * name, size_t name_len)
{
	static int32_t tmpvar;
	struct x400pCardTable_data *StorageTmp = NULL;
	static size_t tmplen;
	size_t newlen = name_len - (sizeof(x400pMIB_variables_oid) / sizeof(oid) + 7 - 1);

	(void) tmplen;		/* not always used */
	DEBUGMSGTL(("x400pMIB", "write_x400pCardAdministrativeState entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(x400pCardTableStorage, NULL, &name[sizeof(x400pMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pCardAdministrativeState not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in long_ret for you to use, and you 
				   have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		tmpvar = StorageTmp->x400pCardAdministrativeState;
		StorageTmp->x400pCardAdministrativeState = *((long *) var_val);
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->x400pCardAdministrativeState = tmpvar;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */

		break;
	}
	return SNMP_ERR_NOERROR;
}

int
write_x400pCardAlarmStatus(int action, uint8_t *var_val, uint8_t var_val_type, size_t var_val_len, uint8_t *statP, oid * name, size_t name_len)
{
	static char *tmpvar;
	struct x400pCardTable_data *StorageTmp = NULL;
	static size_t tmplen;
	size_t newlen = name_len - (sizeof(x400pMIB_variables_oid) / sizeof(oid) + 7 - 1);

	(void) tmplen;		/* not always used */
	DEBUGMSGTL(("x400pMIB", "write_x400pCardAlarmStatus entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(x400pCardTableStorage, NULL, &name[sizeof(x400pMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_BIT_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pCardAlarmStatus not ASN_BIT_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in string for you to use, and you
				   have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		tmpvar = StorageTmp->x400pCardAlarmStatus;
		tmplen = StorageTmp->x400pCardAlarmStatusLen;
		memdup((void *) &StorageTmp->x400pCardAlarmStatus, var_val, var_val_len);
		StorageTmp->x400pCardAlarmStatusLen = var_val_len;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		SNMP_FREE(StorageTmp->x400pCardAlarmStatus);
		StorageTmp->x400pCardAlarmStatus = tmpvar;
		StorageTmp->x400pCardAlarmStatusLen = tmplen;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(tmpvar);
		break;
	}
	return SNMP_ERR_NOERROR;
}

int
write_x400pCardControlStatus(int action, uint8_t *var_val, uint8_t var_val_type, size_t var_val_len, uint8_t *statP, oid * name, size_t name_len)
{
	static char *tmpvar;
	struct x400pCardTable_data *StorageTmp = NULL;
	static size_t tmplen;
	size_t newlen = name_len - (sizeof(x400pMIB_variables_oid) / sizeof(oid) + 7 - 1);

	(void) tmplen;		/* not always used */
	DEBUGMSGTL(("x400pMIB", "write_x400pCardControlStatus entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(x400pCardTableStorage, NULL, &name[sizeof(x400pMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_BIT_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pCardControlStatus not ASN_BIT_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in string for you to use, and you
				   have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		tmpvar = StorageTmp->x400pCardControlStatus;
		tmplen = StorageTmp->x400pCardControlStatusLen;
		memdup((void *) &StorageTmp->x400pCardControlStatus, var_val, var_val_len);
		StorageTmp->x400pCardControlStatusLen = var_val_len;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		SNMP_FREE(StorageTmp->x400pCardControlStatus);
		StorageTmp->x400pCardControlStatus = tmpvar;
		StorageTmp->x400pCardControlStatusLen = tmplen;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(tmpvar);
		break;
	}
	return SNMP_ERR_NOERROR;
}

int
write_x400pSpanAdministrativeState(int action, uint8_t *var_val, uint8_t var_val_type, size_t var_val_len, uint8_t *statP, oid * name, size_t name_len)
{
	static int32_t tmpvar;
	struct x400pSpanTable_data *StorageTmp = NULL;
	static size_t tmplen;
	size_t newlen = name_len - (sizeof(x400pMIB_variables_oid) / sizeof(oid) + 7 - 1);

	(void) tmplen;		/* not always used */
	DEBUGMSGTL(("x400pMIB", "write_x400pSpanAdministrativeState entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(x400pSpanTableStorage, NULL, &name[sizeof(x400pMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanAdministrativeState not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in long_ret for you to use, and you 
				   have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		tmpvar = StorageTmp->x400pSpanAdministrativeState;
		StorageTmp->x400pSpanAdministrativeState = *((long *) var_val);
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->x400pSpanAdministrativeState = tmpvar;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */

		break;
	}
	return SNMP_ERR_NOERROR;
}

int
write_x400pSpanAlarmStatus(int action, uint8_t *var_val, uint8_t var_val_type, size_t var_val_len, uint8_t *statP, oid * name, size_t name_len)
{
	static char *tmpvar;
	struct x400pSpanTable_data *StorageTmp = NULL;
	static size_t tmplen;
	size_t newlen = name_len - (sizeof(x400pMIB_variables_oid) / sizeof(oid) + 7 - 1);

	(void) tmplen;		/* not always used */
	DEBUGMSGTL(("x400pMIB", "write_x400pSpanAlarmStatus entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(x400pSpanTableStorage, NULL, &name[sizeof(x400pMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_BIT_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanAlarmStatus not ASN_BIT_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in string for you to use, and you
				   have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		tmpvar = StorageTmp->x400pSpanAlarmStatus;
		tmplen = StorageTmp->x400pSpanAlarmStatusLen;
		memdup((void *) &StorageTmp->x400pSpanAlarmStatus, var_val, var_val_len);
		StorageTmp->x400pSpanAlarmStatusLen = var_val_len;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		SNMP_FREE(StorageTmp->x400pSpanAlarmStatus);
		StorageTmp->x400pSpanAlarmStatus = tmpvar;
		StorageTmp->x400pSpanAlarmStatusLen = tmplen;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(tmpvar);
		break;
	}
	return SNMP_ERR_NOERROR;
}

int
write_x400pSpanType(int action, uint8_t *var_val, uint8_t var_val_type, size_t var_val_len, uint8_t *statP, oid * name, size_t name_len)
{
	static int32_t tmpvar;
	struct x400pSpanTable_data *StorageTmp = NULL;
	static size_t tmplen;
	size_t newlen = name_len - (sizeof(x400pMIB_variables_oid) / sizeof(oid) + 7 - 1);

	(void) tmplen;		/* not always used */
	DEBUGMSGTL(("x400pMIB", "write_x400pSpanType entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(x400pSpanTableStorage, NULL, &name[sizeof(x400pMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanType not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in long_ret for you to use, and you 
				   have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		tmpvar = StorageTmp->x400pSpanType;
		StorageTmp->x400pSpanType = *((long *) var_val);
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->x400pSpanType = tmpvar;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */

		break;
	}
	return SNMP_ERR_NOERROR;
}

int
write_x400pSpanCrc(int action, uint8_t *var_val, uint8_t var_val_type, size_t var_val_len, uint8_t *statP, oid * name, size_t name_len)
{
	static int32_t tmpvar;
	struct x400pSpanTable_data *StorageTmp = NULL;
	static size_t tmplen;
	size_t newlen = name_len - (sizeof(x400pMIB_variables_oid) / sizeof(oid) + 7 - 1);

	(void) tmplen;		/* not always used */
	DEBUGMSGTL(("x400pMIB", "write_x400pSpanCrc entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(x400pSpanTableStorage, NULL, &name[sizeof(x400pMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanCrc not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in long_ret for you to use, and you 
				   have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		tmpvar = StorageTmp->x400pSpanCrc;
		StorageTmp->x400pSpanCrc = *((long *) var_val);
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->x400pSpanCrc = tmpvar;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */

		break;
	}
	return SNMP_ERR_NOERROR;
}

int
write_x400pSpanClocking(int action, uint8_t *var_val, uint8_t var_val_type, size_t var_val_len, uint8_t *statP, oid * name, size_t name_len)
{
	static int32_t tmpvar;
	struct x400pSpanTable_data *StorageTmp = NULL;
	static size_t tmplen;
	size_t newlen = name_len - (sizeof(x400pMIB_variables_oid) / sizeof(oid) + 7 - 1);

	(void) tmplen;		/* not always used */
	DEBUGMSGTL(("x400pMIB", "write_x400pSpanClocking entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(x400pSpanTableStorage, NULL, &name[sizeof(x400pMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanClocking not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in long_ret for you to use, and you 
				   have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		tmpvar = StorageTmp->x400pSpanClocking;
		StorageTmp->x400pSpanClocking = *((long *) var_val);
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->x400pSpanClocking = tmpvar;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */

		break;
	}
	return SNMP_ERR_NOERROR;
}

int
write_x400pSpanCoding(int action, uint8_t *var_val, uint8_t var_val_type, size_t var_val_len, uint8_t *statP, oid * name, size_t name_len)
{
	static int32_t tmpvar;
	struct x400pSpanTable_data *StorageTmp = NULL;
	static size_t tmplen;
	size_t newlen = name_len - (sizeof(x400pMIB_variables_oid) / sizeof(oid) + 7 - 1);

	(void) tmplen;		/* not always used */
	DEBUGMSGTL(("x400pMIB", "write_x400pSpanCoding entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(x400pSpanTableStorage, NULL, &name[sizeof(x400pMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanCoding not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in long_ret for you to use, and you 
				   have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		tmpvar = StorageTmp->x400pSpanCoding;
		StorageTmp->x400pSpanCoding = *((long *) var_val);
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->x400pSpanCoding = tmpvar;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */

		break;
	}
	return SNMP_ERR_NOERROR;
}

int
write_x400pSpanFraming(int action, uint8_t *var_val, uint8_t var_val_type, size_t var_val_len, uint8_t *statP, oid * name, size_t name_len)
{
	static int32_t tmpvar;
	struct x400pSpanTable_data *StorageTmp = NULL;
	static size_t tmplen;
	size_t newlen = name_len - (sizeof(x400pMIB_variables_oid) / sizeof(oid) + 7 - 1);

	(void) tmplen;		/* not always used */
	DEBUGMSGTL(("x400pMIB", "write_x400pSpanFraming entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(x400pSpanTableStorage, NULL, &name[sizeof(x400pMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanFraming not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in long_ret for you to use, and you 
				   have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		tmpvar = StorageTmp->x400pSpanFraming;
		StorageTmp->x400pSpanFraming = *((long *) var_val);
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->x400pSpanFraming = tmpvar;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */

		break;
	}
	return SNMP_ERR_NOERROR;
}

int
write_x400pSpanLineBuildout(int action, uint8_t *var_val, uint8_t var_val_type, size_t var_val_len, uint8_t *statP, oid * name, size_t name_len)
{
	static int32_t tmpvar;
	struct x400pSpanTable_data *StorageTmp = NULL;
	static size_t tmplen;
	size_t newlen = name_len - (sizeof(x400pMIB_variables_oid) / sizeof(oid) + 7 - 1);

	(void) tmplen;		/* not always used */
	DEBUGMSGTL(("x400pMIB", "write_x400pSpanLineBuildout entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(x400pSpanTableStorage, NULL, &name[sizeof(x400pMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanLineBuildout not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in long_ret for you to use, and you 
				   have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		tmpvar = StorageTmp->x400pSpanLineBuildout;
		StorageTmp->x400pSpanLineBuildout = *((long *) var_val);
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->x400pSpanLineBuildout = tmpvar;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */

		break;
	}
	return SNMP_ERR_NOERROR;
}

int
write_x400pSpanTxLevel(int action, uint8_t *var_val, uint8_t var_val_type, size_t var_val_len, uint8_t *statP, oid * name, size_t name_len)
{
	static int32_t tmpvar;
	struct x400pSpanTable_data *StorageTmp = NULL;
	static size_t tmplen;
	size_t newlen = name_len - (sizeof(x400pMIB_variables_oid) / sizeof(oid) + 7 - 1);

	(void) tmplen;		/* not always used */
	DEBUGMSGTL(("x400pMIB", "write_x400pSpanTxLevel entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(x400pSpanTableStorage, NULL, &name[sizeof(x400pMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanTxLevel not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in long_ret for you to use, and you 
				   have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		tmpvar = StorageTmp->x400pSpanTxLevel;
		StorageTmp->x400pSpanTxLevel = *((long *) var_val);
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->x400pSpanTxLevel = tmpvar;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */

		break;
	}
	return SNMP_ERR_NOERROR;
}

int
write_x400pSpanRxLevel(int action, uint8_t *var_val, uint8_t var_val_type, size_t var_val_len, uint8_t *statP, oid * name, size_t name_len)
{
	static int32_t tmpvar;
	struct x400pSpanTable_data *StorageTmp = NULL;
	static size_t tmplen;
	size_t newlen = name_len - (sizeof(x400pMIB_variables_oid) / sizeof(oid) + 7 - 1);

	(void) tmplen;		/* not always used */
	DEBUGMSGTL(("x400pMIB", "write_x400pSpanRxLevel entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(x400pSpanTableStorage, NULL, &name[sizeof(x400pMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanRxLevel not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in long_ret for you to use, and you 
				   have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		tmpvar = StorageTmp->x400pSpanRxLevel;
		StorageTmp->x400pSpanRxLevel = *((long *) var_val);
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->x400pSpanRxLevel = tmpvar;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */

		break;
	}
	return SNMP_ERR_NOERROR;
}

int
write_x400pChanAdministativeState(int action, uint8_t *var_val, uint8_t var_val_type, size_t var_val_len, uint8_t *statP, oid * name, size_t name_len)
{
	static int32_t tmpvar;
	struct x400pChanTable_data *StorageTmp = NULL;
	static size_t tmplen;
	size_t newlen = name_len - (sizeof(x400pMIB_variables_oid) / sizeof(oid) + 7 - 1);

	(void) tmplen;		/* not always used */
	DEBUGMSGTL(("x400pMIB", "write_x400pChanAdministativeState entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(x400pChanTableStorage, NULL, &name[sizeof(x400pMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pChanAdministativeState not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in long_ret for you to use, and you 
				   have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		tmpvar = StorageTmp->x400pChanAdministativeState;
		StorageTmp->x400pChanAdministativeState = *((long *) var_val);
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->x400pChanAdministativeState = tmpvar;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */

		break;
	}
	return SNMP_ERR_NOERROR;
}

int
write_x400pChanAlarmStatus(int action, uint8_t *var_val, uint8_t var_val_type, size_t var_val_len, uint8_t *statP, oid * name, size_t name_len)
{
	static char *tmpvar;
	struct x400pChanTable_data *StorageTmp = NULL;
	static size_t tmplen;
	size_t newlen = name_len - (sizeof(x400pMIB_variables_oid) / sizeof(oid) + 7 - 1);

	(void) tmplen;		/* not always used */
	DEBUGMSGTL(("x400pMIB", "write_x400pChanAlarmStatus entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(x400pChanTableStorage, NULL, &name[sizeof(x400pMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_BIT_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pChanAlarmStatus not ASN_BIT_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in string for you to use, and you
				   have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		tmpvar = StorageTmp->x400pChanAlarmStatus;
		tmplen = StorageTmp->x400pChanAlarmStatusLen;
		memdup((void *) &StorageTmp->x400pChanAlarmStatus, var_val, var_val_len);
		StorageTmp->x400pChanAlarmStatusLen = var_val_len;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		SNMP_FREE(StorageTmp->x400pChanAlarmStatus);
		StorageTmp->x400pChanAlarmStatus = tmpvar;
		StorageTmp->x400pChanAlarmStatusLen = tmplen;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(tmpvar);
		break;
	}
	return SNMP_ERR_NOERROR;
}

int
write_x400pChanType(int action, uint8_t *var_val, uint8_t var_val_type, size_t var_val_len, uint8_t *statP, oid * name, size_t name_len)
{
	static int32_t tmpvar;
	struct x400pChanTable_data *StorageTmp = NULL;
	static size_t tmplen;
	size_t newlen = name_len - (sizeof(x400pMIB_variables_oid) / sizeof(oid) + 7 - 1);

	(void) tmplen;		/* not always used */
	DEBUGMSGTL(("x400pMIB", "write_x400pChanType entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(x400pChanTableStorage, NULL, &name[sizeof(x400pMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pChanType not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in long_ret for you to use, and you 
				   have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		tmpvar = StorageTmp->x400pChanType;
		StorageTmp->x400pChanType = *((long *) var_val);
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->x400pChanType = tmpvar;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */

		break;
	}
	return SNMP_ERR_NOERROR;
}

int
write_x400pChanRate(int action, uint8_t *var_val, uint8_t var_val_type, size_t var_val_len, uint8_t *statP, oid * name, size_t name_len)
{
	static int32_t tmpvar;
	struct x400pChanTable_data *StorageTmp = NULL;
	static size_t tmplen;
	size_t newlen = name_len - (sizeof(x400pMIB_variables_oid) / sizeof(oid) + 7 - 1);

	(void) tmplen;		/* not always used */
	DEBUGMSGTL(("x400pMIB", "write_x400pChanRate entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(x400pChanTableStorage, NULL, &name[sizeof(x400pMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pChanRate not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in long_ret for you to use, and you 
				   have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		tmpvar = StorageTmp->x400pChanRate;
		StorageTmp->x400pChanRate = *((long *) var_val);
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->x400pChanRate = tmpvar;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */

		break;
	}
	return SNMP_ERR_NOERROR;
}

int
write_x400pChanMode(int action, uint8_t *var_val, uint8_t var_val_type, size_t var_val_len, uint8_t *statP, oid * name, size_t name_len)
{
	static int32_t tmpvar;
	struct x400pChanTable_data *StorageTmp = NULL;
	static size_t tmplen;
	size_t newlen = name_len - (sizeof(x400pMIB_variables_oid) / sizeof(oid) + 7 - 1);

	(void) tmplen;		/* not always used */
	DEBUGMSGTL(("x400pMIB", "write_x400pChanMode entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(x400pChanTableStorage, NULL, &name[sizeof(x400pMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pChanMode not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in long_ret for you to use, and you 
				   have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		tmpvar = StorageTmp->x400pChanMode;
		StorageTmp->x400pChanMode = *((long *) var_val);
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->x400pChanMode = tmpvar;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */

		break;
	}
	return SNMP_ERR_NOERROR;
}

int
write_x400pSyncRowStatus(int action, uint8_t *var_val, uint8_t var_val_type, size_t var_val_len, uint8_t *statP, oid * name, size_t name_len)
{
	struct x400pSyncTable_data *StorageTmp = NULL;
	static struct x400pSyncTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - (sizeof(x400pSyncTable_variables_oid) / sizeof(oid) + 3 - 1);
	static int old_value;
	int set_value;
	static struct variable_list *vars, *vp;
	struct header_complex_index *hciptr;

	StorageTmp = header_complex(x400pSyncTableStorage, NULL, &name[sizeof(x400pSyncTable_variables_oid) / sizeof(oid) + 3 - 1], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSyncRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((int *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY)
		return SNMP_ERR_INCONSISTENTVALUE;
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		if (StorageTmp == NULL) {
			/* create the row now? */
			/* ditch illegal values now */
			if (set_value == RS_ACTIVE || set_value == RS_NOTINSERVICE)
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroying a non-existent row is actually legal */
			if (set_value == RS_DESTROY) {
				return SNMP_ERR_NOERROR;
			}
			/* illegal creation values */
			if (set_value == RS_ACTIVE || set_value == RS_NOTINSERVICE) {
				return SNMP_ERR_INCONSISTENTVALUE;
			}
		} else {
			/* row exists.  Check for a valid state change */
			if (set_value == RS_CREATEANDGO || set_value == RS_CREATEANDWAIT) {
				/* can't create a row that exists */
				return SNMP_ERR_INCONSISTENTVALUE;
			}
			/* XXX: interaction with row storage type needed */
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		if (StorageTmp == NULL) {
			/* creation */
			vars = NULL;
			snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, NULL, 0);	/* x400pSyncGroup 
												 */
			snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, NULL, 0);	/* x400pSyncIndex 
												 */

			if (header_complex_parse_oid(&(name[sizeof(x400pSyncTable_variables_oid) / sizeof(oid) + 2]), newlen, vars) != SNMPERR_SUCCESS) {
				/* XXX: free, zero vars */
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			StorageNew = SNMP_MALLOC_STRUCT(x400pSyncTable_data);
			StorageNew->x400pSyncGroup = *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->x400pSyncIndex = *vp->val.integer;
			vp = vp->next_variable;

			/* XXX: fill in default row values here into StorageNew */

			StorageNew->x400pSyncRowStatus = set_value;
			/* XXX: free, zero vars, no longer needed? */
		}
		break;
	case FREE:
		/* XXX: free, zero vars */
		/* Release any resources that have been allocated */
		break;
	case ACTION:
		/* The variable has been stored in set_value for you to use, and you have just been 
		   asked to do something with it.  Note that anything done here must be reversable
		   in the UNDO case */
		if (StorageTmp == NULL) {
			/* row creation, so add it */
			if (StorageNew != NULL)
				x400pSyncTable_add(StorageNew);
			/* XXX: ack, and if it is NULL? */
		} else if (set_value != RS_DESTROY) {
			/* set the flag? */
			old_value = StorageTmp->x400pSyncRowStatus;
			StorageTmp->x400pSyncRowStatus = *((int *) var_val);
		} else {
			/* destroy...  extract it for now */
			hciptr = header_complex_find_entry(x400pSyncTableStorage, StorageTmp);
			StorageDel = header_complex_extract_entry(&x400pSyncTableStorage, hciptr);
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		if (StorageTmp == NULL) {
			/* row creation, so remove it again */
			hciptr = header_complex_find_entry(x400pSyncTableStorage, StorageTmp);
			StorageDel = header_complex_extract_entry(&x400pSyncTableStorage, hciptr);
			/* XXX: free it */
		} else if (StorageDel != NULL) {
			/* row deletion, so add it again */
			x400pSyncTable_add(StorageDel);
		} else {
			StorageTmp->x400pSyncRowStatus = old_value;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make
		   sure that anything done here can't fail! */
		if (StorageDel != NULL) {
			StorageDel = 0;
			/* XXX: free it, its dead */
		} else {
			if (StorageTmp && StorageTmp->x400pSyncRowStatus == RS_CREATEANDGO) {
				StorageTmp->x400pSyncRowStatus = RS_ACTIVE;
			} else if (StorageTmp && StorageTmp->x400pSyncRowStatus == RS_CREATEANDWAIT) {
				StorageTmp->x400pSyncRowStatus = RS_NOTINSERVICE;
			}
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

int
write_x400pCardStatus(int action, uint8_t *var_val, uint8_t var_val_type, size_t var_val_len, uint8_t *statP, oid * name, size_t name_len)
{
	struct x400pCardTable_data *StorageTmp = NULL;
	static struct x400pCardTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - (sizeof(x400pCardTable_variables_oid) / sizeof(oid) + 3 - 1);
	static int old_value;
	int set_value;
	static struct variable_list *vars, *vp;
	struct header_complex_index *hciptr;

	StorageTmp = header_complex(x400pCardTableStorage, NULL, &name[sizeof(x400pCardTable_variables_oid) / sizeof(oid) + 3 - 1], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pCardStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((int *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY)
		return SNMP_ERR_INCONSISTENTVALUE;
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		if (StorageTmp == NULL) {
			/* create the row now? */
			/* ditch illegal values now */
			if (set_value == RS_ACTIVE || set_value == RS_NOTINSERVICE)
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroying a non-existent row is actually legal */
			if (set_value == RS_DESTROY) {
				return SNMP_ERR_NOERROR;
			}
			/* illegal creation values */
			if (set_value == RS_ACTIVE || set_value == RS_NOTINSERVICE) {
				return SNMP_ERR_INCONSISTENTVALUE;
			}
		} else {
			/* row exists.  Check for a valid state change */
			if (set_value == RS_CREATEANDGO || set_value == RS_CREATEANDWAIT) {
				/* can't create a row that exists */
				return SNMP_ERR_INCONSISTENTVALUE;
			}
			/* XXX: interaction with row storage type needed */
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		if (StorageTmp == NULL) {
			/* creation */
			vars = NULL;
			snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, NULL, 0);	/* x400pCardIndex 
												 */

			if (header_complex_parse_oid(&(name[sizeof(x400pCardTable_variables_oid) / sizeof(oid) + 2]), newlen, vars) != SNMPERR_SUCCESS) {
				/* XXX: free, zero vars */
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			StorageNew = SNMP_MALLOC_STRUCT(x400pCardTable_data);
			StorageNew->x400pCardIndex = *vp->val.integer;
			vp = vp->next_variable;

			/* XXX: fill in default row values here into StorageNew */

			StorageNew->x400pCardStatus = set_value;
			/* XXX: free, zero vars, no longer needed? */
		}
		break;
	case FREE:
		/* XXX: free, zero vars */
		/* Release any resources that have been allocated */
		break;
	case ACTION:
		/* The variable has been stored in set_value for you to use, and you have just been 
		   asked to do something with it.  Note that anything done here must be reversable
		   in the UNDO case */
		if (StorageTmp == NULL) {
			/* row creation, so add it */
			if (StorageNew != NULL)
				x400pCardTable_add(StorageNew);
			/* XXX: ack, and if it is NULL? */
		} else if (set_value != RS_DESTROY) {
			/* set the flag? */
			old_value = StorageTmp->x400pCardStatus;
			StorageTmp->x400pCardStatus = *((int *) var_val);
		} else {
			/* destroy...  extract it for now */
			hciptr = header_complex_find_entry(x400pCardTableStorage, StorageTmp);
			StorageDel = header_complex_extract_entry(&x400pCardTableStorage, hciptr);
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		if (StorageTmp == NULL) {
			/* row creation, so remove it again */
			hciptr = header_complex_find_entry(x400pCardTableStorage, StorageTmp);
			StorageDel = header_complex_extract_entry(&x400pCardTableStorage, hciptr);
			/* XXX: free it */
		} else if (StorageDel != NULL) {
			/* row deletion, so add it again */
			x400pCardTable_add(StorageDel);
		} else {
			StorageTmp->x400pCardStatus = old_value;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make
		   sure that anything done here can't fail! */
		if (StorageDel != NULL) {
			StorageDel = 0;
			/* XXX: free it, its dead */
		} else {
			if (StorageTmp && StorageTmp->x400pCardStatus == RS_CREATEANDGO) {
				StorageTmp->x400pCardStatus = RS_ACTIVE;
			} else if (StorageTmp && StorageTmp->x400pCardStatus == RS_CREATEANDWAIT) {
				StorageTmp->x400pCardStatus = RS_NOTINSERVICE;
			}
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}
