/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.19 $ of : mfd-data-set.m2c,v $
 *
 * $Id:$
 *
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "strApshTable.h"

/** @defgroup data_set data_set: Routines to set data
 *
 * These routines are used to set the value for individual objects. The
 * row context is passed, along with the new value.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table strApshTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * OPENSS7-STREAMS-MIB::strApshTable is subid 1 of strApsh.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.29591.1.1.1.1.1.4.1, length: 14
*/
    /*
     * NOTE: if you update this chart, please update the versions in
     *       local/mib2c-conf.d/parent-set.m2i
     *       agent/mibgroup/helpers/baby_steps.c
     * while you're at it.
     */
    /*
     ***********************************************************************
     * Baby Steps Flow Chart (2004.06.05)                                  *
     *                                                                     *
     * +--------------+    +================+    U = unconditional path    *
     * |optional state|    ||required state||    S = path for success      *
     * +--------------+    +================+    E = path for error        *
     ***********************************************************************
     *
     *                        +--------------+
     *                        |     pre      |
     *                        |   request    |
     *                        +--------------+
     *                               | U
     * +-------------+        +==============+
     * |    row    |f|<-------||  object    ||
     * |  create   |1|      E ||  lookup    ||
     * +-------------+        +==============+
     *     E |   | S                 | S
     *       |   +------------------>|
     *       |                +==============+
     *       |              E ||   check    ||
     *       |<---------------||   values   ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |       +<-------||   undo     ||
     *       |       |      E ||   setup    ||
     *       |       |        +==============+
     *       |       |               | S
     *       |       |        +==============+
     *       |       |        ||    set     ||-------------------------->+
     *       |       |        ||   value    || E                         |
     *       |       |        +==============+                           |
     *       |       |               | S                                 |
     *       |       |        +--------------+                           |
     *       |       |        |    check     |-------------------------->|
     *       |       |        |  consistency | E                         |
     *       |       |        +--------------+                           |
     *       |       |               | S                                 |
     *       |       |        +==============+         +==============+  |
     *       |       |        ||   commit   ||-------->||     undo   ||  |
     *       |       |        ||            || E       ||    commit  ||  |
     *       |       |        +==============+         +==============+  |
     *       |       |               | S                     U |<--------+
     *       |       |        +--------------+         +==============+
     *       |       |        | irreversible |         ||    undo    ||
     *       |       |        |    commit    |         ||     set    ||
     *       |       |        +--------------+         +==============+
     *       |       |               | U                     U |
     *       |       +-------------->|<------------------------+
     *       |                +==============+
     *       |                ||   undo     ||
     *       |                ||  cleanup   ||
     *       |                +==============+
     *       +---------------------->| U
     *                               |
     *                          (err && f1)------------------->+
     *                               |                         |
     *                        +--------------+         +--------------+
     *                        |    post      |<--------|      row     |
     *                        |   request    |       U |    release   |
     *                        +--------------+         +--------------+
     *
     */

/**
 * Setup up context with information needed to undo a set request.
 *
 * This function will be called before the individual node undo setup
 * functions are called. If you need to do any undo setup that is not
 * related to a specific column, you can do it here.
 *
 * Note that the undo context has been allocated with
 * strApshTable_allocate_data(), but may need extra
 * initialization similar to what you may have done in
 * strApshTable_rowreq_ctx_init().
 * Note that an individual node's undo_setup function will only be called
 * if that node is being set to a new value.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in the node's undo_setup
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (strApshTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
strApshTable_undo_setup(strApshTable_rowreq_ctx * rowreq_ctx)
{
        int rc = MFD_SUCCESS;

        DEBUGMSGTL(("verbose:strApshTable:strApshTable_undo_setup", "called\n"));

    /** we should have a non-NULL pointer */
        netsnmp_assert(NULL != rowreq_ctx);

        /*
         * TODO:451:M: |-> Setup strApshTable undo.
         * set up strApshTable undo information, in preparation for a set.
         * Undo storage is in (* strApshStatus_val_ptr )*
         */

        return rc;
}                               /* strApshTable_undo_setup */

/**
 * Undo a set request.
 *
 * This function will be called before the individual node undo
 * functions are called. If you need to do any undo that is not
 * related to a specific column, you can do it here.
 *
 * Note that an individual node's undo function will only be called
 * if that node is being set to a new value.
 *
 * If there is anything  specific to a particular column (e.g. releasing
 * memory for a string), you should do that setup in the node's undo
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (strApshTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
strApshTable_undo(strApshTable_rowreq_ctx * rowreq_ctx)
{
        int rc = MFD_SUCCESS;

        DEBUGMSGTL(("verbose:strApshTable:strApshTable_undo", "called\n"));

    /** we should have a non-NULL pointer */
        netsnmp_assert(NULL != rowreq_ctx);

        /*
         * TODO:451:M: |-> strApshTable undo.
         * strApshTable undo information, in response to a failed set.
         * Undo storage is in (* strApshStatus_val_ptr )*
         */

        return rc;
}                               /* strApshTable_undo_setup */

/**
 * Cleanup up context undo information.
 *
 * This function will be called after set/commit processing. If you
 * allocated any resources in undo_setup, this is the place to release
 * those resources.
 *
 * This function is called regardless of the success or failure of the set
 * request. If you need to perform different steps for cleanup depending
 * on success or failure, you can add a flag to the rowreq_ctx.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (strApshTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
strApshTable_undo_cleanup(strApshTable_rowreq_ctx * rowreq_ctx)
{
        int rc = MFD_SUCCESS;

        DEBUGMSGTL(("verbose:strApshTable:strApshTable_undo_cleanup", "called\n"));

    /** we should have a non-NULL pointer */
        netsnmp_assert(NULL != rowreq_ctx);

        /*
         * TODO:452:M: |-> Cleanup strApshTable undo.
         * Undo storage is in (* strApshStatus_val_ptr )*
         */

        return rc;
}                               /* strApshTable_undo_cleanup */

/**
 * commit new values.
 *
 * At this point, you should have done everything you can to ensure that
 * this commit will not fail.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * strApshTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param strApshTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
strApshTable_commit(strApshTable_rowreq_ctx * rowreq_ctx)
{
        int rc = MFD_SUCCESS;
        int save_flags;

        DEBUGMSGTL(("verbose:strApshTable:strApshTable_commit", "called\n"));

    /** we should have a non-NULL pointer */
        netsnmp_assert(NULL != rowreq_ctx);

        /*
         * save flags, then clear until we actually do something
         */
        save_flags = rowreq_ctx->column_set_flags;
        rowreq_ctx->column_set_flags = 0;

        /*
         * commit strApshTable data
         * 1) check the column's flag in save_flags to see if it was set.
         * 2) clear the flag when you handle that column
         * 3) set the column's flag in column_set_flags if it needs undo
         *    processing in case of a failure.
         */
        if (save_flags & COLUMN_STRAPSHMODULES_FLAG) {
                save_flags &= ~COLUMN_STRAPSHMODULES_FLAG;      /* clear strApshModules */
                /*
                 * TODO:482:o: |-> commit column strApshModules.
                 */
                rc = -1;
                if (-1 == rc) {
                        snmp_log(LOG_ERR, "strApshTable column strApshModules commit failed\n");
                } else {
                        /*
                         * set flag, in case we need to undo strApshModules
                         */
                        rowreq_ctx->column_set_flags |= COLUMN_STRAPSHMODULES_FLAG;
                }
        }

        if (save_flags & COLUMN_STRAPSHSTATUS_FLAG) {
                save_flags &= ~COLUMN_STRAPSHSTATUS_FLAG;       /* clear strApshStatus */
                /*
                 * TODO:482:o: |-> commit column strApshStatus.
                 */
                rc = -1;
                if (-1 == rc) {
                        snmp_log(LOG_ERR, "strApshTable column strApshStatus commit failed\n");
                } else {
                        /*
                         * set flag, in case we need to undo strApshStatus
                         */
                        rowreq_ctx->column_set_flags |= COLUMN_STRAPSHSTATUS_FLAG;
                }
        }

        /*
         * if we successfully commited this row, set the dirty flag.
         */
        if (MFD_SUCCESS == rc) {
                rowreq_ctx->rowreq_flags |= MFD_ROW_DIRTY;
        }

        if (save_flags) {
                snmp_log(LOG_ERR, "unhandled columns (0x%x) in commit\n", save_flags);
                return MFD_ERROR;
        }

        return rc;
}                               /* strApshTable_commit */

/**
 * undo commit new values.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * strApshTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param strApshTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
strApshTable_undo_commit(strApshTable_rowreq_ctx * rowreq_ctx)
{
        int rc = MFD_SUCCESS;

        DEBUGMSGTL(("verbose:strApshTable:strApshTable_undo_commit", "called\n"));

    /** we should have a non-NULL pointer */
        netsnmp_assert(NULL != rowreq_ctx);

        /*
         * TODO:485:M: |-> Undo strApshTable commit.
         * check the column's flag in rowreq_ctx->column_set_flags to see
         * if it was set during commit, then undo it.
         *
         * eg: if (rowreq_ctx->column_set_flags & COLUMN__FLAG) {}
         */

        /*
         * if we successfully un-commited this row, clear the dirty flag.
         */
        if (MFD_SUCCESS == rc) {
                rowreq_ctx->rowreq_flags &= ~MFD_ROW_DIRTY;
        }

        return rc;
}                               /* strApshTable_undo_commit */

/*
 * TODO:440:M: Implement strApshTable node value checks.
 * TODO:450:M: Implement strApshTable undo functions.
 * TODO:460:M: Implement strApshTable set functions.
 * TODO:480:M: Implement strApshTable commit functions.
 */
/*---------------------------------------------------------------------
 * OPENSS7-STREAMS-MIB::strApshEntry.strApshModules
 * strApshModules is subid 5 of strApshEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.29591.1.1.1.1.1.4.1.1.5
 * Description:
Provides a STREAMS module list flowing the 
		 StreamsModuleList textual convention.  This is a 
		 space-separated list of Streams module names.  When 
		 this entry is created, it is persistent accross agent 
		 reboots, and might first be verified by the agent.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 1a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is StreamsModuleList (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param strApshModules_val_ptr
 *        A char containing the new value.
 * @param strApshModules_val_ptr_len
 *        The size (in bytes) of the data pointed to by strApshModules_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * strApshTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.strApshModules).
 *    The length is in (one of) the range set(s):  0 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
strApshModules_check_value(strApshTable_rowreq_ctx * rowreq_ctx, char *strApshModules_val_ptr,
                           size_t strApshModules_val_ptr_len)
{
        DEBUGMSGTL(("verbose:strApshTable:strApshModules_check_value", "called\n"));

    /** should never get a NULL pointer */
        netsnmp_assert(NULL != rowreq_ctx);
        netsnmp_assert(NULL != strApshModules_val_ptr);

        /*
         * TODO:441:o: |-> Check for valid strApshModules value.
         */

        return MFD_SUCCESS;     /* strApshModules value not illegal */
}                               /* strApshModules_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (strApshTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * strApshTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
strApshModules_undo_setup(strApshTable_rowreq_ctx * rowreq_ctx)
{
        DEBUGMSGTL(("verbose:strApshTable:strApshModules_undo_setup", "called\n"));

    /** should never get a NULL pointer */
        netsnmp_assert(NULL != rowreq_ctx);

        /*
         * TODO:455:o: |-> Setup strApshModules undo.
         */
        /*
         * copy strApshModules and strApshModules_len data
         * set rowreq_ctx->undo->strApshModules from rowreq_ctx->data.strApshModules
         */
        memcpy(rowreq_ctx->undo->strApshModules, rowreq_ctx->data.strApshModules,
               (rowreq_ctx->data.strApshModules_len * sizeof(rowreq_ctx->undo->strApshModules[0])));
        rowreq_ctx->undo->strApshModules_len = rowreq_ctx->data.strApshModules_len;

        return MFD_SUCCESS;
}                               /* strApshModules_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param strApshModules_val_ptr
 *        A char containing the new value.
 * @param strApshModules_val_ptr_len
 *        The size (in bytes) of the data pointed to by strApshModules_val_ptr
 */
int
strApshModules_set(strApshTable_rowreq_ctx * rowreq_ctx, char *strApshModules_val_ptr,
                   size_t strApshModules_val_ptr_len)
{

        DEBUGMSGTL(("verbose:strApshTable:strApshModules_set", "called\n"));

    /** should never get a NULL pointer */
        netsnmp_assert(NULL != rowreq_ctx);
        netsnmp_assert(NULL != strApshModules_val_ptr);

        /*
         * TODO:461:M: |-> Set strApshModules value.
         * set strApshModules value in rowreq_ctx->data
         */
        memcpy(rowreq_ctx->data.strApshModules, strApshModules_val_ptr, strApshModules_val_ptr_len);
    /** convert bytes to number of char */
        rowreq_ctx->data.strApshModules_len =
            strApshModules_val_ptr_len / sizeof(strApshModules_val_ptr[0]);

        return MFD_SUCCESS;
}                               /* strApshModules_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
strApshModules_undo(strApshTable_rowreq_ctx * rowreq_ctx)
{

        DEBUGMSGTL(("verbose:strApshTable:strApshModules_undo", "called\n"));

        netsnmp_assert(NULL != rowreq_ctx);

        /*
         * TODO:456:o: |-> Clean up strApshModules undo.
         */
        /*
         * copy strApshModules and strApshModules_len data
         * set rowreq_ctx->data.strApshModules from rowreq_ctx->undo->strApshModules
         */
        memcpy(rowreq_ctx->data.strApshModules, rowreq_ctx->undo->strApshModules,
               (rowreq_ctx->undo->strApshModules_len * sizeof(rowreq_ctx->data.strApshModules[0])));
        rowreq_ctx->data.strApshModules_len = rowreq_ctx->undo->strApshModules_len;

        return MFD_SUCCESS;
}                               /* strApshModules_undo */

/*---------------------------------------------------------------------
 * OPENSS7-STREAMS-MIB::strApshEntry.strApshStatus
 * strApshStatus is subid 6 of strApshEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.29591.1.1.1.1.1.4.1.1.6
 * Description:
Provides management stations with the ability to add 
		 rows to, or delete rows from, the STREAMS autopush table.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  active(1), notInService(2), notReady(3), createAndGo(4), createAndWait(5), destroy(6)
 *
 * Its syntax is RowStatus (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param strApshStatus_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * strApshTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  active(1), notInService(2), notReady(3), createAndGo(4), createAndWait(5), destroy(6)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
strApshStatus_check_value(strApshTable_rowreq_ctx * rowreq_ctx, u_long strApshStatus_val)
{
        DEBUGMSGTL(("verbose:strApshTable:strApshStatus_check_value", "called\n"));

    /** should never get a NULL pointer */
        netsnmp_assert(NULL != rowreq_ctx);

        /*
         * TODO:441:o: |-> Check for valid strApshStatus value.
         */

        return MFD_SUCCESS;     /* strApshStatus value not illegal */
}                               /* strApshStatus_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (strApshTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * strApshTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
strApshStatus_undo_setup(strApshTable_rowreq_ctx * rowreq_ctx)
{
        DEBUGMSGTL(("verbose:strApshTable:strApshStatus_undo_setup", "called\n"));

    /** should never get a NULL pointer */
        netsnmp_assert(NULL != rowreq_ctx);

        /*
         * TODO:455:o: |-> Setup strApshStatus undo.
         */
        /*
         * copy strApshStatus data
         * set rowreq_ctx->undo->strApshStatus from rowreq_ctx->data.strApshStatus
         */
        rowreq_ctx->undo->strApshStatus = rowreq_ctx->data.strApshStatus;

        return MFD_SUCCESS;
}                               /* strApshStatus_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param strApshStatus_val
 *        A long containing the new value.
 */
int
strApshStatus_set(strApshTable_rowreq_ctx * rowreq_ctx, u_long strApshStatus_val)
{

        DEBUGMSGTL(("verbose:strApshTable:strApshStatus_set", "called\n"));

    /** should never get a NULL pointer */
        netsnmp_assert(NULL != rowreq_ctx);

        /*
         * TODO:461:M: |-> Set strApshStatus value.
         * set strApshStatus value in rowreq_ctx->data
         */
        rowreq_ctx->data.strApshStatus = strApshStatus_val;

        return MFD_SUCCESS;
}                               /* strApshStatus_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
strApshStatus_undo(strApshTable_rowreq_ctx * rowreq_ctx)
{

        DEBUGMSGTL(("verbose:strApshTable:strApshStatus_undo", "called\n"));

        netsnmp_assert(NULL != rowreq_ctx);

        /*
         * TODO:456:o: |-> Clean up strApshStatus undo.
         */
        /*
         * copy strApshStatus data
         * set rowreq_ctx->data.strApshStatus from rowreq_ctx->undo->strApshStatus
         */
        rowreq_ctx->data.strApshStatus = rowreq_ctx->undo->strApshStatus;

        return MFD_SUCCESS;
}                               /* strApshStatus_undo */

/** @} */
