/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.19 $ of : mfd-data-set.m2c,v $
 *
 * $Id:$
 *
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "strModInfoTable.h"

/** @defgroup data_set data_set: Routines to set data
 *
 * These routines are used to set the value for individual objects. The
 * row context is passed, along with the new value.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table strModInfoTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * OPENSS7-STREAMS-MIB::strModInfoTable is subid 1 of strModInfo.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.29591.1.1.1.1.1.2.1, length: 14
*/
    /*
     * NOTE: if you update this chart, please update the versions in
     *       local/mib2c-conf.d/parent-set.m2i
     *       agent/mibgroup/helpers/baby_steps.c
     * while you're at it.
     */
    /*
     ***********************************************************************
     * Baby Steps Flow Chart (2004.06.05)                                  *
     *                                                                     *
     * +--------------+    +================+    U = unconditional path    *
     * |optional state|    ||required state||    S = path for success      *
     * +--------------+    +================+    E = path for error        *
     ***********************************************************************
     *
     *                        +--------------+
     *                        |     pre      |
     *                        |   request    |
     *                        +--------------+
     *                               | U
     *                        +==============+
     *       +----------------||  object    ||
     *       |              E ||  lookup    ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |              E ||   check    ||
     *       |<---------------||   values   ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |       +<-------||   undo     ||
     *       |       |      E ||   setup    ||
     *       |       |        +==============+
     *       |       |               | S
     *       |       |        +==============+
     *       |       |        ||    set     ||-------------------------->+
     *       |       |        ||   value    || E                         |
     *       |       |        +==============+                           |
     *       |       |               | S                                 |
     *       |       |        +--------------+                           |
     *       |       |        |    check     |-------------------------->|
     *       |       |        |  consistency | E                         |
     *       |       |        +--------------+                           |
     *       |       |               | S                                 |
     *       |       |        +==============+         +==============+  |
     *       |       |        ||   commit   ||-------->||     undo   ||  |
     *       |       |        ||            || E       ||    commit  ||  |
     *       |       |        +==============+         +==============+  |
     *       |       |               | S                     U |<--------+
     *       |       |        +--------------+         +==============+
     *       |       |        | irreversible |         ||    undo    ||
     *       |       |        |    commit    |         ||     set    ||
     *       |       |        +--------------+         +==============+
     *       |       |               | U                     U |
     *       |       +-------------->|<------------------------+
     *       |                +==============+
     *       |                ||   undo     ||
     *       |                ||  cleanup   ||
     *       |                +==============+
     *       +---------------------->| U
     *                        +--------------+
     *                        |    post      |
     *                        |   request    |
     *                        +--------------+
     *
     */

/**
 * Setup up context with information needed to undo a set request.
 *
 * This function will be called before the individual node undo setup
 * functions are called. If you need to do any undo setup that is not
 * related to a specific column, you can do it here.
 *
 * Note that the undo context has been allocated with
 * strModInfoTable_allocate_data(), but may need extra
 * initialization similar to what you may have done in
 * strModInfoTable_rowreq_ctx_init().
 * Note that an individual node's undo_setup function will only be called
 * if that node is being set to a new value.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in the node's undo_setup
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (strModInfoTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
strModInfoTable_undo_setup(strModInfoTable_rowreq_ctx * rowreq_ctx)
{
        int rc = MFD_SUCCESS;

        DEBUGMSGTL(("verbose:strModInfoTable:strModInfoTable_undo_setup", "called\n"));

    /** we should have a non-NULL pointer */
        netsnmp_assert(NULL != rowreq_ctx);

        /*
         * TODO:451:M: |-> Setup strModInfoTable undo.
         * set up strModInfoTable undo information, in preparation for a set.
         * Undo storage is in (* strModInfoTraceLevel_val_ptr )*
         */

        return rc;
}                               /* strModInfoTable_undo_setup */

/**
 * Undo a set request.
 *
 * This function will be called before the individual node undo
 * functions are called. If you need to do any undo that is not
 * related to a specific column, you can do it here.
 *
 * Note that an individual node's undo function will only be called
 * if that node is being set to a new value.
 *
 * If there is anything  specific to a particular column (e.g. releasing
 * memory for a string), you should do that setup in the node's undo
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (strModInfoTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
strModInfoTable_undo(strModInfoTable_rowreq_ctx * rowreq_ctx)
{
        int rc = MFD_SUCCESS;

        DEBUGMSGTL(("verbose:strModInfoTable:strModInfoTable_undo", "called\n"));

    /** we should have a non-NULL pointer */
        netsnmp_assert(NULL != rowreq_ctx);

        /*
         * TODO:451:M: |-> strModInfoTable undo.
         * strModInfoTable undo information, in response to a failed set.
         * Undo storage is in (* strModInfoTraceLevel_val_ptr )*
         */

        return rc;
}                               /* strModInfoTable_undo_setup */

/**
 * Cleanup up context undo information.
 *
 * This function will be called after set/commit processing. If you
 * allocated any resources in undo_setup, this is the place to release
 * those resources.
 *
 * This function is called regardless of the success or failure of the set
 * request. If you need to perform different steps for cleanup depending
 * on success or failure, you can add a flag to the rowreq_ctx.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (strModInfoTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
strModInfoTable_undo_cleanup(strModInfoTable_rowreq_ctx * rowreq_ctx)
{
        int rc = MFD_SUCCESS;

        DEBUGMSGTL(("verbose:strModInfoTable:strModInfoTable_undo_cleanup", "called\n"));

    /** we should have a non-NULL pointer */
        netsnmp_assert(NULL != rowreq_ctx);

        /*
         * TODO:452:M: |-> Cleanup strModInfoTable undo.
         * Undo storage is in (* strModInfoTraceLevel_val_ptr )*
         */

        return rc;
}                               /* strModInfoTable_undo_cleanup */

/**
 * commit new values.
 *
 * At this point, you should have done everything you can to ensure that
 * this commit will not fail.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * strModInfoTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param strModInfoTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
strModInfoTable_commit(strModInfoTable_rowreq_ctx * rowreq_ctx)
{
        int rc = MFD_SUCCESS;
        int save_flags;

        DEBUGMSGTL(("verbose:strModInfoTable:strModInfoTable_commit", "called\n"));

    /** we should have a non-NULL pointer */
        netsnmp_assert(NULL != rowreq_ctx);

        /*
         * save flags, then clear until we actually do something
         */
        save_flags = rowreq_ctx->column_set_flags;
        rowreq_ctx->column_set_flags = 0;

        /*
         * commit strModInfoTable data
         * 1) check the column's flag in save_flags to see if it was set.
         * 2) clear the flag when you handle that column
         * 3) set the column's flag in column_set_flags if it needs undo
         *    processing in case of a failure.
         */
        if (save_flags & COLUMN_STRMODINFOMINPSZ_FLAG) {
                save_flags &= ~COLUMN_STRMODINFOMINPSZ_FLAG;    /* clear strModInfoMinpsz */
                /*
                 * TODO:482:o: |-> commit column strModInfoMinpsz.
                 */
                rc = -1;
                if (-1 == rc) {
                        snmp_log(LOG_ERR,
                                 "strModInfoTable column strModInfoMinpsz commit failed\n");
                } else {
                        /*
                         * set flag, in case we need to undo strModInfoMinpsz
                         */
                        rowreq_ctx->column_set_flags |= COLUMN_STRMODINFOMINPSZ_FLAG;
                }
        }

        if (save_flags & COLUMN_STRMODINFOMAXPSZ_FLAG) {
                save_flags &= ~COLUMN_STRMODINFOMAXPSZ_FLAG;    /* clear strModInfoMaxpsz */
                /*
                 * TODO:482:o: |-> commit column strModInfoMaxpsz.
                 */
                rc = -1;
                if (-1 == rc) {
                        snmp_log(LOG_ERR,
                                 "strModInfoTable column strModInfoMaxpsz commit failed\n");
                } else {
                        /*
                         * set flag, in case we need to undo strModInfoMaxpsz
                         */
                        rowreq_ctx->column_set_flags |= COLUMN_STRMODINFOMAXPSZ_FLAG;
                }
        }

        if (save_flags & COLUMN_STRMODINFOHIWAT_FLAG) {
                save_flags &= ~COLUMN_STRMODINFOHIWAT_FLAG;     /* clear strModInfoHiwat */
                /*
                 * TODO:482:o: |-> commit column strModInfoHiwat.
                 */
                rc = -1;
                if (-1 == rc) {
                        snmp_log(LOG_ERR, "strModInfoTable column strModInfoHiwat commit failed\n");
                } else {
                        /*
                         * set flag, in case we need to undo strModInfoHiwat
                         */
                        rowreq_ctx->column_set_flags |= COLUMN_STRMODINFOHIWAT_FLAG;
                }
        }

        if (save_flags & COLUMN_STRMODINFOLOWAT_FLAG) {
                save_flags &= ~COLUMN_STRMODINFOLOWAT_FLAG;     /* clear strModInfoLowat */
                /*
                 * TODO:482:o: |-> commit column strModInfoLowat.
                 */
                rc = -1;
                if (-1 == rc) {
                        snmp_log(LOG_ERR, "strModInfoTable column strModInfoLowat commit failed\n");
                } else {
                        /*
                         * set flag, in case we need to undo strModInfoLowat
                         */
                        rowreq_ctx->column_set_flags |= COLUMN_STRMODINFOLOWAT_FLAG;
                }
        }

        if (save_flags & COLUMN_STRMODINFOTRACELEVEL_FLAG) {
                save_flags &= ~COLUMN_STRMODINFOTRACELEVEL_FLAG;        /* clear strModInfoTraceLevel */
                /*
                 * TODO:482:o: |-> commit column strModInfoTraceLevel.
                 */
                rc = -1;
                if (-1 == rc) {
                        snmp_log(LOG_ERR,
                                 "strModInfoTable column strModInfoTraceLevel commit failed\n");
                } else {
                        /*
                         * set flag, in case we need to undo strModInfoTraceLevel
                         */
                        rowreq_ctx->column_set_flags |= COLUMN_STRMODINFOTRACELEVEL_FLAG;
                }
        }

        /*
         * if we successfully commited this row, set the dirty flag.
         */
        if (MFD_SUCCESS == rc) {
                rowreq_ctx->rowreq_flags |= MFD_ROW_DIRTY;
        }

        if (save_flags) {
                snmp_log(LOG_ERR, "unhandled columns (0x%x) in commit\n", save_flags);
                return MFD_ERROR;
        }

        return rc;
}                               /* strModInfoTable_commit */

/**
 * undo commit new values.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * strModInfoTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param strModInfoTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
strModInfoTable_undo_commit(strModInfoTable_rowreq_ctx * rowreq_ctx)
{
        int rc = MFD_SUCCESS;

        DEBUGMSGTL(("verbose:strModInfoTable:strModInfoTable_undo_commit", "called\n"));

    /** we should have a non-NULL pointer */
        netsnmp_assert(NULL != rowreq_ctx);

        /*
         * TODO:485:M: |-> Undo strModInfoTable commit.
         * check the column's flag in rowreq_ctx->column_set_flags to see
         * if it was set during commit, then undo it.
         *
         * eg: if (rowreq_ctx->column_set_flags & COLUMN__FLAG) {}
         */

        /*
         * if we successfully un-commited this row, clear the dirty flag.
         */
        if (MFD_SUCCESS == rc) {
                rowreq_ctx->rowreq_flags &= ~MFD_ROW_DIRTY;
        }

        return rc;
}                               /* strModInfoTable_undo_commit */

/*
 * TODO:440:M: Implement strModInfoTable node value checks.
 * TODO:450:M: Implement strModInfoTable undo functions.
 * TODO:460:M: Implement strModInfoTable set functions.
 * TODO:480:M: Implement strModInfoTable commit functions.
 */
/*---------------------------------------------------------------------
 * OPENSS7-STREAMS-MIB::strModInfoEntry.strModInfoMinpsz
 * strModInfoMinpsz is subid 3 of strModInfoEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.29591.1.1.1.1.1.2.1.1.3
 * Description:
Specifies the minimum packet size associated with the 
		 driver or module for the queues indicated in the 
		 strModInfoQueues column.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is UNSIGNED32 (based on perltype UNSIGNED32)
 * The net-snmp type is ASN_UNSIGNED. The C type decl is u_long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param strModInfoMinpsz_val
 *        A u_long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * strModInfoTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_UNSIGNED
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
strModInfoMinpsz_check_value(strModInfoTable_rowreq_ctx * rowreq_ctx, u_long strModInfoMinpsz_val)
{
        DEBUGMSGTL(("verbose:strModInfoTable:strModInfoMinpsz_check_value", "called\n"));

    /** should never get a NULL pointer */
        netsnmp_assert(NULL != rowreq_ctx);

        /*
         * TODO:441:o: |-> Check for valid strModInfoMinpsz value.
         */

        return MFD_SUCCESS;     /* strModInfoMinpsz value not illegal */
}                               /* strModInfoMinpsz_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (strModInfoTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * strModInfoTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
strModInfoMinpsz_undo_setup(strModInfoTable_rowreq_ctx * rowreq_ctx)
{
        DEBUGMSGTL(("verbose:strModInfoTable:strModInfoMinpsz_undo_setup", "called\n"));

    /** should never get a NULL pointer */
        netsnmp_assert(NULL != rowreq_ctx);

        /*
         * TODO:455:o: |-> Setup strModInfoMinpsz undo.
         */
        /*
         * copy strModInfoMinpsz data
         * set rowreq_ctx->undo->strModInfoMinpsz from rowreq_ctx->data.strModInfoMinpsz
         */
        rowreq_ctx->undo->strModInfoMinpsz = rowreq_ctx->data.strModInfoMinpsz;

        return MFD_SUCCESS;
}                               /* strModInfoMinpsz_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param strModInfoMinpsz_val
 *        A u_long containing the new value.
 */
int
strModInfoMinpsz_set(strModInfoTable_rowreq_ctx * rowreq_ctx, u_long strModInfoMinpsz_val)
{

        DEBUGMSGTL(("verbose:strModInfoTable:strModInfoMinpsz_set", "called\n"));

    /** should never get a NULL pointer */
        netsnmp_assert(NULL != rowreq_ctx);

        /*
         * TODO:461:M: |-> Set strModInfoMinpsz value.
         * set strModInfoMinpsz value in rowreq_ctx->data
         */
        rowreq_ctx->data.strModInfoMinpsz = strModInfoMinpsz_val;

        return MFD_SUCCESS;
}                               /* strModInfoMinpsz_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
strModInfoMinpsz_undo(strModInfoTable_rowreq_ctx * rowreq_ctx)
{

        DEBUGMSGTL(("verbose:strModInfoTable:strModInfoMinpsz_undo", "called\n"));

        netsnmp_assert(NULL != rowreq_ctx);

        /*
         * TODO:456:o: |-> Clean up strModInfoMinpsz undo.
         */
        /*
         * copy strModInfoMinpsz data
         * set rowreq_ctx->data.strModInfoMinpsz from rowreq_ctx->undo->strModInfoMinpsz
         */
        rowreq_ctx->data.strModInfoMinpsz = rowreq_ctx->undo->strModInfoMinpsz;

        return MFD_SUCCESS;
}                               /* strModInfoMinpsz_undo */

/*---------------------------------------------------------------------
 * OPENSS7-STREAMS-MIB::strModInfoEntry.strModInfoMaxpsz
 * strModInfoMaxpsz is subid 4 of strModInfoEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.29591.1.1.1.1.1.2.1.1.4
 * Description:
Specifies the maximum packet size associated with the 
		 driver or module for the queues indicated in the 
		 strModInfoQueues column.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is UNSIGNED32 (based on perltype UNSIGNED32)
 * The net-snmp type is ASN_UNSIGNED. The C type decl is u_long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param strModInfoMaxpsz_val
 *        A u_long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * strModInfoTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_UNSIGNED
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
strModInfoMaxpsz_check_value(strModInfoTable_rowreq_ctx * rowreq_ctx, u_long strModInfoMaxpsz_val)
{
        DEBUGMSGTL(("verbose:strModInfoTable:strModInfoMaxpsz_check_value", "called\n"));

    /** should never get a NULL pointer */
        netsnmp_assert(NULL != rowreq_ctx);

        /*
         * TODO:441:o: |-> Check for valid strModInfoMaxpsz value.
         */

        return MFD_SUCCESS;     /* strModInfoMaxpsz value not illegal */
}                               /* strModInfoMaxpsz_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (strModInfoTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * strModInfoTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
strModInfoMaxpsz_undo_setup(strModInfoTable_rowreq_ctx * rowreq_ctx)
{
        DEBUGMSGTL(("verbose:strModInfoTable:strModInfoMaxpsz_undo_setup", "called\n"));

    /** should never get a NULL pointer */
        netsnmp_assert(NULL != rowreq_ctx);

        /*
         * TODO:455:o: |-> Setup strModInfoMaxpsz undo.
         */
        /*
         * copy strModInfoMaxpsz data
         * set rowreq_ctx->undo->strModInfoMaxpsz from rowreq_ctx->data.strModInfoMaxpsz
         */
        rowreq_ctx->undo->strModInfoMaxpsz = rowreq_ctx->data.strModInfoMaxpsz;

        return MFD_SUCCESS;
}                               /* strModInfoMaxpsz_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param strModInfoMaxpsz_val
 *        A u_long containing the new value.
 */
int
strModInfoMaxpsz_set(strModInfoTable_rowreq_ctx * rowreq_ctx, u_long strModInfoMaxpsz_val)
{

        DEBUGMSGTL(("verbose:strModInfoTable:strModInfoMaxpsz_set", "called\n"));

    /** should never get a NULL pointer */
        netsnmp_assert(NULL != rowreq_ctx);

        /*
         * TODO:461:M: |-> Set strModInfoMaxpsz value.
         * set strModInfoMaxpsz value in rowreq_ctx->data
         */
        rowreq_ctx->data.strModInfoMaxpsz = strModInfoMaxpsz_val;

        return MFD_SUCCESS;
}                               /* strModInfoMaxpsz_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
strModInfoMaxpsz_undo(strModInfoTable_rowreq_ctx * rowreq_ctx)
{

        DEBUGMSGTL(("verbose:strModInfoTable:strModInfoMaxpsz_undo", "called\n"));

        netsnmp_assert(NULL != rowreq_ctx);

        /*
         * TODO:456:o: |-> Clean up strModInfoMaxpsz undo.
         */
        /*
         * copy strModInfoMaxpsz data
         * set rowreq_ctx->data.strModInfoMaxpsz from rowreq_ctx->undo->strModInfoMaxpsz
         */
        rowreq_ctx->data.strModInfoMaxpsz = rowreq_ctx->undo->strModInfoMaxpsz;

        return MFD_SUCCESS;
}                               /* strModInfoMaxpsz_undo */

/*---------------------------------------------------------------------
 * OPENSS7-STREAMS-MIB::strModInfoEntry.strModInfoHiwat
 * strModInfoHiwat is subid 5 of strModInfoEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.29591.1.1.1.1.1.2.1.1.5
 * Description:
Specifies the high water mark associated with the 
		 driver or module for the queues indicated in the 
		 strModInfoQueues column.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is UNSIGNED32 (based on perltype UNSIGNED32)
 * The net-snmp type is ASN_UNSIGNED. The C type decl is u_long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param strModInfoHiwat_val
 *        A u_long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * strModInfoTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_UNSIGNED
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
strModInfoHiwat_check_value(strModInfoTable_rowreq_ctx * rowreq_ctx, u_long strModInfoHiwat_val)
{
        DEBUGMSGTL(("verbose:strModInfoTable:strModInfoHiwat_check_value", "called\n"));

    /** should never get a NULL pointer */
        netsnmp_assert(NULL != rowreq_ctx);

        /*
         * TODO:441:o: |-> Check for valid strModInfoHiwat value.
         */

        return MFD_SUCCESS;     /* strModInfoHiwat value not illegal */
}                               /* strModInfoHiwat_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (strModInfoTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * strModInfoTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
strModInfoHiwat_undo_setup(strModInfoTable_rowreq_ctx * rowreq_ctx)
{
        DEBUGMSGTL(("verbose:strModInfoTable:strModInfoHiwat_undo_setup", "called\n"));

    /** should never get a NULL pointer */
        netsnmp_assert(NULL != rowreq_ctx);

        /*
         * TODO:455:o: |-> Setup strModInfoHiwat undo.
         */
        /*
         * copy strModInfoHiwat data
         * set rowreq_ctx->undo->strModInfoHiwat from rowreq_ctx->data.strModInfoHiwat
         */
        rowreq_ctx->undo->strModInfoHiwat = rowreq_ctx->data.strModInfoHiwat;

        return MFD_SUCCESS;
}                               /* strModInfoHiwat_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param strModInfoHiwat_val
 *        A u_long containing the new value.
 */
int
strModInfoHiwat_set(strModInfoTable_rowreq_ctx * rowreq_ctx, u_long strModInfoHiwat_val)
{

        DEBUGMSGTL(("verbose:strModInfoTable:strModInfoHiwat_set", "called\n"));

    /** should never get a NULL pointer */
        netsnmp_assert(NULL != rowreq_ctx);

        /*
         * TODO:461:M: |-> Set strModInfoHiwat value.
         * set strModInfoHiwat value in rowreq_ctx->data
         */
        rowreq_ctx->data.strModInfoHiwat = strModInfoHiwat_val;

        return MFD_SUCCESS;
}                               /* strModInfoHiwat_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
strModInfoHiwat_undo(strModInfoTable_rowreq_ctx * rowreq_ctx)
{

        DEBUGMSGTL(("verbose:strModInfoTable:strModInfoHiwat_undo", "called\n"));

        netsnmp_assert(NULL != rowreq_ctx);

        /*
         * TODO:456:o: |-> Clean up strModInfoHiwat undo.
         */
        /*
         * copy strModInfoHiwat data
         * set rowreq_ctx->data.strModInfoHiwat from rowreq_ctx->undo->strModInfoHiwat
         */
        rowreq_ctx->data.strModInfoHiwat = rowreq_ctx->undo->strModInfoHiwat;

        return MFD_SUCCESS;
}                               /* strModInfoHiwat_undo */

/*---------------------------------------------------------------------
 * OPENSS7-STREAMS-MIB::strModInfoEntry.strModInfoLowat
 * strModInfoLowat is subid 6 of strModInfoEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.29591.1.1.1.1.1.2.1.1.6
 * Description:
Specifies the low water mark associated with the 
		 driver or module for the queues indicated in the 
		 strModInfoQueues column.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is UNSIGNED32 (based on perltype UNSIGNED32)
 * The net-snmp type is ASN_UNSIGNED. The C type decl is u_long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param strModInfoLowat_val
 *        A u_long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * strModInfoTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_UNSIGNED
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
strModInfoLowat_check_value(strModInfoTable_rowreq_ctx * rowreq_ctx, u_long strModInfoLowat_val)
{
        DEBUGMSGTL(("verbose:strModInfoTable:strModInfoLowat_check_value", "called\n"));

    /** should never get a NULL pointer */
        netsnmp_assert(NULL != rowreq_ctx);

        /*
         * TODO:441:o: |-> Check for valid strModInfoLowat value.
         */

        return MFD_SUCCESS;     /* strModInfoLowat value not illegal */
}                               /* strModInfoLowat_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (strModInfoTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * strModInfoTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
strModInfoLowat_undo_setup(strModInfoTable_rowreq_ctx * rowreq_ctx)
{
        DEBUGMSGTL(("verbose:strModInfoTable:strModInfoLowat_undo_setup", "called\n"));

    /** should never get a NULL pointer */
        netsnmp_assert(NULL != rowreq_ctx);

        /*
         * TODO:455:o: |-> Setup strModInfoLowat undo.
         */
        /*
         * copy strModInfoLowat data
         * set rowreq_ctx->undo->strModInfoLowat from rowreq_ctx->data.strModInfoLowat
         */
        rowreq_ctx->undo->strModInfoLowat = rowreq_ctx->data.strModInfoLowat;

        return MFD_SUCCESS;
}                               /* strModInfoLowat_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param strModInfoLowat_val
 *        A u_long containing the new value.
 */
int
strModInfoLowat_set(strModInfoTable_rowreq_ctx * rowreq_ctx, u_long strModInfoLowat_val)
{

        DEBUGMSGTL(("verbose:strModInfoTable:strModInfoLowat_set", "called\n"));

    /** should never get a NULL pointer */
        netsnmp_assert(NULL != rowreq_ctx);

        /*
         * TODO:461:M: |-> Set strModInfoLowat value.
         * set strModInfoLowat value in rowreq_ctx->data
         */
        rowreq_ctx->data.strModInfoLowat = strModInfoLowat_val;

        return MFD_SUCCESS;
}                               /* strModInfoLowat_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
strModInfoLowat_undo(strModInfoTable_rowreq_ctx * rowreq_ctx)
{

        DEBUGMSGTL(("verbose:strModInfoTable:strModInfoLowat_undo", "called\n"));

        netsnmp_assert(NULL != rowreq_ctx);

        /*
         * TODO:456:o: |-> Clean up strModInfoLowat undo.
         */
        /*
         * copy strModInfoLowat data
         * set rowreq_ctx->data.strModInfoLowat from rowreq_ctx->undo->strModInfoLowat
         */
        rowreq_ctx->data.strModInfoLowat = rowreq_ctx->undo->strModInfoLowat;

        return MFD_SUCCESS;
}                               /* strModInfoLowat_undo */

/*---------------------------------------------------------------------
 * OPENSS7-STREAMS-MIB::strModInfoEntry.strModInfoTraceLevel
 * strModInfoTraceLevel is subid 7 of strModInfoEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.29591.1.1.1.1.1.2.1.1.7
 * Description:
Specifies the trace level associated with the driver or 
		 module for the queues indicated in the strModInfoQueues 
		 column.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is UNSIGNED32 (based on perltype UNSIGNED32)
 * The net-snmp type is ASN_UNSIGNED. The C type decl is u_long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param strModInfoTraceLevel_val
 *        A u_long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * strModInfoTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_UNSIGNED
 *    The value is in (one of) the range set(s):  0 - 255
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
strModInfoTraceLevel_check_value(strModInfoTable_rowreq_ctx * rowreq_ctx,
                                 u_long strModInfoTraceLevel_val)
{
        DEBUGMSGTL(("verbose:strModInfoTable:strModInfoTraceLevel_check_value", "called\n"));

    /** should never get a NULL pointer */
        netsnmp_assert(NULL != rowreq_ctx);

        /*
         * TODO:441:o: |-> Check for valid strModInfoTraceLevel value.
         */

        return MFD_SUCCESS;     /* strModInfoTraceLevel value not illegal */
}                               /* strModInfoTraceLevel_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (strModInfoTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * strModInfoTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
strModInfoTraceLevel_undo_setup(strModInfoTable_rowreq_ctx * rowreq_ctx)
{
        DEBUGMSGTL(("verbose:strModInfoTable:strModInfoTraceLevel_undo_setup", "called\n"));

    /** should never get a NULL pointer */
        netsnmp_assert(NULL != rowreq_ctx);

        /*
         * TODO:455:o: |-> Setup strModInfoTraceLevel undo.
         */
        /*
         * copy strModInfoTraceLevel data
         * set rowreq_ctx->undo->strModInfoTraceLevel from rowreq_ctx->data.strModInfoTraceLevel
         */
        rowreq_ctx->undo->strModInfoTraceLevel = rowreq_ctx->data.strModInfoTraceLevel;

        return MFD_SUCCESS;
}                               /* strModInfoTraceLevel_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param strModInfoTraceLevel_val
 *        A u_long containing the new value.
 */
int
strModInfoTraceLevel_set(strModInfoTable_rowreq_ctx * rowreq_ctx, u_long strModInfoTraceLevel_val)
{

        DEBUGMSGTL(("verbose:strModInfoTable:strModInfoTraceLevel_set", "called\n"));

    /** should never get a NULL pointer */
        netsnmp_assert(NULL != rowreq_ctx);

        /*
         * TODO:461:M: |-> Set strModInfoTraceLevel value.
         * set strModInfoTraceLevel value in rowreq_ctx->data
         */
        rowreq_ctx->data.strModInfoTraceLevel = strModInfoTraceLevel_val;

        return MFD_SUCCESS;
}                               /* strModInfoTraceLevel_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
strModInfoTraceLevel_undo(strModInfoTable_rowreq_ctx * rowreq_ctx)
{

        DEBUGMSGTL(("verbose:strModInfoTable:strModInfoTraceLevel_undo", "called\n"));

        netsnmp_assert(NULL != rowreq_ctx);

        /*
         * TODO:456:o: |-> Clean up strModInfoTraceLevel undo.
         */
        /*
         * copy strModInfoTraceLevel data
         * set rowreq_ctx->data.strModInfoTraceLevel from rowreq_ctx->undo->strModInfoTraceLevel
         */
        rowreq_ctx->data.strModInfoTraceLevel = rowreq_ctx->undo->strModInfoTraceLevel;

        return MFD_SUCCESS;
}                               /* strModInfoTraceLevel_undo */

/** @} */
