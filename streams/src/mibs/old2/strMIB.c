/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.49 $ of : mfd-top.m2c,v $ 
 *
 * $Id:$
 */
/** \mainpage MFD helper for strMIB
 *
 * \section intro Introduction
 * Introductory text.
 *
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "strMIB.h"

#include <net-snmp/agent/mib_modules.h>

#include "strMIB_interface.h"

oid strModTable_oid[] = { STRMODTABLE_OID };
int strModTable_oid_size = OID_LENGTH(strModTable_oid);

strModTable_registration strModTable_user_context;

void initialize_table_strModTable(void);
void shutdown_table_strModTable(void);

oid strModInfoTable_oid[] = { STRMODINFOTABLE_OID };
int strModInfoTable_oid_size = OID_LENGTH(strModInfoTable_oid);

strModInfoTable_registration strModInfoTable_user_context;

void initialize_table_strModInfoTable(void);
void shutdown_table_strModInfoTable(void);

oid strModStatTable_oid[] = { STRMODSTATTABLE_OID };
int strModStatTable_oid_size = OID_LENGTH(strModStatTable_oid);

strModStatTable_registration strModStatTable_user_context;

void initialize_table_strModStatTable(void);
void shutdown_table_strModStatTable(void);

oid strApshTable_oid[] = { STRAPSHTABLE_OID };
int strApshTable_oid_size = OID_LENGTH(strApshTable_oid);

strApshTable_registration strApshTable_user_context;

void initialize_table_strApshTable(void);
void shutdown_table_strApshTable(void);

oid strStatsTable_oid[] = { STRSTATSTABLE_OID };
int strStatsTable_oid_size = OID_LENGTH(strStatsTable_oid);

strStatsTable_registration strStatsTable_user_context;

void initialize_table_strStatsTable(void);
void shutdown_table_strStatsTable(void);

/**
 * Initializes the strMIB module
 */
void
init_strMIB(void)
{
        DEBUGMSGTL(("verbose:strMIB:init_strMIB", "called\n"));

        /*
         * TODO:300:o: Perform strMIB one-time module initialization.
         */

        /*
         * here we initialize all the tables we're planning on supporting
         */
        if (should_init("strStatsTable"))
                initialize_table_strStatsTable();

        if (should_init("strStatsTable"))
                initialize_table_strStatsTable();

        if (should_init("strStatsTable"))
                initialize_table_strStatsTable();

        if (should_init("strStatsTable"))
                initialize_table_strStatsTable();

        if (should_init("strStatsTable"))
                initialize_table_strStatsTable();

}                               /* init_strMIB */

/**
 * Shut-down the strMIB module (agent is exiting)
 */
void
shutdown_strMIB(void)
{
        if (should_init("strStatsTable"))
                shutdown_table_strStatsTable();

        if (should_init("strStatsTable"))
                shutdown_table_strStatsTable();

        if (should_init("strStatsTable"))
                shutdown_table_strStatsTable();

        if (should_init("strStatsTable"))
                shutdown_table_strStatsTable();

        if (should_init("strStatsTable"))
                shutdown_table_strStatsTable();

}

/**
 * Initialize the table strModTable 
 *    (Define its contents and how it's structured)
 */
void
initialize_table_strModTable(void)
{
        strModTable_registration *user_context;
        u_long flags;

        DEBUGMSGTL(("verbose:strModTable:initialize_table_strModTable", "called\n"));

        /*
         * TODO:301:o: Perform strModTable one-time table initialization.
         */

        /*
         * TODO:302:o: |->Initialize strModTable user context
         * if you'd like to pass in a pointer to some data for this
         * table, allocate or set it up here.
         */
        /*
         * a netsnmp_data_list is a simple way to store void pointers. A simple
         * string token is used to add, find or remove pointers.
         */
        user_context = netsnmp_create_data_list("strModTable", NULL, NULL);

        /*
         * No support for any flags yet, but in the future you would
         * set any flags here.
         */
        flags = 0;

        /*
         * call interface initialization code
         */
        _strModTable_initialize_interface(user_context, flags);
}                               /* initialize_table_strModTable */

/**
 * Shutdown the table strModTable 
 */
void
shutdown_table_strModTable(void)
{
        /*
         * call interface shutdown code
         */
        _strModTable_shutdown_interface(&strModTable_user_context);
}

/**
 * extra context initialization (eg default values)
 *
 * @param rowreq_ctx    : row request context
 * @param user_init_ctx : void pointer for user (parameter to rowreq_ctx_allocate)
 *
 * @retval MFD_SUCCESS  : no errors
 * @retval MFD_ERROR    : error (context allocate will fail)
 */
int
strModTable_rowreq_ctx_init(strModTable_rowreq_ctx * rowreq_ctx, void *user_init_ctx)
{
        DEBUGMSGTL(("verbose:strModTable:strModTable_rowreq_ctx_init", "called\n"));

        netsnmp_assert(NULL != rowreq_ctx);

        /*
         * TODO:210:o: |-> Perform extra strModTable rowreq initialization. (eg DEFVALS)
         */

        return MFD_SUCCESS;
}                               /* strModTable_rowreq_ctx_init */

/**
 * extra context cleanup
 *
 */
void
strModTable_rowreq_ctx_cleanup(strModTable_rowreq_ctx * rowreq_ctx)
{
        DEBUGMSGTL(("verbose:strModTable:strModTable_rowreq_ctx_cleanup", "called\n"));

        netsnmp_assert(NULL != rowreq_ctx);

        /*
         * TODO:211:o: |-> Perform extra strModTable rowreq cleanup.
         */
}                               /* strModTable_rowreq_ctx_cleanup */

/**
 * pre-request callback
 *
 *
 * @retval MFD_SUCCESS              : success.
 * @retval MFD_ERROR                : other error
 */
int
strModTable_pre_request(strModTable_registration * user_context)
{
        DEBUGMSGTL(("verbose:strModTable:strModTable_pre_request", "called\n"));

        /*
         * TODO:510:o: Perform strModTable pre-request actions.
         */

        return MFD_SUCCESS;
}                               /* strModTable_pre_request */

/**
 * post-request callback
 *
 * Note:
 *   New rows have been inserted into the container, and
 *   deleted rows have been removed from the container and
 *   released.
 *
 * @param rc : MFD_SUCCESS if all requests succeeded
 *
 * @retval MFD_SUCCESS : success.
 * @retval MFD_ERROR   : other error (ignored)
 */
int
strModTable_post_request(strModTable_registration * user_context, int rc)
{
        DEBUGMSGTL(("verbose:strModTable:strModTable_post_request", "called\n"));

        /*
         * TODO:511:o: Perform strModTable post-request actions.
         */

        return MFD_SUCCESS;
}                               /* strModTable_post_request */

/**
 * Initialize the table strModInfoTable 
 *    (Define its contents and how it's structured)
 */
void
initialize_table_strModInfoTable(void)
{
        strModInfoTable_registration *user_context;
        u_long flags;

        DEBUGMSGTL(("verbose:strModInfoTable:initialize_table_strModInfoTable", "called\n"));

        /*
         * TODO:301:o: Perform strModInfoTable one-time table initialization.
         */

        /*
         * TODO:302:o: |->Initialize strModInfoTable user context
         * if you'd like to pass in a pointer to some data for this
         * table, allocate or set it up here.
         */
        /*
         * a netsnmp_data_list is a simple way to store void pointers. A simple
         * string token is used to add, find or remove pointers.
         */
        user_context = netsnmp_create_data_list("strModInfoTable", NULL, NULL);

        /*
         * No support for any flags yet, but in the future you would
         * set any flags here.
         */
        flags = 0;

        /*
         * call interface initialization code
         */
        _strModInfoTable_initialize_interface(user_context, flags);
}                               /* initialize_table_strModInfoTable */

/**
 * Shutdown the table strModInfoTable 
 */
void
shutdown_table_strModInfoTable(void)
{
        /*
         * call interface shutdown code
         */
        _strModInfoTable_shutdown_interface(&strModInfoTable_user_context);
}

/**
 * extra context initialization (eg default values)
 *
 * @param rowreq_ctx    : row request context
 * @param user_init_ctx : void pointer for user (parameter to rowreq_ctx_allocate)
 *
 * @retval MFD_SUCCESS  : no errors
 * @retval MFD_ERROR    : error (context allocate will fail)
 */
int
strModInfoTable_rowreq_ctx_init(strModInfoTable_rowreq_ctx * rowreq_ctx, void *user_init_ctx)
{
        DEBUGMSGTL(("verbose:strModInfoTable:strModInfoTable_rowreq_ctx_init", "called\n"));

        netsnmp_assert(NULL != rowreq_ctx);

        /*
         * TODO:210:o: |-> Perform extra strModInfoTable rowreq initialization. (eg DEFVALS)
         */

        return MFD_SUCCESS;
}                               /* strModInfoTable_rowreq_ctx_init */

/**
 * extra context cleanup
 *
 */
void
strModInfoTable_rowreq_ctx_cleanup(strModInfoTable_rowreq_ctx * rowreq_ctx)
{
        DEBUGMSGTL(("verbose:strModInfoTable:strModInfoTable_rowreq_ctx_cleanup", "called\n"));

        netsnmp_assert(NULL != rowreq_ctx);

        /*
         * TODO:211:o: |-> Perform extra strModInfoTable rowreq cleanup.
         */
}                               /* strModInfoTable_rowreq_ctx_cleanup */

/************************************************************
 * the *_should_save routine is called to determine if a row
 * should be stored persistently.
 *
 * Note that this is not a 'dirty' check (i.e. if a row has changed),
 * but a check for volatile rows that should not be saved between
 * restarts.
 *
 * return 1 if the row should be stored
 * return 0 if the row should not be stored
 */
int
strModInfoTable_container_should_save(strModInfoTable_rowreq_ctx * rowreq_ctx)
{

        return 1;               /* save the row */
}

/**
 * pre-request callback
 *
 *
 * @retval MFD_SUCCESS              : success.
 * @retval MFD_ERROR                : other error
 */
int
strModInfoTable_pre_request(strModInfoTable_registration * user_context)
{
        DEBUGMSGTL(("verbose:strModInfoTable:strModInfoTable_pre_request", "called\n"));

        /*
         * TODO:510:o: Perform strModInfoTable pre-request actions.
         */

        return MFD_SUCCESS;
}                               /* strModInfoTable_pre_request */

/**
 * post-request callback
 *
 * Note:
 *   New rows have been inserted into the container, and
 *   deleted rows have been removed from the container and
 *   released.
 *
 * @param rc : MFD_SUCCESS if all requests succeeded
 *
 * @retval MFD_SUCCESS : success.
 * @retval MFD_ERROR   : other error (ignored)
 */
int
strModInfoTable_post_request(strModInfoTable_registration * user_context, int rc)
{
        DEBUGMSGTL(("verbose:strModInfoTable:strModInfoTable_post_request", "called\n"));

        /*
         * TODO:511:o: Perform strModInfoTable post-request actions.
         */

        /*
         * check to set if any rows were changed.
         */
        if (strModInfoTable_dirty_get()) {
                /*
                 * check if request was successful. If so, this would be
                 * a good place to save data to its persistent store.
                 */
                if (MFD_SUCCESS == rc) {
                        /*
                         * save changed rows, if you haven't already
                         */
                        snmp_store(netsnmp_ds_get_string(NETSNMP_DS_LIBRARY_ID,
                                                         NETSNMP_DS_LIB_APPTYPE));
                }

                strModInfoTable_dirty_set(0);   /* clear table dirty flag */
        }

        return MFD_SUCCESS;
}                               /* strModInfoTable_post_request */

/**
 * Initialize the table strModStatTable 
 *    (Define its contents and how it's structured)
 */
void
initialize_table_strModStatTable(void)
{
        strModStatTable_registration *user_context;
        u_long flags;

        DEBUGMSGTL(("verbose:strModStatTable:initialize_table_strModStatTable", "called\n"));

        /*
         * TODO:301:o: Perform strModStatTable one-time table initialization.
         */

        /*
         * TODO:302:o: |->Initialize strModStatTable user context
         * if you'd like to pass in a pointer to some data for this
         * table, allocate or set it up here.
         */
        /*
         * a netsnmp_data_list is a simple way to store void pointers. A simple
         * string token is used to add, find or remove pointers.
         */
        user_context = netsnmp_create_data_list("strModStatTable", NULL, NULL);

        /*
         * No support for any flags yet, but in the future you would
         * set any flags here.
         */
        flags = 0;

        /*
         * call interface initialization code
         */
        _strModStatTable_initialize_interface(user_context, flags);
}                               /* initialize_table_strModStatTable */

/**
 * Shutdown the table strModStatTable 
 */
void
shutdown_table_strModStatTable(void)
{
        /*
         * call interface shutdown code
         */
        _strModStatTable_shutdown_interface(&strModStatTable_user_context);
}

/**
 * extra context initialization (eg default values)
 *
 * @param rowreq_ctx    : row request context
 * @param user_init_ctx : void pointer for user (parameter to rowreq_ctx_allocate)
 *
 * @retval MFD_SUCCESS  : no errors
 * @retval MFD_ERROR    : error (context allocate will fail)
 */
int
strModStatTable_rowreq_ctx_init(strModStatTable_rowreq_ctx * rowreq_ctx, void *user_init_ctx)
{
        DEBUGMSGTL(("verbose:strModStatTable:strModStatTable_rowreq_ctx_init", "called\n"));

        netsnmp_assert(NULL != rowreq_ctx);

        /*
         * TODO:210:o: |-> Perform extra strModStatTable rowreq initialization. (eg DEFVALS)
         */

        return MFD_SUCCESS;
}                               /* strModStatTable_rowreq_ctx_init */

/**
 * extra context cleanup
 *
 */
void
strModStatTable_rowreq_ctx_cleanup(strModStatTable_rowreq_ctx * rowreq_ctx)
{
        DEBUGMSGTL(("verbose:strModStatTable:strModStatTable_rowreq_ctx_cleanup", "called\n"));

        netsnmp_assert(NULL != rowreq_ctx);

        /*
         * TODO:211:o: |-> Perform extra strModStatTable rowreq cleanup.
         */
}                               /* strModStatTable_rowreq_ctx_cleanup */

/**
 * pre-request callback
 *
 *
 * @retval MFD_SUCCESS              : success.
 * @retval MFD_ERROR                : other error
 */
int
strModStatTable_pre_request(strModStatTable_registration * user_context)
{
        DEBUGMSGTL(("verbose:strModStatTable:strModStatTable_pre_request", "called\n"));

        /*
         * TODO:510:o: Perform strModStatTable pre-request actions.
         */

        return MFD_SUCCESS;
}                               /* strModStatTable_pre_request */

/**
 * post-request callback
 *
 * Note:
 *   New rows have been inserted into the container, and
 *   deleted rows have been removed from the container and
 *   released.
 *
 * @param rc : MFD_SUCCESS if all requests succeeded
 *
 * @retval MFD_SUCCESS : success.
 * @retval MFD_ERROR   : other error (ignored)
 */
int
strModStatTable_post_request(strModStatTable_registration * user_context, int rc)
{
        DEBUGMSGTL(("verbose:strModStatTable:strModStatTable_post_request", "called\n"));

        /*
         * TODO:511:o: Perform strModStatTable post-request actions.
         */

        return MFD_SUCCESS;
}                               /* strModStatTable_post_request */

/**
 * Initialize the table strApshTable 
 *    (Define its contents and how it's structured)
 */
void
initialize_table_strApshTable(void)
{
        strApshTable_registration *user_context;
        u_long flags;

        DEBUGMSGTL(("verbose:strApshTable:initialize_table_strApshTable", "called\n"));

        /*
         * TODO:301:o: Perform strApshTable one-time table initialization.
         */

        /*
         * TODO:302:o: |->Initialize strApshTable user context
         * if you'd like to pass in a pointer to some data for this
         * table, allocate or set it up here.
         */
        /*
         * a netsnmp_data_list is a simple way to store void pointers. A simple
         * string token is used to add, find or remove pointers.
         */
        user_context = netsnmp_create_data_list("strApshTable", NULL, NULL);

        /*
         * No support for any flags yet, but in the future you would
         * set any flags here.
         */
        flags = 0;

        /*
         * call interface initialization code
         */
        _strApshTable_initialize_interface(user_context, flags);
}                               /* initialize_table_strApshTable */

/**
 * Shutdown the table strApshTable 
 */
void
shutdown_table_strApshTable(void)
{
        /*
         * call interface shutdown code
         */
        _strApshTable_shutdown_interface(&strApshTable_user_context);
}

/**
 * extra context initialization (eg default values)
 *
 * @param rowreq_ctx    : row request context
 * @param user_init_ctx : void pointer for user (parameter to rowreq_ctx_allocate)
 *
 * @retval MFD_SUCCESS  : no errors
 * @retval MFD_ERROR    : error (context allocate will fail)
 */
int
strApshTable_rowreq_ctx_init(strApshTable_rowreq_ctx * rowreq_ctx, void *user_init_ctx)
{
        DEBUGMSGTL(("verbose:strApshTable:strApshTable_rowreq_ctx_init", "called\n"));

        netsnmp_assert(NULL != rowreq_ctx);

        /*
         * TODO:210:o: |-> Perform extra strApshTable rowreq initialization. (eg DEFVALS)
         */

        return MFD_SUCCESS;
}                               /* strApshTable_rowreq_ctx_init */

/**
 * extra context cleanup
 *
 */
void
strApshTable_rowreq_ctx_cleanup(strApshTable_rowreq_ctx * rowreq_ctx)
{
        DEBUGMSGTL(("verbose:strApshTable:strApshTable_rowreq_ctx_cleanup", "called\n"));

        netsnmp_assert(NULL != rowreq_ctx);

        /*
         * TODO:211:o: |-> Perform extra strApshTable rowreq cleanup.
         */
}                               /* strApshTable_rowreq_ctx_cleanup */

/************************************************************
 * the *_should_save routine is called to determine if a row
 * should be stored persistently.
 *
 * Note that this is not a 'dirty' check (i.e. if a row has changed),
 * but a check for volatile rows that should not be saved between
 * restarts.
 *
 * return 1 if the row should be stored
 * return 0 if the row should not be stored
 */
int
strApshTable_container_should_save(strApshTable_rowreq_ctx * rowreq_ctx)
{

        return 1;               /* save the row */
}

/**
 * pre-request callback
 *
 *
 * @retval MFD_SUCCESS              : success.
 * @retval MFD_ERROR                : other error
 */
int
strApshTable_pre_request(strApshTable_registration * user_context)
{
        DEBUGMSGTL(("verbose:strApshTable:strApshTable_pre_request", "called\n"));

        /*
         * TODO:510:o: Perform strApshTable pre-request actions.
         */

        return MFD_SUCCESS;
}                               /* strApshTable_pre_request */

/**
 * post-request callback
 *
 * Note:
 *   New rows have been inserted into the container, and
 *   deleted rows have been removed from the container and
 *   released.
 *
 * @param rc : MFD_SUCCESS if all requests succeeded
 *
 * @retval MFD_SUCCESS : success.
 * @retval MFD_ERROR   : other error (ignored)
 */
int
strApshTable_post_request(strApshTable_registration * user_context, int rc)
{
        DEBUGMSGTL(("verbose:strApshTable:strApshTable_post_request", "called\n"));

        /*
         * TODO:511:o: Perform strApshTable post-request actions.
         */

        /*
         * check to set if any rows were changed.
         */
        if (strApshTable_dirty_get()) {
                /*
                 * check if request was successful. If so, this would be
                 * a good place to save data to its persistent store.
                 */
                if (MFD_SUCCESS == rc) {
                        /*
                         * save changed rows, if you haven't already
                         */
                        snmp_store(netsnmp_ds_get_string(NETSNMP_DS_LIBRARY_ID,
                                                         NETSNMP_DS_LIB_APPTYPE));
                }

                strApshTable_dirty_set(0);      /* clear table dirty flag */
        }

        return MFD_SUCCESS;
}                               /* strApshTable_post_request */

/**
 * Initialize the table strStatsTable 
 *    (Define its contents and how it's structured)
 */
void
initialize_table_strStatsTable(void)
{
        strStatsTable_registration *user_context;
        u_long flags;

        DEBUGMSGTL(("verbose:strStatsTable:initialize_table_strStatsTable", "called\n"));

        /*
         * TODO:301:o: Perform strStatsTable one-time table initialization.
         */

        /*
         * TODO:302:o: |->Initialize strStatsTable user context
         * if you'd like to pass in a pointer to some data for this
         * table, allocate or set it up here.
         */
        /*
         * a netsnmp_data_list is a simple way to store void pointers. A simple
         * string token is used to add, find or remove pointers.
         */
        user_context = netsnmp_create_data_list("strStatsTable", NULL, NULL);

        /*
         * No support for any flags yet, but in the future you would
         * set any flags here.
         */
        flags = 0;

        /*
         * call interface initialization code
         */
        _strStatsTable_initialize_interface(user_context, flags);
}                               /* initialize_table_strStatsTable */

/**
 * Shutdown the table strStatsTable 
 */
void
shutdown_table_strStatsTable(void)
{
        /*
         * call interface shutdown code
         */
        _strStatsTable_shutdown_interface(&strStatsTable_user_context);
}

/**
 * extra context initialization (eg default values)
 *
 * @param rowreq_ctx    : row request context
 * @param user_init_ctx : void pointer for user (parameter to rowreq_ctx_allocate)
 *
 * @retval MFD_SUCCESS  : no errors
 * @retval MFD_ERROR    : error (context allocate will fail)
 */
int
strStatsTable_rowreq_ctx_init(strStatsTable_rowreq_ctx * rowreq_ctx, void *user_init_ctx)
{
        DEBUGMSGTL(("verbose:strStatsTable:strStatsTable_rowreq_ctx_init", "called\n"));

        netsnmp_assert(NULL != rowreq_ctx);

        /*
         * TODO:210:o: |-> Perform extra strStatsTable rowreq initialization. (eg DEFVALS)
         */

        return MFD_SUCCESS;
}                               /* strStatsTable_rowreq_ctx_init */

/**
 * extra context cleanup
 *
 */
void
strStatsTable_rowreq_ctx_cleanup(strStatsTable_rowreq_ctx * rowreq_ctx)
{
        DEBUGMSGTL(("verbose:strStatsTable:strStatsTable_rowreq_ctx_cleanup", "called\n"));

        netsnmp_assert(NULL != rowreq_ctx);

        /*
         * TODO:211:o: |-> Perform extra strStatsTable rowreq cleanup.
         */
}                               /* strStatsTable_rowreq_ctx_cleanup */

/**
 * pre-request callback
 *
 *
 * @retval MFD_SUCCESS              : success.
 * @retval MFD_ERROR                : other error
 */
int
strStatsTable_pre_request(strStatsTable_registration * user_context)
{
        DEBUGMSGTL(("verbose:strStatsTable:strStatsTable_pre_request", "called\n"));

        /*
         * TODO:510:o: Perform strStatsTable pre-request actions.
         */

        return MFD_SUCCESS;
}                               /* strStatsTable_pre_request */

/**
 * post-request callback
 *
 * Note:
 *   New rows have been inserted into the container, and
 *   deleted rows have been removed from the container and
 *   released.
 *
 * @param rc : MFD_SUCCESS if all requests succeeded
 *
 * @retval MFD_SUCCESS : success.
 * @retval MFD_ERROR   : other error (ignored)
 */
int
strStatsTable_post_request(strStatsTable_registration * user_context, int rc)
{
        DEBUGMSGTL(("verbose:strStatsTable:strStatsTable_post_request", "called\n"));

        /*
         * TODO:511:o: Perform strStatsTable post-request actions.
         */

        return MFD_SUCCESS;
}                               /* strStatsTable_post_request */

/** @{ */
