/*****************************************************************************

 @(#) $RCSfile: ds1EXT.c,v $ $Name:  $($Revision: 0.9.2.14 $) $Date: 2009-01-16 20:40:56 $

 -----------------------------------------------------------------------------

 Copyright (c) 2008-2009  Monavacon Limited <http://www.monavacon.com/>
 Copyright (c) 2001-2008  OpenSS7 Corporation <http://www.openss7.com/>
 Copyright (c) 1997-2001  Brian F. G. Bidulock <bidulock@openss7.org>

 All Rights Reserved.

 This program is free software: you can redistribute it and/or modify it under
 the terms of the GNU Affero General Public License as published by the Free
 Software Foundation, version 3 of the license.

 This program is distributed in the hope that it will be useful, but WITHOUT
 ANY WARRANTY; without even the implied warranty of MERCHANTABILITY or FITNESS
 FOR A PARTICULAR PURPOSE.  See the GNU Affero General Public License for more
 details.

 You should have received a copy of the GNU Affero General Public License
 along with this program.  If not, see <http://www.gnu.org/licenses/>, or
 write to the Free Software Foundation, Inc., 675 Mass Ave, Cambridge, MA
 02139, USA.

 -----------------------------------------------------------------------------

 U.S. GOVERNMENT RESTRICTED RIGHTS.  If you are licensing this Software on
 behalf of the U.S. Government ("Government"), the following provisions apply
 to you.  If the Software is supplied by the Department of Defense ("DoD"), it
 is classified as "Commercial Computer Software" under paragraph 252.227-7014
 of the DoD Supplement to the Federal Acquisition Regulations ("DFARS") (or any
 successor regulations) and the Government is acquiring only the license rights
 granted herein (the license rights customarily provided to non-Government
 users).  If the Software is supplied to any unit or agency of the Government
 other than DoD, it is classified as "Restricted Computer Software" and the
 Government's rights in the Software are defined in paragraph 52.227-19 of the
 Federal Acquisition Regulations ("FAR") (or any successor regulations) or, in
 the cases of NASA, in paragraph 18.52.227-86 of the NASA Supplement to the FAR
 (or any successor regulations).

 -----------------------------------------------------------------------------

 Commercial licensing and support of this software is available from OpenSS7
 Corporation at a fee.  See http://www.openss7.com/

 -----------------------------------------------------------------------------

 Last Modified $Date: 2009-01-16 20:40:56 $ by $Author: brian $

 -----------------------------------------------------------------------------

 $Log: ds1EXT.c,v $
 Revision 0.9.2.14  2009-01-16 20:40:56  brian
 - working up mibs and agents

 Revision 0.9.2.13  2009-01-14 14:31:20  brian
 - working up agents

 Revision 0.9.2.12  2009-01-10 17:46:46  brian
 - updated agents and mibs

 Revision 0.9.2.11  2009-01-04 13:16:11  brian
 - updated agents

 Revision 0.9.2.10  2009-01-03 10:51:10  brian
 - updated agent

 Revision 0.9.2.9  2009-01-02 15:46:45  brian
 - corrections

 Revision 0.9.2.8  2009-01-02 15:04:18  brian
 - updated agents

 Revision 0.9.2.7  2008-12-31 16:04:50  brian
 - updated mibs

 Revision 0.9.2.6  2008-12-24 13:40:56  brian
 - updated mibs

 Revision 0.9.2.5  2008-07-31 17:40:16  brian
 - SNMP updates

 Revision 0.9.2.4  2008-04-28 23:39:59  brian
 - updated headers for release

 Revision 0.9.2.3  2008/01/14 19:55:03  brian
 - updated stub files

 Revision 0.9.2.2  2008/01/03 12:45:59  brian
 - updates for new mib2c

 Revision 0.9.2.1  2008/01/01 13:59:37  brian
 - added new mibs

 *****************************************************************************/

#ident "@(#) $RCSfile: ds1EXT.c,v $ $Name:  $($Revision: 0.9.2.14 $) $Date: 2009-01-16 20:40:56 $"

static char const ident[] = "$RCSfile: ds1EXT.c,v $ $Name:  $($Revision: 0.9.2.14 $) $Date: 2009-01-16 20:40:56 $";

/* This file was generated by mib2c and is intended for use as
   a mib module for the ucd-snmp snmpd agent. */
#include <ucd-snmp/ucd-snmp-config.h>
#include <ucd-snmp/ucd-snmp-includes.h>
#include <ucd-snmp/ucd-snmp-agent-includes.h>
#include <ucd-snmp/agent_trap.h>
#include <ucd-snmp/callback.h>
#include <ucd-snmp/snmp-tc.h>
#include <ucd-snmp/default_store.h>
#include <ucd-snmp/snmp_alarm.h>
/* The following header files are mangled in most recent net-snmp releases so
 * the versions from UCD-SNMP 4.2.5 are included here.  */
#if defined HAVE_LIBNETSNMP
#else				/* defined HAVE_LIBNETSNMP */
#endif				/* defined HAVE_LIBNETSNMP */
/* These are messed up on both. */
#include "ds_agent.h"
#ifdef HAVE_UCD_SNMP_UTIL_FUNCS_H
#include <ucd-snmp/util_funcs.h>
/* Many recent net-snmp UCD compatible headers do not declare header_generic. */
int header_generic(struct variable *, oid *, size_t *, int, size_t *, WriteMethod **);
#else				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#include "util_funcs.h"
#endif				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#ifdef HAVE_UCD_SNMP_HEADER_COMPLEX_H
#include <ucd-snmp/header_complex.h>
#else				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
#include "header_complex.h"
#endif				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
/* This one is the other way around: it is just fine for net-snmp, but
 * ucd-snmp does not provide the header file at all.  */
#ifdef HAVE_UCD_SNMP_MIB_MODULES_H
#include <ucd-snmp/mib_modules.h>
#else				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#ifdef HAVE_NET_SNMP_AGENT_MIB_MODULES_H
#include <net-snmp/agent/mib_modules.h>
#else				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#include "mib_modules.h"
#endif				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#endif				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#include <stdint.h>
#include <signal.h>
#include <sys/stat.h>		/* for struct stat, fstat() */
#include <sys/types.h>
#include <unistd.h>
#include <stdlib.h>
#include <pwd.h>		/* for getpwuid() getpwnam() */
#include <grp.h>		/* for getgrgid() getgrnam() */
#include <libgen.h>		/* for basename() */
#include <fcntl.h>		/* for O_CREAT */
#include <sys/sysctl.h>		/* for sysctl */
#include <errno.h>
#include <string.h>
#ifdef _GNU_SOURCE
#include <getopt.h>
#endif
#include "ds1EXT.h"
extern const char sa_program[];

#define MY_FACILITY(__pri)	(LOG_DAEMON|(__pri))
#undef MASTER
#if !defined MODULE
extern int sa_dump;			/* default packet dump */
extern int sa_debug;			/* default no debug */
extern int sa_nomead;			/* default daemon mode */
extern int sa_output;			/* default normal output */
extern int sa_agentx;			/* default agentx mode */
extern int sa_alarms;			/* default application alarms */
extern int sa_logaddr;			/* log addresses */
extern int sa_logfillog;		/* log to sa_logfile */
extern int sa_logstderr;		/* log to standard error */
extern int sa_logstdout;		/* log to standard output */
extern int sa_logsyslog;		/* log to system logs */
extern int sa_logcallog;		/* log to callback logs */
extern int sa_appendlog;		/* append to log file without truncating */
extern char sa_logfile[256];
extern char sa_pidfile[256];
extern char sa_sysctlf[256];

/* file stream for log file */
extern FILE *stdlog;
#endif				/* !defined MODULE */
extern int sa_fclose;			/* default close files between requests */
static int my_fd;			/* file descriptor for this MIB's use */
extern int sa_fd;			/* file descriptor for MIB use */
extern int sa_readfd;			/* file descriptor for autonomnous events */
extern int sa_changed;			/* indication to reread MIB configuration */
extern int sa_stats_refresh;		/* indications that statistics, the mib or its tables need to be refreshed */
extern int sa_request;			/* request number for per-request actions */
volatile int ds1EXT_refresh = 1;
volatile int dsx1ConfigTable_refresh = 1;
volatile int dsx1CurrentTable_refresh = 1;
volatile int dsx1IntervalTable_refresh = 1;
volatile int dsx1TotalTable_refresh = 1;
volatile int dsx1FarEndCurrentTable_refresh = 1;
volatile int dsx1FarEndIntervalTable_refresh = 1;
volatile int dsx1FarEndTotalTable_refresh = 1;
volatile int dsx1FracTable_refresh = 1;
volatile int dsx1ChanMappingTable_refresh = 1;

/*
 * ds1EXT_variables_oid: object identifier for ds1EXT
 * This is the top level oid that we want to register under.  This is essentially a prefix, with the
 * suffix appearing in the variable below.
 */
oid ds1EXT_variables_oid[8] = { 1, 3, 6, 1, 2, 1, 10, 18 };

/*
 * Oids for use in notifications defined in this MIB.
 */
oid dsx1LineStatusChange_oid[11] = { 1, 3, 6, 1, 2, 1, 10, 18, 15, 0, 1 };

/*
 * Oids accessible only for notify defined in this MIB.
 */

/*
 * Other oids defined in this MIB.
 */
oid ds1NearEndConfigGroup_oid[11] = { 1, 3, 6, 1, 2, 1, 10, 18, 14, 1, 1 };
oid ds1NearEndStatisticsGroup_oid[11] = { 1, 3, 6, 1, 2, 1, 10, 18, 14, 1, 2 };
oid ds1FarEndGroup_oid[11] = { 1, 3, 6, 1, 2, 1, 10, 18, 14, 1, 3 };
oid ds1DeprecatedGroup_oid[11] = { 1, 3, 6, 1, 2, 1, 10, 18, 14, 1, 4 };
oid ds1NearEndOptionalConfigGroup_oid[11] = { 1, 3, 6, 1, 2, 1, 10, 18, 14, 1, 5 };
oid ds1DS2Group_oid[11] = { 1, 3, 6, 1, 2, 1, 10, 18, 14, 1, 6 };
oid ds1TransStatsGroup_oid[11] = { 1, 3, 6, 1, 2, 1, 10, 18, 14, 1, 7 };
oid ds1NearEndOptionalTrapGroup_oid[11] = { 1, 3, 6, 1, 2, 1, 10, 18, 14, 1, 8 };
oid ds1ChanMappingGroup_oid[11] = { 1, 3, 6, 1, 2, 1, 10, 18, 14, 1, 9 };
oid ds1NearEndConfigurationGroup_oid[11] = { 1, 3, 6, 1, 2, 1, 10, 18, 14, 1, 10 };
oid ds1NearEndCfgGroup_oid[11] = { 1, 3, 6, 1, 2, 1, 10, 18, 14, 1, 11 };
oid ds1NearEndStatGroup_oid[11] = { 1, 3, 6, 1, 2, 1, 10, 18, 14, 1, 12 };
oid ds1FarEndNGroup_oid[11] = { 1, 3, 6, 1, 2, 1, 10, 18, 14, 1, 13 };
oid ds1Compliance_oid[11] = { 1, 3, 6, 1, 2, 1, 10, 18, 14, 2, 1 };
oid ds1MibT1PriCompliance_oid[11] = { 1, 3, 6, 1, 2, 1, 10, 18, 14, 2, 2 };
oid ds1MibE1PriCompliance_oid[11] = { 1, 3, 6, 1, 2, 1, 10, 18, 14, 2, 3 };
oid ds1Ds2Compliance_oid[11] = { 1, 3, 6, 1, 2, 1, 10, 18, 14, 2, 4 };
oid ds1NCompliance_oid[11] = { 1, 3, 6, 1, 2, 1, 10, 18, 14, 2, 5 };
oid ds1MibT1PriNCompliance_oid[11] = { 1, 3, 6, 1, 2, 1, 10, 18, 14, 2, 6 };
oid ds1MibE1PriNCompliance_oid[11] = { 1, 3, 6, 1, 2, 1, 10, 18, 14, 2, 7 };
oid ds1J1Compliance_oid[11] = { 1, 3, 6, 1, 2, 1, 10, 18, 14, 2, 8 };
oid ds1NMibT1PriNCompliance_oid[11] = { 1, 3, 6, 1, 2, 1, 10, 18, 14, 2, 9 };
oid ds1NMibE1PriNCompliance_oid[11] = { 1, 3, 6, 1, 2, 1, 10, 18, 14, 2, 10 };
static oid zeroDotZero_oid[2] = { 0, 0 };
static oid snmpTrapOID_oid[11] = { 1, 3, 6, 1, 6, 3, 1, 1, 4, 1, 0 };

/*
 * variable4 ds1EXT_variables: tree for ds1EXT
 * This variable defines function callbacks and type return information for the ds1EXT mib section
 */
struct variable4 ds1EXT_variables[] = {
	/* magic number, variable type, ro/rw, callback fn, L, oidsuffix */
#define   DSX1IFINDEX           2
	{(u_char) DSX1IFINDEX, ASN_INTEGER, RONLY, var_dsx1ConfigTable, 3, {6, 1, 2}},
#define   DSX1TIMEELAPSED       3
	{(u_char) DSX1TIMEELAPSED, ASN_INTEGER, RONLY, var_dsx1ConfigTable, 3, {6, 1, 3}},
#define   DSX1VALIDINTERVALS    4
	{(u_char) DSX1VALIDINTERVALS, ASN_INTEGER, RONLY, var_dsx1ConfigTable, 3, {6, 1, 4}},
#define   DSX1LINETYPE          5
	{(u_char) DSX1LINETYPE, ASN_INTEGER, RWRITE, var_dsx1ConfigTable, 3, {6, 1, 5}},
#define   DSX1LINECODING        6
	{(u_char) DSX1LINECODING, ASN_INTEGER, RWRITE, var_dsx1ConfigTable, 3, {6, 1, 6}},
#define   DSX1SENDCODE          7
	{(u_char) DSX1SENDCODE, ASN_INTEGER, RWRITE, var_dsx1ConfigTable, 3, {6, 1, 7}},
#define   DSX1CIRCUITIDENTIFIER  8
	{(u_char) DSX1CIRCUITIDENTIFIER, ASN_OCTET_STR, RWRITE, var_dsx1ConfigTable, 3, {6, 1, 8}},
#define   DSX1LOOPBACKCONFIG    9
	{(u_char) DSX1LOOPBACKCONFIG, ASN_INTEGER, RWRITE, var_dsx1ConfigTable, 3, {6, 1, 9}},
#define   DSX1LINESTATUS        10
	{(u_char) DSX1LINESTATUS, ASN_BIT_STR, RONLY, var_dsx1ConfigTable, 3, {6, 1, 10}},
#define   DSX1SIGNALMODE        11
	{(u_char) DSX1SIGNALMODE, ASN_INTEGER, RWRITE, var_dsx1ConfigTable, 3, {6, 1, 11}},
#define   DSX1TRANSMITCLOCKSOURCE  12
	{(u_char) DSX1TRANSMITCLOCKSOURCE, ASN_INTEGER, RWRITE, var_dsx1ConfigTable, 3, {6, 1, 12}},
#define   DSX1FDL               13
	{(u_char) DSX1FDL, ASN_BIT_STR, RWRITE, var_dsx1ConfigTable, 3, {6, 1, 13}},
#define   DSX1INVALIDINTERVALS  14
	{(u_char) DSX1INVALIDINTERVALS, ASN_INTEGER, RONLY, var_dsx1ConfigTable, 3, {6, 1, 14}},
#define   DSX1LINELENGTH        15
	{(u_char) DSX1LINELENGTH, ASN_INTEGER, RWRITE, var_dsx1ConfigTable, 3, {6, 1, 15}},
#define   DSX1LINESTATUSLASTCHANGE  16
	{(u_char) DSX1LINESTATUSLASTCHANGE, ASN_TIMETICKS, RONLY, var_dsx1ConfigTable, 3, {6, 1, 16}},
#define   DSX1LINESTATUSCHANGETRAPENABLE  17
	{(u_char) DSX1LINESTATUSCHANGETRAPENABLE, ASN_INTEGER, RWRITE, var_dsx1ConfigTable, 3, {6, 1, 17}},
#define   DSX1LOOPBACKSTATUS    18
	{(u_char) DSX1LOOPBACKSTATUS, ASN_BIT_STR, RONLY, var_dsx1ConfigTable, 3, {6, 1, 18}},
#define   DSX1DS1CHANNELNUMBER  19
	{(u_char) DSX1DS1CHANNELNUMBER, ASN_INTEGER, RONLY, var_dsx1ConfigTable, 3, {6, 1, 19}},
#define   DSX1CHANNELIZATION    20
	{(u_char) DSX1CHANNELIZATION, ASN_INTEGER, RWRITE, var_dsx1ConfigTable, 3, {6, 1, 20}},
#define   DSX1LINEMODE          21
	{(u_char) DSX1LINEMODE, ASN_INTEGER, RWRITE, var_dsx1ConfigTable, 3, {6, 1, 21}},
#define   DSX1LINEBUILDOUT      22
	{(u_char) DSX1LINEBUILDOUT, ASN_INTEGER, RWRITE, var_dsx1ConfigTable, 3, {6, 1, 22}},
#define   DSX1LINEIMPEDANCE     23
	{(u_char) DSX1LINEIMPEDANCE, ASN_INTEGER, RWRITE, var_dsx1ConfigTable, 3, {6, 1, 23}},
#define   DSX1CURRENTESS        25
	{(u_char) DSX1CURRENTESS, ASN_GAUGE, RONLY, var_dsx1CurrentTable, 3, {7, 1, 2}},
#define   DSX1CURRENTSESS       26
	{(u_char) DSX1CURRENTSESS, ASN_GAUGE, RONLY, var_dsx1CurrentTable, 3, {7, 1, 3}},
#define   DSX1CURRENTSEFSS      27
	{(u_char) DSX1CURRENTSEFSS, ASN_GAUGE, RONLY, var_dsx1CurrentTable, 3, {7, 1, 4}},
#define   DSX1CURRENTUASS       28
	{(u_char) DSX1CURRENTUASS, ASN_GAUGE, RONLY, var_dsx1CurrentTable, 3, {7, 1, 5}},
#define   DSX1CURRENTCSSS       29
	{(u_char) DSX1CURRENTCSSS, ASN_GAUGE, RONLY, var_dsx1CurrentTable, 3, {7, 1, 6}},
#define   DSX1CURRENTPCVS       30
	{(u_char) DSX1CURRENTPCVS, ASN_GAUGE, RONLY, var_dsx1CurrentTable, 3, {7, 1, 7}},
#define   DSX1CURRENTLESS       31
	{(u_char) DSX1CURRENTLESS, ASN_GAUGE, RONLY, var_dsx1CurrentTable, 3, {7, 1, 8}},
#define   DSX1CURRENTBESS       32
	{(u_char) DSX1CURRENTBESS, ASN_GAUGE, RONLY, var_dsx1CurrentTable, 3, {7, 1, 9}},
#define   DSX1CURRENTDMS        33
	{(u_char) DSX1CURRENTDMS, ASN_GAUGE, RONLY, var_dsx1CurrentTable, 3, {7, 1, 10}},
#define   DSX1CURRENTLCVS       34
	{(u_char) DSX1CURRENTLCVS, ASN_GAUGE, RONLY, var_dsx1CurrentTable, 3, {7, 1, 11}},
#define   DSX1INTERVALESS       37
	{(u_char) DSX1INTERVALESS, ASN_GAUGE, RONLY, var_dsx1IntervalTable, 3, {8, 1, 3}},
#define   DSX1INTERVALSESS      38
	{(u_char) DSX1INTERVALSESS, ASN_GAUGE, RONLY, var_dsx1IntervalTable, 3, {8, 1, 4}},
#define   DSX1INTERVALSEFSS     39
	{(u_char) DSX1INTERVALSEFSS, ASN_GAUGE, RONLY, var_dsx1IntervalTable, 3, {8, 1, 5}},
#define   DSX1INTERVALUASS      40
	{(u_char) DSX1INTERVALUASS, ASN_GAUGE, RONLY, var_dsx1IntervalTable, 3, {8, 1, 6}},
#define   DSX1INTERVALCSSS      41
	{(u_char) DSX1INTERVALCSSS, ASN_GAUGE, RONLY, var_dsx1IntervalTable, 3, {8, 1, 7}},
#define   DSX1INTERVALPCVS      42
	{(u_char) DSX1INTERVALPCVS, ASN_GAUGE, RONLY, var_dsx1IntervalTable, 3, {8, 1, 8}},
#define   DSX1INTERVALLESS      43
	{(u_char) DSX1INTERVALLESS, ASN_GAUGE, RONLY, var_dsx1IntervalTable, 3, {8, 1, 9}},
#define   DSX1INTERVALBESS      44
	{(u_char) DSX1INTERVALBESS, ASN_GAUGE, RONLY, var_dsx1IntervalTable, 3, {8, 1, 10}},
#define   DSX1INTERVALDMS       45
	{(u_char) DSX1INTERVALDMS, ASN_GAUGE, RONLY, var_dsx1IntervalTable, 3, {8, 1, 11}},
#define   DSX1INTERVALLCVS      46
	{(u_char) DSX1INTERVALLCVS, ASN_GAUGE, RONLY, var_dsx1IntervalTable, 3, {8, 1, 12}},
#define   DSX1INTERVALVALIDDATA  47
	{(u_char) DSX1INTERVALVALIDDATA, ASN_INTEGER, RONLY, var_dsx1IntervalTable, 3, {8, 1, 13}},
#define   DSX1TOTALESS          49
	{(u_char) DSX1TOTALESS, ASN_GAUGE, RONLY, var_dsx1TotalTable, 3, {9, 1, 2}},
#define   DSX1TOTALSESS         50
	{(u_char) DSX1TOTALSESS, ASN_GAUGE, RONLY, var_dsx1TotalTable, 3, {9, 1, 3}},
#define   DSX1TOTALSEFSS        51
	{(u_char) DSX1TOTALSEFSS, ASN_GAUGE, RONLY, var_dsx1TotalTable, 3, {9, 1, 4}},
#define   DSX1TOTALUASS         52
	{(u_char) DSX1TOTALUASS, ASN_GAUGE, RONLY, var_dsx1TotalTable, 3, {9, 1, 5}},
#define   DSX1TOTALCSSS         53
	{(u_char) DSX1TOTALCSSS, ASN_GAUGE, RONLY, var_dsx1TotalTable, 3, {9, 1, 6}},
#define   DSX1TOTALPCVS         54
	{(u_char) DSX1TOTALPCVS, ASN_GAUGE, RONLY, var_dsx1TotalTable, 3, {9, 1, 7}},
#define   DSX1TOTALLESS         55
	{(u_char) DSX1TOTALLESS, ASN_GAUGE, RONLY, var_dsx1TotalTable, 3, {9, 1, 8}},
#define   DSX1TOTALBESS         56
	{(u_char) DSX1TOTALBESS, ASN_GAUGE, RONLY, var_dsx1TotalTable, 3, {9, 1, 9}},
#define   DSX1TOTALDMS          57
	{(u_char) DSX1TOTALDMS, ASN_GAUGE, RONLY, var_dsx1TotalTable, 3, {9, 1, 10}},
#define   DSX1TOTALLCVS         58
	{(u_char) DSX1TOTALLCVS, ASN_GAUGE, RONLY, var_dsx1TotalTable, 3, {9, 1, 11}},
#define   DSX1FARENDTIMEELAPSED  60
	{(u_char) DSX1FARENDTIMEELAPSED, ASN_INTEGER, RONLY, var_dsx1FarEndCurrentTable, 3, {10, 1, 2}},
#define   DSX1FARENDVALIDINTERVALS  61
	{(u_char) DSX1FARENDVALIDINTERVALS, ASN_INTEGER, RONLY, var_dsx1FarEndCurrentTable, 3, {10, 1, 3}},
#define   DSX1FARENDCURRENTESS  62
	{(u_char) DSX1FARENDCURRENTESS, ASN_GAUGE, RONLY, var_dsx1FarEndCurrentTable, 3, {10, 1, 4}},
#define   DSX1FARENDCURRENTSESS  63
	{(u_char) DSX1FARENDCURRENTSESS, ASN_GAUGE, RONLY, var_dsx1FarEndCurrentTable, 3, {10, 1, 5}},
#define   DSX1FARENDCURRENTSEFSS  64
	{(u_char) DSX1FARENDCURRENTSEFSS, ASN_GAUGE, RONLY, var_dsx1FarEndCurrentTable, 3, {10, 1, 6}},
#define   DSX1FARENDCURRENTUASS  65
	{(u_char) DSX1FARENDCURRENTUASS, ASN_GAUGE, RONLY, var_dsx1FarEndCurrentTable, 3, {10, 1, 7}},
#define   DSX1FARENDCURRENTCSSS  66
	{(u_char) DSX1FARENDCURRENTCSSS, ASN_GAUGE, RONLY, var_dsx1FarEndCurrentTable, 3, {10, 1, 8}},
#define   DSX1FARENDCURRENTLESS  67
	{(u_char) DSX1FARENDCURRENTLESS, ASN_GAUGE, RONLY, var_dsx1FarEndCurrentTable, 3, {10, 1, 9}},
#define   DSX1FARENDCURRENTPCVS  68
	{(u_char) DSX1FARENDCURRENTPCVS, ASN_GAUGE, RONLY, var_dsx1FarEndCurrentTable, 3, {10, 1, 10}},
#define   DSX1FARENDCURRENTBESS  69
	{(u_char) DSX1FARENDCURRENTBESS, ASN_GAUGE, RONLY, var_dsx1FarEndCurrentTable, 3, {10, 1, 11}},
#define   DSX1FARENDCURRENTDMS  70
	{(u_char) DSX1FARENDCURRENTDMS, ASN_GAUGE, RONLY, var_dsx1FarEndCurrentTable, 3, {10, 1, 12}},
#define   DSX1FARENDINVALIDINTERVALS  71
	{(u_char) DSX1FARENDINVALIDINTERVALS, ASN_INTEGER, RONLY, var_dsx1FarEndCurrentTable, 3, {10, 1, 13}},
#define   DSX1FARENDINTERVALESS  74
	{(u_char) DSX1FARENDINTERVALESS, ASN_GAUGE, RONLY, var_dsx1FarEndIntervalTable, 3, {11, 1, 3}},
#define   DSX1FARENDINTERVALSESS  75
	{(u_char) DSX1FARENDINTERVALSESS, ASN_GAUGE, RONLY, var_dsx1FarEndIntervalTable, 3, {11, 1, 4}},
#define   DSX1FARENDINTERVALSEFSS  76
	{(u_char) DSX1FARENDINTERVALSEFSS, ASN_GAUGE, RONLY, var_dsx1FarEndIntervalTable, 3, {11, 1, 5}},
#define   DSX1FARENDINTERVALUASS  77
	{(u_char) DSX1FARENDINTERVALUASS, ASN_GAUGE, RONLY, var_dsx1FarEndIntervalTable, 3, {11, 1, 6}},
#define   DSX1FARENDINTERVALCSSS  78
	{(u_char) DSX1FARENDINTERVALCSSS, ASN_GAUGE, RONLY, var_dsx1FarEndIntervalTable, 3, {11, 1, 7}},
#define   DSX1FARENDINTERVALLESS  79
	{(u_char) DSX1FARENDINTERVALLESS, ASN_GAUGE, RONLY, var_dsx1FarEndIntervalTable, 3, {11, 1, 8}},
#define   DSX1FARENDINTERVALPCVS  80
	{(u_char) DSX1FARENDINTERVALPCVS, ASN_GAUGE, RONLY, var_dsx1FarEndIntervalTable, 3, {11, 1, 9}},
#define   DSX1FARENDINTERVALBESS  81
	{(u_char) DSX1FARENDINTERVALBESS, ASN_GAUGE, RONLY, var_dsx1FarEndIntervalTable, 3, {11, 1, 10}},
#define   DSX1FARENDINTERVALDMS  82
	{(u_char) DSX1FARENDINTERVALDMS, ASN_GAUGE, RONLY, var_dsx1FarEndIntervalTable, 3, {11, 1, 11}},
#define   DSX1FARENDINTERVALVALIDDATA  83
	{(u_char) DSX1FARENDINTERVALVALIDDATA, ASN_INTEGER, RONLY, var_dsx1FarEndIntervalTable, 3, {11, 1, 12}},
#define   DSX1FARENDTOTALESS    85
	{(u_char) DSX1FARENDTOTALESS, ASN_GAUGE, RONLY, var_dsx1FarEndTotalTable, 3, {12, 1, 2}},
#define   DSX1FARENDTOTALSESS   86
	{(u_char) DSX1FARENDTOTALSESS, ASN_GAUGE, RONLY, var_dsx1FarEndTotalTable, 3, {12, 1, 3}},
#define   DSX1FARENDTOTALSEFSS  87
	{(u_char) DSX1FARENDTOTALSEFSS, ASN_GAUGE, RONLY, var_dsx1FarEndTotalTable, 3, {12, 1, 4}},
#define   DSX1FARENDTOTALUASS   88
	{(u_char) DSX1FARENDTOTALUASS, ASN_GAUGE, RONLY, var_dsx1FarEndTotalTable, 3, {12, 1, 5}},
#define   DSX1FARENDTOTALCSSS   89
	{(u_char) DSX1FARENDTOTALCSSS, ASN_GAUGE, RONLY, var_dsx1FarEndTotalTable, 3, {12, 1, 6}},
#define   DSX1FARENDTOTALLESS   90
	{(u_char) DSX1FARENDTOTALLESS, ASN_GAUGE, RONLY, var_dsx1FarEndTotalTable, 3, {12, 1, 7}},
#define   DSX1FARENDTOTALPCVS   91
	{(u_char) DSX1FARENDTOTALPCVS, ASN_GAUGE, RONLY, var_dsx1FarEndTotalTable, 3, {12, 1, 8}},
#define   DSX1FARENDTOTALBESS   92
	{(u_char) DSX1FARENDTOTALBESS, ASN_GAUGE, RONLY, var_dsx1FarEndTotalTable, 3, {12, 1, 9}},
#define   DSX1FARENDTOTALDMS    93
	{(u_char) DSX1FARENDTOTALDMS, ASN_GAUGE, RONLY, var_dsx1FarEndTotalTable, 3, {12, 1, 10}},
#define   DSX1FRACIFINDEX       96
	{(u_char) DSX1FRACIFINDEX, ASN_INTEGER, RWRITE, var_dsx1FracTable, 3, {13, 1, 3}},
#define   DSX1CHANMAPPEDIFINDEX  99
	{(u_char) DSX1CHANMAPPEDIFINDEX, ASN_INTEGER, RONLY, var_dsx1ChanMappingTable, 3, {16, 1, 1}},
};

/* (L = length of the oidsuffix) */
struct ds1EXT_data *ds1EXTStorage = NULL;

/* global storage of our data, saved in and configured by header_complex() */
struct header_complex_index *dsx1ConfigTableStorage = NULL;
struct header_complex_index *dsx1CurrentTableStorage = NULL;
struct header_complex_index *dsx1IntervalTableStorage = NULL;
struct header_complex_index *dsx1TotalTableStorage = NULL;
struct header_complex_index *dsx1FarEndCurrentTableStorage = NULL;
struct header_complex_index *dsx1FarEndIntervalTableStorage = NULL;
struct header_complex_index *dsx1FarEndTotalTableStorage = NULL;
struct header_complex_index *dsx1FracTableStorage = NULL;
struct header_complex_index *dsx1ChanMappingTableStorage = NULL;

#if defined MODULE
void (*ds1EXTold_signal_handler) (int) = NULL;	/* save old signal handler just in case */
void ds1EXT_loop_handler(int);
void ds1EXT_fd_handler(int, void *);
#endif				/* defined MOUDLE */
/**
 * @fn void init_ds1EXT(void)
 * @brief ds1EXT initialization routine.
 *
 * This is called when the agent starts up.  At a minimum, registration of the MIB variables
 * structure (ds1EXT_variables) should take place here.  By default the function also
 * registers the configuration handler and configuration store callbacks.
 *
 * Additional registrations that may be considered here are calls to regsiter_readfd(),
 * register_writefd() and register_exceptfd() for hooking into the snmpd event loop, but only when
 * used as a loadable module.  By default this function establishes a single file descriptor to
 * read, or upon which to handle exceptions.  Note that the snmpd only supports a maximum of 32
 * extneral file descriptors, so these should be used sparingly.
 *
 * When running as a loadable module, it is also necessary to hook into the snmpd event loop so that
 * the current request number can be deteremined.  This is accomplished by using a trick of the
 * external_signal_scheduled and external_signal_handler mechanism which is called on each event
 * loop when external_signal_scheduled is non-zero.  This is used to increment the sa_request value
 * on each snmpd event loop interation so that calls to MIB tree functions can determine whether
 * they belong to a fresh request or not (primarily for cacheing and possibly to clean up non-polled
 * file descriptors).
 */
void
init_ds1EXT(void)
{
	(void) my_fd;
	(void) zeroDotZero_oid;
	(void) snmpTrapOID_oid;
	DEBUGMSGTL(("ds1EXT", "initializing...  "));
	/* register ourselves with the agent to handle our mib tree */
	REGISTER_MIB("ds1EXT", ds1EXT_variables, variable4, ds1EXT_variables_oid);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_SHUTDOWN, term_ds1EXT, NULL);
	/* register our config handler(s) to deal with registrations */
	snmpd_register_config_handler("ds1EXT", parse_ds1EXT, NULL, "HELP STRING");
	snmpd_register_config_handler("dsx1ConfigTable", parse_dsx1ConfigTable, NULL, "HELP STRING");
	snmpd_register_config_handler("dsx1CurrentTable", parse_dsx1CurrentTable, NULL, "HELP STRING");
	snmpd_register_config_handler("dsx1IntervalTable", parse_dsx1IntervalTable, NULL, "HELP STRING");
	snmpd_register_config_handler("dsx1TotalTable", parse_dsx1TotalTable, NULL, "HELP STRING");
	snmpd_register_config_handler("dsx1FarEndCurrentTable", parse_dsx1FarEndCurrentTable, NULL, "HELP STRING");
	snmpd_register_config_handler("dsx1FarEndIntervalTable", parse_dsx1FarEndIntervalTable, NULL, "HELP STRING");
	snmpd_register_config_handler("dsx1FarEndTotalTable", parse_dsx1FarEndTotalTable, NULL, "HELP STRING");
	snmpd_register_config_handler("dsx1FracTable", parse_dsx1FracTable, NULL, "HELP STRING");
	snmpd_register_config_handler("dsx1ChanMappingTable", parse_dsx1ChanMappingTable, NULL, "HELP STRING");

	/* we need to be called back later to store our data */
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_ds1EXT, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_dsx1ConfigTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_dsx1CurrentTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_dsx1IntervalTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_dsx1TotalTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_dsx1FarEndCurrentTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_dsx1FarEndIntervalTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_dsx1FarEndTotalTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_dsx1FracTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_dsx1ChanMappingTable, NULL);

	/* place any other initialization junk you need here */
#if defined MODULE
	if (sa_readfd != 0) {
		register_readfd(sa_readfd, ds1EXT_fd_handler, (void *) 0);
		register_exceptfd(sa_readfd, ds1EXT_fd_handler, (void *) 1);
	}
#if defined MASTER
	ds1EXTold_signal_handler = external_signal_handler[SIGCHLD];
	external_signal_handler[SIGCHLD] = &ds1EXT_loop_handler;
	external_signal_scheduled[SIGCHLD] = 1;
#endif				/* defined MASTER */
#endif				/* defined MODULE */
	DEBUGMSGTL(("ds1EXT", "done.\n"));
}

/**
 * @fn void deinit_ds1EXT(void)
 * @brief deinitialization routine.
 *
 * This is called before the agent is unloaded.  At a minimum, deregistration of the MIB variables
 * structure (ds1EXT_variables) should take place here.  By default, the function also
 * deregisters the the configuration file handlers for the MIB variables and table rows.
 *
 * Additional deregistrations that may be required here are calls to unregister_readfd(),
 * unregister_writefd() and unregsiter_exceptfd() for unhooking from the snmpd event loop, but only
 * when used as a loadable module.  By default if a read file descriptor exists, it is unregistered.
 */
void
deinit_ds1EXT(void)
{
	DEBUGMSGTL(("ds1EXT", "deinitializating...  "));
#if defined MODULE
#if defined MASTER
	external_signal_handler[SIGCHLD] = ds1EXTold_signal_handler;
#endif				/* defined MASTER */
	if (sa_readfd != 0) {
		unregister_exceptfd(sa_readfd);
		unregister_readfd(sa_readfd);
		close(sa_readfd);
		sa_readfd = 0;
	}
#endif				/* defined MODULE */
	unregister_mib(ds1EXT_variables_oid, sizeof(ds1EXT_variables_oid) / sizeof(oid));
	snmpd_unregister_config_handler("ds1EXT");
	snmpd_unregister_config_handler("dsx1ConfigTable");
	snmpd_unregister_config_handler("dsx1CurrentTable");
	snmpd_unregister_config_handler("dsx1IntervalTable");
	snmpd_unregister_config_handler("dsx1TotalTable");
	snmpd_unregister_config_handler("dsx1FarEndCurrentTable");
	snmpd_unregister_config_handler("dsx1FarEndIntervalTable");
	snmpd_unregister_config_handler("dsx1FarEndTotalTable");
	snmpd_unregister_config_handler("dsx1FracTable");
	snmpd_unregister_config_handler("dsx1ChanMappingTable");

	/* place any other de-initialization junk you need here */
	DEBUGMSGTL(("ds1EXT", "done.\n"));
}

int
term_ds1EXT(int majorID, int minorID, void *serverarg, void *clientarg)
{
	deinit_ds1EXT();
	return 0;
}

/**
 * @fn struct ds1EXT_data *ds1EXT_create(void)
 * @brief create a fresh data structure representing scalars in ds1EXT.
 *
 * Creates a new ds1EXT_data structure by allocating dynamic memory for the structure and
 * initializing the default values of scalars in ds1EXT.
 */
struct ds1EXT_data *
ds1EXT_create(void)
{
	struct ds1EXT_data *StorageNew = SNMP_MALLOC_STRUCT(ds1EXT_data);

	DEBUGMSGTL(("ds1EXT", "creating scalars...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default scalar values here into StorageNew */

	}
	DEBUGMSGTL(("ds1EXT", "done.\n"));
	return (StorageNew);
}

/**
 * @fn int ds1EXT_destroy(struct ds1EXT_data **thedata)
 * @param thedata pointer to the data structure in ds1EXT.
 * @brief delete a scalars structure from ds1EXT.
 *
 * Frees scalars that were previously removed from ds1EXT.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
ds1EXT_destroy(struct ds1EXT_data **thedata)
{
	struct ds1EXT_data *StorageDel;

	DEBUGMSGTL(("ds1EXT", "deleting scalars...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("ds1EXT", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int ds1EXT_add(struct ds1EXT_data *thedata)
 * @param thedata the structure representing ds1EXT scalars.
 * @brief adds node to the ds1EXT scalar data set.
 *
 * Adds a scalar structure to the ds1EXT data set.  Note that this function is necessary even
 * when the scalar values are not peristent.
 */
int
ds1EXT_add(struct ds1EXT_data *thedata)
{
	DEBUGMSGTL(("ds1EXT", "adding data...  "));
	ds1EXTStorage = thedata;
	DEBUGMSGTL(("ds1EXT", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_ds1EXT(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for ds1EXT entries.
 *
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case ds1EXT).  This routine is invoked by
 * UCD-SNMP to read the values of scalars in the MIB from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the MIB.  If there are no configured entries
 * in the configuration MIB, this function will simply not be called.
 */
void
parse_ds1EXT(const char *token, char *line)
{
	size_t tmpsize;
	struct ds1EXT_data *StorageTmp = ds1EXT_create();

	DEBUGMSGTL(("ds1EXT", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual scalars that are not persistent */
	ds1EXT_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("ds1EXT", "done.\n"));
}

/*
 * store_ds1EXT(): stores .conf file entries needed to configure the mib.
 */
int
store_ds1EXT(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct ds1EXT_data *StorageTmp;

	DEBUGMSGTL(("ds1EXT", "storing data...  "));
	refresh_ds1EXT(1);
	if ((StorageTmp = ds1EXTStorage) == NULL) {
		DEBUGMSGTL(("ds1EXT", "error.\n"));
		return SNMPERR_GENERR;
	}
	(void) tmpsize;
	/* XXX: comment entire section if no scalars are persistent */
	{
		memset(line, 0, sizeof(line));
		strcat(line, "ds1EXT ");
		cptr = line + strlen(line);
		/* XXX: remove individual scalars that are not persistent */
		snmpd_store_config(line);
	}
	DEBUGMSGTL(("ds1EXT", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void refresh_ds1EXT(int force)
 * @param force forced refresh when non-zero.
 * @brief refresh the scalar values of ds1EXT.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a scalar has been requested).
 */
void
refresh_ds1EXT(int force)
{
	if (ds1EXTStorage == NULL) {
		struct ds1EXT_data *StorageNew;

		if ((StorageNew = ds1EXT_create()) == NULL)
			return;
		ds1EXTStorage = StorageNew;
		ds1EXT_refresh = 1;
	}
	if (!force && ds1EXT_refresh == 0)
		return;
	ds1EXT_refresh = 0;
	/* XXX: Update scalars as required here... */
}

/**
 * @fn u_char * var_ds1EXT(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @param vp a pointer to the entry in the variables table for the requested variable.
 * @param name the object identifier for which to find.
 * @param length the length of the object identifier.
 * @param exact whether the name is exact.
 * @param var_len a pointer to the length of the representation of the object.
 * @param write_method a pointer to a write method for the object.
 * @brief locate variables in ds1EXT.
 *
 * This function returns a pointer to a memory area that is static across the request that contains
 * the UCD-SNMP representation of the scalar (so that it may be used to read from for a GET,
 * GET-NEXT or GET-BULK request).  This returned pointer may be NULL, in which case the function is
 * telling UCD-SNMP that the scalar does not exist for reading; however, if write_method is
 * overwritten with a non-NULL value, the function is telling UCD-SNMP that the scalar exists for
 * writing.  Write-only objects can be effected in this way.
 */
u_char *
var_ds1EXT(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct ds1EXT_data *StorageTmp;
	u_char *rval;

	if (header_generic(vp, name, length, exact, var_len, write_method) == MATCH_FAILED)
		return NULL;
	/* Refresh the MIB values if required. */
	refresh_ds1EXT(0);
	if ((StorageTmp = ds1EXTStorage) == NULL)
		return NULL;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn struct dsx1ConfigTable_data *dsx1ConfigTable_create(void)
 * @brief create a fresh data structure representing a new row in the dsx1ConfigTable table.
 *
 * Creates a new dsx1ConfigTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct dsx1ConfigTable_data *
dsx1ConfigTable_create(void)
{
	struct dsx1ConfigTable_data *StorageNew = SNMP_MALLOC_STRUCT(dsx1ConfigTable_data);

	DEBUGMSGTL(("dsx1ConfigTable", "creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->dsx1IfIndex = 0;
		StorageNew->dsx1TimeElapsed = 0;
		StorageNew->dsx1ValidIntervals = 0;
		StorageNew->dsx1LineType = 0;
		StorageNew->dsx1LineCoding = 0;
		StorageNew->dsx1SendCode = 0;
		if ((StorageNew->dsx1CircuitIdentifier = (uint8_t *) strdup("")) != NULL)
			StorageNew->dsx1CircuitIdentifierLen = strlen("");
		StorageNew->dsx1LoopbackConfig = 0;
		if (memdup((u_char **) &StorageNew->dsx1LineStatus, (u_char *) "\x00\x00\x00", 3) == SNMPERR_SUCCESS)
			StorageNew->dsx1LineStatusLen = 3;
		StorageNew->dsx1SignalMode = 0;
		StorageNew->dsx1TransmitClockSource = 0;
		if (memdup((u_char **) &StorageNew->dsx1Fdl, (u_char *) "\x00", 1) == SNMPERR_SUCCESS)
			StorageNew->dsx1FdlLen = 1;
		StorageNew->dsx1InvalidIntervals = 0;
		StorageNew->dsx1LineLength = 0;
		StorageNew->dsx1LineStatusLastChange = 0;
		StorageNew->dsx1LineStatusChangeTrapEnable = DSX1LINESTATUSCHANGETRAPENABLE_DISABLED;
		if (memdup((u_char **) &StorageNew->dsx1LoopbackStatus, (u_char *) "\x00", 1) == SNMPERR_SUCCESS)
			StorageNew->dsx1LoopbackStatusLen = 1;
		StorageNew->dsx1Ds1ChannelNumber = 0;
		StorageNew->dsx1Channelization = 0;
		StorageNew->dsx1LineMode = 0;
		StorageNew->dsx1LineBuildOut = 0;
		StorageNew->dsx1LineImpedance = 0;

	}
	DEBUGMSGTL(("dsx1ConfigTable", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct dsx1ConfigTable_data *dsx1ConfigTable_duplicate(struct dsx1ConfigTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicat a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct dsx1ConfigTable_data *
dsx1ConfigTable_duplicate(struct dsx1ConfigTable_data *thedata)
{
	struct dsx1ConfigTable_data *StorageNew = SNMP_MALLOC_STRUCT(dsx1ConfigTable_data);

	DEBUGMSGTL(("dsx1ConfigTable", "duplicating row...  "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("dsx1ConfigTable", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	dsx1ConfigTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int dsx1ConfigTable_destroy(struct dsx1ConfigTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
dsx1ConfigTable_destroy(struct dsx1ConfigTable_data **thedata)
{
	struct dsx1ConfigTable_data *StorageDel;

	DEBUGMSGTL(("dsx1ConfigTable", "deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->dsx1CircuitIdentifier);
		StorageDel->dsx1CircuitIdentifierLen = 0;
		SNMP_FREE(StorageDel->dsx1LineStatus);
		StorageDel->dsx1LineStatusLen = 0;
		SNMP_FREE(StorageDel->dsx1Fdl);
		StorageDel->dsx1FdlLen = 0;
		SNMP_FREE(StorageDel->dsx1LoopbackStatus);
		StorageDel->dsx1LoopbackStatusLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("dsx1ConfigTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int dsx1ConfigTable_add(struct dsx1ConfigTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the dsx1ConfigTable table data set.
 *
 * Adds a table row structure to the dsx1ConfigTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
dsx1ConfigTable_add(struct dsx1ConfigTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("dsx1ConfigTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index the data */
	/* dsx1LineIndex */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->dsx1LineIndex, sizeof(thedata->dsx1LineIndex));
	header_complex_add_data(&dsx1ConfigTableStorage, vars, thedata);
	DEBUGMSGTL(("dsx1ConfigTable", "registered an entry\n"));
	DEBUGMSGTL(("dsx1ConfigTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int dsx1ConfigTable_del(struct dsx1ConfigTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the dsx1ConfigTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
dsx1ConfigTable_del(struct dsx1ConfigTable_data *thedata)
{
	struct dsx1ConfigTable_data *StorageDel;

	DEBUGMSGTL(("dsx1ConfigTable", "deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(dsx1ConfigTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&dsx1ConfigTableStorage, hciptr);
	}
	DEBUGMSGTL(("dsx1ConfigTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_dsx1ConfigTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for dsx1ConfigTable entries.
 *
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case dsx1ConfigTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_dsx1ConfigTable(const char *token, char *line)
{
	size_t tmpsize;
	struct dsx1ConfigTable_data *StorageTmp = dsx1ConfigTable_create();

	DEBUGMSGTL(("dsx1ConfigTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->dsx1LineIndex, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->dsx1IfIndex, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->dsx1TimeElapsed, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->dsx1ValidIntervals, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->dsx1LineType, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->dsx1LineCoding, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->dsx1SendCode, &tmpsize);
	SNMP_FREE(StorageTmp->dsx1CircuitIdentifier);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->dsx1CircuitIdentifier, &StorageTmp->dsx1CircuitIdentifierLen);
	if (StorageTmp->dsx1CircuitIdentifier == NULL) {
		config_perror("invalid specification for dsx1CircuitIdentifier");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->dsx1LoopbackConfig, &tmpsize);
	SNMP_FREE(StorageTmp->dsx1LineStatus);
	line = read_config_read_data(ASN_BIT_STR, line, &StorageTmp->dsx1LineStatus, &StorageTmp->dsx1LineStatusLen);
	if (StorageTmp->dsx1LineStatus == NULL) {
		config_perror("invalid specification for dsx1LineStatus");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->dsx1SignalMode, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->dsx1TransmitClockSource, &tmpsize);
	SNMP_FREE(StorageTmp->dsx1Fdl);
	line = read_config_read_data(ASN_BIT_STR, line, &StorageTmp->dsx1Fdl, &StorageTmp->dsx1FdlLen);
	if (StorageTmp->dsx1Fdl == NULL) {
		config_perror("invalid specification for dsx1Fdl");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->dsx1InvalidIntervals, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->dsx1LineLength, &tmpsize);
	line = read_config_read_data(ASN_TIMETICKS, line, &StorageTmp->dsx1LineStatusLastChange, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->dsx1LineStatusChangeTrapEnable, &tmpsize);
	SNMP_FREE(StorageTmp->dsx1LoopbackStatus);
	line = read_config_read_data(ASN_BIT_STR, line, &StorageTmp->dsx1LoopbackStatus, &StorageTmp->dsx1LoopbackStatusLen);
	if (StorageTmp->dsx1LoopbackStatus == NULL) {
		config_perror("invalid specification for dsx1LoopbackStatus");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->dsx1Ds1ChannelNumber, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->dsx1Channelization, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->dsx1LineMode, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->dsx1LineBuildOut, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->dsx1LineImpedance, &tmpsize);
	dsx1ConfigTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("dsx1ConfigTable", "done.\n"));
}

/*
 * store_dsx1ConfigTable(): store configuraiton file for dsx1ConfigTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_dsx1ConfigTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct dsx1ConfigTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("dsx1ConfigTable", "storing data...  "));
	refresh_dsx1ConfigTable(1);
	(void) tmpsize;
	for (hcindex = dsx1ConfigTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct dsx1ConfigTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "dsx1ConfigTable ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->dsx1LineIndex, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->dsx1IfIndex, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->dsx1TimeElapsed, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->dsx1ValidIntervals, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->dsx1LineType, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->dsx1LineCoding, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->dsx1SendCode, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->dsx1CircuitIdentifier, &StorageTmp->dsx1CircuitIdentifierLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->dsx1LoopbackConfig, &tmpsize);
			cptr = read_config_store_data(ASN_BIT_STR, cptr, &StorageTmp->dsx1LineStatus, &StorageTmp->dsx1LineStatusLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->dsx1SignalMode, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->dsx1TransmitClockSource, &tmpsize);
			cptr = read_config_store_data(ASN_BIT_STR, cptr, &StorageTmp->dsx1Fdl, &StorageTmp->dsx1FdlLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->dsx1InvalidIntervals, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->dsx1LineLength, &tmpsize);
			cptr = read_config_store_data(ASN_TIMETICKS, cptr, &StorageTmp->dsx1LineStatusLastChange, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->dsx1LineStatusChangeTrapEnable, &tmpsize);
			cptr = read_config_store_data(ASN_BIT_STR, cptr, &StorageTmp->dsx1LoopbackStatus, &StorageTmp->dsx1LoopbackStatusLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->dsx1Ds1ChannelNumber, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->dsx1Channelization, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->dsx1LineMode, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->dsx1LineBuildOut, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->dsx1LineImpedance, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("dsx1ConfigTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct dsx1CurrentTable_data *dsx1CurrentTable_create(void)
 * @brief create a fresh data structure representing a new row in the dsx1CurrentTable table.
 *
 * Creates a new dsx1CurrentTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct dsx1CurrentTable_data *
dsx1CurrentTable_create(void)
{
	struct dsx1CurrentTable_data *StorageNew = SNMP_MALLOC_STRUCT(dsx1CurrentTable_data);

	DEBUGMSGTL(("dsx1CurrentTable", "creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->dsx1CurrentESs = 0;
		StorageNew->dsx1CurrentSESs = 0;
		StorageNew->dsx1CurrentSEFSs = 0;
		StorageNew->dsx1CurrentUASs = 0;
		StorageNew->dsx1CurrentCSSs = 0;
		StorageNew->dsx1CurrentPCVs = 0;
		StorageNew->dsx1CurrentLESs = 0;
		StorageNew->dsx1CurrentBESs = 0;
		StorageNew->dsx1CurrentDMs = 0;
		StorageNew->dsx1CurrentLCVs = 0;

	}
	DEBUGMSGTL(("dsx1CurrentTable", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct dsx1CurrentTable_data *dsx1CurrentTable_duplicate(struct dsx1CurrentTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicat a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct dsx1CurrentTable_data *
dsx1CurrentTable_duplicate(struct dsx1CurrentTable_data *thedata)
{
	struct dsx1CurrentTable_data *StorageNew = SNMP_MALLOC_STRUCT(dsx1CurrentTable_data);

	DEBUGMSGTL(("dsx1CurrentTable", "duplicating row...  "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("dsx1CurrentTable", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	dsx1CurrentTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int dsx1CurrentTable_destroy(struct dsx1CurrentTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
dsx1CurrentTable_destroy(struct dsx1CurrentTable_data **thedata)
{
	struct dsx1CurrentTable_data *StorageDel;

	DEBUGMSGTL(("dsx1CurrentTable", "deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("dsx1CurrentTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int dsx1CurrentTable_add(struct dsx1CurrentTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the dsx1CurrentTable table data set.
 *
 * Adds a table row structure to the dsx1CurrentTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
dsx1CurrentTable_add(struct dsx1CurrentTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("dsx1CurrentTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index the data */
	/* dsx1CurrentIndex */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->dsx1CurrentIndex, sizeof(thedata->dsx1CurrentIndex));
	header_complex_add_data(&dsx1CurrentTableStorage, vars, thedata);
	DEBUGMSGTL(("dsx1CurrentTable", "registered an entry\n"));
	DEBUGMSGTL(("dsx1CurrentTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int dsx1CurrentTable_del(struct dsx1CurrentTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the dsx1CurrentTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
dsx1CurrentTable_del(struct dsx1CurrentTable_data *thedata)
{
	struct dsx1CurrentTable_data *StorageDel;

	DEBUGMSGTL(("dsx1CurrentTable", "deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(dsx1CurrentTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&dsx1CurrentTableStorage, hciptr);
	}
	DEBUGMSGTL(("dsx1CurrentTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_dsx1CurrentTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for dsx1CurrentTable entries.
 *
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case dsx1CurrentTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_dsx1CurrentTable(const char *token, char *line)
{
	size_t tmpsize;
	struct dsx1CurrentTable_data *StorageTmp = dsx1CurrentTable_create();

	DEBUGMSGTL(("dsx1CurrentTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->dsx1CurrentIndex, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->dsx1CurrentESs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->dsx1CurrentSESs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->dsx1CurrentSEFSs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->dsx1CurrentUASs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->dsx1CurrentCSSs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->dsx1CurrentPCVs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->dsx1CurrentLESs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->dsx1CurrentBESs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->dsx1CurrentDMs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->dsx1CurrentLCVs, &tmpsize);
	dsx1CurrentTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("dsx1CurrentTable", "done.\n"));
}

/*
 * store_dsx1CurrentTable(): store configuraiton file for dsx1CurrentTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_dsx1CurrentTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct dsx1CurrentTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("dsx1CurrentTable", "storing data...  "));
	refresh_dsx1CurrentTable(1);
	(void) tmpsize;
	for (hcindex = dsx1CurrentTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct dsx1CurrentTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "dsx1CurrentTable ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->dsx1CurrentIndex, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->dsx1CurrentESs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->dsx1CurrentSESs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->dsx1CurrentSEFSs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->dsx1CurrentUASs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->dsx1CurrentCSSs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->dsx1CurrentPCVs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->dsx1CurrentLESs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->dsx1CurrentBESs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->dsx1CurrentDMs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->dsx1CurrentLCVs, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("dsx1CurrentTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct dsx1IntervalTable_data *dsx1IntervalTable_create(void)
 * @brief create a fresh data structure representing a new row in the dsx1IntervalTable table.
 *
 * Creates a new dsx1IntervalTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct dsx1IntervalTable_data *
dsx1IntervalTable_create(void)
{
	struct dsx1IntervalTable_data *StorageNew = SNMP_MALLOC_STRUCT(dsx1IntervalTable_data);

	DEBUGMSGTL(("dsx1IntervalTable", "creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->dsx1IntervalESs = 0;
		StorageNew->dsx1IntervalSESs = 0;
		StorageNew->dsx1IntervalSEFSs = 0;
		StorageNew->dsx1IntervalUASs = 0;
		StorageNew->dsx1IntervalCSSs = 0;
		StorageNew->dsx1IntervalPCVs = 0;
		StorageNew->dsx1IntervalLESs = 0;
		StorageNew->dsx1IntervalBESs = 0;
		StorageNew->dsx1IntervalDMs = 0;
		StorageNew->dsx1IntervalLCVs = 0;
		StorageNew->dsx1IntervalValidData = 0;

	}
	DEBUGMSGTL(("dsx1IntervalTable", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct dsx1IntervalTable_data *dsx1IntervalTable_duplicate(struct dsx1IntervalTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicat a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct dsx1IntervalTable_data *
dsx1IntervalTable_duplicate(struct dsx1IntervalTable_data *thedata)
{
	struct dsx1IntervalTable_data *StorageNew = SNMP_MALLOC_STRUCT(dsx1IntervalTable_data);

	DEBUGMSGTL(("dsx1IntervalTable", "duplicating row...  "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("dsx1IntervalTable", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	dsx1IntervalTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int dsx1IntervalTable_destroy(struct dsx1IntervalTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
dsx1IntervalTable_destroy(struct dsx1IntervalTable_data **thedata)
{
	struct dsx1IntervalTable_data *StorageDel;

	DEBUGMSGTL(("dsx1IntervalTable", "deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("dsx1IntervalTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int dsx1IntervalTable_add(struct dsx1IntervalTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the dsx1IntervalTable table data set.
 *
 * Adds a table row structure to the dsx1IntervalTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
dsx1IntervalTable_add(struct dsx1IntervalTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("dsx1IntervalTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index the data */
	/* dsx1IntervalIndex */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->dsx1IntervalIndex, sizeof(thedata->dsx1IntervalIndex));
	/* dsx1IntervalNumber */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->dsx1IntervalNumber, sizeof(thedata->dsx1IntervalNumber));
	header_complex_add_data(&dsx1IntervalTableStorage, vars, thedata);
	DEBUGMSGTL(("dsx1IntervalTable", "registered an entry\n"));
	DEBUGMSGTL(("dsx1IntervalTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int dsx1IntervalTable_del(struct dsx1IntervalTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the dsx1IntervalTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
dsx1IntervalTable_del(struct dsx1IntervalTable_data *thedata)
{
	struct dsx1IntervalTable_data *StorageDel;

	DEBUGMSGTL(("dsx1IntervalTable", "deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(dsx1IntervalTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&dsx1IntervalTableStorage, hciptr);
	}
	DEBUGMSGTL(("dsx1IntervalTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_dsx1IntervalTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for dsx1IntervalTable entries.
 *
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case dsx1IntervalTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_dsx1IntervalTable(const char *token, char *line)
{
	size_t tmpsize;
	struct dsx1IntervalTable_data *StorageTmp = dsx1IntervalTable_create();

	DEBUGMSGTL(("dsx1IntervalTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->dsx1IntervalIndex, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->dsx1IntervalNumber, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->dsx1IntervalESs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->dsx1IntervalSESs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->dsx1IntervalSEFSs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->dsx1IntervalUASs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->dsx1IntervalCSSs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->dsx1IntervalPCVs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->dsx1IntervalLESs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->dsx1IntervalBESs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->dsx1IntervalDMs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->dsx1IntervalLCVs, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->dsx1IntervalValidData, &tmpsize);
	dsx1IntervalTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("dsx1IntervalTable", "done.\n"));
}

/*
 * store_dsx1IntervalTable(): store configuraiton file for dsx1IntervalTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_dsx1IntervalTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct dsx1IntervalTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("dsx1IntervalTable", "storing data...  "));
	refresh_dsx1IntervalTable(1);
	(void) tmpsize;
	for (hcindex = dsx1IntervalTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct dsx1IntervalTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "dsx1IntervalTable ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->dsx1IntervalIndex, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->dsx1IntervalNumber, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->dsx1IntervalESs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->dsx1IntervalSESs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->dsx1IntervalSEFSs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->dsx1IntervalUASs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->dsx1IntervalCSSs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->dsx1IntervalPCVs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->dsx1IntervalLESs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->dsx1IntervalBESs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->dsx1IntervalDMs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->dsx1IntervalLCVs, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->dsx1IntervalValidData, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("dsx1IntervalTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct dsx1TotalTable_data *dsx1TotalTable_create(void)
 * @brief create a fresh data structure representing a new row in the dsx1TotalTable table.
 *
 * Creates a new dsx1TotalTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct dsx1TotalTable_data *
dsx1TotalTable_create(void)
{
	struct dsx1TotalTable_data *StorageNew = SNMP_MALLOC_STRUCT(dsx1TotalTable_data);

	DEBUGMSGTL(("dsx1TotalTable", "creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->dsx1TotalESs = 0;
		StorageNew->dsx1TotalSESs = 0;
		StorageNew->dsx1TotalSEFSs = 0;
		StorageNew->dsx1TotalUASs = 0;
		StorageNew->dsx1TotalCSSs = 0;
		StorageNew->dsx1TotalPCVs = 0;
		StorageNew->dsx1TotalLESs = 0;
		StorageNew->dsx1TotalBESs = 0;
		StorageNew->dsx1TotalDMs = 0;
		StorageNew->dsx1TotalLCVs = 0;

	}
	DEBUGMSGTL(("dsx1TotalTable", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct dsx1TotalTable_data *dsx1TotalTable_duplicate(struct dsx1TotalTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicat a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct dsx1TotalTable_data *
dsx1TotalTable_duplicate(struct dsx1TotalTable_data *thedata)
{
	struct dsx1TotalTable_data *StorageNew = SNMP_MALLOC_STRUCT(dsx1TotalTable_data);

	DEBUGMSGTL(("dsx1TotalTable", "duplicating row...  "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("dsx1TotalTable", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	dsx1TotalTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int dsx1TotalTable_destroy(struct dsx1TotalTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
dsx1TotalTable_destroy(struct dsx1TotalTable_data **thedata)
{
	struct dsx1TotalTable_data *StorageDel;

	DEBUGMSGTL(("dsx1TotalTable", "deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("dsx1TotalTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int dsx1TotalTable_add(struct dsx1TotalTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the dsx1TotalTable table data set.
 *
 * Adds a table row structure to the dsx1TotalTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
dsx1TotalTable_add(struct dsx1TotalTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("dsx1TotalTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index the data */
	/* dsx1TotalIndex */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->dsx1TotalIndex, sizeof(thedata->dsx1TotalIndex));
	header_complex_add_data(&dsx1TotalTableStorage, vars, thedata);
	DEBUGMSGTL(("dsx1TotalTable", "registered an entry\n"));
	DEBUGMSGTL(("dsx1TotalTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int dsx1TotalTable_del(struct dsx1TotalTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the dsx1TotalTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
dsx1TotalTable_del(struct dsx1TotalTable_data *thedata)
{
	struct dsx1TotalTable_data *StorageDel;

	DEBUGMSGTL(("dsx1TotalTable", "deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(dsx1TotalTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&dsx1TotalTableStorage, hciptr);
	}
	DEBUGMSGTL(("dsx1TotalTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_dsx1TotalTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for dsx1TotalTable entries.
 *
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case dsx1TotalTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_dsx1TotalTable(const char *token, char *line)
{
	size_t tmpsize;
	struct dsx1TotalTable_data *StorageTmp = dsx1TotalTable_create();

	DEBUGMSGTL(("dsx1TotalTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->dsx1TotalIndex, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->dsx1TotalESs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->dsx1TotalSESs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->dsx1TotalSEFSs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->dsx1TotalUASs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->dsx1TotalCSSs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->dsx1TotalPCVs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->dsx1TotalLESs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->dsx1TotalBESs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->dsx1TotalDMs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->dsx1TotalLCVs, &tmpsize);
	dsx1TotalTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("dsx1TotalTable", "done.\n"));
}

/*
 * store_dsx1TotalTable(): store configuraiton file for dsx1TotalTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_dsx1TotalTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct dsx1TotalTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("dsx1TotalTable", "storing data...  "));
	refresh_dsx1TotalTable(1);
	(void) tmpsize;
	for (hcindex = dsx1TotalTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct dsx1TotalTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "dsx1TotalTable ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->dsx1TotalIndex, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->dsx1TotalESs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->dsx1TotalSESs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->dsx1TotalSEFSs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->dsx1TotalUASs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->dsx1TotalCSSs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->dsx1TotalPCVs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->dsx1TotalLESs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->dsx1TotalBESs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->dsx1TotalDMs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->dsx1TotalLCVs, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("dsx1TotalTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct dsx1FarEndCurrentTable_data *dsx1FarEndCurrentTable_create(void)
 * @brief create a fresh data structure representing a new row in the dsx1FarEndCurrentTable table.
 *
 * Creates a new dsx1FarEndCurrentTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct dsx1FarEndCurrentTable_data *
dsx1FarEndCurrentTable_create(void)
{
	struct dsx1FarEndCurrentTable_data *StorageNew = SNMP_MALLOC_STRUCT(dsx1FarEndCurrentTable_data);

	DEBUGMSGTL(("dsx1FarEndCurrentTable", "creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->dsx1FarEndTimeElapsed = 0;
		StorageNew->dsx1FarEndValidIntervals = 0;
		StorageNew->dsx1FarEndCurrentESs = 0;
		StorageNew->dsx1FarEndCurrentSESs = 0;
		StorageNew->dsx1FarEndCurrentSEFSs = 0;
		StorageNew->dsx1FarEndCurrentUASs = 0;
		StorageNew->dsx1FarEndCurrentCSSs = 0;
		StorageNew->dsx1FarEndCurrentLESs = 0;
		StorageNew->dsx1FarEndCurrentPCVs = 0;
		StorageNew->dsx1FarEndCurrentBESs = 0;
		StorageNew->dsx1FarEndCurrentDMs = 0;
		StorageNew->dsx1FarEndInvalidIntervals = 0;

	}
	DEBUGMSGTL(("dsx1FarEndCurrentTable", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct dsx1FarEndCurrentTable_data *dsx1FarEndCurrentTable_duplicate(struct dsx1FarEndCurrentTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicat a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct dsx1FarEndCurrentTable_data *
dsx1FarEndCurrentTable_duplicate(struct dsx1FarEndCurrentTable_data *thedata)
{
	struct dsx1FarEndCurrentTable_data *StorageNew = SNMP_MALLOC_STRUCT(dsx1FarEndCurrentTable_data);

	DEBUGMSGTL(("dsx1FarEndCurrentTable", "duplicating row...  "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("dsx1FarEndCurrentTable", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	dsx1FarEndCurrentTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int dsx1FarEndCurrentTable_destroy(struct dsx1FarEndCurrentTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
dsx1FarEndCurrentTable_destroy(struct dsx1FarEndCurrentTable_data **thedata)
{
	struct dsx1FarEndCurrentTable_data *StorageDel;

	DEBUGMSGTL(("dsx1FarEndCurrentTable", "deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("dsx1FarEndCurrentTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int dsx1FarEndCurrentTable_add(struct dsx1FarEndCurrentTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the dsx1FarEndCurrentTable table data set.
 *
 * Adds a table row structure to the dsx1FarEndCurrentTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
dsx1FarEndCurrentTable_add(struct dsx1FarEndCurrentTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("dsx1FarEndCurrentTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index the data */
	/* dsx1FarEndCurrentIndex */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->dsx1FarEndCurrentIndex, sizeof(thedata->dsx1FarEndCurrentIndex));
	header_complex_add_data(&dsx1FarEndCurrentTableStorage, vars, thedata);
	DEBUGMSGTL(("dsx1FarEndCurrentTable", "registered an entry\n"));
	DEBUGMSGTL(("dsx1FarEndCurrentTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int dsx1FarEndCurrentTable_del(struct dsx1FarEndCurrentTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the dsx1FarEndCurrentTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
dsx1FarEndCurrentTable_del(struct dsx1FarEndCurrentTable_data *thedata)
{
	struct dsx1FarEndCurrentTable_data *StorageDel;

	DEBUGMSGTL(("dsx1FarEndCurrentTable", "deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(dsx1FarEndCurrentTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&dsx1FarEndCurrentTableStorage, hciptr);
	}
	DEBUGMSGTL(("dsx1FarEndCurrentTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_dsx1FarEndCurrentTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for dsx1FarEndCurrentTable entries.
 *
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case dsx1FarEndCurrentTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_dsx1FarEndCurrentTable(const char *token, char *line)
{
	size_t tmpsize;
	struct dsx1FarEndCurrentTable_data *StorageTmp = dsx1FarEndCurrentTable_create();

	DEBUGMSGTL(("dsx1FarEndCurrentTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->dsx1FarEndCurrentIndex, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->dsx1FarEndTimeElapsed, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->dsx1FarEndValidIntervals, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->dsx1FarEndCurrentESs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->dsx1FarEndCurrentSESs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->dsx1FarEndCurrentSEFSs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->dsx1FarEndCurrentUASs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->dsx1FarEndCurrentCSSs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->dsx1FarEndCurrentLESs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->dsx1FarEndCurrentPCVs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->dsx1FarEndCurrentBESs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->dsx1FarEndCurrentDMs, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->dsx1FarEndInvalidIntervals, &tmpsize);
	dsx1FarEndCurrentTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("dsx1FarEndCurrentTable", "done.\n"));
}

/*
 * store_dsx1FarEndCurrentTable(): store configuraiton file for dsx1FarEndCurrentTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_dsx1FarEndCurrentTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct dsx1FarEndCurrentTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("dsx1FarEndCurrentTable", "storing data...  "));
	refresh_dsx1FarEndCurrentTable(1);
	(void) tmpsize;
	for (hcindex = dsx1FarEndCurrentTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct dsx1FarEndCurrentTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "dsx1FarEndCurrentTable ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->dsx1FarEndCurrentIndex, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->dsx1FarEndTimeElapsed, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->dsx1FarEndValidIntervals, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->dsx1FarEndCurrentESs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->dsx1FarEndCurrentSESs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->dsx1FarEndCurrentSEFSs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->dsx1FarEndCurrentUASs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->dsx1FarEndCurrentCSSs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->dsx1FarEndCurrentLESs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->dsx1FarEndCurrentPCVs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->dsx1FarEndCurrentBESs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->dsx1FarEndCurrentDMs, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->dsx1FarEndInvalidIntervals, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("dsx1FarEndCurrentTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct dsx1FarEndIntervalTable_data *dsx1FarEndIntervalTable_create(void)
 * @brief create a fresh data structure representing a new row in the dsx1FarEndIntervalTable table.
 *
 * Creates a new dsx1FarEndIntervalTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct dsx1FarEndIntervalTable_data *
dsx1FarEndIntervalTable_create(void)
{
	struct dsx1FarEndIntervalTable_data *StorageNew = SNMP_MALLOC_STRUCT(dsx1FarEndIntervalTable_data);

	DEBUGMSGTL(("dsx1FarEndIntervalTable", "creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->dsx1FarEndIntervalESs = 0;
		StorageNew->dsx1FarEndIntervalSESs = 0;
		StorageNew->dsx1FarEndIntervalSEFSs = 0;
		StorageNew->dsx1FarEndIntervalUASs = 0;
		StorageNew->dsx1FarEndIntervalCSSs = 0;
		StorageNew->dsx1FarEndIntervalLESs = 0;
		StorageNew->dsx1FarEndIntervalPCVs = 0;
		StorageNew->dsx1FarEndIntervalBESs = 0;
		StorageNew->dsx1FarEndIntervalDMs = 0;
		StorageNew->dsx1FarEndIntervalValidData = 0;

	}
	DEBUGMSGTL(("dsx1FarEndIntervalTable", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct dsx1FarEndIntervalTable_data *dsx1FarEndIntervalTable_duplicate(struct dsx1FarEndIntervalTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicat a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct dsx1FarEndIntervalTable_data *
dsx1FarEndIntervalTable_duplicate(struct dsx1FarEndIntervalTable_data *thedata)
{
	struct dsx1FarEndIntervalTable_data *StorageNew = SNMP_MALLOC_STRUCT(dsx1FarEndIntervalTable_data);

	DEBUGMSGTL(("dsx1FarEndIntervalTable", "duplicating row...  "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("dsx1FarEndIntervalTable", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	dsx1FarEndIntervalTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int dsx1FarEndIntervalTable_destroy(struct dsx1FarEndIntervalTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
dsx1FarEndIntervalTable_destroy(struct dsx1FarEndIntervalTable_data **thedata)
{
	struct dsx1FarEndIntervalTable_data *StorageDel;

	DEBUGMSGTL(("dsx1FarEndIntervalTable", "deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("dsx1FarEndIntervalTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int dsx1FarEndIntervalTable_add(struct dsx1FarEndIntervalTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the dsx1FarEndIntervalTable table data set.
 *
 * Adds a table row structure to the dsx1FarEndIntervalTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
dsx1FarEndIntervalTable_add(struct dsx1FarEndIntervalTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("dsx1FarEndIntervalTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index the data */
	/* dsx1FarEndIntervalIndex */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->dsx1FarEndIntervalIndex, sizeof(thedata->dsx1FarEndIntervalIndex));
	/* dsx1FarEndIntervalNumber */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->dsx1FarEndIntervalNumber, sizeof(thedata->dsx1FarEndIntervalNumber));
	header_complex_add_data(&dsx1FarEndIntervalTableStorage, vars, thedata);
	DEBUGMSGTL(("dsx1FarEndIntervalTable", "registered an entry\n"));
	DEBUGMSGTL(("dsx1FarEndIntervalTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int dsx1FarEndIntervalTable_del(struct dsx1FarEndIntervalTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the dsx1FarEndIntervalTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
dsx1FarEndIntervalTable_del(struct dsx1FarEndIntervalTable_data *thedata)
{
	struct dsx1FarEndIntervalTable_data *StorageDel;

	DEBUGMSGTL(("dsx1FarEndIntervalTable", "deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(dsx1FarEndIntervalTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&dsx1FarEndIntervalTableStorage, hciptr);
	}
	DEBUGMSGTL(("dsx1FarEndIntervalTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_dsx1FarEndIntervalTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for dsx1FarEndIntervalTable entries.
 *
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case dsx1FarEndIntervalTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_dsx1FarEndIntervalTable(const char *token, char *line)
{
	size_t tmpsize;
	struct dsx1FarEndIntervalTable_data *StorageTmp = dsx1FarEndIntervalTable_create();

	DEBUGMSGTL(("dsx1FarEndIntervalTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->dsx1FarEndIntervalIndex, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->dsx1FarEndIntervalNumber, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->dsx1FarEndIntervalESs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->dsx1FarEndIntervalSESs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->dsx1FarEndIntervalSEFSs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->dsx1FarEndIntervalUASs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->dsx1FarEndIntervalCSSs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->dsx1FarEndIntervalLESs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->dsx1FarEndIntervalPCVs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->dsx1FarEndIntervalBESs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->dsx1FarEndIntervalDMs, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->dsx1FarEndIntervalValidData, &tmpsize);
	dsx1FarEndIntervalTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("dsx1FarEndIntervalTable", "done.\n"));
}

/*
 * store_dsx1FarEndIntervalTable(): store configuraiton file for dsx1FarEndIntervalTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_dsx1FarEndIntervalTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct dsx1FarEndIntervalTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("dsx1FarEndIntervalTable", "storing data...  "));
	refresh_dsx1FarEndIntervalTable(1);
	(void) tmpsize;
	for (hcindex = dsx1FarEndIntervalTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct dsx1FarEndIntervalTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "dsx1FarEndIntervalTable ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->dsx1FarEndIntervalIndex, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->dsx1FarEndIntervalNumber, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->dsx1FarEndIntervalESs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->dsx1FarEndIntervalSESs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->dsx1FarEndIntervalSEFSs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->dsx1FarEndIntervalUASs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->dsx1FarEndIntervalCSSs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->dsx1FarEndIntervalLESs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->dsx1FarEndIntervalPCVs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->dsx1FarEndIntervalBESs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->dsx1FarEndIntervalDMs, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->dsx1FarEndIntervalValidData, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("dsx1FarEndIntervalTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct dsx1FarEndTotalTable_data *dsx1FarEndTotalTable_create(void)
 * @brief create a fresh data structure representing a new row in the dsx1FarEndTotalTable table.
 *
 * Creates a new dsx1FarEndTotalTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct dsx1FarEndTotalTable_data *
dsx1FarEndTotalTable_create(void)
{
	struct dsx1FarEndTotalTable_data *StorageNew = SNMP_MALLOC_STRUCT(dsx1FarEndTotalTable_data);

	DEBUGMSGTL(("dsx1FarEndTotalTable", "creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->dsx1FarEndTotalESs = 0;
		StorageNew->dsx1FarEndTotalSESs = 0;
		StorageNew->dsx1FarEndTotalSEFSs = 0;
		StorageNew->dsx1FarEndTotalUASs = 0;
		StorageNew->dsx1FarEndTotalCSSs = 0;
		StorageNew->dsx1FarEndTotalLESs = 0;
		StorageNew->dsx1FarEndTotalPCVs = 0;
		StorageNew->dsx1FarEndTotalBESs = 0;
		StorageNew->dsx1FarEndTotalDMs = 0;

	}
	DEBUGMSGTL(("dsx1FarEndTotalTable", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct dsx1FarEndTotalTable_data *dsx1FarEndTotalTable_duplicate(struct dsx1FarEndTotalTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicat a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct dsx1FarEndTotalTable_data *
dsx1FarEndTotalTable_duplicate(struct dsx1FarEndTotalTable_data *thedata)
{
	struct dsx1FarEndTotalTable_data *StorageNew = SNMP_MALLOC_STRUCT(dsx1FarEndTotalTable_data);

	DEBUGMSGTL(("dsx1FarEndTotalTable", "duplicating row...  "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("dsx1FarEndTotalTable", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	dsx1FarEndTotalTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int dsx1FarEndTotalTable_destroy(struct dsx1FarEndTotalTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
dsx1FarEndTotalTable_destroy(struct dsx1FarEndTotalTable_data **thedata)
{
	struct dsx1FarEndTotalTable_data *StorageDel;

	DEBUGMSGTL(("dsx1FarEndTotalTable", "deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("dsx1FarEndTotalTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int dsx1FarEndTotalTable_add(struct dsx1FarEndTotalTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the dsx1FarEndTotalTable table data set.
 *
 * Adds a table row structure to the dsx1FarEndTotalTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
dsx1FarEndTotalTable_add(struct dsx1FarEndTotalTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("dsx1FarEndTotalTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index the data */
	/* dsx1FarEndTotalIndex */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->dsx1FarEndTotalIndex, sizeof(thedata->dsx1FarEndTotalIndex));
	header_complex_add_data(&dsx1FarEndTotalTableStorage, vars, thedata);
	DEBUGMSGTL(("dsx1FarEndTotalTable", "registered an entry\n"));
	DEBUGMSGTL(("dsx1FarEndTotalTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int dsx1FarEndTotalTable_del(struct dsx1FarEndTotalTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the dsx1FarEndTotalTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
dsx1FarEndTotalTable_del(struct dsx1FarEndTotalTable_data *thedata)
{
	struct dsx1FarEndTotalTable_data *StorageDel;

	DEBUGMSGTL(("dsx1FarEndTotalTable", "deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(dsx1FarEndTotalTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&dsx1FarEndTotalTableStorage, hciptr);
	}
	DEBUGMSGTL(("dsx1FarEndTotalTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_dsx1FarEndTotalTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for dsx1FarEndTotalTable entries.
 *
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case dsx1FarEndTotalTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_dsx1FarEndTotalTable(const char *token, char *line)
{
	size_t tmpsize;
	struct dsx1FarEndTotalTable_data *StorageTmp = dsx1FarEndTotalTable_create();

	DEBUGMSGTL(("dsx1FarEndTotalTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->dsx1FarEndTotalIndex, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->dsx1FarEndTotalESs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->dsx1FarEndTotalSESs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->dsx1FarEndTotalSEFSs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->dsx1FarEndTotalUASs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->dsx1FarEndTotalCSSs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->dsx1FarEndTotalLESs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->dsx1FarEndTotalPCVs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->dsx1FarEndTotalBESs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->dsx1FarEndTotalDMs, &tmpsize);
	dsx1FarEndTotalTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("dsx1FarEndTotalTable", "done.\n"));
}

/*
 * store_dsx1FarEndTotalTable(): store configuraiton file for dsx1FarEndTotalTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_dsx1FarEndTotalTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct dsx1FarEndTotalTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("dsx1FarEndTotalTable", "storing data...  "));
	refresh_dsx1FarEndTotalTable(1);
	(void) tmpsize;
	for (hcindex = dsx1FarEndTotalTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct dsx1FarEndTotalTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "dsx1FarEndTotalTable ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->dsx1FarEndTotalIndex, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->dsx1FarEndTotalESs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->dsx1FarEndTotalSESs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->dsx1FarEndTotalSEFSs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->dsx1FarEndTotalUASs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->dsx1FarEndTotalCSSs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->dsx1FarEndTotalLESs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->dsx1FarEndTotalPCVs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->dsx1FarEndTotalBESs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->dsx1FarEndTotalDMs, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("dsx1FarEndTotalTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct dsx1FracTable_data *dsx1FracTable_create(void)
 * @brief create a fresh data structure representing a new row in the dsx1FracTable table.
 *
 * Creates a new dsx1FracTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct dsx1FracTable_data *
dsx1FracTable_create(void)
{
	struct dsx1FracTable_data *StorageNew = SNMP_MALLOC_STRUCT(dsx1FracTable_data);

	DEBUGMSGTL(("dsx1FracTable", "creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->dsx1FracIfIndex = 0;

	}
	DEBUGMSGTL(("dsx1FracTable", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct dsx1FracTable_data *dsx1FracTable_duplicate(struct dsx1FracTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicat a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct dsx1FracTable_data *
dsx1FracTable_duplicate(struct dsx1FracTable_data *thedata)
{
	struct dsx1FracTable_data *StorageNew = SNMP_MALLOC_STRUCT(dsx1FracTable_data);

	DEBUGMSGTL(("dsx1FracTable", "duplicating row...  "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("dsx1FracTable", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	dsx1FracTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int dsx1FracTable_destroy(struct dsx1FracTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
dsx1FracTable_destroy(struct dsx1FracTable_data **thedata)
{
	struct dsx1FracTable_data *StorageDel;

	DEBUGMSGTL(("dsx1FracTable", "deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("dsx1FracTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int dsx1FracTable_add(struct dsx1FracTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the dsx1FracTable table data set.
 *
 * Adds a table row structure to the dsx1FracTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
dsx1FracTable_add(struct dsx1FracTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("dsx1FracTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index the data */
	/* dsx1FracIndex */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->dsx1FracIndex, sizeof(thedata->dsx1FracIndex));
	/* dsx1FracNumber */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->dsx1FracNumber, sizeof(thedata->dsx1FracNumber));
	header_complex_add_data(&dsx1FracTableStorage, vars, thedata);
	DEBUGMSGTL(("dsx1FracTable", "registered an entry\n"));
	DEBUGMSGTL(("dsx1FracTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int dsx1FracTable_del(struct dsx1FracTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the dsx1FracTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
dsx1FracTable_del(struct dsx1FracTable_data *thedata)
{
	struct dsx1FracTable_data *StorageDel;

	DEBUGMSGTL(("dsx1FracTable", "deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(dsx1FracTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&dsx1FracTableStorage, hciptr);
	}
	DEBUGMSGTL(("dsx1FracTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_dsx1FracTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for dsx1FracTable entries.
 *
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case dsx1FracTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_dsx1FracTable(const char *token, char *line)
{
	size_t tmpsize;
	struct dsx1FracTable_data *StorageTmp = dsx1FracTable_create();

	DEBUGMSGTL(("dsx1FracTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->dsx1FracIndex, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->dsx1FracNumber, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->dsx1FracIfIndex, &tmpsize);
	dsx1FracTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("dsx1FracTable", "done.\n"));
}

/*
 * store_dsx1FracTable(): store configuraiton file for dsx1FracTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_dsx1FracTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct dsx1FracTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("dsx1FracTable", "storing data...  "));
	refresh_dsx1FracTable(1);
	(void) tmpsize;
	for (hcindex = dsx1FracTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct dsx1FracTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "dsx1FracTable ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->dsx1FracIndex, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->dsx1FracNumber, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->dsx1FracIfIndex, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("dsx1FracTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct dsx1ChanMappingTable_data *dsx1ChanMappingTable_create(void)
 * @brief create a fresh data structure representing a new row in the dsx1ChanMappingTable table.
 *
 * Creates a new dsx1ChanMappingTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct dsx1ChanMappingTable_data *
dsx1ChanMappingTable_create(void)
{
	struct dsx1ChanMappingTable_data *StorageNew = SNMP_MALLOC_STRUCT(dsx1ChanMappingTable_data);

	DEBUGMSGTL(("dsx1ChanMappingTable", "creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->ifIndex = 0;
		StorageNew->dsx1Ds1ChannelNumber = 0;
		StorageNew->dsx1ChanMappedIfIndex = 0;

	}
	DEBUGMSGTL(("dsx1ChanMappingTable", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct dsx1ChanMappingTable_data *dsx1ChanMappingTable_duplicate(struct dsx1ChanMappingTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicat a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct dsx1ChanMappingTable_data *
dsx1ChanMappingTable_duplicate(struct dsx1ChanMappingTable_data *thedata)
{
	struct dsx1ChanMappingTable_data *StorageNew = SNMP_MALLOC_STRUCT(dsx1ChanMappingTable_data);

	DEBUGMSGTL(("dsx1ChanMappingTable", "duplicating row...  "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("dsx1ChanMappingTable", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	dsx1ChanMappingTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int dsx1ChanMappingTable_destroy(struct dsx1ChanMappingTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
dsx1ChanMappingTable_destroy(struct dsx1ChanMappingTable_data **thedata)
{
	struct dsx1ChanMappingTable_data *StorageDel;

	DEBUGMSGTL(("dsx1ChanMappingTable", "deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("dsx1ChanMappingTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int dsx1ChanMappingTable_add(struct dsx1ChanMappingTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the dsx1ChanMappingTable table data set.
 *
 * Adds a table row structure to the dsx1ChanMappingTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
dsx1ChanMappingTable_add(struct dsx1ChanMappingTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("dsx1ChanMappingTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index the data */
	/* ifIndex */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->ifIndex, sizeof(thedata->ifIndex));
	/* dsx1Ds1ChannelNumber */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->dsx1Ds1ChannelNumber, sizeof(thedata->dsx1Ds1ChannelNumber));
	header_complex_add_data(&dsx1ChanMappingTableStorage, vars, thedata);
	DEBUGMSGTL(("dsx1ChanMappingTable", "registered an entry\n"));
	DEBUGMSGTL(("dsx1ChanMappingTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int dsx1ChanMappingTable_del(struct dsx1ChanMappingTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the dsx1ChanMappingTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
dsx1ChanMappingTable_del(struct dsx1ChanMappingTable_data *thedata)
{
	struct dsx1ChanMappingTable_data *StorageDel;

	DEBUGMSGTL(("dsx1ChanMappingTable", "deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(dsx1ChanMappingTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&dsx1ChanMappingTableStorage, hciptr);
	}
	DEBUGMSGTL(("dsx1ChanMappingTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_dsx1ChanMappingTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for dsx1ChanMappingTable entries.
 *
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case dsx1ChanMappingTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_dsx1ChanMappingTable(const char *token, char *line)
{
	size_t tmpsize;
	struct dsx1ChanMappingTable_data *StorageTmp = dsx1ChanMappingTable_create();

	DEBUGMSGTL(("dsx1ChanMappingTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->ifIndex, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->dsx1Ds1ChannelNumber, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->dsx1ChanMappedIfIndex, &tmpsize);
	dsx1ChanMappingTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("dsx1ChanMappingTable", "done.\n"));
}

/*
 * store_dsx1ChanMappingTable(): store configuraiton file for dsx1ChanMappingTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_dsx1ChanMappingTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct dsx1ChanMappingTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("dsx1ChanMappingTable", "storing data...  "));
	refresh_dsx1ChanMappingTable(1);
	(void) tmpsize;
	for (hcindex = dsx1ChanMappingTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct dsx1ChanMappingTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "dsx1ChanMappingTable ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->ifIndex, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->dsx1Ds1ChannelNumber, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->dsx1ChanMappedIfIndex, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("dsx1ChanMappingTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void refresh_dsx1ConfigTable_row(struct dsx1ConfigTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the dsx1ConfigTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
void
refresh_dsx1ConfigTable_row(struct dsx1ConfigTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->dsx1ConfigTable_request == sa_request))
		return;
	StorageTmp->dsx1ConfigTable_request = sa_request;
}

/**
 * @fn void refresh_dsx1ConfigTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the dsx1ConfigTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_dsx1ConfigTable(int force)
{
	if (!force && dsx1ConfigTable_refresh == 0)
		return;
	dsx1ConfigTable_refresh = 0;
	/* XXX: Here, update the table as required... */
}

/**
 * @fn u_char *var_dsx1ConfigTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in dsx1ConfigTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ds1EXT above.
 */
u_char *
var_dsx1ConfigTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct dsx1ConfigTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ds1EXT", "var_dsx1ConfigTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_dsx1ConfigTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	StorageTmp = header_complex(dsx1ConfigTableStorage, vp, name, length, exact, var_len, write_method);
	refresh_dsx1ConfigTable_row(StorageTmp, 0);
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) DSX1IFINDEX:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1IfIndex);
		rval = (u_char *) &StorageTmp->dsx1IfIndex;
		break;
	case (u_char) DSX1TIMEELAPSED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1TimeElapsed);
		rval = (u_char *) &StorageTmp->dsx1TimeElapsed;
		break;
	case (u_char) DSX1VALIDINTERVALS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1ValidIntervals);
		rval = (u_char *) &StorageTmp->dsx1ValidIntervals;
		break;
	case (u_char) DSX1LINETYPE:	/* ReadWrite */
		*write_method = write_dsx1LineType;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1LineType);
		rval = (u_char *) &StorageTmp->dsx1LineType;
		break;
	case (u_char) DSX1LINECODING:	/* ReadWrite */
		*write_method = write_dsx1LineCoding;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1LineCoding);
		rval = (u_char *) &StorageTmp->dsx1LineCoding;
		break;
	case (u_char) DSX1SENDCODE:	/* ReadWrite */
		*write_method = write_dsx1SendCode;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1SendCode);
		rval = (u_char *) &StorageTmp->dsx1SendCode;
		break;
	case (u_char) DSX1CIRCUITIDENTIFIER:	/* ReadWrite */
		*write_method = write_dsx1CircuitIdentifier;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->dsx1CircuitIdentifierLen;
		rval = (u_char *) StorageTmp->dsx1CircuitIdentifier;
		break;
	case (u_char) DSX1LOOPBACKCONFIG:	/* ReadWrite */
		*write_method = write_dsx1LoopbackConfig;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1LoopbackConfig);
		rval = (u_char *) &StorageTmp->dsx1LoopbackConfig;
		break;
	case (u_char) DSX1LINESTATUS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->dsx1LineStatusLen;
		rval = (u_char *) StorageTmp->dsx1LineStatus;
		break;
	case (u_char) DSX1SIGNALMODE:	/* ReadWrite */
		*write_method = write_dsx1SignalMode;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1SignalMode);
		rval = (u_char *) &StorageTmp->dsx1SignalMode;
		break;
	case (u_char) DSX1TRANSMITCLOCKSOURCE:	/* ReadWrite */
		*write_method = write_dsx1TransmitClockSource;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1TransmitClockSource);
		rval = (u_char *) &StorageTmp->dsx1TransmitClockSource;
		break;
	case (u_char) DSX1FDL:	/* ReadWrite */
		*write_method = write_dsx1Fdl;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->dsx1FdlLen;
		rval = (u_char *) StorageTmp->dsx1Fdl;
		break;
	case (u_char) DSX1INVALIDINTERVALS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1InvalidIntervals);
		rval = (u_char *) &StorageTmp->dsx1InvalidIntervals;
		break;
	case (u_char) DSX1LINELENGTH:	/* ReadWrite */
		*write_method = write_dsx1LineLength;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1LineLength);
		rval = (u_char *) &StorageTmp->dsx1LineLength;
		break;
	case (u_char) DSX1LINESTATUSLASTCHANGE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1LineStatusLastChange);
		rval = (u_char *) &StorageTmp->dsx1LineStatusLastChange;
		break;
	case (u_char) DSX1LINESTATUSCHANGETRAPENABLE:	/* ReadWrite */
		*write_method = write_dsx1LineStatusChangeTrapEnable;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1LineStatusChangeTrapEnable);
		rval = (u_char *) &StorageTmp->dsx1LineStatusChangeTrapEnable;
		break;
	case (u_char) DSX1LOOPBACKSTATUS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->dsx1LoopbackStatusLen;
		rval = (u_char *) StorageTmp->dsx1LoopbackStatus;
		break;
	case (u_char) DSX1DS1CHANNELNUMBER:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1Ds1ChannelNumber);
		rval = (u_char *) &StorageTmp->dsx1Ds1ChannelNumber;
		break;
	case (u_char) DSX1CHANNELIZATION:	/* ReadWrite */
		*write_method = write_dsx1Channelization;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1Channelization);
		rval = (u_char *) &StorageTmp->dsx1Channelization;
		break;
	case (u_char) DSX1LINEMODE:	/* ReadWrite */
		*write_method = write_dsx1LineMode;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1LineMode);
		rval = (u_char *) &StorageTmp->dsx1LineMode;
		break;
	case (u_char) DSX1LINEBUILDOUT:	/* ReadWrite */
		*write_method = write_dsx1LineBuildOut;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1LineBuildOut);
		rval = (u_char *) &StorageTmp->dsx1LineBuildOut;
		break;
	case (u_char) DSX1LINEIMPEDANCE:	/* ReadWrite */
		*write_method = write_dsx1LineImpedance;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1LineImpedance);
		rval = (u_char *) &StorageTmp->dsx1LineImpedance;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn void refresh_dsx1CurrentTable_row(struct dsx1CurrentTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the dsx1CurrentTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
void
refresh_dsx1CurrentTable_row(struct dsx1CurrentTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->dsx1CurrentTable_request == sa_request))
		return;
	StorageTmp->dsx1CurrentTable_request = sa_request;
}

/**
 * @fn void refresh_dsx1CurrentTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the dsx1CurrentTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_dsx1CurrentTable(int force)
{
	if (!force && dsx1CurrentTable_refresh == 0)
		return;
	dsx1CurrentTable_refresh = 0;
	/* XXX: Here, update the table as required... */
}

/**
 * @fn u_char *var_dsx1CurrentTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in dsx1CurrentTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ds1EXT above.
 */
u_char *
var_dsx1CurrentTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct dsx1CurrentTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ds1EXT", "var_dsx1CurrentTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_dsx1CurrentTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	StorageTmp = header_complex(dsx1CurrentTableStorage, vp, name, length, exact, var_len, write_method);
	refresh_dsx1CurrentTable_row(StorageTmp, 0);
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) DSX1CURRENTESS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1CurrentESs);
		rval = (u_char *) &StorageTmp->dsx1CurrentESs;
		break;
	case (u_char) DSX1CURRENTSESS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1CurrentSESs);
		rval = (u_char *) &StorageTmp->dsx1CurrentSESs;
		break;
	case (u_char) DSX1CURRENTSEFSS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1CurrentSEFSs);
		rval = (u_char *) &StorageTmp->dsx1CurrentSEFSs;
		break;
	case (u_char) DSX1CURRENTUASS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1CurrentUASs);
		rval = (u_char *) &StorageTmp->dsx1CurrentUASs;
		break;
	case (u_char) DSX1CURRENTCSSS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1CurrentCSSs);
		rval = (u_char *) &StorageTmp->dsx1CurrentCSSs;
		break;
	case (u_char) DSX1CURRENTPCVS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1CurrentPCVs);
		rval = (u_char *) &StorageTmp->dsx1CurrentPCVs;
		break;
	case (u_char) DSX1CURRENTLESS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1CurrentLESs);
		rval = (u_char *) &StorageTmp->dsx1CurrentLESs;
		break;
	case (u_char) DSX1CURRENTBESS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1CurrentBESs);
		rval = (u_char *) &StorageTmp->dsx1CurrentBESs;
		break;
	case (u_char) DSX1CURRENTDMS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1CurrentDMs);
		rval = (u_char *) &StorageTmp->dsx1CurrentDMs;
		break;
	case (u_char) DSX1CURRENTLCVS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1CurrentLCVs);
		rval = (u_char *) &StorageTmp->dsx1CurrentLCVs;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn void refresh_dsx1IntervalTable_row(struct dsx1IntervalTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the dsx1IntervalTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
void
refresh_dsx1IntervalTable_row(struct dsx1IntervalTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->dsx1IntervalTable_request == sa_request))
		return;
	StorageTmp->dsx1IntervalTable_request = sa_request;
}

/**
 * @fn void refresh_dsx1IntervalTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the dsx1IntervalTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_dsx1IntervalTable(int force)
{
	if (!force && dsx1IntervalTable_refresh == 0)
		return;
	dsx1IntervalTable_refresh = 0;
	/* XXX: Here, update the table as required... */
}

/**
 * @fn u_char *var_dsx1IntervalTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in dsx1IntervalTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ds1EXT above.
 */
u_char *
var_dsx1IntervalTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct dsx1IntervalTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ds1EXT", "var_dsx1IntervalTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_dsx1IntervalTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	StorageTmp = header_complex(dsx1IntervalTableStorage, vp, name, length, exact, var_len, write_method);
	refresh_dsx1IntervalTable_row(StorageTmp, 0);
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) DSX1INTERVALESS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1IntervalESs);
		rval = (u_char *) &StorageTmp->dsx1IntervalESs;
		break;
	case (u_char) DSX1INTERVALSESS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1IntervalSESs);
		rval = (u_char *) &StorageTmp->dsx1IntervalSESs;
		break;
	case (u_char) DSX1INTERVALSEFSS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1IntervalSEFSs);
		rval = (u_char *) &StorageTmp->dsx1IntervalSEFSs;
		break;
	case (u_char) DSX1INTERVALUASS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1IntervalUASs);
		rval = (u_char *) &StorageTmp->dsx1IntervalUASs;
		break;
	case (u_char) DSX1INTERVALCSSS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1IntervalCSSs);
		rval = (u_char *) &StorageTmp->dsx1IntervalCSSs;
		break;
	case (u_char) DSX1INTERVALPCVS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1IntervalPCVs);
		rval = (u_char *) &StorageTmp->dsx1IntervalPCVs;
		break;
	case (u_char) DSX1INTERVALLESS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1IntervalLESs);
		rval = (u_char *) &StorageTmp->dsx1IntervalLESs;
		break;
	case (u_char) DSX1INTERVALBESS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1IntervalBESs);
		rval = (u_char *) &StorageTmp->dsx1IntervalBESs;
		break;
	case (u_char) DSX1INTERVALDMS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1IntervalDMs);
		rval = (u_char *) &StorageTmp->dsx1IntervalDMs;
		break;
	case (u_char) DSX1INTERVALLCVS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1IntervalLCVs);
		rval = (u_char *) &StorageTmp->dsx1IntervalLCVs;
		break;
	case (u_char) DSX1INTERVALVALIDDATA:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1IntervalValidData);
		rval = (u_char *) &StorageTmp->dsx1IntervalValidData;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn void refresh_dsx1TotalTable_row(struct dsx1TotalTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the dsx1TotalTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
void
refresh_dsx1TotalTable_row(struct dsx1TotalTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->dsx1TotalTable_request == sa_request))
		return;
	StorageTmp->dsx1TotalTable_request = sa_request;
}

/**
 * @fn void refresh_dsx1TotalTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the dsx1TotalTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_dsx1TotalTable(int force)
{
	if (!force && dsx1TotalTable_refresh == 0)
		return;
	dsx1TotalTable_refresh = 0;
	/* XXX: Here, update the table as required... */
}

/**
 * @fn u_char *var_dsx1TotalTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in dsx1TotalTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ds1EXT above.
 */
u_char *
var_dsx1TotalTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct dsx1TotalTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ds1EXT", "var_dsx1TotalTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_dsx1TotalTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	StorageTmp = header_complex(dsx1TotalTableStorage, vp, name, length, exact, var_len, write_method);
	refresh_dsx1TotalTable_row(StorageTmp, 0);
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) DSX1TOTALESS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1TotalESs);
		rval = (u_char *) &StorageTmp->dsx1TotalESs;
		break;
	case (u_char) DSX1TOTALSESS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1TotalSESs);
		rval = (u_char *) &StorageTmp->dsx1TotalSESs;
		break;
	case (u_char) DSX1TOTALSEFSS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1TotalSEFSs);
		rval = (u_char *) &StorageTmp->dsx1TotalSEFSs;
		break;
	case (u_char) DSX1TOTALUASS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1TotalUASs);
		rval = (u_char *) &StorageTmp->dsx1TotalUASs;
		break;
	case (u_char) DSX1TOTALCSSS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1TotalCSSs);
		rval = (u_char *) &StorageTmp->dsx1TotalCSSs;
		break;
	case (u_char) DSX1TOTALPCVS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1TotalPCVs);
		rval = (u_char *) &StorageTmp->dsx1TotalPCVs;
		break;
	case (u_char) DSX1TOTALLESS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1TotalLESs);
		rval = (u_char *) &StorageTmp->dsx1TotalLESs;
		break;
	case (u_char) DSX1TOTALBESS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1TotalBESs);
		rval = (u_char *) &StorageTmp->dsx1TotalBESs;
		break;
	case (u_char) DSX1TOTALDMS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1TotalDMs);
		rval = (u_char *) &StorageTmp->dsx1TotalDMs;
		break;
	case (u_char) DSX1TOTALLCVS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1TotalLCVs);
		rval = (u_char *) &StorageTmp->dsx1TotalLCVs;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn void refresh_dsx1FarEndCurrentTable_row(struct dsx1FarEndCurrentTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the dsx1FarEndCurrentTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
void
refresh_dsx1FarEndCurrentTable_row(struct dsx1FarEndCurrentTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->dsx1FarEndCurrentTable_request == sa_request))
		return;
	StorageTmp->dsx1FarEndCurrentTable_request = sa_request;
}

/**
 * @fn void refresh_dsx1FarEndCurrentTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the dsx1FarEndCurrentTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_dsx1FarEndCurrentTable(int force)
{
	if (!force && dsx1FarEndCurrentTable_refresh == 0)
		return;
	dsx1FarEndCurrentTable_refresh = 0;
	/* XXX: Here, update the table as required... */
}

/**
 * @fn u_char *var_dsx1FarEndCurrentTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in dsx1FarEndCurrentTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ds1EXT above.
 */
u_char *
var_dsx1FarEndCurrentTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct dsx1FarEndCurrentTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ds1EXT", "var_dsx1FarEndCurrentTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_dsx1FarEndCurrentTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	StorageTmp = header_complex(dsx1FarEndCurrentTableStorage, vp, name, length, exact, var_len, write_method);
	refresh_dsx1FarEndCurrentTable_row(StorageTmp, 0);
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) DSX1FARENDTIMEELAPSED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1FarEndTimeElapsed);
		rval = (u_char *) &StorageTmp->dsx1FarEndTimeElapsed;
		break;
	case (u_char) DSX1FARENDVALIDINTERVALS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1FarEndValidIntervals);
		rval = (u_char *) &StorageTmp->dsx1FarEndValidIntervals;
		break;
	case (u_char) DSX1FARENDCURRENTESS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1FarEndCurrentESs);
		rval = (u_char *) &StorageTmp->dsx1FarEndCurrentESs;
		break;
	case (u_char) DSX1FARENDCURRENTSESS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1FarEndCurrentSESs);
		rval = (u_char *) &StorageTmp->dsx1FarEndCurrentSESs;
		break;
	case (u_char) DSX1FARENDCURRENTSEFSS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1FarEndCurrentSEFSs);
		rval = (u_char *) &StorageTmp->dsx1FarEndCurrentSEFSs;
		break;
	case (u_char) DSX1FARENDCURRENTUASS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1FarEndCurrentUASs);
		rval = (u_char *) &StorageTmp->dsx1FarEndCurrentUASs;
		break;
	case (u_char) DSX1FARENDCURRENTCSSS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1FarEndCurrentCSSs);
		rval = (u_char *) &StorageTmp->dsx1FarEndCurrentCSSs;
		break;
	case (u_char) DSX1FARENDCURRENTLESS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1FarEndCurrentLESs);
		rval = (u_char *) &StorageTmp->dsx1FarEndCurrentLESs;
		break;
	case (u_char) DSX1FARENDCURRENTPCVS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1FarEndCurrentPCVs);
		rval = (u_char *) &StorageTmp->dsx1FarEndCurrentPCVs;
		break;
	case (u_char) DSX1FARENDCURRENTBESS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1FarEndCurrentBESs);
		rval = (u_char *) &StorageTmp->dsx1FarEndCurrentBESs;
		break;
	case (u_char) DSX1FARENDCURRENTDMS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1FarEndCurrentDMs);
		rval = (u_char *) &StorageTmp->dsx1FarEndCurrentDMs;
		break;
	case (u_char) DSX1FARENDINVALIDINTERVALS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1FarEndInvalidIntervals);
		rval = (u_char *) &StorageTmp->dsx1FarEndInvalidIntervals;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn void refresh_dsx1FarEndIntervalTable_row(struct dsx1FarEndIntervalTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the dsx1FarEndIntervalTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
void
refresh_dsx1FarEndIntervalTable_row(struct dsx1FarEndIntervalTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->dsx1FarEndIntervalTable_request == sa_request))
		return;
	StorageTmp->dsx1FarEndIntervalTable_request = sa_request;
}

/**
 * @fn void refresh_dsx1FarEndIntervalTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the dsx1FarEndIntervalTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_dsx1FarEndIntervalTable(int force)
{
	if (!force && dsx1FarEndIntervalTable_refresh == 0)
		return;
	dsx1FarEndIntervalTable_refresh = 0;
	/* XXX: Here, update the table as required... */
}

/**
 * @fn u_char *var_dsx1FarEndIntervalTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in dsx1FarEndIntervalTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ds1EXT above.
 */
u_char *
var_dsx1FarEndIntervalTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct dsx1FarEndIntervalTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ds1EXT", "var_dsx1FarEndIntervalTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_dsx1FarEndIntervalTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	StorageTmp = header_complex(dsx1FarEndIntervalTableStorage, vp, name, length, exact, var_len, write_method);
	refresh_dsx1FarEndIntervalTable_row(StorageTmp, 0);
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) DSX1FARENDINTERVALESS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1FarEndIntervalESs);
		rval = (u_char *) &StorageTmp->dsx1FarEndIntervalESs;
		break;
	case (u_char) DSX1FARENDINTERVALSESS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1FarEndIntervalSESs);
		rval = (u_char *) &StorageTmp->dsx1FarEndIntervalSESs;
		break;
	case (u_char) DSX1FARENDINTERVALSEFSS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1FarEndIntervalSEFSs);
		rval = (u_char *) &StorageTmp->dsx1FarEndIntervalSEFSs;
		break;
	case (u_char) DSX1FARENDINTERVALUASS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1FarEndIntervalUASs);
		rval = (u_char *) &StorageTmp->dsx1FarEndIntervalUASs;
		break;
	case (u_char) DSX1FARENDINTERVALCSSS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1FarEndIntervalCSSs);
		rval = (u_char *) &StorageTmp->dsx1FarEndIntervalCSSs;
		break;
	case (u_char) DSX1FARENDINTERVALLESS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1FarEndIntervalLESs);
		rval = (u_char *) &StorageTmp->dsx1FarEndIntervalLESs;
		break;
	case (u_char) DSX1FARENDINTERVALPCVS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1FarEndIntervalPCVs);
		rval = (u_char *) &StorageTmp->dsx1FarEndIntervalPCVs;
		break;
	case (u_char) DSX1FARENDINTERVALBESS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1FarEndIntervalBESs);
		rval = (u_char *) &StorageTmp->dsx1FarEndIntervalBESs;
		break;
	case (u_char) DSX1FARENDINTERVALDMS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1FarEndIntervalDMs);
		rval = (u_char *) &StorageTmp->dsx1FarEndIntervalDMs;
		break;
	case (u_char) DSX1FARENDINTERVALVALIDDATA:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1FarEndIntervalValidData);
		rval = (u_char *) &StorageTmp->dsx1FarEndIntervalValidData;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn void refresh_dsx1FarEndTotalTable_row(struct dsx1FarEndTotalTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the dsx1FarEndTotalTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
void
refresh_dsx1FarEndTotalTable_row(struct dsx1FarEndTotalTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->dsx1FarEndTotalTable_request == sa_request))
		return;
	StorageTmp->dsx1FarEndTotalTable_request = sa_request;
}

/**
 * @fn void refresh_dsx1FarEndTotalTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the dsx1FarEndTotalTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_dsx1FarEndTotalTable(int force)
{
	if (!force && dsx1FarEndTotalTable_refresh == 0)
		return;
	dsx1FarEndTotalTable_refresh = 0;
	/* XXX: Here, update the table as required... */
}

/**
 * @fn u_char *var_dsx1FarEndTotalTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in dsx1FarEndTotalTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ds1EXT above.
 */
u_char *
var_dsx1FarEndTotalTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct dsx1FarEndTotalTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ds1EXT", "var_dsx1FarEndTotalTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_dsx1FarEndTotalTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	StorageTmp = header_complex(dsx1FarEndTotalTableStorage, vp, name, length, exact, var_len, write_method);
	refresh_dsx1FarEndTotalTable_row(StorageTmp, 0);
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) DSX1FARENDTOTALESS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1FarEndTotalESs);
		rval = (u_char *) &StorageTmp->dsx1FarEndTotalESs;
		break;
	case (u_char) DSX1FARENDTOTALSESS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1FarEndTotalSESs);
		rval = (u_char *) &StorageTmp->dsx1FarEndTotalSESs;
		break;
	case (u_char) DSX1FARENDTOTALSEFSS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1FarEndTotalSEFSs);
		rval = (u_char *) &StorageTmp->dsx1FarEndTotalSEFSs;
		break;
	case (u_char) DSX1FARENDTOTALUASS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1FarEndTotalUASs);
		rval = (u_char *) &StorageTmp->dsx1FarEndTotalUASs;
		break;
	case (u_char) DSX1FARENDTOTALCSSS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1FarEndTotalCSSs);
		rval = (u_char *) &StorageTmp->dsx1FarEndTotalCSSs;
		break;
	case (u_char) DSX1FARENDTOTALLESS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1FarEndTotalLESs);
		rval = (u_char *) &StorageTmp->dsx1FarEndTotalLESs;
		break;
	case (u_char) DSX1FARENDTOTALPCVS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1FarEndTotalPCVs);
		rval = (u_char *) &StorageTmp->dsx1FarEndTotalPCVs;
		break;
	case (u_char) DSX1FARENDTOTALBESS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1FarEndTotalBESs);
		rval = (u_char *) &StorageTmp->dsx1FarEndTotalBESs;
		break;
	case (u_char) DSX1FARENDTOTALDMS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1FarEndTotalDMs);
		rval = (u_char *) &StorageTmp->dsx1FarEndTotalDMs;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn void refresh_dsx1FracTable_row(struct dsx1FracTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the dsx1FracTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
void
refresh_dsx1FracTable_row(struct dsx1FracTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->dsx1FracTable_request == sa_request))
		return;
	StorageTmp->dsx1FracTable_request = sa_request;
}

/**
 * @fn void refresh_dsx1FracTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the dsx1FracTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_dsx1FracTable(int force)
{
	if (!force && dsx1FracTable_refresh == 0)
		return;
	dsx1FracTable_refresh = 0;
	/* XXX: Here, update the table as required... */
}

/**
 * @fn u_char *var_dsx1FracTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in dsx1FracTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ds1EXT above.
 */
u_char *
var_dsx1FracTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct dsx1FracTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ds1EXT", "var_dsx1FracTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_dsx1FracTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	StorageTmp = header_complex(dsx1FracTableStorage, vp, name, length, exact, var_len, write_method);
	refresh_dsx1FracTable_row(StorageTmp, 0);
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) DSX1FRACIFINDEX:	/* ReadWrite */
		*write_method = write_dsx1FracIfIndex;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1FracIfIndex);
		rval = (u_char *) &StorageTmp->dsx1FracIfIndex;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn void refresh_dsx1ChanMappingTable_row(struct dsx1ChanMappingTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the dsx1ChanMappingTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
void
refresh_dsx1ChanMappingTable_row(struct dsx1ChanMappingTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->dsx1ChanMappingTable_request == sa_request))
		return;
	StorageTmp->dsx1ChanMappingTable_request = sa_request;
}

/**
 * @fn void refresh_dsx1ChanMappingTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the dsx1ChanMappingTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_dsx1ChanMappingTable(int force)
{
	if (!force && dsx1ChanMappingTable_refresh == 0)
		return;
	dsx1ChanMappingTable_refresh = 0;
	/* XXX: Here, update the table as required... */
}

/**
 * @fn u_char *var_dsx1ChanMappingTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in dsx1ChanMappingTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_ds1EXT above.
 */
u_char *
var_dsx1ChanMappingTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct dsx1ChanMappingTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("ds1EXT", "var_dsx1ChanMappingTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_dsx1ChanMappingTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	StorageTmp = header_complex(dsx1ChanMappingTableStorage, vp, name, length, exact, var_len, write_method);
	refresh_dsx1ChanMappingTable_row(StorageTmp, 0);
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) DSX1CHANMAPPEDIFINDEX:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->dsx1ChanMappedIfIndex);
		rval = (u_char *) &StorageTmp->dsx1ChanMappedIfIndex;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int write_dsx1LineType(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_dsx1LineType(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct dsx1ConfigTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 11;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("ds1EXT", "write_dsx1LineType entering action=%d...  \n", action));
	StorageTmp = header_complex(dsx1ConfigTableStorage, NULL, &name[11], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to dsx1LineType not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to dsx1LineType: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case DSX1LINETYPE_OTHER:
		case DSX1LINETYPE_DSX1ESF:
		case DSX1LINETYPE_DSX1D4:
		case DSX1LINETYPE_DSX1E1:
		case DSX1LINETYPE_DSX1E1CRC:
		case DSX1LINETYPE_DSX1E1MF:
		case DSX1LINETYPE_DSX1E1CRCMF:
		case DSX1LINETYPE_DSX1UNFRAMED:
		case DSX1LINETYPE_DSX1E1UNFRAMED:
		case DSX1LINETYPE_DSX1DS2M12:
		case DSX1LINETYPE_DSX1E2:
		case DSX1LINETYPE_DSX1E1Q50:
		case DSX1LINETYPE_DSX1E1Q50CRC:
		case DSX1LINETYPE_DSX1J1ESF:
		case DSX1LINETYPE_DSX1J1UNFRAMED:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to dsx1LineType: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->dsx1LineType for you to use, and you have just been asked to do something with it.  Note that anything done here must be 
				   reversable in the UNDO case */
		old_value = StorageTmp->dsx1LineType;
		StorageTmp->dsx1LineType = set_value;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->dsx1LineType = old_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_dsx1LineCoding(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_dsx1LineCoding(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct dsx1ConfigTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 11;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("ds1EXT", "write_dsx1LineCoding entering action=%d...  \n", action));
	StorageTmp = header_complex(dsx1ConfigTableStorage, NULL, &name[11], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to dsx1LineCoding not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to dsx1LineCoding: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case DSX1LINECODING_DSX1JBZS:
		case DSX1LINECODING_DSX1B8ZS:
		case DSX1LINECODING_DSX1HDB3:
		case DSX1LINECODING_DSX1ZBTSI:
		case DSX1LINECODING_DSX1AMI:
		case DSX1LINECODING_OTHER:
		case DSX1LINECODING_DSX1B6ZS:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to dsx1LineCoding: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->dsx1LineCoding for you to use, and you have just been asked to do something with it.  Note that anything done here must
				   be reversable in the UNDO case */
		old_value = StorageTmp->dsx1LineCoding;
		StorageTmp->dsx1LineCoding = set_value;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->dsx1LineCoding = old_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_dsx1SendCode(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_dsx1SendCode(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct dsx1ConfigTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 11;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("ds1EXT", "write_dsx1SendCode entering action=%d...  \n", action));
	StorageTmp = header_complex(dsx1ConfigTableStorage, NULL, &name[11], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to dsx1SendCode not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to dsx1SendCode: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case DSX1SENDCODE_DSX1SENDNOCODE:
		case DSX1SENDCODE_DSX1SENDLINECODE:
		case DSX1SENDCODE_DSX1SENDPAYLOADCODE:
		case DSX1SENDCODE_DSX1SENDRESETCODE:
		case DSX1SENDCODE_DSX1SENDQRS:
		case DSX1SENDCODE_DSX1SEND511PATTERN:
		case DSX1SENDCODE_DSX1SEND3IN24PATTERN:
		case DSX1SENDCODE_DSX1SENDOTHERTESTPATTERN:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to dsx1SendCode: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->dsx1SendCode for you to use, and you have just been asked to do something with it.  Note that anything done here must be 
				   reversable in the UNDO case */
		old_value = StorageTmp->dsx1SendCode;
		StorageTmp->dsx1SendCode = set_value;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->dsx1SendCode = old_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_dsx1CircuitIdentifier(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_dsx1CircuitIdentifier(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static uint8_t *old_value;
	struct dsx1ConfigTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 11;
	static size_t old_length = 0;
	static uint8_t *string = NULL;

	DEBUGMSGTL(("ds1EXT", "write_dsx1CircuitIdentifier entering action=%d...  \n", action));
	StorageTmp = header_complex(dsx1ConfigTableStorage, NULL, &name[11], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to dsx1CircuitIdentifier not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 0..255 */
		if (var_val_len > SPRINT_MAX_LEN || ((0 > var_val_len || var_val_len > 255))) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to dsx1CircuitIdentifier: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		break;
	case FREE:		/* Release any resources that have been allocated */
		SNMP_FREE(string);
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->dsx1CircuitIdentifier for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		old_value = StorageTmp->dsx1CircuitIdentifier;
		old_length = StorageTmp->dsx1CircuitIdentifierLen;
		StorageTmp->dsx1CircuitIdentifier = string;
		StorageTmp->dsx1CircuitIdentifierLen = var_val_len;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->dsx1CircuitIdentifier = old_value;
		StorageTmp->dsx1CircuitIdentifierLen = old_length;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(old_value);
		old_length = 0;
		string = NULL;
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_dsx1LoopbackConfig(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_dsx1LoopbackConfig(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct dsx1ConfigTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 11;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("ds1EXT", "write_dsx1LoopbackConfig entering action=%d...  \n", action));
	StorageTmp = header_complex(dsx1ConfigTableStorage, NULL, &name[11], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to dsx1LoopbackConfig not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to dsx1LoopbackConfig: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case DSX1LOOPBACKCONFIG_DSX1NOLOOP:
		case DSX1LOOPBACKCONFIG_DSX1PAYLOADLOOP:
		case DSX1LOOPBACKCONFIG_DSX1LINELOOP:
		case DSX1LOOPBACKCONFIG_DSX1OTHERLOOP:
		case DSX1LOOPBACKCONFIG_DSX1INWARDLOOP:
		case DSX1LOOPBACKCONFIG_DSX1DUALLOOP:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to dsx1LoopbackConfig: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->dsx1LoopbackConfig for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		old_value = StorageTmp->dsx1LoopbackConfig;
		StorageTmp->dsx1LoopbackConfig = set_value;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->dsx1LoopbackConfig = old_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_dsx1SignalMode(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_dsx1SignalMode(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct dsx1ConfigTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 11;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("ds1EXT", "write_dsx1SignalMode entering action=%d...  \n", action));
	StorageTmp = header_complex(dsx1ConfigTableStorage, NULL, &name[11], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to dsx1SignalMode not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to dsx1SignalMode: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case DSX1SIGNALMODE_NONE:
		case DSX1SIGNALMODE_ROBBEDBIT:
		case DSX1SIGNALMODE_BITORIENTED:
		case DSX1SIGNALMODE_MESSAGEORIENTED:
		case DSX1SIGNALMODE_OTHER:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to dsx1SignalMode: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->dsx1SignalMode for you to use, and you have just been asked to do something with it.  Note that anything done here must
				   be reversable in the UNDO case */
		old_value = StorageTmp->dsx1SignalMode;
		StorageTmp->dsx1SignalMode = set_value;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->dsx1SignalMode = old_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_dsx1TransmitClockSource(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_dsx1TransmitClockSource(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct dsx1ConfigTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 11;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("ds1EXT", "write_dsx1TransmitClockSource entering action=%d...  \n", action));
	StorageTmp = header_complex(dsx1ConfigTableStorage, NULL, &name[11], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to dsx1TransmitClockSource not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to dsx1TransmitClockSource: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case DSX1TRANSMITCLOCKSOURCE_LOOPTIMING:
		case DSX1TRANSMITCLOCKSOURCE_LOCALTIMING:
		case DSX1TRANSMITCLOCKSOURCE_THROUGHTIMING:
		case DSX1TRANSMITCLOCKSOURCE_ADAPTIVE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to dsx1TransmitClockSource: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->dsx1TransmitClockSource for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		old_value = StorageTmp->dsx1TransmitClockSource;
		StorageTmp->dsx1TransmitClockSource = set_value;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->dsx1TransmitClockSource = old_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_dsx1Fdl(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_dsx1Fdl(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static uint8_t *old_value;
	struct dsx1ConfigTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 11;
	static size_t old_length = 0;
	static uint8_t *string = NULL;

	DEBUGMSGTL(("ds1EXT", "write_dsx1Fdl entering action=%d...  \n", action));
	StorageTmp = header_complex(dsx1ConfigTableStorage, NULL, &name[11], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_BIT_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to dsx1Fdl not ASN_BIT_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (1 > var_val_len || var_val_len > SPRINT_MAX_LEN || var_val_len != 1) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to dsx1Fdl: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		break;
	case FREE:		/* Release any resources that have been allocated */
		SNMP_FREE(string);
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->dsx1Fdl for you to use, and you have just been asked to do something with it.  Note that anything done here must be
				   reversable in the UNDO case */
		old_value = StorageTmp->dsx1Fdl;
		old_length = StorageTmp->dsx1FdlLen;
		StorageTmp->dsx1Fdl = string;
		StorageTmp->dsx1FdlLen = var_val_len;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->dsx1Fdl = old_value;
		StorageTmp->dsx1FdlLen = old_length;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(old_value);
		old_length = 0;
		string = NULL;
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_dsx1LineLength(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_dsx1LineLength(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct dsx1ConfigTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 11;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("ds1EXT", "write_dsx1LineLength entering action=%d...  \n", action));
	StorageTmp = header_complex(dsx1ConfigTableStorage, NULL, &name[11], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to dsx1LineLength not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to dsx1LineLength: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..64000 */
		if ((0 > set_value || set_value > 64000)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to dsx1LineLength: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->dsx1LineLength for you to use, and you have just been asked to do something with it.  Note that anything done here must
				   be reversable in the UNDO case */
		old_value = StorageTmp->dsx1LineLength;
		StorageTmp->dsx1LineLength = set_value;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->dsx1LineLength = old_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_dsx1LineStatusChangeTrapEnable(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_dsx1LineStatusChangeTrapEnable(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct dsx1ConfigTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 11;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("ds1EXT", "write_dsx1LineStatusChangeTrapEnable entering action=%d...  \n", action));
	StorageTmp = header_complex(dsx1ConfigTableStorage, NULL, &name[11], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to dsx1LineStatusChangeTrapEnable not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to dsx1LineStatusChangeTrapEnable: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value disabled */
		switch (set_value) {
		case DSX1LINESTATUSCHANGETRAPENABLE_ENABLED:
		case DSX1LINESTATUSCHANGETRAPENABLE_DISABLED:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to dsx1LineStatusChangeTrapEnable: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->dsx1LineStatusChangeTrapEnable for you to use, and you have just been asked to do something with it.  Note that anything 
				   done here must be reversable in the UNDO case */
		old_value = StorageTmp->dsx1LineStatusChangeTrapEnable;
		StorageTmp->dsx1LineStatusChangeTrapEnable = set_value;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->dsx1LineStatusChangeTrapEnable = old_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_dsx1Channelization(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_dsx1Channelization(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct dsx1ConfigTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 11;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("ds1EXT", "write_dsx1Channelization entering action=%d...  \n", action));
	StorageTmp = header_complex(dsx1ConfigTableStorage, NULL, &name[11], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to dsx1Channelization not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to dsx1Channelization: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case DSX1CHANNELIZATION_DISABLED:
		case DSX1CHANNELIZATION_ENABLEDDS0:
		case DSX1CHANNELIZATION_ENABLEDDS1:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to dsx1Channelization: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->dsx1Channelization for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		old_value = StorageTmp->dsx1Channelization;
		StorageTmp->dsx1Channelization = set_value;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->dsx1Channelization = old_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_dsx1LineMode(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_dsx1LineMode(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct dsx1ConfigTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 11;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("ds1EXT", "write_dsx1LineMode entering action=%d...  \n", action));
	StorageTmp = header_complex(dsx1ConfigTableStorage, NULL, &name[11], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to dsx1LineMode not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to dsx1LineMode: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case DSX1LINEMODE_CSU:
		case DSX1LINEMODE_DSU:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to dsx1LineMode: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->dsx1LineMode for you to use, and you have just been asked to do something with it.  Note that anything done here must be 
				   reversable in the UNDO case */
		old_value = StorageTmp->dsx1LineMode;
		StorageTmp->dsx1LineMode = set_value;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->dsx1LineMode = old_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_dsx1LineBuildOut(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_dsx1LineBuildOut(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct dsx1ConfigTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 11;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("ds1EXT", "write_dsx1LineBuildOut entering action=%d...  \n", action));
	StorageTmp = header_complex(dsx1ConfigTableStorage, NULL, &name[11], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to dsx1LineBuildOut not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to dsx1LineBuildOut: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case DSX1LINEBUILDOUT_NOTAPPLICABLE:
		case DSX1LINEBUILDOUT_NEG75DB:
		case DSX1LINEBUILDOUT_NEG15DB:
		case DSX1LINEBUILDOUT_NEG225DB:
		case DSX1LINEBUILDOUT_ZERODB:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to dsx1LineBuildOut: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->dsx1LineBuildOut for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		old_value = StorageTmp->dsx1LineBuildOut;
		StorageTmp->dsx1LineBuildOut = set_value;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->dsx1LineBuildOut = old_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_dsx1LineImpedance(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_dsx1LineImpedance(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct dsx1ConfigTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 11;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("ds1EXT", "write_dsx1LineImpedance entering action=%d...  \n", action));
	StorageTmp = header_complex(dsx1ConfigTableStorage, NULL, &name[11], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to dsx1LineImpedance not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to dsx1LineImpedance: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case DSX1LINEIMPEDANCE_NOTAPPLICABLE:
		case DSX1LINEIMPEDANCE_UNBALANCED75OHMS:
		case DSX1LINEIMPEDANCE_BALANCED100OHMS:
		case DSX1LINEIMPEDANCE_BALANCED120OHMS:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to dsx1LineImpedance: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->dsx1LineImpedance for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		old_value = StorageTmp->dsx1LineImpedance;
		StorageTmp->dsx1LineImpedance = set_value;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->dsx1LineImpedance = old_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_dsx1FracIfIndex(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_dsx1FracIfIndex(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct dsx1FracTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 11;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("ds1EXT", "write_dsx1FracIfIndex entering action=%d...  \n", action));
	StorageTmp = header_complex(dsx1FracTableStorage, NULL, &name[11], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to dsx1FracIfIndex not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to dsx1FracIfIndex: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to dsx1FracIfIndex: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->dsx1FracIfIndex for you to use, and you have just been asked to do something with it.  Note that anything done here must 
				   be reversable in the UNDO case */
		old_value = StorageTmp->dsx1FracIfIndex;
		StorageTmp->dsx1FracIfIndex = set_value;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->dsx1FracIfIndex = old_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int dsx1ConfigTable_consistent(struct dsx1ConfigTable_data *thedata)
 * @param thedata the row data to check for consistency.
 * @brief check the internal consistency of a table row.
 *
 * This function checks the internal consistency of a table row for the dsx1ConfigTable table.  If the
 * table row is internally consistent, then this function returns true (1), otherwise the function
 * returns false (0) and it will not be possible to activate the row until the row's internal
 * consistency is corrected.
 */
int
dsx1ConfigTable_consistent(struct dsx1ConfigTable_data *thedata)
{
	/* XXX: check row consistency return true(1) if consistent, or false(0) if not. */
	return (1);
}

/**
 * @fn int dsx1CurrentTable_consistent(struct dsx1CurrentTable_data *thedata)
 * @param thedata the row data to check for consistency.
 * @brief check the internal consistency of a table row.
 *
 * This function checks the internal consistency of a table row for the dsx1CurrentTable table.  If the
 * table row is internally consistent, then this function returns true (1), otherwise the function
 * returns false (0) and it will not be possible to activate the row until the row's internal
 * consistency is corrected.
 */
int
dsx1CurrentTable_consistent(struct dsx1CurrentTable_data *thedata)
{
	/* XXX: check row consistency return true(1) if consistent, or false(0) if not. */
	return (1);
}

/**
 * @fn int dsx1IntervalTable_consistent(struct dsx1IntervalTable_data *thedata)
 * @param thedata the row data to check for consistency.
 * @brief check the internal consistency of a table row.
 *
 * This function checks the internal consistency of a table row for the dsx1IntervalTable table.  If the
 * table row is internally consistent, then this function returns true (1), otherwise the function
 * returns false (0) and it will not be possible to activate the row until the row's internal
 * consistency is corrected.
 */
int
dsx1IntervalTable_consistent(struct dsx1IntervalTable_data *thedata)
{
	/* XXX: check row consistency return true(1) if consistent, or false(0) if not. */
	return (1);
}

/**
 * @fn int dsx1TotalTable_consistent(struct dsx1TotalTable_data *thedata)
 * @param thedata the row data to check for consistency.
 * @brief check the internal consistency of a table row.
 *
 * This function checks the internal consistency of a table row for the dsx1TotalTable table.  If the
 * table row is internally consistent, then this function returns true (1), otherwise the function
 * returns false (0) and it will not be possible to activate the row until the row's internal
 * consistency is corrected.
 */
int
dsx1TotalTable_consistent(struct dsx1TotalTable_data *thedata)
{
	/* XXX: check row consistency return true(1) if consistent, or false(0) if not. */
	return (1);
}

/**
 * @fn int dsx1FarEndCurrentTable_consistent(struct dsx1FarEndCurrentTable_data *thedata)
 * @param thedata the row data to check for consistency.
 * @brief check the internal consistency of a table row.
 *
 * This function checks the internal consistency of a table row for the dsx1FarEndCurrentTable table.  If the
 * table row is internally consistent, then this function returns true (1), otherwise the function
 * returns false (0) and it will not be possible to activate the row until the row's internal
 * consistency is corrected.
 */
int
dsx1FarEndCurrentTable_consistent(struct dsx1FarEndCurrentTable_data *thedata)
{
	/* XXX: check row consistency return true(1) if consistent, or false(0) if not. */
	return (1);
}

/**
 * @fn int dsx1FarEndIntervalTable_consistent(struct dsx1FarEndIntervalTable_data *thedata)
 * @param thedata the row data to check for consistency.
 * @brief check the internal consistency of a table row.
 *
 * This function checks the internal consistency of a table row for the dsx1FarEndIntervalTable table.  If the
 * table row is internally consistent, then this function returns true (1), otherwise the function
 * returns false (0) and it will not be possible to activate the row until the row's internal
 * consistency is corrected.
 */
int
dsx1FarEndIntervalTable_consistent(struct dsx1FarEndIntervalTable_data *thedata)
{
	/* XXX: check row consistency return true(1) if consistent, or false(0) if not. */
	return (1);
}

/**
 * @fn int dsx1FarEndTotalTable_consistent(struct dsx1FarEndTotalTable_data *thedata)
 * @param thedata the row data to check for consistency.
 * @brief check the internal consistency of a table row.
 *
 * This function checks the internal consistency of a table row for the dsx1FarEndTotalTable table.  If the
 * table row is internally consistent, then this function returns true (1), otherwise the function
 * returns false (0) and it will not be possible to activate the row until the row's internal
 * consistency is corrected.
 */
int
dsx1FarEndTotalTable_consistent(struct dsx1FarEndTotalTable_data *thedata)
{
	/* XXX: check row consistency return true(1) if consistent, or false(0) if not. */
	return (1);
}

/**
 * @fn int dsx1FracTable_consistent(struct dsx1FracTable_data *thedata)
 * @param thedata the row data to check for consistency.
 * @brief check the internal consistency of a table row.
 *
 * This function checks the internal consistency of a table row for the dsx1FracTable table.  If the
 * table row is internally consistent, then this function returns true (1), otherwise the function
 * returns false (0) and it will not be possible to activate the row until the row's internal
 * consistency is corrected.
 */
int
dsx1FracTable_consistent(struct dsx1FracTable_data *thedata)
{
	/* XXX: check row consistency return true(1) if consistent, or false(0) if not. */
	return (1);
}

/**
 * @fn int dsx1ChanMappingTable_consistent(struct dsx1ChanMappingTable_data *thedata)
 * @param thedata the row data to check for consistency.
 * @brief check the internal consistency of a table row.
 *
 * This function checks the internal consistency of a table row for the dsx1ChanMappingTable table.  If the
 * table row is internally consistent, then this function returns true (1), otherwise the function
 * returns false (0) and it will not be possible to activate the row until the row's internal
 * consistency is corrected.
 */
int
dsx1ChanMappingTable_consistent(struct dsx1ChanMappingTable_data *thedata)
{
	/* XXX: check row consistency return true(1) if consistent, or false(0) if not. */
	return (1);
}

void
send_dsx1LineStatusChange_v2trap(struct variable_list *vars)
{
	struct variable_list trap;

	trap.next_variable = vars;
	trap.name = snmpTrapOID_oid;
	trap.name_length = sizeof(snmpTrapOID_oid) / sizeof(oid);
	trap.type = ASN_OBJECT_ID;
	trap.val.objid = dsx1LineStatusChange_oid;
	trap.val_len = sizeof(dsx1LineStatusChange_oid);
	trap.index = 0;
	send_v2trap(&trap);
}

#if defined MODULE
#if defined MASTER
/**
 * @fn void ds1EXT_loop_handler(int dummy)
 * @param sig signal number
 * @brief handle event loop interation.
 *
 * This function is registered so that, when operating as a module, snmpd will call it one per event
 * loop interation.  This function is called before the next requst is processed and after the
 * previous request is processed.  Two things are done here:  1) The file descriptor that is used to
 * synchronize the agent with (pseudo-)device drivers is closed.  (Another approach, instead of
 * closing each time, would be to restart a timer each time that a request is made (loop is
 * performed) and if it expires, close the file descriptor).  2) The request number is incremented.
 * Although a request is not generated for each loop of the snmp event loop, it is true that a new
 * request cannot be generated without performing a loop.  Therefore, the sa_request is not the
 * request number but it is a temporally unique identifier for a request.
 */
void
ds1EXT_loop_handler(int sig)
{
	if (external_signal_scheduled[sig] == 0)
		external_signal_scheduled[sig]--;
	/* close files after each request */
	if (sa_fclose) {
		if (sa_fd != 0) {
			close(sa_fd);
			sa_fd = 0;
		}
		if (my_fd != 0) {
			close(my_fd);
			my_fd = 0;
		}
	}
	/* prepare for next request */
	sa_request++;
	if (ds1EXTold_signal_handler != NULL)
		(*ds1EXTold_signal_handler) (sig);
}
#endif				/* defined MASTER */
/**
 * @fn void ds1EXT_fd_handler(int fd, void *dummy)
 * @param fd file descriptor to read.
 * @param dummy client data passed to registration function (always NULL).
 * @brief handle read event on file descriptor.
 *
 * This read file descriptor handler is normally used for (pseudo-)device drivers that generate
 * statistical collection interval events, alarm events, or other operational measurement events, by
 * placing a message on the read queue of the "event handling" Stream.  Normally this routine
 * would adjust counts in some table or scalars, generate SNMP traps representing on-occurence
 * events, first and interval events, and alarm indications.
 */
void
ds1EXT_fd_handler(int fd, void *dummy)
{
	/* XXX: place actions to handle sa_readfd here... */
	return;
}
#endif				/* defined MOUDLE */
#if defined MASTER
const char sa_program[] = "ds1ext";
int sa_fclose = 1;			/* default close files between requests */
int sa_fd = 0;				/* file descriptor for MIB use */
int sa_readfd = 0;			/* file descriptor for autonomnous events */
int sa_changed = 1;			/* indication to reread MIB configuration */
int sa_stats_refresh = 1;		/* indications that statistics, the mib or its tables need to be refreshed */
int sa_request = 1;			/* request number for per-request actions */
#endif				/* defined MASTER */
#if defined MASTER
#if !defined MODULE
int sa_dump = 0;			/* default packet dump */
int sa_debug = 0;			/* default no debug */
int sa_nomead = 1;			/* default daemon mode */
int sa_output = 1;			/* default normal output */
int sa_agentx = 1;			/* default agentx mode */
int sa_alarms = 1;			/* default application alarms */
int sa_logaddr = 0;			/* log addresses */
int sa_logfillog = 0;			/* log to sa_logfile */
int sa_logstderr = 0;			/* log to standard error */
int sa_logstdout = 0;			/* log to standard output */
int sa_logsyslog = 0;			/* log to system logs */
int sa_logcallog = 0;			/* log to callback logs */
int sa_appendlog = 0;			/* append to log file without truncating */
char sa_logfile[256] = "/var/log/ds1ext.log";
char sa_pidfile[256] = "/var/run/ds1ext.pid";
char sa_sysctlf[256] = "/etc/ds1ext.conf";
int allow_severity = LOG_ERR;
int deny_severity = LOG_ERR;

/* file stream for log file */
FILE *stdlog = NULL;
static void
sa_version(int argc, char *argv[])
{
	if (!sa_output && !sa_debug)
		return;
	fprintf(stdout, "\
%2$s\n\
Copyright (c) 2008-2009  Monavacon Limited.  All Rights Reserved.\n\
Distributed under Affero GPL Version 3, included here by reference.\n\
See `%1$s --copying' for copying permissions.\n\
", argv[0], ident);
}
static void
sa_usage(int argc, char *argv[])
{
	if (!sa_output && !sa_debug)
		return;
	fprintf(stderr, "\
Usage:\n\
    %1$s [general-options] [options] [arguments]\n\
    %1$s {-H|--help-directives}\n\
    %1$s {-h|--help}\n\
    %1$s {-V|--version}\n\
    %1$s {-C|--copying}\n\
", argv[0]);
}
static void
sa_help(int argc, char *argv[])
{
	if (!sa_output && !sa_debug)
		return;
	fprintf(stdout, "\
Usage:\n\
    %1$s [general-options] [options] [arguments]\n\
    %1$s {-h|--help}\n\
    %1$s {-V|--version}\n\
    %1$s {-C|--copying}\n\
Arguments:\n\
    None.\n\
Options:\n\
    -a, --log-addresses\n\
        log addresses of connecting management stations.\n\
    -A, --append\n\
        append to logfiles without truncating.\n\
    -c, --config-file CONFIGFILE\n\
        use configuration file CONFIGFILE.\n\
    -C, --config-only\n\
        only load configuration given by -c option.\n\
    -d, --dump\n\
        dump sent and received PDUs.\n\
    -D, --debug [LEVEL]\n\
        set debugging verbosity to LEVEL.\n\
    -D, --debug-tokens [TOKEN[,TOKEN]*]\n\
        debug specified TOKEN's.\n\
    -f, --dont-fork\n\
        run in the foreground.\n\
    -g, --gid, --groupid GID\n\
        become group GID after listening.\n\
    -h, --help, -?, --?\n\
        print usage information and exit.\n\
    -H, --help-directives\n\
        print config directives and exit.\n\
    -I, --initialize [-]MODULE[,MODULE]*\n\
        initialize (or not, '-') these MODULE's.\n\
    -k, --keep-open\n\
        keep system files open between requests.\n\
    -l, --log-file [LOGFILE]\n\
        log to log file name LOGFILE.  [default: /var/log/ds1ext.log]\n\
    -L, --log-stderr\n\
        log to controlling terminal standard error.\n\
    -m, --mibs [+]MIB[,MIB]*\n\
        load these (additional '+') MIBs.\n\
    -M, --master\n\
        run as SNMP master instead of AgentX sub-agent.\n\
    -M, --mibdirs [+]MIBDIR[:MIBDIR]*\n\
        search these (additional, '+') colon separated directories for MIBs.\n\
    -n, --nodaemon\n\
        run in the foreground.\n\
    -n, --name NAME\n\
        use NAME for configuration file base.  [default: ds1ext]\n\
    -p, --port PORTNUM\n\
        listen on port number PORTNUM.  [default: 161]\n\
    -p, --pidfile PIDFILE\n\
        write daemon pid to PIDFILE.  [default: /var/run/ds1ext.pid]\n\
    -P, --pidfile PIDFILE\n\
        write daemon pid to PIDFILE.  [default: /var/run/ds1ext.pid]\n\
    -q, --quiet\n\
        suppress normal output.\n\
    -q, --quick\n\
        abbreviate output for machine readability.\n\
    -r, --noroot\n\
        do not require root privilege.\n\
    -s, --log-syslog\n\
        log to system logs.\n\
    -S, --sysctl-file FILENAME\n\
        write sysctl config file FILENAME.  [default: /etc/streams.conf]\n\
    -t, --agent-alarms\n\
        agent blocks {SIGALARM}.\n\
    -T, --transport [TRANSPORT]\n\
        default transport TRANSPORT.  [default: udp]\n\
    -u, --uid, --userid UID\n\
        become user UID after listening.\n\
    -U, --dont-remove-pidfile\n\
        do not remove PIDFILE when shutting down.\n\
    -v, --version\n\
        print version information and exit.\n\
    -V, --verbose [LEVEL]\n\
        be verbose to LEVEL.  [default: 1]\n\
    -x, --agentx-socket [SOCKET]\n\
        master AgentX on SOCKET.  [default: /var/agentx/master]\n\
    -X, --agentx\n\
        run as AgentX sub-agent instead of master (the default).\n\
    -y, --copying\n\
        print copying information and exit.\n\
", argv[0]);
}
static void
sa_copying(int argc, char *argv[])
{
	if (!sa_output && !sa_debug)
		return;
	fprintf(stdout, "\
--------------------------------------------------------------------------------\n\
%1$s\n\
--------------------------------------------------------------------------------\n\
Copyright (c) 2008-2009  Monavacon Limited <http://www.monavacon.com>\n\
Copyright (c) 2001-2008  OpenSS7 Corporation <http://www.openss7.com>\n\
Copyright (c) 1997-2000  Brian F. G. Bidulock <bidulock@openss7.org>\n\
\n\
All Rights Reserved.\n\
--------------------------------------------------------------------------------\n\
This program is free software; you can  redistribute  it and/or modify  it under\n\
the terms of the GNU Affero General Public License as published by the Free\n\
Software Foundation; Version 3 of the License.\n\
\n\
This program is distributed in the hope that it will  be useful, but WITHOUT ANY\n\
WARRANTY; without even  the implied warranty of MERCHANTABILITY or FITNESS FOR A\n\
PARTICULAR PURPOSE.  See the GNU Affero General Public License for more details.\n\
\n\
You should have received a copy of the GNU  Affero  General Public License along\n\
with this program.   If not, see <http://www.gnu.org/licenses/>, or write to the\n\
Free Software Foundation, Inc., 675 Mass Ave, Cambridge, MA 02139, USA.\n\
--------------------------------------------------------------------------------\n\
U.S. GOVERNMENT RESTRICTED RIGHTS.  If you are licensing this Software on behalf\n\
of the U.S. Government (\"Government\"), the following provisions apply to you. If\n\
the Software is supplied by the  Department of Defense (\"DoD\"), it is classified\n\
as \"Commercial  Computer  Software\"  under  paragraph  252.227-7014  of the  DoD\n\
Supplement  to the  Federal Acquisition Regulations  (\"DFARS\") (or any successor\n\
regulations) and the  Government  is acquiring  only the  license rights granted\n\
herein (the license rights customarily provided to non-Government users). If the\n\
Software is supplied to any unit or agency of the Government  other than DoD, it\n\
is  classified as  \"Restricted Computer Software\" and the Government's rights in\n\
the Software  are defined  in  paragraph 52.227-19  of the  Federal  Acquisition\n\
Regulations (\"FAR\")  (or any successor regulations) or, in the cases of NASA, in\n\
paragraph  18.52.227-86 of  the  NASA  Supplement  to the FAR (or any  successor\n\
regulations).\n\
--------------------------------------------------------------------------------\n\
Commercial  licensing  and  support of this  software is  available from OpenSS7\n\
Corporation at a fee.  See http://www.openss7.com/\n\
--------------------------------------------------------------------------------\n\
", ident);
}

void
sa_help_directives(int argc, char *argv[])
{
	ds_set_boolean(DS_APPLICATION_ID, DS_AGENT_NO_ROOT_ACCESS, 1);
	init_agent("ds1EXT");
	// init_mib_modules();
	init_mib();
	init_snmp("ds1EXT");
	snmp_log(MY_FACILITY(LOG_INFO), "Configuration directives understood:\n");
	/* Unfortunately, read_config_print_usage() uses snmp_log(), meaning that it can only be writen to standard error and not standard output. */
	read_config_print_usage("    ");
}
static int
sa_sig_register(int signum, RETSIGTYPE(*handler) (int))
{
	sigset_t mask;
	struct sigaction act;

	act.sa_handler = handler ? handler : SIG_DFL;
	act.sa_flags = handler ? SA_RESTART : 0;
	sigemptyset(&act.sa_mask);
	if (sigaction(signum, &act, NULL))
		return (-1);
	sigemptyset(&mask);
	sigaddset(&mask, signum);
	sigprocmask(handler ? SIG_UNBLOCK : SIG_BLOCK, &mask, NULL);
	return (0);
}
static int sa_alm_signal = 0;
static int sa_pol_signal = 0;
static int sa_hup_signal = 0;
static int sa_int_signal = 0;
static int sa_trm_signal = 0;
static int sa_alm_handle = 0;
void
sa_alm_callback(uint req, void *arg)
{
	if (req == sa_alm_handle)
		sa_alm_handle = 0;
	sa_alm_signal = 1;
	return;
}

static RETSIGTYPE
sa_alm_handler(int signum)
{
	sa_alm_signal = 1;
	return (RETSIGTYPE) (0);
}
static void
sa_snmp_alm_handler(uint reg, void *clientarg)
{
	sa_alm_signal = 1;
	return;
}
static int
sa_alm_catch(void)
{
	if (sa_alarms)
		return sa_sig_register(SIGALRM, &sa_alm_handler);
	return (-1);
}
static int
sa_alm_block(void)
{
	if (sa_alarms)
		return sa_sig_register(SIGALRM, NULL);
	if (sa_alm_handle) {
		uint handle = sa_alm_handle;

		sa_alm_handle = 0;
		snmp_alarm_unregister(handle);
	}
	return (0);
}
static int
sa_alm_action(void)
{
	sa_alm_signal = 0;
	return (0);
}

static RETSIGTYPE
sa_pol_handler(int signum)
{
	sa_pol_signal = 1;
	return (RETSIGTYPE) (0);
}
static int
sa_pol_catch(void)
{
	return sa_sig_register(SIGPOLL, &sa_pol_handler);
}
static int
sa_pol_block(void)
{
	return sa_sig_register(SIGPOLL, NULL);
}

/*
 * Both the sc(4) module and sad(4) driver issue an M_PCSIG message with
 * SIGPOLL to the stream head whenever the STREAMS configuration or autopush
 * configuration changes, indicating to the agent which has the sc(4) or
 * sad(4) Stream open that it is necessary to reread information from the
 * kernel.  This fact is merely recorded, as this information is not read each
 * time that a configuration change occurs, but only after a request from some
 * portion of that information occurs. This condition is also set when the
 * sc(4) and sad(4) Streams are first opened. The SIGPOLL will also deliver in
 * siginfo the file descriptor issuing the signal, so we could distiguish
 * between sc(4) and sad(4) signals, but since one can be pushed over the
 * other, there is little point in distinguishing.
 *
 * sc(4) or sad(4) also should be modified to provide the general streams
 * statistics supported here; even though they are available through the /proc
 * filesystem on Linux Fast-STREAMS.
 */
static int
sa_pol_action(void)
{
	sa_pol_signal = 0;
	snmp_log(MY_FACILITY(LOG_INFO), "%s: Caught SIGPOLL, will re-read data structures", sa_program);
	sa_changed = 1;
	return (0);
}

static RETSIGTYPE
sa_hup_handler(int signum)
{
	sa_hup_signal = 1;
	return (RETSIGTYPE) (0);
}
static int
sa_hup_catch(void)
{
	if (sa_agentx)
		return sa_sig_register(SIGHUP, &sa_hup_handler);
	return (-1);
}
static int
sa_hup_block(void)
{
	return sa_sig_register(SIGHUP, NULL);
}
static int
sa_hup_action(void)
{
	/* There are several times that we might be sent a SIGHUP.  We might be sent a SIGHUP by logrotate asking us to close and reopen our log files. */
	sa_hup_signal = 0;
	snmp_log(MY_FACILITY(LOG_WARNING), "Caught SIGHUP, reopening files.");
	if (sa_output > 1)
		snmp_log(MY_FACILITY(LOG_NOTICE), "Reopening output file %s", sa_logfile);
	if (sa_logfillog != 0) {
		fflush(stdlog);
		fclose(stdlog);
		snmp_disable_filelog();
		if ((stdlog = freopen(sa_logfile, sa_appendlog ? "a" : "w", stdlog)) == NULL) {
			/* I hope we have another log sink. */
			snmp_log(MY_FACILITY(LOG_ERR), "%s", strerror(errno));
			snmp_log(MY_FACILITY(LOG_ERR), "Could not reopen log file %s", sa_logfile);
		}
		snmp_enable_filelog(sa_logfile, sa_appendlog);
	}
	return (0);
}

static RETSIGTYPE
sa_int_handler(int signum)
{
	sa_int_signal = 1;
	return (RETSIGTYPE) (0);
}
static int
sa_int_catch(void)
{
	return sa_sig_register(SIGINT, &sa_int_handler);
}
static int
sa_int_block(void)
{
	return sa_sig_register(SIGINT, NULL);
}
static void sa_exit(int retval);
static int
sa_int_action(void)
{
	sa_int_signal = 0;
	snmp_log(MY_FACILITY(LOG_WARNING), "%s: Caught SIGINT, shutting down", sa_program);
	sa_exit(0);
	return (0);		/* should be no return */
}

static RETSIGTYPE
sa_trm_handler(int signum)
{
	sa_trm_signal = 1;
	return (RETSIGTYPE) (0);
}
static int
sa_trm_catch(void)
{
	return sa_sig_register(SIGTERM, &sa_trm_handler);
}
static int
sa_trm_block(void)
{
	return sa_sig_register(SIGTERM, NULL);
}
static void sa_exit(int retval);
static int
sa_trm_action(void)
{
	sa_trm_signal = 0;
	snmp_log(MY_FACILITY(LOG_WARNING), "%s: Caught SIGTERM, shutting down", sa_program);
	sa_exit(0);
	return (0);		/* should be no return */
}
static void
sa_sig_catch(void)
{
	sa_alm_catch();
	sa_pol_catch();
	sa_hup_catch();
	sa_int_catch();
	sa_trm_catch();
}
static void
sa_sig_block(void)
{
	sa_alm_block();
	sa_pol_block();
	sa_hup_block();
	sa_int_block();
	sa_trm_block();
}

int
sa_start_timer(long duration)
{
	if (sa_alarms) {
		struct itimerval setting = {
			{0, 0},
			{duration / 1000, (duration % 1000) * 1000}
		};
		if (sa_alm_catch())
			return (-1);
		if (setitimer(ITIMER_REAL, &setting, NULL))
			return (-1);
		sa_alm_signal = 0;
		return (0);
	} else {
#if defined NETSNMP_DS_APPLICATION_ID
		struct timeval setting = {
			duration / 1000, (duration % 1000) * 1000
		};
		sa_alm_handle = snmp_alarm_register_hr(setting, 0, sa_snmp_alm_handler, NULL);
#else
		sa_alm_handle = snmp_alarm_register((duration + 999) / 1000, 0, sa_snmp_alm_handler, NULL);
#endif
		return (sa_alm_handle ? 0 : -1);
	}
}
static void
sa_exit(int retval)
{
	if (retval)
		snmp_log(MY_FACILITY(LOG_ERR), "%s: Exiting %d", sa_program, retval);
	else
		snmp_log(MY_FACILITY(LOG_NOTICE), "%s: Exiting %d", sa_program, retval);
	fflush(stdout);
	fflush(stderr);
	sa_sig_block();
	closelog();
	exit(retval);
}
static void
sa_init_logging(int argc, char *argv[])
{
	static char progname[256];

	/* The purpose of this function is to bring logging up before forking (and while still in the foreground) so that we can use the snmp_log() function before and during forking if necessary.
	   Note that the default configuration for snmp_log() is to send all logs to standard error. */
	strncpy(progname, basename(argv[0]), sizeof(progname));
	snmp_disable_log();
	if (sa_logfillog) {
		snmp_enable_filelog(sa_logfile, sa_appendlog);
	}
	if (sa_logstderr | sa_logstdout) {
#if defined LOG_PERROR
		/* Note that when we have Linux LOG_PERROR, and logs go both to syslog and stderr, it is better to use the LOG_PERROR than to use snmp_log()'s print to stderr, as the former is better 
		   formated. */
		if (!sa_logsyslog)
			snmp_enable_stderrlog();
#else				/* defined LOG_PERROR */
		snmp_enable_stderrlog();
#endif				/* defined LOG_PERROR */
	}
	if (sa_logsyslog) {
#if !defined HAVE_SNMP_ENABLE_SYSLOG_IDENT
		snmp_enable_syslog();
#else				/* !defined HAVE_SNMP_ENABLE_SYSLOG_IDENT */
		snmp_enable_syslog_ident("ds1EXT", LOG_DAEMON);
#endif				/* !defined HAVE_SNMP_ENABLE_SYSLOG_IDENT */
		/* Note that the way that snmp sets up the logger is not really the way we want it, so close the log and reopen it the way we want. */
		closelog();
#if defined LOG_PERROR
		openlog("ds1EXT", LOG_PID | LOG_CONS | LOG_NDELAY | (sa_logstderr ? LOG_PERROR : 0), MY_FACILITY(0));
#else				/* defined LOG_PERROR */
		openlog("ds1EXT", LOG_PID | LOG_CONS | LOG_NDELAY, MY_FACILITY(0));
#endif				/* defined LOG_PERROR */
	}
	if (sa_logcallog) {
		snmp_enable_calllog();
	}
}
static void
sa_enter(int argc, char *argv[])
{
	if (sa_nomead) {
		pid_t pid;

		if ((pid = fork()) < 0) {
			perror(argv[0]);
			exit(2);
		} else if (pid != 0) {
			/* parent exits */
			exit(0);
		}
		setsid();	/* become a session leader */
		/* fork once more for SVR4 */
		if ((pid = fork()) < 0) {
			perror(argv[0]);
			exit(2);
		} else if (pid != 0) {
			/* parent responsible for writing pid file */
			if (sa_nomead || sa_pidfile[0] != '\0') {
				FILE *pidf;

				/* initialize default filename */
				if (sa_pidfile[0] == '\0')
					snprintf(sa_pidfile, sizeof(sa_pidfile), "/var/run/%s.pid", sa_program);
				if (sa_output > 1) {
					snmp_log(MY_FACILITY(LOG_NOTICE), "%s: Writing daemon pid to file %s", sa_program, sa_pidfile);
				}
				if ((pidf = fopen(sa_pidfile, "w+"))) {
					fprintf(pidf, "%d", (int) pid);
					fflush(pidf);
					fclose(pidf);
				} else {
					snmp_log(MY_FACILITY(LOG_ERR), "%s: %m", sa_program);
					snmp_log(MY_FACILITY(LOG_ERR), "%s: Could not write pid to file %s", sa_program, sa_pidfile);
					sa_exit(2);
					/* no return */
				}
			}
			/* parent exits */
			exit(0);
		}
		/* child continues */
		/* release current directory */
		if (chdir("/") < 0) {
			perror(argv[0]);
			exit(2);
		}
		umask(0);	/* clear file creation mask */
		/* rearrange file streams */
		fclose(stdin);
	}
	/* continue as foreground or background */
	sa_init_logging(argc, argv);
	sa_sig_catch();
	snmp_log(MY_FACILITY(LOG_NOTICE), "%s: Startup complete.", sa_program);
}
static void
sa_mloop(int argc, char *argv[])
{
	if (sa_agentx) {
		if (sa_debug)
			snmp_log(MY_FACILITY(LOG_DEBUG), "%s: running as AgentX client\n", argv[0]);
		/* run as an AgentX client */
		ds_set_boolean(DS_APPLICATION_ID, DS_AGENT_ROLE, 1);
	} else {
		if (sa_debug)
			snmp_log(MY_FACILITY(LOG_DEBUG), "%s: running as SNMP master agent\n", argv[0]);
		/* run as SNMP master */
		ds_set_boolean(DS_APPLICATION_ID, DS_AGENT_ROLE, 0);
	}
	if (sa_alarms) {
		if (sa_debug)
			snmp_log(MY_FACILITY(LOG_DEBUG), "%s: using application alarms\n", argv[0]);
		/* use application alarms */
		ds_set_boolean(DS_LIBRARY_ID, DS_LIB_ALARM_DONT_USE_SIG, 1);
	}
	/* initialize agent */
	init_agent("ds1EXT");
	/* initialize MIB */
	init_ds1EXT();
	/* initialize SNMP */
	init_snmp("ds1EXT");
	if (!sa_agentx) {
		if (sa_debug)
			snmp_log(MY_FACILITY(LOG_DEBUG), "%s: running as SNMP master\n", argv[0]);
#if !defined NETSNMP_DS_APPLICATION_ID
		init_master_agent(710, NULL, NULL);
#else
		init_master_agent();
#endif
	}
	for (;;) {
		int retval;

		/* to use select or poll you need to use the snmp_select_info() to obtain the fd of the agentx socket and add it to the fdset. */
		/* note that SIGALRM is used by snmp: use the snmp_alarm() api instead */
#if 0
		if (snmp_select() == 0) {
			if (sa_alarms == 0)
				run_alarms();
		}
#endif
		retval = agent_check_and_process(1);	/* 0 == don't block */
		if (retval == 0) {
			/* alarm occurred, alarm conditions checked */
		} else if (retval == -1) {
			/* error (or signal) ocurred */
			if (sa_alm_signal) {
				sa_alm_action();
			}
			if (sa_pol_signal) {
				sa_pol_action();
			}
			if (sa_hup_signal) {
				sa_hup_action();
			}
			if (sa_int_signal) {
				if (sa_debug)
					snmp_log(MY_FACILITY(LOG_DEBUG), "%s: shutting down\n", argv[0]);
				snmp_shutdown("ds1EXT");
				sa_int_action();	/* no return */
			}
			if (sa_trm_signal) {
				if (sa_debug)
					snmp_log(MY_FACILITY(LOG_DEBUG), "%s: shutting down\n", argv[0]);
				snmp_shutdown("ds1EXT");
				sa_trm_action();	/* no return */
			}
		} else if (retval > 0) {
			/* processed packets */
			if (sa_fclose) {
				/* close files after each request */
				if (sa_fd != 0) {
					int fd = sa_fd;

					sa_fd = 0;
					close(fd);
				}
			}
			sa_stats_refresh = 1;
			sa_request++;
		}
	}
	if (sa_debug)
		snmp_log(MY_FACILITY(LOG_DEBUG), "%s: shutting down\n", argv[0]);
	snmp_shutdown("ds1EXT");
}

int
main(int argc, char *argv[])
{
	for (;;) {
		int c, val, fd;
		char *cptr;
		struct passwd *pw;
		struct group *gr;
		struct stat st;

#if defined _GNU_SOURCE
		int option_index = 0;
                /* *INDENT-OFF* */
                static struct option long_options[] = {
                        {"log-addresses",	no_argument,		NULL, 'a'},
                        {"append",		no_argument,		NULL, 'A'},
                        {"config-file",		required_argument,	NULL, 'c'},
                        {"no-configs",		no_argument,		NULL, 'C'},
                        {"dump",		no_argument,		NULL, 'd'},
                        {"debug",		optional_argument,	NULL, 'D'},
                        {"debug-tokens",	optional_argument,	NULL, 'D'},
                        {"dont-fork",		no_argument,		NULL, 'f'},
                        {"gid",			required_argument,	NULL, 'g'},
                        {"groupid",		required_argument,	NULL, 'g'},
                        {"help",		no_argument,		NULL, 'h'},
                        {"?",			no_argument,		NULL, 'h'},
                        {"help-directives",	no_argument,		NULL, 'H'},
                        {"initialize",		required_argument,	NULL, 'I'},
                        {"init-modules",	required_argument,	NULL, 'I'},
                        {"keep-open",		no_argument,		NULL, 'k'},
                        {"log-file",		optional_argument,	NULL, 'l'},
                        {"logfile",		optional_argument,	NULL, 'l'},
                        {"Lf",			optional_argument,	NULL, 'l'},
                        {"LF",			required_argument,	NULL, 'l'},
                        {"log-stderr",		no_argument,		NULL, 'L'},
                        {"Le",			no_argument,		NULL, 'L'},
                        {"LE",			required_argument,	NULL, 'L'},
                        {"mibs",		required_argument,	NULL, 'm'},
                        {"master",		no_argument,		NULL, 'M'},
                        {"mibdirs",		required_argument,	NULL, 'M'},
                        {"nodaemon",		no_argument,		NULL, 'n'},
                        {"name",		required_argument,	NULL, 'n'},
                        {"dry-run",		no_argument,		NULL, 'N'},
                        {"log-stdout",		no_argument,		NULL, 'o'},
                        {"Lo",			no_argument,		NULL, 'o'},
                        {"LO",			required_argument,	NULL, 'o'},
                        {"port",		required_argument,	NULL, 'p'},
                        {"pidfile",		required_argument,	NULL, 'P'},
                        {"quiet",		no_argument,		NULL, 'q'},
                        {"quick",		no_argument,		NULL, 'q'},
                        {"noroot",		no_argument,		NULL, 'r'},
                        {"log-syslog",		no_argument,		NULL, 's'},
                        {"Ls",			no_argument,		NULL, 's'},
                        {"LS",			required_argument,	NULL, 's'},
                        {"syslog",		no_argument,		NULL, 's'},
                        {"sysctl-file",		required_argument,	NULL, 'S'},
                        {"agent-alarms",	no_argument,		NULL, 't'},
                        {"transport",		optional_argument,	NULL, 'T'},
                        {"uid",			required_argument,	NULL, 'u'},
                        {"userid",		required_argument,	NULL, 'u'},
                        {"dont-remove-pidfile",	no_argument,		NULL, 'U'},
                        {"leave-pidfile",	no_argument,		NULL, 'U'},
                        {"version",		no_argument,		NULL, 'v'},
                        {"verbose",		optional_argument,	NULL, 'V'},
                        {"agentx-socket",	required_argument,	NULL, 'x'},
                        {"agentx",		no_argument,		NULL, 'X'},
                        {"copying",		no_argument,		NULL, 'y'},
#if 0
                        {"directory",		required_argument,	NULL, 'd'},
                        {"basename",		required_argument,	NULL, 'b'},
                        {"outfile",		required_argument,	NULL, 'o'},
                        {"errfile",		required_argument,	NULL, 'e'},
#endif
                        { 0, }
                };
                /* *INDENT-ON* */

		c = getopt_long_only(argc, argv, ":aAc:CdD::fg:hHI:kl::L::m:M::n::o::p:P:qrs::S:tT::u:UvV::x:Xy", long_options, &option_index);
#else				/* defined _GNU_SOURCE */
		c = getopt(argc, argv, ":aAc:CdD::fg:hHI:kl::L::m:M::n::o::p:P:qrs::S:tT::u:UvV::x:Xy");
#endif				/* defined _GNU_SOURCE */
		if (c == -1) {
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: done options processing\n", argv[0]);
			break;
		}
		switch (c) {
		case 0:
			goto bad_usage;
		case 'a':	/* -a, --log-addresses */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: logging addresses\n", argv[0]);
			sa_logaddr++;
			break;
		case 'A':	/* -A, --append */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: will not truncate logfile\n", argv[0]);
#if defined NETSNMP_DS_LIB_APPEND_LOGFILES
			ds_set_boolean(DS_LIBRARY_ID, NETSNMP_DS_LIB_APPEND_LOGFILES, 1);
#endif				/* defined NETSNMP_DS_LIB_APPEND_LOGFILES */
			sa_appendlog = 1;
			break;
		case 'c':	/* -c, --config-file CONFIGFILE */
			if (optarg == NULL)
				goto bad_option;
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: using configuration file %s\n", argv[0], optarg);
			ds_set_string(DS_LIBRARY_ID, DS_LIB_OPTIONALCONFIG, optarg);
			break;
		case 'C':	/* -C, --no-configs */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: not reading default config files\n", argv[0]);
			ds_set_boolean(DS_LIBRARY_ID, DS_LIB_DONT_READ_CONFIGS, 1);
			break;
		case 'd':	/* -d, --dump */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: setting packet dump\n", argv[0]);
			sa_dump = 1;
			// snmp_set_dump_packet(sa_dump);
			ds_set_boolean(DS_LIBRARY_ID, DS_LIB_DUMP_PACKET, sa_dump);
			ds_set_boolean(DS_APPLICATION_ID, DS_AGENT_VERBOSE, sa_dump);
			break;
		case 'D':	/* -D, --debug [LEVEL], --debug-tokens [TOKENS] */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: increasing debug verbosity\n", argv[0]);
			if (optarg == NULL) {
				/* no option: must be -D, --debug */
				sa_debug++;
				if (sa_debug)
					snmp_log(MY_FACILITY(LOG_DEBUG), "%s: debug level is now %d\n", argv[0], sa_debug);
				if (sa_debug)
					snmp_log(MY_FACILITY(LOG_DEBUG), "%s: debugging all tokens\n", argv[0]);
				if (sa_debug)
					debug_register_tokens("ALL");
			} else {
				cptr = optarg;
				if ((val = strtol(optarg, &cptr, 0)) < 0)
					goto bad_option;
				if (*cptr == '\0') {
					/* it is just a number, must be -D, --debug [LEVEL] */
					sa_debug = val;
					if (sa_debug)
						snmp_log(MY_FACILITY(LOG_DEBUG), "%s: debug level is now %d\n", argv[0], sa_debug);
				} else {
					/* not a number, must be -D, --debug-tokens TOKENS */
					if (sa_debug)
						snmp_log(MY_FACILITY(LOG_DEBUG), "%s: debugging tokens %s\n", argv[0], optarg);
					debug_register_tokens(optarg);
				}
			}
			break;
		case 'f':	/* -f, --dont-fork */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: suppressing daemon mode\n", argv[0]);
			sa_nomead = 0;
			break;
		case 'u':	/* -u, --uid, --userid UID */
			cptr = optarg;
			if ((val = strtol(optarg, &cptr, 0)) < 0)
				goto bad_option;
			/* UID can be name or number */
			if ((pw = (*cptr == '\0') ? getpwuid((uid_t) val) : getpwnam(optarg)) == NULL)
				goto bad_option;
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: will run as uid %s(%d)\n", argv[0], pw->pw_name, pw->pw_uid);
			ds_set_int(DS_APPLICATION_ID, DS_AGENT_USERID, pw->pw_uid);
			break;
		case 'g':	/* -g, --gid, --groupdid GID */
			cptr = optarg;
			if ((val = strtol(optarg, &cptr, 0)) < 0)
				goto bad_option;
			/* GID can be name or number */
			if ((gr = (*cptr == '\0') ? getgrgid((gid_t) val) : getgrnam(optarg)) == NULL)
				goto bad_option;
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: will run as gid %s(%d)\n", argv[0], gr->gr_name, gr->gr_gid);
			ds_set_int(DS_APPLICATION_ID, DS_AGENT_GROUPID, gr->gr_gid);
			break;
		case 'h':	/* -h, --help, -?, --? */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: printing help message\n", argv[0]);
			sa_help(argc, argv);
			exit(0);
		case 'H':	/* -H, --help-directives */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: printing config directives\n", argv[0]);
			sa_help_directives(argc, argv);
			exit(0);
		case 'I':	/* -I, --init-modules, --initialize MODULE[{,| |:}MODULE]* */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: will initialize modules: %s\n", argv[0], optarg);
			add_to_init_list(optarg);
			break;
		case 'k':	/* -k, --keep-open */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: keeping files open\n", argv[0]);
			sa_fclose = 0;
			break;
		case 'l':	/* -l, --log-file, --logfile, -Lf, -LF p1[-p2] [LOGFILE] */
			if (optarg != NULL)
				strncpy(sa_logfile, optarg, sizeof(sa_logfile));
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: will log to file %s\n", argv[0], sa_logfile);
			sa_logfillog = 1;
			break;
		case 'L':	/* -L, --log-stderr, -Le, -LE p1[-p2] */
			/* Note that the recent NET-SNMP version of this option is far more complicated: -Le is the same as the old version of the option; -Lf LOGFILE is like the -l option; -Ls is
			   like the -s option; -Lo logs messages to standard output; -LX p1[-p2] [LOGFILE], where X = E, F, S or O, logs priority p1 and above to X, or p1 thru p2 to X. */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: logging to standard error\n", argv[0]);
			sa_logstderr = 1;
			break;
		case 'm':	/* -m, --mibs MIBS */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: using MIBS %s\n", argv[0], optarg);
			break;
		case 'M':	/* -M, --master or -M, --mibdirs MIBDIRS */
			if (optarg) {
				/* -M, --mibdirs MIBDIRS */
				if (sa_debug)
					snmp_log(MY_FACILITY(LOG_DEBUG), "%s: using MIBDIRS %s\n", argv[0], optarg);
			} else {
				/* -M, --master */
				if (sa_debug)
					snmp_log(MY_FACILITY(LOG_DEBUG), "%s: setting SNMP master\n", argv[0]);
				sa_agentx = 0;
				ds_set_boolean(DS_APPLICATION_ID, DS_AGENT_ROLE, 0);
			}
			break;
		case 'n':	/* -n, --nodaemon or -n, --name NAME */
			if (optarg) {
				/* -n, --name NAME */
				if (sa_debug)
					snmp_log(MY_FACILITY(LOG_DEBUG), "%s: using name %s\n", argv[0], optarg);
				ds_set_string(DS_APPLICATION_ID, DS_AGENT_PROGNAME, optarg);
			} else {
				/* -n, --nodaemon */
				if (sa_debug)
					snmp_log(MY_FACILITY(LOG_DEBUG), "%s: suppressing deamon mode\n", argv[0]);
				sa_nomead = 0;
				ds_set_string(DS_APPLICATION_ID, DS_AGENT_PROGNAME, basename(argv[0]));
			}
			break;
		case 'N':	/* -N, --dry-run */
#if defined NETSNMP_DS_AGENT_QUIT_IMMEDIATELY
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: setting for dry-runs startup\n", argv[0]);
			ds_set_boolean(DS_APPLICATION_ID, NETSNMP_DS_AGENT_QUIT_IMMEDIATELY, 1);
			break;
#else				/* defined NETSNMP_DS_AGENT_QUIT_IMMEDIATELY */
			snmp_log(MY_FACILITY(LOG_DEBUG), "%s: -N option not supported\n", argv[0]);
			goto bad_option;
#endif				/* defined NETSNMP_DS_AGENT_QUIT_IMMEDIATELY */
		case 'o':	/* -o, --log-stdout, -Lo, -LO p1[-p2] */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: logging to stdout\n", argv[0]);
			sa_logstdout = 1;
			break;
		case 'p':	/* -p, --port PORTNUM or -p, --pidfile PIDFILE */
			cptr = optarg;
			if ((val = strtol(optarg, &cptr, 0)) < 0 || val > 16383)
				goto bad_option;
			if (*cptr == '\0') {
				char buf[4096];

				/* -p, --port PORTNUM */
				if ((cptr = ds_get_string(DS_APPLICATION_ID, DS_AGENT_PORTS)))
					snprintf(buf, sizeof(buf), "%s,%s", cptr, optarg);
				else
					strncpy(buf, optarg, sizeof(buf));
				ds_set_string(DS_APPLICATION_ID, DS_AGENT_PORTS, buf);
				break;
			}
			/* fall through */
		case 'P':	/* -p, -P, --pidfile PIDFILE */
			if (optarg) {
				/* either it exists */
				if (stat(optarg, &st) == -1) {
					/* or we can create it */
					if ((fd = open(optarg, O_CREAT, 0600)) == -1) {
						perror(argv[0]);
						goto bad_option;
					}
					close(fd);
				}
				if (sa_debug)
					snmp_log(MY_FACILITY(LOG_DEBUG), "%s: setting pid file to %s\n", argv[0], optarg);
				strncpy(sa_pidfile, optarg, sizeof(sa_pidfile));
			}
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: using pidfile %s\n", argv[0], sa_pidfile);
			break;
		case 'q':	/* -q, --quiet, --quick */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: suppressing normal output\n", argv[0]);
			sa_debug = 0;
			sa_output = 0;
			ds_set_boolean(DS_APPLICATION_ID, DS_AGENT_VERBOSE, 0);
			// snmp_set_quick_print();
			ds_set_boolean(DS_LIBRARY_ID, DS_LIB_QUICK_PRINT, 1);
			break;
		case 'r':	/* -r, --noroot */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: setting for non-root access\n", argv[0]);
			ds_set_boolean(DS_APPLICATION_ID, DS_AGENT_NO_ROOT_ACCESS, 1);
			break;
		case 's':	/* -s, --log-syslog, -Ls, -LS p1[-p2] */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: logging to system logs\n", argv[0]);
			sa_logsyslog = 1;
			break;
		case 'S':	/* -S, -sysctl-file FILENAME */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: using %s for backing\n", argv[0], optarg);
			strncpy(sa_sysctlf, optarg, sizeof(sa_sysctlf));
			break;
		case 't':	/* -t, --agent-alarms */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: setting agent alarms\n", argv[0]);
			sa_alarms = 0;
			ds_set_boolean(DS_LIBRARY_ID, DS_LIB_ALARM_DONT_USE_SIG, 1);
			break;
		case 'T':	/* -T, --transport [TRANSPORT] */
			if (optarg == NULL)
				goto udp_transport;
			if (!strcasecmp("TCP", optarg)) {
				if (sa_debug)
					snmp_log(MY_FACILITY(LOG_DEBUG), "%s: setting default transport to TCP\n", argv[0]);
				val = ds_get_int(DS_APPLICATION_ID, DS_AGENT_FLAGS);
				val |= SNMP_FLAGS_STREAM_SOCKET;
				ds_set_int(DS_APPLICATION_ID, DS_AGENT_FLAGS, val);
			} else if (!strcasecmp("UDP", optarg)) {
			      udp_transport:
				if (sa_debug)
					snmp_log(MY_FACILITY(LOG_DEBUG), "%s: setting default transport to UDP\n", argv[0]);
				val = ds_get_int(DS_APPLICATION_ID, DS_AGENT_FLAGS);
				val &= ~SNMP_FLAGS_STREAM_SOCKET;
				ds_set_int(DS_APPLICATION_ID, DS_AGENT_FLAGS, val);
			} else
				goto bad_option;
			break;
		case 'U':
#if defined NETSNMP_DS_AGENT_LEAVE_PIDFILE
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: will leave pidfile after shutdown\n", argv[0]);
			ds_set_boolean(DS_APPLICATION_ID, NETSNMP_DS_AGENT_LEAVE_PIDFILE, 1);
#else
			snmp_log(MY_FACILITY(LOG_DEBUG), "%s: -U option not supported\n");
			goto bad_option;
#endif				/* defined NETSNMP_DS_AGENT_LEAVE_PIDFILE */
			break;
		case 'v':	/* -v, --version */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: printing version message\n", argv[0]);
			sa_version(argc, argv);
			exit(0);
		case 'V':	/* -V, --verbose [LEVEL] */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: increasing output verbosity\n", argv[0]);
			if (optarg == NULL) {
				sa_output++;
			} else {
				if ((val = strtol(optarg, NULL, 0)) < 0)
					goto bad_option;
				sa_output = val;
			}
			if (sa_output > 1)
				ds_set_boolean(DS_APPLICATION_ID, DS_AGENT_VERBOSE, 1);
			else
				ds_set_boolean(DS_APPLICATION_ID, DS_AGENT_VERBOSE, 0);
			break;
		case 'x':	/* -x, --agentx-socket SOCKET */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: setting AgentX socket to %s\n", argv[0], optarg);
			ds_set_string(DS_APPLICATION_ID, DS_AGENT_X_SOCKET, optarg);
			// ds_set_boolean(DS_APPLICATION_ID, DS_AGENT_AGENTX_MASTER, 1);
			break;
		case 'X':	/* -X, --agentx */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: setting AgentX sub-agent\n", argv[0]);
			sa_agentx = 1;
			ds_set_boolean(DS_APPLICATION_ID, DS_AGENT_ROLE, 1);
			break;
		case 'y':	/* -y, --copying */
			if (sa_debug)
				snmp_log(MY_FACILITY(LOG_DEBUG), "%s: printing copying message\n", argv[0]);
			sa_copying(argc, argv);
			exit(0);
		case '?':
		case ':':
		default:
		      bad_option:
			optind--;
			goto bad_nonopt;
		      bad_nonopt:
			if (sa_output || sa_debug) {
				if (optind < argc) {
					fprintf(stderr, "%s: syntax error near '", argv[0]);
					while (optind < argc)
						fprintf(stderr, "%s ", argv[optind++]);
					fprintf(stderr, "'\n");
				} else {
					fprintf(stderr, "%s: missing option or argument", argv[0]);
					fprintf(stderr, "\n");
				}
				fflush(stderr);
			      bad_usage:
				sa_usage(argc, argv);
			}
			exit(2);
		}
	}
	if (optind < argc) {
		if (sa_debug)
			snmp_log(MY_FACILITY(LOG_DEBUG), "%s: excess non-option arguments\n", argv[0]);
		goto bad_nonopt;
	}
	sa_enter(argc, argv);	/* daemonize if necessary */
	sa_mloop(argc, argv);	/* execute main loop */
	exit(0);
}
#endif				/* !defined MODULE */
#endif				/* defined MASTER */
