/*****************************************************************************

 @(#) src/snmp/sigtranMIB.c

 -----------------------------------------------------------------------------

 Copyright (c) 2008-2015  Monavacon Limited <http://www.monavacon.com/>
 Copyright (c) 2001-2008  OpenSS7 Corporation <http://www.openss7.com/>
 Copyright (c) 1997-2001  Brian F. G. Bidulock <bidulock@openss7.org>

 All Rights Reserved.

 This program is free software: you can redistribute it and/or modify it under
 the terms of the GNU Affero General Public License as published by the Free
 Software Foundation, version 3 of the license.

 This program is distributed in the hope that it will be useful, but WITHOUT
 ANY WARRANTY; without even the implied warranty of MERCHANTABILITY or FITNESS
 FOR A PARTICULAR PURPOSE.  See the GNU Affero General Public License for more
 details.

 You should have received a copy of the GNU Affero General Public License
 along with this program.  If not, see <http://www.gnu.org/licenses/>, or
 write to the Free Software Foundation, Inc., 675 Mass Ave, Cambridge, MA
 02139, USA.

 -----------------------------------------------------------------------------

 U.S. GOVERNMENT RESTRICTED RIGHTS.  If you are licensing this Software on
 behalf of the U.S. Government ("Government"), the following provisions apply
 to you.  If the Software is supplied by the Department of Defense ("DoD"), it
 is classified as "Commercial Computer Software" under paragraph 252.227-7014
 of the DoD Supplement to the Federal Acquisition Regulations ("DFARS") (or any
 successor regulations) and the Government is acquiring only the license rights
 granted herein (the license rights customarily provided to non-Government
 users).  If the Software is supplied to any unit or agency of the Government
 other than DoD, it is classified as "Restricted Computer Software" and the
 Government's rights in the Software are defined in paragraph 52.227-19 of the
 Federal Acquisition Regulations ("FAR") (or any successor regulations) or, in
 the cases of NASA, in paragraph 18.52.227-86 of the NASA Supplement to the FAR
 (or any successor regulations).

 -----------------------------------------------------------------------------

 Commercial licensing and support of this software is available from OpenSS7
 Corporation at a fee.  See http://www.openss7.com/

 *****************************************************************************/

static char const ident[] = "src/snmp/sigtranMIB.c (" PACKAGE_ENVR ") " PACKAGE_DATE;

/* This file was generated by mib2c and is intended for use as
   a mib module for the ucd-snmp snmpd agent. */
#include <ucd-snmp/ucd-snmp-config.h>
#include <ucd-snmp/ucd-snmp-includes.h>
#include <ucd-snmp/ucd-snmp-agent-includes.h>
#include <ucd-snmp/agent_trap.h>
#include <ucd-snmp/callback.h>
#include <ucd-snmp/snmp-tc.h>
#include <ucd-snmp/default_store.h>
#include <ucd-snmp/snmp_alarm.h>
/* The following header files are mangled in most recent net-snmp releases so
 * the versions from UCD-SNMP 4.2.5 are included here.  */
#if defined HAVE_LIBNETSNMP
#else				/* defined HAVE_LIBNETSNMP */
#endif				/* defined HAVE_LIBNETSNMP */
/* These are messed up on both. */
#include "ds_agent.h"
#ifdef HAVE_UCD_SNMP_UTIL_FUNCS_H
#include <ucd-snmp/util_funcs.h>
/* Many recent net-snmp UCD compatible headers do not declare header_generic. */
int header_generic(struct variable *, oid *, size_t *, int, size_t *, WriteMethod **);
#else				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#include "util_funcs.h"
#endif				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#ifdef HAVE_UCD_SNMP_HEADER_COMPLEX_H
#include <ucd-snmp/header_complex.h>
#else				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
#include "header_complex.h"
#endif				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
/* This one is the other way around: it is just fine for net-snmp, but
 * ucd-snmp does not provide the header file at all.  */
#ifdef HAVE_UCD_SNMP_MIB_MODULES_H
#include <ucd-snmp/mib_modules.h>
#else				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#ifdef HAVE_NET_SNMP_AGENT_MIB_MODULES_H
#include <net-snmp/agent/mib_modules.h>
#else				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#include "mib_modules.h"
#endif				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#endif				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#ifdef HAVE_NETSNMP_MEMDUP
#include "ucd_memdup.h"
#endif
#include <stdint.h>
#include <signal.h>
#include <sys/stat.h>		/* for struct stat, fstat() */
#include <sys/types.h>
#include <unistd.h>
#include <stdlib.h>
#include <pwd.h>		/* for getpwuid() getpwnam() */
#include <grp.h>		/* for getgrgid() getgrnam() */
#include <libgen.h>		/* for basename() */
#include <fcntl.h>		/* for O_CREAT */
#include <sys/sysctl.h>		/* for sysctl */
#include <errno.h>
#include <string.h>
#ifdef _GNU_SOURCE
#include <getopt.h>
#endif
#include "sigtranMIB.h"
#define MASTER 1
#define MY_FACILITY(__pri)	(LOG_DAEMON|(__pri))
#if defined MODULE
#if defined MASTER
const char sa_program[] = "sigtranMIB";
int sa_fclose = 1;			/* default close files between requests */
int sa_changed = 1;			/* indication to reread MIB configuration */
int sa_stats_refresh = 1;		/* indications that statistics, the mib or its tables need to be refreshed */
int sa_request = 1;			/* request number for per-request actions */
int sa_dump = 0;			/* default packet dump */
int sa_debug = 0;			/* default no debug */
#endif				/* defined MASTER */
#endif				/* defined MODULE */
static int my_fd = -1;			/* file descriptor for this MIB's use */
static int my_readfd = -1;		/* file descriptor for autonomnous events */
volatile int sigtranMIB_refresh = 1;
volatile int sigtranSctpProfileTable_refresh = 1;

/*
 * sigtranMIB_variables_oid: object identifier for sigtranMIB
 * This is the top level oid that we want to register under.  This is essentially a prefix, with the
 * suffix appearing in the variable below.
 */
oid sigtranMIB_variables_oid[9] = { 1, 3, 6, 1, 4, 1, 29591, 1, 15 };
oid sigtranSctpProfileTable_variables_oid[14] = { 1, 3, 6, 1, 4, 1, 29591, 1, 15, 1, 2, 1, 2, 1 };

/*
 * Oids for use in notifications defined in this MIB.
 */

/*
 * Oids accessible only for notify defined in this MIB.
 */

/*
 * Other oids defined in this MIB.
 */
oid sigtranPackage_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 1, 15, 2, 1, 1 };
oid sigtranGeneralCompliance_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 1, 15, 2, 2, 1 };
static oid zeroDotZero_oid[2] = { 0, 0 };
static oid snmpTrapOID_oid[11] = { 1, 3, 6, 1, 6, 3, 1, 1, 4, 1, 0 };

/*
 * variable7 sigtranMIB_variables: tree for sigtranMIB
 * This variable defines function callbacks and type return information for the sigtranMIB mib section
 */
struct variable7 sigtranMIB_variables[] = {
	/* magic number, variable type, ro/rw, callback fn, L, oidsuffix */
#define   SIGTRANSCTPPROFILENEXTINDEX  1
	{(u_char) SIGTRANSCTPPROFILENEXTINDEX, ASN_UNSIGNED, RONLY, var_sigtranMIB, 4, {1, 2, 1, 1}},
#define   SIGTRANSCTPPROFILENAME  2
	{(u_char) SIGTRANSCTPPROFILENAME, ASN_OCTET_STR, RWRITE, var_sigtranSctpProfileTable, 6, {1, 2, 1, 2, 1, 2}},
#define   SIGTRANSCTPPROFILENODELAY  3
	{(u_char) SIGTRANSCTPPROFILENODELAY, ASN_INTEGER, RWRITE, var_sigtranSctpProfileTable, 6, {1, 2, 1, 2, 1, 3}},
#define   SIGTRANSCTPPAYLOADPROTOCOLID  4
	{(u_char) SIGTRANSCTPPAYLOADPROTOCOLID, ASN_UNSIGNED, RWRITE, var_sigtranSctpProfileTable, 6, {1, 2, 1, 2, 1, 4}},
#define   SIGTRANSCTPLIFETIME   5
	{(u_char) SIGTRANSCTPLIFETIME, ASN_INTEGER, RWRITE, var_sigtranSctpProfileTable, 6, {1, 2, 1, 2, 1, 5}},
#define   SIGTRANSCTPPROFILECSUMTYPE  6
	{(u_char) SIGTRANSCTPPROFILECSUMTYPE, ASN_INTEGER, RWRITE, var_sigtranSctpProfileTable, 6, {1, 2, 1, 2, 1, 6}},
#define   SIGTRANSCTPPROFILEMACTYPE  7
	{(u_char) SIGTRANSCTPPROFILEMACTYPE, ASN_INTEGER, RWRITE, var_sigtranSctpProfileTable, 6, {1, 2, 1, 2, 1, 7}},
#define   SIGTRANSCTPPROFILEVALIDCOOKIELIFE  8
	{(u_char) SIGTRANSCTPPROFILEVALIDCOOKIELIFE, ASN_INTEGER, RWRITE, var_sigtranSctpProfileTable, 6, {1, 2, 1, 2, 1, 8}},
#define   SIGTRANSCTPPROFILECOOKIEINC  9
	{(u_char) SIGTRANSCTPPROFILECOOKIEINC, ASN_INTEGER, RWRITE, var_sigtranSctpProfileTable, 6, {1, 2, 1, 2, 1, 9}},
#define   SIGTRANSCTPPROFILEMAXISTREAMS  10
	{(u_char) SIGTRANSCTPPROFILEMAXISTREAMS, ASN_UNSIGNED, RWRITE, var_sigtranSctpProfileTable, 6, {1, 2, 1, 2, 1, 10}},
#define   SIGTRANSCTPPROFILEREQOSTREAMS  11
	{(u_char) SIGTRANSCTPPROFILEREQOSTREAMS, ASN_UNSIGNED, RWRITE, var_sigtranSctpProfileTable, 6, {1, 2, 1, 2, 1, 11}},
#define   SIGTRANSCTPECN        12
	{(u_char) SIGTRANSCTPECN, ASN_INTEGER, RWRITE, var_sigtranSctpProfileTable, 6, {1, 2, 1, 2, 1, 12}},
#define   SIGTRANSCTPADAPTATIONLAYERINFO  13
	{(u_char) SIGTRANSCTPADAPTATIONLAYERINFO, ASN_UNSIGNED, RWRITE, var_sigtranSctpProfileTable, 6, {1, 2, 1, 2, 1, 13}},
#define   SIGTRANSCTPPROFILEMAXINITRETRIES  14
	{(u_char) SIGTRANSCTPPROFILEMAXINITRETRIES, ASN_UNSIGNED, RWRITE, var_sigtranSctpProfileTable, 6, {1, 2, 1, 2, 1, 14}},
#define   SIGTRANSCTPPROFILEMAXBURST  15
	{(u_char) SIGTRANSCTPPROFILEMAXBURST, ASN_UNSIGNED, RWRITE, var_sigtranSctpProfileTable, 6, {1, 2, 1, 2, 1, 15}},
#define   SIGTRANSCTPPROFILEASSOCMAXRETRANS  16
	{(u_char) SIGTRANSCTPPROFILEASSOCMAXRETRANS, ASN_UNSIGNED, RWRITE, var_sigtranSctpProfileTable, 6, {1, 2, 1, 2, 1, 16}},
#define   SIGTRANSCTPPROFILEMAXSACKDELAY  17
	{(u_char) SIGTRANSCTPPROFILEMAXSACKDELAY, ASN_INTEGER, RWRITE, var_sigtranSctpProfileTable, 6, {1, 2, 1, 2, 1, 17}},
#define   SIGTRANSCTPPROFILERTOMIN  18
	{(u_char) SIGTRANSCTPPROFILERTOMIN, ASN_INTEGER, RWRITE, var_sigtranSctpProfileTable, 6, {1, 2, 1, 2, 1, 18}},
#define   SIGTRANSCTPPROFILERTOINITIAL  19
	{(u_char) SIGTRANSCTPPROFILERTOINITIAL, ASN_INTEGER, RWRITE, var_sigtranSctpProfileTable, 6, {1, 2, 1, 2, 1, 19}},
#define   SIGTRANSCTPPROFILERTOMAX  20
	{(u_char) SIGTRANSCTPPROFILERTOMAX, ASN_INTEGER, RWRITE, var_sigtranSctpProfileTable, 6, {1, 2, 1, 2, 1, 20}},
#define   SIGTRANSCTPPROFILEPATHMAXRETRANS  21
	{(u_char) SIGTRANSCTPPROFILEPATHMAXRETRANS, ASN_UNSIGNED, RWRITE, var_sigtranSctpProfileTable, 6, {1, 2, 1, 2, 1, 21}},
#define   SIGTRANSCTPPROFILEHEARTBEATITVL  22
	{(u_char) SIGTRANSCTPPROFILEHEARTBEATITVL, ASN_INTEGER, RWRITE, var_sigtranSctpProfileTable, 6, {1, 2, 1, 2, 1, 22}},
#define   SIGTRANSCTPPROFILESTATUS  23
	{(u_char) SIGTRANSCTPPROFILESTATUS, ASN_INTEGER, RWRITE, var_sigtranSctpProfileTable, 6, {1, 2, 1, 2, 1, 23}},
};

/* (L = length of the oidsuffix) */
struct sigtranMIB_data *sigtranMIBStorage = NULL;

/* global storage of our data, saved in and configured by header_complex() */
struct header_complex_index *sigtranSctpProfileTableStorage = NULL;

void (*sigtranMIBold_signal_handler) (int) = NULL;	/* save old signal handler just in case */
void sigtranMIB_loop_handler(int);
void sigtranMIB_fd_handler(int, void *);

/**
 * @fn void init_sigtranMIB(void)
 * @brief sigtranMIB initialization routine.
 *
 * This is called when the agent starts up.  At a minimum, registration of the MIB variables
 * structure (sigtranMIB_variables) should take place here.  By default the function also
 * registers the configuration handler and configuration store callbacks.
 *
 * Additional registrations that may be considered here are calls to regsiter_readfd(),
 * register_writefd() and register_exceptfd() for hooking into the snmpd event loop, but only when
 * used as a loadable module.  By default this function establishes a single file descriptor to
 * read, or upon which to handle exceptions.  Note that the snmpd only supports a maximum of 32
 * extneral file descriptors, so these should be used sparingly.
 *
 * When running as a loadable module, it is also necessary to hook into the snmpd event loop so that
 * the current request number can be deteremined.  This is accomplished by using a trick of the
 * external_signal_scheduled and external_signal_handler mechanism which is called on each event
 * loop when external_signal_scheduled is non-zero.  This is used to increment the sa_request value
 * on each snmpd event loop interation so that calls to MIB tree functions can determine whether
 * they belong to a fresh request or not (primarily for cacheing and possibly to clean up non-polled
 * file descriptors).
 */
void
init_sigtranMIB(void)
{
	(void) my_fd;
	(void) zeroDotZero_oid;
	(void) snmpTrapOID_oid;
	DEBUGMSGTL(("sigtranMIB", "init_sigtranMIB: initializing...  "));
	/* register ourselves with the agent to handle our mib tree */
	REGISTER_MIB("sigtranMIB", sigtranMIB_variables, variable7, sigtranMIB_variables_oid);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_SHUTDOWN, term_sigtranMIB, NULL);
	/* register our config handler(s) to deal with registrations */
	snmpd_register_config_handler("sigtranMIB", parse_sigtranMIB, NULL, "HELP STRING");
	snmpd_register_config_handler("sigtranSctpProfileTable", parse_sigtranSctpProfileTable, NULL, "HELP STRING");

	/* we need to be called back later to store our data */
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_sigtranMIB, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_sigtranSctpProfileTable, NULL);

	/* place any other initialization junk you need here */
	if (my_readfd >= 0) {
		register_readfd(my_readfd, sigtranMIB_fd_handler, (void *) 0);
		register_exceptfd(my_readfd, sigtranMIB_fd_handler, (void *) 1);
	}
	sigtranMIBold_signal_handler = external_signal_handler[SIGCHLD];
	external_signal_handler[SIGCHLD] = &sigtranMIB_loop_handler;
	external_signal_scheduled[SIGCHLD] = 1;
	DEBUGMSGTL(("sigtranMIB", "done.\n"));
}

/**
 * @fn void deinit_sigtranMIB(void)
 * @brief deinitialization routine.
 *
 * This is called before the agent is unloaded.  At a minimum, deregistration of the MIB variables
 * structure (sigtranMIB_variables) should take place here.  By default, the function also
 * deregisters the the configuration file handlers for the MIB variables and table rows.
 *
 * Additional deregistrations that may be required here are calls to unregister_readfd(),
 * unregister_writefd() and unregsiter_exceptfd() for unhooking from the snmpd event loop, but only
 * when used as a loadable module.  By default if a read file descriptor exists, it is unregistered.
 */
void
deinit_sigtranMIB(void)
{
	DEBUGMSGTL(("sigtranMIB", "deinit_sigtranMIB: deinitializating...  "));
	external_signal_handler[SIGCHLD] = sigtranMIBold_signal_handler;
	if (my_readfd >= 0) {
		unregister_exceptfd(my_readfd);
		unregister_readfd(my_readfd);
		close(my_readfd);
		my_readfd = -1;
	}
	unregister_mib(sigtranMIB_variables_oid, sizeof(sigtranMIB_variables_oid) / sizeof(oid));
	snmpd_unregister_config_handler("sigtranMIB");
	snmpd_unregister_config_handler("sigtranSctpProfileTable");

	/* place any other de-initialization junk you need here */
	DEBUGMSGTL(("sigtranMIB", "done.\n"));
}

int
term_sigtranMIB(int majorID, int minorID, void *serverarg, void *clientarg)
{
	DEBUGMSGTL(("sigtranMIB", "term_sigtranMIB: terminating...  "));
	deinit_sigtranMIB();
	DEBUGMSGTL(("sigtranMIB", "done.\n"));
	return 0;
}

/**
 * @fn struct sigtranMIB_data *sigtranMIB_create(void)
 * @brief create a fresh data structure representing scalars in sigtranMIB.
 *
 * Creates a new sigtranMIB_data structure by allocating dynamic memory for the structure and
 * initializing the default values of scalars in sigtranMIB.
 */
struct sigtranMIB_data *
sigtranMIB_create(void)
{
	struct sigtranMIB_data *StorageNew = SNMP_MALLOC_STRUCT(sigtranMIB_data);

	DEBUGMSGTL(("sigtranMIB", "sigtranMIB_create: creating scalars...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default scalar values here into StorageNew */
		StorageNew->sigtranSctpProfileNextIndex = 0;

	}
      done:
	DEBUGMSGTL(("sigtranMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	sigtranMIB_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct sigtranMIB_data *sigtranMIB_duplicate(struct sigtranMIB_data *thedata)
 * @param thedata the mib structure to duplicate
 * @brief duplicate a mib structure for the mib
 *
 * Duplicates the specified mib structure @param thedata and returns a pointer to the newly
 * allocated mib structure on success, or NULL on failure.
 */
struct sigtranMIB_data *
sigtranMIB_duplicate(struct sigtranMIB_data *thedata)
{
	struct sigtranMIB_data *StorageNew = SNMP_MALLOC_STRUCT(sigtranMIB_data);

	DEBUGMSGTL(("sigtranMIB", "sigtranMIB_duplicate: duplicating mib... "));
	if (StorageNew != NULL) {
		StorageNew->sigtranSctpProfileNextIndex = thedata->sigtranSctpProfileNextIndex;
	}
      done:
	DEBUGMSGTL(("sigtranMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	sigtranMIB_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int sigtranMIB_destroy(struct sigtranMIB_data **thedata)
 * @param thedata pointer to the data structure in sigtranMIB.
 * @brief delete a scalars structure from sigtranMIB.
 *
 * Frees scalars that were previously removed from sigtranMIB.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
sigtranMIB_destroy(struct sigtranMIB_data **thedata)
{
	struct sigtranMIB_data *StorageDel;

	DEBUGMSGTL(("sigtranMIB", "sigtranMIB_destroy: deleting scalars...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("sigtranMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int sigtranMIB_add(struct sigtranMIB_data *thedata)
 * @param thedata the structure representing sigtranMIB scalars.
 * @brief adds node to the sigtranMIB scalar data set.
 *
 * Adds a scalar structure to the sigtranMIB data set.  Note that this function is necessary even
 * when the scalar values are not peristent.
 */
int
sigtranMIB_add(struct sigtranMIB_data *thedata)
{
	DEBUGMSGTL(("sigtranMIB", "sigtranMIB_add: adding data...  "));
	if (thedata)
		sigtranMIBStorage = thedata;
	DEBUGMSGTL(("sigtranMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_sigtranMIB(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for sigtranMIB entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case sigtranMIB).  This routine is invoked by
 * UCD-SNMP to read the values of scalars in the MIB from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the MIB.  If there are no configured entries
 * in the configuration MIB, this function will simply not be called.
 */
void
parse_sigtranMIB(const char *token, char *line)
{
	size_t tmpsize;
	struct sigtranMIB_data *StorageTmp = sigtranMIB_create();

	DEBUGMSGTL(("sigtranMIB", "parse_sigtranMIB: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual scalars that are not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->sigtranSctpProfileNextIndex, &tmpsize);
	sigtranMIB_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("sigtranMIB", "done.\n"));
}

/*
 * store_sigtranMIB(): stores .conf file entries needed to configure the mib.
 */
int
store_sigtranMIB(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct sigtranMIB_data *StorageTmp;

	DEBUGMSGTL(("sigtranMIB", "store_sigtranMIB: storing data...  "));
	refresh_sigtranMIB(1);
	if ((StorageTmp = sigtranMIBStorage) == NULL) {
		DEBUGMSGTL(("sigtranMIB", "error.\n"));
		return SNMPERR_GENERR;
	}
	(void) tmpsize;
	/* XXX: comment entire section if no scalars are persistent */
	{
		memset(line, 0, sizeof(line));
		strcat(line, "sigtranMIB ");
		cptr = line + strlen(line);
		(void) cptr;
		/* XXX: remove individual scalars that are not persistent */
		cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->sigtranSctpProfileNextIndex, &tmpsize);
		snmpd_store_config(line);
	}
	DEBUGMSGTL(("sigtranMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int check_sigtranMIB(struct sigtranMIB_data *StorageTmp, struct sigtranMIB_data *StorageOld)
 * @param StorageTmp the data as updated
 * @param StorageOld the data previous to update
 *
 * This function is used by mibs.  It is used to check, all scalars at a time, the varbinds
 * belonging to the mib.  This function is called for the first varbind in a mib at the beginning of
 * the ACTION phase.  The COMMIT phase does not ensue unless this check passes.  This function can
 * return SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before
 * the varbinds on the mib were applied; the values in StorageTmp are the new values.  The function
 * is permitted to change the values in StorageTmp to correct them; however, preferences should be
 * made for setting values that were not in the varbinds.
 */
int
check_sigtranMIB(struct sigtranMIB_data *StorageTmp, struct sigtranMIB_data *StorageOld)
{
	/* XXX: provide code to check the scalars for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_sigtranMIB(struct sigtranMIB_data *StorageTmp, struct sigtranMIB_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase)
 *
 * This function is used by mibs.  It is used to update, all scalars at a time, the varbinds
 * belonging to the mib.  This function is called for the first varbind in a mib at the beginning of
 * the COMMIT phase.  The start of the ACTION phase performs a consistency check on the mib before
 * allowing the request to proceed to the COMMIT phase.  The COMMIT phase then arrives here with
 * consistency already checked (see check_sigtranMIB()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the mib were applied: the values in StorageTmp are the new values.
 */
int
update_sigtranMIB(struct sigtranMIB_data *StorageTmp, struct sigtranMIB_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	sigtranMIB_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn revert_sigtranMIB(struct 
 * @fn void revert_sigtranMIB(struct sigtranMIB_data *StorageTmp, struct sigtranMIB_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase)
 */
void
revert_sigtranMIB(struct sigtranMIB_data *StorageTmp, struct sigtranMIB_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_sigtranMIB(StorageOld, NULL);
}

/**
 * @fn void refresh_sigtranMIB(int force)
 * @param force forced refresh when non-zero.
 * @brief refresh the scalar values of sigtranMIB.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a scalar has been requested).
 */
void
refresh_sigtranMIB(int force)
{
	if (sigtranMIBStorage == NULL) {
		struct sigtranMIB_data *StorageNew;

		if ((StorageNew = sigtranMIB_create()) == NULL)
			return;
		sigtranMIBStorage = StorageNew;
		sigtranMIB_refresh = 1;
	}
	if (!force && sigtranMIB_refresh == 0)
		return;
	DEBUGMSGTL(("sigtranMIB", "refresh_sigtranMIB: refreshing...  "));
	/* XXX: Update scalars as required here... */
	sigtranMIB_refresh = 0;
	DEBUGMSGTL(("sigtranMIB", "done.\n"));
}

/**
 * @fn u_char * var_sigtranMIB(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @param vp a pointer to the entry in the variables table for the requested variable.
 * @param name the object identifier for which to find.
 * @param length the length of the object identifier.
 * @param exact whether the name is exact.
 * @param var_len a pointer to the length of the representation of the object.
 * @param write_method a pointer to a write method for the object.
 * @brief locate variables in sigtranMIB.
 *
 * This function returns a pointer to a memory area that is static across the request that contains
 * the UCD-SNMP representation of the scalar (so that it may be used to read from for a GET,
 * GET-NEXT or GET-BULK request).  This returned pointer may be NULL, in which case the function is
 * telling UCD-SNMP that the scalar does not exist for reading; however, if write_method is
 * overwritten with a non-NULL value, the function is telling UCD-SNMP that the scalar exists for
 * writing.  Write-only objects can be effected in this way.
 */
u_char *
var_sigtranMIB(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct sigtranMIB_data *StorageTmp;
	u_char *rval;

	DEBUGMSGTL(("sigtranMIB", "var_sigtranMIB: lookup up varbind...  "));
	if (header_generic(vp, name, length, exact, var_len, write_method) == MATCH_FAILED)
		return NULL;
	/* Refresh the MIB values if required. */
	refresh_sigtranMIB(0);
	if ((StorageTmp = sigtranMIBStorage) == NULL) {
		DEBUGMSGTL(("sigtranMIB", "no datastructure.\n"));
		return NULL;
	}
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) SIGTRANSCTPPROFILENEXTINDEX:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->sigtranSctpProfileNextIndex);
		rval = (u_char *) &StorageTmp->sigtranSctpProfileNextIndex;
		break;
	default:
		ERROR_MSG("");
	}
	if (rval)
		DEBUGMSGTL(("sigtranMIB", "found.\n"));
	else
		DEBUGMSGTL(("sigtranMIB", "not found.\n"));
	return (rval);
}

/**
 * @fn struct sigtranSctpProfileTable_data *sigtranSctpProfileTable_create(void)
 * @brief create a fresh data structure representing a new row in the sigtranSctpProfileTable table.
 *
 * Creates a new sigtranSctpProfileTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct sigtranSctpProfileTable_data *
sigtranSctpProfileTable_create(void)
{
	struct sigtranSctpProfileTable_data *StorageNew = SNMP_MALLOC_STRUCT(sigtranSctpProfileTable_data);

	DEBUGMSGTL(("sigtranMIB", "sigtranSctpProfileTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->sigtranSctpProfileName = malloc(1)) == NULL)
			goto nomem;
		StorageNew->sigtranSctpProfileNameLen = 0;
		StorageNew->sigtranSctpProfileName[StorageNew->sigtranSctpProfileNameLen] = 0;
		StorageNew->sigtranSctpProfileNodelay = TV_FALSE;
		StorageNew->sigtranSctpPayloadProtocolId = 0;
		StorageNew->sigtranSctpLifetime = 0;
		StorageNew->sigtranSctpProfileCsumType = SIGTRANSCTPPROFILECSUMTYPE_CRC32C;
		StorageNew->sigtranSctpProfileMacType = SIGTRANSCTPPROFILEMACTYPE_OTHER;
		StorageNew->sigtranSctpProfileValidCookieLife = 6000;
		StorageNew->sigtranSctpProfileCookieInc = 100;
		StorageNew->sigtranSctpProfileMaxIstreams = 257;
		StorageNew->sigtranSctpProfileReqOstreams = 257;
		StorageNew->sigtranSctpEcn = TV_TRUE;
		StorageNew->sigtranSctpAdaptationLayerInfo = 0;
		StorageNew->sigtranSctpProfileMaxInitRetries = 8;
		StorageNew->sigtranSctpProfileMaxBurst = 4;
		StorageNew->sigtranSctpProfileAssocMaxRetrans = 10;
		StorageNew->sigtranSctpProfileMaxSackDelay = 20;
		StorageNew->sigtranSctpProfileRtoMin = 100;
		StorageNew->sigtranSctpProfileRtoInitial = 300;
		StorageNew->sigtranSctpProfileRtoMax = 6000;
		StorageNew->sigtranSctpProfilePathMaxRetrans = 5;
		StorageNew->sigtranSctpProfileHeartbeatItvl = 6000;
		StorageNew->sigtranSctpProfileStatus = 0;
		StorageNew->sigtranSctpProfileStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("sigtranMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	sigtranSctpProfileTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct sigtranSctpProfileTable_data *sigtranSctpProfileTable_duplicate(struct sigtranSctpProfileTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct sigtranSctpProfileTable_data *
sigtranSctpProfileTable_duplicate(struct sigtranSctpProfileTable_data *thedata)
{
	struct sigtranSctpProfileTable_data *StorageNew = SNMP_MALLOC_STRUCT(sigtranSctpProfileTable_data);

	DEBUGMSGTL(("sigtranMIB", "sigtranSctpProfileTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->sigtranSctpProfileTable_id = thedata->sigtranSctpProfileTable_id;
		StorageNew->sigtranSctpProfileIndex = thedata->sigtranSctpProfileIndex;
		if (!(StorageNew->sigtranSctpProfileName = malloc(thedata->sigtranSctpProfileNameLen + 1)))
			goto destroy;
		memcpy(StorageNew->sigtranSctpProfileName, thedata->sigtranSctpProfileName, thedata->sigtranSctpProfileNameLen);
		StorageNew->sigtranSctpProfileNameLen = thedata->sigtranSctpProfileNameLen;
		StorageNew->sigtranSctpProfileName[StorageNew->sigtranSctpProfileNameLen] = 0;
		StorageNew->sigtranSctpProfileNodelay = thedata->sigtranSctpProfileNodelay;
		StorageNew->sigtranSctpPayloadProtocolId = thedata->sigtranSctpPayloadProtocolId;
		StorageNew->sigtranSctpLifetime = thedata->sigtranSctpLifetime;
		StorageNew->sigtranSctpProfileCsumType = thedata->sigtranSctpProfileCsumType;
		StorageNew->sigtranSctpProfileMacType = thedata->sigtranSctpProfileMacType;
		StorageNew->sigtranSctpProfileValidCookieLife = thedata->sigtranSctpProfileValidCookieLife;
		StorageNew->sigtranSctpProfileCookieInc = thedata->sigtranSctpProfileCookieInc;
		StorageNew->sigtranSctpProfileMaxIstreams = thedata->sigtranSctpProfileMaxIstreams;
		StorageNew->sigtranSctpProfileReqOstreams = thedata->sigtranSctpProfileReqOstreams;
		StorageNew->sigtranSctpEcn = thedata->sigtranSctpEcn;
		StorageNew->sigtranSctpAdaptationLayerInfo = thedata->sigtranSctpAdaptationLayerInfo;
		StorageNew->sigtranSctpProfileMaxInitRetries = thedata->sigtranSctpProfileMaxInitRetries;
		StorageNew->sigtranSctpProfileMaxBurst = thedata->sigtranSctpProfileMaxBurst;
		StorageNew->sigtranSctpProfileAssocMaxRetrans = thedata->sigtranSctpProfileAssocMaxRetrans;
		StorageNew->sigtranSctpProfileMaxSackDelay = thedata->sigtranSctpProfileMaxSackDelay;
		StorageNew->sigtranSctpProfileRtoMin = thedata->sigtranSctpProfileRtoMin;
		StorageNew->sigtranSctpProfileRtoInitial = thedata->sigtranSctpProfileRtoInitial;
		StorageNew->sigtranSctpProfileRtoMax = thedata->sigtranSctpProfileRtoMax;
		StorageNew->sigtranSctpProfilePathMaxRetrans = thedata->sigtranSctpProfilePathMaxRetrans;
		StorageNew->sigtranSctpProfileHeartbeatItvl = thedata->sigtranSctpProfileHeartbeatItvl;
		StorageNew->sigtranSctpProfileStatus = thedata->sigtranSctpProfileStatus;
	}
      done:
	DEBUGMSGTL(("sigtranMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	sigtranSctpProfileTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int sigtranSctpProfileTable_destroy(struct sigtranSctpProfileTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
sigtranSctpProfileTable_destroy(struct sigtranSctpProfileTable_data **thedata)
{
	struct sigtranSctpProfileTable_data *StorageDel;

	DEBUGMSGTL(("sigtranMIB", "sigtranSctpProfileTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->sigtranSctpProfileName);
		StorageDel->sigtranSctpProfileNameLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("sigtranMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int sigtranSctpProfileTable_add(struct sigtranSctpProfileTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the sigtranSctpProfileTable table data set.
 *
 * Adds a table row structure to the sigtranSctpProfileTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
sigtranSctpProfileTable_add(struct sigtranSctpProfileTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("sigtranMIB", "sigtranSctpProfileTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* sigtranSctpProfileIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->sigtranSctpProfileIndex, sizeof(thedata->sigtranSctpProfileIndex));
		header_complex_add_data(&sigtranSctpProfileTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("sigtranMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int sigtranSctpProfileTable_del(struct sigtranSctpProfileTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the sigtranSctpProfileTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
sigtranSctpProfileTable_del(struct sigtranSctpProfileTable_data *thedata)
{
	struct sigtranSctpProfileTable_data *StorageDel;

	DEBUGMSGTL(("sigtranMIB", "sigtranSctpProfileTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(sigtranSctpProfileTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&sigtranSctpProfileTableStorage, hciptr);
	}
	DEBUGMSGTL(("sigtranMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_sigtranSctpProfileTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for sigtranSctpProfileTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case sigtranSctpProfileTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_sigtranSctpProfileTable(const char *token, char *line)
{
	size_t tmpsize;
	struct sigtranSctpProfileTable_data *StorageTmp = sigtranSctpProfileTable_create();

	DEBUGMSGTL(("sigtranMIB", "parse_sigtranSctpProfileTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->sigtranSctpProfileIndex, &tmpsize);
	SNMP_FREE(StorageTmp->sigtranSctpProfileName);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->sigtranSctpProfileName, &StorageTmp->sigtranSctpProfileNameLen);
	if (StorageTmp->sigtranSctpProfileName == NULL) {
		config_perror("invalid specification for sigtranSctpProfileName");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->sigtranSctpProfileNodelay, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->sigtranSctpPayloadProtocolId, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->sigtranSctpLifetime, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->sigtranSctpProfileCsumType, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->sigtranSctpProfileMacType, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->sigtranSctpProfileValidCookieLife, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->sigtranSctpProfileCookieInc, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->sigtranSctpProfileMaxIstreams, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->sigtranSctpProfileReqOstreams, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->sigtranSctpEcn, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->sigtranSctpAdaptationLayerInfo, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->sigtranSctpProfileMaxInitRetries, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->sigtranSctpProfileMaxBurst, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->sigtranSctpProfileAssocMaxRetrans, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->sigtranSctpProfileMaxSackDelay, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->sigtranSctpProfileRtoMin, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->sigtranSctpProfileRtoInitial, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->sigtranSctpProfileRtoMax, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->sigtranSctpProfilePathMaxRetrans, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->sigtranSctpProfileHeartbeatItvl, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->sigtranSctpProfileStatus, &tmpsize);
	sigtranSctpProfileTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("sigtranMIB", "done.\n"));
}

/*
 * store_sigtranSctpProfileTable(): store configuraiton file for sigtranSctpProfileTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_sigtranSctpProfileTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct sigtranSctpProfileTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("sigtranMIB", "store_sigtranSctpProfileTable: storing data...  "));
	refresh_sigtranSctpProfileTable(1);
	(void) tmpsize;
	for (hcindex = sigtranSctpProfileTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct sigtranSctpProfileTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "sigtranSctpProfileTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->sigtranSctpProfileIndex, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->sigtranSctpProfileName, &StorageTmp->sigtranSctpProfileNameLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->sigtranSctpProfileNodelay, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->sigtranSctpPayloadProtocolId, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->sigtranSctpLifetime, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->sigtranSctpProfileCsumType, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->sigtranSctpProfileMacType, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->sigtranSctpProfileValidCookieLife, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->sigtranSctpProfileCookieInc, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->sigtranSctpProfileMaxIstreams, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->sigtranSctpProfileReqOstreams, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->sigtranSctpEcn, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->sigtranSctpAdaptationLayerInfo, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->sigtranSctpProfileMaxInitRetries, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->sigtranSctpProfileMaxBurst, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->sigtranSctpProfileAssocMaxRetrans, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->sigtranSctpProfileMaxSackDelay, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->sigtranSctpProfileRtoMin, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->sigtranSctpProfileRtoInitial, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->sigtranSctpProfileRtoMax, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->sigtranSctpProfilePathMaxRetrans, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->sigtranSctpProfileHeartbeatItvl, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->sigtranSctpProfileStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("sigtranMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int activate_sigtranSctpProfileTable_row(struct sigtranSctpProfileTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_sigtranSctpProfileTable_row(struct sigtranSctpProfileTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_sigtranSctpProfileTable_row(struct sigtranSctpProfileTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_sigtranSctpProfileTable_row(struct sigtranSctpProfileTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int check_sigtranSctpProfileTable_row(struct sigtranSctpProfileTable_data *StorageTmp, struct sigtranSctpProfileTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_sigtranSctpProfileTable_row(struct sigtranSctpProfileTable_data *StorageTmp, struct sigtranSctpProfileTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_sigtranSctpProfileTable_row(struct sigtranSctpProfileTable_data *StorageTmp, struct sigtranSctpProfileTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_sigtranSctpProfileTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_sigtranSctpProfileTable_row(struct sigtranSctpProfileTable_data *StorageTmp, struct sigtranSctpProfileTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	sigtranSctpProfileTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_sigtranSctpProfileTable_row(struct sigtranSctpProfileTable_data *StorageTmp, struct sigtranSctpProfileTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_sigtranSctpProfileTable_row(struct sigtranSctpProfileTable_data *StorageTmp, struct sigtranSctpProfileTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_sigtranSctpProfileTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_sigtranSctpProfileTable_row(struct sigtranSctpProfileTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the sigtranSctpProfileTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct sigtranSctpProfileTable_data *
refresh_sigtranSctpProfileTable_row(struct sigtranSctpProfileTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->sigtranSctpProfileTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->sigtranSctpProfileTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_sigtranSctpProfileTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the sigtranSctpProfileTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_sigtranSctpProfileTable(int force)
{
	if (!force && sigtranSctpProfileTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	sigtranSctpProfileTable_refresh = 0;
}

/**
 * @fn u_char *var_sigtranSctpProfileTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in sigtranSctpProfileTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_sigtranMIB above.
 */
u_char *
var_sigtranSctpProfileTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct sigtranSctpProfileTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("sigtranMIB", "var_sigtranSctpProfileTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_sigtranSctpProfileTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(sigtranSctpProfileTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_sigtranSctpProfileTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) SIGTRANSCTPPROFILENAME:	/* Create */
		*write_method = write_sigtranSctpProfileName;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->sigtranSctpProfileNameLen;
		rval = (u_char *) StorageTmp->sigtranSctpProfileName;
		break;
	case (u_char) SIGTRANSCTPPROFILENODELAY:	/* Create */
		*write_method = write_sigtranSctpProfileNodelay;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->sigtranSctpProfileNodelay);
		rval = (u_char *) &StorageTmp->sigtranSctpProfileNodelay;
		break;
	case (u_char) SIGTRANSCTPPAYLOADPROTOCOLID:	/* Create */
		*write_method = write_sigtranSctpPayloadProtocolId;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->sigtranSctpPayloadProtocolId);
		rval = (u_char *) &StorageTmp->sigtranSctpPayloadProtocolId;
		break;
	case (u_char) SIGTRANSCTPLIFETIME:	/* Create */
		*write_method = write_sigtranSctpLifetime;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->sigtranSctpLifetime);
		rval = (u_char *) &StorageTmp->sigtranSctpLifetime;
		break;
	case (u_char) SIGTRANSCTPPROFILECSUMTYPE:	/* Create */
		*write_method = write_sigtranSctpProfileCsumType;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->sigtranSctpProfileCsumType);
		rval = (u_char *) &StorageTmp->sigtranSctpProfileCsumType;
		break;
	case (u_char) SIGTRANSCTPPROFILEMACTYPE:	/* Create */
		*write_method = write_sigtranSctpProfileMacType;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->sigtranSctpProfileMacType);
		rval = (u_char *) &StorageTmp->sigtranSctpProfileMacType;
		break;
	case (u_char) SIGTRANSCTPPROFILEVALIDCOOKIELIFE:	/* Create */
		*write_method = write_sigtranSctpProfileValidCookieLife;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->sigtranSctpProfileValidCookieLife);
		rval = (u_char *) &StorageTmp->sigtranSctpProfileValidCookieLife;
		break;
	case (u_char) SIGTRANSCTPPROFILECOOKIEINC:	/* Create */
		*write_method = write_sigtranSctpProfileCookieInc;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->sigtranSctpProfileCookieInc);
		rval = (u_char *) &StorageTmp->sigtranSctpProfileCookieInc;
		break;
	case (u_char) SIGTRANSCTPPROFILEMAXISTREAMS:	/* Create */
		*write_method = write_sigtranSctpProfileMaxIstreams;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->sigtranSctpProfileMaxIstreams);
		rval = (u_char *) &StorageTmp->sigtranSctpProfileMaxIstreams;
		break;
	case (u_char) SIGTRANSCTPPROFILEREQOSTREAMS:	/* Create */
		*write_method = write_sigtranSctpProfileReqOstreams;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->sigtranSctpProfileReqOstreams);
		rval = (u_char *) &StorageTmp->sigtranSctpProfileReqOstreams;
		break;
	case (u_char) SIGTRANSCTPECN:	/* Create */
		*write_method = write_sigtranSctpEcn;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->sigtranSctpEcn);
		rval = (u_char *) &StorageTmp->sigtranSctpEcn;
		break;
	case (u_char) SIGTRANSCTPADAPTATIONLAYERINFO:	/* Create */
		*write_method = write_sigtranSctpAdaptationLayerInfo;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->sigtranSctpAdaptationLayerInfo);
		rval = (u_char *) &StorageTmp->sigtranSctpAdaptationLayerInfo;
		break;
	case (u_char) SIGTRANSCTPPROFILEMAXINITRETRIES:	/* Create */
		*write_method = write_sigtranSctpProfileMaxInitRetries;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->sigtranSctpProfileMaxInitRetries);
		rval = (u_char *) &StorageTmp->sigtranSctpProfileMaxInitRetries;
		break;
	case (u_char) SIGTRANSCTPPROFILEMAXBURST:	/* Create */
		*write_method = write_sigtranSctpProfileMaxBurst;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->sigtranSctpProfileMaxBurst);
		rval = (u_char *) &StorageTmp->sigtranSctpProfileMaxBurst;
		break;
	case (u_char) SIGTRANSCTPPROFILEASSOCMAXRETRANS:	/* Create */
		*write_method = write_sigtranSctpProfileAssocMaxRetrans;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->sigtranSctpProfileAssocMaxRetrans);
		rval = (u_char *) &StorageTmp->sigtranSctpProfileAssocMaxRetrans;
		break;
	case (u_char) SIGTRANSCTPPROFILEMAXSACKDELAY:	/* Create */
		*write_method = write_sigtranSctpProfileMaxSackDelay;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->sigtranSctpProfileMaxSackDelay);
		rval = (u_char *) &StorageTmp->sigtranSctpProfileMaxSackDelay;
		break;
	case (u_char) SIGTRANSCTPPROFILERTOMIN:	/* Create */
		*write_method = write_sigtranSctpProfileRtoMin;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->sigtranSctpProfileRtoMin);
		rval = (u_char *) &StorageTmp->sigtranSctpProfileRtoMin;
		break;
	case (u_char) SIGTRANSCTPPROFILERTOINITIAL:	/* Create */
		*write_method = write_sigtranSctpProfileRtoInitial;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->sigtranSctpProfileRtoInitial);
		rval = (u_char *) &StorageTmp->sigtranSctpProfileRtoInitial;
		break;
	case (u_char) SIGTRANSCTPPROFILERTOMAX:	/* Create */
		*write_method = write_sigtranSctpProfileRtoMax;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->sigtranSctpProfileRtoMax);
		rval = (u_char *) &StorageTmp->sigtranSctpProfileRtoMax;
		break;
	case (u_char) SIGTRANSCTPPROFILEPATHMAXRETRANS:	/* Create */
		*write_method = write_sigtranSctpProfilePathMaxRetrans;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->sigtranSctpProfilePathMaxRetrans);
		rval = (u_char *) &StorageTmp->sigtranSctpProfilePathMaxRetrans;
		break;
	case (u_char) SIGTRANSCTPPROFILEHEARTBEATITVL:	/* Create */
		*write_method = write_sigtranSctpProfileHeartbeatItvl;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->sigtranSctpProfileHeartbeatItvl);
		rval = (u_char *) &StorageTmp->sigtranSctpProfileHeartbeatItvl;
		break;
	case (u_char) SIGTRANSCTPPROFILESTATUS:	/* Create */
		*write_method = write_sigtranSctpProfileStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->sigtranSctpProfileStatus);
		rval = (u_char *) &StorageTmp->sigtranSctpProfileStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int write_sigtranSctpProfileName(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_sigtranSctpProfileName(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct sigtranSctpProfileTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("sigtranMIB", "write_sigtranSctpProfileName entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(sigtranSctpProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->sigtranSctpProfileStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileName: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileName not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 0..255 */
		if (var_val_len > SPRINT_MAX_LEN || ((0 > var_val_len || var_val_len > 255))) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileName: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			if (StorageTmp->sigtranSctpProfileTable_rsvs == 0)
				if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old = sigtranSctpProfileTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->sigtranSctpProfileTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->sigtranSctpProfileName);
		StorageTmp->sigtranSctpProfileName = string;
		StorageTmp->sigtranSctpProfileNameLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->sigtranSctpProfileTable_tsts == 0)
				if ((ret = check_sigtranSctpProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->sigtranSctpProfileTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->sigtranSctpProfileName for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->sigtranSctpProfileTable_sets == 0)
				if ((ret = update_sigtranSctpProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->sigtranSctpProfileTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			sigtranSctpProfileTable_destroy(&StorageTmp->sigtranSctpProfileTable_old);
			StorageTmp->sigtranSctpProfileTable_rsvs = 0;
			StorageTmp->sigtranSctpProfileTable_tsts = 0;
			StorageTmp->sigtranSctpProfileTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->sigtranSctpProfileTable_sets == 0)
			revert_sigtranSctpProfileTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			break;
		if (StorageOld->sigtranSctpProfileName != NULL) {
			SNMP_FREE(StorageTmp->sigtranSctpProfileName);
			StorageTmp->sigtranSctpProfileName = StorageOld->sigtranSctpProfileName;
			StorageTmp->sigtranSctpProfileNameLen = StorageOld->sigtranSctpProfileNameLen;
			StorageOld->sigtranSctpProfileName = NULL;
			StorageOld->sigtranSctpProfileNameLen = 0;
		}
		if (--StorageTmp->sigtranSctpProfileTable_rsvs == 0)
			sigtranSctpProfileTable_destroy(&StorageTmp->sigtranSctpProfileTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_sigtranSctpProfileNodelay(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_sigtranSctpProfileNodelay(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct sigtranSctpProfileTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("sigtranMIB", "write_sigtranSctpProfileNodelay entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(sigtranSctpProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->sigtranSctpProfileStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileNodelay: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileNodelay not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileNodelay: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value false */
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileNodelay: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			if (StorageTmp->sigtranSctpProfileTable_rsvs == 0)
				if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old = sigtranSctpProfileTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->sigtranSctpProfileTable_rsvs++;
		StorageTmp->sigtranSctpProfileNodelay = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->sigtranSctpProfileTable_tsts == 0)
				if ((ret = check_sigtranSctpProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->sigtranSctpProfileTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->sigtranSctpProfileNodelay for you to use, and you have just been asked to do something with it.  Note that anything done 
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->sigtranSctpProfileTable_sets == 0)
				if ((ret = update_sigtranSctpProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->sigtranSctpProfileTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			sigtranSctpProfileTable_destroy(&StorageTmp->sigtranSctpProfileTable_old);
			StorageTmp->sigtranSctpProfileTable_rsvs = 0;
			StorageTmp->sigtranSctpProfileTable_tsts = 0;
			StorageTmp->sigtranSctpProfileTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->sigtranSctpProfileTable_sets == 0)
			revert_sigtranSctpProfileTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			break;
		StorageTmp->sigtranSctpProfileNodelay = StorageOld->sigtranSctpProfileNodelay;
		if (--StorageTmp->sigtranSctpProfileTable_rsvs == 0)
			sigtranSctpProfileTable_destroy(&StorageTmp->sigtranSctpProfileTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_sigtranSctpPayloadProtocolId(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_sigtranSctpPayloadProtocolId(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct sigtranSctpProfileTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("sigtranMIB", "write_sigtranSctpPayloadProtocolId entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(sigtranSctpProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->sigtranSctpProfileStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpPayloadProtocolId: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpPayloadProtocolId not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpPayloadProtocolId: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 0 */
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			if (StorageTmp->sigtranSctpProfileTable_rsvs == 0)
				if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old = sigtranSctpProfileTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->sigtranSctpProfileTable_rsvs++;
		StorageTmp->sigtranSctpPayloadProtocolId = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->sigtranSctpProfileTable_tsts == 0)
				if ((ret = check_sigtranSctpProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->sigtranSctpProfileTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->sigtranSctpPayloadProtocolId for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->sigtranSctpProfileTable_sets == 0)
				if ((ret = update_sigtranSctpProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->sigtranSctpProfileTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			sigtranSctpProfileTable_destroy(&StorageTmp->sigtranSctpProfileTable_old);
			StorageTmp->sigtranSctpProfileTable_rsvs = 0;
			StorageTmp->sigtranSctpProfileTable_tsts = 0;
			StorageTmp->sigtranSctpProfileTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->sigtranSctpProfileTable_sets == 0)
			revert_sigtranSctpProfileTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			break;
		StorageTmp->sigtranSctpPayloadProtocolId = StorageOld->sigtranSctpPayloadProtocolId;
		if (--StorageTmp->sigtranSctpProfileTable_rsvs == 0)
			sigtranSctpProfileTable_destroy(&StorageTmp->sigtranSctpProfileTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_sigtranSctpLifetime(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_sigtranSctpLifetime(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct sigtranSctpProfileTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("sigtranMIB", "write_sigtranSctpLifetime entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(sigtranSctpProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->sigtranSctpProfileStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpLifetime: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpLifetime not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpLifetime: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpLifetime: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			if (StorageTmp->sigtranSctpProfileTable_rsvs == 0)
				if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old = sigtranSctpProfileTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->sigtranSctpProfileTable_rsvs++;
		StorageTmp->sigtranSctpLifetime = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->sigtranSctpProfileTable_tsts == 0)
				if ((ret = check_sigtranSctpProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->sigtranSctpProfileTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->sigtranSctpLifetime for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->sigtranSctpProfileTable_sets == 0)
				if ((ret = update_sigtranSctpProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->sigtranSctpProfileTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			sigtranSctpProfileTable_destroy(&StorageTmp->sigtranSctpProfileTable_old);
			StorageTmp->sigtranSctpProfileTable_rsvs = 0;
			StorageTmp->sigtranSctpProfileTable_tsts = 0;
			StorageTmp->sigtranSctpProfileTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->sigtranSctpProfileTable_sets == 0)
			revert_sigtranSctpProfileTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			break;
		StorageTmp->sigtranSctpLifetime = StorageOld->sigtranSctpLifetime;
		if (--StorageTmp->sigtranSctpProfileTable_rsvs == 0)
			sigtranSctpProfileTable_destroy(&StorageTmp->sigtranSctpProfileTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_sigtranSctpProfileCsumType(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_sigtranSctpProfileCsumType(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct sigtranSctpProfileTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("sigtranMIB", "write_sigtranSctpProfileCsumType entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(sigtranSctpProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->sigtranSctpProfileStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileCsumType: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileCsumType not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileCsumType: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value crc32c */
		switch (set_value) {
		case SIGTRANSCTPPROFILECSUMTYPE_CRC32C:
		case SIGTRANSCTPPROFILECSUMTYPE_ADLER32:
		case SIGTRANSCTPPROFILECSUMTYPE_NONE:
		case SIGTRANSCTPPROFILECSUMTYPE_ADAPTIVE:
		case SIGTRANSCTPPROFILECSUMTYPE_OTHER:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileCsumType: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			if (StorageTmp->sigtranSctpProfileTable_rsvs == 0)
				if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old = sigtranSctpProfileTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->sigtranSctpProfileTable_rsvs++;
		StorageTmp->sigtranSctpProfileCsumType = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->sigtranSctpProfileTable_tsts == 0)
				if ((ret = check_sigtranSctpProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->sigtranSctpProfileTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->sigtranSctpProfileCsumType for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->sigtranSctpProfileTable_sets == 0)
				if ((ret = update_sigtranSctpProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->sigtranSctpProfileTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			sigtranSctpProfileTable_destroy(&StorageTmp->sigtranSctpProfileTable_old);
			StorageTmp->sigtranSctpProfileTable_rsvs = 0;
			StorageTmp->sigtranSctpProfileTable_tsts = 0;
			StorageTmp->sigtranSctpProfileTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->sigtranSctpProfileTable_sets == 0)
			revert_sigtranSctpProfileTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			break;
		StorageTmp->sigtranSctpProfileCsumType = StorageOld->sigtranSctpProfileCsumType;
		if (--StorageTmp->sigtranSctpProfileTable_rsvs == 0)
			sigtranSctpProfileTable_destroy(&StorageTmp->sigtranSctpProfileTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_sigtranSctpProfileMacType(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_sigtranSctpProfileMacType(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct sigtranSctpProfileTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("sigtranMIB", "write_sigtranSctpProfileMacType entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(sigtranSctpProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->sigtranSctpProfileStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileMacType: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileMacType not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileMacType: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value other */
		switch (set_value) {
		case SIGTRANSCTPPROFILEMACTYPE_MD5:
		case SIGTRANSCTPPROFILEMACTYPE_SHA1:
		case SIGTRANSCTPPROFILEMACTYPE_NONE:
		case SIGTRANSCTPPROFILEMACTYPE_OTHER:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileMacType: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			if (StorageTmp->sigtranSctpProfileTable_rsvs == 0)
				if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old = sigtranSctpProfileTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->sigtranSctpProfileTable_rsvs++;
		StorageTmp->sigtranSctpProfileMacType = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->sigtranSctpProfileTable_tsts == 0)
				if ((ret = check_sigtranSctpProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->sigtranSctpProfileTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->sigtranSctpProfileMacType for you to use, and you have just been asked to do something with it.  Note that anything done 
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->sigtranSctpProfileTable_sets == 0)
				if ((ret = update_sigtranSctpProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->sigtranSctpProfileTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			sigtranSctpProfileTable_destroy(&StorageTmp->sigtranSctpProfileTable_old);
			StorageTmp->sigtranSctpProfileTable_rsvs = 0;
			StorageTmp->sigtranSctpProfileTable_tsts = 0;
			StorageTmp->sigtranSctpProfileTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->sigtranSctpProfileTable_sets == 0)
			revert_sigtranSctpProfileTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			break;
		StorageTmp->sigtranSctpProfileMacType = StorageOld->sigtranSctpProfileMacType;
		if (--StorageTmp->sigtranSctpProfileTable_rsvs == 0)
			sigtranSctpProfileTable_destroy(&StorageTmp->sigtranSctpProfileTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_sigtranSctpProfileValidCookieLife(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_sigtranSctpProfileValidCookieLife(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct sigtranSctpProfileTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("sigtranMIB", "write_sigtranSctpProfileValidCookieLife entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(sigtranSctpProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->sigtranSctpProfileStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileValidCookieLife: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileValidCookieLife not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileValidCookieLife: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 6000 */
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileValidCookieLife: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			if (StorageTmp->sigtranSctpProfileTable_rsvs == 0)
				if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old = sigtranSctpProfileTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->sigtranSctpProfileTable_rsvs++;
		StorageTmp->sigtranSctpProfileValidCookieLife = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->sigtranSctpProfileTable_tsts == 0)
				if ((ret = check_sigtranSctpProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->sigtranSctpProfileTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->sigtranSctpProfileValidCookieLife for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->sigtranSctpProfileTable_sets == 0)
				if ((ret = update_sigtranSctpProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->sigtranSctpProfileTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			sigtranSctpProfileTable_destroy(&StorageTmp->sigtranSctpProfileTable_old);
			StorageTmp->sigtranSctpProfileTable_rsvs = 0;
			StorageTmp->sigtranSctpProfileTable_tsts = 0;
			StorageTmp->sigtranSctpProfileTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->sigtranSctpProfileTable_sets == 0)
			revert_sigtranSctpProfileTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			break;
		StorageTmp->sigtranSctpProfileValidCookieLife = StorageOld->sigtranSctpProfileValidCookieLife;
		if (--StorageTmp->sigtranSctpProfileTable_rsvs == 0)
			sigtranSctpProfileTable_destroy(&StorageTmp->sigtranSctpProfileTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_sigtranSctpProfileCookieInc(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_sigtranSctpProfileCookieInc(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct sigtranSctpProfileTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("sigtranMIB", "write_sigtranSctpProfileCookieInc entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(sigtranSctpProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->sigtranSctpProfileStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileCookieInc: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileCookieInc not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileCookieInc: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 100 */
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileCookieInc: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			if (StorageTmp->sigtranSctpProfileTable_rsvs == 0)
				if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old = sigtranSctpProfileTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->sigtranSctpProfileTable_rsvs++;
		StorageTmp->sigtranSctpProfileCookieInc = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->sigtranSctpProfileTable_tsts == 0)
				if ((ret = check_sigtranSctpProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->sigtranSctpProfileTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->sigtranSctpProfileCookieInc for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->sigtranSctpProfileTable_sets == 0)
				if ((ret = update_sigtranSctpProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->sigtranSctpProfileTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			sigtranSctpProfileTable_destroy(&StorageTmp->sigtranSctpProfileTable_old);
			StorageTmp->sigtranSctpProfileTable_rsvs = 0;
			StorageTmp->sigtranSctpProfileTable_tsts = 0;
			StorageTmp->sigtranSctpProfileTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->sigtranSctpProfileTable_sets == 0)
			revert_sigtranSctpProfileTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			break;
		StorageTmp->sigtranSctpProfileCookieInc = StorageOld->sigtranSctpProfileCookieInc;
		if (--StorageTmp->sigtranSctpProfileTable_rsvs == 0)
			sigtranSctpProfileTable_destroy(&StorageTmp->sigtranSctpProfileTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_sigtranSctpProfileMaxIstreams(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_sigtranSctpProfileMaxIstreams(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct sigtranSctpProfileTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("sigtranMIB", "write_sigtranSctpProfileMaxIstreams entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(sigtranSctpProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->sigtranSctpProfileStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileMaxIstreams: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileMaxIstreams not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileMaxIstreams: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 257 */
		/* Note: ranges 2..16384 */
		if ((2 > set_value || set_value > 16384)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileMaxIstreams: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			if (StorageTmp->sigtranSctpProfileTable_rsvs == 0)
				if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old = sigtranSctpProfileTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->sigtranSctpProfileTable_rsvs++;
		StorageTmp->sigtranSctpProfileMaxIstreams = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->sigtranSctpProfileTable_tsts == 0)
				if ((ret = check_sigtranSctpProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->sigtranSctpProfileTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->sigtranSctpProfileMaxIstreams for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->sigtranSctpProfileTable_sets == 0)
				if ((ret = update_sigtranSctpProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->sigtranSctpProfileTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			sigtranSctpProfileTable_destroy(&StorageTmp->sigtranSctpProfileTable_old);
			StorageTmp->sigtranSctpProfileTable_rsvs = 0;
			StorageTmp->sigtranSctpProfileTable_tsts = 0;
			StorageTmp->sigtranSctpProfileTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->sigtranSctpProfileTable_sets == 0)
			revert_sigtranSctpProfileTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			break;
		StorageTmp->sigtranSctpProfileMaxIstreams = StorageOld->sigtranSctpProfileMaxIstreams;
		if (--StorageTmp->sigtranSctpProfileTable_rsvs == 0)
			sigtranSctpProfileTable_destroy(&StorageTmp->sigtranSctpProfileTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_sigtranSctpProfileReqOstreams(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_sigtranSctpProfileReqOstreams(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct sigtranSctpProfileTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("sigtranMIB", "write_sigtranSctpProfileReqOstreams entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(sigtranSctpProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->sigtranSctpProfileStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileReqOstreams: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileReqOstreams not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileReqOstreams: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 257 */
		/* Note: ranges 2..16384 */
		if ((2 > set_value || set_value > 16384)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileReqOstreams: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			if (StorageTmp->sigtranSctpProfileTable_rsvs == 0)
				if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old = sigtranSctpProfileTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->sigtranSctpProfileTable_rsvs++;
		StorageTmp->sigtranSctpProfileReqOstreams = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->sigtranSctpProfileTable_tsts == 0)
				if ((ret = check_sigtranSctpProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->sigtranSctpProfileTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->sigtranSctpProfileReqOstreams for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->sigtranSctpProfileTable_sets == 0)
				if ((ret = update_sigtranSctpProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->sigtranSctpProfileTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			sigtranSctpProfileTable_destroy(&StorageTmp->sigtranSctpProfileTable_old);
			StorageTmp->sigtranSctpProfileTable_rsvs = 0;
			StorageTmp->sigtranSctpProfileTable_tsts = 0;
			StorageTmp->sigtranSctpProfileTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->sigtranSctpProfileTable_sets == 0)
			revert_sigtranSctpProfileTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			break;
		StorageTmp->sigtranSctpProfileReqOstreams = StorageOld->sigtranSctpProfileReqOstreams;
		if (--StorageTmp->sigtranSctpProfileTable_rsvs == 0)
			sigtranSctpProfileTable_destroy(&StorageTmp->sigtranSctpProfileTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_sigtranSctpEcn(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_sigtranSctpEcn(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct sigtranSctpProfileTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("sigtranMIB", "write_sigtranSctpEcn entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(sigtranSctpProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->sigtranSctpProfileStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpEcn: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpEcn not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpEcn: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value true */
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpEcn: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			if (StorageTmp->sigtranSctpProfileTable_rsvs == 0)
				if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old = sigtranSctpProfileTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->sigtranSctpProfileTable_rsvs++;
		StorageTmp->sigtranSctpEcn = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->sigtranSctpProfileTable_tsts == 0)
				if ((ret = check_sigtranSctpProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->sigtranSctpProfileTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->sigtranSctpEcn for you to use, and you have just been asked to do something with it.  Note that anything done here must
				   be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->sigtranSctpProfileTable_sets == 0)
				if ((ret = update_sigtranSctpProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->sigtranSctpProfileTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			sigtranSctpProfileTable_destroy(&StorageTmp->sigtranSctpProfileTable_old);
			StorageTmp->sigtranSctpProfileTable_rsvs = 0;
			StorageTmp->sigtranSctpProfileTable_tsts = 0;
			StorageTmp->sigtranSctpProfileTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->sigtranSctpProfileTable_sets == 0)
			revert_sigtranSctpProfileTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			break;
		StorageTmp->sigtranSctpEcn = StorageOld->sigtranSctpEcn;
		if (--StorageTmp->sigtranSctpProfileTable_rsvs == 0)
			sigtranSctpProfileTable_destroy(&StorageTmp->sigtranSctpProfileTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_sigtranSctpAdaptationLayerInfo(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_sigtranSctpAdaptationLayerInfo(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct sigtranSctpProfileTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("sigtranMIB", "write_sigtranSctpAdaptationLayerInfo entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(sigtranSctpProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->sigtranSctpProfileStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpAdaptationLayerInfo: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpAdaptationLayerInfo not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpAdaptationLayerInfo: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 0 */
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			if (StorageTmp->sigtranSctpProfileTable_rsvs == 0)
				if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old = sigtranSctpProfileTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->sigtranSctpProfileTable_rsvs++;
		StorageTmp->sigtranSctpAdaptationLayerInfo = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->sigtranSctpProfileTable_tsts == 0)
				if ((ret = check_sigtranSctpProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->sigtranSctpProfileTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->sigtranSctpAdaptationLayerInfo for you to use, and you have just been asked to do something with it.  Note that anything 
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->sigtranSctpProfileTable_sets == 0)
				if ((ret = update_sigtranSctpProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->sigtranSctpProfileTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			sigtranSctpProfileTable_destroy(&StorageTmp->sigtranSctpProfileTable_old);
			StorageTmp->sigtranSctpProfileTable_rsvs = 0;
			StorageTmp->sigtranSctpProfileTable_tsts = 0;
			StorageTmp->sigtranSctpProfileTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->sigtranSctpProfileTable_sets == 0)
			revert_sigtranSctpProfileTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			break;
		StorageTmp->sigtranSctpAdaptationLayerInfo = StorageOld->sigtranSctpAdaptationLayerInfo;
		if (--StorageTmp->sigtranSctpProfileTable_rsvs == 0)
			sigtranSctpProfileTable_destroy(&StorageTmp->sigtranSctpProfileTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_sigtranSctpProfileMaxInitRetries(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_sigtranSctpProfileMaxInitRetries(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct sigtranSctpProfileTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("sigtranMIB", "write_sigtranSctpProfileMaxInitRetries entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(sigtranSctpProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->sigtranSctpProfileStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileMaxInitRetries: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileMaxInitRetries not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileMaxInitRetries: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 8 */
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			if (StorageTmp->sigtranSctpProfileTable_rsvs == 0)
				if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old = sigtranSctpProfileTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->sigtranSctpProfileTable_rsvs++;
		StorageTmp->sigtranSctpProfileMaxInitRetries = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->sigtranSctpProfileTable_tsts == 0)
				if ((ret = check_sigtranSctpProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->sigtranSctpProfileTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->sigtranSctpProfileMaxInitRetries for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->sigtranSctpProfileTable_sets == 0)
				if ((ret = update_sigtranSctpProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->sigtranSctpProfileTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			sigtranSctpProfileTable_destroy(&StorageTmp->sigtranSctpProfileTable_old);
			StorageTmp->sigtranSctpProfileTable_rsvs = 0;
			StorageTmp->sigtranSctpProfileTable_tsts = 0;
			StorageTmp->sigtranSctpProfileTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->sigtranSctpProfileTable_sets == 0)
			revert_sigtranSctpProfileTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			break;
		StorageTmp->sigtranSctpProfileMaxInitRetries = StorageOld->sigtranSctpProfileMaxInitRetries;
		if (--StorageTmp->sigtranSctpProfileTable_rsvs == 0)
			sigtranSctpProfileTable_destroy(&StorageTmp->sigtranSctpProfileTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_sigtranSctpProfileMaxBurst(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_sigtranSctpProfileMaxBurst(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct sigtranSctpProfileTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("sigtranMIB", "write_sigtranSctpProfileMaxBurst entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(sigtranSctpProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->sigtranSctpProfileStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileMaxBurst: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileMaxBurst not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileMaxBurst: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 4 */
		/* Note: ranges 1..-1 */
		if ((1 > set_value || set_value > -1)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileMaxBurst: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			if (StorageTmp->sigtranSctpProfileTable_rsvs == 0)
				if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old = sigtranSctpProfileTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->sigtranSctpProfileTable_rsvs++;
		StorageTmp->sigtranSctpProfileMaxBurst = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->sigtranSctpProfileTable_tsts == 0)
				if ((ret = check_sigtranSctpProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->sigtranSctpProfileTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->sigtranSctpProfileMaxBurst for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->sigtranSctpProfileTable_sets == 0)
				if ((ret = update_sigtranSctpProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->sigtranSctpProfileTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			sigtranSctpProfileTable_destroy(&StorageTmp->sigtranSctpProfileTable_old);
			StorageTmp->sigtranSctpProfileTable_rsvs = 0;
			StorageTmp->sigtranSctpProfileTable_tsts = 0;
			StorageTmp->sigtranSctpProfileTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->sigtranSctpProfileTable_sets == 0)
			revert_sigtranSctpProfileTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			break;
		StorageTmp->sigtranSctpProfileMaxBurst = StorageOld->sigtranSctpProfileMaxBurst;
		if (--StorageTmp->sigtranSctpProfileTable_rsvs == 0)
			sigtranSctpProfileTable_destroy(&StorageTmp->sigtranSctpProfileTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_sigtranSctpProfileAssocMaxRetrans(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_sigtranSctpProfileAssocMaxRetrans(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct sigtranSctpProfileTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("sigtranMIB", "write_sigtranSctpProfileAssocMaxRetrans entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(sigtranSctpProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->sigtranSctpProfileStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileAssocMaxRetrans: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileAssocMaxRetrans not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileAssocMaxRetrans: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 10 */
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			if (StorageTmp->sigtranSctpProfileTable_rsvs == 0)
				if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old = sigtranSctpProfileTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->sigtranSctpProfileTable_rsvs++;
		StorageTmp->sigtranSctpProfileAssocMaxRetrans = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->sigtranSctpProfileTable_tsts == 0)
				if ((ret = check_sigtranSctpProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->sigtranSctpProfileTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->sigtranSctpProfileAssocMaxRetrans for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->sigtranSctpProfileTable_sets == 0)
				if ((ret = update_sigtranSctpProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->sigtranSctpProfileTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			sigtranSctpProfileTable_destroy(&StorageTmp->sigtranSctpProfileTable_old);
			StorageTmp->sigtranSctpProfileTable_rsvs = 0;
			StorageTmp->sigtranSctpProfileTable_tsts = 0;
			StorageTmp->sigtranSctpProfileTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->sigtranSctpProfileTable_sets == 0)
			revert_sigtranSctpProfileTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			break;
		StorageTmp->sigtranSctpProfileAssocMaxRetrans = StorageOld->sigtranSctpProfileAssocMaxRetrans;
		if (--StorageTmp->sigtranSctpProfileTable_rsvs == 0)
			sigtranSctpProfileTable_destroy(&StorageTmp->sigtranSctpProfileTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_sigtranSctpProfileMaxSackDelay(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_sigtranSctpProfileMaxSackDelay(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct sigtranSctpProfileTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("sigtranMIB", "write_sigtranSctpProfileMaxSackDelay entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(sigtranSctpProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->sigtranSctpProfileStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileMaxSackDelay: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileMaxSackDelay not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileMaxSackDelay: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 20 */
		/* Note: ranges 0..50 */
		if ((0 > set_value || set_value > 50)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileMaxSackDelay: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			if (StorageTmp->sigtranSctpProfileTable_rsvs == 0)
				if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old = sigtranSctpProfileTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->sigtranSctpProfileTable_rsvs++;
		StorageTmp->sigtranSctpProfileMaxSackDelay = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->sigtranSctpProfileTable_tsts == 0)
				if ((ret = check_sigtranSctpProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->sigtranSctpProfileTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->sigtranSctpProfileMaxSackDelay for you to use, and you have just been asked to do something with it.  Note that anything 
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->sigtranSctpProfileTable_sets == 0)
				if ((ret = update_sigtranSctpProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->sigtranSctpProfileTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			sigtranSctpProfileTable_destroy(&StorageTmp->sigtranSctpProfileTable_old);
			StorageTmp->sigtranSctpProfileTable_rsvs = 0;
			StorageTmp->sigtranSctpProfileTable_tsts = 0;
			StorageTmp->sigtranSctpProfileTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->sigtranSctpProfileTable_sets == 0)
			revert_sigtranSctpProfileTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			break;
		StorageTmp->sigtranSctpProfileMaxSackDelay = StorageOld->sigtranSctpProfileMaxSackDelay;
		if (--StorageTmp->sigtranSctpProfileTable_rsvs == 0)
			sigtranSctpProfileTable_destroy(&StorageTmp->sigtranSctpProfileTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_sigtranSctpProfileRtoMin(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_sigtranSctpProfileRtoMin(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct sigtranSctpProfileTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("sigtranMIB", "write_sigtranSctpProfileRtoMin entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(sigtranSctpProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->sigtranSctpProfileStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileRtoMin: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileRtoMin not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileRtoMin: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 100 */
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileRtoMin: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			if (StorageTmp->sigtranSctpProfileTable_rsvs == 0)
				if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old = sigtranSctpProfileTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->sigtranSctpProfileTable_rsvs++;
		StorageTmp->sigtranSctpProfileRtoMin = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->sigtranSctpProfileTable_tsts == 0)
				if ((ret = check_sigtranSctpProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->sigtranSctpProfileTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->sigtranSctpProfileRtoMin for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->sigtranSctpProfileTable_sets == 0)
				if ((ret = update_sigtranSctpProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->sigtranSctpProfileTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			sigtranSctpProfileTable_destroy(&StorageTmp->sigtranSctpProfileTable_old);
			StorageTmp->sigtranSctpProfileTable_rsvs = 0;
			StorageTmp->sigtranSctpProfileTable_tsts = 0;
			StorageTmp->sigtranSctpProfileTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->sigtranSctpProfileTable_sets == 0)
			revert_sigtranSctpProfileTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			break;
		StorageTmp->sigtranSctpProfileRtoMin = StorageOld->sigtranSctpProfileRtoMin;
		if (--StorageTmp->sigtranSctpProfileTable_rsvs == 0)
			sigtranSctpProfileTable_destroy(&StorageTmp->sigtranSctpProfileTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_sigtranSctpProfileRtoInitial(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_sigtranSctpProfileRtoInitial(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct sigtranSctpProfileTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("sigtranMIB", "write_sigtranSctpProfileRtoInitial entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(sigtranSctpProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->sigtranSctpProfileStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileRtoInitial: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileRtoInitial not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileRtoInitial: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 300 */
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileRtoInitial: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			if (StorageTmp->sigtranSctpProfileTable_rsvs == 0)
				if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old = sigtranSctpProfileTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->sigtranSctpProfileTable_rsvs++;
		StorageTmp->sigtranSctpProfileRtoInitial = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->sigtranSctpProfileTable_tsts == 0)
				if ((ret = check_sigtranSctpProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->sigtranSctpProfileTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->sigtranSctpProfileRtoInitial for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->sigtranSctpProfileTable_sets == 0)
				if ((ret = update_sigtranSctpProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->sigtranSctpProfileTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			sigtranSctpProfileTable_destroy(&StorageTmp->sigtranSctpProfileTable_old);
			StorageTmp->sigtranSctpProfileTable_rsvs = 0;
			StorageTmp->sigtranSctpProfileTable_tsts = 0;
			StorageTmp->sigtranSctpProfileTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->sigtranSctpProfileTable_sets == 0)
			revert_sigtranSctpProfileTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			break;
		StorageTmp->sigtranSctpProfileRtoInitial = StorageOld->sigtranSctpProfileRtoInitial;
		if (--StorageTmp->sigtranSctpProfileTable_rsvs == 0)
			sigtranSctpProfileTable_destroy(&StorageTmp->sigtranSctpProfileTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_sigtranSctpProfileRtoMax(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_sigtranSctpProfileRtoMax(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct sigtranSctpProfileTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("sigtranMIB", "write_sigtranSctpProfileRtoMax entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(sigtranSctpProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->sigtranSctpProfileStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileRtoMax: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileRtoMax not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileRtoMax: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 6000 */
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileRtoMax: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			if (StorageTmp->sigtranSctpProfileTable_rsvs == 0)
				if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old = sigtranSctpProfileTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->sigtranSctpProfileTable_rsvs++;
		StorageTmp->sigtranSctpProfileRtoMax = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->sigtranSctpProfileTable_tsts == 0)
				if ((ret = check_sigtranSctpProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->sigtranSctpProfileTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->sigtranSctpProfileRtoMax for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->sigtranSctpProfileTable_sets == 0)
				if ((ret = update_sigtranSctpProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->sigtranSctpProfileTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			sigtranSctpProfileTable_destroy(&StorageTmp->sigtranSctpProfileTable_old);
			StorageTmp->sigtranSctpProfileTable_rsvs = 0;
			StorageTmp->sigtranSctpProfileTable_tsts = 0;
			StorageTmp->sigtranSctpProfileTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->sigtranSctpProfileTable_sets == 0)
			revert_sigtranSctpProfileTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			break;
		StorageTmp->sigtranSctpProfileRtoMax = StorageOld->sigtranSctpProfileRtoMax;
		if (--StorageTmp->sigtranSctpProfileTable_rsvs == 0)
			sigtranSctpProfileTable_destroy(&StorageTmp->sigtranSctpProfileTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_sigtranSctpProfilePathMaxRetrans(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_sigtranSctpProfilePathMaxRetrans(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct sigtranSctpProfileTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("sigtranMIB", "write_sigtranSctpProfilePathMaxRetrans entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(sigtranSctpProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->sigtranSctpProfileStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfilePathMaxRetrans: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfilePathMaxRetrans not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfilePathMaxRetrans: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 5 */
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			if (StorageTmp->sigtranSctpProfileTable_rsvs == 0)
				if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old = sigtranSctpProfileTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->sigtranSctpProfileTable_rsvs++;
		StorageTmp->sigtranSctpProfilePathMaxRetrans = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->sigtranSctpProfileTable_tsts == 0)
				if ((ret = check_sigtranSctpProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->sigtranSctpProfileTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->sigtranSctpProfilePathMaxRetrans for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->sigtranSctpProfileTable_sets == 0)
				if ((ret = update_sigtranSctpProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->sigtranSctpProfileTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			sigtranSctpProfileTable_destroy(&StorageTmp->sigtranSctpProfileTable_old);
			StorageTmp->sigtranSctpProfileTable_rsvs = 0;
			StorageTmp->sigtranSctpProfileTable_tsts = 0;
			StorageTmp->sigtranSctpProfileTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->sigtranSctpProfileTable_sets == 0)
			revert_sigtranSctpProfileTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			break;
		StorageTmp->sigtranSctpProfilePathMaxRetrans = StorageOld->sigtranSctpProfilePathMaxRetrans;
		if (--StorageTmp->sigtranSctpProfileTable_rsvs == 0)
			sigtranSctpProfileTable_destroy(&StorageTmp->sigtranSctpProfileTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_sigtranSctpProfileHeartbeatItvl(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_sigtranSctpProfileHeartbeatItvl(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct sigtranSctpProfileTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("sigtranMIB", "write_sigtranSctpProfileHeartbeatItvl entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(sigtranSctpProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->sigtranSctpProfileStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileHeartbeatItvl: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileHeartbeatItvl not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileHeartbeatItvl: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 6000 */
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileHeartbeatItvl: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			if (StorageTmp->sigtranSctpProfileTable_rsvs == 0)
				if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old = sigtranSctpProfileTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->sigtranSctpProfileTable_rsvs++;
		StorageTmp->sigtranSctpProfileHeartbeatItvl = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->sigtranSctpProfileTable_tsts == 0)
				if ((ret = check_sigtranSctpProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->sigtranSctpProfileTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->sigtranSctpProfileHeartbeatItvl for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->sigtranSctpProfileTable_sets == 0)
				if ((ret = update_sigtranSctpProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->sigtranSctpProfileTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
			sigtranSctpProfileTable_destroy(&StorageTmp->sigtranSctpProfileTable_old);
			StorageTmp->sigtranSctpProfileTable_rsvs = 0;
			StorageTmp->sigtranSctpProfileTable_tsts = 0;
			StorageTmp->sigtranSctpProfileTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->sigtranSctpProfileTable_sets == 0)
			revert_sigtranSctpProfileTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
			break;
		StorageTmp->sigtranSctpProfileHeartbeatItvl = StorageOld->sigtranSctpProfileHeartbeatItvl;
		if (--StorageTmp->sigtranSctpProfileTable_rsvs == 0)
			sigtranSctpProfileTable_destroy(&StorageTmp->sigtranSctpProfileTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_sigtranSctpProfileTable_row(struct sigtranSctpProfileTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_sigtranSctpProfileTable_row(struct sigtranSctpProfileTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_sigtranSctpProfileTable_row(struct sigtranSctpProfileTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_sigtranSctpProfileTable_row(struct sigtranSctpProfileTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_sigtranSctpProfileStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_sigtranSctpProfileStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct sigtranSctpProfileTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct sigtranSctpProfileTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 15;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("sigtranMIB", "write_sigtranSctpProfileStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(sigtranSctpProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to sigtranSctpProfileStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* sigtranSctpProfileIndex */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[15]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* sigtranSctpProfileIndex */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index sigtranSctpProfileIndex: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = sigtranSctpProfileTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->sigtranSctpProfileTable_rsvs = 1;
			vp = vars;
			StorageNew->sigtranSctpProfileIndex = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&sigtranSctpProfileTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->sigtranSctpProfileStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->sigtranSctpProfileTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
				if (StorageTmp->sigtranSctpProfileTable_rsvs == 0)
					if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old = sigtranSctpProfileTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->sigtranSctpProfileTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->sigtranSctpProfileTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				sigtranSctpProfileTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_sigtranSctpProfileTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* check that activation is possible */
			if (StorageTmp->sigtranSctpProfileStatus != RS_ACTIVE)
				if ((ret = can_act_sigtranSctpProfileTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->sigtranSctpProfileStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_sigtranSctpProfileTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->sigtranSctpProfileStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_sigtranSctpProfileTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->sigtranSctpProfileStatus for you to use, and you have just been asked to do something with it.  Note that anything done here must be
		   reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_sigtranSctpProfileTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->sigtranSctpProfileStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_sigtranSctpProfileTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->sigtranSctpProfileStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_sigtranSctpProfileTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destruction to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_sigtranSctpProfileTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->sigtranSctpProfileStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->sigtranSctpProfileStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->sigtranSctpProfileStatus = set_value;
			if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) != NULL) {
				sigtranSctpProfileTable_destroy(&StorageTmp->sigtranSctpProfileTable_old);
				StorageTmp->sigtranSctpProfileTable_rsvs = 0;
				StorageTmp->sigtranSctpProfileTable_tsts = 0;
				StorageTmp->sigtranSctpProfileTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			sigtranSctpProfileTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_sigtranSctpProfileTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->sigtranSctpProfileStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_sigtranSctpProfileTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->sigtranSctpProfileStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_sigtranSctpProfileTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				sigtranSctpProfileTable_del(StorageNew);
				sigtranSctpProfileTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->sigtranSctpProfileTable_old) == NULL)
				break;
			if (--StorageTmp->sigtranSctpProfileTable_rsvs == 0)
				sigtranSctpProfileTable_destroy(&StorageTmp->sigtranSctpProfileTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				sigtranSctpProfileTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn void sigtranMIB_loop_handler(int dummy)
 * @param sig signal number
 * @brief handle event loop interation.
 *
 * This function is registered so that, when operating as a module, snmpd will call it one per event
 * loop interation.  This function is called before the next requst is processed and after the
 * previous request is processed.  Two things are done here:  1) The file descriptor that is used to
 * synchronize the agent with (pseudo-)device drivers is closed.  (Another approach, instead of
 * closing each time, would be to restart a timer each time that a request is made (loop is
 * performed) and if it expires, close the file descriptor).  2) The request number is incremented.
 * Although a request is not generated for each loop of the snmp event loop, it is true that a new
 * request cannot be generated without performing a loop.  Therefore, the sa_request is not the
 * request number but it is a temporally unique identifier for a request.
 */
void
sigtranMIB_loop_handler(int sig)
{
	DEBUGMSGTL(("sigtranMIB", "sigtranMIB_loop_handler: executing loop handler...  "));
	/* close files after each request */
	if (sa_fclose) {
		if (my_fd >= 0) {
			close(my_fd);
			my_fd = -1;
		}
	}
#if defined MASTER
	/* prepare for next request */
	sa_request++;
#endif				/* defined MASTER */
	if (external_signal_scheduled[sig] == 0)
		external_signal_scheduled[sig]--;
	if (sigtranMIBold_signal_handler != NULL)
		(*sigtranMIBold_signal_handler) (sig);
	DEBUGMSGTL(("sigtranMIB", "done.\n"));
}

/**
 * @fn void sigtranMIB_fd_handler(int fd, void *dummy)
 * @param fd file descriptor to read.
 * @param dummy client data passed to registration function (always NULL).
 * @brief handle read event on file descriptor.
 *
 * This read file descriptor handler is normally used for (pseudo-)device drivers that generate
 * statistical collection interval events, alarm events, or other operational measurement events, by
 * placing a message on the read queue of the "event handling" Stream.  Normally this routine
 * would adjust counts in some table or scalars, generate SNMP traps representing on-occurence
 * events, first and interval events, and alarm indications.
 */
void
sigtranMIB_fd_handler(int fd, void *dummy)
{
	DEBUGMSGTL(("sigtranMIB", "sigtranMIB_fd_handler: executing fd handler...  "));
	/* XXX: place actions to handle my_fd here... */
	DEBUGMSGTL(("sigtranMIB", "done.\n"));
	return;
}
