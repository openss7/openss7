/*****************************************************************************

 @(#) src/snmp/megacoRingingMIB.c

 -----------------------------------------------------------------------------

 Copyright (c) 2008-2019  Monavacon Limited <http://www.monavacon.com/>
 Copyright (c) 2001-2008  OpenSS7 Corporation <http://www.openss7.com/>
 Copyright (c) 1997-2001  Brian F. G. Bidulock <bidulock@openss7.org>

 All Rights Reserved.

 This program is free software: you can redistribute it and/or modify it under
 the terms of the GNU Affero General Public License as published by the Free
 Software Foundation, version 3 of the license.

 This program is distributed in the hope that it will be useful, but WITHOUT
 ANY WARRANTY; without even the implied warranty of MERCHANTABILITY or FITNESS
 FOR A PARTICULAR PURPOSE.  See the GNU Affero General Public License for more
 details.

 You should have received a copy of the GNU Affero General Public License
 along with this program.  If not, see <http://www.gnu.org/licenses/>, or
 write to the Free Software Foundation, Inc., 675 Mass Ave, Cambridge, MA
 02139, USA.

 -----------------------------------------------------------------------------

 U.S. GOVERNMENT RESTRICTED RIGHTS.  If you are licensing this Software on
 behalf of the U.S. Government ("Government"), the following provisions apply
 to you.  If the Software is supplied by the Department of Defense ("DoD"), it
 is classified as "Commercial Computer Software" under paragraph 252.227-7014
 of the DoD Supplement to the Federal Acquisition Regulations ("DFARS") (or any
 successor regulations) and the Government is acquiring only the license rights
 granted herein (the license rights customarily provided to non-Government
 users).  If the Software is supplied to any unit or agency of the Government
 other than DoD, it is classified as "Restricted Computer Software" and the
 Government's rights in the Software are defined in paragraph 52.227-19 of the
 Federal Acquisition Regulations ("FAR") (or any successor regulations) or, in
 the cases of NASA, in paragraph 18.52.227-86 of the NASA Supplement to the FAR
 (or any successor regulations).

 -----------------------------------------------------------------------------

 Commercial licensing and support of this software is available from OpenSS7
 Corporation at a fee.  See http://www.openss7.com/

 *****************************************************************************/

static char const ident[] __attribute__((unused)) = "src/snmp/megacoRingingMIB.c (" PACKAGE_ENVR ") " PACKAGE_DATE;

/* This file was generated by mib2c and is intended for use as
   a mib module for the ucd-snmp snmpd agent. */
#include <ucd-snmp/ucd-snmp-config.h>
#include <ucd-snmp/ucd-snmp-includes.h>
#include <ucd-snmp/ucd-snmp-agent-includes.h>
#include <ucd-snmp/agent_trap.h>
#include <ucd-snmp/callback.h>
#include <ucd-snmp/snmp-tc.h>
#include <ucd-snmp/default_store.h>
#include <ucd-snmp/snmp_alarm.h>
/* The following header files are mangled in most recent net-snmp releases so
 * the versions from UCD-SNMP 4.2.5 are included here.  */
#if defined HAVE_LIBNETSNMP
#else				/* defined HAVE_LIBNETSNMP */
#endif				/* defined HAVE_LIBNETSNMP */
/* These are messed up on both. */
#include "ds_agent.h"
#ifdef HAVE_UCD_SNMP_UTIL_FUNCS_H
#include <ucd-snmp/util_funcs.h>
/* Many recent net-snmp UCD compatible headers do not declare header_generic. */
int header_generic(struct variable *, oid *, size_t *, int, size_t *, WriteMethod **);
#else				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#include "util_funcs.h"
#endif				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#ifdef HAVE_UCD_SNMP_HEADER_COMPLEX_H
#include <ucd-snmp/header_complex.h>
#else				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
#include "header_complex.h"
#endif				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
/* This one is the other way around: it is just fine for net-snmp, but
 * ucd-snmp does not provide the header file at all.  */
#ifdef HAVE_UCD_SNMP_MIB_MODULES_H
#include <ucd-snmp/mib_modules.h>
#else				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#ifdef HAVE_NET_SNMP_AGENT_MIB_MODULES_H
#include <net-snmp/agent/mib_modules.h>
#else				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#include "mib_modules.h"
#endif				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#endif				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#ifdef HAVE_NETSNMP_MEMDUP
#include "ucd_memdup.h"
#endif
#include <stdint.h>
#include <signal.h>
#include <sys/stat.h>		/* for struct stat, fstat() */
#include <sys/types.h>
#include <unistd.h>
#include <stdlib.h>
#include <pwd.h>		/* for getpwuid() getpwnam() */
#include <grp.h>		/* for getgrgid() getgrnam() */
#include <libgen.h>		/* for basename() */
#include <fcntl.h>		/* for O_CREAT */
#include <errno.h>
#include <string.h>
#ifdef _GNU_SOURCE
#include <getopt.h>
#endif
#include "megacoRingingMIB.h"
#define MASTER 1
#define MY_FACILITY(__pri)	(LOG_DAEMON|(__pri))
#if defined MODULE
#if defined MASTER
const char sa_program[] = "megacoRingingMIB";
int sa_fclose = 1;			/* default close files between requests */
int sa_changed = 1;			/* indication to reread MIB configuration */
int sa_stats_refresh = 1;		/* indications that statistics, the mib or its tables need to be refreshed */
int sa_request = 1;			/* request number for per-request actions */
int sa_dump = 0;			/* default packet dump */
int sa_debug = 0;			/* default no debug */
#endif				/* defined MASTER */
#endif				/* defined MODULE */
static int my_fd = -1;			/* file descriptor for this MIB's use */
static int my_readfd = -1;		/* file descriptor for autonomnous events */
volatile int megacoRingingMIB_refresh = 1;
volatile int mgcoRingingPatternTable_refresh = 1;
volatile int mgcoRingingCadenceGroupTable_refresh = 1;

/*
 * megacoRingingMIB_variables_oid: object identifier for megacoRingingMIB
 * This is the top level oid that we want to register under.  This is essentially a prefix, with the
 * suffix appearing in the variable below.
 */
oid megacoRingingMIB_variables_oid[7] = { 1, 3, 6, 1, 2, 1, 1002 };
oid mgcoRingingPatternTable_variables_oid[11] = { 1, 3, 6, 1, 2, 1, 1002, 1, 1, 1, 1 };
oid mgcoRingingCadenceGroupTable_variables_oid[11] = { 1, 3, 6, 1, 2, 1, 1002, 1, 1, 3, 1 };

/*
 * Oids for use in notifications defined in this MIB.
 */

/*
 * Oids accessible only for notify defined in this MIB.
 */

/*
 * Other oids defined in this MIB.
 */
oid mgcoRingingProperty_oid[10] = { 1, 3, 6, 1, 2, 1, 1002, 1, 2, 1 };
oid mgcoRingingCompliance_oid[10] = { 1, 3, 6, 1, 2, 1, 1002, 3, 1, 1 };
oid mgcoRingingPatternSetGrp_oid[10] = { 1, 3, 6, 1, 2, 1, 1002, 3, 2, 1 };
oid mgcoRingingCadenceGroupGrp_oid[10] = { 1, 3, 6, 1, 2, 1, 1002, 3, 2, 2 };
static oid zeroDotZero_oid[2] = { 0, 0 };
static oid snmpTrapOID_oid[11] = { 1, 3, 6, 1, 6, 3, 1, 1, 4, 1, 0 };

/*
 * variable7 megacoRingingMIB_variables: tree for megacoRingingMIB
 * This variable defines function callbacks and type return information for the megacoRingingMIB mib section
 */
struct variable7 megacoRingingMIB_variables[] = {
	/* magic number, variable type, ro/rw, callback fn, L, oidsuffix */
#define   MGCORINGINGCADENCEGROUPID  1
	{ (u_char) MGCORINGINGCADENCEGROUPID, ASN_UNSIGNED, RWRITE, var_mgcoRingingPatternTable, 5, { 1, 1, 1, 1, 2} },
#define   MGCORINGINGPATTERNNAME  2
	{ (u_char) MGCORINGINGPATTERNNAME, ASN_OCTET_STR, RWRITE, var_mgcoRingingPatternTable, 5, { 1, 1, 1, 1, 3} },
#define   MGCORINGINGPATTERNROWSTATUS  3
	{ (u_char) MGCORINGINGPATTERNROWSTATUS, ASN_INTEGER, RWRITE, var_mgcoRingingPatternTable, 5, { 1, 1, 1, 1, 4} },
#define   MGCORINGINGCADENCENEXTGROUPID  4
	{ (u_char) MGCORINGINGCADENCENEXTGROUPID, ASN_INTEGER, RWRITE, var_megacoRingingMIB, 3, { 1, 1, 2} },
#define   MGCORINGINGCADENCEGROUPNAME  6
	{ (u_char) MGCORINGINGCADENCEGROUPNAME, ASN_OCTET_STR, RWRITE, var_mgcoRingingCadenceGroupTable, 5, { 1, 1, 3, 1, 2} },
#define   MGCORINGINGCADENCEGROUPREF  7
	{ (u_char) MGCORINGINGCADENCEGROUPREF, ASN_INTEGER, RWRITE, var_mgcoRingingCadenceGroupTable, 5, { 1, 1, 3, 1, 3} },
#define   MGCORINGINGCADENCEGROUPRINGINGMINDURATION  8
	{ (u_char) MGCORINGINGCADENCEGROUPRINGINGMINDURATION, ASN_INTEGER, RWRITE, var_mgcoRingingCadenceGroupTable, 5, { 1, 1, 3, 1, 4} },
#define   MGCORINGINGCADENCEGROUPRINGINGNOMDURATION  9
	{ (u_char) MGCORINGINGCADENCEGROUPRINGINGNOMDURATION, ASN_INTEGER, RWRITE, var_mgcoRingingCadenceGroupTable, 5, { 1, 1, 3, 1, 5} },
#define   MGCORINGINGCADENCEGROUPRINGINGMAXDURATION  10
	{ (u_char) MGCORINGINGCADENCEGROUPRINGINGMAXDURATION, ASN_INTEGER, RWRITE, var_mgcoRingingCadenceGroupTable, 5, { 1, 1, 3, 1, 6} },
#define   MGCORINGINGCADENCEGROUPSILENTMINDURATION  11
	{ (u_char) MGCORINGINGCADENCEGROUPSILENTMINDURATION, ASN_INTEGER, RWRITE, var_mgcoRingingCadenceGroupTable, 5, { 1, 1, 3, 1, 7} },
#define   MGCORINGINGCADENCEGROUPSILENTNOMDURATION  12
	{ (u_char) MGCORINGINGCADENCEGROUPSILENTNOMDURATION, ASN_INTEGER, RWRITE, var_mgcoRingingCadenceGroupTable, 5, { 1, 1, 3, 1, 8} },
#define   MGCORINGINGCADENCEGROUPSILENTMAXDURATION  13
	{ (u_char) MGCORINGINGCADENCEGROUPSILENTMAXDURATION, ASN_INTEGER, RWRITE, var_mgcoRingingCadenceGroupTable, 5, { 1, 1, 3, 1, 9} },
#define   MGCORINGINGCADENCEGROUPROWSTATUS  14
	{ (u_char) MGCORINGINGCADENCEGROUPROWSTATUS, ASN_INTEGER, RWRITE, var_mgcoRingingCadenceGroupTable, 5, { 1, 1, 3, 1, 10} },
};

/* (L = length of the oidsuffix) */
struct megacoRingingMIB_data *megacoRingingMIBStorage = NULL;

/* global storage of our data, saved in and configured by header_complex() */
struct header_complex_index *mgcoRingingPatternTableStorage = NULL;
struct header_complex_index *mgcoRingingCadenceGroupTableStorage = NULL;

void (*megacoRingingMIBold_signal_handler)(int) = NULL;	/* save old signal handler just in case */
void megacoRingingMIB_loop_handler(int);
void megacoRingingMIB_fd_handler(int, void *);

/**
 * @fn void init_megacoRingingMIB(void)
 * @brief megacoRingingMIB initialization routine.
 *
 * This is called when the agent starts up.  At a minimum, registration of the MIB variables
 * structure (megacoRingingMIB_variables) should take place here.  By default the function also
 * registers the configuration handler and configuration store callbacks.
 *
 * Additional registrations that may be considered here are calls to regsiter_readfd(),
 * register_writefd() and register_exceptfd() for hooking into the snmpd event loop, but only when
 * used as a loadable module.  By default this function establishes a single file descriptor to
 * read, or upon which to handle exceptions.  Note that the snmpd only supports a maximum of 32
 * extneral file descriptors, so these should be used sparingly.
 *
 * When running as a loadable module, it is also necessary to hook into the snmpd event loop so that
 * the current request number can be deteremined.  This is accomplished by using a trick of the
 * external_signal_scheduled and external_signal_handler mechanism which is called on each event
 * loop when external_signal_scheduled is non-zero.  This is used to increment the sa_request value
 * on each snmpd event loop interation so that calls to MIB tree functions can determine whether
 * they belong to a fresh request or not (primarily for cacheing and possibly to clean up non-polled
 * file descriptors).
 */
void
init_megacoRingingMIB(void)
{
	(void) my_fd;
	(void) zeroDotZero_oid;
	(void) snmpTrapOID_oid;
	DEBUGMSGTL(("megacoRingingMIB", "init_megacoRingingMIB: initializing...  "));
	/* register ourselves with the agent to handle our mib tree */
	REGISTER_MIB("megacoRingingMIB", megacoRingingMIB_variables, variable7, megacoRingingMIB_variables_oid);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_SHUTDOWN, term_megacoRingingMIB, NULL);
	/* register our config handler(s) to deal with registrations */
	snmpd_register_config_handler("megacoRingingMIB", parse_megacoRingingMIB, NULL, "HELP STRING");
	snmpd_register_config_handler("mgcoRingingPatternTable", parse_mgcoRingingPatternTable, NULL, "HELP STRING");
	snmpd_register_config_handler("mgcoRingingCadenceGroupTable", parse_mgcoRingingCadenceGroupTable, NULL, "HELP STRING");

	/* we need to be called back later to store our data */
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_megacoRingingMIB, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_mgcoRingingPatternTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_mgcoRingingCadenceGroupTable, NULL);

	/* place any other initialization junk you need here */
	if (my_readfd >= 0) {
		register_readfd(my_readfd, megacoRingingMIB_fd_handler, (void *) 0);
		register_exceptfd(my_readfd, megacoRingingMIB_fd_handler, (void *) 1);
	}
	megacoRingingMIBold_signal_handler = external_signal_handler[SIGCHLD];
	external_signal_handler[SIGCHLD] = &megacoRingingMIB_loop_handler;
	external_signal_scheduled[SIGCHLD] = 1;
	DEBUGMSGTL(("megacoRingingMIB", "done.\n"));
}

/**
 * @fn void deinit_megacoRingingMIB(void)
 * @brief deinitialization routine.
 *
 * This is called before the agent is unloaded.  At a minimum, deregistration of the MIB variables
 * structure (megacoRingingMIB_variables) should take place here.  By default, the function also
 * deregisters the the configuration file handlers for the MIB variables and table rows.
 *
 * Additional deregistrations that may be required here are calls to unregister_readfd(),
 * unregister_writefd() and unregsiter_exceptfd() for unhooking from the snmpd event loop, but only
 * when used as a loadable module.  By default if a read file descriptor exists, it is unregistered.
 */
void
deinit_megacoRingingMIB(void)
{
	DEBUGMSGTL(("megacoRingingMIB", "deinit_megacoRingingMIB: deinitializating...  "));
	external_signal_handler[SIGCHLD] = megacoRingingMIBold_signal_handler;
	if (my_readfd >= 0) {
		unregister_exceptfd(my_readfd);
		unregister_readfd(my_readfd);
		close(my_readfd);
		my_readfd = -1;
	}
	unregister_mib(megacoRingingMIB_variables_oid, sizeof(megacoRingingMIB_variables_oid) / sizeof(oid));
	snmpd_unregister_config_handler("megacoRingingMIB");
	snmpd_unregister_config_handler("mgcoRingingPatternTable");
	snmpd_unregister_config_handler("mgcoRingingCadenceGroupTable");

	/* place any other de-initialization junk you need here */
	DEBUGMSGTL(("megacoRingingMIB", "done.\n"));
}

int
term_megacoRingingMIB(int majorID, int minorID, void *serverarg, void *clientarg)
{
	DEBUGMSGTL(("megacoRingingMIB", "term_megacoRingingMIB: terminating...  "));
	deinit_megacoRingingMIB();
	DEBUGMSGTL(("megacoRingingMIB", "done.\n"));
	return 0;
}

/**
 * @fn struct megacoRingingMIB_data *megacoRingingMIB_create(void)
 * @brief create a fresh data structure representing scalars in megacoRingingMIB.
 *
 * Creates a new megacoRingingMIB_data structure by allocating dynamic memory for the structure and
 * initializing the default values of scalars in megacoRingingMIB.
 */
struct megacoRingingMIB_data *
megacoRingingMIB_create(void)
{
	struct megacoRingingMIB_data *StorageNew = SNMP_MALLOC_STRUCT(megacoRingingMIB_data);

	DEBUGMSGTL(("megacoRingingMIB", "megacoRingingMIB_create: creating scalars...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default scalar values here into StorageNew */
		StorageNew->mgcoRingingCadenceNextGroupId = 0;

	}
      done:
	DEBUGMSGTL(("megacoRingingMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	megacoRingingMIB_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct megacoRingingMIB_data *megacoRingingMIB_duplicate(struct megacoRingingMIB_data *thedata)
 * @param thedata the mib structure to duplicate
 * @brief duplicate a mib structure for the mib
 *
 * Duplicates the specified mib structure @param thedata and returns a pointer to the newly
 * allocated mib structure on success, or NULL on failure.
 */
struct megacoRingingMIB_data *
megacoRingingMIB_duplicate(struct megacoRingingMIB_data *thedata)
{
	struct megacoRingingMIB_data *StorageNew = SNMP_MALLOC_STRUCT(megacoRingingMIB_data);

	DEBUGMSGTL(("megacoRingingMIB", "megacoRingingMIB_duplicate: duplicating mib... "));
	if (StorageNew != NULL) {
		StorageNew->mgcoRingingCadenceNextGroupId = thedata->mgcoRingingCadenceNextGroupId;
	}
      done:
	DEBUGMSGTL(("megacoRingingMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	megacoRingingMIB_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int megacoRingingMIB_destroy(struct megacoRingingMIB_data **thedata)
 * @param thedata pointer to the data structure in megacoRingingMIB.
 * @brief delete a scalars structure from megacoRingingMIB.
 *
 * Frees scalars that were previously removed from megacoRingingMIB.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
megacoRingingMIB_destroy(struct megacoRingingMIB_data **thedata)
{
	struct megacoRingingMIB_data *StorageDel;

	DEBUGMSGTL(("megacoRingingMIB", "megacoRingingMIB_destroy: deleting scalars...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("megacoRingingMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int megacoRingingMIB_add(struct megacoRingingMIB_data *thedata)
 * @param thedata the structure representing megacoRingingMIB scalars.
 * @brief adds node to the megacoRingingMIB scalar data set.
 *
 * Adds a scalar structure to the megacoRingingMIB data set.  Note that this function is necessary even
 * when the scalar values are not peristent.
 */
int
megacoRingingMIB_add(struct megacoRingingMIB_data *thedata)
{
	DEBUGMSGTL(("megacoRingingMIB", "megacoRingingMIB_add: adding data...  "));
	if (thedata)
		megacoRingingMIBStorage = thedata;
	DEBUGMSGTL(("megacoRingingMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_megacoRingingMIB(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for megacoRingingMIB entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case megacoRingingMIB).  This routine is invoked by
 * UCD-SNMP to read the values of scalars in the MIB from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the MIB.  If there are no configured entries
 * in the configuration MIB, this function will simply not be called.
 */
void
parse_megacoRingingMIB(const char *token, char *line)
{
	size_t tmpsize;
	struct megacoRingingMIB_data *StorageTmp = megacoRingingMIB_create();

	DEBUGMSGTL(("megacoRingingMIB", "parse_megacoRingingMIB: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual scalars that are not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->mgcoRingingCadenceNextGroupId, &tmpsize);
	megacoRingingMIB_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("megacoRingingMIB", "done.\n"));
}

/*
 * store_megacoRingingMIB(): stores .conf file entries needed to configure the mib.
 */
int
store_megacoRingingMIB(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct megacoRingingMIB_data *StorageTmp;

	DEBUGMSGTL(("megacoRingingMIB", "store_megacoRingingMIB: storing data...  "));
	refresh_megacoRingingMIB(1);
	if ((StorageTmp = megacoRingingMIBStorage) == NULL) {
		DEBUGMSGTL(("megacoRingingMIB", "error.\n"));
		return SNMPERR_GENERR;
	}
	(void) tmpsize;
	/* XXX: comment entire section if no scalars are persistent */
	{
		memset(line, 0, sizeof(line));
		strcat(line, "megacoRingingMIB ");
		cptr = line + strlen(line);
		(void) cptr;
		/* XXX: remove individual scalars that are not persistent */
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->mgcoRingingCadenceNextGroupId, &tmpsize);
		snmpd_store_config(line);
	}
	DEBUGMSGTL(("megacoRingingMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int check_megacoRingingMIB(struct megacoRingingMIB_data *StorageTmp, struct megacoRingingMIB_data *StorageOld)
 * @param StorageTmp the data as updated
 * @param StorageOld the data previous to update
 *
 * This function is used by mibs.  It is used to check, all scalars at a time, the varbinds
 * belonging to the mib.  This function is called for the first varbind in a mib at the beginning of
 * the ACTION phase.  The COMMIT phase does not ensue unless this check passes.  This function can
 * return SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before
 * the varbinds on the mib were applied; the values in StorageTmp are the new values.  The function
 * is permitted to change the values in StorageTmp to correct them; however, preferences should be
 * made for setting values that were not in the varbinds.
 */
int
check_megacoRingingMIB(struct megacoRingingMIB_data *StorageTmp, struct megacoRingingMIB_data *StorageOld)
{
	/* XXX: provide code to check the scalars for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_megacoRingingMIB(struct megacoRingingMIB_data *StorageTmp, struct megacoRingingMIB_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase)
 *
 * This function is used by mibs.  It is used to update, all scalars at a time, the varbinds
 * belonging to the mib.  This function is called for the first varbind in a mib at the beginning of
 * the COMMIT phase.  The start of the ACTION phase performs a consistency check on the mib before
 * allowing the request to proceed to the COMMIT phase.  The COMMIT phase then arrives here with
 * consistency already checked (see check_megacoRingingMIB()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the mib were applied: the values in StorageTmp are the new values.
 */
int
update_megacoRingingMIB(struct megacoRingingMIB_data *StorageTmp, struct megacoRingingMIB_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	megacoRingingMIB_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn revert_megacoRingingMIB(struct 
 * @fn void revert_megacoRingingMIB(struct megacoRingingMIB_data *StorageTmp, struct megacoRingingMIB_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase)
 */
void
revert_megacoRingingMIB(struct megacoRingingMIB_data *StorageTmp, struct megacoRingingMIB_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_megacoRingingMIB(StorageOld, NULL);
}

/**
 * @fn void refresh_megacoRingingMIB(int force)
 * @param force forced refresh when non-zero.
 * @brief refresh the scalar values of megacoRingingMIB.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a scalar has been requested).
 */
void
refresh_megacoRingingMIB(int force)
{
	if (megacoRingingMIBStorage == NULL) {
		struct megacoRingingMIB_data *StorageNew;

		if ((StorageNew = megacoRingingMIB_create()) == NULL)
			return;
		megacoRingingMIBStorage = StorageNew;
		megacoRingingMIB_refresh = 1;
	}
	if (!force && megacoRingingMIB_refresh == 0)
		return;
	DEBUGMSGTL(("megacoRingingMIB", "refresh_megacoRingingMIB: refreshing...  "));
	/* XXX: Update scalars as required here... */
	megacoRingingMIB_refresh = 0;
	DEBUGMSGTL(("megacoRingingMIB", "done.\n"));
}

/**
 * @fn u_char * var_megacoRingingMIB(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @param vp a pointer to the entry in the variables table for the requested variable.
 * @param name the object identifier for which to find.
 * @param length the length of the object identifier.
 * @param exact whether the name is exact.
 * @param var_len a pointer to the length of the representation of the object.
 * @param write_method a pointer to a write method for the object.
 * @brief locate variables in megacoRingingMIB.
 *
 * This function returns a pointer to a memory area that is static across the request that contains
 * the UCD-SNMP representation of the scalar (so that it may be used to read from for a GET,
 * GET-NEXT or GET-BULK request).  This returned pointer may be NULL, in which case the function is
 * telling UCD-SNMP that the scalar does not exist for reading; however, if write_method is
 * overwritten with a non-NULL value, the function is telling UCD-SNMP that the scalar exists for
 * writing.  Write-only objects can be effected in this way.
 */
u_char *
var_megacoRingingMIB(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct megacoRingingMIB_data *StorageTmp;
	u_char *rval;

	DEBUGMSGTL(("megacoRingingMIB", "var_megacoRingingMIB: lookup up varbind...  "));
	if (header_generic(vp, name, length, exact, var_len, write_method) == MATCH_FAILED)
		return NULL;
	/* Refresh the MIB values if required. */
	refresh_megacoRingingMIB(0);
	if ((StorageTmp = megacoRingingMIBStorage) == NULL) {
		DEBUGMSGTL(("megacoRingingMIB", "no datastructure.\n"));
		return NULL;
	}
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) MGCORINGINGCADENCENEXTGROUPID:	/* ReadWrite */
		*write_method = write_mgcoRingingCadenceNextGroupId;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->mgcoRingingCadenceNextGroupId);
		rval = (u_char *) &StorageTmp->mgcoRingingCadenceNextGroupId;
		break;
	default:
		ERROR_MSG("");
	}
	if (rval)
		DEBUGMSGTL(("megacoRingingMIB", "found.\n"));
	else
		DEBUGMSGTL(("megacoRingingMIB", "not found.\n"));
	return (rval);
}

/**
 * @fn struct mgcoRingingPatternTable_data *mgcoRingingPatternTable_create(void)
 * @brief create a fresh data structure representing a new row in the mgcoRingingPatternTable table.
 *
 * Creates a new mgcoRingingPatternTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct mgcoRingingPatternTable_data *
mgcoRingingPatternTable_create(void)
{
	struct mgcoRingingPatternTable_data *StorageNew = SNMP_MALLOC_STRUCT(mgcoRingingPatternTable_data);

	DEBUGMSGTL(("megacoRingingMIB", "mgcoRingingPatternTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->mgcoRingingCadenceGroupId = 0;
		if ((StorageNew->mgcoRingingPatternName = malloc(1)) == NULL)
			goto nomem;
		StorageNew->mgcoRingingPatternNameLen = 0;
		StorageNew->mgcoRingingPatternName[StorageNew->mgcoRingingPatternNameLen] = 0;
		StorageNew->mgcoRingingPatternRowStatus = 0;
		StorageNew->mgcoRingingPatternRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("megacoRingingMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	mgcoRingingPatternTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct mgcoRingingPatternTable_data *mgcoRingingPatternTable_duplicate(struct mgcoRingingPatternTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct mgcoRingingPatternTable_data *
mgcoRingingPatternTable_duplicate(struct mgcoRingingPatternTable_data *thedata)
{
	struct mgcoRingingPatternTable_data *StorageNew = SNMP_MALLOC_STRUCT(mgcoRingingPatternTable_data);

	DEBUGMSGTL(("megacoRingingMIB", "mgcoRingingPatternTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->mgcoRingingPatternTable_id = thedata->mgcoRingingPatternTable_id;
		StorageNew->mgcoRingingPatternId = thedata->mgcoRingingPatternId;
		StorageNew->mgcoRingingCadenceGroupId = thedata->mgcoRingingCadenceGroupId;
		if (!(StorageNew->mgcoRingingPatternName = malloc(thedata->mgcoRingingPatternNameLen + 1)))
			goto destroy;
		memcpy(StorageNew->mgcoRingingPatternName, thedata->mgcoRingingPatternName, thedata->mgcoRingingPatternNameLen);
		StorageNew->mgcoRingingPatternNameLen = thedata->mgcoRingingPatternNameLen;
		StorageNew->mgcoRingingPatternName[StorageNew->mgcoRingingPatternNameLen] = 0;
		StorageNew->mgcoRingingPatternRowStatus = thedata->mgcoRingingPatternRowStatus;
	}
      done:
	DEBUGMSGTL(("megacoRingingMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	mgcoRingingPatternTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int mgcoRingingPatternTable_destroy(struct mgcoRingingPatternTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
mgcoRingingPatternTable_destroy(struct mgcoRingingPatternTable_data **thedata)
{
	struct mgcoRingingPatternTable_data *StorageDel;

	DEBUGMSGTL(("megacoRingingMIB", "mgcoRingingPatternTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->mgcoRingingPatternName);
		StorageDel->mgcoRingingPatternNameLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("megacoRingingMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int mgcoRingingPatternTable_add(struct mgcoRingingPatternTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the mgcoRingingPatternTable table data set.
 *
 * Adds a table row structure to the mgcoRingingPatternTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
mgcoRingingPatternTable_add(struct mgcoRingingPatternTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("megacoRingingMIB", "mgcoRingingPatternTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* mgcoRingingPatternId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->mgcoRingingPatternId, sizeof(thedata->mgcoRingingPatternId));
		header_complex_add_data(&mgcoRingingPatternTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("megacoRingingMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int mgcoRingingPatternTable_del(struct mgcoRingingPatternTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the mgcoRingingPatternTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
mgcoRingingPatternTable_del(struct mgcoRingingPatternTable_data *thedata)
{
	struct mgcoRingingPatternTable_data *StorageDel;

	DEBUGMSGTL(("megacoRingingMIB", "mgcoRingingPatternTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(mgcoRingingPatternTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&mgcoRingingPatternTableStorage, hciptr);
	}
	DEBUGMSGTL(("megacoRingingMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_mgcoRingingPatternTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for mgcoRingingPatternTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case mgcoRingingPatternTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_mgcoRingingPatternTable(const char *token, char *line)
{
	size_t tmpsize;
	struct mgcoRingingPatternTable_data *StorageTmp = mgcoRingingPatternTable_create();

	DEBUGMSGTL(("megacoRingingMIB", "parse_mgcoRingingPatternTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->mgcoRingingPatternId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->mgcoRingingCadenceGroupId, &tmpsize);
	SNMP_FREE(StorageTmp->mgcoRingingPatternName);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->mgcoRingingPatternName, &StorageTmp->mgcoRingingPatternNameLen);
	if (StorageTmp->mgcoRingingPatternName == NULL) {
		config_perror("invalid specification for mgcoRingingPatternName");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->mgcoRingingPatternRowStatus, &tmpsize);
	mgcoRingingPatternTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("megacoRingingMIB", "done.\n"));
}

/*
 * store_mgcoRingingPatternTable(): store configuraiton file for mgcoRingingPatternTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_mgcoRingingPatternTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct mgcoRingingPatternTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("megacoRingingMIB", "store_mgcoRingingPatternTable: storing data...  "));
	refresh_mgcoRingingPatternTable(1);
	(void) tmpsize;
	for (hcindex = mgcoRingingPatternTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct mgcoRingingPatternTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "mgcoRingingPatternTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->mgcoRingingPatternId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->mgcoRingingCadenceGroupId, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->mgcoRingingPatternName, &StorageTmp->mgcoRingingPatternNameLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->mgcoRingingPatternRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("megacoRingingMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct mgcoRingingCadenceGroupTable_data *mgcoRingingCadenceGroupTable_create(void)
 * @brief create a fresh data structure representing a new row in the mgcoRingingCadenceGroupTable table.
 *
 * Creates a new mgcoRingingCadenceGroupTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct mgcoRingingCadenceGroupTable_data *
mgcoRingingCadenceGroupTable_create(void)
{
	struct mgcoRingingCadenceGroupTable_data *StorageNew = SNMP_MALLOC_STRUCT(mgcoRingingCadenceGroupTable_data);

	DEBUGMSGTL(("megacoRingingMIB", "mgcoRingingCadenceGroupTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->mgcoRingingCadenceGroupId = 0;
		if ((StorageNew->mgcoRingingCadenceGroupName = malloc(1)) == NULL)
			goto nomem;
		StorageNew->mgcoRingingCadenceGroupNameLen = 0;
		StorageNew->mgcoRingingCadenceGroupName[StorageNew->mgcoRingingCadenceGroupNameLen] = 0;
		StorageNew->mgcoRingingCadenceGroupRef = 0;
		StorageNew->mgcoRingingCadenceGroupRingingMinDuration = 0;
		StorageNew->mgcoRingingCadenceGroupRingingNomDuration = 0;
		StorageNew->mgcoRingingCadenceGroupRingingMaxDuration = 0;
		StorageNew->mgcoRingingCadenceGroupSilentMinDuration = 0;
		StorageNew->mgcoRingingCadenceGroupSilentNomDuration = 0;
		StorageNew->mgcoRingingCadenceGroupSilentMaxDuration = 0;
		StorageNew->mgcoRingingCadenceGroupRowStatus = 0;
		StorageNew->mgcoRingingCadenceGroupRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("megacoRingingMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	mgcoRingingCadenceGroupTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct mgcoRingingCadenceGroupTable_data *mgcoRingingCadenceGroupTable_duplicate(struct mgcoRingingCadenceGroupTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct mgcoRingingCadenceGroupTable_data *
mgcoRingingCadenceGroupTable_duplicate(struct mgcoRingingCadenceGroupTable_data *thedata)
{
	struct mgcoRingingCadenceGroupTable_data *StorageNew = SNMP_MALLOC_STRUCT(mgcoRingingCadenceGroupTable_data);

	DEBUGMSGTL(("megacoRingingMIB", "mgcoRingingCadenceGroupTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->mgcoRingingCadenceGroupTable_id = thedata->mgcoRingingCadenceGroupTable_id;
		StorageNew->mgcoRingingCadenceGroupId = thedata->mgcoRingingCadenceGroupId;
		StorageNew->mgcoRingingCadenceGroupIndex = thedata->mgcoRingingCadenceGroupIndex;
		if (!(StorageNew->mgcoRingingCadenceGroupName = malloc(thedata->mgcoRingingCadenceGroupNameLen + 1)))
			goto destroy;
		memcpy(StorageNew->mgcoRingingCadenceGroupName, thedata->mgcoRingingCadenceGroupName, thedata->mgcoRingingCadenceGroupNameLen);
		StorageNew->mgcoRingingCadenceGroupNameLen = thedata->mgcoRingingCadenceGroupNameLen;
		StorageNew->mgcoRingingCadenceGroupName[StorageNew->mgcoRingingCadenceGroupNameLen] = 0;
		StorageNew->mgcoRingingCadenceGroupRef = thedata->mgcoRingingCadenceGroupRef;
		StorageNew->mgcoRingingCadenceGroupRingingMinDuration = thedata->mgcoRingingCadenceGroupRingingMinDuration;
		StorageNew->mgcoRingingCadenceGroupRingingNomDuration = thedata->mgcoRingingCadenceGroupRingingNomDuration;
		StorageNew->mgcoRingingCadenceGroupRingingMaxDuration = thedata->mgcoRingingCadenceGroupRingingMaxDuration;
		StorageNew->mgcoRingingCadenceGroupSilentMinDuration = thedata->mgcoRingingCadenceGroupSilentMinDuration;
		StorageNew->mgcoRingingCadenceGroupSilentNomDuration = thedata->mgcoRingingCadenceGroupSilentNomDuration;
		StorageNew->mgcoRingingCadenceGroupSilentMaxDuration = thedata->mgcoRingingCadenceGroupSilentMaxDuration;
		StorageNew->mgcoRingingCadenceGroupRowStatus = thedata->mgcoRingingCadenceGroupRowStatus;
	}
      done:
	DEBUGMSGTL(("megacoRingingMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	mgcoRingingCadenceGroupTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int mgcoRingingCadenceGroupTable_destroy(struct mgcoRingingCadenceGroupTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
mgcoRingingCadenceGroupTable_destroy(struct mgcoRingingCadenceGroupTable_data **thedata)
{
	struct mgcoRingingCadenceGroupTable_data *StorageDel;

	DEBUGMSGTL(("megacoRingingMIB", "mgcoRingingCadenceGroupTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->mgcoRingingCadenceGroupName);
		StorageDel->mgcoRingingCadenceGroupNameLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("megacoRingingMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int mgcoRingingCadenceGroupTable_add(struct mgcoRingingCadenceGroupTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the mgcoRingingCadenceGroupTable table data set.
 *
 * Adds a table row structure to the mgcoRingingCadenceGroupTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
mgcoRingingCadenceGroupTable_add(struct mgcoRingingCadenceGroupTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("megacoRingingMIB", "mgcoRingingCadenceGroupTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* mgcoRingingCadenceGroupId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->mgcoRingingCadenceGroupId, sizeof(thedata->mgcoRingingCadenceGroupId));
		/* mgcoRingingCadenceGroupIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->mgcoRingingCadenceGroupIndex, sizeof(thedata->mgcoRingingCadenceGroupIndex));
		header_complex_add_data(&mgcoRingingCadenceGroupTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("megacoRingingMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int mgcoRingingCadenceGroupTable_del(struct mgcoRingingCadenceGroupTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the mgcoRingingCadenceGroupTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
mgcoRingingCadenceGroupTable_del(struct mgcoRingingCadenceGroupTable_data *thedata)
{
	struct mgcoRingingCadenceGroupTable_data *StorageDel;

	DEBUGMSGTL(("megacoRingingMIB", "mgcoRingingCadenceGroupTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(mgcoRingingCadenceGroupTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&mgcoRingingCadenceGroupTableStorage, hciptr);
	}
	DEBUGMSGTL(("megacoRingingMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_mgcoRingingCadenceGroupTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for mgcoRingingCadenceGroupTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case mgcoRingingCadenceGroupTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_mgcoRingingCadenceGroupTable(const char *token, char *line)
{
	size_t tmpsize;
	struct mgcoRingingCadenceGroupTable_data *StorageTmp = mgcoRingingCadenceGroupTable_create();

	DEBUGMSGTL(("megacoRingingMIB", "parse_mgcoRingingCadenceGroupTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->mgcoRingingCadenceGroupId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->mgcoRingingCadenceGroupIndex, &tmpsize);
	SNMP_FREE(StorageTmp->mgcoRingingCadenceGroupName);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->mgcoRingingCadenceGroupName, &StorageTmp->mgcoRingingCadenceGroupNameLen);
	if (StorageTmp->mgcoRingingCadenceGroupName == NULL) {
		config_perror("invalid specification for mgcoRingingCadenceGroupName");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->mgcoRingingCadenceGroupRef, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->mgcoRingingCadenceGroupRingingMinDuration, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->mgcoRingingCadenceGroupRingingNomDuration, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->mgcoRingingCadenceGroupRingingMaxDuration, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->mgcoRingingCadenceGroupSilentMinDuration, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->mgcoRingingCadenceGroupSilentNomDuration, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->mgcoRingingCadenceGroupSilentMaxDuration, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->mgcoRingingCadenceGroupRowStatus, &tmpsize);
	mgcoRingingCadenceGroupTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("megacoRingingMIB", "done.\n"));
}

/*
 * store_mgcoRingingCadenceGroupTable(): store configuraiton file for mgcoRingingCadenceGroupTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_mgcoRingingCadenceGroupTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct mgcoRingingCadenceGroupTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("megacoRingingMIB", "store_mgcoRingingCadenceGroupTable: storing data...  "));
	refresh_mgcoRingingCadenceGroupTable(1);
	(void) tmpsize;
	for (hcindex = mgcoRingingCadenceGroupTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct mgcoRingingCadenceGroupTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "mgcoRingingCadenceGroupTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->mgcoRingingCadenceGroupId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->mgcoRingingCadenceGroupIndex, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->mgcoRingingCadenceGroupName, &StorageTmp->mgcoRingingCadenceGroupNameLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->mgcoRingingCadenceGroupRef, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->mgcoRingingCadenceGroupRingingMinDuration, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->mgcoRingingCadenceGroupRingingNomDuration, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->mgcoRingingCadenceGroupRingingMaxDuration, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->mgcoRingingCadenceGroupSilentMinDuration, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->mgcoRingingCadenceGroupSilentNomDuration, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->mgcoRingingCadenceGroupSilentMaxDuration, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->mgcoRingingCadenceGroupRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("megacoRingingMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int activate_mgcoRingingPatternTable_row(struct mgcoRingingPatternTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_mgcoRingingPatternTable_row(struct mgcoRingingPatternTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_mgcoRingingPatternTable_row(struct mgcoRingingPatternTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_mgcoRingingPatternTable_row(struct mgcoRingingPatternTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_mgcoRingingCadenceGroupTable_row(struct mgcoRingingCadenceGroupTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_mgcoRingingCadenceGroupTable_row(struct mgcoRingingCadenceGroupTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_mgcoRingingCadenceGroupTable_row(struct mgcoRingingCadenceGroupTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_mgcoRingingCadenceGroupTable_row(struct mgcoRingingCadenceGroupTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int check_mgcoRingingPatternTable_row(struct mgcoRingingPatternTable_data *StorageTmp, struct mgcoRingingPatternTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_mgcoRingingPatternTable_row(struct mgcoRingingPatternTable_data *StorageTmp, struct mgcoRingingPatternTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_mgcoRingingPatternTable_row(struct mgcoRingingPatternTable_data *StorageTmp, struct mgcoRingingPatternTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_mgcoRingingPatternTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_mgcoRingingPatternTable_row(struct mgcoRingingPatternTable_data *StorageTmp, struct mgcoRingingPatternTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	mgcoRingingPatternTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_mgcoRingingPatternTable_row(struct mgcoRingingPatternTable_data *StorageTmp, struct mgcoRingingPatternTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_mgcoRingingPatternTable_row(struct mgcoRingingPatternTable_data *StorageTmp, struct mgcoRingingPatternTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_mgcoRingingPatternTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_mgcoRingingPatternTable_row(struct mgcoRingingPatternTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the mgcoRingingPatternTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct mgcoRingingPatternTable_data *
refresh_mgcoRingingPatternTable_row(struct mgcoRingingPatternTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->mgcoRingingPatternTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->mgcoRingingPatternTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_mgcoRingingPatternTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the mgcoRingingPatternTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_mgcoRingingPatternTable(int force)
{
	if (!force && mgcoRingingPatternTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	mgcoRingingPatternTable_refresh = 0;
}

/**
 * @fn u_char *var_mgcoRingingPatternTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in mgcoRingingPatternTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_megacoRingingMIB above.
 */
u_char *
var_mgcoRingingPatternTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct mgcoRingingPatternTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("megacoRingingMIB", "var_mgcoRingingPatternTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_mgcoRingingPatternTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(mgcoRingingPatternTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_mgcoRingingPatternTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) MGCORINGINGCADENCEGROUPID:	/* Create */
		*write_method = write_mgcoRingingCadenceGroupId;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->mgcoRingingCadenceGroupId);
		rval = (u_char *) &StorageTmp->mgcoRingingCadenceGroupId;
		break;
	case (u_char) MGCORINGINGPATTERNNAME:	/* Create */
		*write_method = write_mgcoRingingPatternName;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->mgcoRingingPatternNameLen;
		rval = (u_char *) StorageTmp->mgcoRingingPatternName;
		break;
	case (u_char) MGCORINGINGPATTERNROWSTATUS:	/* Create */
		*write_method = write_mgcoRingingPatternRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->mgcoRingingPatternRowStatus);
		rval = (u_char *) &StorageTmp->mgcoRingingPatternRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_mgcoRingingCadenceGroupTable_row(struct mgcoRingingCadenceGroupTable_data *StorageTmp, struct mgcoRingingCadenceGroupTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_mgcoRingingCadenceGroupTable_row(struct mgcoRingingCadenceGroupTable_data *StorageTmp, struct mgcoRingingCadenceGroupTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_mgcoRingingCadenceGroupTable_row(struct mgcoRingingCadenceGroupTable_data *StorageTmp, struct mgcoRingingCadenceGroupTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_mgcoRingingCadenceGroupTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_mgcoRingingCadenceGroupTable_row(struct mgcoRingingCadenceGroupTable_data *StorageTmp, struct mgcoRingingCadenceGroupTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	mgcoRingingCadenceGroupTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_mgcoRingingCadenceGroupTable_row(struct mgcoRingingCadenceGroupTable_data *StorageTmp, struct mgcoRingingCadenceGroupTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_mgcoRingingCadenceGroupTable_row(struct mgcoRingingCadenceGroupTable_data *StorageTmp, struct mgcoRingingCadenceGroupTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_mgcoRingingCadenceGroupTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_mgcoRingingCadenceGroupTable_row(struct mgcoRingingCadenceGroupTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the mgcoRingingCadenceGroupTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct mgcoRingingCadenceGroupTable_data *
refresh_mgcoRingingCadenceGroupTable_row(struct mgcoRingingCadenceGroupTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->mgcoRingingCadenceGroupTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->mgcoRingingCadenceGroupTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_mgcoRingingCadenceGroupTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the mgcoRingingCadenceGroupTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_mgcoRingingCadenceGroupTable(int force)
{
	if (!force && mgcoRingingCadenceGroupTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	mgcoRingingCadenceGroupTable_refresh = 0;
}

/**
 * @fn u_char *var_mgcoRingingCadenceGroupTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in mgcoRingingCadenceGroupTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_megacoRingingMIB above.
 */
u_char *
var_mgcoRingingCadenceGroupTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct mgcoRingingCadenceGroupTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("megacoRingingMIB", "var_mgcoRingingCadenceGroupTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_mgcoRingingCadenceGroupTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(mgcoRingingCadenceGroupTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_mgcoRingingCadenceGroupTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) MGCORINGINGCADENCEGROUPNAME:	/* Create */
		*write_method = write_mgcoRingingCadenceGroupName;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->mgcoRingingCadenceGroupNameLen;
		rval = (u_char *) StorageTmp->mgcoRingingCadenceGroupName;
		break;
	case (u_char) MGCORINGINGCADENCEGROUPREF:	/* Create */
		*write_method = write_mgcoRingingCadenceGroupRef;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->mgcoRingingCadenceGroupRef);
		rval = (u_char *) &StorageTmp->mgcoRingingCadenceGroupRef;
		break;
	case (u_char) MGCORINGINGCADENCEGROUPRINGINGMINDURATION:	/* Create */
		*write_method = write_mgcoRingingCadenceGroupRingingMinDuration;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->mgcoRingingCadenceGroupRingingMinDuration);
		rval = (u_char *) &StorageTmp->mgcoRingingCadenceGroupRingingMinDuration;
		break;
	case (u_char) MGCORINGINGCADENCEGROUPRINGINGNOMDURATION:	/* Create */
		*write_method = write_mgcoRingingCadenceGroupRingingNomDuration;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->mgcoRingingCadenceGroupRingingNomDuration);
		rval = (u_char *) &StorageTmp->mgcoRingingCadenceGroupRingingNomDuration;
		break;
	case (u_char) MGCORINGINGCADENCEGROUPRINGINGMAXDURATION:	/* Create */
		*write_method = write_mgcoRingingCadenceGroupRingingMaxDuration;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->mgcoRingingCadenceGroupRingingMaxDuration);
		rval = (u_char *) &StorageTmp->mgcoRingingCadenceGroupRingingMaxDuration;
		break;
	case (u_char) MGCORINGINGCADENCEGROUPSILENTMINDURATION:	/* Create */
		*write_method = write_mgcoRingingCadenceGroupSilentMinDuration;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->mgcoRingingCadenceGroupSilentMinDuration);
		rval = (u_char *) &StorageTmp->mgcoRingingCadenceGroupSilentMinDuration;
		break;
	case (u_char) MGCORINGINGCADENCEGROUPSILENTNOMDURATION:	/* Create */
		*write_method = write_mgcoRingingCadenceGroupSilentNomDuration;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->mgcoRingingCadenceGroupSilentNomDuration);
		rval = (u_char *) &StorageTmp->mgcoRingingCadenceGroupSilentNomDuration;
		break;
	case (u_char) MGCORINGINGCADENCEGROUPSILENTMAXDURATION:	/* Create */
		*write_method = write_mgcoRingingCadenceGroupSilentMaxDuration;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->mgcoRingingCadenceGroupSilentMaxDuration);
		rval = (u_char *) &StorageTmp->mgcoRingingCadenceGroupSilentMaxDuration;
		break;
	case (u_char) MGCORINGINGCADENCEGROUPROWSTATUS:	/* Create */
		*write_method = write_mgcoRingingCadenceGroupRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->mgcoRingingCadenceGroupRowStatus);
		rval = (u_char *) &StorageTmp->mgcoRingingCadenceGroupRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int write_mgcoRingingCadenceGroupId(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_mgcoRingingCadenceGroupId(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct mgcoRingingPatternTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("megacoRingingMIB", "write_mgcoRingingCadenceGroupId entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(mgcoRingingPatternTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->mgcoRingingPatternRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to mgcoRingingCadenceGroupId: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to mgcoRingingCadenceGroupId not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to mgcoRingingCadenceGroupId: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 1..2147483647 */
		if ((1 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to mgcoRingingCadenceGroupId: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->mgcoRingingPatternTable_old) == NULL)
			if (StorageTmp->mgcoRingingPatternTable_rsvs == 0)
				if ((StorageOld = StorageTmp->mgcoRingingPatternTable_old = mgcoRingingPatternTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->mgcoRingingPatternTable_rsvs++;
		StorageTmp->mgcoRingingCadenceGroupId = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->mgcoRingingPatternTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->mgcoRingingPatternTable_tsts == 0)
				if ((ret = check_mgcoRingingPatternTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->mgcoRingingPatternTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->mgcoRingingCadenceGroupId for you to use, and you have just been asked to do something with it.  Note that anything done 
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->mgcoRingingPatternTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->mgcoRingingPatternTable_sets == 0)
				if ((ret = update_mgcoRingingPatternTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->mgcoRingingPatternTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->mgcoRingingPatternTable_old) != NULL) {
			mgcoRingingPatternTable_destroy(&StorageTmp->mgcoRingingPatternTable_old);
			StorageTmp->mgcoRingingPatternTable_rsvs = 0;
			StorageTmp->mgcoRingingPatternTable_tsts = 0;
			StorageTmp->mgcoRingingPatternTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->mgcoRingingPatternTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->mgcoRingingPatternTable_sets == 0)
			revert_mgcoRingingPatternTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->mgcoRingingPatternTable_old) == NULL)
			break;
		StorageTmp->mgcoRingingCadenceGroupId = StorageOld->mgcoRingingCadenceGroupId;
		if (--StorageTmp->mgcoRingingPatternTable_rsvs == 0)
			mgcoRingingPatternTable_destroy(&StorageTmp->mgcoRingingPatternTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_mgcoRingingPatternName(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_mgcoRingingPatternName(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct mgcoRingingPatternTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("megacoRingingMIB", "write_mgcoRingingPatternName entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(mgcoRingingPatternTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->mgcoRingingPatternRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to mgcoRingingPatternName: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to mgcoRingingPatternName not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 0..255 */
		if (var_val_len > SPRINT_MAX_LEN || ((0 > var_val_len || var_val_len > 255))) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to mgcoRingingPatternName: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->mgcoRingingPatternTable_old) == NULL)
			if (StorageTmp->mgcoRingingPatternTable_rsvs == 0)
				if ((StorageOld = StorageTmp->mgcoRingingPatternTable_old = mgcoRingingPatternTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->mgcoRingingPatternTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->mgcoRingingPatternName);
		StorageTmp->mgcoRingingPatternName = string;
		StorageTmp->mgcoRingingPatternNameLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->mgcoRingingPatternTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->mgcoRingingPatternTable_tsts == 0)
				if ((ret = check_mgcoRingingPatternTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->mgcoRingingPatternTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->mgcoRingingPatternName for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->mgcoRingingPatternTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->mgcoRingingPatternTable_sets == 0)
				if ((ret = update_mgcoRingingPatternTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->mgcoRingingPatternTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->mgcoRingingPatternTable_old) != NULL) {
			mgcoRingingPatternTable_destroy(&StorageTmp->mgcoRingingPatternTable_old);
			StorageTmp->mgcoRingingPatternTable_rsvs = 0;
			StorageTmp->mgcoRingingPatternTable_tsts = 0;
			StorageTmp->mgcoRingingPatternTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->mgcoRingingPatternTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->mgcoRingingPatternTable_sets == 0)
			revert_mgcoRingingPatternTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->mgcoRingingPatternTable_old) == NULL)
			break;
		if (StorageOld->mgcoRingingPatternName != NULL) {
			SNMP_FREE(StorageTmp->mgcoRingingPatternName);
			StorageTmp->mgcoRingingPatternName = StorageOld->mgcoRingingPatternName;
			StorageTmp->mgcoRingingPatternNameLen = StorageOld->mgcoRingingPatternNameLen;
			StorageOld->mgcoRingingPatternName = NULL;
			StorageOld->mgcoRingingPatternNameLen = 0;
		}
		if (--StorageTmp->mgcoRingingPatternTable_rsvs == 0)
			mgcoRingingPatternTable_destroy(&StorageTmp->mgcoRingingPatternTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_mgcoRingingCadenceGroupId(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_mgcoRingingCadenceGroupId(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct mgcoRingingCadenceGroupTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("megacoRingingMIB", "write_mgcoRingingCadenceGroupId entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(mgcoRingingCadenceGroupTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->mgcoRingingCadenceGroupRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to mgcoRingingCadenceGroupId: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to mgcoRingingCadenceGroupId not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to mgcoRingingCadenceGroupId: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 1..2147483647 */
		if ((1 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to mgcoRingingCadenceGroupId: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old) == NULL)
			if (StorageTmp->mgcoRingingCadenceGroupTable_rsvs == 0)
				if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old = mgcoRingingCadenceGroupTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->mgcoRingingCadenceGroupTable_rsvs++;
		StorageTmp->mgcoRingingCadenceGroupId = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->mgcoRingingCadenceGroupTable_tsts == 0)
				if ((ret = check_mgcoRingingCadenceGroupTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->mgcoRingingCadenceGroupTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->mgcoRingingCadenceGroupId for you to use, and you have just been asked to do something with it.  Note that anything done 
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->mgcoRingingCadenceGroupTable_sets == 0)
				if ((ret = update_mgcoRingingCadenceGroupTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->mgcoRingingCadenceGroupTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old) != NULL) {
			mgcoRingingCadenceGroupTable_destroy(&StorageTmp->mgcoRingingCadenceGroupTable_old);
			StorageTmp->mgcoRingingCadenceGroupTable_rsvs = 0;
			StorageTmp->mgcoRingingCadenceGroupTable_tsts = 0;
			StorageTmp->mgcoRingingCadenceGroupTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->mgcoRingingCadenceGroupTable_sets == 0)
			revert_mgcoRingingCadenceGroupTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old) == NULL)
			break;
		StorageTmp->mgcoRingingCadenceGroupId = StorageOld->mgcoRingingCadenceGroupId;
		if (--StorageTmp->mgcoRingingCadenceGroupTable_rsvs == 0)
			mgcoRingingCadenceGroupTable_destroy(&StorageTmp->mgcoRingingCadenceGroupTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_mgcoRingingCadenceGroupName(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_mgcoRingingCadenceGroupName(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct mgcoRingingCadenceGroupTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("megacoRingingMIB", "write_mgcoRingingCadenceGroupName entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(mgcoRingingCadenceGroupTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->mgcoRingingCadenceGroupRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to mgcoRingingCadenceGroupName: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to mgcoRingingCadenceGroupName not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 0..255 */
		if (var_val_len > SPRINT_MAX_LEN || ((0 > var_val_len || var_val_len > 255))) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to mgcoRingingCadenceGroupName: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old) == NULL)
			if (StorageTmp->mgcoRingingCadenceGroupTable_rsvs == 0)
				if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old = mgcoRingingCadenceGroupTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->mgcoRingingCadenceGroupTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->mgcoRingingCadenceGroupName);
		StorageTmp->mgcoRingingCadenceGroupName = string;
		StorageTmp->mgcoRingingCadenceGroupNameLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->mgcoRingingCadenceGroupTable_tsts == 0)
				if ((ret = check_mgcoRingingCadenceGroupTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->mgcoRingingCadenceGroupTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->mgcoRingingCadenceGroupName for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->mgcoRingingCadenceGroupTable_sets == 0)
				if ((ret = update_mgcoRingingCadenceGroupTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->mgcoRingingCadenceGroupTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old) != NULL) {
			mgcoRingingCadenceGroupTable_destroy(&StorageTmp->mgcoRingingCadenceGroupTable_old);
			StorageTmp->mgcoRingingCadenceGroupTable_rsvs = 0;
			StorageTmp->mgcoRingingCadenceGroupTable_tsts = 0;
			StorageTmp->mgcoRingingCadenceGroupTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->mgcoRingingCadenceGroupTable_sets == 0)
			revert_mgcoRingingCadenceGroupTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old) == NULL)
			break;
		if (StorageOld->mgcoRingingCadenceGroupName != NULL) {
			SNMP_FREE(StorageTmp->mgcoRingingCadenceGroupName);
			StorageTmp->mgcoRingingCadenceGroupName = StorageOld->mgcoRingingCadenceGroupName;
			StorageTmp->mgcoRingingCadenceGroupNameLen = StorageOld->mgcoRingingCadenceGroupNameLen;
			StorageOld->mgcoRingingCadenceGroupName = NULL;
			StorageOld->mgcoRingingCadenceGroupNameLen = 0;
		}
		if (--StorageTmp->mgcoRingingCadenceGroupTable_rsvs == 0)
			mgcoRingingCadenceGroupTable_destroy(&StorageTmp->mgcoRingingCadenceGroupTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_mgcoRingingCadenceGroupRef(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_mgcoRingingCadenceGroupRef(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct mgcoRingingCadenceGroupTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("megacoRingingMIB", "write_mgcoRingingCadenceGroupRef entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(mgcoRingingCadenceGroupTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->mgcoRingingCadenceGroupRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to mgcoRingingCadenceGroupRef: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to mgcoRingingCadenceGroupRef not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to mgcoRingingCadenceGroupRef: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old) == NULL)
			if (StorageTmp->mgcoRingingCadenceGroupTable_rsvs == 0)
				if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old = mgcoRingingCadenceGroupTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->mgcoRingingCadenceGroupTable_rsvs++;
		StorageTmp->mgcoRingingCadenceGroupRef = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->mgcoRingingCadenceGroupTable_tsts == 0)
				if ((ret = check_mgcoRingingCadenceGroupTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->mgcoRingingCadenceGroupTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->mgcoRingingCadenceGroupRef for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->mgcoRingingCadenceGroupTable_sets == 0)
				if ((ret = update_mgcoRingingCadenceGroupTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->mgcoRingingCadenceGroupTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old) != NULL) {
			mgcoRingingCadenceGroupTable_destroy(&StorageTmp->mgcoRingingCadenceGroupTable_old);
			StorageTmp->mgcoRingingCadenceGroupTable_rsvs = 0;
			StorageTmp->mgcoRingingCadenceGroupTable_tsts = 0;
			StorageTmp->mgcoRingingCadenceGroupTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->mgcoRingingCadenceGroupTable_sets == 0)
			revert_mgcoRingingCadenceGroupTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old) == NULL)
			break;
		StorageTmp->mgcoRingingCadenceGroupRef = StorageOld->mgcoRingingCadenceGroupRef;
		if (--StorageTmp->mgcoRingingCadenceGroupTable_rsvs == 0)
			mgcoRingingCadenceGroupTable_destroy(&StorageTmp->mgcoRingingCadenceGroupTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_mgcoRingingCadenceGroupRingingMinDuration(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_mgcoRingingCadenceGroupRingingMinDuration(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct mgcoRingingCadenceGroupTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("megacoRingingMIB", "write_mgcoRingingCadenceGroupRingingMinDuration entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(mgcoRingingCadenceGroupTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->mgcoRingingCadenceGroupRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to mgcoRingingCadenceGroupRingingMinDuration: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to mgcoRingingCadenceGroupRingingMinDuration not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to mgcoRingingCadenceGroupRingingMinDuration: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..5000 */
		if ((0 > set_value || set_value > 5000)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to mgcoRingingCadenceGroupRingingMinDuration: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old) == NULL)
			if (StorageTmp->mgcoRingingCadenceGroupTable_rsvs == 0)
				if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old = mgcoRingingCadenceGroupTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->mgcoRingingCadenceGroupTable_rsvs++;
		StorageTmp->mgcoRingingCadenceGroupRingingMinDuration = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->mgcoRingingCadenceGroupTable_tsts == 0)
				if ((ret = check_mgcoRingingCadenceGroupTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->mgcoRingingCadenceGroupTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->mgcoRingingCadenceGroupRingingMinDuration for you to use, and you have just been asked to do something with it.  Note
				   that anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->mgcoRingingCadenceGroupTable_sets == 0)
				if ((ret = update_mgcoRingingCadenceGroupTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->mgcoRingingCadenceGroupTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old) != NULL) {
			mgcoRingingCadenceGroupTable_destroy(&StorageTmp->mgcoRingingCadenceGroupTable_old);
			StorageTmp->mgcoRingingCadenceGroupTable_rsvs = 0;
			StorageTmp->mgcoRingingCadenceGroupTable_tsts = 0;
			StorageTmp->mgcoRingingCadenceGroupTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->mgcoRingingCadenceGroupTable_sets == 0)
			revert_mgcoRingingCadenceGroupTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old) == NULL)
			break;
		StorageTmp->mgcoRingingCadenceGroupRingingMinDuration = StorageOld->mgcoRingingCadenceGroupRingingMinDuration;
		if (--StorageTmp->mgcoRingingCadenceGroupTable_rsvs == 0)
			mgcoRingingCadenceGroupTable_destroy(&StorageTmp->mgcoRingingCadenceGroupTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_mgcoRingingCadenceGroupRingingNomDuration(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_mgcoRingingCadenceGroupRingingNomDuration(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct mgcoRingingCadenceGroupTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("megacoRingingMIB", "write_mgcoRingingCadenceGroupRingingNomDuration entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(mgcoRingingCadenceGroupTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->mgcoRingingCadenceGroupRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to mgcoRingingCadenceGroupRingingNomDuration: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to mgcoRingingCadenceGroupRingingNomDuration not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to mgcoRingingCadenceGroupRingingNomDuration: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..5000 */
		if ((0 > set_value || set_value > 5000)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to mgcoRingingCadenceGroupRingingNomDuration: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old) == NULL)
			if (StorageTmp->mgcoRingingCadenceGroupTable_rsvs == 0)
				if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old = mgcoRingingCadenceGroupTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->mgcoRingingCadenceGroupTable_rsvs++;
		StorageTmp->mgcoRingingCadenceGroupRingingNomDuration = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->mgcoRingingCadenceGroupTable_tsts == 0)
				if ((ret = check_mgcoRingingCadenceGroupTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->mgcoRingingCadenceGroupTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->mgcoRingingCadenceGroupRingingNomDuration for you to use, and you have just been asked to do something with it.  Note
				   that anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->mgcoRingingCadenceGroupTable_sets == 0)
				if ((ret = update_mgcoRingingCadenceGroupTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->mgcoRingingCadenceGroupTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old) != NULL) {
			mgcoRingingCadenceGroupTable_destroy(&StorageTmp->mgcoRingingCadenceGroupTable_old);
			StorageTmp->mgcoRingingCadenceGroupTable_rsvs = 0;
			StorageTmp->mgcoRingingCadenceGroupTable_tsts = 0;
			StorageTmp->mgcoRingingCadenceGroupTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->mgcoRingingCadenceGroupTable_sets == 0)
			revert_mgcoRingingCadenceGroupTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old) == NULL)
			break;
		StorageTmp->mgcoRingingCadenceGroupRingingNomDuration = StorageOld->mgcoRingingCadenceGroupRingingNomDuration;
		if (--StorageTmp->mgcoRingingCadenceGroupTable_rsvs == 0)
			mgcoRingingCadenceGroupTable_destroy(&StorageTmp->mgcoRingingCadenceGroupTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_mgcoRingingCadenceGroupRingingMaxDuration(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_mgcoRingingCadenceGroupRingingMaxDuration(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct mgcoRingingCadenceGroupTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("megacoRingingMIB", "write_mgcoRingingCadenceGroupRingingMaxDuration entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(mgcoRingingCadenceGroupTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->mgcoRingingCadenceGroupRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to mgcoRingingCadenceGroupRingingMaxDuration: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to mgcoRingingCadenceGroupRingingMaxDuration not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to mgcoRingingCadenceGroupRingingMaxDuration: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..5000 */
		if ((0 > set_value || set_value > 5000)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to mgcoRingingCadenceGroupRingingMaxDuration: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old) == NULL)
			if (StorageTmp->mgcoRingingCadenceGroupTable_rsvs == 0)
				if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old = mgcoRingingCadenceGroupTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->mgcoRingingCadenceGroupTable_rsvs++;
		StorageTmp->mgcoRingingCadenceGroupRingingMaxDuration = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->mgcoRingingCadenceGroupTable_tsts == 0)
				if ((ret = check_mgcoRingingCadenceGroupTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->mgcoRingingCadenceGroupTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->mgcoRingingCadenceGroupRingingMaxDuration for you to use, and you have just been asked to do something with it.  Note
				   that anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->mgcoRingingCadenceGroupTable_sets == 0)
				if ((ret = update_mgcoRingingCadenceGroupTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->mgcoRingingCadenceGroupTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old) != NULL) {
			mgcoRingingCadenceGroupTable_destroy(&StorageTmp->mgcoRingingCadenceGroupTable_old);
			StorageTmp->mgcoRingingCadenceGroupTable_rsvs = 0;
			StorageTmp->mgcoRingingCadenceGroupTable_tsts = 0;
			StorageTmp->mgcoRingingCadenceGroupTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->mgcoRingingCadenceGroupTable_sets == 0)
			revert_mgcoRingingCadenceGroupTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old) == NULL)
			break;
		StorageTmp->mgcoRingingCadenceGroupRingingMaxDuration = StorageOld->mgcoRingingCadenceGroupRingingMaxDuration;
		if (--StorageTmp->mgcoRingingCadenceGroupTable_rsvs == 0)
			mgcoRingingCadenceGroupTable_destroy(&StorageTmp->mgcoRingingCadenceGroupTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_mgcoRingingCadenceGroupSilentMinDuration(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_mgcoRingingCadenceGroupSilentMinDuration(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct mgcoRingingCadenceGroupTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("megacoRingingMIB", "write_mgcoRingingCadenceGroupSilentMinDuration entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(mgcoRingingCadenceGroupTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->mgcoRingingCadenceGroupRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to mgcoRingingCadenceGroupSilentMinDuration: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to mgcoRingingCadenceGroupSilentMinDuration not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to mgcoRingingCadenceGroupSilentMinDuration: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..5000 */
		if ((0 > set_value || set_value > 5000)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to mgcoRingingCadenceGroupSilentMinDuration: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old) == NULL)
			if (StorageTmp->mgcoRingingCadenceGroupTable_rsvs == 0)
				if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old = mgcoRingingCadenceGroupTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->mgcoRingingCadenceGroupTable_rsvs++;
		StorageTmp->mgcoRingingCadenceGroupSilentMinDuration = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->mgcoRingingCadenceGroupTable_tsts == 0)
				if ((ret = check_mgcoRingingCadenceGroupTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->mgcoRingingCadenceGroupTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->mgcoRingingCadenceGroupSilentMinDuration for you to use, and you have just been asked to do something with it.  Note
				   that anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->mgcoRingingCadenceGroupTable_sets == 0)
				if ((ret = update_mgcoRingingCadenceGroupTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->mgcoRingingCadenceGroupTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old) != NULL) {
			mgcoRingingCadenceGroupTable_destroy(&StorageTmp->mgcoRingingCadenceGroupTable_old);
			StorageTmp->mgcoRingingCadenceGroupTable_rsvs = 0;
			StorageTmp->mgcoRingingCadenceGroupTable_tsts = 0;
			StorageTmp->mgcoRingingCadenceGroupTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->mgcoRingingCadenceGroupTable_sets == 0)
			revert_mgcoRingingCadenceGroupTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old) == NULL)
			break;
		StorageTmp->mgcoRingingCadenceGroupSilentMinDuration = StorageOld->mgcoRingingCadenceGroupSilentMinDuration;
		if (--StorageTmp->mgcoRingingCadenceGroupTable_rsvs == 0)
			mgcoRingingCadenceGroupTable_destroy(&StorageTmp->mgcoRingingCadenceGroupTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_mgcoRingingCadenceGroupSilentNomDuration(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_mgcoRingingCadenceGroupSilentNomDuration(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct mgcoRingingCadenceGroupTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("megacoRingingMIB", "write_mgcoRingingCadenceGroupSilentNomDuration entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(mgcoRingingCadenceGroupTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->mgcoRingingCadenceGroupRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to mgcoRingingCadenceGroupSilentNomDuration: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to mgcoRingingCadenceGroupSilentNomDuration not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to mgcoRingingCadenceGroupSilentNomDuration: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..5000 */
		if ((0 > set_value || set_value > 5000)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to mgcoRingingCadenceGroupSilentNomDuration: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old) == NULL)
			if (StorageTmp->mgcoRingingCadenceGroupTable_rsvs == 0)
				if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old = mgcoRingingCadenceGroupTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->mgcoRingingCadenceGroupTable_rsvs++;
		StorageTmp->mgcoRingingCadenceGroupSilentNomDuration = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->mgcoRingingCadenceGroupTable_tsts == 0)
				if ((ret = check_mgcoRingingCadenceGroupTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->mgcoRingingCadenceGroupTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->mgcoRingingCadenceGroupSilentNomDuration for you to use, and you have just been asked to do something with it.  Note
				   that anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->mgcoRingingCadenceGroupTable_sets == 0)
				if ((ret = update_mgcoRingingCadenceGroupTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->mgcoRingingCadenceGroupTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old) != NULL) {
			mgcoRingingCadenceGroupTable_destroy(&StorageTmp->mgcoRingingCadenceGroupTable_old);
			StorageTmp->mgcoRingingCadenceGroupTable_rsvs = 0;
			StorageTmp->mgcoRingingCadenceGroupTable_tsts = 0;
			StorageTmp->mgcoRingingCadenceGroupTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->mgcoRingingCadenceGroupTable_sets == 0)
			revert_mgcoRingingCadenceGroupTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old) == NULL)
			break;
		StorageTmp->mgcoRingingCadenceGroupSilentNomDuration = StorageOld->mgcoRingingCadenceGroupSilentNomDuration;
		if (--StorageTmp->mgcoRingingCadenceGroupTable_rsvs == 0)
			mgcoRingingCadenceGroupTable_destroy(&StorageTmp->mgcoRingingCadenceGroupTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_mgcoRingingCadenceGroupSilentMaxDuration(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_mgcoRingingCadenceGroupSilentMaxDuration(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct mgcoRingingCadenceGroupTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("megacoRingingMIB", "write_mgcoRingingCadenceGroupSilentMaxDuration entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(mgcoRingingCadenceGroupTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->mgcoRingingCadenceGroupRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to mgcoRingingCadenceGroupSilentMaxDuration: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to mgcoRingingCadenceGroupSilentMaxDuration not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to mgcoRingingCadenceGroupSilentMaxDuration: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..5000 */
		if ((0 > set_value || set_value > 5000)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to mgcoRingingCadenceGroupSilentMaxDuration: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old) == NULL)
			if (StorageTmp->mgcoRingingCadenceGroupTable_rsvs == 0)
				if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old = mgcoRingingCadenceGroupTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->mgcoRingingCadenceGroupTable_rsvs++;
		StorageTmp->mgcoRingingCadenceGroupSilentMaxDuration = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->mgcoRingingCadenceGroupTable_tsts == 0)
				if ((ret = check_mgcoRingingCadenceGroupTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->mgcoRingingCadenceGroupTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->mgcoRingingCadenceGroupSilentMaxDuration for you to use, and you have just been asked to do something with it.  Note
				   that anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->mgcoRingingCadenceGroupTable_sets == 0)
				if ((ret = update_mgcoRingingCadenceGroupTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->mgcoRingingCadenceGroupTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old) != NULL) {
			mgcoRingingCadenceGroupTable_destroy(&StorageTmp->mgcoRingingCadenceGroupTable_old);
			StorageTmp->mgcoRingingCadenceGroupTable_rsvs = 0;
			StorageTmp->mgcoRingingCadenceGroupTable_tsts = 0;
			StorageTmp->mgcoRingingCadenceGroupTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->mgcoRingingCadenceGroupTable_sets == 0)
			revert_mgcoRingingCadenceGroupTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old) == NULL)
			break;
		StorageTmp->mgcoRingingCadenceGroupSilentMaxDuration = StorageOld->mgcoRingingCadenceGroupSilentMaxDuration;
		if (--StorageTmp->mgcoRingingCadenceGroupTable_rsvs == 0)
			mgcoRingingCadenceGroupTable_destroy(&StorageTmp->mgcoRingingCadenceGroupTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_mgcoRingingCadenceNextGroupId(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_mgcoRingingCadenceNextGroupId(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct megacoRingingMIB_data *StorageTmp = NULL, *StorageOld = NULL;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("megacoRingingMIB", "write_mgcoRingingCadenceNextGroupId entering action=%d...  \n", action));
	if ((StorageTmp = megacoRingingMIBStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to mgcoRingingCadenceNextGroupId not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to mgcoRingingCadenceNextGroupId: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to mgcoRingingCadenceNextGroupId: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for whole mib */
		if ((StorageOld = StorageTmp->megacoRingingMIB_old) == NULL)
			if (StorageTmp->megacoRingingMIB_rsvs == 0)
				if ((StorageOld = StorageTmp->megacoRingingMIB_old = megacoRingingMIB_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->megacoRingingMIB_rsvs++;
		StorageTmp->mgcoRingingCadenceNextGroupId = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->megacoRingingMIB_old) != NULL) {
			/* one consistency check for the whole mib */
			if (StorageTmp->megacoRingingMIB_tsts == 0)
				if ((ret = check_megacoRingingMIB(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->megacoRingingMIB_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->mgcoRingingCadenceNextGroupId for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole mib */
		if ((StorageOld = StorageTmp->megacoRingingMIB_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->megacoRingingMIB_sets == 0)
				if ((ret = update_megacoRingingMIB(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->megacoRingingMIB_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->megacoRingingMIB_old) != NULL) {
			megacoRingingMIB_destroy(&StorageTmp->megacoRingingMIB_old);
			StorageTmp->megacoRingingMIB_rsvs = 0;
			StorageTmp->megacoRingingMIB_tsts = 0;
			StorageTmp->megacoRingingMIB_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->megacoRingingMIB_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->megacoRingingMIB_tsts == 0)
			revert_megacoRingingMIB(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->megacoRingingMIB_old) == NULL)
			break;
		StorageTmp->mgcoRingingCadenceNextGroupId = StorageOld->mgcoRingingCadenceNextGroupId;
		if (--StorageTmp->megacoRingingMIB_rsvs == 0)
			megacoRingingMIB_destroy(&StorageTmp->megacoRingingMIB_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_mgcoRingingPatternTable_row(struct mgcoRingingPatternTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_mgcoRingingPatternTable_row(struct mgcoRingingPatternTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_mgcoRingingPatternTable_row(struct mgcoRingingPatternTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_mgcoRingingPatternTable_row(struct mgcoRingingPatternTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_mgcoRingingCadenceGroupTable_row(struct mgcoRingingCadenceGroupTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_mgcoRingingCadenceGroupTable_row(struct mgcoRingingCadenceGroupTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_mgcoRingingCadenceGroupTable_row(struct mgcoRingingCadenceGroupTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_mgcoRingingCadenceGroupTable_row(struct mgcoRingingCadenceGroupTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_mgcoRingingPatternRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_mgcoRingingPatternRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct mgcoRingingPatternTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct mgcoRingingPatternTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 12;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("megacoRingingMIB", "write_mgcoRingingPatternRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(mgcoRingingPatternTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to mgcoRingingPatternRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to mgcoRingingPatternRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* mgcoRingingPatternId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[12]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* mgcoRingingPatternId */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index mgcoRingingPatternId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..2147483647 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > 2147483647)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index mgcoRingingPatternId: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = mgcoRingingPatternTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->mgcoRingingPatternTable_rsvs = 1;
			vp = vars;
			StorageNew->mgcoRingingPatternId = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&mgcoRingingPatternTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->mgcoRingingPatternRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->mgcoRingingPatternTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->mgcoRingingPatternTable_old) == NULL)
				if (StorageTmp->mgcoRingingPatternTable_rsvs == 0)
					if ((StorageOld = StorageTmp->mgcoRingingPatternTable_old = mgcoRingingPatternTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->mgcoRingingPatternTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->mgcoRingingPatternTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				mgcoRingingPatternTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_mgcoRingingPatternTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* check that activation is possible */
			if (StorageTmp->mgcoRingingPatternRowStatus != RS_ACTIVE)
				if ((ret = can_act_mgcoRingingPatternTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->mgcoRingingPatternRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_mgcoRingingPatternTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->mgcoRingingPatternRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_mgcoRingingPatternTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->mgcoRingingPatternRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here must be
		   reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_mgcoRingingPatternTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->mgcoRingingPatternRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_mgcoRingingPatternTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->mgcoRingingPatternRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_mgcoRingingPatternTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destruction to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_mgcoRingingPatternTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->mgcoRingingPatternRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->mgcoRingingPatternRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->mgcoRingingPatternRowStatus = set_value;
			if ((StorageOld = StorageTmp->mgcoRingingPatternTable_old) != NULL) {
				mgcoRingingPatternTable_destroy(&StorageTmp->mgcoRingingPatternTable_old);
				StorageTmp->mgcoRingingPatternTable_rsvs = 0;
				StorageTmp->mgcoRingingPatternTable_tsts = 0;
				StorageTmp->mgcoRingingPatternTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			mgcoRingingPatternTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_mgcoRingingPatternTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->mgcoRingingPatternRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_mgcoRingingPatternTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->mgcoRingingPatternRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_mgcoRingingPatternTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				mgcoRingingPatternTable_del(StorageNew);
				mgcoRingingPatternTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->mgcoRingingPatternTable_old) == NULL)
				break;
			if (--StorageTmp->mgcoRingingPatternTable_rsvs == 0)
				mgcoRingingPatternTable_destroy(&StorageTmp->mgcoRingingPatternTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				mgcoRingingPatternTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_mgcoRingingCadenceGroupRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_mgcoRingingCadenceGroupRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct mgcoRingingCadenceGroupTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct mgcoRingingCadenceGroupTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 12;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("megacoRingingMIB", "write_mgcoRingingCadenceGroupRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(mgcoRingingCadenceGroupTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to mgcoRingingCadenceGroupRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to mgcoRingingCadenceGroupRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* mgcoRingingCadenceGroupId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* mgcoRingingCadenceGroupIndex */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[12]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* mgcoRingingCadenceGroupId */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index mgcoRingingCadenceGroupId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..2147483647 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > 2147483647)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index mgcoRingingCadenceGroupId: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* mgcoRingingCadenceGroupIndex */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index mgcoRingingCadenceGroupIndex: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..2147483647 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > 2147483647)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index mgcoRingingCadenceGroupIndex: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = mgcoRingingCadenceGroupTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->mgcoRingingCadenceGroupTable_rsvs = 1;
			vp = vars;
			StorageNew->mgcoRingingCadenceGroupId = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->mgcoRingingCadenceGroupIndex = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&mgcoRingingCadenceGroupTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->mgcoRingingCadenceGroupRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->mgcoRingingCadenceGroupTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old) == NULL)
				if (StorageTmp->mgcoRingingCadenceGroupTable_rsvs == 0)
					if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old = mgcoRingingCadenceGroupTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->mgcoRingingCadenceGroupTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->mgcoRingingCadenceGroupTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				mgcoRingingCadenceGroupTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_mgcoRingingCadenceGroupTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* check that activation is possible */
			if (StorageTmp->mgcoRingingCadenceGroupRowStatus != RS_ACTIVE)
				if ((ret = can_act_mgcoRingingCadenceGroupTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->mgcoRingingCadenceGroupRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_mgcoRingingCadenceGroupTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->mgcoRingingCadenceGroupRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_mgcoRingingCadenceGroupTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->mgcoRingingCadenceGroupRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here
		   must be reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_mgcoRingingCadenceGroupTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->mgcoRingingCadenceGroupRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_mgcoRingingCadenceGroupTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->mgcoRingingCadenceGroupRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_mgcoRingingCadenceGroupTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destruction to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_mgcoRingingCadenceGroupTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->mgcoRingingCadenceGroupRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->mgcoRingingCadenceGroupRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->mgcoRingingCadenceGroupRowStatus = set_value;
			if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old) != NULL) {
				mgcoRingingCadenceGroupTable_destroy(&StorageTmp->mgcoRingingCadenceGroupTable_old);
				StorageTmp->mgcoRingingCadenceGroupTable_rsvs = 0;
				StorageTmp->mgcoRingingCadenceGroupTable_tsts = 0;
				StorageTmp->mgcoRingingCadenceGroupTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			mgcoRingingCadenceGroupTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_mgcoRingingCadenceGroupTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->mgcoRingingCadenceGroupRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_mgcoRingingCadenceGroupTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->mgcoRingingCadenceGroupRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_mgcoRingingCadenceGroupTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				mgcoRingingCadenceGroupTable_del(StorageNew);
				mgcoRingingCadenceGroupTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->mgcoRingingCadenceGroupTable_old) == NULL)
				break;
			if (--StorageTmp->mgcoRingingCadenceGroupTable_rsvs == 0)
				mgcoRingingCadenceGroupTable_destroy(&StorageTmp->mgcoRingingCadenceGroupTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				mgcoRingingCadenceGroupTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn void megacoRingingMIB_loop_handler(int dummy)
 * @param sig signal number
 * @brief handle event loop interation.
 *
 * This function is registered so that, when operating as a module, snmpd will call it one per event
 * loop interation.  This function is called before the next requst is processed and after the
 * previous request is processed.  Two things are done here:  1) The file descriptor that is used to
 * synchronize the agent with (pseudo-)device drivers is closed.  (Another approach, instead of
 * closing each time, would be to restart a timer each time that a request is made (loop is
 * performed) and if it expires, close the file descriptor).  2) The request number is incremented.
 * Although a request is not generated for each loop of the snmp event loop, it is true that a new
 * request cannot be generated without performing a loop.  Therefore, the sa_request is not the
 * request number but it is a temporally unique identifier for a request.
 */
void
megacoRingingMIB_loop_handler(int sig)
{
	DEBUGMSGTL(("megacoRingingMIB", "megacoRingingMIB_loop_handler: executing loop handler...  "));
	/* close files after each request */
	if (sa_fclose) {
		if (my_fd >= 0) {
			close(my_fd);
			my_fd = -1;
		}
	}
#if defined MASTER
	/* prepare for next request */
	sa_request++;
#endif				/* defined MASTER */
	if (external_signal_scheduled[sig] == 0)
		external_signal_scheduled[sig]--;
	if (megacoRingingMIBold_signal_handler != NULL)
		(*megacoRingingMIBold_signal_handler) (sig);
	DEBUGMSGTL(("megacoRingingMIB", "done.\n"));
}

/**
 * @fn void megacoRingingMIB_fd_handler(int fd, void *dummy)
 * @param fd file descriptor to read.
 * @param dummy client data passed to registration function (always NULL).
 * @brief handle read event on file descriptor.
 *
 * This read file descriptor handler is normally used for (pseudo-)device drivers that generate
 * statistical collection interval events, alarm events, or other operational measurement events, by
 * placing a message on the read queue of the "event handling" Stream.  Normally this routine
 * would adjust counts in some table or scalars, generate SNMP traps representing on-occurence
 * events, first and interval events, and alarm indications.
 */
void
megacoRingingMIB_fd_handler(int fd, void *dummy)
{
	DEBUGMSGTL(("megacoRingingMIB", "megacoRingingMIB_fd_handler: executing fd handler...  "));
	/* XXX: place actions to handle my_fd here... */
	DEBUGMSGTL(("megacoRingingMIB", "done.\n"));
	return;
}
