/*****************************************************************************

 @(#) File: src/snmp/x400pMxMIB.c

 -----------------------------------------------------------------------------

 Copyright (c) 2008-2015  Monavacon Limited <http://www.monavacon.com/>
 Copyright (c) 2001-2008  OpenSS7 Corporation <http://www.openss7.com/>
 Copyright (c) 1997-2001  Brian F. G. Bidulock <bidulock@openss7.org>

 All Rights Reserved.

 This program is free software: you can redistribute it and/or modify it under
 the terms of the GNU Affero General Public License as published by the Free
 Software Foundation, version 3 of the license.

 This program is distributed in the hope that it will be useful, but WITHOUT
 ANY WARRANTY; without even the implied warranty of MERCHANTABILITY or FITNESS
 FOR A PARTICULAR PURPOSE.  See the GNU Affero General Public License for more
 details.

 You should have received a copy of the GNU Affero General Public License
 along with this program.  If not, see <http://www.gnu.org/licenses/>, or
 write to the Free Software Foundation, Inc., 675 Mass Ave, Cambridge, MA
 02139, USA.

 -----------------------------------------------------------------------------

 U.S. GOVERNMENT RESTRICTED RIGHTS.  If you are licensing this Software on
 behalf of the U.S. Government ("Government"), the following provisions apply
 to you.  If the Software is supplied by the Department of Defense ("DoD"), it
 is classified as "Commercial Computer Software" under paragraph 252.227-7014
 of the DoD Supplement to the Federal Acquisition Regulations ("DFARS") (or any
 successor regulations) and the Government is acquiring only the license rights
 granted herein (the license rights customarily provided to non-Government
 users).  If the Software is supplied to any unit or agency of the Government
 other than DoD, it is classified as "Restricted Computer Software" and the
 Government's rights in the Software are defined in paragraph 52.227-19 of the
 Federal Acquisition Regulations ("FAR") (or any successor regulations) or, in
 the cases of NASA, in paragraph 18.52.227-86 of the NASA Supplement to the FAR
 (or any successor regulations).

 -----------------------------------------------------------------------------

 Commercial licensing and support of this software is available from OpenSS7
 Corporation at a fee.  See http://www.openss7.com/

 *****************************************************************************/

static char const ident[] = "src/snmp/x400pMxMIB.c (" PACKAGE_ENVR ") " PACKAGE_DATE;

/* This file was generated by mib2c and is intended for use as
   a mib module for the ucd-snmp snmpd agent. */
#include <ucd-snmp/ucd-snmp-config.h>
#include <ucd-snmp/ucd-snmp-includes.h>
#include <ucd-snmp/ucd-snmp-agent-includes.h>
#include <ucd-snmp/agent_trap.h>
#include <ucd-snmp/callback.h>
#include <ucd-snmp/snmp-tc.h>
#include <ucd-snmp/default_store.h>
#include <ucd-snmp/snmp_alarm.h>
/* The following header files are mangled in most recent net-snmp releases so
 * the versions from UCD-SNMP 4.2.5 are included here.  */
#if defined HAVE_LIBNETSNMP
#else				/* defined HAVE_LIBNETSNMP */
#endif				/* defined HAVE_LIBNETSNMP */
/* These are messed up on both. */
#include "ds_agent.h"
#ifdef HAVE_UCD_SNMP_UTIL_FUNCS_H
#include <ucd-snmp/util_funcs.h>
/* Many recent net-snmp UCD compatible headers do not declare header_generic. */
int header_generic(struct variable *, oid *, size_t *, int, size_t *, WriteMethod **);
#else				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#include "util_funcs.h"
#endif				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#ifdef HAVE_UCD_SNMP_HEADER_COMPLEX_H
#include <ucd-snmp/header_complex.h>
#else				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
#include "header_complex.h"
#endif				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
/* This one is the other way around: it is just fine for net-snmp, but
 * ucd-snmp does not provide the header file at all.  */
#ifdef HAVE_UCD_SNMP_MIB_MODULES_H
#include <ucd-snmp/mib_modules.h>
#else				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#ifdef HAVE_NET_SNMP_AGENT_MIB_MODULES_H
#include <net-snmp/agent/mib_modules.h>
#else				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#include "mib_modules.h"
#endif				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#endif				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#include <stdint.h>
#include <signal.h>
#include <sys/stat.h>		/* for struct stat, fstat() */
#include <sys/types.h>
#include <unistd.h>
#include <stdlib.h>
#include <pwd.h>		/* for getpwuid() getpwnam() */
#include <grp.h>		/* for getgrgid() getgrnam() */
#include <libgen.h>		/* for basename() */
#include <fcntl.h>		/* for O_CREAT */
#include <sys/sysctl.h>		/* for sysctl */
#include <errno.h>
#include <string.h>
#ifdef _GNU_SOURCE
#include <getopt.h>
#endif
#include <stropts.h>
#include <ss7/lmi.h>
#include <ss7/lmi_ioctl.h>
#include <sys/mxi.h>
#include <sys/mxi_ioctl2.h>
#include "ds0.h"
#include "ds0Bundle.h"
#include "ds1EXT.h"
#include "phyMIB.h"
#include "x400pMxMIB.h"
#define MASTER 1
#define MY_FACILITY(__pri)	(LOG_DAEMON|(__pri))
#if defined MODULE
#if defined MASTER
const char sa_program[] = "x400pMxMIB";
int sa_fclose = 1;			/* default close files between requests */
int sa_changed = 1;			/* indication to reread MIB configuration */
int sa_stats_refresh = 1;		/* indications that statistics, the mib or its tables need to be refreshed */
int sa_request = 1;			/* request number for per-request actions */
int sa_dump = 0;			/* default packet dump */
int sa_debug = 0;			/* default no debug */
#endif				/* defined MASTER */
#endif				/* defined MODULE */
static int my_fd = -1;			/* file descriptor for this MIB's use */
static int my_readfd = -1;		/* file descriptor for autonomnous events */
volatile int x400pMxMIB_refresh = 1;
volatile int x400pSyncTable_refresh = 1;
volatile int x400pDrivTable_refresh = 1;
volatile int x400pCardTable_refresh = 1;
volatile int x400pSpanTable_refresh = 1;
volatile int x400pBertTable_refresh = 1;
volatile int x400pChanTable_refresh = 1;
volatile int x400pXconTable_refresh = 1;
volatile int x400pNearEndCurrentTable_refresh = 1;
volatile int x400pNearEndIntervalTable_refresh = 1;
volatile int x400pNearEndTotalTable_refresh = 1;
volatile int x400pFarEndCurrentTable_refresh = 1;
volatile int x400pFarEndIntervalTable_refresh = 1;
volatile int x400pFarEndTotalTable_refresh = 1;

/*
 * x400pMxMIB_variables_oid: object identifier for x400pMxMIB
 * This is the top level oid that we want to register under.  This is essentially a prefix, with the
 * suffix appearing in the variable below.
 */
oid x400pMxMIB_variables_oid[9] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10 };
oid x400pSyncTable_variables_oid[14] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10, 1, 1, 1, 1, 1 };
oid x400pDrivTable_variables_oid[14] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10, 1, 1, 2, 1, 1 };
oid x400pCardTable_variables_oid[14] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10, 1, 1, 3, 2, 1 };
oid x400pSpanTable_variables_oid[14] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10, 1, 1, 4, 1, 1 };
oid x400pXconTable_variables_oid[14] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10, 1, 1, 7, 1, 1 };

/*
 * Oids for use in notifications defined in this MIB.
 */

/*
 * Oids accessible only for notify defined in this MIB.
 */

/*
 * Other oids defined in this MIB.
 */
oid x400pCardTypePLX9030_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10, 1, 4, 1, 1 };
oid x400pCardTypePLXDevelopmentBoard_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10, 1, 4, 1, 2 };
oid x400pCardTypeX400P_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10, 1, 4, 1, 3 };
oid x400pCardTypeE400P_oid[14] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10, 1, 4, 1, 3, 1 };
oid x400pCardTypeT400P_oid[14] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10, 1, 4, 1, 3, 2 };
oid x400pCardTypeX400P_SS7_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10, 1, 4, 1, 4 };
oid x400pCardTypeE400P_SS7_oid[14] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10, 1, 4, 1, 4, 1 };
oid x400pCardTypeT400P_SS7_oid[14] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10, 1, 4, 1, 4, 2 };
oid x400pCardTypeV400P_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10, 1, 4, 1, 5 };
oid x400pCardTypeV400PE_oid[14] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10, 1, 4, 1, 5, 1 };
oid x400pCardTypeV400PT_oid[14] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10, 1, 4, 1, 5, 2 };
oid x400pCardTypeA400P_oid[14] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10, 1, 4, 1, 5, 3 };
oid x400pCardTypeV401PE_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10, 1, 4, 1, 6 };
oid x400pCardTypeV401PT_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10, 1, 4, 1, 7 };
oid x400pCardTypeAT400P_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10, 1, 4, 1, 8 };
oid x400pCardTypeAE400P_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10, 1, 4, 1, 9 };
oid x400pCardTypeA400PT_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10, 1, 4, 1, 10 };
oid x400pCardTypeA400PE_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10, 1, 4, 1, 11 };
oid x400pCardTypeCP100_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10, 1, 4, 1, 12 };
oid x400pCardTypeCP100P_oid[14] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10, 1, 4, 1, 12, 1 };
oid x400pCardTypeCP100E_oid[14] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10, 1, 4, 1, 12, 2 };
oid x400pCardTypeCP200_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10, 1, 4, 1, 13 };
oid x400pCardTypeCP200P_oid[14] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10, 1, 4, 1, 13, 1 };
oid x400pCardTypeCP200E_oid[14] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10, 1, 4, 1, 13, 2 };
oid x400pCardTypeCP400_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10, 1, 4, 1, 14 };
oid x400pCardTypeCP400P_oid[14] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10, 1, 4, 1, 14, 1 };
oid x400pCardTypeCP400E_oid[14] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10, 1, 4, 1, 14, 2 };
oid x400pChipTypeDS2152_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10, 1, 4, 2, 1 };
oid x400pChipTypeDS21352_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10, 1, 4, 2, 2 };
oid x400pChipTypeDS21552_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10, 1, 4, 2, 3 };
oid x400pChipTypeDS2154_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10, 1, 4, 2, 4 };
oid x400pChipTypeDS21354_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10, 1, 4, 2, 5 };
oid x400pChipTypeDS21554_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10, 1, 4, 2, 6 };
oid x400pChipTypeDS2155_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10, 1, 4, 2, 7 };
oid x400pChipTypeDS2156_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10, 1, 4, 2, 8 };
oid x400pChipTypeDS21455_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10, 1, 4, 2, 9 };
oid x400pChipTypeDS21458_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10, 1, 4, 2, 10 };
oid x400pSyncMandatoryGroup_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10, 2, 1, 1 };
oid x400pCardIdGroup_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10, 2, 1, 2 };
oid x400pChipGroup_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10, 2, 1, 3 };
oid x400pPciGroup_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10, 2, 1, 4 };
oid x400pCardGroup_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10, 2, 1, 5 };
oid x400pSpanGroup_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10, 2, 1, 6 };
oid x400pChanGroup_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10, 2, 1, 7 };
oid x400pXconGroup_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10, 2, 1, 8 };
oid x400pBertGroup_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10, 2, 1, 9 };
oid x400pScalarGroup_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10, 2, 1, 10 };
oid x400pNearEndCurrentGroup_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10, 2, 1, 11 };
oid x400pNearEndIntervalGroup_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10, 2, 1, 12 };
oid x400pNearEndTotalGroup_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10, 2, 1, 13 };
oid x400pFarEndCurrentGroup_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10, 2, 1, 14 };
oid x400pFarEndIntervalGroup_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10, 2, 1, 15 };
oid x400pFarEndTotalGroup_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10, 2, 1, 16 };
oid x400pDrivGroup_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10, 2, 1, 17 };
oid x400pBasicCompliance_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10, 2, 2, 1 };
oid x400pEnhancedCompliance_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10, 2, 2, 2 };
static oid zeroDotZero_oid[2] = { 0, 0 };
static oid snmpTrapOID_oid[11] = { 1, 3, 6, 1, 6, 3, 1, 1, 4, 1, 0 };

/*
 * variable7 x400pMxMIB_variables: tree for x400pMxMIB
 * This variable defines function callbacks and type return information for the x400pMxMIB mib section
 */
struct variable7 x400pMxMIB_variables[] = {
	/* magic number, variable type, ro/rw, callback fn, L, oidsuffix */
#define   X400PSYNCSPANID          1
	{(u_char) X400PSYNCSPANID, ASN_OCTET_STR, RWRITE, var_x400pSyncTable, 6, {1, 1, 1, 1, 1, 3}},
#define   X400PSYNCROWSTATUS       2
	{(u_char) X400PSYNCROWSTATUS, ASN_INTEGER, RWRITE, var_x400pSyncTable, 6, {1, 1, 1, 1, 1, 4}},
#define   X400PDRIVIDNUM           3
	{(u_char) X400PDRIVIDNUM, ASN_UNSIGNED, RONLY, var_x400pDrivTable, 6, {1, 1, 2, 1, 1, 2}},
#define   X400PDRIVMAJOR           4
	{(u_char) X400PDRIVMAJOR, ASN_UNSIGNED, RONLY, var_x400pDrivTable, 6, {1, 1, 2, 1, 1, 3}},
#define   X400PDRIVDESCRIPTION     5
	{(u_char) X400PDRIVDESCRIPTION, ASN_OCTET_STR, RONLY, var_x400pDrivTable, 6, {1, 1, 2, 1, 1, 4}},
#define   X400PDRIVREVISION        6
	{(u_char) X400PDRIVREVISION, ASN_OCTET_STR, RONLY, var_x400pDrivTable, 6, {1, 1, 2, 1, 1, 5}},
#define   X400PDRIVCOPYRIGHT       7
	{(u_char) X400PDRIVCOPYRIGHT, ASN_OCTET_STR, RONLY, var_x400pDrivTable, 6, {1, 1, 2, 1, 1, 6}},
#define   X400PDRIVSUPPORTEDDEVICE  8
	{(u_char) X400PDRIVSUPPORTEDDEVICE, ASN_OCTET_STR, RONLY, var_x400pDrivTable, 6, {1, 1, 2, 1, 1, 7}},
#define   X400PDRIVCONTACT         9
	{(u_char) X400PDRIVCONTACT, ASN_OCTET_STR, RONLY, var_x400pDrivTable, 6, {1, 1, 2, 1, 1, 8}},
#define   X400PDRIVLICENSE         10
	{(u_char) X400PDRIVLICENSE, ASN_INTEGER, RONLY, var_x400pDrivTable, 6, {1, 1, 2, 1, 1, 9}},
#define   X400PDRIVDATE            11
	{(u_char) X400PDRIVDATE, ASN_OCTET_STR, RONLY, var_x400pDrivTable, 6, {1, 1, 2, 1, 1, 10}},
#define   X400PDRIVROWSTATUS       12
	{(u_char) X400PDRIVROWSTATUS, ASN_INTEGER, RWRITE, var_x400pDrivTable, 6, {1, 1, 2, 1, 1, 11}},
#define   X400PCARDNEXTINDEX       13
	{(u_char) X400PCARDNEXTINDEX, ASN_UNSIGNED, RONLY, var_x400pMxMIB, 4, {1, 1, 3, 1}},
#define   X400PCARDTYPE            14
	{(u_char) X400PCARDTYPE, ASN_OBJECT_ID, RONLY, var_x400pCardTable, 6, {1, 1, 3, 2, 1, 2}},
#define   X400PCARDIDENTIFIER      15
	{(u_char) X400PCARDIDENTIFIER, ASN_INTEGER, RONLY, var_x400pCardTable, 6, {1, 1, 3, 2, 1, 3}},
#define   X400PCARDREVISION        16
	{(u_char) X400PCARDREVISION, ASN_OCTET_STR, RONLY, var_x400pCardTable, 6, {1, 1, 3, 2, 1, 4}},
#define   X400PCARDCHIPTYPE        17
	{(u_char) X400PCARDCHIPTYPE, ASN_OBJECT_ID, RONLY, var_x400pCardTable, 6, {1, 1, 3, 2, 1, 5}},
#define   X400PCARDCHIPREVISION    18
	{(u_char) X400PCARDCHIPREVISION, ASN_OCTET_STR, RONLY, var_x400pCardTable, 6, {1, 1, 3, 2, 1, 6}},
#define   X400PCARDPCIBUS          19
	{(u_char) X400PCARDPCIBUS, ASN_UNSIGNED, RONLY, var_x400pCardTable, 6, {1, 1, 3, 2, 1, 7}},
#define   X400PCARDPCISLOT         20
	{(u_char) X400PCARDPCISLOT, ASN_UNSIGNED, RONLY, var_x400pCardTable, 6, {1, 1, 3, 2, 1, 8}},
#define   X400PCARDPCIIRQ          21
	{(u_char) X400PCARDPCIIRQ, ASN_UNSIGNED, RONLY, var_x400pCardTable, 6, {1, 1, 3, 2, 1, 9}},
#define   X400PCARDSPANTYPE        22
	{(u_char) X400PCARDSPANTYPE, ASN_INTEGER, RWRITE, var_x400pCardTable, 6, {1, 1, 3, 2, 1, 10}},
#define   X400PCARDMODE            23
	{(u_char) X400PCARDMODE, ASN_OCTET_STR, RWRITE, var_x400pCardTable, 6, {1, 1, 3, 2, 1, 11}},
#define   X400PCARDSYNCMASTER      24
	{(u_char) X400PCARDSYNCMASTER, ASN_INTEGER, RWRITE, var_x400pCardTable, 6, {1, 1, 3, 2, 1, 12}},
#define   X400PCARDSYNCSOURCE      25
	{(u_char) X400PCARDSYNCSOURCE, ASN_INTEGER, RWRITE, var_x400pCardTable, 6, {1, 1, 3, 2, 1, 13}},
#define   X400PCARDSYNCGROUP       26
	{(u_char) X400PCARDSYNCGROUP, ASN_UNSIGNED, RWRITE, var_x400pCardTable, 6, {1, 1, 3, 2, 1, 14}},
#define   X400PCARDADMINISTRATIVESTATE  27
	{(u_char) X400PCARDADMINISTRATIVESTATE, ASN_INTEGER, RWRITE, var_x400pCardTable, 6, {1, 1, 3, 2, 1, 15}},
#define   X400PCARDOPERATIONALSTATE  28
	{(u_char) X400PCARDOPERATIONALSTATE, ASN_INTEGER, RONLY, var_x400pCardTable, 6, {1, 1, 3, 2, 1, 16}},
#define   X400PCARDUSAGESTATE      29
	{(u_char) X400PCARDUSAGESTATE, ASN_INTEGER, RONLY, var_x400pCardTable, 6, {1, 1, 3, 2, 1, 17}},
#define   X400PCARDALARMSTATUS     30
	{(u_char) X400PCARDALARMSTATUS, ASN_OCTET_STR, RWRITE, var_x400pCardTable, 6, {1, 1, 3, 2, 1, 18}},
#define   X400PCARDPROCEDURALSTATUS  31
	{(u_char) X400PCARDPROCEDURALSTATUS, ASN_OCTET_STR, RONLY, var_x400pCardTable, 6, {1, 1, 3, 2, 1, 19}},
#define   X400PCARDAVAILABILITYSTATUS  32
	{(u_char) X400PCARDAVAILABILITYSTATUS, ASN_OCTET_STR, RONLY, var_x400pCardTable, 6, {1, 1, 3, 2, 1, 20}},
#define   X400PCARDCONTROLSTATUS   33
	{(u_char) X400PCARDCONTROLSTATUS, ASN_OCTET_STR, RWRITE, var_x400pCardTable, 6, {1, 1, 3, 2, 1, 21}},
#define   X400PCARDUNKNOWNSTATUS   34
	{(u_char) X400PCARDUNKNOWNSTATUS, ASN_INTEGER, RONLY, var_x400pCardTable, 6, {1, 1, 3, 2, 1, 22}},
#define   X400PCARDSTANDBYSTATUS   35
	{(u_char) X400PCARDSTANDBYSTATUS, ASN_INTEGER, RONLY, var_x400pCardTable, 6, {1, 1, 3, 2, 1, 23}},
#define   X400PCARDSYNCSPANID      36
	{(u_char) X400PCARDSYNCSPANID, ASN_OCTET_STR, RONLY, var_x400pCardTable, 6, {1, 1, 3, 2, 1, 24}},
#define   X400PCARDSYNCTRANSITIONS  37
	{(u_char) X400PCARDSYNCTRANSITIONS, ASN_COUNTER, RONLY, var_x400pCardTable, 6, {1, 1, 3, 2, 1, 25}},
#define   X400PCARDNAME            38
	{(u_char) X400PCARDNAME, ASN_OCTET_STR, RONLY, var_x400pCardTable, 6, {1, 1, 3, 2, 1, 26}},
#define   X400PCARDSTATUS          39
	{(u_char) X400PCARDSTATUS, ASN_INTEGER, RWRITE, var_x400pCardTable, 6, {1, 1, 3, 2, 1, 27}},
#define   X400PSPANNAME            40
	{(u_char) X400PSPANNAME, ASN_OCTET_STR, RWRITE, var_x400pSpanTable, 6, {1, 1, 4, 1, 1, 2}},
#define   X400PSPANDEVICE          41
	{(u_char) X400PSPANDEVICE, ASN_OBJECT_ID, RWRITE, var_x400pSpanTable, 6, {1, 1, 4, 1, 1, 3}},
#define   X400PSPANEQUIPMENTID     42
	{(u_char) X400PSPANEQUIPMENTID, ASN_OCTET_STR, RWRITE, var_x400pSpanTable, 6, {1, 1, 4, 1, 1, 4}},
#define   X400PSPANTYPE            43
	{(u_char) X400PSPANTYPE, ASN_INTEGER, RWRITE, var_x400pSpanTable, 6, {1, 1, 4, 1, 1, 5}},
#define   X400PSPANNUMBER          44
	{(u_char) X400PSPANNUMBER, ASN_UNSIGNED, RWRITE, var_x400pSpanTable, 6, {1, 1, 4, 1, 1, 6}},
#define   X400PSPANRATE            45
	{(u_char) X400PSPANRATE, ASN_INTEGER, RONLY, var_x400pSpanTable, 6, {1, 1, 4, 1, 1, 7}},
#define   X400PSPANMODE            46
	{(u_char) X400PSPANMODE, ASN_OCTET_STR, RWRITE, var_x400pSpanTable, 6, {1, 1, 4, 1, 1, 8}},
#define   X400PSPANCRC             47
	{(u_char) X400PSPANCRC, ASN_INTEGER, RWRITE, var_x400pSpanTable, 6, {1, 1, 4, 1, 1, 9}},
#define   X400PSPANCLOCKING        48
	{(u_char) X400PSPANCLOCKING, ASN_INTEGER, RWRITE, var_x400pSpanTable, 6, {1, 1, 4, 1, 1, 10}},
#define   X400PSPANPRIORITY        49
	{(u_char) X400PSPANPRIORITY, ASN_UNSIGNED, RWRITE, var_x400pSpanTable, 6, {1, 1, 4, 1, 1, 11}},
#define   X400PSPANCODING          50
	{(u_char) X400PSPANCODING, ASN_INTEGER, RWRITE, var_x400pSpanTable, 6, {1, 1, 4, 1, 1, 12}},
#define   X400PSPANFRAMING         51
	{(u_char) X400PSPANFRAMING, ASN_INTEGER, RWRITE, var_x400pSpanTable, 6, {1, 1, 4, 1, 1, 13}},
#define   X400PSPANLINEIMPEDANCE   52
	{(u_char) X400PSPANLINEIMPEDANCE, ASN_INTEGER, RWRITE, var_x400pSpanTable, 6, {1, 1, 4, 1, 1, 14}},
#define   X400PSPANLINEMODE        53
	{(u_char) X400PSPANLINEMODE, ASN_INTEGER, RWRITE, var_x400pSpanTable, 6, {1, 1, 4, 1, 1, 15}},
#define   X400PSPANLINELENGTH      54
	{(u_char) X400PSPANLINELENGTH, ASN_INTEGER, RWRITE, var_x400pSpanTable, 6, {1, 1, 4, 1, 1, 16}},
#define   X400PSPANLINEATTENUATION  55
	{(u_char) X400PSPANLINEATTENUATION, ASN_INTEGER, RWRITE, var_x400pSpanTable, 6, {1, 1, 4, 1, 1, 17}},
#define   X400PSPANLINEGAIN        56
	{(u_char) X400PSPANLINEGAIN, ASN_INTEGER, RWRITE, var_x400pSpanTable, 6, {1, 1, 4, 1, 1, 18}},
#define   X400PSPANLINEDELAY       57
	{(u_char) X400PSPANLINEDELAY, ASN_UNSIGNED, RWRITE, var_x400pSpanTable, 6, {1, 1, 4, 1, 1, 19}},
#define   X400PSPANTXLEVEL         58
	{(u_char) X400PSPANTXLEVEL, ASN_INTEGER, RWRITE, var_x400pSpanTable, 6, {1, 1, 4, 1, 1, 20}},
#define   X400PSPANRXLEVEL         59
	{(u_char) X400PSPANRXLEVEL, ASN_INTEGER, RWRITE, var_x400pSpanTable, 6, {1, 1, 4, 1, 1, 21}},
#define   X400PSPANALARMSETTLETIME  60
	{(u_char) X400PSPANALARMSETTLETIME, ASN_INTEGER, RWRITE, var_x400pSpanTable, 6, {1, 1, 4, 1, 1, 22}},
#define   X400PSPANLINECODETIME    61
	{(u_char) X400PSPANLINECODETIME, ASN_INTEGER, RWRITE, var_x400pSpanTable, 6, {1, 1, 4, 1, 1, 23}},
#define   X400PSPANPRIMARY         62
	{(u_char) X400PSPANPRIMARY, ASN_UNSIGNED, RWRITE, var_x400pSpanTable, 6, {1, 1, 4, 1, 1, 24}},
#define   X400PSPANDATALINK        63
	{(u_char) X400PSPANDATALINK, ASN_OCTET_STR, RWRITE, var_x400pSpanTable, 6, {1, 1, 4, 1, 1, 25}},
#define   X400PSPANLINECODE        64
	{(u_char) X400PSPANLINECODE, ASN_INTEGER, RWRITE, var_x400pSpanTable, 6, {1, 1, 4, 1, 1, 26}},
#define   X400PSPANALARMSEVERITYMAPPROFILE  65
	{(u_char) X400PSPANALARMSEVERITYMAPPROFILE, ASN_OBJECT_ID, RWRITE, var_x400pSpanTable, 6, {1, 1, 4, 1, 1, 27}},
#define   X400PSPANADMINISTRATIVESTATE  66
	{(u_char) X400PSPANADMINISTRATIVESTATE, ASN_INTEGER, RWRITE, var_x400pSpanTable, 6, {1, 1, 4, 1, 1, 28}},
#define   X400PSPANOPERATIONALSTATE  67
	{(u_char) X400PSPANOPERATIONALSTATE, ASN_INTEGER, RONLY, var_x400pSpanTable, 6, {1, 1, 4, 1, 1, 29}},
#define   X400PSPANUSAGESTATE      68
	{(u_char) X400PSPANUSAGESTATE, ASN_INTEGER, RONLY, var_x400pSpanTable, 6, {1, 1, 4, 1, 1, 30}},
#define   X400PSPANALARMSTATUS     69
	{(u_char) X400PSPANALARMSTATUS, ASN_OCTET_STR, RWRITE, var_x400pSpanTable, 6, {1, 1, 4, 1, 1, 31}},
#define   X400PSPANPROCEDURALSTATUS  70
	{(u_char) X400PSPANPROCEDURALSTATUS, ASN_OCTET_STR, RONLY, var_x400pSpanTable, 6, {1, 1, 4, 1, 1, 32}},
#define   X400PSPANAVAILABILITYSTATUS  71
	{(u_char) X400PSPANAVAILABILITYSTATUS, ASN_OCTET_STR, RONLY, var_x400pSpanTable, 6, {1, 1, 4, 1, 1, 33}},
#define   X400PSPANCONTROLSTATUS   72
	{(u_char) X400PSPANCONTROLSTATUS, ASN_OCTET_STR, RWRITE, var_x400pSpanTable, 6, {1, 1, 4, 1, 1, 34}},
#define   X400PSPANSTANDBYSTATUS   73
	{(u_char) X400PSPANSTANDBYSTATUS, ASN_INTEGER, RONLY, var_x400pSpanTable, 6, {1, 1, 4, 1, 1, 35}},
#define   X400PSPANUNKNOWNSTATUS   74
	{(u_char) X400PSPANUNKNOWNSTATUS, ASN_INTEGER, RONLY, var_x400pSpanTable, 6, {1, 1, 4, 1, 1, 36}},
#define   X400PSPANSAP             75
	{(u_char) X400PSPANSAP, ASN_OBJECT_ID, RONLY, var_x400pSpanTable, 6, {1, 1, 4, 1, 1, 37}},
#define   X400PSPANLOOPBACKSTATUS  76
	{(u_char) X400PSPANLOOPBACKSTATUS, ASN_OCTET_STR, RONLY, var_x400pSpanTable, 6, {1, 1, 4, 1, 1, 38}},
#define   X400PSPANLINESTATUS      77
	{(u_char) X400PSPANLINESTATUS, ASN_OCTET_STR, RONLY, var_x400pSpanTable, 6, {1, 1, 4, 1, 1, 39}},
#define   X400PSPANALARMS          78
	{(u_char) X400PSPANALARMS, ASN_OCTET_STR, RONLY, var_x400pSpanTable, 6, {1, 1, 4, 1, 1, 40}},
#define   X400PSPANEVENTS          79
	{(u_char) X400PSPANEVENTS, ASN_OCTET_STR, RONLY, var_x400pSpanTable, 6, {1, 1, 4, 1, 1, 41}},
#define   X400PSPANRECEIVELEVEL    80
	{(u_char) X400PSPANRECEIVELEVEL, ASN_UNSIGNED, RONLY, var_x400pSpanTable, 6, {1, 1, 4, 1, 1, 42}},
#define   X400PSPANRECEIVETHRESHOLD  81
	{(u_char) X400PSPANRECEIVETHRESHOLD, ASN_UNSIGNED, RWRITE, var_x400pSpanTable, 6, {1, 1, 4, 1, 1, 43}},
#define   X400PSPANROWSTATUS       82
	{(u_char) X400PSPANROWSTATUS, ASN_INTEGER, RWRITE, var_x400pSpanTable, 6, {1, 1, 4, 1, 1, 44}},
#define   X400PBERTMODE            83
	{(u_char) X400PBERTMODE, ASN_INTEGER, RWRITE, var_x400pBertTable, 6, {1, 1, 5, 1, 1, 1}},
#define   X400PBERTSELECT          84
	{(u_char) X400PBERTSELECT, ASN_INTEGER, RWRITE, var_x400pBertTable, 6, {1, 1, 5, 1, 1, 2}},
#define   X400PBERTPATTERN         85
	{(u_char) X400PBERTPATTERN, ASN_OCTET_STR, RWRITE, var_x400pBertTable, 6, {1, 1, 5, 1, 1, 3}},
#define   X400PBERTOPERATIONALSTATE  86
	{(u_char) X400PBERTOPERATIONALSTATE, ASN_INTEGER, RONLY, var_x400pBertTable, 6, {1, 1, 5, 1, 1, 4}},
#define   X400PBERTPROCEDURALSTATUS  87
	{(u_char) X400PBERTPROCEDURALSTATUS, ASN_OCTET_STR, RONLY, var_x400pBertTable, 6, {1, 1, 5, 1, 1, 5}},
#define   X400PBERTBITCOUNT        88
	{(u_char) X400PBERTBITCOUNT, ASN_COUNTER, RONLY, var_x400pBertTable, 6, {1, 1, 5, 1, 1, 6}},
#define   X400PBERTERRORCOUNT      89
	{(u_char) X400PBERTERRORCOUNT, ASN_COUNTER, RONLY, var_x400pBertTable, 6, {1, 1, 5, 1, 1, 7}},
#define   X400PCHANTYPE            90
	{(u_char) X400PCHANTYPE, ASN_INTEGER, RWRITE, var_x400pChanTable, 6, {1, 1, 6, 1, 1, 2}},
#define   X400PCHANFORMAT          91
	{(u_char) X400PCHANFORMAT, ASN_INTEGER, RWRITE, var_x400pChanTable, 6, {1, 1, 6, 1, 1, 3}},
#define   X400PCHANRATE            92
	{(u_char) X400PCHANRATE, ASN_INTEGER, RWRITE, var_x400pChanTable, 6, {1, 1, 6, 1, 1, 4}},
#define   X400PCHANMODE            93
	{(u_char) X400PCHANMODE, ASN_OCTET_STR, RWRITE, var_x400pChanTable, 6, {1, 1, 6, 1, 1, 5}},
#define   X400PCHANSAP             94
	{(u_char) X400PCHANSAP, ASN_OBJECT_ID, RONLY, var_x400pChanTable, 6, {1, 1, 6, 1, 1, 6}},
#define   X400PCHANADMINISTRATIVESTATE  95
	{(u_char) X400PCHANADMINISTRATIVESTATE, ASN_INTEGER, RWRITE, var_x400pChanTable, 6, {1, 1, 6, 1, 1, 7}},
#define   X400PCHANOPERATIONALSTATE  96
	{(u_char) X400PCHANOPERATIONALSTATE, ASN_INTEGER, RONLY, var_x400pChanTable, 6, {1, 1, 6, 1, 1, 8}},
#define   X400PCHANUSAGESTATE      97
	{(u_char) X400PCHANUSAGESTATE, ASN_INTEGER, RONLY, var_x400pChanTable, 6, {1, 1, 6, 1, 1, 9}},
#define   X400PCHANAVAILABILITYSTATUS  98
	{(u_char) X400PCHANAVAILABILITYSTATUS, ASN_OCTET_STR, RONLY, var_x400pChanTable, 6, {1, 1, 6, 1, 1, 10}},
#define   X400PCHANCONTROLSTATUS   99
	{(u_char) X400PCHANCONTROLSTATUS, ASN_OCTET_STR, RWRITE, var_x400pChanTable, 6, {1, 1, 6, 1, 1, 11}},
#define   X400PCHANPROCEDURALSTATUS  100
	{(u_char) X400PCHANPROCEDURALSTATUS, ASN_OCTET_STR, RONLY, var_x400pChanTable, 6, {1, 1, 6, 1, 1, 12}},
#define   X400PCHANALARMSTATUS     101
	{(u_char) X400PCHANALARMSTATUS, ASN_OCTET_STR, RWRITE, var_x400pChanTable, 6, {1, 1, 6, 1, 1, 13}},
#define   X400PCHANSTANDBYSTATUS   102
	{(u_char) X400PCHANSTANDBYSTATUS, ASN_INTEGER, RONLY, var_x400pChanTable, 6, {1, 1, 6, 1, 1, 14}},
#define   X400PXCONCARDINDEX       103
	{(u_char) X400PXCONCARDINDEX, ASN_UNSIGNED, RWRITE, var_x400pXconTable, 6, {1, 1, 7, 1, 1, 1}},
#define   X400PXCONSPANINDEX       104
	{(u_char) X400PXCONSPANINDEX, ASN_UNSIGNED, RWRITE, var_x400pXconTable, 6, {1, 1, 7, 1, 1, 2}},
#define   X400PXCONCHANINDEX       105
	{(u_char) X400PXCONCHANINDEX, ASN_UNSIGNED, RWRITE, var_x400pXconTable, 6, {1, 1, 7, 1, 1, 3}},
#define   X400PXCONTYPE            106
	{(u_char) X400PXCONTYPE, ASN_INTEGER, RWRITE, var_x400pXconTable, 6, {1, 1, 7, 1, 1, 4}},
#define   X400PXCONSTORAGETYPE     107
	{(u_char) X400PXCONSTORAGETYPE, ASN_INTEGER, RWRITE, var_x400pXconTable, 6, {1, 1, 7, 1, 1, 5}},
#define   X400PXCONROWSTATUS       108
	{(u_char) X400PXCONROWSTATUS, ASN_INTEGER, RWRITE, var_x400pXconTable, 6, {1, 1, 7, 1, 1, 6}},
#define   X400PDISCONTINUITYTIME   109
	{(u_char) X400PDISCONTINUITYTIME, ASN_TIMETICKS, RONLY, var_x400pMxMIB, 3, {1, 2, 1}},
#define   X400PNEARENDCURRENTTIMEELAPSED  110
	{(u_char) X400PNEARENDCURRENTTIMEELAPSED, ASN_INTEGER, RONLY, var_x400pNearEndCurrentTable, 7, {1, 5, 1, 1, 1, 1, 1}},
#define   X400PNEARENDCURRENTESS   111
	{(u_char) X400PNEARENDCURRENTESS, ASN_GAUGE, RONLY, var_x400pNearEndCurrentTable, 7, {1, 5, 1, 1, 1, 1, 2}},
#define   X400PNEARENDCURRENTSESS  112
	{(u_char) X400PNEARENDCURRENTSESS, ASN_GAUGE, RONLY, var_x400pNearEndCurrentTable, 7, {1, 5, 1, 1, 1, 1, 3}},
#define   X400PNEARENDCURRENTSEFSS  113
	{(u_char) X400PNEARENDCURRENTSEFSS, ASN_GAUGE, RONLY, var_x400pNearEndCurrentTable, 7, {1, 5, 1, 1, 1, 1, 4}},
#define   X400PNEARENDCURRENTUASS  114
	{(u_char) X400PNEARENDCURRENTUASS, ASN_GAUGE, RONLY, var_x400pNearEndCurrentTable, 7, {1, 5, 1, 1, 1, 1, 5}},
#define   X400PNEARENDCURRENTCSSS  115
	{(u_char) X400PNEARENDCURRENTCSSS, ASN_GAUGE, RONLY, var_x400pNearEndCurrentTable, 7, {1, 5, 1, 1, 1, 1, 6}},
#define   X400PNEARENDCURRENTPCVS  116
	{(u_char) X400PNEARENDCURRENTPCVS, ASN_GAUGE, RONLY, var_x400pNearEndCurrentTable, 7, {1, 5, 1, 1, 1, 1, 7}},
#define   X400PNEARENDCURRENTLESS  117
	{(u_char) X400PNEARENDCURRENTLESS, ASN_GAUGE, RONLY, var_x400pNearEndCurrentTable, 7, {1, 5, 1, 1, 1, 1, 8}},
#define   X400PNEARENDCURRENTBESS  118
	{(u_char) X400PNEARENDCURRENTBESS, ASN_GAUGE, RONLY, var_x400pNearEndCurrentTable, 7, {1, 5, 1, 1, 1, 1, 9}},
#define   X400PNEARENDCURRENTDMS   119
	{(u_char) X400PNEARENDCURRENTDMS, ASN_GAUGE, RONLY, var_x400pNearEndCurrentTable, 7, {1, 5, 1, 1, 1, 1, 10}},
#define   X400PNEARENDCURRENTLCVS  120
	{(u_char) X400PNEARENDCURRENTLCVS, ASN_GAUGE, RONLY, var_x400pNearEndCurrentTable, 7, {1, 5, 1, 1, 1, 1, 11}},
#define   X400PNEARENDINTERVALESS  121
	{(u_char) X400PNEARENDINTERVALESS, ASN_GAUGE, RONLY, var_x400pNearEndIntervalTable, 7, {1, 5, 1, 2, 1, 1, 2}},
#define   X400PNEARENDINTERVALSESS  122
	{(u_char) X400PNEARENDINTERVALSESS, ASN_GAUGE, RONLY, var_x400pNearEndIntervalTable, 7, {1, 5, 1, 2, 1, 1, 3}},
#define   X400PNEARENDINTERVALSEFSS  123
	{(u_char) X400PNEARENDINTERVALSEFSS, ASN_GAUGE, RONLY, var_x400pNearEndIntervalTable, 7, {1, 5, 1, 2, 1, 1, 4}},
#define   X400PNEARENDINTERVALUASS  124
	{(u_char) X400PNEARENDINTERVALUASS, ASN_GAUGE, RONLY, var_x400pNearEndIntervalTable, 7, {1, 5, 1, 2, 1, 1, 5}},
#define   X400PNEARENDINTERVALCSSS  125
	{(u_char) X400PNEARENDINTERVALCSSS, ASN_GAUGE, RONLY, var_x400pNearEndIntervalTable, 7, {1, 5, 1, 2, 1, 1, 6}},
#define   X400PNEARENDINTERVALPCVS  126
	{(u_char) X400PNEARENDINTERVALPCVS, ASN_GAUGE, RONLY, var_x400pNearEndIntervalTable, 7, {1, 5, 1, 2, 1, 1, 7}},
#define   X400PNEARENDINTERVALLESS  127
	{(u_char) X400PNEARENDINTERVALLESS, ASN_GAUGE, RONLY, var_x400pNearEndIntervalTable, 7, {1, 5, 1, 2, 1, 1, 8}},
#define   X400PNEARENDINTERVALBESS  128
	{(u_char) X400PNEARENDINTERVALBESS, ASN_GAUGE, RONLY, var_x400pNearEndIntervalTable, 7, {1, 5, 1, 2, 1, 1, 9}},
#define   X400PNEARENDINTERVALDMS  129
	{(u_char) X400PNEARENDINTERVALDMS, ASN_GAUGE, RONLY, var_x400pNearEndIntervalTable, 7, {1, 5, 1, 2, 1, 1, 10}},
#define   X400PNEARENDINTERVALLCVS  130
	{(u_char) X400PNEARENDINTERVALLCVS, ASN_GAUGE, RONLY, var_x400pNearEndIntervalTable, 7, {1, 5, 1, 2, 1, 1, 11}},
#define   X400PNEARENDINTERVALVALIDDATA  131
	{(u_char) X400PNEARENDINTERVALVALIDDATA, ASN_INTEGER, RONLY, var_x400pNearEndIntervalTable, 7, {1, 5, 1, 2, 1, 1, 12}},
#define   X400PNEARENDTOTALVALIDINTERVALS  132
	{(u_char) X400PNEARENDTOTALVALIDINTERVALS, ASN_INTEGER, RONLY, var_x400pNearEndTotalTable, 7, {1, 5, 1, 3, 1, 1, 1}},
#define   X400PNEARENDTOTALINVALIDINTERVALS  133
	{(u_char) X400PNEARENDTOTALINVALIDINTERVALS, ASN_INTEGER, RONLY, var_x400pNearEndTotalTable, 7, {1, 5, 1, 3, 1, 1, 2}},
#define   X400PNEARENDTOTALESS     134
	{(u_char) X400PNEARENDTOTALESS, ASN_GAUGE, RONLY, var_x400pNearEndTotalTable, 7, {1, 5, 1, 3, 1, 1, 3}},
#define   X400PNEARENDTOTALSESS    135
	{(u_char) X400PNEARENDTOTALSESS, ASN_GAUGE, RONLY, var_x400pNearEndTotalTable, 7, {1, 5, 1, 3, 1, 1, 4}},
#define   X400PNEARENDTOTALSEFSS   136
	{(u_char) X400PNEARENDTOTALSEFSS, ASN_GAUGE, RONLY, var_x400pNearEndTotalTable, 7, {1, 5, 1, 3, 1, 1, 5}},
#define   X400PNEARENDTOTALUASS    137
	{(u_char) X400PNEARENDTOTALUASS, ASN_GAUGE, RONLY, var_x400pNearEndTotalTable, 7, {1, 5, 1, 3, 1, 1, 6}},
#define   X400PNEARENDTOTALCSSS    138
	{(u_char) X400PNEARENDTOTALCSSS, ASN_GAUGE, RONLY, var_x400pNearEndTotalTable, 7, {1, 5, 1, 3, 1, 1, 7}},
#define   X400PNEARENDTOTALPCVS    139
	{(u_char) X400PNEARENDTOTALPCVS, ASN_GAUGE, RONLY, var_x400pNearEndTotalTable, 7, {1, 5, 1, 3, 1, 1, 8}},
#define   X400PNEARENDTOTALLESS    140
	{(u_char) X400PNEARENDTOTALLESS, ASN_GAUGE, RONLY, var_x400pNearEndTotalTable, 7, {1, 5, 1, 3, 1, 1, 9}},
#define   X400PNEARENDTOTALBESS    141
	{(u_char) X400PNEARENDTOTALBESS, ASN_GAUGE, RONLY, var_x400pNearEndTotalTable, 7, {1, 5, 1, 3, 1, 1, 10}},
#define   X400PNEARENDTOTALDMS     142
	{(u_char) X400PNEARENDTOTALDMS, ASN_GAUGE, RONLY, var_x400pNearEndTotalTable, 7, {1, 5, 1, 3, 1, 1, 11}},
#define   X400PNEARENDTOTALLCVS    143
	{(u_char) X400PNEARENDTOTALLCVS, ASN_GAUGE, RONLY, var_x400pNearEndTotalTable, 7, {1, 5, 1, 3, 1, 1, 12}},
#define   X400PFARENDCURRENTTIMEELAPSED  144
	{(u_char) X400PFARENDCURRENTTIMEELAPSED, ASN_INTEGER, RONLY, var_x400pFarEndCurrentTable, 7, {1, 5, 2, 1, 1, 1, 1}},
#define   X400PFARENDCURRENTESS    145
	{(u_char) X400PFARENDCURRENTESS, ASN_GAUGE, RONLY, var_x400pFarEndCurrentTable, 7, {1, 5, 2, 1, 1, 1, 2}},
#define   X400PFARENDCURRENTSESS   146
	{(u_char) X400PFARENDCURRENTSESS, ASN_GAUGE, RONLY, var_x400pFarEndCurrentTable, 7, {1, 5, 2, 1, 1, 1, 3}},
#define   X400PFARENDCURRENTSEFSS  147
	{(u_char) X400PFARENDCURRENTSEFSS, ASN_GAUGE, RONLY, var_x400pFarEndCurrentTable, 7, {1, 5, 2, 1, 1, 1, 4}},
#define   X400PFARENDCURRENTUASS   148
	{(u_char) X400PFARENDCURRENTUASS, ASN_GAUGE, RONLY, var_x400pFarEndCurrentTable, 7, {1, 5, 2, 1, 1, 1, 5}},
#define   X400PFARENDCURRENTCSSS   149
	{(u_char) X400PFARENDCURRENTCSSS, ASN_GAUGE, RONLY, var_x400pFarEndCurrentTable, 7, {1, 5, 2, 1, 1, 1, 6}},
#define   X400PFARENDCURRENTPCVS   150
	{(u_char) X400PFARENDCURRENTPCVS, ASN_GAUGE, RONLY, var_x400pFarEndCurrentTable, 7, {1, 5, 2, 1, 1, 1, 7}},
#define   X400PFARENDCURRENTLESS   151
	{(u_char) X400PFARENDCURRENTLESS, ASN_GAUGE, RONLY, var_x400pFarEndCurrentTable, 7, {1, 5, 2, 1, 1, 1, 8}},
#define   X400PFARENDCURRENTBESS   152
	{(u_char) X400PFARENDCURRENTBESS, ASN_GAUGE, RONLY, var_x400pFarEndCurrentTable, 7, {1, 5, 2, 1, 1, 1, 9}},
#define   X400PFARENDCURRENTDMS    153
	{(u_char) X400PFARENDCURRENTDMS, ASN_GAUGE, RONLY, var_x400pFarEndCurrentTable, 7, {1, 5, 2, 1, 1, 1, 10}},
#define   X400PFARENDINTERVALESS   154
	{(u_char) X400PFARENDINTERVALESS, ASN_GAUGE, RONLY, var_x400pFarEndIntervalTable, 7, {1, 5, 2, 2, 1, 1, 2}},
#define   X400PFARENDINTERVALSESS  155
	{(u_char) X400PFARENDINTERVALSESS, ASN_GAUGE, RONLY, var_x400pFarEndIntervalTable, 7, {1, 5, 2, 2, 1, 1, 3}},
#define   X400PFARENDINTERVALSEFSS  156
	{(u_char) X400PFARENDINTERVALSEFSS, ASN_GAUGE, RONLY, var_x400pFarEndIntervalTable, 7, {1, 5, 2, 2, 1, 1, 4}},
#define   X400PFARENDINTERVALUASS  157
	{(u_char) X400PFARENDINTERVALUASS, ASN_GAUGE, RONLY, var_x400pFarEndIntervalTable, 7, {1, 5, 2, 2, 1, 1, 5}},
#define   X400PFARENDINTERVALCSSS  158
	{(u_char) X400PFARENDINTERVALCSSS, ASN_GAUGE, RONLY, var_x400pFarEndIntervalTable, 7, {1, 5, 2, 2, 1, 1, 6}},
#define   X400PFARENDINTERVALPCVS  159
	{(u_char) X400PFARENDINTERVALPCVS, ASN_GAUGE, RONLY, var_x400pFarEndIntervalTable, 7, {1, 5, 2, 2, 1, 1, 7}},
#define   X400PFARENDINTERVALLESS  160
	{(u_char) X400PFARENDINTERVALLESS, ASN_GAUGE, RONLY, var_x400pFarEndIntervalTable, 7, {1, 5, 2, 2, 1, 1, 8}},
#define   X400PFARENDINTERVALBESS  161
	{(u_char) X400PFARENDINTERVALBESS, ASN_GAUGE, RONLY, var_x400pFarEndIntervalTable, 7, {1, 5, 2, 2, 1, 1, 9}},
#define   X400PFARENDINTERVALDMS   162
	{(u_char) X400PFARENDINTERVALDMS, ASN_GAUGE, RONLY, var_x400pFarEndIntervalTable, 7, {1, 5, 2, 2, 1, 1, 10}},
#define   X400PFARENDINTERVALVALIDDATA  163
	{(u_char) X400PFARENDINTERVALVALIDDATA, ASN_INTEGER, RONLY, var_x400pFarEndIntervalTable, 7, {1, 5, 2, 2, 1, 1, 11}},
#define   X400PFARENDTOTALVALIDINTERVALS  164
	{(u_char) X400PFARENDTOTALVALIDINTERVALS, ASN_INTEGER, RONLY, var_x400pFarEndTotalTable, 7, {1, 5, 2, 3, 1, 1, 1}},
#define   X400PFARENDTOTALINVALIDINTERVALS  165
	{(u_char) X400PFARENDTOTALINVALIDINTERVALS, ASN_INTEGER, RONLY, var_x400pFarEndTotalTable, 7, {1, 5, 2, 3, 1, 1, 2}},
#define   X400PFARENDTOTALESS      166
	{(u_char) X400PFARENDTOTALESS, ASN_GAUGE, RONLY, var_x400pFarEndTotalTable, 7, {1, 5, 2, 3, 1, 1, 3}},
#define   X400PFARENDTOTALSESS     167
	{(u_char) X400PFARENDTOTALSESS, ASN_GAUGE, RONLY, var_x400pFarEndTotalTable, 7, {1, 5, 2, 3, 1, 1, 4}},
#define   X400PFARENDTOTALSEFSS    168
	{(u_char) X400PFARENDTOTALSEFSS, ASN_GAUGE, RONLY, var_x400pFarEndTotalTable, 7, {1, 5, 2, 3, 1, 1, 5}},
#define   X400PFARENDTOTALUASS     169
	{(u_char) X400PFARENDTOTALUASS, ASN_GAUGE, RONLY, var_x400pFarEndTotalTable, 7, {1, 5, 2, 3, 1, 1, 6}},
#define   X400PFARENDTOTALCSSS     170
	{(u_char) X400PFARENDTOTALCSSS, ASN_GAUGE, RONLY, var_x400pFarEndTotalTable, 7, {1, 5, 2, 3, 1, 1, 7}},
#define   X400PFARENDTOTALPCVS     171
	{(u_char) X400PFARENDTOTALPCVS, ASN_GAUGE, RONLY, var_x400pFarEndTotalTable, 7, {1, 5, 2, 3, 1, 1, 8}},
#define   X400PFARENDTOTALLESS     172
	{(u_char) X400PFARENDTOTALLESS, ASN_GAUGE, RONLY, var_x400pFarEndTotalTable, 7, {1, 5, 2, 3, 1, 1, 9}},
#define   X400PFARENDTOTALBESS     173
	{(u_char) X400PFARENDTOTALBESS, ASN_GAUGE, RONLY, var_x400pFarEndTotalTable, 7, {1, 5, 2, 3, 1, 1, 10}},
#define   X400PFARENDTOTALDMS      174
	{(u_char) X400PFARENDTOTALDMS, ASN_GAUGE, RONLY, var_x400pFarEndTotalTable, 7, {1, 5, 2, 3, 1, 1, 11}},
};

/* (L = length of the oidsuffix) */
struct x400pMxMIB_data *x400pMxMIBStorage = NULL;

/* global storage of our data, saved in and configured by header_complex() */
struct header_complex_index *x400pSyncTableStorage = NULL;
struct header_complex_index *x400pDrivTableStorage = NULL;
struct header_complex_index *x400pCardTableStorage = NULL;
struct header_complex_index *x400pSpanTableStorage = NULL;
struct header_complex_index *x400pBertTableStorage = NULL;
struct header_complex_index *x400pChanTableStorage = NULL;
struct header_complex_index *x400pXconTableStorage = NULL;
struct header_complex_index *x400pNearEndCurrentTableStorage = NULL;
struct header_complex_index *x400pNearEndIntervalTableStorage = NULL;
struct header_complex_index *x400pNearEndTotalTableStorage = NULL;
struct header_complex_index *x400pFarEndCurrentTableStorage = NULL;
struct header_complex_index *x400pFarEndIntervalTableStorage = NULL;
struct header_complex_index *x400pFarEndTotalTableStorage = NULL;

void (*x400pMxMIBold_signal_handler) (int) = NULL;	/* save old signal handler just in case */
void x400pMxMIB_loop_handler(int);
void x400pMxMIB_fd_handler(int, void *);

/**
 * @fn void init_x400pMxMIB(void)
 * @brief x400pMxMIB initialization routine.
 *
 * This is called when the agent starts up.  At a minimum, registration of the MIB variables
 * structure (x400pMxMIB_variables) should take place here.  By default the function also
 * registers the configuration handler and configuration store callbacks.
 *
 * Additional registrations that may be considered here are calls to regsiter_readfd(),
 * register_writefd() and register_exceptfd() for hooking into the snmpd event loop, but only when
 * used as a loadable module.  By default this function establishes a single file descriptor to
 * read, or upon which to handle exceptions.  Note that the snmpd only supports a maximum of 32
 * extneral file descriptors, so these should be used sparingly.
 *
 * When running as a loadable module, it is also necessary to hook into the snmpd event loop so that
 * the current request number can be deteremined.  This is accomplished by using a trick of the
 * external_signal_scheduled and external_signal_handler mechanism which is called on each event
 * loop when external_signal_scheduled is non-zero.  This is used to increment the sa_request value
 * on each snmpd event loop interation so that calls to MIB tree functions can determine whether
 * they belong to a fresh request or not (primarily for cacheing and possibly to clean up non-polled
 * file descriptors).
 */
void
init_x400pMxMIB(void)
{
	(void) my_fd;
	(void) zeroDotZero_oid;
	(void) snmpTrapOID_oid;
	DEBUGMSGTL(("x400pMxMIB", "init_x400pMxMIB: initializing...  "));
	/* register ourselves with the agent to handle our mib tree */
	REGISTER_MIB("x400pMxMIB", x400pMxMIB_variables, variable7, x400pMxMIB_variables_oid);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_SHUTDOWN, term_x400pMxMIB, NULL);
	/* register our config handler(s) to deal with registrations */
	snmpd_register_config_handler("x400pMxMIB", parse_x400pMxMIB, NULL, "HELP STRING");
	snmpd_register_config_handler("x400pSyncTable", parse_x400pSyncTable, NULL, "HELP STRING");
	snmpd_register_config_handler("x400pDrivTable", parse_x400pDrivTable, NULL, "HELP STRING");
	snmpd_register_config_handler("x400pCardTable", parse_x400pCardTable, NULL, "HELP STRING");
	snmpd_register_config_handler("x400pSpanTable", parse_x400pSpanTable, NULL, "HELP STRING");
	snmpd_register_config_handler("x400pBertTable", parse_x400pBertTable, NULL, "HELP STRING");
	snmpd_register_config_handler("x400pChanTable", parse_x400pChanTable, NULL, "HELP STRING");
	snmpd_register_config_handler("x400pXconTable", parse_x400pXconTable, NULL, "HELP STRING");
	snmpd_register_config_handler("x400pNearEndCurrentTable", parse_x400pNearEndCurrentTable, NULL, "HELP STRING");
	snmpd_register_config_handler("x400pNearEndIntervalTable", parse_x400pNearEndIntervalTable, NULL, "HELP STRING");
	snmpd_register_config_handler("x400pNearEndTotalTable", parse_x400pNearEndTotalTable, NULL, "HELP STRING");
	snmpd_register_config_handler("x400pFarEndCurrentTable", parse_x400pFarEndCurrentTable, NULL, "HELP STRING");
	snmpd_register_config_handler("x400pFarEndIntervalTable", parse_x400pFarEndIntervalTable, NULL, "HELP STRING");
	snmpd_register_config_handler("x400pFarEndTotalTable", parse_x400pFarEndTotalTable, NULL, "HELP STRING");

	/* we need to be called back later to store our data */
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_x400pMxMIB, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_x400pSyncTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_x400pDrivTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_x400pCardTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_x400pSpanTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_x400pBertTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_x400pChanTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_x400pXconTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_x400pNearEndCurrentTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_x400pNearEndIntervalTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_x400pNearEndTotalTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_x400pFarEndCurrentTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_x400pFarEndIntervalTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_x400pFarEndTotalTable, NULL);

	/* place any other initialization junk you need here */
	if (my_readfd >= 0) {
		register_readfd(my_readfd, x400pMxMIB_fd_handler, (void *) 0);
		register_exceptfd(my_readfd, x400pMxMIB_fd_handler, (void *) 1);
	}
	x400pMxMIBold_signal_handler = external_signal_handler[SIGCHLD];
	external_signal_handler[SIGCHLD] = &x400pMxMIB_loop_handler;
	external_signal_scheduled[SIGCHLD] = 1;
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	init_ds1EXT();
	init_phyMIB();
	init_ds0Bundle();
	init_ds0();
}

/**
 * @fn void deinit_x400pMxMIB(void)
 * @brief deinitialization routine.
 *
 * This is called before the agent is unloaded.  At a minimum, deregistration of the MIB variables
 * structure (x400pMxMIB_variables) should take place here.  By default, the function also
 * deregisters the the configuration file handlers for the MIB variables and table rows.
 *
 * Additional deregistrations that may be required here are calls to unregister_readfd(),
 * unregister_writefd() and unregsiter_exceptfd() for unhooking from the snmpd event loop, but only
 * when used as a loadable module.  By default if a read file descriptor exists, it is unregistered.
 */
void
deinit_x400pMxMIB(void)
{
	deinit_ds0();
	deinit_ds0Bundle();
	deinit_phyMIB();
	deinit_ds1EXT();
	DEBUGMSGTL(("x400pMxMIB", "deinit_x400pMxMIB: deinitializating...  "));
	external_signal_handler[SIGCHLD] = x400pMxMIBold_signal_handler;
	if (my_readfd >= 0) {
		unregister_exceptfd(my_readfd);
		unregister_readfd(my_readfd);
		close(my_readfd);
		my_readfd = -1;
	}
	unregister_mib(x400pMxMIB_variables_oid, sizeof(x400pMxMIB_variables_oid) / sizeof(oid));
	snmpd_unregister_config_handler("x400pMxMIB");
	snmpd_unregister_config_handler("x400pSyncTable");
	snmpd_unregister_config_handler("x400pDrivTable");
	snmpd_unregister_config_handler("x400pCardTable");
	snmpd_unregister_config_handler("x400pSpanTable");
	snmpd_unregister_config_handler("x400pBertTable");
	snmpd_unregister_config_handler("x400pChanTable");
	snmpd_unregister_config_handler("x400pXconTable");
	snmpd_unregister_config_handler("x400pNearEndCurrentTable");
	snmpd_unregister_config_handler("x400pNearEndIntervalTable");
	snmpd_unregister_config_handler("x400pNearEndTotalTable");
	snmpd_unregister_config_handler("x400pFarEndCurrentTable");
	snmpd_unregister_config_handler("x400pFarEndIntervalTable");
	snmpd_unregister_config_handler("x400pFarEndTotalTable");

	/* place any other de-initialization junk you need here */
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
}

int
term_x400pMxMIB(int majorID, int minorID, void *serverarg, void *clientarg)
{
	DEBUGMSGTL(("x400pMxMIB", "term_x400pMxMIB: terminating...  "));
	deinit_x400pMxMIB();
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return 0;
}

/**
 * @fn struct x400pMxMIB_data *x400pMxMIB_create(void)
 * @brief create a fresh data structure representing scalars in x400pMxMIB.
 *
 * Creates a new x400pMxMIB_data structure by allocating dynamic memory for the structure and
 * initializing the default values of scalars in x400pMxMIB.
 */
struct x400pMxMIB_data *
x400pMxMIB_create(void)
{
	struct x400pMxMIB_data *StorageNew = SNMP_MALLOC_STRUCT(x400pMxMIB_data);

	DEBUGMSGTL(("x400pMxMIB", "x400pMxMIB_create: creating scalars...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default scalar values here into StorageNew */
		StorageNew->x400pCardNextIndex = 0;
		StorageNew->x400pDiscontinuityTime = 0;

	}
      done:
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	x400pMxMIB_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct x400pMxMIB_data *x400pMxMIB_duplicate(struct x400pMxMIB_data *thedata)
 * @param thedata the mib structure to duplicate
 * @brief duplicate a mib structure for the mib
 *
 * Duplicates the specified mib structure @param thedata and returns a pointer to the newly
 * allocated mib structure on success, or NULL on failure.
 */
struct x400pMxMIB_data *
x400pMxMIB_duplicate(struct x400pMxMIB_data *thedata)
{
	struct x400pMxMIB_data *StorageNew = SNMP_MALLOC_STRUCT(x400pMxMIB_data);

	DEBUGMSGTL(("x400pMxMIB", "x400pMxMIB_duplicate: duplicating mib... "));
	if (StorageNew != NULL) {
		StorageNew->x400pCardNextIndex = thedata->x400pCardNextIndex;
		StorageNew->x400pDiscontinuityTime = thedata->x400pDiscontinuityTime;
	}
      done:
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	x400pMxMIB_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int x400pMxMIB_destroy(struct x400pMxMIB_data **thedata)
 * @param thedata pointer to the data structure in x400pMxMIB.
 * @brief delete a scalars structure from x400pMxMIB.
 *
 * Frees scalars that were previously removed from x400pMxMIB.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
x400pMxMIB_destroy(struct x400pMxMIB_data **thedata)
{
	struct x400pMxMIB_data *StorageDel;

	DEBUGMSGTL(("x400pMxMIB", "x400pMxMIB_destroy: deleting scalars...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int x400pMxMIB_add(struct x400pMxMIB_data *thedata)
 * @param thedata the structure representing x400pMxMIB scalars.
 * @brief adds node to the x400pMxMIB scalar data set.
 *
 * Adds a scalar structure to the x400pMxMIB data set.  Note that this function is necessary even
 * when the scalar values are not peristent.
 */
int
x400pMxMIB_add(struct x400pMxMIB_data *thedata)
{
	DEBUGMSGTL(("x400pMxMIB", "x400pMxMIB_add: adding data...  "));
	if (thedata)
		x400pMxMIBStorage = thedata;
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_x400pMxMIB(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for x400pMxMIB entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case x400pMxMIB).  This routine is invoked by
 * UCD-SNMP to read the values of scalars in the MIB from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the MIB.  If there are no configured entries
 * in the configuration MIB, this function will simply not be called.
 */
void
parse_x400pMxMIB(const char *token, char *line)
{
	size_t tmpsize;
	struct x400pMxMIB_data *StorageTmp = x400pMxMIB_create();

	DEBUGMSGTL(("x400pMxMIB", "parse_x400pMxMIB: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual scalars that are not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->x400pCardNextIndex, &tmpsize);
	line = read_config_read_data(ASN_TIMETICKS, line, &StorageTmp->x400pDiscontinuityTime, &tmpsize);
	x400pMxMIB_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
}

/*
 * store_x400pMxMIB(): stores .conf file entries needed to configure the mib.
 */
int
store_x400pMxMIB(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct x400pMxMIB_data *StorageTmp;

	DEBUGMSGTL(("x400pMxMIB", "store_x400pMxMIB: storing data...  "));
	refresh_x400pMxMIB(1);
	if ((StorageTmp = x400pMxMIBStorage) == NULL) {
		DEBUGMSGTL(("x400pMxMIB", "error.\n"));
		return SNMPERR_GENERR;
	}
	(void) tmpsize;
	/* XXX: comment entire section if no scalars are persistent */
	{
		memset(line, 0, sizeof(line));
		strcat(line, "x400pMxMIB ");
		cptr = line + strlen(line);
		(void) cptr;
		/* XXX: remove individual scalars that are not persistent */
		cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->x400pCardNextIndex, &tmpsize);
		cptr = read_config_store_data(ASN_TIMETICKS, cptr, &StorageTmp->x400pDiscontinuityTime, &tmpsize);
		snmpd_store_config(line);
	}
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int check_x400pMxMIB(struct x400pMxMIB_data *StorageTmp, struct x400pMxMIB_data *StorageOld)
 * @param StorageTmp the data as updated
 * @param StorageOld the data previous to update
 *
 * This function is used by mibs.  It is used to check, all scalars at a time, the varbinds
 * belonging to the mib.  This function is called for the first varbind in a mib at the beginning of
 * the ACTION phase.  The COMMIT phase does not ensue unless this check passes.  This function can
 * return SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before
 * the varbinds on the mib were applied; the values in StorageTmp are the new values.  The function
 * is permitted to change the values in StorageTmp to correct them; however, preferences should be
 * made for setting values that were not in the varbinds.
 */
int
check_x400pMxMIB(struct x400pMxMIB_data *StorageTmp, struct x400pMxMIB_data *StorageOld)
{
	/* XXX: provide code to check the scalars for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_x400pMxMIB(struct x400pMxMIB_data *StorageTmp, struct x400pMxMIB_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase)
 *
 * This function is used by mibs.  It is used to update, all scalars at a time, the varbinds
 * belonging to the mib.  This function is called for the first varbind in a mib at the beginning of
 * the COMMIT phase.  The start of the ACTION phase performs a consistency check on the mib before
 * allowing the request to proceed to the COMMIT phase.  The COMMIT phase then arrives here with
 * consistency already checked (see check_x400pMxMIB()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the mib were applied: the values in StorageTmp are the new values.
 */
int
update_x400pMxMIB(struct x400pMxMIB_data *StorageTmp, struct x400pMxMIB_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	x400pMxMIB_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn revert_x400pMxMIB(struct 
 * @fn void revert_x400pMxMIB(struct x400pMxMIB_data *StorageTmp, struct x400pMxMIB_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase)
 */
void
revert_x400pMxMIB(struct x400pMxMIB_data *StorageTmp, struct x400pMxMIB_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_x400pMxMIB(StorageOld, NULL);
}

/**
 * @fn void refresh_x400pMxMIB(int force)
 * @param force forced refresh when non-zero.
 * @brief refresh the scalar values of x400pMxMIB.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a scalar has been requested).
 */
void
refresh_x400pMxMIB(int force)
{
	if (x400pMxMIBStorage == NULL) {
		struct x400pMxMIB_data *StorageNew;

		if ((StorageNew = x400pMxMIB_create()) == NULL)
			return;
		x400pMxMIBStorage = StorageNew;
		x400pMxMIB_refresh = 1;
	}
	if (!force && x400pMxMIB_refresh == 0)
		return;
	DEBUGMSGTL(("x400pMxMIB", "refresh_x400pMxMIB: refreshing...  "));
	/* XXX: Update scalars as required here... */
	x400pMxMIB_refresh = 0;
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
}

/**
 * @fn u_char * var_x400pMxMIB(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @param vp a pointer to the entry in the variables table for the requested variable.
 * @param name the object identifier for which to find.
 * @param length the length of the object identifier.
 * @param exact whether the name is exact.
 * @param var_len a pointer to the length of the representation of the object.
 * @param write_method a pointer to a write method for the object.
 * @brief locate variables in x400pMxMIB.
 *
 * This function returns a pointer to a memory area that is static across the request that contains
 * the UCD-SNMP representation of the scalar (so that it may be used to read from for a GET,
 * GET-NEXT or GET-BULK request).  This returned pointer may be NULL, in which case the function is
 * telling UCD-SNMP that the scalar does not exist for reading; however, if write_method is
 * overwritten with a non-NULL value, the function is telling UCD-SNMP that the scalar exists for
 * writing.  Write-only objects can be effected in this way.
 */
u_char *
var_x400pMxMIB(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct x400pMxMIB_data *StorageTmp;
	u_char *rval;

	DEBUGMSGTL(("x400pMxMIB", "var_x400pMxMIB: lookup up varbind...  "));
	if (header_generic(vp, name, length, exact, var_len, write_method) == MATCH_FAILED)
		return NULL;
	/* Refresh the MIB values if required. */
	refresh_x400pMxMIB(0);
	if ((StorageTmp = x400pMxMIBStorage) == NULL) {
		DEBUGMSGTL(("x400pMxMIB", "no datastructure.\n"));
		return NULL;
	}
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) X400PCARDNEXTINDEX:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pCardNextIndex);
		rval = (u_char *) &StorageTmp->x400pCardNextIndex;
		break;
	case (u_char) X400PDISCONTINUITYTIME:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pDiscontinuityTime);
		rval = (u_char *) &StorageTmp->x400pDiscontinuityTime;
		break;
	default:
		ERROR_MSG("");
	}
	if (rval)
		DEBUGMSGTL(("x400pMxMIB", "found.\n"));
	else
		DEBUGMSGTL(("x400pMxMIB", "not found.\n"));
	return (rval);
}

/**
 * @fn struct x400pSyncTable_data *x400pSyncTable_create(void)
 * @brief create a fresh data structure representing a new row in the x400pSyncTable table.
 *
 * Creates a new x400pSyncTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct x400pSyncTable_data *
x400pSyncTable_create(void)
{
	struct x400pSyncTable_data *StorageNew = SNMP_MALLOC_STRUCT(x400pSyncTable_data);

	DEBUGMSGTL(("x400pMxMIB", "x400pSyncTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->x400pSyncSpanId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->x400pSyncSpanIdLen = 0;
		StorageNew->x400pSyncSpanId[StorageNew->x400pSyncSpanIdLen] = 0;
		StorageNew->x400pSyncRowStatus = 0;
		StorageNew->x400pSyncRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	x400pSyncTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct x400pSyncTable_data *x400pSyncTable_duplicate(struct x400pSyncTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct x400pSyncTable_data *
x400pSyncTable_duplicate(struct x400pSyncTable_data *thedata)
{
	struct x400pSyncTable_data *StorageNew = SNMP_MALLOC_STRUCT(x400pSyncTable_data);

	DEBUGMSGTL(("x400pMxMIB", "x400pSyncTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->x400pSyncTable_id = thedata->x400pSyncTable_id;
		StorageNew->x400pSyncGroup = thedata->x400pSyncGroup;
		StorageNew->x400pSyncIndex = thedata->x400pSyncIndex;
		if (!(StorageNew->x400pSyncSpanId = malloc(thedata->x400pSyncSpanIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->x400pSyncSpanId, thedata->x400pSyncSpanId, thedata->x400pSyncSpanIdLen);
		StorageNew->x400pSyncSpanIdLen = thedata->x400pSyncSpanIdLen;
		StorageNew->x400pSyncSpanId[StorageNew->x400pSyncSpanIdLen] = 0;
		StorageNew->x400pSyncRowStatus = thedata->x400pSyncRowStatus;
	}
      done:
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	x400pSyncTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int x400pSyncTable_destroy(struct x400pSyncTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
x400pSyncTable_destroy(struct x400pSyncTable_data **thedata)
{
	struct x400pSyncTable_data *StorageDel;

	DEBUGMSGTL(("x400pMxMIB", "x400pSyncTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->x400pSyncSpanId);
		StorageDel->x400pSyncSpanIdLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int x400pSyncTable_add(struct x400pSyncTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the x400pSyncTable table data set.
 *
 * Adds a table row structure to the x400pSyncTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
x400pSyncTable_add(struct x400pSyncTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("x400pMxMIB", "x400pSyncTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* x400pSyncGroup */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->x400pSyncGroup, sizeof(thedata->x400pSyncGroup));
		/* x400pSyncIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->x400pSyncIndex, sizeof(thedata->x400pSyncIndex));
		header_complex_add_data(&x400pSyncTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("x400pMxMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int x400pSyncTable_del(struct x400pSyncTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the x400pSyncTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
x400pSyncTable_del(struct x400pSyncTable_data *thedata)
{
	struct x400pSyncTable_data *StorageDel;

	DEBUGMSGTL(("x400pMxMIB", "x400pSyncTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(x400pSyncTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&x400pSyncTableStorage, hciptr);
	}
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_x400pSyncTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for x400pSyncTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case x400pSyncTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_x400pSyncTable(const char *token, char *line)
{
	size_t tmpsize;
	struct x400pSyncTable_data *StorageTmp = x400pSyncTable_create();

	DEBUGMSGTL(("x400pMxMIB", "parse_x400pSyncTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->x400pSyncGroup, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->x400pSyncIndex, &tmpsize);
	SNMP_FREE(StorageTmp->x400pSyncSpanId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->x400pSyncSpanId, &StorageTmp->x400pSyncSpanIdLen);
	if (StorageTmp->x400pSyncSpanId == NULL) {
		config_perror("invalid specification for x400pSyncSpanId");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pSyncRowStatus, &tmpsize);
	x400pSyncTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
}

/*
 * store_x400pSyncTable(): store configuraiton file for x400pSyncTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_x400pSyncTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct x400pSyncTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("x400pMxMIB", "store_x400pSyncTable: storing data...  "));
	refresh_x400pSyncTable(1);
	(void) tmpsize;
	for (hcindex = x400pSyncTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct x400pSyncTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "x400pSyncTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->x400pSyncGroup, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->x400pSyncIndex, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->x400pSyncSpanId, &StorageTmp->x400pSyncSpanIdLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pSyncRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct x400pDrivTable_data *x400pDrivTable_create(void)
 * @brief create a fresh data structure representing a new row in the x400pDrivTable table.
 *
 * Creates a new x400pDrivTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct x400pDrivTable_data *
x400pDrivTable_create(void)
{
	struct x400pDrivTable_data *StorageNew = SNMP_MALLOC_STRUCT(x400pDrivTable_data);

	DEBUGMSGTL(("x400pMxMIB", "x400pDrivTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->x400pDrivIdnum = 0;
		StorageNew->x400pDrivMajor = 0;
		if ((StorageNew->x400pDrivDescription = malloc(1)) == NULL)
			goto nomem;
		StorageNew->x400pDrivDescriptionLen = 0;
		StorageNew->x400pDrivDescription[StorageNew->x400pDrivDescriptionLen] = 0;
		if ((StorageNew->x400pDrivRevision = malloc(1)) == NULL)
			goto nomem;
		StorageNew->x400pDrivRevisionLen = 0;
		StorageNew->x400pDrivRevision[StorageNew->x400pDrivRevisionLen] = 0;
		if ((StorageNew->x400pDrivCopyright = malloc(1)) == NULL)
			goto nomem;
		StorageNew->x400pDrivCopyrightLen = 0;
		StorageNew->x400pDrivCopyright[StorageNew->x400pDrivCopyrightLen] = 0;
		if ((StorageNew->x400pDrivSupportedDevice = malloc(1)) == NULL)
			goto nomem;
		StorageNew->x400pDrivSupportedDeviceLen = 0;
		StorageNew->x400pDrivSupportedDevice[StorageNew->x400pDrivSupportedDeviceLen] = 0;
		if ((StorageNew->x400pDrivContact = malloc(1)) == NULL)
			goto nomem;
		StorageNew->x400pDrivContactLen = 0;
		StorageNew->x400pDrivContact[StorageNew->x400pDrivContactLen] = 0;
		StorageNew->x400pDrivLicense = 0;
		if ((StorageNew->x400pDrivDate = malloc(1)) == NULL)
			goto nomem;
		StorageNew->x400pDrivDateLen = 0;
		StorageNew->x400pDrivDate[StorageNew->x400pDrivDateLen] = 0;
		StorageNew->x400pDrivRowStatus = 0;
		StorageNew->x400pDrivRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	x400pDrivTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct x400pDrivTable_data *x400pDrivTable_duplicate(struct x400pDrivTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct x400pDrivTable_data *
x400pDrivTable_duplicate(struct x400pDrivTable_data *thedata)
{
	struct x400pDrivTable_data *StorageNew = SNMP_MALLOC_STRUCT(x400pDrivTable_data);

	DEBUGMSGTL(("x400pMxMIB", "x400pDrivTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->x400pDrivTable_id = thedata->x400pDrivTable_id;
		if (!(StorageNew->x400pDrivName = malloc(thedata->x400pDrivNameLen + 1)))
			goto destroy;
		memcpy(StorageNew->x400pDrivName, thedata->x400pDrivName, thedata->x400pDrivNameLen);
		StorageNew->x400pDrivNameLen = thedata->x400pDrivNameLen;
		StorageNew->x400pDrivName[StorageNew->x400pDrivNameLen] = 0;
		StorageNew->x400pDrivIdnum = thedata->x400pDrivIdnum;
		StorageNew->x400pDrivMajor = thedata->x400pDrivMajor;
		if (!(StorageNew->x400pDrivDescription = malloc(thedata->x400pDrivDescriptionLen + 1)))
			goto destroy;
		memcpy(StorageNew->x400pDrivDescription, thedata->x400pDrivDescription, thedata->x400pDrivDescriptionLen);
		StorageNew->x400pDrivDescriptionLen = thedata->x400pDrivDescriptionLen;
		StorageNew->x400pDrivDescription[StorageNew->x400pDrivDescriptionLen] = 0;
		if (!(StorageNew->x400pDrivRevision = malloc(thedata->x400pDrivRevisionLen + 1)))
			goto destroy;
		memcpy(StorageNew->x400pDrivRevision, thedata->x400pDrivRevision, thedata->x400pDrivRevisionLen);
		StorageNew->x400pDrivRevisionLen = thedata->x400pDrivRevisionLen;
		StorageNew->x400pDrivRevision[StorageNew->x400pDrivRevisionLen] = 0;
		if (!(StorageNew->x400pDrivCopyright = malloc(thedata->x400pDrivCopyrightLen + 1)))
			goto destroy;
		memcpy(StorageNew->x400pDrivCopyright, thedata->x400pDrivCopyright, thedata->x400pDrivCopyrightLen);
		StorageNew->x400pDrivCopyrightLen = thedata->x400pDrivCopyrightLen;
		StorageNew->x400pDrivCopyright[StorageNew->x400pDrivCopyrightLen] = 0;
		if (!(StorageNew->x400pDrivSupportedDevice = malloc(thedata->x400pDrivSupportedDeviceLen + 1)))
			goto destroy;
		memcpy(StorageNew->x400pDrivSupportedDevice, thedata->x400pDrivSupportedDevice, thedata->x400pDrivSupportedDeviceLen);
		StorageNew->x400pDrivSupportedDeviceLen = thedata->x400pDrivSupportedDeviceLen;
		StorageNew->x400pDrivSupportedDevice[StorageNew->x400pDrivSupportedDeviceLen] = 0;
		if (!(StorageNew->x400pDrivContact = malloc(thedata->x400pDrivContactLen + 1)))
			goto destroy;
		memcpy(StorageNew->x400pDrivContact, thedata->x400pDrivContact, thedata->x400pDrivContactLen);
		StorageNew->x400pDrivContactLen = thedata->x400pDrivContactLen;
		StorageNew->x400pDrivContact[StorageNew->x400pDrivContactLen] = 0;
		StorageNew->x400pDrivLicense = thedata->x400pDrivLicense;
		if (!(StorageNew->x400pDrivDate = malloc(thedata->x400pDrivDateLen + 1)))
			goto destroy;
		memcpy(StorageNew->x400pDrivDate, thedata->x400pDrivDate, thedata->x400pDrivDateLen);
		StorageNew->x400pDrivDateLen = thedata->x400pDrivDateLen;
		StorageNew->x400pDrivDate[StorageNew->x400pDrivDateLen] = 0;
		StorageNew->x400pDrivRowStatus = thedata->x400pDrivRowStatus;
	}
      done:
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	x400pDrivTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int x400pDrivTable_destroy(struct x400pDrivTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
x400pDrivTable_destroy(struct x400pDrivTable_data **thedata)
{
	struct x400pDrivTable_data *StorageDel;

	DEBUGMSGTL(("x400pMxMIB", "x400pDrivTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->x400pDrivName);
		StorageDel->x400pDrivNameLen = 0;
		SNMP_FREE(StorageDel->x400pDrivDescription);
		StorageDel->x400pDrivDescriptionLen = 0;
		SNMP_FREE(StorageDel->x400pDrivRevision);
		StorageDel->x400pDrivRevisionLen = 0;
		SNMP_FREE(StorageDel->x400pDrivCopyright);
		StorageDel->x400pDrivCopyrightLen = 0;
		SNMP_FREE(StorageDel->x400pDrivSupportedDevice);
		StorageDel->x400pDrivSupportedDeviceLen = 0;
		SNMP_FREE(StorageDel->x400pDrivContact);
		StorageDel->x400pDrivContactLen = 0;
		SNMP_FREE(StorageDel->x400pDrivDate);
		StorageDel->x400pDrivDateLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int x400pDrivTable_add(struct x400pDrivTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the x400pDrivTable table data set.
 *
 * Adds a table row structure to the x400pDrivTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
x400pDrivTable_add(struct x400pDrivTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("x400pMxMIB", "x400pDrivTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* x400pDrivName */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->x400pDrivName, thedata->x400pDrivNameLen);
		header_complex_add_data(&x400pDrivTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("x400pMxMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int x400pDrivTable_del(struct x400pDrivTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the x400pDrivTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
x400pDrivTable_del(struct x400pDrivTable_data *thedata)
{
	struct x400pDrivTable_data *StorageDel;

	DEBUGMSGTL(("x400pMxMIB", "x400pDrivTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(x400pDrivTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&x400pDrivTableStorage, hciptr);
	}
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_x400pDrivTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for x400pDrivTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case x400pDrivTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_x400pDrivTable(const char *token, char *line)
{
	size_t tmpsize;
	struct x400pDrivTable_data *StorageTmp = x400pDrivTable_create();

	DEBUGMSGTL(("x400pMxMIB", "parse_x400pDrivTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->x400pDrivName);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->x400pDrivName, &StorageTmp->x400pDrivNameLen);
	if (StorageTmp->x400pDrivName == NULL) {
		config_perror("invalid specification for x400pDrivName");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->x400pDrivIdnum, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->x400pDrivMajor, &tmpsize);
	SNMP_FREE(StorageTmp->x400pDrivDescription);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->x400pDrivDescription, &StorageTmp->x400pDrivDescriptionLen);
	if (StorageTmp->x400pDrivDescription == NULL) {
		config_perror("invalid specification for x400pDrivDescription");
		return;
	}
	SNMP_FREE(StorageTmp->x400pDrivRevision);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->x400pDrivRevision, &StorageTmp->x400pDrivRevisionLen);
	if (StorageTmp->x400pDrivRevision == NULL) {
		config_perror("invalid specification for x400pDrivRevision");
		return;
	}
	SNMP_FREE(StorageTmp->x400pDrivCopyright);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->x400pDrivCopyright, &StorageTmp->x400pDrivCopyrightLen);
	if (StorageTmp->x400pDrivCopyright == NULL) {
		config_perror("invalid specification for x400pDrivCopyright");
		return;
	}
	SNMP_FREE(StorageTmp->x400pDrivSupportedDevice);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->x400pDrivSupportedDevice, &StorageTmp->x400pDrivSupportedDeviceLen);
	if (StorageTmp->x400pDrivSupportedDevice == NULL) {
		config_perror("invalid specification for x400pDrivSupportedDevice");
		return;
	}
	SNMP_FREE(StorageTmp->x400pDrivContact);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->x400pDrivContact, &StorageTmp->x400pDrivContactLen);
	if (StorageTmp->x400pDrivContact == NULL) {
		config_perror("invalid specification for x400pDrivContact");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pDrivLicense, &tmpsize);
	SNMP_FREE(StorageTmp->x400pDrivDate);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->x400pDrivDate, &StorageTmp->x400pDrivDateLen);
	if (StorageTmp->x400pDrivDate == NULL) {
		config_perror("invalid specification for x400pDrivDate");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pDrivRowStatus, &tmpsize);
	x400pDrivTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
}

/*
 * store_x400pDrivTable(): store configuraiton file for x400pDrivTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_x400pDrivTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct x400pDrivTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("x400pMxMIB", "store_x400pDrivTable: storing data...  "));
	refresh_x400pDrivTable(1);
	(void) tmpsize;
	for (hcindex = x400pDrivTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct x400pDrivTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "x400pDrivTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->x400pDrivName, &StorageTmp->x400pDrivNameLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->x400pDrivIdnum, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->x400pDrivMajor, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->x400pDrivDescription, &StorageTmp->x400pDrivDescriptionLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->x400pDrivRevision, &StorageTmp->x400pDrivRevisionLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->x400pDrivCopyright, &StorageTmp->x400pDrivCopyrightLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->x400pDrivSupportedDevice, &StorageTmp->x400pDrivSupportedDeviceLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->x400pDrivContact, &StorageTmp->x400pDrivContactLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pDrivLicense, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->x400pDrivDate, &StorageTmp->x400pDrivDateLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pDrivRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct x400pCardTable_data *x400pCardTable_create(void)
 * @brief create a fresh data structure representing a new row in the x400pCardTable table.
 *
 * Creates a new x400pCardTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct x400pCardTable_data *
x400pCardTable_create(void)
{
	struct x400pCardTable_data *StorageNew = SNMP_MALLOC_STRUCT(x400pCardTable_data);

	DEBUGMSGTL(("x400pMxMIB", "x400pCardTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->x400pDrivName = malloc(1)) == NULL)
			goto nomem;
		StorageNew->x400pDrivNameLen = 0;
		StorageNew->x400pDrivName[StorageNew->x400pDrivNameLen] = 0;
		if ((StorageNew->x400pCardType = snmp_duplicate_objid(zeroDotZero_oid, 2)) == NULL)
			goto nomem;
			StorageNew->x400pCardTypeLen = 2;
		StorageNew->x400pCardIdentifier = 0;
		if ((StorageNew->x400pCardRevision = malloc(1)) == NULL)
			goto nomem;
		StorageNew->x400pCardRevisionLen = 0;
		StorageNew->x400pCardRevision[StorageNew->x400pCardRevisionLen] = 0;
		if ((StorageNew->x400pCardChipType = snmp_duplicate_objid(zeroDotZero_oid, 2)) == NULL)
			goto nomem;
			StorageNew->x400pCardChipTypeLen = 2;
		if ((StorageNew->x400pCardChipRevision = malloc(1)) == NULL)
			goto nomem;
		StorageNew->x400pCardChipRevisionLen = 0;
		StorageNew->x400pCardChipRevision[StorageNew->x400pCardChipRevisionLen] = 0;
		StorageNew->x400pCardPciBus = 0;
		StorageNew->x400pCardPciSlot = 0;
		StorageNew->x400pCardPciIrq = 0;
		StorageNew->x400pCardSpanType = 0;
		if (memdup((u_char **) &StorageNew->x400pCardMode, (u_char *) "\x00", 1) != SNMPERR_SUCCESS)
			goto nomem;
			StorageNew->x400pCardModeLen = 1;
		StorageNew->x400pCardSyncMaster = 0;
		StorageNew->x400pCardSyncSource = 0;
		StorageNew->x400pCardSyncGroup = 0;
		StorageNew->x400pCardAdministrativeState = X400PCARDADMINISTRATIVESTATE_LOCKED;
		StorageNew->x400pCardOperationalState = X400PCARDOPERATIONALSTATE_DISABLED;
		StorageNew->x400pCardUsageState = X400PCARDUSAGESTATE_IDLE;
		if (memdup((u_char **) &StorageNew->x400pCardAlarmStatus, (u_char *) "\x00", 1) != SNMPERR_SUCCESS)
			goto nomem;
			StorageNew->x400pCardAlarmStatusLen = 1;
		if (memdup((u_char **) &StorageNew->x400pCardProceduralStatus, (u_char *) "\x00", 1) != SNMPERR_SUCCESS)
			goto nomem;
			StorageNew->x400pCardProceduralStatusLen = 1;
		if (memdup((u_char **) &StorageNew->x400pCardAvailabilityStatus, (u_char *) "\x00\x00", 2) != SNMPERR_SUCCESS)
			goto nomem;
			StorageNew->x400pCardAvailabilityStatusLen = 2;
		if (memdup((u_char **) &StorageNew->x400pCardControlStatus, (u_char *) "\x00", 1) != SNMPERR_SUCCESS)
			goto nomem;
			StorageNew->x400pCardControlStatusLen = 1;
		StorageNew->x400pCardUnknownStatus = X400PCARDUNKNOWNSTATUS_FALSE;
		StorageNew->x400pCardStandbyStatus = X400PCARDSTANDBYSTATUS_PROVIDINGSERVICE;
		if ((StorageNew->x400pCardSyncSpanId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->x400pCardSyncSpanIdLen = 0;
		StorageNew->x400pCardSyncSpanId[StorageNew->x400pCardSyncSpanIdLen] = 0;
		StorageNew->x400pCardSyncTransitions = 0;
		if ((StorageNew->x400pCardName = malloc(1)) == NULL)
			goto nomem;
		StorageNew->x400pCardNameLen = 0;
		StorageNew->x400pCardName[StorageNew->x400pCardNameLen] = 0;
		StorageNew->x400pCardStatus = 0;
		StorageNew->x400pCardStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	x400pCardTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct x400pCardTable_data *x400pCardTable_duplicate(struct x400pCardTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct x400pCardTable_data *
x400pCardTable_duplicate(struct x400pCardTable_data *thedata)
{
	struct x400pCardTable_data *StorageNew = SNMP_MALLOC_STRUCT(x400pCardTable_data);

	DEBUGMSGTL(("x400pMxMIB", "x400pCardTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->x400pCardTable_id = thedata->x400pCardTable_id;
		if (!(StorageNew->x400pDrivName = malloc(thedata->x400pDrivNameLen + 1)))
			goto destroy;
		memcpy(StorageNew->x400pDrivName, thedata->x400pDrivName, thedata->x400pDrivNameLen);
		StorageNew->x400pDrivNameLen = thedata->x400pDrivNameLen;
		StorageNew->x400pDrivName[StorageNew->x400pDrivNameLen] = 0;
		StorageNew->x400pCardIndex = thedata->x400pCardIndex;
		if (!(StorageNew->x400pCardType = snmp_duplicate_objid(thedata->x400pCardType, thedata->x400pCardTypeLen / sizeof(oid))))
			goto destroy;
		StorageNew->x400pCardTypeLen = thedata->x400pCardTypeLen;
		StorageNew->x400pCardIdentifier = thedata->x400pCardIdentifier;
		if (!(StorageNew->x400pCardRevision = malloc(thedata->x400pCardRevisionLen + 1)))
			goto destroy;
		memcpy(StorageNew->x400pCardRevision, thedata->x400pCardRevision, thedata->x400pCardRevisionLen);
		StorageNew->x400pCardRevisionLen = thedata->x400pCardRevisionLen;
		StorageNew->x400pCardRevision[StorageNew->x400pCardRevisionLen] = 0;
		if (!(StorageNew->x400pCardChipType = snmp_duplicate_objid(thedata->x400pCardChipType, thedata->x400pCardChipTypeLen / sizeof(oid))))
			goto destroy;
		StorageNew->x400pCardChipTypeLen = thedata->x400pCardChipTypeLen;
		if (!(StorageNew->x400pCardChipRevision = malloc(thedata->x400pCardChipRevisionLen + 1)))
			goto destroy;
		memcpy(StorageNew->x400pCardChipRevision, thedata->x400pCardChipRevision, thedata->x400pCardChipRevisionLen);
		StorageNew->x400pCardChipRevisionLen = thedata->x400pCardChipRevisionLen;
		StorageNew->x400pCardChipRevision[StorageNew->x400pCardChipRevisionLen] = 0;
		StorageNew->x400pCardPciBus = thedata->x400pCardPciBus;
		StorageNew->x400pCardPciSlot = thedata->x400pCardPciSlot;
		StorageNew->x400pCardPciIrq = thedata->x400pCardPciIrq;
		StorageNew->x400pCardSpanType = thedata->x400pCardSpanType;
		if (!(StorageNew->x400pCardMode = malloc(thedata->x400pCardModeLen + 1)))
			goto destroy;
		memcpy(StorageNew->x400pCardMode, thedata->x400pCardMode, thedata->x400pCardModeLen);
		StorageNew->x400pCardModeLen = thedata->x400pCardModeLen;
		StorageNew->x400pCardMode[StorageNew->x400pCardModeLen] = 0;
		StorageNew->x400pCardSyncMaster = thedata->x400pCardSyncMaster;
		StorageNew->x400pCardSyncSource = thedata->x400pCardSyncSource;
		StorageNew->x400pCardSyncGroup = thedata->x400pCardSyncGroup;
		StorageNew->x400pCardAdministrativeState = thedata->x400pCardAdministrativeState;
		StorageNew->x400pCardOperationalState = thedata->x400pCardOperationalState;
		StorageNew->x400pCardUsageState = thedata->x400pCardUsageState;
		if (!(StorageNew->x400pCardAlarmStatus = malloc(thedata->x400pCardAlarmStatusLen + 1)))
			goto destroy;
		memcpy(StorageNew->x400pCardAlarmStatus, thedata->x400pCardAlarmStatus, thedata->x400pCardAlarmStatusLen);
		StorageNew->x400pCardAlarmStatusLen = thedata->x400pCardAlarmStatusLen;
		StorageNew->x400pCardAlarmStatus[StorageNew->x400pCardAlarmStatusLen] = 0;
		if (!(StorageNew->x400pCardProceduralStatus = malloc(thedata->x400pCardProceduralStatusLen + 1)))
			goto destroy;
		memcpy(StorageNew->x400pCardProceduralStatus, thedata->x400pCardProceduralStatus, thedata->x400pCardProceduralStatusLen);
		StorageNew->x400pCardProceduralStatusLen = thedata->x400pCardProceduralStatusLen;
		StorageNew->x400pCardProceduralStatus[StorageNew->x400pCardProceduralStatusLen] = 0;
		if (!(StorageNew->x400pCardAvailabilityStatus = malloc(thedata->x400pCardAvailabilityStatusLen + 1)))
			goto destroy;
		memcpy(StorageNew->x400pCardAvailabilityStatus, thedata->x400pCardAvailabilityStatus, thedata->x400pCardAvailabilityStatusLen);
		StorageNew->x400pCardAvailabilityStatusLen = thedata->x400pCardAvailabilityStatusLen;
		StorageNew->x400pCardAvailabilityStatus[StorageNew->x400pCardAvailabilityStatusLen] = 0;
		if (!(StorageNew->x400pCardControlStatus = malloc(thedata->x400pCardControlStatusLen + 1)))
			goto destroy;
		memcpy(StorageNew->x400pCardControlStatus, thedata->x400pCardControlStatus, thedata->x400pCardControlStatusLen);
		StorageNew->x400pCardControlStatusLen = thedata->x400pCardControlStatusLen;
		StorageNew->x400pCardControlStatus[StorageNew->x400pCardControlStatusLen] = 0;
		StorageNew->x400pCardUnknownStatus = thedata->x400pCardUnknownStatus;
		StorageNew->x400pCardStandbyStatus = thedata->x400pCardStandbyStatus;
		if (!(StorageNew->x400pCardSyncSpanId = malloc(thedata->x400pCardSyncSpanIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->x400pCardSyncSpanId, thedata->x400pCardSyncSpanId, thedata->x400pCardSyncSpanIdLen);
		StorageNew->x400pCardSyncSpanIdLen = thedata->x400pCardSyncSpanIdLen;
		StorageNew->x400pCardSyncSpanId[StorageNew->x400pCardSyncSpanIdLen] = 0;
		StorageNew->x400pCardSyncTransitions = thedata->x400pCardSyncTransitions;
		if (!(StorageNew->x400pCardName = malloc(thedata->x400pCardNameLen + 1)))
			goto destroy;
		memcpy(StorageNew->x400pCardName, thedata->x400pCardName, thedata->x400pCardNameLen);
		StorageNew->x400pCardNameLen = thedata->x400pCardNameLen;
		StorageNew->x400pCardName[StorageNew->x400pCardNameLen] = 0;
		StorageNew->x400pCardStatus = thedata->x400pCardStatus;
	}
      done:
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	x400pCardTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int x400pCardTable_destroy(struct x400pCardTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
x400pCardTable_destroy(struct x400pCardTable_data **thedata)
{
	struct x400pCardTable_data *StorageDel;

	DEBUGMSGTL(("x400pMxMIB", "x400pCardTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->x400pDrivName);
		StorageDel->x400pDrivNameLen = 0;
		SNMP_FREE(StorageDel->x400pCardType);
		StorageDel->x400pCardTypeLen = 0;
		SNMP_FREE(StorageDel->x400pCardRevision);
		StorageDel->x400pCardRevisionLen = 0;
		SNMP_FREE(StorageDel->x400pCardChipType);
		StorageDel->x400pCardChipTypeLen = 0;
		SNMP_FREE(StorageDel->x400pCardChipRevision);
		StorageDel->x400pCardChipRevisionLen = 0;
		SNMP_FREE(StorageDel->x400pCardMode);
		StorageDel->x400pCardModeLen = 0;
		SNMP_FREE(StorageDel->x400pCardAlarmStatus);
		StorageDel->x400pCardAlarmStatusLen = 0;
		SNMP_FREE(StorageDel->x400pCardProceduralStatus);
		StorageDel->x400pCardProceduralStatusLen = 0;
		SNMP_FREE(StorageDel->x400pCardAvailabilityStatus);
		StorageDel->x400pCardAvailabilityStatusLen = 0;
		SNMP_FREE(StorageDel->x400pCardControlStatus);
		StorageDel->x400pCardControlStatusLen = 0;
		SNMP_FREE(StorageDel->x400pCardSyncSpanId);
		StorageDel->x400pCardSyncSpanIdLen = 0;
		SNMP_FREE(StorageDel->x400pCardName);
		StorageDel->x400pCardNameLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int x400pCardTable_add(struct x400pCardTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the x400pCardTable table data set.
 *
 * Adds a table row structure to the x400pCardTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
x400pCardTable_add(struct x400pCardTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("x400pMxMIB", "x400pCardTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* x400pDrivName */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->x400pDrivName, thedata->x400pDrivNameLen);
		/* x400pCardIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->x400pCardIndex, sizeof(thedata->x400pCardIndex));
		header_complex_add_data(&x400pCardTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("x400pMxMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int x400pCardTable_del(struct x400pCardTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the x400pCardTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
x400pCardTable_del(struct x400pCardTable_data *thedata)
{
	struct x400pCardTable_data *StorageDel;

	DEBUGMSGTL(("x400pMxMIB", "x400pCardTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(x400pCardTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&x400pCardTableStorage, hciptr);
	}
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_x400pCardTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for x400pCardTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case x400pCardTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_x400pCardTable(const char *token, char *line)
{
	size_t tmpsize;
	struct x400pCardTable_data *StorageTmp = x400pCardTable_create();

	DEBUGMSGTL(("x400pMxMIB", "parse_x400pCardTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->x400pDrivName);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->x400pDrivName, &StorageTmp->x400pDrivNameLen);
	if (StorageTmp->x400pDrivName == NULL) {
		config_perror("invalid specification for x400pDrivName");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->x400pCardIndex, &tmpsize);
	SNMP_FREE(StorageTmp->x400pCardType);
	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->x400pCardType, &StorageTmp->x400pCardTypeLen);
	if (StorageTmp->x400pCardType == NULL) {
		config_perror("invalid specification for x400pCardType");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pCardIdentifier, &tmpsize);
	SNMP_FREE(StorageTmp->x400pCardRevision);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->x400pCardRevision, &StorageTmp->x400pCardRevisionLen);
	if (StorageTmp->x400pCardRevision == NULL) {
		config_perror("invalid specification for x400pCardRevision");
		return;
	}
	SNMP_FREE(StorageTmp->x400pCardChipType);
	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->x400pCardChipType, &StorageTmp->x400pCardChipTypeLen);
	if (StorageTmp->x400pCardChipType == NULL) {
		config_perror("invalid specification for x400pCardChipType");
		return;
	}
	SNMP_FREE(StorageTmp->x400pCardChipRevision);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->x400pCardChipRevision, &StorageTmp->x400pCardChipRevisionLen);
	if (StorageTmp->x400pCardChipRevision == NULL) {
		config_perror("invalid specification for x400pCardChipRevision");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->x400pCardPciBus, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->x400pCardPciSlot, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->x400pCardPciIrq, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pCardSpanType, &tmpsize);
	SNMP_FREE(StorageTmp->x400pCardMode);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->x400pCardMode, &StorageTmp->x400pCardModeLen);
	if (StorageTmp->x400pCardMode == NULL) {
		config_perror("invalid specification for x400pCardMode");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pCardSyncMaster, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pCardSyncSource, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->x400pCardSyncGroup, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pCardAdministrativeState, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pCardOperationalState, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pCardUsageState, &tmpsize);
	SNMP_FREE(StorageTmp->x400pCardAlarmStatus);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->x400pCardAlarmStatus, &StorageTmp->x400pCardAlarmStatusLen);
	if (StorageTmp->x400pCardAlarmStatus == NULL) {
		config_perror("invalid specification for x400pCardAlarmStatus");
		return;
	}
	SNMP_FREE(StorageTmp->x400pCardProceduralStatus);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->x400pCardProceduralStatus, &StorageTmp->x400pCardProceduralStatusLen);
	if (StorageTmp->x400pCardProceduralStatus == NULL) {
		config_perror("invalid specification for x400pCardProceduralStatus");
		return;
	}
	SNMP_FREE(StorageTmp->x400pCardAvailabilityStatus);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->x400pCardAvailabilityStatus, &StorageTmp->x400pCardAvailabilityStatusLen);
	if (StorageTmp->x400pCardAvailabilityStatus == NULL) {
		config_perror("invalid specification for x400pCardAvailabilityStatus");
		return;
	}
	SNMP_FREE(StorageTmp->x400pCardControlStatus);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->x400pCardControlStatus, &StorageTmp->x400pCardControlStatusLen);
	if (StorageTmp->x400pCardControlStatus == NULL) {
		config_perror("invalid specification for x400pCardControlStatus");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pCardUnknownStatus, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pCardStandbyStatus, &tmpsize);
	SNMP_FREE(StorageTmp->x400pCardSyncSpanId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->x400pCardSyncSpanId, &StorageTmp->x400pCardSyncSpanIdLen);
	if (StorageTmp->x400pCardSyncSpanId == NULL) {
		config_perror("invalid specification for x400pCardSyncSpanId");
		return;
	}
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->x400pCardSyncTransitions, &tmpsize);
	SNMP_FREE(StorageTmp->x400pCardName);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->x400pCardName, &StorageTmp->x400pCardNameLen);
	if (StorageTmp->x400pCardName == NULL) {
		config_perror("invalid specification for x400pCardName");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pCardStatus, &tmpsize);
	x400pCardTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
}

/*
 * store_x400pCardTable(): store configuraiton file for x400pCardTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_x400pCardTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct x400pCardTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("x400pMxMIB", "store_x400pCardTable: storing data...  "));
	refresh_x400pCardTable(1);
	(void) tmpsize;
	for (hcindex = x400pCardTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct x400pCardTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "x400pCardTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->x400pDrivName, &StorageTmp->x400pDrivNameLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->x400pCardIndex, &tmpsize);
			cptr = read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->x400pCardType, &StorageTmp->x400pCardTypeLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pCardIdentifier, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->x400pCardRevision, &StorageTmp->x400pCardRevisionLen);
			cptr = read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->x400pCardChipType, &StorageTmp->x400pCardChipTypeLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->x400pCardChipRevision, &StorageTmp->x400pCardChipRevisionLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->x400pCardPciBus, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->x400pCardPciSlot, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->x400pCardPciIrq, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pCardSpanType, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->x400pCardMode, &StorageTmp->x400pCardModeLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pCardSyncMaster, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pCardSyncSource, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->x400pCardSyncGroup, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pCardAdministrativeState, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pCardOperationalState, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pCardUsageState, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->x400pCardAlarmStatus, &StorageTmp->x400pCardAlarmStatusLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->x400pCardProceduralStatus, &StorageTmp->x400pCardProceduralStatusLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->x400pCardAvailabilityStatus, &StorageTmp->x400pCardAvailabilityStatusLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->x400pCardControlStatus, &StorageTmp->x400pCardControlStatusLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pCardUnknownStatus, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pCardStandbyStatus, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->x400pCardSyncSpanId, &StorageTmp->x400pCardSyncSpanIdLen);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->x400pCardSyncTransitions, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->x400pCardName, &StorageTmp->x400pCardNameLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pCardStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct x400pSpanTable_data *x400pSpanTable_create(void)
 * @brief create a fresh data structure representing a new row in the x400pSpanTable table.
 *
 * Creates a new x400pSpanTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct x400pSpanTable_data *
x400pSpanTable_create(void)
{
	struct x400pSpanTable_data *StorageNew = SNMP_MALLOC_STRUCT(x400pSpanTable_data);

	DEBUGMSGTL(("x400pMxMIB", "x400pSpanTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->x400pDrivName = malloc(1)) == NULL)
			goto nomem;
		StorageNew->x400pDrivNameLen = 0;
		StorageNew->x400pDrivName[StorageNew->x400pDrivNameLen] = 0;
		StorageNew->x400pCardIndex = 0;
		if ((StorageNew->x400pSpanName = (uint8_t *) strdup("")) == NULL)
			goto nomem;
			StorageNew->x400pSpanNameLen = strlen("");
		if ((StorageNew->x400pSpanDevice = snmp_duplicate_objid(zeroDotZero_oid, 2)) == NULL)
			goto nomem;
			StorageNew->x400pSpanDeviceLen = 2;
		if ((StorageNew->x400pSpanEquipmentId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->x400pSpanEquipmentIdLen = 0;
		StorageNew->x400pSpanEquipmentId[StorageNew->x400pSpanEquipmentIdLen] = 0;
		StorageNew->x400pSpanType = X400PSPANTYPE_NONE;
		StorageNew->x400pSpanNumber = 0;
		StorageNew->x400pSpanRate = 0;
		if (memdup((u_char **) &StorageNew->x400pSpanMode, (u_char *) "\x00", 1) != SNMPERR_SUCCESS)
			goto nomem;
			StorageNew->x400pSpanModeLen = 1;
		StorageNew->x400pSpanCrc = X400PSPANCRC_NONE;
		StorageNew->x400pSpanClocking = 0;
		StorageNew->x400pSpanPriority = 0;
		StorageNew->x400pSpanCoding = 0;
		StorageNew->x400pSpanFraming = 0;
		StorageNew->x400pSpanLineImpedance = 0;
		StorageNew->x400pSpanLineMode = X400PSPANLINEMODE_NONE;
		StorageNew->x400pSpanLineLength = X400PSPANLINELENGTH_DSX133FT;
		StorageNew->x400pSpanLineAttenuation = X400PSPANLINEATTENUATION_NONE;
		StorageNew->x400pSpanLineGain = X400PSPANLINEGAIN_NONE;
		StorageNew->x400pSpanLineDelay = 0;
		StorageNew->x400pSpanTxLevel = X400PSPANTXLEVEL_OPEN;
		StorageNew->x400pSpanRxLevel = X400PSPANRXLEVEL_OPEN;
		StorageNew->x400pSpanAlarmSettleTime = 500;
		StorageNew->x400pSpanLineCodeTime = 500;
		StorageNew->x400pSpanPrimary = 0;
		if (memdup((u_char **) &StorageNew->x400pSpanDataLink, (u_char *) "\x00", 1) != SNMPERR_SUCCESS)
			goto nomem;
			StorageNew->x400pSpanDataLinkLen = 1;
		StorageNew->x400pSpanLineCode = X400PSPANLINECODE_NOCODE;
		if ((StorageNew->x400pSpanAlarmSeverityMapProfile = snmp_duplicate_objid(zeroDotZero_oid, 2)) == NULL)
			goto nomem;
			StorageNew->x400pSpanAlarmSeverityMapProfileLen = 2;
		StorageNew->x400pSpanAdministrativeState = X400PSPANADMINISTRATIVESTATE_LOCKED;
		StorageNew->x400pSpanOperationalState = 0;
		StorageNew->x400pSpanUsageState = 0;
		if (memdup((u_char **) &StorageNew->x400pSpanAlarmStatus, (u_char *) "\x00", 1) != SNMPERR_SUCCESS)
			goto nomem;
			StorageNew->x400pSpanAlarmStatusLen = 1;
		if (memdup((u_char **) &StorageNew->x400pSpanProceduralStatus, (u_char *) "\x00", 1) != SNMPERR_SUCCESS)
			goto nomem;
			StorageNew->x400pSpanProceduralStatusLen = 1;
		if (memdup((u_char **) &StorageNew->x400pSpanAvailabilityStatus, (u_char *) "\x00\x00", 2) != SNMPERR_SUCCESS)
			goto nomem;
			StorageNew->x400pSpanAvailabilityStatusLen = 2;
		if (memdup((u_char **) &StorageNew->x400pSpanControlStatus, (u_char *) "\x00", 1) != SNMPERR_SUCCESS)
			goto nomem;
			StorageNew->x400pSpanControlStatusLen = 1;
		StorageNew->x400pSpanStandbyStatus = X400PSPANSTANDBYSTATUS_PROVIDINGSERVICE;
		StorageNew->x400pSpanUnknownStatus = 0;
		if ((StorageNew->x400pSpanSap = snmp_duplicate_objid(zeroDotZero_oid, 2)) == NULL)
			goto nomem;
			StorageNew->x400pSpanSapLen = 2;
		if (memdup((u_char **) &StorageNew->x400pSpanLoopbackStatus, (u_char *) "\x00", 1) != SNMPERR_SUCCESS)
			goto nomem;
			StorageNew->x400pSpanLoopbackStatusLen = 1;
		if (memdup((u_char **) &StorageNew->x400pSpanLineStatus, (u_char *) "\x00\x00\x00", 3) != SNMPERR_SUCCESS)
			goto nomem;
			StorageNew->x400pSpanLineStatusLen = 3;
		if (memdup((u_char **) &StorageNew->x400pSpanAlarms, (u_char *) "\x00", 1) != SNMPERR_SUCCESS)
			goto nomem;
			StorageNew->x400pSpanAlarmsLen = 1;
		if (memdup((u_char **) &StorageNew->x400pSpanEvents, (u_char *) "\x00\x00", 2) != SNMPERR_SUCCESS)
			goto nomem;
			StorageNew->x400pSpanEventsLen = 2;
		StorageNew->x400pSpanReceiveLevel = 0;
		StorageNew->x400pSpanReceiveThreshold = 0;
		StorageNew->x400pSpanRowStatus = 0;
		StorageNew->x400pSpanRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	x400pSpanTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct x400pSpanTable_data *x400pSpanTable_duplicate(struct x400pSpanTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct x400pSpanTable_data *
x400pSpanTable_duplicate(struct x400pSpanTable_data *thedata)
{
	struct x400pSpanTable_data *StorageNew = SNMP_MALLOC_STRUCT(x400pSpanTable_data);

	DEBUGMSGTL(("x400pMxMIB", "x400pSpanTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->x400pSpanTable_id = thedata->x400pSpanTable_id;
		if (!(StorageNew->x400pDrivName = malloc(thedata->x400pDrivNameLen + 1)))
			goto destroy;
		memcpy(StorageNew->x400pDrivName, thedata->x400pDrivName, thedata->x400pDrivNameLen);
		StorageNew->x400pDrivNameLen = thedata->x400pDrivNameLen;
		StorageNew->x400pDrivName[StorageNew->x400pDrivNameLen] = 0;
		StorageNew->x400pCardIndex = thedata->x400pCardIndex;
		StorageNew->x400pSpanIndex = thedata->x400pSpanIndex;
		if (!(StorageNew->x400pSpanName = malloc(thedata->x400pSpanNameLen + 1)))
			goto destroy;
		memcpy(StorageNew->x400pSpanName, thedata->x400pSpanName, thedata->x400pSpanNameLen);
		StorageNew->x400pSpanNameLen = thedata->x400pSpanNameLen;
		StorageNew->x400pSpanName[StorageNew->x400pSpanNameLen] = 0;
		if (!(StorageNew->x400pSpanDevice = snmp_duplicate_objid(thedata->x400pSpanDevice, thedata->x400pSpanDeviceLen / sizeof(oid))))
			goto destroy;
		StorageNew->x400pSpanDeviceLen = thedata->x400pSpanDeviceLen;
		if (!(StorageNew->x400pSpanEquipmentId = malloc(thedata->x400pSpanEquipmentIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->x400pSpanEquipmentId, thedata->x400pSpanEquipmentId, thedata->x400pSpanEquipmentIdLen);
		StorageNew->x400pSpanEquipmentIdLen = thedata->x400pSpanEquipmentIdLen;
		StorageNew->x400pSpanEquipmentId[StorageNew->x400pSpanEquipmentIdLen] = 0;
		StorageNew->x400pSpanType = thedata->x400pSpanType;
		StorageNew->x400pSpanNumber = thedata->x400pSpanNumber;
		StorageNew->x400pSpanRate = thedata->x400pSpanRate;
		if (!(StorageNew->x400pSpanMode = malloc(thedata->x400pSpanModeLen + 1)))
			goto destroy;
		memcpy(StorageNew->x400pSpanMode, thedata->x400pSpanMode, thedata->x400pSpanModeLen);
		StorageNew->x400pSpanModeLen = thedata->x400pSpanModeLen;
		StorageNew->x400pSpanMode[StorageNew->x400pSpanModeLen] = 0;
		StorageNew->x400pSpanCrc = thedata->x400pSpanCrc;
		StorageNew->x400pSpanClocking = thedata->x400pSpanClocking;
		StorageNew->x400pSpanPriority = thedata->x400pSpanPriority;
		StorageNew->x400pSpanCoding = thedata->x400pSpanCoding;
		StorageNew->x400pSpanFraming = thedata->x400pSpanFraming;
		StorageNew->x400pSpanLineImpedance = thedata->x400pSpanLineImpedance;
		StorageNew->x400pSpanLineMode = thedata->x400pSpanLineMode;
		StorageNew->x400pSpanLineLength = thedata->x400pSpanLineLength;
		StorageNew->x400pSpanLineAttenuation = thedata->x400pSpanLineAttenuation;
		StorageNew->x400pSpanLineGain = thedata->x400pSpanLineGain;
		StorageNew->x400pSpanLineDelay = thedata->x400pSpanLineDelay;
		StorageNew->x400pSpanTxLevel = thedata->x400pSpanTxLevel;
		StorageNew->x400pSpanRxLevel = thedata->x400pSpanRxLevel;
		StorageNew->x400pSpanAlarmSettleTime = thedata->x400pSpanAlarmSettleTime;
		StorageNew->x400pSpanLineCodeTime = thedata->x400pSpanLineCodeTime;
		StorageNew->x400pSpanPrimary = thedata->x400pSpanPrimary;
		if (!(StorageNew->x400pSpanDataLink = malloc(thedata->x400pSpanDataLinkLen + 1)))
			goto destroy;
		memcpy(StorageNew->x400pSpanDataLink, thedata->x400pSpanDataLink, thedata->x400pSpanDataLinkLen);
		StorageNew->x400pSpanDataLinkLen = thedata->x400pSpanDataLinkLen;
		StorageNew->x400pSpanDataLink[StorageNew->x400pSpanDataLinkLen] = 0;
		StorageNew->x400pSpanLineCode = thedata->x400pSpanLineCode;
		if (!(StorageNew->x400pSpanAlarmSeverityMapProfile = snmp_duplicate_objid(thedata->x400pSpanAlarmSeverityMapProfile, thedata->x400pSpanAlarmSeverityMapProfileLen / sizeof(oid))))
			goto destroy;
		StorageNew->x400pSpanAlarmSeverityMapProfileLen = thedata->x400pSpanAlarmSeverityMapProfileLen;
		StorageNew->x400pSpanAdministrativeState = thedata->x400pSpanAdministrativeState;
		StorageNew->x400pSpanOperationalState = thedata->x400pSpanOperationalState;
		StorageNew->x400pSpanUsageState = thedata->x400pSpanUsageState;
		if (!(StorageNew->x400pSpanAlarmStatus = malloc(thedata->x400pSpanAlarmStatusLen + 1)))
			goto destroy;
		memcpy(StorageNew->x400pSpanAlarmStatus, thedata->x400pSpanAlarmStatus, thedata->x400pSpanAlarmStatusLen);
		StorageNew->x400pSpanAlarmStatusLen = thedata->x400pSpanAlarmStatusLen;
		StorageNew->x400pSpanAlarmStatus[StorageNew->x400pSpanAlarmStatusLen] = 0;
		if (!(StorageNew->x400pSpanProceduralStatus = malloc(thedata->x400pSpanProceduralStatusLen + 1)))
			goto destroy;
		memcpy(StorageNew->x400pSpanProceduralStatus, thedata->x400pSpanProceduralStatus, thedata->x400pSpanProceduralStatusLen);
		StorageNew->x400pSpanProceduralStatusLen = thedata->x400pSpanProceduralStatusLen;
		StorageNew->x400pSpanProceduralStatus[StorageNew->x400pSpanProceduralStatusLen] = 0;
		if (!(StorageNew->x400pSpanAvailabilityStatus = malloc(thedata->x400pSpanAvailabilityStatusLen + 1)))
			goto destroy;
		memcpy(StorageNew->x400pSpanAvailabilityStatus, thedata->x400pSpanAvailabilityStatus, thedata->x400pSpanAvailabilityStatusLen);
		StorageNew->x400pSpanAvailabilityStatusLen = thedata->x400pSpanAvailabilityStatusLen;
		StorageNew->x400pSpanAvailabilityStatus[StorageNew->x400pSpanAvailabilityStatusLen] = 0;
		if (!(StorageNew->x400pSpanControlStatus = malloc(thedata->x400pSpanControlStatusLen + 1)))
			goto destroy;
		memcpy(StorageNew->x400pSpanControlStatus, thedata->x400pSpanControlStatus, thedata->x400pSpanControlStatusLen);
		StorageNew->x400pSpanControlStatusLen = thedata->x400pSpanControlStatusLen;
		StorageNew->x400pSpanControlStatus[StorageNew->x400pSpanControlStatusLen] = 0;
		StorageNew->x400pSpanStandbyStatus = thedata->x400pSpanStandbyStatus;
		StorageNew->x400pSpanUnknownStatus = thedata->x400pSpanUnknownStatus;
		if (!(StorageNew->x400pSpanSap = snmp_duplicate_objid(thedata->x400pSpanSap, thedata->x400pSpanSapLen / sizeof(oid))))
			goto destroy;
		StorageNew->x400pSpanSapLen = thedata->x400pSpanSapLen;
		if (!(StorageNew->x400pSpanLoopbackStatus = malloc(thedata->x400pSpanLoopbackStatusLen + 1)))
			goto destroy;
		memcpy(StorageNew->x400pSpanLoopbackStatus, thedata->x400pSpanLoopbackStatus, thedata->x400pSpanLoopbackStatusLen);
		StorageNew->x400pSpanLoopbackStatusLen = thedata->x400pSpanLoopbackStatusLen;
		StorageNew->x400pSpanLoopbackStatus[StorageNew->x400pSpanLoopbackStatusLen] = 0;
		if (!(StorageNew->x400pSpanLineStatus = malloc(thedata->x400pSpanLineStatusLen + 1)))
			goto destroy;
		memcpy(StorageNew->x400pSpanLineStatus, thedata->x400pSpanLineStatus, thedata->x400pSpanLineStatusLen);
		StorageNew->x400pSpanLineStatusLen = thedata->x400pSpanLineStatusLen;
		StorageNew->x400pSpanLineStatus[StorageNew->x400pSpanLineStatusLen] = 0;
		if (!(StorageNew->x400pSpanAlarms = malloc(thedata->x400pSpanAlarmsLen + 1)))
			goto destroy;
		memcpy(StorageNew->x400pSpanAlarms, thedata->x400pSpanAlarms, thedata->x400pSpanAlarmsLen);
		StorageNew->x400pSpanAlarmsLen = thedata->x400pSpanAlarmsLen;
		StorageNew->x400pSpanAlarms[StorageNew->x400pSpanAlarmsLen] = 0;
		if (!(StorageNew->x400pSpanEvents = malloc(thedata->x400pSpanEventsLen + 1)))
			goto destroy;
		memcpy(StorageNew->x400pSpanEvents, thedata->x400pSpanEvents, thedata->x400pSpanEventsLen);
		StorageNew->x400pSpanEventsLen = thedata->x400pSpanEventsLen;
		StorageNew->x400pSpanEvents[StorageNew->x400pSpanEventsLen] = 0;
		StorageNew->x400pSpanReceiveLevel = thedata->x400pSpanReceiveLevel;
		StorageNew->x400pSpanReceiveThreshold = thedata->x400pSpanReceiveThreshold;
		StorageNew->x400pSpanRowStatus = thedata->x400pSpanRowStatus;
	}
      done:
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	x400pSpanTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int x400pSpanTable_destroy(struct x400pSpanTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
x400pSpanTable_destroy(struct x400pSpanTable_data **thedata)
{
	struct x400pSpanTable_data *StorageDel;

	DEBUGMSGTL(("x400pMxMIB", "x400pSpanTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->x400pDrivName);
		StorageDel->x400pDrivNameLen = 0;
		SNMP_FREE(StorageDel->x400pSpanName);
		StorageDel->x400pSpanNameLen = 0;
		SNMP_FREE(StorageDel->x400pSpanDevice);
		StorageDel->x400pSpanDeviceLen = 0;
		SNMP_FREE(StorageDel->x400pSpanEquipmentId);
		StorageDel->x400pSpanEquipmentIdLen = 0;
		SNMP_FREE(StorageDel->x400pSpanMode);
		StorageDel->x400pSpanModeLen = 0;
		SNMP_FREE(StorageDel->x400pSpanDataLink);
		StorageDel->x400pSpanDataLinkLen = 0;
		SNMP_FREE(StorageDel->x400pSpanAlarmSeverityMapProfile);
		StorageDel->x400pSpanAlarmSeverityMapProfileLen = 0;
		SNMP_FREE(StorageDel->x400pSpanAlarmStatus);
		StorageDel->x400pSpanAlarmStatusLen = 0;
		SNMP_FREE(StorageDel->x400pSpanProceduralStatus);
		StorageDel->x400pSpanProceduralStatusLen = 0;
		SNMP_FREE(StorageDel->x400pSpanAvailabilityStatus);
		StorageDel->x400pSpanAvailabilityStatusLen = 0;
		SNMP_FREE(StorageDel->x400pSpanControlStatus);
		StorageDel->x400pSpanControlStatusLen = 0;
		SNMP_FREE(StorageDel->x400pSpanSap);
		StorageDel->x400pSpanSapLen = 0;
		SNMP_FREE(StorageDel->x400pSpanLoopbackStatus);
		StorageDel->x400pSpanLoopbackStatusLen = 0;
		SNMP_FREE(StorageDel->x400pSpanLineStatus);
		StorageDel->x400pSpanLineStatusLen = 0;
		SNMP_FREE(StorageDel->x400pSpanAlarms);
		StorageDel->x400pSpanAlarmsLen = 0;
		SNMP_FREE(StorageDel->x400pSpanEvents);
		StorageDel->x400pSpanEventsLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int x400pSpanTable_add(struct x400pSpanTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the x400pSpanTable table data set.
 *
 * Adds a table row structure to the x400pSpanTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
x400pSpanTable_add(struct x400pSpanTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("x400pMxMIB", "x400pSpanTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* x400pDrivName */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->x400pDrivName, thedata->x400pDrivNameLen);
		/* x400pCardIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->x400pCardIndex, sizeof(thedata->x400pCardIndex));
		/* x400pSpanIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->x400pSpanIndex, sizeof(thedata->x400pSpanIndex));
		header_complex_add_data(&x400pSpanTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("x400pMxMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int x400pSpanTable_del(struct x400pSpanTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the x400pSpanTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
x400pSpanTable_del(struct x400pSpanTable_data *thedata)
{
	struct x400pSpanTable_data *StorageDel;

	DEBUGMSGTL(("x400pMxMIB", "x400pSpanTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(x400pSpanTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&x400pSpanTableStorage, hciptr);
	}
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_x400pSpanTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for x400pSpanTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case x400pSpanTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_x400pSpanTable(const char *token, char *line)
{
	size_t tmpsize;
	struct x400pSpanTable_data *StorageTmp = x400pSpanTable_create();

	DEBUGMSGTL(("x400pMxMIB", "parse_x400pSpanTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->x400pDrivName);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->x400pDrivName, &StorageTmp->x400pDrivNameLen);
	if (StorageTmp->x400pDrivName == NULL) {
		config_perror("invalid specification for x400pDrivName");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->x400pCardIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->x400pSpanIndex, &tmpsize);
	SNMP_FREE(StorageTmp->x400pSpanName);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->x400pSpanName, &StorageTmp->x400pSpanNameLen);
	if (StorageTmp->x400pSpanName == NULL) {
		config_perror("invalid specification for x400pSpanName");
		return;
	}
	SNMP_FREE(StorageTmp->x400pSpanDevice);
	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->x400pSpanDevice, &StorageTmp->x400pSpanDeviceLen);
	if (StorageTmp->x400pSpanDevice == NULL) {
		config_perror("invalid specification for x400pSpanDevice");
		return;
	}
	SNMP_FREE(StorageTmp->x400pSpanEquipmentId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->x400pSpanEquipmentId, &StorageTmp->x400pSpanEquipmentIdLen);
	if (StorageTmp->x400pSpanEquipmentId == NULL) {
		config_perror("invalid specification for x400pSpanEquipmentId");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pSpanType, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->x400pSpanNumber, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pSpanRate, &tmpsize);
	SNMP_FREE(StorageTmp->x400pSpanMode);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->x400pSpanMode, &StorageTmp->x400pSpanModeLen);
	if (StorageTmp->x400pSpanMode == NULL) {
		config_perror("invalid specification for x400pSpanMode");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pSpanCrc, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pSpanClocking, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->x400pSpanPriority, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pSpanCoding, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pSpanFraming, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pSpanLineImpedance, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pSpanLineMode, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pSpanLineLength, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pSpanLineAttenuation, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pSpanLineGain, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->x400pSpanLineDelay, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pSpanTxLevel, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pSpanRxLevel, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pSpanAlarmSettleTime, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pSpanLineCodeTime, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->x400pSpanPrimary, &tmpsize);
	SNMP_FREE(StorageTmp->x400pSpanDataLink);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->x400pSpanDataLink, &StorageTmp->x400pSpanDataLinkLen);
	if (StorageTmp->x400pSpanDataLink == NULL) {
		config_perror("invalid specification for x400pSpanDataLink");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pSpanLineCode, &tmpsize);
	SNMP_FREE(StorageTmp->x400pSpanAlarmSeverityMapProfile);
	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->x400pSpanAlarmSeverityMapProfile, &StorageTmp->x400pSpanAlarmSeverityMapProfileLen);
	if (StorageTmp->x400pSpanAlarmSeverityMapProfile == NULL) {
		config_perror("invalid specification for x400pSpanAlarmSeverityMapProfile");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pSpanAdministrativeState, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pSpanOperationalState, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pSpanUsageState, &tmpsize);
	SNMP_FREE(StorageTmp->x400pSpanAlarmStatus);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->x400pSpanAlarmStatus, &StorageTmp->x400pSpanAlarmStatusLen);
	if (StorageTmp->x400pSpanAlarmStatus == NULL) {
		config_perror("invalid specification for x400pSpanAlarmStatus");
		return;
	}
	SNMP_FREE(StorageTmp->x400pSpanProceduralStatus);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->x400pSpanProceduralStatus, &StorageTmp->x400pSpanProceduralStatusLen);
	if (StorageTmp->x400pSpanProceduralStatus == NULL) {
		config_perror("invalid specification for x400pSpanProceduralStatus");
		return;
	}
	SNMP_FREE(StorageTmp->x400pSpanAvailabilityStatus);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->x400pSpanAvailabilityStatus, &StorageTmp->x400pSpanAvailabilityStatusLen);
	if (StorageTmp->x400pSpanAvailabilityStatus == NULL) {
		config_perror("invalid specification for x400pSpanAvailabilityStatus");
		return;
	}
	SNMP_FREE(StorageTmp->x400pSpanControlStatus);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->x400pSpanControlStatus, &StorageTmp->x400pSpanControlStatusLen);
	if (StorageTmp->x400pSpanControlStatus == NULL) {
		config_perror("invalid specification for x400pSpanControlStatus");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pSpanStandbyStatus, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pSpanUnknownStatus, &tmpsize);
	SNMP_FREE(StorageTmp->x400pSpanSap);
	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->x400pSpanSap, &StorageTmp->x400pSpanSapLen);
	if (StorageTmp->x400pSpanSap == NULL) {
		config_perror("invalid specification for x400pSpanSap");
		return;
	}
	SNMP_FREE(StorageTmp->x400pSpanLoopbackStatus);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->x400pSpanLoopbackStatus, &StorageTmp->x400pSpanLoopbackStatusLen);
	if (StorageTmp->x400pSpanLoopbackStatus == NULL) {
		config_perror("invalid specification for x400pSpanLoopbackStatus");
		return;
	}
	SNMP_FREE(StorageTmp->x400pSpanLineStatus);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->x400pSpanLineStatus, &StorageTmp->x400pSpanLineStatusLen);
	if (StorageTmp->x400pSpanLineStatus == NULL) {
		config_perror("invalid specification for x400pSpanLineStatus");
		return;
	}
	SNMP_FREE(StorageTmp->x400pSpanAlarms);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->x400pSpanAlarms, &StorageTmp->x400pSpanAlarmsLen);
	if (StorageTmp->x400pSpanAlarms == NULL) {
		config_perror("invalid specification for x400pSpanAlarms");
		return;
	}
	SNMP_FREE(StorageTmp->x400pSpanEvents);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->x400pSpanEvents, &StorageTmp->x400pSpanEventsLen);
	if (StorageTmp->x400pSpanEvents == NULL) {
		config_perror("invalid specification for x400pSpanEvents");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->x400pSpanReceiveLevel, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->x400pSpanReceiveThreshold, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pSpanRowStatus, &tmpsize);
	x400pSpanTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
}

/*
 * store_x400pSpanTable(): store configuraiton file for x400pSpanTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_x400pSpanTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct x400pSpanTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("x400pMxMIB", "store_x400pSpanTable: storing data...  "));
	refresh_x400pSpanTable(1);
	(void) tmpsize;
	for (hcindex = x400pSpanTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct x400pSpanTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "x400pSpanTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->x400pDrivName, &StorageTmp->x400pDrivNameLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->x400pCardIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->x400pSpanIndex, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->x400pSpanName, &StorageTmp->x400pSpanNameLen);
			cptr = read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->x400pSpanDevice, &StorageTmp->x400pSpanDeviceLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->x400pSpanEquipmentId, &StorageTmp->x400pSpanEquipmentIdLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pSpanType, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->x400pSpanNumber, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pSpanRate, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->x400pSpanMode, &StorageTmp->x400pSpanModeLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pSpanCrc, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pSpanClocking, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->x400pSpanPriority, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pSpanCoding, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pSpanFraming, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pSpanLineImpedance, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pSpanLineMode, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pSpanLineLength, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pSpanLineAttenuation, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pSpanLineGain, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->x400pSpanLineDelay, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pSpanTxLevel, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pSpanRxLevel, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pSpanAlarmSettleTime, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pSpanLineCodeTime, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->x400pSpanPrimary, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->x400pSpanDataLink, &StorageTmp->x400pSpanDataLinkLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pSpanLineCode, &tmpsize);
			cptr = read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->x400pSpanAlarmSeverityMapProfile, &StorageTmp->x400pSpanAlarmSeverityMapProfileLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pSpanAdministrativeState, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pSpanOperationalState, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pSpanUsageState, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->x400pSpanAlarmStatus, &StorageTmp->x400pSpanAlarmStatusLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->x400pSpanProceduralStatus, &StorageTmp->x400pSpanProceduralStatusLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->x400pSpanAvailabilityStatus, &StorageTmp->x400pSpanAvailabilityStatusLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->x400pSpanControlStatus, &StorageTmp->x400pSpanControlStatusLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pSpanStandbyStatus, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pSpanUnknownStatus, &tmpsize);
			cptr = read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->x400pSpanSap, &StorageTmp->x400pSpanSapLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->x400pSpanLoopbackStatus, &StorageTmp->x400pSpanLoopbackStatusLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->x400pSpanLineStatus, &StorageTmp->x400pSpanLineStatusLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->x400pSpanAlarms, &StorageTmp->x400pSpanAlarmsLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->x400pSpanEvents, &StorageTmp->x400pSpanEventsLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->x400pSpanReceiveLevel, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->x400pSpanReceiveThreshold, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pSpanRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct x400pBertTable_data *x400pBertTable_create(void)
 * @brief create a fresh data structure representing a new row in the x400pBertTable table.
 *
 * Creates a new x400pBertTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct x400pBertTable_data *
x400pBertTable_create(void)
{
	struct x400pBertTable_data *StorageNew = SNMP_MALLOC_STRUCT(x400pBertTable_data);

	DEBUGMSGTL(("x400pMxMIB", "x400pBertTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->x400pDrivName = malloc(1)) == NULL)
			goto nomem;
		StorageNew->x400pDrivNameLen = 0;
		StorageNew->x400pDrivName[StorageNew->x400pDrivNameLen] = 0;
		StorageNew->x400pCardIndex = 0;
		StorageNew->x400pSpanIndex = 0;
		StorageNew->x400pBertMode = X400PBERTMODE_NONE;
		StorageNew->x400pBertSelect = X400PBERTSELECT_NONE;
		if (memdup((u_char **) &StorageNew->x400pBertPattern, (u_char *) "\x10\xFF\xFF\xFF\xFF", 5) != SNMPERR_SUCCESS)
			goto nomem;
			StorageNew->x400pBertPatternLen = 5;
		StorageNew->x400pBertOperationalState = 0;
		if (memdup((u_char **) &StorageNew->x400pBertProceduralStatus, (u_char *) "\x00", 1) != SNMPERR_SUCCESS)
			goto nomem;
			StorageNew->x400pBertProceduralStatusLen = 1;
		StorageNew->x400pBertBitCount = 0;
		StorageNew->x400pBertErrorCount = 0;
	}
      done:
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	x400pBertTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct x400pBertTable_data *x400pBertTable_duplicate(struct x400pBertTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct x400pBertTable_data *
x400pBertTable_duplicate(struct x400pBertTable_data *thedata)
{
	struct x400pBertTable_data *StorageNew = SNMP_MALLOC_STRUCT(x400pBertTable_data);

	DEBUGMSGTL(("x400pMxMIB", "x400pBertTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->x400pBertTable_id = thedata->x400pBertTable_id;
		if (!(StorageNew->x400pDrivName = malloc(thedata->x400pDrivNameLen + 1)))
			goto destroy;
		memcpy(StorageNew->x400pDrivName, thedata->x400pDrivName, thedata->x400pDrivNameLen);
		StorageNew->x400pDrivNameLen = thedata->x400pDrivNameLen;
		StorageNew->x400pDrivName[StorageNew->x400pDrivNameLen] = 0;
		StorageNew->x400pCardIndex = thedata->x400pCardIndex;
		StorageNew->x400pSpanIndex = thedata->x400pSpanIndex;
		StorageNew->x400pBertMode = thedata->x400pBertMode;
		StorageNew->x400pBertSelect = thedata->x400pBertSelect;
		if (!(StorageNew->x400pBertPattern = malloc(thedata->x400pBertPatternLen + 1)))
			goto destroy;
		memcpy(StorageNew->x400pBertPattern, thedata->x400pBertPattern, thedata->x400pBertPatternLen);
		StorageNew->x400pBertPatternLen = thedata->x400pBertPatternLen;
		StorageNew->x400pBertPattern[StorageNew->x400pBertPatternLen] = 0;
		StorageNew->x400pBertOperationalState = thedata->x400pBertOperationalState;
		if (!(StorageNew->x400pBertProceduralStatus = malloc(thedata->x400pBertProceduralStatusLen + 1)))
			goto destroy;
		memcpy(StorageNew->x400pBertProceduralStatus, thedata->x400pBertProceduralStatus, thedata->x400pBertProceduralStatusLen);
		StorageNew->x400pBertProceduralStatusLen = thedata->x400pBertProceduralStatusLen;
		StorageNew->x400pBertProceduralStatus[StorageNew->x400pBertProceduralStatusLen] = 0;
		StorageNew->x400pBertBitCount = thedata->x400pBertBitCount;
		StorageNew->x400pBertErrorCount = thedata->x400pBertErrorCount;
	}
      done:
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	x400pBertTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int x400pBertTable_destroy(struct x400pBertTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
x400pBertTable_destroy(struct x400pBertTable_data **thedata)
{
	struct x400pBertTable_data *StorageDel;

	DEBUGMSGTL(("x400pMxMIB", "x400pBertTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->x400pDrivName);
		StorageDel->x400pDrivNameLen = 0;
		SNMP_FREE(StorageDel->x400pBertPattern);
		StorageDel->x400pBertPatternLen = 0;
		SNMP_FREE(StorageDel->x400pBertProceduralStatus);
		StorageDel->x400pBertProceduralStatusLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int x400pBertTable_add(struct x400pBertTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the x400pBertTable table data set.
 *
 * Adds a table row structure to the x400pBertTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
x400pBertTable_add(struct x400pBertTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("x400pMxMIB", "x400pBertTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* x400pDrivName */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->x400pDrivName, thedata->x400pDrivNameLen);
		/* x400pCardIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->x400pCardIndex, sizeof(thedata->x400pCardIndex));
		/* x400pSpanIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->x400pSpanIndex, sizeof(thedata->x400pSpanIndex));
		header_complex_add_data(&x400pBertTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("x400pMxMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int x400pBertTable_del(struct x400pBertTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the x400pBertTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
x400pBertTable_del(struct x400pBertTable_data *thedata)
{
	struct x400pBertTable_data *StorageDel;

	DEBUGMSGTL(("x400pMxMIB", "x400pBertTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(x400pBertTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&x400pBertTableStorage, hciptr);
	}
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_x400pBertTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for x400pBertTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case x400pBertTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_x400pBertTable(const char *token, char *line)
{
	size_t tmpsize;
	struct x400pBertTable_data *StorageTmp = x400pBertTable_create();

	DEBUGMSGTL(("x400pMxMIB", "parse_x400pBertTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->x400pDrivName);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->x400pDrivName, &StorageTmp->x400pDrivNameLen);
	if (StorageTmp->x400pDrivName == NULL) {
		config_perror("invalid specification for x400pDrivName");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->x400pCardIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->x400pSpanIndex, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pBertMode, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pBertSelect, &tmpsize);
	SNMP_FREE(StorageTmp->x400pBertPattern);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->x400pBertPattern, &StorageTmp->x400pBertPatternLen);
	if (StorageTmp->x400pBertPattern == NULL) {
		config_perror("invalid specification for x400pBertPattern");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pBertOperationalState, &tmpsize);
	SNMP_FREE(StorageTmp->x400pBertProceduralStatus);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->x400pBertProceduralStatus, &StorageTmp->x400pBertProceduralStatusLen);
	if (StorageTmp->x400pBertProceduralStatus == NULL) {
		config_perror("invalid specification for x400pBertProceduralStatus");
		return;
	}
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->x400pBertBitCount, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->x400pBertErrorCount, &tmpsize);
	x400pBertTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
}

/*
 * store_x400pBertTable(): store configuraiton file for x400pBertTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_x400pBertTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct x400pBertTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("x400pMxMIB", "store_x400pBertTable: storing data...  "));
	refresh_x400pBertTable(1);
	(void) tmpsize;
	for (hcindex = x400pBertTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct x400pBertTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "x400pBertTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->x400pDrivName, &StorageTmp->x400pDrivNameLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->x400pCardIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->x400pSpanIndex, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pBertMode, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pBertSelect, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->x400pBertPattern, &StorageTmp->x400pBertPatternLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pBertOperationalState, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->x400pBertProceduralStatus, &StorageTmp->x400pBertProceduralStatusLen);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->x400pBertBitCount, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->x400pBertErrorCount, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct x400pChanTable_data *x400pChanTable_create(void)
 * @brief create a fresh data structure representing a new row in the x400pChanTable table.
 *
 * Creates a new x400pChanTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct x400pChanTable_data *
x400pChanTable_create(void)
{
	struct x400pChanTable_data *StorageNew = SNMP_MALLOC_STRUCT(x400pChanTable_data);

	DEBUGMSGTL(("x400pMxMIB", "x400pChanTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->x400pDrivName = malloc(1)) == NULL)
			goto nomem;
		StorageNew->x400pDrivNameLen = 0;
		StorageNew->x400pDrivName[StorageNew->x400pDrivNameLen] = 0;
		StorageNew->x400pCardIndex = 0;
		StorageNew->x400pSpanIndex = 0;
		StorageNew->x400pChanType = X400PCHANTYPE_CCS;
		StorageNew->x400pChanFormat = 0;
		StorageNew->x400pChanRate = 0;
		if (memdup((u_char **) &StorageNew->x400pChanMode, (u_char *) "\x00", 1) != SNMPERR_SUCCESS)
			goto nomem;
			StorageNew->x400pChanModeLen = 1;
		if ((StorageNew->x400pChanSap = snmp_duplicate_objid(zeroDotZero_oid, 2)) == NULL)
			goto nomem;
			StorageNew->x400pChanSapLen = 2;
		StorageNew->x400pChanAdministrativeState = X400PCHANADMINISTRATIVESTATE_LOCKED;
		StorageNew->x400pChanOperationalState = X400PCHANOPERATIONALSTATE_DISABLED;
		StorageNew->x400pChanUsageState = 0;
		if (memdup((u_char **) &StorageNew->x400pChanAvailabilityStatus, (u_char *) "\x00\x00", 2) != SNMPERR_SUCCESS)
			goto nomem;
			StorageNew->x400pChanAvailabilityStatusLen = 2;
		if (memdup((u_char **) &StorageNew->x400pChanControlStatus, (u_char *) "\x00", 1) != SNMPERR_SUCCESS)
			goto nomem;
			StorageNew->x400pChanControlStatusLen = 1;
		if (memdup((u_char **) &StorageNew->x400pChanProceduralStatus, (u_char *) "\x00", 1) != SNMPERR_SUCCESS)
			goto nomem;
			StorageNew->x400pChanProceduralStatusLen = 1;
		if (memdup((u_char **) &StorageNew->x400pChanAlarmStatus, (u_char *) "\x00", 1) != SNMPERR_SUCCESS)
			goto nomem;
			StorageNew->x400pChanAlarmStatusLen = 1;
		StorageNew->x400pChanStandbyStatus = X400PCHANSTANDBYSTATUS_PROVIDINGSERVICE;
	}
      done:
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	x400pChanTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct x400pChanTable_data *x400pChanTable_duplicate(struct x400pChanTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct x400pChanTable_data *
x400pChanTable_duplicate(struct x400pChanTable_data *thedata)
{
	struct x400pChanTable_data *StorageNew = SNMP_MALLOC_STRUCT(x400pChanTable_data);

	DEBUGMSGTL(("x400pMxMIB", "x400pChanTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->x400pChanTable_id = thedata->x400pChanTable_id;
		if (!(StorageNew->x400pDrivName = malloc(thedata->x400pDrivNameLen + 1)))
			goto destroy;
		memcpy(StorageNew->x400pDrivName, thedata->x400pDrivName, thedata->x400pDrivNameLen);
		StorageNew->x400pDrivNameLen = thedata->x400pDrivNameLen;
		StorageNew->x400pDrivName[StorageNew->x400pDrivNameLen] = 0;
		StorageNew->x400pCardIndex = thedata->x400pCardIndex;
		StorageNew->x400pSpanIndex = thedata->x400pSpanIndex;
		StorageNew->x400pChanIndex = thedata->x400pChanIndex;
		StorageNew->x400pChanType = thedata->x400pChanType;
		StorageNew->x400pChanFormat = thedata->x400pChanFormat;
		StorageNew->x400pChanRate = thedata->x400pChanRate;
		if (!(StorageNew->x400pChanMode = malloc(thedata->x400pChanModeLen + 1)))
			goto destroy;
		memcpy(StorageNew->x400pChanMode, thedata->x400pChanMode, thedata->x400pChanModeLen);
		StorageNew->x400pChanModeLen = thedata->x400pChanModeLen;
		StorageNew->x400pChanMode[StorageNew->x400pChanModeLen] = 0;
		if (!(StorageNew->x400pChanSap = snmp_duplicate_objid(thedata->x400pChanSap, thedata->x400pChanSapLen / sizeof(oid))))
			goto destroy;
		StorageNew->x400pChanSapLen = thedata->x400pChanSapLen;
		StorageNew->x400pChanAdministrativeState = thedata->x400pChanAdministrativeState;
		StorageNew->x400pChanOperationalState = thedata->x400pChanOperationalState;
		StorageNew->x400pChanUsageState = thedata->x400pChanUsageState;
		if (!(StorageNew->x400pChanAvailabilityStatus = malloc(thedata->x400pChanAvailabilityStatusLen + 1)))
			goto destroy;
		memcpy(StorageNew->x400pChanAvailabilityStatus, thedata->x400pChanAvailabilityStatus, thedata->x400pChanAvailabilityStatusLen);
		StorageNew->x400pChanAvailabilityStatusLen = thedata->x400pChanAvailabilityStatusLen;
		StorageNew->x400pChanAvailabilityStatus[StorageNew->x400pChanAvailabilityStatusLen] = 0;
		if (!(StorageNew->x400pChanControlStatus = malloc(thedata->x400pChanControlStatusLen + 1)))
			goto destroy;
		memcpy(StorageNew->x400pChanControlStatus, thedata->x400pChanControlStatus, thedata->x400pChanControlStatusLen);
		StorageNew->x400pChanControlStatusLen = thedata->x400pChanControlStatusLen;
		StorageNew->x400pChanControlStatus[StorageNew->x400pChanControlStatusLen] = 0;
		if (!(StorageNew->x400pChanProceduralStatus = malloc(thedata->x400pChanProceduralStatusLen + 1)))
			goto destroy;
		memcpy(StorageNew->x400pChanProceduralStatus, thedata->x400pChanProceduralStatus, thedata->x400pChanProceduralStatusLen);
		StorageNew->x400pChanProceduralStatusLen = thedata->x400pChanProceduralStatusLen;
		StorageNew->x400pChanProceduralStatus[StorageNew->x400pChanProceduralStatusLen] = 0;
		if (!(StorageNew->x400pChanAlarmStatus = malloc(thedata->x400pChanAlarmStatusLen + 1)))
			goto destroy;
		memcpy(StorageNew->x400pChanAlarmStatus, thedata->x400pChanAlarmStatus, thedata->x400pChanAlarmStatusLen);
		StorageNew->x400pChanAlarmStatusLen = thedata->x400pChanAlarmStatusLen;
		StorageNew->x400pChanAlarmStatus[StorageNew->x400pChanAlarmStatusLen] = 0;
		StorageNew->x400pChanStandbyStatus = thedata->x400pChanStandbyStatus;
	}
      done:
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	x400pChanTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int x400pChanTable_destroy(struct x400pChanTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
x400pChanTable_destroy(struct x400pChanTable_data **thedata)
{
	struct x400pChanTable_data *StorageDel;

	DEBUGMSGTL(("x400pMxMIB", "x400pChanTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->x400pDrivName);
		StorageDel->x400pDrivNameLen = 0;
		SNMP_FREE(StorageDel->x400pChanMode);
		StorageDel->x400pChanModeLen = 0;
		SNMP_FREE(StorageDel->x400pChanSap);
		StorageDel->x400pChanSapLen = 0;
		SNMP_FREE(StorageDel->x400pChanAvailabilityStatus);
		StorageDel->x400pChanAvailabilityStatusLen = 0;
		SNMP_FREE(StorageDel->x400pChanControlStatus);
		StorageDel->x400pChanControlStatusLen = 0;
		SNMP_FREE(StorageDel->x400pChanProceduralStatus);
		StorageDel->x400pChanProceduralStatusLen = 0;
		SNMP_FREE(StorageDel->x400pChanAlarmStatus);
		StorageDel->x400pChanAlarmStatusLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int x400pChanTable_add(struct x400pChanTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the x400pChanTable table data set.
 *
 * Adds a table row structure to the x400pChanTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
x400pChanTable_add(struct x400pChanTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("x400pMxMIB", "x400pChanTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* x400pDrivName */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->x400pDrivName, thedata->x400pDrivNameLen);
		/* x400pCardIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->x400pCardIndex, sizeof(thedata->x400pCardIndex));
		/* x400pSpanIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->x400pSpanIndex, sizeof(thedata->x400pSpanIndex));
		/* x400pChanIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->x400pChanIndex, sizeof(thedata->x400pChanIndex));
		header_complex_add_data(&x400pChanTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("x400pMxMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int x400pChanTable_del(struct x400pChanTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the x400pChanTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
x400pChanTable_del(struct x400pChanTable_data *thedata)
{
	struct x400pChanTable_data *StorageDel;

	DEBUGMSGTL(("x400pMxMIB", "x400pChanTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(x400pChanTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&x400pChanTableStorage, hciptr);
	}
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_x400pChanTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for x400pChanTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case x400pChanTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_x400pChanTable(const char *token, char *line)
{
	size_t tmpsize;
	struct x400pChanTable_data *StorageTmp = x400pChanTable_create();

	DEBUGMSGTL(("x400pMxMIB", "parse_x400pChanTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->x400pDrivName);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->x400pDrivName, &StorageTmp->x400pDrivNameLen);
	if (StorageTmp->x400pDrivName == NULL) {
		config_perror("invalid specification for x400pDrivName");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->x400pCardIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->x400pSpanIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->x400pChanIndex, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pChanType, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pChanFormat, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pChanRate, &tmpsize);
	SNMP_FREE(StorageTmp->x400pChanMode);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->x400pChanMode, &StorageTmp->x400pChanModeLen);
	if (StorageTmp->x400pChanMode == NULL) {
		config_perror("invalid specification for x400pChanMode");
		return;
	}
	SNMP_FREE(StorageTmp->x400pChanSap);
	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->x400pChanSap, &StorageTmp->x400pChanSapLen);
	if (StorageTmp->x400pChanSap == NULL) {
		config_perror("invalid specification for x400pChanSap");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pChanAdministrativeState, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pChanOperationalState, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pChanUsageState, &tmpsize);
	SNMP_FREE(StorageTmp->x400pChanAvailabilityStatus);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->x400pChanAvailabilityStatus, &StorageTmp->x400pChanAvailabilityStatusLen);
	if (StorageTmp->x400pChanAvailabilityStatus == NULL) {
		config_perror("invalid specification for x400pChanAvailabilityStatus");
		return;
	}
	SNMP_FREE(StorageTmp->x400pChanControlStatus);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->x400pChanControlStatus, &StorageTmp->x400pChanControlStatusLen);
	if (StorageTmp->x400pChanControlStatus == NULL) {
		config_perror("invalid specification for x400pChanControlStatus");
		return;
	}
	SNMP_FREE(StorageTmp->x400pChanProceduralStatus);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->x400pChanProceduralStatus, &StorageTmp->x400pChanProceduralStatusLen);
	if (StorageTmp->x400pChanProceduralStatus == NULL) {
		config_perror("invalid specification for x400pChanProceduralStatus");
		return;
	}
	SNMP_FREE(StorageTmp->x400pChanAlarmStatus);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->x400pChanAlarmStatus, &StorageTmp->x400pChanAlarmStatusLen);
	if (StorageTmp->x400pChanAlarmStatus == NULL) {
		config_perror("invalid specification for x400pChanAlarmStatus");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pChanStandbyStatus, &tmpsize);
	x400pChanTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
}

/*
 * store_x400pChanTable(): store configuraiton file for x400pChanTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_x400pChanTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct x400pChanTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("x400pMxMIB", "store_x400pChanTable: storing data...  "));
	refresh_x400pChanTable(1);
	(void) tmpsize;
	for (hcindex = x400pChanTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct x400pChanTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "x400pChanTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->x400pDrivName, &StorageTmp->x400pDrivNameLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->x400pCardIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->x400pSpanIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->x400pChanIndex, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pChanType, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pChanFormat, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pChanRate, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->x400pChanMode, &StorageTmp->x400pChanModeLen);
			cptr = read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->x400pChanSap, &StorageTmp->x400pChanSapLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pChanAdministrativeState, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pChanOperationalState, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pChanUsageState, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->x400pChanAvailabilityStatus, &StorageTmp->x400pChanAvailabilityStatusLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->x400pChanControlStatus, &StorageTmp->x400pChanControlStatusLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->x400pChanProceduralStatus, &StorageTmp->x400pChanProceduralStatusLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->x400pChanAlarmStatus, &StorageTmp->x400pChanAlarmStatusLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pChanStandbyStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct x400pXconTable_data *x400pXconTable_create(void)
 * @brief create a fresh data structure representing a new row in the x400pXconTable table.
 *
 * Creates a new x400pXconTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct x400pXconTable_data *
x400pXconTable_create(void)
{
	struct x400pXconTable_data *StorageNew = SNMP_MALLOC_STRUCT(x400pXconTable_data);

	DEBUGMSGTL(("x400pMxMIB", "x400pXconTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->x400pDrivName = malloc(1)) == NULL)
			goto nomem;
		StorageNew->x400pDrivNameLen = 0;
		StorageNew->x400pDrivName[StorageNew->x400pDrivNameLen] = 0;
		StorageNew->x400pCardIndex = 0;
		StorageNew->x400pSpanIndex = 0;
		StorageNew->x400pChanIndex = 0;
		StorageNew->x400pXconCardIndex = 0;
		StorageNew->x400pXconSpanIndex = 0;
		StorageNew->x400pXconChanIndex = 0;
		StorageNew->x400pXconType = X400PXCONTYPE_PERMANENT;
		StorageNew->x400pXconStorageType = ST_NONVOLATILE;
		StorageNew->x400pXconRowStatus = 0;
		StorageNew->x400pXconRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	x400pXconTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct x400pXconTable_data *x400pXconTable_duplicate(struct x400pXconTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct x400pXconTable_data *
x400pXconTable_duplicate(struct x400pXconTable_data *thedata)
{
	struct x400pXconTable_data *StorageNew = SNMP_MALLOC_STRUCT(x400pXconTable_data);

	DEBUGMSGTL(("x400pMxMIB", "x400pXconTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->x400pXconTable_id = thedata->x400pXconTable_id;
		if (!(StorageNew->x400pDrivName = malloc(thedata->x400pDrivNameLen + 1)))
			goto destroy;
		memcpy(StorageNew->x400pDrivName, thedata->x400pDrivName, thedata->x400pDrivNameLen);
		StorageNew->x400pDrivNameLen = thedata->x400pDrivNameLen;
		StorageNew->x400pDrivName[StorageNew->x400pDrivNameLen] = 0;
		StorageNew->x400pCardIndex = thedata->x400pCardIndex;
		StorageNew->x400pSpanIndex = thedata->x400pSpanIndex;
		StorageNew->x400pChanIndex = thedata->x400pChanIndex;
		StorageNew->x400pXconCardIndex = thedata->x400pXconCardIndex;
		StorageNew->x400pXconSpanIndex = thedata->x400pXconSpanIndex;
		StorageNew->x400pXconChanIndex = thedata->x400pXconChanIndex;
		StorageNew->x400pXconType = thedata->x400pXconType;
		StorageNew->x400pXconStorageType = thedata->x400pXconStorageType;
		StorageNew->x400pXconRowStatus = thedata->x400pXconRowStatus;
	}
      done:
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	x400pXconTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int x400pXconTable_destroy(struct x400pXconTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
x400pXconTable_destroy(struct x400pXconTable_data **thedata)
{
	struct x400pXconTable_data *StorageDel;

	DEBUGMSGTL(("x400pMxMIB", "x400pXconTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->x400pDrivName);
		StorageDel->x400pDrivNameLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int x400pXconTable_add(struct x400pXconTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the x400pXconTable table data set.
 *
 * Adds a table row structure to the x400pXconTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
x400pXconTable_add(struct x400pXconTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("x400pMxMIB", "x400pXconTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* x400pDrivName */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->x400pDrivName, thedata->x400pDrivNameLen);
		/* x400pCardIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->x400pCardIndex, sizeof(thedata->x400pCardIndex));
		/* x400pSpanIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->x400pSpanIndex, sizeof(thedata->x400pSpanIndex));
		/* x400pChanIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->x400pChanIndex, sizeof(thedata->x400pChanIndex));
		header_complex_add_data(&x400pXconTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("x400pMxMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int x400pXconTable_del(struct x400pXconTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the x400pXconTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
x400pXconTable_del(struct x400pXconTable_data *thedata)
{
	struct x400pXconTable_data *StorageDel;

	DEBUGMSGTL(("x400pMxMIB", "x400pXconTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(x400pXconTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&x400pXconTableStorage, hciptr);
	}
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_x400pXconTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for x400pXconTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case x400pXconTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_x400pXconTable(const char *token, char *line)
{
	size_t tmpsize;
	struct x400pXconTable_data *StorageTmp = x400pXconTable_create();

	DEBUGMSGTL(("x400pMxMIB", "parse_x400pXconTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->x400pDrivName);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->x400pDrivName, &StorageTmp->x400pDrivNameLen);
	if (StorageTmp->x400pDrivName == NULL) {
		config_perror("invalid specification for x400pDrivName");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->x400pCardIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->x400pSpanIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->x400pChanIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->x400pXconCardIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->x400pXconSpanIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->x400pXconChanIndex, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pXconType, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pXconStorageType, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pXconRowStatus, &tmpsize);
	x400pXconTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
}

/*
 * store_x400pXconTable(): store configuraiton file for x400pXconTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_x400pXconTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct x400pXconTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("x400pMxMIB", "store_x400pXconTable: storing data...  "));
	refresh_x400pXconTable(1);
	(void) tmpsize;
	for (hcindex = x400pXconTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct x400pXconTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		if (StorageTmp->x400pXconStorageType == ST_NONVOLATILE) {
			memset(line, 0, sizeof(line));
			strcat(line, "x400pXconTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->x400pDrivName, &StorageTmp->x400pDrivNameLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->x400pCardIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->x400pSpanIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->x400pChanIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->x400pXconCardIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->x400pXconSpanIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->x400pXconChanIndex, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pXconType, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pXconStorageType, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pXconRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct x400pNearEndCurrentTable_data *x400pNearEndCurrentTable_create(void)
 * @brief create a fresh data structure representing a new row in the x400pNearEndCurrentTable table.
 *
 * Creates a new x400pNearEndCurrentTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct x400pNearEndCurrentTable_data *
x400pNearEndCurrentTable_create(void)
{
	struct x400pNearEndCurrentTable_data *StorageNew = SNMP_MALLOC_STRUCT(x400pNearEndCurrentTable_data);

	DEBUGMSGTL(("x400pMxMIB", "x400pNearEndCurrentTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->x400pDrivName = malloc(1)) == NULL)
			goto nomem;
		StorageNew->x400pDrivNameLen = 0;
		StorageNew->x400pDrivName[StorageNew->x400pDrivNameLen] = 0;
		StorageNew->x400pCardIndex = 0;
		StorageNew->x400pSpanIndex = 0;
		StorageNew->x400pNearEndCurrentTimeElapsed = 0;
		StorageNew->x400pNearEndCurrentESs = 0;
		StorageNew->x400pNearEndCurrentSESs = 0;
		StorageNew->x400pNearEndCurrentSEFSs = 0;
		StorageNew->x400pNearEndCurrentUASs = 0;
		StorageNew->x400pNearEndCurrentCSSs = 0;
		StorageNew->x400pNearEndCurrentPCVs = 0;
		StorageNew->x400pNearEndCurrentLESs = 0;
		StorageNew->x400pNearEndCurrentBESs = 0;
		StorageNew->x400pNearEndCurrentDMs = 0;
		StorageNew->x400pNearEndCurrentLCVs = 0;
	}
      done:
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	x400pNearEndCurrentTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct x400pNearEndCurrentTable_data *x400pNearEndCurrentTable_duplicate(struct x400pNearEndCurrentTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct x400pNearEndCurrentTable_data *
x400pNearEndCurrentTable_duplicate(struct x400pNearEndCurrentTable_data *thedata)
{
	struct x400pNearEndCurrentTable_data *StorageNew = SNMP_MALLOC_STRUCT(x400pNearEndCurrentTable_data);

	DEBUGMSGTL(("x400pMxMIB", "x400pNearEndCurrentTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->x400pNearEndCurrentTable_id = thedata->x400pNearEndCurrentTable_id;
		if (!(StorageNew->x400pDrivName = malloc(thedata->x400pDrivNameLen + 1)))
			goto destroy;
		memcpy(StorageNew->x400pDrivName, thedata->x400pDrivName, thedata->x400pDrivNameLen);
		StorageNew->x400pDrivNameLen = thedata->x400pDrivNameLen;
		StorageNew->x400pDrivName[StorageNew->x400pDrivNameLen] = 0;
		StorageNew->x400pCardIndex = thedata->x400pCardIndex;
		StorageNew->x400pSpanIndex = thedata->x400pSpanIndex;
		StorageNew->x400pNearEndCurrentTimeElapsed = thedata->x400pNearEndCurrentTimeElapsed;
		StorageNew->x400pNearEndCurrentESs = thedata->x400pNearEndCurrentESs;
		StorageNew->x400pNearEndCurrentSESs = thedata->x400pNearEndCurrentSESs;
		StorageNew->x400pNearEndCurrentSEFSs = thedata->x400pNearEndCurrentSEFSs;
		StorageNew->x400pNearEndCurrentUASs = thedata->x400pNearEndCurrentUASs;
		StorageNew->x400pNearEndCurrentCSSs = thedata->x400pNearEndCurrentCSSs;
		StorageNew->x400pNearEndCurrentPCVs = thedata->x400pNearEndCurrentPCVs;
		StorageNew->x400pNearEndCurrentLESs = thedata->x400pNearEndCurrentLESs;
		StorageNew->x400pNearEndCurrentBESs = thedata->x400pNearEndCurrentBESs;
		StorageNew->x400pNearEndCurrentDMs = thedata->x400pNearEndCurrentDMs;
		StorageNew->x400pNearEndCurrentLCVs = thedata->x400pNearEndCurrentLCVs;
	}
      done:
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	x400pNearEndCurrentTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int x400pNearEndCurrentTable_destroy(struct x400pNearEndCurrentTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
x400pNearEndCurrentTable_destroy(struct x400pNearEndCurrentTable_data **thedata)
{
	struct x400pNearEndCurrentTable_data *StorageDel;

	DEBUGMSGTL(("x400pMxMIB", "x400pNearEndCurrentTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->x400pDrivName);
		StorageDel->x400pDrivNameLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int x400pNearEndCurrentTable_add(struct x400pNearEndCurrentTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the x400pNearEndCurrentTable table data set.
 *
 * Adds a table row structure to the x400pNearEndCurrentTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
x400pNearEndCurrentTable_add(struct x400pNearEndCurrentTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("x400pMxMIB", "x400pNearEndCurrentTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* x400pDrivName */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->x400pDrivName, thedata->x400pDrivNameLen);
		/* x400pCardIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->x400pCardIndex, sizeof(thedata->x400pCardIndex));
		/* x400pSpanIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->x400pSpanIndex, sizeof(thedata->x400pSpanIndex));
		header_complex_add_data(&x400pNearEndCurrentTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("x400pMxMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int x400pNearEndCurrentTable_del(struct x400pNearEndCurrentTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the x400pNearEndCurrentTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
x400pNearEndCurrentTable_del(struct x400pNearEndCurrentTable_data *thedata)
{
	struct x400pNearEndCurrentTable_data *StorageDel;

	DEBUGMSGTL(("x400pMxMIB", "x400pNearEndCurrentTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(x400pNearEndCurrentTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&x400pNearEndCurrentTableStorage, hciptr);
	}
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_x400pNearEndCurrentTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for x400pNearEndCurrentTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case x400pNearEndCurrentTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_x400pNearEndCurrentTable(const char *token, char *line)
{
	size_t tmpsize;
	struct x400pNearEndCurrentTable_data *StorageTmp = x400pNearEndCurrentTable_create();

	DEBUGMSGTL(("x400pMxMIB", "parse_x400pNearEndCurrentTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->x400pDrivName);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->x400pDrivName, &StorageTmp->x400pDrivNameLen);
	if (StorageTmp->x400pDrivName == NULL) {
		config_perror("invalid specification for x400pDrivName");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->x400pCardIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->x400pSpanIndex, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pNearEndCurrentTimeElapsed, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->x400pNearEndCurrentESs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->x400pNearEndCurrentSESs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->x400pNearEndCurrentSEFSs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->x400pNearEndCurrentUASs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->x400pNearEndCurrentCSSs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->x400pNearEndCurrentPCVs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->x400pNearEndCurrentLESs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->x400pNearEndCurrentBESs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->x400pNearEndCurrentDMs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->x400pNearEndCurrentLCVs, &tmpsize);
	x400pNearEndCurrentTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
}

/*
 * store_x400pNearEndCurrentTable(): store configuraiton file for x400pNearEndCurrentTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_x400pNearEndCurrentTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct x400pNearEndCurrentTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("x400pMxMIB", "store_x400pNearEndCurrentTable: storing data...  "));
	refresh_x400pNearEndCurrentTable(1);
	(void) tmpsize;
	for (hcindex = x400pNearEndCurrentTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct x400pNearEndCurrentTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "x400pNearEndCurrentTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->x400pDrivName, &StorageTmp->x400pDrivNameLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->x400pCardIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->x400pSpanIndex, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pNearEndCurrentTimeElapsed, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->x400pNearEndCurrentESs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->x400pNearEndCurrentSESs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->x400pNearEndCurrentSEFSs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->x400pNearEndCurrentUASs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->x400pNearEndCurrentCSSs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->x400pNearEndCurrentPCVs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->x400pNearEndCurrentLESs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->x400pNearEndCurrentBESs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->x400pNearEndCurrentDMs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->x400pNearEndCurrentLCVs, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct x400pNearEndIntervalTable_data *x400pNearEndIntervalTable_create(void)
 * @brief create a fresh data structure representing a new row in the x400pNearEndIntervalTable table.
 *
 * Creates a new x400pNearEndIntervalTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct x400pNearEndIntervalTable_data *
x400pNearEndIntervalTable_create(void)
{
	struct x400pNearEndIntervalTable_data *StorageNew = SNMP_MALLOC_STRUCT(x400pNearEndIntervalTable_data);

	DEBUGMSGTL(("x400pMxMIB", "x400pNearEndIntervalTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->x400pDrivName = malloc(1)) == NULL)
			goto nomem;
		StorageNew->x400pDrivNameLen = 0;
		StorageNew->x400pDrivName[StorageNew->x400pDrivNameLen] = 0;
		StorageNew->x400pCardIndex = 0;
		StorageNew->x400pSpanIndex = 0;
		StorageNew->x400pNearEndIntervalESs = 0;
		StorageNew->x400pNearEndIntervalSESs = 0;
		StorageNew->x400pNearEndIntervalSEFSs = 0;
		StorageNew->x400pNearEndIntervalUASs = 0;
		StorageNew->x400pNearEndIntervalCSSs = 0;
		StorageNew->x400pNearEndIntervalPCVs = 0;
		StorageNew->x400pNearEndIntervalLESs = 0;
		StorageNew->x400pNearEndIntervalBESs = 0;
		StorageNew->x400pNearEndIntervalDMs = 0;
		StorageNew->x400pNearEndIntervalLCVs = 0;
		StorageNew->x400pNearEndIntervalValidData = 0;
	}
      done:
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	x400pNearEndIntervalTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct x400pNearEndIntervalTable_data *x400pNearEndIntervalTable_duplicate(struct x400pNearEndIntervalTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct x400pNearEndIntervalTable_data *
x400pNearEndIntervalTable_duplicate(struct x400pNearEndIntervalTable_data *thedata)
{
	struct x400pNearEndIntervalTable_data *StorageNew = SNMP_MALLOC_STRUCT(x400pNearEndIntervalTable_data);

	DEBUGMSGTL(("x400pMxMIB", "x400pNearEndIntervalTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->x400pNearEndIntervalTable_id = thedata->x400pNearEndIntervalTable_id;
		if (!(StorageNew->x400pDrivName = malloc(thedata->x400pDrivNameLen + 1)))
			goto destroy;
		memcpy(StorageNew->x400pDrivName, thedata->x400pDrivName, thedata->x400pDrivNameLen);
		StorageNew->x400pDrivNameLen = thedata->x400pDrivNameLen;
		StorageNew->x400pDrivName[StorageNew->x400pDrivNameLen] = 0;
		StorageNew->x400pCardIndex = thedata->x400pCardIndex;
		StorageNew->x400pSpanIndex = thedata->x400pSpanIndex;
		StorageNew->x400pNearEndIntervalIndex = thedata->x400pNearEndIntervalIndex;
		StorageNew->x400pNearEndIntervalESs = thedata->x400pNearEndIntervalESs;
		StorageNew->x400pNearEndIntervalSESs = thedata->x400pNearEndIntervalSESs;
		StorageNew->x400pNearEndIntervalSEFSs = thedata->x400pNearEndIntervalSEFSs;
		StorageNew->x400pNearEndIntervalUASs = thedata->x400pNearEndIntervalUASs;
		StorageNew->x400pNearEndIntervalCSSs = thedata->x400pNearEndIntervalCSSs;
		StorageNew->x400pNearEndIntervalPCVs = thedata->x400pNearEndIntervalPCVs;
		StorageNew->x400pNearEndIntervalLESs = thedata->x400pNearEndIntervalLESs;
		StorageNew->x400pNearEndIntervalBESs = thedata->x400pNearEndIntervalBESs;
		StorageNew->x400pNearEndIntervalDMs = thedata->x400pNearEndIntervalDMs;
		StorageNew->x400pNearEndIntervalLCVs = thedata->x400pNearEndIntervalLCVs;
		StorageNew->x400pNearEndIntervalValidData = thedata->x400pNearEndIntervalValidData;
	}
      done:
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	x400pNearEndIntervalTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int x400pNearEndIntervalTable_destroy(struct x400pNearEndIntervalTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
x400pNearEndIntervalTable_destroy(struct x400pNearEndIntervalTable_data **thedata)
{
	struct x400pNearEndIntervalTable_data *StorageDel;

	DEBUGMSGTL(("x400pMxMIB", "x400pNearEndIntervalTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->x400pDrivName);
		StorageDel->x400pDrivNameLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int x400pNearEndIntervalTable_add(struct x400pNearEndIntervalTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the x400pNearEndIntervalTable table data set.
 *
 * Adds a table row structure to the x400pNearEndIntervalTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
x400pNearEndIntervalTable_add(struct x400pNearEndIntervalTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("x400pMxMIB", "x400pNearEndIntervalTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* x400pDrivName */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->x400pDrivName, thedata->x400pDrivNameLen);
		/* x400pCardIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->x400pCardIndex, sizeof(thedata->x400pCardIndex));
		/* x400pSpanIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->x400pSpanIndex, sizeof(thedata->x400pSpanIndex));
		/* x400pNearEndIntervalIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->x400pNearEndIntervalIndex, sizeof(thedata->x400pNearEndIntervalIndex));
		header_complex_add_data(&x400pNearEndIntervalTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("x400pMxMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int x400pNearEndIntervalTable_del(struct x400pNearEndIntervalTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the x400pNearEndIntervalTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
x400pNearEndIntervalTable_del(struct x400pNearEndIntervalTable_data *thedata)
{
	struct x400pNearEndIntervalTable_data *StorageDel;

	DEBUGMSGTL(("x400pMxMIB", "x400pNearEndIntervalTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(x400pNearEndIntervalTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&x400pNearEndIntervalTableStorage, hciptr);
	}
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_x400pNearEndIntervalTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for x400pNearEndIntervalTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case x400pNearEndIntervalTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_x400pNearEndIntervalTable(const char *token, char *line)
{
	size_t tmpsize;
	struct x400pNearEndIntervalTable_data *StorageTmp = x400pNearEndIntervalTable_create();

	DEBUGMSGTL(("x400pMxMIB", "parse_x400pNearEndIntervalTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->x400pDrivName);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->x400pDrivName, &StorageTmp->x400pDrivNameLen);
	if (StorageTmp->x400pDrivName == NULL) {
		config_perror("invalid specification for x400pDrivName");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->x400pCardIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->x400pSpanIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->x400pNearEndIntervalIndex, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->x400pNearEndIntervalESs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->x400pNearEndIntervalSESs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->x400pNearEndIntervalSEFSs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->x400pNearEndIntervalUASs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->x400pNearEndIntervalCSSs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->x400pNearEndIntervalPCVs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->x400pNearEndIntervalLESs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->x400pNearEndIntervalBESs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->x400pNearEndIntervalDMs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->x400pNearEndIntervalLCVs, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pNearEndIntervalValidData, &tmpsize);
	x400pNearEndIntervalTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
}

/*
 * store_x400pNearEndIntervalTable(): store configuraiton file for x400pNearEndIntervalTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_x400pNearEndIntervalTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct x400pNearEndIntervalTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("x400pMxMIB", "store_x400pNearEndIntervalTable: storing data...  "));
	refresh_x400pNearEndIntervalTable(1);
	(void) tmpsize;
	for (hcindex = x400pNearEndIntervalTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct x400pNearEndIntervalTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "x400pNearEndIntervalTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->x400pDrivName, &StorageTmp->x400pDrivNameLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->x400pCardIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->x400pSpanIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->x400pNearEndIntervalIndex, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->x400pNearEndIntervalESs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->x400pNearEndIntervalSESs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->x400pNearEndIntervalSEFSs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->x400pNearEndIntervalUASs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->x400pNearEndIntervalCSSs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->x400pNearEndIntervalPCVs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->x400pNearEndIntervalLESs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->x400pNearEndIntervalBESs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->x400pNearEndIntervalDMs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->x400pNearEndIntervalLCVs, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pNearEndIntervalValidData, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct x400pNearEndTotalTable_data *x400pNearEndTotalTable_create(void)
 * @brief create a fresh data structure representing a new row in the x400pNearEndTotalTable table.
 *
 * Creates a new x400pNearEndTotalTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct x400pNearEndTotalTable_data *
x400pNearEndTotalTable_create(void)
{
	struct x400pNearEndTotalTable_data *StorageNew = SNMP_MALLOC_STRUCT(x400pNearEndTotalTable_data);

	DEBUGMSGTL(("x400pMxMIB", "x400pNearEndTotalTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->x400pDrivName = malloc(1)) == NULL)
			goto nomem;
		StorageNew->x400pDrivNameLen = 0;
		StorageNew->x400pDrivName[StorageNew->x400pDrivNameLen] = 0;
		StorageNew->x400pCardIndex = 0;
		StorageNew->x400pSpanIndex = 0;
		StorageNew->x400pNearEndTotalValidIntervals = 0;
		StorageNew->x400pNearEndTotalInvalidIntervals = 0;
		StorageNew->x400pNearEndTotalESs = 0;
		StorageNew->x400pNearEndTotalSESs = 0;
		StorageNew->x400pNearEndTotalSEFSs = 0;
		StorageNew->x400pNearEndTotalUASs = 0;
		StorageNew->x400pNearEndTotalCSSs = 0;
		StorageNew->x400pNearEndTotalPCVs = 0;
		StorageNew->x400pNearEndTotalLESs = 0;
		StorageNew->x400pNearEndTotalBESs = 0;
		StorageNew->x400pNearEndTotalDMs = 0;
		StorageNew->x400pNearEndTotalLCVs = 0;
	}
      done:
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	x400pNearEndTotalTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct x400pNearEndTotalTable_data *x400pNearEndTotalTable_duplicate(struct x400pNearEndTotalTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct x400pNearEndTotalTable_data *
x400pNearEndTotalTable_duplicate(struct x400pNearEndTotalTable_data *thedata)
{
	struct x400pNearEndTotalTable_data *StorageNew = SNMP_MALLOC_STRUCT(x400pNearEndTotalTable_data);

	DEBUGMSGTL(("x400pMxMIB", "x400pNearEndTotalTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->x400pNearEndTotalTable_id = thedata->x400pNearEndTotalTable_id;
		if (!(StorageNew->x400pDrivName = malloc(thedata->x400pDrivNameLen + 1)))
			goto destroy;
		memcpy(StorageNew->x400pDrivName, thedata->x400pDrivName, thedata->x400pDrivNameLen);
		StorageNew->x400pDrivNameLen = thedata->x400pDrivNameLen;
		StorageNew->x400pDrivName[StorageNew->x400pDrivNameLen] = 0;
		StorageNew->x400pCardIndex = thedata->x400pCardIndex;
		StorageNew->x400pSpanIndex = thedata->x400pSpanIndex;
		StorageNew->x400pNearEndTotalValidIntervals = thedata->x400pNearEndTotalValidIntervals;
		StorageNew->x400pNearEndTotalInvalidIntervals = thedata->x400pNearEndTotalInvalidIntervals;
		StorageNew->x400pNearEndTotalESs = thedata->x400pNearEndTotalESs;
		StorageNew->x400pNearEndTotalSESs = thedata->x400pNearEndTotalSESs;
		StorageNew->x400pNearEndTotalSEFSs = thedata->x400pNearEndTotalSEFSs;
		StorageNew->x400pNearEndTotalUASs = thedata->x400pNearEndTotalUASs;
		StorageNew->x400pNearEndTotalCSSs = thedata->x400pNearEndTotalCSSs;
		StorageNew->x400pNearEndTotalPCVs = thedata->x400pNearEndTotalPCVs;
		StorageNew->x400pNearEndTotalLESs = thedata->x400pNearEndTotalLESs;
		StorageNew->x400pNearEndTotalBESs = thedata->x400pNearEndTotalBESs;
		StorageNew->x400pNearEndTotalDMs = thedata->x400pNearEndTotalDMs;
		StorageNew->x400pNearEndTotalLCVs = thedata->x400pNearEndTotalLCVs;
	}
      done:
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	x400pNearEndTotalTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int x400pNearEndTotalTable_destroy(struct x400pNearEndTotalTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
x400pNearEndTotalTable_destroy(struct x400pNearEndTotalTable_data **thedata)
{
	struct x400pNearEndTotalTable_data *StorageDel;

	DEBUGMSGTL(("x400pMxMIB", "x400pNearEndTotalTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->x400pDrivName);
		StorageDel->x400pDrivNameLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int x400pNearEndTotalTable_add(struct x400pNearEndTotalTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the x400pNearEndTotalTable table data set.
 *
 * Adds a table row structure to the x400pNearEndTotalTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
x400pNearEndTotalTable_add(struct x400pNearEndTotalTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("x400pMxMIB", "x400pNearEndTotalTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* x400pDrivName */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->x400pDrivName, thedata->x400pDrivNameLen);
		/* x400pCardIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->x400pCardIndex, sizeof(thedata->x400pCardIndex));
		/* x400pSpanIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->x400pSpanIndex, sizeof(thedata->x400pSpanIndex));
		header_complex_add_data(&x400pNearEndTotalTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("x400pMxMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int x400pNearEndTotalTable_del(struct x400pNearEndTotalTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the x400pNearEndTotalTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
x400pNearEndTotalTable_del(struct x400pNearEndTotalTable_data *thedata)
{
	struct x400pNearEndTotalTable_data *StorageDel;

	DEBUGMSGTL(("x400pMxMIB", "x400pNearEndTotalTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(x400pNearEndTotalTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&x400pNearEndTotalTableStorage, hciptr);
	}
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_x400pNearEndTotalTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for x400pNearEndTotalTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case x400pNearEndTotalTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_x400pNearEndTotalTable(const char *token, char *line)
{
	size_t tmpsize;
	struct x400pNearEndTotalTable_data *StorageTmp = x400pNearEndTotalTable_create();

	DEBUGMSGTL(("x400pMxMIB", "parse_x400pNearEndTotalTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->x400pDrivName);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->x400pDrivName, &StorageTmp->x400pDrivNameLen);
	if (StorageTmp->x400pDrivName == NULL) {
		config_perror("invalid specification for x400pDrivName");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->x400pCardIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->x400pSpanIndex, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pNearEndTotalValidIntervals, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pNearEndTotalInvalidIntervals, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->x400pNearEndTotalESs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->x400pNearEndTotalSESs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->x400pNearEndTotalSEFSs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->x400pNearEndTotalUASs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->x400pNearEndTotalCSSs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->x400pNearEndTotalPCVs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->x400pNearEndTotalLESs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->x400pNearEndTotalBESs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->x400pNearEndTotalDMs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->x400pNearEndTotalLCVs, &tmpsize);
	x400pNearEndTotalTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
}

/*
 * store_x400pNearEndTotalTable(): store configuraiton file for x400pNearEndTotalTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_x400pNearEndTotalTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct x400pNearEndTotalTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("x400pMxMIB", "store_x400pNearEndTotalTable: storing data...  "));
	refresh_x400pNearEndTotalTable(1);
	(void) tmpsize;
	for (hcindex = x400pNearEndTotalTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct x400pNearEndTotalTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "x400pNearEndTotalTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->x400pDrivName, &StorageTmp->x400pDrivNameLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->x400pCardIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->x400pSpanIndex, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pNearEndTotalValidIntervals, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pNearEndTotalInvalidIntervals, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->x400pNearEndTotalESs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->x400pNearEndTotalSESs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->x400pNearEndTotalSEFSs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->x400pNearEndTotalUASs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->x400pNearEndTotalCSSs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->x400pNearEndTotalPCVs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->x400pNearEndTotalLESs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->x400pNearEndTotalBESs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->x400pNearEndTotalDMs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->x400pNearEndTotalLCVs, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct x400pFarEndCurrentTable_data *x400pFarEndCurrentTable_create(void)
 * @brief create a fresh data structure representing a new row in the x400pFarEndCurrentTable table.
 *
 * Creates a new x400pFarEndCurrentTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct x400pFarEndCurrentTable_data *
x400pFarEndCurrentTable_create(void)
{
	struct x400pFarEndCurrentTable_data *StorageNew = SNMP_MALLOC_STRUCT(x400pFarEndCurrentTable_data);

	DEBUGMSGTL(("x400pMxMIB", "x400pFarEndCurrentTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->x400pDrivName = malloc(1)) == NULL)
			goto nomem;
		StorageNew->x400pDrivNameLen = 0;
		StorageNew->x400pDrivName[StorageNew->x400pDrivNameLen] = 0;
		StorageNew->x400pCardIndex = 0;
		StorageNew->x400pSpanIndex = 0;
		StorageNew->x400pFarEndCurrentTimeElapsed = 0;
		StorageNew->x400pFarEndCurrentESs = 0;
		StorageNew->x400pFarEndCurrentSESs = 0;
		StorageNew->x400pFarEndCurrentSEFSs = 0;
		StorageNew->x400pFarEndCurrentUASs = 0;
		StorageNew->x400pFarEndCurrentCSSs = 0;
		StorageNew->x400pFarEndCurrentPCVs = 0;
		StorageNew->x400pFarEndCurrentLESs = 0;
		StorageNew->x400pFarEndCurrentBESs = 0;
		StorageNew->x400pFarEndCurrentDMs = 0;
	}
      done:
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	x400pFarEndCurrentTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct x400pFarEndCurrentTable_data *x400pFarEndCurrentTable_duplicate(struct x400pFarEndCurrentTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct x400pFarEndCurrentTable_data *
x400pFarEndCurrentTable_duplicate(struct x400pFarEndCurrentTable_data *thedata)
{
	struct x400pFarEndCurrentTable_data *StorageNew = SNMP_MALLOC_STRUCT(x400pFarEndCurrentTable_data);

	DEBUGMSGTL(("x400pMxMIB", "x400pFarEndCurrentTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->x400pFarEndCurrentTable_id = thedata->x400pFarEndCurrentTable_id;
		if (!(StorageNew->x400pDrivName = malloc(thedata->x400pDrivNameLen + 1)))
			goto destroy;
		memcpy(StorageNew->x400pDrivName, thedata->x400pDrivName, thedata->x400pDrivNameLen);
		StorageNew->x400pDrivNameLen = thedata->x400pDrivNameLen;
		StorageNew->x400pDrivName[StorageNew->x400pDrivNameLen] = 0;
		StorageNew->x400pCardIndex = thedata->x400pCardIndex;
		StorageNew->x400pSpanIndex = thedata->x400pSpanIndex;
		StorageNew->x400pFarEndCurrentTimeElapsed = thedata->x400pFarEndCurrentTimeElapsed;
		StorageNew->x400pFarEndCurrentESs = thedata->x400pFarEndCurrentESs;
		StorageNew->x400pFarEndCurrentSESs = thedata->x400pFarEndCurrentSESs;
		StorageNew->x400pFarEndCurrentSEFSs = thedata->x400pFarEndCurrentSEFSs;
		StorageNew->x400pFarEndCurrentUASs = thedata->x400pFarEndCurrentUASs;
		StorageNew->x400pFarEndCurrentCSSs = thedata->x400pFarEndCurrentCSSs;
		StorageNew->x400pFarEndCurrentPCVs = thedata->x400pFarEndCurrentPCVs;
		StorageNew->x400pFarEndCurrentLESs = thedata->x400pFarEndCurrentLESs;
		StorageNew->x400pFarEndCurrentBESs = thedata->x400pFarEndCurrentBESs;
		StorageNew->x400pFarEndCurrentDMs = thedata->x400pFarEndCurrentDMs;
	}
      done:
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	x400pFarEndCurrentTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int x400pFarEndCurrentTable_destroy(struct x400pFarEndCurrentTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
x400pFarEndCurrentTable_destroy(struct x400pFarEndCurrentTable_data **thedata)
{
	struct x400pFarEndCurrentTable_data *StorageDel;

	DEBUGMSGTL(("x400pMxMIB", "x400pFarEndCurrentTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->x400pDrivName);
		StorageDel->x400pDrivNameLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int x400pFarEndCurrentTable_add(struct x400pFarEndCurrentTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the x400pFarEndCurrentTable table data set.
 *
 * Adds a table row structure to the x400pFarEndCurrentTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
x400pFarEndCurrentTable_add(struct x400pFarEndCurrentTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("x400pMxMIB", "x400pFarEndCurrentTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* x400pDrivName */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->x400pDrivName, thedata->x400pDrivNameLen);
		/* x400pCardIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->x400pCardIndex, sizeof(thedata->x400pCardIndex));
		/* x400pSpanIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->x400pSpanIndex, sizeof(thedata->x400pSpanIndex));
		header_complex_add_data(&x400pFarEndCurrentTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("x400pMxMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int x400pFarEndCurrentTable_del(struct x400pFarEndCurrentTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the x400pFarEndCurrentTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
x400pFarEndCurrentTable_del(struct x400pFarEndCurrentTable_data *thedata)
{
	struct x400pFarEndCurrentTable_data *StorageDel;

	DEBUGMSGTL(("x400pMxMIB", "x400pFarEndCurrentTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(x400pFarEndCurrentTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&x400pFarEndCurrentTableStorage, hciptr);
	}
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_x400pFarEndCurrentTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for x400pFarEndCurrentTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case x400pFarEndCurrentTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_x400pFarEndCurrentTable(const char *token, char *line)
{
	size_t tmpsize;
	struct x400pFarEndCurrentTable_data *StorageTmp = x400pFarEndCurrentTable_create();

	DEBUGMSGTL(("x400pMxMIB", "parse_x400pFarEndCurrentTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->x400pDrivName);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->x400pDrivName, &StorageTmp->x400pDrivNameLen);
	if (StorageTmp->x400pDrivName == NULL) {
		config_perror("invalid specification for x400pDrivName");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->x400pCardIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->x400pSpanIndex, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pFarEndCurrentTimeElapsed, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->x400pFarEndCurrentESs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->x400pFarEndCurrentSESs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->x400pFarEndCurrentSEFSs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->x400pFarEndCurrentUASs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->x400pFarEndCurrentCSSs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->x400pFarEndCurrentPCVs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->x400pFarEndCurrentLESs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->x400pFarEndCurrentBESs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->x400pFarEndCurrentDMs, &tmpsize);
	x400pFarEndCurrentTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
}

/*
 * store_x400pFarEndCurrentTable(): store configuraiton file for x400pFarEndCurrentTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_x400pFarEndCurrentTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct x400pFarEndCurrentTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("x400pMxMIB", "store_x400pFarEndCurrentTable: storing data...  "));
	refresh_x400pFarEndCurrentTable(1);
	(void) tmpsize;
	for (hcindex = x400pFarEndCurrentTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct x400pFarEndCurrentTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "x400pFarEndCurrentTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->x400pDrivName, &StorageTmp->x400pDrivNameLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->x400pCardIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->x400pSpanIndex, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pFarEndCurrentTimeElapsed, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->x400pFarEndCurrentESs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->x400pFarEndCurrentSESs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->x400pFarEndCurrentSEFSs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->x400pFarEndCurrentUASs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->x400pFarEndCurrentCSSs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->x400pFarEndCurrentPCVs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->x400pFarEndCurrentLESs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->x400pFarEndCurrentBESs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->x400pFarEndCurrentDMs, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct x400pFarEndIntervalTable_data *x400pFarEndIntervalTable_create(void)
 * @brief create a fresh data structure representing a new row in the x400pFarEndIntervalTable table.
 *
 * Creates a new x400pFarEndIntervalTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct x400pFarEndIntervalTable_data *
x400pFarEndIntervalTable_create(void)
{
	struct x400pFarEndIntervalTable_data *StorageNew = SNMP_MALLOC_STRUCT(x400pFarEndIntervalTable_data);

	DEBUGMSGTL(("x400pMxMIB", "x400pFarEndIntervalTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->x400pDrivName = malloc(1)) == NULL)
			goto nomem;
		StorageNew->x400pDrivNameLen = 0;
		StorageNew->x400pDrivName[StorageNew->x400pDrivNameLen] = 0;
		StorageNew->x400pCardIndex = 0;
		StorageNew->x400pSpanIndex = 0;
		StorageNew->x400pFarEndIntervalESs = 0;
		StorageNew->x400pFarEndIntervalSESs = 0;
		StorageNew->x400pFarEndIntervalSEFSs = 0;
		StorageNew->x400pFarEndIntervalUASs = 0;
		StorageNew->x400pFarEndIntervalCSSs = 0;
		StorageNew->x400pFarEndIntervalPCVs = 0;
		StorageNew->x400pFarEndIntervalLESs = 0;
		StorageNew->x400pFarEndIntervalBESs = 0;
		StorageNew->x400pFarEndIntervalDMs = 0;
		StorageNew->x400pFarEndIntervalValidData = 0;
	}
      done:
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	x400pFarEndIntervalTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct x400pFarEndIntervalTable_data *x400pFarEndIntervalTable_duplicate(struct x400pFarEndIntervalTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct x400pFarEndIntervalTable_data *
x400pFarEndIntervalTable_duplicate(struct x400pFarEndIntervalTable_data *thedata)
{
	struct x400pFarEndIntervalTable_data *StorageNew = SNMP_MALLOC_STRUCT(x400pFarEndIntervalTable_data);

	DEBUGMSGTL(("x400pMxMIB", "x400pFarEndIntervalTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->x400pFarEndIntervalTable_id = thedata->x400pFarEndIntervalTable_id;
		if (!(StorageNew->x400pDrivName = malloc(thedata->x400pDrivNameLen + 1)))
			goto destroy;
		memcpy(StorageNew->x400pDrivName, thedata->x400pDrivName, thedata->x400pDrivNameLen);
		StorageNew->x400pDrivNameLen = thedata->x400pDrivNameLen;
		StorageNew->x400pDrivName[StorageNew->x400pDrivNameLen] = 0;
		StorageNew->x400pCardIndex = thedata->x400pCardIndex;
		StorageNew->x400pSpanIndex = thedata->x400pSpanIndex;
		StorageNew->x400pFarEndIntervalIndex = thedata->x400pFarEndIntervalIndex;
		StorageNew->x400pFarEndIntervalESs = thedata->x400pFarEndIntervalESs;
		StorageNew->x400pFarEndIntervalSESs = thedata->x400pFarEndIntervalSESs;
		StorageNew->x400pFarEndIntervalSEFSs = thedata->x400pFarEndIntervalSEFSs;
		StorageNew->x400pFarEndIntervalUASs = thedata->x400pFarEndIntervalUASs;
		StorageNew->x400pFarEndIntervalCSSs = thedata->x400pFarEndIntervalCSSs;
		StorageNew->x400pFarEndIntervalPCVs = thedata->x400pFarEndIntervalPCVs;
		StorageNew->x400pFarEndIntervalLESs = thedata->x400pFarEndIntervalLESs;
		StorageNew->x400pFarEndIntervalBESs = thedata->x400pFarEndIntervalBESs;
		StorageNew->x400pFarEndIntervalDMs = thedata->x400pFarEndIntervalDMs;
		StorageNew->x400pFarEndIntervalValidData = thedata->x400pFarEndIntervalValidData;
	}
      done:
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	x400pFarEndIntervalTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int x400pFarEndIntervalTable_destroy(struct x400pFarEndIntervalTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
x400pFarEndIntervalTable_destroy(struct x400pFarEndIntervalTable_data **thedata)
{
	struct x400pFarEndIntervalTable_data *StorageDel;

	DEBUGMSGTL(("x400pMxMIB", "x400pFarEndIntervalTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->x400pDrivName);
		StorageDel->x400pDrivNameLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int x400pFarEndIntervalTable_add(struct x400pFarEndIntervalTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the x400pFarEndIntervalTable table data set.
 *
 * Adds a table row structure to the x400pFarEndIntervalTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
x400pFarEndIntervalTable_add(struct x400pFarEndIntervalTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("x400pMxMIB", "x400pFarEndIntervalTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* x400pDrivName */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->x400pDrivName, thedata->x400pDrivNameLen);
		/* x400pCardIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->x400pCardIndex, sizeof(thedata->x400pCardIndex));
		/* x400pSpanIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->x400pSpanIndex, sizeof(thedata->x400pSpanIndex));
		/* x400pFarEndIntervalIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->x400pFarEndIntervalIndex, sizeof(thedata->x400pFarEndIntervalIndex));
		header_complex_add_data(&x400pFarEndIntervalTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("x400pMxMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int x400pFarEndIntervalTable_del(struct x400pFarEndIntervalTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the x400pFarEndIntervalTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
x400pFarEndIntervalTable_del(struct x400pFarEndIntervalTable_data *thedata)
{
	struct x400pFarEndIntervalTable_data *StorageDel;

	DEBUGMSGTL(("x400pMxMIB", "x400pFarEndIntervalTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(x400pFarEndIntervalTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&x400pFarEndIntervalTableStorage, hciptr);
	}
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_x400pFarEndIntervalTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for x400pFarEndIntervalTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case x400pFarEndIntervalTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_x400pFarEndIntervalTable(const char *token, char *line)
{
	size_t tmpsize;
	struct x400pFarEndIntervalTable_data *StorageTmp = x400pFarEndIntervalTable_create();

	DEBUGMSGTL(("x400pMxMIB", "parse_x400pFarEndIntervalTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->x400pDrivName);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->x400pDrivName, &StorageTmp->x400pDrivNameLen);
	if (StorageTmp->x400pDrivName == NULL) {
		config_perror("invalid specification for x400pDrivName");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->x400pCardIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->x400pSpanIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->x400pFarEndIntervalIndex, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->x400pFarEndIntervalESs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->x400pFarEndIntervalSESs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->x400pFarEndIntervalSEFSs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->x400pFarEndIntervalUASs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->x400pFarEndIntervalCSSs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->x400pFarEndIntervalPCVs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->x400pFarEndIntervalLESs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->x400pFarEndIntervalBESs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->x400pFarEndIntervalDMs, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pFarEndIntervalValidData, &tmpsize);
	x400pFarEndIntervalTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
}

/*
 * store_x400pFarEndIntervalTable(): store configuraiton file for x400pFarEndIntervalTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_x400pFarEndIntervalTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct x400pFarEndIntervalTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("x400pMxMIB", "store_x400pFarEndIntervalTable: storing data...  "));
	refresh_x400pFarEndIntervalTable(1);
	(void) tmpsize;
	for (hcindex = x400pFarEndIntervalTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct x400pFarEndIntervalTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "x400pFarEndIntervalTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->x400pDrivName, &StorageTmp->x400pDrivNameLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->x400pCardIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->x400pSpanIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->x400pFarEndIntervalIndex, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->x400pFarEndIntervalESs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->x400pFarEndIntervalSESs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->x400pFarEndIntervalSEFSs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->x400pFarEndIntervalUASs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->x400pFarEndIntervalCSSs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->x400pFarEndIntervalPCVs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->x400pFarEndIntervalLESs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->x400pFarEndIntervalBESs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->x400pFarEndIntervalDMs, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pFarEndIntervalValidData, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct x400pFarEndTotalTable_data *x400pFarEndTotalTable_create(void)
 * @brief create a fresh data structure representing a new row in the x400pFarEndTotalTable table.
 *
 * Creates a new x400pFarEndTotalTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct x400pFarEndTotalTable_data *
x400pFarEndTotalTable_create(void)
{
	struct x400pFarEndTotalTable_data *StorageNew = SNMP_MALLOC_STRUCT(x400pFarEndTotalTable_data);

	DEBUGMSGTL(("x400pMxMIB", "x400pFarEndTotalTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->x400pDrivName = malloc(1)) == NULL)
			goto nomem;
		StorageNew->x400pDrivNameLen = 0;
		StorageNew->x400pDrivName[StorageNew->x400pDrivNameLen] = 0;
		StorageNew->x400pCardIndex = 0;
		StorageNew->x400pSpanIndex = 0;
		StorageNew->x400pFarEndTotalValidIntervals = 0;
		StorageNew->x400pFarEndTotalInvalidIntervals = 0;
		StorageNew->x400pFarEndTotalESs = 0;
		StorageNew->x400pFarEndTotalSESs = 0;
		StorageNew->x400pFarEndTotalSEFSs = 0;
		StorageNew->x400pFarEndTotalUASs = 0;
		StorageNew->x400pFarEndTotalCSSs = 0;
		StorageNew->x400pFarEndTotalPCVs = 0;
		StorageNew->x400pFarEndTotalLESs = 0;
		StorageNew->x400pFarEndTotalBESs = 0;
		StorageNew->x400pFarEndTotalDMs = 0;
	}
      done:
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	x400pFarEndTotalTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct x400pFarEndTotalTable_data *x400pFarEndTotalTable_duplicate(struct x400pFarEndTotalTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct x400pFarEndTotalTable_data *
x400pFarEndTotalTable_duplicate(struct x400pFarEndTotalTable_data *thedata)
{
	struct x400pFarEndTotalTable_data *StorageNew = SNMP_MALLOC_STRUCT(x400pFarEndTotalTable_data);

	DEBUGMSGTL(("x400pMxMIB", "x400pFarEndTotalTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->x400pFarEndTotalTable_id = thedata->x400pFarEndTotalTable_id;
		if (!(StorageNew->x400pDrivName = malloc(thedata->x400pDrivNameLen + 1)))
			goto destroy;
		memcpy(StorageNew->x400pDrivName, thedata->x400pDrivName, thedata->x400pDrivNameLen);
		StorageNew->x400pDrivNameLen = thedata->x400pDrivNameLen;
		StorageNew->x400pDrivName[StorageNew->x400pDrivNameLen] = 0;
		StorageNew->x400pCardIndex = thedata->x400pCardIndex;
		StorageNew->x400pSpanIndex = thedata->x400pSpanIndex;
		StorageNew->x400pFarEndTotalValidIntervals = thedata->x400pFarEndTotalValidIntervals;
		StorageNew->x400pFarEndTotalInvalidIntervals = thedata->x400pFarEndTotalInvalidIntervals;
		StorageNew->x400pFarEndTotalESs = thedata->x400pFarEndTotalESs;
		StorageNew->x400pFarEndTotalSESs = thedata->x400pFarEndTotalSESs;
		StorageNew->x400pFarEndTotalSEFSs = thedata->x400pFarEndTotalSEFSs;
		StorageNew->x400pFarEndTotalUASs = thedata->x400pFarEndTotalUASs;
		StorageNew->x400pFarEndTotalCSSs = thedata->x400pFarEndTotalCSSs;
		StorageNew->x400pFarEndTotalPCVs = thedata->x400pFarEndTotalPCVs;
		StorageNew->x400pFarEndTotalLESs = thedata->x400pFarEndTotalLESs;
		StorageNew->x400pFarEndTotalBESs = thedata->x400pFarEndTotalBESs;
		StorageNew->x400pFarEndTotalDMs = thedata->x400pFarEndTotalDMs;
	}
      done:
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	x400pFarEndTotalTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int x400pFarEndTotalTable_destroy(struct x400pFarEndTotalTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
x400pFarEndTotalTable_destroy(struct x400pFarEndTotalTable_data **thedata)
{
	struct x400pFarEndTotalTable_data *StorageDel;

	DEBUGMSGTL(("x400pMxMIB", "x400pFarEndTotalTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->x400pDrivName);
		StorageDel->x400pDrivNameLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int x400pFarEndTotalTable_add(struct x400pFarEndTotalTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the x400pFarEndTotalTable table data set.
 *
 * Adds a table row structure to the x400pFarEndTotalTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
x400pFarEndTotalTable_add(struct x400pFarEndTotalTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("x400pMxMIB", "x400pFarEndTotalTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* x400pDrivName */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->x400pDrivName, thedata->x400pDrivNameLen);
		/* x400pCardIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->x400pCardIndex, sizeof(thedata->x400pCardIndex));
		/* x400pSpanIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->x400pSpanIndex, sizeof(thedata->x400pSpanIndex));
		header_complex_add_data(&x400pFarEndTotalTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("x400pMxMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int x400pFarEndTotalTable_del(struct x400pFarEndTotalTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the x400pFarEndTotalTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
x400pFarEndTotalTable_del(struct x400pFarEndTotalTable_data *thedata)
{
	struct x400pFarEndTotalTable_data *StorageDel;

	DEBUGMSGTL(("x400pMxMIB", "x400pFarEndTotalTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(x400pFarEndTotalTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&x400pFarEndTotalTableStorage, hciptr);
	}
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_x400pFarEndTotalTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for x400pFarEndTotalTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case x400pFarEndTotalTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_x400pFarEndTotalTable(const char *token, char *line)
{
	size_t tmpsize;
	struct x400pFarEndTotalTable_data *StorageTmp = x400pFarEndTotalTable_create();

	DEBUGMSGTL(("x400pMxMIB", "parse_x400pFarEndTotalTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->x400pDrivName);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->x400pDrivName, &StorageTmp->x400pDrivNameLen);
	if (StorageTmp->x400pDrivName == NULL) {
		config_perror("invalid specification for x400pDrivName");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->x400pCardIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->x400pSpanIndex, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pFarEndTotalValidIntervals, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->x400pFarEndTotalInvalidIntervals, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->x400pFarEndTotalESs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->x400pFarEndTotalSESs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->x400pFarEndTotalSEFSs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->x400pFarEndTotalUASs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->x400pFarEndTotalCSSs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->x400pFarEndTotalPCVs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->x400pFarEndTotalLESs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->x400pFarEndTotalBESs, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->x400pFarEndTotalDMs, &tmpsize);
	x400pFarEndTotalTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
}

/*
 * store_x400pFarEndTotalTable(): store configuraiton file for x400pFarEndTotalTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_x400pFarEndTotalTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct x400pFarEndTotalTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("x400pMxMIB", "store_x400pFarEndTotalTable: storing data...  "));
	refresh_x400pFarEndTotalTable(1);
	(void) tmpsize;
	for (hcindex = x400pFarEndTotalTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct x400pFarEndTotalTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "x400pFarEndTotalTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->x400pDrivName, &StorageTmp->x400pDrivNameLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->x400pCardIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->x400pSpanIndex, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pFarEndTotalValidIntervals, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->x400pFarEndTotalInvalidIntervals, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->x400pFarEndTotalESs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->x400pFarEndTotalSESs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->x400pFarEndTotalSEFSs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->x400pFarEndTotalUASs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->x400pFarEndTotalCSSs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->x400pFarEndTotalPCVs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->x400pFarEndTotalLESs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->x400pFarEndTotalBESs, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->x400pFarEndTotalDMs, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int activate_x400pSyncTable_row(struct x400pSyncTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_x400pSyncTable_row(struct x400pSyncTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_x400pSyncTable_row(struct x400pSyncTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_x400pSyncTable_row(struct x400pSyncTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_x400pDrivTable_row(struct x400pDrivTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_x400pDrivTable_row(struct x400pDrivTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_x400pDrivTable_row(struct x400pDrivTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_x400pDrivTable_row(struct x400pDrivTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_x400pCardTable_row(struct x400pCardTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_x400pCardTable_row(struct x400pCardTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_x400pCardTable_row(struct x400pCardTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_x400pCardTable_row(struct x400pCardTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_x400pSpanTable_row(struct x400pSpanTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_x400pSpanTable_row(struct x400pSpanTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_x400pSpanTable_row(struct x400pSpanTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_x400pSpanTable_row(struct x400pSpanTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_x400pXconTable_row(struct x400pXconTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_x400pXconTable_row(struct x400pXconTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_x400pXconTable_row(struct x400pXconTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_x400pXconTable_row(struct x400pXconTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int check_x400pSyncTable_row(struct x400pSyncTable_data *StorageTmp, struct x400pSyncTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_x400pSyncTable_row(struct x400pSyncTable_data *StorageTmp, struct x400pSyncTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_x400pSyncTable_row(struct x400pSyncTable_data *StorageTmp, struct x400pSyncTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_x400pSyncTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_x400pSyncTable_row(struct x400pSyncTable_data *StorageTmp, struct x400pSyncTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	x400pSyncTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_x400pSyncTable_row(struct x400pSyncTable_data *StorageTmp, struct x400pSyncTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_x400pSyncTable_row(struct x400pSyncTable_data *StorageTmp, struct x400pSyncTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_x400pSyncTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_x400pSyncTable_row(struct x400pSyncTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the x400pSyncTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct x400pSyncTable_data *
refresh_x400pSyncTable_row(struct x400pSyncTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->x400pSyncTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->x400pSyncTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_x400pSyncTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the x400pSyncTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_x400pSyncTable(int force)
{
	if (!force && x400pSyncTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	x400pSyncTable_refresh = 0;
}

/**
 * @fn u_char *var_x400pSyncTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in x400pSyncTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_x400pMxMIB above.
 */
u_char *
var_x400pSyncTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct x400pSyncTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("x400pMxMIB", "var_x400pSyncTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_x400pSyncTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(x400pSyncTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_x400pSyncTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) X400PSYNCSPANID:	/* Create */
		*write_method = write_x400pSyncSpanId;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->x400pSyncSpanIdLen;
		rval = (u_char *) StorageTmp->x400pSyncSpanId;
		break;
	case (u_char) X400PSYNCROWSTATUS:	/* Create */
		*write_method = write_x400pSyncRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pSyncRowStatus);
		rval = (u_char *) &StorageTmp->x400pSyncRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_x400pDrivTable_row(struct x400pDrivTable_data *StorageTmp, struct x400pDrivTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_x400pDrivTable_row(struct x400pDrivTable_data *StorageTmp, struct x400pDrivTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_x400pDrivTable_row(struct x400pDrivTable_data *StorageTmp, struct x400pDrivTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_x400pDrivTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_x400pDrivTable_row(struct x400pDrivTable_data *StorageTmp, struct x400pDrivTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	x400pDrivTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_x400pDrivTable_row(struct x400pDrivTable_data *StorageTmp, struct x400pDrivTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_x400pDrivTable_row(struct x400pDrivTable_data *StorageTmp, struct x400pDrivTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_x400pDrivTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_x400pDrivTable_row(struct x400pDrivTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the x400pDrivTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct x400pDrivTable_data *
refresh_x400pDrivTable_row(struct x400pDrivTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->x400pDrivTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->x400pDrivTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_x400pDrivTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the x400pDrivTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_x400pDrivTable(int force)
{
	if (!force && x400pDrivTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	x400pDrivTable_refresh = 0;
}

/**
 * @fn u_char *var_x400pDrivTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in x400pDrivTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_x400pMxMIB above.
 */
u_char *
var_x400pDrivTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct x400pDrivTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("x400pMxMIB", "var_x400pDrivTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_x400pDrivTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(x400pDrivTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_x400pDrivTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) X400PDRIVIDNUM:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pDrivIdnum);
		rval = (u_char *) &StorageTmp->x400pDrivIdnum;
		break;
	case (u_char) X400PDRIVMAJOR:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pDrivMajor);
		rval = (u_char *) &StorageTmp->x400pDrivMajor;
		break;
	case (u_char) X400PDRIVDESCRIPTION:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->x400pDrivDescriptionLen;
		rval = (u_char *) StorageTmp->x400pDrivDescription;
		break;
	case (u_char) X400PDRIVREVISION:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->x400pDrivRevisionLen;
		rval = (u_char *) StorageTmp->x400pDrivRevision;
		break;
	case (u_char) X400PDRIVCOPYRIGHT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->x400pDrivCopyrightLen;
		rval = (u_char *) StorageTmp->x400pDrivCopyright;
		break;
	case (u_char) X400PDRIVSUPPORTEDDEVICE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->x400pDrivSupportedDeviceLen;
		rval = (u_char *) StorageTmp->x400pDrivSupportedDevice;
		break;
	case (u_char) X400PDRIVCONTACT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->x400pDrivContactLen;
		rval = (u_char *) StorageTmp->x400pDrivContact;
		break;
	case (u_char) X400PDRIVLICENSE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pDrivLicense);
		rval = (u_char *) &StorageTmp->x400pDrivLicense;
		break;
	case (u_char) X400PDRIVDATE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->x400pDrivDateLen;
		rval = (u_char *) StorageTmp->x400pDrivDate;
		break;
	case (u_char) X400PDRIVROWSTATUS:	/* Create */
		*write_method = write_x400pDrivRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pDrivRowStatus);
		rval = (u_char *) &StorageTmp->x400pDrivRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_x400pCardTable_row(struct x400pCardTable_data *StorageTmp, struct x400pCardTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_x400pCardTable_row(struct x400pCardTable_data *StorageTmp, struct x400pCardTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_x400pCardTable_row(struct x400pCardTable_data *StorageTmp, struct x400pCardTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_x400pCardTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_x400pCardTable_row(struct x400pCardTable_data *StorageTmp, struct x400pCardTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	x400pCardTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_x400pCardTable_row(struct x400pCardTable_data *StorageTmp, struct x400pCardTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_x400pCardTable_row(struct x400pCardTable_data *StorageTmp, struct x400pCardTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_x400pCardTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_x400pCardTable_row(struct x400pCardTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the x400pCardTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct x400pCardTable_data *
refresh_x400pCardTable_row(struct x400pCardTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->x400pCardTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->x400pCardTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_x400pCardTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the x400pCardTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_x400pCardTable(int force)
{
	struct header_complex_index *h, *h_next;

	void uinttobits(uint8_t *ostr, uint val, int max) {
		int i;

		for (i = 0; i <= max; i++)
			ostr[i >> 3] = 0;
		for (i = 0; i <= max; i++)
			if (val & (1 << i))
				ostr[i >> 3] = (7 - (i % 8));
	}
	void uinttobitsalloc(uint8_t **optr, uint val, int max) {
		if ((*optr = calloc((max >> 3) + 1, 1)))
			uinttobits(*optr, val, max);
	}

	if (!force && x400pCardTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	x400pCardTable_refresh = 0;
	for (h = x400pDrivTableStorage; h; h = h->next) {
		/* for each entry in the driver table */
		int fd, count, count2, i;
		uint *val;
		struct x400pDrivTable_data *driv = h->data;
		struct strioctl ioc;
		struct x400p_config config, *cptr = &config;

		if (driv->x400pDrivRowStatus != RS_ACTIVE)
			continue;
		if ((fd = open(driv->x400pDrivTable_devname, O_RDWR)) < 0)
			continue;
		cptr = &config;
		cptr->type = MX_OBJ_TYPE_CARD;
		cptr->id = 0;
		cptr->cmd = MX_GET;
		ioc.ic_cmd = MX_IOCLCONFIG;
		ioc.ic_timout = 0;
		ioc.ic_len = sizeof(*cptr);
		ioc.ic_dp = (char *) cptr;
	      retry:
		if ((count = ioctl(fd, I_STR, &ioc)) < 0) {
			close(fd);
			continue;
		}
		if (!(cptr = malloc(sizeof(*cptr) + count * sizeof(uint)))) {
			close(fd);
			continue;
		}
		cptr->type = MX_OBJ_TYPE_CARD;
		cptr->id = 0;
		cptr->cmd = MX_GET;
		ioc.ic_cmd = MX_IOCLCONFIG;
		ioc.ic_timout = 0;
		ioc.ic_len = sizeof(*cptr) + count * sizeof(uint);
		ioc.ic_dp = (char *) cptr;
		if ((count2 = ioctl(fd, I_STR, &ioc)) < 0) {
			free(cptr);
			close(fd);
			continue;
		}
		if (count2 > count) {
			count = count2;
			goto retry;
		}
		for (i = 0, val = (typeof(val)) (cptr + 1); i < count2; i++, val++) {
			struct header_complex_index *hciptr;
			struct {
				struct x400p_attr attr;
				struct x400p_attr_card card;
			} buf;

			buf.attr.type = MX_OBJ_TYPE_CARD;
			buf.attr.id = *val;
			buf.attr.cmd = MX_GET;
			ioc.ic_cmd = MX_IOCGATTR;
			ioc.ic_timout = 0;
			ioc.ic_len = sizeof(buf);
			ioc.ic_dp = (char *) &buf;
			if (ioctl(fd, I_STR, &buf) < 0)
				continue;
			for (hciptr = x400pCardTableStorage; hciptr; hciptr = hciptr->next) {
				struct x400pCardTable_data *data = hciptr->data;

				if (data->x400pDrivNameLen != driv->x400pDrivNameLen || strncmp((char *) data->x400pDrivName, (char *) driv->x400pDrivName, driv->x400pDrivNameLen))
					continue;
				if (data->x400pCardIndex != *val)
					continue;
				break;
			}
			if (hciptr) {
				/* already have an entry for this card index, simply update it */
				struct x400pCardTable_data *StorageTmp = hciptr->data;
				char *tmp;
				oid *tmpoid;

				StorageTmp->x400pCardTable_request = sa_request;	/* mark as updated */
				StorageTmp->x400pCardTable_refs = 1;	/* mark as used */
				/* copy driver name from driver entry */
				if ((tmp = strndup((char *) driv->x400pDrivName, driv->x400pDrivNameLen))) {
					SNMP_FREE(StorageTmp->x400pDrivName);
					StorageTmp->x400pDrivName = (uint8_t *) tmp;
					StorageTmp->x400pDrivNameLen = driv->x400pDrivNameLen;
					StorageTmp->x400pDrivName[StorageTmp->x400pDrivNameLen] = '\0';
				}
				StorageTmp->x400pCardIndex = buf.card.config.x400pCardIndex;
				StorageTmp->x400pCardSpanType = buf.card.config.x400pCardSpanType;
				StorageTmp->x400pCardSyncGroup = buf.card.config.x400pCardSyncGroup;
				switch (buf.card.inform.x400pCardType) {
				case X400PCARDTYPE_NONE:
				default:
					if ((tmpoid = snmp_duplicate_objid(zeroDotZero_oid, 2))) {
						SNMP_FREE(StorageTmp->x400pCardType);
						StorageTmp->x400pCardType = tmpoid;
						StorageTmp->x400pCardTypeLen = 2;
					}
					break;
				case X400PCARDTYPE_PLX9030:
					if ((tmpoid = snmp_duplicate_objid(x400pCardTypePLX9030_oid, 12))) {
						SNMP_FREE(StorageTmp->x400pCardType);
						StorageTmp->x400pCardType = tmpoid;
						StorageTmp->x400pCardTypeLen = 12;
					}
					break;
				case X400PCARDTYPE_PLXDEVBRD:
					if ((tmpoid = snmp_duplicate_objid(x400pCardTypePLXDevelopmentBoard_oid, 12))) {
						SNMP_FREE(StorageTmp->x400pCardType);
						StorageTmp->x400pCardType = tmpoid;
						StorageTmp->x400pCardTypeLen = 12;
					}
					break;
				case X400PCARDTYPE_X400P:
					if ((tmpoid = snmp_duplicate_objid(x400pCardTypeX400P_oid, 12))) {
						SNMP_FREE(StorageTmp->x400pCardType);
						StorageTmp->x400pCardType = tmpoid;
						StorageTmp->x400pCardTypeLen = 12;
					}
					break;
				case X400PCARDTYPE_E400P:
					if ((tmpoid = snmp_duplicate_objid(x400pCardTypeE400P_oid, 13))) {
						SNMP_FREE(StorageTmp->x400pCardType);
						StorageTmp->x400pCardType = tmpoid;
						StorageTmp->x400pCardTypeLen = 13;
					}
					break;
				case X400PCARDTYPE_T400P:
					if ((tmpoid = snmp_duplicate_objid(x400pCardTypeT400P_oid, 13))) {
						SNMP_FREE(StorageTmp->x400pCardType);
						StorageTmp->x400pCardType = tmpoid;
						StorageTmp->x400pCardTypeLen = 13;
					}
					break;
				case X400PCARDTYPE_X400PSS7:
					if ((tmpoid = snmp_duplicate_objid(x400pCardTypeX400P_SS7_oid, 12))) {
						SNMP_FREE(StorageTmp->x400pCardType);
						StorageTmp->x400pCardType = tmpoid;
						StorageTmp->x400pCardTypeLen = 12;
					}
					break;
				case X400PCARDTYPE_E400PSS7:
					if ((tmpoid = snmp_duplicate_objid(x400pCardTypeE400P_SS7_oid, 13))) {
						SNMP_FREE(StorageTmp->x400pCardType);
						StorageTmp->x400pCardType = tmpoid;
						StorageTmp->x400pCardTypeLen = 13;
					}
					break;
				case X400PCARDTYPE_T400PSS7:
					if ((tmpoid = snmp_duplicate_objid(x400pCardTypeT400P_SS7_oid, 13))) {
						SNMP_FREE(StorageTmp->x400pCardType);
						StorageTmp->x400pCardType = tmpoid;
						StorageTmp->x400pCardTypeLen = 13;
					}
					break;
				case X400PCARDTYPE_V400P:
					if ((tmpoid = snmp_duplicate_objid(x400pCardTypeV400P_oid, 12))) {
						SNMP_FREE(StorageTmp->x400pCardType);
						StorageTmp->x400pCardType = tmpoid;
						StorageTmp->x400pCardTypeLen = 12;
					}
					break;
				case X400PCARDTYPE_V400PE:
					if ((tmpoid = snmp_duplicate_objid(x400pCardTypeV400PE_oid, 13))) {
						SNMP_FREE(StorageTmp->x400pCardType);
						StorageTmp->x400pCardType = tmpoid;
						StorageTmp->x400pCardTypeLen = 13;
					}
					break;
				case X400PCARDTYPE_V400PT:
					if ((tmpoid = snmp_duplicate_objid(x400pCardTypeV400PT_oid, 13))) {
						SNMP_FREE(StorageTmp->x400pCardType);
						StorageTmp->x400pCardType = tmpoid;
						StorageTmp->x400pCardTypeLen = 13;
					}
					break;
				case X400PCARDTYPE_V401PE:
					if ((tmpoid = snmp_duplicate_objid(x400pCardTypeV401PE_oid, 12))) {
						SNMP_FREE(StorageTmp->x400pCardType);
						StorageTmp->x400pCardType = tmpoid;
						StorageTmp->x400pCardTypeLen = 12;
					}
					break;
				case X400PCARDTYPE_V401PT:
					if ((tmpoid = snmp_duplicate_objid(x400pCardTypeV401PT_oid, 12))) {
						SNMP_FREE(StorageTmp->x400pCardType);
						StorageTmp->x400pCardType = tmpoid;
						StorageTmp->x400pCardTypeLen = 12;
					}
					break;
				}
				StorageTmp->x400pCardIdentifier = buf.card.inform.x400pCardIdentifier;
				StorageTmp->x400pCardRevision[0] = buf.card.inform.x400pCardRevision + 65;
				switch (buf.card.inform.x400pCardChipType) {
				case X400PCARDCHIPTYPE_NONE:
				default:
					if ((tmpoid = snmp_duplicate_objid(zeroDotZero_oid, 2))) {
						SNMP_FREE(StorageTmp->x400pCardChipType);
						StorageTmp->x400pCardChipType = tmpoid;
						StorageTmp->x400pCardChipTypeLen = 2;
					}
					break;
				case X400PCARDCHIPTYPE_DS2152:
					if ((tmpoid = snmp_duplicate_objid(x400pChipTypeDS2152_oid, 12))) {
						SNMP_FREE(StorageTmp->x400pCardChipType);
						StorageTmp->x400pCardChipType = tmpoid;
						StorageTmp->x400pCardChipTypeLen = 12;
					}
					break;
				case X400PCARDCHIPTYPE_DS21352:
					if ((tmpoid = snmp_duplicate_objid(x400pChipTypeDS21352_oid, 12))) {
						SNMP_FREE(StorageTmp->x400pCardChipType);
						StorageTmp->x400pCardChipType = tmpoid;
						StorageTmp->x400pCardChipTypeLen = 12;
					}
					break;
				case X400PCARDCHIPTYPE_DS21552:
					if ((tmpoid = snmp_duplicate_objid(x400pChipTypeDS21552_oid, 12))) {
						SNMP_FREE(StorageTmp->x400pCardChipType);
						StorageTmp->x400pCardChipType = tmpoid;
						StorageTmp->x400pCardChipTypeLen = 12;
					}
					break;
				case X400PCARDCHIPTYPE_DS2154:
					if ((tmpoid = snmp_duplicate_objid(x400pChipTypeDS2154_oid, 12))) {
						SNMP_FREE(StorageTmp->x400pCardChipType);
						StorageTmp->x400pCardChipType = tmpoid;
						StorageTmp->x400pCardChipTypeLen = 12;
					}
					break;
				case X400PCARDCHIPTYPE_DS21354:
					if ((tmpoid = snmp_duplicate_objid(x400pChipTypeDS21354_oid, 12))) {
						SNMP_FREE(StorageTmp->x400pCardChipType);
						StorageTmp->x400pCardChipType = tmpoid;
						StorageTmp->x400pCardChipTypeLen = 12;
					}
					break;
				case X400PCARDCHIPTYPE_DS21554:
					if ((tmpoid = snmp_duplicate_objid(x400pChipTypeDS21554_oid, 12))) {
						SNMP_FREE(StorageTmp->x400pCardChipType);
						StorageTmp->x400pCardChipType = tmpoid;
						StorageTmp->x400pCardChipTypeLen = 12;
					}
					break;
				case X400PCARDCHIPTYPE_DS2155:
					if ((tmpoid = snmp_duplicate_objid(x400pChipTypeDS2155_oid, 12))) {
						SNMP_FREE(StorageTmp->x400pCardChipType);
						StorageTmp->x400pCardChipType = tmpoid;
						StorageTmp->x400pCardChipTypeLen = 12;
					}
					break;
				case X400PCARDCHIPTYPE_DS2156:
					if ((tmpoid = snmp_duplicate_objid(x400pChipTypeDS2156_oid, 12))) {
						SNMP_FREE(StorageTmp->x400pCardChipType);
						StorageTmp->x400pCardChipType = tmpoid;
						StorageTmp->x400pCardChipTypeLen = 12;
					}
					break;
				case X400PCARDCHIPTYPE_DS21455:
					if ((tmpoid = snmp_duplicate_objid(x400pChipTypeDS21455_oid, 12))) {
						SNMP_FREE(StorageTmp->x400pCardChipType);
						StorageTmp->x400pCardChipType = tmpoid;
						StorageTmp->x400pCardChipTypeLen = 12;
					}
					break;
				case X400PCARDCHIPTYPE_DS21458:
					if ((tmpoid = snmp_duplicate_objid(x400pChipTypeDS21458_oid, 12))) {
						SNMP_FREE(StorageTmp->x400pCardChipType);
						StorageTmp->x400pCardChipType = tmpoid;
						StorageTmp->x400pCardChipTypeLen = 12;
					}
					break;
				}
				StorageTmp->x400pCardChipRevision[0] = buf.card.inform.x400pCardChipRevision + 65;
				StorageTmp->x400pCardPciBus = buf.card.inform.x400pCardPciBus;
				StorageTmp->x400pCardPciSlot = buf.card.inform.x400pCardPciSlot;
				StorageTmp->x400pCardPciIrq = buf.card.inform.x400pCardPciIrq;
				if ((tmp = strndup(buf.card.inform.x400pCardName, 32))) {
					SNMP_FREE(StorageTmp->x400pCardName);
					StorageTmp->x400pCardName = (uint8_t *) tmp;
					StorageTmp->x400pCardNameLen = strnlen(buf.card.inform.x400pCardName, 32);
				}
				uinttobits(StorageTmp->x400pCardMode, buf.card.option.x400pCardMode, 1);
				StorageTmp->x400pCardSyncMaster = buf.card.option.x400pCardSyncMaster;
				StorageTmp->x400pCardSyncSource = buf.card.option.x400pCardSyncSource;
				StorageTmp->x400pCardAdministrativeState = buf.card.status.x400pCardAdministrativeState;
				StorageTmp->x400pCardOperationalState = buf.card.status.x400pCardOperationalState;
				StorageTmp->x400pCardUsageState = buf.card.status.x400pCardUsageState;
				uinttobits(StorageTmp->x400pCardAlarmStatus, buf.card.status.x400pCardAlarmStatus, 4);
				uinttobits(StorageTmp->x400pCardProceduralStatus, buf.card.status.x400pCardProceduralStatus, 4);
				uinttobits(StorageTmp->x400pCardAvailabilityStatus, buf.card.status.x400pCardAvailabilityStatus, 8);
				uinttobits(StorageTmp->x400pCardControlStatus, buf.card.status.x400pCardControlStatus, 3);
				StorageTmp->x400pCardUnknownStatus = buf.card.status.x400pCardUnknownStatus;
				StorageTmp->x400pCardStandbyStatus = buf.card.status.x400pCardStandbyStatus;
				StorageTmp->x400pCardSyncTransitions = buf.card.stats.x400pCardSyncTransitions;
			} else {
				/* no entry for this card index, create it */
				struct x400pCardTable_data *StorageNew = SNMP_MALLOC_STRUCT(x400pCardTable_data);

				StorageNew->x400pCardTable_request = sa_request;	/* mark as updated */
				StorageNew->x400pCardTable_refs = 1;	/* mark as used */
				/* copy driver name from driver entry */
				StorageNew->x400pDrivName = (uint8_t *) strndup((char *) driv->x400pDrivName, driv->x400pDrivNameLen);
				StorageNew->x400pDrivNameLen = driv->x400pDrivNameLen;
				StorageNew->x400pDrivName[StorageNew->x400pDrivNameLen] = '\0';
				StorageNew->x400pCardIndex = buf.card.config.x400pCardIndex;
				StorageNew->x400pCardSpanType = buf.card.config.x400pCardSpanType;
				StorageNew->x400pCardSyncGroup = buf.card.config.x400pCardSyncGroup;
				switch (buf.card.inform.x400pCardType) {
				case X400PCARDTYPE_NONE:
				default:
					StorageNew->x400pCardType = snmp_duplicate_objid(zeroDotZero_oid, 2);
					StorageNew->x400pCardTypeLen = 2;
					break;
				case X400PCARDTYPE_PLX9030:
					StorageNew->x400pCardType = snmp_duplicate_objid(x400pCardTypePLX9030_oid, 12);
					StorageNew->x400pCardTypeLen = 12;
					break;
				case X400PCARDTYPE_PLXDEVBRD:
					StorageNew->x400pCardType = snmp_duplicate_objid(x400pCardTypePLXDevelopmentBoard_oid, 12);
					StorageNew->x400pCardTypeLen = 12;
					break;
				case X400PCARDTYPE_X400P:
					StorageNew->x400pCardType = snmp_duplicate_objid(x400pCardTypeX400P_oid, 12);
					StorageNew->x400pCardTypeLen = 12;
					break;
				case X400PCARDTYPE_E400P:
					StorageNew->x400pCardType = snmp_duplicate_objid(x400pCardTypeE400P_oid, 13);
					StorageNew->x400pCardTypeLen = 13;
					break;
				case X400PCARDTYPE_T400P:
					StorageNew->x400pCardType = snmp_duplicate_objid(x400pCardTypeT400P_oid, 13);
					StorageNew->x400pCardTypeLen = 13;
					break;
				case X400PCARDTYPE_X400PSS7:
					StorageNew->x400pCardType = snmp_duplicate_objid(x400pCardTypeX400P_SS7_oid, 12);
					StorageNew->x400pCardTypeLen = 12;
					break;
				case X400PCARDTYPE_E400PSS7:
					StorageNew->x400pCardType = snmp_duplicate_objid(x400pCardTypeE400P_SS7_oid, 13);
					StorageNew->x400pCardTypeLen = 13;
					break;
				case X400PCARDTYPE_T400PSS7:
					StorageNew->x400pCardType = snmp_duplicate_objid(x400pCardTypeT400P_SS7_oid, 13);
					StorageNew->x400pCardTypeLen = 13;
					break;
				case X400PCARDTYPE_V400P:
					StorageNew->x400pCardType = snmp_duplicate_objid(x400pCardTypeV400P_oid, 12);
					StorageNew->x400pCardTypeLen = 12;
					break;
				case X400PCARDTYPE_V400PE:
					StorageNew->x400pCardType = snmp_duplicate_objid(x400pCardTypeV400PE_oid, 13);
					StorageNew->x400pCardTypeLen = 13;
					break;
				case X400PCARDTYPE_V400PT:
					StorageNew->x400pCardType = snmp_duplicate_objid(x400pCardTypeV400PT_oid, 13);
					StorageNew->x400pCardTypeLen = 13;
					break;
				case X400PCARDTYPE_V401PE:
					StorageNew->x400pCardType = snmp_duplicate_objid(x400pCardTypeV401PE_oid, 12);
					StorageNew->x400pCardTypeLen = 12;
					break;
				case X400PCARDTYPE_V401PT:
					StorageNew->x400pCardType = snmp_duplicate_objid(x400pCardTypeV401PT_oid, 12);
					StorageNew->x400pCardTypeLen = 12;
					break;
				}
				StorageNew->x400pCardIdentifier = buf.card.inform.x400pCardIdentifier;
				StorageNew->x400pCardRevision = (uint8_t *) strdup("A");
				StorageNew->x400pCardRevisionLen = 1;
				StorageNew->x400pCardRevision[0] = buf.card.inform.x400pCardRevision + 65;
				switch (buf.card.inform.x400pCardChipType) {
				case X400PCARDCHIPTYPE_NONE:
				default:
					StorageNew->x400pCardChipType = snmp_duplicate_objid(zeroDotZero_oid, 2);
					StorageNew->x400pCardChipTypeLen = 2;
					break;
				case X400PCARDCHIPTYPE_DS2152:
					StorageNew->x400pCardChipType = snmp_duplicate_objid(x400pChipTypeDS2152_oid, 12);
					StorageNew->x400pCardChipTypeLen = 12;
					break;
				case X400PCARDCHIPTYPE_DS21352:
					StorageNew->x400pCardChipType = snmp_duplicate_objid(x400pChipTypeDS21352_oid, 12);
					StorageNew->x400pCardChipTypeLen = 12;
					break;
				case X400PCARDCHIPTYPE_DS21552:
					StorageNew->x400pCardChipType = snmp_duplicate_objid(x400pChipTypeDS21552_oid, 12);
					StorageNew->x400pCardChipTypeLen = 12;
					break;
				case X400PCARDCHIPTYPE_DS2154:
					StorageNew->x400pCardChipType = snmp_duplicate_objid(x400pChipTypeDS2154_oid, 12);
					StorageNew->x400pCardChipTypeLen = 12;
					break;
				case X400PCARDCHIPTYPE_DS21354:
					StorageNew->x400pCardChipType = snmp_duplicate_objid(x400pChipTypeDS21354_oid, 12);
					StorageNew->x400pCardChipTypeLen = 12;
					break;
				case X400PCARDCHIPTYPE_DS21554:
					StorageNew->x400pCardChipType = snmp_duplicate_objid(x400pChipTypeDS21554_oid, 12);
					StorageNew->x400pCardChipTypeLen = 12;
					break;
				case X400PCARDCHIPTYPE_DS2155:
					StorageNew->x400pCardChipType = snmp_duplicate_objid(x400pChipTypeDS2155_oid, 12);
					StorageNew->x400pCardChipTypeLen = 12;
					break;
				case X400PCARDCHIPTYPE_DS2156:
					StorageNew->x400pCardChipType = snmp_duplicate_objid(x400pChipTypeDS2156_oid, 12);
					StorageNew->x400pCardChipTypeLen = 12;
					break;
				case X400PCARDCHIPTYPE_DS21455:
					StorageNew->x400pCardChipType = snmp_duplicate_objid(x400pChipTypeDS21455_oid, 12);
					StorageNew->x400pCardChipTypeLen = 12;
					break;
				case X400PCARDCHIPTYPE_DS21458:
					StorageNew->x400pCardChipType = snmp_duplicate_objid(x400pChipTypeDS21458_oid, 12);
					StorageNew->x400pCardChipTypeLen = 12;
					break;
				}
				StorageNew->x400pCardChipRevision = (uint8_t *) strdup("A");
				StorageNew->x400pCardChipRevisionLen = 1;
				StorageNew->x400pCardChipRevision[0] = buf.card.inform.x400pCardChipRevision + 65;
				StorageNew->x400pCardPciBus = buf.card.inform.x400pCardPciBus;
				StorageNew->x400pCardPciSlot = buf.card.inform.x400pCardPciSlot;
				StorageNew->x400pCardPciIrq = buf.card.inform.x400pCardPciIrq;
				memdup(&StorageNew->x400pCardName, (uint8_t *) buf.card.inform.x400pCardName, strnlen(buf.card.inform.x400pCardName, 32) + 1);
				StorageNew->x400pCardNameLen = strnlen(buf.card.inform.x400pCardName, 32);
				StorageNew->x400pCardName[StorageNew->x400pCardNameLen] = '\0';
				uinttobitsalloc(&StorageNew->x400pCardMode, buf.card.option.x400pCardMode, 1);
				StorageNew->x400pCardSyncMaster = buf.card.option.x400pCardSyncMaster;
				StorageNew->x400pCardSyncSource = buf.card.option.x400pCardSyncSource;
				StorageNew->x400pCardAdministrativeState = buf.card.status.x400pCardAdministrativeState;
				StorageNew->x400pCardOperationalState = buf.card.status.x400pCardOperationalState;
				StorageNew->x400pCardUsageState = buf.card.status.x400pCardUsageState;
				uinttobitsalloc(&StorageNew->x400pCardAlarmStatus, buf.card.status.x400pCardAlarmStatus, 4);
				uinttobitsalloc(&StorageNew->x400pCardProceduralStatus, buf.card.status.x400pCardProceduralStatus, 4);
				uinttobitsalloc(&StorageNew->x400pCardAvailabilityStatus, buf.card.status.x400pCardAvailabilityStatus, 8);
				// uinttobitsalloc(&StorageNew->x400pCardControlStatus,
				// buf.card.status.x400pCardControlStatusLen, 3);
				StorageNew->x400pCardUnknownStatus = buf.card.status.x400pCardUnknownStatus;
				StorageNew->x400pCardStandbyStatus = buf.card.status.x400pCardStandbyStatus;
				StorageNew->x400pCardSyncSpanId = (uint8_t *) strdup("");
				StorageNew->x400pCardSyncSpanIdLen = 0;
				StorageNew->x400pCardSyncTransitions = buf.card.stats.x400pCardSyncTransitions;
				x400pCardTable_add(StorageNew);
			}
		}
		free(cptr);
		close(fd);
	}
	/* delete unused entries */
	h_next = x400pCardTableStorage;
	while ((h = h_next)) {
		struct x400pCardTable_data *data = h->data;

		if (data->x400pCardTable_refs)
			data->x400pCardTable_refs = 0;
		else {
			header_complex_extract_entry(&x400pCardTableStorage, h);
			x400pCardTable_destroy(&data);
		}
	}
	/* done */
	return;
}

/**
 * @fn u_char *var_x400pCardTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in x400pCardTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_x400pMxMIB above.
 */
u_char *
var_x400pCardTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct x400pCardTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("x400pMxMIB", "var_x400pCardTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_x400pCardTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(x400pCardTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_x400pCardTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) X400PCARDTYPE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->x400pCardTypeLen * sizeof(oid);
		rval = (u_char *) StorageTmp->x400pCardType;
		break;
	case (u_char) X400PCARDIDENTIFIER:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pCardIdentifier);
		rval = (u_char *) &StorageTmp->x400pCardIdentifier;
		break;
	case (u_char) X400PCARDREVISION:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->x400pCardRevisionLen;
		rval = (u_char *) StorageTmp->x400pCardRevision;
		break;
	case (u_char) X400PCARDCHIPTYPE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->x400pCardChipTypeLen * sizeof(oid);
		rval = (u_char *) StorageTmp->x400pCardChipType;
		break;
	case (u_char) X400PCARDCHIPREVISION:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->x400pCardChipRevisionLen;
		rval = (u_char *) StorageTmp->x400pCardChipRevision;
		break;
	case (u_char) X400PCARDPCIBUS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pCardPciBus);
		rval = (u_char *) &StorageTmp->x400pCardPciBus;
		break;
	case (u_char) X400PCARDPCISLOT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pCardPciSlot);
		rval = (u_char *) &StorageTmp->x400pCardPciSlot;
		break;
	case (u_char) X400PCARDPCIIRQ:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pCardPciIrq);
		rval = (u_char *) &StorageTmp->x400pCardPciIrq;
		break;
	case (u_char) X400PCARDSPANTYPE:	/* Create */
		*write_method = write_x400pCardSpanType;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pCardSpanType);
		rval = (u_char *) &StorageTmp->x400pCardSpanType;
		break;
	case (u_char) X400PCARDMODE:	/* Create */
		*write_method = write_x400pCardMode;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->x400pCardModeLen;
		rval = (u_char *) StorageTmp->x400pCardMode;
		break;
	case (u_char) X400PCARDSYNCMASTER:	/* Create */
		*write_method = write_x400pCardSyncMaster;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pCardSyncMaster);
		rval = (u_char *) &StorageTmp->x400pCardSyncMaster;
		break;
	case (u_char) X400PCARDSYNCSOURCE:	/* Create */
		*write_method = write_x400pCardSyncSource;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pCardSyncSource);
		rval = (u_char *) &StorageTmp->x400pCardSyncSource;
		break;
	case (u_char) X400PCARDSYNCGROUP:	/* Create */
		*write_method = write_x400pCardSyncGroup;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pCardSyncGroup);
		rval = (u_char *) &StorageTmp->x400pCardSyncGroup;
		break;
	case (u_char) X400PCARDADMINISTRATIVESTATE:	/* Create */
		*write_method = write_x400pCardAdministrativeState;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pCardAdministrativeState);
		rval = (u_char *) &StorageTmp->x400pCardAdministrativeState;
		break;
	case (u_char) X400PCARDOPERATIONALSTATE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pCardOperationalState);
		rval = (u_char *) &StorageTmp->x400pCardOperationalState;
		break;
	case (u_char) X400PCARDUSAGESTATE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pCardUsageState);
		rval = (u_char *) &StorageTmp->x400pCardUsageState;
		break;
	case (u_char) X400PCARDALARMSTATUS:	/* Create */
		*write_method = write_x400pCardAlarmStatus;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->x400pCardAlarmStatusLen;
		rval = (u_char *) StorageTmp->x400pCardAlarmStatus;
		break;
	case (u_char) X400PCARDPROCEDURALSTATUS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->x400pCardProceduralStatusLen;
		rval = (u_char *) StorageTmp->x400pCardProceduralStatus;
		break;
	case (u_char) X400PCARDAVAILABILITYSTATUS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->x400pCardAvailabilityStatusLen;
		rval = (u_char *) StorageTmp->x400pCardAvailabilityStatus;
		break;
	case (u_char) X400PCARDCONTROLSTATUS:	/* Create */
		*write_method = write_x400pCardControlStatus;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->x400pCardControlStatusLen;
		rval = (u_char *) StorageTmp->x400pCardControlStatus;
		break;
	case (u_char) X400PCARDUNKNOWNSTATUS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pCardUnknownStatus);
		rval = (u_char *) &StorageTmp->x400pCardUnknownStatus;
		break;
	case (u_char) X400PCARDSTANDBYSTATUS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pCardStandbyStatus);
		rval = (u_char *) &StorageTmp->x400pCardStandbyStatus;
		break;
	case (u_char) X400PCARDSYNCSPANID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->x400pCardSyncSpanIdLen;
		rval = (u_char *) StorageTmp->x400pCardSyncSpanId;
		break;
	case (u_char) X400PCARDSYNCTRANSITIONS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pCardSyncTransitions);
		rval = (u_char *) &StorageTmp->x400pCardSyncTransitions;
		break;
	case (u_char) X400PCARDNAME:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->x400pCardNameLen;
		rval = (u_char *) StorageTmp->x400pCardName;
		break;
	case (u_char) X400PCARDSTATUS:	/* Create */
		*write_method = write_x400pCardStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pCardStatus);
		rval = (u_char *) &StorageTmp->x400pCardStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_x400pSpanTable_row(struct x400pSpanTable_data *StorageTmp, struct x400pSpanTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_x400pSpanTable_row(struct x400pSpanTable_data *StorageTmp, struct x400pSpanTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_x400pSpanTable_row(struct x400pSpanTable_data *StorageTmp, struct x400pSpanTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_x400pSpanTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_x400pSpanTable_row(struct x400pSpanTable_data *StorageTmp, struct x400pSpanTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	x400pSpanTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_x400pSpanTable_row(struct x400pSpanTable_data *StorageTmp, struct x400pSpanTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_x400pSpanTable_row(struct x400pSpanTable_data *StorageTmp, struct x400pSpanTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_x400pSpanTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_x400pSpanTable_row(struct x400pSpanTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the x400pSpanTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct x400pSpanTable_data *
refresh_x400pSpanTable_row(struct x400pSpanTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->x400pSpanTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->x400pSpanTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_x400pSpanTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the x400pSpanTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_x400pSpanTable(int force)
{
	struct header_complex_index *h, *h_next;

	void uinttobits(uint8_t *ostr, uint val, int max) {
		int i;

		for (i = 0; i <= max; i++)
			ostr[i >> 3] = 0;
		for (i = 0; i <= max; i++)
			if (val & (1 << i))
				ostr[i >> 3] = (7 - (i % 8));
	}
	void uinttobitsalloc(uint8_t **optr, uint val, int max) {
		if ((*optr = calloc((max >> 3) + 1, 1)))
			uinttobits(*optr, val, max);
	}

	refresh_x400pCardTable(force);
	if (!force && x400pSpanTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	x400pSpanTable_refresh = 0;
	for (h = x400pDrivTableStorage; h; h = h->next) {
		/* for each entry in the driver table */
		int fd, count, count2, i;
		uint *val;
		struct x400pDrivTable_data *driv = h->data;
		struct strioctl ioc;
		struct x400p_config config, *cptr = &config;

		if (driv->x400pDrivRowStatus != RS_ACTIVE)
			continue;
		if ((fd = open(driv->x400pDrivTable_devname, O_RDWR)) < 0)
			continue;
		cptr = &config;
		cptr->type = MX_OBJ_TYPE_SPAN;
		cptr->id = 0;
		cptr->cmd = MX_GET;
		ioc.ic_cmd = MX_IOCLCONFIG;
		ioc.ic_timout = 0;
		ioc.ic_len = sizeof(*cptr);
		ioc.ic_dp = (char *) cptr;
	      retry:
		if ((count = ioctl(fd, I_STR, &ioc)) < 0) {
			close(fd);
			continue;
		}
		if (!(cptr = malloc(sizeof(*cptr) + count * sizeof(uint)))) {
			close(fd);
			continue;
		}
		cptr->type = MX_OBJ_TYPE_SPAN;
		cptr->id = 0;
		cptr->cmd = MX_GET;
		ioc.ic_cmd = MX_IOCLCONFIG;
		ioc.ic_timout = 0;
		ioc.ic_len = sizeof(*cptr) + count * sizeof(uint);
		ioc.ic_dp = (char *) cptr;
		if ((count2 = ioctl(fd, I_STR, &ioc)) < 0) {
			free(cptr);
			close(fd);
			continue;
		}
		if (count2 > count) {
			count = count2;
			goto retry;
		}
		for (i = 0, val = (typeof(val)) (cptr + 1); i < count2; i++, val++) {
			struct header_complex_index *hciptr;
			struct {
				struct x400p_attr attr;
				struct x400p_attr_span span;
			} buf;

			buf.attr.type = MX_OBJ_TYPE_SPAN;
			buf.attr.id = *val;
			buf.attr.cmd = MX_GET;
			ioc.ic_cmd = MX_IOCGATTR;
			ioc.ic_timout = 0;
			ioc.ic_len = sizeof(buf);
			ioc.ic_dp = (char *) &buf;
			if (ioctl(fd, I_STR, &buf) < 0)
				continue;
			for (hciptr = x400pSpanTableStorage; hciptr; hciptr = hciptr->next) {
				struct x400pSpanTable_data *data = hciptr->data;

				if (data->x400pDrivNameLen != driv->x400pDrivNameLen || strncmp((char *) data->x400pDrivName, (char *) driv->x400pDrivName, driv->x400pDrivNameLen))
					continue;
				if (data->x400pSpanIndex != *val)
					continue;
				break;
			}
			if (hciptr) {
				/* already have an entry for this card index, simply update it */
				struct x400pSpanTable_data *StorageTmp = hciptr->data;
				char *tmp;
				oid *tmpoid;

				StorageTmp->x400pSpanTable_request = sa_request;	/* mark as updated */

				StorageTmp->x400pSpanTable_refs = 1;	/* mark as used */
				/* copy driver name from driver entry */
				if ((tmp = strndup((char *) driv->x400pDrivName, driv->x400pDrivNameLen))) {
					SNMP_FREE(StorageTmp->x400pDrivName);
					StorageTmp->x400pDrivName = (uint8_t *) tmp;
					StorageTmp->x400pDrivNameLen = driv->x400pDrivNameLen;
					StorageTmp->x400pDrivName[StorageTmp->x400pDrivNameLen] = '\0';
				}
				StorageTmp->x400pCardIndex = buf.span.config.x400pCardIndex;
				StorageTmp->x400pSpanIndex = buf.span.config.x400pSpanIndex;
				if ((tmp = strndup(buf.span.config.x400pSpanName, 32))) {
					SNMP_FREE(StorageTmp->x400pSpanName);
					StorageTmp->x400pSpanName = (uint8_t *) tmp;
					StorageTmp->x400pSpanNameLen = strnlen(buf.span.config.x400pSpanName, 32);
				}
				if ((tmpoid = snmp_duplicate_objid(zeroDotZero_oid, 2))) {
					SNMP_FREE(StorageTmp->x400pSpanDevice);
					StorageTmp->x400pSpanDevice = tmpoid;
					StorageTmp->x400pSpanDeviceLen = 2;
				}
				/* x400pSpanEquipmentId ???? XXX */
				StorageTmp->x400pSpanType = buf.span.config.x400pSpanType;
				StorageTmp->x400pSpanNumber = 0;
				switch (StorageTmp->x400pSpanType) {
				case MXSPANTYPE_NONE:
				default:
					StorageTmp->x400pSpanRate = 0;
					break;
				case MXSPANTYPE_T1:
					StorageTmp->x400pSpanRate = 1544;
					break;
				case MXSPANTYPE_E1:
					StorageTmp->x400pSpanRate = 2048;
					break;
				case MXSPANTYPE_J1:
					StorageTmp->x400pSpanRate = 1544;
					break;
				case MXSPANTYPE_E2:
					StorageTmp->x400pSpanRate = 2048;
					break;
				case MXSPANTYPE_E3:
					StorageTmp->x400pSpanRate = 2048;
					break;
				case MXSPANTYPE_T3:
					StorageTmp->x400pSpanRate = 1544;
					break;
				}
				StorageTmp->x400pSpanCrc = buf.span.config.x400pSpanCrc;
				StorageTmp->x400pSpanCoding = buf.span.config.x400pSpanCoding;
				StorageTmp->x400pSpanFraming = buf.span.config.x400pSpanFraming;
				uinttobits(StorageTmp->x400pSpanDataLink, buf.span.config.x400pSpanDataLink, 2);
				if ((tmpoid = snmp_duplicate_objid(zeroDotZero_oid, 2))) {
					SNMP_FREE(StorageTmp->x400pSpanAlarmSeverityMapProfile);
					StorageTmp->x400pSpanAlarmSeverityMapProfile = tmpoid;
					StorageTmp->x400pSpanAlarmSeverityMapProfileLen = 2;
				}
				StorageTmp->x400pSpanPriority = buf.span.config.x400pSpanPriority;
				StorageTmp->x400pSpanPrimary = buf.span.config.x400pSpanPrimary;
				uinttobits(StorageTmp->x400pSpanMode, buf.span.option.x400pSpanMode, 3);
				StorageTmp->x400pSpanClocking = buf.span.option.x400pSpanClocking;
				StorageTmp->x400pSpanLineImpedance = buf.span.option.x400pSpanLineImpedance;
				StorageTmp->x400pSpanLineMode = buf.span.option.x400pSpanLineMode;
				StorageTmp->x400pSpanLineLength = buf.span.option.x400pSpanLineLength;
				StorageTmp->x400pSpanLineAttenuation = buf.span.option.x400pSpanLineAttenuation;
				StorageTmp->x400pSpanLineGain = buf.span.option.x400pSpanLineGain;
				StorageTmp->x400pSpanLineDelay = buf.span.option.x400pSpanLineDelay;
				StorageTmp->x400pSpanTxLevel = buf.span.option.x400pSpanTxLevel;
				StorageTmp->x400pSpanRxLevel = buf.span.option.x400pSpanRxLevel;
				StorageTmp->x400pSpanAlarmSettleTime = buf.span.option.x400pSpanAlarmSettleTime;
				StorageTmp->x400pSpanLineCodeTime = buf.span.option.x400pSpanLineCodeTime;
				StorageTmp->x400pSpanLineCode = buf.span.option.x400pSpanLineCode;
				StorageTmp->x400pSpanReceiveThreshold = buf.span.option.x400pSpanReceiveThreshold;
				if ((tmpoid = snmp_duplicate_objid(zeroDotZero_oid, 2))) {
					SNMP_FREE(StorageTmp->x400pSpanSap);
					StorageTmp->x400pSpanSap = tmpoid;
					StorageTmp->x400pSpanSapLen = 2;
				}
				StorageTmp->x400pSpanAdministrativeState = buf.span.status.x400pSpanAdministrativeState;
				StorageTmp->x400pSpanOperationalState = buf.span.status.x400pSpanOperationalState;
				StorageTmp->x400pSpanUsageState = buf.span.status.x400pSpanUsageState;
				uinttobits(StorageTmp->x400pSpanAlarmStatus, buf.span.status.x400pSpanAlarmStatus, 4);
				uinttobits(StorageTmp->x400pSpanProceduralStatus, buf.span.status.x400pSpanProceduralStatus, 4);
				uinttobits(StorageTmp->x400pSpanAvailabilityStatus, buf.span.status.x400pSpanAvailabilityStatus, 8);
				uinttobits(StorageTmp->x400pSpanControlStatus, buf.span.status.x400pSpanControlStatus, 3);
				StorageTmp->x400pSpanStandbyStatus = buf.span.status.x400pSpanStandbyStatus;
				StorageTmp->x400pSpanUnknownStatus = buf.span.status.x400pSpanUnknownStatus;
				uinttobits(StorageTmp->x400pSpanLoopbackStatus, buf.span.status.x400pSpanLoopbackStatus, 5);
				uinttobits(StorageTmp->x400pSpanLineStatus, buf.span.status.x400pSpanLineStatus, 16);
				uinttobits(StorageTmp->x400pSpanAlarms, buf.span.status.x400pSpanAlarms, 3);
				uinttobits(StorageTmp->x400pSpanEvents, buf.span.status.x400pSpanEvents, 13);
			} else {
				/* no entry for this span index, create it */
				struct x400pSpanTable_data *StorageNew = SNMP_MALLOC_STRUCT(x400pSpanTable_data);

				StorageNew->x400pSpanTable_request = sa_request;	/* mark as updated */
				StorageNew->x400pSpanTable_refs = 1;	/* mark as used */
				/* copy driver name from driver entry */
				StorageNew->x400pDrivName = (uint8_t *) strndup((char *) driv->x400pDrivName, driv->x400pDrivNameLen);
				StorageNew->x400pDrivNameLen = driv->x400pDrivNameLen;
				StorageNew->x400pDrivName[StorageNew->x400pDrivNameLen] = '\0';
				StorageNew->x400pCardIndex = buf.span.config.x400pCardIndex;
				StorageNew->x400pSpanIndex = buf.span.config.x400pSpanIndex;
				StorageNew->x400pSpanName = (uint8_t *) strndup(buf.span.config.x400pSpanName, 32);
				StorageNew->x400pSpanNameLen = strnlen(buf.span.config.x400pSpanName, 32);
				StorageNew->x400pSpanDevice = snmp_duplicate_objid(zeroDotZero_oid, 2);
				StorageNew->x400pSpanDeviceLen = 2;
				/* x400pSpanEquipmentId ???? XXX */
				StorageNew->x400pSpanType = buf.span.config.x400pSpanType;
				StorageNew->x400pSpanNumber = 0;
				switch (StorageNew->x400pSpanType) {
				case MXSPANTYPE_NONE:
				default:
					StorageNew->x400pSpanRate = 0;
					break;
				case MXSPANTYPE_T1:
					StorageNew->x400pSpanRate = 1544;
					break;
				case MXSPANTYPE_E1:
					StorageNew->x400pSpanRate = 2048;
					break;
				case MXSPANTYPE_J1:
					StorageNew->x400pSpanRate = 1544;
					break;
				case MXSPANTYPE_E2:
					StorageNew->x400pSpanRate = 2048;
					break;
				case MXSPANTYPE_E3:
					StorageNew->x400pSpanRate = 2048;
					break;
				case MXSPANTYPE_T3:
					StorageNew->x400pSpanRate = 1544;
					break;
				}
				StorageNew->x400pSpanCrc = buf.span.config.x400pSpanCrc;
				StorageNew->x400pSpanCoding = buf.span.config.x400pSpanCoding;
				StorageNew->x400pSpanFraming = buf.span.config.x400pSpanFraming;
				uinttobitsalloc(&StorageNew->x400pSpanDataLink, buf.span.config.x400pSpanDataLink, 2);
				StorageNew->x400pSpanAlarmSeverityMapProfile = snmp_duplicate_objid(zeroDotZero_oid, 2);
				StorageNew->x400pSpanAlarmSeverityMapProfileLen = 2;
				StorageNew->x400pSpanPriority = buf.span.config.x400pSpanPriority;
				StorageNew->x400pSpanPrimary = buf.span.config.x400pSpanPrimary;
				uinttobitsalloc(&StorageNew->x400pSpanMode, buf.span.option.x400pSpanMode, 3);
				StorageNew->x400pSpanClocking = buf.span.option.x400pSpanClocking;
				StorageNew->x400pSpanLineImpedance = buf.span.option.x400pSpanLineImpedance;
				StorageNew->x400pSpanLineMode = buf.span.option.x400pSpanLineMode;
				StorageNew->x400pSpanLineLength = buf.span.option.x400pSpanLineLength;
				StorageNew->x400pSpanLineAttenuation = buf.span.option.x400pSpanLineAttenuation;
				StorageNew->x400pSpanLineGain = buf.span.option.x400pSpanLineGain;
				StorageNew->x400pSpanLineDelay = buf.span.option.x400pSpanLineDelay;
				StorageNew->x400pSpanTxLevel = buf.span.option.x400pSpanTxLevel;
				StorageNew->x400pSpanRxLevel = buf.span.option.x400pSpanRxLevel;
				StorageNew->x400pSpanAlarmSettleTime = buf.span.option.x400pSpanAlarmSettleTime;
				StorageNew->x400pSpanLineCodeTime = buf.span.option.x400pSpanLineCodeTime;
				StorageNew->x400pSpanLineCode = buf.span.option.x400pSpanLineCode;
				StorageNew->x400pSpanReceiveThreshold = buf.span.option.x400pSpanReceiveThreshold;
				StorageNew->x400pSpanSap = snmp_duplicate_objid(zeroDotZero_oid, 2);
				StorageNew->x400pSpanSapLen = 2;
				StorageNew->x400pSpanAdministrativeState = buf.span.status.x400pSpanAdministrativeState;
				StorageNew->x400pSpanOperationalState = buf.span.status.x400pSpanOperationalState;
				StorageNew->x400pSpanUsageState = buf.span.status.x400pSpanUsageState;
				uinttobitsalloc(&StorageNew->x400pSpanAlarmStatus, buf.span.status.x400pSpanAlarmStatus, 4);
				uinttobitsalloc(&StorageNew->x400pSpanProceduralStatus, buf.span.status.x400pSpanProceduralStatus, 4);
				uinttobitsalloc(&StorageNew->x400pSpanAvailabilityStatus, buf.span.status.x400pSpanAvailabilityStatus, 8);
				uinttobitsalloc(&StorageNew->x400pSpanControlStatus, buf.span.status.x400pSpanControlStatus, 3);
				StorageNew->x400pSpanStandbyStatus = buf.span.status.x400pSpanStandbyStatus;
				StorageNew->x400pSpanUnknownStatus = buf.span.status.x400pSpanUnknownStatus;
				uinttobitsalloc(&StorageNew->x400pSpanLoopbackStatus, buf.span.status.x400pSpanLoopbackStatus, 5);
				uinttobitsalloc(&StorageNew->x400pSpanLineStatus, buf.span.status.x400pSpanLineStatus, 16);
				uinttobitsalloc(&StorageNew->x400pSpanAlarms, buf.span.status.x400pSpanAlarms, 3);
				uinttobitsalloc(&StorageNew->x400pSpanEvents, buf.span.status.x400pSpanEvents, 13);
				x400pSpanTable_add(StorageNew);
			}
		}
		free(cptr);
		close(fd);
	}
	/* delete unused entries */
	h_next = x400pSpanTableStorage;
	while ((h = h_next)) {
		struct x400pSpanTable_data *data = h->data;

		if (data->x400pSpanTable_refs)
			data->x400pSpanTable_refs = 0;
		else {
			header_complex_extract_entry(&x400pSpanTableStorage, h);
			x400pSpanTable_destroy(&data);
		}
	}
	/* done */
	return;
}

/**
 * @fn u_char *var_x400pSpanTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in x400pSpanTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_x400pMxMIB above.
 */
u_char *
var_x400pSpanTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct x400pSpanTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("x400pMxMIB", "var_x400pSpanTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_x400pSpanTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(x400pSpanTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_x400pSpanTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) X400PSPANNAME:	/* Create */
		*write_method = write_x400pSpanName;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->x400pSpanNameLen;
		rval = (u_char *) StorageTmp->x400pSpanName;
		break;
	case (u_char) X400PSPANDEVICE:	/* Create */
		*write_method = write_x400pSpanDevice;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->x400pSpanDeviceLen * sizeof(oid);
		rval = (u_char *) StorageTmp->x400pSpanDevice;
		break;
	case (u_char) X400PSPANEQUIPMENTID:	/* Create */
		*write_method = write_x400pSpanEquipmentId;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->x400pSpanEquipmentIdLen;
		rval = (u_char *) StorageTmp->x400pSpanEquipmentId;
		break;
	case (u_char) X400PSPANTYPE:	/* Create */
		*write_method = write_x400pSpanType;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pSpanType);
		rval = (u_char *) &StorageTmp->x400pSpanType;
		break;
	case (u_char) X400PSPANNUMBER:	/* Create */
		*write_method = write_x400pSpanNumber;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pSpanNumber);
		rval = (u_char *) &StorageTmp->x400pSpanNumber;
		break;
	case (u_char) X400PSPANRATE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pSpanRate);
		rval = (u_char *) &StorageTmp->x400pSpanRate;
		break;
	case (u_char) X400PSPANMODE:	/* Create */
		*write_method = write_x400pSpanMode;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->x400pSpanModeLen;
		rval = (u_char *) StorageTmp->x400pSpanMode;
		break;
	case (u_char) X400PSPANCRC:	/* Create */
		*write_method = write_x400pSpanCrc;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pSpanCrc);
		rval = (u_char *) &StorageTmp->x400pSpanCrc;
		break;
	case (u_char) X400PSPANCLOCKING:	/* Create */
		*write_method = write_x400pSpanClocking;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pSpanClocking);
		rval = (u_char *) &StorageTmp->x400pSpanClocking;
		break;
	case (u_char) X400PSPANPRIORITY:	/* Create */
		*write_method = write_x400pSpanPriority;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pSpanPriority);
		rval = (u_char *) &StorageTmp->x400pSpanPriority;
		break;
	case (u_char) X400PSPANCODING:	/* Create */
		*write_method = write_x400pSpanCoding;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pSpanCoding);
		rval = (u_char *) &StorageTmp->x400pSpanCoding;
		break;
	case (u_char) X400PSPANFRAMING:	/* Create */
		*write_method = write_x400pSpanFraming;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pSpanFraming);
		rval = (u_char *) &StorageTmp->x400pSpanFraming;
		break;
	case (u_char) X400PSPANLINEIMPEDANCE:	/* Create */
		*write_method = write_x400pSpanLineImpedance;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pSpanLineImpedance);
		rval = (u_char *) &StorageTmp->x400pSpanLineImpedance;
		break;
	case (u_char) X400PSPANLINEMODE:	/* Create */
		*write_method = write_x400pSpanLineMode;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pSpanLineMode);
		rval = (u_char *) &StorageTmp->x400pSpanLineMode;
		break;
	case (u_char) X400PSPANLINELENGTH:	/* Create */
		*write_method = write_x400pSpanLineLength;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pSpanLineLength);
		rval = (u_char *) &StorageTmp->x400pSpanLineLength;
		break;
	case (u_char) X400PSPANLINEATTENUATION:	/* Create */
		*write_method = write_x400pSpanLineAttenuation;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pSpanLineAttenuation);
		rval = (u_char *) &StorageTmp->x400pSpanLineAttenuation;
		break;
	case (u_char) X400PSPANLINEGAIN:	/* Create */
		*write_method = write_x400pSpanLineGain;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pSpanLineGain);
		rval = (u_char *) &StorageTmp->x400pSpanLineGain;
		break;
	case (u_char) X400PSPANLINEDELAY:	/* Create */
		*write_method = write_x400pSpanLineDelay;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pSpanLineDelay);
		rval = (u_char *) &StorageTmp->x400pSpanLineDelay;
		break;
	case (u_char) X400PSPANTXLEVEL:	/* Create */
		*write_method = write_x400pSpanTxLevel;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pSpanTxLevel);
		rval = (u_char *) &StorageTmp->x400pSpanTxLevel;
		break;
	case (u_char) X400PSPANRXLEVEL:	/* Create */
		*write_method = write_x400pSpanRxLevel;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pSpanRxLevel);
		rval = (u_char *) &StorageTmp->x400pSpanRxLevel;
		break;
	case (u_char) X400PSPANALARMSETTLETIME:	/* Create */
		*write_method = write_x400pSpanAlarmSettleTime;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pSpanAlarmSettleTime);
		rval = (u_char *) &StorageTmp->x400pSpanAlarmSettleTime;
		break;
	case (u_char) X400PSPANLINECODETIME:	/* Create */
		*write_method = write_x400pSpanLineCodeTime;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pSpanLineCodeTime);
		rval = (u_char *) &StorageTmp->x400pSpanLineCodeTime;
		break;
	case (u_char) X400PSPANPRIMARY:	/* Create */
		*write_method = write_x400pSpanPrimary;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pSpanPrimary);
		rval = (u_char *) &StorageTmp->x400pSpanPrimary;
		break;
	case (u_char) X400PSPANDATALINK:	/* Create */
		*write_method = write_x400pSpanDataLink;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->x400pSpanDataLinkLen;
		rval = (u_char *) StorageTmp->x400pSpanDataLink;
		break;
	case (u_char) X400PSPANLINECODE:	/* Create */
		*write_method = write_x400pSpanLineCode;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pSpanLineCode);
		rval = (u_char *) &StorageTmp->x400pSpanLineCode;
		break;
	case (u_char) X400PSPANALARMSEVERITYMAPPROFILE:	/* Create */
		*write_method = write_x400pSpanAlarmSeverityMapProfile;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->x400pSpanAlarmSeverityMapProfileLen * sizeof(oid);
		rval = (u_char *) StorageTmp->x400pSpanAlarmSeverityMapProfile;
		break;
	case (u_char) X400PSPANADMINISTRATIVESTATE:	/* Create */
		*write_method = write_x400pSpanAdministrativeState;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pSpanAdministrativeState);
		rval = (u_char *) &StorageTmp->x400pSpanAdministrativeState;
		break;
	case (u_char) X400PSPANOPERATIONALSTATE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pSpanOperationalState);
		rval = (u_char *) &StorageTmp->x400pSpanOperationalState;
		break;
	case (u_char) X400PSPANUSAGESTATE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pSpanUsageState);
		rval = (u_char *) &StorageTmp->x400pSpanUsageState;
		break;
	case (u_char) X400PSPANALARMSTATUS:	/* Create */
		*write_method = write_x400pSpanAlarmStatus;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->x400pSpanAlarmStatusLen;
		rval = (u_char *) StorageTmp->x400pSpanAlarmStatus;
		break;
	case (u_char) X400PSPANPROCEDURALSTATUS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->x400pSpanProceduralStatusLen;
		rval = (u_char *) StorageTmp->x400pSpanProceduralStatus;
		break;
	case (u_char) X400PSPANAVAILABILITYSTATUS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->x400pSpanAvailabilityStatusLen;
		rval = (u_char *) StorageTmp->x400pSpanAvailabilityStatus;
		break;
	case (u_char) X400PSPANCONTROLSTATUS:	/* Create */
		*write_method = write_x400pSpanControlStatus;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->x400pSpanControlStatusLen;
		rval = (u_char *) StorageTmp->x400pSpanControlStatus;
		break;
	case (u_char) X400PSPANSTANDBYSTATUS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pSpanStandbyStatus);
		rval = (u_char *) &StorageTmp->x400pSpanStandbyStatus;
		break;
	case (u_char) X400PSPANUNKNOWNSTATUS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pSpanUnknownStatus);
		rval = (u_char *) &StorageTmp->x400pSpanUnknownStatus;
		break;
	case (u_char) X400PSPANSAP:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->x400pSpanSapLen * sizeof(oid);
		rval = (u_char *) StorageTmp->x400pSpanSap;
		break;
	case (u_char) X400PSPANLOOPBACKSTATUS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->x400pSpanLoopbackStatusLen;
		rval = (u_char *) StorageTmp->x400pSpanLoopbackStatus;
		break;
	case (u_char) X400PSPANLINESTATUS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->x400pSpanLineStatusLen;
		rval = (u_char *) StorageTmp->x400pSpanLineStatus;
		break;
	case (u_char) X400PSPANALARMS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->x400pSpanAlarmsLen;
		rval = (u_char *) StorageTmp->x400pSpanAlarms;
		break;
	case (u_char) X400PSPANEVENTS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->x400pSpanEventsLen;
		rval = (u_char *) StorageTmp->x400pSpanEvents;
		break;
	case (u_char) X400PSPANRECEIVELEVEL:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pSpanReceiveLevel);
		rval = (u_char *) &StorageTmp->x400pSpanReceiveLevel;
		break;
	case (u_char) X400PSPANRECEIVETHRESHOLD:	/* Create */
		*write_method = write_x400pSpanReceiveThreshold;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pSpanReceiveThreshold);
		rval = (u_char *) &StorageTmp->x400pSpanReceiveThreshold;
		break;
	case (u_char) X400PSPANROWSTATUS:	/* Create */
		*write_method = write_x400pSpanRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pSpanRowStatus);
		rval = (u_char *) &StorageTmp->x400pSpanRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_x400pBertTable_row(struct x400pBertTable_data *StorageTmp, struct x400pBertTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_x400pBertTable_row(struct x400pBertTable_data *StorageTmp, struct x400pBertTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_x400pBertTable_row(struct x400pBertTable_data *StorageTmp, struct x400pBertTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_x400pBertTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_x400pBertTable_row(struct x400pBertTable_data *StorageTmp, struct x400pBertTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	x400pBertTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_x400pBertTable_row(struct x400pBertTable_data *StorageTmp, struct x400pBertTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_x400pBertTable_row(struct x400pBertTable_data *StorageTmp, struct x400pBertTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_x400pBertTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_x400pBertTable_row(struct x400pBertTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the x400pBertTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct x400pBertTable_data *
refresh_x400pBertTable_row(struct x400pBertTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->x400pBertTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->x400pBertTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_x400pBertTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the x400pBertTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_x400pBertTable(int force)
{
	refresh_x400pSpanTable(force);
	if (!force && x400pBertTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	x400pBertTable_refresh = 0;
}

/**
 * @fn u_char *var_x400pBertTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in x400pBertTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_x400pMxMIB above.
 */
u_char *
var_x400pBertTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct x400pBertTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("x400pMxMIB", "var_x400pBertTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_x400pBertTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(x400pBertTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_x400pBertTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) X400PBERTMODE:	/* ReadWrite */
		*write_method = write_x400pBertMode;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pBertMode);
		rval = (u_char *) &StorageTmp->x400pBertMode;
		break;
	case (u_char) X400PBERTSELECT:	/* ReadWrite */
		*write_method = write_x400pBertSelect;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pBertSelect);
		rval = (u_char *) &StorageTmp->x400pBertSelect;
		break;
	case (u_char) X400PBERTPATTERN:	/* ReadWrite */
		*write_method = write_x400pBertPattern;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->x400pBertPatternLen;
		rval = (u_char *) StorageTmp->x400pBertPattern;
		break;
	case (u_char) X400PBERTOPERATIONALSTATE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pBertOperationalState);
		rval = (u_char *) &StorageTmp->x400pBertOperationalState;
		break;
	case (u_char) X400PBERTPROCEDURALSTATUS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->x400pBertProceduralStatusLen;
		rval = (u_char *) StorageTmp->x400pBertProceduralStatus;
		break;
	case (u_char) X400PBERTBITCOUNT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pBertBitCount);
		rval = (u_char *) &StorageTmp->x400pBertBitCount;
		break;
	case (u_char) X400PBERTERRORCOUNT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pBertErrorCount);
		rval = (u_char *) &StorageTmp->x400pBertErrorCount;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_x400pChanTable_row(struct x400pChanTable_data *StorageTmp, struct x400pChanTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_x400pChanTable_row(struct x400pChanTable_data *StorageTmp, struct x400pChanTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_x400pChanTable_row(struct x400pChanTable_data *StorageTmp, struct x400pChanTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_x400pChanTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_x400pChanTable_row(struct x400pChanTable_data *StorageTmp, struct x400pChanTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	x400pChanTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_x400pChanTable_row(struct x400pChanTable_data *StorageTmp, struct x400pChanTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_x400pChanTable_row(struct x400pChanTable_data *StorageTmp, struct x400pChanTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_x400pChanTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_x400pChanTable_row(struct x400pChanTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the x400pChanTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct x400pChanTable_data *
refresh_x400pChanTable_row(struct x400pChanTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->x400pChanTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->x400pChanTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_x400pChanTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the x400pChanTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_x400pChanTable(int force)
{
	refresh_x400pSpanTable(force);
	if (!force && x400pChanTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	x400pChanTable_refresh = 0;
}

/**
 * @fn u_char *var_x400pChanTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in x400pChanTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_x400pMxMIB above.
 */
u_char *
var_x400pChanTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct x400pChanTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("x400pMxMIB", "var_x400pChanTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_x400pChanTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(x400pChanTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_x400pChanTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) X400PCHANTYPE:	/* ReadWrite */
		*write_method = write_x400pChanType;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pChanType);
		rval = (u_char *) &StorageTmp->x400pChanType;
		break;
	case (u_char) X400PCHANFORMAT:	/* ReadWrite */
		*write_method = write_x400pChanFormat;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pChanFormat);
		rval = (u_char *) &StorageTmp->x400pChanFormat;
		break;
	case (u_char) X400PCHANRATE:	/* ReadWrite */
		*write_method = write_x400pChanRate;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pChanRate);
		rval = (u_char *) &StorageTmp->x400pChanRate;
		break;
	case (u_char) X400PCHANMODE:	/* ReadWrite */
		*write_method = write_x400pChanMode;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->x400pChanModeLen;
		rval = (u_char *) StorageTmp->x400pChanMode;
		break;
	case (u_char) X400PCHANSAP:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->x400pChanSapLen * sizeof(oid);
		rval = (u_char *) StorageTmp->x400pChanSap;
		break;
	case (u_char) X400PCHANADMINISTRATIVESTATE:	/* ReadWrite */
		*write_method = write_x400pChanAdministrativeState;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pChanAdministrativeState);
		rval = (u_char *) &StorageTmp->x400pChanAdministrativeState;
		break;
	case (u_char) X400PCHANOPERATIONALSTATE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pChanOperationalState);
		rval = (u_char *) &StorageTmp->x400pChanOperationalState;
		break;
	case (u_char) X400PCHANUSAGESTATE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pChanUsageState);
		rval = (u_char *) &StorageTmp->x400pChanUsageState;
		break;
	case (u_char) X400PCHANAVAILABILITYSTATUS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->x400pChanAvailabilityStatusLen;
		rval = (u_char *) StorageTmp->x400pChanAvailabilityStatus;
		break;
	case (u_char) X400PCHANCONTROLSTATUS:	/* ReadWrite */
		*write_method = write_x400pChanControlStatus;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->x400pChanControlStatusLen;
		rval = (u_char *) StorageTmp->x400pChanControlStatus;
		break;
	case (u_char) X400PCHANPROCEDURALSTATUS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->x400pChanProceduralStatusLen;
		rval = (u_char *) StorageTmp->x400pChanProceduralStatus;
		break;
	case (u_char) X400PCHANALARMSTATUS:	/* ReadWrite */
		*write_method = write_x400pChanAlarmStatus;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->x400pChanAlarmStatusLen;
		rval = (u_char *) StorageTmp->x400pChanAlarmStatus;
		break;
	case (u_char) X400PCHANSTANDBYSTATUS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pChanStandbyStatus);
		rval = (u_char *) &StorageTmp->x400pChanStandbyStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_x400pXconTable_row(struct x400pXconTable_data *StorageTmp, struct x400pXconTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_x400pXconTable_row(struct x400pXconTable_data *StorageTmp, struct x400pXconTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_x400pXconTable_row(struct x400pXconTable_data *StorageTmp, struct x400pXconTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_x400pXconTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_x400pXconTable_row(struct x400pXconTable_data *StorageTmp, struct x400pXconTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	x400pXconTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_x400pXconTable_row(struct x400pXconTable_data *StorageTmp, struct x400pXconTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_x400pXconTable_row(struct x400pXconTable_data *StorageTmp, struct x400pXconTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_x400pXconTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_x400pXconTable_row(struct x400pXconTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the x400pXconTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct x400pXconTable_data *
refresh_x400pXconTable_row(struct x400pXconTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->x400pXconTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->x400pXconTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_x400pXconTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the x400pXconTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_x400pXconTable(int force)
{
	refresh_x400pChanTable(force);
	if (!force && x400pXconTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	x400pXconTable_refresh = 0;
}

/**
 * @fn u_char *var_x400pXconTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in x400pXconTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_x400pMxMIB above.
 */
u_char *
var_x400pXconTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct x400pXconTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("x400pMxMIB", "var_x400pXconTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_x400pXconTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(x400pXconTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_x400pXconTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) X400PXCONCARDINDEX:	/* Create */
		*write_method = write_x400pXconCardIndex;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pXconCardIndex);
		rval = (u_char *) &StorageTmp->x400pXconCardIndex;
		break;
	case (u_char) X400PXCONSPANINDEX:	/* Create */
		*write_method = write_x400pXconSpanIndex;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pXconSpanIndex);
		rval = (u_char *) &StorageTmp->x400pXconSpanIndex;
		break;
	case (u_char) X400PXCONCHANINDEX:	/* Create */
		*write_method = write_x400pXconChanIndex;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pXconChanIndex);
		rval = (u_char *) &StorageTmp->x400pXconChanIndex;
		break;
	case (u_char) X400PXCONTYPE:	/* Create */
		*write_method = write_x400pXconType;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pXconType);
		rval = (u_char *) &StorageTmp->x400pXconType;
		break;
	case (u_char) X400PXCONSTORAGETYPE:	/* Create */
		*write_method = write_x400pXconStorageType;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pXconStorageType);
		rval = (u_char *) &StorageTmp->x400pXconStorageType;
		break;
	case (u_char) X400PXCONROWSTATUS:	/* Create */
		*write_method = write_x400pXconRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pXconRowStatus);
		rval = (u_char *) &StorageTmp->x400pXconRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_x400pNearEndCurrentTable_row(struct x400pNearEndCurrentTable_data *StorageTmp, struct x400pNearEndCurrentTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_x400pNearEndCurrentTable_row(struct x400pNearEndCurrentTable_data *StorageTmp, struct x400pNearEndCurrentTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_x400pNearEndCurrentTable_row(struct x400pNearEndCurrentTable_data *StorageTmp, struct x400pNearEndCurrentTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_x400pNearEndCurrentTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_x400pNearEndCurrentTable_row(struct x400pNearEndCurrentTable_data *StorageTmp, struct x400pNearEndCurrentTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	x400pNearEndCurrentTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_x400pNearEndCurrentTable_row(struct x400pNearEndCurrentTable_data *StorageTmp, struct x400pNearEndCurrentTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_x400pNearEndCurrentTable_row(struct x400pNearEndCurrentTable_data *StorageTmp, struct x400pNearEndCurrentTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_x400pNearEndCurrentTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_x400pNearEndCurrentTable_row(struct x400pNearEndCurrentTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the x400pNearEndCurrentTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct x400pNearEndCurrentTable_data *
refresh_x400pNearEndCurrentTable_row(struct x400pNearEndCurrentTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->x400pNearEndCurrentTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->x400pNearEndCurrentTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_x400pNearEndCurrentTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the x400pNearEndCurrentTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_x400pNearEndCurrentTable(int force)
{
	refresh_x400pSpanTable(force);
	if (!force && x400pNearEndCurrentTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	x400pNearEndCurrentTable_refresh = 0;
}

/**
 * @fn u_char *var_x400pNearEndCurrentTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in x400pNearEndCurrentTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_x400pMxMIB above.
 */
u_char *
var_x400pNearEndCurrentTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct x400pNearEndCurrentTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("x400pMxMIB", "var_x400pNearEndCurrentTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_x400pNearEndCurrentTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(x400pNearEndCurrentTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_x400pNearEndCurrentTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) X400PNEARENDCURRENTTIMEELAPSED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pNearEndCurrentTimeElapsed);
		rval = (u_char *) &StorageTmp->x400pNearEndCurrentTimeElapsed;
		break;
	case (u_char) X400PNEARENDCURRENTESS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pNearEndCurrentESs);
		rval = (u_char *) &StorageTmp->x400pNearEndCurrentESs;
		break;
	case (u_char) X400PNEARENDCURRENTSESS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pNearEndCurrentSESs);
		rval = (u_char *) &StorageTmp->x400pNearEndCurrentSESs;
		break;
	case (u_char) X400PNEARENDCURRENTSEFSS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pNearEndCurrentSEFSs);
		rval = (u_char *) &StorageTmp->x400pNearEndCurrentSEFSs;
		break;
	case (u_char) X400PNEARENDCURRENTUASS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pNearEndCurrentUASs);
		rval = (u_char *) &StorageTmp->x400pNearEndCurrentUASs;
		break;
	case (u_char) X400PNEARENDCURRENTCSSS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pNearEndCurrentCSSs);
		rval = (u_char *) &StorageTmp->x400pNearEndCurrentCSSs;
		break;
	case (u_char) X400PNEARENDCURRENTPCVS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pNearEndCurrentPCVs);
		rval = (u_char *) &StorageTmp->x400pNearEndCurrentPCVs;
		break;
	case (u_char) X400PNEARENDCURRENTLESS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pNearEndCurrentLESs);
		rval = (u_char *) &StorageTmp->x400pNearEndCurrentLESs;
		break;
	case (u_char) X400PNEARENDCURRENTBESS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pNearEndCurrentBESs);
		rval = (u_char *) &StorageTmp->x400pNearEndCurrentBESs;
		break;
	case (u_char) X400PNEARENDCURRENTDMS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pNearEndCurrentDMs);
		rval = (u_char *) &StorageTmp->x400pNearEndCurrentDMs;
		break;
	case (u_char) X400PNEARENDCURRENTLCVS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pNearEndCurrentLCVs);
		rval = (u_char *) &StorageTmp->x400pNearEndCurrentLCVs;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_x400pNearEndIntervalTable_row(struct x400pNearEndIntervalTable_data *StorageTmp, struct x400pNearEndIntervalTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_x400pNearEndIntervalTable_row(struct x400pNearEndIntervalTable_data *StorageTmp, struct x400pNearEndIntervalTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_x400pNearEndIntervalTable_row(struct x400pNearEndIntervalTable_data *StorageTmp, struct x400pNearEndIntervalTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_x400pNearEndIntervalTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_x400pNearEndIntervalTable_row(struct x400pNearEndIntervalTable_data *StorageTmp, struct x400pNearEndIntervalTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	x400pNearEndIntervalTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_x400pNearEndIntervalTable_row(struct x400pNearEndIntervalTable_data *StorageTmp, struct x400pNearEndIntervalTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_x400pNearEndIntervalTable_row(struct x400pNearEndIntervalTable_data *StorageTmp, struct x400pNearEndIntervalTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_x400pNearEndIntervalTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_x400pNearEndIntervalTable_row(struct x400pNearEndIntervalTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the x400pNearEndIntervalTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct x400pNearEndIntervalTable_data *
refresh_x400pNearEndIntervalTable_row(struct x400pNearEndIntervalTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->x400pNearEndIntervalTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->x400pNearEndIntervalTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_x400pNearEndIntervalTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the x400pNearEndIntervalTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_x400pNearEndIntervalTable(int force)
{
	refresh_x400pSpanTable(force);
	if (!force && x400pNearEndIntervalTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	x400pNearEndIntervalTable_refresh = 0;
}

/**
 * @fn u_char *var_x400pNearEndIntervalTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in x400pNearEndIntervalTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_x400pMxMIB above.
 */
u_char *
var_x400pNearEndIntervalTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct x400pNearEndIntervalTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("x400pMxMIB", "var_x400pNearEndIntervalTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_x400pNearEndIntervalTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(x400pNearEndIntervalTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_x400pNearEndIntervalTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) X400PNEARENDINTERVALESS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pNearEndIntervalESs);
		rval = (u_char *) &StorageTmp->x400pNearEndIntervalESs;
		break;
	case (u_char) X400PNEARENDINTERVALSESS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pNearEndIntervalSESs);
		rval = (u_char *) &StorageTmp->x400pNearEndIntervalSESs;
		break;
	case (u_char) X400PNEARENDINTERVALSEFSS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pNearEndIntervalSEFSs);
		rval = (u_char *) &StorageTmp->x400pNearEndIntervalSEFSs;
		break;
	case (u_char) X400PNEARENDINTERVALUASS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pNearEndIntervalUASs);
		rval = (u_char *) &StorageTmp->x400pNearEndIntervalUASs;
		break;
	case (u_char) X400PNEARENDINTERVALCSSS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pNearEndIntervalCSSs);
		rval = (u_char *) &StorageTmp->x400pNearEndIntervalCSSs;
		break;
	case (u_char) X400PNEARENDINTERVALPCVS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pNearEndIntervalPCVs);
		rval = (u_char *) &StorageTmp->x400pNearEndIntervalPCVs;
		break;
	case (u_char) X400PNEARENDINTERVALLESS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pNearEndIntervalLESs);
		rval = (u_char *) &StorageTmp->x400pNearEndIntervalLESs;
		break;
	case (u_char) X400PNEARENDINTERVALBESS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pNearEndIntervalBESs);
		rval = (u_char *) &StorageTmp->x400pNearEndIntervalBESs;
		break;
	case (u_char) X400PNEARENDINTERVALDMS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pNearEndIntervalDMs);
		rval = (u_char *) &StorageTmp->x400pNearEndIntervalDMs;
		break;
	case (u_char) X400PNEARENDINTERVALLCVS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pNearEndIntervalLCVs);
		rval = (u_char *) &StorageTmp->x400pNearEndIntervalLCVs;
		break;
	case (u_char) X400PNEARENDINTERVALVALIDDATA:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pNearEndIntervalValidData);
		rval = (u_char *) &StorageTmp->x400pNearEndIntervalValidData;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_x400pNearEndTotalTable_row(struct x400pNearEndTotalTable_data *StorageTmp, struct x400pNearEndTotalTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_x400pNearEndTotalTable_row(struct x400pNearEndTotalTable_data *StorageTmp, struct x400pNearEndTotalTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_x400pNearEndTotalTable_row(struct x400pNearEndTotalTable_data *StorageTmp, struct x400pNearEndTotalTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_x400pNearEndTotalTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_x400pNearEndTotalTable_row(struct x400pNearEndTotalTable_data *StorageTmp, struct x400pNearEndTotalTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	x400pNearEndTotalTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_x400pNearEndTotalTable_row(struct x400pNearEndTotalTable_data *StorageTmp, struct x400pNearEndTotalTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_x400pNearEndTotalTable_row(struct x400pNearEndTotalTable_data *StorageTmp, struct x400pNearEndTotalTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_x400pNearEndTotalTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_x400pNearEndTotalTable_row(struct x400pNearEndTotalTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the x400pNearEndTotalTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct x400pNearEndTotalTable_data *
refresh_x400pNearEndTotalTable_row(struct x400pNearEndTotalTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->x400pNearEndTotalTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->x400pNearEndTotalTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_x400pNearEndTotalTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the x400pNearEndTotalTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_x400pNearEndTotalTable(int force)
{
	refresh_x400pSpanTable(force);
	if (!force && x400pNearEndTotalTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	x400pNearEndTotalTable_refresh = 0;
}

/**
 * @fn u_char *var_x400pNearEndTotalTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in x400pNearEndTotalTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_x400pMxMIB above.
 */
u_char *
var_x400pNearEndTotalTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct x400pNearEndTotalTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("x400pMxMIB", "var_x400pNearEndTotalTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_x400pNearEndTotalTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(x400pNearEndTotalTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_x400pNearEndTotalTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) X400PNEARENDTOTALVALIDINTERVALS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pNearEndTotalValidIntervals);
		rval = (u_char *) &StorageTmp->x400pNearEndTotalValidIntervals;
		break;
	case (u_char) X400PNEARENDTOTALINVALIDINTERVALS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pNearEndTotalInvalidIntervals);
		rval = (u_char *) &StorageTmp->x400pNearEndTotalInvalidIntervals;
		break;
	case (u_char) X400PNEARENDTOTALESS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pNearEndTotalESs);
		rval = (u_char *) &StorageTmp->x400pNearEndTotalESs;
		break;
	case (u_char) X400PNEARENDTOTALSESS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pNearEndTotalSESs);
		rval = (u_char *) &StorageTmp->x400pNearEndTotalSESs;
		break;
	case (u_char) X400PNEARENDTOTALSEFSS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pNearEndTotalSEFSs);
		rval = (u_char *) &StorageTmp->x400pNearEndTotalSEFSs;
		break;
	case (u_char) X400PNEARENDTOTALUASS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pNearEndTotalUASs);
		rval = (u_char *) &StorageTmp->x400pNearEndTotalUASs;
		break;
	case (u_char) X400PNEARENDTOTALCSSS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pNearEndTotalCSSs);
		rval = (u_char *) &StorageTmp->x400pNearEndTotalCSSs;
		break;
	case (u_char) X400PNEARENDTOTALPCVS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pNearEndTotalPCVs);
		rval = (u_char *) &StorageTmp->x400pNearEndTotalPCVs;
		break;
	case (u_char) X400PNEARENDTOTALLESS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pNearEndTotalLESs);
		rval = (u_char *) &StorageTmp->x400pNearEndTotalLESs;
		break;
	case (u_char) X400PNEARENDTOTALBESS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pNearEndTotalBESs);
		rval = (u_char *) &StorageTmp->x400pNearEndTotalBESs;
		break;
	case (u_char) X400PNEARENDTOTALDMS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pNearEndTotalDMs);
		rval = (u_char *) &StorageTmp->x400pNearEndTotalDMs;
		break;
	case (u_char) X400PNEARENDTOTALLCVS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pNearEndTotalLCVs);
		rval = (u_char *) &StorageTmp->x400pNearEndTotalLCVs;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_x400pFarEndCurrentTable_row(struct x400pFarEndCurrentTable_data *StorageTmp, struct x400pFarEndCurrentTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_x400pFarEndCurrentTable_row(struct x400pFarEndCurrentTable_data *StorageTmp, struct x400pFarEndCurrentTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_x400pFarEndCurrentTable_row(struct x400pFarEndCurrentTable_data *StorageTmp, struct x400pFarEndCurrentTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_x400pFarEndCurrentTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_x400pFarEndCurrentTable_row(struct x400pFarEndCurrentTable_data *StorageTmp, struct x400pFarEndCurrentTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	x400pFarEndCurrentTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_x400pFarEndCurrentTable_row(struct x400pFarEndCurrentTable_data *StorageTmp, struct x400pFarEndCurrentTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_x400pFarEndCurrentTable_row(struct x400pFarEndCurrentTable_data *StorageTmp, struct x400pFarEndCurrentTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_x400pFarEndCurrentTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_x400pFarEndCurrentTable_row(struct x400pFarEndCurrentTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the x400pFarEndCurrentTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct x400pFarEndCurrentTable_data *
refresh_x400pFarEndCurrentTable_row(struct x400pFarEndCurrentTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->x400pFarEndCurrentTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->x400pFarEndCurrentTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_x400pFarEndCurrentTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the x400pFarEndCurrentTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_x400pFarEndCurrentTable(int force)
{
	refresh_x400pSpanTable(force);
	if (!force && x400pFarEndCurrentTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	x400pFarEndCurrentTable_refresh = 0;
}

/**
 * @fn u_char *var_x400pFarEndCurrentTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in x400pFarEndCurrentTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_x400pMxMIB above.
 */
u_char *
var_x400pFarEndCurrentTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct x400pFarEndCurrentTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("x400pMxMIB", "var_x400pFarEndCurrentTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_x400pFarEndCurrentTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(x400pFarEndCurrentTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_x400pFarEndCurrentTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) X400PFARENDCURRENTTIMEELAPSED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pFarEndCurrentTimeElapsed);
		rval = (u_char *) &StorageTmp->x400pFarEndCurrentTimeElapsed;
		break;
	case (u_char) X400PFARENDCURRENTESS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pFarEndCurrentESs);
		rval = (u_char *) &StorageTmp->x400pFarEndCurrentESs;
		break;
	case (u_char) X400PFARENDCURRENTSESS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pFarEndCurrentSESs);
		rval = (u_char *) &StorageTmp->x400pFarEndCurrentSESs;
		break;
	case (u_char) X400PFARENDCURRENTSEFSS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pFarEndCurrentSEFSs);
		rval = (u_char *) &StorageTmp->x400pFarEndCurrentSEFSs;
		break;
	case (u_char) X400PFARENDCURRENTUASS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pFarEndCurrentUASs);
		rval = (u_char *) &StorageTmp->x400pFarEndCurrentUASs;
		break;
	case (u_char) X400PFARENDCURRENTCSSS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pFarEndCurrentCSSs);
		rval = (u_char *) &StorageTmp->x400pFarEndCurrentCSSs;
		break;
	case (u_char) X400PFARENDCURRENTPCVS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pFarEndCurrentPCVs);
		rval = (u_char *) &StorageTmp->x400pFarEndCurrentPCVs;
		break;
	case (u_char) X400PFARENDCURRENTLESS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pFarEndCurrentLESs);
		rval = (u_char *) &StorageTmp->x400pFarEndCurrentLESs;
		break;
	case (u_char) X400PFARENDCURRENTBESS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pFarEndCurrentBESs);
		rval = (u_char *) &StorageTmp->x400pFarEndCurrentBESs;
		break;
	case (u_char) X400PFARENDCURRENTDMS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pFarEndCurrentDMs);
		rval = (u_char *) &StorageTmp->x400pFarEndCurrentDMs;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_x400pFarEndIntervalTable_row(struct x400pFarEndIntervalTable_data *StorageTmp, struct x400pFarEndIntervalTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_x400pFarEndIntervalTable_row(struct x400pFarEndIntervalTable_data *StorageTmp, struct x400pFarEndIntervalTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_x400pFarEndIntervalTable_row(struct x400pFarEndIntervalTable_data *StorageTmp, struct x400pFarEndIntervalTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_x400pFarEndIntervalTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_x400pFarEndIntervalTable_row(struct x400pFarEndIntervalTable_data *StorageTmp, struct x400pFarEndIntervalTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	x400pFarEndIntervalTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_x400pFarEndIntervalTable_row(struct x400pFarEndIntervalTable_data *StorageTmp, struct x400pFarEndIntervalTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_x400pFarEndIntervalTable_row(struct x400pFarEndIntervalTable_data *StorageTmp, struct x400pFarEndIntervalTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_x400pFarEndIntervalTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_x400pFarEndIntervalTable_row(struct x400pFarEndIntervalTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the x400pFarEndIntervalTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct x400pFarEndIntervalTable_data *
refresh_x400pFarEndIntervalTable_row(struct x400pFarEndIntervalTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->x400pFarEndIntervalTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->x400pFarEndIntervalTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_x400pFarEndIntervalTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the x400pFarEndIntervalTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_x400pFarEndIntervalTable(int force)
{
	refresh_x400pSpanTable(force);
	if (!force && x400pFarEndIntervalTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	x400pFarEndIntervalTable_refresh = 0;
}

/**
 * @fn u_char *var_x400pFarEndIntervalTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in x400pFarEndIntervalTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_x400pMxMIB above.
 */
u_char *
var_x400pFarEndIntervalTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct x400pFarEndIntervalTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("x400pMxMIB", "var_x400pFarEndIntervalTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_x400pFarEndIntervalTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(x400pFarEndIntervalTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_x400pFarEndIntervalTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) X400PFARENDINTERVALESS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pFarEndIntervalESs);
		rval = (u_char *) &StorageTmp->x400pFarEndIntervalESs;
		break;
	case (u_char) X400PFARENDINTERVALSESS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pFarEndIntervalSESs);
		rval = (u_char *) &StorageTmp->x400pFarEndIntervalSESs;
		break;
	case (u_char) X400PFARENDINTERVALSEFSS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pFarEndIntervalSEFSs);
		rval = (u_char *) &StorageTmp->x400pFarEndIntervalSEFSs;
		break;
	case (u_char) X400PFARENDINTERVALUASS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pFarEndIntervalUASs);
		rval = (u_char *) &StorageTmp->x400pFarEndIntervalUASs;
		break;
	case (u_char) X400PFARENDINTERVALCSSS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pFarEndIntervalCSSs);
		rval = (u_char *) &StorageTmp->x400pFarEndIntervalCSSs;
		break;
	case (u_char) X400PFARENDINTERVALPCVS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pFarEndIntervalPCVs);
		rval = (u_char *) &StorageTmp->x400pFarEndIntervalPCVs;
		break;
	case (u_char) X400PFARENDINTERVALLESS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pFarEndIntervalLESs);
		rval = (u_char *) &StorageTmp->x400pFarEndIntervalLESs;
		break;
	case (u_char) X400PFARENDINTERVALBESS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pFarEndIntervalBESs);
		rval = (u_char *) &StorageTmp->x400pFarEndIntervalBESs;
		break;
	case (u_char) X400PFARENDINTERVALDMS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pFarEndIntervalDMs);
		rval = (u_char *) &StorageTmp->x400pFarEndIntervalDMs;
		break;
	case (u_char) X400PFARENDINTERVALVALIDDATA:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pFarEndIntervalValidData);
		rval = (u_char *) &StorageTmp->x400pFarEndIntervalValidData;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_x400pFarEndTotalTable_row(struct x400pFarEndTotalTable_data *StorageTmp, struct x400pFarEndTotalTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_x400pFarEndTotalTable_row(struct x400pFarEndTotalTable_data *StorageTmp, struct x400pFarEndTotalTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_x400pFarEndTotalTable_row(struct x400pFarEndTotalTable_data *StorageTmp, struct x400pFarEndTotalTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_x400pFarEndTotalTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_x400pFarEndTotalTable_row(struct x400pFarEndTotalTable_data *StorageTmp, struct x400pFarEndTotalTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	x400pFarEndTotalTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_x400pFarEndTotalTable_row(struct x400pFarEndTotalTable_data *StorageTmp, struct x400pFarEndTotalTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_x400pFarEndTotalTable_row(struct x400pFarEndTotalTable_data *StorageTmp, struct x400pFarEndTotalTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_x400pFarEndTotalTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_x400pFarEndTotalTable_row(struct x400pFarEndTotalTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the x400pFarEndTotalTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct x400pFarEndTotalTable_data *
refresh_x400pFarEndTotalTable_row(struct x400pFarEndTotalTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->x400pFarEndTotalTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->x400pFarEndTotalTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_x400pFarEndTotalTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the x400pFarEndTotalTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_x400pFarEndTotalTable(int force)
{
	refresh_x400pSpanTable(force);
	if (!force && x400pFarEndTotalTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	x400pFarEndTotalTable_refresh = 0;
}

/**
 * @fn u_char *var_x400pFarEndTotalTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in x400pFarEndTotalTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_x400pMxMIB above.
 */
u_char *
var_x400pFarEndTotalTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct x400pFarEndTotalTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("x400pMxMIB", "var_x400pFarEndTotalTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_x400pFarEndTotalTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(x400pFarEndTotalTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_x400pFarEndTotalTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) X400PFARENDTOTALVALIDINTERVALS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pFarEndTotalValidIntervals);
		rval = (u_char *) &StorageTmp->x400pFarEndTotalValidIntervals;
		break;
	case (u_char) X400PFARENDTOTALINVALIDINTERVALS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pFarEndTotalInvalidIntervals);
		rval = (u_char *) &StorageTmp->x400pFarEndTotalInvalidIntervals;
		break;
	case (u_char) X400PFARENDTOTALESS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pFarEndTotalESs);
		rval = (u_char *) &StorageTmp->x400pFarEndTotalESs;
		break;
	case (u_char) X400PFARENDTOTALSESS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pFarEndTotalSESs);
		rval = (u_char *) &StorageTmp->x400pFarEndTotalSESs;
		break;
	case (u_char) X400PFARENDTOTALSEFSS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pFarEndTotalSEFSs);
		rval = (u_char *) &StorageTmp->x400pFarEndTotalSEFSs;
		break;
	case (u_char) X400PFARENDTOTALUASS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pFarEndTotalUASs);
		rval = (u_char *) &StorageTmp->x400pFarEndTotalUASs;
		break;
	case (u_char) X400PFARENDTOTALCSSS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pFarEndTotalCSSs);
		rval = (u_char *) &StorageTmp->x400pFarEndTotalCSSs;
		break;
	case (u_char) X400PFARENDTOTALPCVS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pFarEndTotalPCVs);
		rval = (u_char *) &StorageTmp->x400pFarEndTotalPCVs;
		break;
	case (u_char) X400PFARENDTOTALLESS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pFarEndTotalLESs);
		rval = (u_char *) &StorageTmp->x400pFarEndTotalLESs;
		break;
	case (u_char) X400PFARENDTOTALBESS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pFarEndTotalBESs);
		rval = (u_char *) &StorageTmp->x400pFarEndTotalBESs;
		break;
	case (u_char) X400PFARENDTOTALDMS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->x400pFarEndTotalDMs);
		rval = (u_char *) &StorageTmp->x400pFarEndTotalDMs;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int write_x400pSyncSpanId(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_x400pSyncSpanId(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct x400pSyncTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("x400pMxMIB", "write_x400pSyncSpanId entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(x400pSyncTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->x400pSyncRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSyncSpanId: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSyncSpanId not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 0..0 4..4 */
		if (var_val_len > SPRINT_MAX_LEN || (var_val_len != 0 && var_val_len != 4)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSyncSpanId: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->x400pSyncTable_old) == NULL)
			if (StorageTmp->x400pSyncTable_rsvs == 0)
				if ((StorageOld = StorageTmp->x400pSyncTable_old = x400pSyncTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->x400pSyncTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->x400pSyncSpanId);
		StorageTmp->x400pSyncSpanId = string;
		StorageTmp->x400pSyncSpanIdLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->x400pSyncTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->x400pSyncTable_tsts == 0)
				if ((ret = check_x400pSyncTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pSyncTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->x400pSyncSpanId for you to use, and you have just been asked to do something with it.  Note that anything done here must
				   be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->x400pSyncTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->x400pSyncTable_sets == 0)
				if ((ret = update_x400pSyncTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pSyncTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->x400pSyncTable_old) != NULL) {
			x400pSyncTable_destroy(&StorageTmp->x400pSyncTable_old);
			StorageTmp->x400pSyncTable_rsvs = 0;
			StorageTmp->x400pSyncTable_tsts = 0;
			StorageTmp->x400pSyncTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->x400pSyncTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->x400pSyncTable_sets == 0)
			revert_x400pSyncTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->x400pSyncTable_old) == NULL)
			break;
		if (StorageOld->x400pSyncSpanId != NULL) {
			SNMP_FREE(StorageTmp->x400pSyncSpanId);
			StorageTmp->x400pSyncSpanId = StorageOld->x400pSyncSpanId;
			StorageTmp->x400pSyncSpanIdLen = StorageOld->x400pSyncSpanIdLen;
			StorageOld->x400pSyncSpanId = NULL;
			StorageOld->x400pSyncSpanIdLen = 0;
		}
		if (--StorageTmp->x400pSyncTable_rsvs == 0)
			x400pSyncTable_destroy(&StorageTmp->x400pSyncTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_x400pCardSpanType(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_x400pCardSpanType(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct x400pCardTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("x400pMxMIB", "write_x400pCardSpanType entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(x400pCardTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->x400pCardStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pCardSpanType: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pCardSpanType not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pCardSpanType: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case X400PCARDSPANTYPE_NONE:
		case X400PCARDSPANTYPE_T1:
		case X400PCARDSPANTYPE_E1:
		case X400PCARDSPANTYPE_J1:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pCardSpanType: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->x400pCardTable_old) == NULL)
			if (StorageTmp->x400pCardTable_rsvs == 0)
				if ((StorageOld = StorageTmp->x400pCardTable_old = x400pCardTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->x400pCardTable_rsvs++;
		StorageTmp->x400pCardSpanType = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->x400pCardTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->x400pCardTable_tsts == 0)
				if ((ret = check_x400pCardTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pCardTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->x400pCardSpanType for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->x400pCardTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->x400pCardTable_sets == 0)
				if ((ret = update_x400pCardTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pCardTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->x400pCardTable_old) != NULL) {
			x400pCardTable_destroy(&StorageTmp->x400pCardTable_old);
			StorageTmp->x400pCardTable_rsvs = 0;
			StorageTmp->x400pCardTable_tsts = 0;
			StorageTmp->x400pCardTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->x400pCardTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->x400pCardTable_sets == 0)
			revert_x400pCardTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->x400pCardTable_old) == NULL)
			break;
		StorageTmp->x400pCardSpanType = StorageOld->x400pCardSpanType;
		if (--StorageTmp->x400pCardTable_rsvs == 0)
			x400pCardTable_destroy(&StorageTmp->x400pCardTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_x400pCardMode(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_x400pCardMode(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct x400pCardTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("x400pMxMIB", "write_x400pCardMode entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(x400pCardTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->x400pCardStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pCardMode: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if ((var_val_type != ASN_BIT_STR && var_val_type != ASN_OCTET_STR)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pCardMode not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_type == ASN_BIT_STR) {
			if (1 > var_val_len || var_val_len > SPRINT_MAX_LEN || var_val_len != 2) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pCardMode: bad length\n");
				return SNMP_ERR_WRONGLENGTH;
			}
		}
		if (var_val_type == ASN_OCTET_STR) {
			if (var_val_len > SPRINT_MAX_LEN || var_val_len != 1) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pCardMode: bad length\n");
				return SNMP_ERR_WRONGLENGTH;
			}
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->x400pCardTable_old) == NULL)
			if (StorageTmp->x400pCardTable_rsvs == 0)
				if ((StorageOld = StorageTmp->x400pCardTable_old = x400pCardTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->x400pCardTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->x400pCardMode);
		StorageTmp->x400pCardMode = string;
		StorageTmp->x400pCardModeLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->x400pCardTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->x400pCardTable_tsts == 0)
				if ((ret = check_x400pCardTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pCardTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->x400pCardMode for you to use, and you have just been asked to do something with it.  Note that anything done here must
				   be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->x400pCardTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->x400pCardTable_sets == 0)
				if ((ret = update_x400pCardTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pCardTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->x400pCardTable_old) != NULL) {
			x400pCardTable_destroy(&StorageTmp->x400pCardTable_old);
			StorageTmp->x400pCardTable_rsvs = 0;
			StorageTmp->x400pCardTable_tsts = 0;
			StorageTmp->x400pCardTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->x400pCardTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->x400pCardTable_sets == 0)
			revert_x400pCardTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->x400pCardTable_old) == NULL)
			break;
		if (StorageOld->x400pCardMode != NULL) {
			SNMP_FREE(StorageTmp->x400pCardMode);
			StorageTmp->x400pCardMode = StorageOld->x400pCardMode;
			StorageTmp->x400pCardModeLen = StorageOld->x400pCardModeLen;
			StorageOld->x400pCardMode = NULL;
			StorageOld->x400pCardModeLen = 0;
		}
		if (--StorageTmp->x400pCardTable_rsvs == 0)
			x400pCardTable_destroy(&StorageTmp->x400pCardTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_x400pCardSyncMaster(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_x400pCardSyncMaster(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct x400pCardTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("x400pMxMIB", "write_x400pCardSyncMaster entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(x400pCardTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->x400pCardStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pCardSyncMaster: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pCardSyncMaster not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pCardSyncMaster: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case X400PCARDSYNCMASTER_SLAVE:
		case X400PCARDSYNCMASTER_MASTER:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pCardSyncMaster: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->x400pCardTable_old) == NULL)
			if (StorageTmp->x400pCardTable_rsvs == 0)
				if ((StorageOld = StorageTmp->x400pCardTable_old = x400pCardTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->x400pCardTable_rsvs++;
		StorageTmp->x400pCardSyncMaster = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->x400pCardTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->x400pCardTable_tsts == 0)
				if ((ret = check_x400pCardTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pCardTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->x400pCardSyncMaster for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->x400pCardTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->x400pCardTable_sets == 0)
				if ((ret = update_x400pCardTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pCardTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->x400pCardTable_old) != NULL) {
			x400pCardTable_destroy(&StorageTmp->x400pCardTable_old);
			StorageTmp->x400pCardTable_rsvs = 0;
			StorageTmp->x400pCardTable_tsts = 0;
			StorageTmp->x400pCardTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->x400pCardTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->x400pCardTable_sets == 0)
			revert_x400pCardTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->x400pCardTable_old) == NULL)
			break;
		StorageTmp->x400pCardSyncMaster = StorageOld->x400pCardSyncMaster;
		if (--StorageTmp->x400pCardTable_rsvs == 0)
			x400pCardTable_destroy(&StorageTmp->x400pCardTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_x400pCardSyncSource(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_x400pCardSyncSource(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct x400pCardTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("x400pMxMIB", "write_x400pCardSyncSource entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(x400pCardTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->x400pCardStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pCardSyncSource: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pCardSyncSource not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pCardSyncSource: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case X400PCARDSYNCSOURCE_SYNCSELF:
		case X400PCARDSYNCSOURCE_SYNC1:
		case X400PCARDSYNCSOURCE_SYNC2:
		case X400PCARDSYNCSOURCE_SYNC3:
		case X400PCARDSYNCSOURCE_SYNC4:
		case X400PCARDSYNCSOURCE_SYNCEXTERN:
		case X400PCARDSYNCSOURCE_SYNCAUTO:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pCardSyncSource: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->x400pCardTable_old) == NULL)
			if (StorageTmp->x400pCardTable_rsvs == 0)
				if ((StorageOld = StorageTmp->x400pCardTable_old = x400pCardTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->x400pCardTable_rsvs++;
		StorageTmp->x400pCardSyncSource = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->x400pCardTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->x400pCardTable_tsts == 0)
				if ((ret = check_x400pCardTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pCardTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->x400pCardSyncSource for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->x400pCardTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->x400pCardTable_sets == 0)
				if ((ret = update_x400pCardTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pCardTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->x400pCardTable_old) != NULL) {
			x400pCardTable_destroy(&StorageTmp->x400pCardTable_old);
			StorageTmp->x400pCardTable_rsvs = 0;
			StorageTmp->x400pCardTable_tsts = 0;
			StorageTmp->x400pCardTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->x400pCardTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->x400pCardTable_sets == 0)
			revert_x400pCardTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->x400pCardTable_old) == NULL)
			break;
		StorageTmp->x400pCardSyncSource = StorageOld->x400pCardSyncSource;
		if (--StorageTmp->x400pCardTable_rsvs == 0)
			x400pCardTable_destroy(&StorageTmp->x400pCardTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_x400pCardSyncGroup(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_x400pCardSyncGroup(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct x400pCardTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("x400pMxMIB", "write_x400pCardSyncGroup entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(x400pCardTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->x400pCardStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pCardSyncGroup: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pCardSyncGroup not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pCardSyncGroup: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 1..16 */
		if ((1 > set_value || set_value > 16)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pCardSyncGroup: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->x400pCardTable_old) == NULL)
			if (StorageTmp->x400pCardTable_rsvs == 0)
				if ((StorageOld = StorageTmp->x400pCardTable_old = x400pCardTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->x400pCardTable_rsvs++;
		StorageTmp->x400pCardSyncGroup = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->x400pCardTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->x400pCardTable_tsts == 0)
				if ((ret = check_x400pCardTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pCardTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->x400pCardSyncGroup for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->x400pCardTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->x400pCardTable_sets == 0)
				if ((ret = update_x400pCardTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pCardTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->x400pCardTable_old) != NULL) {
			x400pCardTable_destroy(&StorageTmp->x400pCardTable_old);
			StorageTmp->x400pCardTable_rsvs = 0;
			StorageTmp->x400pCardTable_tsts = 0;
			StorageTmp->x400pCardTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->x400pCardTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->x400pCardTable_sets == 0)
			revert_x400pCardTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->x400pCardTable_old) == NULL)
			break;
		StorageTmp->x400pCardSyncGroup = StorageOld->x400pCardSyncGroup;
		if (--StorageTmp->x400pCardTable_rsvs == 0)
			x400pCardTable_destroy(&StorageTmp->x400pCardTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_x400pCardAdministrativeState(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_x400pCardAdministrativeState(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct x400pCardTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("x400pMxMIB", "write_x400pCardAdministrativeState entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(x400pCardTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->x400pCardStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pCardAdministrativeState: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pCardAdministrativeState not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pCardAdministrativeState: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value locked */
		switch (set_value) {
		case X400PCARDADMINISTRATIVESTATE_LOCKED:
		case X400PCARDADMINISTRATIVESTATE_UNLOCKED:
		case X400PCARDADMINISTRATIVESTATE_SHUTTINGDOWN:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pCardAdministrativeState: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->x400pCardTable_old) == NULL)
			if (StorageTmp->x400pCardTable_rsvs == 0)
				if ((StorageOld = StorageTmp->x400pCardTable_old = x400pCardTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->x400pCardTable_rsvs++;
		StorageTmp->x400pCardAdministrativeState = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->x400pCardTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->x400pCardTable_tsts == 0)
				if ((ret = check_x400pCardTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pCardTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->x400pCardAdministrativeState for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->x400pCardTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->x400pCardTable_sets == 0)
				if ((ret = update_x400pCardTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pCardTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->x400pCardTable_old) != NULL) {
			x400pCardTable_destroy(&StorageTmp->x400pCardTable_old);
			StorageTmp->x400pCardTable_rsvs = 0;
			StorageTmp->x400pCardTable_tsts = 0;
			StorageTmp->x400pCardTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->x400pCardTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->x400pCardTable_sets == 0)
			revert_x400pCardTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->x400pCardTable_old) == NULL)
			break;
		StorageTmp->x400pCardAdministrativeState = StorageOld->x400pCardAdministrativeState;
		if (--StorageTmp->x400pCardTable_rsvs == 0)
			x400pCardTable_destroy(&StorageTmp->x400pCardTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_x400pCardAlarmStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_x400pCardAlarmStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct x400pCardTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("x400pMxMIB", "write_x400pCardAlarmStatus entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(x400pCardTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->x400pCardStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pCardAlarmStatus: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if ((var_val_type != ASN_BIT_STR && var_val_type != ASN_OCTET_STR)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pCardAlarmStatus not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_type == ASN_BIT_STR) {
			if (1 > var_val_len || var_val_len > SPRINT_MAX_LEN || var_val_len != 2) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pCardAlarmStatus: bad length\n");
				return SNMP_ERR_WRONGLENGTH;
			}
		}
		if (var_val_type == ASN_OCTET_STR) {
			if (var_val_len > SPRINT_MAX_LEN || var_val_len != 1) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pCardAlarmStatus: bad length\n");
				return SNMP_ERR_WRONGLENGTH;
			}
		}
		/* Note: default value { } */
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->x400pCardTable_old) == NULL)
			if (StorageTmp->x400pCardTable_rsvs == 0)
				if ((StorageOld = StorageTmp->x400pCardTable_old = x400pCardTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->x400pCardTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->x400pCardAlarmStatus);
		StorageTmp->x400pCardAlarmStatus = string;
		StorageTmp->x400pCardAlarmStatusLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->x400pCardTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->x400pCardTable_tsts == 0)
				if ((ret = check_x400pCardTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pCardTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->x400pCardAlarmStatus for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->x400pCardTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->x400pCardTable_sets == 0)
				if ((ret = update_x400pCardTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pCardTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->x400pCardTable_old) != NULL) {
			x400pCardTable_destroy(&StorageTmp->x400pCardTable_old);
			StorageTmp->x400pCardTable_rsvs = 0;
			StorageTmp->x400pCardTable_tsts = 0;
			StorageTmp->x400pCardTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->x400pCardTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->x400pCardTable_sets == 0)
			revert_x400pCardTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->x400pCardTable_old) == NULL)
			break;
		if (StorageOld->x400pCardAlarmStatus != NULL) {
			SNMP_FREE(StorageTmp->x400pCardAlarmStatus);
			StorageTmp->x400pCardAlarmStatus = StorageOld->x400pCardAlarmStatus;
			StorageTmp->x400pCardAlarmStatusLen = StorageOld->x400pCardAlarmStatusLen;
			StorageOld->x400pCardAlarmStatus = NULL;
			StorageOld->x400pCardAlarmStatusLen = 0;
		}
		if (--StorageTmp->x400pCardTable_rsvs == 0)
			x400pCardTable_destroy(&StorageTmp->x400pCardTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_x400pCardControlStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_x400pCardControlStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct x400pCardTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("x400pMxMIB", "write_x400pCardControlStatus entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(x400pCardTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->x400pCardStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pCardControlStatus: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if ((var_val_type != ASN_BIT_STR && var_val_type != ASN_OCTET_STR)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pCardControlStatus not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_type == ASN_BIT_STR) {
			if (1 > var_val_len || var_val_len > SPRINT_MAX_LEN || var_val_len != 2) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pCardControlStatus: bad length\n");
				return SNMP_ERR_WRONGLENGTH;
			}
		}
		if (var_val_type == ASN_OCTET_STR) {
			if (var_val_len > SPRINT_MAX_LEN || var_val_len != 1) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pCardControlStatus: bad length\n");
				return SNMP_ERR_WRONGLENGTH;
			}
		}
		/* Note: default value { } */
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->x400pCardTable_old) == NULL)
			if (StorageTmp->x400pCardTable_rsvs == 0)
				if ((StorageOld = StorageTmp->x400pCardTable_old = x400pCardTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->x400pCardTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->x400pCardControlStatus);
		StorageTmp->x400pCardControlStatus = string;
		StorageTmp->x400pCardControlStatusLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->x400pCardTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->x400pCardTable_tsts == 0)
				if ((ret = check_x400pCardTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pCardTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->x400pCardControlStatus for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->x400pCardTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->x400pCardTable_sets == 0)
				if ((ret = update_x400pCardTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pCardTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->x400pCardTable_old) != NULL) {
			x400pCardTable_destroy(&StorageTmp->x400pCardTable_old);
			StorageTmp->x400pCardTable_rsvs = 0;
			StorageTmp->x400pCardTable_tsts = 0;
			StorageTmp->x400pCardTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->x400pCardTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->x400pCardTable_sets == 0)
			revert_x400pCardTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->x400pCardTable_old) == NULL)
			break;
		if (StorageOld->x400pCardControlStatus != NULL) {
			SNMP_FREE(StorageTmp->x400pCardControlStatus);
			StorageTmp->x400pCardControlStatus = StorageOld->x400pCardControlStatus;
			StorageTmp->x400pCardControlStatusLen = StorageOld->x400pCardControlStatusLen;
			StorageOld->x400pCardControlStatus = NULL;
			StorageOld->x400pCardControlStatusLen = 0;
		}
		if (--StorageTmp->x400pCardTable_rsvs == 0)
			x400pCardTable_destroy(&StorageTmp->x400pCardTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_x400pSpanName(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_x400pSpanName(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct x400pSpanTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("x400pMxMIB", "write_x400pSpanName entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(x400pSpanTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->x400pSpanRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanName: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanName not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 0..32 */
		if (var_val_len > SPRINT_MAX_LEN || ((0 > var_val_len || var_val_len > 32))) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanName: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value \"\" */
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			if (StorageTmp->x400pSpanTable_rsvs == 0)
				if ((StorageOld = StorageTmp->x400pSpanTable_old = x400pSpanTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->x400pSpanTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->x400pSpanName);
		StorageTmp->x400pSpanName = string;
		StorageTmp->x400pSpanNameLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->x400pSpanTable_tsts == 0)
				if ((ret = check_x400pSpanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pSpanTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->x400pSpanName for you to use, and you have just been asked to do something with it.  Note that anything done here must
				   be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->x400pSpanTable_sets == 0)
				if ((ret = update_x400pSpanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pSpanTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			x400pSpanTable_destroy(&StorageTmp->x400pSpanTable_old);
			StorageTmp->x400pSpanTable_rsvs = 0;
			StorageTmp->x400pSpanTable_tsts = 0;
			StorageTmp->x400pSpanTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->x400pSpanTable_sets == 0)
			revert_x400pSpanTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			break;
		if (StorageOld->x400pSpanName != NULL) {
			SNMP_FREE(StorageTmp->x400pSpanName);
			StorageTmp->x400pSpanName = StorageOld->x400pSpanName;
			StorageTmp->x400pSpanNameLen = StorageOld->x400pSpanNameLen;
			StorageOld->x400pSpanName = NULL;
			StorageOld->x400pSpanNameLen = 0;
		}
		if (--StorageTmp->x400pSpanTable_rsvs == 0)
			x400pSpanTable_destroy(&StorageTmp->x400pSpanTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_x400pSpanDevice(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_x400pSpanDevice(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct x400pSpanTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	oid *objid = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("x400pMxMIB", "write_x400pSpanDevice entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(x400pSpanTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->x400pSpanRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanDevice: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OBJECT_ID) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanDevice not ASN_OBJECT_ID\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (MIN_OID_LEN * sizeof(oid) > var_val_len || var_val_len > MAX_OID_LEN * sizeof(oid)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanDevice: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value zeroDotZero */
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			if (StorageTmp->x400pSpanTable_rsvs == 0)
				if ((StorageOld = StorageTmp->x400pSpanTable_old = x400pSpanTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->x400pSpanTable_rsvs++;
		if ((objid = snmp_duplicate_objid((void *) var_val, var_val_len / sizeof(oid))) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		SNMP_FREE(StorageTmp->x400pSpanDevice);
		StorageTmp->x400pSpanDevice = objid;
		StorageTmp->x400pSpanDeviceLen = var_val_len / sizeof(oid);
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->x400pSpanTable_tsts == 0)
				if ((ret = check_x400pSpanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pSpanTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->x400pSpanDevice for you to use, and you have just been asked to do something with it.  Note that anything done here must 
				   be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->x400pSpanTable_sets == 0)
				if ((ret = update_x400pSpanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pSpanTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			x400pSpanTable_destroy(&StorageTmp->x400pSpanTable_old);
			StorageTmp->x400pSpanTable_rsvs = 0;
			StorageTmp->x400pSpanTable_tsts = 0;
			StorageTmp->x400pSpanTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->x400pSpanTable_sets == 0)
			revert_x400pSpanTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			break;
		if (StorageOld->x400pSpanDevice != NULL) {
			SNMP_FREE(StorageTmp->x400pSpanDevice);
			StorageTmp->x400pSpanDevice = StorageOld->x400pSpanDevice;
			StorageTmp->x400pSpanDeviceLen = StorageOld->x400pSpanDeviceLen;
			StorageOld->x400pSpanDevice = NULL;
			StorageOld->x400pSpanDeviceLen = 0;
		}
		if (--StorageTmp->x400pSpanTable_rsvs == 0)
			x400pSpanTable_destroy(&StorageTmp->x400pSpanTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_x400pSpanEquipmentId(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_x400pSpanEquipmentId(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct x400pSpanTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("x400pMxMIB", "write_x400pSpanEquipmentId entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(x400pSpanTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->x400pSpanRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanEquipmentId: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanEquipmentId not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 2..2 */
		if (var_val_len > SPRINT_MAX_LEN || (var_val_len != 2)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanEquipmentId: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			if (StorageTmp->x400pSpanTable_rsvs == 0)
				if ((StorageOld = StorageTmp->x400pSpanTable_old = x400pSpanTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->x400pSpanTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->x400pSpanEquipmentId);
		StorageTmp->x400pSpanEquipmentId = string;
		StorageTmp->x400pSpanEquipmentIdLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->x400pSpanTable_tsts == 0)
				if ((ret = check_x400pSpanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pSpanTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->x400pSpanEquipmentId for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->x400pSpanTable_sets == 0)
				if ((ret = update_x400pSpanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pSpanTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			x400pSpanTable_destroy(&StorageTmp->x400pSpanTable_old);
			StorageTmp->x400pSpanTable_rsvs = 0;
			StorageTmp->x400pSpanTable_tsts = 0;
			StorageTmp->x400pSpanTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->x400pSpanTable_sets == 0)
			revert_x400pSpanTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			break;
		if (StorageOld->x400pSpanEquipmentId != NULL) {
			SNMP_FREE(StorageTmp->x400pSpanEquipmentId);
			StorageTmp->x400pSpanEquipmentId = StorageOld->x400pSpanEquipmentId;
			StorageTmp->x400pSpanEquipmentIdLen = StorageOld->x400pSpanEquipmentIdLen;
			StorageOld->x400pSpanEquipmentId = NULL;
			StorageOld->x400pSpanEquipmentIdLen = 0;
		}
		if (--StorageTmp->x400pSpanTable_rsvs == 0)
			x400pSpanTable_destroy(&StorageTmp->x400pSpanTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_x400pSpanType(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_x400pSpanType(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct x400pSpanTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("x400pMxMIB", "write_x400pSpanType entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(x400pSpanTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->x400pSpanRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanType: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanType not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanType: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value none */
		switch (set_value) {
		case X400PSPANTYPE_NONE:
		case X400PSPANTYPE_T1:
		case X400PSPANTYPE_E1:
		case X400PSPANTYPE_J1:
		case X400PSPANTYPE_E2:
		case X400PSPANTYPE_E3:
		case X400PSPANTYPE_T3:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanType: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			if (StorageTmp->x400pSpanTable_rsvs == 0)
				if ((StorageOld = StorageTmp->x400pSpanTable_old = x400pSpanTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->x400pSpanTable_rsvs++;
		StorageTmp->x400pSpanType = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->x400pSpanTable_tsts == 0)
				if ((ret = check_x400pSpanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pSpanTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->x400pSpanType for you to use, and you have just been asked to do something with it.  Note that anything done here must
				   be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->x400pSpanTable_sets == 0)
				if ((ret = update_x400pSpanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pSpanTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			x400pSpanTable_destroy(&StorageTmp->x400pSpanTable_old);
			StorageTmp->x400pSpanTable_rsvs = 0;
			StorageTmp->x400pSpanTable_tsts = 0;
			StorageTmp->x400pSpanTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->x400pSpanTable_sets == 0)
			revert_x400pSpanTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			break;
		StorageTmp->x400pSpanType = StorageOld->x400pSpanType;
		if (--StorageTmp->x400pSpanTable_rsvs == 0)
			x400pSpanTable_destroy(&StorageTmp->x400pSpanTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_x400pSpanNumber(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_x400pSpanNumber(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct x400pSpanTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("x400pMxMIB", "write_x400pSpanNumber entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(x400pSpanTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->x400pSpanRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanNumber: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanNumber not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanNumber: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..32 */
		if ((0 > set_value || set_value > 32)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanNumber: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			if (StorageTmp->x400pSpanTable_rsvs == 0)
				if ((StorageOld = StorageTmp->x400pSpanTable_old = x400pSpanTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->x400pSpanTable_rsvs++;
		StorageTmp->x400pSpanNumber = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->x400pSpanTable_tsts == 0)
				if ((ret = check_x400pSpanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pSpanTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->x400pSpanNumber for you to use, and you have just been asked to do something with it.  Note that anything done here must 
				   be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->x400pSpanTable_sets == 0)
				if ((ret = update_x400pSpanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pSpanTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			x400pSpanTable_destroy(&StorageTmp->x400pSpanTable_old);
			StorageTmp->x400pSpanTable_rsvs = 0;
			StorageTmp->x400pSpanTable_tsts = 0;
			StorageTmp->x400pSpanTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->x400pSpanTable_sets == 0)
			revert_x400pSpanTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			break;
		StorageTmp->x400pSpanNumber = StorageOld->x400pSpanNumber;
		if (--StorageTmp->x400pSpanTable_rsvs == 0)
			x400pSpanTable_destroy(&StorageTmp->x400pSpanTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_x400pSpanMode(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_x400pSpanMode(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct x400pSpanTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("x400pMxMIB", "write_x400pSpanMode entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(x400pSpanTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->x400pSpanRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanMode: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if ((var_val_type != ASN_BIT_STR && var_val_type != ASN_OCTET_STR)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanMode not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_type == ASN_BIT_STR) {
			if (1 > var_val_len || var_val_len > SPRINT_MAX_LEN || var_val_len != 2) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanMode: bad length\n");
				return SNMP_ERR_WRONGLENGTH;
			}
		}
		if (var_val_type == ASN_OCTET_STR) {
			if (var_val_len > SPRINT_MAX_LEN || var_val_len != 1) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanMode: bad length\n");
				return SNMP_ERR_WRONGLENGTH;
			}
		}
		/* Note: default value { } */
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			if (StorageTmp->x400pSpanTable_rsvs == 0)
				if ((StorageOld = StorageTmp->x400pSpanTable_old = x400pSpanTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->x400pSpanTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->x400pSpanMode);
		StorageTmp->x400pSpanMode = string;
		StorageTmp->x400pSpanModeLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->x400pSpanTable_tsts == 0)
				if ((ret = check_x400pSpanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pSpanTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->x400pSpanMode for you to use, and you have just been asked to do something with it.  Note that anything done here must
				   be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->x400pSpanTable_sets == 0)
				if ((ret = update_x400pSpanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pSpanTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			x400pSpanTable_destroy(&StorageTmp->x400pSpanTable_old);
			StorageTmp->x400pSpanTable_rsvs = 0;
			StorageTmp->x400pSpanTable_tsts = 0;
			StorageTmp->x400pSpanTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->x400pSpanTable_sets == 0)
			revert_x400pSpanTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			break;
		if (StorageOld->x400pSpanMode != NULL) {
			SNMP_FREE(StorageTmp->x400pSpanMode);
			StorageTmp->x400pSpanMode = StorageOld->x400pSpanMode;
			StorageTmp->x400pSpanModeLen = StorageOld->x400pSpanModeLen;
			StorageOld->x400pSpanMode = NULL;
			StorageOld->x400pSpanModeLen = 0;
		}
		if (--StorageTmp->x400pSpanTable_rsvs == 0)
			x400pSpanTable_destroy(&StorageTmp->x400pSpanTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_x400pSpanCrc(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_x400pSpanCrc(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct x400pSpanTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("x400pMxMIB", "write_x400pSpanCrc entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(x400pSpanTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->x400pSpanRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanCrc: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanCrc not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanCrc: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value none */
		switch (set_value) {
		case X400PSPANCRC_NONE:
		case X400PSPANCRC_CRC4:
		case X400PSPANCRC_CRC5:
		case X400PSPANCRC_CRC6:
		case X400PSPANCRC_CRC6J:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanCrc: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			if (StorageTmp->x400pSpanTable_rsvs == 0)
				if ((StorageOld = StorageTmp->x400pSpanTable_old = x400pSpanTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->x400pSpanTable_rsvs++;
		StorageTmp->x400pSpanCrc = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->x400pSpanTable_tsts == 0)
				if ((ret = check_x400pSpanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pSpanTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->x400pSpanCrc for you to use, and you have just been asked to do something with it.  Note that anything done here must be
				   reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->x400pSpanTable_sets == 0)
				if ((ret = update_x400pSpanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pSpanTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			x400pSpanTable_destroy(&StorageTmp->x400pSpanTable_old);
			StorageTmp->x400pSpanTable_rsvs = 0;
			StorageTmp->x400pSpanTable_tsts = 0;
			StorageTmp->x400pSpanTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->x400pSpanTable_sets == 0)
			revert_x400pSpanTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			break;
		StorageTmp->x400pSpanCrc = StorageOld->x400pSpanCrc;
		if (--StorageTmp->x400pSpanTable_rsvs == 0)
			x400pSpanTable_destroy(&StorageTmp->x400pSpanTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_x400pSpanClocking(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_x400pSpanClocking(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct x400pSpanTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("x400pMxMIB", "write_x400pSpanClocking entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(x400pSpanTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->x400pSpanRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanClocking: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanClocking not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanClocking: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case X400PSPANCLOCKING_NONE:
		case X400PSPANCLOCKING_LOOP:
		case X400PSPANCLOCKING_LOCAL:
		case X400PSPANCLOCKING_INTERNAL:
		case X400PSPANCLOCKING_EXTERNAL:
		case X400PSPANCLOCKING_ADAPTIVE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanClocking: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			if (StorageTmp->x400pSpanTable_rsvs == 0)
				if ((StorageOld = StorageTmp->x400pSpanTable_old = x400pSpanTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->x400pSpanTable_rsvs++;
		StorageTmp->x400pSpanClocking = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->x400pSpanTable_tsts == 0)
				if ((ret = check_x400pSpanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pSpanTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->x400pSpanClocking for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->x400pSpanTable_sets == 0)
				if ((ret = update_x400pSpanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pSpanTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			x400pSpanTable_destroy(&StorageTmp->x400pSpanTable_old);
			StorageTmp->x400pSpanTable_rsvs = 0;
			StorageTmp->x400pSpanTable_tsts = 0;
			StorageTmp->x400pSpanTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->x400pSpanTable_sets == 0)
			revert_x400pSpanTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			break;
		StorageTmp->x400pSpanClocking = StorageOld->x400pSpanClocking;
		if (--StorageTmp->x400pSpanTable_rsvs == 0)
			x400pSpanTable_destroy(&StorageTmp->x400pSpanTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_x400pSpanPriority(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_x400pSpanPriority(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct x400pSpanTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("x400pMxMIB", "write_x400pSpanPriority entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(x400pSpanTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->x400pSpanRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanPriority: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanPriority not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanPriority: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			if (StorageTmp->x400pSpanTable_rsvs == 0)
				if ((StorageOld = StorageTmp->x400pSpanTable_old = x400pSpanTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->x400pSpanTable_rsvs++;
		StorageTmp->x400pSpanPriority = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->x400pSpanTable_tsts == 0)
				if ((ret = check_x400pSpanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pSpanTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->x400pSpanPriority for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->x400pSpanTable_sets == 0)
				if ((ret = update_x400pSpanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pSpanTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			x400pSpanTable_destroy(&StorageTmp->x400pSpanTable_old);
			StorageTmp->x400pSpanTable_rsvs = 0;
			StorageTmp->x400pSpanTable_tsts = 0;
			StorageTmp->x400pSpanTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->x400pSpanTable_sets == 0)
			revert_x400pSpanTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			break;
		StorageTmp->x400pSpanPriority = StorageOld->x400pSpanPriority;
		if (--StorageTmp->x400pSpanTable_rsvs == 0)
			x400pSpanTable_destroy(&StorageTmp->x400pSpanTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_x400pSpanCoding(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_x400pSpanCoding(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct x400pSpanTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("x400pMxMIB", "write_x400pSpanCoding entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(x400pSpanTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->x400pSpanRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanCoding: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanCoding not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanCoding: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case X400PSPANCODING_NONE:
		case X400PSPANCODING_AMI:
		case X400PSPANCODING_B6ZS:
		case X400PSPANCODING_B8ZS:
		case X400PSPANCODING_HDB3:
		case X400PSPANCODING_JBZS:
		case X400PSPANCODING_ZBTSI:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanCoding: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			if (StorageTmp->x400pSpanTable_rsvs == 0)
				if ((StorageOld = StorageTmp->x400pSpanTable_old = x400pSpanTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->x400pSpanTable_rsvs++;
		StorageTmp->x400pSpanCoding = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->x400pSpanTable_tsts == 0)
				if ((ret = check_x400pSpanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pSpanTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->x400pSpanCoding for you to use, and you have just been asked to do something with it.  Note that anything done here must 
				   be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->x400pSpanTable_sets == 0)
				if ((ret = update_x400pSpanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pSpanTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			x400pSpanTable_destroy(&StorageTmp->x400pSpanTable_old);
			StorageTmp->x400pSpanTable_rsvs = 0;
			StorageTmp->x400pSpanTable_tsts = 0;
			StorageTmp->x400pSpanTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->x400pSpanTable_sets == 0)
			revert_x400pSpanTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			break;
		StorageTmp->x400pSpanCoding = StorageOld->x400pSpanCoding;
		if (--StorageTmp->x400pSpanTable_rsvs == 0)
			x400pSpanTable_destroy(&StorageTmp->x400pSpanTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_x400pSpanFraming(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_x400pSpanFraming(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct x400pSpanTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("x400pMxMIB", "write_x400pSpanFraming entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(x400pSpanTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->x400pSpanRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanFraming: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanFraming not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanFraming: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case X400PSPANFRAMING_NONE:
		case X400PSPANFRAMING_CCS:
		case X400PSPANFRAMING_CAS:
		case X400PSPANFRAMING_SF:
		case X400PSPANFRAMING_D4:
		case X400PSPANFRAMING_ESF:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanFraming: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			if (StorageTmp->x400pSpanTable_rsvs == 0)
				if ((StorageOld = StorageTmp->x400pSpanTable_old = x400pSpanTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->x400pSpanTable_rsvs++;
		StorageTmp->x400pSpanFraming = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->x400pSpanTable_tsts == 0)
				if ((ret = check_x400pSpanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pSpanTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->x400pSpanFraming for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->x400pSpanTable_sets == 0)
				if ((ret = update_x400pSpanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pSpanTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			x400pSpanTable_destroy(&StorageTmp->x400pSpanTable_old);
			StorageTmp->x400pSpanTable_rsvs = 0;
			StorageTmp->x400pSpanTable_tsts = 0;
			StorageTmp->x400pSpanTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->x400pSpanTable_sets == 0)
			revert_x400pSpanTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			break;
		StorageTmp->x400pSpanFraming = StorageOld->x400pSpanFraming;
		if (--StorageTmp->x400pSpanTable_rsvs == 0)
			x400pSpanTable_destroy(&StorageTmp->x400pSpanTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_x400pSpanLineImpedance(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_x400pSpanLineImpedance(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct x400pSpanTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("x400pMxMIB", "write_x400pSpanLineImpedance entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(x400pSpanTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->x400pSpanRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanLineImpedance: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanLineImpedance not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanLineImpedance: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case X400PSPANLINEIMPEDANCE_NONE:
		case X400PSPANLINEIMPEDANCE_UNBALANCED75OHM:
		case X400PSPANLINEIMPEDANCE_BALANCED100OHM:
		case X400PSPANLINEIMPEDANCE_BALANCED120OHM:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanLineImpedance: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			if (StorageTmp->x400pSpanTable_rsvs == 0)
				if ((StorageOld = StorageTmp->x400pSpanTable_old = x400pSpanTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->x400pSpanTable_rsvs++;
		StorageTmp->x400pSpanLineImpedance = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->x400pSpanTable_tsts == 0)
				if ((ret = check_x400pSpanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pSpanTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->x400pSpanLineImpedance for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->x400pSpanTable_sets == 0)
				if ((ret = update_x400pSpanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pSpanTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			x400pSpanTable_destroy(&StorageTmp->x400pSpanTable_old);
			StorageTmp->x400pSpanTable_rsvs = 0;
			StorageTmp->x400pSpanTable_tsts = 0;
			StorageTmp->x400pSpanTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->x400pSpanTable_sets == 0)
			revert_x400pSpanTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			break;
		StorageTmp->x400pSpanLineImpedance = StorageOld->x400pSpanLineImpedance;
		if (--StorageTmp->x400pSpanTable_rsvs == 0)
			x400pSpanTable_destroy(&StorageTmp->x400pSpanTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_x400pSpanLineMode(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_x400pSpanLineMode(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct x400pSpanTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("x400pMxMIB", "write_x400pSpanLineMode entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(x400pSpanTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->x400pSpanRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanLineMode: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanLineMode not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanLineMode: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value none */
		switch (set_value) {
		case X400PSPANLINEMODE_NONE:
		case X400PSPANLINEMODE_DSU:
		case X400PSPANLINEMODE_CSU:
		case X400PSPANLINEMODE_MONITOR:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanLineMode: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			if (StorageTmp->x400pSpanTable_rsvs == 0)
				if ((StorageOld = StorageTmp->x400pSpanTable_old = x400pSpanTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->x400pSpanTable_rsvs++;
		StorageTmp->x400pSpanLineMode = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->x400pSpanTable_tsts == 0)
				if ((ret = check_x400pSpanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pSpanTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->x400pSpanLineMode for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->x400pSpanTable_sets == 0)
				if ((ret = update_x400pSpanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pSpanTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			x400pSpanTable_destroy(&StorageTmp->x400pSpanTable_old);
			StorageTmp->x400pSpanTable_rsvs = 0;
			StorageTmp->x400pSpanTable_tsts = 0;
			StorageTmp->x400pSpanTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->x400pSpanTable_sets == 0)
			revert_x400pSpanTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			break;
		StorageTmp->x400pSpanLineMode = StorageOld->x400pSpanLineMode;
		if (--StorageTmp->x400pSpanTable_rsvs == 0)
			x400pSpanTable_destroy(&StorageTmp->x400pSpanTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_x400pSpanLineLength(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_x400pSpanLineLength(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct x400pSpanTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("x400pMxMIB", "write_x400pSpanLineLength entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(x400pSpanTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->x400pSpanRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanLineLength: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanLineLength not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanLineLength: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value dsx133ft */
		switch (set_value) {
		case X400PSPANLINELENGTH_NONE:
		case X400PSPANLINELENGTH_DSX133FT:
		case X400PSPANLINELENGTH_DSX266FT:
		case X400PSPANLINELENGTH_DSX399FT:
		case X400PSPANLINELENGTH_DSX533FT:
		case X400PSPANLINELENGTH_DSX666FT:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanLineLength: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			if (StorageTmp->x400pSpanTable_rsvs == 0)
				if ((StorageOld = StorageTmp->x400pSpanTable_old = x400pSpanTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->x400pSpanTable_rsvs++;
		StorageTmp->x400pSpanLineLength = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->x400pSpanTable_tsts == 0)
				if ((ret = check_x400pSpanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pSpanTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->x400pSpanLineLength for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->x400pSpanTable_sets == 0)
				if ((ret = update_x400pSpanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pSpanTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			x400pSpanTable_destroy(&StorageTmp->x400pSpanTable_old);
			StorageTmp->x400pSpanTable_rsvs = 0;
			StorageTmp->x400pSpanTable_tsts = 0;
			StorageTmp->x400pSpanTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->x400pSpanTable_sets == 0)
			revert_x400pSpanTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			break;
		StorageTmp->x400pSpanLineLength = StorageOld->x400pSpanLineLength;
		if (--StorageTmp->x400pSpanTable_rsvs == 0)
			x400pSpanTable_destroy(&StorageTmp->x400pSpanTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_x400pSpanLineAttenuation(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_x400pSpanLineAttenuation(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct x400pSpanTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("x400pMxMIB", "write_x400pSpanLineAttenuation entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(x400pSpanTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->x400pSpanRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanLineAttenuation: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanLineAttenuation not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanLineAttenuation: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value none */
		switch (set_value) {
		case X400PSPANLINEATTENUATION_NONE:
		case X400PSPANLINEATTENUATION_CSU0DB:
		case X400PSPANLINEATTENUATION_CSU8DB:
		case X400PSPANLINEATTENUATION_CSU15DB:
		case X400PSPANLINEATTENUATION_CSU23DB:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanLineAttenuation: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			if (StorageTmp->x400pSpanTable_rsvs == 0)
				if ((StorageOld = StorageTmp->x400pSpanTable_old = x400pSpanTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->x400pSpanTable_rsvs++;
		StorageTmp->x400pSpanLineAttenuation = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->x400pSpanTable_tsts == 0)
				if ((ret = check_x400pSpanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pSpanTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->x400pSpanLineAttenuation for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->x400pSpanTable_sets == 0)
				if ((ret = update_x400pSpanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pSpanTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			x400pSpanTable_destroy(&StorageTmp->x400pSpanTable_old);
			StorageTmp->x400pSpanTable_rsvs = 0;
			StorageTmp->x400pSpanTable_tsts = 0;
			StorageTmp->x400pSpanTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->x400pSpanTable_sets == 0)
			revert_x400pSpanTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			break;
		StorageTmp->x400pSpanLineAttenuation = StorageOld->x400pSpanLineAttenuation;
		if (--StorageTmp->x400pSpanTable_rsvs == 0)
			x400pSpanTable_destroy(&StorageTmp->x400pSpanTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_x400pSpanLineGain(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_x400pSpanLineGain(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct x400pSpanTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("x400pMxMIB", "write_x400pSpanLineGain entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(x400pSpanTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->x400pSpanRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanLineGain: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanLineGain not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanLineGain: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value none */
		switch (set_value) {
		case X400PSPANLINEGAIN_NONE:
		case X400PSPANLINEGAIN_MON0DB:
		case X400PSPANLINEGAIN_MON12DB:
		case X400PSPANLINEGAIN_MON20DB:
		case X400PSPANLINEGAIN_MON26DB:
		case X400PSPANLINEGAIN_MON30DB:
		case X400PSPANLINEGAIN_MON32DB:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanLineGain: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			if (StorageTmp->x400pSpanTable_rsvs == 0)
				if ((StorageOld = StorageTmp->x400pSpanTable_old = x400pSpanTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->x400pSpanTable_rsvs++;
		StorageTmp->x400pSpanLineGain = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->x400pSpanTable_tsts == 0)
				if ((ret = check_x400pSpanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pSpanTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->x400pSpanLineGain for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->x400pSpanTable_sets == 0)
				if ((ret = update_x400pSpanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pSpanTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			x400pSpanTable_destroy(&StorageTmp->x400pSpanTable_old);
			StorageTmp->x400pSpanTable_rsvs = 0;
			StorageTmp->x400pSpanTable_tsts = 0;
			StorageTmp->x400pSpanTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->x400pSpanTable_sets == 0)
			revert_x400pSpanTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			break;
		StorageTmp->x400pSpanLineGain = StorageOld->x400pSpanLineGain;
		if (--StorageTmp->x400pSpanTable_rsvs == 0)
			x400pSpanTable_destroy(&StorageTmp->x400pSpanTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_x400pSpanLineDelay(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_x400pSpanLineDelay(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct x400pSpanTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("x400pMxMIB", "write_x400pSpanLineDelay entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(x400pSpanTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->x400pSpanRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanLineDelay: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanLineDelay not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanLineDelay: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 0 */
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			if (StorageTmp->x400pSpanTable_rsvs == 0)
				if ((StorageOld = StorageTmp->x400pSpanTable_old = x400pSpanTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->x400pSpanTable_rsvs++;
		StorageTmp->x400pSpanLineDelay = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->x400pSpanTable_tsts == 0)
				if ((ret = check_x400pSpanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pSpanTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->x400pSpanLineDelay for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->x400pSpanTable_sets == 0)
				if ((ret = update_x400pSpanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pSpanTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			x400pSpanTable_destroy(&StorageTmp->x400pSpanTable_old);
			StorageTmp->x400pSpanTable_rsvs = 0;
			StorageTmp->x400pSpanTable_tsts = 0;
			StorageTmp->x400pSpanTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->x400pSpanTable_sets == 0)
			revert_x400pSpanTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			break;
		StorageTmp->x400pSpanLineDelay = StorageOld->x400pSpanLineDelay;
		if (--StorageTmp->x400pSpanTable_rsvs == 0)
			x400pSpanTable_destroy(&StorageTmp->x400pSpanTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_x400pSpanTxLevel(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_x400pSpanTxLevel(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct x400pSpanTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("x400pMxMIB", "write_x400pSpanTxLevel entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(x400pSpanTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->x400pSpanRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanTxLevel: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanTxLevel not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanTxLevel: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value open */
		switch (set_value) {
		case X400PSPANTXLEVEL_OPEN:
		case X400PSPANTXLEVEL_OFF:
		case X400PSPANTXLEVEL_ON:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanTxLevel: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			if (StorageTmp->x400pSpanTable_rsvs == 0)
				if ((StorageOld = StorageTmp->x400pSpanTable_old = x400pSpanTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->x400pSpanTable_rsvs++;
		StorageTmp->x400pSpanTxLevel = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->x400pSpanTable_tsts == 0)
				if ((ret = check_x400pSpanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pSpanTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->x400pSpanTxLevel for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->x400pSpanTable_sets == 0)
				if ((ret = update_x400pSpanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pSpanTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			x400pSpanTable_destroy(&StorageTmp->x400pSpanTable_old);
			StorageTmp->x400pSpanTable_rsvs = 0;
			StorageTmp->x400pSpanTable_tsts = 0;
			StorageTmp->x400pSpanTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->x400pSpanTable_sets == 0)
			revert_x400pSpanTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			break;
		StorageTmp->x400pSpanTxLevel = StorageOld->x400pSpanTxLevel;
		if (--StorageTmp->x400pSpanTable_rsvs == 0)
			x400pSpanTable_destroy(&StorageTmp->x400pSpanTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_x400pSpanRxLevel(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_x400pSpanRxLevel(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct x400pSpanTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("x400pMxMIB", "write_x400pSpanRxLevel entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(x400pSpanTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->x400pSpanRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanRxLevel: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanRxLevel not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanRxLevel: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value open */
		switch (set_value) {
		case X400PSPANRXLEVEL_OPEN:
		case X400PSPANRXLEVEL_OFF:
		case X400PSPANRXLEVEL_ON:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanRxLevel: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			if (StorageTmp->x400pSpanTable_rsvs == 0)
				if ((StorageOld = StorageTmp->x400pSpanTable_old = x400pSpanTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->x400pSpanTable_rsvs++;
		StorageTmp->x400pSpanRxLevel = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->x400pSpanTable_tsts == 0)
				if ((ret = check_x400pSpanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pSpanTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->x400pSpanRxLevel for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->x400pSpanTable_sets == 0)
				if ((ret = update_x400pSpanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pSpanTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			x400pSpanTable_destroy(&StorageTmp->x400pSpanTable_old);
			StorageTmp->x400pSpanTable_rsvs = 0;
			StorageTmp->x400pSpanTable_tsts = 0;
			StorageTmp->x400pSpanTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->x400pSpanTable_sets == 0)
			revert_x400pSpanTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			break;
		StorageTmp->x400pSpanRxLevel = StorageOld->x400pSpanRxLevel;
		if (--StorageTmp->x400pSpanTable_rsvs == 0)
			x400pSpanTable_destroy(&StorageTmp->x400pSpanTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_x400pSpanAlarmSettleTime(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_x400pSpanAlarmSettleTime(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct x400pSpanTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("x400pMxMIB", "write_x400pSpanAlarmSettleTime entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(x400pSpanTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->x400pSpanRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanAlarmSettleTime: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanAlarmSettleTime not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanAlarmSettleTime: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 500 */
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanAlarmSettleTime: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			if (StorageTmp->x400pSpanTable_rsvs == 0)
				if ((StorageOld = StorageTmp->x400pSpanTable_old = x400pSpanTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->x400pSpanTable_rsvs++;
		StorageTmp->x400pSpanAlarmSettleTime = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->x400pSpanTable_tsts == 0)
				if ((ret = check_x400pSpanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pSpanTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->x400pSpanAlarmSettleTime for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->x400pSpanTable_sets == 0)
				if ((ret = update_x400pSpanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pSpanTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			x400pSpanTable_destroy(&StorageTmp->x400pSpanTable_old);
			StorageTmp->x400pSpanTable_rsvs = 0;
			StorageTmp->x400pSpanTable_tsts = 0;
			StorageTmp->x400pSpanTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->x400pSpanTable_sets == 0)
			revert_x400pSpanTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			break;
		StorageTmp->x400pSpanAlarmSettleTime = StorageOld->x400pSpanAlarmSettleTime;
		if (--StorageTmp->x400pSpanTable_rsvs == 0)
			x400pSpanTable_destroy(&StorageTmp->x400pSpanTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_x400pSpanLineCodeTime(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_x400pSpanLineCodeTime(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct x400pSpanTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("x400pMxMIB", "write_x400pSpanLineCodeTime entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(x400pSpanTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->x400pSpanRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanLineCodeTime: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanLineCodeTime not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanLineCodeTime: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 500 */
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanLineCodeTime: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			if (StorageTmp->x400pSpanTable_rsvs == 0)
				if ((StorageOld = StorageTmp->x400pSpanTable_old = x400pSpanTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->x400pSpanTable_rsvs++;
		StorageTmp->x400pSpanLineCodeTime = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->x400pSpanTable_tsts == 0)
				if ((ret = check_x400pSpanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pSpanTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->x400pSpanLineCodeTime for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->x400pSpanTable_sets == 0)
				if ((ret = update_x400pSpanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pSpanTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			x400pSpanTable_destroy(&StorageTmp->x400pSpanTable_old);
			StorageTmp->x400pSpanTable_rsvs = 0;
			StorageTmp->x400pSpanTable_tsts = 0;
			StorageTmp->x400pSpanTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->x400pSpanTable_sets == 0)
			revert_x400pSpanTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			break;
		StorageTmp->x400pSpanLineCodeTime = StorageOld->x400pSpanLineCodeTime;
		if (--StorageTmp->x400pSpanTable_rsvs == 0)
			x400pSpanTable_destroy(&StorageTmp->x400pSpanTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_x400pSpanPrimary(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_x400pSpanPrimary(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct x400pSpanTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("x400pMxMIB", "write_x400pSpanPrimary entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(x400pSpanTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->x400pSpanRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanPrimary: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanPrimary not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanPrimary: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			if (StorageTmp->x400pSpanTable_rsvs == 0)
				if ((StorageOld = StorageTmp->x400pSpanTable_old = x400pSpanTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->x400pSpanTable_rsvs++;
		StorageTmp->x400pSpanPrimary = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->x400pSpanTable_tsts == 0)
				if ((ret = check_x400pSpanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pSpanTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->x400pSpanPrimary for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->x400pSpanTable_sets == 0)
				if ((ret = update_x400pSpanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pSpanTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			x400pSpanTable_destroy(&StorageTmp->x400pSpanTable_old);
			StorageTmp->x400pSpanTable_rsvs = 0;
			StorageTmp->x400pSpanTable_tsts = 0;
			StorageTmp->x400pSpanTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->x400pSpanTable_sets == 0)
			revert_x400pSpanTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			break;
		StorageTmp->x400pSpanPrimary = StorageOld->x400pSpanPrimary;
		if (--StorageTmp->x400pSpanTable_rsvs == 0)
			x400pSpanTable_destroy(&StorageTmp->x400pSpanTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_x400pSpanDataLink(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_x400pSpanDataLink(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct x400pSpanTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("x400pMxMIB", "write_x400pSpanDataLink entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(x400pSpanTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->x400pSpanRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanDataLink: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if ((var_val_type != ASN_BIT_STR && var_val_type != ASN_OCTET_STR)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanDataLink not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_type == ASN_BIT_STR) {
			if (1 > var_val_len || var_val_len > SPRINT_MAX_LEN || var_val_len != 2) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanDataLink: bad length\n");
				return SNMP_ERR_WRONGLENGTH;
			}
		}
		if (var_val_type == ASN_OCTET_STR) {
			if (var_val_len > SPRINT_MAX_LEN || var_val_len != 1) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanDataLink: bad length\n");
				return SNMP_ERR_WRONGLENGTH;
			}
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			if (StorageTmp->x400pSpanTable_rsvs == 0)
				if ((StorageOld = StorageTmp->x400pSpanTable_old = x400pSpanTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->x400pSpanTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->x400pSpanDataLink);
		StorageTmp->x400pSpanDataLink = string;
		StorageTmp->x400pSpanDataLinkLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->x400pSpanTable_tsts == 0)
				if ((ret = check_x400pSpanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pSpanTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->x400pSpanDataLink for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->x400pSpanTable_sets == 0)
				if ((ret = update_x400pSpanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pSpanTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			x400pSpanTable_destroy(&StorageTmp->x400pSpanTable_old);
			StorageTmp->x400pSpanTable_rsvs = 0;
			StorageTmp->x400pSpanTable_tsts = 0;
			StorageTmp->x400pSpanTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->x400pSpanTable_sets == 0)
			revert_x400pSpanTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			break;
		if (StorageOld->x400pSpanDataLink != NULL) {
			SNMP_FREE(StorageTmp->x400pSpanDataLink);
			StorageTmp->x400pSpanDataLink = StorageOld->x400pSpanDataLink;
			StorageTmp->x400pSpanDataLinkLen = StorageOld->x400pSpanDataLinkLen;
			StorageOld->x400pSpanDataLink = NULL;
			StorageOld->x400pSpanDataLinkLen = 0;
		}
		if (--StorageTmp->x400pSpanTable_rsvs == 0)
			x400pSpanTable_destroy(&StorageTmp->x400pSpanTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_x400pSpanLineCode(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_x400pSpanLineCode(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct x400pSpanTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("x400pMxMIB", "write_x400pSpanLineCode entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(x400pSpanTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->x400pSpanRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanLineCode: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanLineCode not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanLineCode: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value noCode */
		switch (set_value) {
		case X400PSPANLINECODE_NOCODE:
		case X400PSPANLINECODE_LINECODE:
		case X400PSPANLINECODE_PAYLOADCODE:
		case X400PSPANLINECODE_RESETCODE:
		case X400PSPANLINECODE_TESTCODE:
		case X400PSPANLINECODE_UNFRAMEDALLONES:
		case X400PSPANLINECODE_UNFRAMEDONEANDZERO:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanLineCode: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			if (StorageTmp->x400pSpanTable_rsvs == 0)
				if ((StorageOld = StorageTmp->x400pSpanTable_old = x400pSpanTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->x400pSpanTable_rsvs++;
		StorageTmp->x400pSpanLineCode = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->x400pSpanTable_tsts == 0)
				if ((ret = check_x400pSpanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pSpanTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->x400pSpanLineCode for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->x400pSpanTable_sets == 0)
				if ((ret = update_x400pSpanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pSpanTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			x400pSpanTable_destroy(&StorageTmp->x400pSpanTable_old);
			StorageTmp->x400pSpanTable_rsvs = 0;
			StorageTmp->x400pSpanTable_tsts = 0;
			StorageTmp->x400pSpanTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->x400pSpanTable_sets == 0)
			revert_x400pSpanTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			break;
		StorageTmp->x400pSpanLineCode = StorageOld->x400pSpanLineCode;
		if (--StorageTmp->x400pSpanTable_rsvs == 0)
			x400pSpanTable_destroy(&StorageTmp->x400pSpanTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_x400pSpanAlarmSeverityMapProfile(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_x400pSpanAlarmSeverityMapProfile(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct x400pSpanTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	oid *objid = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("x400pMxMIB", "write_x400pSpanAlarmSeverityMapProfile entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(x400pSpanTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->x400pSpanRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanAlarmSeverityMapProfile: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OBJECT_ID) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanAlarmSeverityMapProfile not ASN_OBJECT_ID\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (MIN_OID_LEN * sizeof(oid) > var_val_len || var_val_len > MAX_OID_LEN * sizeof(oid)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanAlarmSeverityMapProfile: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value zeroDotZero */
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			if (StorageTmp->x400pSpanTable_rsvs == 0)
				if ((StorageOld = StorageTmp->x400pSpanTable_old = x400pSpanTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->x400pSpanTable_rsvs++;
		if ((objid = snmp_duplicate_objid((void *) var_val, var_val_len / sizeof(oid))) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		SNMP_FREE(StorageTmp->x400pSpanAlarmSeverityMapProfile);
		StorageTmp->x400pSpanAlarmSeverityMapProfile = objid;
		StorageTmp->x400pSpanAlarmSeverityMapProfileLen = var_val_len / sizeof(oid);
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->x400pSpanTable_tsts == 0)
				if ((ret = check_x400pSpanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pSpanTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->x400pSpanAlarmSeverityMapProfile for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->x400pSpanTable_sets == 0)
				if ((ret = update_x400pSpanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pSpanTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			x400pSpanTable_destroy(&StorageTmp->x400pSpanTable_old);
			StorageTmp->x400pSpanTable_rsvs = 0;
			StorageTmp->x400pSpanTable_tsts = 0;
			StorageTmp->x400pSpanTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->x400pSpanTable_sets == 0)
			revert_x400pSpanTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			break;
		if (StorageOld->x400pSpanAlarmSeverityMapProfile != NULL) {
			SNMP_FREE(StorageTmp->x400pSpanAlarmSeverityMapProfile);
			StorageTmp->x400pSpanAlarmSeverityMapProfile = StorageOld->x400pSpanAlarmSeverityMapProfile;
			StorageTmp->x400pSpanAlarmSeverityMapProfileLen = StorageOld->x400pSpanAlarmSeverityMapProfileLen;
			StorageOld->x400pSpanAlarmSeverityMapProfile = NULL;
			StorageOld->x400pSpanAlarmSeverityMapProfileLen = 0;
		}
		if (--StorageTmp->x400pSpanTable_rsvs == 0)
			x400pSpanTable_destroy(&StorageTmp->x400pSpanTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_x400pSpanAdministrativeState(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_x400pSpanAdministrativeState(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct x400pSpanTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("x400pMxMIB", "write_x400pSpanAdministrativeState entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(x400pSpanTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->x400pSpanRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanAdministrativeState: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanAdministrativeState not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanAdministrativeState: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value locked */
		switch (set_value) {
		case X400PSPANADMINISTRATIVESTATE_LOCKED:
		case X400PSPANADMINISTRATIVESTATE_UNLOCKED:
		case X400PSPANADMINISTRATIVESTATE_SHUTTINGDOWN:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanAdministrativeState: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			if (StorageTmp->x400pSpanTable_rsvs == 0)
				if ((StorageOld = StorageTmp->x400pSpanTable_old = x400pSpanTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->x400pSpanTable_rsvs++;
		StorageTmp->x400pSpanAdministrativeState = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->x400pSpanTable_tsts == 0)
				if ((ret = check_x400pSpanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pSpanTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->x400pSpanAdministrativeState for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->x400pSpanTable_sets == 0)
				if ((ret = update_x400pSpanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pSpanTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			x400pSpanTable_destroy(&StorageTmp->x400pSpanTable_old);
			StorageTmp->x400pSpanTable_rsvs = 0;
			StorageTmp->x400pSpanTable_tsts = 0;
			StorageTmp->x400pSpanTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->x400pSpanTable_sets == 0)
			revert_x400pSpanTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			break;
		StorageTmp->x400pSpanAdministrativeState = StorageOld->x400pSpanAdministrativeState;
		if (--StorageTmp->x400pSpanTable_rsvs == 0)
			x400pSpanTable_destroy(&StorageTmp->x400pSpanTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_x400pSpanAlarmStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_x400pSpanAlarmStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct x400pSpanTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("x400pMxMIB", "write_x400pSpanAlarmStatus entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(x400pSpanTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->x400pSpanRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanAlarmStatus: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if ((var_val_type != ASN_BIT_STR && var_val_type != ASN_OCTET_STR)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanAlarmStatus not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_type == ASN_BIT_STR) {
			if (1 > var_val_len || var_val_len > SPRINT_MAX_LEN || var_val_len != 2) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanAlarmStatus: bad length\n");
				return SNMP_ERR_WRONGLENGTH;
			}
		}
		if (var_val_type == ASN_OCTET_STR) {
			if (var_val_len > SPRINT_MAX_LEN || var_val_len != 1) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanAlarmStatus: bad length\n");
				return SNMP_ERR_WRONGLENGTH;
			}
		}
		/* Note: default value { } */
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			if (StorageTmp->x400pSpanTable_rsvs == 0)
				if ((StorageOld = StorageTmp->x400pSpanTable_old = x400pSpanTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->x400pSpanTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->x400pSpanAlarmStatus);
		StorageTmp->x400pSpanAlarmStatus = string;
		StorageTmp->x400pSpanAlarmStatusLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->x400pSpanTable_tsts == 0)
				if ((ret = check_x400pSpanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pSpanTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->x400pSpanAlarmStatus for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->x400pSpanTable_sets == 0)
				if ((ret = update_x400pSpanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pSpanTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			x400pSpanTable_destroy(&StorageTmp->x400pSpanTable_old);
			StorageTmp->x400pSpanTable_rsvs = 0;
			StorageTmp->x400pSpanTable_tsts = 0;
			StorageTmp->x400pSpanTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->x400pSpanTable_sets == 0)
			revert_x400pSpanTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			break;
		if (StorageOld->x400pSpanAlarmStatus != NULL) {
			SNMP_FREE(StorageTmp->x400pSpanAlarmStatus);
			StorageTmp->x400pSpanAlarmStatus = StorageOld->x400pSpanAlarmStatus;
			StorageTmp->x400pSpanAlarmStatusLen = StorageOld->x400pSpanAlarmStatusLen;
			StorageOld->x400pSpanAlarmStatus = NULL;
			StorageOld->x400pSpanAlarmStatusLen = 0;
		}
		if (--StorageTmp->x400pSpanTable_rsvs == 0)
			x400pSpanTable_destroy(&StorageTmp->x400pSpanTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_x400pSpanControlStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_x400pSpanControlStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct x400pSpanTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("x400pMxMIB", "write_x400pSpanControlStatus entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(x400pSpanTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->x400pSpanRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanControlStatus: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if ((var_val_type != ASN_BIT_STR && var_val_type != ASN_OCTET_STR)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanControlStatus not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_type == ASN_BIT_STR) {
			if (1 > var_val_len || var_val_len > SPRINT_MAX_LEN || var_val_len != 2) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanControlStatus: bad length\n");
				return SNMP_ERR_WRONGLENGTH;
			}
		}
		if (var_val_type == ASN_OCTET_STR) {
			if (var_val_len > SPRINT_MAX_LEN || var_val_len != 1) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanControlStatus: bad length\n");
				return SNMP_ERR_WRONGLENGTH;
			}
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			if (StorageTmp->x400pSpanTable_rsvs == 0)
				if ((StorageOld = StorageTmp->x400pSpanTable_old = x400pSpanTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->x400pSpanTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->x400pSpanControlStatus);
		StorageTmp->x400pSpanControlStatus = string;
		StorageTmp->x400pSpanControlStatusLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->x400pSpanTable_tsts == 0)
				if ((ret = check_x400pSpanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pSpanTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->x400pSpanControlStatus for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->x400pSpanTable_sets == 0)
				if ((ret = update_x400pSpanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pSpanTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			x400pSpanTable_destroy(&StorageTmp->x400pSpanTable_old);
			StorageTmp->x400pSpanTable_rsvs = 0;
			StorageTmp->x400pSpanTable_tsts = 0;
			StorageTmp->x400pSpanTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->x400pSpanTable_sets == 0)
			revert_x400pSpanTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			break;
		if (StorageOld->x400pSpanControlStatus != NULL) {
			SNMP_FREE(StorageTmp->x400pSpanControlStatus);
			StorageTmp->x400pSpanControlStatus = StorageOld->x400pSpanControlStatus;
			StorageTmp->x400pSpanControlStatusLen = StorageOld->x400pSpanControlStatusLen;
			StorageOld->x400pSpanControlStatus = NULL;
			StorageOld->x400pSpanControlStatusLen = 0;
		}
		if (--StorageTmp->x400pSpanTable_rsvs == 0)
			x400pSpanTable_destroy(&StorageTmp->x400pSpanTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_x400pSpanReceiveThreshold(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_x400pSpanReceiveThreshold(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct x400pSpanTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("x400pMxMIB", "write_x400pSpanReceiveThreshold entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(x400pSpanTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->x400pSpanRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanReceiveThreshold: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanReceiveThreshold not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanReceiveThreshold: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..600 */
		if ((0 > set_value || set_value > 600)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanReceiveThreshold: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			if (StorageTmp->x400pSpanTable_rsvs == 0)
				if ((StorageOld = StorageTmp->x400pSpanTable_old = x400pSpanTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->x400pSpanTable_rsvs++;
		StorageTmp->x400pSpanReceiveThreshold = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->x400pSpanTable_tsts == 0)
				if ((ret = check_x400pSpanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pSpanTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->x400pSpanReceiveThreshold for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->x400pSpanTable_sets == 0)
				if ((ret = update_x400pSpanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pSpanTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
			x400pSpanTable_destroy(&StorageTmp->x400pSpanTable_old);
			StorageTmp->x400pSpanTable_rsvs = 0;
			StorageTmp->x400pSpanTable_tsts = 0;
			StorageTmp->x400pSpanTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->x400pSpanTable_sets == 0)
			revert_x400pSpanTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
			break;
		StorageTmp->x400pSpanReceiveThreshold = StorageOld->x400pSpanReceiveThreshold;
		if (--StorageTmp->x400pSpanTable_rsvs == 0)
			x400pSpanTable_destroy(&StorageTmp->x400pSpanTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_x400pBertMode(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_x400pBertMode(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct x400pBertTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("x400pMxMIB", "write_x400pBertMode entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(x400pBertTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pBertMode not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pBertMode: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value none */
		switch (set_value) {
		case X400PBERTMODE_NONE:
		case X400PBERTMODE_SPAN:
		case X400PBERTMODE_CHANNEL:
		case X400PBERTMODE_FBIT:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pBertMode: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->x400pBertTable_old) == NULL)
			if (StorageTmp->x400pBertTable_rsvs == 0)
				if ((StorageOld = StorageTmp->x400pBertTable_old = x400pBertTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->x400pBertTable_rsvs++;
		StorageTmp->x400pBertMode = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->x400pBertTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->x400pBertTable_tsts == 0)
				if ((ret = check_x400pBertTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pBertTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->x400pBertMode for you to use, and you have just been asked to do something with it.  Note that anything done here must
				   be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->x400pBertTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->x400pBertTable_sets == 0)
				if ((ret = update_x400pBertTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pBertTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->x400pBertTable_old) != NULL) {
			x400pBertTable_destroy(&StorageTmp->x400pBertTable_old);
			StorageTmp->x400pBertTable_rsvs = 0;
			StorageTmp->x400pBertTable_tsts = 0;
			StorageTmp->x400pBertTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->x400pBertTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->x400pBertTable_sets == 0)
			revert_x400pBertTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->x400pBertTable_old) == NULL)
			break;
		StorageTmp->x400pBertMode = StorageOld->x400pBertMode;
		if (--StorageTmp->x400pBertTable_rsvs == 0)
			x400pBertTable_destroy(&StorageTmp->x400pBertTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_x400pBertSelect(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_x400pBertSelect(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct x400pBertTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("x400pMxMIB", "write_x400pBertSelect entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(x400pBertTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pBertSelect not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pBertSelect: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value none */
		switch (set_value) {
		case X400PBERTSELECT_NONE:
		case X400PBERTSELECT_PRE2E7MINUS1:
		case X400PBERTSELECT_PRE2E9MINUS1:
		case X400PBERTSELECT_PRE2E11MINUS1:
		case X400PBERTSELECT_PRE2E15MINUS1:
		case X400PBERTSELECT_QRSS:
		case X400PBERTSELECT_DALY:
		case X400PBERTSELECT_REPEATING:
		case X400PBERTSELECT_ALTERNATING:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pBertSelect: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->x400pBertTable_old) == NULL)
			if (StorageTmp->x400pBertTable_rsvs == 0)
				if ((StorageOld = StorageTmp->x400pBertTable_old = x400pBertTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->x400pBertTable_rsvs++;
		StorageTmp->x400pBertSelect = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->x400pBertTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->x400pBertTable_tsts == 0)
				if ((ret = check_x400pBertTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pBertTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->x400pBertSelect for you to use, and you have just been asked to do something with it.  Note that anything done here must
				   be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->x400pBertTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->x400pBertTable_sets == 0)
				if ((ret = update_x400pBertTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pBertTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->x400pBertTable_old) != NULL) {
			x400pBertTable_destroy(&StorageTmp->x400pBertTable_old);
			StorageTmp->x400pBertTable_rsvs = 0;
			StorageTmp->x400pBertTable_tsts = 0;
			StorageTmp->x400pBertTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->x400pBertTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->x400pBertTable_sets == 0)
			revert_x400pBertTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->x400pBertTable_old) == NULL)
			break;
		StorageTmp->x400pBertSelect = StorageOld->x400pBertSelect;
		if (--StorageTmp->x400pBertTable_rsvs == 0)
			x400pBertTable_destroy(&StorageTmp->x400pBertTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_x400pBertPattern(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_x400pBertPattern(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct x400pBertTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("x400pMxMIB", "write_x400pBertPattern entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(x400pBertTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pBertPattern not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 0..33 */
		if (var_val_len > SPRINT_MAX_LEN || ((0 > var_val_len || var_val_len > 33))) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pBertPattern: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value \"\x10\xFF\xFF\xFF\xFF\" */
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->x400pBertTable_old) == NULL)
			if (StorageTmp->x400pBertTable_rsvs == 0)
				if ((StorageOld = StorageTmp->x400pBertTable_old = x400pBertTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->x400pBertTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->x400pBertPattern);
		StorageTmp->x400pBertPattern = string;
		StorageTmp->x400pBertPatternLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->x400pBertTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->x400pBertTable_tsts == 0)
				if ((ret = check_x400pBertTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pBertTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->x400pBertPattern for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->x400pBertTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->x400pBertTable_sets == 0)
				if ((ret = update_x400pBertTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pBertTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->x400pBertTable_old) != NULL) {
			x400pBertTable_destroy(&StorageTmp->x400pBertTable_old);
			StorageTmp->x400pBertTable_rsvs = 0;
			StorageTmp->x400pBertTable_tsts = 0;
			StorageTmp->x400pBertTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->x400pBertTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->x400pBertTable_sets == 0)
			revert_x400pBertTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->x400pBertTable_old) == NULL)
			break;
		if (StorageOld->x400pBertPattern != NULL) {
			SNMP_FREE(StorageTmp->x400pBertPattern);
			StorageTmp->x400pBertPattern = StorageOld->x400pBertPattern;
			StorageTmp->x400pBertPatternLen = StorageOld->x400pBertPatternLen;
			StorageOld->x400pBertPattern = NULL;
			StorageOld->x400pBertPatternLen = 0;
		}
		if (--StorageTmp->x400pBertTable_rsvs == 0)
			x400pBertTable_destroy(&StorageTmp->x400pBertTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_x400pChanType(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_x400pChanType(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct x400pChanTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("x400pMxMIB", "write_x400pChanType entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(x400pChanTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pChanType not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pChanType: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value ccs */
		switch (set_value) {
		case X400PCHANTYPE_NONE:
		case X400PCHANTYPE_CAS:
		case X400PCHANTYPE_CCS:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pChanType: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->x400pChanTable_old) == NULL)
			if (StorageTmp->x400pChanTable_rsvs == 0)
				if ((StorageOld = StorageTmp->x400pChanTable_old = x400pChanTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->x400pChanTable_rsvs++;
		StorageTmp->x400pChanType = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->x400pChanTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->x400pChanTable_tsts == 0)
				if ((ret = check_x400pChanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pChanTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->x400pChanType for you to use, and you have just been asked to do something with it.  Note that anything done here must
				   be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->x400pChanTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->x400pChanTable_sets == 0)
				if ((ret = update_x400pChanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pChanTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->x400pChanTable_old) != NULL) {
			x400pChanTable_destroy(&StorageTmp->x400pChanTable_old);
			StorageTmp->x400pChanTable_rsvs = 0;
			StorageTmp->x400pChanTable_tsts = 0;
			StorageTmp->x400pChanTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->x400pChanTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->x400pChanTable_sets == 0)
			revert_x400pChanTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->x400pChanTable_old) == NULL)
			break;
		StorageTmp->x400pChanType = StorageOld->x400pChanType;
		if (--StorageTmp->x400pChanTable_rsvs == 0)
			x400pChanTable_destroy(&StorageTmp->x400pChanTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_x400pChanFormat(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_x400pChanFormat(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct x400pChanTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("x400pMxMIB", "write_x400pChanFormat entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(x400pChanTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pChanFormat not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pChanFormat: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case X400PCHANFORMAT_NONE:
		case X400PCHANFORMAT_DS0A:
		case X400PCHANFORMAT_DS0:
		case X400PCHANFORMAT_T1:
		case X400PCHANFORMAT_J1:
		case X400PCHANFORMAT_E1:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pChanFormat: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->x400pChanTable_old) == NULL)
			if (StorageTmp->x400pChanTable_rsvs == 0)
				if ((StorageOld = StorageTmp->x400pChanTable_old = x400pChanTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->x400pChanTable_rsvs++;
		StorageTmp->x400pChanFormat = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->x400pChanTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->x400pChanTable_tsts == 0)
				if ((ret = check_x400pChanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pChanTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->x400pChanFormat for you to use, and you have just been asked to do something with it.  Note that anything done here must
				   be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->x400pChanTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->x400pChanTable_sets == 0)
				if ((ret = update_x400pChanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pChanTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->x400pChanTable_old) != NULL) {
			x400pChanTable_destroy(&StorageTmp->x400pChanTable_old);
			StorageTmp->x400pChanTable_rsvs = 0;
			StorageTmp->x400pChanTable_tsts = 0;
			StorageTmp->x400pChanTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->x400pChanTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->x400pChanTable_sets == 0)
			revert_x400pChanTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->x400pChanTable_old) == NULL)
			break;
		StorageTmp->x400pChanFormat = StorageOld->x400pChanFormat;
		if (--StorageTmp->x400pChanTable_rsvs == 0)
			x400pChanTable_destroy(&StorageTmp->x400pChanTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_x400pChanRate(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_x400pChanRate(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct x400pChanTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("x400pMxMIB", "write_x400pChanRate entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(x400pChanTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pChanRate not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pChanRate: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case X400PCHANRATE_VARIABLE:
		case X400PCHANRATE_KBITS56:
		case X400PCHANRATE_KBITS64:
		case X400PCHANRATE_KBITS1536:
		case X400PCHANRATE_KBITS1984:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pChanRate: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->x400pChanTable_old) == NULL)
			if (StorageTmp->x400pChanTable_rsvs == 0)
				if ((StorageOld = StorageTmp->x400pChanTable_old = x400pChanTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->x400pChanTable_rsvs++;
		StorageTmp->x400pChanRate = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->x400pChanTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->x400pChanTable_tsts == 0)
				if ((ret = check_x400pChanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pChanTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->x400pChanRate for you to use, and you have just been asked to do something with it.  Note that anything done here must
				   be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->x400pChanTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->x400pChanTable_sets == 0)
				if ((ret = update_x400pChanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pChanTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->x400pChanTable_old) != NULL) {
			x400pChanTable_destroy(&StorageTmp->x400pChanTable_old);
			StorageTmp->x400pChanTable_rsvs = 0;
			StorageTmp->x400pChanTable_tsts = 0;
			StorageTmp->x400pChanTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->x400pChanTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->x400pChanTable_sets == 0)
			revert_x400pChanTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->x400pChanTable_old) == NULL)
			break;
		StorageTmp->x400pChanRate = StorageOld->x400pChanRate;
		if (--StorageTmp->x400pChanTable_rsvs == 0)
			x400pChanTable_destroy(&StorageTmp->x400pChanTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_x400pChanMode(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_x400pChanMode(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct x400pChanTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("x400pMxMIB", "write_x400pChanMode entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(x400pChanTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if ((var_val_type != ASN_BIT_STR && var_val_type != ASN_OCTET_STR)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pChanMode not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_type == ASN_BIT_STR) {
			if (1 > var_val_len || var_val_len > SPRINT_MAX_LEN || var_val_len != 2) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pChanMode: bad length\n");
				return SNMP_ERR_WRONGLENGTH;
			}
		}
		if (var_val_type == ASN_OCTET_STR) {
			if (var_val_len > SPRINT_MAX_LEN || var_val_len != 1) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pChanMode: bad length\n");
				return SNMP_ERR_WRONGLENGTH;
			}
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->x400pChanTable_old) == NULL)
			if (StorageTmp->x400pChanTable_rsvs == 0)
				if ((StorageOld = StorageTmp->x400pChanTable_old = x400pChanTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->x400pChanTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->x400pChanMode);
		StorageTmp->x400pChanMode = string;
		StorageTmp->x400pChanModeLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->x400pChanTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->x400pChanTable_tsts == 0)
				if ((ret = check_x400pChanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pChanTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->x400pChanMode for you to use, and you have just been asked to do something with it.  Note that anything done here must
				   be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->x400pChanTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->x400pChanTable_sets == 0)
				if ((ret = update_x400pChanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pChanTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->x400pChanTable_old) != NULL) {
			x400pChanTable_destroy(&StorageTmp->x400pChanTable_old);
			StorageTmp->x400pChanTable_rsvs = 0;
			StorageTmp->x400pChanTable_tsts = 0;
			StorageTmp->x400pChanTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->x400pChanTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->x400pChanTable_sets == 0)
			revert_x400pChanTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->x400pChanTable_old) == NULL)
			break;
		if (StorageOld->x400pChanMode != NULL) {
			SNMP_FREE(StorageTmp->x400pChanMode);
			StorageTmp->x400pChanMode = StorageOld->x400pChanMode;
			StorageTmp->x400pChanModeLen = StorageOld->x400pChanModeLen;
			StorageOld->x400pChanMode = NULL;
			StorageOld->x400pChanModeLen = 0;
		}
		if (--StorageTmp->x400pChanTable_rsvs == 0)
			x400pChanTable_destroy(&StorageTmp->x400pChanTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_x400pChanAdministrativeState(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_x400pChanAdministrativeState(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct x400pChanTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("x400pMxMIB", "write_x400pChanAdministrativeState entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(x400pChanTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pChanAdministrativeState not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pChanAdministrativeState: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value locked */
		switch (set_value) {
		case X400PCHANADMINISTRATIVESTATE_LOCKED:
		case X400PCHANADMINISTRATIVESTATE_UNLOCKED:
		case X400PCHANADMINISTRATIVESTATE_SHUTTINGDOWN:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pChanAdministrativeState: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->x400pChanTable_old) == NULL)
			if (StorageTmp->x400pChanTable_rsvs == 0)
				if ((StorageOld = StorageTmp->x400pChanTable_old = x400pChanTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->x400pChanTable_rsvs++;
		StorageTmp->x400pChanAdministrativeState = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->x400pChanTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->x400pChanTable_tsts == 0)
				if ((ret = check_x400pChanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pChanTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->x400pChanAdministrativeState for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->x400pChanTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->x400pChanTable_sets == 0)
				if ((ret = update_x400pChanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pChanTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->x400pChanTable_old) != NULL) {
			x400pChanTable_destroy(&StorageTmp->x400pChanTable_old);
			StorageTmp->x400pChanTable_rsvs = 0;
			StorageTmp->x400pChanTable_tsts = 0;
			StorageTmp->x400pChanTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->x400pChanTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->x400pChanTable_sets == 0)
			revert_x400pChanTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->x400pChanTable_old) == NULL)
			break;
		StorageTmp->x400pChanAdministrativeState = StorageOld->x400pChanAdministrativeState;
		if (--StorageTmp->x400pChanTable_rsvs == 0)
			x400pChanTable_destroy(&StorageTmp->x400pChanTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_x400pChanControlStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_x400pChanControlStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct x400pChanTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("x400pMxMIB", "write_x400pChanControlStatus entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(x400pChanTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if ((var_val_type != ASN_BIT_STR && var_val_type != ASN_OCTET_STR)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pChanControlStatus not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_type == ASN_BIT_STR) {
			if (1 > var_val_len || var_val_len > SPRINT_MAX_LEN || var_val_len != 2) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pChanControlStatus: bad length\n");
				return SNMP_ERR_WRONGLENGTH;
			}
		}
		if (var_val_type == ASN_OCTET_STR) {
			if (var_val_len > SPRINT_MAX_LEN || var_val_len != 1) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pChanControlStatus: bad length\n");
				return SNMP_ERR_WRONGLENGTH;
			}
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->x400pChanTable_old) == NULL)
			if (StorageTmp->x400pChanTable_rsvs == 0)
				if ((StorageOld = StorageTmp->x400pChanTable_old = x400pChanTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->x400pChanTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->x400pChanControlStatus);
		StorageTmp->x400pChanControlStatus = string;
		StorageTmp->x400pChanControlStatusLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->x400pChanTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->x400pChanTable_tsts == 0)
				if ((ret = check_x400pChanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pChanTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->x400pChanControlStatus for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->x400pChanTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->x400pChanTable_sets == 0)
				if ((ret = update_x400pChanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pChanTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->x400pChanTable_old) != NULL) {
			x400pChanTable_destroy(&StorageTmp->x400pChanTable_old);
			StorageTmp->x400pChanTable_rsvs = 0;
			StorageTmp->x400pChanTable_tsts = 0;
			StorageTmp->x400pChanTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->x400pChanTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->x400pChanTable_sets == 0)
			revert_x400pChanTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->x400pChanTable_old) == NULL)
			break;
		if (StorageOld->x400pChanControlStatus != NULL) {
			SNMP_FREE(StorageTmp->x400pChanControlStatus);
			StorageTmp->x400pChanControlStatus = StorageOld->x400pChanControlStatus;
			StorageTmp->x400pChanControlStatusLen = StorageOld->x400pChanControlStatusLen;
			StorageOld->x400pChanControlStatus = NULL;
			StorageOld->x400pChanControlStatusLen = 0;
		}
		if (--StorageTmp->x400pChanTable_rsvs == 0)
			x400pChanTable_destroy(&StorageTmp->x400pChanTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_x400pChanAlarmStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_x400pChanAlarmStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct x400pChanTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("x400pMxMIB", "write_x400pChanAlarmStatus entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(x400pChanTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if ((var_val_type != ASN_BIT_STR && var_val_type != ASN_OCTET_STR)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pChanAlarmStatus not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_type == ASN_BIT_STR) {
			if (1 > var_val_len || var_val_len > SPRINT_MAX_LEN || var_val_len != 2) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pChanAlarmStatus: bad length\n");
				return SNMP_ERR_WRONGLENGTH;
			}
		}
		if (var_val_type == ASN_OCTET_STR) {
			if (var_val_len > SPRINT_MAX_LEN || var_val_len != 1) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pChanAlarmStatus: bad length\n");
				return SNMP_ERR_WRONGLENGTH;
			}
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->x400pChanTable_old) == NULL)
			if (StorageTmp->x400pChanTable_rsvs == 0)
				if ((StorageOld = StorageTmp->x400pChanTable_old = x400pChanTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->x400pChanTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->x400pChanAlarmStatus);
		StorageTmp->x400pChanAlarmStatus = string;
		StorageTmp->x400pChanAlarmStatusLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->x400pChanTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->x400pChanTable_tsts == 0)
				if ((ret = check_x400pChanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pChanTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->x400pChanAlarmStatus for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->x400pChanTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->x400pChanTable_sets == 0)
				if ((ret = update_x400pChanTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pChanTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->x400pChanTable_old) != NULL) {
			x400pChanTable_destroy(&StorageTmp->x400pChanTable_old);
			StorageTmp->x400pChanTable_rsvs = 0;
			StorageTmp->x400pChanTable_tsts = 0;
			StorageTmp->x400pChanTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->x400pChanTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->x400pChanTable_sets == 0)
			revert_x400pChanTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->x400pChanTable_old) == NULL)
			break;
		if (StorageOld->x400pChanAlarmStatus != NULL) {
			SNMP_FREE(StorageTmp->x400pChanAlarmStatus);
			StorageTmp->x400pChanAlarmStatus = StorageOld->x400pChanAlarmStatus;
			StorageTmp->x400pChanAlarmStatusLen = StorageOld->x400pChanAlarmStatusLen;
			StorageOld->x400pChanAlarmStatus = NULL;
			StorageOld->x400pChanAlarmStatusLen = 0;
		}
		if (--StorageTmp->x400pChanTable_rsvs == 0)
			x400pChanTable_destroy(&StorageTmp->x400pChanTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_x400pXconCardIndex(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_x400pXconCardIndex(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct x400pXconTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("x400pMxMIB", "write_x400pXconCardIndex entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(x400pXconTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->x400pXconRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pXconCardIndex: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pXconCardIndex not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pXconCardIndex: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 1..16 */
		if ((1 > set_value || set_value > 16)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pXconCardIndex: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->x400pXconTable_old) == NULL)
			if (StorageTmp->x400pXconTable_rsvs == 0)
				if ((StorageOld = StorageTmp->x400pXconTable_old = x400pXconTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->x400pXconTable_rsvs++;
		StorageTmp->x400pXconCardIndex = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->x400pXconTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->x400pXconTable_tsts == 0)
				if ((ret = check_x400pXconTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pXconTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->x400pXconCardIndex for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->x400pXconTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->x400pXconTable_sets == 0)
				if ((ret = update_x400pXconTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pXconTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->x400pXconTable_old) != NULL) {
			x400pXconTable_destroy(&StorageTmp->x400pXconTable_old);
			StorageTmp->x400pXconTable_rsvs = 0;
			StorageTmp->x400pXconTable_tsts = 0;
			StorageTmp->x400pXconTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->x400pXconTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->x400pXconTable_sets == 0)
			revert_x400pXconTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->x400pXconTable_old) == NULL)
			break;
		StorageTmp->x400pXconCardIndex = StorageOld->x400pXconCardIndex;
		if (--StorageTmp->x400pXconTable_rsvs == 0)
			x400pXconTable_destroy(&StorageTmp->x400pXconTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_x400pXconSpanIndex(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_x400pXconSpanIndex(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct x400pXconTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("x400pMxMIB", "write_x400pXconSpanIndex entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(x400pXconTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->x400pXconRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pXconSpanIndex: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pXconSpanIndex not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pXconSpanIndex: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 1..4 */
		if ((1 > set_value || set_value > 4)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pXconSpanIndex: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->x400pXconTable_old) == NULL)
			if (StorageTmp->x400pXconTable_rsvs == 0)
				if ((StorageOld = StorageTmp->x400pXconTable_old = x400pXconTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->x400pXconTable_rsvs++;
		StorageTmp->x400pXconSpanIndex = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->x400pXconTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->x400pXconTable_tsts == 0)
				if ((ret = check_x400pXconTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pXconTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->x400pXconSpanIndex for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->x400pXconTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->x400pXconTable_sets == 0)
				if ((ret = update_x400pXconTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pXconTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->x400pXconTable_old) != NULL) {
			x400pXconTable_destroy(&StorageTmp->x400pXconTable_old);
			StorageTmp->x400pXconTable_rsvs = 0;
			StorageTmp->x400pXconTable_tsts = 0;
			StorageTmp->x400pXconTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->x400pXconTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->x400pXconTable_sets == 0)
			revert_x400pXconTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->x400pXconTable_old) == NULL)
			break;
		StorageTmp->x400pXconSpanIndex = StorageOld->x400pXconSpanIndex;
		if (--StorageTmp->x400pXconTable_rsvs == 0)
			x400pXconTable_destroy(&StorageTmp->x400pXconTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_x400pXconChanIndex(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_x400pXconChanIndex(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct x400pXconTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("x400pMxMIB", "write_x400pXconChanIndex entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(x400pXconTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->x400pXconRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pXconChanIndex: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pXconChanIndex not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pXconChanIndex: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..31 */
		if ((0 > set_value || set_value > 31)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pXconChanIndex: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->x400pXconTable_old) == NULL)
			if (StorageTmp->x400pXconTable_rsvs == 0)
				if ((StorageOld = StorageTmp->x400pXconTable_old = x400pXconTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->x400pXconTable_rsvs++;
		StorageTmp->x400pXconChanIndex = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->x400pXconTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->x400pXconTable_tsts == 0)
				if ((ret = check_x400pXconTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pXconTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->x400pXconChanIndex for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->x400pXconTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->x400pXconTable_sets == 0)
				if ((ret = update_x400pXconTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pXconTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->x400pXconTable_old) != NULL) {
			x400pXconTable_destroy(&StorageTmp->x400pXconTable_old);
			StorageTmp->x400pXconTable_rsvs = 0;
			StorageTmp->x400pXconTable_tsts = 0;
			StorageTmp->x400pXconTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->x400pXconTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->x400pXconTable_sets == 0)
			revert_x400pXconTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->x400pXconTable_old) == NULL)
			break;
		StorageTmp->x400pXconChanIndex = StorageOld->x400pXconChanIndex;
		if (--StorageTmp->x400pXconTable_rsvs == 0)
			x400pXconTable_destroy(&StorageTmp->x400pXconTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_x400pXconType(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_x400pXconType(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct x400pXconTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("x400pMxMIB", "write_x400pXconType entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(x400pXconTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->x400pXconRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pXconType: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pXconType not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pXconType: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value permanent */
		switch (set_value) {
		case X400PXCONTYPE_SWITCHED:
		case X400PXCONTYPE_SEMIPERMANENT:
		case X400PXCONTYPE_PERMANENT:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pXconType: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->x400pXconTable_old) == NULL)
			if (StorageTmp->x400pXconTable_rsvs == 0)
				if ((StorageOld = StorageTmp->x400pXconTable_old = x400pXconTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->x400pXconTable_rsvs++;
		StorageTmp->x400pXconType = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->x400pXconTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->x400pXconTable_tsts == 0)
				if ((ret = check_x400pXconTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pXconTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->x400pXconType for you to use, and you have just been asked to do something with it.  Note that anything done here must
				   be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->x400pXconTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->x400pXconTable_sets == 0)
				if ((ret = update_x400pXconTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pXconTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->x400pXconTable_old) != NULL) {
			x400pXconTable_destroy(&StorageTmp->x400pXconTable_old);
			StorageTmp->x400pXconTable_rsvs = 0;
			StorageTmp->x400pXconTable_tsts = 0;
			StorageTmp->x400pXconTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->x400pXconTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->x400pXconTable_sets == 0)
			revert_x400pXconTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->x400pXconTable_old) == NULL)
			break;
		StorageTmp->x400pXconType = StorageOld->x400pXconType;
		if (--StorageTmp->x400pXconTable_rsvs == 0)
			x400pXconTable_destroy(&StorageTmp->x400pXconTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_x400pXconStorageType(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_x400pXconStorageType(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct x400pXconTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("x400pMxMIB", "write_x400pXconStorageType entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(x400pXconTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->x400pXconRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pXconStorageType: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pXconStorageType not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pXconStorageType: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value nonVolatile */
		switch (set_value) {
		case ST_OTHER:
		case ST_VOLATILE:
		case ST_NONVOLATILE:
		case ST_PERMANENT:
		case ST_READONLY:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pXconStorageType: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->x400pXconTable_old) == NULL)
			if (StorageTmp->x400pXconTable_rsvs == 0)
				if ((StorageOld = StorageTmp->x400pXconTable_old = x400pXconTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->x400pXconTable_rsvs++;
		StorageTmp->x400pXconStorageType = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->x400pXconTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->x400pXconTable_tsts == 0)
				if ((ret = check_x400pXconTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pXconTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->x400pXconStorageType for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->x400pXconTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->x400pXconTable_sets == 0)
				if ((ret = update_x400pXconTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->x400pXconTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->x400pXconTable_old) != NULL) {
			x400pXconTable_destroy(&StorageTmp->x400pXconTable_old);
			StorageTmp->x400pXconTable_rsvs = 0;
			StorageTmp->x400pXconTable_tsts = 0;
			StorageTmp->x400pXconTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->x400pXconTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->x400pXconTable_sets == 0)
			revert_x400pXconTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->x400pXconTable_old) == NULL)
			break;
		StorageTmp->x400pXconStorageType = StorageOld->x400pXconStorageType;
		if (--StorageTmp->x400pXconTable_rsvs == 0)
			x400pXconTable_destroy(&StorageTmp->x400pXconTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_x400pSyncTable_row(struct x400pSyncTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_x400pSyncTable_row(struct x400pSyncTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_x400pSyncTable_row(struct x400pSyncTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_x400pSyncTable_row(struct x400pSyncTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_x400pDrivTable_row(struct x400pDrivTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_x400pDrivTable_row(struct x400pDrivTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_x400pDrivTable_row(struct x400pDrivTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_x400pDrivTable_row(struct x400pDrivTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_x400pCardTable_row(struct x400pCardTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_x400pCardTable_row(struct x400pCardTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_x400pCardTable_row(struct x400pCardTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_x400pCardTable_row(struct x400pCardTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_x400pSpanTable_row(struct x400pSpanTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_x400pSpanTable_row(struct x400pSpanTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_x400pSpanTable_row(struct x400pSpanTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_x400pSpanTable_row(struct x400pSpanTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_x400pXconTable_row(struct x400pXconTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_x400pXconTable_row(struct x400pXconTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_x400pXconTable_row(struct x400pXconTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_x400pXconTable_row(struct x400pXconTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_x400pSyncRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_x400pSyncRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct x400pSyncTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct x400pSyncTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 15;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("x400pMxMIB", "write_x400pSyncRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(x400pSyncTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSyncRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSyncRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* x400pSyncGroup */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* x400pSyncIndex */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[15]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* x400pSyncGroup */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index x400pSyncGroup: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..16 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > 16)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index x400pSyncGroup: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* x400pSyncIndex */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index x400pSyncIndex: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..8 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > 8)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index x400pSyncIndex: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = x400pSyncTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->x400pSyncTable_rsvs = 1;
			vp = vars;
			StorageNew->x400pSyncGroup = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->x400pSyncIndex = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&x400pSyncTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->x400pSyncRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->x400pSyncTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->x400pSyncTable_old) == NULL)
				if (StorageTmp->x400pSyncTable_rsvs == 0)
					if ((StorageOld = StorageTmp->x400pSyncTable_old = x400pSyncTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->x400pSyncTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->x400pSyncTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				x400pSyncTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_x400pSyncTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
				/* check that activation is possible */
			if (StorageTmp->x400pSyncRowStatus != RS_ACTIVE)
				if ((ret = can_act_x400pSyncTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->x400pSyncRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_x400pSyncTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->x400pSyncRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_x400pSyncTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
				}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->x400pSyncRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here must be
		   reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_x400pSyncTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->x400pSyncRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_x400pSyncTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->x400pSyncRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_x400pSyncTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
			break;
			/* deactivate with underlying device */
			if (deactivate_x400pSyncTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->x400pSyncRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->x400pSyncRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->x400pSyncRowStatus = set_value;
			if ((StorageOld = StorageTmp->x400pSyncTable_old) != NULL) {
				x400pSyncTable_destroy(&StorageTmp->x400pSyncTable_old);
				StorageTmp->x400pSyncTable_rsvs = 0;
				StorageTmp->x400pSyncTable_tsts = 0;
				StorageTmp->x400pSyncTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			x400pSyncTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_x400pSyncTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->x400pSyncRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_x400pSyncTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->x400pSyncRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_x400pSyncTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				x400pSyncTable_del(StorageNew);
				x400pSyncTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->x400pSyncTable_old) == NULL)
				break;
			if (--StorageTmp->x400pSyncTable_rsvs == 0)
				x400pSyncTable_destroy(&StorageTmp->x400pSyncTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				x400pSyncTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_x400pDrivRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_x400pDrivRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct x400pDrivTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct x400pDrivTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 15;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("x400pMxMIB", "write_x400pDrivRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(x400pDrivTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pDrivRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pDrivRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* x400pDrivName */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[15]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* x400pDrivName */
			/* Note: ranges 1..8 */
			if (vp->val_len > SPRINT_MAX_LEN || ((1 > vp->val_len || vp->val_len > 8))) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index x400pDrivName: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = x400pDrivTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->x400pDrivTable_rsvs = 1;
			vp = vars;
			memdup((void *) &StorageNew->x400pDrivName, vp->val.string, vp->val_len);
			StorageNew->x400pDrivNameLen = vp->val_len;
			vp = vp->next_variable;
			header_complex_add_data(&x400pDrivTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->x400pDrivRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->x400pDrivTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->x400pDrivTable_old) == NULL)
				if (StorageTmp->x400pDrivTable_rsvs == 0)
					if ((StorageOld = StorageTmp->x400pDrivTable_old = x400pDrivTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->x400pDrivTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->x400pDrivTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				x400pDrivTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_x400pDrivTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
				/* check that activation is possible */
			if (StorageTmp->x400pDrivRowStatus != RS_ACTIVE)
				if ((ret = can_act_x400pDrivTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->x400pDrivRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_x400pDrivTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->x400pDrivRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_x400pDrivTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
				}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->x400pDrivRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here must be
		   reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_x400pDrivTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->x400pDrivRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_x400pDrivTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->x400pDrivRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_x400pDrivTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
			break;
			/* deactivate with underlying device */
			if (deactivate_x400pDrivTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->x400pDrivRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->x400pDrivRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->x400pDrivRowStatus = set_value;
			if ((StorageOld = StorageTmp->x400pDrivTable_old) != NULL) {
				x400pDrivTable_destroy(&StorageTmp->x400pDrivTable_old);
				StorageTmp->x400pDrivTable_rsvs = 0;
				StorageTmp->x400pDrivTable_tsts = 0;
				StorageTmp->x400pDrivTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			x400pDrivTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_x400pDrivTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->x400pDrivRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_x400pDrivTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->x400pDrivRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_x400pDrivTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				x400pDrivTable_del(StorageNew);
				x400pDrivTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->x400pDrivTable_old) == NULL)
				break;
			if (--StorageTmp->x400pDrivTable_rsvs == 0)
				x400pDrivTable_destroy(&StorageTmp->x400pDrivTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				x400pDrivTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_x400pCardStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_x400pCardStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct x400pCardTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct x400pCardTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 15;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("x400pMxMIB", "write_x400pCardStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(x400pCardTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pCardStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pCardStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* x400pDrivName */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* x400pCardIndex */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[15]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* x400pDrivName */
			/* Note: ranges 1..8 */
			if (vp->val_len > SPRINT_MAX_LEN || ((1 > vp->val_len || vp->val_len > 8))) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index x400pDrivName: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* x400pCardIndex */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index x400pCardIndex: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..16 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > 16)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index x400pCardIndex: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = x400pCardTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->x400pCardTable_rsvs = 1;
			vp = vars;
			memdup((void *) &StorageNew->x400pDrivName, vp->val.string, vp->val_len);
			StorageNew->x400pDrivNameLen = vp->val_len;
			vp = vp->next_variable;
			StorageNew->x400pCardIndex = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&x400pCardTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->x400pCardStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->x400pCardTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->x400pCardTable_old) == NULL)
				if (StorageTmp->x400pCardTable_rsvs == 0)
					if ((StorageOld = StorageTmp->x400pCardTable_old = x400pCardTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->x400pCardTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->x400pCardTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				x400pCardTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_x400pCardTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* check that activation is possible */
			if (StorageTmp->x400pCardStatus != RS_ACTIVE)
				if ((ret = can_act_x400pCardTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->x400pCardStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_x400pCardTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->x400pCardStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_x400pCardTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->x400pCardStatus for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable
		   in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_x400pCardTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->x400pCardStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_x400pCardTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
				}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->x400pCardStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_x400pCardTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_x400pCardTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->x400pCardStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->x400pCardStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->x400pCardStatus = set_value;
			if ((StorageOld = StorageTmp->x400pCardTable_old) != NULL) {
				x400pCardTable_destroy(&StorageTmp->x400pCardTable_old);
				StorageTmp->x400pCardTable_rsvs = 0;
				StorageTmp->x400pCardTable_tsts = 0;
				StorageTmp->x400pCardTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			x400pCardTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_x400pCardTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->x400pCardStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_x400pCardTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->x400pCardStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_x400pCardTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				x400pCardTable_del(StorageNew);
				x400pCardTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->x400pCardTable_old) == NULL)
				break;
			if (--StorageTmp->x400pCardTable_rsvs == 0)
				x400pCardTable_destroy(&StorageTmp->x400pCardTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				x400pCardTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_x400pSpanRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_x400pSpanRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct x400pSpanTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct x400pSpanTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 15;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("x400pMxMIB", "write_x400pSpanRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(x400pSpanTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pSpanRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* x400pDrivName */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* x400pCardIndex */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* x400pSpanIndex */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[15]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* x400pDrivName */
			/* Note: ranges 1..8 */
			if (vp->val_len > SPRINT_MAX_LEN || ((1 > vp->val_len || vp->val_len > 8))) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index x400pDrivName: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* x400pCardIndex */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index x400pCardIndex: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..16 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > 16)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index x400pCardIndex: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* x400pSpanIndex */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index x400pSpanIndex: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 0..4 */
			if ((0 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > 4)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index x400pSpanIndex: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = x400pSpanTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->x400pSpanTable_rsvs = 1;
			vp = vars;
			memdup((void *) &StorageNew->x400pDrivName, vp->val.string, vp->val_len);
			StorageNew->x400pDrivNameLen = vp->val_len;
			vp = vp->next_variable;
			StorageNew->x400pCardIndex = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->x400pSpanIndex = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&x400pSpanTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->x400pSpanRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->x400pSpanTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
				if (StorageTmp->x400pSpanTable_rsvs == 0)
					if ((StorageOld = StorageTmp->x400pSpanTable_old = x400pSpanTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->x400pSpanTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->x400pSpanTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				x400pSpanTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_x400pSpanTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
				/* check that activation is possible */
			if (StorageTmp->x400pSpanRowStatus != RS_ACTIVE)
				if ((ret = can_act_x400pSpanTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->x400pSpanRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_x400pSpanTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->x400pSpanRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_x400pSpanTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
				}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->x400pSpanRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here must be
		   reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_x400pSpanTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->x400pSpanRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_x400pSpanTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->x400pSpanRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_x400pSpanTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_x400pSpanTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->x400pSpanRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->x400pSpanRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->x400pSpanRowStatus = set_value;
			if ((StorageOld = StorageTmp->x400pSpanTable_old) != NULL) {
				x400pSpanTable_destroy(&StorageTmp->x400pSpanTable_old);
				StorageTmp->x400pSpanTable_rsvs = 0;
				StorageTmp->x400pSpanTable_tsts = 0;
				StorageTmp->x400pSpanTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			x400pSpanTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_x400pSpanTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->x400pSpanRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_x400pSpanTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->x400pSpanRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_x400pSpanTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				x400pSpanTable_del(StorageNew);
				x400pSpanTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->x400pSpanTable_old) == NULL)
				break;
			if (--StorageTmp->x400pSpanTable_rsvs == 0)
				x400pSpanTable_destroy(&StorageTmp->x400pSpanTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				x400pSpanTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_x400pXconRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_x400pXconRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct x400pXconTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct x400pXconTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 15;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("x400pMxMIB", "write_x400pXconRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(x400pXconTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pXconRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to x400pXconRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* x400pDrivName */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* x400pCardIndex */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* x400pSpanIndex */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* x400pChanIndex */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[15]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* x400pDrivName */
			/* Note: ranges 1..8 */
			if (vp->val_len > SPRINT_MAX_LEN || ((1 > vp->val_len || vp->val_len > 8))) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index x400pDrivName: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* x400pCardIndex */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index x400pCardIndex: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..16 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > 16)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index x400pCardIndex: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* x400pSpanIndex */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index x400pSpanIndex: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 0..4 */
			if ((0 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > 4)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index x400pSpanIndex: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* x400pChanIndex */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index x400pChanIndex: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..31 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > 31)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index x400pChanIndex: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = x400pXconTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->x400pXconTable_rsvs = 1;
			vp = vars;
			memdup((void *) &StorageNew->x400pDrivName, vp->val.string, vp->val_len);
			StorageNew->x400pDrivNameLen = vp->val_len;
			vp = vp->next_variable;
			StorageNew->x400pCardIndex = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->x400pSpanIndex = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->x400pChanIndex = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&x400pXconTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->x400pXconRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->x400pXconTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->x400pXconTable_old) == NULL)
				if (StorageTmp->x400pXconTable_rsvs == 0)
					if ((StorageOld = StorageTmp->x400pXconTable_old = x400pXconTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->x400pXconTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->x400pXconTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				x400pXconTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_x400pXconTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
				/* check that activation is possible */
			if (StorageTmp->x400pXconRowStatus != RS_ACTIVE)
				if ((ret = can_act_x400pXconTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->x400pXconRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_x400pXconTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->x400pXconRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_x400pXconTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
				}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->x400pXconRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here must be
		   reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_x400pXconTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->x400pXconRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_x400pXconTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->x400pXconRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_x400pXconTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_x400pXconTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->x400pXconRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->x400pXconRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->x400pXconRowStatus = set_value;
			if ((StorageOld = StorageTmp->x400pXconTable_old) != NULL) {
				x400pXconTable_destroy(&StorageTmp->x400pXconTable_old);
				StorageTmp->x400pXconTable_rsvs = 0;
				StorageTmp->x400pXconTable_tsts = 0;
				StorageTmp->x400pXconTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			x400pXconTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_x400pXconTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->x400pXconRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_x400pXconTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->x400pXconRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_x400pXconTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				x400pXconTable_del(StorageNew);
				x400pXconTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->x400pXconTable_old) == NULL)
				break;
			if (--StorageTmp->x400pXconTable_rsvs == 0)
				x400pXconTable_destroy(&StorageTmp->x400pXconTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				x400pXconTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn void x400pMxMIB_loop_handler(int dummy)
 * @param sig signal number
 * @brief handle event loop interation.
 *
 * This function is registered so that, when operating as a module, snmpd will call it one per event
 * loop interation.  This function is called before the next requst is processed and after the
 * previous request is processed.  Two things are done here:  1) The file descriptor that is used to
 * synchronize the agent with (pseudo-)device drivers is closed.  (Another approach, instead of
 * closing each time, would be to restart a timer each time that a request is made (loop is
 * performed) and if it expires, close the file descriptor).  2) The request number is incremented.
 * Although a request is not generated for each loop of the snmp event loop, it is true that a new
 * request cannot be generated without performing a loop.  Therefore, the sa_request is not the
 * request number but it is a temporally unique identifier for a request.
 */
void
x400pMxMIB_loop_handler(int sig)
{
	DEBUGMSGTL(("x400pMxMIB", "x400pMxMIB_loop_handler: executing loop handler...  "));
	/* close files after each request */
	if (sa_fclose) {
		if (my_fd >= 0) {
			close(my_fd);
			my_fd = -1;
		}
	}
#if defined MASTER
	/* prepare for next request */
	sa_request++;
#endif				/* defined MASTER */
	if (external_signal_scheduled[sig] == 0)
		external_signal_scheduled[sig]--;
	if (x400pMxMIBold_signal_handler != NULL)
		(*x400pMxMIBold_signal_handler) (sig);
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
}

/**
 * @fn void x400pMxMIB_fd_handler(int fd, void *dummy)
 * @param fd file descriptor to read.
 * @param dummy client data passed to registration function (always NULL).
 * @brief handle read event on file descriptor.
 *
 * This read file descriptor handler is normally used for (pseudo-)device drivers that generate
 * statistical collection interval events, alarm events, or other operational measurement events, by
 * placing a message on the read queue of the "event handling" Stream.  Normally this routine
 * would adjust counts in some table or scalars, generate SNMP traps representing on-occurence
 * events, first and interval events, and alarm indications.
 */
void
x400pMxMIB_fd_handler(int fd, void *dummy)
{
	DEBUGMSGTL(("x400pMxMIB", "x400pMxMIB_fd_handler: executing fd handler...  "));
	/* XXX: place actions to handle my_fd here... */
	DEBUGMSGTL(("x400pMxMIB", "done.\n"));
	return;
}
