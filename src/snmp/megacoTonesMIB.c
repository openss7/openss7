/*****************************************************************************

 @(#) src/snmp/megacoTonesMIB.c

 -----------------------------------------------------------------------------

 Copyright (c) 2008-2019  Monavacon Limited <http://www.monavacon.com/>
 Copyright (c) 2001-2008  OpenSS7 Corporation <http://www.openss7.com/>
 Copyright (c) 1997-2001  Brian F. G. Bidulock <bidulock@openss7.org>

 All Rights Reserved.

 This program is free software: you can redistribute it and/or modify it under
 the terms of the GNU Affero General Public License as published by the Free
 Software Foundation, version 3 of the license.

 This program is distributed in the hope that it will be useful, but WITHOUT
 ANY WARRANTY; without even the implied warranty of MERCHANTABILITY or FITNESS
 FOR A PARTICULAR PURPOSE.  See the GNU Affero General Public License for more
 details.

 You should have received a copy of the GNU Affero General Public License
 along with this program.  If not, see <http://www.gnu.org/licenses/>, or
 write to the Free Software Foundation, Inc., 675 Mass Ave, Cambridge, MA
 02139, USA.

 -----------------------------------------------------------------------------

 U.S. GOVERNMENT RESTRICTED RIGHTS.  If you are licensing this Software on
 behalf of the U.S. Government ("Government"), the following provisions apply
 to you.  If the Software is supplied by the Department of Defense ("DoD"), it
 is classified as "Commercial Computer Software" under paragraph 252.227-7014
 of the DoD Supplement to the Federal Acquisition Regulations ("DFARS") (or any
 successor regulations) and the Government is acquiring only the license rights
 granted herein (the license rights customarily provided to non-Government
 users).  If the Software is supplied to any unit or agency of the Government
 other than DoD, it is classified as "Restricted Computer Software" and the
 Government's rights in the Software are defined in paragraph 52.227-19 of the
 Federal Acquisition Regulations ("FAR") (or any successor regulations) or, in
 the cases of NASA, in paragraph 18.52.227-86 of the NASA Supplement to the FAR
 (or any successor regulations).

 -----------------------------------------------------------------------------

 Commercial licensing and support of this software is available from OpenSS7
 Corporation at a fee.  See http://www.openss7.com/

 *****************************************************************************/

static char const ident[] __attribute__((unused)) = "src/snmp/megacoTonesMIB.c (" PACKAGE_ENVR ") " PACKAGE_DATE;

/* This file was generated by mib2c and is intended for use as
   a mib module for the ucd-snmp snmpd agent. */
#include <ucd-snmp/ucd-snmp-config.h>
#include <ucd-snmp/ucd-snmp-includes.h>
#include <ucd-snmp/ucd-snmp-agent-includes.h>
#include <ucd-snmp/agent_trap.h>
#include <ucd-snmp/callback.h>
#include <ucd-snmp/snmp-tc.h>
#include <ucd-snmp/default_store.h>
#include <ucd-snmp/snmp_alarm.h>
/* The following header files are mangled in most recent net-snmp releases so
 * the versions from UCD-SNMP 4.2.5 are included here.  */
#if defined HAVE_LIBNETSNMP
#else				/* defined HAVE_LIBNETSNMP */
#endif				/* defined HAVE_LIBNETSNMP */
/* These are messed up on both. */
#include "ds_agent.h"
#ifdef HAVE_UCD_SNMP_UTIL_FUNCS_H
#include <ucd-snmp/util_funcs.h>
/* Many recent net-snmp UCD compatible headers do not declare header_generic. */
int header_generic(struct variable *, oid *, size_t *, int, size_t *, WriteMethod **);
#else				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#include "util_funcs.h"
#endif				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#ifdef HAVE_UCD_SNMP_HEADER_COMPLEX_H
#include <ucd-snmp/header_complex.h>
#else				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
#include "header_complex.h"
#endif				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
/* This one is the other way around: it is just fine for net-snmp, but
 * ucd-snmp does not provide the header file at all.  */
#ifdef HAVE_UCD_SNMP_MIB_MODULES_H
#include <ucd-snmp/mib_modules.h>
#else				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#ifdef HAVE_NET_SNMP_AGENT_MIB_MODULES_H
#include <net-snmp/agent/mib_modules.h>
#else				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#include "mib_modules.h"
#endif				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#endif				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#ifdef HAVE_NETSNMP_MEMDUP
#include "ucd_memdup.h"
#endif
#include <stdint.h>
#include <signal.h>
#include <sys/stat.h>		/* for struct stat, fstat() */
#include <sys/types.h>
#include <unistd.h>
#include <stdlib.h>
#include <pwd.h>		/* for getpwuid() getpwnam() */
#include <grp.h>		/* for getgrgid() getgrnam() */
#include <libgen.h>		/* for basename() */
#include <fcntl.h>		/* for O_CREAT */
#include <errno.h>
#include <string.h>
#ifdef _GNU_SOURCE
#include <getopt.h>
#endif
#include "megacoTonesMIB.h"
#define MASTER 1
#define MY_FACILITY(__pri)	(LOG_DAEMON|(__pri))
#if defined MODULE
#if defined MASTER
const char sa_program[] = "megacoTonesMIB";
int sa_fclose = 1;			/* default close files between requests */
int sa_changed = 1;			/* indication to reread MIB configuration */
int sa_stats_refresh = 1;		/* indications that statistics, the mib or its tables need to be refreshed */
int sa_request = 1;			/* request number for per-request actions */
int sa_dump = 0;			/* default packet dump */
int sa_debug = 0;			/* default no debug */
#endif				/* defined MASTER */
#endif				/* defined MODULE */
static int my_fd = -1;			/* file descriptor for this MIB's use */
static int my_readfd = -1;		/* file descriptor for autonomnous events */
volatile int megacoTonesMIB_refresh = 1;
volatile int megTonesToneTable_refresh = 1;
volatile int megTonesGroupTable_refresh = 1;

/*
 * megacoTonesMIB_variables_oid: object identifier for megacoTonesMIB
 * This is the top level oid that we want to register under.  This is essentially a prefix, with the
 * suffix appearing in the variable below.
 */
oid megacoTonesMIB_variables_oid[7] = { 1, 3, 6, 1, 2, 1, 1001 };
oid megTonesToneTable_variables_oid[11] = { 1, 3, 6, 1, 2, 1, 1001, 1, 1, 1, 1 };
oid megTonesGroupTable_variables_oid[11] = { 1, 3, 6, 1, 2, 1, 1001, 1, 1, 4, 1 };

/*
 * Oids for use in notifications defined in this MIB.
 */

/*
 * Oids accessible only for notify defined in this MIB.
 */

/*
 * Other oids defined in this MIB.
 */
oid megTonesProperty_oid[10] = { 1, 3, 6, 1, 2, 1, 1001, 1, 2, 1 };
oid megTonesCompliance_oid[10] = { 1, 3, 6, 1, 2, 1, 1001, 3, 1, 1 };
oid megTonesToneSetGrp_oid[10] = { 1, 3, 6, 1, 2, 1, 1001, 3, 2, 1 };
oid megTonesGroupGrp_oid[10] = { 1, 3, 6, 1, 2, 1, 1001, 3, 2, 2 };
static oid zeroDotZero_oid[2] = { 0, 0 };
static oid snmpTrapOID_oid[11] = { 1, 3, 6, 1, 6, 3, 1, 1, 4, 1, 0 };

/*
 * variable7 megacoTonesMIB_variables: tree for megacoTonesMIB
 * This variable defines function callbacks and type return information for the megacoTonesMIB mib section
 */
struct variable7 megacoTonesMIB_variables[] = {
	/* magic number, variable type, ro/rw, callback fn, L, oidsuffix */
#define   MEGTONESTONEGROUPID   1
	{ (u_char) MEGTONESTONEGROUPID, ASN_UNSIGNED, RWRITE, var_megTonesToneTable, 5, { 1, 1, 1, 1, 3} },
#define   MEGTONESTONENAME      2
	{ (u_char) MEGTONESTONENAME, ASN_OCTET_STR, RWRITE, var_megTonesToneTable, 5, { 1, 1, 1, 1, 4} },
#define   MEGTONESTONETIMEOUT   3
	{ (u_char) MEGTONESTONETIMEOUT, ASN_INTEGER, RWRITE, var_megTonesToneTable, 5, { 1, 1, 1, 1, 5} },
#define   MEGTONESTONEROWSTATUS  4
	{ (u_char) MEGTONESTONEROWSTATUS, ASN_INTEGER, RWRITE, var_megTonesToneTable, 5, { 1, 1, 1, 1, 6} },
#define   MEGTONESNEXTGROUPID   5
	{ (u_char) MEGTONESNEXTGROUPID, ASN_INTEGER, RWRITE, var_megacoTonesMIB, 3, { 1, 1, 3} },
#define   MEGTONESGROUPREF      7
	{ (u_char) MEGTONESGROUPREF, ASN_INTEGER, RWRITE, var_megTonesGroupTable, 5, { 1, 1, 4, 1, 2} },
#define   MEGTONESGROUPGROUPLINK  8
	{ (u_char) MEGTONESGROUPGROUPLINK, ASN_INTEGER, RWRITE, var_megTonesGroupTable, 5, { 1, 1, 4, 1, 3} },
#define   MEGTONESGROUPFREQUENCY  9
	{ (u_char) MEGTONESGROUPFREQUENCY, ASN_INTEGER, RWRITE, var_megTonesGroupTable, 5, { 1, 1, 4, 1, 4} },
#define   MEGTONESGROUPLEVEL    10
	{ (u_char) MEGTONESGROUPLEVEL, ASN_INTEGER, RWRITE, var_megTonesGroupTable, 5, { 1, 1, 4, 1, 5} },
#define   MEGTONESGROUPDURATION  11
	{ (u_char) MEGTONESGROUPDURATION, ASN_INTEGER, RWRITE, var_megTonesGroupTable, 5, { 1, 1, 4, 1, 6} },
#define   MEGTONESGROUPREPEATCOUNT  12
	{ (u_char) MEGTONESGROUPREPEATCOUNT, ASN_INTEGER, RWRITE, var_megTonesGroupTable, 5, { 1, 1, 4, 1, 7} },
#define   MEGTONESGROUPROWSTATUS  13
	{ (u_char) MEGTONESGROUPROWSTATUS, ASN_INTEGER, RWRITE, var_megTonesGroupTable, 5, { 1, 1, 4, 1, 8} },
};

/* (L = length of the oidsuffix) */
struct megacoTonesMIB_data *megacoTonesMIBStorage = NULL;

/* global storage of our data, saved in and configured by header_complex() */
struct header_complex_index *megTonesToneTableStorage = NULL;
struct header_complex_index *megTonesGroupTableStorage = NULL;

void (*megacoTonesMIBold_signal_handler)(int) = NULL;	/* save old signal handler just in case */
void megacoTonesMIB_loop_handler(int);
void megacoTonesMIB_fd_handler(int, void *);

/**
 * @fn void init_megacoTonesMIB(void)
 * @brief megacoTonesMIB initialization routine.
 *
 * This is called when the agent starts up.  At a minimum, registration of the MIB variables
 * structure (megacoTonesMIB_variables) should take place here.  By default the function also
 * registers the configuration handler and configuration store callbacks.
 *
 * Additional registrations that may be considered here are calls to regsiter_readfd(),
 * register_writefd() and register_exceptfd() for hooking into the snmpd event loop, but only when
 * used as a loadable module.  By default this function establishes a single file descriptor to
 * read, or upon which to handle exceptions.  Note that the snmpd only supports a maximum of 32
 * extneral file descriptors, so these should be used sparingly.
 *
 * When running as a loadable module, it is also necessary to hook into the snmpd event loop so that
 * the current request number can be deteremined.  This is accomplished by using a trick of the
 * external_signal_scheduled and external_signal_handler mechanism which is called on each event
 * loop when external_signal_scheduled is non-zero.  This is used to increment the sa_request value
 * on each snmpd event loop interation so that calls to MIB tree functions can determine whether
 * they belong to a fresh request or not (primarily for cacheing and possibly to clean up non-polled
 * file descriptors).
 */
void
init_megacoTonesMIB(void)
{
	(void) my_fd;
	(void) zeroDotZero_oid;
	(void) snmpTrapOID_oid;
	DEBUGMSGTL(("megacoTonesMIB", "init_megacoTonesMIB: initializing...  "));
	/* register ourselves with the agent to handle our mib tree */
	REGISTER_MIB("megacoTonesMIB", megacoTonesMIB_variables, variable7, megacoTonesMIB_variables_oid);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_SHUTDOWN, term_megacoTonesMIB, NULL);
	/* register our config handler(s) to deal with registrations */
	snmpd_register_config_handler("megacoTonesMIB", parse_megacoTonesMIB, NULL, "HELP STRING");
	snmpd_register_config_handler("megTonesToneTable", parse_megTonesToneTable, NULL, "HELP STRING");
	snmpd_register_config_handler("megTonesGroupTable", parse_megTonesGroupTable, NULL, "HELP STRING");

	/* we need to be called back later to store our data */
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_megacoTonesMIB, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_megTonesToneTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_megTonesGroupTable, NULL);

	/* place any other initialization junk you need here */
	if (my_readfd >= 0) {
		register_readfd(my_readfd, megacoTonesMIB_fd_handler, (void *) 0);
		register_exceptfd(my_readfd, megacoTonesMIB_fd_handler, (void *) 1);
	}
	megacoTonesMIBold_signal_handler = external_signal_handler[SIGCHLD];
	external_signal_handler[SIGCHLD] = &megacoTonesMIB_loop_handler;
	external_signal_scheduled[SIGCHLD] = 1;
	DEBUGMSGTL(("megacoTonesMIB", "done.\n"));
}

/**
 * @fn void deinit_megacoTonesMIB(void)
 * @brief deinitialization routine.
 *
 * This is called before the agent is unloaded.  At a minimum, deregistration of the MIB variables
 * structure (megacoTonesMIB_variables) should take place here.  By default, the function also
 * deregisters the the configuration file handlers for the MIB variables and table rows.
 *
 * Additional deregistrations that may be required here are calls to unregister_readfd(),
 * unregister_writefd() and unregsiter_exceptfd() for unhooking from the snmpd event loop, but only
 * when used as a loadable module.  By default if a read file descriptor exists, it is unregistered.
 */
void
deinit_megacoTonesMIB(void)
{
	DEBUGMSGTL(("megacoTonesMIB", "deinit_megacoTonesMIB: deinitializating...  "));
	external_signal_handler[SIGCHLD] = megacoTonesMIBold_signal_handler;
	if (my_readfd >= 0) {
		unregister_exceptfd(my_readfd);
		unregister_readfd(my_readfd);
		close(my_readfd);
		my_readfd = -1;
	}
	unregister_mib(megacoTonesMIB_variables_oid, sizeof(megacoTonesMIB_variables_oid) / sizeof(oid));
	snmpd_unregister_config_handler("megacoTonesMIB");
	snmpd_unregister_config_handler("megTonesToneTable");
	snmpd_unregister_config_handler("megTonesGroupTable");

	/* place any other de-initialization junk you need here */
	DEBUGMSGTL(("megacoTonesMIB", "done.\n"));
}

int
term_megacoTonesMIB(int majorID, int minorID, void *serverarg, void *clientarg)
{
	DEBUGMSGTL(("megacoTonesMIB", "term_megacoTonesMIB: terminating...  "));
	deinit_megacoTonesMIB();
	DEBUGMSGTL(("megacoTonesMIB", "done.\n"));
	return 0;
}

/**
 * @fn struct megacoTonesMIB_data *megacoTonesMIB_create(void)
 * @brief create a fresh data structure representing scalars in megacoTonesMIB.
 *
 * Creates a new megacoTonesMIB_data structure by allocating dynamic memory for the structure and
 * initializing the default values of scalars in megacoTonesMIB.
 */
struct megacoTonesMIB_data *
megacoTonesMIB_create(void)
{
	struct megacoTonesMIB_data *StorageNew = SNMP_MALLOC_STRUCT(megacoTonesMIB_data);

	DEBUGMSGTL(("megacoTonesMIB", "megacoTonesMIB_create: creating scalars...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default scalar values here into StorageNew */
		StorageNew->megTonesNextGroupId = 0;

	}
      done:
	DEBUGMSGTL(("megacoTonesMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	megacoTonesMIB_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct megacoTonesMIB_data *megacoTonesMIB_duplicate(struct megacoTonesMIB_data *thedata)
 * @param thedata the mib structure to duplicate
 * @brief duplicate a mib structure for the mib
 *
 * Duplicates the specified mib structure @param thedata and returns a pointer to the newly
 * allocated mib structure on success, or NULL on failure.
 */
struct megacoTonesMIB_data *
megacoTonesMIB_duplicate(struct megacoTonesMIB_data *thedata)
{
	struct megacoTonesMIB_data *StorageNew = SNMP_MALLOC_STRUCT(megacoTonesMIB_data);

	DEBUGMSGTL(("megacoTonesMIB", "megacoTonesMIB_duplicate: duplicating mib... "));
	if (StorageNew != NULL) {
		StorageNew->megTonesNextGroupId = thedata->megTonesNextGroupId;
	}
      done:
	DEBUGMSGTL(("megacoTonesMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	megacoTonesMIB_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int megacoTonesMIB_destroy(struct megacoTonesMIB_data **thedata)
 * @param thedata pointer to the data structure in megacoTonesMIB.
 * @brief delete a scalars structure from megacoTonesMIB.
 *
 * Frees scalars that were previously removed from megacoTonesMIB.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
megacoTonesMIB_destroy(struct megacoTonesMIB_data **thedata)
{
	struct megacoTonesMIB_data *StorageDel;

	DEBUGMSGTL(("megacoTonesMIB", "megacoTonesMIB_destroy: deleting scalars...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("megacoTonesMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int megacoTonesMIB_add(struct megacoTonesMIB_data *thedata)
 * @param thedata the structure representing megacoTonesMIB scalars.
 * @brief adds node to the megacoTonesMIB scalar data set.
 *
 * Adds a scalar structure to the megacoTonesMIB data set.  Note that this function is necessary even
 * when the scalar values are not peristent.
 */
int
megacoTonesMIB_add(struct megacoTonesMIB_data *thedata)
{
	DEBUGMSGTL(("megacoTonesMIB", "megacoTonesMIB_add: adding data...  "));
	if (thedata)
		megacoTonesMIBStorage = thedata;
	DEBUGMSGTL(("megacoTonesMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_megacoTonesMIB(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for megacoTonesMIB entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case megacoTonesMIB).  This routine is invoked by
 * UCD-SNMP to read the values of scalars in the MIB from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the MIB.  If there are no configured entries
 * in the configuration MIB, this function will simply not be called.
 */
void
parse_megacoTonesMIB(const char *token, char *line)
{
	size_t tmpsize;
	struct megacoTonesMIB_data *StorageTmp = megacoTonesMIB_create();

	DEBUGMSGTL(("megacoTonesMIB", "parse_megacoTonesMIB: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual scalars that are not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->megTonesNextGroupId, &tmpsize);
	megacoTonesMIB_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("megacoTonesMIB", "done.\n"));
}

/*
 * store_megacoTonesMIB(): stores .conf file entries needed to configure the mib.
 */
int
store_megacoTonesMIB(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct megacoTonesMIB_data *StorageTmp;

	DEBUGMSGTL(("megacoTonesMIB", "store_megacoTonesMIB: storing data...  "));
	refresh_megacoTonesMIB(1);
	if ((StorageTmp = megacoTonesMIBStorage) == NULL) {
		DEBUGMSGTL(("megacoTonesMIB", "error.\n"));
		return SNMPERR_GENERR;
	}
	(void) tmpsize;
	/* XXX: comment entire section if no scalars are persistent */
	{
		memset(line, 0, sizeof(line));
		strcat(line, "megacoTonesMIB ");
		cptr = line + strlen(line);
		(void) cptr;
		/* XXX: remove individual scalars that are not persistent */
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->megTonesNextGroupId, &tmpsize);
		snmpd_store_config(line);
	}
	DEBUGMSGTL(("megacoTonesMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int check_megacoTonesMIB(struct megacoTonesMIB_data *StorageTmp, struct megacoTonesMIB_data *StorageOld)
 * @param StorageTmp the data as updated
 * @param StorageOld the data previous to update
 *
 * This function is used by mibs.  It is used to check, all scalars at a time, the varbinds
 * belonging to the mib.  This function is called for the first varbind in a mib at the beginning of
 * the ACTION phase.  The COMMIT phase does not ensue unless this check passes.  This function can
 * return SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before
 * the varbinds on the mib were applied; the values in StorageTmp are the new values.  The function
 * is permitted to change the values in StorageTmp to correct them; however, preferences should be
 * made for setting values that were not in the varbinds.
 */
int
check_megacoTonesMIB(struct megacoTonesMIB_data *StorageTmp, struct megacoTonesMIB_data *StorageOld)
{
	/* XXX: provide code to check the scalars for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_megacoTonesMIB(struct megacoTonesMIB_data *StorageTmp, struct megacoTonesMIB_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase)
 *
 * This function is used by mibs.  It is used to update, all scalars at a time, the varbinds
 * belonging to the mib.  This function is called for the first varbind in a mib at the beginning of
 * the COMMIT phase.  The start of the ACTION phase performs a consistency check on the mib before
 * allowing the request to proceed to the COMMIT phase.  The COMMIT phase then arrives here with
 * consistency already checked (see check_megacoTonesMIB()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the mib were applied: the values in StorageTmp are the new values.
 */
int
update_megacoTonesMIB(struct megacoTonesMIB_data *StorageTmp, struct megacoTonesMIB_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	megacoTonesMIB_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn revert_megacoTonesMIB(struct 
 * @fn void revert_megacoTonesMIB(struct megacoTonesMIB_data *StorageTmp, struct megacoTonesMIB_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase)
 */
void
revert_megacoTonesMIB(struct megacoTonesMIB_data *StorageTmp, struct megacoTonesMIB_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_megacoTonesMIB(StorageOld, NULL);
}

/**
 * @fn void refresh_megacoTonesMIB(int force)
 * @param force forced refresh when non-zero.
 * @brief refresh the scalar values of megacoTonesMIB.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a scalar has been requested).
 */
void
refresh_megacoTonesMIB(int force)
{
	if (megacoTonesMIBStorage == NULL) {
		struct megacoTonesMIB_data *StorageNew;

		if ((StorageNew = megacoTonesMIB_create()) == NULL)
			return;
		megacoTonesMIBStorage = StorageNew;
		megacoTonesMIB_refresh = 1;
	}
	if (!force && megacoTonesMIB_refresh == 0)
		return;
	DEBUGMSGTL(("megacoTonesMIB", "refresh_megacoTonesMIB: refreshing...  "));
	/* XXX: Update scalars as required here... */
	megacoTonesMIB_refresh = 0;
	DEBUGMSGTL(("megacoTonesMIB", "done.\n"));
}

/**
 * @fn u_char * var_megacoTonesMIB(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @param vp a pointer to the entry in the variables table for the requested variable.
 * @param name the object identifier for which to find.
 * @param length the length of the object identifier.
 * @param exact whether the name is exact.
 * @param var_len a pointer to the length of the representation of the object.
 * @param write_method a pointer to a write method for the object.
 * @brief locate variables in megacoTonesMIB.
 *
 * This function returns a pointer to a memory area that is static across the request that contains
 * the UCD-SNMP representation of the scalar (so that it may be used to read from for a GET,
 * GET-NEXT or GET-BULK request).  This returned pointer may be NULL, in which case the function is
 * telling UCD-SNMP that the scalar does not exist for reading; however, if write_method is
 * overwritten with a non-NULL value, the function is telling UCD-SNMP that the scalar exists for
 * writing.  Write-only objects can be effected in this way.
 */
u_char *
var_megacoTonesMIB(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct megacoTonesMIB_data *StorageTmp;
	u_char *rval;

	DEBUGMSGTL(("megacoTonesMIB", "var_megacoTonesMIB: lookup up varbind...  "));
	if (header_generic(vp, name, length, exact, var_len, write_method) == MATCH_FAILED)
		return NULL;
	/* Refresh the MIB values if required. */
	refresh_megacoTonesMIB(0);
	if ((StorageTmp = megacoTonesMIBStorage) == NULL) {
		DEBUGMSGTL(("megacoTonesMIB", "no datastructure.\n"));
		return NULL;
	}
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) MEGTONESNEXTGROUPID:	/* ReadWrite */
		*write_method = write_megTonesNextGroupId;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->megTonesNextGroupId);
		rval = (u_char *) &StorageTmp->megTonesNextGroupId;
		break;
	default:
		ERROR_MSG("");
	}
	if (rval)
		DEBUGMSGTL(("megacoTonesMIB", "found.\n"));
	else
		DEBUGMSGTL(("megacoTonesMIB", "not found.\n"));
	return (rval);
}

/**
 * @fn struct megTonesToneTable_data *megTonesToneTable_create(void)
 * @brief create a fresh data structure representing a new row in the megTonesToneTable table.
 *
 * Creates a new megTonesToneTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct megTonesToneTable_data *
megTonesToneTable_create(void)
{
	struct megTonesToneTable_data *StorageNew = SNMP_MALLOC_STRUCT(megTonesToneTable_data);

	DEBUGMSGTL(("megacoTonesMIB", "megTonesToneTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->megTonesToneGroupId = 0;
		if ((StorageNew->megTonesToneName = malloc(1)) == NULL)
			goto nomem;
		StorageNew->megTonesToneNameLen = 0;
		StorageNew->megTonesToneName[StorageNew->megTonesToneNameLen] = 0;
		StorageNew->megTonesToneTimeout = 0;
		StorageNew->megTonesToneRowStatus = 0;
		StorageNew->megTonesToneRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("megacoTonesMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	megTonesToneTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct megTonesToneTable_data *megTonesToneTable_duplicate(struct megTonesToneTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct megTonesToneTable_data *
megTonesToneTable_duplicate(struct megTonesToneTable_data *thedata)
{
	struct megTonesToneTable_data *StorageNew = SNMP_MALLOC_STRUCT(megTonesToneTable_data);

	DEBUGMSGTL(("megacoTonesMIB", "megTonesToneTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->megTonesToneTable_id = thedata->megTonesToneTable_id;
		StorageNew->megTonesToneSetId = thedata->megTonesToneSetId;
		StorageNew->megTonesToneId = thedata->megTonesToneId;
		StorageNew->megTonesToneGroupId = thedata->megTonesToneGroupId;
		if (!(StorageNew->megTonesToneName = malloc(thedata->megTonesToneNameLen + 1)))
			goto destroy;
		memcpy(StorageNew->megTonesToneName, thedata->megTonesToneName, thedata->megTonesToneNameLen);
		StorageNew->megTonesToneNameLen = thedata->megTonesToneNameLen;
		StorageNew->megTonesToneName[StorageNew->megTonesToneNameLen] = 0;
		StorageNew->megTonesToneTimeout = thedata->megTonesToneTimeout;
		StorageNew->megTonesToneRowStatus = thedata->megTonesToneRowStatus;
	}
      done:
	DEBUGMSGTL(("megacoTonesMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	megTonesToneTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int megTonesToneTable_destroy(struct megTonesToneTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
megTonesToneTable_destroy(struct megTonesToneTable_data **thedata)
{
	struct megTonesToneTable_data *StorageDel;

	DEBUGMSGTL(("megacoTonesMIB", "megTonesToneTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->megTonesToneName);
		StorageDel->megTonesToneNameLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("megacoTonesMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int megTonesToneTable_add(struct megTonesToneTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the megTonesToneTable table data set.
 *
 * Adds a table row structure to the megTonesToneTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
megTonesToneTable_add(struct megTonesToneTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("megacoTonesMIB", "megTonesToneTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* megTonesToneSetId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->megTonesToneSetId, sizeof(thedata->megTonesToneSetId));
		/* megTonesToneId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->megTonesToneId, sizeof(thedata->megTonesToneId));
		header_complex_add_data(&megTonesToneTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("megacoTonesMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int megTonesToneTable_del(struct megTonesToneTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the megTonesToneTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
megTonesToneTable_del(struct megTonesToneTable_data *thedata)
{
	struct megTonesToneTable_data *StorageDel;

	DEBUGMSGTL(("megacoTonesMIB", "megTonesToneTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(megTonesToneTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&megTonesToneTableStorage, hciptr);
	}
	DEBUGMSGTL(("megacoTonesMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_megTonesToneTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for megTonesToneTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case megTonesToneTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_megTonesToneTable(const char *token, char *line)
{
	size_t tmpsize;
	struct megTonesToneTable_data *StorageTmp = megTonesToneTable_create();

	DEBUGMSGTL(("megacoTonesMIB", "parse_megTonesToneTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->megTonesToneSetId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->megTonesToneId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->megTonesToneGroupId, &tmpsize);
	SNMP_FREE(StorageTmp->megTonesToneName);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->megTonesToneName, &StorageTmp->megTonesToneNameLen);
	if (StorageTmp->megTonesToneName == NULL) {
		config_perror("invalid specification for megTonesToneName");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->megTonesToneTimeout, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->megTonesToneRowStatus, &tmpsize);
	megTonesToneTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("megacoTonesMIB", "done.\n"));
}

/*
 * store_megTonesToneTable(): store configuraiton file for megTonesToneTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_megTonesToneTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct megTonesToneTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("megacoTonesMIB", "store_megTonesToneTable: storing data...  "));
	refresh_megTonesToneTable(1);
	(void) tmpsize;
	for (hcindex = megTonesToneTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct megTonesToneTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "megTonesToneTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->megTonesToneSetId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->megTonesToneId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->megTonesToneGroupId, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->megTonesToneName, &StorageTmp->megTonesToneNameLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->megTonesToneTimeout, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->megTonesToneRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("megacoTonesMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct megTonesGroupTable_data *megTonesGroupTable_create(void)
 * @brief create a fresh data structure representing a new row in the megTonesGroupTable table.
 *
 * Creates a new megTonesGroupTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct megTonesGroupTable_data *
megTonesGroupTable_create(void)
{
	struct megTonesGroupTable_data *StorageNew = SNMP_MALLOC_STRUCT(megTonesGroupTable_data);

	DEBUGMSGTL(("megacoTonesMIB", "megTonesGroupTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->megTonesToneGroupId = 0;
		StorageNew->megTonesGroupRef = 0;
		StorageNew->megTonesGroupGroupLink = 0;
		StorageNew->megTonesGroupFrequency = 0;
		StorageNew->megTonesGroupLevel = 0;
		StorageNew->megTonesGroupDuration = 0;
		StorageNew->megTonesGroupRepeatCount = 0;
		StorageNew->megTonesGroupRowStatus = 0;
		StorageNew->megTonesGroupRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("megacoTonesMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	megTonesGroupTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct megTonesGroupTable_data *megTonesGroupTable_duplicate(struct megTonesGroupTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct megTonesGroupTable_data *
megTonesGroupTable_duplicate(struct megTonesGroupTable_data *thedata)
{
	struct megTonesGroupTable_data *StorageNew = SNMP_MALLOC_STRUCT(megTonesGroupTable_data);

	DEBUGMSGTL(("megacoTonesMIB", "megTonesGroupTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->megTonesGroupTable_id = thedata->megTonesGroupTable_id;
		StorageNew->megTonesToneGroupId = thedata->megTonesToneGroupId;
		StorageNew->megTonesGroupIndex = thedata->megTonesGroupIndex;
		StorageNew->megTonesGroupRef = thedata->megTonesGroupRef;
		StorageNew->megTonesGroupGroupLink = thedata->megTonesGroupGroupLink;
		StorageNew->megTonesGroupFrequency = thedata->megTonesGroupFrequency;
		StorageNew->megTonesGroupLevel = thedata->megTonesGroupLevel;
		StorageNew->megTonesGroupDuration = thedata->megTonesGroupDuration;
		StorageNew->megTonesGroupRepeatCount = thedata->megTonesGroupRepeatCount;
		StorageNew->megTonesGroupRowStatus = thedata->megTonesGroupRowStatus;
	}
      done:
	DEBUGMSGTL(("megacoTonesMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	megTonesGroupTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int megTonesGroupTable_destroy(struct megTonesGroupTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
megTonesGroupTable_destroy(struct megTonesGroupTable_data **thedata)
{
	struct megTonesGroupTable_data *StorageDel;

	DEBUGMSGTL(("megacoTonesMIB", "megTonesGroupTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("megacoTonesMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int megTonesGroupTable_add(struct megTonesGroupTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the megTonesGroupTable table data set.
 *
 * Adds a table row structure to the megTonesGroupTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
megTonesGroupTable_add(struct megTonesGroupTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("megacoTonesMIB", "megTonesGroupTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* megTonesToneGroupId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->megTonesToneGroupId, sizeof(thedata->megTonesToneGroupId));
		/* megTonesGroupIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->megTonesGroupIndex, sizeof(thedata->megTonesGroupIndex));
		header_complex_add_data(&megTonesGroupTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("megacoTonesMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int megTonesGroupTable_del(struct megTonesGroupTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the megTonesGroupTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
megTonesGroupTable_del(struct megTonesGroupTable_data *thedata)
{
	struct megTonesGroupTable_data *StorageDel;

	DEBUGMSGTL(("megacoTonesMIB", "megTonesGroupTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(megTonesGroupTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&megTonesGroupTableStorage, hciptr);
	}
	DEBUGMSGTL(("megacoTonesMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_megTonesGroupTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for megTonesGroupTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case megTonesGroupTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_megTonesGroupTable(const char *token, char *line)
{
	size_t tmpsize;
	struct megTonesGroupTable_data *StorageTmp = megTonesGroupTable_create();

	DEBUGMSGTL(("megacoTonesMIB", "parse_megTonesGroupTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->megTonesToneGroupId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->megTonesGroupIndex, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->megTonesGroupRef, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->megTonesGroupGroupLink, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->megTonesGroupFrequency, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->megTonesGroupLevel, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->megTonesGroupDuration, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->megTonesGroupRepeatCount, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->megTonesGroupRowStatus, &tmpsize);
	megTonesGroupTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("megacoTonesMIB", "done.\n"));
}

/*
 * store_megTonesGroupTable(): store configuraiton file for megTonesGroupTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_megTonesGroupTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct megTonesGroupTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("megacoTonesMIB", "store_megTonesGroupTable: storing data...  "));
	refresh_megTonesGroupTable(1);
	(void) tmpsize;
	for (hcindex = megTonesGroupTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct megTonesGroupTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "megTonesGroupTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->megTonesToneGroupId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->megTonesGroupIndex, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->megTonesGroupRef, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->megTonesGroupGroupLink, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->megTonesGroupFrequency, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->megTonesGroupLevel, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->megTonesGroupDuration, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->megTonesGroupRepeatCount, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->megTonesGroupRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("megacoTonesMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int activate_megTonesToneTable_row(struct megTonesToneTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_megTonesToneTable_row(struct megTonesToneTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_megTonesToneTable_row(struct megTonesToneTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_megTonesToneTable_row(struct megTonesToneTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_megTonesGroupTable_row(struct megTonesGroupTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_megTonesGroupTable_row(struct megTonesGroupTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_megTonesGroupTable_row(struct megTonesGroupTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_megTonesGroupTable_row(struct megTonesGroupTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int check_megTonesToneTable_row(struct megTonesToneTable_data *StorageTmp, struct megTonesToneTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_megTonesToneTable_row(struct megTonesToneTable_data *StorageTmp, struct megTonesToneTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_megTonesToneTable_row(struct megTonesToneTable_data *StorageTmp, struct megTonesToneTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_megTonesToneTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_megTonesToneTable_row(struct megTonesToneTable_data *StorageTmp, struct megTonesToneTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	megTonesToneTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_megTonesToneTable_row(struct megTonesToneTable_data *StorageTmp, struct megTonesToneTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_megTonesToneTable_row(struct megTonesToneTable_data *StorageTmp, struct megTonesToneTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_megTonesToneTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_megTonesToneTable_row(struct megTonesToneTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the megTonesToneTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct megTonesToneTable_data *
refresh_megTonesToneTable_row(struct megTonesToneTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->megTonesToneTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->megTonesToneTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_megTonesToneTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the megTonesToneTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_megTonesToneTable(int force)
{
	if (!force && megTonesToneTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	megTonesToneTable_refresh = 0;
}

/**
 * @fn u_char *var_megTonesToneTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in megTonesToneTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_megacoTonesMIB above.
 */
u_char *
var_megTonesToneTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct megTonesToneTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("megacoTonesMIB", "var_megTonesToneTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_megTonesToneTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(megTonesToneTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_megTonesToneTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) MEGTONESTONEGROUPID:	/* Create */
		*write_method = write_megTonesToneGroupId;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->megTonesToneGroupId);
		rval = (u_char *) &StorageTmp->megTonesToneGroupId;
		break;
	case (u_char) MEGTONESTONENAME:	/* Create */
		*write_method = write_megTonesToneName;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->megTonesToneNameLen;
		rval = (u_char *) StorageTmp->megTonesToneName;
		break;
	case (u_char) MEGTONESTONETIMEOUT:	/* Create */
		*write_method = write_megTonesToneTimeout;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->megTonesToneTimeout);
		rval = (u_char *) &StorageTmp->megTonesToneTimeout;
		break;
	case (u_char) MEGTONESTONEROWSTATUS:	/* Create */
		*write_method = write_megTonesToneRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->megTonesToneRowStatus);
		rval = (u_char *) &StorageTmp->megTonesToneRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_megTonesGroupTable_row(struct megTonesGroupTable_data *StorageTmp, struct megTonesGroupTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_megTonesGroupTable_row(struct megTonesGroupTable_data *StorageTmp, struct megTonesGroupTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_megTonesGroupTable_row(struct megTonesGroupTable_data *StorageTmp, struct megTonesGroupTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_megTonesGroupTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_megTonesGroupTable_row(struct megTonesGroupTable_data *StorageTmp, struct megTonesGroupTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	megTonesGroupTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_megTonesGroupTable_row(struct megTonesGroupTable_data *StorageTmp, struct megTonesGroupTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_megTonesGroupTable_row(struct megTonesGroupTable_data *StorageTmp, struct megTonesGroupTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_megTonesGroupTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_megTonesGroupTable_row(struct megTonesGroupTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the megTonesGroupTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct megTonesGroupTable_data *
refresh_megTonesGroupTable_row(struct megTonesGroupTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->megTonesGroupTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->megTonesGroupTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_megTonesGroupTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the megTonesGroupTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_megTonesGroupTable(int force)
{
	if (!force && megTonesGroupTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	megTonesGroupTable_refresh = 0;
}

/**
 * @fn u_char *var_megTonesGroupTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in megTonesGroupTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_megacoTonesMIB above.
 */
u_char *
var_megTonesGroupTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct megTonesGroupTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("megacoTonesMIB", "var_megTonesGroupTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_megTonesGroupTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(megTonesGroupTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_megTonesGroupTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) MEGTONESGROUPREF:	/* Create */
		*write_method = write_megTonesGroupRef;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->megTonesGroupRef);
		rval = (u_char *) &StorageTmp->megTonesGroupRef;
		break;
	case (u_char) MEGTONESGROUPGROUPLINK:	/* Create */
		*write_method = write_megTonesGroupGroupLink;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->megTonesGroupGroupLink);
		rval = (u_char *) &StorageTmp->megTonesGroupGroupLink;
		break;
	case (u_char) MEGTONESGROUPFREQUENCY:	/* Create */
		*write_method = write_megTonesGroupFrequency;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->megTonesGroupFrequency);
		rval = (u_char *) &StorageTmp->megTonesGroupFrequency;
		break;
	case (u_char) MEGTONESGROUPLEVEL:	/* Create */
		*write_method = write_megTonesGroupLevel;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->megTonesGroupLevel);
		rval = (u_char *) &StorageTmp->megTonesGroupLevel;
		break;
	case (u_char) MEGTONESGROUPDURATION:	/* Create */
		*write_method = write_megTonesGroupDuration;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->megTonesGroupDuration);
		rval = (u_char *) &StorageTmp->megTonesGroupDuration;
		break;
	case (u_char) MEGTONESGROUPREPEATCOUNT:	/* Create */
		*write_method = write_megTonesGroupRepeatCount;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->megTonesGroupRepeatCount);
		rval = (u_char *) &StorageTmp->megTonesGroupRepeatCount;
		break;
	case (u_char) MEGTONESGROUPROWSTATUS:	/* Create */
		*write_method = write_megTonesGroupRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->megTonesGroupRowStatus);
		rval = (u_char *) &StorageTmp->megTonesGroupRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int write_megTonesToneGroupId(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_megTonesToneGroupId(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct megTonesToneTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("megacoTonesMIB", "write_megTonesToneGroupId entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(megTonesToneTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->megTonesToneRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to megTonesToneGroupId: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to megTonesToneGroupId not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to megTonesToneGroupId: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 1..2147483647 */
		if ((1 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to megTonesToneGroupId: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->megTonesToneTable_old) == NULL)
			if (StorageTmp->megTonesToneTable_rsvs == 0)
				if ((StorageOld = StorageTmp->megTonesToneTable_old = megTonesToneTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->megTonesToneTable_rsvs++;
		StorageTmp->megTonesToneGroupId = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->megTonesToneTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->megTonesToneTable_tsts == 0)
				if ((ret = check_megTonesToneTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->megTonesToneTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->megTonesToneGroupId for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->megTonesToneTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->megTonesToneTable_sets == 0)
				if ((ret = update_megTonesToneTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->megTonesToneTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->megTonesToneTable_old) != NULL) {
			megTonesToneTable_destroy(&StorageTmp->megTonesToneTable_old);
			StorageTmp->megTonesToneTable_rsvs = 0;
			StorageTmp->megTonesToneTable_tsts = 0;
			StorageTmp->megTonesToneTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->megTonesToneTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->megTonesToneTable_sets == 0)
			revert_megTonesToneTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->megTonesToneTable_old) == NULL)
			break;
		StorageTmp->megTonesToneGroupId = StorageOld->megTonesToneGroupId;
		if (--StorageTmp->megTonesToneTable_rsvs == 0)
			megTonesToneTable_destroy(&StorageTmp->megTonesToneTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_megTonesToneName(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_megTonesToneName(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct megTonesToneTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("megacoTonesMIB", "write_megTonesToneName entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(megTonesToneTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->megTonesToneRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to megTonesToneName: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to megTonesToneName not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 0..255 */
		if (var_val_len > SPRINT_MAX_LEN || ((0 > var_val_len || var_val_len > 255))) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to megTonesToneName: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->megTonesToneTable_old) == NULL)
			if (StorageTmp->megTonesToneTable_rsvs == 0)
				if ((StorageOld = StorageTmp->megTonesToneTable_old = megTonesToneTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->megTonesToneTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->megTonesToneName);
		StorageTmp->megTonesToneName = string;
		StorageTmp->megTonesToneNameLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->megTonesToneTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->megTonesToneTable_tsts == 0)
				if ((ret = check_megTonesToneTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->megTonesToneTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->megTonesToneName for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->megTonesToneTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->megTonesToneTable_sets == 0)
				if ((ret = update_megTonesToneTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->megTonesToneTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->megTonesToneTable_old) != NULL) {
			megTonesToneTable_destroy(&StorageTmp->megTonesToneTable_old);
			StorageTmp->megTonesToneTable_rsvs = 0;
			StorageTmp->megTonesToneTable_tsts = 0;
			StorageTmp->megTonesToneTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->megTonesToneTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->megTonesToneTable_sets == 0)
			revert_megTonesToneTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->megTonesToneTable_old) == NULL)
			break;
		if (StorageOld->megTonesToneName != NULL) {
			SNMP_FREE(StorageTmp->megTonesToneName);
			StorageTmp->megTonesToneName = StorageOld->megTonesToneName;
			StorageTmp->megTonesToneNameLen = StorageOld->megTonesToneNameLen;
			StorageOld->megTonesToneName = NULL;
			StorageOld->megTonesToneNameLen = 0;
		}
		if (--StorageTmp->megTonesToneTable_rsvs == 0)
			megTonesToneTable_destroy(&StorageTmp->megTonesToneTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_megTonesToneTimeout(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_megTonesToneTimeout(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct megTonesToneTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("megacoTonesMIB", "write_megTonesToneTimeout entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(megTonesToneTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->megTonesToneRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to megTonesToneTimeout: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to megTonesToneTimeout not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to megTonesToneTimeout: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->megTonesToneTable_old) == NULL)
			if (StorageTmp->megTonesToneTable_rsvs == 0)
				if ((StorageOld = StorageTmp->megTonesToneTable_old = megTonesToneTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->megTonesToneTable_rsvs++;
		StorageTmp->megTonesToneTimeout = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->megTonesToneTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->megTonesToneTable_tsts == 0)
				if ((ret = check_megTonesToneTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->megTonesToneTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->megTonesToneTimeout for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->megTonesToneTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->megTonesToneTable_sets == 0)
				if ((ret = update_megTonesToneTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->megTonesToneTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->megTonesToneTable_old) != NULL) {
			megTonesToneTable_destroy(&StorageTmp->megTonesToneTable_old);
			StorageTmp->megTonesToneTable_rsvs = 0;
			StorageTmp->megTonesToneTable_tsts = 0;
			StorageTmp->megTonesToneTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->megTonesToneTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->megTonesToneTable_sets == 0)
			revert_megTonesToneTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->megTonesToneTable_old) == NULL)
			break;
		StorageTmp->megTonesToneTimeout = StorageOld->megTonesToneTimeout;
		if (--StorageTmp->megTonesToneTable_rsvs == 0)
			megTonesToneTable_destroy(&StorageTmp->megTonesToneTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_megTonesToneGroupId(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_megTonesToneGroupId(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct megTonesGroupTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("megacoTonesMIB", "write_megTonesToneGroupId entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(megTonesGroupTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->megTonesGroupRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to megTonesToneGroupId: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to megTonesToneGroupId not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to megTonesToneGroupId: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 1..2147483647 */
		if ((1 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to megTonesToneGroupId: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->megTonesGroupTable_old) == NULL)
			if (StorageTmp->megTonesGroupTable_rsvs == 0)
				if ((StorageOld = StorageTmp->megTonesGroupTable_old = megTonesGroupTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->megTonesGroupTable_rsvs++;
		StorageTmp->megTonesToneGroupId = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->megTonesGroupTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->megTonesGroupTable_tsts == 0)
				if ((ret = check_megTonesGroupTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->megTonesGroupTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->megTonesToneGroupId for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->megTonesGroupTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->megTonesGroupTable_sets == 0)
				if ((ret = update_megTonesGroupTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->megTonesGroupTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->megTonesGroupTable_old) != NULL) {
			megTonesGroupTable_destroy(&StorageTmp->megTonesGroupTable_old);
			StorageTmp->megTonesGroupTable_rsvs = 0;
			StorageTmp->megTonesGroupTable_tsts = 0;
			StorageTmp->megTonesGroupTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->megTonesGroupTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->megTonesGroupTable_sets == 0)
			revert_megTonesGroupTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->megTonesGroupTable_old) == NULL)
			break;
		StorageTmp->megTonesToneGroupId = StorageOld->megTonesToneGroupId;
		if (--StorageTmp->megTonesGroupTable_rsvs == 0)
			megTonesGroupTable_destroy(&StorageTmp->megTonesGroupTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_megTonesGroupRef(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_megTonesGroupRef(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct megTonesGroupTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("megacoTonesMIB", "write_megTonesGroupRef entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(megTonesGroupTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->megTonesGroupRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to megTonesGroupRef: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to megTonesGroupRef not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to megTonesGroupRef: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->megTonesGroupTable_old) == NULL)
			if (StorageTmp->megTonesGroupTable_rsvs == 0)
				if ((StorageOld = StorageTmp->megTonesGroupTable_old = megTonesGroupTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->megTonesGroupTable_rsvs++;
		StorageTmp->megTonesGroupRef = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->megTonesGroupTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->megTonesGroupTable_tsts == 0)
				if ((ret = check_megTonesGroupTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->megTonesGroupTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->megTonesGroupRef for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->megTonesGroupTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->megTonesGroupTable_sets == 0)
				if ((ret = update_megTonesGroupTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->megTonesGroupTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->megTonesGroupTable_old) != NULL) {
			megTonesGroupTable_destroy(&StorageTmp->megTonesGroupTable_old);
			StorageTmp->megTonesGroupTable_rsvs = 0;
			StorageTmp->megTonesGroupTable_tsts = 0;
			StorageTmp->megTonesGroupTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->megTonesGroupTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->megTonesGroupTable_sets == 0)
			revert_megTonesGroupTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->megTonesGroupTable_old) == NULL)
			break;
		StorageTmp->megTonesGroupRef = StorageOld->megTonesGroupRef;
		if (--StorageTmp->megTonesGroupTable_rsvs == 0)
			megTonesGroupTable_destroy(&StorageTmp->megTonesGroupTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_megTonesGroupGroupLink(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_megTonesGroupGroupLink(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct megTonesGroupTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("megacoTonesMIB", "write_megTonesGroupGroupLink entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(megTonesGroupTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->megTonesGroupRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to megTonesGroupGroupLink: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to megTonesGroupGroupLink not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to megTonesGroupGroupLink: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case MEGTONESGROUPGROUPLINK_SEQ:
		case MEGTONESGROUPGROUPLINK_MIX:
		case MEGTONESGROUPGROUPLINK_MODAMP:
		case MEGTONESGROUPGROUPLINK_MODFREQ:
		case MEGTONESGROUPGROUPLINK_PCM:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to megTonesGroupGroupLink: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->megTonesGroupTable_old) == NULL)
			if (StorageTmp->megTonesGroupTable_rsvs == 0)
				if ((StorageOld = StorageTmp->megTonesGroupTable_old = megTonesGroupTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->megTonesGroupTable_rsvs++;
		StorageTmp->megTonesGroupGroupLink = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->megTonesGroupTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->megTonesGroupTable_tsts == 0)
				if ((ret = check_megTonesGroupTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->megTonesGroupTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->megTonesGroupGroupLink for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->megTonesGroupTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->megTonesGroupTable_sets == 0)
				if ((ret = update_megTonesGroupTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->megTonesGroupTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->megTonesGroupTable_old) != NULL) {
			megTonesGroupTable_destroy(&StorageTmp->megTonesGroupTable_old);
			StorageTmp->megTonesGroupTable_rsvs = 0;
			StorageTmp->megTonesGroupTable_tsts = 0;
			StorageTmp->megTonesGroupTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->megTonesGroupTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->megTonesGroupTable_sets == 0)
			revert_megTonesGroupTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->megTonesGroupTable_old) == NULL)
			break;
		StorageTmp->megTonesGroupGroupLink = StorageOld->megTonesGroupGroupLink;
		if (--StorageTmp->megTonesGroupTable_rsvs == 0)
			megTonesGroupTable_destroy(&StorageTmp->megTonesGroupTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_megTonesGroupFrequency(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_megTonesGroupFrequency(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct megTonesGroupTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("megacoTonesMIB", "write_megTonesGroupFrequency entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(megTonesGroupTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->megTonesGroupRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to megTonesGroupFrequency: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to megTonesGroupFrequency not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to megTonesGroupFrequency: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..4000 */
		if ((0 > set_value || set_value > 4000)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to megTonesGroupFrequency: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->megTonesGroupTable_old) == NULL)
			if (StorageTmp->megTonesGroupTable_rsvs == 0)
				if ((StorageOld = StorageTmp->megTonesGroupTable_old = megTonesGroupTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->megTonesGroupTable_rsvs++;
		StorageTmp->megTonesGroupFrequency = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->megTonesGroupTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->megTonesGroupTable_tsts == 0)
				if ((ret = check_megTonesGroupTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->megTonesGroupTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->megTonesGroupFrequency for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->megTonesGroupTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->megTonesGroupTable_sets == 0)
				if ((ret = update_megTonesGroupTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->megTonesGroupTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->megTonesGroupTable_old) != NULL) {
			megTonesGroupTable_destroy(&StorageTmp->megTonesGroupTable_old);
			StorageTmp->megTonesGroupTable_rsvs = 0;
			StorageTmp->megTonesGroupTable_tsts = 0;
			StorageTmp->megTonesGroupTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->megTonesGroupTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->megTonesGroupTable_sets == 0)
			revert_megTonesGroupTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->megTonesGroupTable_old) == NULL)
			break;
		StorageTmp->megTonesGroupFrequency = StorageOld->megTonesGroupFrequency;
		if (--StorageTmp->megTonesGroupTable_rsvs == 0)
			megTonesGroupTable_destroy(&StorageTmp->megTonesGroupTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_megTonesGroupLevel(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_megTonesGroupLevel(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct megTonesGroupTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("megacoTonesMIB", "write_megTonesGroupLevel entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(megTonesGroupTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->megTonesGroupRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to megTonesGroupLevel: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to megTonesGroupLevel not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to megTonesGroupLevel: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges -128..0 */
		if ((-128 > set_value || set_value > 0)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to megTonesGroupLevel: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->megTonesGroupTable_old) == NULL)
			if (StorageTmp->megTonesGroupTable_rsvs == 0)
				if ((StorageOld = StorageTmp->megTonesGroupTable_old = megTonesGroupTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->megTonesGroupTable_rsvs++;
		StorageTmp->megTonesGroupLevel = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->megTonesGroupTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->megTonesGroupTable_tsts == 0)
				if ((ret = check_megTonesGroupTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->megTonesGroupTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->megTonesGroupLevel for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->megTonesGroupTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->megTonesGroupTable_sets == 0)
				if ((ret = update_megTonesGroupTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->megTonesGroupTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->megTonesGroupTable_old) != NULL) {
			megTonesGroupTable_destroy(&StorageTmp->megTonesGroupTable_old);
			StorageTmp->megTonesGroupTable_rsvs = 0;
			StorageTmp->megTonesGroupTable_tsts = 0;
			StorageTmp->megTonesGroupTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->megTonesGroupTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->megTonesGroupTable_sets == 0)
			revert_megTonesGroupTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->megTonesGroupTable_old) == NULL)
			break;
		StorageTmp->megTonesGroupLevel = StorageOld->megTonesGroupLevel;
		if (--StorageTmp->megTonesGroupTable_rsvs == 0)
			megTonesGroupTable_destroy(&StorageTmp->megTonesGroupTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_megTonesGroupDuration(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_megTonesGroupDuration(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct megTonesGroupTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("megacoTonesMIB", "write_megTonesGroupDuration entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(megTonesGroupTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->megTonesGroupRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to megTonesGroupDuration: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to megTonesGroupDuration not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to megTonesGroupDuration: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..32767 */
		if ((0 > set_value || set_value > 32767)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to megTonesGroupDuration: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->megTonesGroupTable_old) == NULL)
			if (StorageTmp->megTonesGroupTable_rsvs == 0)
				if ((StorageOld = StorageTmp->megTonesGroupTable_old = megTonesGroupTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->megTonesGroupTable_rsvs++;
		StorageTmp->megTonesGroupDuration = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->megTonesGroupTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->megTonesGroupTable_tsts == 0)
				if ((ret = check_megTonesGroupTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->megTonesGroupTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->megTonesGroupDuration for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->megTonesGroupTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->megTonesGroupTable_sets == 0)
				if ((ret = update_megTonesGroupTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->megTonesGroupTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->megTonesGroupTable_old) != NULL) {
			megTonesGroupTable_destroy(&StorageTmp->megTonesGroupTable_old);
			StorageTmp->megTonesGroupTable_rsvs = 0;
			StorageTmp->megTonesGroupTable_tsts = 0;
			StorageTmp->megTonesGroupTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->megTonesGroupTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->megTonesGroupTable_sets == 0)
			revert_megTonesGroupTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->megTonesGroupTable_old) == NULL)
			break;
		StorageTmp->megTonesGroupDuration = StorageOld->megTonesGroupDuration;
		if (--StorageTmp->megTonesGroupTable_rsvs == 0)
			megTonesGroupTable_destroy(&StorageTmp->megTonesGroupTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_megTonesGroupRepeatCount(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_megTonesGroupRepeatCount(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct megTonesGroupTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 12;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("megacoTonesMIB", "write_megTonesGroupRepeatCount entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(megTonesGroupTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->megTonesGroupRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to megTonesGroupRepeatCount: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to megTonesGroupRepeatCount not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to megTonesGroupRepeatCount: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..32767 */
		if ((0 > set_value || set_value > 32767)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to megTonesGroupRepeatCount: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->megTonesGroupTable_old) == NULL)
			if (StorageTmp->megTonesGroupTable_rsvs == 0)
				if ((StorageOld = StorageTmp->megTonesGroupTable_old = megTonesGroupTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->megTonesGroupTable_rsvs++;
		StorageTmp->megTonesGroupRepeatCount = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->megTonesGroupTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->megTonesGroupTable_tsts == 0)
				if ((ret = check_megTonesGroupTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->megTonesGroupTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->megTonesGroupRepeatCount for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->megTonesGroupTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->megTonesGroupTable_sets == 0)
				if ((ret = update_megTonesGroupTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->megTonesGroupTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->megTonesGroupTable_old) != NULL) {
			megTonesGroupTable_destroy(&StorageTmp->megTonesGroupTable_old);
			StorageTmp->megTonesGroupTable_rsvs = 0;
			StorageTmp->megTonesGroupTable_tsts = 0;
			StorageTmp->megTonesGroupTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->megTonesGroupTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->megTonesGroupTable_sets == 0)
			revert_megTonesGroupTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->megTonesGroupTable_old) == NULL)
			break;
		StorageTmp->megTonesGroupRepeatCount = StorageOld->megTonesGroupRepeatCount;
		if (--StorageTmp->megTonesGroupTable_rsvs == 0)
			megTonesGroupTable_destroy(&StorageTmp->megTonesGroupTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_megTonesNextGroupId(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_megTonesNextGroupId(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct megacoTonesMIB_data *StorageTmp = NULL, *StorageOld = NULL;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("megacoTonesMIB", "write_megTonesNextGroupId entering action=%d...  \n", action));
	if ((StorageTmp = megacoTonesMIBStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to megTonesNextGroupId not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to megTonesNextGroupId: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to megTonesNextGroupId: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for whole mib */
		if ((StorageOld = StorageTmp->megacoTonesMIB_old) == NULL)
			if (StorageTmp->megacoTonesMIB_rsvs == 0)
				if ((StorageOld = StorageTmp->megacoTonesMIB_old = megacoTonesMIB_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->megacoTonesMIB_rsvs++;
		StorageTmp->megTonesNextGroupId = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->megacoTonesMIB_old) != NULL) {
			/* one consistency check for the whole mib */
			if (StorageTmp->megacoTonesMIB_tsts == 0)
				if ((ret = check_megacoTonesMIB(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->megacoTonesMIB_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->megTonesNextGroupId for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole mib */
		if ((StorageOld = StorageTmp->megacoTonesMIB_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->megacoTonesMIB_sets == 0)
				if ((ret = update_megacoTonesMIB(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->megacoTonesMIB_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->megacoTonesMIB_old) != NULL) {
			megacoTonesMIB_destroy(&StorageTmp->megacoTonesMIB_old);
			StorageTmp->megacoTonesMIB_rsvs = 0;
			StorageTmp->megacoTonesMIB_tsts = 0;
			StorageTmp->megacoTonesMIB_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->megacoTonesMIB_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->megacoTonesMIB_tsts == 0)
			revert_megacoTonesMIB(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->megacoTonesMIB_old) == NULL)
			break;
		StorageTmp->megTonesNextGroupId = StorageOld->megTonesNextGroupId;
		if (--StorageTmp->megacoTonesMIB_rsvs == 0)
			megacoTonesMIB_destroy(&StorageTmp->megacoTonesMIB_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_megTonesToneTable_row(struct megTonesToneTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_megTonesToneTable_row(struct megTonesToneTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_megTonesToneTable_row(struct megTonesToneTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_megTonesToneTable_row(struct megTonesToneTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_megTonesGroupTable_row(struct megTonesGroupTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_megTonesGroupTable_row(struct megTonesGroupTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_megTonesGroupTable_row(struct megTonesGroupTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_megTonesGroupTable_row(struct megTonesGroupTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_megTonesToneRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_megTonesToneRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct megTonesToneTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct megTonesToneTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 12;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("megacoTonesMIB", "write_megTonesToneRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(megTonesToneTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to megTonesToneRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to megTonesToneRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* megTonesToneSetId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* megTonesToneId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[12]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* megTonesToneSetId */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index megTonesToneSetId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..2147483647 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > 2147483647)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index megTonesToneSetId: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* megTonesToneId */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index megTonesToneId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..2147483647 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > 2147483647)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index megTonesToneId: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = megTonesToneTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->megTonesToneTable_rsvs = 1;
			vp = vars;
			StorageNew->megTonesToneSetId = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->megTonesToneId = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&megTonesToneTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->megTonesToneRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->megTonesToneTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->megTonesToneTable_old) == NULL)
				if (StorageTmp->megTonesToneTable_rsvs == 0)
					if ((StorageOld = StorageTmp->megTonesToneTable_old = megTonesToneTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->megTonesToneTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->megTonesToneTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				megTonesToneTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_megTonesToneTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* check that activation is possible */
			if (StorageTmp->megTonesToneRowStatus != RS_ACTIVE)
				if ((ret = can_act_megTonesToneTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->megTonesToneRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_megTonesToneTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->megTonesToneRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_megTonesToneTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->megTonesToneRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here must be
		   reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_megTonesToneTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->megTonesToneRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_megTonesToneTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->megTonesToneRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_megTonesToneTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destruction to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_megTonesToneTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->megTonesToneRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->megTonesToneRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->megTonesToneRowStatus = set_value;
			if ((StorageOld = StorageTmp->megTonesToneTable_old) != NULL) {
				megTonesToneTable_destroy(&StorageTmp->megTonesToneTable_old);
				StorageTmp->megTonesToneTable_rsvs = 0;
				StorageTmp->megTonesToneTable_tsts = 0;
				StorageTmp->megTonesToneTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			megTonesToneTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_megTonesToneTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->megTonesToneRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_megTonesToneTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->megTonesToneRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_megTonesToneTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				megTonesToneTable_del(StorageNew);
				megTonesToneTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->megTonesToneTable_old) == NULL)
				break;
			if (--StorageTmp->megTonesToneTable_rsvs == 0)
				megTonesToneTable_destroy(&StorageTmp->megTonesToneTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				megTonesToneTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_megTonesGroupRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_megTonesGroupRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct megTonesGroupTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct megTonesGroupTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 12;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("megacoTonesMIB", "write_megTonesGroupRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(megTonesGroupTableStorage, NULL, &name[12], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to megTonesGroupRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to megTonesGroupRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* megTonesToneGroupId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* megTonesGroupIndex */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[12]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* megTonesToneGroupId */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index megTonesToneGroupId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..2147483647 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > 2147483647)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index megTonesToneGroupId: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* megTonesGroupIndex */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index megTonesGroupIndex: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			/* Note: ranges 1..2147483647 */
			if ((1 > (ulong) *vp->val.integer || (ulong) *vp->val.integer > 2147483647)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index megTonesGroupIndex: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = megTonesGroupTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->megTonesGroupTable_rsvs = 1;
			vp = vars;
			StorageNew->megTonesToneGroupId = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->megTonesGroupIndex = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&megTonesGroupTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->megTonesGroupRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->megTonesGroupTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->megTonesGroupTable_old) == NULL)
				if (StorageTmp->megTonesGroupTable_rsvs == 0)
					if ((StorageOld = StorageTmp->megTonesGroupTable_old = megTonesGroupTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->megTonesGroupTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->megTonesGroupTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				megTonesGroupTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_megTonesGroupTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* check that activation is possible */
			if (StorageTmp->megTonesGroupRowStatus != RS_ACTIVE)
				if ((ret = can_act_megTonesGroupTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->megTonesGroupRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_megTonesGroupTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->megTonesGroupRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_megTonesGroupTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->megTonesGroupRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here must be
		   reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_megTonesGroupTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->megTonesGroupRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_megTonesGroupTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->megTonesGroupRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_megTonesGroupTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destruction to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_megTonesGroupTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->megTonesGroupRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->megTonesGroupRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->megTonesGroupRowStatus = set_value;
			if ((StorageOld = StorageTmp->megTonesGroupTable_old) != NULL) {
				megTonesGroupTable_destroy(&StorageTmp->megTonesGroupTable_old);
				StorageTmp->megTonesGroupTable_rsvs = 0;
				StorageTmp->megTonesGroupTable_tsts = 0;
				StorageTmp->megTonesGroupTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			megTonesGroupTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_megTonesGroupTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->megTonesGroupRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_megTonesGroupTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->megTonesGroupRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_megTonesGroupTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				megTonesGroupTable_del(StorageNew);
				megTonesGroupTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->megTonesGroupTable_old) == NULL)
				break;
			if (--StorageTmp->megTonesGroupTable_rsvs == 0)
				megTonesGroupTable_destroy(&StorageTmp->megTonesGroupTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				megTonesGroupTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn void megacoTonesMIB_loop_handler(int dummy)
 * @param sig signal number
 * @brief handle event loop interation.
 *
 * This function is registered so that, when operating as a module, snmpd will call it one per event
 * loop interation.  This function is called before the next requst is processed and after the
 * previous request is processed.  Two things are done here:  1) The file descriptor that is used to
 * synchronize the agent with (pseudo-)device drivers is closed.  (Another approach, instead of
 * closing each time, would be to restart a timer each time that a request is made (loop is
 * performed) and if it expires, close the file descriptor).  2) The request number is incremented.
 * Although a request is not generated for each loop of the snmp event loop, it is true that a new
 * request cannot be generated without performing a loop.  Therefore, the sa_request is not the
 * request number but it is a temporally unique identifier for a request.
 */
void
megacoTonesMIB_loop_handler(int sig)
{
	DEBUGMSGTL(("megacoTonesMIB", "megacoTonesMIB_loop_handler: executing loop handler...  "));
	/* close files after each request */
	if (sa_fclose) {
		if (my_fd >= 0) {
			close(my_fd);
			my_fd = -1;
		}
	}
#if defined MASTER
	/* prepare for next request */
	sa_request++;
#endif				/* defined MASTER */
	if (external_signal_scheduled[sig] == 0)
		external_signal_scheduled[sig]--;
	if (megacoTonesMIBold_signal_handler != NULL)
		(*megacoTonesMIBold_signal_handler) (sig);
	DEBUGMSGTL(("megacoTonesMIB", "done.\n"));
}

/**
 * @fn void megacoTonesMIB_fd_handler(int fd, void *dummy)
 * @param fd file descriptor to read.
 * @param dummy client data passed to registration function (always NULL).
 * @brief handle read event on file descriptor.
 *
 * This read file descriptor handler is normally used for (pseudo-)device drivers that generate
 * statistical collection interval events, alarm events, or other operational measurement events, by
 * placing a message on the read queue of the "event handling" Stream.  Normally this routine
 * would adjust counts in some table or scalars, generate SNMP traps representing on-occurence
 * events, first and interval events, and alarm indications.
 */
void
megacoTonesMIB_fd_handler(int fd, void *dummy)
{
	DEBUGMSGTL(("megacoTonesMIB", "megacoTonesMIB_fd_handler: executing fd handler...  "));
	/* XXX: place actions to handle my_fd here... */
	DEBUGMSGTL(("megacoTonesMIB", "done.\n"));
	return;
}
