/*****************************************************************************

 @(#) $RCSfile$ $Name$($Revision$) $Date$

 -----------------------------------------------------------------------------

 Copyright (c) 2008-2009  Monavacon Limited <http://www.monavacon.com/>
 Copyright (c) 2001-2008  OpenSS7 Corporation <http://www.openss7.com/>
 Copyright (c) 1997-2001  Brian F. G. Bidulock <bidulock@openss7.org>

 All Rights Reserved.

 This program is free software: you can redistribute it and/or modify it under
 the terms of the GNU Affero General Public License as published by the Free
 Software Foundation, version 3 of the license.

 This program is distributed in the hope that it will be useful, but WITHOUT
 ANY WARRANTY; without even the implied warranty of MERCHANTABILITY or FITNESS
 FOR A PARTICULAR PURPOSE.  See the GNU Affero General Public License for more
 details.

 You should have received a copy of the GNU Affero General Public License
 along with this program.  If not, see <http://www.gnu.org/licenses/>, or
 write to the Free Software Foundation, Inc., 675 Mass Ave, Cambridge, MA
 02139, USA.

 -----------------------------------------------------------------------------

 U.S. GOVERNMENT RESTRICTED RIGHTS.  If you are licensing this Software on
 behalf of the U.S. Government ("Government"), the following provisions apply
 to you.  If the Software is supplied by the Department of Defense ("DoD"), it
 is classified as "Commercial Computer Software" under paragraph 252.227-7014
 of the DoD Supplement to the Federal Acquisition Regulations ("DFARS") (or any
 successor regulations) and the Government is acquiring only the license rights
 granted herein (the license rights customarily provided to non-Government
 users).  If the Software is supplied to any unit or agency of the Government
 other than DoD, it is classified as "Restricted Computer Software" and the
 Government's rights in the Software are defined in paragraph 52.227-19 of the
 Federal Acquisition Regulations ("FAR") (or any successor regulations) or, in
 the cases of NASA, in paragraph 18.52.227-86 of the NASA Supplement to the FAR
 (or any successor regulations).

 -----------------------------------------------------------------------------

 Commercial licensing and support of this software is available from OpenSS7
 Corporation at a fee.  See http://www.openss7.com/

 -----------------------------------------------------------------------------

 Last Modified $Date$ by $Author$

 -----------------------------------------------------------------------------

 $Log$
 Revision 0.9.2.8  2009-02-18 20:18:41  brian
 - updated agents

 Revision 0.9.2.7  2009-01-19 13:31:36  brian
 - updating standalone agents

 Revision 0.9.2.6  2009-01-10 17:31:00  brian
 - agent updates

 Revision 0.9.2.5  2009-01-04 13:16:07  brian
 - updated agents

 Revision 0.9.2.4  2009-01-03 10:50:58  brian
 - updated agent

 Revision 0.9.2.3  2009-01-02 15:04:08  brian
 - updated agents

 Revision 0.9.2.2  2008-12-31 16:04:40  brian
 - updated mibs

 Revision 0.9.2.1  2008-12-27 10:01:52  brian
 - updated mibs and stubs

 *****************************************************************************/

#ident "@(#) $RCSfile$ $Name$($Revision$) $Date$"

static char const ident[] = "$RCSfile$ $Name$($Revision$) $Date$";

/* This file was generated by mib2c and is intended for use as
   a mib module for the ucd-snmp snmpd agent. */
#include <ucd-snmp/ucd-snmp-config.h>
#include <ucd-snmp/ucd-snmp-includes.h>
#include <ucd-snmp/ucd-snmp-agent-includes.h>
#include <ucd-snmp/agent_trap.h>
#include <ucd-snmp/callback.h>
#include <ucd-snmp/snmp-tc.h>
#include <ucd-snmp/default_store.h>
#include <ucd-snmp/snmp_alarm.h>
/* The following header files are mangled in most recent net-snmp releases so
 * the versions from UCD-SNMP 4.2.5 are included here.  */
#if defined HAVE_LIBNETSNMP
#else				/* defined HAVE_LIBNETSNMP */
#endif				/* defined HAVE_LIBNETSNMP */
/* These are messed up on both. */
#include "ds_agent.h"
#ifdef HAVE_UCD_SNMP_UTIL_FUNCS_H
#include <ucd-snmp/util_funcs.h>
/* Many recent net-snmp UCD compatible headers do not declare header_generic. */
int header_generic(struct variable *, oid *, size_t *, int, size_t *, WriteMethod **);
#else				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#include "util_funcs.h"
#endif				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#ifdef HAVE_UCD_SNMP_HEADER_COMPLEX_H
#include <ucd-snmp/header_complex.h>
#else				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
#include "header_complex.h"
#endif				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
/* This one is the other way around: it is just fine for net-snmp, but
 * ucd-snmp does not provide the header file at all.  */
#ifdef HAVE_UCD_SNMP_MIB_MODULES_H
#include <ucd-snmp/mib_modules.h>
#else				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#ifdef HAVE_NET_SNMP_AGENT_MIB_MODULES_H
#include <net-snmp/agent/mib_modules.h>
#else				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#include "mib_modules.h"
#endif				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#endif				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#include <stdint.h>
#include <signal.h>
#include <sys/stat.h>		/* for struct stat, fstat() */
#include <sys/types.h>
#include <unistd.h>
#include <stdlib.h>
#include <pwd.h>		/* for getpwuid() getpwnam() */
#include <grp.h>		/* for getgrgid() getgrnam() */
#include <libgen.h>		/* for basename() */
#include <fcntl.h>		/* for O_CREAT */
#include <sys/sysctl.h>		/* for sysctl */
#include <errno.h>
#include <string.h>
#ifdef _GNU_SOURCE
#include <getopt.h>
#endif
#include "m2paMIB.h"
#define MASTER 1
#define MY_FACILITY(__pri)	(LOG_DAEMON|(__pri))
#if defined MODULE
#if defined MASTER
const char sa_program[] = "m2paMIB";
int sa_fclose = 1;			/* default close files between requests */
int sa_changed = 1;			/* indication to reread MIB configuration */
int sa_stats_refresh = 1;		/* indications that statistics, the mib or its tables need to be refreshed */
int sa_request = 1;			/* request number for per-request actions */
int sa_dump = 0;			/* default packet dump */
int sa_debug = 0;			/* default no debug */
#endif				/* defined MASTER */
#endif				/* defined MODULE */
static int my_fd = -1;			/* file descriptor for this MIB's use */
static int my_readfd = -1;		/* file descriptor for autonomnous events */
volatile int m2paMIB_refresh = 1;
volatile int m2paProtocolProfileTable_refresh = 1;

/*
 * m2paMIB_variables_oid: object identifier for m2paMIB
 * This is the top level oid that we want to register under.  This is essentially a prefix, with the
 * suffix appearing in the variable below.
 */
oid m2paMIB_variables_oid[9] = { 1, 3, 6, 1, 4, 1, 29591, 1, 202 };
oid m2paProtocolProfileTable_variables_oid[14] = { 1, 3, 6, 1, 4, 1, 29591, 1, 202, 1, 1, 1, 1, 1 };

/*
 * Oids for use in notifications defined in this MIB.
 */

/*
 * Oids accessible only for notify defined in this MIB.
 */

/*
 * Other oids defined in this MIB.
 */
oid m2paFullCompliance_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 1, 202, 2, 1, 1 };
oid m2paCompliance_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 1, 202, 2, 1, 2 };
oid m2paBasicCongestionCompliance_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 1, 202, 2, 1, 3 };
oid m2paMultipleCongestionStatesCompliance_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 1, 202, 2, 1, 4 };
oid m2paMultipleCongestionLevelsCompliance_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 1, 202, 2, 1, 5 };
oid m2paMultipleCongestionLeveslWithMessagePriorityCompliance_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 1, 202, 2, 1, 6 };
oid m2paObjectGroup_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 1, 202, 2, 2, 1 };
oid m2paBasicSS7Group_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 1, 202, 2, 2, 2 };
oid m2paBasicSS7TransmitCongestionPackage_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 1, 202, 2, 2, 3 };
oid m2paBasicSCTPGroup_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 1, 202, 2, 2, 4 };
oid m2paProvingPackage_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 1, 202, 2, 2, 5 };
oid m2paBufferMechanismPackage_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 1, 202, 2, 2, 6 };
oid m2paCongestionHandlingPackage_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 1, 202, 2, 2, 7 };
oid m2paMultipleTransmissionCongestionStatesPackage_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 1, 202, 2, 2, 8 };
oid m2paReceiveCongestionLevelPackage_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 1, 202, 2, 2, 9 };
oid m2paProtocolProfileNamePackage_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 1, 202, 2, 2, 10 };
oid m2paThresholdsForMultCongLevelsPackage_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 1, 202, 2, 2, 11 };
oid m2paThresholdsForMultCongLevelsWithPrioPackage_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 1, 202, 2, 2, 12 };
static oid zeroDotZero_oid[2] = { 0, 0 };
static oid snmpTrapOID_oid[11] = { 1, 3, 6, 1, 6, 3, 1, 1, 4, 1, 0 };

/*
 * variable7 m2paMIB_variables: tree for m2paMIB
 * This variable defines function callbacks and type return information for the m2paMIB mib section
 */
struct variable7 m2paMIB_variables[] = {
	/* magic number, variable type, ro/rw, callback fn, L, oidsuffix */
#define   M2PAN1                1
	{(u_char) M2PAN1, ASN_UNSIGNED, RWRITE, var_m2paProtocolProfileTable, 6, {1, 1, 1, 1, 1, 2}},
#define   M2PAPROVING           2
	{(u_char) M2PAPROVING, ASN_INTEGER, RWRITE, var_m2paProtocolProfileTable, 6, {1, 1, 1, 1, 1, 3}},
#define   M2PAMANAGEMENTPROVINGSTATE  3
	{(u_char) M2PAMANAGEMENTPROVINGSTATE, ASN_INTEGER, RWRITE, var_m2paProtocolProfileTable, 6, {1, 1, 1, 1, 1, 4}},
#define   M2PALOOPDELAYLOWER    4
	{(u_char) M2PALOOPDELAYLOWER, ASN_INTEGER, RWRITE, var_m2paProtocolProfileTable, 6, {1, 1, 1, 1, 1, 5}},
#define   M2PALOOPDELAYUPPER    5
	{(u_char) M2PALOOPDELAYUPPER, ASN_INTEGER, RWRITE, var_m2paProtocolProfileTable, 6, {1, 1, 1, 1, 1, 6}},
#define   M2PATRANSMISSIONRATEINTERVALLOWER  6
	{(u_char) M2PATRANSMISSIONRATEINTERVALLOWER, ASN_UNSIGNED, RWRITE, var_m2paProtocolProfileTable, 6, {1, 1, 1, 1, 1, 7}},
#define   M2PATRANSMISSIONRATEINTERVALUPPER  7
	{(u_char) M2PATRANSMISSIONRATEINTERVALUPPER, ASN_UNSIGNED, RWRITE, var_m2paProtocolProfileTable, 6, {1, 1, 1, 1, 1, 8}},
#define   M2PASCTPNODELAY       8
	{(u_char) M2PASCTPNODELAY, ASN_INTEGER, RWRITE, var_m2paProtocolProfileTable, 6, {1, 1, 1, 1, 1, 9}},
#define   M2PASCTPMAXSEG        9
	{(u_char) M2PASCTPMAXSEG, ASN_UNSIGNED, RWRITE, var_m2paProtocolProfileTable, 6, {1, 1, 1, 1, 1, 10}},
#define   M2PASCTPHEARTBEATITVL  10
	{(u_char) M2PASCTPHEARTBEATITVL, ASN_INTEGER, RWRITE, var_m2paProtocolProfileTable, 6, {1, 1, 1, 1, 1, 11}},
#define   M2PASCTPHEARTBEAT     11
	{(u_char) M2PASCTPHEARTBEAT, ASN_INTEGER, RWRITE, var_m2paProtocolProfileTable, 6, {1, 1, 1, 1, 1, 12}},
#define   M2PASCTPRTOINITIAL    12
	{(u_char) M2PASCTPRTOINITIAL, ASN_INTEGER, RWRITE, var_m2paProtocolProfileTable, 6, {1, 1, 1, 1, 1, 13}},
#define   M2PASCTPRTOMIN        13
	{(u_char) M2PASCTPRTOMIN, ASN_INTEGER, RWRITE, var_m2paProtocolProfileTable, 6, {1, 1, 1, 1, 1, 14}},
#define   M2PASCTPRTOMAX        14
	{(u_char) M2PASCTPRTOMAX, ASN_INTEGER, RWRITE, var_m2paProtocolProfileTable, 6, {1, 1, 1, 1, 1, 15}},
#define   M2PASCTPPATHMAXRETRANS  15
	{(u_char) M2PASCTPPATHMAXRETRANS, ASN_UNSIGNED, RWRITE, var_m2paProtocolProfileTable, 6, {1, 1, 1, 1, 1, 16}},
#define   M2PASCTPCOOKIELIFE    16
	{(u_char) M2PASCTPCOOKIELIFE, ASN_INTEGER, RWRITE, var_m2paProtocolProfileTable, 6, {1, 1, 1, 1, 1, 17}},
#define   M2PASCTPCOOKIEINC     17
	{(u_char) M2PASCTPCOOKIEINC, ASN_INTEGER, RWRITE, var_m2paProtocolProfileTable, 6, {1, 1, 1, 1, 1, 18}},
#define   M2PASCTPMAXINITRETRIES  18
	{(u_char) M2PASCTPMAXINITRETRIES, ASN_UNSIGNED, RWRITE, var_m2paProtocolProfileTable, 6, {1, 1, 1, 1, 1, 19}},
#define   M2PASCTPMAXBURST      19
	{(u_char) M2PASCTPMAXBURST, ASN_UNSIGNED, RWRITE, var_m2paProtocolProfileTable, 6, {1, 1, 1, 1, 1, 20}},
#define   M2PASCTPASSOCMAXRETRANS  20
	{(u_char) M2PASCTPASSOCMAXRETRANS, ASN_UNSIGNED, RWRITE, var_m2paProtocolProfileTable, 6, {1, 1, 1, 1, 1, 21}},
#define   M2PASCTPSACKDELAY     21
	{(u_char) M2PASCTPSACKDELAY, ASN_INTEGER, RWRITE, var_m2paProtocolProfileTable, 6, {1, 1, 1, 1, 1, 22}},
#define   M2PASCTPLIFETIME      22
	{(u_char) M2PASCTPLIFETIME, ASN_INTEGER, RWRITE, var_m2paProtocolProfileTable, 6, {1, 1, 1, 1, 1, 23}},
#define   M2PATIMERT1           23
	{(u_char) M2PATIMERT1, ASN_INTEGER, RWRITE, var_m2paProtocolProfileTable, 6, {1, 1, 1, 1, 1, 24}},
#define   M2PATIMERT2           24
	{(u_char) M2PATIMERT2, ASN_INTEGER, RWRITE, var_m2paProtocolProfileTable, 6, {1, 1, 1, 1, 1, 25}},
#define   M2PATIMERT2L          25
	{(u_char) M2PATIMERT2L, ASN_INTEGER, RWRITE, var_m2paProtocolProfileTable, 6, {1, 1, 1, 1, 1, 26}},
#define   M2PATIMERT2H          26
	{(u_char) M2PATIMERT2H, ASN_INTEGER, RWRITE, var_m2paProtocolProfileTable, 6, {1, 1, 1, 1, 1, 27}},
#define   M2PATIMERT3           27
	{(u_char) M2PATIMERT3, ASN_INTEGER, RWRITE, var_m2paProtocolProfileTable, 6, {1, 1, 1, 1, 1, 28}},
#define   M2PATIMERT4N          28
	{(u_char) M2PATIMERT4N, ASN_INTEGER, RWRITE, var_m2paProtocolProfileTable, 6, {1, 1, 1, 1, 1, 29}},
#define   M2PATIMERT4E          29
	{(u_char) M2PATIMERT4E, ASN_INTEGER, RWRITE, var_m2paProtocolProfileTable, 6, {1, 1, 1, 1, 1, 30}},
#define   M2PATIMERT6           30
	{(u_char) M2PATIMERT6, ASN_INTEGER, RWRITE, var_m2paProtocolProfileTable, 6, {1, 1, 1, 1, 1, 31}},
#define   M2PATIMERT7           31
	{(u_char) M2PATIMERT7, ASN_INTEGER, RWRITE, var_m2paProtocolProfileTable, 6, {1, 1, 1, 1, 1, 32}},
#define   M2PATRANSCONGTHRESHOLDABATEMENTL1MESSAGES  32
	{(u_char) M2PATRANSCONGTHRESHOLDABATEMENTL1MESSAGES, ASN_UNSIGNED, RWRITE, var_m2paProtocolProfileTable, 6, {1, 1, 1, 1, 1, 33}},
#define   M2PATRANSCONGTHRESHOLDABATEMENTL1OCTETS  33
	{(u_char) M2PATRANSCONGTHRESHOLDABATEMENTL1OCTETS, ASN_UNSIGNED, RWRITE, var_m2paProtocolProfileTable, 6, {1, 1, 1, 1, 1, 34}},
#define   M2PATRANSCONGTHRESHOLDONSETL1MESSAGES  34
	{(u_char) M2PATRANSCONGTHRESHOLDONSETL1MESSAGES, ASN_UNSIGNED, RWRITE, var_m2paProtocolProfileTable, 6, {1, 1, 1, 1, 1, 35}},
#define   M2PATRANSCONGTHRESHOLDONSETL1OCTETS  35
	{(u_char) M2PATRANSCONGTHRESHOLDONSETL1OCTETS, ASN_UNSIGNED, RWRITE, var_m2paProtocolProfileTable, 6, {1, 1, 1, 1, 1, 36}},
#define   M2PAPROVINGATTEMPTS   36
	{(u_char) M2PAPROVINGATTEMPTS, ASN_UNSIGNED, RWRITE, var_m2paProtocolProfileTable, 6, {1, 1, 1, 1, 1, 37}},
#define   M2PANUMBEROFTHRESHOLDLEVELS  37
	{(u_char) M2PANUMBEROFTHRESHOLDLEVELS, ASN_UNSIGNED, RWRITE, var_m2paProtocolProfileTable, 6, {1, 1, 1, 1, 1, 38}},
#define   M2PACONGESTIONCOUNTING  38
	{(u_char) M2PACONGESTIONCOUNTING, ASN_INTEGER, RWRITE, var_m2paProtocolProfileTable, 6, {1, 1, 1, 1, 1, 39}},
#define   M2PACONGESTIONREPORTINGBASEOBJECT  39
	{(u_char) M2PACONGESTIONREPORTINGBASEOBJECT, ASN_INTEGER, RWRITE, var_m2paProtocolProfileTable, 6, {1, 1, 1, 1, 1, 40}},
#define   M2PATIMERTX           40
	{(u_char) M2PATIMERTX, ASN_INTEGER, RWRITE, var_m2paProtocolProfileTable, 6, {1, 1, 1, 1, 1, 41}},
#define   M2PATIMERTY           41
	{(u_char) M2PATIMERTY, ASN_INTEGER, RWRITE, var_m2paProtocolProfileTable, 6, {1, 1, 1, 1, 1, 42}},
#define   M2PANUMBEROFCONGESTIONSTATES  42
	{(u_char) M2PANUMBEROFCONGESTIONSTATES, ASN_UNSIGNED, RWRITE, var_m2paProtocolProfileTable, 6, {1, 1, 1, 1, 1, 43}},
#define   M2PAINITIALLEVELOFCONGESTION  43
	{(u_char) M2PAINITIALLEVELOFCONGESTION, ASN_UNSIGNED, RWRITE, var_m2paProtocolProfileTable, 6, {1, 1, 1, 1, 1, 44}},
#define   M2PARECEIVECONGESTIONTHRESHOLDABATEMENT  44
	{(u_char) M2PARECEIVECONGESTIONTHRESHOLDABATEMENT, ASN_UNSIGNED, RWRITE, var_m2paProtocolProfileTable, 6, {1, 1, 1, 1, 1, 45}},
#define   M2PARECEIVECONGESTIONTHRESHOLDONSET  45
	{(u_char) M2PARECEIVECONGESTIONTHRESHOLDONSET, ASN_UNSIGNED, RWRITE, var_m2paProtocolProfileTable, 6, {1, 1, 1, 1, 1, 46}},
#define   M2PARECEIVECONGESTIONTHRESHOLDDISCARD  46
	{(u_char) M2PARECEIVECONGESTIONTHRESHOLDDISCARD, ASN_UNSIGNED, RWRITE, var_m2paProtocolProfileTable, 6, {1, 1, 1, 1, 1, 47}},
#define   M2PAPROTOCOLPROFILENAME  47
	{(u_char) M2PAPROTOCOLPROFILENAME, ASN_OCTET_STR, RWRITE, var_m2paProtocolProfileTable, 6, {1, 1, 1, 1, 1, 48}},
#define   M2PATRANSCONGTHRESHOLDABATEMENTL2MESSAGES  48
	{(u_char) M2PATRANSCONGTHRESHOLDABATEMENTL2MESSAGES, ASN_UNSIGNED, RWRITE, var_m2paProtocolProfileTable, 6, {1, 1, 1, 1, 1, 49}},
#define   M2PATRANSCONGTHRESHOLDABATEMENTL2OCTETS  49
	{(u_char) M2PATRANSCONGTHRESHOLDABATEMENTL2OCTETS, ASN_UNSIGNED, RWRITE, var_m2paProtocolProfileTable, 6, {1, 1, 1, 1, 1, 50}},
#define   M2PATRANSCONGTHRESHOLDONSETL2MESSAGES  50
	{(u_char) M2PATRANSCONGTHRESHOLDONSETL2MESSAGES, ASN_UNSIGNED, RWRITE, var_m2paProtocolProfileTable, 6, {1, 1, 1, 1, 1, 51}},
#define   M2PATRANSCONGTHRESHOLDONSETL2OCTETS  51
	{(u_char) M2PATRANSCONGTHRESHOLDONSETL2OCTETS, ASN_UNSIGNED, RWRITE, var_m2paProtocolProfileTable, 6, {1, 1, 1, 1, 1, 52}},
#define   M2PATRANSCONGTHRESHOLDABATEMENTL3MESSAGES  52
	{(u_char) M2PATRANSCONGTHRESHOLDABATEMENTL3MESSAGES, ASN_UNSIGNED, RWRITE, var_m2paProtocolProfileTable, 6, {1, 1, 1, 1, 1, 53}},
#define   M2PATRANSCONGTHRESHOLDABATEMENTL3OCTETS  53
	{(u_char) M2PATRANSCONGTHRESHOLDABATEMENTL3OCTETS, ASN_UNSIGNED, RWRITE, var_m2paProtocolProfileTable, 6, {1, 1, 1, 1, 1, 54}},
#define   M2PATRANSCONGTHRESHOLDONSETL3MESSAGES  54
	{(u_char) M2PATRANSCONGTHRESHOLDONSETL3MESSAGES, ASN_UNSIGNED, RWRITE, var_m2paProtocolProfileTable, 6, {1, 1, 1, 1, 1, 55}},
#define   M2PATRANSCONGTHRESHOLDONSETL3OCTETS  55
	{(u_char) M2PATRANSCONGTHRESHOLDONSETL3OCTETS, ASN_UNSIGNED, RWRITE, var_m2paProtocolProfileTable, 6, {1, 1, 1, 1, 1, 56}},
#define   M2PATRANSCONGTHRESHOLDDISCARDL1MESSAGES  56
	{(u_char) M2PATRANSCONGTHRESHOLDDISCARDL1MESSAGES, ASN_UNSIGNED, RWRITE, var_m2paProtocolProfileTable, 6, {1, 1, 1, 1, 1, 57}},
#define   M2PATRANSCONGTHRESHOLDDISCARDL1OCTETS  57
	{(u_char) M2PATRANSCONGTHRESHOLDDISCARDL1OCTETS, ASN_UNSIGNED, RWRITE, var_m2paProtocolProfileTable, 6, {1, 1, 1, 1, 1, 58}},
#define   M2PATRANSCONGTHRESHOLDDISCARDL2MESSAGES  58
	{(u_char) M2PATRANSCONGTHRESHOLDDISCARDL2MESSAGES, ASN_UNSIGNED, RWRITE, var_m2paProtocolProfileTable, 6, {1, 1, 1, 1, 1, 59}},
#define   M2PATRANSCONGTHRESHOLDDISCARDL2OCTETS  59
	{(u_char) M2PATRANSCONGTHRESHOLDDISCARDL2OCTETS, ASN_UNSIGNED, RWRITE, var_m2paProtocolProfileTable, 6, {1, 1, 1, 1, 1, 60}},
#define   M2PATRANSCONGTHRESHOLDDISCARDL3MESSAGES  60
	{(u_char) M2PATRANSCONGTHRESHOLDDISCARDL3MESSAGES, ASN_UNSIGNED, RWRITE, var_m2paProtocolProfileTable, 6, {1, 1, 1, 1, 1, 61}},
#define   M2PATRANSCONGTHRESHOLDDISCARDL3OCTETS  61
	{(u_char) M2PATRANSCONGTHRESHOLDDISCARDL3OCTETS, ASN_UNSIGNED, RWRITE, var_m2paProtocolProfileTable, 6, {1, 1, 1, 1, 1, 62}},
#define   M2PAPROTOCOLPROFILEROWSTATUS  62
	{(u_char) M2PAPROTOCOLPROFILEROWSTATUS, ASN_INTEGER, RWRITE, var_m2paProtocolProfileTable, 6, {1, 1, 1, 1, 1, 63}},
#define   M2PADEFAULTSCTPNODELAY  63
	{(u_char) M2PADEFAULTSCTPNODELAY, ASN_INTEGER, RWRITE, var_m2paMIB, 3, {1, 2, 1}},
#define   M2PADEFAULTSCTPMAXSEG  64
	{(u_char) M2PADEFAULTSCTPMAXSEG, ASN_UNSIGNED, RWRITE, var_m2paMIB, 3, {1, 2, 2}},
#define   M2PADEFAULTSCTPHEARTBEATITVL  65
	{(u_char) M2PADEFAULTSCTPHEARTBEATITVL, ASN_INTEGER, RWRITE, var_m2paMIB, 3, {1, 2, 3}},
#define   M2PADEFAULTSCTPHEARTBEAT  66
	{(u_char) M2PADEFAULTSCTPHEARTBEAT, ASN_INTEGER, RWRITE, var_m2paMIB, 3, {1, 2, 4}},
#define   M2PADEFAULTSCTPRTOINITIAL  67
	{(u_char) M2PADEFAULTSCTPRTOINITIAL, ASN_INTEGER, RWRITE, var_m2paMIB, 3, {1, 2, 5}},
#define   M2PADEFAULTSCTPRTOMIN  68
	{(u_char) M2PADEFAULTSCTPRTOMIN, ASN_INTEGER, RWRITE, var_m2paMIB, 3, {1, 2, 6}},
#define   M2PADEFAULTSCTPRTOMAX  69
	{(u_char) M2PADEFAULTSCTPRTOMAX, ASN_INTEGER, RWRITE, var_m2paMIB, 3, {1, 2, 7}},
#define   M2PADEFAULTSCTPPATHMAXRETRANS  70
	{(u_char) M2PADEFAULTSCTPPATHMAXRETRANS, ASN_UNSIGNED, RWRITE, var_m2paMIB, 3, {1, 2, 8}},
#define   M2PADEFAULTSCTPCOOKIELIFE  71
	{(u_char) M2PADEFAULTSCTPCOOKIELIFE, ASN_INTEGER, RWRITE, var_m2paMIB, 3, {1, 2, 9}},
#define   M2PADEFAULTSCTPCOOKIEINC  72
	{(u_char) M2PADEFAULTSCTPCOOKIEINC, ASN_INTEGER, RWRITE, var_m2paMIB, 3, {1, 2, 10}},
#define   M2PADEFAULTSCTPMAXINITRETRIES  73
	{(u_char) M2PADEFAULTSCTPMAXINITRETRIES, ASN_UNSIGNED, RWRITE, var_m2paMIB, 3, {1, 2, 11}},
#define   M2PADEFAULTSCTPMAXBURST  74
	{(u_char) M2PADEFAULTSCTPMAXBURST, ASN_UNSIGNED, RWRITE, var_m2paMIB, 3, {1, 2, 12}},
#define   M2PADEFAULTSCTPASSOCMAXRETRANS  75
	{(u_char) M2PADEFAULTSCTPASSOCMAXRETRANS, ASN_UNSIGNED, RWRITE, var_m2paMIB, 3, {1, 2, 13}},
#define   M2PADEFAULTSCTPSACKDELAY  76
	{(u_char) M2PADEFAULTSCTPSACKDELAY, ASN_INTEGER, RWRITE, var_m2paMIB, 3, {1, 2, 14}},
#define   M2PADEFAULTSCTPLIFETIME  77
	{(u_char) M2PADEFAULTSCTPLIFETIME, ASN_INTEGER, RWRITE, var_m2paMIB, 3, {1, 2, 15}},
};

/* (L = length of the oidsuffix) */
struct m2paMIB_data *m2paMIBStorage = NULL;

/* global storage of our data, saved in and configured by header_complex() */
struct header_complex_index *m2paProtocolProfileTableStorage = NULL;

void (*m2paMIBold_signal_handler) (int) = NULL;	/* save old signal handler just in case */
void m2paMIB_loop_handler(int);
void m2paMIB_fd_handler(int, void *);

/**
 * @fn void init_m2paMIB(void)
 * @brief m2paMIB initialization routine.
 *
 * This is called when the agent starts up.  At a minimum, registration of the MIB variables
 * structure (m2paMIB_variables) should take place here.  By default the function also
 * registers the configuration handler and configuration store callbacks.
 *
 * Additional registrations that may be considered here are calls to regsiter_readfd(),
 * register_writefd() and register_exceptfd() for hooking into the snmpd event loop, but only when
 * used as a loadable module.  By default this function establishes a single file descriptor to
 * read, or upon which to handle exceptions.  Note that the snmpd only supports a maximum of 32
 * extneral file descriptors, so these should be used sparingly.
 *
 * When running as a loadable module, it is also necessary to hook into the snmpd event loop so that
 * the current request number can be deteremined.  This is accomplished by using a trick of the
 * external_signal_scheduled and external_signal_handler mechanism which is called on each event
 * loop when external_signal_scheduled is non-zero.  This is used to increment the sa_request value
 * on each snmpd event loop interation so that calls to MIB tree functions can determine whether
 * they belong to a fresh request or not (primarily for cacheing and possibly to clean up non-polled
 * file descriptors).
 */
void
init_m2paMIB(void)
{
	(void) my_fd;
	(void) zeroDotZero_oid;
	(void) snmpTrapOID_oid;
	DEBUGMSGTL(("m2paMIB", "init_m2paMIB: initializing...  "));
	/* register ourselves with the agent to handle our mib tree */
	REGISTER_MIB("m2paMIB", m2paMIB_variables, variable7, m2paMIB_variables_oid);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_SHUTDOWN, term_m2paMIB, NULL);
	/* register our config handler(s) to deal with registrations */
	snmpd_register_config_handler("m2paMIB", parse_m2paMIB, NULL, "HELP STRING");
	snmpd_register_config_handler("m2paProtocolProfileTable", parse_m2paProtocolProfileTable, NULL, "HELP STRING");

	/* we need to be called back later to store our data */
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_m2paMIB, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_m2paProtocolProfileTable, NULL);

	/* place any other initialization junk you need here */
	if (my_readfd >= 0) {
		register_readfd(my_readfd, m2paMIB_fd_handler, (void *) 0);
		register_exceptfd(my_readfd, m2paMIB_fd_handler, (void *) 1);
	}
	m2paMIBold_signal_handler = external_signal_handler[SIGCHLD];
	external_signal_handler[SIGCHLD] = &m2paMIB_loop_handler;
	external_signal_scheduled[SIGCHLD] = 1;
	DEBUGMSGTL(("m2paMIB", "done.\n"));
}

/**
 * @fn void deinit_m2paMIB(void)
 * @brief deinitialization routine.
 *
 * This is called before the agent is unloaded.  At a minimum, deregistration of the MIB variables
 * structure (m2paMIB_variables) should take place here.  By default, the function also
 * deregisters the the configuration file handlers for the MIB variables and table rows.
 *
 * Additional deregistrations that may be required here are calls to unregister_readfd(),
 * unregister_writefd() and unregsiter_exceptfd() for unhooking from the snmpd event loop, but only
 * when used as a loadable module.  By default if a read file descriptor exists, it is unregistered.
 */
void
deinit_m2paMIB(void)
{
	DEBUGMSGTL(("m2paMIB", "deinit_m2paMIB: deinitializating...  "));
	external_signal_handler[SIGCHLD] = m2paMIBold_signal_handler;
	if (my_readfd >= 0) {
		unregister_exceptfd(my_readfd);
		unregister_readfd(my_readfd);
		close(my_readfd);
		my_readfd = -1;
	}
	unregister_mib(m2paMIB_variables_oid, sizeof(m2paMIB_variables_oid) / sizeof(oid));
	snmpd_unregister_config_handler("m2paMIB");
	snmpd_unregister_config_handler("m2paProtocolProfileTable");

	/* place any other de-initialization junk you need here */
	DEBUGMSGTL(("m2paMIB", "done.\n"));
}

int
term_m2paMIB(int majorID, int minorID, void *serverarg, void *clientarg)
{
	DEBUGMSGTL(("m2paMIB", "term_m2paMIB: terminating...  "));
	deinit_m2paMIB();
	DEBUGMSGTL(("m2paMIB", "done.\n"));
	return 0;
}

/**
 * @fn struct m2paMIB_data *m2paMIB_create(void)
 * @brief create a fresh data structure representing scalars in m2paMIB.
 *
 * Creates a new m2paMIB_data structure by allocating dynamic memory for the structure and
 * initializing the default values of scalars in m2paMIB.
 */
struct m2paMIB_data *
m2paMIB_create(void)
{
	struct m2paMIB_data *StorageNew = SNMP_MALLOC_STRUCT(m2paMIB_data);

	DEBUGMSGTL(("m2paMIB", "m2paMIB_create: creating scalars...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default scalar values here into StorageNew */
		StorageNew->m2paDefaultSctpNoDelay = 1;
		StorageNew->m2paDefaultSctpMaxseg = 65536;
		StorageNew->m2paDefaultSctpHeartbeatItvl = 6000;
		StorageNew->m2paDefaultSctpHeartbeat = 0;
		StorageNew->m2paDefaultSctpRtoInitial = 300;
		StorageNew->m2paDefaultSctpRtoMin = 100;
		StorageNew->m2paDefaultSctpRtoMax = 6000;
		StorageNew->m2paDefaultSctpPathMaxRetrans = 5;
		StorageNew->m2paDefaultSctpCookieLife = 6000;
		StorageNew->m2paDefaultSctpCookieInc = 100;
		StorageNew->m2paDefaultSctpMaxInitRetries = 8;
		StorageNew->m2paDefaultSctpMaxBurst = 4;
		StorageNew->m2paDefaultSctpAssocMaxRetrans = 10;
		StorageNew->m2paDefaultSctpSackDelay = 20;
		StorageNew->m2paDefaultSctpLifetime = 0;

	}
	DEBUGMSGTL(("m2paMIB", "done.\n"));
	return (StorageNew);
}

/**
 * @fn int m2paMIB_destroy(struct m2paMIB_data **thedata)
 * @param thedata pointer to the data structure in m2paMIB.
 * @brief delete a scalars structure from m2paMIB.
 *
 * Frees scalars that were previously removed from m2paMIB.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
m2paMIB_destroy(struct m2paMIB_data **thedata)
{
	struct m2paMIB_data *StorageDel;

	DEBUGMSGTL(("m2paMIB", "m2paMIB_destroy: deleting scalars...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("m2paMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int m2paMIB_add(struct m2paMIB_data *thedata)
 * @param thedata the structure representing m2paMIB scalars.
 * @brief adds node to the m2paMIB scalar data set.
 *
 * Adds a scalar structure to the m2paMIB data set.  Note that this function is necessary even
 * when the scalar values are not peristent.
 */
int
m2paMIB_add(struct m2paMIB_data *thedata)
{
	DEBUGMSGTL(("m2paMIB", "m2paMIB_add: adding data...  "));
	if (thedata)
		m2paMIBStorage = thedata;
	DEBUGMSGTL(("m2paMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_m2paMIB(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for m2paMIB entries.
 *
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case m2paMIB).  This routine is invoked by
 * UCD-SNMP to read the values of scalars in the MIB from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the MIB.  If there are no configured entries
 * in the configuration MIB, this function will simply not be called.
 */
void
parse_m2paMIB(const char *token, char *line)
{
	size_t tmpsize;
	struct m2paMIB_data *StorageTmp = m2paMIB_create();

	DEBUGMSGTL(("m2paMIB", "parse_m2paMIB: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual scalars that are not persistent */
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2paDefaultSctpNoDelay, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->m2paDefaultSctpMaxseg, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2paDefaultSctpHeartbeatItvl, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2paDefaultSctpHeartbeat, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2paDefaultSctpRtoInitial, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2paDefaultSctpRtoMin, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2paDefaultSctpRtoMax, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->m2paDefaultSctpPathMaxRetrans, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2paDefaultSctpCookieLife, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2paDefaultSctpCookieInc, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->m2paDefaultSctpMaxInitRetries, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->m2paDefaultSctpMaxBurst, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->m2paDefaultSctpAssocMaxRetrans, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2paDefaultSctpSackDelay, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2paDefaultSctpLifetime, &tmpsize);
	m2paMIB_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("m2paMIB", "done.\n"));
}

/*
 * store_m2paMIB(): stores .conf file entries needed to configure the mib.
 */
int
store_m2paMIB(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct m2paMIB_data *StorageTmp;

	DEBUGMSGTL(("m2paMIB", "store_m2paMIB: storing data...  "));
	refresh_m2paMIB(1);
	if ((StorageTmp = m2paMIBStorage) == NULL) {
		DEBUGMSGTL(("m2paMIB", "error.\n"));
		return SNMPERR_GENERR;
	}
	(void) tmpsize;
	/* XXX: comment entire section if no scalars are persistent */
	{
		memset(line, 0, sizeof(line));
		strcat(line, "m2paMIB ");
		cptr = line + strlen(line);
		/* XXX: remove individual scalars that are not persistent */
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2paDefaultSctpNoDelay, &tmpsize);
		cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->m2paDefaultSctpMaxseg, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2paDefaultSctpHeartbeatItvl, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2paDefaultSctpHeartbeat, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2paDefaultSctpRtoInitial, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2paDefaultSctpRtoMin, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2paDefaultSctpRtoMax, &tmpsize);
		cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->m2paDefaultSctpPathMaxRetrans, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2paDefaultSctpCookieLife, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2paDefaultSctpCookieInc, &tmpsize);
		cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->m2paDefaultSctpMaxInitRetries, &tmpsize);
		cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->m2paDefaultSctpMaxBurst, &tmpsize);
		cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->m2paDefaultSctpAssocMaxRetrans, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2paDefaultSctpSackDelay, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2paDefaultSctpLifetime, &tmpsize);
		snmpd_store_config(line);
	}
	DEBUGMSGTL(("m2paMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void refresh_m2paMIB(int force)
 * @param force forced refresh when non-zero.
 * @brief refresh the scalar values of m2paMIB.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a scalar has been requested).
 */
void
refresh_m2paMIB(int force)
{
	if (m2paMIBStorage == NULL) {
		struct m2paMIB_data *StorageNew;

		if ((StorageNew = m2paMIB_create()) == NULL)
			return;
		m2paMIBStorage = StorageNew;
		m2paMIB_refresh = 1;
	}
	if (!force && m2paMIB_refresh == 0)
		return;
	DEBUGMSGTL(("m2paMIB", "refresh_m2paMIB: refreshing...  "));
	/* XXX: Update scalars as required here... */
	m2paMIB_refresh = 0;
	DEBUGMSGTL(("m2paMIB", "done.\n"));
}

/**
 * @fn u_char * var_m2paMIB(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @param vp a pointer to the entry in the variables table for the requested variable.
 * @param name the object identifier for which to find.
 * @param length the length of the object identifier.
 * @param exact whether the name is exact.
 * @param var_len a pointer to the length of the representation of the object.
 * @param write_method a pointer to a write method for the object.
 * @brief locate variables in m2paMIB.
 *
 * This function returns a pointer to a memory area that is static across the request that contains
 * the UCD-SNMP representation of the scalar (so that it may be used to read from for a GET,
 * GET-NEXT or GET-BULK request).  This returned pointer may be NULL, in which case the function is
 * telling UCD-SNMP that the scalar does not exist for reading; however, if write_method is
 * overwritten with a non-NULL value, the function is telling UCD-SNMP that the scalar exists for
 * writing.  Write-only objects can be effected in this way.
 */
u_char *
var_m2paMIB(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct m2paMIB_data *StorageTmp;
	u_char *rval;

	DEBUGMSGTL(("m2paMIB", "var_m2paMIB: lookup up varbind...  "));
	if (header_generic(vp, name, length, exact, var_len, write_method) == MATCH_FAILED)
		return NULL;
	/* Refresh the MIB values if required. */
	refresh_m2paMIB(0);
	if ((StorageTmp = m2paMIBStorage) == NULL) {
		DEBUGMSGTL(("m2paMIB", "no datastructure.\n"));
		return NULL;
	}
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) M2PADEFAULTSCTPNODELAY:	/* ReadWrite */
		*write_method = write_m2paDefaultSctpNoDelay;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paDefaultSctpNoDelay);
		rval = (u_char *) &StorageTmp->m2paDefaultSctpNoDelay;
		break;
	case (u_char) M2PADEFAULTSCTPMAXSEG:	/* ReadWrite */
		*write_method = write_m2paDefaultSctpMaxseg;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paDefaultSctpMaxseg);
		rval = (u_char *) &StorageTmp->m2paDefaultSctpMaxseg;
		break;
	case (u_char) M2PADEFAULTSCTPHEARTBEATITVL:	/* ReadWrite */
		*write_method = write_m2paDefaultSctpHeartbeatItvl;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paDefaultSctpHeartbeatItvl);
		rval = (u_char *) &StorageTmp->m2paDefaultSctpHeartbeatItvl;
		break;
	case (u_char) M2PADEFAULTSCTPHEARTBEAT:	/* ReadWrite */
		*write_method = write_m2paDefaultSctpHeartbeat;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paDefaultSctpHeartbeat);
		rval = (u_char *) &StorageTmp->m2paDefaultSctpHeartbeat;
		break;
	case (u_char) M2PADEFAULTSCTPRTOINITIAL:	/* ReadWrite */
		*write_method = write_m2paDefaultSctpRtoInitial;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paDefaultSctpRtoInitial);
		rval = (u_char *) &StorageTmp->m2paDefaultSctpRtoInitial;
		break;
	case (u_char) M2PADEFAULTSCTPRTOMIN:	/* ReadWrite */
		*write_method = write_m2paDefaultSctpRtoMin;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paDefaultSctpRtoMin);
		rval = (u_char *) &StorageTmp->m2paDefaultSctpRtoMin;
		break;
	case (u_char) M2PADEFAULTSCTPRTOMAX:	/* ReadWrite */
		*write_method = write_m2paDefaultSctpRtoMax;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paDefaultSctpRtoMax);
		rval = (u_char *) &StorageTmp->m2paDefaultSctpRtoMax;
		break;
	case (u_char) M2PADEFAULTSCTPPATHMAXRETRANS:	/* ReadWrite */
		*write_method = write_m2paDefaultSctpPathMaxRetrans;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paDefaultSctpPathMaxRetrans);
		rval = (u_char *) &StorageTmp->m2paDefaultSctpPathMaxRetrans;
		break;
	case (u_char) M2PADEFAULTSCTPCOOKIELIFE:	/* ReadWrite */
		*write_method = write_m2paDefaultSctpCookieLife;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paDefaultSctpCookieLife);
		rval = (u_char *) &StorageTmp->m2paDefaultSctpCookieLife;
		break;
	case (u_char) M2PADEFAULTSCTPCOOKIEINC:	/* ReadWrite */
		*write_method = write_m2paDefaultSctpCookieInc;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paDefaultSctpCookieInc);
		rval = (u_char *) &StorageTmp->m2paDefaultSctpCookieInc;
		break;
	case (u_char) M2PADEFAULTSCTPMAXINITRETRIES:	/* ReadWrite */
		*write_method = write_m2paDefaultSctpMaxInitRetries;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paDefaultSctpMaxInitRetries);
		rval = (u_char *) &StorageTmp->m2paDefaultSctpMaxInitRetries;
		break;
	case (u_char) M2PADEFAULTSCTPMAXBURST:	/* ReadWrite */
		*write_method = write_m2paDefaultSctpMaxBurst;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paDefaultSctpMaxBurst);
		rval = (u_char *) &StorageTmp->m2paDefaultSctpMaxBurst;
		break;
	case (u_char) M2PADEFAULTSCTPASSOCMAXRETRANS:	/* ReadWrite */
		*write_method = write_m2paDefaultSctpAssocMaxRetrans;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paDefaultSctpAssocMaxRetrans);
		rval = (u_char *) &StorageTmp->m2paDefaultSctpAssocMaxRetrans;
		break;
	case (u_char) M2PADEFAULTSCTPSACKDELAY:	/* ReadWrite */
		*write_method = write_m2paDefaultSctpSackDelay;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paDefaultSctpSackDelay);
		rval = (u_char *) &StorageTmp->m2paDefaultSctpSackDelay;
		break;
	case (u_char) M2PADEFAULTSCTPLIFETIME:	/* ReadWrite */
		*write_method = write_m2paDefaultSctpLifetime;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paDefaultSctpLifetime);
		rval = (u_char *) &StorageTmp->m2paDefaultSctpLifetime;
		break;
	default:
		ERROR_MSG("");
	}
	if (rval)
		DEBUGMSGTL(("m2paMIB", "found.\n"));
	else
		DEBUGMSGTL(("m2paMIB", "not found.\n"));
	return (rval);
}

/**
 * @fn struct m2paProtocolProfileTable_data *m2paProtocolProfileTable_create(void)
 * @brief create a fresh data structure representing a new row in the m2paProtocolProfileTable table.
 *
 * Creates a new m2paProtocolProfileTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct m2paProtocolProfileTable_data *
m2paProtocolProfileTable_create(void)
{
	struct m2paProtocolProfileTable_data *StorageNew = SNMP_MALLOC_STRUCT(m2paProtocolProfileTable_data);

	DEBUGMSGTL(("m2paMIB", "m2paProtocolProfileTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->m2paN1 = 1000;
		StorageNew->m2paProving = 1;
		StorageNew->m2paManagementProvingState = 0;
		StorageNew->m2paLoopDelayLower = 0;
		StorageNew->m2paLoopDelayUpper = 0;
		StorageNew->m2paTransmissionRateIntervalLower = 0;
		StorageNew->m2paTransmissionRateIntervalUpper = 0;
		StorageNew->m2paSctpNoDelay = 0;
		StorageNew->m2paSctpMaxseg = 0;
		StorageNew->m2paSctpHeartbeatItvl = 0;
		StorageNew->m2paSctpHeartbeat = 0;
		StorageNew->m2paSctpRtoInitial = 0;
		StorageNew->m2paSctpRtoMin = 0;
		StorageNew->m2paSctpRtoMax = 0;
		StorageNew->m2paSctpPathMaxRetrans = 0;
		StorageNew->m2paSctpCookieLife = 0;
		StorageNew->m2paSctpCookieInc = 0;
		StorageNew->m2paSctpMaxInitRetries = 0;
		StorageNew->m2paSctpMaxBurst = 0;
		StorageNew->m2paSctpAssocMaxRetrans = 0;
		StorageNew->m2paSctpSackDelay = 20;
		StorageNew->m2paSctpLifetime = 0;
		StorageNew->m2paTimerT1 = 0;
		StorageNew->m2paTimerT2 = 0;
		StorageNew->m2paTimerT2L = 0;
		StorageNew->m2paTimerT2H = 0;
		StorageNew->m2paTimerT3 = 0;
		StorageNew->m2paTimerT4N = 0;
		StorageNew->m2paTimerT4E = 0;
		StorageNew->m2paTimerT6 = 0;
		StorageNew->m2paTimerT7 = 0;
		StorageNew->m2paTransCongThresholdAbatementL1Messages = 0;
		StorageNew->m2paTransCongThresholdAbatementL1Octets = 0;
		StorageNew->m2paTransCongThresholdOnsetL1Messages = 0;
		StorageNew->m2paTransCongThresholdOnsetL1Octets = 0;
		StorageNew->m2paProvingAttempts = 0;
		StorageNew->m2paNumberOfThresholdLevels = 0;
		StorageNew->m2paCongestionCounting = 0;
		StorageNew->m2paCongestionReportingBaseObject = 0;
		StorageNew->m2paTimerTx = 0;
		StorageNew->m2paTimerTy = 0;
		StorageNew->m2paNumberOfCongestionStates = 0;
		StorageNew->m2paInitialLevelOfCongestion = 0;
		StorageNew->m2paReceiveCongestionThresholdAbatement = 3;
		StorageNew->m2paReceiveCongestionThresholdOnset = 6;
		StorageNew->m2paReceiveCongestionThresholdDiscard = 9;
		if ((StorageNew->m2paProtocolProfileName = (uint8_t *) strdup("")) != NULL)
			StorageNew->m2paProtocolProfileNameLen = strlen("");
		StorageNew->m2paTransCongThresholdAbatementL2Messages = 0;
		StorageNew->m2paTransCongThresholdAbatementL2Octets = 0;
		StorageNew->m2paTransCongThresholdOnsetL2Messages = 0;
		StorageNew->m2paTransCongThresholdOnsetL2Octets = 0;
		StorageNew->m2paTransCongThresholdAbatementL3Messages = 0;
		StorageNew->m2paTransCongThresholdAbatementL3Octets = 0;
		StorageNew->m2paTransCongThresholdOnsetL3Messages = 0;
		StorageNew->m2paTransCongThresholdOnsetL3Octets = 0;
		StorageNew->m2paTransCongThresholdDiscardL1Messages = 0;
		StorageNew->m2paTransCongThresholdDiscardL1Octets = 0;
		StorageNew->m2paTransCongThresholdDiscardL2Messages = 0;
		StorageNew->m2paTransCongThresholdDiscardL2Octets = 0;
		StorageNew->m2paTransCongThresholdDiscardL3Messages = 0;
		StorageNew->m2paTransCongThresholdDiscardL3Octets = 0;
		StorageNew->m2paProtocolProfileRowStatus = 0;
		StorageNew->m2paProtocolProfileRowStatus = RS_NOTREADY;
	}
	DEBUGMSGTL(("m2paMIB", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct m2paProtocolProfileTable_data *m2paProtocolProfileTable_duplicate(struct m2paProtocolProfileTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicat a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct m2paProtocolProfileTable_data *
m2paProtocolProfileTable_duplicate(struct m2paProtocolProfileTable_data *thedata)
{
	struct m2paProtocolProfileTable_data *StorageNew = SNMP_MALLOC_STRUCT(m2paProtocolProfileTable_data);

	DEBUGMSGTL(("m2paMIB", "m2paProtocolProfileTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("m2paMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	m2paProtocolProfileTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int m2paProtocolProfileTable_destroy(struct m2paProtocolProfileTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
m2paProtocolProfileTable_destroy(struct m2paProtocolProfileTable_data **thedata)
{
	struct m2paProtocolProfileTable_data *StorageDel;

	DEBUGMSGTL(("m2paMIB", "m2paProtocolProfileTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->m2paProtocolProfileId);
		StorageDel->m2paProtocolProfileIdLen = 0;
		SNMP_FREE(StorageDel->m2paProtocolProfileName);
		StorageDel->m2paProtocolProfileNameLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("m2paMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int m2paProtocolProfileTable_add(struct m2paProtocolProfileTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the m2paProtocolProfileTable table data set.
 *
 * Adds a table row structure to the m2paProtocolProfileTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
m2paProtocolProfileTable_add(struct m2paProtocolProfileTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("m2paMIB", "m2paProtocolProfileTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* m2paProtocolProfileId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->m2paProtocolProfileId, thedata->m2paProtocolProfileIdLen);
		header_complex_add_data(&m2paProtocolProfileTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("m2paMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int m2paProtocolProfileTable_del(struct m2paProtocolProfileTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the m2paProtocolProfileTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
m2paProtocolProfileTable_del(struct m2paProtocolProfileTable_data *thedata)
{
	struct m2paProtocolProfileTable_data *StorageDel;

	DEBUGMSGTL(("m2paMIB", "m2paProtocolProfileTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(m2paProtocolProfileTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&m2paProtocolProfileTableStorage, hciptr);
	}
	DEBUGMSGTL(("m2paMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_m2paProtocolProfileTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for m2paProtocolProfileTable entries.
 *
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case m2paProtocolProfileTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_m2paProtocolProfileTable(const char *token, char *line)
{
	size_t tmpsize;
	struct m2paProtocolProfileTable_data *StorageTmp = m2paProtocolProfileTable_create();

	DEBUGMSGTL(("m2paMIB", "parse_m2paProtocolProfileTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->m2paProtocolProfileId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->m2paProtocolProfileId, &StorageTmp->m2paProtocolProfileIdLen);
	if (StorageTmp->m2paProtocolProfileId == NULL) {
		config_perror("invalid specification for m2paProtocolProfileId");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->m2paN1, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2paProving, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2paManagementProvingState, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2paLoopDelayLower, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2paLoopDelayUpper, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->m2paTransmissionRateIntervalLower, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->m2paTransmissionRateIntervalUpper, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2paSctpNoDelay, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->m2paSctpMaxseg, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2paSctpHeartbeatItvl, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2paSctpHeartbeat, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2paSctpRtoInitial, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2paSctpRtoMin, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2paSctpRtoMax, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->m2paSctpPathMaxRetrans, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2paSctpCookieLife, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2paSctpCookieInc, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->m2paSctpMaxInitRetries, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->m2paSctpMaxBurst, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->m2paSctpAssocMaxRetrans, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2paSctpSackDelay, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2paSctpLifetime, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2paTimerT1, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2paTimerT2, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2paTimerT2L, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2paTimerT2H, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2paTimerT3, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2paTimerT4N, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2paTimerT4E, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2paTimerT6, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2paTimerT7, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->m2paTransCongThresholdAbatementL1Messages, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->m2paTransCongThresholdAbatementL1Octets, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->m2paTransCongThresholdOnsetL1Messages, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->m2paTransCongThresholdOnsetL1Octets, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->m2paProvingAttempts, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->m2paNumberOfThresholdLevels, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2paCongestionCounting, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2paCongestionReportingBaseObject, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2paTimerTx, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2paTimerTy, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->m2paNumberOfCongestionStates, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->m2paInitialLevelOfCongestion, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->m2paReceiveCongestionThresholdAbatement, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->m2paReceiveCongestionThresholdOnset, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->m2paReceiveCongestionThresholdDiscard, &tmpsize);
	SNMP_FREE(StorageTmp->m2paProtocolProfileName);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->m2paProtocolProfileName, &StorageTmp->m2paProtocolProfileNameLen);
	if (StorageTmp->m2paProtocolProfileName == NULL) {
		config_perror("invalid specification for m2paProtocolProfileName");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->m2paTransCongThresholdAbatementL2Messages, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->m2paTransCongThresholdAbatementL2Octets, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->m2paTransCongThresholdOnsetL2Messages, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->m2paTransCongThresholdOnsetL2Octets, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->m2paTransCongThresholdAbatementL3Messages, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->m2paTransCongThresholdAbatementL3Octets, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->m2paTransCongThresholdOnsetL3Messages, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->m2paTransCongThresholdOnsetL3Octets, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->m2paTransCongThresholdDiscardL1Messages, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->m2paTransCongThresholdDiscardL1Octets, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->m2paTransCongThresholdDiscardL2Messages, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->m2paTransCongThresholdDiscardL2Octets, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->m2paTransCongThresholdDiscardL3Messages, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->m2paTransCongThresholdDiscardL3Octets, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2paProtocolProfileRowStatus, &tmpsize);
	m2paProtocolProfileTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("m2paMIB", "done.\n"));
}

/*
 * store_m2paProtocolProfileTable(): store configuraiton file for m2paProtocolProfileTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_m2paProtocolProfileTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct m2paProtocolProfileTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("m2paMIB", "store_m2paProtocolProfileTable: storing data...  "));
	refresh_m2paProtocolProfileTable(1);
	(void) tmpsize;
	for (hcindex = m2paProtocolProfileTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct m2paProtocolProfileTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "m2paProtocolProfileTable ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->m2paProtocolProfileId, &StorageTmp->m2paProtocolProfileIdLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->m2paN1, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2paProving, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2paManagementProvingState, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2paLoopDelayLower, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2paLoopDelayUpper, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->m2paTransmissionRateIntervalLower, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->m2paTransmissionRateIntervalUpper, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2paSctpNoDelay, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->m2paSctpMaxseg, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2paSctpHeartbeatItvl, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2paSctpHeartbeat, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2paSctpRtoInitial, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2paSctpRtoMin, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2paSctpRtoMax, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->m2paSctpPathMaxRetrans, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2paSctpCookieLife, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2paSctpCookieInc, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->m2paSctpMaxInitRetries, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->m2paSctpMaxBurst, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->m2paSctpAssocMaxRetrans, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2paSctpSackDelay, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2paSctpLifetime, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2paTimerT1, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2paTimerT2, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2paTimerT2L, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2paTimerT2H, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2paTimerT3, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2paTimerT4N, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2paTimerT4E, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2paTimerT6, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2paTimerT7, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->m2paTransCongThresholdAbatementL1Messages, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->m2paTransCongThresholdAbatementL1Octets, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->m2paTransCongThresholdOnsetL1Messages, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->m2paTransCongThresholdOnsetL1Octets, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->m2paProvingAttempts, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->m2paNumberOfThresholdLevels, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2paCongestionCounting, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2paCongestionReportingBaseObject, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2paTimerTx, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2paTimerTy, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->m2paNumberOfCongestionStates, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->m2paInitialLevelOfCongestion, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->m2paReceiveCongestionThresholdAbatement, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->m2paReceiveCongestionThresholdOnset, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->m2paReceiveCongestionThresholdDiscard, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->m2paProtocolProfileName, &StorageTmp->m2paProtocolProfileNameLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->m2paTransCongThresholdAbatementL2Messages, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->m2paTransCongThresholdAbatementL2Octets, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->m2paTransCongThresholdOnsetL2Messages, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->m2paTransCongThresholdOnsetL2Octets, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->m2paTransCongThresholdAbatementL3Messages, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->m2paTransCongThresholdAbatementL3Octets, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->m2paTransCongThresholdOnsetL3Messages, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->m2paTransCongThresholdOnsetL3Octets, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->m2paTransCongThresholdDiscardL1Messages, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->m2paTransCongThresholdDiscardL1Octets, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->m2paTransCongThresholdDiscardL2Messages, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->m2paTransCongThresholdDiscardL2Octets, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->m2paTransCongThresholdDiscardL3Messages, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->m2paTransCongThresholdDiscardL3Octets, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2paProtocolProfileRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("m2paMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void refresh_m2paProtocolProfileTable_row(struct m2paProtocolProfileTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the m2paProtocolProfileTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct m2paProtocolProfileTable_data *
refresh_m2paProtocolProfileTable_row(struct m2paProtocolProfileTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->m2paProtocolProfileTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->m2paProtocolProfileTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_m2paProtocolProfileTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the m2paProtocolProfileTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_m2paProtocolProfileTable(int force)
{
	if (!force && m2paProtocolProfileTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	m2paProtocolProfileTable_refresh = 0;
}

/**
 * @fn u_char *var_m2paProtocolProfileTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in m2paProtocolProfileTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_m2paMIB above.
 */
u_char *
var_m2paProtocolProfileTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("m2paMIB", "var_m2paProtocolProfileTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_m2paProtocolProfileTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(m2paProtocolProfileTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_m2paProtocolProfileTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) M2PAN1:	/* Create */
		*write_method = write_m2paN1;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paN1);
		rval = (u_char *) &StorageTmp->m2paN1;
		break;
	case (u_char) M2PAPROVING:	/* Create */
		*write_method = write_m2paProving;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paProving);
		rval = (u_char *) &StorageTmp->m2paProving;
		break;
	case (u_char) M2PAMANAGEMENTPROVINGSTATE:	/* Create */
		*write_method = write_m2paManagementProvingState;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paManagementProvingState);
		rval = (u_char *) &StorageTmp->m2paManagementProvingState;
		break;
	case (u_char) M2PALOOPDELAYLOWER:	/* Create */
		*write_method = write_m2paLoopDelayLower;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paLoopDelayLower);
		rval = (u_char *) &StorageTmp->m2paLoopDelayLower;
		break;
	case (u_char) M2PALOOPDELAYUPPER:	/* Create */
		*write_method = write_m2paLoopDelayUpper;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paLoopDelayUpper);
		rval = (u_char *) &StorageTmp->m2paLoopDelayUpper;
		break;
	case (u_char) M2PATRANSMISSIONRATEINTERVALLOWER:	/* Create */
		*write_method = write_m2paTransmissionRateIntervalLower;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paTransmissionRateIntervalLower);
		rval = (u_char *) &StorageTmp->m2paTransmissionRateIntervalLower;
		break;
	case (u_char) M2PATRANSMISSIONRATEINTERVALUPPER:	/* Create */
		*write_method = write_m2paTransmissionRateIntervalUpper;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paTransmissionRateIntervalUpper);
		rval = (u_char *) &StorageTmp->m2paTransmissionRateIntervalUpper;
		break;
	case (u_char) M2PASCTPNODELAY:	/* Create */
		*write_method = write_m2paSctpNoDelay;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paSctpNoDelay);
		rval = (u_char *) &StorageTmp->m2paSctpNoDelay;
		break;
	case (u_char) M2PASCTPMAXSEG:	/* Create */
		*write_method = write_m2paSctpMaxseg;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paSctpMaxseg);
		rval = (u_char *) &StorageTmp->m2paSctpMaxseg;
		break;
	case (u_char) M2PASCTPHEARTBEATITVL:	/* Create */
		*write_method = write_m2paSctpHeartbeatItvl;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paSctpHeartbeatItvl);
		rval = (u_char *) &StorageTmp->m2paSctpHeartbeatItvl;
		break;
	case (u_char) M2PASCTPHEARTBEAT:	/* Create */
		*write_method = write_m2paSctpHeartbeat;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paSctpHeartbeat);
		rval = (u_char *) &StorageTmp->m2paSctpHeartbeat;
		break;
	case (u_char) M2PASCTPRTOINITIAL:	/* Create */
		*write_method = write_m2paSctpRtoInitial;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paSctpRtoInitial);
		rval = (u_char *) &StorageTmp->m2paSctpRtoInitial;
		break;
	case (u_char) M2PASCTPRTOMIN:	/* Create */
		*write_method = write_m2paSctpRtoMin;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paSctpRtoMin);
		rval = (u_char *) &StorageTmp->m2paSctpRtoMin;
		break;
	case (u_char) M2PASCTPRTOMAX:	/* Create */
		*write_method = write_m2paSctpRtoMax;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paSctpRtoMax);
		rval = (u_char *) &StorageTmp->m2paSctpRtoMax;
		break;
	case (u_char) M2PASCTPPATHMAXRETRANS:	/* Create */
		*write_method = write_m2paSctpPathMaxRetrans;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paSctpPathMaxRetrans);
		rval = (u_char *) &StorageTmp->m2paSctpPathMaxRetrans;
		break;
	case (u_char) M2PASCTPCOOKIELIFE:	/* Create */
		*write_method = write_m2paSctpCookieLife;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paSctpCookieLife);
		rval = (u_char *) &StorageTmp->m2paSctpCookieLife;
		break;
	case (u_char) M2PASCTPCOOKIEINC:	/* Create */
		*write_method = write_m2paSctpCookieInc;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paSctpCookieInc);
		rval = (u_char *) &StorageTmp->m2paSctpCookieInc;
		break;
	case (u_char) M2PASCTPMAXINITRETRIES:	/* Create */
		*write_method = write_m2paSctpMaxInitRetries;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paSctpMaxInitRetries);
		rval = (u_char *) &StorageTmp->m2paSctpMaxInitRetries;
		break;
	case (u_char) M2PASCTPMAXBURST:	/* Create */
		*write_method = write_m2paSctpMaxBurst;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paSctpMaxBurst);
		rval = (u_char *) &StorageTmp->m2paSctpMaxBurst;
		break;
	case (u_char) M2PASCTPASSOCMAXRETRANS:	/* Create */
		*write_method = write_m2paSctpAssocMaxRetrans;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paSctpAssocMaxRetrans);
		rval = (u_char *) &StorageTmp->m2paSctpAssocMaxRetrans;
		break;
	case (u_char) M2PASCTPSACKDELAY:	/* Create */
		*write_method = write_m2paSctpSackDelay;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paSctpSackDelay);
		rval = (u_char *) &StorageTmp->m2paSctpSackDelay;
		break;
	case (u_char) M2PASCTPLIFETIME:	/* Create */
		*write_method = write_m2paSctpLifetime;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paSctpLifetime);
		rval = (u_char *) &StorageTmp->m2paSctpLifetime;
		break;
	case (u_char) M2PATIMERT1:	/* Create */
		*write_method = write_m2paTimerT1;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paTimerT1);
		rval = (u_char *) &StorageTmp->m2paTimerT1;
		break;
	case (u_char) M2PATIMERT2:	/* Create */
		*write_method = write_m2paTimerT2;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paTimerT2);
		rval = (u_char *) &StorageTmp->m2paTimerT2;
		break;
	case (u_char) M2PATIMERT2L:	/* Create */
		*write_method = write_m2paTimerT2L;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paTimerT2L);
		rval = (u_char *) &StorageTmp->m2paTimerT2L;
		break;
	case (u_char) M2PATIMERT2H:	/* Create */
		*write_method = write_m2paTimerT2H;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paTimerT2H);
		rval = (u_char *) &StorageTmp->m2paTimerT2H;
		break;
	case (u_char) M2PATIMERT3:	/* Create */
		*write_method = write_m2paTimerT3;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paTimerT3);
		rval = (u_char *) &StorageTmp->m2paTimerT3;
		break;
	case (u_char) M2PATIMERT4N:	/* Create */
		*write_method = write_m2paTimerT4N;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paTimerT4N);
		rval = (u_char *) &StorageTmp->m2paTimerT4N;
		break;
	case (u_char) M2PATIMERT4E:	/* Create */
		*write_method = write_m2paTimerT4E;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paTimerT4E);
		rval = (u_char *) &StorageTmp->m2paTimerT4E;
		break;
	case (u_char) M2PATIMERT6:	/* Create */
		*write_method = write_m2paTimerT6;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paTimerT6);
		rval = (u_char *) &StorageTmp->m2paTimerT6;
		break;
	case (u_char) M2PATIMERT7:	/* Create */
		*write_method = write_m2paTimerT7;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paTimerT7);
		rval = (u_char *) &StorageTmp->m2paTimerT7;
		break;
	case (u_char) M2PATRANSCONGTHRESHOLDABATEMENTL1MESSAGES:	/* Create */
		*write_method = write_m2paTransCongThresholdAbatementL1Messages;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paTransCongThresholdAbatementL1Messages);
		rval = (u_char *) &StorageTmp->m2paTransCongThresholdAbatementL1Messages;
		break;
	case (u_char) M2PATRANSCONGTHRESHOLDABATEMENTL1OCTETS:	/* Create */
		*write_method = write_m2paTransCongThresholdAbatementL1Octets;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paTransCongThresholdAbatementL1Octets);
		rval = (u_char *) &StorageTmp->m2paTransCongThresholdAbatementL1Octets;
		break;
	case (u_char) M2PATRANSCONGTHRESHOLDONSETL1MESSAGES:	/* Create */
		*write_method = write_m2paTransCongThresholdOnsetL1Messages;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paTransCongThresholdOnsetL1Messages);
		rval = (u_char *) &StorageTmp->m2paTransCongThresholdOnsetL1Messages;
		break;
	case (u_char) M2PATRANSCONGTHRESHOLDONSETL1OCTETS:	/* Create */
		*write_method = write_m2paTransCongThresholdOnsetL1Octets;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paTransCongThresholdOnsetL1Octets);
		rval = (u_char *) &StorageTmp->m2paTransCongThresholdOnsetL1Octets;
		break;
	case (u_char) M2PAPROVINGATTEMPTS:	/* Create */
		*write_method = write_m2paProvingAttempts;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paProvingAttempts);
		rval = (u_char *) &StorageTmp->m2paProvingAttempts;
		break;
	case (u_char) M2PANUMBEROFTHRESHOLDLEVELS:	/* Create */
		*write_method = write_m2paNumberOfThresholdLevels;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paNumberOfThresholdLevels);
		rval = (u_char *) &StorageTmp->m2paNumberOfThresholdLevels;
		break;
	case (u_char) M2PACONGESTIONCOUNTING:	/* Create */
		*write_method = write_m2paCongestionCounting;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paCongestionCounting);
		rval = (u_char *) &StorageTmp->m2paCongestionCounting;
		break;
	case (u_char) M2PACONGESTIONREPORTINGBASEOBJECT:	/* Create */
		*write_method = write_m2paCongestionReportingBaseObject;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paCongestionReportingBaseObject);
		rval = (u_char *) &StorageTmp->m2paCongestionReportingBaseObject;
		break;
	case (u_char) M2PATIMERTX:	/* Create */
		*write_method = write_m2paTimerTx;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paTimerTx);
		rval = (u_char *) &StorageTmp->m2paTimerTx;
		break;
	case (u_char) M2PATIMERTY:	/* Create */
		*write_method = write_m2paTimerTy;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paTimerTy);
		rval = (u_char *) &StorageTmp->m2paTimerTy;
		break;
	case (u_char) M2PANUMBEROFCONGESTIONSTATES:	/* Create */
		*write_method = write_m2paNumberOfCongestionStates;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paNumberOfCongestionStates);
		rval = (u_char *) &StorageTmp->m2paNumberOfCongestionStates;
		break;
	case (u_char) M2PAINITIALLEVELOFCONGESTION:	/* Create */
		*write_method = write_m2paInitialLevelOfCongestion;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paInitialLevelOfCongestion);
		rval = (u_char *) &StorageTmp->m2paInitialLevelOfCongestion;
		break;
	case (u_char) M2PARECEIVECONGESTIONTHRESHOLDABATEMENT:	/* Create */
		*write_method = write_m2paReceiveCongestionThresholdAbatement;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paReceiveCongestionThresholdAbatement);
		rval = (u_char *) &StorageTmp->m2paReceiveCongestionThresholdAbatement;
		break;
	case (u_char) M2PARECEIVECONGESTIONTHRESHOLDONSET:	/* Create */
		*write_method = write_m2paReceiveCongestionThresholdOnset;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paReceiveCongestionThresholdOnset);
		rval = (u_char *) &StorageTmp->m2paReceiveCongestionThresholdOnset;
		break;
	case (u_char) M2PARECEIVECONGESTIONTHRESHOLDDISCARD:	/* Create */
		*write_method = write_m2paReceiveCongestionThresholdDiscard;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paReceiveCongestionThresholdDiscard);
		rval = (u_char *) &StorageTmp->m2paReceiveCongestionThresholdDiscard;
		break;
	case (u_char) M2PAPROTOCOLPROFILENAME:	/* Create */
		*write_method = write_m2paProtocolProfileName;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->m2paProtocolProfileNameLen;
		rval = (u_char *) StorageTmp->m2paProtocolProfileName;
		break;
	case (u_char) M2PATRANSCONGTHRESHOLDABATEMENTL2MESSAGES:	/* Create */
		*write_method = write_m2paTransCongThresholdAbatementL2Messages;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paTransCongThresholdAbatementL2Messages);
		rval = (u_char *) &StorageTmp->m2paTransCongThresholdAbatementL2Messages;
		break;
	case (u_char) M2PATRANSCONGTHRESHOLDABATEMENTL2OCTETS:	/* Create */
		*write_method = write_m2paTransCongThresholdAbatementL2Octets;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paTransCongThresholdAbatementL2Octets);
		rval = (u_char *) &StorageTmp->m2paTransCongThresholdAbatementL2Octets;
		break;
	case (u_char) M2PATRANSCONGTHRESHOLDONSETL2MESSAGES:	/* Create */
		*write_method = write_m2paTransCongThresholdOnsetL2Messages;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paTransCongThresholdOnsetL2Messages);
		rval = (u_char *) &StorageTmp->m2paTransCongThresholdOnsetL2Messages;
		break;
	case (u_char) M2PATRANSCONGTHRESHOLDONSETL2OCTETS:	/* Create */
		*write_method = write_m2paTransCongThresholdOnsetL2Octets;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paTransCongThresholdOnsetL2Octets);
		rval = (u_char *) &StorageTmp->m2paTransCongThresholdOnsetL2Octets;
		break;
	case (u_char) M2PATRANSCONGTHRESHOLDABATEMENTL3MESSAGES:	/* Create */
		*write_method = write_m2paTransCongThresholdAbatementL3Messages;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paTransCongThresholdAbatementL3Messages);
		rval = (u_char *) &StorageTmp->m2paTransCongThresholdAbatementL3Messages;
		break;
	case (u_char) M2PATRANSCONGTHRESHOLDABATEMENTL3OCTETS:	/* Create */
		*write_method = write_m2paTransCongThresholdAbatementL3Octets;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paTransCongThresholdAbatementL3Octets);
		rval = (u_char *) &StorageTmp->m2paTransCongThresholdAbatementL3Octets;
		break;
	case (u_char) M2PATRANSCONGTHRESHOLDONSETL3MESSAGES:	/* Create */
		*write_method = write_m2paTransCongThresholdOnsetL3Messages;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paTransCongThresholdOnsetL3Messages);
		rval = (u_char *) &StorageTmp->m2paTransCongThresholdOnsetL3Messages;
		break;
	case (u_char) M2PATRANSCONGTHRESHOLDONSETL3OCTETS:	/* Create */
		*write_method = write_m2paTransCongThresholdOnsetL3Octets;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paTransCongThresholdOnsetL3Octets);
		rval = (u_char *) &StorageTmp->m2paTransCongThresholdOnsetL3Octets;
		break;
	case (u_char) M2PATRANSCONGTHRESHOLDDISCARDL1MESSAGES:	/* Create */
		*write_method = write_m2paTransCongThresholdDiscardL1Messages;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paTransCongThresholdDiscardL1Messages);
		rval = (u_char *) &StorageTmp->m2paTransCongThresholdDiscardL1Messages;
		break;
	case (u_char) M2PATRANSCONGTHRESHOLDDISCARDL1OCTETS:	/* Create */
		*write_method = write_m2paTransCongThresholdDiscardL1Octets;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paTransCongThresholdDiscardL1Octets);
		rval = (u_char *) &StorageTmp->m2paTransCongThresholdDiscardL1Octets;
		break;
	case (u_char) M2PATRANSCONGTHRESHOLDDISCARDL2MESSAGES:	/* Create */
		*write_method = write_m2paTransCongThresholdDiscardL2Messages;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paTransCongThresholdDiscardL2Messages);
		rval = (u_char *) &StorageTmp->m2paTransCongThresholdDiscardL2Messages;
		break;
	case (u_char) M2PATRANSCONGTHRESHOLDDISCARDL2OCTETS:	/* Create */
		*write_method = write_m2paTransCongThresholdDiscardL2Octets;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paTransCongThresholdDiscardL2Octets);
		rval = (u_char *) &StorageTmp->m2paTransCongThresholdDiscardL2Octets;
		break;
	case (u_char) M2PATRANSCONGTHRESHOLDDISCARDL3MESSAGES:	/* Create */
		*write_method = write_m2paTransCongThresholdDiscardL3Messages;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paTransCongThresholdDiscardL3Messages);
		rval = (u_char *) &StorageTmp->m2paTransCongThresholdDiscardL3Messages;
		break;
	case (u_char) M2PATRANSCONGTHRESHOLDDISCARDL3OCTETS:	/* Create */
		*write_method = write_m2paTransCongThresholdDiscardL3Octets;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paTransCongThresholdDiscardL3Octets);
		rval = (u_char *) &StorageTmp->m2paTransCongThresholdDiscardL3Octets;
		break;
	case (u_char) M2PAPROTOCOLPROFILEROWSTATUS:	/* Create */
		*write_method = write_m2paProtocolProfileRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m2paProtocolProfileRowStatus);
		rval = (u_char *) &StorageTmp->m2paProtocolProfileRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int write_m2paN1(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2paN1(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static ulong old_value;
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	ulong set_value = *((ulong *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paN1 entering action=%d...  \n", action));
	StorageTmp = header_complex(m2paProtocolProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2paProtocolProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paN1: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paN1 not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paN1: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 1000 */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m2paN1 for you to use, and you have just been asked to do something with it.  Note that anything done here must be
				   reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->m2paN1;
		StorageTmp->m2paN1 = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paN1 = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paProving(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2paProving(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paProving entering action=%d...  \n", action));
	StorageTmp = header_complex(m2paProtocolProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2paProtocolProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paProving: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paProving not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paProving: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 1 */
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paProving: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m2paProving for you to use, and you have just been asked to do something with it.  Note that anything done here must be
				   reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->m2paProving;
		StorageTmp->m2paProving = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paProving = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paManagementProvingState(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2paManagementProvingState(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paManagementProvingState entering action=%d...  \n", action));
	StorageTmp = header_complex(m2paProtocolProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2paProtocolProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paManagementProvingState: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paManagementProvingState not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paManagementProvingState: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case M2PAMANAGEMENTPROVINGSTATE_NORMAL:
		case M2PAMANAGEMENTPROVINGSTATE_EMERGENCY:
		case M2PAMANAGEMENTPROVINGSTATE_NEUTRAL:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paManagementProvingState: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m2paManagementProvingState for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->m2paManagementProvingState;
		StorageTmp->m2paManagementProvingState = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paManagementProvingState = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paLoopDelayLower(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2paLoopDelayLower(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paLoopDelayLower entering action=%d...  \n", action));
	StorageTmp = header_complex(m2paProtocolProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2paProtocolProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paLoopDelayLower: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paLoopDelayLower not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paLoopDelayLower: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paLoopDelayLower: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m2paLoopDelayLower for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->m2paLoopDelayLower;
		StorageTmp->m2paLoopDelayLower = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paLoopDelayLower = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paLoopDelayUpper(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2paLoopDelayUpper(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paLoopDelayUpper entering action=%d...  \n", action));
	StorageTmp = header_complex(m2paProtocolProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2paProtocolProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paLoopDelayUpper: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paLoopDelayUpper not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paLoopDelayUpper: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paLoopDelayUpper: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m2paLoopDelayUpper for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->m2paLoopDelayUpper;
		StorageTmp->m2paLoopDelayUpper = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paLoopDelayUpper = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paTransmissionRateIntervalLower(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2paTransmissionRateIntervalLower(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static ulong old_value;
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	ulong set_value = *((ulong *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paTransmissionRateIntervalLower entering action=%d...  \n", action));
	StorageTmp = header_complex(m2paProtocolProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2paProtocolProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTransmissionRateIntervalLower: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTransmissionRateIntervalLower not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTransmissionRateIntervalLower: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m2paTransmissionRateIntervalLower for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->m2paTransmissionRateIntervalLower;
		StorageTmp->m2paTransmissionRateIntervalLower = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paTransmissionRateIntervalLower = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paTransmissionRateIntervalUpper(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2paTransmissionRateIntervalUpper(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static ulong old_value;
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	ulong set_value = *((ulong *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paTransmissionRateIntervalUpper entering action=%d...  \n", action));
	StorageTmp = header_complex(m2paProtocolProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2paProtocolProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTransmissionRateIntervalUpper: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTransmissionRateIntervalUpper not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTransmissionRateIntervalUpper: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m2paTransmissionRateIntervalUpper for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->m2paTransmissionRateIntervalUpper;
		StorageTmp->m2paTransmissionRateIntervalUpper = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paTransmissionRateIntervalUpper = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paSctpNoDelay(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2paSctpNoDelay(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paSctpNoDelay entering action=%d...  \n", action));
	StorageTmp = header_complex(m2paProtocolProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2paProtocolProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paSctpNoDelay: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paSctpNoDelay not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paSctpNoDelay: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paSctpNoDelay: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m2paSctpNoDelay for you to use, and you have just been asked to do something with it.  Note that anything done here must 
				   be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->m2paSctpNoDelay;
		StorageTmp->m2paSctpNoDelay = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paSctpNoDelay = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paSctpMaxseg(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2paSctpMaxseg(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static ulong old_value;
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	ulong set_value = *((ulong *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paSctpMaxseg entering action=%d...  \n", action));
	StorageTmp = header_complex(m2paProtocolProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2paProtocolProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paSctpMaxseg: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paSctpMaxseg not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paSctpMaxseg: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 1..65536 */
		if ((1 > set_value || set_value > 65536)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paSctpMaxseg: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m2paSctpMaxseg for you to use, and you have just been asked to do something with it.  Note that anything done here must
				   be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->m2paSctpMaxseg;
		StorageTmp->m2paSctpMaxseg = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paSctpMaxseg = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paSctpHeartbeatItvl(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2paSctpHeartbeatItvl(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paSctpHeartbeatItvl entering action=%d...  \n", action));
	StorageTmp = header_complex(m2paProtocolProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2paProtocolProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paSctpHeartbeatItvl: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paSctpHeartbeatItvl not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paSctpHeartbeatItvl: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paSctpHeartbeatItvl: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m2paSctpHeartbeatItvl for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->m2paSctpHeartbeatItvl;
		StorageTmp->m2paSctpHeartbeatItvl = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paSctpHeartbeatItvl = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paSctpHeartbeat(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2paSctpHeartbeat(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paSctpHeartbeat entering action=%d...  \n", action));
	StorageTmp = header_complex(m2paProtocolProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2paProtocolProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paSctpHeartbeat: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paSctpHeartbeat not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paSctpHeartbeat: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paSctpHeartbeat: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m2paSctpHeartbeat for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->m2paSctpHeartbeat;
		StorageTmp->m2paSctpHeartbeat = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paSctpHeartbeat = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paSctpRtoInitial(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2paSctpRtoInitial(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paSctpRtoInitial entering action=%d...  \n", action));
	StorageTmp = header_complex(m2paProtocolProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2paProtocolProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paSctpRtoInitial: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paSctpRtoInitial not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paSctpRtoInitial: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paSctpRtoInitial: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m2paSctpRtoInitial for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->m2paSctpRtoInitial;
		StorageTmp->m2paSctpRtoInitial = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paSctpRtoInitial = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paSctpRtoMin(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2paSctpRtoMin(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paSctpRtoMin entering action=%d...  \n", action));
	StorageTmp = header_complex(m2paProtocolProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2paProtocolProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paSctpRtoMin: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paSctpRtoMin not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paSctpRtoMin: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paSctpRtoMin: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m2paSctpRtoMin for you to use, and you have just been asked to do something with it.  Note that anything done here must
				   be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->m2paSctpRtoMin;
		StorageTmp->m2paSctpRtoMin = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paSctpRtoMin = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paSctpRtoMax(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2paSctpRtoMax(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paSctpRtoMax entering action=%d...  \n", action));
	StorageTmp = header_complex(m2paProtocolProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2paProtocolProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paSctpRtoMax: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paSctpRtoMax not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paSctpRtoMax: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paSctpRtoMax: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m2paSctpRtoMax for you to use, and you have just been asked to do something with it.  Note that anything done here must
				   be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->m2paSctpRtoMax;
		StorageTmp->m2paSctpRtoMax = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paSctpRtoMax = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paSctpPathMaxRetrans(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2paSctpPathMaxRetrans(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static ulong old_value;
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	ulong set_value = *((ulong *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paSctpPathMaxRetrans entering action=%d...  \n", action));
	StorageTmp = header_complex(m2paProtocolProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2paProtocolProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paSctpPathMaxRetrans: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paSctpPathMaxRetrans not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paSctpPathMaxRetrans: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m2paSctpPathMaxRetrans for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->m2paSctpPathMaxRetrans;
		StorageTmp->m2paSctpPathMaxRetrans = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paSctpPathMaxRetrans = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paSctpCookieLife(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2paSctpCookieLife(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paSctpCookieLife entering action=%d...  \n", action));
	StorageTmp = header_complex(m2paProtocolProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2paProtocolProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paSctpCookieLife: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paSctpCookieLife not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paSctpCookieLife: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paSctpCookieLife: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m2paSctpCookieLife for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->m2paSctpCookieLife;
		StorageTmp->m2paSctpCookieLife = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paSctpCookieLife = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paSctpCookieInc(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2paSctpCookieInc(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paSctpCookieInc entering action=%d...  \n", action));
	StorageTmp = header_complex(m2paProtocolProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2paProtocolProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paSctpCookieInc: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paSctpCookieInc not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paSctpCookieInc: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paSctpCookieInc: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m2paSctpCookieInc for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->m2paSctpCookieInc;
		StorageTmp->m2paSctpCookieInc = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paSctpCookieInc = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paSctpMaxInitRetries(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2paSctpMaxInitRetries(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static ulong old_value;
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	ulong set_value = *((ulong *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paSctpMaxInitRetries entering action=%d...  \n", action));
	StorageTmp = header_complex(m2paProtocolProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2paProtocolProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paSctpMaxInitRetries: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paSctpMaxInitRetries not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paSctpMaxInitRetries: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m2paSctpMaxInitRetries for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->m2paSctpMaxInitRetries;
		StorageTmp->m2paSctpMaxInitRetries = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paSctpMaxInitRetries = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paSctpMaxBurst(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2paSctpMaxBurst(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static ulong old_value;
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	ulong set_value = *((ulong *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paSctpMaxBurst entering action=%d...  \n", action));
	StorageTmp = header_complex(m2paProtocolProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2paProtocolProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paSctpMaxBurst: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paSctpMaxBurst not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paSctpMaxBurst: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m2paSctpMaxBurst for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->m2paSctpMaxBurst;
		StorageTmp->m2paSctpMaxBurst = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paSctpMaxBurst = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paSctpAssocMaxRetrans(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2paSctpAssocMaxRetrans(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static ulong old_value;
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	ulong set_value = *((ulong *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paSctpAssocMaxRetrans entering action=%d...  \n", action));
	StorageTmp = header_complex(m2paProtocolProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2paProtocolProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paSctpAssocMaxRetrans: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paSctpAssocMaxRetrans not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paSctpAssocMaxRetrans: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m2paSctpAssocMaxRetrans for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->m2paSctpAssocMaxRetrans;
		StorageTmp->m2paSctpAssocMaxRetrans = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paSctpAssocMaxRetrans = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paSctpSackDelay(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2paSctpSackDelay(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paSctpSackDelay entering action=%d...  \n", action));
	StorageTmp = header_complex(m2paProtocolProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2paProtocolProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paSctpSackDelay: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paSctpSackDelay not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paSctpSackDelay: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 20 */
		/* Note: ranges 0..50 */
		if ((0 > set_value || set_value > 50)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paSctpSackDelay: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m2paSctpSackDelay for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->m2paSctpSackDelay;
		StorageTmp->m2paSctpSackDelay = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paSctpSackDelay = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paSctpLifetime(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2paSctpLifetime(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paSctpLifetime entering action=%d...  \n", action));
	StorageTmp = header_complex(m2paProtocolProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2paProtocolProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paSctpLifetime: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paSctpLifetime not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paSctpLifetime: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paSctpLifetime: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m2paSctpLifetime for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->m2paSctpLifetime;
		StorageTmp->m2paSctpLifetime = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paSctpLifetime = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paTimerT1(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2paTimerT1(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paTimerT1 entering action=%d...  \n", action));
	StorageTmp = header_complex(m2paProtocolProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2paProtocolProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTimerT1: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTimerT1 not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTimerT1: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTimerT1: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m2paTimerT1 for you to use, and you have just been asked to do something with it.  Note that anything done here must be
				   reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->m2paTimerT1;
		StorageTmp->m2paTimerT1 = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paTimerT1 = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paTimerT2(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2paTimerT2(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paTimerT2 entering action=%d...  \n", action));
	StorageTmp = header_complex(m2paProtocolProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2paProtocolProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTimerT2: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTimerT2 not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTimerT2: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTimerT2: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m2paTimerT2 for you to use, and you have just been asked to do something with it.  Note that anything done here must be
				   reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->m2paTimerT2;
		StorageTmp->m2paTimerT2 = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paTimerT2 = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paTimerT2L(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2paTimerT2L(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paTimerT2L entering action=%d...  \n", action));
	StorageTmp = header_complex(m2paProtocolProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2paProtocolProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTimerT2L: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTimerT2L not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTimerT2L: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTimerT2L: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m2paTimerT2L for you to use, and you have just been asked to do something with it.  Note that anything done here must be 
				   reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->m2paTimerT2L;
		StorageTmp->m2paTimerT2L = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paTimerT2L = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paTimerT2H(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2paTimerT2H(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paTimerT2H entering action=%d...  \n", action));
	StorageTmp = header_complex(m2paProtocolProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2paProtocolProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTimerT2H: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTimerT2H not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTimerT2H: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTimerT2H: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m2paTimerT2H for you to use, and you have just been asked to do something with it.  Note that anything done here must be 
				   reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->m2paTimerT2H;
		StorageTmp->m2paTimerT2H = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paTimerT2H = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paTimerT3(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2paTimerT3(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paTimerT3 entering action=%d...  \n", action));
	StorageTmp = header_complex(m2paProtocolProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2paProtocolProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTimerT3: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTimerT3 not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTimerT3: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTimerT3: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m2paTimerT3 for you to use, and you have just been asked to do something with it.  Note that anything done here must be
				   reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->m2paTimerT3;
		StorageTmp->m2paTimerT3 = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paTimerT3 = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paTimerT4N(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2paTimerT4N(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paTimerT4N entering action=%d...  \n", action));
	StorageTmp = header_complex(m2paProtocolProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2paProtocolProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTimerT4N: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTimerT4N not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTimerT4N: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTimerT4N: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m2paTimerT4N for you to use, and you have just been asked to do something with it.  Note that anything done here must be 
				   reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->m2paTimerT4N;
		StorageTmp->m2paTimerT4N = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paTimerT4N = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paTimerT4E(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2paTimerT4E(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paTimerT4E entering action=%d...  \n", action));
	StorageTmp = header_complex(m2paProtocolProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2paProtocolProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTimerT4E: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTimerT4E not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTimerT4E: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTimerT4E: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m2paTimerT4E for you to use, and you have just been asked to do something with it.  Note that anything done here must be 
				   reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->m2paTimerT4E;
		StorageTmp->m2paTimerT4E = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paTimerT4E = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paTimerT6(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2paTimerT6(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paTimerT6 entering action=%d...  \n", action));
	StorageTmp = header_complex(m2paProtocolProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2paProtocolProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTimerT6: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTimerT6 not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTimerT6: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTimerT6: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m2paTimerT6 for you to use, and you have just been asked to do something with it.  Note that anything done here must be
				   reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->m2paTimerT6;
		StorageTmp->m2paTimerT6 = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paTimerT6 = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paTimerT7(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2paTimerT7(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paTimerT7 entering action=%d...  \n", action));
	StorageTmp = header_complex(m2paProtocolProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2paProtocolProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTimerT7: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTimerT7 not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTimerT7: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTimerT7: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m2paTimerT7 for you to use, and you have just been asked to do something with it.  Note that anything done here must be
				   reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->m2paTimerT7;
		StorageTmp->m2paTimerT7 = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paTimerT7 = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paTransCongThresholdAbatementL1Messages(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2paTransCongThresholdAbatementL1Messages(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static ulong old_value;
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	ulong set_value = *((ulong *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paTransCongThresholdAbatementL1Messages entering action=%d...  \n", action));
	StorageTmp = header_complex(m2paProtocolProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2paProtocolProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTransCongThresholdAbatementL1Messages: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTransCongThresholdAbatementL1Messages not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTransCongThresholdAbatementL1Messages: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m2paTransCongThresholdAbatementL1Messages for you to use, and you have just been asked to do something with it.  Note
				   that anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->m2paTransCongThresholdAbatementL1Messages;
		StorageTmp->m2paTransCongThresholdAbatementL1Messages = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paTransCongThresholdAbatementL1Messages = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paTransCongThresholdAbatementL1Octets(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2paTransCongThresholdAbatementL1Octets(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static ulong old_value;
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	ulong set_value = *((ulong *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paTransCongThresholdAbatementL1Octets entering action=%d...  \n", action));
	StorageTmp = header_complex(m2paProtocolProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2paProtocolProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTransCongThresholdAbatementL1Octets: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTransCongThresholdAbatementL1Octets not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTransCongThresholdAbatementL1Octets: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m2paTransCongThresholdAbatementL1Octets for you to use, and you have just been asked to do something with it.  Note that 
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->m2paTransCongThresholdAbatementL1Octets;
		StorageTmp->m2paTransCongThresholdAbatementL1Octets = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paTransCongThresholdAbatementL1Octets = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paTransCongThresholdOnsetL1Messages(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2paTransCongThresholdOnsetL1Messages(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static ulong old_value;
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	ulong set_value = *((ulong *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paTransCongThresholdOnsetL1Messages entering action=%d...  \n", action));
	StorageTmp = header_complex(m2paProtocolProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2paProtocolProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTransCongThresholdOnsetL1Messages: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTransCongThresholdOnsetL1Messages not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTransCongThresholdOnsetL1Messages: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m2paTransCongThresholdOnsetL1Messages for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->m2paTransCongThresholdOnsetL1Messages;
		StorageTmp->m2paTransCongThresholdOnsetL1Messages = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paTransCongThresholdOnsetL1Messages = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paTransCongThresholdOnsetL1Octets(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2paTransCongThresholdOnsetL1Octets(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static ulong old_value;
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	ulong set_value = *((ulong *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paTransCongThresholdOnsetL1Octets entering action=%d...  \n", action));
	StorageTmp = header_complex(m2paProtocolProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2paProtocolProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTransCongThresholdOnsetL1Octets: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTransCongThresholdOnsetL1Octets not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTransCongThresholdOnsetL1Octets: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m2paTransCongThresholdOnsetL1Octets for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->m2paTransCongThresholdOnsetL1Octets;
		StorageTmp->m2paTransCongThresholdOnsetL1Octets = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paTransCongThresholdOnsetL1Octets = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paProvingAttempts(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2paProvingAttempts(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static ulong old_value;
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	ulong set_value = *((ulong *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paProvingAttempts entering action=%d...  \n", action));
	StorageTmp = header_complex(m2paProtocolProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2paProtocolProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paProvingAttempts: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paProvingAttempts not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paProvingAttempts: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m2paProvingAttempts for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->m2paProvingAttempts;
		StorageTmp->m2paProvingAttempts = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paProvingAttempts = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paNumberOfThresholdLevels(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2paNumberOfThresholdLevels(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static ulong old_value;
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	ulong set_value = *((ulong *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paNumberOfThresholdLevels entering action=%d...  \n", action));
	StorageTmp = header_complex(m2paProtocolProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2paProtocolProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paNumberOfThresholdLevels: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paNumberOfThresholdLevels not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paNumberOfThresholdLevels: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 2..3 */
		if ((2 > set_value || set_value > 3)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paNumberOfThresholdLevels: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m2paNumberOfThresholdLevels for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->m2paNumberOfThresholdLevels;
		StorageTmp->m2paNumberOfThresholdLevels = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paNumberOfThresholdLevels = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paCongestionCounting(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2paCongestionCounting(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paCongestionCounting entering action=%d...  \n", action));
	StorageTmp = header_complex(m2paProtocolProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2paProtocolProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paCongestionCounting: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paCongestionCounting not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paCongestionCounting: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case M2PACONGESTIONCOUNTING_OCTET:
		case M2PACONGESTIONCOUNTING_MESSAGE:
		case M2PACONGESTIONCOUNTING_BOTH:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paCongestionCounting: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m2paCongestionCounting for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->m2paCongestionCounting;
		StorageTmp->m2paCongestionCounting = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paCongestionCounting = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paCongestionReportingBaseObject(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2paCongestionReportingBaseObject(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paCongestionReportingBaseObject entering action=%d...  \n", action));
	StorageTmp = header_complex(m2paProtocolProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2paProtocolProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paCongestionReportingBaseObject: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paCongestionReportingBaseObject not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paCongestionReportingBaseObject: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case M2PACONGESTIONREPORTINGBASEOBJECT_ROUTESET:
		case M2PACONGESTIONREPORTINGBASEOBJECT_LINKSETOFCONGESTEDROUTESET:
		case M2PACONGESTIONREPORTINGBASEOBJECT_LINKOFCONGESTEDROUTESET:
		case M2PACONGESTIONREPORTINGBASEOBJECT_CONGESTEDLINKOFCONGESTEDROUTE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paCongestionReportingBaseObject: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m2paCongestionReportingBaseObject for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->m2paCongestionReportingBaseObject;
		StorageTmp->m2paCongestionReportingBaseObject = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paCongestionReportingBaseObject = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paTimerTx(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2paTimerTx(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paTimerTx entering action=%d...  \n", action));
	StorageTmp = header_complex(m2paProtocolProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2paProtocolProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTimerTx: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTimerTx not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTimerTx: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTimerTx: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m2paTimerTx for you to use, and you have just been asked to do something with it.  Note that anything done here must be
				   reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->m2paTimerTx;
		StorageTmp->m2paTimerTx = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paTimerTx = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paTimerTy(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2paTimerTy(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paTimerTy entering action=%d...  \n", action));
	StorageTmp = header_complex(m2paProtocolProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2paProtocolProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTimerTy: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTimerTy not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTimerTy: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTimerTy: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m2paTimerTy for you to use, and you have just been asked to do something with it.  Note that anything done here must be
				   reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->m2paTimerTy;
		StorageTmp->m2paTimerTy = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paTimerTy = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paNumberOfCongestionStates(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2paNumberOfCongestionStates(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static ulong old_value;
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	ulong set_value = *((ulong *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paNumberOfCongestionStates entering action=%d...  \n", action));
	StorageTmp = header_complex(m2paProtocolProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2paProtocolProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paNumberOfCongestionStates: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paNumberOfCongestionStates not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paNumberOfCongestionStates: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m2paNumberOfCongestionStates for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->m2paNumberOfCongestionStates;
		StorageTmp->m2paNumberOfCongestionStates = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paNumberOfCongestionStates = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paInitialLevelOfCongestion(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2paInitialLevelOfCongestion(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static ulong old_value;
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	ulong set_value = *((ulong *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paInitialLevelOfCongestion entering action=%d...  \n", action));
	StorageTmp = header_complex(m2paProtocolProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2paProtocolProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paInitialLevelOfCongestion: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paInitialLevelOfCongestion not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paInitialLevelOfCongestion: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m2paInitialLevelOfCongestion for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->m2paInitialLevelOfCongestion;
		StorageTmp->m2paInitialLevelOfCongestion = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paInitialLevelOfCongestion = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paReceiveCongestionThresholdAbatement(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2paReceiveCongestionThresholdAbatement(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static ulong old_value;
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	ulong set_value = *((ulong *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paReceiveCongestionThresholdAbatement entering action=%d...  \n", action));
	StorageTmp = header_complex(m2paProtocolProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2paProtocolProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paReceiveCongestionThresholdAbatement: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paReceiveCongestionThresholdAbatement not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paReceiveCongestionThresholdAbatement: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 3 */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m2paReceiveCongestionThresholdAbatement for you to use, and you have just been asked to do something with it.  Note that 
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->m2paReceiveCongestionThresholdAbatement;
		StorageTmp->m2paReceiveCongestionThresholdAbatement = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paReceiveCongestionThresholdAbatement = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paReceiveCongestionThresholdOnset(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2paReceiveCongestionThresholdOnset(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static ulong old_value;
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	ulong set_value = *((ulong *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paReceiveCongestionThresholdOnset entering action=%d...  \n", action));
	StorageTmp = header_complex(m2paProtocolProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2paProtocolProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paReceiveCongestionThresholdOnset: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paReceiveCongestionThresholdOnset not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paReceiveCongestionThresholdOnset: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 6 */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m2paReceiveCongestionThresholdOnset for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->m2paReceiveCongestionThresholdOnset;
		StorageTmp->m2paReceiveCongestionThresholdOnset = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paReceiveCongestionThresholdOnset = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paReceiveCongestionThresholdDiscard(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2paReceiveCongestionThresholdDiscard(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static ulong old_value;
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	ulong set_value = *((ulong *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paReceiveCongestionThresholdDiscard entering action=%d...  \n", action));
	StorageTmp = header_complex(m2paProtocolProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2paProtocolProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paReceiveCongestionThresholdDiscard: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paReceiveCongestionThresholdDiscard not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paReceiveCongestionThresholdDiscard: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 9 */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m2paReceiveCongestionThresholdDiscard for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->m2paReceiveCongestionThresholdDiscard;
		StorageTmp->m2paReceiveCongestionThresholdDiscard = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paReceiveCongestionThresholdDiscard = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paProtocolProfileName(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2paProtocolProfileName(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static uint8_t *old_value;
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	static size_t old_length = 0;
	static uint8_t *string = NULL;

	DEBUGMSGTL(("m2paMIB", "write_m2paProtocolProfileName entering action=%d...  \n", action));
	StorageTmp = header_complex(m2paProtocolProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		string = NULL;
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2paProtocolProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paProtocolProfileName: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paProtocolProfileName not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 0..255 */
		if (var_val_len > SPRINT_MAX_LEN || ((0 > var_val_len || var_val_len > 255))) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paProtocolProfileName: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m2paProtocolProfileName for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->m2paProtocolProfileName;
		old_length = StorageTmp->m2paProtocolProfileNameLen;
		StorageTmp->m2paProtocolProfileName = string;
		StorageTmp->m2paProtocolProfileNameLen = var_val_len;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(old_value);
		old_length = 0;
		string = NULL;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paProtocolProfileName = old_value;
		StorageTmp->m2paProtocolProfileNameLen = old_length;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		SNMP_FREE(string);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paTransCongThresholdAbatementL2Messages(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2paTransCongThresholdAbatementL2Messages(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static ulong old_value;
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	ulong set_value = *((ulong *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paTransCongThresholdAbatementL2Messages entering action=%d...  \n", action));
	StorageTmp = header_complex(m2paProtocolProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2paProtocolProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTransCongThresholdAbatementL2Messages: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTransCongThresholdAbatementL2Messages not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTransCongThresholdAbatementL2Messages: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m2paTransCongThresholdAbatementL2Messages for you to use, and you have just been asked to do something with it.  Note
				   that anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->m2paTransCongThresholdAbatementL2Messages;
		StorageTmp->m2paTransCongThresholdAbatementL2Messages = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paTransCongThresholdAbatementL2Messages = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paTransCongThresholdAbatementL2Octets(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2paTransCongThresholdAbatementL2Octets(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static ulong old_value;
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	ulong set_value = *((ulong *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paTransCongThresholdAbatementL2Octets entering action=%d...  \n", action));
	StorageTmp = header_complex(m2paProtocolProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2paProtocolProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTransCongThresholdAbatementL2Octets: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTransCongThresholdAbatementL2Octets not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTransCongThresholdAbatementL2Octets: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m2paTransCongThresholdAbatementL2Octets for you to use, and you have just been asked to do something with it.  Note that 
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->m2paTransCongThresholdAbatementL2Octets;
		StorageTmp->m2paTransCongThresholdAbatementL2Octets = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paTransCongThresholdAbatementL2Octets = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paTransCongThresholdOnsetL2Messages(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2paTransCongThresholdOnsetL2Messages(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static ulong old_value;
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	ulong set_value = *((ulong *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paTransCongThresholdOnsetL2Messages entering action=%d...  \n", action));
	StorageTmp = header_complex(m2paProtocolProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2paProtocolProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTransCongThresholdOnsetL2Messages: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTransCongThresholdOnsetL2Messages not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTransCongThresholdOnsetL2Messages: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m2paTransCongThresholdOnsetL2Messages for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->m2paTransCongThresholdOnsetL2Messages;
		StorageTmp->m2paTransCongThresholdOnsetL2Messages = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paTransCongThresholdOnsetL2Messages = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paTransCongThresholdOnsetL2Octets(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2paTransCongThresholdOnsetL2Octets(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static ulong old_value;
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	ulong set_value = *((ulong *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paTransCongThresholdOnsetL2Octets entering action=%d...  \n", action));
	StorageTmp = header_complex(m2paProtocolProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2paProtocolProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTransCongThresholdOnsetL2Octets: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTransCongThresholdOnsetL2Octets not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTransCongThresholdOnsetL2Octets: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m2paTransCongThresholdOnsetL2Octets for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->m2paTransCongThresholdOnsetL2Octets;
		StorageTmp->m2paTransCongThresholdOnsetL2Octets = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paTransCongThresholdOnsetL2Octets = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paTransCongThresholdAbatementL3Messages(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2paTransCongThresholdAbatementL3Messages(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static ulong old_value;
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	ulong set_value = *((ulong *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paTransCongThresholdAbatementL3Messages entering action=%d...  \n", action));
	StorageTmp = header_complex(m2paProtocolProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2paProtocolProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTransCongThresholdAbatementL3Messages: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTransCongThresholdAbatementL3Messages not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTransCongThresholdAbatementL3Messages: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m2paTransCongThresholdAbatementL3Messages for you to use, and you have just been asked to do something with it.  Note
				   that anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->m2paTransCongThresholdAbatementL3Messages;
		StorageTmp->m2paTransCongThresholdAbatementL3Messages = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paTransCongThresholdAbatementL3Messages = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paTransCongThresholdAbatementL3Octets(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2paTransCongThresholdAbatementL3Octets(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static ulong old_value;
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	ulong set_value = *((ulong *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paTransCongThresholdAbatementL3Octets entering action=%d...  \n", action));
	StorageTmp = header_complex(m2paProtocolProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2paProtocolProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTransCongThresholdAbatementL3Octets: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTransCongThresholdAbatementL3Octets not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTransCongThresholdAbatementL3Octets: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m2paTransCongThresholdAbatementL3Octets for you to use, and you have just been asked to do something with it.  Note that 
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->m2paTransCongThresholdAbatementL3Octets;
		StorageTmp->m2paTransCongThresholdAbatementL3Octets = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paTransCongThresholdAbatementL3Octets = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paTransCongThresholdOnsetL3Messages(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2paTransCongThresholdOnsetL3Messages(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static ulong old_value;
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	ulong set_value = *((ulong *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paTransCongThresholdOnsetL3Messages entering action=%d...  \n", action));
	StorageTmp = header_complex(m2paProtocolProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2paProtocolProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTransCongThresholdOnsetL3Messages: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTransCongThresholdOnsetL3Messages not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTransCongThresholdOnsetL3Messages: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m2paTransCongThresholdOnsetL3Messages for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->m2paTransCongThresholdOnsetL3Messages;
		StorageTmp->m2paTransCongThresholdOnsetL3Messages = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paTransCongThresholdOnsetL3Messages = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paTransCongThresholdOnsetL3Octets(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2paTransCongThresholdOnsetL3Octets(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static ulong old_value;
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	ulong set_value = *((ulong *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paTransCongThresholdOnsetL3Octets entering action=%d...  \n", action));
	StorageTmp = header_complex(m2paProtocolProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2paProtocolProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTransCongThresholdOnsetL3Octets: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTransCongThresholdOnsetL3Octets not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTransCongThresholdOnsetL3Octets: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m2paTransCongThresholdOnsetL3Octets for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->m2paTransCongThresholdOnsetL3Octets;
		StorageTmp->m2paTransCongThresholdOnsetL3Octets = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paTransCongThresholdOnsetL3Octets = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paTransCongThresholdDiscardL1Messages(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2paTransCongThresholdDiscardL1Messages(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static ulong old_value;
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	ulong set_value = *((ulong *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paTransCongThresholdDiscardL1Messages entering action=%d...  \n", action));
	StorageTmp = header_complex(m2paProtocolProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2paProtocolProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTransCongThresholdDiscardL1Messages: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTransCongThresholdDiscardL1Messages not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTransCongThresholdDiscardL1Messages: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m2paTransCongThresholdDiscardL1Messages for you to use, and you have just been asked to do something with it.  Note that 
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->m2paTransCongThresholdDiscardL1Messages;
		StorageTmp->m2paTransCongThresholdDiscardL1Messages = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paTransCongThresholdDiscardL1Messages = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paTransCongThresholdDiscardL1Octets(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2paTransCongThresholdDiscardL1Octets(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static ulong old_value;
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	ulong set_value = *((ulong *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paTransCongThresholdDiscardL1Octets entering action=%d...  \n", action));
	StorageTmp = header_complex(m2paProtocolProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2paProtocolProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTransCongThresholdDiscardL1Octets: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTransCongThresholdDiscardL1Octets not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTransCongThresholdDiscardL1Octets: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m2paTransCongThresholdDiscardL1Octets for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->m2paTransCongThresholdDiscardL1Octets;
		StorageTmp->m2paTransCongThresholdDiscardL1Octets = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paTransCongThresholdDiscardL1Octets = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paTransCongThresholdDiscardL2Messages(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2paTransCongThresholdDiscardL2Messages(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static ulong old_value;
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	ulong set_value = *((ulong *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paTransCongThresholdDiscardL2Messages entering action=%d...  \n", action));
	StorageTmp = header_complex(m2paProtocolProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2paProtocolProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTransCongThresholdDiscardL2Messages: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTransCongThresholdDiscardL2Messages not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTransCongThresholdDiscardL2Messages: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m2paTransCongThresholdDiscardL2Messages for you to use, and you have just been asked to do something with it.  Note that 
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->m2paTransCongThresholdDiscardL2Messages;
		StorageTmp->m2paTransCongThresholdDiscardL2Messages = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paTransCongThresholdDiscardL2Messages = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paTransCongThresholdDiscardL2Octets(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2paTransCongThresholdDiscardL2Octets(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static ulong old_value;
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	ulong set_value = *((ulong *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paTransCongThresholdDiscardL2Octets entering action=%d...  \n", action));
	StorageTmp = header_complex(m2paProtocolProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2paProtocolProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTransCongThresholdDiscardL2Octets: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTransCongThresholdDiscardL2Octets not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTransCongThresholdDiscardL2Octets: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m2paTransCongThresholdDiscardL2Octets for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->m2paTransCongThresholdDiscardL2Octets;
		StorageTmp->m2paTransCongThresholdDiscardL2Octets = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paTransCongThresholdDiscardL2Octets = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paTransCongThresholdDiscardL3Messages(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2paTransCongThresholdDiscardL3Messages(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static ulong old_value;
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	ulong set_value = *((ulong *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paTransCongThresholdDiscardL3Messages entering action=%d...  \n", action));
	StorageTmp = header_complex(m2paProtocolProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2paProtocolProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTransCongThresholdDiscardL3Messages: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTransCongThresholdDiscardL3Messages not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTransCongThresholdDiscardL3Messages: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m2paTransCongThresholdDiscardL3Messages for you to use, and you have just been asked to do something with it.  Note that 
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->m2paTransCongThresholdDiscardL3Messages;
		StorageTmp->m2paTransCongThresholdDiscardL3Messages = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paTransCongThresholdDiscardL3Messages = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paTransCongThresholdDiscardL3Octets(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2paTransCongThresholdDiscardL3Octets(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static ulong old_value;
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	ulong set_value = *((ulong *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paTransCongThresholdDiscardL3Octets entering action=%d...  \n", action));
	StorageTmp = header_complex(m2paProtocolProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2paProtocolProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTransCongThresholdDiscardL3Octets: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTransCongThresholdDiscardL3Octets not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paTransCongThresholdDiscardL3Octets: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m2paTransCongThresholdDiscardL3Octets for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->m2paTransCongThresholdDiscardL3Octets;
		StorageTmp->m2paTransCongThresholdDiscardL3Octets = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paTransCongThresholdDiscardL3Octets = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paDefaultSctpNoDelay(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_m2paDefaultSctpNoDelay(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m2paMIB_data *StorageTmp = NULL;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paDefaultSctpNoDelay entering action=%d...  \n", action));
	if ((StorageTmp = m2paMIBStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paDefaultSctpNoDelay not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paDefaultSctpNoDelay: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 1 */
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paDefaultSctpNoDelay: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in set_value for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in
				   the UNDO case */
		old_value = StorageTmp->m2paDefaultSctpNoDelay;
		StorageTmp->m2paDefaultSctpNoDelay = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paDefaultSctpNoDelay = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paDefaultSctpMaxseg(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_m2paDefaultSctpMaxseg(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static ulong old_value;
	struct m2paMIB_data *StorageTmp = NULL;
	ulong set_value = *((ulong *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paDefaultSctpMaxseg entering action=%d...  \n", action));
	if ((StorageTmp = m2paMIBStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paDefaultSctpMaxseg not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paDefaultSctpMaxseg: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 65536 */
		/* Note: ranges 1..65536 */
		if ((1 > set_value || set_value > 65536)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paDefaultSctpMaxseg: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in set_value for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in
				   the UNDO case */
		old_value = StorageTmp->m2paDefaultSctpMaxseg;
		StorageTmp->m2paDefaultSctpMaxseg = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paDefaultSctpMaxseg = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paDefaultSctpHeartbeatItvl(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_m2paDefaultSctpHeartbeatItvl(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m2paMIB_data *StorageTmp = NULL;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paDefaultSctpHeartbeatItvl entering action=%d...  \n", action));
	if ((StorageTmp = m2paMIBStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paDefaultSctpHeartbeatItvl not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paDefaultSctpHeartbeatItvl: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 6000 */
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paDefaultSctpHeartbeatItvl: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in set_value for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in
				   the UNDO case */
		old_value = StorageTmp->m2paDefaultSctpHeartbeatItvl;
		StorageTmp->m2paDefaultSctpHeartbeatItvl = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paDefaultSctpHeartbeatItvl = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paDefaultSctpHeartbeat(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_m2paDefaultSctpHeartbeat(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m2paMIB_data *StorageTmp = NULL;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paDefaultSctpHeartbeat entering action=%d...  \n", action));
	if ((StorageTmp = m2paMIBStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paDefaultSctpHeartbeat not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paDefaultSctpHeartbeat: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paDefaultSctpHeartbeat: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in set_value for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in
				   the UNDO case */
		old_value = StorageTmp->m2paDefaultSctpHeartbeat;
		StorageTmp->m2paDefaultSctpHeartbeat = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paDefaultSctpHeartbeat = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paDefaultSctpRtoInitial(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_m2paDefaultSctpRtoInitial(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m2paMIB_data *StorageTmp = NULL;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paDefaultSctpRtoInitial entering action=%d...  \n", action));
	if ((StorageTmp = m2paMIBStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paDefaultSctpRtoInitial not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paDefaultSctpRtoInitial: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 300 */
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paDefaultSctpRtoInitial: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in set_value for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in
				   the UNDO case */
		old_value = StorageTmp->m2paDefaultSctpRtoInitial;
		StorageTmp->m2paDefaultSctpRtoInitial = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paDefaultSctpRtoInitial = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paDefaultSctpRtoMin(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_m2paDefaultSctpRtoMin(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m2paMIB_data *StorageTmp = NULL;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paDefaultSctpRtoMin entering action=%d...  \n", action));
	if ((StorageTmp = m2paMIBStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paDefaultSctpRtoMin not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paDefaultSctpRtoMin: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 100 */
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paDefaultSctpRtoMin: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in set_value for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in
				   the UNDO case */
		old_value = StorageTmp->m2paDefaultSctpRtoMin;
		StorageTmp->m2paDefaultSctpRtoMin = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paDefaultSctpRtoMin = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paDefaultSctpRtoMax(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_m2paDefaultSctpRtoMax(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m2paMIB_data *StorageTmp = NULL;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paDefaultSctpRtoMax entering action=%d...  \n", action));
	if ((StorageTmp = m2paMIBStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paDefaultSctpRtoMax not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paDefaultSctpRtoMax: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 6000 */
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paDefaultSctpRtoMax: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in set_value for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in
				   the UNDO case */
		old_value = StorageTmp->m2paDefaultSctpRtoMax;
		StorageTmp->m2paDefaultSctpRtoMax = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paDefaultSctpRtoMax = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paDefaultSctpPathMaxRetrans(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_m2paDefaultSctpPathMaxRetrans(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static ulong old_value;
	struct m2paMIB_data *StorageTmp = NULL;
	ulong set_value = *((ulong *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paDefaultSctpPathMaxRetrans entering action=%d...  \n", action));
	if ((StorageTmp = m2paMIBStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paDefaultSctpPathMaxRetrans not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paDefaultSctpPathMaxRetrans: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 5 */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in set_value for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in
				   the UNDO case */
		old_value = StorageTmp->m2paDefaultSctpPathMaxRetrans;
		StorageTmp->m2paDefaultSctpPathMaxRetrans = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paDefaultSctpPathMaxRetrans = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paDefaultSctpCookieLife(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_m2paDefaultSctpCookieLife(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m2paMIB_data *StorageTmp = NULL;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paDefaultSctpCookieLife entering action=%d...  \n", action));
	if ((StorageTmp = m2paMIBStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paDefaultSctpCookieLife not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paDefaultSctpCookieLife: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 6000 */
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paDefaultSctpCookieLife: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in set_value for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in
				   the UNDO case */
		old_value = StorageTmp->m2paDefaultSctpCookieLife;
		StorageTmp->m2paDefaultSctpCookieLife = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paDefaultSctpCookieLife = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paDefaultSctpCookieInc(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_m2paDefaultSctpCookieInc(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m2paMIB_data *StorageTmp = NULL;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paDefaultSctpCookieInc entering action=%d...  \n", action));
	if ((StorageTmp = m2paMIBStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paDefaultSctpCookieInc not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paDefaultSctpCookieInc: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 100 */
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paDefaultSctpCookieInc: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in set_value for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in
				   the UNDO case */
		old_value = StorageTmp->m2paDefaultSctpCookieInc;
		StorageTmp->m2paDefaultSctpCookieInc = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paDefaultSctpCookieInc = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paDefaultSctpMaxInitRetries(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_m2paDefaultSctpMaxInitRetries(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static ulong old_value;
	struct m2paMIB_data *StorageTmp = NULL;
	ulong set_value = *((ulong *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paDefaultSctpMaxInitRetries entering action=%d...  \n", action));
	if ((StorageTmp = m2paMIBStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paDefaultSctpMaxInitRetries not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paDefaultSctpMaxInitRetries: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 8 */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in set_value for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in
				   the UNDO case */
		old_value = StorageTmp->m2paDefaultSctpMaxInitRetries;
		StorageTmp->m2paDefaultSctpMaxInitRetries = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paDefaultSctpMaxInitRetries = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paDefaultSctpMaxBurst(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_m2paDefaultSctpMaxBurst(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static ulong old_value;
	struct m2paMIB_data *StorageTmp = NULL;
	ulong set_value = *((ulong *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paDefaultSctpMaxBurst entering action=%d...  \n", action));
	if ((StorageTmp = m2paMIBStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paDefaultSctpMaxBurst not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paDefaultSctpMaxBurst: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 4 */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in set_value for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in
				   the UNDO case */
		old_value = StorageTmp->m2paDefaultSctpMaxBurst;
		StorageTmp->m2paDefaultSctpMaxBurst = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paDefaultSctpMaxBurst = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paDefaultSctpAssocMaxRetrans(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_m2paDefaultSctpAssocMaxRetrans(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static ulong old_value;
	struct m2paMIB_data *StorageTmp = NULL;
	ulong set_value = *((ulong *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paDefaultSctpAssocMaxRetrans entering action=%d...  \n", action));
	if ((StorageTmp = m2paMIBStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paDefaultSctpAssocMaxRetrans not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paDefaultSctpAssocMaxRetrans: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 10 */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in set_value for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in
				   the UNDO case */
		old_value = StorageTmp->m2paDefaultSctpAssocMaxRetrans;
		StorageTmp->m2paDefaultSctpAssocMaxRetrans = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paDefaultSctpAssocMaxRetrans = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paDefaultSctpSackDelay(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_m2paDefaultSctpSackDelay(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m2paMIB_data *StorageTmp = NULL;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paDefaultSctpSackDelay entering action=%d...  \n", action));
	if ((StorageTmp = m2paMIBStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paDefaultSctpSackDelay not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paDefaultSctpSackDelay: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 20 */
		/* Note: ranges 0..50 */
		if ((0 > set_value || set_value > 50)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paDefaultSctpSackDelay: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in set_value for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in
				   the UNDO case */
		old_value = StorageTmp->m2paDefaultSctpSackDelay;
		StorageTmp->m2paDefaultSctpSackDelay = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paDefaultSctpSackDelay = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2paDefaultSctpLifetime(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_m2paDefaultSctpLifetime(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m2paMIB_data *StorageTmp = NULL;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m2paMIB", "write_m2paDefaultSctpLifetime entering action=%d...  \n", action));
	if ((StorageTmp = m2paMIBStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paDefaultSctpLifetime not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paDefaultSctpLifetime: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 0 */
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paDefaultSctpLifetime: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in set_value for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in
				   the UNDO case */
		old_value = StorageTmp->m2paDefaultSctpLifetime;
		StorageTmp->m2paDefaultSctpLifetime = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2paDefaultSctpLifetime = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int m2paProtocolProfileTable_consistent(struct m2paProtocolProfileTable_data *thedata)
 * @param thedata the row data to check for consistency.
 * @brief check the internal consistency of a table row.
 *
 * This function checks the internal consistency of a table row for the m2paProtocolProfileTable table.  If the
 * table row is internally consistent, then this function returns SNMP_ERR_NOERROR, otherwise the
 * function returns an SNMP error code and it will not be possible to activate the row until the
 * row's internal consistency is corrected.  This function might use a 'test' operation against the
 * driver to ensure that the commit phase will succeed.
 */
int
m2paProtocolProfileTable_consistent(struct m2paProtocolProfileTable_data *thedata)
{
	/* XXX: check row consistency return SNMP_ERR_NOERROR if consistent, or an SNMP error code if not. */
	return (SNMP_ERR_NOERROR);
}

/**
 * @fn int write_m2paProtocolProfileRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_m2paProtocolProfileRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct m2paProtocolProfileTable_data *StorageTmp = NULL;
	static struct m2paProtocolProfileTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 15;
	static int old_value;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("m2paMIB", "write_m2paProtocolProfileRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(m2paProtocolProfileTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paProtocolProfileRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2paProtocolProfileRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->m2paProtocolProfileRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->m2paProtocolProfileTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_DESTROY:
			/* destroying existent or non-existent row is ok */
			if (StorageTmp == NULL)
				break;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->m2paProtocolProfileTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			vars = NULL;
			/* m2paProtocolProfileId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[15]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* m2paProtocolProfileId */
			/* Note: ranges 1..32 */
			if (vp->val_len > SPRINT_MAX_LEN || ((1 > vp->val_len || vp->val_len > 32))) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index m2paProtocolProfileId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = m2paProtocolProfileTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			vp = vars;
			memdup((void *) &StorageNew->m2paProtocolProfileId, vp->val.string, vp->val_len);
			StorageNew->m2paProtocolProfileIdLen = vp->val_len;
			vp = vp->next_variable;
			header_complex_add_data(&m2paProtocolProfileTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_DESTROY:
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				m2paProtocolProfileTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		}
		break;
	case ACTION:
		/* The variable has been stored in set_value for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = m2paProtocolProfileTable_consistent(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			/* row does not have to be consistent */
			break;
		case RS_ACTIVE:
			old_value = StorageTmp->m2paProtocolProfileRowStatus;
			StorageTmp->m2paProtocolProfileRowStatus = set_value;
			if (old_value != RS_ACTIVE) {
				/* check that activation is possible */
				if ((ret = m2paProtocolProfileTable_consistent(StorageTmp)) != SNMP_ERR_NOERROR) {
					StorageTmp->m2paProtocolProfileRowStatus = old_value;
					return (ret);
				}
			}
			break;
		case RS_NOTINSERVICE:
			/* set the flag? */
			old_value = StorageTmp->m2paProtocolProfileRowStatus;
			StorageTmp->m2paProtocolProfileRowStatus = set_value;
			break;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* row creation, set final state */
			/* XXX: commit creation to underlying device */
			/* XXX: activate with underlying device */
			StorageNew->m2paProtocolProfileRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			/* row creation, set final state */
			StorageNew->m2paProtocolProfileRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (old_value != set_value) {
				switch (set_value) {
				case RS_ACTIVE:
					/* XXX: activate with underlying device */
					break;
				case RS_NOTINSERVICE:
					/* XXX: deactivate with underlying device */
					break;
				}
			}
			break;
		case RS_DESTROY:
			/* row deletion, free it its dead */
			m2paProtocolProfileTable_destroy(&StorageDel);
			/* m2paProtocolProfileTable_destroy() can handle NULL pointers. */
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* restore state */
			StorageTmp->m2paProtocolProfileRowStatus = old_value;
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				m2paProtocolProfileTable_del(StorageNew);
				m2paProtocolProfileTable_destroy(&StorageNew);
			}
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				m2paProtocolProfileTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn void m2paMIB_loop_handler(int dummy)
 * @param sig signal number
 * @brief handle event loop interation.
 *
 * This function is registered so that, when operating as a module, snmpd will call it one per event
 * loop interation.  This function is called before the next requst is processed and after the
 * previous request is processed.  Two things are done here:  1) The file descriptor that is used to
 * synchronize the agent with (pseudo-)device drivers is closed.  (Another approach, instead of
 * closing each time, would be to restart a timer each time that a request is made (loop is
 * performed) and if it expires, close the file descriptor).  2) The request number is incremented.
 * Although a request is not generated for each loop of the snmp event loop, it is true that a new
 * request cannot be generated without performing a loop.  Therefore, the sa_request is not the
 * request number but it is a temporally unique identifier for a request.
 */
void
m2paMIB_loop_handler(int sig)
{
	DEBUGMSGTL(("m2paMIB", "m2paMIB_loop_handler: executing loop handler...  "));
	/* close files after each request */
	if (sa_fclose) {
		if (my_fd >= 0) {
			close(my_fd);
			my_fd = -1;
		}
	}
#if defined MASTER
	/* prepare for next request */
	sa_request++;
#endif				/* defined MASTER */
	if (external_signal_scheduled[sig] == 0)
		external_signal_scheduled[sig]--;
	if (m2paMIBold_signal_handler != NULL)
		(*m2paMIBold_signal_handler) (sig);
	DEBUGMSGTL(("m2paMIB", "done.\n"));
}

/**
 * @fn void m2paMIB_fd_handler(int fd, void *dummy)
 * @param fd file descriptor to read.
 * @param dummy client data passed to registration function (always NULL).
 * @brief handle read event on file descriptor.
 *
 * This read file descriptor handler is normally used for (pseudo-)device drivers that generate
 * statistical collection interval events, alarm events, or other operational measurement events, by
 * placing a message on the read queue of the "event handling" Stream.  Normally this routine
 * would adjust counts in some table or scalars, generate SNMP traps representing on-occurence
 * events, first and interval events, and alarm indications.
 */
void
m2paMIB_fd_handler(int fd, void *dummy)
{
	DEBUGMSGTL(("m2paMIB", "m2paMIB_fd_handler: executing fd handler...  "));
	/* XXX: place actions to handle my_fd here... */
	DEBUGMSGTL(("m2paMIB", "done.\n"));
	return;
}
