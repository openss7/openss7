/*****************************************************************************

 @(#) src/snmp/tcapOmMIB.c

 -----------------------------------------------------------------------------

 Copyright (c) 2008-2019  Monavacon Limited <http://www.monavacon.com/>
 Copyright (c) 2001-2008  OpenSS7 Corporation <http://www.openss7.com/>
 Copyright (c) 1997-2001  Brian F. G. Bidulock <bidulock@openss7.org>

 All Rights Reserved.

 This program is free software: you can redistribute it and/or modify it under
 the terms of the GNU Affero General Public License as published by the Free
 Software Foundation, version 3 of the license.

 This program is distributed in the hope that it will be useful, but WITHOUT
 ANY WARRANTY; without even the implied warranty of MERCHANTABILITY or FITNESS
 FOR A PARTICULAR PURPOSE.  See the GNU Affero General Public License for more
 details.

 You should have received a copy of the GNU Affero General Public License
 along with this program.  If not, see <http://www.gnu.org/licenses/>, or
 write to the Free Software Foundation, Inc., 675 Mass Ave, Cambridge, MA
 02139, USA.

 -----------------------------------------------------------------------------

 U.S. GOVERNMENT RESTRICTED RIGHTS.  If you are licensing this Software on
 behalf of the U.S. Government ("Government"), the following provisions apply
 to you.  If the Software is supplied by the Department of Defense ("DoD"), it
 is classified as "Commercial Computer Software" under paragraph 252.227-7014
 of the DoD Supplement to the Federal Acquisition Regulations ("DFARS") (or any
 successor regulations) and the Government is acquiring only the license rights
 granted herein (the license rights customarily provided to non-Government
 users).  If the Software is supplied to any unit or agency of the Government
 other than DoD, it is classified as "Restricted Computer Software" and the
 Government's rights in the Software are defined in paragraph 52.227-19 of the
 Federal Acquisition Regulations ("FAR") (or any successor regulations) or, in
 the cases of NASA, in paragraph 18.52.227-86 of the NASA Supplement to the FAR
 (or any successor regulations).

 -----------------------------------------------------------------------------

 Commercial licensing and support of this software is available from OpenSS7
 Corporation at a fee.  See http://www.openss7.com/

 *****************************************************************************/

static char const ident[] __attribute__((unused)) = "src/snmp/tcapOmMIB.c (" PACKAGE_ENVR ") " PACKAGE_DATE;

/* This file was generated by mib2c and is intended for use as
   a mib module for the ucd-snmp snmpd agent. */
#include <ucd-snmp/ucd-snmp-config.h>
#include <ucd-snmp/ucd-snmp-includes.h>
#include <ucd-snmp/ucd-snmp-agent-includes.h>
#include <ucd-snmp/agent_trap.h>
#include <ucd-snmp/callback.h>
#include <ucd-snmp/snmp-tc.h>
#include <ucd-snmp/default_store.h>
#include <ucd-snmp/snmp_alarm.h>
/* The following header files are mangled in most recent net-snmp releases so
 * the versions from UCD-SNMP 4.2.5 are included here.  */
#if defined HAVE_LIBNETSNMP
#else				/* defined HAVE_LIBNETSNMP */
#endif				/* defined HAVE_LIBNETSNMP */
/* These are messed up on both. */
#include "ds_agent.h"
#ifdef HAVE_UCD_SNMP_UTIL_FUNCS_H
#include <ucd-snmp/util_funcs.h>
/* Many recent net-snmp UCD compatible headers do not declare header_generic. */
int header_generic(struct variable *, oid *, size_t *, int, size_t *, WriteMethod **);
#else				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#include "util_funcs.h"
#endif				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#ifdef HAVE_UCD_SNMP_HEADER_COMPLEX_H
#include <ucd-snmp/header_complex.h>
#else				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
#include "header_complex.h"
#endif				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
/* This one is the other way around: it is just fine for net-snmp, but
 * ucd-snmp does not provide the header file at all.  */
#ifdef HAVE_UCD_SNMP_MIB_MODULES_H
#include <ucd-snmp/mib_modules.h>
#else				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#ifdef HAVE_NET_SNMP_AGENT_MIB_MODULES_H
#include <net-snmp/agent/mib_modules.h>
#else				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#include "mib_modules.h"
#endif				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#endif				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#ifdef HAVE_NETSNMP_MEMDUP
#include "ucd_memdup.h"
#endif
#include <stdint.h>
#include <signal.h>
#include <sys/stat.h>		/* for struct stat, fstat() */
#include <sys/types.h>
#include <unistd.h>
#include <stdlib.h>
#include <pwd.h>		/* for getpwuid() getpwnam() */
#include <grp.h>		/* for getgrgid() getgrnam() */
#include <libgen.h>		/* for basename() */
#include <fcntl.h>		/* for O_CREAT */
#include <errno.h>
#include <string.h>
#ifdef _GNU_SOURCE
#include <getopt.h>
#endif
#include "tcapOmMIB.h"
#undef MASTER
#define MY_FACILITY(__pri)	(LOG_DAEMON|(__pri))
#if defined MODULE
#if defined MASTER
const char sa_program[] = "tcapOmMIB";
int sa_fclose = 1;			/* default close files between requests */
int sa_changed = 1;			/* indication to reread MIB configuration */
int sa_stats_refresh = 1;		/* indications that statistics, the mib or its tables need to be refreshed */
int sa_request = 1;			/* request number for per-request actions */
int sa_dump = 0;			/* default packet dump */
int sa_debug = 0;			/* default no debug */
#endif				/* defined MASTER */
#endif				/* defined MODULE */
static int my_fd = -1;			/* file descriptor for this MIB's use */
static int my_readfd = -1;		/* file descriptor for autonomnous events */
volatile int tcapOmMIB_refresh = 1;
volatile int tcapOmNodeTable_refresh = 1;
volatile int tcapOmNodeCurrentTable_refresh = 1;
volatile int tcapOmNode5minIntTable_refresh = 1;
volatile int tcapOmNode15minIntTable_refresh = 1;
volatile int tcapOmMsgsTable_refresh = 1;
volatile int tcapOmMsgsCurrentTable_refresh = 1;
volatile int tcapOmMsgs5minIntTable_refresh = 1;
volatile int tcapOmMsgs15minIntTable_refresh = 1;
volatile int tcapOmAETable_refresh = 1;
volatile int tcapOmAECurrentTable_refresh = 1;
volatile int tcapOmAE5minIntTable_refresh = 1;
volatile int tcapOmAE15minIntTable_refresh = 1;
volatile int tcapOmTcTable_refresh = 1;
volatile int tcapOmTcCurrentTable_refresh = 1;
volatile int tcapOmTc5minIntTable_refresh = 1;
volatile int tcapOmTc15minIntTable_refresh = 1;
volatile int tcapOmDevelRecvTable_refresh = 1;
volatile int tcapOmDevelRecvCurrentTable_refresh = 1;
volatile int tcapOmDevelRecv5minIntTable_refresh = 1;
volatile int tcapOmDevelRecv15minIntTable_refresh = 1;
volatile int tcapOmDevelSentTable_refresh = 1;
volatile int tcapOmDevelSentCurrentTable_refresh = 1;
volatile int tcapOmDevelSent5minIntTable_refresh = 1;
volatile int tcapOmDevelSent15minIntTable_refresh = 1;

/*
 * tcapOmMIB_variables_oid: object identifier for tcapOmMIB
 * This is the top level oid that we want to register under.  This is essentially a prefix, with the
 * suffix appearing in the variable below.
 */
oid tcapOmMIB_variables_oid[10] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 4 };

/*
 * Oids for use in notifications defined in this MIB.
 */

/*
 * Oids accessible only for notify defined in this MIB.
 */
oid tcapOmMessageTypeNotif_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 4, 1, 3, 1 };

/*
 * Other oids defined in this MIB.
 */
oid tcapOmBasicCompliance_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 4, 2, 1, 1 };
oid tcapOmActivationGroup_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 4, 2, 2, 1 };
oid tcapOmNodeGroup_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 4, 2, 2, 2 };
oid tcapOmNodeCurrentGroup_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 4, 2, 2, 3 };
oid tcapOmNode5minGroup_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 4, 2, 2, 4 };
oid tcapOmNode15minGroup_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 4, 2, 2, 5 };
oid tcapOmMsgsGroup_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 4, 2, 2, 6 };
oid tcapOmMsgsCurrentGroup_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 4, 2, 2, 7 };
oid tcapOmMsgs5minGroup_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 4, 2, 2, 8 };
oid tcapOmMsgs15minGroup_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 4, 2, 2, 9 };
oid tcapOmAEGroup_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 4, 2, 2, 10 };
oid tcapOmAECurrentGroup_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 4, 2, 2, 11 };
oid tcapOmAE5minGroup_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 4, 2, 2, 12 };
oid tcapOmAE15minGroup_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 4, 2, 2, 13 };
oid tcapOmTcGroup_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 4, 2, 2, 14 };
oid tcapOmTcCurrentGroup_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 4, 2, 2, 15 };
oid tcapOmTc5minGroup_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 4, 2, 2, 16 };
oid tcapOmTc15minGroup_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 4, 2, 2, 17 };
oid tcapOmDevelRecvGroup_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 4, 2, 2, 18 };
oid tcapOmDevelRecvCurrentGroup_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 4, 2, 2, 19 };
oid tcapOmDevelRecv5minGroup_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 4, 2, 2, 20 };
oid tcapOmDevelRecv15minGroup_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 4, 2, 2, 21 };
oid tcapOmDevelSentGroup_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 4, 2, 2, 22 };
oid tcapOmDevelSentCurrentGroup_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 4, 2, 2, 23 };
oid tcapOmDevelSent5minGroup_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 4, 2, 2, 24 };
oid tcapOmDevelSent15minGroup_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 17, 752, 4, 2, 2, 25 };
static oid zeroDotZero_oid[2] = { 0, 0 };
static oid snmpTrapOID_oid[11] = { 1, 3, 6, 1, 6, 3, 1, 1, 4, 1, 0 };

/*
 * variable7 tcapOmMIB_variables: tree for tcapOmMIB
 * This variable defines function callbacks and type return information for the tcapOmMIB mib section
 */
struct variable7 tcapOmMIB_variables[] = {
	/* magic number, variable type, ro/rw, callback fn, L, oidsuffix */
#define   TCAPOMNODEMESSAGESSENT  1
	{ (u_char) TCAPOMNODEMESSAGESSENT, ASN_COUNTER, RONLY, var_tcapOmNodeTable, 6, { 1, 1, 1, 1, 1, 1} },
#define   TCAPOMNODEMESSAGESRECV  2
	{ (u_char) TCAPOMNODEMESSAGESRECV, ASN_COUNTER, RONLY, var_tcapOmNodeTable, 6, { 1, 1, 1, 1, 1, 2} },
#define   TCAPOMNODECOMPONENTSSENT  3
	{ (u_char) TCAPOMNODECOMPONENTSSENT, ASN_COUNTER, RONLY, var_tcapOmNodeTable, 6, { 1, 1, 1, 1, 1, 3} },
#define   TCAPOMNODECOMPONENTSRECV  4
	{ (u_char) TCAPOMNODECOMPONENTSRECV, ASN_COUNTER, RONLY, var_tcapOmNodeTable, 6, { 1, 1, 1, 1, 1, 4} },
#define   TCAPOMNODE5MINVALIDINTERVALS  5
	{ (u_char) TCAPOMNODE5MINVALIDINTERVALS, ASN_INTEGER, RONLY, var_tcapOmNodeTable, 6, { 1, 1, 1, 1, 1, 5} },
#define   TCAPOMNODE15MINVALIDINTERVALS  6
	{ (u_char) TCAPOMNODE15MINVALIDINTERVALS, ASN_INTEGER, RONLY, var_tcapOmNodeTable, 6, { 1, 1, 1, 1, 1, 6} },
#define   TCAPOMNODECURRENTMESSAGESSENT  7
	{ (u_char) TCAPOMNODECURRENTMESSAGESSENT, ASN_GAUGE, RONLY, var_tcapOmNodeCurrentTable, 6, { 1, 1, 1, 2, 1, 1} },
#define   TCAPOMNODECURRENTMESSAGESRECV  8
	{ (u_char) TCAPOMNODECURRENTMESSAGESRECV, ASN_GAUGE, RONLY, var_tcapOmNodeCurrentTable, 6, { 1, 1, 1, 2, 1, 2} },
#define   TCAPOMNODECURRENTCOMPONENTSSENT  9
	{ (u_char) TCAPOMNODECURRENTCOMPONENTSSENT, ASN_GAUGE, RONLY, var_tcapOmNodeCurrentTable, 6, { 1, 1, 1, 2, 1, 3} },
#define   TCAPOMNODECURRENTCOMPONENTSRECV  10
	{ (u_char) TCAPOMNODECURRENTCOMPONENTSRECV, ASN_GAUGE, RONLY, var_tcapOmNodeCurrentTable, 6, { 1, 1, 1, 2, 1, 4} },
#define   TCAPOMNODE5MININTMESSAGESSENT  11
	{ (u_char) TCAPOMNODE5MININTMESSAGESSENT, ASN_GAUGE, RONLY, var_tcapOmNode5minIntTable, 6, { 1, 1, 1, 3, 1, 2} },
#define   TCAPOMNODE5MININTMESSAGESRECV  12
	{ (u_char) TCAPOMNODE5MININTMESSAGESRECV, ASN_GAUGE, RONLY, var_tcapOmNode5minIntTable, 6, { 1, 1, 1, 3, 1, 3} },
#define   TCAPOMNODE5MININTCOMPONENTSSENT  13
	{ (u_char) TCAPOMNODE5MININTCOMPONENTSSENT, ASN_GAUGE, RONLY, var_tcapOmNode5minIntTable, 6, { 1, 1, 1, 3, 1, 4} },
#define   TCAPOMNODE5MININTCOMPONENTSRECV  14
	{ (u_char) TCAPOMNODE5MININTCOMPONENTSRECV, ASN_GAUGE, RONLY, var_tcapOmNode5minIntTable, 6, { 1, 1, 1, 3, 1, 5} },
#define   TCAPOMNODE15MININTMESSAGESSENT  15
	{ (u_char) TCAPOMNODE15MININTMESSAGESSENT, ASN_GAUGE, RONLY, var_tcapOmNode15minIntTable, 6, { 1, 1, 1, 4, 1, 1} },
#define   TCAPOMNODE15MININTMESSAGESRECV  16
	{ (u_char) TCAPOMNODE15MININTMESSAGESRECV, ASN_GAUGE, RONLY, var_tcapOmNode15minIntTable, 6, { 1, 1, 1, 4, 1, 2} },
#define   TCAPOMNODE15MININTCOMPONENTSSENT  17
	{ (u_char) TCAPOMNODE15MININTCOMPONENTSSENT, ASN_GAUGE, RONLY, var_tcapOmNode15minIntTable, 6, { 1, 1, 1, 4, 1, 3} },
#define   TCAPOMNODE15MININTCOMPONENTSRECV  18
	{ (u_char) TCAPOMNODE15MININTCOMPONENTSRECV, ASN_GAUGE, RONLY, var_tcapOmNode15minIntTable, 6, { 1, 1, 1, 4, 1, 4} },
#define   TCAPOMMSGSMESSAGESSENT  19
	{ (u_char) TCAPOMMSGSMESSAGESSENT, ASN_COUNTER, RONLY, var_tcapOmMsgsTable, 6, { 1, 1, 2, 1, 1, 2} },
#define   TCAPOMMSGSMESSAGESRECV  20
	{ (u_char) TCAPOMMSGSMESSAGESRECV, ASN_COUNTER, RONLY, var_tcapOmMsgsTable, 6, { 1, 1, 2, 1, 1, 3} },
#define   TCAPOMMSGS5MINVALIDINTERVALS  21
	{ (u_char) TCAPOMMSGS5MINVALIDINTERVALS, ASN_INTEGER, RONLY, var_tcapOmMsgsTable, 6, { 1, 1, 2, 1, 1, 4} },
#define   TCAPOMMSGS15MINVALIDINTERVALS  22
	{ (u_char) TCAPOMMSGS15MINVALIDINTERVALS, ASN_INTEGER, RONLY, var_tcapOmMsgsTable, 6, { 1, 1, 2, 1, 1, 5} },
#define   TCAPOMMSGSCURRENTMESSAGESSENT  23
	{ (u_char) TCAPOMMSGSCURRENTMESSAGESSENT, ASN_GAUGE, RONLY, var_tcapOmMsgsCurrentTable, 6, { 1, 1, 2, 2, 1, 1} },
#define   TCAPOMMSGSCURRENTMESSAGESRECV  24
	{ (u_char) TCAPOMMSGSCURRENTMESSAGESRECV, ASN_GAUGE, RONLY, var_tcapOmMsgsCurrentTable, 6, { 1, 1, 2, 2, 1, 2} },
#define   TCAPOMMSGS5MININTMESSAGESSENT  25
	{ (u_char) TCAPOMMSGS5MININTMESSAGESSENT, ASN_GAUGE, RONLY, var_tcapOmMsgs5minIntTable, 6, { 1, 1, 2, 3, 1, 1} },
#define   TCAPOMMSGS5MININTMESSAGESRECV  26
	{ (u_char) TCAPOMMSGS5MININTMESSAGESRECV, ASN_GAUGE, RONLY, var_tcapOmMsgs5minIntTable, 6, { 1, 1, 2, 3, 1, 2} },
#define   TCAPOMMSGS15MININTMESSAGESSENT  27
	{ (u_char) TCAPOMMSGS15MININTMESSAGESSENT, ASN_GAUGE, RONLY, var_tcapOmMsgs15minIntTable, 6, { 1, 1, 2, 4, 1, 1} },
#define   TCAPOMMSGS15MININTMESSAGESRECV  28
	{ (u_char) TCAPOMMSGS15MININTMESSAGESRECV, ASN_GAUGE, RONLY, var_tcapOmMsgs15minIntTable, 6, { 1, 1, 2, 4, 1, 2} },
#define   TCAPOMAEMEASUREMENTDELETED  29
	{ (u_char) TCAPOMAEMEASUREMENTDELETED, ASN_COUNTER, RONLY, var_tcapOmAETable, 6, { 1, 1, 3, 1, 1, 1} },
#define   TCAPOMAENEWTRANSACTIONS  30
	{ (u_char) TCAPOMAENEWTRANSACTIONS, ASN_COUNTER, RONLY, var_tcapOmAETable, 6, { 1, 1, 3, 1, 1, 2} },
#define   TCAPOMAEMEANOPENTRANSACTIONS  31
	{ (u_char) TCAPOMAEMEANOPENTRANSACTIONS, ASN_COUNTER, RONLY, var_tcapOmAETable, 6, { 1, 1, 3, 1, 1, 3} },
#define   TCAPOMAENOTUSED       32
	{ (u_char) TCAPOMAENOTUSED, ASN_COUNTER, RONLY, var_tcapOmAETable, 6, { 1, 1, 3, 1, 1, 4} },
#define   TCAPOMAECUMMULATIVEMEANDURATION  33
	{ (u_char) TCAPOMAECUMMULATIVEMEANDURATION, ASN_COUNTER, RONLY, var_tcapOmAETable, 6, { 1, 1, 3, 1, 1, 5} },
#define   TCAPOMAEMAXIMUMOPENTRANACTIONS  34
	{ (u_char) TCAPOMAEMAXIMUMOPENTRANACTIONS, ASN_COUNTER, RONLY, var_tcapOmAETable, 6, { 1, 1, 3, 1, 1, 6} },
#define   TCAPOMAETHRESHOLDEXCEEDED  35
	{ (u_char) TCAPOMAETHRESHOLDEXCEEDED, ASN_COUNTER, RONLY, var_tcapOmAETable, 6, { 1, 1, 3, 1, 1, 7} },
#define   TCAPOMAE5MINVALIDINTERVALS  36
	{ (u_char) TCAPOMAE5MINVALIDINTERVALS, ASN_INTEGER, RONLY, var_tcapOmAETable, 6, { 1, 1, 3, 1, 1, 8} },
#define   TCAPOMAE15MINVALIDINTERVALS  37
	{ (u_char) TCAPOMAE15MINVALIDINTERVALS, ASN_INTEGER, RONLY, var_tcapOmAETable, 6, { 1, 1, 3, 1, 1, 9} },
#define   TCAPOMAECURRENTMEASUREMENTDELETED  38
	{ (u_char) TCAPOMAECURRENTMEASUREMENTDELETED, ASN_GAUGE, RONLY, var_tcapOmAECurrentTable, 6, { 1, 1, 3, 2, 1, 1} },
#define   TCAPOMAECURRENTNEWTRANSACTIONS  39
	{ (u_char) TCAPOMAECURRENTNEWTRANSACTIONS, ASN_GAUGE, RONLY, var_tcapOmAECurrentTable, 6, { 1, 1, 3, 2, 1, 2} },
#define   TCAPOMAECURRENTMEANOPENTRANSACTIONS  40
	{ (u_char) TCAPOMAECURRENTMEANOPENTRANSACTIONS, ASN_GAUGE, RONLY, var_tcapOmAECurrentTable, 6, { 1, 1, 3, 2, 1, 3} },
#define   TCAPOMAECURRENTNOTUSED  41
	{ (u_char) TCAPOMAECURRENTNOTUSED, ASN_GAUGE, RONLY, var_tcapOmAECurrentTable, 6, { 1, 1, 3, 2, 1, 4} },
#define   TCAPOMAECURRENTCUMMULATIVEMEANDURATION  42
	{ (u_char) TCAPOMAECURRENTCUMMULATIVEMEANDURATION, ASN_GAUGE, RONLY, var_tcapOmAECurrentTable, 6, { 1, 1, 3, 2, 1, 5} },
#define   TCAPOMAECURRENTMAXIMUMOPENTRANACTIONS  43
	{ (u_char) TCAPOMAECURRENTMAXIMUMOPENTRANACTIONS, ASN_GAUGE, RONLY, var_tcapOmAECurrentTable, 6, { 1, 1, 3, 2, 1, 6} },
#define   TCAPOMAECURRENTTHRESHOLDEXCEEDED  44
	{ (u_char) TCAPOMAECURRENTTHRESHOLDEXCEEDED, ASN_GAUGE, RONLY, var_tcapOmAECurrentTable, 6, { 1, 1, 3, 2, 1, 7} },
#define   TCAPOMAE5MININTMEASUREMENTDELETED  45
	{ (u_char) TCAPOMAE5MININTMEASUREMENTDELETED, ASN_GAUGE, RONLY, var_tcapOmAE5minIntTable, 6, { 1, 1, 3, 3, 1, 1} },
#define   TCAPOMAE5MININTNEWTRANSACTIONS  46
	{ (u_char) TCAPOMAE5MININTNEWTRANSACTIONS, ASN_GAUGE, RONLY, var_tcapOmAE5minIntTable, 6, { 1, 1, 3, 3, 1, 2} },
#define   TCAPOMAE5MININTMEANOPENTRANSACTIONS  47
	{ (u_char) TCAPOMAE5MININTMEANOPENTRANSACTIONS, ASN_GAUGE, RONLY, var_tcapOmAE5minIntTable, 6, { 1, 1, 3, 3, 1, 3} },
#define   TCAPOMAE5MININTNOTUSED  48
	{ (u_char) TCAPOMAE5MININTNOTUSED, ASN_GAUGE, RONLY, var_tcapOmAE5minIntTable, 6, { 1, 1, 3, 3, 1, 4} },
#define   TCAPOMAE5MININTCUMMULATIVEMEANDURATION  49
	{ (u_char) TCAPOMAE5MININTCUMMULATIVEMEANDURATION, ASN_GAUGE, RONLY, var_tcapOmAE5minIntTable, 6, { 1, 1, 3, 3, 1, 5} },
#define   TCAPOMAE5MININTMAXIMUMOPENTRANACTIONS  50
	{ (u_char) TCAPOMAE5MININTMAXIMUMOPENTRANACTIONS, ASN_GAUGE, RONLY, var_tcapOmAE5minIntTable, 6, { 1, 1, 3, 3, 1, 6} },
#define   TCAPOMAE5MININTTHRESHOLDEXCEEDED  51
	{ (u_char) TCAPOMAE5MININTTHRESHOLDEXCEEDED, ASN_GAUGE, RONLY, var_tcapOmAE5minIntTable, 6, { 1, 1, 3, 3, 1, 7} },
#define   TCAPOMAE15MININTMEASUREMENTDELETED  52
	{ (u_char) TCAPOMAE15MININTMEASUREMENTDELETED, ASN_GAUGE, RONLY, var_tcapOmAE15minIntTable, 6, { 1, 1, 3, 4, 1, 1} },
#define   TCAPOMAE15MININTNEWTRANSACTIONS  53
	{ (u_char) TCAPOMAE15MININTNEWTRANSACTIONS, ASN_GAUGE, RONLY, var_tcapOmAE15minIntTable, 6, { 1, 1, 3, 4, 1, 2} },
#define   TCAPOMAE15MININTMEANOPENTRANSACTIONS  54
	{ (u_char) TCAPOMAE15MININTMEANOPENTRANSACTIONS, ASN_GAUGE, RONLY, var_tcapOmAE15minIntTable, 6, { 1, 1, 3, 4, 1, 3} },
#define   TCAPOMAE15MININTNOTUSED  55
	{ (u_char) TCAPOMAE15MININTNOTUSED, ASN_GAUGE, RONLY, var_tcapOmAE15minIntTable, 6, { 1, 1, 3, 4, 1, 4} },
#define   TCAPOMAE15MININTCUMMULATIVEMEANDURATION  56
	{ (u_char) TCAPOMAE15MININTCUMMULATIVEMEANDURATION, ASN_GAUGE, RONLY, var_tcapOmAE15minIntTable, 6, { 1, 1, 3, 4, 1, 5} },
#define   TCAPOMAE15MININTMAXIMUMOPENTRANACTIONS  57
	{ (u_char) TCAPOMAE15MININTMAXIMUMOPENTRANACTIONS, ASN_GAUGE, RONLY, var_tcapOmAE15minIntTable, 6, { 1, 1, 3, 4, 1, 6} },
#define   TCAPOMAE15MININTTHRESHOLDEXCEEDED  58
	{ (u_char) TCAPOMAE15MININTTHRESHOLDEXCEEDED, ASN_GAUGE, RONLY, var_tcapOmAE15minIntTable, 6, { 1, 1, 3, 4, 1, 7} },
#define   TCAPOMTCPABORTRECVUNRECTID  59
	{ (u_char) TCAPOMTCPABORTRECVUNRECTID, ASN_COUNTER, RONLY, var_tcapOmTcTable, 6, { 1, 1, 4, 1, 1, 1} },
#define   TCAPOMTCPABORTRECVRESOURCELIM  60
	{ (u_char) TCAPOMTCPABORTRECVRESOURCELIM, ASN_COUNTER, RONLY, var_tcapOmTcTable, 6, { 1, 1, 4, 1, 1, 2} },
#define   TCAPOMTCREJECTRECVRESOURCELIM  61
	{ (u_char) TCAPOMTCREJECTRECVRESOURCELIM, ASN_COUNTER, RONLY, var_tcapOmTcTable, 6, { 1, 1, 4, 1, 1, 3} },
#define   TCAPOMTCPABORTSENTUNRECTID  62
	{ (u_char) TCAPOMTCPABORTSENTUNRECTID, ASN_COUNTER, RONLY, var_tcapOmTcTable, 6, { 1, 1, 4, 1, 1, 4} },
#define   TCAPOMTCPABORTSENTRESOURCELIM  63
	{ (u_char) TCAPOMTCPABORTSENTRESOURCELIM, ASN_COUNTER, RONLY, var_tcapOmTcTable, 6, { 1, 1, 4, 1, 1, 5} },
#define   TCAPOMTCREJECTSENTRESOURCELIM  64
	{ (u_char) TCAPOMTCREJECTSENTRESOURCELIM, ASN_COUNTER, RONLY, var_tcapOmTcTable, 6, { 1, 1, 4, 1, 1, 6} },
#define   TCAPOMTCTCUSERCANCEL  65
	{ (u_char) TCAPOMTCTCUSERCANCEL, ASN_COUNTER, RONLY, var_tcapOmTcTable, 6, { 1, 1, 4, 1, 1, 7} },
#define   TCAPOMTCMESSAGESDISCARDED  66
	{ (u_char) TCAPOMTCMESSAGESDISCARDED, ASN_COUNTER, RONLY, var_tcapOmTcTable, 6, { 1, 1, 4, 1, 1, 8} },
#define   TCAPOMTCNOTUSED       67
	{ (u_char) TCAPOMTCNOTUSED, ASN_COUNTER, RONLY, var_tcapOmTcTable, 6, { 1, 1, 4, 1, 1, 9} },
#define   TCAPOMTCPABORTSRECEIVED  68
	{ (u_char) TCAPOMTCPABORTSRECEIVED, ASN_COUNTER, RONLY, var_tcapOmTcTable, 6, { 1, 1, 4, 1, 1, 10} },
#define   TCAPOMTCTCUSERREJECTSRECEIVED  69
	{ (u_char) TCAPOMTCTCUSERREJECTSRECEIVED, ASN_COUNTER, RONLY, var_tcapOmTcTable, 6, { 1, 1, 4, 1, 1, 11} },
#define   TCAPOMTCTPERRORS      70
	{ (u_char) TCAPOMTCTPERRORS, ASN_COUNTER, RONLY, var_tcapOmTcTable, 6, { 1, 1, 4, 1, 1, 12} },
#define   TCAPOMTC5MINVALIDINTERVALS  71
	{ (u_char) TCAPOMTC5MINVALIDINTERVALS, ASN_INTEGER, RONLY, var_tcapOmTcTable, 6, { 1, 1, 4, 1, 1, 13} },
#define   TCAPOMTC15MINVALIDINTERVALS  72
	{ (u_char) TCAPOMTC15MINVALIDINTERVALS, ASN_INTEGER, RONLY, var_tcapOmTcTable, 6, { 1, 1, 4, 1, 1, 14} },
#define   TCAPOMTCCURRENTPABORTRECVUNRECTID  73
	{ (u_char) TCAPOMTCCURRENTPABORTRECVUNRECTID, ASN_GAUGE, RONLY, var_tcapOmTcCurrentTable, 6, { 1, 1, 4, 2, 1, 1} },
#define   TCAPOMTCCURRENTPABORTRECVRESOURCELIM  74
	{ (u_char) TCAPOMTCCURRENTPABORTRECVRESOURCELIM, ASN_GAUGE, RONLY, var_tcapOmTcCurrentTable, 6, { 1, 1, 4, 2, 1, 2} },
#define   TCAPOMTCCURRENTREJECTRECVRESOURCELIM  75
	{ (u_char) TCAPOMTCCURRENTREJECTRECVRESOURCELIM, ASN_GAUGE, RONLY, var_tcapOmTcCurrentTable, 6, { 1, 1, 4, 2, 1, 3} },
#define   TCAPOMTCCURRENTPABORTSENTUNRECTID  76
	{ (u_char) TCAPOMTCCURRENTPABORTSENTUNRECTID, ASN_GAUGE, RONLY, var_tcapOmTcCurrentTable, 6, { 1, 1, 4, 2, 1, 4} },
#define   TCAPOMTCCURRENTPABORTSENTRESOURCELIM  77
	{ (u_char) TCAPOMTCCURRENTPABORTSENTRESOURCELIM, ASN_GAUGE, RONLY, var_tcapOmTcCurrentTable, 6, { 1, 1, 4, 2, 1, 5} },
#define   TCAPOMTCCURRENTREJECTSENTRESOURCELIM  78
	{ (u_char) TCAPOMTCCURRENTREJECTSENTRESOURCELIM, ASN_GAUGE, RONLY, var_tcapOmTcCurrentTable, 6, { 1, 1, 4, 2, 1, 6} },
#define   TCAPOMTCCURRENTTCUSERCANCEL  79
	{ (u_char) TCAPOMTCCURRENTTCUSERCANCEL, ASN_GAUGE, RONLY, var_tcapOmTcCurrentTable, 6, { 1, 1, 4, 2, 1, 7} },
#define   TCAPOMTCCURRENTMESSAGESDISCARDED  80
	{ (u_char) TCAPOMTCCURRENTMESSAGESDISCARDED, ASN_GAUGE, RONLY, var_tcapOmTcCurrentTable, 6, { 1, 1, 4, 2, 1, 8} },
#define   TCAPOMTCCURRENTNOTUSED  81
	{ (u_char) TCAPOMTCCURRENTNOTUSED, ASN_GAUGE, RONLY, var_tcapOmTcCurrentTable, 6, { 1, 1, 4, 2, 1, 9} },
#define   TCAPOMTCCURRENTPABORTSRECEIVED  82
	{ (u_char) TCAPOMTCCURRENTPABORTSRECEIVED, ASN_GAUGE, RONLY, var_tcapOmTcCurrentTable, 6, { 1, 1, 4, 2, 1, 10} },
#define   TCAPOMTCCURRENTTCUSERREJECTSRECEIVED  83
	{ (u_char) TCAPOMTCCURRENTTCUSERREJECTSRECEIVED, ASN_GAUGE, RONLY, var_tcapOmTcCurrentTable, 6, { 1, 1, 4, 2, 1, 11} },
#define   TCAPOMTCCURRENTTPERRORS  84
	{ (u_char) TCAPOMTCCURRENTTPERRORS, ASN_GAUGE, RONLY, var_tcapOmTcCurrentTable, 6, { 1, 1, 4, 2, 1, 12} },
#define   TCAPOMTC5MININTPABORTRECVUNRECTID  85
	{ (u_char) TCAPOMTC5MININTPABORTRECVUNRECTID, ASN_GAUGE, RONLY, var_tcapOmTc5minIntTable, 6, { 1, 1, 4, 3, 1, 1} },
#define   TCAPOMTC5MININTPABORTRECVRESOURCELIM  86
	{ (u_char) TCAPOMTC5MININTPABORTRECVRESOURCELIM, ASN_GAUGE, RONLY, var_tcapOmTc5minIntTable, 6, { 1, 1, 4, 3, 1, 2} },
#define   TCAPOMTC5MININTREJECTRECVRESOURCELIM  87
	{ (u_char) TCAPOMTC5MININTREJECTRECVRESOURCELIM, ASN_GAUGE, RONLY, var_tcapOmTc5minIntTable, 6, { 1, 1, 4, 3, 1, 3} },
#define   TCAPOMTC5MININTPABORTSENTUNRECTID  88
	{ (u_char) TCAPOMTC5MININTPABORTSENTUNRECTID, ASN_GAUGE, RONLY, var_tcapOmTc5minIntTable, 6, { 1, 1, 4, 3, 1, 4} },
#define   TCAPOMTC5MININTPABORTSENTRESOURCELIM  89
	{ (u_char) TCAPOMTC5MININTPABORTSENTRESOURCELIM, ASN_GAUGE, RONLY, var_tcapOmTc5minIntTable, 6, { 1, 1, 4, 3, 1, 5} },
#define   TCAPOMTC5MININTREJECTSENTRESOURCELIM  90
	{ (u_char) TCAPOMTC5MININTREJECTSENTRESOURCELIM, ASN_GAUGE, RONLY, var_tcapOmTc5minIntTable, 6, { 1, 1, 4, 3, 1, 6} },
#define   TCAPOMTC5MININTTCUSERCANCEL  91
	{ (u_char) TCAPOMTC5MININTTCUSERCANCEL, ASN_GAUGE, RONLY, var_tcapOmTc5minIntTable, 6, { 1, 1, 4, 3, 1, 7} },
#define   TCAPOMTC5MININTMESSAGESDISCARDED  92
	{ (u_char) TCAPOMTC5MININTMESSAGESDISCARDED, ASN_GAUGE, RONLY, var_tcapOmTc5minIntTable, 6, { 1, 1, 4, 3, 1, 8} },
#define   TCAPOMTC5MININTNOTUSED  93
	{ (u_char) TCAPOMTC5MININTNOTUSED, ASN_GAUGE, RONLY, var_tcapOmTc5minIntTable, 6, { 1, 1, 4, 3, 1, 9} },
#define   TCAPOMTC5MININTPABORTSRECEIVED  94
	{ (u_char) TCAPOMTC5MININTPABORTSRECEIVED, ASN_GAUGE, RONLY, var_tcapOmTc5minIntTable, 6, { 1, 1, 4, 3, 1, 10} },
#define   TCAPOMTC5MININTTCUSERREJECTSRECEIVED  95
	{ (u_char) TCAPOMTC5MININTTCUSERREJECTSRECEIVED, ASN_GAUGE, RONLY, var_tcapOmTc5minIntTable, 6, { 1, 1, 4, 3, 1, 11} },
#define   TCAPOMTC5MININTTPERRORS  96
	{ (u_char) TCAPOMTC5MININTTPERRORS, ASN_GAUGE, RONLY, var_tcapOmTc5minIntTable, 6, { 1, 1, 4, 3, 1, 12} },
#define   TCAPOMTC15MININTPABORTRECVUNRECTID  97
	{ (u_char) TCAPOMTC15MININTPABORTRECVUNRECTID, ASN_GAUGE, RONLY, var_tcapOmTc15minIntTable, 6, { 1, 1, 4, 4, 1, 1} },
#define   TCAPOMTC15MININTPABORTRECVRESOURCELIM  98
	{ (u_char) TCAPOMTC15MININTPABORTRECVRESOURCELIM, ASN_GAUGE, RONLY, var_tcapOmTc15minIntTable, 6, { 1, 1, 4, 4, 1, 2} },
#define   TCAPOMTC15MININTREJECTRECVRESOURCELIM  99
	{ (u_char) TCAPOMTC15MININTREJECTRECVRESOURCELIM, ASN_GAUGE, RONLY, var_tcapOmTc15minIntTable, 6, { 1, 1, 4, 4, 1, 3} },
#define   TCAPOMTC15MININTPABORTSENTUNRECTID  100
	{ (u_char) TCAPOMTC15MININTPABORTSENTUNRECTID, ASN_GAUGE, RONLY, var_tcapOmTc15minIntTable, 6, { 1, 1, 4, 4, 1, 4} },
#define   TCAPOMTC15MININTPABORTSENTRESOURCELIM  101
	{ (u_char) TCAPOMTC15MININTPABORTSENTRESOURCELIM, ASN_GAUGE, RONLY, var_tcapOmTc15minIntTable, 6, { 1, 1, 4, 4, 1, 5} },
#define   TCAPOMTC15MININTREJECTSENTRESOURCELIM  102
	{ (u_char) TCAPOMTC15MININTREJECTSENTRESOURCELIM, ASN_GAUGE, RONLY, var_tcapOmTc15minIntTable, 6, { 1, 1, 4, 4, 1, 6} },
#define   TCAPOMTC15MININTTCUSERCANCEL  103
	{ (u_char) TCAPOMTC15MININTTCUSERCANCEL, ASN_GAUGE, RONLY, var_tcapOmTc15minIntTable, 6, { 1, 1, 4, 4, 1, 7} },
#define   TCAPOMTC15MININTMESSAGESDISCARDED  104
	{ (u_char) TCAPOMTC15MININTMESSAGESDISCARDED, ASN_GAUGE, RONLY, var_tcapOmTc15minIntTable, 6, { 1, 1, 4, 4, 1, 8} },
#define   TCAPOMTC15MININTNOTUSED  105
	{ (u_char) TCAPOMTC15MININTNOTUSED, ASN_GAUGE, RONLY, var_tcapOmTc15minIntTable, 6, { 1, 1, 4, 4, 1, 9} },
#define   TCAPOMTC15MININTPABORTSRECEIVED  106
	{ (u_char) TCAPOMTC15MININTPABORTSRECEIVED, ASN_GAUGE, RONLY, var_tcapOmTc15minIntTable, 6, { 1, 1, 4, 4, 1, 10} },
#define   TCAPOMTC15MININTTCUSERREJECTSRECEIVED  107
	{ (u_char) TCAPOMTC15MININTTCUSERREJECTSRECEIVED, ASN_GAUGE, RONLY, var_tcapOmTc15minIntTable, 6, { 1, 1, 4, 4, 1, 11} },
#define   TCAPOMTC15MININTTPERRORS  108
	{ (u_char) TCAPOMTC15MININTTPERRORS, ASN_GAUGE, RONLY, var_tcapOmTc15minIntTable, 6, { 1, 1, 4, 4, 1, 12} },
#define   TCAPOMDEVELRECVPABORTMESSAGETYPE  109
	{ (u_char) TCAPOMDEVELRECVPABORTMESSAGETYPE, ASN_COUNTER, RONLY, var_tcapOmDevelRecvTable, 6, { 1, 1, 5, 1, 1, 1} },
#define   TCAPOMDEVELRECVPABORTINCORRECTTP  110
	{ (u_char) TCAPOMDEVELRECVPABORTINCORRECTTP, ASN_COUNTER, RONLY, var_tcapOmDevelRecvTable, 6, { 1, 1, 5, 1, 1, 2} },
#define   TCAPOMDEVELRECVPABORTBADTP  111
	{ (u_char) TCAPOMDEVELRECVPABORTBADTP, ASN_COUNTER, RONLY, var_tcapOmDevelRecvTable, 6, { 1, 1, 5, 1, 1, 3} },
#define   TCAPOMDEVELRECVREJECTUNRECCOMP  112
	{ (u_char) TCAPOMDEVELRECVREJECTUNRECCOMP, ASN_COUNTER, RONLY, var_tcapOmDevelRecvTable, 6, { 1, 1, 5, 1, 1, 4} },
#define   TCAPOMDEVELRECVREJECTMISTYPECOMP  113
	{ (u_char) TCAPOMDEVELRECVREJECTMISTYPECOMP, ASN_COUNTER, RONLY, var_tcapOmDevelRecvTable, 6, { 1, 1, 5, 1, 1, 5} },
#define   TCAPOMDEVELRECVREJECTBADCOMPSTRUCT  114
	{ (u_char) TCAPOMDEVELRECVREJECTBADCOMPSTRUCT, ASN_COUNTER, RONLY, var_tcapOmDevelRecvTable, 6, { 1, 1, 5, 1, 1, 6} },
#define   TCAPOMDEVELRECVREJECTUNRECLINKID  115
	{ (u_char) TCAPOMDEVELRECVREJECTUNRECLINKID, ASN_COUNTER, RONLY, var_tcapOmDevelRecvTable, 6, { 1, 1, 5, 1, 1, 7} },
#define   TCAPOMDEVELRECVREJECTUNRECIDRETRES  116
	{ (u_char) TCAPOMDEVELRECVREJECTUNRECIDRETRES, ASN_COUNTER, RONLY, var_tcapOmDevelRecvTable, 6, { 1, 1, 5, 1, 1, 8} },
#define   TCAPOMDEVELRECVREJECTUNEXPRETRES  117
	{ (u_char) TCAPOMDEVELRECVREJECTUNEXPRETRES, ASN_COUNTER, RONLY, var_tcapOmDevelRecvTable, 6, { 1, 1, 5, 1, 1, 9} },
#define   TCAPOMDEVELRECVREJECTUNRECIDRETERR  118
	{ (u_char) TCAPOMDEVELRECVREJECTUNRECIDRETERR, ASN_COUNTER, RONLY, var_tcapOmDevelRecvTable, 6, { 1, 1, 5, 1, 1, 10} },
#define   TCAPOMDEVELRECVREJECTUNEXPRETERR  119
	{ (u_char) TCAPOMDEVELRECVREJECTUNEXPRETERR, ASN_COUNTER, RONLY, var_tcapOmDevelRecvTable, 6, { 1, 1, 5, 1, 1, 11} },
#define   TCAPOMDEVELRECVUSRREJDUPINVOKEID  120
	{ (u_char) TCAPOMDEVELRECVUSRREJDUPINVOKEID, ASN_COUNTER, RONLY, var_tcapOmDevelRecvTable, 6, { 1, 1, 5, 1, 1, 12} },
#define   TCAPOMDEVELRECVUSRREJUNRECOPER  121
	{ (u_char) TCAPOMDEVELRECVUSRREJUNRECOPER, ASN_COUNTER, RONLY, var_tcapOmDevelRecvTable, 6, { 1, 1, 5, 1, 1, 13} },
#define   TCAPOMDEVELRECVUSRREJPARMINVOKE  122
	{ (u_char) TCAPOMDEVELRECVUSRREJPARMINVOKE, ASN_COUNTER, RONLY, var_tcapOmDevelRecvTable, 6, { 1, 1, 5, 1, 1, 14} },
#define   TCAPOMDEVELRECVUSRREJRELEASING  123
	{ (u_char) TCAPOMDEVELRECVUSRREJRELEASING, ASN_COUNTER, RONLY, var_tcapOmDevelRecvTable, 6, { 1, 1, 5, 1, 1, 15} },
#define   TCAPOMDEVELRECVUSRREJUNEXPLINKRESP  124
	{ (u_char) TCAPOMDEVELRECVUSRREJUNEXPLINKRESP, ASN_COUNTER, RONLY, var_tcapOmDevelRecvTable, 6, { 1, 1, 5, 1, 1, 16} },
#define   TCAPOMDEVELRECVUSRREJUNEXPLINKOPER  125
	{ (u_char) TCAPOMDEVELRECVUSRREJUNEXPLINKOPER, ASN_COUNTER, RONLY, var_tcapOmDevelRecvTable, 6, { 1, 1, 5, 1, 1, 17} },
#define   TCAPOMDEVELRECVUSRREJUNRECERROR  126
	{ (u_char) TCAPOMDEVELRECVUSRREJUNRECERROR, ASN_COUNTER, RONLY, var_tcapOmDevelRecvTable, 6, { 1, 1, 5, 1, 1, 18} },
#define   TCAPOMDEVELRECVUSRREJUNEXPERROR  127
	{ (u_char) TCAPOMDEVELRECVUSRREJUNEXPERROR, ASN_COUNTER, RONLY, var_tcapOmDevelRecvTable, 6, { 1, 1, 5, 1, 1, 19} },
#define   TCAPOMDEVELRECVUSRREJPARMRETRESULT  128
	{ (u_char) TCAPOMDEVELRECVUSRREJPARMRETRESULT, ASN_COUNTER, RONLY, var_tcapOmDevelRecvTable, 6, { 1, 1, 5, 1, 1, 20} },
#define   TCAPOMDEVELRECVUSRREJPARMRETERROR  129
	{ (u_char) TCAPOMDEVELRECVUSRREJPARMRETERROR, ASN_COUNTER, RONLY, var_tcapOmDevelRecvTable, 6, { 1, 1, 5, 1, 1, 21} },
#define   TCAPOMDEVELRECV5MINVALIDINTERVALS  130
	{ (u_char) TCAPOMDEVELRECV5MINVALIDINTERVALS, ASN_INTEGER, RONLY, var_tcapOmDevelRecvTable, 6, { 1, 1, 5, 1, 1, 22} },
#define   TCAPOMDEVELRECV15MINVALIDINTERVALS  131
	{ (u_char) TCAPOMDEVELRECV15MINVALIDINTERVALS, ASN_INTEGER, RONLY, var_tcapOmDevelRecvTable, 6, { 1, 1, 5, 1, 1, 23} },
#define   TCAPOMDEVELRECVCURRENTPABORTMESSAGETYPE  132
	{ (u_char) TCAPOMDEVELRECVCURRENTPABORTMESSAGETYPE, ASN_GAUGE, RONLY, var_tcapOmDevelRecvCurrentTable, 6, { 1, 1, 5, 2, 1, 1} },
#define   TCAPOMDEVELRECVCURRENTPABORTINCORRECTTP  133
	{ (u_char) TCAPOMDEVELRECVCURRENTPABORTINCORRECTTP, ASN_GAUGE, RONLY, var_tcapOmDevelRecvCurrentTable, 6, { 1, 1, 5, 2, 1, 2} },
#define   TCAPOMDEVELRECVCURRENTPABORTBADTP  134
	{ (u_char) TCAPOMDEVELRECVCURRENTPABORTBADTP, ASN_GAUGE, RONLY, var_tcapOmDevelRecvCurrentTable, 6, { 1, 1, 5, 2, 1, 3} },
#define   TCAPOMDEVELRECVCURRENTREJECTUNRECCOMP  135
	{ (u_char) TCAPOMDEVELRECVCURRENTREJECTUNRECCOMP, ASN_GAUGE, RONLY, var_tcapOmDevelRecvCurrentTable, 6, { 1, 1, 5, 2, 1, 4} },
#define   TCAPOMDEVELRECVCURRENTREJECTMISTYPECOMP  136
	{ (u_char) TCAPOMDEVELRECVCURRENTREJECTMISTYPECOMP, ASN_GAUGE, RONLY, var_tcapOmDevelRecvCurrentTable, 6, { 1, 1, 5, 2, 1, 5} },
#define   TCAPOMDEVELRECVCURRENTREJECTBADCOMPSTRUCT  137
	{ (u_char) TCAPOMDEVELRECVCURRENTREJECTBADCOMPSTRUCT, ASN_GAUGE, RONLY, var_tcapOmDevelRecvCurrentTable, 6, { 1, 1, 5, 2, 1, 6} },
#define   TCAPOMDEVELRECVCURRENTREJECTUNRECLINKID  138
	{ (u_char) TCAPOMDEVELRECVCURRENTREJECTUNRECLINKID, ASN_GAUGE, RONLY, var_tcapOmDevelRecvCurrentTable, 6, { 1, 1, 5, 2, 1, 7} },
#define   TCAPOMDEVELRECVCURRENTREJECTUNRECIDRETRES  139
	{ (u_char) TCAPOMDEVELRECVCURRENTREJECTUNRECIDRETRES, ASN_GAUGE, RONLY, var_tcapOmDevelRecvCurrentTable, 6, { 1, 1, 5, 2, 1, 8} },
#define   TCAPOMDEVELRECVCURRENTREJECTUNEXPRETRES  140
	{ (u_char) TCAPOMDEVELRECVCURRENTREJECTUNEXPRETRES, ASN_GAUGE, RONLY, var_tcapOmDevelRecvCurrentTable, 6, { 1, 1, 5, 2, 1, 9} },
#define   TCAPOMDEVELRECVCURRENTREJECTUNRECIDRETERR  141
	{ (u_char) TCAPOMDEVELRECVCURRENTREJECTUNRECIDRETERR, ASN_GAUGE, RONLY, var_tcapOmDevelRecvCurrentTable, 6, { 1, 1, 5, 2, 1, 10} },
#define   TCAPOMDEVELRECVCURRENTREJECTUNEXPRETERR  142
	{ (u_char) TCAPOMDEVELRECVCURRENTREJECTUNEXPRETERR, ASN_GAUGE, RONLY, var_tcapOmDevelRecvCurrentTable, 6, { 1, 1, 5, 2, 1, 11} },
#define   TCAPOMDEVELRECVCURRENTUSRREJDUPINVOKEID  143
	{ (u_char) TCAPOMDEVELRECVCURRENTUSRREJDUPINVOKEID, ASN_GAUGE, RONLY, var_tcapOmDevelRecvCurrentTable, 6, { 1, 1, 5, 2, 1, 12} },
#define   TCAPOMDEVELRECVCURRENTUSRREJUNRECOPER  144
	{ (u_char) TCAPOMDEVELRECVCURRENTUSRREJUNRECOPER, ASN_GAUGE, RONLY, var_tcapOmDevelRecvCurrentTable, 6, { 1, 1, 5, 2, 1, 13} },
#define   TCAPOMDEVELRECVCURRENTUSRREJPARMINVOKE  145
	{ (u_char) TCAPOMDEVELRECVCURRENTUSRREJPARMINVOKE, ASN_GAUGE, RONLY, var_tcapOmDevelRecvCurrentTable, 6, { 1, 1, 5, 2, 1, 14} },
#define   TCAPOMDEVELRECVCURRENTUSRREJRELEASING  146
	{ (u_char) TCAPOMDEVELRECVCURRENTUSRREJRELEASING, ASN_GAUGE, RONLY, var_tcapOmDevelRecvCurrentTable, 6, { 1, 1, 5, 2, 1, 15} },
#define   TCAPOMDEVELRECVCURRENTUSRREJUNEXPLINKRESP  147
	{ (u_char) TCAPOMDEVELRECVCURRENTUSRREJUNEXPLINKRESP, ASN_GAUGE, RONLY, var_tcapOmDevelRecvCurrentTable, 6, { 1, 1, 5, 2, 1, 16} },
#define   TCAPOMDEVELRECVCURRENTUSRREJUNEXPLINKOPER  148
	{ (u_char) TCAPOMDEVELRECVCURRENTUSRREJUNEXPLINKOPER, ASN_GAUGE, RONLY, var_tcapOmDevelRecvCurrentTable, 6, { 1, 1, 5, 2, 1, 17} },
#define   TCAPOMDEVELRECVCURRENTUSRREJUNRECERROR  149
	{ (u_char) TCAPOMDEVELRECVCURRENTUSRREJUNRECERROR, ASN_GAUGE, RONLY, var_tcapOmDevelRecvCurrentTable, 6, { 1, 1, 5, 2, 1, 18} },
#define   TCAPOMDEVELRECVCURRENTUSRREJUNEXPERROR  150
	{ (u_char) TCAPOMDEVELRECVCURRENTUSRREJUNEXPERROR, ASN_GAUGE, RONLY, var_tcapOmDevelRecvCurrentTable, 6, { 1, 1, 5, 2, 1, 19} },
#define   TCAPOMDEVELRECVCURRENTUSRREJPARMRETRESULT  151
	{ (u_char) TCAPOMDEVELRECVCURRENTUSRREJPARMRETRESULT, ASN_GAUGE, RONLY, var_tcapOmDevelRecvCurrentTable, 6, { 1, 1, 5, 2, 1, 20} },
#define   TCAPOMDEVELRECVCURRENTUSRREJPARMRETERROR  152
	{ (u_char) TCAPOMDEVELRECVCURRENTUSRREJPARMRETERROR, ASN_GAUGE, RONLY, var_tcapOmDevelRecvCurrentTable, 6, { 1, 1, 5, 2, 1, 21} },
#define   TCAPOMDEVELRECV5MININTPABORTMESSAGETYPE  153
	{ (u_char) TCAPOMDEVELRECV5MININTPABORTMESSAGETYPE, ASN_GAUGE, RONLY, var_tcapOmDevelRecv5minIntTable, 6, { 1, 1, 5, 3, 1, 1} },
#define   TCAPOMDEVELRECV5MININTPABORTINCORRECTTP  154
	{ (u_char) TCAPOMDEVELRECV5MININTPABORTINCORRECTTP, ASN_GAUGE, RONLY, var_tcapOmDevelRecv5minIntTable, 6, { 1, 1, 5, 3, 1, 2} },
#define   TCAPOMDEVELRECV5MININTPABORTBADTP  155
	{ (u_char) TCAPOMDEVELRECV5MININTPABORTBADTP, ASN_GAUGE, RONLY, var_tcapOmDevelRecv5minIntTable, 6, { 1, 1, 5, 3, 1, 3} },
#define   TCAPOMDEVELRECV5MININTREJECTUNRECCOMP  156
	{ (u_char) TCAPOMDEVELRECV5MININTREJECTUNRECCOMP, ASN_GAUGE, RONLY, var_tcapOmDevelRecv5minIntTable, 6, { 1, 1, 5, 3, 1, 4} },
#define   TCAPOMDEVELRECV5MININTREJECTMISTYPECOMP  157
	{ (u_char) TCAPOMDEVELRECV5MININTREJECTMISTYPECOMP, ASN_GAUGE, RONLY, var_tcapOmDevelRecv5minIntTable, 6, { 1, 1, 5, 3, 1, 5} },
#define   TCAPOMDEVELRECV5MININTREJECTBADCOMPSTRUCT  158
	{ (u_char) TCAPOMDEVELRECV5MININTREJECTBADCOMPSTRUCT, ASN_GAUGE, RONLY, var_tcapOmDevelRecv5minIntTable, 6, { 1, 1, 5, 3, 1, 6} },
#define   TCAPOMDEVELRECV5MININTREJECTUNRECLINKID  159
	{ (u_char) TCAPOMDEVELRECV5MININTREJECTUNRECLINKID, ASN_GAUGE, RONLY, var_tcapOmDevelRecv5minIntTable, 6, { 1, 1, 5, 3, 1, 7} },
#define   TCAPOMDEVELRECV5MININTREJECTUNRECIDRETRES  160
	{ (u_char) TCAPOMDEVELRECV5MININTREJECTUNRECIDRETRES, ASN_GAUGE, RONLY, var_tcapOmDevelRecv5minIntTable, 6, { 1, 1, 5, 3, 1, 8} },
#define   TCAPOMDEVELRECV5MININTREJECTUNEXPRETRES  161
	{ (u_char) TCAPOMDEVELRECV5MININTREJECTUNEXPRETRES, ASN_GAUGE, RONLY, var_tcapOmDevelRecv5minIntTable, 6, { 1, 1, 5, 3, 1, 9} },
#define   TCAPOMDEVELRECV5MININTREJECTUNRECIDRETERR  162
	{ (u_char) TCAPOMDEVELRECV5MININTREJECTUNRECIDRETERR, ASN_GAUGE, RONLY, var_tcapOmDevelRecv5minIntTable, 6, { 1, 1, 5, 3, 1, 10} },
#define   TCAPOMDEVELRECV5MININTREJECTUNEXPRETERR  163
	{ (u_char) TCAPOMDEVELRECV5MININTREJECTUNEXPRETERR, ASN_GAUGE, RONLY, var_tcapOmDevelRecv5minIntTable, 6, { 1, 1, 5, 3, 1, 11} },
#define   TCAPOMDEVELRECV5MININTUSRREJDUPINVOKEID  164
	{ (u_char) TCAPOMDEVELRECV5MININTUSRREJDUPINVOKEID, ASN_GAUGE, RONLY, var_tcapOmDevelRecv5minIntTable, 6, { 1, 1, 5, 3, 1, 12} },
#define   TCAPOMDEVELRECV5MININTUSRREJUNRECOPER  165
	{ (u_char) TCAPOMDEVELRECV5MININTUSRREJUNRECOPER, ASN_GAUGE, RONLY, var_tcapOmDevelRecv5minIntTable, 6, { 1, 1, 5, 3, 1, 13} },
#define   TCAPOMDEVELRECV5MININTUSRREJPARMINVOKE  166
	{ (u_char) TCAPOMDEVELRECV5MININTUSRREJPARMINVOKE, ASN_GAUGE, RONLY, var_tcapOmDevelRecv5minIntTable, 6, { 1, 1, 5, 3, 1, 14} },
#define   TCAPOMDEVELRECV5MININTUSRREJRELEASING  167
	{ (u_char) TCAPOMDEVELRECV5MININTUSRREJRELEASING, ASN_GAUGE, RONLY, var_tcapOmDevelRecv5minIntTable, 6, { 1, 1, 5, 3, 1, 15} },
#define   TCAPOMDEVELRECV5MININTUSRREJUNEXPLINKRESP  168
	{ (u_char) TCAPOMDEVELRECV5MININTUSRREJUNEXPLINKRESP, ASN_GAUGE, RONLY, var_tcapOmDevelRecv5minIntTable, 6, { 1, 1, 5, 3, 1, 16} },
#define   TCAPOMDEVELRECV5MININTUSRREJUNEXPLINKOPER  169
	{ (u_char) TCAPOMDEVELRECV5MININTUSRREJUNEXPLINKOPER, ASN_GAUGE, RONLY, var_tcapOmDevelRecv5minIntTable, 6, { 1, 1, 5, 3, 1, 17} },
#define   TCAPOMDEVELRECV5MININTUSRREJUNRECERROR  170
	{ (u_char) TCAPOMDEVELRECV5MININTUSRREJUNRECERROR, ASN_GAUGE, RONLY, var_tcapOmDevelRecv5minIntTable, 6, { 1, 1, 5, 3, 1, 18} },
#define   TCAPOMDEVELRECV5MININTUSRREJUNEXPERROR  171
	{ (u_char) TCAPOMDEVELRECV5MININTUSRREJUNEXPERROR, ASN_GAUGE, RONLY, var_tcapOmDevelRecv5minIntTable, 6, { 1, 1, 5, 3, 1, 19} },
#define   TCAPOMDEVELRECV5MININTUSRREJPARMRETRESULT  172
	{ (u_char) TCAPOMDEVELRECV5MININTUSRREJPARMRETRESULT, ASN_GAUGE, RONLY, var_tcapOmDevelRecv5minIntTable, 6, { 1, 1, 5, 3, 1, 20} },
#define   TCAPOMDEVELRECV5MININTUSRREJPARMRETERROR  173
	{ (u_char) TCAPOMDEVELRECV5MININTUSRREJPARMRETERROR, ASN_GAUGE, RONLY, var_tcapOmDevelRecv5minIntTable, 6, { 1, 1, 5, 3, 1, 21} },
#define   TCAPOMDEVELRECV15MININTPABORTMESSAGETYPE  174
	{ (u_char) TCAPOMDEVELRECV15MININTPABORTMESSAGETYPE, ASN_GAUGE, RONLY, var_tcapOmDevelRecv15minIntTable, 6, { 1, 1, 5, 4, 1, 1} },
#define   TCAPOMDEVELRECV15MININTPABORTINCORRECTTP  175
	{ (u_char) TCAPOMDEVELRECV15MININTPABORTINCORRECTTP, ASN_GAUGE, RONLY, var_tcapOmDevelRecv15minIntTable, 6, { 1, 1, 5, 4, 1, 2} },
#define   TCAPOMDEVELRECV15MININTPABORTBADTP  176
	{ (u_char) TCAPOMDEVELRECV15MININTPABORTBADTP, ASN_GAUGE, RONLY, var_tcapOmDevelRecv15minIntTable, 6, { 1, 1, 5, 4, 1, 3} },
#define   TCAPOMDEVELRECV15MININTREJECTUNRECCOMP  177
	{ (u_char) TCAPOMDEVELRECV15MININTREJECTUNRECCOMP, ASN_GAUGE, RONLY, var_tcapOmDevelRecv15minIntTable, 6, { 1, 1, 5, 4, 1, 4} },
#define   TCAPOMDEVELRECV15MININTREJECTMISTYPECOMP  178
	{ (u_char) TCAPOMDEVELRECV15MININTREJECTMISTYPECOMP, ASN_GAUGE, RONLY, var_tcapOmDevelRecv15minIntTable, 6, { 1, 1, 5, 4, 1, 5} },
#define   TCAPOMDEVELRECV15MININTREJECTBADCOMPSTRUCT  179
	{ (u_char) TCAPOMDEVELRECV15MININTREJECTBADCOMPSTRUCT, ASN_GAUGE, RONLY, var_tcapOmDevelRecv15minIntTable, 6, { 1, 1, 5, 4, 1, 6} },
#define   TCAPOMDEVELRECV15MININTREJECTUNRECLINKID  180
	{ (u_char) TCAPOMDEVELRECV15MININTREJECTUNRECLINKID, ASN_GAUGE, RONLY, var_tcapOmDevelRecv15minIntTable, 6, { 1, 1, 5, 4, 1, 7} },
#define   TCAPOMDEVELRECV15MININTREJECTUNRECIDRETRES  181
	{ (u_char) TCAPOMDEVELRECV15MININTREJECTUNRECIDRETRES, ASN_GAUGE, RONLY, var_tcapOmDevelRecv15minIntTable, 6, { 1, 1, 5, 4, 1, 8} },
#define   TCAPOMDEVELRECV15MININTREJECTUNEXPRETRES  182
	{ (u_char) TCAPOMDEVELRECV15MININTREJECTUNEXPRETRES, ASN_GAUGE, RONLY, var_tcapOmDevelRecv15minIntTable, 6, { 1, 1, 5, 4, 1, 9} },
#define   TCAPOMDEVELRECV15MININTREJECTUNRECIDRETERR  183
	{ (u_char) TCAPOMDEVELRECV15MININTREJECTUNRECIDRETERR, ASN_GAUGE, RONLY, var_tcapOmDevelRecv15minIntTable, 6, { 1, 1, 5, 4, 1, 10} },
#define   TCAPOMDEVELRECV15MININTREJECTUNEXPRETERR  184
	{ (u_char) TCAPOMDEVELRECV15MININTREJECTUNEXPRETERR, ASN_GAUGE, RONLY, var_tcapOmDevelRecv15minIntTable, 6, { 1, 1, 5, 4, 1, 11} },
#define   TCAPOMDEVELRECV15MININTUSRREJDUPINVOKEID  185
	{ (u_char) TCAPOMDEVELRECV15MININTUSRREJDUPINVOKEID, ASN_GAUGE, RONLY, var_tcapOmDevelRecv15minIntTable, 6, { 1, 1, 5, 4, 1, 12} },
#define   TCAPOMDEVELRECV15MININTUSRREJUNRECOPER  186
	{ (u_char) TCAPOMDEVELRECV15MININTUSRREJUNRECOPER, ASN_GAUGE, RONLY, var_tcapOmDevelRecv15minIntTable, 6, { 1, 1, 5, 4, 1, 13} },
#define   TCAPOMDEVELRECV15MININTUSRREJPARMINVOKE  187
	{ (u_char) TCAPOMDEVELRECV15MININTUSRREJPARMINVOKE, ASN_GAUGE, RONLY, var_tcapOmDevelRecv15minIntTable, 6, { 1, 1, 5, 4, 1, 14} },
#define   TCAPOMDEVELRECV15MININTUSRREJRELEASING  188
	{ (u_char) TCAPOMDEVELRECV15MININTUSRREJRELEASING, ASN_GAUGE, RONLY, var_tcapOmDevelRecv15minIntTable, 6, { 1, 1, 5, 4, 1, 15} },
#define   TCAPOMDEVELRECV15MININTUSRREJUNEXPLINKRESP  189
	{ (u_char) TCAPOMDEVELRECV15MININTUSRREJUNEXPLINKRESP, ASN_GAUGE, RONLY, var_tcapOmDevelRecv15minIntTable, 6, { 1, 1, 5, 4, 1, 16} },
#define   TCAPOMDEVELRECV15MININTUSRREJUNEXPLINKOPER  190
	{ (u_char) TCAPOMDEVELRECV15MININTUSRREJUNEXPLINKOPER, ASN_GAUGE, RONLY, var_tcapOmDevelRecv15minIntTable, 6, { 1, 1, 5, 4, 1, 17} },
#define   TCAPOMDEVELRECV15MININTUSRREJUNRECERROR  191
	{ (u_char) TCAPOMDEVELRECV15MININTUSRREJUNRECERROR, ASN_GAUGE, RONLY, var_tcapOmDevelRecv15minIntTable, 6, { 1, 1, 5, 4, 1, 18} },
#define   TCAPOMDEVELRECV15MININTUSRREJUNEXPERROR  192
	{ (u_char) TCAPOMDEVELRECV15MININTUSRREJUNEXPERROR, ASN_GAUGE, RONLY, var_tcapOmDevelRecv15minIntTable, 6, { 1, 1, 5, 4, 1, 19} },
#define   TCAPOMDEVELRECV15MININTUSRREJPARMRETRESULT  193
	{ (u_char) TCAPOMDEVELRECV15MININTUSRREJPARMRETRESULT, ASN_GAUGE, RONLY, var_tcapOmDevelRecv15minIntTable, 6, { 1, 1, 5, 4, 1, 20} },
#define   TCAPOMDEVELRECV15MININTUSRREJPARMRETERROR  194
	{ (u_char) TCAPOMDEVELRECV15MININTUSRREJPARMRETERROR, ASN_GAUGE, RONLY, var_tcapOmDevelRecv15minIntTable, 6, { 1, 1, 5, 4, 1, 21} },
#define   TCAPOMDEVELSENTPABORTMESSAGETYPE  195
	{ (u_char) TCAPOMDEVELSENTPABORTMESSAGETYPE, ASN_COUNTER, RONLY, var_tcapOmDevelSentTable, 6, { 1, 1, 5, 5, 1, 1} },
#define   TCAPOMDEVELSENTPABORTINCORRECTTP  196
	{ (u_char) TCAPOMDEVELSENTPABORTINCORRECTTP, ASN_COUNTER, RONLY, var_tcapOmDevelSentTable, 6, { 1, 1, 5, 5, 1, 2} },
#define   TCAPOMDEVELSENTPABORTBADTP  197
	{ (u_char) TCAPOMDEVELSENTPABORTBADTP, ASN_COUNTER, RONLY, var_tcapOmDevelSentTable, 6, { 1, 1, 5, 5, 1, 3} },
#define   TCAPOMDEVELSENTREJECTUNRECCOMP  198
	{ (u_char) TCAPOMDEVELSENTREJECTUNRECCOMP, ASN_COUNTER, RONLY, var_tcapOmDevelSentTable, 6, { 1, 1, 5, 5, 1, 4} },
#define   TCAPOMDEVELSENTREJECTMISTYPECOMP  199
	{ (u_char) TCAPOMDEVELSENTREJECTMISTYPECOMP, ASN_COUNTER, RONLY, var_tcapOmDevelSentTable, 6, { 1, 1, 5, 5, 1, 5} },
#define   TCAPOMDEVELSENTREJECTBADCOMPSTRUCT  200
	{ (u_char) TCAPOMDEVELSENTREJECTBADCOMPSTRUCT, ASN_COUNTER, RONLY, var_tcapOmDevelSentTable, 6, { 1, 1, 5, 5, 1, 6} },
#define   TCAPOMDEVELSENTREJECTUNRECLINKID  201
	{ (u_char) TCAPOMDEVELSENTREJECTUNRECLINKID, ASN_COUNTER, RONLY, var_tcapOmDevelSentTable, 6, { 1, 1, 5, 5, 1, 7} },
#define   TCAPOMDEVELSENTREJECTUNRECIDRETRES  202
	{ (u_char) TCAPOMDEVELSENTREJECTUNRECIDRETRES, ASN_COUNTER, RONLY, var_tcapOmDevelSentTable, 6, { 1, 1, 5, 5, 1, 8} },
#define   TCAPOMDEVELSENTREJECTUNEXPRETRES  203
	{ (u_char) TCAPOMDEVELSENTREJECTUNEXPRETRES, ASN_COUNTER, RONLY, var_tcapOmDevelSentTable, 6, { 1, 1, 5, 5, 1, 9} },
#define   TCAPOMDEVELSENTREJECTUNRECIDRETERR  204
	{ (u_char) TCAPOMDEVELSENTREJECTUNRECIDRETERR, ASN_COUNTER, RONLY, var_tcapOmDevelSentTable, 6, { 1, 1, 5, 5, 1, 10} },
#define   TCAPOMDEVELSENTREJECTUNEXPRETERR  205
	{ (u_char) TCAPOMDEVELSENTREJECTUNEXPRETERR, ASN_COUNTER, RONLY, var_tcapOmDevelSentTable, 6, { 1, 1, 5, 5, 1, 11} },
#define   TCAPOMDEVELSENTUSRREJDUPINVOKEID  206
	{ (u_char) TCAPOMDEVELSENTUSRREJDUPINVOKEID, ASN_COUNTER, RONLY, var_tcapOmDevelSentTable, 6, { 1, 1, 5, 5, 1, 12} },
#define   TCAPOMDEVELSENTUSRREJUNRECOPER  207
	{ (u_char) TCAPOMDEVELSENTUSRREJUNRECOPER, ASN_COUNTER, RONLY, var_tcapOmDevelSentTable, 6, { 1, 1, 5, 5, 1, 13} },
#define   TCAPOMDEVELSENTUSRREJPARMINVOKE  208
	{ (u_char) TCAPOMDEVELSENTUSRREJPARMINVOKE, ASN_COUNTER, RONLY, var_tcapOmDevelSentTable, 6, { 1, 1, 5, 5, 1, 14} },
#define   TCAPOMDEVELSENTUSRREJRELEASING  209
	{ (u_char) TCAPOMDEVELSENTUSRREJRELEASING, ASN_COUNTER, RONLY, var_tcapOmDevelSentTable, 6, { 1, 1, 5, 5, 1, 15} },
#define   TCAPOMDEVELSENTUSRREJUNEXPLINKRESP  210
	{ (u_char) TCAPOMDEVELSENTUSRREJUNEXPLINKRESP, ASN_COUNTER, RONLY, var_tcapOmDevelSentTable, 6, { 1, 1, 5, 5, 1, 16} },
#define   TCAPOMDEVELSENTUSRREJUNEXPLINKOPER  211
	{ (u_char) TCAPOMDEVELSENTUSRREJUNEXPLINKOPER, ASN_COUNTER, RONLY, var_tcapOmDevelSentTable, 6, { 1, 1, 5, 5, 1, 17} },
#define   TCAPOMDEVELSENTUSRREJUNRECERROR  212
	{ (u_char) TCAPOMDEVELSENTUSRREJUNRECERROR, ASN_COUNTER, RONLY, var_tcapOmDevelSentTable, 6, { 1, 1, 5, 5, 1, 18} },
#define   TCAPOMDEVELSENTUSRREJUNEXPERROR  213
	{ (u_char) TCAPOMDEVELSENTUSRREJUNEXPERROR, ASN_COUNTER, RONLY, var_tcapOmDevelSentTable, 6, { 1, 1, 5, 5, 1, 19} },
#define   TCAPOMDEVELSENTUSRREJPARMRETRESULT  214
	{ (u_char) TCAPOMDEVELSENTUSRREJPARMRETRESULT, ASN_COUNTER, RONLY, var_tcapOmDevelSentTable, 6, { 1, 1, 5, 5, 1, 20} },
#define   TCAPOMDEVELSENTUSRREJPARMRETERROR  215
	{ (u_char) TCAPOMDEVELSENTUSRREJPARMRETERROR, ASN_COUNTER, RONLY, var_tcapOmDevelSentTable, 6, { 1, 1, 5, 5, 1, 21} },
#define   TCAPOMDEVELSENT5MINVALIDINTERVALS  216
	{ (u_char) TCAPOMDEVELSENT5MINVALIDINTERVALS, ASN_INTEGER, RONLY, var_tcapOmDevelSentTable, 6, { 1, 1, 5, 5, 1, 22} },
#define   TCAPOMDEVELSENT15MINVALIDINTERVALS  217
	{ (u_char) TCAPOMDEVELSENT15MINVALIDINTERVALS, ASN_INTEGER, RONLY, var_tcapOmDevelSentTable, 6, { 1, 1, 5, 5, 1, 23} },
#define   TCAPOMDEVELSENTCURRENTPABORTMESSAGETYPE  218
	{ (u_char) TCAPOMDEVELSENTCURRENTPABORTMESSAGETYPE, ASN_GAUGE, RONLY, var_tcapOmDevelSentCurrentTable, 6, { 1, 1, 5, 6, 1, 1} },
#define   TCAPOMDEVELSENTCURRENTPABORTINCORRECTTP  219
	{ (u_char) TCAPOMDEVELSENTCURRENTPABORTINCORRECTTP, ASN_GAUGE, RONLY, var_tcapOmDevelSentCurrentTable, 6, { 1, 1, 5, 6, 1, 2} },
#define   TCAPOMDEVELSENTCURRENTPABORTBADTP  220
	{ (u_char) TCAPOMDEVELSENTCURRENTPABORTBADTP, ASN_GAUGE, RONLY, var_tcapOmDevelSentCurrentTable, 6, { 1, 1, 5, 6, 1, 3} },
#define   TCAPOMDEVELSENTCURRENTREJECTUNRECCOMP  221
	{ (u_char) TCAPOMDEVELSENTCURRENTREJECTUNRECCOMP, ASN_GAUGE, RONLY, var_tcapOmDevelSentCurrentTable, 6, { 1, 1, 5, 6, 1, 4} },
#define   TCAPOMDEVELSENTCURRENTREJECTMISTYPECOMP  222
	{ (u_char) TCAPOMDEVELSENTCURRENTREJECTMISTYPECOMP, ASN_GAUGE, RONLY, var_tcapOmDevelSentCurrentTable, 6, { 1, 1, 5, 6, 1, 5} },
#define   TCAPOMDEVELSENTCURRENTREJECTBADCOMPSTRUCT  223
	{ (u_char) TCAPOMDEVELSENTCURRENTREJECTBADCOMPSTRUCT, ASN_GAUGE, RONLY, var_tcapOmDevelSentCurrentTable, 6, { 1, 1, 5, 6, 1, 6} },
#define   TCAPOMDEVELSENTCURRENTREJECTUNRECLINKID  224
	{ (u_char) TCAPOMDEVELSENTCURRENTREJECTUNRECLINKID, ASN_GAUGE, RONLY, var_tcapOmDevelSentCurrentTable, 6, { 1, 1, 5, 6, 1, 7} },
#define   TCAPOMDEVELSENTCURRENTREJECTUNRECIDRETRES  225
	{ (u_char) TCAPOMDEVELSENTCURRENTREJECTUNRECIDRETRES, ASN_GAUGE, RONLY, var_tcapOmDevelSentCurrentTable, 6, { 1, 1, 5, 6, 1, 8} },
#define   TCAPOMDEVELSENTCURRENTREJECTUNEXPRETRES  226
	{ (u_char) TCAPOMDEVELSENTCURRENTREJECTUNEXPRETRES, ASN_GAUGE, RONLY, var_tcapOmDevelSentCurrentTable, 6, { 1, 1, 5, 6, 1, 9} },
#define   TCAPOMDEVELSENTCURRENTREJECTUNRECIDRETERR  227
	{ (u_char) TCAPOMDEVELSENTCURRENTREJECTUNRECIDRETERR, ASN_GAUGE, RONLY, var_tcapOmDevelSentCurrentTable, 6, { 1, 1, 5, 6, 1, 10} },
#define   TCAPOMDEVELSENTCURRENTREJECTUNEXPRETERR  228
	{ (u_char) TCAPOMDEVELSENTCURRENTREJECTUNEXPRETERR, ASN_GAUGE, RONLY, var_tcapOmDevelSentCurrentTable, 6, { 1, 1, 5, 6, 1, 11} },
#define   TCAPOMDEVELSENTCURRENTUSRREJDUPINVOKEID  229
	{ (u_char) TCAPOMDEVELSENTCURRENTUSRREJDUPINVOKEID, ASN_GAUGE, RONLY, var_tcapOmDevelSentCurrentTable, 6, { 1, 1, 5, 6, 1, 12} },
#define   TCAPOMDEVELSENTCURRENTUSRREJUNRECOPER  230
	{ (u_char) TCAPOMDEVELSENTCURRENTUSRREJUNRECOPER, ASN_GAUGE, RONLY, var_tcapOmDevelSentCurrentTable, 6, { 1, 1, 5, 6, 1, 13} },
#define   TCAPOMDEVELSENTCURRENTUSRREJPARMINVOKE  231
	{ (u_char) TCAPOMDEVELSENTCURRENTUSRREJPARMINVOKE, ASN_GAUGE, RONLY, var_tcapOmDevelSentCurrentTable, 6, { 1, 1, 5, 6, 1, 14} },
#define   TCAPOMDEVELSENTCURRENTUSRREJRELEASING  232
	{ (u_char) TCAPOMDEVELSENTCURRENTUSRREJRELEASING, ASN_GAUGE, RONLY, var_tcapOmDevelSentCurrentTable, 6, { 1, 1, 5, 6, 1, 15} },
#define   TCAPOMDEVELSENTCURRENTUSRREJUNEXPLINKRESP  233
	{ (u_char) TCAPOMDEVELSENTCURRENTUSRREJUNEXPLINKRESP, ASN_GAUGE, RONLY, var_tcapOmDevelSentCurrentTable, 6, { 1, 1, 5, 6, 1, 16} },
#define   TCAPOMDEVELSENTCURRENTUSRREJUNEXPLINKOPER  234
	{ (u_char) TCAPOMDEVELSENTCURRENTUSRREJUNEXPLINKOPER, ASN_GAUGE, RONLY, var_tcapOmDevelSentCurrentTable, 6, { 1, 1, 5, 6, 1, 17} },
#define   TCAPOMDEVELSENTCURRENTUSRREJUNRECERROR  235
	{ (u_char) TCAPOMDEVELSENTCURRENTUSRREJUNRECERROR, ASN_GAUGE, RONLY, var_tcapOmDevelSentCurrentTable, 6, { 1, 1, 5, 6, 1, 18} },
#define   TCAPOMDEVELSENTCURRENTUSRREJUNEXPERROR  236
	{ (u_char) TCAPOMDEVELSENTCURRENTUSRREJUNEXPERROR, ASN_GAUGE, RONLY, var_tcapOmDevelSentCurrentTable, 6, { 1, 1, 5, 6, 1, 19} },
#define   TCAPOMDEVELSENTCURRENTUSRREJPARMRETRESULT  237
	{ (u_char) TCAPOMDEVELSENTCURRENTUSRREJPARMRETRESULT, ASN_GAUGE, RONLY, var_tcapOmDevelSentCurrentTable, 6, { 1, 1, 5, 6, 1, 20} },
#define   TCAPOMDEVELSENTCURRENTUSRREJPARMRETERROR  238
	{ (u_char) TCAPOMDEVELSENTCURRENTUSRREJPARMRETERROR, ASN_GAUGE, RONLY, var_tcapOmDevelSentCurrentTable, 6, { 1, 1, 5, 6, 1, 21} },
#define   TCAPOMDEVELSENT5MININTPABORTMESSAGETYPE  239
	{ (u_char) TCAPOMDEVELSENT5MININTPABORTMESSAGETYPE, ASN_GAUGE, RONLY, var_tcapOmDevelSent5minIntTable, 6, { 1, 1, 5, 7, 1, 1} },
#define   TCAPOMDEVELSENT5MININTPABORTINCORRECTTP  240
	{ (u_char) TCAPOMDEVELSENT5MININTPABORTINCORRECTTP, ASN_GAUGE, RONLY, var_tcapOmDevelSent5minIntTable, 6, { 1, 1, 5, 7, 1, 2} },
#define   TCAPOMDEVELSENT5MININTPABORTBADTP  241
	{ (u_char) TCAPOMDEVELSENT5MININTPABORTBADTP, ASN_GAUGE, RONLY, var_tcapOmDevelSent5minIntTable, 6, { 1, 1, 5, 7, 1, 3} },
#define   TCAPOMDEVELSENT5MININTREJECTUNRECCOMP  242
	{ (u_char) TCAPOMDEVELSENT5MININTREJECTUNRECCOMP, ASN_GAUGE, RONLY, var_tcapOmDevelSent5minIntTable, 6, { 1, 1, 5, 7, 1, 4} },
#define   TCAPOMDEVELSENT5MININTREJECTMISTYPECOMP  243
	{ (u_char) TCAPOMDEVELSENT5MININTREJECTMISTYPECOMP, ASN_GAUGE, RONLY, var_tcapOmDevelSent5minIntTable, 6, { 1, 1, 5, 7, 1, 5} },
#define   TCAPOMDEVELSENT5MININTREJECTBADCOMPSTRUCT  244
	{ (u_char) TCAPOMDEVELSENT5MININTREJECTBADCOMPSTRUCT, ASN_GAUGE, RONLY, var_tcapOmDevelSent5minIntTable, 6, { 1, 1, 5, 7, 1, 6} },
#define   TCAPOMDEVELSENT5MININTREJECTUNRECLINKID  245
	{ (u_char) TCAPOMDEVELSENT5MININTREJECTUNRECLINKID, ASN_GAUGE, RONLY, var_tcapOmDevelSent5minIntTable, 6, { 1, 1, 5, 7, 1, 7} },
#define   TCAPOMDEVELSENT5MININTREJECTUNRECIDRETRES  246
	{ (u_char) TCAPOMDEVELSENT5MININTREJECTUNRECIDRETRES, ASN_GAUGE, RONLY, var_tcapOmDevelSent5minIntTable, 6, { 1, 1, 5, 7, 1, 8} },
#define   TCAPOMDEVELSENT5MININTREJECTUNEXPRETRES  247
	{ (u_char) TCAPOMDEVELSENT5MININTREJECTUNEXPRETRES, ASN_GAUGE, RONLY, var_tcapOmDevelSent5minIntTable, 6, { 1, 1, 5, 7, 1, 9} },
#define   TCAPOMDEVELSENT5MININTREJECTUNRECIDRETERR  248
	{ (u_char) TCAPOMDEVELSENT5MININTREJECTUNRECIDRETERR, ASN_GAUGE, RONLY, var_tcapOmDevelSent5minIntTable, 6, { 1, 1, 5, 7, 1, 10} },
#define   TCAPOMDEVELSENT5MININTREJECTUNEXPRETERR  249
	{ (u_char) TCAPOMDEVELSENT5MININTREJECTUNEXPRETERR, ASN_GAUGE, RONLY, var_tcapOmDevelSent5minIntTable, 6, { 1, 1, 5, 7, 1, 11} },
#define   TCAPOMDEVELSENT5MININTUSRREJDUPINVOKEID  250
	{ (u_char) TCAPOMDEVELSENT5MININTUSRREJDUPINVOKEID, ASN_GAUGE, RONLY, var_tcapOmDevelSent5minIntTable, 6, { 1, 1, 5, 7, 1, 12} },
#define   TCAPOMDEVELSENT5MININTUSRREJUNRECOPER  251
	{ (u_char) TCAPOMDEVELSENT5MININTUSRREJUNRECOPER, ASN_GAUGE, RONLY, var_tcapOmDevelSent5minIntTable, 6, { 1, 1, 5, 7, 1, 13} },
#define   TCAPOMDEVELSENT5MININTUSRREJPARMINVOKE  252
	{ (u_char) TCAPOMDEVELSENT5MININTUSRREJPARMINVOKE, ASN_GAUGE, RONLY, var_tcapOmDevelSent5minIntTable, 6, { 1, 1, 5, 7, 1, 14} },
#define   TCAPOMDEVELSENT5MININTUSRREJRELEASING  253
	{ (u_char) TCAPOMDEVELSENT5MININTUSRREJRELEASING, ASN_GAUGE, RONLY, var_tcapOmDevelSent5minIntTable, 6, { 1, 1, 5, 7, 1, 15} },
#define   TCAPOMDEVELSENT5MININTUSRREJUNEXPLINKRESP  254
	{ (u_char) TCAPOMDEVELSENT5MININTUSRREJUNEXPLINKRESP, ASN_GAUGE, RONLY, var_tcapOmDevelSent5minIntTable, 6, { 1, 1, 5, 7, 1, 16} },
#define   TCAPOMDEVELSENT5MININTUSRREJUNEXPLINKOPER  255
	{ (u_char) TCAPOMDEVELSENT5MININTUSRREJUNEXPLINKOPER, ASN_GAUGE, RONLY, var_tcapOmDevelSent5minIntTable, 6, { 1, 1, 5, 7, 1, 17} },
#define   TCAPOMDEVELSENT5MININTUSRREJUNRECERROR  256
	{ (u_char) TCAPOMDEVELSENT5MININTUSRREJUNRECERROR, ASN_GAUGE, RONLY, var_tcapOmDevelSent5minIntTable, 6, { 1, 1, 5, 7, 1, 18} },
#define   TCAPOMDEVELSENT5MININTUSRREJUNEXPERROR  257
	{ (u_char) TCAPOMDEVELSENT5MININTUSRREJUNEXPERROR, ASN_GAUGE, RONLY, var_tcapOmDevelSent5minIntTable, 6, { 1, 1, 5, 7, 1, 19} },
#define   TCAPOMDEVELSENT5MININTUSRREJPARMRETRESULT  258
	{ (u_char) TCAPOMDEVELSENT5MININTUSRREJPARMRETRESULT, ASN_GAUGE, RONLY, var_tcapOmDevelSent5minIntTable, 6, { 1, 1, 5, 7, 1, 20} },
#define   TCAPOMDEVELSENT5MININTUSRREJPARMRETERROR  259
	{ (u_char) TCAPOMDEVELSENT5MININTUSRREJPARMRETERROR, ASN_GAUGE, RONLY, var_tcapOmDevelSent5minIntTable, 6, { 1, 1, 5, 7, 1, 21} },
#define   TCAPOMDEVELSENT15MININTPABORTMESSAGETYPE  260
	{ (u_char) TCAPOMDEVELSENT15MININTPABORTMESSAGETYPE, ASN_GAUGE, RONLY, var_tcapOmDevelSent15minIntTable, 6, { 1, 1, 5, 8, 1, 1} },
#define   TCAPOMDEVELSENT15MININTPABORTINCORRECTTP  261
	{ (u_char) TCAPOMDEVELSENT15MININTPABORTINCORRECTTP, ASN_GAUGE, RONLY, var_tcapOmDevelSent15minIntTable, 6, { 1, 1, 5, 8, 1, 2} },
#define   TCAPOMDEVELSENT15MININTPABORTBADTP  262
	{ (u_char) TCAPOMDEVELSENT15MININTPABORTBADTP, ASN_GAUGE, RONLY, var_tcapOmDevelSent15minIntTable, 6, { 1, 1, 5, 8, 1, 3} },
#define   TCAPOMDEVELSENT15MININTREJECTUNRECCOMP  263
	{ (u_char) TCAPOMDEVELSENT15MININTREJECTUNRECCOMP, ASN_GAUGE, RONLY, var_tcapOmDevelSent15minIntTable, 6, { 1, 1, 5, 8, 1, 4} },
#define   TCAPOMDEVELSENT15MININTREJECTMISTYPECOMP  264
	{ (u_char) TCAPOMDEVELSENT15MININTREJECTMISTYPECOMP, ASN_GAUGE, RONLY, var_tcapOmDevelSent15minIntTable, 6, { 1, 1, 5, 8, 1, 5} },
#define   TCAPOMDEVELSENT15MININTREJECTBADCOMPSTRUCT  265
	{ (u_char) TCAPOMDEVELSENT15MININTREJECTBADCOMPSTRUCT, ASN_GAUGE, RONLY, var_tcapOmDevelSent15minIntTable, 6, { 1, 1, 5, 8, 1, 6} },
#define   TCAPOMDEVELSENT15MININTREJECTUNRECLINKID  266
	{ (u_char) TCAPOMDEVELSENT15MININTREJECTUNRECLINKID, ASN_GAUGE, RONLY, var_tcapOmDevelSent15minIntTable, 6, { 1, 1, 5, 8, 1, 7} },
#define   TCAPOMDEVELSENT15MININTREJECTUNRECIDRETRES  267
	{ (u_char) TCAPOMDEVELSENT15MININTREJECTUNRECIDRETRES, ASN_GAUGE, RONLY, var_tcapOmDevelSent15minIntTable, 6, { 1, 1, 5, 8, 1, 8} },
#define   TCAPOMDEVELSENT15MININTREJECTUNEXPRETRES  268
	{ (u_char) TCAPOMDEVELSENT15MININTREJECTUNEXPRETRES, ASN_GAUGE, RONLY, var_tcapOmDevelSent15minIntTable, 6, { 1, 1, 5, 8, 1, 9} },
#define   TCAPOMDEVELSENT15MININTREJECTUNRECIDRETERR  269
	{ (u_char) TCAPOMDEVELSENT15MININTREJECTUNRECIDRETERR, ASN_GAUGE, RONLY, var_tcapOmDevelSent15minIntTable, 6, { 1, 1, 5, 8, 1, 10} },
#define   TCAPOMDEVELSENT15MININTREJECTUNEXPRETERR  270
	{ (u_char) TCAPOMDEVELSENT15MININTREJECTUNEXPRETERR, ASN_GAUGE, RONLY, var_tcapOmDevelSent15minIntTable, 6, { 1, 1, 5, 8, 1, 11} },
#define   TCAPOMDEVELSENT15MININTUSRREJDUPINVOKEID  271
	{ (u_char) TCAPOMDEVELSENT15MININTUSRREJDUPINVOKEID, ASN_GAUGE, RONLY, var_tcapOmDevelSent15minIntTable, 6, { 1, 1, 5, 8, 1, 12} },
#define   TCAPOMDEVELSENT15MININTUSRREJUNRECOPER  272
	{ (u_char) TCAPOMDEVELSENT15MININTUSRREJUNRECOPER, ASN_GAUGE, RONLY, var_tcapOmDevelSent15minIntTable, 6, { 1, 1, 5, 8, 1, 13} },
#define   TCAPOMDEVELSENT15MININTUSRREJPARMINVOKE  273
	{ (u_char) TCAPOMDEVELSENT15MININTUSRREJPARMINVOKE, ASN_GAUGE, RONLY, var_tcapOmDevelSent15minIntTable, 6, { 1, 1, 5, 8, 1, 14} },
#define   TCAPOMDEVELSENT15MININTUSRREJRELEASING  274
	{ (u_char) TCAPOMDEVELSENT15MININTUSRREJRELEASING, ASN_GAUGE, RONLY, var_tcapOmDevelSent15minIntTable, 6, { 1, 1, 5, 8, 1, 15} },
#define   TCAPOMDEVELSENT15MININTUSRREJUNEXPLINKRESP  275
	{ (u_char) TCAPOMDEVELSENT15MININTUSRREJUNEXPLINKRESP, ASN_GAUGE, RONLY, var_tcapOmDevelSent15minIntTable, 6, { 1, 1, 5, 8, 1, 16} },
#define   TCAPOMDEVELSENT15MININTUSRREJUNEXPLINKOPER  276
	{ (u_char) TCAPOMDEVELSENT15MININTUSRREJUNEXPLINKOPER, ASN_GAUGE, RONLY, var_tcapOmDevelSent15minIntTable, 6, { 1, 1, 5, 8, 1, 17} },
#define   TCAPOMDEVELSENT15MININTUSRREJUNRECERROR  277
	{ (u_char) TCAPOMDEVELSENT15MININTUSRREJUNRECERROR, ASN_GAUGE, RONLY, var_tcapOmDevelSent15minIntTable, 6, { 1, 1, 5, 8, 1, 18} },
#define   TCAPOMDEVELSENT15MININTUSRREJUNEXPERROR  278
	{ (u_char) TCAPOMDEVELSENT15MININTUSRREJUNEXPERROR, ASN_GAUGE, RONLY, var_tcapOmDevelSent15minIntTable, 6, { 1, 1, 5, 8, 1, 19} },
#define   TCAPOMDEVELSENT15MININTUSRREJPARMRETRESULT  279
	{ (u_char) TCAPOMDEVELSENT15MININTUSRREJPARMRETRESULT, ASN_GAUGE, RONLY, var_tcapOmDevelSent15minIntTable, 6, { 1, 1, 5, 8, 1, 20} },
#define   TCAPOMDEVELSENT15MININTUSRREJPARMRETERROR  280
	{ (u_char) TCAPOMDEVELSENT15MININTUSRREJPARMRETERROR, ASN_GAUGE, RONLY, var_tcapOmDevelSent15minIntTable, 6, { 1, 1, 5, 8, 1, 21} },
#define   TCAPOM1STANDINTERVALACTIVATE  281
	{ (u_char) TCAPOM1STANDINTERVALACTIVATE, ASN_OBJECT_ID, RWRITE, var_tcapOmMIB, 3, { 1, 2, 1} },
#define   TCAPOM1STANDINTERVALDEACTIVATE  282
	{ (u_char) TCAPOM1STANDINTERVALDEACTIVATE, ASN_OBJECT_ID, RWRITE, var_tcapOmMIB, 3, { 1, 2, 2} },
#define   TCAPOM5MINACTIVATE    283
	{ (u_char) TCAPOM5MINACTIVATE, ASN_OBJECT_ID, RWRITE, var_tcapOmMIB, 3, { 1, 2, 3} },
#define   TCAPOM5MINDEAACTIVATE  284
	{ (u_char) TCAPOM5MINDEAACTIVATE, ASN_OBJECT_ID, RWRITE, var_tcapOmMIB, 3, { 1, 2, 4} },
#define   TCAPOM15MINACTIVATE   285
	{ (u_char) TCAPOM15MINACTIVATE, ASN_OBJECT_ID, RWRITE, var_tcapOmMIB, 3, { 1, 2, 5} },
#define   TCAPOM15MINDEAACTIVATE  286
	{ (u_char) TCAPOM15MINDEAACTIVATE, ASN_OBJECT_ID, RWRITE, var_tcapOmMIB, 3, { 1, 2, 6} },
#define   TCAPOMDISCONTINUITYTIME  287
	{ (u_char) TCAPOMDISCONTINUITYTIME, ASN_TIMETICKS, RONLY, var_tcapOmMIB, 3, { 1, 2, 8} },
#define   TCAPOMTIMESTAMP       288
	{ (u_char) TCAPOMTIMESTAMP, ASN_TIMETICKS, RONLY, var_tcapOmMIB, 3, { 1, 2, 9} },
#define   TCAPOM5MINMAXINTERVALS  289
	{ (u_char) TCAPOM5MINMAXINTERVALS, ASN_UNSIGNED, RWRITE, var_tcapOmMIB, 3, { 1, 2, 10} },
#define   TCAPOM15MINMAXINTERVALS  290
	{ (u_char) TCAPOM15MINMAXINTERVALS, ASN_UNSIGNED, RWRITE, var_tcapOmMIB, 3, { 1, 2, 11} },
};

/* (L = length of the oidsuffix) */
struct tcapOmMIB_data *tcapOmMIBStorage = NULL;

/* global storage of our data, saved in and configured by header_complex() */
struct header_complex_index *tcapOmNodeTableStorage = NULL;
struct header_complex_index *tcapOmNodeCurrentTableStorage = NULL;
struct header_complex_index *tcapOmNode5minIntTableStorage = NULL;
struct header_complex_index *tcapOmNode15minIntTableStorage = NULL;
struct header_complex_index *tcapOmMsgsTableStorage = NULL;
struct header_complex_index *tcapOmMsgsCurrentTableStorage = NULL;
struct header_complex_index *tcapOmMsgs5minIntTableStorage = NULL;
struct header_complex_index *tcapOmMsgs15minIntTableStorage = NULL;
struct header_complex_index *tcapOmAETableStorage = NULL;
struct header_complex_index *tcapOmAECurrentTableStorage = NULL;
struct header_complex_index *tcapOmAE5minIntTableStorage = NULL;
struct header_complex_index *tcapOmAE15minIntTableStorage = NULL;
struct header_complex_index *tcapOmTcTableStorage = NULL;
struct header_complex_index *tcapOmTcCurrentTableStorage = NULL;
struct header_complex_index *tcapOmTc5minIntTableStorage = NULL;
struct header_complex_index *tcapOmTc15minIntTableStorage = NULL;
struct header_complex_index *tcapOmDevelRecvTableStorage = NULL;
struct header_complex_index *tcapOmDevelRecvCurrentTableStorage = NULL;
struct header_complex_index *tcapOmDevelRecv5minIntTableStorage = NULL;
struct header_complex_index *tcapOmDevelRecv15minIntTableStorage = NULL;
struct header_complex_index *tcapOmDevelSentTableStorage = NULL;
struct header_complex_index *tcapOmDevelSentCurrentTableStorage = NULL;
struct header_complex_index *tcapOmDevelSent5minIntTableStorage = NULL;
struct header_complex_index *tcapOmDevelSent15minIntTableStorage = NULL;

void (*tcapOmMIBold_signal_handler)(int) = NULL;	/* save old signal handler just in case */
void tcapOmMIB_loop_handler(int);
void tcapOmMIB_fd_handler(int, void *);

/**
 * @fn void init_tcapOmMIB(void)
 * @brief tcapOmMIB initialization routine.
 *
 * This is called when the agent starts up.  At a minimum, registration of the MIB variables
 * structure (tcapOmMIB_variables) should take place here.  By default the function also
 * registers the configuration handler and configuration store callbacks.
 *
 * Additional registrations that may be considered here are calls to regsiter_readfd(),
 * register_writefd() and register_exceptfd() for hooking into the snmpd event loop, but only when
 * used as a loadable module.  By default this function establishes a single file descriptor to
 * read, or upon which to handle exceptions.  Note that the snmpd only supports a maximum of 32
 * extneral file descriptors, so these should be used sparingly.
 *
 * When running as a loadable module, it is also necessary to hook into the snmpd event loop so that
 * the current request number can be deteremined.  This is accomplished by using a trick of the
 * external_signal_scheduled and external_signal_handler mechanism which is called on each event
 * loop when external_signal_scheduled is non-zero.  This is used to increment the sa_request value
 * on each snmpd event loop interation so that calls to MIB tree functions can determine whether
 * they belong to a fresh request or not (primarily for cacheing and possibly to clean up non-polled
 * file descriptors).
 */
void
init_tcapOmMIB(void)
{
	(void) my_fd;
	(void) zeroDotZero_oid;
	(void) snmpTrapOID_oid;
	DEBUGMSGTL(("tcapOmMIB", "init_tcapOmMIB: initializing...  "));
	/* register ourselves with the agent to handle our mib tree */
	REGISTER_MIB("tcapOmMIB", tcapOmMIB_variables, variable7, tcapOmMIB_variables_oid);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_SHUTDOWN, term_tcapOmMIB, NULL);
	/* register our config handler(s) to deal with registrations */
	snmpd_register_config_handler("tcapOmMIB", parse_tcapOmMIB, NULL, "HELP STRING");
	snmpd_register_config_handler("tcapOmNodeTable", parse_tcapOmNodeTable, NULL, "HELP STRING");
	snmpd_register_config_handler("tcapOmNodeCurrentTable", parse_tcapOmNodeCurrentTable, NULL, "HELP STRING");
	snmpd_register_config_handler("tcapOmNode5minIntTable", parse_tcapOmNode5minIntTable, NULL, "HELP STRING");
	snmpd_register_config_handler("tcapOmNode15minIntTable", parse_tcapOmNode15minIntTable, NULL, "HELP STRING");
	snmpd_register_config_handler("tcapOmMsgsTable", parse_tcapOmMsgsTable, NULL, "HELP STRING");
	snmpd_register_config_handler("tcapOmMsgsCurrentTable", parse_tcapOmMsgsCurrentTable, NULL, "HELP STRING");
	snmpd_register_config_handler("tcapOmMsgs5minIntTable", parse_tcapOmMsgs5minIntTable, NULL, "HELP STRING");
	snmpd_register_config_handler("tcapOmMsgs15minIntTable", parse_tcapOmMsgs15minIntTable, NULL, "HELP STRING");
	snmpd_register_config_handler("tcapOmAETable", parse_tcapOmAETable, NULL, "HELP STRING");
	snmpd_register_config_handler("tcapOmAECurrentTable", parse_tcapOmAECurrentTable, NULL, "HELP STRING");
	snmpd_register_config_handler("tcapOmAE5minIntTable", parse_tcapOmAE5minIntTable, NULL, "HELP STRING");
	snmpd_register_config_handler("tcapOmAE15minIntTable", parse_tcapOmAE15minIntTable, NULL, "HELP STRING");
	snmpd_register_config_handler("tcapOmTcTable", parse_tcapOmTcTable, NULL, "HELP STRING");
	snmpd_register_config_handler("tcapOmTcCurrentTable", parse_tcapOmTcCurrentTable, NULL, "HELP STRING");
	snmpd_register_config_handler("tcapOmTc5minIntTable", parse_tcapOmTc5minIntTable, NULL, "HELP STRING");
	snmpd_register_config_handler("tcapOmTc15minIntTable", parse_tcapOmTc15minIntTable, NULL, "HELP STRING");
	snmpd_register_config_handler("tcapOmDevelRecvTable", parse_tcapOmDevelRecvTable, NULL, "HELP STRING");
	snmpd_register_config_handler("tcapOmDevelRecvCurrentTable", parse_tcapOmDevelRecvCurrentTable, NULL, "HELP STRING");
	snmpd_register_config_handler("tcapOmDevelRecv5minIntTable", parse_tcapOmDevelRecv5minIntTable, NULL, "HELP STRING");
	snmpd_register_config_handler("tcapOmDevelRecv15minIntTable", parse_tcapOmDevelRecv15minIntTable, NULL, "HELP STRING");
	snmpd_register_config_handler("tcapOmDevelSentTable", parse_tcapOmDevelSentTable, NULL, "HELP STRING");
	snmpd_register_config_handler("tcapOmDevelSentCurrentTable", parse_tcapOmDevelSentCurrentTable, NULL, "HELP STRING");
	snmpd_register_config_handler("tcapOmDevelSent5minIntTable", parse_tcapOmDevelSent5minIntTable, NULL, "HELP STRING");
	snmpd_register_config_handler("tcapOmDevelSent15minIntTable", parse_tcapOmDevelSent15minIntTable, NULL, "HELP STRING");

	/* we need to be called back later to store our data */
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_tcapOmMIB, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_tcapOmNodeTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_tcapOmNodeCurrentTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_tcapOmNode5minIntTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_tcapOmNode15minIntTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_tcapOmMsgsTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_tcapOmMsgsCurrentTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_tcapOmMsgs5minIntTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_tcapOmMsgs15minIntTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_tcapOmAETable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_tcapOmAECurrentTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_tcapOmAE5minIntTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_tcapOmAE15minIntTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_tcapOmTcTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_tcapOmTcCurrentTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_tcapOmTc5minIntTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_tcapOmTc15minIntTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_tcapOmDevelRecvTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_tcapOmDevelRecvCurrentTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_tcapOmDevelRecv5minIntTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_tcapOmDevelRecv15minIntTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_tcapOmDevelSentTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_tcapOmDevelSentCurrentTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_tcapOmDevelSent5minIntTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_tcapOmDevelSent15minIntTable, NULL);

	/* place any other initialization junk you need here */
	if (my_readfd >= 0) {
		register_readfd(my_readfd, tcapOmMIB_fd_handler, (void *) 0);
		register_exceptfd(my_readfd, tcapOmMIB_fd_handler, (void *) 1);
	}
	tcapOmMIBold_signal_handler = external_signal_handler[SIGCHLD];
	external_signal_handler[SIGCHLD] = &tcapOmMIB_loop_handler;
	external_signal_scheduled[SIGCHLD] = 1;
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
}

/**
 * @fn void deinit_tcapOmMIB(void)
 * @brief deinitialization routine.
 *
 * This is called before the agent is unloaded.  At a minimum, deregistration of the MIB variables
 * structure (tcapOmMIB_variables) should take place here.  By default, the function also
 * deregisters the the configuration file handlers for the MIB variables and table rows.
 *
 * Additional deregistrations that may be required here are calls to unregister_readfd(),
 * unregister_writefd() and unregsiter_exceptfd() for unhooking from the snmpd event loop, but only
 * when used as a loadable module.  By default if a read file descriptor exists, it is unregistered.
 */
void
deinit_tcapOmMIB(void)
{
	DEBUGMSGTL(("tcapOmMIB", "deinit_tcapOmMIB: deinitializating...  "));
	external_signal_handler[SIGCHLD] = tcapOmMIBold_signal_handler;
	if (my_readfd >= 0) {
		unregister_exceptfd(my_readfd);
		unregister_readfd(my_readfd);
		close(my_readfd);
		my_readfd = -1;
	}
	unregister_mib(tcapOmMIB_variables_oid, sizeof(tcapOmMIB_variables_oid) / sizeof(oid));
	snmpd_unregister_config_handler("tcapOmMIB");
	snmpd_unregister_config_handler("tcapOmNodeTable");
	snmpd_unregister_config_handler("tcapOmNodeCurrentTable");
	snmpd_unregister_config_handler("tcapOmNode5minIntTable");
	snmpd_unregister_config_handler("tcapOmNode15minIntTable");
	snmpd_unregister_config_handler("tcapOmMsgsTable");
	snmpd_unregister_config_handler("tcapOmMsgsCurrentTable");
	snmpd_unregister_config_handler("tcapOmMsgs5minIntTable");
	snmpd_unregister_config_handler("tcapOmMsgs15minIntTable");
	snmpd_unregister_config_handler("tcapOmAETable");
	snmpd_unregister_config_handler("tcapOmAECurrentTable");
	snmpd_unregister_config_handler("tcapOmAE5minIntTable");
	snmpd_unregister_config_handler("tcapOmAE15minIntTable");
	snmpd_unregister_config_handler("tcapOmTcTable");
	snmpd_unregister_config_handler("tcapOmTcCurrentTable");
	snmpd_unregister_config_handler("tcapOmTc5minIntTable");
	snmpd_unregister_config_handler("tcapOmTc15minIntTable");
	snmpd_unregister_config_handler("tcapOmDevelRecvTable");
	snmpd_unregister_config_handler("tcapOmDevelRecvCurrentTable");
	snmpd_unregister_config_handler("tcapOmDevelRecv5minIntTable");
	snmpd_unregister_config_handler("tcapOmDevelRecv15minIntTable");
	snmpd_unregister_config_handler("tcapOmDevelSentTable");
	snmpd_unregister_config_handler("tcapOmDevelSentCurrentTable");
	snmpd_unregister_config_handler("tcapOmDevelSent5minIntTable");
	snmpd_unregister_config_handler("tcapOmDevelSent15minIntTable");

	/* place any other de-initialization junk you need here */
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
}

int
term_tcapOmMIB(int majorID, int minorID, void *serverarg, void *clientarg)
{
	(void) majorID;
	(void) minorID;
	(void) serverarg;
	(void) clientarg;
	DEBUGMSGTL(("tcapOmMIB", "term_tcapOmMIB: terminating...  "));
	deinit_tcapOmMIB();
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return 0;
}

/**
 * @fn struct tcapOmMIB_data *tcapOmMIB_create(void)
 * @brief create a fresh data structure representing scalars in tcapOmMIB.
 *
 * Creates a new tcapOmMIB_data structure by allocating dynamic memory for the structure and
 * initializing the default values of scalars in tcapOmMIB.
 */
struct tcapOmMIB_data *
tcapOmMIB_create(void)
{
	struct tcapOmMIB_data *StorageNew = SNMP_MALLOC_STRUCT(tcapOmMIB_data);

	DEBUGMSGTL(("tcapOmMIB", "tcapOmMIB_create: creating scalars...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default scalar values here into StorageNew */
		if ((StorageNew->tcapOm1stAndIntervalActivate = snmp_duplicate_objid(zeroDotZero_oid, 2)) == NULL)
			goto nomem;
		StorageNew->tcapOm1stAndIntervalActivateLen = 2;
		if ((StorageNew->tcapOm1stAndIntervalDeactivate = snmp_duplicate_objid(zeroDotZero_oid, 2)) == NULL)
			goto nomem;
		StorageNew->tcapOm1stAndIntervalDeactivateLen = 2;
		if ((StorageNew->tcapOm5MinActivate = snmp_duplicate_objid(zeroDotZero_oid, 2)) == NULL)
			goto nomem;
		StorageNew->tcapOm5MinActivateLen = 2;
		if ((StorageNew->tcapOm5MinDeaActivate = snmp_duplicate_objid(zeroDotZero_oid, 2)) == NULL)
			goto nomem;
		StorageNew->tcapOm5MinDeaActivateLen = 2;
		if ((StorageNew->tcapOm15MinActivate = snmp_duplicate_objid(zeroDotZero_oid, 2)) == NULL)
			goto nomem;
		StorageNew->tcapOm15MinActivateLen = 2;
		if ((StorageNew->tcapOm15MinDeaActivate = snmp_duplicate_objid(zeroDotZero_oid, 2)) == NULL)
			goto nomem;
		StorageNew->tcapOm15MinDeaActivateLen = 2;
		StorageNew->tcapOmDiscontinuityTime = 0;
		StorageNew->tcapOmTimeStamp = 0;
		StorageNew->tcapOm5MinMaxIntervals = 96;
		StorageNew->tcapOm15MinMaxIntervals = 96;

	}
      done:
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	tcapOmMIB_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct tcapOmMIB_data *tcapOmMIB_duplicate(struct tcapOmMIB_data *thedata)
 * @param thedata the mib structure to duplicate
 * @brief duplicate a mib structure for the mib
 *
 * Duplicates the specified mib structure @param thedata and returns a pointer to the newly
 * allocated mib structure on success, or NULL on failure.
 */
struct tcapOmMIB_data *
tcapOmMIB_duplicate(struct tcapOmMIB_data *thedata)
{
	struct tcapOmMIB_data *StorageNew = SNMP_MALLOC_STRUCT(tcapOmMIB_data);

	(void) thedata;
	DEBUGMSGTL(("tcapOmMIB", "tcapOmMIB_duplicate: duplicating mib... "));
	if (StorageNew != NULL) {
		if (!(StorageNew->tcapOm1stAndIntervalActivate = snmp_duplicate_objid(thedata->tcapOm1stAndIntervalActivate, thedata->tcapOm1stAndIntervalActivateLen / sizeof(oid))))
			goto destroy;
		StorageNew->tcapOm1stAndIntervalActivateLen = thedata->tcapOm1stAndIntervalActivateLen;
		if (!(StorageNew->tcapOm1stAndIntervalDeactivate = snmp_duplicate_objid(thedata->tcapOm1stAndIntervalDeactivate, thedata->tcapOm1stAndIntervalDeactivateLen / sizeof(oid))))
			goto destroy;
		StorageNew->tcapOm1stAndIntervalDeactivateLen = thedata->tcapOm1stAndIntervalDeactivateLen;
		if (!(StorageNew->tcapOm5MinActivate = snmp_duplicate_objid(thedata->tcapOm5MinActivate, thedata->tcapOm5MinActivateLen / sizeof(oid))))
			goto destroy;
		StorageNew->tcapOm5MinActivateLen = thedata->tcapOm5MinActivateLen;
		if (!(StorageNew->tcapOm5MinDeaActivate = snmp_duplicate_objid(thedata->tcapOm5MinDeaActivate, thedata->tcapOm5MinDeaActivateLen / sizeof(oid))))
			goto destroy;
		StorageNew->tcapOm5MinDeaActivateLen = thedata->tcapOm5MinDeaActivateLen;
		if (!(StorageNew->tcapOm15MinActivate = snmp_duplicate_objid(thedata->tcapOm15MinActivate, thedata->tcapOm15MinActivateLen / sizeof(oid))))
			goto destroy;
		StorageNew->tcapOm15MinActivateLen = thedata->tcapOm15MinActivateLen;
		if (!(StorageNew->tcapOm15MinDeaActivate = snmp_duplicate_objid(thedata->tcapOm15MinDeaActivate, thedata->tcapOm15MinDeaActivateLen / sizeof(oid))))
			goto destroy;
		StorageNew->tcapOm15MinDeaActivateLen = thedata->tcapOm15MinDeaActivateLen;
		StorageNew->tcapOmDiscontinuityTime = thedata->tcapOmDiscontinuityTime;
		StorageNew->tcapOmTimeStamp = thedata->tcapOmTimeStamp;
		StorageNew->tcapOm5MinMaxIntervals = thedata->tcapOm5MinMaxIntervals;
		StorageNew->tcapOm15MinMaxIntervals = thedata->tcapOm15MinMaxIntervals;
	}
      done:
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	tcapOmMIB_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int tcapOmMIB_destroy(struct tcapOmMIB_data **thedata)
 * @param thedata pointer to the data structure in tcapOmMIB.
 * @brief delete a scalars structure from tcapOmMIB.
 *
 * Frees scalars that were previously removed from tcapOmMIB.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
tcapOmMIB_destroy(struct tcapOmMIB_data **thedata)
{
	struct tcapOmMIB_data *StorageDel;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmMIB_destroy: deleting scalars...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->tcapOm1stAndIntervalActivate);
		StorageDel->tcapOm1stAndIntervalActivateLen = 0;
		SNMP_FREE(StorageDel->tcapOm1stAndIntervalDeactivate);
		StorageDel->tcapOm1stAndIntervalDeactivateLen = 0;
		SNMP_FREE(StorageDel->tcapOm5MinActivate);
		StorageDel->tcapOm5MinActivateLen = 0;
		SNMP_FREE(StorageDel->tcapOm5MinDeaActivate);
		StorageDel->tcapOm5MinDeaActivateLen = 0;
		SNMP_FREE(StorageDel->tcapOm15MinActivate);
		StorageDel->tcapOm15MinActivateLen = 0;
		SNMP_FREE(StorageDel->tcapOm15MinDeaActivate);
		StorageDel->tcapOm15MinDeaActivateLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int tcapOmMIB_add(struct tcapOmMIB_data *thedata)
 * @param thedata the structure representing tcapOmMIB scalars.
 * @brief adds node to the tcapOmMIB scalar data set.
 *
 * Adds a scalar structure to the tcapOmMIB data set.  Note that this function is necessary even
 * when the scalar values are not peristent.
 */
int
tcapOmMIB_add(struct tcapOmMIB_data *thedata)
{
	DEBUGMSGTL(("tcapOmMIB", "tcapOmMIB_add: adding data...  "));
	if (thedata)
		tcapOmMIBStorage = thedata;
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_tcapOmMIB(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for tcapOmMIB entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case tcapOmMIB).  This routine is invoked by
 * UCD-SNMP to read the values of scalars in the MIB from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the MIB.  If there are no configured entries
 * in the configuration MIB, this function will simply not be called.
 */
void
parse_tcapOmMIB(const char *token, char *line)
{
	(void) token;
	(void) line;
	size_t tmpsize;
	struct tcapOmMIB_data *StorageTmp = tcapOmMIB_create();

	DEBUGMSGTL(("tcapOmMIB", "parse_tcapOmMIB: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual scalars that are not persistent */
	SNMP_FREE(StorageTmp->tcapOm1stAndIntervalActivate);
	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->tcapOm1stAndIntervalActivate, &StorageTmp->tcapOm1stAndIntervalActivateLen);
	if (StorageTmp->tcapOm1stAndIntervalActivate == NULL) {
		config_perror("invalid specification for tcapOm1stAndIntervalActivate");
		return;
	}
	SNMP_FREE(StorageTmp->tcapOm1stAndIntervalDeactivate);
	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->tcapOm1stAndIntervalDeactivate, &StorageTmp->tcapOm1stAndIntervalDeactivateLen);
	if (StorageTmp->tcapOm1stAndIntervalDeactivate == NULL) {
		config_perror("invalid specification for tcapOm1stAndIntervalDeactivate");
		return;
	}
	SNMP_FREE(StorageTmp->tcapOm5MinActivate);
	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->tcapOm5MinActivate, &StorageTmp->tcapOm5MinActivateLen);
	if (StorageTmp->tcapOm5MinActivate == NULL) {
		config_perror("invalid specification for tcapOm5MinActivate");
		return;
	}
	SNMP_FREE(StorageTmp->tcapOm5MinDeaActivate);
	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->tcapOm5MinDeaActivate, &StorageTmp->tcapOm5MinDeaActivateLen);
	if (StorageTmp->tcapOm5MinDeaActivate == NULL) {
		config_perror("invalid specification for tcapOm5MinDeaActivate");
		return;
	}
	SNMP_FREE(StorageTmp->tcapOm15MinActivate);
	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->tcapOm15MinActivate, &StorageTmp->tcapOm15MinActivateLen);
	if (StorageTmp->tcapOm15MinActivate == NULL) {
		config_perror("invalid specification for tcapOm15MinActivate");
		return;
	}
	SNMP_FREE(StorageTmp->tcapOm15MinDeaActivate);
	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->tcapOm15MinDeaActivate, &StorageTmp->tcapOm15MinDeaActivateLen);
	if (StorageTmp->tcapOm15MinDeaActivate == NULL) {
		config_perror("invalid specification for tcapOm15MinDeaActivate");
		return;
	}
	line = read_config_read_data(ASN_TIMETICKS, line, &StorageTmp->tcapOmDiscontinuityTime, &tmpsize);
	line = read_config_read_data(ASN_TIMETICKS, line, &StorageTmp->tcapOmTimeStamp, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->tcapOm5MinMaxIntervals, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->tcapOm15MinMaxIntervals, &tmpsize);
	tcapOmMIB_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
}

/*
 * store_tcapOmMIB(): stores .conf file entries needed to configure the mib.
 */
int
store_tcapOmMIB(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct tcapOmMIB_data *StorageTmp;

	(void) majorID;
	(void) minorID;
	(void) serverarg;
	(void) clientarg;
	DEBUGMSGTL(("tcapOmMIB", "store_tcapOmMIB: storing data...  "));
	refresh_tcapOmMIB(1);
	if ((StorageTmp = tcapOmMIBStorage) == NULL) {
		DEBUGMSGTL(("tcapOmMIB", "error.\n"));
		return SNMPERR_GENERR;
	}
	(void) tmpsize;
	/* XXX: comment entire section if no scalars are persistent */
	{
		memset(line, 0, sizeof(line));
		strcat(line, "tcapOmMIB ");
		cptr = line + strlen(line);
		(void) cptr;
		/* XXX: remove individual scalars that are not persistent */
		cptr = read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->tcapOm1stAndIntervalActivate, &StorageTmp->tcapOm1stAndIntervalActivateLen);
		cptr = read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->tcapOm1stAndIntervalDeactivate, &StorageTmp->tcapOm1stAndIntervalDeactivateLen);
		cptr = read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->tcapOm5MinActivate, &StorageTmp->tcapOm5MinActivateLen);
		cptr = read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->tcapOm5MinDeaActivate, &StorageTmp->tcapOm5MinDeaActivateLen);
		cptr = read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->tcapOm15MinActivate, &StorageTmp->tcapOm15MinActivateLen);
		cptr = read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->tcapOm15MinDeaActivate, &StorageTmp->tcapOm15MinDeaActivateLen);
		cptr = read_config_store_data(ASN_TIMETICKS, cptr, &StorageTmp->tcapOmDiscontinuityTime, &tmpsize);
		cptr = read_config_store_data(ASN_TIMETICKS, cptr, &StorageTmp->tcapOmTimeStamp, &tmpsize);
		cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->tcapOm5MinMaxIntervals, &tmpsize);
		cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->tcapOm15MinMaxIntervals, &tmpsize);
		snmpd_store_config(line);
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int check_tcapOmMIB(struct tcapOmMIB_data *StorageTmp, struct tcapOmMIB_data *StorageOld)
 * @param StorageTmp the data as updated
 * @param StorageOld the data previous to update
 *
 * This function is used by mibs.  It is used to check, all scalars at a time, the varbinds
 * belonging to the mib.  This function is called for the first varbind in a mib at the beginning of
 * the ACTION phase.  The COMMIT phase does not ensue unless this check passes.  This function can
 * return SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before
 * the varbinds on the mib were applied; the values in StorageTmp are the new values.  The function
 * is permitted to change the values in StorageTmp to correct them; however, preferences should be
 * made for setting values that were not in the varbinds.
 */
int
check_tcapOmMIB(struct tcapOmMIB_data *StorageTmp, struct tcapOmMIB_data *StorageOld)
{
	(void) StorageTmp;
	(void) StorageOld;
	/* XXX: provide code to check the scalars for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_tcapOmMIB(struct tcapOmMIB_data *StorageTmp, struct tcapOmMIB_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase)
 *
 * This function is used by mibs.  It is used to update, all scalars at a time, the varbinds
 * belonging to the mib.  This function is called for the first varbind in a mib at the beginning of
 * the COMMIT phase.  The start of the ACTION phase performs a consistency check on the mib before
 * allowing the request to proceed to the COMMIT phase.  The COMMIT phase then arrives here with
 * consistency already checked (see check_tcapOmMIB()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the mib were applied: the values in StorageTmp are the new values.
 */
int
update_tcapOmMIB(struct tcapOmMIB_data *StorageTmp, struct tcapOmMIB_data *StorageOld)
{
	(void) StorageTmp;
	(void) StorageOld;
	/* XXX: provide code to update the row with the underlying device */
	tcapOmMIB_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn revert_tcapOmMIB(struct 
 * @fn void revert_tcapOmMIB(struct tcapOmMIB_data *StorageTmp, struct tcapOmMIB_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase)
 */
void
revert_tcapOmMIB(struct tcapOmMIB_data *StorageTmp, struct tcapOmMIB_data *StorageOld)
{
	(void) StorageTmp;
	/* XXX: provide code to revert the row with the underlying device */
	update_tcapOmMIB(StorageOld, NULL);
}

/**
 * @fn void refresh_tcapOmMIB(int force)
 * @param force forced refresh when non-zero.
 * @brief refresh the scalar values of tcapOmMIB.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a scalar has been requested).
 */
void
refresh_tcapOmMIB(int force)
{
	if (tcapOmMIBStorage == NULL) {
		struct tcapOmMIB_data *StorageNew;

		if ((StorageNew = tcapOmMIB_create()) == NULL)
			return;
		tcapOmMIBStorage = StorageNew;
		tcapOmMIB_refresh = 1;
	}
	if (!force && tcapOmMIB_refresh == 0)
		return;
	DEBUGMSGTL(("tcapOmMIB", "refresh_tcapOmMIB: refreshing...  "));
	/* XXX: Update scalars as required here... */
	tcapOmMIB_refresh = 0;
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
}

/**
 * @fn u_char * var_tcapOmMIB(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @param vp a pointer to the entry in the variables table for the requested variable.
 * @param name the object identifier for which to find.
 * @param length the length of the object identifier.
 * @param exact whether the name is exact.
 * @param var_len a pointer to the length of the representation of the object.
 * @param write_method a pointer to a write method for the object.
 * @brief locate variables in tcapOmMIB.
 *
 * This function returns a pointer to a memory area that is static across the request that contains
 * the UCD-SNMP representation of the scalar (so that it may be used to read from for a GET,
 * GET-NEXT or GET-BULK request).  This returned pointer may be NULL, in which case the function is
 * telling UCD-SNMP that the scalar does not exist for reading; however, if write_method is
 * overwritten with a non-NULL value, the function is telling UCD-SNMP that the scalar exists for
 * writing.  Write-only objects can be effected in this way.
 */
u_char *
var_tcapOmMIB(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct tcapOmMIB_data *StorageTmp;
	u_char *rval;

	DEBUGMSGTL(("tcapOmMIB", "var_tcapOmMIB: lookup up varbind...  "));
	if (header_generic(vp, name, length, exact, var_len, write_method) == MATCH_FAILED)
		return NULL;
	/* Refresh the MIB values if required. */
	refresh_tcapOmMIB(0);
	if ((StorageTmp = tcapOmMIBStorage) == NULL) {
		DEBUGMSGTL(("tcapOmMIB", "no datastructure.\n"));
		return NULL;
	}
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) TCAPOM1STANDINTERVALACTIVATE:	/* ReadWrite */
		*write_method = write_tcapOm1stAndIntervalActivate;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->tcapOm1stAndIntervalActivateLen * sizeof(oid);
		rval = (u_char *) StorageTmp->tcapOm1stAndIntervalActivate;
		break;
	case (u_char) TCAPOM1STANDINTERVALDEACTIVATE:	/* ReadWrite */
		*write_method = write_tcapOm1stAndIntervalDeactivate;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->tcapOm1stAndIntervalDeactivateLen * sizeof(oid);
		rval = (u_char *) StorageTmp->tcapOm1stAndIntervalDeactivate;
		break;
	case (u_char) TCAPOM5MINACTIVATE:	/* ReadWrite */
		*write_method = write_tcapOm5MinActivate;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->tcapOm5MinActivateLen * sizeof(oid);
		rval = (u_char *) StorageTmp->tcapOm5MinActivate;
		break;
	case (u_char) TCAPOM5MINDEAACTIVATE:	/* ReadWrite */
		*write_method = write_tcapOm5MinDeaActivate;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->tcapOm5MinDeaActivateLen * sizeof(oid);
		rval = (u_char *) StorageTmp->tcapOm5MinDeaActivate;
		break;
	case (u_char) TCAPOM15MINACTIVATE:	/* ReadWrite */
		*write_method = write_tcapOm15MinActivate;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->tcapOm15MinActivateLen * sizeof(oid);
		rval = (u_char *) StorageTmp->tcapOm15MinActivate;
		break;
	case (u_char) TCAPOM15MINDEAACTIVATE:	/* ReadWrite */
		*write_method = write_tcapOm15MinDeaActivate;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->tcapOm15MinDeaActivateLen * sizeof(oid);
		rval = (u_char *) StorageTmp->tcapOm15MinDeaActivate;
		break;
	case (u_char) TCAPOMDISCONTINUITYTIME:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDiscontinuityTime);
		rval = (u_char *) &StorageTmp->tcapOmDiscontinuityTime;
		break;
	case (u_char) TCAPOMTIMESTAMP:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmTimeStamp);
		rval = (u_char *) &StorageTmp->tcapOmTimeStamp;
		break;
	case (u_char) TCAPOM5MINMAXINTERVALS:	/* ReadWrite */
		*write_method = write_tcapOm5MinMaxIntervals;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOm5MinMaxIntervals);
		rval = (u_char *) &StorageTmp->tcapOm5MinMaxIntervals;
		break;
	case (u_char) TCAPOM15MINMAXINTERVALS:	/* ReadWrite */
		*write_method = write_tcapOm15MinMaxIntervals;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOm15MinMaxIntervals);
		rval = (u_char *) &StorageTmp->tcapOm15MinMaxIntervals;
		break;
	default:
		ERROR_MSG("");
	}
	if (rval)
		DEBUGMSGTL(("tcapOmMIB", "found.\n"));
	else
		DEBUGMSGTL(("tcapOmMIB", "not found.\n"));
	return (rval);
}

/**
 * @fn struct tcapOmNodeTable_data *tcapOmNodeTable_create(void)
 * @brief create a fresh data structure representing a new row in the tcapOmNodeTable table.
 *
 * Creates a new tcapOmNodeTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct tcapOmNodeTable_data *
tcapOmNodeTable_create(void)
{
	struct tcapOmNodeTable_data *StorageNew = SNMP_MALLOC_STRUCT(tcapOmNodeTable_data);

	DEBUGMSGTL(("tcapOmMIB", "tcapOmNodeTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->sccpNetworkEntityId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->sccpNetworkEntityIdLen = 0;
		StorageNew->sccpNetworkEntityId[StorageNew->sccpNetworkEntityIdLen] = 0;
		StorageNew->tcapOmNodeMessagesSent = 0;
		StorageNew->tcapOmNodeMessagesRecv = 0;
		StorageNew->tcapOmNodeComponentsSent = 0;
		StorageNew->tcapOmNodeComponentsRecv = 0;
		StorageNew->tcapOmNode5minValidIntervals = 0;
		StorageNew->tcapOmNode15minValidIntervals = 0;
	}
      done:
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	tcapOmNodeTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct tcapOmNodeTable_data *tcapOmNodeTable_duplicate(struct tcapOmNodeTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct tcapOmNodeTable_data *
tcapOmNodeTable_duplicate(struct tcapOmNodeTable_data *thedata)
{
	struct tcapOmNodeTable_data *StorageNew = SNMP_MALLOC_STRUCT(tcapOmNodeTable_data);

	DEBUGMSGTL(("tcapOmMIB", "tcapOmNodeTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->tcapOmNodeTable_id = thedata->tcapOmNodeTable_id;
		if (!(StorageNew->sccpNetworkEntityId = malloc(thedata->sccpNetworkEntityIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->sccpNetworkEntityId, thedata->sccpNetworkEntityId, thedata->sccpNetworkEntityIdLen);
		StorageNew->sccpNetworkEntityIdLen = thedata->sccpNetworkEntityIdLen;
		StorageNew->sccpNetworkEntityId[StorageNew->sccpNetworkEntityIdLen] = 0;
		StorageNew->tcapOmNodeMessagesSent = thedata->tcapOmNodeMessagesSent;
		StorageNew->tcapOmNodeMessagesRecv = thedata->tcapOmNodeMessagesRecv;
		StorageNew->tcapOmNodeComponentsSent = thedata->tcapOmNodeComponentsSent;
		StorageNew->tcapOmNodeComponentsRecv = thedata->tcapOmNodeComponentsRecv;
		StorageNew->tcapOmNode5minValidIntervals = thedata->tcapOmNode5minValidIntervals;
		StorageNew->tcapOmNode15minValidIntervals = thedata->tcapOmNode15minValidIntervals;
	}
      done:
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	tcapOmNodeTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int tcapOmNodeTable_destroy(struct tcapOmNodeTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
tcapOmNodeTable_destroy(struct tcapOmNodeTable_data **thedata)
{
	struct tcapOmNodeTable_data *StorageDel;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmNodeTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->sccpNetworkEntityId);
		StorageDel->sccpNetworkEntityIdLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int tcapOmNodeTable_add(struct tcapOmNodeTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the tcapOmNodeTable table data set.
 *
 * Adds a table row structure to the tcapOmNodeTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
tcapOmNodeTable_add(struct tcapOmNodeTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmNodeTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* sccpNetworkEntityId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->sccpNetworkEntityId, thedata->sccpNetworkEntityIdLen);
		header_complex_add_data(&tcapOmNodeTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("tcapOmMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int tcapOmNodeTable_del(struct tcapOmNodeTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the tcapOmNodeTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
tcapOmNodeTable_del(struct tcapOmNodeTable_data *thedata)
{
	struct tcapOmNodeTable_data *StorageDel;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmNodeTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(tcapOmNodeTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&tcapOmNodeTableStorage, hciptr);
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_tcapOmNodeTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for tcapOmNodeTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case tcapOmNodeTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_tcapOmNodeTable(const char *token, char *line)
{
	size_t tmpsize;
	struct tcapOmNodeTable_data *StorageTmp = tcapOmNodeTable_create();

	(void) token;
	(void) line;
	DEBUGMSGTL(("tcapOmMIB", "parse_tcapOmNodeTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->sccpNetworkEntityId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->sccpNetworkEntityId, &StorageTmp->sccpNetworkEntityIdLen);
	if (StorageTmp->sccpNetworkEntityId == NULL) {
		config_perror("invalid specification for sccpNetworkEntityId");
		return;
	}
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmNodeMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmNodeMessagesRecv, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmNodeComponentsSent, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmNodeComponentsRecv, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->tcapOmNode5minValidIntervals, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->tcapOmNode15minValidIntervals, &tmpsize);
	tcapOmNodeTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
}

/*
 * store_tcapOmNodeTable(): store configuraiton file for tcapOmNodeTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_tcapOmNodeTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct tcapOmNodeTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	(void) majorID;
	(void) minorID;
	(void) serverarg;
	(void) clientarg;
	DEBUGMSGTL(("tcapOmMIB", "store_tcapOmNodeTable: storing data...  "));
	refresh_tcapOmNodeTable(1);
	(void) tmpsize;
	for (hcindex = tcapOmNodeTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct tcapOmNodeTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "tcapOmNodeTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->sccpNetworkEntityId, &StorageTmp->sccpNetworkEntityIdLen);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmNodeMessagesSent, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmNodeMessagesRecv, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmNodeComponentsSent, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmNodeComponentsRecv, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->tcapOmNode5minValidIntervals, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->tcapOmNode15minValidIntervals, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct tcapOmNodeCurrentTable_data *tcapOmNodeCurrentTable_create(void)
 * @brief create a fresh data structure representing a new row in the tcapOmNodeCurrentTable table.
 *
 * Creates a new tcapOmNodeCurrentTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct tcapOmNodeCurrentTable_data *
tcapOmNodeCurrentTable_create(void)
{
	struct tcapOmNodeCurrentTable_data *StorageNew = SNMP_MALLOC_STRUCT(tcapOmNodeCurrentTable_data);

	DEBUGMSGTL(("tcapOmMIB", "tcapOmNodeCurrentTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->sccpNetworkEntityId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->sccpNetworkEntityIdLen = 0;
		StorageNew->sccpNetworkEntityId[StorageNew->sccpNetworkEntityIdLen] = 0;
		StorageNew->tcapOmNodeCurrentMessagesSent = 0;
		StorageNew->tcapOmNodeCurrentMessagesRecv = 0;
		StorageNew->tcapOmNodeCurrentComponentsSent = 0;
		StorageNew->tcapOmNodeCurrentComponentsRecv = 0;
	}
      done:
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	tcapOmNodeCurrentTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct tcapOmNodeCurrentTable_data *tcapOmNodeCurrentTable_duplicate(struct tcapOmNodeCurrentTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct tcapOmNodeCurrentTable_data *
tcapOmNodeCurrentTable_duplicate(struct tcapOmNodeCurrentTable_data *thedata)
{
	struct tcapOmNodeCurrentTable_data *StorageNew = SNMP_MALLOC_STRUCT(tcapOmNodeCurrentTable_data);

	DEBUGMSGTL(("tcapOmMIB", "tcapOmNodeCurrentTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->tcapOmNodeCurrentTable_id = thedata->tcapOmNodeCurrentTable_id;
		if (!(StorageNew->sccpNetworkEntityId = malloc(thedata->sccpNetworkEntityIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->sccpNetworkEntityId, thedata->sccpNetworkEntityId, thedata->sccpNetworkEntityIdLen);
		StorageNew->sccpNetworkEntityIdLen = thedata->sccpNetworkEntityIdLen;
		StorageNew->sccpNetworkEntityId[StorageNew->sccpNetworkEntityIdLen] = 0;
		StorageNew->tcapOmNodeCurrentMessagesSent = thedata->tcapOmNodeCurrentMessagesSent;
		StorageNew->tcapOmNodeCurrentMessagesRecv = thedata->tcapOmNodeCurrentMessagesRecv;
		StorageNew->tcapOmNodeCurrentComponentsSent = thedata->tcapOmNodeCurrentComponentsSent;
		StorageNew->tcapOmNodeCurrentComponentsRecv = thedata->tcapOmNodeCurrentComponentsRecv;
	}
      done:
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	tcapOmNodeCurrentTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int tcapOmNodeCurrentTable_destroy(struct tcapOmNodeCurrentTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
tcapOmNodeCurrentTable_destroy(struct tcapOmNodeCurrentTable_data **thedata)
{
	struct tcapOmNodeCurrentTable_data *StorageDel;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmNodeCurrentTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->sccpNetworkEntityId);
		StorageDel->sccpNetworkEntityIdLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int tcapOmNodeCurrentTable_add(struct tcapOmNodeCurrentTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the tcapOmNodeCurrentTable table data set.
 *
 * Adds a table row structure to the tcapOmNodeCurrentTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
tcapOmNodeCurrentTable_add(struct tcapOmNodeCurrentTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmNodeCurrentTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* sccpNetworkEntityId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->sccpNetworkEntityId, thedata->sccpNetworkEntityIdLen);
		header_complex_add_data(&tcapOmNodeCurrentTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("tcapOmMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int tcapOmNodeCurrentTable_del(struct tcapOmNodeCurrentTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the tcapOmNodeCurrentTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
tcapOmNodeCurrentTable_del(struct tcapOmNodeCurrentTable_data *thedata)
{
	struct tcapOmNodeCurrentTable_data *StorageDel;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmNodeCurrentTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(tcapOmNodeCurrentTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&tcapOmNodeCurrentTableStorage, hciptr);
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_tcapOmNodeCurrentTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for tcapOmNodeCurrentTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case tcapOmNodeCurrentTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_tcapOmNodeCurrentTable(const char *token, char *line)
{
	size_t tmpsize;
	struct tcapOmNodeCurrentTable_data *StorageTmp = tcapOmNodeCurrentTable_create();

	(void) token;
	(void) line;
	DEBUGMSGTL(("tcapOmMIB", "parse_tcapOmNodeCurrentTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->sccpNetworkEntityId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->sccpNetworkEntityId, &StorageTmp->sccpNetworkEntityIdLen);
	if (StorageTmp->sccpNetworkEntityId == NULL) {
		config_perror("invalid specification for sccpNetworkEntityId");
		return;
	}
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmNodeCurrentMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmNodeCurrentMessagesRecv, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmNodeCurrentComponentsSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmNodeCurrentComponentsRecv, &tmpsize);
	tcapOmNodeCurrentTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
}

/*
 * store_tcapOmNodeCurrentTable(): store configuraiton file for tcapOmNodeCurrentTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_tcapOmNodeCurrentTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct tcapOmNodeCurrentTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	(void) majorID;
	(void) minorID;
	(void) serverarg;
	(void) clientarg;
	DEBUGMSGTL(("tcapOmMIB", "store_tcapOmNodeCurrentTable: storing data...  "));
	refresh_tcapOmNodeCurrentTable(1);
	(void) tmpsize;
	for (hcindex = tcapOmNodeCurrentTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct tcapOmNodeCurrentTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "tcapOmNodeCurrentTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->sccpNetworkEntityId, &StorageTmp->sccpNetworkEntityIdLen);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmNodeCurrentMessagesSent, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmNodeCurrentMessagesRecv, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmNodeCurrentComponentsSent, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmNodeCurrentComponentsRecv, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct tcapOmNode5minIntTable_data *tcapOmNode5minIntTable_create(void)
 * @brief create a fresh data structure representing a new row in the tcapOmNode5minIntTable table.
 *
 * Creates a new tcapOmNode5minIntTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct tcapOmNode5minIntTable_data *
tcapOmNode5minIntTable_create(void)
{
	struct tcapOmNode5minIntTable_data *StorageNew = SNMP_MALLOC_STRUCT(tcapOmNode5minIntTable_data);

	DEBUGMSGTL(("tcapOmMIB", "tcapOmNode5minIntTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->sccpNetworkEntityId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->sccpNetworkEntityIdLen = 0;
		StorageNew->sccpNetworkEntityId[StorageNew->sccpNetworkEntityIdLen] = 0;
		StorageNew->tcapOmNode5minIntMessagesSent = 0;
		StorageNew->tcapOmNode5minIntMessagesRecv = 0;
		StorageNew->tcapOmNode5minIntComponentsSent = 0;
		StorageNew->tcapOmNode5minIntComponentsRecv = 0;
	}
      done:
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	tcapOmNode5minIntTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct tcapOmNode5minIntTable_data *tcapOmNode5minIntTable_duplicate(struct tcapOmNode5minIntTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct tcapOmNode5minIntTable_data *
tcapOmNode5minIntTable_duplicate(struct tcapOmNode5minIntTable_data *thedata)
{
	struct tcapOmNode5minIntTable_data *StorageNew = SNMP_MALLOC_STRUCT(tcapOmNode5minIntTable_data);

	DEBUGMSGTL(("tcapOmMIB", "tcapOmNode5minIntTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->tcapOmNode5minIntTable_id = thedata->tcapOmNode5minIntTable_id;
		if (!(StorageNew->sccpNetworkEntityId = malloc(thedata->sccpNetworkEntityIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->sccpNetworkEntityId, thedata->sccpNetworkEntityId, thedata->sccpNetworkEntityIdLen);
		StorageNew->sccpNetworkEntityIdLen = thedata->sccpNetworkEntityIdLen;
		StorageNew->sccpNetworkEntityId[StorageNew->sccpNetworkEntityIdLen] = 0;
		StorageNew->tcapOmInterval = thedata->tcapOmInterval;
		StorageNew->tcapOmNode5minIntMessagesSent = thedata->tcapOmNode5minIntMessagesSent;
		StorageNew->tcapOmNode5minIntMessagesRecv = thedata->tcapOmNode5minIntMessagesRecv;
		StorageNew->tcapOmNode5minIntComponentsSent = thedata->tcapOmNode5minIntComponentsSent;
		StorageNew->tcapOmNode5minIntComponentsRecv = thedata->tcapOmNode5minIntComponentsRecv;
	}
      done:
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	tcapOmNode5minIntTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int tcapOmNode5minIntTable_destroy(struct tcapOmNode5minIntTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
tcapOmNode5minIntTable_destroy(struct tcapOmNode5minIntTable_data **thedata)
{
	struct tcapOmNode5minIntTable_data *StorageDel;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmNode5minIntTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->sccpNetworkEntityId);
		StorageDel->sccpNetworkEntityIdLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int tcapOmNode5minIntTable_add(struct tcapOmNode5minIntTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the tcapOmNode5minIntTable table data set.
 *
 * Adds a table row structure to the tcapOmNode5minIntTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
tcapOmNode5minIntTable_add(struct tcapOmNode5minIntTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmNode5minIntTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* sccpNetworkEntityId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->sccpNetworkEntityId, thedata->sccpNetworkEntityIdLen);
		/* tcapOmInterval */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->tcapOmInterval, sizeof(thedata->tcapOmInterval));
		header_complex_add_data(&tcapOmNode5minIntTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("tcapOmMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int tcapOmNode5minIntTable_del(struct tcapOmNode5minIntTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the tcapOmNode5minIntTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
tcapOmNode5minIntTable_del(struct tcapOmNode5minIntTable_data *thedata)
{
	struct tcapOmNode5minIntTable_data *StorageDel;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmNode5minIntTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(tcapOmNode5minIntTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&tcapOmNode5minIntTableStorage, hciptr);
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_tcapOmNode5minIntTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for tcapOmNode5minIntTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case tcapOmNode5minIntTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_tcapOmNode5minIntTable(const char *token, char *line)
{
	size_t tmpsize;
	struct tcapOmNode5minIntTable_data *StorageTmp = tcapOmNode5minIntTable_create();

	(void) token;
	(void) line;
	DEBUGMSGTL(("tcapOmMIB", "parse_tcapOmNode5minIntTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->sccpNetworkEntityId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->sccpNetworkEntityId, &StorageTmp->sccpNetworkEntityIdLen);
	if (StorageTmp->sccpNetworkEntityId == NULL) {
		config_perror("invalid specification for sccpNetworkEntityId");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->tcapOmInterval, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmNode5minIntMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmNode5minIntMessagesRecv, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmNode5minIntComponentsSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmNode5minIntComponentsRecv, &tmpsize);
	tcapOmNode5minIntTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
}

/*
 * store_tcapOmNode5minIntTable(): store configuraiton file for tcapOmNode5minIntTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_tcapOmNode5minIntTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct tcapOmNode5minIntTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	(void) majorID;
	(void) minorID;
	(void) serverarg;
	(void) clientarg;
	DEBUGMSGTL(("tcapOmMIB", "store_tcapOmNode5minIntTable: storing data...  "));
	refresh_tcapOmNode5minIntTable(1);
	(void) tmpsize;
	for (hcindex = tcapOmNode5minIntTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct tcapOmNode5minIntTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "tcapOmNode5minIntTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->sccpNetworkEntityId, &StorageTmp->sccpNetworkEntityIdLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->tcapOmInterval, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmNode5minIntMessagesSent, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmNode5minIntMessagesRecv, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmNode5minIntComponentsSent, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmNode5minIntComponentsRecv, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct tcapOmNode15minIntTable_data *tcapOmNode15minIntTable_create(void)
 * @brief create a fresh data structure representing a new row in the tcapOmNode15minIntTable table.
 *
 * Creates a new tcapOmNode15minIntTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct tcapOmNode15minIntTable_data *
tcapOmNode15minIntTable_create(void)
{
	struct tcapOmNode15minIntTable_data *StorageNew = SNMP_MALLOC_STRUCT(tcapOmNode15minIntTable_data);

	DEBUGMSGTL(("tcapOmMIB", "tcapOmNode15minIntTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->sccpNetworkEntityId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->sccpNetworkEntityIdLen = 0;
		StorageNew->sccpNetworkEntityId[StorageNew->sccpNetworkEntityIdLen] = 0;
		StorageNew->tcapOmInterval = 0;
		StorageNew->tcapOmNode15minIntMessagesSent = 0;
		StorageNew->tcapOmNode15minIntMessagesRecv = 0;
		StorageNew->tcapOmNode15minIntComponentsSent = 0;
		StorageNew->tcapOmNode15minIntComponentsRecv = 0;
	}
      done:
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	tcapOmNode15minIntTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct tcapOmNode15minIntTable_data *tcapOmNode15minIntTable_duplicate(struct tcapOmNode15minIntTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct tcapOmNode15minIntTable_data *
tcapOmNode15minIntTable_duplicate(struct tcapOmNode15minIntTable_data *thedata)
{
	struct tcapOmNode15minIntTable_data *StorageNew = SNMP_MALLOC_STRUCT(tcapOmNode15minIntTable_data);

	DEBUGMSGTL(("tcapOmMIB", "tcapOmNode15minIntTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->tcapOmNode15minIntTable_id = thedata->tcapOmNode15minIntTable_id;
		if (!(StorageNew->sccpNetworkEntityId = malloc(thedata->sccpNetworkEntityIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->sccpNetworkEntityId, thedata->sccpNetworkEntityId, thedata->sccpNetworkEntityIdLen);
		StorageNew->sccpNetworkEntityIdLen = thedata->sccpNetworkEntityIdLen;
		StorageNew->sccpNetworkEntityId[StorageNew->sccpNetworkEntityIdLen] = 0;
		StorageNew->tcapOmInterval = thedata->tcapOmInterval;
		StorageNew->tcapOmNode15minIntMessagesSent = thedata->tcapOmNode15minIntMessagesSent;
		StorageNew->tcapOmNode15minIntMessagesRecv = thedata->tcapOmNode15minIntMessagesRecv;
		StorageNew->tcapOmNode15minIntComponentsSent = thedata->tcapOmNode15minIntComponentsSent;
		StorageNew->tcapOmNode15minIntComponentsRecv = thedata->tcapOmNode15minIntComponentsRecv;
	}
      done:
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	tcapOmNode15minIntTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int tcapOmNode15minIntTable_destroy(struct tcapOmNode15minIntTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
tcapOmNode15minIntTable_destroy(struct tcapOmNode15minIntTable_data **thedata)
{
	struct tcapOmNode15minIntTable_data *StorageDel;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmNode15minIntTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->sccpNetworkEntityId);
		StorageDel->sccpNetworkEntityIdLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int tcapOmNode15minIntTable_add(struct tcapOmNode15minIntTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the tcapOmNode15minIntTable table data set.
 *
 * Adds a table row structure to the tcapOmNode15minIntTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
tcapOmNode15minIntTable_add(struct tcapOmNode15minIntTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmNode15minIntTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* sccpNetworkEntityId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->sccpNetworkEntityId, thedata->sccpNetworkEntityIdLen);
		/* tcapOmInterval */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->tcapOmInterval, sizeof(thedata->tcapOmInterval));
		header_complex_add_data(&tcapOmNode15minIntTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("tcapOmMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int tcapOmNode15minIntTable_del(struct tcapOmNode15minIntTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the tcapOmNode15minIntTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
tcapOmNode15minIntTable_del(struct tcapOmNode15minIntTable_data *thedata)
{
	struct tcapOmNode15minIntTable_data *StorageDel;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmNode15minIntTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(tcapOmNode15minIntTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&tcapOmNode15minIntTableStorage, hciptr);
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_tcapOmNode15minIntTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for tcapOmNode15minIntTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case tcapOmNode15minIntTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_tcapOmNode15minIntTable(const char *token, char *line)
{
	size_t tmpsize;
	struct tcapOmNode15minIntTable_data *StorageTmp = tcapOmNode15minIntTable_create();

	(void) token;
	(void) line;
	DEBUGMSGTL(("tcapOmMIB", "parse_tcapOmNode15minIntTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->sccpNetworkEntityId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->sccpNetworkEntityId, &StorageTmp->sccpNetworkEntityIdLen);
	if (StorageTmp->sccpNetworkEntityId == NULL) {
		config_perror("invalid specification for sccpNetworkEntityId");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->tcapOmInterval, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmNode15minIntMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmNode15minIntMessagesRecv, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmNode15minIntComponentsSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmNode15minIntComponentsRecv, &tmpsize);
	tcapOmNode15minIntTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
}

/*
 * store_tcapOmNode15minIntTable(): store configuraiton file for tcapOmNode15minIntTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_tcapOmNode15minIntTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct tcapOmNode15minIntTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	(void) majorID;
	(void) minorID;
	(void) serverarg;
	(void) clientarg;
	DEBUGMSGTL(("tcapOmMIB", "store_tcapOmNode15minIntTable: storing data...  "));
	refresh_tcapOmNode15minIntTable(1);
	(void) tmpsize;
	for (hcindex = tcapOmNode15minIntTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct tcapOmNode15minIntTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "tcapOmNode15minIntTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->sccpNetworkEntityId, &StorageTmp->sccpNetworkEntityIdLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->tcapOmInterval, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmNode15minIntMessagesSent, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmNode15minIntMessagesRecv, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmNode15minIntComponentsSent, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmNode15minIntComponentsRecv, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct tcapOmMsgsTable_data *tcapOmMsgsTable_create(void)
 * @brief create a fresh data structure representing a new row in the tcapOmMsgsTable table.
 *
 * Creates a new tcapOmMsgsTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct tcapOmMsgsTable_data *
tcapOmMsgsTable_create(void)
{
	struct tcapOmMsgsTable_data *StorageNew = SNMP_MALLOC_STRUCT(tcapOmMsgsTable_data);

	DEBUGMSGTL(("tcapOmMIB", "tcapOmMsgsTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->sccpNetworkEntityId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->sccpNetworkEntityIdLen = 0;
		StorageNew->sccpNetworkEntityId[StorageNew->sccpNetworkEntityIdLen] = 0;
		StorageNew->tcapOmMsgsMessagesSent = 0;
		StorageNew->tcapOmMsgsMessagesRecv = 0;
		StorageNew->tcapOmMsgs5minValidIntervals = 0;
		StorageNew->tcapOmMsgs15minValidIntervals = 0;
	}
      done:
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	tcapOmMsgsTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct tcapOmMsgsTable_data *tcapOmMsgsTable_duplicate(struct tcapOmMsgsTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct tcapOmMsgsTable_data *
tcapOmMsgsTable_duplicate(struct tcapOmMsgsTable_data *thedata)
{
	struct tcapOmMsgsTable_data *StorageNew = SNMP_MALLOC_STRUCT(tcapOmMsgsTable_data);

	DEBUGMSGTL(("tcapOmMIB", "tcapOmMsgsTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->tcapOmMsgsTable_id = thedata->tcapOmMsgsTable_id;
		if (!(StorageNew->sccpNetworkEntityId = malloc(thedata->sccpNetworkEntityIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->sccpNetworkEntityId, thedata->sccpNetworkEntityId, thedata->sccpNetworkEntityIdLen);
		StorageNew->sccpNetworkEntityIdLen = thedata->sccpNetworkEntityIdLen;
		StorageNew->sccpNetworkEntityId[StorageNew->sccpNetworkEntityIdLen] = 0;
		StorageNew->tcapOmMessageType = thedata->tcapOmMessageType;
		StorageNew->tcapOmMsgsMessagesSent = thedata->tcapOmMsgsMessagesSent;
		StorageNew->tcapOmMsgsMessagesRecv = thedata->tcapOmMsgsMessagesRecv;
		StorageNew->tcapOmMsgs5minValidIntervals = thedata->tcapOmMsgs5minValidIntervals;
		StorageNew->tcapOmMsgs15minValidIntervals = thedata->tcapOmMsgs15minValidIntervals;
	}
      done:
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	tcapOmMsgsTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int tcapOmMsgsTable_destroy(struct tcapOmMsgsTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
tcapOmMsgsTable_destroy(struct tcapOmMsgsTable_data **thedata)
{
	struct tcapOmMsgsTable_data *StorageDel;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmMsgsTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->sccpNetworkEntityId);
		StorageDel->sccpNetworkEntityIdLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int tcapOmMsgsTable_add(struct tcapOmMsgsTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the tcapOmMsgsTable table data set.
 *
 * Adds a table row structure to the tcapOmMsgsTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
tcapOmMsgsTable_add(struct tcapOmMsgsTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmMsgsTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* sccpNetworkEntityId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->sccpNetworkEntityId, thedata->sccpNetworkEntityIdLen);
		/* tcapOmMessageType */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->tcapOmMessageType, sizeof(thedata->tcapOmMessageType));
		header_complex_add_data(&tcapOmMsgsTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("tcapOmMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int tcapOmMsgsTable_del(struct tcapOmMsgsTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the tcapOmMsgsTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
tcapOmMsgsTable_del(struct tcapOmMsgsTable_data *thedata)
{
	struct tcapOmMsgsTable_data *StorageDel;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmMsgsTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(tcapOmMsgsTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&tcapOmMsgsTableStorage, hciptr);
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_tcapOmMsgsTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for tcapOmMsgsTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case tcapOmMsgsTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_tcapOmMsgsTable(const char *token, char *line)
{
	size_t tmpsize;
	struct tcapOmMsgsTable_data *StorageTmp = tcapOmMsgsTable_create();

	(void) token;
	(void) line;
	DEBUGMSGTL(("tcapOmMIB", "parse_tcapOmMsgsTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->sccpNetworkEntityId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->sccpNetworkEntityId, &StorageTmp->sccpNetworkEntityIdLen);
	if (StorageTmp->sccpNetworkEntityId == NULL) {
		config_perror("invalid specification for sccpNetworkEntityId");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->tcapOmMessageType, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmMsgsMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmMsgsMessagesRecv, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->tcapOmMsgs5minValidIntervals, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->tcapOmMsgs15minValidIntervals, &tmpsize);
	tcapOmMsgsTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
}

/*
 * store_tcapOmMsgsTable(): store configuraiton file for tcapOmMsgsTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_tcapOmMsgsTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct tcapOmMsgsTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	(void) majorID;
	(void) minorID;
	(void) serverarg;
	(void) clientarg;
	DEBUGMSGTL(("tcapOmMIB", "store_tcapOmMsgsTable: storing data...  "));
	refresh_tcapOmMsgsTable(1);
	(void) tmpsize;
	for (hcindex = tcapOmMsgsTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct tcapOmMsgsTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "tcapOmMsgsTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->sccpNetworkEntityId, &StorageTmp->sccpNetworkEntityIdLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->tcapOmMessageType, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmMsgsMessagesSent, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmMsgsMessagesRecv, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->tcapOmMsgs5minValidIntervals, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->tcapOmMsgs15minValidIntervals, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct tcapOmMsgsCurrentTable_data *tcapOmMsgsCurrentTable_create(void)
 * @brief create a fresh data structure representing a new row in the tcapOmMsgsCurrentTable table.
 *
 * Creates a new tcapOmMsgsCurrentTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct tcapOmMsgsCurrentTable_data *
tcapOmMsgsCurrentTable_create(void)
{
	struct tcapOmMsgsCurrentTable_data *StorageNew = SNMP_MALLOC_STRUCT(tcapOmMsgsCurrentTable_data);

	DEBUGMSGTL(("tcapOmMIB", "tcapOmMsgsCurrentTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->sccpNetworkEntityId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->sccpNetworkEntityIdLen = 0;
		StorageNew->sccpNetworkEntityId[StorageNew->sccpNetworkEntityIdLen] = 0;
		StorageNew->tcapOmMessageType = 0;
		StorageNew->tcapOmMsgsCurrentMessagesSent = 0;
		StorageNew->tcapOmMsgsCurrentMessagesRecv = 0;
	}
      done:
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	tcapOmMsgsCurrentTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct tcapOmMsgsCurrentTable_data *tcapOmMsgsCurrentTable_duplicate(struct tcapOmMsgsCurrentTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct tcapOmMsgsCurrentTable_data *
tcapOmMsgsCurrentTable_duplicate(struct tcapOmMsgsCurrentTable_data *thedata)
{
	struct tcapOmMsgsCurrentTable_data *StorageNew = SNMP_MALLOC_STRUCT(tcapOmMsgsCurrentTable_data);

	DEBUGMSGTL(("tcapOmMIB", "tcapOmMsgsCurrentTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->tcapOmMsgsCurrentTable_id = thedata->tcapOmMsgsCurrentTable_id;
		if (!(StorageNew->sccpNetworkEntityId = malloc(thedata->sccpNetworkEntityIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->sccpNetworkEntityId, thedata->sccpNetworkEntityId, thedata->sccpNetworkEntityIdLen);
		StorageNew->sccpNetworkEntityIdLen = thedata->sccpNetworkEntityIdLen;
		StorageNew->sccpNetworkEntityId[StorageNew->sccpNetworkEntityIdLen] = 0;
		StorageNew->tcapOmMessageType = thedata->tcapOmMessageType;
		StorageNew->tcapOmMsgsCurrentMessagesSent = thedata->tcapOmMsgsCurrentMessagesSent;
		StorageNew->tcapOmMsgsCurrentMessagesRecv = thedata->tcapOmMsgsCurrentMessagesRecv;
	}
      done:
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	tcapOmMsgsCurrentTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int tcapOmMsgsCurrentTable_destroy(struct tcapOmMsgsCurrentTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
tcapOmMsgsCurrentTable_destroy(struct tcapOmMsgsCurrentTable_data **thedata)
{
	struct tcapOmMsgsCurrentTable_data *StorageDel;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmMsgsCurrentTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->sccpNetworkEntityId);
		StorageDel->sccpNetworkEntityIdLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int tcapOmMsgsCurrentTable_add(struct tcapOmMsgsCurrentTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the tcapOmMsgsCurrentTable table data set.
 *
 * Adds a table row structure to the tcapOmMsgsCurrentTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
tcapOmMsgsCurrentTable_add(struct tcapOmMsgsCurrentTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmMsgsCurrentTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* sccpNetworkEntityId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->sccpNetworkEntityId, thedata->sccpNetworkEntityIdLen);
		/* tcapOmMessageType */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->tcapOmMessageType, sizeof(thedata->tcapOmMessageType));
		header_complex_add_data(&tcapOmMsgsCurrentTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("tcapOmMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int tcapOmMsgsCurrentTable_del(struct tcapOmMsgsCurrentTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the tcapOmMsgsCurrentTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
tcapOmMsgsCurrentTable_del(struct tcapOmMsgsCurrentTable_data *thedata)
{
	struct tcapOmMsgsCurrentTable_data *StorageDel;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmMsgsCurrentTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(tcapOmMsgsCurrentTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&tcapOmMsgsCurrentTableStorage, hciptr);
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_tcapOmMsgsCurrentTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for tcapOmMsgsCurrentTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case tcapOmMsgsCurrentTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_tcapOmMsgsCurrentTable(const char *token, char *line)
{
	size_t tmpsize;
	struct tcapOmMsgsCurrentTable_data *StorageTmp = tcapOmMsgsCurrentTable_create();

	(void) token;
	(void) line;
	DEBUGMSGTL(("tcapOmMIB", "parse_tcapOmMsgsCurrentTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->sccpNetworkEntityId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->sccpNetworkEntityId, &StorageTmp->sccpNetworkEntityIdLen);
	if (StorageTmp->sccpNetworkEntityId == NULL) {
		config_perror("invalid specification for sccpNetworkEntityId");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->tcapOmMessageType, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmMsgsCurrentMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmMsgsCurrentMessagesRecv, &tmpsize);
	tcapOmMsgsCurrentTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
}

/*
 * store_tcapOmMsgsCurrentTable(): store configuraiton file for tcapOmMsgsCurrentTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_tcapOmMsgsCurrentTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct tcapOmMsgsCurrentTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	(void) majorID;
	(void) minorID;
	(void) serverarg;
	(void) clientarg;
	DEBUGMSGTL(("tcapOmMIB", "store_tcapOmMsgsCurrentTable: storing data...  "));
	refresh_tcapOmMsgsCurrentTable(1);
	(void) tmpsize;
	for (hcindex = tcapOmMsgsCurrentTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct tcapOmMsgsCurrentTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "tcapOmMsgsCurrentTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->sccpNetworkEntityId, &StorageTmp->sccpNetworkEntityIdLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->tcapOmMessageType, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmMsgsCurrentMessagesSent, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmMsgsCurrentMessagesRecv, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct tcapOmMsgs5minIntTable_data *tcapOmMsgs5minIntTable_create(void)
 * @brief create a fresh data structure representing a new row in the tcapOmMsgs5minIntTable table.
 *
 * Creates a new tcapOmMsgs5minIntTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct tcapOmMsgs5minIntTable_data *
tcapOmMsgs5minIntTable_create(void)
{
	struct tcapOmMsgs5minIntTable_data *StorageNew = SNMP_MALLOC_STRUCT(tcapOmMsgs5minIntTable_data);

	DEBUGMSGTL(("tcapOmMIB", "tcapOmMsgs5minIntTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->sccpNetworkEntityId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->sccpNetworkEntityIdLen = 0;
		StorageNew->sccpNetworkEntityId[StorageNew->sccpNetworkEntityIdLen] = 0;
		StorageNew->tcapOmMessageType = 0;
		StorageNew->tcapOmInterval = 0;
		StorageNew->tcapOmMsgs5minIntMessagesSent = 0;
		StorageNew->tcapOmMsgs5minIntMessagesRecv = 0;
	}
      done:
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	tcapOmMsgs5minIntTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct tcapOmMsgs5minIntTable_data *tcapOmMsgs5minIntTable_duplicate(struct tcapOmMsgs5minIntTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct tcapOmMsgs5minIntTable_data *
tcapOmMsgs5minIntTable_duplicate(struct tcapOmMsgs5minIntTable_data *thedata)
{
	struct tcapOmMsgs5minIntTable_data *StorageNew = SNMP_MALLOC_STRUCT(tcapOmMsgs5minIntTable_data);

	DEBUGMSGTL(("tcapOmMIB", "tcapOmMsgs5minIntTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->tcapOmMsgs5minIntTable_id = thedata->tcapOmMsgs5minIntTable_id;
		if (!(StorageNew->sccpNetworkEntityId = malloc(thedata->sccpNetworkEntityIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->sccpNetworkEntityId, thedata->sccpNetworkEntityId, thedata->sccpNetworkEntityIdLen);
		StorageNew->sccpNetworkEntityIdLen = thedata->sccpNetworkEntityIdLen;
		StorageNew->sccpNetworkEntityId[StorageNew->sccpNetworkEntityIdLen] = 0;
		StorageNew->tcapOmMessageType = thedata->tcapOmMessageType;
		StorageNew->tcapOmInterval = thedata->tcapOmInterval;
		StorageNew->tcapOmMsgs5minIntMessagesSent = thedata->tcapOmMsgs5minIntMessagesSent;
		StorageNew->tcapOmMsgs5minIntMessagesRecv = thedata->tcapOmMsgs5minIntMessagesRecv;
	}
      done:
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	tcapOmMsgs5minIntTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int tcapOmMsgs5minIntTable_destroy(struct tcapOmMsgs5minIntTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
tcapOmMsgs5minIntTable_destroy(struct tcapOmMsgs5minIntTable_data **thedata)
{
	struct tcapOmMsgs5minIntTable_data *StorageDel;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmMsgs5minIntTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->sccpNetworkEntityId);
		StorageDel->sccpNetworkEntityIdLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int tcapOmMsgs5minIntTable_add(struct tcapOmMsgs5minIntTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the tcapOmMsgs5minIntTable table data set.
 *
 * Adds a table row structure to the tcapOmMsgs5minIntTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
tcapOmMsgs5minIntTable_add(struct tcapOmMsgs5minIntTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmMsgs5minIntTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* sccpNetworkEntityId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->sccpNetworkEntityId, thedata->sccpNetworkEntityIdLen);
		/* tcapOmMessageType */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->tcapOmMessageType, sizeof(thedata->tcapOmMessageType));
		/* tcapOmInterval */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->tcapOmInterval, sizeof(thedata->tcapOmInterval));
		header_complex_add_data(&tcapOmMsgs5minIntTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("tcapOmMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int tcapOmMsgs5minIntTable_del(struct tcapOmMsgs5minIntTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the tcapOmMsgs5minIntTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
tcapOmMsgs5minIntTable_del(struct tcapOmMsgs5minIntTable_data *thedata)
{
	struct tcapOmMsgs5minIntTable_data *StorageDel;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmMsgs5minIntTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(tcapOmMsgs5minIntTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&tcapOmMsgs5minIntTableStorage, hciptr);
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_tcapOmMsgs5minIntTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for tcapOmMsgs5minIntTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case tcapOmMsgs5minIntTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_tcapOmMsgs5minIntTable(const char *token, char *line)
{
	size_t tmpsize;
	struct tcapOmMsgs5minIntTable_data *StorageTmp = tcapOmMsgs5minIntTable_create();

	(void) token;
	(void) line;
	DEBUGMSGTL(("tcapOmMIB", "parse_tcapOmMsgs5minIntTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->sccpNetworkEntityId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->sccpNetworkEntityId, &StorageTmp->sccpNetworkEntityIdLen);
	if (StorageTmp->sccpNetworkEntityId == NULL) {
		config_perror("invalid specification for sccpNetworkEntityId");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->tcapOmMessageType, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->tcapOmInterval, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmMsgs5minIntMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmMsgs5minIntMessagesRecv, &tmpsize);
	tcapOmMsgs5minIntTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
}

/*
 * store_tcapOmMsgs5minIntTable(): store configuraiton file for tcapOmMsgs5minIntTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_tcapOmMsgs5minIntTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct tcapOmMsgs5minIntTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	(void) majorID;
	(void) minorID;
	(void) serverarg;
	(void) clientarg;
	DEBUGMSGTL(("tcapOmMIB", "store_tcapOmMsgs5minIntTable: storing data...  "));
	refresh_tcapOmMsgs5minIntTable(1);
	(void) tmpsize;
	for (hcindex = tcapOmMsgs5minIntTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct tcapOmMsgs5minIntTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "tcapOmMsgs5minIntTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->sccpNetworkEntityId, &StorageTmp->sccpNetworkEntityIdLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->tcapOmMessageType, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->tcapOmInterval, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmMsgs5minIntMessagesSent, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmMsgs5minIntMessagesRecv, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct tcapOmMsgs15minIntTable_data *tcapOmMsgs15minIntTable_create(void)
 * @brief create a fresh data structure representing a new row in the tcapOmMsgs15minIntTable table.
 *
 * Creates a new tcapOmMsgs15minIntTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct tcapOmMsgs15minIntTable_data *
tcapOmMsgs15minIntTable_create(void)
{
	struct tcapOmMsgs15minIntTable_data *StorageNew = SNMP_MALLOC_STRUCT(tcapOmMsgs15minIntTable_data);

	DEBUGMSGTL(("tcapOmMIB", "tcapOmMsgs15minIntTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->sccpNetworkEntityId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->sccpNetworkEntityIdLen = 0;
		StorageNew->sccpNetworkEntityId[StorageNew->sccpNetworkEntityIdLen] = 0;
		StorageNew->tcapOmMessageType = 0;
		StorageNew->tcapOmInterval = 0;
		StorageNew->tcapOmMsgs15minIntMessagesSent = 0;
		StorageNew->tcapOmMsgs15minIntMessagesRecv = 0;
	}
      done:
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	tcapOmMsgs15minIntTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct tcapOmMsgs15minIntTable_data *tcapOmMsgs15minIntTable_duplicate(struct tcapOmMsgs15minIntTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct tcapOmMsgs15minIntTable_data *
tcapOmMsgs15minIntTable_duplicate(struct tcapOmMsgs15minIntTable_data *thedata)
{
	struct tcapOmMsgs15minIntTable_data *StorageNew = SNMP_MALLOC_STRUCT(tcapOmMsgs15minIntTable_data);

	DEBUGMSGTL(("tcapOmMIB", "tcapOmMsgs15minIntTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->tcapOmMsgs15minIntTable_id = thedata->tcapOmMsgs15minIntTable_id;
		if (!(StorageNew->sccpNetworkEntityId = malloc(thedata->sccpNetworkEntityIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->sccpNetworkEntityId, thedata->sccpNetworkEntityId, thedata->sccpNetworkEntityIdLen);
		StorageNew->sccpNetworkEntityIdLen = thedata->sccpNetworkEntityIdLen;
		StorageNew->sccpNetworkEntityId[StorageNew->sccpNetworkEntityIdLen] = 0;
		StorageNew->tcapOmMessageType = thedata->tcapOmMessageType;
		StorageNew->tcapOmInterval = thedata->tcapOmInterval;
		StorageNew->tcapOmMsgs15minIntMessagesSent = thedata->tcapOmMsgs15minIntMessagesSent;
		StorageNew->tcapOmMsgs15minIntMessagesRecv = thedata->tcapOmMsgs15minIntMessagesRecv;
	}
      done:
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	tcapOmMsgs15minIntTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int tcapOmMsgs15minIntTable_destroy(struct tcapOmMsgs15minIntTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
tcapOmMsgs15minIntTable_destroy(struct tcapOmMsgs15minIntTable_data **thedata)
{
	struct tcapOmMsgs15minIntTable_data *StorageDel;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmMsgs15minIntTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->sccpNetworkEntityId);
		StorageDel->sccpNetworkEntityIdLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int tcapOmMsgs15minIntTable_add(struct tcapOmMsgs15minIntTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the tcapOmMsgs15minIntTable table data set.
 *
 * Adds a table row structure to the tcapOmMsgs15minIntTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
tcapOmMsgs15minIntTable_add(struct tcapOmMsgs15minIntTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmMsgs15minIntTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* sccpNetworkEntityId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->sccpNetworkEntityId, thedata->sccpNetworkEntityIdLen);
		/* tcapOmMessageType */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->tcapOmMessageType, sizeof(thedata->tcapOmMessageType));
		/* tcapOmInterval */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->tcapOmInterval, sizeof(thedata->tcapOmInterval));
		header_complex_add_data(&tcapOmMsgs15minIntTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("tcapOmMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int tcapOmMsgs15minIntTable_del(struct tcapOmMsgs15minIntTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the tcapOmMsgs15minIntTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
tcapOmMsgs15minIntTable_del(struct tcapOmMsgs15minIntTable_data *thedata)
{
	struct tcapOmMsgs15minIntTable_data *StorageDel;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmMsgs15minIntTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(tcapOmMsgs15minIntTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&tcapOmMsgs15minIntTableStorage, hciptr);
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_tcapOmMsgs15minIntTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for tcapOmMsgs15minIntTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case tcapOmMsgs15minIntTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_tcapOmMsgs15minIntTable(const char *token, char *line)
{
	size_t tmpsize;
	struct tcapOmMsgs15minIntTable_data *StorageTmp = tcapOmMsgs15minIntTable_create();

	(void) token;
	(void) line;
	DEBUGMSGTL(("tcapOmMIB", "parse_tcapOmMsgs15minIntTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->sccpNetworkEntityId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->sccpNetworkEntityId, &StorageTmp->sccpNetworkEntityIdLen);
	if (StorageTmp->sccpNetworkEntityId == NULL) {
		config_perror("invalid specification for sccpNetworkEntityId");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->tcapOmMessageType, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->tcapOmInterval, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmMsgs15minIntMessagesSent, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmMsgs15minIntMessagesRecv, &tmpsize);
	tcapOmMsgs15minIntTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
}

/*
 * store_tcapOmMsgs15minIntTable(): store configuraiton file for tcapOmMsgs15minIntTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_tcapOmMsgs15minIntTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct tcapOmMsgs15minIntTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	(void) majorID;
	(void) minorID;
	(void) serverarg;
	(void) clientarg;
	DEBUGMSGTL(("tcapOmMIB", "store_tcapOmMsgs15minIntTable: storing data...  "));
	refresh_tcapOmMsgs15minIntTable(1);
	(void) tmpsize;
	for (hcindex = tcapOmMsgs15minIntTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct tcapOmMsgs15minIntTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "tcapOmMsgs15minIntTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->sccpNetworkEntityId, &StorageTmp->sccpNetworkEntityIdLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->tcapOmMessageType, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->tcapOmInterval, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmMsgs15minIntMessagesSent, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmMsgs15minIntMessagesRecv, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct tcapOmAETable_data *tcapOmAETable_create(void)
 * @brief create a fresh data structure representing a new row in the tcapOmAETable table.
 *
 * Creates a new tcapOmAETable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct tcapOmAETable_data *
tcapOmAETable_create(void)
{
	struct tcapOmAETable_data *StorageNew = SNMP_MALLOC_STRUCT(tcapOmAETable_data);

	DEBUGMSGTL(("tcapOmMIB", "tcapOmAETable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->tcapApplicationSubsystemId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->tcapApplicationSubsystemIdLen = 0;
		StorageNew->tcapApplicationSubsystemId[StorageNew->tcapApplicationSubsystemIdLen] = 0;
		if ((StorageNew->tcapApplicationEntityId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->tcapApplicationEntityIdLen = 0;
		StorageNew->tcapApplicationEntityId[StorageNew->tcapApplicationEntityIdLen] = 0;
		StorageNew->tcapOmAEMeasurementDeleted = 0;
		StorageNew->tcapOmAENewTransactions = 0;
		StorageNew->tcapOmAEMeanOpenTransactions = 0;
		StorageNew->tcapOmAENotUsed = 0;
		StorageNew->tcapOmAECummulativeMeanDuration = 0;
		StorageNew->tcapOmAEMaximumOpenTranactions = 0;
		StorageNew->tcapOmAEThresholdExceeded = 0;
		StorageNew->tcapOmAE5minValidIntervals = 0;
		StorageNew->tcapOmAE15minValidIntervals = 0;
	}
      done:
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	tcapOmAETable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct tcapOmAETable_data *tcapOmAETable_duplicate(struct tcapOmAETable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct tcapOmAETable_data *
tcapOmAETable_duplicate(struct tcapOmAETable_data *thedata)
{
	struct tcapOmAETable_data *StorageNew = SNMP_MALLOC_STRUCT(tcapOmAETable_data);

	DEBUGMSGTL(("tcapOmMIB", "tcapOmAETable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->tcapOmAETable_id = thedata->tcapOmAETable_id;
		if (!(StorageNew->tcapApplicationSubsystemId = malloc(thedata->tcapApplicationSubsystemIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->tcapApplicationSubsystemId, thedata->tcapApplicationSubsystemId, thedata->tcapApplicationSubsystemIdLen);
		StorageNew->tcapApplicationSubsystemIdLen = thedata->tcapApplicationSubsystemIdLen;
		StorageNew->tcapApplicationSubsystemId[StorageNew->tcapApplicationSubsystemIdLen] = 0;
		if (!(StorageNew->tcapApplicationEntityId = malloc(thedata->tcapApplicationEntityIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->tcapApplicationEntityId, thedata->tcapApplicationEntityId, thedata->tcapApplicationEntityIdLen);
		StorageNew->tcapApplicationEntityIdLen = thedata->tcapApplicationEntityIdLen;
		StorageNew->tcapApplicationEntityId[StorageNew->tcapApplicationEntityIdLen] = 0;
		StorageNew->tcapOmAEMeasurementDeleted = thedata->tcapOmAEMeasurementDeleted;
		StorageNew->tcapOmAENewTransactions = thedata->tcapOmAENewTransactions;
		StorageNew->tcapOmAEMeanOpenTransactions = thedata->tcapOmAEMeanOpenTransactions;
		StorageNew->tcapOmAENotUsed = thedata->tcapOmAENotUsed;
		StorageNew->tcapOmAECummulativeMeanDuration = thedata->tcapOmAECummulativeMeanDuration;
		StorageNew->tcapOmAEMaximumOpenTranactions = thedata->tcapOmAEMaximumOpenTranactions;
		StorageNew->tcapOmAEThresholdExceeded = thedata->tcapOmAEThresholdExceeded;
		StorageNew->tcapOmAE5minValidIntervals = thedata->tcapOmAE5minValidIntervals;
		StorageNew->tcapOmAE15minValidIntervals = thedata->tcapOmAE15minValidIntervals;
	}
      done:
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	tcapOmAETable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int tcapOmAETable_destroy(struct tcapOmAETable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
tcapOmAETable_destroy(struct tcapOmAETable_data **thedata)
{
	struct tcapOmAETable_data *StorageDel;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmAETable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->tcapApplicationSubsystemId);
		StorageDel->tcapApplicationSubsystemIdLen = 0;
		SNMP_FREE(StorageDel->tcapApplicationEntityId);
		StorageDel->tcapApplicationEntityIdLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int tcapOmAETable_add(struct tcapOmAETable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the tcapOmAETable table data set.
 *
 * Adds a table row structure to the tcapOmAETable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
tcapOmAETable_add(struct tcapOmAETable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmAETable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* tcapApplicationSubsystemId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->tcapApplicationSubsystemId, thedata->tcapApplicationSubsystemIdLen);
		/* tcapApplicationEntityId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->tcapApplicationEntityId, thedata->tcapApplicationEntityIdLen);
		header_complex_add_data(&tcapOmAETableStorage, vars, thedata);
	}
	DEBUGMSGTL(("tcapOmMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int tcapOmAETable_del(struct tcapOmAETable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the tcapOmAETable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
tcapOmAETable_del(struct tcapOmAETable_data *thedata)
{
	struct tcapOmAETable_data *StorageDel;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmAETable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(tcapOmAETableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&tcapOmAETableStorage, hciptr);
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_tcapOmAETable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for tcapOmAETable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case tcapOmAETable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_tcapOmAETable(const char *token, char *line)
{
	size_t tmpsize;
	struct tcapOmAETable_data *StorageTmp = tcapOmAETable_create();

	(void) token;
	(void) line;
	DEBUGMSGTL(("tcapOmMIB", "parse_tcapOmAETable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->tcapApplicationSubsystemId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->tcapApplicationSubsystemId, &StorageTmp->tcapApplicationSubsystemIdLen);
	if (StorageTmp->tcapApplicationSubsystemId == NULL) {
		config_perror("invalid specification for tcapApplicationSubsystemId");
		return;
	}
	SNMP_FREE(StorageTmp->tcapApplicationEntityId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->tcapApplicationEntityId, &StorageTmp->tcapApplicationEntityIdLen);
	if (StorageTmp->tcapApplicationEntityId == NULL) {
		config_perror("invalid specification for tcapApplicationEntityId");
		return;
	}
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmAEMeasurementDeleted, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmAENewTransactions, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmAEMeanOpenTransactions, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmAENotUsed, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmAECummulativeMeanDuration, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmAEMaximumOpenTranactions, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmAEThresholdExceeded, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->tcapOmAE5minValidIntervals, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->tcapOmAE15minValidIntervals, &tmpsize);
	tcapOmAETable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
}

/*
 * store_tcapOmAETable(): store configuraiton file for tcapOmAETable
 * stores .conf file entries needed to configure the mib.
 */
int
store_tcapOmAETable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct tcapOmAETable_data *StorageTmp;
	struct header_complex_index *hcindex;

	(void) majorID;
	(void) minorID;
	(void) serverarg;
	(void) clientarg;
	DEBUGMSGTL(("tcapOmMIB", "store_tcapOmAETable: storing data...  "));
	refresh_tcapOmAETable(1);
	(void) tmpsize;
	for (hcindex = tcapOmAETableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct tcapOmAETable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "tcapOmAETable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->tcapApplicationSubsystemId, &StorageTmp->tcapApplicationSubsystemIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->tcapApplicationEntityId, &StorageTmp->tcapApplicationEntityIdLen);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmAEMeasurementDeleted, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmAENewTransactions, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmAEMeanOpenTransactions, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmAENotUsed, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmAECummulativeMeanDuration, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmAEMaximumOpenTranactions, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmAEThresholdExceeded, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->tcapOmAE5minValidIntervals, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->tcapOmAE15minValidIntervals, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct tcapOmAECurrentTable_data *tcapOmAECurrentTable_create(void)
 * @brief create a fresh data structure representing a new row in the tcapOmAECurrentTable table.
 *
 * Creates a new tcapOmAECurrentTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct tcapOmAECurrentTable_data *
tcapOmAECurrentTable_create(void)
{
	struct tcapOmAECurrentTable_data *StorageNew = SNMP_MALLOC_STRUCT(tcapOmAECurrentTable_data);

	DEBUGMSGTL(("tcapOmMIB", "tcapOmAECurrentTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->tcapApplicationSubsystemId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->tcapApplicationSubsystemIdLen = 0;
		StorageNew->tcapApplicationSubsystemId[StorageNew->tcapApplicationSubsystemIdLen] = 0;
		if ((StorageNew->tcapApplicationEntityId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->tcapApplicationEntityIdLen = 0;
		StorageNew->tcapApplicationEntityId[StorageNew->tcapApplicationEntityIdLen] = 0;
		StorageNew->tcapOmAECurrentMeasurementDeleted = 0;
		StorageNew->tcapOmAECurrentNewTransactions = 0;
		StorageNew->tcapOmAECurrentMeanOpenTransactions = 0;
		StorageNew->tcapOmAECurrentNotUsed = 0;
		StorageNew->tcapOmAECurrentCummulativeMeanDuration = 0;
		StorageNew->tcapOmAECurrentMaximumOpenTranactions = 0;
		StorageNew->tcapOmAECurrentThresholdExceeded = 0;
	}
      done:
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	tcapOmAECurrentTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct tcapOmAECurrentTable_data *tcapOmAECurrentTable_duplicate(struct tcapOmAECurrentTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct tcapOmAECurrentTable_data *
tcapOmAECurrentTable_duplicate(struct tcapOmAECurrentTable_data *thedata)
{
	struct tcapOmAECurrentTable_data *StorageNew = SNMP_MALLOC_STRUCT(tcapOmAECurrentTable_data);

	DEBUGMSGTL(("tcapOmMIB", "tcapOmAECurrentTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->tcapOmAECurrentTable_id = thedata->tcapOmAECurrentTable_id;
		if (!(StorageNew->tcapApplicationSubsystemId = malloc(thedata->tcapApplicationSubsystemIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->tcapApplicationSubsystemId, thedata->tcapApplicationSubsystemId, thedata->tcapApplicationSubsystemIdLen);
		StorageNew->tcapApplicationSubsystemIdLen = thedata->tcapApplicationSubsystemIdLen;
		StorageNew->tcapApplicationSubsystemId[StorageNew->tcapApplicationSubsystemIdLen] = 0;
		if (!(StorageNew->tcapApplicationEntityId = malloc(thedata->tcapApplicationEntityIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->tcapApplicationEntityId, thedata->tcapApplicationEntityId, thedata->tcapApplicationEntityIdLen);
		StorageNew->tcapApplicationEntityIdLen = thedata->tcapApplicationEntityIdLen;
		StorageNew->tcapApplicationEntityId[StorageNew->tcapApplicationEntityIdLen] = 0;
		StorageNew->tcapOmAECurrentMeasurementDeleted = thedata->tcapOmAECurrentMeasurementDeleted;
		StorageNew->tcapOmAECurrentNewTransactions = thedata->tcapOmAECurrentNewTransactions;
		StorageNew->tcapOmAECurrentMeanOpenTransactions = thedata->tcapOmAECurrentMeanOpenTransactions;
		StorageNew->tcapOmAECurrentNotUsed = thedata->tcapOmAECurrentNotUsed;
		StorageNew->tcapOmAECurrentCummulativeMeanDuration = thedata->tcapOmAECurrentCummulativeMeanDuration;
		StorageNew->tcapOmAECurrentMaximumOpenTranactions = thedata->tcapOmAECurrentMaximumOpenTranactions;
		StorageNew->tcapOmAECurrentThresholdExceeded = thedata->tcapOmAECurrentThresholdExceeded;
	}
      done:
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	tcapOmAECurrentTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int tcapOmAECurrentTable_destroy(struct tcapOmAECurrentTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
tcapOmAECurrentTable_destroy(struct tcapOmAECurrentTable_data **thedata)
{
	struct tcapOmAECurrentTable_data *StorageDel;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmAECurrentTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->tcapApplicationSubsystemId);
		StorageDel->tcapApplicationSubsystemIdLen = 0;
		SNMP_FREE(StorageDel->tcapApplicationEntityId);
		StorageDel->tcapApplicationEntityIdLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int tcapOmAECurrentTable_add(struct tcapOmAECurrentTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the tcapOmAECurrentTable table data set.
 *
 * Adds a table row structure to the tcapOmAECurrentTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
tcapOmAECurrentTable_add(struct tcapOmAECurrentTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmAECurrentTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* tcapApplicationSubsystemId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->tcapApplicationSubsystemId, thedata->tcapApplicationSubsystemIdLen);
		/* tcapApplicationEntityId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->tcapApplicationEntityId, thedata->tcapApplicationEntityIdLen);
		header_complex_add_data(&tcapOmAECurrentTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("tcapOmMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int tcapOmAECurrentTable_del(struct tcapOmAECurrentTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the tcapOmAECurrentTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
tcapOmAECurrentTable_del(struct tcapOmAECurrentTable_data *thedata)
{
	struct tcapOmAECurrentTable_data *StorageDel;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmAECurrentTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(tcapOmAECurrentTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&tcapOmAECurrentTableStorage, hciptr);
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_tcapOmAECurrentTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for tcapOmAECurrentTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case tcapOmAECurrentTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_tcapOmAECurrentTable(const char *token, char *line)
{
	size_t tmpsize;
	struct tcapOmAECurrentTable_data *StorageTmp = tcapOmAECurrentTable_create();

	(void) token;
	(void) line;
	DEBUGMSGTL(("tcapOmMIB", "parse_tcapOmAECurrentTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->tcapApplicationSubsystemId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->tcapApplicationSubsystemId, &StorageTmp->tcapApplicationSubsystemIdLen);
	if (StorageTmp->tcapApplicationSubsystemId == NULL) {
		config_perror("invalid specification for tcapApplicationSubsystemId");
		return;
	}
	SNMP_FREE(StorageTmp->tcapApplicationEntityId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->tcapApplicationEntityId, &StorageTmp->tcapApplicationEntityIdLen);
	if (StorageTmp->tcapApplicationEntityId == NULL) {
		config_perror("invalid specification for tcapApplicationEntityId");
		return;
	}
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmAECurrentMeasurementDeleted, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmAECurrentNewTransactions, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmAECurrentMeanOpenTransactions, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmAECurrentNotUsed, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmAECurrentCummulativeMeanDuration, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmAECurrentMaximumOpenTranactions, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmAECurrentThresholdExceeded, &tmpsize);
	tcapOmAECurrentTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
}

/*
 * store_tcapOmAECurrentTable(): store configuraiton file for tcapOmAECurrentTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_tcapOmAECurrentTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct tcapOmAECurrentTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	(void) majorID;
	(void) minorID;
	(void) serverarg;
	(void) clientarg;
	DEBUGMSGTL(("tcapOmMIB", "store_tcapOmAECurrentTable: storing data...  "));
	refresh_tcapOmAECurrentTable(1);
	(void) tmpsize;
	for (hcindex = tcapOmAECurrentTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct tcapOmAECurrentTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "tcapOmAECurrentTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->tcapApplicationSubsystemId, &StorageTmp->tcapApplicationSubsystemIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->tcapApplicationEntityId, &StorageTmp->tcapApplicationEntityIdLen);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmAECurrentMeasurementDeleted, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmAECurrentNewTransactions, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmAECurrentMeanOpenTransactions, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmAECurrentNotUsed, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmAECurrentCummulativeMeanDuration, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmAECurrentMaximumOpenTranactions, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmAECurrentThresholdExceeded, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct tcapOmAE5minIntTable_data *tcapOmAE5minIntTable_create(void)
 * @brief create a fresh data structure representing a new row in the tcapOmAE5minIntTable table.
 *
 * Creates a new tcapOmAE5minIntTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct tcapOmAE5minIntTable_data *
tcapOmAE5minIntTable_create(void)
{
	struct tcapOmAE5minIntTable_data *StorageNew = SNMP_MALLOC_STRUCT(tcapOmAE5minIntTable_data);

	DEBUGMSGTL(("tcapOmMIB", "tcapOmAE5minIntTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->tcapApplicationSubsystemId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->tcapApplicationSubsystemIdLen = 0;
		StorageNew->tcapApplicationSubsystemId[StorageNew->tcapApplicationSubsystemIdLen] = 0;
		if ((StorageNew->tcapApplicationEntityId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->tcapApplicationEntityIdLen = 0;
		StorageNew->tcapApplicationEntityId[StorageNew->tcapApplicationEntityIdLen] = 0;
		StorageNew->tcapOmInterval = 0;
		StorageNew->tcapOmAE5minIntMeasurementDeleted = 0;
		StorageNew->tcapOmAE5minIntNewTransactions = 0;
		StorageNew->tcapOmAE5minIntMeanOpenTransactions = 0;
		StorageNew->tcapOmAE5minIntNotUsed = 0;
		StorageNew->tcapOmAE5minIntCummulativeMeanDuration = 0;
		StorageNew->tcapOmAE5minIntMaximumOpenTranactions = 0;
		StorageNew->tcapOmAE5minIntThresholdExceeded = 0;
	}
      done:
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	tcapOmAE5minIntTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct tcapOmAE5minIntTable_data *tcapOmAE5minIntTable_duplicate(struct tcapOmAE5minIntTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct tcapOmAE5minIntTable_data *
tcapOmAE5minIntTable_duplicate(struct tcapOmAE5minIntTable_data *thedata)
{
	struct tcapOmAE5minIntTable_data *StorageNew = SNMP_MALLOC_STRUCT(tcapOmAE5minIntTable_data);

	DEBUGMSGTL(("tcapOmMIB", "tcapOmAE5minIntTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->tcapOmAE5minIntTable_id = thedata->tcapOmAE5minIntTable_id;
		if (!(StorageNew->tcapApplicationSubsystemId = malloc(thedata->tcapApplicationSubsystemIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->tcapApplicationSubsystemId, thedata->tcapApplicationSubsystemId, thedata->tcapApplicationSubsystemIdLen);
		StorageNew->tcapApplicationSubsystemIdLen = thedata->tcapApplicationSubsystemIdLen;
		StorageNew->tcapApplicationSubsystemId[StorageNew->tcapApplicationSubsystemIdLen] = 0;
		if (!(StorageNew->tcapApplicationEntityId = malloc(thedata->tcapApplicationEntityIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->tcapApplicationEntityId, thedata->tcapApplicationEntityId, thedata->tcapApplicationEntityIdLen);
		StorageNew->tcapApplicationEntityIdLen = thedata->tcapApplicationEntityIdLen;
		StorageNew->tcapApplicationEntityId[StorageNew->tcapApplicationEntityIdLen] = 0;
		StorageNew->tcapOmInterval = thedata->tcapOmInterval;
		StorageNew->tcapOmAE5minIntMeasurementDeleted = thedata->tcapOmAE5minIntMeasurementDeleted;
		StorageNew->tcapOmAE5minIntNewTransactions = thedata->tcapOmAE5minIntNewTransactions;
		StorageNew->tcapOmAE5minIntMeanOpenTransactions = thedata->tcapOmAE5minIntMeanOpenTransactions;
		StorageNew->tcapOmAE5minIntNotUsed = thedata->tcapOmAE5minIntNotUsed;
		StorageNew->tcapOmAE5minIntCummulativeMeanDuration = thedata->tcapOmAE5minIntCummulativeMeanDuration;
		StorageNew->tcapOmAE5minIntMaximumOpenTranactions = thedata->tcapOmAE5minIntMaximumOpenTranactions;
		StorageNew->tcapOmAE5minIntThresholdExceeded = thedata->tcapOmAE5minIntThresholdExceeded;
	}
      done:
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	tcapOmAE5minIntTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int tcapOmAE5minIntTable_destroy(struct tcapOmAE5minIntTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
tcapOmAE5minIntTable_destroy(struct tcapOmAE5minIntTable_data **thedata)
{
	struct tcapOmAE5minIntTable_data *StorageDel;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmAE5minIntTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->tcapApplicationSubsystemId);
		StorageDel->tcapApplicationSubsystemIdLen = 0;
		SNMP_FREE(StorageDel->tcapApplicationEntityId);
		StorageDel->tcapApplicationEntityIdLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int tcapOmAE5minIntTable_add(struct tcapOmAE5minIntTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the tcapOmAE5minIntTable table data set.
 *
 * Adds a table row structure to the tcapOmAE5minIntTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
tcapOmAE5minIntTable_add(struct tcapOmAE5minIntTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmAE5minIntTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* tcapApplicationSubsystemId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->tcapApplicationSubsystemId, thedata->tcapApplicationSubsystemIdLen);
		/* tcapApplicationEntityId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->tcapApplicationEntityId, thedata->tcapApplicationEntityIdLen);
		/* tcapOmInterval */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->tcapOmInterval, sizeof(thedata->tcapOmInterval));
		header_complex_add_data(&tcapOmAE5minIntTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("tcapOmMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int tcapOmAE5minIntTable_del(struct tcapOmAE5minIntTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the tcapOmAE5minIntTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
tcapOmAE5minIntTable_del(struct tcapOmAE5minIntTable_data *thedata)
{
	struct tcapOmAE5minIntTable_data *StorageDel;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmAE5minIntTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(tcapOmAE5minIntTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&tcapOmAE5minIntTableStorage, hciptr);
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_tcapOmAE5minIntTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for tcapOmAE5minIntTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case tcapOmAE5minIntTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_tcapOmAE5minIntTable(const char *token, char *line)
{
	size_t tmpsize;
	struct tcapOmAE5minIntTable_data *StorageTmp = tcapOmAE5minIntTable_create();

	(void) token;
	(void) line;
	DEBUGMSGTL(("tcapOmMIB", "parse_tcapOmAE5minIntTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->tcapApplicationSubsystemId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->tcapApplicationSubsystemId, &StorageTmp->tcapApplicationSubsystemIdLen);
	if (StorageTmp->tcapApplicationSubsystemId == NULL) {
		config_perror("invalid specification for tcapApplicationSubsystemId");
		return;
	}
	SNMP_FREE(StorageTmp->tcapApplicationEntityId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->tcapApplicationEntityId, &StorageTmp->tcapApplicationEntityIdLen);
	if (StorageTmp->tcapApplicationEntityId == NULL) {
		config_perror("invalid specification for tcapApplicationEntityId");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->tcapOmInterval, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmAE5minIntMeasurementDeleted, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmAE5minIntNewTransactions, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmAE5minIntMeanOpenTransactions, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmAE5minIntNotUsed, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmAE5minIntCummulativeMeanDuration, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmAE5minIntMaximumOpenTranactions, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmAE5minIntThresholdExceeded, &tmpsize);
	tcapOmAE5minIntTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
}

/*
 * store_tcapOmAE5minIntTable(): store configuraiton file for tcapOmAE5minIntTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_tcapOmAE5minIntTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct tcapOmAE5minIntTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	(void) majorID;
	(void) minorID;
	(void) serverarg;
	(void) clientarg;
	DEBUGMSGTL(("tcapOmMIB", "store_tcapOmAE5minIntTable: storing data...  "));
	refresh_tcapOmAE5minIntTable(1);
	(void) tmpsize;
	for (hcindex = tcapOmAE5minIntTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct tcapOmAE5minIntTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "tcapOmAE5minIntTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->tcapApplicationSubsystemId, &StorageTmp->tcapApplicationSubsystemIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->tcapApplicationEntityId, &StorageTmp->tcapApplicationEntityIdLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->tcapOmInterval, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmAE5minIntMeasurementDeleted, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmAE5minIntNewTransactions, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmAE5minIntMeanOpenTransactions, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmAE5minIntNotUsed, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmAE5minIntCummulativeMeanDuration, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmAE5minIntMaximumOpenTranactions, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmAE5minIntThresholdExceeded, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct tcapOmAE15minIntTable_data *tcapOmAE15minIntTable_create(void)
 * @brief create a fresh data structure representing a new row in the tcapOmAE15minIntTable table.
 *
 * Creates a new tcapOmAE15minIntTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct tcapOmAE15minIntTable_data *
tcapOmAE15minIntTable_create(void)
{
	struct tcapOmAE15minIntTable_data *StorageNew = SNMP_MALLOC_STRUCT(tcapOmAE15minIntTable_data);

	DEBUGMSGTL(("tcapOmMIB", "tcapOmAE15minIntTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->tcapApplicationSubsystemId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->tcapApplicationSubsystemIdLen = 0;
		StorageNew->tcapApplicationSubsystemId[StorageNew->tcapApplicationSubsystemIdLen] = 0;
		if ((StorageNew->tcapApplicationEntityId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->tcapApplicationEntityIdLen = 0;
		StorageNew->tcapApplicationEntityId[StorageNew->tcapApplicationEntityIdLen] = 0;
		StorageNew->tcapOmInterval = 0;
		StorageNew->tcapOmAE15minIntMeasurementDeleted = 0;
		StorageNew->tcapOmAE15minIntNewTransactions = 0;
		StorageNew->tcapOmAE15minIntMeanOpenTransactions = 0;
		StorageNew->tcapOmAE15minIntNotUsed = 0;
		StorageNew->tcapOmAE15minIntCummulativeMeanDuration = 0;
		StorageNew->tcapOmAE15minIntMaximumOpenTranactions = 0;
		StorageNew->tcapOmAE15minIntThresholdExceeded = 0;
	}
      done:
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	tcapOmAE15minIntTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct tcapOmAE15minIntTable_data *tcapOmAE15minIntTable_duplicate(struct tcapOmAE15minIntTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct tcapOmAE15minIntTable_data *
tcapOmAE15minIntTable_duplicate(struct tcapOmAE15minIntTable_data *thedata)
{
	struct tcapOmAE15minIntTable_data *StorageNew = SNMP_MALLOC_STRUCT(tcapOmAE15minIntTable_data);

	DEBUGMSGTL(("tcapOmMIB", "tcapOmAE15minIntTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->tcapOmAE15minIntTable_id = thedata->tcapOmAE15minIntTable_id;
		if (!(StorageNew->tcapApplicationSubsystemId = malloc(thedata->tcapApplicationSubsystemIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->tcapApplicationSubsystemId, thedata->tcapApplicationSubsystemId, thedata->tcapApplicationSubsystemIdLen);
		StorageNew->tcapApplicationSubsystemIdLen = thedata->tcapApplicationSubsystemIdLen;
		StorageNew->tcapApplicationSubsystemId[StorageNew->tcapApplicationSubsystemIdLen] = 0;
		if (!(StorageNew->tcapApplicationEntityId = malloc(thedata->tcapApplicationEntityIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->tcapApplicationEntityId, thedata->tcapApplicationEntityId, thedata->tcapApplicationEntityIdLen);
		StorageNew->tcapApplicationEntityIdLen = thedata->tcapApplicationEntityIdLen;
		StorageNew->tcapApplicationEntityId[StorageNew->tcapApplicationEntityIdLen] = 0;
		StorageNew->tcapOmInterval = thedata->tcapOmInterval;
		StorageNew->tcapOmAE15minIntMeasurementDeleted = thedata->tcapOmAE15minIntMeasurementDeleted;
		StorageNew->tcapOmAE15minIntNewTransactions = thedata->tcapOmAE15minIntNewTransactions;
		StorageNew->tcapOmAE15minIntMeanOpenTransactions = thedata->tcapOmAE15minIntMeanOpenTransactions;
		StorageNew->tcapOmAE15minIntNotUsed = thedata->tcapOmAE15minIntNotUsed;
		StorageNew->tcapOmAE15minIntCummulativeMeanDuration = thedata->tcapOmAE15minIntCummulativeMeanDuration;
		StorageNew->tcapOmAE15minIntMaximumOpenTranactions = thedata->tcapOmAE15minIntMaximumOpenTranactions;
		StorageNew->tcapOmAE15minIntThresholdExceeded = thedata->tcapOmAE15minIntThresholdExceeded;
	}
      done:
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	tcapOmAE15minIntTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int tcapOmAE15minIntTable_destroy(struct tcapOmAE15minIntTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
tcapOmAE15minIntTable_destroy(struct tcapOmAE15minIntTable_data **thedata)
{
	struct tcapOmAE15minIntTable_data *StorageDel;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmAE15minIntTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->tcapApplicationSubsystemId);
		StorageDel->tcapApplicationSubsystemIdLen = 0;
		SNMP_FREE(StorageDel->tcapApplicationEntityId);
		StorageDel->tcapApplicationEntityIdLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int tcapOmAE15minIntTable_add(struct tcapOmAE15minIntTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the tcapOmAE15minIntTable table data set.
 *
 * Adds a table row structure to the tcapOmAE15minIntTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
tcapOmAE15minIntTable_add(struct tcapOmAE15minIntTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmAE15minIntTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* tcapApplicationSubsystemId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->tcapApplicationSubsystemId, thedata->tcapApplicationSubsystemIdLen);
		/* tcapApplicationEntityId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->tcapApplicationEntityId, thedata->tcapApplicationEntityIdLen);
		/* tcapOmInterval */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->tcapOmInterval, sizeof(thedata->tcapOmInterval));
		header_complex_add_data(&tcapOmAE15minIntTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("tcapOmMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int tcapOmAE15minIntTable_del(struct tcapOmAE15minIntTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the tcapOmAE15minIntTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
tcapOmAE15minIntTable_del(struct tcapOmAE15minIntTable_data *thedata)
{
	struct tcapOmAE15minIntTable_data *StorageDel;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmAE15minIntTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(tcapOmAE15minIntTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&tcapOmAE15minIntTableStorage, hciptr);
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_tcapOmAE15minIntTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for tcapOmAE15minIntTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case tcapOmAE15minIntTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_tcapOmAE15minIntTable(const char *token, char *line)
{
	size_t tmpsize;
	struct tcapOmAE15minIntTable_data *StorageTmp = tcapOmAE15minIntTable_create();

	(void) token;
	(void) line;
	DEBUGMSGTL(("tcapOmMIB", "parse_tcapOmAE15minIntTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->tcapApplicationSubsystemId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->tcapApplicationSubsystemId, &StorageTmp->tcapApplicationSubsystemIdLen);
	if (StorageTmp->tcapApplicationSubsystemId == NULL) {
		config_perror("invalid specification for tcapApplicationSubsystemId");
		return;
	}
	SNMP_FREE(StorageTmp->tcapApplicationEntityId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->tcapApplicationEntityId, &StorageTmp->tcapApplicationEntityIdLen);
	if (StorageTmp->tcapApplicationEntityId == NULL) {
		config_perror("invalid specification for tcapApplicationEntityId");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->tcapOmInterval, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmAE15minIntMeasurementDeleted, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmAE15minIntNewTransactions, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmAE15minIntMeanOpenTransactions, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmAE15minIntNotUsed, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmAE15minIntCummulativeMeanDuration, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmAE15minIntMaximumOpenTranactions, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmAE15minIntThresholdExceeded, &tmpsize);
	tcapOmAE15minIntTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
}

/*
 * store_tcapOmAE15minIntTable(): store configuraiton file for tcapOmAE15minIntTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_tcapOmAE15minIntTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct tcapOmAE15minIntTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	(void) majorID;
	(void) minorID;
	(void) serverarg;
	(void) clientarg;
	DEBUGMSGTL(("tcapOmMIB", "store_tcapOmAE15minIntTable: storing data...  "));
	refresh_tcapOmAE15minIntTable(1);
	(void) tmpsize;
	for (hcindex = tcapOmAE15minIntTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct tcapOmAE15minIntTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "tcapOmAE15minIntTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->tcapApplicationSubsystemId, &StorageTmp->tcapApplicationSubsystemIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->tcapApplicationEntityId, &StorageTmp->tcapApplicationEntityIdLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->tcapOmInterval, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmAE15minIntMeasurementDeleted, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmAE15minIntNewTransactions, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmAE15minIntMeanOpenTransactions, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmAE15minIntNotUsed, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmAE15minIntCummulativeMeanDuration, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmAE15minIntMaximumOpenTranactions, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmAE15minIntThresholdExceeded, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct tcapOmTcTable_data *tcapOmTcTable_create(void)
 * @brief create a fresh data structure representing a new row in the tcapOmTcTable table.
 *
 * Creates a new tcapOmTcTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct tcapOmTcTable_data *
tcapOmTcTable_create(void)
{
	struct tcapOmTcTable_data *StorageNew = SNMP_MALLOC_STRUCT(tcapOmTcTable_data);

	DEBUGMSGTL(("tcapOmMIB", "tcapOmTcTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->tcapApplicationSubsystemId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->tcapApplicationSubsystemIdLen = 0;
		StorageNew->tcapApplicationSubsystemId[StorageNew->tcapApplicationSubsystemIdLen] = 0;
		if ((StorageNew->tcapApplicationEntityId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->tcapApplicationEntityIdLen = 0;
		StorageNew->tcapApplicationEntityId[StorageNew->tcapApplicationEntityIdLen] = 0;
		StorageNew->tcapOmTcPabortRecvUnrecTid = 0;
		StorageNew->tcapOmTcPabortRecvResourceLim = 0;
		StorageNew->tcapOmTcRejectRecvResourceLim = 0;
		StorageNew->tcapOmTcPabortSentUnrecTid = 0;
		StorageNew->tcapOmTcPabortSentResourceLim = 0;
		StorageNew->tcapOmTcRejectSentResourceLim = 0;
		StorageNew->tcapOmTcTcUserCancel = 0;
		StorageNew->tcapOmTcMessagesDiscarded = 0;
		StorageNew->tcapOmTcNotUsed = 0;
		StorageNew->tcapOmTcPabortsReceived = 0;
		StorageNew->tcapOmTcTcUserRejectsReceived = 0;
		StorageNew->tcapOmTcTpErrors = 0;
		StorageNew->tcapOmTc5minValidIntervals = 0;
		StorageNew->tcapOmTc15minValidIntervals = 0;
	}
      done:
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	tcapOmTcTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct tcapOmTcTable_data *tcapOmTcTable_duplicate(struct tcapOmTcTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct tcapOmTcTable_data *
tcapOmTcTable_duplicate(struct tcapOmTcTable_data *thedata)
{
	struct tcapOmTcTable_data *StorageNew = SNMP_MALLOC_STRUCT(tcapOmTcTable_data);

	DEBUGMSGTL(("tcapOmMIB", "tcapOmTcTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->tcapOmTcTable_id = thedata->tcapOmTcTable_id;
		if (!(StorageNew->tcapApplicationSubsystemId = malloc(thedata->tcapApplicationSubsystemIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->tcapApplicationSubsystemId, thedata->tcapApplicationSubsystemId, thedata->tcapApplicationSubsystemIdLen);
		StorageNew->tcapApplicationSubsystemIdLen = thedata->tcapApplicationSubsystemIdLen;
		StorageNew->tcapApplicationSubsystemId[StorageNew->tcapApplicationSubsystemIdLen] = 0;
		if (!(StorageNew->tcapApplicationEntityId = malloc(thedata->tcapApplicationEntityIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->tcapApplicationEntityId, thedata->tcapApplicationEntityId, thedata->tcapApplicationEntityIdLen);
		StorageNew->tcapApplicationEntityIdLen = thedata->tcapApplicationEntityIdLen;
		StorageNew->tcapApplicationEntityId[StorageNew->tcapApplicationEntityIdLen] = 0;
		StorageNew->tcapOmTcPabortRecvUnrecTid = thedata->tcapOmTcPabortRecvUnrecTid;
		StorageNew->tcapOmTcPabortRecvResourceLim = thedata->tcapOmTcPabortRecvResourceLim;
		StorageNew->tcapOmTcRejectRecvResourceLim = thedata->tcapOmTcRejectRecvResourceLim;
		StorageNew->tcapOmTcPabortSentUnrecTid = thedata->tcapOmTcPabortSentUnrecTid;
		StorageNew->tcapOmTcPabortSentResourceLim = thedata->tcapOmTcPabortSentResourceLim;
		StorageNew->tcapOmTcRejectSentResourceLim = thedata->tcapOmTcRejectSentResourceLim;
		StorageNew->tcapOmTcTcUserCancel = thedata->tcapOmTcTcUserCancel;
		StorageNew->tcapOmTcMessagesDiscarded = thedata->tcapOmTcMessagesDiscarded;
		StorageNew->tcapOmTcNotUsed = thedata->tcapOmTcNotUsed;
		StorageNew->tcapOmTcPabortsReceived = thedata->tcapOmTcPabortsReceived;
		StorageNew->tcapOmTcTcUserRejectsReceived = thedata->tcapOmTcTcUserRejectsReceived;
		StorageNew->tcapOmTcTpErrors = thedata->tcapOmTcTpErrors;
		StorageNew->tcapOmTc5minValidIntervals = thedata->tcapOmTc5minValidIntervals;
		StorageNew->tcapOmTc15minValidIntervals = thedata->tcapOmTc15minValidIntervals;
	}
      done:
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	tcapOmTcTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int tcapOmTcTable_destroy(struct tcapOmTcTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
tcapOmTcTable_destroy(struct tcapOmTcTable_data **thedata)
{
	struct tcapOmTcTable_data *StorageDel;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmTcTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->tcapApplicationSubsystemId);
		StorageDel->tcapApplicationSubsystemIdLen = 0;
		SNMP_FREE(StorageDel->tcapApplicationEntityId);
		StorageDel->tcapApplicationEntityIdLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int tcapOmTcTable_add(struct tcapOmTcTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the tcapOmTcTable table data set.
 *
 * Adds a table row structure to the tcapOmTcTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
tcapOmTcTable_add(struct tcapOmTcTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmTcTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* tcapApplicationSubsystemId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->tcapApplicationSubsystemId, thedata->tcapApplicationSubsystemIdLen);
		/* tcapApplicationEntityId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->tcapApplicationEntityId, thedata->tcapApplicationEntityIdLen);
		header_complex_add_data(&tcapOmTcTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("tcapOmMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int tcapOmTcTable_del(struct tcapOmTcTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the tcapOmTcTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
tcapOmTcTable_del(struct tcapOmTcTable_data *thedata)
{
	struct tcapOmTcTable_data *StorageDel;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmTcTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(tcapOmTcTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&tcapOmTcTableStorage, hciptr);
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_tcapOmTcTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for tcapOmTcTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case tcapOmTcTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_tcapOmTcTable(const char *token, char *line)
{
	size_t tmpsize;
	struct tcapOmTcTable_data *StorageTmp = tcapOmTcTable_create();

	(void) token;
	(void) line;
	DEBUGMSGTL(("tcapOmMIB", "parse_tcapOmTcTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->tcapApplicationSubsystemId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->tcapApplicationSubsystemId, &StorageTmp->tcapApplicationSubsystemIdLen);
	if (StorageTmp->tcapApplicationSubsystemId == NULL) {
		config_perror("invalid specification for tcapApplicationSubsystemId");
		return;
	}
	SNMP_FREE(StorageTmp->tcapApplicationEntityId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->tcapApplicationEntityId, &StorageTmp->tcapApplicationEntityIdLen);
	if (StorageTmp->tcapApplicationEntityId == NULL) {
		config_perror("invalid specification for tcapApplicationEntityId");
		return;
	}
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmTcPabortRecvUnrecTid, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmTcPabortRecvResourceLim, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmTcRejectRecvResourceLim, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmTcPabortSentUnrecTid, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmTcPabortSentResourceLim, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmTcRejectSentResourceLim, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmTcTcUserCancel, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmTcMessagesDiscarded, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmTcNotUsed, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmTcPabortsReceived, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmTcTcUserRejectsReceived, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmTcTpErrors, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->tcapOmTc5minValidIntervals, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->tcapOmTc15minValidIntervals, &tmpsize);
	tcapOmTcTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
}

/*
 * store_tcapOmTcTable(): store configuraiton file for tcapOmTcTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_tcapOmTcTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct tcapOmTcTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	(void) majorID;
	(void) minorID;
	(void) serverarg;
	(void) clientarg;
	DEBUGMSGTL(("tcapOmMIB", "store_tcapOmTcTable: storing data...  "));
	refresh_tcapOmTcTable(1);
	(void) tmpsize;
	for (hcindex = tcapOmTcTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct tcapOmTcTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "tcapOmTcTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->tcapApplicationSubsystemId, &StorageTmp->tcapApplicationSubsystemIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->tcapApplicationEntityId, &StorageTmp->tcapApplicationEntityIdLen);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmTcPabortRecvUnrecTid, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmTcPabortRecvResourceLim, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmTcRejectRecvResourceLim, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmTcPabortSentUnrecTid, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmTcPabortSentResourceLim, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmTcRejectSentResourceLim, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmTcTcUserCancel, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmTcMessagesDiscarded, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmTcNotUsed, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmTcPabortsReceived, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmTcTcUserRejectsReceived, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmTcTpErrors, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->tcapOmTc5minValidIntervals, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->tcapOmTc15minValidIntervals, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct tcapOmTcCurrentTable_data *tcapOmTcCurrentTable_create(void)
 * @brief create a fresh data structure representing a new row in the tcapOmTcCurrentTable table.
 *
 * Creates a new tcapOmTcCurrentTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct tcapOmTcCurrentTable_data *
tcapOmTcCurrentTable_create(void)
{
	struct tcapOmTcCurrentTable_data *StorageNew = SNMP_MALLOC_STRUCT(tcapOmTcCurrentTable_data);

	DEBUGMSGTL(("tcapOmMIB", "tcapOmTcCurrentTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->tcapApplicationSubsystemId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->tcapApplicationSubsystemIdLen = 0;
		StorageNew->tcapApplicationSubsystemId[StorageNew->tcapApplicationSubsystemIdLen] = 0;
		if ((StorageNew->tcapApplicationEntityId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->tcapApplicationEntityIdLen = 0;
		StorageNew->tcapApplicationEntityId[StorageNew->tcapApplicationEntityIdLen] = 0;
		StorageNew->tcapOmTcCurrentPabortRecvUnrecTid = 0;
		StorageNew->tcapOmTcCurrentPabortRecvResourceLim = 0;
		StorageNew->tcapOmTcCurrentRejectRecvResourceLim = 0;
		StorageNew->tcapOmTcCurrentPabortSentUnrecTid = 0;
		StorageNew->tcapOmTcCurrentPabortSentResourceLim = 0;
		StorageNew->tcapOmTcCurrentRejectSentResourceLim = 0;
		StorageNew->tcapOmTcCurrentTcUserCancel = 0;
		StorageNew->tcapOmTcCurrentMessagesDiscarded = 0;
		StorageNew->tcapOmTcCurrentNotUsed = 0;
		StorageNew->tcapOmTcCurrentPabortsReceived = 0;
		StorageNew->tcapOmTcCurrentTcUserRejectsReceived = 0;
		StorageNew->tcapOmTcCurrentTpErrors = 0;
	}
      done:
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	tcapOmTcCurrentTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct tcapOmTcCurrentTable_data *tcapOmTcCurrentTable_duplicate(struct tcapOmTcCurrentTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct tcapOmTcCurrentTable_data *
tcapOmTcCurrentTable_duplicate(struct tcapOmTcCurrentTable_data *thedata)
{
	struct tcapOmTcCurrentTable_data *StorageNew = SNMP_MALLOC_STRUCT(tcapOmTcCurrentTable_data);

	DEBUGMSGTL(("tcapOmMIB", "tcapOmTcCurrentTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->tcapOmTcCurrentTable_id = thedata->tcapOmTcCurrentTable_id;
		if (!(StorageNew->tcapApplicationSubsystemId = malloc(thedata->tcapApplicationSubsystemIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->tcapApplicationSubsystemId, thedata->tcapApplicationSubsystemId, thedata->tcapApplicationSubsystemIdLen);
		StorageNew->tcapApplicationSubsystemIdLen = thedata->tcapApplicationSubsystemIdLen;
		StorageNew->tcapApplicationSubsystemId[StorageNew->tcapApplicationSubsystemIdLen] = 0;
		if (!(StorageNew->tcapApplicationEntityId = malloc(thedata->tcapApplicationEntityIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->tcapApplicationEntityId, thedata->tcapApplicationEntityId, thedata->tcapApplicationEntityIdLen);
		StorageNew->tcapApplicationEntityIdLen = thedata->tcapApplicationEntityIdLen;
		StorageNew->tcapApplicationEntityId[StorageNew->tcapApplicationEntityIdLen] = 0;
		StorageNew->tcapOmTcCurrentPabortRecvUnrecTid = thedata->tcapOmTcCurrentPabortRecvUnrecTid;
		StorageNew->tcapOmTcCurrentPabortRecvResourceLim = thedata->tcapOmTcCurrentPabortRecvResourceLim;
		StorageNew->tcapOmTcCurrentRejectRecvResourceLim = thedata->tcapOmTcCurrentRejectRecvResourceLim;
		StorageNew->tcapOmTcCurrentPabortSentUnrecTid = thedata->tcapOmTcCurrentPabortSentUnrecTid;
		StorageNew->tcapOmTcCurrentPabortSentResourceLim = thedata->tcapOmTcCurrentPabortSentResourceLim;
		StorageNew->tcapOmTcCurrentRejectSentResourceLim = thedata->tcapOmTcCurrentRejectSentResourceLim;
		StorageNew->tcapOmTcCurrentTcUserCancel = thedata->tcapOmTcCurrentTcUserCancel;
		StorageNew->tcapOmTcCurrentMessagesDiscarded = thedata->tcapOmTcCurrentMessagesDiscarded;
		StorageNew->tcapOmTcCurrentNotUsed = thedata->tcapOmTcCurrentNotUsed;
		StorageNew->tcapOmTcCurrentPabortsReceived = thedata->tcapOmTcCurrentPabortsReceived;
		StorageNew->tcapOmTcCurrentTcUserRejectsReceived = thedata->tcapOmTcCurrentTcUserRejectsReceived;
		StorageNew->tcapOmTcCurrentTpErrors = thedata->tcapOmTcCurrentTpErrors;
	}
      done:
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	tcapOmTcCurrentTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int tcapOmTcCurrentTable_destroy(struct tcapOmTcCurrentTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
tcapOmTcCurrentTable_destroy(struct tcapOmTcCurrentTable_data **thedata)
{
	struct tcapOmTcCurrentTable_data *StorageDel;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmTcCurrentTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->tcapApplicationSubsystemId);
		StorageDel->tcapApplicationSubsystemIdLen = 0;
		SNMP_FREE(StorageDel->tcapApplicationEntityId);
		StorageDel->tcapApplicationEntityIdLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int tcapOmTcCurrentTable_add(struct tcapOmTcCurrentTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the tcapOmTcCurrentTable table data set.
 *
 * Adds a table row structure to the tcapOmTcCurrentTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
tcapOmTcCurrentTable_add(struct tcapOmTcCurrentTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmTcCurrentTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* tcapApplicationSubsystemId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->tcapApplicationSubsystemId, thedata->tcapApplicationSubsystemIdLen);
		/* tcapApplicationEntityId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->tcapApplicationEntityId, thedata->tcapApplicationEntityIdLen);
		header_complex_add_data(&tcapOmTcCurrentTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("tcapOmMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int tcapOmTcCurrentTable_del(struct tcapOmTcCurrentTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the tcapOmTcCurrentTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
tcapOmTcCurrentTable_del(struct tcapOmTcCurrentTable_data *thedata)
{
	struct tcapOmTcCurrentTable_data *StorageDel;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmTcCurrentTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(tcapOmTcCurrentTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&tcapOmTcCurrentTableStorage, hciptr);
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_tcapOmTcCurrentTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for tcapOmTcCurrentTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case tcapOmTcCurrentTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_tcapOmTcCurrentTable(const char *token, char *line)
{
	size_t tmpsize;
	struct tcapOmTcCurrentTable_data *StorageTmp = tcapOmTcCurrentTable_create();

	(void) token;
	(void) line;
	DEBUGMSGTL(("tcapOmMIB", "parse_tcapOmTcCurrentTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->tcapApplicationSubsystemId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->tcapApplicationSubsystemId, &StorageTmp->tcapApplicationSubsystemIdLen);
	if (StorageTmp->tcapApplicationSubsystemId == NULL) {
		config_perror("invalid specification for tcapApplicationSubsystemId");
		return;
	}
	SNMP_FREE(StorageTmp->tcapApplicationEntityId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->tcapApplicationEntityId, &StorageTmp->tcapApplicationEntityIdLen);
	if (StorageTmp->tcapApplicationEntityId == NULL) {
		config_perror("invalid specification for tcapApplicationEntityId");
		return;
	}
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmTcCurrentPabortRecvUnrecTid, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmTcCurrentPabortRecvResourceLim, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmTcCurrentRejectRecvResourceLim, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmTcCurrentPabortSentUnrecTid, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmTcCurrentPabortSentResourceLim, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmTcCurrentRejectSentResourceLim, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmTcCurrentTcUserCancel, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmTcCurrentMessagesDiscarded, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmTcCurrentNotUsed, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmTcCurrentPabortsReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmTcCurrentTcUserRejectsReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmTcCurrentTpErrors, &tmpsize);
	tcapOmTcCurrentTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
}

/*
 * store_tcapOmTcCurrentTable(): store configuraiton file for tcapOmTcCurrentTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_tcapOmTcCurrentTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct tcapOmTcCurrentTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	(void) majorID;
	(void) minorID;
	(void) serverarg;
	(void) clientarg;
	DEBUGMSGTL(("tcapOmMIB", "store_tcapOmTcCurrentTable: storing data...  "));
	refresh_tcapOmTcCurrentTable(1);
	(void) tmpsize;
	for (hcindex = tcapOmTcCurrentTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct tcapOmTcCurrentTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "tcapOmTcCurrentTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->tcapApplicationSubsystemId, &StorageTmp->tcapApplicationSubsystemIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->tcapApplicationEntityId, &StorageTmp->tcapApplicationEntityIdLen);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmTcCurrentPabortRecvUnrecTid, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmTcCurrentPabortRecvResourceLim, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmTcCurrentRejectRecvResourceLim, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmTcCurrentPabortSentUnrecTid, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmTcCurrentPabortSentResourceLim, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmTcCurrentRejectSentResourceLim, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmTcCurrentTcUserCancel, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmTcCurrentMessagesDiscarded, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmTcCurrentNotUsed, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmTcCurrentPabortsReceived, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmTcCurrentTcUserRejectsReceived, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmTcCurrentTpErrors, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct tcapOmTc5minIntTable_data *tcapOmTc5minIntTable_create(void)
 * @brief create a fresh data structure representing a new row in the tcapOmTc5minIntTable table.
 *
 * Creates a new tcapOmTc5minIntTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct tcapOmTc5minIntTable_data *
tcapOmTc5minIntTable_create(void)
{
	struct tcapOmTc5minIntTable_data *StorageNew = SNMP_MALLOC_STRUCT(tcapOmTc5minIntTable_data);

	DEBUGMSGTL(("tcapOmMIB", "tcapOmTc5minIntTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->tcapApplicationSubsystemId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->tcapApplicationSubsystemIdLen = 0;
		StorageNew->tcapApplicationSubsystemId[StorageNew->tcapApplicationSubsystemIdLen] = 0;
		if ((StorageNew->tcapApplicationEntityId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->tcapApplicationEntityIdLen = 0;
		StorageNew->tcapApplicationEntityId[StorageNew->tcapApplicationEntityIdLen] = 0;
		StorageNew->tcapOmInterval = 0;
		StorageNew->tcapOmTc5minIntPabortRecvUnrecTid = 0;
		StorageNew->tcapOmTc5minIntPabortRecvResourceLim = 0;
		StorageNew->tcapOmTc5minIntRejectRecvResourceLim = 0;
		StorageNew->tcapOmTc5minIntPabortSentUnrecTid = 0;
		StorageNew->tcapOmTc5minIntPabortSentResourceLim = 0;
		StorageNew->tcapOmTc5minIntRejectSentResourceLim = 0;
		StorageNew->tcapOmTc5minIntTcUserCancel = 0;
		StorageNew->tcapOmTc5minIntMessagesDiscarded = 0;
		StorageNew->tcapOmTc5minIntNotUsed = 0;
		StorageNew->tcapOmTc5minIntPabortsReceived = 0;
		StorageNew->tcapOmTc5minIntTcUserRejectsReceived = 0;
		StorageNew->tcapOmTc5minIntTpErrors = 0;
	}
      done:
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	tcapOmTc5minIntTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct tcapOmTc5minIntTable_data *tcapOmTc5minIntTable_duplicate(struct tcapOmTc5minIntTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct tcapOmTc5minIntTable_data *
tcapOmTc5minIntTable_duplicate(struct tcapOmTc5minIntTable_data *thedata)
{
	struct tcapOmTc5minIntTable_data *StorageNew = SNMP_MALLOC_STRUCT(tcapOmTc5minIntTable_data);

	DEBUGMSGTL(("tcapOmMIB", "tcapOmTc5minIntTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->tcapOmTc5minIntTable_id = thedata->tcapOmTc5minIntTable_id;
		if (!(StorageNew->tcapApplicationSubsystemId = malloc(thedata->tcapApplicationSubsystemIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->tcapApplicationSubsystemId, thedata->tcapApplicationSubsystemId, thedata->tcapApplicationSubsystemIdLen);
		StorageNew->tcapApplicationSubsystemIdLen = thedata->tcapApplicationSubsystemIdLen;
		StorageNew->tcapApplicationSubsystemId[StorageNew->tcapApplicationSubsystemIdLen] = 0;
		if (!(StorageNew->tcapApplicationEntityId = malloc(thedata->tcapApplicationEntityIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->tcapApplicationEntityId, thedata->tcapApplicationEntityId, thedata->tcapApplicationEntityIdLen);
		StorageNew->tcapApplicationEntityIdLen = thedata->tcapApplicationEntityIdLen;
		StorageNew->tcapApplicationEntityId[StorageNew->tcapApplicationEntityIdLen] = 0;
		StorageNew->tcapOmInterval = thedata->tcapOmInterval;
		StorageNew->tcapOmTc5minIntPabortRecvUnrecTid = thedata->tcapOmTc5minIntPabortRecvUnrecTid;
		StorageNew->tcapOmTc5minIntPabortRecvResourceLim = thedata->tcapOmTc5minIntPabortRecvResourceLim;
		StorageNew->tcapOmTc5minIntRejectRecvResourceLim = thedata->tcapOmTc5minIntRejectRecvResourceLim;
		StorageNew->tcapOmTc5minIntPabortSentUnrecTid = thedata->tcapOmTc5minIntPabortSentUnrecTid;
		StorageNew->tcapOmTc5minIntPabortSentResourceLim = thedata->tcapOmTc5minIntPabortSentResourceLim;
		StorageNew->tcapOmTc5minIntRejectSentResourceLim = thedata->tcapOmTc5minIntRejectSentResourceLim;
		StorageNew->tcapOmTc5minIntTcUserCancel = thedata->tcapOmTc5minIntTcUserCancel;
		StorageNew->tcapOmTc5minIntMessagesDiscarded = thedata->tcapOmTc5minIntMessagesDiscarded;
		StorageNew->tcapOmTc5minIntNotUsed = thedata->tcapOmTc5minIntNotUsed;
		StorageNew->tcapOmTc5minIntPabortsReceived = thedata->tcapOmTc5minIntPabortsReceived;
		StorageNew->tcapOmTc5minIntTcUserRejectsReceived = thedata->tcapOmTc5minIntTcUserRejectsReceived;
		StorageNew->tcapOmTc5minIntTpErrors = thedata->tcapOmTc5minIntTpErrors;
	}
      done:
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	tcapOmTc5minIntTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int tcapOmTc5minIntTable_destroy(struct tcapOmTc5minIntTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
tcapOmTc5minIntTable_destroy(struct tcapOmTc5minIntTable_data **thedata)
{
	struct tcapOmTc5minIntTable_data *StorageDel;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmTc5minIntTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->tcapApplicationSubsystemId);
		StorageDel->tcapApplicationSubsystemIdLen = 0;
		SNMP_FREE(StorageDel->tcapApplicationEntityId);
		StorageDel->tcapApplicationEntityIdLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int tcapOmTc5minIntTable_add(struct tcapOmTc5minIntTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the tcapOmTc5minIntTable table data set.
 *
 * Adds a table row structure to the tcapOmTc5minIntTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
tcapOmTc5minIntTable_add(struct tcapOmTc5minIntTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmTc5minIntTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* tcapApplicationSubsystemId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->tcapApplicationSubsystemId, thedata->tcapApplicationSubsystemIdLen);
		/* tcapApplicationEntityId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->tcapApplicationEntityId, thedata->tcapApplicationEntityIdLen);
		/* tcapOmInterval */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->tcapOmInterval, sizeof(thedata->tcapOmInterval));
		header_complex_add_data(&tcapOmTc5minIntTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("tcapOmMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int tcapOmTc5minIntTable_del(struct tcapOmTc5minIntTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the tcapOmTc5minIntTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
tcapOmTc5minIntTable_del(struct tcapOmTc5minIntTable_data *thedata)
{
	struct tcapOmTc5minIntTable_data *StorageDel;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmTc5minIntTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(tcapOmTc5minIntTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&tcapOmTc5minIntTableStorage, hciptr);
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_tcapOmTc5minIntTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for tcapOmTc5minIntTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case tcapOmTc5minIntTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_tcapOmTc5minIntTable(const char *token, char *line)
{
	size_t tmpsize;
	struct tcapOmTc5minIntTable_data *StorageTmp = tcapOmTc5minIntTable_create();

	(void) token;
	(void) line;
	DEBUGMSGTL(("tcapOmMIB", "parse_tcapOmTc5minIntTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->tcapApplicationSubsystemId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->tcapApplicationSubsystemId, &StorageTmp->tcapApplicationSubsystemIdLen);
	if (StorageTmp->tcapApplicationSubsystemId == NULL) {
		config_perror("invalid specification for tcapApplicationSubsystemId");
		return;
	}
	SNMP_FREE(StorageTmp->tcapApplicationEntityId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->tcapApplicationEntityId, &StorageTmp->tcapApplicationEntityIdLen);
	if (StorageTmp->tcapApplicationEntityId == NULL) {
		config_perror("invalid specification for tcapApplicationEntityId");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->tcapOmInterval, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmTc5minIntPabortRecvUnrecTid, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmTc5minIntPabortRecvResourceLim, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmTc5minIntRejectRecvResourceLim, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmTc5minIntPabortSentUnrecTid, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmTc5minIntPabortSentResourceLim, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmTc5minIntRejectSentResourceLim, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmTc5minIntTcUserCancel, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmTc5minIntMessagesDiscarded, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmTc5minIntNotUsed, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmTc5minIntPabortsReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmTc5minIntTcUserRejectsReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmTc5minIntTpErrors, &tmpsize);
	tcapOmTc5minIntTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
}

/*
 * store_tcapOmTc5minIntTable(): store configuraiton file for tcapOmTc5minIntTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_tcapOmTc5minIntTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct tcapOmTc5minIntTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	(void) majorID;
	(void) minorID;
	(void) serverarg;
	(void) clientarg;
	DEBUGMSGTL(("tcapOmMIB", "store_tcapOmTc5minIntTable: storing data...  "));
	refresh_tcapOmTc5minIntTable(1);
	(void) tmpsize;
	for (hcindex = tcapOmTc5minIntTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct tcapOmTc5minIntTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "tcapOmTc5minIntTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->tcapApplicationSubsystemId, &StorageTmp->tcapApplicationSubsystemIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->tcapApplicationEntityId, &StorageTmp->tcapApplicationEntityIdLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->tcapOmInterval, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmTc5minIntPabortRecvUnrecTid, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmTc5minIntPabortRecvResourceLim, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmTc5minIntRejectRecvResourceLim, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmTc5minIntPabortSentUnrecTid, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmTc5minIntPabortSentResourceLim, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmTc5minIntRejectSentResourceLim, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmTc5minIntTcUserCancel, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmTc5minIntMessagesDiscarded, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmTc5minIntNotUsed, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmTc5minIntPabortsReceived, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmTc5minIntTcUserRejectsReceived, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmTc5minIntTpErrors, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct tcapOmTc15minIntTable_data *tcapOmTc15minIntTable_create(void)
 * @brief create a fresh data structure representing a new row in the tcapOmTc15minIntTable table.
 *
 * Creates a new tcapOmTc15minIntTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct tcapOmTc15minIntTable_data *
tcapOmTc15minIntTable_create(void)
{
	struct tcapOmTc15minIntTable_data *StorageNew = SNMP_MALLOC_STRUCT(tcapOmTc15minIntTable_data);

	DEBUGMSGTL(("tcapOmMIB", "tcapOmTc15minIntTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->tcapApplicationSubsystemId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->tcapApplicationSubsystemIdLen = 0;
		StorageNew->tcapApplicationSubsystemId[StorageNew->tcapApplicationSubsystemIdLen] = 0;
		if ((StorageNew->tcapApplicationEntityId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->tcapApplicationEntityIdLen = 0;
		StorageNew->tcapApplicationEntityId[StorageNew->tcapApplicationEntityIdLen] = 0;
		StorageNew->tcapOmInterval = 0;
		StorageNew->tcapOmTc15minIntPabortRecvUnrecTid = 0;
		StorageNew->tcapOmTc15minIntPabortRecvResourceLim = 0;
		StorageNew->tcapOmTc15minIntRejectRecvResourceLim = 0;
		StorageNew->tcapOmTc15minIntPabortSentUnrecTid = 0;
		StorageNew->tcapOmTc15minIntPabortSentResourceLim = 0;
		StorageNew->tcapOmTc15minIntRejectSentResourceLim = 0;
		StorageNew->tcapOmTc15minIntTcUserCancel = 0;
		StorageNew->tcapOmTc15minIntMessagesDiscarded = 0;
		StorageNew->tcapOmTc15minIntNotUsed = 0;
		StorageNew->tcapOmTc15minIntPabortsReceived = 0;
		StorageNew->tcapOmTc15minIntTcUserRejectsReceived = 0;
		StorageNew->tcapOmTc15minIntTpErrors = 0;
	}
      done:
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	tcapOmTc15minIntTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct tcapOmTc15minIntTable_data *tcapOmTc15minIntTable_duplicate(struct tcapOmTc15minIntTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct tcapOmTc15minIntTable_data *
tcapOmTc15minIntTable_duplicate(struct tcapOmTc15minIntTable_data *thedata)
{
	struct tcapOmTc15minIntTable_data *StorageNew = SNMP_MALLOC_STRUCT(tcapOmTc15minIntTable_data);

	DEBUGMSGTL(("tcapOmMIB", "tcapOmTc15minIntTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->tcapOmTc15minIntTable_id = thedata->tcapOmTc15minIntTable_id;
		if (!(StorageNew->tcapApplicationSubsystemId = malloc(thedata->tcapApplicationSubsystemIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->tcapApplicationSubsystemId, thedata->tcapApplicationSubsystemId, thedata->tcapApplicationSubsystemIdLen);
		StorageNew->tcapApplicationSubsystemIdLen = thedata->tcapApplicationSubsystemIdLen;
		StorageNew->tcapApplicationSubsystemId[StorageNew->tcapApplicationSubsystemIdLen] = 0;
		if (!(StorageNew->tcapApplicationEntityId = malloc(thedata->tcapApplicationEntityIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->tcapApplicationEntityId, thedata->tcapApplicationEntityId, thedata->tcapApplicationEntityIdLen);
		StorageNew->tcapApplicationEntityIdLen = thedata->tcapApplicationEntityIdLen;
		StorageNew->tcapApplicationEntityId[StorageNew->tcapApplicationEntityIdLen] = 0;
		StorageNew->tcapOmInterval = thedata->tcapOmInterval;
		StorageNew->tcapOmTc15minIntPabortRecvUnrecTid = thedata->tcapOmTc15minIntPabortRecvUnrecTid;
		StorageNew->tcapOmTc15minIntPabortRecvResourceLim = thedata->tcapOmTc15minIntPabortRecvResourceLim;
		StorageNew->tcapOmTc15minIntRejectRecvResourceLim = thedata->tcapOmTc15minIntRejectRecvResourceLim;
		StorageNew->tcapOmTc15minIntPabortSentUnrecTid = thedata->tcapOmTc15minIntPabortSentUnrecTid;
		StorageNew->tcapOmTc15minIntPabortSentResourceLim = thedata->tcapOmTc15minIntPabortSentResourceLim;
		StorageNew->tcapOmTc15minIntRejectSentResourceLim = thedata->tcapOmTc15minIntRejectSentResourceLim;
		StorageNew->tcapOmTc15minIntTcUserCancel = thedata->tcapOmTc15minIntTcUserCancel;
		StorageNew->tcapOmTc15minIntMessagesDiscarded = thedata->tcapOmTc15minIntMessagesDiscarded;
		StorageNew->tcapOmTc15minIntNotUsed = thedata->tcapOmTc15minIntNotUsed;
		StorageNew->tcapOmTc15minIntPabortsReceived = thedata->tcapOmTc15minIntPabortsReceived;
		StorageNew->tcapOmTc15minIntTcUserRejectsReceived = thedata->tcapOmTc15minIntTcUserRejectsReceived;
		StorageNew->tcapOmTc15minIntTpErrors = thedata->tcapOmTc15minIntTpErrors;
	}
      done:
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	tcapOmTc15minIntTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int tcapOmTc15minIntTable_destroy(struct tcapOmTc15minIntTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
tcapOmTc15minIntTable_destroy(struct tcapOmTc15minIntTable_data **thedata)
{
	struct tcapOmTc15minIntTable_data *StorageDel;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmTc15minIntTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->tcapApplicationSubsystemId);
		StorageDel->tcapApplicationSubsystemIdLen = 0;
		SNMP_FREE(StorageDel->tcapApplicationEntityId);
		StorageDel->tcapApplicationEntityIdLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int tcapOmTc15minIntTable_add(struct tcapOmTc15minIntTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the tcapOmTc15minIntTable table data set.
 *
 * Adds a table row structure to the tcapOmTc15minIntTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
tcapOmTc15minIntTable_add(struct tcapOmTc15minIntTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmTc15minIntTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* tcapApplicationSubsystemId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->tcapApplicationSubsystemId, thedata->tcapApplicationSubsystemIdLen);
		/* tcapApplicationEntityId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->tcapApplicationEntityId, thedata->tcapApplicationEntityIdLen);
		/* tcapOmInterval */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->tcapOmInterval, sizeof(thedata->tcapOmInterval));
		header_complex_add_data(&tcapOmTc15minIntTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("tcapOmMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int tcapOmTc15minIntTable_del(struct tcapOmTc15minIntTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the tcapOmTc15minIntTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
tcapOmTc15minIntTable_del(struct tcapOmTc15minIntTable_data *thedata)
{
	struct tcapOmTc15minIntTable_data *StorageDel;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmTc15minIntTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(tcapOmTc15minIntTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&tcapOmTc15minIntTableStorage, hciptr);
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_tcapOmTc15minIntTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for tcapOmTc15minIntTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case tcapOmTc15minIntTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_tcapOmTc15minIntTable(const char *token, char *line)
{
	size_t tmpsize;
	struct tcapOmTc15minIntTable_data *StorageTmp = tcapOmTc15minIntTable_create();

	(void) token;
	(void) line;
	DEBUGMSGTL(("tcapOmMIB", "parse_tcapOmTc15minIntTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->tcapApplicationSubsystemId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->tcapApplicationSubsystemId, &StorageTmp->tcapApplicationSubsystemIdLen);
	if (StorageTmp->tcapApplicationSubsystemId == NULL) {
		config_perror("invalid specification for tcapApplicationSubsystemId");
		return;
	}
	SNMP_FREE(StorageTmp->tcapApplicationEntityId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->tcapApplicationEntityId, &StorageTmp->tcapApplicationEntityIdLen);
	if (StorageTmp->tcapApplicationEntityId == NULL) {
		config_perror("invalid specification for tcapApplicationEntityId");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->tcapOmInterval, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmTc15minIntPabortRecvUnrecTid, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmTc15minIntPabortRecvResourceLim, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmTc15minIntRejectRecvResourceLim, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmTc15minIntPabortSentUnrecTid, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmTc15minIntPabortSentResourceLim, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmTc15minIntRejectSentResourceLim, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmTc15minIntTcUserCancel, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmTc15minIntMessagesDiscarded, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmTc15minIntNotUsed, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmTc15minIntPabortsReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmTc15minIntTcUserRejectsReceived, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmTc15minIntTpErrors, &tmpsize);
	tcapOmTc15minIntTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
}

/*
 * store_tcapOmTc15minIntTable(): store configuraiton file for tcapOmTc15minIntTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_tcapOmTc15minIntTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct tcapOmTc15minIntTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	(void) majorID;
	(void) minorID;
	(void) serverarg;
	(void) clientarg;
	DEBUGMSGTL(("tcapOmMIB", "store_tcapOmTc15minIntTable: storing data...  "));
	refresh_tcapOmTc15minIntTable(1);
	(void) tmpsize;
	for (hcindex = tcapOmTc15minIntTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct tcapOmTc15minIntTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "tcapOmTc15minIntTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->tcapApplicationSubsystemId, &StorageTmp->tcapApplicationSubsystemIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->tcapApplicationEntityId, &StorageTmp->tcapApplicationEntityIdLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->tcapOmInterval, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmTc15minIntPabortRecvUnrecTid, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmTc15minIntPabortRecvResourceLim, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmTc15minIntRejectRecvResourceLim, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmTc15minIntPabortSentUnrecTid, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmTc15minIntPabortSentResourceLim, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmTc15minIntRejectSentResourceLim, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmTc15minIntTcUserCancel, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmTc15minIntMessagesDiscarded, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmTc15minIntNotUsed, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmTc15minIntPabortsReceived, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmTc15minIntTcUserRejectsReceived, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmTc15minIntTpErrors, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct tcapOmDevelRecvTable_data *tcapOmDevelRecvTable_create(void)
 * @brief create a fresh data structure representing a new row in the tcapOmDevelRecvTable table.
 *
 * Creates a new tcapOmDevelRecvTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct tcapOmDevelRecvTable_data *
tcapOmDevelRecvTable_create(void)
{
	struct tcapOmDevelRecvTable_data *StorageNew = SNMP_MALLOC_STRUCT(tcapOmDevelRecvTable_data);

	DEBUGMSGTL(("tcapOmMIB", "tcapOmDevelRecvTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->tcapApplicationSubsystemId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->tcapApplicationSubsystemIdLen = 0;
		StorageNew->tcapApplicationSubsystemId[StorageNew->tcapApplicationSubsystemIdLen] = 0;
		if ((StorageNew->tcapApplicationEntityId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->tcapApplicationEntityIdLen = 0;
		StorageNew->tcapApplicationEntityId[StorageNew->tcapApplicationEntityIdLen] = 0;
		StorageNew->tcapOmDevelRecvPabortMessageType = 0;
		StorageNew->tcapOmDevelRecvPabortIncorrectTp = 0;
		StorageNew->tcapOmDevelRecvPabortBadTp = 0;
		StorageNew->tcapOmDevelRecvRejectUnrecComp = 0;
		StorageNew->tcapOmDevelRecvRejectMistypeComp = 0;
		StorageNew->tcapOmDevelRecvRejectBadCompStruct = 0;
		StorageNew->tcapOmDevelRecvRejectUnrecLinkId = 0;
		StorageNew->tcapOmDevelRecvRejectUnrecIdRetRes = 0;
		StorageNew->tcapOmDevelRecvRejectUnexpRetRes = 0;
		StorageNew->tcapOmDevelRecvRejectUnrecIdRetErr = 0;
		StorageNew->tcapOmDevelRecvRejectUnexpRetErr = 0;
		StorageNew->tcapOmDevelRecvUsrRejDupInvokeId = 0;
		StorageNew->tcapOmDevelRecvUsrRejUnrecOper = 0;
		StorageNew->tcapOmDevelRecvUsrRejParmInvoke = 0;
		StorageNew->tcapOmDevelRecvUsrRejReleasing = 0;
		StorageNew->tcapOmDevelRecvUsrRejUnexpLinkResp = 0;
		StorageNew->tcapOmDevelRecvUsrRejUnexpLinkOper = 0;
		StorageNew->tcapOmDevelRecvUsrRejUnrecError = 0;
		StorageNew->tcapOmDevelRecvUsrRejUnexpError = 0;
		StorageNew->tcapOmDevelRecvUsrRejParmRetResult = 0;
		StorageNew->tcapOmDevelRecvUsrRejParmRetError = 0;
		StorageNew->tcapOmDevelRecv5minValidIntervals = 0;
		StorageNew->tcapOmDevelRecv15minValidIntervals = 0;
	}
      done:
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	tcapOmDevelRecvTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct tcapOmDevelRecvTable_data *tcapOmDevelRecvTable_duplicate(struct tcapOmDevelRecvTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct tcapOmDevelRecvTable_data *
tcapOmDevelRecvTable_duplicate(struct tcapOmDevelRecvTable_data *thedata)
{
	struct tcapOmDevelRecvTable_data *StorageNew = SNMP_MALLOC_STRUCT(tcapOmDevelRecvTable_data);

	DEBUGMSGTL(("tcapOmMIB", "tcapOmDevelRecvTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->tcapOmDevelRecvTable_id = thedata->tcapOmDevelRecvTable_id;
		if (!(StorageNew->tcapApplicationSubsystemId = malloc(thedata->tcapApplicationSubsystemIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->tcapApplicationSubsystemId, thedata->tcapApplicationSubsystemId, thedata->tcapApplicationSubsystemIdLen);
		StorageNew->tcapApplicationSubsystemIdLen = thedata->tcapApplicationSubsystemIdLen;
		StorageNew->tcapApplicationSubsystemId[StorageNew->tcapApplicationSubsystemIdLen] = 0;
		if (!(StorageNew->tcapApplicationEntityId = malloc(thedata->tcapApplicationEntityIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->tcapApplicationEntityId, thedata->tcapApplicationEntityId, thedata->tcapApplicationEntityIdLen);
		StorageNew->tcapApplicationEntityIdLen = thedata->tcapApplicationEntityIdLen;
		StorageNew->tcapApplicationEntityId[StorageNew->tcapApplicationEntityIdLen] = 0;
		StorageNew->tcapOmDevelRecvPabortMessageType = thedata->tcapOmDevelRecvPabortMessageType;
		StorageNew->tcapOmDevelRecvPabortIncorrectTp = thedata->tcapOmDevelRecvPabortIncorrectTp;
		StorageNew->tcapOmDevelRecvPabortBadTp = thedata->tcapOmDevelRecvPabortBadTp;
		StorageNew->tcapOmDevelRecvRejectUnrecComp = thedata->tcapOmDevelRecvRejectUnrecComp;
		StorageNew->tcapOmDevelRecvRejectMistypeComp = thedata->tcapOmDevelRecvRejectMistypeComp;
		StorageNew->tcapOmDevelRecvRejectBadCompStruct = thedata->tcapOmDevelRecvRejectBadCompStruct;
		StorageNew->tcapOmDevelRecvRejectUnrecLinkId = thedata->tcapOmDevelRecvRejectUnrecLinkId;
		StorageNew->tcapOmDevelRecvRejectUnrecIdRetRes = thedata->tcapOmDevelRecvRejectUnrecIdRetRes;
		StorageNew->tcapOmDevelRecvRejectUnexpRetRes = thedata->tcapOmDevelRecvRejectUnexpRetRes;
		StorageNew->tcapOmDevelRecvRejectUnrecIdRetErr = thedata->tcapOmDevelRecvRejectUnrecIdRetErr;
		StorageNew->tcapOmDevelRecvRejectUnexpRetErr = thedata->tcapOmDevelRecvRejectUnexpRetErr;
		StorageNew->tcapOmDevelRecvUsrRejDupInvokeId = thedata->tcapOmDevelRecvUsrRejDupInvokeId;
		StorageNew->tcapOmDevelRecvUsrRejUnrecOper = thedata->tcapOmDevelRecvUsrRejUnrecOper;
		StorageNew->tcapOmDevelRecvUsrRejParmInvoke = thedata->tcapOmDevelRecvUsrRejParmInvoke;
		StorageNew->tcapOmDevelRecvUsrRejReleasing = thedata->tcapOmDevelRecvUsrRejReleasing;
		StorageNew->tcapOmDevelRecvUsrRejUnexpLinkResp = thedata->tcapOmDevelRecvUsrRejUnexpLinkResp;
		StorageNew->tcapOmDevelRecvUsrRejUnexpLinkOper = thedata->tcapOmDevelRecvUsrRejUnexpLinkOper;
		StorageNew->tcapOmDevelRecvUsrRejUnrecError = thedata->tcapOmDevelRecvUsrRejUnrecError;
		StorageNew->tcapOmDevelRecvUsrRejUnexpError = thedata->tcapOmDevelRecvUsrRejUnexpError;
		StorageNew->tcapOmDevelRecvUsrRejParmRetResult = thedata->tcapOmDevelRecvUsrRejParmRetResult;
		StorageNew->tcapOmDevelRecvUsrRejParmRetError = thedata->tcapOmDevelRecvUsrRejParmRetError;
		StorageNew->tcapOmDevelRecv5minValidIntervals = thedata->tcapOmDevelRecv5minValidIntervals;
		StorageNew->tcapOmDevelRecv15minValidIntervals = thedata->tcapOmDevelRecv15minValidIntervals;
	}
      done:
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	tcapOmDevelRecvTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int tcapOmDevelRecvTable_destroy(struct tcapOmDevelRecvTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
tcapOmDevelRecvTable_destroy(struct tcapOmDevelRecvTable_data **thedata)
{
	struct tcapOmDevelRecvTable_data *StorageDel;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmDevelRecvTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->tcapApplicationSubsystemId);
		StorageDel->tcapApplicationSubsystemIdLen = 0;
		SNMP_FREE(StorageDel->tcapApplicationEntityId);
		StorageDel->tcapApplicationEntityIdLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int tcapOmDevelRecvTable_add(struct tcapOmDevelRecvTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the tcapOmDevelRecvTable table data set.
 *
 * Adds a table row structure to the tcapOmDevelRecvTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
tcapOmDevelRecvTable_add(struct tcapOmDevelRecvTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmDevelRecvTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* tcapApplicationSubsystemId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->tcapApplicationSubsystemId, thedata->tcapApplicationSubsystemIdLen);
		/* tcapApplicationEntityId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->tcapApplicationEntityId, thedata->tcapApplicationEntityIdLen);
		header_complex_add_data(&tcapOmDevelRecvTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("tcapOmMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int tcapOmDevelRecvTable_del(struct tcapOmDevelRecvTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the tcapOmDevelRecvTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
tcapOmDevelRecvTable_del(struct tcapOmDevelRecvTable_data *thedata)
{
	struct tcapOmDevelRecvTable_data *StorageDel;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmDevelRecvTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(tcapOmDevelRecvTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&tcapOmDevelRecvTableStorage, hciptr);
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_tcapOmDevelRecvTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for tcapOmDevelRecvTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case tcapOmDevelRecvTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_tcapOmDevelRecvTable(const char *token, char *line)
{
	size_t tmpsize;
	struct tcapOmDevelRecvTable_data *StorageTmp = tcapOmDevelRecvTable_create();

	(void) token;
	(void) line;
	DEBUGMSGTL(("tcapOmMIB", "parse_tcapOmDevelRecvTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->tcapApplicationSubsystemId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->tcapApplicationSubsystemId, &StorageTmp->tcapApplicationSubsystemIdLen);
	if (StorageTmp->tcapApplicationSubsystemId == NULL) {
		config_perror("invalid specification for tcapApplicationSubsystemId");
		return;
	}
	SNMP_FREE(StorageTmp->tcapApplicationEntityId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->tcapApplicationEntityId, &StorageTmp->tcapApplicationEntityIdLen);
	if (StorageTmp->tcapApplicationEntityId == NULL) {
		config_perror("invalid specification for tcapApplicationEntityId");
		return;
	}
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmDevelRecvPabortMessageType, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmDevelRecvPabortIncorrectTp, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmDevelRecvPabortBadTp, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmDevelRecvRejectUnrecComp, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmDevelRecvRejectMistypeComp, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmDevelRecvRejectBadCompStruct, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmDevelRecvRejectUnrecLinkId, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmDevelRecvRejectUnrecIdRetRes, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmDevelRecvRejectUnexpRetRes, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmDevelRecvRejectUnrecIdRetErr, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmDevelRecvRejectUnexpRetErr, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmDevelRecvUsrRejDupInvokeId, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmDevelRecvUsrRejUnrecOper, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmDevelRecvUsrRejParmInvoke, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmDevelRecvUsrRejReleasing, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmDevelRecvUsrRejUnexpLinkResp, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmDevelRecvUsrRejUnexpLinkOper, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmDevelRecvUsrRejUnrecError, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmDevelRecvUsrRejUnexpError, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmDevelRecvUsrRejParmRetResult, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmDevelRecvUsrRejParmRetError, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->tcapOmDevelRecv5minValidIntervals, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->tcapOmDevelRecv15minValidIntervals, &tmpsize);
	tcapOmDevelRecvTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
}

/*
 * store_tcapOmDevelRecvTable(): store configuraiton file for tcapOmDevelRecvTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_tcapOmDevelRecvTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct tcapOmDevelRecvTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	(void) majorID;
	(void) minorID;
	(void) serverarg;
	(void) clientarg;
	DEBUGMSGTL(("tcapOmMIB", "store_tcapOmDevelRecvTable: storing data...  "));
	refresh_tcapOmDevelRecvTable(1);
	(void) tmpsize;
	for (hcindex = tcapOmDevelRecvTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct tcapOmDevelRecvTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "tcapOmDevelRecvTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->tcapApplicationSubsystemId, &StorageTmp->tcapApplicationSubsystemIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->tcapApplicationEntityId, &StorageTmp->tcapApplicationEntityIdLen);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmDevelRecvPabortMessageType, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmDevelRecvPabortIncorrectTp, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmDevelRecvPabortBadTp, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmDevelRecvRejectUnrecComp, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmDevelRecvRejectMistypeComp, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmDevelRecvRejectBadCompStruct, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmDevelRecvRejectUnrecLinkId, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmDevelRecvRejectUnrecIdRetRes, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmDevelRecvRejectUnexpRetRes, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmDevelRecvRejectUnrecIdRetErr, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmDevelRecvRejectUnexpRetErr, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmDevelRecvUsrRejDupInvokeId, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmDevelRecvUsrRejUnrecOper, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmDevelRecvUsrRejParmInvoke, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmDevelRecvUsrRejReleasing, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmDevelRecvUsrRejUnexpLinkResp, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmDevelRecvUsrRejUnexpLinkOper, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmDevelRecvUsrRejUnrecError, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmDevelRecvUsrRejUnexpError, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmDevelRecvUsrRejParmRetResult, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmDevelRecvUsrRejParmRetError, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->tcapOmDevelRecv5minValidIntervals, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->tcapOmDevelRecv15minValidIntervals, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct tcapOmDevelRecvCurrentTable_data *tcapOmDevelRecvCurrentTable_create(void)
 * @brief create a fresh data structure representing a new row in the tcapOmDevelRecvCurrentTable table.
 *
 * Creates a new tcapOmDevelRecvCurrentTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct tcapOmDevelRecvCurrentTable_data *
tcapOmDevelRecvCurrentTable_create(void)
{
	struct tcapOmDevelRecvCurrentTable_data *StorageNew = SNMP_MALLOC_STRUCT(tcapOmDevelRecvCurrentTable_data);

	DEBUGMSGTL(("tcapOmMIB", "tcapOmDevelRecvCurrentTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->tcapApplicationSubsystemId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->tcapApplicationSubsystemIdLen = 0;
		StorageNew->tcapApplicationSubsystemId[StorageNew->tcapApplicationSubsystemIdLen] = 0;
		if ((StorageNew->tcapApplicationEntityId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->tcapApplicationEntityIdLen = 0;
		StorageNew->tcapApplicationEntityId[StorageNew->tcapApplicationEntityIdLen] = 0;
		StorageNew->tcapOmDevelRecvCurrentPabortMessageType = 0;
		StorageNew->tcapOmDevelRecvCurrentPabortIncorrectTp = 0;
		StorageNew->tcapOmDevelRecvCurrentPabortBadTp = 0;
		StorageNew->tcapOmDevelRecvCurrentRejectUnrecComp = 0;
		StorageNew->tcapOmDevelRecvCurrentRejectMistypeComp = 0;
		StorageNew->tcapOmDevelRecvCurrentRejectBadCompStruct = 0;
		StorageNew->tcapOmDevelRecvCurrentRejectUnrecLinkId = 0;
		StorageNew->tcapOmDevelRecvCurrentRejectUnrecIdRetRes = 0;
		StorageNew->tcapOmDevelRecvCurrentRejectUnexpRetRes = 0;
		StorageNew->tcapOmDevelRecvCurrentRejectUnrecIdRetErr = 0;
		StorageNew->tcapOmDevelRecvCurrentRejectUnexpRetErr = 0;
		StorageNew->tcapOmDevelRecvCurrentUsrRejDupInvokeId = 0;
		StorageNew->tcapOmDevelRecvCurrentUsrRejUnrecOper = 0;
		StorageNew->tcapOmDevelRecvCurrentUsrRejParmInvoke = 0;
		StorageNew->tcapOmDevelRecvCurrentUsrRejReleasing = 0;
		StorageNew->tcapOmDevelRecvCurrentUsrRejUnexpLinkResp = 0;
		StorageNew->tcapOmDevelRecvCurrentUsrRejUnexpLinkOper = 0;
		StorageNew->tcapOmDevelRecvCurrentUsrRejUnrecError = 0;
		StorageNew->tcapOmDevelRecvCurrentUsrRejUnexpError = 0;
		StorageNew->tcapOmDevelRecvCurrentUsrRejParmRetResult = 0;
		StorageNew->tcapOmDevelRecvCurrentUsrRejParmRetError = 0;
	}
      done:
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	tcapOmDevelRecvCurrentTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct tcapOmDevelRecvCurrentTable_data *tcapOmDevelRecvCurrentTable_duplicate(struct tcapOmDevelRecvCurrentTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct tcapOmDevelRecvCurrentTable_data *
tcapOmDevelRecvCurrentTable_duplicate(struct tcapOmDevelRecvCurrentTable_data *thedata)
{
	struct tcapOmDevelRecvCurrentTable_data *StorageNew = SNMP_MALLOC_STRUCT(tcapOmDevelRecvCurrentTable_data);

	DEBUGMSGTL(("tcapOmMIB", "tcapOmDevelRecvCurrentTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->tcapOmDevelRecvCurrentTable_id = thedata->tcapOmDevelRecvCurrentTable_id;
		if (!(StorageNew->tcapApplicationSubsystemId = malloc(thedata->tcapApplicationSubsystemIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->tcapApplicationSubsystemId, thedata->tcapApplicationSubsystemId, thedata->tcapApplicationSubsystemIdLen);
		StorageNew->tcapApplicationSubsystemIdLen = thedata->tcapApplicationSubsystemIdLen;
		StorageNew->tcapApplicationSubsystemId[StorageNew->tcapApplicationSubsystemIdLen] = 0;
		if (!(StorageNew->tcapApplicationEntityId = malloc(thedata->tcapApplicationEntityIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->tcapApplicationEntityId, thedata->tcapApplicationEntityId, thedata->tcapApplicationEntityIdLen);
		StorageNew->tcapApplicationEntityIdLen = thedata->tcapApplicationEntityIdLen;
		StorageNew->tcapApplicationEntityId[StorageNew->tcapApplicationEntityIdLen] = 0;
		StorageNew->tcapOmDevelRecvCurrentPabortMessageType = thedata->tcapOmDevelRecvCurrentPabortMessageType;
		StorageNew->tcapOmDevelRecvCurrentPabortIncorrectTp = thedata->tcapOmDevelRecvCurrentPabortIncorrectTp;
		StorageNew->tcapOmDevelRecvCurrentPabortBadTp = thedata->tcapOmDevelRecvCurrentPabortBadTp;
		StorageNew->tcapOmDevelRecvCurrentRejectUnrecComp = thedata->tcapOmDevelRecvCurrentRejectUnrecComp;
		StorageNew->tcapOmDevelRecvCurrentRejectMistypeComp = thedata->tcapOmDevelRecvCurrentRejectMistypeComp;
		StorageNew->tcapOmDevelRecvCurrentRejectBadCompStruct = thedata->tcapOmDevelRecvCurrentRejectBadCompStruct;
		StorageNew->tcapOmDevelRecvCurrentRejectUnrecLinkId = thedata->tcapOmDevelRecvCurrentRejectUnrecLinkId;
		StorageNew->tcapOmDevelRecvCurrentRejectUnrecIdRetRes = thedata->tcapOmDevelRecvCurrentRejectUnrecIdRetRes;
		StorageNew->tcapOmDevelRecvCurrentRejectUnexpRetRes = thedata->tcapOmDevelRecvCurrentRejectUnexpRetRes;
		StorageNew->tcapOmDevelRecvCurrentRejectUnrecIdRetErr = thedata->tcapOmDevelRecvCurrentRejectUnrecIdRetErr;
		StorageNew->tcapOmDevelRecvCurrentRejectUnexpRetErr = thedata->tcapOmDevelRecvCurrentRejectUnexpRetErr;
		StorageNew->tcapOmDevelRecvCurrentUsrRejDupInvokeId = thedata->tcapOmDevelRecvCurrentUsrRejDupInvokeId;
		StorageNew->tcapOmDevelRecvCurrentUsrRejUnrecOper = thedata->tcapOmDevelRecvCurrentUsrRejUnrecOper;
		StorageNew->tcapOmDevelRecvCurrentUsrRejParmInvoke = thedata->tcapOmDevelRecvCurrentUsrRejParmInvoke;
		StorageNew->tcapOmDevelRecvCurrentUsrRejReleasing = thedata->tcapOmDevelRecvCurrentUsrRejReleasing;
		StorageNew->tcapOmDevelRecvCurrentUsrRejUnexpLinkResp = thedata->tcapOmDevelRecvCurrentUsrRejUnexpLinkResp;
		StorageNew->tcapOmDevelRecvCurrentUsrRejUnexpLinkOper = thedata->tcapOmDevelRecvCurrentUsrRejUnexpLinkOper;
		StorageNew->tcapOmDevelRecvCurrentUsrRejUnrecError = thedata->tcapOmDevelRecvCurrentUsrRejUnrecError;
		StorageNew->tcapOmDevelRecvCurrentUsrRejUnexpError = thedata->tcapOmDevelRecvCurrentUsrRejUnexpError;
		StorageNew->tcapOmDevelRecvCurrentUsrRejParmRetResult = thedata->tcapOmDevelRecvCurrentUsrRejParmRetResult;
		StorageNew->tcapOmDevelRecvCurrentUsrRejParmRetError = thedata->tcapOmDevelRecvCurrentUsrRejParmRetError;
	}
      done:
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	tcapOmDevelRecvCurrentTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int tcapOmDevelRecvCurrentTable_destroy(struct tcapOmDevelRecvCurrentTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
tcapOmDevelRecvCurrentTable_destroy(struct tcapOmDevelRecvCurrentTable_data **thedata)
{
	struct tcapOmDevelRecvCurrentTable_data *StorageDel;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmDevelRecvCurrentTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->tcapApplicationSubsystemId);
		StorageDel->tcapApplicationSubsystemIdLen = 0;
		SNMP_FREE(StorageDel->tcapApplicationEntityId);
		StorageDel->tcapApplicationEntityIdLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int tcapOmDevelRecvCurrentTable_add(struct tcapOmDevelRecvCurrentTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the tcapOmDevelRecvCurrentTable table data set.
 *
 * Adds a table row structure to the tcapOmDevelRecvCurrentTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
tcapOmDevelRecvCurrentTable_add(struct tcapOmDevelRecvCurrentTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmDevelRecvCurrentTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* tcapApplicationSubsystemId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->tcapApplicationSubsystemId, thedata->tcapApplicationSubsystemIdLen);
		/* tcapApplicationEntityId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->tcapApplicationEntityId, thedata->tcapApplicationEntityIdLen);
		header_complex_add_data(&tcapOmDevelRecvCurrentTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("tcapOmMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int tcapOmDevelRecvCurrentTable_del(struct tcapOmDevelRecvCurrentTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the tcapOmDevelRecvCurrentTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
tcapOmDevelRecvCurrentTable_del(struct tcapOmDevelRecvCurrentTable_data *thedata)
{
	struct tcapOmDevelRecvCurrentTable_data *StorageDel;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmDevelRecvCurrentTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(tcapOmDevelRecvCurrentTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&tcapOmDevelRecvCurrentTableStorage, hciptr);
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_tcapOmDevelRecvCurrentTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for tcapOmDevelRecvCurrentTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case tcapOmDevelRecvCurrentTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_tcapOmDevelRecvCurrentTable(const char *token, char *line)
{
	size_t tmpsize;
	struct tcapOmDevelRecvCurrentTable_data *StorageTmp = tcapOmDevelRecvCurrentTable_create();

	(void) token;
	(void) line;
	DEBUGMSGTL(("tcapOmMIB", "parse_tcapOmDevelRecvCurrentTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->tcapApplicationSubsystemId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->tcapApplicationSubsystemId, &StorageTmp->tcapApplicationSubsystemIdLen);
	if (StorageTmp->tcapApplicationSubsystemId == NULL) {
		config_perror("invalid specification for tcapApplicationSubsystemId");
		return;
	}
	SNMP_FREE(StorageTmp->tcapApplicationEntityId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->tcapApplicationEntityId, &StorageTmp->tcapApplicationEntityIdLen);
	if (StorageTmp->tcapApplicationEntityId == NULL) {
		config_perror("invalid specification for tcapApplicationEntityId");
		return;
	}
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecvCurrentPabortMessageType, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecvCurrentPabortIncorrectTp, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecvCurrentPabortBadTp, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecvCurrentRejectUnrecComp, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecvCurrentRejectMistypeComp, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecvCurrentRejectBadCompStruct, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecvCurrentRejectUnrecLinkId, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecvCurrentRejectUnrecIdRetRes, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecvCurrentRejectUnexpRetRes, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecvCurrentRejectUnrecIdRetErr, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecvCurrentRejectUnexpRetErr, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecvCurrentUsrRejDupInvokeId, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecvCurrentUsrRejUnrecOper, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecvCurrentUsrRejParmInvoke, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecvCurrentUsrRejReleasing, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecvCurrentUsrRejUnexpLinkResp, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecvCurrentUsrRejUnexpLinkOper, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecvCurrentUsrRejUnrecError, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecvCurrentUsrRejUnexpError, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecvCurrentUsrRejParmRetResult, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecvCurrentUsrRejParmRetError, &tmpsize);
	tcapOmDevelRecvCurrentTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
}

/*
 * store_tcapOmDevelRecvCurrentTable(): store configuraiton file for tcapOmDevelRecvCurrentTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_tcapOmDevelRecvCurrentTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct tcapOmDevelRecvCurrentTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	(void) majorID;
	(void) minorID;
	(void) serverarg;
	(void) clientarg;
	DEBUGMSGTL(("tcapOmMIB", "store_tcapOmDevelRecvCurrentTable: storing data...  "));
	refresh_tcapOmDevelRecvCurrentTable(1);
	(void) tmpsize;
	for (hcindex = tcapOmDevelRecvCurrentTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct tcapOmDevelRecvCurrentTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "tcapOmDevelRecvCurrentTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->tcapApplicationSubsystemId, &StorageTmp->tcapApplicationSubsystemIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->tcapApplicationEntityId, &StorageTmp->tcapApplicationEntityIdLen);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecvCurrentPabortMessageType, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecvCurrentPabortIncorrectTp, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecvCurrentPabortBadTp, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecvCurrentRejectUnrecComp, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecvCurrentRejectMistypeComp, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecvCurrentRejectBadCompStruct, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecvCurrentRejectUnrecLinkId, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecvCurrentRejectUnrecIdRetRes, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecvCurrentRejectUnexpRetRes, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecvCurrentRejectUnrecIdRetErr, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecvCurrentRejectUnexpRetErr, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecvCurrentUsrRejDupInvokeId, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecvCurrentUsrRejUnrecOper, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecvCurrentUsrRejParmInvoke, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecvCurrentUsrRejReleasing, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecvCurrentUsrRejUnexpLinkResp, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecvCurrentUsrRejUnexpLinkOper, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecvCurrentUsrRejUnrecError, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecvCurrentUsrRejUnexpError, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecvCurrentUsrRejParmRetResult, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecvCurrentUsrRejParmRetError, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct tcapOmDevelRecv5minIntTable_data *tcapOmDevelRecv5minIntTable_create(void)
 * @brief create a fresh data structure representing a new row in the tcapOmDevelRecv5minIntTable table.
 *
 * Creates a new tcapOmDevelRecv5minIntTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct tcapOmDevelRecv5minIntTable_data *
tcapOmDevelRecv5minIntTable_create(void)
{
	struct tcapOmDevelRecv5minIntTable_data *StorageNew = SNMP_MALLOC_STRUCT(tcapOmDevelRecv5minIntTable_data);

	DEBUGMSGTL(("tcapOmMIB", "tcapOmDevelRecv5minIntTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->tcapApplicationSubsystemId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->tcapApplicationSubsystemIdLen = 0;
		StorageNew->tcapApplicationSubsystemId[StorageNew->tcapApplicationSubsystemIdLen] = 0;
		if ((StorageNew->tcapApplicationEntityId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->tcapApplicationEntityIdLen = 0;
		StorageNew->tcapApplicationEntityId[StorageNew->tcapApplicationEntityIdLen] = 0;
		StorageNew->tcapOmInterval = 0;
		StorageNew->tcapOmDevelRecv5minIntPabortMessageType = 0;
		StorageNew->tcapOmDevelRecv5minIntPabortIncorrectTp = 0;
		StorageNew->tcapOmDevelRecv5minIntPabortBadTp = 0;
		StorageNew->tcapOmDevelRecv5minIntRejectUnrecComp = 0;
		StorageNew->tcapOmDevelRecv5minIntRejectMistypeComp = 0;
		StorageNew->tcapOmDevelRecv5minIntRejectBadCompStruct = 0;
		StorageNew->tcapOmDevelRecv5minIntRejectUnrecLinkId = 0;
		StorageNew->tcapOmDevelRecv5minIntRejectUnrecIdRetRes = 0;
		StorageNew->tcapOmDevelRecv5minIntRejectUnexpRetRes = 0;
		StorageNew->tcapOmDevelRecv5minIntRejectUnrecIdRetErr = 0;
		StorageNew->tcapOmDevelRecv5minIntRejectUnexpRetErr = 0;
		StorageNew->tcapOmDevelRecv5minIntUsrRejDupInvokeId = 0;
		StorageNew->tcapOmDevelRecv5minIntUsrRejUnrecOper = 0;
		StorageNew->tcapOmDevelRecv5minIntUsrRejParmInvoke = 0;
		StorageNew->tcapOmDevelRecv5minIntUsrRejReleasing = 0;
		StorageNew->tcapOmDevelRecv5minIntUsrRejUnexpLinkResp = 0;
		StorageNew->tcapOmDevelRecv5minIntUsrRejUnexpLinkOper = 0;
		StorageNew->tcapOmDevelRecv5minIntUsrRejUnrecError = 0;
		StorageNew->tcapOmDevelRecv5minIntUsrRejUnexpError = 0;
		StorageNew->tcapOmDevelRecv5minIntUsrRejParmRetResult = 0;
		StorageNew->tcapOmDevelRecv5minIntUsrRejParmRetError = 0;
	}
      done:
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	tcapOmDevelRecv5minIntTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct tcapOmDevelRecv5minIntTable_data *tcapOmDevelRecv5minIntTable_duplicate(struct tcapOmDevelRecv5minIntTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct tcapOmDevelRecv5minIntTable_data *
tcapOmDevelRecv5minIntTable_duplicate(struct tcapOmDevelRecv5minIntTable_data *thedata)
{
	struct tcapOmDevelRecv5minIntTable_data *StorageNew = SNMP_MALLOC_STRUCT(tcapOmDevelRecv5minIntTable_data);

	DEBUGMSGTL(("tcapOmMIB", "tcapOmDevelRecv5minIntTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->tcapOmDevelRecv5minIntTable_id = thedata->tcapOmDevelRecv5minIntTable_id;
		if (!(StorageNew->tcapApplicationSubsystemId = malloc(thedata->tcapApplicationSubsystemIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->tcapApplicationSubsystemId, thedata->tcapApplicationSubsystemId, thedata->tcapApplicationSubsystemIdLen);
		StorageNew->tcapApplicationSubsystemIdLen = thedata->tcapApplicationSubsystemIdLen;
		StorageNew->tcapApplicationSubsystemId[StorageNew->tcapApplicationSubsystemIdLen] = 0;
		if (!(StorageNew->tcapApplicationEntityId = malloc(thedata->tcapApplicationEntityIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->tcapApplicationEntityId, thedata->tcapApplicationEntityId, thedata->tcapApplicationEntityIdLen);
		StorageNew->tcapApplicationEntityIdLen = thedata->tcapApplicationEntityIdLen;
		StorageNew->tcapApplicationEntityId[StorageNew->tcapApplicationEntityIdLen] = 0;
		StorageNew->tcapOmInterval = thedata->tcapOmInterval;
		StorageNew->tcapOmDevelRecv5minIntPabortMessageType = thedata->tcapOmDevelRecv5minIntPabortMessageType;
		StorageNew->tcapOmDevelRecv5minIntPabortIncorrectTp = thedata->tcapOmDevelRecv5minIntPabortIncorrectTp;
		StorageNew->tcapOmDevelRecv5minIntPabortBadTp = thedata->tcapOmDevelRecv5minIntPabortBadTp;
		StorageNew->tcapOmDevelRecv5minIntRejectUnrecComp = thedata->tcapOmDevelRecv5minIntRejectUnrecComp;
		StorageNew->tcapOmDevelRecv5minIntRejectMistypeComp = thedata->tcapOmDevelRecv5minIntRejectMistypeComp;
		StorageNew->tcapOmDevelRecv5minIntRejectBadCompStruct = thedata->tcapOmDevelRecv5minIntRejectBadCompStruct;
		StorageNew->tcapOmDevelRecv5minIntRejectUnrecLinkId = thedata->tcapOmDevelRecv5minIntRejectUnrecLinkId;
		StorageNew->tcapOmDevelRecv5minIntRejectUnrecIdRetRes = thedata->tcapOmDevelRecv5minIntRejectUnrecIdRetRes;
		StorageNew->tcapOmDevelRecv5minIntRejectUnexpRetRes = thedata->tcapOmDevelRecv5minIntRejectUnexpRetRes;
		StorageNew->tcapOmDevelRecv5minIntRejectUnrecIdRetErr = thedata->tcapOmDevelRecv5minIntRejectUnrecIdRetErr;
		StorageNew->tcapOmDevelRecv5minIntRejectUnexpRetErr = thedata->tcapOmDevelRecv5minIntRejectUnexpRetErr;
		StorageNew->tcapOmDevelRecv5minIntUsrRejDupInvokeId = thedata->tcapOmDevelRecv5minIntUsrRejDupInvokeId;
		StorageNew->tcapOmDevelRecv5minIntUsrRejUnrecOper = thedata->tcapOmDevelRecv5minIntUsrRejUnrecOper;
		StorageNew->tcapOmDevelRecv5minIntUsrRejParmInvoke = thedata->tcapOmDevelRecv5minIntUsrRejParmInvoke;
		StorageNew->tcapOmDevelRecv5minIntUsrRejReleasing = thedata->tcapOmDevelRecv5minIntUsrRejReleasing;
		StorageNew->tcapOmDevelRecv5minIntUsrRejUnexpLinkResp = thedata->tcapOmDevelRecv5minIntUsrRejUnexpLinkResp;
		StorageNew->tcapOmDevelRecv5minIntUsrRejUnexpLinkOper = thedata->tcapOmDevelRecv5minIntUsrRejUnexpLinkOper;
		StorageNew->tcapOmDevelRecv5minIntUsrRejUnrecError = thedata->tcapOmDevelRecv5minIntUsrRejUnrecError;
		StorageNew->tcapOmDevelRecv5minIntUsrRejUnexpError = thedata->tcapOmDevelRecv5minIntUsrRejUnexpError;
		StorageNew->tcapOmDevelRecv5minIntUsrRejParmRetResult = thedata->tcapOmDevelRecv5minIntUsrRejParmRetResult;
		StorageNew->tcapOmDevelRecv5minIntUsrRejParmRetError = thedata->tcapOmDevelRecv5minIntUsrRejParmRetError;
	}
      done:
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	tcapOmDevelRecv5minIntTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int tcapOmDevelRecv5minIntTable_destroy(struct tcapOmDevelRecv5minIntTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
tcapOmDevelRecv5minIntTable_destroy(struct tcapOmDevelRecv5minIntTable_data **thedata)
{
	struct tcapOmDevelRecv5minIntTable_data *StorageDel;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmDevelRecv5minIntTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->tcapApplicationSubsystemId);
		StorageDel->tcapApplicationSubsystemIdLen = 0;
		SNMP_FREE(StorageDel->tcapApplicationEntityId);
		StorageDel->tcapApplicationEntityIdLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int tcapOmDevelRecv5minIntTable_add(struct tcapOmDevelRecv5minIntTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the tcapOmDevelRecv5minIntTable table data set.
 *
 * Adds a table row structure to the tcapOmDevelRecv5minIntTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
tcapOmDevelRecv5minIntTable_add(struct tcapOmDevelRecv5minIntTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmDevelRecv5minIntTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* tcapApplicationSubsystemId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->tcapApplicationSubsystemId, thedata->tcapApplicationSubsystemIdLen);
		/* tcapApplicationEntityId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->tcapApplicationEntityId, thedata->tcapApplicationEntityIdLen);
		/* tcapOmInterval */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->tcapOmInterval, sizeof(thedata->tcapOmInterval));
		header_complex_add_data(&tcapOmDevelRecv5minIntTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("tcapOmMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int tcapOmDevelRecv5minIntTable_del(struct tcapOmDevelRecv5minIntTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the tcapOmDevelRecv5minIntTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
tcapOmDevelRecv5minIntTable_del(struct tcapOmDevelRecv5minIntTable_data *thedata)
{
	struct tcapOmDevelRecv5minIntTable_data *StorageDel;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmDevelRecv5minIntTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(tcapOmDevelRecv5minIntTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&tcapOmDevelRecv5minIntTableStorage, hciptr);
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_tcapOmDevelRecv5minIntTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for tcapOmDevelRecv5minIntTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case tcapOmDevelRecv5minIntTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_tcapOmDevelRecv5minIntTable(const char *token, char *line)
{
	size_t tmpsize;
	struct tcapOmDevelRecv5minIntTable_data *StorageTmp = tcapOmDevelRecv5minIntTable_create();

	(void) token;
	(void) line;
	DEBUGMSGTL(("tcapOmMIB", "parse_tcapOmDevelRecv5minIntTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->tcapApplicationSubsystemId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->tcapApplicationSubsystemId, &StorageTmp->tcapApplicationSubsystemIdLen);
	if (StorageTmp->tcapApplicationSubsystemId == NULL) {
		config_perror("invalid specification for tcapApplicationSubsystemId");
		return;
	}
	SNMP_FREE(StorageTmp->tcapApplicationEntityId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->tcapApplicationEntityId, &StorageTmp->tcapApplicationEntityIdLen);
	if (StorageTmp->tcapApplicationEntityId == NULL) {
		config_perror("invalid specification for tcapApplicationEntityId");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->tcapOmInterval, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecv5minIntPabortMessageType, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecv5minIntPabortIncorrectTp, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecv5minIntPabortBadTp, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecv5minIntRejectUnrecComp, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecv5minIntRejectMistypeComp, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecv5minIntRejectBadCompStruct, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecv5minIntRejectUnrecLinkId, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecv5minIntRejectUnrecIdRetRes, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecv5minIntRejectUnexpRetRes, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecv5minIntRejectUnrecIdRetErr, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecv5minIntRejectUnexpRetErr, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecv5minIntUsrRejDupInvokeId, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecv5minIntUsrRejUnrecOper, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecv5minIntUsrRejParmInvoke, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecv5minIntUsrRejReleasing, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecv5minIntUsrRejUnexpLinkResp, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecv5minIntUsrRejUnexpLinkOper, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecv5minIntUsrRejUnrecError, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecv5minIntUsrRejUnexpError, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecv5minIntUsrRejParmRetResult, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecv5minIntUsrRejParmRetError, &tmpsize);
	tcapOmDevelRecv5minIntTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
}

/*
 * store_tcapOmDevelRecv5minIntTable(): store configuraiton file for tcapOmDevelRecv5minIntTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_tcapOmDevelRecv5minIntTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct tcapOmDevelRecv5minIntTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	(void) majorID;
	(void) minorID;
	(void) serverarg;
	(void) clientarg;
	DEBUGMSGTL(("tcapOmMIB", "store_tcapOmDevelRecv5minIntTable: storing data...  "));
	refresh_tcapOmDevelRecv5minIntTable(1);
	(void) tmpsize;
	for (hcindex = tcapOmDevelRecv5minIntTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct tcapOmDevelRecv5minIntTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "tcapOmDevelRecv5minIntTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->tcapApplicationSubsystemId, &StorageTmp->tcapApplicationSubsystemIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->tcapApplicationEntityId, &StorageTmp->tcapApplicationEntityIdLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->tcapOmInterval, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecv5minIntPabortMessageType, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecv5minIntPabortIncorrectTp, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecv5minIntPabortBadTp, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecv5minIntRejectUnrecComp, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecv5minIntRejectMistypeComp, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecv5minIntRejectBadCompStruct, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecv5minIntRejectUnrecLinkId, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecv5minIntRejectUnrecIdRetRes, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecv5minIntRejectUnexpRetRes, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecv5minIntRejectUnrecIdRetErr, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecv5minIntRejectUnexpRetErr, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecv5minIntUsrRejDupInvokeId, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecv5minIntUsrRejUnrecOper, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecv5minIntUsrRejParmInvoke, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecv5minIntUsrRejReleasing, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecv5minIntUsrRejUnexpLinkResp, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecv5minIntUsrRejUnexpLinkOper, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecv5minIntUsrRejUnrecError, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecv5minIntUsrRejUnexpError, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecv5minIntUsrRejParmRetResult, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecv5minIntUsrRejParmRetError, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct tcapOmDevelRecv15minIntTable_data *tcapOmDevelRecv15minIntTable_create(void)
 * @brief create a fresh data structure representing a new row in the tcapOmDevelRecv15minIntTable table.
 *
 * Creates a new tcapOmDevelRecv15minIntTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct tcapOmDevelRecv15minIntTable_data *
tcapOmDevelRecv15minIntTable_create(void)
{
	struct tcapOmDevelRecv15minIntTable_data *StorageNew = SNMP_MALLOC_STRUCT(tcapOmDevelRecv15minIntTable_data);

	DEBUGMSGTL(("tcapOmMIB", "tcapOmDevelRecv15minIntTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->tcapApplicationSubsystemId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->tcapApplicationSubsystemIdLen = 0;
		StorageNew->tcapApplicationSubsystemId[StorageNew->tcapApplicationSubsystemIdLen] = 0;
		if ((StorageNew->tcapApplicationEntityId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->tcapApplicationEntityIdLen = 0;
		StorageNew->tcapApplicationEntityId[StorageNew->tcapApplicationEntityIdLen] = 0;
		StorageNew->tcapOmInterval = 0;
		StorageNew->tcapOmDevelRecv15minIntPabortMessageType = 0;
		StorageNew->tcapOmDevelRecv15minIntPabortIncorrectTp = 0;
		StorageNew->tcapOmDevelRecv15minIntPabortBadTp = 0;
		StorageNew->tcapOmDevelRecv15minIntRejectUnrecComp = 0;
		StorageNew->tcapOmDevelRecv15minIntRejectMistypeComp = 0;
		StorageNew->tcapOmDevelRecv15minIntRejectBadCompStruct = 0;
		StorageNew->tcapOmDevelRecv15minIntRejectUnrecLinkId = 0;
		StorageNew->tcapOmDevelRecv15minIntRejectUnrecIdRetRes = 0;
		StorageNew->tcapOmDevelRecv15minIntRejectUnexpRetRes = 0;
		StorageNew->tcapOmDevelRecv15minIntRejectUnrecIdRetErr = 0;
		StorageNew->tcapOmDevelRecv15minIntRejectUnexpRetErr = 0;
		StorageNew->tcapOmDevelRecv15minIntUsrRejDupInvokeId = 0;
		StorageNew->tcapOmDevelRecv15minIntUsrRejUnrecOper = 0;
		StorageNew->tcapOmDevelRecv15minIntUsrRejParmInvoke = 0;
		StorageNew->tcapOmDevelRecv15minIntUsrRejReleasing = 0;
		StorageNew->tcapOmDevelRecv15minIntUsrRejUnexpLinkResp = 0;
		StorageNew->tcapOmDevelRecv15minIntUsrRejUnexpLinkOper = 0;
		StorageNew->tcapOmDevelRecv15minIntUsrRejUnrecError = 0;
		StorageNew->tcapOmDevelRecv15minIntUsrRejUnexpError = 0;
		StorageNew->tcapOmDevelRecv15minIntUsrRejParmRetResult = 0;
		StorageNew->tcapOmDevelRecv15minIntUsrRejParmRetError = 0;
	}
      done:
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	tcapOmDevelRecv15minIntTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct tcapOmDevelRecv15minIntTable_data *tcapOmDevelRecv15minIntTable_duplicate(struct tcapOmDevelRecv15minIntTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct tcapOmDevelRecv15minIntTable_data *
tcapOmDevelRecv15minIntTable_duplicate(struct tcapOmDevelRecv15minIntTable_data *thedata)
{
	struct tcapOmDevelRecv15minIntTable_data *StorageNew = SNMP_MALLOC_STRUCT(tcapOmDevelRecv15minIntTable_data);

	DEBUGMSGTL(("tcapOmMIB", "tcapOmDevelRecv15minIntTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->tcapOmDevelRecv15minIntTable_id = thedata->tcapOmDevelRecv15minIntTable_id;
		if (!(StorageNew->tcapApplicationSubsystemId = malloc(thedata->tcapApplicationSubsystemIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->tcapApplicationSubsystemId, thedata->tcapApplicationSubsystemId, thedata->tcapApplicationSubsystemIdLen);
		StorageNew->tcapApplicationSubsystemIdLen = thedata->tcapApplicationSubsystemIdLen;
		StorageNew->tcapApplicationSubsystemId[StorageNew->tcapApplicationSubsystemIdLen] = 0;
		if (!(StorageNew->tcapApplicationEntityId = malloc(thedata->tcapApplicationEntityIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->tcapApplicationEntityId, thedata->tcapApplicationEntityId, thedata->tcapApplicationEntityIdLen);
		StorageNew->tcapApplicationEntityIdLen = thedata->tcapApplicationEntityIdLen;
		StorageNew->tcapApplicationEntityId[StorageNew->tcapApplicationEntityIdLen] = 0;
		StorageNew->tcapOmInterval = thedata->tcapOmInterval;
		StorageNew->tcapOmDevelRecv15minIntPabortMessageType = thedata->tcapOmDevelRecv15minIntPabortMessageType;
		StorageNew->tcapOmDevelRecv15minIntPabortIncorrectTp = thedata->tcapOmDevelRecv15minIntPabortIncorrectTp;
		StorageNew->tcapOmDevelRecv15minIntPabortBadTp = thedata->tcapOmDevelRecv15minIntPabortBadTp;
		StorageNew->tcapOmDevelRecv15minIntRejectUnrecComp = thedata->tcapOmDevelRecv15minIntRejectUnrecComp;
		StorageNew->tcapOmDevelRecv15minIntRejectMistypeComp = thedata->tcapOmDevelRecv15minIntRejectMistypeComp;
		StorageNew->tcapOmDevelRecv15minIntRejectBadCompStruct = thedata->tcapOmDevelRecv15minIntRejectBadCompStruct;
		StorageNew->tcapOmDevelRecv15minIntRejectUnrecLinkId = thedata->tcapOmDevelRecv15minIntRejectUnrecLinkId;
		StorageNew->tcapOmDevelRecv15minIntRejectUnrecIdRetRes = thedata->tcapOmDevelRecv15minIntRejectUnrecIdRetRes;
		StorageNew->tcapOmDevelRecv15minIntRejectUnexpRetRes = thedata->tcapOmDevelRecv15minIntRejectUnexpRetRes;
		StorageNew->tcapOmDevelRecv15minIntRejectUnrecIdRetErr = thedata->tcapOmDevelRecv15minIntRejectUnrecIdRetErr;
		StorageNew->tcapOmDevelRecv15minIntRejectUnexpRetErr = thedata->tcapOmDevelRecv15minIntRejectUnexpRetErr;
		StorageNew->tcapOmDevelRecv15minIntUsrRejDupInvokeId = thedata->tcapOmDevelRecv15minIntUsrRejDupInvokeId;
		StorageNew->tcapOmDevelRecv15minIntUsrRejUnrecOper = thedata->tcapOmDevelRecv15minIntUsrRejUnrecOper;
		StorageNew->tcapOmDevelRecv15minIntUsrRejParmInvoke = thedata->tcapOmDevelRecv15minIntUsrRejParmInvoke;
		StorageNew->tcapOmDevelRecv15minIntUsrRejReleasing = thedata->tcapOmDevelRecv15minIntUsrRejReleasing;
		StorageNew->tcapOmDevelRecv15minIntUsrRejUnexpLinkResp = thedata->tcapOmDevelRecv15minIntUsrRejUnexpLinkResp;
		StorageNew->tcapOmDevelRecv15minIntUsrRejUnexpLinkOper = thedata->tcapOmDevelRecv15minIntUsrRejUnexpLinkOper;
		StorageNew->tcapOmDevelRecv15minIntUsrRejUnrecError = thedata->tcapOmDevelRecv15minIntUsrRejUnrecError;
		StorageNew->tcapOmDevelRecv15minIntUsrRejUnexpError = thedata->tcapOmDevelRecv15minIntUsrRejUnexpError;
		StorageNew->tcapOmDevelRecv15minIntUsrRejParmRetResult = thedata->tcapOmDevelRecv15minIntUsrRejParmRetResult;
		StorageNew->tcapOmDevelRecv15minIntUsrRejParmRetError = thedata->tcapOmDevelRecv15minIntUsrRejParmRetError;
	}
      done:
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	tcapOmDevelRecv15minIntTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int tcapOmDevelRecv15minIntTable_destroy(struct tcapOmDevelRecv15minIntTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
tcapOmDevelRecv15minIntTable_destroy(struct tcapOmDevelRecv15minIntTable_data **thedata)
{
	struct tcapOmDevelRecv15minIntTable_data *StorageDel;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmDevelRecv15minIntTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->tcapApplicationSubsystemId);
		StorageDel->tcapApplicationSubsystemIdLen = 0;
		SNMP_FREE(StorageDel->tcapApplicationEntityId);
		StorageDel->tcapApplicationEntityIdLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int tcapOmDevelRecv15minIntTable_add(struct tcapOmDevelRecv15minIntTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the tcapOmDevelRecv15minIntTable table data set.
 *
 * Adds a table row structure to the tcapOmDevelRecv15minIntTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
tcapOmDevelRecv15minIntTable_add(struct tcapOmDevelRecv15minIntTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmDevelRecv15minIntTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* tcapApplicationSubsystemId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->tcapApplicationSubsystemId, thedata->tcapApplicationSubsystemIdLen);
		/* tcapApplicationEntityId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->tcapApplicationEntityId, thedata->tcapApplicationEntityIdLen);
		/* tcapOmInterval */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->tcapOmInterval, sizeof(thedata->tcapOmInterval));
		header_complex_add_data(&tcapOmDevelRecv15minIntTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("tcapOmMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int tcapOmDevelRecv15minIntTable_del(struct tcapOmDevelRecv15minIntTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the tcapOmDevelRecv15minIntTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
tcapOmDevelRecv15minIntTable_del(struct tcapOmDevelRecv15minIntTable_data *thedata)
{
	struct tcapOmDevelRecv15minIntTable_data *StorageDel;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmDevelRecv15minIntTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(tcapOmDevelRecv15minIntTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&tcapOmDevelRecv15minIntTableStorage, hciptr);
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_tcapOmDevelRecv15minIntTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for tcapOmDevelRecv15minIntTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case tcapOmDevelRecv15minIntTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_tcapOmDevelRecv15minIntTable(const char *token, char *line)
{
	size_t tmpsize;
	struct tcapOmDevelRecv15minIntTable_data *StorageTmp = tcapOmDevelRecv15minIntTable_create();

	(void) token;
	(void) line;
	DEBUGMSGTL(("tcapOmMIB", "parse_tcapOmDevelRecv15minIntTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->tcapApplicationSubsystemId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->tcapApplicationSubsystemId, &StorageTmp->tcapApplicationSubsystemIdLen);
	if (StorageTmp->tcapApplicationSubsystemId == NULL) {
		config_perror("invalid specification for tcapApplicationSubsystemId");
		return;
	}
	SNMP_FREE(StorageTmp->tcapApplicationEntityId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->tcapApplicationEntityId, &StorageTmp->tcapApplicationEntityIdLen);
	if (StorageTmp->tcapApplicationEntityId == NULL) {
		config_perror("invalid specification for tcapApplicationEntityId");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->tcapOmInterval, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecv15minIntPabortMessageType, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecv15minIntPabortIncorrectTp, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecv15minIntPabortBadTp, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecv15minIntRejectUnrecComp, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecv15minIntRejectMistypeComp, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecv15minIntRejectBadCompStruct, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecv15minIntRejectUnrecLinkId, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecv15minIntRejectUnrecIdRetRes, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecv15minIntRejectUnexpRetRes, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecv15minIntRejectUnrecIdRetErr, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecv15minIntRejectUnexpRetErr, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecv15minIntUsrRejDupInvokeId, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecv15minIntUsrRejUnrecOper, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecv15minIntUsrRejParmInvoke, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecv15minIntUsrRejReleasing, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecv15minIntUsrRejUnexpLinkResp, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecv15minIntUsrRejUnexpLinkOper, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecv15minIntUsrRejUnrecError, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecv15minIntUsrRejUnexpError, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecv15minIntUsrRejParmRetResult, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelRecv15minIntUsrRejParmRetError, &tmpsize);
	tcapOmDevelRecv15minIntTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
}

/*
 * store_tcapOmDevelRecv15minIntTable(): store configuraiton file for tcapOmDevelRecv15minIntTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_tcapOmDevelRecv15minIntTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct tcapOmDevelRecv15minIntTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	(void) majorID;
	(void) minorID;
	(void) serverarg;
	(void) clientarg;
	DEBUGMSGTL(("tcapOmMIB", "store_tcapOmDevelRecv15minIntTable: storing data...  "));
	refresh_tcapOmDevelRecv15minIntTable(1);
	(void) tmpsize;
	for (hcindex = tcapOmDevelRecv15minIntTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct tcapOmDevelRecv15minIntTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "tcapOmDevelRecv15minIntTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->tcapApplicationSubsystemId, &StorageTmp->tcapApplicationSubsystemIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->tcapApplicationEntityId, &StorageTmp->tcapApplicationEntityIdLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->tcapOmInterval, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecv15minIntPabortMessageType, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecv15minIntPabortIncorrectTp, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecv15minIntPabortBadTp, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecv15minIntRejectUnrecComp, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecv15minIntRejectMistypeComp, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecv15minIntRejectBadCompStruct, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecv15minIntRejectUnrecLinkId, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecv15minIntRejectUnrecIdRetRes, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecv15minIntRejectUnexpRetRes, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecv15minIntRejectUnrecIdRetErr, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecv15minIntRejectUnexpRetErr, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecv15minIntUsrRejDupInvokeId, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecv15minIntUsrRejUnrecOper, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecv15minIntUsrRejParmInvoke, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecv15minIntUsrRejReleasing, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecv15minIntUsrRejUnexpLinkResp, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecv15minIntUsrRejUnexpLinkOper, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecv15minIntUsrRejUnrecError, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecv15minIntUsrRejUnexpError, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecv15minIntUsrRejParmRetResult, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelRecv15minIntUsrRejParmRetError, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct tcapOmDevelSentTable_data *tcapOmDevelSentTable_create(void)
 * @brief create a fresh data structure representing a new row in the tcapOmDevelSentTable table.
 *
 * Creates a new tcapOmDevelSentTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct tcapOmDevelSentTable_data *
tcapOmDevelSentTable_create(void)
{
	struct tcapOmDevelSentTable_data *StorageNew = SNMP_MALLOC_STRUCT(tcapOmDevelSentTable_data);

	DEBUGMSGTL(("tcapOmMIB", "tcapOmDevelSentTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->tcapApplicationSubsystemId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->tcapApplicationSubsystemIdLen = 0;
		StorageNew->tcapApplicationSubsystemId[StorageNew->tcapApplicationSubsystemIdLen] = 0;
		StorageNew->tcapTcUserId = 0;
		StorageNew->tcapOmDevelSentPabortMessageType = 0;
		StorageNew->tcapOmDevelSentPabortIncorrectTp = 0;
		StorageNew->tcapOmDevelSentPabortBadTp = 0;
		StorageNew->tcapOmDevelSentRejectUnrecComp = 0;
		StorageNew->tcapOmDevelSentRejectMistypeComp = 0;
		StorageNew->tcapOmDevelSentRejectBadCompStruct = 0;
		StorageNew->tcapOmDevelSentRejectUnrecLinkId = 0;
		StorageNew->tcapOmDevelSentRejectUnrecIdRetRes = 0;
		StorageNew->tcapOmDevelSentRejectUnexpRetRes = 0;
		StorageNew->tcapOmDevelSentRejectUnrecIdRetErr = 0;
		StorageNew->tcapOmDevelSentRejectUnexpRetErr = 0;
		StorageNew->tcapOmDevelSentUsrRejDupInvokeId = 0;
		StorageNew->tcapOmDevelSentUsrRejUnrecOper = 0;
		StorageNew->tcapOmDevelSentUsrRejParmInvoke = 0;
		StorageNew->tcapOmDevelSentUsrRejReleasing = 0;
		StorageNew->tcapOmDevelSentUsrRejUnexpLinkResp = 0;
		StorageNew->tcapOmDevelSentUsrRejUnexpLinkOper = 0;
		StorageNew->tcapOmDevelSentUsrRejUnrecError = 0;
		StorageNew->tcapOmDevelSentUsrRejUnexpError = 0;
		StorageNew->tcapOmDevelSentUsrRejParmRetResult = 0;
		StorageNew->tcapOmDevelSentUsrRejParmRetError = 0;
		StorageNew->tcapOmDevelSent5minValidIntervals = 0;
		StorageNew->tcapOmDevelSent15minValidIntervals = 0;
	}
      done:
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	tcapOmDevelSentTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct tcapOmDevelSentTable_data *tcapOmDevelSentTable_duplicate(struct tcapOmDevelSentTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct tcapOmDevelSentTable_data *
tcapOmDevelSentTable_duplicate(struct tcapOmDevelSentTable_data *thedata)
{
	struct tcapOmDevelSentTable_data *StorageNew = SNMP_MALLOC_STRUCT(tcapOmDevelSentTable_data);

	DEBUGMSGTL(("tcapOmMIB", "tcapOmDevelSentTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->tcapOmDevelSentTable_id = thedata->tcapOmDevelSentTable_id;
		if (!(StorageNew->tcapApplicationSubsystemId = malloc(thedata->tcapApplicationSubsystemIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->tcapApplicationSubsystemId, thedata->tcapApplicationSubsystemId, thedata->tcapApplicationSubsystemIdLen);
		StorageNew->tcapApplicationSubsystemIdLen = thedata->tcapApplicationSubsystemIdLen;
		StorageNew->tcapApplicationSubsystemId[StorageNew->tcapApplicationSubsystemIdLen] = 0;
		StorageNew->tcapTcUserId = thedata->tcapTcUserId;
		StorageNew->tcapOmDevelSentPabortMessageType = thedata->tcapOmDevelSentPabortMessageType;
		StorageNew->tcapOmDevelSentPabortIncorrectTp = thedata->tcapOmDevelSentPabortIncorrectTp;
		StorageNew->tcapOmDevelSentPabortBadTp = thedata->tcapOmDevelSentPabortBadTp;
		StorageNew->tcapOmDevelSentRejectUnrecComp = thedata->tcapOmDevelSentRejectUnrecComp;
		StorageNew->tcapOmDevelSentRejectMistypeComp = thedata->tcapOmDevelSentRejectMistypeComp;
		StorageNew->tcapOmDevelSentRejectBadCompStruct = thedata->tcapOmDevelSentRejectBadCompStruct;
		StorageNew->tcapOmDevelSentRejectUnrecLinkId = thedata->tcapOmDevelSentRejectUnrecLinkId;
		StorageNew->tcapOmDevelSentRejectUnrecIdRetRes = thedata->tcapOmDevelSentRejectUnrecIdRetRes;
		StorageNew->tcapOmDevelSentRejectUnexpRetRes = thedata->tcapOmDevelSentRejectUnexpRetRes;
		StorageNew->tcapOmDevelSentRejectUnrecIdRetErr = thedata->tcapOmDevelSentRejectUnrecIdRetErr;
		StorageNew->tcapOmDevelSentRejectUnexpRetErr = thedata->tcapOmDevelSentRejectUnexpRetErr;
		StorageNew->tcapOmDevelSentUsrRejDupInvokeId = thedata->tcapOmDevelSentUsrRejDupInvokeId;
		StorageNew->tcapOmDevelSentUsrRejUnrecOper = thedata->tcapOmDevelSentUsrRejUnrecOper;
		StorageNew->tcapOmDevelSentUsrRejParmInvoke = thedata->tcapOmDevelSentUsrRejParmInvoke;
		StorageNew->tcapOmDevelSentUsrRejReleasing = thedata->tcapOmDevelSentUsrRejReleasing;
		StorageNew->tcapOmDevelSentUsrRejUnexpLinkResp = thedata->tcapOmDevelSentUsrRejUnexpLinkResp;
		StorageNew->tcapOmDevelSentUsrRejUnexpLinkOper = thedata->tcapOmDevelSentUsrRejUnexpLinkOper;
		StorageNew->tcapOmDevelSentUsrRejUnrecError = thedata->tcapOmDevelSentUsrRejUnrecError;
		StorageNew->tcapOmDevelSentUsrRejUnexpError = thedata->tcapOmDevelSentUsrRejUnexpError;
		StorageNew->tcapOmDevelSentUsrRejParmRetResult = thedata->tcapOmDevelSentUsrRejParmRetResult;
		StorageNew->tcapOmDevelSentUsrRejParmRetError = thedata->tcapOmDevelSentUsrRejParmRetError;
		StorageNew->tcapOmDevelSent5minValidIntervals = thedata->tcapOmDevelSent5minValidIntervals;
		StorageNew->tcapOmDevelSent15minValidIntervals = thedata->tcapOmDevelSent15minValidIntervals;
	}
      done:
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	tcapOmDevelSentTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int tcapOmDevelSentTable_destroy(struct tcapOmDevelSentTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
tcapOmDevelSentTable_destroy(struct tcapOmDevelSentTable_data **thedata)
{
	struct tcapOmDevelSentTable_data *StorageDel;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmDevelSentTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->tcapApplicationSubsystemId);
		StorageDel->tcapApplicationSubsystemIdLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int tcapOmDevelSentTable_add(struct tcapOmDevelSentTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the tcapOmDevelSentTable table data set.
 *
 * Adds a table row structure to the tcapOmDevelSentTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
tcapOmDevelSentTable_add(struct tcapOmDevelSentTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmDevelSentTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* tcapApplicationSubsystemId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->tcapApplicationSubsystemId, thedata->tcapApplicationSubsystemIdLen);
		/* tcapTcUserId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->tcapTcUserId, sizeof(thedata->tcapTcUserId));
		header_complex_add_data(&tcapOmDevelSentTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("tcapOmMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int tcapOmDevelSentTable_del(struct tcapOmDevelSentTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the tcapOmDevelSentTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
tcapOmDevelSentTable_del(struct tcapOmDevelSentTable_data *thedata)
{
	struct tcapOmDevelSentTable_data *StorageDel;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmDevelSentTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(tcapOmDevelSentTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&tcapOmDevelSentTableStorage, hciptr);
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_tcapOmDevelSentTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for tcapOmDevelSentTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case tcapOmDevelSentTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_tcapOmDevelSentTable(const char *token, char *line)
{
	size_t tmpsize;
	struct tcapOmDevelSentTable_data *StorageTmp = tcapOmDevelSentTable_create();

	(void) token;
	(void) line;
	DEBUGMSGTL(("tcapOmMIB", "parse_tcapOmDevelSentTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->tcapApplicationSubsystemId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->tcapApplicationSubsystemId, &StorageTmp->tcapApplicationSubsystemIdLen);
	if (StorageTmp->tcapApplicationSubsystemId == NULL) {
		config_perror("invalid specification for tcapApplicationSubsystemId");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->tcapTcUserId, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmDevelSentPabortMessageType, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmDevelSentPabortIncorrectTp, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmDevelSentPabortBadTp, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmDevelSentRejectUnrecComp, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmDevelSentRejectMistypeComp, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmDevelSentRejectBadCompStruct, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmDevelSentRejectUnrecLinkId, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmDevelSentRejectUnrecIdRetRes, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmDevelSentRejectUnexpRetRes, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmDevelSentRejectUnrecIdRetErr, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmDevelSentRejectUnexpRetErr, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmDevelSentUsrRejDupInvokeId, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmDevelSentUsrRejUnrecOper, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmDevelSentUsrRejParmInvoke, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmDevelSentUsrRejReleasing, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmDevelSentUsrRejUnexpLinkResp, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmDevelSentUsrRejUnexpLinkOper, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmDevelSentUsrRejUnrecError, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmDevelSentUsrRejUnexpError, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmDevelSentUsrRejParmRetResult, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->tcapOmDevelSentUsrRejParmRetError, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->tcapOmDevelSent5minValidIntervals, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->tcapOmDevelSent15minValidIntervals, &tmpsize);
	tcapOmDevelSentTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
}

/*
 * store_tcapOmDevelSentTable(): store configuraiton file for tcapOmDevelSentTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_tcapOmDevelSentTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct tcapOmDevelSentTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	(void) majorID;
	(void) minorID;
	(void) serverarg;
	(void) clientarg;
	DEBUGMSGTL(("tcapOmMIB", "store_tcapOmDevelSentTable: storing data...  "));
	refresh_tcapOmDevelSentTable(1);
	(void) tmpsize;
	for (hcindex = tcapOmDevelSentTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct tcapOmDevelSentTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "tcapOmDevelSentTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->tcapApplicationSubsystemId, &StorageTmp->tcapApplicationSubsystemIdLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->tcapTcUserId, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmDevelSentPabortMessageType, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmDevelSentPabortIncorrectTp, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmDevelSentPabortBadTp, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmDevelSentRejectUnrecComp, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmDevelSentRejectMistypeComp, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmDevelSentRejectBadCompStruct, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmDevelSentRejectUnrecLinkId, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmDevelSentRejectUnrecIdRetRes, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmDevelSentRejectUnexpRetRes, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmDevelSentRejectUnrecIdRetErr, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmDevelSentRejectUnexpRetErr, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmDevelSentUsrRejDupInvokeId, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmDevelSentUsrRejUnrecOper, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmDevelSentUsrRejParmInvoke, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmDevelSentUsrRejReleasing, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmDevelSentUsrRejUnexpLinkResp, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmDevelSentUsrRejUnexpLinkOper, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmDevelSentUsrRejUnrecError, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmDevelSentUsrRejUnexpError, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmDevelSentUsrRejParmRetResult, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->tcapOmDevelSentUsrRejParmRetError, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->tcapOmDevelSent5minValidIntervals, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->tcapOmDevelSent15minValidIntervals, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct tcapOmDevelSentCurrentTable_data *tcapOmDevelSentCurrentTable_create(void)
 * @brief create a fresh data structure representing a new row in the tcapOmDevelSentCurrentTable table.
 *
 * Creates a new tcapOmDevelSentCurrentTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct tcapOmDevelSentCurrentTable_data *
tcapOmDevelSentCurrentTable_create(void)
{
	struct tcapOmDevelSentCurrentTable_data *StorageNew = SNMP_MALLOC_STRUCT(tcapOmDevelSentCurrentTable_data);

	DEBUGMSGTL(("tcapOmMIB", "tcapOmDevelSentCurrentTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->tcapApplicationSubsystemId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->tcapApplicationSubsystemIdLen = 0;
		StorageNew->tcapApplicationSubsystemId[StorageNew->tcapApplicationSubsystemIdLen] = 0;
		StorageNew->tcapTcUserId = 0;
		StorageNew->tcapOmDevelSentCurrentPabortMessageType = 0;
		StorageNew->tcapOmDevelSentCurrentPabortIncorrectTp = 0;
		StorageNew->tcapOmDevelSentCurrentPabortBadTp = 0;
		StorageNew->tcapOmDevelSentCurrentRejectUnrecComp = 0;
		StorageNew->tcapOmDevelSentCurrentRejectMistypeComp = 0;
		StorageNew->tcapOmDevelSentCurrentRejectBadCompStruct = 0;
		StorageNew->tcapOmDevelSentCurrentRejectUnrecLinkId = 0;
		StorageNew->tcapOmDevelSentCurrentRejectUnrecIdRetRes = 0;
		StorageNew->tcapOmDevelSentCurrentRejectUnexpRetRes = 0;
		StorageNew->tcapOmDevelSentCurrentRejectUnrecIdRetErr = 0;
		StorageNew->tcapOmDevelSentCurrentRejectUnexpRetErr = 0;
		StorageNew->tcapOmDevelSentCurrentUsrRejDupInvokeId = 0;
		StorageNew->tcapOmDevelSentCurrentUsrRejUnrecOper = 0;
		StorageNew->tcapOmDevelSentCurrentUsrRejParmInvoke = 0;
		StorageNew->tcapOmDevelSentCurrentUsrRejReleasing = 0;
		StorageNew->tcapOmDevelSentCurrentUsrRejUnexpLinkResp = 0;
		StorageNew->tcapOmDevelSentCurrentUsrRejUnexpLinkOper = 0;
		StorageNew->tcapOmDevelSentCurrentUsrRejUnrecError = 0;
		StorageNew->tcapOmDevelSentCurrentUsrRejUnexpError = 0;
		StorageNew->tcapOmDevelSentCurrentUsrRejParmRetResult = 0;
		StorageNew->tcapOmDevelSentCurrentUsrRejParmRetError = 0;
	}
      done:
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	tcapOmDevelSentCurrentTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct tcapOmDevelSentCurrentTable_data *tcapOmDevelSentCurrentTable_duplicate(struct tcapOmDevelSentCurrentTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct tcapOmDevelSentCurrentTable_data *
tcapOmDevelSentCurrentTable_duplicate(struct tcapOmDevelSentCurrentTable_data *thedata)
{
	struct tcapOmDevelSentCurrentTable_data *StorageNew = SNMP_MALLOC_STRUCT(tcapOmDevelSentCurrentTable_data);

	DEBUGMSGTL(("tcapOmMIB", "tcapOmDevelSentCurrentTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->tcapOmDevelSentCurrentTable_id = thedata->tcapOmDevelSentCurrentTable_id;
		if (!(StorageNew->tcapApplicationSubsystemId = malloc(thedata->tcapApplicationSubsystemIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->tcapApplicationSubsystemId, thedata->tcapApplicationSubsystemId, thedata->tcapApplicationSubsystemIdLen);
		StorageNew->tcapApplicationSubsystemIdLen = thedata->tcapApplicationSubsystemIdLen;
		StorageNew->tcapApplicationSubsystemId[StorageNew->tcapApplicationSubsystemIdLen] = 0;
		StorageNew->tcapTcUserId = thedata->tcapTcUserId;
		StorageNew->tcapOmDevelSentCurrentPabortMessageType = thedata->tcapOmDevelSentCurrentPabortMessageType;
		StorageNew->tcapOmDevelSentCurrentPabortIncorrectTp = thedata->tcapOmDevelSentCurrentPabortIncorrectTp;
		StorageNew->tcapOmDevelSentCurrentPabortBadTp = thedata->tcapOmDevelSentCurrentPabortBadTp;
		StorageNew->tcapOmDevelSentCurrentRejectUnrecComp = thedata->tcapOmDevelSentCurrentRejectUnrecComp;
		StorageNew->tcapOmDevelSentCurrentRejectMistypeComp = thedata->tcapOmDevelSentCurrentRejectMistypeComp;
		StorageNew->tcapOmDevelSentCurrentRejectBadCompStruct = thedata->tcapOmDevelSentCurrentRejectBadCompStruct;
		StorageNew->tcapOmDevelSentCurrentRejectUnrecLinkId = thedata->tcapOmDevelSentCurrentRejectUnrecLinkId;
		StorageNew->tcapOmDevelSentCurrentRejectUnrecIdRetRes = thedata->tcapOmDevelSentCurrentRejectUnrecIdRetRes;
		StorageNew->tcapOmDevelSentCurrentRejectUnexpRetRes = thedata->tcapOmDevelSentCurrentRejectUnexpRetRes;
		StorageNew->tcapOmDevelSentCurrentRejectUnrecIdRetErr = thedata->tcapOmDevelSentCurrentRejectUnrecIdRetErr;
		StorageNew->tcapOmDevelSentCurrentRejectUnexpRetErr = thedata->tcapOmDevelSentCurrentRejectUnexpRetErr;
		StorageNew->tcapOmDevelSentCurrentUsrRejDupInvokeId = thedata->tcapOmDevelSentCurrentUsrRejDupInvokeId;
		StorageNew->tcapOmDevelSentCurrentUsrRejUnrecOper = thedata->tcapOmDevelSentCurrentUsrRejUnrecOper;
		StorageNew->tcapOmDevelSentCurrentUsrRejParmInvoke = thedata->tcapOmDevelSentCurrentUsrRejParmInvoke;
		StorageNew->tcapOmDevelSentCurrentUsrRejReleasing = thedata->tcapOmDevelSentCurrentUsrRejReleasing;
		StorageNew->tcapOmDevelSentCurrentUsrRejUnexpLinkResp = thedata->tcapOmDevelSentCurrentUsrRejUnexpLinkResp;
		StorageNew->tcapOmDevelSentCurrentUsrRejUnexpLinkOper = thedata->tcapOmDevelSentCurrentUsrRejUnexpLinkOper;
		StorageNew->tcapOmDevelSentCurrentUsrRejUnrecError = thedata->tcapOmDevelSentCurrentUsrRejUnrecError;
		StorageNew->tcapOmDevelSentCurrentUsrRejUnexpError = thedata->tcapOmDevelSentCurrentUsrRejUnexpError;
		StorageNew->tcapOmDevelSentCurrentUsrRejParmRetResult = thedata->tcapOmDevelSentCurrentUsrRejParmRetResult;
		StorageNew->tcapOmDevelSentCurrentUsrRejParmRetError = thedata->tcapOmDevelSentCurrentUsrRejParmRetError;
	}
      done:
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	tcapOmDevelSentCurrentTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int tcapOmDevelSentCurrentTable_destroy(struct tcapOmDevelSentCurrentTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
tcapOmDevelSentCurrentTable_destroy(struct tcapOmDevelSentCurrentTable_data **thedata)
{
	struct tcapOmDevelSentCurrentTable_data *StorageDel;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmDevelSentCurrentTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->tcapApplicationSubsystemId);
		StorageDel->tcapApplicationSubsystemIdLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int tcapOmDevelSentCurrentTable_add(struct tcapOmDevelSentCurrentTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the tcapOmDevelSentCurrentTable table data set.
 *
 * Adds a table row structure to the tcapOmDevelSentCurrentTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
tcapOmDevelSentCurrentTable_add(struct tcapOmDevelSentCurrentTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmDevelSentCurrentTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* tcapApplicationSubsystemId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->tcapApplicationSubsystemId, thedata->tcapApplicationSubsystemIdLen);
		/* tcapTcUserId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->tcapTcUserId, sizeof(thedata->tcapTcUserId));
		header_complex_add_data(&tcapOmDevelSentCurrentTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("tcapOmMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int tcapOmDevelSentCurrentTable_del(struct tcapOmDevelSentCurrentTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the tcapOmDevelSentCurrentTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
tcapOmDevelSentCurrentTable_del(struct tcapOmDevelSentCurrentTable_data *thedata)
{
	struct tcapOmDevelSentCurrentTable_data *StorageDel;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmDevelSentCurrentTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(tcapOmDevelSentCurrentTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&tcapOmDevelSentCurrentTableStorage, hciptr);
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_tcapOmDevelSentCurrentTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for tcapOmDevelSentCurrentTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case tcapOmDevelSentCurrentTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_tcapOmDevelSentCurrentTable(const char *token, char *line)
{
	size_t tmpsize;
	struct tcapOmDevelSentCurrentTable_data *StorageTmp = tcapOmDevelSentCurrentTable_create();

	(void) token;
	(void) line;
	DEBUGMSGTL(("tcapOmMIB", "parse_tcapOmDevelSentCurrentTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->tcapApplicationSubsystemId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->tcapApplicationSubsystemId, &StorageTmp->tcapApplicationSubsystemIdLen);
	if (StorageTmp->tcapApplicationSubsystemId == NULL) {
		config_perror("invalid specification for tcapApplicationSubsystemId");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->tcapTcUserId, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSentCurrentPabortMessageType, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSentCurrentPabortIncorrectTp, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSentCurrentPabortBadTp, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSentCurrentRejectUnrecComp, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSentCurrentRejectMistypeComp, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSentCurrentRejectBadCompStruct, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSentCurrentRejectUnrecLinkId, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSentCurrentRejectUnrecIdRetRes, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSentCurrentRejectUnexpRetRes, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSentCurrentRejectUnrecIdRetErr, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSentCurrentRejectUnexpRetErr, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSentCurrentUsrRejDupInvokeId, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSentCurrentUsrRejUnrecOper, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSentCurrentUsrRejParmInvoke, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSentCurrentUsrRejReleasing, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSentCurrentUsrRejUnexpLinkResp, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSentCurrentUsrRejUnexpLinkOper, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSentCurrentUsrRejUnrecError, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSentCurrentUsrRejUnexpError, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSentCurrentUsrRejParmRetResult, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSentCurrentUsrRejParmRetError, &tmpsize);
	tcapOmDevelSentCurrentTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
}

/*
 * store_tcapOmDevelSentCurrentTable(): store configuraiton file for tcapOmDevelSentCurrentTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_tcapOmDevelSentCurrentTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct tcapOmDevelSentCurrentTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	(void) majorID;
	(void) minorID;
	(void) serverarg;
	(void) clientarg;
	DEBUGMSGTL(("tcapOmMIB", "store_tcapOmDevelSentCurrentTable: storing data...  "));
	refresh_tcapOmDevelSentCurrentTable(1);
	(void) tmpsize;
	for (hcindex = tcapOmDevelSentCurrentTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct tcapOmDevelSentCurrentTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "tcapOmDevelSentCurrentTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->tcapApplicationSubsystemId, &StorageTmp->tcapApplicationSubsystemIdLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->tcapTcUserId, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSentCurrentPabortMessageType, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSentCurrentPabortIncorrectTp, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSentCurrentPabortBadTp, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSentCurrentRejectUnrecComp, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSentCurrentRejectMistypeComp, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSentCurrentRejectBadCompStruct, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSentCurrentRejectUnrecLinkId, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSentCurrentRejectUnrecIdRetRes, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSentCurrentRejectUnexpRetRes, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSentCurrentRejectUnrecIdRetErr, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSentCurrentRejectUnexpRetErr, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSentCurrentUsrRejDupInvokeId, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSentCurrentUsrRejUnrecOper, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSentCurrentUsrRejParmInvoke, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSentCurrentUsrRejReleasing, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSentCurrentUsrRejUnexpLinkResp, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSentCurrentUsrRejUnexpLinkOper, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSentCurrentUsrRejUnrecError, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSentCurrentUsrRejUnexpError, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSentCurrentUsrRejParmRetResult, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSentCurrentUsrRejParmRetError, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct tcapOmDevelSent5minIntTable_data *tcapOmDevelSent5minIntTable_create(void)
 * @brief create a fresh data structure representing a new row in the tcapOmDevelSent5minIntTable table.
 *
 * Creates a new tcapOmDevelSent5minIntTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct tcapOmDevelSent5minIntTable_data *
tcapOmDevelSent5minIntTable_create(void)
{
	struct tcapOmDevelSent5minIntTable_data *StorageNew = SNMP_MALLOC_STRUCT(tcapOmDevelSent5minIntTable_data);

	DEBUGMSGTL(("tcapOmMIB", "tcapOmDevelSent5minIntTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->tcapApplicationSubsystemId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->tcapApplicationSubsystemIdLen = 0;
		StorageNew->tcapApplicationSubsystemId[StorageNew->tcapApplicationSubsystemIdLen] = 0;
		StorageNew->tcapTcUserId = 0;
		StorageNew->tcapOmInterval = 0;
		StorageNew->tcapOmDevelSent5minIntPabortMessageType = 0;
		StorageNew->tcapOmDevelSent5minIntPabortIncorrectTp = 0;
		StorageNew->tcapOmDevelSent5minIntPabortBadTp = 0;
		StorageNew->tcapOmDevelSent5minIntRejectUnrecComp = 0;
		StorageNew->tcapOmDevelSent5minIntRejectMistypeComp = 0;
		StorageNew->tcapOmDevelSent5minIntRejectBadCompStruct = 0;
		StorageNew->tcapOmDevelSent5minIntRejectUnrecLinkId = 0;
		StorageNew->tcapOmDevelSent5minIntRejectUnrecIdRetRes = 0;
		StorageNew->tcapOmDevelSent5minIntRejectUnexpRetRes = 0;
		StorageNew->tcapOmDevelSent5minIntRejectUnrecIdRetErr = 0;
		StorageNew->tcapOmDevelSent5minIntRejectUnexpRetErr = 0;
		StorageNew->tcapOmDevelSent5minIntUsrRejDupInvokeId = 0;
		StorageNew->tcapOmDevelSent5minIntUsrRejUnrecOper = 0;
		StorageNew->tcapOmDevelSent5minIntUsrRejParmInvoke = 0;
		StorageNew->tcapOmDevelSent5minIntUsrRejReleasing = 0;
		StorageNew->tcapOmDevelSent5minIntUsrRejUnexpLinkResp = 0;
		StorageNew->tcapOmDevelSent5minIntUsrRejUnexpLinkOper = 0;
		StorageNew->tcapOmDevelSent5minIntUsrRejUnrecError = 0;
		StorageNew->tcapOmDevelSent5minIntUsrRejUnexpError = 0;
		StorageNew->tcapOmDevelSent5minIntUsrRejParmRetResult = 0;
		StorageNew->tcapOmDevelSent5minIntUsrRejParmRetError = 0;
	}
      done:
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	tcapOmDevelSent5minIntTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct tcapOmDevelSent5minIntTable_data *tcapOmDevelSent5minIntTable_duplicate(struct tcapOmDevelSent5minIntTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct tcapOmDevelSent5minIntTable_data *
tcapOmDevelSent5minIntTable_duplicate(struct tcapOmDevelSent5minIntTable_data *thedata)
{
	struct tcapOmDevelSent5minIntTable_data *StorageNew = SNMP_MALLOC_STRUCT(tcapOmDevelSent5minIntTable_data);

	DEBUGMSGTL(("tcapOmMIB", "tcapOmDevelSent5minIntTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->tcapOmDevelSent5minIntTable_id = thedata->tcapOmDevelSent5minIntTable_id;
		if (!(StorageNew->tcapApplicationSubsystemId = malloc(thedata->tcapApplicationSubsystemIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->tcapApplicationSubsystemId, thedata->tcapApplicationSubsystemId, thedata->tcapApplicationSubsystemIdLen);
		StorageNew->tcapApplicationSubsystemIdLen = thedata->tcapApplicationSubsystemIdLen;
		StorageNew->tcapApplicationSubsystemId[StorageNew->tcapApplicationSubsystemIdLen] = 0;
		StorageNew->tcapTcUserId = thedata->tcapTcUserId;
		StorageNew->tcapOmInterval = thedata->tcapOmInterval;
		StorageNew->tcapOmDevelSent5minIntPabortMessageType = thedata->tcapOmDevelSent5minIntPabortMessageType;
		StorageNew->tcapOmDevelSent5minIntPabortIncorrectTp = thedata->tcapOmDevelSent5minIntPabortIncorrectTp;
		StorageNew->tcapOmDevelSent5minIntPabortBadTp = thedata->tcapOmDevelSent5minIntPabortBadTp;
		StorageNew->tcapOmDevelSent5minIntRejectUnrecComp = thedata->tcapOmDevelSent5minIntRejectUnrecComp;
		StorageNew->tcapOmDevelSent5minIntRejectMistypeComp = thedata->tcapOmDevelSent5minIntRejectMistypeComp;
		StorageNew->tcapOmDevelSent5minIntRejectBadCompStruct = thedata->tcapOmDevelSent5minIntRejectBadCompStruct;
		StorageNew->tcapOmDevelSent5minIntRejectUnrecLinkId = thedata->tcapOmDevelSent5minIntRejectUnrecLinkId;
		StorageNew->tcapOmDevelSent5minIntRejectUnrecIdRetRes = thedata->tcapOmDevelSent5minIntRejectUnrecIdRetRes;
		StorageNew->tcapOmDevelSent5minIntRejectUnexpRetRes = thedata->tcapOmDevelSent5minIntRejectUnexpRetRes;
		StorageNew->tcapOmDevelSent5minIntRejectUnrecIdRetErr = thedata->tcapOmDevelSent5minIntRejectUnrecIdRetErr;
		StorageNew->tcapOmDevelSent5minIntRejectUnexpRetErr = thedata->tcapOmDevelSent5minIntRejectUnexpRetErr;
		StorageNew->tcapOmDevelSent5minIntUsrRejDupInvokeId = thedata->tcapOmDevelSent5minIntUsrRejDupInvokeId;
		StorageNew->tcapOmDevelSent5minIntUsrRejUnrecOper = thedata->tcapOmDevelSent5minIntUsrRejUnrecOper;
		StorageNew->tcapOmDevelSent5minIntUsrRejParmInvoke = thedata->tcapOmDevelSent5minIntUsrRejParmInvoke;
		StorageNew->tcapOmDevelSent5minIntUsrRejReleasing = thedata->tcapOmDevelSent5minIntUsrRejReleasing;
		StorageNew->tcapOmDevelSent5minIntUsrRejUnexpLinkResp = thedata->tcapOmDevelSent5minIntUsrRejUnexpLinkResp;
		StorageNew->tcapOmDevelSent5minIntUsrRejUnexpLinkOper = thedata->tcapOmDevelSent5minIntUsrRejUnexpLinkOper;
		StorageNew->tcapOmDevelSent5minIntUsrRejUnrecError = thedata->tcapOmDevelSent5minIntUsrRejUnrecError;
		StorageNew->tcapOmDevelSent5minIntUsrRejUnexpError = thedata->tcapOmDevelSent5minIntUsrRejUnexpError;
		StorageNew->tcapOmDevelSent5minIntUsrRejParmRetResult = thedata->tcapOmDevelSent5minIntUsrRejParmRetResult;
		StorageNew->tcapOmDevelSent5minIntUsrRejParmRetError = thedata->tcapOmDevelSent5minIntUsrRejParmRetError;
	}
      done:
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	tcapOmDevelSent5minIntTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int tcapOmDevelSent5minIntTable_destroy(struct tcapOmDevelSent5minIntTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
tcapOmDevelSent5minIntTable_destroy(struct tcapOmDevelSent5minIntTable_data **thedata)
{
	struct tcapOmDevelSent5minIntTable_data *StorageDel;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmDevelSent5minIntTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->tcapApplicationSubsystemId);
		StorageDel->tcapApplicationSubsystemIdLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int tcapOmDevelSent5minIntTable_add(struct tcapOmDevelSent5minIntTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the tcapOmDevelSent5minIntTable table data set.
 *
 * Adds a table row structure to the tcapOmDevelSent5minIntTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
tcapOmDevelSent5minIntTable_add(struct tcapOmDevelSent5minIntTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmDevelSent5minIntTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* tcapApplicationSubsystemId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->tcapApplicationSubsystemId, thedata->tcapApplicationSubsystemIdLen);
		/* tcapTcUserId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->tcapTcUserId, sizeof(thedata->tcapTcUserId));
		/* tcapOmInterval */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->tcapOmInterval, sizeof(thedata->tcapOmInterval));
		header_complex_add_data(&tcapOmDevelSent5minIntTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("tcapOmMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int tcapOmDevelSent5minIntTable_del(struct tcapOmDevelSent5minIntTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the tcapOmDevelSent5minIntTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
tcapOmDevelSent5minIntTable_del(struct tcapOmDevelSent5minIntTable_data *thedata)
{
	struct tcapOmDevelSent5minIntTable_data *StorageDel;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmDevelSent5minIntTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(tcapOmDevelSent5minIntTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&tcapOmDevelSent5minIntTableStorage, hciptr);
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_tcapOmDevelSent5minIntTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for tcapOmDevelSent5minIntTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case tcapOmDevelSent5minIntTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_tcapOmDevelSent5minIntTable(const char *token, char *line)
{
	size_t tmpsize;
	struct tcapOmDevelSent5minIntTable_data *StorageTmp = tcapOmDevelSent5minIntTable_create();

	(void) token;
	(void) line;
	DEBUGMSGTL(("tcapOmMIB", "parse_tcapOmDevelSent5minIntTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->tcapApplicationSubsystemId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->tcapApplicationSubsystemId, &StorageTmp->tcapApplicationSubsystemIdLen);
	if (StorageTmp->tcapApplicationSubsystemId == NULL) {
		config_perror("invalid specification for tcapApplicationSubsystemId");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->tcapTcUserId, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->tcapOmInterval, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSent5minIntPabortMessageType, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSent5minIntPabortIncorrectTp, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSent5minIntPabortBadTp, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSent5minIntRejectUnrecComp, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSent5minIntRejectMistypeComp, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSent5minIntRejectBadCompStruct, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSent5minIntRejectUnrecLinkId, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSent5minIntRejectUnrecIdRetRes, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSent5minIntRejectUnexpRetRes, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSent5minIntRejectUnrecIdRetErr, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSent5minIntRejectUnexpRetErr, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSent5minIntUsrRejDupInvokeId, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSent5minIntUsrRejUnrecOper, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSent5minIntUsrRejParmInvoke, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSent5minIntUsrRejReleasing, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSent5minIntUsrRejUnexpLinkResp, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSent5minIntUsrRejUnexpLinkOper, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSent5minIntUsrRejUnrecError, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSent5minIntUsrRejUnexpError, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSent5minIntUsrRejParmRetResult, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSent5minIntUsrRejParmRetError, &tmpsize);
	tcapOmDevelSent5minIntTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
}

/*
 * store_tcapOmDevelSent5minIntTable(): store configuraiton file for tcapOmDevelSent5minIntTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_tcapOmDevelSent5minIntTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct tcapOmDevelSent5minIntTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	(void) majorID;
	(void) minorID;
	(void) serverarg;
	(void) clientarg;
	DEBUGMSGTL(("tcapOmMIB", "store_tcapOmDevelSent5minIntTable: storing data...  "));
	refresh_tcapOmDevelSent5minIntTable(1);
	(void) tmpsize;
	for (hcindex = tcapOmDevelSent5minIntTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct tcapOmDevelSent5minIntTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "tcapOmDevelSent5minIntTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->tcapApplicationSubsystemId, &StorageTmp->tcapApplicationSubsystemIdLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->tcapTcUserId, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->tcapOmInterval, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSent5minIntPabortMessageType, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSent5minIntPabortIncorrectTp, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSent5minIntPabortBadTp, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSent5minIntRejectUnrecComp, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSent5minIntRejectMistypeComp, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSent5minIntRejectBadCompStruct, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSent5minIntRejectUnrecLinkId, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSent5minIntRejectUnrecIdRetRes, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSent5minIntRejectUnexpRetRes, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSent5minIntRejectUnrecIdRetErr, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSent5minIntRejectUnexpRetErr, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSent5minIntUsrRejDupInvokeId, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSent5minIntUsrRejUnrecOper, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSent5minIntUsrRejParmInvoke, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSent5minIntUsrRejReleasing, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSent5minIntUsrRejUnexpLinkResp, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSent5minIntUsrRejUnexpLinkOper, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSent5minIntUsrRejUnrecError, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSent5minIntUsrRejUnexpError, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSent5minIntUsrRejParmRetResult, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSent5minIntUsrRejParmRetError, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct tcapOmDevelSent15minIntTable_data *tcapOmDevelSent15minIntTable_create(void)
 * @brief create a fresh data structure representing a new row in the tcapOmDevelSent15minIntTable table.
 *
 * Creates a new tcapOmDevelSent15minIntTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct tcapOmDevelSent15minIntTable_data *
tcapOmDevelSent15minIntTable_create(void)
{
	struct tcapOmDevelSent15minIntTable_data *StorageNew = SNMP_MALLOC_STRUCT(tcapOmDevelSent15minIntTable_data);

	DEBUGMSGTL(("tcapOmMIB", "tcapOmDevelSent15minIntTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->tcapApplicationSubsystemId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->tcapApplicationSubsystemIdLen = 0;
		StorageNew->tcapApplicationSubsystemId[StorageNew->tcapApplicationSubsystemIdLen] = 0;
		StorageNew->tcapTcUserId = 0;
		StorageNew->tcapOmInterval = 0;
		StorageNew->tcapOmDevelSent15minIntPabortMessageType = 0;
		StorageNew->tcapOmDevelSent15minIntPabortIncorrectTp = 0;
		StorageNew->tcapOmDevelSent15minIntPabortBadTp = 0;
		StorageNew->tcapOmDevelSent15minIntRejectUnrecComp = 0;
		StorageNew->tcapOmDevelSent15minIntRejectMistypeComp = 0;
		StorageNew->tcapOmDevelSent15minIntRejectBadCompStruct = 0;
		StorageNew->tcapOmDevelSent15minIntRejectUnrecLinkId = 0;
		StorageNew->tcapOmDevelSent15minIntRejectUnrecIdRetRes = 0;
		StorageNew->tcapOmDevelSent15minIntRejectUnexpRetRes = 0;
		StorageNew->tcapOmDevelSent15minIntRejectUnrecIdRetErr = 0;
		StorageNew->tcapOmDevelSent15minIntRejectUnexpRetErr = 0;
		StorageNew->tcapOmDevelSent15minIntUsrRejDupInvokeId = 0;
		StorageNew->tcapOmDevelSent15minIntUsrRejUnrecOper = 0;
		StorageNew->tcapOmDevelSent15minIntUsrRejParmInvoke = 0;
		StorageNew->tcapOmDevelSent15minIntUsrRejReleasing = 0;
		StorageNew->tcapOmDevelSent15minIntUsrRejUnexpLinkResp = 0;
		StorageNew->tcapOmDevelSent15minIntUsrRejUnexpLinkOper = 0;
		StorageNew->tcapOmDevelSent15minIntUsrRejUnrecError = 0;
		StorageNew->tcapOmDevelSent15minIntUsrRejUnexpError = 0;
		StorageNew->tcapOmDevelSent15minIntUsrRejParmRetResult = 0;
		StorageNew->tcapOmDevelSent15minIntUsrRejParmRetError = 0;
	}
      done:
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	tcapOmDevelSent15minIntTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct tcapOmDevelSent15minIntTable_data *tcapOmDevelSent15minIntTable_duplicate(struct tcapOmDevelSent15minIntTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct tcapOmDevelSent15minIntTable_data *
tcapOmDevelSent15minIntTable_duplicate(struct tcapOmDevelSent15minIntTable_data *thedata)
{
	struct tcapOmDevelSent15minIntTable_data *StorageNew = SNMP_MALLOC_STRUCT(tcapOmDevelSent15minIntTable_data);

	DEBUGMSGTL(("tcapOmMIB", "tcapOmDevelSent15minIntTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->tcapOmDevelSent15minIntTable_id = thedata->tcapOmDevelSent15minIntTable_id;
		if (!(StorageNew->tcapApplicationSubsystemId = malloc(thedata->tcapApplicationSubsystemIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->tcapApplicationSubsystemId, thedata->tcapApplicationSubsystemId, thedata->tcapApplicationSubsystemIdLen);
		StorageNew->tcapApplicationSubsystemIdLen = thedata->tcapApplicationSubsystemIdLen;
		StorageNew->tcapApplicationSubsystemId[StorageNew->tcapApplicationSubsystemIdLen] = 0;
		StorageNew->tcapTcUserId = thedata->tcapTcUserId;
		StorageNew->tcapOmInterval = thedata->tcapOmInterval;
		StorageNew->tcapOmDevelSent15minIntPabortMessageType = thedata->tcapOmDevelSent15minIntPabortMessageType;
		StorageNew->tcapOmDevelSent15minIntPabortIncorrectTp = thedata->tcapOmDevelSent15minIntPabortIncorrectTp;
		StorageNew->tcapOmDevelSent15minIntPabortBadTp = thedata->tcapOmDevelSent15minIntPabortBadTp;
		StorageNew->tcapOmDevelSent15minIntRejectUnrecComp = thedata->tcapOmDevelSent15minIntRejectUnrecComp;
		StorageNew->tcapOmDevelSent15minIntRejectMistypeComp = thedata->tcapOmDevelSent15minIntRejectMistypeComp;
		StorageNew->tcapOmDevelSent15minIntRejectBadCompStruct = thedata->tcapOmDevelSent15minIntRejectBadCompStruct;
		StorageNew->tcapOmDevelSent15minIntRejectUnrecLinkId = thedata->tcapOmDevelSent15minIntRejectUnrecLinkId;
		StorageNew->tcapOmDevelSent15minIntRejectUnrecIdRetRes = thedata->tcapOmDevelSent15minIntRejectUnrecIdRetRes;
		StorageNew->tcapOmDevelSent15minIntRejectUnexpRetRes = thedata->tcapOmDevelSent15minIntRejectUnexpRetRes;
		StorageNew->tcapOmDevelSent15minIntRejectUnrecIdRetErr = thedata->tcapOmDevelSent15minIntRejectUnrecIdRetErr;
		StorageNew->tcapOmDevelSent15minIntRejectUnexpRetErr = thedata->tcapOmDevelSent15minIntRejectUnexpRetErr;
		StorageNew->tcapOmDevelSent15minIntUsrRejDupInvokeId = thedata->tcapOmDevelSent15minIntUsrRejDupInvokeId;
		StorageNew->tcapOmDevelSent15minIntUsrRejUnrecOper = thedata->tcapOmDevelSent15minIntUsrRejUnrecOper;
		StorageNew->tcapOmDevelSent15minIntUsrRejParmInvoke = thedata->tcapOmDevelSent15minIntUsrRejParmInvoke;
		StorageNew->tcapOmDevelSent15minIntUsrRejReleasing = thedata->tcapOmDevelSent15minIntUsrRejReleasing;
		StorageNew->tcapOmDevelSent15minIntUsrRejUnexpLinkResp = thedata->tcapOmDevelSent15minIntUsrRejUnexpLinkResp;
		StorageNew->tcapOmDevelSent15minIntUsrRejUnexpLinkOper = thedata->tcapOmDevelSent15minIntUsrRejUnexpLinkOper;
		StorageNew->tcapOmDevelSent15minIntUsrRejUnrecError = thedata->tcapOmDevelSent15minIntUsrRejUnrecError;
		StorageNew->tcapOmDevelSent15minIntUsrRejUnexpError = thedata->tcapOmDevelSent15minIntUsrRejUnexpError;
		StorageNew->tcapOmDevelSent15minIntUsrRejParmRetResult = thedata->tcapOmDevelSent15minIntUsrRejParmRetResult;
		StorageNew->tcapOmDevelSent15minIntUsrRejParmRetError = thedata->tcapOmDevelSent15minIntUsrRejParmRetError;
	}
      done:
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	tcapOmDevelSent15minIntTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int tcapOmDevelSent15minIntTable_destroy(struct tcapOmDevelSent15minIntTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
tcapOmDevelSent15minIntTable_destroy(struct tcapOmDevelSent15minIntTable_data **thedata)
{
	struct tcapOmDevelSent15minIntTable_data *StorageDel;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmDevelSent15minIntTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->tcapApplicationSubsystemId);
		StorageDel->tcapApplicationSubsystemIdLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int tcapOmDevelSent15minIntTable_add(struct tcapOmDevelSent15minIntTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the tcapOmDevelSent15minIntTable table data set.
 *
 * Adds a table row structure to the tcapOmDevelSent15minIntTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
tcapOmDevelSent15minIntTable_add(struct tcapOmDevelSent15minIntTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmDevelSent15minIntTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* tcapApplicationSubsystemId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->tcapApplicationSubsystemId, thedata->tcapApplicationSubsystemIdLen);
		/* tcapTcUserId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->tcapTcUserId, sizeof(thedata->tcapTcUserId));
		/* tcapOmInterval */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->tcapOmInterval, sizeof(thedata->tcapOmInterval));
		header_complex_add_data(&tcapOmDevelSent15minIntTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("tcapOmMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int tcapOmDevelSent15minIntTable_del(struct tcapOmDevelSent15minIntTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the tcapOmDevelSent15minIntTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
tcapOmDevelSent15minIntTable_del(struct tcapOmDevelSent15minIntTable_data *thedata)
{
	struct tcapOmDevelSent15minIntTable_data *StorageDel;

	DEBUGMSGTL(("tcapOmMIB", "tcapOmDevelSent15minIntTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(tcapOmDevelSent15minIntTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&tcapOmDevelSent15minIntTableStorage, hciptr);
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_tcapOmDevelSent15minIntTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for tcapOmDevelSent15minIntTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case tcapOmDevelSent15minIntTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_tcapOmDevelSent15minIntTable(const char *token, char *line)
{
	size_t tmpsize;
	struct tcapOmDevelSent15minIntTable_data *StorageTmp = tcapOmDevelSent15minIntTable_create();

	(void) token;
	(void) line;
	DEBUGMSGTL(("tcapOmMIB", "parse_tcapOmDevelSent15minIntTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->tcapApplicationSubsystemId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->tcapApplicationSubsystemId, &StorageTmp->tcapApplicationSubsystemIdLen);
	if (StorageTmp->tcapApplicationSubsystemId == NULL) {
		config_perror("invalid specification for tcapApplicationSubsystemId");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->tcapTcUserId, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->tcapOmInterval, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSent15minIntPabortMessageType, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSent15minIntPabortIncorrectTp, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSent15minIntPabortBadTp, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSent15minIntRejectUnrecComp, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSent15minIntRejectMistypeComp, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSent15minIntRejectBadCompStruct, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSent15minIntRejectUnrecLinkId, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSent15minIntRejectUnrecIdRetRes, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSent15minIntRejectUnexpRetRes, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSent15minIntRejectUnrecIdRetErr, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSent15minIntRejectUnexpRetErr, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSent15minIntUsrRejDupInvokeId, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSent15minIntUsrRejUnrecOper, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSent15minIntUsrRejParmInvoke, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSent15minIntUsrRejReleasing, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSent15minIntUsrRejUnexpLinkResp, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSent15minIntUsrRejUnexpLinkOper, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSent15minIntUsrRejUnrecError, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSent15minIntUsrRejUnexpError, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSent15minIntUsrRejParmRetResult, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->tcapOmDevelSent15minIntUsrRejParmRetError, &tmpsize);
	tcapOmDevelSent15minIntTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
}

/*
 * store_tcapOmDevelSent15minIntTable(): store configuraiton file for tcapOmDevelSent15minIntTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_tcapOmDevelSent15minIntTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct tcapOmDevelSent15minIntTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	(void) majorID;
	(void) minorID;
	(void) serverarg;
	(void) clientarg;
	DEBUGMSGTL(("tcapOmMIB", "store_tcapOmDevelSent15minIntTable: storing data...  "));
	refresh_tcapOmDevelSent15minIntTable(1);
	(void) tmpsize;
	for (hcindex = tcapOmDevelSent15minIntTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct tcapOmDevelSent15minIntTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "tcapOmDevelSent15minIntTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->tcapApplicationSubsystemId, &StorageTmp->tcapApplicationSubsystemIdLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->tcapTcUserId, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->tcapOmInterval, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSent15minIntPabortMessageType, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSent15minIntPabortIncorrectTp, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSent15minIntPabortBadTp, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSent15minIntRejectUnrecComp, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSent15minIntRejectMistypeComp, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSent15minIntRejectBadCompStruct, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSent15minIntRejectUnrecLinkId, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSent15minIntRejectUnrecIdRetRes, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSent15minIntRejectUnexpRetRes, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSent15minIntRejectUnrecIdRetErr, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSent15minIntRejectUnexpRetErr, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSent15minIntUsrRejDupInvokeId, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSent15minIntUsrRejUnrecOper, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSent15minIntUsrRejParmInvoke, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSent15minIntUsrRejReleasing, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSent15minIntUsrRejUnexpLinkResp, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSent15minIntUsrRejUnexpLinkOper, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSent15minIntUsrRejUnrecError, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSent15minIntUsrRejUnexpError, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSent15minIntUsrRejParmRetResult, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->tcapOmDevelSent15minIntUsrRejParmRetError, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int check_tcapOmNodeTable_row(struct tcapOmNodeTable_data *StorageTmp, struct tcapOmNodeTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_tcapOmNodeTable_row(struct tcapOmNodeTable_data *StorageTmp, struct tcapOmNodeTable_data *StorageOld)
{
	(void) StorageTmp;
	(void) StorageOld;
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_tcapOmNodeTable_row(struct tcapOmNodeTable_data *StorageTmp, struct tcapOmNodeTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_tcapOmNodeTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_tcapOmNodeTable_row(struct tcapOmNodeTable_data *StorageTmp, struct tcapOmNodeTable_data *StorageOld)
{
	(void) StorageTmp;
	(void) StorageOld;
	/* XXX: provide code to update the row with the underlying device */
	tcapOmNodeTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_tcapOmNodeTable_row(struct tcapOmNodeTable_data *StorageTmp, struct tcapOmNodeTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_tcapOmNodeTable_row(struct tcapOmNodeTable_data *StorageTmp, struct tcapOmNodeTable_data *StorageOld)
{
	(void) StorageTmp;
	/* XXX: provide code to revert the row with the underlying device */
	update_tcapOmNodeTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_tcapOmNodeTable_row(struct tcapOmNodeTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the tcapOmNodeTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct tcapOmNodeTable_data *
refresh_tcapOmNodeTable_row(struct tcapOmNodeTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->tcapOmNodeTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->tcapOmNodeTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_tcapOmNodeTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the tcapOmNodeTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_tcapOmNodeTable(int force)
{
	if (!force && tcapOmNodeTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	tcapOmNodeTable_refresh = 0;
}

/**
 * @fn u_char *var_tcapOmNodeTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in tcapOmNodeTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_tcapOmMIB above.
 */
u_char *
var_tcapOmNodeTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct tcapOmNodeTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("tcapOmMIB", "var_tcapOmNodeTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_tcapOmNodeTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(tcapOmNodeTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_tcapOmNodeTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) TCAPOMNODEMESSAGESSENT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmNodeMessagesSent);
		rval = (u_char *) &StorageTmp->tcapOmNodeMessagesSent;
		break;
	case (u_char) TCAPOMNODEMESSAGESRECV:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmNodeMessagesRecv);
		rval = (u_char *) &StorageTmp->tcapOmNodeMessagesRecv;
		break;
	case (u_char) TCAPOMNODECOMPONENTSSENT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmNodeComponentsSent);
		rval = (u_char *) &StorageTmp->tcapOmNodeComponentsSent;
		break;
	case (u_char) TCAPOMNODECOMPONENTSRECV:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmNodeComponentsRecv);
		rval = (u_char *) &StorageTmp->tcapOmNodeComponentsRecv;
		break;
	case (u_char) TCAPOMNODE5MINVALIDINTERVALS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmNode5minValidIntervals);
		rval = (u_char *) &StorageTmp->tcapOmNode5minValidIntervals;
		break;
	case (u_char) TCAPOMNODE15MINVALIDINTERVALS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmNode15minValidIntervals);
		rval = (u_char *) &StorageTmp->tcapOmNode15minValidIntervals;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_tcapOmNodeCurrentTable_row(struct tcapOmNodeCurrentTable_data *StorageTmp, struct tcapOmNodeCurrentTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_tcapOmNodeCurrentTable_row(struct tcapOmNodeCurrentTable_data *StorageTmp, struct tcapOmNodeCurrentTable_data *StorageOld)
{
	(void) StorageTmp;
	(void) StorageOld;
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_tcapOmNodeCurrentTable_row(struct tcapOmNodeCurrentTable_data *StorageTmp, struct tcapOmNodeCurrentTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_tcapOmNodeCurrentTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_tcapOmNodeCurrentTable_row(struct tcapOmNodeCurrentTable_data *StorageTmp, struct tcapOmNodeCurrentTable_data *StorageOld)
{
	(void) StorageTmp;
	(void) StorageOld;
	/* XXX: provide code to update the row with the underlying device */
	tcapOmNodeCurrentTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_tcapOmNodeCurrentTable_row(struct tcapOmNodeCurrentTable_data *StorageTmp, struct tcapOmNodeCurrentTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_tcapOmNodeCurrentTable_row(struct tcapOmNodeCurrentTable_data *StorageTmp, struct tcapOmNodeCurrentTable_data *StorageOld)
{
	(void) StorageTmp;
	/* XXX: provide code to revert the row with the underlying device */
	update_tcapOmNodeCurrentTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_tcapOmNodeCurrentTable_row(struct tcapOmNodeCurrentTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the tcapOmNodeCurrentTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct tcapOmNodeCurrentTable_data *
refresh_tcapOmNodeCurrentTable_row(struct tcapOmNodeCurrentTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->tcapOmNodeCurrentTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->tcapOmNodeCurrentTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_tcapOmNodeCurrentTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the tcapOmNodeCurrentTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_tcapOmNodeCurrentTable(int force)
{
	if (!force && tcapOmNodeCurrentTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	tcapOmNodeCurrentTable_refresh = 0;
}

/**
 * @fn u_char *var_tcapOmNodeCurrentTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in tcapOmNodeCurrentTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_tcapOmMIB above.
 */
u_char *
var_tcapOmNodeCurrentTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct tcapOmNodeCurrentTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("tcapOmMIB", "var_tcapOmNodeCurrentTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_tcapOmNodeCurrentTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(tcapOmNodeCurrentTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_tcapOmNodeCurrentTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) TCAPOMNODECURRENTMESSAGESSENT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmNodeCurrentMessagesSent);
		rval = (u_char *) &StorageTmp->tcapOmNodeCurrentMessagesSent;
		break;
	case (u_char) TCAPOMNODECURRENTMESSAGESRECV:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmNodeCurrentMessagesRecv);
		rval = (u_char *) &StorageTmp->tcapOmNodeCurrentMessagesRecv;
		break;
	case (u_char) TCAPOMNODECURRENTCOMPONENTSSENT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmNodeCurrentComponentsSent);
		rval = (u_char *) &StorageTmp->tcapOmNodeCurrentComponentsSent;
		break;
	case (u_char) TCAPOMNODECURRENTCOMPONENTSRECV:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmNodeCurrentComponentsRecv);
		rval = (u_char *) &StorageTmp->tcapOmNodeCurrentComponentsRecv;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_tcapOmNode5minIntTable_row(struct tcapOmNode5minIntTable_data *StorageTmp, struct tcapOmNode5minIntTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_tcapOmNode5minIntTable_row(struct tcapOmNode5minIntTable_data *StorageTmp, struct tcapOmNode5minIntTable_data *StorageOld)
{
	(void) StorageTmp;
	(void) StorageOld;
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_tcapOmNode5minIntTable_row(struct tcapOmNode5minIntTable_data *StorageTmp, struct tcapOmNode5minIntTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_tcapOmNode5minIntTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_tcapOmNode5minIntTable_row(struct tcapOmNode5minIntTable_data *StorageTmp, struct tcapOmNode5minIntTable_data *StorageOld)
{
	(void) StorageTmp;
	(void) StorageOld;
	/* XXX: provide code to update the row with the underlying device */
	tcapOmNode5minIntTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_tcapOmNode5minIntTable_row(struct tcapOmNode5minIntTable_data *StorageTmp, struct tcapOmNode5minIntTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_tcapOmNode5minIntTable_row(struct tcapOmNode5minIntTable_data *StorageTmp, struct tcapOmNode5minIntTable_data *StorageOld)
{
	(void) StorageTmp;
	/* XXX: provide code to revert the row with the underlying device */
	update_tcapOmNode5minIntTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_tcapOmNode5minIntTable_row(struct tcapOmNode5minIntTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the tcapOmNode5minIntTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct tcapOmNode5minIntTable_data *
refresh_tcapOmNode5minIntTable_row(struct tcapOmNode5minIntTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->tcapOmNode5minIntTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->tcapOmNode5minIntTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_tcapOmNode5minIntTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the tcapOmNode5minIntTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_tcapOmNode5minIntTable(int force)
{
	if (!force && tcapOmNode5minIntTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	tcapOmNode5minIntTable_refresh = 0;
}

/**
 * @fn u_char *var_tcapOmNode5minIntTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in tcapOmNode5minIntTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_tcapOmMIB above.
 */
u_char *
var_tcapOmNode5minIntTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct tcapOmNode5minIntTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("tcapOmMIB", "var_tcapOmNode5minIntTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_tcapOmNode5minIntTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(tcapOmNode5minIntTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_tcapOmNode5minIntTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) TCAPOMNODE5MININTMESSAGESSENT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmNode5minIntMessagesSent);
		rval = (u_char *) &StorageTmp->tcapOmNode5minIntMessagesSent;
		break;
	case (u_char) TCAPOMNODE5MININTMESSAGESRECV:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmNode5minIntMessagesRecv);
		rval = (u_char *) &StorageTmp->tcapOmNode5minIntMessagesRecv;
		break;
	case (u_char) TCAPOMNODE5MININTCOMPONENTSSENT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmNode5minIntComponentsSent);
		rval = (u_char *) &StorageTmp->tcapOmNode5minIntComponentsSent;
		break;
	case (u_char) TCAPOMNODE5MININTCOMPONENTSRECV:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmNode5minIntComponentsRecv);
		rval = (u_char *) &StorageTmp->tcapOmNode5minIntComponentsRecv;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_tcapOmNode15minIntTable_row(struct tcapOmNode15minIntTable_data *StorageTmp, struct tcapOmNode15minIntTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_tcapOmNode15minIntTable_row(struct tcapOmNode15minIntTable_data *StorageTmp, struct tcapOmNode15minIntTable_data *StorageOld)
{
	(void) StorageTmp;
	(void) StorageOld;
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_tcapOmNode15minIntTable_row(struct tcapOmNode15minIntTable_data *StorageTmp, struct tcapOmNode15minIntTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_tcapOmNode15minIntTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_tcapOmNode15minIntTable_row(struct tcapOmNode15minIntTable_data *StorageTmp, struct tcapOmNode15minIntTable_data *StorageOld)
{
	(void) StorageTmp;
	(void) StorageOld;
	/* XXX: provide code to update the row with the underlying device */
	tcapOmNode15minIntTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_tcapOmNode15minIntTable_row(struct tcapOmNode15minIntTable_data *StorageTmp, struct tcapOmNode15minIntTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_tcapOmNode15minIntTable_row(struct tcapOmNode15minIntTable_data *StorageTmp, struct tcapOmNode15minIntTable_data *StorageOld)
{
	(void) StorageTmp;
	/* XXX: provide code to revert the row with the underlying device */
	update_tcapOmNode15minIntTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_tcapOmNode15minIntTable_row(struct tcapOmNode15minIntTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the tcapOmNode15minIntTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct tcapOmNode15minIntTable_data *
refresh_tcapOmNode15minIntTable_row(struct tcapOmNode15minIntTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->tcapOmNode15minIntTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->tcapOmNode15minIntTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_tcapOmNode15minIntTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the tcapOmNode15minIntTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_tcapOmNode15minIntTable(int force)
{
	if (!force && tcapOmNode15minIntTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	tcapOmNode15minIntTable_refresh = 0;
}

/**
 * @fn u_char *var_tcapOmNode15minIntTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in tcapOmNode15minIntTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_tcapOmMIB above.
 */
u_char *
var_tcapOmNode15minIntTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct tcapOmNode15minIntTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("tcapOmMIB", "var_tcapOmNode15minIntTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_tcapOmNode15minIntTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(tcapOmNode15minIntTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_tcapOmNode15minIntTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) TCAPOMNODE15MININTMESSAGESSENT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmNode15minIntMessagesSent);
		rval = (u_char *) &StorageTmp->tcapOmNode15minIntMessagesSent;
		break;
	case (u_char) TCAPOMNODE15MININTMESSAGESRECV:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmNode15minIntMessagesRecv);
		rval = (u_char *) &StorageTmp->tcapOmNode15minIntMessagesRecv;
		break;
	case (u_char) TCAPOMNODE15MININTCOMPONENTSSENT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmNode15minIntComponentsSent);
		rval = (u_char *) &StorageTmp->tcapOmNode15minIntComponentsSent;
		break;
	case (u_char) TCAPOMNODE15MININTCOMPONENTSRECV:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmNode15minIntComponentsRecv);
		rval = (u_char *) &StorageTmp->tcapOmNode15minIntComponentsRecv;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_tcapOmMsgsTable_row(struct tcapOmMsgsTable_data *StorageTmp, struct tcapOmMsgsTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_tcapOmMsgsTable_row(struct tcapOmMsgsTable_data *StorageTmp, struct tcapOmMsgsTable_data *StorageOld)
{
	(void) StorageTmp;
	(void) StorageOld;
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_tcapOmMsgsTable_row(struct tcapOmMsgsTable_data *StorageTmp, struct tcapOmMsgsTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_tcapOmMsgsTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_tcapOmMsgsTable_row(struct tcapOmMsgsTable_data *StorageTmp, struct tcapOmMsgsTable_data *StorageOld)
{
	(void) StorageTmp;
	(void) StorageOld;
	/* XXX: provide code to update the row with the underlying device */
	tcapOmMsgsTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_tcapOmMsgsTable_row(struct tcapOmMsgsTable_data *StorageTmp, struct tcapOmMsgsTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_tcapOmMsgsTable_row(struct tcapOmMsgsTable_data *StorageTmp, struct tcapOmMsgsTable_data *StorageOld)
{
	(void) StorageTmp;
	/* XXX: provide code to revert the row with the underlying device */
	update_tcapOmMsgsTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_tcapOmMsgsTable_row(struct tcapOmMsgsTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the tcapOmMsgsTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct tcapOmMsgsTable_data *
refresh_tcapOmMsgsTable_row(struct tcapOmMsgsTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->tcapOmMsgsTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->tcapOmMsgsTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_tcapOmMsgsTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the tcapOmMsgsTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_tcapOmMsgsTable(int force)
{
	if (!force && tcapOmMsgsTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	tcapOmMsgsTable_refresh = 0;
}

/**
 * @fn u_char *var_tcapOmMsgsTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in tcapOmMsgsTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_tcapOmMIB above.
 */
u_char *
var_tcapOmMsgsTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct tcapOmMsgsTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("tcapOmMIB", "var_tcapOmMsgsTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_tcapOmMsgsTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(tcapOmMsgsTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_tcapOmMsgsTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) TCAPOMMSGSMESSAGESSENT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmMsgsMessagesSent);
		rval = (u_char *) &StorageTmp->tcapOmMsgsMessagesSent;
		break;
	case (u_char) TCAPOMMSGSMESSAGESRECV:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmMsgsMessagesRecv);
		rval = (u_char *) &StorageTmp->tcapOmMsgsMessagesRecv;
		break;
	case (u_char) TCAPOMMSGS5MINVALIDINTERVALS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmMsgs5minValidIntervals);
		rval = (u_char *) &StorageTmp->tcapOmMsgs5minValidIntervals;
		break;
	case (u_char) TCAPOMMSGS15MINVALIDINTERVALS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmMsgs15minValidIntervals);
		rval = (u_char *) &StorageTmp->tcapOmMsgs15minValidIntervals;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_tcapOmMsgsCurrentTable_row(struct tcapOmMsgsCurrentTable_data *StorageTmp, struct tcapOmMsgsCurrentTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_tcapOmMsgsCurrentTable_row(struct tcapOmMsgsCurrentTable_data *StorageTmp, struct tcapOmMsgsCurrentTable_data *StorageOld)
{
	(void) StorageTmp;
	(void) StorageOld;
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_tcapOmMsgsCurrentTable_row(struct tcapOmMsgsCurrentTable_data *StorageTmp, struct tcapOmMsgsCurrentTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_tcapOmMsgsCurrentTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_tcapOmMsgsCurrentTable_row(struct tcapOmMsgsCurrentTable_data *StorageTmp, struct tcapOmMsgsCurrentTable_data *StorageOld)
{
	(void) StorageTmp;
	(void) StorageOld;
	/* XXX: provide code to update the row with the underlying device */
	tcapOmMsgsCurrentTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_tcapOmMsgsCurrentTable_row(struct tcapOmMsgsCurrentTable_data *StorageTmp, struct tcapOmMsgsCurrentTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_tcapOmMsgsCurrentTable_row(struct tcapOmMsgsCurrentTable_data *StorageTmp, struct tcapOmMsgsCurrentTable_data *StorageOld)
{
	(void) StorageTmp;
	/* XXX: provide code to revert the row with the underlying device */
	update_tcapOmMsgsCurrentTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_tcapOmMsgsCurrentTable_row(struct tcapOmMsgsCurrentTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the tcapOmMsgsCurrentTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct tcapOmMsgsCurrentTable_data *
refresh_tcapOmMsgsCurrentTable_row(struct tcapOmMsgsCurrentTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->tcapOmMsgsCurrentTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->tcapOmMsgsCurrentTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_tcapOmMsgsCurrentTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the tcapOmMsgsCurrentTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_tcapOmMsgsCurrentTable(int force)
{
	if (!force && tcapOmMsgsCurrentTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	tcapOmMsgsCurrentTable_refresh = 0;
}

/**
 * @fn u_char *var_tcapOmMsgsCurrentTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in tcapOmMsgsCurrentTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_tcapOmMIB above.
 */
u_char *
var_tcapOmMsgsCurrentTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct tcapOmMsgsCurrentTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("tcapOmMIB", "var_tcapOmMsgsCurrentTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_tcapOmMsgsCurrentTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(tcapOmMsgsCurrentTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_tcapOmMsgsCurrentTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) TCAPOMMSGSCURRENTMESSAGESSENT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmMsgsCurrentMessagesSent);
		rval = (u_char *) &StorageTmp->tcapOmMsgsCurrentMessagesSent;
		break;
	case (u_char) TCAPOMMSGSCURRENTMESSAGESRECV:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmMsgsCurrentMessagesRecv);
		rval = (u_char *) &StorageTmp->tcapOmMsgsCurrentMessagesRecv;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_tcapOmMsgs5minIntTable_row(struct tcapOmMsgs5minIntTable_data *StorageTmp, struct tcapOmMsgs5minIntTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_tcapOmMsgs5minIntTable_row(struct tcapOmMsgs5minIntTable_data *StorageTmp, struct tcapOmMsgs5minIntTable_data *StorageOld)
{
	(void) StorageTmp;
	(void) StorageOld;
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_tcapOmMsgs5minIntTable_row(struct tcapOmMsgs5minIntTable_data *StorageTmp, struct tcapOmMsgs5minIntTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_tcapOmMsgs5minIntTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_tcapOmMsgs5minIntTable_row(struct tcapOmMsgs5minIntTable_data *StorageTmp, struct tcapOmMsgs5minIntTable_data *StorageOld)
{
	(void) StorageTmp;
	(void) StorageOld;
	/* XXX: provide code to update the row with the underlying device */
	tcapOmMsgs5minIntTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_tcapOmMsgs5minIntTable_row(struct tcapOmMsgs5minIntTable_data *StorageTmp, struct tcapOmMsgs5minIntTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_tcapOmMsgs5minIntTable_row(struct tcapOmMsgs5minIntTable_data *StorageTmp, struct tcapOmMsgs5minIntTable_data *StorageOld)
{
	(void) StorageTmp;
	/* XXX: provide code to revert the row with the underlying device */
	update_tcapOmMsgs5minIntTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_tcapOmMsgs5minIntTable_row(struct tcapOmMsgs5minIntTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the tcapOmMsgs5minIntTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct tcapOmMsgs5minIntTable_data *
refresh_tcapOmMsgs5minIntTable_row(struct tcapOmMsgs5minIntTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->tcapOmMsgs5minIntTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->tcapOmMsgs5minIntTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_tcapOmMsgs5minIntTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the tcapOmMsgs5minIntTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_tcapOmMsgs5minIntTable(int force)
{
	if (!force && tcapOmMsgs5minIntTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	tcapOmMsgs5minIntTable_refresh = 0;
}

/**
 * @fn u_char *var_tcapOmMsgs5minIntTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in tcapOmMsgs5minIntTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_tcapOmMIB above.
 */
u_char *
var_tcapOmMsgs5minIntTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct tcapOmMsgs5minIntTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("tcapOmMIB", "var_tcapOmMsgs5minIntTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_tcapOmMsgs5minIntTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(tcapOmMsgs5minIntTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_tcapOmMsgs5minIntTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) TCAPOMMSGS5MININTMESSAGESSENT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmMsgs5minIntMessagesSent);
		rval = (u_char *) &StorageTmp->tcapOmMsgs5minIntMessagesSent;
		break;
	case (u_char) TCAPOMMSGS5MININTMESSAGESRECV:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmMsgs5minIntMessagesRecv);
		rval = (u_char *) &StorageTmp->tcapOmMsgs5minIntMessagesRecv;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_tcapOmMsgs15minIntTable_row(struct tcapOmMsgs15minIntTable_data *StorageTmp, struct tcapOmMsgs15minIntTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_tcapOmMsgs15minIntTable_row(struct tcapOmMsgs15minIntTable_data *StorageTmp, struct tcapOmMsgs15minIntTable_data *StorageOld)
{
	(void) StorageTmp;
	(void) StorageOld;
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_tcapOmMsgs15minIntTable_row(struct tcapOmMsgs15minIntTable_data *StorageTmp, struct tcapOmMsgs15minIntTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_tcapOmMsgs15minIntTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_tcapOmMsgs15minIntTable_row(struct tcapOmMsgs15minIntTable_data *StorageTmp, struct tcapOmMsgs15minIntTable_data *StorageOld)
{
	(void) StorageTmp;
	(void) StorageOld;
	/* XXX: provide code to update the row with the underlying device */
	tcapOmMsgs15minIntTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_tcapOmMsgs15minIntTable_row(struct tcapOmMsgs15minIntTable_data *StorageTmp, struct tcapOmMsgs15minIntTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_tcapOmMsgs15minIntTable_row(struct tcapOmMsgs15minIntTable_data *StorageTmp, struct tcapOmMsgs15minIntTable_data *StorageOld)
{
	(void) StorageTmp;
	/* XXX: provide code to revert the row with the underlying device */
	update_tcapOmMsgs15minIntTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_tcapOmMsgs15minIntTable_row(struct tcapOmMsgs15minIntTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the tcapOmMsgs15minIntTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct tcapOmMsgs15minIntTable_data *
refresh_tcapOmMsgs15minIntTable_row(struct tcapOmMsgs15minIntTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->tcapOmMsgs15minIntTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->tcapOmMsgs15minIntTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_tcapOmMsgs15minIntTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the tcapOmMsgs15minIntTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_tcapOmMsgs15minIntTable(int force)
{
	if (!force && tcapOmMsgs15minIntTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	tcapOmMsgs15minIntTable_refresh = 0;
}

/**
 * @fn u_char *var_tcapOmMsgs15minIntTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in tcapOmMsgs15minIntTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_tcapOmMIB above.
 */
u_char *
var_tcapOmMsgs15minIntTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct tcapOmMsgs15minIntTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("tcapOmMIB", "var_tcapOmMsgs15minIntTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_tcapOmMsgs15minIntTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(tcapOmMsgs15minIntTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_tcapOmMsgs15minIntTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) TCAPOMMSGS15MININTMESSAGESSENT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmMsgs15minIntMessagesSent);
		rval = (u_char *) &StorageTmp->tcapOmMsgs15minIntMessagesSent;
		break;
	case (u_char) TCAPOMMSGS15MININTMESSAGESRECV:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmMsgs15minIntMessagesRecv);
		rval = (u_char *) &StorageTmp->tcapOmMsgs15minIntMessagesRecv;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_tcapOmAETable_row(struct tcapOmAETable_data *StorageTmp, struct tcapOmAETable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_tcapOmAETable_row(struct tcapOmAETable_data *StorageTmp, struct tcapOmAETable_data *StorageOld)
{
	(void) StorageTmp;
	(void) StorageOld;
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_tcapOmAETable_row(struct tcapOmAETable_data *StorageTmp, struct tcapOmAETable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_tcapOmAETable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_tcapOmAETable_row(struct tcapOmAETable_data *StorageTmp, struct tcapOmAETable_data *StorageOld)
{
	(void) StorageTmp;
	(void) StorageOld;
	/* XXX: provide code to update the row with the underlying device */
	tcapOmAETable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_tcapOmAETable_row(struct tcapOmAETable_data *StorageTmp, struct tcapOmAETable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_tcapOmAETable_row(struct tcapOmAETable_data *StorageTmp, struct tcapOmAETable_data *StorageOld)
{
	(void) StorageTmp;
	/* XXX: provide code to revert the row with the underlying device */
	update_tcapOmAETable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_tcapOmAETable_row(struct tcapOmAETable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the tcapOmAETable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct tcapOmAETable_data *
refresh_tcapOmAETable_row(struct tcapOmAETable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->tcapOmAETable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->tcapOmAETable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_tcapOmAETable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the tcapOmAETable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_tcapOmAETable(int force)
{
	if (!force && tcapOmAETable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	tcapOmAETable_refresh = 0;
}

/**
 * @fn u_char *var_tcapOmAETable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in tcapOmAETable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_tcapOmMIB above.
 */
u_char *
var_tcapOmAETable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct tcapOmAETable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("tcapOmMIB", "var_tcapOmAETable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_tcapOmAETable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(tcapOmAETableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_tcapOmAETable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) TCAPOMAEMEASUREMENTDELETED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmAEMeasurementDeleted);
		rval = (u_char *) &StorageTmp->tcapOmAEMeasurementDeleted;
		break;
	case (u_char) TCAPOMAENEWTRANSACTIONS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmAENewTransactions);
		rval = (u_char *) &StorageTmp->tcapOmAENewTransactions;
		break;
	case (u_char) TCAPOMAEMEANOPENTRANSACTIONS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmAEMeanOpenTransactions);
		rval = (u_char *) &StorageTmp->tcapOmAEMeanOpenTransactions;
		break;
	case (u_char) TCAPOMAENOTUSED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmAENotUsed);
		rval = (u_char *) &StorageTmp->tcapOmAENotUsed;
		break;
	case (u_char) TCAPOMAECUMMULATIVEMEANDURATION:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmAECummulativeMeanDuration);
		rval = (u_char *) &StorageTmp->tcapOmAECummulativeMeanDuration;
		break;
	case (u_char) TCAPOMAEMAXIMUMOPENTRANACTIONS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmAEMaximumOpenTranactions);
		rval = (u_char *) &StorageTmp->tcapOmAEMaximumOpenTranactions;
		break;
	case (u_char) TCAPOMAETHRESHOLDEXCEEDED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmAEThresholdExceeded);
		rval = (u_char *) &StorageTmp->tcapOmAEThresholdExceeded;
		break;
	case (u_char) TCAPOMAE5MINVALIDINTERVALS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmAE5minValidIntervals);
		rval = (u_char *) &StorageTmp->tcapOmAE5minValidIntervals;
		break;
	case (u_char) TCAPOMAE15MINVALIDINTERVALS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmAE15minValidIntervals);
		rval = (u_char *) &StorageTmp->tcapOmAE15minValidIntervals;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_tcapOmAECurrentTable_row(struct tcapOmAECurrentTable_data *StorageTmp, struct tcapOmAECurrentTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_tcapOmAECurrentTable_row(struct tcapOmAECurrentTable_data *StorageTmp, struct tcapOmAECurrentTable_data *StorageOld)
{
	(void) StorageTmp;
	(void) StorageOld;
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_tcapOmAECurrentTable_row(struct tcapOmAECurrentTable_data *StorageTmp, struct tcapOmAECurrentTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_tcapOmAECurrentTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_tcapOmAECurrentTable_row(struct tcapOmAECurrentTable_data *StorageTmp, struct tcapOmAECurrentTable_data *StorageOld)
{
	(void) StorageTmp;
	(void) StorageOld;
	/* XXX: provide code to update the row with the underlying device */
	tcapOmAECurrentTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_tcapOmAECurrentTable_row(struct tcapOmAECurrentTable_data *StorageTmp, struct tcapOmAECurrentTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_tcapOmAECurrentTable_row(struct tcapOmAECurrentTable_data *StorageTmp, struct tcapOmAECurrentTable_data *StorageOld)
{
	(void) StorageTmp;
	/* XXX: provide code to revert the row with the underlying device */
	update_tcapOmAECurrentTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_tcapOmAECurrentTable_row(struct tcapOmAECurrentTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the tcapOmAECurrentTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct tcapOmAECurrentTable_data *
refresh_tcapOmAECurrentTable_row(struct tcapOmAECurrentTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->tcapOmAECurrentTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->tcapOmAECurrentTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_tcapOmAECurrentTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the tcapOmAECurrentTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_tcapOmAECurrentTable(int force)
{
	if (!force && tcapOmAECurrentTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	tcapOmAECurrentTable_refresh = 0;
}

/**
 * @fn u_char *var_tcapOmAECurrentTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in tcapOmAECurrentTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_tcapOmMIB above.
 */
u_char *
var_tcapOmAECurrentTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct tcapOmAECurrentTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("tcapOmMIB", "var_tcapOmAECurrentTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_tcapOmAECurrentTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(tcapOmAECurrentTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_tcapOmAECurrentTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) TCAPOMAECURRENTMEASUREMENTDELETED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmAECurrentMeasurementDeleted);
		rval = (u_char *) &StorageTmp->tcapOmAECurrentMeasurementDeleted;
		break;
	case (u_char) TCAPOMAECURRENTNEWTRANSACTIONS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmAECurrentNewTransactions);
		rval = (u_char *) &StorageTmp->tcapOmAECurrentNewTransactions;
		break;
	case (u_char) TCAPOMAECURRENTMEANOPENTRANSACTIONS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmAECurrentMeanOpenTransactions);
		rval = (u_char *) &StorageTmp->tcapOmAECurrentMeanOpenTransactions;
		break;
	case (u_char) TCAPOMAECURRENTNOTUSED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmAECurrentNotUsed);
		rval = (u_char *) &StorageTmp->tcapOmAECurrentNotUsed;
		break;
	case (u_char) TCAPOMAECURRENTCUMMULATIVEMEANDURATION:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmAECurrentCummulativeMeanDuration);
		rval = (u_char *) &StorageTmp->tcapOmAECurrentCummulativeMeanDuration;
		break;
	case (u_char) TCAPOMAECURRENTMAXIMUMOPENTRANACTIONS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmAECurrentMaximumOpenTranactions);
		rval = (u_char *) &StorageTmp->tcapOmAECurrentMaximumOpenTranactions;
		break;
	case (u_char) TCAPOMAECURRENTTHRESHOLDEXCEEDED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmAECurrentThresholdExceeded);
		rval = (u_char *) &StorageTmp->tcapOmAECurrentThresholdExceeded;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_tcapOmAE5minIntTable_row(struct tcapOmAE5minIntTable_data *StorageTmp, struct tcapOmAE5minIntTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_tcapOmAE5minIntTable_row(struct tcapOmAE5minIntTable_data *StorageTmp, struct tcapOmAE5minIntTable_data *StorageOld)
{
	(void) StorageTmp;
	(void) StorageOld;
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_tcapOmAE5minIntTable_row(struct tcapOmAE5minIntTable_data *StorageTmp, struct tcapOmAE5minIntTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_tcapOmAE5minIntTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_tcapOmAE5minIntTable_row(struct tcapOmAE5minIntTable_data *StorageTmp, struct tcapOmAE5minIntTable_data *StorageOld)
{
	(void) StorageTmp;
	(void) StorageOld;
	/* XXX: provide code to update the row with the underlying device */
	tcapOmAE5minIntTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_tcapOmAE5minIntTable_row(struct tcapOmAE5minIntTable_data *StorageTmp, struct tcapOmAE5minIntTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_tcapOmAE5minIntTable_row(struct tcapOmAE5minIntTable_data *StorageTmp, struct tcapOmAE5minIntTable_data *StorageOld)
{
	(void) StorageTmp;
	/* XXX: provide code to revert the row with the underlying device */
	update_tcapOmAE5minIntTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_tcapOmAE5minIntTable_row(struct tcapOmAE5minIntTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the tcapOmAE5minIntTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct tcapOmAE5minIntTable_data *
refresh_tcapOmAE5minIntTable_row(struct tcapOmAE5minIntTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->tcapOmAE5minIntTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->tcapOmAE5minIntTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_tcapOmAE5minIntTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the tcapOmAE5minIntTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_tcapOmAE5minIntTable(int force)
{
	if (!force && tcapOmAE5minIntTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	tcapOmAE5minIntTable_refresh = 0;
}

/**
 * @fn u_char *var_tcapOmAE5minIntTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in tcapOmAE5minIntTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_tcapOmMIB above.
 */
u_char *
var_tcapOmAE5minIntTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct tcapOmAE5minIntTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("tcapOmMIB", "var_tcapOmAE5minIntTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_tcapOmAE5minIntTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(tcapOmAE5minIntTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_tcapOmAE5minIntTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) TCAPOMAE5MININTMEASUREMENTDELETED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmAE5minIntMeasurementDeleted);
		rval = (u_char *) &StorageTmp->tcapOmAE5minIntMeasurementDeleted;
		break;
	case (u_char) TCAPOMAE5MININTNEWTRANSACTIONS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmAE5minIntNewTransactions);
		rval = (u_char *) &StorageTmp->tcapOmAE5minIntNewTransactions;
		break;
	case (u_char) TCAPOMAE5MININTMEANOPENTRANSACTIONS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmAE5minIntMeanOpenTransactions);
		rval = (u_char *) &StorageTmp->tcapOmAE5minIntMeanOpenTransactions;
		break;
	case (u_char) TCAPOMAE5MININTNOTUSED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmAE5minIntNotUsed);
		rval = (u_char *) &StorageTmp->tcapOmAE5minIntNotUsed;
		break;
	case (u_char) TCAPOMAE5MININTCUMMULATIVEMEANDURATION:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmAE5minIntCummulativeMeanDuration);
		rval = (u_char *) &StorageTmp->tcapOmAE5minIntCummulativeMeanDuration;
		break;
	case (u_char) TCAPOMAE5MININTMAXIMUMOPENTRANACTIONS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmAE5minIntMaximumOpenTranactions);
		rval = (u_char *) &StorageTmp->tcapOmAE5minIntMaximumOpenTranactions;
		break;
	case (u_char) TCAPOMAE5MININTTHRESHOLDEXCEEDED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmAE5minIntThresholdExceeded);
		rval = (u_char *) &StorageTmp->tcapOmAE5minIntThresholdExceeded;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_tcapOmAE15minIntTable_row(struct tcapOmAE15minIntTable_data *StorageTmp, struct tcapOmAE15minIntTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_tcapOmAE15minIntTable_row(struct tcapOmAE15minIntTable_data *StorageTmp, struct tcapOmAE15minIntTable_data *StorageOld)
{
	(void) StorageTmp;
	(void) StorageOld;
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_tcapOmAE15minIntTable_row(struct tcapOmAE15minIntTable_data *StorageTmp, struct tcapOmAE15minIntTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_tcapOmAE15minIntTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_tcapOmAE15minIntTable_row(struct tcapOmAE15minIntTable_data *StorageTmp, struct tcapOmAE15minIntTable_data *StorageOld)
{
	(void) StorageTmp;
	(void) StorageOld;
	/* XXX: provide code to update the row with the underlying device */
	tcapOmAE15minIntTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_tcapOmAE15minIntTable_row(struct tcapOmAE15minIntTable_data *StorageTmp, struct tcapOmAE15minIntTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_tcapOmAE15minIntTable_row(struct tcapOmAE15minIntTable_data *StorageTmp, struct tcapOmAE15minIntTable_data *StorageOld)
{
	(void) StorageTmp;
	/* XXX: provide code to revert the row with the underlying device */
	update_tcapOmAE15minIntTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_tcapOmAE15minIntTable_row(struct tcapOmAE15minIntTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the tcapOmAE15minIntTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct tcapOmAE15minIntTable_data *
refresh_tcapOmAE15minIntTable_row(struct tcapOmAE15minIntTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->tcapOmAE15minIntTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->tcapOmAE15minIntTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_tcapOmAE15minIntTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the tcapOmAE15minIntTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_tcapOmAE15minIntTable(int force)
{
	if (!force && tcapOmAE15minIntTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	tcapOmAE15minIntTable_refresh = 0;
}

/**
 * @fn u_char *var_tcapOmAE15minIntTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in tcapOmAE15minIntTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_tcapOmMIB above.
 */
u_char *
var_tcapOmAE15minIntTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct tcapOmAE15minIntTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("tcapOmMIB", "var_tcapOmAE15minIntTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_tcapOmAE15minIntTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(tcapOmAE15minIntTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_tcapOmAE15minIntTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) TCAPOMAE15MININTMEASUREMENTDELETED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmAE15minIntMeasurementDeleted);
		rval = (u_char *) &StorageTmp->tcapOmAE15minIntMeasurementDeleted;
		break;
	case (u_char) TCAPOMAE15MININTNEWTRANSACTIONS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmAE15minIntNewTransactions);
		rval = (u_char *) &StorageTmp->tcapOmAE15minIntNewTransactions;
		break;
	case (u_char) TCAPOMAE15MININTMEANOPENTRANSACTIONS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmAE15minIntMeanOpenTransactions);
		rval = (u_char *) &StorageTmp->tcapOmAE15minIntMeanOpenTransactions;
		break;
	case (u_char) TCAPOMAE15MININTNOTUSED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmAE15minIntNotUsed);
		rval = (u_char *) &StorageTmp->tcapOmAE15minIntNotUsed;
		break;
	case (u_char) TCAPOMAE15MININTCUMMULATIVEMEANDURATION:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmAE15minIntCummulativeMeanDuration);
		rval = (u_char *) &StorageTmp->tcapOmAE15minIntCummulativeMeanDuration;
		break;
	case (u_char) TCAPOMAE15MININTMAXIMUMOPENTRANACTIONS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmAE15minIntMaximumOpenTranactions);
		rval = (u_char *) &StorageTmp->tcapOmAE15minIntMaximumOpenTranactions;
		break;
	case (u_char) TCAPOMAE15MININTTHRESHOLDEXCEEDED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmAE15minIntThresholdExceeded);
		rval = (u_char *) &StorageTmp->tcapOmAE15minIntThresholdExceeded;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_tcapOmTcTable_row(struct tcapOmTcTable_data *StorageTmp, struct tcapOmTcTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_tcapOmTcTable_row(struct tcapOmTcTable_data *StorageTmp, struct tcapOmTcTable_data *StorageOld)
{
	(void) StorageTmp;
	(void) StorageOld;
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_tcapOmTcTable_row(struct tcapOmTcTable_data *StorageTmp, struct tcapOmTcTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_tcapOmTcTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_tcapOmTcTable_row(struct tcapOmTcTable_data *StorageTmp, struct tcapOmTcTable_data *StorageOld)
{
	(void) StorageTmp;
	(void) StorageOld;
	/* XXX: provide code to update the row with the underlying device */
	tcapOmTcTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_tcapOmTcTable_row(struct tcapOmTcTable_data *StorageTmp, struct tcapOmTcTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_tcapOmTcTable_row(struct tcapOmTcTable_data *StorageTmp, struct tcapOmTcTable_data *StorageOld)
{
	(void) StorageTmp;
	/* XXX: provide code to revert the row with the underlying device */
	update_tcapOmTcTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_tcapOmTcTable_row(struct tcapOmTcTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the tcapOmTcTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct tcapOmTcTable_data *
refresh_tcapOmTcTable_row(struct tcapOmTcTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->tcapOmTcTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->tcapOmTcTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_tcapOmTcTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the tcapOmTcTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_tcapOmTcTable(int force)
{
	if (!force && tcapOmTcTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	tcapOmTcTable_refresh = 0;
}

/**
 * @fn u_char *var_tcapOmTcTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in tcapOmTcTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_tcapOmMIB above.
 */
u_char *
var_tcapOmTcTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct tcapOmTcTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("tcapOmMIB", "var_tcapOmTcTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_tcapOmTcTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(tcapOmTcTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_tcapOmTcTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) TCAPOMTCPABORTRECVUNRECTID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmTcPabortRecvUnrecTid);
		rval = (u_char *) &StorageTmp->tcapOmTcPabortRecvUnrecTid;
		break;
	case (u_char) TCAPOMTCPABORTRECVRESOURCELIM:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmTcPabortRecvResourceLim);
		rval = (u_char *) &StorageTmp->tcapOmTcPabortRecvResourceLim;
		break;
	case (u_char) TCAPOMTCREJECTRECVRESOURCELIM:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmTcRejectRecvResourceLim);
		rval = (u_char *) &StorageTmp->tcapOmTcRejectRecvResourceLim;
		break;
	case (u_char) TCAPOMTCPABORTSENTUNRECTID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmTcPabortSentUnrecTid);
		rval = (u_char *) &StorageTmp->tcapOmTcPabortSentUnrecTid;
		break;
	case (u_char) TCAPOMTCPABORTSENTRESOURCELIM:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmTcPabortSentResourceLim);
		rval = (u_char *) &StorageTmp->tcapOmTcPabortSentResourceLim;
		break;
	case (u_char) TCAPOMTCREJECTSENTRESOURCELIM:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmTcRejectSentResourceLim);
		rval = (u_char *) &StorageTmp->tcapOmTcRejectSentResourceLim;
		break;
	case (u_char) TCAPOMTCTCUSERCANCEL:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmTcTcUserCancel);
		rval = (u_char *) &StorageTmp->tcapOmTcTcUserCancel;
		break;
	case (u_char) TCAPOMTCMESSAGESDISCARDED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmTcMessagesDiscarded);
		rval = (u_char *) &StorageTmp->tcapOmTcMessagesDiscarded;
		break;
	case (u_char) TCAPOMTCNOTUSED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmTcNotUsed);
		rval = (u_char *) &StorageTmp->tcapOmTcNotUsed;
		break;
	case (u_char) TCAPOMTCPABORTSRECEIVED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmTcPabortsReceived);
		rval = (u_char *) &StorageTmp->tcapOmTcPabortsReceived;
		break;
	case (u_char) TCAPOMTCTCUSERREJECTSRECEIVED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmTcTcUserRejectsReceived);
		rval = (u_char *) &StorageTmp->tcapOmTcTcUserRejectsReceived;
		break;
	case (u_char) TCAPOMTCTPERRORS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmTcTpErrors);
		rval = (u_char *) &StorageTmp->tcapOmTcTpErrors;
		break;
	case (u_char) TCAPOMTC5MINVALIDINTERVALS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmTc5minValidIntervals);
		rval = (u_char *) &StorageTmp->tcapOmTc5minValidIntervals;
		break;
	case (u_char) TCAPOMTC15MINVALIDINTERVALS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmTc15minValidIntervals);
		rval = (u_char *) &StorageTmp->tcapOmTc15minValidIntervals;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_tcapOmTcCurrentTable_row(struct tcapOmTcCurrentTable_data *StorageTmp, struct tcapOmTcCurrentTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_tcapOmTcCurrentTable_row(struct tcapOmTcCurrentTable_data *StorageTmp, struct tcapOmTcCurrentTable_data *StorageOld)
{
	(void) StorageTmp;
	(void) StorageOld;
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_tcapOmTcCurrentTable_row(struct tcapOmTcCurrentTable_data *StorageTmp, struct tcapOmTcCurrentTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_tcapOmTcCurrentTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_tcapOmTcCurrentTable_row(struct tcapOmTcCurrentTable_data *StorageTmp, struct tcapOmTcCurrentTable_data *StorageOld)
{
	(void) StorageTmp;
	(void) StorageOld;
	/* XXX: provide code to update the row with the underlying device */
	tcapOmTcCurrentTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_tcapOmTcCurrentTable_row(struct tcapOmTcCurrentTable_data *StorageTmp, struct tcapOmTcCurrentTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_tcapOmTcCurrentTable_row(struct tcapOmTcCurrentTable_data *StorageTmp, struct tcapOmTcCurrentTable_data *StorageOld)
{
	(void) StorageTmp;
	/* XXX: provide code to revert the row with the underlying device */
	update_tcapOmTcCurrentTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_tcapOmTcCurrentTable_row(struct tcapOmTcCurrentTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the tcapOmTcCurrentTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct tcapOmTcCurrentTable_data *
refresh_tcapOmTcCurrentTable_row(struct tcapOmTcCurrentTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->tcapOmTcCurrentTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->tcapOmTcCurrentTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_tcapOmTcCurrentTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the tcapOmTcCurrentTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_tcapOmTcCurrentTable(int force)
{
	if (!force && tcapOmTcCurrentTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	tcapOmTcCurrentTable_refresh = 0;
}

/**
 * @fn u_char *var_tcapOmTcCurrentTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in tcapOmTcCurrentTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_tcapOmMIB above.
 */
u_char *
var_tcapOmTcCurrentTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct tcapOmTcCurrentTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("tcapOmMIB", "var_tcapOmTcCurrentTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_tcapOmTcCurrentTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(tcapOmTcCurrentTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_tcapOmTcCurrentTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) TCAPOMTCCURRENTPABORTRECVUNRECTID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmTcCurrentPabortRecvUnrecTid);
		rval = (u_char *) &StorageTmp->tcapOmTcCurrentPabortRecvUnrecTid;
		break;
	case (u_char) TCAPOMTCCURRENTPABORTRECVRESOURCELIM:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmTcCurrentPabortRecvResourceLim);
		rval = (u_char *) &StorageTmp->tcapOmTcCurrentPabortRecvResourceLim;
		break;
	case (u_char) TCAPOMTCCURRENTREJECTRECVRESOURCELIM:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmTcCurrentRejectRecvResourceLim);
		rval = (u_char *) &StorageTmp->tcapOmTcCurrentRejectRecvResourceLim;
		break;
	case (u_char) TCAPOMTCCURRENTPABORTSENTUNRECTID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmTcCurrentPabortSentUnrecTid);
		rval = (u_char *) &StorageTmp->tcapOmTcCurrentPabortSentUnrecTid;
		break;
	case (u_char) TCAPOMTCCURRENTPABORTSENTRESOURCELIM:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmTcCurrentPabortSentResourceLim);
		rval = (u_char *) &StorageTmp->tcapOmTcCurrentPabortSentResourceLim;
		break;
	case (u_char) TCAPOMTCCURRENTREJECTSENTRESOURCELIM:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmTcCurrentRejectSentResourceLim);
		rval = (u_char *) &StorageTmp->tcapOmTcCurrentRejectSentResourceLim;
		break;
	case (u_char) TCAPOMTCCURRENTTCUSERCANCEL:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmTcCurrentTcUserCancel);
		rval = (u_char *) &StorageTmp->tcapOmTcCurrentTcUserCancel;
		break;
	case (u_char) TCAPOMTCCURRENTMESSAGESDISCARDED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmTcCurrentMessagesDiscarded);
		rval = (u_char *) &StorageTmp->tcapOmTcCurrentMessagesDiscarded;
		break;
	case (u_char) TCAPOMTCCURRENTNOTUSED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmTcCurrentNotUsed);
		rval = (u_char *) &StorageTmp->tcapOmTcCurrentNotUsed;
		break;
	case (u_char) TCAPOMTCCURRENTPABORTSRECEIVED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmTcCurrentPabortsReceived);
		rval = (u_char *) &StorageTmp->tcapOmTcCurrentPabortsReceived;
		break;
	case (u_char) TCAPOMTCCURRENTTCUSERREJECTSRECEIVED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmTcCurrentTcUserRejectsReceived);
		rval = (u_char *) &StorageTmp->tcapOmTcCurrentTcUserRejectsReceived;
		break;
	case (u_char) TCAPOMTCCURRENTTPERRORS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmTcCurrentTpErrors);
		rval = (u_char *) &StorageTmp->tcapOmTcCurrentTpErrors;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_tcapOmTc5minIntTable_row(struct tcapOmTc5minIntTable_data *StorageTmp, struct tcapOmTc5minIntTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_tcapOmTc5minIntTable_row(struct tcapOmTc5minIntTable_data *StorageTmp, struct tcapOmTc5minIntTable_data *StorageOld)
{
	(void) StorageTmp;
	(void) StorageOld;
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_tcapOmTc5minIntTable_row(struct tcapOmTc5minIntTable_data *StorageTmp, struct tcapOmTc5minIntTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_tcapOmTc5minIntTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_tcapOmTc5minIntTable_row(struct tcapOmTc5minIntTable_data *StorageTmp, struct tcapOmTc5minIntTable_data *StorageOld)
{
	(void) StorageTmp;
	(void) StorageOld;
	/* XXX: provide code to update the row with the underlying device */
	tcapOmTc5minIntTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_tcapOmTc5minIntTable_row(struct tcapOmTc5minIntTable_data *StorageTmp, struct tcapOmTc5minIntTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_tcapOmTc5minIntTable_row(struct tcapOmTc5minIntTable_data *StorageTmp, struct tcapOmTc5minIntTable_data *StorageOld)
{
	(void) StorageTmp;
	/* XXX: provide code to revert the row with the underlying device */
	update_tcapOmTc5minIntTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_tcapOmTc5minIntTable_row(struct tcapOmTc5minIntTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the tcapOmTc5minIntTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct tcapOmTc5minIntTable_data *
refresh_tcapOmTc5minIntTable_row(struct tcapOmTc5minIntTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->tcapOmTc5minIntTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->tcapOmTc5minIntTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_tcapOmTc5minIntTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the tcapOmTc5minIntTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_tcapOmTc5minIntTable(int force)
{
	if (!force && tcapOmTc5minIntTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	tcapOmTc5minIntTable_refresh = 0;
}

/**
 * @fn u_char *var_tcapOmTc5minIntTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in tcapOmTc5minIntTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_tcapOmMIB above.
 */
u_char *
var_tcapOmTc5minIntTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct tcapOmTc5minIntTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("tcapOmMIB", "var_tcapOmTc5minIntTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_tcapOmTc5minIntTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(tcapOmTc5minIntTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_tcapOmTc5minIntTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) TCAPOMTC5MININTPABORTRECVUNRECTID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmTc5minIntPabortRecvUnrecTid);
		rval = (u_char *) &StorageTmp->tcapOmTc5minIntPabortRecvUnrecTid;
		break;
	case (u_char) TCAPOMTC5MININTPABORTRECVRESOURCELIM:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmTc5minIntPabortRecvResourceLim);
		rval = (u_char *) &StorageTmp->tcapOmTc5minIntPabortRecvResourceLim;
		break;
	case (u_char) TCAPOMTC5MININTREJECTRECVRESOURCELIM:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmTc5minIntRejectRecvResourceLim);
		rval = (u_char *) &StorageTmp->tcapOmTc5minIntRejectRecvResourceLim;
		break;
	case (u_char) TCAPOMTC5MININTPABORTSENTUNRECTID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmTc5minIntPabortSentUnrecTid);
		rval = (u_char *) &StorageTmp->tcapOmTc5minIntPabortSentUnrecTid;
		break;
	case (u_char) TCAPOMTC5MININTPABORTSENTRESOURCELIM:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmTc5minIntPabortSentResourceLim);
		rval = (u_char *) &StorageTmp->tcapOmTc5minIntPabortSentResourceLim;
		break;
	case (u_char) TCAPOMTC5MININTREJECTSENTRESOURCELIM:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmTc5minIntRejectSentResourceLim);
		rval = (u_char *) &StorageTmp->tcapOmTc5minIntRejectSentResourceLim;
		break;
	case (u_char) TCAPOMTC5MININTTCUSERCANCEL:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmTc5minIntTcUserCancel);
		rval = (u_char *) &StorageTmp->tcapOmTc5minIntTcUserCancel;
		break;
	case (u_char) TCAPOMTC5MININTMESSAGESDISCARDED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmTc5minIntMessagesDiscarded);
		rval = (u_char *) &StorageTmp->tcapOmTc5minIntMessagesDiscarded;
		break;
	case (u_char) TCAPOMTC5MININTNOTUSED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmTc5minIntNotUsed);
		rval = (u_char *) &StorageTmp->tcapOmTc5minIntNotUsed;
		break;
	case (u_char) TCAPOMTC5MININTPABORTSRECEIVED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmTc5minIntPabortsReceived);
		rval = (u_char *) &StorageTmp->tcapOmTc5minIntPabortsReceived;
		break;
	case (u_char) TCAPOMTC5MININTTCUSERREJECTSRECEIVED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmTc5minIntTcUserRejectsReceived);
		rval = (u_char *) &StorageTmp->tcapOmTc5minIntTcUserRejectsReceived;
		break;
	case (u_char) TCAPOMTC5MININTTPERRORS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmTc5minIntTpErrors);
		rval = (u_char *) &StorageTmp->tcapOmTc5minIntTpErrors;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_tcapOmTc15minIntTable_row(struct tcapOmTc15minIntTable_data *StorageTmp, struct tcapOmTc15minIntTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_tcapOmTc15minIntTable_row(struct tcapOmTc15minIntTable_data *StorageTmp, struct tcapOmTc15minIntTable_data *StorageOld)
{
	(void) StorageTmp;
	(void) StorageOld;
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_tcapOmTc15minIntTable_row(struct tcapOmTc15minIntTable_data *StorageTmp, struct tcapOmTc15minIntTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_tcapOmTc15minIntTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_tcapOmTc15minIntTable_row(struct tcapOmTc15minIntTable_data *StorageTmp, struct tcapOmTc15minIntTable_data *StorageOld)
{
	(void) StorageTmp;
	(void) StorageOld;
	/* XXX: provide code to update the row with the underlying device */
	tcapOmTc15minIntTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_tcapOmTc15minIntTable_row(struct tcapOmTc15minIntTable_data *StorageTmp, struct tcapOmTc15minIntTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_tcapOmTc15minIntTable_row(struct tcapOmTc15minIntTable_data *StorageTmp, struct tcapOmTc15minIntTable_data *StorageOld)
{
	(void) StorageTmp;
	/* XXX: provide code to revert the row with the underlying device */
	update_tcapOmTc15minIntTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_tcapOmTc15minIntTable_row(struct tcapOmTc15minIntTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the tcapOmTc15minIntTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct tcapOmTc15minIntTable_data *
refresh_tcapOmTc15minIntTable_row(struct tcapOmTc15minIntTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->tcapOmTc15minIntTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->tcapOmTc15minIntTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_tcapOmTc15minIntTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the tcapOmTc15minIntTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_tcapOmTc15minIntTable(int force)
{
	if (!force && tcapOmTc15minIntTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	tcapOmTc15minIntTable_refresh = 0;
}

/**
 * @fn u_char *var_tcapOmTc15minIntTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in tcapOmTc15minIntTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_tcapOmMIB above.
 */
u_char *
var_tcapOmTc15minIntTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct tcapOmTc15minIntTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("tcapOmMIB", "var_tcapOmTc15minIntTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_tcapOmTc15minIntTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(tcapOmTc15minIntTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_tcapOmTc15minIntTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) TCAPOMTC15MININTPABORTRECVUNRECTID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmTc15minIntPabortRecvUnrecTid);
		rval = (u_char *) &StorageTmp->tcapOmTc15minIntPabortRecvUnrecTid;
		break;
	case (u_char) TCAPOMTC15MININTPABORTRECVRESOURCELIM:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmTc15minIntPabortRecvResourceLim);
		rval = (u_char *) &StorageTmp->tcapOmTc15minIntPabortRecvResourceLim;
		break;
	case (u_char) TCAPOMTC15MININTREJECTRECVRESOURCELIM:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmTc15minIntRejectRecvResourceLim);
		rval = (u_char *) &StorageTmp->tcapOmTc15minIntRejectRecvResourceLim;
		break;
	case (u_char) TCAPOMTC15MININTPABORTSENTUNRECTID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmTc15minIntPabortSentUnrecTid);
		rval = (u_char *) &StorageTmp->tcapOmTc15minIntPabortSentUnrecTid;
		break;
	case (u_char) TCAPOMTC15MININTPABORTSENTRESOURCELIM:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmTc15minIntPabortSentResourceLim);
		rval = (u_char *) &StorageTmp->tcapOmTc15minIntPabortSentResourceLim;
		break;
	case (u_char) TCAPOMTC15MININTREJECTSENTRESOURCELIM:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmTc15minIntRejectSentResourceLim);
		rval = (u_char *) &StorageTmp->tcapOmTc15minIntRejectSentResourceLim;
		break;
	case (u_char) TCAPOMTC15MININTTCUSERCANCEL:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmTc15minIntTcUserCancel);
		rval = (u_char *) &StorageTmp->tcapOmTc15minIntTcUserCancel;
		break;
	case (u_char) TCAPOMTC15MININTMESSAGESDISCARDED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmTc15minIntMessagesDiscarded);
		rval = (u_char *) &StorageTmp->tcapOmTc15minIntMessagesDiscarded;
		break;
	case (u_char) TCAPOMTC15MININTNOTUSED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmTc15minIntNotUsed);
		rval = (u_char *) &StorageTmp->tcapOmTc15minIntNotUsed;
		break;
	case (u_char) TCAPOMTC15MININTPABORTSRECEIVED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmTc15minIntPabortsReceived);
		rval = (u_char *) &StorageTmp->tcapOmTc15minIntPabortsReceived;
		break;
	case (u_char) TCAPOMTC15MININTTCUSERREJECTSRECEIVED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmTc15minIntTcUserRejectsReceived);
		rval = (u_char *) &StorageTmp->tcapOmTc15minIntTcUserRejectsReceived;
		break;
	case (u_char) TCAPOMTC15MININTTPERRORS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmTc15minIntTpErrors);
		rval = (u_char *) &StorageTmp->tcapOmTc15minIntTpErrors;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_tcapOmDevelRecvTable_row(struct tcapOmDevelRecvTable_data *StorageTmp, struct tcapOmDevelRecvTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_tcapOmDevelRecvTable_row(struct tcapOmDevelRecvTable_data *StorageTmp, struct tcapOmDevelRecvTable_data *StorageOld)
{
	(void) StorageTmp;
	(void) StorageOld;
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_tcapOmDevelRecvTable_row(struct tcapOmDevelRecvTable_data *StorageTmp, struct tcapOmDevelRecvTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_tcapOmDevelRecvTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_tcapOmDevelRecvTable_row(struct tcapOmDevelRecvTable_data *StorageTmp, struct tcapOmDevelRecvTable_data *StorageOld)
{
	(void) StorageTmp;
	(void) StorageOld;
	/* XXX: provide code to update the row with the underlying device */
	tcapOmDevelRecvTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_tcapOmDevelRecvTable_row(struct tcapOmDevelRecvTable_data *StorageTmp, struct tcapOmDevelRecvTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_tcapOmDevelRecvTable_row(struct tcapOmDevelRecvTable_data *StorageTmp, struct tcapOmDevelRecvTable_data *StorageOld)
{
	(void) StorageTmp;
	/* XXX: provide code to revert the row with the underlying device */
	update_tcapOmDevelRecvTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_tcapOmDevelRecvTable_row(struct tcapOmDevelRecvTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the tcapOmDevelRecvTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct tcapOmDevelRecvTable_data *
refresh_tcapOmDevelRecvTable_row(struct tcapOmDevelRecvTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->tcapOmDevelRecvTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->tcapOmDevelRecvTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_tcapOmDevelRecvTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the tcapOmDevelRecvTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_tcapOmDevelRecvTable(int force)
{
	if (!force && tcapOmDevelRecvTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	tcapOmDevelRecvTable_refresh = 0;
}

/**
 * @fn u_char *var_tcapOmDevelRecvTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in tcapOmDevelRecvTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_tcapOmMIB above.
 */
u_char *
var_tcapOmDevelRecvTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct tcapOmDevelRecvTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("tcapOmMIB", "var_tcapOmDevelRecvTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_tcapOmDevelRecvTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(tcapOmDevelRecvTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_tcapOmDevelRecvTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) TCAPOMDEVELRECVPABORTMESSAGETYPE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecvPabortMessageType);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecvPabortMessageType;
		break;
	case (u_char) TCAPOMDEVELRECVPABORTINCORRECTTP:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecvPabortIncorrectTp);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecvPabortIncorrectTp;
		break;
	case (u_char) TCAPOMDEVELRECVPABORTBADTP:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecvPabortBadTp);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecvPabortBadTp;
		break;
	case (u_char) TCAPOMDEVELRECVREJECTUNRECCOMP:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecvRejectUnrecComp);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecvRejectUnrecComp;
		break;
	case (u_char) TCAPOMDEVELRECVREJECTMISTYPECOMP:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecvRejectMistypeComp);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecvRejectMistypeComp;
		break;
	case (u_char) TCAPOMDEVELRECVREJECTBADCOMPSTRUCT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecvRejectBadCompStruct);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecvRejectBadCompStruct;
		break;
	case (u_char) TCAPOMDEVELRECVREJECTUNRECLINKID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecvRejectUnrecLinkId);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecvRejectUnrecLinkId;
		break;
	case (u_char) TCAPOMDEVELRECVREJECTUNRECIDRETRES:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecvRejectUnrecIdRetRes);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecvRejectUnrecIdRetRes;
		break;
	case (u_char) TCAPOMDEVELRECVREJECTUNEXPRETRES:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecvRejectUnexpRetRes);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecvRejectUnexpRetRes;
		break;
	case (u_char) TCAPOMDEVELRECVREJECTUNRECIDRETERR:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecvRejectUnrecIdRetErr);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecvRejectUnrecIdRetErr;
		break;
	case (u_char) TCAPOMDEVELRECVREJECTUNEXPRETERR:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecvRejectUnexpRetErr);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecvRejectUnexpRetErr;
		break;
	case (u_char) TCAPOMDEVELRECVUSRREJDUPINVOKEID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecvUsrRejDupInvokeId);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecvUsrRejDupInvokeId;
		break;
	case (u_char) TCAPOMDEVELRECVUSRREJUNRECOPER:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecvUsrRejUnrecOper);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecvUsrRejUnrecOper;
		break;
	case (u_char) TCAPOMDEVELRECVUSRREJPARMINVOKE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecvUsrRejParmInvoke);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecvUsrRejParmInvoke;
		break;
	case (u_char) TCAPOMDEVELRECVUSRREJRELEASING:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecvUsrRejReleasing);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecvUsrRejReleasing;
		break;
	case (u_char) TCAPOMDEVELRECVUSRREJUNEXPLINKRESP:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecvUsrRejUnexpLinkResp);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecvUsrRejUnexpLinkResp;
		break;
	case (u_char) TCAPOMDEVELRECVUSRREJUNEXPLINKOPER:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecvUsrRejUnexpLinkOper);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecvUsrRejUnexpLinkOper;
		break;
	case (u_char) TCAPOMDEVELRECVUSRREJUNRECERROR:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecvUsrRejUnrecError);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecvUsrRejUnrecError;
		break;
	case (u_char) TCAPOMDEVELRECVUSRREJUNEXPERROR:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecvUsrRejUnexpError);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecvUsrRejUnexpError;
		break;
	case (u_char) TCAPOMDEVELRECVUSRREJPARMRETRESULT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecvUsrRejParmRetResult);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecvUsrRejParmRetResult;
		break;
	case (u_char) TCAPOMDEVELRECVUSRREJPARMRETERROR:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecvUsrRejParmRetError);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecvUsrRejParmRetError;
		break;
	case (u_char) TCAPOMDEVELRECV5MINVALIDINTERVALS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecv5minValidIntervals);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecv5minValidIntervals;
		break;
	case (u_char) TCAPOMDEVELRECV15MINVALIDINTERVALS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecv15minValidIntervals);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecv15minValidIntervals;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_tcapOmDevelRecvCurrentTable_row(struct tcapOmDevelRecvCurrentTable_data *StorageTmp, struct tcapOmDevelRecvCurrentTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_tcapOmDevelRecvCurrentTable_row(struct tcapOmDevelRecvCurrentTable_data *StorageTmp, struct tcapOmDevelRecvCurrentTable_data *StorageOld)
{
	(void) StorageTmp;
	(void) StorageOld;
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_tcapOmDevelRecvCurrentTable_row(struct tcapOmDevelRecvCurrentTable_data *StorageTmp, struct tcapOmDevelRecvCurrentTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_tcapOmDevelRecvCurrentTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_tcapOmDevelRecvCurrentTable_row(struct tcapOmDevelRecvCurrentTable_data *StorageTmp, struct tcapOmDevelRecvCurrentTable_data *StorageOld)
{
	(void) StorageTmp;
	(void) StorageOld;
	/* XXX: provide code to update the row with the underlying device */
	tcapOmDevelRecvCurrentTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_tcapOmDevelRecvCurrentTable_row(struct tcapOmDevelRecvCurrentTable_data *StorageTmp, struct tcapOmDevelRecvCurrentTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_tcapOmDevelRecvCurrentTable_row(struct tcapOmDevelRecvCurrentTable_data *StorageTmp, struct tcapOmDevelRecvCurrentTable_data *StorageOld)
{
	(void) StorageTmp;
	/* XXX: provide code to revert the row with the underlying device */
	update_tcapOmDevelRecvCurrentTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_tcapOmDevelRecvCurrentTable_row(struct tcapOmDevelRecvCurrentTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the tcapOmDevelRecvCurrentTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct tcapOmDevelRecvCurrentTable_data *
refresh_tcapOmDevelRecvCurrentTable_row(struct tcapOmDevelRecvCurrentTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->tcapOmDevelRecvCurrentTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->tcapOmDevelRecvCurrentTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_tcapOmDevelRecvCurrentTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the tcapOmDevelRecvCurrentTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_tcapOmDevelRecvCurrentTable(int force)
{
	if (!force && tcapOmDevelRecvCurrentTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	tcapOmDevelRecvCurrentTable_refresh = 0;
}

/**
 * @fn u_char *var_tcapOmDevelRecvCurrentTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in tcapOmDevelRecvCurrentTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_tcapOmMIB above.
 */
u_char *
var_tcapOmDevelRecvCurrentTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct tcapOmDevelRecvCurrentTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("tcapOmMIB", "var_tcapOmDevelRecvCurrentTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_tcapOmDevelRecvCurrentTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(tcapOmDevelRecvCurrentTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_tcapOmDevelRecvCurrentTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) TCAPOMDEVELRECVCURRENTPABORTMESSAGETYPE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecvCurrentPabortMessageType);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecvCurrentPabortMessageType;
		break;
	case (u_char) TCAPOMDEVELRECVCURRENTPABORTINCORRECTTP:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecvCurrentPabortIncorrectTp);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecvCurrentPabortIncorrectTp;
		break;
	case (u_char) TCAPOMDEVELRECVCURRENTPABORTBADTP:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecvCurrentPabortBadTp);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecvCurrentPabortBadTp;
		break;
	case (u_char) TCAPOMDEVELRECVCURRENTREJECTUNRECCOMP:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecvCurrentRejectUnrecComp);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecvCurrentRejectUnrecComp;
		break;
	case (u_char) TCAPOMDEVELRECVCURRENTREJECTMISTYPECOMP:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecvCurrentRejectMistypeComp);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecvCurrentRejectMistypeComp;
		break;
	case (u_char) TCAPOMDEVELRECVCURRENTREJECTBADCOMPSTRUCT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecvCurrentRejectBadCompStruct);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecvCurrentRejectBadCompStruct;
		break;
	case (u_char) TCAPOMDEVELRECVCURRENTREJECTUNRECLINKID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecvCurrentRejectUnrecLinkId);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecvCurrentRejectUnrecLinkId;
		break;
	case (u_char) TCAPOMDEVELRECVCURRENTREJECTUNRECIDRETRES:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecvCurrentRejectUnrecIdRetRes);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecvCurrentRejectUnrecIdRetRes;
		break;
	case (u_char) TCAPOMDEVELRECVCURRENTREJECTUNEXPRETRES:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecvCurrentRejectUnexpRetRes);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecvCurrentRejectUnexpRetRes;
		break;
	case (u_char) TCAPOMDEVELRECVCURRENTREJECTUNRECIDRETERR:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecvCurrentRejectUnrecIdRetErr);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecvCurrentRejectUnrecIdRetErr;
		break;
	case (u_char) TCAPOMDEVELRECVCURRENTREJECTUNEXPRETERR:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecvCurrentRejectUnexpRetErr);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecvCurrentRejectUnexpRetErr;
		break;
	case (u_char) TCAPOMDEVELRECVCURRENTUSRREJDUPINVOKEID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecvCurrentUsrRejDupInvokeId);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecvCurrentUsrRejDupInvokeId;
		break;
	case (u_char) TCAPOMDEVELRECVCURRENTUSRREJUNRECOPER:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecvCurrentUsrRejUnrecOper);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecvCurrentUsrRejUnrecOper;
		break;
	case (u_char) TCAPOMDEVELRECVCURRENTUSRREJPARMINVOKE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecvCurrentUsrRejParmInvoke);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecvCurrentUsrRejParmInvoke;
		break;
	case (u_char) TCAPOMDEVELRECVCURRENTUSRREJRELEASING:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecvCurrentUsrRejReleasing);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecvCurrentUsrRejReleasing;
		break;
	case (u_char) TCAPOMDEVELRECVCURRENTUSRREJUNEXPLINKRESP:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecvCurrentUsrRejUnexpLinkResp);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecvCurrentUsrRejUnexpLinkResp;
		break;
	case (u_char) TCAPOMDEVELRECVCURRENTUSRREJUNEXPLINKOPER:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecvCurrentUsrRejUnexpLinkOper);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecvCurrentUsrRejUnexpLinkOper;
		break;
	case (u_char) TCAPOMDEVELRECVCURRENTUSRREJUNRECERROR:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecvCurrentUsrRejUnrecError);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecvCurrentUsrRejUnrecError;
		break;
	case (u_char) TCAPOMDEVELRECVCURRENTUSRREJUNEXPERROR:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecvCurrentUsrRejUnexpError);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecvCurrentUsrRejUnexpError;
		break;
	case (u_char) TCAPOMDEVELRECVCURRENTUSRREJPARMRETRESULT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecvCurrentUsrRejParmRetResult);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecvCurrentUsrRejParmRetResult;
		break;
	case (u_char) TCAPOMDEVELRECVCURRENTUSRREJPARMRETERROR:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecvCurrentUsrRejParmRetError);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecvCurrentUsrRejParmRetError;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_tcapOmDevelRecv5minIntTable_row(struct tcapOmDevelRecv5minIntTable_data *StorageTmp, struct tcapOmDevelRecv5minIntTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_tcapOmDevelRecv5minIntTable_row(struct tcapOmDevelRecv5minIntTable_data *StorageTmp, struct tcapOmDevelRecv5minIntTable_data *StorageOld)
{
	(void) StorageTmp;
	(void) StorageOld;
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_tcapOmDevelRecv5minIntTable_row(struct tcapOmDevelRecv5minIntTable_data *StorageTmp, struct tcapOmDevelRecv5minIntTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_tcapOmDevelRecv5minIntTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_tcapOmDevelRecv5minIntTable_row(struct tcapOmDevelRecv5minIntTable_data *StorageTmp, struct tcapOmDevelRecv5minIntTable_data *StorageOld)
{
	(void) StorageTmp;
	(void) StorageOld;
	/* XXX: provide code to update the row with the underlying device */
	tcapOmDevelRecv5minIntTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_tcapOmDevelRecv5minIntTable_row(struct tcapOmDevelRecv5minIntTable_data *StorageTmp, struct tcapOmDevelRecv5minIntTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_tcapOmDevelRecv5minIntTable_row(struct tcapOmDevelRecv5minIntTable_data *StorageTmp, struct tcapOmDevelRecv5minIntTable_data *StorageOld)
{
	(void) StorageTmp;
	/* XXX: provide code to revert the row with the underlying device */
	update_tcapOmDevelRecv5minIntTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_tcapOmDevelRecv5minIntTable_row(struct tcapOmDevelRecv5minIntTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the tcapOmDevelRecv5minIntTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct tcapOmDevelRecv5minIntTable_data *
refresh_tcapOmDevelRecv5minIntTable_row(struct tcapOmDevelRecv5minIntTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->tcapOmDevelRecv5minIntTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->tcapOmDevelRecv5minIntTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_tcapOmDevelRecv5minIntTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the tcapOmDevelRecv5minIntTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_tcapOmDevelRecv5minIntTable(int force)
{
	if (!force && tcapOmDevelRecv5minIntTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	tcapOmDevelRecv5minIntTable_refresh = 0;
}

/**
 * @fn u_char *var_tcapOmDevelRecv5minIntTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in tcapOmDevelRecv5minIntTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_tcapOmMIB above.
 */
u_char *
var_tcapOmDevelRecv5minIntTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct tcapOmDevelRecv5minIntTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("tcapOmMIB", "var_tcapOmDevelRecv5minIntTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_tcapOmDevelRecv5minIntTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(tcapOmDevelRecv5minIntTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_tcapOmDevelRecv5minIntTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) TCAPOMDEVELRECV5MININTPABORTMESSAGETYPE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecv5minIntPabortMessageType);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecv5minIntPabortMessageType;
		break;
	case (u_char) TCAPOMDEVELRECV5MININTPABORTINCORRECTTP:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecv5minIntPabortIncorrectTp);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecv5minIntPabortIncorrectTp;
		break;
	case (u_char) TCAPOMDEVELRECV5MININTPABORTBADTP:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecv5minIntPabortBadTp);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecv5minIntPabortBadTp;
		break;
	case (u_char) TCAPOMDEVELRECV5MININTREJECTUNRECCOMP:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecv5minIntRejectUnrecComp);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecv5minIntRejectUnrecComp;
		break;
	case (u_char) TCAPOMDEVELRECV5MININTREJECTMISTYPECOMP:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecv5minIntRejectMistypeComp);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecv5minIntRejectMistypeComp;
		break;
	case (u_char) TCAPOMDEVELRECV5MININTREJECTBADCOMPSTRUCT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecv5minIntRejectBadCompStruct);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecv5minIntRejectBadCompStruct;
		break;
	case (u_char) TCAPOMDEVELRECV5MININTREJECTUNRECLINKID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecv5minIntRejectUnrecLinkId);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecv5minIntRejectUnrecLinkId;
		break;
	case (u_char) TCAPOMDEVELRECV5MININTREJECTUNRECIDRETRES:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecv5minIntRejectUnrecIdRetRes);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecv5minIntRejectUnrecIdRetRes;
		break;
	case (u_char) TCAPOMDEVELRECV5MININTREJECTUNEXPRETRES:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecv5minIntRejectUnexpRetRes);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecv5minIntRejectUnexpRetRes;
		break;
	case (u_char) TCAPOMDEVELRECV5MININTREJECTUNRECIDRETERR:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecv5minIntRejectUnrecIdRetErr);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecv5minIntRejectUnrecIdRetErr;
		break;
	case (u_char) TCAPOMDEVELRECV5MININTREJECTUNEXPRETERR:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecv5minIntRejectUnexpRetErr);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecv5minIntRejectUnexpRetErr;
		break;
	case (u_char) TCAPOMDEVELRECV5MININTUSRREJDUPINVOKEID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecv5minIntUsrRejDupInvokeId);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecv5minIntUsrRejDupInvokeId;
		break;
	case (u_char) TCAPOMDEVELRECV5MININTUSRREJUNRECOPER:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecv5minIntUsrRejUnrecOper);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecv5minIntUsrRejUnrecOper;
		break;
	case (u_char) TCAPOMDEVELRECV5MININTUSRREJPARMINVOKE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecv5minIntUsrRejParmInvoke);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecv5minIntUsrRejParmInvoke;
		break;
	case (u_char) TCAPOMDEVELRECV5MININTUSRREJRELEASING:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecv5minIntUsrRejReleasing);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecv5minIntUsrRejReleasing;
		break;
	case (u_char) TCAPOMDEVELRECV5MININTUSRREJUNEXPLINKRESP:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecv5minIntUsrRejUnexpLinkResp);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecv5minIntUsrRejUnexpLinkResp;
		break;
	case (u_char) TCAPOMDEVELRECV5MININTUSRREJUNEXPLINKOPER:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecv5minIntUsrRejUnexpLinkOper);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecv5minIntUsrRejUnexpLinkOper;
		break;
	case (u_char) TCAPOMDEVELRECV5MININTUSRREJUNRECERROR:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecv5minIntUsrRejUnrecError);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecv5minIntUsrRejUnrecError;
		break;
	case (u_char) TCAPOMDEVELRECV5MININTUSRREJUNEXPERROR:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecv5minIntUsrRejUnexpError);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecv5minIntUsrRejUnexpError;
		break;
	case (u_char) TCAPOMDEVELRECV5MININTUSRREJPARMRETRESULT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecv5minIntUsrRejParmRetResult);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecv5minIntUsrRejParmRetResult;
		break;
	case (u_char) TCAPOMDEVELRECV5MININTUSRREJPARMRETERROR:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecv5minIntUsrRejParmRetError);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecv5minIntUsrRejParmRetError;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_tcapOmDevelRecv15minIntTable_row(struct tcapOmDevelRecv15minIntTable_data *StorageTmp, struct tcapOmDevelRecv15minIntTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_tcapOmDevelRecv15minIntTable_row(struct tcapOmDevelRecv15minIntTable_data *StorageTmp, struct tcapOmDevelRecv15minIntTable_data *StorageOld)
{
	(void) StorageTmp;
	(void) StorageOld;
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_tcapOmDevelRecv15minIntTable_row(struct tcapOmDevelRecv15minIntTable_data *StorageTmp, struct tcapOmDevelRecv15minIntTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_tcapOmDevelRecv15minIntTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_tcapOmDevelRecv15minIntTable_row(struct tcapOmDevelRecv15minIntTable_data *StorageTmp, struct tcapOmDevelRecv15minIntTable_data *StorageOld)
{
	(void) StorageTmp;
	(void) StorageOld;
	/* XXX: provide code to update the row with the underlying device */
	tcapOmDevelRecv15minIntTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_tcapOmDevelRecv15minIntTable_row(struct tcapOmDevelRecv15minIntTable_data *StorageTmp, struct tcapOmDevelRecv15minIntTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_tcapOmDevelRecv15minIntTable_row(struct tcapOmDevelRecv15minIntTable_data *StorageTmp, struct tcapOmDevelRecv15minIntTable_data *StorageOld)
{
	(void) StorageTmp;
	/* XXX: provide code to revert the row with the underlying device */
	update_tcapOmDevelRecv15minIntTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_tcapOmDevelRecv15minIntTable_row(struct tcapOmDevelRecv15minIntTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the tcapOmDevelRecv15minIntTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct tcapOmDevelRecv15minIntTable_data *
refresh_tcapOmDevelRecv15minIntTable_row(struct tcapOmDevelRecv15minIntTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->tcapOmDevelRecv15minIntTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->tcapOmDevelRecv15minIntTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_tcapOmDevelRecv15minIntTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the tcapOmDevelRecv15minIntTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_tcapOmDevelRecv15minIntTable(int force)
{
	if (!force && tcapOmDevelRecv15minIntTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	tcapOmDevelRecv15minIntTable_refresh = 0;
}

/**
 * @fn u_char *var_tcapOmDevelRecv15minIntTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in tcapOmDevelRecv15minIntTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_tcapOmMIB above.
 */
u_char *
var_tcapOmDevelRecv15minIntTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct tcapOmDevelRecv15minIntTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("tcapOmMIB", "var_tcapOmDevelRecv15minIntTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_tcapOmDevelRecv15minIntTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(tcapOmDevelRecv15minIntTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_tcapOmDevelRecv15minIntTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) TCAPOMDEVELRECV15MININTPABORTMESSAGETYPE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecv15minIntPabortMessageType);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecv15minIntPabortMessageType;
		break;
	case (u_char) TCAPOMDEVELRECV15MININTPABORTINCORRECTTP:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecv15minIntPabortIncorrectTp);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecv15minIntPabortIncorrectTp;
		break;
	case (u_char) TCAPOMDEVELRECV15MININTPABORTBADTP:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecv15minIntPabortBadTp);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecv15minIntPabortBadTp;
		break;
	case (u_char) TCAPOMDEVELRECV15MININTREJECTUNRECCOMP:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecv15minIntRejectUnrecComp);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecv15minIntRejectUnrecComp;
		break;
	case (u_char) TCAPOMDEVELRECV15MININTREJECTMISTYPECOMP:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecv15minIntRejectMistypeComp);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecv15minIntRejectMistypeComp;
		break;
	case (u_char) TCAPOMDEVELRECV15MININTREJECTBADCOMPSTRUCT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecv15minIntRejectBadCompStruct);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecv15minIntRejectBadCompStruct;
		break;
	case (u_char) TCAPOMDEVELRECV15MININTREJECTUNRECLINKID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecv15minIntRejectUnrecLinkId);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecv15minIntRejectUnrecLinkId;
		break;
	case (u_char) TCAPOMDEVELRECV15MININTREJECTUNRECIDRETRES:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecv15minIntRejectUnrecIdRetRes);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecv15minIntRejectUnrecIdRetRes;
		break;
	case (u_char) TCAPOMDEVELRECV15MININTREJECTUNEXPRETRES:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecv15minIntRejectUnexpRetRes);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecv15minIntRejectUnexpRetRes;
		break;
	case (u_char) TCAPOMDEVELRECV15MININTREJECTUNRECIDRETERR:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecv15minIntRejectUnrecIdRetErr);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecv15minIntRejectUnrecIdRetErr;
		break;
	case (u_char) TCAPOMDEVELRECV15MININTREJECTUNEXPRETERR:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecv15minIntRejectUnexpRetErr);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecv15minIntRejectUnexpRetErr;
		break;
	case (u_char) TCAPOMDEVELRECV15MININTUSRREJDUPINVOKEID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecv15minIntUsrRejDupInvokeId);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecv15minIntUsrRejDupInvokeId;
		break;
	case (u_char) TCAPOMDEVELRECV15MININTUSRREJUNRECOPER:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecv15minIntUsrRejUnrecOper);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecv15minIntUsrRejUnrecOper;
		break;
	case (u_char) TCAPOMDEVELRECV15MININTUSRREJPARMINVOKE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecv15minIntUsrRejParmInvoke);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecv15minIntUsrRejParmInvoke;
		break;
	case (u_char) TCAPOMDEVELRECV15MININTUSRREJRELEASING:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecv15minIntUsrRejReleasing);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecv15minIntUsrRejReleasing;
		break;
	case (u_char) TCAPOMDEVELRECV15MININTUSRREJUNEXPLINKRESP:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecv15minIntUsrRejUnexpLinkResp);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecv15minIntUsrRejUnexpLinkResp;
		break;
	case (u_char) TCAPOMDEVELRECV15MININTUSRREJUNEXPLINKOPER:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecv15minIntUsrRejUnexpLinkOper);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecv15minIntUsrRejUnexpLinkOper;
		break;
	case (u_char) TCAPOMDEVELRECV15MININTUSRREJUNRECERROR:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecv15minIntUsrRejUnrecError);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecv15minIntUsrRejUnrecError;
		break;
	case (u_char) TCAPOMDEVELRECV15MININTUSRREJUNEXPERROR:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecv15minIntUsrRejUnexpError);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecv15minIntUsrRejUnexpError;
		break;
	case (u_char) TCAPOMDEVELRECV15MININTUSRREJPARMRETRESULT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecv15minIntUsrRejParmRetResult);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecv15minIntUsrRejParmRetResult;
		break;
	case (u_char) TCAPOMDEVELRECV15MININTUSRREJPARMRETERROR:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelRecv15minIntUsrRejParmRetError);
		rval = (u_char *) &StorageTmp->tcapOmDevelRecv15minIntUsrRejParmRetError;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_tcapOmDevelSentTable_row(struct tcapOmDevelSentTable_data *StorageTmp, struct tcapOmDevelSentTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_tcapOmDevelSentTable_row(struct tcapOmDevelSentTable_data *StorageTmp, struct tcapOmDevelSentTable_data *StorageOld)
{
	(void) StorageTmp;
	(void) StorageOld;
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_tcapOmDevelSentTable_row(struct tcapOmDevelSentTable_data *StorageTmp, struct tcapOmDevelSentTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_tcapOmDevelSentTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_tcapOmDevelSentTable_row(struct tcapOmDevelSentTable_data *StorageTmp, struct tcapOmDevelSentTable_data *StorageOld)
{
	(void) StorageTmp;
	(void) StorageOld;
	/* XXX: provide code to update the row with the underlying device */
	tcapOmDevelSentTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_tcapOmDevelSentTable_row(struct tcapOmDevelSentTable_data *StorageTmp, struct tcapOmDevelSentTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_tcapOmDevelSentTable_row(struct tcapOmDevelSentTable_data *StorageTmp, struct tcapOmDevelSentTable_data *StorageOld)
{
	(void) StorageTmp;
	/* XXX: provide code to revert the row with the underlying device */
	update_tcapOmDevelSentTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_tcapOmDevelSentTable_row(struct tcapOmDevelSentTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the tcapOmDevelSentTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct tcapOmDevelSentTable_data *
refresh_tcapOmDevelSentTable_row(struct tcapOmDevelSentTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->tcapOmDevelSentTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->tcapOmDevelSentTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_tcapOmDevelSentTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the tcapOmDevelSentTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_tcapOmDevelSentTable(int force)
{
	if (!force && tcapOmDevelSentTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	tcapOmDevelSentTable_refresh = 0;
}

/**
 * @fn u_char *var_tcapOmDevelSentTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in tcapOmDevelSentTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_tcapOmMIB above.
 */
u_char *
var_tcapOmDevelSentTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct tcapOmDevelSentTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("tcapOmMIB", "var_tcapOmDevelSentTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_tcapOmDevelSentTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(tcapOmDevelSentTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_tcapOmDevelSentTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) TCAPOMDEVELSENTPABORTMESSAGETYPE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSentPabortMessageType);
		rval = (u_char *) &StorageTmp->tcapOmDevelSentPabortMessageType;
		break;
	case (u_char) TCAPOMDEVELSENTPABORTINCORRECTTP:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSentPabortIncorrectTp);
		rval = (u_char *) &StorageTmp->tcapOmDevelSentPabortIncorrectTp;
		break;
	case (u_char) TCAPOMDEVELSENTPABORTBADTP:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSentPabortBadTp);
		rval = (u_char *) &StorageTmp->tcapOmDevelSentPabortBadTp;
		break;
	case (u_char) TCAPOMDEVELSENTREJECTUNRECCOMP:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSentRejectUnrecComp);
		rval = (u_char *) &StorageTmp->tcapOmDevelSentRejectUnrecComp;
		break;
	case (u_char) TCAPOMDEVELSENTREJECTMISTYPECOMP:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSentRejectMistypeComp);
		rval = (u_char *) &StorageTmp->tcapOmDevelSentRejectMistypeComp;
		break;
	case (u_char) TCAPOMDEVELSENTREJECTBADCOMPSTRUCT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSentRejectBadCompStruct);
		rval = (u_char *) &StorageTmp->tcapOmDevelSentRejectBadCompStruct;
		break;
	case (u_char) TCAPOMDEVELSENTREJECTUNRECLINKID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSentRejectUnrecLinkId);
		rval = (u_char *) &StorageTmp->tcapOmDevelSentRejectUnrecLinkId;
		break;
	case (u_char) TCAPOMDEVELSENTREJECTUNRECIDRETRES:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSentRejectUnrecIdRetRes);
		rval = (u_char *) &StorageTmp->tcapOmDevelSentRejectUnrecIdRetRes;
		break;
	case (u_char) TCAPOMDEVELSENTREJECTUNEXPRETRES:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSentRejectUnexpRetRes);
		rval = (u_char *) &StorageTmp->tcapOmDevelSentRejectUnexpRetRes;
		break;
	case (u_char) TCAPOMDEVELSENTREJECTUNRECIDRETERR:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSentRejectUnrecIdRetErr);
		rval = (u_char *) &StorageTmp->tcapOmDevelSentRejectUnrecIdRetErr;
		break;
	case (u_char) TCAPOMDEVELSENTREJECTUNEXPRETERR:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSentRejectUnexpRetErr);
		rval = (u_char *) &StorageTmp->tcapOmDevelSentRejectUnexpRetErr;
		break;
	case (u_char) TCAPOMDEVELSENTUSRREJDUPINVOKEID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSentUsrRejDupInvokeId);
		rval = (u_char *) &StorageTmp->tcapOmDevelSentUsrRejDupInvokeId;
		break;
	case (u_char) TCAPOMDEVELSENTUSRREJUNRECOPER:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSentUsrRejUnrecOper);
		rval = (u_char *) &StorageTmp->tcapOmDevelSentUsrRejUnrecOper;
		break;
	case (u_char) TCAPOMDEVELSENTUSRREJPARMINVOKE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSentUsrRejParmInvoke);
		rval = (u_char *) &StorageTmp->tcapOmDevelSentUsrRejParmInvoke;
		break;
	case (u_char) TCAPOMDEVELSENTUSRREJRELEASING:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSentUsrRejReleasing);
		rval = (u_char *) &StorageTmp->tcapOmDevelSentUsrRejReleasing;
		break;
	case (u_char) TCAPOMDEVELSENTUSRREJUNEXPLINKRESP:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSentUsrRejUnexpLinkResp);
		rval = (u_char *) &StorageTmp->tcapOmDevelSentUsrRejUnexpLinkResp;
		break;
	case (u_char) TCAPOMDEVELSENTUSRREJUNEXPLINKOPER:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSentUsrRejUnexpLinkOper);
		rval = (u_char *) &StorageTmp->tcapOmDevelSentUsrRejUnexpLinkOper;
		break;
	case (u_char) TCAPOMDEVELSENTUSRREJUNRECERROR:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSentUsrRejUnrecError);
		rval = (u_char *) &StorageTmp->tcapOmDevelSentUsrRejUnrecError;
		break;
	case (u_char) TCAPOMDEVELSENTUSRREJUNEXPERROR:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSentUsrRejUnexpError);
		rval = (u_char *) &StorageTmp->tcapOmDevelSentUsrRejUnexpError;
		break;
	case (u_char) TCAPOMDEVELSENTUSRREJPARMRETRESULT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSentUsrRejParmRetResult);
		rval = (u_char *) &StorageTmp->tcapOmDevelSentUsrRejParmRetResult;
		break;
	case (u_char) TCAPOMDEVELSENTUSRREJPARMRETERROR:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSentUsrRejParmRetError);
		rval = (u_char *) &StorageTmp->tcapOmDevelSentUsrRejParmRetError;
		break;
	case (u_char) TCAPOMDEVELSENT5MINVALIDINTERVALS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSent5minValidIntervals);
		rval = (u_char *) &StorageTmp->tcapOmDevelSent5minValidIntervals;
		break;
	case (u_char) TCAPOMDEVELSENT15MINVALIDINTERVALS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSent15minValidIntervals);
		rval = (u_char *) &StorageTmp->tcapOmDevelSent15minValidIntervals;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_tcapOmDevelSentCurrentTable_row(struct tcapOmDevelSentCurrentTable_data *StorageTmp, struct tcapOmDevelSentCurrentTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_tcapOmDevelSentCurrentTable_row(struct tcapOmDevelSentCurrentTable_data *StorageTmp, struct tcapOmDevelSentCurrentTable_data *StorageOld)
{
	(void) StorageTmp;
	(void) StorageOld;
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_tcapOmDevelSentCurrentTable_row(struct tcapOmDevelSentCurrentTable_data *StorageTmp, struct tcapOmDevelSentCurrentTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_tcapOmDevelSentCurrentTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_tcapOmDevelSentCurrentTable_row(struct tcapOmDevelSentCurrentTable_data *StorageTmp, struct tcapOmDevelSentCurrentTable_data *StorageOld)
{
	(void) StorageTmp;
	(void) StorageOld;
	/* XXX: provide code to update the row with the underlying device */
	tcapOmDevelSentCurrentTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_tcapOmDevelSentCurrentTable_row(struct tcapOmDevelSentCurrentTable_data *StorageTmp, struct tcapOmDevelSentCurrentTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_tcapOmDevelSentCurrentTable_row(struct tcapOmDevelSentCurrentTable_data *StorageTmp, struct tcapOmDevelSentCurrentTable_data *StorageOld)
{
	(void) StorageTmp;
	/* XXX: provide code to revert the row with the underlying device */
	update_tcapOmDevelSentCurrentTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_tcapOmDevelSentCurrentTable_row(struct tcapOmDevelSentCurrentTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the tcapOmDevelSentCurrentTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct tcapOmDevelSentCurrentTable_data *
refresh_tcapOmDevelSentCurrentTable_row(struct tcapOmDevelSentCurrentTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->tcapOmDevelSentCurrentTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->tcapOmDevelSentCurrentTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_tcapOmDevelSentCurrentTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the tcapOmDevelSentCurrentTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_tcapOmDevelSentCurrentTable(int force)
{
	if (!force && tcapOmDevelSentCurrentTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	tcapOmDevelSentCurrentTable_refresh = 0;
}

/**
 * @fn u_char *var_tcapOmDevelSentCurrentTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in tcapOmDevelSentCurrentTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_tcapOmMIB above.
 */
u_char *
var_tcapOmDevelSentCurrentTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct tcapOmDevelSentCurrentTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("tcapOmMIB", "var_tcapOmDevelSentCurrentTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_tcapOmDevelSentCurrentTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(tcapOmDevelSentCurrentTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_tcapOmDevelSentCurrentTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) TCAPOMDEVELSENTCURRENTPABORTMESSAGETYPE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSentCurrentPabortMessageType);
		rval = (u_char *) &StorageTmp->tcapOmDevelSentCurrentPabortMessageType;
		break;
	case (u_char) TCAPOMDEVELSENTCURRENTPABORTINCORRECTTP:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSentCurrentPabortIncorrectTp);
		rval = (u_char *) &StorageTmp->tcapOmDevelSentCurrentPabortIncorrectTp;
		break;
	case (u_char) TCAPOMDEVELSENTCURRENTPABORTBADTP:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSentCurrentPabortBadTp);
		rval = (u_char *) &StorageTmp->tcapOmDevelSentCurrentPabortBadTp;
		break;
	case (u_char) TCAPOMDEVELSENTCURRENTREJECTUNRECCOMP:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSentCurrentRejectUnrecComp);
		rval = (u_char *) &StorageTmp->tcapOmDevelSentCurrentRejectUnrecComp;
		break;
	case (u_char) TCAPOMDEVELSENTCURRENTREJECTMISTYPECOMP:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSentCurrentRejectMistypeComp);
		rval = (u_char *) &StorageTmp->tcapOmDevelSentCurrentRejectMistypeComp;
		break;
	case (u_char) TCAPOMDEVELSENTCURRENTREJECTBADCOMPSTRUCT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSentCurrentRejectBadCompStruct);
		rval = (u_char *) &StorageTmp->tcapOmDevelSentCurrentRejectBadCompStruct;
		break;
	case (u_char) TCAPOMDEVELSENTCURRENTREJECTUNRECLINKID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSentCurrentRejectUnrecLinkId);
		rval = (u_char *) &StorageTmp->tcapOmDevelSentCurrentRejectUnrecLinkId;
		break;
	case (u_char) TCAPOMDEVELSENTCURRENTREJECTUNRECIDRETRES:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSentCurrentRejectUnrecIdRetRes);
		rval = (u_char *) &StorageTmp->tcapOmDevelSentCurrentRejectUnrecIdRetRes;
		break;
	case (u_char) TCAPOMDEVELSENTCURRENTREJECTUNEXPRETRES:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSentCurrentRejectUnexpRetRes);
		rval = (u_char *) &StorageTmp->tcapOmDevelSentCurrentRejectUnexpRetRes;
		break;
	case (u_char) TCAPOMDEVELSENTCURRENTREJECTUNRECIDRETERR:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSentCurrentRejectUnrecIdRetErr);
		rval = (u_char *) &StorageTmp->tcapOmDevelSentCurrentRejectUnrecIdRetErr;
		break;
	case (u_char) TCAPOMDEVELSENTCURRENTREJECTUNEXPRETERR:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSentCurrentRejectUnexpRetErr);
		rval = (u_char *) &StorageTmp->tcapOmDevelSentCurrentRejectUnexpRetErr;
		break;
	case (u_char) TCAPOMDEVELSENTCURRENTUSRREJDUPINVOKEID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSentCurrentUsrRejDupInvokeId);
		rval = (u_char *) &StorageTmp->tcapOmDevelSentCurrentUsrRejDupInvokeId;
		break;
	case (u_char) TCAPOMDEVELSENTCURRENTUSRREJUNRECOPER:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSentCurrentUsrRejUnrecOper);
		rval = (u_char *) &StorageTmp->tcapOmDevelSentCurrentUsrRejUnrecOper;
		break;
	case (u_char) TCAPOMDEVELSENTCURRENTUSRREJPARMINVOKE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSentCurrentUsrRejParmInvoke);
		rval = (u_char *) &StorageTmp->tcapOmDevelSentCurrentUsrRejParmInvoke;
		break;
	case (u_char) TCAPOMDEVELSENTCURRENTUSRREJRELEASING:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSentCurrentUsrRejReleasing);
		rval = (u_char *) &StorageTmp->tcapOmDevelSentCurrentUsrRejReleasing;
		break;
	case (u_char) TCAPOMDEVELSENTCURRENTUSRREJUNEXPLINKRESP:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSentCurrentUsrRejUnexpLinkResp);
		rval = (u_char *) &StorageTmp->tcapOmDevelSentCurrentUsrRejUnexpLinkResp;
		break;
	case (u_char) TCAPOMDEVELSENTCURRENTUSRREJUNEXPLINKOPER:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSentCurrentUsrRejUnexpLinkOper);
		rval = (u_char *) &StorageTmp->tcapOmDevelSentCurrentUsrRejUnexpLinkOper;
		break;
	case (u_char) TCAPOMDEVELSENTCURRENTUSRREJUNRECERROR:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSentCurrentUsrRejUnrecError);
		rval = (u_char *) &StorageTmp->tcapOmDevelSentCurrentUsrRejUnrecError;
		break;
	case (u_char) TCAPOMDEVELSENTCURRENTUSRREJUNEXPERROR:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSentCurrentUsrRejUnexpError);
		rval = (u_char *) &StorageTmp->tcapOmDevelSentCurrentUsrRejUnexpError;
		break;
	case (u_char) TCAPOMDEVELSENTCURRENTUSRREJPARMRETRESULT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSentCurrentUsrRejParmRetResult);
		rval = (u_char *) &StorageTmp->tcapOmDevelSentCurrentUsrRejParmRetResult;
		break;
	case (u_char) TCAPOMDEVELSENTCURRENTUSRREJPARMRETERROR:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSentCurrentUsrRejParmRetError);
		rval = (u_char *) &StorageTmp->tcapOmDevelSentCurrentUsrRejParmRetError;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_tcapOmDevelSent5minIntTable_row(struct tcapOmDevelSent5minIntTable_data *StorageTmp, struct tcapOmDevelSent5minIntTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_tcapOmDevelSent5minIntTable_row(struct tcapOmDevelSent5minIntTable_data *StorageTmp, struct tcapOmDevelSent5minIntTable_data *StorageOld)
{
	(void) StorageTmp;
	(void) StorageOld;
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_tcapOmDevelSent5minIntTable_row(struct tcapOmDevelSent5minIntTable_data *StorageTmp, struct tcapOmDevelSent5minIntTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_tcapOmDevelSent5minIntTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_tcapOmDevelSent5minIntTable_row(struct tcapOmDevelSent5minIntTable_data *StorageTmp, struct tcapOmDevelSent5minIntTable_data *StorageOld)
{
	(void) StorageTmp;
	(void) StorageOld;
	/* XXX: provide code to update the row with the underlying device */
	tcapOmDevelSent5minIntTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_tcapOmDevelSent5minIntTable_row(struct tcapOmDevelSent5minIntTable_data *StorageTmp, struct tcapOmDevelSent5minIntTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_tcapOmDevelSent5minIntTable_row(struct tcapOmDevelSent5minIntTable_data *StorageTmp, struct tcapOmDevelSent5minIntTable_data *StorageOld)
{
	(void) StorageTmp;
	/* XXX: provide code to revert the row with the underlying device */
	update_tcapOmDevelSent5minIntTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_tcapOmDevelSent5minIntTable_row(struct tcapOmDevelSent5minIntTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the tcapOmDevelSent5minIntTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct tcapOmDevelSent5minIntTable_data *
refresh_tcapOmDevelSent5minIntTable_row(struct tcapOmDevelSent5minIntTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->tcapOmDevelSent5minIntTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->tcapOmDevelSent5minIntTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_tcapOmDevelSent5minIntTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the tcapOmDevelSent5minIntTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_tcapOmDevelSent5minIntTable(int force)
{
	if (!force && tcapOmDevelSent5minIntTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	tcapOmDevelSent5minIntTable_refresh = 0;
}

/**
 * @fn u_char *var_tcapOmDevelSent5minIntTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in tcapOmDevelSent5minIntTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_tcapOmMIB above.
 */
u_char *
var_tcapOmDevelSent5minIntTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct tcapOmDevelSent5minIntTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("tcapOmMIB", "var_tcapOmDevelSent5minIntTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_tcapOmDevelSent5minIntTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(tcapOmDevelSent5minIntTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_tcapOmDevelSent5minIntTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) TCAPOMDEVELSENT5MININTPABORTMESSAGETYPE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSent5minIntPabortMessageType);
		rval = (u_char *) &StorageTmp->tcapOmDevelSent5minIntPabortMessageType;
		break;
	case (u_char) TCAPOMDEVELSENT5MININTPABORTINCORRECTTP:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSent5minIntPabortIncorrectTp);
		rval = (u_char *) &StorageTmp->tcapOmDevelSent5minIntPabortIncorrectTp;
		break;
	case (u_char) TCAPOMDEVELSENT5MININTPABORTBADTP:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSent5minIntPabortBadTp);
		rval = (u_char *) &StorageTmp->tcapOmDevelSent5minIntPabortBadTp;
		break;
	case (u_char) TCAPOMDEVELSENT5MININTREJECTUNRECCOMP:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSent5minIntRejectUnrecComp);
		rval = (u_char *) &StorageTmp->tcapOmDevelSent5minIntRejectUnrecComp;
		break;
	case (u_char) TCAPOMDEVELSENT5MININTREJECTMISTYPECOMP:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSent5minIntRejectMistypeComp);
		rval = (u_char *) &StorageTmp->tcapOmDevelSent5minIntRejectMistypeComp;
		break;
	case (u_char) TCAPOMDEVELSENT5MININTREJECTBADCOMPSTRUCT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSent5minIntRejectBadCompStruct);
		rval = (u_char *) &StorageTmp->tcapOmDevelSent5minIntRejectBadCompStruct;
		break;
	case (u_char) TCAPOMDEVELSENT5MININTREJECTUNRECLINKID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSent5minIntRejectUnrecLinkId);
		rval = (u_char *) &StorageTmp->tcapOmDevelSent5minIntRejectUnrecLinkId;
		break;
	case (u_char) TCAPOMDEVELSENT5MININTREJECTUNRECIDRETRES:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSent5minIntRejectUnrecIdRetRes);
		rval = (u_char *) &StorageTmp->tcapOmDevelSent5minIntRejectUnrecIdRetRes;
		break;
	case (u_char) TCAPOMDEVELSENT5MININTREJECTUNEXPRETRES:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSent5minIntRejectUnexpRetRes);
		rval = (u_char *) &StorageTmp->tcapOmDevelSent5minIntRejectUnexpRetRes;
		break;
	case (u_char) TCAPOMDEVELSENT5MININTREJECTUNRECIDRETERR:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSent5minIntRejectUnrecIdRetErr);
		rval = (u_char *) &StorageTmp->tcapOmDevelSent5minIntRejectUnrecIdRetErr;
		break;
	case (u_char) TCAPOMDEVELSENT5MININTREJECTUNEXPRETERR:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSent5minIntRejectUnexpRetErr);
		rval = (u_char *) &StorageTmp->tcapOmDevelSent5minIntRejectUnexpRetErr;
		break;
	case (u_char) TCAPOMDEVELSENT5MININTUSRREJDUPINVOKEID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSent5minIntUsrRejDupInvokeId);
		rval = (u_char *) &StorageTmp->tcapOmDevelSent5minIntUsrRejDupInvokeId;
		break;
	case (u_char) TCAPOMDEVELSENT5MININTUSRREJUNRECOPER:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSent5minIntUsrRejUnrecOper);
		rval = (u_char *) &StorageTmp->tcapOmDevelSent5minIntUsrRejUnrecOper;
		break;
	case (u_char) TCAPOMDEVELSENT5MININTUSRREJPARMINVOKE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSent5minIntUsrRejParmInvoke);
		rval = (u_char *) &StorageTmp->tcapOmDevelSent5minIntUsrRejParmInvoke;
		break;
	case (u_char) TCAPOMDEVELSENT5MININTUSRREJRELEASING:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSent5minIntUsrRejReleasing);
		rval = (u_char *) &StorageTmp->tcapOmDevelSent5minIntUsrRejReleasing;
		break;
	case (u_char) TCAPOMDEVELSENT5MININTUSRREJUNEXPLINKRESP:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSent5minIntUsrRejUnexpLinkResp);
		rval = (u_char *) &StorageTmp->tcapOmDevelSent5minIntUsrRejUnexpLinkResp;
		break;
	case (u_char) TCAPOMDEVELSENT5MININTUSRREJUNEXPLINKOPER:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSent5minIntUsrRejUnexpLinkOper);
		rval = (u_char *) &StorageTmp->tcapOmDevelSent5minIntUsrRejUnexpLinkOper;
		break;
	case (u_char) TCAPOMDEVELSENT5MININTUSRREJUNRECERROR:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSent5minIntUsrRejUnrecError);
		rval = (u_char *) &StorageTmp->tcapOmDevelSent5minIntUsrRejUnrecError;
		break;
	case (u_char) TCAPOMDEVELSENT5MININTUSRREJUNEXPERROR:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSent5minIntUsrRejUnexpError);
		rval = (u_char *) &StorageTmp->tcapOmDevelSent5minIntUsrRejUnexpError;
		break;
	case (u_char) TCAPOMDEVELSENT5MININTUSRREJPARMRETRESULT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSent5minIntUsrRejParmRetResult);
		rval = (u_char *) &StorageTmp->tcapOmDevelSent5minIntUsrRejParmRetResult;
		break;
	case (u_char) TCAPOMDEVELSENT5MININTUSRREJPARMRETERROR:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSent5minIntUsrRejParmRetError);
		rval = (u_char *) &StorageTmp->tcapOmDevelSent5minIntUsrRejParmRetError;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_tcapOmDevelSent15minIntTable_row(struct tcapOmDevelSent15minIntTable_data *StorageTmp, struct tcapOmDevelSent15minIntTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_tcapOmDevelSent15minIntTable_row(struct tcapOmDevelSent15minIntTable_data *StorageTmp, struct tcapOmDevelSent15minIntTable_data *StorageOld)
{
	(void) StorageTmp;
	(void) StorageOld;
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_tcapOmDevelSent15minIntTable_row(struct tcapOmDevelSent15minIntTable_data *StorageTmp, struct tcapOmDevelSent15minIntTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_tcapOmDevelSent15minIntTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_tcapOmDevelSent15minIntTable_row(struct tcapOmDevelSent15minIntTable_data *StorageTmp, struct tcapOmDevelSent15minIntTable_data *StorageOld)
{
	(void) StorageTmp;
	(void) StorageOld;
	/* XXX: provide code to update the row with the underlying device */
	tcapOmDevelSent15minIntTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_tcapOmDevelSent15minIntTable_row(struct tcapOmDevelSent15minIntTable_data *StorageTmp, struct tcapOmDevelSent15minIntTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_tcapOmDevelSent15minIntTable_row(struct tcapOmDevelSent15minIntTable_data *StorageTmp, struct tcapOmDevelSent15minIntTable_data *StorageOld)
{
	(void) StorageTmp;
	/* XXX: provide code to revert the row with the underlying device */
	update_tcapOmDevelSent15minIntTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_tcapOmDevelSent15minIntTable_row(struct tcapOmDevelSent15minIntTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the tcapOmDevelSent15minIntTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct tcapOmDevelSent15minIntTable_data *
refresh_tcapOmDevelSent15minIntTable_row(struct tcapOmDevelSent15minIntTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->tcapOmDevelSent15minIntTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->tcapOmDevelSent15minIntTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_tcapOmDevelSent15minIntTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the tcapOmDevelSent15minIntTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_tcapOmDevelSent15minIntTable(int force)
{
	if (!force && tcapOmDevelSent15minIntTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	tcapOmDevelSent15minIntTable_refresh = 0;
}

/**
 * @fn u_char *var_tcapOmDevelSent15minIntTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in tcapOmDevelSent15minIntTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_tcapOmMIB above.
 */
u_char *
var_tcapOmDevelSent15minIntTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct tcapOmDevelSent15minIntTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("tcapOmMIB", "var_tcapOmDevelSent15minIntTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_tcapOmDevelSent15minIntTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(tcapOmDevelSent15minIntTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_tcapOmDevelSent15minIntTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) TCAPOMDEVELSENT15MININTPABORTMESSAGETYPE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSent15minIntPabortMessageType);
		rval = (u_char *) &StorageTmp->tcapOmDevelSent15minIntPabortMessageType;
		break;
	case (u_char) TCAPOMDEVELSENT15MININTPABORTINCORRECTTP:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSent15minIntPabortIncorrectTp);
		rval = (u_char *) &StorageTmp->tcapOmDevelSent15minIntPabortIncorrectTp;
		break;
	case (u_char) TCAPOMDEVELSENT15MININTPABORTBADTP:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSent15minIntPabortBadTp);
		rval = (u_char *) &StorageTmp->tcapOmDevelSent15minIntPabortBadTp;
		break;
	case (u_char) TCAPOMDEVELSENT15MININTREJECTUNRECCOMP:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSent15minIntRejectUnrecComp);
		rval = (u_char *) &StorageTmp->tcapOmDevelSent15minIntRejectUnrecComp;
		break;
	case (u_char) TCAPOMDEVELSENT15MININTREJECTMISTYPECOMP:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSent15minIntRejectMistypeComp);
		rval = (u_char *) &StorageTmp->tcapOmDevelSent15minIntRejectMistypeComp;
		break;
	case (u_char) TCAPOMDEVELSENT15MININTREJECTBADCOMPSTRUCT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSent15minIntRejectBadCompStruct);
		rval = (u_char *) &StorageTmp->tcapOmDevelSent15minIntRejectBadCompStruct;
		break;
	case (u_char) TCAPOMDEVELSENT15MININTREJECTUNRECLINKID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSent15minIntRejectUnrecLinkId);
		rval = (u_char *) &StorageTmp->tcapOmDevelSent15minIntRejectUnrecLinkId;
		break;
	case (u_char) TCAPOMDEVELSENT15MININTREJECTUNRECIDRETRES:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSent15minIntRejectUnrecIdRetRes);
		rval = (u_char *) &StorageTmp->tcapOmDevelSent15minIntRejectUnrecIdRetRes;
		break;
	case (u_char) TCAPOMDEVELSENT15MININTREJECTUNEXPRETRES:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSent15minIntRejectUnexpRetRes);
		rval = (u_char *) &StorageTmp->tcapOmDevelSent15minIntRejectUnexpRetRes;
		break;
	case (u_char) TCAPOMDEVELSENT15MININTREJECTUNRECIDRETERR:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSent15minIntRejectUnrecIdRetErr);
		rval = (u_char *) &StorageTmp->tcapOmDevelSent15minIntRejectUnrecIdRetErr;
		break;
	case (u_char) TCAPOMDEVELSENT15MININTREJECTUNEXPRETERR:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSent15minIntRejectUnexpRetErr);
		rval = (u_char *) &StorageTmp->tcapOmDevelSent15minIntRejectUnexpRetErr;
		break;
	case (u_char) TCAPOMDEVELSENT15MININTUSRREJDUPINVOKEID:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSent15minIntUsrRejDupInvokeId);
		rval = (u_char *) &StorageTmp->tcapOmDevelSent15minIntUsrRejDupInvokeId;
		break;
	case (u_char) TCAPOMDEVELSENT15MININTUSRREJUNRECOPER:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSent15minIntUsrRejUnrecOper);
		rval = (u_char *) &StorageTmp->tcapOmDevelSent15minIntUsrRejUnrecOper;
		break;
	case (u_char) TCAPOMDEVELSENT15MININTUSRREJPARMINVOKE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSent15minIntUsrRejParmInvoke);
		rval = (u_char *) &StorageTmp->tcapOmDevelSent15minIntUsrRejParmInvoke;
		break;
	case (u_char) TCAPOMDEVELSENT15MININTUSRREJRELEASING:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSent15minIntUsrRejReleasing);
		rval = (u_char *) &StorageTmp->tcapOmDevelSent15minIntUsrRejReleasing;
		break;
	case (u_char) TCAPOMDEVELSENT15MININTUSRREJUNEXPLINKRESP:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSent15minIntUsrRejUnexpLinkResp);
		rval = (u_char *) &StorageTmp->tcapOmDevelSent15minIntUsrRejUnexpLinkResp;
		break;
	case (u_char) TCAPOMDEVELSENT15MININTUSRREJUNEXPLINKOPER:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSent15minIntUsrRejUnexpLinkOper);
		rval = (u_char *) &StorageTmp->tcapOmDevelSent15minIntUsrRejUnexpLinkOper;
		break;
	case (u_char) TCAPOMDEVELSENT15MININTUSRREJUNRECERROR:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSent15minIntUsrRejUnrecError);
		rval = (u_char *) &StorageTmp->tcapOmDevelSent15minIntUsrRejUnrecError;
		break;
	case (u_char) TCAPOMDEVELSENT15MININTUSRREJUNEXPERROR:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSent15minIntUsrRejUnexpError);
		rval = (u_char *) &StorageTmp->tcapOmDevelSent15minIntUsrRejUnexpError;
		break;
	case (u_char) TCAPOMDEVELSENT15MININTUSRREJPARMRETRESULT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSent15minIntUsrRejParmRetResult);
		rval = (u_char *) &StorageTmp->tcapOmDevelSent15minIntUsrRejParmRetResult;
		break;
	case (u_char) TCAPOMDEVELSENT15MININTUSRREJPARMRETERROR:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->tcapOmDevelSent15minIntUsrRejParmRetError);
		rval = (u_char *) &StorageTmp->tcapOmDevelSent15minIntUsrRejParmRetError;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int write_tcapOm1stAndIntervalActivate(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_tcapOm1stAndIntervalActivate(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct tcapOmMIB_data *StorageTmp = NULL, *StorageOld = NULL;
	oid *objid = NULL;
	int ret = SNMP_ERR_NOERROR;

	(void) var_val_len;	/* not always used */
	(void) statP;
	(void) name;
	(void) name_len;
	DEBUGMSGTL(("tcapOmMIB", "write_tcapOm1stAndIntervalActivate entering action=%d...  \n", action));
	if ((StorageTmp = tcapOmMIBStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_OBJECT_ID) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to tcapOm1stAndIntervalActivate not ASN_OBJECT_ID\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (MIN_OID_LEN * sizeof(oid) > var_val_len || var_val_len > MAX_OID_LEN * sizeof(oid)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to tcapOm1stAndIntervalActivate: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for whole mib */
		if ((StorageOld = StorageTmp->tcapOmMIB_old) == NULL)
			if (StorageTmp->tcapOmMIB_rsvs == 0)
				if ((StorageOld = StorageTmp->tcapOmMIB_old = tcapOmMIB_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->tcapOmMIB_rsvs++;
		if ((objid = snmp_duplicate_objid((void *) var_val, var_val_len / sizeof(oid))) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		SNMP_FREE(StorageTmp->tcapOm1stAndIntervalActivate);
		StorageTmp->tcapOm1stAndIntervalActivate = objid;
		StorageTmp->tcapOm1stAndIntervalActivateLen = var_val_len / sizeof(oid);
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->tcapOmMIB_old) != NULL) {
			/* one consistency check for the whole mib */
			if (StorageTmp->tcapOmMIB_tsts == 0)
				if ((ret = check_tcapOmMIB(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->tcapOmMIB_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->tcapOm1stAndIntervalActivate for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole mib */
		if ((StorageOld = StorageTmp->tcapOmMIB_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->tcapOmMIB_sets == 0)
				if ((ret = update_tcapOmMIB(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->tcapOmMIB_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->tcapOmMIB_old) != NULL) {
			tcapOmMIB_destroy(&StorageTmp->tcapOmMIB_old);
			StorageTmp->tcapOmMIB_rsvs = 0;
			StorageTmp->tcapOmMIB_tsts = 0;
			StorageTmp->tcapOmMIB_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->tcapOmMIB_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->tcapOmMIB_tsts == 0)
			revert_tcapOmMIB(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->tcapOmMIB_old) == NULL)
			break;
		if (StorageOld->tcapOm1stAndIntervalActivate != NULL) {
			SNMP_FREE(StorageTmp->tcapOm1stAndIntervalActivate);
			StorageTmp->tcapOm1stAndIntervalActivate = StorageOld->tcapOm1stAndIntervalActivate;
			StorageTmp->tcapOm1stAndIntervalActivateLen = StorageOld->tcapOm1stAndIntervalActivateLen;
			StorageOld->tcapOm1stAndIntervalActivate = NULL;
			StorageOld->tcapOm1stAndIntervalActivateLen = 0;
		}
		if (--StorageTmp->tcapOmMIB_rsvs == 0)
			tcapOmMIB_destroy(&StorageTmp->tcapOmMIB_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_tcapOm1stAndIntervalDeactivate(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_tcapOm1stAndIntervalDeactivate(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct tcapOmMIB_data *StorageTmp = NULL, *StorageOld = NULL;
	oid *objid = NULL;
	int ret = SNMP_ERR_NOERROR;

	(void) var_val_len;	/* not always used */
	(void) statP;
	(void) name;
	(void) name_len;
	DEBUGMSGTL(("tcapOmMIB", "write_tcapOm1stAndIntervalDeactivate entering action=%d...  \n", action));
	if ((StorageTmp = tcapOmMIBStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_OBJECT_ID) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to tcapOm1stAndIntervalDeactivate not ASN_OBJECT_ID\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (MIN_OID_LEN * sizeof(oid) > var_val_len || var_val_len > MAX_OID_LEN * sizeof(oid)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to tcapOm1stAndIntervalDeactivate: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for whole mib */
		if ((StorageOld = StorageTmp->tcapOmMIB_old) == NULL)
			if (StorageTmp->tcapOmMIB_rsvs == 0)
				if ((StorageOld = StorageTmp->tcapOmMIB_old = tcapOmMIB_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->tcapOmMIB_rsvs++;
		if ((objid = snmp_duplicate_objid((void *) var_val, var_val_len / sizeof(oid))) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		SNMP_FREE(StorageTmp->tcapOm1stAndIntervalDeactivate);
		StorageTmp->tcapOm1stAndIntervalDeactivate = objid;
		StorageTmp->tcapOm1stAndIntervalDeactivateLen = var_val_len / sizeof(oid);
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->tcapOmMIB_old) != NULL) {
			/* one consistency check for the whole mib */
			if (StorageTmp->tcapOmMIB_tsts == 0)
				if ((ret = check_tcapOmMIB(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->tcapOmMIB_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->tcapOm1stAndIntervalDeactivate for you to use, and you have just been asked to do something with it.  Note that anything 
				   done here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole mib */
		if ((StorageOld = StorageTmp->tcapOmMIB_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->tcapOmMIB_sets == 0)
				if ((ret = update_tcapOmMIB(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->tcapOmMIB_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->tcapOmMIB_old) != NULL) {
			tcapOmMIB_destroy(&StorageTmp->tcapOmMIB_old);
			StorageTmp->tcapOmMIB_rsvs = 0;
			StorageTmp->tcapOmMIB_tsts = 0;
			StorageTmp->tcapOmMIB_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->tcapOmMIB_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->tcapOmMIB_tsts == 0)
			revert_tcapOmMIB(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->tcapOmMIB_old) == NULL)
			break;
		if (StorageOld->tcapOm1stAndIntervalDeactivate != NULL) {
			SNMP_FREE(StorageTmp->tcapOm1stAndIntervalDeactivate);
			StorageTmp->tcapOm1stAndIntervalDeactivate = StorageOld->tcapOm1stAndIntervalDeactivate;
			StorageTmp->tcapOm1stAndIntervalDeactivateLen = StorageOld->tcapOm1stAndIntervalDeactivateLen;
			StorageOld->tcapOm1stAndIntervalDeactivate = NULL;
			StorageOld->tcapOm1stAndIntervalDeactivateLen = 0;
		}
		if (--StorageTmp->tcapOmMIB_rsvs == 0)
			tcapOmMIB_destroy(&StorageTmp->tcapOmMIB_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_tcapOm5MinActivate(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_tcapOm5MinActivate(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct tcapOmMIB_data *StorageTmp = NULL, *StorageOld = NULL;
	oid *objid = NULL;
	int ret = SNMP_ERR_NOERROR;

	(void) var_val_len;	/* not always used */
	(void) statP;
	(void) name;
	(void) name_len;
	DEBUGMSGTL(("tcapOmMIB", "write_tcapOm5MinActivate entering action=%d...  \n", action));
	if ((StorageTmp = tcapOmMIBStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_OBJECT_ID) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to tcapOm5MinActivate not ASN_OBJECT_ID\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (MIN_OID_LEN * sizeof(oid) > var_val_len || var_val_len > MAX_OID_LEN * sizeof(oid)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to tcapOm5MinActivate: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for whole mib */
		if ((StorageOld = StorageTmp->tcapOmMIB_old) == NULL)
			if (StorageTmp->tcapOmMIB_rsvs == 0)
				if ((StorageOld = StorageTmp->tcapOmMIB_old = tcapOmMIB_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->tcapOmMIB_rsvs++;
		if ((objid = snmp_duplicate_objid((void *) var_val, var_val_len / sizeof(oid))) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		SNMP_FREE(StorageTmp->tcapOm5MinActivate);
		StorageTmp->tcapOm5MinActivate = objid;
		StorageTmp->tcapOm5MinActivateLen = var_val_len / sizeof(oid);
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->tcapOmMIB_old) != NULL) {
			/* one consistency check for the whole mib */
			if (StorageTmp->tcapOmMIB_tsts == 0)
				if ((ret = check_tcapOmMIB(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->tcapOmMIB_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->tcapOm5MinActivate for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole mib */
		if ((StorageOld = StorageTmp->tcapOmMIB_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->tcapOmMIB_sets == 0)
				if ((ret = update_tcapOmMIB(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->tcapOmMIB_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->tcapOmMIB_old) != NULL) {
			tcapOmMIB_destroy(&StorageTmp->tcapOmMIB_old);
			StorageTmp->tcapOmMIB_rsvs = 0;
			StorageTmp->tcapOmMIB_tsts = 0;
			StorageTmp->tcapOmMIB_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->tcapOmMIB_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->tcapOmMIB_tsts == 0)
			revert_tcapOmMIB(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->tcapOmMIB_old) == NULL)
			break;
		if (StorageOld->tcapOm5MinActivate != NULL) {
			SNMP_FREE(StorageTmp->tcapOm5MinActivate);
			StorageTmp->tcapOm5MinActivate = StorageOld->tcapOm5MinActivate;
			StorageTmp->tcapOm5MinActivateLen = StorageOld->tcapOm5MinActivateLen;
			StorageOld->tcapOm5MinActivate = NULL;
			StorageOld->tcapOm5MinActivateLen = 0;
		}
		if (--StorageTmp->tcapOmMIB_rsvs == 0)
			tcapOmMIB_destroy(&StorageTmp->tcapOmMIB_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_tcapOm5MinDeaActivate(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_tcapOm5MinDeaActivate(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct tcapOmMIB_data *StorageTmp = NULL, *StorageOld = NULL;
	oid *objid = NULL;
	int ret = SNMP_ERR_NOERROR;

	(void) var_val_len;	/* not always used */
	(void) statP;
	(void) name;
	(void) name_len;
	DEBUGMSGTL(("tcapOmMIB", "write_tcapOm5MinDeaActivate entering action=%d...  \n", action));
	if ((StorageTmp = tcapOmMIBStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_OBJECT_ID) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to tcapOm5MinDeaActivate not ASN_OBJECT_ID\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (MIN_OID_LEN * sizeof(oid) > var_val_len || var_val_len > MAX_OID_LEN * sizeof(oid)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to tcapOm5MinDeaActivate: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for whole mib */
		if ((StorageOld = StorageTmp->tcapOmMIB_old) == NULL)
			if (StorageTmp->tcapOmMIB_rsvs == 0)
				if ((StorageOld = StorageTmp->tcapOmMIB_old = tcapOmMIB_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->tcapOmMIB_rsvs++;
		if ((objid = snmp_duplicate_objid((void *) var_val, var_val_len / sizeof(oid))) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		SNMP_FREE(StorageTmp->tcapOm5MinDeaActivate);
		StorageTmp->tcapOm5MinDeaActivate = objid;
		StorageTmp->tcapOm5MinDeaActivateLen = var_val_len / sizeof(oid);
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->tcapOmMIB_old) != NULL) {
			/* one consistency check for the whole mib */
			if (StorageTmp->tcapOmMIB_tsts == 0)
				if ((ret = check_tcapOmMIB(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->tcapOmMIB_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->tcapOm5MinDeaActivate for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole mib */
		if ((StorageOld = StorageTmp->tcapOmMIB_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->tcapOmMIB_sets == 0)
				if ((ret = update_tcapOmMIB(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->tcapOmMIB_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->tcapOmMIB_old) != NULL) {
			tcapOmMIB_destroy(&StorageTmp->tcapOmMIB_old);
			StorageTmp->tcapOmMIB_rsvs = 0;
			StorageTmp->tcapOmMIB_tsts = 0;
			StorageTmp->tcapOmMIB_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->tcapOmMIB_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->tcapOmMIB_tsts == 0)
			revert_tcapOmMIB(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->tcapOmMIB_old) == NULL)
			break;
		if (StorageOld->tcapOm5MinDeaActivate != NULL) {
			SNMP_FREE(StorageTmp->tcapOm5MinDeaActivate);
			StorageTmp->tcapOm5MinDeaActivate = StorageOld->tcapOm5MinDeaActivate;
			StorageTmp->tcapOm5MinDeaActivateLen = StorageOld->tcapOm5MinDeaActivateLen;
			StorageOld->tcapOm5MinDeaActivate = NULL;
			StorageOld->tcapOm5MinDeaActivateLen = 0;
		}
		if (--StorageTmp->tcapOmMIB_rsvs == 0)
			tcapOmMIB_destroy(&StorageTmp->tcapOmMIB_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_tcapOm15MinActivate(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_tcapOm15MinActivate(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct tcapOmMIB_data *StorageTmp = NULL, *StorageOld = NULL;
	oid *objid = NULL;
	int ret = SNMP_ERR_NOERROR;

	(void) var_val_len;	/* not always used */
	(void) statP;
	(void) name;
	(void) name_len;
	DEBUGMSGTL(("tcapOmMIB", "write_tcapOm15MinActivate entering action=%d...  \n", action));
	if ((StorageTmp = tcapOmMIBStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_OBJECT_ID) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to tcapOm15MinActivate not ASN_OBJECT_ID\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (MIN_OID_LEN * sizeof(oid) > var_val_len || var_val_len > MAX_OID_LEN * sizeof(oid)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to tcapOm15MinActivate: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for whole mib */
		if ((StorageOld = StorageTmp->tcapOmMIB_old) == NULL)
			if (StorageTmp->tcapOmMIB_rsvs == 0)
				if ((StorageOld = StorageTmp->tcapOmMIB_old = tcapOmMIB_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->tcapOmMIB_rsvs++;
		if ((objid = snmp_duplicate_objid((void *) var_val, var_val_len / sizeof(oid))) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		SNMP_FREE(StorageTmp->tcapOm15MinActivate);
		StorageTmp->tcapOm15MinActivate = objid;
		StorageTmp->tcapOm15MinActivateLen = var_val_len / sizeof(oid);
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->tcapOmMIB_old) != NULL) {
			/* one consistency check for the whole mib */
			if (StorageTmp->tcapOmMIB_tsts == 0)
				if ((ret = check_tcapOmMIB(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->tcapOmMIB_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->tcapOm15MinActivate for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole mib */
		if ((StorageOld = StorageTmp->tcapOmMIB_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->tcapOmMIB_sets == 0)
				if ((ret = update_tcapOmMIB(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->tcapOmMIB_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->tcapOmMIB_old) != NULL) {
			tcapOmMIB_destroy(&StorageTmp->tcapOmMIB_old);
			StorageTmp->tcapOmMIB_rsvs = 0;
			StorageTmp->tcapOmMIB_tsts = 0;
			StorageTmp->tcapOmMIB_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->tcapOmMIB_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->tcapOmMIB_tsts == 0)
			revert_tcapOmMIB(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->tcapOmMIB_old) == NULL)
			break;
		if (StorageOld->tcapOm15MinActivate != NULL) {
			SNMP_FREE(StorageTmp->tcapOm15MinActivate);
			StorageTmp->tcapOm15MinActivate = StorageOld->tcapOm15MinActivate;
			StorageTmp->tcapOm15MinActivateLen = StorageOld->tcapOm15MinActivateLen;
			StorageOld->tcapOm15MinActivate = NULL;
			StorageOld->tcapOm15MinActivateLen = 0;
		}
		if (--StorageTmp->tcapOmMIB_rsvs == 0)
			tcapOmMIB_destroy(&StorageTmp->tcapOmMIB_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_tcapOm15MinDeaActivate(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_tcapOm15MinDeaActivate(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct tcapOmMIB_data *StorageTmp = NULL, *StorageOld = NULL;
	oid *objid = NULL;
	int ret = SNMP_ERR_NOERROR;

	(void) var_val_len;	/* not always used */
	(void) statP;
	(void) name;
	(void) name_len;
	DEBUGMSGTL(("tcapOmMIB", "write_tcapOm15MinDeaActivate entering action=%d...  \n", action));
	if ((StorageTmp = tcapOmMIBStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_OBJECT_ID) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to tcapOm15MinDeaActivate not ASN_OBJECT_ID\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (MIN_OID_LEN * sizeof(oid) > var_val_len || var_val_len > MAX_OID_LEN * sizeof(oid)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to tcapOm15MinDeaActivate: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for whole mib */
		if ((StorageOld = StorageTmp->tcapOmMIB_old) == NULL)
			if (StorageTmp->tcapOmMIB_rsvs == 0)
				if ((StorageOld = StorageTmp->tcapOmMIB_old = tcapOmMIB_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->tcapOmMIB_rsvs++;
		if ((objid = snmp_duplicate_objid((void *) var_val, var_val_len / sizeof(oid))) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		SNMP_FREE(StorageTmp->tcapOm15MinDeaActivate);
		StorageTmp->tcapOm15MinDeaActivate = objid;
		StorageTmp->tcapOm15MinDeaActivateLen = var_val_len / sizeof(oid);
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->tcapOmMIB_old) != NULL) {
			/* one consistency check for the whole mib */
			if (StorageTmp->tcapOmMIB_tsts == 0)
				if ((ret = check_tcapOmMIB(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->tcapOmMIB_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->tcapOm15MinDeaActivate for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole mib */
		if ((StorageOld = StorageTmp->tcapOmMIB_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->tcapOmMIB_sets == 0)
				if ((ret = update_tcapOmMIB(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->tcapOmMIB_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->tcapOmMIB_old) != NULL) {
			tcapOmMIB_destroy(&StorageTmp->tcapOmMIB_old);
			StorageTmp->tcapOmMIB_rsvs = 0;
			StorageTmp->tcapOmMIB_tsts = 0;
			StorageTmp->tcapOmMIB_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->tcapOmMIB_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->tcapOmMIB_tsts == 0)
			revert_tcapOmMIB(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->tcapOmMIB_old) == NULL)
			break;
		if (StorageOld->tcapOm15MinDeaActivate != NULL) {
			SNMP_FREE(StorageTmp->tcapOm15MinDeaActivate);
			StorageTmp->tcapOm15MinDeaActivate = StorageOld->tcapOm15MinDeaActivate;
			StorageTmp->tcapOm15MinDeaActivateLen = StorageOld->tcapOm15MinDeaActivateLen;
			StorageOld->tcapOm15MinDeaActivate = NULL;
			StorageOld->tcapOm15MinDeaActivateLen = 0;
		}
		if (--StorageTmp->tcapOmMIB_rsvs == 0)
			tcapOmMIB_destroy(&StorageTmp->tcapOmMIB_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_tcapOm5MinMaxIntervals(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_tcapOm5MinMaxIntervals(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct tcapOmMIB_data *StorageTmp = NULL, *StorageOld = NULL;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	(void) var_val_len;	/* not always used */
	(void) statP;
	(void) name;
	(void) name_len;
	DEBUGMSGTL(("tcapOmMIB", "write_tcapOm5MinMaxIntervals entering action=%d...  \n", action));
	if ((StorageTmp = tcapOmMIBStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to tcapOm5MinMaxIntervals not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to tcapOm5MinMaxIntervals: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 96 */
		/* Note: ranges 0..288 */
		if ((set_value > 288)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to tcapOm5MinMaxIntervals: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for whole mib */
		if ((StorageOld = StorageTmp->tcapOmMIB_old) == NULL)
			if (StorageTmp->tcapOmMIB_rsvs == 0)
				if ((StorageOld = StorageTmp->tcapOmMIB_old = tcapOmMIB_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->tcapOmMIB_rsvs++;
		StorageTmp->tcapOm5MinMaxIntervals = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->tcapOmMIB_old) != NULL) {
			/* one consistency check for the whole mib */
			if (StorageTmp->tcapOmMIB_tsts == 0)
				if ((ret = check_tcapOmMIB(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->tcapOmMIB_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->tcapOm5MinMaxIntervals for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole mib */
		if ((StorageOld = StorageTmp->tcapOmMIB_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->tcapOmMIB_sets == 0)
				if ((ret = update_tcapOmMIB(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->tcapOmMIB_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->tcapOmMIB_old) != NULL) {
			tcapOmMIB_destroy(&StorageTmp->tcapOmMIB_old);
			StorageTmp->tcapOmMIB_rsvs = 0;
			StorageTmp->tcapOmMIB_tsts = 0;
			StorageTmp->tcapOmMIB_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->tcapOmMIB_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->tcapOmMIB_tsts == 0)
			revert_tcapOmMIB(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->tcapOmMIB_old) == NULL)
			break;
		StorageTmp->tcapOm5MinMaxIntervals = StorageOld->tcapOm5MinMaxIntervals;
		if (--StorageTmp->tcapOmMIB_rsvs == 0)
			tcapOmMIB_destroy(&StorageTmp->tcapOmMIB_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_tcapOm15MinMaxIntervals(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_tcapOm15MinMaxIntervals(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct tcapOmMIB_data *StorageTmp = NULL, *StorageOld = NULL;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	(void) var_val_len;	/* not always used */
	(void) statP;
	(void) name;
	(void) name_len;
	DEBUGMSGTL(("tcapOmMIB", "write_tcapOm15MinMaxIntervals entering action=%d...  \n", action));
	if ((StorageTmp = tcapOmMIBStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to tcapOm15MinMaxIntervals not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to tcapOm15MinMaxIntervals: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 96 */
		/* Note: ranges 0..672 */
		if ((set_value > 672)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to tcapOm15MinMaxIntervals: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for whole mib */
		if ((StorageOld = StorageTmp->tcapOmMIB_old) == NULL)
			if (StorageTmp->tcapOmMIB_rsvs == 0)
				if ((StorageOld = StorageTmp->tcapOmMIB_old = tcapOmMIB_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->tcapOmMIB_rsvs++;
		StorageTmp->tcapOm15MinMaxIntervals = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->tcapOmMIB_old) != NULL) {
			/* one consistency check for the whole mib */
			if (StorageTmp->tcapOmMIB_tsts == 0)
				if ((ret = check_tcapOmMIB(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->tcapOmMIB_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->tcapOm15MinMaxIntervals for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole mib */
		if ((StorageOld = StorageTmp->tcapOmMIB_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->tcapOmMIB_sets == 0)
				if ((ret = update_tcapOmMIB(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->tcapOmMIB_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->tcapOmMIB_old) != NULL) {
			tcapOmMIB_destroy(&StorageTmp->tcapOmMIB_old);
			StorageTmp->tcapOmMIB_rsvs = 0;
			StorageTmp->tcapOmMIB_tsts = 0;
			StorageTmp->tcapOmMIB_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->tcapOmMIB_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->tcapOmMIB_tsts == 0)
			revert_tcapOmMIB(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->tcapOmMIB_old) == NULL)
			break;
		StorageTmp->tcapOm15MinMaxIntervals = StorageOld->tcapOm15MinMaxIntervals;
		if (--StorageTmp->tcapOmMIB_rsvs == 0)
			tcapOmMIB_destroy(&StorageTmp->tcapOmMIB_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn void tcapOmMIB_loop_handler(int dummy)
 * @param sig signal number
 * @brief handle event loop interation.
 *
 * This function is registered so that, when operating as a module, snmpd will call it one per event
 * loop interation.  This function is called before the next requst is processed and after the
 * previous request is processed.  Two things are done here:  1) The file descriptor that is used to
 * synchronize the agent with (pseudo-)device drivers is closed.  (Another approach, instead of
 * closing each time, would be to restart a timer each time that a request is made (loop is
 * performed) and if it expires, close the file descriptor).  2) The request number is incremented.
 * Although a request is not generated for each loop of the snmp event loop, it is true that a new
 * request cannot be generated without performing a loop.  Therefore, the sa_request is not the
 * request number but it is a temporally unique identifier for a request.
 */
void
tcapOmMIB_loop_handler(int sig)
{
	DEBUGMSGTL(("tcapOmMIB", "tcapOmMIB_loop_handler: executing loop handler...  "));
	/* close files after each request */
	if (sa_fclose) {
		if (my_fd >= 0) {
			close(my_fd);
			my_fd = -1;
		}
	}
#if defined MASTER
	/* prepare for next request */
	sa_request++;
#endif				/* defined MASTER */
	if (external_signal_scheduled[sig] == 0)
		external_signal_scheduled[sig]--;
	if (tcapOmMIBold_signal_handler != NULL)
		(*tcapOmMIBold_signal_handler) (sig);
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
}

/**
 * @fn void tcapOmMIB_fd_handler(int fd, void *dummy)
 * @param fd file descriptor to read.
 * @param dummy client data passed to registration function (always NULL).
 * @brief handle read event on file descriptor.
 *
 * This read file descriptor handler is normally used for (pseudo-)device drivers that generate
 * statistical collection interval events, alarm events, or other operational measurement events, by
 * placing a message on the read queue of the "event handling" Stream.  Normally this routine
 * would adjust counts in some table or scalars, generate SNMP traps representing on-occurence
 * events, first and interval events, and alarm indications.
 */
void
tcapOmMIB_fd_handler(int fd, void *dummy)
{
	(void) fd;
	(void) dummy;
	DEBUGMSGTL(("tcapOmMIB", "tcapOmMIB_fd_handler: executing fd handler...  "));
	/* XXX: place actions to handle my_fd here... */
	DEBUGMSGTL(("tcapOmMIB", "done.\n"));
	return;
}
