/*****************************************************************************

 @(#) src/snmp/slMIB.c

 -----------------------------------------------------------------------------

 Copyright (c) 2008-2015  Monavacon Limited <http://www.monavacon.com/>
 Copyright (c) 2001-2008  OpenSS7 Corporation <http://www.openss7.com/>
 Copyright (c) 1997-2001  Brian F. G. Bidulock <bidulock@openss7.org>

 All Rights Reserved.

 This program is free software: you can redistribute it and/or modify it under
 the terms of the GNU Affero General Public License as published by the Free
 Software Foundation, version 3 of the license.

 This program is distributed in the hope that it will be useful, but WITHOUT
 ANY WARRANTY; without even the implied warranty of MERCHANTABILITY or FITNESS
 FOR A PARTICULAR PURPOSE.  See the GNU Affero General Public License for more
 details.

 You should have received a copy of the GNU Affero General Public License
 along with this program.  If not, see <http://www.gnu.org/licenses/>, or
 write to the Free Software Foundation, Inc., 675 Mass Ave, Cambridge, MA
 02139, USA.

 -----------------------------------------------------------------------------

 U.S. GOVERNMENT RESTRICTED RIGHTS.  If you are licensing this Software on
 behalf of the U.S. Government ("Government"), the following provisions apply
 to you.  If the Software is supplied by the Department of Defense ("DoD"), it
 is classified as "Commercial Computer Software" under paragraph 252.227-7014
 of the DoD Supplement to the Federal Acquisition Regulations ("DFARS") (or any
 successor regulations) and the Government is acquiring only the license rights
 granted herein (the license rights customarily provided to non-Government
 users).  If the Software is supplied to any unit or agency of the Government
 other than DoD, it is classified as "Restricted Computer Software" and the
 Government's rights in the Software are defined in paragraph 52.227-19 of the
 Federal Acquisition Regulations ("FAR") (or any successor regulations) or, in
 the cases of NASA, in paragraph 18.52.227-86 of the NASA Supplement to the FAR
 (or any successor regulations).

 -----------------------------------------------------------------------------

 Commercial licensing and support of this software is available from OpenSS7
 Corporation at a fee.  See http://www.openss7.com/

 *****************************************************************************/

static char const ident[] = "src/snmp/slMIB.c (" PACKAGE_ENVR ") " PACKAGE_DATE;

/* This file was generated by mib2c and is intended for use as
   a mib module for the ucd-snmp snmpd agent. */
#include <ucd-snmp/ucd-snmp-config.h>
#include <ucd-snmp/ucd-snmp-includes.h>
#include <ucd-snmp/ucd-snmp-agent-includes.h>
#include <ucd-snmp/agent_trap.h>
#include <ucd-snmp/callback.h>
#include <ucd-snmp/snmp-tc.h>
#include <ucd-snmp/default_store.h>
#include <ucd-snmp/snmp_alarm.h>
/* The following header files are mangled in most recent net-snmp releases so
 * the versions from UCD-SNMP 4.2.5 are included here.  */
#if defined HAVE_LIBNETSNMP
#else				/* defined HAVE_LIBNETSNMP */
#endif				/* defined HAVE_LIBNETSNMP */
/* These are messed up on both. */
#include "ds_agent.h"
#ifdef HAVE_UCD_SNMP_UTIL_FUNCS_H
#include <ucd-snmp/util_funcs.h>
/* Many recent net-snmp UCD compatible headers do not declare header_generic. */
int header_generic(struct variable *, oid *, size_t *, int, size_t *, WriteMethod **);
#else				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#include "util_funcs.h"
#endif				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#ifdef HAVE_UCD_SNMP_HEADER_COMPLEX_H
#include <ucd-snmp/header_complex.h>
#else				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
#include "header_complex.h"
#endif				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
/* This one is the other way around: it is just fine for net-snmp, but
 * ucd-snmp does not provide the header file at all.  */
#ifdef HAVE_UCD_SNMP_MIB_MODULES_H
#include <ucd-snmp/mib_modules.h>
#else				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#ifdef HAVE_NET_SNMP_AGENT_MIB_MODULES_H
#include <net-snmp/agent/mib_modules.h>
#else				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#include "mib_modules.h"
#endif				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#endif				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#ifdef HAVE_NETSNMP_MEMDUP
#include "ucd_memdup.h"
#endif
#include <stdint.h>
#include <signal.h>
#include <sys/stat.h>		/* for struct stat, fstat() */
#include <sys/types.h>
#include <unistd.h>
#include <stdlib.h>
#include <pwd.h>		/* for getpwuid() getpwnam() */
#include <grp.h>		/* for getgrgid() getgrnam() */
#include <libgen.h>		/* for basename() */
#include <fcntl.h>		/* for O_CREAT */
#include <sys/sysctl.h>		/* for sysctl */
#include <errno.h>
#include <string.h>
#ifdef _GNU_SOURCE
#include <getopt.h>
#endif
#include "slMIB.h"
#undef MASTER
#define MY_FACILITY(__pri)	(LOG_DAEMON|(__pri))
#if defined MODULE
#if defined MASTER
const char sa_program[] = "slMIB";
int sa_fclose = 1;			/* default close files between requests */
int sa_changed = 1;			/* indication to reread MIB configuration */
int sa_stats_refresh = 1;		/* indications that statistics, the mib or its tables need to be refreshed */
int sa_request = 1;			/* request number for per-request actions */
int sa_dump = 0;			/* default packet dump */
int sa_debug = 0;			/* default no debug */
#endif				/* defined MASTER */
#endif				/* defined MODULE */
static int my_fd = -1;			/* file descriptor for this MIB's use */
static int my_readfd = -1;		/* file descriptor for autonomnous events */
volatile int slMIB_refresh = 1;
volatile int slProfileTable_refresh = 1;
volatile int slTable_refresh = 1;
volatile int slSdlListTable_refresh = 1;
volatile int slSdtListTable_refresh = 1;
volatile int slNbTable_refresh = 1;
volatile int slSaalTable_refresh = 1;
volatile int slM2paTable_refresh = 1;
volatile int slSdtTable_refresh = 1;
volatile int slSdlTable_refresh = 1;

/*
 * slMIB_variables_oid: object identifier for slMIB
 * This is the top level oid that we want to register under.  This is essentially a prefix, with the
 * suffix appearing in the variable below.
 */
oid slMIB_variables_oid[10] = { 1, 3, 6, 1, 4, 1, 29591, 17, 751, 11 };
oid slProfileTable_variables_oid[15] = { 1, 3, 6, 1, 4, 1, 29591, 17, 751, 11, 1, 1, 1, 1, 1 };
oid slTable_variables_oid[15] = { 1, 3, 6, 1, 4, 1, 29591, 17, 751, 11, 1, 1, 1, 2, 1 };
oid slSdlListTable_variables_oid[15] = { 1, 3, 6, 1, 4, 1, 29591, 17, 751, 11, 1, 1, 1, 3, 1 };
oid slSdtListTable_variables_oid[15] = { 1, 3, 6, 1, 4, 1, 29591, 17, 751, 11, 1, 1, 1, 4, 1 };
oid slNbTable_variables_oid[15] = { 1, 3, 6, 1, 4, 1, 29591, 17, 751, 11, 1, 1, 2, 1, 1 };
oid slSaalTable_variables_oid[15] = { 1, 3, 6, 1, 4, 1, 29591, 17, 751, 11, 1, 1, 2, 2, 1 };
oid slM2paTable_variables_oid[15] = { 1, 3, 6, 1, 4, 1, 29591, 17, 751, 11, 1, 1, 2, 3, 1 };
oid slSdtTable_variables_oid[15] = { 1, 3, 6, 1, 4, 1, 29591, 17, 751, 11, 1, 1, 2, 5, 1 };
oid slSdlTable_variables_oid[15] = { 1, 3, 6, 1, 4, 1, 29591, 17, 751, 11, 1, 1, 3, 2, 1 };

/*
 * Oids for use in notifications defined in this MIB.
 */

/*
 * Oids accessible only for notify defined in this MIB.
 */

/*
 * Other oids defined in this MIB.
 */
oid slPackage_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 17, 751, 11, 2, 1, 1 };
oid slGeneralCompliance_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 17, 751, 11, 2, 2, 1 };
static oid zeroDotZero_oid[2] = { 0, 0 };
static oid snmpTrapOID_oid[11] = { 1, 3, 6, 1, 6, 3, 1, 1, 4, 1, 0 };

/*
 * variable7 slMIB_variables: tree for slMIB
 * This variable defines function callbacks and type return information for the slMIB mib section
 */
struct variable7 slMIB_variables[] = {
	/* magic number, variable type, ro/rw, callback fn, L, oidsuffix */
#define   SLPROFILEERRORCORRECTIONMETHOD  1
	{(u_char) SLPROFILEERRORCORRECTIONMETHOD, ASN_INTEGER, RWRITE, var_slProfileTable, 6, {1, 1, 1, 1, 1, 3}},
#define   SLPROFILEPROTOCOLVARIANT  2
	{(u_char) SLPROFILEPROTOCOLVARIANT, ASN_OBJECT_ID, RWRITE, var_slProfileTable, 6, {1, 1, 1, 1, 1, 4}},
#define   SLPROFILETIMERT1      3
	{(u_char) SLPROFILETIMERT1, ASN_INTEGER, RWRITE, var_slProfileTable, 6, {1, 1, 1, 1, 1, 5}},
#define   SLPROFILETIMERT2      4
	{(u_char) SLPROFILETIMERT2, ASN_INTEGER, RWRITE, var_slProfileTable, 6, {1, 1, 1, 1, 1, 6}},
#define   SLPROFILETIMERT2L     5
	{(u_char) SLPROFILETIMERT2L, ASN_INTEGER, RWRITE, var_slProfileTable, 6, {1, 1, 1, 1, 1, 7}},
#define   SLPROFILETIMERT2H     6
	{(u_char) SLPROFILETIMERT2H, ASN_INTEGER, RWRITE, var_slProfileTable, 6, {1, 1, 1, 1, 1, 8}},
#define   SLPROFILETIMERT3      7
	{(u_char) SLPROFILETIMERT3, ASN_INTEGER, RWRITE, var_slProfileTable, 6, {1, 1, 1, 1, 1, 9}},
#define   SLPROFILETIMERT4N     8
	{(u_char) SLPROFILETIMERT4N, ASN_INTEGER, RWRITE, var_slProfileTable, 6, {1, 1, 1, 1, 1, 10}},
#define   SLPROFILETIMERT4E     9
	{(u_char) SLPROFILETIMERT4E, ASN_INTEGER, RWRITE, var_slProfileTable, 6, {1, 1, 1, 1, 1, 11}},
#define   SLPROFILETIMERT5      10
	{(u_char) SLPROFILETIMERT5, ASN_INTEGER, RWRITE, var_slProfileTable, 6, {1, 1, 1, 1, 1, 12}},
#define   SLPROFILETIMERT6      11
	{(u_char) SLPROFILETIMERT6, ASN_INTEGER, RWRITE, var_slProfileTable, 6, {1, 1, 1, 1, 1, 13}},
#define   SLPROFILETIMERT7      12
	{(u_char) SLPROFILETIMERT7, ASN_INTEGER, RWRITE, var_slProfileTable, 6, {1, 1, 1, 1, 1, 14}},
#define   SLPROFILETBONSET1     13
	{(u_char) SLPROFILETBONSET1, ASN_UNSIGNED, RWRITE, var_slProfileTable, 6, {1, 1, 1, 1, 1, 15}},
#define   SLPROFILETBABATE1     14
	{(u_char) SLPROFILETBABATE1, ASN_UNSIGNED, RWRITE, var_slProfileTable, 6, {1, 1, 1, 1, 1, 16}},
#define   SLPROFILENUMBEROFTHRESHOLDLEVELS  15
	{(u_char) SLPROFILENUMBEROFTHRESHOLDLEVELS, ASN_UNSIGNED, RWRITE, var_slProfileTable, 6, {1, 1, 1, 1, 1, 17}},
#define   SLPROFILECONGESTIONCOUNTING  16
	{(u_char) SLPROFILECONGESTIONCOUNTING, ASN_INTEGER, RWRITE, var_slProfileTable, 6, {1, 1, 1, 1, 1, 18}},
#define   SLPROFILECONGESTIONREPBASEOBJECT  17
	{(u_char) SLPROFILECONGESTIONREPBASEOBJECT, ASN_INTEGER, RWRITE, var_slProfileTable, 6, {1, 1, 1, 1, 1, 19}},
#define   SLPROFILELOOPDELAY    18
	{(u_char) SLPROFILELOOPDELAY, ASN_UNSIGNED, RWRITE, var_slProfileTable, 6, {1, 1, 1, 1, 1, 20}},
#define   SLPROFILENAME         19
	{(u_char) SLPROFILENAME, ASN_OCTET_STR, RWRITE, var_slProfileTable, 6, {1, 1, 1, 1, 1, 21}},
#define   SLPROFILETBONSET2     20
	{(u_char) SLPROFILETBONSET2, ASN_UNSIGNED, RWRITE, var_slProfileTable, 6, {1, 1, 1, 1, 1, 22}},
#define   SLPROFILETBABATE2     21
	{(u_char) SLPROFILETBABATE2, ASN_UNSIGNED, RWRITE, var_slProfileTable, 6, {1, 1, 1, 1, 1, 23}},
#define   SLPROFILETBONSET3     22
	{(u_char) SLPROFILETBONSET3, ASN_UNSIGNED, RWRITE, var_slProfileTable, 6, {1, 1, 1, 1, 1, 24}},
#define   SLPROFILETBABATE3     23
	{(u_char) SLPROFILETBABATE3, ASN_UNSIGNED, RWRITE, var_slProfileTable, 6, {1, 1, 1, 1, 1, 25}},
#define   SLPROFILETBDISCARD1   24
	{(u_char) SLPROFILETBDISCARD1, ASN_UNSIGNED, RWRITE, var_slProfileTable, 6, {1, 1, 1, 1, 1, 26}},
#define   SLPROFILETBDISCARD2   25
	{(u_char) SLPROFILETBDISCARD2, ASN_UNSIGNED, RWRITE, var_slProfileTable, 6, {1, 1, 1, 1, 1, 27}},
#define   SLPROFILETBDISCARD3   26
	{(u_char) SLPROFILETBDISCARD3, ASN_UNSIGNED, RWRITE, var_slProfileTable, 6, {1, 1, 1, 1, 1, 28}},
#define   SLPROFILETIMERTX      27
	{(u_char) SLPROFILETIMERTX, ASN_INTEGER, RWRITE, var_slProfileTable, 6, {1, 1, 1, 1, 1, 29}},
#define   SLPROFILETIMERTY      28
	{(u_char) SLPROFILETIMERTY, ASN_INTEGER, RWRITE, var_slProfileTable, 6, {1, 1, 1, 1, 1, 30}},
#define   SLPROFILENUMOFCONGESTIONSTATES  29
	{(u_char) SLPROFILENUMOFCONGESTIONSTATES, ASN_UNSIGNED, RWRITE, var_slProfileTable, 6, {1, 1, 1, 1, 1, 31}},
#define   SLPROFILEINITLEVELOFCONGESTION  30
	{(u_char) SLPROFILEINITLEVELOFCONGESTION, ASN_UNSIGNED, RWRITE, var_slProfileTable, 6, {1, 1, 1, 1, 1, 32}},
#define   SLPROFILEMAXMSUSRETRANSN1  31
	{(u_char) SLPROFILEMAXMSUSRETRANSN1, ASN_UNSIGNED, RWRITE, var_slProfileTable, 6, {1, 1, 1, 1, 1, 33}},
#define   SLPROFILEMAXOCTRETRANSN2  32
	{(u_char) SLPROFILEMAXOCTRETRANSN2, ASN_UNSIGNED, RWRITE, var_slProfileTable, 6, {1, 1, 1, 1, 1, 34}},
#define   SLPROFILERECEIVECONGTHRESHONSET  33
	{(u_char) SLPROFILERECEIVECONGTHRESHONSET, ASN_UNSIGNED, RWRITE, var_slProfileTable, 6, {1, 1, 1, 1, 1, 35}},
#define   SLPROFILERECEIVECONGTHRESHABATE  34
	{(u_char) SLPROFILERECEIVECONGTHRESHABATE, ASN_UNSIGNED, RWRITE, var_slProfileTable, 6, {1, 1, 1, 1, 1, 36}},
#define   SLPROFILERECEIVECONGTHRESHDISC  35
	{(u_char) SLPROFILERECEIVECONGTHRESHDISC, ASN_UNSIGNED, RWRITE, var_slProfileTable, 6, {1, 1, 1, 1, 1, 37}},
#define   SLPROFILEM            36
	{(u_char) SLPROFILEM, ASN_UNSIGNED, RONLY, var_slProfileTable, 6, {1, 1, 1, 1, 1, 38}},
#define   SLPROFILEROWSTATUS    37
	{(u_char) SLPROFILEROWSTATUS, ASN_INTEGER, RWRITE, var_slProfileTable, 6, {1, 1, 1, 1, 1, 39}},
#define   SLMAXCAPACITYSL       38
	{(u_char) SLMAXCAPACITYSL, ASN_UNSIGNED, RWRITE, var_slTable, 6, {1, 1, 1, 2, 1, 2}},
#define   SLPROCEDURALSTATUS    39
	{(u_char) SLPROCEDURALSTATUS, ASN_OCTET_STR, RONLY, var_slTable, 6, {1, 1, 1, 2, 1, 3}},
#define   SLPROFILEPOINTER      40
	{(u_char) SLPROFILEPOINTER, ASN_OCTET_STR, RWRITE, var_slTable, 6, {1, 1, 1, 2, 1, 4}},
#define   SLSIGNTERMPOINTER     41
	{(u_char) SLSIGNTERMPOINTER, ASN_OCTET_STR, RWRITE, var_slTable, 6, {1, 1, 1, 2, 1, 5}},
#define   SLSIGNDATALINKTPPOINTER  42
	{(u_char) SLSIGNDATALINKTPPOINTER, ASN_OCTET_STR, RWRITE, var_slTable, 6, {1, 1, 1, 2, 1, 6}},
#define   SLREPLACEST           43
	{(u_char) SLREPLACEST, ASN_INTEGER, RWRITE, var_slTable, 6, {1, 1, 1, 2, 1, 7}},
#define   SLCONGESTIONLEVEL     44
	{(u_char) SLCONGESTIONLEVEL, ASN_INTEGER, RONLY, var_slTable, 6, {1, 1, 1, 2, 1, 8}},
#define   SLRELATEDLINKGROUPNUMBER  45
	{(u_char) SLRELATEDLINKGROUPNUMBER, ASN_UNSIGNED, RWRITE, var_slTable, 6, {1, 1, 1, 2, 1, 9}},
#define   SLSDLLIST             46
	{(u_char) SLSDLLIST, ASN_UNSIGNED, RWRITE, var_slTable, 6, {1, 1, 1, 2, 1, 10}},
#define   SLNAME                47
	{(u_char) SLNAME, ASN_OCTET_STR, RWRITE, var_slTable, 6, {1, 1, 1, 2, 1, 11}},
#define   SLSDTLIST             48
	{(u_char) SLSDTLIST, ASN_UNSIGNED, RWRITE, var_slTable, 6, {1, 1, 1, 2, 1, 12}},
#define   SLROWSTATUS           49
	{(u_char) SLROWSTATUS, ASN_INTEGER, RWRITE, var_slTable, 6, {1, 1, 1, 2, 1, 13}},
#define   SLSDLLISTPOINTER      50
	{(u_char) SLSDLLISTPOINTER, ASN_OCTET_STR, RWRITE, var_slSdlListTable, 6, {1, 1, 1, 3, 1, 2}},
#define   SLSDLLISTROWSTATUS    51
	{(u_char) SLSDLLISTROWSTATUS, ASN_INTEGER, RWRITE, var_slSdlListTable, 6, {1, 1, 1, 3, 1, 3}},
#define   SLSDTLISTPOINTER      52
	{(u_char) SLSDTLISTPOINTER, ASN_OCTET_STR, RWRITE, var_slSdtListTable, 6, {1, 1, 1, 4, 1, 2}},
#define   SLSDTLISTROWSTATUS    53
	{(u_char) SLSDTLISTROWSTATUS, ASN_INTEGER, RWRITE, var_slSdtListTable, 6, {1, 1, 1, 4, 1, 3}},
#define   SLNBTRANSMISSIONRATE  54
	{(u_char) SLNBTRANSMISSIONRATE, ASN_INTEGER, RWRITE, var_slNbTable, 6, {1, 1, 2, 1, 1, 2}},
#define   SLNBTIMERT8           55
	{(u_char) SLNBTIMERT8, ASN_UNSIGNED, RONLY, var_slNbTable, 6, {1, 1, 2, 1, 1, 3}},
#define   SLNBTIN               56
	{(u_char) SLNBTIN, ASN_UNSIGNED, RONLY, var_slNbTable, 6, {1, 1, 2, 1, 1, 4}},
#define   SLNBTIE               57
	{(u_char) SLNBTIE, ASN_UNSIGNED, RONLY, var_slNbTable, 6, {1, 1, 2, 1, 1, 5}},
#define   SLNBT                 58
	{(u_char) SLNBT, ASN_UNSIGNED, RONLY, var_slNbTable, 6, {1, 1, 2, 1, 1, 6}},
#define   SLNBD                 59
	{(u_char) SLNBD, ASN_UNSIGNED, RONLY, var_slNbTable, 6, {1, 1, 2, 1, 1, 7}},
#define   SLNBTE                60
	{(u_char) SLNBTE, ASN_UNSIGNED, RONLY, var_slNbTable, 6, {1, 1, 2, 1, 1, 8}},
#define   SLNBDE                61
	{(u_char) SLNBDE, ASN_UNSIGNED, RONLY, var_slNbTable, 6, {1, 1, 2, 1, 1, 9}},
#define   SLNBUE                62
	{(u_char) SLNBUE, ASN_UNSIGNED, RONLY, var_slNbTable, 6, {1, 1, 2, 1, 1, 10}},
#define   SLNBN                 63
	{(u_char) SLNBN, ASN_UNSIGNED, RONLY, var_slNbTable, 6, {1, 1, 2, 1, 1, 11}},
#define   SLNBM                 64
	{(u_char) SLNBM, ASN_UNSIGNED, RONLY, var_slNbTable, 6, {1, 1, 2, 1, 1, 12}},
#define   SLNBB                 65
	{(u_char) SLNBB, ASN_UNSIGNED, RONLY, var_slNbTable, 6, {1, 1, 2, 1, 1, 13}},
#define   SLNBF                 66
	{(u_char) SLNBF, ASN_INTEGER, RWRITE, var_slNbTable, 6, {1, 1, 2, 1, 1, 14}},
#define   SLNBROWSTATUS         67
	{(u_char) SLNBROWSTATUS, ASN_INTEGER, RWRITE, var_slNbTable, 6, {1, 1, 2, 1, 1, 15}},
#define   SLSAALBUFFERRELEASE   68
	{(u_char) SLSAALBUFFERRELEASE, ASN_INTEGER, RWRITE, var_slSaalTable, 6, {1, 1, 2, 2, 1, 1}},
#define   SLSAALMAXCC           69
	{(u_char) SLSAALMAXCC, ASN_UNSIGNED, RWRITE, var_slSaalTable, 6, {1, 1, 2, 2, 1, 2}},
#define   SLSAALMAXNRP          70
	{(u_char) SLSAALMAXNRP, ASN_UNSIGNED, RWRITE, var_slSaalTable, 6, {1, 1, 2, 2, 1, 3}},
#define   SLSAALMAXINFORMATIONFIELDLENGTH  71
	{(u_char) SLSAALMAXINFORMATIONFIELDLENGTH, ASN_UNSIGNED, RWRITE, var_slSaalTable, 6, {1, 1, 2, 2, 1, 4}},
#define   SLSAALMAXLENGTHSSCOPUUFIELD  72
	{(u_char) SLSAALMAXLENGTHSSCOPUUFIELD, ASN_UNSIGNED, RWRITE, var_slSaalTable, 6, {1, 1, 2, 2, 1, 5}},
#define   SLSAALMAXPD           73
	{(u_char) SLSAALMAXPD, ASN_UNSIGNED, RWRITE, var_slSaalTable, 6, {1, 1, 2, 2, 1, 6}},
#define   SLSAALMAXSSCOPCREDITTOPEER  74
	{(u_char) SLSAALMAXSSCOPCREDITTOPEER, ASN_UNSIGNED, RWRITE, var_slSaalTable, 6, {1, 1, 2, 2, 1, 7}},
#define   SLSAALMAXSTAT         75
	{(u_char) SLSAALMAXSTAT, ASN_UNSIGNED, RWRITE, var_slSaalTable, 6, {1, 1, 2, 2, 1, 8}},
#define   SLSAALN1              76
	{(u_char) SLSAALN1, ASN_UNSIGNED, RWRITE, var_slSaalTable, 6, {1, 1, 2, 2, 1, 9}},
#define   SLSAALNNILAYERMGRPROVINGSTATE  77
	{(u_char) SLSAALNNILAYERMGRPROVINGSTATE, ASN_INTEGER, RWRITE, var_slSaalTable, 6, {1, 1, 2, 2, 1, 10}},
#define   SLSAALNNILAYERMGRTIMERNOCREDIT  78
	{(u_char) SLSAALNNILAYERMGRTIMERNOCREDIT, ASN_INTEGER, RWRITE, var_slSaalTable, 6, {1, 1, 2, 2, 1, 11}},
#define   SLSAALNNILAYERMGRTIMERREPEATSREC  79
	{(u_char) SLSAALNNILAYERMGRTIMERREPEATSREC, ASN_INTEGER, RWRITE, var_slSaalTable, 6, {1, 1, 2, 2, 1, 12}},
#define   SLSAALNNITIMERT1      80
	{(u_char) SLSAALNNITIMERT1, ASN_INTEGER, RWRITE, var_slSaalTable, 6, {1, 1, 2, 2, 1, 13}},
#define   SLSAALNNITIMERT2      81
	{(u_char) SLSAALNNITIMERT2, ASN_INTEGER, RWRITE, var_slSaalTable, 6, {1, 1, 2, 2, 1, 14}},
#define   SLSAALNNITIMERT3      82
	{(u_char) SLSAALNNITIMERT3, ASN_INTEGER, RWRITE, var_slSaalTable, 6, {1, 1, 2, 2, 1, 15}},
#define   SLSAALSSCOPTIMERCC    83
	{(u_char) SLSAALSSCOPTIMERCC, ASN_INTEGER, RWRITE, var_slSaalTable, 6, {1, 1, 2, 2, 1, 16}},
#define   SLSAALSSCOPTIMERIDLE  84
	{(u_char) SLSAALSSCOPTIMERIDLE, ASN_INTEGER, RWRITE, var_slSaalTable, 6, {1, 1, 2, 2, 1, 17}},
#define   SLSAALSSCOPTIMERKEEPALIVE  85
	{(u_char) SLSAALSSCOPTIMERKEEPALIVE, ASN_INTEGER, RWRITE, var_slSaalTable, 6, {1, 1, 2, 2, 1, 18}},
#define   SLSAALSSCOPTIMERNORESPONSE  86
	{(u_char) SLSAALSSCOPTIMERNORESPONSE, ASN_INTEGER, RWRITE, var_slSaalTable, 6, {1, 1, 2, 2, 1, 19}},
#define   SLSAALSSCOPTIMERPOLL  87
	{(u_char) SLSAALSSCOPTIMERPOLL, ASN_INTEGER, RWRITE, var_slSaalTable, 6, {1, 1, 2, 2, 1, 20}},
#define   SLSAALTRANSRATEINTERVALLOWER  88
	{(u_char) SLSAALTRANSRATEINTERVALLOWER, ASN_UNSIGNED, RWRITE, var_slSaalTable, 6, {1, 1, 2, 2, 1, 21}},
#define   SLSAALTRANSRATEINTERVALUPPER  89
	{(u_char) SLSAALTRANSRATEINTERVALUPPER, ASN_UNSIGNED, RWRITE, var_slSaalTable, 6, {1, 1, 2, 2, 1, 22}},
#define   SLSAALEGRESSTRANSRATEINTVLLOWER  90
	{(u_char) SLSAALEGRESSTRANSRATEINTVLLOWER, ASN_UNSIGNED, RWRITE, var_slSaalTable, 6, {1, 1, 2, 2, 1, 23}},
#define   SLSAALEGRESSTRANSRATEINTVLUPPER  91
	{(u_char) SLSAALEGRESSTRANSRATEINTVLUPPER, ASN_UNSIGNED, RWRITE, var_slSaalTable, 6, {1, 1, 2, 2, 1, 24}},
#define   SLSAALPOLLAFTERRETRANSMISSION  92
	{(u_char) SLSAALPOLLAFTERRETRANSMISSION, ASN_INTEGER, RWRITE, var_slSaalTable, 6, {1, 1, 2, 2, 1, 25}},
#define   SLSAALROWSTATUS       93
	{(u_char) SLSAALROWSTATUS, ASN_INTEGER, RWRITE, var_slSaalTable, 6, {1, 1, 2, 2, 1, 26}},
#define   SLM2PAN1              94
	{(u_char) SLM2PAN1, ASN_UNSIGNED, RWRITE, var_slM2paTable, 6, {1, 1, 2, 3, 1, 1}},
#define   SLM2PAPROVING         95
	{(u_char) SLM2PAPROVING, ASN_INTEGER, RWRITE, var_slM2paTable, 6, {1, 1, 2, 3, 1, 2}},
#define   SLM2PAMANAGEMENTPROVINGSTATE  96
	{(u_char) SLM2PAMANAGEMENTPROVINGSTATE, ASN_INTEGER, RWRITE, var_slM2paTable, 6, {1, 1, 2, 3, 1, 3}},
#define   SLM2PALOOPDELAYLOWER  97
	{(u_char) SLM2PALOOPDELAYLOWER, ASN_UNSIGNED, RWRITE, var_slM2paTable, 6, {1, 1, 2, 3, 1, 4}},
#define   SLM2PALOOPDELAYUPPER  98
	{(u_char) SLM2PALOOPDELAYUPPER, ASN_UNSIGNED, RWRITE, var_slM2paTable, 6, {1, 1, 2, 3, 1, 5}},
#define   SLM2PATRANSRATEINTERVALLOWER  99
	{(u_char) SLM2PATRANSRATEINTERVALLOWER, ASN_UNSIGNED, RWRITE, var_slM2paTable, 6, {1, 1, 2, 3, 1, 6}},
#define   SLM2PATRANSRATEINTERVALUPPER  100
	{(u_char) SLM2PATRANSRATEINTERVALUPPER, ASN_UNSIGNED, RWRITE, var_slM2paTable, 6, {1, 1, 2, 3, 1, 7}},
#define   SLM2PASCTPNODELAY     101
	{(u_char) SLM2PASCTPNODELAY, ASN_INTEGER, RWRITE, var_slM2paTable, 6, {1, 1, 2, 3, 1, 8}},
#define   SLM2PASCTPMAXSEG      102
	{(u_char) SLM2PASCTPMAXSEG, ASN_UNSIGNED, RWRITE, var_slM2paTable, 6, {1, 1, 2, 3, 1, 9}},
#define   SLM2PASCTPHEARTBEATITVL  103
	{(u_char) SLM2PASCTPHEARTBEATITVL, ASN_UNSIGNED, RWRITE, var_slM2paTable, 6, {1, 1, 2, 3, 1, 10}},
#define   SLM2PASCTPHEARTBEAT   104
	{(u_char) SLM2PASCTPHEARTBEAT, ASN_INTEGER, RWRITE, var_slM2paTable, 6, {1, 1, 2, 3, 1, 11}},
#define   SLM2PASCTPRTOINITIAL  105
	{(u_char) SLM2PASCTPRTOINITIAL, ASN_UNSIGNED, RWRITE, var_slM2paTable, 6, {1, 1, 2, 3, 1, 12}},
#define   SLM2PASCTPRTOMIN      106
	{(u_char) SLM2PASCTPRTOMIN, ASN_UNSIGNED, RWRITE, var_slM2paTable, 6, {1, 1, 2, 3, 1, 13}},
#define   SLM2PASCTPRTOMAX      107
	{(u_char) SLM2PASCTPRTOMAX, ASN_UNSIGNED, RWRITE, var_slM2paTable, 6, {1, 1, 2, 3, 1, 14}},
#define   SLM2PASCTPPATHMAXRETRANS  108
	{(u_char) SLM2PASCTPPATHMAXRETRANS, ASN_UNSIGNED, RWRITE, var_slM2paTable, 6, {1, 1, 2, 3, 1, 15}},
#define   SLM2PASCTPCOOKIELIFE  109
	{(u_char) SLM2PASCTPCOOKIELIFE, ASN_UNSIGNED, RWRITE, var_slM2paTable, 6, {1, 1, 2, 3, 1, 16}},
#define   SLM2PASCTPCOOKIEINC   110
	{(u_char) SLM2PASCTPCOOKIEINC, ASN_UNSIGNED, RWRITE, var_slM2paTable, 6, {1, 1, 2, 3, 1, 17}},
#define   SLM2PASCTPMAXINITRETRIES  111
	{(u_char) SLM2PASCTPMAXINITRETRIES, ASN_UNSIGNED, RWRITE, var_slM2paTable, 6, {1, 1, 2, 3, 1, 18}},
#define   SLM2PASCTPMAXBURST    112
	{(u_char) SLM2PASCTPMAXBURST, ASN_UNSIGNED, RWRITE, var_slM2paTable, 6, {1, 1, 2, 3, 1, 19}},
#define   SLM2PASCTPASSOCMAXRETRANS  113
	{(u_char) SLM2PASCTPASSOCMAXRETRANS, ASN_UNSIGNED, RWRITE, var_slM2paTable, 6, {1, 1, 2, 3, 1, 20}},
#define   SLM2PASCTPSACKDELAY   114
	{(u_char) SLM2PASCTPSACKDELAY, ASN_UNSIGNED, RWRITE, var_slM2paTable, 6, {1, 1, 2, 3, 1, 21}},
#define   SLM2PASCTPLIFETIME    115
	{(u_char) SLM2PASCTPLIFETIME, ASN_UNSIGNED, RWRITE, var_slM2paTable, 6, {1, 1, 2, 3, 1, 22}},
#define   SLM2PAPROVINGATTEMPTS  116
	{(u_char) SLM2PAPROVINGATTEMPTS, ASN_UNSIGNED, RWRITE, var_slM2paTable, 6, {1, 1, 2, 3, 1, 23}},
#define   SLM2PAROWSTATUS       117
	{(u_char) SLM2PAROWSTATUS, ASN_INTEGER, RWRITE, var_slM2paTable, 6, {1, 1, 2, 3, 1, 24}},
#define   SLSDTNEXTINDEX        118
	{(u_char) SLSDTNEXTINDEX, ASN_UNSIGNED, RONLY, var_slMIB, 4, {1, 1, 2, 4}},
#define   SLSDTADMINISTRATIVESTATE  119
	{(u_char) SLSDTADMINISTRATIVESTATE, ASN_INTEGER, RWRITE, var_slSdtTable, 6, {1, 1, 2, 5, 1, 2}},
#define   SLSDTOPERATIONALSTATE  120
	{(u_char) SLSDTOPERATIONALSTATE, ASN_INTEGER, RONLY, var_slSdtTable, 6, {1, 1, 2, 5, 1, 3}},
#define   SLSDTUSAGESTATE       121
	{(u_char) SLSDTUSAGESTATE, ASN_INTEGER, RONLY, var_slSdtTable, 6, {1, 1, 2, 5, 1, 4}},
#define   SLSDTPROCEDURALSTATUS  122
	{(u_char) SLSDTPROCEDURALSTATUS, ASN_OCTET_STR, RONLY, var_slSdtTable, 6, {1, 1, 2, 5, 1, 5}},
#define   SLSDTAVAILABILITYSTATUS  123
	{(u_char) SLSDTAVAILABILITYSTATUS, ASN_OCTET_STR, RONLY, var_slSdtTable, 6, {1, 1, 2, 5, 1, 6}},
#define   SLSDTEQUIPMENTPOINTER  124
	{(u_char) SLSDTEQUIPMENTPOINTER, ASN_OBJECT_ID, RWRITE, var_slSdtTable, 6, {1, 1, 2, 5, 1, 7}},
#define   SLSDTNAME             125
	{(u_char) SLSDTNAME, ASN_OCTET_STR, RWRITE, var_slSdtTable, 6, {1, 1, 2, 5, 1, 8}},
#define   SLSDTPROFILEPOINTER   126
	{(u_char) SLSDTPROFILEPOINTER, ASN_OCTET_STR, RWRITE, var_slSdtTable, 6, {1, 1, 2, 5, 1, 9}},
#define   SLSDTROWSTATUS        127
	{(u_char) SLSDTROWSTATUS, ASN_INTEGER, RWRITE, var_slSdtTable, 6, {1, 1, 2, 5, 1, 10}},
#define   SLSDLNEXTINDEX        128
	{(u_char) SLSDLNEXTINDEX, ASN_UNSIGNED, RONLY, var_slMIB, 4, {1, 1, 3, 1}},
#define   SLSDLADJPC            129
	{(u_char) SLSDLADJPC, ASN_OCTET_STR, RWRITE, var_slSdlTable, 6, {1, 1, 3, 2, 1, 2}},
#define   SLSDLTRANSMISSIONRATE  130
	{(u_char) SLSDLTRANSMISSIONRATE, ASN_INTEGER, RWRITE, var_slSdlTable, 6, {1, 1, 3, 2, 1, 3}},
#define   SLSDLLOOPDELAY        131
	{(u_char) SLSDLLOOPDELAY, ASN_UNSIGNED, RWRITE, var_slSdlTable, 6, {1, 1, 3, 2, 1, 4}},
#define   SLSDLOPERATIONALSTATE  132
	{(u_char) SLSDLOPERATIONALSTATE, ASN_INTEGER, RONLY, var_slSdlTable, 6, {1, 1, 3, 2, 1, 5}},
#define   SLSDLEQUIPMENTPOINTER  133
	{(u_char) SLSDLEQUIPMENTPOINTER, ASN_OBJECT_ID, RWRITE, var_slSdlTable, 6, {1, 1, 3, 2, 1, 6}},
#define   SLSDLCIC              134
	{(u_char) SLSDLCIC, ASN_INTEGER, RWRITE, var_slSdlTable, 6, {1, 1, 3, 2, 1, 7}},
#define   SLSDLNAME             135
	{(u_char) SLSDLNAME, ASN_OCTET_STR, RWRITE, var_slSdlTable, 6, {1, 1, 3, 2, 1, 8}},
#define   SLSDLSTMCHANNEL       136
	{(u_char) SLSDLSTMCHANNEL, ASN_OCTET_STR, RWRITE, var_slSdlTable, 6, {1, 1, 3, 2, 1, 9}},
#define   SLSDLVCTTPPOINTER     137
	{(u_char) SLSDLVCTTPPOINTER, ASN_OBJECT_ID, RWRITE, var_slSdlTable, 6, {1, 1, 3, 2, 1, 10}},
#define   SLSDLROWSTATUS        138
	{(u_char) SLSDLROWSTATUS, ASN_INTEGER, RWRITE, var_slSdlTable, 6, {1, 1, 3, 2, 1, 11}},
};

/* (L = length of the oidsuffix) */
struct slMIB_data *slMIBStorage = NULL;

/* global storage of our data, saved in and configured by header_complex() */
struct header_complex_index *slProfileTableStorage = NULL;
struct header_complex_index *slTableStorage = NULL;
struct header_complex_index *slSdlListTableStorage = NULL;
struct header_complex_index *slSdtListTableStorage = NULL;
struct header_complex_index *slNbTableStorage = NULL;
struct header_complex_index *slSaalTableStorage = NULL;
struct header_complex_index *slM2paTableStorage = NULL;
struct header_complex_index *slSdtTableStorage = NULL;
struct header_complex_index *slSdlTableStorage = NULL;

void (*slMIBold_signal_handler) (int) = NULL;	/* save old signal handler just in case */
void slMIB_loop_handler(int);
void slMIB_fd_handler(int, void *);

/**
 * @fn void init_slMIB(void)
 * @brief slMIB initialization routine.
 *
 * This is called when the agent starts up.  At a minimum, registration of the MIB variables
 * structure (slMIB_variables) should take place here.  By default the function also
 * registers the configuration handler and configuration store callbacks.
 *
 * Additional registrations that may be considered here are calls to regsiter_readfd(),
 * register_writefd() and register_exceptfd() for hooking into the snmpd event loop, but only when
 * used as a loadable module.  By default this function establishes a single file descriptor to
 * read, or upon which to handle exceptions.  Note that the snmpd only supports a maximum of 32
 * extneral file descriptors, so these should be used sparingly.
 *
 * When running as a loadable module, it is also necessary to hook into the snmpd event loop so that
 * the current request number can be deteremined.  This is accomplished by using a trick of the
 * external_signal_scheduled and external_signal_handler mechanism which is called on each event
 * loop when external_signal_scheduled is non-zero.  This is used to increment the sa_request value
 * on each snmpd event loop interation so that calls to MIB tree functions can determine whether
 * they belong to a fresh request or not (primarily for cacheing and possibly to clean up non-polled
 * file descriptors).
 */
void
init_slMIB(void)
{
	(void) my_fd;
	(void) zeroDotZero_oid;
	(void) snmpTrapOID_oid;
	DEBUGMSGTL(("slMIB", "init_slMIB: initializing...  "));
	/* register ourselves with the agent to handle our mib tree */
	REGISTER_MIB("slMIB", slMIB_variables, variable7, slMIB_variables_oid);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_SHUTDOWN, term_slMIB, NULL);
	/* register our config handler(s) to deal with registrations */
	snmpd_register_config_handler("slMIB", parse_slMIB, NULL, "HELP STRING");
	snmpd_register_config_handler("slProfileTable", parse_slProfileTable, NULL, "HELP STRING");
	snmpd_register_config_handler("slTable", parse_slTable, NULL, "HELP STRING");
	snmpd_register_config_handler("slSdlListTable", parse_slSdlListTable, NULL, "HELP STRING");
	snmpd_register_config_handler("slSdtListTable", parse_slSdtListTable, NULL, "HELP STRING");
	snmpd_register_config_handler("slNbTable", parse_slNbTable, NULL, "HELP STRING");
	snmpd_register_config_handler("slSaalTable", parse_slSaalTable, NULL, "HELP STRING");
	snmpd_register_config_handler("slM2paTable", parse_slM2paTable, NULL, "HELP STRING");
	snmpd_register_config_handler("slSdtTable", parse_slSdtTable, NULL, "HELP STRING");
	snmpd_register_config_handler("slSdlTable", parse_slSdlTable, NULL, "HELP STRING");

	/* we need to be called back later to store our data */
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_slMIB, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_slProfileTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_slTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_slSdlListTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_slSdtListTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_slNbTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_slSaalTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_slM2paTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_slSdtTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_slSdlTable, NULL);

	/* place any other initialization junk you need here */
	if (my_readfd >= 0) {
		register_readfd(my_readfd, slMIB_fd_handler, (void *) 0);
		register_exceptfd(my_readfd, slMIB_fd_handler, (void *) 1);
	}
	slMIBold_signal_handler = external_signal_handler[SIGCHLD];
	external_signal_handler[SIGCHLD] = &slMIB_loop_handler;
	external_signal_scheduled[SIGCHLD] = 1;
	DEBUGMSGTL(("slMIB", "done.\n"));
}

/**
 * @fn void deinit_slMIB(void)
 * @brief deinitialization routine.
 *
 * This is called before the agent is unloaded.  At a minimum, deregistration of the MIB variables
 * structure (slMIB_variables) should take place here.  By default, the function also
 * deregisters the the configuration file handlers for the MIB variables and table rows.
 *
 * Additional deregistrations that may be required here are calls to unregister_readfd(),
 * unregister_writefd() and unregsiter_exceptfd() for unhooking from the snmpd event loop, but only
 * when used as a loadable module.  By default if a read file descriptor exists, it is unregistered.
 */
void
deinit_slMIB(void)
{
	DEBUGMSGTL(("slMIB", "deinit_slMIB: deinitializating...  "));
	external_signal_handler[SIGCHLD] = slMIBold_signal_handler;
	if (my_readfd >= 0) {
		unregister_exceptfd(my_readfd);
		unregister_readfd(my_readfd);
		close(my_readfd);
		my_readfd = -1;
	}
	unregister_mib(slMIB_variables_oid, sizeof(slMIB_variables_oid) / sizeof(oid));
	snmpd_unregister_config_handler("slMIB");
	snmpd_unregister_config_handler("slProfileTable");
	snmpd_unregister_config_handler("slTable");
	snmpd_unregister_config_handler("slSdlListTable");
	snmpd_unregister_config_handler("slSdtListTable");
	snmpd_unregister_config_handler("slNbTable");
	snmpd_unregister_config_handler("slSaalTable");
	snmpd_unregister_config_handler("slM2paTable");
	snmpd_unregister_config_handler("slSdtTable");
	snmpd_unregister_config_handler("slSdlTable");

	/* place any other de-initialization junk you need here */
	DEBUGMSGTL(("slMIB", "done.\n"));
}

int
term_slMIB(int majorID, int minorID, void *serverarg, void *clientarg)
{
	DEBUGMSGTL(("slMIB", "term_slMIB: terminating...  "));
	deinit_slMIB();
	DEBUGMSGTL(("slMIB", "done.\n"));
	return 0;
}

/**
 * @fn struct slMIB_data *slMIB_create(void)
 * @brief create a fresh data structure representing scalars in slMIB.
 *
 * Creates a new slMIB_data structure by allocating dynamic memory for the structure and
 * initializing the default values of scalars in slMIB.
 */
struct slMIB_data *
slMIB_create(void)
{
	struct slMIB_data *StorageNew = SNMP_MALLOC_STRUCT(slMIB_data);

	DEBUGMSGTL(("slMIB", "slMIB_create: creating scalars...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default scalar values here into StorageNew */
		StorageNew->slSdtNextIndex = 0;
		StorageNew->slSdlNextIndex = 0;

	}
      done:
	DEBUGMSGTL(("slMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	slMIB_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct slMIB_data *slMIB_duplicate(struct slMIB_data *thedata)
 * @param thedata the mib structure to duplicate
 * @brief duplicate a mib structure for the mib
 *
 * Duplicates the specified mib structure @param thedata and returns a pointer to the newly
 * allocated mib structure on success, or NULL on failure.
 */
struct slMIB_data *
slMIB_duplicate(struct slMIB_data *thedata)
{
	struct slMIB_data *StorageNew = SNMP_MALLOC_STRUCT(slMIB_data);

	DEBUGMSGTL(("slMIB", "slMIB_duplicate: duplicating mib... "));
	if (StorageNew != NULL) {
		StorageNew->slSdtNextIndex = thedata->slSdtNextIndex;
		StorageNew->slSdlNextIndex = thedata->slSdlNextIndex;
	}
      done:
	DEBUGMSGTL(("slMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	slMIB_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int slMIB_destroy(struct slMIB_data **thedata)
 * @param thedata pointer to the data structure in slMIB.
 * @brief delete a scalars structure from slMIB.
 *
 * Frees scalars that were previously removed from slMIB.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
slMIB_destroy(struct slMIB_data **thedata)
{
	struct slMIB_data *StorageDel;

	DEBUGMSGTL(("slMIB", "slMIB_destroy: deleting scalars...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("slMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int slMIB_add(struct slMIB_data *thedata)
 * @param thedata the structure representing slMIB scalars.
 * @brief adds node to the slMIB scalar data set.
 *
 * Adds a scalar structure to the slMIB data set.  Note that this function is necessary even
 * when the scalar values are not peristent.
 */
int
slMIB_add(struct slMIB_data *thedata)
{
	DEBUGMSGTL(("slMIB", "slMIB_add: adding data...  "));
	if (thedata)
		slMIBStorage = thedata;
	DEBUGMSGTL(("slMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_slMIB(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for slMIB entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case slMIB).  This routine is invoked by
 * UCD-SNMP to read the values of scalars in the MIB from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the MIB.  If there are no configured entries
 * in the configuration MIB, this function will simply not be called.
 */
void
parse_slMIB(const char *token, char *line)
{
	size_t tmpsize;
	struct slMIB_data *StorageTmp = slMIB_create();

	DEBUGMSGTL(("slMIB", "parse_slMIB: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual scalars that are not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slSdtNextIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slSdlNextIndex, &tmpsize);
	slMIB_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("slMIB", "done.\n"));
}

/*
 * store_slMIB(): stores .conf file entries needed to configure the mib.
 */
int
store_slMIB(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct slMIB_data *StorageTmp;

	DEBUGMSGTL(("slMIB", "store_slMIB: storing data...  "));
	refresh_slMIB(1);
	if ((StorageTmp = slMIBStorage) == NULL) {
		DEBUGMSGTL(("slMIB", "error.\n"));
		return SNMPERR_GENERR;
	}
	(void) tmpsize;
	/* XXX: comment entire section if no scalars are persistent */
	{
		memset(line, 0, sizeof(line));
		strcat(line, "slMIB ");
		cptr = line + strlen(line);
		(void) cptr;
		/* XXX: remove individual scalars that are not persistent */
		cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slSdtNextIndex, &tmpsize);
		cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slSdlNextIndex, &tmpsize);
		snmpd_store_config(line);
	}
	DEBUGMSGTL(("slMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int check_slMIB(struct slMIB_data *StorageTmp, struct slMIB_data *StorageOld)
 * @param StorageTmp the data as updated
 * @param StorageOld the data previous to update
 *
 * This function is used by mibs.  It is used to check, all scalars at a time, the varbinds
 * belonging to the mib.  This function is called for the first varbind in a mib at the beginning of
 * the ACTION phase.  The COMMIT phase does not ensue unless this check passes.  This function can
 * return SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before
 * the varbinds on the mib were applied; the values in StorageTmp are the new values.  The function
 * is permitted to change the values in StorageTmp to correct them; however, preferences should be
 * made for setting values that were not in the varbinds.
 */
int
check_slMIB(struct slMIB_data *StorageTmp, struct slMIB_data *StorageOld)
{
	/* XXX: provide code to check the scalars for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_slMIB(struct slMIB_data *StorageTmp, struct slMIB_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase)
 *
 * This function is used by mibs.  It is used to update, all scalars at a time, the varbinds
 * belonging to the mib.  This function is called for the first varbind in a mib at the beginning of
 * the COMMIT phase.  The start of the ACTION phase performs a consistency check on the mib before
 * allowing the request to proceed to the COMMIT phase.  The COMMIT phase then arrives here with
 * consistency already checked (see check_slMIB()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the mib were applied: the values in StorageTmp are the new values.
 */
int
update_slMIB(struct slMIB_data *StorageTmp, struct slMIB_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	slMIB_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn revert_slMIB(struct 
 * @fn void revert_slMIB(struct slMIB_data *StorageTmp, struct slMIB_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase)
 */
void
revert_slMIB(struct slMIB_data *StorageTmp, struct slMIB_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_slMIB(StorageOld, NULL);
}

/**
 * @fn void refresh_slMIB(int force)
 * @param force forced refresh when non-zero.
 * @brief refresh the scalar values of slMIB.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a scalar has been requested).
 */
void
refresh_slMIB(int force)
{
	if (slMIBStorage == NULL) {
		struct slMIB_data *StorageNew;

		if ((StorageNew = slMIB_create()) == NULL)
			return;
		slMIBStorage = StorageNew;
		slMIB_refresh = 1;
	}
	if (!force && slMIB_refresh == 0)
		return;
	DEBUGMSGTL(("slMIB", "refresh_slMIB: refreshing...  "));
	/* XXX: Update scalars as required here... */
	slMIB_refresh = 0;
	DEBUGMSGTL(("slMIB", "done.\n"));
}

/**
 * @fn u_char * var_slMIB(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @param vp a pointer to the entry in the variables table for the requested variable.
 * @param name the object identifier for which to find.
 * @param length the length of the object identifier.
 * @param exact whether the name is exact.
 * @param var_len a pointer to the length of the representation of the object.
 * @param write_method a pointer to a write method for the object.
 * @brief locate variables in slMIB.
 *
 * This function returns a pointer to a memory area that is static across the request that contains
 * the UCD-SNMP representation of the scalar (so that it may be used to read from for a GET,
 * GET-NEXT or GET-BULK request).  This returned pointer may be NULL, in which case the function is
 * telling UCD-SNMP that the scalar does not exist for reading; however, if write_method is
 * overwritten with a non-NULL value, the function is telling UCD-SNMP that the scalar exists for
 * writing.  Write-only objects can be effected in this way.
 */
u_char *
var_slMIB(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct slMIB_data *StorageTmp;
	u_char *rval;

	DEBUGMSGTL(("slMIB", "var_slMIB: lookup up varbind...  "));
	if (header_generic(vp, name, length, exact, var_len, write_method) == MATCH_FAILED)
		return NULL;
	/* Refresh the MIB values if required. */
	refresh_slMIB(0);
	if ((StorageTmp = slMIBStorage) == NULL) {
		DEBUGMSGTL(("slMIB", "no datastructure.\n"));
		return NULL;
	}
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) SLSDTNEXTINDEX:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slSdtNextIndex);
		rval = (u_char *) &StorageTmp->slSdtNextIndex;
		break;
	case (u_char) SLSDLNEXTINDEX:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slSdlNextIndex);
		rval = (u_char *) &StorageTmp->slSdlNextIndex;
		break;
	default:
		ERROR_MSG("");
	}
	if (rval)
		DEBUGMSGTL(("slMIB", "found.\n"));
	else
		DEBUGMSGTL(("slMIB", "not found.\n"));
	return (rval);
}

/**
 * @fn struct slProfileTable_data *slProfileTable_create(void)
 * @brief create a fresh data structure representing a new row in the slProfileTable table.
 *
 * Creates a new slProfileTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct slProfileTable_data *
slProfileTable_create(void)
{
	struct slProfileTable_data *StorageNew = SNMP_MALLOC_STRUCT(slProfileTable_data);

	DEBUGMSGTL(("slMIB", "slProfileTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->slProfileErrorCorrectionMethod = SLPROFILEERRORCORRECTIONMETHOD_BASICRETR;
		if ((StorageNew->slProfileProtocolVariant = snmp_duplicate_objid(zeroDotZero_oid, 2)) == NULL)
			goto nomem;
		StorageNew->slProfileProtocolVariantLen = 2;
		StorageNew->slProfileTimerT1 = 4500;
		StorageNew->slProfileTimerT2 = 500;
		StorageNew->slProfileTimerT2L = 2000;
		StorageNew->slProfileTimerT2H = 10000;
		StorageNew->slProfileTimerT3 = 100;
		StorageNew->slProfileTimerT4N = 800;
		StorageNew->slProfileTimerT4E = 50;
		StorageNew->slProfileTimerT5 = 10;
		StorageNew->slProfileTimerT6 = 400;
		StorageNew->slProfileTimerT7 = 100;
		StorageNew->slProfileTbOnset1 = 69632;
		StorageNew->slProfileTbAbate1 = 34816;
		StorageNew->slProfileNumberOfThresholdLevels = 1;
		StorageNew->slProfileCongestionCounting = SLPROFILECONGESTIONCOUNTING_OCTET;
		StorageNew->slProfileCongestionRepBaseObject = SLPROFILECONGESTIONREPBASEOBJECT_ROUTESET;
		StorageNew->slProfileLoopDelay = 0;
		if ((StorageNew->slProfileName = malloc(1)) == NULL)
			goto nomem;
		StorageNew->slProfileNameLen = 0;
		StorageNew->slProfileName[StorageNew->slProfileNameLen] = 0;
		StorageNew->slProfileTbOnset2 = 174080;
		StorageNew->slProfileTbAbate2 = 139264;
		StorageNew->slProfileTbOnset3 = 278528;
		StorageNew->slProfileTbAbate3 = 243712;
		StorageNew->slProfileTbDiscard1 = 104448;
		StorageNew->slProfileTbDiscard2 = 208896;
		StorageNew->slProfileTbDiscard3 = 313344;
		StorageNew->slProfileTimerTx = 0;
		StorageNew->slProfileTimerTy = 0;
		StorageNew->slProfileNumOfCongestionStates = 0;
		StorageNew->slProfileInitLevelOfCongestion = 0;
		StorageNew->slProfileMaxMSUsRetransN1 = 127;
		StorageNew->slProfileMaxOctRetransN2 = 8192;
		StorageNew->slProfileReceiveCongThreshOnset = 6;
		StorageNew->slProfileReceiveCongThreshAbate = 3;
		StorageNew->slProfileReceiveCongThreshDisc = 9;
		StorageNew->slProfileM = 5;
		StorageNew->slProfileRowStatus = 0;
		StorageNew->slProfileRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("slMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	slProfileTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct slProfileTable_data *slProfileTable_duplicate(struct slProfileTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct slProfileTable_data *
slProfileTable_duplicate(struct slProfileTable_data *thedata)
{
	struct slProfileTable_data *StorageNew = SNMP_MALLOC_STRUCT(slProfileTable_data);

	DEBUGMSGTL(("slMIB", "slProfileTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->slProfileTable_id = thedata->slProfileTable_id;
		if (!(StorageNew->slProfileId = malloc(thedata->slProfileIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->slProfileId, thedata->slProfileId, thedata->slProfileIdLen);
		StorageNew->slProfileIdLen = thedata->slProfileIdLen;
		StorageNew->slProfileId[StorageNew->slProfileIdLen] = 0;
		StorageNew->slProfileTransmissionRate = thedata->slProfileTransmissionRate;
		StorageNew->slProfileErrorCorrectionMethod = thedata->slProfileErrorCorrectionMethod;
		if (!(StorageNew->slProfileProtocolVariant = snmp_duplicate_objid(thedata->slProfileProtocolVariant, thedata->slProfileProtocolVariantLen / sizeof(oid))))
			goto destroy;
		StorageNew->slProfileProtocolVariantLen = thedata->slProfileProtocolVariantLen;
		StorageNew->slProfileTimerT1 = thedata->slProfileTimerT1;
		StorageNew->slProfileTimerT2 = thedata->slProfileTimerT2;
		StorageNew->slProfileTimerT2L = thedata->slProfileTimerT2L;
		StorageNew->slProfileTimerT2H = thedata->slProfileTimerT2H;
		StorageNew->slProfileTimerT3 = thedata->slProfileTimerT3;
		StorageNew->slProfileTimerT4N = thedata->slProfileTimerT4N;
		StorageNew->slProfileTimerT4E = thedata->slProfileTimerT4E;
		StorageNew->slProfileTimerT5 = thedata->slProfileTimerT5;
		StorageNew->slProfileTimerT6 = thedata->slProfileTimerT6;
		StorageNew->slProfileTimerT7 = thedata->slProfileTimerT7;
		StorageNew->slProfileTbOnset1 = thedata->slProfileTbOnset1;
		StorageNew->slProfileTbAbate1 = thedata->slProfileTbAbate1;
		StorageNew->slProfileNumberOfThresholdLevels = thedata->slProfileNumberOfThresholdLevels;
		StorageNew->slProfileCongestionCounting = thedata->slProfileCongestionCounting;
		StorageNew->slProfileCongestionRepBaseObject = thedata->slProfileCongestionRepBaseObject;
		StorageNew->slProfileLoopDelay = thedata->slProfileLoopDelay;
		if (!(StorageNew->slProfileName = malloc(thedata->slProfileNameLen + 1)))
			goto destroy;
		memcpy(StorageNew->slProfileName, thedata->slProfileName, thedata->slProfileNameLen);
		StorageNew->slProfileNameLen = thedata->slProfileNameLen;
		StorageNew->slProfileName[StorageNew->slProfileNameLen] = 0;
		StorageNew->slProfileTbOnset2 = thedata->slProfileTbOnset2;
		StorageNew->slProfileTbAbate2 = thedata->slProfileTbAbate2;
		StorageNew->slProfileTbOnset3 = thedata->slProfileTbOnset3;
		StorageNew->slProfileTbAbate3 = thedata->slProfileTbAbate3;
		StorageNew->slProfileTbDiscard1 = thedata->slProfileTbDiscard1;
		StorageNew->slProfileTbDiscard2 = thedata->slProfileTbDiscard2;
		StorageNew->slProfileTbDiscard3 = thedata->slProfileTbDiscard3;
		StorageNew->slProfileTimerTx = thedata->slProfileTimerTx;
		StorageNew->slProfileTimerTy = thedata->slProfileTimerTy;
		StorageNew->slProfileNumOfCongestionStates = thedata->slProfileNumOfCongestionStates;
		StorageNew->slProfileInitLevelOfCongestion = thedata->slProfileInitLevelOfCongestion;
		StorageNew->slProfileMaxMSUsRetransN1 = thedata->slProfileMaxMSUsRetransN1;
		StorageNew->slProfileMaxOctRetransN2 = thedata->slProfileMaxOctRetransN2;
		StorageNew->slProfileReceiveCongThreshOnset = thedata->slProfileReceiveCongThreshOnset;
		StorageNew->slProfileReceiveCongThreshAbate = thedata->slProfileReceiveCongThreshAbate;
		StorageNew->slProfileReceiveCongThreshDisc = thedata->slProfileReceiveCongThreshDisc;
		StorageNew->slProfileM = thedata->slProfileM;
		StorageNew->slProfileRowStatus = thedata->slProfileRowStatus;
	}
      done:
	DEBUGMSGTL(("slMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	slProfileTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int slProfileTable_destroy(struct slProfileTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
slProfileTable_destroy(struct slProfileTable_data **thedata)
{
	struct slProfileTable_data *StorageDel;

	DEBUGMSGTL(("slMIB", "slProfileTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->slProfileId);
		StorageDel->slProfileIdLen = 0;
		SNMP_FREE(StorageDel->slProfileProtocolVariant);
		StorageDel->slProfileProtocolVariantLen = 0;
		SNMP_FREE(StorageDel->slProfileName);
		StorageDel->slProfileNameLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("slMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int slProfileTable_add(struct slProfileTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the slProfileTable table data set.
 *
 * Adds a table row structure to the slProfileTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
slProfileTable_add(struct slProfileTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("slMIB", "slProfileTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* slProfileId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->slProfileId, thedata->slProfileIdLen);
		/* slProfileTransmissionRate */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, (u_char *) &thedata->slProfileTransmissionRate, sizeof(thedata->slProfileTransmissionRate));
		header_complex_add_data(&slProfileTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("slMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int slProfileTable_del(struct slProfileTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the slProfileTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
slProfileTable_del(struct slProfileTable_data *thedata)
{
	struct slProfileTable_data *StorageDel;

	DEBUGMSGTL(("slMIB", "slProfileTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(slProfileTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&slProfileTableStorage, hciptr);
	}
	DEBUGMSGTL(("slMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_slProfileTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for slProfileTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case slProfileTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_slProfileTable(const char *token, char *line)
{
	size_t tmpsize;
	struct slProfileTable_data *StorageTmp = slProfileTable_create();

	DEBUGMSGTL(("slMIB", "parse_slProfileTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->slProfileId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->slProfileId, &StorageTmp->slProfileIdLen);
	if (StorageTmp->slProfileId == NULL) {
		config_perror("invalid specification for slProfileId");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->slProfileTransmissionRate, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->slProfileErrorCorrectionMethod, &tmpsize);
	SNMP_FREE(StorageTmp->slProfileProtocolVariant);
	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->slProfileProtocolVariant, &StorageTmp->slProfileProtocolVariantLen);
	if (StorageTmp->slProfileProtocolVariant == NULL) {
		config_perror("invalid specification for slProfileProtocolVariant");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->slProfileTimerT1, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->slProfileTimerT2, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->slProfileTimerT2L, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->slProfileTimerT2H, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->slProfileTimerT3, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->slProfileTimerT4N, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->slProfileTimerT4E, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->slProfileTimerT5, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->slProfileTimerT6, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->slProfileTimerT7, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slProfileTbOnset1, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slProfileTbAbate1, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slProfileNumberOfThresholdLevels, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->slProfileCongestionCounting, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->slProfileCongestionRepBaseObject, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slProfileLoopDelay, &tmpsize);
	SNMP_FREE(StorageTmp->slProfileName);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->slProfileName, &StorageTmp->slProfileNameLen);
	if (StorageTmp->slProfileName == NULL) {
		config_perror("invalid specification for slProfileName");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slProfileTbOnset2, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slProfileTbAbate2, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slProfileTbOnset3, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slProfileTbAbate3, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slProfileTbDiscard1, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slProfileTbDiscard2, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slProfileTbDiscard3, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->slProfileTimerTx, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->slProfileTimerTy, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slProfileNumOfCongestionStates, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slProfileInitLevelOfCongestion, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slProfileMaxMSUsRetransN1, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slProfileMaxOctRetransN2, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slProfileReceiveCongThreshOnset, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slProfileReceiveCongThreshAbate, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slProfileReceiveCongThreshDisc, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slProfileM, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->slProfileRowStatus, &tmpsize);
	slProfileTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("slMIB", "done.\n"));
}

/*
 * store_slProfileTable(): store configuraiton file for slProfileTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_slProfileTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct slProfileTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("slMIB", "store_slProfileTable: storing data...  "));
	refresh_slProfileTable(1);
	(void) tmpsize;
	for (hcindex = slProfileTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct slProfileTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "slProfileTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->slProfileId, &StorageTmp->slProfileIdLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->slProfileTransmissionRate, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->slProfileErrorCorrectionMethod, &tmpsize);
			cptr = read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->slProfileProtocolVariant, &StorageTmp->slProfileProtocolVariantLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->slProfileTimerT1, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->slProfileTimerT2, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->slProfileTimerT2L, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->slProfileTimerT2H, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->slProfileTimerT3, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->slProfileTimerT4N, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->slProfileTimerT4E, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->slProfileTimerT5, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->slProfileTimerT6, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->slProfileTimerT7, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slProfileTbOnset1, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slProfileTbAbate1, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slProfileNumberOfThresholdLevels, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->slProfileCongestionCounting, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->slProfileCongestionRepBaseObject, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slProfileLoopDelay, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->slProfileName, &StorageTmp->slProfileNameLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slProfileTbOnset2, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slProfileTbAbate2, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slProfileTbOnset3, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slProfileTbAbate3, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slProfileTbDiscard1, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slProfileTbDiscard2, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slProfileTbDiscard3, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->slProfileTimerTx, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->slProfileTimerTy, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slProfileNumOfCongestionStates, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slProfileInitLevelOfCongestion, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slProfileMaxMSUsRetransN1, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slProfileMaxOctRetransN2, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slProfileReceiveCongThreshOnset, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slProfileReceiveCongThreshAbate, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slProfileReceiveCongThreshDisc, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slProfileM, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->slProfileRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("slMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct slTable_data *slTable_create(void)
 * @brief create a fresh data structure representing a new row in the slTable table.
 *
 * Creates a new slTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct slTable_data *
slTable_create(void)
{
	struct slTable_data *StorageNew = SNMP_MALLOC_STRUCT(slTable_data);

	DEBUGMSGTL(("slMIB", "slTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->slMaxCapacitySL = 0;
		if (memdup((u_char **) &StorageNew->slProceduralStatus, (u_char *) "\x80", 1) != SNMPERR_SUCCESS)
			goto nomem;
		StorageNew->slProceduralStatusLen = 1;
		if ((StorageNew->slProfilePointer = malloc(1)) == NULL)
			goto nomem;
		StorageNew->slProfilePointerLen = 0;
		StorageNew->slProfilePointer[StorageNew->slProfilePointerLen] = 0;
		if ((StorageNew->slSignTermPointer = malloc(1)) == NULL)
			goto nomem;
		StorageNew->slSignTermPointerLen = 0;
		StorageNew->slSignTermPointer[StorageNew->slSignTermPointerLen] = 0;
		if ((StorageNew->slSignDataLinkTpPointer = malloc(1)) == NULL)
			goto nomem;
		StorageNew->slSignDataLinkTpPointerLen = 0;
		StorageNew->slSignDataLinkTpPointer[StorageNew->slSignDataLinkTpPointerLen] = 0;
		StorageNew->slReplaceSt = 0;
		StorageNew->slCongestionLevel = 0;
		StorageNew->slRelatedLinkGroupNumber = 0;
		StorageNew->slSdlList = 0;
		if ((StorageNew->slName = malloc(1)) == NULL)
			goto nomem;
		StorageNew->slNameLen = 0;
		StorageNew->slName[StorageNew->slNameLen] = 0;
		StorageNew->slSdtList = 0;
		StorageNew->slRowStatus = 0;
		StorageNew->slRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("slMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	slTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct slTable_data *slTable_duplicate(struct slTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct slTable_data *
slTable_duplicate(struct slTable_data *thedata)
{
	struct slTable_data *StorageNew = SNMP_MALLOC_STRUCT(slTable_data);

	DEBUGMSGTL(("slMIB", "slTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->slTable_id = thedata->slTable_id;
		StorageNew->slId = thedata->slId;
		StorageNew->slMaxCapacitySL = thedata->slMaxCapacitySL;
		if (!(StorageNew->slProceduralStatus = malloc(thedata->slProceduralStatusLen + 1)))
			goto destroy;
		memcpy(StorageNew->slProceduralStatus, thedata->slProceduralStatus, thedata->slProceduralStatusLen);
		StorageNew->slProceduralStatusLen = thedata->slProceduralStatusLen;
		StorageNew->slProceduralStatus[StorageNew->slProceduralStatusLen] = 0;
		if (!(StorageNew->slProfilePointer = malloc(thedata->slProfilePointerLen + 1)))
			goto destroy;
		memcpy(StorageNew->slProfilePointer, thedata->slProfilePointer, thedata->slProfilePointerLen);
		StorageNew->slProfilePointerLen = thedata->slProfilePointerLen;
		StorageNew->slProfilePointer[StorageNew->slProfilePointerLen] = 0;
		if (!(StorageNew->slSignTermPointer = malloc(thedata->slSignTermPointerLen + 1)))
			goto destroy;
		memcpy(StorageNew->slSignTermPointer, thedata->slSignTermPointer, thedata->slSignTermPointerLen);
		StorageNew->slSignTermPointerLen = thedata->slSignTermPointerLen;
		StorageNew->slSignTermPointer[StorageNew->slSignTermPointerLen] = 0;
		if (!(StorageNew->slSignDataLinkTpPointer = malloc(thedata->slSignDataLinkTpPointerLen + 1)))
			goto destroy;
		memcpy(StorageNew->slSignDataLinkTpPointer, thedata->slSignDataLinkTpPointer, thedata->slSignDataLinkTpPointerLen);
		StorageNew->slSignDataLinkTpPointerLen = thedata->slSignDataLinkTpPointerLen;
		StorageNew->slSignDataLinkTpPointer[StorageNew->slSignDataLinkTpPointerLen] = 0;
		StorageNew->slReplaceSt = thedata->slReplaceSt;
		StorageNew->slCongestionLevel = thedata->slCongestionLevel;
		StorageNew->slRelatedLinkGroupNumber = thedata->slRelatedLinkGroupNumber;
		StorageNew->slSdlList = thedata->slSdlList;
		if (!(StorageNew->slName = malloc(thedata->slNameLen + 1)))
			goto destroy;
		memcpy(StorageNew->slName, thedata->slName, thedata->slNameLen);
		StorageNew->slNameLen = thedata->slNameLen;
		StorageNew->slName[StorageNew->slNameLen] = 0;
		StorageNew->slSdtList = thedata->slSdtList;
		StorageNew->slRowStatus = thedata->slRowStatus;
	}
      done:
	DEBUGMSGTL(("slMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	slTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int slTable_destroy(struct slTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
slTable_destroy(struct slTable_data **thedata)
{
	struct slTable_data *StorageDel;

	DEBUGMSGTL(("slMIB", "slTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->slProceduralStatus);
		StorageDel->slProceduralStatusLen = 0;
		SNMP_FREE(StorageDel->slProfilePointer);
		StorageDel->slProfilePointerLen = 0;
		SNMP_FREE(StorageDel->slSignTermPointer);
		StorageDel->slSignTermPointerLen = 0;
		SNMP_FREE(StorageDel->slSignDataLinkTpPointer);
		StorageDel->slSignDataLinkTpPointerLen = 0;
		SNMP_FREE(StorageDel->slName);
		StorageDel->slNameLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("slMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int slTable_add(struct slTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the slTable table data set.
 *
 * Adds a table row structure to the slTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
slTable_add(struct slTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("slMIB", "slTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* slId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->slId, sizeof(thedata->slId));
		header_complex_add_data(&slTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("slMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int slTable_del(struct slTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the slTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
slTable_del(struct slTable_data *thedata)
{
	struct slTable_data *StorageDel;

	DEBUGMSGTL(("slMIB", "slTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(slTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&slTableStorage, hciptr);
	}
	DEBUGMSGTL(("slMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_slTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for slTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case slTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_slTable(const char *token, char *line)
{
	size_t tmpsize;
	struct slTable_data *StorageTmp = slTable_create();

	DEBUGMSGTL(("slMIB", "parse_slTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slMaxCapacitySL, &tmpsize);
	SNMP_FREE(StorageTmp->slProceduralStatus);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->slProceduralStatus, &StorageTmp->slProceduralStatusLen);
	if (StorageTmp->slProceduralStatus == NULL) {
		config_perror("invalid specification for slProceduralStatus");
		return;
	}
	SNMP_FREE(StorageTmp->slProfilePointer);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->slProfilePointer, &StorageTmp->slProfilePointerLen);
	if (StorageTmp->slProfilePointer == NULL) {
		config_perror("invalid specification for slProfilePointer");
		return;
	}
	SNMP_FREE(StorageTmp->slSignTermPointer);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->slSignTermPointer, &StorageTmp->slSignTermPointerLen);
	if (StorageTmp->slSignTermPointer == NULL) {
		config_perror("invalid specification for slSignTermPointer");
		return;
	}
	SNMP_FREE(StorageTmp->slSignDataLinkTpPointer);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->slSignDataLinkTpPointer, &StorageTmp->slSignDataLinkTpPointerLen);
	if (StorageTmp->slSignDataLinkTpPointer == NULL) {
		config_perror("invalid specification for slSignDataLinkTpPointer");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->slReplaceSt, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->slCongestionLevel, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slRelatedLinkGroupNumber, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slSdlList, &tmpsize);
	SNMP_FREE(StorageTmp->slName);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->slName, &StorageTmp->slNameLen);
	if (StorageTmp->slName == NULL) {
		config_perror("invalid specification for slName");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slSdtList, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->slRowStatus, &tmpsize);
	slTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("slMIB", "done.\n"));
}

/*
 * store_slTable(): store configuraiton file for slTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_slTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct slTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("slMIB", "store_slTable: storing data...  "));
	refresh_slTable(1);
	(void) tmpsize;
	for (hcindex = slTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct slTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "slTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slMaxCapacitySL, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->slProceduralStatus, &StorageTmp->slProceduralStatusLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->slProfilePointer, &StorageTmp->slProfilePointerLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->slSignTermPointer, &StorageTmp->slSignTermPointerLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->slSignDataLinkTpPointer, &StorageTmp->slSignDataLinkTpPointerLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->slReplaceSt, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->slCongestionLevel, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slRelatedLinkGroupNumber, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slSdlList, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->slName, &StorageTmp->slNameLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slSdtList, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->slRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("slMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct slSdlListTable_data *slSdlListTable_create(void)
 * @brief create a fresh data structure representing a new row in the slSdlListTable table.
 *
 * Creates a new slSdlListTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct slSdlListTable_data *
slSdlListTable_create(void)
{
	struct slSdlListTable_data *StorageNew = SNMP_MALLOC_STRUCT(slSdlListTable_data);

	DEBUGMSGTL(("slMIB", "slSdlListTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->slId = 0;
		if ((StorageNew->slSdlListPointer = malloc(1)) == NULL)
			goto nomem;
		StorageNew->slSdlListPointerLen = 0;
		StorageNew->slSdlListPointer[StorageNew->slSdlListPointerLen] = 0;
		StorageNew->slSdlListRowStatus = 0;
		StorageNew->slSdlListRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("slMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	slSdlListTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct slSdlListTable_data *slSdlListTable_duplicate(struct slSdlListTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct slSdlListTable_data *
slSdlListTable_duplicate(struct slSdlListTable_data *thedata)
{
	struct slSdlListTable_data *StorageNew = SNMP_MALLOC_STRUCT(slSdlListTable_data);

	DEBUGMSGTL(("slMIB", "slSdlListTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->slSdlListTable_id = thedata->slSdlListTable_id;
		StorageNew->slId = thedata->slId;
		StorageNew->slSdlListId = thedata->slSdlListId;
		if (!(StorageNew->slSdlListPointer = malloc(thedata->slSdlListPointerLen + 1)))
			goto destroy;
		memcpy(StorageNew->slSdlListPointer, thedata->slSdlListPointer, thedata->slSdlListPointerLen);
		StorageNew->slSdlListPointerLen = thedata->slSdlListPointerLen;
		StorageNew->slSdlListPointer[StorageNew->slSdlListPointerLen] = 0;
		StorageNew->slSdlListRowStatus = thedata->slSdlListRowStatus;
	}
      done:
	DEBUGMSGTL(("slMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	slSdlListTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int slSdlListTable_destroy(struct slSdlListTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
slSdlListTable_destroy(struct slSdlListTable_data **thedata)
{
	struct slSdlListTable_data *StorageDel;

	DEBUGMSGTL(("slMIB", "slSdlListTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->slSdlListPointer);
		StorageDel->slSdlListPointerLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("slMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int slSdlListTable_add(struct slSdlListTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the slSdlListTable table data set.
 *
 * Adds a table row structure to the slSdlListTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
slSdlListTable_add(struct slSdlListTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("slMIB", "slSdlListTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* slId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->slId, sizeof(thedata->slId));
		/* slSdlListId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->slSdlListId, sizeof(thedata->slSdlListId));
		header_complex_add_data(&slSdlListTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("slMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int slSdlListTable_del(struct slSdlListTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the slSdlListTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
slSdlListTable_del(struct slSdlListTable_data *thedata)
{
	struct slSdlListTable_data *StorageDel;

	DEBUGMSGTL(("slMIB", "slSdlListTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(slSdlListTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&slSdlListTableStorage, hciptr);
	}
	DEBUGMSGTL(("slMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_slSdlListTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for slSdlListTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case slSdlListTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_slSdlListTable(const char *token, char *line)
{
	size_t tmpsize;
	struct slSdlListTable_data *StorageTmp = slSdlListTable_create();

	DEBUGMSGTL(("slMIB", "parse_slSdlListTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slSdlListId, &tmpsize);
	SNMP_FREE(StorageTmp->slSdlListPointer);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->slSdlListPointer, &StorageTmp->slSdlListPointerLen);
	if (StorageTmp->slSdlListPointer == NULL) {
		config_perror("invalid specification for slSdlListPointer");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->slSdlListRowStatus, &tmpsize);
	slSdlListTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("slMIB", "done.\n"));
}

/*
 * store_slSdlListTable(): store configuraiton file for slSdlListTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_slSdlListTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct slSdlListTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("slMIB", "store_slSdlListTable: storing data...  "));
	refresh_slSdlListTable(1);
	(void) tmpsize;
	for (hcindex = slSdlListTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct slSdlListTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "slSdlListTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slSdlListId, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->slSdlListPointer, &StorageTmp->slSdlListPointerLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->slSdlListRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("slMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct slSdtListTable_data *slSdtListTable_create(void)
 * @brief create a fresh data structure representing a new row in the slSdtListTable table.
 *
 * Creates a new slSdtListTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct slSdtListTable_data *
slSdtListTable_create(void)
{
	struct slSdtListTable_data *StorageNew = SNMP_MALLOC_STRUCT(slSdtListTable_data);

	DEBUGMSGTL(("slMIB", "slSdtListTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->slId = 0;
		if ((StorageNew->slSdtListPointer = malloc(1)) == NULL)
			goto nomem;
		StorageNew->slSdtListPointerLen = 0;
		StorageNew->slSdtListPointer[StorageNew->slSdtListPointerLen] = 0;
		StorageNew->slSdtListRowStatus = 0;
		StorageNew->slSdtListRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("slMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	slSdtListTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct slSdtListTable_data *slSdtListTable_duplicate(struct slSdtListTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct slSdtListTable_data *
slSdtListTable_duplicate(struct slSdtListTable_data *thedata)
{
	struct slSdtListTable_data *StorageNew = SNMP_MALLOC_STRUCT(slSdtListTable_data);

	DEBUGMSGTL(("slMIB", "slSdtListTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->slSdtListTable_id = thedata->slSdtListTable_id;
		StorageNew->slId = thedata->slId;
		StorageNew->slSdtListId = thedata->slSdtListId;
		if (!(StorageNew->slSdtListPointer = malloc(thedata->slSdtListPointerLen + 1)))
			goto destroy;
		memcpy(StorageNew->slSdtListPointer, thedata->slSdtListPointer, thedata->slSdtListPointerLen);
		StorageNew->slSdtListPointerLen = thedata->slSdtListPointerLen;
		StorageNew->slSdtListPointer[StorageNew->slSdtListPointerLen] = 0;
		StorageNew->slSdtListRowStatus = thedata->slSdtListRowStatus;
	}
      done:
	DEBUGMSGTL(("slMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	slSdtListTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int slSdtListTable_destroy(struct slSdtListTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
slSdtListTable_destroy(struct slSdtListTable_data **thedata)
{
	struct slSdtListTable_data *StorageDel;

	DEBUGMSGTL(("slMIB", "slSdtListTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->slSdtListPointer);
		StorageDel->slSdtListPointerLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("slMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int slSdtListTable_add(struct slSdtListTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the slSdtListTable table data set.
 *
 * Adds a table row structure to the slSdtListTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
slSdtListTable_add(struct slSdtListTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("slMIB", "slSdtListTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* slId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->slId, sizeof(thedata->slId));
		/* slSdtListId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->slSdtListId, sizeof(thedata->slSdtListId));
		header_complex_add_data(&slSdtListTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("slMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int slSdtListTable_del(struct slSdtListTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the slSdtListTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
slSdtListTable_del(struct slSdtListTable_data *thedata)
{
	struct slSdtListTable_data *StorageDel;

	DEBUGMSGTL(("slMIB", "slSdtListTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(slSdtListTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&slSdtListTableStorage, hciptr);
	}
	DEBUGMSGTL(("slMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_slSdtListTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for slSdtListTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case slSdtListTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_slSdtListTable(const char *token, char *line)
{
	size_t tmpsize;
	struct slSdtListTable_data *StorageTmp = slSdtListTable_create();

	DEBUGMSGTL(("slMIB", "parse_slSdtListTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slSdtListId, &tmpsize);
	SNMP_FREE(StorageTmp->slSdtListPointer);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->slSdtListPointer, &StorageTmp->slSdtListPointerLen);
	if (StorageTmp->slSdtListPointer == NULL) {
		config_perror("invalid specification for slSdtListPointer");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->slSdtListRowStatus, &tmpsize);
	slSdtListTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("slMIB", "done.\n"));
}

/*
 * store_slSdtListTable(): store configuraiton file for slSdtListTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_slSdtListTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct slSdtListTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("slMIB", "store_slSdtListTable: storing data...  "));
	refresh_slSdtListTable(1);
	(void) tmpsize;
	for (hcindex = slSdtListTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct slSdtListTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "slSdtListTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slSdtListId, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->slSdtListPointer, &StorageTmp->slSdtListPointerLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->slSdtListRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("slMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct slNbTable_data *slNbTable_create(void)
 * @brief create a fresh data structure representing a new row in the slNbTable table.
 *
 * Creates a new slNbTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct slNbTable_data *
slNbTable_create(void)
{
	struct slNbTable_data *StorageNew = SNMP_MALLOC_STRUCT(slNbTable_data);

	DEBUGMSGTL(("slMIB", "slNbTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->slNbTransmissionRate = SLNBTRANSMISSIONRATE_KBITS64;
		StorageNew->slNbTimerT8 = 100;
		StorageNew->slNbTin = 4;
		StorageNew->slNbTie = 1;
		StorageNew->slNbT = 64;
		StorageNew->slNbD = 256;
		StorageNew->slNbTe = 793544;
		StorageNew->slNbDe = 11328;
		StorageNew->slNbUe = 198384;
		StorageNew->slNbN = 16;
		StorageNew->slNbm = 272;
		StorageNew->slNbb = 0;
		StorageNew->slNbf = SLNBF_ONE;
		StorageNew->slNbRowStatus = 0;
		StorageNew->slNbRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("slMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	slNbTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct slNbTable_data *slNbTable_duplicate(struct slNbTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct slNbTable_data *
slNbTable_duplicate(struct slNbTable_data *thedata)
{
	struct slNbTable_data *StorageNew = SNMP_MALLOC_STRUCT(slNbTable_data);

	DEBUGMSGTL(("slMIB", "slNbTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->slNbTable_id = thedata->slNbTable_id;
		if (!(StorageNew->slSdtProfileId = malloc(thedata->slSdtProfileIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->slSdtProfileId, thedata->slSdtProfileId, thedata->slSdtProfileIdLen);
		StorageNew->slSdtProfileIdLen = thedata->slSdtProfileIdLen;
		StorageNew->slSdtProfileId[StorageNew->slSdtProfileIdLen] = 0;
		StorageNew->slNbTransmissionRate = thedata->slNbTransmissionRate;
		StorageNew->slNbTimerT8 = thedata->slNbTimerT8;
		StorageNew->slNbTin = thedata->slNbTin;
		StorageNew->slNbTie = thedata->slNbTie;
		StorageNew->slNbT = thedata->slNbT;
		StorageNew->slNbD = thedata->slNbD;
		StorageNew->slNbTe = thedata->slNbTe;
		StorageNew->slNbDe = thedata->slNbDe;
		StorageNew->slNbUe = thedata->slNbUe;
		StorageNew->slNbN = thedata->slNbN;
		StorageNew->slNbm = thedata->slNbm;
		StorageNew->slNbb = thedata->slNbb;
		StorageNew->slNbf = thedata->slNbf;
		StorageNew->slNbRowStatus = thedata->slNbRowStatus;
	}
      done:
	DEBUGMSGTL(("slMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	slNbTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int slNbTable_destroy(struct slNbTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
slNbTable_destroy(struct slNbTable_data **thedata)
{
	struct slNbTable_data *StorageDel;

	DEBUGMSGTL(("slMIB", "slNbTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->slSdtProfileId);
		StorageDel->slSdtProfileIdLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("slMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int slNbTable_add(struct slNbTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the slNbTable table data set.
 *
 * Adds a table row structure to the slNbTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
slNbTable_add(struct slNbTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("slMIB", "slNbTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* slSdtProfileId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->slSdtProfileId, thedata->slSdtProfileIdLen);
		header_complex_add_data(&slNbTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("slMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int slNbTable_del(struct slNbTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the slNbTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
slNbTable_del(struct slNbTable_data *thedata)
{
	struct slNbTable_data *StorageDel;

	DEBUGMSGTL(("slMIB", "slNbTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(slNbTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&slNbTableStorage, hciptr);
	}
	DEBUGMSGTL(("slMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_slNbTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for slNbTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case slNbTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_slNbTable(const char *token, char *line)
{
	size_t tmpsize;
	struct slNbTable_data *StorageTmp = slNbTable_create();

	DEBUGMSGTL(("slMIB", "parse_slNbTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->slSdtProfileId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->slSdtProfileId, &StorageTmp->slSdtProfileIdLen);
	if (StorageTmp->slSdtProfileId == NULL) {
		config_perror("invalid specification for slSdtProfileId");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->slNbTransmissionRate, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slNbTimerT8, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slNbTin, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slNbTie, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slNbT, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slNbD, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slNbTe, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slNbDe, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slNbUe, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slNbN, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slNbm, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slNbb, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->slNbf, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->slNbRowStatus, &tmpsize);
	slNbTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("slMIB", "done.\n"));
}

/*
 * store_slNbTable(): store configuraiton file for slNbTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_slNbTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct slNbTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("slMIB", "store_slNbTable: storing data...  "));
	refresh_slNbTable(1);
	(void) tmpsize;
	for (hcindex = slNbTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct slNbTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "slNbTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->slSdtProfileId, &StorageTmp->slSdtProfileIdLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->slNbTransmissionRate, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slNbTimerT8, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slNbTin, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slNbTie, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slNbT, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slNbD, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slNbTe, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slNbDe, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slNbUe, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slNbN, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slNbm, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slNbb, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->slNbf, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->slNbRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("slMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct slSaalTable_data *slSaalTable_create(void)
 * @brief create a fresh data structure representing a new row in the slSaalTable table.
 *
 * Creates a new slSaalTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct slSaalTable_data *
slSaalTable_create(void)
{
	struct slSaalTable_data *StorageNew = SNMP_MALLOC_STRUCT(slSaalTable_data);

	DEBUGMSGTL(("slMIB", "slSaalTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->slSdtProfileId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->slSdtProfileIdLen = 0;
		StorageNew->slSdtProfileId[StorageNew->slSdtProfileIdLen] = 0;
		StorageNew->slSaalBufferRelease = 0;
		StorageNew->slSaalMaxCc = 4;
		StorageNew->slSaalMaxNrp = 0;
		StorageNew->slSaalMaxInformationFieldLength = 4096;
		StorageNew->slSaalMaxLengthSscopUuField = 4;
		StorageNew->slSaalMaxPd = 500;
		StorageNew->slSaalMaxSscopCreditToPeer = 0;
		StorageNew->slSaalMaxStat = 67;
		StorageNew->slSaalN1 = 1000;
		StorageNew->slSaalNniLayerMgrProvingState = 0;
		StorageNew->slSaalNniLayerMgrTimerNoCredit = 0;
		StorageNew->slSaalNniLayerMgrTimerRepeatSrec = 0;
		StorageNew->slSaalNniTimerT1 = 500;
		StorageNew->slSaalNniTimerT2 = 3000;
		StorageNew->slSaalNniTimerT3 = 0;
		StorageNew->slSaalSscopTimerCc = 20;
		StorageNew->slSaalSscopTimerIdle = 10;
		StorageNew->slSaalSscopTimerKeepAlive = 10;
		StorageNew->slSaalSscopTimerNoResponse = 150;
		StorageNew->slSaalSscopTimerPoll = 10;
		StorageNew->slSaalTransRateIntervalLower = 129;
		StorageNew->slSaalTransRateIntervalUpper = 256;
		StorageNew->slSaalEgressTransRateIntvlLower = 0;
		StorageNew->slSaalEgressTransRateIntvlUpper = 0;
		StorageNew->slSaalPollAfterRetransmission = 0;
		StorageNew->slSaalRowStatus = 0;
		StorageNew->slSaalRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("slMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	slSaalTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct slSaalTable_data *slSaalTable_duplicate(struct slSaalTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct slSaalTable_data *
slSaalTable_duplicate(struct slSaalTable_data *thedata)
{
	struct slSaalTable_data *StorageNew = SNMP_MALLOC_STRUCT(slSaalTable_data);

	DEBUGMSGTL(("slMIB", "slSaalTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->slSaalTable_id = thedata->slSaalTable_id;
		if (!(StorageNew->slSdtProfileId = malloc(thedata->slSdtProfileIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->slSdtProfileId, thedata->slSdtProfileId, thedata->slSdtProfileIdLen);
		StorageNew->slSdtProfileIdLen = thedata->slSdtProfileIdLen;
		StorageNew->slSdtProfileId[StorageNew->slSdtProfileIdLen] = 0;
		StorageNew->slSaalBufferRelease = thedata->slSaalBufferRelease;
		StorageNew->slSaalMaxCc = thedata->slSaalMaxCc;
		StorageNew->slSaalMaxNrp = thedata->slSaalMaxNrp;
		StorageNew->slSaalMaxInformationFieldLength = thedata->slSaalMaxInformationFieldLength;
		StorageNew->slSaalMaxLengthSscopUuField = thedata->slSaalMaxLengthSscopUuField;
		StorageNew->slSaalMaxPd = thedata->slSaalMaxPd;
		StorageNew->slSaalMaxSscopCreditToPeer = thedata->slSaalMaxSscopCreditToPeer;
		StorageNew->slSaalMaxStat = thedata->slSaalMaxStat;
		StorageNew->slSaalN1 = thedata->slSaalN1;
		StorageNew->slSaalNniLayerMgrProvingState = thedata->slSaalNniLayerMgrProvingState;
		StorageNew->slSaalNniLayerMgrTimerNoCredit = thedata->slSaalNniLayerMgrTimerNoCredit;
		StorageNew->slSaalNniLayerMgrTimerRepeatSrec = thedata->slSaalNniLayerMgrTimerRepeatSrec;
		StorageNew->slSaalNniTimerT1 = thedata->slSaalNniTimerT1;
		StorageNew->slSaalNniTimerT2 = thedata->slSaalNniTimerT2;
		StorageNew->slSaalNniTimerT3 = thedata->slSaalNniTimerT3;
		StorageNew->slSaalSscopTimerCc = thedata->slSaalSscopTimerCc;
		StorageNew->slSaalSscopTimerIdle = thedata->slSaalSscopTimerIdle;
		StorageNew->slSaalSscopTimerKeepAlive = thedata->slSaalSscopTimerKeepAlive;
		StorageNew->slSaalSscopTimerNoResponse = thedata->slSaalSscopTimerNoResponse;
		StorageNew->slSaalSscopTimerPoll = thedata->slSaalSscopTimerPoll;
		StorageNew->slSaalTransRateIntervalLower = thedata->slSaalTransRateIntervalLower;
		StorageNew->slSaalTransRateIntervalUpper = thedata->slSaalTransRateIntervalUpper;
		StorageNew->slSaalEgressTransRateIntvlLower = thedata->slSaalEgressTransRateIntvlLower;
		StorageNew->slSaalEgressTransRateIntvlUpper = thedata->slSaalEgressTransRateIntvlUpper;
		StorageNew->slSaalPollAfterRetransmission = thedata->slSaalPollAfterRetransmission;
		StorageNew->slSaalRowStatus = thedata->slSaalRowStatus;
	}
      done:
	DEBUGMSGTL(("slMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	slSaalTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int slSaalTable_destroy(struct slSaalTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
slSaalTable_destroy(struct slSaalTable_data **thedata)
{
	struct slSaalTable_data *StorageDel;

	DEBUGMSGTL(("slMIB", "slSaalTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->slSdtProfileId);
		StorageDel->slSdtProfileIdLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("slMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int slSaalTable_add(struct slSaalTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the slSaalTable table data set.
 *
 * Adds a table row structure to the slSaalTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
slSaalTable_add(struct slSaalTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("slMIB", "slSaalTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* slSdtProfileId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->slSdtProfileId, thedata->slSdtProfileIdLen);
		header_complex_add_data(&slSaalTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("slMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int slSaalTable_del(struct slSaalTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the slSaalTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
slSaalTable_del(struct slSaalTable_data *thedata)
{
	struct slSaalTable_data *StorageDel;

	DEBUGMSGTL(("slMIB", "slSaalTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(slSaalTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&slSaalTableStorage, hciptr);
	}
	DEBUGMSGTL(("slMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_slSaalTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for slSaalTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case slSaalTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_slSaalTable(const char *token, char *line)
{
	size_t tmpsize;
	struct slSaalTable_data *StorageTmp = slSaalTable_create();

	DEBUGMSGTL(("slMIB", "parse_slSaalTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->slSdtProfileId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->slSdtProfileId, &StorageTmp->slSdtProfileIdLen);
	if (StorageTmp->slSdtProfileId == NULL) {
		config_perror("invalid specification for slSdtProfileId");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->slSaalBufferRelease, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slSaalMaxCc, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slSaalMaxNrp, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slSaalMaxInformationFieldLength, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slSaalMaxLengthSscopUuField, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slSaalMaxPd, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slSaalMaxSscopCreditToPeer, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slSaalMaxStat, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slSaalN1, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->slSaalNniLayerMgrProvingState, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->slSaalNniLayerMgrTimerNoCredit, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->slSaalNniLayerMgrTimerRepeatSrec, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->slSaalNniTimerT1, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->slSaalNniTimerT2, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->slSaalNniTimerT3, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->slSaalSscopTimerCc, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->slSaalSscopTimerIdle, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->slSaalSscopTimerKeepAlive, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->slSaalSscopTimerNoResponse, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->slSaalSscopTimerPoll, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slSaalTransRateIntervalLower, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slSaalTransRateIntervalUpper, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slSaalEgressTransRateIntvlLower, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slSaalEgressTransRateIntvlUpper, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->slSaalPollAfterRetransmission, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->slSaalRowStatus, &tmpsize);
	slSaalTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("slMIB", "done.\n"));
}

/*
 * store_slSaalTable(): store configuraiton file for slSaalTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_slSaalTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct slSaalTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("slMIB", "store_slSaalTable: storing data...  "));
	refresh_slSaalTable(1);
	(void) tmpsize;
	for (hcindex = slSaalTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct slSaalTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "slSaalTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->slSdtProfileId, &StorageTmp->slSdtProfileIdLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->slSaalBufferRelease, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slSaalMaxCc, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slSaalMaxNrp, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slSaalMaxInformationFieldLength, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slSaalMaxLengthSscopUuField, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slSaalMaxPd, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slSaalMaxSscopCreditToPeer, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slSaalMaxStat, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slSaalN1, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->slSaalNniLayerMgrProvingState, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->slSaalNniLayerMgrTimerNoCredit, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->slSaalNniLayerMgrTimerRepeatSrec, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->slSaalNniTimerT1, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->slSaalNniTimerT2, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->slSaalNniTimerT3, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->slSaalSscopTimerCc, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->slSaalSscopTimerIdle, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->slSaalSscopTimerKeepAlive, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->slSaalSscopTimerNoResponse, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->slSaalSscopTimerPoll, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slSaalTransRateIntervalLower, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slSaalTransRateIntervalUpper, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slSaalEgressTransRateIntvlLower, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slSaalEgressTransRateIntvlUpper, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->slSaalPollAfterRetransmission, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->slSaalRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("slMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct slM2paTable_data *slM2paTable_create(void)
 * @brief create a fresh data structure representing a new row in the slM2paTable table.
 *
 * Creates a new slM2paTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct slM2paTable_data *
slM2paTable_create(void)
{
	struct slM2paTable_data *StorageNew = SNMP_MALLOC_STRUCT(slM2paTable_data);

	DEBUGMSGTL(("slMIB", "slM2paTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->slSdtProfileId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->slSdtProfileIdLen = 0;
		StorageNew->slSdtProfileId[StorageNew->slSdtProfileIdLen] = 0;
		StorageNew->slM2paN1 = 1000;
		StorageNew->slM2paProving = TV_TRUE;
		StorageNew->slM2paManagementProvingState = 0;
		StorageNew->slM2paLoopDelayLower = 0;
		StorageNew->slM2paLoopDelayUpper = 0;
		StorageNew->slM2paTransRateIntervalLower = 0;
		StorageNew->slM2paTransRateIntervalUpper = 0;
		StorageNew->slM2paSctpNoDelay = 0;
		StorageNew->slM2paSctpMaxseg = 0;
		StorageNew->slM2paSctpHeartbeatItvl = 0;
		StorageNew->slM2paSctpHeartbeat = 0;
		StorageNew->slM2paSctpRtoInitial = 0;
		StorageNew->slM2paSctpRtoMin = 0;
		StorageNew->slM2paSctpRtoMax = 0;
		StorageNew->slM2paSctpPathMaxRetrans = 0;
		StorageNew->slM2paSctpCookieLife = 0;
		StorageNew->slM2paSctpCookieInc = 0;
		StorageNew->slM2paSctpMaxInitRetries = 0;
		StorageNew->slM2paSctpMaxBurst = 0;
		StorageNew->slM2paSctpAssocMaxRetrans = 20;
		StorageNew->slM2paSctpSackDelay = 20;
		StorageNew->slM2paSctpLifetime = 0;
		StorageNew->slM2paProvingAttempts = 0;
		StorageNew->slM2paRowStatus = 0;
		StorageNew->slM2paRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("slMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	slM2paTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct slM2paTable_data *slM2paTable_duplicate(struct slM2paTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct slM2paTable_data *
slM2paTable_duplicate(struct slM2paTable_data *thedata)
{
	struct slM2paTable_data *StorageNew = SNMP_MALLOC_STRUCT(slM2paTable_data);

	DEBUGMSGTL(("slMIB", "slM2paTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->slM2paTable_id = thedata->slM2paTable_id;
		if (!(StorageNew->slSdtProfileId = malloc(thedata->slSdtProfileIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->slSdtProfileId, thedata->slSdtProfileId, thedata->slSdtProfileIdLen);
		StorageNew->slSdtProfileIdLen = thedata->slSdtProfileIdLen;
		StorageNew->slSdtProfileId[StorageNew->slSdtProfileIdLen] = 0;
		StorageNew->slM2paN1 = thedata->slM2paN1;
		StorageNew->slM2paProving = thedata->slM2paProving;
		StorageNew->slM2paManagementProvingState = thedata->slM2paManagementProvingState;
		StorageNew->slM2paLoopDelayLower = thedata->slM2paLoopDelayLower;
		StorageNew->slM2paLoopDelayUpper = thedata->slM2paLoopDelayUpper;
		StorageNew->slM2paTransRateIntervalLower = thedata->slM2paTransRateIntervalLower;
		StorageNew->slM2paTransRateIntervalUpper = thedata->slM2paTransRateIntervalUpper;
		StorageNew->slM2paSctpNoDelay = thedata->slM2paSctpNoDelay;
		StorageNew->slM2paSctpMaxseg = thedata->slM2paSctpMaxseg;
		StorageNew->slM2paSctpHeartbeatItvl = thedata->slM2paSctpHeartbeatItvl;
		StorageNew->slM2paSctpHeartbeat = thedata->slM2paSctpHeartbeat;
		StorageNew->slM2paSctpRtoInitial = thedata->slM2paSctpRtoInitial;
		StorageNew->slM2paSctpRtoMin = thedata->slM2paSctpRtoMin;
		StorageNew->slM2paSctpRtoMax = thedata->slM2paSctpRtoMax;
		StorageNew->slM2paSctpPathMaxRetrans = thedata->slM2paSctpPathMaxRetrans;
		StorageNew->slM2paSctpCookieLife = thedata->slM2paSctpCookieLife;
		StorageNew->slM2paSctpCookieInc = thedata->slM2paSctpCookieInc;
		StorageNew->slM2paSctpMaxInitRetries = thedata->slM2paSctpMaxInitRetries;
		StorageNew->slM2paSctpMaxBurst = thedata->slM2paSctpMaxBurst;
		StorageNew->slM2paSctpAssocMaxRetrans = thedata->slM2paSctpAssocMaxRetrans;
		StorageNew->slM2paSctpSackDelay = thedata->slM2paSctpSackDelay;
		StorageNew->slM2paSctpLifetime = thedata->slM2paSctpLifetime;
		StorageNew->slM2paProvingAttempts = thedata->slM2paProvingAttempts;
		StorageNew->slM2paRowStatus = thedata->slM2paRowStatus;
	}
      done:
	DEBUGMSGTL(("slMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	slM2paTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int slM2paTable_destroy(struct slM2paTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
slM2paTable_destroy(struct slM2paTable_data **thedata)
{
	struct slM2paTable_data *StorageDel;

	DEBUGMSGTL(("slMIB", "slM2paTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->slSdtProfileId);
		StorageDel->slSdtProfileIdLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("slMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int slM2paTable_add(struct slM2paTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the slM2paTable table data set.
 *
 * Adds a table row structure to the slM2paTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
slM2paTable_add(struct slM2paTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("slMIB", "slM2paTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* slSdtProfileId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->slSdtProfileId, thedata->slSdtProfileIdLen);
		header_complex_add_data(&slM2paTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("slMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int slM2paTable_del(struct slM2paTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the slM2paTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
slM2paTable_del(struct slM2paTable_data *thedata)
{
	struct slM2paTable_data *StorageDel;

	DEBUGMSGTL(("slMIB", "slM2paTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(slM2paTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&slM2paTableStorage, hciptr);
	}
	DEBUGMSGTL(("slMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_slM2paTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for slM2paTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case slM2paTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_slM2paTable(const char *token, char *line)
{
	size_t tmpsize;
	struct slM2paTable_data *StorageTmp = slM2paTable_create();

	DEBUGMSGTL(("slMIB", "parse_slM2paTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->slSdtProfileId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->slSdtProfileId, &StorageTmp->slSdtProfileIdLen);
	if (StorageTmp->slSdtProfileId == NULL) {
		config_perror("invalid specification for slSdtProfileId");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slM2paN1, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->slM2paProving, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->slM2paManagementProvingState, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slM2paLoopDelayLower, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slM2paLoopDelayUpper, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slM2paTransRateIntervalLower, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slM2paTransRateIntervalUpper, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->slM2paSctpNoDelay, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slM2paSctpMaxseg, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slM2paSctpHeartbeatItvl, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->slM2paSctpHeartbeat, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slM2paSctpRtoInitial, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slM2paSctpRtoMin, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slM2paSctpRtoMax, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slM2paSctpPathMaxRetrans, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slM2paSctpCookieLife, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slM2paSctpCookieInc, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slM2paSctpMaxInitRetries, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slM2paSctpMaxBurst, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slM2paSctpAssocMaxRetrans, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slM2paSctpSackDelay, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slM2paSctpLifetime, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slM2paProvingAttempts, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->slM2paRowStatus, &tmpsize);
	slM2paTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("slMIB", "done.\n"));
}

/*
 * store_slM2paTable(): store configuraiton file for slM2paTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_slM2paTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct slM2paTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("slMIB", "store_slM2paTable: storing data...  "));
	refresh_slM2paTable(1);
	(void) tmpsize;
	for (hcindex = slM2paTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct slM2paTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "slM2paTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->slSdtProfileId, &StorageTmp->slSdtProfileIdLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slM2paN1, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->slM2paProving, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->slM2paManagementProvingState, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slM2paLoopDelayLower, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slM2paLoopDelayUpper, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slM2paTransRateIntervalLower, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slM2paTransRateIntervalUpper, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->slM2paSctpNoDelay, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slM2paSctpMaxseg, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slM2paSctpHeartbeatItvl, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->slM2paSctpHeartbeat, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slM2paSctpRtoInitial, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slM2paSctpRtoMin, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slM2paSctpRtoMax, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slM2paSctpPathMaxRetrans, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slM2paSctpCookieLife, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slM2paSctpCookieInc, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slM2paSctpMaxInitRetries, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slM2paSctpMaxBurst, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slM2paSctpAssocMaxRetrans, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slM2paSctpSackDelay, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slM2paSctpLifetime, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slM2paProvingAttempts, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->slM2paRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("slMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct slSdtTable_data *slSdtTable_create(void)
 * @brief create a fresh data structure representing a new row in the slSdtTable table.
 *
 * Creates a new slSdtTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct slSdtTable_data *
slSdtTable_create(void)
{
	struct slSdtTable_data *StorageNew = SNMP_MALLOC_STRUCT(slSdtTable_data);

	DEBUGMSGTL(("slMIB", "slSdtTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->slSdtAdministrativeState = SLSDTADMINISTRATIVESTATE_LOCKED;
		StorageNew->slSdtOperationalState = SLSDTOPERATIONALSTATE_DISABLED;
		StorageNew->slSdtUsageState = SLSDTUSAGESTATE_IDLE;
		if (memdup((u_char **) &StorageNew->slSdtProceduralStatus, (u_char *) "\x80", 1) != SNMPERR_SUCCESS)
			goto nomem;
		StorageNew->slSdtProceduralStatusLen = 1;
		if (memdup((u_char **) &StorageNew->slSdtAvailabilityStatus, (u_char *) "\x00\x00", 2) != SNMPERR_SUCCESS)
			goto nomem;
		StorageNew->slSdtAvailabilityStatusLen = 2;
		if ((StorageNew->slSdtEquipmentPointer = snmp_duplicate_objid(zeroDotZero_oid, 2)) == NULL)
			goto nomem;
		StorageNew->slSdtEquipmentPointerLen = 2;
		if ((StorageNew->slSdtName = malloc(1)) == NULL)
			goto nomem;
		StorageNew->slSdtNameLen = 0;
		StorageNew->slSdtName[StorageNew->slSdtNameLen] = 0;
		if ((StorageNew->slSdtProfilePointer = malloc(1)) == NULL)
			goto nomem;
		StorageNew->slSdtProfilePointerLen = 0;
		StorageNew->slSdtProfilePointer[StorageNew->slSdtProfilePointerLen] = 0;
		StorageNew->slSdtRowStatus = 0;
		StorageNew->slSdtRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("slMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	slSdtTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct slSdtTable_data *slSdtTable_duplicate(struct slSdtTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct slSdtTable_data *
slSdtTable_duplicate(struct slSdtTable_data *thedata)
{
	struct slSdtTable_data *StorageNew = SNMP_MALLOC_STRUCT(slSdtTable_data);

	DEBUGMSGTL(("slMIB", "slSdtTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->slSdtTable_id = thedata->slSdtTable_id;
		StorageNew->slSdtId = thedata->slSdtId;
		StorageNew->slSdtAdministrativeState = thedata->slSdtAdministrativeState;
		StorageNew->slSdtOperationalState = thedata->slSdtOperationalState;
		StorageNew->slSdtUsageState = thedata->slSdtUsageState;
		if (!(StorageNew->slSdtProceduralStatus = malloc(thedata->slSdtProceduralStatusLen + 1)))
			goto destroy;
		memcpy(StorageNew->slSdtProceduralStatus, thedata->slSdtProceduralStatus, thedata->slSdtProceduralStatusLen);
		StorageNew->slSdtProceduralStatusLen = thedata->slSdtProceduralStatusLen;
		StorageNew->slSdtProceduralStatus[StorageNew->slSdtProceduralStatusLen] = 0;
		if (!(StorageNew->slSdtAvailabilityStatus = malloc(thedata->slSdtAvailabilityStatusLen + 1)))
			goto destroy;
		memcpy(StorageNew->slSdtAvailabilityStatus, thedata->slSdtAvailabilityStatus, thedata->slSdtAvailabilityStatusLen);
		StorageNew->slSdtAvailabilityStatusLen = thedata->slSdtAvailabilityStatusLen;
		StorageNew->slSdtAvailabilityStatus[StorageNew->slSdtAvailabilityStatusLen] = 0;
		if (!(StorageNew->slSdtEquipmentPointer = snmp_duplicate_objid(thedata->slSdtEquipmentPointer, thedata->slSdtEquipmentPointerLen / sizeof(oid))))
			goto destroy;
		StorageNew->slSdtEquipmentPointerLen = thedata->slSdtEquipmentPointerLen;
		if (!(StorageNew->slSdtName = malloc(thedata->slSdtNameLen + 1)))
			goto destroy;
		memcpy(StorageNew->slSdtName, thedata->slSdtName, thedata->slSdtNameLen);
		StorageNew->slSdtNameLen = thedata->slSdtNameLen;
		StorageNew->slSdtName[StorageNew->slSdtNameLen] = 0;
		if (!(StorageNew->slSdtProfilePointer = malloc(thedata->slSdtProfilePointerLen + 1)))
			goto destroy;
		memcpy(StorageNew->slSdtProfilePointer, thedata->slSdtProfilePointer, thedata->slSdtProfilePointerLen);
		StorageNew->slSdtProfilePointerLen = thedata->slSdtProfilePointerLen;
		StorageNew->slSdtProfilePointer[StorageNew->slSdtProfilePointerLen] = 0;
		StorageNew->slSdtRowStatus = thedata->slSdtRowStatus;
	}
      done:
	DEBUGMSGTL(("slMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	slSdtTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int slSdtTable_destroy(struct slSdtTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
slSdtTable_destroy(struct slSdtTable_data **thedata)
{
	struct slSdtTable_data *StorageDel;

	DEBUGMSGTL(("slMIB", "slSdtTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->slSdtProceduralStatus);
		StorageDel->slSdtProceduralStatusLen = 0;
		SNMP_FREE(StorageDel->slSdtAvailabilityStatus);
		StorageDel->slSdtAvailabilityStatusLen = 0;
		SNMP_FREE(StorageDel->slSdtEquipmentPointer);
		StorageDel->slSdtEquipmentPointerLen = 0;
		SNMP_FREE(StorageDel->slSdtName);
		StorageDel->slSdtNameLen = 0;
		SNMP_FREE(StorageDel->slSdtProfilePointer);
		StorageDel->slSdtProfilePointerLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("slMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int slSdtTable_add(struct slSdtTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the slSdtTable table data set.
 *
 * Adds a table row structure to the slSdtTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
slSdtTable_add(struct slSdtTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("slMIB", "slSdtTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* slSdtId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->slSdtId, sizeof(thedata->slSdtId));
		header_complex_add_data(&slSdtTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("slMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int slSdtTable_del(struct slSdtTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the slSdtTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
slSdtTable_del(struct slSdtTable_data *thedata)
{
	struct slSdtTable_data *StorageDel;

	DEBUGMSGTL(("slMIB", "slSdtTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(slSdtTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&slSdtTableStorage, hciptr);
	}
	DEBUGMSGTL(("slMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_slSdtTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for slSdtTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case slSdtTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_slSdtTable(const char *token, char *line)
{
	size_t tmpsize;
	struct slSdtTable_data *StorageTmp = slSdtTable_create();

	DEBUGMSGTL(("slMIB", "parse_slSdtTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slSdtId, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->slSdtAdministrativeState, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->slSdtOperationalState, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->slSdtUsageState, &tmpsize);
	SNMP_FREE(StorageTmp->slSdtProceduralStatus);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->slSdtProceduralStatus, &StorageTmp->slSdtProceduralStatusLen);
	if (StorageTmp->slSdtProceduralStatus == NULL) {
		config_perror("invalid specification for slSdtProceduralStatus");
		return;
	}
	SNMP_FREE(StorageTmp->slSdtAvailabilityStatus);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->slSdtAvailabilityStatus, &StorageTmp->slSdtAvailabilityStatusLen);
	if (StorageTmp->slSdtAvailabilityStatus == NULL) {
		config_perror("invalid specification for slSdtAvailabilityStatus");
		return;
	}
	SNMP_FREE(StorageTmp->slSdtEquipmentPointer);
	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->slSdtEquipmentPointer, &StorageTmp->slSdtEquipmentPointerLen);
	if (StorageTmp->slSdtEquipmentPointer == NULL) {
		config_perror("invalid specification for slSdtEquipmentPointer");
		return;
	}
	SNMP_FREE(StorageTmp->slSdtName);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->slSdtName, &StorageTmp->slSdtNameLen);
	if (StorageTmp->slSdtName == NULL) {
		config_perror("invalid specification for slSdtName");
		return;
	}
	SNMP_FREE(StorageTmp->slSdtProfilePointer);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->slSdtProfilePointer, &StorageTmp->slSdtProfilePointerLen);
	if (StorageTmp->slSdtProfilePointer == NULL) {
		config_perror("invalid specification for slSdtProfilePointer");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->slSdtRowStatus, &tmpsize);
	slSdtTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("slMIB", "done.\n"));
}

/*
 * store_slSdtTable(): store configuraiton file for slSdtTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_slSdtTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct slSdtTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("slMIB", "store_slSdtTable: storing data...  "));
	refresh_slSdtTable(1);
	(void) tmpsize;
	for (hcindex = slSdtTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct slSdtTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "slSdtTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slSdtId, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->slSdtAdministrativeState, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->slSdtOperationalState, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->slSdtUsageState, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->slSdtProceduralStatus, &StorageTmp->slSdtProceduralStatusLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->slSdtAvailabilityStatus, &StorageTmp->slSdtAvailabilityStatusLen);
			cptr = read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->slSdtEquipmentPointer, &StorageTmp->slSdtEquipmentPointerLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->slSdtName, &StorageTmp->slSdtNameLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->slSdtProfilePointer, &StorageTmp->slSdtProfilePointerLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->slSdtRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("slMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct slSdlTable_data *slSdlTable_create(void)
 * @brief create a fresh data structure representing a new row in the slSdlTable table.
 *
 * Creates a new slSdlTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct slSdlTable_data *
slSdlTable_create(void)
{
	struct slSdlTable_data *StorageNew = SNMP_MALLOC_STRUCT(slSdlTable_data);

	DEBUGMSGTL(("slMIB", "slSdlTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->slSdlAdjPc = malloc(1)) == NULL)
			goto nomem;
		StorageNew->slSdlAdjPcLen = 0;
		StorageNew->slSdlAdjPc[StorageNew->slSdlAdjPcLen] = 0;
		StorageNew->slSdlTransmissionRate = 0;
		StorageNew->slSdlLoopDelay = 0;
		StorageNew->slSdlOperationalState = SLSDLOPERATIONALSTATE_DISABLED;
		if ((StorageNew->slSdlEquipmentPointer = snmp_duplicate_objid(zeroDotZero_oid, 2)) == NULL)
			goto nomem;
		StorageNew->slSdlEquipmentPointerLen = 2;
		StorageNew->slSdlCIC = 0;
		if ((StorageNew->slSdlName = malloc(1)) == NULL)
			goto nomem;
		StorageNew->slSdlNameLen = 0;
		StorageNew->slSdlName[StorageNew->slSdlNameLen] = 0;
		if ((StorageNew->slSdlStmChannel = malloc(1)) == NULL)
			goto nomem;
		StorageNew->slSdlStmChannelLen = 0;
		StorageNew->slSdlStmChannel[StorageNew->slSdlStmChannelLen] = 0;
		if ((StorageNew->slSdlVcTTpPointer = snmp_duplicate_objid(zeroDotZero_oid, 2)) == NULL)
			goto nomem;
		StorageNew->slSdlVcTTpPointerLen = 2;
		StorageNew->slSdlRowStatus = 0;
		StorageNew->slSdlRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("slMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	slSdlTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct slSdlTable_data *slSdlTable_duplicate(struct slSdlTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct slSdlTable_data *
slSdlTable_duplicate(struct slSdlTable_data *thedata)
{
	struct slSdlTable_data *StorageNew = SNMP_MALLOC_STRUCT(slSdlTable_data);

	DEBUGMSGTL(("slMIB", "slSdlTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->slSdlTable_id = thedata->slSdlTable_id;
		StorageNew->slSdlId = thedata->slSdlId;
		if (!(StorageNew->slSdlAdjPc = malloc(thedata->slSdlAdjPcLen + 1)))
			goto destroy;
		memcpy(StorageNew->slSdlAdjPc, thedata->slSdlAdjPc, thedata->slSdlAdjPcLen);
		StorageNew->slSdlAdjPcLen = thedata->slSdlAdjPcLen;
		StorageNew->slSdlAdjPc[StorageNew->slSdlAdjPcLen] = 0;
		StorageNew->slSdlTransmissionRate = thedata->slSdlTransmissionRate;
		StorageNew->slSdlLoopDelay = thedata->slSdlLoopDelay;
		StorageNew->slSdlOperationalState = thedata->slSdlOperationalState;
		if (!(StorageNew->slSdlEquipmentPointer = snmp_duplicate_objid(thedata->slSdlEquipmentPointer, thedata->slSdlEquipmentPointerLen / sizeof(oid))))
			goto destroy;
		StorageNew->slSdlEquipmentPointerLen = thedata->slSdlEquipmentPointerLen;
		StorageNew->slSdlCIC = thedata->slSdlCIC;
		if (!(StorageNew->slSdlName = malloc(thedata->slSdlNameLen + 1)))
			goto destroy;
		memcpy(StorageNew->slSdlName, thedata->slSdlName, thedata->slSdlNameLen);
		StorageNew->slSdlNameLen = thedata->slSdlNameLen;
		StorageNew->slSdlName[StorageNew->slSdlNameLen] = 0;
		if (!(StorageNew->slSdlStmChannel = malloc(thedata->slSdlStmChannelLen + 1)))
			goto destroy;
		memcpy(StorageNew->slSdlStmChannel, thedata->slSdlStmChannel, thedata->slSdlStmChannelLen);
		StorageNew->slSdlStmChannelLen = thedata->slSdlStmChannelLen;
		StorageNew->slSdlStmChannel[StorageNew->slSdlStmChannelLen] = 0;
		if (!(StorageNew->slSdlVcTTpPointer = snmp_duplicate_objid(thedata->slSdlVcTTpPointer, thedata->slSdlVcTTpPointerLen / sizeof(oid))))
			goto destroy;
		StorageNew->slSdlVcTTpPointerLen = thedata->slSdlVcTTpPointerLen;
		StorageNew->slSdlRowStatus = thedata->slSdlRowStatus;
	}
      done:
	DEBUGMSGTL(("slMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	slSdlTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int slSdlTable_destroy(struct slSdlTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
slSdlTable_destroy(struct slSdlTable_data **thedata)
{
	struct slSdlTable_data *StorageDel;

	DEBUGMSGTL(("slMIB", "slSdlTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->slSdlAdjPc);
		StorageDel->slSdlAdjPcLen = 0;
		SNMP_FREE(StorageDel->slSdlEquipmentPointer);
		StorageDel->slSdlEquipmentPointerLen = 0;
		SNMP_FREE(StorageDel->slSdlName);
		StorageDel->slSdlNameLen = 0;
		SNMP_FREE(StorageDel->slSdlStmChannel);
		StorageDel->slSdlStmChannelLen = 0;
		SNMP_FREE(StorageDel->slSdlVcTTpPointer);
		StorageDel->slSdlVcTTpPointerLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("slMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int slSdlTable_add(struct slSdlTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the slSdlTable table data set.
 *
 * Adds a table row structure to the slSdlTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
slSdlTable_add(struct slSdlTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("slMIB", "slSdlTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* slSdlId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->slSdlId, sizeof(thedata->slSdlId));
		header_complex_add_data(&slSdlTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("slMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int slSdlTable_del(struct slSdlTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the slSdlTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
slSdlTable_del(struct slSdlTable_data *thedata)
{
	struct slSdlTable_data *StorageDel;

	DEBUGMSGTL(("slMIB", "slSdlTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(slSdlTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&slSdlTableStorage, hciptr);
	}
	DEBUGMSGTL(("slMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_slSdlTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for slSdlTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case slSdlTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_slSdlTable(const char *token, char *line)
{
	size_t tmpsize;
	struct slSdlTable_data *StorageTmp = slSdlTable_create();

	DEBUGMSGTL(("slMIB", "parse_slSdlTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slSdlId, &tmpsize);
	SNMP_FREE(StorageTmp->slSdlAdjPc);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->slSdlAdjPc, &StorageTmp->slSdlAdjPcLen);
	if (StorageTmp->slSdlAdjPc == NULL) {
		config_perror("invalid specification for slSdlAdjPc");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->slSdlTransmissionRate, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->slSdlLoopDelay, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->slSdlOperationalState, &tmpsize);
	SNMP_FREE(StorageTmp->slSdlEquipmentPointer);
	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->slSdlEquipmentPointer, &StorageTmp->slSdlEquipmentPointerLen);
	if (StorageTmp->slSdlEquipmentPointer == NULL) {
		config_perror("invalid specification for slSdlEquipmentPointer");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->slSdlCIC, &tmpsize);
	SNMP_FREE(StorageTmp->slSdlName);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->slSdlName, &StorageTmp->slSdlNameLen);
	if (StorageTmp->slSdlName == NULL) {
		config_perror("invalid specification for slSdlName");
		return;
	}
	SNMP_FREE(StorageTmp->slSdlStmChannel);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->slSdlStmChannel, &StorageTmp->slSdlStmChannelLen);
	if (StorageTmp->slSdlStmChannel == NULL) {
		config_perror("invalid specification for slSdlStmChannel");
		return;
	}
	SNMP_FREE(StorageTmp->slSdlVcTTpPointer);
	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->slSdlVcTTpPointer, &StorageTmp->slSdlVcTTpPointerLen);
	if (StorageTmp->slSdlVcTTpPointer == NULL) {
		config_perror("invalid specification for slSdlVcTTpPointer");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->slSdlRowStatus, &tmpsize);
	slSdlTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("slMIB", "done.\n"));
}

/*
 * store_slSdlTable(): store configuraiton file for slSdlTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_slSdlTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct slSdlTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("slMIB", "store_slSdlTable: storing data...  "));
	refresh_slSdlTable(1);
	(void) tmpsize;
	for (hcindex = slSdlTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct slSdlTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "slSdlTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slSdlId, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->slSdlAdjPc, &StorageTmp->slSdlAdjPcLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->slSdlTransmissionRate, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->slSdlLoopDelay, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->slSdlOperationalState, &tmpsize);
			cptr = read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->slSdlEquipmentPointer, &StorageTmp->slSdlEquipmentPointerLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->slSdlCIC, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->slSdlName, &StorageTmp->slSdlNameLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->slSdlStmChannel, &StorageTmp->slSdlStmChannelLen);
			cptr = read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->slSdlVcTTpPointer, &StorageTmp->slSdlVcTTpPointerLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->slSdlRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("slMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int activate_slProfileTable_row(struct slProfileTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_slProfileTable_row(struct slProfileTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_slProfileTable_row(struct slProfileTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_slProfileTable_row(struct slProfileTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_slTable_row(struct slTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_slTable_row(struct slTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_slTable_row(struct slTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_slTable_row(struct slTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_slSdlListTable_row(struct slSdlListTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_slSdlListTable_row(struct slSdlListTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_slSdlListTable_row(struct slSdlListTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_slSdlListTable_row(struct slSdlListTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_slSdtListTable_row(struct slSdtListTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_slSdtListTable_row(struct slSdtListTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_slSdtListTable_row(struct slSdtListTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_slSdtListTable_row(struct slSdtListTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_slNbTable_row(struct slNbTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_slNbTable_row(struct slNbTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_slNbTable_row(struct slNbTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_slNbTable_row(struct slNbTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_slSaalTable_row(struct slSaalTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_slSaalTable_row(struct slSaalTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_slSaalTable_row(struct slSaalTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_slSaalTable_row(struct slSaalTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_slM2paTable_row(struct slM2paTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_slM2paTable_row(struct slM2paTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_slM2paTable_row(struct slM2paTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_slM2paTable_row(struct slM2paTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_slSdtTable_row(struct slSdtTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_slSdtTable_row(struct slSdtTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_slSdtTable_row(struct slSdtTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_slSdtTable_row(struct slSdtTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_slSdlTable_row(struct slSdlTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_slSdlTable_row(struct slSdlTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_slSdlTable_row(struct slSdlTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_slSdlTable_row(struct slSdlTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int check_slProfileTable_row(struct slProfileTable_data *StorageTmp, struct slProfileTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_slProfileTable_row(struct slProfileTable_data *StorageTmp, struct slProfileTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_slProfileTable_row(struct slProfileTable_data *StorageTmp, struct slProfileTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_slProfileTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_slProfileTable_row(struct slProfileTable_data *StorageTmp, struct slProfileTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	slProfileTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_slProfileTable_row(struct slProfileTable_data *StorageTmp, struct slProfileTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_slProfileTable_row(struct slProfileTable_data *StorageTmp, struct slProfileTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_slProfileTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_slProfileTable_row(struct slProfileTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the slProfileTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct slProfileTable_data *
refresh_slProfileTable_row(struct slProfileTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->slProfileTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->slProfileTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_slProfileTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the slProfileTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_slProfileTable(int force)
{
	if (!force && slProfileTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	slProfileTable_refresh = 0;
}

/**
 * @fn u_char *var_slProfileTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in slProfileTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_slMIB above.
 */
u_char *
var_slProfileTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct slProfileTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("slMIB", "var_slProfileTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_slProfileTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(slProfileTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_slProfileTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) SLPROFILEERRORCORRECTIONMETHOD:	/* Create */
		*write_method = write_slProfileErrorCorrectionMethod;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slProfileErrorCorrectionMethod);
		rval = (u_char *) &StorageTmp->slProfileErrorCorrectionMethod;
		break;
	case (u_char) SLPROFILEPROTOCOLVARIANT:	/* Create */
		*write_method = write_slProfileProtocolVariant;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->slProfileProtocolVariantLen * sizeof(oid);
		rval = (u_char *) StorageTmp->slProfileProtocolVariant;
		break;
	case (u_char) SLPROFILETIMERT1:	/* Create */
		*write_method = write_slProfileTimerT1;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slProfileTimerT1);
		rval = (u_char *) &StorageTmp->slProfileTimerT1;
		break;
	case (u_char) SLPROFILETIMERT2:	/* Create */
		*write_method = write_slProfileTimerT2;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slProfileTimerT2);
		rval = (u_char *) &StorageTmp->slProfileTimerT2;
		break;
	case (u_char) SLPROFILETIMERT2L:	/* Create */
		*write_method = write_slProfileTimerT2L;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slProfileTimerT2L);
		rval = (u_char *) &StorageTmp->slProfileTimerT2L;
		break;
	case (u_char) SLPROFILETIMERT2H:	/* Create */
		*write_method = write_slProfileTimerT2H;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slProfileTimerT2H);
		rval = (u_char *) &StorageTmp->slProfileTimerT2H;
		break;
	case (u_char) SLPROFILETIMERT3:	/* Create */
		*write_method = write_slProfileTimerT3;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slProfileTimerT3);
		rval = (u_char *) &StorageTmp->slProfileTimerT3;
		break;
	case (u_char) SLPROFILETIMERT4N:	/* Create */
		*write_method = write_slProfileTimerT4N;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slProfileTimerT4N);
		rval = (u_char *) &StorageTmp->slProfileTimerT4N;
		break;
	case (u_char) SLPROFILETIMERT4E:	/* Create */
		*write_method = write_slProfileTimerT4E;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slProfileTimerT4E);
		rval = (u_char *) &StorageTmp->slProfileTimerT4E;
		break;
	case (u_char) SLPROFILETIMERT5:	/* Create */
		*write_method = write_slProfileTimerT5;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slProfileTimerT5);
		rval = (u_char *) &StorageTmp->slProfileTimerT5;
		break;
	case (u_char) SLPROFILETIMERT6:	/* Create */
		*write_method = write_slProfileTimerT6;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slProfileTimerT6);
		rval = (u_char *) &StorageTmp->slProfileTimerT6;
		break;
	case (u_char) SLPROFILETIMERT7:	/* Create */
		*write_method = write_slProfileTimerT7;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slProfileTimerT7);
		rval = (u_char *) &StorageTmp->slProfileTimerT7;
		break;
	case (u_char) SLPROFILETBONSET1:	/* Create */
		*write_method = write_slProfileTbOnset1;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slProfileTbOnset1);
		rval = (u_char *) &StorageTmp->slProfileTbOnset1;
		break;
	case (u_char) SLPROFILETBABATE1:	/* Create */
		*write_method = write_slProfileTbAbate1;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slProfileTbAbate1);
		rval = (u_char *) &StorageTmp->slProfileTbAbate1;
		break;
	case (u_char) SLPROFILENUMBEROFTHRESHOLDLEVELS:	/* Create */
		*write_method = write_slProfileNumberOfThresholdLevels;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slProfileNumberOfThresholdLevels);
		rval = (u_char *) &StorageTmp->slProfileNumberOfThresholdLevels;
		break;
	case (u_char) SLPROFILECONGESTIONCOUNTING:	/* Create */
		*write_method = write_slProfileCongestionCounting;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slProfileCongestionCounting);
		rval = (u_char *) &StorageTmp->slProfileCongestionCounting;
		break;
	case (u_char) SLPROFILECONGESTIONREPBASEOBJECT:	/* Create */
		*write_method = write_slProfileCongestionRepBaseObject;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slProfileCongestionRepBaseObject);
		rval = (u_char *) &StorageTmp->slProfileCongestionRepBaseObject;
		break;
	case (u_char) SLPROFILELOOPDELAY:	/* Create */
		*write_method = write_slProfileLoopDelay;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slProfileLoopDelay);
		rval = (u_char *) &StorageTmp->slProfileLoopDelay;
		break;
	case (u_char) SLPROFILENAME:	/* Create */
		*write_method = write_slProfileName;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->slProfileNameLen;
		rval = (u_char *) StorageTmp->slProfileName;
		break;
	case (u_char) SLPROFILETBONSET2:	/* Create */
		*write_method = write_slProfileTbOnset2;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slProfileTbOnset2);
		rval = (u_char *) &StorageTmp->slProfileTbOnset2;
		break;
	case (u_char) SLPROFILETBABATE2:	/* Create */
		*write_method = write_slProfileTbAbate2;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slProfileTbAbate2);
		rval = (u_char *) &StorageTmp->slProfileTbAbate2;
		break;
	case (u_char) SLPROFILETBONSET3:	/* Create */
		*write_method = write_slProfileTbOnset3;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slProfileTbOnset3);
		rval = (u_char *) &StorageTmp->slProfileTbOnset3;
		break;
	case (u_char) SLPROFILETBABATE3:	/* Create */
		*write_method = write_slProfileTbAbate3;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slProfileTbAbate3);
		rval = (u_char *) &StorageTmp->slProfileTbAbate3;
		break;
	case (u_char) SLPROFILETBDISCARD1:	/* Create */
		*write_method = write_slProfileTbDiscard1;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slProfileTbDiscard1);
		rval = (u_char *) &StorageTmp->slProfileTbDiscard1;
		break;
	case (u_char) SLPROFILETBDISCARD2:	/* Create */
		*write_method = write_slProfileTbDiscard2;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slProfileTbDiscard2);
		rval = (u_char *) &StorageTmp->slProfileTbDiscard2;
		break;
	case (u_char) SLPROFILETBDISCARD3:	/* Create */
		*write_method = write_slProfileTbDiscard3;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slProfileTbDiscard3);
		rval = (u_char *) &StorageTmp->slProfileTbDiscard3;
		break;
	case (u_char) SLPROFILETIMERTX:	/* Create */
		*write_method = write_slProfileTimerTx;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slProfileTimerTx);
		rval = (u_char *) &StorageTmp->slProfileTimerTx;
		break;
	case (u_char) SLPROFILETIMERTY:	/* Create */
		*write_method = write_slProfileTimerTy;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slProfileTimerTy);
		rval = (u_char *) &StorageTmp->slProfileTimerTy;
		break;
	case (u_char) SLPROFILENUMOFCONGESTIONSTATES:	/* Create */
		*write_method = write_slProfileNumOfCongestionStates;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slProfileNumOfCongestionStates);
		rval = (u_char *) &StorageTmp->slProfileNumOfCongestionStates;
		break;
	case (u_char) SLPROFILEINITLEVELOFCONGESTION:	/* Create */
		*write_method = write_slProfileInitLevelOfCongestion;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slProfileInitLevelOfCongestion);
		rval = (u_char *) &StorageTmp->slProfileInitLevelOfCongestion;
		break;
	case (u_char) SLPROFILEMAXMSUSRETRANSN1:	/* Create */
		*write_method = write_slProfileMaxMSUsRetransN1;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slProfileMaxMSUsRetransN1);
		rval = (u_char *) &StorageTmp->slProfileMaxMSUsRetransN1;
		break;
	case (u_char) SLPROFILEMAXOCTRETRANSN2:	/* Create */
		*write_method = write_slProfileMaxOctRetransN2;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slProfileMaxOctRetransN2);
		rval = (u_char *) &StorageTmp->slProfileMaxOctRetransN2;
		break;
	case (u_char) SLPROFILERECEIVECONGTHRESHONSET:	/* Create */
		*write_method = write_slProfileReceiveCongThreshOnset;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slProfileReceiveCongThreshOnset);
		rval = (u_char *) &StorageTmp->slProfileReceiveCongThreshOnset;
		break;
	case (u_char) SLPROFILERECEIVECONGTHRESHABATE:	/* Create */
		*write_method = write_slProfileReceiveCongThreshAbate;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slProfileReceiveCongThreshAbate);
		rval = (u_char *) &StorageTmp->slProfileReceiveCongThreshAbate;
		break;
	case (u_char) SLPROFILERECEIVECONGTHRESHDISC:	/* Create */
		*write_method = write_slProfileReceiveCongThreshDisc;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slProfileReceiveCongThreshDisc);
		rval = (u_char *) &StorageTmp->slProfileReceiveCongThreshDisc;
		break;
	case (u_char) SLPROFILEM:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slProfileM);
		rval = (u_char *) &StorageTmp->slProfileM;
		break;
	case (u_char) SLPROFILEROWSTATUS:	/* Create */
		*write_method = write_slProfileRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slProfileRowStatus);
		rval = (u_char *) &StorageTmp->slProfileRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_slTable_row(struct slTable_data *StorageTmp, struct slTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_slTable_row(struct slTable_data *StorageTmp, struct slTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_slTable_row(struct slTable_data *StorageTmp, struct slTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_slTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_slTable_row(struct slTable_data *StorageTmp, struct slTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	slTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_slTable_row(struct slTable_data *StorageTmp, struct slTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_slTable_row(struct slTable_data *StorageTmp, struct slTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_slTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_slTable_row(struct slTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the slTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct slTable_data *
refresh_slTable_row(struct slTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->slTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->slTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_slTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the slTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_slTable(int force)
{
	if (!force && slTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	slTable_refresh = 0;
}

/**
 * @fn u_char *var_slTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in slTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_slMIB above.
 */
u_char *
var_slTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct slTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("slMIB", "var_slTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_slTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(slTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_slTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) SLMAXCAPACITYSL:	/* Create */
		*write_method = write_slMaxCapacitySL;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slMaxCapacitySL);
		rval = (u_char *) &StorageTmp->slMaxCapacitySL;
		break;
	case (u_char) SLPROCEDURALSTATUS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->slProceduralStatusLen;
		rval = (u_char *) StorageTmp->slProceduralStatus;
		break;
	case (u_char) SLPROFILEPOINTER:	/* Create */
		*write_method = write_slProfilePointer;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->slProfilePointerLen;
		rval = (u_char *) StorageTmp->slProfilePointer;
		break;
	case (u_char) SLSIGNTERMPOINTER:	/* Create */
		*write_method = write_slSignTermPointer;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->slSignTermPointerLen;
		rval = (u_char *) StorageTmp->slSignTermPointer;
		break;
	case (u_char) SLSIGNDATALINKTPPOINTER:	/* Create */
		*write_method = write_slSignDataLinkTpPointer;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->slSignDataLinkTpPointerLen;
		rval = (u_char *) StorageTmp->slSignDataLinkTpPointer;
		break;
	case (u_char) SLREPLACEST:	/* Create */
		*write_method = write_slReplaceSt;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slReplaceSt);
		rval = (u_char *) &StorageTmp->slReplaceSt;
		break;
	case (u_char) SLCONGESTIONLEVEL:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slCongestionLevel);
		rval = (u_char *) &StorageTmp->slCongestionLevel;
		break;
	case (u_char) SLRELATEDLINKGROUPNUMBER:	/* Create */
		*write_method = write_slRelatedLinkGroupNumber;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slRelatedLinkGroupNumber);
		rval = (u_char *) &StorageTmp->slRelatedLinkGroupNumber;
		break;
	case (u_char) SLSDLLIST:	/* Create */
		*write_method = write_slSdlList;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slSdlList);
		rval = (u_char *) &StorageTmp->slSdlList;
		break;
	case (u_char) SLNAME:	/* Create */
		*write_method = write_slName;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->slNameLen;
		rval = (u_char *) StorageTmp->slName;
		break;
	case (u_char) SLSDTLIST:	/* Create */
		*write_method = write_slSdtList;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slSdtList);
		rval = (u_char *) &StorageTmp->slSdtList;
		break;
	case (u_char) SLROWSTATUS:	/* Create */
		*write_method = write_slRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slRowStatus);
		rval = (u_char *) &StorageTmp->slRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_slSdlListTable_row(struct slSdlListTable_data *StorageTmp, struct slSdlListTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_slSdlListTable_row(struct slSdlListTable_data *StorageTmp, struct slSdlListTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_slSdlListTable_row(struct slSdlListTable_data *StorageTmp, struct slSdlListTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_slSdlListTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_slSdlListTable_row(struct slSdlListTable_data *StorageTmp, struct slSdlListTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	slSdlListTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_slSdlListTable_row(struct slSdlListTable_data *StorageTmp, struct slSdlListTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_slSdlListTable_row(struct slSdlListTable_data *StorageTmp, struct slSdlListTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_slSdlListTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_slSdlListTable_row(struct slSdlListTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the slSdlListTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct slSdlListTable_data *
refresh_slSdlListTable_row(struct slSdlListTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->slSdlListTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->slSdlListTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_slSdlListTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the slSdlListTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_slSdlListTable(int force)
{
	if (!force && slSdlListTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	slSdlListTable_refresh = 0;
}

/**
 * @fn u_char *var_slSdlListTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in slSdlListTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_slMIB above.
 */
u_char *
var_slSdlListTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct slSdlListTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("slMIB", "var_slSdlListTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_slSdlListTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(slSdlListTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_slSdlListTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) SLSDLLISTPOINTER:	/* Create */
		*write_method = write_slSdlListPointer;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->slSdlListPointerLen;
		rval = (u_char *) StorageTmp->slSdlListPointer;
		break;
	case (u_char) SLSDLLISTROWSTATUS:	/* Create */
		*write_method = write_slSdlListRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slSdlListRowStatus);
		rval = (u_char *) &StorageTmp->slSdlListRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_slSdtListTable_row(struct slSdtListTable_data *StorageTmp, struct slSdtListTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_slSdtListTable_row(struct slSdtListTable_data *StorageTmp, struct slSdtListTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_slSdtListTable_row(struct slSdtListTable_data *StorageTmp, struct slSdtListTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_slSdtListTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_slSdtListTable_row(struct slSdtListTable_data *StorageTmp, struct slSdtListTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	slSdtListTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_slSdtListTable_row(struct slSdtListTable_data *StorageTmp, struct slSdtListTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_slSdtListTable_row(struct slSdtListTable_data *StorageTmp, struct slSdtListTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_slSdtListTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_slSdtListTable_row(struct slSdtListTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the slSdtListTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct slSdtListTable_data *
refresh_slSdtListTable_row(struct slSdtListTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->slSdtListTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->slSdtListTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_slSdtListTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the slSdtListTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_slSdtListTable(int force)
{
	if (!force && slSdtListTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	slSdtListTable_refresh = 0;
}

/**
 * @fn u_char *var_slSdtListTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in slSdtListTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_slMIB above.
 */
u_char *
var_slSdtListTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct slSdtListTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("slMIB", "var_slSdtListTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_slSdtListTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(slSdtListTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_slSdtListTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) SLSDTLISTPOINTER:	/* Create */
		*write_method = write_slSdtListPointer;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->slSdtListPointerLen;
		rval = (u_char *) StorageTmp->slSdtListPointer;
		break;
	case (u_char) SLSDTLISTROWSTATUS:	/* Create */
		*write_method = write_slSdtListRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slSdtListRowStatus);
		rval = (u_char *) &StorageTmp->slSdtListRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_slNbTable_row(struct slNbTable_data *StorageTmp, struct slNbTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_slNbTable_row(struct slNbTable_data *StorageTmp, struct slNbTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_slNbTable_row(struct slNbTable_data *StorageTmp, struct slNbTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_slNbTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_slNbTable_row(struct slNbTable_data *StorageTmp, struct slNbTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	slNbTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_slNbTable_row(struct slNbTable_data *StorageTmp, struct slNbTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_slNbTable_row(struct slNbTable_data *StorageTmp, struct slNbTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_slNbTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_slNbTable_row(struct slNbTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the slNbTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct slNbTable_data *
refresh_slNbTable_row(struct slNbTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->slNbTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->slNbTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_slNbTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the slNbTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_slNbTable(int force)
{
	if (!force && slNbTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	slNbTable_refresh = 0;
}

/**
 * @fn u_char *var_slNbTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in slNbTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_slMIB above.
 */
u_char *
var_slNbTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct slNbTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("slMIB", "var_slNbTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_slNbTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(slNbTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_slNbTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) SLNBTRANSMISSIONRATE:	/* Create */
		*write_method = write_slNbTransmissionRate;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slNbTransmissionRate);
		rval = (u_char *) &StorageTmp->slNbTransmissionRate;
		break;
	case (u_char) SLNBTIMERT8:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slNbTimerT8);
		rval = (u_char *) &StorageTmp->slNbTimerT8;
		break;
	case (u_char) SLNBTIN:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slNbTin);
		rval = (u_char *) &StorageTmp->slNbTin;
		break;
	case (u_char) SLNBTIE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slNbTie);
		rval = (u_char *) &StorageTmp->slNbTie;
		break;
	case (u_char) SLNBT:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slNbT);
		rval = (u_char *) &StorageTmp->slNbT;
		break;
	case (u_char) SLNBD:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slNbD);
		rval = (u_char *) &StorageTmp->slNbD;
		break;
	case (u_char) SLNBTE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slNbTe);
		rval = (u_char *) &StorageTmp->slNbTe;
		break;
	case (u_char) SLNBDE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slNbDe);
		rval = (u_char *) &StorageTmp->slNbDe;
		break;
	case (u_char) SLNBUE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slNbUe);
		rval = (u_char *) &StorageTmp->slNbUe;
		break;
	case (u_char) SLNBN:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slNbN);
		rval = (u_char *) &StorageTmp->slNbN;
		break;
	case (u_char) SLNBM:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slNbm);
		rval = (u_char *) &StorageTmp->slNbm;
		break;
	case (u_char) SLNBB:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slNbb);
		rval = (u_char *) &StorageTmp->slNbb;
		break;
	case (u_char) SLNBF:	/* Create */
		*write_method = write_slNbf;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slNbf);
		rval = (u_char *) &StorageTmp->slNbf;
		break;
	case (u_char) SLNBROWSTATUS:	/* Create */
		*write_method = write_slNbRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slNbRowStatus);
		rval = (u_char *) &StorageTmp->slNbRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_slSaalTable_row(struct slSaalTable_data *StorageTmp, struct slSaalTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_slSaalTable_row(struct slSaalTable_data *StorageTmp, struct slSaalTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_slSaalTable_row(struct slSaalTable_data *StorageTmp, struct slSaalTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_slSaalTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_slSaalTable_row(struct slSaalTable_data *StorageTmp, struct slSaalTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	slSaalTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_slSaalTable_row(struct slSaalTable_data *StorageTmp, struct slSaalTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_slSaalTable_row(struct slSaalTable_data *StorageTmp, struct slSaalTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_slSaalTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_slSaalTable_row(struct slSaalTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the slSaalTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct slSaalTable_data *
refresh_slSaalTable_row(struct slSaalTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->slSaalTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->slSaalTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_slSaalTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the slSaalTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_slSaalTable(int force)
{
	if (!force && slSaalTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	slSaalTable_refresh = 0;
}

/**
 * @fn u_char *var_slSaalTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in slSaalTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_slMIB above.
 */
u_char *
var_slSaalTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct slSaalTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("slMIB", "var_slSaalTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_slSaalTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(slSaalTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_slSaalTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) SLSAALBUFFERRELEASE:	/* Create */
		*write_method = write_slSaalBufferRelease;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slSaalBufferRelease);
		rval = (u_char *) &StorageTmp->slSaalBufferRelease;
		break;
	case (u_char) SLSAALMAXCC:	/* Create */
		*write_method = write_slSaalMaxCc;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slSaalMaxCc);
		rval = (u_char *) &StorageTmp->slSaalMaxCc;
		break;
	case (u_char) SLSAALMAXNRP:	/* Create */
		*write_method = write_slSaalMaxNrp;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slSaalMaxNrp);
		rval = (u_char *) &StorageTmp->slSaalMaxNrp;
		break;
	case (u_char) SLSAALMAXINFORMATIONFIELDLENGTH:	/* Create */
		*write_method = write_slSaalMaxInformationFieldLength;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slSaalMaxInformationFieldLength);
		rval = (u_char *) &StorageTmp->slSaalMaxInformationFieldLength;
		break;
	case (u_char) SLSAALMAXLENGTHSSCOPUUFIELD:	/* Create */
		*write_method = write_slSaalMaxLengthSscopUuField;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slSaalMaxLengthSscopUuField);
		rval = (u_char *) &StorageTmp->slSaalMaxLengthSscopUuField;
		break;
	case (u_char) SLSAALMAXPD:	/* Create */
		*write_method = write_slSaalMaxPd;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slSaalMaxPd);
		rval = (u_char *) &StorageTmp->slSaalMaxPd;
		break;
	case (u_char) SLSAALMAXSSCOPCREDITTOPEER:	/* Create */
		*write_method = write_slSaalMaxSscopCreditToPeer;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slSaalMaxSscopCreditToPeer);
		rval = (u_char *) &StorageTmp->slSaalMaxSscopCreditToPeer;
		break;
	case (u_char) SLSAALMAXSTAT:	/* Create */
		*write_method = write_slSaalMaxStat;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slSaalMaxStat);
		rval = (u_char *) &StorageTmp->slSaalMaxStat;
		break;
	case (u_char) SLSAALN1:	/* Create */
		*write_method = write_slSaalN1;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slSaalN1);
		rval = (u_char *) &StorageTmp->slSaalN1;
		break;
	case (u_char) SLSAALNNILAYERMGRPROVINGSTATE:	/* Create */
		*write_method = write_slSaalNniLayerMgrProvingState;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slSaalNniLayerMgrProvingState);
		rval = (u_char *) &StorageTmp->slSaalNniLayerMgrProvingState;
		break;
	case (u_char) SLSAALNNILAYERMGRTIMERNOCREDIT:	/* Create */
		*write_method = write_slSaalNniLayerMgrTimerNoCredit;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slSaalNniLayerMgrTimerNoCredit);
		rval = (u_char *) &StorageTmp->slSaalNniLayerMgrTimerNoCredit;
		break;
	case (u_char) SLSAALNNILAYERMGRTIMERREPEATSREC:	/* Create */
		*write_method = write_slSaalNniLayerMgrTimerRepeatSrec;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slSaalNniLayerMgrTimerRepeatSrec);
		rval = (u_char *) &StorageTmp->slSaalNniLayerMgrTimerRepeatSrec;
		break;
	case (u_char) SLSAALNNITIMERT1:	/* Create */
		*write_method = write_slSaalNniTimerT1;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slSaalNniTimerT1);
		rval = (u_char *) &StorageTmp->slSaalNniTimerT1;
		break;
	case (u_char) SLSAALNNITIMERT2:	/* Create */
		*write_method = write_slSaalNniTimerT2;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slSaalNniTimerT2);
		rval = (u_char *) &StorageTmp->slSaalNniTimerT2;
		break;
	case (u_char) SLSAALNNITIMERT3:	/* Create */
		*write_method = write_slSaalNniTimerT3;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slSaalNniTimerT3);
		rval = (u_char *) &StorageTmp->slSaalNniTimerT3;
		break;
	case (u_char) SLSAALSSCOPTIMERCC:	/* Create */
		*write_method = write_slSaalSscopTimerCc;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slSaalSscopTimerCc);
		rval = (u_char *) &StorageTmp->slSaalSscopTimerCc;
		break;
	case (u_char) SLSAALSSCOPTIMERIDLE:	/* Create */
		*write_method = write_slSaalSscopTimerIdle;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slSaalSscopTimerIdle);
		rval = (u_char *) &StorageTmp->slSaalSscopTimerIdle;
		break;
	case (u_char) SLSAALSSCOPTIMERKEEPALIVE:	/* Create */
		*write_method = write_slSaalSscopTimerKeepAlive;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slSaalSscopTimerKeepAlive);
		rval = (u_char *) &StorageTmp->slSaalSscopTimerKeepAlive;
		break;
	case (u_char) SLSAALSSCOPTIMERNORESPONSE:	/* Create */
		*write_method = write_slSaalSscopTimerNoResponse;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slSaalSscopTimerNoResponse);
		rval = (u_char *) &StorageTmp->slSaalSscopTimerNoResponse;
		break;
	case (u_char) SLSAALSSCOPTIMERPOLL:	/* Create */
		*write_method = write_slSaalSscopTimerPoll;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slSaalSscopTimerPoll);
		rval = (u_char *) &StorageTmp->slSaalSscopTimerPoll;
		break;
	case (u_char) SLSAALTRANSRATEINTERVALLOWER:	/* Create */
		*write_method = write_slSaalTransRateIntervalLower;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slSaalTransRateIntervalLower);
		rval = (u_char *) &StorageTmp->slSaalTransRateIntervalLower;
		break;
	case (u_char) SLSAALTRANSRATEINTERVALUPPER:	/* Create */
		*write_method = write_slSaalTransRateIntervalUpper;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slSaalTransRateIntervalUpper);
		rval = (u_char *) &StorageTmp->slSaalTransRateIntervalUpper;
		break;
	case (u_char) SLSAALEGRESSTRANSRATEINTVLLOWER:	/* Create */
		*write_method = write_slSaalEgressTransRateIntvlLower;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slSaalEgressTransRateIntvlLower);
		rval = (u_char *) &StorageTmp->slSaalEgressTransRateIntvlLower;
		break;
	case (u_char) SLSAALEGRESSTRANSRATEINTVLUPPER:	/* Create */
		*write_method = write_slSaalEgressTransRateIntvlUpper;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slSaalEgressTransRateIntvlUpper);
		rval = (u_char *) &StorageTmp->slSaalEgressTransRateIntvlUpper;
		break;
	case (u_char) SLSAALPOLLAFTERRETRANSMISSION:	/* Create */
		*write_method = write_slSaalPollAfterRetransmission;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slSaalPollAfterRetransmission);
		rval = (u_char *) &StorageTmp->slSaalPollAfterRetransmission;
		break;
	case (u_char) SLSAALROWSTATUS:	/* Create */
		*write_method = write_slSaalRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slSaalRowStatus);
		rval = (u_char *) &StorageTmp->slSaalRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_slM2paTable_row(struct slM2paTable_data *StorageTmp, struct slM2paTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_slM2paTable_row(struct slM2paTable_data *StorageTmp, struct slM2paTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_slM2paTable_row(struct slM2paTable_data *StorageTmp, struct slM2paTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_slM2paTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_slM2paTable_row(struct slM2paTable_data *StorageTmp, struct slM2paTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	slM2paTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_slM2paTable_row(struct slM2paTable_data *StorageTmp, struct slM2paTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_slM2paTable_row(struct slM2paTable_data *StorageTmp, struct slM2paTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_slM2paTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_slM2paTable_row(struct slM2paTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the slM2paTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct slM2paTable_data *
refresh_slM2paTable_row(struct slM2paTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->slM2paTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->slM2paTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_slM2paTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the slM2paTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_slM2paTable(int force)
{
	if (!force && slM2paTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	slM2paTable_refresh = 0;
}

/**
 * @fn u_char *var_slM2paTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in slM2paTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_slMIB above.
 */
u_char *
var_slM2paTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct slM2paTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("slMIB", "var_slM2paTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_slM2paTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(slM2paTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_slM2paTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) SLM2PAN1:	/* Create */
		*write_method = write_slM2paN1;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slM2paN1);
		rval = (u_char *) &StorageTmp->slM2paN1;
		break;
	case (u_char) SLM2PAPROVING:	/* Create */
		*write_method = write_slM2paProving;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slM2paProving);
		rval = (u_char *) &StorageTmp->slM2paProving;
		break;
	case (u_char) SLM2PAMANAGEMENTPROVINGSTATE:	/* Create */
		*write_method = write_slM2paManagementProvingState;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slM2paManagementProvingState);
		rval = (u_char *) &StorageTmp->slM2paManagementProvingState;
		break;
	case (u_char) SLM2PALOOPDELAYLOWER:	/* Create */
		*write_method = write_slM2paLoopDelayLower;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slM2paLoopDelayLower);
		rval = (u_char *) &StorageTmp->slM2paLoopDelayLower;
		break;
	case (u_char) SLM2PALOOPDELAYUPPER:	/* Create */
		*write_method = write_slM2paLoopDelayUpper;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slM2paLoopDelayUpper);
		rval = (u_char *) &StorageTmp->slM2paLoopDelayUpper;
		break;
	case (u_char) SLM2PATRANSRATEINTERVALLOWER:	/* Create */
		*write_method = write_slM2paTransRateIntervalLower;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slM2paTransRateIntervalLower);
		rval = (u_char *) &StorageTmp->slM2paTransRateIntervalLower;
		break;
	case (u_char) SLM2PATRANSRATEINTERVALUPPER:	/* Create */
		*write_method = write_slM2paTransRateIntervalUpper;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slM2paTransRateIntervalUpper);
		rval = (u_char *) &StorageTmp->slM2paTransRateIntervalUpper;
		break;
	case (u_char) SLM2PASCTPNODELAY:	/* Create */
		*write_method = write_slM2paSctpNoDelay;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slM2paSctpNoDelay);
		rval = (u_char *) &StorageTmp->slM2paSctpNoDelay;
		break;
	case (u_char) SLM2PASCTPMAXSEG:	/* Create */
		*write_method = write_slM2paSctpMaxseg;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slM2paSctpMaxseg);
		rval = (u_char *) &StorageTmp->slM2paSctpMaxseg;
		break;
	case (u_char) SLM2PASCTPHEARTBEATITVL:	/* Create */
		*write_method = write_slM2paSctpHeartbeatItvl;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slM2paSctpHeartbeatItvl);
		rval = (u_char *) &StorageTmp->slM2paSctpHeartbeatItvl;
		break;
	case (u_char) SLM2PASCTPHEARTBEAT:	/* Create */
		*write_method = write_slM2paSctpHeartbeat;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slM2paSctpHeartbeat);
		rval = (u_char *) &StorageTmp->slM2paSctpHeartbeat;
		break;
	case (u_char) SLM2PASCTPRTOINITIAL:	/* Create */
		*write_method = write_slM2paSctpRtoInitial;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slM2paSctpRtoInitial);
		rval = (u_char *) &StorageTmp->slM2paSctpRtoInitial;
		break;
	case (u_char) SLM2PASCTPRTOMIN:	/* Create */
		*write_method = write_slM2paSctpRtoMin;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slM2paSctpRtoMin);
		rval = (u_char *) &StorageTmp->slM2paSctpRtoMin;
		break;
	case (u_char) SLM2PASCTPRTOMAX:	/* Create */
		*write_method = write_slM2paSctpRtoMax;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slM2paSctpRtoMax);
		rval = (u_char *) &StorageTmp->slM2paSctpRtoMax;
		break;
	case (u_char) SLM2PASCTPPATHMAXRETRANS:	/* Create */
		*write_method = write_slM2paSctpPathMaxRetrans;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slM2paSctpPathMaxRetrans);
		rval = (u_char *) &StorageTmp->slM2paSctpPathMaxRetrans;
		break;
	case (u_char) SLM2PASCTPCOOKIELIFE:	/* Create */
		*write_method = write_slM2paSctpCookieLife;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slM2paSctpCookieLife);
		rval = (u_char *) &StorageTmp->slM2paSctpCookieLife;
		break;
	case (u_char) SLM2PASCTPCOOKIEINC:	/* Create */
		*write_method = write_slM2paSctpCookieInc;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slM2paSctpCookieInc);
		rval = (u_char *) &StorageTmp->slM2paSctpCookieInc;
		break;
	case (u_char) SLM2PASCTPMAXINITRETRIES:	/* Create */
		*write_method = write_slM2paSctpMaxInitRetries;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slM2paSctpMaxInitRetries);
		rval = (u_char *) &StorageTmp->slM2paSctpMaxInitRetries;
		break;
	case (u_char) SLM2PASCTPMAXBURST:	/* Create */
		*write_method = write_slM2paSctpMaxBurst;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slM2paSctpMaxBurst);
		rval = (u_char *) &StorageTmp->slM2paSctpMaxBurst;
		break;
	case (u_char) SLM2PASCTPASSOCMAXRETRANS:	/* Create */
		*write_method = write_slM2paSctpAssocMaxRetrans;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slM2paSctpAssocMaxRetrans);
		rval = (u_char *) &StorageTmp->slM2paSctpAssocMaxRetrans;
		break;
	case (u_char) SLM2PASCTPSACKDELAY:	/* Create */
		*write_method = write_slM2paSctpSackDelay;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slM2paSctpSackDelay);
		rval = (u_char *) &StorageTmp->slM2paSctpSackDelay;
		break;
	case (u_char) SLM2PASCTPLIFETIME:	/* Create */
		*write_method = write_slM2paSctpLifetime;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slM2paSctpLifetime);
		rval = (u_char *) &StorageTmp->slM2paSctpLifetime;
		break;
	case (u_char) SLM2PAPROVINGATTEMPTS:	/* Create */
		*write_method = write_slM2paProvingAttempts;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slM2paProvingAttempts);
		rval = (u_char *) &StorageTmp->slM2paProvingAttempts;
		break;
	case (u_char) SLM2PAROWSTATUS:	/* Create */
		*write_method = write_slM2paRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slM2paRowStatus);
		rval = (u_char *) &StorageTmp->slM2paRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_slSdtTable_row(struct slSdtTable_data *StorageTmp, struct slSdtTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_slSdtTable_row(struct slSdtTable_data *StorageTmp, struct slSdtTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_slSdtTable_row(struct slSdtTable_data *StorageTmp, struct slSdtTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_slSdtTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_slSdtTable_row(struct slSdtTable_data *StorageTmp, struct slSdtTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	slSdtTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_slSdtTable_row(struct slSdtTable_data *StorageTmp, struct slSdtTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_slSdtTable_row(struct slSdtTable_data *StorageTmp, struct slSdtTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_slSdtTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_slSdtTable_row(struct slSdtTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the slSdtTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct slSdtTable_data *
refresh_slSdtTable_row(struct slSdtTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->slSdtTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->slSdtTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_slSdtTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the slSdtTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_slSdtTable(int force)
{
	if (!force && slSdtTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	slSdtTable_refresh = 0;
}

/**
 * @fn u_char *var_slSdtTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in slSdtTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_slMIB above.
 */
u_char *
var_slSdtTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct slSdtTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("slMIB", "var_slSdtTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_slSdtTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(slSdtTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_slSdtTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) SLSDTADMINISTRATIVESTATE:	/* Create */
		*write_method = write_slSdtAdministrativeState;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slSdtAdministrativeState);
		rval = (u_char *) &StorageTmp->slSdtAdministrativeState;
		break;
	case (u_char) SLSDTOPERATIONALSTATE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slSdtOperationalState);
		rval = (u_char *) &StorageTmp->slSdtOperationalState;
		break;
	case (u_char) SLSDTUSAGESTATE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slSdtUsageState);
		rval = (u_char *) &StorageTmp->slSdtUsageState;
		break;
	case (u_char) SLSDTPROCEDURALSTATUS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->slSdtProceduralStatusLen;
		rval = (u_char *) StorageTmp->slSdtProceduralStatus;
		break;
	case (u_char) SLSDTAVAILABILITYSTATUS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->slSdtAvailabilityStatusLen;
		rval = (u_char *) StorageTmp->slSdtAvailabilityStatus;
		break;
	case (u_char) SLSDTEQUIPMENTPOINTER:	/* Create */
		*write_method = write_slSdtEquipmentPointer;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->slSdtEquipmentPointerLen * sizeof(oid);
		rval = (u_char *) StorageTmp->slSdtEquipmentPointer;
		break;
	case (u_char) SLSDTNAME:	/* Create */
		*write_method = write_slSdtName;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->slSdtNameLen;
		rval = (u_char *) StorageTmp->slSdtName;
		break;
	case (u_char) SLSDTPROFILEPOINTER:	/* Create */
		*write_method = write_slSdtProfilePointer;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->slSdtProfilePointerLen;
		rval = (u_char *) StorageTmp->slSdtProfilePointer;
		break;
	case (u_char) SLSDTROWSTATUS:	/* Create */
		*write_method = write_slSdtRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slSdtRowStatus);
		rval = (u_char *) &StorageTmp->slSdtRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_slSdlTable_row(struct slSdlTable_data *StorageTmp, struct slSdlTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_slSdlTable_row(struct slSdlTable_data *StorageTmp, struct slSdlTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_slSdlTable_row(struct slSdlTable_data *StorageTmp, struct slSdlTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_slSdlTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_slSdlTable_row(struct slSdlTable_data *StorageTmp, struct slSdlTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	slSdlTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_slSdlTable_row(struct slSdlTable_data *StorageTmp, struct slSdlTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_slSdlTable_row(struct slSdlTable_data *StorageTmp, struct slSdlTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_slSdlTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_slSdlTable_row(struct slSdlTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the slSdlTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct slSdlTable_data *
refresh_slSdlTable_row(struct slSdlTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->slSdlTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->slSdlTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_slSdlTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the slSdlTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_slSdlTable(int force)
{
	if (!force && slSdlTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	slSdlTable_refresh = 0;
}

/**
 * @fn u_char *var_slSdlTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in slSdlTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_slMIB above.
 */
u_char *
var_slSdlTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct slSdlTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("slMIB", "var_slSdlTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_slSdlTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(slSdlTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_slSdlTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) SLSDLADJPC:	/* Create */
		*write_method = write_slSdlAdjPc;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->slSdlAdjPcLen;
		rval = (u_char *) StorageTmp->slSdlAdjPc;
		break;
	case (u_char) SLSDLTRANSMISSIONRATE:	/* Create */
		*write_method = write_slSdlTransmissionRate;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slSdlTransmissionRate);
		rval = (u_char *) &StorageTmp->slSdlTransmissionRate;
		break;
	case (u_char) SLSDLLOOPDELAY:	/* Create */
		*write_method = write_slSdlLoopDelay;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slSdlLoopDelay);
		rval = (u_char *) &StorageTmp->slSdlLoopDelay;
		break;
	case (u_char) SLSDLOPERATIONALSTATE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slSdlOperationalState);
		rval = (u_char *) &StorageTmp->slSdlOperationalState;
		break;
	case (u_char) SLSDLEQUIPMENTPOINTER:	/* Create */
		*write_method = write_slSdlEquipmentPointer;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->slSdlEquipmentPointerLen * sizeof(oid);
		rval = (u_char *) StorageTmp->slSdlEquipmentPointer;
		break;
	case (u_char) SLSDLCIC:	/* Create */
		*write_method = write_slSdlCIC;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slSdlCIC);
		rval = (u_char *) &StorageTmp->slSdlCIC;
		break;
	case (u_char) SLSDLNAME:	/* Create */
		*write_method = write_slSdlName;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->slSdlNameLen;
		rval = (u_char *) StorageTmp->slSdlName;
		break;
	case (u_char) SLSDLSTMCHANNEL:	/* Create */
		*write_method = write_slSdlStmChannel;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->slSdlStmChannelLen;
		rval = (u_char *) StorageTmp->slSdlStmChannel;
		break;
	case (u_char) SLSDLVCTTPPOINTER:	/* Create */
		*write_method = write_slSdlVcTTpPointer;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->slSdlVcTTpPointerLen * sizeof(oid);
		rval = (u_char *) StorageTmp->slSdlVcTTpPointer;
		break;
	case (u_char) SLSDLROWSTATUS:	/* Create */
		*write_method = write_slSdlRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->slSdlRowStatus);
		rval = (u_char *) &StorageTmp->slSdlRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int write_slProfileErrorCorrectionMethod(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slProfileErrorCorrectionMethod(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slProfileTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slProfileErrorCorrectionMethod entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slProfileTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileErrorCorrectionMethod: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileErrorCorrectionMethod not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileErrorCorrectionMethod: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value basicRetr */
		switch (set_value) {
		case SLPROFILEERRORCORRECTIONMETHOD_BASICRETR:
		case SLPROFILEERRORCORRECTIONMETHOD_PREVCYCLRETR:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileErrorCorrectionMethod: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			if (StorageTmp->slProfileTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slProfileTable_old = slProfileTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slProfileTable_rsvs++;
		StorageTmp->slProfileErrorCorrectionMethod = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slProfileTable_tsts == 0)
				if ((ret = check_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slProfileErrorCorrectionMethod for you to use, and you have just been asked to do something with it.  Note that anything 
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slProfileTable_sets == 0)
				if ((ret = update_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
			StorageTmp->slProfileTable_rsvs = 0;
			StorageTmp->slProfileTable_tsts = 0;
			StorageTmp->slProfileTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slProfileTable_sets == 0)
			revert_slProfileTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		StorageTmp->slProfileErrorCorrectionMethod = StorageOld->slProfileErrorCorrectionMethod;
		if (--StorageTmp->slProfileTable_rsvs == 0)
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slProfileProtocolVariant(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slProfileProtocolVariant(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slProfileTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	oid *objid = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slProfileProtocolVariant entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slProfileTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileProtocolVariant: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OBJECT_ID) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileProtocolVariant not ASN_OBJECT_ID\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (MIN_OID_LEN * sizeof(oid) > var_val_len || var_val_len > MAX_OID_LEN * sizeof(oid)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileProtocolVariant: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			if (StorageTmp->slProfileTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slProfileTable_old = slProfileTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slProfileTable_rsvs++;
		if ((objid = snmp_duplicate_objid((void *) var_val, var_val_len / sizeof(oid))) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		SNMP_FREE(StorageTmp->slProfileProtocolVariant);
		StorageTmp->slProfileProtocolVariant = objid;
		StorageTmp->slProfileProtocolVariantLen = var_val_len / sizeof(oid);
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slProfileTable_tsts == 0)
				if ((ret = check_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slProfileProtocolVariant for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slProfileTable_sets == 0)
				if ((ret = update_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
			StorageTmp->slProfileTable_rsvs = 0;
			StorageTmp->slProfileTable_tsts = 0;
			StorageTmp->slProfileTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slProfileTable_sets == 0)
			revert_slProfileTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		if (StorageOld->slProfileProtocolVariant != NULL) {
			SNMP_FREE(StorageTmp->slProfileProtocolVariant);
			StorageTmp->slProfileProtocolVariant = StorageOld->slProfileProtocolVariant;
			StorageTmp->slProfileProtocolVariantLen = StorageOld->slProfileProtocolVariantLen;
			StorageOld->slProfileProtocolVariant = NULL;
			StorageOld->slProfileProtocolVariantLen = 0;
		}
		if (--StorageTmp->slProfileTable_rsvs == 0)
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slProfileTimerT1(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slProfileTimerT1(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slProfileTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slProfileTimerT1 entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slProfileTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTimerT1: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTimerT1 not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTimerT1: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 4500 */
		/* Note: ranges 1290..60000 */
		if ((1290 > set_value || set_value > 60000)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTimerT1: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			if (StorageTmp->slProfileTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slProfileTable_old = slProfileTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slProfileTable_rsvs++;
		StorageTmp->slProfileTimerT1 = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slProfileTable_tsts == 0)
				if ((ret = check_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slProfileTimerT1 for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slProfileTable_sets == 0)
				if ((ret = update_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
			StorageTmp->slProfileTable_rsvs = 0;
			StorageTmp->slProfileTable_tsts = 0;
			StorageTmp->slProfileTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slProfileTable_sets == 0)
			revert_slProfileTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		StorageTmp->slProfileTimerT1 = StorageOld->slProfileTimerT1;
		if (--StorageTmp->slProfileTable_rsvs == 0)
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slProfileTimerT2(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slProfileTimerT2(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slProfileTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slProfileTimerT2 entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slProfileTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTimerT2: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTimerT2 not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTimerT2: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 500 */
		/* Note: ranges 500..15000 */
		if ((500 > set_value || set_value > 15000)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTimerT2: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			if (StorageTmp->slProfileTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slProfileTable_old = slProfileTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slProfileTable_rsvs++;
		StorageTmp->slProfileTimerT2 = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slProfileTable_tsts == 0)
				if ((ret = check_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slProfileTimerT2 for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slProfileTable_sets == 0)
				if ((ret = update_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
			StorageTmp->slProfileTable_rsvs = 0;
			StorageTmp->slProfileTable_tsts = 0;
			StorageTmp->slProfileTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slProfileTable_sets == 0)
			revert_slProfileTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		StorageTmp->slProfileTimerT2 = StorageOld->slProfileTimerT2;
		if (--StorageTmp->slProfileTable_rsvs == 0)
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slProfileTimerT2L(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slProfileTimerT2L(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slProfileTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slProfileTimerT2L entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slProfileTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTimerT2L: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTimerT2L not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTimerT2L: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 2000 */
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTimerT2L: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			if (StorageTmp->slProfileTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slProfileTable_old = slProfileTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slProfileTable_rsvs++;
		StorageTmp->slProfileTimerT2L = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slProfileTable_tsts == 0)
				if ((ret = check_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slProfileTimerT2L for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slProfileTable_sets == 0)
				if ((ret = update_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
			StorageTmp->slProfileTable_rsvs = 0;
			StorageTmp->slProfileTable_tsts = 0;
			StorageTmp->slProfileTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slProfileTable_sets == 0)
			revert_slProfileTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		StorageTmp->slProfileTimerT2L = StorageOld->slProfileTimerT2L;
		if (--StorageTmp->slProfileTable_rsvs == 0)
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slProfileTimerT2H(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slProfileTimerT2H(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slProfileTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slProfileTimerT2H entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slProfileTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTimerT2H: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTimerT2H not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTimerT2H: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 10000 */
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTimerT2H: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			if (StorageTmp->slProfileTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slProfileTable_old = slProfileTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slProfileTable_rsvs++;
		StorageTmp->slProfileTimerT2H = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slProfileTable_tsts == 0)
				if ((ret = check_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slProfileTimerT2H for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slProfileTable_sets == 0)
				if ((ret = update_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
			StorageTmp->slProfileTable_rsvs = 0;
			StorageTmp->slProfileTable_tsts = 0;
			StorageTmp->slProfileTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slProfileTable_sets == 0)
			revert_slProfileTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		StorageTmp->slProfileTimerT2H = StorageOld->slProfileTimerT2H;
		if (--StorageTmp->slProfileTable_rsvs == 0)
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slProfileTimerT3(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slProfileTimerT3(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slProfileTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slProfileTimerT3 entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slProfileTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTimerT3: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTimerT3 not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTimerT3: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 100 */
		/* Note: ranges 100..1400 */
		if ((100 > set_value || set_value > 1400)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTimerT3: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			if (StorageTmp->slProfileTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slProfileTable_old = slProfileTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slProfileTable_rsvs++;
		StorageTmp->slProfileTimerT3 = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slProfileTable_tsts == 0)
				if ((ret = check_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slProfileTimerT3 for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slProfileTable_sets == 0)
				if ((ret = update_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
			StorageTmp->slProfileTable_rsvs = 0;
			StorageTmp->slProfileTable_tsts = 0;
			StorageTmp->slProfileTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slProfileTable_sets == 0)
			revert_slProfileTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		StorageTmp->slProfileTimerT3 = StorageOld->slProfileTimerT3;
		if (--StorageTmp->slProfileTable_rsvs == 0)
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slProfileTimerT4N(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slProfileTimerT4N(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slProfileTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slProfileTimerT4N entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slProfileTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTimerT4N: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTimerT4N not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTimerT4N: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 800 */
		/* Note: ranges 230..12000 */
		if ((230 > set_value || set_value > 12000)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTimerT4N: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			if (StorageTmp->slProfileTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slProfileTable_old = slProfileTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slProfileTable_rsvs++;
		StorageTmp->slProfileTimerT4N = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slProfileTable_tsts == 0)
				if ((ret = check_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slProfileTimerT4N for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slProfileTable_sets == 0)
				if ((ret = update_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
			StorageTmp->slProfileTable_rsvs = 0;
			StorageTmp->slProfileTable_tsts = 0;
			StorageTmp->slProfileTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slProfileTable_sets == 0)
			revert_slProfileTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		StorageTmp->slProfileTimerT4N = StorageOld->slProfileTimerT4N;
		if (--StorageTmp->slProfileTable_rsvs == 0)
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slProfileTimerT4E(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slProfileTimerT4E(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slProfileTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slProfileTimerT4E entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slProfileTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTimerT4E: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTimerT4E not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTimerT4E: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 50 */
		/* Note: ranges 40..800 */
		if ((40 > set_value || set_value > 800)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTimerT4E: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			if (StorageTmp->slProfileTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slProfileTable_old = slProfileTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slProfileTable_rsvs++;
		StorageTmp->slProfileTimerT4E = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slProfileTable_tsts == 0)
				if ((ret = check_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slProfileTimerT4E for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slProfileTable_sets == 0)
				if ((ret = update_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
			StorageTmp->slProfileTable_rsvs = 0;
			StorageTmp->slProfileTable_tsts = 0;
			StorageTmp->slProfileTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slProfileTable_sets == 0)
			revert_slProfileTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		StorageTmp->slProfileTimerT4E = StorageOld->slProfileTimerT4E;
		if (--StorageTmp->slProfileTable_rsvs == 0)
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slProfileTimerT5(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slProfileTimerT5(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slProfileTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slProfileTimerT5 entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slProfileTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTimerT5: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTimerT5 not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTimerT5: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 10 */
		/* Note: ranges 8..12 */
		if ((8 > set_value || set_value > 12)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTimerT5: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			if (StorageTmp->slProfileTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slProfileTable_old = slProfileTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slProfileTable_rsvs++;
		StorageTmp->slProfileTimerT5 = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slProfileTable_tsts == 0)
				if ((ret = check_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slProfileTimerT5 for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slProfileTable_sets == 0)
				if ((ret = update_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
			StorageTmp->slProfileTable_rsvs = 0;
			StorageTmp->slProfileTable_tsts = 0;
			StorageTmp->slProfileTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slProfileTable_sets == 0)
			revert_slProfileTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		StorageTmp->slProfileTimerT5 = StorageOld->slProfileTimerT5;
		if (--StorageTmp->slProfileTable_rsvs == 0)
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slProfileTimerT6(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slProfileTimerT6(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slProfileTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slProfileTimerT6 entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slProfileTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTimerT6: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTimerT6 not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTimerT6: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 400 */
		/* Note: ranges 300..1200 */
		if ((300 > set_value || set_value > 1200)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTimerT6: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			if (StorageTmp->slProfileTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slProfileTable_old = slProfileTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slProfileTable_rsvs++;
		StorageTmp->slProfileTimerT6 = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slProfileTable_tsts == 0)
				if ((ret = check_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slProfileTimerT6 for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slProfileTable_sets == 0)
				if ((ret = update_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
			StorageTmp->slProfileTable_rsvs = 0;
			StorageTmp->slProfileTable_tsts = 0;
			StorageTmp->slProfileTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slProfileTable_sets == 0)
			revert_slProfileTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		StorageTmp->slProfileTimerT6 = StorageOld->slProfileTimerT6;
		if (--StorageTmp->slProfileTable_rsvs == 0)
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slProfileTimerT7(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slProfileTimerT7(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slProfileTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slProfileTimerT7 entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slProfileTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTimerT7: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTimerT7 not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTimerT7: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 100 */
		/* Note: ranges 50..600 */
		if ((50 > set_value || set_value > 600)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTimerT7: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			if (StorageTmp->slProfileTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slProfileTable_old = slProfileTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slProfileTable_rsvs++;
		StorageTmp->slProfileTimerT7 = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slProfileTable_tsts == 0)
				if ((ret = check_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slProfileTimerT7 for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slProfileTable_sets == 0)
				if ((ret = update_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
			StorageTmp->slProfileTable_rsvs = 0;
			StorageTmp->slProfileTable_tsts = 0;
			StorageTmp->slProfileTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slProfileTable_sets == 0)
			revert_slProfileTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		StorageTmp->slProfileTimerT7 = StorageOld->slProfileTimerT7;
		if (--StorageTmp->slProfileTable_rsvs == 0)
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slProfileTbOnset1(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slProfileTbOnset1(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slProfileTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slProfileTbOnset1 entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slProfileTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTbOnset1: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTbOnset1 not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTbOnset1: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 69632 */
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			if (StorageTmp->slProfileTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slProfileTable_old = slProfileTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slProfileTable_rsvs++;
		StorageTmp->slProfileTbOnset1 = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slProfileTable_tsts == 0)
				if ((ret = check_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slProfileTbOnset1 for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slProfileTable_sets == 0)
				if ((ret = update_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
			StorageTmp->slProfileTable_rsvs = 0;
			StorageTmp->slProfileTable_tsts = 0;
			StorageTmp->slProfileTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slProfileTable_sets == 0)
			revert_slProfileTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		StorageTmp->slProfileTbOnset1 = StorageOld->slProfileTbOnset1;
		if (--StorageTmp->slProfileTable_rsvs == 0)
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slProfileTbAbate1(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slProfileTbAbate1(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slProfileTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slProfileTbAbate1 entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slProfileTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTbAbate1: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTbAbate1 not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTbAbate1: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 34816 */
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			if (StorageTmp->slProfileTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slProfileTable_old = slProfileTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slProfileTable_rsvs++;
		StorageTmp->slProfileTbAbate1 = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slProfileTable_tsts == 0)
				if ((ret = check_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slProfileTbAbate1 for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slProfileTable_sets == 0)
				if ((ret = update_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
			StorageTmp->slProfileTable_rsvs = 0;
			StorageTmp->slProfileTable_tsts = 0;
			StorageTmp->slProfileTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slProfileTable_sets == 0)
			revert_slProfileTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		StorageTmp->slProfileTbAbate1 = StorageOld->slProfileTbAbate1;
		if (--StorageTmp->slProfileTable_rsvs == 0)
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slProfileNumberOfThresholdLevels(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slProfileNumberOfThresholdLevels(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slProfileTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slProfileNumberOfThresholdLevels entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slProfileTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileNumberOfThresholdLevels: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileNumberOfThresholdLevels not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileNumberOfThresholdLevels: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 1 */
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			if (StorageTmp->slProfileTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slProfileTable_old = slProfileTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slProfileTable_rsvs++;
		StorageTmp->slProfileNumberOfThresholdLevels = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slProfileTable_tsts == 0)
				if ((ret = check_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slProfileNumberOfThresholdLevels for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slProfileTable_sets == 0)
				if ((ret = update_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
			StorageTmp->slProfileTable_rsvs = 0;
			StorageTmp->slProfileTable_tsts = 0;
			StorageTmp->slProfileTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slProfileTable_sets == 0)
			revert_slProfileTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		StorageTmp->slProfileNumberOfThresholdLevels = StorageOld->slProfileNumberOfThresholdLevels;
		if (--StorageTmp->slProfileTable_rsvs == 0)
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slProfileCongestionCounting(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slProfileCongestionCounting(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slProfileTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slProfileCongestionCounting entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slProfileTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileCongestionCounting: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileCongestionCounting not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileCongestionCounting: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value octet */
		switch (set_value) {
		case SLPROFILECONGESTIONCOUNTING_OCTET:
		case SLPROFILECONGESTIONCOUNTING_MESSAGE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileCongestionCounting: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			if (StorageTmp->slProfileTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slProfileTable_old = slProfileTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slProfileTable_rsvs++;
		StorageTmp->slProfileCongestionCounting = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slProfileTable_tsts == 0)
				if ((ret = check_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slProfileCongestionCounting for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slProfileTable_sets == 0)
				if ((ret = update_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
			StorageTmp->slProfileTable_rsvs = 0;
			StorageTmp->slProfileTable_tsts = 0;
			StorageTmp->slProfileTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slProfileTable_sets == 0)
			revert_slProfileTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		StorageTmp->slProfileCongestionCounting = StorageOld->slProfileCongestionCounting;
		if (--StorageTmp->slProfileTable_rsvs == 0)
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slProfileCongestionRepBaseObject(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slProfileCongestionRepBaseObject(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slProfileTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slProfileCongestionRepBaseObject entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slProfileTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileCongestionRepBaseObject: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileCongestionRepBaseObject not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileCongestionRepBaseObject: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value routeSet */
		switch (set_value) {
		case SLPROFILECONGESTIONREPBASEOBJECT_ROUTESET:
		case SLPROFILECONGESTIONREPBASEOBJECT_LINKSETOFCONGESTEDROUTESET:
		case SLPROFILECONGESTIONREPBASEOBJECT_LINKOFCONGESTEDROUTESET:
		case SLPROFILECONGESTIONREPBASEOBJECT_CONGESTEDLINKOFCONGESTEDROUTE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileCongestionRepBaseObject: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			if (StorageTmp->slProfileTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slProfileTable_old = slProfileTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slProfileTable_rsvs++;
		StorageTmp->slProfileCongestionRepBaseObject = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slProfileTable_tsts == 0)
				if ((ret = check_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slProfileCongestionRepBaseObject for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slProfileTable_sets == 0)
				if ((ret = update_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
			StorageTmp->slProfileTable_rsvs = 0;
			StorageTmp->slProfileTable_tsts = 0;
			StorageTmp->slProfileTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slProfileTable_sets == 0)
			revert_slProfileTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		StorageTmp->slProfileCongestionRepBaseObject = StorageOld->slProfileCongestionRepBaseObject;
		if (--StorageTmp->slProfileTable_rsvs == 0)
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slProfileLoopDelay(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slProfileLoopDelay(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slProfileTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slProfileLoopDelay entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slProfileTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileLoopDelay: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileLoopDelay not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileLoopDelay: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			if (StorageTmp->slProfileTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slProfileTable_old = slProfileTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slProfileTable_rsvs++;
		StorageTmp->slProfileLoopDelay = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slProfileTable_tsts == 0)
				if ((ret = check_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slProfileLoopDelay for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slProfileTable_sets == 0)
				if ((ret = update_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
			StorageTmp->slProfileTable_rsvs = 0;
			StorageTmp->slProfileTable_tsts = 0;
			StorageTmp->slProfileTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slProfileTable_sets == 0)
			revert_slProfileTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		StorageTmp->slProfileLoopDelay = StorageOld->slProfileLoopDelay;
		if (--StorageTmp->slProfileTable_rsvs == 0)
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slProfileName(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slProfileName(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slProfileTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slProfileName entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slProfileTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileName: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileName not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 1..32 */
		if (var_val_len > SPRINT_MAX_LEN || ((1 > var_val_len || var_val_len > 32))) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileName: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			if (StorageTmp->slProfileTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slProfileTable_old = slProfileTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slProfileTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->slProfileName);
		StorageTmp->slProfileName = string;
		StorageTmp->slProfileNameLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slProfileTable_tsts == 0)
				if ((ret = check_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slProfileName for you to use, and you have just been asked to do something with it.  Note that anything done here must
				   be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slProfileTable_sets == 0)
				if ((ret = update_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
			StorageTmp->slProfileTable_rsvs = 0;
			StorageTmp->slProfileTable_tsts = 0;
			StorageTmp->slProfileTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slProfileTable_sets == 0)
			revert_slProfileTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		if (StorageOld->slProfileName != NULL) {
			SNMP_FREE(StorageTmp->slProfileName);
			StorageTmp->slProfileName = StorageOld->slProfileName;
			StorageTmp->slProfileNameLen = StorageOld->slProfileNameLen;
			StorageOld->slProfileName = NULL;
			StorageOld->slProfileNameLen = 0;
		}
		if (--StorageTmp->slProfileTable_rsvs == 0)
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slProfileTbOnset2(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slProfileTbOnset2(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slProfileTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slProfileTbOnset2 entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slProfileTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTbOnset2: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTbOnset2 not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTbOnset2: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 174080 */
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			if (StorageTmp->slProfileTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slProfileTable_old = slProfileTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slProfileTable_rsvs++;
		StorageTmp->slProfileTbOnset2 = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slProfileTable_tsts == 0)
				if ((ret = check_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slProfileTbOnset2 for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slProfileTable_sets == 0)
				if ((ret = update_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
			StorageTmp->slProfileTable_rsvs = 0;
			StorageTmp->slProfileTable_tsts = 0;
			StorageTmp->slProfileTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slProfileTable_sets == 0)
			revert_slProfileTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		StorageTmp->slProfileTbOnset2 = StorageOld->slProfileTbOnset2;
		if (--StorageTmp->slProfileTable_rsvs == 0)
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slProfileTbAbate2(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slProfileTbAbate2(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slProfileTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slProfileTbAbate2 entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slProfileTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTbAbate2: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTbAbate2 not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTbAbate2: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 139264 */
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			if (StorageTmp->slProfileTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slProfileTable_old = slProfileTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slProfileTable_rsvs++;
		StorageTmp->slProfileTbAbate2 = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slProfileTable_tsts == 0)
				if ((ret = check_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slProfileTbAbate2 for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slProfileTable_sets == 0)
				if ((ret = update_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
			StorageTmp->slProfileTable_rsvs = 0;
			StorageTmp->slProfileTable_tsts = 0;
			StorageTmp->slProfileTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slProfileTable_sets == 0)
			revert_slProfileTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		StorageTmp->slProfileTbAbate2 = StorageOld->slProfileTbAbate2;
		if (--StorageTmp->slProfileTable_rsvs == 0)
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slProfileTbOnset3(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slProfileTbOnset3(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slProfileTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slProfileTbOnset3 entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slProfileTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTbOnset3: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTbOnset3 not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTbOnset3: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 278528 */
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			if (StorageTmp->slProfileTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slProfileTable_old = slProfileTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slProfileTable_rsvs++;
		StorageTmp->slProfileTbOnset3 = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slProfileTable_tsts == 0)
				if ((ret = check_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slProfileTbOnset3 for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slProfileTable_sets == 0)
				if ((ret = update_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
			StorageTmp->slProfileTable_rsvs = 0;
			StorageTmp->slProfileTable_tsts = 0;
			StorageTmp->slProfileTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slProfileTable_sets == 0)
			revert_slProfileTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		StorageTmp->slProfileTbOnset3 = StorageOld->slProfileTbOnset3;
		if (--StorageTmp->slProfileTable_rsvs == 0)
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slProfileTbAbate3(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slProfileTbAbate3(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slProfileTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slProfileTbAbate3 entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slProfileTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTbAbate3: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTbAbate3 not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTbAbate3: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 243712 */
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			if (StorageTmp->slProfileTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slProfileTable_old = slProfileTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slProfileTable_rsvs++;
		StorageTmp->slProfileTbAbate3 = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slProfileTable_tsts == 0)
				if ((ret = check_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slProfileTbAbate3 for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slProfileTable_sets == 0)
				if ((ret = update_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
			StorageTmp->slProfileTable_rsvs = 0;
			StorageTmp->slProfileTable_tsts = 0;
			StorageTmp->slProfileTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slProfileTable_sets == 0)
			revert_slProfileTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		StorageTmp->slProfileTbAbate3 = StorageOld->slProfileTbAbate3;
		if (--StorageTmp->slProfileTable_rsvs == 0)
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slProfileTbDiscard1(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slProfileTbDiscard1(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slProfileTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slProfileTbDiscard1 entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slProfileTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTbDiscard1: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTbDiscard1 not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTbDiscard1: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 104448 */
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			if (StorageTmp->slProfileTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slProfileTable_old = slProfileTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slProfileTable_rsvs++;
		StorageTmp->slProfileTbDiscard1 = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slProfileTable_tsts == 0)
				if ((ret = check_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slProfileTbDiscard1 for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slProfileTable_sets == 0)
				if ((ret = update_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
			StorageTmp->slProfileTable_rsvs = 0;
			StorageTmp->slProfileTable_tsts = 0;
			StorageTmp->slProfileTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slProfileTable_sets == 0)
			revert_slProfileTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		StorageTmp->slProfileTbDiscard1 = StorageOld->slProfileTbDiscard1;
		if (--StorageTmp->slProfileTable_rsvs == 0)
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slProfileTbDiscard2(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slProfileTbDiscard2(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slProfileTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slProfileTbDiscard2 entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slProfileTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTbDiscard2: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTbDiscard2 not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTbDiscard2: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 208896 */
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			if (StorageTmp->slProfileTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slProfileTable_old = slProfileTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slProfileTable_rsvs++;
		StorageTmp->slProfileTbDiscard2 = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slProfileTable_tsts == 0)
				if ((ret = check_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slProfileTbDiscard2 for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slProfileTable_sets == 0)
				if ((ret = update_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
			StorageTmp->slProfileTable_rsvs = 0;
			StorageTmp->slProfileTable_tsts = 0;
			StorageTmp->slProfileTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slProfileTable_sets == 0)
			revert_slProfileTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		StorageTmp->slProfileTbDiscard2 = StorageOld->slProfileTbDiscard2;
		if (--StorageTmp->slProfileTable_rsvs == 0)
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slProfileTbDiscard3(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slProfileTbDiscard3(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slProfileTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slProfileTbDiscard3 entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slProfileTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTbDiscard3: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTbDiscard3 not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTbDiscard3: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 313344 */
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			if (StorageTmp->slProfileTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slProfileTable_old = slProfileTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slProfileTable_rsvs++;
		StorageTmp->slProfileTbDiscard3 = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slProfileTable_tsts == 0)
				if ((ret = check_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slProfileTbDiscard3 for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slProfileTable_sets == 0)
				if ((ret = update_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
			StorageTmp->slProfileTable_rsvs = 0;
			StorageTmp->slProfileTable_tsts = 0;
			StorageTmp->slProfileTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slProfileTable_sets == 0)
			revert_slProfileTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		StorageTmp->slProfileTbDiscard3 = StorageOld->slProfileTbDiscard3;
		if (--StorageTmp->slProfileTable_rsvs == 0)
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slProfileTimerTx(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slProfileTimerTx(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slProfileTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slProfileTimerTx entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slProfileTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTimerTx: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTimerTx not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTimerTx: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTimerTx: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			if (StorageTmp->slProfileTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slProfileTable_old = slProfileTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slProfileTable_rsvs++;
		StorageTmp->slProfileTimerTx = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slProfileTable_tsts == 0)
				if ((ret = check_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slProfileTimerTx for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slProfileTable_sets == 0)
				if ((ret = update_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
			StorageTmp->slProfileTable_rsvs = 0;
			StorageTmp->slProfileTable_tsts = 0;
			StorageTmp->slProfileTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slProfileTable_sets == 0)
			revert_slProfileTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		StorageTmp->slProfileTimerTx = StorageOld->slProfileTimerTx;
		if (--StorageTmp->slProfileTable_rsvs == 0)
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slProfileTimerTy(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slProfileTimerTy(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slProfileTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slProfileTimerTy entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slProfileTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTimerTy: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTimerTy not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTimerTy: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileTimerTy: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			if (StorageTmp->slProfileTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slProfileTable_old = slProfileTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slProfileTable_rsvs++;
		StorageTmp->slProfileTimerTy = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slProfileTable_tsts == 0)
				if ((ret = check_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slProfileTimerTy for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slProfileTable_sets == 0)
				if ((ret = update_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
			StorageTmp->slProfileTable_rsvs = 0;
			StorageTmp->slProfileTable_tsts = 0;
			StorageTmp->slProfileTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slProfileTable_sets == 0)
			revert_slProfileTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		StorageTmp->slProfileTimerTy = StorageOld->slProfileTimerTy;
		if (--StorageTmp->slProfileTable_rsvs == 0)
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slProfileNumOfCongestionStates(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slProfileNumOfCongestionStates(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slProfileTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slProfileNumOfCongestionStates entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slProfileTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileNumOfCongestionStates: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileNumOfCongestionStates not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileNumOfCongestionStates: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			if (StorageTmp->slProfileTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slProfileTable_old = slProfileTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slProfileTable_rsvs++;
		StorageTmp->slProfileNumOfCongestionStates = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slProfileTable_tsts == 0)
				if ((ret = check_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slProfileNumOfCongestionStates for you to use, and you have just been asked to do something with it.  Note that anything 
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slProfileTable_sets == 0)
				if ((ret = update_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
			StorageTmp->slProfileTable_rsvs = 0;
			StorageTmp->slProfileTable_tsts = 0;
			StorageTmp->slProfileTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slProfileTable_sets == 0)
			revert_slProfileTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		StorageTmp->slProfileNumOfCongestionStates = StorageOld->slProfileNumOfCongestionStates;
		if (--StorageTmp->slProfileTable_rsvs == 0)
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slProfileInitLevelOfCongestion(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slProfileInitLevelOfCongestion(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slProfileTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slProfileInitLevelOfCongestion entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slProfileTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileInitLevelOfCongestion: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileInitLevelOfCongestion not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileInitLevelOfCongestion: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			if (StorageTmp->slProfileTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slProfileTable_old = slProfileTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slProfileTable_rsvs++;
		StorageTmp->slProfileInitLevelOfCongestion = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slProfileTable_tsts == 0)
				if ((ret = check_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slProfileInitLevelOfCongestion for you to use, and you have just been asked to do something with it.  Note that anything 
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slProfileTable_sets == 0)
				if ((ret = update_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
			StorageTmp->slProfileTable_rsvs = 0;
			StorageTmp->slProfileTable_tsts = 0;
			StorageTmp->slProfileTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slProfileTable_sets == 0)
			revert_slProfileTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		StorageTmp->slProfileInitLevelOfCongestion = StorageOld->slProfileInitLevelOfCongestion;
		if (--StorageTmp->slProfileTable_rsvs == 0)
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slProfileMaxMSUsRetransN1(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slProfileMaxMSUsRetransN1(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slProfileTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slProfileMaxMSUsRetransN1 entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slProfileTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileMaxMSUsRetransN1: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileMaxMSUsRetransN1 not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileMaxMSUsRetransN1: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 127 */
		/* Note: ranges 0..16777215 */
		if ((0 > set_value || set_value > 16777215)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileMaxMSUsRetransN1: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			if (StorageTmp->slProfileTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slProfileTable_old = slProfileTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slProfileTable_rsvs++;
		StorageTmp->slProfileMaxMSUsRetransN1 = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slProfileTable_tsts == 0)
				if ((ret = check_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slProfileMaxMSUsRetransN1 for you to use, and you have just been asked to do something with it.  Note that anything done 
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slProfileTable_sets == 0)
				if ((ret = update_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
			StorageTmp->slProfileTable_rsvs = 0;
			StorageTmp->slProfileTable_tsts = 0;
			StorageTmp->slProfileTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slProfileTable_sets == 0)
			revert_slProfileTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		StorageTmp->slProfileMaxMSUsRetransN1 = StorageOld->slProfileMaxMSUsRetransN1;
		if (--StorageTmp->slProfileTable_rsvs == 0)
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slProfileMaxOctRetransN2(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slProfileMaxOctRetransN2(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slProfileTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slProfileMaxOctRetransN2 entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slProfileTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileMaxOctRetransN2: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileMaxOctRetransN2 not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileMaxOctRetransN2: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 8192 */
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			if (StorageTmp->slProfileTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slProfileTable_old = slProfileTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slProfileTable_rsvs++;
		StorageTmp->slProfileMaxOctRetransN2 = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slProfileTable_tsts == 0)
				if ((ret = check_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slProfileMaxOctRetransN2 for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slProfileTable_sets == 0)
				if ((ret = update_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
			StorageTmp->slProfileTable_rsvs = 0;
			StorageTmp->slProfileTable_tsts = 0;
			StorageTmp->slProfileTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slProfileTable_sets == 0)
			revert_slProfileTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		StorageTmp->slProfileMaxOctRetransN2 = StorageOld->slProfileMaxOctRetransN2;
		if (--StorageTmp->slProfileTable_rsvs == 0)
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slProfileReceiveCongThreshOnset(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slProfileReceiveCongThreshOnset(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slProfileTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slProfileReceiveCongThreshOnset entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slProfileTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileReceiveCongThreshOnset: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileReceiveCongThreshOnset not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileReceiveCongThreshOnset: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 6 */
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			if (StorageTmp->slProfileTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slProfileTable_old = slProfileTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slProfileTable_rsvs++;
		StorageTmp->slProfileReceiveCongThreshOnset = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slProfileTable_tsts == 0)
				if ((ret = check_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slProfileReceiveCongThreshOnset for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slProfileTable_sets == 0)
				if ((ret = update_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
			StorageTmp->slProfileTable_rsvs = 0;
			StorageTmp->slProfileTable_tsts = 0;
			StorageTmp->slProfileTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slProfileTable_sets == 0)
			revert_slProfileTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		StorageTmp->slProfileReceiveCongThreshOnset = StorageOld->slProfileReceiveCongThreshOnset;
		if (--StorageTmp->slProfileTable_rsvs == 0)
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slProfileReceiveCongThreshAbate(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slProfileReceiveCongThreshAbate(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slProfileTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slProfileReceiveCongThreshAbate entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slProfileTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileReceiveCongThreshAbate: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileReceiveCongThreshAbate not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileReceiveCongThreshAbate: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 3 */
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			if (StorageTmp->slProfileTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slProfileTable_old = slProfileTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slProfileTable_rsvs++;
		StorageTmp->slProfileReceiveCongThreshAbate = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slProfileTable_tsts == 0)
				if ((ret = check_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slProfileReceiveCongThreshAbate for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slProfileTable_sets == 0)
				if ((ret = update_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
			StorageTmp->slProfileTable_rsvs = 0;
			StorageTmp->slProfileTable_tsts = 0;
			StorageTmp->slProfileTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slProfileTable_sets == 0)
			revert_slProfileTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		StorageTmp->slProfileReceiveCongThreshAbate = StorageOld->slProfileReceiveCongThreshAbate;
		if (--StorageTmp->slProfileTable_rsvs == 0)
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slProfileReceiveCongThreshDisc(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slProfileReceiveCongThreshDisc(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slProfileTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slProfileReceiveCongThreshDisc entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slProfileTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slProfileRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileReceiveCongThreshDisc: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileReceiveCongThreshDisc not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileReceiveCongThreshDisc: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 9 */
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			if (StorageTmp->slProfileTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slProfileTable_old = slProfileTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slProfileTable_rsvs++;
		StorageTmp->slProfileReceiveCongThreshDisc = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slProfileTable_tsts == 0)
				if ((ret = check_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slProfileReceiveCongThreshDisc for you to use, and you have just been asked to do something with it.  Note that anything 
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slProfileTable_sets == 0)
				if ((ret = update_slProfileTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slProfileTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
			StorageTmp->slProfileTable_rsvs = 0;
			StorageTmp->slProfileTable_tsts = 0;
			StorageTmp->slProfileTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slProfileTable_sets == 0)
			revert_slProfileTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
			break;
		StorageTmp->slProfileReceiveCongThreshDisc = StorageOld->slProfileReceiveCongThreshDisc;
		if (--StorageTmp->slProfileTable_rsvs == 0)
			slProfileTable_destroy(&StorageTmp->slProfileTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slMaxCapacitySL(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slMaxCapacitySL(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slMaxCapacitySL entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slMaxCapacitySL: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slMaxCapacitySL not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slMaxCapacitySL: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..512000 */
		if ((0 > set_value || set_value > 512000)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slMaxCapacitySL: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slTable_old) == NULL)
			if (StorageTmp->slTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slTable_old = slTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slTable_rsvs++;
		StorageTmp->slMaxCapacitySL = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slTable_tsts == 0)
				if ((ret = check_slTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slMaxCapacitySL for you to use, and you have just been asked to do something with it.  Note that anything done here must 
				   be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slTable_sets == 0)
				if ((ret = update_slTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slTable_old) != NULL) {
			slTable_destroy(&StorageTmp->slTable_old);
			StorageTmp->slTable_rsvs = 0;
			StorageTmp->slTable_tsts = 0;
			StorageTmp->slTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slTable_sets == 0)
			revert_slTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slTable_old) == NULL)
			break;
		StorageTmp->slMaxCapacitySL = StorageOld->slMaxCapacitySL;
		if (--StorageTmp->slTable_rsvs == 0)
			slTable_destroy(&StorageTmp->slTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slProfilePointer(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slProfilePointer(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slProfilePointer entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfilePointer: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfilePointer not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 1..32 */
		if (var_val_len > SPRINT_MAX_LEN || ((1 > var_val_len || var_val_len > 32))) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfilePointer: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slTable_old) == NULL)
			if (StorageTmp->slTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slTable_old = slTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->slProfilePointer);
		StorageTmp->slProfilePointer = string;
		StorageTmp->slProfilePointerLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slTable_tsts == 0)
				if ((ret = check_slTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slProfilePointer for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slTable_sets == 0)
				if ((ret = update_slTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slTable_old) != NULL) {
			slTable_destroy(&StorageTmp->slTable_old);
			StorageTmp->slTable_rsvs = 0;
			StorageTmp->slTable_tsts = 0;
			StorageTmp->slTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slTable_sets == 0)
			revert_slTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slTable_old) == NULL)
			break;
		if (StorageOld->slProfilePointer != NULL) {
			SNMP_FREE(StorageTmp->slProfilePointer);
			StorageTmp->slProfilePointer = StorageOld->slProfilePointer;
			StorageTmp->slProfilePointerLen = StorageOld->slProfilePointerLen;
			StorageOld->slProfilePointer = NULL;
			StorageOld->slProfilePointerLen = 0;
		}
		if (--StorageTmp->slTable_rsvs == 0)
			slTable_destroy(&StorageTmp->slTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slSignTermPointer(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slSignTermPointer(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slSignTermPointer entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSignTermPointer: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSignTermPointer not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 1..32 */
		if (var_val_len > SPRINT_MAX_LEN || ((1 > var_val_len || var_val_len > 32))) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSignTermPointer: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slTable_old) == NULL)
			if (StorageTmp->slTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slTable_old = slTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->slSignTermPointer);
		StorageTmp->slSignTermPointer = string;
		StorageTmp->slSignTermPointerLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slTable_tsts == 0)
				if ((ret = check_slTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slSignTermPointer for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slTable_sets == 0)
				if ((ret = update_slTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slTable_old) != NULL) {
			slTable_destroy(&StorageTmp->slTable_old);
			StorageTmp->slTable_rsvs = 0;
			StorageTmp->slTable_tsts = 0;
			StorageTmp->slTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slTable_sets == 0)
			revert_slTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slTable_old) == NULL)
			break;
		if (StorageOld->slSignTermPointer != NULL) {
			SNMP_FREE(StorageTmp->slSignTermPointer);
			StorageTmp->slSignTermPointer = StorageOld->slSignTermPointer;
			StorageTmp->slSignTermPointerLen = StorageOld->slSignTermPointerLen;
			StorageOld->slSignTermPointer = NULL;
			StorageOld->slSignTermPointerLen = 0;
		}
		if (--StorageTmp->slTable_rsvs == 0)
			slTable_destroy(&StorageTmp->slTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slSignDataLinkTpPointer(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slSignDataLinkTpPointer(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slSignDataLinkTpPointer entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSignDataLinkTpPointer: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSignDataLinkTpPointer not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 1..32 */
		if (var_val_len > SPRINT_MAX_LEN || ((1 > var_val_len || var_val_len > 32))) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSignDataLinkTpPointer: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slTable_old) == NULL)
			if (StorageTmp->slTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slTable_old = slTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->slSignDataLinkTpPointer);
		StorageTmp->slSignDataLinkTpPointer = string;
		StorageTmp->slSignDataLinkTpPointerLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slTable_tsts == 0)
				if ((ret = check_slTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slSignDataLinkTpPointer for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slTable_sets == 0)
				if ((ret = update_slTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slTable_old) != NULL) {
			slTable_destroy(&StorageTmp->slTable_old);
			StorageTmp->slTable_rsvs = 0;
			StorageTmp->slTable_tsts = 0;
			StorageTmp->slTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slTable_sets == 0)
			revert_slTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slTable_old) == NULL)
			break;
		if (StorageOld->slSignDataLinkTpPointer != NULL) {
			SNMP_FREE(StorageTmp->slSignDataLinkTpPointer);
			StorageTmp->slSignDataLinkTpPointer = StorageOld->slSignDataLinkTpPointer;
			StorageTmp->slSignDataLinkTpPointerLen = StorageOld->slSignDataLinkTpPointerLen;
			StorageOld->slSignDataLinkTpPointer = NULL;
			StorageOld->slSignDataLinkTpPointerLen = 0;
		}
		if (--StorageTmp->slTable_rsvs == 0)
			slTable_destroy(&StorageTmp->slTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slReplaceSt(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slReplaceSt(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slReplaceSt entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slReplaceSt: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slReplaceSt not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slReplaceSt: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case SLREPLACEST_SUCCESS:
		case SLREPLACEST_UNSUCCESSFUL:
		case SLREPLACEST_NOTPERFORMED:
		case SLREPLACEST_LINKALREADYINHIBITED:
		case SLREPLACEST_LINKNOTINHIBITED:
		case SLREPLACEST_WRONGCLASSREFERENCED:
		case SLREPLACEST_SIGNTERMNONEXISTING:
		case SLREPLACEST_SIGNTERMALREADYREFERENCED:
		case SLREPLACEST_WRONGSIGNLINKTPSTATUS:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slReplaceSt: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slTable_old) == NULL)
			if (StorageTmp->slTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slTable_old = slTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slTable_rsvs++;
		StorageTmp->slReplaceSt = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slTable_tsts == 0)
				if ((ret = check_slTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slReplaceSt for you to use, and you have just been asked to do something with it.  Note that anything done here must be
				   reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slTable_sets == 0)
				if ((ret = update_slTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slTable_old) != NULL) {
			slTable_destroy(&StorageTmp->slTable_old);
			StorageTmp->slTable_rsvs = 0;
			StorageTmp->slTable_tsts = 0;
			StorageTmp->slTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slTable_sets == 0)
			revert_slTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slTable_old) == NULL)
			break;
		StorageTmp->slReplaceSt = StorageOld->slReplaceSt;
		if (--StorageTmp->slTable_rsvs == 0)
			slTable_destroy(&StorageTmp->slTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slRelatedLinkGroupNumber(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slRelatedLinkGroupNumber(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slRelatedLinkGroupNumber entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slRelatedLinkGroupNumber: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slRelatedLinkGroupNumber not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slRelatedLinkGroupNumber: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..15 */
		if ((0 > set_value || set_value > 15)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slRelatedLinkGroupNumber: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slTable_old) == NULL)
			if (StorageTmp->slTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slTable_old = slTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slTable_rsvs++;
		StorageTmp->slRelatedLinkGroupNumber = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slTable_tsts == 0)
				if ((ret = check_slTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slRelatedLinkGroupNumber for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slTable_sets == 0)
				if ((ret = update_slTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slTable_old) != NULL) {
			slTable_destroy(&StorageTmp->slTable_old);
			StorageTmp->slTable_rsvs = 0;
			StorageTmp->slTable_tsts = 0;
			StorageTmp->slTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slTable_sets == 0)
			revert_slTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slTable_old) == NULL)
			break;
		StorageTmp->slRelatedLinkGroupNumber = StorageOld->slRelatedLinkGroupNumber;
		if (--StorageTmp->slTable_rsvs == 0)
			slTable_destroy(&StorageTmp->slTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slSdlList(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slSdlList(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slSdlList entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSdlList: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSdlList not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSdlList: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slTable_old) == NULL)
			if (StorageTmp->slTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slTable_old = slTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slTable_rsvs++;
		StorageTmp->slSdlList = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slTable_tsts == 0)
				if ((ret = check_slTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slSdlList for you to use, and you have just been asked to do something with it.  Note that anything done here must be
				   reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slTable_sets == 0)
				if ((ret = update_slTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slTable_old) != NULL) {
			slTable_destroy(&StorageTmp->slTable_old);
			StorageTmp->slTable_rsvs = 0;
			StorageTmp->slTable_tsts = 0;
			StorageTmp->slTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slTable_sets == 0)
			revert_slTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slTable_old) == NULL)
			break;
		StorageTmp->slSdlList = StorageOld->slSdlList;
		if (--StorageTmp->slTable_rsvs == 0)
			slTable_destroy(&StorageTmp->slTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slName(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slName(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slName entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slName: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slName not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 0..32 */
		if (var_val_len > SPRINT_MAX_LEN || ((0 > var_val_len || var_val_len > 32))) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slName: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slTable_old) == NULL)
			if (StorageTmp->slTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slTable_old = slTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->slName);
		StorageTmp->slName = string;
		StorageTmp->slNameLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slTable_tsts == 0)
				if ((ret = check_slTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slName for you to use, and you have just been asked to do something with it.  Note that anything done here must be
				   reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slTable_sets == 0)
				if ((ret = update_slTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slTable_old) != NULL) {
			slTable_destroy(&StorageTmp->slTable_old);
			StorageTmp->slTable_rsvs = 0;
			StorageTmp->slTable_tsts = 0;
			StorageTmp->slTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slTable_sets == 0)
			revert_slTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slTable_old) == NULL)
			break;
		if (StorageOld->slName != NULL) {
			SNMP_FREE(StorageTmp->slName);
			StorageTmp->slName = StorageOld->slName;
			StorageTmp->slNameLen = StorageOld->slNameLen;
			StorageOld->slName = NULL;
			StorageOld->slNameLen = 0;
		}
		if (--StorageTmp->slTable_rsvs == 0)
			slTable_destroy(&StorageTmp->slTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slSdtList(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slSdtList(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slSdtList entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSdtList: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSdtList not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSdtList: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slTable_old) == NULL)
			if (StorageTmp->slTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slTable_old = slTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slTable_rsvs++;
		StorageTmp->slSdtList = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slTable_tsts == 0)
				if ((ret = check_slTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slSdtList for you to use, and you have just been asked to do something with it.  Note that anything done here must be
				   reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slTable_sets == 0)
				if ((ret = update_slTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slTable_old) != NULL) {
			slTable_destroy(&StorageTmp->slTable_old);
			StorageTmp->slTable_rsvs = 0;
			StorageTmp->slTable_tsts = 0;
			StorageTmp->slTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slTable_sets == 0)
			revert_slTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slTable_old) == NULL)
			break;
		StorageTmp->slSdtList = StorageOld->slSdtList;
		if (--StorageTmp->slTable_rsvs == 0)
			slTable_destroy(&StorageTmp->slTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slSdlListPointer(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slSdlListPointer(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slSdlListTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slSdlListPointer entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slSdlListTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slSdlListRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSdlListPointer: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSdlListPointer not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 1..32 */
		if (var_val_len > SPRINT_MAX_LEN || ((1 > var_val_len || var_val_len > 32))) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSdlListPointer: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slSdlListTable_old) == NULL)
			if (StorageTmp->slSdlListTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slSdlListTable_old = slSdlListTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slSdlListTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->slSdlListPointer);
		StorageTmp->slSdlListPointer = string;
		StorageTmp->slSdlListPointerLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slSdlListTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slSdlListTable_tsts == 0)
				if ((ret = check_slSdlListTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSdlListTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slSdlListPointer for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slSdlListTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slSdlListTable_sets == 0)
				if ((ret = update_slSdlListTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSdlListTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slSdlListTable_old) != NULL) {
			slSdlListTable_destroy(&StorageTmp->slSdlListTable_old);
			StorageTmp->slSdlListTable_rsvs = 0;
			StorageTmp->slSdlListTable_tsts = 0;
			StorageTmp->slSdlListTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slSdlListTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slSdlListTable_sets == 0)
			revert_slSdlListTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slSdlListTable_old) == NULL)
			break;
		if (StorageOld->slSdlListPointer != NULL) {
			SNMP_FREE(StorageTmp->slSdlListPointer);
			StorageTmp->slSdlListPointer = StorageOld->slSdlListPointer;
			StorageTmp->slSdlListPointerLen = StorageOld->slSdlListPointerLen;
			StorageOld->slSdlListPointer = NULL;
			StorageOld->slSdlListPointerLen = 0;
		}
		if (--StorageTmp->slSdlListTable_rsvs == 0)
			slSdlListTable_destroy(&StorageTmp->slSdlListTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slSdtListPointer(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slSdtListPointer(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slSdtListTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slSdtListPointer entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slSdtListTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slSdtListRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSdtListPointer: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSdtListPointer not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 1..32 */
		if (var_val_len > SPRINT_MAX_LEN || ((1 > var_val_len || var_val_len > 32))) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSdtListPointer: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slSdtListTable_old) == NULL)
			if (StorageTmp->slSdtListTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slSdtListTable_old = slSdtListTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slSdtListTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->slSdtListPointer);
		StorageTmp->slSdtListPointer = string;
		StorageTmp->slSdtListPointerLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slSdtListTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slSdtListTable_tsts == 0)
				if ((ret = check_slSdtListTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSdtListTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slSdtListPointer for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slSdtListTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slSdtListTable_sets == 0)
				if ((ret = update_slSdtListTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSdtListTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slSdtListTable_old) != NULL) {
			slSdtListTable_destroy(&StorageTmp->slSdtListTable_old);
			StorageTmp->slSdtListTable_rsvs = 0;
			StorageTmp->slSdtListTable_tsts = 0;
			StorageTmp->slSdtListTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slSdtListTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slSdtListTable_sets == 0)
			revert_slSdtListTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slSdtListTable_old) == NULL)
			break;
		if (StorageOld->slSdtListPointer != NULL) {
			SNMP_FREE(StorageTmp->slSdtListPointer);
			StorageTmp->slSdtListPointer = StorageOld->slSdtListPointer;
			StorageTmp->slSdtListPointerLen = StorageOld->slSdtListPointerLen;
			StorageOld->slSdtListPointer = NULL;
			StorageOld->slSdtListPointerLen = 0;
		}
		if (--StorageTmp->slSdtListTable_rsvs == 0)
			slSdtListTable_destroy(&StorageTmp->slSdtListTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slNbTransmissionRate(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slNbTransmissionRate(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slNbTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slNbTransmissionRate entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slNbTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slNbRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slNbTransmissionRate: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slNbTransmissionRate not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slNbTransmissionRate: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value kbits64 */
		switch (set_value) {
		case SLNBTRANSMISSIONRATE_KBITS4POINT8:
		case SLNBTRANSMISSIONRATE_KBITS56:
		case SLNBTRANSMISSIONRATE_KBITS64:
		case SLNBTRANSMISSIONRATE_KBITS1544:
		case SLNBTRANSMISSIONRATE_KBITS2048:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slNbTransmissionRate: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slNbTable_old) == NULL)
			if (StorageTmp->slNbTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slNbTable_old = slNbTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slNbTable_rsvs++;
		StorageTmp->slNbTransmissionRate = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slNbTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slNbTable_tsts == 0)
				if ((ret = check_slNbTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slNbTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slNbTransmissionRate for you to use, and you have just been asked to do something with it.  Note that anything done here 
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slNbTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slNbTable_sets == 0)
				if ((ret = update_slNbTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slNbTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slNbTable_old) != NULL) {
			slNbTable_destroy(&StorageTmp->slNbTable_old);
			StorageTmp->slNbTable_rsvs = 0;
			StorageTmp->slNbTable_tsts = 0;
			StorageTmp->slNbTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slNbTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slNbTable_sets == 0)
			revert_slNbTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slNbTable_old) == NULL)
			break;
		StorageTmp->slNbTransmissionRate = StorageOld->slNbTransmissionRate;
		if (--StorageTmp->slNbTable_rsvs == 0)
			slNbTable_destroy(&StorageTmp->slNbTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slNbf(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slNbf(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slNbTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slNbf entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slNbTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slNbRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slNbf: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slNbf not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slNbf: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value one */
		switch (set_value) {
		case SLNBF_ONE:
		case SLNBF_SHARED:
		case SLNBF_TWO:
		case SLNBF_THREE:
		case SLNBF_FOUR:
		case SLNBF_FIVE:
		case SLNBF_SIX:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slNbf: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slNbTable_old) == NULL)
			if (StorageTmp->slNbTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slNbTable_old = slNbTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slNbTable_rsvs++;
		StorageTmp->slNbf = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slNbTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slNbTable_tsts == 0)
				if ((ret = check_slNbTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slNbTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slNbf for you to use, and you have just been asked to do something with it.  Note that anything done here must be
				   reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slNbTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slNbTable_sets == 0)
				if ((ret = update_slNbTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slNbTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slNbTable_old) != NULL) {
			slNbTable_destroy(&StorageTmp->slNbTable_old);
			StorageTmp->slNbTable_rsvs = 0;
			StorageTmp->slNbTable_tsts = 0;
			StorageTmp->slNbTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slNbTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slNbTable_sets == 0)
			revert_slNbTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slNbTable_old) == NULL)
			break;
		StorageTmp->slNbf = StorageOld->slNbf;
		if (--StorageTmp->slNbTable_rsvs == 0)
			slNbTable_destroy(&StorageTmp->slNbTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slSaalBufferRelease(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slSaalBufferRelease(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slSaalTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slSaalBufferRelease entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slSaalTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slSaalRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalBufferRelease: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalBufferRelease not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalBufferRelease: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalBufferRelease: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			if (StorageTmp->slSaalTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slSaalTable_old = slSaalTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slSaalTable_rsvs++;
		StorageTmp->slSaalBufferRelease = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slSaalTable_tsts == 0)
				if ((ret = check_slSaalTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSaalTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slSaalBufferRelease for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slSaalTable_sets == 0)
				if ((ret = update_slSaalTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSaalTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			slSaalTable_destroy(&StorageTmp->slSaalTable_old);
			StorageTmp->slSaalTable_rsvs = 0;
			StorageTmp->slSaalTable_tsts = 0;
			StorageTmp->slSaalTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slSaalTable_sets == 0)
			revert_slSaalTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			break;
		StorageTmp->slSaalBufferRelease = StorageOld->slSaalBufferRelease;
		if (--StorageTmp->slSaalTable_rsvs == 0)
			slSaalTable_destroy(&StorageTmp->slSaalTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slSaalMaxCc(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slSaalMaxCc(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slSaalTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slSaalMaxCc entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slSaalTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slSaalRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalMaxCc: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalMaxCc not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalMaxCc: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 4 */
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			if (StorageTmp->slSaalTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slSaalTable_old = slSaalTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slSaalTable_rsvs++;
		StorageTmp->slSaalMaxCc = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slSaalTable_tsts == 0)
				if ((ret = check_slSaalTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSaalTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slSaalMaxCc for you to use, and you have just been asked to do something with it.  Note that anything done here must be
				   reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slSaalTable_sets == 0)
				if ((ret = update_slSaalTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSaalTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			slSaalTable_destroy(&StorageTmp->slSaalTable_old);
			StorageTmp->slSaalTable_rsvs = 0;
			StorageTmp->slSaalTable_tsts = 0;
			StorageTmp->slSaalTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slSaalTable_sets == 0)
			revert_slSaalTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			break;
		StorageTmp->slSaalMaxCc = StorageOld->slSaalMaxCc;
		if (--StorageTmp->slSaalTable_rsvs == 0)
			slSaalTable_destroy(&StorageTmp->slSaalTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slSaalMaxNrp(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slSaalMaxNrp(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slSaalTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slSaalMaxNrp entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slSaalTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slSaalRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalMaxNrp: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalMaxNrp not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalMaxNrp: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			if (StorageTmp->slSaalTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slSaalTable_old = slSaalTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slSaalTable_rsvs++;
		StorageTmp->slSaalMaxNrp = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slSaalTable_tsts == 0)
				if ((ret = check_slSaalTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSaalTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slSaalMaxNrp for you to use, and you have just been asked to do something with it.  Note that anything done here must be 
				   reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slSaalTable_sets == 0)
				if ((ret = update_slSaalTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSaalTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			slSaalTable_destroy(&StorageTmp->slSaalTable_old);
			StorageTmp->slSaalTable_rsvs = 0;
			StorageTmp->slSaalTable_tsts = 0;
			StorageTmp->slSaalTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slSaalTable_sets == 0)
			revert_slSaalTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			break;
		StorageTmp->slSaalMaxNrp = StorageOld->slSaalMaxNrp;
		if (--StorageTmp->slSaalTable_rsvs == 0)
			slSaalTable_destroy(&StorageTmp->slSaalTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slSaalMaxInformationFieldLength(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slSaalMaxInformationFieldLength(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slSaalTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slSaalMaxInformationFieldLength entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slSaalTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slSaalRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalMaxInformationFieldLength: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalMaxInformationFieldLength not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalMaxInformationFieldLength: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 4096 */
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			if (StorageTmp->slSaalTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slSaalTable_old = slSaalTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slSaalTable_rsvs++;
		StorageTmp->slSaalMaxInformationFieldLength = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slSaalTable_tsts == 0)
				if ((ret = check_slSaalTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSaalTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slSaalMaxInformationFieldLength for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slSaalTable_sets == 0)
				if ((ret = update_slSaalTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSaalTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			slSaalTable_destroy(&StorageTmp->slSaalTable_old);
			StorageTmp->slSaalTable_rsvs = 0;
			StorageTmp->slSaalTable_tsts = 0;
			StorageTmp->slSaalTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slSaalTable_sets == 0)
			revert_slSaalTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			break;
		StorageTmp->slSaalMaxInformationFieldLength = StorageOld->slSaalMaxInformationFieldLength;
		if (--StorageTmp->slSaalTable_rsvs == 0)
			slSaalTable_destroy(&StorageTmp->slSaalTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slSaalMaxLengthSscopUuField(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slSaalMaxLengthSscopUuField(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slSaalTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slSaalMaxLengthSscopUuField entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slSaalTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slSaalRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalMaxLengthSscopUuField: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalMaxLengthSscopUuField not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalMaxLengthSscopUuField: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 4 */
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			if (StorageTmp->slSaalTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slSaalTable_old = slSaalTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slSaalTable_rsvs++;
		StorageTmp->slSaalMaxLengthSscopUuField = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slSaalTable_tsts == 0)
				if ((ret = check_slSaalTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSaalTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slSaalMaxLengthSscopUuField for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slSaalTable_sets == 0)
				if ((ret = update_slSaalTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSaalTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			slSaalTable_destroy(&StorageTmp->slSaalTable_old);
			StorageTmp->slSaalTable_rsvs = 0;
			StorageTmp->slSaalTable_tsts = 0;
			StorageTmp->slSaalTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slSaalTable_sets == 0)
			revert_slSaalTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			break;
		StorageTmp->slSaalMaxLengthSscopUuField = StorageOld->slSaalMaxLengthSscopUuField;
		if (--StorageTmp->slSaalTable_rsvs == 0)
			slSaalTable_destroy(&StorageTmp->slSaalTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slSaalMaxPd(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slSaalMaxPd(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slSaalTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slSaalMaxPd entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slSaalTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slSaalRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalMaxPd: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalMaxPd not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalMaxPd: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 500 */
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			if (StorageTmp->slSaalTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slSaalTable_old = slSaalTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slSaalTable_rsvs++;
		StorageTmp->slSaalMaxPd = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slSaalTable_tsts == 0)
				if ((ret = check_slSaalTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSaalTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slSaalMaxPd for you to use, and you have just been asked to do something with it.  Note that anything done here must be
				   reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slSaalTable_sets == 0)
				if ((ret = update_slSaalTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSaalTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			slSaalTable_destroy(&StorageTmp->slSaalTable_old);
			StorageTmp->slSaalTable_rsvs = 0;
			StorageTmp->slSaalTable_tsts = 0;
			StorageTmp->slSaalTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slSaalTable_sets == 0)
			revert_slSaalTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			break;
		StorageTmp->slSaalMaxPd = StorageOld->slSaalMaxPd;
		if (--StorageTmp->slSaalTable_rsvs == 0)
			slSaalTable_destroy(&StorageTmp->slSaalTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slSaalMaxSscopCreditToPeer(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slSaalMaxSscopCreditToPeer(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slSaalTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slSaalMaxSscopCreditToPeer entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slSaalTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slSaalRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalMaxSscopCreditToPeer: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalMaxSscopCreditToPeer not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalMaxSscopCreditToPeer: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			if (StorageTmp->slSaalTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slSaalTable_old = slSaalTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slSaalTable_rsvs++;
		StorageTmp->slSaalMaxSscopCreditToPeer = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slSaalTable_tsts == 0)
				if ((ret = check_slSaalTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSaalTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slSaalMaxSscopCreditToPeer for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slSaalTable_sets == 0)
				if ((ret = update_slSaalTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSaalTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			slSaalTable_destroy(&StorageTmp->slSaalTable_old);
			StorageTmp->slSaalTable_rsvs = 0;
			StorageTmp->slSaalTable_tsts = 0;
			StorageTmp->slSaalTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slSaalTable_sets == 0)
			revert_slSaalTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			break;
		StorageTmp->slSaalMaxSscopCreditToPeer = StorageOld->slSaalMaxSscopCreditToPeer;
		if (--StorageTmp->slSaalTable_rsvs == 0)
			slSaalTable_destroy(&StorageTmp->slSaalTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slSaalMaxStat(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slSaalMaxStat(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slSaalTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slSaalMaxStat entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slSaalTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slSaalRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalMaxStat: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalMaxStat not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalMaxStat: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 67 */
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			if (StorageTmp->slSaalTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slSaalTable_old = slSaalTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slSaalTable_rsvs++;
		StorageTmp->slSaalMaxStat = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slSaalTable_tsts == 0)
				if ((ret = check_slSaalTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSaalTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slSaalMaxStat for you to use, and you have just been asked to do something with it.  Note that anything done here must
				   be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slSaalTable_sets == 0)
				if ((ret = update_slSaalTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSaalTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			slSaalTable_destroy(&StorageTmp->slSaalTable_old);
			StorageTmp->slSaalTable_rsvs = 0;
			StorageTmp->slSaalTable_tsts = 0;
			StorageTmp->slSaalTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slSaalTable_sets == 0)
			revert_slSaalTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			break;
		StorageTmp->slSaalMaxStat = StorageOld->slSaalMaxStat;
		if (--StorageTmp->slSaalTable_rsvs == 0)
			slSaalTable_destroy(&StorageTmp->slSaalTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slSaalN1(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slSaalN1(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slSaalTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slSaalN1 entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slSaalTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slSaalRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalN1: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalN1 not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalN1: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 1000 */
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			if (StorageTmp->slSaalTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slSaalTable_old = slSaalTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slSaalTable_rsvs++;
		StorageTmp->slSaalN1 = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slSaalTable_tsts == 0)
				if ((ret = check_slSaalTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSaalTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slSaalN1 for you to use, and you have just been asked to do something with it.  Note that anything done here must be
				   reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slSaalTable_sets == 0)
				if ((ret = update_slSaalTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSaalTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			slSaalTable_destroy(&StorageTmp->slSaalTable_old);
			StorageTmp->slSaalTable_rsvs = 0;
			StorageTmp->slSaalTable_tsts = 0;
			StorageTmp->slSaalTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slSaalTable_sets == 0)
			revert_slSaalTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			break;
		StorageTmp->slSaalN1 = StorageOld->slSaalN1;
		if (--StorageTmp->slSaalTable_rsvs == 0)
			slSaalTable_destroy(&StorageTmp->slSaalTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slSaalNniLayerMgrProvingState(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slSaalNniLayerMgrProvingState(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slSaalTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slSaalNniLayerMgrProvingState entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slSaalTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slSaalRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalNniLayerMgrProvingState: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalNniLayerMgrProvingState not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalNniLayerMgrProvingState: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case SLSAALNNILAYERMGRPROVINGSTATE_NORMAL:
		case SLSAALNNILAYERMGRPROVINGSTATE_EMERGENCY:
		case SLSAALNNILAYERMGRPROVINGSTATE_NEUTRAL:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalNniLayerMgrProvingState: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			if (StorageTmp->slSaalTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slSaalTable_old = slSaalTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slSaalTable_rsvs++;
		StorageTmp->slSaalNniLayerMgrProvingState = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slSaalTable_tsts == 0)
				if ((ret = check_slSaalTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSaalTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slSaalNniLayerMgrProvingState for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slSaalTable_sets == 0)
				if ((ret = update_slSaalTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSaalTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			slSaalTable_destroy(&StorageTmp->slSaalTable_old);
			StorageTmp->slSaalTable_rsvs = 0;
			StorageTmp->slSaalTable_tsts = 0;
			StorageTmp->slSaalTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slSaalTable_sets == 0)
			revert_slSaalTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			break;
		StorageTmp->slSaalNniLayerMgrProvingState = StorageOld->slSaalNniLayerMgrProvingState;
		if (--StorageTmp->slSaalTable_rsvs == 0)
			slSaalTable_destroy(&StorageTmp->slSaalTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slSaalNniLayerMgrTimerNoCredit(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slSaalNniLayerMgrTimerNoCredit(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slSaalTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slSaalNniLayerMgrTimerNoCredit entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slSaalTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slSaalRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalNniLayerMgrTimerNoCredit: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalNniLayerMgrTimerNoCredit not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalNniLayerMgrTimerNoCredit: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalNniLayerMgrTimerNoCredit: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			if (StorageTmp->slSaalTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slSaalTable_old = slSaalTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slSaalTable_rsvs++;
		StorageTmp->slSaalNniLayerMgrTimerNoCredit = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slSaalTable_tsts == 0)
				if ((ret = check_slSaalTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSaalTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slSaalNniLayerMgrTimerNoCredit for you to use, and you have just been asked to do something with it.  Note that anything 
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slSaalTable_sets == 0)
				if ((ret = update_slSaalTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSaalTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			slSaalTable_destroy(&StorageTmp->slSaalTable_old);
			StorageTmp->slSaalTable_rsvs = 0;
			StorageTmp->slSaalTable_tsts = 0;
			StorageTmp->slSaalTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slSaalTable_sets == 0)
			revert_slSaalTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			break;
		StorageTmp->slSaalNniLayerMgrTimerNoCredit = StorageOld->slSaalNniLayerMgrTimerNoCredit;
		if (--StorageTmp->slSaalTable_rsvs == 0)
			slSaalTable_destroy(&StorageTmp->slSaalTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slSaalNniLayerMgrTimerRepeatSrec(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slSaalNniLayerMgrTimerRepeatSrec(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slSaalTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slSaalNniLayerMgrTimerRepeatSrec entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slSaalTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slSaalRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalNniLayerMgrTimerRepeatSrec: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalNniLayerMgrTimerRepeatSrec not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalNniLayerMgrTimerRepeatSrec: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalNniLayerMgrTimerRepeatSrec: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			if (StorageTmp->slSaalTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slSaalTable_old = slSaalTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slSaalTable_rsvs++;
		StorageTmp->slSaalNniLayerMgrTimerRepeatSrec = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slSaalTable_tsts == 0)
				if ((ret = check_slSaalTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSaalTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slSaalNniLayerMgrTimerRepeatSrec for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slSaalTable_sets == 0)
				if ((ret = update_slSaalTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSaalTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			slSaalTable_destroy(&StorageTmp->slSaalTable_old);
			StorageTmp->slSaalTable_rsvs = 0;
			StorageTmp->slSaalTable_tsts = 0;
			StorageTmp->slSaalTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slSaalTable_sets == 0)
			revert_slSaalTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			break;
		StorageTmp->slSaalNniLayerMgrTimerRepeatSrec = StorageOld->slSaalNniLayerMgrTimerRepeatSrec;
		if (--StorageTmp->slSaalTable_rsvs == 0)
			slSaalTable_destroy(&StorageTmp->slSaalTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slSaalNniTimerT1(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slSaalNniTimerT1(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slSaalTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slSaalNniTimerT1 entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slSaalTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slSaalRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalNniTimerT1: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalNniTimerT1 not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalNniTimerT1: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 500 */
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalNniTimerT1: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			if (StorageTmp->slSaalTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slSaalTable_old = slSaalTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slSaalTable_rsvs++;
		StorageTmp->slSaalNniTimerT1 = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slSaalTable_tsts == 0)
				if ((ret = check_slSaalTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSaalTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slSaalNniTimerT1 for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slSaalTable_sets == 0)
				if ((ret = update_slSaalTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSaalTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			slSaalTable_destroy(&StorageTmp->slSaalTable_old);
			StorageTmp->slSaalTable_rsvs = 0;
			StorageTmp->slSaalTable_tsts = 0;
			StorageTmp->slSaalTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slSaalTable_sets == 0)
			revert_slSaalTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			break;
		StorageTmp->slSaalNniTimerT1 = StorageOld->slSaalNniTimerT1;
		if (--StorageTmp->slSaalTable_rsvs == 0)
			slSaalTable_destroy(&StorageTmp->slSaalTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slSaalNniTimerT2(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slSaalNniTimerT2(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slSaalTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slSaalNniTimerT2 entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slSaalTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slSaalRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalNniTimerT2: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalNniTimerT2 not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalNniTimerT2: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 3000 */
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalNniTimerT2: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			if (StorageTmp->slSaalTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slSaalTable_old = slSaalTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slSaalTable_rsvs++;
		StorageTmp->slSaalNniTimerT2 = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slSaalTable_tsts == 0)
				if ((ret = check_slSaalTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSaalTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slSaalNniTimerT2 for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slSaalTable_sets == 0)
				if ((ret = update_slSaalTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSaalTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			slSaalTable_destroy(&StorageTmp->slSaalTable_old);
			StorageTmp->slSaalTable_rsvs = 0;
			StorageTmp->slSaalTable_tsts = 0;
			StorageTmp->slSaalTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slSaalTable_sets == 0)
			revert_slSaalTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			break;
		StorageTmp->slSaalNniTimerT2 = StorageOld->slSaalNniTimerT2;
		if (--StorageTmp->slSaalTable_rsvs == 0)
			slSaalTable_destroy(&StorageTmp->slSaalTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slSaalNniTimerT3(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slSaalNniTimerT3(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slSaalTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slSaalNniTimerT3 entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slSaalTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slSaalRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalNniTimerT3: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalNniTimerT3 not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalNniTimerT3: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalNniTimerT3: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			if (StorageTmp->slSaalTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slSaalTable_old = slSaalTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slSaalTable_rsvs++;
		StorageTmp->slSaalNniTimerT3 = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slSaalTable_tsts == 0)
				if ((ret = check_slSaalTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSaalTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slSaalNniTimerT3 for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slSaalTable_sets == 0)
				if ((ret = update_slSaalTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSaalTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			slSaalTable_destroy(&StorageTmp->slSaalTable_old);
			StorageTmp->slSaalTable_rsvs = 0;
			StorageTmp->slSaalTable_tsts = 0;
			StorageTmp->slSaalTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slSaalTable_sets == 0)
			revert_slSaalTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			break;
		StorageTmp->slSaalNniTimerT3 = StorageOld->slSaalNniTimerT3;
		if (--StorageTmp->slSaalTable_rsvs == 0)
			slSaalTable_destroy(&StorageTmp->slSaalTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slSaalSscopTimerCc(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slSaalSscopTimerCc(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slSaalTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slSaalSscopTimerCc entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slSaalTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slSaalRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalSscopTimerCc: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalSscopTimerCc not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalSscopTimerCc: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 20 */
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalSscopTimerCc: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			if (StorageTmp->slSaalTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slSaalTable_old = slSaalTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slSaalTable_rsvs++;
		StorageTmp->slSaalSscopTimerCc = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slSaalTable_tsts == 0)
				if ((ret = check_slSaalTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSaalTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slSaalSscopTimerCc for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slSaalTable_sets == 0)
				if ((ret = update_slSaalTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSaalTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			slSaalTable_destroy(&StorageTmp->slSaalTable_old);
			StorageTmp->slSaalTable_rsvs = 0;
			StorageTmp->slSaalTable_tsts = 0;
			StorageTmp->slSaalTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slSaalTable_sets == 0)
			revert_slSaalTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			break;
		StorageTmp->slSaalSscopTimerCc = StorageOld->slSaalSscopTimerCc;
		if (--StorageTmp->slSaalTable_rsvs == 0)
			slSaalTable_destroy(&StorageTmp->slSaalTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slSaalSscopTimerIdle(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slSaalSscopTimerIdle(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slSaalTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slSaalSscopTimerIdle entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slSaalTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slSaalRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalSscopTimerIdle: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalSscopTimerIdle not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalSscopTimerIdle: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 10 */
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalSscopTimerIdle: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			if (StorageTmp->slSaalTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slSaalTable_old = slSaalTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slSaalTable_rsvs++;
		StorageTmp->slSaalSscopTimerIdle = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slSaalTable_tsts == 0)
				if ((ret = check_slSaalTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSaalTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slSaalSscopTimerIdle for you to use, and you have just been asked to do something with it.  Note that anything done here 
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slSaalTable_sets == 0)
				if ((ret = update_slSaalTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSaalTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			slSaalTable_destroy(&StorageTmp->slSaalTable_old);
			StorageTmp->slSaalTable_rsvs = 0;
			StorageTmp->slSaalTable_tsts = 0;
			StorageTmp->slSaalTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slSaalTable_sets == 0)
			revert_slSaalTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			break;
		StorageTmp->slSaalSscopTimerIdle = StorageOld->slSaalSscopTimerIdle;
		if (--StorageTmp->slSaalTable_rsvs == 0)
			slSaalTable_destroy(&StorageTmp->slSaalTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slSaalSscopTimerKeepAlive(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slSaalSscopTimerKeepAlive(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slSaalTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slSaalSscopTimerKeepAlive entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slSaalTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slSaalRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalSscopTimerKeepAlive: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalSscopTimerKeepAlive not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalSscopTimerKeepAlive: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 10 */
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalSscopTimerKeepAlive: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			if (StorageTmp->slSaalTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slSaalTable_old = slSaalTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slSaalTable_rsvs++;
		StorageTmp->slSaalSscopTimerKeepAlive = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slSaalTable_tsts == 0)
				if ((ret = check_slSaalTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSaalTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slSaalSscopTimerKeepAlive for you to use, and you have just been asked to do something with it.  Note that anything done 
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slSaalTable_sets == 0)
				if ((ret = update_slSaalTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSaalTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			slSaalTable_destroy(&StorageTmp->slSaalTable_old);
			StorageTmp->slSaalTable_rsvs = 0;
			StorageTmp->slSaalTable_tsts = 0;
			StorageTmp->slSaalTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slSaalTable_sets == 0)
			revert_slSaalTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			break;
		StorageTmp->slSaalSscopTimerKeepAlive = StorageOld->slSaalSscopTimerKeepAlive;
		if (--StorageTmp->slSaalTable_rsvs == 0)
			slSaalTable_destroy(&StorageTmp->slSaalTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slSaalSscopTimerNoResponse(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slSaalSscopTimerNoResponse(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slSaalTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slSaalSscopTimerNoResponse entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slSaalTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slSaalRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalSscopTimerNoResponse: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalSscopTimerNoResponse not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalSscopTimerNoResponse: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 150 */
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalSscopTimerNoResponse: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			if (StorageTmp->slSaalTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slSaalTable_old = slSaalTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slSaalTable_rsvs++;
		StorageTmp->slSaalSscopTimerNoResponse = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slSaalTable_tsts == 0)
				if ((ret = check_slSaalTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSaalTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slSaalSscopTimerNoResponse for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slSaalTable_sets == 0)
				if ((ret = update_slSaalTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSaalTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			slSaalTable_destroy(&StorageTmp->slSaalTable_old);
			StorageTmp->slSaalTable_rsvs = 0;
			StorageTmp->slSaalTable_tsts = 0;
			StorageTmp->slSaalTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slSaalTable_sets == 0)
			revert_slSaalTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			break;
		StorageTmp->slSaalSscopTimerNoResponse = StorageOld->slSaalSscopTimerNoResponse;
		if (--StorageTmp->slSaalTable_rsvs == 0)
			slSaalTable_destroy(&StorageTmp->slSaalTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slSaalSscopTimerPoll(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slSaalSscopTimerPoll(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slSaalTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slSaalSscopTimerPoll entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slSaalTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slSaalRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalSscopTimerPoll: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalSscopTimerPoll not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalSscopTimerPoll: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 10 */
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalSscopTimerPoll: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			if (StorageTmp->slSaalTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slSaalTable_old = slSaalTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slSaalTable_rsvs++;
		StorageTmp->slSaalSscopTimerPoll = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slSaalTable_tsts == 0)
				if ((ret = check_slSaalTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSaalTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slSaalSscopTimerPoll for you to use, and you have just been asked to do something with it.  Note that anything done here 
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slSaalTable_sets == 0)
				if ((ret = update_slSaalTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSaalTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			slSaalTable_destroy(&StorageTmp->slSaalTable_old);
			StorageTmp->slSaalTable_rsvs = 0;
			StorageTmp->slSaalTable_tsts = 0;
			StorageTmp->slSaalTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slSaalTable_sets == 0)
			revert_slSaalTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			break;
		StorageTmp->slSaalSscopTimerPoll = StorageOld->slSaalSscopTimerPoll;
		if (--StorageTmp->slSaalTable_rsvs == 0)
			slSaalTable_destroy(&StorageTmp->slSaalTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slSaalTransRateIntervalLower(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slSaalTransRateIntervalLower(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slSaalTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slSaalTransRateIntervalLower entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slSaalTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slSaalRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalTransRateIntervalLower: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalTransRateIntervalLower not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalTransRateIntervalLower: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 129 */
		/* Note: ranges 64..4096 */
		if ((64 > set_value || set_value > 4096)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalTransRateIntervalLower: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			if (StorageTmp->slSaalTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slSaalTable_old = slSaalTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slSaalTable_rsvs++;
		StorageTmp->slSaalTransRateIntervalLower = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slSaalTable_tsts == 0)
				if ((ret = check_slSaalTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSaalTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slSaalTransRateIntervalLower for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slSaalTable_sets == 0)
				if ((ret = update_slSaalTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSaalTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			slSaalTable_destroy(&StorageTmp->slSaalTable_old);
			StorageTmp->slSaalTable_rsvs = 0;
			StorageTmp->slSaalTable_tsts = 0;
			StorageTmp->slSaalTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slSaalTable_sets == 0)
			revert_slSaalTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			break;
		StorageTmp->slSaalTransRateIntervalLower = StorageOld->slSaalTransRateIntervalLower;
		if (--StorageTmp->slSaalTable_rsvs == 0)
			slSaalTable_destroy(&StorageTmp->slSaalTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slSaalTransRateIntervalUpper(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slSaalTransRateIntervalUpper(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slSaalTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slSaalTransRateIntervalUpper entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slSaalTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slSaalRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalTransRateIntervalUpper: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalTransRateIntervalUpper not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalTransRateIntervalUpper: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 256 */
		/* Note: ranges 64..4096 */
		if ((64 > set_value || set_value > 4096)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalTransRateIntervalUpper: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			if (StorageTmp->slSaalTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slSaalTable_old = slSaalTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slSaalTable_rsvs++;
		StorageTmp->slSaalTransRateIntervalUpper = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slSaalTable_tsts == 0)
				if ((ret = check_slSaalTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSaalTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slSaalTransRateIntervalUpper for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slSaalTable_sets == 0)
				if ((ret = update_slSaalTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSaalTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			slSaalTable_destroy(&StorageTmp->slSaalTable_old);
			StorageTmp->slSaalTable_rsvs = 0;
			StorageTmp->slSaalTable_tsts = 0;
			StorageTmp->slSaalTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slSaalTable_sets == 0)
			revert_slSaalTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			break;
		StorageTmp->slSaalTransRateIntervalUpper = StorageOld->slSaalTransRateIntervalUpper;
		if (--StorageTmp->slSaalTable_rsvs == 0)
			slSaalTable_destroy(&StorageTmp->slSaalTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slSaalEgressTransRateIntvlLower(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slSaalEgressTransRateIntvlLower(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slSaalTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slSaalEgressTransRateIntvlLower entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slSaalTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slSaalRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalEgressTransRateIntvlLower: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalEgressTransRateIntvlLower not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalEgressTransRateIntvlLower: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 64..4096 */
		if ((64 > set_value || set_value > 4096)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalEgressTransRateIntvlLower: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			if (StorageTmp->slSaalTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slSaalTable_old = slSaalTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slSaalTable_rsvs++;
		StorageTmp->slSaalEgressTransRateIntvlLower = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slSaalTable_tsts == 0)
				if ((ret = check_slSaalTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSaalTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slSaalEgressTransRateIntvlLower for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slSaalTable_sets == 0)
				if ((ret = update_slSaalTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSaalTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			slSaalTable_destroy(&StorageTmp->slSaalTable_old);
			StorageTmp->slSaalTable_rsvs = 0;
			StorageTmp->slSaalTable_tsts = 0;
			StorageTmp->slSaalTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slSaalTable_sets == 0)
			revert_slSaalTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			break;
		StorageTmp->slSaalEgressTransRateIntvlLower = StorageOld->slSaalEgressTransRateIntvlLower;
		if (--StorageTmp->slSaalTable_rsvs == 0)
			slSaalTable_destroy(&StorageTmp->slSaalTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slSaalEgressTransRateIntvlUpper(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slSaalEgressTransRateIntvlUpper(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slSaalTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slSaalEgressTransRateIntvlUpper entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slSaalTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slSaalRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalEgressTransRateIntvlUpper: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalEgressTransRateIntvlUpper not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalEgressTransRateIntvlUpper: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 64..4096 */
		if ((64 > set_value || set_value > 4096)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalEgressTransRateIntvlUpper: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			if (StorageTmp->slSaalTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slSaalTable_old = slSaalTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slSaalTable_rsvs++;
		StorageTmp->slSaalEgressTransRateIntvlUpper = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slSaalTable_tsts == 0)
				if ((ret = check_slSaalTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSaalTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slSaalEgressTransRateIntvlUpper for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slSaalTable_sets == 0)
				if ((ret = update_slSaalTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSaalTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			slSaalTable_destroy(&StorageTmp->slSaalTable_old);
			StorageTmp->slSaalTable_rsvs = 0;
			StorageTmp->slSaalTable_tsts = 0;
			StorageTmp->slSaalTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slSaalTable_sets == 0)
			revert_slSaalTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			break;
		StorageTmp->slSaalEgressTransRateIntvlUpper = StorageOld->slSaalEgressTransRateIntvlUpper;
		if (--StorageTmp->slSaalTable_rsvs == 0)
			slSaalTable_destroy(&StorageTmp->slSaalTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slSaalPollAfterRetransmission(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slSaalPollAfterRetransmission(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slSaalTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slSaalPollAfterRetransmission entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slSaalTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slSaalRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalPollAfterRetransmission: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalPollAfterRetransmission not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalPollAfterRetransmission: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalPollAfterRetransmission: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			if (StorageTmp->slSaalTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slSaalTable_old = slSaalTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slSaalTable_rsvs++;
		StorageTmp->slSaalPollAfterRetransmission = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slSaalTable_tsts == 0)
				if ((ret = check_slSaalTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSaalTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slSaalPollAfterRetransmission for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slSaalTable_sets == 0)
				if ((ret = update_slSaalTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSaalTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
			slSaalTable_destroy(&StorageTmp->slSaalTable_old);
			StorageTmp->slSaalTable_rsvs = 0;
			StorageTmp->slSaalTable_tsts = 0;
			StorageTmp->slSaalTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slSaalTable_sets == 0)
			revert_slSaalTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
			break;
		StorageTmp->slSaalPollAfterRetransmission = StorageOld->slSaalPollAfterRetransmission;
		if (--StorageTmp->slSaalTable_rsvs == 0)
			slSaalTable_destroy(&StorageTmp->slSaalTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slM2paN1(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slM2paN1(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slM2paTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slM2paN1 entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slM2paTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slM2paRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paN1: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paN1 not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paN1: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 1000 */
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			if (StorageTmp->slM2paTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slM2paTable_old = slM2paTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slM2paTable_rsvs++;
		StorageTmp->slM2paN1 = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slM2paTable_tsts == 0)
				if ((ret = check_slM2paTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slM2paTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slM2paN1 for you to use, and you have just been asked to do something with it.  Note that anything done here must be
				   reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slM2paTable_sets == 0)
				if ((ret = update_slM2paTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slM2paTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			slM2paTable_destroy(&StorageTmp->slM2paTable_old);
			StorageTmp->slM2paTable_rsvs = 0;
			StorageTmp->slM2paTable_tsts = 0;
			StorageTmp->slM2paTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slM2paTable_sets == 0)
			revert_slM2paTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			break;
		StorageTmp->slM2paN1 = StorageOld->slM2paN1;
		if (--StorageTmp->slM2paTable_rsvs == 0)
			slM2paTable_destroy(&StorageTmp->slM2paTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slM2paProving(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slM2paProving(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slM2paTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slM2paProving entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slM2paTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slM2paRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paProving: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paProving not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paProving: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value true */
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paProving: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			if (StorageTmp->slM2paTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slM2paTable_old = slM2paTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slM2paTable_rsvs++;
		StorageTmp->slM2paProving = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slM2paTable_tsts == 0)
				if ((ret = check_slM2paTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slM2paTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slM2paProving for you to use, and you have just been asked to do something with it.  Note that anything done here must
				   be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slM2paTable_sets == 0)
				if ((ret = update_slM2paTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slM2paTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			slM2paTable_destroy(&StorageTmp->slM2paTable_old);
			StorageTmp->slM2paTable_rsvs = 0;
			StorageTmp->slM2paTable_tsts = 0;
			StorageTmp->slM2paTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slM2paTable_sets == 0)
			revert_slM2paTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			break;
		StorageTmp->slM2paProving = StorageOld->slM2paProving;
		if (--StorageTmp->slM2paTable_rsvs == 0)
			slM2paTable_destroy(&StorageTmp->slM2paTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slM2paManagementProvingState(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slM2paManagementProvingState(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slM2paTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slM2paManagementProvingState entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slM2paTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slM2paRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paManagementProvingState: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paManagementProvingState not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paManagementProvingState: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case SLM2PAMANAGEMENTPROVINGSTATE_NORMAL:
		case SLM2PAMANAGEMENTPROVINGSTATE_EMERGENCY:
		case SLM2PAMANAGEMENTPROVINGSTATE_NEUTRAL:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paManagementProvingState: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			if (StorageTmp->slM2paTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slM2paTable_old = slM2paTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slM2paTable_rsvs++;
		StorageTmp->slM2paManagementProvingState = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slM2paTable_tsts == 0)
				if ((ret = check_slM2paTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slM2paTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slM2paManagementProvingState for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slM2paTable_sets == 0)
				if ((ret = update_slM2paTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slM2paTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			slM2paTable_destroy(&StorageTmp->slM2paTable_old);
			StorageTmp->slM2paTable_rsvs = 0;
			StorageTmp->slM2paTable_tsts = 0;
			StorageTmp->slM2paTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slM2paTable_sets == 0)
			revert_slM2paTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			break;
		StorageTmp->slM2paManagementProvingState = StorageOld->slM2paManagementProvingState;
		if (--StorageTmp->slM2paTable_rsvs == 0)
			slM2paTable_destroy(&StorageTmp->slM2paTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slM2paLoopDelayLower(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slM2paLoopDelayLower(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slM2paTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slM2paLoopDelayLower entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slM2paTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slM2paRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paLoopDelayLower: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paLoopDelayLower not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paLoopDelayLower: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			if (StorageTmp->slM2paTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slM2paTable_old = slM2paTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slM2paTable_rsvs++;
		StorageTmp->slM2paLoopDelayLower = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slM2paTable_tsts == 0)
				if ((ret = check_slM2paTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slM2paTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slM2paLoopDelayLower for you to use, and you have just been asked to do something with it.  Note that anything done here 
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slM2paTable_sets == 0)
				if ((ret = update_slM2paTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slM2paTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			slM2paTable_destroy(&StorageTmp->slM2paTable_old);
			StorageTmp->slM2paTable_rsvs = 0;
			StorageTmp->slM2paTable_tsts = 0;
			StorageTmp->slM2paTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slM2paTable_sets == 0)
			revert_slM2paTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			break;
		StorageTmp->slM2paLoopDelayLower = StorageOld->slM2paLoopDelayLower;
		if (--StorageTmp->slM2paTable_rsvs == 0)
			slM2paTable_destroy(&StorageTmp->slM2paTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slM2paLoopDelayUpper(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slM2paLoopDelayUpper(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slM2paTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slM2paLoopDelayUpper entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slM2paTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slM2paRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paLoopDelayUpper: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paLoopDelayUpper not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paLoopDelayUpper: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			if (StorageTmp->slM2paTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slM2paTable_old = slM2paTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slM2paTable_rsvs++;
		StorageTmp->slM2paLoopDelayUpper = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slM2paTable_tsts == 0)
				if ((ret = check_slM2paTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slM2paTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slM2paLoopDelayUpper for you to use, and you have just been asked to do something with it.  Note that anything done here 
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slM2paTable_sets == 0)
				if ((ret = update_slM2paTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slM2paTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			slM2paTable_destroy(&StorageTmp->slM2paTable_old);
			StorageTmp->slM2paTable_rsvs = 0;
			StorageTmp->slM2paTable_tsts = 0;
			StorageTmp->slM2paTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slM2paTable_sets == 0)
			revert_slM2paTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			break;
		StorageTmp->slM2paLoopDelayUpper = StorageOld->slM2paLoopDelayUpper;
		if (--StorageTmp->slM2paTable_rsvs == 0)
			slM2paTable_destroy(&StorageTmp->slM2paTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slM2paTransRateIntervalLower(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slM2paTransRateIntervalLower(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slM2paTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slM2paTransRateIntervalLower entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slM2paTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slM2paRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paTransRateIntervalLower: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paTransRateIntervalLower not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paTransRateIntervalLower: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			if (StorageTmp->slM2paTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slM2paTable_old = slM2paTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slM2paTable_rsvs++;
		StorageTmp->slM2paTransRateIntervalLower = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slM2paTable_tsts == 0)
				if ((ret = check_slM2paTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slM2paTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slM2paTransRateIntervalLower for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slM2paTable_sets == 0)
				if ((ret = update_slM2paTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slM2paTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			slM2paTable_destroy(&StorageTmp->slM2paTable_old);
			StorageTmp->slM2paTable_rsvs = 0;
			StorageTmp->slM2paTable_tsts = 0;
			StorageTmp->slM2paTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slM2paTable_sets == 0)
			revert_slM2paTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			break;
		StorageTmp->slM2paTransRateIntervalLower = StorageOld->slM2paTransRateIntervalLower;
		if (--StorageTmp->slM2paTable_rsvs == 0)
			slM2paTable_destroy(&StorageTmp->slM2paTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slM2paTransRateIntervalUpper(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slM2paTransRateIntervalUpper(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slM2paTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slM2paTransRateIntervalUpper entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slM2paTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slM2paRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paTransRateIntervalUpper: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paTransRateIntervalUpper not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paTransRateIntervalUpper: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			if (StorageTmp->slM2paTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slM2paTable_old = slM2paTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slM2paTable_rsvs++;
		StorageTmp->slM2paTransRateIntervalUpper = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slM2paTable_tsts == 0)
				if ((ret = check_slM2paTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slM2paTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slM2paTransRateIntervalUpper for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slM2paTable_sets == 0)
				if ((ret = update_slM2paTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slM2paTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			slM2paTable_destroy(&StorageTmp->slM2paTable_old);
			StorageTmp->slM2paTable_rsvs = 0;
			StorageTmp->slM2paTable_tsts = 0;
			StorageTmp->slM2paTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slM2paTable_sets == 0)
			revert_slM2paTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			break;
		StorageTmp->slM2paTransRateIntervalUpper = StorageOld->slM2paTransRateIntervalUpper;
		if (--StorageTmp->slM2paTable_rsvs == 0)
			slM2paTable_destroy(&StorageTmp->slM2paTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slM2paSctpNoDelay(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slM2paSctpNoDelay(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slM2paTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slM2paSctpNoDelay entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slM2paTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slM2paRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paSctpNoDelay: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paSctpNoDelay not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paSctpNoDelay: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paSctpNoDelay: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			if (StorageTmp->slM2paTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slM2paTable_old = slM2paTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slM2paTable_rsvs++;
		StorageTmp->slM2paSctpNoDelay = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slM2paTable_tsts == 0)
				if ((ret = check_slM2paTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slM2paTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slM2paSctpNoDelay for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slM2paTable_sets == 0)
				if ((ret = update_slM2paTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slM2paTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			slM2paTable_destroy(&StorageTmp->slM2paTable_old);
			StorageTmp->slM2paTable_rsvs = 0;
			StorageTmp->slM2paTable_tsts = 0;
			StorageTmp->slM2paTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slM2paTable_sets == 0)
			revert_slM2paTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			break;
		StorageTmp->slM2paSctpNoDelay = StorageOld->slM2paSctpNoDelay;
		if (--StorageTmp->slM2paTable_rsvs == 0)
			slM2paTable_destroy(&StorageTmp->slM2paTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slM2paSctpMaxseg(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slM2paSctpMaxseg(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slM2paTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slM2paSctpMaxseg entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slM2paTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slM2paRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paSctpMaxseg: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paSctpMaxseg not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paSctpMaxseg: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 1..65536 */
		if ((1 > set_value || set_value > 65536)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paSctpMaxseg: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			if (StorageTmp->slM2paTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slM2paTable_old = slM2paTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slM2paTable_rsvs++;
		StorageTmp->slM2paSctpMaxseg = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slM2paTable_tsts == 0)
				if ((ret = check_slM2paTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slM2paTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slM2paSctpMaxseg for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slM2paTable_sets == 0)
				if ((ret = update_slM2paTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slM2paTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			slM2paTable_destroy(&StorageTmp->slM2paTable_old);
			StorageTmp->slM2paTable_rsvs = 0;
			StorageTmp->slM2paTable_tsts = 0;
			StorageTmp->slM2paTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slM2paTable_sets == 0)
			revert_slM2paTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			break;
		StorageTmp->slM2paSctpMaxseg = StorageOld->slM2paSctpMaxseg;
		if (--StorageTmp->slM2paTable_rsvs == 0)
			slM2paTable_destroy(&StorageTmp->slM2paTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slM2paSctpHeartbeatItvl(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slM2paSctpHeartbeatItvl(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slM2paTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slM2paSctpHeartbeatItvl entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slM2paTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slM2paRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paSctpHeartbeatItvl: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paSctpHeartbeatItvl not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paSctpHeartbeatItvl: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			if (StorageTmp->slM2paTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slM2paTable_old = slM2paTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slM2paTable_rsvs++;
		StorageTmp->slM2paSctpHeartbeatItvl = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slM2paTable_tsts == 0)
				if ((ret = check_slM2paTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slM2paTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slM2paSctpHeartbeatItvl for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slM2paTable_sets == 0)
				if ((ret = update_slM2paTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slM2paTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			slM2paTable_destroy(&StorageTmp->slM2paTable_old);
			StorageTmp->slM2paTable_rsvs = 0;
			StorageTmp->slM2paTable_tsts = 0;
			StorageTmp->slM2paTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slM2paTable_sets == 0)
			revert_slM2paTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			break;
		StorageTmp->slM2paSctpHeartbeatItvl = StorageOld->slM2paSctpHeartbeatItvl;
		if (--StorageTmp->slM2paTable_rsvs == 0)
			slM2paTable_destroy(&StorageTmp->slM2paTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slM2paSctpHeartbeat(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slM2paSctpHeartbeat(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slM2paTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slM2paSctpHeartbeat entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slM2paTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slM2paRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paSctpHeartbeat: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paSctpHeartbeat not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paSctpHeartbeat: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paSctpHeartbeat: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			if (StorageTmp->slM2paTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slM2paTable_old = slM2paTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slM2paTable_rsvs++;
		StorageTmp->slM2paSctpHeartbeat = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slM2paTable_tsts == 0)
				if ((ret = check_slM2paTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slM2paTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slM2paSctpHeartbeat for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slM2paTable_sets == 0)
				if ((ret = update_slM2paTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slM2paTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			slM2paTable_destroy(&StorageTmp->slM2paTable_old);
			StorageTmp->slM2paTable_rsvs = 0;
			StorageTmp->slM2paTable_tsts = 0;
			StorageTmp->slM2paTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slM2paTable_sets == 0)
			revert_slM2paTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			break;
		StorageTmp->slM2paSctpHeartbeat = StorageOld->slM2paSctpHeartbeat;
		if (--StorageTmp->slM2paTable_rsvs == 0)
			slM2paTable_destroy(&StorageTmp->slM2paTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slM2paSctpRtoInitial(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slM2paSctpRtoInitial(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slM2paTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slM2paSctpRtoInitial entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slM2paTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slM2paRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paSctpRtoInitial: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paSctpRtoInitial not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paSctpRtoInitial: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			if (StorageTmp->slM2paTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slM2paTable_old = slM2paTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slM2paTable_rsvs++;
		StorageTmp->slM2paSctpRtoInitial = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slM2paTable_tsts == 0)
				if ((ret = check_slM2paTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slM2paTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slM2paSctpRtoInitial for you to use, and you have just been asked to do something with it.  Note that anything done here 
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slM2paTable_sets == 0)
				if ((ret = update_slM2paTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slM2paTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			slM2paTable_destroy(&StorageTmp->slM2paTable_old);
			StorageTmp->slM2paTable_rsvs = 0;
			StorageTmp->slM2paTable_tsts = 0;
			StorageTmp->slM2paTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slM2paTable_sets == 0)
			revert_slM2paTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			break;
		StorageTmp->slM2paSctpRtoInitial = StorageOld->slM2paSctpRtoInitial;
		if (--StorageTmp->slM2paTable_rsvs == 0)
			slM2paTable_destroy(&StorageTmp->slM2paTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slM2paSctpRtoMin(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slM2paSctpRtoMin(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slM2paTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slM2paSctpRtoMin entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slM2paTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slM2paRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paSctpRtoMin: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paSctpRtoMin not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paSctpRtoMin: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			if (StorageTmp->slM2paTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slM2paTable_old = slM2paTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slM2paTable_rsvs++;
		StorageTmp->slM2paSctpRtoMin = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slM2paTable_tsts == 0)
				if ((ret = check_slM2paTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slM2paTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slM2paSctpRtoMin for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slM2paTable_sets == 0)
				if ((ret = update_slM2paTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slM2paTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			slM2paTable_destroy(&StorageTmp->slM2paTable_old);
			StorageTmp->slM2paTable_rsvs = 0;
			StorageTmp->slM2paTable_tsts = 0;
			StorageTmp->slM2paTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slM2paTable_sets == 0)
			revert_slM2paTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			break;
		StorageTmp->slM2paSctpRtoMin = StorageOld->slM2paSctpRtoMin;
		if (--StorageTmp->slM2paTable_rsvs == 0)
			slM2paTable_destroy(&StorageTmp->slM2paTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slM2paSctpRtoMax(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slM2paSctpRtoMax(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slM2paTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slM2paSctpRtoMax entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slM2paTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slM2paRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paSctpRtoMax: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paSctpRtoMax not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paSctpRtoMax: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			if (StorageTmp->slM2paTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slM2paTable_old = slM2paTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slM2paTable_rsvs++;
		StorageTmp->slM2paSctpRtoMax = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slM2paTable_tsts == 0)
				if ((ret = check_slM2paTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slM2paTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slM2paSctpRtoMax for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slM2paTable_sets == 0)
				if ((ret = update_slM2paTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slM2paTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			slM2paTable_destroy(&StorageTmp->slM2paTable_old);
			StorageTmp->slM2paTable_rsvs = 0;
			StorageTmp->slM2paTable_tsts = 0;
			StorageTmp->slM2paTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slM2paTable_sets == 0)
			revert_slM2paTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			break;
		StorageTmp->slM2paSctpRtoMax = StorageOld->slM2paSctpRtoMax;
		if (--StorageTmp->slM2paTable_rsvs == 0)
			slM2paTable_destroy(&StorageTmp->slM2paTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slM2paSctpPathMaxRetrans(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slM2paSctpPathMaxRetrans(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slM2paTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slM2paSctpPathMaxRetrans entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slM2paTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slM2paRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paSctpPathMaxRetrans: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paSctpPathMaxRetrans not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paSctpPathMaxRetrans: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			if (StorageTmp->slM2paTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slM2paTable_old = slM2paTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slM2paTable_rsvs++;
		StorageTmp->slM2paSctpPathMaxRetrans = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slM2paTable_tsts == 0)
				if ((ret = check_slM2paTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slM2paTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slM2paSctpPathMaxRetrans for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slM2paTable_sets == 0)
				if ((ret = update_slM2paTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slM2paTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			slM2paTable_destroy(&StorageTmp->slM2paTable_old);
			StorageTmp->slM2paTable_rsvs = 0;
			StorageTmp->slM2paTable_tsts = 0;
			StorageTmp->slM2paTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slM2paTable_sets == 0)
			revert_slM2paTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			break;
		StorageTmp->slM2paSctpPathMaxRetrans = StorageOld->slM2paSctpPathMaxRetrans;
		if (--StorageTmp->slM2paTable_rsvs == 0)
			slM2paTable_destroy(&StorageTmp->slM2paTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slM2paSctpCookieLife(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slM2paSctpCookieLife(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slM2paTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slM2paSctpCookieLife entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slM2paTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slM2paRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paSctpCookieLife: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paSctpCookieLife not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paSctpCookieLife: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			if (StorageTmp->slM2paTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slM2paTable_old = slM2paTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slM2paTable_rsvs++;
		StorageTmp->slM2paSctpCookieLife = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slM2paTable_tsts == 0)
				if ((ret = check_slM2paTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slM2paTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slM2paSctpCookieLife for you to use, and you have just been asked to do something with it.  Note that anything done here 
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slM2paTable_sets == 0)
				if ((ret = update_slM2paTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slM2paTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			slM2paTable_destroy(&StorageTmp->slM2paTable_old);
			StorageTmp->slM2paTable_rsvs = 0;
			StorageTmp->slM2paTable_tsts = 0;
			StorageTmp->slM2paTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slM2paTable_sets == 0)
			revert_slM2paTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			break;
		StorageTmp->slM2paSctpCookieLife = StorageOld->slM2paSctpCookieLife;
		if (--StorageTmp->slM2paTable_rsvs == 0)
			slM2paTable_destroy(&StorageTmp->slM2paTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slM2paSctpCookieInc(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slM2paSctpCookieInc(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slM2paTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slM2paSctpCookieInc entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slM2paTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slM2paRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paSctpCookieInc: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paSctpCookieInc not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paSctpCookieInc: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			if (StorageTmp->slM2paTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slM2paTable_old = slM2paTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slM2paTable_rsvs++;
		StorageTmp->slM2paSctpCookieInc = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slM2paTable_tsts == 0)
				if ((ret = check_slM2paTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slM2paTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slM2paSctpCookieInc for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slM2paTable_sets == 0)
				if ((ret = update_slM2paTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slM2paTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			slM2paTable_destroy(&StorageTmp->slM2paTable_old);
			StorageTmp->slM2paTable_rsvs = 0;
			StorageTmp->slM2paTable_tsts = 0;
			StorageTmp->slM2paTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slM2paTable_sets == 0)
			revert_slM2paTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			break;
		StorageTmp->slM2paSctpCookieInc = StorageOld->slM2paSctpCookieInc;
		if (--StorageTmp->slM2paTable_rsvs == 0)
			slM2paTable_destroy(&StorageTmp->slM2paTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slM2paSctpMaxInitRetries(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slM2paSctpMaxInitRetries(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slM2paTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slM2paSctpMaxInitRetries entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slM2paTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slM2paRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paSctpMaxInitRetries: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paSctpMaxInitRetries not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paSctpMaxInitRetries: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			if (StorageTmp->slM2paTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slM2paTable_old = slM2paTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slM2paTable_rsvs++;
		StorageTmp->slM2paSctpMaxInitRetries = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slM2paTable_tsts == 0)
				if ((ret = check_slM2paTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slM2paTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slM2paSctpMaxInitRetries for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slM2paTable_sets == 0)
				if ((ret = update_slM2paTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slM2paTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			slM2paTable_destroy(&StorageTmp->slM2paTable_old);
			StorageTmp->slM2paTable_rsvs = 0;
			StorageTmp->slM2paTable_tsts = 0;
			StorageTmp->slM2paTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slM2paTable_sets == 0)
			revert_slM2paTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			break;
		StorageTmp->slM2paSctpMaxInitRetries = StorageOld->slM2paSctpMaxInitRetries;
		if (--StorageTmp->slM2paTable_rsvs == 0)
			slM2paTable_destroy(&StorageTmp->slM2paTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slM2paSctpMaxBurst(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slM2paSctpMaxBurst(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slM2paTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slM2paSctpMaxBurst entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slM2paTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slM2paRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paSctpMaxBurst: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paSctpMaxBurst not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paSctpMaxBurst: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			if (StorageTmp->slM2paTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slM2paTable_old = slM2paTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slM2paTable_rsvs++;
		StorageTmp->slM2paSctpMaxBurst = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slM2paTable_tsts == 0)
				if ((ret = check_slM2paTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slM2paTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slM2paSctpMaxBurst for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slM2paTable_sets == 0)
				if ((ret = update_slM2paTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slM2paTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			slM2paTable_destroy(&StorageTmp->slM2paTable_old);
			StorageTmp->slM2paTable_rsvs = 0;
			StorageTmp->slM2paTable_tsts = 0;
			StorageTmp->slM2paTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slM2paTable_sets == 0)
			revert_slM2paTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			break;
		StorageTmp->slM2paSctpMaxBurst = StorageOld->slM2paSctpMaxBurst;
		if (--StorageTmp->slM2paTable_rsvs == 0)
			slM2paTable_destroy(&StorageTmp->slM2paTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slM2paSctpAssocMaxRetrans(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slM2paSctpAssocMaxRetrans(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slM2paTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slM2paSctpAssocMaxRetrans entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slM2paTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slM2paRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paSctpAssocMaxRetrans: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paSctpAssocMaxRetrans not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paSctpAssocMaxRetrans: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 20 */
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			if (StorageTmp->slM2paTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slM2paTable_old = slM2paTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slM2paTable_rsvs++;
		StorageTmp->slM2paSctpAssocMaxRetrans = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slM2paTable_tsts == 0)
				if ((ret = check_slM2paTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slM2paTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slM2paSctpAssocMaxRetrans for you to use, and you have just been asked to do something with it.  Note that anything done 
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slM2paTable_sets == 0)
				if ((ret = update_slM2paTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slM2paTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			slM2paTable_destroy(&StorageTmp->slM2paTable_old);
			StorageTmp->slM2paTable_rsvs = 0;
			StorageTmp->slM2paTable_tsts = 0;
			StorageTmp->slM2paTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slM2paTable_sets == 0)
			revert_slM2paTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			break;
		StorageTmp->slM2paSctpAssocMaxRetrans = StorageOld->slM2paSctpAssocMaxRetrans;
		if (--StorageTmp->slM2paTable_rsvs == 0)
			slM2paTable_destroy(&StorageTmp->slM2paTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slM2paSctpSackDelay(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slM2paSctpSackDelay(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slM2paTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slM2paSctpSackDelay entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slM2paTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slM2paRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paSctpSackDelay: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paSctpSackDelay not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paSctpSackDelay: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 20 */
		/* Note: ranges 0..50 */
		if ((0 > set_value || set_value > 50)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paSctpSackDelay: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			if (StorageTmp->slM2paTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slM2paTable_old = slM2paTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slM2paTable_rsvs++;
		StorageTmp->slM2paSctpSackDelay = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slM2paTable_tsts == 0)
				if ((ret = check_slM2paTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slM2paTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slM2paSctpSackDelay for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slM2paTable_sets == 0)
				if ((ret = update_slM2paTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slM2paTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			slM2paTable_destroy(&StorageTmp->slM2paTable_old);
			StorageTmp->slM2paTable_rsvs = 0;
			StorageTmp->slM2paTable_tsts = 0;
			StorageTmp->slM2paTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slM2paTable_sets == 0)
			revert_slM2paTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			break;
		StorageTmp->slM2paSctpSackDelay = StorageOld->slM2paSctpSackDelay;
		if (--StorageTmp->slM2paTable_rsvs == 0)
			slM2paTable_destroy(&StorageTmp->slM2paTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slM2paSctpLifetime(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slM2paSctpLifetime(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slM2paTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slM2paSctpLifetime entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slM2paTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slM2paRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paSctpLifetime: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paSctpLifetime not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paSctpLifetime: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			if (StorageTmp->slM2paTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slM2paTable_old = slM2paTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slM2paTable_rsvs++;
		StorageTmp->slM2paSctpLifetime = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slM2paTable_tsts == 0)
				if ((ret = check_slM2paTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slM2paTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slM2paSctpLifetime for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slM2paTable_sets == 0)
				if ((ret = update_slM2paTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slM2paTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			slM2paTable_destroy(&StorageTmp->slM2paTable_old);
			StorageTmp->slM2paTable_rsvs = 0;
			StorageTmp->slM2paTable_tsts = 0;
			StorageTmp->slM2paTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slM2paTable_sets == 0)
			revert_slM2paTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			break;
		StorageTmp->slM2paSctpLifetime = StorageOld->slM2paSctpLifetime;
		if (--StorageTmp->slM2paTable_rsvs == 0)
			slM2paTable_destroy(&StorageTmp->slM2paTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slM2paProvingAttempts(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slM2paProvingAttempts(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slM2paTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slM2paProvingAttempts entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slM2paTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slM2paRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paProvingAttempts: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paProvingAttempts not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paProvingAttempts: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			if (StorageTmp->slM2paTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slM2paTable_old = slM2paTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slM2paTable_rsvs++;
		StorageTmp->slM2paProvingAttempts = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slM2paTable_tsts == 0)
				if ((ret = check_slM2paTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slM2paTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slM2paProvingAttempts for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slM2paTable_sets == 0)
				if ((ret = update_slM2paTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slM2paTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
			slM2paTable_destroy(&StorageTmp->slM2paTable_old);
			StorageTmp->slM2paTable_rsvs = 0;
			StorageTmp->slM2paTable_tsts = 0;
			StorageTmp->slM2paTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slM2paTable_sets == 0)
			revert_slM2paTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
			break;
		StorageTmp->slM2paProvingAttempts = StorageOld->slM2paProvingAttempts;
		if (--StorageTmp->slM2paTable_rsvs == 0)
			slM2paTable_destroy(&StorageTmp->slM2paTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slSdtAdministrativeState(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slSdtAdministrativeState(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slSdtTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slSdtAdministrativeState entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slSdtTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slSdtRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSdtAdministrativeState: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSdtAdministrativeState not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSdtAdministrativeState: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value locked */
		switch (set_value) {
		case SLSDTADMINISTRATIVESTATE_LOCKED:
		case SLSDTADMINISTRATIVESTATE_UNLOCKED:
		case SLSDTADMINISTRATIVESTATE_SHUTTINGDOWN:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSdtAdministrativeState: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slSdtTable_old) == NULL)
			if (StorageTmp->slSdtTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slSdtTable_old = slSdtTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slSdtTable_rsvs++;
		StorageTmp->slSdtAdministrativeState = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slSdtTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slSdtTable_tsts == 0)
				if ((ret = check_slSdtTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSdtTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slSdtAdministrativeState for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slSdtTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slSdtTable_sets == 0)
				if ((ret = update_slSdtTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSdtTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slSdtTable_old) != NULL) {
			slSdtTable_destroy(&StorageTmp->slSdtTable_old);
			StorageTmp->slSdtTable_rsvs = 0;
			StorageTmp->slSdtTable_tsts = 0;
			StorageTmp->slSdtTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slSdtTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slSdtTable_sets == 0)
			revert_slSdtTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slSdtTable_old) == NULL)
			break;
		StorageTmp->slSdtAdministrativeState = StorageOld->slSdtAdministrativeState;
		if (--StorageTmp->slSdtTable_rsvs == 0)
			slSdtTable_destroy(&StorageTmp->slSdtTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slSdtEquipmentPointer(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slSdtEquipmentPointer(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slSdtTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	oid *objid = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slSdtEquipmentPointer entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slSdtTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slSdtRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSdtEquipmentPointer: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OBJECT_ID) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSdtEquipmentPointer not ASN_OBJECT_ID\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (MIN_OID_LEN * sizeof(oid) > var_val_len || var_val_len > MAX_OID_LEN * sizeof(oid)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSdtEquipmentPointer: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value zeroDotZero */
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slSdtTable_old) == NULL)
			if (StorageTmp->slSdtTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slSdtTable_old = slSdtTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slSdtTable_rsvs++;
		if ((objid = snmp_duplicate_objid((void *) var_val, var_val_len / sizeof(oid))) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		SNMP_FREE(StorageTmp->slSdtEquipmentPointer);
		StorageTmp->slSdtEquipmentPointer = objid;
		StorageTmp->slSdtEquipmentPointerLen = var_val_len / sizeof(oid);
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slSdtTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slSdtTable_tsts == 0)
				if ((ret = check_slSdtTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSdtTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slSdtEquipmentPointer for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slSdtTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slSdtTable_sets == 0)
				if ((ret = update_slSdtTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSdtTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slSdtTable_old) != NULL) {
			slSdtTable_destroy(&StorageTmp->slSdtTable_old);
			StorageTmp->slSdtTable_rsvs = 0;
			StorageTmp->slSdtTable_tsts = 0;
			StorageTmp->slSdtTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slSdtTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slSdtTable_sets == 0)
			revert_slSdtTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slSdtTable_old) == NULL)
			break;
		if (StorageOld->slSdtEquipmentPointer != NULL) {
			SNMP_FREE(StorageTmp->slSdtEquipmentPointer);
			StorageTmp->slSdtEquipmentPointer = StorageOld->slSdtEquipmentPointer;
			StorageTmp->slSdtEquipmentPointerLen = StorageOld->slSdtEquipmentPointerLen;
			StorageOld->slSdtEquipmentPointer = NULL;
			StorageOld->slSdtEquipmentPointerLen = 0;
		}
		if (--StorageTmp->slSdtTable_rsvs == 0)
			slSdtTable_destroy(&StorageTmp->slSdtTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slSdtName(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slSdtName(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slSdtTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slSdtName entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slSdtTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slSdtRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSdtName: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSdtName not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 0..32 */
		if (var_val_len > SPRINT_MAX_LEN || ((0 > var_val_len || var_val_len > 32))) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSdtName: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slSdtTable_old) == NULL)
			if (StorageTmp->slSdtTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slSdtTable_old = slSdtTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slSdtTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->slSdtName);
		StorageTmp->slSdtName = string;
		StorageTmp->slSdtNameLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slSdtTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slSdtTable_tsts == 0)
				if ((ret = check_slSdtTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSdtTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slSdtName for you to use, and you have just been asked to do something with it.  Note that anything done here must be
				   reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slSdtTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slSdtTable_sets == 0)
				if ((ret = update_slSdtTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSdtTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slSdtTable_old) != NULL) {
			slSdtTable_destroy(&StorageTmp->slSdtTable_old);
			StorageTmp->slSdtTable_rsvs = 0;
			StorageTmp->slSdtTable_tsts = 0;
			StorageTmp->slSdtTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slSdtTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slSdtTable_sets == 0)
			revert_slSdtTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slSdtTable_old) == NULL)
			break;
		if (StorageOld->slSdtName != NULL) {
			SNMP_FREE(StorageTmp->slSdtName);
			StorageTmp->slSdtName = StorageOld->slSdtName;
			StorageTmp->slSdtNameLen = StorageOld->slSdtNameLen;
			StorageOld->slSdtName = NULL;
			StorageOld->slSdtNameLen = 0;
		}
		if (--StorageTmp->slSdtTable_rsvs == 0)
			slSdtTable_destroy(&StorageTmp->slSdtTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slSdtProfilePointer(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slSdtProfilePointer(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slSdtTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slSdtProfilePointer entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slSdtTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slSdtRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSdtProfilePointer: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSdtProfilePointer not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 1..32 */
		if (var_val_len > SPRINT_MAX_LEN || ((1 > var_val_len || var_val_len > 32))) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSdtProfilePointer: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slSdtTable_old) == NULL)
			if (StorageTmp->slSdtTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slSdtTable_old = slSdtTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slSdtTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->slSdtProfilePointer);
		StorageTmp->slSdtProfilePointer = string;
		StorageTmp->slSdtProfilePointerLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slSdtTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slSdtTable_tsts == 0)
				if ((ret = check_slSdtTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSdtTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slSdtProfilePointer for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slSdtTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slSdtTable_sets == 0)
				if ((ret = update_slSdtTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSdtTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slSdtTable_old) != NULL) {
			slSdtTable_destroy(&StorageTmp->slSdtTable_old);
			StorageTmp->slSdtTable_rsvs = 0;
			StorageTmp->slSdtTable_tsts = 0;
			StorageTmp->slSdtTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slSdtTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slSdtTable_sets == 0)
			revert_slSdtTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slSdtTable_old) == NULL)
			break;
		if (StorageOld->slSdtProfilePointer != NULL) {
			SNMP_FREE(StorageTmp->slSdtProfilePointer);
			StorageTmp->slSdtProfilePointer = StorageOld->slSdtProfilePointer;
			StorageTmp->slSdtProfilePointerLen = StorageOld->slSdtProfilePointerLen;
			StorageOld->slSdtProfilePointer = NULL;
			StorageOld->slSdtProfilePointerLen = 0;
		}
		if (--StorageTmp->slSdtTable_rsvs == 0)
			slSdtTable_destroy(&StorageTmp->slSdtTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slSdlAdjPc(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slSdlAdjPc(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slSdlTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slSdlAdjPc entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slSdlTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slSdlRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSdlAdjPc: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSdlAdjPc not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 4..4 */
		if (var_val_len > SPRINT_MAX_LEN || (var_val_len != 4)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSdlAdjPc: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slSdlTable_old) == NULL)
			if (StorageTmp->slSdlTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slSdlTable_old = slSdlTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slSdlTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->slSdlAdjPc);
		StorageTmp->slSdlAdjPc = string;
		StorageTmp->slSdlAdjPcLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slSdlTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slSdlTable_tsts == 0)
				if ((ret = check_slSdlTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSdlTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slSdlAdjPc for you to use, and you have just been asked to do something with it.  Note that anything done here must be
				   reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slSdlTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slSdlTable_sets == 0)
				if ((ret = update_slSdlTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSdlTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slSdlTable_old) != NULL) {
			slSdlTable_destroy(&StorageTmp->slSdlTable_old);
			StorageTmp->slSdlTable_rsvs = 0;
			StorageTmp->slSdlTable_tsts = 0;
			StorageTmp->slSdlTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slSdlTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slSdlTable_sets == 0)
			revert_slSdlTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slSdlTable_old) == NULL)
			break;
		if (StorageOld->slSdlAdjPc != NULL) {
			SNMP_FREE(StorageTmp->slSdlAdjPc);
			StorageTmp->slSdlAdjPc = StorageOld->slSdlAdjPc;
			StorageTmp->slSdlAdjPcLen = StorageOld->slSdlAdjPcLen;
			StorageOld->slSdlAdjPc = NULL;
			StorageOld->slSdlAdjPcLen = 0;
		}
		if (--StorageTmp->slSdlTable_rsvs == 0)
			slSdlTable_destroy(&StorageTmp->slSdlTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slSdlTransmissionRate(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slSdlTransmissionRate(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slSdlTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slSdlTransmissionRate entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slSdlTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slSdlRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSdlTransmissionRate: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSdlTransmissionRate not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSdlTransmissionRate: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case SLSDLTRANSMISSIONRATE_KBITS4POINT8:
		case SLSDLTRANSMISSIONRATE_KBITS56:
		case SLSDLTRANSMISSIONRATE_KBITS64:
		case SLSDLTRANSMISSIONRATE_KBITS1544:
		case SLSDLTRANSMISSIONRATE_KBITS2048:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSdlTransmissionRate: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slSdlTable_old) == NULL)
			if (StorageTmp->slSdlTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slSdlTable_old = slSdlTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slSdlTable_rsvs++;
		StorageTmp->slSdlTransmissionRate = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slSdlTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slSdlTable_tsts == 0)
				if ((ret = check_slSdlTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSdlTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slSdlTransmissionRate for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slSdlTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slSdlTable_sets == 0)
				if ((ret = update_slSdlTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSdlTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slSdlTable_old) != NULL) {
			slSdlTable_destroy(&StorageTmp->slSdlTable_old);
			StorageTmp->slSdlTable_rsvs = 0;
			StorageTmp->slSdlTable_tsts = 0;
			StorageTmp->slSdlTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slSdlTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slSdlTable_sets == 0)
			revert_slSdlTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slSdlTable_old) == NULL)
			break;
		StorageTmp->slSdlTransmissionRate = StorageOld->slSdlTransmissionRate;
		if (--StorageTmp->slSdlTable_rsvs == 0)
			slSdlTable_destroy(&StorageTmp->slSdlTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slSdlLoopDelay(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slSdlLoopDelay(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slSdlTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	ulong set_value = *((ulong *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slSdlLoopDelay entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slSdlTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slSdlRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSdlLoopDelay: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSdlLoopDelay not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSdlLoopDelay: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 0 */
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slSdlTable_old) == NULL)
			if (StorageTmp->slSdlTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slSdlTable_old = slSdlTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slSdlTable_rsvs++;
		StorageTmp->slSdlLoopDelay = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slSdlTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slSdlTable_tsts == 0)
				if ((ret = check_slSdlTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSdlTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slSdlLoopDelay for you to use, and you have just been asked to do something with it.  Note that anything done here must
				   be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slSdlTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slSdlTable_sets == 0)
				if ((ret = update_slSdlTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSdlTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slSdlTable_old) != NULL) {
			slSdlTable_destroy(&StorageTmp->slSdlTable_old);
			StorageTmp->slSdlTable_rsvs = 0;
			StorageTmp->slSdlTable_tsts = 0;
			StorageTmp->slSdlTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slSdlTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slSdlTable_sets == 0)
			revert_slSdlTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slSdlTable_old) == NULL)
			break;
		StorageTmp->slSdlLoopDelay = StorageOld->slSdlLoopDelay;
		if (--StorageTmp->slSdlTable_rsvs == 0)
			slSdlTable_destroy(&StorageTmp->slSdlTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slSdlEquipmentPointer(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slSdlEquipmentPointer(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slSdlTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	oid *objid = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slSdlEquipmentPointer entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slSdlTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slSdlRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSdlEquipmentPointer: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OBJECT_ID) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSdlEquipmentPointer not ASN_OBJECT_ID\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (MIN_OID_LEN * sizeof(oid) > var_val_len || var_val_len > MAX_OID_LEN * sizeof(oid)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSdlEquipmentPointer: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value zeroDotZero */
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slSdlTable_old) == NULL)
			if (StorageTmp->slSdlTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slSdlTable_old = slSdlTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slSdlTable_rsvs++;
		if ((objid = snmp_duplicate_objid((void *) var_val, var_val_len / sizeof(oid))) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		SNMP_FREE(StorageTmp->slSdlEquipmentPointer);
		StorageTmp->slSdlEquipmentPointer = objid;
		StorageTmp->slSdlEquipmentPointerLen = var_val_len / sizeof(oid);
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slSdlTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slSdlTable_tsts == 0)
				if ((ret = check_slSdlTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSdlTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slSdlEquipmentPointer for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slSdlTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slSdlTable_sets == 0)
				if ((ret = update_slSdlTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSdlTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slSdlTable_old) != NULL) {
			slSdlTable_destroy(&StorageTmp->slSdlTable_old);
			StorageTmp->slSdlTable_rsvs = 0;
			StorageTmp->slSdlTable_tsts = 0;
			StorageTmp->slSdlTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slSdlTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slSdlTable_sets == 0)
			revert_slSdlTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slSdlTable_old) == NULL)
			break;
		if (StorageOld->slSdlEquipmentPointer != NULL) {
			SNMP_FREE(StorageTmp->slSdlEquipmentPointer);
			StorageTmp->slSdlEquipmentPointer = StorageOld->slSdlEquipmentPointer;
			StorageTmp->slSdlEquipmentPointerLen = StorageOld->slSdlEquipmentPointerLen;
			StorageOld->slSdlEquipmentPointer = NULL;
			StorageOld->slSdlEquipmentPointerLen = 0;
		}
		if (--StorageTmp->slSdlTable_rsvs == 0)
			slSdlTable_destroy(&StorageTmp->slSdlTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slSdlCIC(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slSdlCIC(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slSdlTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	long set_value = *((long *) var_val);
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slSdlCIC entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slSdlTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slSdlRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSdlCIC: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSdlCIC not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSdlCIC: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 0 */
		/* Note: ranges 0..65535 */
		if ((0 > set_value || set_value > 65535)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSdlCIC: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slSdlTable_old) == NULL)
			if (StorageTmp->slSdlTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slSdlTable_old = slSdlTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slSdlTable_rsvs++;
		StorageTmp->slSdlCIC = set_value;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slSdlTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slSdlTable_tsts == 0)
				if ((ret = check_slSdlTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSdlTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slSdlCIC for you to use, and you have just been asked to do something with it.  Note that anything done here must be
				   reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slSdlTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slSdlTable_sets == 0)
				if ((ret = update_slSdlTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSdlTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slSdlTable_old) != NULL) {
			slSdlTable_destroy(&StorageTmp->slSdlTable_old);
			StorageTmp->slSdlTable_rsvs = 0;
			StorageTmp->slSdlTable_tsts = 0;
			StorageTmp->slSdlTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slSdlTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slSdlTable_sets == 0)
			revert_slSdlTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slSdlTable_old) == NULL)
			break;
		StorageTmp->slSdlCIC = StorageOld->slSdlCIC;
		if (--StorageTmp->slSdlTable_rsvs == 0)
			slSdlTable_destroy(&StorageTmp->slSdlTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slSdlName(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slSdlName(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slSdlTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slSdlName entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slSdlTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slSdlRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSdlName: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSdlName not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 0..32 */
		if (var_val_len > SPRINT_MAX_LEN || ((0 > var_val_len || var_val_len > 32))) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSdlName: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slSdlTable_old) == NULL)
			if (StorageTmp->slSdlTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slSdlTable_old = slSdlTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slSdlTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->slSdlName);
		StorageTmp->slSdlName = string;
		StorageTmp->slSdlNameLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slSdlTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slSdlTable_tsts == 0)
				if ((ret = check_slSdlTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSdlTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slSdlName for you to use, and you have just been asked to do something with it.  Note that anything done here must be
				   reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slSdlTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slSdlTable_sets == 0)
				if ((ret = update_slSdlTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSdlTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slSdlTable_old) != NULL) {
			slSdlTable_destroy(&StorageTmp->slSdlTable_old);
			StorageTmp->slSdlTable_rsvs = 0;
			StorageTmp->slSdlTable_tsts = 0;
			StorageTmp->slSdlTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slSdlTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slSdlTable_sets == 0)
			revert_slSdlTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slSdlTable_old) == NULL)
			break;
		if (StorageOld->slSdlName != NULL) {
			SNMP_FREE(StorageTmp->slSdlName);
			StorageTmp->slSdlName = StorageOld->slSdlName;
			StorageTmp->slSdlNameLen = StorageOld->slSdlNameLen;
			StorageOld->slSdlName = NULL;
			StorageOld->slSdlNameLen = 0;
		}
		if (--StorageTmp->slSdlTable_rsvs == 0)
			slSdlTable_destroy(&StorageTmp->slSdlTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slSdlStmChannel(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slSdlStmChannel(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slSdlTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	uint8_t *string = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slSdlStmChannel entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slSdlTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slSdlRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSdlStmChannel: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSdlStmChannel not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 4..4 */
		if (var_val_len > SPRINT_MAX_LEN || (var_val_len != 4)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSdlStmChannel: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slSdlTable_old) == NULL)
			if (StorageTmp->slSdlTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slSdlTable_old = slSdlTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slSdlTable_rsvs++;
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		SNMP_FREE(StorageTmp->slSdlStmChannel);
		StorageTmp->slSdlStmChannel = string;
		StorageTmp->slSdlStmChannelLen = var_val_len;
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slSdlTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slSdlTable_tsts == 0)
				if ((ret = check_slSdlTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSdlTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slSdlStmChannel for you to use, and you have just been asked to do something with it.  Note that anything done here must 
				   be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slSdlTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slSdlTable_sets == 0)
				if ((ret = update_slSdlTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSdlTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slSdlTable_old) != NULL) {
			slSdlTable_destroy(&StorageTmp->slSdlTable_old);
			StorageTmp->slSdlTable_rsvs = 0;
			StorageTmp->slSdlTable_tsts = 0;
			StorageTmp->slSdlTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slSdlTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slSdlTable_sets == 0)
			revert_slSdlTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slSdlTable_old) == NULL)
			break;
		if (StorageOld->slSdlStmChannel != NULL) {
			SNMP_FREE(StorageTmp->slSdlStmChannel);
			StorageTmp->slSdlStmChannel = StorageOld->slSdlStmChannel;
			StorageTmp->slSdlStmChannelLen = StorageOld->slSdlStmChannelLen;
			StorageOld->slSdlStmChannel = NULL;
			StorageOld->slSdlStmChannelLen = 0;
		}
		if (--StorageTmp->slSdlTable_rsvs == 0)
			slSdlTable_destroy(&StorageTmp->slSdlTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slSdlVcTTpPointer(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_slSdlVcTTpPointer(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slSdlTable_data *StorageTmp = NULL, *StorageOld = NULL;
	size_t newlen = name_len - 16;
	oid *objid = NULL;
	int ret = SNMP_ERR_NOERROR;

	DEBUGMSGTL(("slMIB", "write_slSdlVcTTpPointer entering action=%d...  \n", action));
	if (StorageTmp == NULL)
		return SNMP_ERR_NOSUCHNAME;
	StorageTmp = header_complex(slSdlTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->slSdlRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSdlVcTTpPointer: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OBJECT_ID) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSdlVcTTpPointer not ASN_OBJECT_ID\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (MIN_OID_LEN * sizeof(oid) > var_val_len || var_val_len > MAX_OID_LEN * sizeof(oid)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSdlVcTTpPointer: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* one allocation for the whole row */
		if ((StorageOld = StorageTmp->slSdlTable_old) == NULL)
			if (StorageTmp->slSdlTable_rsvs == 0)
				if ((StorageOld = StorageTmp->slSdlTable_old = slSdlTable_duplicate(StorageTmp)) == NULL)
					return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageOld != NULL)
			StorageTmp->slSdlTable_rsvs++;
		if ((objid = snmp_duplicate_objid((void *) var_val, var_val_len / sizeof(oid))) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		SNMP_FREE(StorageTmp->slSdlVcTTpPointer);
		StorageTmp->slSdlVcTTpPointer = objid;
		StorageTmp->slSdlVcTTpPointerLen = var_val_len / sizeof(oid);
		/* XXX: insert code to consistency check this particular varbind, if necessary (so error codes are applied to varbinds) */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((StorageOld = StorageTmp->slSdlTable_old) != NULL) {
			/* one consistency check for the whole row */
			if (StorageTmp->slSdlTable_tsts == 0)
				if ((ret = check_slSdlTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSdlTable_tsts++;
		}
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->slSdlVcTTpPointer for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		/* XXX: insert code to set this particular varbind, if necessary */
		/* one set action for the whole row */
		if ((StorageOld = StorageTmp->slSdlTable_old) != NULL) {
			/* XXX: insert code to set this particular varbind, if necessary */
			if (StorageTmp->slSdlTable_sets == 0)
				if ((ret = update_slSdlTable_row(StorageTmp, StorageOld)) != SNMP_ERR_NOERROR)
					return (ret);
			StorageTmp->slSdlTable_sets++;
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		/* one commit for the whole mib */
		if ((StorageOld = StorageTmp->slSdlTable_old) != NULL) {
			slSdlTable_destroy(&StorageTmp->slSdlTable_old);
			StorageTmp->slSdlTable_rsvs = 0;
			StorageTmp->slSdlTable_tsts = 0;
			StorageTmp->slSdlTable_sets = 0;
		}
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		if ((StorageOld = StorageTmp->slSdlTable_old) == NULL)
			break;
		/* XXX: insert code to undo any action performed on this particular varbind */
		if (--StorageTmp->slSdlTable_sets == 0)
			revert_slSdlTable_row(StorageTmp, StorageOld);
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		if ((StorageOld = StorageTmp->slSdlTable_old) == NULL)
			break;
		if (StorageOld->slSdlVcTTpPointer != NULL) {
			SNMP_FREE(StorageTmp->slSdlVcTTpPointer);
			StorageTmp->slSdlVcTTpPointer = StorageOld->slSdlVcTTpPointer;
			StorageTmp->slSdlVcTTpPointerLen = StorageOld->slSdlVcTTpPointerLen;
			StorageOld->slSdlVcTTpPointer = NULL;
			StorageOld->slSdlVcTTpPointerLen = 0;
		}
		if (--StorageTmp->slSdlTable_rsvs == 0)
			slSdlTable_destroy(&StorageTmp->slSdlTable_old);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_slProfileTable_row(struct slProfileTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_slProfileTable_row(struct slProfileTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_slProfileTable_row(struct slProfileTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_slProfileTable_row(struct slProfileTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_slTable_row(struct slTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_slTable_row(struct slTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_slTable_row(struct slTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_slTable_row(struct slTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_slSdlListTable_row(struct slSdlListTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_slSdlListTable_row(struct slSdlListTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_slSdlListTable_row(struct slSdlListTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_slSdlListTable_row(struct slSdlListTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_slSdtListTable_row(struct slSdtListTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_slSdtListTable_row(struct slSdtListTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_slSdtListTable_row(struct slSdtListTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_slSdtListTable_row(struct slSdtListTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_slNbTable_row(struct slNbTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_slNbTable_row(struct slNbTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_slNbTable_row(struct slNbTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_slNbTable_row(struct slNbTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_slSaalTable_row(struct slSaalTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_slSaalTable_row(struct slSaalTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_slSaalTable_row(struct slSaalTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_slSaalTable_row(struct slSaalTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_slM2paTable_row(struct slM2paTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_slM2paTable_row(struct slM2paTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_slM2paTable_row(struct slM2paTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_slM2paTable_row(struct slM2paTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_slSdtTable_row(struct slSdtTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_slSdtTable_row(struct slSdtTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_slSdtTable_row(struct slSdtTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_slSdtTable_row(struct slSdtTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_slSdlTable_row(struct slSdlTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_slSdlTable_row(struct slSdlTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_slSdlTable_row(struct slSdlTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_slSdlTable_row(struct slSdlTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slProfileRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_slProfileRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slProfileTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct slProfileTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 16;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("slMIB", "write_slProfileRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(slProfileTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to slProfileRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* slProfileId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* slProfileTransmissionRate */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_INTEGER, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[16]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* slProfileId */
			/* Note: ranges 1..32 */
			if (vp->val_len > SPRINT_MAX_LEN || ((1 > vp->val_len || vp->val_len > 32))) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index slProfileId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* slProfileTransmissionRate */
			if (vp->val_len > sizeof(int32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index slProfileTransmissionRate: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			switch ((long) *vp->val.integer) {
			case SLPROFILETRANSMISSIONRATE_KBITS4POINT8:
			case SLPROFILETRANSMISSIONRATE_KBITS56:
			case SLPROFILETRANSMISSIONRATE_KBITS64:
			case SLPROFILETRANSMISSIONRATE_KBITS1544:
			case SLPROFILETRANSMISSIONRATE_KBITS2048:
				break;
			default:
				snmp_log(MY_FACILITY(LOG_NOTICE), "index slProfileTransmissionRate: bad value\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = slProfileTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->slProfileTable_rsvs = 1;
			vp = vars;
			memdup((void *) &StorageNew->slProfileId, vp->val.string, vp->val_len);
			StorageNew->slProfileIdLen = vp->val_len;
			vp = vp->next_variable;
			StorageNew->slProfileTransmissionRate = (long) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&slProfileTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->slProfileRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->slProfileTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
				if (StorageTmp->slProfileTable_rsvs == 0)
					if ((StorageOld = StorageTmp->slProfileTable_old = slProfileTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->slProfileTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->slProfileTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				slProfileTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_slProfileTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* check that activation is possible */
			if (StorageTmp->slProfileRowStatus != RS_ACTIVE)
				if ((ret = can_act_slProfileTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->slProfileRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_slProfileTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->slProfileRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_slProfileTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->slProfileRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here must be
		   reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_slProfileTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->slProfileRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_slProfileTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->slProfileRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_slProfileTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destruction to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_slProfileTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->slProfileRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->slProfileRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->slProfileRowStatus = set_value;
			if ((StorageOld = StorageTmp->slProfileTable_old) != NULL) {
				slProfileTable_destroy(&StorageTmp->slProfileTable_old);
				StorageTmp->slProfileTable_rsvs = 0;
				StorageTmp->slProfileTable_tsts = 0;
				StorageTmp->slProfileTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			slProfileTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_slProfileTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->slProfileRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_slProfileTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->slProfileRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_slProfileTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				slProfileTable_del(StorageNew);
				slProfileTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->slProfileTable_old) == NULL)
				break;
			if (--StorageTmp->slProfileTable_rsvs == 0)
				slProfileTable_destroy(&StorageTmp->slProfileTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				slProfileTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_slRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct slTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 16;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("slMIB", "write_slRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(slTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to slRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to slRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* slId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[16]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* slId */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index slId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = slTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->slTable_rsvs = 1;
			vp = vars;
			StorageNew->slId = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&slTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->slRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->slTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->slTable_old) == NULL)
				if (StorageTmp->slTable_rsvs == 0)
					if ((StorageOld = StorageTmp->slTable_old = slTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->slTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->slTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				slTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_slTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* check that activation is possible */
			if (StorageTmp->slRowStatus != RS_ACTIVE)
				if ((ret = can_act_slTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->slRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_slTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->slRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_slTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->slRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in
		   the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_slTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->slRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_slTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->slRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_slTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destruction to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_slTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->slRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->slRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->slRowStatus = set_value;
			if ((StorageOld = StorageTmp->slTable_old) != NULL) {
				slTable_destroy(&StorageTmp->slTable_old);
				StorageTmp->slTable_rsvs = 0;
				StorageTmp->slTable_tsts = 0;
				StorageTmp->slTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			slTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_slTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->slRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_slTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->slRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_slTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				slTable_del(StorageNew);
				slTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->slTable_old) == NULL)
				break;
			if (--StorageTmp->slTable_rsvs == 0)
				slTable_destroy(&StorageTmp->slTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				slTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slSdlListRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_slSdlListRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slSdlListTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct slSdlListTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 16;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("slMIB", "write_slSdlListRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(slSdlListTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSdlListRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSdlListRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* slId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* slSdlListId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[16]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* slId */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index slId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* slSdlListId */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index slSdlListId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = slSdlListTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->slSdlListTable_rsvs = 1;
			vp = vars;
			StorageNew->slId = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->slSdlListId = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&slSdlListTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->slSdlListRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->slSdlListTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->slSdlListTable_old) == NULL)
				if (StorageTmp->slSdlListTable_rsvs == 0)
					if ((StorageOld = StorageTmp->slSdlListTable_old = slSdlListTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->slSdlListTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->slSdlListTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				slSdlListTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_slSdlListTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* check that activation is possible */
			if (StorageTmp->slSdlListRowStatus != RS_ACTIVE)
				if ((ret = can_act_slSdlListTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->slSdlListRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_slSdlListTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->slSdlListRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_slSdlListTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->slSdlListRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here must be
		   reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_slSdlListTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->slSdlListRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_slSdlListTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->slSdlListRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_slSdlListTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destruction to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_slSdlListTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->slSdlListRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->slSdlListRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->slSdlListRowStatus = set_value;
			if ((StorageOld = StorageTmp->slSdlListTable_old) != NULL) {
				slSdlListTable_destroy(&StorageTmp->slSdlListTable_old);
				StorageTmp->slSdlListTable_rsvs = 0;
				StorageTmp->slSdlListTable_tsts = 0;
				StorageTmp->slSdlListTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			slSdlListTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_slSdlListTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->slSdlListRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_slSdlListTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->slSdlListRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_slSdlListTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				slSdlListTable_del(StorageNew);
				slSdlListTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->slSdlListTable_old) == NULL)
				break;
			if (--StorageTmp->slSdlListTable_rsvs == 0)
				slSdlListTable_destroy(&StorageTmp->slSdlListTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				slSdlListTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slSdtListRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_slSdtListRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slSdtListTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct slSdtListTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 16;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("slMIB", "write_slSdtListRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(slSdtListTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSdtListRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSdtListRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* slId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* slSdtListId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[16]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* slId */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index slId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* slSdtListId */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index slSdtListId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = slSdtListTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->slSdtListTable_rsvs = 1;
			vp = vars;
			StorageNew->slId = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->slSdtListId = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&slSdtListTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->slSdtListRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->slSdtListTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->slSdtListTable_old) == NULL)
				if (StorageTmp->slSdtListTable_rsvs == 0)
					if ((StorageOld = StorageTmp->slSdtListTable_old = slSdtListTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->slSdtListTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->slSdtListTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				slSdtListTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_slSdtListTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* check that activation is possible */
			if (StorageTmp->slSdtListRowStatus != RS_ACTIVE)
				if ((ret = can_act_slSdtListTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->slSdtListRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_slSdtListTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->slSdtListRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_slSdtListTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->slSdtListRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here must be
		   reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_slSdtListTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->slSdtListRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_slSdtListTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->slSdtListRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_slSdtListTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destruction to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_slSdtListTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->slSdtListRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->slSdtListRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->slSdtListRowStatus = set_value;
			if ((StorageOld = StorageTmp->slSdtListTable_old) != NULL) {
				slSdtListTable_destroy(&StorageTmp->slSdtListTable_old);
				StorageTmp->slSdtListTable_rsvs = 0;
				StorageTmp->slSdtListTable_tsts = 0;
				StorageTmp->slSdtListTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			slSdtListTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_slSdtListTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->slSdtListRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_slSdtListTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->slSdtListRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_slSdtListTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				slSdtListTable_del(StorageNew);
				slSdtListTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->slSdtListTable_old) == NULL)
				break;
			if (--StorageTmp->slSdtListTable_rsvs == 0)
				slSdtListTable_destroy(&StorageTmp->slSdtListTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				slSdtListTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slNbRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_slNbRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slNbTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct slNbTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 16;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("slMIB", "write_slNbRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(slNbTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to slNbRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to slNbRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* slSdtProfileId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[16]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* slSdtProfileId */
			/* Note: ranges 1..32 */
			if (vp->val_len > SPRINT_MAX_LEN || ((1 > vp->val_len || vp->val_len > 32))) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index slSdtProfileId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = slNbTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->slNbTable_rsvs = 1;
			vp = vars;
			memdup((void *) &StorageNew->slSdtProfileId, vp->val.string, vp->val_len);
			StorageNew->slSdtProfileIdLen = vp->val_len;
			vp = vp->next_variable;
			header_complex_add_data(&slNbTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->slNbRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->slNbTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->slNbTable_old) == NULL)
				if (StorageTmp->slNbTable_rsvs == 0)
					if ((StorageOld = StorageTmp->slNbTable_old = slNbTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->slNbTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->slNbTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				slNbTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_slNbTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* check that activation is possible */
			if (StorageTmp->slNbRowStatus != RS_ACTIVE)
				if ((ret = can_act_slNbTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->slNbRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_slNbTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->slNbRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_slNbTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->slNbRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in
		   the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_slNbTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->slNbRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_slNbTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->slNbRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_slNbTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destruction to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_slNbTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->slNbRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->slNbRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->slNbRowStatus = set_value;
			if ((StorageOld = StorageTmp->slNbTable_old) != NULL) {
				slNbTable_destroy(&StorageTmp->slNbTable_old);
				StorageTmp->slNbTable_rsvs = 0;
				StorageTmp->slNbTable_tsts = 0;
				StorageTmp->slNbTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			slNbTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_slNbTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->slNbRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_slNbTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->slNbRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_slNbTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				slNbTable_del(StorageNew);
				slNbTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->slNbTable_old) == NULL)
				break;
			if (--StorageTmp->slNbTable_rsvs == 0)
				slNbTable_destroy(&StorageTmp->slNbTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				slNbTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slSaalRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_slSaalRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slSaalTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct slSaalTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 16;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("slMIB", "write_slSaalRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(slSaalTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSaalRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* slSdtProfileId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[16]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* slSdtProfileId */
			/* Note: ranges 1..32 */
			if (vp->val_len > SPRINT_MAX_LEN || ((1 > vp->val_len || vp->val_len > 32))) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index slSdtProfileId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = slSaalTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->slSaalTable_rsvs = 1;
			vp = vars;
			memdup((void *) &StorageNew->slSdtProfileId, vp->val.string, vp->val_len);
			StorageNew->slSdtProfileIdLen = vp->val_len;
			vp = vp->next_variable;
			header_complex_add_data(&slSaalTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->slSaalRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->slSaalTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
				if (StorageTmp->slSaalTable_rsvs == 0)
					if ((StorageOld = StorageTmp->slSaalTable_old = slSaalTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->slSaalTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->slSaalTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				slSaalTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_slSaalTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* check that activation is possible */
			if (StorageTmp->slSaalRowStatus != RS_ACTIVE)
				if ((ret = can_act_slSaalTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->slSaalRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_slSaalTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->slSaalRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_slSaalTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->slSaalRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable
		   in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_slSaalTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->slSaalRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_slSaalTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->slSaalRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_slSaalTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destruction to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_slSaalTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->slSaalRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->slSaalRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->slSaalRowStatus = set_value;
			if ((StorageOld = StorageTmp->slSaalTable_old) != NULL) {
				slSaalTable_destroy(&StorageTmp->slSaalTable_old);
				StorageTmp->slSaalTable_rsvs = 0;
				StorageTmp->slSaalTable_tsts = 0;
				StorageTmp->slSaalTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			slSaalTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_slSaalTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->slSaalRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_slSaalTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->slSaalRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_slSaalTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				slSaalTable_del(StorageNew);
				slSaalTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->slSaalTable_old) == NULL)
				break;
			if (--StorageTmp->slSaalTable_rsvs == 0)
				slSaalTable_destroy(&StorageTmp->slSaalTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				slSaalTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slM2paRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_slM2paRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slM2paTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct slM2paTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 16;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("slMIB", "write_slM2paRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(slM2paTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to slM2paRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* slSdtProfileId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[16]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* slSdtProfileId */
			/* Note: ranges 1..32 */
			if (vp->val_len > SPRINT_MAX_LEN || ((1 > vp->val_len || vp->val_len > 32))) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index slSdtProfileId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = slM2paTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->slM2paTable_rsvs = 1;
			vp = vars;
			memdup((void *) &StorageNew->slSdtProfileId, vp->val.string, vp->val_len);
			StorageNew->slSdtProfileIdLen = vp->val_len;
			vp = vp->next_variable;
			header_complex_add_data(&slM2paTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->slM2paRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->slM2paTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
				if (StorageTmp->slM2paTable_rsvs == 0)
					if ((StorageOld = StorageTmp->slM2paTable_old = slM2paTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->slM2paTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->slM2paTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				slM2paTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_slM2paTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* check that activation is possible */
			if (StorageTmp->slM2paRowStatus != RS_ACTIVE)
				if ((ret = can_act_slM2paTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->slM2paRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_slM2paTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->slM2paRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_slM2paTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->slM2paRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable
		   in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_slM2paTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->slM2paRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_slM2paTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->slM2paRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_slM2paTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destruction to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_slM2paTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->slM2paRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->slM2paRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->slM2paRowStatus = set_value;
			if ((StorageOld = StorageTmp->slM2paTable_old) != NULL) {
				slM2paTable_destroy(&StorageTmp->slM2paTable_old);
				StorageTmp->slM2paTable_rsvs = 0;
				StorageTmp->slM2paTable_tsts = 0;
				StorageTmp->slM2paTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			slM2paTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_slM2paTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->slM2paRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_slM2paTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->slM2paRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_slM2paTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				slM2paTable_del(StorageNew);
				slM2paTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->slM2paTable_old) == NULL)
				break;
			if (--StorageTmp->slM2paTable_rsvs == 0)
				slM2paTable_destroy(&StorageTmp->slM2paTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				slM2paTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slSdtRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_slSdtRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slSdtTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct slSdtTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 16;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("slMIB", "write_slSdtRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(slSdtTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSdtRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSdtRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* slSdtId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[16]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* slSdtId */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index slSdtId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = slSdtTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->slSdtTable_rsvs = 1;
			vp = vars;
			StorageNew->slSdtId = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&slSdtTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->slSdtRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->slSdtTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->slSdtTable_old) == NULL)
				if (StorageTmp->slSdtTable_rsvs == 0)
					if ((StorageOld = StorageTmp->slSdtTable_old = slSdtTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->slSdtTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->slSdtTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				slSdtTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_slSdtTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* check that activation is possible */
			if (StorageTmp->slSdtRowStatus != RS_ACTIVE)
				if ((ret = can_act_slSdtTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->slSdtRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_slSdtTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->slSdtRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_slSdtTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->slSdtRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in 
		   the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_slSdtTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->slSdtRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_slSdtTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->slSdtRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_slSdtTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destruction to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_slSdtTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->slSdtRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->slSdtRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->slSdtRowStatus = set_value;
			if ((StorageOld = StorageTmp->slSdtTable_old) != NULL) {
				slSdtTable_destroy(&StorageTmp->slSdtTable_old);
				StorageTmp->slSdtTable_rsvs = 0;
				StorageTmp->slSdtTable_tsts = 0;
				StorageTmp->slSdtTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			slSdtTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_slSdtTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->slSdtRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_slSdtTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->slSdtRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_slSdtTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				slSdtTable_del(StorageNew);
				slSdtTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->slSdtTable_old) == NULL)
				break;
			if (--StorageTmp->slSdtTable_rsvs == 0)
				slSdtTable_destroy(&StorageTmp->slSdtTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				slSdtTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_slSdlRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_slSdlRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct slSdlTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct slSdlTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 16;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("slMIB", "write_slSdlRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(slSdlTableStorage, NULL, &name[16], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSdlRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to slSdlRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* slSdlId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[16]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* slSdlId */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index slSdlId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = slSdlTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->slSdlTable_rsvs = 1;
			vp = vars;
			StorageNew->slSdlId = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&slSdlTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->slSdlRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->slSdlTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->slSdlTable_old) == NULL)
				if (StorageTmp->slSdlTable_rsvs == 0)
					if ((StorageOld = StorageTmp->slSdlTable_old = slSdlTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->slSdlTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->slSdlTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				slSdlTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_slSdlTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* check that activation is possible */
			if (StorageTmp->slSdlRowStatus != RS_ACTIVE)
				if ((ret = can_act_slSdlTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->slSdlRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_slSdlTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->slSdlRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_slSdlTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->slSdlRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in 
		   the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_slSdlTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->slSdlRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_slSdlTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->slSdlRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_slSdlTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destruction to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_slSdlTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->slSdlRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->slSdlRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->slSdlRowStatus = set_value;
			if ((StorageOld = StorageTmp->slSdlTable_old) != NULL) {
				slSdlTable_destroy(&StorageTmp->slSdlTable_old);
				StorageTmp->slSdlTable_rsvs = 0;
				StorageTmp->slSdlTable_tsts = 0;
				StorageTmp->slSdlTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			slSdlTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_slSdlTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->slSdlRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_slSdlTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->slSdlRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_slSdlTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				slSdlTable_del(StorageNew);
				slSdlTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->slSdlTable_old) == NULL)
				break;
			if (--StorageTmp->slSdlTable_rsvs == 0)
				slSdlTable_destroy(&StorageTmp->slSdlTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				slSdlTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn void slMIB_loop_handler(int dummy)
 * @param sig signal number
 * @brief handle event loop interation.
 *
 * This function is registered so that, when operating as a module, snmpd will call it one per event
 * loop interation.  This function is called before the next requst is processed and after the
 * previous request is processed.  Two things are done here:  1) The file descriptor that is used to
 * synchronize the agent with (pseudo-)device drivers is closed.  (Another approach, instead of
 * closing each time, would be to restart a timer each time that a request is made (loop is
 * performed) and if it expires, close the file descriptor).  2) The request number is incremented.
 * Although a request is not generated for each loop of the snmp event loop, it is true that a new
 * request cannot be generated without performing a loop.  Therefore, the sa_request is not the
 * request number but it is a temporally unique identifier for a request.
 */
void
slMIB_loop_handler(int sig)
{
	DEBUGMSGTL(("slMIB", "slMIB_loop_handler: executing loop handler...  "));
	/* close files after each request */
	if (sa_fclose) {
		if (my_fd >= 0) {
			close(my_fd);
			my_fd = -1;
		}
	}
#if defined MASTER
	/* prepare for next request */
	sa_request++;
#endif				/* defined MASTER */
	if (external_signal_scheduled[sig] == 0)
		external_signal_scheduled[sig]--;
	if (slMIBold_signal_handler != NULL)
		(*slMIBold_signal_handler) (sig);
	DEBUGMSGTL(("slMIB", "done.\n"));
}

/**
 * @fn void slMIB_fd_handler(int fd, void *dummy)
 * @param fd file descriptor to read.
 * @param dummy client data passed to registration function (always NULL).
 * @brief handle read event on file descriptor.
 *
 * This read file descriptor handler is normally used for (pseudo-)device drivers that generate
 * statistical collection interval events, alarm events, or other operational measurement events, by
 * placing a message on the read queue of the "event handling" Stream.  Normally this routine
 * would adjust counts in some table or scalars, generate SNMP traps representing on-occurence
 * events, first and interval events, and alarm indications.
 */
void
slMIB_fd_handler(int fd, void *dummy)
{
	DEBUGMSGTL(("slMIB", "slMIB_fd_handler: executing fd handler...  "));
	/* XXX: place actions to handle my_fd here... */
	DEBUGMSGTL(("slMIB", "done.\n"));
	return;
}
