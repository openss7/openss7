/*****************************************************************************

 @(#) File: src/snmp/metricMIB.c

 -----------------------------------------------------------------------------

 Copyright (c) 2008-2015  Monavacon Limited <http://www.monavacon.com/>
 Copyright (c) 2001-2008  OpenSS7 Corporation <http://www.openss7.com/>
 Copyright (c) 1997-2001  Brian F. G. Bidulock <bidulock@openss7.org>

 All Rights Reserved.

 This program is free software: you can redistribute it and/or modify it under
 the terms of the GNU Affero General Public License as published by the Free
 Software Foundation, version 3 of the license.

 This program is distributed in the hope that it will be useful, but WITHOUT
 ANY WARRANTY; without even the implied warranty of MERCHANTABILITY or FITNESS
 FOR A PARTICULAR PURPOSE.  See the GNU Affero General Public License for more
 details.

 You should have received a copy of the GNU Affero General Public License
 along with this program.  If not, see <http://www.gnu.org/licenses/>, or
 write to the Free Software Foundation, Inc., 675 Mass Ave, Cambridge, MA
 02139, USA.

 -----------------------------------------------------------------------------

 U.S. GOVERNMENT RESTRICTED RIGHTS.  If you are licensing this Software on
 behalf of the U.S. Government ("Government"), the following provisions apply
 to you.  If the Software is supplied by the Department of Defense ("DoD"), it
 is classified as "Commercial Computer Software" under paragraph 252.227-7014
 of the DoD Supplement to the Federal Acquisition Regulations ("DFARS") (or any
 successor regulations) and the Government is acquiring only the license rights
 granted herein (the license rights customarily provided to non-Government
 users).  If the Software is supplied to any unit or agency of the Government
 other than DoD, it is classified as "Restricted Computer Software" and the
 Government's rights in the Software are defined in paragraph 52.227-19 of the
 Federal Acquisition Regulations ("FAR") (or any successor regulations) or, in
 the cases of NASA, in paragraph 18.52.227-86 of the NASA Supplement to the FAR
 (or any successor regulations).

 -----------------------------------------------------------------------------

 Commercial licensing and support of this software is available from OpenSS7
 Corporation at a fee.  See http://www.openss7.com/

 *****************************************************************************/

static char const ident[] = "File: " __FILE__ "  Version: " PACKAGE_ENVR "  Date: " PACKAGE_DATE;

/* This file was generated by mib2c and is intended for use as
   a mib module for the ucd-snmp snmpd agent. */
#include <ucd-snmp/ucd-snmp-config.h>
#include <ucd-snmp/ucd-snmp-includes.h>
#include <ucd-snmp/ucd-snmp-agent-includes.h>
#include <ucd-snmp/agent_trap.h>
#include <ucd-snmp/callback.h>
#include <ucd-snmp/snmp-tc.h>
#include <ucd-snmp/default_store.h>
#include <ucd-snmp/snmp_alarm.h>
/* The following header files are mangled in most recent net-snmp releases so
 * the versions from UCD-SNMP 4.2.5 are included here.  */
#if defined HAVE_LIBNETSNMP
#else				/* defined HAVE_LIBNETSNMP */
#endif				/* defined HAVE_LIBNETSNMP */
/* These are messed up on both. */
#include "ds_agent.h"
#ifdef HAVE_UCD_SNMP_UTIL_FUNCS_H
#include <ucd-snmp/util_funcs.h>
/* Many recent net-snmp UCD compatible headers do not declare header_generic. */
int header_generic(struct variable *, oid *, size_t *, int, size_t *, WriteMethod **);
#else				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#include "util_funcs.h"
#endif				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#ifdef HAVE_UCD_SNMP_HEADER_COMPLEX_H
#include <ucd-snmp/header_complex.h>
#else				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
#include "header_complex.h"
#endif				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
/* This one is the other way around: it is just fine for net-snmp, but
 * ucd-snmp does not provide the header file at all.  */
#ifdef HAVE_UCD_SNMP_MIB_MODULES_H
#include <ucd-snmp/mib_modules.h>
#else				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#ifdef HAVE_NET_SNMP_AGENT_MIB_MODULES_H
#include <net-snmp/agent/mib_modules.h>
#else				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#include "mib_modules.h"
#endif				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#endif				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#include <stdint.h>
#include <signal.h>
#include <sys/stat.h>		/* for struct stat, fstat() */
#include <sys/types.h>
#include <unistd.h>
#include <stdlib.h>
#include <pwd.h>		/* for getpwuid() getpwnam() */
#include <grp.h>		/* for getgrgid() getgrnam() */
#include <libgen.h>		/* for basename() */
#include <fcntl.h>		/* for O_CREAT */
#include <sys/sysctl.h>		/* for sysctl */
#include <errno.h>
#include <string.h>
#ifdef _GNU_SOURCE
#include <getopt.h>
#endif
#include "metricMIB.h"
#define MASTER 1
#define MY_FACILITY(__pri)	(LOG_DAEMON|(__pri))
#if defined MODULE
#if defined MASTER
const char sa_program[] = "metricMIB";
int sa_fclose = 1;			/* default close files between requests */
int sa_changed = 1;			/* indication to reread MIB configuration */
int sa_stats_refresh = 1;		/* indications that statistics, the mib or its tables need to be refreshed */
int sa_request = 1;			/* request number for per-request actions */
int sa_dump = 0;			/* default packet dump */
int sa_debug = 0;			/* default no debug */
#endif				/* defined MASTER */
#endif				/* defined MODULE */
static int my_fd = -1;			/* file descriptor for this MIB's use */
static int my_readfd = -1;		/* file descriptor for autonomnous events */
volatile int metricMIB_refresh = 1;
volatile int scannerTable_refresh = 1;
volatile int monitorMetricTable_refresh = 1;
volatile int meanMonitorTable_refresh = 1;
volatile int algorithmIndicatingMeanMonitorTable_refresh = 1;
volatile int movingAverageMeanMonitorTable_refresh = 1;
volatile int meanAndPercentileMonitorTable_refresh = 1;
volatile int meanAndVarianceMonitorTable_refresh = 1;
volatile int meanAndMinMaxMonitorTable_refresh = 1;

/*
 * metricMIB_variables_oid: object identifier for metricMIB
 * This is the top level oid that we want to register under.  This is essentially a prefix, with the
 * suffix appearing in the variable below.
 */
oid metricMIB_variables_oid[10] = { 1, 3, 6, 1, 4, 1, 29591, 9, 2, 11 };
oid scannerTable_variables_oid[14] = { 1, 3, 6, 1, 4, 1, 29591, 9, 2, 11, 3, 1, 1, 1 };
oid monitorMetricTable_variables_oid[14] = { 1, 3, 6, 1, 4, 1, 29591, 9, 2, 11, 3, 2, 1, 1 };
oid meanMonitorTable_variables_oid[14] = { 1, 3, 6, 1, 4, 1, 29591, 9, 2, 11, 3, 3, 1, 1 };
oid movingAverageMeanMonitorTable_variables_oid[14] = { 1, 3, 6, 1, 4, 1, 29591, 9, 2, 11, 3, 5, 1, 1 };
oid meanAndPercentileMonitorTable_variables_oid[14] = { 1, 3, 6, 1, 4, 1, 29591, 9, 2, 11, 3, 6, 1, 1 };
oid meanAndVarianceMonitorTable_variables_oid[14] = { 1, 3, 6, 1, 4, 1, 29591, 9, 2, 11, 3, 7, 1, 1 };
oid meanAndMinMaxMonitorTable_variables_oid[14] = { 1, 3, 6, 1, 4, 1, 29591, 9, 2, 11, 3, 8, 1, 1 };

/*
 * Oids for use in notifications defined in this MIB.
 */

/*
 * Oids accessible only for notify defined in this MIB.
 */

/*
 * Other oids defined in this MIB.
 */
oid metricObjectGroup_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 2, 11, 5, 1 };
oid metricGeneralCompliance_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 9, 2, 11, 6, 1 };
static oid zeroDotZero_oid[2] = { 0, 0 };
static oid snmpTrapOID_oid[11] = { 1, 3, 6, 1, 6, 3, 1, 1, 4, 1, 0 };

/*
 * variable7 metricMIB_variables: tree for metricMIB
 * This variable defines function callbacks and type return information for the metricMIB mib section
 */
struct variable7 metricMIB_variables[] = {
	/* magic number, variable type, ro/rw, callback fn, L, oidsuffix */
#define   SCANNERADMINISTRATIVESTATE  1
	{(u_char) SCANNERADMINISTRATIVESTATE, ASN_INTEGER, RWRITE, var_scannerTable, 5, {3, 1, 1, 1, 2}},
#define   GRANULARITYPERIOD     2
	{(u_char) GRANULARITYPERIOD, ASN_TIMETICKS, RWRITE, var_scannerTable, 5, {3, 1, 1, 1, 3}},
#define   SCANNEROPERATIONALSTATE  3
	{(u_char) SCANNEROPERATIONALSTATE, ASN_INTEGER, RONLY, var_scannerTable, 5, {3, 1, 1, 1, 4}},
#define   SCANNERAVAIABILITYSTATUS  4
	{(u_char) SCANNERAVAIABILITYSTATUS, ASN_OCTET_STR, RONLY, var_scannerTable, 5, {3, 1, 1, 1, 5}},
#define   SCANNERSTARTTIME      5
	{(u_char) SCANNERSTARTTIME, ASN_OCTET_STR, RWRITE, var_scannerTable, 5, {3, 1, 1, 1, 6}},
#define   SCANNERSTOPTIME       6
	{(u_char) SCANNERSTOPTIME, ASN_OCTET_STR, RWRITE, var_scannerTable, 5, {3, 1, 1, 1, 7}},
#define   SCANNERINTERVALSOFDAY  7
	{(u_char) SCANNERINTERVALSOFDAY, ASN_OCTET_STR, RWRITE, var_scannerTable, 5, {3, 1, 1, 1, 8}},
#define   SCANNERWEEKMASK       8
	{(u_char) SCANNERWEEKMASK, ASN_OCTET_STR, RWRITE, var_scannerTable, 5, {3, 1, 1, 1, 9}},
#define   SCANNERSCHEDULERNAME  9
	{(u_char) SCANNERSCHEDULERNAME, ASN_OBJECT_ID, RWRITE, var_scannerTable, 5, {3, 1, 1, 1, 10}},
#define   SCANNERPERIODSYNCHRONIZATIONTIME  10
	{(u_char) SCANNERPERIODSYNCHRONIZATIONTIME, ASN_OCTET_STR, RWRITE, var_scannerTable, 5, {3, 1, 1, 1, 12}},
#define   SCANNERCREATDELETENOTIFICATIONS  11
	{(u_char) SCANNERCREATDELETENOTIFICATIONS, ASN_INTEGER, RWRITE, var_scannerTable, 5, {3, 1, 1, 1, 13}},
#define   SCANNERATTRIBUTEVALUECHANGENOTIFICATIONS  12
	{(u_char) SCANNERATTRIBUTEVALUECHANGENOTIFICATIONS, ASN_INTEGER, RWRITE, var_scannerTable, 5, {3, 1, 1, 1, 14}},
#define   SCANNERSTATECHANGENOTIFICATIONS  13
	{(u_char) SCANNERSTATECHANGENOTIFICATIONS, ASN_INTEGER, RWRITE, var_scannerTable, 5, {3, 1, 1, 1, 15}},
#define   SCANNERROWSTATUS      14
	{(u_char) SCANNERROWSTATUS, ASN_INTEGER, RWRITE, var_scannerTable, 5, {3, 1, 1, 1, 16}},
#define   OBSERVEDOBJECTINSTANCE  15
	{(u_char) OBSERVEDOBJECTINSTANCE, ASN_OBJECT_ID, RWRITE, var_monitorMetricTable, 5, {3, 2, 1, 1, 1}},
#define   OBSERVEDATTRIBUTEID   16
	{(u_char) OBSERVEDATTRIBUTEID, ASN_OBJECT_ID, RWRITE, var_monitorMetricTable, 5, {3, 2, 1, 1, 2}},
#define   DERIVEDGAUGE          17
	{(u_char) DERIVEDGAUGE, ASN_GAUGE, RWRITE, var_monitorMetricTable, 5, {3, 2, 1, 1, 3}},
#define   COUNTERDIFFERENCEPACKAGE  18
	{(u_char) COUNTERDIFFERENCEPACKAGE, ASN_INTEGER, RWRITE, var_monitorMetricTable, 5, {3, 2, 1, 1, 4}},
#define   PREVIOUSSCANCOUNTERVALUE  19
	{(u_char) PREVIOUSSCANCOUNTERVALUE, ASN_COUNTER, RONLY, var_monitorMetricTable, 5, {3, 2, 1, 1, 5}},
#define   COUNTERDIFFERENCEPROCEDURALSTATUS  20
	{(u_char) COUNTERDIFFERENCEPROCEDURALSTATUS, ASN_OCTET_STR, RWRITE, var_monitorMetricTable, 5, {3, 2, 1, 1, 6}},
#define   COUNTEROVERFLOWPACKAGE  21
	{(u_char) COUNTEROVERFLOWPACKAGE, ASN_INTEGER, RWRITE, var_monitorMetricTable, 5, {3, 2, 1, 1, 7}},
#define   MODULUSVALUE          22
	{(u_char) MODULUSVALUE, ASN_COUNTER, RONLY, var_monitorMetricTable, 5, {3, 2, 1, 1, 8}},
#define   GAUGEDIFFERENCEPACKAGE  23
	{(u_char) GAUGEDIFFERENCEPACKAGE, ASN_INTEGER, RWRITE, var_monitorMetricTable, 5, {3, 2, 1, 1, 9}},
#define   PREVIOUSSCANGAUGEVALUE  24
	{(u_char) PREVIOUSSCANGAUGEVALUE, ASN_GAUGE, RWRITE, var_monitorMetricTable, 5, {3, 2, 1, 1, 10}},
#define   GAUGEDIRFERENCEPROCEDURALSTATUS  25
	{(u_char) GAUGEDIRFERENCEPROCEDURALSTATUS, ASN_OCTET_STR, RWRITE, var_monitorMetricTable, 5, {3, 2, 1, 1, 11}},
#define   METRICLOWTHRESHOLD    26
	{(u_char) METRICLOWTHRESHOLD, ASN_INTEGER, RWRITE, var_monitorMetricTable, 5, {3, 2, 1, 1, 12}},
#define   METRICLOWNOTIFY       27
	{(u_char) METRICLOWNOTIFY, ASN_INTEGER, RWRITE, var_monitorMetricTable, 5, {3, 2, 1, 1, 13}},
#define   METRICLOWSEVERITYINDICATION  28
	{(u_char) METRICLOWSEVERITYINDICATION, ASN_INTEGER, RWRITE, var_monitorMetricTable, 5, {3, 2, 1, 1, 14}},
#define   METRICHIGHTHRESHOLD   29
	{(u_char) METRICHIGHTHRESHOLD, ASN_INTEGER, RWRITE, var_monitorMetricTable, 5, {3, 2, 1, 1, 15}},
#define   METRICHIGHNOTIFY      30
	{(u_char) METRICHIGHNOTIFY, ASN_INTEGER, RWRITE, var_monitorMetricTable, 5, {3, 2, 1, 1, 16}},
#define   METRICHIGHSEVERITYINDICATION  31
	{(u_char) METRICHIGHSEVERITYINDICATION, ASN_INTEGER, RWRITE, var_monitorMetricTable, 5, {3, 2, 1, 1, 17}},
#define   METRICSPECIFICPROBLEMSINDICATOR  32
	{(u_char) METRICSPECIFICPROBLEMSINDICATOR, ASN_OBJECT_ID, RWRITE, var_monitorMetricTable, 5, {3, 2, 1, 1, 18}},
#define   DERIVEDGAUGETIMESTAMP  33
	{(u_char) DERIVEDGAUGETIMESTAMP, ASN_OCTET_STR, RWRITE, var_monitorMetricTable, 5, {3, 2, 1, 1, 19}},
#define   MONITORMETRICROWSTATUS  34
	{(u_char) MONITORMETRICROWSTATUS, ASN_INTEGER, RWRITE, var_monitorMetricTable, 5, {3, 2, 1, 1, 20}},
#define   ESTIMATEOFMEAN        35
	{(u_char) ESTIMATEOFMEAN, ASN_GAUGE, RONLY, var_meanMonitorTable, 5, {3, 3, 1, 1, 1}},
#define   MOVINGTIMEPERIOD      36
	{(u_char) MOVINGTIMEPERIOD, ASN_TIMETICKS, RWRITE, var_meanMonitorTable, 5, {3, 3, 1, 1, 2}},
#define   ESTIMATEOFMEANLOWTHRESHOLD  37
	{(u_char) ESTIMATEOFMEANLOWTHRESHOLD, ASN_INTEGER, RWRITE, var_meanMonitorTable, 5, {3, 3, 1, 1, 3}},
#define   ESTIMATEOFMEANLOWNOTIFY  38
	{(u_char) ESTIMATEOFMEANLOWNOTIFY, ASN_INTEGER, RWRITE, var_meanMonitorTable, 5, {3, 3, 1, 1, 4}},
#define   ESTIMATEOFMEANLOWSEVERITYINDICATION  39
	{(u_char) ESTIMATEOFMEANLOWSEVERITYINDICATION, ASN_INTEGER, RWRITE, var_meanMonitorTable, 5, {3, 3, 1, 1, 5}},
#define   ESTIMATEOFMEANHIGHTHRESHOLD  40
	{(u_char) ESTIMATEOFMEANHIGHTHRESHOLD, ASN_INTEGER, RWRITE, var_meanMonitorTable, 5, {3, 3, 1, 1, 6}},
#define   ESTIMATEOFMEANHIGHNOTIFY  41
	{(u_char) ESTIMATEOFMEANHIGHNOTIFY, ASN_INTEGER, RWRITE, var_meanMonitorTable, 5, {3, 3, 1, 1, 7}},
#define   ESTIMATEOFMEANHIGHSEVERITYINDICATION  42
	{(u_char) ESTIMATEOFMEANHIGHSEVERITYINDICATION, ASN_INTEGER, RWRITE, var_meanMonitorTable, 5, {3, 3, 1, 1, 8}},
#define   ESTIMATEOFMEANSPECIFICPROBLEMSINDICATOR  43
	{(u_char) ESTIMATEOFMEANSPECIFICPROBLEMSINDICATOR, ASN_OBJECT_ID, RWRITE, var_meanMonitorTable, 5, {3, 3, 1, 1, 9}},
#define   MEANMONITORROWSTATUS  44
	{(u_char) MEANMONITORROWSTATUS, ASN_INTEGER, RWRITE, var_meanMonitorTable, 5, {3, 3, 1, 1, 10}},
#define   ALGORITHMIDENTIFIER   45
	{(u_char) ALGORITHMIDENTIFIER, ASN_OBJECT_ID, RONLY, var_algorithmIndicatingMeanMonitorTable, 5, {3, 4, 1, 1, 1}},
#define   MOVINGAVERAGEMEANMONITORROWSTATUS  46
	{(u_char) MOVINGAVERAGEMEANMONITORROWSTATUS, ASN_INTEGER, RWRITE, var_movingAverageMeanMonitorTable, 5, {3, 5, 1, 1, 1}},
#define   PERCENTILESECONDMOVINGTIMEPERIOD  47
	{(u_char) PERCENTILESECONDMOVINGTIMEPERIOD, ASN_TIMETICKS, RWRITE, var_meanAndPercentileMonitorTable, 5, {3, 6, 1, 1, 1}},
#define   ESTIMATEOFLARGESTINREPLICATION  48
	{(u_char) ESTIMATEOFLARGESTINREPLICATION, ASN_GAUGE, RONLY, var_meanAndPercentileMonitorTable, 5, {3, 6, 1, 1, 2}},
#define   ESTIMATEOFSMALLESTINREPLICATION  49
	{(u_char) ESTIMATEOFSMALLESTINREPLICATION, ASN_GAUGE, RONLY, var_meanAndPercentileMonitorTable, 5, {3, 6, 1, 1, 3}},
#define   ESTIMATEOFMEDIAN      50
	{(u_char) ESTIMATEOFMEDIAN, ASN_GAUGE, RONLY, var_meanAndPercentileMonitorTable, 5, {3, 6, 1, 1, 4}},
#define   ESTIMATEOF100PCTPERCENTILE  51
	{(u_char) ESTIMATEOF100PCTPERCENTILE, ASN_GAUGE, RONLY, var_meanAndPercentileMonitorTable, 5, {3, 6, 1, 1, 5}},
#define   ESTIMATEOFPCTPERCENTILE  52
	{(u_char) ESTIMATEOFPCTPERCENTILE, ASN_GAUGE, RONLY, var_meanAndPercentileMonitorTable, 5, {3, 6, 1, 1, 6}},
#define   NUMBEROFREPLICATIONS  53
	{(u_char) NUMBEROFREPLICATIONS, ASN_INTEGER, RWRITE, var_meanAndPercentileMonitorTable, 5, {3, 6, 1, 1, 7}},
#define   CONFIGURABLEPERCENTILEPACKAGE  54
	{(u_char) CONFIGURABLEPERCENTILEPACKAGE, ASN_INTEGER, RWRITE, var_meanAndPercentileMonitorTable, 5, {3, 6, 1, 1, 8}},
#define   MEANANDPERCENTILEMONITORROWSTATUS  55
	{(u_char) MEANANDPERCENTILEMONITORROWSTATUS, ASN_INTEGER, RWRITE, var_meanAndPercentileMonitorTable, 5, {3, 6, 1, 1, 9}},
#define   VARIANCESECONDMOVINGTIMEPERIOD  56
	{(u_char) VARIANCESECONDMOVINGTIMEPERIOD, ASN_TIMETICKS, RWRITE, var_meanAndVarianceMonitorTable, 5, {3, 7, 1, 1, 1}},
#define   ESTIMATEOFVARIANCE    57
	{(u_char) ESTIMATEOFVARIANCE, ASN_GAUGE, RONLY, var_meanAndVarianceMonitorTable, 5, {3, 7, 1, 1, 2}},
#define   MEANANDVARIANCEMONITORROWSTATUS  58
	{(u_char) MEANANDVARIANCEMONITORROWSTATUS, ASN_INTEGER, RWRITE, var_meanAndVarianceMonitorTable, 5, {3, 7, 1, 1, 3}},
#define   ESTIMATEOFLARGEST     59
	{(u_char) ESTIMATEOFLARGEST, ASN_GAUGE, RONLY, var_meanAndMinMaxMonitorTable, 5, {3, 8, 1, 1, 1}},
#define   ESTIMATEOFSMALLEST    60
	{(u_char) ESTIMATEOFSMALLEST, ASN_GAUGE, RONLY, var_meanAndMinMaxMonitorTable, 5, {3, 8, 1, 1, 2}},
#define   MEANANDMINMAXMONITORROWSTATUS  61
	{(u_char) MEANANDMINMAXMONITORROWSTATUS, ASN_INTEGER, RWRITE, var_meanAndMinMaxMonitorTable, 5, {3, 8, 1, 1, 3}},
};

/* (L = length of the oidsuffix) */
struct metricMIB_data *metricMIBStorage = NULL;

/* global storage of our data, saved in and configured by header_complex() */
struct header_complex_index *scannerTableStorage = NULL;
struct header_complex_index *monitorMetricTableStorage = NULL;
struct header_complex_index *meanMonitorTableStorage = NULL;
struct header_complex_index *algorithmIndicatingMeanMonitorTableStorage = NULL;
struct header_complex_index *movingAverageMeanMonitorTableStorage = NULL;
struct header_complex_index *meanAndPercentileMonitorTableStorage = NULL;
struct header_complex_index *meanAndVarianceMonitorTableStorage = NULL;
struct header_complex_index *meanAndMinMaxMonitorTableStorage = NULL;

void (*metricMIBold_signal_handler) (int) = NULL;	/* save old signal handler just in case */
void metricMIB_loop_handler(int);
void metricMIB_fd_handler(int, void *);

/**
 * @fn void init_metricMIB(void)
 * @brief metricMIB initialization routine.
 *
 * This is called when the agent starts up.  At a minimum, registration of the MIB variables
 * structure (metricMIB_variables) should take place here.  By default the function also
 * registers the configuration handler and configuration store callbacks.
 *
 * Additional registrations that may be considered here are calls to regsiter_readfd(),
 * register_writefd() and register_exceptfd() for hooking into the snmpd event loop, but only when
 * used as a loadable module.  By default this function establishes a single file descriptor to
 * read, or upon which to handle exceptions.  Note that the snmpd only supports a maximum of 32
 * extneral file descriptors, so these should be used sparingly.
 *
 * When running as a loadable module, it is also necessary to hook into the snmpd event loop so that
 * the current request number can be deteremined.  This is accomplished by using a trick of the
 * external_signal_scheduled and external_signal_handler mechanism which is called on each event
 * loop when external_signal_scheduled is non-zero.  This is used to increment the sa_request value
 * on each snmpd event loop interation so that calls to MIB tree functions can determine whether
 * they belong to a fresh request or not (primarily for cacheing and possibly to clean up non-polled
 * file descriptors).
 */
void
init_metricMIB(void)
{
	(void) my_fd;
	(void) zeroDotZero_oid;
	(void) snmpTrapOID_oid;
	DEBUGMSGTL(("metricMIB", "init_metricMIB: initializing...  "));
	/* register ourselves with the agent to handle our mib tree */
	REGISTER_MIB("metricMIB", metricMIB_variables, variable7, metricMIB_variables_oid);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_SHUTDOWN, term_metricMIB, NULL);
	/* register our config handler(s) to deal with registrations */
	snmpd_register_config_handler("metricMIB", parse_metricMIB, NULL, "HELP STRING");
	snmpd_register_config_handler("scannerTable", parse_scannerTable, NULL, "HELP STRING");
	snmpd_register_config_handler("monitorMetricTable", parse_monitorMetricTable, NULL, "HELP STRING");
	snmpd_register_config_handler("meanMonitorTable", parse_meanMonitorTable, NULL, "HELP STRING");
	snmpd_register_config_handler("algorithmIndicatingMeanMonitorTable", parse_algorithmIndicatingMeanMonitorTable, NULL, "HELP STRING");
	snmpd_register_config_handler("movingAverageMeanMonitorTable", parse_movingAverageMeanMonitorTable, NULL, "HELP STRING");
	snmpd_register_config_handler("meanAndPercentileMonitorTable", parse_meanAndPercentileMonitorTable, NULL, "HELP STRING");
	snmpd_register_config_handler("meanAndVarianceMonitorTable", parse_meanAndVarianceMonitorTable, NULL, "HELP STRING");
	snmpd_register_config_handler("meanAndMinMaxMonitorTable", parse_meanAndMinMaxMonitorTable, NULL, "HELP STRING");

	/* we need to be called back later to store our data */
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_metricMIB, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_scannerTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_monitorMetricTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_meanMonitorTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_algorithmIndicatingMeanMonitorTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_movingAverageMeanMonitorTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_meanAndPercentileMonitorTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_meanAndVarianceMonitorTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_meanAndMinMaxMonitorTable, NULL);

	/* place any other initialization junk you need here */
	if (my_readfd >= 0) {
		register_readfd(my_readfd, metricMIB_fd_handler, (void *) 0);
		register_exceptfd(my_readfd, metricMIB_fd_handler, (void *) 1);
	}
	metricMIBold_signal_handler = external_signal_handler[SIGCHLD];
	external_signal_handler[SIGCHLD] = &metricMIB_loop_handler;
	external_signal_scheduled[SIGCHLD] = 1;
	DEBUGMSGTL(("metricMIB", "done.\n"));
}

/**
 * @fn void deinit_metricMIB(void)
 * @brief deinitialization routine.
 *
 * This is called before the agent is unloaded.  At a minimum, deregistration of the MIB variables
 * structure (metricMIB_variables) should take place here.  By default, the function also
 * deregisters the the configuration file handlers for the MIB variables and table rows.
 *
 * Additional deregistrations that may be required here are calls to unregister_readfd(),
 * unregister_writefd() and unregsiter_exceptfd() for unhooking from the snmpd event loop, but only
 * when used as a loadable module.  By default if a read file descriptor exists, it is unregistered.
 */
void
deinit_metricMIB(void)
{
	DEBUGMSGTL(("metricMIB", "deinit_metricMIB: deinitializating...  "));
	external_signal_handler[SIGCHLD] = metricMIBold_signal_handler;
	if (my_readfd >= 0) {
		unregister_exceptfd(my_readfd);
		unregister_readfd(my_readfd);
		close(my_readfd);
		my_readfd = -1;
	}
	unregister_mib(metricMIB_variables_oid, sizeof(metricMIB_variables_oid) / sizeof(oid));
	snmpd_unregister_config_handler("metricMIB");
	snmpd_unregister_config_handler("scannerTable");
	snmpd_unregister_config_handler("monitorMetricTable");
	snmpd_unregister_config_handler("meanMonitorTable");
	snmpd_unregister_config_handler("algorithmIndicatingMeanMonitorTable");
	snmpd_unregister_config_handler("movingAverageMeanMonitorTable");
	snmpd_unregister_config_handler("meanAndPercentileMonitorTable");
	snmpd_unregister_config_handler("meanAndVarianceMonitorTable");
	snmpd_unregister_config_handler("meanAndMinMaxMonitorTable");

	/* place any other de-initialization junk you need here */
	DEBUGMSGTL(("metricMIB", "done.\n"));
}

int
term_metricMIB(int majorID, int minorID, void *serverarg, void *clientarg)
{
	DEBUGMSGTL(("metricMIB", "term_metricMIB: terminating...  "));
	deinit_metricMIB();
	DEBUGMSGTL(("metricMIB", "done.\n"));
	return 0;
}

/**
 * @fn struct metricMIB_data *metricMIB_create(void)
 * @brief create a fresh data structure representing scalars in metricMIB.
 *
 * Creates a new metricMIB_data structure by allocating dynamic memory for the structure and
 * initializing the default values of scalars in metricMIB.
 */
struct metricMIB_data *
metricMIB_create(void)
{
	struct metricMIB_data *StorageNew = SNMP_MALLOC_STRUCT(metricMIB_data);

	DEBUGMSGTL(("metricMIB", "metricMIB_create: creating scalars...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default scalar values here into StorageNew */

	}
	DEBUGMSGTL(("metricMIB", "done.\n"));
	return (StorageNew);
}

/**
 * @fn int metricMIB_destroy(struct metricMIB_data **thedata)
 * @param thedata pointer to the data structure in metricMIB.
 * @brief delete a scalars structure from metricMIB.
 *
 * Frees scalars that were previously removed from metricMIB.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
metricMIB_destroy(struct metricMIB_data **thedata)
{
	struct metricMIB_data *StorageDel;

	DEBUGMSGTL(("metricMIB", "metricMIB_destroy: deleting scalars...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("metricMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int metricMIB_add(struct metricMIB_data *thedata)
 * @param thedata the structure representing metricMIB scalars.
 * @brief adds node to the metricMIB scalar data set.
 *
 * Adds a scalar structure to the metricMIB data set.  Note that this function is necessary even
 * when the scalar values are not peristent.
 */
int
metricMIB_add(struct metricMIB_data *thedata)
{
	DEBUGMSGTL(("metricMIB", "metricMIB_add: adding data...  "));
	if (thedata)
		metricMIBStorage = thedata;
	DEBUGMSGTL(("metricMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_metricMIB(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for metricMIB entries.
 *
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case metricMIB).  This routine is invoked by
 * UCD-SNMP to read the values of scalars in the MIB from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the MIB.  If there are no configured entries
 * in the configuration MIB, this function will simply not be called.
 */
void
parse_metricMIB(const char *token, char *line)
{
	size_t tmpsize;
	struct metricMIB_data *StorageTmp = metricMIB_create();

	DEBUGMSGTL(("metricMIB", "parse_metricMIB: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual scalars that are not persistent */
	metricMIB_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("metricMIB", "done.\n"));
}

/*
 * store_metricMIB(): stores .conf file entries needed to configure the mib.
 */
int
store_metricMIB(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct metricMIB_data *StorageTmp;

	DEBUGMSGTL(("metricMIB", "store_metricMIB: storing data...  "));
	refresh_metricMIB(1);
	if ((StorageTmp = metricMIBStorage) == NULL) {
		DEBUGMSGTL(("metricMIB", "error.\n"));
		return SNMPERR_GENERR;
	}
	(void) tmpsize;
	/* XXX: comment entire section if no scalars are persistent */
	{
		memset(line, 0, sizeof(line));
		strcat(line, "metricMIB ");
		cptr = line + strlen(line);
		(void) cptr;
		/* XXX: remove individual scalars that are not persistent */
		snmpd_store_config(line);
	}
	DEBUGMSGTL(("metricMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void refresh_metricMIB(int force)
 * @param force forced refresh when non-zero.
 * @brief refresh the scalar values of metricMIB.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a scalar has been requested).
 */
void
refresh_metricMIB(int force)
{
	if (metricMIBStorage == NULL) {
		struct metricMIB_data *StorageNew;

		if ((StorageNew = metricMIB_create()) == NULL)
			return;
		metricMIBStorage = StorageNew;
		metricMIB_refresh = 1;
	}
	if (!force && metricMIB_refresh == 0)
		return;
	DEBUGMSGTL(("metricMIB", "refresh_metricMIB: refreshing...  "));
	/* XXX: Update scalars as required here... */
	metricMIB_refresh = 0;
	DEBUGMSGTL(("metricMIB", "done.\n"));
}

/**
 * @fn u_char * var_metricMIB(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @param vp a pointer to the entry in the variables table for the requested variable.
 * @param name the object identifier for which to find.
 * @param length the length of the object identifier.
 * @param exact whether the name is exact.
 * @param var_len a pointer to the length of the representation of the object.
 * @param write_method a pointer to a write method for the object.
 * @brief locate variables in metricMIB.
 *
 * This function returns a pointer to a memory area that is static across the request that contains
 * the UCD-SNMP representation of the scalar (so that it may be used to read from for a GET,
 * GET-NEXT or GET-BULK request).  This returned pointer may be NULL, in which case the function is
 * telling UCD-SNMP that the scalar does not exist for reading; however, if write_method is
 * overwritten with a non-NULL value, the function is telling UCD-SNMP that the scalar exists for
 * writing.  Write-only objects can be effected in this way.
 */
u_char *
var_metricMIB(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct metricMIB_data *StorageTmp;
	u_char *rval;

	DEBUGMSGTL(("metricMIB", "var_metricMIB: lookup up varbind...  "));
	if (header_generic(vp, name, length, exact, var_len, write_method) == MATCH_FAILED)
		return NULL;
	/* Refresh the MIB values if required. */
	refresh_metricMIB(0);
	if ((StorageTmp = metricMIBStorage) == NULL) {
		DEBUGMSGTL(("metricMIB", "no datastructure.\n"));
		return NULL;
	}
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	default:
		ERROR_MSG("");
	}
	if (rval)
		DEBUGMSGTL(("metricMIB", "found.\n"));
	else
		DEBUGMSGTL(("metricMIB", "not found.\n"));
	return (rval);
}

/**
 * @fn struct scannerTable_data *scannerTable_create(void)
 * @brief create a fresh data structure representing a new row in the scannerTable table.
 *
 * Creates a new scannerTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct scannerTable_data *
scannerTable_create(void)
{
	struct scannerTable_data *StorageNew = SNMP_MALLOC_STRUCT(scannerTable_data);

	DEBUGMSGTL(("metricMIB", "scannerTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->scannerAdministrativeState = 0;
		StorageNew->granularityPeriod = 0;
		StorageNew->scannerOperationalState = 0;
		if (memdup((u_char **) &StorageNew->scannerAvaiabilityStatus, (u_char *) "\x00\x00", 2) == SNMPERR_SUCCESS)
			StorageNew->scannerAvaiabilityStatusLen = 2;
		if ((StorageNew->scannerStartTime = (uint8_t *) strdup("")) != NULL)
			StorageNew->scannerStartTimeLen = strlen("");
		if ((StorageNew->scannerStopTime = (uint8_t *) strdup("")) != NULL)
			StorageNew->scannerStopTimeLen = strlen("");
		if ((StorageNew->scannerIntervalsOfDay = (uint8_t *) strdup("")) != NULL)
			StorageNew->scannerIntervalsOfDayLen = strlen("");
		if ((StorageNew->scannerWeekMask = (uint8_t *) strdup("")) != NULL)
			StorageNew->scannerWeekMaskLen = strlen("");
		if ((StorageNew->scannerSchedulerName = snmp_duplicate_objid(zeroDotZero_oid, 2)))
			StorageNew->scannerSchedulerNameLen = 2;
		if ((StorageNew->scannerPeriodSynchronizationTime = (uint8_t *) strdup("")) != NULL)
			StorageNew->scannerPeriodSynchronizationTimeLen = strlen("");
		StorageNew->scannerCreatDeleteNotifications = 0;
		StorageNew->scannerAttributeValueChangeNotifications = 0;
		StorageNew->scannerStateChangeNotifications = 0;
		StorageNew->scannerRowStatus = 0;
		StorageNew->scannerRowStatus = RS_NOTREADY;
	}
	DEBUGMSGTL(("metricMIB", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct scannerTable_data *scannerTable_duplicate(struct scannerTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicat a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct scannerTable_data *
scannerTable_duplicate(struct scannerTable_data *thedata)
{
	struct scannerTable_data *StorageNew = SNMP_MALLOC_STRUCT(scannerTable_data);

	DEBUGMSGTL(("metricMIB", "scannerTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("metricMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	scannerTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int scannerTable_destroy(struct scannerTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
scannerTable_destroy(struct scannerTable_data **thedata)
{
	struct scannerTable_data *StorageDel;

	DEBUGMSGTL(("metricMIB", "scannerTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->scannerId);
		StorageDel->scannerIdLen = 0;
		SNMP_FREE(StorageDel->scannerAvaiabilityStatus);
		StorageDel->scannerAvaiabilityStatusLen = 0;
		SNMP_FREE(StorageDel->scannerStartTime);
		StorageDel->scannerStartTimeLen = 0;
		SNMP_FREE(StorageDel->scannerStopTime);
		StorageDel->scannerStopTimeLen = 0;
		SNMP_FREE(StorageDel->scannerIntervalsOfDay);
		StorageDel->scannerIntervalsOfDayLen = 0;
		SNMP_FREE(StorageDel->scannerWeekMask);
		StorageDel->scannerWeekMaskLen = 0;
		SNMP_FREE(StorageDel->scannerSchedulerName);
		StorageDel->scannerSchedulerNameLen = 0;
		SNMP_FREE(StorageDel->scannerPeriodSynchronizationTime);
		StorageDel->scannerPeriodSynchronizationTimeLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("metricMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int scannerTable_add(struct scannerTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the scannerTable table data set.
 *
 * Adds a table row structure to the scannerTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
scannerTable_add(struct scannerTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("metricMIB", "scannerTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* scannerId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->scannerId, thedata->scannerIdLen);
		header_complex_add_data(&scannerTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("metricMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int scannerTable_del(struct scannerTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the scannerTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
scannerTable_del(struct scannerTable_data *thedata)
{
	struct scannerTable_data *StorageDel;

	DEBUGMSGTL(("metricMIB", "scannerTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(scannerTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&scannerTableStorage, hciptr);
	}
	DEBUGMSGTL(("metricMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_scannerTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for scannerTable entries.
 *
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case scannerTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_scannerTable(const char *token, char *line)
{
	size_t tmpsize;
	struct scannerTable_data *StorageTmp = scannerTable_create();

	DEBUGMSGTL(("metricMIB", "parse_scannerTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->scannerId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->scannerId, &StorageTmp->scannerIdLen);
	if (StorageTmp->scannerId == NULL) {
		config_perror("invalid specification for scannerId");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->scannerAdministrativeState, &tmpsize);
	line = read_config_read_data(ASN_TIMETICKS, line, &StorageTmp->granularityPeriod, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->scannerOperationalState, &tmpsize);
	SNMP_FREE(StorageTmp->scannerAvaiabilityStatus);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->scannerAvaiabilityStatus, &StorageTmp->scannerAvaiabilityStatusLen);
	if (StorageTmp->scannerAvaiabilityStatus == NULL) {
		config_perror("invalid specification for scannerAvaiabilityStatus");
		return;
	}
	SNMP_FREE(StorageTmp->scannerStartTime);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->scannerStartTime, &StorageTmp->scannerStartTimeLen);
	if (StorageTmp->scannerStartTime == NULL) {
		config_perror("invalid specification for scannerStartTime");
		return;
	}
	SNMP_FREE(StorageTmp->scannerStopTime);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->scannerStopTime, &StorageTmp->scannerStopTimeLen);
	if (StorageTmp->scannerStopTime == NULL) {
		config_perror("invalid specification for scannerStopTime");
		return;
	}
	SNMP_FREE(StorageTmp->scannerIntervalsOfDay);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->scannerIntervalsOfDay, &StorageTmp->scannerIntervalsOfDayLen);
	if (StorageTmp->scannerIntervalsOfDay == NULL) {
		config_perror("invalid specification for scannerIntervalsOfDay");
		return;
	}
	SNMP_FREE(StorageTmp->scannerWeekMask);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->scannerWeekMask, &StorageTmp->scannerWeekMaskLen);
	if (StorageTmp->scannerWeekMask == NULL) {
		config_perror("invalid specification for scannerWeekMask");
		return;
	}
	SNMP_FREE(StorageTmp->scannerSchedulerName);
	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->scannerSchedulerName, &StorageTmp->scannerSchedulerNameLen);
	if (StorageTmp->scannerSchedulerName == NULL) {
		config_perror("invalid specification for scannerSchedulerName");
		return;
	}
	SNMP_FREE(StorageTmp->scannerPeriodSynchronizationTime);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->scannerPeriodSynchronizationTime, &StorageTmp->scannerPeriodSynchronizationTimeLen);
	if (StorageTmp->scannerPeriodSynchronizationTime == NULL) {
		config_perror("invalid specification for scannerPeriodSynchronizationTime");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->scannerCreatDeleteNotifications, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->scannerAttributeValueChangeNotifications, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->scannerStateChangeNotifications, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->scannerRowStatus, &tmpsize);
	scannerTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("metricMIB", "done.\n"));
}

/*
 * store_scannerTable(): store configuraiton file for scannerTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_scannerTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct scannerTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("metricMIB", "store_scannerTable: storing data...  "));
	refresh_scannerTable(1);
	(void) tmpsize;
	for (hcindex = scannerTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct scannerTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "scannerTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->scannerId, &StorageTmp->scannerIdLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->scannerAdministrativeState, &tmpsize);
			cptr = read_config_store_data(ASN_TIMETICKS, cptr, &StorageTmp->granularityPeriod, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->scannerOperationalState, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->scannerAvaiabilityStatus, &StorageTmp->scannerAvaiabilityStatusLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->scannerStartTime, &StorageTmp->scannerStartTimeLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->scannerStopTime, &StorageTmp->scannerStopTimeLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->scannerIntervalsOfDay, &StorageTmp->scannerIntervalsOfDayLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->scannerWeekMask, &StorageTmp->scannerWeekMaskLen);
			cptr = read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->scannerSchedulerName, &StorageTmp->scannerSchedulerNameLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->scannerPeriodSynchronizationTime, &StorageTmp->scannerPeriodSynchronizationTimeLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->scannerCreatDeleteNotifications, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->scannerAttributeValueChangeNotifications, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->scannerStateChangeNotifications, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->scannerRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("metricMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct monitorMetricTable_data *monitorMetricTable_create(void)
 * @brief create a fresh data structure representing a new row in the monitorMetricTable table.
 *
 * Creates a new monitorMetricTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct monitorMetricTable_data *
monitorMetricTable_create(void)
{
	struct monitorMetricTable_data *StorageNew = SNMP_MALLOC_STRUCT(monitorMetricTable_data);

	DEBUGMSGTL(("metricMIB", "monitorMetricTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->scannerId = (uint8_t *) strdup("")) != NULL)
			StorageNew->scannerIdLen = strlen("");
		if ((StorageNew->observedObjectInstance = snmp_duplicate_objid(zeroDotZero_oid, 2)))
			StorageNew->observedObjectInstanceLen = 2;
		if ((StorageNew->observedAttributeId = snmp_duplicate_objid(zeroDotZero_oid, 2)))
			StorageNew->observedAttributeIdLen = 2;
		StorageNew->derivedGauge = 0;
		StorageNew->counterDifferencePackage = 0;
		StorageNew->previousScanCounterValue = 0;
		if (memdup((u_char **) &StorageNew->counterDifferenceProceduralStatus, (u_char *) "\x00", 1) == SNMPERR_SUCCESS)
			StorageNew->counterDifferenceProceduralStatusLen = 1;
		StorageNew->counterOverflowPackage = 0;
		StorageNew->modulusValue = 0;
		StorageNew->gaugeDifferencePackage = 0;
		StorageNew->previousScanGaugeValue = 0;
		if (memdup((u_char **) &StorageNew->gaugeDirferenceProceduralStatus, (u_char *) "\x00", 1) == SNMPERR_SUCCESS)
			StorageNew->gaugeDirferenceProceduralStatusLen = 1;
		StorageNew->metricLowThreshold = 0;
		StorageNew->metricLowNotify = 0;
		StorageNew->metricLowSeverityIndication = 0;
		StorageNew->metricHighThreshold = 0;
		StorageNew->metricHighNotify = 0;
		StorageNew->metricHighSeverityIndication = 0;
		if ((StorageNew->metricSpecificProblemsIndicator = snmp_duplicate_objid(zeroDotZero_oid, 2)))
			StorageNew->metricSpecificProblemsIndicatorLen = 2;
		if ((StorageNew->derivedGaugeTimeStamp = (uint8_t *) strdup("")) != NULL)
			StorageNew->derivedGaugeTimeStampLen = strlen("");
		StorageNew->monitorMetricRowStatus = 0;
		StorageNew->monitorMetricRowStatus = RS_NOTREADY;
	}
	DEBUGMSGTL(("metricMIB", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct monitorMetricTable_data *monitorMetricTable_duplicate(struct monitorMetricTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicat a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct monitorMetricTable_data *
monitorMetricTable_duplicate(struct monitorMetricTable_data *thedata)
{
	struct monitorMetricTable_data *StorageNew = SNMP_MALLOC_STRUCT(monitorMetricTable_data);

	DEBUGMSGTL(("metricMIB", "monitorMetricTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("metricMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	monitorMetricTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int monitorMetricTable_destroy(struct monitorMetricTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
monitorMetricTable_destroy(struct monitorMetricTable_data **thedata)
{
	struct monitorMetricTable_data *StorageDel;

	DEBUGMSGTL(("metricMIB", "monitorMetricTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->scannerId);
		StorageDel->scannerIdLen = 0;
		SNMP_FREE(StorageDel->observedObjectInstance);
		StorageDel->observedObjectInstanceLen = 0;
		SNMP_FREE(StorageDel->observedAttributeId);
		StorageDel->observedAttributeIdLen = 0;
		SNMP_FREE(StorageDel->counterDifferenceProceduralStatus);
		StorageDel->counterDifferenceProceduralStatusLen = 0;
		SNMP_FREE(StorageDel->gaugeDirferenceProceduralStatus);
		StorageDel->gaugeDirferenceProceduralStatusLen = 0;
		SNMP_FREE(StorageDel->metricSpecificProblemsIndicator);
		StorageDel->metricSpecificProblemsIndicatorLen = 0;
		SNMP_FREE(StorageDel->derivedGaugeTimeStamp);
		StorageDel->derivedGaugeTimeStampLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("metricMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int monitorMetricTable_add(struct monitorMetricTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the monitorMetricTable table data set.
 *
 * Adds a table row structure to the monitorMetricTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
monitorMetricTable_add(struct monitorMetricTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("metricMIB", "monitorMetricTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* scannerId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->scannerId, thedata->scannerIdLen);
		header_complex_add_data(&monitorMetricTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("metricMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int monitorMetricTable_del(struct monitorMetricTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the monitorMetricTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
monitorMetricTable_del(struct monitorMetricTable_data *thedata)
{
	struct monitorMetricTable_data *StorageDel;

	DEBUGMSGTL(("metricMIB", "monitorMetricTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(monitorMetricTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&monitorMetricTableStorage, hciptr);
	}
	DEBUGMSGTL(("metricMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_monitorMetricTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for monitorMetricTable entries.
 *
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case monitorMetricTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_monitorMetricTable(const char *token, char *line)
{
	size_t tmpsize;
	struct monitorMetricTable_data *StorageTmp = monitorMetricTable_create();

	DEBUGMSGTL(("metricMIB", "parse_monitorMetricTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->scannerId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->scannerId, &StorageTmp->scannerIdLen);
	if (StorageTmp->scannerId == NULL) {
		config_perror("invalid specification for scannerId");
		return;
	}
	SNMP_FREE(StorageTmp->observedObjectInstance);
	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->observedObjectInstance, &StorageTmp->observedObjectInstanceLen);
	if (StorageTmp->observedObjectInstance == NULL) {
		config_perror("invalid specification for observedObjectInstance");
		return;
	}
	SNMP_FREE(StorageTmp->observedAttributeId);
	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->observedAttributeId, &StorageTmp->observedAttributeIdLen);
	if (StorageTmp->observedAttributeId == NULL) {
		config_perror("invalid specification for observedAttributeId");
		return;
	}
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->derivedGauge, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->counterDifferencePackage, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->previousScanCounterValue, &tmpsize);
	SNMP_FREE(StorageTmp->counterDifferenceProceduralStatus);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->counterDifferenceProceduralStatus, &StorageTmp->counterDifferenceProceduralStatusLen);
	if (StorageTmp->counterDifferenceProceduralStatus == NULL) {
		config_perror("invalid specification for counterDifferenceProceduralStatus");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->counterOverflowPackage, &tmpsize);
	line = read_config_read_data(ASN_COUNTER, line, &StorageTmp->modulusValue, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->gaugeDifferencePackage, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->previousScanGaugeValue, &tmpsize);
	SNMP_FREE(StorageTmp->gaugeDirferenceProceduralStatus);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->gaugeDirferenceProceduralStatus, &StorageTmp->gaugeDirferenceProceduralStatusLen);
	if (StorageTmp->gaugeDirferenceProceduralStatus == NULL) {
		config_perror("invalid specification for gaugeDirferenceProceduralStatus");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->metricLowThreshold, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->metricLowNotify, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->metricLowSeverityIndication, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->metricHighThreshold, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->metricHighNotify, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->metricHighSeverityIndication, &tmpsize);
	SNMP_FREE(StorageTmp->metricSpecificProblemsIndicator);
	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->metricSpecificProblemsIndicator, &StorageTmp->metricSpecificProblemsIndicatorLen);
	if (StorageTmp->metricSpecificProblemsIndicator == NULL) {
		config_perror("invalid specification for metricSpecificProblemsIndicator");
		return;
	}
	SNMP_FREE(StorageTmp->derivedGaugeTimeStamp);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->derivedGaugeTimeStamp, &StorageTmp->derivedGaugeTimeStampLen);
	if (StorageTmp->derivedGaugeTimeStamp == NULL) {
		config_perror("invalid specification for derivedGaugeTimeStamp");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->monitorMetricRowStatus, &tmpsize);
	monitorMetricTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("metricMIB", "done.\n"));
}

/*
 * store_monitorMetricTable(): store configuraiton file for monitorMetricTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_monitorMetricTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct monitorMetricTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("metricMIB", "store_monitorMetricTable: storing data...  "));
	refresh_monitorMetricTable(1);
	(void) tmpsize;
	for (hcindex = monitorMetricTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct monitorMetricTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "monitorMetricTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->scannerId, &StorageTmp->scannerIdLen);
			cptr = read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->observedObjectInstance, &StorageTmp->observedObjectInstanceLen);
			cptr = read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->observedAttributeId, &StorageTmp->observedAttributeIdLen);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->derivedGauge, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->counterDifferencePackage, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->previousScanCounterValue, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->counterDifferenceProceduralStatus, &StorageTmp->counterDifferenceProceduralStatusLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->counterOverflowPackage, &tmpsize);
			cptr = read_config_store_data(ASN_COUNTER, cptr, &StorageTmp->modulusValue, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->gaugeDifferencePackage, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->previousScanGaugeValue, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->gaugeDirferenceProceduralStatus, &StorageTmp->gaugeDirferenceProceduralStatusLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->metricLowThreshold, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->metricLowNotify, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->metricLowSeverityIndication, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->metricHighThreshold, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->metricHighNotify, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->metricHighSeverityIndication, &tmpsize);
			cptr = read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->metricSpecificProblemsIndicator, &StorageTmp->metricSpecificProblemsIndicatorLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->derivedGaugeTimeStamp, &StorageTmp->derivedGaugeTimeStampLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->monitorMetricRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("metricMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct meanMonitorTable_data *meanMonitorTable_create(void)
 * @brief create a fresh data structure representing a new row in the meanMonitorTable table.
 *
 * Creates a new meanMonitorTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct meanMonitorTable_data *
meanMonitorTable_create(void)
{
	struct meanMonitorTable_data *StorageNew = SNMP_MALLOC_STRUCT(meanMonitorTable_data);

	DEBUGMSGTL(("metricMIB", "meanMonitorTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->scannerId = (uint8_t *) strdup("")) != NULL)
			StorageNew->scannerIdLen = strlen("");
		StorageNew->estimateOfMean = 0;
		StorageNew->movingTimePeriod = 0;
		StorageNew->estimateOfMeanLowThreshold = 0;
		StorageNew->estimateOfMeanLowNotify = 0;
		StorageNew->estimateOfMeanLowSeverityIndication = 0;
		StorageNew->estimateOfMeanHighThreshold = 0;
		StorageNew->estimateOfMeanHighNotify = 0;
		StorageNew->estimateOfMeanHighSeverityIndication = 0;
		if ((StorageNew->estimateOfMeanSpecificProblemsIndicator = snmp_duplicate_objid(zeroDotZero_oid, 2)))
			StorageNew->estimateOfMeanSpecificProblemsIndicatorLen = 2;
		StorageNew->meanMonitorRowStatus = 0;
		StorageNew->meanMonitorRowStatus = RS_NOTREADY;
	}
	DEBUGMSGTL(("metricMIB", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct meanMonitorTable_data *meanMonitorTable_duplicate(struct meanMonitorTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicat a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct meanMonitorTable_data *
meanMonitorTable_duplicate(struct meanMonitorTable_data *thedata)
{
	struct meanMonitorTable_data *StorageNew = SNMP_MALLOC_STRUCT(meanMonitorTable_data);

	DEBUGMSGTL(("metricMIB", "meanMonitorTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("metricMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	meanMonitorTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int meanMonitorTable_destroy(struct meanMonitorTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
meanMonitorTable_destroy(struct meanMonitorTable_data **thedata)
{
	struct meanMonitorTable_data *StorageDel;

	DEBUGMSGTL(("metricMIB", "meanMonitorTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->scannerId);
		StorageDel->scannerIdLen = 0;
		SNMP_FREE(StorageDel->estimateOfMeanSpecificProblemsIndicator);
		StorageDel->estimateOfMeanSpecificProblemsIndicatorLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("metricMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int meanMonitorTable_add(struct meanMonitorTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the meanMonitorTable table data set.
 *
 * Adds a table row structure to the meanMonitorTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
meanMonitorTable_add(struct meanMonitorTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("metricMIB", "meanMonitorTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* scannerId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->scannerId, thedata->scannerIdLen);
		header_complex_add_data(&meanMonitorTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("metricMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int meanMonitorTable_del(struct meanMonitorTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the meanMonitorTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
meanMonitorTable_del(struct meanMonitorTable_data *thedata)
{
	struct meanMonitorTable_data *StorageDel;

	DEBUGMSGTL(("metricMIB", "meanMonitorTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(meanMonitorTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&meanMonitorTableStorage, hciptr);
	}
	DEBUGMSGTL(("metricMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_meanMonitorTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for meanMonitorTable entries.
 *
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case meanMonitorTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_meanMonitorTable(const char *token, char *line)
{
	size_t tmpsize;
	struct meanMonitorTable_data *StorageTmp = meanMonitorTable_create();

	DEBUGMSGTL(("metricMIB", "parse_meanMonitorTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->scannerId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->scannerId, &StorageTmp->scannerIdLen);
	if (StorageTmp->scannerId == NULL) {
		config_perror("invalid specification for scannerId");
		return;
	}
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->estimateOfMean, &tmpsize);
	line = read_config_read_data(ASN_TIMETICKS, line, &StorageTmp->movingTimePeriod, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->estimateOfMeanLowThreshold, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->estimateOfMeanLowNotify, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->estimateOfMeanLowSeverityIndication, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->estimateOfMeanHighThreshold, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->estimateOfMeanHighNotify, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->estimateOfMeanHighSeverityIndication, &tmpsize);
	SNMP_FREE(StorageTmp->estimateOfMeanSpecificProblemsIndicator);
	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->estimateOfMeanSpecificProblemsIndicator, &StorageTmp->estimateOfMeanSpecificProblemsIndicatorLen);
	if (StorageTmp->estimateOfMeanSpecificProblemsIndicator == NULL) {
		config_perror("invalid specification for estimateOfMeanSpecificProblemsIndicator");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->meanMonitorRowStatus, &tmpsize);
	meanMonitorTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("metricMIB", "done.\n"));
}

/*
 * store_meanMonitorTable(): store configuraiton file for meanMonitorTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_meanMonitorTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct meanMonitorTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("metricMIB", "store_meanMonitorTable: storing data...  "));
	refresh_meanMonitorTable(1);
	(void) tmpsize;
	for (hcindex = meanMonitorTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct meanMonitorTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "meanMonitorTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->scannerId, &StorageTmp->scannerIdLen);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->estimateOfMean, &tmpsize);
			cptr = read_config_store_data(ASN_TIMETICKS, cptr, &StorageTmp->movingTimePeriod, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->estimateOfMeanLowThreshold, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->estimateOfMeanLowNotify, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->estimateOfMeanLowSeverityIndication, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->estimateOfMeanHighThreshold, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->estimateOfMeanHighNotify, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->estimateOfMeanHighSeverityIndication, &tmpsize);
			cptr = read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->estimateOfMeanSpecificProblemsIndicator, &StorageTmp->estimateOfMeanSpecificProblemsIndicatorLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->meanMonitorRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("metricMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct algorithmIndicatingMeanMonitorTable_data *algorithmIndicatingMeanMonitorTable_create(void)
 * @brief create a fresh data structure representing a new row in the algorithmIndicatingMeanMonitorTable table.
 *
 * Creates a new algorithmIndicatingMeanMonitorTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct algorithmIndicatingMeanMonitorTable_data *
algorithmIndicatingMeanMonitorTable_create(void)
{
	struct algorithmIndicatingMeanMonitorTable_data *StorageNew = SNMP_MALLOC_STRUCT(algorithmIndicatingMeanMonitorTable_data);

	DEBUGMSGTL(("metricMIB", "algorithmIndicatingMeanMonitorTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->scannerId = (uint8_t *) strdup("")) != NULL)
			StorageNew->scannerIdLen = strlen("");
		if ((StorageNew->algorithmIdentifier = snmp_duplicate_objid(zeroDotZero_oid, 2)))
			StorageNew->algorithmIdentifierLen = 2;

	}
	DEBUGMSGTL(("metricMIB", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct algorithmIndicatingMeanMonitorTable_data *algorithmIndicatingMeanMonitorTable_duplicate(struct algorithmIndicatingMeanMonitorTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicat a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct algorithmIndicatingMeanMonitorTable_data *
algorithmIndicatingMeanMonitorTable_duplicate(struct algorithmIndicatingMeanMonitorTable_data *thedata)
{
	struct algorithmIndicatingMeanMonitorTable_data *StorageNew = SNMP_MALLOC_STRUCT(algorithmIndicatingMeanMonitorTable_data);

	DEBUGMSGTL(("metricMIB", "algorithmIndicatingMeanMonitorTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("metricMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	algorithmIndicatingMeanMonitorTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int algorithmIndicatingMeanMonitorTable_destroy(struct algorithmIndicatingMeanMonitorTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
algorithmIndicatingMeanMonitorTable_destroy(struct algorithmIndicatingMeanMonitorTable_data **thedata)
{
	struct algorithmIndicatingMeanMonitorTable_data *StorageDel;

	DEBUGMSGTL(("metricMIB", "algorithmIndicatingMeanMonitorTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->scannerId);
		StorageDel->scannerIdLen = 0;
		SNMP_FREE(StorageDel->algorithmIdentifier);
		StorageDel->algorithmIdentifierLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("metricMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int algorithmIndicatingMeanMonitorTable_add(struct algorithmIndicatingMeanMonitorTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the algorithmIndicatingMeanMonitorTable table data set.
 *
 * Adds a table row structure to the algorithmIndicatingMeanMonitorTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
algorithmIndicatingMeanMonitorTable_add(struct algorithmIndicatingMeanMonitorTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("metricMIB", "algorithmIndicatingMeanMonitorTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* scannerId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->scannerId, thedata->scannerIdLen);
		header_complex_add_data(&algorithmIndicatingMeanMonitorTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("metricMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int algorithmIndicatingMeanMonitorTable_del(struct algorithmIndicatingMeanMonitorTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the algorithmIndicatingMeanMonitorTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
algorithmIndicatingMeanMonitorTable_del(struct algorithmIndicatingMeanMonitorTable_data *thedata)
{
	struct algorithmIndicatingMeanMonitorTable_data *StorageDel;

	DEBUGMSGTL(("metricMIB", "algorithmIndicatingMeanMonitorTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(algorithmIndicatingMeanMonitorTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&algorithmIndicatingMeanMonitorTableStorage, hciptr);
	}
	DEBUGMSGTL(("metricMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_algorithmIndicatingMeanMonitorTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for algorithmIndicatingMeanMonitorTable entries.
 *
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case algorithmIndicatingMeanMonitorTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_algorithmIndicatingMeanMonitorTable(const char *token, char *line)
{
	size_t tmpsize;
	struct algorithmIndicatingMeanMonitorTable_data *StorageTmp = algorithmIndicatingMeanMonitorTable_create();

	DEBUGMSGTL(("metricMIB", "parse_algorithmIndicatingMeanMonitorTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->scannerId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->scannerId, &StorageTmp->scannerIdLen);
	if (StorageTmp->scannerId == NULL) {
		config_perror("invalid specification for scannerId");
		return;
	}
	SNMP_FREE(StorageTmp->algorithmIdentifier);
	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->algorithmIdentifier, &StorageTmp->algorithmIdentifierLen);
	if (StorageTmp->algorithmIdentifier == NULL) {
		config_perror("invalid specification for algorithmIdentifier");
		return;
	}
	algorithmIndicatingMeanMonitorTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("metricMIB", "done.\n"));
}

/*
 * store_algorithmIndicatingMeanMonitorTable(): store configuraiton file for algorithmIndicatingMeanMonitorTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_algorithmIndicatingMeanMonitorTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct algorithmIndicatingMeanMonitorTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("metricMIB", "store_algorithmIndicatingMeanMonitorTable: storing data...  "));
	refresh_algorithmIndicatingMeanMonitorTable(1);
	(void) tmpsize;
	for (hcindex = algorithmIndicatingMeanMonitorTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct algorithmIndicatingMeanMonitorTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "algorithmIndicatingMeanMonitorTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->scannerId, &StorageTmp->scannerIdLen);
			cptr = read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->algorithmIdentifier, &StorageTmp->algorithmIdentifierLen);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("metricMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct movingAverageMeanMonitorTable_data *movingAverageMeanMonitorTable_create(void)
 * @brief create a fresh data structure representing a new row in the movingAverageMeanMonitorTable table.
 *
 * Creates a new movingAverageMeanMonitorTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct movingAverageMeanMonitorTable_data *
movingAverageMeanMonitorTable_create(void)
{
	struct movingAverageMeanMonitorTable_data *StorageNew = SNMP_MALLOC_STRUCT(movingAverageMeanMonitorTable_data);

	DEBUGMSGTL(("metricMIB", "movingAverageMeanMonitorTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->scannerId = (uint8_t *) strdup("")) != NULL)
			StorageNew->scannerIdLen = strlen("");
		StorageNew->movingAverageMeanMonitorRowStatus = 0;
		StorageNew->movingAverageMeanMonitorRowStatus = RS_NOTREADY;
	}
	DEBUGMSGTL(("metricMIB", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct movingAverageMeanMonitorTable_data *movingAverageMeanMonitorTable_duplicate(struct movingAverageMeanMonitorTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicat a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct movingAverageMeanMonitorTable_data *
movingAverageMeanMonitorTable_duplicate(struct movingAverageMeanMonitorTable_data *thedata)
{
	struct movingAverageMeanMonitorTable_data *StorageNew = SNMP_MALLOC_STRUCT(movingAverageMeanMonitorTable_data);

	DEBUGMSGTL(("metricMIB", "movingAverageMeanMonitorTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("metricMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	movingAverageMeanMonitorTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int movingAverageMeanMonitorTable_destroy(struct movingAverageMeanMonitorTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
movingAverageMeanMonitorTable_destroy(struct movingAverageMeanMonitorTable_data **thedata)
{
	struct movingAverageMeanMonitorTable_data *StorageDel;

	DEBUGMSGTL(("metricMIB", "movingAverageMeanMonitorTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->scannerId);
		StorageDel->scannerIdLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("metricMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int movingAverageMeanMonitorTable_add(struct movingAverageMeanMonitorTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the movingAverageMeanMonitorTable table data set.
 *
 * Adds a table row structure to the movingAverageMeanMonitorTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
movingAverageMeanMonitorTable_add(struct movingAverageMeanMonitorTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("metricMIB", "movingAverageMeanMonitorTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* scannerId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->scannerId, thedata->scannerIdLen);
		header_complex_add_data(&movingAverageMeanMonitorTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("metricMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int movingAverageMeanMonitorTable_del(struct movingAverageMeanMonitorTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the movingAverageMeanMonitorTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
movingAverageMeanMonitorTable_del(struct movingAverageMeanMonitorTable_data *thedata)
{
	struct movingAverageMeanMonitorTable_data *StorageDel;

	DEBUGMSGTL(("metricMIB", "movingAverageMeanMonitorTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(movingAverageMeanMonitorTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&movingAverageMeanMonitorTableStorage, hciptr);
	}
	DEBUGMSGTL(("metricMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_movingAverageMeanMonitorTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for movingAverageMeanMonitorTable entries.
 *
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case movingAverageMeanMonitorTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_movingAverageMeanMonitorTable(const char *token, char *line)
{
	size_t tmpsize;
	struct movingAverageMeanMonitorTable_data *StorageTmp = movingAverageMeanMonitorTable_create();

	DEBUGMSGTL(("metricMIB", "parse_movingAverageMeanMonitorTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->scannerId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->scannerId, &StorageTmp->scannerIdLen);
	if (StorageTmp->scannerId == NULL) {
		config_perror("invalid specification for scannerId");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->movingAverageMeanMonitorRowStatus, &tmpsize);
	movingAverageMeanMonitorTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("metricMIB", "done.\n"));
}

/*
 * store_movingAverageMeanMonitorTable(): store configuraiton file for movingAverageMeanMonitorTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_movingAverageMeanMonitorTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct movingAverageMeanMonitorTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("metricMIB", "store_movingAverageMeanMonitorTable: storing data...  "));
	refresh_movingAverageMeanMonitorTable(1);
	(void) tmpsize;
	for (hcindex = movingAverageMeanMonitorTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct movingAverageMeanMonitorTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "movingAverageMeanMonitorTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->scannerId, &StorageTmp->scannerIdLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->movingAverageMeanMonitorRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("metricMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct meanAndPercentileMonitorTable_data *meanAndPercentileMonitorTable_create(void)
 * @brief create a fresh data structure representing a new row in the meanAndPercentileMonitorTable table.
 *
 * Creates a new meanAndPercentileMonitorTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct meanAndPercentileMonitorTable_data *
meanAndPercentileMonitorTable_create(void)
{
	struct meanAndPercentileMonitorTable_data *StorageNew = SNMP_MALLOC_STRUCT(meanAndPercentileMonitorTable_data);

	DEBUGMSGTL(("metricMIB", "meanAndPercentileMonitorTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->scannerId = (uint8_t *) strdup("")) != NULL)
			StorageNew->scannerIdLen = strlen("");
		StorageNew->percentileSecondMovingTimePeriod = 0;
		StorageNew->estimateOfLargestInReplication = 0;
		StorageNew->estimateOfSmallestInReplication = 0;
		StorageNew->estimateOfMedian = 0;
		StorageNew->estimateOf100PCTPercentile = 0;
		StorageNew->estimateOfPCTPercentile = 0;
		StorageNew->numberOfReplications = 0;
		StorageNew->configurablePercentilePackage = 0;
		StorageNew->meanAndPercentileMonitorRowStatus = 0;
		StorageNew->meanAndPercentileMonitorRowStatus = RS_NOTREADY;
	}
	DEBUGMSGTL(("metricMIB", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct meanAndPercentileMonitorTable_data *meanAndPercentileMonitorTable_duplicate(struct meanAndPercentileMonitorTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicat a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct meanAndPercentileMonitorTable_data *
meanAndPercentileMonitorTable_duplicate(struct meanAndPercentileMonitorTable_data *thedata)
{
	struct meanAndPercentileMonitorTable_data *StorageNew = SNMP_MALLOC_STRUCT(meanAndPercentileMonitorTable_data);

	DEBUGMSGTL(("metricMIB", "meanAndPercentileMonitorTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("metricMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	meanAndPercentileMonitorTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int meanAndPercentileMonitorTable_destroy(struct meanAndPercentileMonitorTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
meanAndPercentileMonitorTable_destroy(struct meanAndPercentileMonitorTable_data **thedata)
{
	struct meanAndPercentileMonitorTable_data *StorageDel;

	DEBUGMSGTL(("metricMIB", "meanAndPercentileMonitorTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->scannerId);
		StorageDel->scannerIdLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("metricMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int meanAndPercentileMonitorTable_add(struct meanAndPercentileMonitorTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the meanAndPercentileMonitorTable table data set.
 *
 * Adds a table row structure to the meanAndPercentileMonitorTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
meanAndPercentileMonitorTable_add(struct meanAndPercentileMonitorTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("metricMIB", "meanAndPercentileMonitorTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* scannerId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->scannerId, thedata->scannerIdLen);
		header_complex_add_data(&meanAndPercentileMonitorTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("metricMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int meanAndPercentileMonitorTable_del(struct meanAndPercentileMonitorTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the meanAndPercentileMonitorTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
meanAndPercentileMonitorTable_del(struct meanAndPercentileMonitorTable_data *thedata)
{
	struct meanAndPercentileMonitorTable_data *StorageDel;

	DEBUGMSGTL(("metricMIB", "meanAndPercentileMonitorTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(meanAndPercentileMonitorTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&meanAndPercentileMonitorTableStorage, hciptr);
	}
	DEBUGMSGTL(("metricMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_meanAndPercentileMonitorTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for meanAndPercentileMonitorTable entries.
 *
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case meanAndPercentileMonitorTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_meanAndPercentileMonitorTable(const char *token, char *line)
{
	size_t tmpsize;
	struct meanAndPercentileMonitorTable_data *StorageTmp = meanAndPercentileMonitorTable_create();

	DEBUGMSGTL(("metricMIB", "parse_meanAndPercentileMonitorTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->scannerId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->scannerId, &StorageTmp->scannerIdLen);
	if (StorageTmp->scannerId == NULL) {
		config_perror("invalid specification for scannerId");
		return;
	}
	line = read_config_read_data(ASN_TIMETICKS, line, &StorageTmp->percentileSecondMovingTimePeriod, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->estimateOfLargestInReplication, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->estimateOfSmallestInReplication, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->estimateOfMedian, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->estimateOf100PCTPercentile, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->estimateOfPCTPercentile, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->numberOfReplications, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->configurablePercentilePackage, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->meanAndPercentileMonitorRowStatus, &tmpsize);
	meanAndPercentileMonitorTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("metricMIB", "done.\n"));
}

/*
 * store_meanAndPercentileMonitorTable(): store configuraiton file for meanAndPercentileMonitorTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_meanAndPercentileMonitorTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct meanAndPercentileMonitorTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("metricMIB", "store_meanAndPercentileMonitorTable: storing data...  "));
	refresh_meanAndPercentileMonitorTable(1);
	(void) tmpsize;
	for (hcindex = meanAndPercentileMonitorTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct meanAndPercentileMonitorTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "meanAndPercentileMonitorTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->scannerId, &StorageTmp->scannerIdLen);
			cptr = read_config_store_data(ASN_TIMETICKS, cptr, &StorageTmp->percentileSecondMovingTimePeriod, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->estimateOfLargestInReplication, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->estimateOfSmallestInReplication, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->estimateOfMedian, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->estimateOf100PCTPercentile, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->estimateOfPCTPercentile, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->numberOfReplications, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->configurablePercentilePackage, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->meanAndPercentileMonitorRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("metricMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct meanAndVarianceMonitorTable_data *meanAndVarianceMonitorTable_create(void)
 * @brief create a fresh data structure representing a new row in the meanAndVarianceMonitorTable table.
 *
 * Creates a new meanAndVarianceMonitorTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct meanAndVarianceMonitorTable_data *
meanAndVarianceMonitorTable_create(void)
{
	struct meanAndVarianceMonitorTable_data *StorageNew = SNMP_MALLOC_STRUCT(meanAndVarianceMonitorTable_data);

	DEBUGMSGTL(("metricMIB", "meanAndVarianceMonitorTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->scannerId = (uint8_t *) strdup("")) != NULL)
			StorageNew->scannerIdLen = strlen("");
		StorageNew->varianceSecondMovingTimePeriod = 0;
		StorageNew->estimateOfVariance = 0;
		StorageNew->meanAndVarianceMonitorRowStatus = 0;
		StorageNew->meanAndVarianceMonitorRowStatus = RS_NOTREADY;
	}
	DEBUGMSGTL(("metricMIB", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct meanAndVarianceMonitorTable_data *meanAndVarianceMonitorTable_duplicate(struct meanAndVarianceMonitorTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicat a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct meanAndVarianceMonitorTable_data *
meanAndVarianceMonitorTable_duplicate(struct meanAndVarianceMonitorTable_data *thedata)
{
	struct meanAndVarianceMonitorTable_data *StorageNew = SNMP_MALLOC_STRUCT(meanAndVarianceMonitorTable_data);

	DEBUGMSGTL(("metricMIB", "meanAndVarianceMonitorTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("metricMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	meanAndVarianceMonitorTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int meanAndVarianceMonitorTable_destroy(struct meanAndVarianceMonitorTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
meanAndVarianceMonitorTable_destroy(struct meanAndVarianceMonitorTable_data **thedata)
{
	struct meanAndVarianceMonitorTable_data *StorageDel;

	DEBUGMSGTL(("metricMIB", "meanAndVarianceMonitorTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->scannerId);
		StorageDel->scannerIdLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("metricMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int meanAndVarianceMonitorTable_add(struct meanAndVarianceMonitorTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the meanAndVarianceMonitorTable table data set.
 *
 * Adds a table row structure to the meanAndVarianceMonitorTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
meanAndVarianceMonitorTable_add(struct meanAndVarianceMonitorTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("metricMIB", "meanAndVarianceMonitorTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* scannerId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->scannerId, thedata->scannerIdLen);
		header_complex_add_data(&meanAndVarianceMonitorTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("metricMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int meanAndVarianceMonitorTable_del(struct meanAndVarianceMonitorTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the meanAndVarianceMonitorTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
meanAndVarianceMonitorTable_del(struct meanAndVarianceMonitorTable_data *thedata)
{
	struct meanAndVarianceMonitorTable_data *StorageDel;

	DEBUGMSGTL(("metricMIB", "meanAndVarianceMonitorTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(meanAndVarianceMonitorTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&meanAndVarianceMonitorTableStorage, hciptr);
	}
	DEBUGMSGTL(("metricMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_meanAndVarianceMonitorTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for meanAndVarianceMonitorTable entries.
 *
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case meanAndVarianceMonitorTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_meanAndVarianceMonitorTable(const char *token, char *line)
{
	size_t tmpsize;
	struct meanAndVarianceMonitorTable_data *StorageTmp = meanAndVarianceMonitorTable_create();

	DEBUGMSGTL(("metricMIB", "parse_meanAndVarianceMonitorTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->scannerId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->scannerId, &StorageTmp->scannerIdLen);
	if (StorageTmp->scannerId == NULL) {
		config_perror("invalid specification for scannerId");
		return;
	}
	line = read_config_read_data(ASN_TIMETICKS, line, &StorageTmp->varianceSecondMovingTimePeriod, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->estimateOfVariance, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->meanAndVarianceMonitorRowStatus, &tmpsize);
	meanAndVarianceMonitorTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("metricMIB", "done.\n"));
}

/*
 * store_meanAndVarianceMonitorTable(): store configuraiton file for meanAndVarianceMonitorTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_meanAndVarianceMonitorTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct meanAndVarianceMonitorTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("metricMIB", "store_meanAndVarianceMonitorTable: storing data...  "));
	refresh_meanAndVarianceMonitorTable(1);
	(void) tmpsize;
	for (hcindex = meanAndVarianceMonitorTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct meanAndVarianceMonitorTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "meanAndVarianceMonitorTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->scannerId, &StorageTmp->scannerIdLen);
			cptr = read_config_store_data(ASN_TIMETICKS, cptr, &StorageTmp->varianceSecondMovingTimePeriod, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->estimateOfVariance, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->meanAndVarianceMonitorRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("metricMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct meanAndMinMaxMonitorTable_data *meanAndMinMaxMonitorTable_create(void)
 * @brief create a fresh data structure representing a new row in the meanAndMinMaxMonitorTable table.
 *
 * Creates a new meanAndMinMaxMonitorTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct meanAndMinMaxMonitorTable_data *
meanAndMinMaxMonitorTable_create(void)
{
	struct meanAndMinMaxMonitorTable_data *StorageNew = SNMP_MALLOC_STRUCT(meanAndMinMaxMonitorTable_data);

	DEBUGMSGTL(("metricMIB", "meanAndMinMaxMonitorTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->scannerId = (uint8_t *) strdup("")) != NULL)
			StorageNew->scannerIdLen = strlen("");
		StorageNew->estimateOfLargest = 0;
		StorageNew->estimateOfSmallest = 0;
		StorageNew->meanAndMinMaxMonitorRowStatus = 0;
		StorageNew->meanAndMinMaxMonitorRowStatus = RS_NOTREADY;
	}
	DEBUGMSGTL(("metricMIB", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct meanAndMinMaxMonitorTable_data *meanAndMinMaxMonitorTable_duplicate(struct meanAndMinMaxMonitorTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicat a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct meanAndMinMaxMonitorTable_data *
meanAndMinMaxMonitorTable_duplicate(struct meanAndMinMaxMonitorTable_data *thedata)
{
	struct meanAndMinMaxMonitorTable_data *StorageNew = SNMP_MALLOC_STRUCT(meanAndMinMaxMonitorTable_data);

	DEBUGMSGTL(("metricMIB", "meanAndMinMaxMonitorTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("metricMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	meanAndMinMaxMonitorTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int meanAndMinMaxMonitorTable_destroy(struct meanAndMinMaxMonitorTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
meanAndMinMaxMonitorTable_destroy(struct meanAndMinMaxMonitorTable_data **thedata)
{
	struct meanAndMinMaxMonitorTable_data *StorageDel;

	DEBUGMSGTL(("metricMIB", "meanAndMinMaxMonitorTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->scannerId);
		StorageDel->scannerIdLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("metricMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int meanAndMinMaxMonitorTable_add(struct meanAndMinMaxMonitorTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the meanAndMinMaxMonitorTable table data set.
 *
 * Adds a table row structure to the meanAndMinMaxMonitorTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
meanAndMinMaxMonitorTable_add(struct meanAndMinMaxMonitorTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("metricMIB", "meanAndMinMaxMonitorTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* scannerId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->scannerId, thedata->scannerIdLen);
		header_complex_add_data(&meanAndMinMaxMonitorTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("metricMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int meanAndMinMaxMonitorTable_del(struct meanAndMinMaxMonitorTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the meanAndMinMaxMonitorTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
meanAndMinMaxMonitorTable_del(struct meanAndMinMaxMonitorTable_data *thedata)
{
	struct meanAndMinMaxMonitorTable_data *StorageDel;

	DEBUGMSGTL(("metricMIB", "meanAndMinMaxMonitorTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(meanAndMinMaxMonitorTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&meanAndMinMaxMonitorTableStorage, hciptr);
	}
	DEBUGMSGTL(("metricMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_meanAndMinMaxMonitorTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for meanAndMinMaxMonitorTable entries.
 *
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case meanAndMinMaxMonitorTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_meanAndMinMaxMonitorTable(const char *token, char *line)
{
	size_t tmpsize;
	struct meanAndMinMaxMonitorTable_data *StorageTmp = meanAndMinMaxMonitorTable_create();

	DEBUGMSGTL(("metricMIB", "parse_meanAndMinMaxMonitorTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->scannerId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->scannerId, &StorageTmp->scannerIdLen);
	if (StorageTmp->scannerId == NULL) {
		config_perror("invalid specification for scannerId");
		return;
	}
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->estimateOfLargest, &tmpsize);
	line = read_config_read_data(ASN_GAUGE, line, &StorageTmp->estimateOfSmallest, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->meanAndMinMaxMonitorRowStatus, &tmpsize);
	meanAndMinMaxMonitorTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("metricMIB", "done.\n"));
}

/*
 * store_meanAndMinMaxMonitorTable(): store configuraiton file for meanAndMinMaxMonitorTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_meanAndMinMaxMonitorTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct meanAndMinMaxMonitorTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("metricMIB", "store_meanAndMinMaxMonitorTable: storing data...  "));
	refresh_meanAndMinMaxMonitorTable(1);
	(void) tmpsize;
	for (hcindex = meanAndMinMaxMonitorTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct meanAndMinMaxMonitorTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "meanAndMinMaxMonitorTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->scannerId, &StorageTmp->scannerIdLen);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->estimateOfLargest, &tmpsize);
			cptr = read_config_store_data(ASN_GAUGE, cptr, &StorageTmp->estimateOfSmallest, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->meanAndMinMaxMonitorRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("metricMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void refresh_scannerTable_row(struct scannerTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the scannerTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct scannerTable_data *
refresh_scannerTable_row(struct scannerTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->scannerTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->scannerTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_scannerTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the scannerTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_scannerTable(int force)
{
	if (!force && scannerTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	scannerTable_refresh = 0;
}

/**
 * @fn u_char *var_scannerTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in scannerTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_metricMIB above.
 */
u_char *
var_scannerTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct scannerTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("metricMIB", "var_scannerTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_scannerTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(scannerTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_scannerTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) SCANNERADMINISTRATIVESTATE:	/* Create */
		*write_method = write_scannerAdministrativeState;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->scannerAdministrativeState);
		rval = (u_char *) &StorageTmp->scannerAdministrativeState;
		break;
	case (u_char) GRANULARITYPERIOD:	/* Create */
		*write_method = write_granularityPeriod;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->granularityPeriod);
		rval = (u_char *) &StorageTmp->granularityPeriod;
		break;
	case (u_char) SCANNEROPERATIONALSTATE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->scannerOperationalState);
		rval = (u_char *) &StorageTmp->scannerOperationalState;
		break;
	case (u_char) SCANNERAVAIABILITYSTATUS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->scannerAvaiabilityStatusLen;
		rval = (u_char *) StorageTmp->scannerAvaiabilityStatus;
		break;
	case (u_char) SCANNERSTARTTIME:	/* Create */
		*write_method = write_scannerStartTime;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->scannerStartTimeLen;
		rval = (u_char *) StorageTmp->scannerStartTime;
		break;
	case (u_char) SCANNERSTOPTIME:	/* Create */
		*write_method = write_scannerStopTime;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->scannerStopTimeLen;
		rval = (u_char *) StorageTmp->scannerStopTime;
		break;
	case (u_char) SCANNERINTERVALSOFDAY:	/* Create */
		*write_method = write_scannerIntervalsOfDay;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->scannerIntervalsOfDayLen;
		rval = (u_char *) StorageTmp->scannerIntervalsOfDay;
		break;
	case (u_char) SCANNERWEEKMASK:	/* Create */
		*write_method = write_scannerWeekMask;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->scannerWeekMaskLen;
		rval = (u_char *) StorageTmp->scannerWeekMask;
		break;
	case (u_char) SCANNERSCHEDULERNAME:	/* Create */
		*write_method = write_scannerSchedulerName;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->scannerSchedulerNameLen * sizeof(oid);
		rval = (u_char *) StorageTmp->scannerSchedulerName;
		break;
	case (u_char) SCANNERPERIODSYNCHRONIZATIONTIME:	/* Create */
		*write_method = write_scannerPeriodSynchronizationTime;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->scannerPeriodSynchronizationTimeLen;
		rval = (u_char *) StorageTmp->scannerPeriodSynchronizationTime;
		break;
	case (u_char) SCANNERCREATDELETENOTIFICATIONS:	/* Create */
		*write_method = write_scannerCreatDeleteNotifications;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->scannerCreatDeleteNotifications);
		rval = (u_char *) &StorageTmp->scannerCreatDeleteNotifications;
		break;
	case (u_char) SCANNERATTRIBUTEVALUECHANGENOTIFICATIONS:	/* Create */
		*write_method = write_scannerAttributeValueChangeNotifications;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->scannerAttributeValueChangeNotifications);
		rval = (u_char *) &StorageTmp->scannerAttributeValueChangeNotifications;
		break;
	case (u_char) SCANNERSTATECHANGENOTIFICATIONS:	/* Create */
		*write_method = write_scannerStateChangeNotifications;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->scannerStateChangeNotifications);
		rval = (u_char *) &StorageTmp->scannerStateChangeNotifications;
		break;
	case (u_char) SCANNERROWSTATUS:	/* Create */
		*write_method = write_scannerRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->scannerRowStatus);
		rval = (u_char *) &StorageTmp->scannerRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn void refresh_monitorMetricTable_row(struct monitorMetricTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the monitorMetricTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct monitorMetricTable_data *
refresh_monitorMetricTable_row(struct monitorMetricTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->monitorMetricTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->monitorMetricTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_monitorMetricTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the monitorMetricTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_monitorMetricTable(int force)
{
	if (!force && monitorMetricTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	monitorMetricTable_refresh = 0;
}

/**
 * @fn u_char *var_monitorMetricTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in monitorMetricTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_metricMIB above.
 */
u_char *
var_monitorMetricTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct monitorMetricTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("metricMIB", "var_monitorMetricTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_monitorMetricTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(monitorMetricTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_monitorMetricTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) OBSERVEDOBJECTINSTANCE:	/* Create */
		*write_method = write_observedObjectInstance;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->observedObjectInstanceLen * sizeof(oid);
		rval = (u_char *) StorageTmp->observedObjectInstance;
		break;
	case (u_char) OBSERVEDATTRIBUTEID:	/* Create */
		*write_method = write_observedAttributeId;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->observedAttributeIdLen * sizeof(oid);
		rval = (u_char *) StorageTmp->observedAttributeId;
		break;
	case (u_char) DERIVEDGAUGE:	/* Create */
		*write_method = write_derivedGauge;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->derivedGauge);
		rval = (u_char *) &StorageTmp->derivedGauge;
		break;
	case (u_char) COUNTERDIFFERENCEPACKAGE:	/* Create */
		*write_method = write_counterDifferencePackage;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->counterDifferencePackage);
		rval = (u_char *) &StorageTmp->counterDifferencePackage;
		break;
	case (u_char) PREVIOUSSCANCOUNTERVALUE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->previousScanCounterValue);
		rval = (u_char *) &StorageTmp->previousScanCounterValue;
		break;
	case (u_char) COUNTERDIFFERENCEPROCEDURALSTATUS:	/* Create */
		*write_method = write_counterDifferenceProceduralStatus;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->counterDifferenceProceduralStatusLen;
		rval = (u_char *) StorageTmp->counterDifferenceProceduralStatus;
		break;
	case (u_char) COUNTEROVERFLOWPACKAGE:	/* Create */
		*write_method = write_counterOverflowPackage;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->counterOverflowPackage);
		rval = (u_char *) &StorageTmp->counterOverflowPackage;
		break;
	case (u_char) MODULUSVALUE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->modulusValue);
		rval = (u_char *) &StorageTmp->modulusValue;
		break;
	case (u_char) GAUGEDIFFERENCEPACKAGE:	/* Create */
		*write_method = write_gaugeDifferencePackage;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->gaugeDifferencePackage);
		rval = (u_char *) &StorageTmp->gaugeDifferencePackage;
		break;
	case (u_char) PREVIOUSSCANGAUGEVALUE:	/* Create */
		*write_method = write_previousScanGaugeValue;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->previousScanGaugeValue);
		rval = (u_char *) &StorageTmp->previousScanGaugeValue;
		break;
	case (u_char) GAUGEDIRFERENCEPROCEDURALSTATUS:	/* Create */
		*write_method = write_gaugeDirferenceProceduralStatus;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->gaugeDirferenceProceduralStatusLen;
		rval = (u_char *) StorageTmp->gaugeDirferenceProceduralStatus;
		break;
	case (u_char) METRICLOWTHRESHOLD:	/* Create */
		*write_method = write_metricLowThreshold;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->metricLowThreshold);
		rval = (u_char *) &StorageTmp->metricLowThreshold;
		break;
	case (u_char) METRICLOWNOTIFY:	/* Create */
		*write_method = write_metricLowNotify;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->metricLowNotify);
		rval = (u_char *) &StorageTmp->metricLowNotify;
		break;
	case (u_char) METRICLOWSEVERITYINDICATION:	/* Create */
		*write_method = write_metricLowSeverityIndication;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->metricLowSeverityIndication);
		rval = (u_char *) &StorageTmp->metricLowSeverityIndication;
		break;
	case (u_char) METRICHIGHTHRESHOLD:	/* Create */
		*write_method = write_metricHighThreshold;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->metricHighThreshold);
		rval = (u_char *) &StorageTmp->metricHighThreshold;
		break;
	case (u_char) METRICHIGHNOTIFY:	/* Create */
		*write_method = write_metricHighNotify;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->metricHighNotify);
		rval = (u_char *) &StorageTmp->metricHighNotify;
		break;
	case (u_char) METRICHIGHSEVERITYINDICATION:	/* Create */
		*write_method = write_metricHighSeverityIndication;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->metricHighSeverityIndication);
		rval = (u_char *) &StorageTmp->metricHighSeverityIndication;
		break;
	case (u_char) METRICSPECIFICPROBLEMSINDICATOR:	/* Create */
		*write_method = write_metricSpecificProblemsIndicator;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->metricSpecificProblemsIndicatorLen * sizeof(oid);
		rval = (u_char *) StorageTmp->metricSpecificProblemsIndicator;
		break;
	case (u_char) DERIVEDGAUGETIMESTAMP:	/* Create */
		*write_method = write_derivedGaugeTimeStamp;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->derivedGaugeTimeStampLen;
		rval = (u_char *) StorageTmp->derivedGaugeTimeStamp;
		break;
	case (u_char) MONITORMETRICROWSTATUS:	/* Create */
		*write_method = write_monitorMetricRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->monitorMetricRowStatus);
		rval = (u_char *) &StorageTmp->monitorMetricRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn void refresh_meanMonitorTable_row(struct meanMonitorTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the meanMonitorTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct meanMonitorTable_data *
refresh_meanMonitorTable_row(struct meanMonitorTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->meanMonitorTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->meanMonitorTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_meanMonitorTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the meanMonitorTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_meanMonitorTable(int force)
{
	if (!force && meanMonitorTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	meanMonitorTable_refresh = 0;
}

/**
 * @fn u_char *var_meanMonitorTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in meanMonitorTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_metricMIB above.
 */
u_char *
var_meanMonitorTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct meanMonitorTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("metricMIB", "var_meanMonitorTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_meanMonitorTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(meanMonitorTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_meanMonitorTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) ESTIMATEOFMEAN:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->estimateOfMean);
		rval = (u_char *) &StorageTmp->estimateOfMean;
		break;
	case (u_char) MOVINGTIMEPERIOD:	/* Create */
		*write_method = write_movingTimePeriod;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->movingTimePeriod);
		rval = (u_char *) &StorageTmp->movingTimePeriod;
		break;
	case (u_char) ESTIMATEOFMEANLOWTHRESHOLD:	/* Create */
		*write_method = write_estimateOfMeanLowThreshold;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->estimateOfMeanLowThreshold);
		rval = (u_char *) &StorageTmp->estimateOfMeanLowThreshold;
		break;
	case (u_char) ESTIMATEOFMEANLOWNOTIFY:	/* Create */
		*write_method = write_estimateOfMeanLowNotify;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->estimateOfMeanLowNotify);
		rval = (u_char *) &StorageTmp->estimateOfMeanLowNotify;
		break;
	case (u_char) ESTIMATEOFMEANLOWSEVERITYINDICATION:	/* Create */
		*write_method = write_estimateOfMeanLowSeverityIndication;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->estimateOfMeanLowSeverityIndication);
		rval = (u_char *) &StorageTmp->estimateOfMeanLowSeverityIndication;
		break;
	case (u_char) ESTIMATEOFMEANHIGHTHRESHOLD:	/* Create */
		*write_method = write_estimateOfMeanHighThreshold;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->estimateOfMeanHighThreshold);
		rval = (u_char *) &StorageTmp->estimateOfMeanHighThreshold;
		break;
	case (u_char) ESTIMATEOFMEANHIGHNOTIFY:	/* Create */
		*write_method = write_estimateOfMeanHighNotify;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->estimateOfMeanHighNotify);
		rval = (u_char *) &StorageTmp->estimateOfMeanHighNotify;
		break;
	case (u_char) ESTIMATEOFMEANHIGHSEVERITYINDICATION:	/* Create */
		*write_method = write_estimateOfMeanHighSeverityIndication;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->estimateOfMeanHighSeverityIndication);
		rval = (u_char *) &StorageTmp->estimateOfMeanHighSeverityIndication;
		break;
	case (u_char) ESTIMATEOFMEANSPECIFICPROBLEMSINDICATOR:	/* Create */
		*write_method = write_estimateOfMeanSpecificProblemsIndicator;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->estimateOfMeanSpecificProblemsIndicatorLen * sizeof(oid);
		rval = (u_char *) StorageTmp->estimateOfMeanSpecificProblemsIndicator;
		break;
	case (u_char) MEANMONITORROWSTATUS:	/* Create */
		*write_method = write_meanMonitorRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->meanMonitorRowStatus);
		rval = (u_char *) &StorageTmp->meanMonitorRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn void refresh_algorithmIndicatingMeanMonitorTable_row(struct algorithmIndicatingMeanMonitorTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the algorithmIndicatingMeanMonitorTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct algorithmIndicatingMeanMonitorTable_data *
refresh_algorithmIndicatingMeanMonitorTable_row(struct algorithmIndicatingMeanMonitorTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->algorithmIndicatingMeanMonitorTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->algorithmIndicatingMeanMonitorTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_algorithmIndicatingMeanMonitorTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the algorithmIndicatingMeanMonitorTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_algorithmIndicatingMeanMonitorTable(int force)
{
	if (!force && algorithmIndicatingMeanMonitorTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	algorithmIndicatingMeanMonitorTable_refresh = 0;
}

/**
 * @fn u_char *var_algorithmIndicatingMeanMonitorTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in algorithmIndicatingMeanMonitorTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_metricMIB above.
 */
u_char *
var_algorithmIndicatingMeanMonitorTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct algorithmIndicatingMeanMonitorTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("metricMIB", "var_algorithmIndicatingMeanMonitorTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_algorithmIndicatingMeanMonitorTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(algorithmIndicatingMeanMonitorTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_algorithmIndicatingMeanMonitorTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) ALGORITHMIDENTIFIER:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->algorithmIdentifierLen * sizeof(oid);
		rval = (u_char *) StorageTmp->algorithmIdentifier;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn void refresh_movingAverageMeanMonitorTable_row(struct movingAverageMeanMonitorTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the movingAverageMeanMonitorTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct movingAverageMeanMonitorTable_data *
refresh_movingAverageMeanMonitorTable_row(struct movingAverageMeanMonitorTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->movingAverageMeanMonitorTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->movingAverageMeanMonitorTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_movingAverageMeanMonitorTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the movingAverageMeanMonitorTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_movingAverageMeanMonitorTable(int force)
{
	if (!force && movingAverageMeanMonitorTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	movingAverageMeanMonitorTable_refresh = 0;
}

/**
 * @fn u_char *var_movingAverageMeanMonitorTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in movingAverageMeanMonitorTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_metricMIB above.
 */
u_char *
var_movingAverageMeanMonitorTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct movingAverageMeanMonitorTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("metricMIB", "var_movingAverageMeanMonitorTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_movingAverageMeanMonitorTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(movingAverageMeanMonitorTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_movingAverageMeanMonitorTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) MOVINGAVERAGEMEANMONITORROWSTATUS:	/* Create */
		*write_method = write_movingAverageMeanMonitorRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->movingAverageMeanMonitorRowStatus);
		rval = (u_char *) &StorageTmp->movingAverageMeanMonitorRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn void refresh_meanAndPercentileMonitorTable_row(struct meanAndPercentileMonitorTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the meanAndPercentileMonitorTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct meanAndPercentileMonitorTable_data *
refresh_meanAndPercentileMonitorTable_row(struct meanAndPercentileMonitorTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->meanAndPercentileMonitorTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->meanAndPercentileMonitorTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_meanAndPercentileMonitorTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the meanAndPercentileMonitorTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_meanAndPercentileMonitorTable(int force)
{
	if (!force && meanAndPercentileMonitorTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	meanAndPercentileMonitorTable_refresh = 0;
}

/**
 * @fn u_char *var_meanAndPercentileMonitorTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in meanAndPercentileMonitorTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_metricMIB above.
 */
u_char *
var_meanAndPercentileMonitorTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct meanAndPercentileMonitorTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("metricMIB", "var_meanAndPercentileMonitorTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_meanAndPercentileMonitorTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(meanAndPercentileMonitorTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_meanAndPercentileMonitorTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) PERCENTILESECONDMOVINGTIMEPERIOD:	/* Create */
		*write_method = write_percentileSecondMovingTimePeriod;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->percentileSecondMovingTimePeriod);
		rval = (u_char *) &StorageTmp->percentileSecondMovingTimePeriod;
		break;
	case (u_char) ESTIMATEOFLARGESTINREPLICATION:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->estimateOfLargestInReplication);
		rval = (u_char *) &StorageTmp->estimateOfLargestInReplication;
		break;
	case (u_char) ESTIMATEOFSMALLESTINREPLICATION:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->estimateOfSmallestInReplication);
		rval = (u_char *) &StorageTmp->estimateOfSmallestInReplication;
		break;
	case (u_char) ESTIMATEOFMEDIAN:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->estimateOfMedian);
		rval = (u_char *) &StorageTmp->estimateOfMedian;
		break;
	case (u_char) ESTIMATEOF100PCTPERCENTILE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->estimateOf100PCTPercentile);
		rval = (u_char *) &StorageTmp->estimateOf100PCTPercentile;
		break;
	case (u_char) ESTIMATEOFPCTPERCENTILE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->estimateOfPCTPercentile);
		rval = (u_char *) &StorageTmp->estimateOfPCTPercentile;
		break;
	case (u_char) NUMBEROFREPLICATIONS:	/* Create */
		*write_method = write_numberOfReplications;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->numberOfReplications);
		rval = (u_char *) &StorageTmp->numberOfReplications;
		break;
	case (u_char) CONFIGURABLEPERCENTILEPACKAGE:	/* Create */
		*write_method = write_configurablePercentilePackage;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->configurablePercentilePackage);
		rval = (u_char *) &StorageTmp->configurablePercentilePackage;
		break;
	case (u_char) MEANANDPERCENTILEMONITORROWSTATUS:	/* Create */
		*write_method = write_meanAndPercentileMonitorRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->meanAndPercentileMonitorRowStatus);
		rval = (u_char *) &StorageTmp->meanAndPercentileMonitorRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn void refresh_meanAndVarianceMonitorTable_row(struct meanAndVarianceMonitorTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the meanAndVarianceMonitorTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct meanAndVarianceMonitorTable_data *
refresh_meanAndVarianceMonitorTable_row(struct meanAndVarianceMonitorTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->meanAndVarianceMonitorTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->meanAndVarianceMonitorTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_meanAndVarianceMonitorTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the meanAndVarianceMonitorTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_meanAndVarianceMonitorTable(int force)
{
	if (!force && meanAndVarianceMonitorTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	meanAndVarianceMonitorTable_refresh = 0;
}

/**
 * @fn u_char *var_meanAndVarianceMonitorTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in meanAndVarianceMonitorTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_metricMIB above.
 */
u_char *
var_meanAndVarianceMonitorTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct meanAndVarianceMonitorTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("metricMIB", "var_meanAndVarianceMonitorTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_meanAndVarianceMonitorTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(meanAndVarianceMonitorTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_meanAndVarianceMonitorTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) VARIANCESECONDMOVINGTIMEPERIOD:	/* Create */
		*write_method = write_varianceSecondMovingTimePeriod;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->varianceSecondMovingTimePeriod);
		rval = (u_char *) &StorageTmp->varianceSecondMovingTimePeriod;
		break;
	case (u_char) ESTIMATEOFVARIANCE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->estimateOfVariance);
		rval = (u_char *) &StorageTmp->estimateOfVariance;
		break;
	case (u_char) MEANANDVARIANCEMONITORROWSTATUS:	/* Create */
		*write_method = write_meanAndVarianceMonitorRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->meanAndVarianceMonitorRowStatus);
		rval = (u_char *) &StorageTmp->meanAndVarianceMonitorRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn void refresh_meanAndMinMaxMonitorTable_row(struct meanAndMinMaxMonitorTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the meanAndMinMaxMonitorTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct meanAndMinMaxMonitorTable_data *
refresh_meanAndMinMaxMonitorTable_row(struct meanAndMinMaxMonitorTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->meanAndMinMaxMonitorTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->meanAndMinMaxMonitorTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_meanAndMinMaxMonitorTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the meanAndMinMaxMonitorTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_meanAndMinMaxMonitorTable(int force)
{
	if (!force && meanAndMinMaxMonitorTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	meanAndMinMaxMonitorTable_refresh = 0;
}

/**
 * @fn u_char *var_meanAndMinMaxMonitorTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in meanAndMinMaxMonitorTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_metricMIB above.
 */
u_char *
var_meanAndMinMaxMonitorTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct meanAndMinMaxMonitorTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("metricMIB", "var_meanAndMinMaxMonitorTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_meanAndMinMaxMonitorTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(meanAndMinMaxMonitorTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_meanAndMinMaxMonitorTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) ESTIMATEOFLARGEST:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->estimateOfLargest);
		rval = (u_char *) &StorageTmp->estimateOfLargest;
		break;
	case (u_char) ESTIMATEOFSMALLEST:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->estimateOfSmallest);
		rval = (u_char *) &StorageTmp->estimateOfSmallest;
		break;
	case (u_char) MEANANDMINMAXMONITORROWSTATUS:	/* Create */
		*write_method = write_meanAndMinMaxMonitorRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->meanAndMinMaxMonitorRowStatus);
		rval = (u_char *) &StorageTmp->meanAndMinMaxMonitorRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int write_scannerAdministrativeState(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_scannerAdministrativeState(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct scannerTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("metricMIB", "write_scannerAdministrativeState entering action=%d...  \n", action));
	StorageTmp = header_complex(scannerTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->scannerRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to scannerAdministrativeState: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to scannerAdministrativeState not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to scannerAdministrativeState: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case SCANNERADMINISTRATIVESTATE_LOCKED:
		case SCANNERADMINISTRATIVESTATE_UNLOCKED:
		case SCANNERADMINISTRATIVESTATE_SHUTTINGDOWN:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to scannerAdministrativeState: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->scannerAdministrativeState for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->scannerAdministrativeState;
		StorageTmp->scannerAdministrativeState = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->scannerAdministrativeState = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_granularityPeriod(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_granularityPeriod(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct scannerTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("metricMIB", "write_granularityPeriod entering action=%d...  \n", action));
	StorageTmp = header_complex(scannerTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->scannerRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to granularityPeriod: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_TIMETICKS) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to granularityPeriod not ASN_TIMETICKS\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to granularityPeriod: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->granularityPeriod for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->granularityPeriod;
		StorageTmp->granularityPeriod = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->granularityPeriod = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_scannerStartTime(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_scannerStartTime(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static uint8_t *old_value;
	struct scannerTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	static size_t old_length = 0;
	static uint8_t *string = NULL;

	DEBUGMSGTL(("metricMIB", "write_scannerStartTime entering action=%d...  \n", action));
	StorageTmp = header_complex(scannerTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		string = NULL;
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->scannerRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to scannerStartTime: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to scannerStartTime not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 8..8 11..11 */
		if (var_val_len > SPRINT_MAX_LEN || (var_val_len != 8 && var_val_len != 11)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to scannerStartTime: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->scannerStartTime for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->scannerStartTime;
		old_length = StorageTmp->scannerStartTimeLen;
		StorageTmp->scannerStartTime = string;
		StorageTmp->scannerStartTimeLen = var_val_len;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(old_value);
		old_length = 0;
		string = NULL;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->scannerStartTime = old_value;
		StorageTmp->scannerStartTimeLen = old_length;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		SNMP_FREE(string);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_scannerStopTime(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_scannerStopTime(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static uint8_t *old_value;
	struct scannerTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	static size_t old_length = 0;
	static uint8_t *string = NULL;

	DEBUGMSGTL(("metricMIB", "write_scannerStopTime entering action=%d...  \n", action));
	StorageTmp = header_complex(scannerTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		string = NULL;
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->scannerRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to scannerStopTime: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to scannerStopTime not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 8..8 11..11 */
		if (var_val_len > SPRINT_MAX_LEN || (var_val_len != 8 && var_val_len != 11)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to scannerStopTime: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->scannerStopTime for you to use, and you have just been asked to do something with it.  Note that anything done here must 
				   be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->scannerStopTime;
		old_length = StorageTmp->scannerStopTimeLen;
		StorageTmp->scannerStopTime = string;
		StorageTmp->scannerStopTimeLen = var_val_len;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(old_value);
		old_length = 0;
		string = NULL;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->scannerStopTime = old_value;
		StorageTmp->scannerStopTimeLen = old_length;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		SNMP_FREE(string);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_scannerIntervalsOfDay(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_scannerIntervalsOfDay(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static uint8_t *old_value;
	struct scannerTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	static size_t old_length = 0;
	static uint8_t *string = NULL;

	DEBUGMSGTL(("metricMIB", "write_scannerIntervalsOfDay entering action=%d...  \n", action));
	StorageTmp = header_complex(scannerTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		string = NULL;
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->scannerRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to scannerIntervalsOfDay: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to scannerIntervalsOfDay not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > SPRINT_MAX_LEN) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to scannerIntervalsOfDay: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->scannerIntervalsOfDay for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->scannerIntervalsOfDay;
		old_length = StorageTmp->scannerIntervalsOfDayLen;
		StorageTmp->scannerIntervalsOfDay = string;
		StorageTmp->scannerIntervalsOfDayLen = var_val_len;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(old_value);
		old_length = 0;
		string = NULL;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->scannerIntervalsOfDay = old_value;
		StorageTmp->scannerIntervalsOfDayLen = old_length;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		SNMP_FREE(string);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_scannerWeekMask(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_scannerWeekMask(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static uint8_t *old_value;
	struct scannerTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	static size_t old_length = 0;
	static uint8_t *string = NULL;

	DEBUGMSGTL(("metricMIB", "write_scannerWeekMask entering action=%d...  \n", action));
	StorageTmp = header_complex(scannerTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		string = NULL;
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->scannerRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to scannerWeekMask: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to scannerWeekMask not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > SPRINT_MAX_LEN) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to scannerWeekMask: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->scannerWeekMask for you to use, and you have just been asked to do something with it.  Note that anything done here must 
				   be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->scannerWeekMask;
		old_length = StorageTmp->scannerWeekMaskLen;
		StorageTmp->scannerWeekMask = string;
		StorageTmp->scannerWeekMaskLen = var_val_len;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(old_value);
		old_length = 0;
		string = NULL;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->scannerWeekMask = old_value;
		StorageTmp->scannerWeekMaskLen = old_length;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		SNMP_FREE(string);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_scannerSchedulerName(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_scannerSchedulerName(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static oid *old_value;
	struct scannerTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	static size_t old_length = 0;
	static oid *objid = NULL;

	DEBUGMSGTL(("metricMIB", "write_scannerSchedulerName entering action=%d...  \n", action));
	StorageTmp = header_complex(scannerTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		objid = NULL;
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->scannerRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to scannerSchedulerName: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OBJECT_ID) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to scannerSchedulerName not ASN_OBJECT_ID\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (MIN_OID_LEN * sizeof(oid) > var_val_len || var_val_len > MAX_OID_LEN * sizeof(oid)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to scannerSchedulerName: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((objid = snmp_duplicate_objid((void *) var_val, var_val_len / sizeof(oid))) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->scannerSchedulerName for you to use, and you have just been asked to do something with it.  Note that anything done here 
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->scannerSchedulerName;
		old_length = StorageTmp->scannerSchedulerNameLen;
		StorageTmp->scannerSchedulerName = objid;
		StorageTmp->scannerSchedulerNameLen = var_val_len / sizeof(oid);
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(old_value);
		old_length = 0;
		objid = NULL;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->scannerSchedulerName = old_value;
		StorageTmp->scannerSchedulerNameLen = old_length;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		SNMP_FREE(objid);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_scannerPeriodSynchronizationTime(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_scannerPeriodSynchronizationTime(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static uint8_t *old_value;
	struct scannerTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	static size_t old_length = 0;
	static uint8_t *string = NULL;

	DEBUGMSGTL(("metricMIB", "write_scannerPeriodSynchronizationTime entering action=%d...  \n", action));
	StorageTmp = header_complex(scannerTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		string = NULL;
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->scannerRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to scannerPeriodSynchronizationTime: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to scannerPeriodSynchronizationTime not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 8..8 11..11 */
		if (var_val_len > SPRINT_MAX_LEN || (var_val_len != 8 && var_val_len != 11)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to scannerPeriodSynchronizationTime: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->scannerPeriodSynchronizationTime for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->scannerPeriodSynchronizationTime;
		old_length = StorageTmp->scannerPeriodSynchronizationTimeLen;
		StorageTmp->scannerPeriodSynchronizationTime = string;
		StorageTmp->scannerPeriodSynchronizationTimeLen = var_val_len;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(old_value);
		old_length = 0;
		string = NULL;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->scannerPeriodSynchronizationTime = old_value;
		StorageTmp->scannerPeriodSynchronizationTimeLen = old_length;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		SNMP_FREE(string);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_scannerCreatDeleteNotifications(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_scannerCreatDeleteNotifications(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct scannerTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("metricMIB", "write_scannerCreatDeleteNotifications entering action=%d...  \n", action));
	StorageTmp = header_complex(scannerTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->scannerRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to scannerCreatDeleteNotifications: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to scannerCreatDeleteNotifications not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to scannerCreatDeleteNotifications: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to scannerCreatDeleteNotifications: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->scannerCreatDeleteNotifications for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->scannerCreatDeleteNotifications;
		StorageTmp->scannerCreatDeleteNotifications = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->scannerCreatDeleteNotifications = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_scannerAttributeValueChangeNotifications(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_scannerAttributeValueChangeNotifications(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct scannerTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("metricMIB", "write_scannerAttributeValueChangeNotifications entering action=%d...  \n", action));
	StorageTmp = header_complex(scannerTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->scannerRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to scannerAttributeValueChangeNotifications: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to scannerAttributeValueChangeNotifications not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to scannerAttributeValueChangeNotifications: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to scannerAttributeValueChangeNotifications: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->scannerAttributeValueChangeNotifications for you to use, and you have just been asked to do something with it.  Note
				   that anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->scannerAttributeValueChangeNotifications;
		StorageTmp->scannerAttributeValueChangeNotifications = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->scannerAttributeValueChangeNotifications = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_scannerStateChangeNotifications(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_scannerStateChangeNotifications(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct scannerTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("metricMIB", "write_scannerStateChangeNotifications entering action=%d...  \n", action));
	StorageTmp = header_complex(scannerTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->scannerRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to scannerStateChangeNotifications: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to scannerStateChangeNotifications not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to scannerStateChangeNotifications: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to scannerStateChangeNotifications: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->scannerStateChangeNotifications for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->scannerStateChangeNotifications;
		StorageTmp->scannerStateChangeNotifications = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->scannerStateChangeNotifications = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_observedObjectInstance(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_observedObjectInstance(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static oid *old_value;
	struct monitorMetricTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	static size_t old_length = 0;
	static oid *objid = NULL;

	DEBUGMSGTL(("metricMIB", "write_observedObjectInstance entering action=%d...  \n", action));
	StorageTmp = header_complex(monitorMetricTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		objid = NULL;
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->monitorMetricRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to observedObjectInstance: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OBJECT_ID) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to observedObjectInstance not ASN_OBJECT_ID\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (MIN_OID_LEN * sizeof(oid) > var_val_len || var_val_len > MAX_OID_LEN * sizeof(oid)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to observedObjectInstance: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((objid = snmp_duplicate_objid((void *) var_val, var_val_len / sizeof(oid))) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->observedObjectInstance for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->observedObjectInstance;
		old_length = StorageTmp->observedObjectInstanceLen;
		StorageTmp->observedObjectInstance = objid;
		StorageTmp->observedObjectInstanceLen = var_val_len / sizeof(oid);
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(old_value);
		old_length = 0;
		objid = NULL;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->observedObjectInstance = old_value;
		StorageTmp->observedObjectInstanceLen = old_length;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		SNMP_FREE(objid);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_observedAttributeId(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_observedAttributeId(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static oid *old_value;
	struct monitorMetricTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	static size_t old_length = 0;
	static oid *objid = NULL;

	DEBUGMSGTL(("metricMIB", "write_observedAttributeId entering action=%d...  \n", action));
	StorageTmp = header_complex(monitorMetricTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		objid = NULL;
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->monitorMetricRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to observedAttributeId: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OBJECT_ID) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to observedAttributeId not ASN_OBJECT_ID\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (MIN_OID_LEN * sizeof(oid) > var_val_len || var_val_len > MAX_OID_LEN * sizeof(oid)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to observedAttributeId: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((objid = snmp_duplicate_objid((void *) var_val, var_val_len / sizeof(oid))) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->observedAttributeId for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->observedAttributeId;
		old_length = StorageTmp->observedAttributeIdLen;
		StorageTmp->observedAttributeId = objid;
		StorageTmp->observedAttributeIdLen = var_val_len / sizeof(oid);
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(old_value);
		old_length = 0;
		objid = NULL;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->observedAttributeId = old_value;
		StorageTmp->observedAttributeIdLen = old_length;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		SNMP_FREE(objid);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_derivedGauge(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_derivedGauge(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct monitorMetricTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("metricMIB", "write_derivedGauge entering action=%d...  \n", action));
	StorageTmp = header_complex(monitorMetricTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->monitorMetricRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to derivedGauge: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_GAUGE) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to derivedGauge not ASN_GAUGE\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to derivedGauge: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->derivedGauge for you to use, and you have just been asked to do something with it.  Note that anything done here must be 
				   reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->derivedGauge;
		StorageTmp->derivedGauge = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->derivedGauge = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_counterDifferencePackage(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_counterDifferencePackage(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct monitorMetricTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("metricMIB", "write_counterDifferencePackage entering action=%d...  \n", action));
	StorageTmp = header_complex(monitorMetricTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->monitorMetricRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to counterDifferencePackage: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to counterDifferencePackage not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to counterDifferencePackage: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to counterDifferencePackage: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->counterDifferencePackage for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->counterDifferencePackage;
		StorageTmp->counterDifferencePackage = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->counterDifferencePackage = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_counterDifferenceProceduralStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_counterDifferenceProceduralStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static uint8_t *old_value;
	struct monitorMetricTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	static size_t old_length = 0;
	static uint8_t *string = NULL;

	DEBUGMSGTL(("metricMIB", "write_counterDifferenceProceduralStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(monitorMetricTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		string = NULL;
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->monitorMetricRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to counterDifferenceProceduralStatus: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if ((var_val_type != ASN_BIT_STR && var_val_type != ASN_OCTET_STR)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to counterDifferenceProceduralStatus not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_type == ASN_BIT_STR) {
			if (1 > var_val_len || var_val_len > SPRINT_MAX_LEN || var_val_len != 2) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to counterDifferenceProceduralStatus: bad length\n");
				return SNMP_ERR_WRONGLENGTH;
			}
		}
		if (var_val_type == ASN_OCTET_STR) {
			if (var_val_len > SPRINT_MAX_LEN || var_val_len != 1) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to counterDifferenceProceduralStatus: bad length\n");
				return SNMP_ERR_WRONGLENGTH;
			}
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->counterDifferenceProceduralStatus for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->counterDifferenceProceduralStatus;
		old_length = StorageTmp->counterDifferenceProceduralStatusLen;
		StorageTmp->counterDifferenceProceduralStatus = string;
		StorageTmp->counterDifferenceProceduralStatusLen = var_val_len;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(old_value);
		old_length = 0;
		string = NULL;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->counterDifferenceProceduralStatus = old_value;
		StorageTmp->counterDifferenceProceduralStatusLen = old_length;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		SNMP_FREE(string);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_counterOverflowPackage(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_counterOverflowPackage(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct monitorMetricTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("metricMIB", "write_counterOverflowPackage entering action=%d...  \n", action));
	StorageTmp = header_complex(monitorMetricTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->monitorMetricRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to counterOverflowPackage: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to counterOverflowPackage not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to counterOverflowPackage: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to counterOverflowPackage: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->counterOverflowPackage for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->counterOverflowPackage;
		StorageTmp->counterOverflowPackage = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->counterOverflowPackage = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_gaugeDifferencePackage(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_gaugeDifferencePackage(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct monitorMetricTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("metricMIB", "write_gaugeDifferencePackage entering action=%d...  \n", action));
	StorageTmp = header_complex(monitorMetricTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->monitorMetricRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to gaugeDifferencePackage: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to gaugeDifferencePackage not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to gaugeDifferencePackage: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to gaugeDifferencePackage: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->gaugeDifferencePackage for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->gaugeDifferencePackage;
		StorageTmp->gaugeDifferencePackage = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->gaugeDifferencePackage = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_previousScanGaugeValue(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_previousScanGaugeValue(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct monitorMetricTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("metricMIB", "write_previousScanGaugeValue entering action=%d...  \n", action));
	StorageTmp = header_complex(monitorMetricTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->monitorMetricRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to previousScanGaugeValue: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_GAUGE) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to previousScanGaugeValue not ASN_GAUGE\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to previousScanGaugeValue: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->previousScanGaugeValue for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->previousScanGaugeValue;
		StorageTmp->previousScanGaugeValue = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->previousScanGaugeValue = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_gaugeDirferenceProceduralStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_gaugeDirferenceProceduralStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static uint8_t *old_value;
	struct monitorMetricTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	static size_t old_length = 0;
	static uint8_t *string = NULL;

	DEBUGMSGTL(("metricMIB", "write_gaugeDirferenceProceduralStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(monitorMetricTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		string = NULL;
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->monitorMetricRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to gaugeDirferenceProceduralStatus: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if ((var_val_type != ASN_BIT_STR && var_val_type != ASN_OCTET_STR)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to gaugeDirferenceProceduralStatus not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_type == ASN_BIT_STR) {
			if (1 > var_val_len || var_val_len > SPRINT_MAX_LEN || var_val_len != 2) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to gaugeDirferenceProceduralStatus: bad length\n");
				return SNMP_ERR_WRONGLENGTH;
			}
		}
		if (var_val_type == ASN_OCTET_STR) {
			if (var_val_len > SPRINT_MAX_LEN || var_val_len != 1) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to gaugeDirferenceProceduralStatus: bad length\n");
				return SNMP_ERR_WRONGLENGTH;
			}
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->gaugeDirferenceProceduralStatus for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->gaugeDirferenceProceduralStatus;
		old_length = StorageTmp->gaugeDirferenceProceduralStatusLen;
		StorageTmp->gaugeDirferenceProceduralStatus = string;
		StorageTmp->gaugeDirferenceProceduralStatusLen = var_val_len;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(old_value);
		old_length = 0;
		string = NULL;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->gaugeDirferenceProceduralStatus = old_value;
		StorageTmp->gaugeDirferenceProceduralStatusLen = old_length;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		SNMP_FREE(string);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_metricLowThreshold(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_metricLowThreshold(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct monitorMetricTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("metricMIB", "write_metricLowThreshold entering action=%d...  \n", action));
	StorageTmp = header_complex(monitorMetricTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->monitorMetricRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to metricLowThreshold: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to metricLowThreshold not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to metricLowThreshold: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->metricLowThreshold for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->metricLowThreshold;
		StorageTmp->metricLowThreshold = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->metricLowThreshold = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_metricLowNotify(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_metricLowNotify(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct monitorMetricTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("metricMIB", "write_metricLowNotify entering action=%d...  \n", action));
	StorageTmp = header_complex(monitorMetricTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->monitorMetricRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to metricLowNotify: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to metricLowNotify not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to metricLowNotify: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to metricLowNotify: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->metricLowNotify for you to use, and you have just been asked to do something with it.  Note that anything done here must 
				   be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->metricLowNotify;
		StorageTmp->metricLowNotify = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->metricLowNotify = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_metricLowSeverityIndication(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_metricLowSeverityIndication(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct monitorMetricTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("metricMIB", "write_metricLowSeverityIndication entering action=%d...  \n", action));
	StorageTmp = header_complex(monitorMetricTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->monitorMetricRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to metricLowSeverityIndication: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to metricLowSeverityIndication not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to metricLowSeverityIndication: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case METRICLOWSEVERITYINDICATION_INDETERMINATE:
		case METRICLOWSEVERITYINDICATION_CRITICAL:
		case METRICLOWSEVERITYINDICATION_MAJOR:
		case METRICLOWSEVERITYINDICATION_MINOR:
		case METRICLOWSEVERITYINDICATION_WARNING:
		case METRICLOWSEVERITYINDICATION_CLEARED:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to metricLowSeverityIndication: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->metricLowSeverityIndication for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->metricLowSeverityIndication;
		StorageTmp->metricLowSeverityIndication = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->metricLowSeverityIndication = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_metricHighThreshold(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_metricHighThreshold(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct monitorMetricTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("metricMIB", "write_metricHighThreshold entering action=%d...  \n", action));
	StorageTmp = header_complex(monitorMetricTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->monitorMetricRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to metricHighThreshold: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to metricHighThreshold not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to metricHighThreshold: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->metricHighThreshold for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->metricHighThreshold;
		StorageTmp->metricHighThreshold = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->metricHighThreshold = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_metricHighNotify(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_metricHighNotify(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct monitorMetricTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("metricMIB", "write_metricHighNotify entering action=%d...  \n", action));
	StorageTmp = header_complex(monitorMetricTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->monitorMetricRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to metricHighNotify: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to metricHighNotify not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to metricHighNotify: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to metricHighNotify: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->metricHighNotify for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->metricHighNotify;
		StorageTmp->metricHighNotify = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->metricHighNotify = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_metricHighSeverityIndication(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_metricHighSeverityIndication(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct monitorMetricTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("metricMIB", "write_metricHighSeverityIndication entering action=%d...  \n", action));
	StorageTmp = header_complex(monitorMetricTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->monitorMetricRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to metricHighSeverityIndication: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to metricHighSeverityIndication not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to metricHighSeverityIndication: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case METRICHIGHSEVERITYINDICATION_INDETERMINATE:
		case METRICHIGHSEVERITYINDICATION_CRITICAL:
		case METRICHIGHSEVERITYINDICATION_MAJOR:
		case METRICHIGHSEVERITYINDICATION_MINOR:
		case METRICHIGHSEVERITYINDICATION_WARNING:
		case METRICHIGHSEVERITYINDICATION_CLEARED:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to metricHighSeverityIndication: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->metricHighSeverityIndication for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->metricHighSeverityIndication;
		StorageTmp->metricHighSeverityIndication = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->metricHighSeverityIndication = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_metricSpecificProblemsIndicator(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_metricSpecificProblemsIndicator(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static oid *old_value;
	struct monitorMetricTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	static size_t old_length = 0;
	static oid *objid = NULL;

	DEBUGMSGTL(("metricMIB", "write_metricSpecificProblemsIndicator entering action=%d...  \n", action));
	StorageTmp = header_complex(monitorMetricTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		objid = NULL;
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->monitorMetricRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to metricSpecificProblemsIndicator: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OBJECT_ID) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to metricSpecificProblemsIndicator not ASN_OBJECT_ID\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (MIN_OID_LEN * sizeof(oid) > var_val_len || var_val_len > MAX_OID_LEN * sizeof(oid)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to metricSpecificProblemsIndicator: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((objid = snmp_duplicate_objid((void *) var_val, var_val_len / sizeof(oid))) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->metricSpecificProblemsIndicator for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->metricSpecificProblemsIndicator;
		old_length = StorageTmp->metricSpecificProblemsIndicatorLen;
		StorageTmp->metricSpecificProblemsIndicator = objid;
		StorageTmp->metricSpecificProblemsIndicatorLen = var_val_len / sizeof(oid);
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(old_value);
		old_length = 0;
		objid = NULL;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->metricSpecificProblemsIndicator = old_value;
		StorageTmp->metricSpecificProblemsIndicatorLen = old_length;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		SNMP_FREE(objid);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_derivedGaugeTimeStamp(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_derivedGaugeTimeStamp(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static uint8_t *old_value;
	struct monitorMetricTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	static size_t old_length = 0;
	static uint8_t *string = NULL;

	DEBUGMSGTL(("metricMIB", "write_derivedGaugeTimeStamp entering action=%d...  \n", action));
	StorageTmp = header_complex(monitorMetricTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		string = NULL;
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->monitorMetricRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to derivedGaugeTimeStamp: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to derivedGaugeTimeStamp not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 8..8 11..11 */
		if (var_val_len > SPRINT_MAX_LEN || (var_val_len != 8 && var_val_len != 11)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to derivedGaugeTimeStamp: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->derivedGaugeTimeStamp for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->derivedGaugeTimeStamp;
		old_length = StorageTmp->derivedGaugeTimeStampLen;
		StorageTmp->derivedGaugeTimeStamp = string;
		StorageTmp->derivedGaugeTimeStampLen = var_val_len;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(old_value);
		old_length = 0;
		string = NULL;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->derivedGaugeTimeStamp = old_value;
		StorageTmp->derivedGaugeTimeStampLen = old_length;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		SNMP_FREE(string);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_movingTimePeriod(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_movingTimePeriod(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct meanMonitorTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("metricMIB", "write_movingTimePeriod entering action=%d...  \n", action));
	StorageTmp = header_complex(meanMonitorTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->meanMonitorRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to movingTimePeriod: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_TIMETICKS) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to movingTimePeriod not ASN_TIMETICKS\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to movingTimePeriod: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->movingTimePeriod for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->movingTimePeriod;
		StorageTmp->movingTimePeriod = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->movingTimePeriod = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_estimateOfMeanLowThreshold(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_estimateOfMeanLowThreshold(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct meanMonitorTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("metricMIB", "write_estimateOfMeanLowThreshold entering action=%d...  \n", action));
	StorageTmp = header_complex(meanMonitorTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->meanMonitorRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to estimateOfMeanLowThreshold: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to estimateOfMeanLowThreshold not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to estimateOfMeanLowThreshold: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->estimateOfMeanLowThreshold for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->estimateOfMeanLowThreshold;
		StorageTmp->estimateOfMeanLowThreshold = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->estimateOfMeanLowThreshold = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_estimateOfMeanLowNotify(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_estimateOfMeanLowNotify(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct meanMonitorTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("metricMIB", "write_estimateOfMeanLowNotify entering action=%d...  \n", action));
	StorageTmp = header_complex(meanMonitorTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->meanMonitorRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to estimateOfMeanLowNotify: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to estimateOfMeanLowNotify not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to estimateOfMeanLowNotify: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to estimateOfMeanLowNotify: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->estimateOfMeanLowNotify for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->estimateOfMeanLowNotify;
		StorageTmp->estimateOfMeanLowNotify = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->estimateOfMeanLowNotify = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_estimateOfMeanLowSeverityIndication(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_estimateOfMeanLowSeverityIndication(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct meanMonitorTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("metricMIB", "write_estimateOfMeanLowSeverityIndication entering action=%d...  \n", action));
	StorageTmp = header_complex(meanMonitorTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->meanMonitorRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to estimateOfMeanLowSeverityIndication: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to estimateOfMeanLowSeverityIndication not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to estimateOfMeanLowSeverityIndication: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case ESTIMATEOFMEANLOWSEVERITYINDICATION_INDETERMINATE:
		case ESTIMATEOFMEANLOWSEVERITYINDICATION_CRITICAL:
		case ESTIMATEOFMEANLOWSEVERITYINDICATION_MAJOR:
		case ESTIMATEOFMEANLOWSEVERITYINDICATION_MINOR:
		case ESTIMATEOFMEANLOWSEVERITYINDICATION_WARNING:
		case ESTIMATEOFMEANLOWSEVERITYINDICATION_CLEARED:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to estimateOfMeanLowSeverityIndication: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->estimateOfMeanLowSeverityIndication for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->estimateOfMeanLowSeverityIndication;
		StorageTmp->estimateOfMeanLowSeverityIndication = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->estimateOfMeanLowSeverityIndication = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_estimateOfMeanHighThreshold(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_estimateOfMeanHighThreshold(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct meanMonitorTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("metricMIB", "write_estimateOfMeanHighThreshold entering action=%d...  \n", action));
	StorageTmp = header_complex(meanMonitorTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->meanMonitorRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to estimateOfMeanHighThreshold: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to estimateOfMeanHighThreshold not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to estimateOfMeanHighThreshold: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->estimateOfMeanHighThreshold for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->estimateOfMeanHighThreshold;
		StorageTmp->estimateOfMeanHighThreshold = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->estimateOfMeanHighThreshold = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_estimateOfMeanHighNotify(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_estimateOfMeanHighNotify(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct meanMonitorTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("metricMIB", "write_estimateOfMeanHighNotify entering action=%d...  \n", action));
	StorageTmp = header_complex(meanMonitorTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->meanMonitorRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to estimateOfMeanHighNotify: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to estimateOfMeanHighNotify not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to estimateOfMeanHighNotify: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to estimateOfMeanHighNotify: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->estimateOfMeanHighNotify for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->estimateOfMeanHighNotify;
		StorageTmp->estimateOfMeanHighNotify = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->estimateOfMeanHighNotify = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_estimateOfMeanHighSeverityIndication(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_estimateOfMeanHighSeverityIndication(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct meanMonitorTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("metricMIB", "write_estimateOfMeanHighSeverityIndication entering action=%d...  \n", action));
	StorageTmp = header_complex(meanMonitorTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->meanMonitorRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to estimateOfMeanHighSeverityIndication: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to estimateOfMeanHighSeverityIndication not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to estimateOfMeanHighSeverityIndication: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case ESTIMATEOFMEANHIGHSEVERITYINDICATION_INDETERMINATE:
		case ESTIMATEOFMEANHIGHSEVERITYINDICATION_CRITICAL:
		case ESTIMATEOFMEANHIGHSEVERITYINDICATION_MAJOR:
		case ESTIMATEOFMEANHIGHSEVERITYINDICATION_MINOR:
		case ESTIMATEOFMEANHIGHSEVERITYINDICATION_WARNING:
		case ESTIMATEOFMEANHIGHSEVERITYINDICATION_CLEARED:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to estimateOfMeanHighSeverityIndication: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->estimateOfMeanHighSeverityIndication for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->estimateOfMeanHighSeverityIndication;
		StorageTmp->estimateOfMeanHighSeverityIndication = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->estimateOfMeanHighSeverityIndication = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_estimateOfMeanSpecificProblemsIndicator(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_estimateOfMeanSpecificProblemsIndicator(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static oid *old_value;
	struct meanMonitorTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	static size_t old_length = 0;
	static oid *objid = NULL;

	DEBUGMSGTL(("metricMIB", "write_estimateOfMeanSpecificProblemsIndicator entering action=%d...  \n", action));
	StorageTmp = header_complex(meanMonitorTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		objid = NULL;
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->meanMonitorRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to estimateOfMeanSpecificProblemsIndicator: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OBJECT_ID) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to estimateOfMeanSpecificProblemsIndicator not ASN_OBJECT_ID\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (MIN_OID_LEN * sizeof(oid) > var_val_len || var_val_len > MAX_OID_LEN * sizeof(oid)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to estimateOfMeanSpecificProblemsIndicator: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((objid = snmp_duplicate_objid((void *) var_val, var_val_len / sizeof(oid))) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->estimateOfMeanSpecificProblemsIndicator for you to use, and you have just been asked to do something with it.  Note that 
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->estimateOfMeanSpecificProblemsIndicator;
		old_length = StorageTmp->estimateOfMeanSpecificProblemsIndicatorLen;
		StorageTmp->estimateOfMeanSpecificProblemsIndicator = objid;
		StorageTmp->estimateOfMeanSpecificProblemsIndicatorLen = var_val_len / sizeof(oid);
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(old_value);
		old_length = 0;
		objid = NULL;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->estimateOfMeanSpecificProblemsIndicator = old_value;
		StorageTmp->estimateOfMeanSpecificProblemsIndicatorLen = old_length;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		SNMP_FREE(objid);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_percentileSecondMovingTimePeriod(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_percentileSecondMovingTimePeriod(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct meanAndPercentileMonitorTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("metricMIB", "write_percentileSecondMovingTimePeriod entering action=%d...  \n", action));
	StorageTmp = header_complex(meanAndPercentileMonitorTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->meanAndPercentileMonitorRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to percentileSecondMovingTimePeriod: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_TIMETICKS) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to percentileSecondMovingTimePeriod not ASN_TIMETICKS\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to percentileSecondMovingTimePeriod: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->percentileSecondMovingTimePeriod for you to use, and you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->percentileSecondMovingTimePeriod;
		StorageTmp->percentileSecondMovingTimePeriod = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->percentileSecondMovingTimePeriod = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_numberOfReplications(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_numberOfReplications(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct meanAndPercentileMonitorTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("metricMIB", "write_numberOfReplications entering action=%d...  \n", action));
	StorageTmp = header_complex(meanAndPercentileMonitorTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->meanAndPercentileMonitorRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to numberOfReplications: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to numberOfReplications not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to numberOfReplications: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->numberOfReplications for you to use, and you have just been asked to do something with it.  Note that anything done here 
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->numberOfReplications;
		StorageTmp->numberOfReplications = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->numberOfReplications = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_configurablePercentilePackage(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_configurablePercentilePackage(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct meanAndPercentileMonitorTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("metricMIB", "write_configurablePercentilePackage entering action=%d...  \n", action));
	StorageTmp = header_complex(meanAndPercentileMonitorTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->meanAndPercentileMonitorRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to configurablePercentilePackage: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to configurablePercentilePackage not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to configurablePercentilePackage: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to configurablePercentilePackage: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->configurablePercentilePackage for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->configurablePercentilePackage;
		StorageTmp->configurablePercentilePackage = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->configurablePercentilePackage = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_varianceSecondMovingTimePeriod(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_varianceSecondMovingTimePeriod(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct meanAndVarianceMonitorTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("metricMIB", "write_varianceSecondMovingTimePeriod entering action=%d...  \n", action));
	StorageTmp = header_complex(meanAndVarianceMonitorTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->meanAndVarianceMonitorRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to varianceSecondMovingTimePeriod: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_TIMETICKS) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to varianceSecondMovingTimePeriod not ASN_TIMETICKS\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to varianceSecondMovingTimePeriod: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->varianceSecondMovingTimePeriod for you to use, and you have just been asked to do something with it.  Note that anything 
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->varianceSecondMovingTimePeriod;
		StorageTmp->varianceSecondMovingTimePeriod = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->varianceSecondMovingTimePeriod = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int scannerTable_consistent(struct scannerTable_data *thedata)
 * @param thedata the row data to check for consistency.
 * @brief check the internal consistency of a table row.
 *
 * This function checks the internal consistency of a table row for the scannerTable table.  If the
 * table row is internally consistent, then this function returns SNMP_ERR_NOERROR, otherwise the
 * function returns an SNMP error code and it will not be possible to activate the row until the
 * row's internal consistency is corrected.  This function might use a 'test' operation against the
 * driver to ensure that the commit phase will succeed.
 */
int
scannerTable_consistent(struct scannerTable_data *thedata)
{
	/* XXX: check row consistency return SNMP_ERR_NOERROR if consistent, or an SNMP error code if not. */
	return (SNMP_ERR_NOERROR);
}

/**
 * @fn int monitorMetricTable_consistent(struct monitorMetricTable_data *thedata)
 * @param thedata the row data to check for consistency.
 * @brief check the internal consistency of a table row.
 *
 * This function checks the internal consistency of a table row for the monitorMetricTable table.  If the
 * table row is internally consistent, then this function returns SNMP_ERR_NOERROR, otherwise the
 * function returns an SNMP error code and it will not be possible to activate the row until the
 * row's internal consistency is corrected.  This function might use a 'test' operation against the
 * driver to ensure that the commit phase will succeed.
 */
int
monitorMetricTable_consistent(struct monitorMetricTable_data *thedata)
{
	/* XXX: check row consistency return SNMP_ERR_NOERROR if consistent, or an SNMP error code if not. */
	return (SNMP_ERR_NOERROR);
}

/**
 * @fn int meanMonitorTable_consistent(struct meanMonitorTable_data *thedata)
 * @param thedata the row data to check for consistency.
 * @brief check the internal consistency of a table row.
 *
 * This function checks the internal consistency of a table row for the meanMonitorTable table.  If the
 * table row is internally consistent, then this function returns SNMP_ERR_NOERROR, otherwise the
 * function returns an SNMP error code and it will not be possible to activate the row until the
 * row's internal consistency is corrected.  This function might use a 'test' operation against the
 * driver to ensure that the commit phase will succeed.
 */
int
meanMonitorTable_consistent(struct meanMonitorTable_data *thedata)
{
	/* XXX: check row consistency return SNMP_ERR_NOERROR if consistent, or an SNMP error code if not. */
	return (SNMP_ERR_NOERROR);
}

/**
 * @fn int algorithmIndicatingMeanMonitorTable_consistent(struct algorithmIndicatingMeanMonitorTable_data *thedata)
 * @param thedata the row data to check for consistency.
 * @brief check the internal consistency of a table row.
 *
 * This function checks the internal consistency of a table row for the algorithmIndicatingMeanMonitorTable table.  If the
 * table row is internally consistent, then this function returns SNMP_ERR_NOERROR, otherwise the
 * function returns an SNMP error code and it will not be possible to activate the row until the
 * row's internal consistency is corrected.  This function might use a 'test' operation against the
 * driver to ensure that the commit phase will succeed.
 */
int
algorithmIndicatingMeanMonitorTable_consistent(struct algorithmIndicatingMeanMonitorTable_data *thedata)
{
	/* XXX: check row consistency return SNMP_ERR_NOERROR if consistent, or an SNMP error code if not. */
	return (SNMP_ERR_NOERROR);
}

/**
 * @fn int movingAverageMeanMonitorTable_consistent(struct movingAverageMeanMonitorTable_data *thedata)
 * @param thedata the row data to check for consistency.
 * @brief check the internal consistency of a table row.
 *
 * This function checks the internal consistency of a table row for the movingAverageMeanMonitorTable table.  If the
 * table row is internally consistent, then this function returns SNMP_ERR_NOERROR, otherwise the
 * function returns an SNMP error code and it will not be possible to activate the row until the
 * row's internal consistency is corrected.  This function might use a 'test' operation against the
 * driver to ensure that the commit phase will succeed.
 */
int
movingAverageMeanMonitorTable_consistent(struct movingAverageMeanMonitorTable_data *thedata)
{
	/* XXX: check row consistency return SNMP_ERR_NOERROR if consistent, or an SNMP error code if not. */
	return (SNMP_ERR_NOERROR);
}

/**
 * @fn int meanAndPercentileMonitorTable_consistent(struct meanAndPercentileMonitorTable_data *thedata)
 * @param thedata the row data to check for consistency.
 * @brief check the internal consistency of a table row.
 *
 * This function checks the internal consistency of a table row for the meanAndPercentileMonitorTable table.  If the
 * table row is internally consistent, then this function returns SNMP_ERR_NOERROR, otherwise the
 * function returns an SNMP error code and it will not be possible to activate the row until the
 * row's internal consistency is corrected.  This function might use a 'test' operation against the
 * driver to ensure that the commit phase will succeed.
 */
int
meanAndPercentileMonitorTable_consistent(struct meanAndPercentileMonitorTable_data *thedata)
{
	/* XXX: check row consistency return SNMP_ERR_NOERROR if consistent, or an SNMP error code if not. */
	return (SNMP_ERR_NOERROR);
}

/**
 * @fn int meanAndVarianceMonitorTable_consistent(struct meanAndVarianceMonitorTable_data *thedata)
 * @param thedata the row data to check for consistency.
 * @brief check the internal consistency of a table row.
 *
 * This function checks the internal consistency of a table row for the meanAndVarianceMonitorTable table.  If the
 * table row is internally consistent, then this function returns SNMP_ERR_NOERROR, otherwise the
 * function returns an SNMP error code and it will not be possible to activate the row until the
 * row's internal consistency is corrected.  This function might use a 'test' operation against the
 * driver to ensure that the commit phase will succeed.
 */
int
meanAndVarianceMonitorTable_consistent(struct meanAndVarianceMonitorTable_data *thedata)
{
	/* XXX: check row consistency return SNMP_ERR_NOERROR if consistent, or an SNMP error code if not. */
	return (SNMP_ERR_NOERROR);
}

/**
 * @fn int meanAndMinMaxMonitorTable_consistent(struct meanAndMinMaxMonitorTable_data *thedata)
 * @param thedata the row data to check for consistency.
 * @brief check the internal consistency of a table row.
 *
 * This function checks the internal consistency of a table row for the meanAndMinMaxMonitorTable table.  If the
 * table row is internally consistent, then this function returns SNMP_ERR_NOERROR, otherwise the
 * function returns an SNMP error code and it will not be possible to activate the row until the
 * row's internal consistency is corrected.  This function might use a 'test' operation against the
 * driver to ensure that the commit phase will succeed.
 */
int
meanAndMinMaxMonitorTable_consistent(struct meanAndMinMaxMonitorTable_data *thedata)
{
	/* XXX: check row consistency return SNMP_ERR_NOERROR if consistent, or an SNMP error code if not. */
	return (SNMP_ERR_NOERROR);
}

/**
 * @fn int write_scannerRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_scannerRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct scannerTable_data *StorageTmp = NULL;
	static struct scannerTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 15;
	static int old_value;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("metricMIB", "write_scannerRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(scannerTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to scannerRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to scannerRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->scannerRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->scannerTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_DESTROY:
			/* destroying existent or non-existent row is ok */
			if (StorageTmp == NULL)
				break;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->scannerTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			vars = NULL;
			/* scannerId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[15]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* scannerId */
			/* Note: ranges 1..32 */
			if (vp->val_len > SPRINT_MAX_LEN || ((1 > vp->val_len || vp->val_len > 32))) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index scannerId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = scannerTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			vp = vars;
			memdup((void *) &StorageNew->scannerId, vp->val.string, vp->val_len);
			StorageNew->scannerIdLen = vp->val_len;
			vp = vp->next_variable;
			header_complex_add_data(&scannerTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_DESTROY:
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				scannerTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		}
		break;
	case ACTION:
		/* The variable has been stored in set_value for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = scannerTable_consistent(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			/* row does not have to be consistent */
			break;
		case RS_ACTIVE:
			old_value = StorageTmp->scannerRowStatus;
			StorageTmp->scannerRowStatus = set_value;
			if (old_value != RS_ACTIVE) {
				/* check that activation is possible */
				if ((ret = scannerTable_consistent(StorageTmp)) != SNMP_ERR_NOERROR) {
					StorageTmp->scannerRowStatus = old_value;
					return (ret);
				}
			}
			break;
		case RS_NOTINSERVICE:
			/* set the flag? */
			old_value = StorageTmp->scannerRowStatus;
			StorageTmp->scannerRowStatus = set_value;
			break;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* row creation, set final state */
			/* XXX: commit creation to underlying device */
			/* XXX: activate with underlying device */
			StorageNew->scannerRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			/* row creation, set final state */
			StorageNew->scannerRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (old_value != set_value) {
				switch (set_value) {
				case RS_ACTIVE:
					/* XXX: activate with underlying device */
					break;
				case RS_NOTINSERVICE:
					/* XXX: deactivate with underlying device */
					break;
				}
			}
			break;
		case RS_DESTROY:
			/* row deletion, free it its dead */
			scannerTable_destroy(&StorageDel);
			/* scannerTable_destroy() can handle NULL pointers. */
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* restore state */
			StorageTmp->scannerRowStatus = old_value;
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				scannerTable_del(StorageNew);
				scannerTable_destroy(&StorageNew);
			}
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				scannerTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_monitorMetricRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_monitorMetricRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct monitorMetricTable_data *StorageTmp = NULL;
	static struct monitorMetricTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 15;
	static int old_value;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("metricMIB", "write_monitorMetricRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(monitorMetricTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to monitorMetricRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to monitorMetricRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->monitorMetricRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->monitorMetricTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_DESTROY:
			/* destroying existent or non-existent row is ok */
			if (StorageTmp == NULL)
				break;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->monitorMetricTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			vars = NULL;
			/* scannerId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[15]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* scannerId */
			/* Note: ranges 1..32 */
			if (vp->val_len > SPRINT_MAX_LEN || ((1 > vp->val_len || vp->val_len > 32))) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index scannerId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = monitorMetricTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			vp = vars;
			memdup((void *) &StorageNew->scannerId, vp->val.string, vp->val_len);
			StorageNew->scannerIdLen = vp->val_len;
			vp = vp->next_variable;
			header_complex_add_data(&monitorMetricTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_DESTROY:
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				monitorMetricTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		}
		break;
	case ACTION:
		/* The variable has been stored in set_value for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = monitorMetricTable_consistent(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			/* row does not have to be consistent */
			break;
		case RS_ACTIVE:
			old_value = StorageTmp->monitorMetricRowStatus;
			StorageTmp->monitorMetricRowStatus = set_value;
			if (old_value != RS_ACTIVE) {
				/* check that activation is possible */
				if ((ret = monitorMetricTable_consistent(StorageTmp)) != SNMP_ERR_NOERROR) {
					StorageTmp->monitorMetricRowStatus = old_value;
					return (ret);
				}
			}
			break;
		case RS_NOTINSERVICE:
			/* set the flag? */
			old_value = StorageTmp->monitorMetricRowStatus;
			StorageTmp->monitorMetricRowStatus = set_value;
			break;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* row creation, set final state */
			/* XXX: commit creation to underlying device */
			/* XXX: activate with underlying device */
			StorageNew->monitorMetricRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			/* row creation, set final state */
			StorageNew->monitorMetricRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (old_value != set_value) {
				switch (set_value) {
				case RS_ACTIVE:
					/* XXX: activate with underlying device */
					break;
				case RS_NOTINSERVICE:
					/* XXX: deactivate with underlying device */
					break;
				}
			}
			break;
		case RS_DESTROY:
			/* row deletion, free it its dead */
			monitorMetricTable_destroy(&StorageDel);
			/* monitorMetricTable_destroy() can handle NULL pointers. */
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* restore state */
			StorageTmp->monitorMetricRowStatus = old_value;
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				monitorMetricTable_del(StorageNew);
				monitorMetricTable_destroy(&StorageNew);
			}
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				monitorMetricTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_meanMonitorRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_meanMonitorRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct meanMonitorTable_data *StorageTmp = NULL;
	static struct meanMonitorTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 15;
	static int old_value;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("metricMIB", "write_meanMonitorRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(meanMonitorTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to meanMonitorRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to meanMonitorRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->meanMonitorRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->meanMonitorTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_DESTROY:
			/* destroying existent or non-existent row is ok */
			if (StorageTmp == NULL)
				break;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->meanMonitorTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			vars = NULL;
			/* scannerId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[15]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* scannerId */
			/* Note: ranges 1..32 */
			if (vp->val_len > SPRINT_MAX_LEN || ((1 > vp->val_len || vp->val_len > 32))) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index scannerId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = meanMonitorTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			vp = vars;
			memdup((void *) &StorageNew->scannerId, vp->val.string, vp->val_len);
			StorageNew->scannerIdLen = vp->val_len;
			vp = vp->next_variable;
			header_complex_add_data(&meanMonitorTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_DESTROY:
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				meanMonitorTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		}
		break;
	case ACTION:
		/* The variable has been stored in set_value for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = meanMonitorTable_consistent(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			/* row does not have to be consistent */
			break;
		case RS_ACTIVE:
			old_value = StorageTmp->meanMonitorRowStatus;
			StorageTmp->meanMonitorRowStatus = set_value;
			if (old_value != RS_ACTIVE) {
				/* check that activation is possible */
				if ((ret = meanMonitorTable_consistent(StorageTmp)) != SNMP_ERR_NOERROR) {
					StorageTmp->meanMonitorRowStatus = old_value;
					return (ret);
				}
			}
			break;
		case RS_NOTINSERVICE:
			/* set the flag? */
			old_value = StorageTmp->meanMonitorRowStatus;
			StorageTmp->meanMonitorRowStatus = set_value;
			break;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* row creation, set final state */
			/* XXX: commit creation to underlying device */
			/* XXX: activate with underlying device */
			StorageNew->meanMonitorRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			/* row creation, set final state */
			StorageNew->meanMonitorRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (old_value != set_value) {
				switch (set_value) {
				case RS_ACTIVE:
					/* XXX: activate with underlying device */
					break;
				case RS_NOTINSERVICE:
					/* XXX: deactivate with underlying device */
					break;
				}
			}
			break;
		case RS_DESTROY:
			/* row deletion, free it its dead */
			meanMonitorTable_destroy(&StorageDel);
			/* meanMonitorTable_destroy() can handle NULL pointers. */
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* restore state */
			StorageTmp->meanMonitorRowStatus = old_value;
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				meanMonitorTable_del(StorageNew);
				meanMonitorTable_destroy(&StorageNew);
			}
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				meanMonitorTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_movingAverageMeanMonitorRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_movingAverageMeanMonitorRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct movingAverageMeanMonitorTable_data *StorageTmp = NULL;
	static struct movingAverageMeanMonitorTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 15;
	static int old_value;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("metricMIB", "write_movingAverageMeanMonitorRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(movingAverageMeanMonitorTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to movingAverageMeanMonitorRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to movingAverageMeanMonitorRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->movingAverageMeanMonitorRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->movingAverageMeanMonitorTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_DESTROY:
			/* destroying existent or non-existent row is ok */
			if (StorageTmp == NULL)
				break;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->movingAverageMeanMonitorTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			vars = NULL;
			/* scannerId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[15]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* scannerId */
			/* Note: ranges 1..32 */
			if (vp->val_len > SPRINT_MAX_LEN || ((1 > vp->val_len || vp->val_len > 32))) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index scannerId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = movingAverageMeanMonitorTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			vp = vars;
			memdup((void *) &StorageNew->scannerId, vp->val.string, vp->val_len);
			StorageNew->scannerIdLen = vp->val_len;
			vp = vp->next_variable;
			header_complex_add_data(&movingAverageMeanMonitorTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_DESTROY:
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				movingAverageMeanMonitorTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		}
		break;
	case ACTION:
		/* The variable has been stored in set_value for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = movingAverageMeanMonitorTable_consistent(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			/* row does not have to be consistent */
			break;
		case RS_ACTIVE:
			old_value = StorageTmp->movingAverageMeanMonitorRowStatus;
			StorageTmp->movingAverageMeanMonitorRowStatus = set_value;
			if (old_value != RS_ACTIVE) {
				/* check that activation is possible */
				if ((ret = movingAverageMeanMonitorTable_consistent(StorageTmp)) != SNMP_ERR_NOERROR) {
					StorageTmp->movingAverageMeanMonitorRowStatus = old_value;
					return (ret);
				}
			}
			break;
		case RS_NOTINSERVICE:
			/* set the flag? */
			old_value = StorageTmp->movingAverageMeanMonitorRowStatus;
			StorageTmp->movingAverageMeanMonitorRowStatus = set_value;
			break;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* row creation, set final state */
			/* XXX: commit creation to underlying device */
			/* XXX: activate with underlying device */
			StorageNew->movingAverageMeanMonitorRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			/* row creation, set final state */
			StorageNew->movingAverageMeanMonitorRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (old_value != set_value) {
				switch (set_value) {
				case RS_ACTIVE:
					/* XXX: activate with underlying device */
					break;
				case RS_NOTINSERVICE:
					/* XXX: deactivate with underlying device */
					break;
				}
			}
			break;
		case RS_DESTROY:
			/* row deletion, free it its dead */
			movingAverageMeanMonitorTable_destroy(&StorageDel);
			/* movingAverageMeanMonitorTable_destroy() can handle NULL pointers. */
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* restore state */
			StorageTmp->movingAverageMeanMonitorRowStatus = old_value;
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				movingAverageMeanMonitorTable_del(StorageNew);
				movingAverageMeanMonitorTable_destroy(&StorageNew);
			}
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				movingAverageMeanMonitorTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_meanAndPercentileMonitorRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_meanAndPercentileMonitorRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct meanAndPercentileMonitorTable_data *StorageTmp = NULL;
	static struct meanAndPercentileMonitorTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 15;
	static int old_value;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("metricMIB", "write_meanAndPercentileMonitorRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(meanAndPercentileMonitorTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to meanAndPercentileMonitorRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to meanAndPercentileMonitorRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->meanAndPercentileMonitorRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->meanAndPercentileMonitorTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_DESTROY:
			/* destroying existent or non-existent row is ok */
			if (StorageTmp == NULL)
				break;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->meanAndPercentileMonitorTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			vars = NULL;
			/* scannerId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[15]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* scannerId */
			/* Note: ranges 1..32 */
			if (vp->val_len > SPRINT_MAX_LEN || ((1 > vp->val_len || vp->val_len > 32))) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index scannerId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = meanAndPercentileMonitorTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			vp = vars;
			memdup((void *) &StorageNew->scannerId, vp->val.string, vp->val_len);
			StorageNew->scannerIdLen = vp->val_len;
			vp = vp->next_variable;
			header_complex_add_data(&meanAndPercentileMonitorTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_DESTROY:
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				meanAndPercentileMonitorTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		}
		break;
	case ACTION:
		/* The variable has been stored in set_value for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = meanAndPercentileMonitorTable_consistent(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			/* row does not have to be consistent */
			break;
		case RS_ACTIVE:
			old_value = StorageTmp->meanAndPercentileMonitorRowStatus;
			StorageTmp->meanAndPercentileMonitorRowStatus = set_value;
			if (old_value != RS_ACTIVE) {
				/* check that activation is possible */
				if ((ret = meanAndPercentileMonitorTable_consistent(StorageTmp)) != SNMP_ERR_NOERROR) {
					StorageTmp->meanAndPercentileMonitorRowStatus = old_value;
					return (ret);
				}
			}
			break;
		case RS_NOTINSERVICE:
			/* set the flag? */
			old_value = StorageTmp->meanAndPercentileMonitorRowStatus;
			StorageTmp->meanAndPercentileMonitorRowStatus = set_value;
			break;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* row creation, set final state */
			/* XXX: commit creation to underlying device */
			/* XXX: activate with underlying device */
			StorageNew->meanAndPercentileMonitorRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			/* row creation, set final state */
			StorageNew->meanAndPercentileMonitorRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (old_value != set_value) {
				switch (set_value) {
				case RS_ACTIVE:
					/* XXX: activate with underlying device */
					break;
				case RS_NOTINSERVICE:
					/* XXX: deactivate with underlying device */
					break;
				}
			}
			break;
		case RS_DESTROY:
			/* row deletion, free it its dead */
			meanAndPercentileMonitorTable_destroy(&StorageDel);
			/* meanAndPercentileMonitorTable_destroy() can handle NULL pointers. */
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* restore state */
			StorageTmp->meanAndPercentileMonitorRowStatus = old_value;
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				meanAndPercentileMonitorTable_del(StorageNew);
				meanAndPercentileMonitorTable_destroy(&StorageNew);
			}
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				meanAndPercentileMonitorTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_meanAndVarianceMonitorRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_meanAndVarianceMonitorRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct meanAndVarianceMonitorTable_data *StorageTmp = NULL;
	static struct meanAndVarianceMonitorTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 15;
	static int old_value;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("metricMIB", "write_meanAndVarianceMonitorRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(meanAndVarianceMonitorTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to meanAndVarianceMonitorRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to meanAndVarianceMonitorRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->meanAndVarianceMonitorRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->meanAndVarianceMonitorTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_DESTROY:
			/* destroying existent or non-existent row is ok */
			if (StorageTmp == NULL)
				break;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->meanAndVarianceMonitorTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			vars = NULL;
			/* scannerId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[15]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* scannerId */
			/* Note: ranges 1..32 */
			if (vp->val_len > SPRINT_MAX_LEN || ((1 > vp->val_len || vp->val_len > 32))) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index scannerId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = meanAndVarianceMonitorTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			vp = vars;
			memdup((void *) &StorageNew->scannerId, vp->val.string, vp->val_len);
			StorageNew->scannerIdLen = vp->val_len;
			vp = vp->next_variable;
			header_complex_add_data(&meanAndVarianceMonitorTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_DESTROY:
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				meanAndVarianceMonitorTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		}
		break;
	case ACTION:
		/* The variable has been stored in set_value for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = meanAndVarianceMonitorTable_consistent(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			/* row does not have to be consistent */
			break;
		case RS_ACTIVE:
			old_value = StorageTmp->meanAndVarianceMonitorRowStatus;
			StorageTmp->meanAndVarianceMonitorRowStatus = set_value;
			if (old_value != RS_ACTIVE) {
				/* check that activation is possible */
				if ((ret = meanAndVarianceMonitorTable_consistent(StorageTmp)) != SNMP_ERR_NOERROR) {
					StorageTmp->meanAndVarianceMonitorRowStatus = old_value;
					return (ret);
				}
			}
			break;
		case RS_NOTINSERVICE:
			/* set the flag? */
			old_value = StorageTmp->meanAndVarianceMonitorRowStatus;
			StorageTmp->meanAndVarianceMonitorRowStatus = set_value;
			break;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* row creation, set final state */
			/* XXX: commit creation to underlying device */
			/* XXX: activate with underlying device */
			StorageNew->meanAndVarianceMonitorRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			/* row creation, set final state */
			StorageNew->meanAndVarianceMonitorRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (old_value != set_value) {
				switch (set_value) {
				case RS_ACTIVE:
					/* XXX: activate with underlying device */
					break;
				case RS_NOTINSERVICE:
					/* XXX: deactivate with underlying device */
					break;
				}
			}
			break;
		case RS_DESTROY:
			/* row deletion, free it its dead */
			meanAndVarianceMonitorTable_destroy(&StorageDel);
			/* meanAndVarianceMonitorTable_destroy() can handle NULL pointers. */
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* restore state */
			StorageTmp->meanAndVarianceMonitorRowStatus = old_value;
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				meanAndVarianceMonitorTable_del(StorageNew);
				meanAndVarianceMonitorTable_destroy(&StorageNew);
			}
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				meanAndVarianceMonitorTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_meanAndMinMaxMonitorRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_meanAndMinMaxMonitorRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct meanAndMinMaxMonitorTable_data *StorageTmp = NULL;
	static struct meanAndMinMaxMonitorTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 15;
	static int old_value;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("metricMIB", "write_meanAndMinMaxMonitorRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(meanAndMinMaxMonitorTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to meanAndMinMaxMonitorRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to meanAndMinMaxMonitorRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->meanAndMinMaxMonitorRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->meanAndMinMaxMonitorTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_DESTROY:
			/* destroying existent or non-existent row is ok */
			if (StorageTmp == NULL)
				break;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->meanAndMinMaxMonitorTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			vars = NULL;
			/* scannerId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[15]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* scannerId */
			/* Note: ranges 1..32 */
			if (vp->val_len > SPRINT_MAX_LEN || ((1 > vp->val_len || vp->val_len > 32))) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index scannerId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = meanAndMinMaxMonitorTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			vp = vars;
			memdup((void *) &StorageNew->scannerId, vp->val.string, vp->val_len);
			StorageNew->scannerIdLen = vp->val_len;
			vp = vp->next_variable;
			header_complex_add_data(&meanAndMinMaxMonitorTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_DESTROY:
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				meanAndMinMaxMonitorTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		}
		break;
	case ACTION:
		/* The variable has been stored in set_value for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = meanAndMinMaxMonitorTable_consistent(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			/* row does not have to be consistent */
			break;
		case RS_ACTIVE:
			old_value = StorageTmp->meanAndMinMaxMonitorRowStatus;
			StorageTmp->meanAndMinMaxMonitorRowStatus = set_value;
			if (old_value != RS_ACTIVE) {
				/* check that activation is possible */
				if ((ret = meanAndMinMaxMonitorTable_consistent(StorageTmp)) != SNMP_ERR_NOERROR) {
					StorageTmp->meanAndMinMaxMonitorRowStatus = old_value;
					return (ret);
				}
			}
			break;
		case RS_NOTINSERVICE:
			/* set the flag? */
			old_value = StorageTmp->meanAndMinMaxMonitorRowStatus;
			StorageTmp->meanAndMinMaxMonitorRowStatus = set_value;
			break;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* row creation, set final state */
			/* XXX: commit creation to underlying device */
			/* XXX: activate with underlying device */
			StorageNew->meanAndMinMaxMonitorRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			/* row creation, set final state */
			StorageNew->meanAndMinMaxMonitorRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (old_value != set_value) {
				switch (set_value) {
				case RS_ACTIVE:
					/* XXX: activate with underlying device */
					break;
				case RS_NOTINSERVICE:
					/* XXX: deactivate with underlying device */
					break;
				}
			}
			break;
		case RS_DESTROY:
			/* row deletion, free it its dead */
			meanAndMinMaxMonitorTable_destroy(&StorageDel);
			/* meanAndMinMaxMonitorTable_destroy() can handle NULL pointers. */
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* restore state */
			StorageTmp->meanAndMinMaxMonitorRowStatus = old_value;
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				meanAndMinMaxMonitorTable_del(StorageNew);
				meanAndMinMaxMonitorTable_destroy(&StorageNew);
			}
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				meanAndMinMaxMonitorTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn void metricMIB_loop_handler(int dummy)
 * @param sig signal number
 * @brief handle event loop interation.
 *
 * This function is registered so that, when operating as a module, snmpd will call it one per event
 * loop interation.  This function is called before the next requst is processed and after the
 * previous request is processed.  Two things are done here:  1) The file descriptor that is used to
 * synchronize the agent with (pseudo-)device drivers is closed.  (Another approach, instead of
 * closing each time, would be to restart a timer each time that a request is made (loop is
 * performed) and if it expires, close the file descriptor).  2) The request number is incremented.
 * Although a request is not generated for each loop of the snmp event loop, it is true that a new
 * request cannot be generated without performing a loop.  Therefore, the sa_request is not the
 * request number but it is a temporally unique identifier for a request.
 */
void
metricMIB_loop_handler(int sig)
{
	DEBUGMSGTL(("metricMIB", "metricMIB_loop_handler: executing loop handler...  "));
	/* close files after each request */
	if (sa_fclose) {
		if (my_fd >= 0) {
			close(my_fd);
			my_fd = -1;
		}
	}
#if defined MASTER
	/* prepare for next request */
	sa_request++;
#endif				/* defined MASTER */
	if (external_signal_scheduled[sig] == 0)
		external_signal_scheduled[sig]--;
	if (metricMIBold_signal_handler != NULL)
		(*metricMIBold_signal_handler) (sig);
	DEBUGMSGTL(("metricMIB", "done.\n"));
}

/**
 * @fn void metricMIB_fd_handler(int fd, void *dummy)
 * @param fd file descriptor to read.
 * @param dummy client data passed to registration function (always NULL).
 * @brief handle read event on file descriptor.
 *
 * This read file descriptor handler is normally used for (pseudo-)device drivers that generate
 * statistical collection interval events, alarm events, or other operational measurement events, by
 * placing a message on the read queue of the "event handling" Stream.  Normally this routine
 * would adjust counts in some table or scalars, generate SNMP traps representing on-occurence
 * events, first and interval events, and alarm indications.
 */
void
metricMIB_fd_handler(int fd, void *dummy)
{
	DEBUGMSGTL(("metricMIB", "metricMIB_fd_handler: executing fd handler...  "));
	/* XXX: place actions to handle my_fd here... */
	DEBUGMSGTL(("metricMIB", "done.\n"));
	return;
}
