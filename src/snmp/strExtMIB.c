/*****************************************************************************

 @(#) $RCSfile: strExtMIB.c,v $ $Name:  $($Revision: 1.1.2.1 $) $Date: 2009-06-21 11:42:35 $

 -----------------------------------------------------------------------------

 Copyright (c) 2008-2009  Monavacon Limited <http://www.monavacon.com/>
 Copyright (c) 2001-2008  OpenSS7 Corporation <http://www.openss7.com/>
 Copyright (c) 1997-2001  Brian F. G. Bidulock <bidulock@openss7.org>

 All Rights Reserved.

 This program is free software: you can redistribute it and/or modify it under
 the terms of the GNU Affero General Public License as published by the Free
 Software Foundation, version 3 of the license.

 This program is distributed in the hope that it will be useful, but WITHOUT
 ANY WARRANTY; without even the implied warranty of MERCHANTABILITY or FITNESS
 FOR A PARTICULAR PURPOSE.  See the GNU Affero General Public License for more
 details.

 You should have received a copy of the GNU Affero General Public License
 along with this program.  If not, see <http://www.gnu.org/licenses/>, or
 write to the Free Software Foundation, Inc., 675 Mass Ave, Cambridge, MA
 02139, USA.

 -----------------------------------------------------------------------------

 U.S. GOVERNMENT RESTRICTED RIGHTS.  If you are licensing this Software on
 behalf of the U.S. Government ("Government"), the following provisions apply
 to you.  If the Software is supplied by the Department of Defense ("DoD"), it
 is classified as "Commercial Computer Software" under paragraph 252.227-7014
 of the DoD Supplement to the Federal Acquisition Regulations ("DFARS") (or any
 successor regulations) and the Government is acquiring only the license rights
 granted herein (the license rights customarily provided to non-Government
 users).  If the Software is supplied to any unit or agency of the Government
 other than DoD, it is classified as "Restricted Computer Software" and the
 Government's rights in the Software are defined in paragraph 52.227-19 of the
 Federal Acquisition Regulations ("FAR") (or any successor regulations) or, in
 the cases of NASA, in paragraph 18.52.227-86 of the NASA Supplement to the FAR
 (or any successor regulations).

 -----------------------------------------------------------------------------

 Commercial licensing and support of this software is available from OpenSS7
 Corporation at a fee.  See http://www.openss7.com/

 -----------------------------------------------------------------------------

 Last Modified $Date: 2009-06-21 11:42:35 $ by $Author: brian $

 -----------------------------------------------------------------------------

 $Log: strExtMIB.c,v $
 Revision 1.1.2.1  2009-06-21 11:42:35  brian
 - added files to new distro

 Revision 0.9.2.12  2009-02-18 20:19:01  brian
 - updated agents

 Revision 0.9.2.11  2009-01-19 10:39:41  brian
 - added new standalone agent

 Revision 0.9.2.10  2009-01-16 20:25:49  brian
 - working up streams mibs and agents

 Revision 0.9.2.9  2009-01-14 14:36:18  brian
 - latest script updates

 Revision 0.9.2.8  2009-01-14 14:32:00  brian
 - working up agents

 Revision 0.9.2.7  2009-01-10 17:19:50  brian
 - updated agents

 Revision 0.9.2.6  2009-01-04 13:16:13  brian
 - updated agents

 Revision 0.9.2.5  2009-01-03 10:51:13  brian
 - updated agent

 Revision 0.9.2.4  2009-01-02 15:46:46  brian
 - corrections

 Revision 0.9.2.3  2009-01-02 15:04:19  brian
 - updated agents

 Revision 0.9.2.2  2008-12-31 16:04:52  brian
 - updated mibs

 Revision 0.9.2.1  2008-12-27 15:36:41  brian
 - added extention mib

 *****************************************************************************/

#ident "@(#) $RCSfile: strExtMIB.c,v $ $Name:  $($Revision: 1.1.2.1 $) $Date: 2009-06-21 11:42:35 $"

static char const ident[] = "$RCSfile: strExtMIB.c,v $ $Name:  $($Revision: 1.1.2.1 $) $Date: 2009-06-21 11:42:35 $";

/* This file was generated by mib2c and is intended for use as
   a mib module for the ucd-snmp snmpd agent. */
#include <ucd-snmp/ucd-snmp-config.h>
#include <ucd-snmp/ucd-snmp-includes.h>
#include <ucd-snmp/ucd-snmp-agent-includes.h>
#include <ucd-snmp/agent_trap.h>
#include <ucd-snmp/callback.h>
#include <ucd-snmp/snmp-tc.h>
#include <ucd-snmp/default_store.h>
#include <ucd-snmp/snmp_alarm.h>
/* The following header files are mangled in most recent net-snmp releases so
 * the versions from UCD-SNMP 4.2.5 are included here.  */
#if defined HAVE_LIBNETSNMP
#else				/* defined HAVE_LIBNETSNMP */
#endif				/* defined HAVE_LIBNETSNMP */
/* These are messed up on both. */
#include "ds_agent.h"
#ifdef HAVE_UCD_SNMP_UTIL_FUNCS_H
#include <ucd-snmp/util_funcs.h>
/* Many recent net-snmp UCD compatible headers do not declare header_generic. */
int header_generic(struct variable *, oid *, size_t *, int, size_t *, WriteMethod **);
#else				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#include "util_funcs.h"
#endif				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#ifdef HAVE_UCD_SNMP_HEADER_COMPLEX_H
#include <ucd-snmp/header_complex.h>
#else				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
#include "header_complex.h"
#endif				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
/* This one is the other way around: it is just fine for net-snmp, but
 * ucd-snmp does not provide the header file at all.  */
#ifdef HAVE_UCD_SNMP_MIB_MODULES_H
#include <ucd-snmp/mib_modules.h>
#else				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#ifdef HAVE_NET_SNMP_AGENT_MIB_MODULES_H
#include <net-snmp/agent/mib_modules.h>
#else				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#include "mib_modules.h"
#endif				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#endif				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#include <stdint.h>
#include <signal.h>
#include <sys/stat.h>		/* for struct stat, fstat() */
#include <sys/types.h>
#include <unistd.h>
#include <stdlib.h>
#include <pwd.h>		/* for getpwuid() getpwnam() */
#include <grp.h>		/* for getgrgid() getgrnam() */
#include <libgen.h>		/* for basename() */
#include <fcntl.h>		/* for O_CREAT */
#include <sys/sysctl.h>		/* for sysctl */
#include <errno.h>
#include <string.h>
#ifdef _GNU_SOURCE
#include <getopt.h>
#endif
#include <stddef.h>
#include <stropts.h>
#include <stdio.h>
#include <ctype.h>
#include <strlog.h>
#include <syslog.h>
#include <sys/time.h>
#include <time.h>
#include <sys/utsname.h>
#include <sys/strlog.h>
#include "strExtMIB.h"
#undef MASTER
#define MY_FACILITY(__pri)	(LOG_DAEMON|(__pri))
#if defined MODULE
#if defined MASTER
const char sa_program[] = "strExtMIB";
int sa_fclose = 1;			/* default close files between requests */
int sa_changed = 1;			/* indication to reread MIB configuration */
int sa_stats_refresh = 1;		/* indications that statistics, the mib or its tables need to be refreshed */
int sa_request = 1;			/* request number for per-request actions */
int sa_dump = 0;			/* default packet dump */
int sa_debug = 0;			/* default no debug */
#endif				/* defined MASTER */
#endif				/* defined MODULE */
static int my_fd = -1;			/* file descriptor for this MIB's use */
static int my_readfd = -1;		/* file descriptor for autonomnous events */
static int my_fd_console = -1;		/* console logger file descriptor */
static int my_fd_error = -1;		/* error logger file descriptor */
static int my_fd_trace = -1;		/* trace logger file descriptor */
volatile int strExtMIB_refresh = 1;
volatile int strExtStrlogRecordTable_refresh = 1;
volatile int strExtTraceTable_refresh = 1;

void strExtConsoleLog_fd_handler(int, void *);
static int strExtConsoleLog_start(void);
static int strExtConsoleLog_stop(void);
void strExtErrorLog_fd_handler(int, void *);
static int strExtErrorLog_start(void);
static int strExtErrorLog_stop(void);
void strExtTraceLog_fd_handler(int, void *);
static int strExtTraceLog_start(void);
static int strExtTraceLog_stop(void);

/*
 * strExtMIB_variables_oid: object identifier for strExtMIB
 * This is the top level oid that we want to register under.  This is essentially a prefix, with the
 * suffix appearing in the variable below.
 */
oid strExtMIB_variables_oid[9] = { 1, 3, 6, 1, 4, 1, 29591, 1, 1000 };
oid strExtStrlogRecordTable_variables_oid[14] = { 1, 3, 6, 1, 4, 1, 29591, 1, 1000, 1, 1, 1, 2, 1 };
oid strExtTraceTable_variables_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 1, 1000, 1, 1, 2, 1 };

/*
 * Oids for use in notifications defined in this MIB.
 */
oid strStrlogRecord_oid[11] = { 1, 3, 6, 1, 4, 1, 29591, 1, 1000, 0, 1 };

/*
 * Oids accessible only for notify defined in this MIB.
 */
oid strExtStrlogIndex_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 1, 1000, 1, 3, 1, 1 };
oid strExtStrlogSeqNo_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 1, 1000, 1, 3, 1, 2 };
oid strExtStrlogDateAndTime_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 1, 1000, 1, 3, 1, 3 };
oid strExtStrlogTimeStamp_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 1, 1000, 1, 3, 1, 4 };
oid strExtStrlogMid_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 1, 1000, 1, 3, 1, 5 };
oid strExtStrlogSid_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 1, 1000, 1, 3, 1, 6 };
oid strExtStrlogLevel_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 1, 1000, 1, 3, 1, 7 };
oid strExtStrlogFlags_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 1, 1000, 1, 3, 1, 8 };
oid strExtStrlogFmtString_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 1, 1000, 1, 3, 1, 9 };
oid strExtStrlogInteger_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 1, 1000, 1, 3, 1, 10 };
oid strExtStrlogUnsigned_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 1, 1000, 1, 3, 1, 11 };
oid strExtStrlogString_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 1, 1000, 1, 3, 1, 12 };
oid strExtStrlogInteger64_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 1, 1000, 1, 3, 1, 13 };
oid strExtStrlogUnsigned64_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 1, 1000, 1, 3, 1, 14 };
oid strExtStrlogInteger128_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 1, 1000, 1, 3, 1, 15 };
oid strExtStrlogUnsigned128_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 1, 1000, 1, 3, 1, 16 };

/*
 * Other oids defined in this MIB.
 */

static oid zeroDotZero_oid[2] = { 0, 0 };
static oid snmpTrapOID_oid[11] = { 1, 3, 6, 1, 6, 3, 1, 1, 4, 1, 0 };

/*
 * variable7 strExtMIB_variables: tree for strExtMIB
 * This variable defines function callbacks and type return information for the strExtMIB mib section
 */
struct variable7 strExtMIB_variables[] = {
	/* magic number, variable type, ro/rw, callback fn, L, oidsuffix */
#define   STREXTSTRLOGRECORDNEXTINDEX  1
	{(u_char) STREXTSTRLOGRECORDNEXTINDEX, ASN_UNSIGNED, RONLY, var_strExtMIB, 4, {1, 1, 1, 1}},
#define   STREXTSTRLOGRECORDSEQNO  2
	{(u_char) STREXTSTRLOGRECORDSEQNO, ASN_INTEGER, RONLY, var_strExtStrlogRecordTable, 6, {1, 1, 1, 2, 1, 2}},
#define   STREXTSTRLOGRECORDDATEANDTIME  3
	{(u_char) STREXTSTRLOGRECORDDATEANDTIME, ASN_OCTET_STR, RONLY, var_strExtStrlogRecordTable, 6, {1, 1, 1, 2, 1, 3}},
#define   STREXTSTRLOGRECORDTIMESTAMP  4
	{(u_char) STREXTSTRLOGRECORDTIMESTAMP, ASN_TIMETICKS, RONLY, var_strExtStrlogRecordTable, 6, {1, 1, 1, 2, 1, 4}},
#define   STREXTSTRLOGRECORDMID  5
	{(u_char) STREXTSTRLOGRECORDMID, ASN_UNSIGNED, RWRITE, var_strExtStrlogRecordTable, 6, {1, 1, 1, 2, 1, 5}},
#define   STREXTSTRLOGRECORDSID  6
	{(u_char) STREXTSTRLOGRECORDSID, ASN_UNSIGNED, RWRITE, var_strExtStrlogRecordTable, 6, {1, 1, 1, 2, 1, 6}},
#define   STREXTSTRLOGRECORDLEVEL  7
	{(u_char) STREXTSTRLOGRECORDLEVEL, ASN_UNSIGNED, RWRITE, var_strExtStrlogRecordTable, 6, {1, 1, 1, 2, 1, 7}},
#define   STREXTSTRLOGRECORDFLAGS  8
	{(u_char) STREXTSTRLOGRECORDFLAGS, ASN_OCTET_STR, RWRITE, var_strExtStrlogRecordTable, 6, {1, 1, 1, 2, 1, 8}},
#define   STREXTSTRLOGRECORDMSGSTRING  9
	{(u_char) STREXTSTRLOGRECORDMSGSTRING, ASN_OCTET_STR, RWRITE, var_strExtStrlogRecordTable, 6, {1, 1, 1, 2, 1, 9}},
#define   STREXTSTRLOGRECORDROWSTATUS  10
	{(u_char) STREXTSTRLOGRECORDROWSTATUS, ASN_INTEGER, RWRITE, var_strExtStrlogRecordTable, 6, {1, 1, 1, 2, 1, 10}},
#define   STREXTTRACEMID        11
	{(u_char) STREXTTRACEMID, ASN_INTEGER, RWRITE, var_strExtTraceTable, 5, {1, 1, 2, 1, 2}},
#define   STREXTTRACESID        12
	{(u_char) STREXTTRACESID, ASN_INTEGER, RWRITE, var_strExtTraceTable, 5, {1, 1, 2, 1, 3}},
#define   STREXTTRACELEVEL      13
	{(u_char) STREXTTRACELEVEL, ASN_INTEGER, RWRITE, var_strExtTraceTable, 5, {1, 1, 2, 1, 4}},
#define   STREXTTRACEROWSTATUS  14
	{(u_char) STREXTTRACEROWSTATUS, ASN_INTEGER, RWRITE, var_strExtTraceTable, 5, {1, 1, 2, 1, 5}},
#define   STRNLOGARGS           15
	{(u_char) STRNLOGARGS, ASN_UNSIGNED, RWRITE, var_strExtMIB, 3, {1, 2, 1}},
#define   STREXTLOGMSGSIZE      16
	{(u_char) STREXTLOGMSGSIZE, ASN_UNSIGNED, RWRITE, var_strExtMIB, 3, {1, 2, 2}},
#define   STREXTCONSOLELOG      17
	{(u_char) STREXTCONSOLELOG, ASN_INTEGER, RWRITE, var_strExtMIB, 3, {1, 2, 3}},
#define   STREXTERRORLOG        18
	{(u_char) STREXTERRORLOG, ASN_INTEGER, RWRITE, var_strExtMIB, 3, {1, 2, 4}},
#define   STREXTTRACELOG        19
	{(u_char) STREXTTRACELOG, ASN_INTEGER, RWRITE, var_strExtMIB, 3, {1, 2, 5}},
};

/* (L = length of the oidsuffix) */
struct strExtMIB_data *strExtMIBStorage = NULL;

/* global storage of our data, saved in and configured by header_complex() */
struct header_complex_index *strExtStrlogRecordTableStorage = NULL;
struct header_complex_index *strExtTraceTableStorage = NULL;

void (*strExtMIBold_signal_handler) (int) = NULL;	/* save old signal handler just in case */
void strExtMIB_loop_handler(int);
void strExtMIB_fd_handler(int, void *);

/**
 * @fn void init_strExtMIB(void)
 * @brief strExtMIB initialization routine.
 *
 * This is called when the agent starts up.  At a minimum, registration of the MIB variables
 * structure (strExtMIB_variables) should take place here.  By default the function also
 * registers the configuration handler and configuration store callbacks.
 *
 * Additional registrations that may be considered here are calls to regsiter_readfd(),
 * register_writefd() and register_exceptfd() for hooking into the snmpd event loop, but only when
 * used as a loadable module.  By default this function establishes a single file descriptor to
 * read, or upon which to handle exceptions.  Note that the snmpd only supports a maximum of 32
 * extneral file descriptors, so these should be used sparingly.
 *
 * When running as a loadable module, it is also necessary to hook into the snmpd event loop so that
 * the current request number can be deteremined.  This is accomplished by using a trick of the
 * external_signal_scheduled and external_signal_handler mechanism which is called on each event
 * loop when external_signal_scheduled is non-zero.  This is used to increment the sa_request value
 * on each snmpd event loop interation so that calls to MIB tree functions can determine whether
 * they belong to a fresh request or not (primarily for cacheing and possibly to clean up non-polled
 * file descriptors).
 */
void
init_strExtMIB(void)
{
	(void) my_fd;
	(void) zeroDotZero_oid;
	(void) snmpTrapOID_oid;
	DEBUGMSGTL(("strExtMIB", "init_strExtMIB: initializing...  "));
	/* register ourselves with the agent to handle our mib tree */
	REGISTER_MIB("strExtMIB", strExtMIB_variables, variable7, strExtMIB_variables_oid);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_SHUTDOWN, term_strExtMIB, NULL);
	/* register our config handler(s) to deal with registrations */
	snmpd_register_config_handler("strExtMIB", parse_strExtMIB, NULL, "HELP STRING");
	snmpd_register_config_handler("strExtStrlogRecordTable", parse_strExtStrlogRecordTable, NULL, "HELP STRING");
	snmpd_register_config_handler("strExtTraceTable", parse_strExtTraceTable, NULL, "HELP STRING");

	/* we need to be called back later to store our data */
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_strExtMIB, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_strExtStrlogRecordTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_strExtTraceTable, NULL);

	/* place any other initialization junk you need here */
	if (my_readfd >= 0) {
		register_readfd(my_readfd, strExtMIB_fd_handler, (void *) 0);
		register_exceptfd(my_readfd, strExtMIB_fd_handler, (void *) 1);
	}
	strExtMIBold_signal_handler = external_signal_handler[SIGCHLD];
	external_signal_handler[SIGCHLD] = &strExtMIB_loop_handler;
	external_signal_scheduled[SIGCHLD] = 1;
	DEBUGMSGTL(("strExtMIB", "done.\n"));
}

/**
 * @fn void deinit_strExtMIB(void)
 * @brief deinitialization routine.
 *
 * This is called before the agent is unloaded.  At a minimum, deregistration of the MIB variables
 * structure (strExtMIB_variables) should take place here.  By default, the function also
 * deregisters the the configuration file handlers for the MIB variables and table rows.
 *
 * Additional deregistrations that may be required here are calls to unregister_readfd(),
 * unregister_writefd() and unregsiter_exceptfd() for unhooking from the snmpd event loop, but only
 * when used as a loadable module.  By default if a read file descriptor exists, it is unregistered.
 */
void
deinit_strExtMIB(void)
{
	DEBUGMSGTL(("strExtMIB", "deinit_strExtMIB: deinitializating...  "));
	external_signal_handler[SIGCHLD] = strExtMIBold_signal_handler;
	if (my_readfd >= 0) {
		unregister_exceptfd(my_readfd);
		unregister_readfd(my_readfd);
		close(my_readfd);
		my_readfd = -1;
	}
	unregister_mib(strExtMIB_variables_oid, sizeof(strExtMIB_variables_oid) / sizeof(oid));
	snmpd_unregister_config_handler("strExtMIB");
	snmpd_unregister_config_handler("strExtStrlogRecordTable");
	snmpd_unregister_config_handler("strExtTraceTable");

	/* place any other de-initialization junk you need here */
	DEBUGMSGTL(("strExtMIB", "done.\n"));
}

int
term_strExtMIB(int majorID, int minorID, void *serverarg, void *clientarg)
{
	DEBUGMSGTL(("strExtMIB", "term_strExtMIB: terminating...  "));
	deinit_strExtMIB();
	DEBUGMSGTL(("strExtMIB", "done.\n"));
	return 0;
}

/**
 * @fn struct strExtMIB_data *strExtMIB_create(void)
 * @brief create a fresh data structure representing scalars in strExtMIB.
 *
 * Creates a new strExtMIB_data structure by allocating dynamic memory for the structure and
 * initializing the default values of scalars in strExtMIB.
 */
struct strExtMIB_data *
strExtMIB_create(void)
{
	struct strExtMIB_data *StorageNew = SNMP_MALLOC_STRUCT(strExtMIB_data);

	DEBUGMSGTL(("strExtMIB", "strExtMIB_create: creating scalars...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default scalar values here into StorageNew */
		StorageNew->strExtStrlogRecordNextIndex = 0;
		StorageNew->strNlogargs = 20;
		StorageNew->strExtLogMsgSize = 1024;
		StorageNew->strExtConsoleLog = TV_FALSE;
		StorageNew->strExtErrorLog = TV_FALSE;
		StorageNew->strExtTraceLog = TV_FALSE;

	}
	DEBUGMSGTL(("strExtMIB", "done.\n"));
	return (StorageNew);
}

/**
 * @fn int strExtMIB_destroy(struct strExtMIB_data **thedata)
 * @param thedata pointer to the data structure in strExtMIB.
 * @brief delete a scalars structure from strExtMIB.
 *
 * Frees scalars that were previously removed from strExtMIB.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
strExtMIB_destroy(struct strExtMIB_data **thedata)
{
	struct strExtMIB_data *StorageDel;

	DEBUGMSGTL(("strExtMIB", "strExtMIB_destroy: deleting scalars...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("strExtMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int strExtMIB_add(struct strExtMIB_data *thedata)
 * @param thedata the structure representing strExtMIB scalars.
 * @brief adds node to the strExtMIB scalar data set.
 *
 * Adds a scalar structure to the strExtMIB data set.  Note that this function is necessary even
 * when the scalar values are not peristent.
 */
int
strExtMIB_add(struct strExtMIB_data *thedata)
{
	DEBUGMSGTL(("strExtMIB", "strExtMIB_add: adding data...  "));
	if (thedata)
		strExtMIBStorage = thedata;
	DEBUGMSGTL(("strExtMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_strExtMIB(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for strExtMIB entries.
 *
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case strExtMIB).  This routine is invoked by
 * UCD-SNMP to read the values of scalars in the MIB from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the MIB.  If there are no configured entries
 * in the configuration MIB, this function will simply not be called.
 */
void
parse_strExtMIB(const char *token, char *line)
{
	size_t tmpsize;
	struct strExtMIB_data *StorageTmp = strExtMIB_create();

	DEBUGMSGTL(("strExtMIB", "parse_strExtMIB: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual scalars that are not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->strExtStrlogRecordNextIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->strNlogargs, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->strExtLogMsgSize, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->strExtConsoleLog, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->strExtErrorLog, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->strExtTraceLog, &tmpsize);
	strExtMIB_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("strExtMIB", "done.\n"));
}

/*
 * store_strExtMIB(): stores .conf file entries needed to configure the mib.
 */
int
store_strExtMIB(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct strExtMIB_data *StorageTmp;

	DEBUGMSGTL(("strExtMIB", "store_strExtMIB: storing data...  "));
	refresh_strExtMIB(1);
	if ((StorageTmp = strExtMIBStorage) == NULL) {
		DEBUGMSGTL(("strExtMIB", "error.\n"));
		return SNMPERR_GENERR;
	}
	(void) tmpsize;
	/* XXX: comment entire section if no scalars are persistent */
	{
		memset(line, 0, sizeof(line));
		strcat(line, "strExtMIB ");
		cptr = line + strlen(line);
		/* XXX: remove individual scalars that are not persistent */
		cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->strExtStrlogRecordNextIndex, &tmpsize);
		cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->strNlogargs, &tmpsize);
		cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->strExtLogMsgSize, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->strExtConsoleLog, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->strExtErrorLog, &tmpsize);
		cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->strExtTraceLog, &tmpsize);
		snmpd_store_config(line);
	}
	DEBUGMSGTL(("strExtMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void refresh_strExtMIB(int force)
 * @param force forced refresh when non-zero.
 * @brief refresh the scalar values of strExtMIB.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a scalar has been requested).
 */
void
refresh_strExtMIB(int force)
{
	if (strExtMIBStorage == NULL) {
		struct strExtMIB_data *StorageNew;

		if ((StorageNew = strExtMIB_create()) == NULL)
			return;
		strExtMIBStorage = StorageNew;
		strExtMIB_refresh = 1;
	}
	if (!force && strExtMIB_refresh == 0)
		return;
	DEBUGMSGTL(("strExtMIB", "refresh_strExtMIB: refreshing...  "));
	/* XXX: Update scalars as required here... */
	strExtMIB_refresh = 0;
	DEBUGMSGTL(("strExtMIB", "done.\n"));
}

/**
 * @fn u_char * var_strExtMIB(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @param vp a pointer to the entry in the variables table for the requested variable.
 * @param name the object identifier for which to find.
 * @param length the length of the object identifier.
 * @param exact whether the name is exact.
 * @param var_len a pointer to the length of the representation of the object.
 * @param write_method a pointer to a write method for the object.
 * @brief locate variables in strExtMIB.
 *
 * This function returns a pointer to a memory area that is static across the request that contains
 * the UCD-SNMP representation of the scalar (so that it may be used to read from for a GET,
 * GET-NEXT or GET-BULK request).  This returned pointer may be NULL, in which case the function is
 * telling UCD-SNMP that the scalar does not exist for reading; however, if write_method is
 * overwritten with a non-NULL value, the function is telling UCD-SNMP that the scalar exists for
 * writing.  Write-only objects can be effected in this way.
 */
u_char *
var_strExtMIB(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct strExtMIB_data *StorageTmp;
	u_char *rval;

	DEBUGMSGTL(("strExtMIB", "var_strExtMIB: lookup up varbind...  "));
	if (header_generic(vp, name, length, exact, var_len, write_method) == MATCH_FAILED)
		return NULL;
	/* Refresh the MIB values if required. */
	refresh_strExtMIB(0);
	if ((StorageTmp = strExtMIBStorage) == NULL) {
		DEBUGMSGTL(("strExtMIB", "no datastructure.\n"));
		return NULL;
	}
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) STREXTSTRLOGRECORDNEXTINDEX:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->strExtStrlogRecordNextIndex);
		rval = (u_char *) &StorageTmp->strExtStrlogRecordNextIndex;
		break;
	case (u_char) STRNLOGARGS:	/* ReadWrite */
		*write_method = write_strNlogargs;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->strNlogargs);
		rval = (u_char *) &StorageTmp->strNlogargs;
		break;
	case (u_char) STREXTLOGMSGSIZE:	/* ReadWrite */
		*write_method = write_strExtLogMsgSize;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->strExtLogMsgSize);
		rval = (u_char *) &StorageTmp->strExtLogMsgSize;
		break;
	case (u_char) STREXTCONSOLELOG:	/* ReadWrite */
		*write_method = write_strExtConsoleLog;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->strExtConsoleLog);
		rval = (u_char *) &StorageTmp->strExtConsoleLog;
		break;
	case (u_char) STREXTERRORLOG:	/* ReadWrite */
		*write_method = write_strExtErrorLog;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->strExtErrorLog);
		rval = (u_char *) &StorageTmp->strExtErrorLog;
		break;
	case (u_char) STREXTTRACELOG:	/* ReadWrite */
		*write_method = write_strExtTraceLog;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->strExtTraceLog);
		rval = (u_char *) &StorageTmp->strExtTraceLog;
		break;
	default:
		ERROR_MSG("");
	}
	if (rval)
		DEBUGMSGTL(("strExtMIB", "found.\n"));
	else
		DEBUGMSGTL(("strExtMIB", "not found.\n"));
	return (rval);
}

/**
 * @fn struct strExtStrlogRecordTable_data *strExtStrlogRecordTable_create(void)
 * @brief create a fresh data structure representing a new row in the strExtStrlogRecordTable table.
 *
 * Creates a new strExtStrlogRecordTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct strExtStrlogRecordTable_data *
strExtStrlogRecordTable_create(void)
{
	struct strExtStrlogRecordTable_data *StorageNew = SNMP_MALLOC_STRUCT(strExtStrlogRecordTable_data);

	DEBUGMSGTL(("strExtMIB", "strExtStrlogRecordTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->strExtStrlogRecordSeqNo = 0;
		if ((StorageNew->strExtStrlogRecordDateAndTime = (uint8_t *) strdup("")) != NULL)
			StorageNew->strExtStrlogRecordDateAndTimeLen = strlen("");
		StorageNew->strExtStrlogRecordTimeStamp = 0;
		StorageNew->strExtStrlogRecordMid = 0;
		StorageNew->strExtStrlogRecordSid = 0;
		StorageNew->strExtStrlogRecordLevel = 0;
		if (memdup((u_char **) &StorageNew->strExtStrlogRecordFlags, (u_char *) "\x00", 1) == SNMPERR_SUCCESS)
			StorageNew->strExtStrlogRecordFlagsLen = 1;
		if ((StorageNew->strExtStrlogRecordMsgString = (uint8_t *) strdup("")) != NULL)
			StorageNew->strExtStrlogRecordMsgStringLen = strlen("");
		StorageNew->strExtStrlogRecordRowStatus = 0;
		StorageNew->strExtStrlogRecordRowStatus = RS_NOTREADY;
	}
	DEBUGMSGTL(("strExtMIB", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct strExtStrlogRecordTable_data *strExtStrlogRecordTable_duplicate(struct strExtStrlogRecordTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicat a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct strExtStrlogRecordTable_data *
strExtStrlogRecordTable_duplicate(struct strExtStrlogRecordTable_data *thedata)
{
	struct strExtStrlogRecordTable_data *StorageNew = SNMP_MALLOC_STRUCT(strExtStrlogRecordTable_data);

	DEBUGMSGTL(("strExtMIB", "strExtStrlogRecordTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("strExtMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	strExtStrlogRecordTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int strExtStrlogRecordTable_destroy(struct strExtStrlogRecordTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
strExtStrlogRecordTable_destroy(struct strExtStrlogRecordTable_data **thedata)
{
	struct strExtStrlogRecordTable_data *StorageDel;

	DEBUGMSGTL(("strExtMIB", "strExtStrlogRecordTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->strExtStrlogRecordDateAndTime);
		StorageDel->strExtStrlogRecordDateAndTimeLen = 0;
		SNMP_FREE(StorageDel->strExtStrlogRecordFlags);
		StorageDel->strExtStrlogRecordFlagsLen = 0;
		SNMP_FREE(StorageDel->strExtStrlogRecordMsgString);
		StorageDel->strExtStrlogRecordMsgStringLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("strExtMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int strExtStrlogRecordTable_add(struct strExtStrlogRecordTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the strExtStrlogRecordTable table data set.
 *
 * Adds a table row structure to the strExtStrlogRecordTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
strExtStrlogRecordTable_add(struct strExtStrlogRecordTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("strExtMIB", "strExtStrlogRecordTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* strExtStrlogRecordIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->strExtStrlogRecordIndex, sizeof(thedata->strExtStrlogRecordIndex));
		header_complex_add_data(&strExtStrlogRecordTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("strExtMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int strExtStrlogRecordTable_del(struct strExtStrlogRecordTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the strExtStrlogRecordTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
strExtStrlogRecordTable_del(struct strExtStrlogRecordTable_data *thedata)
{
	struct strExtStrlogRecordTable_data *StorageDel;

	DEBUGMSGTL(("strExtMIB", "strExtStrlogRecordTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(strExtStrlogRecordTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&strExtStrlogRecordTableStorage, hciptr);
	}
	DEBUGMSGTL(("strExtMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_strExtStrlogRecordTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for strExtStrlogRecordTable entries.
 *
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case strExtStrlogRecordTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_strExtStrlogRecordTable(const char *token, char *line)
{
	size_t tmpsize;
	struct strExtStrlogRecordTable_data *StorageTmp = strExtStrlogRecordTable_create();

	DEBUGMSGTL(("strExtMIB", "parse_strExtStrlogRecordTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->strExtStrlogRecordIndex, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->strExtStrlogRecordSeqNo, &tmpsize);
	SNMP_FREE(StorageTmp->strExtStrlogRecordDateAndTime);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->strExtStrlogRecordDateAndTime, &StorageTmp->strExtStrlogRecordDateAndTimeLen);
	if (StorageTmp->strExtStrlogRecordDateAndTime == NULL) {
		config_perror("invalid specification for strExtStrlogRecordDateAndTime");
		return;
	}
	line = read_config_read_data(ASN_TIMETICKS, line, &StorageTmp->strExtStrlogRecordTimeStamp, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->strExtStrlogRecordMid, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->strExtStrlogRecordSid, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->strExtStrlogRecordLevel, &tmpsize);
	SNMP_FREE(StorageTmp->strExtStrlogRecordFlags);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->strExtStrlogRecordFlags, &StorageTmp->strExtStrlogRecordFlagsLen);
	if (StorageTmp->strExtStrlogRecordFlags == NULL) {
		config_perror("invalid specification for strExtStrlogRecordFlags");
		return;
	}
	SNMP_FREE(StorageTmp->strExtStrlogRecordMsgString);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->strExtStrlogRecordMsgString, &StorageTmp->strExtStrlogRecordMsgStringLen);
	if (StorageTmp->strExtStrlogRecordMsgString == NULL) {
		config_perror("invalid specification for strExtStrlogRecordMsgString");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->strExtStrlogRecordRowStatus, &tmpsize);
	strExtStrlogRecordTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("strExtMIB", "done.\n"));
}

/*
 * store_strExtStrlogRecordTable(): store configuraiton file for strExtStrlogRecordTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_strExtStrlogRecordTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct strExtStrlogRecordTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("strExtMIB", "store_strExtStrlogRecordTable: storing data...  "));
	refresh_strExtStrlogRecordTable(1);
	(void) tmpsize;
	for (hcindex = strExtStrlogRecordTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct strExtStrlogRecordTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "strExtStrlogRecordTable ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->strExtStrlogRecordIndex, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->strExtStrlogRecordSeqNo, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->strExtStrlogRecordDateAndTime, &StorageTmp->strExtStrlogRecordDateAndTimeLen);
			cptr = read_config_store_data(ASN_TIMETICKS, cptr, &StorageTmp->strExtStrlogRecordTimeStamp, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->strExtStrlogRecordMid, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->strExtStrlogRecordSid, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->strExtStrlogRecordLevel, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->strExtStrlogRecordFlags, &StorageTmp->strExtStrlogRecordFlagsLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->strExtStrlogRecordMsgString, &StorageTmp->strExtStrlogRecordMsgStringLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->strExtStrlogRecordRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("strExtMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct strExtTraceTable_data *strExtTraceTable_create(void)
 * @brief create a fresh data structure representing a new row in the strExtTraceTable table.
 *
 * Creates a new strExtTraceTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct strExtTraceTable_data *
strExtTraceTable_create(void)
{
	struct strExtTraceTable_data *StorageNew = SNMP_MALLOC_STRUCT(strExtTraceTable_data);

	DEBUGMSGTL(("strExtMIB", "strExtTraceTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->strExtTraceMid = 0;
		StorageNew->strExtTraceSid = 0;
		StorageNew->strExtTraceLevel = 0;
		StorageNew->strExtTraceRowStatus = 0;
		StorageNew->strExtTraceRowStatus = RS_NOTREADY;
	}
	DEBUGMSGTL(("strExtMIB", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct strExtTraceTable_data *strExtTraceTable_duplicate(struct strExtTraceTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicat a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct strExtTraceTable_data *
strExtTraceTable_duplicate(struct strExtTraceTable_data *thedata)
{
	struct strExtTraceTable_data *StorageNew = SNMP_MALLOC_STRUCT(strExtTraceTable_data);

	DEBUGMSGTL(("strExtMIB", "strExtTraceTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("strExtMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	strExtTraceTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int strExtTraceTable_destroy(struct strExtTraceTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
strExtTraceTable_destroy(struct strExtTraceTable_data **thedata)
{
	struct strExtTraceTable_data *StorageDel;

	DEBUGMSGTL(("strExtMIB", "strExtTraceTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("strExtMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int strExtTraceTable_add(struct strExtTraceTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the strExtTraceTable table data set.
 *
 * Adds a table row structure to the strExtTraceTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
strExtTraceTable_add(struct strExtTraceTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("strExtMIB", "strExtTraceTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* strExtTraceId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->strExtTraceId, sizeof(thedata->strExtTraceId));
		header_complex_add_data(&strExtTraceTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("strExtMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int strExtTraceTable_del(struct strExtTraceTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the strExtTraceTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
strExtTraceTable_del(struct strExtTraceTable_data *thedata)
{
	struct strExtTraceTable_data *StorageDel;

	DEBUGMSGTL(("strExtMIB", "strExtTraceTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(strExtTraceTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&strExtTraceTableStorage, hciptr);
	}
	DEBUGMSGTL(("strExtMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_strExtTraceTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for strExtTraceTable entries.
 *
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case strExtTraceTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_strExtTraceTable(const char *token, char *line)
{
	size_t tmpsize;
	struct strExtTraceTable_data *StorageTmp = strExtTraceTable_create();

	DEBUGMSGTL(("strExtMIB", "parse_strExtTraceTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->strExtTraceId, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->strExtTraceMid, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->strExtTraceSid, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->strExtTraceLevel, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->strExtTraceRowStatus, &tmpsize);
	strExtTraceTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("strExtMIB", "done.\n"));
}

/*
 * store_strExtTraceTable(): store configuraiton file for strExtTraceTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_strExtTraceTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct strExtTraceTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("strExtMIB", "store_strExtTraceTable: storing data...  "));
	refresh_strExtTraceTable(1);
	(void) tmpsize;
	for (hcindex = strExtTraceTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct strExtTraceTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "strExtTraceTable ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->strExtTraceId, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->strExtTraceMid, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->strExtTraceSid, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->strExtTraceLevel, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->strExtTraceRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("strExtMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void refresh_strExtStrlogRecordTable_row(struct strExtStrlogRecordTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the strExtStrlogRecordTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct strExtStrlogRecordTable_data *
refresh_strExtStrlogRecordTable_row(struct strExtStrlogRecordTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->strExtStrlogRecordTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->strExtStrlogRecordTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_strExtStrlogRecordTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the strExtStrlogRecordTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_strExtStrlogRecordTable(int force)
{
	if (!force && strExtStrlogRecordTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	strExtStrlogRecordTable_refresh = 0;
}

/**
 * @fn u_char *var_strExtStrlogRecordTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in strExtStrlogRecordTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_strExtMIB above.
 */
u_char *
var_strExtStrlogRecordTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct strExtStrlogRecordTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("strExtMIB", "var_strExtStrlogRecordTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_strExtStrlogRecordTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(strExtStrlogRecordTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_strExtStrlogRecordTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) STREXTSTRLOGRECORDSEQNO:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->strExtStrlogRecordSeqNo);
		rval = (u_char *) &StorageTmp->strExtStrlogRecordSeqNo;
		break;
	case (u_char) STREXTSTRLOGRECORDDATEANDTIME:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->strExtStrlogRecordDateAndTimeLen;
		rval = (u_char *) StorageTmp->strExtStrlogRecordDateAndTime;
		break;
	case (u_char) STREXTSTRLOGRECORDTIMESTAMP:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->strExtStrlogRecordTimeStamp);
		rval = (u_char *) &StorageTmp->strExtStrlogRecordTimeStamp;
		break;
	case (u_char) STREXTSTRLOGRECORDMID:	/* Create */
		*write_method = write_strExtStrlogRecordMid;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->strExtStrlogRecordMid);
		rval = (u_char *) &StorageTmp->strExtStrlogRecordMid;
		break;
	case (u_char) STREXTSTRLOGRECORDSID:	/* Create */
		*write_method = write_strExtStrlogRecordSid;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->strExtStrlogRecordSid);
		rval = (u_char *) &StorageTmp->strExtStrlogRecordSid;
		break;
	case (u_char) STREXTSTRLOGRECORDLEVEL:	/* Create */
		*write_method = write_strExtStrlogRecordLevel;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->strExtStrlogRecordLevel);
		rval = (u_char *) &StorageTmp->strExtStrlogRecordLevel;
		break;
	case (u_char) STREXTSTRLOGRECORDFLAGS:	/* Create */
		*write_method = write_strExtStrlogRecordFlags;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->strExtStrlogRecordFlagsLen;
		rval = (u_char *) StorageTmp->strExtStrlogRecordFlags;
		break;
	case (u_char) STREXTSTRLOGRECORDMSGSTRING:	/* Create */
		*write_method = write_strExtStrlogRecordMsgString;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->strExtStrlogRecordMsgStringLen;
		rval = (u_char *) StorageTmp->strExtStrlogRecordMsgString;
		break;
	case (u_char) STREXTSTRLOGRECORDROWSTATUS:	/* Create */
		*write_method = write_strExtStrlogRecordRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->strExtStrlogRecordRowStatus);
		rval = (u_char *) &StorageTmp->strExtStrlogRecordRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn void refresh_strExtTraceTable_row(struct strExtTraceTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the strExtTraceTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct strExtTraceTable_data *
refresh_strExtTraceTable_row(struct strExtTraceTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->strExtTraceTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->strExtTraceTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_strExtTraceTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the strExtTraceTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_strExtTraceTable(int force)
{
	if (!force && strExtTraceTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	strExtTraceTable_refresh = 0;
}

/**
 * @fn u_char *var_strExtTraceTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in strExtTraceTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_strExtMIB above.
 */
u_char *
var_strExtTraceTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct strExtTraceTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("strExtMIB", "var_strExtTraceTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_strExtTraceTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(strExtTraceTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_strExtTraceTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) STREXTTRACEMID:	/* Create */
		*write_method = write_strExtTraceMid;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->strExtTraceMid);
		rval = (u_char *) &StorageTmp->strExtTraceMid;
		break;
	case (u_char) STREXTTRACESID:	/* Create */
		*write_method = write_strExtTraceSid;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->strExtTraceSid);
		rval = (u_char *) &StorageTmp->strExtTraceSid;
		break;
	case (u_char) STREXTTRACELEVEL:	/* Create */
		*write_method = write_strExtTraceLevel;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->strExtTraceLevel);
		rval = (u_char *) &StorageTmp->strExtTraceLevel;
		break;
	case (u_char) STREXTTRACEROWSTATUS:	/* Create */
		*write_method = write_strExtTraceRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->strExtTraceRowStatus);
		rval = (u_char *) &StorageTmp->strExtTraceRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int write_strExtStrlogRecordMid(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_strExtStrlogRecordMid(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static ulong old_value;
	struct strExtStrlogRecordTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	ulong set_value = *((ulong *) var_val);

	DEBUGMSGTL(("strExtMIB", "write_strExtStrlogRecordMid entering action=%d...  \n", action));
	StorageTmp = header_complex(strExtStrlogRecordTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->strExtStrlogRecordRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to strExtStrlogRecordMid: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to strExtStrlogRecordMid not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to strExtStrlogRecordMid: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..16383 */
		if ((0 > set_value || set_value > 16383)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to strExtStrlogRecordMid: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->strExtStrlogRecordMid for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->strExtStrlogRecordMid;
		StorageTmp->strExtStrlogRecordMid = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->strExtStrlogRecordMid = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_strExtStrlogRecordSid(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_strExtStrlogRecordSid(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static ulong old_value;
	struct strExtStrlogRecordTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	ulong set_value = *((ulong *) var_val);

	DEBUGMSGTL(("strExtMIB", "write_strExtStrlogRecordSid entering action=%d...  \n", action));
	StorageTmp = header_complex(strExtStrlogRecordTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->strExtStrlogRecordRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to strExtStrlogRecordSid: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to strExtStrlogRecordSid not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to strExtStrlogRecordSid: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..16383 */
		if ((0 > set_value || set_value > 16383)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to strExtStrlogRecordSid: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->strExtStrlogRecordSid for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->strExtStrlogRecordSid;
		StorageTmp->strExtStrlogRecordSid = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->strExtStrlogRecordSid = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_strExtStrlogRecordLevel(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_strExtStrlogRecordLevel(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static ulong old_value;
	struct strExtStrlogRecordTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	ulong set_value = *((ulong *) var_val);

	DEBUGMSGTL(("strExtMIB", "write_strExtStrlogRecordLevel entering action=%d...  \n", action));
	StorageTmp = header_complex(strExtStrlogRecordTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->strExtStrlogRecordRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to strExtStrlogRecordLevel: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to strExtStrlogRecordLevel not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to strExtStrlogRecordLevel: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..255 */
		if ((0 > set_value || set_value > 255)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to strExtStrlogRecordLevel: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->strExtStrlogRecordLevel for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->strExtStrlogRecordLevel;
		StorageTmp->strExtStrlogRecordLevel = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->strExtStrlogRecordLevel = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_strExtStrlogRecordFlags(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_strExtStrlogRecordFlags(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static uint8_t *old_value;
	struct strExtStrlogRecordTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	static size_t old_length = 0;
	static uint8_t *string = NULL;

	DEBUGMSGTL(("strExtMIB", "write_strExtStrlogRecordFlags entering action=%d...  \n", action));
	StorageTmp = header_complex(strExtStrlogRecordTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		string = NULL;
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->strExtStrlogRecordRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to strExtStrlogRecordFlags: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if ((var_val_type != ASN_BIT_STR && var_val_type != ASN_OCTET_STR)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to strExtStrlogRecordFlags not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_type == ASN_BIT_STR) {
			if (1 > var_val_len || var_val_len > SPRINT_MAX_LEN || var_val_len != 2) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to strExtStrlogRecordFlags: bad length\n");
				return SNMP_ERR_WRONGLENGTH;
			}
		}
		if (var_val_type == ASN_OCTET_STR) {
			if (var_val_len > SPRINT_MAX_LEN || var_val_len != 1) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to strExtStrlogRecordFlags: bad length\n");
				return SNMP_ERR_WRONGLENGTH;
			}
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->strExtStrlogRecordFlags for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->strExtStrlogRecordFlags;
		old_length = StorageTmp->strExtStrlogRecordFlagsLen;
		StorageTmp->strExtStrlogRecordFlags = string;
		StorageTmp->strExtStrlogRecordFlagsLen = var_val_len;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(old_value);
		old_length = 0;
		string = NULL;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->strExtStrlogRecordFlags = old_value;
		StorageTmp->strExtStrlogRecordFlagsLen = old_length;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		SNMP_FREE(string);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_strExtStrlogRecordMsgString(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_strExtStrlogRecordMsgString(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static uint8_t *old_value;
	struct strExtStrlogRecordTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	static size_t old_length = 0;
	static uint8_t *string = NULL;

	DEBUGMSGTL(("strExtMIB", "write_strExtStrlogRecordMsgString entering action=%d...  \n", action));
	StorageTmp = header_complex(strExtStrlogRecordTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		string = NULL;
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->strExtStrlogRecordRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to strExtStrlogRecordMsgString: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to strExtStrlogRecordMsgString not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 0..255 */
		if (var_val_len > SPRINT_MAX_LEN || ((0 > var_val_len || var_val_len > 255))) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to strExtStrlogRecordMsgString: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->strExtStrlogRecordMsgString for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->strExtStrlogRecordMsgString;
		old_length = StorageTmp->strExtStrlogRecordMsgStringLen;
		StorageTmp->strExtStrlogRecordMsgString = string;
		StorageTmp->strExtStrlogRecordMsgStringLen = var_val_len;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(old_value);
		old_length = 0;
		string = NULL;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->strExtStrlogRecordMsgString = old_value;
		StorageTmp->strExtStrlogRecordMsgStringLen = old_length;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		SNMP_FREE(string);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_strExtTraceMid(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_strExtTraceMid(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct strExtTraceTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 14;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("strExtMIB", "write_strExtTraceMid entering action=%d...  \n", action));
	StorageTmp = header_complex(strExtTraceTableStorage, NULL, &name[14], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->strExtTraceRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to strExtTraceMid: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to strExtTraceMid not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to strExtTraceMid: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges -1..-1 0..16383 */
		if (set_value != -1 && (0 > set_value || set_value > 16383)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to strExtTraceMid: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->strExtTraceMid for you to use, and you have just been asked to do something with it.  Note that anything done here must
				   be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->strExtTraceMid;
		StorageTmp->strExtTraceMid = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->strExtTraceMid = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_strExtTraceSid(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_strExtTraceSid(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct strExtTraceTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 14;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("strExtMIB", "write_strExtTraceSid entering action=%d...  \n", action));
	StorageTmp = header_complex(strExtTraceTableStorage, NULL, &name[14], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->strExtTraceRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to strExtTraceSid: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to strExtTraceSid not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to strExtTraceSid: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges -1..-1 0..16383 */
		if (set_value != -1 && (0 > set_value || set_value > 16383)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to strExtTraceSid: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->strExtTraceSid for you to use, and you have just been asked to do something with it.  Note that anything done here must
				   be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->strExtTraceSid;
		StorageTmp->strExtTraceSid = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->strExtTraceSid = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_strExtTraceLevel(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_strExtTraceLevel(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct strExtTraceTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 14;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("strExtMIB", "write_strExtTraceLevel entering action=%d...  \n", action));
	StorageTmp = header_complex(strExtTraceTableStorage, NULL, &name[14], &newlen, 1, NULL, NULL);
	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->strExtTraceRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to strExtTraceLevel: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to strExtTraceLevel not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to strExtTraceLevel: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges -1..-1 0..255 */
		if (set_value != -1 && (0 > set_value || set_value > 255)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to strExtTraceLevel: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->strExtTraceLevel for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		old_value = StorageTmp->strExtTraceLevel;
		StorageTmp->strExtTraceLevel = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->strExtTraceLevel = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_strNlogargs(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_strNlogargs(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static ulong old_value;
	struct strExtMIB_data *StorageTmp = NULL;
	ulong set_value = *((ulong *) var_val);

	DEBUGMSGTL(("strExtMIB", "write_strNlogargs entering action=%d...  \n", action));
	if ((StorageTmp = strExtMIBStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to strNlogargs not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to strNlogargs: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 20 */
		/* Note: ranges 0..64 */
		if ((0 > set_value || set_value > 64)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to strNlogargs: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in set_value for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in
				   the UNDO case */
		old_value = StorageTmp->strNlogargs;
		StorageTmp->strNlogargs = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->strNlogargs = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_strExtLogMsgSize(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_strExtLogMsgSize(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static ulong old_value;
	struct strExtMIB_data *StorageTmp = NULL;
	ulong set_value = *((ulong *) var_val);

	DEBUGMSGTL(("strExtMIB", "write_strExtLogMsgSize entering action=%d...  \n", action));
	if ((StorageTmp = strExtMIBStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to strExtLogMsgSize not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to strExtLogMsgSize: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 1024 */
		/* Note: ranges 256..4096 */
		if ((256 > set_value || set_value > 4096)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to strExtLogMsgSize: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in set_value for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in
				   the UNDO case */
		old_value = StorageTmp->strExtLogMsgSize;
		StorageTmp->strExtLogMsgSize = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->strExtLogMsgSize = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

static int
strExtConsoleLog_start()
{
	if (my_fd_console < 0) {
		struct strioctl ioc;

		if ((my_fd_console = open("/dev/streams/clone/log", O_RDWR | O_NONBLOCK)) < 0) {
			snmp_log_perror("write_strExtConsoleLog: open(/dev/streams/clone/log)");
			return SNMP_ERR_GENERR;
		}
		register_readfd(my_fd_console, strExtConsoleLog_fd_handler, (void *) 0);
		register_exceptfd(my_fd_console, strExtConsoleLog_fd_handler, (void *) 1);
		ioc.ic_cmd = I_CONSLOG;
		ioc.ic_timout = 0;
		ioc.ic_len = 0;
		ioc.ic_dp = NULL;
		if (ioctl(my_fd_console, I_STR, &ioc) < 0) {
			snmp_log_perror("write_strExtConsoleLog: ioctl(I_CONSLOG)");
			close(my_fd_console);
			my_fd_console = -1;
			return SNMP_ERR_GENERR;
		}
	}
	return SNMP_ERR_NOERROR;
}

static int
strExtConsoleLog_stop()
{
	if (my_fd_console >= 0) {
		unregister_exceptfd(my_fd_console);
		unregister_readfd(my_fd_console);
		close(my_fd_console);
		my_fd_console = -1;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_strExtConsoleLog(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_strExtConsoleLog(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct strExtMIB_data *StorageTmp = NULL;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("strExtMIB", "write_strExtConsoleLog entering action=%d...  \n", action));
	if ((StorageTmp = strExtMIBStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to strExtConsoleLog not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to strExtConsoleLog: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value false */
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to strExtConsoleLog: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in set_value for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in
				   the UNDO case */
		old_value = StorageTmp->strExtConsoleLog;
		StorageTmp->strExtConsoleLog = set_value;
		if (old_value != set_value) {
			switch (set_value) {
			case TV_TRUE:
				/* turn it on */
				if (strExtConsoleLog_start() != SNMP_ERR_NOERROR)
					return SNMP_ERR_GENERR;
				break;
			case TV_FALSE:
				/* turn it off */
				if (strExtConsoleLog_stop() != SNMP_ERR_NOERROR)
					return SNMP_ERR_GENERR;
				break;
			}
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->strExtConsoleLog = old_value;
		if (old_value != set_value) {
			switch (old_value) {
			case TV_TRUE:
				/* turn it back on */
				if (strExtConsoleLog_start() != SNMP_ERR_NOERROR)
					return SNMP_ERR_GENERR;
				break;
			case TV_FALSE:
				/* turn it off */
				if (strExtConsoleLog_stop() != SNMP_ERR_NOERROR)
					return SNMP_ERR_GENERR;
				break;
			}
		}
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

static int
strExtErrorLog_start()
{
	if (my_fd_error < 0) {
		struct strioctl ioc;

		if ((my_fd_error = open("/dev/streams/clone/log", O_RDWR | O_NONBLOCK)) < 0) {
			snmp_log_perror("write_strExtErrorLog: open(/dev/streams/clone/log)");
			return SNMP_ERR_GENERR;
		}
		register_readfd(my_fd_error, strExtErrorLog_fd_handler, (void *) 0);
		register_exceptfd(my_fd_error, strExtErrorLog_fd_handler, (void *) 1);
		ioc.ic_cmd = I_ERRLOG;
		ioc.ic_timout = 0;
		ioc.ic_len = 0;
		ioc.ic_dp = NULL;
		if (ioctl(my_fd_error, I_STR, &ioc) < 0) {
			snmp_log_perror("write_strExtErrorLog: ioctl(I_ERRLOG)");
			close(my_fd_error);
			my_fd_error = -1;
			return SNMP_ERR_GENERR;
		}
	}
	return SNMP_ERR_NOERROR;
}

static int
strExtErrorLog_stop()
{
	if (my_fd_error >= 0) {
		unregister_exceptfd(my_fd_error);
		unregister_readfd(my_fd_error);
		close(my_fd_error);
		my_fd_error = -1;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_strExtErrorLog(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_strExtErrorLog(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct strExtMIB_data *StorageTmp = NULL;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("strExtMIB", "write_strExtErrorLog entering action=%d...  \n", action));
	if ((StorageTmp = strExtMIBStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to strExtErrorLog not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to strExtErrorLog: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value false */
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to strExtErrorLog: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in set_value for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in
				   the UNDO case */
		old_value = StorageTmp->strExtErrorLog;
		StorageTmp->strExtErrorLog = set_value;
		if (old_value != set_value) {
			switch (set_value) {
			case TV_TRUE:
				/* turn it on */
				if (strExtErrorLog_start() != SNMP_ERR_NOERROR)
					return SNMP_ERR_GENERR;
				break;
			case TV_FALSE:
				/* turn it off */
				if (strExtErrorLog_stop() != SNMP_ERR_NOERROR)
					return SNMP_ERR_GENERR;
				break;
			}
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->strExtErrorLog = old_value;
		if (old_value != set_value) {
			switch (old_value) {
			case TV_TRUE:
				/* turn it back on */
				if (strExtErrorLog_start() != SNMP_ERR_NOERROR)
					return SNMP_ERR_GENERR;
				break;
			case TV_FALSE:
				/* turn it back off */
				if (strExtErrorLog_stop() != SNMP_ERR_NOERROR)
					return SNMP_ERR_GENERR;
				break;
			}
		}
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

static int
strExtTraceLog_start()
{
	if (my_fd_trace < 0) {
		struct strioctl ioc;
		struct trace_ids *tid;
		struct header_complex_index *h;
		int i, count;

		for (count = 0, h = strExtTraceTableStorage; h; h = h->next) {
			struct strExtTraceTable_data *d = h->data;

			if (d->strExtTraceRowStatus == RS_ACTIVE)
				count++;
		}
		if (count == 0)
			/* does not need to be turned on */
			return SNMP_ERR_NOERROR;

		if ((tid = calloc(sizeof(*tid), count)) == NULL) {
			snmp_log_perror("write_strExtTraceLog: calloc");
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		}
		for (i = 0, h = strExtTraceTableStorage; h; h = h->next) {
			struct strExtTraceTable_data *d = h->data;

			if (d->strExtTraceRowStatus == RS_ACTIVE) {
				tid[i].ti_mid = d->strExtTraceMid;
				tid[i].ti_sid = d->strExtTraceSid;
				tid[i].ti_level = d->strExtTraceLevel;
				i++;
			}
		}
		if ((my_fd_trace = open("/dev/streams/clone/log", O_RDWR | O_NONBLOCK)) < 0) {
			snmp_log_perror("write_strExtTraceLog: open(/dev/streams/clone/log)");
			SNMP_FREE(tid);
			return SNMP_ERR_GENERR;
		}
		register_readfd(my_fd_trace, strExtTraceLog_fd_handler, (void *) 0);
		register_exceptfd(my_fd_trace, strExtTraceLog_fd_handler, (void *) 1);
		ioc.ic_cmd = I_TRCLOG;
		ioc.ic_timout = 0;
		ioc.ic_len = count * sizeof(*tid);
		ioc.ic_dp = (char *) tid;
		if (ioctl(my_fd_trace, I_STR, &ioc) < 0) {
			snmp_log_perror("write_strExtTraceLog: ioctl(I_TRCLOG)");
			close(my_fd_trace);
			my_fd_trace = -1;
			SNMP_FREE(tid);
			return SNMP_ERR_GENERR;
		}
		SNMP_FREE(tid);
	}
	return SNMP_ERR_NOERROR;
}

static int
strExtTraceLog_stop()
{
	if (my_fd_trace >= 0) {
		unregister_exceptfd(my_fd_trace);
		unregister_readfd(my_fd_trace);
		close(my_fd_trace);
		my_fd_trace = -1;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_strExtTraceLog(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Scalar write routine.
 */
int
write_strExtTraceLog(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct strExtMIB_data *StorageTmp = NULL;
	long set_value = *((long *) var_val);
	int ret;

	DEBUGMSGTL(("strExtMIB", "write_strExtTraceLog entering action=%d...  \n", action));
	if ((StorageTmp = strExtMIBStorage) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to strExtTraceLog not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to strExtTraceLog: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value false */
		switch (set_value) {
		case TV_TRUE:
		case TV_FALSE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to strExtTraceLog: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case ACTION:		/* The variable has been stored in set_value for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in
				   the UNDO case */
		old_value = StorageTmp->strExtTraceLog;
		StorageTmp->strExtTraceLog = set_value;
		if (old_value != set_value) {
			switch (set_value) {
			case TV_TRUE:
				/* turn it on */
				if ((ret = strExtTraceLog_start()) != SNMP_ERR_NOERROR)
					return (ret);
				break;
			case TV_FALSE:
				/* turn it off */
				if ((ret = strExtTraceLog_stop()) != SNMP_ERR_NOERROR)
					return (ret);
				break;
			}
		}
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->strExtTraceLog = old_value;
		if (old_value != set_value) {
			switch (old_value) {
			case TV_TRUE:
				/* turn it back on */
				if ((ret = strExtTraceLog_start()) != SNMP_ERR_NOERROR)
					return (ret);
				break;
			case TV_FALSE:
				/* turn it back off */
				if ((ret = strExtTraceLog_stop()) != SNMP_ERR_NOERROR)
					return (ret);
				break;
			}
		}
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int strExtStrlogRecordTable_consistent(struct strExtStrlogRecordTable_data *thedata)
 * @param thedata the row data to check for consistency.
 * @brief check the internal consistency of a table row.
 *
 * This function checks the internal consistency of a table row for the strExtStrlogRecordTable table.  If the
 * table row is internally consistent, then this function returns SNMP_ERR_NOERROR, otherwise the
 * function returns an SNMP error code and it will not be possible to activate the row until the
 * row's internal consistency is corrected.  This function might use a 'test' operation against the
 * driver to ensure that the commit phase will succeed.
 */
int
strExtStrlogRecordTable_consistent(struct strExtStrlogRecordTable_data *thedata)
{
	/* XXX: check row consistency return SNMP_ERR_NOERROR if consistent, or an SNMP error code if not. */
	return (SNMP_ERR_NOERROR);
}

/**
 * @fn int strExtTraceTable_consistent(struct strExtTraceTable_data *thedata)
 * @param thedata the row data to check for consistency.
 * @brief check the internal consistency of a table row.
 *
 * This function checks the internal consistency of a table row for the strExtTraceTable table.  If the
 * table row is internally consistent, then this function returns SNMP_ERR_NOERROR, otherwise the
 * function returns an SNMP error code and it will not be possible to activate the row until the
 * row's internal consistency is corrected.  This function might use a 'test' operation against the
 * driver to ensure that the commit phase will succeed.
 */
int
strExtTraceTable_consistent(struct strExtTraceTable_data *thedata)
{
	/* XXX: check row consistency return SNMP_ERR_NOERROR if consistent, or an SNMP error code if not. */
	return (SNMP_ERR_NOERROR);
}

/**
 * @fn int write_strExtStrlogRecordRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_strExtStrlogRecordRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct strExtStrlogRecordTable_data *StorageTmp = NULL;
	static struct strExtStrlogRecordTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 15;
	static int old_value;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("strExtMIB", "write_strExtStrlogRecordRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(strExtStrlogRecordTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to strExtStrlogRecordRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to strExtStrlogRecordRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->strExtStrlogRecordRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->strExtStrlogRecordTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_DESTROY:
			/* destroying existent or non-existent row is ok */
			if (StorageTmp == NULL)
				break;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->strExtStrlogRecordTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			vars = NULL;
			/* strExtStrlogRecordIndex */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[15]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* strExtStrlogRecordIndex */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index strExtStrlogRecordIndex: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = strExtStrlogRecordTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			vp = vars;
			StorageNew->strExtStrlogRecordIndex = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&strExtStrlogRecordTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_DESTROY:
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				strExtStrlogRecordTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		}
		break;
	case ACTION:
		/* The variable has been stored in set_value for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = strExtStrlogRecordTable_consistent(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			/* row does not have to be consistent */
			break;
		case RS_ACTIVE:
			old_value = StorageTmp->strExtStrlogRecordRowStatus;
			StorageTmp->strExtStrlogRecordRowStatus = set_value;
			if (old_value != RS_ACTIVE) {
				/* check that activation is possible */
				if ((ret = strExtStrlogRecordTable_consistent(StorageTmp)) != SNMP_ERR_NOERROR) {
					StorageTmp->strExtStrlogRecordRowStatus = old_value;
					return (ret);
				}
			}
			break;
		case RS_NOTINSERVICE:
			/* set the flag? */
			old_value = StorageTmp->strExtStrlogRecordRowStatus;
			StorageTmp->strExtStrlogRecordRowStatus = set_value;
			break;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* row creation, set final state */
			/* XXX: commit creation to underlying device */
			/* XXX: activate with underlying device */
			StorageNew->strExtStrlogRecordRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			/* row creation, set final state */
			StorageNew->strExtStrlogRecordRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (old_value != set_value) {
				switch (set_value) {
				case RS_ACTIVE:
					/* XXX: activate with underlying device */
					break;
				case RS_NOTINSERVICE:
					/* XXX: deactivate with underlying device */
					break;
				}
			}
			break;
		case RS_DESTROY:
			/* row deletion, free it its dead */
			strExtStrlogRecordTable_destroy(&StorageDel);
			/* strExtStrlogRecordTable_destroy() can handle NULL pointers. */
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* restore state */
			StorageTmp->strExtStrlogRecordRowStatus = old_value;
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				strExtStrlogRecordTable_del(StorageNew);
				strExtStrlogRecordTable_destroy(&StorageNew);
			}
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				strExtStrlogRecordTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_strExtTraceRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_strExtTraceRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct strExtTraceTable_data *StorageTmp = NULL;
	static struct strExtTraceTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 14;
	static int old_value;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("strExtMIB", "write_strExtTraceRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(strExtTraceTableStorage, NULL, &name[14], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to strExtTraceRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to strExtTraceRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->strExtTraceRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->strExtTraceTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_DESTROY:
			/* destroying existent or non-existent row is ok */
			if (StorageTmp == NULL)
				break;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->strExtTraceTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			vars = NULL;
			/* strExtTraceId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[14]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* strExtTraceId */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index strExtTraceId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = strExtTraceTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			vp = vars;
			StorageNew->strExtTraceId = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&strExtTraceTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_DESTROY:
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				strExtTraceTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		}
		break;
	case ACTION:
		/* The variable has been stored in set_value for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = strExtTraceTable_consistent(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			/* row does not have to be consistent */
			break;
		case RS_ACTIVE:
			old_value = StorageTmp->strExtTraceRowStatus;
			StorageTmp->strExtTraceRowStatus = set_value;
			if (old_value != RS_ACTIVE) {
				/* check that activation is possible */
				if ((ret = strExtTraceTable_consistent(StorageTmp)) != SNMP_ERR_NOERROR) {
					StorageTmp->strExtTraceRowStatus = old_value;
					return (ret);
				}
			}
			break;
		case RS_NOTINSERVICE:
			/* set the flag? */
			old_value = StorageTmp->strExtTraceRowStatus;
			StorageTmp->strExtTraceRowStatus = set_value;
			break;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* row creation, set final state */
			/* XXX: commit creation to underlying device */
			/* XXX: activate with underlying device */
			StorageNew->strExtTraceRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			/* row creation, set final state */
			StorageNew->strExtTraceRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (old_value != set_value) {
				switch (set_value) {
				case RS_ACTIVE:
					/* XXX: activate with underlying device */
					break;
				case RS_NOTINSERVICE:
					/* XXX: deactivate with underlying device */
					break;
				}
			}
			break;
		case RS_DESTROY:
			/* row deletion, free it its dead */
			strExtTraceTable_destroy(&StorageDel);
			/* strExtTraceTable_destroy() can handle NULL pointers. */
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* restore state */
			StorageTmp->strExtTraceRowStatus = old_value;
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				strExtTraceTable_del(StorageNew);
				strExtTraceTable_destroy(&StorageNew);
			}
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				strExtTraceTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

void
send_strStrlogRecord_v2trap(struct variable_list *vars)
{
	struct variable_list trap;

	DEBUGMSGTL(("strExtMIB", "send_strStrlogRecord_v2trap: sending trap...  "));
	trap.next_variable = vars;
	trap.name = snmpTrapOID_oid;
	trap.name_length = sizeof(snmpTrapOID_oid) / sizeof(oid);
	trap.type = ASN_OBJECT_ID;
	trap.val.objid = strStrlogRecord_oid;
	trap.val_len = sizeof(strStrlogRecord_oid);
	trap.index = 0;
	/* strExtStrlogSeqNo */
	/* strExtStrlogDateAndTime */
	/* strExtStrlogTimeStamp */
	/* strExtStrlogMid */
	/* strExtStrlogSid */
	/* strExtStrlogLogLevel */
	/* strExtStrlogFlags */
	/* strExtStrlogFmtString */
	/* optional: strExtStrlogInteger */
	/* optional: strExtStrlogUnsigned */
	/* optional: strExtStrlogString */
	/* optional: strExtStrlogInteger64 */
	/* optional: strExtStrlogUnsigned64 */
	/* optional: strExtStrlogInteger128 */
	/* optional: strExtStrlogUnsigned128 */
	send_v2trap(&trap);
	DEBUGMSGTL(("strExtMIB", "done.\n"));
}

void
strExtConsoleLog_fd_handler(int fd, void *dummy)
{
	return strExtMIB_fd_handler(fd, dummy);
}

void
strExtErrorLog_fd_handler(int fd, void *dummy)
{
	return strExtMIB_fd_handler(fd, dummy);
}

void
strExtTraceLog_fd_handler(int fd, void *dummy)
{
	return strExtMIB_fd_handler(fd, dummy);
}

#undef MASTER
/**
 * @fn void strExtMIB_loop_handler(int dummy)
 * @param sig signal number
 * @brief handle event loop interation.
 *
 * This function is registered so that, when operating as a module, snmpd will call it one per event
 * loop interation.  This function is called before the next requst is processed and after the
 * previous request is processed.  Two things are done here:  1) The file descriptor that is used to
 * synchronize the agent with (pseudo-)device drivers is closed.  (Another approach, instead of
 * closing each time, would be to restart a timer each time that a request is made (loop is
 * performed) and if it expires, close the file descriptor).  2) The request number is incremented.
 * Although a request is not generated for each loop of the snmp event loop, it is true that a new
 * request cannot be generated without performing a loop.  Therefore, the sa_request is not the
 * request number but it is a temporally unique identifier for a request.
 */
void
strExtMIB_loop_handler(int sig)
{
	DEBUGMSGTL(("strExtMIB", "strExtMIB_loop_handler: executing loop handler...  "));
	/* close files after each request */
	if (sa_fclose) {
		if (my_fd >= 0) {
			close(my_fd);
			my_fd = -1;
		}
	}
#if defined MASTER
	/* prepare for next request */
	sa_request++;
#endif				/* defined MASTER */
	if (external_signal_scheduled[sig] == 0)
		external_signal_scheduled[sig]--;
	if (strExtMIBold_signal_handler != NULL)
		(*strExtMIBold_signal_handler) (sig);
	DEBUGMSGTL(("strExtMIB", "done.\n"));
}

#define FLAG_ZEROPAD	(1<<0)	/* pad with zero */
#define FLAG_SIGN	(1<<1)	/* unsigned/signed long */
#define FLAG_PLUS	(1<<2)	/* show plus */
#define FLAG_SPACE	(1<<3)	/* space if plus */
#define FLAG_LEFT	(1<<4)	/* left justified */
#define FLAG_SPECIAL	(1<<5)	/* 0x */
#define FLAG_LARGE	(1<<6)	/* use 'ABCDEF' instead of 'abcdef' */

#define PROMOTE_TYPE		int
#define PROMOTE_SIZE		(sizeof(PROMOTE_TYPE))
#define PROMOTE_ALIGN(__x)	(((__x) + PROMOTE_SIZE - 1) & ~(PROMOTE_SIZE - 1))
#define PROMOTE_SIZEOF(__x)	((sizeof(__x) < PROMOTE_SIZE) ? PROMOTE_SIZE : sizeof(__x))
#define PROMOTE_ARGVAL(__type,__ptr) \
	(long long)({ \
		long long val; \
		if (sizeof(__type) < PROMOTE_SIZEOF(__type)) { \
			val = (long long)*((PROMOTE_TYPE *)(__ptr)); \
			__ptr = (typeof(__ptr))((char *)__ptr + PROMOTE_SIZE); \
		} else { \
			val = (long long)*((__type *)(__ptr)); \
			__ptr = (typeof(__ptr))((char *)__ptr + sizeof(__type)); \
		} \
		val; \
	})

static void
add_varlist_integer(struct variable_list **vp, long value)
{
	snmp_varlist_add_variable(vp, strExtStrlogInteger_oid, sizeof(strExtStrlogInteger_oid) / sizeof(oid), ASN_INTEGER, (u_char *) &value, sizeof(int));
}
static void
add_varlist_unsigned(struct variable_list **vp, ulong value)
{
	snmp_varlist_add_variable(vp, strExtStrlogUnsigned_oid, sizeof(strExtStrlogUnsigned_oid) / sizeof(oid), ASN_UNSIGNED, (u_char *) &value, sizeof(uint));
}
static void
add_varlist_string(struct variable_list **vp, const char *string, size_t len)
{
	snmp_varlist_add_variable(vp, strExtStrlogString_oid, sizeof(strExtStrlogString_oid) / sizeof(oid), ASN_OCTET_STR, (u_char *) string, len);
}
static void
add_varlist_integer64(struct variable_list **vp, long long value)
{
	struct counter64 c64;

	c64.high = (value >> 32) & 0xffffffff;
	c64.low = (value >> 0) & 0xffffffff;
	snmp_varlist_add_variable(vp, strExtStrlogInteger64_oid, sizeof(strExtStrlogInteger64_oid) / sizeof(oid), ASN_INTEGER64, (u_char *) &c64, sizeof(c64));
}

static void
add_varlist_unsigned64(struct variable_list **vp, unsigned long long value)
{
	struct counter64 c64;

	c64.high = (value >> 32) & 0xffffffff;
	c64.low = (value >> 0) & 0xffffffff;
	snmp_varlist_add_variable(vp, strExtStrlogUnsigned64_oid, sizeof(strExtStrlogUnsigned64_oid) / sizeof(oid), ASN_UNSIGNED64, (u_char *) &c64, sizeof(c64));
}

static void
add_varlist_integer128(struct variable_list **vp, long long value)
{
	u_char opaque[16];
	int i;

	for (i = 0; i < 16; i++) {
		opaque[15 - i] = value & 0xff;
		value >>= 8;

	}
	snmp_varlist_add_variable(vp, strExtStrlogInteger128_oid, sizeof(strExtStrlogInteger128_oid) / sizeof(oid), ASN_OPAQUE, opaque, 16);
}

static void
add_varlist_unsigned128(struct variable_list **vp, unsigned long long value)
{
	u_char opaque[16];
	int i;

	for (i = 0; i < 16; i++) {
		opaque[15 - i] = value & 0xff;
		value >>= 8;

	}
	snmp_varlist_add_variable(vp, strExtStrlogUnsigned128_oid, sizeof(strExtStrlogUnsigned128_oid) / sizeof(oid), ASN_OPAQUE, opaque, 16);
}

/**
 * number: - print formatted number to buffer
 * @sbuf: buffer to print to
 * @end: end of buffer
 * @num: the number to print
 * @base: the base for the representation of the number
 * @width: the width of the formatted number field
 * @decimal: the precision of the decimal number
 * @flags: formatting flags
 *
 *  This function prints a formatted number to a buffer.  It is largely
 *  taken from /usr/src/linux/lib/vsprintf.c
 */
static int
number(char *sbuf, const char *end, long long num, int base, int width, int decimal, int flags)
{
	char sign;
	int i, iszero = (num == 0);
	char *str;
	char tmp[66];

	str = sbuf;
	if (flags & FLAG_LEFT)
		flags &= ~FLAG_ZEROPAD;
	if (base < 2 || base > 16)
		goto done;
	sign = '\0';
	if (flags & FLAG_SIGN) {
		if (num < 0) {
			sign = '-';
			num = -num;
			width--;
		} else if (flags & FLAG_PLUS) {
			sign = '+';
			width--;
		} else if (flags & FLAG_SPACE) {
			sign = ' ';
			width--;
		}
	}
	if (flags & FLAG_SPECIAL) {
		if (!iszero) {
			switch (base) {
			case 16:
				width -= 2;	/* for 0x */
				break;
			case 8:
				width--;	/* for 0 */
				break;
			}
		}
	}
	i = 0;
	if (num == 0)
		tmp[i++] = '0';
	else {
		const char *hexdig = (flags & FLAG_LARGE) ? "0123456789ABCDEF" : "0123456789abcdef";

		while (num != 0) {
			lldiv_t result = lldiv(num, base);

			tmp[i++] = hexdig[result.rem];
			num = result.quot;
		}
	}
	if (i > decimal)
		decimal = i;
	width -= decimal;
	if (!(flags & (FLAG_ZEROPAD | FLAG_LEFT)))
		if (width > 0) {
			while (width-- > 0) {
				*str = ' ';
				if (++str >= end)
					goto done;
			}
		}
	if (sign != '\0') {
		*str = sign;
		if (++str >= end)
			goto done;
	}
	if (flags & FLAG_SPECIAL) {
		if (!iszero) {
			switch (base) {
			case 8:
				*str = '0';
				if (++str >= end)
					goto done;
				break;
			case 16:
				*str = '0';
				if (++str >= end)
					goto done;
				if (flags & FLAG_LARGE)
					*str = 'X';
				else
					*str = 'x';
				if (++str >= end)
					goto done;
				break;
			}
		}
	}
	if (!(flags & FLAG_LEFT)) {
		char pad = (flags & FLAG_ZEROPAD) ? '0' : ' ';

		if (width > 0) {
			while (width-- > 0) {
				*str = pad;
				if (++str >= end)
					goto done;
			}
		}
	}
	if (i < decimal) {
		while (i < decimal--) {
			*str = '0';
			if (++str >= end)
				goto done;
		}
	}
	if (i > 0) {
		while (i-- > 0) {
			*str = tmp[i];
			if (++str >= end)
				goto done;
		}
	}
	if (width > 0) {
		while (width-- > 0) {
			*str = ' ';
			if (++str >= end)
				goto done;
		}
	}
      done:
	return (str - sbuf);
}

/**
 * snprintfit: - print strlog(9) format string and create varbinds
 * @sbuf: string buffer to print to
 * @slen: length of string buffer to print to
 * @buf: format string (followed by promoted arguments)(
 * @len: length of format string (including promoted arguments)
 * @vars: list to add varbinds to
 *
 * This function does an snprintf from the buffer using a slight variation of the snprintf that is
 * identical to that used inside the kernel.  There are some constructs that are not supported.
 * Also, the kernel passes a format string followed by the arguments on the next word aligned
 * boundary, and this is what is necessary to format them.  This function is largely adapted from
 * /usr/src/linux/lib/vsprintf.c.  Also, this function adds each argument and value to the varbind
 * list @vars.
 */
static int
snprintfit(char *sbuf, size_t slen, const char *buf, int len, struct variable_list **vp)
{
	const char *fmt, *args, *aend, *end;
	char *str;
	char type;
	size_t flen, plen;
	int flags = 0, width = -1, decimal = -1, base = 10;

	if (slen == 0)
		return (0);
	fmt = buf;
	flen = strnlen(fmt, len);
	plen = PROMOTE_ALIGN(flen + 1);
	args = &buf[plen];
	aend = buf + len;
	str = sbuf;
	end = str + slen - 1;	/* room for null */
	for (; *fmt; ++fmt) {
		const char *pos;
		unsigned long long num = 0;

		if (*fmt != '%') {
			*str = *fmt;
			if (++str >= end)
				goto done;
			continue;
		}
		flags = 0;
		width = -1;
		decimal = -1;
		base = 10;
		pos = fmt;	/* remember position of % */
		/* process flags */
		for (++fmt;; ++fmt) {
			switch (*fmt) {
			case '-':
				flags |= FLAG_LEFT;
				continue;
			case '+':
				flags |= FLAG_PLUS;
				continue;
			case ' ':
				flags |= FLAG_SPACE;
				continue;
			case '#':
				flags |= FLAG_SPECIAL;
				continue;
			case '0':
				flags |= FLAG_ZEROPAD;
				continue;
			default:
				break;
			}
			break;
		}
		/* get field width */
		if (isdigit(*fmt))
			for (width = 0; isdigit(*fmt); width *= 10, width += (*fmt++ - '0')) ;
		else if (*fmt == '*') {
			++fmt;
			if (args + PROMOTE_SIZEOF(int) <= aend) {
				width = PROMOTE_ARGVAL(int, args);

				add_varlist_integer(vp, width);
				if (width < 0) {
					width = -width;
					flags |= FLAG_LEFT;
				}
			} else
				args = aend;
		}
		/* get decimal precision */
		if (*fmt == '.') {
			++fmt;
			if (isdigit(*fmt))
				for (decimal = 0; isdigit(*fmt); decimal *= 10, decimal += (*fmt - '0')) ;
			else if (*fmt == '*') {
				if (args + PROMOTE_SIZEOF(int) <= aend) {
					decimal = PROMOTE_ARGVAL(int, args);

					add_varlist_integer(vp, decimal);
					if (decimal < 0)
						decimal = 0;
				} else
					args = aend;
			}
		}
		/* get conversion type */
		type = 'i';
		switch (*fmt) {
		case 'h':
			type = *fmt;
			++fmt;
			if (*fmt == 'h') {
				type = 'c';
				++fmt;
			}
			break;
		case 'l':
			type = *fmt;
			++fmt;
			if (*fmt == 'l') {
				type = 'L';
				++fmt;
			}
			break;
		case 'q':
		case 'L':
		case 'Z':
		case 'z':
		case 't':
			type = *fmt;
			++fmt;
			break;
		}
		switch (*fmt) {
		case 'c':
		{
			char c = ' ';

			if (!(flags & FLAG_LEFT))
				if (width > 0) {
					while (--width > 0) {
						*str = ' ';
						if (++str >= end)
							goto done;
					}
				}
			if (args + PROMOTE_SIZEOF(char) <= aend) {
				c = PROMOTE_ARGVAL(char, args);

				add_varlist_integer(vp, c);
			} else
				args = aend;
			*str = c;
			if (++str >= end)
				goto done;
			if (width > 0) {
				while (--width > 0) {
					*str = ' ';
					if (++str >= end)
						goto done;
				}
			}
			continue;
		}
		case 's':
		{
			const char *s;
			int i;
			int slen = 0, splen = 0;

			s = args;
			if (args < aend) {
				slen = strlen(s);
				splen = PROMOTE_ALIGN(slen + 1);
				add_varlist_string(vp, s, slen);
			} else
				args = aend;
			if (args + splen <= aend)
				args += splen;
			else
				args = aend;
			if (slen > (size_t) decimal)
				slen = (size_t) decimal;
			if (!(flags & FLAG_LEFT))
				if (slen < width) {
					while (slen < width--) {
						*str = ' ';
						if (++str >= end)
							goto done;
					}
				}
			for (i = 0; i < slen; ++i, ++s) {
				*str = *s;
				if (++str >= end)
					goto done;
			}
			if (slen < width) {
				while (slen < width--) {
					*str = ' ';
					if (++str >= end)
						goto done;
				}
			}
			continue;
		}
		case '%':
			*str = '%';
			if (++str >= end)
				goto done;
			continue;
		case 'p':
		case 'o':
		case 'X':
		case 'x':
		case 'd':
		case 'i':
		case 'u':
			switch (*fmt) {
			case 'p':
				type = 'p';
				if (width == -1) {
					width = 2 * sizeof(void *);
					flags |= FLAG_ZEROPAD;
				}
				flags |= FLAG_SPECIAL;
				base = 16;
				break;
			case 'o':
				base = 8;
				break;
			case 'X':
				flags |= FLAG_LARGE;
			case 'x':
				base = 16;
				break;
			case 'd':
			case 'i':
				flags |= FLAG_SIGN;
			case 'u':
				break;
			}
			switch (type) {
			case 'c':	/* really 'hh' type */
				if (args + PROMOTE_SIZEOF(unsigned char) <= aend)
					 num = PROMOTE_ARGVAL(unsigned char, args);

				else
					args = aend;
				if (flags & FLAG_SIGN) {
					num = (signed char) num;
					add_varlist_integer(vp, num);
				} else {
					add_varlist_unsigned(vp, num);
				}
				break;
			case 'h':
				if (args + PROMOTE_SIZEOF(unsigned short) <= aend)
					 num = PROMOTE_ARGVAL(unsigned short, args);

				else
					args = aend;
				if (flags & FLAG_SIGN) {
					num = (signed short) num;
					add_varlist_integer(vp, num);
				} else {
					add_varlist_unsigned(vp, num);
				}
				break;
			case 'p':	/* really 'p' conversion */
				if (args + PROMOTE_SIZEOF(uintptr_t) <= aend)
					num = PROMOTE_ARGVAL(uintptr_t, args);
				else
					args = aend;
				flags &= ~FLAG_SIGN;
				switch (sizeof(uintptr_t)) {
				case 4:
					add_varlist_integer(vp, num);
					break;
				case 8:
					add_varlist_integer64(vp, num);
					break;
				case 16:
					add_varlist_integer128(vp, num);
					break;
				}
				break;
			case 'l':
				if (args + PROMOTE_SIZEOF(unsigned long) <= aend)
					 num = PROMOTE_ARGVAL(unsigned long, args);

				else
					args = aend;
				if (flags & FLAG_SIGN) {
					num = (signed long) num;
					switch (sizeof(signed long)) {
					case 4:
						add_varlist_integer(vp, num);
						break;
					case 8:
						add_varlist_integer64(vp, num);
						break;
					case 16:
						add_varlist_integer128(vp, num);
						break;
					}
				} else {
					switch (sizeof(unsigned long)) {
					case 4:
						add_varlist_unsigned(vp, num);
						break;
					case 8:
						add_varlist_unsigned64(vp, num);
						break;
					case 16:
						add_varlist_unsigned128(vp, num);
						break;
					}
				}
				break;
			case 'q':
			case 'L':	/* really 'll' type */
				if (args + PROMOTE_SIZEOF(unsigned long long) <= aend)
					 num = PROMOTE_ARGVAL(unsigned long long, args);

				else
					args = aend;
				if (flags & FLAG_SIGN) {
					num = (signed long long) num;
					switch (sizeof(signed long long)) {
					case 4:
						add_varlist_integer(vp, num);
						break;
					case 8:
						add_varlist_integer64(vp, num);
						break;
					case 16:
						add_varlist_integer128(vp, num);
						break;
					}
				} else {
					switch (sizeof(unsigned long long)) {
					case 4:
						add_varlist_unsigned(vp, num);
						break;
					case 8:
						add_varlist_unsigned64(vp, num);
						break;
					case 16:
						add_varlist_unsigned128(vp, num);
						break;
					}
				}
				break;
			case 'Z':
			case 'z':
				if (args + PROMOTE_SIZEOF(size_t) <= aend)
					 num = PROMOTE_ARGVAL(size_t, args);

				else
					args = aend;
				if (flags & FLAG_SIGN) {
					num = (ssize_t) num;
					switch (sizeof(ssize_t)) {
					case 4:
						add_varlist_integer(vp, num);
						break;
					case 8:
						add_varlist_integer64(vp, num);
						break;
					case 16:
						add_varlist_integer128(vp, num);
						break;
					}
				} else {
					switch (sizeof(size_t)) {
					case 4:
						add_varlist_unsigned(vp, num);
						break;
					case 8:
						add_varlist_unsigned64(vp, num);
						break;
					case 16:
						add_varlist_unsigned128(vp, num);
						break;
					}
				}
				break;
			case 't':
				if (args + PROMOTE_SIZEOF(ptrdiff_t) <= aend)
					num = PROMOTE_ARGVAL(ptrdiff_t, args);
				else
					args = aend;
				switch (sizeof(ptrdiff_t)) {
				case 4:
					add_varlist_integer(vp, num);
					break;
				case 8:
					add_varlist_integer64(vp, num);
					break;
				case 16:
					add_varlist_integer128(vp, num);
					break;
				}
				break;
			default:
				if (args + PROMOTE_SIZEOF(unsigned int) <= aend)
					 num = PROMOTE_ARGVAL(unsigned int, args);

				else
					args = aend;
				if (flags & FLAG_SIGN) {
					num = (signed int) num;
					add_varlist_integer(vp, num);
				} else {
					add_varlist_unsigned(vp, num);
				}
				break;
			}
			str += number(str, end, num, base, width, decimal, flags);
			if (str >= end)
				goto done;
			continue;
		case '\0':
		default:
			*str = '%';	/* put the original % back */
			if (++str >= end)
				goto done;
			/* put the bad format characters */
			for (; fmt > pos; pos++) {
				*str = *pos;
				if (++str >= end)
					goto done;
			}
			if (*fmt == '\0')
				break;
			continue;
		}
		break;
	}
      done:
	*str = '\0';
	return (str - sbuf);
}

/**
 * @fn void strExtMIB_fd_handler(int fd, void *dummy)
 * @param fd file descriptor to read.
 * @param dummy client data passed to registration function (always NULL).
 * @brief handle read event on file descriptor.
 *
 * This read file descriptor handler is normally used for (pseudo-)device drivers that generate
 * statistical collection interval events, alarm events, or other operational measurement events, by
 * placing a message on the read queue of the "event handling" Stream.  Normally this routine
 * would adjust counts in some table or scalars, generate SNMP traps representing on-occurence
 * events, first and interval events, and alarm indications.
 */
void
strExtMIB_fd_handler(int fd, void *dummy)
{
	int ret, flag = 0;
	char cbuf[1024];
	char dbuf[2048];
	struct strbuf ctl = { 1024, 1024, cbuf };
	struct strbuf dat = { 2048, 2048, dbuf };
	struct log_ctl *lc;
	time_t time;
	struct tm tm;
	uint8_t dateandtime[8], flags[1];

	DEBUGMSGTL(("strExtMIB", "strExtMIB_fd_handler: executing fd handler...  "));
	/* XXX: place actions to handle my_fd here... */
	for (;;) {
		struct variable_list *vars = NULL;
		struct strExtStrlogRecordTable_data *d;
		struct header_complex_index *h_prev, *h;
		size_t len, mlen;
		char msg[LOGMSGSZ << 1], *fmt;

		if ((ret = getmsg(fd, &ctl, &dat, &flag)) < 0) {
			if (errno == EINTR)
				continue;
			if (errno == EAGAIN || errno == ETIMEDOUT || errno == EWOULDBLOCK)
				break;
			snmp_log_perror("strExtMIB_fd_handler: getmsg()");
			break;
		}
		if (ret != 0) {
			errno = ERANGE;
			snmp_log_perror("strExtMIB_fd_handler: getmsg()");
			continue;;
		}
		lc = (struct log_ctl *) cbuf;
		if (ctl.len < sizeof(*lc))
			continue;
		if (dat.len <= 0)
			continue;
		if ((d = SNMP_MALLOC_STRUCT(strExtStrlogRecordTable_data)) == NULL)
			continue;
		d->strExtStrlogRecordSeqNo = lc->seq_no;
		time = lc->ltime;
		gmtime_r(&time, &tm);
		dateandtime[0] = tm.tm_year >> 8;
		dateandtime[1] = tm.tm_year;
		dateandtime[2] = tm.tm_mon;
		dateandtime[3] = tm.tm_mday;
		dateandtime[4] = tm.tm_hour;
		dateandtime[5] = tm.tm_min;
		dateandtime[6] = tm.tm_sec;
		dateandtime[7] = 0;
		if ((d->strExtStrlogRecordDateAndTime = calloc(11, 1)) != NULL) {
			memcpy(&d->strExtStrlogRecordDateAndTime, dateandtime, 8);
			d->strExtStrlogRecordDateAndTimeLen = 8;
		}
		d->strExtStrlogRecordTimeStamp = lc->ttime;
		d->strExtStrlogRecordMid = lc->mid;
		d->strExtStrlogRecordSid = lc->sid;
		d->strExtStrlogRecordLevel = lc->level;
		flags[0] = 0;
		if (lc->flags & SL_CONSOLE)
			flags[0] |= (1 << (7 - STREXTSTRLOGFLAGS_SLCONSOLE));
		if (lc->flags & SL_ERROR)
			flags[0] |= (1 << (7 - STREXTSTRLOGFLAGS_SLERROR));
		if (lc->flags & SL_TRACE)
			flags[0] |= (1 << (7 - STREXTSTRLOGFLAGS_SLTRACE));
		if (lc->flags & SL_FATAL)
			flags[0] |= (1 << (7 - STREXTSTRLOGFLAGS_SLFATAL));
		if (lc->flags & SL_WARN)
			flags[0] |= (1 << (7 - STREXTSTRLOGFLAGS_SLWARN));
		if (lc->flags & SL_NOTE)
			flags[0] |= (1 << (7 - STREXTSTRLOGFLAGS_SLNOTE));
		if (lc->flags & SL_NOTIFY)
			flags[0] |= (1 << (7 - STREXTSTRLOGFLAGS_SLNOTIFY));
		if (lc->flags & SL_NOPUTBUF)
			flags[0] |= (1 << (7 - STREXTSTRLOGFLAGS_SLNOPUTBUF));
		if ((d->strExtStrlogRecordFlags = calloc(2, 1)) != NULL) {
			memcpy(&d->strExtStrlogRecordFlags, flags, 1);
			d->strExtStrlogRecordFlagsLen = 1;
		}
		d->strExtStrlogRecordRowStatus = RS_ACTIVE;
		for (h_prev = NULL, h = strExtStrlogRecordTableStorage; h; h_prev = h, h = h->next) ;
		if ((h = SNMP_MALLOC_STRUCT(header_complex_index)) == NULL) {
			SNMP_FREE(d);
			continue;
		}
		if ((h->name = calloc(1, sizeof(oid))) == NULL) {
			SNMP_FREE(d);
			SNMP_FREE(h);
			continue;
		}
		h->namelen = 1;
		h->data = d;
		h->next = NULL;
		if (h_prev) {
			h->name[0] = h_prev->name[0] + 1;
		} else {
			h->name[0] = 1;
			strExtStrlogRecordTableStorage = h;
		}
		d->strExtStrlogRecordIndex = h->name[0];
		snmp_varlist_add_variable(&vars, strExtStrlogIndex_oid, sizeof(strExtStrlogIndex_oid) / sizeof(oid), ASN_UNSIGNED, (u_char *) &d->strExtStrlogRecordIndex, sizeof(uint));
		snmp_varlist_add_variable(&vars,
					  strExtStrlogDateAndTime_oid, sizeof(strExtStrlogDateAndTime_oid) / sizeof(oid),
					  ASN_OCTET_STR, (u_char *) d->strExtStrlogRecordDateAndTime, d->strExtStrlogRecordDateAndTimeLen);
		snmp_varlist_add_variable(&vars, strExtStrlogTimeStamp_oid, sizeof(strExtStrlogTimeStamp_oid) / sizeof(oid), ASN_TIMETICKS, (u_char *) &d->strExtStrlogRecordTimeStamp, sizeof(uint));
		snmp_varlist_add_variable(&vars, strExtStrlogMid_oid, sizeof(strExtStrlogMid_oid) / sizeof(oid), ASN_UNSIGNED, (u_char *) &d->strExtStrlogRecordMid, sizeof(uint));
		snmp_varlist_add_variable(&vars, strExtStrlogSid_oid, sizeof(strExtStrlogSid_oid) / sizeof(oid), ASN_UNSIGNED, (u_char *) &d->strExtStrlogRecordSid, sizeof(uint));
		snmp_varlist_add_variable(&vars, strExtStrlogLevel_oid, sizeof(strExtStrlogLevel_oid) / sizeof(oid), ASN_UNSIGNED, (u_char *) &d->strExtStrlogRecordLevel, sizeof(uint));
		snmp_varlist_add_variable(&vars, strExtStrlogFlags_oid, sizeof(strExtStrlogFlags_oid) / sizeof(oid), ASN_BIT_STR, (u_char *) d->strExtStrlogRecordFlags, d->strExtStrlogRecordFlagsLen);
		fmt = dat.buf;
		len = strnlen(fmt, LOGMSGSZ);
		snmp_varlist_add_variable(&vars, strExtStrlogFmtString_oid, sizeof(strExtStrlogFmtString_oid) / sizeof(oid), ASN_UNSIGNED, (u_char *) fmt, len);
		/* generates formatted string and also adds promoted variables to varlist */
		mlen = snprintfit(msg, sizeof(msg), fmt, len, &vars);
		/* add formatted string to record */
		if ((d->strExtStrlogRecordMsgString = (u_char *) strdup(msg)) != NULL)
			d->strExtStrlogRecordMsgStringLen = strnlen(msg, sizeof(msg));
		/* send the trap */
		send_strStrlogRecord_v2trap(vars);
		snmp_free_varbind(vars);
	}
	DEBUGMSGTL(("strExtMIB", "done.\n"));
	return;
}
