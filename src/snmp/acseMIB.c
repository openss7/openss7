/*****************************************************************************

 @(#) $RCSfile$ $Name$($Revision$) $Date$

 -----------------------------------------------------------------------------

 Copyright (c) 2008-2012  Monavacon Limited <http://www.monavacon.com/>
 Copyright (c) 2001-2008  OpenSS7 Corporation <http://www.openss7.com/>
 Copyright (c) 1997-2001  Brian F. G. Bidulock <bidulock@openss7.org>

 All Rights Reserved.

 This program is free software: you can redistribute it and/or modify it under
 the terms of the GNU Affero General Public License as published by the Free
 Software Foundation, version 3 of the license.

 This program is distributed in the hope that it will be useful, but WITHOUT
 ANY WARRANTY; without even the implied warranty of MERCHANTABILITY or FITNESS
 FOR A PARTICULAR PURPOSE.  See the GNU Affero General Public License for more
 details.

 You should have received a copy of the GNU Affero General Public License
 along with this program.  If not, see <http://www.gnu.org/licenses/>, or
 write to the Free Software Foundation, Inc., 675 Mass Ave, Cambridge, MA
 02139, USA.

 -----------------------------------------------------------------------------

 U.S. GOVERNMENT RESTRICTED RIGHTS.  If you are licensing this Software on
 behalf of the U.S. Government ("Government"), the following provisions apply
 to you.  If the Software is supplied by the Department of Defense ("DoD"), it
 is classified as "Commercial Computer Software" under paragraph 252.227-7014
 of the DoD Supplement to the Federal Acquisition Regulations ("DFARS") (or any
 successor regulations) and the Government is acquiring only the license rights
 granted herein (the license rights customarily provided to non-Government
 users).  If the Software is supplied to any unit or agency of the Government
 other than DoD, it is classified as "Restricted Computer Software" and the
 Government's rights in the Software are defined in paragraph 52.227-19 of the
 Federal Acquisition Regulations ("FAR") (or any successor regulations) or, in
 the cases of NASA, in paragraph 18.52.227-86 of the NASA Supplement to the FAR
 (or any successor regulations).

 -----------------------------------------------------------------------------

 Commercial licensing and support of this software is available from OpenSS7
 Corporation at a fee.  See http://www.openss7.com/

 -----------------------------------------------------------------------------

 Last Modified $Date$ by $Author$

 -----------------------------------------------------------------------------

 $Log$
 *****************************************************************************/

static char const ident[] = "$RCSfile$ $Name$($Revision$) $Date$";

/* This file was generated by mib2c and is intended for use as
   a mib module for the ucd-snmp snmpd agent. */
#include <ucd-snmp/ucd-snmp-config.h>
#include <ucd-snmp/ucd-snmp-includes.h>
#include <ucd-snmp/ucd-snmp-agent-includes.h>
#include <ucd-snmp/agent_trap.h>
#include <ucd-snmp/callback.h>
#include <ucd-snmp/snmp-tc.h>
#include <ucd-snmp/default_store.h>
#include <ucd-snmp/snmp_alarm.h>
/* The following header files are mangled in most recent net-snmp releases so
 * the versions from UCD-SNMP 4.2.5 are included here.  */
#if defined HAVE_LIBNETSNMP
#else				/* defined HAVE_LIBNETSNMP */
#endif				/* defined HAVE_LIBNETSNMP */
/* These are messed up on both. */
#include "ds_agent.h"
#ifdef HAVE_UCD_SNMP_UTIL_FUNCS_H
#include <ucd-snmp/util_funcs.h>
/* Many recent net-snmp UCD compatible headers do not declare header_generic. */
int header_generic(struct variable *, oid *, size_t *, int, size_t *, WriteMethod **);
#else				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#include "util_funcs.h"
#endif				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#ifdef HAVE_UCD_SNMP_HEADER_COMPLEX_H
#include <ucd-snmp/header_complex.h>
#else				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
#include "header_complex.h"
#endif				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
/* This one is the other way around: it is just fine for net-snmp, but
 * ucd-snmp does not provide the header file at all.  */
#ifdef HAVE_UCD_SNMP_MIB_MODULES_H
#include <ucd-snmp/mib_modules.h>
#else				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#ifdef HAVE_NET_SNMP_AGENT_MIB_MODULES_H
#include <net-snmp/agent/mib_modules.h>
#else				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#include "mib_modules.h"
#endif				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#endif				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#include <stdint.h>
#include <signal.h>
#include <sys/stat.h>		/* for struct stat, fstat() */
#include <sys/types.h>
#include <unistd.h>
#include <stdlib.h>
#include <pwd.h>		/* for getpwuid() getpwnam() */
#include <grp.h>		/* for getgrgid() getgrnam() */
#include <libgen.h>		/* for basename() */
#include <fcntl.h>		/* for O_CREAT */
#include <sys/sysctl.h>		/* for sysctl */
#include <errno.h>
#include <string.h>
#ifdef _GNU_SOURCE
#include <getopt.h>
#endif
#include "acseMIB.h"
#define MASTER 1
#define MY_FACILITY(__pri)	(LOG_DAEMON|(__pri))
#if defined MODULE
#if defined MASTER
const char sa_program[] = "acseMIB";
int sa_fclose = 1;			/* default close files between requests */
int sa_changed = 1;			/* indication to reread MIB configuration */
int sa_stats_refresh = 1;		/* indications that statistics, the mib or its tables need to be refreshed */
int sa_request = 1;			/* request number for per-request actions */
int sa_dump = 0;			/* default packet dump */
int sa_debug = 0;			/* default no debug */
#endif				/* defined MASTER */
#endif				/* defined MODULE */
static int my_fd = -1;			/* file descriptor for this MIB's use */
static int my_readfd = -1;		/* file descriptor for autonomnous events */
volatile int acseMIB_refresh = 1;
volatile int acseTable_refresh = 1;
volatile int acseAssociationTable_refresh = 1;
volatile int apInvocationTable_refresh = 1;
volatile int applicationEntityInvocationTable_refresh = 1;
volatile int applicationSubsystemTable_refresh = 1;
volatile int asoTable_refresh = 1;
volatile int asoEntityTable_refresh = 1;
volatile int asoInvocationTable_refresh = 1;
volatile int monoULConnectionTable_refresh = 1;
volatile int monoULEntityTable_refresh = 1;
volatile int monolithicUpperLayerSubsystemTable_refresh = 1;
volatile int pSapTable_refresh = 1;
volatile int presentationConnectionTable_refresh = 1;
volatile int presentationCopmTable_refresh = 1;
volatile int presentationEntityTable_refresh = 1;
volatile int presentationSubsystemTable_refresh = 1;
volatile int sSapTable_refresh = 1;
volatile int sessionConnectionTable_refresh = 1;
volatile int sessionCopmTable_refresh = 1;
volatile int sessionEntityTable_refresh = 1;
volatile int sessionSubsystemTable_refresh = 1;
volatile int acseEntityTable_refresh = 1;
volatile int acseAccessPointTable_refresh = 1;
volatile int acseLinkageTable_refresh = 1;

/*
 * acseMIB_variables_oid: object identifier for acseMIB
 * This is the top level oid that we want to register under.  This is essentially a prefix, with the
 * suffix appearing in the variable below.
 */
oid acseMIB_variables_oid[10] = { 1, 3, 6, 1, 4, 1, 29591, 17, 751, 47 };
oid acseAssociationTable_variables_oid[14] = { 1, 3, 6, 1, 4, 1, 29591, 17, 751, 47, 3, 2, 1, 1 };
oid apInvocationTable_variables_oid[14] = { 1, 3, 6, 1, 4, 1, 29591, 17, 751, 47, 3, 3, 1, 1 };
oid applicationEntityInvocationTable_variables_oid[14] = { 1, 3, 6, 1, 4, 1, 29591, 17, 751, 47, 3, 5, 1, 1 };
oid asoTable_variables_oid[14] = { 1, 3, 6, 1, 4, 1, 29591, 17, 751, 47, 3, 7, 1, 1 };
oid asoEntityTable_variables_oid[14] = { 1, 3, 6, 1, 4, 1, 29591, 17, 751, 47, 3, 8, 1, 1 };
oid asoInvocationTable_variables_oid[14] = { 1, 3, 6, 1, 4, 1, 29591, 17, 751, 47, 3, 9, 1, 1 };
oid monoULConnectionTable_variables_oid[14] = { 1, 3, 6, 1, 4, 1, 29591, 17, 751, 47, 3, 10, 1, 1 };
oid monolithicUpperLayerSubsystemTable_variables_oid[14] = { 1, 3, 6, 1, 4, 1, 29591, 17, 751, 47, 3, 12, 1, 1 };
oid pSapTable_variables_oid[14] = { 1, 3, 6, 1, 4, 1, 29591, 17, 751, 47, 3, 13, 1, 1 };
oid presentationConnectionTable_variables_oid[14] = { 1, 3, 6, 1, 4, 1, 29591, 17, 751, 47, 3, 14, 1, 1 };
oid presentationCopmTable_variables_oid[14] = { 1, 3, 6, 1, 4, 1, 29591, 17, 751, 47, 3, 15, 1, 1 };
oid presentationEntityTable_variables_oid[14] = { 1, 3, 6, 1, 4, 1, 29591, 17, 751, 47, 3, 16, 1, 1 };
oid presentationSubsystemTable_variables_oid[14] = { 1, 3, 6, 1, 4, 1, 29591, 17, 751, 47, 3, 17, 1, 1 };
oid sSapTable_variables_oid[14] = { 1, 3, 6, 1, 4, 1, 29591, 17, 751, 47, 3, 18, 1, 1 };
oid sessionConnectionTable_variables_oid[14] = { 1, 3, 6, 1, 4, 1, 29591, 17, 751, 47, 3, 19, 1, 1 };
oid sessionCopmTable_variables_oid[14] = { 1, 3, 6, 1, 4, 1, 29591, 17, 751, 47, 3, 20, 1, 1 };
oid sessionEntityTable_variables_oid[14] = { 1, 3, 6, 1, 4, 1, 29591, 17, 751, 47, 3, 21, 1, 1 };
oid sessionSubsystemTable_variables_oid[14] = { 1, 3, 6, 1, 4, 1, 29591, 17, 751, 47, 3, 22, 1, 1 };
oid acseEntityTable_variables_oid[14] = { 1, 3, 6, 1, 4, 1, 29591, 17, 751, 47, 3, 23, 1, 1 };
oid acseAccessPointTable_variables_oid[14] = { 1, 3, 6, 1, 4, 1, 29591, 17, 751, 47, 3, 24, 1, 1 };
oid acseLinkageTable_variables_oid[14] = { 1, 3, 6, 1, 4, 1, 29591, 17, 751, 47, 3, 25, 1, 1 };

/*
 * Oids for use in notifications defined in this MIB.
 */

/*
 * Oids accessible only for notify defined in this MIB.
 */

/*
 * Other oids defined in this MIB.
 */
oid acseBasicGroup_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 17, 751, 47, 6, 1 };
oid acseBasicCompliance_oid[12] = { 1, 3, 6, 1, 4, 1, 29591, 17, 751, 47, 7, 1 };
static oid zeroDotZero_oid[2] = { 0, 0 };
static oid snmpTrapOID_oid[11] = { 1, 3, 6, 1, 6, 3, 1, 1, 4, 1, 0 };

/*
 * variable7 acseMIB_variables: tree for acseMIB
 * This variable defines function callbacks and type return information for the acseMIB mib section
 */
struct variable7 acseMIB_variables[] = {
	/* magic number, variable type, ro/rw, callback fn, L, oidsuffix */
#define   ACSEFUINITIATOR       2
	{(u_char) ACSEFUINITIATOR, ASN_OCTET_STR, RONLY, var_acseTable, 5, {3, 1, 1, 1, 1}},
#define   ACSEEDITIONSSUPPORTED  3
	{(u_char) ACSEEDITIONSSUPPORTED, ASN_OCTET_STR, RONLY, var_acseTable, 5, {3, 1, 1, 1, 2}},
#define   ACSEFURESPONDER       4
	{(u_char) ACSEFURESPONDER, ASN_OCTET_STR, RONLY, var_acseTable, 5, {3, 1, 1, 1, 3}},
#define   ACSEASSOCIATIONFUINUSE  5
	{(u_char) ACSEASSOCIATIONFUINUSE, ASN_OCTET_STR, RONLY, var_acseAssociationTable, 5, {3, 2, 1, 1, 2}},
#define   ACSEASSOCIATIONAPPLCONTEXTINUSE  6
	{(u_char) ACSEASSOCIATIONAPPLCONTEXTINUSE, ASN_OBJECT_ID, RONLY, var_acseAssociationTable, 5, {3, 2, 1, 1, 3}},
#define   ACSEASSOCIATIONPARENTASOINVOC  7
	{(u_char) ACSEASSOCIATIONPARENTASOINVOC, ASN_OBJECT_ID, RONLY, var_acseAssociationTable, 5, {3, 2, 1, 1, 4}},
#define   ACSEASSOCIATIONCALLEDAETITLE  8
	{(u_char) ACSEASSOCIATIONCALLEDAETITLE, ASN_OCTET_STR, RONLY, var_acseAssociationTable, 5, {3, 2, 1, 1, 5}},
#define   ACSEASSOCIATIONROWSTATUS  9
	{(u_char) ACSEASSOCIATIONROWSTATUS, ASN_INTEGER, RWRITE, var_acseAssociationTable, 5, {3, 2, 1, 1, 6}},
#define   APINVOCATIONROWSTATUS  10
	{(u_char) APINVOCATIONROWSTATUS, ASN_INTEGER, RWRITE, var_apInvocationTable, 5, {3, 3, 1, 1, 3}},
#define   APPLICATIONENTITYINVOCATIONROWSTATUS  11
	{(u_char) APPLICATIONENTITYINVOCATIONROWSTATUS, ASN_INTEGER, RWRITE, var_applicationEntityInvocationTable, 5, {3, 5, 1, 1, 4}},
#define   ASOROWSTATUS          14
	{(u_char) ASOROWSTATUS, ASN_INTEGER, RWRITE, var_asoTable, 5, {3, 7, 1, 1, 5}},
#define   ASOENTITYROWSTATUS    16
	{(u_char) ASOENTITYROWSTATUS, ASN_INTEGER, RWRITE, var_asoEntityTable, 5, {3, 8, 1, 1, 7}},
#define   ASOINVOCATIONROWSTATUS  17
	{(u_char) ASOINVOCATIONROWSTATUS, ASN_INTEGER, RWRITE, var_asoInvocationTable, 5, {3, 9, 1, 1, 3}},
#define   MONOULCONNECTIONROWSTATUS  18
	{(u_char) MONOULCONNECTIONROWSTATUS, ASN_INTEGER, RWRITE, var_monoULConnectionTable, 5, {3, 10, 1, 1, 1}},
#define   MONOLITHICUPPERLAYERSUBSSYTEMROWSTATUS  20
	{(u_char) MONOLITHICUPPERLAYERSUBSSYTEMROWSTATUS, ASN_INTEGER, RWRITE, var_monolithicUpperLayerSubsystemTable, 5, {3, 12, 1, 1, 2}},
#define   PSAPROWSTATUS         22
	{(u_char) PSAPROWSTATUS, ASN_INTEGER, RWRITE, var_pSapTable, 5, {3, 13, 1, 1, 4}},
#define   PRESENTATIONCONNECTIONROWSTATUS  23
	{(u_char) PRESENTATIONCONNECTIONROWSTATUS, ASN_INTEGER, RWRITE, var_presentationConnectionTable, 5, {3, 14, 1, 1, 6}},
#define   PRESENTATIONCOPMROWSTATUS  24
	{(u_char) PRESENTATIONCOPMROWSTATUS, ASN_INTEGER, RWRITE, var_presentationCopmTable, 5, {3, 15, 1, 1, 6}},
#define   PRESENTATIONENTITYROWSTATUS  25
	{(u_char) PRESENTATIONENTITYROWSTATUS, ASN_INTEGER, RWRITE, var_presentationEntityTable, 5, {3, 16, 1, 1, 4}},
#define   PRESENTATIONSUBSYSTEMROWSTATUS  26
	{(u_char) PRESENTATIONSUBSYSTEMROWSTATUS, ASN_INTEGER, RWRITE, var_presentationSubsystemTable, 5, {3, 17, 1, 1, 2}},
#define   SSAPROWSTATUS         28
	{(u_char) SSAPROWSTATUS, ASN_INTEGER, RWRITE, var_sSapTable, 5, {3, 18, 1, 1, 4}},
#define   SESSIONCONNECTIONROWSTATUS  29
	{(u_char) SESSIONCONNECTIONROWSTATUS, ASN_INTEGER, RWRITE, var_sessionConnectionTable, 5, {3, 19, 1, 1, 7}},
#define   SESSIONCOPMROWSTATUS  30
	{(u_char) SESSIONCOPMROWSTATUS, ASN_INTEGER, RWRITE, var_sessionCopmTable, 5, {3, 20, 1, 1, 6}},
#define   SESSIONENTITYROWSTATUS  31
	{(u_char) SESSIONENTITYROWSTATUS, ASN_INTEGER, RWRITE, var_sessionEntityTable, 5, {3, 21, 1, 1, 4}},
#define   SESSIONSUBSYSTEMROWSTATUS  32
	{(u_char) SESSIONSUBSYSTEMROWSTATUS, ASN_INTEGER, RWRITE, var_sessionSubsystemTable, 5, {3, 22, 1, 1, 2}},
#define   ACSEENTITYROWSTATUS   33
	{(u_char) ACSEENTITYROWSTATUS, ASN_INTEGER, RWRITE, var_acseEntityTable, 5, {3, 23, 1, 1, 2}},
#define   ACSEACCESSPOINTROWSTATUS  34
	{(u_char) ACSEACCESSPOINTROWSTATUS, ASN_INTEGER, RWRITE, var_acseAccessPointTable, 5, {3, 24, 1, 1, 2}},
#define   ACSELINKAGEROWSTATUS  35
	{(u_char) ACSELINKAGEROWSTATUS, ASN_INTEGER, RWRITE, var_acseLinkageTable, 5, {3, 25, 1, 1, 1}},
};

/* (L = length of the oidsuffix) */
struct acseMIB_data *acseMIBStorage = NULL;

/* global storage of our data, saved in and configured by header_complex() */
struct header_complex_index *acseTableStorage = NULL;
struct header_complex_index *acseAssociationTableStorage = NULL;
struct header_complex_index *apInvocationTableStorage = NULL;
struct header_complex_index *applicationEntityInvocationTableStorage = NULL;
struct header_complex_index *applicationSubsystemTableStorage = NULL;
struct header_complex_index *asoTableStorage = NULL;
struct header_complex_index *asoEntityTableStorage = NULL;
struct header_complex_index *asoInvocationTableStorage = NULL;
struct header_complex_index *monoULConnectionTableStorage = NULL;
struct header_complex_index *monoULEntityTableStorage = NULL;
struct header_complex_index *monolithicUpperLayerSubsystemTableStorage = NULL;
struct header_complex_index *pSapTableStorage = NULL;
struct header_complex_index *presentationConnectionTableStorage = NULL;
struct header_complex_index *presentationCopmTableStorage = NULL;
struct header_complex_index *presentationEntityTableStorage = NULL;
struct header_complex_index *presentationSubsystemTableStorage = NULL;
struct header_complex_index *sSapTableStorage = NULL;
struct header_complex_index *sessionConnectionTableStorage = NULL;
struct header_complex_index *sessionCopmTableStorage = NULL;
struct header_complex_index *sessionEntityTableStorage = NULL;
struct header_complex_index *sessionSubsystemTableStorage = NULL;
struct header_complex_index *acseEntityTableStorage = NULL;
struct header_complex_index *acseAccessPointTableStorage = NULL;
struct header_complex_index *acseLinkageTableStorage = NULL;

void (*acseMIBold_signal_handler) (int) = NULL;	/* save old signal handler just in case */
void acseMIB_loop_handler(int);
void acseMIB_fd_handler(int, void *);

/**
 * @fn void init_acseMIB(void)
 * @brief acseMIB initialization routine.
 *
 * This is called when the agent starts up.  At a minimum, registration of the MIB variables
 * structure (acseMIB_variables) should take place here.  By default the function also
 * registers the configuration handler and configuration store callbacks.
 *
 * Additional registrations that may be considered here are calls to regsiter_readfd(),
 * register_writefd() and register_exceptfd() for hooking into the snmpd event loop, but only when
 * used as a loadable module.  By default this function establishes a single file descriptor to
 * read, or upon which to handle exceptions.  Note that the snmpd only supports a maximum of 32
 * extneral file descriptors, so these should be used sparingly.
 *
 * When running as a loadable module, it is also necessary to hook into the snmpd event loop so that
 * the current request number can be deteremined.  This is accomplished by using a trick of the
 * external_signal_scheduled and external_signal_handler mechanism which is called on each event
 * loop when external_signal_scheduled is non-zero.  This is used to increment the sa_request value
 * on each snmpd event loop interation so that calls to MIB tree functions can determine whether
 * they belong to a fresh request or not (primarily for cacheing and possibly to clean up non-polled
 * file descriptors).
 */
void
init_acseMIB(void)
{
	(void) my_fd;
	(void) zeroDotZero_oid;
	(void) snmpTrapOID_oid;
	DEBUGMSGTL(("acseMIB", "init_acseMIB: initializing...  "));
	/* register ourselves with the agent to handle our mib tree */
	REGISTER_MIB("acseMIB", acseMIB_variables, variable7, acseMIB_variables_oid);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_SHUTDOWN, term_acseMIB, NULL);
	/* register our config handler(s) to deal with registrations */
	snmpd_register_config_handler("acseMIB", parse_acseMIB, NULL, "HELP STRING");
	snmpd_register_config_handler("acseTable", parse_acseTable, NULL, "HELP STRING");
	snmpd_register_config_handler("acseAssociationTable", parse_acseAssociationTable, NULL, "HELP STRING");
	snmpd_register_config_handler("apInvocationTable", parse_apInvocationTable, NULL, "HELP STRING");
	snmpd_register_config_handler("applicationEntityInvocationTable", parse_applicationEntityInvocationTable, NULL, "HELP STRING");
	snmpd_register_config_handler("applicationSubsystemTable", parse_applicationSubsystemTable, NULL, "HELP STRING");
	snmpd_register_config_handler("asoTable", parse_asoTable, NULL, "HELP STRING");
	snmpd_register_config_handler("asoEntityTable", parse_asoEntityTable, NULL, "HELP STRING");
	snmpd_register_config_handler("asoInvocationTable", parse_asoInvocationTable, NULL, "HELP STRING");
	snmpd_register_config_handler("monoULConnectionTable", parse_monoULConnectionTable, NULL, "HELP STRING");
	snmpd_register_config_handler("monoULEntityTable", parse_monoULEntityTable, NULL, "HELP STRING");
	snmpd_register_config_handler("monolithicUpperLayerSubsystemTable", parse_monolithicUpperLayerSubsystemTable, NULL, "HELP STRING");
	snmpd_register_config_handler("pSapTable", parse_pSapTable, NULL, "HELP STRING");
	snmpd_register_config_handler("presentationConnectionTable", parse_presentationConnectionTable, NULL, "HELP STRING");
	snmpd_register_config_handler("presentationCopmTable", parse_presentationCopmTable, NULL, "HELP STRING");
	snmpd_register_config_handler("presentationEntityTable", parse_presentationEntityTable, NULL, "HELP STRING");
	snmpd_register_config_handler("presentationSubsystemTable", parse_presentationSubsystemTable, NULL, "HELP STRING");
	snmpd_register_config_handler("sSapTable", parse_sSapTable, NULL, "HELP STRING");
	snmpd_register_config_handler("sessionConnectionTable", parse_sessionConnectionTable, NULL, "HELP STRING");
	snmpd_register_config_handler("sessionCopmTable", parse_sessionCopmTable, NULL, "HELP STRING");
	snmpd_register_config_handler("sessionEntityTable", parse_sessionEntityTable, NULL, "HELP STRING");
	snmpd_register_config_handler("sessionSubsystemTable", parse_sessionSubsystemTable, NULL, "HELP STRING");
	snmpd_register_config_handler("acseEntityTable", parse_acseEntityTable, NULL, "HELP STRING");
	snmpd_register_config_handler("acseAccessPointTable", parse_acseAccessPointTable, NULL, "HELP STRING");
	snmpd_register_config_handler("acseLinkageTable", parse_acseLinkageTable, NULL, "HELP STRING");

	/* we need to be called back later to store our data */
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_acseMIB, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_acseTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_acseAssociationTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_apInvocationTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_applicationEntityInvocationTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_applicationSubsystemTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_asoTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_asoEntityTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_asoInvocationTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_monoULConnectionTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_monoULEntityTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_monolithicUpperLayerSubsystemTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_pSapTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_presentationConnectionTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_presentationCopmTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_presentationEntityTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_presentationSubsystemTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_sSapTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_sessionConnectionTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_sessionCopmTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_sessionEntityTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_sessionSubsystemTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_acseEntityTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_acseAccessPointTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_acseLinkageTable, NULL);

	/* place any other initialization junk you need here */
	if (my_readfd >= 0) {
		register_readfd(my_readfd, acseMIB_fd_handler, (void *) 0);
		register_exceptfd(my_readfd, acseMIB_fd_handler, (void *) 1);
	}
	acseMIBold_signal_handler = external_signal_handler[SIGCHLD];
	external_signal_handler[SIGCHLD] = &acseMIB_loop_handler;
	external_signal_scheduled[SIGCHLD] = 1;
	DEBUGMSGTL(("acseMIB", "done.\n"));
}

/**
 * @fn void deinit_acseMIB(void)
 * @brief deinitialization routine.
 *
 * This is called before the agent is unloaded.  At a minimum, deregistration of the MIB variables
 * structure (acseMIB_variables) should take place here.  By default, the function also
 * deregisters the the configuration file handlers for the MIB variables and table rows.
 *
 * Additional deregistrations that may be required here are calls to unregister_readfd(),
 * unregister_writefd() and unregsiter_exceptfd() for unhooking from the snmpd event loop, but only
 * when used as a loadable module.  By default if a read file descriptor exists, it is unregistered.
 */
void
deinit_acseMIB(void)
{
	DEBUGMSGTL(("acseMIB", "deinit_acseMIB: deinitializating...  "));
	external_signal_handler[SIGCHLD] = acseMIBold_signal_handler;
	if (my_readfd >= 0) {
		unregister_exceptfd(my_readfd);
		unregister_readfd(my_readfd);
		close(my_readfd);
		my_readfd = -1;
	}
	unregister_mib(acseMIB_variables_oid, sizeof(acseMIB_variables_oid) / sizeof(oid));
	snmpd_unregister_config_handler("acseMIB");
	snmpd_unregister_config_handler("acseTable");
	snmpd_unregister_config_handler("acseAssociationTable");
	snmpd_unregister_config_handler("apInvocationTable");
	snmpd_unregister_config_handler("applicationEntityInvocationTable");
	snmpd_unregister_config_handler("applicationSubsystemTable");
	snmpd_unregister_config_handler("asoTable");
	snmpd_unregister_config_handler("asoEntityTable");
	snmpd_unregister_config_handler("asoInvocationTable");
	snmpd_unregister_config_handler("monoULConnectionTable");
	snmpd_unregister_config_handler("monoULEntityTable");
	snmpd_unregister_config_handler("monolithicUpperLayerSubsystemTable");
	snmpd_unregister_config_handler("pSapTable");
	snmpd_unregister_config_handler("presentationConnectionTable");
	snmpd_unregister_config_handler("presentationCopmTable");
	snmpd_unregister_config_handler("presentationEntityTable");
	snmpd_unregister_config_handler("presentationSubsystemTable");
	snmpd_unregister_config_handler("sSapTable");
	snmpd_unregister_config_handler("sessionConnectionTable");
	snmpd_unregister_config_handler("sessionCopmTable");
	snmpd_unregister_config_handler("sessionEntityTable");
	snmpd_unregister_config_handler("sessionSubsystemTable");
	snmpd_unregister_config_handler("acseEntityTable");
	snmpd_unregister_config_handler("acseAccessPointTable");
	snmpd_unregister_config_handler("acseLinkageTable");

	/* place any other de-initialization junk you need here */
	DEBUGMSGTL(("acseMIB", "done.\n"));
}

int
term_acseMIB(int majorID, int minorID, void *serverarg, void *clientarg)
{
	DEBUGMSGTL(("acseMIB", "term_acseMIB: terminating...  "));
	deinit_acseMIB();
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return 0;
}

/**
 * @fn struct acseMIB_data *acseMIB_create(void)
 * @brief create a fresh data structure representing scalars in acseMIB.
 *
 * Creates a new acseMIB_data structure by allocating dynamic memory for the structure and
 * initializing the default values of scalars in acseMIB.
 */
struct acseMIB_data *
acseMIB_create(void)
{
	struct acseMIB_data *StorageNew = SNMP_MALLOC_STRUCT(acseMIB_data);

	DEBUGMSGTL(("acseMIB", "acseMIB_create: creating scalars...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default scalar values here into StorageNew */

	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct acseMIB_data *acseMIB_duplicate(struct acseMIB_data *thedata)
 * @param thedata the mib structure to duplicate
 * @brief duplicate a mib structure for the mib
 *
 * Duplicates the specified mib structure @param thedata and returns a pointer to the newly
 * allocated mib structure on success, or NULL on failure.
 */
struct acseMIB_data *
acseMIB_duplicate(struct acseMIB_data *thedata)
{
	struct acseMIB_data *StorageNew = SNMP_MALLOC_STRUCT(acseMIB_data);

	DEBUGMSGTL(("acseMIB", "acseMIB_duplicate: duplicating mib... "));
	if (StorageNew != NULL) {
		StorageNew->acseTcUserId = thedata->acseTcUserId;
		StorageNew->acseApplicationEntityId = thedata->acseApplicationEntityId;
	}
      done:
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	acseMIB_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int acseMIB_destroy(struct acseMIB_data **thedata)
 * @param thedata pointer to the data structure in acseMIB.
 * @brief delete a scalars structure from acseMIB.
 *
 * Frees scalars that were previously removed from acseMIB.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
acseMIB_destroy(struct acseMIB_data **thedata)
{
	struct acseMIB_data *StorageDel;

	DEBUGMSGTL(("acseMIB", "acseMIB_destroy: deleting scalars...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int acseMIB_add(struct acseMIB_data *thedata)
 * @param thedata the structure representing acseMIB scalars.
 * @brief adds node to the acseMIB scalar data set.
 *
 * Adds a scalar structure to the acseMIB data set.  Note that this function is necessary even
 * when the scalar values are not peristent.
 */
int
acseMIB_add(struct acseMIB_data *thedata)
{
	DEBUGMSGTL(("acseMIB", "acseMIB_add: adding data...  "));
	if (thedata)
		acseMIBStorage = thedata;
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_acseMIB(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for acseMIB entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case acseMIB).  This routine is invoked by
 * UCD-SNMP to read the values of scalars in the MIB from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the MIB.  If there are no configured entries
 * in the configuration MIB, this function will simply not be called.
 */
void
parse_acseMIB(const char *token, char *line)
{
	size_t tmpsize;
	struct acseMIB_data *StorageTmp = acseMIB_create();

	DEBUGMSGTL(("acseMIB", "parse_acseMIB: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual scalars that are not persistent */
	acseMIB_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("acseMIB", "done.\n"));
}

/*
 * store_acseMIB(): stores .conf file entries needed to configure the mib.
 */
int
store_acseMIB(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct acseMIB_data *StorageTmp;

	DEBUGMSGTL(("acseMIB", "store_acseMIB: storing data...  "));
	refresh_acseMIB(1);
	if ((StorageTmp = acseMIBStorage) == NULL) {
		DEBUGMSGTL(("acseMIB", "error.\n"));
		return SNMPERR_GENERR;
	}
	(void) tmpsize;
	/* XXX: comment entire section if no scalars are persistent */
	{
		memset(line, 0, sizeof(line));
		strcat(line, "acseMIB ");
		cptr = line + strlen(line);
		(void) cptr;
		/* XXX: remove individual scalars that are not persistent */
		snmpd_store_config(line);
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int check_acseMIB(struct acseMIB_data *StorageTmp, struct acseMIB_data *StorageOld)
 * @param StorageTmp the data as updated
 * @param StorageOld the data previous to update
 *
 * This function is used by mibs.  It is used to check, all scalars at a time, the varbinds
 * belonging to the mib.  This function is called for the first varbind in a mib at the beginning of
 * the ACTION phase.  The COMMIT phase does not ensue unless this check passes.  This function can
 * return SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before
 * the varbinds on the mib were applied; the values in StorageTmp are the new values.  The function
 * is permitted to change the values in StorageTmp to correct them; however, preferences should be
 * made for setting values that were not in the varbinds.
 */
int
check_acseMIB(struct acseMIB_data *StorageTmp, struct acseMIB_data *StorageOld)
{
	/* XXX: provide code to check the scalars for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_acseMIB(struct acseMIB_data *StorageTmp, struct acseMIB_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase)
 *
 * This function is used by mibs.  It is used to update, all scalars at a time, the varbinds
 * belonging to the mib.  This function is called for the first varbind in a mib at the beginning of
 * the COMMIT phase.  The start of the ACTION phase performs a consistency check on the mib before
 * allowing the request to proceed to the COMMIT phase.  The COMMIT phase then arrives here with
 * consistency already checked (see check_acseMIB()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the mib were applied: the values in StorageTmp are the new values.
 */
int
update_acseMIB(struct acseMIB_data *StorageTmp, struct acseMIB_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	acseMIB_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn revert_acseMIB(struct 
 * @fn void revert_acseMIB(struct acseMIB_data *StorageTmp, struct acseMIB_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase)
 */
void
revert_acseMIB(struct acseMIB_data *StorageTmp, struct acseMIB_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_acseMIB(StorageOld, NULL);
}

/**
 * @fn void refresh_acseMIB(int force)
 * @param force forced refresh when non-zero.
 * @brief refresh the scalar values of acseMIB.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a scalar has been requested).
 */
void
refresh_acseMIB(int force)
{
	if (acseMIBStorage == NULL) {
		struct acseMIB_data *StorageNew;

		if ((StorageNew = acseMIB_create()) == NULL)
			return;
		acseMIBStorage = StorageNew;
		acseMIB_refresh = 1;
	}
	if (!force && acseMIB_refresh == 0)
		return;
	DEBUGMSGTL(("acseMIB", "refresh_acseMIB: refreshing...  "));
	/* XXX: Update scalars as required here... */
	acseMIB_refresh = 0;
	DEBUGMSGTL(("acseMIB", "done.\n"));
}

/**
 * @fn u_char * var_acseMIB(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @param vp a pointer to the entry in the variables table for the requested variable.
 * @param name the object identifier for which to find.
 * @param length the length of the object identifier.
 * @param exact whether the name is exact.
 * @param var_len a pointer to the length of the representation of the object.
 * @param write_method a pointer to a write method for the object.
 * @brief locate variables in acseMIB.
 *
 * This function returns a pointer to a memory area that is static across the request that contains
 * the UCD-SNMP representation of the scalar (so that it may be used to read from for a GET,
 * GET-NEXT or GET-BULK request).  This returned pointer may be NULL, in which case the function is
 * telling UCD-SNMP that the scalar does not exist for reading; however, if write_method is
 * overwritten with a non-NULL value, the function is telling UCD-SNMP that the scalar exists for
 * writing.  Write-only objects can be effected in this way.
 */
u_char *
var_acseMIB(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct acseMIB_data *StorageTmp;
	u_char *rval;

	DEBUGMSGTL(("acseMIB", "var_acseMIB: lookup up varbind...  "));
	if (header_generic(vp, name, length, exact, var_len, write_method) == MATCH_FAILED)
		return NULL;
	/* Refresh the MIB values if required. */
	refresh_acseMIB(0);
	if ((StorageTmp = acseMIBStorage) == NULL) {
		DEBUGMSGTL(("acseMIB", "no datastructure.\n"));
		return NULL;
	}
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	default:
		ERROR_MSG("");
	}
	if (rval)
		DEBUGMSGTL(("acseMIB", "found.\n"));
	else
		DEBUGMSGTL(("acseMIB", "not found.\n"));
	return (rval);
}

/**
 * @fn struct acseTable_data *acseTable_create(void)
 * @brief create a fresh data structure representing a new row in the acseTable table.
 *
 * Creates a new acseTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct acseTable_data *
acseTable_create(void)
{
	struct acseTable_data *StorageNew = SNMP_MALLOC_STRUCT(acseTable_data);

	DEBUGMSGTL(("acseMIB", "acseTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->subsystemId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->subsystemIdLen = 0;
		StorageNew->subsystemId[StorageNew->subsystemIdLen] = 0;
		if ((StorageNew->applicationProcessId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->applicationProcessIdLen = 0;
		StorageNew->applicationProcessId[StorageNew->applicationProcessIdLen] = 0;
		if ((StorageNew->asoEntityId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->asoEntityIdLen = 0;
		StorageNew->asoEntityId[StorageNew->asoEntityIdLen] = 0;
		if ((StorageNew->asoId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->asoIdLen = 0;
		StorageNew->asoId[StorageNew->asoIdLen] = 0;
		if (memdup((u_char **) &StorageNew->acseFUinitiator, (u_char *) "\x00", 1) != SNMPERR_SUCCESS)
			goto nomem;
			StorageNew->acseFUinitiatorLen = 1;
		if (memdup((u_char **) &StorageNew->acseEditionsSupported, (u_char *) "\x00", 1) != SNMPERR_SUCCESS)
			goto nomem;
			StorageNew->acseEditionsSupportedLen = 1;
		if (memdup((u_char **) &StorageNew->acseFUresponder, (u_char *) "\x00", 1) != SNMPERR_SUCCESS)
			goto nomem;
			StorageNew->acseFUresponderLen = 1;
	}
      done:
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	acseTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct acseTable_data *acseTable_duplicate(struct acseTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct acseTable_data *
acseTable_duplicate(struct acseTable_data *thedata)
{
	struct acseTable_data *StorageNew = SNMP_MALLOC_STRUCT(acseTable_data);

	DEBUGMSGTL(("acseMIB", "acseTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		if (!(StorageNew->subsystemId = malloc(thedata->subsystemIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->subsystemId, thedata->subsystemId, thedata->subsystemIdLen);
		StorageNew->subsystemIdLen = thedata->subsystemIdLen;
		StorageNew->subsystemId[StorageNew->subsystemIdLen] = 0;
		if (!(StorageNew->applicationProcessId = malloc(thedata->applicationProcessIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->applicationProcessId, thedata->applicationProcessId, thedata->applicationProcessIdLen);
		StorageNew->applicationProcessIdLen = thedata->applicationProcessIdLen;
		StorageNew->applicationProcessId[StorageNew->applicationProcessIdLen] = 0;
		if (!(StorageNew->asoEntityId = malloc(thedata->asoEntityIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->asoEntityId, thedata->asoEntityId, thedata->asoEntityIdLen);
		StorageNew->asoEntityIdLen = thedata->asoEntityIdLen;
		StorageNew->asoEntityId[StorageNew->asoEntityIdLen] = 0;
		if (!(StorageNew->asoId = malloc(thedata->asoIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->asoId, thedata->asoId, thedata->asoIdLen);
		StorageNew->asoIdLen = thedata->asoIdLen;
		StorageNew->asoId[StorageNew->asoIdLen] = 0;
		if (!(StorageNew->acseFUinitiator = malloc(thedata->acseFUinitiatorLen + 1)))
			goto destroy;
		memcpy(StorageNew->acseFUinitiator, thedata->acseFUinitiator, thedata->acseFUinitiatorLen);
		StorageNew->acseFUinitiatorLen = thedata->acseFUinitiatorLen;
		StorageNew->acseFUinitiator[StorageNew->acseFUinitiatorLen] = 0;
		if (!(StorageNew->acseEditionsSupported = malloc(thedata->acseEditionsSupportedLen + 1)))
			goto destroy;
		memcpy(StorageNew->acseEditionsSupported, thedata->acseEditionsSupported, thedata->acseEditionsSupportedLen);
		StorageNew->acseEditionsSupportedLen = thedata->acseEditionsSupportedLen;
		StorageNew->acseEditionsSupported[StorageNew->acseEditionsSupportedLen] = 0;
		if (!(StorageNew->acseFUresponder = malloc(thedata->acseFUresponderLen + 1)))
			goto destroy;
		memcpy(StorageNew->acseFUresponder, thedata->acseFUresponder, thedata->acseFUresponderLen);
		StorageNew->acseFUresponderLen = thedata->acseFUresponderLen;
		StorageNew->acseFUresponder[StorageNew->acseFUresponderLen] = 0;
	}
      done:
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	acseTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int acseTable_destroy(struct acseTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
acseTable_destroy(struct acseTable_data **thedata)
{
	struct acseTable_data *StorageDel;

	DEBUGMSGTL(("acseMIB", "acseTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->subsystemId);
		StorageDel->subsystemIdLen = 0;
		SNMP_FREE(StorageDel->applicationProcessId);
		StorageDel->applicationProcessIdLen = 0;
		SNMP_FREE(StorageDel->asoEntityId);
		StorageDel->asoEntityIdLen = 0;
		SNMP_FREE(StorageDel->asoId);
		StorageDel->asoIdLen = 0;
		SNMP_FREE(StorageDel->acseFUinitiator);
		StorageDel->acseFUinitiatorLen = 0;
		SNMP_FREE(StorageDel->acseEditionsSupported);
		StorageDel->acseEditionsSupportedLen = 0;
		SNMP_FREE(StorageDel->acseFUresponder);
		StorageDel->acseFUresponderLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int acseTable_add(struct acseTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the acseTable table data set.
 *
 * Adds a table row structure to the acseTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
acseTable_add(struct acseTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("acseMIB", "acseTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* subsystemId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->subsystemId, thedata->subsystemIdLen);
		/* applicationProcessId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->applicationProcessId, thedata->applicationProcessIdLen);
		/* asoEntityId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->asoEntityId, thedata->asoEntityIdLen);
		/* asoId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->asoId, thedata->asoIdLen);
		header_complex_add_data(&acseTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("acseMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int acseTable_del(struct acseTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the acseTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
acseTable_del(struct acseTable_data *thedata)
{
	struct acseTable_data *StorageDel;

	DEBUGMSGTL(("acseMIB", "acseTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(acseTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&acseTableStorage, hciptr);
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_acseTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for acseTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case acseTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_acseTable(const char *token, char *line)
{
	size_t tmpsize;
	struct acseTable_data *StorageTmp = acseTable_create();

	DEBUGMSGTL(("acseMIB", "parse_acseTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->subsystemId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->subsystemId, &StorageTmp->subsystemIdLen);
	if (StorageTmp->subsystemId == NULL) {
		config_perror("invalid specification for subsystemId");
		return;
	}
	SNMP_FREE(StorageTmp->applicationProcessId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->applicationProcessId, &StorageTmp->applicationProcessIdLen);
	if (StorageTmp->applicationProcessId == NULL) {
		config_perror("invalid specification for applicationProcessId");
		return;
	}
	SNMP_FREE(StorageTmp->asoEntityId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->asoEntityId, &StorageTmp->asoEntityIdLen);
	if (StorageTmp->asoEntityId == NULL) {
		config_perror("invalid specification for asoEntityId");
		return;
	}
	SNMP_FREE(StorageTmp->asoId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->asoId, &StorageTmp->asoIdLen);
	if (StorageTmp->asoId == NULL) {
		config_perror("invalid specification for asoId");
		return;
	}
	SNMP_FREE(StorageTmp->acseFUinitiator);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->acseFUinitiator, &StorageTmp->acseFUinitiatorLen);
	if (StorageTmp->acseFUinitiator == NULL) {
		config_perror("invalid specification for acseFUinitiator");
		return;
	}
	SNMP_FREE(StorageTmp->acseEditionsSupported);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->acseEditionsSupported, &StorageTmp->acseEditionsSupportedLen);
	if (StorageTmp->acseEditionsSupported == NULL) {
		config_perror("invalid specification for acseEditionsSupported");
		return;
	}
	SNMP_FREE(StorageTmp->acseFUresponder);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->acseFUresponder, &StorageTmp->acseFUresponderLen);
	if (StorageTmp->acseFUresponder == NULL) {
		config_perror("invalid specification for acseFUresponder");
		return;
	}
	acseTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("acseMIB", "done.\n"));
}

/*
 * store_acseTable(): store configuraiton file for acseTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_acseTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct acseTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("acseMIB", "store_acseTable: storing data...  "));
	refresh_acseTable(1);
	(void) tmpsize;
	for (hcindex = acseTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct acseTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "acseTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->subsystemId, &StorageTmp->subsystemIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->applicationProcessId, &StorageTmp->applicationProcessIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->asoEntityId, &StorageTmp->asoEntityIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->asoId, &StorageTmp->asoIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->acseFUinitiator, &StorageTmp->acseFUinitiatorLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->acseEditionsSupported, &StorageTmp->acseEditionsSupportedLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->acseFUresponder, &StorageTmp->acseFUresponderLen);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct acseAssociationTable_data *acseAssociationTable_create(void)
 * @brief create a fresh data structure representing a new row in the acseAssociationTable table.
 *
 * Creates a new acseAssociationTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct acseAssociationTable_data *
acseAssociationTable_create(void)
{
	struct acseAssociationTable_data *StorageNew = SNMP_MALLOC_STRUCT(acseAssociationTable_data);

	DEBUGMSGTL(("acseMIB", "acseAssociationTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->applicationSubsystemId = 0;
		if ((StorageNew->apInvocationId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->apInvocationIdLen = 0;
		StorageNew->apInvocationId[StorageNew->apInvocationIdLen] = 0;
		if ((StorageNew->asoInvocationId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->asoInvocationIdLen = 0;
		StorageNew->asoInvocationId[StorageNew->asoInvocationIdLen] = 0;
		if ((StorageNew->acseAssociationCallingAEtitle = malloc(1)) == NULL)
			goto nomem;
		StorageNew->acseAssociationCallingAEtitleLen = 0;
		StorageNew->acseAssociationCallingAEtitle[StorageNew->acseAssociationCallingAEtitleLen] = 0;
		if (memdup((u_char **) &StorageNew->acseAssociationFUinUse, (u_char *) "\x00", 1) != SNMPERR_SUCCESS)
			goto nomem;
			StorageNew->acseAssociationFUinUseLen = 1;
		if ((StorageNew->acseAssociationApplContextInUse = snmp_duplicate_objid(zeroDotZero_oid, 2)) == NULL)
			goto nomem;
			StorageNew->acseAssociationApplContextInUseLen = 2;
		if ((StorageNew->acseAssociationParentAsoinvoc = snmp_duplicate_objid(zeroDotZero_oid, 2)) == NULL)
			goto nomem;
			StorageNew->acseAssociationParentAsoinvocLen = 2;
		if ((StorageNew->acseAssociationCalledAEtitle = malloc(1)) == NULL)
			goto nomem;
		StorageNew->acseAssociationCalledAEtitleLen = 0;
		StorageNew->acseAssociationCalledAEtitle[StorageNew->acseAssociationCalledAEtitleLen] = 0;
		StorageNew->acseAssociationRowStatus = 0;
		StorageNew->acseAssociationRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	acseAssociationTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct acseAssociationTable_data *acseAssociationTable_duplicate(struct acseAssociationTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct acseAssociationTable_data *
acseAssociationTable_duplicate(struct acseAssociationTable_data *thedata)
{
	struct acseAssociationTable_data *StorageNew = SNMP_MALLOC_STRUCT(acseAssociationTable_data);

	DEBUGMSGTL(("acseMIB", "acseAssociationTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->applicationSubsystemId = thedata->applicationSubsystemId;
		if (!(StorageNew->apInvocationId = malloc(thedata->apInvocationIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->apInvocationId, thedata->apInvocationId, thedata->apInvocationIdLen);
		StorageNew->apInvocationIdLen = thedata->apInvocationIdLen;
		StorageNew->apInvocationId[StorageNew->apInvocationIdLen] = 0;
		if (!(StorageNew->asoInvocationId = malloc(thedata->asoInvocationIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->asoInvocationId, thedata->asoInvocationId, thedata->asoInvocationIdLen);
		StorageNew->asoInvocationIdLen = thedata->asoInvocationIdLen;
		StorageNew->asoInvocationId[StorageNew->asoInvocationIdLen] = 0;
		if (!(StorageNew->acseAssociationCallingAEtitle = malloc(thedata->acseAssociationCallingAEtitleLen + 1)))
			goto destroy;
		memcpy(StorageNew->acseAssociationCallingAEtitle, thedata->acseAssociationCallingAEtitle, thedata->acseAssociationCallingAEtitleLen);
		StorageNew->acseAssociationCallingAEtitleLen = thedata->acseAssociationCallingAEtitleLen;
		StorageNew->acseAssociationCallingAEtitle[StorageNew->acseAssociationCallingAEtitleLen] = 0;
		if (!(StorageNew->acseAssociationFUinUse = malloc(thedata->acseAssociationFUinUseLen + 1)))
			goto destroy;
		memcpy(StorageNew->acseAssociationFUinUse, thedata->acseAssociationFUinUse, thedata->acseAssociationFUinUseLen);
		StorageNew->acseAssociationFUinUseLen = thedata->acseAssociationFUinUseLen;
		StorageNew->acseAssociationFUinUse[StorageNew->acseAssociationFUinUseLen] = 0;
		if (!(StorageNew->acseAssociationApplContextInUse = snmp_duplicate_objid(thedata->acseAssociationApplContextInUse, thedata->acseAssociationApplContextInUseLen / sizeof(oid))))
			goto destroy;
		StorageNew->acseAssociationApplContextInUseLen = thedata->acseAssociationApplContextInUseLen;
		if (!(StorageNew->acseAssociationParentAsoinvoc = snmp_duplicate_objid(thedata->acseAssociationParentAsoinvoc, thedata->acseAssociationParentAsoinvocLen / sizeof(oid))))
			goto destroy;
		StorageNew->acseAssociationParentAsoinvocLen = thedata->acseAssociationParentAsoinvocLen;
		if (!(StorageNew->acseAssociationCalledAEtitle = malloc(thedata->acseAssociationCalledAEtitleLen + 1)))
			goto destroy;
		memcpy(StorageNew->acseAssociationCalledAEtitle, thedata->acseAssociationCalledAEtitle, thedata->acseAssociationCalledAEtitleLen);
		StorageNew->acseAssociationCalledAEtitleLen = thedata->acseAssociationCalledAEtitleLen;
		StorageNew->acseAssociationCalledAEtitle[StorageNew->acseAssociationCalledAEtitleLen] = 0;
		StorageNew->acseAssociationRowStatus = thedata->acseAssociationRowStatus;
	}
      done:
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	acseAssociationTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int acseAssociationTable_destroy(struct acseAssociationTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
acseAssociationTable_destroy(struct acseAssociationTable_data **thedata)
{
	struct acseAssociationTable_data *StorageDel;

	DEBUGMSGTL(("acseMIB", "acseAssociationTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->apInvocationId);
		StorageDel->apInvocationIdLen = 0;
		SNMP_FREE(StorageDel->asoInvocationId);
		StorageDel->asoInvocationIdLen = 0;
		SNMP_FREE(StorageDel->acseAssociationCallingAEtitle);
		StorageDel->acseAssociationCallingAEtitleLen = 0;
		SNMP_FREE(StorageDel->acseAssociationFUinUse);
		StorageDel->acseAssociationFUinUseLen = 0;
		SNMP_FREE(StorageDel->acseAssociationApplContextInUse);
		StorageDel->acseAssociationApplContextInUseLen = 0;
		SNMP_FREE(StorageDel->acseAssociationParentAsoinvoc);
		StorageDel->acseAssociationParentAsoinvocLen = 0;
		SNMP_FREE(StorageDel->acseAssociationCalledAEtitle);
		StorageDel->acseAssociationCalledAEtitleLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int acseAssociationTable_add(struct acseAssociationTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the acseAssociationTable table data set.
 *
 * Adds a table row structure to the acseAssociationTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
acseAssociationTable_add(struct acseAssociationTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("acseMIB", "acseAssociationTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* applicationSubsystemId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->applicationSubsystemId, sizeof(thedata->applicationSubsystemId));
		/* apInvocationId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->apInvocationId, thedata->apInvocationIdLen);
		/* asoInvocationId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->asoInvocationId, thedata->asoInvocationIdLen);
		header_complex_add_data(&acseAssociationTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("acseMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int acseAssociationTable_del(struct acseAssociationTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the acseAssociationTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
acseAssociationTable_del(struct acseAssociationTable_data *thedata)
{
	struct acseAssociationTable_data *StorageDel;

	DEBUGMSGTL(("acseMIB", "acseAssociationTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(acseAssociationTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&acseAssociationTableStorage, hciptr);
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_acseAssociationTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for acseAssociationTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case acseAssociationTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_acseAssociationTable(const char *token, char *line)
{
	size_t tmpsize;
	struct acseAssociationTable_data *StorageTmp = acseAssociationTable_create();

	DEBUGMSGTL(("acseMIB", "parse_acseAssociationTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->applicationSubsystemId, &tmpsize);
	SNMP_FREE(StorageTmp->apInvocationId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->apInvocationId, &StorageTmp->apInvocationIdLen);
	if (StorageTmp->apInvocationId == NULL) {
		config_perror("invalid specification for apInvocationId");
		return;
	}
	SNMP_FREE(StorageTmp->asoInvocationId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->asoInvocationId, &StorageTmp->asoInvocationIdLen);
	if (StorageTmp->asoInvocationId == NULL) {
		config_perror("invalid specification for asoInvocationId");
		return;
	}
	SNMP_FREE(StorageTmp->acseAssociationCallingAEtitle);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->acseAssociationCallingAEtitle, &StorageTmp->acseAssociationCallingAEtitleLen);
	if (StorageTmp->acseAssociationCallingAEtitle == NULL) {
		config_perror("invalid specification for acseAssociationCallingAEtitle");
		return;
	}
	SNMP_FREE(StorageTmp->acseAssociationFUinUse);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->acseAssociationFUinUse, &StorageTmp->acseAssociationFUinUseLen);
	if (StorageTmp->acseAssociationFUinUse == NULL) {
		config_perror("invalid specification for acseAssociationFUinUse");
		return;
	}
	SNMP_FREE(StorageTmp->acseAssociationApplContextInUse);
	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->acseAssociationApplContextInUse, &StorageTmp->acseAssociationApplContextInUseLen);
	if (StorageTmp->acseAssociationApplContextInUse == NULL) {
		config_perror("invalid specification for acseAssociationApplContextInUse");
		return;
	}
	SNMP_FREE(StorageTmp->acseAssociationParentAsoinvoc);
	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->acseAssociationParentAsoinvoc, &StorageTmp->acseAssociationParentAsoinvocLen);
	if (StorageTmp->acseAssociationParentAsoinvoc == NULL) {
		config_perror("invalid specification for acseAssociationParentAsoinvoc");
		return;
	}
	SNMP_FREE(StorageTmp->acseAssociationCalledAEtitle);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->acseAssociationCalledAEtitle, &StorageTmp->acseAssociationCalledAEtitleLen);
	if (StorageTmp->acseAssociationCalledAEtitle == NULL) {
		config_perror("invalid specification for acseAssociationCalledAEtitle");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->acseAssociationRowStatus, &tmpsize);
	acseAssociationTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("acseMIB", "done.\n"));
}

/*
 * store_acseAssociationTable(): store configuraiton file for acseAssociationTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_acseAssociationTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct acseAssociationTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("acseMIB", "store_acseAssociationTable: storing data...  "));
	refresh_acseAssociationTable(1);
	(void) tmpsize;
	for (hcindex = acseAssociationTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct acseAssociationTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "acseAssociationTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->applicationSubsystemId, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->apInvocationId, &StorageTmp->apInvocationIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->asoInvocationId, &StorageTmp->asoInvocationIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->acseAssociationCallingAEtitle, &StorageTmp->acseAssociationCallingAEtitleLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->acseAssociationFUinUse, &StorageTmp->acseAssociationFUinUseLen);
			cptr = read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->acseAssociationApplContextInUse, &StorageTmp->acseAssociationApplContextInUseLen);
			cptr = read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->acseAssociationParentAsoinvoc, &StorageTmp->acseAssociationParentAsoinvocLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->acseAssociationCalledAEtitle, &StorageTmp->acseAssociationCalledAEtitleLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->acseAssociationRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct apInvocationTable_data *apInvocationTable_create(void)
 * @brief create a fresh data structure representing a new row in the apInvocationTable table.
 *
 * Creates a new apInvocationTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct apInvocationTable_data *
apInvocationTable_create(void)
{
	struct apInvocationTable_data *StorageNew = SNMP_MALLOC_STRUCT(apInvocationTable_data);

	DEBUGMSGTL(("acseMIB", "apInvocationTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->applicationSubsystemId = 0;
		if ((StorageNew->apInvocationOfPointer = snmp_duplicate_objid(zeroDotZero_oid, 2)) == NULL)
			goto nomem;
			StorageNew->apInvocationOfPointerLen = 2;
		StorageNew->apInvocationRowStatus = 0;
		StorageNew->apInvocationRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	apInvocationTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct apInvocationTable_data *apInvocationTable_duplicate(struct apInvocationTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct apInvocationTable_data *
apInvocationTable_duplicate(struct apInvocationTable_data *thedata)
{
	struct apInvocationTable_data *StorageNew = SNMP_MALLOC_STRUCT(apInvocationTable_data);

	DEBUGMSGTL(("acseMIB", "apInvocationTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->applicationSubsystemId = thedata->applicationSubsystemId;
		if (!(StorageNew->apInvocationId = malloc(thedata->apInvocationIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->apInvocationId, thedata->apInvocationId, thedata->apInvocationIdLen);
		StorageNew->apInvocationIdLen = thedata->apInvocationIdLen;
		StorageNew->apInvocationId[StorageNew->apInvocationIdLen] = 0;
		if (!(StorageNew->apInvocationOfPointer = snmp_duplicate_objid(thedata->apInvocationOfPointer, thedata->apInvocationOfPointerLen / sizeof(oid))))
			goto destroy;
		StorageNew->apInvocationOfPointerLen = thedata->apInvocationOfPointerLen;
		StorageNew->apInvocationRowStatus = thedata->apInvocationRowStatus;
	}
      done:
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	apInvocationTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int apInvocationTable_destroy(struct apInvocationTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
apInvocationTable_destroy(struct apInvocationTable_data **thedata)
{
	struct apInvocationTable_data *StorageDel;

	DEBUGMSGTL(("acseMIB", "apInvocationTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->apInvocationId);
		StorageDel->apInvocationIdLen = 0;
		SNMP_FREE(StorageDel->apInvocationOfPointer);
		StorageDel->apInvocationOfPointerLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int apInvocationTable_add(struct apInvocationTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the apInvocationTable table data set.
 *
 * Adds a table row structure to the apInvocationTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
apInvocationTable_add(struct apInvocationTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("acseMIB", "apInvocationTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* applicationSubsystemId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->applicationSubsystemId, sizeof(thedata->applicationSubsystemId));
		/* apInvocationId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->apInvocationId, thedata->apInvocationIdLen);
		header_complex_add_data(&apInvocationTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("acseMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int apInvocationTable_del(struct apInvocationTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the apInvocationTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
apInvocationTable_del(struct apInvocationTable_data *thedata)
{
	struct apInvocationTable_data *StorageDel;

	DEBUGMSGTL(("acseMIB", "apInvocationTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(apInvocationTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&apInvocationTableStorage, hciptr);
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_apInvocationTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for apInvocationTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case apInvocationTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_apInvocationTable(const char *token, char *line)
{
	size_t tmpsize;
	struct apInvocationTable_data *StorageTmp = apInvocationTable_create();

	DEBUGMSGTL(("acseMIB", "parse_apInvocationTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->applicationSubsystemId, &tmpsize);
	SNMP_FREE(StorageTmp->apInvocationId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->apInvocationId, &StorageTmp->apInvocationIdLen);
	if (StorageTmp->apInvocationId == NULL) {
		config_perror("invalid specification for apInvocationId");
		return;
	}
	SNMP_FREE(StorageTmp->apInvocationOfPointer);
	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->apInvocationOfPointer, &StorageTmp->apInvocationOfPointerLen);
	if (StorageTmp->apInvocationOfPointer == NULL) {
		config_perror("invalid specification for apInvocationOfPointer");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->apInvocationRowStatus, &tmpsize);
	apInvocationTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("acseMIB", "done.\n"));
}

/*
 * store_apInvocationTable(): store configuraiton file for apInvocationTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_apInvocationTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct apInvocationTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("acseMIB", "store_apInvocationTable: storing data...  "));
	refresh_apInvocationTable(1);
	(void) tmpsize;
	for (hcindex = apInvocationTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct apInvocationTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "apInvocationTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->applicationSubsystemId, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->apInvocationId, &StorageTmp->apInvocationIdLen);
			cptr = read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->apInvocationOfPointer, &StorageTmp->apInvocationOfPointerLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->apInvocationRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct applicationEntityInvocationTable_data *applicationEntityInvocationTable_create(void)
 * @brief create a fresh data structure representing a new row in the applicationEntityInvocationTable table.
 *
 * Creates a new applicationEntityInvocationTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct applicationEntityInvocationTable_data *
applicationEntityInvocationTable_create(void)
{
	struct applicationEntityInvocationTable_data *StorageNew = SNMP_MALLOC_STRUCT(applicationEntityInvocationTable_data);

	DEBUGMSGTL(("acseMIB", "applicationEntityInvocationTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->applicationSubsystemId = 0;
		if ((StorageNew->apInvocationId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->apInvocationIdLen = 0;
		StorageNew->apInvocationId[StorageNew->apInvocationIdLen] = 0;
		if ((StorageNew->applicationEntityInvocationUnderlyingConnectionNames = snmp_duplicate_objid(zeroDotZero_oid, 2)) == NULL)
			goto nomem;
			StorageNew->applicationEntityInvocationUnderlyingConnectionNamesLen = 2;
		if ((StorageNew->applicationEntityInvocationSupportedConnectionNames = snmp_duplicate_objid(zeroDotZero_oid, 2)) == NULL)
			goto nomem;
			StorageNew->applicationEntityInvocationSupportedConnectionNamesLen = 2;
		if ((StorageNew->applicationEntityInvocationInvocationOfPointer = snmp_duplicate_objid(zeroDotZero_oid, 2)) == NULL)
			goto nomem;
			StorageNew->applicationEntityInvocationInvocationOfPointerLen = 2;
		StorageNew->applicationEntityInvocationRowStatus = 0;
		StorageNew->applicationEntityInvocationRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	applicationEntityInvocationTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct applicationEntityInvocationTable_data *applicationEntityInvocationTable_duplicate(struct applicationEntityInvocationTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct applicationEntityInvocationTable_data *
applicationEntityInvocationTable_duplicate(struct applicationEntityInvocationTable_data *thedata)
{
	struct applicationEntityInvocationTable_data *StorageNew = SNMP_MALLOC_STRUCT(applicationEntityInvocationTable_data);

	DEBUGMSGTL(("acseMIB", "applicationEntityInvocationTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->applicationSubsystemId = thedata->applicationSubsystemId;
		if (!(StorageNew->apInvocationId = malloc(thedata->apInvocationIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->apInvocationId, thedata->apInvocationId, thedata->apInvocationIdLen);
		StorageNew->apInvocationIdLen = thedata->apInvocationIdLen;
		StorageNew->apInvocationId[StorageNew->apInvocationIdLen] = 0;
		if (!
		    (StorageNew->applicationEntityInvocationUnderlyingConnectionNames =
		     snmp_duplicate_objid(thedata->applicationEntityInvocationUnderlyingConnectionNames, thedata->applicationEntityInvocationUnderlyingConnectionNamesLen / sizeof(oid))))
			goto destroy;
		StorageNew->applicationEntityInvocationUnderlyingConnectionNamesLen = thedata->applicationEntityInvocationUnderlyingConnectionNamesLen;
		if (!
		    (StorageNew->applicationEntityInvocationSupportedConnectionNames =
		     snmp_duplicate_objid(thedata->applicationEntityInvocationSupportedConnectionNames, thedata->applicationEntityInvocationSupportedConnectionNamesLen / sizeof(oid))))
			goto destroy;
		StorageNew->applicationEntityInvocationSupportedConnectionNamesLen = thedata->applicationEntityInvocationSupportedConnectionNamesLen;
		if (!
		    (StorageNew->applicationEntityInvocationInvocationOfPointer =
		     snmp_duplicate_objid(thedata->applicationEntityInvocationInvocationOfPointer, thedata->applicationEntityInvocationInvocationOfPointerLen / sizeof(oid))))
			goto destroy;
		StorageNew->applicationEntityInvocationInvocationOfPointerLen = thedata->applicationEntityInvocationInvocationOfPointerLen;
		StorageNew->applicationEntityInvocationRowStatus = thedata->applicationEntityInvocationRowStatus;
	}
      done:
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	applicationEntityInvocationTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int applicationEntityInvocationTable_destroy(struct applicationEntityInvocationTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
applicationEntityInvocationTable_destroy(struct applicationEntityInvocationTable_data **thedata)
{
	struct applicationEntityInvocationTable_data *StorageDel;

	DEBUGMSGTL(("acseMIB", "applicationEntityInvocationTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->apInvocationId);
		StorageDel->apInvocationIdLen = 0;
		SNMP_FREE(StorageDel->applicationEntityInvocationUnderlyingConnectionNames);
		StorageDel->applicationEntityInvocationUnderlyingConnectionNamesLen = 0;
		SNMP_FREE(StorageDel->applicationEntityInvocationSupportedConnectionNames);
		StorageDel->applicationEntityInvocationSupportedConnectionNamesLen = 0;
		SNMP_FREE(StorageDel->applicationEntityInvocationInvocationOfPointer);
		StorageDel->applicationEntityInvocationInvocationOfPointerLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int applicationEntityInvocationTable_add(struct applicationEntityInvocationTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the applicationEntityInvocationTable table data set.
 *
 * Adds a table row structure to the applicationEntityInvocationTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
applicationEntityInvocationTable_add(struct applicationEntityInvocationTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("acseMIB", "applicationEntityInvocationTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* applicationSubsystemId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->applicationSubsystemId, sizeof(thedata->applicationSubsystemId));
		/* apInvocationId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->apInvocationId, thedata->apInvocationIdLen);
		header_complex_add_data(&applicationEntityInvocationTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("acseMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int applicationEntityInvocationTable_del(struct applicationEntityInvocationTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the applicationEntityInvocationTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
applicationEntityInvocationTable_del(struct applicationEntityInvocationTable_data *thedata)
{
	struct applicationEntityInvocationTable_data *StorageDel;

	DEBUGMSGTL(("acseMIB", "applicationEntityInvocationTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(applicationEntityInvocationTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&applicationEntityInvocationTableStorage, hciptr);
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_applicationEntityInvocationTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for applicationEntityInvocationTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case applicationEntityInvocationTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_applicationEntityInvocationTable(const char *token, char *line)
{
	size_t tmpsize;
	struct applicationEntityInvocationTable_data *StorageTmp = applicationEntityInvocationTable_create();

	DEBUGMSGTL(("acseMIB", "parse_applicationEntityInvocationTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->applicationSubsystemId, &tmpsize);
	SNMP_FREE(StorageTmp->apInvocationId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->apInvocationId, &StorageTmp->apInvocationIdLen);
	if (StorageTmp->apInvocationId == NULL) {
		config_perror("invalid specification for apInvocationId");
		return;
	}
	SNMP_FREE(StorageTmp->applicationEntityInvocationUnderlyingConnectionNames);
	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->applicationEntityInvocationUnderlyingConnectionNames, &StorageTmp->applicationEntityInvocationUnderlyingConnectionNamesLen);
	if (StorageTmp->applicationEntityInvocationUnderlyingConnectionNames == NULL) {
		config_perror("invalid specification for applicationEntityInvocationUnderlyingConnectionNames");
		return;
	}
	SNMP_FREE(StorageTmp->applicationEntityInvocationSupportedConnectionNames);
	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->applicationEntityInvocationSupportedConnectionNames, &StorageTmp->applicationEntityInvocationSupportedConnectionNamesLen);
	if (StorageTmp->applicationEntityInvocationSupportedConnectionNames == NULL) {
		config_perror("invalid specification for applicationEntityInvocationSupportedConnectionNames");
		return;
	}
	SNMP_FREE(StorageTmp->applicationEntityInvocationInvocationOfPointer);
	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->applicationEntityInvocationInvocationOfPointer, &StorageTmp->applicationEntityInvocationInvocationOfPointerLen);
	if (StorageTmp->applicationEntityInvocationInvocationOfPointer == NULL) {
		config_perror("invalid specification for applicationEntityInvocationInvocationOfPointer");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->applicationEntityInvocationRowStatus, &tmpsize);
	applicationEntityInvocationTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("acseMIB", "done.\n"));
}

/*
 * store_applicationEntityInvocationTable(): store configuraiton file for applicationEntityInvocationTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_applicationEntityInvocationTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct applicationEntityInvocationTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("acseMIB", "store_applicationEntityInvocationTable: storing data...  "));
	refresh_applicationEntityInvocationTable(1);
	(void) tmpsize;
	for (hcindex = applicationEntityInvocationTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct applicationEntityInvocationTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "applicationEntityInvocationTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->applicationSubsystemId, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->apInvocationId, &StorageTmp->apInvocationIdLen);
			cptr =
			    read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->applicationEntityInvocationUnderlyingConnectionNames,
						   &StorageTmp->applicationEntityInvocationUnderlyingConnectionNamesLen);
			cptr =
			    read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->applicationEntityInvocationSupportedConnectionNames,
						   &StorageTmp->applicationEntityInvocationSupportedConnectionNamesLen);
			cptr = read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->applicationEntityInvocationInvocationOfPointer, &StorageTmp->applicationEntityInvocationInvocationOfPointerLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->applicationEntityInvocationRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct applicationSubsystemTable_data *applicationSubsystemTable_create(void)
 * @brief create a fresh data structure representing a new row in the applicationSubsystemTable table.
 *
 * Creates a new applicationSubsystemTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct applicationSubsystemTable_data *
applicationSubsystemTable_create(void)
{
	struct applicationSubsystemTable_data *StorageNew = SNMP_MALLOC_STRUCT(applicationSubsystemTable_data);

	DEBUGMSGTL(("acseMIB", "applicationSubsystemTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->subsystemId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->subsystemIdLen = 0;
		StorageNew->subsystemId[StorageNew->subsystemIdLen] = 0;
		StorageNew->applicationSubsystemId = 0;
	}
      done:
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	applicationSubsystemTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct applicationSubsystemTable_data *applicationSubsystemTable_duplicate(struct applicationSubsystemTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct applicationSubsystemTable_data *
applicationSubsystemTable_duplicate(struct applicationSubsystemTable_data *thedata)
{
	struct applicationSubsystemTable_data *StorageNew = SNMP_MALLOC_STRUCT(applicationSubsystemTable_data);

	DEBUGMSGTL(("acseMIB", "applicationSubsystemTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		if (!(StorageNew->subsystemId = malloc(thedata->subsystemIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->subsystemId, thedata->subsystemId, thedata->subsystemIdLen);
		StorageNew->subsystemIdLen = thedata->subsystemIdLen;
		StorageNew->subsystemId[StorageNew->subsystemIdLen] = 0;
		StorageNew->applicationSubsystemId = thedata->applicationSubsystemId;
	}
      done:
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	applicationSubsystemTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int applicationSubsystemTable_destroy(struct applicationSubsystemTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
applicationSubsystemTable_destroy(struct applicationSubsystemTable_data **thedata)
{
	struct applicationSubsystemTable_data *StorageDel;

	DEBUGMSGTL(("acseMIB", "applicationSubsystemTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->subsystemId);
		StorageDel->subsystemIdLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int applicationSubsystemTable_add(struct applicationSubsystemTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the applicationSubsystemTable table data set.
 *
 * Adds a table row structure to the applicationSubsystemTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
applicationSubsystemTable_add(struct applicationSubsystemTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("acseMIB", "applicationSubsystemTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* subsystemId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->subsystemId, thedata->subsystemIdLen);
		header_complex_add_data(&applicationSubsystemTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("acseMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int applicationSubsystemTable_del(struct applicationSubsystemTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the applicationSubsystemTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
applicationSubsystemTable_del(struct applicationSubsystemTable_data *thedata)
{
	struct applicationSubsystemTable_data *StorageDel;

	DEBUGMSGTL(("acseMIB", "applicationSubsystemTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(applicationSubsystemTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&applicationSubsystemTableStorage, hciptr);
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_applicationSubsystemTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for applicationSubsystemTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case applicationSubsystemTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_applicationSubsystemTable(const char *token, char *line)
{
	size_t tmpsize;
	struct applicationSubsystemTable_data *StorageTmp = applicationSubsystemTable_create();

	DEBUGMSGTL(("acseMIB", "parse_applicationSubsystemTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->subsystemId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->subsystemId, &StorageTmp->subsystemIdLen);
	if (StorageTmp->subsystemId == NULL) {
		config_perror("invalid specification for subsystemId");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->applicationSubsystemId, &tmpsize);
	applicationSubsystemTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("acseMIB", "done.\n"));
}

/*
 * store_applicationSubsystemTable(): store configuraiton file for applicationSubsystemTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_applicationSubsystemTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct applicationSubsystemTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("acseMIB", "store_applicationSubsystemTable: storing data...  "));
	refresh_applicationSubsystemTable(1);
	(void) tmpsize;
	for (hcindex = applicationSubsystemTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct applicationSubsystemTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "applicationSubsystemTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->subsystemId, &StorageTmp->subsystemIdLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->applicationSubsystemId, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct asoTable_data *asoTable_create(void)
 * @brief create a fresh data structure representing a new row in the asoTable table.
 *
 * Creates a new asoTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct asoTable_data *
asoTable_create(void)
{
	struct asoTable_data *StorageNew = SNMP_MALLOC_STRUCT(asoTable_data);

	DEBUGMSGTL(("acseMIB", "asoTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->subsystemId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->subsystemIdLen = 0;
		StorageNew->subsystemId[StorageNew->subsystemIdLen] = 0;
		if ((StorageNew->applicationProcessId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->applicationProcessIdLen = 0;
		StorageNew->applicationProcessId[StorageNew->applicationProcessIdLen] = 0;
		if ((StorageNew->asoEntityId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->asoEntityIdLen = 0;
		StorageNew->asoEntityId[StorageNew->asoEntityIdLen] = 0;
		if ((StorageNew->asoTitle = snmp_duplicate_objid(zeroDotZero_oid, 2)) == NULL)
			goto nomem;
			StorageNew->asoTitleLen = 2;
		if ((StorageNew->asoQualifier = snmp_duplicate_objid(zeroDotZero_oid, 2)) == NULL)
			goto nomem;
			StorageNew->asoQualifierLen = 2;
		StorageNew->asoOperationalState = 0;
		StorageNew->asoRowStatus = 0;
		StorageNew->asoRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	asoTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct asoTable_data *asoTable_duplicate(struct asoTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct asoTable_data *
asoTable_duplicate(struct asoTable_data *thedata)
{
	struct asoTable_data *StorageNew = SNMP_MALLOC_STRUCT(asoTable_data);

	DEBUGMSGTL(("acseMIB", "asoTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		if (!(StorageNew->subsystemId = malloc(thedata->subsystemIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->subsystemId, thedata->subsystemId, thedata->subsystemIdLen);
		StorageNew->subsystemIdLen = thedata->subsystemIdLen;
		StorageNew->subsystemId[StorageNew->subsystemIdLen] = 0;
		if (!(StorageNew->applicationProcessId = malloc(thedata->applicationProcessIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->applicationProcessId, thedata->applicationProcessId, thedata->applicationProcessIdLen);
		StorageNew->applicationProcessIdLen = thedata->applicationProcessIdLen;
		StorageNew->applicationProcessId[StorageNew->applicationProcessIdLen] = 0;
		if (!(StorageNew->asoEntityId = malloc(thedata->asoEntityIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->asoEntityId, thedata->asoEntityId, thedata->asoEntityIdLen);
		StorageNew->asoEntityIdLen = thedata->asoEntityIdLen;
		StorageNew->asoEntityId[StorageNew->asoEntityIdLen] = 0;
		if (!(StorageNew->asoId = malloc(thedata->asoIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->asoId, thedata->asoId, thedata->asoIdLen);
		StorageNew->asoIdLen = thedata->asoIdLen;
		StorageNew->asoId[StorageNew->asoIdLen] = 0;
		if (!(StorageNew->asoTitle = snmp_duplicate_objid(thedata->asoTitle, thedata->asoTitleLen / sizeof(oid))))
			goto destroy;
		StorageNew->asoTitleLen = thedata->asoTitleLen;
		if (!(StorageNew->asoQualifier = snmp_duplicate_objid(thedata->asoQualifier, thedata->asoQualifierLen / sizeof(oid))))
			goto destroy;
		StorageNew->asoQualifierLen = thedata->asoQualifierLen;
		StorageNew->asoOperationalState = thedata->asoOperationalState;
		StorageNew->asoRowStatus = thedata->asoRowStatus;
	}
      done:
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	asoTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int asoTable_destroy(struct asoTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
asoTable_destroy(struct asoTable_data **thedata)
{
	struct asoTable_data *StorageDel;

	DEBUGMSGTL(("acseMIB", "asoTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->subsystemId);
		StorageDel->subsystemIdLen = 0;
		SNMP_FREE(StorageDel->applicationProcessId);
		StorageDel->applicationProcessIdLen = 0;
		SNMP_FREE(StorageDel->asoEntityId);
		StorageDel->asoEntityIdLen = 0;
		SNMP_FREE(StorageDel->asoId);
		StorageDel->asoIdLen = 0;
		SNMP_FREE(StorageDel->asoTitle);
		StorageDel->asoTitleLen = 0;
		SNMP_FREE(StorageDel->asoQualifier);
		StorageDel->asoQualifierLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int asoTable_add(struct asoTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the asoTable table data set.
 *
 * Adds a table row structure to the asoTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
asoTable_add(struct asoTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("acseMIB", "asoTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* subsystemId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->subsystemId, thedata->subsystemIdLen);
		/* applicationProcessId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->applicationProcessId, thedata->applicationProcessIdLen);
		/* asoEntityId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->asoEntityId, thedata->asoEntityIdLen);
		/* asoId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->asoId, thedata->asoIdLen);
		header_complex_add_data(&asoTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("acseMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int asoTable_del(struct asoTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the asoTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
asoTable_del(struct asoTable_data *thedata)
{
	struct asoTable_data *StorageDel;

	DEBUGMSGTL(("acseMIB", "asoTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(asoTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&asoTableStorage, hciptr);
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_asoTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for asoTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case asoTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_asoTable(const char *token, char *line)
{
	size_t tmpsize;
	struct asoTable_data *StorageTmp = asoTable_create();

	DEBUGMSGTL(("acseMIB", "parse_asoTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->subsystemId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->subsystemId, &StorageTmp->subsystemIdLen);
	if (StorageTmp->subsystemId == NULL) {
		config_perror("invalid specification for subsystemId");
		return;
	}
	SNMP_FREE(StorageTmp->applicationProcessId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->applicationProcessId, &StorageTmp->applicationProcessIdLen);
	if (StorageTmp->applicationProcessId == NULL) {
		config_perror("invalid specification for applicationProcessId");
		return;
	}
	SNMP_FREE(StorageTmp->asoEntityId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->asoEntityId, &StorageTmp->asoEntityIdLen);
	if (StorageTmp->asoEntityId == NULL) {
		config_perror("invalid specification for asoEntityId");
		return;
	}
	SNMP_FREE(StorageTmp->asoId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->asoId, &StorageTmp->asoIdLen);
	if (StorageTmp->asoId == NULL) {
		config_perror("invalid specification for asoId");
		return;
	}
	SNMP_FREE(StorageTmp->asoTitle);
	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->asoTitle, &StorageTmp->asoTitleLen);
	if (StorageTmp->asoTitle == NULL) {
		config_perror("invalid specification for asoTitle");
		return;
	}
	SNMP_FREE(StorageTmp->asoQualifier);
	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->asoQualifier, &StorageTmp->asoQualifierLen);
	if (StorageTmp->asoQualifier == NULL) {
		config_perror("invalid specification for asoQualifier");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->asoOperationalState, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->asoRowStatus, &tmpsize);
	asoTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("acseMIB", "done.\n"));
}

/*
 * store_asoTable(): store configuraiton file for asoTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_asoTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct asoTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("acseMIB", "store_asoTable: storing data...  "));
	refresh_asoTable(1);
	(void) tmpsize;
	for (hcindex = asoTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct asoTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "asoTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->subsystemId, &StorageTmp->subsystemIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->applicationProcessId, &StorageTmp->applicationProcessIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->asoEntityId, &StorageTmp->asoEntityIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->asoId, &StorageTmp->asoIdLen);
			cptr = read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->asoTitle, &StorageTmp->asoTitleLen);
			cptr = read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->asoQualifier, &StorageTmp->asoQualifierLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->asoOperationalState, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->asoRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct asoEntityTable_data *asoEntityTable_create(void)
 * @brief create a fresh data structure representing a new row in the asoEntityTable table.
 *
 * Creates a new asoEntityTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct asoEntityTable_data *
asoEntityTable_create(void)
{
	struct asoEntityTable_data *StorageNew = SNMP_MALLOC_STRUCT(asoEntityTable_data);

	DEBUGMSGTL(("acseMIB", "asoEntityTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->subsystemId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->subsystemIdLen = 0;
		StorageNew->subsystemId[StorageNew->subsystemIdLen] = 0;
		if ((StorageNew->applicationProcessId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->applicationProcessIdLen = 0;
		StorageNew->applicationProcessId[StorageNew->applicationProcessIdLen] = 0;
		if ((StorageNew->asoEntityLocalSapNames = malloc(1)) == NULL)
			goto nomem;
		StorageNew->asoEntityLocalSapNamesLen = 0;
		StorageNew->asoEntityLocalSapNames[StorageNew->asoEntityLocalSapNamesLen] = 0;
		StorageNew->asoEntityOperationalState = 0;
		if ((StorageNew->asoEntityTitle = snmp_duplicate_objid(zeroDotZero_oid, 2)) == NULL)
			goto nomem;
			StorageNew->asoEntityTitleLen = 2;
		if ((StorageNew->asoEntityQualifier = snmp_duplicate_objid(zeroDotZero_oid, 2)) == NULL)
			goto nomem;
			StorageNew->asoEntityQualifierLen = 2;
		if ((StorageNew->asoEntityApplicationContextNameSupport = malloc(1)) == NULL)
			goto nomem;
		StorageNew->asoEntityApplicationContextNameSupportLen = 0;
		StorageNew->asoEntityApplicationContextNameSupport[StorageNew->asoEntityApplicationContextNameSupportLen] = 0;
		StorageNew->asoEntityRowStatus = 0;
		StorageNew->asoEntityRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	asoEntityTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct asoEntityTable_data *asoEntityTable_duplicate(struct asoEntityTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct asoEntityTable_data *
asoEntityTable_duplicate(struct asoEntityTable_data *thedata)
{
	struct asoEntityTable_data *StorageNew = SNMP_MALLOC_STRUCT(asoEntityTable_data);

	DEBUGMSGTL(("acseMIB", "asoEntityTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		if (!(StorageNew->subsystemId = malloc(thedata->subsystemIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->subsystemId, thedata->subsystemId, thedata->subsystemIdLen);
		StorageNew->subsystemIdLen = thedata->subsystemIdLen;
		StorageNew->subsystemId[StorageNew->subsystemIdLen] = 0;
		if (!(StorageNew->applicationProcessId = malloc(thedata->applicationProcessIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->applicationProcessId, thedata->applicationProcessId, thedata->applicationProcessIdLen);
		StorageNew->applicationProcessIdLen = thedata->applicationProcessIdLen;
		StorageNew->applicationProcessId[StorageNew->applicationProcessIdLen] = 0;
		if (!(StorageNew->asoEntityId = malloc(thedata->asoEntityIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->asoEntityId, thedata->asoEntityId, thedata->asoEntityIdLen);
		StorageNew->asoEntityIdLen = thedata->asoEntityIdLen;
		StorageNew->asoEntityId[StorageNew->asoEntityIdLen] = 0;
		if (!(StorageNew->asoEntityLocalSapNames = malloc(thedata->asoEntityLocalSapNamesLen + 1)))
			goto destroy;
		memcpy(StorageNew->asoEntityLocalSapNames, thedata->asoEntityLocalSapNames, thedata->asoEntityLocalSapNamesLen);
		StorageNew->asoEntityLocalSapNamesLen = thedata->asoEntityLocalSapNamesLen;
		StorageNew->asoEntityLocalSapNames[StorageNew->asoEntityLocalSapNamesLen] = 0;
		StorageNew->asoEntityOperationalState = thedata->asoEntityOperationalState;
		if (!(StorageNew->asoEntityTitle = snmp_duplicate_objid(thedata->asoEntityTitle, thedata->asoEntityTitleLen / sizeof(oid))))
			goto destroy;
		StorageNew->asoEntityTitleLen = thedata->asoEntityTitleLen;
		if (!(StorageNew->asoEntityQualifier = snmp_duplicate_objid(thedata->asoEntityQualifier, thedata->asoEntityQualifierLen / sizeof(oid))))
			goto destroy;
		StorageNew->asoEntityQualifierLen = thedata->asoEntityQualifierLen;
		if (!(StorageNew->asoEntityApplicationContextNameSupport = malloc(thedata->asoEntityApplicationContextNameSupportLen + 1)))
			goto destroy;
		memcpy(StorageNew->asoEntityApplicationContextNameSupport, thedata->asoEntityApplicationContextNameSupport, thedata->asoEntityApplicationContextNameSupportLen);
		StorageNew->asoEntityApplicationContextNameSupportLen = thedata->asoEntityApplicationContextNameSupportLen;
		StorageNew->asoEntityApplicationContextNameSupport[StorageNew->asoEntityApplicationContextNameSupportLen] = 0;
		StorageNew->asoEntityRowStatus = thedata->asoEntityRowStatus;
	}
      done:
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	asoEntityTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int asoEntityTable_destroy(struct asoEntityTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
asoEntityTable_destroy(struct asoEntityTable_data **thedata)
{
	struct asoEntityTable_data *StorageDel;

	DEBUGMSGTL(("acseMIB", "asoEntityTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->subsystemId);
		StorageDel->subsystemIdLen = 0;
		SNMP_FREE(StorageDel->applicationProcessId);
		StorageDel->applicationProcessIdLen = 0;
		SNMP_FREE(StorageDel->asoEntityId);
		StorageDel->asoEntityIdLen = 0;
		SNMP_FREE(StorageDel->asoEntityTitle);
		StorageDel->asoEntityTitleLen = 0;
		SNMP_FREE(StorageDel->asoEntityQualifier);
		StorageDel->asoEntityQualifierLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int asoEntityTable_add(struct asoEntityTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the asoEntityTable table data set.
 *
 * Adds a table row structure to the asoEntityTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
asoEntityTable_add(struct asoEntityTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("acseMIB", "asoEntityTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* subsystemId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->subsystemId, thedata->subsystemIdLen);
		/* applicationProcessId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->applicationProcessId, thedata->applicationProcessIdLen);
		/* asoEntityId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->asoEntityId, thedata->asoEntityIdLen);
		header_complex_add_data(&asoEntityTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("acseMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int asoEntityTable_del(struct asoEntityTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the asoEntityTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
asoEntityTable_del(struct asoEntityTable_data *thedata)
{
	struct asoEntityTable_data *StorageDel;

	DEBUGMSGTL(("acseMIB", "asoEntityTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(asoEntityTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&asoEntityTableStorage, hciptr);
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_asoEntityTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for asoEntityTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case asoEntityTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_asoEntityTable(const char *token, char *line)
{
	size_t tmpsize;
	struct asoEntityTable_data *StorageTmp = asoEntityTable_create();

	DEBUGMSGTL(("acseMIB", "parse_asoEntityTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->subsystemId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->subsystemId, &StorageTmp->subsystemIdLen);
	if (StorageTmp->subsystemId == NULL) {
		config_perror("invalid specification for subsystemId");
		return;
	}
	SNMP_FREE(StorageTmp->applicationProcessId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->applicationProcessId, &StorageTmp->applicationProcessIdLen);
	if (StorageTmp->applicationProcessId == NULL) {
		config_perror("invalid specification for applicationProcessId");
		return;
	}
	SNMP_FREE(StorageTmp->asoEntityId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->asoEntityId, &StorageTmp->asoEntityIdLen);
	if (StorageTmp->asoEntityId == NULL) {
		config_perror("invalid specification for asoEntityId");
		return;
	}
	SNMP_FREE(StorageTmp->asoEntityLocalSapNames);
	line = read_config_read_data(ASN_OPAQUE, line, &StorageTmp->asoEntityLocalSapNames, &StorageTmp->asoEntityLocalSapNamesLen);
	if (StorageTmp->asoEntityLocalSapNames == NULL) {
		config_perror("invalid specification for asoEntityLocalSapNames");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->asoEntityOperationalState, &tmpsize);
	SNMP_FREE(StorageTmp->asoEntityTitle);
	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->asoEntityTitle, &StorageTmp->asoEntityTitleLen);
	if (StorageTmp->asoEntityTitle == NULL) {
		config_perror("invalid specification for asoEntityTitle");
		return;
	}
	SNMP_FREE(StorageTmp->asoEntityQualifier);
	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->asoEntityQualifier, &StorageTmp->asoEntityQualifierLen);
	if (StorageTmp->asoEntityQualifier == NULL) {
		config_perror("invalid specification for asoEntityQualifier");
		return;
	}
	SNMP_FREE(StorageTmp->asoEntityApplicationContextNameSupport);
	line = read_config_read_data(ASN_OPAQUE, line, &StorageTmp->asoEntityApplicationContextNameSupport, &StorageTmp->asoEntityApplicationContextNameSupportLen);
	if (StorageTmp->asoEntityApplicationContextNameSupport == NULL) {
		config_perror("invalid specification for asoEntityApplicationContextNameSupport");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->asoEntityRowStatus, &tmpsize);
	asoEntityTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("acseMIB", "done.\n"));
}

/*
 * store_asoEntityTable(): store configuraiton file for asoEntityTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_asoEntityTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct asoEntityTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("acseMIB", "store_asoEntityTable: storing data...  "));
	refresh_asoEntityTable(1);
	(void) tmpsize;
	for (hcindex = asoEntityTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct asoEntityTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "asoEntityTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->subsystemId, &StorageTmp->subsystemIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->applicationProcessId, &StorageTmp->applicationProcessIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->asoEntityId, &StorageTmp->asoEntityIdLen);
			cptr = read_config_store_data(ASN_OPAQUE, cptr, &StorageTmp->asoEntityLocalSapNames, &StorageTmp->asoEntityLocalSapNamesLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->asoEntityOperationalState, &tmpsize);
			cptr = read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->asoEntityTitle, &StorageTmp->asoEntityTitleLen);
			cptr = read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->asoEntityQualifier, &StorageTmp->asoEntityQualifierLen);
			cptr = read_config_store_data(ASN_OPAQUE, cptr, &StorageTmp->asoEntityApplicationContextNameSupport, &StorageTmp->asoEntityApplicationContextNameSupportLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->asoEntityRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct asoInvocationTable_data *asoInvocationTable_create(void)
 * @brief create a fresh data structure representing a new row in the asoInvocationTable table.
 *
 * Creates a new asoInvocationTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct asoInvocationTable_data *
asoInvocationTable_create(void)
{
	struct asoInvocationTable_data *StorageNew = SNMP_MALLOC_STRUCT(asoInvocationTable_data);

	DEBUGMSGTL(("acseMIB", "asoInvocationTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->applicationSubsystemId = 0;
		if ((StorageNew->apInvocationId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->apInvocationIdLen = 0;
		StorageNew->apInvocationId[StorageNew->apInvocationIdLen] = 0;
		if ((StorageNew->asoInvocationOfPointer = snmp_duplicate_objid(zeroDotZero_oid, 2)) == NULL)
			goto nomem;
			StorageNew->asoInvocationOfPointerLen = 2;
		StorageNew->asoInvocationRowStatus = 0;
		StorageNew->asoInvocationRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	asoInvocationTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct asoInvocationTable_data *asoInvocationTable_duplicate(struct asoInvocationTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct asoInvocationTable_data *
asoInvocationTable_duplicate(struct asoInvocationTable_data *thedata)
{
	struct asoInvocationTable_data *StorageNew = SNMP_MALLOC_STRUCT(asoInvocationTable_data);

	DEBUGMSGTL(("acseMIB", "asoInvocationTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->applicationSubsystemId = thedata->applicationSubsystemId;
		if (!(StorageNew->apInvocationId = malloc(thedata->apInvocationIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->apInvocationId, thedata->apInvocationId, thedata->apInvocationIdLen);
		StorageNew->apInvocationIdLen = thedata->apInvocationIdLen;
		StorageNew->apInvocationId[StorageNew->apInvocationIdLen] = 0;
		if (!(StorageNew->asoInvocationId = malloc(thedata->asoInvocationIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->asoInvocationId, thedata->asoInvocationId, thedata->asoInvocationIdLen);
		StorageNew->asoInvocationIdLen = thedata->asoInvocationIdLen;
		StorageNew->asoInvocationId[StorageNew->asoInvocationIdLen] = 0;
		if (!(StorageNew->asoInvocationOfPointer = snmp_duplicate_objid(thedata->asoInvocationOfPointer, thedata->asoInvocationOfPointerLen / sizeof(oid))))
			goto destroy;
		StorageNew->asoInvocationOfPointerLen = thedata->asoInvocationOfPointerLen;
		StorageNew->asoInvocationRowStatus = thedata->asoInvocationRowStatus;
	}
      done:
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	asoInvocationTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int asoInvocationTable_destroy(struct asoInvocationTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
asoInvocationTable_destroy(struct asoInvocationTable_data **thedata)
{
	struct asoInvocationTable_data *StorageDel;

	DEBUGMSGTL(("acseMIB", "asoInvocationTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->apInvocationId);
		StorageDel->apInvocationIdLen = 0;
		SNMP_FREE(StorageDel->asoInvocationId);
		StorageDel->asoInvocationIdLen = 0;
		SNMP_FREE(StorageDel->asoInvocationOfPointer);
		StorageDel->asoInvocationOfPointerLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int asoInvocationTable_add(struct asoInvocationTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the asoInvocationTable table data set.
 *
 * Adds a table row structure to the asoInvocationTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
asoInvocationTable_add(struct asoInvocationTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("acseMIB", "asoInvocationTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* applicationSubsystemId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->applicationSubsystemId, sizeof(thedata->applicationSubsystemId));
		/* apInvocationId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->apInvocationId, thedata->apInvocationIdLen);
		/* asoInvocationId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->asoInvocationId, thedata->asoInvocationIdLen);
		header_complex_add_data(&asoInvocationTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("acseMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int asoInvocationTable_del(struct asoInvocationTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the asoInvocationTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
asoInvocationTable_del(struct asoInvocationTable_data *thedata)
{
	struct asoInvocationTable_data *StorageDel;

	DEBUGMSGTL(("acseMIB", "asoInvocationTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(asoInvocationTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&asoInvocationTableStorage, hciptr);
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_asoInvocationTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for asoInvocationTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case asoInvocationTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_asoInvocationTable(const char *token, char *line)
{
	size_t tmpsize;
	struct asoInvocationTable_data *StorageTmp = asoInvocationTable_create();

	DEBUGMSGTL(("acseMIB", "parse_asoInvocationTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->applicationSubsystemId, &tmpsize);
	SNMP_FREE(StorageTmp->apInvocationId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->apInvocationId, &StorageTmp->apInvocationIdLen);
	if (StorageTmp->apInvocationId == NULL) {
		config_perror("invalid specification for apInvocationId");
		return;
	}
	SNMP_FREE(StorageTmp->asoInvocationId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->asoInvocationId, &StorageTmp->asoInvocationIdLen);
	if (StorageTmp->asoInvocationId == NULL) {
		config_perror("invalid specification for asoInvocationId");
		return;
	}
	SNMP_FREE(StorageTmp->asoInvocationOfPointer);
	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->asoInvocationOfPointer, &StorageTmp->asoInvocationOfPointerLen);
	if (StorageTmp->asoInvocationOfPointer == NULL) {
		config_perror("invalid specification for asoInvocationOfPointer");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->asoInvocationRowStatus, &tmpsize);
	asoInvocationTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("acseMIB", "done.\n"));
}

/*
 * store_asoInvocationTable(): store configuraiton file for asoInvocationTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_asoInvocationTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct asoInvocationTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("acseMIB", "store_asoInvocationTable: storing data...  "));
	refresh_asoInvocationTable(1);
	(void) tmpsize;
	for (hcindex = asoInvocationTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct asoInvocationTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "asoInvocationTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->applicationSubsystemId, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->apInvocationId, &StorageTmp->apInvocationIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->asoInvocationId, &StorageTmp->asoInvocationIdLen);
			cptr = read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->asoInvocationOfPointer, &StorageTmp->asoInvocationOfPointerLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->asoInvocationRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct monoULConnectionTable_data *monoULConnectionTable_create(void)
 * @brief create a fresh data structure representing a new row in the monoULConnectionTable table.
 *
 * Creates a new monoULConnectionTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct monoULConnectionTable_data *
monoULConnectionTable_create(void)
{
	struct monoULConnectionTable_data *StorageNew = SNMP_MALLOC_STRUCT(monoULConnectionTable_data);

	DEBUGMSGTL(("acseMIB", "monoULConnectionTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->applicationSubsystemId = 0;
		if ((StorageNew->apInvocationId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->apInvocationIdLen = 0;
		StorageNew->apInvocationId[StorageNew->apInvocationIdLen] = 0;
		StorageNew->monoULConnectionRowStatus = 0;
		StorageNew->monoULConnectionRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	monoULConnectionTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct monoULConnectionTable_data *monoULConnectionTable_duplicate(struct monoULConnectionTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct monoULConnectionTable_data *
monoULConnectionTable_duplicate(struct monoULConnectionTable_data *thedata)
{
	struct monoULConnectionTable_data *StorageNew = SNMP_MALLOC_STRUCT(monoULConnectionTable_data);

	DEBUGMSGTL(("acseMIB", "monoULConnectionTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->applicationSubsystemId = thedata->applicationSubsystemId;
		if (!(StorageNew->apInvocationId = malloc(thedata->apInvocationIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->apInvocationId, thedata->apInvocationId, thedata->apInvocationIdLen);
		StorageNew->apInvocationIdLen = thedata->apInvocationIdLen;
		StorageNew->apInvocationId[StorageNew->apInvocationIdLen] = 0;
		StorageNew->monoULConnectionRowStatus = thedata->monoULConnectionRowStatus;
	}
      done:
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	monoULConnectionTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int monoULConnectionTable_destroy(struct monoULConnectionTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
monoULConnectionTable_destroy(struct monoULConnectionTable_data **thedata)
{
	struct monoULConnectionTable_data *StorageDel;

	DEBUGMSGTL(("acseMIB", "monoULConnectionTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->apInvocationId);
		StorageDel->apInvocationIdLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int monoULConnectionTable_add(struct monoULConnectionTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the monoULConnectionTable table data set.
 *
 * Adds a table row structure to the monoULConnectionTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
monoULConnectionTable_add(struct monoULConnectionTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("acseMIB", "monoULConnectionTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* applicationSubsystemId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->applicationSubsystemId, sizeof(thedata->applicationSubsystemId));
		/* apInvocationId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->apInvocationId, thedata->apInvocationIdLen);
		header_complex_add_data(&monoULConnectionTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("acseMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int monoULConnectionTable_del(struct monoULConnectionTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the monoULConnectionTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
monoULConnectionTable_del(struct monoULConnectionTable_data *thedata)
{
	struct monoULConnectionTable_data *StorageDel;

	DEBUGMSGTL(("acseMIB", "monoULConnectionTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(monoULConnectionTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&monoULConnectionTableStorage, hciptr);
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_monoULConnectionTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for monoULConnectionTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case monoULConnectionTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_monoULConnectionTable(const char *token, char *line)
{
	size_t tmpsize;
	struct monoULConnectionTable_data *StorageTmp = monoULConnectionTable_create();

	DEBUGMSGTL(("acseMIB", "parse_monoULConnectionTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->applicationSubsystemId, &tmpsize);
	SNMP_FREE(StorageTmp->apInvocationId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->apInvocationId, &StorageTmp->apInvocationIdLen);
	if (StorageTmp->apInvocationId == NULL) {
		config_perror("invalid specification for apInvocationId");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->monoULConnectionRowStatus, &tmpsize);
	monoULConnectionTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("acseMIB", "done.\n"));
}

/*
 * store_monoULConnectionTable(): store configuraiton file for monoULConnectionTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_monoULConnectionTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct monoULConnectionTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("acseMIB", "store_monoULConnectionTable: storing data...  "));
	refresh_monoULConnectionTable(1);
	(void) tmpsize;
	for (hcindex = monoULConnectionTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct monoULConnectionTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "monoULConnectionTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->applicationSubsystemId, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->apInvocationId, &StorageTmp->apInvocationIdLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->monoULConnectionRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct monoULEntityTable_data *monoULEntityTable_create(void)
 * @brief create a fresh data structure representing a new row in the monoULEntityTable table.
 *
 * Creates a new monoULEntityTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct monoULEntityTable_data *
monoULEntityTable_create(void)
{
	struct monoULEntityTable_data *StorageNew = SNMP_MALLOC_STRUCT(monoULEntityTable_data);

	DEBUGMSGTL(("acseMIB", "monoULEntityTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->subsystemId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->subsystemIdLen = 0;
		StorageNew->subsystemId[StorageNew->subsystemIdLen] = 0;
		StorageNew->monoUlEntityId = 0;
	}
      done:
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	monoULEntityTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct monoULEntityTable_data *monoULEntityTable_duplicate(struct monoULEntityTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct monoULEntityTable_data *
monoULEntityTable_duplicate(struct monoULEntityTable_data *thedata)
{
	struct monoULEntityTable_data *StorageNew = SNMP_MALLOC_STRUCT(monoULEntityTable_data);

	DEBUGMSGTL(("acseMIB", "monoULEntityTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		if (!(StorageNew->subsystemId = malloc(thedata->subsystemIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->subsystemId, thedata->subsystemId, thedata->subsystemIdLen);
		StorageNew->subsystemIdLen = thedata->subsystemIdLen;
		StorageNew->subsystemId[StorageNew->subsystemIdLen] = 0;
		StorageNew->monoUlEntityId = thedata->monoUlEntityId;
	}
      done:
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	monoULEntityTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int monoULEntityTable_destroy(struct monoULEntityTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
monoULEntityTable_destroy(struct monoULEntityTable_data **thedata)
{
	struct monoULEntityTable_data *StorageDel;

	DEBUGMSGTL(("acseMIB", "monoULEntityTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->subsystemId);
		StorageDel->subsystemIdLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int monoULEntityTable_add(struct monoULEntityTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the monoULEntityTable table data set.
 *
 * Adds a table row structure to the monoULEntityTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
monoULEntityTable_add(struct monoULEntityTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("acseMIB", "monoULEntityTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* subsystemId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->subsystemId, thedata->subsystemIdLen);
		header_complex_add_data(&monoULEntityTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("acseMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int monoULEntityTable_del(struct monoULEntityTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the monoULEntityTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
monoULEntityTable_del(struct monoULEntityTable_data *thedata)
{
	struct monoULEntityTable_data *StorageDel;

	DEBUGMSGTL(("acseMIB", "monoULEntityTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(monoULEntityTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&monoULEntityTableStorage, hciptr);
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_monoULEntityTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for monoULEntityTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case monoULEntityTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_monoULEntityTable(const char *token, char *line)
{
	size_t tmpsize;
	struct monoULEntityTable_data *StorageTmp = monoULEntityTable_create();

	DEBUGMSGTL(("acseMIB", "parse_monoULEntityTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->subsystemId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->subsystemId, &StorageTmp->subsystemIdLen);
	if (StorageTmp->subsystemId == NULL) {
		config_perror("invalid specification for subsystemId");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->monoUlEntityId, &tmpsize);
	monoULEntityTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("acseMIB", "done.\n"));
}

/*
 * store_monoULEntityTable(): store configuraiton file for monoULEntityTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_monoULEntityTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct monoULEntityTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("acseMIB", "store_monoULEntityTable: storing data...  "));
	refresh_monoULEntityTable(1);
	(void) tmpsize;
	for (hcindex = monoULEntityTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct monoULEntityTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "monoULEntityTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->subsystemId, &StorageTmp->subsystemIdLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->monoUlEntityId, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct monolithicUpperLayerSubsystemTable_data *monolithicUpperLayerSubsystemTable_create(void)
 * @brief create a fresh data structure representing a new row in the monolithicUpperLayerSubsystemTable table.
 *
 * Creates a new monolithicUpperLayerSubsystemTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct monolithicUpperLayerSubsystemTable_data *
monolithicUpperLayerSubsystemTable_create(void)
{
	struct monolithicUpperLayerSubsystemTable_data *StorageNew = SNMP_MALLOC_STRUCT(monolithicUpperLayerSubsystemTable_data);

	DEBUGMSGTL(("acseMIB", "monolithicUpperLayerSubsystemTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->monolithicUpperLayerSubssytemRowStatus = 0;
		StorageNew->monolithicUpperLayerSubssytemRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	monolithicUpperLayerSubsystemTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct monolithicUpperLayerSubsystemTable_data *monolithicUpperLayerSubsystemTable_duplicate(struct monolithicUpperLayerSubsystemTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct monolithicUpperLayerSubsystemTable_data *
monolithicUpperLayerSubsystemTable_duplicate(struct monolithicUpperLayerSubsystemTable_data *thedata)
{
	struct monolithicUpperLayerSubsystemTable_data *StorageNew = SNMP_MALLOC_STRUCT(monolithicUpperLayerSubsystemTable_data);

	DEBUGMSGTL(("acseMIB", "monolithicUpperLayerSubsystemTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		if (!(StorageNew->monolithicUpperLayerSubsystemId = malloc(thedata->monolithicUpperLayerSubsystemIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->monolithicUpperLayerSubsystemId, thedata->monolithicUpperLayerSubsystemId, thedata->monolithicUpperLayerSubsystemIdLen);
		StorageNew->monolithicUpperLayerSubsystemIdLen = thedata->monolithicUpperLayerSubsystemIdLen;
		StorageNew->monolithicUpperLayerSubsystemId[StorageNew->monolithicUpperLayerSubsystemIdLen] = 0;
		StorageNew->monolithicUpperLayerSubssytemRowStatus = thedata->monolithicUpperLayerSubssytemRowStatus;
	}
      done:
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	monolithicUpperLayerSubsystemTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int monolithicUpperLayerSubsystemTable_destroy(struct monolithicUpperLayerSubsystemTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
monolithicUpperLayerSubsystemTable_destroy(struct monolithicUpperLayerSubsystemTable_data **thedata)
{
	struct monolithicUpperLayerSubsystemTable_data *StorageDel;

	DEBUGMSGTL(("acseMIB", "monolithicUpperLayerSubsystemTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->monolithicUpperLayerSubsystemId);
		StorageDel->monolithicUpperLayerSubsystemIdLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int monolithicUpperLayerSubsystemTable_add(struct monolithicUpperLayerSubsystemTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the monolithicUpperLayerSubsystemTable table data set.
 *
 * Adds a table row structure to the monolithicUpperLayerSubsystemTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
monolithicUpperLayerSubsystemTable_add(struct monolithicUpperLayerSubsystemTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("acseMIB", "monolithicUpperLayerSubsystemTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* monolithicUpperLayerSubsystemId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->monolithicUpperLayerSubsystemId, thedata->monolithicUpperLayerSubsystemIdLen);
		header_complex_add_data(&monolithicUpperLayerSubsystemTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("acseMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int monolithicUpperLayerSubsystemTable_del(struct monolithicUpperLayerSubsystemTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the monolithicUpperLayerSubsystemTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
monolithicUpperLayerSubsystemTable_del(struct monolithicUpperLayerSubsystemTable_data *thedata)
{
	struct monolithicUpperLayerSubsystemTable_data *StorageDel;

	DEBUGMSGTL(("acseMIB", "monolithicUpperLayerSubsystemTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(monolithicUpperLayerSubsystemTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&monolithicUpperLayerSubsystemTableStorage, hciptr);
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_monolithicUpperLayerSubsystemTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for monolithicUpperLayerSubsystemTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case monolithicUpperLayerSubsystemTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_monolithicUpperLayerSubsystemTable(const char *token, char *line)
{
	size_t tmpsize;
	struct monolithicUpperLayerSubsystemTable_data *StorageTmp = monolithicUpperLayerSubsystemTable_create();

	DEBUGMSGTL(("acseMIB", "parse_monolithicUpperLayerSubsystemTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->monolithicUpperLayerSubsystemId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->monolithicUpperLayerSubsystemId, &StorageTmp->monolithicUpperLayerSubsystemIdLen);
	if (StorageTmp->monolithicUpperLayerSubsystemId == NULL) {
		config_perror("invalid specification for monolithicUpperLayerSubsystemId");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->monolithicUpperLayerSubssytemRowStatus, &tmpsize);
	monolithicUpperLayerSubsystemTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("acseMIB", "done.\n"));
}

/*
 * store_monolithicUpperLayerSubsystemTable(): store configuraiton file for monolithicUpperLayerSubsystemTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_monolithicUpperLayerSubsystemTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct monolithicUpperLayerSubsystemTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("acseMIB", "store_monolithicUpperLayerSubsystemTable: storing data...  "));
	refresh_monolithicUpperLayerSubsystemTable(1);
	(void) tmpsize;
	for (hcindex = monolithicUpperLayerSubsystemTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct monolithicUpperLayerSubsystemTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "monolithicUpperLayerSubsystemTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->monolithicUpperLayerSubsystemId, &StorageTmp->monolithicUpperLayerSubsystemIdLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->monolithicUpperLayerSubssytemRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct pSapTable_data *pSapTable_create(void)
 * @brief create a fresh data structure representing a new row in the pSapTable table.
 *
 * Creates a new pSapTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct pSapTable_data *
pSapTable_create(void)
{
	struct pSapTable_data *StorageNew = SNMP_MALLOC_STRUCT(pSapTable_data);

	DEBUGMSGTL(("acseMIB", "pSapTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->subsystemId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->subsystemIdLen = 0;
		StorageNew->subsystemId[StorageNew->subsystemIdLen] = 0;
		if ((StorageNew->pSapId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->pSapIdLen = 0;
		StorageNew->pSapId[StorageNew->pSapIdLen] = 0;
		if ((StorageNew->pSapSap1Address = malloc(1)) == NULL)
			goto nomem;
		StorageNew->pSapSap1AddressLen = 0;
		StorageNew->pSapSap1Address[StorageNew->pSapSap1AddressLen] = 0;
		if ((StorageNew->pSapUserEntityNames = malloc(1)) == NULL)
			goto nomem;
		StorageNew->pSapUserEntityNamesLen = 0;
		StorageNew->pSapUserEntityNames[StorageNew->pSapUserEntityNamesLen] = 0;
		StorageNew->pSapRowStatus = 0;
		StorageNew->pSapRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	pSapTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct pSapTable_data *pSapTable_duplicate(struct pSapTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct pSapTable_data *
pSapTable_duplicate(struct pSapTable_data *thedata)
{
	struct pSapTable_data *StorageNew = SNMP_MALLOC_STRUCT(pSapTable_data);

	DEBUGMSGTL(("acseMIB", "pSapTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		if (!(StorageNew->subsystemId = malloc(thedata->subsystemIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->subsystemId, thedata->subsystemId, thedata->subsystemIdLen);
		StorageNew->subsystemIdLen = thedata->subsystemIdLen;
		StorageNew->subsystemId[StorageNew->subsystemIdLen] = 0;
		if (!(StorageNew->pSapId = malloc(thedata->pSapIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->pSapId, thedata->pSapId, thedata->pSapIdLen);
		StorageNew->pSapIdLen = thedata->pSapIdLen;
		StorageNew->pSapId[StorageNew->pSapIdLen] = 0;
		if (!(StorageNew->pSapSap1Address = malloc(thedata->pSapSap1AddressLen + 1)))
			goto destroy;
		memcpy(StorageNew->pSapSap1Address, thedata->pSapSap1Address, thedata->pSapSap1AddressLen);
		StorageNew->pSapSap1AddressLen = thedata->pSapSap1AddressLen;
		StorageNew->pSapSap1Address[StorageNew->pSapSap1AddressLen] = 0;
		if (!(StorageNew->pSapUserEntityNames = malloc(thedata->pSapUserEntityNamesLen + 1)))
			goto destroy;
		memcpy(StorageNew->pSapUserEntityNames, thedata->pSapUserEntityNames, thedata->pSapUserEntityNamesLen);
		StorageNew->pSapUserEntityNamesLen = thedata->pSapUserEntityNamesLen;
		StorageNew->pSapUserEntityNames[StorageNew->pSapUserEntityNamesLen] = 0;
		StorageNew->pSapRowStatus = thedata->pSapRowStatus;
	}
      done:
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	pSapTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int pSapTable_destroy(struct pSapTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
pSapTable_destroy(struct pSapTable_data **thedata)
{
	struct pSapTable_data *StorageDel;

	DEBUGMSGTL(("acseMIB", "pSapTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->subsystemId);
		StorageDel->subsystemIdLen = 0;
		SNMP_FREE(StorageDel->pSapId);
		StorageDel->pSapIdLen = 0;
		SNMP_FREE(StorageDel->pSapSap1Address);
		StorageDel->pSapSap1AddressLen = 0;
		SNMP_FREE(StorageDel->pSapUserEntityNames);
		StorageDel->pSapUserEntityNamesLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int pSapTable_add(struct pSapTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the pSapTable table data set.
 *
 * Adds a table row structure to the pSapTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
pSapTable_add(struct pSapTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("acseMIB", "pSapTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* subsystemId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->subsystemId, thedata->subsystemIdLen);
		header_complex_add_data(&pSapTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("acseMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int pSapTable_del(struct pSapTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the pSapTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
pSapTable_del(struct pSapTable_data *thedata)
{
	struct pSapTable_data *StorageDel;

	DEBUGMSGTL(("acseMIB", "pSapTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(pSapTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&pSapTableStorage, hciptr);
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_pSapTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for pSapTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case pSapTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_pSapTable(const char *token, char *line)
{
	size_t tmpsize;
	struct pSapTable_data *StorageTmp = pSapTable_create();

	DEBUGMSGTL(("acseMIB", "parse_pSapTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->subsystemId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->subsystemId, &StorageTmp->subsystemIdLen);
	if (StorageTmp->subsystemId == NULL) {
		config_perror("invalid specification for subsystemId");
		return;
	}
	SNMP_FREE(StorageTmp->pSapId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->pSapId, &StorageTmp->pSapIdLen);
	if (StorageTmp->pSapId == NULL) {
		config_perror("invalid specification for pSapId");
		return;
	}
	SNMP_FREE(StorageTmp->pSapSap1Address);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->pSapSap1Address, &StorageTmp->pSapSap1AddressLen);
	if (StorageTmp->pSapSap1Address == NULL) {
		config_perror("invalid specification for pSapSap1Address");
		return;
	}
	SNMP_FREE(StorageTmp->pSapUserEntityNames);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->pSapUserEntityNames, &StorageTmp->pSapUserEntityNamesLen);
	if (StorageTmp->pSapUserEntityNames == NULL) {
		config_perror("invalid specification for pSapUserEntityNames");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->pSapRowStatus, &tmpsize);
	pSapTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("acseMIB", "done.\n"));
}

/*
 * store_pSapTable(): store configuraiton file for pSapTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_pSapTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct pSapTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("acseMIB", "store_pSapTable: storing data...  "));
	refresh_pSapTable(1);
	(void) tmpsize;
	for (hcindex = pSapTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct pSapTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "pSapTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->subsystemId, &StorageTmp->subsystemIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->pSapId, &StorageTmp->pSapIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->pSapSap1Address, &StorageTmp->pSapSap1AddressLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->pSapUserEntityNames, &StorageTmp->pSapUserEntityNamesLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->pSapRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct presentationConnectionTable_data *presentationConnectionTable_create(void)
 * @brief create a fresh data structure representing a new row in the presentationConnectionTable table.
 *
 * Creates a new presentationConnectionTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct presentationConnectionTable_data *
presentationConnectionTable_create(void)
{
	struct presentationConnectionTable_data *StorageNew = SNMP_MALLOC_STRUCT(presentationConnectionTable_data);

	DEBUGMSGTL(("acseMIB", "presentationConnectionTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->presentationSubsystemId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->presentationSubsystemIdLen = 0;
		StorageNew->presentationSubsystemId[StorageNew->presentationSubsystemIdLen] = 0;
		if ((StorageNew->presentationEntityId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->presentationEntityIdLen = 0;
		StorageNew->presentationEntityId[StorageNew->presentationEntityIdLen] = 0;
		if ((StorageNew->presentationCopmId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->presentationCopmIdLen = 0;
		StorageNew->presentationCopmId[StorageNew->presentationCopmIdLen] = 0;
		if ((StorageNew->presentationConnectionUnderlyingConnectionNames = malloc(1)) == NULL)
			goto nomem;
		StorageNew->presentationConnectionUnderlyingConnectionNamesLen = 0;
		StorageNew->presentationConnectionUnderlyingConnectionNames[StorageNew->presentationConnectionUnderlyingConnectionNamesLen] = 0;
		if ((StorageNew->presentationConnectionSupportedConnectionNames = malloc(1)) == NULL)
			goto nomem;
		StorageNew->presentationConnectionSupportedConnectionNamesLen = 0;
		StorageNew->presentationConnectionSupportedConnectionNames[StorageNew->presentationConnectionSupportedConnectionNamesLen] = 0;
		if (memdup((u_char **) &StorageNew->presentationConnectionPresFUinUse, (u_char *) "\x00", 1) != SNMPERR_SUCCESS)
			goto nomem;
			StorageNew->presentationConnectionPresFUinUseLen = 1;
		if ((StorageNew->presentationConnectionPresContextInUse = snmp_duplicate_objid(zeroDotZero_oid, 2)) == NULL)
			goto nomem;
			StorageNew->presentationConnectionPresContextInUseLen = 2;
		StorageNew->presentationCOnnectionRowStatus = 0;
		StorageNew->presentationCOnnectionRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	presentationConnectionTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct presentationConnectionTable_data *presentationConnectionTable_duplicate(struct presentationConnectionTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct presentationConnectionTable_data *
presentationConnectionTable_duplicate(struct presentationConnectionTable_data *thedata)
{
	struct presentationConnectionTable_data *StorageNew = SNMP_MALLOC_STRUCT(presentationConnectionTable_data);

	DEBUGMSGTL(("acseMIB", "presentationConnectionTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		if (!(StorageNew->presentationSubsystemId = malloc(thedata->presentationSubsystemIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->presentationSubsystemId, thedata->presentationSubsystemId, thedata->presentationSubsystemIdLen);
		StorageNew->presentationSubsystemIdLen = thedata->presentationSubsystemIdLen;
		StorageNew->presentationSubsystemId[StorageNew->presentationSubsystemIdLen] = 0;
		if (!(StorageNew->presentationEntityId = malloc(thedata->presentationEntityIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->presentationEntityId, thedata->presentationEntityId, thedata->presentationEntityIdLen);
		StorageNew->presentationEntityIdLen = thedata->presentationEntityIdLen;
		StorageNew->presentationEntityId[StorageNew->presentationEntityIdLen] = 0;
		if (!(StorageNew->presentationCopmId = malloc(thedata->presentationCopmIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->presentationCopmId, thedata->presentationCopmId, thedata->presentationCopmIdLen);
		StorageNew->presentationCopmIdLen = thedata->presentationCopmIdLen;
		StorageNew->presentationCopmId[StorageNew->presentationCopmIdLen] = 0;
		if (!(StorageNew->presentationConnectionId = malloc(thedata->presentationConnectionIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->presentationConnectionId, thedata->presentationConnectionId, thedata->presentationConnectionIdLen);
		StorageNew->presentationConnectionIdLen = thedata->presentationConnectionIdLen;
		StorageNew->presentationConnectionId[StorageNew->presentationConnectionIdLen] = 0;
		if (!(StorageNew->presentationConnectionUnderlyingConnectionNames = malloc(thedata->presentationConnectionUnderlyingConnectionNamesLen + 1)))
			goto destroy;
		memcpy(StorageNew->presentationConnectionUnderlyingConnectionNames, thedata->presentationConnectionUnderlyingConnectionNames,
		       thedata->presentationConnectionUnderlyingConnectionNamesLen);
		StorageNew->presentationConnectionUnderlyingConnectionNamesLen = thedata->presentationConnectionUnderlyingConnectionNamesLen;
		StorageNew->presentationConnectionUnderlyingConnectionNames[StorageNew->presentationConnectionUnderlyingConnectionNamesLen] = 0;
		if (!(StorageNew->presentationConnectionSupportedConnectionNames = malloc(thedata->presentationConnectionSupportedConnectionNamesLen + 1)))
			goto destroy;
		memcpy(StorageNew->presentationConnectionSupportedConnectionNames, thedata->presentationConnectionSupportedConnectionNames, thedata->presentationConnectionSupportedConnectionNamesLen);
		StorageNew->presentationConnectionSupportedConnectionNamesLen = thedata->presentationConnectionSupportedConnectionNamesLen;
		StorageNew->presentationConnectionSupportedConnectionNames[StorageNew->presentationConnectionSupportedConnectionNamesLen] = 0;
		if (!(StorageNew->presentationConnectionPresFUinUse = malloc(thedata->presentationConnectionPresFUinUseLen + 1)))
			goto destroy;
		memcpy(StorageNew->presentationConnectionPresFUinUse, thedata->presentationConnectionPresFUinUse, thedata->presentationConnectionPresFUinUseLen);
		StorageNew->presentationConnectionPresFUinUseLen = thedata->presentationConnectionPresFUinUseLen;
		StorageNew->presentationConnectionPresFUinUse[StorageNew->presentationConnectionPresFUinUseLen] = 0;
		if (!
		    (StorageNew->presentationConnectionPresContextInUse =
		     snmp_duplicate_objid(thedata->presentationConnectionPresContextInUse, thedata->presentationConnectionPresContextInUseLen / sizeof(oid))))
			goto destroy;
		StorageNew->presentationConnectionPresContextInUseLen = thedata->presentationConnectionPresContextInUseLen;
		StorageNew->presentationCOnnectionRowStatus = thedata->presentationCOnnectionRowStatus;
	}
      done:
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	presentationConnectionTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int presentationConnectionTable_destroy(struct presentationConnectionTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
presentationConnectionTable_destroy(struct presentationConnectionTable_data **thedata)
{
	struct presentationConnectionTable_data *StorageDel;

	DEBUGMSGTL(("acseMIB", "presentationConnectionTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->presentationSubsystemId);
		StorageDel->presentationSubsystemIdLen = 0;
		SNMP_FREE(StorageDel->presentationEntityId);
		StorageDel->presentationEntityIdLen = 0;
		SNMP_FREE(StorageDel->presentationCopmId);
		StorageDel->presentationCopmIdLen = 0;
		SNMP_FREE(StorageDel->presentationConnectionId);
		StorageDel->presentationConnectionIdLen = 0;
		SNMP_FREE(StorageDel->presentationConnectionUnderlyingConnectionNames);
		StorageDel->presentationConnectionUnderlyingConnectionNamesLen = 0;
		SNMP_FREE(StorageDel->presentationConnectionSupportedConnectionNames);
		StorageDel->presentationConnectionSupportedConnectionNamesLen = 0;
		SNMP_FREE(StorageDel->presentationConnectionPresFUinUse);
		StorageDel->presentationConnectionPresFUinUseLen = 0;
		SNMP_FREE(StorageDel->presentationConnectionPresContextInUse);
		StorageDel->presentationConnectionPresContextInUseLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int presentationConnectionTable_add(struct presentationConnectionTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the presentationConnectionTable table data set.
 *
 * Adds a table row structure to the presentationConnectionTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
presentationConnectionTable_add(struct presentationConnectionTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("acseMIB", "presentationConnectionTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* presentationSubsystemId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->presentationSubsystemId, thedata->presentationSubsystemIdLen);
		/* presentationEntityId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->presentationEntityId, thedata->presentationEntityIdLen);
		/* presentationCopmId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->presentationCopmId, thedata->presentationCopmIdLen);
		/* presentationConnectionId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->presentationConnectionId, thedata->presentationConnectionIdLen);
		header_complex_add_data(&presentationConnectionTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("acseMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int presentationConnectionTable_del(struct presentationConnectionTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the presentationConnectionTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
presentationConnectionTable_del(struct presentationConnectionTable_data *thedata)
{
	struct presentationConnectionTable_data *StorageDel;

	DEBUGMSGTL(("acseMIB", "presentationConnectionTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(presentationConnectionTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&presentationConnectionTableStorage, hciptr);
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_presentationConnectionTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for presentationConnectionTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case presentationConnectionTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_presentationConnectionTable(const char *token, char *line)
{
	size_t tmpsize;
	struct presentationConnectionTable_data *StorageTmp = presentationConnectionTable_create();

	DEBUGMSGTL(("acseMIB", "parse_presentationConnectionTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->presentationSubsystemId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->presentationSubsystemId, &StorageTmp->presentationSubsystemIdLen);
	if (StorageTmp->presentationSubsystemId == NULL) {
		config_perror("invalid specification for presentationSubsystemId");
		return;
	}
	SNMP_FREE(StorageTmp->presentationEntityId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->presentationEntityId, &StorageTmp->presentationEntityIdLen);
	if (StorageTmp->presentationEntityId == NULL) {
		config_perror("invalid specification for presentationEntityId");
		return;
	}
	SNMP_FREE(StorageTmp->presentationCopmId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->presentationCopmId, &StorageTmp->presentationCopmIdLen);
	if (StorageTmp->presentationCopmId == NULL) {
		config_perror("invalid specification for presentationCopmId");
		return;
	}
	SNMP_FREE(StorageTmp->presentationConnectionId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->presentationConnectionId, &StorageTmp->presentationConnectionIdLen);
	if (StorageTmp->presentationConnectionId == NULL) {
		config_perror("invalid specification for presentationConnectionId");
		return;
	}
	SNMP_FREE(StorageTmp->presentationConnectionUnderlyingConnectionNames);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->presentationConnectionUnderlyingConnectionNames, &StorageTmp->presentationConnectionUnderlyingConnectionNamesLen);
	if (StorageTmp->presentationConnectionUnderlyingConnectionNames == NULL) {
		config_perror("invalid specification for presentationConnectionUnderlyingConnectionNames");
		return;
	}
	SNMP_FREE(StorageTmp->presentationConnectionSupportedConnectionNames);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->presentationConnectionSupportedConnectionNames, &StorageTmp->presentationConnectionSupportedConnectionNamesLen);
	if (StorageTmp->presentationConnectionSupportedConnectionNames == NULL) {
		config_perror("invalid specification for presentationConnectionSupportedConnectionNames");
		return;
	}
	SNMP_FREE(StorageTmp->presentationConnectionPresFUinUse);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->presentationConnectionPresFUinUse, &StorageTmp->presentationConnectionPresFUinUseLen);
	if (StorageTmp->presentationConnectionPresFUinUse == NULL) {
		config_perror("invalid specification for presentationConnectionPresFUinUse");
		return;
	}
	SNMP_FREE(StorageTmp->presentationConnectionPresContextInUse);
	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->presentationConnectionPresContextInUse, &StorageTmp->presentationConnectionPresContextInUseLen);
	if (StorageTmp->presentationConnectionPresContextInUse == NULL) {
		config_perror("invalid specification for presentationConnectionPresContextInUse");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->presentationCOnnectionRowStatus, &tmpsize);
	presentationConnectionTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("acseMIB", "done.\n"));
}

/*
 * store_presentationConnectionTable(): store configuraiton file for presentationConnectionTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_presentationConnectionTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct presentationConnectionTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("acseMIB", "store_presentationConnectionTable: storing data...  "));
	refresh_presentationConnectionTable(1);
	(void) tmpsize;
	for (hcindex = presentationConnectionTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct presentationConnectionTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "presentationConnectionTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->presentationSubsystemId, &StorageTmp->presentationSubsystemIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->presentationEntityId, &StorageTmp->presentationEntityIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->presentationCopmId, &StorageTmp->presentationCopmIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->presentationConnectionId, &StorageTmp->presentationConnectionIdLen);
			cptr =
			    read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->presentationConnectionUnderlyingConnectionNames, &StorageTmp->presentationConnectionUnderlyingConnectionNamesLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->presentationConnectionSupportedConnectionNames, &StorageTmp->presentationConnectionSupportedConnectionNamesLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->presentationConnectionPresFUinUse, &StorageTmp->presentationConnectionPresFUinUseLen);
			cptr = read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->presentationConnectionPresContextInUse, &StorageTmp->presentationConnectionPresContextInUseLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->presentationCOnnectionRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct presentationCopmTable_data *presentationCopmTable_create(void)
 * @brief create a fresh data structure representing a new row in the presentationCopmTable table.
 *
 * Creates a new presentationCopmTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct presentationCopmTable_data *
presentationCopmTable_create(void)
{
	struct presentationCopmTable_data *StorageNew = SNMP_MALLOC_STRUCT(presentationCopmTable_data);

	DEBUGMSGTL(("acseMIB", "presentationCopmTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->presentationSubsystemId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->presentationSubsystemIdLen = 0;
		StorageNew->presentationSubsystemId[StorageNew->presentationSubsystemIdLen] = 0;
		if ((StorageNew->presentationEntityId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->presentationEntityIdLen = 0;
		StorageNew->presentationEntityId[StorageNew->presentationEntityIdLen] = 0;
		StorageNew->presentationCopmOperationalState = 0;
		if (memdup((u_char **) &StorageNew->presentationCopmPresFUsupport, (u_char *) "\x00", 1) != SNMPERR_SUCCESS)
			goto nomem;
			StorageNew->presentationCopmPresFUsupportLen = 1;
		if ((StorageNew->presentationCopmabstrSyntaxSupport = malloc(1)) == NULL)
			goto nomem;
		StorageNew->presentationCopmabstrSyntaxSupportLen = 0;
		StorageNew->presentationCopmabstrSyntaxSupport[StorageNew->presentationCopmabstrSyntaxSupportLen] = 0;
		if ((StorageNew->presentationCopmtransSyntaxSupport = malloc(1)) == NULL)
			goto nomem;
		StorageNew->presentationCopmtransSyntaxSupportLen = 0;
		StorageNew->presentationCopmtransSyntaxSupport[StorageNew->presentationCopmtransSyntaxSupportLen] = 0;
		StorageNew->presentationCopmRowStatus = 0;
		StorageNew->presentationCopmRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	presentationCopmTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct presentationCopmTable_data *presentationCopmTable_duplicate(struct presentationCopmTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct presentationCopmTable_data *
presentationCopmTable_duplicate(struct presentationCopmTable_data *thedata)
{
	struct presentationCopmTable_data *StorageNew = SNMP_MALLOC_STRUCT(presentationCopmTable_data);

	DEBUGMSGTL(("acseMIB", "presentationCopmTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		if (!(StorageNew->presentationSubsystemId = malloc(thedata->presentationSubsystemIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->presentationSubsystemId, thedata->presentationSubsystemId, thedata->presentationSubsystemIdLen);
		StorageNew->presentationSubsystemIdLen = thedata->presentationSubsystemIdLen;
		StorageNew->presentationSubsystemId[StorageNew->presentationSubsystemIdLen] = 0;
		if (!(StorageNew->presentationEntityId = malloc(thedata->presentationEntityIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->presentationEntityId, thedata->presentationEntityId, thedata->presentationEntityIdLen);
		StorageNew->presentationEntityIdLen = thedata->presentationEntityIdLen;
		StorageNew->presentationEntityId[StorageNew->presentationEntityIdLen] = 0;
		if (!(StorageNew->presentationCopmId = malloc(thedata->presentationCopmIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->presentationCopmId, thedata->presentationCopmId, thedata->presentationCopmIdLen);
		StorageNew->presentationCopmIdLen = thedata->presentationCopmIdLen;
		StorageNew->presentationCopmId[StorageNew->presentationCopmIdLen] = 0;
		StorageNew->presentationCopmOperationalState = thedata->presentationCopmOperationalState;
		if (!(StorageNew->presentationCopmPresFUsupport = malloc(thedata->presentationCopmPresFUsupportLen + 1)))
			goto destroy;
		memcpy(StorageNew->presentationCopmPresFUsupport, thedata->presentationCopmPresFUsupport, thedata->presentationCopmPresFUsupportLen);
		StorageNew->presentationCopmPresFUsupportLen = thedata->presentationCopmPresFUsupportLen;
		StorageNew->presentationCopmPresFUsupport[StorageNew->presentationCopmPresFUsupportLen] = 0;
		if (!(StorageNew->presentationCopmabstrSyntaxSupport = malloc(thedata->presentationCopmabstrSyntaxSupportLen + 1)))
			goto destroy;
		memcpy(StorageNew->presentationCopmabstrSyntaxSupport, thedata->presentationCopmabstrSyntaxSupport, thedata->presentationCopmabstrSyntaxSupportLen);
		StorageNew->presentationCopmabstrSyntaxSupportLen = thedata->presentationCopmabstrSyntaxSupportLen;
		StorageNew->presentationCopmabstrSyntaxSupport[StorageNew->presentationCopmabstrSyntaxSupportLen] = 0;
		if (!(StorageNew->presentationCopmtransSyntaxSupport = malloc(thedata->presentationCopmtransSyntaxSupportLen + 1)))
			goto destroy;
		memcpy(StorageNew->presentationCopmtransSyntaxSupport, thedata->presentationCopmtransSyntaxSupport, thedata->presentationCopmtransSyntaxSupportLen);
		StorageNew->presentationCopmtransSyntaxSupportLen = thedata->presentationCopmtransSyntaxSupportLen;
		StorageNew->presentationCopmtransSyntaxSupport[StorageNew->presentationCopmtransSyntaxSupportLen] = 0;
		StorageNew->presentationCopmRowStatus = thedata->presentationCopmRowStatus;
	}
      done:
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	presentationCopmTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int presentationCopmTable_destroy(struct presentationCopmTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
presentationCopmTable_destroy(struct presentationCopmTable_data **thedata)
{
	struct presentationCopmTable_data *StorageDel;

	DEBUGMSGTL(("acseMIB", "presentationCopmTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->presentationSubsystemId);
		StorageDel->presentationSubsystemIdLen = 0;
		SNMP_FREE(StorageDel->presentationEntityId);
		StorageDel->presentationEntityIdLen = 0;
		SNMP_FREE(StorageDel->presentationCopmId);
		StorageDel->presentationCopmIdLen = 0;
		SNMP_FREE(StorageDel->presentationCopmPresFUsupport);
		StorageDel->presentationCopmPresFUsupportLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int presentationCopmTable_add(struct presentationCopmTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the presentationCopmTable table data set.
 *
 * Adds a table row structure to the presentationCopmTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
presentationCopmTable_add(struct presentationCopmTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("acseMIB", "presentationCopmTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* presentationSubsystemId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->presentationSubsystemId, thedata->presentationSubsystemIdLen);
		/* presentationEntityId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->presentationEntityId, thedata->presentationEntityIdLen);
		/* presentationCopmId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->presentationCopmId, thedata->presentationCopmIdLen);
		header_complex_add_data(&presentationCopmTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("acseMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int presentationCopmTable_del(struct presentationCopmTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the presentationCopmTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
presentationCopmTable_del(struct presentationCopmTable_data *thedata)
{
	struct presentationCopmTable_data *StorageDel;

	DEBUGMSGTL(("acseMIB", "presentationCopmTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(presentationCopmTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&presentationCopmTableStorage, hciptr);
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_presentationCopmTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for presentationCopmTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case presentationCopmTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_presentationCopmTable(const char *token, char *line)
{
	size_t tmpsize;
	struct presentationCopmTable_data *StorageTmp = presentationCopmTable_create();

	DEBUGMSGTL(("acseMIB", "parse_presentationCopmTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->presentationSubsystemId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->presentationSubsystemId, &StorageTmp->presentationSubsystemIdLen);
	if (StorageTmp->presentationSubsystemId == NULL) {
		config_perror("invalid specification for presentationSubsystemId");
		return;
	}
	SNMP_FREE(StorageTmp->presentationEntityId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->presentationEntityId, &StorageTmp->presentationEntityIdLen);
	if (StorageTmp->presentationEntityId == NULL) {
		config_perror("invalid specification for presentationEntityId");
		return;
	}
	SNMP_FREE(StorageTmp->presentationCopmId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->presentationCopmId, &StorageTmp->presentationCopmIdLen);
	if (StorageTmp->presentationCopmId == NULL) {
		config_perror("invalid specification for presentationCopmId");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->presentationCopmOperationalState, &tmpsize);
	SNMP_FREE(StorageTmp->presentationCopmPresFUsupport);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->presentationCopmPresFUsupport, &StorageTmp->presentationCopmPresFUsupportLen);
	if (StorageTmp->presentationCopmPresFUsupport == NULL) {
		config_perror("invalid specification for presentationCopmPresFUsupport");
		return;
	}
	SNMP_FREE(StorageTmp->presentationCopmabstrSyntaxSupport);
	line = read_config_read_data(ASN_OPAQUE, line, &StorageTmp->presentationCopmabstrSyntaxSupport, &StorageTmp->presentationCopmabstrSyntaxSupportLen);
	if (StorageTmp->presentationCopmabstrSyntaxSupport == NULL) {
		config_perror("invalid specification for presentationCopmabstrSyntaxSupport");
		return;
	}
	SNMP_FREE(StorageTmp->presentationCopmtransSyntaxSupport);
	line = read_config_read_data(ASN_OPAQUE, line, &StorageTmp->presentationCopmtransSyntaxSupport, &StorageTmp->presentationCopmtransSyntaxSupportLen);
	if (StorageTmp->presentationCopmtransSyntaxSupport == NULL) {
		config_perror("invalid specification for presentationCopmtransSyntaxSupport");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->presentationCopmRowStatus, &tmpsize);
	presentationCopmTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("acseMIB", "done.\n"));
}

/*
 * store_presentationCopmTable(): store configuraiton file for presentationCopmTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_presentationCopmTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct presentationCopmTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("acseMIB", "store_presentationCopmTable: storing data...  "));
	refresh_presentationCopmTable(1);
	(void) tmpsize;
	for (hcindex = presentationCopmTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct presentationCopmTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "presentationCopmTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->presentationSubsystemId, &StorageTmp->presentationSubsystemIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->presentationEntityId, &StorageTmp->presentationEntityIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->presentationCopmId, &StorageTmp->presentationCopmIdLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->presentationCopmOperationalState, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->presentationCopmPresFUsupport, &StorageTmp->presentationCopmPresFUsupportLen);
			cptr = read_config_store_data(ASN_OPAQUE, cptr, &StorageTmp->presentationCopmabstrSyntaxSupport, &StorageTmp->presentationCopmabstrSyntaxSupportLen);
			cptr = read_config_store_data(ASN_OPAQUE, cptr, &StorageTmp->presentationCopmtransSyntaxSupport, &StorageTmp->presentationCopmtransSyntaxSupportLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->presentationCopmRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct presentationEntityTable_data *presentationEntityTable_create(void)
 * @brief create a fresh data structure representing a new row in the presentationEntityTable table.
 *
 * Creates a new presentationEntityTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct presentationEntityTable_data *
presentationEntityTable_create(void)
{
	struct presentationEntityTable_data *StorageNew = SNMP_MALLOC_STRUCT(presentationEntityTable_data);

	DEBUGMSGTL(("acseMIB", "presentationEntityTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->presentationSubsystemId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->presentationSubsystemIdLen = 0;
		StorageNew->presentationSubsystemId[StorageNew->presentationSubsystemIdLen] = 0;
		if ((StorageNew->presentationEntityLocalSapNames = malloc(1)) == NULL)
			goto nomem;
		StorageNew->presentationEntityLocalSapNamesLen = 0;
		StorageNew->presentationEntityLocalSapNames[StorageNew->presentationEntityLocalSapNamesLen] = 0;
		StorageNew->presentationEntityOperationalState = 0;
		StorageNew->presentationEntityRowStatus = 0;
		StorageNew->presentationEntityRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	presentationEntityTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct presentationEntityTable_data *presentationEntityTable_duplicate(struct presentationEntityTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct presentationEntityTable_data *
presentationEntityTable_duplicate(struct presentationEntityTable_data *thedata)
{
	struct presentationEntityTable_data *StorageNew = SNMP_MALLOC_STRUCT(presentationEntityTable_data);

	DEBUGMSGTL(("acseMIB", "presentationEntityTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		if (!(StorageNew->presentationSubsystemId = malloc(thedata->presentationSubsystemIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->presentationSubsystemId, thedata->presentationSubsystemId, thedata->presentationSubsystemIdLen);
		StorageNew->presentationSubsystemIdLen = thedata->presentationSubsystemIdLen;
		StorageNew->presentationSubsystemId[StorageNew->presentationSubsystemIdLen] = 0;
		if (!(StorageNew->presentationEntityId = malloc(thedata->presentationEntityIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->presentationEntityId, thedata->presentationEntityId, thedata->presentationEntityIdLen);
		StorageNew->presentationEntityIdLen = thedata->presentationEntityIdLen;
		StorageNew->presentationEntityId[StorageNew->presentationEntityIdLen] = 0;
		if (!(StorageNew->presentationEntityLocalSapNames = malloc(thedata->presentationEntityLocalSapNamesLen + 1)))
			goto destroy;
		memcpy(StorageNew->presentationEntityLocalSapNames, thedata->presentationEntityLocalSapNames, thedata->presentationEntityLocalSapNamesLen);
		StorageNew->presentationEntityLocalSapNamesLen = thedata->presentationEntityLocalSapNamesLen;
		StorageNew->presentationEntityLocalSapNames[StorageNew->presentationEntityLocalSapNamesLen] = 0;
		StorageNew->presentationEntityOperationalState = thedata->presentationEntityOperationalState;
		StorageNew->presentationEntityRowStatus = thedata->presentationEntityRowStatus;
	}
      done:
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	presentationEntityTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int presentationEntityTable_destroy(struct presentationEntityTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
presentationEntityTable_destroy(struct presentationEntityTable_data **thedata)
{
	struct presentationEntityTable_data *StorageDel;

	DEBUGMSGTL(("acseMIB", "presentationEntityTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->presentationSubsystemId);
		StorageDel->presentationSubsystemIdLen = 0;
		SNMP_FREE(StorageDel->presentationEntityId);
		StorageDel->presentationEntityIdLen = 0;
		SNMP_FREE(StorageDel->presentationEntityLocalSapNames);
		StorageDel->presentationEntityLocalSapNamesLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int presentationEntityTable_add(struct presentationEntityTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the presentationEntityTable table data set.
 *
 * Adds a table row structure to the presentationEntityTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
presentationEntityTable_add(struct presentationEntityTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("acseMIB", "presentationEntityTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* presentationSubsystemId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->presentationSubsystemId, thedata->presentationSubsystemIdLen);
		/* presentationEntityId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->presentationEntityId, thedata->presentationEntityIdLen);
		header_complex_add_data(&presentationEntityTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("acseMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int presentationEntityTable_del(struct presentationEntityTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the presentationEntityTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
presentationEntityTable_del(struct presentationEntityTable_data *thedata)
{
	struct presentationEntityTable_data *StorageDel;

	DEBUGMSGTL(("acseMIB", "presentationEntityTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(presentationEntityTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&presentationEntityTableStorage, hciptr);
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_presentationEntityTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for presentationEntityTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case presentationEntityTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_presentationEntityTable(const char *token, char *line)
{
	size_t tmpsize;
	struct presentationEntityTable_data *StorageTmp = presentationEntityTable_create();

	DEBUGMSGTL(("acseMIB", "parse_presentationEntityTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->presentationSubsystemId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->presentationSubsystemId, &StorageTmp->presentationSubsystemIdLen);
	if (StorageTmp->presentationSubsystemId == NULL) {
		config_perror("invalid specification for presentationSubsystemId");
		return;
	}
	SNMP_FREE(StorageTmp->presentationEntityId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->presentationEntityId, &StorageTmp->presentationEntityIdLen);
	if (StorageTmp->presentationEntityId == NULL) {
		config_perror("invalid specification for presentationEntityId");
		return;
	}
	SNMP_FREE(StorageTmp->presentationEntityLocalSapNames);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->presentationEntityLocalSapNames, &StorageTmp->presentationEntityLocalSapNamesLen);
	if (StorageTmp->presentationEntityLocalSapNames == NULL) {
		config_perror("invalid specification for presentationEntityLocalSapNames");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->presentationEntityOperationalState, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->presentationEntityRowStatus, &tmpsize);
	presentationEntityTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("acseMIB", "done.\n"));
}

/*
 * store_presentationEntityTable(): store configuraiton file for presentationEntityTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_presentationEntityTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct presentationEntityTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("acseMIB", "store_presentationEntityTable: storing data...  "));
	refresh_presentationEntityTable(1);
	(void) tmpsize;
	for (hcindex = presentationEntityTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct presentationEntityTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "presentationEntityTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->presentationSubsystemId, &StorageTmp->presentationSubsystemIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->presentationEntityId, &StorageTmp->presentationEntityIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->presentationEntityLocalSapNames, &StorageTmp->presentationEntityLocalSapNamesLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->presentationEntityOperationalState, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->presentationEntityRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct presentationSubsystemTable_data *presentationSubsystemTable_create(void)
 * @brief create a fresh data structure representing a new row in the presentationSubsystemTable table.
 *
 * Creates a new presentationSubsystemTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct presentationSubsystemTable_data *
presentationSubsystemTable_create(void)
{
	struct presentationSubsystemTable_data *StorageNew = SNMP_MALLOC_STRUCT(presentationSubsystemTable_data);

	DEBUGMSGTL(("acseMIB", "presentationSubsystemTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->presentationSubsystemRowStatus = 0;
		StorageNew->presentationSubsystemRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	presentationSubsystemTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct presentationSubsystemTable_data *presentationSubsystemTable_duplicate(struct presentationSubsystemTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct presentationSubsystemTable_data *
presentationSubsystemTable_duplicate(struct presentationSubsystemTable_data *thedata)
{
	struct presentationSubsystemTable_data *StorageNew = SNMP_MALLOC_STRUCT(presentationSubsystemTable_data);

	DEBUGMSGTL(("acseMIB", "presentationSubsystemTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		if (!(StorageNew->presentationSubsystemId = malloc(thedata->presentationSubsystemIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->presentationSubsystemId, thedata->presentationSubsystemId, thedata->presentationSubsystemIdLen);
		StorageNew->presentationSubsystemIdLen = thedata->presentationSubsystemIdLen;
		StorageNew->presentationSubsystemId[StorageNew->presentationSubsystemIdLen] = 0;
		StorageNew->presentationSubsystemRowStatus = thedata->presentationSubsystemRowStatus;
	}
      done:
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	presentationSubsystemTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int presentationSubsystemTable_destroy(struct presentationSubsystemTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
presentationSubsystemTable_destroy(struct presentationSubsystemTable_data **thedata)
{
	struct presentationSubsystemTable_data *StorageDel;

	DEBUGMSGTL(("acseMIB", "presentationSubsystemTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->presentationSubsystemId);
		StorageDel->presentationSubsystemIdLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int presentationSubsystemTable_add(struct presentationSubsystemTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the presentationSubsystemTable table data set.
 *
 * Adds a table row structure to the presentationSubsystemTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
presentationSubsystemTable_add(struct presentationSubsystemTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("acseMIB", "presentationSubsystemTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* presentationSubsystemId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->presentationSubsystemId, thedata->presentationSubsystemIdLen);
		header_complex_add_data(&presentationSubsystemTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("acseMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int presentationSubsystemTable_del(struct presentationSubsystemTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the presentationSubsystemTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
presentationSubsystemTable_del(struct presentationSubsystemTable_data *thedata)
{
	struct presentationSubsystemTable_data *StorageDel;

	DEBUGMSGTL(("acseMIB", "presentationSubsystemTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(presentationSubsystemTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&presentationSubsystemTableStorage, hciptr);
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_presentationSubsystemTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for presentationSubsystemTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case presentationSubsystemTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_presentationSubsystemTable(const char *token, char *line)
{
	size_t tmpsize;
	struct presentationSubsystemTable_data *StorageTmp = presentationSubsystemTable_create();

	DEBUGMSGTL(("acseMIB", "parse_presentationSubsystemTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->presentationSubsystemId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->presentationSubsystemId, &StorageTmp->presentationSubsystemIdLen);
	if (StorageTmp->presentationSubsystemId == NULL) {
		config_perror("invalid specification for presentationSubsystemId");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->presentationSubsystemRowStatus, &tmpsize);
	presentationSubsystemTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("acseMIB", "done.\n"));
}

/*
 * store_presentationSubsystemTable(): store configuraiton file for presentationSubsystemTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_presentationSubsystemTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct presentationSubsystemTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("acseMIB", "store_presentationSubsystemTable: storing data...  "));
	refresh_presentationSubsystemTable(1);
	(void) tmpsize;
	for (hcindex = presentationSubsystemTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct presentationSubsystemTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "presentationSubsystemTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->presentationSubsystemId, &StorageTmp->presentationSubsystemIdLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->presentationSubsystemRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct sSapTable_data *sSapTable_create(void)
 * @brief create a fresh data structure representing a new row in the sSapTable table.
 *
 * Creates a new sSapTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct sSapTable_data *
sSapTable_create(void)
{
	struct sSapTable_data *StorageNew = SNMP_MALLOC_STRUCT(sSapTable_data);

	DEBUGMSGTL(("acseMIB", "sSapTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->subsystemId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->subsystemIdLen = 0;
		StorageNew->subsystemId[StorageNew->subsystemIdLen] = 0;
		if ((StorageNew->sSapId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->sSapIdLen = 0;
		StorageNew->sSapId[StorageNew->sSapIdLen] = 0;
		if ((StorageNew->sSapSap1Address = malloc(1)) == NULL)
			goto nomem;
		StorageNew->sSapSap1AddressLen = 0;
		StorageNew->sSapSap1Address[StorageNew->sSapSap1AddressLen] = 0;
		if ((StorageNew->sSapUserEntityNames = malloc(1)) == NULL)
			goto nomem;
		StorageNew->sSapUserEntityNamesLen = 0;
		StorageNew->sSapUserEntityNames[StorageNew->sSapUserEntityNamesLen] = 0;
		StorageNew->sSapRowStatus = 0;
		StorageNew->sSapRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	sSapTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct sSapTable_data *sSapTable_duplicate(struct sSapTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct sSapTable_data *
sSapTable_duplicate(struct sSapTable_data *thedata)
{
	struct sSapTable_data *StorageNew = SNMP_MALLOC_STRUCT(sSapTable_data);

	DEBUGMSGTL(("acseMIB", "sSapTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		if (!(StorageNew->subsystemId = malloc(thedata->subsystemIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->subsystemId, thedata->subsystemId, thedata->subsystemIdLen);
		StorageNew->subsystemIdLen = thedata->subsystemIdLen;
		StorageNew->subsystemId[StorageNew->subsystemIdLen] = 0;
		if (!(StorageNew->sSapId = malloc(thedata->sSapIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->sSapId, thedata->sSapId, thedata->sSapIdLen);
		StorageNew->sSapIdLen = thedata->sSapIdLen;
		StorageNew->sSapId[StorageNew->sSapIdLen] = 0;
		if (!(StorageNew->sSapSap1Address = malloc(thedata->sSapSap1AddressLen + 1)))
			goto destroy;
		memcpy(StorageNew->sSapSap1Address, thedata->sSapSap1Address, thedata->sSapSap1AddressLen);
		StorageNew->sSapSap1AddressLen = thedata->sSapSap1AddressLen;
		StorageNew->sSapSap1Address[StorageNew->sSapSap1AddressLen] = 0;
		if (!(StorageNew->sSapUserEntityNames = malloc(thedata->sSapUserEntityNamesLen + 1)))
			goto destroy;
		memcpy(StorageNew->sSapUserEntityNames, thedata->sSapUserEntityNames, thedata->sSapUserEntityNamesLen);
		StorageNew->sSapUserEntityNamesLen = thedata->sSapUserEntityNamesLen;
		StorageNew->sSapUserEntityNames[StorageNew->sSapUserEntityNamesLen] = 0;
		StorageNew->sSapRowStatus = thedata->sSapRowStatus;
	}
      done:
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	sSapTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int sSapTable_destroy(struct sSapTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
sSapTable_destroy(struct sSapTable_data **thedata)
{
	struct sSapTable_data *StorageDel;

	DEBUGMSGTL(("acseMIB", "sSapTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->subsystemId);
		StorageDel->subsystemIdLen = 0;
		SNMP_FREE(StorageDel->sSapId);
		StorageDel->sSapIdLen = 0;
		SNMP_FREE(StorageDel->sSapSap1Address);
		StorageDel->sSapSap1AddressLen = 0;
		SNMP_FREE(StorageDel->sSapUserEntityNames);
		StorageDel->sSapUserEntityNamesLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int sSapTable_add(struct sSapTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the sSapTable table data set.
 *
 * Adds a table row structure to the sSapTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
sSapTable_add(struct sSapTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("acseMIB", "sSapTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* subsystemId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->subsystemId, thedata->subsystemIdLen);
		header_complex_add_data(&sSapTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("acseMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int sSapTable_del(struct sSapTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the sSapTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
sSapTable_del(struct sSapTable_data *thedata)
{
	struct sSapTable_data *StorageDel;

	DEBUGMSGTL(("acseMIB", "sSapTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(sSapTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&sSapTableStorage, hciptr);
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_sSapTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for sSapTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case sSapTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_sSapTable(const char *token, char *line)
{
	size_t tmpsize;
	struct sSapTable_data *StorageTmp = sSapTable_create();

	DEBUGMSGTL(("acseMIB", "parse_sSapTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->subsystemId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->subsystemId, &StorageTmp->subsystemIdLen);
	if (StorageTmp->subsystemId == NULL) {
		config_perror("invalid specification for subsystemId");
		return;
	}
	SNMP_FREE(StorageTmp->sSapId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->sSapId, &StorageTmp->sSapIdLen);
	if (StorageTmp->sSapId == NULL) {
		config_perror("invalid specification for sSapId");
		return;
	}
	SNMP_FREE(StorageTmp->sSapSap1Address);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->sSapSap1Address, &StorageTmp->sSapSap1AddressLen);
	if (StorageTmp->sSapSap1Address == NULL) {
		config_perror("invalid specification for sSapSap1Address");
		return;
	}
	SNMP_FREE(StorageTmp->sSapUserEntityNames);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->sSapUserEntityNames, &StorageTmp->sSapUserEntityNamesLen);
	if (StorageTmp->sSapUserEntityNames == NULL) {
		config_perror("invalid specification for sSapUserEntityNames");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->sSapRowStatus, &tmpsize);
	sSapTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("acseMIB", "done.\n"));
}

/*
 * store_sSapTable(): store configuraiton file for sSapTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_sSapTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct sSapTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("acseMIB", "store_sSapTable: storing data...  "));
	refresh_sSapTable(1);
	(void) tmpsize;
	for (hcindex = sSapTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct sSapTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "sSapTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->subsystemId, &StorageTmp->subsystemIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->sSapId, &StorageTmp->sSapIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->sSapSap1Address, &StorageTmp->sSapSap1AddressLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->sSapUserEntityNames, &StorageTmp->sSapUserEntityNamesLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->sSapRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct sessionConnectionTable_data *sessionConnectionTable_create(void)
 * @brief create a fresh data structure representing a new row in the sessionConnectionTable table.
 *
 * Creates a new sessionConnectionTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct sessionConnectionTable_data *
sessionConnectionTable_create(void)
{
	struct sessionConnectionTable_data *StorageNew = SNMP_MALLOC_STRUCT(sessionConnectionTable_data);

	DEBUGMSGTL(("acseMIB", "sessionConnectionTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->sessionSubsystemId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->sessionSubsystemIdLen = 0;
		StorageNew->sessionSubsystemId[StorageNew->sessionSubsystemIdLen] = 0;
		if ((StorageNew->sessionEntityId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->sessionEntityIdLen = 0;
		StorageNew->sessionEntityId[StorageNew->sessionEntityIdLen] = 0;
		if ((StorageNew->sessionCopmId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->sessionCopmIdLen = 0;
		StorageNew->sessionCopmId[StorageNew->sessionCopmIdLen] = 0;
		if ((StorageNew->sessionConnectionUnderlyingConnectionNames = malloc(1)) == NULL)
			goto nomem;
		StorageNew->sessionConnectionUnderlyingConnectionNamesLen = 0;
		StorageNew->sessionConnectionUnderlyingConnectionNames[StorageNew->sessionConnectionUnderlyingConnectionNamesLen] = 0;
		if ((StorageNew->sessionConnectionSupportedConnectionNames = malloc(1)) == NULL)
			goto nomem;
		StorageNew->sessionConnectionSupportedConnectionNamesLen = 0;
		StorageNew->sessionConnectionSupportedConnectionNames[StorageNew->sessionConnectionSupportedConnectionNamesLen] = 0;
		if (memdup((u_char **) &StorageNew->sessionConnectionSessProtInUse, (u_char *) "\x00", 1) != SNMPERR_SUCCESS)
			goto nomem;
			StorageNew->sessionConnectionSessProtInUseLen = 1;
		if (memdup((u_char **) &StorageNew->sessionConnectionSessFUinUse, (u_char *) "\x00", 1) != SNMPERR_SUCCESS)
			goto nomem;
			StorageNew->sessionConnectionSessFUinUseLen = 1;
		StorageNew->sessionConnectionMaxTSDUsize = 0;
		StorageNew->sessionCOnnectionRowStatus = 0;
		StorageNew->sessionCOnnectionRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	sessionConnectionTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct sessionConnectionTable_data *sessionConnectionTable_duplicate(struct sessionConnectionTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct sessionConnectionTable_data *
sessionConnectionTable_duplicate(struct sessionConnectionTable_data *thedata)
{
	struct sessionConnectionTable_data *StorageNew = SNMP_MALLOC_STRUCT(sessionConnectionTable_data);

	DEBUGMSGTL(("acseMIB", "sessionConnectionTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		if (!(StorageNew->sessionSubsystemId = malloc(thedata->sessionSubsystemIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->sessionSubsystemId, thedata->sessionSubsystemId, thedata->sessionSubsystemIdLen);
		StorageNew->sessionSubsystemIdLen = thedata->sessionSubsystemIdLen;
		StorageNew->sessionSubsystemId[StorageNew->sessionSubsystemIdLen] = 0;
		if (!(StorageNew->sessionEntityId = malloc(thedata->sessionEntityIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->sessionEntityId, thedata->sessionEntityId, thedata->sessionEntityIdLen);
		StorageNew->sessionEntityIdLen = thedata->sessionEntityIdLen;
		StorageNew->sessionEntityId[StorageNew->sessionEntityIdLen] = 0;
		if (!(StorageNew->sessionCopmId = malloc(thedata->sessionCopmIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->sessionCopmId, thedata->sessionCopmId, thedata->sessionCopmIdLen);
		StorageNew->sessionCopmIdLen = thedata->sessionCopmIdLen;
		StorageNew->sessionCopmId[StorageNew->sessionCopmIdLen] = 0;
		if (!(StorageNew->sessionConnectionId = malloc(thedata->sessionConnectionIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->sessionConnectionId, thedata->sessionConnectionId, thedata->sessionConnectionIdLen);
		StorageNew->sessionConnectionIdLen = thedata->sessionConnectionIdLen;
		StorageNew->sessionConnectionId[StorageNew->sessionConnectionIdLen] = 0;
		if (!(StorageNew->sessionConnectionUnderlyingConnectionNames = malloc(thedata->sessionConnectionUnderlyingConnectionNamesLen + 1)))
			goto destroy;
		memcpy(StorageNew->sessionConnectionUnderlyingConnectionNames, thedata->sessionConnectionUnderlyingConnectionNames, thedata->sessionConnectionUnderlyingConnectionNamesLen);
		StorageNew->sessionConnectionUnderlyingConnectionNamesLen = thedata->sessionConnectionUnderlyingConnectionNamesLen;
		StorageNew->sessionConnectionUnderlyingConnectionNames[StorageNew->sessionConnectionUnderlyingConnectionNamesLen] = 0;
		if (!(StorageNew->sessionConnectionSupportedConnectionNames = malloc(thedata->sessionConnectionSupportedConnectionNamesLen + 1)))
			goto destroy;
		memcpy(StorageNew->sessionConnectionSupportedConnectionNames, thedata->sessionConnectionSupportedConnectionNames, thedata->sessionConnectionSupportedConnectionNamesLen);
		StorageNew->sessionConnectionSupportedConnectionNamesLen = thedata->sessionConnectionSupportedConnectionNamesLen;
		StorageNew->sessionConnectionSupportedConnectionNames[StorageNew->sessionConnectionSupportedConnectionNamesLen] = 0;
		if (!(StorageNew->sessionConnectionSessProtInUse = malloc(thedata->sessionConnectionSessProtInUseLen + 1)))
			goto destroy;
		memcpy(StorageNew->sessionConnectionSessProtInUse, thedata->sessionConnectionSessProtInUse, thedata->sessionConnectionSessProtInUseLen);
		StorageNew->sessionConnectionSessProtInUseLen = thedata->sessionConnectionSessProtInUseLen;
		StorageNew->sessionConnectionSessProtInUse[StorageNew->sessionConnectionSessProtInUseLen] = 0;
		if (!(StorageNew->sessionConnectionSessFUinUse = malloc(thedata->sessionConnectionSessFUinUseLen + 1)))
			goto destroy;
		memcpy(StorageNew->sessionConnectionSessFUinUse, thedata->sessionConnectionSessFUinUse, thedata->sessionConnectionSessFUinUseLen);
		StorageNew->sessionConnectionSessFUinUseLen = thedata->sessionConnectionSessFUinUseLen;
		StorageNew->sessionConnectionSessFUinUse[StorageNew->sessionConnectionSessFUinUseLen] = 0;
		StorageNew->sessionConnectionMaxTSDUsize = thedata->sessionConnectionMaxTSDUsize;
		StorageNew->sessionCOnnectionRowStatus = thedata->sessionCOnnectionRowStatus;
	}
      done:
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	sessionConnectionTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int sessionConnectionTable_destroy(struct sessionConnectionTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
sessionConnectionTable_destroy(struct sessionConnectionTable_data **thedata)
{
	struct sessionConnectionTable_data *StorageDel;

	DEBUGMSGTL(("acseMIB", "sessionConnectionTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->sessionSubsystemId);
		StorageDel->sessionSubsystemIdLen = 0;
		SNMP_FREE(StorageDel->sessionEntityId);
		StorageDel->sessionEntityIdLen = 0;
		SNMP_FREE(StorageDel->sessionCopmId);
		StorageDel->sessionCopmIdLen = 0;
		SNMP_FREE(StorageDel->sessionConnectionId);
		StorageDel->sessionConnectionIdLen = 0;
		SNMP_FREE(StorageDel->sessionConnectionUnderlyingConnectionNames);
		StorageDel->sessionConnectionUnderlyingConnectionNamesLen = 0;
		SNMP_FREE(StorageDel->sessionConnectionSupportedConnectionNames);
		StorageDel->sessionConnectionSupportedConnectionNamesLen = 0;
		SNMP_FREE(StorageDel->sessionConnectionSessProtInUse);
		StorageDel->sessionConnectionSessProtInUseLen = 0;
		SNMP_FREE(StorageDel->sessionConnectionSessFUinUse);
		StorageDel->sessionConnectionSessFUinUseLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int sessionConnectionTable_add(struct sessionConnectionTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the sessionConnectionTable table data set.
 *
 * Adds a table row structure to the sessionConnectionTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
sessionConnectionTable_add(struct sessionConnectionTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("acseMIB", "sessionConnectionTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* sessionSubsystemId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->sessionSubsystemId, thedata->sessionSubsystemIdLen);
		/* sessionEntityId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->sessionEntityId, thedata->sessionEntityIdLen);
		/* sessionCopmId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->sessionCopmId, thedata->sessionCopmIdLen);
		/* sessionConnectionId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->sessionConnectionId, thedata->sessionConnectionIdLen);
		header_complex_add_data(&sessionConnectionTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("acseMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int sessionConnectionTable_del(struct sessionConnectionTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the sessionConnectionTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
sessionConnectionTable_del(struct sessionConnectionTable_data *thedata)
{
	struct sessionConnectionTable_data *StorageDel;

	DEBUGMSGTL(("acseMIB", "sessionConnectionTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(sessionConnectionTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&sessionConnectionTableStorage, hciptr);
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_sessionConnectionTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for sessionConnectionTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case sessionConnectionTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_sessionConnectionTable(const char *token, char *line)
{
	size_t tmpsize;
	struct sessionConnectionTable_data *StorageTmp = sessionConnectionTable_create();

	DEBUGMSGTL(("acseMIB", "parse_sessionConnectionTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->sessionSubsystemId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->sessionSubsystemId, &StorageTmp->sessionSubsystemIdLen);
	if (StorageTmp->sessionSubsystemId == NULL) {
		config_perror("invalid specification for sessionSubsystemId");
		return;
	}
	SNMP_FREE(StorageTmp->sessionEntityId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->sessionEntityId, &StorageTmp->sessionEntityIdLen);
	if (StorageTmp->sessionEntityId == NULL) {
		config_perror("invalid specification for sessionEntityId");
		return;
	}
	SNMP_FREE(StorageTmp->sessionCopmId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->sessionCopmId, &StorageTmp->sessionCopmIdLen);
	if (StorageTmp->sessionCopmId == NULL) {
		config_perror("invalid specification for sessionCopmId");
		return;
	}
	SNMP_FREE(StorageTmp->sessionConnectionId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->sessionConnectionId, &StorageTmp->sessionConnectionIdLen);
	if (StorageTmp->sessionConnectionId == NULL) {
		config_perror("invalid specification for sessionConnectionId");
		return;
	}
	SNMP_FREE(StorageTmp->sessionConnectionUnderlyingConnectionNames);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->sessionConnectionUnderlyingConnectionNames, &StorageTmp->sessionConnectionUnderlyingConnectionNamesLen);
	if (StorageTmp->sessionConnectionUnderlyingConnectionNames == NULL) {
		config_perror("invalid specification for sessionConnectionUnderlyingConnectionNames");
		return;
	}
	SNMP_FREE(StorageTmp->sessionConnectionSupportedConnectionNames);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->sessionConnectionSupportedConnectionNames, &StorageTmp->sessionConnectionSupportedConnectionNamesLen);
	if (StorageTmp->sessionConnectionSupportedConnectionNames == NULL) {
		config_perror("invalid specification for sessionConnectionSupportedConnectionNames");
		return;
	}
	SNMP_FREE(StorageTmp->sessionConnectionSessProtInUse);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->sessionConnectionSessProtInUse, &StorageTmp->sessionConnectionSessProtInUseLen);
	if (StorageTmp->sessionConnectionSessProtInUse == NULL) {
		config_perror("invalid specification for sessionConnectionSessProtInUse");
		return;
	}
	SNMP_FREE(StorageTmp->sessionConnectionSessFUinUse);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->sessionConnectionSessFUinUse, &StorageTmp->sessionConnectionSessFUinUseLen);
	if (StorageTmp->sessionConnectionSessFUinUse == NULL) {
		config_perror("invalid specification for sessionConnectionSessFUinUse");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->sessionConnectionMaxTSDUsize, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->sessionCOnnectionRowStatus, &tmpsize);
	sessionConnectionTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("acseMIB", "done.\n"));
}

/*
 * store_sessionConnectionTable(): store configuraiton file for sessionConnectionTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_sessionConnectionTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct sessionConnectionTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("acseMIB", "store_sessionConnectionTable: storing data...  "));
	refresh_sessionConnectionTable(1);
	(void) tmpsize;
	for (hcindex = sessionConnectionTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct sessionConnectionTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "sessionConnectionTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->sessionSubsystemId, &StorageTmp->sessionSubsystemIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->sessionEntityId, &StorageTmp->sessionEntityIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->sessionCopmId, &StorageTmp->sessionCopmIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->sessionConnectionId, &StorageTmp->sessionConnectionIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->sessionConnectionUnderlyingConnectionNames, &StorageTmp->sessionConnectionUnderlyingConnectionNamesLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->sessionConnectionSupportedConnectionNames, &StorageTmp->sessionConnectionSupportedConnectionNamesLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->sessionConnectionSessProtInUse, &StorageTmp->sessionConnectionSessProtInUseLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->sessionConnectionSessFUinUse, &StorageTmp->sessionConnectionSessFUinUseLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->sessionConnectionMaxTSDUsize, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->sessionCOnnectionRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct sessionCopmTable_data *sessionCopmTable_create(void)
 * @brief create a fresh data structure representing a new row in the sessionCopmTable table.
 *
 * Creates a new sessionCopmTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct sessionCopmTable_data *
sessionCopmTable_create(void)
{
	struct sessionCopmTable_data *StorageNew = SNMP_MALLOC_STRUCT(sessionCopmTable_data);

	DEBUGMSGTL(("acseMIB", "sessionCopmTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->sessionSubsystemId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->sessionSubsystemIdLen = 0;
		StorageNew->sessionSubsystemId[StorageNew->sessionSubsystemIdLen] = 0;
		if ((StorageNew->sessionEntityId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->sessionEntityIdLen = 0;
		StorageNew->sessionEntityId[StorageNew->sessionEntityIdLen] = 0;
		StorageNew->sessionCopmOperationalState = 0;
		if (memdup((u_char **) &StorageNew->sessionCopmSessProtVerSupport, (u_char *) "\x00", 1) != SNMPERR_SUCCESS)
			goto nomem;
			StorageNew->sessionCopmSessProtVerSupportLen = 1;
		if (memdup((u_char **) &StorageNew->sessionCopmSessFUsupport, (u_char *) "\x00", 1) != SNMPERR_SUCCESS)
			goto nomem;
			StorageNew->sessionCopmSessFUsupportLen = 1;
		if (memdup((u_char **) &StorageNew->sessionCopmOptSupport, (u_char *) "\x00", 1) != SNMPERR_SUCCESS)
			goto nomem;
			StorageNew->sessionCopmOptSupportLen = 1;
		StorageNew->sessionCopmRowStatus = 0;
		StorageNew->sessionCopmRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	sessionCopmTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct sessionCopmTable_data *sessionCopmTable_duplicate(struct sessionCopmTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct sessionCopmTable_data *
sessionCopmTable_duplicate(struct sessionCopmTable_data *thedata)
{
	struct sessionCopmTable_data *StorageNew = SNMP_MALLOC_STRUCT(sessionCopmTable_data);

	DEBUGMSGTL(("acseMIB", "sessionCopmTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		if (!(StorageNew->sessionSubsystemId = malloc(thedata->sessionSubsystemIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->sessionSubsystemId, thedata->sessionSubsystemId, thedata->sessionSubsystemIdLen);
		StorageNew->sessionSubsystemIdLen = thedata->sessionSubsystemIdLen;
		StorageNew->sessionSubsystemId[StorageNew->sessionSubsystemIdLen] = 0;
		if (!(StorageNew->sessionEntityId = malloc(thedata->sessionEntityIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->sessionEntityId, thedata->sessionEntityId, thedata->sessionEntityIdLen);
		StorageNew->sessionEntityIdLen = thedata->sessionEntityIdLen;
		StorageNew->sessionEntityId[StorageNew->sessionEntityIdLen] = 0;
		if (!(StorageNew->sessionCopmId = malloc(thedata->sessionCopmIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->sessionCopmId, thedata->sessionCopmId, thedata->sessionCopmIdLen);
		StorageNew->sessionCopmIdLen = thedata->sessionCopmIdLen;
		StorageNew->sessionCopmId[StorageNew->sessionCopmIdLen] = 0;
		StorageNew->sessionCopmOperationalState = thedata->sessionCopmOperationalState;
		if (!(StorageNew->sessionCopmSessProtVerSupport = malloc(thedata->sessionCopmSessProtVerSupportLen + 1)))
			goto destroy;
		memcpy(StorageNew->sessionCopmSessProtVerSupport, thedata->sessionCopmSessProtVerSupport, thedata->sessionCopmSessProtVerSupportLen);
		StorageNew->sessionCopmSessProtVerSupportLen = thedata->sessionCopmSessProtVerSupportLen;
		StorageNew->sessionCopmSessProtVerSupport[StorageNew->sessionCopmSessProtVerSupportLen] = 0;
		if (!(StorageNew->sessionCopmSessFUsupport = malloc(thedata->sessionCopmSessFUsupportLen + 1)))
			goto destroy;
		memcpy(StorageNew->sessionCopmSessFUsupport, thedata->sessionCopmSessFUsupport, thedata->sessionCopmSessFUsupportLen);
		StorageNew->sessionCopmSessFUsupportLen = thedata->sessionCopmSessFUsupportLen;
		StorageNew->sessionCopmSessFUsupport[StorageNew->sessionCopmSessFUsupportLen] = 0;
		if (!(StorageNew->sessionCopmOptSupport = malloc(thedata->sessionCopmOptSupportLen + 1)))
			goto destroy;
		memcpy(StorageNew->sessionCopmOptSupport, thedata->sessionCopmOptSupport, thedata->sessionCopmOptSupportLen);
		StorageNew->sessionCopmOptSupportLen = thedata->sessionCopmOptSupportLen;
		StorageNew->sessionCopmOptSupport[StorageNew->sessionCopmOptSupportLen] = 0;
		StorageNew->sessionCopmRowStatus = thedata->sessionCopmRowStatus;
	}
      done:
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	sessionCopmTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int sessionCopmTable_destroy(struct sessionCopmTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
sessionCopmTable_destroy(struct sessionCopmTable_data **thedata)
{
	struct sessionCopmTable_data *StorageDel;

	DEBUGMSGTL(("acseMIB", "sessionCopmTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->sessionSubsystemId);
		StorageDel->sessionSubsystemIdLen = 0;
		SNMP_FREE(StorageDel->sessionEntityId);
		StorageDel->sessionEntityIdLen = 0;
		SNMP_FREE(StorageDel->sessionCopmId);
		StorageDel->sessionCopmIdLen = 0;
		SNMP_FREE(StorageDel->sessionCopmSessProtVerSupport);
		StorageDel->sessionCopmSessProtVerSupportLen = 0;
		SNMP_FREE(StorageDel->sessionCopmSessFUsupport);
		StorageDel->sessionCopmSessFUsupportLen = 0;
		SNMP_FREE(StorageDel->sessionCopmOptSupport);
		StorageDel->sessionCopmOptSupportLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int sessionCopmTable_add(struct sessionCopmTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the sessionCopmTable table data set.
 *
 * Adds a table row structure to the sessionCopmTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
sessionCopmTable_add(struct sessionCopmTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("acseMIB", "sessionCopmTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* sessionSubsystemId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->sessionSubsystemId, thedata->sessionSubsystemIdLen);
		/* sessionEntityId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->sessionEntityId, thedata->sessionEntityIdLen);
		/* sessionCopmId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->sessionCopmId, thedata->sessionCopmIdLen);
		header_complex_add_data(&sessionCopmTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("acseMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int sessionCopmTable_del(struct sessionCopmTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the sessionCopmTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
sessionCopmTable_del(struct sessionCopmTable_data *thedata)
{
	struct sessionCopmTable_data *StorageDel;

	DEBUGMSGTL(("acseMIB", "sessionCopmTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(sessionCopmTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&sessionCopmTableStorage, hciptr);
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_sessionCopmTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for sessionCopmTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case sessionCopmTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_sessionCopmTable(const char *token, char *line)
{
	size_t tmpsize;
	struct sessionCopmTable_data *StorageTmp = sessionCopmTable_create();

	DEBUGMSGTL(("acseMIB", "parse_sessionCopmTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->sessionSubsystemId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->sessionSubsystemId, &StorageTmp->sessionSubsystemIdLen);
	if (StorageTmp->sessionSubsystemId == NULL) {
		config_perror("invalid specification for sessionSubsystemId");
		return;
	}
	SNMP_FREE(StorageTmp->sessionEntityId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->sessionEntityId, &StorageTmp->sessionEntityIdLen);
	if (StorageTmp->sessionEntityId == NULL) {
		config_perror("invalid specification for sessionEntityId");
		return;
	}
	SNMP_FREE(StorageTmp->sessionCopmId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->sessionCopmId, &StorageTmp->sessionCopmIdLen);
	if (StorageTmp->sessionCopmId == NULL) {
		config_perror("invalid specification for sessionCopmId");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->sessionCopmOperationalState, &tmpsize);
	SNMP_FREE(StorageTmp->sessionCopmSessProtVerSupport);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->sessionCopmSessProtVerSupport, &StorageTmp->sessionCopmSessProtVerSupportLen);
	if (StorageTmp->sessionCopmSessProtVerSupport == NULL) {
		config_perror("invalid specification for sessionCopmSessProtVerSupport");
		return;
	}
	SNMP_FREE(StorageTmp->sessionCopmSessFUsupport);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->sessionCopmSessFUsupport, &StorageTmp->sessionCopmSessFUsupportLen);
	if (StorageTmp->sessionCopmSessFUsupport == NULL) {
		config_perror("invalid specification for sessionCopmSessFUsupport");
		return;
	}
	SNMP_FREE(StorageTmp->sessionCopmOptSupport);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->sessionCopmOptSupport, &StorageTmp->sessionCopmOptSupportLen);
	if (StorageTmp->sessionCopmOptSupport == NULL) {
		config_perror("invalid specification for sessionCopmOptSupport");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->sessionCopmRowStatus, &tmpsize);
	sessionCopmTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("acseMIB", "done.\n"));
}

/*
 * store_sessionCopmTable(): store configuraiton file for sessionCopmTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_sessionCopmTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct sessionCopmTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("acseMIB", "store_sessionCopmTable: storing data...  "));
	refresh_sessionCopmTable(1);
	(void) tmpsize;
	for (hcindex = sessionCopmTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct sessionCopmTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "sessionCopmTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->sessionSubsystemId, &StorageTmp->sessionSubsystemIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->sessionEntityId, &StorageTmp->sessionEntityIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->sessionCopmId, &StorageTmp->sessionCopmIdLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->sessionCopmOperationalState, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->sessionCopmSessProtVerSupport, &StorageTmp->sessionCopmSessProtVerSupportLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->sessionCopmSessFUsupport, &StorageTmp->sessionCopmSessFUsupportLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->sessionCopmOptSupport, &StorageTmp->sessionCopmOptSupportLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->sessionCopmRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct sessionEntityTable_data *sessionEntityTable_create(void)
 * @brief create a fresh data structure representing a new row in the sessionEntityTable table.
 *
 * Creates a new sessionEntityTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct sessionEntityTable_data *
sessionEntityTable_create(void)
{
	struct sessionEntityTable_data *StorageNew = SNMP_MALLOC_STRUCT(sessionEntityTable_data);

	DEBUGMSGTL(("acseMIB", "sessionEntityTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->sessionSubsystemId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->sessionSubsystemIdLen = 0;
		StorageNew->sessionSubsystemId[StorageNew->sessionSubsystemIdLen] = 0;
		if ((StorageNew->sessionEntityLocalSapNames = malloc(1)) == NULL)
			goto nomem;
		StorageNew->sessionEntityLocalSapNamesLen = 0;
		StorageNew->sessionEntityLocalSapNames[StorageNew->sessionEntityLocalSapNamesLen] = 0;
		StorageNew->sessionEntityOperationalState = 0;
		StorageNew->sessionEntityRowStatus = 0;
		StorageNew->sessionEntityRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	sessionEntityTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct sessionEntityTable_data *sessionEntityTable_duplicate(struct sessionEntityTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct sessionEntityTable_data *
sessionEntityTable_duplicate(struct sessionEntityTable_data *thedata)
{
	struct sessionEntityTable_data *StorageNew = SNMP_MALLOC_STRUCT(sessionEntityTable_data);

	DEBUGMSGTL(("acseMIB", "sessionEntityTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		if (!(StorageNew->sessionSubsystemId = malloc(thedata->sessionSubsystemIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->sessionSubsystemId, thedata->sessionSubsystemId, thedata->sessionSubsystemIdLen);
		StorageNew->sessionSubsystemIdLen = thedata->sessionSubsystemIdLen;
		StorageNew->sessionSubsystemId[StorageNew->sessionSubsystemIdLen] = 0;
		if (!(StorageNew->sessionEntityId = malloc(thedata->sessionEntityIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->sessionEntityId, thedata->sessionEntityId, thedata->sessionEntityIdLen);
		StorageNew->sessionEntityIdLen = thedata->sessionEntityIdLen;
		StorageNew->sessionEntityId[StorageNew->sessionEntityIdLen] = 0;
		if (!(StorageNew->sessionEntityLocalSapNames = malloc(thedata->sessionEntityLocalSapNamesLen + 1)))
			goto destroy;
		memcpy(StorageNew->sessionEntityLocalSapNames, thedata->sessionEntityLocalSapNames, thedata->sessionEntityLocalSapNamesLen);
		StorageNew->sessionEntityLocalSapNamesLen = thedata->sessionEntityLocalSapNamesLen;
		StorageNew->sessionEntityLocalSapNames[StorageNew->sessionEntityLocalSapNamesLen] = 0;
		StorageNew->sessionEntityOperationalState = thedata->sessionEntityOperationalState;
		StorageNew->sessionEntityRowStatus = thedata->sessionEntityRowStatus;
	}
      done:
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	sessionEntityTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int sessionEntityTable_destroy(struct sessionEntityTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
sessionEntityTable_destroy(struct sessionEntityTable_data **thedata)
{
	struct sessionEntityTable_data *StorageDel;

	DEBUGMSGTL(("acseMIB", "sessionEntityTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->sessionSubsystemId);
		StorageDel->sessionSubsystemIdLen = 0;
		SNMP_FREE(StorageDel->sessionEntityId);
		StorageDel->sessionEntityIdLen = 0;
		SNMP_FREE(StorageDel->sessionEntityLocalSapNames);
		StorageDel->sessionEntityLocalSapNamesLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int sessionEntityTable_add(struct sessionEntityTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the sessionEntityTable table data set.
 *
 * Adds a table row structure to the sessionEntityTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
sessionEntityTable_add(struct sessionEntityTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("acseMIB", "sessionEntityTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* sessionSubsystemId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->sessionSubsystemId, thedata->sessionSubsystemIdLen);
		/* sessionEntityId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->sessionEntityId, thedata->sessionEntityIdLen);
		header_complex_add_data(&sessionEntityTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("acseMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int sessionEntityTable_del(struct sessionEntityTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the sessionEntityTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
sessionEntityTable_del(struct sessionEntityTable_data *thedata)
{
	struct sessionEntityTable_data *StorageDel;

	DEBUGMSGTL(("acseMIB", "sessionEntityTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(sessionEntityTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&sessionEntityTableStorage, hciptr);
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_sessionEntityTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for sessionEntityTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case sessionEntityTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_sessionEntityTable(const char *token, char *line)
{
	size_t tmpsize;
	struct sessionEntityTable_data *StorageTmp = sessionEntityTable_create();

	DEBUGMSGTL(("acseMIB", "parse_sessionEntityTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->sessionSubsystemId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->sessionSubsystemId, &StorageTmp->sessionSubsystemIdLen);
	if (StorageTmp->sessionSubsystemId == NULL) {
		config_perror("invalid specification for sessionSubsystemId");
		return;
	}
	SNMP_FREE(StorageTmp->sessionEntityId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->sessionEntityId, &StorageTmp->sessionEntityIdLen);
	if (StorageTmp->sessionEntityId == NULL) {
		config_perror("invalid specification for sessionEntityId");
		return;
	}
	SNMP_FREE(StorageTmp->sessionEntityLocalSapNames);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->sessionEntityLocalSapNames, &StorageTmp->sessionEntityLocalSapNamesLen);
	if (StorageTmp->sessionEntityLocalSapNames == NULL) {
		config_perror("invalid specification for sessionEntityLocalSapNames");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->sessionEntityOperationalState, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->sessionEntityRowStatus, &tmpsize);
	sessionEntityTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("acseMIB", "done.\n"));
}

/*
 * store_sessionEntityTable(): store configuraiton file for sessionEntityTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_sessionEntityTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct sessionEntityTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("acseMIB", "store_sessionEntityTable: storing data...  "));
	refresh_sessionEntityTable(1);
	(void) tmpsize;
	for (hcindex = sessionEntityTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct sessionEntityTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "sessionEntityTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->sessionSubsystemId, &StorageTmp->sessionSubsystemIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->sessionEntityId, &StorageTmp->sessionEntityIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->sessionEntityLocalSapNames, &StorageTmp->sessionEntityLocalSapNamesLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->sessionEntityOperationalState, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->sessionEntityRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct sessionSubsystemTable_data *sessionSubsystemTable_create(void)
 * @brief create a fresh data structure representing a new row in the sessionSubsystemTable table.
 *
 * Creates a new sessionSubsystemTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct sessionSubsystemTable_data *
sessionSubsystemTable_create(void)
{
	struct sessionSubsystemTable_data *StorageNew = SNMP_MALLOC_STRUCT(sessionSubsystemTable_data);

	DEBUGMSGTL(("acseMIB", "sessionSubsystemTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->sessionSubsystemRowStatus = 0;
		StorageNew->sessionSubsystemRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	sessionSubsystemTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct sessionSubsystemTable_data *sessionSubsystemTable_duplicate(struct sessionSubsystemTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct sessionSubsystemTable_data *
sessionSubsystemTable_duplicate(struct sessionSubsystemTable_data *thedata)
{
	struct sessionSubsystemTable_data *StorageNew = SNMP_MALLOC_STRUCT(sessionSubsystemTable_data);

	DEBUGMSGTL(("acseMIB", "sessionSubsystemTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		if (!(StorageNew->sessionSubsystemId = malloc(thedata->sessionSubsystemIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->sessionSubsystemId, thedata->sessionSubsystemId, thedata->sessionSubsystemIdLen);
		StorageNew->sessionSubsystemIdLen = thedata->sessionSubsystemIdLen;
		StorageNew->sessionSubsystemId[StorageNew->sessionSubsystemIdLen] = 0;
		StorageNew->sessionSubsystemRowStatus = thedata->sessionSubsystemRowStatus;
	}
      done:
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	sessionSubsystemTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int sessionSubsystemTable_destroy(struct sessionSubsystemTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
sessionSubsystemTable_destroy(struct sessionSubsystemTable_data **thedata)
{
	struct sessionSubsystemTable_data *StorageDel;

	DEBUGMSGTL(("acseMIB", "sessionSubsystemTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->sessionSubsystemId);
		StorageDel->sessionSubsystemIdLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int sessionSubsystemTable_add(struct sessionSubsystemTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the sessionSubsystemTable table data set.
 *
 * Adds a table row structure to the sessionSubsystemTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
sessionSubsystemTable_add(struct sessionSubsystemTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("acseMIB", "sessionSubsystemTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* sessionSubsystemId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->sessionSubsystemId, thedata->sessionSubsystemIdLen);
		header_complex_add_data(&sessionSubsystemTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("acseMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int sessionSubsystemTable_del(struct sessionSubsystemTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the sessionSubsystemTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
sessionSubsystemTable_del(struct sessionSubsystemTable_data *thedata)
{
	struct sessionSubsystemTable_data *StorageDel;

	DEBUGMSGTL(("acseMIB", "sessionSubsystemTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(sessionSubsystemTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&sessionSubsystemTableStorage, hciptr);
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_sessionSubsystemTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for sessionSubsystemTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case sessionSubsystemTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_sessionSubsystemTable(const char *token, char *line)
{
	size_t tmpsize;
	struct sessionSubsystemTable_data *StorageTmp = sessionSubsystemTable_create();

	DEBUGMSGTL(("acseMIB", "parse_sessionSubsystemTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	SNMP_FREE(StorageTmp->sessionSubsystemId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->sessionSubsystemId, &StorageTmp->sessionSubsystemIdLen);
	if (StorageTmp->sessionSubsystemId == NULL) {
		config_perror("invalid specification for sessionSubsystemId");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->sessionSubsystemRowStatus, &tmpsize);
	sessionSubsystemTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("acseMIB", "done.\n"));
}

/*
 * store_sessionSubsystemTable(): store configuraiton file for sessionSubsystemTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_sessionSubsystemTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct sessionSubsystemTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("acseMIB", "store_sessionSubsystemTable: storing data...  "));
	refresh_sessionSubsystemTable(1);
	(void) tmpsize;
	for (hcindex = sessionSubsystemTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct sessionSubsystemTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "sessionSubsystemTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->sessionSubsystemId, &StorageTmp->sessionSubsystemIdLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->sessionSubsystemRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct acseEntityTable_data *acseEntityTable_create(void)
 * @brief create a fresh data structure representing a new row in the acseEntityTable table.
 *
 * Creates a new acseEntityTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct acseEntityTable_data *
acseEntityTable_create(void)
{
	struct acseEntityTable_data *StorageNew = SNMP_MALLOC_STRUCT(acseEntityTable_data);

	DEBUGMSGTL(("acseMIB", "acseEntityTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->mtpMsId = 0;
		StorageNew->acseEntityRowStatus = 0;
		StorageNew->acseEntityRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	acseEntityTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct acseEntityTable_data *acseEntityTable_duplicate(struct acseEntityTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct acseEntityTable_data *
acseEntityTable_duplicate(struct acseEntityTable_data *thedata)
{
	struct acseEntityTable_data *StorageNew = SNMP_MALLOC_STRUCT(acseEntityTable_data);

	DEBUGMSGTL(("acseMIB", "acseEntityTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->mtpMsId = thedata->mtpMsId;
		StorageNew->acseEntityId = thedata->acseEntityId;
		StorageNew->acseEntityRowStatus = thedata->acseEntityRowStatus;
	}
      done:
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	acseEntityTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int acseEntityTable_destroy(struct acseEntityTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
acseEntityTable_destroy(struct acseEntityTable_data **thedata)
{
	struct acseEntityTable_data *StorageDel;

	DEBUGMSGTL(("acseMIB", "acseEntityTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int acseEntityTable_add(struct acseEntityTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the acseEntityTable table data set.
 *
 * Adds a table row structure to the acseEntityTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
acseEntityTable_add(struct acseEntityTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("acseMIB", "acseEntityTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* mtpMsId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->mtpMsId, sizeof(thedata->mtpMsId));
		/* acseEntityId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->acseEntityId, sizeof(thedata->acseEntityId));
		header_complex_add_data(&acseEntityTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("acseMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int acseEntityTable_del(struct acseEntityTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the acseEntityTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
acseEntityTable_del(struct acseEntityTable_data *thedata)
{
	struct acseEntityTable_data *StorageDel;

	DEBUGMSGTL(("acseMIB", "acseEntityTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(acseEntityTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&acseEntityTableStorage, hciptr);
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_acseEntityTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for acseEntityTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case acseEntityTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_acseEntityTable(const char *token, char *line)
{
	size_t tmpsize;
	struct acseEntityTable_data *StorageTmp = acseEntityTable_create();

	DEBUGMSGTL(("acseMIB", "parse_acseEntityTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->mtpMsId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->acseEntityId, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->acseEntityRowStatus, &tmpsize);
	acseEntityTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("acseMIB", "done.\n"));
}

/*
 * store_acseEntityTable(): store configuraiton file for acseEntityTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_acseEntityTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct acseEntityTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("acseMIB", "store_acseEntityTable: storing data...  "));
	refresh_acseEntityTable(1);
	(void) tmpsize;
	for (hcindex = acseEntityTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct acseEntityTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "acseEntityTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->mtpMsId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->acseEntityId, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->acseEntityRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct acseAccessPointTable_data *acseAccessPointTable_create(void)
 * @brief create a fresh data structure representing a new row in the acseAccessPointTable table.
 *
 * Creates a new acseAccessPointTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct acseAccessPointTable_data *
acseAccessPointTable_create(void)
{
	struct acseAccessPointTable_data *StorageNew = SNMP_MALLOC_STRUCT(acseAccessPointTable_data);

	DEBUGMSGTL(("acseMIB", "acseAccessPointTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->mtpMsId = 0;
		StorageNew->acseEntityId = 0;
		StorageNew->acseAccessPointRowStatus = 0;
		StorageNew->acseAccessPointRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	acseAccessPointTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct acseAccessPointTable_data *acseAccessPointTable_duplicate(struct acseAccessPointTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct acseAccessPointTable_data *
acseAccessPointTable_duplicate(struct acseAccessPointTable_data *thedata)
{
	struct acseAccessPointTable_data *StorageNew = SNMP_MALLOC_STRUCT(acseAccessPointTable_data);

	DEBUGMSGTL(("acseMIB", "acseAccessPointTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->mtpMsId = thedata->mtpMsId;
		StorageNew->acseEntityId = thedata->acseEntityId;
		StorageNew->acseAccessPointId = thedata->acseAccessPointId;
		StorageNew->acseAccessPointRowStatus = thedata->acseAccessPointRowStatus;
	}
      done:
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	acseAccessPointTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int acseAccessPointTable_destroy(struct acseAccessPointTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
acseAccessPointTable_destroy(struct acseAccessPointTable_data **thedata)
{
	struct acseAccessPointTable_data *StorageDel;

	DEBUGMSGTL(("acseMIB", "acseAccessPointTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int acseAccessPointTable_add(struct acseAccessPointTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the acseAccessPointTable table data set.
 *
 * Adds a table row structure to the acseAccessPointTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
acseAccessPointTable_add(struct acseAccessPointTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("acseMIB", "acseAccessPointTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* mtpMsId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->mtpMsId, sizeof(thedata->mtpMsId));
		/* acseEntityId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->acseEntityId, sizeof(thedata->acseEntityId));
		/* acseAccessPointId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->acseAccessPointId, sizeof(thedata->acseAccessPointId));
		header_complex_add_data(&acseAccessPointTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("acseMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int acseAccessPointTable_del(struct acseAccessPointTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the acseAccessPointTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
acseAccessPointTable_del(struct acseAccessPointTable_data *thedata)
{
	struct acseAccessPointTable_data *StorageDel;

	DEBUGMSGTL(("acseMIB", "acseAccessPointTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(acseAccessPointTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&acseAccessPointTableStorage, hciptr);
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_acseAccessPointTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for acseAccessPointTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case acseAccessPointTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_acseAccessPointTable(const char *token, char *line)
{
	size_t tmpsize;
	struct acseAccessPointTable_data *StorageTmp = acseAccessPointTable_create();

	DEBUGMSGTL(("acseMIB", "parse_acseAccessPointTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->mtpMsId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->acseEntityId, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->acseAccessPointId, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->acseAccessPointRowStatus, &tmpsize);
	acseAccessPointTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("acseMIB", "done.\n"));
}

/*
 * store_acseAccessPointTable(): store configuraiton file for acseAccessPointTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_acseAccessPointTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct acseAccessPointTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("acseMIB", "store_acseAccessPointTable: storing data...  "));
	refresh_acseAccessPointTable(1);
	(void) tmpsize;
	for (hcindex = acseAccessPointTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct acseAccessPointTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "acseAccessPointTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->mtpMsId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->acseEntityId, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->acseAccessPointId, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->acseAccessPointRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct acseLinkageTable_data *acseLinkageTable_create(void)
 * @brief create a fresh data structure representing a new row in the acseLinkageTable table.
 *
 * Creates a new acseLinkageTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct acseLinkageTable_data *
acseLinkageTable_create(void)
{
	struct acseLinkageTable_data *StorageNew = SNMP_MALLOC_STRUCT(acseLinkageTable_data);

	DEBUGMSGTL(("acseMIB", "acseLinkageTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->mtpMsId = 0;
		if ((StorageNew->sccpNetworkEntityId = malloc(1)) == NULL)
			goto nomem;
		StorageNew->sccpNetworkEntityIdLen = 0;
		StorageNew->sccpNetworkEntityId[StorageNew->sccpNetworkEntityIdLen] = 0;
		StorageNew->acseLinkageRowStatus = 0;
		StorageNew->acseLinkageRowStatus = RS_NOTREADY;
	}
      done:
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return (StorageNew);
	goto nomem;
      nomem:
	acseLinkageTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn struct acseLinkageTable_data *acseLinkageTable_duplicate(struct acseLinkageTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicate a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct acseLinkageTable_data *
acseLinkageTable_duplicate(struct acseLinkageTable_data *thedata)
{
	struct acseLinkageTable_data *StorageNew = SNMP_MALLOC_STRUCT(acseLinkageTable_data);

	DEBUGMSGTL(("acseMIB", "acseLinkageTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
		StorageNew->mtpMsId = thedata->mtpMsId;
		if (!(StorageNew->sccpNetworkEntityId = malloc(thedata->sccpNetworkEntityIdLen + 1)))
			goto destroy;
		memcpy(StorageNew->sccpNetworkEntityId, thedata->sccpNetworkEntityId, thedata->sccpNetworkEntityIdLen);
		StorageNew->sccpNetworkEntityIdLen = thedata->sccpNetworkEntityIdLen;
		StorageNew->sccpNetworkEntityId[StorageNew->sccpNetworkEntityIdLen] = 0;
		StorageNew->acseLinkageRowStatus = thedata->acseLinkageRowStatus;
	}
      done:
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	acseLinkageTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int acseLinkageTable_destroy(struct acseLinkageTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
acseLinkageTable_destroy(struct acseLinkageTable_data **thedata)
{
	struct acseLinkageTable_data *StorageDel;

	DEBUGMSGTL(("acseMIB", "acseLinkageTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->sccpNetworkEntityId);
		StorageDel->sccpNetworkEntityIdLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int acseLinkageTable_add(struct acseLinkageTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the acseLinkageTable table data set.
 *
 * Adds a table row structure to the acseLinkageTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
acseLinkageTable_add(struct acseLinkageTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("acseMIB", "acseLinkageTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* mtpMsId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->mtpMsId, sizeof(thedata->mtpMsId));
		/* sccpNetworkEntityId */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->sccpNetworkEntityId, thedata->sccpNetworkEntityIdLen);
		header_complex_add_data(&acseLinkageTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("acseMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int acseLinkageTable_del(struct acseLinkageTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the acseLinkageTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
acseLinkageTable_del(struct acseLinkageTable_data *thedata)
{
	struct acseLinkageTable_data *StorageDel;

	DEBUGMSGTL(("acseMIB", "acseLinkageTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(acseLinkageTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&acseLinkageTableStorage, hciptr);
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_acseLinkageTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for acseLinkageTable entries.
 *
 * This callback is called by UCD-SNMP when it parses a configuration file and finds a configuration
 * file line for the registsred token (in this case acseLinkageTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_acseLinkageTable(const char *token, char *line)
{
	size_t tmpsize;
	struct acseLinkageTable_data *StorageTmp = acseLinkageTable_create();

	DEBUGMSGTL(("acseMIB", "parse_acseLinkageTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->mtpMsId, &tmpsize);
	SNMP_FREE(StorageTmp->sccpNetworkEntityId);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->sccpNetworkEntityId, &StorageTmp->sccpNetworkEntityIdLen);
	if (StorageTmp->sccpNetworkEntityId == NULL) {
		config_perror("invalid specification for sccpNetworkEntityId");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->acseLinkageRowStatus, &tmpsize);
	acseLinkageTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("acseMIB", "done.\n"));
}

/*
 * store_acseLinkageTable(): store configuraiton file for acseLinkageTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_acseLinkageTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct acseLinkageTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("acseMIB", "store_acseLinkageTable: storing data...  "));
	refresh_acseLinkageTable(1);
	(void) tmpsize;
	for (hcindex = acseLinkageTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct acseLinkageTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "acseLinkageTable ");
			cptr = line + strlen(line);
			(void) cptr;
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->mtpMsId, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->sccpNetworkEntityId, &StorageTmp->sccpNetworkEntityIdLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->acseLinkageRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int activate_acseAssociationTable_row(struct acseAssociationTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_acseAssociationTable_row(struct acseAssociationTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_acseAssociationTable_row(struct acseAssociationTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_acseAssociationTable_row(struct acseAssociationTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_apInvocationTable_row(struct apInvocationTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_apInvocationTable_row(struct apInvocationTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_apInvocationTable_row(struct apInvocationTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_apInvocationTable_row(struct apInvocationTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_applicationEntityInvocationTable_row(struct applicationEntityInvocationTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_applicationEntityInvocationTable_row(struct applicationEntityInvocationTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_applicationEntityInvocationTable_row(struct applicationEntityInvocationTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_applicationEntityInvocationTable_row(struct applicationEntityInvocationTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_asoTable_row(struct asoTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_asoTable_row(struct asoTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_asoTable_row(struct asoTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_asoTable_row(struct asoTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_asoEntityTable_row(struct asoEntityTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_asoEntityTable_row(struct asoEntityTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_asoEntityTable_row(struct asoEntityTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_asoEntityTable_row(struct asoEntityTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_asoInvocationTable_row(struct asoInvocationTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_asoInvocationTable_row(struct asoInvocationTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_asoInvocationTable_row(struct asoInvocationTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_asoInvocationTable_row(struct asoInvocationTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_monoULConnectionTable_row(struct monoULConnectionTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_monoULConnectionTable_row(struct monoULConnectionTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_monoULConnectionTable_row(struct monoULConnectionTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_monoULConnectionTable_row(struct monoULConnectionTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_monolithicUpperLayerSubsystemTable_row(struct monolithicUpperLayerSubsystemTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_monolithicUpperLayerSubsystemTable_row(struct monolithicUpperLayerSubsystemTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_monolithicUpperLayerSubsystemTable_row(struct monolithicUpperLayerSubsystemTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_monolithicUpperLayerSubsystemTable_row(struct monolithicUpperLayerSubsystemTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_pSapTable_row(struct pSapTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_pSapTable_row(struct pSapTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_pSapTable_row(struct pSapTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_pSapTable_row(struct pSapTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_presentationConnectionTable_row(struct presentationConnectionTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_presentationConnectionTable_row(struct presentationConnectionTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_presentationConnectionTable_row(struct presentationConnectionTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_presentationConnectionTable_row(struct presentationConnectionTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_presentationCopmTable_row(struct presentationCopmTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_presentationCopmTable_row(struct presentationCopmTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_presentationCopmTable_row(struct presentationCopmTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_presentationCopmTable_row(struct presentationCopmTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_presentationEntityTable_row(struct presentationEntityTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_presentationEntityTable_row(struct presentationEntityTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_presentationEntityTable_row(struct presentationEntityTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_presentationEntityTable_row(struct presentationEntityTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_presentationSubsystemTable_row(struct presentationSubsystemTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_presentationSubsystemTable_row(struct presentationSubsystemTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_presentationSubsystemTable_row(struct presentationSubsystemTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_presentationSubsystemTable_row(struct presentationSubsystemTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_sSapTable_row(struct sSapTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_sSapTable_row(struct sSapTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_sSapTable_row(struct sSapTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_sSapTable_row(struct sSapTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_sessionConnectionTable_row(struct sessionConnectionTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_sessionConnectionTable_row(struct sessionConnectionTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_sessionConnectionTable_row(struct sessionConnectionTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_sessionConnectionTable_row(struct sessionConnectionTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_sessionCopmTable_row(struct sessionCopmTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_sessionCopmTable_row(struct sessionCopmTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_sessionCopmTable_row(struct sessionCopmTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_sessionCopmTable_row(struct sessionCopmTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_sessionEntityTable_row(struct sessionEntityTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_sessionEntityTable_row(struct sessionEntityTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_sessionEntityTable_row(struct sessionEntityTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_sessionEntityTable_row(struct sessionEntityTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_sessionSubsystemTable_row(struct sessionSubsystemTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_sessionSubsystemTable_row(struct sessionSubsystemTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_sessionSubsystemTable_row(struct sessionSubsystemTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_sessionSubsystemTable_row(struct sessionSubsystemTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_acseEntityTable_row(struct acseEntityTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_acseEntityTable_row(struct acseEntityTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_acseEntityTable_row(struct acseEntityTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_acseEntityTable_row(struct acseEntityTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_acseAccessPointTable_row(struct acseAccessPointTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_acseAccessPointTable_row(struct acseAccessPointTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_acseAccessPointTable_row(struct acseAccessPointTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_acseAccessPointTable_row(struct acseAccessPointTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int activate_acseLinkageTable_row(struct acseLinkageTable_data *StorageTmp)
 * @param StorageTmp the data row to activate
 * @brief commit activation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_NOTINSERVICE state to the RS_ACTIVE state.  It is also used when transitioning from the
 * RS_CREATEANDGO state to the RS_ACTIVE state.  If activation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
activate_acseLinkageTable_row(struct acseLinkageTable_data *StorageTmp)
{
	/* XXX: provide code to activate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int deactivate_acseLinkageTable_row(struct acseLinkageTable_data *StorageTmp)
 * @param StorageTmp the data row to deactivate
 * @brief commit deactivation of a row to the underlying device
 *
 * This function is used by tables that contain a RowStatus object.  It is used to move the row from
 * the RS_ACTIVE state to the RS_NOTINSERVICE state.  It is also used when transitioning from the
 * RS_ACTIVE state to the RS_DESTROY state.  If deactivation fails, the function should return
 * SNMP_ERR_COMMITFAILED; otherwise, SNMP_ERR_NOERROR.
 */
int
deactivate_acseLinkageTable_row(struct acseLinkageTable_data *StorageTmp)
{
	/* XXX: provide code to deactivate the row with the underlying device */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int check_acseTable_row(struct acseTable_data *StorageTmp, struct acseTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_acseTable_row(struct acseTable_data *StorageTmp, struct acseTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_acseTable_row(struct acseTable_data *StorageTmp, struct acseTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_acseTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_acseTable_row(struct acseTable_data *StorageTmp, struct acseTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	acseTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_acseTable_row(struct acseTable_data *StorageTmp, struct acseTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_acseTable_row(struct acseTable_data *StorageTmp, struct acseTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_acseTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_acseTable_row(struct acseTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the acseTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct acseTable_data *
refresh_acseTable_row(struct acseTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->acseTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->acseTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_acseTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the acseTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_acseTable(int force)
{
	if (!force && acseTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	acseTable_refresh = 0;
}

/**
 * @fn u_char *var_acseTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in acseTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_acseMIB above.
 */
u_char *
var_acseTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct acseTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("acseMIB", "var_acseTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_acseTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(acseTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_acseTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) ACSEFUINITIATOR:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->acseFUinitiatorLen;
		rval = (u_char *) StorageTmp->acseFUinitiator;
		break;
	case (u_char) ACSEEDITIONSSUPPORTED:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->acseEditionsSupportedLen;
		rval = (u_char *) StorageTmp->acseEditionsSupported;
		break;
	case (u_char) ACSEFURESPONDER:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->acseFUresponderLen;
		rval = (u_char *) StorageTmp->acseFUresponder;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_acseAssociationTable_row(struct acseAssociationTable_data *StorageTmp, struct acseAssociationTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_acseAssociationTable_row(struct acseAssociationTable_data *StorageTmp, struct acseAssociationTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_acseAssociationTable_row(struct acseAssociationTable_data *StorageTmp, struct acseAssociationTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_acseAssociationTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_acseAssociationTable_row(struct acseAssociationTable_data *StorageTmp, struct acseAssociationTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	acseAssociationTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_acseAssociationTable_row(struct acseAssociationTable_data *StorageTmp, struct acseAssociationTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_acseAssociationTable_row(struct acseAssociationTable_data *StorageTmp, struct acseAssociationTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_acseAssociationTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_acseAssociationTable_row(struct acseAssociationTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the acseAssociationTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct acseAssociationTable_data *
refresh_acseAssociationTable_row(struct acseAssociationTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->acseAssociationTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->acseAssociationTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_acseAssociationTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the acseAssociationTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_acseAssociationTable(int force)
{
	if (!force && acseAssociationTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	acseAssociationTable_refresh = 0;
}

/**
 * @fn u_char *var_acseAssociationTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in acseAssociationTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_acseMIB above.
 */
u_char *
var_acseAssociationTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct acseAssociationTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("acseMIB", "var_acseAssociationTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_acseAssociationTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(acseAssociationTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_acseAssociationTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) ACSEASSOCIATIONFUINUSE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->acseAssociationFUinUseLen;
		rval = (u_char *) StorageTmp->acseAssociationFUinUse;
		break;
	case (u_char) ACSEASSOCIATIONAPPLCONTEXTINUSE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->acseAssociationApplContextInUseLen * sizeof(oid);
		rval = (u_char *) StorageTmp->acseAssociationApplContextInUse;
		break;
	case (u_char) ACSEASSOCIATIONPARENTASOINVOC:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->acseAssociationParentAsoinvocLen * sizeof(oid);
		rval = (u_char *) StorageTmp->acseAssociationParentAsoinvoc;
		break;
	case (u_char) ACSEASSOCIATIONCALLEDAETITLE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->acseAssociationCalledAEtitleLen;
		rval = (u_char *) StorageTmp->acseAssociationCalledAEtitle;
		break;
	case (u_char) ACSEASSOCIATIONROWSTATUS:	/* Create */
		*write_method = write_acseAssociationRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->acseAssociationRowStatus);
		rval = (u_char *) &StorageTmp->acseAssociationRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_apInvocationTable_row(struct apInvocationTable_data *StorageTmp, struct apInvocationTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_apInvocationTable_row(struct apInvocationTable_data *StorageTmp, struct apInvocationTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_apInvocationTable_row(struct apInvocationTable_data *StorageTmp, struct apInvocationTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_apInvocationTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_apInvocationTable_row(struct apInvocationTable_data *StorageTmp, struct apInvocationTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	apInvocationTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_apInvocationTable_row(struct apInvocationTable_data *StorageTmp, struct apInvocationTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_apInvocationTable_row(struct apInvocationTable_data *StorageTmp, struct apInvocationTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_apInvocationTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_apInvocationTable_row(struct apInvocationTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the apInvocationTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct apInvocationTable_data *
refresh_apInvocationTable_row(struct apInvocationTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->apInvocationTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->apInvocationTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_apInvocationTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the apInvocationTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_apInvocationTable(int force)
{
	if (!force && apInvocationTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	apInvocationTable_refresh = 0;
}

/**
 * @fn u_char *var_apInvocationTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in apInvocationTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_acseMIB above.
 */
u_char *
var_apInvocationTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct apInvocationTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("acseMIB", "var_apInvocationTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_apInvocationTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(apInvocationTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_apInvocationTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) APINVOCATIONROWSTATUS:	/* Create */
		*write_method = write_apInvocationRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->apInvocationRowStatus);
		rval = (u_char *) &StorageTmp->apInvocationRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_applicationEntityInvocationTable_row(struct applicationEntityInvocationTable_data *StorageTmp, struct applicationEntityInvocationTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_applicationEntityInvocationTable_row(struct applicationEntityInvocationTable_data *StorageTmp, struct applicationEntityInvocationTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_applicationEntityInvocationTable_row(struct applicationEntityInvocationTable_data *StorageTmp, struct applicationEntityInvocationTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_applicationEntityInvocationTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_applicationEntityInvocationTable_row(struct applicationEntityInvocationTable_data *StorageTmp, struct applicationEntityInvocationTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	applicationEntityInvocationTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_applicationEntityInvocationTable_row(struct applicationEntityInvocationTable_data *StorageTmp, struct applicationEntityInvocationTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_applicationEntityInvocationTable_row(struct applicationEntityInvocationTable_data *StorageTmp, struct applicationEntityInvocationTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_applicationEntityInvocationTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_applicationEntityInvocationTable_row(struct applicationEntityInvocationTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the applicationEntityInvocationTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct applicationEntityInvocationTable_data *
refresh_applicationEntityInvocationTable_row(struct applicationEntityInvocationTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->applicationEntityInvocationTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->applicationEntityInvocationTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_applicationEntityInvocationTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the applicationEntityInvocationTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_applicationEntityInvocationTable(int force)
{
	if (!force && applicationEntityInvocationTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	applicationEntityInvocationTable_refresh = 0;
}

/**
 * @fn u_char *var_applicationEntityInvocationTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in applicationEntityInvocationTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_acseMIB above.
 */
u_char *
var_applicationEntityInvocationTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct applicationEntityInvocationTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("acseMIB", "var_applicationEntityInvocationTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_applicationEntityInvocationTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(applicationEntityInvocationTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_applicationEntityInvocationTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) APPLICATIONENTITYINVOCATIONROWSTATUS:	/* Create */
		*write_method = write_applicationEntityInvocationRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->applicationEntityInvocationRowStatus);
		rval = (u_char *) &StorageTmp->applicationEntityInvocationRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_applicationSubsystemTable_row(struct applicationSubsystemTable_data *StorageTmp, struct applicationSubsystemTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_applicationSubsystemTable_row(struct applicationSubsystemTable_data *StorageTmp, struct applicationSubsystemTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_applicationSubsystemTable_row(struct applicationSubsystemTable_data *StorageTmp, struct applicationSubsystemTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_applicationSubsystemTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_applicationSubsystemTable_row(struct applicationSubsystemTable_data *StorageTmp, struct applicationSubsystemTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	applicationSubsystemTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_applicationSubsystemTable_row(struct applicationSubsystemTable_data *StorageTmp, struct applicationSubsystemTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_applicationSubsystemTable_row(struct applicationSubsystemTable_data *StorageTmp, struct applicationSubsystemTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_applicationSubsystemTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_applicationSubsystemTable_row(struct applicationSubsystemTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the applicationSubsystemTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct applicationSubsystemTable_data *
refresh_applicationSubsystemTable_row(struct applicationSubsystemTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->applicationSubsystemTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->applicationSubsystemTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_applicationSubsystemTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the applicationSubsystemTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_applicationSubsystemTable(int force)
{
	if (!force && applicationSubsystemTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	applicationSubsystemTable_refresh = 0;
}

/**
 * @fn u_char *var_applicationSubsystemTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in applicationSubsystemTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_acseMIB above.
 */
u_char *
var_applicationSubsystemTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct applicationSubsystemTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("acseMIB", "var_applicationSubsystemTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_applicationSubsystemTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(applicationSubsystemTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_applicationSubsystemTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_asoTable_row(struct asoTable_data *StorageTmp, struct asoTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_asoTable_row(struct asoTable_data *StorageTmp, struct asoTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_asoTable_row(struct asoTable_data *StorageTmp, struct asoTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_asoTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_asoTable_row(struct asoTable_data *StorageTmp, struct asoTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	asoTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_asoTable_row(struct asoTable_data *StorageTmp, struct asoTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_asoTable_row(struct asoTable_data *StorageTmp, struct asoTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_asoTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_asoTable_row(struct asoTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the asoTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct asoTable_data *
refresh_asoTable_row(struct asoTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->asoTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->asoTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_asoTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the asoTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_asoTable(int force)
{
	if (!force && asoTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	asoTable_refresh = 0;
}

/**
 * @fn u_char *var_asoTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in asoTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_acseMIB above.
 */
u_char *
var_asoTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct asoTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("acseMIB", "var_asoTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_asoTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(asoTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_asoTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) ASOROWSTATUS:	/* Create */
		*write_method = write_asoRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->asoRowStatus);
		rval = (u_char *) &StorageTmp->asoRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_asoEntityTable_row(struct asoEntityTable_data *StorageTmp, struct asoEntityTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_asoEntityTable_row(struct asoEntityTable_data *StorageTmp, struct asoEntityTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_asoEntityTable_row(struct asoEntityTable_data *StorageTmp, struct asoEntityTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_asoEntityTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_asoEntityTable_row(struct asoEntityTable_data *StorageTmp, struct asoEntityTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	asoEntityTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_asoEntityTable_row(struct asoEntityTable_data *StorageTmp, struct asoEntityTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_asoEntityTable_row(struct asoEntityTable_data *StorageTmp, struct asoEntityTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_asoEntityTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_asoEntityTable_row(struct asoEntityTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the asoEntityTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct asoEntityTable_data *
refresh_asoEntityTable_row(struct asoEntityTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->asoEntityTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->asoEntityTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_asoEntityTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the asoEntityTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_asoEntityTable(int force)
{
	if (!force && asoEntityTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	asoEntityTable_refresh = 0;
}

/**
 * @fn u_char *var_asoEntityTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in asoEntityTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_acseMIB above.
 */
u_char *
var_asoEntityTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct asoEntityTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("acseMIB", "var_asoEntityTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_asoEntityTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(asoEntityTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_asoEntityTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) ASOENTITYROWSTATUS:	/* Create */
		*write_method = write_asoEntityRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->asoEntityRowStatus);
		rval = (u_char *) &StorageTmp->asoEntityRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_asoInvocationTable_row(struct asoInvocationTable_data *StorageTmp, struct asoInvocationTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_asoInvocationTable_row(struct asoInvocationTable_data *StorageTmp, struct asoInvocationTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_asoInvocationTable_row(struct asoInvocationTable_data *StorageTmp, struct asoInvocationTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_asoInvocationTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_asoInvocationTable_row(struct asoInvocationTable_data *StorageTmp, struct asoInvocationTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	asoInvocationTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_asoInvocationTable_row(struct asoInvocationTable_data *StorageTmp, struct asoInvocationTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_asoInvocationTable_row(struct asoInvocationTable_data *StorageTmp, struct asoInvocationTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_asoInvocationTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_asoInvocationTable_row(struct asoInvocationTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the asoInvocationTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct asoInvocationTable_data *
refresh_asoInvocationTable_row(struct asoInvocationTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->asoInvocationTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->asoInvocationTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_asoInvocationTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the asoInvocationTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_asoInvocationTable(int force)
{
	if (!force && asoInvocationTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	asoInvocationTable_refresh = 0;
}

/**
 * @fn u_char *var_asoInvocationTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in asoInvocationTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_acseMIB above.
 */
u_char *
var_asoInvocationTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct asoInvocationTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("acseMIB", "var_asoInvocationTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_asoInvocationTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(asoInvocationTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_asoInvocationTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) ASOINVOCATIONROWSTATUS:	/* Create */
		*write_method = write_asoInvocationRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->asoInvocationRowStatus);
		rval = (u_char *) &StorageTmp->asoInvocationRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_monoULConnectionTable_row(struct monoULConnectionTable_data *StorageTmp, struct monoULConnectionTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_monoULConnectionTable_row(struct monoULConnectionTable_data *StorageTmp, struct monoULConnectionTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_monoULConnectionTable_row(struct monoULConnectionTable_data *StorageTmp, struct monoULConnectionTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_monoULConnectionTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_monoULConnectionTable_row(struct monoULConnectionTable_data *StorageTmp, struct monoULConnectionTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	monoULConnectionTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_monoULConnectionTable_row(struct monoULConnectionTable_data *StorageTmp, struct monoULConnectionTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_monoULConnectionTable_row(struct monoULConnectionTable_data *StorageTmp, struct monoULConnectionTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_monoULConnectionTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_monoULConnectionTable_row(struct monoULConnectionTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the monoULConnectionTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct monoULConnectionTable_data *
refresh_monoULConnectionTable_row(struct monoULConnectionTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->monoULConnectionTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->monoULConnectionTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_monoULConnectionTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the monoULConnectionTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_monoULConnectionTable(int force)
{
	if (!force && monoULConnectionTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	monoULConnectionTable_refresh = 0;
}

/**
 * @fn u_char *var_monoULConnectionTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in monoULConnectionTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_acseMIB above.
 */
u_char *
var_monoULConnectionTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct monoULConnectionTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("acseMIB", "var_monoULConnectionTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_monoULConnectionTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(monoULConnectionTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_monoULConnectionTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) MONOULCONNECTIONROWSTATUS:	/* Create */
		*write_method = write_monoULConnectionRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->monoULConnectionRowStatus);
		rval = (u_char *) &StorageTmp->monoULConnectionRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_monoULEntityTable_row(struct monoULEntityTable_data *StorageTmp, struct monoULEntityTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_monoULEntityTable_row(struct monoULEntityTable_data *StorageTmp, struct monoULEntityTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_monoULEntityTable_row(struct monoULEntityTable_data *StorageTmp, struct monoULEntityTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_monoULEntityTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_monoULEntityTable_row(struct monoULEntityTable_data *StorageTmp, struct monoULEntityTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	monoULEntityTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_monoULEntityTable_row(struct monoULEntityTable_data *StorageTmp, struct monoULEntityTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_monoULEntityTable_row(struct monoULEntityTable_data *StorageTmp, struct monoULEntityTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_monoULEntityTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_monoULEntityTable_row(struct monoULEntityTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the monoULEntityTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct monoULEntityTable_data *
refresh_monoULEntityTable_row(struct monoULEntityTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->monoULEntityTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->monoULEntityTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_monoULEntityTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the monoULEntityTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_monoULEntityTable(int force)
{
	if (!force && monoULEntityTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	monoULEntityTable_refresh = 0;
}

/**
 * @fn u_char *var_monoULEntityTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in monoULEntityTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_acseMIB above.
 */
u_char *
var_monoULEntityTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct monoULEntityTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("acseMIB", "var_monoULEntityTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_monoULEntityTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(monoULEntityTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_monoULEntityTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_monolithicUpperLayerSubsystemTable_row(struct monolithicUpperLayerSubsystemTable_data *StorageTmp, struct monolithicUpperLayerSubsystemTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_monolithicUpperLayerSubsystemTable_row(struct monolithicUpperLayerSubsystemTable_data *StorageTmp, struct monolithicUpperLayerSubsystemTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_monolithicUpperLayerSubsystemTable_row(struct monolithicUpperLayerSubsystemTable_data *StorageTmp, struct monolithicUpperLayerSubsystemTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_monolithicUpperLayerSubsystemTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_monolithicUpperLayerSubsystemTable_row(struct monolithicUpperLayerSubsystemTable_data *StorageTmp, struct monolithicUpperLayerSubsystemTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	monolithicUpperLayerSubsystemTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_monolithicUpperLayerSubsystemTable_row(struct monolithicUpperLayerSubsystemTable_data *StorageTmp, struct monolithicUpperLayerSubsystemTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_monolithicUpperLayerSubsystemTable_row(struct monolithicUpperLayerSubsystemTable_data *StorageTmp, struct monolithicUpperLayerSubsystemTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_monolithicUpperLayerSubsystemTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_monolithicUpperLayerSubsystemTable_row(struct monolithicUpperLayerSubsystemTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the monolithicUpperLayerSubsystemTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct monolithicUpperLayerSubsystemTable_data *
refresh_monolithicUpperLayerSubsystemTable_row(struct monolithicUpperLayerSubsystemTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->monolithicUpperLayerSubsystemTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->monolithicUpperLayerSubsystemTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_monolithicUpperLayerSubsystemTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the monolithicUpperLayerSubsystemTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_monolithicUpperLayerSubsystemTable(int force)
{
	if (!force && monolithicUpperLayerSubsystemTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	monolithicUpperLayerSubsystemTable_refresh = 0;
}

/**
 * @fn u_char *var_monolithicUpperLayerSubsystemTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in monolithicUpperLayerSubsystemTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_acseMIB above.
 */
u_char *
var_monolithicUpperLayerSubsystemTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct monolithicUpperLayerSubsystemTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("acseMIB", "var_monolithicUpperLayerSubsystemTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_monolithicUpperLayerSubsystemTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(monolithicUpperLayerSubsystemTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_monolithicUpperLayerSubsystemTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) MONOLITHICUPPERLAYERSUBSSYTEMROWSTATUS:	/* Create */
		*write_method = write_monolithicUpperLayerSubssytemRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->monolithicUpperLayerSubssytemRowStatus);
		rval = (u_char *) &StorageTmp->monolithicUpperLayerSubssytemRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_pSapTable_row(struct pSapTable_data *StorageTmp, struct pSapTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_pSapTable_row(struct pSapTable_data *StorageTmp, struct pSapTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_pSapTable_row(struct pSapTable_data *StorageTmp, struct pSapTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_pSapTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_pSapTable_row(struct pSapTable_data *StorageTmp, struct pSapTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	pSapTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_pSapTable_row(struct pSapTable_data *StorageTmp, struct pSapTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_pSapTable_row(struct pSapTable_data *StorageTmp, struct pSapTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_pSapTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_pSapTable_row(struct pSapTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the pSapTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct pSapTable_data *
refresh_pSapTable_row(struct pSapTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->pSapTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->pSapTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_pSapTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the pSapTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_pSapTable(int force)
{
	if (!force && pSapTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	pSapTable_refresh = 0;
}

/**
 * @fn u_char *var_pSapTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in pSapTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_acseMIB above.
 */
u_char *
var_pSapTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct pSapTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("acseMIB", "var_pSapTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_pSapTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(pSapTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_pSapTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) PSAPROWSTATUS:	/* Create */
		*write_method = write_pSapRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->pSapRowStatus);
		rval = (u_char *) &StorageTmp->pSapRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_presentationConnectionTable_row(struct presentationConnectionTable_data *StorageTmp, struct presentationConnectionTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_presentationConnectionTable_row(struct presentationConnectionTable_data *StorageTmp, struct presentationConnectionTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_presentationConnectionTable_row(struct presentationConnectionTable_data *StorageTmp, struct presentationConnectionTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_presentationConnectionTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_presentationConnectionTable_row(struct presentationConnectionTable_data *StorageTmp, struct presentationConnectionTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	presentationConnectionTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_presentationConnectionTable_row(struct presentationConnectionTable_data *StorageTmp, struct presentationConnectionTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_presentationConnectionTable_row(struct presentationConnectionTable_data *StorageTmp, struct presentationConnectionTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_presentationConnectionTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_presentationConnectionTable_row(struct presentationConnectionTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the presentationConnectionTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct presentationConnectionTable_data *
refresh_presentationConnectionTable_row(struct presentationConnectionTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->presentationConnectionTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->presentationConnectionTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_presentationConnectionTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the presentationConnectionTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_presentationConnectionTable(int force)
{
	if (!force && presentationConnectionTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	presentationConnectionTable_refresh = 0;
}

/**
 * @fn u_char *var_presentationConnectionTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in presentationConnectionTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_acseMIB above.
 */
u_char *
var_presentationConnectionTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct presentationConnectionTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("acseMIB", "var_presentationConnectionTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_presentationConnectionTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(presentationConnectionTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_presentationConnectionTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) PRESENTATIONCONNECTIONROWSTATUS:	/* Create */
		*write_method = write_presentationCOnnectionRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->presentationCOnnectionRowStatus);
		rval = (u_char *) &StorageTmp->presentationCOnnectionRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_presentationCopmTable_row(struct presentationCopmTable_data *StorageTmp, struct presentationCopmTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_presentationCopmTable_row(struct presentationCopmTable_data *StorageTmp, struct presentationCopmTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_presentationCopmTable_row(struct presentationCopmTable_data *StorageTmp, struct presentationCopmTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_presentationCopmTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_presentationCopmTable_row(struct presentationCopmTable_data *StorageTmp, struct presentationCopmTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	presentationCopmTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_presentationCopmTable_row(struct presentationCopmTable_data *StorageTmp, struct presentationCopmTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_presentationCopmTable_row(struct presentationCopmTable_data *StorageTmp, struct presentationCopmTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_presentationCopmTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_presentationCopmTable_row(struct presentationCopmTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the presentationCopmTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct presentationCopmTable_data *
refresh_presentationCopmTable_row(struct presentationCopmTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->presentationCopmTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->presentationCopmTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_presentationCopmTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the presentationCopmTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_presentationCopmTable(int force)
{
	if (!force && presentationCopmTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	presentationCopmTable_refresh = 0;
}

/**
 * @fn u_char *var_presentationCopmTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in presentationCopmTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_acseMIB above.
 */
u_char *
var_presentationCopmTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct presentationCopmTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("acseMIB", "var_presentationCopmTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_presentationCopmTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(presentationCopmTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_presentationCopmTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) PRESENTATIONCOPMROWSTATUS:	/* Create */
		*write_method = write_presentationCopmRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->presentationCopmRowStatus);
		rval = (u_char *) &StorageTmp->presentationCopmRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_presentationEntityTable_row(struct presentationEntityTable_data *StorageTmp, struct presentationEntityTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_presentationEntityTable_row(struct presentationEntityTable_data *StorageTmp, struct presentationEntityTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_presentationEntityTable_row(struct presentationEntityTable_data *StorageTmp, struct presentationEntityTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_presentationEntityTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_presentationEntityTable_row(struct presentationEntityTable_data *StorageTmp, struct presentationEntityTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	presentationEntityTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_presentationEntityTable_row(struct presentationEntityTable_data *StorageTmp, struct presentationEntityTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_presentationEntityTable_row(struct presentationEntityTable_data *StorageTmp, struct presentationEntityTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_presentationEntityTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_presentationEntityTable_row(struct presentationEntityTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the presentationEntityTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct presentationEntityTable_data *
refresh_presentationEntityTable_row(struct presentationEntityTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->presentationEntityTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->presentationEntityTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_presentationEntityTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the presentationEntityTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_presentationEntityTable(int force)
{
	if (!force && presentationEntityTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	presentationEntityTable_refresh = 0;
}

/**
 * @fn u_char *var_presentationEntityTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in presentationEntityTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_acseMIB above.
 */
u_char *
var_presentationEntityTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct presentationEntityTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("acseMIB", "var_presentationEntityTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_presentationEntityTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(presentationEntityTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_presentationEntityTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) PRESENTATIONENTITYROWSTATUS:	/* Create */
		*write_method = write_presentationEntityRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->presentationEntityRowStatus);
		rval = (u_char *) &StorageTmp->presentationEntityRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_presentationSubsystemTable_row(struct presentationSubsystemTable_data *StorageTmp, struct presentationSubsystemTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_presentationSubsystemTable_row(struct presentationSubsystemTable_data *StorageTmp, struct presentationSubsystemTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_presentationSubsystemTable_row(struct presentationSubsystemTable_data *StorageTmp, struct presentationSubsystemTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_presentationSubsystemTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_presentationSubsystemTable_row(struct presentationSubsystemTable_data *StorageTmp, struct presentationSubsystemTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	presentationSubsystemTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_presentationSubsystemTable_row(struct presentationSubsystemTable_data *StorageTmp, struct presentationSubsystemTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_presentationSubsystemTable_row(struct presentationSubsystemTable_data *StorageTmp, struct presentationSubsystemTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_presentationSubsystemTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_presentationSubsystemTable_row(struct presentationSubsystemTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the presentationSubsystemTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct presentationSubsystemTable_data *
refresh_presentationSubsystemTable_row(struct presentationSubsystemTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->presentationSubsystemTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->presentationSubsystemTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_presentationSubsystemTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the presentationSubsystemTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_presentationSubsystemTable(int force)
{
	if (!force && presentationSubsystemTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	presentationSubsystemTable_refresh = 0;
}

/**
 * @fn u_char *var_presentationSubsystemTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in presentationSubsystemTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_acseMIB above.
 */
u_char *
var_presentationSubsystemTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct presentationSubsystemTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("acseMIB", "var_presentationSubsystemTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_presentationSubsystemTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(presentationSubsystemTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_presentationSubsystemTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) PRESENTATIONSUBSYSTEMROWSTATUS:	/* Create */
		*write_method = write_presentationSubsystemRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->presentationSubsystemRowStatus);
		rval = (u_char *) &StorageTmp->presentationSubsystemRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_sSapTable_row(struct sSapTable_data *StorageTmp, struct sSapTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_sSapTable_row(struct sSapTable_data *StorageTmp, struct sSapTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_sSapTable_row(struct sSapTable_data *StorageTmp, struct sSapTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_sSapTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_sSapTable_row(struct sSapTable_data *StorageTmp, struct sSapTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	sSapTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_sSapTable_row(struct sSapTable_data *StorageTmp, struct sSapTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_sSapTable_row(struct sSapTable_data *StorageTmp, struct sSapTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_sSapTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_sSapTable_row(struct sSapTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the sSapTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct sSapTable_data *
refresh_sSapTable_row(struct sSapTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->sSapTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->sSapTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_sSapTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the sSapTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_sSapTable(int force)
{
	if (!force && sSapTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	sSapTable_refresh = 0;
}

/**
 * @fn u_char *var_sSapTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in sSapTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_acseMIB above.
 */
u_char *
var_sSapTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct sSapTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("acseMIB", "var_sSapTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_sSapTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(sSapTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_sSapTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) SSAPROWSTATUS:	/* Create */
		*write_method = write_sSapRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->sSapRowStatus);
		rval = (u_char *) &StorageTmp->sSapRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_sessionConnectionTable_row(struct sessionConnectionTable_data *StorageTmp, struct sessionConnectionTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_sessionConnectionTable_row(struct sessionConnectionTable_data *StorageTmp, struct sessionConnectionTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_sessionConnectionTable_row(struct sessionConnectionTable_data *StorageTmp, struct sessionConnectionTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_sessionConnectionTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_sessionConnectionTable_row(struct sessionConnectionTable_data *StorageTmp, struct sessionConnectionTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	sessionConnectionTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_sessionConnectionTable_row(struct sessionConnectionTable_data *StorageTmp, struct sessionConnectionTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_sessionConnectionTable_row(struct sessionConnectionTable_data *StorageTmp, struct sessionConnectionTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_sessionConnectionTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_sessionConnectionTable_row(struct sessionConnectionTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the sessionConnectionTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct sessionConnectionTable_data *
refresh_sessionConnectionTable_row(struct sessionConnectionTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->sessionConnectionTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->sessionConnectionTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_sessionConnectionTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the sessionConnectionTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_sessionConnectionTable(int force)
{
	if (!force && sessionConnectionTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	sessionConnectionTable_refresh = 0;
}

/**
 * @fn u_char *var_sessionConnectionTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in sessionConnectionTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_acseMIB above.
 */
u_char *
var_sessionConnectionTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct sessionConnectionTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("acseMIB", "var_sessionConnectionTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_sessionConnectionTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(sessionConnectionTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_sessionConnectionTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) SESSIONCONNECTIONROWSTATUS:	/* Create */
		*write_method = write_sessionCOnnectionRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->sessionCOnnectionRowStatus);
		rval = (u_char *) &StorageTmp->sessionCOnnectionRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_sessionCopmTable_row(struct sessionCopmTable_data *StorageTmp, struct sessionCopmTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_sessionCopmTable_row(struct sessionCopmTable_data *StorageTmp, struct sessionCopmTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_sessionCopmTable_row(struct sessionCopmTable_data *StorageTmp, struct sessionCopmTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_sessionCopmTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_sessionCopmTable_row(struct sessionCopmTable_data *StorageTmp, struct sessionCopmTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	sessionCopmTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_sessionCopmTable_row(struct sessionCopmTable_data *StorageTmp, struct sessionCopmTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_sessionCopmTable_row(struct sessionCopmTable_data *StorageTmp, struct sessionCopmTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_sessionCopmTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_sessionCopmTable_row(struct sessionCopmTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the sessionCopmTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct sessionCopmTable_data *
refresh_sessionCopmTable_row(struct sessionCopmTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->sessionCopmTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->sessionCopmTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_sessionCopmTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the sessionCopmTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_sessionCopmTable(int force)
{
	if (!force && sessionCopmTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	sessionCopmTable_refresh = 0;
}

/**
 * @fn u_char *var_sessionCopmTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in sessionCopmTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_acseMIB above.
 */
u_char *
var_sessionCopmTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct sessionCopmTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("acseMIB", "var_sessionCopmTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_sessionCopmTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(sessionCopmTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_sessionCopmTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) SESSIONCOPMROWSTATUS:	/* Create */
		*write_method = write_sessionCopmRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->sessionCopmRowStatus);
		rval = (u_char *) &StorageTmp->sessionCopmRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_sessionEntityTable_row(struct sessionEntityTable_data *StorageTmp, struct sessionEntityTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_sessionEntityTable_row(struct sessionEntityTable_data *StorageTmp, struct sessionEntityTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_sessionEntityTable_row(struct sessionEntityTable_data *StorageTmp, struct sessionEntityTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_sessionEntityTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_sessionEntityTable_row(struct sessionEntityTable_data *StorageTmp, struct sessionEntityTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	sessionEntityTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_sessionEntityTable_row(struct sessionEntityTable_data *StorageTmp, struct sessionEntityTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_sessionEntityTable_row(struct sessionEntityTable_data *StorageTmp, struct sessionEntityTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_sessionEntityTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_sessionEntityTable_row(struct sessionEntityTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the sessionEntityTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct sessionEntityTable_data *
refresh_sessionEntityTable_row(struct sessionEntityTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->sessionEntityTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->sessionEntityTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_sessionEntityTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the sessionEntityTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_sessionEntityTable(int force)
{
	if (!force && sessionEntityTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	sessionEntityTable_refresh = 0;
}

/**
 * @fn u_char *var_sessionEntityTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in sessionEntityTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_acseMIB above.
 */
u_char *
var_sessionEntityTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct sessionEntityTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("acseMIB", "var_sessionEntityTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_sessionEntityTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(sessionEntityTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_sessionEntityTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) SESSIONENTITYROWSTATUS:	/* Create */
		*write_method = write_sessionEntityRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->sessionEntityRowStatus);
		rval = (u_char *) &StorageTmp->sessionEntityRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_sessionSubsystemTable_row(struct sessionSubsystemTable_data *StorageTmp, struct sessionSubsystemTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_sessionSubsystemTable_row(struct sessionSubsystemTable_data *StorageTmp, struct sessionSubsystemTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_sessionSubsystemTable_row(struct sessionSubsystemTable_data *StorageTmp, struct sessionSubsystemTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_sessionSubsystemTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_sessionSubsystemTable_row(struct sessionSubsystemTable_data *StorageTmp, struct sessionSubsystemTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	sessionSubsystemTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_sessionSubsystemTable_row(struct sessionSubsystemTable_data *StorageTmp, struct sessionSubsystemTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_sessionSubsystemTable_row(struct sessionSubsystemTable_data *StorageTmp, struct sessionSubsystemTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_sessionSubsystemTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_sessionSubsystemTable_row(struct sessionSubsystemTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the sessionSubsystemTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct sessionSubsystemTable_data *
refresh_sessionSubsystemTable_row(struct sessionSubsystemTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->sessionSubsystemTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->sessionSubsystemTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_sessionSubsystemTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the sessionSubsystemTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_sessionSubsystemTable(int force)
{
	if (!force && sessionSubsystemTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	sessionSubsystemTable_refresh = 0;
}

/**
 * @fn u_char *var_sessionSubsystemTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in sessionSubsystemTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_acseMIB above.
 */
u_char *
var_sessionSubsystemTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct sessionSubsystemTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("acseMIB", "var_sessionSubsystemTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_sessionSubsystemTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(sessionSubsystemTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_sessionSubsystemTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) SESSIONSUBSYSTEMROWSTATUS:	/* Create */
		*write_method = write_sessionSubsystemRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->sessionSubsystemRowStatus);
		rval = (u_char *) &StorageTmp->sessionSubsystemRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_acseEntityTable_row(struct acseEntityTable_data *StorageTmp, struct acseEntityTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_acseEntityTable_row(struct acseEntityTable_data *StorageTmp, struct acseEntityTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_acseEntityTable_row(struct acseEntityTable_data *StorageTmp, struct acseEntityTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_acseEntityTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_acseEntityTable_row(struct acseEntityTable_data *StorageTmp, struct acseEntityTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	acseEntityTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_acseEntityTable_row(struct acseEntityTable_data *StorageTmp, struct acseEntityTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_acseEntityTable_row(struct acseEntityTable_data *StorageTmp, struct acseEntityTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_acseEntityTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_acseEntityTable_row(struct acseEntityTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the acseEntityTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct acseEntityTable_data *
refresh_acseEntityTable_row(struct acseEntityTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->acseEntityTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->acseEntityTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_acseEntityTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the acseEntityTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_acseEntityTable(int force)
{
	if (!force && acseEntityTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	acseEntityTable_refresh = 0;
}

/**
 * @fn u_char *var_acseEntityTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in acseEntityTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_acseMIB above.
 */
u_char *
var_acseEntityTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct acseEntityTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("acseMIB", "var_acseEntityTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_acseEntityTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(acseEntityTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_acseEntityTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) ACSEENTITYROWSTATUS:	/* Create */
		*write_method = write_acseEntityRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->acseEntityRowStatus);
		rval = (u_char *) &StorageTmp->acseEntityRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_acseAccessPointTable_row(struct acseAccessPointTable_data *StorageTmp, struct acseAccessPointTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_acseAccessPointTable_row(struct acseAccessPointTable_data *StorageTmp, struct acseAccessPointTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_acseAccessPointTable_row(struct acseAccessPointTable_data *StorageTmp, struct acseAccessPointTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_acseAccessPointTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_acseAccessPointTable_row(struct acseAccessPointTable_data *StorageTmp, struct acseAccessPointTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	acseAccessPointTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_acseAccessPointTable_row(struct acseAccessPointTable_data *StorageTmp, struct acseAccessPointTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_acseAccessPointTable_row(struct acseAccessPointTable_data *StorageTmp, struct acseAccessPointTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_acseAccessPointTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_acseAccessPointTable_row(struct acseAccessPointTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the acseAccessPointTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct acseAccessPointTable_data *
refresh_acseAccessPointTable_row(struct acseAccessPointTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->acseAccessPointTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->acseAccessPointTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_acseAccessPointTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the acseAccessPointTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_acseAccessPointTable(int force)
{
	if (!force && acseAccessPointTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	acseAccessPointTable_refresh = 0;
}

/**
 * @fn u_char *var_acseAccessPointTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in acseAccessPointTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_acseMIB above.
 */
u_char *
var_acseAccessPointTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct acseAccessPointTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("acseMIB", "var_acseAccessPointTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_acseAccessPointTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(acseAccessPointTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_acseAccessPointTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) ACSEACCESSPOINTROWSTATUS:	/* Create */
		*write_method = write_acseAccessPointRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->acseAccessPointRowStatus);
		rval = (u_char *) &StorageTmp->acseAccessPointRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int check_acseLinkageTable_row(struct acseLinkageTable_data *StorageTmp, struct acseLinkageTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to check, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the ACTION phase.  The start of the ACTION pahse performs this consitency check
 * on the row before allowing the request to proceed to the COMMIT phase.  This function can return
 * SNMP_ERR_NOERR or a specific SNMP error value.  Values in StorageOld are the values before the
 * varbinds on the mib were applied; the values in StorageTmp are the new values.  The function is
 * permitted to change the values in StorageTmp to correct them; however, preference should be made
 * for setting values where were not in the varbinds.
 */
int
check_acseLinkageTable_row(struct acseLinkageTable_data *StorageTmp, struct acseLinkageTable_data *StorageOld)
{
	/* XXX: provide code to check the row for consistency */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int update_acseLinkageTable_row(struct acseLinkageTable_data *StorageTmp, struct acseLinkageTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the set operation (ACTION phase) on a row
 *
 * This function is used both by tables that do and do not contain a RowStatus object.  It is used
 * to update, row-at-a-time, the varbinds belonging to the row.  Note that this function is not used
 * when rows are created or destroyed.  This function is called for the first varbind in a row at
 * the beginning of the COMMIT phase.  The start of the ACTION phase performs a consistency check on
 * the row before allowing the request to proceed to the COMMIT phase.  The COMMIT phase then
 * arrives here with consistency already checked (see check_acseLinkageTable_row()).  This function can
 * return SNMP_ERR_NOERROR or SNMP_ERR_COMMITFAILED.  Values in StorageOld are the values before the
 * varbinds on the row were applied: the values in StorageTmp are the new values.
 */
int
update_acseLinkageTable_row(struct acseLinkageTable_data *StorageTmp, struct acseLinkageTable_data *StorageOld)
{
	/* XXX: provide code to update the row with the underlying device */
	acseLinkageTable_refresh = 1;
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int revert_acseLinkageTable_row(struct acseLinkageTable_data *StorageTmp, struct acseLinkageTable_data *StorageOld)
 * @param StorageTmp the data as updated.
 * @param StorageOld the data previous to update.
 * @brief perform the undo operation (UNDO phase) on a row
 */
void
revert_acseLinkageTable_row(struct acseLinkageTable_data *StorageTmp, struct acseLinkageTable_data *StorageOld)
{
	/* XXX: provide code to revert the row with the underlying device */
	update_acseLinkageTable_row(StorageOld, NULL);
}

/**
 * @fn void refresh_acseLinkageTable_row(struct acseLinkageTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the acseLinkageTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct acseLinkageTable_data *
refresh_acseLinkageTable_row(struct acseLinkageTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->acseLinkageTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->acseLinkageTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_acseLinkageTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the acseLinkageTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_acseLinkageTable(int force)
{
	if (!force && acseLinkageTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	acseLinkageTable_refresh = 0;
}

/**
 * @fn u_char *var_acseLinkageTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in acseLinkageTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_acseMIB above.
 */
u_char *
var_acseLinkageTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct acseLinkageTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("acseMIB", "var_acseLinkageTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_acseLinkageTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(acseLinkageTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_acseLinkageTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) ACSELINKAGEROWSTATUS:	/* Create */
		*write_method = write_acseLinkageRowStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->acseLinkageRowStatus);
		rval = (u_char *) &StorageTmp->acseLinkageRowStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int can_act_acseAssociationTable_row(struct acseAssociationTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_acseAssociationTable_row(struct acseAssociationTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_acseAssociationTable_row(struct acseAssociationTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_acseAssociationTable_row(struct acseAssociationTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_apInvocationTable_row(struct apInvocationTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_apInvocationTable_row(struct apInvocationTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_apInvocationTable_row(struct apInvocationTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_apInvocationTable_row(struct apInvocationTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_applicationEntityInvocationTable_row(struct applicationEntityInvocationTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_applicationEntityInvocationTable_row(struct applicationEntityInvocationTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_applicationEntityInvocationTable_row(struct applicationEntityInvocationTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_applicationEntityInvocationTable_row(struct applicationEntityInvocationTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_asoTable_row(struct asoTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_asoTable_row(struct asoTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_asoTable_row(struct asoTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_asoTable_row(struct asoTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_asoEntityTable_row(struct asoEntityTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_asoEntityTable_row(struct asoEntityTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_asoEntityTable_row(struct asoEntityTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_asoEntityTable_row(struct asoEntityTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_asoInvocationTable_row(struct asoInvocationTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_asoInvocationTable_row(struct asoInvocationTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_asoInvocationTable_row(struct asoInvocationTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_asoInvocationTable_row(struct asoInvocationTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_monoULConnectionTable_row(struct monoULConnectionTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_monoULConnectionTable_row(struct monoULConnectionTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_monoULConnectionTable_row(struct monoULConnectionTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_monoULConnectionTable_row(struct monoULConnectionTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_monolithicUpperLayerSubsystemTable_row(struct monolithicUpperLayerSubsystemTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_monolithicUpperLayerSubsystemTable_row(struct monolithicUpperLayerSubsystemTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_monolithicUpperLayerSubsystemTable_row(struct monolithicUpperLayerSubsystemTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_monolithicUpperLayerSubsystemTable_row(struct monolithicUpperLayerSubsystemTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_pSapTable_row(struct pSapTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_pSapTable_row(struct pSapTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_pSapTable_row(struct pSapTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_pSapTable_row(struct pSapTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_presentationConnectionTable_row(struct presentationConnectionTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_presentationConnectionTable_row(struct presentationConnectionTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_presentationConnectionTable_row(struct presentationConnectionTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_presentationConnectionTable_row(struct presentationConnectionTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_presentationCopmTable_row(struct presentationCopmTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_presentationCopmTable_row(struct presentationCopmTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_presentationCopmTable_row(struct presentationCopmTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_presentationCopmTable_row(struct presentationCopmTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_presentationEntityTable_row(struct presentationEntityTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_presentationEntityTable_row(struct presentationEntityTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_presentationEntityTable_row(struct presentationEntityTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_presentationEntityTable_row(struct presentationEntityTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_presentationSubsystemTable_row(struct presentationSubsystemTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_presentationSubsystemTable_row(struct presentationSubsystemTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_presentationSubsystemTable_row(struct presentationSubsystemTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_presentationSubsystemTable_row(struct presentationSubsystemTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_sSapTable_row(struct sSapTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_sSapTable_row(struct sSapTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_sSapTable_row(struct sSapTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_sSapTable_row(struct sSapTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_sessionConnectionTable_row(struct sessionConnectionTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_sessionConnectionTable_row(struct sessionConnectionTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_sessionConnectionTable_row(struct sessionConnectionTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_sessionConnectionTable_row(struct sessionConnectionTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_sessionCopmTable_row(struct sessionCopmTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_sessionCopmTable_row(struct sessionCopmTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_sessionCopmTable_row(struct sessionCopmTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_sessionCopmTable_row(struct sessionCopmTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_sessionEntityTable_row(struct sessionEntityTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_sessionEntityTable_row(struct sessionEntityTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_sessionEntityTable_row(struct sessionEntityTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_sessionEntityTable_row(struct sessionEntityTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_sessionSubsystemTable_row(struct sessionSubsystemTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_sessionSubsystemTable_row(struct sessionSubsystemTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_sessionSubsystemTable_row(struct sessionSubsystemTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_sessionSubsystemTable_row(struct sessionSubsystemTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_acseEntityTable_row(struct acseEntityTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_acseEntityTable_row(struct acseEntityTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_acseEntityTable_row(struct acseEntityTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_acseEntityTable_row(struct acseEntityTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_acseAccessPointTable_row(struct acseAccessPointTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_acseAccessPointTable_row(struct acseAccessPointTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_acseAccessPointTable_row(struct acseAccessPointTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_acseAccessPointTable_row(struct acseAccessPointTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_act_acseLinkageTable_row(struct acseLinkageTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an inactive table row can be activated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an inactive table
 * row can be activated.  Returns SNMP_ERR_NOERROR when activation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_act_acseLinkageTable_row(struct acseLinkageTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the new or inactive table row can be activated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int can_deact_acseLinkageTable_row(struct acseLinkageTable_data *StorageTmp)
 * @param StorageTmp the data (as updated)
 * @brief check whether an active table row can be deactivated
 *
 * This function is used by the ACTION phase of a RowStatus object to test whether an active table
 * row can be deactivated.  Returns SNMP_ERR_NOERROR when deactivation is permitted; an SNMP error
 * value, otherwise.  This function might use a 'test' operation against the driver to ensure that
 * the commit phase will succeed.
 */
int
can_deact_acseLinkageTable_row(struct acseLinkageTable_data *StorageTmp)
{
	/* XXX: provide code to check whether the active table row can be deactivated */
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_acseAssociationRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_acseAssociationRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct acseAssociationTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct acseAssociationTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 15;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("acseMIB", "write_acseAssociationRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(acseAssociationTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to acseAssociationRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to acseAssociationRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* applicationSubsystemId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* apInvocationId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* asoInvocationId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[15]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* applicationSubsystemId */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index applicationSubsystemId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* apInvocationId */
			/* Note: ranges 1..32 */
			if (vp->val_len > SPRINT_MAX_LEN || ((1 > vp->val_len || vp->val_len > 32))) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index apInvocationId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* asoInvocationId */
			/* Note: ranges 1..32 */
			if (vp->val_len > SPRINT_MAX_LEN || ((1 > vp->val_len || vp->val_len > 32))) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index asoInvocationId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = acseAssociationTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->acseAssociationTable_rsvs = 1;
			vp = vars;
			StorageNew->applicationSubsystemId = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			memdup((void *) &StorageNew->apInvocationId, vp->val.string, vp->val_len);
			StorageNew->apInvocationIdLen = vp->val_len;
			vp = vp->next_variable;
			memdup((void *) &StorageNew->asoInvocationId, vp->val.string, vp->val_len);
			StorageNew->asoInvocationIdLen = vp->val_len;
			vp = vp->next_variable;
			header_complex_add_data(&acseAssociationTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->acseAssociationRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->acseAssociationTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->acseAssociationTable_old) == NULL)
				if (StorageTmp->acseAssociationTable_rsvs == 0)
					if ((StorageOld = StorageTmp->acseAssociationTable_old = acseAssociationTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->acseAssociationTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->acseAssociationTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				acseAssociationTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_acseAssociationTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
				/* check that activation is possible */
			if (StorageTmp->acseAssociationRowStatus != RS_ACTIVE)
				if ((ret = can_act_acseAssociationTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->acseAssociationRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_acseAssociationTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->acseAssociationRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_acseAssociationTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
				}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->acseAssociationRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here must be
		   reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_acseAssociationTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->acseAssociationRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_acseAssociationTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->acseAssociationRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_acseAssociationTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_acseAssociationTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->acseAssociationRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->acseAssociationRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->acseAssociationRowStatus = set_value;
			if ((StorageOld = StorageTmp->acseAssociationTable_old) != NULL) {
				acseAssociationTable_destroy(&StorageTmp->acseAssociationTable_old);
				StorageTmp->acseAssociationTable_rsvs = 0;
				StorageTmp->acseAssociationTable_tsts = 0;
				StorageTmp->acseAssociationTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			acseAssociationTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_acseAssociationTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->acseAssociationRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_acseAssociationTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->acseAssociationRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_acseAssociationTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				acseAssociationTable_del(StorageNew);
				acseAssociationTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->acseAssociationTable_old) == NULL)
				break;
			if (--StorageTmp->acseAssociationTable_rsvs == 0)
				acseAssociationTable_destroy(&StorageTmp->acseAssociationTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				acseAssociationTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_apInvocationRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_apInvocationRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct apInvocationTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct apInvocationTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 15;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("acseMIB", "write_apInvocationRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(apInvocationTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to apInvocationRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to apInvocationRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* applicationSubsystemId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* apInvocationId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[15]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* applicationSubsystemId */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index applicationSubsystemId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* apInvocationId */
			/* Note: ranges 1..32 */
			if (vp->val_len > SPRINT_MAX_LEN || ((1 > vp->val_len || vp->val_len > 32))) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index apInvocationId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = apInvocationTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->apInvocationTable_rsvs = 1;
			vp = vars;
			StorageNew->applicationSubsystemId = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			memdup((void *) &StorageNew->apInvocationId, vp->val.string, vp->val_len);
			StorageNew->apInvocationIdLen = vp->val_len;
			vp = vp->next_variable;
			header_complex_add_data(&apInvocationTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->apInvocationRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->apInvocationTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->apInvocationTable_old) == NULL)
				if (StorageTmp->apInvocationTable_rsvs == 0)
					if ((StorageOld = StorageTmp->apInvocationTable_old = apInvocationTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->apInvocationTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->apInvocationTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				apInvocationTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_apInvocationTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
				/* check that activation is possible */
			if (StorageTmp->apInvocationRowStatus != RS_ACTIVE)
				if ((ret = can_act_apInvocationTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->apInvocationRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_apInvocationTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->apInvocationRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_apInvocationTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
				}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->apInvocationRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here must be
		   reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_apInvocationTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->apInvocationRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_apInvocationTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->apInvocationRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_apInvocationTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_apInvocationTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->apInvocationRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->apInvocationRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->apInvocationRowStatus = set_value;
			if ((StorageOld = StorageTmp->apInvocationTable_old) != NULL) {
				apInvocationTable_destroy(&StorageTmp->apInvocationTable_old);
				StorageTmp->apInvocationTable_rsvs = 0;
				StorageTmp->apInvocationTable_tsts = 0;
				StorageTmp->apInvocationTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			apInvocationTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_apInvocationTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->apInvocationRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_apInvocationTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->apInvocationRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_apInvocationTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				apInvocationTable_del(StorageNew);
				apInvocationTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->apInvocationTable_old) == NULL)
				break;
			if (--StorageTmp->apInvocationTable_rsvs == 0)
				apInvocationTable_destroy(&StorageTmp->apInvocationTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				apInvocationTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_applicationEntityInvocationRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_applicationEntityInvocationRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct applicationEntityInvocationTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct applicationEntityInvocationTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 15;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("acseMIB", "write_applicationEntityInvocationRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(applicationEntityInvocationTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to applicationEntityInvocationRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to applicationEntityInvocationRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* applicationSubsystemId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* apInvocationId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[15]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* applicationSubsystemId */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index applicationSubsystemId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* apInvocationId */
			/* Note: ranges 1..32 */
			if (vp->val_len > SPRINT_MAX_LEN || ((1 > vp->val_len || vp->val_len > 32))) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index apInvocationId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = applicationEntityInvocationTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->applicationEntityInvocationTable_rsvs = 1;
			vp = vars;
			StorageNew->applicationSubsystemId = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			memdup((void *) &StorageNew->apInvocationId, vp->val.string, vp->val_len);
			StorageNew->apInvocationIdLen = vp->val_len;
			vp = vp->next_variable;
			header_complex_add_data(&applicationEntityInvocationTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->applicationEntityInvocationRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->applicationEntityInvocationTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->applicationEntityInvocationTable_old) == NULL)
				if (StorageTmp->applicationEntityInvocationTable_rsvs == 0)
					if ((StorageOld = StorageTmp->applicationEntityInvocationTable_old = applicationEntityInvocationTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->applicationEntityInvocationTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->applicationEntityInvocationTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				applicationEntityInvocationTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_applicationEntityInvocationTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
				/* check that activation is possible */
			if (StorageTmp->applicationEntityInvocationRowStatus != RS_ACTIVE)
				if ((ret = can_act_applicationEntityInvocationTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->applicationEntityInvocationRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_applicationEntityInvocationTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->applicationEntityInvocationRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_applicationEntityInvocationTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
				}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->applicationEntityInvocationRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here 
		   must be reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_applicationEntityInvocationTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->applicationEntityInvocationRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_applicationEntityInvocationTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->applicationEntityInvocationRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_applicationEntityInvocationTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_applicationEntityInvocationTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->applicationEntityInvocationRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->applicationEntityInvocationRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->applicationEntityInvocationRowStatus = set_value;
			if ((StorageOld = StorageTmp->applicationEntityInvocationTable_old) != NULL) {
				applicationEntityInvocationTable_destroy(&StorageTmp->applicationEntityInvocationTable_old);
				StorageTmp->applicationEntityInvocationTable_rsvs = 0;
				StorageTmp->applicationEntityInvocationTable_tsts = 0;
				StorageTmp->applicationEntityInvocationTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			applicationEntityInvocationTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_applicationEntityInvocationTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->applicationEntityInvocationRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_applicationEntityInvocationTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->applicationEntityInvocationRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_applicationEntityInvocationTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				applicationEntityInvocationTable_del(StorageNew);
				applicationEntityInvocationTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->applicationEntityInvocationTable_old) == NULL)
				break;
			if (--StorageTmp->applicationEntityInvocationTable_rsvs == 0)
				applicationEntityInvocationTable_destroy(&StorageTmp->applicationEntityInvocationTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				applicationEntityInvocationTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_asoRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_asoRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct asoTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct asoTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 15;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("acseMIB", "write_asoRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(asoTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to asoRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to asoRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* subsystemId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* applicationProcessId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* asoEntityId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* asoId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[15]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* subsystemId */
			/* Note: ranges 1..32 */
			if (vp->val_len > SPRINT_MAX_LEN || ((1 > vp->val_len || vp->val_len > 32))) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index subsystemId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* applicationProcessId */
			/* Note: ranges 1..32 */
			if (vp->val_len > SPRINT_MAX_LEN || ((1 > vp->val_len || vp->val_len > 32))) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index applicationProcessId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* asoEntityId */
			/* Note: ranges 1..32 */
			if (vp->val_len > SPRINT_MAX_LEN || ((1 > vp->val_len || vp->val_len > 32))) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index asoEntityId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* asoId */
			/* Note: ranges 1..32 */
			if (vp->val_len > SPRINT_MAX_LEN || ((1 > vp->val_len || vp->val_len > 32))) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index asoId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = asoTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->asoTable_rsvs = 1;
			vp = vars;
			memdup((void *) &StorageNew->subsystemId, vp->val.string, vp->val_len);
			StorageNew->subsystemIdLen = vp->val_len;
			vp = vp->next_variable;
			memdup((void *) &StorageNew->applicationProcessId, vp->val.string, vp->val_len);
			StorageNew->applicationProcessIdLen = vp->val_len;
			vp = vp->next_variable;
			memdup((void *) &StorageNew->asoEntityId, vp->val.string, vp->val_len);
			StorageNew->asoEntityIdLen = vp->val_len;
			vp = vp->next_variable;
			memdup((void *) &StorageNew->asoId, vp->val.string, vp->val_len);
			StorageNew->asoIdLen = vp->val_len;
			vp = vp->next_variable;
			header_complex_add_data(&asoTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->asoRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->asoTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->asoTable_old) == NULL)
				if (StorageTmp->asoTable_rsvs == 0)
					if ((StorageOld = StorageTmp->asoTable_old = asoTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->asoTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->asoTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				asoTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_asoTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
				/* check that activation is possible */
			if (StorageTmp->asoRowStatus != RS_ACTIVE)
				if ((ret = can_act_asoTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->asoRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_asoTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->asoRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_asoTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
				}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->asoRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in
		   the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_asoTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->asoRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_asoTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->asoRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_asoTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
			break;
			/* deactivate with underlying device */
			if (deactivate_asoTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->asoRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->asoRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->asoRowStatus = set_value;
			if ((StorageOld = StorageTmp->asoTable_old) != NULL) {
				asoTable_destroy(&StorageTmp->asoTable_old);
				StorageTmp->asoTable_rsvs = 0;
				StorageTmp->asoTable_tsts = 0;
				StorageTmp->asoTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			asoTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_asoTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->asoRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_asoTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->asoRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_asoTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				asoTable_del(StorageNew);
				asoTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->asoTable_old) == NULL)
				break;
			if (--StorageTmp->asoTable_rsvs == 0)
				asoTable_destroy(&StorageTmp->asoTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				asoTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_asoEntityRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_asoEntityRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct asoEntityTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct asoEntityTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 15;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("acseMIB", "write_asoEntityRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(asoEntityTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to asoEntityRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to asoEntityRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* subsystemId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* applicationProcessId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* asoEntityId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[15]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* subsystemId */
			/* Note: ranges 1..32 */
			if (vp->val_len > SPRINT_MAX_LEN || ((1 > vp->val_len || vp->val_len > 32))) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index subsystemId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* applicationProcessId */
			/* Note: ranges 1..32 */
			if (vp->val_len > SPRINT_MAX_LEN || ((1 > vp->val_len || vp->val_len > 32))) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index applicationProcessId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* asoEntityId */
			/* Note: ranges 1..32 */
			if (vp->val_len > SPRINT_MAX_LEN || ((1 > vp->val_len || vp->val_len > 32))) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index asoEntityId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = asoEntityTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->asoEntityTable_rsvs = 1;
			vp = vars;
			memdup((void *) &StorageNew->subsystemId, vp->val.string, vp->val_len);
			StorageNew->subsystemIdLen = vp->val_len;
			vp = vp->next_variable;
			memdup((void *) &StorageNew->applicationProcessId, vp->val.string, vp->val_len);
			StorageNew->applicationProcessIdLen = vp->val_len;
			vp = vp->next_variable;
			memdup((void *) &StorageNew->asoEntityId, vp->val.string, vp->val_len);
			StorageNew->asoEntityIdLen = vp->val_len;
			vp = vp->next_variable;
			header_complex_add_data(&asoEntityTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->asoEntityRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->asoEntityTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->asoEntityTable_old) == NULL)
				if (StorageTmp->asoEntityTable_rsvs == 0)
					if ((StorageOld = StorageTmp->asoEntityTable_old = asoEntityTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->asoEntityTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->asoEntityTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				asoEntityTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_asoEntityTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
				/* check that activation is possible */
			if (StorageTmp->asoEntityRowStatus != RS_ACTIVE)
				if ((ret = can_act_asoEntityTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->asoEntityRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_asoEntityTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->asoEntityRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_asoEntityTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
				}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->asoEntityRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here must be
		   reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_asoEntityTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->asoEntityRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_asoEntityTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->asoEntityRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_asoEntityTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_asoEntityTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->asoEntityRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->asoEntityRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->asoEntityRowStatus = set_value;
			if ((StorageOld = StorageTmp->asoEntityTable_old) != NULL) {
				asoEntityTable_destroy(&StorageTmp->asoEntityTable_old);
				StorageTmp->asoEntityTable_rsvs = 0;
				StorageTmp->asoEntityTable_tsts = 0;
				StorageTmp->asoEntityTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			asoEntityTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_asoEntityTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->asoEntityRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_asoEntityTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->asoEntityRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_asoEntityTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				asoEntityTable_del(StorageNew);
				asoEntityTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->asoEntityTable_old) == NULL)
				break;
			if (--StorageTmp->asoEntityTable_rsvs == 0)
				asoEntityTable_destroy(&StorageTmp->asoEntityTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				asoEntityTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_asoInvocationRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_asoInvocationRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct asoInvocationTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct asoInvocationTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 15;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("acseMIB", "write_asoInvocationRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(asoInvocationTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to asoInvocationRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to asoInvocationRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* applicationSubsystemId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* apInvocationId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* asoInvocationId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[15]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* applicationSubsystemId */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index applicationSubsystemId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* apInvocationId */
			/* Note: ranges 1..32 */
			if (vp->val_len > SPRINT_MAX_LEN || ((1 > vp->val_len || vp->val_len > 32))) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index apInvocationId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* asoInvocationId */
			/* Note: ranges 1..32 */
			if (vp->val_len > SPRINT_MAX_LEN || ((1 > vp->val_len || vp->val_len > 32))) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index asoInvocationId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = asoInvocationTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->asoInvocationTable_rsvs = 1;
			vp = vars;
			StorageNew->applicationSubsystemId = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			memdup((void *) &StorageNew->apInvocationId, vp->val.string, vp->val_len);
			StorageNew->apInvocationIdLen = vp->val_len;
			vp = vp->next_variable;
			memdup((void *) &StorageNew->asoInvocationId, vp->val.string, vp->val_len);
			StorageNew->asoInvocationIdLen = vp->val_len;
			vp = vp->next_variable;
			header_complex_add_data(&asoInvocationTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->asoInvocationRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->asoInvocationTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->asoInvocationTable_old) == NULL)
				if (StorageTmp->asoInvocationTable_rsvs == 0)
					if ((StorageOld = StorageTmp->asoInvocationTable_old = asoInvocationTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->asoInvocationTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->asoInvocationTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				asoInvocationTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_asoInvocationTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
				/* check that activation is possible */
			if (StorageTmp->asoInvocationRowStatus != RS_ACTIVE)
				if ((ret = can_act_asoInvocationTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->asoInvocationRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_asoInvocationTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->asoInvocationRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_asoInvocationTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
				}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->asoInvocationRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here must be
		   reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_asoInvocationTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->asoInvocationRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_asoInvocationTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->asoInvocationRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_asoInvocationTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_asoInvocationTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->asoInvocationRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->asoInvocationRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->asoInvocationRowStatus = set_value;
			if ((StorageOld = StorageTmp->asoInvocationTable_old) != NULL) {
				asoInvocationTable_destroy(&StorageTmp->asoInvocationTable_old);
				StorageTmp->asoInvocationTable_rsvs = 0;
				StorageTmp->asoInvocationTable_tsts = 0;
				StorageTmp->asoInvocationTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			asoInvocationTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_asoInvocationTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->asoInvocationRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_asoInvocationTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->asoInvocationRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_asoInvocationTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				asoInvocationTable_del(StorageNew);
				asoInvocationTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->asoInvocationTable_old) == NULL)
				break;
			if (--StorageTmp->asoInvocationTable_rsvs == 0)
				asoInvocationTable_destroy(&StorageTmp->asoInvocationTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				asoInvocationTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_monoULConnectionRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_monoULConnectionRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct monoULConnectionTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct monoULConnectionTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 15;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("acseMIB", "write_monoULConnectionRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(monoULConnectionTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to monoULConnectionRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to monoULConnectionRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* applicationSubsystemId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* apInvocationId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[15]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* applicationSubsystemId */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index applicationSubsystemId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* apInvocationId */
			/* Note: ranges 1..32 */
			if (vp->val_len > SPRINT_MAX_LEN || ((1 > vp->val_len || vp->val_len > 32))) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index apInvocationId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = monoULConnectionTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->monoULConnectionTable_rsvs = 1;
			vp = vars;
			StorageNew->applicationSubsystemId = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			memdup((void *) &StorageNew->apInvocationId, vp->val.string, vp->val_len);
			StorageNew->apInvocationIdLen = vp->val_len;
			vp = vp->next_variable;
			header_complex_add_data(&monoULConnectionTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->monoULConnectionRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->monoULConnectionTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->monoULConnectionTable_old) == NULL)
				if (StorageTmp->monoULConnectionTable_rsvs == 0)
					if ((StorageOld = StorageTmp->monoULConnectionTable_old = monoULConnectionTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->monoULConnectionTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->monoULConnectionTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				monoULConnectionTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_monoULConnectionTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
				/* check that activation is possible */
			if (StorageTmp->monoULConnectionRowStatus != RS_ACTIVE)
				if ((ret = can_act_monoULConnectionTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->monoULConnectionRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_monoULConnectionTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->monoULConnectionRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_monoULConnectionTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
				}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->monoULConnectionRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here must be
		   reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_monoULConnectionTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->monoULConnectionRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_monoULConnectionTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->monoULConnectionRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_monoULConnectionTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_monoULConnectionTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->monoULConnectionRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->monoULConnectionRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->monoULConnectionRowStatus = set_value;
			if ((StorageOld = StorageTmp->monoULConnectionTable_old) != NULL) {
				monoULConnectionTable_destroy(&StorageTmp->monoULConnectionTable_old);
				StorageTmp->monoULConnectionTable_rsvs = 0;
				StorageTmp->monoULConnectionTable_tsts = 0;
				StorageTmp->monoULConnectionTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			monoULConnectionTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_monoULConnectionTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->monoULConnectionRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_monoULConnectionTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->monoULConnectionRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_monoULConnectionTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				monoULConnectionTable_del(StorageNew);
				monoULConnectionTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->monoULConnectionTable_old) == NULL)
				break;
			if (--StorageTmp->monoULConnectionTable_rsvs == 0)
				monoULConnectionTable_destroy(&StorageTmp->monoULConnectionTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				monoULConnectionTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_monolithicUpperLayerSubssytemRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_monolithicUpperLayerSubssytemRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct monolithicUpperLayerSubsystemTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct monolithicUpperLayerSubsystemTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 15;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("acseMIB", "write_monolithicUpperLayerSubssytemRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(monolithicUpperLayerSubsystemTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to monolithicUpperLayerSubssytemRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to monolithicUpperLayerSubssytemRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* monolithicUpperLayerSubsystemId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[15]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* monolithicUpperLayerSubsystemId */
			/* Note: ranges 1..32 */
			if (vp->val_len > SPRINT_MAX_LEN || ((1 > vp->val_len || vp->val_len > 32))) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index monolithicUpperLayerSubsystemId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = monolithicUpperLayerSubsystemTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->monolithicUpperLayerSubsystemTable_rsvs = 1;
			vp = vars;
			memdup((void *) &StorageNew->monolithicUpperLayerSubsystemId, vp->val.string, vp->val_len);
			StorageNew->monolithicUpperLayerSubsystemIdLen = vp->val_len;
			vp = vp->next_variable;
			header_complex_add_data(&monolithicUpperLayerSubsystemTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->monolithicUpperLayerSubssytemRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->monolithicUpperLayerSubsystemTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->monolithicUpperLayerSubsystemTable_old) == NULL)
				if (StorageTmp->monolithicUpperLayerSubsystemTable_rsvs == 0)
					if ((StorageOld = StorageTmp->monolithicUpperLayerSubsystemTable_old = monolithicUpperLayerSubsystemTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->monolithicUpperLayerSubsystemTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->monolithicUpperLayerSubsystemTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				monolithicUpperLayerSubsystemTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_monolithicUpperLayerSubsystemTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
				/* check that activation is possible */
			if (StorageTmp->monolithicUpperLayerSubssytemRowStatus != RS_ACTIVE)
				if ((ret = can_act_monolithicUpperLayerSubsystemTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->monolithicUpperLayerSubssytemRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_monolithicUpperLayerSubsystemTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->monolithicUpperLayerSubssytemRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_monolithicUpperLayerSubsystemTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
				}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->monolithicUpperLayerSubssytemRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done
		   here must be reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_monolithicUpperLayerSubsystemTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->monolithicUpperLayerSubssytemRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_monolithicUpperLayerSubsystemTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->monolithicUpperLayerSubssytemRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_monolithicUpperLayerSubsystemTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
			break;
			/* deactivate with underlying device */
			if (deactivate_monolithicUpperLayerSubsystemTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->monolithicUpperLayerSubssytemRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->monolithicUpperLayerSubssytemRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->monolithicUpperLayerSubssytemRowStatus = set_value;
			if ((StorageOld = StorageTmp->monolithicUpperLayerSubsystemTable_old) != NULL) {
				monolithicUpperLayerSubsystemTable_destroy(&StorageTmp->monolithicUpperLayerSubsystemTable_old);
				StorageTmp->monolithicUpperLayerSubsystemTable_rsvs = 0;
				StorageTmp->monolithicUpperLayerSubsystemTable_tsts = 0;
				StorageTmp->monolithicUpperLayerSubsystemTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			monolithicUpperLayerSubsystemTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_monolithicUpperLayerSubsystemTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->monolithicUpperLayerSubssytemRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_monolithicUpperLayerSubsystemTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->monolithicUpperLayerSubssytemRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_monolithicUpperLayerSubsystemTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				monolithicUpperLayerSubsystemTable_del(StorageNew);
				monolithicUpperLayerSubsystemTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->monolithicUpperLayerSubsystemTable_old) == NULL)
				break;
			if (--StorageTmp->monolithicUpperLayerSubsystemTable_rsvs == 0)
				monolithicUpperLayerSubsystemTable_destroy(&StorageTmp->monolithicUpperLayerSubsystemTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				monolithicUpperLayerSubsystemTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_pSapRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_pSapRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct pSapTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct pSapTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 15;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("acseMIB", "write_pSapRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(pSapTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to pSapRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to pSapRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* subsystemId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[15]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* subsystemId */
			/* Note: ranges 1..32 */
			if (vp->val_len > SPRINT_MAX_LEN || ((1 > vp->val_len || vp->val_len > 32))) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index subsystemId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = pSapTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->pSapTable_rsvs = 1;
			vp = vars;
			memdup((void *) &StorageNew->subsystemId, vp->val.string, vp->val_len);
			StorageNew->subsystemIdLen = vp->val_len;
			vp = vp->next_variable;
			header_complex_add_data(&pSapTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->pSapRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->pSapTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->pSapTable_old) == NULL)
				if (StorageTmp->pSapTable_rsvs == 0)
					if ((StorageOld = StorageTmp->pSapTable_old = pSapTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->pSapTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->pSapTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				pSapTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_pSapTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
				/* check that activation is possible */
			if (StorageTmp->pSapRowStatus != RS_ACTIVE)
				if ((ret = can_act_pSapTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->pSapRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_pSapTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->pSapRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_pSapTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
				}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->pSapRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in
		   the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_pSapTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->pSapRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_pSapTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->pSapRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_pSapTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_pSapTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->pSapRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->pSapRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->pSapRowStatus = set_value;
			if ((StorageOld = StorageTmp->pSapTable_old) != NULL) {
				pSapTable_destroy(&StorageTmp->pSapTable_old);
				StorageTmp->pSapTable_rsvs = 0;
				StorageTmp->pSapTable_tsts = 0;
				StorageTmp->pSapTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			pSapTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_pSapTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->pSapRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_pSapTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->pSapRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_pSapTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				pSapTable_del(StorageNew);
				pSapTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->pSapTable_old) == NULL)
				break;
			if (--StorageTmp->pSapTable_rsvs == 0)
				pSapTable_destroy(&StorageTmp->pSapTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				pSapTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_presentationCOnnectionRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_presentationCOnnectionRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct presentationConnectionTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct presentationConnectionTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 15;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("acseMIB", "write_presentationCOnnectionRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(presentationConnectionTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to presentationCOnnectionRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to presentationCOnnectionRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* presentationSubsystemId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* presentationEntityId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* presentationCopmId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* presentationConnectionId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[15]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* presentationSubsystemId */
			/* Note: ranges 1..32 */
			if (vp->val_len > SPRINT_MAX_LEN || ((1 > vp->val_len || vp->val_len > 32))) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index presentationSubsystemId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* presentationEntityId */
			/* Note: ranges 1..32 */
			if (vp->val_len > SPRINT_MAX_LEN || ((1 > vp->val_len || vp->val_len > 32))) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index presentationEntityId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* presentationCopmId */
			/* Note: ranges 1..32 */
			if (vp->val_len > SPRINT_MAX_LEN || ((1 > vp->val_len || vp->val_len > 32))) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index presentationCopmId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* presentationConnectionId */
			/* Note: ranges 1..32 */
			if (vp->val_len > SPRINT_MAX_LEN || ((1 > vp->val_len || vp->val_len > 32))) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index presentationConnectionId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = presentationConnectionTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->presentationConnectionTable_rsvs = 1;
			vp = vars;
			memdup((void *) &StorageNew->presentationSubsystemId, vp->val.string, vp->val_len);
			StorageNew->presentationSubsystemIdLen = vp->val_len;
			vp = vp->next_variable;
			memdup((void *) &StorageNew->presentationEntityId, vp->val.string, vp->val_len);
			StorageNew->presentationEntityIdLen = vp->val_len;
			vp = vp->next_variable;
			memdup((void *) &StorageNew->presentationCopmId, vp->val.string, vp->val_len);
			StorageNew->presentationCopmIdLen = vp->val_len;
			vp = vp->next_variable;
			memdup((void *) &StorageNew->presentationConnectionId, vp->val.string, vp->val_len);
			StorageNew->presentationConnectionIdLen = vp->val_len;
			vp = vp->next_variable;
			header_complex_add_data(&presentationConnectionTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->presentationCOnnectionRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->presentationConnectionTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->presentationConnectionTable_old) == NULL)
				if (StorageTmp->presentationConnectionTable_rsvs == 0)
					if ((StorageOld = StorageTmp->presentationConnectionTable_old = presentationConnectionTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->presentationConnectionTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->presentationConnectionTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				presentationConnectionTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_presentationConnectionTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
				/* check that activation is possible */
			if (StorageTmp->presentationCOnnectionRowStatus != RS_ACTIVE)
				if ((ret = can_act_presentationConnectionTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->presentationCOnnectionRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_presentationConnectionTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->presentationCOnnectionRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_presentationConnectionTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
				}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->presentationCOnnectionRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here must 
		   be reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_presentationConnectionTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->presentationCOnnectionRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_presentationConnectionTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->presentationCOnnectionRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_presentationConnectionTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_presentationConnectionTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->presentationCOnnectionRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->presentationCOnnectionRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->presentationCOnnectionRowStatus = set_value;
			if ((StorageOld = StorageTmp->presentationConnectionTable_old) != NULL) {
				presentationConnectionTable_destroy(&StorageTmp->presentationConnectionTable_old);
				StorageTmp->presentationConnectionTable_rsvs = 0;
				StorageTmp->presentationConnectionTable_tsts = 0;
				StorageTmp->presentationConnectionTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			presentationConnectionTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_presentationConnectionTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->presentationCOnnectionRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_presentationConnectionTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->presentationCOnnectionRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_presentationConnectionTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				presentationConnectionTable_del(StorageNew);
				presentationConnectionTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->presentationConnectionTable_old) == NULL)
				break;
			if (--StorageTmp->presentationConnectionTable_rsvs == 0)
				presentationConnectionTable_destroy(&StorageTmp->presentationConnectionTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				presentationConnectionTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_presentationCopmRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_presentationCopmRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct presentationCopmTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct presentationCopmTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 15;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("acseMIB", "write_presentationCopmRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(presentationCopmTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to presentationCopmRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to presentationCopmRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* presentationSubsystemId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* presentationEntityId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* presentationCopmId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[15]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* presentationSubsystemId */
			/* Note: ranges 1..32 */
			if (vp->val_len > SPRINT_MAX_LEN || ((1 > vp->val_len || vp->val_len > 32))) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index presentationSubsystemId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* presentationEntityId */
			/* Note: ranges 1..32 */
			if (vp->val_len > SPRINT_MAX_LEN || ((1 > vp->val_len || vp->val_len > 32))) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index presentationEntityId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* presentationCopmId */
			/* Note: ranges 1..32 */
			if (vp->val_len > SPRINT_MAX_LEN || ((1 > vp->val_len || vp->val_len > 32))) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index presentationCopmId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = presentationCopmTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->presentationCopmTable_rsvs = 1;
			vp = vars;
			memdup((void *) &StorageNew->presentationSubsystemId, vp->val.string, vp->val_len);
			StorageNew->presentationSubsystemIdLen = vp->val_len;
			vp = vp->next_variable;
			memdup((void *) &StorageNew->presentationEntityId, vp->val.string, vp->val_len);
			StorageNew->presentationEntityIdLen = vp->val_len;
			vp = vp->next_variable;
			memdup((void *) &StorageNew->presentationCopmId, vp->val.string, vp->val_len);
			StorageNew->presentationCopmIdLen = vp->val_len;
			vp = vp->next_variable;
			header_complex_add_data(&presentationCopmTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->presentationCopmRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->presentationCopmTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->presentationCopmTable_old) == NULL)
				if (StorageTmp->presentationCopmTable_rsvs == 0)
					if ((StorageOld = StorageTmp->presentationCopmTable_old = presentationCopmTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->presentationCopmTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->presentationCopmTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				presentationCopmTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_presentationCopmTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
				/* check that activation is possible */
			if (StorageTmp->presentationCopmRowStatus != RS_ACTIVE)
				if ((ret = can_act_presentationCopmTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->presentationCopmRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_presentationCopmTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->presentationCopmRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_presentationCopmTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
				}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->presentationCopmRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here must be
		   reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_presentationCopmTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->presentationCopmRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_presentationCopmTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->presentationCopmRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_presentationCopmTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_presentationCopmTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->presentationCopmRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->presentationCopmRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->presentationCopmRowStatus = set_value;
			if ((StorageOld = StorageTmp->presentationCopmTable_old) != NULL) {
				presentationCopmTable_destroy(&StorageTmp->presentationCopmTable_old);
				StorageTmp->presentationCopmTable_rsvs = 0;
				StorageTmp->presentationCopmTable_tsts = 0;
				StorageTmp->presentationCopmTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			presentationCopmTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_presentationCopmTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->presentationCopmRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_presentationCopmTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->presentationCopmRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_presentationCopmTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				presentationCopmTable_del(StorageNew);
				presentationCopmTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->presentationCopmTable_old) == NULL)
				break;
			if (--StorageTmp->presentationCopmTable_rsvs == 0)
				presentationCopmTable_destroy(&StorageTmp->presentationCopmTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				presentationCopmTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_presentationEntityRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_presentationEntityRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct presentationEntityTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct presentationEntityTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 15;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("acseMIB", "write_presentationEntityRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(presentationEntityTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to presentationEntityRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to presentationEntityRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* presentationSubsystemId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* presentationEntityId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[15]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* presentationSubsystemId */
			/* Note: ranges 1..32 */
			if (vp->val_len > SPRINT_MAX_LEN || ((1 > vp->val_len || vp->val_len > 32))) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index presentationSubsystemId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* presentationEntityId */
			/* Note: ranges 1..32 */
			if (vp->val_len > SPRINT_MAX_LEN || ((1 > vp->val_len || vp->val_len > 32))) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index presentationEntityId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = presentationEntityTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->presentationEntityTable_rsvs = 1;
			vp = vars;
			memdup((void *) &StorageNew->presentationSubsystemId, vp->val.string, vp->val_len);
			StorageNew->presentationSubsystemIdLen = vp->val_len;
			vp = vp->next_variable;
			memdup((void *) &StorageNew->presentationEntityId, vp->val.string, vp->val_len);
			StorageNew->presentationEntityIdLen = vp->val_len;
			vp = vp->next_variable;
			header_complex_add_data(&presentationEntityTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->presentationEntityRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->presentationEntityTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->presentationEntityTable_old) == NULL)
				if (StorageTmp->presentationEntityTable_rsvs == 0)
					if ((StorageOld = StorageTmp->presentationEntityTable_old = presentationEntityTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->presentationEntityTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->presentationEntityTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				presentationEntityTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_presentationEntityTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
				/* check that activation is possible */
			if (StorageTmp->presentationEntityRowStatus != RS_ACTIVE)
				if ((ret = can_act_presentationEntityTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->presentationEntityRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_presentationEntityTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->presentationEntityRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_presentationEntityTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
				}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->presentationEntityRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here must be
		   reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_presentationEntityTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->presentationEntityRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_presentationEntityTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->presentationEntityRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_presentationEntityTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_presentationEntityTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->presentationEntityRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->presentationEntityRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->presentationEntityRowStatus = set_value;
			if ((StorageOld = StorageTmp->presentationEntityTable_old) != NULL) {
				presentationEntityTable_destroy(&StorageTmp->presentationEntityTable_old);
				StorageTmp->presentationEntityTable_rsvs = 0;
				StorageTmp->presentationEntityTable_tsts = 0;
				StorageTmp->presentationEntityTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			presentationEntityTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_presentationEntityTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->presentationEntityRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_presentationEntityTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->presentationEntityRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_presentationEntityTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				presentationEntityTable_del(StorageNew);
				presentationEntityTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->presentationEntityTable_old) == NULL)
				break;
			if (--StorageTmp->presentationEntityTable_rsvs == 0)
				presentationEntityTable_destroy(&StorageTmp->presentationEntityTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				presentationEntityTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_presentationSubsystemRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_presentationSubsystemRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct presentationSubsystemTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct presentationSubsystemTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 15;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("acseMIB", "write_presentationSubsystemRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(presentationSubsystemTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to presentationSubsystemRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to presentationSubsystemRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* presentationSubsystemId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[15]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* presentationSubsystemId */
			/* Note: ranges 1..32 */
			if (vp->val_len > SPRINT_MAX_LEN || ((1 > vp->val_len || vp->val_len > 32))) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index presentationSubsystemId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = presentationSubsystemTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->presentationSubsystemTable_rsvs = 1;
			vp = vars;
			memdup((void *) &StorageNew->presentationSubsystemId, vp->val.string, vp->val_len);
			StorageNew->presentationSubsystemIdLen = vp->val_len;
			vp = vp->next_variable;
			header_complex_add_data(&presentationSubsystemTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->presentationSubsystemRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->presentationSubsystemTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->presentationSubsystemTable_old) == NULL)
				if (StorageTmp->presentationSubsystemTable_rsvs == 0)
					if ((StorageOld = StorageTmp->presentationSubsystemTable_old = presentationSubsystemTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->presentationSubsystemTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->presentationSubsystemTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				presentationSubsystemTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_presentationSubsystemTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
				/* check that activation is possible */
			if (StorageTmp->presentationSubsystemRowStatus != RS_ACTIVE)
				if ((ret = can_act_presentationSubsystemTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->presentationSubsystemRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_presentationSubsystemTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->presentationSubsystemRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_presentationSubsystemTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
				}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->presentationSubsystemRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here must
		   be reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_presentationSubsystemTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->presentationSubsystemRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_presentationSubsystemTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->presentationSubsystemRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_presentationSubsystemTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_presentationSubsystemTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->presentationSubsystemRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->presentationSubsystemRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->presentationSubsystemRowStatus = set_value;
			if ((StorageOld = StorageTmp->presentationSubsystemTable_old) != NULL) {
				presentationSubsystemTable_destroy(&StorageTmp->presentationSubsystemTable_old);
				StorageTmp->presentationSubsystemTable_rsvs = 0;
				StorageTmp->presentationSubsystemTable_tsts = 0;
				StorageTmp->presentationSubsystemTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			presentationSubsystemTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_presentationSubsystemTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->presentationSubsystemRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_presentationSubsystemTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->presentationSubsystemRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_presentationSubsystemTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				presentationSubsystemTable_del(StorageNew);
				presentationSubsystemTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->presentationSubsystemTable_old) == NULL)
				break;
			if (--StorageTmp->presentationSubsystemTable_rsvs == 0)
				presentationSubsystemTable_destroy(&StorageTmp->presentationSubsystemTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				presentationSubsystemTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_sSapRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_sSapRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct sSapTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct sSapTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 15;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("acseMIB", "write_sSapRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(sSapTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to sSapRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to sSapRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* subsystemId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[15]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* subsystemId */
			/* Note: ranges 1..32 */
			if (vp->val_len > SPRINT_MAX_LEN || ((1 > vp->val_len || vp->val_len > 32))) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index subsystemId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = sSapTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->sSapTable_rsvs = 1;
			vp = vars;
			memdup((void *) &StorageNew->subsystemId, vp->val.string, vp->val_len);
			StorageNew->subsystemIdLen = vp->val_len;
			vp = vp->next_variable;
			header_complex_add_data(&sSapTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->sSapRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->sSapTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->sSapTable_old) == NULL)
				if (StorageTmp->sSapTable_rsvs == 0)
					if ((StorageOld = StorageTmp->sSapTable_old = sSapTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->sSapTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->sSapTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				sSapTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_sSapTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
				/* check that activation is possible */
			if (StorageTmp->sSapRowStatus != RS_ACTIVE)
				if ((ret = can_act_sSapTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->sSapRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_sSapTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->sSapRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_sSapTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
				}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->sSapRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in
		   the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_sSapTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->sSapRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_sSapTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->sSapRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_sSapTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
			break;
			/* deactivate with underlying device */
			if (deactivate_sSapTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->sSapRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->sSapRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->sSapRowStatus = set_value;
			if ((StorageOld = StorageTmp->sSapTable_old) != NULL) {
				sSapTable_destroy(&StorageTmp->sSapTable_old);
				StorageTmp->sSapTable_rsvs = 0;
				StorageTmp->sSapTable_tsts = 0;
				StorageTmp->sSapTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			sSapTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_sSapTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->sSapRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_sSapTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->sSapRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_sSapTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				sSapTable_del(StorageNew);
				sSapTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->sSapTable_old) == NULL)
				break;
			if (--StorageTmp->sSapTable_rsvs == 0)
				sSapTable_destroy(&StorageTmp->sSapTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				sSapTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_sessionCOnnectionRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_sessionCOnnectionRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct sessionConnectionTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct sessionConnectionTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 15;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("acseMIB", "write_sessionCOnnectionRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(sessionConnectionTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to sessionCOnnectionRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to sessionCOnnectionRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* sessionSubsystemId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* sessionEntityId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* sessionCopmId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* sessionConnectionId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[15]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* sessionSubsystemId */
			/* Note: ranges 1..32 */
			if (vp->val_len > SPRINT_MAX_LEN || ((1 > vp->val_len || vp->val_len > 32))) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index sessionSubsystemId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* sessionEntityId */
			/* Note: ranges 1..32 */
			if (vp->val_len > SPRINT_MAX_LEN || ((1 > vp->val_len || vp->val_len > 32))) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index sessionEntityId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* sessionCopmId */
			/* Note: ranges 1..32 */
			if (vp->val_len > SPRINT_MAX_LEN || ((1 > vp->val_len || vp->val_len > 32))) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index sessionCopmId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* sessionConnectionId */
			/* Note: ranges 1..32 */
			if (vp->val_len > SPRINT_MAX_LEN || ((1 > vp->val_len || vp->val_len > 32))) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index sessionConnectionId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = sessionConnectionTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->sessionConnectionTable_rsvs = 1;
			vp = vars;
			memdup((void *) &StorageNew->sessionSubsystemId, vp->val.string, vp->val_len);
			StorageNew->sessionSubsystemIdLen = vp->val_len;
			vp = vp->next_variable;
			memdup((void *) &StorageNew->sessionEntityId, vp->val.string, vp->val_len);
			StorageNew->sessionEntityIdLen = vp->val_len;
			vp = vp->next_variable;
			memdup((void *) &StorageNew->sessionCopmId, vp->val.string, vp->val_len);
			StorageNew->sessionCopmIdLen = vp->val_len;
			vp = vp->next_variable;
			memdup((void *) &StorageNew->sessionConnectionId, vp->val.string, vp->val_len);
			StorageNew->sessionConnectionIdLen = vp->val_len;
			vp = vp->next_variable;
			header_complex_add_data(&sessionConnectionTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->sessionCOnnectionRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->sessionConnectionTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->sessionConnectionTable_old) == NULL)
				if (StorageTmp->sessionConnectionTable_rsvs == 0)
					if ((StorageOld = StorageTmp->sessionConnectionTable_old = sessionConnectionTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->sessionConnectionTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->sessionConnectionTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				sessionConnectionTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_sessionConnectionTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
				/* check that activation is possible */
			if (StorageTmp->sessionCOnnectionRowStatus != RS_ACTIVE)
				if ((ret = can_act_sessionConnectionTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->sessionCOnnectionRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_sessionConnectionTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->sessionCOnnectionRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_sessionConnectionTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
				}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->sessionCOnnectionRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here must be
		   reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_sessionConnectionTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->sessionCOnnectionRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_sessionConnectionTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->sessionCOnnectionRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_sessionConnectionTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_sessionConnectionTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->sessionCOnnectionRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->sessionCOnnectionRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->sessionCOnnectionRowStatus = set_value;
			if ((StorageOld = StorageTmp->sessionConnectionTable_old) != NULL) {
				sessionConnectionTable_destroy(&StorageTmp->sessionConnectionTable_old);
				StorageTmp->sessionConnectionTable_rsvs = 0;
				StorageTmp->sessionConnectionTable_tsts = 0;
				StorageTmp->sessionConnectionTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			sessionConnectionTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_sessionConnectionTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->sessionCOnnectionRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_sessionConnectionTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->sessionCOnnectionRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_sessionConnectionTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				sessionConnectionTable_del(StorageNew);
				sessionConnectionTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->sessionConnectionTable_old) == NULL)
				break;
			if (--StorageTmp->sessionConnectionTable_rsvs == 0)
				sessionConnectionTable_destroy(&StorageTmp->sessionConnectionTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				sessionConnectionTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_sessionCopmRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_sessionCopmRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct sessionCopmTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct sessionCopmTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 15;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("acseMIB", "write_sessionCopmRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(sessionCopmTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to sessionCopmRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to sessionCopmRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* sessionSubsystemId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* sessionEntityId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* sessionCopmId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[15]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* sessionSubsystemId */
			/* Note: ranges 1..32 */
			if (vp->val_len > SPRINT_MAX_LEN || ((1 > vp->val_len || vp->val_len > 32))) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index sessionSubsystemId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* sessionEntityId */
			/* Note: ranges 1..32 */
			if (vp->val_len > SPRINT_MAX_LEN || ((1 > vp->val_len || vp->val_len > 32))) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index sessionEntityId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* sessionCopmId */
			/* Note: ranges 1..32 */
			if (vp->val_len > SPRINT_MAX_LEN || ((1 > vp->val_len || vp->val_len > 32))) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index sessionCopmId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = sessionCopmTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->sessionCopmTable_rsvs = 1;
			vp = vars;
			memdup((void *) &StorageNew->sessionSubsystemId, vp->val.string, vp->val_len);
			StorageNew->sessionSubsystemIdLen = vp->val_len;
			vp = vp->next_variable;
			memdup((void *) &StorageNew->sessionEntityId, vp->val.string, vp->val_len);
			StorageNew->sessionEntityIdLen = vp->val_len;
			vp = vp->next_variable;
			memdup((void *) &StorageNew->sessionCopmId, vp->val.string, vp->val_len);
			StorageNew->sessionCopmIdLen = vp->val_len;
			vp = vp->next_variable;
			header_complex_add_data(&sessionCopmTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->sessionCopmRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->sessionCopmTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->sessionCopmTable_old) == NULL)
				if (StorageTmp->sessionCopmTable_rsvs == 0)
					if ((StorageOld = StorageTmp->sessionCopmTable_old = sessionCopmTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->sessionCopmTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->sessionCopmTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				sessionCopmTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_sessionCopmTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
				/* check that activation is possible */
			if (StorageTmp->sessionCopmRowStatus != RS_ACTIVE)
				if ((ret = can_act_sessionCopmTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->sessionCopmRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_sessionCopmTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->sessionCopmRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_sessionCopmTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
				}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->sessionCopmRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here must be
		   reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_sessionCopmTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->sessionCopmRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_sessionCopmTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->sessionCopmRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_sessionCopmTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_sessionCopmTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->sessionCopmRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->sessionCopmRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->sessionCopmRowStatus = set_value;
			if ((StorageOld = StorageTmp->sessionCopmTable_old) != NULL) {
				sessionCopmTable_destroy(&StorageTmp->sessionCopmTable_old);
				StorageTmp->sessionCopmTable_rsvs = 0;
				StorageTmp->sessionCopmTable_tsts = 0;
				StorageTmp->sessionCopmTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			sessionCopmTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_sessionCopmTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->sessionCopmRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_sessionCopmTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->sessionCopmRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_sessionCopmTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				sessionCopmTable_del(StorageNew);
				sessionCopmTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->sessionCopmTable_old) == NULL)
				break;
			if (--StorageTmp->sessionCopmTable_rsvs == 0)
				sessionCopmTable_destroy(&StorageTmp->sessionCopmTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				sessionCopmTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_sessionEntityRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_sessionEntityRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct sessionEntityTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct sessionEntityTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 15;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("acseMIB", "write_sessionEntityRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(sessionEntityTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to sessionEntityRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to sessionEntityRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* sessionSubsystemId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* sessionEntityId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[15]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* sessionSubsystemId */
			/* Note: ranges 1..32 */
			if (vp->val_len > SPRINT_MAX_LEN || ((1 > vp->val_len || vp->val_len > 32))) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index sessionSubsystemId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* sessionEntityId */
			/* Note: ranges 1..32 */
			if (vp->val_len > SPRINT_MAX_LEN || ((1 > vp->val_len || vp->val_len > 32))) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index sessionEntityId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = sessionEntityTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->sessionEntityTable_rsvs = 1;
			vp = vars;
			memdup((void *) &StorageNew->sessionSubsystemId, vp->val.string, vp->val_len);
			StorageNew->sessionSubsystemIdLen = vp->val_len;
			vp = vp->next_variable;
			memdup((void *) &StorageNew->sessionEntityId, vp->val.string, vp->val_len);
			StorageNew->sessionEntityIdLen = vp->val_len;
			vp = vp->next_variable;
			header_complex_add_data(&sessionEntityTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->sessionEntityRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->sessionEntityTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->sessionEntityTable_old) == NULL)
				if (StorageTmp->sessionEntityTable_rsvs == 0)
					if ((StorageOld = StorageTmp->sessionEntityTable_old = sessionEntityTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->sessionEntityTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->sessionEntityTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				sessionEntityTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_sessionEntityTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
				/* check that activation is possible */
			if (StorageTmp->sessionEntityRowStatus != RS_ACTIVE)
				if ((ret = can_act_sessionEntityTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->sessionEntityRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_sessionEntityTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->sessionEntityRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_sessionEntityTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
				}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->sessionEntityRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here must be
		   reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_sessionEntityTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->sessionEntityRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_sessionEntityTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->sessionEntityRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_sessionEntityTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_sessionEntityTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->sessionEntityRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->sessionEntityRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->sessionEntityRowStatus = set_value;
			if ((StorageOld = StorageTmp->sessionEntityTable_old) != NULL) {
				sessionEntityTable_destroy(&StorageTmp->sessionEntityTable_old);
				StorageTmp->sessionEntityTable_rsvs = 0;
				StorageTmp->sessionEntityTable_tsts = 0;
				StorageTmp->sessionEntityTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			sessionEntityTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_sessionEntityTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->sessionEntityRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_sessionEntityTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->sessionEntityRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_sessionEntityTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				sessionEntityTable_del(StorageNew);
				sessionEntityTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->sessionEntityTable_old) == NULL)
				break;
			if (--StorageTmp->sessionEntityTable_rsvs == 0)
				sessionEntityTable_destroy(&StorageTmp->sessionEntityTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				sessionEntityTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_sessionSubsystemRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_sessionSubsystemRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct sessionSubsystemTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct sessionSubsystemTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 15;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("acseMIB", "write_sessionSubsystemRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(sessionSubsystemTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to sessionSubsystemRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to sessionSubsystemRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* sessionSubsystemId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[15]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* sessionSubsystemId */
			/* Note: ranges 1..32 */
			if (vp->val_len > SPRINT_MAX_LEN || ((1 > vp->val_len || vp->val_len > 32))) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index sessionSubsystemId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = sessionSubsystemTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->sessionSubsystemTable_rsvs = 1;
			vp = vars;
			memdup((void *) &StorageNew->sessionSubsystemId, vp->val.string, vp->val_len);
			StorageNew->sessionSubsystemIdLen = vp->val_len;
			vp = vp->next_variable;
			header_complex_add_data(&sessionSubsystemTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->sessionSubsystemRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->sessionSubsystemTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->sessionSubsystemTable_old) == NULL)
				if (StorageTmp->sessionSubsystemTable_rsvs == 0)
					if ((StorageOld = StorageTmp->sessionSubsystemTable_old = sessionSubsystemTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->sessionSubsystemTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->sessionSubsystemTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				sessionSubsystemTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_sessionSubsystemTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
				/* check that activation is possible */
			if (StorageTmp->sessionSubsystemRowStatus != RS_ACTIVE)
				if ((ret = can_act_sessionSubsystemTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->sessionSubsystemRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_sessionSubsystemTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->sessionSubsystemRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_sessionSubsystemTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
				}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->sessionSubsystemRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here must be
		   reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_sessionSubsystemTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->sessionSubsystemRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_sessionSubsystemTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->sessionSubsystemRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_sessionSubsystemTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
			break;
			/* deactivate with underlying device */
			if (deactivate_sessionSubsystemTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->sessionSubsystemRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->sessionSubsystemRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->sessionSubsystemRowStatus = set_value;
			if ((StorageOld = StorageTmp->sessionSubsystemTable_old) != NULL) {
				sessionSubsystemTable_destroy(&StorageTmp->sessionSubsystemTable_old);
				StorageTmp->sessionSubsystemTable_rsvs = 0;
				StorageTmp->sessionSubsystemTable_tsts = 0;
				StorageTmp->sessionSubsystemTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			sessionSubsystemTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_sessionSubsystemTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->sessionSubsystemRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_sessionSubsystemTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->sessionSubsystemRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_sessionSubsystemTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				sessionSubsystemTable_del(StorageNew);
				sessionSubsystemTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->sessionSubsystemTable_old) == NULL)
				break;
			if (--StorageTmp->sessionSubsystemTable_rsvs == 0)
				sessionSubsystemTable_destroy(&StorageTmp->sessionSubsystemTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				sessionSubsystemTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_acseEntityRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_acseEntityRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct acseEntityTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct acseEntityTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 15;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("acseMIB", "write_acseEntityRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(acseEntityTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to acseEntityRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to acseEntityRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* mtpMsId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* acseEntityId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[15]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* mtpMsId */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index mtpMsId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* acseEntityId */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index acseEntityId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = acseEntityTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->acseEntityTable_rsvs = 1;
			vp = vars;
			StorageNew->mtpMsId = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->acseEntityId = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&acseEntityTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->acseEntityRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->acseEntityTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->acseEntityTable_old) == NULL)
				if (StorageTmp->acseEntityTable_rsvs == 0)
					if ((StorageOld = StorageTmp->acseEntityTable_old = acseEntityTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->acseEntityTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->acseEntityTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				acseEntityTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_acseEntityTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
				/* check that activation is possible */
			if (StorageTmp->acseEntityRowStatus != RS_ACTIVE)
				if ((ret = can_act_acseEntityTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->acseEntityRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_acseEntityTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->acseEntityRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_acseEntityTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
				}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->acseEntityRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here must be
		   reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_acseEntityTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->acseEntityRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_acseEntityTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->acseEntityRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_acseEntityTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_acseEntityTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->acseEntityRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->acseEntityRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->acseEntityRowStatus = set_value;
			if ((StorageOld = StorageTmp->acseEntityTable_old) != NULL) {
				acseEntityTable_destroy(&StorageTmp->acseEntityTable_old);
				StorageTmp->acseEntityTable_rsvs = 0;
				StorageTmp->acseEntityTable_tsts = 0;
				StorageTmp->acseEntityTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			acseEntityTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_acseEntityTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->acseEntityRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_acseEntityTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->acseEntityRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_acseEntityTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				acseEntityTable_del(StorageNew);
				acseEntityTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->acseEntityTable_old) == NULL)
				break;
			if (--StorageTmp->acseEntityTable_rsvs == 0)
				acseEntityTable_destroy(&StorageTmp->acseEntityTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				acseEntityTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_acseAccessPointRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_acseAccessPointRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct acseAccessPointTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct acseAccessPointTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 15;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("acseMIB", "write_acseAccessPointRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(acseAccessPointTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to acseAccessPointRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to acseAccessPointRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* mtpMsId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* acseEntityId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* acseAccessPointId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[15]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* mtpMsId */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index mtpMsId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* acseEntityId */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index acseEntityId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* acseAccessPointId */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index acseAccessPointId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = acseAccessPointTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->acseAccessPointTable_rsvs = 1;
			vp = vars;
			StorageNew->mtpMsId = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->acseEntityId = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			StorageNew->acseAccessPointId = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&acseAccessPointTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->acseAccessPointRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->acseAccessPointTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->acseAccessPointTable_old) == NULL)
				if (StorageTmp->acseAccessPointTable_rsvs == 0)
					if ((StorageOld = StorageTmp->acseAccessPointTable_old = acseAccessPointTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->acseAccessPointTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->acseAccessPointTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				acseAccessPointTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_acseAccessPointTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
				/* check that activation is possible */
			if (StorageTmp->acseAccessPointRowStatus != RS_ACTIVE)
				if ((ret = can_act_acseAccessPointTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->acseAccessPointRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_acseAccessPointTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->acseAccessPointRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_acseAccessPointTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
				}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->acseAccessPointRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here must be
		   reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_acseAccessPointTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->acseAccessPointRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_acseAccessPointTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->acseAccessPointRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_acseAccessPointTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_acseAccessPointTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->acseAccessPointRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->acseAccessPointRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->acseAccessPointRowStatus = set_value;
			if ((StorageOld = StorageTmp->acseAccessPointTable_old) != NULL) {
				acseAccessPointTable_destroy(&StorageTmp->acseAccessPointTable_old);
				StorageTmp->acseAccessPointTable_rsvs = 0;
				StorageTmp->acseAccessPointTable_tsts = 0;
				StorageTmp->acseAccessPointTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			acseAccessPointTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_acseAccessPointTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->acseAccessPointRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_acseAccessPointTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->acseAccessPointRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_acseAccessPointTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				acseAccessPointTable_del(StorageNew);
				acseAccessPointTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->acseAccessPointTable_old) == NULL)
				break;
			if (--StorageTmp->acseAccessPointTable_rsvs == 0)
				acseAccessPointTable_destroy(&StorageTmp->acseAccessPointTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				acseAccessPointTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_acseLinkageRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_acseLinkageRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct acseLinkageTable_data *StorageTmp = NULL, *StorageOld = NULL;
	static struct acseLinkageTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 15;
	int set_value, ret;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("acseMIB", "write_acseLinkageRowStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(acseLinkageTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to acseLinkageRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to acseLinkageRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* creation */
			vars = NULL;
			/* mtpMsId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			/* sccpNetworkEntityId */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[15]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* mtpMsId */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index mtpMsId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			/* sccpNetworkEntityId */
			/* Note: ranges 1..32 */
			if (vp->val_len > SPRINT_MAX_LEN || ((1 > vp->val_len || vp->val_len > 32))) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index sccpNetworkEntityId: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = acseLinkageTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			StorageNew->acseLinkageTable_rsvs = 1;
			vp = vars;
			StorageNew->mtpMsId = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			memdup((void *) &StorageNew->sccpNetworkEntityId, vp->val.string, vp->val_len);
			StorageNew->sccpNetworkEntityIdLen = vp->val_len;
			vp = vp->next_variable;
			header_complex_add_data(&acseLinkageTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->acseLinkageRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->acseLinkageTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* activate or deactivate */
			if (StorageTmp == NULL)
				return SNMP_ERR_NOSUCHNAME;
			/* one allocation for the whole row */
			if ((StorageOld = StorageTmp->acseLinkageTable_old) == NULL)
				if (StorageTmp->acseLinkageTable_rsvs == 0)
					if ((StorageOld = StorageTmp->acseLinkageTable_old = acseLinkageTable_duplicate(StorageTmp)) == NULL)
						return SNMP_ERR_RESOURCEUNAVAILABLE;
			if (StorageOld != NULL)
				StorageTmp->acseLinkageTable_rsvs++;
			break;
		case RS_DESTROY:
			if (StorageTmp == NULL)
				/* cannot destroy non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->acseLinkageTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				acseLinkageTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* check that activation is possible */
			if ((ret = can_act_acseLinkageTable_row(StorageNew)) != SNMP_ERR_NOERROR)
				return (ret);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
				/* check that activation is possible */
			if (StorageTmp->acseLinkageRowStatus != RS_ACTIVE)
				if ((ret = can_act_acseLinkageTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_NOTINSERVICE:
			/* check that deactivation is possible */
			if (StorageTmp->acseLinkageRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_acseLinkageTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		case RS_DESTROY:
			/* check that deactivation is possible */
			if (StorageTmp->acseLinkageRowStatus != RS_NOTINSERVICE)
				if ((ret = can_deact_acseLinkageTable_row(StorageTmp)) != SNMP_ERR_NOERROR)
					return (ret);
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
				}
		break;
	case ACTION:
		/* The variable has been stored in StorageTmp->acseLinkageRowStatus for you to use, and you have just been asked to do something with it.  Note that anything done here must be
		   reversable in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* activate with underlying device */
			if (activate_acseLinkageTable_row(StorageNew) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			/* state change already performed */
			if (StorageTmp->acseLinkageRowStatus != RS_ACTIVE) {
				/* activate with underlying device */
				if (activate_acseLinkageTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (StorageTmp->acseLinkageRowStatus != RS_NOTINSERVICE) {
				/* deactivate with underlying device */
				if (deactivate_acseLinkageTable_row(StorageTmp) != SNMP_ERR_NOERROR)
					return SNMP_ERR_COMMITFAILED;
			}
			break;
		case RS_DESTROY:
			/* commit destrution to underlying device */
			if (StorageDel == NULL)
				break;
			/* deactivate with underlying device */
			if (deactivate_acseLinkageTable_row(StorageDel) != SNMP_ERR_NOERROR)
				return SNMP_ERR_COMMITFAILED;
			break;
		default:
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			StorageNew->acseLinkageRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			StorageNew->acseLinkageRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			StorageNew->acseLinkageRowStatus = set_value;
			if ((StorageOld = StorageTmp->acseLinkageTable_old) != NULL) {
				acseLinkageTable_destroy(&StorageTmp->acseLinkageTable_old);
				StorageTmp->acseLinkageTable_rsvs = 0;
				StorageTmp->acseLinkageTable_tsts = 0;
				StorageTmp->acseLinkageTable_sets = 0;
			}
			break;
		case RS_DESTROY:
			acseLinkageTable_destroy(&StorageDel);
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* deactivate with underlying device */
			deactivate_acseLinkageTable_row(StorageNew);
			break;
		case RS_CREATEANDWAIT:
			break;
		case RS_ACTIVE:
			if (StorageTmp->acseLinkageRowStatus == RS_NOTINSERVICE)
				/* deactivate with underlying device */
				deactivate_acseLinkageTable_row(StorageTmp);
			break;
		case RS_NOTINSERVICE:
			if (StorageTmp->acseLinkageRowStatus == RS_ACTIVE)
				/* activate with underlying device */
				activate_acseLinkageTable_row(StorageTmp);
			break;
		case RS_DESTROY:
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				acseLinkageTable_del(StorageNew);
				acseLinkageTable_destroy(&StorageNew);
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if ((StorageOld = StorageTmp->acseLinkageTable_old) == NULL)
				break;
			if (--StorageTmp->acseLinkageTable_rsvs == 0)
				acseLinkageTable_destroy(&StorageTmp->acseLinkageTable_old);
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				acseLinkageTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn void acseMIB_loop_handler(int dummy)
 * @param sig signal number
 * @brief handle event loop interation.
 *
 * This function is registered so that, when operating as a module, snmpd will call it one per event
 * loop interation.  This function is called before the next requst is processed and after the
 * previous request is processed.  Two things are done here:  1) The file descriptor that is used to
 * synchronize the agent with (pseudo-)device drivers is closed.  (Another approach, instead of
 * closing each time, would be to restart a timer each time that a request is made (loop is
 * performed) and if it expires, close the file descriptor).  2) The request number is incremented.
 * Although a request is not generated for each loop of the snmp event loop, it is true that a new
 * request cannot be generated without performing a loop.  Therefore, the sa_request is not the
 * request number but it is a temporally unique identifier for a request.
 */
void
acseMIB_loop_handler(int sig)
{
	DEBUGMSGTL(("acseMIB", "acseMIB_loop_handler: executing loop handler...  "));
	/* close files after each request */
	if (sa_fclose) {
		if (my_fd >= 0) {
			close(my_fd);
			my_fd = -1;
		}
	}
#if defined MASTER
	/* prepare for next request */
	sa_request++;
#endif				/* defined MASTER */
	if (external_signal_scheduled[sig] == 0)
		external_signal_scheduled[sig]--;
	if (acseMIBold_signal_handler != NULL)
		(*acseMIBold_signal_handler) (sig);
	DEBUGMSGTL(("acseMIB", "done.\n"));
}

/**
 * @fn void acseMIB_fd_handler(int fd, void *dummy)
 * @param fd file descriptor to read.
 * @param dummy client data passed to registration function (always NULL).
 * @brief handle read event on file descriptor.
 *
 * This read file descriptor handler is normally used for (pseudo-)device drivers that generate
 * statistical collection interval events, alarm events, or other operational measurement events, by
 * placing a message on the read queue of the "event handling" Stream.  Normally this routine
 * would adjust counts in some table or scalars, generate SNMP traps representing on-occurence
 * events, first and interval events, and alarm indications.
 */
void
acseMIB_fd_handler(int fd, void *dummy)
{
	DEBUGMSGTL(("acseMIB", "acseMIB_fd_handler: executing fd handler...  "));
	/* XXX: place actions to handle my_fd here... */
	DEBUGMSGTL(("acseMIB", "done.\n"));
	return;
}
