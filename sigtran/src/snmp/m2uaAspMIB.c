/*****************************************************************************

 @(#) $RCSfile: m2uaAspMIB.c,v $ $Name:  $($Revision: 0.9.2.3 $) $Date: 2009-01-02 15:04:08 $

 -----------------------------------------------------------------------------

 Copyright (c) 2008-2009  Monavacom Limited <http://www.monavacom.com/>
 Copyright (c) 2001-2008  OpenSS7 Corporation <http://www.openss7.com/>
 Copyright (c) 1997-2001  Brian F. G. Bidulock <bidulock@openss7.org>

 All Rights Reserved.

 This program is free software: you can redistribute it and/or modify it under
 the terms of the GNU Affero General Public License as published by the Free
 Software Foundation, version 3 of the license.

 This program is distributed in the hope that it will be useful, but WITHOUT
 ANY WARRANTY; without even the implied warranty of MERCHANTABILITY or FITNESS
 FOR A PARTICULAR PURPOSE.  See the GNU Affero General Public License for more
 details.

 You should have received a copy of the GNU Affero General Public License
 along with this program.  If not, see <http://www.gnu.org/licenses/>, or
 write to the Free Software Foundation, Inc., 675 Mass Ave, Cambridge, MA
 02139, USA.

 -----------------------------------------------------------------------------

 U.S. GOVERNMENT RESTRICTED RIGHTS.  If you are licensing this Software on
 behalf of the U.S. Government ("Government"), the following provisions apply
 to you.  If the Software is supplied by the Department of Defense ("DoD"), it
 is classified as "Commercial Computer Software" under paragraph 252.227-7014
 of the DoD Supplement to the Federal Acquisition Regulations ("DFARS") (or any
 successor regulations) and the Government is acquiring only the license rights
 granted herein (the license rights customarily provided to non-Government
 users).  If the Software is supplied to any unit or agency of the Government
 other than DoD, it is classified as "Restricted Computer Software" and the
 Government's rights in the Software are defined in paragraph 52.227-19 of the
 Federal Acquisition Regulations ("FAR") (or any successor regulations) or, in
 the cases of NASA, in paragraph 18.52.227-86 of the NASA Supplement to the FAR
 (or any successor regulations).

 -----------------------------------------------------------------------------

 Commercial licensing and support of this software is available from OpenSS7
 Corporation at a fee.  See http://www.openss7.com/

 -----------------------------------------------------------------------------

 Last Modified $Date: 2009-01-02 15:04:08 $ by $Author: brian $

 -----------------------------------------------------------------------------

 $Log: m2uaAspMIB.c,v $
 Revision 0.9.2.3  2009-01-02 15:04:08  brian
 - updated agents

 Revision 0.9.2.2  2008-12-31 16:04:40  brian
 - updated mibs

 Revision 0.9.2.1  2008-12-27 10:01:52  brian
 - updated mibs and stubs

 *****************************************************************************/

#ident "@(#) $RCSfile: m2uaAspMIB.c,v $ $Name:  $($Revision: 0.9.2.3 $) $Date: 2009-01-02 15:04:08 $"

static char const ident[] = "$RCSfile: m2uaAspMIB.c,v $ $Name:  $($Revision: 0.9.2.3 $) $Date: 2009-01-02 15:04:08 $";

/* This file was generated by mib2c and is intended for use as
   a mib module for the ucd-snmp snmpd agent. */
#include <ucd-snmp/ucd-snmp-config.h>
#include <ucd-snmp/ucd-snmp-includes.h>
#include <ucd-snmp/ucd-snmp-agent-includes.h>
#include <ucd-snmp/callback.h>
#include <ucd-snmp/snmp-tc.h>
#include <ucd-snmp/default_store.h>
#include <ucd-snmp/snmp_alarm.h>
/* The following header files are mangled in most recent net-snmp releases so
 * the versions from UCD-SNMP 4.2.5 are included here.  */
#if defined HAVE_LIBNETSNMP
#else				/* defined HAVE_LIBNETSNMP */
#endif				/* defined HAVE_LIBNETSNMP */
/* These are messed up on both. */
#include "ds_agent.h"
#ifdef HAVE_UCD_SNMP_UTIL_FUNCS_H
#include <ucd-snmp/util_funcs.h>
/* Many recent net-snmp UCD compatible headers do not declard header_generic. */
int header_generic(struct variable *, oid *, size_t *, int, size_t *, WriteMethod **);
#else				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#include "util_funcs.h"
#endif				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#ifdef HAVE_UCD_SNMP_HEADER_COMPLEX_H
#include <ucd-snmp/header_complex.h>
#else				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
#include "header_complex.h"
#endif				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
/* This one is the other way around: it is just fine for net-snmp, but
 * ucd-snmp does not provide the header file at all.  */
#ifdef HAVE_UCD_SNMP_MIB_MODULES_H
#include <ucd-snmp/mib_modules.h>
#else				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#ifdef HAVE_NET_SNMP_AGENT_MIB_MODULES_H
#include <net-snmp/agent/mib_modules.h>
#else				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#include "mib_modules.h"
#endif				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#endif				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#include <stdint.h>
#include <signal.h>
#include <sys/stat.h>		/* for struct stat, fstat() */
#include <sys/types.h>
#include <unistd.h>
#include <stdlib.h>
#include <pwd.h>		/* for getpwuid() getpwnam() */
#include <grp.h>		/* for getgrgid() getgrnam() */
#include <libgen.h>		/* for basename() */
#include <fcntl.h>		/* for O_CREAT */
#include <sys/sysctl.h>		/* for sysctl */
#include <errno.h>
#include <string.h>
#ifdef _GNU_SOURCE
#include <getopt.h>
#endif
#include "m2uaAspMIB.h"
extern const char sa_program[];

#define MY_FACILITY(__pri)	(LOG_DAEMON|(__pri))
#if !defined MODULE
extern int sa_dump;			/* default packet dump */
extern int sa_debug;			/* default no debug */
extern int sa_nomead;			/* default daemon mode */
extern int sa_output;			/* default normal output */
extern int sa_agentx;			/* default agentx mode */
extern int sa_alarms;			/* default application alarms */
extern int sa_fclose;			/* default close files between requests */
extern int sa_logaddr;			/* log addresses */
extern int sa_logfillog;		/* log to sa_logfile */
extern int sa_logstderr;		/* log to standard error */
extern int sa_logstdout;		/* log to standard output */
extern int sa_logsyslog;		/* log to system logs */
extern int sa_logcallog;		/* log to callback logs */
extern int sa_appendlog;		/* append to log file without truncating */
extern char sa_logfile[256];
extern char sa_pidfile[256];
extern char sa_sysctlf[256];

/* file stream for log file */
extern FILE *stdlog;

/* file descriptor for MIB use */
extern int sa_fd;

/* indication to reread MIB configuration */
extern int sa_changed;

/* indications that statistics, the mib or its tables need to be refreshed */
extern int sa_stats_refresh;
#endif				/* !defined MODULE */
/* request number for per-request actions */
extern int sa_request;
volatile int m2uaAspMIB_refresh = 1;
volatile int m2uaAspTable_refresh = 1;
volatile int m2uaSgTable_refresh = 1;
volatile int m2uaAspSgTable_refresh = 1;
volatile int m2uaAsTable_refresh = 1;
volatile int m2uaIfTable_refresh = 1;
volatile int m2uaAsIfTable_refresh = 1;
volatile int m2uaSgAsTable_refresh = 1;
volatile int m2uaAspAsTable_refresh = 1;
volatile int m2uaAspSgAsTable_refresh = 1;

/*
 * m2uaAspMIB_variables_oid: object identifier for m2uaAspMIB
 * This is the top level oid that we want to register under.  This is essentially a prefix, with the
 * suffix appearing in the variable below.
 */
oid m2uaAspMIB_variables_oid[] = { 1, 3, 6, 1, 4, 1, 29591, 1, 221 };
oid m2uaAspTable_variables_oid[] = { 1, 3, 6, 1, 4, 1, 29591, 1, 221, 1, 1, 1, 1, 1 };
oid m2uaSgTable_variables_oid[] = { 1, 3, 6, 1, 4, 1, 29591, 1, 221, 1, 1, 2, 1, 1 };
oid m2uaAspSgTable_variables_oid[] = { 1, 3, 6, 1, 4, 1, 29591, 1, 221, 1, 1, 3, 1, 1 };
oid m2uaAsTable_variables_oid[] = { 1, 3, 6, 1, 4, 1, 29591, 1, 221, 1, 1, 4, 1, 1 };
oid m2uaIfTable_variables_oid[] = { 1, 3, 6, 1, 4, 1, 29591, 1, 221, 1, 1, 5, 1, 1 };
oid m2uaSgAsTable_variables_oid[] = { 1, 3, 6, 1, 4, 1, 29591, 1, 221, 1, 1, 7, 1, 1 };
oid m2uaAspAsTable_variables_oid[] = { 1, 3, 6, 1, 4, 1, 29591, 1, 221, 1, 1, 8, 1, 1 };
oid m2uaAspAsTable_variables_oid[] = { 1, 3, 6, 1, 4, 1, 29591, 1, 221, 1, 1, 8, 1, 1 };

/*
 * Oids for use in notifications defined in this MIB.
 */

/*
 * Oids accessible only for notify defined in this MIB.
 */

/*
 * Other oids defined in this MIB.
 */

/*
 * variable7 m2uaAspMIB_variables: tree for m2uaAspMIB
 * This variable defines function callbacks and type return information for the m2uaAspMIB mib section
 */
struct variable7 m2uaAspMIB_variables[] = {
	/* magic number, variable type, ro/rw, callback fn, L, oidsuffix */
#define   M2UAASPADMINISTRATIVESTATE  (4 % 256)
	{M2UAASPADMINISTRATIVESTATE, ASN_INTEGER, RWRITE, var_m2uaAspTable, 6, {1, 1, 1, 1, 1, 2}},
#define   M2UAASPOPERATIONALSTATE  (5 % 256)
	{M2UAASPOPERATIONALSTATE, ASN_INTEGER, RONLY, var_m2uaAspTable, 6, {1, 1, 1, 1, 1, 3}},
#define   M2UAASPPROCEDURALSTATUS  (6 % 256)
	{M2UAASPPROCEDURALSTATUS, ASN_BIT_STR, RONLY, var_m2uaAspTable, 6, {1, 1, 1, 1, 1, 4}},
#define   M2UAASPUSAGESTATE     (7 % 256)
	{M2UAASPUSAGESTATE, ASN_INTEGER, RONLY, var_m2uaAspTable, 6, {1, 1, 1, 1, 1, 5}},
#define   M2UAASPASPSTATE       (8 % 256)
	{M2UAASPASPSTATE, ASN_INTEGER, RWRITE, var_m2uaAspTable, 6, {1, 1, 1, 1, 1, 6}},
#define   M2UAASPCAPABLITIES    (9 % 256)
	{M2UAASPCAPABLITIES, ASN_BIT_STR, RWRITE, var_m2uaAspTable, 6, {1, 1, 1, 1, 1, 7}},
#define   M2UAASPREGISTRATIONPOLICY  (10 % 256)
	{M2UAASPREGISTRATIONPOLICY, ASN_INTEGER, RWRITE, var_m2uaAspTable, 6, {1, 1, 1, 1, 1, 8}},
#define   M2UAASPNAME           (11 % 256)
	{M2UAASPNAME, ASN_OCTET_STR, RWRITE, var_m2uaAspTable, 6, {1, 1, 1, 1, 1, 9}},
#define   M2UAASPROWSTATUS      (12 % 256)
	{M2UAASPROWSTATUS, ASN_INTEGER, RWRITE, var_m2uaAspTable, 6, {1, 1, 1, 1, 1, 10}},
#define   M2UASGNAME            (16 % 256)
	{M2UASGNAME, ASN_OCTET_STR, RWRITE, var_m2uaSgTable, 6, {1, 1, 2, 1, 1, 2}},
#define   M2UASGADMINISTRATIVESTATE  (17 % 256)
	{M2UASGADMINISTRATIVESTATE, ASN_INTEGER, RWRITE, var_m2uaSgTable, 6, {1, 1, 2, 1, 1, 3}},
#define   M2UASGOPERATIONALSTATE  (18 % 256)
	{M2UASGOPERATIONALSTATE, ASN_INTEGER, RONLY, var_m2uaSgTable, 6, {1, 1, 2, 1, 1, 4}},
#define   M2UASGPROCEDURALSTATUS  (19 % 256)
	{M2UASGPROCEDURALSTATUS, ASN_BIT_STR, RONLY, var_m2uaSgTable, 6, {1, 1, 2, 1, 1, 5}},
#define   M2UASGSTANDBYSTATUS   (20 % 256)
	{M2UASGSTANDBYSTATUS, ASN_INTEGER, RONLY, var_m2uaSgTable, 6, {1, 1, 2, 1, 1, 6}},
#define   M2UASGPRIMARYSG       (21 % 256)
	{M2UASGPRIMARYSG, ASN_UNSIGNED, RONLY, var_m2uaSgTable, 6, {1, 1, 2, 1, 1, 7}},
#define   M2UASGASPSTATE        (22 % 256)
	{M2UASGASPSTATE, ASN_INTEGER, RWRITE, var_m2uaSgTable, 6, {1, 1, 2, 1, 1, 8}},
#define   M2UASGUSAGESTATE      (23 % 256)
	{M2UASGUSAGESTATE, ASN_INTEGER, RONLY, var_m2uaSgTable, 6, {1, 1, 2, 1, 1, 9}},
#define   M2UASGVERSION         (24 % 256)
	{M2UASGVERSION, ASN_INTEGER, RWRITE, var_m2uaSgTable, 6, {1, 1, 2, 1, 1, 10}},
#define   M2UASGOPTIONS         (25 % 256)
	{M2UASGOPTIONS, ASN_BIT_STR, RWRITE, var_m2uaSgTable, 6, {1, 1, 2, 1, 1, 11}},
#define   M2UASGREGISTRATIONPOLICY  (26 % 256)
	{M2UASGREGISTRATIONPOLICY, ASN_INTEGER, RWRITE, var_m2uaSgTable, 6, {1, 1, 2, 1, 1, 12}},
#define   M2UASGPROTOCOLPAYLOADID  (27 % 256)
	{M2UASGPROTOCOLPAYLOADID, ASN_INTEGER, RWRITE, var_m2uaSgTable, 6, {1, 1, 2, 1, 1, 13}},
#define   M2UASGIPPORT          (28 % 256)
	{M2UASGIPPORT, ASN_INTEGER, RWRITE, var_m2uaSgTable, 6, {1, 1, 2, 1, 1, 14}},
#define   M2UASGPRIMARYIPADDRESS  (29 % 256)
	{M2UASGPRIMARYIPADDRESS, ASN_IPADDRESS, RWRITE, var_m2uaSgTable, 6, {1, 1, 2, 1, 1, 15}},
#define   M2UASGMINOSTREAMS     (30 % 256)
	{M2UASGMINOSTREAMS, ASN_INTEGER, RWRITE, var_m2uaSgTable, 6, {1, 1, 2, 1, 1, 16}},
#define   M2UASGMAXISTREAMS     (31 % 256)
	{M2UASGMAXISTREAMS, ASN_INTEGER, RWRITE, var_m2uaSgTable, 6, {1, 1, 2, 1, 1, 17}},
#define   M2UASGROWSTATUS       (32 % 256)
	{M2UASGROWSTATUS, ASN_INTEGER, RWRITE, var_m2uaSgTable, 6, {1, 1, 2, 1, 1, 18}},
#define   M2UAASPSGADMINSTRATIVESTATE  (37 % 256)
	{M2UAASPSGADMINSTRATIVESTATE, ASN_INTEGER, RWRITE, var_m2uaAspSgTable, 6, {1, 1, 3, 1, 1, 1}},
#define   M2UAASPSGOPERATIONALSTATE  (38 % 256)
	{M2UAASPSGOPERATIONALSTATE, ASN_INTEGER, RONLY, var_m2uaAspSgTable, 6, {1, 1, 3, 1, 1, 2}},
#define   M2UAASPSGPROCEDURALSTATUS  (39 % 256)
	{M2UAASPSGPROCEDURALSTATUS, ASN_BIT_STR, RONLY, var_m2uaAspSgTable, 6, {1, 1, 3, 1, 1, 3}},
#define   M2UAASPSGUSAGESTATE   (40 % 256)
	{M2UAASPSGUSAGESTATE, ASN_INTEGER, RONLY, var_m2uaAspSgTable, 6, {1, 1, 3, 1, 1, 4}},
#define   M2UAASPSGOPERATIONALSTATE  (41 % 256)
	{M2UAASPSGOPERATIONALSTATE, ASN_INTEGER, RONLY, var_m2uaAspSgTable, 6, {1, 1, 3, 1, 1, 5}},
#define   M2UAASPSGAVAILABILITYSTATUS  (42 % 256)
	{M2UAASPSGAVAILABILITYSTATUS, ASN_BIT_STR, RONLY, var_m2uaAspSgTable, 6, {1, 1, 3, 1, 1, 6}},
#define   M2UAASPSGASPSTATE     (43 % 256)
	{M2UAASPSGASPSTATE, ASN_INTEGER, RWRITE, var_m2uaAspSgTable, 6, {1, 1, 3, 1, 1, 7}},
#define   M2UAASPSGASPIDPOLICY  (44 % 256)
	{M2UAASPSGASPIDPOLICY, ASN_INTEGER, RWRITE, var_m2uaAspSgTable, 6, {1, 1, 3, 1, 1, 8}},
#define   M2UAASPSGASPID        (45 % 256)
	{M2UAASPSGASPID, ASN_UNSIGNED, RWRITE, var_m2uaAspSgTable, 6, {1, 1, 3, 1, 1, 9}},
#define   M2UAASPSGASSOCIATIONPOLICY  (46 % 256)
	{M2UAASPSGASSOCIATIONPOLICY, ASN_INTEGER, RWRITE, var_m2uaAspSgTable, 6, {1, 1, 3, 1, 1, 10}},
#define   M2UAASPSGIPPORT       (47 % 256)
	{M2UAASPSGIPPORT, ASN_INTEGER, RWRITE, var_m2uaAspSgTable, 6, {1, 1, 3, 1, 1, 11}},
#define   M2UAASPSGPRIMARYIPADDRESS  (48 % 256)
	{M2UAASPSGPRIMARYIPADDRESS, ASN_IPADDRESS, RWRITE, var_m2uaAspSgTable, 6, {1, 1, 3, 1, 1, 12}},
#define   M2UAASPSGROWSTATUS    (49 % 256)
	{M2UAASPSGROWSTATUS, ASN_INTEGER, RWRITE, var_m2uaAspSgTable, 6, {1, 1, 3, 1, 1, 13}},
#define   M2UAASNAME            (53 % 256)
	{M2UAASNAME, ASN_OCTET_STR, RWRITE, var_m2uaAsTable, 6, {1, 1, 4, 1, 1, 2}},
#define   M2UAASTRAFFICMODE     (54 % 256)
	{M2UAASTRAFFICMODE, ASN_OBJECT_ID, RWRITE, var_m2uaAsTable, 6, {1, 1, 4, 1, 1, 3}},
#define   M2UAASADMINISTRATIVESTATE  (55 % 256)
	{M2UAASADMINISTRATIVESTATE, ASN_INTEGER, RWRITE, var_m2uaAsTable, 6, {1, 1, 4, 1, 1, 4}},
#define   M2UAASOPERATIONALSTATE  (56 % 256)
	{M2UAASOPERATIONALSTATE, ASN_INTEGER, RONLY, var_m2uaAsTable, 6, {1, 1, 4, 1, 1, 5}},
#define   M2UAASPROCEDURALSTATUS  (57 % 256)
	{M2UAASPROCEDURALSTATUS, ASN_BIT_STR, RWRITE, var_m2uaAsTable, 6, {1, 1, 4, 1, 1, 6}},
#define   M2UAASUSAGESTATE      (58 % 256)
	{M2UAASUSAGESTATE, ASN_INTEGER, RWRITE, var_m2uaAsTable, 6, {1, 1, 4, 1, 1, 7}},
#define   M2UAASASSTATE         (59 % 256)
	{M2UAASASSTATE, ASN_INTEGER, RWRITE, var_m2uaAsTable, 6, {1, 1, 4, 1, 1, 8}},
#define   M2UAASROWSTATUS       (60 % 256)
	{M2UAASROWSTATUS, ASN_INTEGER, RWRITE, var_m2uaAsTable, 6, {1, 1, 4, 1, 1, 9}},
#define   M2UAIFASINDEX         (64 % 256)
	{M2UAIFASINDEX, ASN_UNSIGNED, RWRITE, var_m2uaIfTable, 6, {1, 1, 5, 1, 1, 2}},
#define   M2UAIFIDENTIFIER      (65 % 256)
	{M2UAIFIDENTIFIER, ASN_OCTET_STR, RWRITE, var_m2uaIfTable, 6, {1, 1, 5, 1, 1, 3}},
#define   M2UAIFNAME            (66 % 256)
	{M2UAIFNAME, ASN_OCTET_STR, RWRITE, var_m2uaIfTable, 6, {1, 1, 5, 1, 1, 4}},
#define   M2UAIFROWSTATUS       (67 % 256)
	{M2UAIFROWSTATUS, ASN_INTEGER, RWRITE, var_m2uaIfTable, 6, {1, 1, 5, 1, 1, 5}},
#define   M2UASGASORDERING      (76 % 256)
	{M2UASGASORDERING, ASN_UNSIGNED, RWRITE, var_m2uaSgAsTable, 6, {1, 1, 7, 1, 1, 1}},
#define   M2UASGASADMINISTRATIVESTATE  (77 % 256)
	{M2UASGASADMINISTRATIVESTATE, ASN_INTEGER, RWRITE, var_m2uaSgAsTable, 6, {1, 1, 7, 1, 1, 2}},
#define   M2UASGASOPERATIONALSTATE  (78 % 256)
	{M2UASGASOPERATIONALSTATE, ASN_INTEGER, RONLY, var_m2uaSgAsTable, 6, {1, 1, 7, 1, 1, 3}},
#define   M2UASGASPROCEDURALSTATUS  (79 % 256)
	{M2UASGASPROCEDURALSTATUS, ASN_BIT_STR, RONLY, var_m2uaSgAsTable, 6, {1, 1, 7, 1, 1, 4}},
#define   M2UASGASUSAGESTATE    (80 % 256)
	{M2UASGASUSAGESTATE, ASN_INTEGER, RONLY, var_m2uaSgAsTable, 6, {1, 1, 7, 1, 1, 5}},
#define   M2UASGASOPERATIONALSTATE  (81 % 256)
	{M2UASGASOPERATIONALSTATE, ASN_INTEGER, RONLY, var_m2uaSgAsTable, 6, {1, 1, 7, 1, 1, 6}},
#define   M2UASGASAVAILABILITYSTATUS  (82 % 256)
	{M2UASGASAVAILABILITYSTATUS, ASN_BIT_STR, RONLY, var_m2uaSgAsTable, 6, {1, 1, 7, 1, 1, 7}},
#define   M2UASGASSTANDBYSTATUS  (83 % 256)
	{M2UASGASSTANDBYSTATUS, ASN_INTEGER, RONLY, var_m2uaSgAsTable, 6, {1, 1, 7, 1, 1, 8}},
#define   M2UASGASPRIMARYSG     (84 % 256)
	{M2UASGASPRIMARYSG, ASN_UNSIGNED, RONLY, var_m2uaSgAsTable, 6, {1, 1, 7, 1, 1, 9}},
#define   M2UASGASASSTATE       (85 % 256)
	{M2UASGASASSTATE, ASN_INTEGER, RONLY, var_m2uaSgAsTable, 6, {1, 1, 7, 1, 1, 10}},
#define   M2UASGASROWSTATUS     (86 % 256)
	{M2UASGASROWSTATUS, ASN_INTEGER, RWRITE, var_m2uaSgAsTable, 6, {1, 1, 7, 1, 1, 11}},
#define   M2UAASPASADMINISTRATIVESTATE  (91 % 256)
	{M2UAASPASADMINISTRATIVESTATE, ASN_INTEGER, RWRITE, var_m2uaAspAsTable, 6, {1, 1, 8, 1, 1, 1}},
#define   M2UAASPASOPERATIONALSTATE  (92 % 256)
	{M2UAASPASOPERATIONALSTATE, ASN_INTEGER, RONLY, var_m2uaAspAsTable, 6, {1, 1, 8, 1, 1, 2}},
#define   M2UAASPASPROCEDURALSTATUS  (93 % 256)
	{M2UAASPASPROCEDURALSTATUS, ASN_BIT_STR, RONLY, var_m2uaAspAsTable, 6, {1, 1, 8, 1, 1, 3}},
#define   M2UAASPASUSAGESTATE   (94 % 256)
	{M2UAASPASUSAGESTATE, ASN_INTEGER, RONLY, var_m2uaAspAsTable, 6, {1, 1, 8, 1, 1, 4}},
#define   M2UAASPASASSTATE      (95 % 256)
	{M2UAASPASASSTATE, ASN_INTEGER, RWRITE, var_m2uaAspAsTable, 6, {1, 1, 8, 1, 1, 5}},
#define   M2UAASPASROWSTATUS    (96 % 256)
	{M2UAASPASROWSTATUS, ASN_INTEGER, RWRITE, var_m2uaAspAsTable, 6, {1, 1, 8, 1, 1, 6}},
#define   M2UAASPSGASADMINISTRATIVESTATE  (102 % 256)
	{M2UAASPSGASADMINISTRATIVESTATE, ASN_INTEGER, RWRITE, var_m2uaAspSgAsTable, 6, {1, 1, 9, 1, 1, 1}},
#define   M2UAASPSGASOPERATIONALSTATE  (103 % 256)
	{M2UAASPSGASOPERATIONALSTATE, ASN_INTEGER, RONLY, var_m2uaAspSgAsTable, 6, {1, 1, 9, 1, 1, 2}},
#define   M2UAASPSGASPROCEDURALSTATUS  (104 % 256)
	{M2UAASPSGASPROCEDURALSTATUS, ASN_BIT_STR, RONLY, var_m2uaAspSgAsTable, 6, {1, 1, 9, 1, 1, 3}},
#define   M2UAASPSGASUSAGESTATE  (105 % 256)
	{M2UAASPSGASUSAGESTATE, ASN_INTEGER, RONLY, var_m2uaAspSgAsTable, 6, {1, 1, 9, 1, 1, 4}},
#define   M2UAASPSGASSTANDBYSTATUS  (106 % 256)
	{M2UAASPSGASSTANDBYSTATUS, ASN_INTEGER, RONLY, var_m2uaAspSgAsTable, 6, {1, 1, 9, 1, 1, 5}},
#define   M2UAASPSGASROWSTATUS  (107 % 256)
	{M2UAASPSGASROWSTATUS, ASN_INTEGER, RWRITE, var_m2uaAspSgAsTable, 6, {1, 1, 9, 1, 1, 6}},
};

/* (L = length of the oidsuffix) */
struct m2uaAspMIB_data *m2uaAspMIBStorage = NULL;

/* global storage of our data, saved in and configured by header_complex() */
struct header_complex_index *m2uaAspTableStorage = NULL;
struct header_complex_index *m2uaSgTableStorage = NULL;
struct header_complex_index *m2uaAspSgTableStorage = NULL;
struct header_complex_index *m2uaAsTableStorage = NULL;
struct header_complex_index *m2uaIfTableStorage = NULL;
struct header_complex_index *m2uaAsIfTableStorage = NULL;
struct header_complex_index *m2uaSgAsTableStorage = NULL;
struct header_complex_index *m2uaAspAsTableStorage = NULL;
struct header_complex_index *m2uaAspSgAsTableStorage = NULL;

/*
 * init_m2uaAspMIB(): Initialization routine.
 * This is called when the agent starts up.  At a minimum, registration of your variables should
 * take place here.
 */
void
init_m2uaAspMIB(void)
{
	DEBUGMSGTL(("m2uaAspMIB", "initializing...  "));
	/* register ourselves with the agent to handle our mib tree */
	REGISTER_MIB("m2uaAspMIB", m2uaAspMIB_variables, variable7, m2uaAspMIB_variables_oid);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_SHUTDOWN, term_m2uaAspMIB, NULL);
	/* register our config handler(s) to deal with registrations */
	snmpd_register_config_handler("m2uaAspMIB", parse_m2uaAspMIB, NULL, "HELP STRING");
	snmpd_register_config_handler("m2uaAspTable", parse_m2uaAspTable, NULL, "HELP STRING");
	snmpd_register_config_handler("m2uaSgTable", parse_m2uaSgTable, NULL, "HELP STRING");
	snmpd_register_config_handler("m2uaAspSgTable", parse_m2uaAspSgTable, NULL, "HELP STRING");
	snmpd_register_config_handler("m2uaAsTable", parse_m2uaAsTable, NULL, "HELP STRING");
	snmpd_register_config_handler("m2uaIfTable", parse_m2uaIfTable, NULL, "HELP STRING");
	snmpd_register_config_handler("m2uaAsIfTable", parse_m2uaAsIfTable, NULL, "HELP STRING");
	snmpd_register_config_handler("m2uaSgAsTable", parse_m2uaSgAsTable, NULL, "HELP STRING");
	snmpd_register_config_handler("m2uaAspAsTable", parse_m2uaAspAsTable, NULL, "HELP STRING");
	snmpd_register_config_handler("m2uaAspSgAsTable", parse_m2uaAspSgAsTable, NULL, "HELP STRING");

	/* we need to be called back later to store our data */
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_m2uaAspMIB, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_m2uaAspTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_m2uaSgTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_m2uaAspSgTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_m2uaAsTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_m2uaIfTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_m2uaAsIfTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_m2uaSgAsTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_m2uaAspAsTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_m2uaAspSgAsTable, NULL);

	/* place any other initialization junk you need here */
	DEBUGMSGTL(("m2uaAspMIB", "done.\n"));
}

/*
 * deinit_m2uaAspMIB(): Deinitialization routine.
 * This is called before the agent is unloaded.  At a minimum, deregistration of your variables
 * should take place here.
 */
void
deinit_m2uaAspMIB(void)
{
	DEBUGMSGTL(("m2uaAspMIB", "deinitializating...  "));
	unregister_mib(m2uaAspMIB_variables_oid, sizeof(m2uaAspMIB_variables_oid) / sizeof(oid));
	snmpd_unregister_config_handler("m2uaAspMIB");
	snmpd_unregister_config_handler("m2uaAspTable");
	snmpd_unregister_config_handler("m2uaSgTable");
	snmpd_unregister_config_handler("m2uaAspSgTable");
	snmpd_unregister_config_handler("m2uaAsTable");
	snmpd_unregister_config_handler("m2uaIfTable");
	snmpd_unregister_config_handler("m2uaAsIfTable");
	snmpd_unregister_config_handler("m2uaSgAsTable");
	snmpd_unregister_config_handler("m2uaAspAsTable");
	snmpd_unregister_config_handler("m2uaAspSgAsTable");

	/* place any other de-initialization junk you need here */
	DEBUGMSGTL(("m2uaAspMIB", "done.\n"));
}

int
term_m2uaAspMIB(int majorID, int minorID, void *serverarg, void *clientarg)
{
	deinit_m2uaAspMIB();
	return 0;
}

/**
 * @fn struct m2uaAspMIB_data *m2uaAspMIB_create(void)
 * @brief create a fresh data structure representing scalars in m2uaAspMIB.
 * Creates a new m2uaAspMIB_data structure by allocating dynamic memory for the structure and
 * initializing the default values of scalars in m2uaAspMIB.
 */
struct m2uaAspMIB_data *
m2uaAspMIB_create(void)
{
	struct m2uaAspMIB_data *StorageNew = SNMP_MALLOC_STRUCT(m2uaAspMIB_data);

	DBUGMSGTL(("m2uaAspMIB", "creating scalars...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default scalar values here into StorageNew */

	}
	DEBUGMSGTL(("m2uaAspMIB", "done.\n"));
	return (StorageNew);
}

/**
 * @fn int m2uaAspMIB_destroy(struct m2uaAspMIB_data **thedata)
 * @brief delete a scalars structure from m2uaAspMIB.
 * @param thedata pointer to the data structure in m2uaAspMIB.
 * Frees scalars that were previously removed from m2uaAspMIB.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in  thedata will be set to NULL if it is not already
 * NULL.
 */
int
m2uaAspMIB_destroy(struct m2uaAspMIB_data **thedata)
{
	struct m2uaAspMIB_data *StorageDel;

	DEBUGMSGTL(("m2uaAspMIB", "deleting scalars...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("m2uaAspMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int m2uaAspMIB_add(struct m2uaAspMIB_data *thedata)
 * @param thedata the structure representing m2uaAspMIB scalars.
 * @brief adds node to the m2uaAspMIB scalar data set.
 * Adds a scalar structure to the m2uaAspMIB data set.  Note that this function is necessary even
 * when the scalar values are not peristent.
 */
int
m2uaAspMIB_add(struct m2uaAspMIB_data *thedata)
{
	DEBUGMSGTL(("m2uaAspMIB", "adding data...  "));
	m2uaAspMIBStorage = thedata;
	DEBUGMSGTL(("m2uaAspMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_m2uaAspMIB(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for m2uaAspMIB entries.
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case m2uaAspMIB).  This routine is invoked by
 * UCD-SNMP to read the values of scalars in the MIB from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the MIB.  If there are no configured entries
 * in the configuration MIB, this function will simply not be called.
 */
void
parse_m2uaAspMIB(const char *token, char *line)
{
	size_t tmpsize;
	struct m2uaAspMIB_data *StorageTmp = m2uaAspMIB_create();

	DEBUGMSGTL(("m2uaAspMIB", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual scalars that are not persistent */
	m2uaAspMIB_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("m2uaAspMIB", "done.\n"));
}

/*
 * store_m2uaAspMIB(): stores .conf file entries needed to configure the mib.
 */
int
store_m2uaAspMIB(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct m2uaAspMIB_data *StorageTmp;

	DEBUGMSGTL(("m2uaAspMIB", "storing data...  "));
	refresh_m2uaAspMIB();
	if ((StorageTmp = m2uaAspMIBStorage) == NULL) {
		DEBUGMSGTL(("m2uaAspMIB", "error.\n"));
		return SNMPERR_GENERR;
	}
	(void) tmpsize;
	/* XXX: comment entire section if no scalars are persistent */
	{
		memset(line, 0, sizeof(line));
		strcat(line, "m2uaAspMIB ");
		cptr = line + strlen(line);
		/* XXX: remove individual scalars that are not persistent */
		snmpd_store_config(line);
	}
	DEBUGMSGTL(("m2uaAspMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void refresh_m2uaAspMIB(void)
 * @brief refresh the scalar values of m2uaAspMIB.
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a scalar has been requested).
 */
void
refresh_m2uaAspMIB(void)
{
	if (m2uaAspMIBStorage == NULL) {
		struct m2uaAspMIB_data *StorageNew;

		if ((StorageNew = m2uaAspMIB_create()) == NULL)
			return;
		m2uaAspMIBStorage = StorageNew;
		m2uaAspMIB_refresh = 1;
	}
	if (m2uaAspMIB_refresh == 0)
		return;
	m2uaAspMIB_refresh = 0;
	/* XXX: Update scalars as required here... */
}

/**
* @fn u_char * var_m2uaAspMIB(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
* @param vp a pointer to the entry in the variables table for the requested variable.
* @param name the object identifier for which to find.
* @param length the length of the object identifier.
* @param exact whether the name is exact.
* @param var_len a pointer to the length of the representation of the object.
* @param write_method a pointer to a write method for the object.
* @brief locate variables in m2uaAspMIB.
* This function returns a pointer to a memory area that is static across the request that contains
* the UCD-SNMP representation of the scalar (so that it may be used to read from for a GET,
* GET-NEXT or GET-BULK request).  This returned pointer may be NULL, in which case the function is
* telling UCD-SNMP that the scalar does not exist for reading; however, if write_method is
* overwritten with a non-NULL value, the function is telling UCD-SNMP that the scalar exists for
* writing.  Write-only objects can be effected in this way.
*/
u_char *
var_m2uaAspMIB(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct m2uaAspMIB_data *StorageTmp;

	if (header_generic(vp, name, length, exact, var_len, write_method) == MATCH_FAILED)
		return NULL;
	/* Refresh the MIB values if required. */
	refresh_m2uaAspMIB();
	if ((StorageTmp = m2uaAspMIBStorage) == NULL)
		return NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	default:
		ERROR_MSG("");
	}
	return NULL;
}

/**
 * @fn struct m2uaAspTable_data *m2uaAspTable_create(void)
 * @brief create a fresh data structure representing a new row in the m2uaAspTable table.
 * Creates a new m2uaAspTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct m2uaAspTable_data *
m2uaAspTable_create(void)
{
	struct m2uaAspTable_data *StorageNew = SNMP_MALLOC_STRUCT(m2uaAspTable_data);

	DBUGMSGTL(("m2uaAspTable", "creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->m2uaAspRowStatus = RS_NOTREADY;
	}
	DEBUGMSGTL(("m2uaAspTable", "done.\n"));
	return (StorageNew);
}

/**
 * @fn int m2uaAspTable_destroy(struct m2uaAspTable_data **thedata)
 * @brief delete a row structure from a table.
 * @param thedata pointer to the extracted or existing data structure in the table.
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in  thedata will be set to NULL if it is not already
 * NULL.
 */
int
m2uaAspTable_destroy(struct m2uaAspTable_data **thedata)
{
	struct m2uaAspTable_data *StorageDel;

	DEBUGMSGTL(("m2uaAspTable", "deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->m2uaAspId);
		StorageDel->m2uaAspIdLen = 0;
		SNMP_FREE(StorageDel->m2uaAspProceduralStatus);
		StorageDel->m2uaAspProceduralStatusLen = 0;
		SNMP_FREE(StorageDel->m2uaAspCapablities);
		StorageDel->m2uaAspCapablitiesLen = 0;
		SNMP_FREE(StorageDel->m2uaAspName);
		StorageDel->m2uaAspNameLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("m2uaAspTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int m2uaAspTable_add(struct m2uaAspTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the m2uaAspTable table data set.
 * Adds a table row structure to the m2uaAspTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
m2uaAspTable_add(struct m2uaAspTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("m2uaAspTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index
	   the data */
	/* m2uaAspId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->m2uaAspId, thedata->m2uaAspIdLen);
	header_complex_add_data(&m2uaAspTableStorage, vars, thedata);
	DEBUGMSGTL(("m2uaAspTable", "registered an entry\n"));
	DEBUGMSGTL(("m2uaAspTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int m2uaAspTable_del(struct m2uaAspTable_data *thedata)
 * @brief delete a row structure from a table.
 * @param thedata pointer to the extracted or existing data structure in the table.
 * Deletes a table row structure from the m2uaAspTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
m2uaAspTable_del(struct m2uaAspTable_data *thedata)
{
	struct m2uaAspTable_data *StorageDel;

	DEBUGMSGTL(("m2uaAspTable", "deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(m2uaAspTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&m2uaAspTableStorage, hciptr);
	}
	DEBUGMSGTL(("m2uaAspTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_m2uaAspTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for m2uaAspTable entries.
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case m2uaAspTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_m2uaAspTable(const char *token, char *line)
{
	size_t tmpsize;
	struct m2uaAspTable_data *StorageTmp = m2uaAspTable_create();

	DEBUGMSGTL(("m2uaAspTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->m2uaAspId, &StorageTmp->m2uaAspIdLen);
	if (StorageTmp->m2uaAspId == NULL) {
		config_perror("invalid specification for m2uaAspId");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2uaAspAdministrativeState, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2uaAspOperationalState, &tmpsize);
	line = read_config_read_data(ASN_BIT_STR, line, &StorageTmp->m2uaAspProceduralStatus, &StorageTmp->m2uaAspProceduralStatusLen);
	if (StorageTmp->m2uaAspProceduralStatus == NULL) {
		config_perror("invalid specification for m2uaAspProceduralStatus");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2uaAspUsageState, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2uaAspAspState, &tmpsize);
	line = read_config_read_data(ASN_BIT_STR, line, &StorageTmp->m2uaAspCapablities, &StorageTmp->m2uaAspCapablitiesLen);
	if (StorageTmp->m2uaAspCapablities == NULL) {
		config_perror("invalid specification for m2uaAspCapablities");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2uaAspRegistrationPolicy, &tmpsize);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->m2uaAspName, &StorageTmp->m2uaAspNameLen);
	if (StorageTmp->m2uaAspName == NULL) {
		config_perror("invalid specification for m2uaAspName");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2uaAspRowStatus, &tmpsize);
	m2uaAspTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("m2uaAspTable", "done.\n"));
}

/*
 * store_m2uaAspTable(): store configuraiton file for m2uaAspTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_m2uaAspTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct m2uaAspTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("m2uaAspTable", "storing data...  "));
	refresh_m2uaAspTable();
	(void) tmpsize;
	for (hcindex = m2uaAspTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct m2uaAspTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "m2uaAspTable ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->m2uaAspId, &StorageTmp->m2uaAspIdLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2uaAspAdministrativeState, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2uaAspOperationalState, &tmpsize);
			cptr = read_config_store_data(ASN_BIT_STR, cptr, &StorageTmp->m2uaAspProceduralStatus, &StorageTmp->m2uaAspProceduralStatusLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2uaAspUsageState, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2uaAspAspState, &tmpsize);
			cptr = read_config_store_data(ASN_BIT_STR, cptr, &StorageTmp->m2uaAspCapablities, &StorageTmp->m2uaAspCapablitiesLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2uaAspRegistrationPolicy, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->m2uaAspName, &StorageTmp->m2uaAspNameLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2uaAspRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("m2uaAspTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct m2uaSgTable_data *m2uaSgTable_create(void)
 * @brief create a fresh data structure representing a new row in the m2uaSgTable table.
 * Creates a new m2uaSgTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct m2uaSgTable_data *
m2uaSgTable_create(void)
{
	struct m2uaSgTable_data *StorageNew = SNMP_MALLOC_STRUCT(m2uaSgTable_data);

	DBUGMSGTL(("m2uaSgTable", "creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->m2uaSgVersion = 1;
		StorageNew->m2uaSgProtocolPayloadId = 2;
		StorageNew->m2uaSgIpPort = 2904;
		StorageNew->m2uaSgMinOstreams = 32;
		StorageNew->m2uaSgMaxIstreams = 32;
		StorageNew->m2uaSgRowStatus = RS_NOTREADY;
	}
	DEBUGMSGTL(("m2uaSgTable", "done.\n"));
	return (StorageNew);
}

/**
 * @fn int m2uaSgTable_destroy(struct m2uaSgTable_data **thedata)
 * @brief delete a row structure from a table.
 * @param thedata pointer to the extracted or existing data structure in the table.
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in  thedata will be set to NULL if it is not already
 * NULL.
 */
int
m2uaSgTable_destroy(struct m2uaSgTable_data **thedata)
{
	struct m2uaSgTable_data *StorageDel;

	DEBUGMSGTL(("m2uaSgTable", "deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->m2uaSgId);
		StorageDel->m2uaSgIdLen = 0;
		SNMP_FREE(StorageDel->m2uaSgName);
		StorageDel->m2uaSgNameLen = 0;
		SNMP_FREE(StorageDel->m2uaSgProceduralStatus);
		StorageDel->m2uaSgProceduralStatusLen = 0;
		SNMP_FREE(StorageDel->m2uaSgOptions);
		StorageDel->m2uaSgOptionsLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("m2uaSgTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int m2uaSgTable_add(struct m2uaSgTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the m2uaSgTable table data set.
 * Adds a table row structure to the m2uaSgTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
m2uaSgTable_add(struct m2uaSgTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("m2uaSgTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index
	   the data */
	/* m2uaSgId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->m2uaSgId, thedata->m2uaSgIdLen);
	header_complex_add_data(&m2uaSgTableStorage, vars, thedata);
	DEBUGMSGTL(("m2uaSgTable", "registered an entry\n"));
	DEBUGMSGTL(("m2uaSgTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int m2uaSgTable_del(struct m2uaSgTable_data *thedata)
 * @brief delete a row structure from a table.
 * @param thedata pointer to the extracted or existing data structure in the table.
 * Deletes a table row structure from the m2uaSgTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
m2uaSgTable_del(struct m2uaSgTable_data *thedata)
{
	struct m2uaSgTable_data *StorageDel;

	DEBUGMSGTL(("m2uaSgTable", "deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(m2uaSgTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&m2uaSgTableStorage, hciptr);
	}
	DEBUGMSGTL(("m2uaSgTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_m2uaSgTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for m2uaSgTable entries.
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case m2uaSgTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_m2uaSgTable(const char *token, char *line)
{
	size_t tmpsize;
	struct m2uaSgTable_data *StorageTmp = m2uaSgTable_create();

	DEBUGMSGTL(("m2uaSgTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->m2uaSgId, &StorageTmp->m2uaSgIdLen);
	if (StorageTmp->m2uaSgId == NULL) {
		config_perror("invalid specification for m2uaSgId");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->m2uaSgName, &StorageTmp->m2uaSgNameLen);
	if (StorageTmp->m2uaSgName == NULL) {
		config_perror("invalid specification for m2uaSgName");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2uaSgAdministrativeState, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2uaSgOperationalState, &tmpsize);
	line = read_config_read_data(ASN_BIT_STR, line, &StorageTmp->m2uaSgProceduralStatus, &StorageTmp->m2uaSgProceduralStatusLen);
	if (StorageTmp->m2uaSgProceduralStatus == NULL) {
		config_perror("invalid specification for m2uaSgProceduralStatus");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2uaSgStandbyStatus, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->m2uaSgPrimarySg, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2uaSgAspState, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2uaSgUsageState, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2uaSgVersion, &tmpsize);
	line = read_config_read_data(ASN_BIT_STR, line, &StorageTmp->m2uaSgOptions, &StorageTmp->m2uaSgOptionsLen);
	if (StorageTmp->m2uaSgOptions == NULL) {
		config_perror("invalid specification for m2uaSgOptions");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2uaSgRegistrationPolicy, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2uaSgProtocolPayloadId, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2uaSgIpPort, &tmpsize);
	line = read_config_read_data(ASN_IPADDRESS, line, &StorageTmp->m2uaSgPrimaryIpAddress, &StorageTmp->m2uaSgPrimaryIpAddressLen);
	if (StorageTmp->m2uaSgPrimaryIpAddress == NULL) {
		config_perror("invalid specification for m2uaSgPrimaryIpAddress");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2uaSgMinOstreams, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2uaSgMaxIstreams, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2uaSgRowStatus, &tmpsize);
	m2uaSgTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("m2uaSgTable", "done.\n"));
}

/*
 * store_m2uaSgTable(): store configuraiton file for m2uaSgTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_m2uaSgTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct m2uaSgTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("m2uaSgTable", "storing data...  "));
	refresh_m2uaSgTable();
	(void) tmpsize;
	for (hcindex = m2uaSgTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct m2uaSgTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "m2uaSgTable ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->m2uaSgId, &StorageTmp->m2uaSgIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->m2uaSgName, &StorageTmp->m2uaSgNameLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2uaSgAdministrativeState, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2uaSgOperationalState, &tmpsize);
			cptr = read_config_store_data(ASN_BIT_STR, cptr, &StorageTmp->m2uaSgProceduralStatus, &StorageTmp->m2uaSgProceduralStatusLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2uaSgStandbyStatus, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->m2uaSgPrimarySg, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2uaSgAspState, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2uaSgUsageState, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2uaSgVersion, &tmpsize);
			cptr = read_config_store_data(ASN_BIT_STR, cptr, &StorageTmp->m2uaSgOptions, &StorageTmp->m2uaSgOptionsLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2uaSgRegistrationPolicy, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2uaSgProtocolPayloadId, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2uaSgIpPort, &tmpsize);
			cptr = read_config_store_data(ASN_IPADDRESS, cptr, &StorageTmp->m2uaSgPrimaryIpAddress, &StorageTmp->m2uaSgPrimaryIpAddressLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2uaSgMinOstreams, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2uaSgMaxIstreams, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2uaSgRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("m2uaSgTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct m2uaAspSgTable_data *m2uaAspSgTable_create(void)
 * @brief create a fresh data structure representing a new row in the m2uaAspSgTable table.
 * Creates a new m2uaAspSgTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct m2uaAspSgTable_data *
m2uaAspSgTable_create(void)
{
	struct m2uaAspSgTable_data *StorageNew = SNMP_MALLOC_STRUCT(m2uaAspSgTable_data);

	DBUGMSGTL(("m2uaAspSgTable", "creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->m2uaAspSgRowStatus = RS_NOTREADY;
	}
	DEBUGMSGTL(("m2uaAspSgTable", "done.\n"));
	return (StorageNew);
}

/**
 * @fn int m2uaAspSgTable_destroy(struct m2uaAspSgTable_data **thedata)
 * @brief delete a row structure from a table.
 * @param thedata pointer to the extracted or existing data structure in the table.
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in  thedata will be set to NULL if it is not already
 * NULL.
 */
int
m2uaAspSgTable_destroy(struct m2uaAspSgTable_data **thedata)
{
	struct m2uaAspSgTable_data *StorageDel;

	DEBUGMSGTL(("m2uaAspSgTable", "deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->m2uaAspId);
		StorageDel->m2uaAspIdLen = 0;
		SNMP_FREE(StorageDel->m2uaSgId);
		StorageDel->m2uaSgIdLen = 0;
		SNMP_FREE(StorageDel->m2uaAspSgProceduralStatus);
		StorageDel->m2uaAspSgProceduralStatusLen = 0;
		SNMP_FREE(StorageDel->m2uaAspSgAvailabilityStatus);
		StorageDel->m2uaAspSgAvailabilityStatusLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("m2uaAspSgTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int m2uaAspSgTable_add(struct m2uaAspSgTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the m2uaAspSgTable table data set.
 * Adds a table row structure to the m2uaAspSgTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
m2uaAspSgTable_add(struct m2uaAspSgTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("m2uaAspSgTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index
	   the data */
	/* m2uaAspId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->m2uaAspId, thedata->m2uaAspIdLen);
	/* m2uaSgId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->m2uaSgId, thedata->m2uaSgIdLen);
	header_complex_add_data(&m2uaAspSgTableStorage, vars, thedata);
	DEBUGMSGTL(("m2uaAspSgTable", "registered an entry\n"));
	DEBUGMSGTL(("m2uaAspSgTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int m2uaAspSgTable_del(struct m2uaAspSgTable_data *thedata)
 * @brief delete a row structure from a table.
 * @param thedata pointer to the extracted or existing data structure in the table.
 * Deletes a table row structure from the m2uaAspSgTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
m2uaAspSgTable_del(struct m2uaAspSgTable_data *thedata)
{
	struct m2uaAspSgTable_data *StorageDel;

	DEBUGMSGTL(("m2uaAspSgTable", "deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(m2uaAspSgTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&m2uaAspSgTableStorage, hciptr);
	}
	DEBUGMSGTL(("m2uaAspSgTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_m2uaAspSgTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for m2uaAspSgTable entries.
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case m2uaAspSgTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_m2uaAspSgTable(const char *token, char *line)
{
	size_t tmpsize;
	struct m2uaAspSgTable_data *StorageTmp = m2uaAspSgTable_create();

	DEBUGMSGTL(("m2uaAspSgTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->m2uaAspId, &StorageTmp->m2uaAspIdLen);
	if (StorageTmp->m2uaAspId == NULL) {
		config_perror("invalid specification for m2uaAspId");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->m2uaSgId, &StorageTmp->m2uaSgIdLen);
	if (StorageTmp->m2uaSgId == NULL) {
		config_perror("invalid specification for m2uaSgId");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2uaAspSgAdminstrativeState, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2uaAspSgOperationalState, &tmpsize);
	line = read_config_read_data(ASN_BIT_STR, line, &StorageTmp->m2uaAspSgProceduralStatus, &StorageTmp->m2uaAspSgProceduralStatusLen);
	if (StorageTmp->m2uaAspSgProceduralStatus == NULL) {
		config_perror("invalid specification for m2uaAspSgProceduralStatus");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2uaAspSgUsageState, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2uaAspSgOperationalState, &tmpsize);
	line = read_config_read_data(ASN_BIT_STR, line, &StorageTmp->m2uaAspSgAvailabilityStatus, &StorageTmp->m2uaAspSgAvailabilityStatusLen);
	if (StorageTmp->m2uaAspSgAvailabilityStatus == NULL) {
		config_perror("invalid specification for m2uaAspSgAvailabilityStatus");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2uaAspSgAspState, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2uaAspSgAspIdPolicy, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->m2uaAspSgAspId, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2uaAspSgAssociationPolicy, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2uaAspSgIpPort, &tmpsize);
	line = read_config_read_data(ASN_IPADDRESS, line, &StorageTmp->m2uaAspSgPrimaryIpAddress, &StorageTmp->m2uaAspSgPrimaryIpAddressLen);
	if (StorageTmp->m2uaAspSgPrimaryIpAddress == NULL) {
		config_perror("invalid specification for m2uaAspSgPrimaryIpAddress");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2uaAspSgRowStatus, &tmpsize);
	m2uaAspSgTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("m2uaAspSgTable", "done.\n"));
}

/*
 * store_m2uaAspSgTable(): store configuraiton file for m2uaAspSgTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_m2uaAspSgTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct m2uaAspSgTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("m2uaAspSgTable", "storing data...  "));
	refresh_m2uaAspSgTable();
	(void) tmpsize;
	for (hcindex = m2uaAspSgTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct m2uaAspSgTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "m2uaAspSgTable ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->m2uaAspId, &StorageTmp->m2uaAspIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->m2uaSgId, &StorageTmp->m2uaSgIdLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2uaAspSgAdminstrativeState, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2uaAspSgOperationalState, &tmpsize);
			cptr = read_config_store_data(ASN_BIT_STR, cptr, &StorageTmp->m2uaAspSgProceduralStatus, &StorageTmp->m2uaAspSgProceduralStatusLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2uaAspSgUsageState, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2uaAspSgOperationalState, &tmpsize);
			cptr = read_config_store_data(ASN_BIT_STR, cptr, &StorageTmp->m2uaAspSgAvailabilityStatus, &StorageTmp->m2uaAspSgAvailabilityStatusLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2uaAspSgAspState, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2uaAspSgAspIdPolicy, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->m2uaAspSgAspId, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2uaAspSgAssociationPolicy, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2uaAspSgIpPort, &tmpsize);
			cptr = read_config_store_data(ASN_IPADDRESS, cptr, &StorageTmp->m2uaAspSgPrimaryIpAddress, &StorageTmp->m2uaAspSgPrimaryIpAddressLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2uaAspSgRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("m2uaAspSgTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct m2uaAsTable_data *m2uaAsTable_create(void)
 * @brief create a fresh data structure representing a new row in the m2uaAsTable table.
 * Creates a new m2uaAsTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct m2uaAsTable_data *
m2uaAsTable_create(void)
{
	struct m2uaAsTable_data *StorageNew = SNMP_MALLOC_STRUCT(m2uaAsTable_data);

	DBUGMSGTL(("m2uaAsTable", "creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->m2uaAsRowStatus = RS_NOTREADY;
	}
	DEBUGMSGTL(("m2uaAsTable", "done.\n"));
	return (StorageNew);
}

/**
 * @fn int m2uaAsTable_destroy(struct m2uaAsTable_data **thedata)
 * @brief delete a row structure from a table.
 * @param thedata pointer to the extracted or existing data structure in the table.
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in  thedata will be set to NULL if it is not already
 * NULL.
 */
int
m2uaAsTable_destroy(struct m2uaAsTable_data **thedata)
{
	struct m2uaAsTable_data *StorageDel;

	DEBUGMSGTL(("m2uaAsTable", "deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->m2uaAsId);
		StorageDel->m2uaAsIdLen = 0;
		SNMP_FREE(StorageDel->m2uaAsName);
		StorageDel->m2uaAsNameLen = 0;
		SNMP_FREE(StorageDel->m2uaAsTrafficMode);
		StorageDel->m2uaAsTrafficModeLen = 0;
		SNMP_FREE(StorageDel->m2uaAsProceduralStatus);
		StorageDel->m2uaAsProceduralStatusLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("m2uaAsTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int m2uaAsTable_add(struct m2uaAsTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the m2uaAsTable table data set.
 * Adds a table row structure to the m2uaAsTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
m2uaAsTable_add(struct m2uaAsTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("m2uaAsTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index
	   the data */
	/* m2uaAsId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->m2uaAsId, thedata->m2uaAsIdLen);
	header_complex_add_data(&m2uaAsTableStorage, vars, thedata);
	DEBUGMSGTL(("m2uaAsTable", "registered an entry\n"));
	DEBUGMSGTL(("m2uaAsTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int m2uaAsTable_del(struct m2uaAsTable_data *thedata)
 * @brief delete a row structure from a table.
 * @param thedata pointer to the extracted or existing data structure in the table.
 * Deletes a table row structure from the m2uaAsTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
m2uaAsTable_del(struct m2uaAsTable_data *thedata)
{
	struct m2uaAsTable_data *StorageDel;

	DEBUGMSGTL(("m2uaAsTable", "deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(m2uaAsTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&m2uaAsTableStorage, hciptr);
	}
	DEBUGMSGTL(("m2uaAsTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_m2uaAsTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for m2uaAsTable entries.
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case m2uaAsTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_m2uaAsTable(const char *token, char *line)
{
	size_t tmpsize;
	struct m2uaAsTable_data *StorageTmp = m2uaAsTable_create();

	DEBUGMSGTL(("m2uaAsTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->m2uaAsId, &StorageTmp->m2uaAsIdLen);
	if (StorageTmp->m2uaAsId == NULL) {
		config_perror("invalid specification for m2uaAsId");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->m2uaAsName, &StorageTmp->m2uaAsNameLen);
	if (StorageTmp->m2uaAsName == NULL) {
		config_perror("invalid specification for m2uaAsName");
		return;
	}
	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->m2uaAsTrafficMode, &StorageTmp->m2uaAsTrafficModeLen);
	if (StorageTmp->m2uaAsTrafficMode == NULL) {
		config_perror("invalid specification for m2uaAsTrafficMode");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2uaAsAdministrativeState, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2uaAsOperationalState, &tmpsize);
	line = read_config_read_data(ASN_BIT_STR, line, &StorageTmp->m2uaAsProceduralStatus, &StorageTmp->m2uaAsProceduralStatusLen);
	if (StorageTmp->m2uaAsProceduralStatus == NULL) {
		config_perror("invalid specification for m2uaAsProceduralStatus");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2uaAsUsageState, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2uaAsAsState, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2uaAsRowStatus, &tmpsize);
	m2uaAsTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("m2uaAsTable", "done.\n"));
}

/*
 * store_m2uaAsTable(): store configuraiton file for m2uaAsTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_m2uaAsTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct m2uaAsTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("m2uaAsTable", "storing data...  "));
	refresh_m2uaAsTable();
	(void) tmpsize;
	for (hcindex = m2uaAsTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct m2uaAsTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "m2uaAsTable ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->m2uaAsId, &StorageTmp->m2uaAsIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->m2uaAsName, &StorageTmp->m2uaAsNameLen);
			cptr = read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->m2uaAsTrafficMode, &StorageTmp->m2uaAsTrafficModeLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2uaAsAdministrativeState, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2uaAsOperationalState, &tmpsize);
			cptr = read_config_store_data(ASN_BIT_STR, cptr, &StorageTmp->m2uaAsProceduralStatus, &StorageTmp->m2uaAsProceduralStatusLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2uaAsUsageState, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2uaAsAsState, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2uaAsRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("m2uaAsTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct m2uaIfTable_data *m2uaIfTable_create(void)
 * @brief create a fresh data structure representing a new row in the m2uaIfTable table.
 * Creates a new m2uaIfTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct m2uaIfTable_data *
m2uaIfTable_create(void)
{
	struct m2uaIfTable_data *StorageNew = SNMP_MALLOC_STRUCT(m2uaIfTable_data);

	DBUGMSGTL(("m2uaIfTable", "creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->m2uaIfRowStatus = RS_NOTREADY;
	}
	DEBUGMSGTL(("m2uaIfTable", "done.\n"));
	return (StorageNew);
}

/**
 * @fn int m2uaIfTable_destroy(struct m2uaIfTable_data **thedata)
 * @brief delete a row structure from a table.
 * @param thedata pointer to the extracted or existing data structure in the table.
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in  thedata will be set to NULL if it is not already
 * NULL.
 */
int
m2uaIfTable_destroy(struct m2uaIfTable_data **thedata)
{
	struct m2uaIfTable_data *StorageDel;

	DEBUGMSGTL(("m2uaIfTable", "deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->m2uaIfId);
		StorageDel->m2uaIfIdLen = 0;
		SNMP_FREE(StorageDel->m2uaIfIdentifier);
		StorageDel->m2uaIfIdentifierLen = 0;
		SNMP_FREE(StorageDel->m2uaIfName);
		StorageDel->m2uaIfNameLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("m2uaIfTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int m2uaIfTable_add(struct m2uaIfTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the m2uaIfTable table data set.
 * Adds a table row structure to the m2uaIfTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
m2uaIfTable_add(struct m2uaIfTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("m2uaIfTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index
	   the data */
	/* m2uaIfId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->m2uaIfId, thedata->m2uaIfIdLen);
	header_complex_add_data(&m2uaIfTableStorage, vars, thedata);
	DEBUGMSGTL(("m2uaIfTable", "registered an entry\n"));
	DEBUGMSGTL(("m2uaIfTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int m2uaIfTable_del(struct m2uaIfTable_data *thedata)
 * @brief delete a row structure from a table.
 * @param thedata pointer to the extracted or existing data structure in the table.
 * Deletes a table row structure from the m2uaIfTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
m2uaIfTable_del(struct m2uaIfTable_data *thedata)
{
	struct m2uaIfTable_data *StorageDel;

	DEBUGMSGTL(("m2uaIfTable", "deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(m2uaIfTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&m2uaIfTableStorage, hciptr);
	}
	DEBUGMSGTL(("m2uaIfTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_m2uaIfTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for m2uaIfTable entries.
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case m2uaIfTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_m2uaIfTable(const char *token, char *line)
{
	size_t tmpsize;
	struct m2uaIfTable_data *StorageTmp = m2uaIfTable_create();

	DEBUGMSGTL(("m2uaIfTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->m2uaIfId, &StorageTmp->m2uaIfIdLen);
	if (StorageTmp->m2uaIfId == NULL) {
		config_perror("invalid specification for m2uaIfId");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->m2uaIfAsIndex, &tmpsize);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->m2uaIfIdentifier, &StorageTmp->m2uaIfIdentifierLen);
	if (StorageTmp->m2uaIfIdentifier == NULL) {
		config_perror("invalid specification for m2uaIfIdentifier");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->m2uaIfName, &StorageTmp->m2uaIfNameLen);
	if (StorageTmp->m2uaIfName == NULL) {
		config_perror("invalid specification for m2uaIfName");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2uaIfRowStatus, &tmpsize);
	m2uaIfTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("m2uaIfTable", "done.\n"));
}

/*
 * store_m2uaIfTable(): store configuraiton file for m2uaIfTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_m2uaIfTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct m2uaIfTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("m2uaIfTable", "storing data...  "));
	refresh_m2uaIfTable();
	(void) tmpsize;
	for (hcindex = m2uaIfTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct m2uaIfTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "m2uaIfTable ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->m2uaIfId, &StorageTmp->m2uaIfIdLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->m2uaIfAsIndex, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->m2uaIfIdentifier, &StorageTmp->m2uaIfIdentifierLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->m2uaIfName, &StorageTmp->m2uaIfNameLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2uaIfRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("m2uaIfTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct m2uaAsIfTable_data *m2uaAsIfTable_create(void)
 * @brief create a fresh data structure representing a new row in the m2uaAsIfTable table.
 * Creates a new m2uaAsIfTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct m2uaAsIfTable_data *
m2uaAsIfTable_create(void)
{
	struct m2uaAsIfTable_data *StorageNew = SNMP_MALLOC_STRUCT(m2uaAsIfTable_data);

	DBUGMSGTL(("m2uaAsIfTable", "creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */

	}
	DEBUGMSGTL(("m2uaAsIfTable", "done.\n"));
	return (StorageNew);
}

/**
 * @fn int m2uaAsIfTable_destroy(struct m2uaAsIfTable_data **thedata)
 * @brief delete a row structure from a table.
 * @param thedata pointer to the extracted or existing data structure in the table.
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in  thedata will be set to NULL if it is not already
 * NULL.
 */
int
m2uaAsIfTable_destroy(struct m2uaAsIfTable_data **thedata)
{
	struct m2uaAsIfTable_data *StorageDel;

	DEBUGMSGTL(("m2uaAsIfTable", "deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->m2uaAsId);
		StorageDel->m2uaAsIdLen = 0;
		SNMP_FREE(StorageDel->m2uaIfId);
		StorageDel->m2uaIfIdLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("m2uaAsIfTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int m2uaAsIfTable_add(struct m2uaAsIfTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the m2uaAsIfTable table data set.
 * Adds a table row structure to the m2uaAsIfTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
m2uaAsIfTable_add(struct m2uaAsIfTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("m2uaAsIfTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index
	   the data */
	/* m2uaAsId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->m2uaAsId, thedata->m2uaAsIdLen);
	/* m2uaIfId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->m2uaIfId, thedata->m2uaIfIdLen);
	header_complex_add_data(&m2uaAsIfTableStorage, vars, thedata);
	DEBUGMSGTL(("m2uaAsIfTable", "registered an entry\n"));
	DEBUGMSGTL(("m2uaAsIfTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int m2uaAsIfTable_del(struct m2uaAsIfTable_data *thedata)
 * @brief delete a row structure from a table.
 * @param thedata pointer to the extracted or existing data structure in the table.
 * Deletes a table row structure from the m2uaAsIfTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
m2uaAsIfTable_del(struct m2uaAsIfTable_data *thedata)
{
	struct m2uaAsIfTable_data *StorageDel;

	DEBUGMSGTL(("m2uaAsIfTable", "deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(m2uaAsIfTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&m2uaAsIfTableStorage, hciptr);
	}
	DEBUGMSGTL(("m2uaAsIfTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_m2uaAsIfTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for m2uaAsIfTable entries.
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case m2uaAsIfTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_m2uaAsIfTable(const char *token, char *line)
{
	size_t tmpsize;
	struct m2uaAsIfTable_data *StorageTmp = m2uaAsIfTable_create();

	DEBUGMSGTL(("m2uaAsIfTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->m2uaAsId, &StorageTmp->m2uaAsIdLen);
	if (StorageTmp->m2uaAsId == NULL) {
		config_perror("invalid specification for m2uaAsId");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->m2uaIfId, &StorageTmp->m2uaIfIdLen);
	if (StorageTmp->m2uaIfId == NULL) {
		config_perror("invalid specification for m2uaIfId");
		return;
	}
	m2uaAsIfTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("m2uaAsIfTable", "done.\n"));
}

/*
 * store_m2uaAsIfTable(): store configuraiton file for m2uaAsIfTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_m2uaAsIfTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct m2uaAsIfTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("m2uaAsIfTable", "storing data...  "));
	refresh_m2uaAsIfTable();
	(void) tmpsize;
	for (hcindex = m2uaAsIfTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct m2uaAsIfTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "m2uaAsIfTable ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->m2uaAsId, &StorageTmp->m2uaAsIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->m2uaIfId, &StorageTmp->m2uaIfIdLen);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("m2uaAsIfTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct m2uaSgAsTable_data *m2uaSgAsTable_create(void)
 * @brief create a fresh data structure representing a new row in the m2uaSgAsTable table.
 * Creates a new m2uaSgAsTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct m2uaSgAsTable_data *
m2uaSgAsTable_create(void)
{
	struct m2uaSgAsTable_data *StorageNew = SNMP_MALLOC_STRUCT(m2uaSgAsTable_data);

	DBUGMSGTL(("m2uaSgAsTable", "creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->m2uaSgAsRowStatus = RS_NOTREADY;
	}
	DEBUGMSGTL(("m2uaSgAsTable", "done.\n"));
	return (StorageNew);
}

/**
 * @fn int m2uaSgAsTable_destroy(struct m2uaSgAsTable_data **thedata)
 * @brief delete a row structure from a table.
 * @param thedata pointer to the extracted or existing data structure in the table.
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in  thedata will be set to NULL if it is not already
 * NULL.
 */
int
m2uaSgAsTable_destroy(struct m2uaSgAsTable_data **thedata)
{
	struct m2uaSgAsTable_data *StorageDel;

	DEBUGMSGTL(("m2uaSgAsTable", "deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->m2uaSgId);
		StorageDel->m2uaSgIdLen = 0;
		SNMP_FREE(StorageDel->m2uaAsId);
		StorageDel->m2uaAsIdLen = 0;
		SNMP_FREE(StorageDel->m2uaSgAsProceduralStatus);
		StorageDel->m2uaSgAsProceduralStatusLen = 0;
		SNMP_FREE(StorageDel->m2uaSgAsAvailabilityStatus);
		StorageDel->m2uaSgAsAvailabilityStatusLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("m2uaSgAsTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int m2uaSgAsTable_add(struct m2uaSgAsTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the m2uaSgAsTable table data set.
 * Adds a table row structure to the m2uaSgAsTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
m2uaSgAsTable_add(struct m2uaSgAsTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("m2uaSgAsTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index
	   the data */
	/* m2uaSgId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->m2uaSgId, thedata->m2uaSgIdLen);
	/* m2uaAsId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->m2uaAsId, thedata->m2uaAsIdLen);
	header_complex_add_data(&m2uaSgAsTableStorage, vars, thedata);
	DEBUGMSGTL(("m2uaSgAsTable", "registered an entry\n"));
	DEBUGMSGTL(("m2uaSgAsTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int m2uaSgAsTable_del(struct m2uaSgAsTable_data *thedata)
 * @brief delete a row structure from a table.
 * @param thedata pointer to the extracted or existing data structure in the table.
 * Deletes a table row structure from the m2uaSgAsTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
m2uaSgAsTable_del(struct m2uaSgAsTable_data *thedata)
{
	struct m2uaSgAsTable_data *StorageDel;

	DEBUGMSGTL(("m2uaSgAsTable", "deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(m2uaSgAsTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&m2uaSgAsTableStorage, hciptr);
	}
	DEBUGMSGTL(("m2uaSgAsTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_m2uaSgAsTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for m2uaSgAsTable entries.
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case m2uaSgAsTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_m2uaSgAsTable(const char *token, char *line)
{
	size_t tmpsize;
	struct m2uaSgAsTable_data *StorageTmp = m2uaSgAsTable_create();

	DEBUGMSGTL(("m2uaSgAsTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->m2uaSgId, &StorageTmp->m2uaSgIdLen);
	if (StorageTmp->m2uaSgId == NULL) {
		config_perror("invalid specification for m2uaSgId");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->m2uaAsId, &StorageTmp->m2uaAsIdLen);
	if (StorageTmp->m2uaAsId == NULL) {
		config_perror("invalid specification for m2uaAsId");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->m2uaSgAsOrdering, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2uaSgAsAdministrativeState, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2uaSgAsOperationalState, &tmpsize);
	line = read_config_read_data(ASN_BIT_STR, line, &StorageTmp->m2uaSgAsProceduralStatus, &StorageTmp->m2uaSgAsProceduralStatusLen);
	if (StorageTmp->m2uaSgAsProceduralStatus == NULL) {
		config_perror("invalid specification for m2uaSgAsProceduralStatus");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2uaSgAsUsageState, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2uaSgAsOperationalState, &tmpsize);
	line = read_config_read_data(ASN_BIT_STR, line, &StorageTmp->m2uaSgAsAvailabilityStatus, &StorageTmp->m2uaSgAsAvailabilityStatusLen);
	if (StorageTmp->m2uaSgAsAvailabilityStatus == NULL) {
		config_perror("invalid specification for m2uaSgAsAvailabilityStatus");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2uaSgAsStandbyStatus, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->m2uaSgAsPrimarySg, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2uaSgAsAsState, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2uaSgAsRowStatus, &tmpsize);
	m2uaSgAsTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("m2uaSgAsTable", "done.\n"));
}

/*
 * store_m2uaSgAsTable(): store configuraiton file for m2uaSgAsTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_m2uaSgAsTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct m2uaSgAsTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("m2uaSgAsTable", "storing data...  "));
	refresh_m2uaSgAsTable();
	(void) tmpsize;
	for (hcindex = m2uaSgAsTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct m2uaSgAsTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "m2uaSgAsTable ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->m2uaSgId, &StorageTmp->m2uaSgIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->m2uaAsId, &StorageTmp->m2uaAsIdLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->m2uaSgAsOrdering, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2uaSgAsAdministrativeState, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2uaSgAsOperationalState, &tmpsize);
			cptr = read_config_store_data(ASN_BIT_STR, cptr, &StorageTmp->m2uaSgAsProceduralStatus, &StorageTmp->m2uaSgAsProceduralStatusLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2uaSgAsUsageState, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2uaSgAsOperationalState, &tmpsize);
			cptr = read_config_store_data(ASN_BIT_STR, cptr, &StorageTmp->m2uaSgAsAvailabilityStatus, &StorageTmp->m2uaSgAsAvailabilityStatusLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2uaSgAsStandbyStatus, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->m2uaSgAsPrimarySg, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2uaSgAsAsState, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2uaSgAsRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("m2uaSgAsTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct m2uaAspAsTable_data *m2uaAspAsTable_create(void)
 * @brief create a fresh data structure representing a new row in the m2uaAspAsTable table.
 * Creates a new m2uaAspAsTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct m2uaAspAsTable_data *
m2uaAspAsTable_create(void)
{
	struct m2uaAspAsTable_data *StorageNew = SNMP_MALLOC_STRUCT(m2uaAspAsTable_data);

	DBUGMSGTL(("m2uaAspAsTable", "creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->m2uaAspAsAsState = RS_NOTREADY;
	}
	DEBUGMSGTL(("m2uaAspAsTable", "done.\n"));
	return (StorageNew);
}

/**
 * @fn int m2uaAspAsTable_destroy(struct m2uaAspAsTable_data **thedata)
 * @brief delete a row structure from a table.
 * @param thedata pointer to the extracted or existing data structure in the table.
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in  thedata will be set to NULL if it is not already
 * NULL.
 */
int
m2uaAspAsTable_destroy(struct m2uaAspAsTable_data **thedata)
{
	struct m2uaAspAsTable_data *StorageDel;

	DEBUGMSGTL(("m2uaAspAsTable", "deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->m2uaAspId);
		StorageDel->m2uaAspIdLen = 0;
		SNMP_FREE(StorageDel->m2uaAsId);
		StorageDel->m2uaAsIdLen = 0;
		SNMP_FREE(StorageDel->m2uaAspAsProceduralStatus);
		StorageDel->m2uaAspAsProceduralStatusLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("m2uaAspAsTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int m2uaAspAsTable_add(struct m2uaAspAsTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the m2uaAspAsTable table data set.
 * Adds a table row structure to the m2uaAspAsTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
m2uaAspAsTable_add(struct m2uaAspAsTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("m2uaAspAsTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index
	   the data */
	/* m2uaAspId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->m2uaAspId, thedata->m2uaAspIdLen);
	/* m2uaAsId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->m2uaAsId, thedata->m2uaAsIdLen);
	header_complex_add_data(&m2uaAspAsTableStorage, vars, thedata);
	DEBUGMSGTL(("m2uaAspAsTable", "registered an entry\n"));
	DEBUGMSGTL(("m2uaAspAsTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int m2uaAspAsTable_del(struct m2uaAspAsTable_data *thedata)
 * @brief delete a row structure from a table.
 * @param thedata pointer to the extracted or existing data structure in the table.
 * Deletes a table row structure from the m2uaAspAsTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
m2uaAspAsTable_del(struct m2uaAspAsTable_data *thedata)
{
	struct m2uaAspAsTable_data *StorageDel;

	DEBUGMSGTL(("m2uaAspAsTable", "deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(m2uaAspAsTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&m2uaAspAsTableStorage, hciptr);
	}
	DEBUGMSGTL(("m2uaAspAsTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_m2uaAspAsTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for m2uaAspAsTable entries.
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case m2uaAspAsTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_m2uaAspAsTable(const char *token, char *line)
{
	size_t tmpsize;
	struct m2uaAspAsTable_data *StorageTmp = m2uaAspAsTable_create();

	DEBUGMSGTL(("m2uaAspAsTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->m2uaAspId, &StorageTmp->m2uaAspIdLen);
	if (StorageTmp->m2uaAspId == NULL) {
		config_perror("invalid specification for m2uaAspId");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->m2uaAsId, &StorageTmp->m2uaAsIdLen);
	if (StorageTmp->m2uaAsId == NULL) {
		config_perror("invalid specification for m2uaAsId");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2uaAspAsAdministrativeState, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2uaAspAsOperationalState, &tmpsize);
	line = read_config_read_data(ASN_BIT_STR, line, &StorageTmp->m2uaAspAsProceduralStatus, &StorageTmp->m2uaAspAsProceduralStatusLen);
	if (StorageTmp->m2uaAspAsProceduralStatus == NULL) {
		config_perror("invalid specification for m2uaAspAsProceduralStatus");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2uaAspAsUsageState, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2uaAspAsAsState, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2uaAspAsRowStatus, &tmpsize);
	m2uaAspAsTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("m2uaAspAsTable", "done.\n"));
}

/*
 * store_m2uaAspAsTable(): store configuraiton file for m2uaAspAsTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_m2uaAspAsTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct m2uaAspAsTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("m2uaAspAsTable", "storing data...  "));
	refresh_m2uaAspAsTable();
	(void) tmpsize;
	for (hcindex = m2uaAspAsTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct m2uaAspAsTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "m2uaAspAsTable ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->m2uaAspId, &StorageTmp->m2uaAspIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->m2uaAsId, &StorageTmp->m2uaAsIdLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2uaAspAsAdministrativeState, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2uaAspAsOperationalState, &tmpsize);
			cptr = read_config_store_data(ASN_BIT_STR, cptr, &StorageTmp->m2uaAspAsProceduralStatus, &StorageTmp->m2uaAspAsProceduralStatusLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2uaAspAsUsageState, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2uaAspAsAsState, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2uaAspAsRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("m2uaAspAsTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct m2uaAspSgAsTable_data *m2uaAspSgAsTable_create(void)
 * @brief create a fresh data structure representing a new row in the m2uaAspSgAsTable table.
 * Creates a new m2uaAspSgAsTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct m2uaAspSgAsTable_data *
m2uaAspSgAsTable_create(void)
{
	struct m2uaAspSgAsTable_data *StorageNew = SNMP_MALLOC_STRUCT(m2uaAspSgAsTable_data);

	DBUGMSGTL(("m2uaAspSgAsTable", "creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */

	}
	DEBUGMSGTL(("m2uaAspSgAsTable", "done.\n"));
	return (StorageNew);
}

/**
 * @fn int m2uaAspSgAsTable_destroy(struct m2uaAspSgAsTable_data **thedata)
 * @brief delete a row structure from a table.
 * @param thedata pointer to the extracted or existing data structure in the table.
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in  thedata will be set to NULL if it is not already
 * NULL.
 */
int
m2uaAspSgAsTable_destroy(struct m2uaAspSgAsTable_data **thedata)
{
	struct m2uaAspSgAsTable_data *StorageDel;

	DEBUGMSGTL(("m2uaAspSgAsTable", "deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->m2uaAspId);
		StorageDel->m2uaAspIdLen = 0;
		SNMP_FREE(StorageDel->m2uaSgId);
		StorageDel->m2uaSgIdLen = 0;
		SNMP_FREE(StorageDel->m2uaAsId);
		StorageDel->m2uaAsIdLen = 0;
		SNMP_FREE(StorageDel->m2uaAspSgAsProceduralStatus);
		StorageDel->m2uaAspSgAsProceduralStatusLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("m2uaAspSgAsTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int m2uaAspSgAsTable_add(struct m2uaAspSgAsTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the m2uaAspSgAsTable table data set.
 * Adds a table row structure to the m2uaAspSgAsTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
m2uaAspSgAsTable_add(struct m2uaAspSgAsTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("m2uaAspSgAsTable", "adding data...  "));
	/* add the index variables to the varbind list, which is used by header_complex to index
	   the data */
	/* m2uaAspId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->m2uaAspId, thedata->m2uaAspIdLen);
	/* m2uaSgId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->m2uaSgId, thedata->m2uaSgIdLen);
	/* m2uaAsId */
	snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, (u_char *) thedata->m2uaAsId, thedata->m2uaAsIdLen);
	header_complex_add_data(&m2uaAspSgAsTableStorage, vars, thedata);
	DEBUGMSGTL(("m2uaAspSgAsTable", "registered an entry\n"));
	DEBUGMSGTL(("m2uaAspSgAsTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int m2uaAspSgAsTable_del(struct m2uaAspSgAsTable_data *thedata)
 * @brief delete a row structure from a table.
 * @param thedata pointer to the extracted or existing data structure in the table.
 * Deletes a table row structure from the m2uaAspSgAsTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
m2uaAspSgAsTable_del(struct m2uaAspSgAsTable_data *thedata)
{
	struct m2uaAspSgAsTable_data *StorageDel;

	DEBUGMSGTL(("m2uaAspSgAsTable", "deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(m2uaAspSgAsTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&m2uaAspSgAsTableStorage, hciptr);
	}
	DEBUGMSGTL(("m2uaAspSgAsTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_m2uaAspSgAsTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for m2uaAspSgAsTable entries.
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case m2uaAspSgAsTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_m2uaAspSgAsTable(const char *token, char *line)
{
	size_t tmpsize;
	struct m2uaAspSgAsTable_data *StorageTmp = m2uaAspSgAsTable_create();

	DEBUGMSGTL(("m2uaAspSgAsTable", "parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->m2uaAspId, &StorageTmp->m2uaAspIdLen);
	if (StorageTmp->m2uaAspId == NULL) {
		config_perror("invalid specification for m2uaAspId");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->m2uaSgId, &StorageTmp->m2uaSgIdLen);
	if (StorageTmp->m2uaSgId == NULL) {
		config_perror("invalid specification for m2uaSgId");
		return;
	}
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->m2uaAsId, &StorageTmp->m2uaAsIdLen);
	if (StorageTmp->m2uaAsId == NULL) {
		config_perror("invalid specification for m2uaAsId");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2uaAspSgAsAdministrativeState, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2uaAspSgAsOperationalState, &tmpsize);
	line = read_config_read_data(ASN_BIT_STR, line, &StorageTmp->m2uaAspSgAsProceduralStatus, &StorageTmp->m2uaAspSgAsProceduralStatusLen);
	if (StorageTmp->m2uaAspSgAsProceduralStatus == NULL) {
		config_perror("invalid specification for m2uaAspSgAsProceduralStatus");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2uaAspSgAsUsageState, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2uaAspSgAsStandbyStatus, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m2uaAspSgAsRowStatus, &tmpsize);
	m2uaAspSgAsTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("m2uaAspSgAsTable", "done.\n"));
}

/*
 * store_m2uaAspSgAsTable(): store configuraiton file for m2uaAspSgAsTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_m2uaAspSgAsTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct m2uaAspSgAsTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("m2uaAspSgAsTable", "storing data...  "));
	refresh_m2uaAspSgAsTable();
	(void) tmpsize;
	for (hcindex = m2uaAspSgAsTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct m2uaAspSgAsTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "m2uaAspSgAsTable ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->m2uaAspId, &StorageTmp->m2uaAspIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->m2uaSgId, &StorageTmp->m2uaSgIdLen);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->m2uaAsId, &StorageTmp->m2uaAsIdLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2uaAspSgAsAdministrativeState, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2uaAspSgAsOperationalState, &tmpsize);
			cptr = read_config_store_data(ASN_BIT_STR, cptr, &StorageTmp->m2uaAspSgAsProceduralStatus, &StorageTmp->m2uaAspSgAsProceduralStatusLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2uaAspSgAsUsageState, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2uaAspSgAsStandbyStatus, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m2uaAspSgAsRowStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("m2uaAspSgAsTable", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void refresh_m2uaAspTable(void)
 * @brief refresh the scalar values of the m2uaAspTable.
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_m2uaAspTable(void)
{
	if (m2uaAspTable_refresh == 0)
		return;
	m2uaAspTable_refresh = 0;
	/* XXX: Here, update the table as required... */
}

/**
 * @fn void refresh_m2uaAspTable_row(struct m2uaAspTable_data *StorageTmp)
 * @brief refresh the contents of the m2uaAspTable row.
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
void
refresh_m2uaAspTable_row(struct m2uaAspTable_data *StorageTmp)
{
	if (StorageTmp->m2uaAspTable_request == sa_request)
		return;
	StorageTmp->m2uaAspTable_request = sa_request;
}

/**
 * @fn u_char *var_m2uaAspTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in m2uaAspTable.
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_m2uaAspMIB above.
 */
u_char *
var_m2uaAspTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct m2uaAspTable_data *StorageTmp = NULL;

	DEBUGMSGTL(("m2uaAspMIB", "var_m2uaAspTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the
	   header. */
	refresh_m2uaAspTable();
	/* This assumes you have registered all your data properly with header_complex_add()
	   somewhere before this. */
	if ((StorageTmp = header_complex(m2uaAspTableStorage, vp, name, length, exact, var_len, write_method)) == NULL)
		return NULL;
	refresh_m2uaAspTable_row(StorageTmp);
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case M2UAASPADMINISTRATIVESTATE:
		*write_method = write_m2uaAspAdministrativeState;
		*var_len = sizeof(StorageTmp->m2uaAspAdministrativeState);
		return (u_char *) &StorageTmp->m2uaAspAdministrativeState;
	case M2UAASPOPERATIONALSTATE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->m2uaAspOperationalState);
		return (u_char *) &StorageTmp->m2uaAspOperationalState;
	case M2UAASPPROCEDURALSTATUS:
		*write_method = NULL;	/* read-only */
		*var_len = StorageTmp->m2uaAspProceduralStatusLen;
		return (u_char *) StorageTmp->m2uaAspProceduralStatus;
	case M2UAASPUSAGESTATE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->m2uaAspUsageState);
		return (u_char *) &StorageTmp->m2uaAspUsageState;
	case M2UAASPASPSTATE:
		*write_method = write_m2uaAspAspState;
		*var_len = sizeof(StorageTmp->m2uaAspAspState);
		return (u_char *) &StorageTmp->m2uaAspAspState;
	case M2UAASPCAPABLITIES:
		*write_method = write_m2uaAspCapablities;
		*var_len = StorageTmp->m2uaAspCapablitiesLen;
		return (u_char *) StorageTmp->m2uaAspCapablities;
	case M2UAASPREGISTRATIONPOLICY:
		*write_method = write_m2uaAspRegistrationPolicy;
		*var_len = sizeof(StorageTmp->m2uaAspRegistrationPolicy);
		return (u_char *) &StorageTmp->m2uaAspRegistrationPolicy;
	case M2UAASPNAME:
		*write_method = write_m2uaAspName;
		*var_len = StorageTmp->m2uaAspNameLen;
		return (u_char *) StorageTmp->m2uaAspName;
	case M2UAASPROWSTATUS:
		*write_method = write_m2uaAspRowStatus;
		*var_len = sizeof(StorageTmp->m2uaAspRowStatus);
		return (u_char *) &StorageTmp->m2uaAspRowStatus;
	default:
		ERROR_MSG("");
	}
	return NULL;
}

/**
 * @fn void refresh_m2uaSgTable(void)
 * @brief refresh the scalar values of the m2uaSgTable.
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_m2uaSgTable(void)
{
	if (m2uaSgTable_refresh == 0)
		return;
	m2uaSgTable_refresh = 0;
	/* XXX: Here, update the table as required... */
}

/**
 * @fn void refresh_m2uaSgTable_row(struct m2uaSgTable_data *StorageTmp)
 * @brief refresh the contents of the m2uaSgTable row.
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
void
refresh_m2uaSgTable_row(struct m2uaSgTable_data *StorageTmp)
{
	if (StorageTmp->m2uaSgTable_request == sa_request)
		return;
	StorageTmp->m2uaSgTable_request = sa_request;
}

/**
 * @fn u_char *var_m2uaSgTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in m2uaSgTable.
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_m2uaAspMIB above.
 */
u_char *
var_m2uaSgTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct m2uaSgTable_data *StorageTmp = NULL;

	DEBUGMSGTL(("m2uaAspMIB", "var_m2uaSgTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the
	   header. */
	refresh_m2uaSgTable();
	/* This assumes you have registered all your data properly with header_complex_add()
	   somewhere before this. */
	if ((StorageTmp = header_complex(m2uaSgTableStorage, vp, name, length, exact, var_len, write_method)) == NULL)
		return NULL;
	refresh_m2uaSgTable_row(StorageTmp);
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case M2UASGNAME:
		*write_method = write_m2uaSgName;
		*var_len = StorageTmp->m2uaSgNameLen;
		return (u_char *) StorageTmp->m2uaSgName;
	case M2UASGADMINISTRATIVESTATE:
		*write_method = write_m2uaSgAdministrativeState;
		*var_len = sizeof(StorageTmp->m2uaSgAdministrativeState);
		return (u_char *) &StorageTmp->m2uaSgAdministrativeState;
	case M2UASGOPERATIONALSTATE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->m2uaSgOperationalState);
		return (u_char *) &StorageTmp->m2uaSgOperationalState;
	case M2UASGPROCEDURALSTATUS:
		*write_method = NULL;	/* read-only */
		*var_len = StorageTmp->m2uaSgProceduralStatusLen;
		return (u_char *) StorageTmp->m2uaSgProceduralStatus;
	case M2UASGSTANDBYSTATUS:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->m2uaSgStandbyStatus);
		return (u_char *) &StorageTmp->m2uaSgStandbyStatus;
	case M2UASGPRIMARYSG:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->m2uaSgPrimarySg);
		return (u_char *) &StorageTmp->m2uaSgPrimarySg;
	case M2UASGASPSTATE:
		*write_method = write_m2uaSgAspState;
		*var_len = sizeof(StorageTmp->m2uaSgAspState);
		return (u_char *) &StorageTmp->m2uaSgAspState;
	case M2UASGUSAGESTATE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->m2uaSgUsageState);
		return (u_char *) &StorageTmp->m2uaSgUsageState;
	case M2UASGVERSION:
		*write_method = write_m2uaSgVersion;
		*var_len = sizeof(StorageTmp->m2uaSgVersion);
		return (u_char *) &StorageTmp->m2uaSgVersion;
	case M2UASGOPTIONS:
		*write_method = write_m2uaSgOptions;
		*var_len = StorageTmp->m2uaSgOptionsLen;
		return (u_char *) StorageTmp->m2uaSgOptions;
	case M2UASGREGISTRATIONPOLICY:
		*write_method = write_m2uaSgRegistrationPolicy;
		*var_len = sizeof(StorageTmp->m2uaSgRegistrationPolicy);
		return (u_char *) &StorageTmp->m2uaSgRegistrationPolicy;
	case M2UASGPROTOCOLPAYLOADID:
		*write_method = write_m2uaSgProtocolPayloadId;
		*var_len = sizeof(StorageTmp->m2uaSgProtocolPayloadId);
		return (u_char *) &StorageTmp->m2uaSgProtocolPayloadId;
	case M2UASGIPPORT:
		*write_method = write_m2uaSgIpPort;
		*var_len = sizeof(StorageTmp->m2uaSgIpPort);
		return (u_char *) &StorageTmp->m2uaSgIpPort;
	case M2UASGPRIMARYIPADDRESS:
		*write_method = write_m2uaSgPrimaryIpAddress;
		*var_len = StorageTmp->m2uaSgPrimaryIpAddressLen;
		return (u_char *) StorageTmp->m2uaSgPrimaryIpAddress;
	case M2UASGMINOSTREAMS:
		*write_method = write_m2uaSgMinOstreams;
		*var_len = sizeof(StorageTmp->m2uaSgMinOstreams);
		return (u_char *) &StorageTmp->m2uaSgMinOstreams;
	case M2UASGMAXISTREAMS:
		*write_method = write_m2uaSgMaxIstreams;
		*var_len = sizeof(StorageTmp->m2uaSgMaxIstreams);
		return (u_char *) &StorageTmp->m2uaSgMaxIstreams;
	case M2UASGROWSTATUS:
		*write_method = write_m2uaSgRowStatus;
		*var_len = sizeof(StorageTmp->m2uaSgRowStatus);
		return (u_char *) &StorageTmp->m2uaSgRowStatus;
	default:
		ERROR_MSG("");
	}
	return NULL;
}

/**
 * @fn void refresh_m2uaAspSgTable(void)
 * @brief refresh the scalar values of the m2uaAspSgTable.
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_m2uaAspSgTable(void)
{
	if (m2uaAspSgTable_refresh == 0)
		return;
	m2uaAspSgTable_refresh = 0;
	/* XXX: Here, update the table as required... */
}

/**
 * @fn void refresh_m2uaAspSgTable_row(struct m2uaAspSgTable_data *StorageTmp)
 * @brief refresh the contents of the m2uaAspSgTable row.
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
void
refresh_m2uaAspSgTable_row(struct m2uaAspSgTable_data *StorageTmp)
{
	if (StorageTmp->m2uaAspSgTable_request == sa_request)
		return;
	StorageTmp->m2uaAspSgTable_request = sa_request;
}

/**
 * @fn u_char *var_m2uaAspSgTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in m2uaAspSgTable.
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_m2uaAspMIB above.
 */
u_char *
var_m2uaAspSgTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct m2uaAspSgTable_data *StorageTmp = NULL;

	DEBUGMSGTL(("m2uaAspMIB", "var_m2uaAspSgTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the
	   header. */
	refresh_m2uaAspSgTable();
	/* This assumes you have registered all your data properly with header_complex_add()
	   somewhere before this. */
	if ((StorageTmp = header_complex(m2uaAspSgTableStorage, vp, name, length, exact, var_len, write_method)) == NULL)
		return NULL;
	refresh_m2uaAspSgTable_row(StorageTmp);
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case M2UAASPSGADMINSTRATIVESTATE:
		*write_method = write_m2uaAspSgAdminstrativeState;
		*var_len = sizeof(StorageTmp->m2uaAspSgAdminstrativeState);
		return (u_char *) &StorageTmp->m2uaAspSgAdminstrativeState;
	case M2UAASPSGOPERATIONALSTATE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->m2uaAspSgOperationalState);
		return (u_char *) &StorageTmp->m2uaAspSgOperationalState;
	case M2UAASPSGPROCEDURALSTATUS:
		*write_method = NULL;	/* read-only */
		*var_len = StorageTmp->m2uaAspSgProceduralStatusLen;
		return (u_char *) StorageTmp->m2uaAspSgProceduralStatus;
	case M2UAASPSGUSAGESTATE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->m2uaAspSgUsageState);
		return (u_char *) &StorageTmp->m2uaAspSgUsageState;
	case M2UAASPSGOPERATIONALSTATE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->m2uaAspSgOperationalState);
		return (u_char *) &StorageTmp->m2uaAspSgOperationalState;
	case M2UAASPSGAVAILABILITYSTATUS:
		*write_method = NULL;	/* read-only */
		*var_len = StorageTmp->m2uaAspSgAvailabilityStatusLen;
		return (u_char *) StorageTmp->m2uaAspSgAvailabilityStatus;
	case M2UAASPSGASPSTATE:
		*write_method = write_m2uaAspSgAspState;
		*var_len = sizeof(StorageTmp->m2uaAspSgAspState);
		return (u_char *) &StorageTmp->m2uaAspSgAspState;
	case M2UAASPSGASPIDPOLICY:
		*write_method = write_m2uaAspSgAspIdPolicy;
		*var_len = sizeof(StorageTmp->m2uaAspSgAspIdPolicy);
		return (u_char *) &StorageTmp->m2uaAspSgAspIdPolicy;
	case M2UAASPSGASPID:
		*write_method = write_m2uaAspSgAspId;
		*var_len = sizeof(StorageTmp->m2uaAspSgAspId);
		return (u_char *) &StorageTmp->m2uaAspSgAspId;
	case M2UAASPSGASSOCIATIONPOLICY:
		*write_method = write_m2uaAspSgAssociationPolicy;
		*var_len = sizeof(StorageTmp->m2uaAspSgAssociationPolicy);
		return (u_char *) &StorageTmp->m2uaAspSgAssociationPolicy;
	case M2UAASPSGIPPORT:
		*write_method = write_m2uaAspSgIpPort;
		*var_len = sizeof(StorageTmp->m2uaAspSgIpPort);
		return (u_char *) &StorageTmp->m2uaAspSgIpPort;
	case M2UAASPSGPRIMARYIPADDRESS:
		*write_method = write_m2uaAspSgPrimaryIpAddress;
		*var_len = StorageTmp->m2uaAspSgPrimaryIpAddressLen;
		return (u_char *) StorageTmp->m2uaAspSgPrimaryIpAddress;
	case M2UAASPSGROWSTATUS:
		*write_method = write_m2uaAspSgRowStatus;
		*var_len = sizeof(StorageTmp->m2uaAspSgRowStatus);
		return (u_char *) &StorageTmp->m2uaAspSgRowStatus;
	default:
		ERROR_MSG("");
	}
	return NULL;
}

/**
 * @fn void refresh_m2uaAsTable(void)
 * @brief refresh the scalar values of the m2uaAsTable.
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_m2uaAsTable(void)
{
	if (m2uaAsTable_refresh == 0)
		return;
	m2uaAsTable_refresh = 0;
	/* XXX: Here, update the table as required... */
}

/**
 * @fn void refresh_m2uaAsTable_row(struct m2uaAsTable_data *StorageTmp)
 * @brief refresh the contents of the m2uaAsTable row.
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
void
refresh_m2uaAsTable_row(struct m2uaAsTable_data *StorageTmp)
{
	if (StorageTmp->m2uaAsTable_request == sa_request)
		return;
	StorageTmp->m2uaAsTable_request = sa_request;
}

/**
 * @fn u_char *var_m2uaAsTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in m2uaAsTable.
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_m2uaAspMIB above.
 */
u_char *
var_m2uaAsTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct m2uaAsTable_data *StorageTmp = NULL;

	DEBUGMSGTL(("m2uaAspMIB", "var_m2uaAsTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the
	   header. */
	refresh_m2uaAsTable();
	/* This assumes you have registered all your data properly with header_complex_add()
	   somewhere before this. */
	if ((StorageTmp = header_complex(m2uaAsTableStorage, vp, name, length, exact, var_len, write_method)) == NULL)
		return NULL;
	refresh_m2uaAsTable_row(StorageTmp);
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case M2UAASNAME:
		*write_method = write_m2uaAsName;
		*var_len = StorageTmp->m2uaAsNameLen;
		return (u_char *) StorageTmp->m2uaAsName;
	case M2UAASTRAFFICMODE:
		*write_method = write_m2uaAsTrafficMode;
		*var_len = StorageTmp->m2uaAsTrafficModeLen;
		return (u_char *) StorageTmp->m2uaAsTrafficMode;
	case M2UAASADMINISTRATIVESTATE:
		*write_method = write_m2uaAsAdministrativeState;
		*var_len = sizeof(StorageTmp->m2uaAsAdministrativeState);
		return (u_char *) &StorageTmp->m2uaAsAdministrativeState;
	case M2UAASOPERATIONALSTATE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->m2uaAsOperationalState);
		return (u_char *) &StorageTmp->m2uaAsOperationalState;
	case M2UAASPROCEDURALSTATUS:
		*write_method = write_m2uaAsProceduralStatus;
		*var_len = StorageTmp->m2uaAsProceduralStatusLen;
		return (u_char *) StorageTmp->m2uaAsProceduralStatus;
	case M2UAASUSAGESTATE:
		*write_method = write_m2uaAsUsageState;
		*var_len = sizeof(StorageTmp->m2uaAsUsageState);
		return (u_char *) &StorageTmp->m2uaAsUsageState;
	case M2UAASASSTATE:
		*write_method = write_m2uaAsAsState;
		*var_len = sizeof(StorageTmp->m2uaAsAsState);
		return (u_char *) &StorageTmp->m2uaAsAsState;
	case M2UAASROWSTATUS:
		*write_method = write_m2uaAsRowStatus;
		*var_len = sizeof(StorageTmp->m2uaAsRowStatus);
		return (u_char *) &StorageTmp->m2uaAsRowStatus;
	default:
		ERROR_MSG("");
	}
	return NULL;
}

/**
 * @fn void refresh_m2uaIfTable(void)
 * @brief refresh the scalar values of the m2uaIfTable.
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_m2uaIfTable(void)
{
	if (m2uaIfTable_refresh == 0)
		return;
	m2uaIfTable_refresh = 0;
	/* XXX: Here, update the table as required... */
}

/**
 * @fn void refresh_m2uaIfTable_row(struct m2uaIfTable_data *StorageTmp)
 * @brief refresh the contents of the m2uaIfTable row.
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
void
refresh_m2uaIfTable_row(struct m2uaIfTable_data *StorageTmp)
{
	if (StorageTmp->m2uaIfTable_request == sa_request)
		return;
	StorageTmp->m2uaIfTable_request = sa_request;
}

/**
 * @fn u_char *var_m2uaIfTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in m2uaIfTable.
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_m2uaAspMIB above.
 */
u_char *
var_m2uaIfTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct m2uaIfTable_data *StorageTmp = NULL;

	DEBUGMSGTL(("m2uaAspMIB", "var_m2uaIfTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the
	   header. */
	refresh_m2uaIfTable();
	/* This assumes you have registered all your data properly with header_complex_add()
	   somewhere before this. */
	if ((StorageTmp = header_complex(m2uaIfTableStorage, vp, name, length, exact, var_len, write_method)) == NULL)
		return NULL;
	refresh_m2uaIfTable_row(StorageTmp);
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case M2UAIFASINDEX:
		*write_method = write_m2uaIfAsIndex;
		*var_len = sizeof(StorageTmp->m2uaIfAsIndex);
		return (u_char *) &StorageTmp->m2uaIfAsIndex;
	case M2UAIFIDENTIFIER:
		*write_method = write_m2uaIfIdentifier;
		*var_len = StorageTmp->m2uaIfIdentifierLen;
		return (u_char *) StorageTmp->m2uaIfIdentifier;
	case M2UAIFNAME:
		*write_method = write_m2uaIfName;
		*var_len = StorageTmp->m2uaIfNameLen;
		return (u_char *) StorageTmp->m2uaIfName;
	case M2UAIFROWSTATUS:
		*write_method = write_m2uaIfRowStatus;
		*var_len = sizeof(StorageTmp->m2uaIfRowStatus);
		return (u_char *) &StorageTmp->m2uaIfRowStatus;
	default:
		ERROR_MSG("");
	}
	return NULL;
}

/**
 * @fn void refresh_m2uaAsIfTable(void)
 * @brief refresh the scalar values of the m2uaAsIfTable.
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_m2uaAsIfTable(void)
{
	if (m2uaAsIfTable_refresh == 0)
		return;
	m2uaAsIfTable_refresh = 0;
	/* XXX: Here, update the table as required... */
}

/**
 * @fn void refresh_m2uaAsIfTable_row(struct m2uaAsIfTable_data *StorageTmp)
 * @brief refresh the contents of the m2uaAsIfTable row.
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
void
refresh_m2uaAsIfTable_row(struct m2uaAsIfTable_data *StorageTmp)
{
	if (StorageTmp->m2uaAsIfTable_request == sa_request)
		return;
	StorageTmp->m2uaAsIfTable_request = sa_request;
}

/**
 * @fn u_char *var_m2uaAsIfTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in m2uaAsIfTable.
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_m2uaAspMIB above.
 */
u_char *
var_m2uaAsIfTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct m2uaAsIfTable_data *StorageTmp = NULL;

	DEBUGMSGTL(("m2uaAspMIB", "var_m2uaAsIfTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the
	   header. */
	refresh_m2uaAsIfTable();
	/* This assumes you have registered all your data properly with header_complex_add()
	   somewhere before this. */
	if ((StorageTmp = header_complex(m2uaAsIfTableStorage, vp, name, length, exact, var_len, write_method)) == NULL)
		return NULL;
	refresh_m2uaAsIfTable_row(StorageTmp);
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	default:
		ERROR_MSG("");
	}
	return NULL;
}

/**
 * @fn void refresh_m2uaSgAsTable(void)
 * @brief refresh the scalar values of the m2uaSgAsTable.
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_m2uaSgAsTable(void)
{
	if (m2uaSgAsTable_refresh == 0)
		return;
	m2uaSgAsTable_refresh = 0;
	/* XXX: Here, update the table as required... */
}

/**
 * @fn void refresh_m2uaSgAsTable_row(struct m2uaSgAsTable_data *StorageTmp)
 * @brief refresh the contents of the m2uaSgAsTable row.
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
void
refresh_m2uaSgAsTable_row(struct m2uaSgAsTable_data *StorageTmp)
{
	if (StorageTmp->m2uaSgAsTable_request == sa_request)
		return;
	StorageTmp->m2uaSgAsTable_request = sa_request;
}

/**
 * @fn u_char *var_m2uaSgAsTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in m2uaSgAsTable.
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_m2uaAspMIB above.
 */
u_char *
var_m2uaSgAsTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct m2uaSgAsTable_data *StorageTmp = NULL;

	DEBUGMSGTL(("m2uaAspMIB", "var_m2uaSgAsTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the
	   header. */
	refresh_m2uaSgAsTable();
	/* This assumes you have registered all your data properly with header_complex_add()
	   somewhere before this. */
	if ((StorageTmp = header_complex(m2uaSgAsTableStorage, vp, name, length, exact, var_len, write_method)) == NULL)
		return NULL;
	refresh_m2uaSgAsTable_row(StorageTmp);
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case M2UASGASORDERING:
		*write_method = write_m2uaSgAsOrdering;
		*var_len = sizeof(StorageTmp->m2uaSgAsOrdering);
		return (u_char *) &StorageTmp->m2uaSgAsOrdering;
	case M2UASGASADMINISTRATIVESTATE:
		*write_method = write_m2uaSgAsAdministrativeState;
		*var_len = sizeof(StorageTmp->m2uaSgAsAdministrativeState);
		return (u_char *) &StorageTmp->m2uaSgAsAdministrativeState;
	case M2UASGASOPERATIONALSTATE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->m2uaSgAsOperationalState);
		return (u_char *) &StorageTmp->m2uaSgAsOperationalState;
	case M2UASGASPROCEDURALSTATUS:
		*write_method = NULL;	/* read-only */
		*var_len = StorageTmp->m2uaSgAsProceduralStatusLen;
		return (u_char *) StorageTmp->m2uaSgAsProceduralStatus;
	case M2UASGASUSAGESTATE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->m2uaSgAsUsageState);
		return (u_char *) &StorageTmp->m2uaSgAsUsageState;
	case M2UASGASOPERATIONALSTATE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->m2uaSgAsOperationalState);
		return (u_char *) &StorageTmp->m2uaSgAsOperationalState;
	case M2UASGASAVAILABILITYSTATUS:
		*write_method = NULL;	/* read-only */
		*var_len = StorageTmp->m2uaSgAsAvailabilityStatusLen;
		return (u_char *) StorageTmp->m2uaSgAsAvailabilityStatus;
	case M2UASGASSTANDBYSTATUS:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->m2uaSgAsStandbyStatus);
		return (u_char *) &StorageTmp->m2uaSgAsStandbyStatus;
	case M2UASGASPRIMARYSG:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->m2uaSgAsPrimarySg);
		return (u_char *) &StorageTmp->m2uaSgAsPrimarySg;
	case M2UASGASASSTATE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->m2uaSgAsAsState);
		return (u_char *) &StorageTmp->m2uaSgAsAsState;
	case M2UASGASROWSTATUS:
		*write_method = write_m2uaSgAsRowStatus;
		*var_len = sizeof(StorageTmp->m2uaSgAsRowStatus);
		return (u_char *) &StorageTmp->m2uaSgAsRowStatus;
	default:
		ERROR_MSG("");
	}
	return NULL;
}

/**
 * @fn void refresh_m2uaAspAsTable(void)
 * @brief refresh the scalar values of the m2uaAspAsTable.
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_m2uaAspAsTable(void)
{
	if (m2uaAspAsTable_refresh == 0)
		return;
	m2uaAspAsTable_refresh = 0;
	/* XXX: Here, update the table as required... */
}

/**
 * @fn void refresh_m2uaAspAsTable_row(struct m2uaAspAsTable_data *StorageTmp)
 * @brief refresh the contents of the m2uaAspAsTable row.
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
void
refresh_m2uaAspAsTable_row(struct m2uaAspAsTable_data *StorageTmp)
{
	if (StorageTmp->m2uaAspAsTable_request == sa_request)
		return;
	StorageTmp->m2uaAspAsTable_request = sa_request;
}

/**
 * @fn u_char *var_m2uaAspAsTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in m2uaAspAsTable.
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_m2uaAspMIB above.
 */
u_char *
var_m2uaAspAsTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct m2uaAspAsTable_data *StorageTmp = NULL;

	DEBUGMSGTL(("m2uaAspMIB", "var_m2uaAspAsTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the
	   header. */
	refresh_m2uaAspAsTable();
	/* This assumes you have registered all your data properly with header_complex_add()
	   somewhere before this. */
	if ((StorageTmp = header_complex(m2uaAspAsTableStorage, vp, name, length, exact, var_len, write_method)) == NULL)
		return NULL;
	refresh_m2uaAspAsTable_row(StorageTmp);
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case M2UAASPASADMINISTRATIVESTATE:
		*write_method = write_m2uaAspAsAdministrativeState;
		*var_len = sizeof(StorageTmp->m2uaAspAsAdministrativeState);
		return (u_char *) &StorageTmp->m2uaAspAsAdministrativeState;
	case M2UAASPASOPERATIONALSTATE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->m2uaAspAsOperationalState);
		return (u_char *) &StorageTmp->m2uaAspAsOperationalState;
	case M2UAASPASPROCEDURALSTATUS:
		*write_method = NULL;	/* read-only */
		*var_len = StorageTmp->m2uaAspAsProceduralStatusLen;
		return (u_char *) StorageTmp->m2uaAspAsProceduralStatus;
	case M2UAASPASUSAGESTATE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->m2uaAspAsUsageState);
		return (u_char *) &StorageTmp->m2uaAspAsUsageState;
	case M2UAASPASASSTATE:
		*write_method = write_m2uaAspAsAsState;
		*var_len = sizeof(StorageTmp->m2uaAspAsAsState);
		return (u_char *) &StorageTmp->m2uaAspAsAsState;
	case M2UAASPASROWSTATUS:
		*write_method = write_m2uaAspAsRowStatus;
		*var_len = sizeof(StorageTmp->m2uaAspAsRowStatus);
		return (u_char *) &StorageTmp->m2uaAspAsRowStatus;
	default:
		ERROR_MSG("");
	}
	return NULL;
}

/**
 * @fn void refresh_m2uaAspSgAsTable(void)
 * @brief refresh the scalar values of the m2uaAspSgAsTable.
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_m2uaAspSgAsTable(void)
{
	if (m2uaAspSgAsTable_refresh == 0)
		return;
	m2uaAspSgAsTable_refresh = 0;
	/* XXX: Here, update the table as required... */
}

/**
 * @fn void refresh_m2uaAspSgAsTable_row(struct m2uaAspSgAsTable_data *StorageTmp)
 * @brief refresh the contents of the m2uaAspSgAsTable row.
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
void
refresh_m2uaAspSgAsTable_row(struct m2uaAspSgAsTable_data *StorageTmp)
{
	if (StorageTmp->m2uaAspSgAsTable_request == sa_request)
		return;
	StorageTmp->m2uaAspSgAsTable_request = sa_request;
}

/**
 * @fn u_char *var_m2uaAspSgAsTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in m2uaAspSgAsTable.
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_m2uaAspMIB above.
 */
u_char *
var_m2uaAspSgAsTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct m2uaAspSgAsTable_data *StorageTmp = NULL;

	DEBUGMSGTL(("m2uaAspMIB", "var_m2uaAspSgAsTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the
	   header. */
	refresh_m2uaAspSgAsTable();
	/* This assumes you have registered all your data properly with header_complex_add()
	   somewhere before this. */
	if ((StorageTmp = header_complex(m2uaAspSgAsTableStorage, vp, name, length, exact, var_len, write_method)) == NULL)
		return NULL;
	refresh_m2uaAspSgAsTable_row(StorageTmp);
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case M2UAASPSGASADMINISTRATIVESTATE:
		*write_method = write_m2uaAspSgAsAdministrativeState;
		*var_len = sizeof(StorageTmp->m2uaAspSgAsAdministrativeState);
		return (u_char *) &StorageTmp->m2uaAspSgAsAdministrativeState;
	case M2UAASPSGASOPERATIONALSTATE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->m2uaAspSgAsOperationalState);
		return (u_char *) &StorageTmp->m2uaAspSgAsOperationalState;
	case M2UAASPSGASPROCEDURALSTATUS:
		*write_method = NULL;	/* read-only */
		*var_len = StorageTmp->m2uaAspSgAsProceduralStatusLen;
		return (u_char *) StorageTmp->m2uaAspSgAsProceduralStatus;
	case M2UAASPSGASUSAGESTATE:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->m2uaAspSgAsUsageState);
		return (u_char *) &StorageTmp->m2uaAspSgAsUsageState;
	case M2UAASPSGASSTANDBYSTATUS:
		*write_method = NULL;	/* read-only */
		*var_len = sizeof(StorageTmp->m2uaAspSgAsStandbyStatus);
		return (u_char *) &StorageTmp->m2uaAspSgAsStandbyStatus;
	case M2UAASPSGASROWSTATUS:
		*write_method = write_m2uaAspSgAsRowStatus;
		*var_len = sizeof(StorageTmp->m2uaAspSgAsRowStatus);
		return (u_char *) &StorageTmp->m2uaAspSgAsRowStatus;
	default:
		ERROR_MSG("");
	}
	return NULL;
}

/**
 * @fn int write_m2uaAspAdministrativeState(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2uaAspAdministrativeState(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m2uaAspTable_data *StorageTmp = NULL;
	size_t newlen = name_len - (sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1);
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m2uaAspMIB", "write_m2uaAspAdministrativeState entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(m2uaAspTableStorage, NULL, &name[sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2uaAspRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspAdministrativeState: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspAdministrativeState not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspAdministrativeState: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case M2UAASPADMINISTRATIVESTATE_LOCKED:
		case M2UAASPADMINISTRATIVESTATE_UNLOCKED:
		case M2UAASPADMINISTRATIVESTATE_SHUTTINGDOWN:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspAdministrativeState: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in set_value for you to use, and
				   you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		old_value = StorageTmp->m2uaAspAdministrativeState;
		StorageTmp->m2uaAspAdministrativeState = set_value;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2uaAspAdministrativeState = old_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2uaAspAspState(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2uaAspAspState(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m2uaAspTable_data *StorageTmp = NULL;
	size_t newlen = name_len - (sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1);
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m2uaAspMIB", "write_m2uaAspAspState entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(m2uaAspTableStorage, NULL, &name[sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2uaAspRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspAspState: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspAspState not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspAspState: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case M2UAASPASPSTATE_DOWN:
		case M2UAASPASPSTATE_INITIALIZING:
		case M2UAASPASPSTATE_TERMINATING:
		case M2UAASPASPSTATE_UP:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspAspState: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in set_value for you to use, and
				   you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		old_value = StorageTmp->m2uaAspAspState;
		StorageTmp->m2uaAspAspState = set_value;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2uaAspAspState = old_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2uaAspCapablities(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2uaAspCapablities(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static uint8_t *old_value;
	struct m2uaAspTable_data *StorageTmp = NULL;
	size_t newlen = name_len - (sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1);
	static size_t old_length = 0;
	static uint8_t *string = NULL;

	DEBUGMSGTL(("m2uaAspMIB", "write_m2uaAspCapablities entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(m2uaAspTableStorage, NULL, &name[sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2uaAspRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspCapablities: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_BIT_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspCapablities not ASN_BIT_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (2 > var_val_len || var_val_len > SPRINT_MAX_LEN || var_val_len != ((9 >> 3) + 2)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspCapablities: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		if (var_val_len < 1 || *var_val != (8 - (9 & 0x07))) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspCapablities: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((string = malloc(var_val_len)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		break;
	case FREE:		/* Release any resources that have been allocated */
		SNMP_FREE(string);
		break;
	case ACTION:		/* The variable has been stored in string for you to use, and you
				   have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		old_value = StorageTmp->m2uaAspCapablities;
		old_length = StorageTmp->m2uaAspCapablitiesLen;
		StorageTmp->m2uaAspCapablities = string;
		StorageTmp->m2uaAspCapablitiesLen = var_val_len;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2uaAspCapablities = old_value;
		StorageTmp->m2uaAspCapablitiesLen = old_length;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(old_value);
		old_length = 0;
		string = NULL;
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2uaAspRegistrationPolicy(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2uaAspRegistrationPolicy(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m2uaAspTable_data *StorageTmp = NULL;
	size_t newlen = name_len - (sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1);
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m2uaAspMIB", "write_m2uaAspRegistrationPolicy entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(m2uaAspTableStorage, NULL, &name[sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2uaAspRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspRegistrationPolicy: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspRegistrationPolicy not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspRegistrationPolicy: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 0 */
		switch (set_value) {
		case M2UAASPREGISTRATIONPOLICY_UNKNOWN:
		case M2UAASPREGISTRATIONPOLICY_FORBIDDEN:
		case M2UAASPREGISTRATIONPOLICY_PERMITTED:
		case M2UAASPREGISTRATIONPOLICY_REQUIRED:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspRegistrationPolicy: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in set_value for you to use, and
				   you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		old_value = StorageTmp->m2uaAspRegistrationPolicy;
		StorageTmp->m2uaAspRegistrationPolicy = set_value;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2uaAspRegistrationPolicy = old_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2uaAspName(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2uaAspName(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static uint8_t *old_value;
	struct m2uaAspTable_data *StorageTmp = NULL;
	size_t newlen = name_len - (sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1);
	static size_t old_length = 0;
	static uint8_t *string = NULL;

	DEBUGMSGTL(("m2uaAspMIB", "write_m2uaAspName entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(m2uaAspTableStorage, NULL, &name[sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2uaAspRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspName: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspName not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 0..32 */
		if (2 > var_val_len || var_val_len > SPRINT_MAX_LEN || ((0 > var_val_len || var_val_len > 32))) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspName: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((string = malloc(var_val_len)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		break;
	case FREE:		/* Release any resources that have been allocated */
		SNMP_FREE(string);
		break;
	case ACTION:		/* The variable has been stored in string for you to use, and you
				   have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		old_value = StorageTmp->m2uaAspName;
		old_length = StorageTmp->m2uaAspNameLen;
		StorageTmp->m2uaAspName = string;
		StorageTmp->m2uaAspNameLen = var_val_len;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2uaAspName = old_value;
		StorageTmp->m2uaAspNameLen = old_length;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(old_value);
		old_length = 0;
		string = NULL;
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2uaSgName(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2uaSgName(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static uint8_t *old_value;
	struct m2uaSgTable_data *StorageTmp = NULL;
	size_t newlen = name_len - (sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1);
	static size_t old_length = 0;
	static uint8_t *string = NULL;

	DEBUGMSGTL(("m2uaAspMIB", "write_m2uaSgName entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(m2uaSgTableStorage, NULL, &name[sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2uaSgRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaSgName: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaSgName not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 0..32 */
		if (2 > var_val_len || var_val_len > SPRINT_MAX_LEN || ((0 > var_val_len || var_val_len > 32))) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaSgName: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((string = malloc(var_val_len)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		break;
	case FREE:		/* Release any resources that have been allocated */
		SNMP_FREE(string);
		break;
	case ACTION:		/* The variable has been stored in string for you to use, and you
				   have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		old_value = StorageTmp->m2uaSgName;
		old_length = StorageTmp->m2uaSgNameLen;
		StorageTmp->m2uaSgName = string;
		StorageTmp->m2uaSgNameLen = var_val_len;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2uaSgName = old_value;
		StorageTmp->m2uaSgNameLen = old_length;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(old_value);
		old_length = 0;
		string = NULL;
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2uaSgAdministrativeState(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2uaSgAdministrativeState(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m2uaSgTable_data *StorageTmp = NULL;
	size_t newlen = name_len - (sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1);
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m2uaAspMIB", "write_m2uaSgAdministrativeState entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(m2uaSgTableStorage, NULL, &name[sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2uaSgRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaSgAdministrativeState: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaSgAdministrativeState not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaSgAdministrativeState: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 0 */
		switch (set_value) {
		case M2UASGADMINISTRATIVESTATE_LOCKED:
		case M2UASGADMINISTRATIVESTATE_UNLOCKED:
		case M2UASGADMINISTRATIVESTATE_SHUTTINGDOWN:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaSgAdministrativeState: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in set_value for you to use, and
				   you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		old_value = StorageTmp->m2uaSgAdministrativeState;
		StorageTmp->m2uaSgAdministrativeState = set_value;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2uaSgAdministrativeState = old_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2uaSgAspState(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2uaSgAspState(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m2uaSgTable_data *StorageTmp = NULL;
	size_t newlen = name_len - (sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1);
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m2uaAspMIB", "write_m2uaSgAspState entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(m2uaSgTableStorage, NULL, &name[sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2uaSgRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaSgAspState: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaSgAspState not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaSgAspState: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 0 */
		switch (set_value) {
		case M2UASGASPSTATE_DOWN:
		case M2UASGASPSTATE_INITIALIZING:
		case M2UASGASPSTATE_TERMINATING:
		case M2UASGASPSTATE_UP:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaSgAspState: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in set_value for you to use, and
				   you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		old_value = StorageTmp->m2uaSgAspState;
		StorageTmp->m2uaSgAspState = set_value;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2uaSgAspState = old_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2uaSgVersion(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2uaSgVersion(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m2uaSgTable_data *StorageTmp = NULL;
	size_t newlen = name_len - (sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1);
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m2uaAspMIB", "write_m2uaSgVersion entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(m2uaSgTableStorage, NULL, &name[sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2uaSgRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaSgVersion: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaSgVersion not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaSgVersion: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 1 */
		switch (set_value) {
		case M2UASGVERSION_INTEROPERABLE:
		case M2UASGVERSION_RFC3331:
		case M2UASGVERSION_TS102141:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaSgVersion: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in set_value for you to use, and
				   you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		old_value = StorageTmp->m2uaSgVersion;
		StorageTmp->m2uaSgVersion = set_value;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2uaSgVersion = old_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2uaSgOptions(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2uaSgOptions(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static uint8_t *old_value;
	struct m2uaSgTable_data *StorageTmp = NULL;
	size_t newlen = name_len - (sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1);
	static size_t old_length = 0;
	static uint8_t *string = NULL;

	DEBUGMSGTL(("m2uaAspMIB", "write_m2uaSgOptions entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(m2uaSgTableStorage, NULL, &name[sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2uaSgRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaSgOptions: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_BIT_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaSgOptions not ASN_BIT_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (2 > var_val_len || var_val_len > SPRINT_MAX_LEN || var_val_len != ((9 >> 3) + 2)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaSgOptions: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		if (var_val_len < 1 || *var_val != (8 - (9 & 0x07))) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaSgOptions: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((string = malloc(var_val_len)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		break;
	case FREE:		/* Release any resources that have been allocated */
		SNMP_FREE(string);
		break;
	case ACTION:		/* The variable has been stored in string for you to use, and you
				   have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		old_value = StorageTmp->m2uaSgOptions;
		old_length = StorageTmp->m2uaSgOptionsLen;
		StorageTmp->m2uaSgOptions = string;
		StorageTmp->m2uaSgOptionsLen = var_val_len;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2uaSgOptions = old_value;
		StorageTmp->m2uaSgOptionsLen = old_length;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(old_value);
		old_length = 0;
		string = NULL;
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2uaSgRegistrationPolicy(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2uaSgRegistrationPolicy(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m2uaSgTable_data *StorageTmp = NULL;
	size_t newlen = name_len - (sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1);
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m2uaAspMIB", "write_m2uaSgRegistrationPolicy entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(m2uaSgTableStorage, NULL, &name[sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2uaSgRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaSgRegistrationPolicy: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaSgRegistrationPolicy not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaSgRegistrationPolicy: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 0 */
		switch (set_value) {
		case M2UASGREGISTRATIONPOLICY_UNKNOWN:
		case M2UASGREGISTRATIONPOLICY_FORBIDDEN:
		case M2UASGREGISTRATIONPOLICY_PERMITTED:
		case M2UASGREGISTRATIONPOLICY_REQUIRED:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaSgRegistrationPolicy: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in set_value for you to use, and
				   you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		old_value = StorageTmp->m2uaSgRegistrationPolicy;
		StorageTmp->m2uaSgRegistrationPolicy = set_value;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2uaSgRegistrationPolicy = old_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2uaSgProtocolPayloadId(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2uaSgProtocolPayloadId(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m2uaSgTable_data *StorageTmp = NULL;
	size_t newlen = name_len - (sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1);
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m2uaAspMIB", "write_m2uaSgProtocolPayloadId entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(m2uaSgTableStorage, NULL, &name[sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2uaSgRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaSgProtocolPayloadId: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaSgProtocolPayloadId not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaSgProtocolPayloadId: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 2 */
		/* Note: ranges 0..0 2..2 */
		if (set_value != 0 && set_value != 2) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaSgProtocolPayloadId: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in set_value for you to use, and
				   you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		old_value = StorageTmp->m2uaSgProtocolPayloadId;
		StorageTmp->m2uaSgProtocolPayloadId = set_value;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2uaSgProtocolPayloadId = old_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2uaSgIpPort(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2uaSgIpPort(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m2uaSgTable_data *StorageTmp = NULL;
	size_t newlen = name_len - (sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1);
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m2uaAspMIB", "write_m2uaSgIpPort entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(m2uaSgTableStorage, NULL, &name[sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2uaSgRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaSgIpPort: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaSgIpPort not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaSgIpPort: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 2904 */
		/* Note: ranges 1..65535 */
		if ((1 > set_value || set_value > 65535)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaSgIpPort: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in set_value for you to use, and
				   you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		old_value = StorageTmp->m2uaSgIpPort;
		StorageTmp->m2uaSgIpPort = set_value;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2uaSgIpPort = old_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2uaSgPrimaryIpAddress(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2uaSgPrimaryIpAddress(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static uint8_t *old_value;
	struct m2uaSgTable_data *StorageTmp = NULL;
	size_t newlen = name_len - (sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1);
	static size_t old_length = 0;
	static uint8_t *string = NULL;

	DEBUGMSGTL(("m2uaAspMIB", "write_m2uaSgPrimaryIpAddress entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(m2uaSgTableStorage, NULL, &name[sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2uaSgRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaSgPrimaryIpAddress: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_IPADDRESS) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaSgPrimaryIpAddress not ASN_IPADDRESS\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (2 > var_val_len || var_val_len > SPRINT_MAX_LEN) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaSgPrimaryIpAddress: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((string = malloc(var_val_len)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		break;
	case FREE:		/* Release any resources that have been allocated */
		SNMP_FREE(string);
		break;
	case ACTION:		/* The variable has been stored in string for you to use, and you
				   have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		old_value = StorageTmp->m2uaSgPrimaryIpAddress;
		old_length = StorageTmp->m2uaSgPrimaryIpAddressLen;
		StorageTmp->m2uaSgPrimaryIpAddress = string;
		StorageTmp->m2uaSgPrimaryIpAddressLen = var_val_len;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2uaSgPrimaryIpAddress = old_value;
		StorageTmp->m2uaSgPrimaryIpAddressLen = old_length;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(old_value);
		old_length = 0;
		string = NULL;
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2uaSgMinOstreams(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2uaSgMinOstreams(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m2uaSgTable_data *StorageTmp = NULL;
	size_t newlen = name_len - (sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1);
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m2uaAspMIB", "write_m2uaSgMinOstreams entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(m2uaSgTableStorage, NULL, &name[sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2uaSgRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaSgMinOstreams: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaSgMinOstreams not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaSgMinOstreams: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 32 */
		/* Note: ranges 2..16384 */
		if ((2 > set_value || set_value > 16384)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaSgMinOstreams: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in set_value for you to use, and
				   you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		old_value = StorageTmp->m2uaSgMinOstreams;
		StorageTmp->m2uaSgMinOstreams = set_value;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2uaSgMinOstreams = old_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2uaSgMaxIstreams(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2uaSgMaxIstreams(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m2uaSgTable_data *StorageTmp = NULL;
	size_t newlen = name_len - (sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1);
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m2uaAspMIB", "write_m2uaSgMaxIstreams entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(m2uaSgTableStorage, NULL, &name[sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2uaSgRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaSgMaxIstreams: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaSgMaxIstreams not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaSgMaxIstreams: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 32 */
		/* Note: ranges 2..16384 */
		if ((2 > set_value || set_value > 16384)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaSgMaxIstreams: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in set_value for you to use, and
				   you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		old_value = StorageTmp->m2uaSgMaxIstreams;
		StorageTmp->m2uaSgMaxIstreams = set_value;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2uaSgMaxIstreams = old_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2uaAspSgAdminstrativeState(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2uaAspSgAdminstrativeState(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m2uaAspSgTable_data *StorageTmp = NULL;
	size_t newlen = name_len - (sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1);
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m2uaAspMIB", "write_m2uaAspSgAdminstrativeState entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(m2uaAspSgTableStorage, NULL, &name[sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2uaAspSgRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspSgAdminstrativeState: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspSgAdminstrativeState not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspSgAdminstrativeState: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case M2UAASPSGADMINSTRATIVESTATE_LOCKED:
		case M2UAASPSGADMINSTRATIVESTATE_UNLOCKED:
		case M2UAASPSGADMINSTRATIVESTATE_SHUTTINGDOWN:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspSgAdminstrativeState: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in set_value for you to use, and
				   you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		old_value = StorageTmp->m2uaAspSgAdminstrativeState;
		StorageTmp->m2uaAspSgAdminstrativeState = set_value;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2uaAspSgAdminstrativeState = old_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2uaAspSgAspState(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2uaAspSgAspState(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m2uaAspSgTable_data *StorageTmp = NULL;
	size_t newlen = name_len - (sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1);
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m2uaAspMIB", "write_m2uaAspSgAspState entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(m2uaAspSgTableStorage, NULL, &name[sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2uaAspSgRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspSgAspState: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspSgAspState not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspSgAspState: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case M2UAASPSGASPSTATE_DOWN:
		case M2UAASPSGASPSTATE_INITIALIZING:
		case M2UAASPSGASPSTATE_TERMINATING:
		case M2UAASPSGASPSTATE_UP:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspSgAspState: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in set_value for you to use, and
				   you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		old_value = StorageTmp->m2uaAspSgAspState;
		StorageTmp->m2uaAspSgAspState = set_value;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2uaAspSgAspState = old_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2uaAspSgAspIdPolicy(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2uaAspSgAspIdPolicy(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m2uaAspSgTable_data *StorageTmp = NULL;
	size_t newlen = name_len - (sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1);
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m2uaAspMIB", "write_m2uaAspSgAspIdPolicy entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(m2uaAspSgTableStorage, NULL, &name[sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2uaAspSgRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspSgAspIdPolicy: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspSgAspIdPolicy not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspSgAspIdPolicy: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case M2UAASPSGASPIDPOLICY_UNKNOWN:
		case M2UAASPSGASPIDPOLICY_PERMITTED:
		case M2UAASPSGASPIDPOLICY_REQUIRED:
		case M2UAASPSGASPIDPOLICY_FORBIDDEN:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspSgAspIdPolicy: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in set_value for you to use, and
				   you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		old_value = StorageTmp->m2uaAspSgAspIdPolicy;
		StorageTmp->m2uaAspSgAspIdPolicy = set_value;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2uaAspSgAspIdPolicy = old_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2uaAspSgAspId(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2uaAspSgAspId(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static ulong old_value;
	struct m2uaAspSgTable_data *StorageTmp = NULL;
	size_t newlen = name_len - (sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1);
	ulong set_value = *((ulong *) var_val);

	DEBUGMSGTL(("m2uaAspMIB", "write_m2uaAspSgAspId entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(m2uaAspSgTableStorage, NULL, &name[sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2uaAspSgRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspSgAspId: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspSgAspId not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspSgAspId: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 0 */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in set_value for you to use, and
				   you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		old_value = StorageTmp->m2uaAspSgAspId;
		StorageTmp->m2uaAspSgAspId = set_value;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2uaAspSgAspId = old_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2uaAspSgAssociationPolicy(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2uaAspSgAssociationPolicy(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m2uaAspSgTable_data *StorageTmp = NULL;
	size_t newlen = name_len - (sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1);
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m2uaAspMIB", "write_m2uaAspSgAssociationPolicy entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(m2uaAspSgTableStorage, NULL, &name[sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2uaAspSgRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspSgAssociationPolicy: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspSgAssociationPolicy not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspSgAssociationPolicy: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 0 */
		switch (set_value) {
		case M2UAASPSGASSOCIATIONPOLICY_UNKNOWN:
		case M2UAASPSGASSOCIATIONPOLICY_ALWAYS:
		case M2UAASPSGASSOCIATIONPOLICY_ONDEMAND:
		case M2UAASPSGASSOCIATIONPOLICY_OTHER:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspSgAssociationPolicy: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in set_value for you to use, and
				   you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		old_value = StorageTmp->m2uaAspSgAssociationPolicy;
		StorageTmp->m2uaAspSgAssociationPolicy = set_value;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2uaAspSgAssociationPolicy = old_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2uaAspSgIpPort(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2uaAspSgIpPort(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m2uaAspSgTable_data *StorageTmp = NULL;
	size_t newlen = name_len - (sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1);
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m2uaAspMIB", "write_m2uaAspSgIpPort entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(m2uaAspSgTableStorage, NULL, &name[sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2uaAspSgRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspSgIpPort: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspSgIpPort not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspSgIpPort: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 0 */
		/* Note: ranges 1..65535 */
		if ((1 > set_value || set_value > 65535)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspSgIpPort: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in set_value for you to use, and
				   you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		old_value = StorageTmp->m2uaAspSgIpPort;
		StorageTmp->m2uaAspSgIpPort = set_value;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2uaAspSgIpPort = old_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2uaAspSgPrimaryIpAddress(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2uaAspSgPrimaryIpAddress(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static uint8_t *old_value;
	struct m2uaAspSgTable_data *StorageTmp = NULL;
	size_t newlen = name_len - (sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1);
	static size_t old_length = 0;
	static uint8_t *string = NULL;

	DEBUGMSGTL(("m2uaAspMIB", "write_m2uaAspSgPrimaryIpAddress entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(m2uaAspSgTableStorage, NULL, &name[sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2uaAspSgRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspSgPrimaryIpAddress: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_IPADDRESS) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspSgPrimaryIpAddress not ASN_IPADDRESS\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (2 > var_val_len || var_val_len > SPRINT_MAX_LEN) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspSgPrimaryIpAddress: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((string = malloc(var_val_len)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		break;
	case FREE:		/* Release any resources that have been allocated */
		SNMP_FREE(string);
		break;
	case ACTION:		/* The variable has been stored in string for you to use, and you
				   have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		old_value = StorageTmp->m2uaAspSgPrimaryIpAddress;
		old_length = StorageTmp->m2uaAspSgPrimaryIpAddressLen;
		StorageTmp->m2uaAspSgPrimaryIpAddress = string;
		StorageTmp->m2uaAspSgPrimaryIpAddressLen = var_val_len;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2uaAspSgPrimaryIpAddress = old_value;
		StorageTmp->m2uaAspSgPrimaryIpAddressLen = old_length;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(old_value);
		old_length = 0;
		string = NULL;
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2uaAsName(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2uaAsName(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static uint8_t *old_value;
	struct m2uaAsTable_data *StorageTmp = NULL;
	size_t newlen = name_len - (sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1);
	static size_t old_length = 0;
	static uint8_t *string = NULL;

	DEBUGMSGTL(("m2uaAspMIB", "write_m2uaAsName entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(m2uaAsTableStorage, NULL, &name[sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2uaAsRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAsName: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAsName not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 0..32 */
		if (2 > var_val_len || var_val_len > SPRINT_MAX_LEN || ((0 > var_val_len || var_val_len > 32))) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAsName: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((string = malloc(var_val_len)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		break;
	case FREE:		/* Release any resources that have been allocated */
		SNMP_FREE(string);
		break;
	case ACTION:		/* The variable has been stored in string for you to use, and you
				   have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		old_value = StorageTmp->m2uaAsName;
		old_length = StorageTmp->m2uaAsNameLen;
		StorageTmp->m2uaAsName = string;
		StorageTmp->m2uaAsNameLen = var_val_len;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2uaAsName = old_value;
		StorageTmp->m2uaAsNameLen = old_length;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(old_value);
		old_length = 0;
		string = NULL;
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2uaAsTrafficMode(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2uaAsTrafficMode(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static oid *old_value;
	struct m2uaAsTable_data *StorageTmp = NULL;
	size_t newlen = name_len - (sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1);
	static size_t old_length = 0;
	static oid *objid = NULL;

	DEBUGMSGTL(("m2uaAspMIB", "write_m2uaAsTrafficMode entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(m2uaAsTableStorage, NULL, &name[sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2uaAsRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAsTrafficMode: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OBJECT_ID) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAsTrafficMode not ASN_OBJECT_ID\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (MIN_OID_LEN * sizeof(oid) > var_val_len || var_val_len > MAX_OID_LEN * sizeof(oid)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAsTrafficMode: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((objid = snmp_duplicate_objid((void *) var_val, var_val_len / sizeof(oid))) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		break;
	case FREE:		/* Release any resources that have been allocated */
		SNMP_FREE(objid);
		break;
	case ACTION:		/* The variable has been stored in objid for you to use, and you
				   have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		old_value = StorageTmp->m2uaAsTrafficMode;
		old_length = StorageTmp->m2uaAsTrafficModeLen;
		StorageTmp->m2uaAsTrafficMode = objid;
		StorageTmp->m2uaAsTrafficModeLen = var_val_len / sizeof(oid);
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2uaAsTrafficMode = old_value;
		StorageTmp->m2uaAsTrafficModeLen = old_length;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(old_value);
		old_length = 0;
		objid = NULL;
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2uaAsAdministrativeState(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2uaAsAdministrativeState(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m2uaAsTable_data *StorageTmp = NULL;
	size_t newlen = name_len - (sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1);
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m2uaAspMIB", "write_m2uaAsAdministrativeState entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(m2uaAsTableStorage, NULL, &name[sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2uaAsRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAsAdministrativeState: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAsAdministrativeState not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAsAdministrativeState: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case M2UAASADMINISTRATIVESTATE_LOCKED:
		case M2UAASADMINISTRATIVESTATE_UNLOCKED:
		case M2UAASADMINISTRATIVESTATE_SHUTTINGDOWN:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAsAdministrativeState: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in set_value for you to use, and
				   you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		old_value = StorageTmp->m2uaAsAdministrativeState;
		StorageTmp->m2uaAsAdministrativeState = set_value;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2uaAsAdministrativeState = old_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2uaAsProceduralStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2uaAsProceduralStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static uint8_t *old_value;
	struct m2uaAsTable_data *StorageTmp = NULL;
	size_t newlen = name_len - (sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1);
	static size_t old_length = 0;
	static uint8_t *string = NULL;

	DEBUGMSGTL(("m2uaAspMIB", "write_m2uaAsProceduralStatus entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(m2uaAsTableStorage, NULL, &name[sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2uaAsRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAsProceduralStatus: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_BIT_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAsProceduralStatus not ASN_BIT_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (2 > var_val_len || var_val_len > SPRINT_MAX_LEN || var_val_len != ((4 >> 3) + 2)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAsProceduralStatus: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		if (var_val_len < 1 || *var_val != (8 - (4 & 0x07))) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAsProceduralStatus: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((string = malloc(var_val_len)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		break;
	case FREE:		/* Release any resources that have been allocated */
		SNMP_FREE(string);
		break;
	case ACTION:		/* The variable has been stored in string for you to use, and you
				   have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		old_value = StorageTmp->m2uaAsProceduralStatus;
		old_length = StorageTmp->m2uaAsProceduralStatusLen;
		StorageTmp->m2uaAsProceduralStatus = string;
		StorageTmp->m2uaAsProceduralStatusLen = var_val_len;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2uaAsProceduralStatus = old_value;
		StorageTmp->m2uaAsProceduralStatusLen = old_length;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(old_value);
		old_length = 0;
		string = NULL;
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2uaAsUsageState(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2uaAsUsageState(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m2uaAsTable_data *StorageTmp = NULL;
	size_t newlen = name_len - (sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1);
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m2uaAspMIB", "write_m2uaAsUsageState entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(m2uaAsTableStorage, NULL, &name[sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2uaAsRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAsUsageState: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAsUsageState not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAsUsageState: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case M2UAASUSAGESTATE_IDLE:
		case M2UAASUSAGESTATE_ACTIVE:
		case M2UAASUSAGESTATE_BUSY:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAsUsageState: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in set_value for you to use, and
				   you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		old_value = StorageTmp->m2uaAsUsageState;
		StorageTmp->m2uaAsUsageState = set_value;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2uaAsUsageState = old_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2uaAsAsState(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2uaAsAsState(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m2uaAsTable_data *StorageTmp = NULL;
	size_t newlen = name_len - (sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1);
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m2uaAspMIB", "write_m2uaAsAsState entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(m2uaAsTableStorage, NULL, &name[sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2uaAsRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAsAsState: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAsAsState not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAsAsState: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case M2UAASASSTATE_DOWN:
		case M2UAASASSTATE_INITIALIZING:
		case M2UAASASSTATE_TERMINATING:
		case M2UAASASSTATE_INACTIVE:
		case M2UAASASSTATE_ACTIVATING:
		case M2UAASASSTATE_DEACTIVATING:
		case M2UAASASSTATE_ACTIVE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAsAsState: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in set_value for you to use, and
				   you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		old_value = StorageTmp->m2uaAsAsState;
		StorageTmp->m2uaAsAsState = set_value;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2uaAsAsState = old_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2uaIfAsIndex(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2uaIfAsIndex(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static ulong old_value;
	struct m2uaIfTable_data *StorageTmp = NULL;
	size_t newlen = name_len - (sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1);
	ulong set_value = *((ulong *) var_val);

	DEBUGMSGTL(("m2uaAspMIB", "write_m2uaIfAsIndex entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(m2uaIfTableStorage, NULL, &name[sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2uaIfRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaIfAsIndex: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaIfAsIndex not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaIfAsIndex: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in set_value for you to use, and
				   you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		old_value = StorageTmp->m2uaIfAsIndex;
		StorageTmp->m2uaIfAsIndex = set_value;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2uaIfAsIndex = old_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2uaIfIdentifier(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2uaIfIdentifier(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static uint8_t *old_value;
	struct m2uaIfTable_data *StorageTmp = NULL;
	size_t newlen = name_len - (sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1);
	static size_t old_length = 0;
	static uint8_t *string = NULL;

	DEBUGMSGTL(("m2uaAspMIB", "write_m2uaIfIdentifier entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(m2uaIfTableStorage, NULL, &name[sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2uaIfRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaIfIdentifier: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaIfIdentifier not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 1..16 */
		if (2 > var_val_len || var_val_len > SPRINT_MAX_LEN || ((1 > var_val_len || var_val_len > 16))) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaIfIdentifier: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((string = malloc(var_val_len)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		break;
	case FREE:		/* Release any resources that have been allocated */
		SNMP_FREE(string);
		break;
	case ACTION:		/* The variable has been stored in string for you to use, and you
				   have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		old_value = StorageTmp->m2uaIfIdentifier;
		old_length = StorageTmp->m2uaIfIdentifierLen;
		StorageTmp->m2uaIfIdentifier = string;
		StorageTmp->m2uaIfIdentifierLen = var_val_len;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2uaIfIdentifier = old_value;
		StorageTmp->m2uaIfIdentifierLen = old_length;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(old_value);
		old_length = 0;
		string = NULL;
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2uaIfName(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2uaIfName(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static uint8_t *old_value;
	struct m2uaIfTable_data *StorageTmp = NULL;
	size_t newlen = name_len - (sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1);
	static size_t old_length = 0;
	static uint8_t *string = NULL;

	DEBUGMSGTL(("m2uaAspMIB", "write_m2uaIfName entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(m2uaIfTableStorage, NULL, &name[sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2uaIfRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaIfName: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaIfName not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 0..32 */
		if (2 > var_val_len || var_val_len > SPRINT_MAX_LEN || ((0 > var_val_len || var_val_len > 32))) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaIfName: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((string = malloc(var_val_len)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		break;
	case FREE:		/* Release any resources that have been allocated */
		SNMP_FREE(string);
		break;
	case ACTION:		/* The variable has been stored in string for you to use, and you
				   have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		old_value = StorageTmp->m2uaIfName;
		old_length = StorageTmp->m2uaIfNameLen;
		StorageTmp->m2uaIfName = string;
		StorageTmp->m2uaIfNameLen = var_val_len;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2uaIfName = old_value;
		StorageTmp->m2uaIfNameLen = old_length;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(old_value);
		old_length = 0;
		string = NULL;
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2uaSgAsOrdering(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2uaSgAsOrdering(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static ulong old_value;
	struct m2uaSgAsTable_data *StorageTmp = NULL;
	size_t newlen = name_len - (sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1);
	ulong set_value = *((ulong *) var_val);

	DEBUGMSGTL(("m2uaAspMIB", "write_m2uaSgAsOrdering entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(m2uaSgAsTableStorage, NULL, &name[sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2uaSgAsRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaSgAsOrdering: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaSgAsOrdering not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaSgAsOrdering: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in set_value for you to use, and
				   you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		old_value = StorageTmp->m2uaSgAsOrdering;
		StorageTmp->m2uaSgAsOrdering = set_value;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2uaSgAsOrdering = old_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2uaSgAsAdministrativeState(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2uaSgAsAdministrativeState(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m2uaSgAsTable_data *StorageTmp = NULL;
	size_t newlen = name_len - (sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1);
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m2uaAspMIB", "write_m2uaSgAsAdministrativeState entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(m2uaSgAsTableStorage, NULL, &name[sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2uaSgAsRowStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaSgAsAdministrativeState: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaSgAsAdministrativeState not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaSgAsAdministrativeState: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case M2UASGASADMINISTRATIVESTATE_LOCKED:
		case M2UASGASADMINISTRATIVESTATE_UNLOCKED:
		case M2UASGASADMINISTRATIVESTATE_SHUTTINGDOWN:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaSgAsAdministrativeState: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in set_value for you to use, and
				   you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		old_value = StorageTmp->m2uaSgAsAdministrativeState;
		StorageTmp->m2uaSgAsAdministrativeState = set_value;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2uaSgAsAdministrativeState = old_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2uaAspAsAdministrativeState(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2uaAspAsAdministrativeState(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m2uaAspAsTable_data *StorageTmp = NULL;
	size_t newlen = name_len - (sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1);
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m2uaAspMIB", "write_m2uaAspAsAdministrativeState entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(m2uaAspAsTableStorage, NULL, &name[sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m2uaAspAsAsState) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspAsAdministrativeState: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspAsAdministrativeState not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspAsAdministrativeState: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 0 */
		switch (set_value) {
		case M2UAASPASADMINISTRATIVESTATE_LOCKED:
		case M2UAASPASADMINISTRATIVESTATE_UNLOCKED:
		case M2UAASPASADMINISTRATIVESTATE_SHUTTINGDOWN:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspAsAdministrativeState: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in set_value for you to use, and
				   you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		old_value = StorageTmp->m2uaAspAsAdministrativeState;
		StorageTmp->m2uaAspAsAdministrativeState = set_value;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2uaAspAsAdministrativeState = old_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2uaAspSgAsAdministrativeState(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2uaAspSgAsAdministrativeState(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m2uaAspSgAsTable_data *StorageTmp = NULL;
	size_t newlen = name_len - (sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1);
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m2uaAspMIB", "write_m2uaAspSgAsAdministrativeState entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(m2uaAspSgAsTableStorage, NULL, &name[sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspSgAsAdministrativeState not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspSgAsAdministrativeState: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case M2UAASPSGASADMINISTRATIVESTATE_LOCKED:
		case M2UAASPSGASADMINISTRATIVESTATE_UNLOCKED:
		case M2UAASPSGASADMINISTRATIVESTATE_SHUTTINGDOWN:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspSgAsAdministrativeState: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in set_value for you to use, and
				   you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		old_value = StorageTmp->m2uaAspSgAsAdministrativeState;
		StorageTmp->m2uaAspSgAsAdministrativeState = set_value;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2uaAspSgAsAdministrativeState = old_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2uaAspSgAsRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m2uaAspSgAsRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m2uaAspSgAsTable_data *StorageTmp = NULL;
	size_t newlen = name_len - (sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1);
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m2uaAspMIB", "write_m2uaAspSgAsRowStatus entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(m2uaAspSgAsTableStorage, NULL, &name[sizeof(m2uaAspMIB_variables_oid) / sizeof(oid) + 7 - 1], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */
	switch (action) {
	case RESERVE1:
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspSgAsRowStatus not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspSgAsRowStatus: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		switch (set_value) {
		case M2UAASPSGASROWSTATUS_DOWN:
		case M2UAASPSGASROWSTATUS_INITIALIZING:
		case M2UAASPSGASROWSTATUS_TERMINATING:
		case M2UAASPSGASROWSTATUS_INACTIVE:
		case M2UAASPSGASROWSTATUS_ACTIVATING:
		case M2UAASPSGASROWSTATUS_DEACTIVATING:
		case M2UAASPSGASROWSTATUS_ACTIVE:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspSgAsRowStatus: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		break;
	case FREE:		/* Release any resources that have been allocated */
		break;
	case ACTION:		/* The variable has been stored in set_value for you to use, and
				   you have just been asked to do something with it.  Note that
				   anything done here must be reversable in the UNDO case */
		old_value = StorageTmp->m2uaAspSgAsRowStatus;
		StorageTmp->m2uaAspSgAsRowStatus = set_value;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m2uaAspSgAsRowStatus = old_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change
				   permanently.  Make sure that anything done here can't fail! */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2uaAspRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_m2uaAspRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct m2uaAspTable_data *StorageTmp = NULL;
	static struct m2uaAspTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - (sizeof(m2uaAspTable_variables_oid) / sizeof(oid) + 3 - 1);
	static int old_value;
	int set_value;
	static struct variable_list *vars, *vp;

	StorageTmp = header_complex(m2uaAspTableStorage, NULL, &name[sizeof(m2uaAspTable_variables_oid) / sizeof(oid) + 3 - 1], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->m2uaAspRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->m2uaAspTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_DESTROY:
			/* destroying existent or non-existent row is ok */
			if (StorageTmp == NULL)
				break;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->m2uaAspTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			vars = NULL;
			/* m2uaAspId */
			snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0);
			if (header_complex_parse_oid(&(name[sizeof(m2uaAspTable_variables_oid) / sizeof(oid) + 2]), newlen, vars) != SNMPERR_SUCCESS) {
				/* XXX: free, zero vars */
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			if ((StorageNew = m2uaAspTable_create()) == NULL) {
				/* XXX: free, zero vars */
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			memdup((void *) &StorageNew->m2uaAspId, vp->val.string, vp->val_len);
			StorageNew->m2uaAspIdLen = vp->val_len;
			vp = vp->next_variable;
			/* XXX: free, zero vars, no longer needed? */
			break;
		}
		break;
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			m2uaAspTable_destroy(&StorageNew);
			/* XXX: free, zero vars */
			break;
		}
		break;
	case ACTION:
		/* The variable has been stored in set_value for you to use, and you have just been 
		   asked to do something with it.  Note that anything done here must be reversable
		   in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* row creation, so add it */
			m2uaAspTable_add(StorageNew);
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* set the flag? */
			old_value = StorageTmp->m2uaAspRowStatus;
			StorageTmp->m2uaAspRowStatus = set_value;
			break;
		case RS_DESTROY:
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				m2uaAspTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* row creation, so remove it again */
			m2uaAspTable_del(StorageNew);
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* restore state */
			StorageTmp->m2uaAspRowStatus = old_value;
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			m2uaAspTable_add(StorageDel);
			break;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make
		   sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* row creation, set final state */
			/* XXX: check if row is ready, otherwise leave at RS_NOTREADY */
			StorageTmp->m2uaAspRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			/* row creation, set final state */
			/* XXX: check if row is ready, otherwise leave at RS_NOTREADY */
			StorageTmp->m2uaAspRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* state change already performed */
			break;
		case RS_DESTROY:
			/* row deletion, free it its dead */
			m2uaAspTable_destroy(&StorageDel);
			/* m2uaAspTable_destroy() can handle NULL pointers. */
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2uaSgRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_m2uaSgRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct m2uaSgTable_data *StorageTmp = NULL;
	static struct m2uaSgTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - (sizeof(m2uaSgTable_variables_oid) / sizeof(oid) + 3 - 1);
	static int old_value;
	int set_value;
	static struct variable_list *vars, *vp;

	StorageTmp = header_complex(m2uaSgTableStorage, NULL, &name[sizeof(m2uaSgTable_variables_oid) / sizeof(oid) + 3 - 1], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaSgRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaSgRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->m2uaSgRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->m2uaSgTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_DESTROY:
			/* destroying existent or non-existent row is ok */
			if (StorageTmp == NULL)
				break;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->m2uaSgTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			vars = NULL;
			/* m2uaSgId */
			snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0);
			if (header_complex_parse_oid(&(name[sizeof(m2uaSgTable_variables_oid) / sizeof(oid) + 2]), newlen, vars) != SNMPERR_SUCCESS) {
				/* XXX: free, zero vars */
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			if ((StorageNew = m2uaSgTable_create()) == NULL) {
				/* XXX: free, zero vars */
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			memdup((void *) &StorageNew->m2uaSgId, vp->val.string, vp->val_len);
			StorageNew->m2uaSgIdLen = vp->val_len;
			vp = vp->next_variable;
			/* XXX: free, zero vars, no longer needed? */
			break;
		}
		break;
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			m2uaSgTable_destroy(&StorageNew);
			/* XXX: free, zero vars */
			break;
		}
		break;
	case ACTION:
		/* The variable has been stored in set_value for you to use, and you have just been 
		   asked to do something with it.  Note that anything done here must be reversable
		   in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* row creation, so add it */
			m2uaSgTable_add(StorageNew);
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* set the flag? */
			old_value = StorageTmp->m2uaSgRowStatus;
			StorageTmp->m2uaSgRowStatus = set_value;
			break;
		case RS_DESTROY:
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				m2uaSgTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* row creation, so remove it again */
			m2uaSgTable_del(StorageNew);
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* restore state */
			StorageTmp->m2uaSgRowStatus = old_value;
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			m2uaSgTable_add(StorageDel);
			break;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make
		   sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* row creation, set final state */
			/* XXX: check if row is ready, otherwise leave at RS_NOTREADY */
			StorageTmp->m2uaSgRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			/* row creation, set final state */
			/* XXX: check if row is ready, otherwise leave at RS_NOTREADY */
			StorageTmp->m2uaSgRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* state change already performed */
			break;
		case RS_DESTROY:
			/* row deletion, free it its dead */
			m2uaSgTable_destroy(&StorageDel);
			/* m2uaSgTable_destroy() can handle NULL pointers. */
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2uaAspSgRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_m2uaAspSgRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct m2uaAspSgTable_data *StorageTmp = NULL;
	static struct m2uaAspSgTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - (sizeof(m2uaAspSgTable_variables_oid) / sizeof(oid) + 3 - 1);
	static int old_value;
	int set_value;
	static struct variable_list *vars, *vp;

	StorageTmp = header_complex(m2uaAspSgTableStorage, NULL, &name[sizeof(m2uaAspSgTable_variables_oid) / sizeof(oid) + 3 - 1], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspSgRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspSgRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->m2uaAspSgRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->m2uaAspSgTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_DESTROY:
			/* destroying existent or non-existent row is ok */
			if (StorageTmp == NULL)
				break;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->m2uaAspSgTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			vars = NULL;
			/* m2uaAspId */
			snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0);
			/* m2uaSgId */
			snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0);
			if (header_complex_parse_oid(&(name[sizeof(m2uaAspSgTable_variables_oid) / sizeof(oid) + 2]), newlen, vars) != SNMPERR_SUCCESS) {
				/* XXX: free, zero vars */
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			if ((StorageNew = m2uaAspSgTable_create()) == NULL) {
				/* XXX: free, zero vars */
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			memdup((void *) &StorageNew->m2uaAspId, vp->val.string, vp->val_len);
			StorageNew->m2uaAspIdLen = vp->val_len;
			vp = vp->next_variable;
			memdup((void *) &StorageNew->m2uaSgId, vp->val.string, vp->val_len);
			StorageNew->m2uaSgIdLen = vp->val_len;
			vp = vp->next_variable;
			/* XXX: free, zero vars, no longer needed? */
			break;
		}
		break;
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			m2uaAspSgTable_destroy(&StorageNew);
			/* XXX: free, zero vars */
			break;
		}
		break;
	case ACTION:
		/* The variable has been stored in set_value for you to use, and you have just been 
		   asked to do something with it.  Note that anything done here must be reversable
		   in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* row creation, so add it */
			m2uaAspSgTable_add(StorageNew);
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* set the flag? */
			old_value = StorageTmp->m2uaAspSgRowStatus;
			StorageTmp->m2uaAspSgRowStatus = set_value;
			break;
		case RS_DESTROY:
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				m2uaAspSgTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* row creation, so remove it again */
			m2uaAspSgTable_del(StorageNew);
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* restore state */
			StorageTmp->m2uaAspSgRowStatus = old_value;
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			m2uaAspSgTable_add(StorageDel);
			break;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make
		   sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* row creation, set final state */
			/* XXX: check if row is ready, otherwise leave at RS_NOTREADY */
			StorageTmp->m2uaAspSgRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			/* row creation, set final state */
			/* XXX: check if row is ready, otherwise leave at RS_NOTREADY */
			StorageTmp->m2uaAspSgRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* state change already performed */
			break;
		case RS_DESTROY:
			/* row deletion, free it its dead */
			m2uaAspSgTable_destroy(&StorageDel);
			/* m2uaAspSgTable_destroy() can handle NULL pointers. */
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2uaAsRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_m2uaAsRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct m2uaAsTable_data *StorageTmp = NULL;
	static struct m2uaAsTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - (sizeof(m2uaAsTable_variables_oid) / sizeof(oid) + 3 - 1);
	static int old_value;
	int set_value;
	static struct variable_list *vars, *vp;

	StorageTmp = header_complex(m2uaAsTableStorage, NULL, &name[sizeof(m2uaAsTable_variables_oid) / sizeof(oid) + 3 - 1], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAsRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAsRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->m2uaAsRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->m2uaAsTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_DESTROY:
			/* destroying existent or non-existent row is ok */
			if (StorageTmp == NULL)
				break;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->m2uaAsTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			vars = NULL;
			/* m2uaAsId */
			snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0);
			if (header_complex_parse_oid(&(name[sizeof(m2uaAsTable_variables_oid) / sizeof(oid) + 2]), newlen, vars) != SNMPERR_SUCCESS) {
				/* XXX: free, zero vars */
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			if ((StorageNew = m2uaAsTable_create()) == NULL) {
				/* XXX: free, zero vars */
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			memdup((void *) &StorageNew->m2uaAsId, vp->val.string, vp->val_len);
			StorageNew->m2uaAsIdLen = vp->val_len;
			vp = vp->next_variable;
			/* XXX: free, zero vars, no longer needed? */
			break;
		}
		break;
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			m2uaAsTable_destroy(&StorageNew);
			/* XXX: free, zero vars */
			break;
		}
		break;
	case ACTION:
		/* The variable has been stored in set_value for you to use, and you have just been 
		   asked to do something with it.  Note that anything done here must be reversable
		   in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* row creation, so add it */
			m2uaAsTable_add(StorageNew);
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* set the flag? */
			old_value = StorageTmp->m2uaAsRowStatus;
			StorageTmp->m2uaAsRowStatus = set_value;
			break;
		case RS_DESTROY:
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				m2uaAsTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* row creation, so remove it again */
			m2uaAsTable_del(StorageNew);
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* restore state */
			StorageTmp->m2uaAsRowStatus = old_value;
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			m2uaAsTable_add(StorageDel);
			break;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make
		   sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* row creation, set final state */
			/* XXX: check if row is ready, otherwise leave at RS_NOTREADY */
			StorageTmp->m2uaAsRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			/* row creation, set final state */
			/* XXX: check if row is ready, otherwise leave at RS_NOTREADY */
			StorageTmp->m2uaAsRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* state change already performed */
			break;
		case RS_DESTROY:
			/* row deletion, free it its dead */
			m2uaAsTable_destroy(&StorageDel);
			/* m2uaAsTable_destroy() can handle NULL pointers. */
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2uaIfRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_m2uaIfRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct m2uaIfTable_data *StorageTmp = NULL;
	static struct m2uaIfTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - (sizeof(m2uaIfTable_variables_oid) / sizeof(oid) + 3 - 1);
	static int old_value;
	int set_value;
	static struct variable_list *vars, *vp;

	StorageTmp = header_complex(m2uaIfTableStorage, NULL, &name[sizeof(m2uaIfTable_variables_oid) / sizeof(oid) + 3 - 1], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaIfRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaIfRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->m2uaIfRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->m2uaIfTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_DESTROY:
			/* destroying existent or non-existent row is ok */
			if (StorageTmp == NULL)
				break;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->m2uaIfTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			vars = NULL;
			/* m2uaIfId */
			snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0);
			if (header_complex_parse_oid(&(name[sizeof(m2uaIfTable_variables_oid) / sizeof(oid) + 2]), newlen, vars) != SNMPERR_SUCCESS) {
				/* XXX: free, zero vars */
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			if ((StorageNew = m2uaIfTable_create()) == NULL) {
				/* XXX: free, zero vars */
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			memdup((void *) &StorageNew->m2uaIfId, vp->val.string, vp->val_len);
			StorageNew->m2uaIfIdLen = vp->val_len;
			vp = vp->next_variable;
			/* XXX: free, zero vars, no longer needed? */
			break;
		}
		break;
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			m2uaIfTable_destroy(&StorageNew);
			/* XXX: free, zero vars */
			break;
		}
		break;
	case ACTION:
		/* The variable has been stored in set_value for you to use, and you have just been 
		   asked to do something with it.  Note that anything done here must be reversable
		   in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* row creation, so add it */
			m2uaIfTable_add(StorageNew);
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* set the flag? */
			old_value = StorageTmp->m2uaIfRowStatus;
			StorageTmp->m2uaIfRowStatus = set_value;
			break;
		case RS_DESTROY:
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				m2uaIfTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* row creation, so remove it again */
			m2uaIfTable_del(StorageNew);
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* restore state */
			StorageTmp->m2uaIfRowStatus = old_value;
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			m2uaIfTable_add(StorageDel);
			break;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make
		   sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* row creation, set final state */
			/* XXX: check if row is ready, otherwise leave at RS_NOTREADY */
			StorageTmp->m2uaIfRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			/* row creation, set final state */
			/* XXX: check if row is ready, otherwise leave at RS_NOTREADY */
			StorageTmp->m2uaIfRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* state change already performed */
			break;
		case RS_DESTROY:
			/* row deletion, free it its dead */
			m2uaIfTable_destroy(&StorageDel);
			/* m2uaIfTable_destroy() can handle NULL pointers. */
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2uaSgAsRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_m2uaSgAsRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct m2uaSgAsTable_data *StorageTmp = NULL;
	static struct m2uaSgAsTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - (sizeof(m2uaSgAsTable_variables_oid) / sizeof(oid) + 3 - 1);
	static int old_value;
	int set_value;
	static struct variable_list *vars, *vp;

	StorageTmp = header_complex(m2uaSgAsTableStorage, NULL, &name[sizeof(m2uaSgAsTable_variables_oid) / sizeof(oid) + 3 - 1], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaSgAsRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaSgAsRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->m2uaSgAsRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->m2uaSgAsTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_DESTROY:
			/* destroying existent or non-existent row is ok */
			if (StorageTmp == NULL)
				break;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->m2uaSgAsTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			vars = NULL;
			/* m2uaSgId */
			snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0);
			/* m2uaAsId */
			snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0);
			if (header_complex_parse_oid(&(name[sizeof(m2uaSgAsTable_variables_oid) / sizeof(oid) + 2]), newlen, vars) != SNMPERR_SUCCESS) {
				/* XXX: free, zero vars */
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			if ((StorageNew = m2uaSgAsTable_create()) == NULL) {
				/* XXX: free, zero vars */
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			memdup((void *) &StorageNew->m2uaSgId, vp->val.string, vp->val_len);
			StorageNew->m2uaSgIdLen = vp->val_len;
			vp = vp->next_variable;
			memdup((void *) &StorageNew->m2uaAsId, vp->val.string, vp->val_len);
			StorageNew->m2uaAsIdLen = vp->val_len;
			vp = vp->next_variable;
			/* XXX: free, zero vars, no longer needed? */
			break;
		}
		break;
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			m2uaSgAsTable_destroy(&StorageNew);
			/* XXX: free, zero vars */
			break;
		}
		break;
	case ACTION:
		/* The variable has been stored in set_value for you to use, and you have just been 
		   asked to do something with it.  Note that anything done here must be reversable
		   in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* row creation, so add it */
			m2uaSgAsTable_add(StorageNew);
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* set the flag? */
			old_value = StorageTmp->m2uaSgAsRowStatus;
			StorageTmp->m2uaSgAsRowStatus = set_value;
			break;
		case RS_DESTROY:
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				m2uaSgAsTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* row creation, so remove it again */
			m2uaSgAsTable_del(StorageNew);
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* restore state */
			StorageTmp->m2uaSgAsRowStatus = old_value;
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			m2uaSgAsTable_add(StorageDel);
			break;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make
		   sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* row creation, set final state */
			/* XXX: check if row is ready, otherwise leave at RS_NOTREADY */
			StorageTmp->m2uaSgAsRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			/* row creation, set final state */
			/* XXX: check if row is ready, otherwise leave at RS_NOTREADY */
			StorageTmp->m2uaSgAsRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* state change already performed */
			break;
		case RS_DESTROY:
			/* row deletion, free it its dead */
			m2uaSgAsTable_destroy(&StorageDel);
			/* m2uaSgAsTable_destroy() can handle NULL pointers. */
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2uaAspAsAsState(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_m2uaAspAsAsState(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct m2uaAspAsTable_data *StorageTmp = NULL;
	static struct m2uaAspAsTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - (sizeof(m2uaAspAsTable_variables_oid) / sizeof(oid) + 3 - 1);
	static int old_value;
	int set_value;
	static struct variable_list *vars, *vp;

	StorageTmp = header_complex(m2uaAspAsTableStorage, NULL, &name[sizeof(m2uaAspAsTable_variables_oid) / sizeof(oid) + 3 - 1], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspAsAsState not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspAsAsState: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->m2uaAspAsAsState == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->m2uaAspAsTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_DESTROY:
			/* destroying existent or non-existent row is ok */
			if (StorageTmp == NULL)
				break;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->m2uaAspAsTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			vars = NULL;
			/* m2uaAspId */
			snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0);
			/* m2uaAsId */
			snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0);
			if (header_complex_parse_oid(&(name[sizeof(m2uaAspAsTable_variables_oid) / sizeof(oid) + 2]), newlen, vars) != SNMPERR_SUCCESS) {
				/* XXX: free, zero vars */
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			if ((StorageNew = m2uaAspAsTable_create()) == NULL) {
				/* XXX: free, zero vars */
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			memdup((void *) &StorageNew->m2uaAspId, vp->val.string, vp->val_len);
			StorageNew->m2uaAspIdLen = vp->val_len;
			vp = vp->next_variable;
			memdup((void *) &StorageNew->m2uaAsId, vp->val.string, vp->val_len);
			StorageNew->m2uaAsIdLen = vp->val_len;
			vp = vp->next_variable;
			/* XXX: free, zero vars, no longer needed? */
			break;
		}
		break;
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			m2uaAspAsTable_destroy(&StorageNew);
			/* XXX: free, zero vars */
			break;
		}
		break;
	case ACTION:
		/* The variable has been stored in set_value for you to use, and you have just been 
		   asked to do something with it.  Note that anything done here must be reversable
		   in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* row creation, so add it */
			m2uaAspAsTable_add(StorageNew);
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* set the flag? */
			old_value = StorageTmp->m2uaAspAsAsState;
			StorageTmp->m2uaAspAsAsState = set_value;
			break;
		case RS_DESTROY:
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				m2uaAspAsTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* row creation, so remove it again */
			m2uaAspAsTable_del(StorageNew);
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* restore state */
			StorageTmp->m2uaAspAsAsState = old_value;
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			m2uaAspAsTable_add(StorageDel);
			break;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make
		   sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* row creation, set final state */
			/* XXX: check if row is ready, otherwise leave at RS_NOTREADY */
			StorageTmp->m2uaAspAsAsState = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			/* row creation, set final state */
			/* XXX: check if row is ready, otherwise leave at RS_NOTREADY */
			StorageTmp->m2uaAspAsAsState = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* state change already performed */
			break;
		case RS_DESTROY:
			/* row deletion, free it its dead */
			m2uaAspAsTable_destroy(&StorageDel);
			/* m2uaAspAsTable_destroy() can handle NULL pointers. */
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m2uaAspAsRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_m2uaAspAsRowStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct m2uaAspAsTable_data *StorageTmp = NULL;
	static struct m2uaAspAsTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - (sizeof(m2uaAspAsTable_variables_oid) / sizeof(oid) + 3 - 1);
	static int old_value;
	int set_value;
	static struct variable_list *vars, *vp;

	StorageTmp = header_complex(m2uaAspAsTableStorage, NULL, &name[sizeof(m2uaAspAsTable_variables_oid) / sizeof(oid) + 3 - 1], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspAsRowStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to m2uaAspAsRowStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->m2uaAspAsRowStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->m2uaAspAsTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_DESTROY:
			/* destroying existent or non-existent row is ok */
			if (StorageTmp == NULL)
				break;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->m2uaAspAsTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			vars = NULL;
			/* m2uaAspId */
			snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0);
			/* m2uaAsId */
			snmp_varlist_add_variable(&vars, NULL, 0, ASN_OCTET_STR, NULL, 0);
			if (header_complex_parse_oid(&(name[sizeof(m2uaAspAsTable_variables_oid) / sizeof(oid) + 2]), newlen, vars) != SNMPERR_SUCCESS) {
				/* XXX: free, zero vars */
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			if ((StorageNew = m2uaAspAsTable_create()) == NULL) {
				/* XXX: free, zero vars */
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			memdup((void *) &StorageNew->m2uaAspId, vp->val.string, vp->val_len);
			StorageNew->m2uaAspIdLen = vp->val_len;
			vp = vp->next_variable;
			memdup((void *) &StorageNew->m2uaAsId, vp->val.string, vp->val_len);
			StorageNew->m2uaAsIdLen = vp->val_len;
			vp = vp->next_variable;
			/* XXX: free, zero vars, no longer needed? */
			break;
		}
		break;
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			m2uaAspAsTable_destroy(&StorageNew);
			/* XXX: free, zero vars */
			break;
		}
		break;
	case ACTION:
		/* The variable has been stored in set_value for you to use, and you have just been 
		   asked to do something with it.  Note that anything done here must be reversable
		   in the UNDO case */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* row creation, so add it */
			m2uaAspAsTable_add(StorageNew);
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* set the flag? */
			old_value = StorageTmp->m2uaAspAsRowStatus;
			StorageTmp->m2uaAspAsRowStatus = set_value;
			break;
		case RS_DESTROY:
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				m2uaAspAsTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* row creation, so remove it again */
			m2uaAspAsTable_del(StorageNew);
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* restore state */
			StorageTmp->m2uaAspAsRowStatus = old_value;
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			m2uaAspAsTable_add(StorageDel);
			break;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make
		   sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* row creation, set final state */
			/* XXX: check if row is ready, otherwise leave at RS_NOTREADY */
			StorageTmp->m2uaAspAsRowStatus = RS_ACTIVE;
			break;
		case RS_CREATEANDWAIT:
			/* row creation, set final state */
			/* XXX: check if row is ready, otherwise leave at RS_NOTREADY */
			StorageTmp->m2uaAspAsRowStatus = RS_NOTINSERVICE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* state change already performed */
			break;
		case RS_DESTROY:
			/* row deletion, free it its dead */
			m2uaAspAsTable_destroy(&StorageDel);
			/* m2uaAspAsTable_destroy() can handle NULL pointers. */
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}
