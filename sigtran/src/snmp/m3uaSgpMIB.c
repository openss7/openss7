/*****************************************************************************

 @(#) $RCSfile: m3uaSgpMIB.c,v $ $Name:  $($Revision: 0.9.2.7 $) $Date: 2009-01-19 13:31:38 $

 -----------------------------------------------------------------------------

 Copyright (c) 2008-2009  Monavacon Limited <http://www.monavacon.com/>
 Copyright (c) 2001-2008  OpenSS7 Corporation <http://www.openss7.com/>
 Copyright (c) 1997-2001  Brian F. G. Bidulock <bidulock@openss7.org>

 All Rights Reserved.

 This program is free software: you can redistribute it and/or modify it under
 the terms of the GNU Affero General Public License as published by the Free
 Software Foundation, version 3 of the license.

 This program is distributed in the hope that it will be useful, but WITHOUT
 ANY WARRANTY; without even the implied warranty of MERCHANTABILITY or FITNESS
 FOR A PARTICULAR PURPOSE.  See the GNU Affero General Public License for more
 details.

 You should have received a copy of the GNU Affero General Public License
 along with this program.  If not, see <http://www.gnu.org/licenses/>, or
 write to the Free Software Foundation, Inc., 675 Mass Ave, Cambridge, MA
 02139, USA.

 -----------------------------------------------------------------------------

 U.S. GOVERNMENT RESTRICTED RIGHTS.  If you are licensing this Software on
 behalf of the U.S. Government ("Government"), the following provisions apply
 to you.  If the Software is supplied by the Department of Defense ("DoD"), it
 is classified as "Commercial Computer Software" under paragraph 252.227-7014
 of the DoD Supplement to the Federal Acquisition Regulations ("DFARS") (or any
 successor regulations) and the Government is acquiring only the license rights
 granted herein (the license rights customarily provided to non-Government
 users).  If the Software is supplied to any unit or agency of the Government
 other than DoD, it is classified as "Restricted Computer Software" and the
 Government's rights in the Software are defined in paragraph 52.227-19 of the
 Federal Acquisition Regulations ("FAR") (or any successor regulations) or, in
 the cases of NASA, in paragraph 18.52.227-86 of the NASA Supplement to the FAR
 (or any successor regulations).

 -----------------------------------------------------------------------------

 Commercial licensing and support of this software is available from OpenSS7
 Corporation at a fee.  See http://www.openss7.com/

 -----------------------------------------------------------------------------

 Last Modified $Date: 2009-01-19 13:31:38 $ by $Author: brian $

 -----------------------------------------------------------------------------

 $Log: m3uaSgpMIB.c,v $
 Revision 0.9.2.7  2009-01-19 13:31:38  brian
 - updating standalone agents

 Revision 0.9.2.6  2009-01-10 17:31:01  brian
 - agent updates

 Revision 0.9.2.5  2009-01-04 13:16:09  brian
 - updated agents

 Revision 0.9.2.4  2009-01-03 10:50:59  brian
 - updated agent

 Revision 0.9.2.3  2009-01-02 15:04:09  brian
 - updated agents

 Revision 0.9.2.2  2008-12-31 16:04:42  brian
 - updated mibs

 Revision 0.9.2.1  2008-12-27 10:01:52  brian
 - updated mibs and stubs

 *****************************************************************************/

#ident "@(#) $RCSfile: m3uaSgpMIB.c,v $ $Name:  $($Revision: 0.9.2.7 $) $Date: 2009-01-19 13:31:38 $"

static char const ident[] = "$RCSfile: m3uaSgpMIB.c,v $ $Name:  $($Revision: 0.9.2.7 $) $Date: 2009-01-19 13:31:38 $";

/* This file was generated by mib2c and is intended for use as
   a mib module for the ucd-snmp snmpd agent. */
#include <ucd-snmp/ucd-snmp-config.h>
#include <ucd-snmp/ucd-snmp-includes.h>
#include <ucd-snmp/ucd-snmp-agent-includes.h>
#include <ucd-snmp/agent_trap.h>
#include <ucd-snmp/callback.h>
#include <ucd-snmp/snmp-tc.h>
#include <ucd-snmp/default_store.h>
#include <ucd-snmp/snmp_alarm.h>
/* The following header files are mangled in most recent net-snmp releases so
 * the versions from UCD-SNMP 4.2.5 are included here.  */
#if defined HAVE_LIBNETSNMP
#else				/* defined HAVE_LIBNETSNMP */
#endif				/* defined HAVE_LIBNETSNMP */
/* These are messed up on both. */
#include "ds_agent.h"
#ifdef HAVE_UCD_SNMP_UTIL_FUNCS_H
#include <ucd-snmp/util_funcs.h>
/* Many recent net-snmp UCD compatible headers do not declare header_generic. */
int header_generic(struct variable *, oid *, size_t *, int, size_t *, WriteMethod **);
#else				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#include "util_funcs.h"
#endif				/* HAVE_UCD_SNMP_UTIL_FUNCS_H */
#ifdef HAVE_UCD_SNMP_HEADER_COMPLEX_H
#include <ucd-snmp/header_complex.h>
#else				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
#include "header_complex.h"
#endif				/* HAVE_UCD_SNMP_HEADER_COMPLEX_H */
/* This one is the other way around: it is just fine for net-snmp, but
 * ucd-snmp does not provide the header file at all.  */
#ifdef HAVE_UCD_SNMP_MIB_MODULES_H
#include <ucd-snmp/mib_modules.h>
#else				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#ifdef HAVE_NET_SNMP_AGENT_MIB_MODULES_H
#include <net-snmp/agent/mib_modules.h>
#else				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#include "mib_modules.h"
#endif				/* HAVE_NET_SNMP_AGENT_MIB_MODULES_H */
#endif				/* HAVE_UCD_SNMP_MIB_MODULES_H */
#include <stdint.h>
#include <signal.h>
#include <sys/stat.h>		/* for struct stat, fstat() */
#include <sys/types.h>
#include <unistd.h>
#include <stdlib.h>
#include <pwd.h>		/* for getpwuid() getpwnam() */
#include <grp.h>		/* for getgrgid() getgrnam() */
#include <libgen.h>		/* for basename() */
#include <fcntl.h>		/* for O_CREAT */
#include <sys/sysctl.h>		/* for sysctl */
#include <errno.h>
#include <string.h>
#ifdef _GNU_SOURCE
#include <getopt.h>
#endif
#include "m3uaSgpMIB.h"
#define MY_FACILITY(__pri)	(LOG_DAEMON|(__pri))
#if defined MODULE
#if defined MASTER
const char sa_program[] = "m3uaSgpMIB";
int sa_fclose = 1;			/* default close files between requests */
int sa_changed = 1;			/* indication to reread MIB configuration */
int sa_stats_refresh = 1;		/* indications that statistics, the mib or its tables need to be refreshed */
int sa_request = 1;			/* request number for per-request actions */
int sa_dump = 0;			/* default packet dump */
int sa_debug = 0;			/* default no debug */
#endif				/* defined MASTER */
#endif				/* defined MODULE */
static int my_fd = -1;			/* file descriptor for this MIB's use */
static int my_readfd = -1;		/* file descriptor for autonomnous events */
volatile int m3uaSgpMIB_refresh = 1;
volatile int m3uaSgpAgTable_refresh = 1;
volatile int m3uaSgpSgTable_refresh = 1;
volatile int m3uaSgpAspTable_refresh = 1;
volatile int m3uaSgpTable_refresh = 1;

/*
 * m3uaSgpMIB_variables_oid: object identifier for m3uaSgpMIB
 * This is the top level oid that we want to register under.  This is essentially a prefix, with the
 * suffix appearing in the variable below.
 */
oid m3uaSgpMIB_variables_oid[9] = { 1, 3, 6, 1, 4, 1, 29591, 1, 321 };
oid m3uaSgpAgTable_variables_oid[14] = { 1, 3, 6, 1, 4, 1, 29591, 1, 321, 1, 2, 1, 2, 1 };
oid m3uaSgpSgTable_variables_oid[14] = { 1, 3, 6, 1, 4, 1, 29591, 1, 321, 1, 2, 2, 1, 1 };
oid m3uaSgpAspTable_variables_oid[14] = { 1, 3, 6, 1, 4, 1, 29591, 1, 321, 1, 2, 4, 1, 1 };

/*
 * Oids for use in notifications defined in this MIB.
 */

/*
 * Oids accessible only for notify defined in this MIB.
 */

/*
 * Other oids defined in this MIB.
 */
oid m3uaRfc3332ProtocolVersion_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 1, 321, 1, 4, 1, 1 };
oid m3uaTs102142ProtocolVersion_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 1, 321, 1, 4, 1, 2 };
oid m3uaRfc4666ProtocolVersion_oid[13] = { 1, 3, 6, 1, 4, 1, 29591, 1, 321, 1, 4, 1, 3 };
static oid zeroDotZero_oid[2] = { 0, 0 };
static oid snmpTrapOID_oid[11] = { 1, 3, 6, 1, 6, 3, 1, 1, 4, 1, 0 };

/*
 * variable7 m3uaSgpMIB_variables: tree for m3uaSgpMIB
 * This variable defines function callbacks and type return information for the m3uaSgpMIB mib section
 */
struct variable7 m3uaSgpMIB_variables[] = {
	/* magic number, variable type, ro/rw, callback fn, L, oidsuffix */
#define   M3UASGPAGNEXTINDEX    1
	{(u_char) M3UASGPAGNEXTINDEX, ASN_UNSIGNED, RONLY, var_m3uaSgpMIB, 4, {1, 2, 1, 1}},
#define   M3UASGPAGNAME         2
	{(u_char) M3UASGPAGNAME, ASN_OCTET_STR, RWRITE, var_m3uaSgpAgTable, 6, {1, 2, 1, 2, 1, 2}},
#define   M3UASGPAGPROTOCOLVERSION  3
	{(u_char) M3UASGPAGPROTOCOLVERSION, ASN_OBJECT_ID, RWRITE, var_m3uaSgpAgTable, 6, {1, 2, 1, 2, 1, 3}},
#define   M3UASGPAGOPTIONS      4
	{(u_char) M3UASGPAGOPTIONS, ASN_BIT_STR, RWRITE, var_m3uaSgpAgTable, 6, {1, 2, 1, 2, 1, 4}},
#define   M3UASGPAGREGISTRATIONPOLICY  5
	{(u_char) M3UASGPAGREGISTRATIONPOLICY, ASN_INTEGER, RWRITE, var_m3uaSgpAgTable, 6, {1, 2, 1, 2, 1, 5}},
#define   M3UASGPAGASPIDPOLICY  6
	{(u_char) M3UASGPAGASPIDPOLICY, ASN_INTEGER, RWRITE, var_m3uaSgpAgTable, 6, {1, 2, 1, 2, 1, 6}},
#define   M3UASGPAGPROTOCOLPAYLOADID  7
	{(u_char) M3UASGPAGPROTOCOLPAYLOADID, ASN_INTEGER, RWRITE, var_m3uaSgpAgTable, 6, {1, 2, 1, 2, 1, 7}},
#define   M3UASGPAGIPPORT       8
	{(u_char) M3UASGPAGIPPORT, ASN_INTEGER, RWRITE, var_m3uaSgpAgTable, 6, {1, 2, 1, 2, 1, 8}},
#define   M3UASGPAGMINOSTREAMS  9
	{(u_char) M3UASGPAGMINOSTREAMS, ASN_INTEGER, RWRITE, var_m3uaSgpAgTable, 6, {1, 2, 1, 2, 1, 9}},
#define   M3UASGPAGMAXISTREAMS  10
	{(u_char) M3UASGPAGMAXISTREAMS, ASN_INTEGER, RWRITE, var_m3uaSgpAgTable, 6, {1, 2, 1, 2, 1, 10}},
#define   M3UASGPAGSTATUS       11
	{(u_char) M3UASGPAGSTATUS, ASN_INTEGER, RWRITE, var_m3uaSgpAgTable, 6, {1, 2, 1, 2, 1, 11}},
#define   M3UASGPSGNAME         12
	{(u_char) M3UASGPSGNAME, ASN_OCTET_STR, RWRITE, var_m3uaSgpSgTable, 6, {1, 2, 2, 1, 1, 2}},
#define   M3UASGPSGSTATUS       13
	{(u_char) M3UASGPSGSTATUS, ASN_INTEGER, RWRITE, var_m3uaSgpSgTable, 6, {1, 2, 2, 1, 1, 3}},
#define   M3UASGPSCTPPROFILENEXTINDEX  14
	{(u_char) M3UASGPSCTPPROFILENEXTINDEX,, RONLY, var_m3uaSgpMIB, 4, {1, 2, 3, 1}},
#define   M3UASGPSCTPPROFILETABLE  15
	{(u_char) M3UASGPSCTPPROFILETABLE,, RONLY, var_m3uaSgpMIB, 4, {1, 2, 3, 2}},
#define   M3UASGPASPNAME        16
	{(u_char) M3UASGPASPNAME, ASN_OCTET_STR, RWRITE, var_m3uaSgpAspTable, 6, {1, 2, 4, 1, 1, 2}},
#define   M3UASGPASPOPERATIONALSTATE  17
	{(u_char) M3UASGPASPOPERATIONALSTATE, ASN_INTEGER, RONLY, var_m3uaSgpAspTable, 6, {1, 2, 4, 1, 1, 3}},
#define   M3UASGPASPUSAGESTATE  18
	{(u_char) M3UASGPASPUSAGESTATE, ASN_INTEGER, RONLY, var_m3uaSgpAspTable, 6, {1, 2, 4, 1, 1, 4}},
#define   M3UASGPASPADMINISTRATIVESTATE  19
	{(u_char) M3UASGPASPADMINISTRATIVESTATE, ASN_INTEGER, RWRITE, var_m3uaSgpAspTable, 6, {1, 2, 4, 1, 1, 5}},
#define   M3UASGPASPALARMSTATUS  20
	{(u_char) M3UASGPASPALARMSTATUS, ASN_BIT_STR, RWRITE, var_m3uaSgpAspTable, 6, {1, 2, 4, 1, 1, 6}},
#define   M3UASGPASPPROCEDURALSTATUS  21
	{(u_char) M3UASGPASPPROCEDURALSTATUS, ASN_BIT_STR, RONLY, var_m3uaSgpAspTable, 6, {1, 2, 4, 1, 1, 7}},
#define   M3UASGPASPAVAILABILITYSTATUS  22
	{(u_char) M3UASGPASPAVAILABILITYSTATUS, ASN_BIT_STR, RONLY, var_m3uaSgpAspTable, 6, {1, 2, 4, 1, 1, 8}},
#define   M3UASGPASPASPSTATE    23
	{(u_char) M3UASGPASPASPSTATE, ASN_INTEGER, RWRITE, var_m3uaSgpAspTable, 6, {1, 2, 4, 1, 1, 9}},
#define   M3UASGPASPSCTPPROFILE  24
	{(u_char) M3UASGPASPSCTPPROFILE, ASN_OBJECT_ID, RWRITE, var_m3uaSgpAspTable, 6, {1, 2, 4, 1, 1, 10}},
#define   M3UASGPASPMAXINITRETRIES  25
	{(u_char) M3UASGPASPMAXINITRETRIES, ASN_UNSIGNED, RWRITE, var_m3uaSgpAspTable, 6, {1, 2, 4, 1, 1, 11}},
#define   M3UASGPASPMAXPATHRETRANS  26
	{(u_char) M3UASGPASPMAXPATHRETRANS, ASN_UNSIGNED, RWRITE, var_m3uaSgpAspTable, 6, {1, 2, 4, 1, 1, 12}},
#define   M3UASGPASPRTOMIN      27
	{(u_char) M3UASGPASPRTOMIN, ASN_INTEGER, RWRITE, var_m3uaSgpAspTable, 6, {1, 2, 4, 1, 1, 13}},
#define   M3UASGPASPRTOMAX      28
	{(u_char) M3UASGPASPRTOMAX, ASN_INTEGER, RWRITE, var_m3uaSgpAspTable, 6, {1, 2, 4, 1, 1, 14}},
#define   M3UASGPASPHEARTBEATINTERVAL  29
	{(u_char) M3UASGPASPHEARTBEATINTERVAL, ASN_INTEGER, RWRITE, var_m3uaSgpAspTable, 6, {1, 2, 4, 1, 1, 15}},
#define   M3UASGPASPMAXLIFETIME  30
	{(u_char) M3UASGPASPMAXLIFETIME, ASN_INTEGER, RWRITE, var_m3uaSgpAspTable, 6, {1, 2, 4, 1, 1, 16}},
#define   M3UASGPASPTIMERDIVERT  31
	{(u_char) M3UASGPASPTIMERDIVERT, ASN_INTEGER, RWRITE, var_m3uaSgpAspTable, 6, {1, 2, 4, 1, 1, 17}},
#define   M3UASGPASPSTATUS      32
	{(u_char) M3UASGPASPSTATUS, ASN_INTEGER, RWRITE, var_m3uaSgpAspTable, 6, {1, 2, 4, 1, 1, 18}},
#define   M3UASGPNEXTINDEX      33
	{(u_char) M3UASGPNEXTINDEX, ASN_UNSIGNED, RONLY, var_m3uaSgpMIB, 4, {1, 2, 5, 1}},
};

/* (L = length of the oidsuffix) */
struct m3uaSgpMIB_data *m3uaSgpMIBStorage = NULL;

/* global storage of our data, saved in and configured by header_complex() */
struct header_complex_index *m3uaSgpAgTableStorage = NULL;
struct header_complex_index *m3uaSgpSgTableStorage = NULL;
struct header_complex_index *m3uaSgpAspTableStorage = NULL;
struct header_complex_index *m3uaSgpTableStorage = NULL;

void (*m3uaSgpMIBold_signal_handler) (int) = NULL;	/* save old signal handler just in case */
void m3uaSgpMIB_loop_handler(int);
void m3uaSgpMIB_fd_handler(int, void *);

/**
 * @fn void init_m3uaSgpMIB(void)
 * @brief m3uaSgpMIB initialization routine.
 *
 * This is called when the agent starts up.  At a minimum, registration of the MIB variables
 * structure (m3uaSgpMIB_variables) should take place here.  By default the function also
 * registers the configuration handler and configuration store callbacks.
 *
 * Additional registrations that may be considered here are calls to regsiter_readfd(),
 * register_writefd() and register_exceptfd() for hooking into the snmpd event loop, but only when
 * used as a loadable module.  By default this function establishes a single file descriptor to
 * read, or upon which to handle exceptions.  Note that the snmpd only supports a maximum of 32
 * extneral file descriptors, so these should be used sparingly.
 *
 * When running as a loadable module, it is also necessary to hook into the snmpd event loop so that
 * the current request number can be deteremined.  This is accomplished by using a trick of the
 * external_signal_scheduled and external_signal_handler mechanism which is called on each event
 * loop when external_signal_scheduled is non-zero.  This is used to increment the sa_request value
 * on each snmpd event loop interation so that calls to MIB tree functions can determine whether
 * they belong to a fresh request or not (primarily for cacheing and possibly to clean up non-polled
 * file descriptors).
 */
void
init_m3uaSgpMIB(void)
{
	(void) my_fd;
	(void) zeroDotZero_oid;
	(void) snmpTrapOID_oid;
	DEBUGMSGTL(("m3uaSgpMIB", "init_m3uaSgpMIB: initializing...  "));
	/* register ourselves with the agent to handle our mib tree */
	REGISTER_MIB("m3uaSgpMIB", m3uaSgpMIB_variables, variable7, m3uaSgpMIB_variables_oid);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_SHUTDOWN, term_m3uaSgpMIB, NULL);
	/* register our config handler(s) to deal with registrations */
	snmpd_register_config_handler("m3uaSgpMIB", parse_m3uaSgpMIB, NULL, "HELP STRING");
	snmpd_register_config_handler("m3uaSgpAgTable", parse_m3uaSgpAgTable, NULL, "HELP STRING");
	snmpd_register_config_handler("m3uaSgpSgTable", parse_m3uaSgpSgTable, NULL, "HELP STRING");
	snmpd_register_config_handler("m3uaSgpAspTable", parse_m3uaSgpAspTable, NULL, "HELP STRING");
	snmpd_register_config_handler("m3uaSgpTable", parse_m3uaSgpTable, NULL, "HELP STRING");

	/* we need to be called back later to store our data */
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_m3uaSgpMIB, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_m3uaSgpAgTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_m3uaSgpSgTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_m3uaSgpAspTable, NULL);
	snmp_register_callback(SNMP_CALLBACK_LIBRARY, SNMP_CALLBACK_STORE_DATA, store_m3uaSgpTable, NULL);

	/* place any other initialization junk you need here */
	if (my_readfd >= 0) {
		register_readfd(my_readfd, m3uaSgpMIB_fd_handler, (void *) 0);
		register_exceptfd(my_readfd, m3uaSgpMIB_fd_handler, (void *) 1);
	}
	m3uaSgpMIBold_signal_handler = external_signal_handler[SIGCHLD];
	external_signal_handler[SIGCHLD] = &m3uaSgpMIB_loop_handler;
	external_signal_scheduled[SIGCHLD] = 1;
	DEBUGMSGTL(("m3uaSgpMIB", "done.\n"));
}

/**
 * @fn void deinit_m3uaSgpMIB(void)
 * @brief deinitialization routine.
 *
 * This is called before the agent is unloaded.  At a minimum, deregistration of the MIB variables
 * structure (m3uaSgpMIB_variables) should take place here.  By default, the function also
 * deregisters the the configuration file handlers for the MIB variables and table rows.
 *
 * Additional deregistrations that may be required here are calls to unregister_readfd(),
 * unregister_writefd() and unregsiter_exceptfd() for unhooking from the snmpd event loop, but only
 * when used as a loadable module.  By default if a read file descriptor exists, it is unregistered.
 */
void
deinit_m3uaSgpMIB(void)
{
	DEBUGMSGTL(("m3uaSgpMIB", "deinit_m3uaSgpMIB: deinitializating...  "));
	external_signal_handler[SIGCHLD] = m3uaSgpMIBold_signal_handler;
	if (my_readfd >= 0) {
		unregister_exceptfd(my_readfd);
		unregister_readfd(my_readfd);
		close(my_readfd);
		my_readfd = -1;
	}
	unregister_mib(m3uaSgpMIB_variables_oid, sizeof(m3uaSgpMIB_variables_oid) / sizeof(oid));
	snmpd_unregister_config_handler("m3uaSgpMIB");
	snmpd_unregister_config_handler("m3uaSgpAgTable");
	snmpd_unregister_config_handler("m3uaSgpSgTable");
	snmpd_unregister_config_handler("m3uaSgpAspTable");
	snmpd_unregister_config_handler("m3uaSgpTable");

	/* place any other de-initialization junk you need here */
	DEBUGMSGTL(("m3uaSgpMIB", "done.\n"));
}

int
term_m3uaSgpMIB(int majorID, int minorID, void *serverarg, void *clientarg)
{
	DEBUGMSGTL(("m3uaSgpMIB", "term_m3uaSgpMIB: terminating...  "));
	deinit_m3uaSgpMIB();
	DEBUGMSGTL(("m3uaSgpMIB", "done.\n"));
	return 0;
}

/**
 * @fn struct m3uaSgpMIB_data *m3uaSgpMIB_create(void)
 * @brief create a fresh data structure representing scalars in m3uaSgpMIB.
 *
 * Creates a new m3uaSgpMIB_data structure by allocating dynamic memory for the structure and
 * initializing the default values of scalars in m3uaSgpMIB.
 */
struct m3uaSgpMIB_data *
m3uaSgpMIB_create(void)
{
	struct m3uaSgpMIB_data *StorageNew = SNMP_MALLOC_STRUCT(m3uaSgpMIB_data);

	DEBUGMSGTL(("m3uaSgpMIB", "m3uaSgpMIB_create: creating scalars...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default scalar values here into StorageNew */
		StorageNew->m3uaSgpAgNextIndex = 0;
		StorageNew->m3uaSgpNextIndex = 0;

	}
	DEBUGMSGTL(("m3uaSgpMIB", "done.\n"));
	return (StorageNew);
}

/**
 * @fn int m3uaSgpMIB_destroy(struct m3uaSgpMIB_data **thedata)
 * @param thedata pointer to the data structure in m3uaSgpMIB.
 * @brief delete a scalars structure from m3uaSgpMIB.
 *
 * Frees scalars that were previously removed from m3uaSgpMIB.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
m3uaSgpMIB_destroy(struct m3uaSgpMIB_data **thedata)
{
	struct m3uaSgpMIB_data *StorageDel;

	DEBUGMSGTL(("m3uaSgpMIB", "m3uaSgpMIB_destroy: deleting scalars...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("m3uaSgpMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int m3uaSgpMIB_add(struct m3uaSgpMIB_data *thedata)
 * @param thedata the structure representing m3uaSgpMIB scalars.
 * @brief adds node to the m3uaSgpMIB scalar data set.
 *
 * Adds a scalar structure to the m3uaSgpMIB data set.  Note that this function is necessary even
 * when the scalar values are not peristent.
 */
int
m3uaSgpMIB_add(struct m3uaSgpMIB_data *thedata)
{
	DEBUGMSGTL(("m3uaSgpMIB", "m3uaSgpMIB_add: adding data...  "));
	if (thedata)
		m3uaSgpMIBStorage = thedata;
	DEBUGMSGTL(("m3uaSgpMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_m3uaSgpMIB(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for m3uaSgpMIB entries.
 *
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case m3uaSgpMIB).  This routine is invoked by
 * UCD-SNMP to read the values of scalars in the MIB from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the MIB.  If there are no configured entries
 * in the configuration MIB, this function will simply not be called.
 */
void
parse_m3uaSgpMIB(const char *token, char *line)
{
	size_t tmpsize;
	struct m3uaSgpMIB_data *StorageTmp = m3uaSgpMIB_create();

	DEBUGMSGTL(("m3uaSgpMIB", "parse_m3uaSgpMIB: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual scalars that are not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->m3uaSgpAgNextIndex, &tmpsize);
	line = read_config_read_data(, line, &StorageTmp->m3uaSgpSctpProfileNextIndex, &);
	line = read_config_read_data(, line, &StorageTmp->m3uaSgpSctpProfileTable, &);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->m3uaSgpNextIndex, &tmpsize);
	m3uaSgpMIB_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("m3uaSgpMIB", "done.\n"));
}

/*
 * store_m3uaSgpMIB(): stores .conf file entries needed to configure the mib.
 */
int
store_m3uaSgpMIB(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct m3uaSgpMIB_data *StorageTmp;

	DEBUGMSGTL(("m3uaSgpMIB", "store_m3uaSgpMIB: storing data...  "));
	refresh_m3uaSgpMIB(1);
	if ((StorageTmp = m3uaSgpMIBStorage) == NULL) {
		DEBUGMSGTL(("m3uaSgpMIB", "error.\n"));
		return SNMPERR_GENERR;
	}
	(void) tmpsize;
	/* XXX: comment entire section if no scalars are persistent */
	{
		memset(line, 0, sizeof(line));
		strcat(line, "m3uaSgpMIB ");
		cptr = line + strlen(line);
		/* XXX: remove individual scalars that are not persistent */
		cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->m3uaSgpAgNextIndex, &tmpsize);
		cptr = read_config_store_data(, cptr, &StorageTmp->m3uaSgpSctpProfileNextIndex, &);
		cptr = read_config_store_data(, cptr, &StorageTmp->m3uaSgpSctpProfileTable, &);
		cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->m3uaSgpNextIndex, &tmpsize);
		snmpd_store_config(line);
	}
	DEBUGMSGTL(("m3uaSgpMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void refresh_m3uaSgpMIB(int force)
 * @param force forced refresh when non-zero.
 * @brief refresh the scalar values of m3uaSgpMIB.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a scalar has been requested).
 */
void
refresh_m3uaSgpMIB(int force)
{
	if (m3uaSgpMIBStorage == NULL) {
		struct m3uaSgpMIB_data *StorageNew;

		if ((StorageNew = m3uaSgpMIB_create()) == NULL)
			return;
		m3uaSgpMIBStorage = StorageNew;
		m3uaSgpMIB_refresh = 1;
	}
	if (!force && m3uaSgpMIB_refresh == 0)
		return;
	DEBUGMSGTL(("m3uaSgpMIB", "refresh_m3uaSgpMIB: refreshing...  "));
	/* XXX: Update scalars as required here... */
	m3uaSgpMIB_refresh = 0;
	DEBUGMSGTL(("m3uaSgpMIB", "done.\n"));
}

/**
 * @fn u_char * var_m3uaSgpMIB(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @param vp a pointer to the entry in the variables table for the requested variable.
 * @param name the object identifier for which to find.
 * @param length the length of the object identifier.
 * @param exact whether the name is exact.
 * @param var_len a pointer to the length of the representation of the object.
 * @param write_method a pointer to a write method for the object.
 * @brief locate variables in m3uaSgpMIB.
 *
 * This function returns a pointer to a memory area that is static across the request that contains
 * the UCD-SNMP representation of the scalar (so that it may be used to read from for a GET,
 * GET-NEXT or GET-BULK request).  This returned pointer may be NULL, in which case the function is
 * telling UCD-SNMP that the scalar does not exist for reading; however, if write_method is
 * overwritten with a non-NULL value, the function is telling UCD-SNMP that the scalar exists for
 * writing.  Write-only objects can be effected in this way.
 */
u_char *
var_m3uaSgpMIB(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct m3uaSgpMIB_data *StorageTmp;
	u_char *rval;

	DEBUGMSGTL(("m3uaSgpMIB", "var_m3uaSgpMIB: lookup up varbind...  "));
	if (header_generic(vp, name, length, exact, var_len, write_method) == MATCH_FAILED)
		return NULL;
	/* Refresh the MIB values if required. */
	refresh_m3uaSgpMIB(0);
	if ((StorageTmp = m3uaSgpMIBStorage) == NULL) {
		DEBUGMSGTL(("m3uaSgpMIB", "no datastructure.\n"));
		return NULL;
	}
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) M3UASGPAGNEXTINDEX:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m3uaSgpAgNextIndex);
		rval = (u_char *) &StorageTmp->m3uaSgpAgNextIndex;
		break;
	case (u_char) M3UASGPSCTPPROFILENEXTINDEX:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->m3uaSgpSctpProfileNextIndex;
		rval = (u_char *) StorageTmp->m3uaSgpSctpProfileNextIndex;
		break;
	case (u_char) M3UASGPSCTPPROFILETABLE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->m3uaSgpSctpProfileTable;
		rval = (u_char *) StorageTmp->m3uaSgpSctpProfileTable;
		break;
	case (u_char) M3UASGPNEXTINDEX:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m3uaSgpNextIndex);
		rval = (u_char *) &StorageTmp->m3uaSgpNextIndex;
		break;
	default:
		ERROR_MSG("");
	}
	if (rval)
		DEBUGMSGTL(("m3uaSgpMIB", "found.\n"));
	else
		DEBUGMSGTL(("m3uaSgpMIB", "not found.\n"));
	return (rval);
}

/**
 * @fn struct m3uaSgpAgTable_data *m3uaSgpAgTable_create(void)
 * @brief create a fresh data structure representing a new row in the m3uaSgpAgTable table.
 *
 * Creates a new m3uaSgpAgTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct m3uaSgpAgTable_data *
m3uaSgpAgTable_create(void)
{
	struct m3uaSgpAgTable_data *StorageNew = SNMP_MALLOC_STRUCT(m3uaSgpAgTable_data);

	DEBUGMSGTL(("m3uaSgpMIB", "m3uaSgpAgTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->m3uaSgpAgName = (uint8_t *) strdup("")) != NULL)
			StorageNew->m3uaSgpAgNameLen = strlen("");
		{
			static oid tmpoid[11] = { 1, 3, 6, 1, 4, 1, 29591, 1, 10, 2, 1 };
			if ((StorageNew->m3uaSgpAgProtocolVersion = snmp_duplicate_objid(tmpoid, 11)))
				StorageNew->m3uaSgpAgProtocolVersionLen = 11;
		}
		if (memdup((u_char **) &StorageNew->m3uaSgpAgOptions, (u_char *) "\x00\x00", 2) == SNMPERR_SUCCESS)
			StorageNew->m3uaSgpAgOptionsLen = 2;
		StorageNew->m3uaSgpAgRegistrationPolicy = 1;
		StorageNew->m3uaSgpAgAspIdPolicy = 1;
		StorageNew->m3uaSgpAgProtocolPayloadId = 3;
		StorageNew->m3uaSgpAgIpPort = 2905;
		StorageNew->m3uaSgpAgMinOstreams = 33;
		StorageNew->m3uaSgpAgMaxIstreams = 33;
		StorageNew->m3uaSgpAgStatus = 0;
		StorageNew->m3uaSgpAgStatus = RS_NOTREADY;
	}
	DEBUGMSGTL(("m3uaSgpMIB", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct m3uaSgpAgTable_data *m3uaSgpAgTable_duplicate(struct m3uaSgpAgTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicat a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct m3uaSgpAgTable_data *
m3uaSgpAgTable_duplicate(struct m3uaSgpAgTable_data *thedata)
{
	struct m3uaSgpAgTable_data *StorageNew = SNMP_MALLOC_STRUCT(m3uaSgpAgTable_data);

	DEBUGMSGTL(("m3uaSgpMIB", "m3uaSgpAgTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("m3uaSgpMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	m3uaSgpAgTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int m3uaSgpAgTable_destroy(struct m3uaSgpAgTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
m3uaSgpAgTable_destroy(struct m3uaSgpAgTable_data **thedata)
{
	struct m3uaSgpAgTable_data *StorageDel;

	DEBUGMSGTL(("m3uaSgpMIB", "m3uaSgpAgTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->m3uaSgpAgName);
		StorageDel->m3uaSgpAgNameLen = 0;
		SNMP_FREE(StorageDel->m3uaSgpAgProtocolVersion);
		StorageDel->m3uaSgpAgProtocolVersionLen = 0;
		SNMP_FREE(StorageDel->m3uaSgpAgOptions);
		StorageDel->m3uaSgpAgOptionsLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("m3uaSgpMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int m3uaSgpAgTable_add(struct m3uaSgpAgTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the m3uaSgpAgTable table data set.
 *
 * Adds a table row structure to the m3uaSgpAgTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
m3uaSgpAgTable_add(struct m3uaSgpAgTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("m3uaSgpMIB", "m3uaSgpAgTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* m3uaSgpAgIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->m3uaSgpAgIndex, sizeof(thedata->m3uaSgpAgIndex));
		header_complex_add_data(&m3uaSgpAgTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("m3uaSgpMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int m3uaSgpAgTable_del(struct m3uaSgpAgTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the m3uaSgpAgTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
m3uaSgpAgTable_del(struct m3uaSgpAgTable_data *thedata)
{
	struct m3uaSgpAgTable_data *StorageDel;

	DEBUGMSGTL(("m3uaSgpMIB", "m3uaSgpAgTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(m3uaSgpAgTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&m3uaSgpAgTableStorage, hciptr);
	}
	DEBUGMSGTL(("m3uaSgpMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_m3uaSgpAgTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for m3uaSgpAgTable entries.
 *
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case m3uaSgpAgTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_m3uaSgpAgTable(const char *token, char *line)
{
	size_t tmpsize;
	struct m3uaSgpAgTable_data *StorageTmp = m3uaSgpAgTable_create();

	DEBUGMSGTL(("m3uaSgpMIB", "parse_m3uaSgpAgTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->m3uaSgpAgIndex, &tmpsize);
	SNMP_FREE(StorageTmp->m3uaSgpAgName);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->m3uaSgpAgName, &StorageTmp->m3uaSgpAgNameLen);
	if (StorageTmp->m3uaSgpAgName == NULL) {
		config_perror("invalid specification for m3uaSgpAgName");
		return;
	}
	SNMP_FREE(StorageTmp->m3uaSgpAgProtocolVersion);
	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->m3uaSgpAgProtocolVersion, &StorageTmp->m3uaSgpAgProtocolVersionLen);
	if (StorageTmp->m3uaSgpAgProtocolVersion == NULL) {
		config_perror("invalid specification for m3uaSgpAgProtocolVersion");
		return;
	}
	SNMP_FREE(StorageTmp->m3uaSgpAgOptions);
	line = read_config_read_data(ASN_BIT_STR, line, &StorageTmp->m3uaSgpAgOptions, &StorageTmp->m3uaSgpAgOptionsLen);
	if (StorageTmp->m3uaSgpAgOptions == NULL) {
		config_perror("invalid specification for m3uaSgpAgOptions");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m3uaSgpAgRegistrationPolicy, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m3uaSgpAgAspIdPolicy, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m3uaSgpAgProtocolPayloadId, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m3uaSgpAgIpPort, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m3uaSgpAgMinOstreams, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m3uaSgpAgMaxIstreams, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m3uaSgpAgStatus, &tmpsize);
	m3uaSgpAgTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("m3uaSgpMIB", "done.\n"));
}

/*
 * store_m3uaSgpAgTable(): store configuraiton file for m3uaSgpAgTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_m3uaSgpAgTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct m3uaSgpAgTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("m3uaSgpMIB", "store_m3uaSgpAgTable: storing data...  "));
	refresh_m3uaSgpAgTable(1);
	(void) tmpsize;
	for (hcindex = m3uaSgpAgTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct m3uaSgpAgTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "m3uaSgpAgTable ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->m3uaSgpAgIndex, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->m3uaSgpAgName, &StorageTmp->m3uaSgpAgNameLen);
			cptr = read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->m3uaSgpAgProtocolVersion, &StorageTmp->m3uaSgpAgProtocolVersionLen);
			cptr = read_config_store_data(ASN_BIT_STR, cptr, &StorageTmp->m3uaSgpAgOptions, &StorageTmp->m3uaSgpAgOptionsLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m3uaSgpAgRegistrationPolicy, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m3uaSgpAgAspIdPolicy, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m3uaSgpAgProtocolPayloadId, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m3uaSgpAgIpPort, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m3uaSgpAgMinOstreams, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m3uaSgpAgMaxIstreams, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m3uaSgpAgStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("m3uaSgpMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct m3uaSgpSgTable_data *m3uaSgpSgTable_create(void)
 * @brief create a fresh data structure representing a new row in the m3uaSgpSgTable table.
 *
 * Creates a new m3uaSgpSgTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct m3uaSgpSgTable_data *
m3uaSgpSgTable_create(void)
{
	struct m3uaSgpSgTable_data *StorageNew = SNMP_MALLOC_STRUCT(m3uaSgpSgTable_data);

	DEBUGMSGTL(("m3uaSgpMIB", "m3uaSgpSgTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->m3uaSgpAgIndex = 0;
		StorageNew->m3uaSgpSgIndex = 0;
		if ((StorageNew->m3uaSgpSgName = (uint8_t *) strdup("")) != NULL)
			StorageNew->m3uaSgpSgNameLen = strlen("");
		StorageNew->m3uaSgpSgStatus = 0;
		StorageNew->m3uaSgpSgStatus = RS_NOTREADY;
	}
	DEBUGMSGTL(("m3uaSgpMIB", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct m3uaSgpSgTable_data *m3uaSgpSgTable_duplicate(struct m3uaSgpSgTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicat a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct m3uaSgpSgTable_data *
m3uaSgpSgTable_duplicate(struct m3uaSgpSgTable_data *thedata)
{
	struct m3uaSgpSgTable_data *StorageNew = SNMP_MALLOC_STRUCT(m3uaSgpSgTable_data);

	DEBUGMSGTL(("m3uaSgpMIB", "m3uaSgpSgTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("m3uaSgpMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	m3uaSgpSgTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int m3uaSgpSgTable_destroy(struct m3uaSgpSgTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
m3uaSgpSgTable_destroy(struct m3uaSgpSgTable_data **thedata)
{
	struct m3uaSgpSgTable_data *StorageDel;

	DEBUGMSGTL(("m3uaSgpMIB", "m3uaSgpSgTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->m3uaSgpSgName);
		StorageDel->m3uaSgpSgNameLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("m3uaSgpMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int m3uaSgpSgTable_add(struct m3uaSgpSgTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the m3uaSgpSgTable table data set.
 *
 * Adds a table row structure to the m3uaSgpSgTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
m3uaSgpSgTable_add(struct m3uaSgpSgTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("m3uaSgpMIB", "m3uaSgpSgTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* m3uaSgpAgIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->m3uaSgpAgIndex, sizeof(thedata->m3uaSgpAgIndex));
		header_complex_add_data(&m3uaSgpSgTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("m3uaSgpMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int m3uaSgpSgTable_del(struct m3uaSgpSgTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the m3uaSgpSgTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
m3uaSgpSgTable_del(struct m3uaSgpSgTable_data *thedata)
{
	struct m3uaSgpSgTable_data *StorageDel;

	DEBUGMSGTL(("m3uaSgpMIB", "m3uaSgpSgTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(m3uaSgpSgTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&m3uaSgpSgTableStorage, hciptr);
	}
	DEBUGMSGTL(("m3uaSgpMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_m3uaSgpSgTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for m3uaSgpSgTable entries.
 *
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case m3uaSgpSgTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_m3uaSgpSgTable(const char *token, char *line)
{
	size_t tmpsize;
	struct m3uaSgpSgTable_data *StorageTmp = m3uaSgpSgTable_create();

	DEBUGMSGTL(("m3uaSgpMIB", "parse_m3uaSgpSgTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->m3uaSgpAgIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->m3uaSgpSgIndex, &tmpsize);
	SNMP_FREE(StorageTmp->m3uaSgpSgName);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->m3uaSgpSgName, &StorageTmp->m3uaSgpSgNameLen);
	if (StorageTmp->m3uaSgpSgName == NULL) {
		config_perror("invalid specification for m3uaSgpSgName");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m3uaSgpSgStatus, &tmpsize);
	m3uaSgpSgTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("m3uaSgpMIB", "done.\n"));
}

/*
 * store_m3uaSgpSgTable(): store configuraiton file for m3uaSgpSgTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_m3uaSgpSgTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct m3uaSgpSgTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("m3uaSgpMIB", "store_m3uaSgpSgTable: storing data...  "));
	refresh_m3uaSgpSgTable(1);
	(void) tmpsize;
	for (hcindex = m3uaSgpSgTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct m3uaSgpSgTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "m3uaSgpSgTable ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->m3uaSgpAgIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->m3uaSgpSgIndex, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->m3uaSgpSgName, &StorageTmp->m3uaSgpSgNameLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m3uaSgpSgStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("m3uaSgpMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct m3uaSgpAspTable_data *m3uaSgpAspTable_create(void)
 * @brief create a fresh data structure representing a new row in the m3uaSgpAspTable table.
 *
 * Creates a new m3uaSgpAspTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct m3uaSgpAspTable_data *
m3uaSgpAspTable_create(void)
{
	struct m3uaSgpAspTable_data *StorageNew = SNMP_MALLOC_STRUCT(m3uaSgpAspTable_data);

	DEBUGMSGTL(("m3uaSgpMIB", "m3uaSgpAspTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		if ((StorageNew->m3uaSgpAspName = (uint8_t *) strdup("")) != NULL)
			StorageNew->m3uaSgpAspNameLen = strlen("");
		StorageNew->m3uaSgpAspOperationalState = 0;
		StorageNew->m3uaSgpAspUsageState = 0;
		StorageNew->m3uaSgpAspAdministrativeState = 0;
		if (memdup((u_char **) &StorageNew->m3uaSgpAspAlarmStatus, (u_char *) "\x00", 1) == SNMPERR_SUCCESS)
			StorageNew->m3uaSgpAspAlarmStatusLen = 1;
		if (memdup((u_char **) &StorageNew->m3uaSgpAspProceduralStatus, (u_char *) "\x00", 1) == SNMPERR_SUCCESS)
			StorageNew->m3uaSgpAspProceduralStatusLen = 1;
		if (memdup((u_char **) &StorageNew->m3uaSgpAspAvailabilityStatus, (u_char *) "\x00\x00", 2) == SNMPERR_SUCCESS)
			StorageNew->m3uaSgpAspAvailabilityStatusLen = 2;
		StorageNew->m3uaSgpAspAspState = 0;
		if ((StorageNew->m3uaSgpAspSctpProfile = snmp_duplicate_objid(zeroDotZero_oid, 2)))
			StorageNew->m3uaSgpAspSctpProfileLen = 2;
		StorageNew->m3uaSgpAspMaxInitRetries = 0;
		StorageNew->m3uaSgpAspMaxPathRetrans = 0;
		StorageNew->m3uaSgpAspRtoMin = 0;
		StorageNew->m3uaSgpAspRtoMax = 0;
		StorageNew->m3uaSgpAspHeartbeatInterval = 0;
		StorageNew->m3uaSgpAspMaxLifeTime = 0;
		StorageNew->m3uaSgpAspTimerDivert = 0;
		StorageNew->m3uaSgpAspStatus = 0;
		StorageNew->m3uaSgpAspStatus = RS_NOTREADY;
	}
	DEBUGMSGTL(("m3uaSgpMIB", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct m3uaSgpAspTable_data *m3uaSgpAspTable_duplicate(struct m3uaSgpAspTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicat a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct m3uaSgpAspTable_data *
m3uaSgpAspTable_duplicate(struct m3uaSgpAspTable_data *thedata)
{
	struct m3uaSgpAspTable_data *StorageNew = SNMP_MALLOC_STRUCT(m3uaSgpAspTable_data);

	DEBUGMSGTL(("m3uaSgpMIB", "m3uaSgpAspTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("m3uaSgpMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	m3uaSgpAspTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int m3uaSgpAspTable_destroy(struct m3uaSgpAspTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
m3uaSgpAspTable_destroy(struct m3uaSgpAspTable_data **thedata)
{
	struct m3uaSgpAspTable_data *StorageDel;

	DEBUGMSGTL(("m3uaSgpMIB", "m3uaSgpAspTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel->m3uaSgpAspName);
		StorageDel->m3uaSgpAspNameLen = 0;
		SNMP_FREE(StorageDel->m3uaSgpAspAlarmStatus);
		StorageDel->m3uaSgpAspAlarmStatusLen = 0;
		SNMP_FREE(StorageDel->m3uaSgpAspProceduralStatus);
		StorageDel->m3uaSgpAspProceduralStatusLen = 0;
		SNMP_FREE(StorageDel->m3uaSgpAspAvailabilityStatus);
		StorageDel->m3uaSgpAspAvailabilityStatusLen = 0;
		SNMP_FREE(StorageDel->m3uaSgpAspSctpProfile);
		StorageDel->m3uaSgpAspSctpProfileLen = 0;
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("m3uaSgpMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int m3uaSgpAspTable_add(struct m3uaSgpAspTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the m3uaSgpAspTable table data set.
 *
 * Adds a table row structure to the m3uaSgpAspTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
m3uaSgpAspTable_add(struct m3uaSgpAspTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("m3uaSgpMIB", "m3uaSgpAspTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* m3uaSgpAspIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->m3uaSgpAspIndex, sizeof(thedata->m3uaSgpAspIndex));
		header_complex_add_data(&m3uaSgpAspTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("m3uaSgpMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int m3uaSgpAspTable_del(struct m3uaSgpAspTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the m3uaSgpAspTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
m3uaSgpAspTable_del(struct m3uaSgpAspTable_data *thedata)
{
	struct m3uaSgpAspTable_data *StorageDel;

	DEBUGMSGTL(("m3uaSgpMIB", "m3uaSgpAspTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(m3uaSgpAspTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&m3uaSgpAspTableStorage, hciptr);
	}
	DEBUGMSGTL(("m3uaSgpMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_m3uaSgpAspTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for m3uaSgpAspTable entries.
 *
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case m3uaSgpAspTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_m3uaSgpAspTable(const char *token, char *line)
{
	size_t tmpsize;
	struct m3uaSgpAspTable_data *StorageTmp = m3uaSgpAspTable_create();

	DEBUGMSGTL(("m3uaSgpMIB", "parse_m3uaSgpAspTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->m3uaSgpAspIndex, &tmpsize);
	SNMP_FREE(StorageTmp->m3uaSgpAspName);
	line = read_config_read_data(ASN_OCTET_STR, line, &StorageTmp->m3uaSgpAspName, &StorageTmp->m3uaSgpAspNameLen);
	if (StorageTmp->m3uaSgpAspName == NULL) {
		config_perror("invalid specification for m3uaSgpAspName");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m3uaSgpAspOperationalState, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m3uaSgpAspUsageState, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m3uaSgpAspAdministrativeState, &tmpsize);
	SNMP_FREE(StorageTmp->m3uaSgpAspAlarmStatus);
	line = read_config_read_data(ASN_BIT_STR, line, &StorageTmp->m3uaSgpAspAlarmStatus, &StorageTmp->m3uaSgpAspAlarmStatusLen);
	if (StorageTmp->m3uaSgpAspAlarmStatus == NULL) {
		config_perror("invalid specification for m3uaSgpAspAlarmStatus");
		return;
	}
	SNMP_FREE(StorageTmp->m3uaSgpAspProceduralStatus);
	line = read_config_read_data(ASN_BIT_STR, line, &StorageTmp->m3uaSgpAspProceduralStatus, &StorageTmp->m3uaSgpAspProceduralStatusLen);
	if (StorageTmp->m3uaSgpAspProceduralStatus == NULL) {
		config_perror("invalid specification for m3uaSgpAspProceduralStatus");
		return;
	}
	SNMP_FREE(StorageTmp->m3uaSgpAspAvailabilityStatus);
	line = read_config_read_data(ASN_BIT_STR, line, &StorageTmp->m3uaSgpAspAvailabilityStatus, &StorageTmp->m3uaSgpAspAvailabilityStatusLen);
	if (StorageTmp->m3uaSgpAspAvailabilityStatus == NULL) {
		config_perror("invalid specification for m3uaSgpAspAvailabilityStatus");
		return;
	}
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m3uaSgpAspAspState, &tmpsize);
	SNMP_FREE(StorageTmp->m3uaSgpAspSctpProfile);
	line = read_config_read_data(ASN_OBJECT_ID, line, &StorageTmp->m3uaSgpAspSctpProfile, &StorageTmp->m3uaSgpAspSctpProfileLen);
	if (StorageTmp->m3uaSgpAspSctpProfile == NULL) {
		config_perror("invalid specification for m3uaSgpAspSctpProfile");
		return;
	}
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->m3uaSgpAspMaxInitRetries, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->m3uaSgpAspMaxPathRetrans, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m3uaSgpAspRtoMin, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m3uaSgpAspRtoMax, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m3uaSgpAspHeartbeatInterval, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m3uaSgpAspMaxLifeTime, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m3uaSgpAspTimerDivert, &tmpsize);
	line = read_config_read_data(ASN_INTEGER, line, &StorageTmp->m3uaSgpAspStatus, &tmpsize);
	m3uaSgpAspTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("m3uaSgpMIB", "done.\n"));
}

/*
 * store_m3uaSgpAspTable(): store configuraiton file for m3uaSgpAspTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_m3uaSgpAspTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct m3uaSgpAspTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("m3uaSgpMIB", "store_m3uaSgpAspTable: storing data...  "));
	refresh_m3uaSgpAspTable(1);
	(void) tmpsize;
	for (hcindex = m3uaSgpAspTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct m3uaSgpAspTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "m3uaSgpAspTable ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->m3uaSgpAspIndex, &tmpsize);
			cptr = read_config_store_data(ASN_OCTET_STR, cptr, &StorageTmp->m3uaSgpAspName, &StorageTmp->m3uaSgpAspNameLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m3uaSgpAspOperationalState, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m3uaSgpAspUsageState, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m3uaSgpAspAdministrativeState, &tmpsize);
			cptr = read_config_store_data(ASN_BIT_STR, cptr, &StorageTmp->m3uaSgpAspAlarmStatus, &StorageTmp->m3uaSgpAspAlarmStatusLen);
			cptr = read_config_store_data(ASN_BIT_STR, cptr, &StorageTmp->m3uaSgpAspProceduralStatus, &StorageTmp->m3uaSgpAspProceduralStatusLen);
			cptr = read_config_store_data(ASN_BIT_STR, cptr, &StorageTmp->m3uaSgpAspAvailabilityStatus, &StorageTmp->m3uaSgpAspAvailabilityStatusLen);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m3uaSgpAspAspState, &tmpsize);
			cptr = read_config_store_data(ASN_OBJECT_ID, cptr, &StorageTmp->m3uaSgpAspSctpProfile, &StorageTmp->m3uaSgpAspSctpProfileLen);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->m3uaSgpAspMaxInitRetries, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->m3uaSgpAspMaxPathRetrans, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m3uaSgpAspRtoMin, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m3uaSgpAspRtoMax, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m3uaSgpAspHeartbeatInterval, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m3uaSgpAspMaxLifeTime, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m3uaSgpAspTimerDivert, &tmpsize);
			cptr = read_config_store_data(ASN_INTEGER, cptr, &StorageTmp->m3uaSgpAspStatus, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("m3uaSgpMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn struct m3uaSgpTable_data *m3uaSgpTable_create(void)
 * @brief create a fresh data structure representing a new row in the m3uaSgpTable table.
 *
 * Creates a new m3uaSgpTable_data structure by allocating dynamic memory for the structure and
 * initializing the default values of columns in the table.  The row status object, if any, should
 * be set to RS_NOTREADY.
 */
struct m3uaSgpTable_data *
m3uaSgpTable_create(void)
{
	struct m3uaSgpTable_data *StorageNew = SNMP_MALLOC_STRUCT(m3uaSgpTable_data);

	DEBUGMSGTL(("m3uaSgpMIB", "m3uaSgpTable_create: creating row...  "));
	if (StorageNew != NULL) {
		/* XXX: fill in default row values here into StorageNew */
		StorageNew->m3uaSgpAgIndex = 0;
		StorageNew->m3uaSgpSgIndex = 0;

	}
	DEBUGMSGTL(("m3uaSgpMIB", "done.\n"));
	return (StorageNew);
}

/**
 * @fn struct m3uaSgpTable_data *m3uaSgpTable_duplicate(struct m3uaSgpTable_data *thedata)
 * @param thedata the row structure to duplicate.
 * @brief duplicat a row structure for a table.
 *
 * Duplicates the specified row structure @param thedata and returns a pointer to the newly
 * allocated row structure on success, or NULL on failure.
 */
struct m3uaSgpTable_data *
m3uaSgpTable_duplicate(struct m3uaSgpTable_data *thedata)
{
	struct m3uaSgpTable_data *StorageNew = SNMP_MALLOC_STRUCT(m3uaSgpTable_data);

	DEBUGMSGTL(("m3uaSgpMIB", "m3uaSgpTable_duplicate: duplicating row...  "));
	if (StorageNew != NULL) {
	}
      done:
	DEBUGMSGTL(("m3uaSgpMIB", "done.\n"));
	return (StorageNew);
	goto destroy;
      destroy:
	m3uaSgpTable_destroy(&StorageNew);
	goto done;
}

/**
 * @fn int m3uaSgpTable_destroy(struct m3uaSgpTable_data **thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Frees a table row that was previously removed from a table.  Note that the strings associated
 * with octet strings, object identifiers and bit strings still attached to the structure will also
 * be freed.  The pointer that was passed in @param thedata will be set to NULL if it is not already
 * NULL.
 */
int
m3uaSgpTable_destroy(struct m3uaSgpTable_data **thedata)
{
	struct m3uaSgpTable_data *StorageDel;

	DEBUGMSGTL(("m3uaSgpMIB", "m3uaSgpTable_destroy: deleting row...  "));
	if ((StorageDel = *thedata) != NULL) {
		SNMP_FREE(StorageDel);
		*thedata = StorageDel;
	}
	DEBUGMSGTL(("m3uaSgpMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int m3uaSgpTable_add(struct m3uaSgpTable_data *thedata)
 * @param thedata the structure representing the new row in the table.
 * @brief adds a row to the m3uaSgpTable table data set.
 *
 * Adds a table row structure to the m3uaSgpTable table.  Note that this function is necessary even
 * when the table rows are not peristent.  This function can be used within this MIB or other MIBs
 * by the agent to create rows within the table autonomously.
 */
int
m3uaSgpTable_add(struct m3uaSgpTable_data *thedata)
{
	struct variable_list *vars = NULL;

	DEBUGMSGTL(("m3uaSgpMIB", "m3uaSgpTable_add: adding data...  "));
	if (thedata) {
		/* add the index variables to the varbind list, which is used by header_complex to index the data */
		/* m3uaSgpAgIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->m3uaSgpAgIndex, sizeof(thedata->m3uaSgpAgIndex));
		/* m3uaSgpSgIndex */
		snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, (u_char *) &thedata->m3uaSgpSgIndex, sizeof(thedata->m3uaSgpSgIndex));
		header_complex_add_data(&m3uaSgpTableStorage, vars, thedata);
	}
	DEBUGMSGTL(("m3uaSgpMIB", "registered an entry.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn int m3uaSgpTable_del(struct m3uaSgpTable_data *thedata)
 * @param thedata pointer to the extracted or existing data structure in the table.
 * @brief delete a row structure from a table.
 *
 * Deletes a table row structure from the m3uaSgpTable table but does not free it.  Note that this
 * function is necessary even when the table rows are not persistent.  This function can be used
 * within this MIB or another MIB by the agent to delete rows from the table autonomously.  The data
 * structure may either be already extracted from the data set, or the structure may still exist in
 * the data set.  This function will extract the row from the table if it has not already been
 * performed by the caller.
 */
int
m3uaSgpTable_del(struct m3uaSgpTable_data *thedata)
{
	struct m3uaSgpTable_data *StorageDel;

	DEBUGMSGTL(("m3uaSgpMIB", "m3uaSgpTable_data: deleting data...  "));
	if ((StorageDel = thedata) != NULL) {
		struct header_complex_index *hciptr;

		if ((hciptr = header_complex_find_entry(m3uaSgpTableStorage, StorageDel)) != NULL)
			header_complex_extract_entry(&m3uaSgpTableStorage, hciptr);
	}
	DEBUGMSGTL(("m3uaSgpMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void parse_m3uaSgpTable(const char *token, char *line)
 * @param token token used within the configuration file.
 * @param line line from configuration file matching the token.
 * @brief parse configuration file for m3uaSgpTable entries.
 *
 * This callback is called by UCD-SNMP when it prases a configuration file and finds a configuration
 * file line for the registsred token (in this case m3uaSgpTable).  This routine is invoked by UCD-SNMP
 * to read the values of each row in the table from the configuration file.  Note that this
 * procedure may exist regardless of the persistence of the table.  If there are no configured
 * entries in the configuration table, this function will simply not be called.
 */
void
parse_m3uaSgpTable(const char *token, char *line)
{
	size_t tmpsize;
	struct m3uaSgpTable_data *StorageTmp = m3uaSgpTable_create();

	DEBUGMSGTL(("m3uaSgpMIB", "parse_m3uaSgpTable: parsing config...  "));
	if (StorageTmp == NULL) {
		config_perror("malloc failure");
		return;
	}
	/* XXX: remove individual columns if not persistent */
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->m3uaSgpAgIndex, &tmpsize);
	line = read_config_read_data(ASN_UNSIGNED, line, &StorageTmp->m3uaSgpSgIndex, &tmpsize);
	m3uaSgpTable_add(StorageTmp);
	(void) tmpsize;
	DEBUGMSGTL(("m3uaSgpMIB", "done.\n"));
}

/*
 * store_m3uaSgpTable(): store configuraiton file for m3uaSgpTable
 * stores .conf file entries needed to configure the mib.
 */
int
store_m3uaSgpTable(int majorID, int minorID, void *serverarg, void *clientarg)
{
	char line[SNMP_MAXBUF];
	char *cptr;
	size_t tmpsize;
	struct m3uaSgpTable_data *StorageTmp;
	struct header_complex_index *hcindex;

	DEBUGMSGTL(("m3uaSgpMIB", "store_m3uaSgpTable: storing data...  "));
	refresh_m3uaSgpTable(1);
	(void) tmpsize;
	for (hcindex = m3uaSgpTableStorage; hcindex != NULL; hcindex = hcindex->next) {
		StorageTmp = (struct m3uaSgpTable_data *) hcindex->data;
		/* XXX: comment entire section if row not persistent */
		{
			memset(line, 0, sizeof(line));
			strcat(line, "m3uaSgpTable ");
			cptr = line + strlen(line);
			/* XXX: remove individual columns if not persistent */
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->m3uaSgpAgIndex, &tmpsize);
			cptr = read_config_store_data(ASN_UNSIGNED, cptr, &StorageTmp->m3uaSgpSgIndex, &tmpsize);
			snmpd_store_config(line);
		}
	}
	DEBUGMSGTL(("m3uaSgpMIB", "done.\n"));
	return SNMPERR_SUCCESS;
}

/**
 * @fn void refresh_m3uaSgpAgTable_row(struct m3uaSgpAgTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the m3uaSgpAgTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct m3uaSgpAgTable_data *
refresh_m3uaSgpAgTable_row(struct m3uaSgpAgTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->m3uaSgpAgTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->m3uaSgpAgTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_m3uaSgpAgTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the m3uaSgpAgTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_m3uaSgpAgTable(int force)
{
	if (!force && m3uaSgpAgTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	m3uaSgpAgTable_refresh = 0;
}

/**
 * @fn u_char *var_m3uaSgpAgTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in m3uaSgpAgTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_m3uaSgpMIB above.
 */
u_char *
var_m3uaSgpAgTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct m3uaSgpAgTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("m3uaSgpMIB", "var_m3uaSgpAgTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_m3uaSgpAgTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(m3uaSgpAgTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_m3uaSgpAgTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) M3UASGPAGNAME:	/* Create */
		*write_method = write_m3uaSgpAgName;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->m3uaSgpAgNameLen;
		rval = (u_char *) StorageTmp->m3uaSgpAgName;
		break;
	case (u_char) M3UASGPAGPROTOCOLVERSION:	/* Create */
		*write_method = write_m3uaSgpAgProtocolVersion;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->m3uaSgpAgProtocolVersionLen * sizeof(oid);
		rval = (u_char *) StorageTmp->m3uaSgpAgProtocolVersion;
		break;
	case (u_char) M3UASGPAGOPTIONS:	/* Create */
		*write_method = write_m3uaSgpAgOptions;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->m3uaSgpAgOptionsLen;
		rval = (u_char *) StorageTmp->m3uaSgpAgOptions;
		break;
	case (u_char) M3UASGPAGREGISTRATIONPOLICY:	/* Create */
		*write_method = write_m3uaSgpAgRegistrationPolicy;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m3uaSgpAgRegistrationPolicy);
		rval = (u_char *) &StorageTmp->m3uaSgpAgRegistrationPolicy;
		break;
	case (u_char) M3UASGPAGASPIDPOLICY:	/* Create */
		*write_method = write_m3uaSgpAgAspIdPolicy;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m3uaSgpAgAspIdPolicy);
		rval = (u_char *) &StorageTmp->m3uaSgpAgAspIdPolicy;
		break;
	case (u_char) M3UASGPAGPROTOCOLPAYLOADID:	/* Create */
		*write_method = write_m3uaSgpAgProtocolPayloadId;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m3uaSgpAgProtocolPayloadId);
		rval = (u_char *) &StorageTmp->m3uaSgpAgProtocolPayloadId;
		break;
	case (u_char) M3UASGPAGIPPORT:	/* Create */
		*write_method = write_m3uaSgpAgIpPort;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m3uaSgpAgIpPort);
		rval = (u_char *) &StorageTmp->m3uaSgpAgIpPort;
		break;
	case (u_char) M3UASGPAGMINOSTREAMS:	/* Create */
		*write_method = write_m3uaSgpAgMinOstreams;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m3uaSgpAgMinOstreams);
		rval = (u_char *) &StorageTmp->m3uaSgpAgMinOstreams;
		break;
	case (u_char) M3UASGPAGMAXISTREAMS:	/* Create */
		*write_method = write_m3uaSgpAgMaxIstreams;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m3uaSgpAgMaxIstreams);
		rval = (u_char *) &StorageTmp->m3uaSgpAgMaxIstreams;
		break;
	case (u_char) M3UASGPAGSTATUS:	/* Create */
		*write_method = write_m3uaSgpAgStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m3uaSgpAgStatus);
		rval = (u_char *) &StorageTmp->m3uaSgpAgStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn void refresh_m3uaSgpSgTable_row(struct m3uaSgpSgTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the m3uaSgpSgTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct m3uaSgpSgTable_data *
refresh_m3uaSgpSgTable_row(struct m3uaSgpSgTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->m3uaSgpSgTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->m3uaSgpSgTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_m3uaSgpSgTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the m3uaSgpSgTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_m3uaSgpSgTable(int force)
{
	if (!force && m3uaSgpSgTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	m3uaSgpSgTable_refresh = 0;
}

/**
 * @fn u_char *var_m3uaSgpSgTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in m3uaSgpSgTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_m3uaSgpMIB above.
 */
u_char *
var_m3uaSgpSgTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct m3uaSgpSgTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("m3uaSgpMIB", "var_m3uaSgpSgTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_m3uaSgpSgTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(m3uaSgpSgTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_m3uaSgpSgTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) M3UASGPSGNAME:	/* Create */
		*write_method = write_m3uaSgpSgName;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->m3uaSgpSgNameLen;
		rval = (u_char *) StorageTmp->m3uaSgpSgName;
		break;
	case (u_char) M3UASGPSGSTATUS:	/* Create */
		*write_method = write_m3uaSgpSgStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m3uaSgpSgStatus);
		rval = (u_char *) &StorageTmp->m3uaSgpSgStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn void refresh_m3uaSgpAspTable_row(struct m3uaSgpAspTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the m3uaSgpAspTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct m3uaSgpAspTable_data *
refresh_m3uaSgpAspTable_row(struct m3uaSgpAspTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->m3uaSgpAspTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->m3uaSgpAspTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_m3uaSgpAspTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the m3uaSgpAspTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_m3uaSgpAspTable(int force)
{
	if (!force && m3uaSgpAspTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	m3uaSgpAspTable_refresh = 0;
}

/**
 * @fn u_char *var_m3uaSgpAspTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in m3uaSgpAspTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_m3uaSgpMIB above.
 */
u_char *
var_m3uaSgpAspTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct m3uaSgpAspTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("m3uaSgpMIB", "var_m3uaSgpAspTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_m3uaSgpAspTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(m3uaSgpAspTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_m3uaSgpAspTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	case (u_char) M3UASGPASPNAME:	/* Create */
		*write_method = write_m3uaSgpAspName;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->m3uaSgpAspNameLen;
		rval = (u_char *) StorageTmp->m3uaSgpAspName;
		break;
	case (u_char) M3UASGPASPOPERATIONALSTATE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m3uaSgpAspOperationalState);
		rval = (u_char *) &StorageTmp->m3uaSgpAspOperationalState;
		break;
	case (u_char) M3UASGPASPUSAGESTATE:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m3uaSgpAspUsageState);
		rval = (u_char *) &StorageTmp->m3uaSgpAspUsageState;
		break;
	case (u_char) M3UASGPASPADMINISTRATIVESTATE:	/* Create */
		*write_method = write_m3uaSgpAspAdministrativeState;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m3uaSgpAspAdministrativeState);
		rval = (u_char *) &StorageTmp->m3uaSgpAspAdministrativeState;
		break;
	case (u_char) M3UASGPASPALARMSTATUS:	/* Create */
		*write_method = write_m3uaSgpAspAlarmStatus;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->m3uaSgpAspAlarmStatusLen;
		rval = (u_char *) StorageTmp->m3uaSgpAspAlarmStatus;
		break;
	case (u_char) M3UASGPASPPROCEDURALSTATUS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->m3uaSgpAspProceduralStatusLen;
		rval = (u_char *) StorageTmp->m3uaSgpAspProceduralStatus;
		break;
	case (u_char) M3UASGPASPAVAILABILITYSTATUS:	/* ReadOnly */
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->m3uaSgpAspAvailabilityStatusLen;
		rval = (u_char *) StorageTmp->m3uaSgpAspAvailabilityStatus;
		break;
	case (u_char) M3UASGPASPASPSTATE:	/* Create */
		*write_method = write_m3uaSgpAspAspState;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m3uaSgpAspAspState);
		rval = (u_char *) &StorageTmp->m3uaSgpAspAspState;
		break;
	case (u_char) M3UASGPASPSCTPPROFILE:	/* Create */
		*write_method = write_m3uaSgpAspSctpProfile;
		if (!StorageTmp)
			break;
		*var_len = StorageTmp->m3uaSgpAspSctpProfileLen * sizeof(oid);
		rval = (u_char *) StorageTmp->m3uaSgpAspSctpProfile;
		break;
	case (u_char) M3UASGPASPMAXINITRETRIES:	/* Create */
		*write_method = write_m3uaSgpAspMaxInitRetries;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m3uaSgpAspMaxInitRetries);
		rval = (u_char *) &StorageTmp->m3uaSgpAspMaxInitRetries;
		break;
	case (u_char) M3UASGPASPMAXPATHRETRANS:	/* Create */
		*write_method = write_m3uaSgpAspMaxPathRetrans;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m3uaSgpAspMaxPathRetrans);
		rval = (u_char *) &StorageTmp->m3uaSgpAspMaxPathRetrans;
		break;
	case (u_char) M3UASGPASPRTOMIN:	/* Create */
		*write_method = write_m3uaSgpAspRtoMin;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m3uaSgpAspRtoMin);
		rval = (u_char *) &StorageTmp->m3uaSgpAspRtoMin;
		break;
	case (u_char) M3UASGPASPRTOMAX:	/* Create */
		*write_method = write_m3uaSgpAspRtoMax;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m3uaSgpAspRtoMax);
		rval = (u_char *) &StorageTmp->m3uaSgpAspRtoMax;
		break;
	case (u_char) M3UASGPASPHEARTBEATINTERVAL:	/* Create */
		*write_method = write_m3uaSgpAspHeartbeatInterval;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m3uaSgpAspHeartbeatInterval);
		rval = (u_char *) &StorageTmp->m3uaSgpAspHeartbeatInterval;
		break;
	case (u_char) M3UASGPASPMAXLIFETIME:	/* Create */
		*write_method = write_m3uaSgpAspMaxLifeTime;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m3uaSgpAspMaxLifeTime);
		rval = (u_char *) &StorageTmp->m3uaSgpAspMaxLifeTime;
		break;
	case (u_char) M3UASGPASPTIMERDIVERT:	/* Create */
		*write_method = write_m3uaSgpAspTimerDivert;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m3uaSgpAspTimerDivert);
		rval = (u_char *) &StorageTmp->m3uaSgpAspTimerDivert;
		break;
	case (u_char) M3UASGPASPSTATUS:	/* Create */
		*write_method = write_m3uaSgpAspStatus;
		if (!StorageTmp)
			break;
		*var_len = sizeof(StorageTmp->m3uaSgpAspStatus);
		rval = (u_char *) &StorageTmp->m3uaSgpAspStatus;
		break;
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn void refresh_m3uaSgpTable_row(struct m3uaSgpTable_data *StorageTmp, int force)
 * @param StorageTmp the data row to refresh.
 * @param force force refresh if non-zero.
 * @brief refresh the contents of the m3uaSgpTable row.
 *
 * Normally the values retrieved from the operating system are cached.  However, if a row contains
 * temporal values, such as statistics counters, gauges, timestamps, or other transient columns, it
 * may be necessary to refresh the row on some other basis, but normally only once per request.
 */
struct m3uaSgpTable_data *
refresh_m3uaSgpTable_row(struct m3uaSgpTable_data *StorageTmp, int force)
{
	if (!StorageTmp || (!force && StorageTmp->m3uaSgpTable_request == sa_request))
		return (StorageTmp);
	/* XXX: update row; delete it and return NULL if the row has disappeared */
	StorageTmp->m3uaSgpTable_request = sa_request;
	return (StorageTmp);
}

/**
 * @fn void refresh_m3uaSgpTable(int force)
 * @param force force refresh if non-zero.
 * @brief refresh the scalar values of the m3uaSgpTable.
 *
 * Normally the values retrieved from the operating system are cached.  When the agent receives a
 * SIGPOLL from an open STREAMS configuration or administrative driver Stream, the STREAMS subsystem
 * indicates to the agent that the cache has been invalidated and that it should reread scalars and
 * tables from the STREAMS subsystem.  This function is used when the agent starts for the first
 * time, or after a SIGPOLL has been received (and a row or column has been requested).
 */
void
refresh_m3uaSgpTable(int force)
{
	if (!force && m3uaSgpTable_refresh == 0)
		return;
	/* XXX: Here, update the table as required... */
	m3uaSgpTable_refresh = 0;
}

/**
 * @fn u_char *var_m3uaSgpTable(struct variable *vp, oid *name, size_t *length, int exact, size_t *var_len, WriteMethod **write_method)
 * @brief locate variables in m3uaSgpTable.
 *
 * Handle this table separately from the scalar value case.  The workings of this are basically the
 * same as for var_m3uaSgpMIB above.
 */
u_char *
var_m3uaSgpTable(struct variable *vp, oid * name, size_t *length, int exact, size_t *var_len, WriteMethod ** write_method)
{
	struct m3uaSgpTable_data *StorageTmp = NULL;
	u_char *rval;

	DEBUGMSGTL(("m3uaSgpMIB", "var_m3uaSgpTable: Entering...  \n"));
	/* Make sure that the storage data does not need to be refreshed before checking the header. */
	refresh_m3uaSgpTable(0);
	/* This assumes you have registered all your data properly with header_complex_add() somewhere before this. */
	while ((StorageTmp = header_complex(m3uaSgpTableStorage, vp, name, length, exact, var_len, write_method)))
		if ((StorageTmp = refresh_m3uaSgpTable_row(StorageTmp, 0)) || exact)
			break;
	*write_method = NULL;
	*var_len = 0;
	rval = NULL;
	/* This is where we do the value assignments for the mib results. */
	switch (vp->magic) {
	default:
		ERROR_MSG("");
	}
	return (rval);
}

/**
 * @fn int write_m3uaSgpAgName(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m3uaSgpAgName(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static uint8_t *old_value;
	struct m3uaSgpAgTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	static size_t old_length = 0;
	static uint8_t *string = NULL;

	DEBUGMSGTL(("m3uaSgpMIB", "write_m3uaSgpAgName entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(m3uaSgpAgTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */

	switch (action) {
	case RESERVE1:
		string = NULL;
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m3uaSgpAgStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAgName: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAgName not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 0..32 */
		if (var_val_len > SPRINT_MAX_LEN || ((0 > var_val_len || var_val_len > 32))) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAgName: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m3uaSgpAgName for you to use, and you have just been asked to do something with it.  Note that anything done here must
				   be reversable in the UNDO case */
		old_value = StorageTmp->m3uaSgpAgName;
		old_length = StorageTmp->m3uaSgpAgNameLen;
		StorageTmp->m3uaSgpAgName = string;
		StorageTmp->m3uaSgpAgNameLen = var_val_len;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(old_value);
		old_length = 0;
		string = NULL;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m3uaSgpAgName = old_value;
		StorageTmp->m3uaSgpAgNameLen = old_length;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		SNMP_FREE(string);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m3uaSgpAgProtocolVersion(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m3uaSgpAgProtocolVersion(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static oid *old_value;
	struct m3uaSgpAgTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	static size_t old_length = 0;
	static oid *objid = NULL;

	DEBUGMSGTL(("m3uaSgpMIB", "write_m3uaSgpAgProtocolVersion entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(m3uaSgpAgTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */

	switch (action) {
	case RESERVE1:
		objid = NULL;
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m3uaSgpAgStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAgProtocolVersion: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OBJECT_ID) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAgProtocolVersion not ASN_OBJECT_ID\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (MIN_OID_LEN * sizeof(oid) > var_val_len || var_val_len > MAX_OID_LEN * sizeof(oid)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAgProtocolVersion: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value { OPENSS7-SIGTRAN-TC : interoperableVersion } */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((objid = snmp_duplicate_objid((void *) var_val, var_val_len / sizeof(oid))) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m3uaSgpAgProtocolVersion for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		old_value = StorageTmp->m3uaSgpAgProtocolVersion;
		old_length = StorageTmp->m3uaSgpAgProtocolVersionLen;
		StorageTmp->m3uaSgpAgProtocolVersion = objid;
		StorageTmp->m3uaSgpAgProtocolVersionLen = var_val_len / sizeof(oid);
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(old_value);
		old_length = 0;
		objid = NULL;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m3uaSgpAgProtocolVersion = old_value;
		StorageTmp->m3uaSgpAgProtocolVersionLen = old_length;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		SNMP_FREE(objid);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m3uaSgpAgOptions(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m3uaSgpAgOptions(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static uint8_t *old_value;
	struct m3uaSgpAgTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	static size_t old_length = 0;
	static uint8_t *string = NULL;

	DEBUGMSGTL(("m3uaSgpMIB", "write_m3uaSgpAgOptions entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(m3uaSgpAgTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */

	switch (action) {
	case RESERVE1:
		string = NULL;
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m3uaSgpAgStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAgOptions: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_BIT_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAgOptions not ASN_BIT_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (1 > var_val_len || var_val_len > SPRINT_MAX_LEN || var_val_len != 2) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAgOptions: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m3uaSgpAgOptions for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		old_value = StorageTmp->m3uaSgpAgOptions;
		old_length = StorageTmp->m3uaSgpAgOptionsLen;
		StorageTmp->m3uaSgpAgOptions = string;
		StorageTmp->m3uaSgpAgOptionsLen = var_val_len;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(old_value);
		old_length = 0;
		string = NULL;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m3uaSgpAgOptions = old_value;
		StorageTmp->m3uaSgpAgOptionsLen = old_length;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		SNMP_FREE(string);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m3uaSgpAgRegistrationPolicy(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m3uaSgpAgRegistrationPolicy(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m3uaSgpAgTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m3uaSgpMIB", "write_m3uaSgpAgRegistrationPolicy entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(m3uaSgpAgTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */

	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m3uaSgpAgStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAgRegistrationPolicy: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAgRegistrationPolicy not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAgRegistrationPolicy: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 1 */
		switch (set_value) {
		case M3UASGPAGREGISTRATIONPOLICY_UNKNOWN:
		case M3UASGPAGREGISTRATIONPOLICY_FORBIDDEN:
		case M3UASGPAGREGISTRATIONPOLICY_PERMITTED:
		case M3UASGPAGREGISTRATIONPOLICY_REQUIRED:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAgRegistrationPolicy: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m3uaSgpAgRegistrationPolicy for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		old_value = StorageTmp->m3uaSgpAgRegistrationPolicy;
		StorageTmp->m3uaSgpAgRegistrationPolicy = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m3uaSgpAgRegistrationPolicy = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m3uaSgpAgAspIdPolicy(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m3uaSgpAgAspIdPolicy(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m3uaSgpAgTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m3uaSgpMIB", "write_m3uaSgpAgAspIdPolicy entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(m3uaSgpAgTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */

	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m3uaSgpAgStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAgAspIdPolicy: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAgAspIdPolicy not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAgAspIdPolicy: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 1 */
		switch (set_value) {
		case M3UASGPAGASPIDPOLICY_UNKNOWN:
		case M3UASGPAGASPIDPOLICY_PERMITTED:
		case M3UASGPAGASPIDPOLICY_REQUIRED:
		case M3UASGPAGASPIDPOLICY_FORBIDDEN:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAgAspIdPolicy: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m3uaSgpAgAspIdPolicy for you to use, and you have just been asked to do something with it.  Note that anything done here 
				   must be reversable in the UNDO case */
		old_value = StorageTmp->m3uaSgpAgAspIdPolicy;
		StorageTmp->m3uaSgpAgAspIdPolicy = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m3uaSgpAgAspIdPolicy = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m3uaSgpAgProtocolPayloadId(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m3uaSgpAgProtocolPayloadId(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m3uaSgpAgTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m3uaSgpMIB", "write_m3uaSgpAgProtocolPayloadId entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(m3uaSgpAgTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */

	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m3uaSgpAgStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAgProtocolPayloadId: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAgProtocolPayloadId not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAgProtocolPayloadId: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 3 */
		/* Note: ranges 0..0 3..3 */
		if (set_value != 0 && set_value != 3) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAgProtocolPayloadId: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m3uaSgpAgProtocolPayloadId for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		old_value = StorageTmp->m3uaSgpAgProtocolPayloadId;
		StorageTmp->m3uaSgpAgProtocolPayloadId = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m3uaSgpAgProtocolPayloadId = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m3uaSgpAgIpPort(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m3uaSgpAgIpPort(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m3uaSgpAgTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m3uaSgpMIB", "write_m3uaSgpAgIpPort entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(m3uaSgpAgTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */

	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m3uaSgpAgStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAgIpPort: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAgIpPort not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAgIpPort: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 2905 */
		/* Note: ranges 0..16383 */
		if ((0 > set_value || set_value > 16383)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAgIpPort: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m3uaSgpAgIpPort for you to use, and you have just been asked to do something with it.  Note that anything done here must 
				   be reversable in the UNDO case */
		old_value = StorageTmp->m3uaSgpAgIpPort;
		StorageTmp->m3uaSgpAgIpPort = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m3uaSgpAgIpPort = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m3uaSgpAgMinOstreams(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m3uaSgpAgMinOstreams(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m3uaSgpAgTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m3uaSgpMIB", "write_m3uaSgpAgMinOstreams entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(m3uaSgpAgTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */

	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m3uaSgpAgStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAgMinOstreams: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAgMinOstreams not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAgMinOstreams: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 33 */
		/* Note: ranges 2..16384 */
		if ((2 > set_value || set_value > 16384)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAgMinOstreams: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m3uaSgpAgMinOstreams for you to use, and you have just been asked to do something with it.  Note that anything done here 
				   must be reversable in the UNDO case */
		old_value = StorageTmp->m3uaSgpAgMinOstreams;
		StorageTmp->m3uaSgpAgMinOstreams = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m3uaSgpAgMinOstreams = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m3uaSgpAgMaxIstreams(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m3uaSgpAgMaxIstreams(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m3uaSgpAgTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m3uaSgpMIB", "write_m3uaSgpAgMaxIstreams entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(m3uaSgpAgTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */

	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m3uaSgpAgStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAgMaxIstreams: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAgMaxIstreams not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAgMaxIstreams: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 33 */
		/* Note: ranges 2..16384 */
		if ((2 > set_value || set_value > 16384)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAgMaxIstreams: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m3uaSgpAgMaxIstreams for you to use, and you have just been asked to do something with it.  Note that anything done here 
				   must be reversable in the UNDO case */
		old_value = StorageTmp->m3uaSgpAgMaxIstreams;
		StorageTmp->m3uaSgpAgMaxIstreams = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m3uaSgpAgMaxIstreams = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m3uaSgpSgName(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m3uaSgpSgName(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static uint8_t *old_value;
	struct m3uaSgpSgTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	static size_t old_length = 0;
	static uint8_t *string = NULL;

	DEBUGMSGTL(("m3uaSgpMIB", "write_m3uaSgpSgName entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(m3uaSgpSgTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */

	switch (action) {
	case RESERVE1:
		string = NULL;
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m3uaSgpSgStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpSgName: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpSgName not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 0..32 */
		if (var_val_len > SPRINT_MAX_LEN || ((0 > var_val_len || var_val_len > 32))) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpSgName: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m3uaSgpSgName for you to use, and you have just been asked to do something with it.  Note that anything done here must
				   be reversable in the UNDO case */
		old_value = StorageTmp->m3uaSgpSgName;
		old_length = StorageTmp->m3uaSgpSgNameLen;
		StorageTmp->m3uaSgpSgName = string;
		StorageTmp->m3uaSgpSgNameLen = var_val_len;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(old_value);
		old_length = 0;
		string = NULL;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m3uaSgpSgName = old_value;
		StorageTmp->m3uaSgpSgNameLen = old_length;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		SNMP_FREE(string);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m3uaSgpAspName(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m3uaSgpAspName(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static uint8_t *old_value;
	struct m3uaSgpAspTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	static size_t old_length = 0;
	static uint8_t *string = NULL;

	DEBUGMSGTL(("m3uaSgpMIB", "write_m3uaSgpAspName entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(m3uaSgpAspTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */

	switch (action) {
	case RESERVE1:
		string = NULL;
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m3uaSgpAspStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAspName: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OCTET_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAspName not ASN_OCTET_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		/* Note: ranges 0..32 */
		if (var_val_len > SPRINT_MAX_LEN || ((0 > var_val_len || var_val_len > 32))) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAspName: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m3uaSgpAspName for you to use, and you have just been asked to do something with it.  Note that anything done here must
				   be reversable in the UNDO case */
		old_value = StorageTmp->m3uaSgpAspName;
		old_length = StorageTmp->m3uaSgpAspNameLen;
		StorageTmp->m3uaSgpAspName = string;
		StorageTmp->m3uaSgpAspNameLen = var_val_len;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(old_value);
		old_length = 0;
		string = NULL;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m3uaSgpAspName = old_value;
		StorageTmp->m3uaSgpAspNameLen = old_length;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		SNMP_FREE(string);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m3uaSgpAspAdministrativeState(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m3uaSgpAspAdministrativeState(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m3uaSgpAspTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m3uaSgpMIB", "write_m3uaSgpAspAdministrativeState entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(m3uaSgpAspTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */

	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m3uaSgpAspStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAspAdministrativeState: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAspAdministrativeState not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAspAdministrativeState: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 0 */
		switch (set_value) {
		case M3UASGPASPADMINISTRATIVESTATE_LOCKED:
		case M3UASGPASPADMINISTRATIVESTATE_UNLOCKED:
		case M3UASGPASPADMINISTRATIVESTATE_SHUTTINGDOWN:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAspAdministrativeState: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m3uaSgpAspAdministrativeState for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		old_value = StorageTmp->m3uaSgpAspAdministrativeState;
		StorageTmp->m3uaSgpAspAdministrativeState = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m3uaSgpAspAdministrativeState = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m3uaSgpAspAlarmStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m3uaSgpAspAlarmStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static uint8_t *old_value;
	struct m3uaSgpAspTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	static size_t old_length = 0;
	static uint8_t *string = NULL;

	DEBUGMSGTL(("m3uaSgpMIB", "write_m3uaSgpAspAlarmStatus entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(m3uaSgpAspTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */

	switch (action) {
	case RESERVE1:
		string = NULL;
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m3uaSgpAspStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAspAlarmStatus: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_BIT_STR) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAspAlarmStatus not ASN_BIT_STR\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (1 > var_val_len || var_val_len > SPRINT_MAX_LEN || var_val_len != 1) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAspAlarmStatus: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((string = malloc(var_val_len + 1)) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		memcpy((void *) string, (void *) var_val, var_val_len);
		string[var_val_len] = 0;
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m3uaSgpAspAlarmStatus for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		old_value = StorageTmp->m3uaSgpAspAlarmStatus;
		old_length = StorageTmp->m3uaSgpAspAlarmStatusLen;
		StorageTmp->m3uaSgpAspAlarmStatus = string;
		StorageTmp->m3uaSgpAspAlarmStatusLen = var_val_len;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(old_value);
		old_length = 0;
		string = NULL;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m3uaSgpAspAlarmStatus = old_value;
		StorageTmp->m3uaSgpAspAlarmStatusLen = old_length;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		SNMP_FREE(string);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m3uaSgpAspAspState(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m3uaSgpAspAspState(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m3uaSgpAspTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m3uaSgpMIB", "write_m3uaSgpAspAspState entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(m3uaSgpAspTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */

	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m3uaSgpAspStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAspAspState: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAspAspState not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAspAspState: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value 0 */
		switch (set_value) {
		case M3UASGPASPASPSTATE_DOWN:
		case M3UASGPASPASPSTATE_INITIALIZING:
		case M3UASGPASPASPSTATE_TERMINATING:
		case M3UASGPASPASPSTATE_UP:
			break;
		default:
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAspAspState: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m3uaSgpAspAspState for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		old_value = StorageTmp->m3uaSgpAspAspState;
		StorageTmp->m3uaSgpAspAspState = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m3uaSgpAspAspState = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m3uaSgpAspSctpProfile(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m3uaSgpAspSctpProfile(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static oid *old_value;
	struct m3uaSgpAspTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	static size_t old_length = 0;
	static oid *objid = NULL;

	DEBUGMSGTL(("m3uaSgpMIB", "write_m3uaSgpAspSctpProfile entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(m3uaSgpAspTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */

	switch (action) {
	case RESERVE1:
		objid = NULL;
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m3uaSgpAspStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAspSctpProfile: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_OBJECT_ID) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAspSctpProfile not ASN_OBJECT_ID\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (MIN_OID_LEN * sizeof(oid) > var_val_len || var_val_len > MAX_OID_LEN * sizeof(oid)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAspSctpProfile: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: default value { zeroDotZero } */
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if ((objid = snmp_duplicate_objid((void *) var_val, var_val_len / sizeof(oid))) == NULL)
			return SNMP_ERR_RESOURCEUNAVAILABLE;
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m3uaSgpAspSctpProfile for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		old_value = StorageTmp->m3uaSgpAspSctpProfile;
		old_length = StorageTmp->m3uaSgpAspSctpProfileLen;
		StorageTmp->m3uaSgpAspSctpProfile = objid;
		StorageTmp->m3uaSgpAspSctpProfileLen = var_val_len / sizeof(oid);
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		SNMP_FREE(old_value);
		old_length = 0;
		objid = NULL;
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m3uaSgpAspSctpProfile = old_value;
		StorageTmp->m3uaSgpAspSctpProfileLen = old_length;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		SNMP_FREE(objid);
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m3uaSgpAspMaxInitRetries(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m3uaSgpAspMaxInitRetries(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static ulong old_value;
	struct m3uaSgpAspTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	ulong set_value = *((ulong *) var_val);

	DEBUGMSGTL(("m3uaSgpMIB", "write_m3uaSgpAspMaxInitRetries entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(m3uaSgpAspTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */

	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m3uaSgpAspStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAspMaxInitRetries: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAspMaxInitRetries not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAspMaxInitRetries: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m3uaSgpAspMaxInitRetries for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		old_value = StorageTmp->m3uaSgpAspMaxInitRetries;
		StorageTmp->m3uaSgpAspMaxInitRetries = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m3uaSgpAspMaxInitRetries = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m3uaSgpAspMaxPathRetrans(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m3uaSgpAspMaxPathRetrans(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static ulong old_value;
	struct m3uaSgpAspTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	ulong set_value = *((ulong *) var_val);

	DEBUGMSGTL(("m3uaSgpMIB", "write_m3uaSgpAspMaxPathRetrans entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(m3uaSgpAspTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */

	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m3uaSgpAspStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAspMaxPathRetrans: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_UNSIGNED) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAspMaxPathRetrans not ASN_UNSIGNED\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(uint32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAspMaxPathRetrans: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m3uaSgpAspMaxPathRetrans for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		old_value = StorageTmp->m3uaSgpAspMaxPathRetrans;
		StorageTmp->m3uaSgpAspMaxPathRetrans = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m3uaSgpAspMaxPathRetrans = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m3uaSgpAspRtoMin(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m3uaSgpAspRtoMin(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m3uaSgpAspTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m3uaSgpMIB", "write_m3uaSgpAspRtoMin entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(m3uaSgpAspTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */

	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m3uaSgpAspStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAspRtoMin: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAspRtoMin not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAspRtoMin: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAspRtoMin: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m3uaSgpAspRtoMin for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		old_value = StorageTmp->m3uaSgpAspRtoMin;
		StorageTmp->m3uaSgpAspRtoMin = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m3uaSgpAspRtoMin = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m3uaSgpAspRtoMax(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m3uaSgpAspRtoMax(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m3uaSgpAspTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m3uaSgpMIB", "write_m3uaSgpAspRtoMax entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(m3uaSgpAspTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */

	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m3uaSgpAspStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAspRtoMax: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAspRtoMax not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAspRtoMax: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAspRtoMax: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m3uaSgpAspRtoMax for you to use, and you have just been asked to do something with it.  Note that anything done here
				   must be reversable in the UNDO case */
		old_value = StorageTmp->m3uaSgpAspRtoMax;
		StorageTmp->m3uaSgpAspRtoMax = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m3uaSgpAspRtoMax = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m3uaSgpAspHeartbeatInterval(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m3uaSgpAspHeartbeatInterval(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m3uaSgpAspTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m3uaSgpMIB", "write_m3uaSgpAspHeartbeatInterval entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(m3uaSgpAspTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */

	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m3uaSgpAspStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAspHeartbeatInterval: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAspHeartbeatInterval not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAspHeartbeatInterval: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAspHeartbeatInterval: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m3uaSgpAspHeartbeatInterval for you to use, and you have just been asked to do something with it.  Note that anything
				   done here must be reversable in the UNDO case */
		old_value = StorageTmp->m3uaSgpAspHeartbeatInterval;
		StorageTmp->m3uaSgpAspHeartbeatInterval = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m3uaSgpAspHeartbeatInterval = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m3uaSgpAspMaxLifeTime(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m3uaSgpAspMaxLifeTime(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m3uaSgpAspTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m3uaSgpMIB", "write_m3uaSgpAspMaxLifeTime entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(m3uaSgpAspTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */

	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m3uaSgpAspStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAspMaxLifeTime: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAspMaxLifeTime not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAspMaxLifeTime: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAspMaxLifeTime: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m3uaSgpAspMaxLifeTime for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		old_value = StorageTmp->m3uaSgpAspMaxLifeTime;
		StorageTmp->m3uaSgpAspMaxLifeTime = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m3uaSgpAspMaxLifeTime = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m3uaSgpAspTimerDivert(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Table row and column write routine.
 */
int
write_m3uaSgpAspTimerDivert(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	static long old_value;
	struct m3uaSgpAspTable_data *StorageTmp = NULL;
	size_t newlen = name_len - 15;
	long set_value = *((long *) var_val);

	DEBUGMSGTL(("m3uaSgpMIB", "write_m3uaSgpAspTimerDivert entering action=%d...  \n", action));
	if ((StorageTmp = header_complex(m3uaSgpAspTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL)) == NULL)
		return SNMP_ERR_NOSUCHNAME;	/* remove if you support creation here */

	switch (action) {
	case RESERVE1:
		if (StorageTmp != NULL && statP == NULL) {
			/* have row but no column */
			switch (StorageTmp->m3uaSgpAspStatus) {
			case RS_ACTIVE:
				/* cannot create non-existent column while active */
				snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAspTimerDivert: but column non-existent\n");
				return SNMP_ERR_INCONSISTENTVALUE;
			case RS_NOTINSERVICE:
			case RS_NOTREADY:
				/* assume column can be created */
				break;
			}
		}
		if (var_val_type != ASN_INTEGER) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAspTimerDivert not ASN_INTEGER\n");
			return SNMP_ERR_WRONGTYPE;
		}
		if (var_val_len > sizeof(int32_t)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAspTimerDivert: bad length\n");
			return SNMP_ERR_WRONGLENGTH;
		}
		/* Note: ranges 0..2147483647 */
		if ((0 > set_value || set_value > 2147483647)) {
			snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAspTimerDivert: bad value\n");
			return SNMP_ERR_WRONGVALUE;
		}
		break;
	case RESERVE2:		/* memory reseveration, final preparation... */
		if (StorageTmp == NULL)
			return SNMP_ERR_NOSUCHNAME;
		break;
	case ACTION:		/* The variable has been stored in StorageTmp->m3uaSgpAspTimerDivert for you to use, and you have just been asked to do something with it.  Note that anything done
				   here must be reversable in the UNDO case */
		old_value = StorageTmp->m3uaSgpAspTimerDivert;
		StorageTmp->m3uaSgpAspTimerDivert = set_value;
		break;
	case COMMIT:		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		break;
	case UNDO:		/* Back out any changes made in the ACTION case */
		StorageTmp->m3uaSgpAspTimerDivert = old_value;
		/* fall through */
	case FREE:		/* Release any resources that have been allocated */
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int m3uaSgpAgTable_consistent(struct m3uaSgpAgTable_data *thedata)
 * @param thedata the row data to check for consistency.
 * @brief check the internal consistency of a table row.
 *
 * This function checks the internal consistency of a table row for the m3uaSgpAgTable table.  If the
 * table row is internally consistent, then this function returns true (1), otherwise the function
 * returns false (0) and it will not be possible to activate the row until the row's internal
 * consistency is corrected.
 */
int
m3uaSgpAgTable_consistent(struct m3uaSgpAgTable_data *thedata)
{
	/* XXX: check row consistency return true(1) if consistent, or false(0) if not. */
	return (1);
}

/**
 * @fn int m3uaSgpSgTable_consistent(struct m3uaSgpSgTable_data *thedata)
 * @param thedata the row data to check for consistency.
 * @brief check the internal consistency of a table row.
 *
 * This function checks the internal consistency of a table row for the m3uaSgpSgTable table.  If the
 * table row is internally consistent, then this function returns true (1), otherwise the function
 * returns false (0) and it will not be possible to activate the row until the row's internal
 * consistency is corrected.
 */
int
m3uaSgpSgTable_consistent(struct m3uaSgpSgTable_data *thedata)
{
	/* XXX: check row consistency return true(1) if consistent, or false(0) if not. */
	return (1);
}

/**
 * @fn int m3uaSgpAspTable_consistent(struct m3uaSgpAspTable_data *thedata)
 * @param thedata the row data to check for consistency.
 * @brief check the internal consistency of a table row.
 *
 * This function checks the internal consistency of a table row for the m3uaSgpAspTable table.  If the
 * table row is internally consistent, then this function returns true (1), otherwise the function
 * returns false (0) and it will not be possible to activate the row until the row's internal
 * consistency is corrected.
 */
int
m3uaSgpAspTable_consistent(struct m3uaSgpAspTable_data *thedata)
{
	/* XXX: check row consistency return true(1) if consistent, or false(0) if not. */
	return (1);
}

/**
 * @fn int m3uaSgpTable_consistent(struct m3uaSgpTable_data *thedata)
 * @param thedata the row data to check for consistency.
 * @brief check the internal consistency of a table row.
 *
 * This function checks the internal consistency of a table row for the m3uaSgpTable table.  If the
 * table row is internally consistent, then this function returns true (1), otherwise the function
 * returns false (0) and it will not be possible to activate the row until the row's internal
 * consistency is corrected.
 */
int
m3uaSgpTable_consistent(struct m3uaSgpTable_data *thedata)
{
	/* XXX: check row consistency return true(1) if consistent, or false(0) if not. */
	return (1);
}

/**
 * @fn int write_m3uaSgpAgStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_m3uaSgpAgStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct m3uaSgpAgTable_data *StorageTmp = NULL;
	static struct m3uaSgpAgTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 15;
	static int old_value;
	int set_value;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("m3uaSgpMIB", "write_m3uaSgpAgStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(m3uaSgpAgTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAgStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAgStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->m3uaSgpAgStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->m3uaSgpAgTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_DESTROY:
			/* destroying existent or non-existent row is ok */
			if (StorageTmp == NULL)
				break;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->m3uaSgpAgTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			vars = NULL;
			/* m3uaSgpAgIndex */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[15]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* m3uaSgpAgIndex */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index m3uaSgpAgIndex: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = m3uaSgpAgTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			vp = vars;
			StorageNew->m3uaSgpAgIndex = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&m3uaSgpAgTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_DESTROY:
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				m3uaSgpAgTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		}
		break;
	case ACTION:
		/* The variable has been stored in set_value for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in the UNDO case */
		switch (set_value) {
		case RS_ACTIVE:
			old_value = StorageTmp->m3uaSgpAgStatus;
			StorageTmp->m3uaSgpAgStatus = set_value;
			if (old_value != RS_ACTIVE) {
				/* check that activation is possible */
				if (!m3uaSgpAgTable_consistent(StorageTmp)) {
					StorageTmp->m3uaSgpAgStatus = old_value;
					return SNMP_ERR_INCONSISTENTVALUE;
				}
			}
			break;
		case RS_NOTINSERVICE:
			/* set the flag? */
			old_value = StorageTmp->m3uaSgpAgStatus;
			StorageTmp->m3uaSgpAgStatus = set_value;
			break;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* row creation, set final state */
			/* check if row is ready, otherwise leave at RS_NOTREADY */
			if (m3uaSgpAgTable_consistent(StorageNew)) {
				/* XXX: commit creation to underlying device */
				/* XXX: activate with underlying device */
				StorageNew->m3uaSgpAgStatus = RS_ACTIVE;
			}
			break;
		case RS_CREATEANDWAIT:
			/* row creation, set final state */
			/* check if row is ready, otherwise leave at RS_NOTREADY */
			if (m3uaSgpAgTable_consistent(StorageNew)) {
				/* XXX: commit creation to underlying device, inactive */
				StorageNew->m3uaSgpAgStatus = RS_NOTINSERVICE;
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (old_value != set_value) {
				switch (set_value) {
				case RS_ACTIVE:
					/* XXX: activate with underlying device */
					break;
				case RS_NOTINSERVICE:
					/* XXX: deactivate with underlying device */
					break;
				}
			}
			break;
		case RS_DESTROY:
			/* row deletion, free it its dead */
			m3uaSgpAgTable_destroy(&StorageDel);
			/* m3uaSgpAgTable_destroy() can handle NULL pointers. */
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* restore state */
			StorageTmp->m3uaSgpAgStatus = old_value;
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				m3uaSgpAgTable_del(StorageNew);
				m3uaSgpAgTable_destroy(&StorageNew);
			}
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				m3uaSgpAgTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m3uaSgpSgStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_m3uaSgpSgStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct m3uaSgpSgTable_data *StorageTmp = NULL;
	static struct m3uaSgpSgTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 15;
	static int old_value;
	int set_value;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("m3uaSgpMIB", "write_m3uaSgpSgStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(m3uaSgpSgTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpSgStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpSgStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->m3uaSgpSgStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->m3uaSgpSgTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_DESTROY:
			/* destroying existent or non-existent row is ok */
			if (StorageTmp == NULL)
				break;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->m3uaSgpSgTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			vars = NULL;
			/* m3uaSgpAgIndex */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[15]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* m3uaSgpAgIndex */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index m3uaSgpAgIndex: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = m3uaSgpSgTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			vp = vars;
			StorageNew->m3uaSgpAgIndex = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&m3uaSgpSgTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_DESTROY:
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				m3uaSgpSgTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		}
		break;
	case ACTION:
		/* The variable has been stored in set_value for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in the UNDO case */
		switch (set_value) {
		case RS_ACTIVE:
			old_value = StorageTmp->m3uaSgpSgStatus;
			StorageTmp->m3uaSgpSgStatus = set_value;
			if (old_value != RS_ACTIVE) {
				/* check that activation is possible */
				if (!m3uaSgpSgTable_consistent(StorageTmp)) {
					StorageTmp->m3uaSgpSgStatus = old_value;
					return SNMP_ERR_INCONSISTENTVALUE;
				}
			}
			break;
		case RS_NOTINSERVICE:
			/* set the flag? */
			old_value = StorageTmp->m3uaSgpSgStatus;
			StorageTmp->m3uaSgpSgStatus = set_value;
			break;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* row creation, set final state */
			/* check if row is ready, otherwise leave at RS_NOTREADY */
			if (m3uaSgpSgTable_consistent(StorageNew)) {
				/* XXX: commit creation to underlying device */
				/* XXX: activate with underlying device */
				StorageNew->m3uaSgpSgStatus = RS_ACTIVE;
			}
			break;
		case RS_CREATEANDWAIT:
			/* row creation, set final state */
			/* check if row is ready, otherwise leave at RS_NOTREADY */
			if (m3uaSgpSgTable_consistent(StorageNew)) {
				/* XXX: commit creation to underlying device, inactive */
				StorageNew->m3uaSgpSgStatus = RS_NOTINSERVICE;
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (old_value != set_value) {
				switch (set_value) {
				case RS_ACTIVE:
					/* XXX: activate with underlying device */
					break;
				case RS_NOTINSERVICE:
					/* XXX: deactivate with underlying device */
					break;
				}
			}
			break;
		case RS_DESTROY:
			/* row deletion, free it its dead */
			m3uaSgpSgTable_destroy(&StorageDel);
			/* m3uaSgpSgTable_destroy() can handle NULL pointers. */
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* restore state */
			StorageTmp->m3uaSgpSgStatus = old_value;
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				m3uaSgpSgTable_del(StorageNew);
				m3uaSgpSgTable_destroy(&StorageNew);
			}
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				m3uaSgpSgTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn int write_m3uaSgpAspStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid *name, size_t name_len)
 * @param action the stage of the SET operation.
 * @param var_val pointer to the varbind value.
 * @param var_val_type the ASN type.
 * @param var_val_len the length of the varbind value.
 * @param statP static pointer.
 * @param name the varbind OID.
 * @param name_len number of elements in OID.
 * @brief Row status write routine.
 */
int
write_m3uaSgpAspStatus(int action, u_char *var_val, u_char var_val_type, size_t var_val_len, u_char *statP, oid * name, size_t name_len)
{
	struct m3uaSgpAspTable_data *StorageTmp = NULL;
	static struct m3uaSgpAspTable_data *StorageNew, *StorageDel;
	size_t newlen = name_len - 15;
	static int old_value;
	int set_value;
	static struct variable_list *vars, *vp;

	DEBUGMSGTL(("m3uaSgpMIB", "write_m3uaSgpAspStatus entering action=%d...  \n", action));
	StorageTmp = header_complex(m3uaSgpAspTableStorage, NULL, &name[15], &newlen, 1, NULL, NULL);
	if (var_val_type != ASN_INTEGER || var_val == NULL) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAspStatus not ASN_INTEGER\n");
		return SNMP_ERR_WRONGTYPE;
	}
	set_value = *((long *) var_val);
	/* check legal range, and notReady is reserved for us, not a user */
	if (set_value < 1 || set_value > 6 || set_value == RS_NOTREADY) {
		snmp_log(MY_FACILITY(LOG_NOTICE), "write to m3uaSgpAspStatus: bad value\n");
		return SNMP_ERR_WRONGVALUE;
	}
	switch (action) {
	case RESERVE1:
		/* stage one: test validity */
		StorageNew = StorageDel = NULL;
		vars = vp = NULL;
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			if (StorageTmp != NULL)
				/* cannot create existing row */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			if (StorageTmp == NULL)
				/* cannot change state of non-existent row */
				return SNMP_ERR_INCONSISTENTVALUE;
			if (StorageTmp->m3uaSgpAspStatus == RS_NOTREADY)
				/* cannot change state of row that is not ready */
				return SNMP_ERR_INCONSISTENTVALUE;
			/* XXX: interaction with row storage type needed */
			if (set_value == RS_NOTINSERVICE && StorageTmp->m3uaSgpAspTable_refs > 0)
				/* row is busy and cannot be moved to the RS_NOTINSERVICE state */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_DESTROY:
			/* destroying existent or non-existent row is ok */
			if (StorageTmp == NULL)
				break;
			/* XXX: interaction with row storage type needed */
			if (StorageTmp->m3uaSgpAspTable_refs > 0)
				/* row is busy and cannot be deleted */
				return SNMP_ERR_INCONSISTENTVALUE;
			break;
		case RS_NOTREADY:
			/* management station cannot set this, only agent can */
		default:
			return SNMP_ERR_INCONSISTENTVALUE;
		}
		break;
	case RESERVE2:
		/* memory reseveration, final preparation... */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			vars = NULL;
			/* m3uaSgpAspIndex */
			if ((vp = snmp_varlist_add_variable(&vars, NULL, 0, ASN_UNSIGNED, NULL, 0)) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			if (header_complex_parse_oid(&(name[15]), newlen, vars) != SNMPERR_SUCCESS) {
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vars;
			/* m3uaSgpAspIndex */
			if (vp->val_len > sizeof(uint32_t)) {
				snmp_log(MY_FACILITY(LOG_NOTICE), "index m3uaSgpAspIndex: bad length\n");
				snmp_free_varbind(vars);
				return SNMP_ERR_INCONSISTENTNAME;
			}
			vp = vp->next_variable;
			if ((StorageNew = m3uaSgpAspTable_create()) == NULL) {
				snmp_free_varbind(vars);
				return SNMP_ERR_RESOURCEUNAVAILABLE;
			}
			vp = vars;
			StorageNew->m3uaSgpAspIndex = (ulong) *vp->val.integer;
			vp = vp->next_variable;
			header_complex_add_data(&m3uaSgpAspTableStorage, vars, StorageNew);	/* frees vars */
			break;
		case RS_DESTROY:
			/* destroy */
			if (StorageTmp != NULL) {
				/* exists, extract it for now */
				StorageDel = StorageTmp;
				m3uaSgpAspTable_del(StorageDel);
			} else {
				StorageDel = NULL;
			}
			break;
		}
		break;
	case ACTION:
		/* The variable has been stored in set_value for you to use, and you have just been asked to do something with it.  Note that anything done here must be reversable in the UNDO case */
		switch (set_value) {
		case RS_ACTIVE:
			old_value = StorageTmp->m3uaSgpAspStatus;
			StorageTmp->m3uaSgpAspStatus = set_value;
			if (old_value != RS_ACTIVE) {
				/* check that activation is possible */
				if (!m3uaSgpAspTable_consistent(StorageTmp)) {
					StorageTmp->m3uaSgpAspStatus = old_value;
					return SNMP_ERR_INCONSISTENTVALUE;
				}
			}
			break;
		case RS_NOTINSERVICE:
			/* set the flag? */
			old_value = StorageTmp->m3uaSgpAspStatus;
			StorageTmp->m3uaSgpAspStatus = set_value;
			break;
		}
		break;
	case COMMIT:
		/* Things are working well, so it's now safe to make the change permanently.  Make sure that anything done here can't fail! */
		switch (set_value) {
		case RS_CREATEANDGO:
			/* row creation, set final state */
			/* check if row is ready, otherwise leave at RS_NOTREADY */
			if (m3uaSgpAspTable_consistent(StorageNew)) {
				/* XXX: commit creation to underlying device */
				/* XXX: activate with underlying device */
				StorageNew->m3uaSgpAspStatus = RS_ACTIVE;
			}
			break;
		case RS_CREATEANDWAIT:
			/* row creation, set final state */
			/* check if row is ready, otherwise leave at RS_NOTREADY */
			if (m3uaSgpAspTable_consistent(StorageNew)) {
				/* XXX: commit creation to underlying device, inactive */
				StorageNew->m3uaSgpAspStatus = RS_NOTINSERVICE;
			}
			break;
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* state change already performed */
			if (old_value != set_value) {
				switch (set_value) {
				case RS_ACTIVE:
					/* XXX: activate with underlying device */
					break;
				case RS_NOTINSERVICE:
					/* XXX: deactivate with underlying device */
					break;
				}
			}
			break;
		case RS_DESTROY:
			/* row deletion, free it its dead */
			m3uaSgpAspTable_destroy(&StorageDel);
			/* m3uaSgpAspTable_destroy() can handle NULL pointers. */
			break;
		}
		break;
	case UNDO:
		/* Back out any changes made in the ACTION case */
		switch (set_value) {
		case RS_ACTIVE:
		case RS_NOTINSERVICE:
			/* restore state */
			StorageTmp->m3uaSgpAspStatus = old_value;
			break;
		}
		/* fall through */
	case FREE:
		/* Release any resources that have been allocated */
		switch (set_value) {
		case RS_CREATEANDGO:
		case RS_CREATEANDWAIT:
			/* creation */
			if (StorageNew) {
				m3uaSgpAspTable_del(StorageNew);
				m3uaSgpAspTable_destroy(&StorageNew);
			}
			break;
		case RS_DESTROY:
			/* row deletion, so add it again */
			if (StorageDel)
				m3uaSgpAspTable_add(StorageDel);
			break;
		}
		break;
	}
	return SNMP_ERR_NOERROR;
}

/**
 * @fn void m3uaSgpMIB_loop_handler(int dummy)
 * @param sig signal number
 * @brief handle event loop interation.
 *
 * This function is registered so that, when operating as a module, snmpd will call it one per event
 * loop interation.  This function is called before the next requst is processed and after the
 * previous request is processed.  Two things are done here:  1) The file descriptor that is used to
 * synchronize the agent with (pseudo-)device drivers is closed.  (Another approach, instead of
 * closing each time, would be to restart a timer each time that a request is made (loop is
 * performed) and if it expires, close the file descriptor).  2) The request number is incremented.
 * Although a request is not generated for each loop of the snmp event loop, it is true that a new
 * request cannot be generated without performing a loop.  Therefore, the sa_request is not the
 * request number but it is a temporally unique identifier for a request.
 */
void
m3uaSgpMIB_loop_handler(int sig)
{
	DEBUGMSGTL(("m3uaSgpMIB", "m3uaSgpMIB_loop_handler: executing loop handler...  "));
	/* close files after each request */
	if (sa_fclose) {
		if (my_fd >= 0) {
			close(my_fd);
			my_fd = -1;
		}
	}
#if defined MASTER
	/* prepare for next request */
	sa_request++;
#endif				/* defined MASTER */
	if (external_signal_scheduled[sig] == 0)
		external_signal_scheduled[sig]--;
	if (m3uaSgpMIBold_signal_handler != NULL)
		(*m3uaSgpMIBold_signal_handler) (sig);
	DEBUGMSGTL(("m3uaSgpMIB", "done.\n"));
}

/**
 * @fn void m3uaSgpMIB_fd_handler(int fd, void *dummy)
 * @param fd file descriptor to read.
 * @param dummy client data passed to registration function (always NULL).
 * @brief handle read event on file descriptor.
 *
 * This read file descriptor handler is normally used for (pseudo-)device drivers that generate
 * statistical collection interval events, alarm events, or other operational measurement events, by
 * placing a message on the read queue of the "event handling" Stream.  Normally this routine
 * would adjust counts in some table or scalars, generate SNMP traps representing on-occurence
 * events, first and interval events, and alarm indications.
 */
void
m3uaSgpMIB_fd_handler(int fd, void *dummy)
{
	DEBUGMSGTL(("m3uaSgpMIB", "m3uaSgpMIB_fd_handler: executing fd handler...  "));
	/* XXX: place actions to handle my_fd here... */
	DEBUGMSGTL(("m3uaSgpMIB", "done.\n"));
	return;
}
